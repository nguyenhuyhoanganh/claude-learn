It's now time to really get started with GitHub Actions and dive into the fundamentals you must know when working with GitHub Actions into the key building blocks and it's important to understand how you can build your own GitHub action workflows and how you can use this GitHub Actions feature. For that, in this course section, we'll dive into the core elements that make up this GitHub Actions feature. We'll dive into these key elements you must know and we will work with workflows, jobs and steps and you will understand what these things are. And of course, we're not just going to do that in boring theory and with slides only. Instead, we'll take a look at a couple of concrete example workflows and demos that help you understand and practice these concepts. Now, for this course section and for this course in general, you need a GitHub account. You can create one for free as shown in the Git-GitHub Crash course section, and you should have a basic understanding of how Git and GitHub works. So if you don't have that, definitely go through that crash course section first.

Now to get started with GitHub Actions, I wanna explore the key elements, the key components or building blocks you must understand when working with this GitHub Actions service or feature. And of course, we'll also see all these things in action in a couple of minutes. But let's take a look at these elements in theory first. And we got basically three main building blocks you must know and understand when working with GitHub Actions. And that would be Workflows, Jobs, and Steps. And workflows, Jobs, and Steps are related like this. You have a code Repository on GitHub that's important because this is a GitHub feature and it is related to repositories on GitHub. So you have such a Git Repository on GitHub and typically you'll be storing some code in there, but it could of course be any kind of project. The important thing just is that workflows are attached to those GitHub repositories. You can add workflows to GitHub repositories and you can add as many workflows as you want to a given repository. So this workflow thing is kind of the first thing you build or you create when setting up an automation process with GitHub Actions. Now such a workflow then includes one or more jobs, and that's the next important building block, jobs. So we have workflows that belong to repositories and then in those workflows we have jobs. Now those jobs then contain one or more steps that will be executed in the order in which they're specified. And these steps to define the actual things that will be done. Like for example, download the code in the first step, install the dependencies in the second step, and run automated tests in the third step. That's what you could do with GitHub Actions and with such a workflow that contains a job with the appropriate steps. And as mentioned, you can have as many workflows as you want, as many jobs per workflow as you want, and as many steps in a job as you want. So workflows, jobs, and steps are important. Now let's take a closer look at these three key building blocks though. As mentioned, workflows are attached to GitHub repositories, and they contain one or more jobs, that's what I already mentioned. Now what's important though is that you build those workflows with those jobs to set up some automated process that should be executed. But of course, it should not be executed all the time, but instead, with GitHub Actions, you assign so-called triggers or events to your workflows. These events define when a given workflow will be executed. For example, you could add an event that requires manual activation of a workflow, but you could also add an event that triggers and executes a workflow whenever a new commit is pushed to a certain branch. Now those jobs are then the things inside of the events that contain the steps that will be executed. Every job defines a so-called runner which is simply the execution environment, the machine and operating system that will be used for executing these steps. And with GitHub Actions, these runners can either be runners that are predefined by GitHub, they offer runners for Linux, Mac OS, and Windows, but you could also configure your own runners, but I'll get back to that later. Now, these jobs then also define the steps that will be executed in this runner environment, so on that machine you specified. And as mentioned before, you can have one or multiple jobs in a given Workflow. And if you have multiple jobs, they run in parallel by default, but you can also configure them to run in sequential order, so one job after another. You can also set up conditional jobs which will not always run, but which instead need a certain condition to be met. Now the part where the actual work happens or where you define what should be done, to be precise, are the steps. Steps belong to jobs, and a job can have one or more steps. And a step is either a shell script, a command in the command line that should be executed, or an action, which is another important building block, we'll take a closer look at later. Basically, actions in the context of GitHub Action are predefined scripts that perform a certain task you could say. And you can build your own actions if you need to or use third party actions, but for simple tasks, you can, as mentioned, also just execute a command in the command line, and you'll see all these things in action throughout the course, of course. Now a job must at least have one step, typically, you'll have more than one step though, and those steps are then executed in order, they don't run in parallel, but instead, step after step, and they can also be conditional. Now that's the theory. Let's now take a look at all these things in practice.

GitHub Actions: Availability & Pricing In public repositories, you can use GitHub Actions for free. For private repositories, only a certain amount of monthly usage is available for free - extra usage on top must be paid. The exact quotas and payment details depend on your GitHub plan, a detailed summary can be found here: https://docs.github.com/en/billing/managing-billing-for-github-actions/about-billing-for-github-actions If you can't find an "Actions" tab in your GitHub repository, you can should enable them as described here: https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/enabling-features-for-your-repository/managing-github-actions-settings-for-a-repository

Now, to take a look at GitHub Actions in well, action, I'm back here on GitHub in one of these dummy accounts I created in the crash course section, but of course, any GitHub account will do. I just wanna note that this is a GitHub account that uses the free plan. So everything I'm showing to you here works in that free plan. Now, to get started with actions, I'll create a new GitHub repository. And I'll name this gh-first-action. The name, of course, is totally up to and does not matter. I'll also keep this as a public repository. And I will indeed add a README file here so that we have some content in this repository right from the start because I will not connect this repository to some existing local Git repository. Instead for this first action, we'll stay on GitHub. We will soon switch to a local repository again and connect that to GitHub but for this start, we'll stick to this GitHub-only repository. So I'll create this repository. And once it's done, we land on a screen that should look something like this with this dummy README file. And we could, of course, now clone this to work on it locally but we won't do this here. Instead, we wanna get started with our first action, and we can do so by clicking on this Actions tab here. This is the place where you can view and also create actions that belong to this repository. Though actually it would be more correct to say that here you can create your workflows that belong to this repository. I'm currently also using the word action as a synonym for workflow, and for the moment, that's fine but as you will learn later in this course, and in this section also already, there's a separate feature called action. So what we're creating here is actually a workflow and that's also what I mentioned on the slide earlier. You have these three main building blocks: workflows, jobs and steps, and it's a workflow that's attached to a repository. And that's what we're doing here. And indeed to get started, we can choose this simple workflow template here and click on Configure. Alternatively, we also can get started with one of the many other templates that perform certain tasks. And doing so can definitely also be interesting to see some more complex action definitions, though of course, throughout this course, you will learn how to build more complex action workflows. For this start, we'll start with this simple workflow. Now, what this does is it gives us an editor in the browser. You can also create actions locally, and we'll do that later, but for the moment, we'll stick to this browser editor. And here we have to give this file a name. That's the first thing we should do and I'll name it first-action. Please also note the path where this file will be stored. As you can see, it's actually stored in the repository and then in a .github folder. There in a workflows subfolder and then in a YAML file. That's what .yml stands for. It's a YAML file and YAML is simply a way of formatting text. And this is indeed where you should store your GitHub Action workflows so that GitHub is able to detect them. You must store them in a .github folder in a workflows subfolder and then in such a YAML file. So that's the file name and location. Down here we have the content of the file. And we get some annotations here for this basic workflow that was created but I will actually delete all of that and write it with you together from scratch so that we fully understand how such a action is configured and created. And it all starts with giving your action a name. You do this in this YAML file by adding the name key here in the first line, and then a colon. And this must be name. So this key here must be named name, and then after the colon, you specify your workflow name. And I'll name its First Workflow. Now, keep in mind, you can have as many workflows as you want by adding multiple YAML files in this workflows folder but here in this first file, this is the name of this workflow. Now, after giving this workflow a name, you have to also define when this workflow should be executed. And you do this by adding the on key. The on key is just like name, a reserved keyword or name for which GitHub Actions will be looking. And after on, you define the event or the events that should trigger this workflow to run. Now, we'll see multiple events in action later. For the moment, you can just add one event by writing workflow_dispatch here. Now, GitHub Actions defines many events to which you can listen and I'll get back to an overview of the available events later. This specific event here, workflow_dispatch, makes sure that we can manually trigger this workflow. So it's an event that waits for the user to manually start this workflow. Of course, there also are many events that will automatically start the workflow, and we'll take a look at those very soon. So now we have a name and we have our trigger. What's missing is the work that should be done. And for this, we define jobs with the jobs key. And it's important that this must be jobs, not job. It has to be jobs. Now, jobs does not take a value immediately, as we had it for name or on, instead you move to a new line and hit the Tab key on your keyboard because YAML, this format, works with indentation. And if you indent a field below another field, then it belongs to that field. And jobs needs a field that belongs to it and that is simply the first job you wanna define. Now you give that job a name. For example, first-job. This here is totally up to you. It can be first-job or firstjob like this or basic, whatever you want. I'll stick to first-job. Now, this then also doesn't take a value directly. Instead, you move to a new line and hit Tab again because just like the jobs field here, the job definition field, which we created here with an identifier of our choice also needs a couple of related fields that define some details about this job. So here we're saying that we have a bunch of jobs. Then we create a first-job with an identifier of our choice. And now for this first-job, we first of all have to define the runner on which the job will execute the different steps. So the environment in which the different steps should be executed. And this is defined by adding the runs-on key, like this. This again is a reserved name now. So the only identifier that's not reserved thus far is this job name. All other identifiers, like name, on, jobs and runs-on are reserved and have to be written like this. Now here we then define the environment we wanna use and GitHub Actions provides multiple environments we can use. If you search for GitHub Actions runners, you will find an overview of the runners you can use, some details about that, and also, if you scroll down, a list of the runners currently supported by GitHub Actions. Now, obviously that list will change over time but at the moment I'm recording this, you get various Windows Server environments, various Ubuntu environments we can use and various macOS environments we can use. And you see the identifiers you have to use in your script or in your workflow definition here in this middle column. And in my case here, I'm using ubuntu-latest, which at the time I'm recording this will lead to this Ubuntu version to be installed on that hosting machine. You see the hardware specifications of that hosting machine up here where you see the specification for Windows and Linux machines and for macOS machines. And this is all predefined by GitHub. So here I will ubuntu-latest to use this Linux runner on that given hardware specification. And as a next step, we have to define the steps that should be executed with the steps key. Again, this is a reserved name and now we move to a new line and indent again, and now we add a list of steps. And that list is added slightly differently than we added keys before because now we have to add a dash here and then thereafter again a couple of key-value pairs that describe this step. And multiple steps get multiple dashes. Now, that first step takes a name. And you should give your steps names to make it easier to identify them. Here I'll name this print greeting. That value, of course, is up to you though. Thereafter in a new line but still belonging to that same dash, we use the run key to define the command that should be executed in the command line. Now as mentioned before, when defining steps, you can either execute scripts or commands in the shell or use predefined actions. Here when using the run key, we're doing the former, we are defining a command that will be executed in the command line. And here I'll just echo something, which means I'll output a message in the command line where I just say Hello World between double quotes. And then I'll add a second step with a different name, which is print goodbye, for example, for this first basic example job, and here I'll also echo something in the command line and say done - bye. And this is now my first basic workflow with one job with two steps. And of course, this workflow doesn't do anything meaningful or useful yet but we're just getting started here and you already see the course syntax in action here.

With that job defined, we can start a commit, which means we save it and we add it to this repository, and I'll just commit it like this, and we create a commit here because, keep in mind, this is in a file which is in a sub-folder in another folder in our repository, so we technically added it to this project's code, therefore we technically created a commit. And that's really important. GitHub Action workflows are part of your code. You define them in files that reside in your Git repository. This is not something that's outside of the Git repository, instead it's defined in a file in your Git repository in your code and project. And now with that workflow added like this, we can go back to the Actions tab, and now the look of this tab changed. Now here we get an overview of past workflow runs, which will, of course, populate as soon as we have workflows that run from time to time. We can still add a new workflow if you want to but we also have a list of all the workflows that were identified by GitHub, and those are identified by GitHub looking into this .github/workflows folder and then into the YAML files it finds there. So here I have this first workflow, which is that name I gave my workflow, and you see that GitHub identifies that this workflow has a workflow dispatch trigger, and that's why we can dispatch and trigger it manually by clicking this button. If we wouldn't have added this workflow dispatch event, this button would not show up here, but since we did so, we can now click this button to run this workflow against the main branch, doesn't matter, it's the only branch we have here, and now this Workflow will be executed. Now, as soon as you start running a workflow, you can also go back to this Actions tab, or eventually it should also update automatically, and you see your workflow run here. And this yellow dot which you saw briefly indicates that it's currently running, and then this green check mark tells us that it executed successfully. You can also click on this workflow here, and you'll then see more details about this workflow. For example, you see that for this workflow file and this event, we had this job that was executed, which in this case is the only job that belongs to this workflow, and we can also click on this job for more details. And in here we see the different steps, the two steps we defined and some steps that are executed automatically, a Setup and a Cleanup step, and we can expand these steps to get more details about them. For example, we see that in the Setup step that was added automatically, the runner system is basically set up. In the Complete step, we see that some cleanup was done, and then in the two steps we defined, we see the command that we executed and then the result of that command. And, of course, the same is true for Print goodbye. Here we see more details about how this command was executed. And that's how we can define and execute our first workflow and how we can also then evaluate the details of that workflow execution. And, of course, we can run this workflow as often as we want. So if I go to my workflow again here on the left, I can trigger it again, just as often as I want to.

Running Multi-Line Shell Commands Thus far, you learned how to run simple shell commands like echo "Something" via run: echo "Something". If you need to run multiple shell commands (or multi-line commands, e.g., for readability), you can easily do so by adding the pipe symbol (|) as a value after the run: key. Like this: ... run: | echo "First output" echo "Second output" This will run both commands in one step.

Now in the previous lectures, we got started with GitHub Actions and we built our first basic workflow. Now, in the meantime, I cloned this demo onto my local system to also show you how this would be stored locally. This is this remote repository in the end. And there you see this .github folder with the workflows folder inside of it. So that's a nested folder in .github. And then in the workflows folder, we have this first action YML file. Now we'll move on to a different example though and you find this example project attached to this lecture. This is a very simple ReactJS web application And you don't need to know ReactJS or web development in order to follow along. We will not write any meaningful ReactJS or web development code and you don't need to understand these things to understand how GitHub Actions work. But I created this project so that we have a slightly more realistic project to work with. Now if you wanted to, you could run this application locally. For this, you must have Node.js installed on your system and with Node.js installed locally, you can navigate into this project folder, into this second action react demo folder or into that folder you downloaded to be precise. And then in there you can run npm install to install all third party dependencies required by this project. And once that finished, you can start a development server that allows you to preview this very simple web app here. So here, installation finished. Now you can run npm run dev to execute this script and start a development server, which hosts the website on this address on your local system. Here we go. That's this very simple demo website. It's of course not very fancy, but it will do the trick. The interesting part about this demo project is that we can't just preview the app locally, but instead we also have a test script, which will execute some predefined automated tests. Now I do have a separate course on testing if you would wanna learn more about these automated tests, unit tests specifically. In the end, what we have here is a simple test file next to the component it belongs to. And in that file I have two tests that are executed that simply check whether the site works as intended or whether this component here, this site building block works as intended. Again, you don't need to understand the code. The only important thing to understand here is that we do have those tests. Now with all the dependencies installed and Node.js installed, we could run these tests by running npm test and that would then execute them locally. And we should see that by default, they're all passing. Now the thing is these are all things we'll typically do locally, but it would also be quite realistic that we wanna manage this project with Git, that we wanna have a remote Git repository on GitHub to store our code there. Possibly also to collaborate with colleagues. And it would also be quite reasonable that we maybe wanna run all these tests here automatically whenever a new version of that code is pushed to this remote repository. And that's exactly what will set up now with help of GitHub Actions. Now for that, I'll first of all start by initializing a Git repository here. Now I reinitialized it here because I already had one, but you should do this for the downloaded project as well. Once you created that repository, it's worth noting that we have a .gitignore file here, which actually ignores a bunch of files and folders. For example, it ignores the node modules folder, this folder here, which includes all these third party dependencies that were installed when we ran npm install. This is ignored because this folder can always be recreated by running npm install and it should not be checked into version control. But that's just a side note. With this Git repository initialized, we can of course create our initial commit by staging all files and creating this initial commit like this. And now we can create a remote repository for this project if we want to. For that, I'll move back to GitHub and to my repositories. And there I wanna add a new repository. So under repositories I'll add a new repository and I'll name this second action react demo. The name as always is up to you. You can add a description and you could turn it into a private repository if you want to. And actually here to mix things up. I will do that. Now I won't add any files to it because this time this is a repository, which I plan to connect to my local Git repository which already includes files. Therefore, I'll set it up like this and create this repository and then I'll grab that URL here. Go back to my local project and add this link to this remote repository. With the git remote command, add my origin. So this is the identifier I use for this remote repository and add that URL, but I'll not add it like this. Instead, to make sure that I'm prompted for a password when I try to push to this repository, I'll add my GitHub username here. Of course, you should add your username here. With that, we should be able to push our code to this repository and we should be prompted for a password unless we already have such a personal access token stored on our system because we used it before on other projects. So here I wanna push and to make sure that git push works like that, we have to execute this command once to create this link between our local main branch and this remote main branch, so to say. So after running this once, this will be pushed by just running git push in the future. Now here, it did already push though. And with that, if we reload this remote repository, our code is on GitHub. But right now, there of course, has no action related to it.

So now the plan is to add an action that will automatically run these tests. Now we could do this again by going to actions and adding this new action here, right from inside GitHub. We can do this but actually since it is just a file in our project anyways, we can also write it locally. So in this second-action-react-demo folder, I'll add a new sub folder, which has to be named .github and this is a name you must use. In there, you also must add a workflows folder. Again, this name is not up to you, it has to be named workflows. And in this .github/workflows folder, you add your workflow file and that file name is now totally up to you. Here, I will just name it test.yml but the name is up to you as mentioned. And in this test.yml file, you now define your workflow that should be executed. Now as before, for this first workflow we created, you have to add a name, a trigger and define the jobs you want. So that's the same for this new workflow which we create locally. And in general, there is no difference between creating it locally or on GitHub, the structure and the keywords always are the same. So here I'll add a name and I'll name it Test Project because that is what this workflow will do, it will test this project and next we add a trigger. Now the question is what could be a trigger here though? Let's take a closer look at workflow triggers and events and just to be clear, that's the same thing. So let's take a closer look. And GitHub Actions actually offers tons of events you can listen to. Most of them are repository related and there you got events like a push happening or a pull_request being opened, or maybe a new branch was created or the repository was forked, or an issue was opened or deleted. So there also are different variations for some of these events, like an issue being opened or deleted, both things can trigger a workflow and of course you can control which exact event, and which exact variation of an event should trigger a workflow. So there are many events related to repositories and there also are some other events. For example, we already saw workflow_dispatch which allows us to manually trigger workflows. We also got repository_dispatch which would allow us to send REST API request to a specific GitHub REST API to trigger a workflow, we can run a workflow on a schedule so every day at 8:00 AM for example, and we can also allow a workflow to be called by other workflows as something we will explore later. To get a thorough overview and explanation of all available events and event variations, it's best if you simply search for GitHub Actions events and then you should find a page that describes all the events that can trigger a workflow, how exactly these events are triggered and for example, also here at the example of the pull_request event, which variations of that event can trigger a workflow. So if for example, for this pull_request, the workflow should be triggered when a new pull_request is opened or maybe when it's closed or any other variation. So there are many things you can do here and it should be needless to say that throughout this course, we'll see many different event types and variations in action, so that you get a better understanding for these different events and what they do. But this documentation page, which you also find linked to this lecture, also is a very useful page, you should definitely take a look at because this contains all the information you could possibly need for any use case you have. Now back here to this project, since we have so many events available, the question is what's the right event for this workflow? Well, that of course simply depends on what your goal is but here I would say it makes sense that we wanna run all these tests whenever a new push happens, so whenever we push a new commit to our remote repository. And therefore we can simply write push here. And push is of course one of the events supported by GitHub and as you can probably imagine, one of the more important events, which is used quite a bit. So here it states push event and there are actually different ways of defining this push event because you can for example, also define that only pushes to specific branches should trigger this workflow but for the moment, that does not matter to us so we can use this simple form here of defining this event, we'll dive into more advanced definition of forms later. So now this workflow with this name should run whenever a new commit is pushed to the repository, next we have to define a jobs that should be executed. And we'll start with one simple job here which I'll name test, which will perform these well, tests. And here what we have to do as a first step is we have to define our runner, which in my case will again ubuntu-latest on which we wanna run this job or these steps that belong to this job, and the steps are the next thing that has to be defined. Here, we use these dashes to define all these steps and I'll give my steps names. And the first step which I define here, is a step that should actually get the code, download the code from the repository because that's really important to understand, your jobs and the steps that belong to these jobs, run on servers, on machines owned by GitHub or by you if you bring your own servers, but here we're using a GitHub server. They don't run inside of your repository, you could think that they do, but they don't. They are related to the repository, they can get easy access to the repository code but they don't run in the repository. The repository is no server, if anything, it's just a folder on a hard drive on some machine owned by GitHub. That's really important to keep in mind because if you keep that in mind, it should be clear that this server which you get by GitHub to run your steps on, does not have your code automatically. Instead, if you have some steps that rely on your code to be there as these steps here will, you have to download your code as a first step. If you wanna have steps that install dependencies and run your tests, you must first get your code onto this server that's provided by GitHub and that's therefore the actual first step I'll define here, a step that will download the code from this GitHub repository onto this runner, onto this server. And that's where we will explore another key building block provided by GitHub Actions and action.

Actions are another key building block, or component of GitHub Actions. And actually, when we got started with GitHub Actions and our first Workflows, I often said that we're creating an Action. But what I actually meant was that we created a Workflow, because technically there is a difference, and an Action is a separate feature offered by GitHub Actions. and an Action, in the context of GitHub Actions, is simply a custom or third-party application that performs a typically complex, frequently repeated task. Like for example, fetching the code from a GitHub Repository, and downloading it onto such a runner machine. Now, the alternative to such an Action is a command that's defined with help of the "run" keyword in your YAML file. And that's what we saw before, and what we used before, in this first Action. With this "run" command we simply execute typically simple shell commands on that runner machine, and these commands are, well, written down and defined by us. Now, of course, these commands could in theory also get more complex. You could also run more complex scripts with help of these commands. So it's not always necessarily a simple process that's started with such a command, but it will be a command, written by you, in your YAML file. Now, Actions on the other hand, are these predefined applications, which actually also can be distributed through a marketplace, because you can build your own Actions, and will do that later in the course, but you can also use official, or community Actions. And now here, back in this project, where we wanna download code from the Repository onto this runner, we can use such an Action, because this is such a common task, that an Action for it exists. We could write our own command that uses Git to clone the code from the Repository onto this machine, but this is simply unnecessary work, because for this extremely common task, an Action has been created. And you can search for GitHub Action Checkout to find this official Checkout Action. And this is actually a really official Action, that is created and maintained by the GitHub team. There also are other Actions, which are created and maintained by other companies, or by simple community members, so, open source Actions, and you could create your own Actions, but this is an Action created by the GitHub team. Now you see that here on this Repository, I can actually view this on the Marketplace because, as mentioned, there is a Marketplace for these Actions. Now it's called Marketplace, but these actions don't cost you money. Instead, they are free, but this Marketplace is a central place where you can browse different Actions. Now you see that here for this Checkout Action, it's from a verified creator, by the GitHub team. And this badge can give you some extra confidence, in that this Action isn't doing anything malicious, or anything like that. And by the way, later in the course we'll also dive into security, and GitHub Actions, and certain things you should watch out for. So this is a verified creator, this is this Checkout Action, and here we find some instructions on how we can use this Action, though we will use it together in a second. Now, before we use it though, I wanna go to this Marketplace here. Now here you find all kinds of Actions you can use. Actually, you find Apps and Actions. Apps are a different GitHub feature, for us, Actions are interesting. And here you find a broad variety of Actions for all kinds of tasks you might wanna perform. You can use an action to install Node.js, though the runner we're using actually has it pre-installed, but if you would be using a different runner, without Node.js, you could use this Action to install Node.js. And as you can tell, there are a broad variety of Actions for all kinds of things. Now here, I'll search for this Checkout Action again, because that's the Action we need right now. And now we'll actually use that Action.

So how can we use this checkout action? Well, we actually find some instructions here, but of course we'll also simply walk through all the steps together. If you wanna use a action, instead of running a command, you don't use the "run" keyword, that's for running a command, but instead the "uses" keyword. That's the keyword you use when you want to use an action. And then as a next step you simply define the identifier of the action. In this case here, it's actions/checkout. And you find this identifier on the action page, here or of course also in this example. In the end, it's simply the GitHub internal link to the Repository that holds the action code. Now you also see here that you can specify a version. And this can be very useful, because the code behind these actions might be updated in the future. And there might be breaking changes that cause the action to work slightly different. To make sure that your Workflow doesn't suddenly start doing things you don't want it to do, or maybe breaks, you should lock in a certain version, to make sure that your Workflow always uses that version, until you're ready to update. So at the point of time I'm recording this, for this GitHub action, the latest version we can use is this v3 version here. It actually says v2 here, that can be confusing, but the latest version is v3. We can also see this if we visit the Repository behind this action. There we can see that under releases, version three is the latest version. Now this will likely be different when you are watching this. To make sure that your Workflow works exactly as mine, I recommend that you simply all to use version three, because you can even use that if it's a bit older. So here I'll add @v3 to lock in that version. And with that, this is all we have to do for this action. Some actions require some configuration though, and that could be provided with the "with" keyword, which belongs to the "use" keyword in the end. This "with" keyword then takes various keys that configure the action. And which keys are accepted under "with"? So here simply depends on the action. And the official documentation is always the place to go to learn more about that. For this checkout action, you also see that you can define various options with "with". For example you could control which GitHub Repository will be used. But by default, the Repository to which the Workflow is attached will be used. So we don't need to change this, and we also don't need to change any other settings, but for more advanced usage you could change some settings here. And throughout the course we will of course also dive into some actions that do always need some configuration. Here we could add "with", but we don't need to, because we don't need to change any settings. And that's now our first step. We download that code.

Now that we added this first step and we downloaded this code with help of this action, it's time for the second step of this job. Keep in mind, our goal is to run these automated tests. Now that we got the code, the next step, is to make sure that we got Node.js installed, and that we install all dependencies of this project. Now, since I'm using Ubuntu-latest, Node.js is already installed. How do I know that it is installed? Well, GitHub publishes a list of software that's preinstalled on its runners. You see that if you go to Supported Software, here on this page, under Preinstalled Software, there you select your runner. In my case, it's this one. And then here you find a list of preinstalled programming languages and tools and software. And here you actually see that it comes with Node.js preinstalled. So since I need Node.js, this is already there and I don't need to install it manually. If we would need to install Node.js, though, we could add a separate step and use a separate action for this. The Setup-node Action. Again, you could find this action by searching for GitHub Actions, install Node.js, and you would probably find this action, which you can add to your Install Node.js on your runner. And again, you find instructions on how to use this action here. Now to be really clear, we don't need to do this here because we got Node.js preinstalled, but to practice the usage of actions I will actually use this action here. Also Version v3, in this case, as you can see in the official docs. And then here, I will add some configuration. And in case of this Setup-node Action one piece of configuration we can provide, is the Node Version we want to install. And here, I will actually pick node-version: 18 to make this a bit more meaningful. Because my runner actually has Version 16 installed, so maybe I want that latest version, which at the point of time I'm recording this, is Version 18, and then I could indeed install Version 18, like this. Again, technically that's not necessary, but it also doesn't hurt. So that's now my second step.

Now for the third step here, I wanna install my dependencies. For this, we might be able to find some action but actually here we just need to run a simple command in the command line. And that is npm install or better npm ci. Now this also installs all dependencies but it uses dependency versions that are locked into this package lock JSON file. It's simply a NPM feature that makes sure that we're always using the exact same versions we used during development. Npm install would be an alternative though but npm ci is a bit safer and makes sure that we don't suddenly install some breaking version. Now with that, with the dependencies installed, and keep in mind that we did install them on our local system way early in this section as well when we got started with this demo. But now with them installed on this runner, we can also run our tests on that runner. For that, I'll add one last step here and here I also just run a regular command because I wanna run this npm test command which I showed you locally on my machine before as well. And that's it, that's my first job. As you see, it's more complex than that first action job we had before and it's also doing more meaningful things but it has the same structure and syntax. And with that added, we can save that file. And now we wanna create a new commit and push that to GitHub so that GitHub is aware of that workflow. And of course, that works just the way it always works. We make sure that we're in this project directory, then we stage all files or this specific file in this specific sub folder. And then we create a new commit where I say added test workflow. And now we can push this. And as I try to push this, I get an error here as you can tell. And this is an important error which I wanna show you. The error I'm getting here is basically that I can't push this because the personal access token that's being used for identifying this request, this push request, has no permission to create workflows. It has permission to add code to that repository, but since this specific code will lead to the creation of a GitHub action workflow, the personal access token also needs permission for that. So therefore, in order to make sure that we can push, we have to go to the settings of our developer here, and under developer settings down here and personal access token, we have to generate a new token with GA actions, that does not just grant the right to push to our repo, but that also gives us this permission here to update GitHub action workflows. That's what need here. So we also have to check this. Now, that's all we have to add, but with that we got a brand new token. And in order to make sure that this token is used, we have to delete the token we're currently using. Now attached, you find instructions on how to do this on different platforms. Here in my case, I did update it, and now if I try to push again, this succeeds. And with that done, if I now reload, this code was updated. But under actions, you will also see that this workflow was added and that it's actually already executing. Because I added a new workflow that should be executed whenever we push to this repository, and therefore well, this workflow is triggered automatically because we did push. Now here we can also see that it actually executed successfully, and we can of course inspect it and the jobs inside of this workflow to learn more about the different steps. Here we see that all these steps were performed, that dependencies were installed for example, and that these tests were executed. And here we can also see these test results, which we previously saw locally. And that's really important. You can inspect the results of your workflows and jobs, which of course can be super important especially when you're running automated tests, because these tests could also be failing, and in that case, you wanna find out why they failed. And speaking off that, let's see what happens if a workflow fails in the next lecture.

Thus far, all our workflows just worked, and of course, that's the desired result, but sometimes they can fail. And especially if you have a workflow that runs automated tests, the idea is that it does fail if something goes wrong. So to simulate that something fails, I will simply break one of my tests here. For that, I'll go to the src components folder, and then the MainContent.test.jsx file. And we can break one of these tests by simply adding .not here in front of .toBeInTheDocument in line 19. This simple change will formulate an expectation that will not be met, and that will cause this test to fail. We can simulate this locally with this change in place if I run my tests with npm test, you will see that this fails. Now, of course, we broke the test deliberately, but this is a demo. So let's say this would be our real expectation and our code just doesn't behave the way it should, and therefore we create a commit here and we give it a message, and we then push this. Now we push this updated code to GitHub, and of course, since this is a push action that's happening here, it will trigger another workflow run. Here you see the workflow executing. So now it's executing, and if we go into this, and we can also inspect this whilst it's running, we will see that here, run tests failed. And you see this in the overview page with that red cross here that it failed. And you can then dive into this workflow and job, and into the step that failed to learn why it failed. And GitHub Actions gives you a detailed log here, to be precise it shows you the log, your step printed. In this case it's the log this testing tool generated, and this allows you to understand why this job or this step failed. And of course, there's a broad variety of possible reasons. In this case, it's simply because one of our tests has an expectation that isn't met. And by analyzing this, we can then either fix the code so that the test expectation is met, or fix the test so that the expectation is a different one. And that's the correct step here, because here the correct expectation is to expect this element toBeInTheDocument not to have it not in the document. Therefore, what I'll do here is I will simply revert this commit. I could also delete it manually and create a new commit, but I will simply copy this commit ID here and then run git revert this commit. And as you learned in this crash course section or as you already know anyways, this creates a new commit that will revert that old commit. So here I created this new commit with the default commit message, you see that the code changed, and now I can push this new commit that reverts the old commit, and that, of course, as you can imagine, will trigger a new workflow run, because it is another push. So here we got this workflow and this job running, we're going through all these steps again, and you will see that as soon as we reach the test step, this time it will finish successfully.

Okay, so that's how we can deal with failing tests. Now I wanna move on to something we haven't done before. I wanna add a second job to my Workflow here because as mentioned earlier in the course, your workflows that are part of your repositories can have multiple jobs inside of them and then they have these different steps in the different jobs. But having multiple jobs in a Workflow is definitely something that's also quite common to have. It simply depends on the goal of your Workflow if you need one or multiple jobs. And here we could say that maybe this Workflow here should not just be about testing this project, but it should eventually also deploy that code, upload it to some hosting server. Here I have no hosting server but we can still simulate that it does that. Therefore, I'll rename this file to deployment, since that seems to be more fitting. You don't have to rename it but I wanna have a descriptive file name and I will rename my Workflow to Deploy Project. Now it should still execute on push but now testing is just one job. Instead, now I'll add a second job, which I'll name deploy. Now what's really important is that these job identifiers are on the same level regarding their indentation. So here I indented once for test. Here we have one level of indentation for deploy. This identifier here is up to you just as test is up to you. You can name this testing or whatever you want but here I chose test and now deploy. And now here we also need a runner that's the same. Every job has its own runner. And of course, different jobs can have different runners, even if they're in the same Workflow. Nonetheless, here, I'll stick to ubuntu-latest and then I can add my steps. Now many steps will be the same now. I want to get my code, I want to install this specific node js version and I want to install my dependencies. Therefore, I will actually copy these steps and paste them down here. Just make sure you don't accidentally break the indentation because in yml files, the indentation is extremely important. You should have that same structure as you see it here with that list of steps indented once below steps and steps and runs on indented once below deploy. So now we have these steps, but now in the next steps, we won't run our tests, but instead build the project. Now many projects must be built before they can be deployed. For example, a mobile app must be built and this website project also must be built because the code you write during development will actually not be the code that's later served to your visitors. Therefore, I will add a build step and here I'll run a simple command in the shell. And that's this npm run build command. This will target this build script, which is part of package.json, and by running npm run build, this will make sure that this project, this website is built. Now thereafter, we would typically want to upload it to some web host, to some server that serves the website. But here for this example, I have no such hosting provider. Therefore, what I'll do here is I will add a step named Deploy but I'll just echo something here. I'll just echo deploying like this. With that however, we added a second job to this one Workflow which we have with its own steps where some steps are the same as before but where also some steps are brand new. Now, what's important to note is that these two jobs test and deploy will run in parallel and we'll see this in action as soon as we create a commit here and push this to get up, which is what I'll do next, I'll stage my changed files, in this case, the deployment yml file, which was previously named test.yml, and I'll create a new commit where I say, renamed Workflow added deploy job. And with this commit added and created, we can now push this updated code to "GitHub". And there we'll now see a difference. The code of course changed, but under actions we'll see that the Workflow name changed. And in this run for this Workflow, because of this push, we now see two jobs down there which should make a lot of sense because we now have two jobs. They will run in parallel which we will see as soon as this job execution starts. Now, here actually it looks like it started already and just didn't update the UI, but they ran simultaneously. As you can also tell by the total duration here which is not the sum of the individual durations. this proves that they ran in parallel and the difference simply is some extra setup time that was taken by "GitHub Actions". So now both jobs ran in parallel. Both jobs ran successfully, as you can tell because all steps finished successfully and that's how you can execute multiple jobs. But of course, for some jobs it also makes sense to run them after each other instead of in parallel.

Here for these two jobs, testing and deploying, it might make sense to run them after each other instead of parallel to each other, because we probably only wanna deploy if testing finished successfully. If we got a failing test, we don't wanna deploy. Now to run jobs after each other, we just have to add one simple keyword here to the job, and that's the needs keyword. With needs, you can point to the identifier of another job that must finish before this job will start execution. In this case, I can enter test here to clarify that this job, the deploy job, needs the test job to have finished before this job starts. And it waits for this test job to finish successfully before it starts. Now there are different ways of configuring all these things, for example, you could wrap this in brackets to wait for multiple jobs to finish, but here we just have one other job for which you wanna wait. However, that's how I specified this here, and with this change implemented, we can push this updated job here, this updated Workflow. Run jobs after each other. That sounds like a good commit message, and now we can push this to GitHub, and there we will then see that for this Workflow here, these now run after each other and it's also reflected visually here. We see that deploy will only start after testing finished. So let's wait for these jobs to finish, for test to finish, and then for deploy to finish. Here we go, test finished. Now deploy starts, and of course, eventually that will finish as well. Now it is of course worth noting that overall, this Workflow will therefore take longer. That should make a lot of sense because now the jobs don't run in parallel, but after each other. Nonetheless, it can make a lot of sense to make sure that we're not deploying an updated version of this website if our tests failed. And to also show you what happens if this first job fails, let's break it again. Back here in the project, we can break it in the same way as we did it before, by adding this not keyword here, in line 19 in MainContent.test.jsx. And then I'll add a new commit, where I say broke test and which I then push, and with that, unsurprisingly, another Workflow execution will be triggered. But this time, the deployed shop never start running because tests will eventually finish unsuccessfully. So let's wait for that to happen. Here we go. As you can see, this job did not finish successfully and therefore this job wasn't even started. Now of course, again, that's not really the desired result here, and therefore I'll revert this last commit by getting its ID, and then using get revert to get rid of it. I'll then confirm this and push this updated code with this commit that reverts this breaking change. And therefore of course, another Workflow run will be triggered, and this time both jobs will finish successfully again. Here we go.

So at this point we already covered a lot of important essentials about GitHub Actions and you are already able to write your first more meaningful GitHub Action workflows. Now, there are two last concepts I want to explore as part of this section before we then dive way deeper into GitHub Actions in all the other sections. And the first concept I wanna explore, is the concept of having multiple triggers. Thus far, we always only had one simple trigger. Now let's say, we don't just wanna trigger this Workflow if we push to this Repository but we instead also wanna allow ourselves to manually trigger this workflow in the future. If that's our goal, we can wrap this here in square brackets, this event name and add workflow-dispatch as a second event because that's that event that allows us to trigger this workflow manually. And of course, we can add as many other events here as we want. So that's a quick and easy way of adding multiple event triggers. With this changed, I can again create a commit, added multiple events and push this to GitHub. And you will notice that now that this was pushed, it of course still executes automatically because we still had a push event here. But if I take a look at this workflow by clicking on it, I now also have this Run Workflow button, which allows me to trigger it manually. And I can do this here even if the previous workflow execution is still running. Now I got both executions running to be precise, they're both queued at the moment and they will eventually be executed. Here we go. Now they are both running simultaneously. It's worth noting that this execution here is coming from me manually starting it. And this first execution was triggered because we pushed to this Repository. So that's how we can use multiple events as triggers. Now we can actually (indistinct) configure these triggers in way greater detail. We can, for example configure that only for pushes to specific branches. This workflow should be triggered, which could make sense if you only wanna run your tests and deploy the website if we're pushing to the main branch, but not if we're pushing to other branches. But that's something we'll explore in the next course sections, where we dive way deeper into GitHub Actions.

To finally conclude this section here, there's one last concept I wanna dive in, and that would be expressions and GitHub context. Thus far in all our steps, we had simple commands, which we just executed like this, and that will very often be the case. But sometimes, you also have certain actions or commands that need some metadata from the environment. For example, you could be working with environment variables, something we will explore later in the course. To show you what I mean without getting too advanced, I'll add a second workflow here, which I'll just name output.yml or that's the file name, and I'll name the workflow Output information. And I wanna trigger this workflow manually with workflow_dispatch. And now here, I'll just define one job, which I'll name info, and this job runs on ubuntu-latest. And what I wanna do here in my steps is I wanna output some meta information that's automatically passed into these jobs and steps. And this meta information is collectively named context. GitHub actions creates some context data with various pieces of information about the event trigger and the runner and other things that can be accessed inside of these steps. And to get a better feeling for the kind of information you're getting, which could be useful for certain tasks you wanna perform here, I'll just output some of the information we're getting. For example, here in my first step, I wanna output my GitHub context, which is an object or a collection of data with some general information about this workflow and the event trigger. Here I'll add the run key to execute a command in the shell and I'll output some information here. However, this time, I don't wanna hard code some text but instead, I wanna output that data I'm getting from GitHub. And for this, we're getting a special identifier, which we can use in our command line here. And that's the github identifier. However, if I just write it like this, I would just output the text GitHub here. Instead to access this dynamic value, this data object that's provided by GitHub here in this line of code in this command, I have to use a special syntax, which is a dollar sign and then two opening and two closing curly braces. And now this syntax signals to GitHub that this should now not be treated as text that's output in the command line or that's used like text in the command, but that instead here we wanna access some surrounding metadata, some metadata that's made available by GitHub. And in this case here, it's the github key which I wanna access, which is one of the officially available keys in the context of such a job. So this is not an arbitrary word. Instead, github is a reserved key name here, a reserved word so to say. And to make it readable, I wrap it in a function called toJSON, which is provided by GitHub. And you can search for GitHub actions context to learn about the different context objects that are available. Here you, for example, see that github object, which we're using right now, and we'll see other objects throughout this course. No worries. And you can search for GitHub actions functions or expressions to find this information page about this expression feature we're using here because this syntax here is called an expression, which simply means we're not outputting this as plain text. Instead, we're getting some metadata, some context data from GitHub and we're then converting this back to text that can be used in the commands, in this case, to output that data. And since this is a data object, we must convert it to some printable text, and that's what this toJSON function does. The expression syntax has a couple of built-in functions that can be used and toJSON is one of them. Again, these are all things we'll explore throughout this course and we'll see various expressions and features in action throughout this course. We're just getting started with it here. So no worries if it's not 100% clear yet. Now, with that, however, we can save this and we can create a new commit for this basic workflow we created here. So I'll commit this added output workflow and push this to my repository, just as I did before. Now, what this does is it creates a brand new workflow, this output information workflow and since this has a workflow_dispatch trigger and only that trigger, we have to trigger it manually, which is what I'll do here. With that, this workflow starts executing and it finishes super fast because we're just outputting some information here but what you see in this Output GitHub context step is that this data here, that's all fetched from this GitHub context object. So that's data that's provided by GitHub to this job and step. And that's data we could use in that step. For example, what we see is that here we get information about the repository to which this job and step and workflow belongs. This would have been useful before, especially with that URL if we would have manually cloned this code for this repository instead of using this checkout action, which we did, which is of course better but we could have used this metadata to manually clone the data. And we get a bunch of other information in here as well as you can tell, and that's just one of the available context objects. There also are other objects with other pieces of data. For example, about the runner environment or possible environment variables we might be using. Again, this is something we'll explore again and again throughout the course and we'll dive deeper into this throughout the course. I just wanna get you started with expressions and this context here or these context objects here. It's, by the way, also this GitHub context where we get information about the event that triggered this workflow and about some event data that's related to that. But that's it for getting started with that. It is another important feature you should be aware of though.

Now that's it for this course section. In this section you learned about these core components that make up GitHub Actions. And there it all starts with Workflows, which are attached to GitHub Repositories and where you define for which event, for which trigger, which Jobs should be executed. And one Workflow can have one or more Jobs. And Jobs are the next important building block you must know. With Jobs, you define the environment in which your work will be executed, so the server on which it will be executed and the Steps that will be executed in order. And these Steps are the things that do the actual work. There you execute command line commands or you use predefined Actions, as you learned. And for the Jobs you can run them in parallel which is the default or sequential, after another. Now these events and triggers are also very important. As you learned, there is a broad variety of events available and these events are either Repository related, for example that you pushed and you commit to the Repository. But there also are some other events, for example that you manually started a Workflow. And Workflows have at least one event but they can have more than one event. Now when it comes to defining Workflows, you do that in .GitHub/Workflows/ some YAML file. And that file can either be created directly on GitHub, as we did it for the first Workflow, or typically locally, that's what you do in most cases. But it has to be a file in that Workflow's folder in that .GitHub folder. And in that file you have to follow the GitHub Actions syntax. So the keywords GitHub Actions knows and you saw some of the most important keywords and Action in this section already. Now as mentioned, your Jobs and the Steps of these Jobs then execute on so-called runners which are simply the servers, the machines that execute the Jobs. In there you got various predefined runners provided by GitHub, which run in their data centers, so to say. And GitHub offers different runners with different operating systems. They do support Linux, Windows, and macOS. And then depending on which operating system you chose you got different hardware profiles. If you're not happy with those predefined runners you can also bring your own, but that's something we didn't do in this course section. Now, regarding the Workflow execution, they are of course executed whenever their connected event is triggered, and then you can follow their execution and get detailed insights and logs directly on the GitHub page, as you saw multiple times throughout this section when we inspected the logs shown by a Workflow. Now I also mentioned it before that for your Workflows, for the Steps you execute in your Jobs you can also use Actions. Because you can run your own shell commands with this run key. And that's what we did in most cases throughout this section. But for more advanced and typically repeated tasks you can also use Actions. Either official Actions provided by GitHub itself or Actions by other communities, or the community or your own custom built Actions. And we'll build our own Actions later in the course. But with that, you get these essentials which you need in order to work with GitHub Actions. And we're now ready to dive in deeper.

Now that we explored a lot of key concepts it's time to practice what you learned. Because only practice makes perfect. And for that you find another example project attached which you should download and turn into a local Git Repository. And then you should also create a GitHub Repository for that attached project. So a remote Repository and connect the local Git Repository to that remote GitHub Repository. So that in that GitHub Repository or for that GitHub Repository you can create two GitHub Actions Workflows. And the first Workflow will be a Workflow that tests and Lints the project and then also simulates the Deployment of the project whenever a new Commit is pushed. That's the first Workflow you should build and for this Workflow you should of course download the project code whenever it's triggered. You should then install any dependencies that are needed. And then you should run the Lint test and build scripts and also output some message like deploying project as part of the Workflow. Now the Lint test and build scripts are scripts that are included in the detached project in the package.jason file. There it's this lint, this test and this build script that should be executed as part of the GitHub Actions Workflow your building. And thereafter as mentioned you should simply output some dummy message in the shell of that Workflow runner. So in that Workflow environment. Now for this first Workflow it's up to you whether you want to use one Job for all three scripts or three different Jobs running after each other. And if you feel like it you can of course also build both versions. One version one Workflow with one job, and one Workflow with three jobs so that you practice both approaches. But that's just one of the two Workflows you should build this Lint, Test, Deploy Workflow. The other Workflow will require you to listen to an event to which we haven't listened yet in this course section. Because for this second Workflow I want you to build a GitHub Actions Workflow where you listen for the Issues Event. Which just like Push is one of the official events one of the official triggers you can listen to, which should start that Workflow whenever a new Issue related action happens. So for example when a new Issue is created. And all you should do in that Workflow is output the event details in the shell via echo. And I showed you how you can get access to some of the metadata that's gathered and provided by GitHub in your GitHub Action Workflow early in this section. So use that knowledge to simply output that collected metadata that is provided by GitHub Actions for your Workflow. So for this second Workflow which I want you to build you should listen to the Issues Event and use that GitHub Context Object I mentioned earlier in this course section. So that's my exercise for you. In the next lecture I'll share my solution with you so that you can compare your solution and your approach to mine.

So did you succeed? In this lecture here, I'll show you my solution for this problem here, or for this task here, for these two Workflows that should be built. And here I am in this attached project, and you can of course also run it locally if you wanna see it an action locally, but I'll not do that, instead, I'll initialize this as a Git Repository, because that's the first thing I wanna do, I will then add all files, or I will stage all files for my first commit, which I'll name initial commit. And with that created, I wanna create a remote GitHub Repository because that of course will be needed in order to then well, run some Workflows with GitHub Actions. For that I am back here on GitHub, and I'll create a brand new Repository in that account here, and I will name it basic-exercise, but the name is totally up to you as always. It can be public or private, that is up to you. And here I will again go for private, and create this Repository, and then grab that URL here, that URL in this box, and link it here, or set it up here in my local Git Repository, as a remote Repository with Git remote add. And then here I'll add origin as an identifier, we must not forget that. And then here in the URL, I will add my username here, because that will help me with authenticating my request to that remote Repository, and hit enter. And then if we run Git push, we get this message here. So I'll quickly run this command here once to set up this connection. And now I'm prompted for my password here because I actually entered the wrong username here. That should be maxacademind1 in my case here, so let me cancel this here, and quickly fix this by using Git remote set URL for origin, and I'll copy that URL here, put the correct URL I wanna set includes maxacademind1, which in my case is to correct username. And now this command here should succeed, and use that already stored token, that personal access token which I have set up on my local machine. Okay, so now with that, the code is on GitHub, but of course we got no GitHub Actions Workflows yet. And that's the next thing I wanna do therefore. For this, you learned that you must create a new folder named .github in your project folder, and in there add a Workflows folder, and in there you can add your Workflow YAML files, one YAML file per Workflow. So here I'll add the first YAML file, and I'll name this deployment1yaml, or yml, both extensions are valid. And now we have to add the definition for this Workflow. And that all starts with a name, and the name here is Deployment Exercise 1, or anything like this, of course, the name is totally up to you. We also need an event, and the event here is push because that's the event I'm asking for here on the slide, on this left Workflow, we wanna trigger these linting, testing, and deployment Steps whenever we push to this reload Repository. So therefore the event is push, and will dive deeper into available events, and how we can configure these event triggers in the next course section, but here it's just on push. And then we define our Jobs. And now I mentioned that it will be up to you whether you want to use one, or three Jobs, and I'll show you both alternatives. And in this deployment1.yaml file, I'll just set up one Job. And here this Job is my deploy Job, that's the name we could assign, this name here is up to you as you learned, this identifier here. And then for this Job, we have to define a runner, and I will use Ubuntu latest because I don't need a Windows, or Mac OS platform, and Ubuntu latest is relatively common, and typically a good choice. Then we have to set up our Steps, and here we now got a couple of things that need to be done. It all starts for example, with us getting our code in the GitHub Repository onto that runner machine. And that can be done with help of a third party action as you learned in this course section. I'll give this a Step, a name of get code, and then I want to use a GitHub Action here, action distributed by GitHub itself in this case, which is actions/checkout, and then I'll set the version to free here, in my case to be free. So that's one of the actions we can get in the marketplace. In this case as mentioned, an action distributed by GitHub itself. We could add width to provide further configuration if needed, but here no extra configuration is needed. And this will ensure that our code is downloaded onto this runner machine. Now the next Step with the code being avoidable on the runner machine, is to install the the dependencies. We could also install Node.js as I did it in this course section, in an earlier example, and you could do this here as well, but it's technically not required, hence I won't do it here, but instead I wanna install my dependencies, and we'll do that by running a simple shell command in this project folder which was downloaded with help of this action, and that can be done with the run keyword as you learned. This allows us to execute commands in the shell, so in the terminal, or command prompt you could say of this runner in the project folder, and here I wanna run npm ci, which installs all the dependencies of this project. Alternatively, we could also use npm install, npm ci, just make sure that we're using the exact same versions as we did during development. And with that done, I didn't wanna run the linting script, the testing script, and the build script, and output some dummy deployment message. So here I'll give this next Step by name of lint, and run another command in the shell, and now it's npm run lint, because that runs this script here, this script, this lint script here, is executed by running npm run lint. And that's therefore what I do here. And thereafter, the next script in line, or the next thing I wanna do is run, or test my code, and run the npm run test command. And for this special script, you could also use just npm test that is allowed by Node.js, and the package JSON file, but you can also use npm run test like this. And thereafter for building the code, I will run npm run build. So that's how we can run these different scripts from package.json. And last but not least, to simulate the deployment, or to deploy the code, and simulate that, I'll just output some dummy message where I say deploying. And I'm doing this here because at this point we have no real deployment process yet because that would require some hosting provider for this project, and we don't have one here because that's also not the focus of this course if we're being honest. So here it's just this dummy message. But with that, with all these Steps added, and with all that configuration added, we should have a Workflow that does exactly what's requested on the left side of this slide here. I'll also already build the version with three Jobs instead of one Job, and for that, I'll just copy this deployment1.yaml file, and name my copy deployment2.yaml, because in the end I have the same trigger, I'll just name it deployment exercise two instead of one, and I'll have multiple Jobs, one Job for linting, one Job for testing, and then one Job for deploying. And here the idea really is that for linting, I also wanna use Ubuntu latest, and then here I have my Steps, and we still will need to get the code for example, we still will need to install the dependencies, and we still of course need to run the lint script, but we won't do anything else thereafter. Hence, we can copy these three Steps and paste them in here, and make sure that you get the indentation right, otherwise it will break. And then we can copy this configuration, and copy it for the test Job because there we also need to get the code, and also need to install the dependencies. But instead of linting, we here of course want to test the code, and run npm run test. So that's what we want to do here for the test Job. Choose a different name for this Step, and run npm run test. And with that, for the deploy Job here, we still wanna keep the Step where we get the code, and we want to keep the Step where we install dependencies, but we now get rid of linting and testing because we did that in separate Jobs. And that's there for the alternative with three Jobs where we have a couple of repeated Steps in all Jobs, but then the last Step is typically different. But that would be the alternative with three Jobs instead of one, and now we have both alternatives here, and with that we can create a new commit, and then push this. So here all give us a message of added deploy Workflows, and push this to the remote depository. And with that, since pushes are trigger, those actions should be registered, and detected, and added, and they should also be triggered right away. And indeed, we see that both Workflows are being executed here. So both Workflows were detected, were added, and are executed. And now let's see whether they execute successfully because that is of course what should happen here. The first Workflow already finished, that was the version with three Jobs. And the other one is still running, should finish momentarily as well though. Yep, now it's done. And now both Workflows finished successfully. Now the version with three Jobs was a bit faster here, but ultimately, both Workflows were super fast. And it is of course worth noting that for diversion with three Jobs, all three Jobs runs simultaneously, so in parallel because that's the default for GitHub Actions. If you want to run these Jobs after each other, you can do this by adding the needs keyword. So here for testing, we could say needs lint, so that this lint Job must be finished before the test Job runs, and for deploy, we can say needs test, so that the test Job must be finished before the deployment starts. And it simply depends on your exact requirements which option you wanna choose, which approach you wanna choose if you wanna have parallel, or sequential Jobs. Here, I'll show you this alternative as well, and I'll add a message of run Jobs after each other, and push this updated Workflow. And with that of course, both Workflows should be triggered again because we pushed to this Repository, but now the version with the three Jobs, should run these Jobs after each other as you can already see here in this visualization. So now it's first running the lint Job, then the test Job, and then the deploy Job, but ultimately, of course, it should take a bit longer, but still finish successfully. Here we go. The first two Jobs already finished successfully, the third Job is running right now, and it should finish successfully any second as well. Let's see. Yep, here it's done. And that all worked as expected. So that was the first part of this exercise here. We built this lint, test, and deployment Workflow, and we built it in different versions. Now we also have that second task where you should add a new Workflow that's actually not triggered whenever you push to that Repository, but instead, whenever something happens with the issues of that Repository, when a new issue is added, for example. And all we wanna do here is output the details about this event in our shell, in the shell of the runner on which the Job of the Workflow executes. For this task, I'll add a new Workflow file, which I'll name issues.yml name as always is up to you. And then here, I'll give this a name of handle issues, you can choose whichever name you want, but the event trigger is very important. And here it's not on push, but it's instead on issues which is one of the officially supported events that will trigger this Workflow whenever anything issue related happens on the Repository. With that added, of course, we still define our Jobs, and here we only need one Job, which we could name info, or output, or output info, whatever you want, and then here we choose our runner which still is Ubuntu latest in my case, and we set up our Steps. And for the first Step, and in this case the only Step, I'll just say output event details. And we do output these details by using the echo command, but then we use this special expression syntax which I taught you, where you have a dollar sign, and then two opening and closing curly braces, and between those opening and closing curly braces, you can run special commands, and access special data objects that are provided by GitHub in that Workflow. And here I wanna output this special GitHub object which is provided, to be precise, to turn it into text. So from data to text that can be output, I'll wrap it with the two JSON function, which is a function provided by GitHub Actions, which can be used inside of such expressions. So this will output some information about this GitHub context object. And since I'm only interested in the event specific data, I'll actually access github.event. I showed you earlier in this course that this GitHub context object is a quite complex object with lots of data inside of it, and it actually also has an event field, so a nested object inside of this GitHub object with event specific data. And we could output the entire GitHub object here, but since we're only interested in the event data, I'll dive into this nested object, and output the data in that nested object here. And with that, I'll create another commit, add all my files, give this a message of added issues, Workflow, and push that. And of course, once we push, the other two Workflows here will be triggered as you can tell, but this newly added Workflow, which you can see here on the left, is not triggered because it's not listening to pushes, but instead two issues being created, or edited. So to simulate this, we can simply create a new issue here, because it does not matter who creates an issue, if that's us, or someone else. I'll just say new issue, and submit this new issue because it doesn't really matter, we just need some issue action in order to trigger this Workflow as you can tell. Now this Workflow is triggered, and in here you see it outputs the event details. All that here are the details of this event, and we see that an issue was open for example, we can learn more about the URLs that lead to that issue, and so on. Simply, have a look at all the data that is part of that event data, and of course that event data could then be used in more complex Workflows to for example, automatically analyze open issues, add labels, or do anything like this. Here, however, we're just outputting this data because that's everything that needs to be done to complete this exercise.