In this course section, we will explore how you can use containers with GitHub Actions, and for that I'll also provide a very brief refresher on what containers are in the first place and why you might want to use these container things. Though you will get the most out of this section if you do have some knowledge about containers already. Otherwise, you can also skip this section. But it will reintroduce you to the concept and we will then learn how we can utilize containers for running our jobs in containers instead of just the runner machines, which we can specify. And we will explore a concept called Service Containers that's offered by GitHub Actions which is also a concept that can help us in our automated workflows.

Now, let's start with one of the most important questions to make sense of this entire course section. And that question, of course, is, what are containers? And here for this section, I mean Docker containers when I say containers, but most people mean Docker containers when they say containers. So, what are these Docker containers? Though the idea behind containers, no matter if they're Docker containers or not, is generally the same when we're talking about them in the context of development. Because the idea behind such containers is that you create packages, you could say, that contain both your code, your application, or website code as well as the environment that's needed to execute or build that code. And the advantage of using this container approach where you build these packages is that you build a reproducible execution environments where you always know that this code with its environment will run in exactly the same way. Because if you just have your code, and you then run it on machine A and machine B, those machines naturally have different configurations, different operating system versions, different software installed. With containers, you have isolated environments, isolated packages with clearly defined software, and you have always exactly the same execution environment no matter where you then run this container. That's the idea behind such containers.

Now when it comes to Docker Containers, you can create them by writing a Docker file that defines how your Container so how this package should be set up. And you always start by defining a base image as it's called on which your Container is based. So in this case here, that for example would be an image that has node js installed on top of some Linux operating system. I have some environment variables to find for this Container. And then I have some steps that are always executed when this Container is prepared and which give me an environment in which I can run this application whenever I want to on whichever machine I want to as long as that machine supports Docker. But I do have a complete course about Docker and Containers where you can learn all about this Container thing and where you can dive much deeper. This is not a Container course, and if it doesn't make any sense to you at all you can skip this section because GitHub Actions supports Containers but you don't have to use them. But using Containers can have various advantages because with GitHub Actions you can run your jobs and their steps on "Just the Runner" machine, which you define. That's what we did thus far in this course but you can also run them inside of a Container defined by you. And the advantage of using Containers instead of "Just the Runner" machine as we did it thus far, is that with a Container, since you defined the entire environment you have full control over the environment over the installed software and the setup steps that were performed for setting up that environment. If you are running your jobs and their steps directly on the Runner, then you are stuck with that Runner. And that, for example, means that you have an environment with a predefined operating system version and a predefined list of software. I did show you this information about the pre-installed software earlier, and it depends on which runner you are using. And depending on that choice, you then have various tools and services pre-installed and running on that runner machine provided by GitHub. Now, often that maybe all the software you need but sometimes some software or setting might be missing or you might have a version that you don't wanna work with. Now of course, you can always go to a Workflow and add a new step where you install some extra software. That's always an option, but when using Containers you could create such a predefined environment separately from your GitHub Actions Workflow definition file and you could reuse the container in different workflows wherever you need that environment. So that could be the advantage of using such a Container that you can build your own clearly defined environment in which your code will run or will be built or tested or whatever you want to do with it. Now as mentioned, GitHub Actions supports Docker Containers and you can simply run Docker Containers on top of those Runners provided by GitHub and by GitHub Actions. Your containerized job is then simply hosted by the Runner so it's running on that Runner machine but in an isolated environment. The environment defined by you in the Container definition and the steps, and that's the important part the steps of the job are then executed inside the Container. And they're also is another feature called Services or Service containers, which we will also explore later in this section, which allows you to run extra services in Containers that might be needed by your main build job or by the main thing you might be doing in your job and it will become clearer later when also dive into a concrete example.

Now speaking of examples, attached, you find an example project, and that is that backend API, which I also showed you earlier in the course, with slightly altered code, but generally the same. But the attached project also comes with a Docker file. And if you have Docker installed on your system, then you can use this Docker file to build a Docker image as it's called, and then run a container based on that image, because that's how Docker and containers work, you build images based on definitions like this, where you simply define and build that environment in which your code would run. And then you can run multiple containers based on the same image, on different machines wherever you want to, basically, as long as Docker is installed on that machine. But again, I do have a complete course about that, and it's not the goal of this course to build, or run any containers on our machines. I just want to show you such an example definition file where I would be building my own container image for this application here, for this web API here, which ultimately would start the web server, and which would therefore run the web server in that isolated environment that's based on some node image, so on some other image that was built by other people, that comes with Node.js pre-installed on top of Linux. So that's a Docker image definition file, which ultimately would allow us, and enable us to run containers based on that definition. Other than that, it's the same project as before, and we actually don't need that Docker file for this section, I just wanted to show you how this would look like, if you would be using Docker locally, or on some server that maybe hosts your website. But as mentioned, GitHub Actions also supports Docker containers. And therefore, what I'll do here, is I'll again turn this into a Git Repository, and add my initial commit, as always, basically. And then here I'll create a new remote Repository, a public Repository here where I wanna well, host this Git Repository. So where I then also want to run my GitHub Actions of course. And I choose a public Repository here, because I wanna use this environment feature which I showed you early in the course, so that I can define environment specific secrets which are picked up by the Workflow definition. Then we can wrap this URL, and add this remote Repository here like this. And here in my case, I will again add my username to make sure that I authenticate correctly, and we can then push with this command as always. And that pushes our project here to GitHub. And it actually already registers a Workflow because this attached project comes with a Workflow file already, where I listen to pushes to the main branch, and where I then try to, in the end, run a web server as part of the test Job, which should connect to the MongoDB Database, and then run some tests. However, in my case here this will fail because for one, I haven't registered these secrets in my Repository yet. And I also shutdown my MongoDB Database server, which I did set up in a different course section. Therefore here, my action will actually start, but it will then get stuck, and eventually time out because it fails to run this server therefore here, I'll simply cancel it manually. Because in order to really run this, I would have to go back to MongoDB ,and sign in with that account I created earlier, or create a new account, but I did show all these Steps in an earlier course section. And on MongoDB, you must make sure that you do have a cluster, and that you do allow access from all IP addresses, that is very important here, so that the Workflow running on some server provided by GitHub Actions is able to reach out to this database, and then you also must make sure that you do have a user, and the password of that user. Therefore, I'll assign a new password here, and you use these credentials here, and store them as secrets. So therefore here for this Repository under settings, I can go to environments as I did early on this course. And here, we wanna make sure that we do add an environment if you don't have it yet where you do add these secrets. So here I have this testing environment where I add one secret called MongoDB password, where I paste in that password I just created on MongoDB, so on that database server. And then I'll add that other secret, MongoDB username here, like this which in my case is maximilian. And after adding these secrets, and the testing environment in general, which is the environment I'm referencing in my Workflow definition file after performing these Steps combined with the IP address, which I'm allowing, the global IP address under network access. After doing all these things here, we should be able to rerun this action, which we can do by visiting the action which we canceled, and choosing rerun all Jobs. And this will now, well, as the name suggests, rerun all Jobs, and it should now succeed when it comes to connecting to the database. But this is all also nothing new, and this is all not using any containers. Instead, let's just quickly wait to see whether this works, and then we'll dive into using containers, and setting up containers with GitHub Actions in the next lecture. So let's quickly wait here. And it actually still failed for me because I used the wrong cluster address, so that should also be correct. So there you should also make sure that you're using the correct one by using Connect, connect your application, and then using this address here. And now with that, I canceled the other Workflow run. And after updating this cluster address, if we push this, a new Workflow run should be started, and should now succeed. Here we go, it now connected successfully, it ran all the tests, and performed this entire Workflow successfully. But now onwards to using containers.

So what's now the idea behind using containers? Well, right now we're running this test job directly on this Ubuntu machine provided by GitHub Actions, so on some server that comes with that Linux distribution installed on it provided by GitHub. And just to be very clear, there is nothing wrong with that. You can absolutely run your jobs directly on these runner machines, and in many cases there's absolutely no reason to use containers instead, we had no problems using these runner machines throughout this course. But it could make sense to use containers with your own definitions or based on public based images to have full control over the existing environment in certain cases. To show you one example where using a container could make sense is my own website, academind.com. There, I also have a testing job and this is the real deployment workflow or part of the real deployment workflow which I use for my own website. And in this testing job, I'm using this playwright tool to run some integration tests. Now what this tool does is it opens different browsers and tests the website in different browsers. And here I'm using a base image provided by the team behind this playwright testing tool that actually comes with all these browsers pre-installed. I'm doing this so that I don't have to add an extra step here in my workflow where I would install these browsers manually. I could do this as well, but it would take some extra time for which I'm paying, since I'm paying for execution time here in my private repository. And besides paying for that time it also simply wastes some time if I can just use this predefined environment that has all these browsers installed, and where I therefore don't have to go through these installation steps manually. So that's one example where I actually put one job, the testing job, in this real workflow into a container. A container that's based on some image that comes with a certain environment, which I want to use here, an environment that has certain browsers pre-installed. Now here, back to our simpler example in this course, we technically don't need an image we could just run all these steps on just the runner machine, it would work just fine but we can also put all these steps into a container by using some base image, so some base environment, simply to see how that would work. And that's exactly what we're going to do here. And to put a job and its steps into a container all you have to do is you have to add the container key here to your job definition in your workflow definition file. So nested below your job identifier, you put the keyword container. And then as a value for this container you can specify the name of the image which you want to use. For example, node. Now, when it comes to these image names you can use you have to use names of images that are available on public repositories. Like for example, Docker Hub, which is the official docker image repository. You could publish your own images on Docker Hub as well but you also find many official images there. For example, a base image that comes with just Ubuntu installed, but also an image for the Postgres database if you needed an environment that comes with that database pre-installed. And amongst all these other images you for example, also find a node image here which gives you an environment that comes with Node.js pre-installed. And if you then click on such an image here on Docker Hub you learn more about it and about the different versions of the image are available and how they differentiate from each other, and what's included in this image. And now here I could use node, which is one possible identifier I can use or node:16, which is a different identifier I can use which gives me a image based on Node Version 16. And I can run all my steps of this job in a container that's based on this image because all docker containers are based on some image. As an alternative to this way of defining the container image, we can also add a nested image key and define our image like this. This syntax is important if you also have further information you want to pass into your container. For example, some environment variable values that might be needed by this image. And that's important, environment variable values that are needed by the image itself, not by your steps, that will then later run in the image. Those environment variables which are needed by your steps stay in the env configuration which stays on the same level as container. This nested env field for the container is just about environment variables that are directly needed by the image, and if an image would need such variables it would be mentioned in the official image documentation or if you're using your own image, which as mentioned before, could also be published to Docker Hub so that you can use it, then you would of course know which environment variables that image might need. Here for this node image, no environment variables are needed and therefore that's all I need to specify here to make sure that all these steps which belong to the test job will run inside of the container based on this node image and therefore inside of an environment that comes with Node Version 16 installed. Now this will then all still run on this Ubuntu machine, but this machine will just host the image. The steps running in the image will not have direct access to the machine and will not have direct access to the tools made available by that machine. Instead, they really run in that environment defined by that container. You can still use ever GitHub Actions like the checkout or cache action though, that all still works. GitHub Actions make sure that all these steps still work as before even though they're now running in a container technically. And with that all done, I can of course create a new commit here and give it a message of put test job into container and then push this to trigger a new action, or workflow run to be precise. And what you should see is that as this workflow is being executed that it still works as before and all these steps run successfully but now they're technically running in this extra container. So let's wait for this job to really start here. Here we go, it did start. And what you see is that it, first of all, initialized containers where it basically made sure that docker was set up and the container environment was created, the environment for that one job that we put in a container. Therefore here it actually downloaded that node 16 image which I used as a base and it then performed all the steps needed to spin up that docker container and then all the steps were executed inside of that container. So that is what happened here. And with that, we have the same result as before but technically we ran all these steps in a different environment and that can of course be very useful if you need full control over the environment in which you're running your jobs.

So, that's how you can put a Job, and its Steps into a container, which as mentioned can be useful, if you need full control over the environment. Now, you can also use containers in conjunction with GitHub Actions, to spin up so-called service containers. Now, we did just see how we can put a Job into a container, and for example, run our tests inside of a container. But no matter, if you did that or not. So no matter, if you're using a container to run your Job in or if you're running the Job directly on the runner itself, you sometimes have Steps that would benefit from having some extra service to interact with. In a concrete example, could be automated tests, where your code should work with some database, that's not the production database, because your automated tests should not accidentally, manipulate production data. So, these automated tests, which we might be running as part of our Workflow, as we're actually also doing it here, in this example I showed you with the test Job, where I do have this test step that does run some automated tests that will actually, lead to our database being queried. Such tests should not work with production data because they could accidentally change that data. And even if they don't do that, they still lead to some extra work that must be performed by the production database server. And you of course, don't wanna add more work than necessary to that production database server. And they offer as a solution, you might use a testing database, and that's where service containers come into play, because you could, of course, host a testing database that runs on some extra testing database server. And that is what I also talked about earlier in the course where, for example, with MongoDB Atlas, you could of course, add an extra cluster, that's only used by your automated tests, and your development environment so that you have a separation between your database and your database server, that's used for production. And the database and the database server, that's used for testing. But of course, setting up such an extra server is extra work. And of course, that server might also be running at times where it's not needed, where you're not performing any tests. And then you might be paying for that testing database server. That's why, it could be nice to actually, run that testing database server inside of a container. So, inside of an environment that has everything needed by the database only whilst, your tests are running. And which is then shut down, once your tests are finished. And that's exactly what these GitHub Actions service containers are all about. They allow you to run extra services side-by-side with your Jobs and their Steps. So that they can be used by your Jobs and the Steps. And you could, for example, run a database in a container, whilst, your jobs are running so that your Steps and your Jobs can reach out to that database, that's running side-by-side. And as soon as your Workflow finishes, those service containers will also be shut down. And therefore, these testing databases will only be available when your Jobs are currently running. And of course, using service containers for running testing databases is just one example, but a quite common and popular one. You could use this feature for any service, that's needed by your Jobs, though. So, that's the idea behind this service container feature.

So let's see how we can use this service container feature in our concrete workflow here. And by the way, you don't have to run your job in a container just to add a extra service container. You can use service containers with the jobs that are running in containers themselves but you can also use service containers in conjunction with jobs that run directly on the runner machine. So therefore here, I do have a job running in a container but that is technically not needed. But with such a job running in a container, how can we add a service to this job? Well, you do that by adding a extra services key. And that's important, services, plural, not service because you can add more than one service container for a job. And that's also important. Services belong to a job, so to one specific job. If different jobs need different services, you do set them up with different services settings for those different jobs. So here I added services for this test job. And then nested below services, you add an identifier, a label for your service for the first service you wanna add. And here I wanna add a service that runs a MongoDB database, which can be used by these automated tests. Therefore, I'll give this service a name of mongodb, a label of mongodb but as mentioned, this is totally up to you and could be anything. But here, mongodb makes a lot of sense. Now, I could now add another-service here if I wanted to but here I only need one service and that is that mongodb service but that service needs more configuration. Therefore with one additional level of nesting, you can add more service-specific keys. And now the keys which you can add here are clearly defined by GitHub Actions. And the first key which you always must add is the image key because your services always run in containers. So this is not up to you. They don't run on the runner itself. Services instead always run inside of images. Therefore, you always have to add the image key and then define your base image that should be used by this service. Now, which image you wanna use depends on which service you're starting here. Here I'm using the mongo image, written like this because that's another official image hosted on Docker Hub. Here you find that mongo image, which gives you an environment that comes with the MongoDB database pre-installed. So an environment that runs a MongoDB server with the MongoDB database on top of it. Now, you can learn more about the image and your configuration options here but that is the image I'm using here. However, this image now needs some additional configuration in order to work correctly. To be precise, here we also can, and in this case also must add the env key. It depends on the image whether you need to add some environment variables but this mongo image needs some environment variables. To be precise, you learn in the official documentation here that in the end, this image here need two environment variables that must be defined. Now, this here generally is a totally different definition file but these two environment variables must be added as part of our GitHub Actions definition as well because this controls which username and password will be used for connecting to that database because keep in mind, here we're not connecting to it, we're spinning up a new database instead inside of that container but of course, in order to then connect to that database later from our web app, which is used by these automated tests, we need some credentials. And here with these environment variables, we define those credentials that will be used for connecting. So we configured that database that will be started inside of the container and we therefore also configure the credentials that will be used for connecting. And here one environment variable that must be added for this services container is the MONGO_INITDB_ROOT_USERNAME, and here I'll just set this to root. And the other variable is this password variable, which I will set to example here. Now, these values are up to you but they don't have to be too complex because this is only a testing database that's running on this GitHub Actions server as long as our workflow is running. So it can't really be used by anybody else and it won't be online after the workflow finished. So exposing these credentials here is no problem unless it's the same credentials you would be using for your production database, which you, of course, shouldn't use here for this testing database. Instead here, you can have really simple dummy credentials, like root and example. Now, this also must be the credentials that are used for connecting thereafter and therefore, you should also use the same credentials here for the environment variables that are used by the steps, which for example, run these automated tests and the web server, and which therefore are the environment variables used in code. And here we need the same credentials so that the connection to this testing database here can be established successfully. So that's how we now define our service container here. This now defines that we wanna use this mongo image, which gives us a MongoDB server, which should be initialized with these credentials. And that service is then running side by side whilst our other steps are executing. Therefore, as our other steps start the web server, for example, the web server will be able to successfully connect to this database. At least as soon as we also update that other data here, the cluster address, for example, because that's also important and that's now also where it will matter whether your job runs in a container or not.

So I did show you how you can add a service. In this case here, such as testing MongoDB database, which is running in a container. But now when it comes to communicating with that service container, from inside your Job, therefore there are some things to watch out for, and it will now matter, whether your Job runs in a container, or not. If your Job does run in a container, as it does here, then connecting to container services, is super simple because GitHub Actions will automatically create a networking environment, where you can use the label of your service container, as a connection address. So in my case here, I can just use MongoDB, here, since that is the label I'm using down there. So this label can now be used for establishing a network connection. So that's one important thing. And this only works if your Job runs in a container, as my Job does here. I also must change the connection protocol here, to just MongoDB, but that's a MongoDB specific setting, which I must change here, for this MongoDB image, which I'm using here. And you learn more about how to connect to it here, in the official documentation. But that is something that must be changed to make this work. With that, however, with the correct credentials being used here, and with the cluster address, being equal to that label name here, we got all the settings we do need to successfully run this Workflow, which now does not need any external MongoDB clusters, at all, but which instead works totally isolated, with help of that service container. So here, I'll create a new commit, added MongoDB service container, and I'll push this here. And this of course leads to another Workflow being triggered. Here we go. And as part of this Workflow, the service container will be set up, here, in the initialized containers step. It does download this Mongo image, and does set up that service container. It starts this MongoDB service container, as you see, and thereafter, it runs all these steps, step by step, as before, in this case, also inside of a container. And you will see, that it will successfully run all these tests, and the web server, as the service container will be used. Here, it connected successfully, and now runs all these tests. And therefore now we have a totally isolated Workflow, that runs successfully, that does not use any external testing database, or testing database server. So even if I would remove this public IP acceptance, here, my Workflow would still run successfully here. Now things change slightly, if our Job is not running in a container. So if I removed this, or commented this out. Now the Job will run on the runner itself, and not inside of this container anymore. If you do that, you can still use service containers, and you still set them up as before, here. But now there won't be some automated network, where you can use these service container label for connecting. That won't work anymore. Instead, now you have to use this local host IP address, one, two, seven, dot, zero, dot, zero, dot, one. And if there are any specific ports that are needed, you also must add that port here. In case of MongoDB, the default port used is this 2, 7, 0, 17 port, and I wanna connect to that port here. And even more importantly, I have to open that port here, on my service container. That's done by adding the extra ports setting. And then here the internal 2, 7, 0, 1, 7 port, should be forwarded to that port on the runner machine. So that my process running on the runner machine, my steps, which are running on the runner machine, can connect to the MongoDB database server, running inside of this container, through this port. Now, which ports must be opened, simply depends on which process you are running, in your container, in your service container, to be precise, and which ports are used by these processes. That 2, 7, 0, 17 port is simply the port used by MongoDB. Therefore, that's the address we should use for connecting. And that's the port that should be opened, by that service container, to that runner machine, so that processes running, on that runner machine, can connect to that service running, in that service container, through that port. And with that adjustment made, that is again everything, and we can create a commit, adjusted network settings could be the commit message. And with that, you will see that again, everything will be set up and will be executed, as expected, and this Workflow will run successfully, again, using this service container, but now in conjunction with a Job, that's running just on the runner, and not inside of a container. So here it's now, just setting up the MongoDB service container, and no other containers, and once that was done, all these Job steps will be executed, and it will still successfully run the server, as you see here, where it connected successfully. And thereafter, it will also run all these tests, as expected, and they will succeed. And we're still using this service container, but now, in conjunction with a Job, that's not using containers. And again, it is totally up to you, and simply depends on what you want to do, whether you wanna use containers, service containers, or none of these features.

The only thing that matters is that these images are hosted on some Docker registry, some website that hosts those images and then this can be a very useful feature provided by GitHub Actions that's great for jobs that need extra tools to be installed or lots of customization. Related to GitHub Actions, support for jobs in containers is the feature of service containers, which you can also use if your job is not running in a container. The idea here is that you can spin up extra services that can be used by your steps in your jobs, like for example, that totally isolated locally running testing database which is only available whilst your Workflow is running. So this service container feature can be great for use cases like this and you can also build those service containers based on public images, community images or also your own images as long as they're hosted on some Docker registry.
