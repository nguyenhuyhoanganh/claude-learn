In this course section, we're going to dive even deeper into GitHub Actions, and we're going to explore how we can use Environment Variables and Secrets in our GitHub Action workflows. For that, we will first of all analyze what exactly Environment Variables are, why we need them, and then of course, also see how we can use them and work with them in our GitHub Actions workflows. We'll also explore the idea behind Secrets, what exactly that is and how it can be used and we'll dive into GitHub Actions Environments and understand which possible problem this feature solves. Somehow that feature may be used in our workflows.

Now for this section I prepared another demo project and this now is a different project than what we had in the previous course sections. This is a REST API built with Node and Express, and you don't need to understand Node and Express to follow along, of course. But what is important about this demo project, which you, of course, find attached to this video, is that in some code files, this project uses environment variables. For example, here, whenever you see this process.env thing here in this Node Express code, we are using environment variables. And that of course brings up one important question. What exactly are environment variables? Well, the idea behind environment variables is that in your code you may have certain variables or values that are dynamic. For example, the password that's used for logging into a database, so that's used for connecting your server to a database. That password is maybe not always the same. Instead, the concrete password value might depend on the environment in which your application or website is running. For example, you could have a testing and a production environment, and during testing you might be connecting to a testing database that has different credentials than your production database and they offer different password values might be needed for these different environments in which your web server is running. And that's not some made-up scenario. Instead, this is quite common. Back in this demo project which I'm providing to you, this is the exact use case we have here. I got four variables here, or constants, which are used in this code, which actually get dynamic values from environment variables. These all-caps names here are the environment variables, though the name is totally up to you. It doesn't have to be all caps, but that is a convention you often see in different languages and environments. And the idea is that this connection string here, which is used for establishing a connection from that backend application here to the database, the idea is that that string is constructed dynamically, and that we use maybe a different username, a different password, and even a different database address for different environments in which this application might be running. So that we can, for example, run our tests against some testing database and we don't accidentally change production data. We don't actually work with real user data during our tests. That's the idea behind environment variables. And this application here uses environment variables in this database.js file, but also in the app.js file here for configuring which port will be used for listening for incoming requests. And also in this test config file here where we also specify this port which tells the testing tool where to find our server. Because here I'm using Playwright which is a end-to-end testing tool, which is used for running some automated tests against this API here. So that's what's going on in this project, and the key thing here, also for this section, are these environment variables because in this project you also find an example of GitHub Actions Workflow, which is not finished yet. And this Workflow runs our tests, for example. It runs these automated tests, and of course, that means that the application server must be started so that the tests can then work with that server. Now, since that server must be started, values for the database address and the database credentials must be provided. And those values must be provided in our Workflow whilst our Workflow is running. So we need some way of providing environment variable values as part of our GitHub Actions Workflow here. And that is something you will definitely encounter sooner or later when working with GitHub Actions or any other CICD provider that you need to provide environment variable values, and that's therefore what we'll explore in the next lectures.

So how can we provide values for these environment variables or for this port environment variable? Well, in your GitHub Actions Workflow definition file, there are different places where environment variable values can be provided. For example, you can provide some values right here on the overall workflow level. You do this by adding the env field here on the same level as you have name and on, and the nested below this end field and therefore indented, you add your environment variables. For example here, we could, for example say, that for all jobs in this workflow, we wanna use the same database name. The address and password and username might be different but the name of the database should always be the same because it would be fair to assume that even if we're using different database servers for testing and production, the name of the database on that server will always be the same. Therefore, we could grab this MONGODB_DB_NAME environment variable here, which is used for setting the database name and add this here as a key under env. And then as a value, you provide the value that should be injected in the code when that server, when that backend application is started as part of our workflow. Here, we could for example use gha-demo, but the name is up to you. By the way, here in this example, I'm using MONGODB which is a database engine and a company providing various database services. It is of course, just one example. The general approach and idea would be the same no matter which kind of database engine you are using. And you also don't just use environment variables for databases of course. But here, we now set this environment variable on workflow level since I added the env key here. And setting this on workflow level simply means, that this environment variable, this MONGODB_DB_NAME variable, will be available in all jobs unless a job overrides this variable by defining a variable of the same name, which is not the case here. Now, of course, not limited to defining environment variables on workflow level. You can also define them on job level. For example here, for the test job, we could define some environment variables by adding the env key under this test job. So indented and nested under this test job. Now any environment variables specified here will only be available in this test job and not in any other jobs. And here we could provide our cluster address, the MONGODB cluster address, and also the MONGODB username and password environment variables. Because it would be reasonable to assume that for testing, we wanna use a specific database with its own credentials that probably differs from our production database. Now in this demo workflow, we have no other job that needs to connect to the database but if we had other jobs, then they might be using different servers and credentials and therefore, defining this on job level might make a lot of sense, but it will of course depend on your exact use case. Now, in order to define our values here, we of course need database, and therefore in the next lecture, I'll quickly create one.

Now, in order to continue with a real database that we can use in this example, I quickly create a new MongoDB database by visiting mongodb.com and using their cloud database offering, which is called Atlas. You can get started with that for free, but of course, you can also just follow along by watching the videos. But you can get started for free by creating an account here. And once the account was created, if it's a brand new account, you can create a new database. And again, this will be a database hosted by MongoDB in a cloud environment provided by them. And you can get started for free with such a shared database. And I'll just keep all these free options here to make sure that I'm not paying for it. So therefore, I'll keep all the defaults to get started for free. And as mentioned, you don't have to follow along, but here I will now also create a user, username Maximilian, and then a password, which I autogenerate, and which I will also copy. And that value combined with the username is already the first value I wanna store here. So the password and the username. And I will say right away that storing credentials like this in the Workflow file is not ideal and we'll dive into a better solution in the next lectures. But for the moment, I'll just add that dummy data off that dummy database here. And with that, we can also finish this database creation here by creating that user and adding my current IP address and finishing this. And now with that created, I can click this connect button here and connect your application to get this address here off the database. This part here. And that's what I'll enter here as MongoDB cluster address. With that, I am providing all these environment variable values and data for later when my application runs in this environment, so on a runner hosted by GitHub Actions. These values will be provided to the application, so they will be available here and they will be used in that running application to successfully connect to the database. Now, for that, I just also must add it something here and go to network access and actually add another IP address and allow access from anywhere so that my application running on a GitHub Actions runner is able to connect to this database. But with that, I got this database created, or at least soon it will be created, and I got these values for these environment variables provided here as part of my Workflow file. There is just one last environment variable missing, and that's the port environment variable, which is used in app.js and in this playwright.config file. And then of course, also must be provided so that the web server is spun up successfully and lists is on the right port. And we can also do this on Workflow level or Job level if we have different jobs that should be using different ports, and I'll do the latter here and provide this port environment variable on Job level and set this to a value like 8080 or 3000, whatever you want.

Now in the previous lecture I did add some environment variables and provide some values for them. And I did create that cloud database here, which I'll use here to really show you how this works in action. But I'm not fully finished with this Workflow definition file yet. Because there are some parts of that file that still must be added, for example, down here, this command in the end makes sure that the server, the web server, is started and it must be started in order to run the tests thereafter. But this part is then important to make sure that the next steps can be executed without shutting down the server first. And here I must enter the port of the web server. So the port on which I'm listening. Now of course the port is dynamic. I do set it here with this environment variable. So therefore, this environment variable is not just used in my code and configuration files. It's also used to write in here in this Workflow definition file. And that's not uncommon, that you have some environment variables that also must be used inside of the Workflow definition file. And thankfully, using environment variables in there is quite straightforward as well. Whenever you need to use an environment variable in such a run instruction, you can interpolate it by using dollar sign and then the name of the environment variable, in this case port. Now the exact syntax can be different if you're using a different shell here. If you're running this on a Windows machine, for example instead of a Linux machine as I'm doing it. But for a Linux, this is the syntax you use in a default shell to interpolate and output the value of an environment variable. On Windows where the PowerShell is used by default, this would be the syntax you use instead, for example. In general, you can also use the Shell key when defining a step to switch from the default operating shell to a different one. For example, instead of using Bash, which is the default on non-Windows platforms, you could switch to Python to execute all your commands as Python commands. And therefore, if you are switching to a different shell, by adding this shell key to your steps, you might need to change the syntax for interpolating environment variables, but in that case, the official shell documentation is the place to go. So here on Ubuntu latest in that default shell this is the syntax for interpolating and outputting an environment variable. But there also is another way of getting access to an environment variable value in your Workflow definition file. And we can take a look at this way down here, where I wanna output the value of an environment variable. For example, let's say here, I wanna say MONGODB_USERNAME. So this is just some text here, with that I'm not outputting anything, but thereafter I want to output the MONGODB_username environment variable value, which should be maximilian in my case. I could of course, again, interpolate it like this. That's what we saw before already. But we can also use that other syntax we already saw before in the course, this expression syntax here and access a specific environment-variable context object provided by GitHub, and that's the env object. So just as we had the steps and the needs and the GitHub objects in earlier course sections, here we have the env object which gives us access to all the environment variables that are available in this job or in this step here. And therefore here I could access env.MONGODB_USERNAME to output that value. So that's how we can output it, and I'll also repeat this down there in that other job also to show you that here some values will not be available because these are job specific environment variables. This on the other hand here is a Workflow environment variable, so that value should be available here. If I output MONGODB_NAME, for example, with interpolation or by using the env context object, doesn't matter. This value here should be available, this here should not be available in this job. So that's how we can output these values, how we can use them in this file and how we can define them so that they are also available in the code that's executed in this environment. So that's executed on this job's runner. And therefore, with all of that out of the way, this database created and the network settings configured as shown before, this should all work and we're ready to create a commit and push this to a remote repository. So therefore, here, I'll initialize this and then add my commit where I say initial commit, for example. And now I just need a remote repository. So therefore here I'm back in my GitHub account and I will create a new repository gha-env. It will be a public repository because later I want to show you a feature that requires a public repository. And with that, I'll create it. And then as always, grab that URL and add this as a remote. And then push this, with help of this command which is required initially. So now this is pushed, and since I have a push trigger here that should of course also lead to this action being executed. And it looks like I have an error in my YAML syntax in line 32. Yeah, let's move this to new line by writing it like this, so that this is interpreted correctly again. Fixed syntax and push this, and now this should execute. Here we go. And now I'll wait for this execution to finish and see whether everything works as intended to then see how these environment variables were used. So here for me, this all finished successfully. If it failed for you make sure that you did create your database correctly, that you did allow all IP addresses to connect and that you are using the correct credentials for your environment variable values. And if I now inspect my jobs, you see that first of all obviously the environment variables were injected in the code, otherwise the server wouldn't have started successfully. So it was able to connect to the database and that's only possible if the environment variable values were provided. So that obviously worked. And we can also see that under output information, I am outputting that MONGODB username and that is that value that is provided in the Workflow file. Under deploy on the other hand, we see that there I'm not able to output the MONGODB username. There is no value here, because this job doesn't have access to this username. Because if we take a look at our Workflow file again then this deploy job has no job-specific environment variables and also no step-specific environment variables. And the environment variables of the other job won't be available here. Therefore, it only has access to the Workflow environment variables, which in this case is only this one and therefore that environment variable and it's value is also output successfully. But the other environment variable, which is only defined in the other job, is not output successfully. But this shows us how environment variables can be set and how they can be used.

Default Environment Variables You learned how to set and use your own environment variables. GitHub Actions also provides a couple of default environment variables that are set automatically: https://docs.github.com/en/actions/learn-github-actions/environment-variables#default-environment-variables These environment variable can, for example, give you quick access to the repository to which the workflow belongs, the name of the event that triggered the workflow and many other things.

So, that's how we can use environment variables. And this is an important feature because environment variables are needed in a lot of Workflows to inject values, as we're doing it here. But we also have values, like these credentials here. Like, the username and the password, which we don't necessarily, wanna store in our Workflow file. Because you must keep in mind, that we pushed this to GitHub, so therefore, these credentials are on GitHub. Everybody with access to this Repository, can view these credentials here. Sure, that's just a testing database here, but we could have some production credentials here, as well. And even for the testing database, we typically, don't wanna expose such values. And that's where secrets come into play. Some environment variables, like these credentials I showed you, should never be exposed. They shouldn't be part of your code, or your definition files. That's why GitHub Actions and basically, all providers that provide services like this, allow you to store so-called secrets. These are essentially, also environment variables, but stored, such that no one can access them, no one can see them. And when using GitHub, there are a couple of places where such secrets can be stored. If you would have an organization account, you can store them on organization level by going to your settings. But I don't have that account here. So therefore here, I can't store my secrets on organization level. But you can store them on Repository level, by going to the settings of your Repository. There, under security, you have that secrets area. And there, you can store action specific secrets. So, values that will be available in all actions, in all Workflows, that are executed for this Repository. You add such a secret, by simply adding a new Repository secret And then you give it any name of your choice. Though, it makes sense to pick a name similar to the environment variable name here, but it doesn't have to be the same one. Here, I will use the same one though, my MongoDB username, and also store the username value here. So, the value goes into the secret box. And then after adding this, this can't be viewed anymore. You can update it, but you can't view it anymore, and nobody can view it. Especially, not people that might be visiting your Repository. Now, I will also add a second secret, and that's my MongoDB password. And here, of course, I also copy that value, and provided, like this. And now, these two credential values are stored as secrets. Now, we must update our Workflow file to remove the values from there, and reference those secrets, instead. And that can be done by using GitHub Actions expression syntax, with the dollar sign and the double curly braces. And then there's a special secrets context object. So just, as we have the end context object for accessing environment variables. We have the secrets context object for accessing secrets that are provided for this Repository. And since, I just added two secrets to this Repository. These two secrets here. I can access these two secrets and their values via the secrets context object. So here, I can access secrets.MONGODB_PASSWORD or to be precise, that should be down here. And here, it's secrets.MONGODB_USERNAME. So, that's here, for this first environment variable. And with that, when this Workflow runs the actual values stored as secrets will be injected, but nobody will see that. The actual values will never be exposed anymore. Actually, when you try to print such a value, as I'm doing it here with the username, you will later see that GitHub Actions recognizes this, and will actually hide this value so that you can't accidentally, print it and expose it, in your Workflow logs. So, GitHub Actions really takes care that your secrets stay secrets. So therefore, with these changes made, with those secrets added, and the Workflow definition file updated, I will create a new commit, switched to secrets, and I'll push that updated code, and therefore, now of course, will trigger another Workflow run. And that Workflow run should also succeed, but this time, it will be using secrets under the hood. So, let's wait for that to finish here. Here we go. And now, if we take a look, we see that both workflows finished successfully, and in the first Workflow, in the first Job, sorry, both Jobs finished successfully. In the first Job, under output information, you can see that there GitHub Actions actually hit that value, that username value, because it saw that this is actually a secret, and therefore, it's hiding it, when you're trying to expose it. So, these secrets, really are secrets.

Now, in the previous lecture we got started with those secrets and we added two secrets as repository secrets. Now, you might have seen that there also are environment secrets, and you shouldn't see this if you are on a private repository in the free plan because environment secrets require the environment's feature which is only available in public repositories under the free plan or private repositories if you're paying for GitHub. But what are these environments in the first place? GitHub offers this environments feature for your actions workflows. And the idea behind these GitHub environments is that you attach them to repositories and workflow jobs can then reference these environments. And if a job targets such an environment you can, for example, provide extra configuration to that job. To be precise, extra secrets that are only available in a certain environment. You can also add special protect rules to, for example, make sure that no matter what the job and the workflow defines only pushes to certain branches can lead to a certain job being executed. But let's take a look at that in action. Here at the moment on this demo repository, I have no environments yet and you can add environments under Settings, Environments. Now, the idea is that you could, for example, set up a testing and a staging and a production environment and maybe all environments provide the same secrets. The secrets needed here in this workflow file, these secrets, maybe you provide the same secret names in all environments but you might have different values. And that of course, wouldn't be unreasonable to assume. Because let's say that you are connecting to a testing database in the test job, but you wanna connect to a different database to the staging database in your staging job, for example. So the same workflow might have different jobs that should connect to different databases with different credentials. Therefore, just having a general MongoDB username and password secret might not do the trick because you need different user names and different passwords depending on which job is currently executing. And at the moment, we just have general repository wide secrets which are always the same. So if you wanted different secret values for different jobs, we would need to add more secrets here and give them different names. With environments you can work around that. We can create a new environment, for example, testing. And now in that environment we can add our environment-specific secrets. So for example, here, I could again add MongoDB username. Make sure to not introduce a typo. And also add another secret, my MongoDB password. And here I'll grab that password from my database configuration here. I will actually auto-generate a new password and use that and update my user with that new password and use that value here. And now in that environment, we're using these secrets. Now, of course, the question is how that environment can be used. And for that, back here in your workflow definition file, you reference an environment inside of your jobs. So inside of a specific job, you can add the environment key. Not env, that's used for defining environment variables, but instead environment written like this. And then the name of the environment that should be used for this job. So now the test job uses the testing environment which is that environment I just created here. That's the testing environment. That's the one I just created. And therefore, this job, this job here uses this environment. And therefore, whenever this job accesses my secrets here, it will be using the secrets that are stored for this environment and not the general repository wide secrets which we stored before. I could indeed delete these repository wide secrets and the job would still work because it's using these environment-specific secrets now. And, of course, you can add multiple environments and use different environments in different jobs. Now, adding secrets to environments is one thing. You can do more with environments. For example, you can require special reviewers to make sure that whenever a workflow executes, a reviewer has to approve that workflow before it actually starts running. You could also add a wait timer so that every job pauses for 15 minutes before it actually starts. And you can also add some branch protection. And for example, make sure that a job that uses this environment will only be executed if it was triggered based on a branch you selected. For example, here I could add a deployment branch rule and say that only if the main branch was part of the event that triggered the workflow execution, only if that branch was part of the event, jobs that referenced the testing environment should execute. So if I would push to a different branch, to the dev branch, and that would trigger the workflow execution, any jobs referencing the testing environment would not be executed. And in general, this workflow can be triggered if I push to the dev branch. But thanks to this rule, this test job would not execute. And I will save that rule, so I will add this rule and keep this change so that I can also show this to you in action. So with that, I finish my environment configuration. I added that rule and my secrets. I updated the workflow file by adding this environment to the test job. And therefore, now if I create a new commit, add an environment, and I push this, you will see that here where I'm pushing to the main branch this will all execute successfully because it's now using these secrets from that environment and it's passing this protection rule. But if I later try the same by pushing to the dev branch, you will see that it will fail. But first of all, let's wait for this execution to finish here. Here we go. So the secrets were obviously picked up successfully, otherwise this wouldn't have finished successfully. And if I now go back and check out a new branch which I named dev and I now push this dev branch to my remote repository, you will see that the workflow also will be triggered because I have this trigger added to my workflow configuration file. But nonetheless, you see that it actually failed because this first job, this test job wasn't executed because the branch dev is not allowed to deploy to the testing environment due to this protection rule that I added.

And that's it for this course module. In this module we did take a look at Environment Variables which are used to inject Dynamic values into your code. For example the name of the database or all the use them inside of your Workflow definition file as you saw. Now these values these Environment Variable values might differ from Workflow to Workflow and you can define them on a Workflow-Job or Step-level. We didn't see Step-level in action in this section but you can add this End key which we added to the Workflow or a Job also to a specific Step, if certain Environment Variables are only needed in that Step. Now as mentioned these Environment Variables are then automatically provided to the code that's executing on your Workflow-Job runner. And the Environment Variable values are also available inside of the Workflow definition file. So you can for example interpolate them when running commands or use the env context object to get access to the Environment Variables. We also have Secrets which are kind of related but where the idea is that you are storing values that must not be exposed as Secrets. And typical examples would be Database credentials API keys or things like that. Now these Secrets can be stored on Repository or Environment-level and for organizations also on Organization-level. And you can then reference these Secrets via the Secrets Context Object in your Workflow file as you also saw it in this course section. Last but not least we did take a look at these GitHub Actions Environments where the idea is that different Jobs can reference different GitHub Actions Environments. And in your Environments you can add extra protection rules to for example deny the execution of certain Jobs under certain circumstances. But you can also specify Secrets on Environment-level so that different Jobs can reference different Environments and get different values for their Secrets.
