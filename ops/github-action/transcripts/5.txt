At this point in the course, we're able to write solid GitHub Actions workflows that do a variety of things. But also at this point in the course, we're only able to write workflows that always execute from A to C. So from start to finish and that stop execution if an error occurs. If something goes wrong, if some step fails then the job to which the step belongs is canceled. All subsequent steps after the failing step are not executed and all other jobs are not executed. If one job fails because of a failing step. That's the default behavior and often that behavior makes a lot of sense. But sometimes you want to continue executing steps even if one step failed and some steps should even only be executed if another step failed. And it's not just about failing steps. In general we typically want more control over when exactly which job and step executes, because it's not the case that you always want to execute all jobs and all steps. Instead, often you have some conditions. And therefore discourse section is about controlling execution flow. It's about controlling when a job executes and when the steps in a job execute. You will therefore learn how to run your jobs and steps conditionally, and which tools GitHub Actions gives you to control that. And we'll then also dive into other execution flow related topics, like for example using an execution matrix or a job matrix, and you will learn what that is and when you might want to use that feature. And we'll explore the concept of reusable workflows, which can save us some time and some extra writing.

Now let's dive right in and let's start with executing jobs and steps conditionally. And we have two main tools for that. For one, there is the if field that can be added to both jobs and steps to add a condition to a job or a step, and only if that condition is met, that job or step will be executed. Steps also have a second field that can be added. The continue-on-error field, which allows you to specify that the other steps of the job should still execute even if a single step fails. And for both the if fields as well as the continue-on-error field, you can add your own conditions with help of Expressions, but that's exactly what we'll explore in practice over the next lectures.

So I'm back in a brand new project which you as always, find attached, and in general, the project isn't that new. It's that same react application we worked on earlier in the course, because again, it's not really about the application, it's about the workflow, and as you can see, I'm providing a starting workflow to you here, and this workflow works as it is. This workflow reacts on pushes to the main branch and then simply lints the code by running this linting script. It then also runs some automated tests and uploads the test reports with the upload artifact action, and I then also have a build step where I run the build command and also upload those build files. Last but not least, there is the deploy job which doesn't really deploy anything but it does download these build files which would be uploaded to some hosting provider, and then we're just outputting some dummy content. Now, one quick note regarding the test job. There, I'm uploading the test report and that is something I haven't talked about earlier in the course. In the end, this test script just produces a report in this project because of the configuration I provide here in this vite.config file, this test.json file is generated to be precise, and it's this test.json file that's uploaded with help of the upload artifact action, about which we learned earlier in the course already. So that's this demo workflow, and right now there is nothing conditional in that workflow because that's exactly what we'll explore and add together throughout this course section.

So let's dive right in, and let's add some conditions. And of course there are various places where we could think about adding conditions. But here's one thing I wanna do. Uploading that test report. You could argue that we're only interested in that test report if our tests fail. If all our tests work, we realistically might not evaluate that test report anyways, and therefore uploading and storing that test report only makes sense if we got some failing tests. Therefore, we could add a condition here, to this Upload test report step, by adding the IF key, that ensures that this step only runs if this step that runs the tests fails. And that's, by the way, important for another reason as well. If this test step fails, this step normally wouldn't even be executed, because, as explained a couple of minutes ago, the default behavior of GitHub Actions is that if one step fails, the entire job to which the step belongs is canceled, is aborted, and therefore this step wouldn't execute typically, if that step failed. And the other jobs, by the way, typically also wouldn't be executed, if any step like this step in another job fails. So therefore let's actually quickly evaluate that default behavior before we add that IF field. And therefore, I'll go to this main content test file, and make sure that my test fails, by removing that 'a' here. With that change made, I can create a new commit broke test, and now we can push this to a remote repository. Therefore, I'll quickly create one here, give it a name of gh-executionflow, name is up to you. I'll make it a private repository, but it doesn't matter for this section. I won't dive into any features that would require a public or private repository. And then with that URL copied, I'll quickly connect it, and thereafter we can push with that well known command. So now this is pushed, and since I'm pushing to the main branch, the action that was added will be executed right away. And as you will see, my test will fail. By the way, here you also see that I got three jobs that run after each other, and two jobs that run in parallel. The test and the lint job run in parallel, and build and deploy depend on the test job to finish first. But let's see what happens if the test job fails here. Here we see that it failed, and as a result build and deploy aren't even executed, as you can see. And the lint job still finishes, because it ran in parallel, it was started before this test job failed, but build and deploy, since they depend on the test job, don't even run. That's the default behavior we have here. The other dependent jobs were canceled because test failed. Now let's add this IF condition.

So let's now add this condition here to this upload test report step. For that, we can add our if field here and then here we write the condition that must be met to execute this step. And the condition we write here can be wrapped in this GitHub Actions expressions syntax here with the dollar sign and the double curly braces since we will evaluate an expression here since we will look at some dynamic values here but the if field is actually special since you typically use expressions here because typically, you don't just wanna hard code true or false here because in that case, you could just omit the if clause all together because that would always be true or false. Since you typically need a dynamically derived value here, GitHub Actions allows you to omit this syntax here. It allows you to omit the dollar sign and curly braces even though you can still add them if you want to but you can omit them and you can also just write your expression just like this. That's an exception for that if field because you typically need dynamically derived values here. Now, here we wanna make sure that this runs if this step failed. That's essentially what we wanna do. If this step succeeds, this will run anyways and actually, we don't want it to run in that case but therefore, if we would add a condition that makes sure that this step only runs of that step failed, we would make sure that it does not run if that steps succeeds, so if our tests succeed but on the other hand, it does still run if our tests fail, which currently is not the case as you just saw. Now, therefore, we need a way of referencing this step. We must find out whether that step failed or succeeded, and to do that we can add the id field to that step. Now, we did see the id field in action a little bit earlier in the course already. You can add the id field to any step you have and that allows you to assign a unique id to every step which then can be used to reference that step and use data from that step in other steps or in your job. For example, we used it earlier in the course to get hold of some outputs that were set in a step. Here I'll give this step an id of run-tests but the id name is totally up to you. It doesn't have to be run-tests. And here in the if condition, we can then reach out to the special steps context object, which is one of the context objects supported by GitHub. Here it's this one. That's the steps context object, which we're using now. And on this steps object, we can reach out to other steps by their IDs, so now here I add run-tests to reach out to this specific step. And then we learn in the official documentation that on this steps context object, when we do reach out to a specific step by its id, we actually can get information about the outputs produced by that step, which is what we did earlier in the course already but we can also get the conclusion or the outcome of that step. And in the end, these two fields simply tell us whether the step succeeded or failed. Now, we've got two fields here, conclusion and outcome, instead of just one field because we also have this continue-on-error field, which we'll explore later, which actually allows us to continue after a step even if it failed and in that case, we've got one field that tells us whether the step would have failed without continue-on-error but then actually was treated as a success because of that field added but since we don't know that field right now, we can ignore it and we can simply use the outcome here. So here we can use steps.run-tests.outcome to find out whether that step succeeded or failed. And in the end, both outcome and conclusion then contains some text that tells us whether the step succeeded and we get, for example, success as a text if it succeeded, failure if it well, failed, canceled if it was manually canceled or skipped if it was skipped maybe due to some other if condition that was attached to that step. So in our case here, we're looking for the failure text because we know that if this step failed, this run-tests step failed, then we wanna upload our test reports. So here we check if the outcome is equal to failure. And failure is wrapped between single quotes because that is some text. It's not some context object or anything like that but it's some text, it's a string to stay in the programming world and therefore, it's wrapped with single quotes. And the double equals sign operator here is used for comparing this value for equality to this value. So whether outcome is equal to failure. A single equal sign is not enough. You need two equal signs here. By the way, there are various operators you can use in your if conditions. For example, that double equal sign operator to check for equality but you can also check for inequality by adding an exclamation mark. You can combine multiple conditions with logical ands, or ors. And you can also look for greater or smaller or greater or equal or smaller or equal values. So you can basically do what you know from if conditions in programming languages. You have a very similar syntax here with GitHub Actions. Now, with this condition added to this step, what we should see is that this step here runs if this step fails but does not run if this step succeeds. So if we now create a new commit here, if we stage our files and create a commit and push this, we'll see whether that works. And we see that as this executes, our test step fails as expected but we still skip this upload test report step. So it's still not working as we want it to work. I can tell you that indeed, this step, the upload test report step would be skipped if test code succeeds but we haven't met our goal of running upload test report if test code fails. And that, of course, is an important goal to achieve. Now, this step here should execute when this step fails but that's not happening here and the question is why is this not happening? Well, the reason is that yes, our condition here does indeed check whether that step failed but GitHub Actions still ignores this step because it still has its default behavior of not executing any other steps after this failing step. To change that default behavior, we must add a special condition here. We must execute a special function here in this if condition of this step. And that's the failure function written like this, which we combine with that other check with this and operator. Now, what's this failure function here though?

GitHub actions has four special functions which you can use typically in your if conditions to be precise. Failure, for example, the function we just saw in our code, in our workflow always returns true when any previous step or job failed. Success returns true when none of the previous steps have failed. Always, always returns true. So it makes sure that any job or step that uses always in its condition will always execute. And canceled returns true if a workflow has been canceled. So these functions can be used in your conditions to ensure that certain steps only run when other steps failed or succeeded, or that they always run or only run if a workflow has been canceled. Now by adding failure to this condition here we make sure that GitHub actions realizes that this step should still be evaluated if we have failing steps or jobs before that step. And it then also takes this second condition here into account since I combined them logically with the end. So if for some reason installing the dependencies would fail failure would still return true but this condition would not return true because this step here running our tests wouldn't have failed because it would've been skipped instead if installing dependencies failed. And therefore uploading test report would not run if installing dependencies or restoring the cache or getting the code failed. But if running our tests failed, then failure returns true and leads to GitHub actions evaluating that step. And then this condition makes sure that this step is executed if run tests is the reason for this job failing, so if that is the step that failed. So now if we save everything and we create another commit where we added failure, we can push this. And now we'll see that our action of course is triggered again, but this time in this test job, it should run this upload report step even though we are failing to run our tests because we added this special failure expression. So here our test code failed but now you see upload test report still executed. That step was still executed, it was not skipped and therefore, for the overall action here, added failure we also can retrieve our test report as an artifact down there because it was uploaded despite this job failing, or actually not despite this job failing but because that job failed. Because in our condition we clearly specified that uploading test reports should only run if that step failed.

So now we got started with that if field on a step to ensure that a step runs even if a previous step failed. Now the if field is also available on job level so that we can add it to jobs instead of just steps. And to show this at a concrete example, let's say we have a new job here, which I'll name report which will run on ubuntu-latest. And where I have a step called output information where I simply run some echo command here where I say "something went wrong." And then let's say we actually output something across multiple lines and we then output the GitHub context object like this. So this is now a new step in a new job and my goal is to run this job only if some other job failed. Now, if that's the goal, we can add a if check here and use this special failure function to make sure that this job will run if any other job failed because failure will return true here if any other job failed, and only then this job will be executed. That's the idea here. So now with that added, we can create a new commit "added conditional job" and then push this to the repository. Now there, this workflow of course is now triggered and what you'll notice is that now report here is actually skipped. This I can hear signals that it was skipped. Now it was skipped because the way we configured it actually runs this job in parallel to all the other jobs. That's why it's also shown visually below the other jobs. It runs in parallel. That means that it's started right at the beginning of the workflow run. And there of course, no other job has failed yet. So GitHub is not waiting with this report job until all other jobs have succeeded or failed. Instead, it will execute report right away. And there, this failure condition simply is not met. So this alone won't do the trick. Instead of our goal is to run this job only if any other job failed, we must add the needs key here and then need both the lint job. So this first job here as well as the deploy job which is the last job in line after build and test. So now we add these two jobs as prerequisites for the report job and therefore GitHub Actions will delay the execution of the report job until these other jobs have finished. And therefore now it will evaluate if any other job in front of that job has failed. And it will not just look at lint and deploy, but instead at the whole chain of jobs that might belong to these jobs. For example, deploy only runs after build ran and build only runs after test ran and GitHub Actions will look at all these jobs when looking for failures when we set it up like this. So therefore now, if we create a new commit "fixed conditional job" and we push this to our repository. You will see that now this graphic here changed. Now report clearly depends on lint and the result of this entire job chain. And now we will see that when testing fails, when this test job fails, the report job therefore executes. So lint succeeded. That does not trigger report to run, of course, but if test fails, then you will see that report starts executing because ultimately deploy was skipped because test failed. And as mentioned, the entire chain is evaluated and therefore the report job executed and output that information as requested. And here as a side note, we're just seeing object because I forgot to turn this into adjacent string, so into text that contains the data information. But that's just a side note, not related to the if statement. Instead, this is how we can use the if statement on a job. And of course you're not limited to just failure, success, canceled, so to these main functions, but here you could also add other conditions. You could, for example, check the output of another job or do anything you want. Your imagination is basically the limit here.

Now before we move on to other features, there's one last thing I wanna do with this if field. And for that I'll go back to step level. We got this caching step, and currently what we're doing here is we're caching this global npm cache folder and they offer this as restored when we have it cached and the dependencies didn't change, and therefore this npm ci step will be a bit faster because it will use this internally managed npm cache. It would be even faster though if it would cache the entire node modules folder which is the local folder with all the dependencies of this project installed. And we would not even run the npm ci step if we restored that folder. Because skipping this step also skips all the setup work related to that step and skips the part where npm internally checks whether it has its cache folder. So therefore we can speed up this workflow, this job and all the other jobs that use caching, by not cashing this global npm folder but instead the local node modules folder. And now I wanna skip this step here, this install dependencies step, if we successfully used our cache. Now to do that, we can add a if condition to this installed dependencies step. And now we just did a way of finding out whether we successfully got data from the cache, so if we had a cache hit as it's called. Now if we take a look at this cache action which we're using at the official documentation, we learned that this action actually has an output, the cache hit field, which is a boolean value, so true or false, that indicates whether we had a match, so whether the cache was used. And therefore we can look at this output and use that as the condition or in the condition of the install dependencies step. Now, I did already add an ID to this cache dependencies step so that we can get data from that step with help of that ID. And we can therefore use this steps context object use the cache step, so the step with this ID here, the cache ID, and then here dive into the outputs field. And now it's not an output set by us, with this set output command you learned about earlier in the course, but instead now it's an output of this action which is executed in this step and that output is the cache-hit output. And here we simply check if that's not equal to true. Now I'm wrapping this in single quotes because it's actually converted to a string in this steps context object so therefore I'm checking for the string value. And I'm checking if it's not true, because if it isn't true, if we were not able to use the cache then I wanna manually install the dependencies again. If on the other hand we had a cache hit, so if this is true then this step will be skipped with this condition. And now we can use this setup in all the places where we use the cache and install dependencies to always use this extra if condition and store node modules instead of this npm folder. So I'll do this in all these places here. And with that done, we can of course stage these changes and create a new commit, optimized cache and push this, and what you'll notice now is of course that as this executes it's the first time that we're caching node modules. So therefore, in test and lint, which run in parallel, they both don't have node modules cached initially therefore they install the dependencies. This step is not skipped but executed because dependencies were not cached before but the next time we run this, the cache will be used. Now since I haven't fixed my testing code yet we don't see this yet because build and deploy were skipped, otherwise the cache would've been used in these steps. But we will see it if we rerun this job, however, I will take this opportunity and fix my test here by readding this A, to make sure that in the future we always execute all these dependent jobs. So now here. I've fixed my tests and I will push this. And with that done, we will see that now this entire chain will execute, therefore, report will not execute anymore by the way, and we will see that now the cache is used now even in these first jobs already because it was cached previously. So here the cache was received and therefore, as you can see install dependencies was skipped because of our if check. And I wanted to show this example to you because that's never thing where you can use if checks, and this also shows you that you're not limited to using them only for checking for failure. Here we're not checking for failure at all we're just checking which result was produced by another step, and depending on that we either execute that step or not. And here, by the way, as mentioned, you also see that report was skipped because all jobs succeeded and therefore the condition of report was not met because the condition of report is that at least one job failed, which was not the case here.

So that was a lot of information about if and it is an important field because being able to run steps and jobs conditionally is important. But we don't just have if, we also have the special continue-on-error field and I also wanna show you what this field does. Now to show you what it does and to also see a comparison to the if field. I'll copy the execution flow file and name it, continue yml. And now in here what I'll do is I will remove this upload test report if check here and instead here on the run tests step, I will add the special continue-on-error field and set this to true. Now we could also use an expression here to derive the value dynamically but this is often something which you wanna hard code because typically you know whether this step should not block other steps from executing even if it fails, because that's what continue-on-error does. If you add it like this, if you set it to true you are saying that the job should continue execution even if that step fails. So the next step and also the next jobs thereafter will still be executed even if that step failed, that's what continue-on-error does. And that might sound similar to adding a if check here, but you'll see that it's not the same. And to show you that it's not the same I will break my test again by removing that A and I will add a new commit, create a new commit. Broke tests, added continue-on-error, and then push this. Now we can take a look at our actions and we now got two workflows running here because we added two workflow files with the same name, which actually can be confusing, I'll change this actually right now, but we'll still see the difference. I'll name this, continue website development since it's about continue-on-error. But nonetheless, here we will see that both workflows ran and we'll see a difference between these workflows. But I'll wait for these workflows to finish to show you what the difference is. So now both workflows finished and you can already see that one of them succeeded and the other one failed. And keep in mind, these workflows were triggered based on the same commit and all we changed is that we added continue-on-error and removed the if check on upload test report. Now the workflow that failed is the old workflow, which we worked on before. There, the test code step failed and therefore the upload test report step was executed because of our if check. But since test code failed, the overall job still counts as failed and the other jobs, build and deploy, were skipped. Because we only executed upload test report conditionally but this does not change the result that a step failed and therefore the overall job failed. Now the other workflow succeeded despite test code producing an error, you see the error here as well but you see that we have a check mark here because we added continue-on-error to that step This line of code here. So the workflow that succeeded overall is the workflow that has this line because continue-on-error does just what the name implies, it treats this step as a success despite its technically failing because we're basically overriding that by telling GitHub Actions that even if we have an error here we want to continue and treat this as successful. That's why the next step in line executes even without an if condition, because this step didn't fail officially. And all the other jobs also execute because again, test didn't fail because test code officially didn't fail, that's the difference here. And that's also the part where this conclusion and outcome value becomes important. You might remember that's the value we were able to access on a single step with help of the steps context. Now the conclusion field simply tells us whether the job is treated as a success after continue-on-error was evaluated. And typically that will be success because continue-on-error has the goal of treating the step as a success even if it failed. Now it could still fail for technical reasons if the runner somehow crashes and you could still manually cancel the job, and it could also be skipped if there was some if condition attached to the step, if that caused the step to not even be started but in most cases you will get success here. Now, outcome on the other hand is the value before continue-on-error was evaluated. Therefore, as it also says down here, when continue-on-error is set to true, outcome can be failure but the final conclusion is still success. And that's in the end what we had here in our example and that's what continue-on-error does. So should you use continue-on-error or if? That's the big question. And the answer is it depends, it depends on what your goals are. If you want your entire workflow to continue, even if that step fails, sure, use continue-on-error. If on the other hand you just wanna run some selected other steps or jobs and not the overall workflow, use if checks as we did it before instead.

Now that's it for if and continue on error. Now there is never feature which I also wanna explore in this section. And that would be matrix jobs or running jobs with help of a matrix. Now what's that about though? For that, let's quickly create a new Workflow Matrix Demo which should be executed once we push. And then here I'll add one job, which I'll name build. And this job will run on Ubuntu latest let's say, and will then receive some steps, and the first step will be to get the code. So that's the name of that step. And then here we use the checkout action as we did before in this course. And then thereafter we have another step where I want to install nodeJS. Now it would be pre-installed on Ubuntu, but let's say we wanna use a specific version, so here we can use the setup-node@ for that. And then we can specify the exact node version we want like 14 for example. And then I install my dependencies, and we could add caching in front of that, but to keep things a bit simpler, I'll not do that here. And then I will build my project. Build project by running npm run build. So that's a very simple Workflow we could execute and it of course does not use any features we wouldn't have used before. But now I wanna run this job this build job with help of a matrix. And the idea behind a matrix simply is that you can run the same job with different configurations for example, on different runners or different node versions or any other values you might be interested in changing at the same time. You do that by adding the strategy field to your job, so directly to the job to which it should apply. And then the matrix below it. And then here under matrix, you can add any keys of your choice, for example, node-version, but this is totally up to you. It can be any value that could be changing and for which you wanna run the job for different values. For example, here we could specify multiple node versions 12, 14, 16, and we could also add the OS key or the operating-system key, whatever you want. And also specify multiple values here, multiple runner values, like Ubuntu latest, and windows latest for example. So this now specifies a couple of different values for keys we're using in our configuration down there. For example, for the runner, we can actually turn this into a dynamic field by using an expression. You can use this expression syntax basically everywhere in your Workflow file. And here we're now using it to set the runner dynamically. It's not something we did before in the course but it is something we can do, and it's often done in conjunction with such a matrix strategy. Because we can now use the special matrix context object which just like the GitHub context object or the steps context object is provided by GitHub Actions. And then access operating-system here. So this key, so one of the keys we define for the matrix. And what this will do is it will tell GitHub Actions to run this built job multiple times, once per value, in that operating system array in that list of operating systems. And those jobs will be run in parallel by default. And we can also use the node versions down here to install different versions of node. For that, we can use the expression index here, and access the same matrix context object, and now the node version key. Now it's a matrix because now actually this job will run for all combinations of these values. So it will run for Ubuntu latest and versions 12, 14, 16, and Windows latest and versions 12, 14, 16. And therefore this matrix feature allows you to quickly run the same job with different configurations. And again, you could use any values anywhere in your steps or in your job configuration. Again, your imagination is the limit. And this feature can be useful because sometimes you have for example a testing process that should run on different operating systems, or you wanna build your application against the different versions of node, as we're doing it here. Here it's technically just a dummy example but still it is a feature you could need for the exact problem you are trying to solve, in your work. So now therefore here, we added this matrix, and with that we can push this to the Repository. And what you'll see there under actions is that because of this added matrix, our Matrix Demo Workflow runs of course. And now you see here that it's actually executing six jobs in parallel. And we can view all these jobs, and you see they run in parallel for these different combinations. So node version 12 on Ubuntu and Windows, node version 14 on Ubuntu and Windows, and so on. And you will also see by the way that for some combinations it will fail because node version 12 actually doesn't support all features needed by the project I provided to you. And therefore other jobs are also skipped because we have one failing job. But you see that some combinations also succeeded. And that's by the way also something to be aware of. When using this matrix strategy, if one job fails, the other jobs are canceled and skipped. If you don't want that behavior, you can use continue-on-error on job level, and set this to true. Because continue-on-error on job level tells GitHub Actions to continue executing jobs that are related to this matrix even if some jobs for some combinations failed. So here, therefore now I can create a new commit, added continue-on-error, and push this to the Repository. And now with that change made, you will see that again Matrix Demo will run this Workflow, and all these jobs are running in parallel. But now you will see that the version 12 jobs, so the first two jobs will still fail, but the other jobs will not be canceled just because they failed. Instead they will continue running because we added continue-on-error on job level. So let's wait for that to finish here. Here we go. As you can see, the first two jobs failed, but the other jobs still continued and actually succeeded, they were not canceled because we added continue-on-error to this job with a matrix.

Now there also are two other features related to this matrix configuration, which I also wanna mention. And the first feature is that you can add the special include key here under matrix. In general, these keys are up to you and you can add any keys you want and use them in your job and step configuration thereafter. But the include key is a reserved key. This allows you to add a list with dashes of key values that should be included without adding a bunch of new combinations. For example here, we could add one combination where node-version is 18 and operating system is ubuntu-latest. Of course, I could have just added 18 here to this list up there, but that would've added two new combinations where version 18 would've been used on ubuntu-latest and windows-latest. Now, if I know that I want to have all these combinations but then this additional single combination where I use node-version 18 on ubuntu-latest I can add this single combination with the include key. So include allows you to add standalone combinations and you can even add brand new keys here which weren't specified up here. So this can be used to add single combinations. Similarly, you can also add the exclude key to exclude some combinations. If I, for example, know that I don't wanna use node-version 12 on windows-latest, but I wanna construct all these other combinations, I could either add ubuntu-latest with node 12 as a standalone combination with help of include or I set it up as I did up here and I add an excluded combination where I exclude node version 12 on operating system windows-latest. So now this combination would be excluded from the generated matrix. So with this is made, we can create a new commit, add it include and exclude and push this and you will now see that for matrix demo. This of course again runs, and you see that now the version 12 Windows combination is missing. There's only one version 12 combination on Ubuntu. But on the other hand, we do have version 18 on Ubuntu. So that's the included combination. And the missing version 12 on Windows combination is the one we removed with exclude. So that's how you can also use include and exclude to add or remove combinations.

Now using a matrix to control which versions of a job are executed and to easily use multiple configurations in one and the same job, can also be a very useful feature. To conclude this core section, we'll dive into a never very useful feature and that would be a reusable workflows. The idea here is that you could have a workflow with a job and some steps, or also with multiple jobs of course, that you might wanna reuse in other workflows. For example, one workflow could be uploading the website code to a hosting server. And that such a common task that you want to use that same workflow in other workflows that might in addition also do some testing or building or whatever. So you have one workflow that just uploads the code and then other workflows that also do additional work. But you now wanna use that one workflow inside of a job of that other workflow. And that is indeed something that's possible with help of GitHub Actions because it supports reusable workflows. Now to show you this reusable workflow feature, I'll add a new workflow file, reusable.yml. Which will be used to define the workflow that should be reusable. In here we'll give this a name, reusable deploy for example, and I'll add the trigger a little bit later. For the moment I'll just add my jobs. And here I'll keep this very simple. I'll add a simple dummy deploy job where I say runs on Ubuntu latest. And where in my steps I add one step where I say output information. And then here I just say deploying and uploading or anything like that. So it's just a dummy workflow. We'll add more to it later but for the moment that's all. And I'll get back to this on trigger in a couple of seconds as well. But that's now the reusable workflow. Now to make it reusable, it must be callable by other workflows. And that's where the on trigger the on field becomes important. Because here we don't wanna run this workflow whenever we push or when a poll request is opened or anything like this. Instead, we wanna run this workflow from inside other workflows. That's the idea behind a reusable workflow. And for that, GitHub Actions gives us a special event, the workflow call event. Not workflow dispatch that was used for manually running a workflow, but instead workflow call. This allows this workflow to be called from other workflows. And that's there for the other part now. I'll copy the execution flow yml file and name it use reuse yml using reusable workflow could be the name we assign here so that we can easily identify it once it's running. And here we're still reacting to a push to the main branch and we still might be linting and we have all the conditional code in there. We could also remove it, it does not matter. But then here for the deployment step, I want to change things in this use reuse yml file. Instead of running my own steps here or doing my own work here, I will remove them. I will also remove runs on and instead we can use the uses key here. Now, we already know the uses key from steps that use actions. And actions also are kind of reusable scripts you could say. Later in the course we will also learn how we can write our own actions. But actions are used in steps. Now we want to use an entire workflow which is a combination of steps or even a combination of jobs if we want to. And we wanna use that predefined workflow which we defined here for this deploy job. For that we can add the user's key right to the job. And then here we don't reference an action but instead an entire workflow. We do that by providing a full path to the workflow file seen relative from the route project folder. So we dive into the /.GitHub folder into the workflows folder. And then here it's the reusable.yml file that defines the workflow which I wanna reuse. This could also be a path to a workflow defined in another repository. But here it is a relative path to a workflow defined in the same repository. So now with that we're using this workflow when this job runs. So therefore, because we added workflow call as an event here, this workflow will be kicked off by GitHub Actions whenever this job starts. Now, there's more we can do with reusable workflows but let's first of all run this simple example by creating a new commit and pushing this. So that's the first step. And now under actions we see that we got this using reusable workflow here, we also got reusable deploy. This is not started because that is the reusable workflow which has not started itself when we push. Instead using reusable workflow is started. And that's the workflow that uses the reusable workflow. And in here we see that in this deploy step here, we'll use our reusable workflow. Now I just realized that we won't get there because my tests are still failing. So let's quickly fix this by reading that A here in that failing test in maincontent.test.jsx. And let's add a new commit where we fix this, fixed failing test. And then push this so that we can actually see the reusable workflow in action. Because at the moment we are not even reaching it. So now with this fixed, here we triggered another workflow run and now we will make it to this deploy step. And there this reusable workflow will then be used. So let's wait until we get there. Here we go, it's starting to execute. So this job that uses the reusable workflow is starting to execute. And what you see here is that the output information step was executed and there we see deploying and uploading. And that's exactly what our reusable workflow does. Here, we're outputting, deploying and uploading and that's the only part where we're outputting this. So indeed, our reusable workflow was executed here and that's how we generally can use reusable workflows.

So that's how we can use reusable workflows, but that's not the only thing we can do with them. Of course, when you use a reusable workflow, it's not unrealistic to assume that you also need some data in that reusable workflow. For example here, if we are really deploying code, if we're uploading code, then we need that code that should be uploaded. Now in use reuse YAML, I'm generating that code with the build step, and I'm uploading it as a artifact. Now to get and use, and eventually upload that code, I need to download that artifact. So in reusable YAML, we could of course add a new step, where we say get code, and then here, we use the download artifact action, which we also saw in action before in this course. But we now need to configure this action and provide the key of the artifact, which we wanna download. Now, if you take a look at the other workflow, we see that the key is a dist-files. So of course, we can add that here as a name, right? Well the problem is that now this workflow isn't really reusable, we can only use it in other workflows that do upload artifacts with a key of dist-files, and that might not always be the case. We might be using different artifact identifiers in different workflows, and whenever we use anything but dist-files, this workflow would not be usable there, because here we're only looking for a dist-files artifact. So instead of hard coding the artifact name here, this should be dynamic. It should be resolved with help of a GitHub Actions expression. But that of course means that we must get some data from the workflow that's using this reusable workflow. We must get the artifact name from that other workflow. So you can think of that reusable workflow as a function that's called by the workflow, that's using that reusable workflow. Now thankfully, when using the workflow call event you can add some extra information to it. For that, you nest it like this, and now under workflow call here, we can add the inputs key, which is a reserved word. And under inputs, you can add any inputs this reusable workflow needs. For example here, we could expect to get the artifact name, and this key is now up to you again, you can add any inputs you want here. But here, we need the artifact name, and then nested under that, we can add a description where we could specify what exactly is expected here, the name of the deployable artifact files, for example. Then we can add required, and set this to true or false, indicating whether this workflow should fail if we're not getting this input. If required is false, then this workflow can be called without providing a value. If we set it to true, the value is needed. And it of course, depends on what your workflow does, whether this is required or not, and you can also provide a default value. For example, we could set required to false, so that this workflow can be called without providing a value for artifact name. And if we still know that we need that value later in our steps, we could add a default value, like this, so that we assume that if we did not get any other artifact name, this is the value that should be used down there. We also must add a type, where we specify which kind of value we expect here. Is it a number, a Boolean, or as we have it here, a string? So that's how we can accept an input. Now to use this input, we can go down there where we need it, and here we can use the special inputs context object. And this is simply another context object provided by GitHub Actions. This one. And this contains the inputs of a reusable or manually triggered workflow, as we learn here. So the other, that's the context object I'm using here, and in this context object, we can access all these inputs we specified here. So in this case, .artifact-name gives me the value for this input, and that's then used here as a name for the artifact I'm downloading. Then in the next step, I can list my files with the LS command to check whether downloading the artifact worked. So that's how we can add such an input. And therefore, now if we create a new commit, we can push this, and see whether this works as expected. For that, under actions, of course, a new workflow run was triggered. And here, we should see that once deploy is reached, we should be downloading artifacts, and we should be outputting the content of these artifacts. However, you'll also notice that something will not work, as expected. Here we are in the deploy step, and it's getting started here, using that reusable workflow. But you'll notice that get code fails, and it fails because it was not able to find an artifact with the name dist. Now it's looking for an artifact with the name dist, because that's our default value here, and it's using that default because we're not providing any other value. And that's the problem we currently have here. Of course, when we're using our reusable workflow here, we have to provide a value for that input, unless we're happy with the default, which is dist, but I'm not happy with the default here, because I'm using dist-files. Sounds similar, but it is different. Therefore, we add the with key, just as we do it for actions with uses and with, but now for this reusable workflow. And under with, we provide all the input values that must be provided. So in my case here, I'm expecting an artifact name input. So that's what I specify here under with. And now here, we provide the value we're using, dist-files. And now with this change made, this will work as intended, because now we're providing an input value, and therefore now when we push this, this entire workflow that uses the reusable workflow, will run successfully. So let's take another look, and wait until we reach deploy. Here we go. The deploy job is starting. And here, we will see that now getting the code did succeed. We were able to list our files. This is indeed the code which we got, and therefore the overall job and workflow succeeded, because now we are passing the required input to this reusable workflow.

Now there's more to reusable workflows. Besides expecting and using inputs you can also expect secrets by adding a secrets key on the same level as you added inputs nested below workflow_call. Now, secrets are what the name implies, secret values that might be needed by this workflow. Now, this workflow doesn't need any secrets but of course you might have a reusable workflows that do and therefore here we could have some secret. We can then specify whether it is required or not. You can't add a default value or anything like this. And then once you added a secret here, required or not, you can provide it here when using that reusable workflow. Of course, you must provide it if the secret is required. Alternatively, you can optionally provide it. Now, when it comes to providing a secret value you don't do this with, with. You use that for inputs only instead. But instead you use the secrets key, which you add to the same job where you added uses and with and you add it on the same level as you added with and uses. And then under secrets you can add these secrets using the same identifiers as set up here and provide your secret values. For example, by using the secrets context, some-secret. Now here, I'll comment this out by adding a hash symbol because I haven't added a secret with that name. But that is how you could pass a secret to your workflows. Here again, I'll comment it all out but that is how you would do it.

Now there's one last thing related to reusable workflows, and that would be outputs. Of course, you cannot just accept inputs, but instead your reusable workflow might be doing something that should be passed back to the Workflow that called this reusable Workflow. If you think of reusable Workflows as functions, then inputs are the arguments you pass to them and outputs are the return values. And you do add outputs by adding the outputs key under the Workflow call event name. And then here you can add your outputs, for example an output named result. Give it a description, where you could say the result of the deployment operation and you provide the value. And the value of course is typically taken from the steps in one of your jobs. For example, here, let's assume we would really be uploading the content here. Of course, this upload could fail. Now in that case, we might want to set result here to a value like failure or to false or whatever. To do that I'll add a new step here, which I'll name, set result output and in this step, I will run a command and echo this special set output command we learned about earlier in the course. Remember I taught you about setting outputs and steps and using those outputs. Now we're doing the exact same thing just in this reusable Workflow, but we still use the colon, colon set output command in conjunction with echo in a run instruction. And then here we specify name and then equals the name of the output we want to use, for example, step dash result. And then two colons, and then the value that should be stored in that output. For example success, you can also wrap this between single quotes, but you don't have to. So now the string success would be stored in a step output named step dash result. Now to use this output here as output for the reusable Workflow, I will first of all, make it an output of the overall job here by adding outputs to this job. And that's what we did earlier in the course already. And that actually belongs to the deploy job, so it should be nested inside of deploy. So here I add outputs, the outputs key to this specific job, and then here we could have a name of outcome. We could also use result here but I'm already using result up here, and to make it clear which output belongs to which part to which step or job, I'll use outcome here. And then here we use a dynamic expression and we access our steps and we give this step an ID set dash result for example so that here we can access steps dot set dash result dot outputs and then access the step dash result output step result because that's what I'm also using down here. So now with that, we're getting the output that was set in this step and we're making it an output of this deploy job, an output named outcome. And now we can reference this outcome output in our overall workflow output. For that, we go to outputs in our Workflow. And here under value we use another dynamic expression. And here we use the special jobs context object which of course is another officially supported context object for reusable workflows only as you see here. And this allows us to get the output of the deploy job. So here I named this job deployed. That's why I'm using deploy here as well. and then the outputs of that job and then in my case outcome. So now the output of this deploy job the outcome output is the result output of this Workflow. And therefore in the Workflow where we use the reusable Workflow we can get this output and use it. To use it we could add another job, print deploy result or anything like this and run this on Ubuntu latest make sure that it needs to deploy job because this should run after the deploy job finished. And then here I'll just add one step print deploy output, where I simply echo and then the result of some dynamic expression. And here I want to access the needs context object Which gives me access to the job I'm depending on and then here I need the deploy job, that's the only job I depend on here, but I could be depending on more jobs, that's why we have to specify the job ID here. And then we access the outputs of that job, which of course are the outputs of the reusable Workflow in this case. And then in my case here, I named the output result so here I also access result. Now with all that done, we have a chain of outputs and we can therefore now create a commit where I say added outputs and push this. And now again, the reusable Workflow will be used by our Workflow, by this Workflow here. And you will see that in this print deploy result job here we should successfully print the result set by the deploy job. And the deploy job uses this reusable Workflow where the output is set to the string success. So we should see success being output here in print deploy result. So let's wait until we reach this job and until it's completed. Here we go, it completed. And if we take a look at print deploy output we see that there success was output as expected.

Now that's it for this course section. We started with conditional jobs and steps and you can control step or job execution with the if field and dynamic expressions as you learned. That's very useful because in many scenarios you don't wanna execute all steps or jobs all the time and sometimes you wanna execute jobs or steps if they normally would not have been executed. And that's also where the failure success canceled and always functions come into play because these functions allow you to change GitHub Actions default behavior, and for example execute a job even if other dependent jobs failed, or for example execute some job or a step if a workflow was canceled. You can also use to continue on error field on a step to ignore the failure of that step and proceed as if that step would've succeeded. Now, that's one thing. We also learned about matrix jobs where you can easily run the same job with different job configurations and where a matrix of combinations is created automatically. You can also add or remove individual combinations as you learned and you can control whether a single job failure should lead to all other jobs being canceled or not with help of the continue on error setting. Last but not least, we also explored reusable workflows, which is a feature that allows you to build a workflow that can be used inside of other workflows and you simply create such a reusable workflow by adding the workflow call event to it. Any workflow that has this event can be called by other workflows. You can then reuse any logic you want and add as many jobs and steps as needed to that reusable workflow so you can really do whatever you want there. And you can also use inputs, outputs, and secrets to give that reusable workflow all the data it might need and to also pass data back to the workflow that uses the reusable workflow.

