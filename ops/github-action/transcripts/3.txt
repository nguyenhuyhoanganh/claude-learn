With this course section, we're going to dive even deeper into GitHub Actions, and we're going to focus on working with data in our jobs and steps and producing and using outputs by and with our jobs. To be precise, we'll explore how we can work with artifacts, and what exactly artifacts are to begin with, before we'll then dive into job outputs and learn how they are different from artifacts and explore the concept of dependency caching, another key concept you should know when working with GitHub Actions.

Now for this section, I got another example project for you. It's actually still the same project as before but it comes with an example workflow already in the .github/workflows folder, and this is a finished workflow that will work like that, and that will be activated whenever we push to the main branch, but that's the workflow we'll also work on and enhance throughout this course section. Now of course we also need a GitHub repository, as always, and therefore here I'll initialize this project here with Git to get a local Git repository, and then I'll create my initial commit here, like this, and with that initial commit created here, I'll also switch to GitHub again to create a new repository there, and I'll name it gh-data, but as always the name is up to you. It can be public or private. Here, I'll make it private, but it does not matter for this section, and then with that repository created, as always, we grab the URL and add it as a remote repository here, with the origin identifier and with the username added here to make sure that we're using that user for authenticating, and then I will push my main branch after establishing the link like this, and with that done, we got that GitHub repository with which we'll work throughout this course section.

Now, when it comes to working with data, in our GitHub Actions Workflows, there is a broad variety of data definitions we could be referring to. But we will start with Job Artifacts here. And to understand Job Artifacts, let's take a look at an example Job, that might be building an app. So, this Job could be producing the website files, that are uploaded to a web server, to a web hosting provider. But we could also be building a mobile app here, where we then upload some package to the app stores. Or we could be producing an executable here, which we distribute to our users. So, a desktop application, that can be executed on their machines, for example. We could be building anything, but let's assume that we have a Job that builds, such an app or website. Of course, if we have such a Job, this Job will produce an output. It will produce the website files that should be uploaded, or the binary executable file that should be distributed to users, or that package, that should be uploaded to app stores. So, we generate some assets that must be uploaded somewhere, or distributed somewhere. And these assets are commonly referred to as Job Artifacts. That's what a Job Artifacts is. It's the output, the assets generated by a Job. And with GitHub Actions, we can upload, and save those artifacts that are produced by a Job, so that as a user, as the owner of the Repository, we could download them manually, thereafter, to then for example, take them and upload them to an app store. But we can also download, and use them automatically in other Jobs. If we have a subsequent Job, that should do something with the produced output. For example, we could have a Job that automatically picks up those produced files, and uploads them to some web hosting provider. So, Job Artifacts are these outputs generated by Jobs. And just to be clear, it's not necessarily, just app binaries and similar files, we could also have log files that are produced by a testing Job or anything like that. But we're talking about files here, files and folders produced by a Job, that we want to use in other Jobs, or manually inspect after the Job finished. For example, if we had a Job that runs some testing scripts, and those testing scripts produce some log files, we might want to take a look at those log files, as the owner of Repository. So, that's what artifacts are. And with GitHub Actions, we have a way of storing these artifacts, so that we can then use them manually, or automatically. And that's what we'll take a closer look at now.

For this back here in this demo workflow, it's worth taking a closer look at this workflow to understand what it's doing. It's named Deploy website, but of course the name isn't too important, and it will be activated when we push to the main branch. And then we got three jobs in there: A test job, a build job, and a deploy job. Now the deploy job is just logging some dummy message to the console, so to the terminal. But the test job does something useful, actually. It gets the code, installs dependencies, and then runs the linting and testing scripts to validate whether our code is written correctly and works as intended. The build job then runs after the test job because it needs the test job. And you learned earlier that you can run jobs after each other with help of that needs configuration instead of running them in parallel. And then in this build job, I also get the code. We also install the dependencies, but then we run the build script. And this build script here, for this demo project which I'm providing to you, will actually produce some output files. And to see which files will be produced, I will run it locally here. After installing the dependencies, if I run npm run build, this script, or this command will produce this dist folder, and it would be the contents of this dist folder that would be uploaded to some web server, some web host, to host this website. Which is being built here. So that's the output of this build step. And of course, it would be reasonable to assume that we want access to these produced files, because they don't help us that much if they only live inside of that workflow and on that runner machine. Because once our workflow is finished the runner machine will be shut down and these produced files will be lost. So it would be great if we could store these produced files, such that we as a user can download them and so that we can inspect them, test them locally, or upload to some web hosting provider. We also might want to use them in this deploy job, but that will be the second step, where we then access those files automatically. As a first step let's make sure that we can store these produced files and access them manually. And of course this dist folder, in the end, is the artifact produced by this build job. Or the contents of this dist folder are the artifacts, if we wanna put it like this. So we wanna store this dist folder as an artifact and to do this we can simply add a new step here.

So if we have jobs in our GitHub Actions Workflow that produce output that should be stored, we're dealing with artifacts. And to store these artifacts we can simply add a new step. Now, this step can of course receive any name of your choice and I'll name it Upload Artifacts, because that is what we'll be doing. And then here we don't run a script but instead we use an action. To be precise, there is an official Upload Artifact action developed and provided by the GitHub team. And this action does just what the name implies. It allows us to upload artifacts. As always on the official Repository, you can learn more about this action and how this action may be used and configured. But I'll also show you how to use it in this course section. So here we want to use this actions/upload-artifact action. And at the point of time I'm recording this, I want to use version three. And then we can add the with key to provide some additional configuration. And some additional configuration is needed here for this action because I must give my artifacts a name. This will then be useful for retrieving those artifacts, especially if you want to do that automatically in subsequent jobs. Which is something we'll do soon. And here I'll simply name it "dist-files" but this name is totally up to you, of course. And then we have to add a path. And this path simply describes where to find the files that should be stored. And you can store as many files as you need to. In the official documentation of this Repository you see that you can specify multiple paths here, so that multiple files would be organized under a single artifact identifier. And all these files could then be accessed manually or automatically from other steps. You can also exclude certain paths if you know that some files should be included, but some other files that would be part of the other paths should be excluded. Again, the official documentation covers all the use cases and all the configuration items you can set. Here, in my case, I just want to store the files in a dist folder. But to also simulate how multiple files would be stored and organized, and how we could then retrieve them, I will actually add the dist folder and then also add another path here, even though it's technically not needed. And store in a package.json file as an artifact as well. Again, this is redundant, I'm just doing this so that we also see how multiple files and paths could be put into a single artifact. Please note, that if you have multi-line instructions like this, you need to add the pipe symbol here, and then you can put your different instructions into different lines here. Now with that, if we saved this, we manipulated this job here, this build job, such that we store degenerated output under the dist-files identifier. And now to see this in action we just have to create a new commit here. So I will stage my files and create a commit where I say "added upload artifacts step". And then we can push this. And since we activate this Workflow upon pushing to the main branch, and I am pushing to the main branch here, this will also automatically run this Workflow already, as we can see here under actions. And there, if we wait for this Workflow to finish, we'll also be able to retrieve those stored artifacts. So let's wait for this Workflow to finish here. Here we go, the Workflow finished. And now you can see the artifacts here at the bottom of this page. Here you see the dist-files artifact, and of course that is the identifier we chose here in our Workflow definition file. And we can click on this artifact to download it. So now it's downloaded as a ZIP file, and if we extract that ZIP file, we get a folder that includes the dist folder, with all its contents, and the package.json file. Because those were the artifacts I added here. So that's how we can use this Upload Artifact action. Now here, I'll tweak it a little bit and actually get rid of this definition. I'll comment it out so that we can still see it. But I'll change the path here to actually just include the dist folder with all its contents, because that is all that's needed here.

So that is how we can upload artifacts and you also saw how they can be accessed manually. Now let's say we wanna use these artifacts in a deploy job though because that would, of course, be reasonable to assume. We're generating those files here in the build job and now let's say this deploy job does not just output some dummy message as it does here, but instead, it really takes those files and stores them on a web server. I'm not doing this here because there's a broad variety of web hosting providers and most of them provide their own actions that could be used for uploading files to them and it's simply not the main focus of the course. That's why we have this dummy script here, though we will see a complete deployment process a little bit later in the course. But let's simply assume that we're really using those files here. And if we're doing that, if that's the plan in this deploy job, then we, of course, need a way of getting hold of the files produced and uploaded in the previous job. Now, since these jobs run on their own runners, every job gets its own runner machine. The files that are produced in the build job will not automatically exist in the deploy job because this runs on a different machine, even though it has the same runner definition but it's a different machine on which this job will be executed. So in order to get those artifacts that were updated in the previous job, we need to download them here in this job. Therefore here, I'll add a new step, which I'll name Get build artifacts and then here we can use another action provided by the GitHub team and that is the download-artifact action. And here I'll also use version three. So we get the upload-artifact action, which does exactly what the name implies and we get the download-artifact action, which also does what the name implies. It will download the artifacts uploaded by a previous upload artifact action. Therefore, it's, of course, important that we have this needs definition here because this deploy job must run after the build job because if it would run in parallel, we of course couldn't download the artifacts because they wouldn't have been uploaded yet. Now, for this download-artifact action, we also must add some configuration here with the with key because we, of course, must specify the artifact identifier, otherwise the workflow would not know which artifacts to download. You can upload multiple artifacts in one and the same job or in different jobs and that's why the identifier is important. So if we're here for download-artifact, you also add the name key and then we should use the same name as we used for uploading artifacts, dist-files in my case. This ensures that we download the right artifacts here and what this will do is it will simply grab the zip file that is produced by upload-artifact, download it and unzip it here. So therefore, thereafter these dist-files will be available in this deployed job. What's important to note though is that we'll loose the dist-folder and all the files that were in the dist folder will directly be available here in the folder in which this deploy job runs. You can see this if we output the contents here and we run the simple ls commend, which lists all the contents of the folder in which we currently are. Here we should see that we simply have all the contents of the dist folder available in that folder in which the deploy job runs. And that could be important for uploading them so that we don't try to navigate into some dist folder first and grab the files from in there but that instead the files that were in the dist folder will directly be available here because that is how they will be unpacked here by download-artifact. Now, enough of the talking. To make this a bit easier to grasp, I will simply stage those changes, create a new commit, added download artifact action, and push this, and just as before, the job will, of course, run automatically since I'm pushing to main. So again, we can wait for this workflow to complete so that we can then analyze what happened in deploy. The workflow finished here and if we now click into this deploy job, we can see that we've got our build artifacts here and new see that the total number of files downloaded was five in the end because it unpacked those artifacts automatically for us. And under output contents, we see that after running ls, which simply lists the contents of the folder we're currently in, we got all the content of the dist folder in here. So that's what I meant. We don't see the dist folder here into which we would have to navigate in order to access these files but instead we have the files themselves already. And that's just something to keep in mind if you would be using these files in subsequent steps. And that's how we can upload and download our artifacts, both manually and automatically.

Now, in our GitHub Actions workflows in our jobs, we are not just dealing with artifacts, which might be produced by our jobs, but instead, we also have job outputs, and it's important to understand the difference. Artifacts are these log files or these app binaries, which we typically wanna share with someone or something or which we wanna analyze. However, our jobs cannot just produce such files as outputs, which we would then treat as artifacts, but instead, our jobs can also output simple values that we might wanna use in subsequent jobs. So one job might produce some output value that should be reused by another job. For example, we could be dealing with a file in one job and the name of the file matters in another job, and because that's the case, we want to kind of make that file name available as a value that can be used in the other job. So it's not the file itself necessarily that should be made available, but it's the name of the file we could say. And of course, this is just one simple made up example, it could be any kind of value that we wanna make available across jobs, so values that are derived in one job and must be available in other jobs, that could be dates, hashes, random values, anything like that.

And to again come up with a somewhat made-up example here, let's say that in this build job here, where we produce the dist folder, which we then upload as an artifact, we also wanna get the name of the generated JavaScript file in this assets folder in the dist folder. So of this JavaScript file. We wanna get the name of that file because that name is actually kind of randomly generated. It's not always the same name. Now, of course, we could get that name in the deploy job by simply looking into the assets folder, which we download as an artifact here but let's say that for some reason, we wanna get access to that name here in the build job and we then wanna make that file name available to the deploy job. So that we have one value that's derived by one job, the name of the file derived in the build job that should be usable in another job. So in the deploy job here. If that's kind of what we need, and again, I'm aware that it's kind of a made-up example, but it shows how to work with outputs, which is the important part here. If that's what we need, then GitHub Actions has a feature we can use for that because jobs can produce outputs. And by the way, this feature will become even more important later in the course once we dive into building custom actions because custom actions also will be able to produce outputs but again, I will get back to that later. Then this feature will make even more sense but it's good to get started with it right here already. So let's say here after building the website, we wanna get hold of that file name of that JavaScript file. I'll give this step a name of Publish JS filename because the goal of this job will be to get the JavaScript file name and to then publish it so that other jobs can use it. And for that, we need to run a script here. Now, the command is a bit more complex. That's why you also find it attached in the text file. This here is the command that should be executed. It's a regular Linux command that finds us the JavaScript file in the assets folder, in the dist folder, and then it basically executes the echo command to output that file name. If we would run this command like this on a Linux machine, it would simply output the file name of this JavaScript file. And since I am on macOS here, which builds up on Linux kind of, I can show you this command. If I run this here in my local terminal, it outputs the file name of this JavaScript file. That's what this command currently does. But that's actually not too helpful yet because yes, this outputs the file name but the idea is to publish the file name to make it available in other jobs. And to make it available, we must create a so-called output, which is a separate thing in GitHub Actions, which is different from artifacts. An output or an output value is a simple value. So not a file but instead, a number or some text or anything like this. A simple value that should be produced by the job that should be accessible by other jobs. And to generate and share such a output, we must add a new key here at the top level of this job definition before we define the steps. And that's the outputs key. Outputs then allows us to define any outputs of our choice, and as many outputs as we want. For example, the script-file output. This identifier is again up to you because this is the identifier by which you later will be able to access the output value. So here I add my script-file output. And now with that added, we're not fully done here but with that added down here, we can set our output. And now to set an output value, what you should do is you should first of all, choose an output name of your choice. For example, script-file, but it can be any name of your choice. It doesn't have to be the output name defined up there because here as a first step, we're just setting an output name for this step here, not for the overall job. We're just adding output to this step and this name here, therefore, will be the name of this steps output and it is totally up to you. After the name of your step output, you then add an equal sign and on the right side of the equal sign, you have the value that should be accessible under that output name. In my case, it's this special placeholder here but it could be anything. It could be some text but here again, it's this special placeholder, which in the end contains our JavaScript file name. Now, that's not everything though. You then also have to add two angle brackets after this closing quotation mark here and point at $GITHUB_OUTPUT. Now, what this does is it targets a file, a special file created by GitHub in the environment in which your job runs where the output key-value pair is written to. So this overall command simply writes the output to a file, a file which path is stored in this special variable, which is provided by GitHub Actions. So this simply writes some data into some output file, managed by GitHub. And that then overall sets the output for this step. However, of course, here we wanna set an overall output for the job, not just for this step. And that can be done by then adding an expression to the job here with dollar sign, curly braces. And then there, you wanna access the steps context value. Now, that is a special context value made available by GitHub Actions. Just as we could access the GitHub context, for example, but steps is a special value with information about your steps. And we can now access the output of this step with help of this steps context by giving our step a unique ID by adding the ID field to it. And that's important. We must add this ID field to our step because we'll be able to access data from that step via that ID. So here we could add an id of publish, though that name is up to you. And then here for the job output, you can reference steps.publish. So the ID of your step here, .outputs and then the output name you defined in your step. In my case, .script-file but if I would have chosen a different name here, like something, I would have to use something up here. So these two values have to match. But here I'll stick to script-file. And that's how you can set an output in a step and then also reference that output here for setting an output for the overall job. Now, an alternative syntax, which you sometimes see, and which had to be used in the past was that you set an output in a step by using a special instruction, which you add in your echoed string here. Instead of setting your output as a key-value pair like this, you would have added ::set-output and then a white space, then name equals, and then the name of your output. And then, another double colon, and the value that should be stored under that name. And you would omit that part where you write something into that special file. So that's another syntax you might see and that is actually the syntax I used for recording the course, which is why you see it in some of the later videos as well but you should actually set the output like this instead. And not use this set output instruction, which also will not be supported in the future anymore by GitHub Actions. So this is the right way of setting output for a step and for then referencing it when you wanna set it as an output for the overall job.

So we published an output here. To use it in another job, in the deploy job, to be precise, we simply have to go into this job and then, for example, add a new step, where I say output, file name. The name of course is up to you. And then here I wanna run a command that uses the output by the build job. And here I simply wanna output that file name to keep things simple. So I'll use the echo command, but then here between my quotes I again use this dynamic expression syntax to evaluate some expression. And now I wanna get data from that build job. And we can get that data by using the needs context object. Again, we have a lot of objects here and we can get data about other jobs with help of the jobs context object. But for outputs, a better object to use is the needs object down there. Because that needs object contains the outputs of all jobs that are defined as a dependency of the current job. So here, when I use the needs object I can then dive into my job on which this job depends. In this case that's the build job because that's specified as my only need here. So I can access needs dot build here. Since I'm using build here as an identifier. And then for this job, we can dive into outputs. That's again, a field defined by GitHub Actions, which exists on all the jobs that are specified as needs. And now we can use the output identifier specified here, which is script file. So that's the identifier I define as an output here in my build job, hence I can access it down there. And now this line should output the file name published by that other job. And of course, that was a lot of work here because I also tried to explain it in detail. But this can be a very useful feature for passing simple data between different jobs and in conjunction with artifacts. This therefore allows you to pass any kind of data between jobs. And as mentioned before already, this will become especially important once we dive into building custom actions. Nonetheless, here with all that finished, I will now stage these changes and create a new commit, added outputs and then push this. And now when this Workflow runs again we should see that in the deploy step, in the deploy job, we successfully output the file name that is stored or provided as an output by the build job. So let's wait for this Workflow to finish here. So here, the Workflow finished. And now if I click on deploy and we take a look at the output file name step, we see that I did successfully output that file name that was published as an output in the build step. Here in published JS file name, I did publish that file name. We don't see the file name here because it's derived dynamically but it's data output that was generated here and that was then stored as an output here. When the job completed it's that output that is successfully accessed in the output file name step of the deploy job.

Now to conclude this course section, I want to dive in another important data-related topic and that would be dependency caching. If you take a look at the workflows I ran here, you will notice that they all took around a minute to execute. Sometimes a bit faster, sometimes a bit slower, but always around a minute. Now, it took around a minute here because these were all relatively simple jobs in these workflows, but, of course, I have three jobs running after each other. So therefore, since they're not running in parallel, it's the sum of the jobs that's taken to complete the workflow, plus additional setup and cleanup work that is done by GitHub Actions. Now, running the jobs in parallel wouldn't be an option here because indeed these jobs depend on each other and that will often be the case. Nonetheless, if we take a look at the workflow definition file, you will notice that there are some steps that are always repeated. For example, getting the code and installing the dependencies. Now, getting the code is something we simply have to do in every job because, as you learned, every job runs on its own runner, its own separate machine, so therefore we have to get the code again and again every time. But this is also relatively fast and shouldn't be a huge issue. If we take a look at our jobs here, we see that getting the code only takes around one second, so that's not really too slow. What is slower is the installation of dependencies. It takes 12 seconds here in the build job, in the test job it took eight seconds here in this workflow run. So if we take a look at all the steps, we see that installing the dependencies is actually the step that takes the longest. So if we would be able to kind of shorten this step, if we could reduce the duration it takes to install the dependencies, we could save a lot of time for the overall workflow execution. And even though we're not paying for it here in the free plan, reducing the overall workflow duration might still be something you aim for, because ultimately it would also mean that your website gets deployed faster. And, of course, if you are paying for your workflows, then you have an even stronger incentive to reduce the amount of time your workflow execution takes. Now, no matter what you're doing, if you're building a website or other kinds of apps, it's pretty common that you have some step like the dependency installation step we have at here. Very often you must install third party dependencies or run some setup scripts that generate some files. This is pretty common for most CI/CD workflows. And a great way of speeding up that installation process for those dependencies is to cache those dependencies. Because if you got two workflows that do different things but have this shared step of installing dependencies, which tends to take a bit longer and which produces files that are needed by the other steps in the jobs, if you have something like this, then it makes sense to only install those dependencies once in one job and then cache them so that other jobs can reuse those cached dependencies. Because those dependencies typically don't change frequently and therefore you can save time if you do cache them so that the cached dependencies can be reused thereafter instead of being downloaded again, which is the part that takes rather long. And it's not just caching across different jobs that matters here, you can actually also cache dependencies or files and folders in general across workflow executions, because if you have certain files and folders that don't change at all across different workflows, it makes sense to cache them across different workflows so that they don't have to be re-downloaded or recreated all the time. And this is such a common scenario that GitHub Actions has an official action that helps with caching files and folders across jobs and workflows. And it's this cache action which we're going to use now.

So back here in our Workflow definition file, it would make sense to kind of cache these dependencies the first time we install them. So here in this first job when we install dependencies we wanna cache those dependencies so that the cached dependencies can be used in subsequent jobs and also in subsequent Workflow runs unless these dependencies changed and must be reinstalled. Now to add caching, we must add a new step before the step that actually produces the files that should be cached. So the caching step should execute here before we have that dependency installation step. I'll give it a name of cache dependencies, and then here we want to use an action the cache action in version free, in my case that's published by the GitHub team. This action needs some configuration with the with key. And to be precise you must specify the path or paths that should be cached. Now again, you learn about all the details in the official documentation and there you also find plenty of examples for usage with different kinds of workflows and projects. But in the end, you always have to specify a path or multiple paths that point at the files and folders that should be cached. Now here in this case on this Linux runner for the node dependencies that we are installing here it's this path that should be cached. This is the path used by this npm tool for its internal dependency cache, which we now wanna store with help of the cache action across jobs and across workflows. Because that's important, npm actually already builds a cache that contains the dependencies that were installed and that cache would be used if you then install the dependencies again in the future. But since different jobs used different machines and different Workflow executions, of course also use different machines that automatically created cache by default would not be stored and used. By using the cache action here, we tell GitHub Actions in the end with help of that action that this npm created cache folder should be stored in some place in GitHub's cloud, so on some GitHub server and should be reused in other jobs and other Workflow executions. So that's the path here of the folder that should be cached and stored by GitHub. And then we must give this cache a key. And the idea is that this key will be used for retrieving the cache in the future and recreating that folder on the runner machine based on that cache managed by GitHub in the future. But the key also indicates whether the cache should be discarded because it must be recreated because, for example, some dependency changed. Therefore, you wanna give this cache a name that's not fully static and hard coded but that contains some dynamic element that can change and that will change whenever the cache should be discarded. Hence here we can again include such a GitHub Actions expression with that syntax you already know. And here I'll use another function provided by GitHub Actions, the hash files function which simply produces a unique hash value based on a file path we pass to it and that hash value will change whenever the file we pass to it changes. So the contents of the file change. And here we pass that file path between single quotes. And the path here is package dot lock dot Jason, no matter where it is located, so, no matter in which sub folder it might be, actually this collects all package lock Jason files and builds a hash on the combination of all these files. Now, package lock dot Jason simply is this file that specifies all the dependencies of this project and the exact versions of these dependencies. And the idea here is that whenever this file changes because we updated a dependency, for example, the hash that will be produced by hash files will not be the same as before. And therefore we end up with a different hash key indicating to GitHub Actions and this cache action that the currently stored cache should be discarded and not be used, and therefore instead we would reinstall the dependencies. That's how this works. Now, of course, install dependencies will always execute, this step won't be skipped the way this Workflow is set up but since we either reinstate this npm cache folder or not, the npm ci command will automatically use this npm folder here. If it exists, it will be used. If it doesn't exist, dependencies will be re-downloaded. That's how this works. Now, the cache step here will also automatically look at this folder after the job finished and put that folder into the cache if it didn't have that folder before. So the cache step does not just execute here but also after the overall job finished to update the cache so that if new dependencies were installed the new dependencies will be cached again, this is how that works. And of course, the way you use this cache action will depend on which project setup you have. Different project setups and different packages and programming languages have different files and folders that should be cached to speed up your workflows. That's why you have all these official examples here so that you can find an example that fits your use case. This is how you can do it for npm. And with that done, we must repeat this step here in all the jobs that do install dependencies because with that, we're managing the cache for our dependencies in this job but we also need it in the build job because there I'm also installing dependencies. So we simply copy that step here into the build job and make sure you get the indentation right. And with that we're also going to cache our dependencies here. And what's important is that the cache action actually uses one central cache. So if we put our dependencies into the cache in the test job already the build job will use that same cache. So it's not managing different job specific caches but one central cache which is also available across Workflow executions. So if dependencies were cached in one job there will be available in other jobs and other Workflow execution cycles. And with that done, we added caching. And now if we stage those changes we can create a new commit, added caching and what you'll see now when I push this and when the Workflow executes again, therefore is that it will still install the dependencies in the first job because we haven't cached them before. But the build job should then already use those dependencies. And if we would then run the Workflow again thereafter even the test job should use the dependencies in the future as long as no dependency changed. So let's see this in action here. Let's take a look at the test job. And you see here that again install dependencies is taking some time but what you'll also notice is that under cache dependencies it says that it did not find any cache. But then here it's executing this post cache dependencies step, which was added automatically by the cache action we added. And here you see that it's saved the cache successfully. And that's the key that was generated this year. That's the key that was generated. Now with that cache saved in the build step here you see that install dependencies was way faster. You see it's way faster because it's just taking four seconds now instead of the 12 or 15 seconds it took before because under a cache dependencies, it actually found a cache this time it downloaded the cache and restored it and then installed dependencies still went through all the installation steps. But internally, npm used that cache folder that was recreated by the caching step. So that's how we can save time here.

And again, if we would now execute this workflow again because I, for example changed some file and create a new commit. If I change this title from H3 to H2 here and I then create a new commit, change to H2 and I push this, you will see that now even for the first job install dependencies will be much faster because that cache is used across workflow executions. So now if we wait for this test job to start, you will notice that cache dependencies again was able to restore the cache and therefore install dependencies also finishes much faster. Occasionally it might take longer but typically it should finish much faster. And the same of course, should be true for the install dependencies step in our jobs. Now, of course if I do change my dependencies, for example here if I run NPM outdated, I can update some dependencies. It's not too important. But if I run NPM update to update some dependencies, that will change both package dot JSON and package lock dot JSON as you can see here by the color coding. And if I now create a new commit here where I say updated dependencies, and I push this commit you will see that now the cache won't be reused because package lock dot JSON changed and therefore the cache key will change. You will see that it's not able to use the cache for this new key and the old cache will therefore eventually be discarded and the newly downloaded dependencies will now instead be stored in the cache by the post cache dependencies step. But after that, there again is a cache that can be used by other jobs and workflows. So that's how you can cache dependencies and how you can significantly speed up your workflows with help of caching.

And that's it for this course section. In this course section, we got started with artifacts, which are these assets that are often produced by jobs that should be shared or analyzed. Things like the files you would deploy for a website or test log files or binaries or anything like that. These assets are referred to as artifacts or job artifacts and Git up actions provides you with actions that help you upload those artifacts and download them in other jobs. Alternatively, you can also access and download them manually, via the GitHub website or its REST API. Now, besides these files and folders, these artifacts, you also sometimes have simple values that should be shared across steps or jobs, and that would be those outputs. You share them in your steps with help of the set output command that's provided by GitHub. So this set output command you use together with echo and the run instruction and then jobs can pick up and share these step outputs with help of the steps context in the outputs key that's defined in your job definition. And other jobs can then use these job outputs by other jobs via the needs context as you saw. And then there also is caching. Caching helps use speed up repeated slow steps like for example, installing and managing dependencies of your project. This is a very typical use case, but of course, any files and folders can be cached. The cache Action provided by GitHub automatically stores and updates to cache for you based on the cache key which you provide, which of course should change whenever your cache must update. Now what's important is that you should not use caching for your artifacts. So don't mistake artifacts with cacheable files or anything like that. Caching is for things like dependencies. It's there to speed up your workflows. Artifacts on the upper hand is for the output files produced by your workflows so the results you are interested in.

