At this point in the course we worked quite a bit with GitHub Actions and with a lot of different features provided by GitHub Actions. And one feature which we used a lot already in our workflows are actions. Actions like the cache action here, or the checkout action or the action for uploading artifacts. We used such actions in our steps in our jobs to perform various tasks. And the idea behind such actions is that they can perform any task of your choice of any complexity, simply depending on what a specific action does. For example, upload artifact takes some files and makes them available outside of the workflow or in subsequent workflow jobs and their steps. So these actions simply perform all kinds of tasks and things in single steps and that's the idea for using such actions. But up to this point in the course we always used public existing actions created by the GitHub team or other community members. We did not create any custom actions ourselves. But that is also something you can do and that is what we'll do in this course section here. For that, we're first of all going to explore why we might wanna build custom actions and what exactly custom actions are, before we then dive into the different types of custom actions you can build because there's more than one kind of custom action you can build. And before we then of course also will build and use custom actions so that you also see all these things and concepts related to custom actions in action.

So let's start with one of the most important questions: why would we use custom actions? I mean, after all there are plenty of public actions available like this upload-artifact action. Or if you visit the GitHub Marketplace for actions, you will find that there are tons of other actions available as well. Lots of actions published by various companies, but also lots of open-source projects, so, lots of community actions, you could say. So, there are lots of actions for performing all kinds of things with GitHub actions, and in your GitHub action workflows and jobs. So, why would you build custom actions? Well, there are two main reasons. The first reason is that you might want to simplify various steps you have in your workflows. For example, instead of writing multiple, possibly very complex, step definitions, you could build and use a single custom action to simplify your workflow. You could also reuse that action across multiple jobs if you had multiple jobs that performed the same steps, for example. And indeed that is something we have seen throughout this course. For example, here in this project, which will be the demo project we'll work on throughout this course section, but more on that later. But, for example, on this project, I have a step where I cache dependencies, followed by a step where I install dependencies. And I have these two steps in a lot of jobs in this workflow. I have these steps in the lint job, in the test job, and in the build job. So we have the same combination of steps with the exact same work that's being done in these steps in multiple jobs. And therefore, one reason for creating custom actions could be that you want to group such steps together and reuse them across multiple jobs, so that you don't have to copy and paste these step definitions across these jobs. Another reason for building and using a custom action could also simply be that there is no existing public action available that does exactly what you need to do. The existing public actions might not solve the specific problem you're facing in your workflow in the way you want them to solve this problem. Or you might want to build your own action because you have more control over how it solves a certain problem. Therefore, you might want to build a custom action instead of using a public one because custom actions can contain any logic you need to solve your specific workflow problems. And as you will learn throughout this course section, there are also different ways of creating custom actions, so that you can also use any programming language of your choice in the end. And by the way, last but not least, as soon as you did create a custom action, you could, of course, also publish it in the marketplace to make it available to other users, so that they can benefit from your action in the future. So, this could be another reason for building custom actions that you simply want to contribute back to the community.

So now that we have an idea of why we might want to use custom actions, it's important to understand that there are different types of custom actions you can build. To be precise, there are three main types of custom actions you can build: JavaScript Actions, Docker Actions, and Composite Actions. Now, these are the three approaches you can use for building actions, and we will use all three approaches in this course section so that you see and learn how these three approaches work, but the result will always be the same. So the result will always be an action that you use like you're using these public actions, but when it comes to building custom actions, that's where you have some choices between these three approaches I mentioned. Now, JavaScript actions are simply actions where you write the logic of the action in JavaScript, so you define JavaScript code in a JavaScript file and that file will be executed whenever your action runs. That's the idea here. And in this action code, you can use JavaScript, to be precise, the NodeJS runtime with all its features and any packages of your choice to write any JavaScript application in the end that should be executed whenever your action runs. And it's there for a pretty straightforward way of writing a custom action as you will see in this course section at least if you know JavaScript. And that of course is an important prerequisite. Therefore, if you don't know JavaScript, then this is not a great approach because you must write your code with JavaScript if you choose this action type. Now an alternative to building your action as a JavaScript action would be to build a Docker action. And this makes a lot of sense if you, for example, don't know JavaScript but another programming language. A Docker action is basically a containerized action, so an application that you put into a container just as you could put jobs or services into containers earlier, and you build such a Docker action by defining a Dockerfile which simply configures how the image for this action container will be built and configured and then that Dockerfile will be used by GitHub to build a container based on the image defined by your Dockerfile and you can then perform any task of your choice with any programming language of your choice in that container since it is your container. You can use any base image, set up any environment you want, and therefore, of course, also create any environment for any programming language you want to use. Therefore, you have lots of flexibility but basic Docker knowledge is definitely required in order to get started with this approach. Nonetheless, we are of course also going to explore that approach in this course section. Now, the last kind of action you can build is a composite action or are composite actions. And the idea behind composite actions is that you don't write any code with any programming language but instead you combine multiple Workflow steps as you could have written them directly in the Workflow file itself as well in one single action. So you combine multiple run or uses commands or actions in a single composite action and that composite action can then be used in a Workflow. And therefore, composite actions are great for grouping and combining multiple steps that might be reused across jobs or workflows. And the great thing about composite actions is that no extra knowledge or skillset is needed because you won't write any code in any programming language. Instead, you're just going to define some steps as you already did it throughout this course. So these are the three kinds of actions you can create and these are the three kind of actions we're going to take a closer look at throughout this course section here.

So let's start building custom actions, and for that I'm going to start with Composite Actions, since that's the easiest kind of action to get started with. But before we do that, please note that attached to find the starting project for this course section, it is a simple react app as we had it multiple times throughout this course, and it comes with a workflow file in the .github/workflows folder, but in that file right now, no new features are being used. Instead, this might look familiar from other course sections. Now what I'm going to do here, of course is I'm going to initialize this repository and create my initial commit as I did it in all the other course sections as well, and with that done, I'm going to create a new repository, named like this, for example. It could be public or private. That does not matter. Here, I'll choose private and I'll create that repository and link it to my local Git repository as I always did it throughout this course. After this was linked, I'll push and establish this connection between my local and the remote main branches, and with that, this was pushed, and since it includes a workflow, that workflow should also get triggered and it should also finish successfully, but of course it won't use any actions yet. Instead, that's what we're going to get started with in the next lecture now. We're going to get started with building custom actions there, and we're going to start by building a custom composite action.

So we're going to start building a custom action here and we're going to start with composite actions simply because that's the simplest kind of action to get started with but just to be clear, this does not mean that it's a useless kind of action. Instead, the opposite is the case. This is a very useful kind of action for simplifying your workflows and reusing repeated logic and steps. And if we take a look at this example workflow in this example project, there are some steps which are repeated in a lot of jobs there. For example, the step for checking out the code, foe then caching the dependencies or restoring the cached dependencies and for installing the dependencies if they were not cached. These steps, these three steps are repeated in three jobs of this workflow. And if you have something like this, that's always a great use case for building a custom composite action. Of course, you don't have to build a custom composite action. You can simply copy and paste the code and repeat the configuration code here but you could also build a custom action and that's what we will do here. Now, when it comes to building custom actions, you have a couple of choices. You can create a brand new project and a brand new repository, a brand new GitHub repository for your action and then if it's a public repository, that could be used by any other workflow in any other repository as an action. So your actions can live in their own repositories and then you could use them straight from there. But you can also add actions to projects you already have so that they're then only available in those projects. And that's what I'll do here. In this existing project, under .github next to the workflows folder, I'll add an actions folder, though this name is up to you. It doesn't have to be actions. But it is, of course, a common choice since it makes a lot of sense. Now, in there, in that folder, I'm going to create the actions that will then only be usable by this project. So by workflows that are part of this project, not by any other workflows. If you wanna create actions that are available to different workflows in different repositories, you must create them as standalone repositories. Those actions must be standalone repositories if you want that. So here I'm going to create a locally available action instead and in this actions folder, I'm going to create another subfolder, which I'm going to name cached-deps for cached dependencies because my goal is to outsource the step for caching the dependencies and for installing the dependencies into a separate action. Not the code for checking out the repository code because since my action will only be part of that repository here, I must get the code of the repository before I will be able to use my locally defined action. If my action would be defined as a standalone public repository, then I could move the checkout code into that separate action because I could then reference the action via the repository URL. But since the definition for my action will be part of this project, the checkout code can't be part of the action itself because we must check out the project code before we can use the action defined in the project code, and in the project files. That hopefully makes sense. That's why I'm just moving the caching and the dependency installation step into my custom action. But speaking of that custom action here, how can a custom action be created now in that folder which we created here? Well, no matter which kind of action you're creating, if it's a composite action, a JavaScript or a Docker action, you always must add a action.yml file. So a file that contains the configuration and definition of that action. This file must be part of any action you create. It also must be part of any repository that holds an action you might create. So here in this file, we now configure this action. And for this composite action, this also is the only file we need. For JavaScript and Docker, we'll need more files as you will learn later, but for a composite action, this is the only file, and in this action.yml file, we start by giving our action a name. For example Get Dependencies or maybe Get & Cache Dependencies is a better name. So that's similar to the workflow definition file where we also specify the name. However here, we specify a name but now we don't specify an on key. That does not exist for custom actions because actions are not workflows, instead they are used as part of workflows. Therefore, they don't need a trigger. Instead they will be included in workflow definition files later. Hence the next key we add here instead is a description where we could say get the dependencies and cache them. Maybe we also add some clarification that we'll use npm for getting the dependencies. This description is important because if we would publish an action to the marketplace, it's essentially the description that's showing up here, the title that shows up here, the name of the action and then the description below it. So therefore now we have that name and that description. Next, for the composite action, we add the runs key. So not steps or anything like that but instead runs. And runs then needs a nested key called using, which must be set to composite here. So this must be composite. Make sure you don't have a typo in there and this also must be using and this must be runs. Up to this point, you have no choices regarding that. And this will tell GitHub Actions that this is a custom composite action. Now as a next step, no pun intended, we add the steps key here. So now we have steps here and now nested below that, we define steps basically as we also defined them here in our workflow definition file. Though I will highlight that it's the official syntax documentation for custom actions that should be visited to learn all about the different fields that are supported for these steps. Here you can learn all about the fields that can be added to the individual steps but in general, this is similar to what you learned about steps and workflows. So therefore, what I can actually do here is I can grab these cached dependencies and the installed dependencies steps from my workflow, and simply paste this here below steps in my custom action. Just make sure you don't mess up the indentation. It should be indented like this. So now I add these two steps as steps to this custom action, and I can keep all the keys I was using in those steps because they're still supported. That's especially also true for the if key here where I can still run this step conditionally only if we had no cache hit here. Now, one special key that must be added if you're using the run key though is the shell key where you choose which shell should be used for executing the step. And here I wanna use bash as a shell. So that's an extra setting you must add here when creating a custom composite action and you're using the run key. When using another action, as I'm doing it here, you don't have to add the shell key. And that's, by the way, also important. You can use other actions in your own actions. That is absolutely fine and totally normal. So with that, we added these two steps here and with that, we built our first action. Now we can use that action in our workflow.

Now, how do we use actions and workflows? Well, for that I'll go to my workflow file, and now here in that lint job, I wanna replace these two steps with my custom action. To do this, I'll give it a name, load and cache dependencies, for example. And then we use the uses key, just as we used it for actions before as well, there is no difference regarding that if you're using your own custom actions, you still use the uses key. Now, the value you define here simply depends on where your custom action is stored. If you did create your action as a standalone GitHub Repository, you would simply add your Repository Identifier here. For example, Maxacademind1/my-action, if my GitHub Repository name would be my action and this would be my account name. That's how I could refer to my own action stored in my own GitHub Repository. Here I have a local action though, stored locally in this project, and therefore what you pass as a value to uses instead is a path to this action folder. For that you start with ./ to construct a path relative from the root project folder and then we dive into the GitHub folder and then into /actions in my case since I named this folder, actions, and then cached-deps is my other folder name. You don't have to point at action.yml here, this is not required because GitHub Actions will automatically look for such a action.yml file. As mentioned before, you must add such a action.yml file to any custom action folder or repository you have. And that's how we can use our custom action in this workflow. What's really important here is that this path starts at the root level of this project, It's not relative to the workflow definition file as you could think, instead, it's relative to the root folder of your project. With that added, we should be able to create a new commit where I say, added a first custom action and then push this to GitHub. With that, we should of course trigger a new workflow run, and here we should see that in the linting job here, because that's where I'm using this custom action, here in the lint job. That in here we are using this custom action, and indeed we did. Here, that is the custom action we used, that step uses the custom action and you see that it ran successfully and it did use the cache here and then therefore not install the dependencies again because in custom action I have this if check to only install dependencies if they were not found in the cache yet, which was not the case here, here instead, the dependencies were restored from the cache. But the most important thing is that our custom action was used successfully. With that, we can of course use that same step in all the other jobs as well and replace the two steps where we cache and install the dependencies with that one single action here. And that also shows why composite actions are useful because now this workflow file became simpler, we got rid of some duplicate code, some duplicate definition statements and instead we're using this custom composite action. With that, I'm going to create another commit using custom action in all jobs, and I will push this as well to trigger another workflow execution, of course, where now this custom action is used in lint, test and build. And still the overall workflow should finish successfully because the custom action is used successfully. Here we go, all jobs finished successfully.

So, that's a first custom action. It's a pretty straightforward and simple custom action, but nonetheless, very useful. Now, sometimes you also want to make your actions configurable. You wanna accept inputs in your actions so that in the Workflow definition file, the same action can be used with slightly different inputs. For example, we already see inputs here, for the upload artifact action, where we have the name and path inputs, where we give the artifact package, a name, and we defined, which artifact should be included. The same here, for downloading artifacts. I choose, which artifacts to download by name, and then I also define the path, where those artifact files should be downloaded to, on this runner machine. So these here, are inputs which we set on this action, or the upload artifact action. And we can also accept and use inputs on our own custom actions, no matter, which action type it is. Now, to use inputs on custom actions, you simply add the inputs key on the same level as you added name, description, and runs. And then nested below, inputs you, add your inputs with any names of your choice. For example, here, we could say that we wanna add an input that allows users to control whether dependencies should always be installed, no matter, whether they were cached before or not. So essentially, we wanna offer an input, that allows users of this action to turn off caching, for whichever reason, but we wanna give them the option of doing so. Therefore, here, I'll add a input called caching. This name is totally up to you. And then nested below that, we first of all add a description. This is now, not up to you. This description key must be added Here, you describe the input and what it does. And here, I'll say, whether to cache dependencies or not. As a next field, you add the required field, where you define, whether this input is needed or not. This can be set to true. In that case, users must provide a value in their Workflows when using this action. And if they don't, the action will fail. Or you set it to false, if it's actually, not needed. If you do choose, required false. You must make sure that your action code does not fail, if it's not provided. And you can, for example, make sure that your action will still work, by adding a default value, with the default key under your input name. This allows you to, well, specify a default value, like, for example, true between quotes here. We could then use the caching input in our steps here, and assume a default value of true, unless users specify different value, when using this action their Workflows. And now, we can use this input, for example, by adding the if key to the cache dependencies step, and then using the inputs context object, which is one of these context objects provided by GitHub Actions, and available in a custom action to reach out to these inputs that were received by that action. Here, I'm using the caching input, and it's caching down here, because it's caching up there. So, you have to use the same key name. And this gives us access to his caching key, and the value that's received on that input. And I check, whether it's equal to true between quotes. So, if this is true, if you wanna use caching, then this step will be executed. If it's set to any value, but true, this step here, will not be executed, thanks to this, if check. Now here, for installing dependencies, I already have an, iF check, but now, I wanna modify this check. I wanna add an, or condition. So, an additional alternative condition, where I check, if inputs.caching is not equal to true, because if this is not equal to true, if we don't wanna use caching, then this step should execute. That's why, I'm using, or instead of end, I don't want both conditions here, to be met. But instead, if either of the two conditions is not met, I wanna run this dependency installation command. So therefore, I'm checking for the caching input, like this here. And that's how you can add a custom input to your custom action. And of course, you can add as many other inputs as you want, and need. You can add just as many as needed by your action, and we'll see more inputs in action, in later lectures. With that added now, we could create a commit and push it, because this action is not required because we have a default. Therefore, this action would actually, not fail. But we can now, also add a different value, when we're using the action. For example, we could say, that we wanna use caching for testing and building, but for some reason, not when linting. To achieve this, we can add the, with keyword, just as we used it for third party actions as well. And then nested below with, we can define the inputs expected by our action here. So in this case, we have the caching input. Therefore, I add the caching key here, and for example, set this to false. And this should disable caching for this action, in this Job here, or in this step here, to be precise. Not in any other steps or Jobs. And with that, if I now create a new commit, added caching input, we should see that as we push this, of course, the Workflow runs again. But now, for the linting script, we should actually see that it does not cache the dependencies, but instead, it will reinstall all dependencies every time this Job runs. And for the average Jobs, testing and building, it should use caching. So here, for linting, you indeed see that, for our custom action step, it did use this command to install the dependencies. So, it did not use caching here. For testing, on the other hand, if we take a look at this step, we see that instead, it did get that data from the cache, and we see that it did restore the cache successfully, and that it did not run the command to install the dependencies. So, this custom input is working as intended.

Now, just as you can add inputs to your custom actions, you can also add outputs to your custom actions. And to add an output, you add the outputs key on the same level as you added inputs, then nested below it, any output with any identifier of your choice. For example, used dash cache. And then nested below that, a description for this output. Cache was used, for example, or whether the cache was used. So that's how you define an output. To now assign a value to it, you have to add the value key just as we did it earlier in the course when we added an output to one of our jobs. Now, the value here should refer to our steps and therefore here I'm using a dynamic expression, and then the steps context. And now we must refer to one of our steps with help of the IDs assigned to the steps. For example, here I could add an ID to the install dependencies step called install, and use that same ID up here, so that ID which I defined down there. And then in that install dependencies step I could run multiple commands where one of the commands installs the dependencies, but the other command uses the set dash output command we learned about earlier in the course. To set an output with the name, let's say cache, this name is up to you though, and set it equal to a value of between single quotes. And now here I want to use another dynamic expression and actually use my input here which I called caching. So here I can then use inputs.caching. Of course therefore, the output is kind of redundant because in the end, I set it to the same value as this input was set. But I just wanna show you how you can use outputs in general. We'll see a more useful example for an output a little bit later when we build other kinds of actions. So here I am setting the cache output of this step to the value of the caching input. And therefore, now here when we set the value for the overall action output, we reach out to this install step, now there to the cache output by using .outputs.cache, and that will make sure that the value provided here, which is of course the input value in this case, will be provided for this used cache output. And this output of this action could now be used in the Workflow after using the action. Here, we could add a new step, output information, where I simply output some text. And to be precise, I will say cache used, and then add a dynamic expression where I reach out to my previous step by giving this step an ID, cache steps for example, and using steps.cache deps.outputs here. And then the output I defined, which in this case was used cache, so that is what I use here. And that will now print the output value of this custom action step here in this step. That's how it should work. Now we can create a new commit, added output and push this, and we should see that now our output is set and used as expected. So here for the linking step it did now finish our custom action step, and then in output information, it indeed does print that output from our own custom action step.

So now we got started with custom actions to be precise with building custom composite actions. As you learned, that's just one kind of action you can build though and we're now going to continue by building a custom JavaScript action as a next step. Now, one important note about that, of course you need JavaScript knowledge to develop JavaScript actions but you can still follow along here in this course if you don't have any JavaScript knowledge, because we're not going to write a complex action and I am going to provide the key code to you. So back here in the project, in order to get started with that next action, I'm going to create a new folder under my actions folder in the dot GitHub folder so it's going to be another local action here. And here I'll name it, deploy-S3-javascript. Now what this action will do is it will take the built website code and upload it to Amazon S3, which is a service offered by AWS, Amazon Web Services, that can be used for hosting static websites. Now, this is not an AWS course. I do have a separate course about that if you wanna learn more about AWS and get started with it and even get AWS certified. But for this course, you don't need any AWS knowledge and you also don't have to create an account if you don't wanna follow along. You're still going to learn all about GitHub actions and custom JavaScript actions even without having an AWS account. If you do wanna follow along step-by-step though you can create such an account. You will need a credit card for that though. And then in that account, we're soon going to create an S3 bucket, as it will be called, which will then later be used for storing the website files. But this will be one of the next steps. As a first step, it's just important to create this folder in the actions folder and in there create an action.yml file because that's always needed no matter which kind of action you're building.

Now in this brand new action.yml file we start by adding another name. So this field is the same as before for the composite action. And here the name could be 'deploy to AWS S3' because that's what this action will do. It will deploy the website files to that service so that we actually have a life website in the internet and we add a description where we could say, 'Deploy a static website via AWS S3.' Now the next step is to again add the runs key as we also did it for the composite action. There we also added runs. We're also adding it here for the JavaScript action. And we also added the using key but this is now not set to composite, but instead to node and then one of the main versions of node. At the point of time I'm recording this node16 is the version that should be used here, as always the official documentation is the place to go to learn about the latest version you should set or in general about the versions that are supported. At the point of time I'm recording this I could set it to node12 or 16 but this might change when you are viewing this. The code will still work though. So in my case here it's node16 and this tells, get up actions that this is a custom JavaScript action because we're using the node runtime which is a JavaScript runtime. Now, as soon as you're using a JavaScript action you also have to add a main field here. And that's now of course different from the composite action where we instead added steps. Now we're adding main instead. And as a value for main, we specified the name of the file that should be executed for this custom action. And in my case here, I'll add a new file called main.js though that name is up to you. It does not have to be main, but here I'll name it main.js. And in the action.yml file for this main field we now specify that file name or that path to that file. In this case, the file is in the same folder as the action.yml file, hence just the file name suffices. So the file name is all we need. So we add the file name like this here, and this is the bare minimum we need to define a custom JavaScript action. And what GitHub Actions will do then is it will execute that main.js file and therefore execute all the logic that might be in that file whenever this custom action is used in a Workflow step. Now there are more fields you could add. For example, all the pre and post next to main. And this would allow you to point at extra JavaScript files that should be executed before or after this main JavaScript file executed. In some situations, you might need to perform extra setup or clean up work, and in those cases pre and post can be useful. Here for this example we don't need any extra scripts to be executed before or after this main script and therefore this main script is all I need here. And with that, we can now start writing some logic here.

Now, regarding the logic in that JavaScript file, as mentioned before, it, of course, is important to have some JavaScript knowledge but for this course, we'll stick to basic code so you will be able to follow along without that knowledge. Now, what I'll do in this file is I'll create a so-called function called run and I'll execute it right thereafter. This is technically not required and the name also doesn't have to be run. You just see it in a lot of actions and it's therefore a pattern I also use here. Ultimately, this function will be executed right when this file is being executed, so you could also put your code right into this file without this function but here I'll use the function, which also allows us to, for example, turn it into a async function, which is a more advanced feature and not too important here. But with that we'll put the main code inside that run function here, and what I wanna do here to start simple and keep things simple for a start is I just wanna output some info message. So basically, the equivalent to echo something. But we can't use echo here because that's not JavaScript code. Well, if we wanna send a message to GitHub Actions and its log file here, we have to use a special function provided by GitHub Actions. And to do that, I'll open my terminal and navigate into the github/actions folder and then there into the deploy-s3-javascript folder and run npm init -y here. For this command to succeed, you must have Node.js installed on your system and you can simply install the latest version from nodejs.org if you wanna follow along. That will unlock the npm command and you will be able to run npm init -y. And this is important because as our next step, we're going to install some specific dependencies that we can use in our JavaScript actions. And these dependencies are the @actions/core, @actions/github, and @actions/exec dependencies. These dependencies are actually all part of the Actions toolkit provided by GitHub Actions, an official set of JavaScript packages you can use in your JavaScript actions. These are various packages that can be installed that provide various pieces of functionality that can be used in your own JavaScript actions. That's why I'm installing these three packages into this action folder here by running npm install inside of that action folder. And this will now add these actions as entries in my package.json file, which manages the dependencies of this project. And it adds the node_modules folder here, which contains the actual package code. And now in main.js, we can import those packages with the specific import syntax by Node.js by, for example, importing the core package, like this. And that can be repeated for the github package and the exec package. Now, we don't need the exec package yet and we'll also dive into the github package a little bit later but the core package gives us access to various utility methods and features that we can use in our JavaScript action code. For example, it has the notice method, which we can use to log a message to the GitHub Actions workflow log. For example here, we could say Hello from my custom JavaScript Action! And this will print this message to the log. And right now, that's actually the only thing I wanna do. With that done, we can use this custom action in our workflow just as we used the other custom action before. And here I'll add a brand new job called information on the same level as deploy and build and test, of course. And this job does not depend on the other jobs. It should run in parallel instead, still on ubuntu-latest though. And then here I'll just add one single step, which I'll name run custom action, and here I use the uses keyword to use my custom action. And just as before, we must grab the path to the action that should be used, which in this case is, of course, .github/actions/ and then the name of this folder here. And that will now use this custom action that should trigger this main.js file and should therefore log this message. Now back here in the root project folder, we can add our files and stage them and create a new commit, added first custom js action. And push this. And what's really important before I push this is that here this node_modules folder is not ignored. If I had such a node_modules folder on my root project level, it would be ignored thanks to this gitignore line but the nested node_modules folder here won't be ignored because your custom JavaScript actions must include all the code and all the dependencies that are required to run this action successfully. GitHub Actions will not download and install dependencies when it encounters a custom JavaScript action. Instead it expects that all the code is available that's needed to run this custom JavaScript code and that includes the code for third-party actions. That's why this is also committed so that this package code is available when it's needed right away. And with that, I'll push this to GitHub and there a new workflow run is started. And here in the information job, we should see our custom action being executed and I get an error. I get an error that it can't find action.yml. Now, why is that? This is an error you can sometimes get when you're building custom actions. It's quite common to run into. And why are we getting this error? I mean, we clearly have a action.yml file here for this custom action. Well, yes, we have the file but in that job where I used a custom action, I only have that custom action step. I never check out my repository code. I never get the code that includes the custom action definition. And that is required in order to use the custom action because it's a local action stored in this repository. If this would be an action stored in some other repository, we could use it without checking out the code first. But here since it's part of this repository, we must add an extra step here where we do check out the code so that the action code is available before we try to use it. And with that, we can create a new commit. Check out code, and push this, and now this action should execute successfully. So let's evaluate that new workflow run that's being started here and let's take a look at the information job. And it still fails here but this time it's some dependency error, some missing module. And the reason for this missing module file is that I still have an error in my gitignore file. I'm ignoring all files that are inside of some dist folder. Turns out that in this node_modules folder in my action folder for this uuid dependency, which this error message is about, I ignore the dist folder and therefore, it's part of my local project here but not of the Git and GitHub repositories. To fix this problem, I should make sure that only the root-level dist folder is ignored by adjusting this gitignore line like this. And with that done, this dist folder here should be included and therefore now if we create another new commit, I can say added extra files and push these and now we're including all the files that are needed to run this action. And that's really important because as you see, it's a common gotcha to accidentally remove files that are needed to run the action but as mentioned before, all the code that's needed by the action must be part of the project repository. GitHub Actions will not start installing any missing dependencies for you. That's not going to happen. But with that, it did now execute this custom action and we see our log message here. So that's our first basic JavaScript action. Now let's create a more useful one, which really does what the name suggests: an action that will deploy code to AWS S3.

Now for the next course lectures, I will use AWS S3. I will use AWS S3 as a hosting provider for this demo static website. Now you can follow along by creating an AWS account but of course you can all suggest follow along by watching the videos. This course is not about AWS and it will still show you all the steps that must be taken in your action code and your action configuration and you can follow along with that even without an AWS account. But you can of course also create a new AWS account or use an existing account if you want to. And here I'm in my AWS account. And there you can search for the S3 service. And what we will do here can be done for free, if you're within the first 12 months after creating your AWS account. And there you will need to create a new bucket which is simply a folder in the cloud you could say, a directory to store files in, in the cloud, in the AWS cloud, because AWS is a cloud services provider allowing you to store data files and processes in data centers. And then you have to give this bucket a name which must be globally unique. Therefore, here I'll choose something like gha custom action hosting, something like this. You might need to try a couple of combinations to find a bucket name that has not been used by any other users. Next, you have to choose a region which basically defines which data center will be used for storing your data and I'll stick to the default. You can keep all the other default settings but you should uncheck this box here and check this box instead. So that we do allow public access to this bucket because we will use it for hosting a website, hence public access must be granted. And with this, this bucket can be created. And thereafter we can click into this bucket. And under properties, at the bottom of this page, we enable static website hosting and specify index.html as the index document to make sure that this bucket acts as our website host for our aesthetic website which will eventually upload here. And under permissions, we now also go to bucket policy, click edit and then policy examples. And here we grab that read only permissions to anonymous user policy and paste that into the policy field here and replace doc example bucket with our bucket name, which can be found up here. So we paste that in here, keeping the slash and the star thereafter. And then we save this here at the bottom, save changes. This makes sure that we can actually use this bucket for hosting later, though here I have an accidental blank at the beginning, which must be removed. That's why I got an error. Now let's try this again. And with this we can upload files into this bucket to well serve a website and the files that should be uploaded here are the files that are produced by this project when running this build script. So the files that are actually produced as part of our build job. These files which we upload here, these artifacts, that is what we would need to put into this bucket to have a website. But we're not going to do this manually but instead automatically and we're going to write our own custom action that does that because that's a great practice and even though we would find actions in the marketplace that help us deploy to S3, writing our own action here allows us to dive deeper into custom actions.

So let's now start working on this JavaScript action that will deploy the code that's produced by the Build step in our Workflow to AWS S3, to that bucket we created. And for that in Main JS, we got a couple of things we have to do instead of this Run function. And the first thing we'll have to do here is that we get some input values, because we'll actually need some information about the deployment target, so about this S3 bucket, in order to move our code here. Of course we could write this JavaScript action such that we hard-code all the values for this specific use-case, and this specific bucket, for this specific AWS account, into this action. But that wouldn't make too much sense. We typically wanna build actions that are reusable in theory and that get dynamic values via inputs. And therefore I'll go back to the action of YAML file, and first of all, add some inputs. Because you can do that for JavaScript actions, and also for Docker actions, which we'll take a look at later, just as you can do it for composite actions. And here this action needs a couple of input values. For example, it needs the identifier, the name of the S3 bucket to which the files should be uploaded. So, in our case, the name of this bucket here. But again I don't wanna hard-code it into the action. I wanna create an action that could be reused. Therefore, I added the this bucket input here. Of course, this input name is up to you. And then we have to add a description where I simply say the S3 bucket name. And we have to define whether it's required or not. If it's not required you can again add a default value here. However, I will require it because there is no sensible default value we could add. The bucket name, could be anything after all. So that's the first input. The next input is the region of the bucket, because with AWS you actually deploy services in regions. When we created that bucket, we had to pick a region to which this bucket belongs, and therefore this bucket region input is required here, or is needed here, in order to move that code to the right bucket in the right region. Even though the bucket name must be unique, we still must specify the region when uploading our code. Therefore, here I'll add a description where I say the region of the S3 bucket. And this could be required because we need the region but unlike for the bucket name, here I will set it to false, because we can make a guess regarding that region and set a sensible default which would be US-east-1 which is typically the default region selected by AWS when you haven't picked any other region. So it makes sense to assume this as a default. Of course, it can still be overwritten by users of that action inside of their Workflow files. Now the last input, which I want here is the Dist folder which should simply be the path to the folder that contains the files that should be uploaded to that bucket. So the website-files in our case. Therefore here I'll add another description which is the folder containing the deployable files. And then here I set Required to true because we need that folder. Of course, we could set it to false and set some default like Dist if we expect this kind of folder name. But here I will simply require that. So with that, I've got three inputs which must be specified when this action is used. And now we can extract those input values in our JavaScript code here.

Now to get input values in the JavaScript code of this JavaScript action, we can use this core object, which we're importing from this GitHub provided package. This core object does not just have a notice method, but it also has a getInput method, which allows us to get one of the provided inputs. Here we have to pass a string, so some text as a first argument to this method. And the string, which we do pass here, should contain one of these input names. For example, to get the provided bucket, I'll use bucket here, because that's the key I chose. And in main.js, I'll add this here as the key I wanna retrieve. Then we can add a second argument, which is a JavaScript object, created with those curly braces, where you can set some extra options. Like for example, that excess white space should be removed automatically, or that it's required, which this is. And then getInput will return a value, which I'll store in a constant called bucket, here. And now I can retrieve this for the bucket-region, since that's another name I chose here. Bucket-region. And I'll name this bucketRegion here. And I also set it to required here, because whilst it's not required here, I do need it here in my code. And here, I'll set required to false, because actually in my action.yml file this is also false. By the way, this required setting would simply cause an error if we try to receive the value, and no value is yielded. Here, I could also set this to true because even though it's not required here, we do set a default value and therefore we will always return a value even if the user of this action didn't provide one. And I will actually use true here, now that I think about it, because this value will be needed later and the code would not work, as expected, if this would be empty or if no value would be provided. Now there's one last input which I wanna retrieve. And that's of course the dist-folder input. So we also add this here as a key, and store it in dist-folder, like this, for example. And with that we also extract this input here. Now that the inputs were extracted, we can use them. And I wanna use them to target this S3 bucket, which data I get through these arguments here, through these inputs. And I wanna send my files in the dist-folder, to this bucket. So here I wanna upload my files. Now to upload these files, there are a couple of approaches we could use. We could, for example, use the AWS SDK, the Software Development Kit for JavaScript, to use some JavaScript methods provided by AWS, to interact with AWS services like S3. We could do this, but here I'll choose another approach. Here I'll use the exact package, provided by GitHub, to actually run a command that we could also run in the command line. But I will run it from inside this JavaScript script file here For that, on the exec object, we can call exec method. And that method needs a command as a string. Here, we could, for example, use echo because this will be executed in the regular shell of our runner. But here, I don't wanna echo. Instead I'll use the AWS CLI, which is actually pre-installed on these Ubuntu images, there. If you take a look at the software that is installed on these images, right from the start You will see that it does come with the AWS CLI. And this is a command line interface. It's a tool, executed in the command line, that allows us to send commands to our AWS account. And here we can use the "AWS S3 sync" command, with whitespace between AWS, S3, and sync, to synchronize a local folder to an S3 bucket. For that, we now just need to specify the local folder here. And then the S3 bucket path, here. And then we can add some extra option flags. Now for the local folder, that of course is my dist-folder. And therefore we can create a constant but we don't have to. For the S3Uri, for the identifier of our S3 bucket, that is a path which I'll construct dynamically, by using back ticks here. Two back ticks, not single quotes, but back ticks. To construct a dynamic string value, where I inject a value with this special JavaScript syntax. So this is all JavaScript. Has nothing to do with GitHub actions. And here, I will inject my bucket. So the name of the bucket. This is my S3 bucket path, therefore. Hence, I will also inject this here, by converting these single quotes to back ticks. So that I can use this same injection syntax here in the exec line. Line 13. And I'll inject this S3Uri value here. So this identifier of the bucket. And I'll inject a value for the local folder, and that will be my dist-folder, here. So now, with that, I'm telling the AWS CLI which local folders should be synchronized with which remote S3 bucket. And this command could be executed in the regular shell of our runner. And that will indeed happen behind the scenes. But we're triggering the command through our JavaScript action file here. And that's something you can do with the help of this exec package here. We're not done though, I will add an extra flag with "--region", to specify the region where this bucket can be found. And this must be added here. And here, I'll inject the bucketRegion, which I extracted. So I'm using all these inputs, which we're getting for this action. And this will now trigger this command, which should try to send our files to the S3 bucket. Now we're almost done with this custom action. And before we use it in our Workflow, there's just one thing I also wanna show you real quick. And that's this Github, this "actions/github" package here, where we import the GitHub object. We're not using it here. But this object would give us access to the Octokit client, which in the end is a tool provided by GitHub that makes it easy to send requests to the GitHub REST API. And we get access to a context object which offers some values. Not all values but some values of the GitHub context object. For example, we could get the name of the action that's currently executing, and a couple of other values as well. We don't need it here, but I wanted to mention, that this GitHub package does give you this extra data, and this client, this Octokit client as it's called, which can be used for communicating with GitHub's APIs. But that's it for this script file, at least for the moment. Let's now see how we can use this custom action and how we can use it such that everything works.

So how can we use this custom JavaScript action? Well, we again use it in our Workflow file of course because that's where we use all actions. And here I want to use it in this deploy site step here. In this deploy site step of the deploy job I did download my built files, my artifacts which were built in the previous job in the build job here. I did download them and store them in a folder called dist, like this. And now I wanna replace this step here where I just output a dummy message right now with a step where I use my custom action. For that I again, point to that action folder just as we did it before for other custom actions as well. Here, my action is called Deploy s3 Javascript. I will by the way remove that action from the information step. I will actually remove the entire information job because I don't need it anymore. So that's how I point at this action. And then we must add with to provide some input values. Because keep in mind at least the values for bucket and dist folder must be provided because required is set to true. So therefore, here I'll add the bucket input. And there you wanna provide your bucket name so this name here in my case. Then I also must provide a value for the dist folder, and here that should be dot slash dist pointing at dist folder, which contains the downloaded build files. So that the files from this folder will be uploaded. And you could also provide a value for bucket region if your bucket was created in a different region than US east one then you could, for example, add US east 2 But here, this is not needed. Hence I'll comment it out. This will run our custom action, but at the moment this won't succeed. But why won't it succeed? At the moment, our custom JavaScript action would fail because in that actions code, we're trying to run this command here where we use the aws cli. Now this command tries to upload local files to s3 bucket. It should make sense that not everyone is allowed to do that out of the box though. Otherwise, everyone could start targeting your bucket and upload random files to it or download or delete files with other commands. This is clearly not something you want and therefore by default this will fail because you don't have permission to interact with that bucket. Now, how do you get the permission to do so though? Well, for this, aws offers a feature which you can reach by clicking on your account name in the top right corner, and then security credentials. Here you can generate and download some keys that can be used for authenticating commands that you're sending to this AWS account. These access keys here to be precise. Here you must create a new access key and these values will only be shown once. You can download them though. And these two values, the access key ID and the secret access key can be used to authenticate this command. There also is a different way of authenticating commands to which I'll come back to in the next course section but this is one quick and easy way of doing so. So therefore I'll grab this access key. And now the question is how do I provide it to that command? Well, you do that with help of environment variables. The aws cli will look for certain environment variables with certain names and extract values from them. And one environment variable it will look for is the AWS access key ID environment variable written like that. So if you set that variable to that access key, which you see here then AWS will use that for authenticating requests. But you can't set the environment variable like this. And we wouldn't want to do this in this JavaScript code anyways because again that wouldn't make that code very reusable. Instead, we wanna set this in our Workflow and we can set the environment variables needed by that action right in that step with help of the env keyword here. Before in the course we already used env on job level or even Workflow level and we could do the same here. But these environment variables are really only needed by this action in this step. Therefore, I'll add step specific environment variables which are only accessible by this step or by the action performed by this step by adding the env key to this step. And then nested below the env key, we can add this special AWS underscore access underscore key underscore ID environment variable and set it to this key here and do the same for this secret access key where I add my AWS secret access key environment variable written like this. Now of course, these are values that shouldn't be added like this in code because everyone who sees them will be able to send commands to this account and do malicious things with it potentially. And therefore, by the way, I will also delete these access keys after recording this video. So you don't have to try using them, but you can of course but it won't work. But we shouldn't expose them. Nobody should be able to see these values. And of course, we learned how we can make sure that nobody sees these values. We can use a secret. For that I'll go back here to this Repository and we can add secrets by adding an environment. If this would be a public Repository or we had a paid plan or we add Repository wide secrets, which I will do here. I'll add a new secret here, which holds my access key. And I'll name it AWS access key id and add the secret and do the same here with the secret access key, add a new Repository secret with a name of AWS secret access key. And now these values are stored securely. And here we can extract them by using secrets.aws access key. And here it's secrets.aws secret access key. With that, we're setting these environment variables. And as mentioned before, this aws cli command will automatically look for these variables with these names to authenticate this request. And therefore, now with that finally we can create a new commit added custom deploy action and see whether this all works by pushing this to the Repository. Here a new Workflow run is triggered and this should now hopefully succeed. And in the deploy job upload these files to this s3 Repository. For that we can go back to s3 and then also dive into this bucket here. And eventually the website files should be uploaded there. But right now, this is still all executing. So let's wait until this Workflow finished and here it did all finish. And if we take a look at this deploy job, we see that under Deploy site, our own action was executed here. It did use these environment variables and our extra inputs and it then synchronized the local files to this s3 bucket which we can also see if we refresh these objects here. Here they are. And now we can view this website if we want to by clicking on properties on this bucket. And down here we find this url, which is auto generated by aws, which allows us to view this deployed website which is now really out there in the internet.

So that we can build and use this custom JavaScript action. To really finish it now, I want to add an output to this action, and I wanna dynamically generate this URL of this website, so that we can visit this URL right from this summary screen here on GitHub Actions. Or, to be precise, ah when visiting this action. There, I want to add an extra step where I output that URL, so that I don't have to go into my AWS account to get hold of that URL. So, therefore, this action should generate an output, this URL. And to do that, we first of all have to register an output in action on the YAML. Just as before for the custom composite action, we do that by adding the outputs key. And then, we add an output with any identifier of our choice, like for example, website dash URL. Now, this takes a description, which is the URL of the deployed website, for example, but it doesn't take anything else. You don't add a value here. We did that for the composite action but we don't do it here. The reason is that for the composite action we had all the logic right here in this action YAML file and we needed to tell GitHub where to get the value for this output from. With the custom JavaScript action, that's different. We just registered this output but we then set it from inside our code. And here right after deploying this instead of logging this dummy message here, I will generate this URL instead by adding a new constant name, the website URL. And then again I'm using backticks to build a string dynamically. And here this is an HTTP colon slash slash and then we inject the bucket name because if you take a look at that URL, that is how it's built. It's http then comes the bucket name then dot S three slash website slash, and then the region. And that's what we're going to build manually here. So after the bucket name, we add dot S three website dash and then we inject the bucket region and then thereafter dot amazon aws dot com. That's how this URL is built. And with it being built, we can then use the core package again, which we also used for getting the inputs to set an output with set output. And that's basically the equivalent to using set output in the echo commands. So that's the equivalent here. And here we target this website URL output. So I'm using this identifier here as a first argument to set output. And the second argument is the Value we wanna set. And here that's that website URL. And with that this GitHub action will set an output return and output. So to say that is the automatically generated URL of the website that was deployed. This output can now be used in the Workflow file where I will simply add a new step after deploying the site where I say output information as a name. And then here I wanna run a command where I echo live URL. And then here I use the GitHub expression syntax for generating and injecting a dynamic value. And here I wanna reach out to my steps context object and then to the previous step. And for that I'll add an id to it, which you of course can do for steps with custom actions as well. And I'll name it deploy, the id, of course is up to you. And then here it's steps dot deploy dot outputs. And this must be outputs here, but then your output name. And in my case for this action the output name is website dash URL. So that is the output name we're accessing here. And I'm simply outputting this here in my log. And with that, we can create yet another commit add an output, and push this. And now the website should not just be built and deployed but we should also get an extra log message in the last step of the deployed job which links us or which leads us to the deployed website. So let's again wait for this to finish here. Here we go, it did now finish, and now we got this output information step where we output this URL and which leads us to this website that was deployed. And that's our custom JavaScript action finished and shows you how you can build and use such a custom JavaScript action.

So we did now build and use composite and JavaScript actions, let's now do the same for a Docker action. And for this, I'm back in that same demo project as before, but I'll add a new folder in this actions folder, and I'll name it deploy-s3-docker, because I'm still going to build the action that will upload the website code to S3, but we're now going to do this with a different programming language than JavaScript, and therefore, we'll use a Docker container to set up an environment that supports that programming language, because GitHub Actions out of the box only supports JavaScript and Docker, and that's why we're using it now, because with Docker, you can build a container with any environment your code needs, including an environment that supports the programming language you want. Now, since this is no Docker and no Python course, and we already spend some time writing code here with JavaScript, I provide the files that are needed for this action for you, at least all the files except for the action.yml file. You'll find them attached, and you should paste them into this deploy-s3-docker file. Here, you get a deployment.py file, which contains the actual Python code for getting all the files that should be uploaded and for then uploading those files, and this code uses a package called Boto3, which is provided by AWS, which is the AWS SDK for Python to be precise, which gives us methods for uploading files to S3, this upload_file method here most importantly. So this method will upload files to an S3 bucket, and the rest of the code will simply identify the files that should be uploaded and then upload them, and then I'm still generating a website URL here. I'm also setting it as an output, but I'll get back to this later, and I'm also extracting some inputs here, but I'll get back to this later as well. For the moment, it's enough to know that this is all the Python code that's needed, and the Docker file simply creates an environment where this code will run. It creates a new container based on some Python based image, which gives us an environment where Python is installed, Python version 3 to be precise, then I copy this requirements text file into this container, and this simply specifies some packages that should be installed into the environment for the Python code to work, and that most importantly is this Boto3 package and some extra dependencies, so that is copied over, and then this command installs all the dependencies mentioned in this file. And then here, we also copy over the deployment.python file, and execute it whenever a container is started based on this image. This is what this Docker file does, this is what the requirements text file does, and then we get this deployment.py file. Again, I'll get back to some lines of code in there, but for the moment, this is the base setup we need.

Now in order to create a Docker action based on that Docker file and the code that's kind of related to it, I'll add a new action.yml file. As mentioned multiple times, that's always the same. Now, we also add a name here, deploy to AWS S3, for example, and a description, and I'll simply copy the description from my JavaScript action because it is the same. And then, we also add the runs key just as we did it for the composite and the JavaScript actions. Now here we also must set using, but here we must set it to Docker. That's important, it must be set to Docker, and this tells GitHub Actions that this is a Docker action. You also must add the image key then, and here, we have to write Dockerfile, to be precise, we have to provide the path to our Docker file that defines the container. Alternatively, you could also refer to some publicly available image here, for example, an image on Docker hub. And as before, the official documentation informs you about all the values that can be added to runs for Docker actions. Besides the image, which as mentioned, could also point at some public repository, you can also override various settings from the image file, but this is a bit more advanced and not something we'll need for this example. These here are the two main settings you must provide though. Now with that, this Docker file would be executed or would be used and the container would be created based on that file in the end, but that's not enough for this action. Instead, just as the JavaScript action, this action needs some inputs, because it does the same thing as the JavaScript action. It uploads files to a S3 bucket and therefore we must inform the action about the bucket name, the region, and the folder that contains the files that should be uploaded. Therefore, I'll copy all the inputs from the JavaScript action.yml file over to this Docker action.yml file, because we need the same inputs. I also want to get the same output so I'll copy this as well. And that's also important regarding inputs and outputs, the syntax is the same for Docker actions and JavaScript actions. We register inputs like this and as explained before for the JavaScript action, and we do the same for the outputs. Now, what is different though is how we access those inputs in the code that's executed in the container and how we set outputs from inside that code. And you see how it works here in deployment.py. For accessing inputs, we're effectively using special environment variable, and these are environment variables which are automatically generated by GitHub Actions. For every input, we specify in our action.yml file, GitHub Actions generates an environment variable that starts with input underscore, all caps, and then the name of your input also all caps. So our bucket input becomes INPUT_BUCKET. Our bucket-region input becomes INPUT_BUCKET-REGION. So that's how these inputs are made available inside of your code running in the container. And it's a super easy way of extracting and using those input values in that code, by simply using these automatically set environment variables. Of course, you could still add extra environment variables in your Workflow file and we'll actually do that later. And those environment variables would also be available here. But these are some automatically generated variables that can be used for using the inputs. And when it comes to setting that output that we have here, the website url output, that's done with a syntax that we know from earlier in the course with this ::set-output command which is simply print to the console here. Print in Python is like echo in the shell, and it simply uses this set output command to set the website url output of this action to this dynamic value, which is this dynamically generated website url. So this line of code does not output the value in the command line or in the shell, but instead, it uses this special command to set the output of this action. And that's how this Docker action works. And with that, we're already done and we can use this action and I will use it again here in the workflows file. There, I will simply replace this JavaScript action with the Docker action because it fulfills the same purpose, it uploads files, it takes the same input, it returns the same output, therefore, this action folder name is the only thing that must be replaced. And what's also important is that we still must set the access keys here, the AWS access keys. We still must set those because as before with the JavaScript action, of course by default, you would not be able to upload files to S3. By default, that's not allowed. But like the AWS CLI, this AWS SDK, which we're using here in our code, this boto3 package in the end automatically looks for environment variables with these names to authenticate those outgoing AWS requests. So if these environment variables exist here with valid values for your account, this upload file command will succeed. Otherwise, it would fail. And that's why we must provide these environment variables for this step with this custom action as well. Now, with that, we can save all files, create a new commit, added docker action, and push this. With this, a new workflow run is triggered and we cannot wait for this to finish and it should finish successfully this time using our custom Docker action. So let's wait for it to finish here. Here we go, it did finish successfully. It outputs this url which we can click to view this website. And under deploy site, we see that it used this Docker image that it built that image that various environment variables were provided automatically to the image, amongst them, these input variables, which I mentioned before. And overall, the files were deployed, everything worked as intended, and this is how you can build custom Docker actions.

Storing Actions In Repositories & Sharing Actions With Others In this module, we created custom Actions that were stored in the same repository as our Workflow(s). Alternatively, we could've stored the custom Actions in separate repositories (which therefore then only include the Action definition + code). This is actually quite straightforward: Create a new, local project folder which contains your action.yml file + all the code belonging to the action (Important: Don't put your action.yml file or code in a .github/actions folder or anything like that - just keep it directly on the root level of your created project!) Add a local Git repository to your created project (via git init) Create your commit(s) via git add and git commit Create a GitHub repository and connect it to your local Git repository (via git remote add) Add a tag via git tag -a -m "My action release" v1 Push your local code to the remote GitHub repository (via git push --follow tags) Use your custom Action in any other Workflow (in any other project and repository) by referencing the repository which contains your action (e.g., my-account/my-action@v1) If your custom Action is stored in a public repository, it can also be published to the GitHub Actions Marketplace as described here: https://docs.github.com/en/actions/creating-actions/publishing-actions-in-github-marketplace#publishing-an-action

And that's it for this module. In this module, we learned that custom actions can help you simplify workflows and can help you avoid repeated steps. You can also implement logic that solves a problem that's currently not solved by any publicly available action because, of course, you did learn that there is this marketplace with plenty of actions that you can use just like that, but you can also build your own actions. And, of course, if you're solving a specific problem that might affect others as well. You can also share actions with the community and develop actions as open source projects potentially. Now, when it comes to creating actions there are three types of actions you can create and the first type you learned about are composite actions. Composite actions are basically actions that combine multiple steps that you could also define like this in a workflow. So you could also say that composite actions are kind of workflow excerpts. You can use actions while the users key and commands via run just as needed and simply build reusable step groups that you can then use anywhere in your workflow definition files. Besides composite actions, you also have JavaScript and Docker actions, and with JavaScript actions, you can implement any logic you want with JavaScript and the Node.js runtime and this special actions toolkit package provided by GitHub, which gives you various objects and helper functions for accessing inputs and so on. Alternatively, if you don't know JavaScript, or if you want to use a different language, or if you need more control over the execution environment, you can build a docker action where you define the environment with help of a docker file and where a container will be created based on that file for you. And either way, you can, of course, work with inputs, set outputs, and perform any logic you want as shown in this course section here.

