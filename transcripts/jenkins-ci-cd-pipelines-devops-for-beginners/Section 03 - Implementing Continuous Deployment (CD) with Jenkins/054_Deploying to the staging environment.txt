# Deploying to the staging environment

So how can we create a staging environment? We could go to Netlify. Com and from the site's menu. We could go ahead and create a new site, provide it with a prefix like staging and use that. We already know how to deploy a site, so using something similar for a staging environment would work pretty much the same. But this would be boring because we already know how to do this. Instead, we will use the Netlify CLI to deploy the project to a staging environment that will be automatically created by Netlify for us. So let me show you how this is done. Let's go ahead and open the Jenkins file. As we said earlier, deploying to a staging environment should pretty much use the same commands as deploying to the production environment. So all we need to do is figure out what we need for the deploy environment and try to replicate that. So what am I going to do next is I'm going to go ahead and copy the deploy stage. And indeed right on top. And next we're going to rename a few things. This is going to be deploy staging. And the next one is going to be deploy prod from production. So let's briefly recap what we already have. We are installing Netlify CLI. We are printing out the Netlify version. Then we are displaying a message. I'm going to change this from deploying to production to deploying to staging. We're using the command Netlify status to see if we're logged in or not. And finally we have Netlify deploy. We're specifying the directory, which is the build directory. And then we are specifying here this prod flag. If the deploy command does not detect the prod flag being sent, it will automatically create a temporary preview environment. And we will consider this to be our staging environment. I'm going to go ahead and remove this prod flag. This is practically the only difference between the two stages. The rest of the commands and how they are structured are absolutely the same. So if you deploy the same content from the build directory to this staging environment, I'm going to go ahead and commit these changes. Let the pipeline run and we're going to take a look at the logs. All right, so this took a few minutes to run. But if we're taking a look at the pipeline, we're going to see here the new stage being added. So let's go ahead and take a look at the logs to see what's there. I'm going to expand the last command that we have sent out. I'm going to go all the way down. And what you're going to see here is the last command. This is what's interesting for us. So we're going to see here the Netlify build command telling us all the information. But this time it's saying, hey, we're deploying to a draft URL. There are no errors or anything suspicious. And we're going to get here the website draft URL. So let's go ahead and click on it. And what we're going to see here is temporary address where we can see our application. This is where the application has been deployed. And it's different from the address where we have been accessing it so far. Essentially, if we're taking a look at the URL, we're going to see here that it contains this string in front of it. It's a random number. It is publicly accessible, but only we know it. So from that perspective, it's still a decent way of deploying this and testing the deployment before sending it out. Now. Still, if we're taking a look here at our pipeline, we will interrupt this pipeline if there is an issue, if there are any errors from the deploy staging. But if we for some reason deploy the wrong website, it's still going to be too late. And this is something that we're going to address in the upcoming lectures.
