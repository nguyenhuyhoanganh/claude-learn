# Post-deployment tests

You could think at this time that we have a fully automated pipeline where we're building the software, we're testing it, and then we're deploying it. But if we're thinking about the deploy stage. Actually, after deployment we went to the website and manually checked if something appears here. So in this lecture we would like to replace this manual step of opening the website and checking it with an automated test. And we already have almost everything in place. If we're taking a look at the pipeline, we're gonna find here the end to end test, which was actually executed against the website. But to get this to work locally, we have used here serve to start the local version of the website. So we can still reuse this test here, but this time against the production deployment. So let's go ahead and copy this entire stage. And after the deploy stage, I can paste it here and I select the once again and press shift tab two times to bring it back at the right indentation. I'm going to rename this prod e2e so that we know that these are end to end tests that are run against the production environment. Apart from this, these tests are publishing an HTML report. So we are taking a look at the configuration here. We're going to see here the report name. So if we run this test two times we're going to have two HTML reports. And it's important to be able to distinguish between the two of them. So we need a way to rename the report so we can right here playwright E to E. That's going to be the name of this report. And the name of the previous report, we can call it simply Playwright Local. Right. So we have everything in place here, or at least we have a large portion of it. So let's go ahead and go step by step through this configuration to make sure that everything that we have here is correct. So why do we have so the name of the stage is prod e to e. We're using here a Docker image which is bringing us playwright. Perfect. Then we go inside the steps. There are a few things that we no longer need. For example, we are no longer needing serve because this is deployed at Netlify, so it's already running there. We don't need to install anything locally. We don't need to start server locally on our Jenkins instance to be able to use it. So we can just remove the steps here. So all we have here is Npx playwright test. Now the question is. How does playwright know to use this local installation that we are starting here? Where is this configured? Actually this is configured inside this file here, which is called playwright dot config dot js. And somewhere here there is a local configuration. So what we'll see here is that this test have a base URL. And for this base URL there are two options. Either there is an environment variable which is called ci, underscore environment, underscore URL, or. If this variable is not available, we simply use localhost on port 3000, which is the default port where we are starting this application locally in Jenkins. So in order to use this exact same configuration and test against this production environment that we have here, which has this address for my installation, some random name here, that Netlify dot app. We need to create an environment variable. We can go ahead and copy this here. So CI underscore environment URL and we can go ahead and close this file. We already defined environment variables right here on top. So we have the Netlify site ID and we also have the Netlify auth token. So these are already environment variables. Variables defined in an environment directive used at a top level pipeline like these ones here, are available to all steps in the pipeline. This is why when we inside the deploy stage, these variables that we have defined on top are also available here. But if, for example, we define this variable psi environment URL equals and we put here the value from this address. This will be available in all stages, including this end to end test stage that we still have here. So instead of testing locally, we're going to test against the production environment. And this is something that we don't want. Now. Luckily, there is a way around this. I'm going to go ahead and copy this block as it is right now. Can I remove this line from here? And what we're actually going to do is we're going to create an environment directive in a stage. So inside our prod e2e stage, we can simply go ahead here and paste what I've copied and select again the entire block. Press two times tab key on the keyboard to have the proper indentation. Here. We don't need this here. And now we have defined the variable psi environment URL. We've given it this value and this is available inside prod e2e. It's one more thing that we need to quickly check. Currently the node modules are open, so this is why the list is so long. We go again inside the playwright directory. You're going to see here that localhost does not have a forward slash at the end. I want to make sure that we are also removing this forward slash here at the end, just to keep things consistent. So I'm going to go ahead and commit these changes and let the pipeline run. After the execution. We can see here that in the pipeline we have now an additional stage, the prod e2e stage. And if we refresh the page, we should also see here two different reports. I'm going to see here playwright local and playwright E2E so different runs different executions is when I see that this is the report for the production server. Again, the production server is the one that is running here at this address. Something random that Netlify dot app. Now the question is, are we now off the hook? Is this everything that we need to do? How do we know that these tests were actually executed against the production environment, and not against something locally, or who knows where? And my recommendation with tests is always to test the test. Because if we have a pipeline where everything is green, everything is successful, and nothing ever fails, even if it should fail, then we have just wasted our time configuring anything. So whenever we're doing something, we should also try, especially in terms of tests to make the tests fail to simulate a failure. So here's what I suggest. If you go back to Netlify and click on deploys, we're going to see here all the versions that we have deployed. If we scroll here further down, we're gonna see here this upload functionality. And as a resource to this folder you'll find the zip file. And if you unzip that file you're gonna find there a build folder. So I'm going to click here on Browse and Upload. This is the build folder. I'm going to select it and click on upload and confirm it in the browser. I'm just going to take a few seconds. And now the deployment has been successful, I'm going to open again. The production version and you're probably familiar with this website. So this is not our application. So now in this case I'm just simulating that for some reason there's a different version that has been deployed. There's a different application. I'm just trying to see if the test that we have will detect this issue. So what we can do here is we can click on the latest build. And we have this option here restart from stage. So I'm going to click on this. And we can select the stage name. Stage name will be prod e2e. Let's click on run. And what we're going to see this time is that all the previous stages have been skipped, and we are only running the prod e to E stage. This is one way how we can quickly recheck if something is working without going to the pipeline, without commenting out stages and so on. So we're keeping the configuration intact. The only thing that is changing is essentially this verification. And now, after a few seconds, we're going to see here that this stage is failing. And we can check the logs if we wish. But typically there's not so much information inside here, so it's a bit harder to read. But remember we have the HTML report so we can check it again. And what we're going to see here. There are different tests we have here. A test is checking if the title is Learn Jenkins. Well it's not this website doesn't have a title. It only shows this text. And so on. So obviously if we replace the diploid version and we're running this test once again they will fail. We can just as well restart the entire pipeline. But this has been just a quick way on how we can test the tests and make sure that if for some reason we're deploying something unexpected, we have the test in place, they are working, and we're going to get the information that something is wrong. So now we have post deployment tests that replace the need for us to go open the production website and check everything because we have the playwright tests.
