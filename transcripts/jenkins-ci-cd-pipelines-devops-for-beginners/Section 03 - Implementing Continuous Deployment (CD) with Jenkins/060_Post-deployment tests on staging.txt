# Post-deployment tests on staging

Now we have all the knowledge required to finally run the end to end tests on the staging environment. So we have here deploy staging and right before the approval step, this is where we want to add the end to end test for staging. And we're going to scroll all the way down here where we can find the production end to end test. And we're going to use them as an inspiration. So I'm going to go ahead and select this. Scroll a bit up until we find here this approval stage. And I'm going to go ahead and paste this configuration because it's very, very similar. I'm going to change here the name from prod e to E to staging. And apart from this we want to change the name of the report. So this is going to be playwright E2E. I think it should be staging E2E and also the production one. Let's make sure that we also have the right name there. So we're going to call this prod e2e. All right. So we have the name of the report properly. We don't know anything about the URL at this point. So we're just gonna put here a fix me. But apart from this, the staging E2E is almost done. But there's still a bit of work that we need to do here. Inside deploy staging. So what we want to do is pass a variable from this stage to this stage. And the information that we want to pass is already outputted by this command. So just a short recap. We have used JQ to parse the information provided in this file. Deploy output type JSON which contains the URL and this information is provided by Netlify deploy. After providing the JSON flag. So this is what we need to do. How can we do this? In order to set this variable, we're going to open up here a script block. And inside the script block we're going to set a variable. So the name of the variable is going to be staging URL. So that's the name of the variable. And what we're going to have here is we're going to run a shell script. So currently we're not sure which script this will be. But whatever script it will be we're gonna run it. We're going to store the value in staging URL. And in order to get this also to run properly, we need to specify here the parameter return stdout to true. This is the script that we want to run. So the only thing that is missing here is the parsing of this. So we don't really care about the rest of the commands that we're executing. But we do care about parsing. So I'm going to copy this script and paste it here. Now I want you to pay close attention to this. We have started here the script with a single quote. We are ending the script here with a single quote. But this command also uses single quotes here to specify the deploy URLs, or to specify which property inside the file to look for. If we keep this as it is, it's going to be an invalid syntax because we cannot have multiple quotes, because whenever we have a quote, it means that we are opening something here and closing it here. So this is not going to be a good idea. So what we can do is at the beginning and at the end, change the single quote into a double quote. You're going to see here that this entire color here it's going to stay the same. So whenever you're noticing that the color changes in an ID for a string that you want to pass, because this entire command is like a string, which we're going to be executed. So we need to keep this intact. And we don't want to mess with the command. We're going to keep the command as it is. So this is why we're switching here to quotes. So just be mindful about these quotes. In this context. We can easily use single or double quotes because we have no variable that needs to be resolved. All right. So that's. Practically it. Here we're setting this variable. And. Later on in the staging. E2E. What we need to do here is to read this variable. So all we have to do is write again our syntax with dollar sign brace. And we're going to paste here staging URL. But we also need to paste that env in the beginning. If we keep this between single quotes, this is going to be considered a string. Let's see what is happening if we replace this with double quotes. I'm going to put here a double quote here and a double quote here. You notice how syntax highlighting changes. This is no longer a string. This is something that's going to be replaced when the pipeline is executed. So in a nutshell what will happen is this stage is going to set this variable. And then when this stage is starting it's going to set another environment variable CI environment URL. This is what playwright is looking for. Playwright has absolutely no knowledge of our staging URL. And this syntax here will lead to Jenkins taking this value from staging URL and setting it inside CI environment URL. Let's give this pipeline a run to see how it works and if it works. And look at me. Silly mistake. Let's take a look at the logs to see what is going on. So it says here. Unknown stage section. Script. Steps in a stage must be in a steps block. Let's take a look at what we did here in the pipeline. Inside deployed staging. We have put the script outside of the steps. It can happen. Okay, so let's go ahead and remove it from here and put it right below the safe block. And also make sure that it is properly indented. So now this time Jenkins was a bit more specific about the error. So it wasn't so critical. Let's see if it works with this change now. So taking a brief look at the pipeline we're going to see here the deploy staging and staging E2E which has been successful. So these tests have been executed here. We only seen here the report, and we can refresh this page so that we can get the report as well. Let's see where it is. For some reason it's not showing up. But let's continue here with approval. So yes, we want to deploy to production. And we're going to let the entire pipeline run. And maybe at the end that report will show up. Otherwise, we need to take a second look at the configuration to see if we did anything wrong. So after the pipeline has successfully executed also the deployment to production and the production end to end tests, we see here the local execution, staging and prod. We probably should also rename this to local E2E to have a consistency. But we can see here the staging and the prod e2e test. So apparently we have successfully added the stage and we have dynamically passed information from one stage to the other. Now, to accomplish this, in all honesty, we did some crazy tricks here with Linux, so there's no denying that this is anything but trivial. So in conclusion, to be able to come up with such solutions on your own, you need to learn how to use Linux commands. I've included in the resources folder a quick Introduction to Linux for DevOps. Linux is just one of the timeless skills you need to dedicate time to learn, as it will pay off on the long run. Regardless of which tools are you using, Linux is gonna stay there. So if you have a bit of time and wish to practice more, I suggest doing a short detour.
