# Combining pipeline stages

As you can probably see, our pipeline is getting a bit big. Or you could say also it's getting a bit long with all these stages here, and even using this other view provided by the Jenkins Blue Ocean plugin doesn't show much shorter pipeline. So how can we make some improvements to it? For example, here we have deploy staging and staging end to end. And if we're thinking about this, this two stages are related because we are deploying. And immediately after the deployment we are running the test. But just as well we could consider merging these two stages into one redeploying. And if something is wrong in the deploying stage, the stage will fail. But then if the end to end tests are failing, we can also consider the deployment as a whole failed. Right? So if there's something wrong with the test, then it doesn't really matter that deploy staging was successful. So there's a logical reason for wanting to merge these stages together. And the same goes also for deploying to production. So we're deploying to production. And then we're running the end to end test to see if the deployment to production has been successful. Now, because there is a dependency between these stages, we cannot run them in parallel. So what we're going to do in this lecture, we're going to take a look and explore the possibility of merging them together. Let's take a look at our Jenkins file, and we're going to scroll all the way down to the bottom where we have the production end to end test and deploy production. What we could do is we could take the commands from one of these jobs and just place them in a logical order after the existing commands. This stats only have one command, but the key difference between these two stages is that we are using different Docker images. So in deploying to production, we're using a generic NodeJS image that has node version 18. And here we are using a Docker image that has playwright. Now, in this particular situation, playwright is actually also using a NodeJS environment. So if you don't believe me, all you have to do is go inside this stage here and write something like node dash, dash version. And you're going to see here that there is a node version installed. It may or may not be node 18. But for the kind of tools that we're using here. So for example, we're installing Netlify CLI. This really doesn't matter so much. So if this image already has NodeJS, why not try using this image which has playwright and then we can install netlify on this image. It is worth a try at least. So all we have to do here is copy the commands that we already have, and we're going to place them right below here node dash dash version. Because if we don't have node, at least we're going to know in the beginning that there is no node available. And we're going to see what else we have. So we don't care about the stalker part. We already copied all the commands and there's nothing else here. So we could theoretically copy also the name. And completely rename the stage. And because we have already removed everything from here, we can remove the stage as well. So in a logical part, we are still doing the deployment. And after the deployment and rerunning the playwright tests. And finally, we are publishing the report. Now, the only dependency that we may have here is that maybe the deployment is started and is done. But this command sometimes could need a few seconds. So for example, we are telling Netlify Netlify deploy this, but the Netlify still needs a few seconds, so Netlify may say back, yeah, sure, I'm deploying this here is some information, but maybe still needs a bit of time. So in this situation, it may be interesting to also add a sleep here so that we wait a few seconds after the deployment. I'm not going to add it here, but this is just something to keep in mind because just because we're getting a response from Netlify doesn't mean that the deployment is all done and ready to go. All right. So let's give it a try to see at least if from this Docker perspective we have figured everything right. So I'm going to commit this run the pipeline and take a look at the results. And what do you know surprise surprise deploy Prod has been successful. And we can also take a look here inside the logs to see what has happened. So we are here. We can see our node version. So this particular playwright Docker image is using node version 18. So it works perfectly. We have seen this here. That's already good. And then we're installing everything. And all the rest of the commands are working perfectly. And as you can see here, the prod e2e doesn't exist anymore. Probably it takes few runs to get this also to go away. We can also open the blue ocean view. And inside the blue ocean. We're going to see how our pipeline looks like. So if we compare it to what we see still here for staging. Staging we're deploying. And then rerunning the tests. Now deploy Prod has everything in one stage. Now. And apart from this, we have also saved a bit of resources because we are no longer starting to different stages, no longer starting and terminating two different Docker images, we only have one. So that's also an added benefit. So because we have been successful, let's take a look at how we can transform the staging part as well. So we want to merge deploy staging with staging end to end. So inside the Jenkins file, let's take a look where this is. So we have here staging end to end. But here things are slightly more different. So we can still rely on this Docker image playwright. And we can run most of the commands. But we still have some tricky parts. We still have this script here. And we still have this environment declaration here. So these are a few changes that we need to make. But let's focus on what we know. And I'm going to use here cut so that we can keep track on what we did and what is still left to be done. So let's remove the first commands that we have. I'm going to move them here right on top. The reinstalling Netlify cli. We're installing node jq so this is all needed. Here we are creating this deploy output file. But in this case we no longer need to pass information from one stage to the other. So what we could do is to simply run the command that we have. This is the command. I'm going to copy it again and bring it back to the script here. So pretty much we are done with this. We're gonna copy also the name. And apart from this, there's nothing inside that is still of interest. Let's go ahead and rename the stage altogether. And what we can do here, because we are in the same script, we can define our own environment variables. So we're going to define the environment variable CI environment URL. And super important here do not add any spaces. So it's the name of the variable equals. And then what we will do here is we're going to have the dollar sign and between parentheses this is where we can put a command. In the command that we're going to add is exactly this one. So what this does is pretty similar to what a script did earlier. When we use SSH. This will lead the command to be executed and it's going to be stored here inside the CR environment URL variable. So let me briefly show you how this is supposed to work. And I'll bring back a terminal here for just one second. So let's imagine that we want to define here a variable. I'm gonna call it my var without any space equal dollar sign parentheses. And we're going to put here the command date. So we're going to execute this. We're not going to see anything. But if we use the echo command. To print Myvar. What we're going to see here is the date. So the principle is absolutely the same. The main difference here is that this variable is only going to be available in this script here. So only this script has access to this variable. To be more specific because it's defined here, the only script that is going to have access to it is here when we're running the playwright tests. So this is the theory on how this is supposed to work. Let's go ahead and remove this environment to see if this is gonna work as well. We're going to see if we're getting some errors. We may still need to fix this a bit. I'm going to commit the changes and let the pipeline run. So now if we're going back to the pipeline, we're going to see that unfortunately deploy staging has failed. So something has happened here. To understand what has happened, we're going to click here on the logs. And we're going to explore what went wrong. And the fastest way is to simply scroll all the way down. We're going to see here that by the way it looks here, we can see that there is a problem. We're going to see here. Connection refused at localhost port 3000. So this is the error that we're getting. So this playwright test and the question that we have is like why is it looking at localhost. Because we don't have anything running on localhost. Go back to our project and take a look at the files. Probably remember playwright dot config dot js. And right here on line 31 we have the base URL. And this is checking for the following information. So if this environment variable is set it's going to use it. Otherwise it's going to use localhost on port 3000. Well it appears that this environment variable has not been set. So despite the fact that we have set it here in our script, it's not being detected by playwright when this execution is happening. So what we're going to try next is to still keep the environment definition. And we're going to use it here. But we're going to set the environment URL to something like undefined or staging URL to be set. Something that we know. If we see this particular value, we know from where this is coming. So let's give it another try and see. Now, if we define this variable at the beginning of the execution when we're running this particular script, if we can then set this variable properly without using a script block. And now going back to the pipeline execution. The second time. Now the deployment is successful and the tests are running properly. So we don't have any issues here. And yeah, I think we have managed to successfully merge the deployment with the testing, have a faster pipeline and at the same time still do all the testing necessary.
