# Passing dynamic data between stages

Sometimes we need to pass dynamic data from one stage to the other. Like in our case, we used to pass the URL of the staging environment. So this is done here in deploy staging. And we want to pass that information to the end to end stage which currently doesn't exist. Dynamic data is anything that is unknown before the execution of the pipeline, so we cannot hardcode that information in a variable somewhere beforehand. So in this lecture we're going to learn how to set environment variables from our pipelines in a stage and how to read the same information in a different stage. Because we're learning a different concept, we're going to use our experimentation pipeline to learn more. So I'm going to go here to the learning pipeline. Click on configure. And here inside the pipeline configuration we're going to learn a few things. So first of all, let's remove this approval stage if you still have it. And we're going to simulate that this is the deploy job. And here we're going to simulate that this is the end to end. So we have some information here inside the deploy. And we want to pass it here to the end to end. So let me also fix the naming here. So how can we do this? The thing is, we cannot use here a shell script to set that variable and pass it over to a different stage. Instead, we're going to use a script block. So the script block will be, as the name implies, going to be here. Script. And here we can write some script. In Jenkins pipelines. The script block serves a specific purpose. It allows us to run a groovy script within the pipeline, which is particularly useful when we need to perform some more complex logic. It's kind of like a scripting language. So we can write some scripts, which typically our declarative pipeline doesn't allow us to do. In this case, we're going to write a script to manipulate the variables in a way that is not directly supported. So we're going to use a script to set a variable, and then we'll be able to use it in the end to end stage. And trust me, this is super, super simple. So the main difference here is we have this script block. If we don't have the script block, this is not going to work. Now here inside the script block, what we're going to do here is we're going to write here env because this is an environment variable. And let's say that we want to set a variable which is called my underscore var. So it's my variable. And we're going to set here a value. So let's say something like this is set in deploy. Super simple. Now here in the end to end stage what we need to do is to simply call it. So between double quotes we're going to use here simply echo as a step. We can write here my var. Is. And for debugging purposes we're gonna print here the value. So we want to access it. We're going to use here this special syntax. So it's going to be a dollar sign. And open a brace and close the brace. And inside here we're going to write N dot. And then my var. So the name of the variable. I'm going to save this configuration now and run the pipeline. Let's take a look at the logs to understand what has happened. So we see here we are inside the deploy stage. We are writing here. Deploying. There's nothing else that is happening. So this script here will have no output. And then inside the end to end stage, we're going to print the message end to end. And then the value of my var. It's going to be this one. So we have now successfully set a variable in a stage, and we have managed to access it in a different stage. All right. But this doesn't really apply to our use case because if we're taking a look at the configuration again we're going to see here that this value is really hard coded. So we already knew it in advance. Now let's use a Linux command that provides us a different value every time. And for that we're going to use a simple command which is called date. And I'm going to show you briefly how date works. So I have here a terminal open I'm going to write here date. And this will print the current date. And if I run this once again, we're going to see here that the second has changed. So it was second 29 right about here. And here is second 34. So this is something that is computed every time. It changes all the time. And we cannot hard code it. All right. So how can we execute a command and store the result of that command in a variable. Well pretty pretty easy. But for that we're going to use something a bit different. So here's what we need to do. We need to use here ssh. So ssh will execute a shell script. And we need to specify well what is the script. So we're going to write here script column. And between quotes. The command that we're going to run is called date. And apart from this we also need to specify that running the shell script to return this to the variable. And for that we're going to need to pass here. An additional parameter in the name of the parameter is return. STD out. So STD out simply means standard output, which in our case is anything that prints to the console is anything that we can see inside the console logs in Jenkins. Or when we're running a command, we run here the command date and we what we see here in the terminal. This is the standard output STD output. That's just a fancy name for whatever we see on the screen. So we're going to specify this with a value. True. So want to return whatever the script here is outputting to the terminal. But instead of seeing it in the terminal we're going to get it back here. And we're going to set it in a variable. All right. Sounds a bit more complicated than it is in reality. We have now this configuration. We haven't changed here. Nothing in the end to end stage. But this time this variable is going to be dynamic. We have here nothing hard coded. This is exciting. I'm going to run this pipeline. I'm going to take a look at the console logs. So the execution has been again successful. Let's click on it. And what we're going to see here is. This is a dynamic value. This is something that has been computed in a stage and it has been passed to a different stage. Now, if you understood this concept, this is super powerful because it allows you to apply the same concept to passing any dynamic data from one stage to the other.
