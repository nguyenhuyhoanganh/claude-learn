# Assignment: Add a manual approval step - Solution

So let's go ahead and create the approval stage. And as the assignment requires it, the approval stage needs to be between deploy staging and deploy production essentially one half the stage before we deploy to production. So let's go ahead and right here stage the name will be approval. And let's start the steps block. And here inside the steps, what we need to do is to add that input message. So we're going to use this snippet generator. I'm going to select here I'm going to search in the list for input. And our input message will be danger. Approving this will result in a deployment to production. So the question will be. Do you wish to deploy to production? And then in order to add the customer key message, we need to expand here the advanced. And we can write our own. Okay. Button caption. And then we can go ahead and generate the pipeline script. So this is the code that we need. Go back to the Jenkins file. Insert it. I'm going to commit these changes, wait for the pipeline to run. And we're going to take a look when we arrive at this approval stage. And now taking a look at the pipeline, we're going to see that we are done with the deploy staging. And now we are waiting for approval. Let's see what this message is saying. Do you wish to deploy to production? Yes, I am sure. So we can select this and then continue with the rest of the pipeline. And while this is still executed, let's jump back here to the Jenkins file. And actually add a timeout of 15 minutes. So in order to do that, all we have to do is go here to the pipeline. Syntax. And from the available steps I'm going to select here. Timeout. And this is going to be a timeout of 15 minutes. You generate the pipeline script. You don't like the fact that this minutes don't actually show up. So what we're going to do is we're going to select a different unit. We're going to select your hours. I'm going to copy this code block. Go back to the Jenkins file. And here inside the Jenkins file and actually inside the stage approval within the steps I'm going to paste here the timeout. And we tab going to make sure that this has the right indentation. So it needs to be within the steps. Notice here the steps are starting here ending here. The timeout is starting here and ending here. So if you want to have this input message timeout after 15 minutes then we need to copy it from here essentially from the outside put it inside the timeout block. And we still need to replace here the unit from hours to minutes. So let's go ahead and commit these changes. Run the pipeline and we should be done with this assignment.
