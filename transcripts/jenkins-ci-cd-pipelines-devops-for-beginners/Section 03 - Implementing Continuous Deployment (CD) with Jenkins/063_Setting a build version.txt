# Setting a build version

In this lecture we're going to remove the manual approval step, but also improve the way we run our tests against the different environment. So let me better explain what I mean by that. In order to remove the approval step, we really need to be sure that the version that we have built is also the version that has been deployed. Previously we could have disabled, for example, the build stage, but we could still have deployed because there was something in the workspace to ensure that there's absolutely no chance of deploying the wrong version. What we're going to add is a specific application version based on the build. But let's start with the beginning. This is the approval step here and we're going to get rid of it. So bye bye approval step. Now we want to do continuous deployment. But to improve our pipeline and to be really really sure we're going to add here an application version. Going here to the source code in src we have our app.js. This is pretty much what we see when our application is running. And you probably already seen this. We are writing here application version one and this is currently hard coded. What we actually want to do is add a dynamic application versions. We want to change this application version and through our tests, we also want to check that this version has been updated so that we have the right version. Don't worry if this sounds a bit weird right now, it's going to make sense in a second once we get started. What are we going to do in our pipeline? Here is define a new variable. This variable needs to have a very specific name because some of the code is already checking for this variable. So the name of the variable needs to be react underscore app. Underscore version. Let's go ahead and provide a hard coded value. I'm just going to write here 1.2.3 so that we cannot confuse this with anything else. So we have defined this variable here. And this will be available during the build environment. So so when we are building here our application this variable is available here. And through a bit of magic in our application we can simply write here instead of a hard coded version of one we can have a brace opening and closing. We can write here process dot, env and then dot. We need to specify the name of the variable. So I'm going to copy it from here. So mistakes are less likely. This is a bit of magic. But in a nutshell during the build time this value here is going to be replaced. Apart from this, there's much more happening in the background. We use here the search function and search for react app version. You're going to see where this is used. And one important place where this is used is in the tests. Let's click here. Inside app dot spec dot js. We have here a test which is checking for the application version. Now, by default, this variable is not defined. It's just gonna default to one. It's going to check inside the page if this is the right version. So that's super important. And this is the check that we actually want to do. So we're going to go first here with this hard coded value just to make sure that everything is working as expected. I'm going to build the pipeline and we're going to take a look at the deployed version. So the execution of the pipeline has been successful. We no longer have the manual approval step, but let's also take a look at our production environment. So if you no longer know the URL, let's simply click on the last job and we can expand here. The shell script go all the way down. And we're going to see here the website URL. And what you will see here. Right at the bottom is now the application version. So the variable that we have defined inside the pipeline is now visible here. So if we look in the Jenkins file we're going to see here 123. And this is exactly what you see now on the website. Having a hard coded value is not really that useful because it's the same value. What we actually want is to have an incremental version so that the version of our application changes with every build. And luckily, Jenkins is there to help us with some predefined variables. So as a resource to this lecture you're going to find this page. And we're going to see here some environment variables that we can use. But one of the environment variables that is important here is build underscore ID. And build underscore ID contains the value of the current Jenkins build. So just to give an example going back here to the pipeline. We are here at build number 49. So this is the build ID. So what we can do in our pipeline is we can have, for example, version 1.0. and essentially the last place of the version, the minor version. If you want to follow semantic versioning we can add there the build ID which will be different with every build. And this will help us identify which is the running version on different environments. So how do we do this? Well we have the variable build ID. So this is the name I'm going to copy it. And with the dollar sign. I'm going to paste it here. I want you to pay close attention to this. We have a single quote in the beginning and a single quote at the end. If we try to use variables, those variables are not going to be replaced. You can also notice it here with the syntax. It's not colored. There's no difference in color between 1.0 and then the dollar sign with the variable ID. So this should be a hint that this is not going to be replaced. So we want to replace the single quote with a double quote. Let's go here at the end as well. And now you can see build ID has a different color. So this is going to be replaced. So let's run this pipeline once again with the next run build ID should be 50. So we should have version 1.0. 50. So a few minutes later, we have another successful pipeline execution. We're going to take a look at the logs here and grab again the URL. So here is the website URL. Let's open it up in a new tab. And what we're going to see here application version 1.0. 50. And you may have a different number depending on how many times you have executed the pipeline. But with every execution this will go up. So we're building a new version of the application. And then that version is going to be built into the application. So that when we have this application running on different environments, we can see in this environment we have this application version. And on that environment we have a different application version. And we can easily identify this. And more importantly, when we're deploying something we can check against the different environments if that version is there on the environment. Because if something happens with the deployment and we have for some reason a different application version and we cannot find this version there, the test should fail, right? Well, how do we know if they will fail? Well, it is best with tests to test the tests. So what we're going to do next is we're going to use an interesting feature of Jenkins, which you already know. We're going to click here on the build. And we're going to click here on restart from stage. And we're going to select the stage name deploy prod. But it should work absolutely the same with deploy staging. Now this pipeline is going to start again, is gonna skip the build. So the build is not going to be executed. Whoa. Went away. So it's here. The build is here. Build is not going to be executed. So we're going to use the old build. We're going to use the build number 50. And we're going to deploy it on production. And now the test. The test will see that it is a different version. So this is the idea because the test expects to have version 51 because we are now in build 51, because in our Jenkins file, we say our current application version is one point 0.51 because of the environment variable. But then if we're looking on the deploy server, we're going to find version 1.0. 50 because we haven't executed the build once again. So now we expect the test to fail. And to mark this deployment overall as failed because there's this version mismatch. So what do you see? Deploy. Prod has failed. We can click here on the logs to investigate why something has failed. And we're going to see here right at the end there are different retries. But the last command that we have executed are the playwright tests. We can also take a look at the prod end to end because this is where the HTML report is. And what do you know which test has failed? The test, which has expected app version, we can click on it. It's going to be a bit more technical why it has failed. And we also have here the standard output. So we have expected that it should be 1.0. 51. But if we're taking a look here at the production environment I'm going to click here on refresh. You're going to see here application version is the older one. And now this test is failing the pipeline and is letting us know that something went wrong. So now we have also tested the test. And we know that if for some reason we manage to screw up and deploy the wrong application version and we will be alerted.
