# Parsing response data

Our next goal is to remove the remaining bit of manual work that we still have. So what we previously did after deploying to staging, we still did a bit of manual testing here. We went to the staging environment. We checked if the website looks all right, and after that we clicked on that approval bottles that we can deploy to production. But there's still manual work and we want to fully automate the pipeline. So what stands in the way to add here a stage where we're running end to end tests on the staging environment. Well, the thing is, if we're taking a look here at the deployment logs, we're going to see here that Netlify is creating a random address every time we deploy it. So let's go all the way down here. We're going to see here that the website draft URL is this one. And this part here is something random is something that we don't know in advance. So the URL is randomly assigned by Netlify and we cannot hardcode any configuration in the pipeline. So what we need to do in the upcoming lectures is to extract this staging URL that we see here in the logs, add another end to end stage, and pass this URL to the end to end stage so that they can be executed against this URL. Right. So what's the problem? Why can we do it? Is it complicated? Well, the problem is, as it stands right now, we're getting here a bunch of logs from Netlify. A lot of information, and this isn't an information that is easily understandable by a machine. We can easily read it and understand where this URL is, but it's kind of difficult to take this information and pass it to another command to one running the end to end test using playwright. In this situation. What we actually need is to get a response in a structured format like JSON. If you don't know JSON. JSON is a simple text format for storing and sharing data between applications. It is both human and machine readable. So let me show you how it looks like for this use case. Let me go ahead and close this. And what we need to do is to change the deploy command a bit and pass an additional flag. And we're going to try a different approach this time because we're trying to experiment and we're trying to save time and not run this long pipeline again from the beginning. So what we need to do is to click on the latest build, and it needs to be a successful build, even if in this case it has been aborted. And then we're going to click here on replay. And what you will see here is the entire pipeline. Now, there are a few stages here that really want to throw away, so we're not going to run the build. We're not going to run all these tests here that we have. I want to remove those as well. We have deployed staging. And that is already everything that we need. So we are interested inside deploy staging to get a response from Netlify deploy in the JSON format. So what we will do here and let me zoom in a bit. What we will do here is pass the JSON flag. So it's going to be dash dash JSON. Let's click on the run button. So the pipeline will start and it will skip all the previous stages. It will only run the configuration that we have saved here. And this is without doing any changes in the Jenkins file. This is without committing any changes. It's a super useful approach because it allows us to easily try things out without going through the normal process of going to the Jenkins file, making changes, committing them, running the entire pipeline. Then if there are some issues, going back, committing again, waiting for five more minutes. I'm gonna let this run, and we're going to take a look at the end, at the results. Little deployment on staging is done. Let's go ahead and click on the logs band here, the script all the way down. And what you will see here as an output from the Netlify deploy command is this here. And what you see here is JSON, JSON format for representing data. And it is using a key value format. So you will see here on the left hand side for example site name is a key. And on the right hand side you will see this string being the value. And the same goes for deploy ID, deploy URL and so on. So this format is readable by us humans as well. But for computers this is amazing because it can be parsed. It can be understood. So the next step would be to use a tool to parse this information. But before we can parse it, the first step would be to actually temporarily save it to a file so that we can pick up that file with another command and read it. Before we do that, let's open up here our online IDE. And right here on the top you can open up terminal window. I'm going to click on this one. And we're going to get here the terminal. And let's do a bit of recap in terms of how Linux works in general. So we can have a command like echo. And we're printing here a message. So if you're running echo. Hello. What we're going to get back here is. Hello. And this is the output of the command. It is called the standard output. The regular output. It's the output that we see in the logs. And we can take this output and save it to a file. So we can still use echo hello. And then using the redirection operator which is simply the greater than sign, we can take the output from a command and save it to a file. So we can name here a file. Hello dot txt we can run this again and we're going to see we have no output anymore. We don't see hello anymore. But if you're looking here in our files we're going to see here Hello.txt the new file. And it has exactly the message that we seen previously. Do not confuse the redirect operator with the append operator, which are two greater than signs. If you run this again, this will append information to the file. So we're going to see here we have two. Hello. The redirect operator A single grade sign will overwrite the file if it already exists. Just keep that in mind. Okay so enough with the basics. What we're going to do here in the pipeline is actually take the output from here and save it to a file that's super easy. Will it go ahead and abort this execution? We're going to click on the latest build. Click on replay and what you will see here is our latest configuration, which doesn't contain all the other stages. And I'll zoom in a bit. And now here with a redirect operator we're going to add the redirect operator right after the command. So this is the entire command with all the flags that we have. And we're going to redirect this output which we currently see in the logs to a file which is called deploy dot output. Bring on a comet exchanges. And the pipeline would be immediately executed. And. I'm going to jump directly to the part where the results are visible. Going back to the pipeline. Let's go ahead and abort this execution. And we can take a quick look at the logs to see what has happened there. And we're going to see here that this command now generates no output. And this is because it has been taken and redirected to a local file which is now present in the workspace. So all we need to do is to click on the build number, go to workspaces. Select the right workspace and what you will see here is the deploy output file. Go ahead and click on it. And I have here an additional Jenkins plugin that can properly read this. But it typically looks something like this. But it can look a bit prettier. So we're going to see here that we now have this file. All right. Perfect. But how does this help us? Why are we creating this? And where are we going with this? Well, essentially, it's not so easy to actually get to this address here and read it. We have asked Netlify to give us this output in JSON. We have now saved this JSON to a file, and now we need an additional tool to read the JSON file and extract the information that we need. So this new tool that we're going to use is called JQ. And it is a tool that can parse JSON. Essentially parsing JSON means that we're taking a JSON string like this one. And through jq we are accessing, for example, a specific property. So this is why we need GQ. Otherwise we cannot read this information. We cannot access the deploy URL if we don't use a tool like GQ. Now we can install GQ directly, but because we have a project that uses NodeJS, we're going to use a wrapper for GQ because it's a bit easier to install GQ in that way. So we're going to see here we already have the command npm install node gq. I'm going to go ahead and copy this. Go back to our project. So this is my build. Click on replay. And we're going to do here a few things. So first of all we already have npm install netlify cli. I'm going to paste here npm install node jq. If we want to install multiple tools, we can run the command npm install one after the other. It's not so efficient. So what we can do here is put a space and add node GQ here. So this would be the first thing we need to install node GQ. The second step is reading this file with node GQ and extracting that respective property from the response. So what we're going to do here is we're going to keep this path that we already have from Netlify. So no, Jake is still going to be installed in node modules. That bin and the path will be node GQ. And then we're going to pass here the flag dash R. And between quotes we're going to be searching for a property which is called deploy. Earl the name of the property that we have. And we need to add here a dot in front. And now the next part is specifying where to look for this property. And we're going to have to look here in this file. So with the first command we are curating this file. And then with JQ we are reading this file and we're looking for the deploy URL property. We are all set when I run this pipeline. And we're going to take a look to see if we now can access this URL. Let's go ahead and open the staging logs and take a look at what has happened last and what you will see here right at the end. Maybe it doesn't look so spectacular, but we have used here JQ. To look for this property inside this file. And this is the output. A lot of work for apparently something rather insignificant. But it's actually major, because using this approach allows us to access dynamic information that we can then use throughout the pipeline. So the pipeline is working as it stands right now, and we should go ahead and integrate these changes in our main pipeline. So again let's go ahead and simply click on this build. We can click on replay. We have all the changes here. And the changes are rather small. So it's going to be relatively easy to get this into deploy staging. So we have changed the first line. Where we installing GQ? Let's open up here. The pipeline file. So where? Here. Inside. Deploy. Staging. Let's replace this line. And going back to the config. What else? We have changed. We have changed the Netlify deploy commands, so we're going to replace that as well. And below it we're going to parse this information. So that's enough for now. I'm going to commit the changes by the entire pipeline run, and we're going to take it from there.
