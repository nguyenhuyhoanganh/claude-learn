# Environment variables recap (practice lab)

Welcome to this lab, which deals with environment variables in a CI CD setting. Now the first step is for you to determine if the environment variable version, which is spelled like this version, is defined. So this is the first task figure out is this environment variable version defined or not? Pause the video and try to determine this on your own. Okay, so probably one of the easiest way to determine if this particular variable is defined is by using echo. And what we can do is to put here a dollar sign in front of it. Otherwise it's not going to work properly. So if this variable is defined we're going to see its value appearing here. The fact that we are not getting anything back means that this variable has not been defined. And for that reason we cannot really use it. Or it may appear as empty wherever we're trying to use it. Another way to determine if this environment variable is defined is by using the env command. So the env command is going to list all the environment variables that are available, and we may draw some inspiration from here. In this particular setup here, there are not a lot of environment variables being defined, but inside a real CI CD pipeline, we're going to see a list of very long list of environment variables that have been defined. And we could use those environment variables in our scripts. We're going to see exactly their current value and so on and so forth. So these are at least two ways how you can determine if a specific environment variable has been defined or not. The next step is to define an environment variable and to display it inside a message. So I'm going to give you a bit of help. And we're going to do the first step together. So I'm going to use here the export command I'm going to write version. And I'm going to specify the version to be 123 with dots. So now this environment variable has been exported. If we use env we're going to see it also appearing here. And we're going to see it right here. And of course if we use echo this variable will also appear here. So this is how we define it. Now the task for you is to display this variable in an echo message and to combine it with text as well. So what you need to display as a message is the following building version. And then you need to reference the version environment variable. Pause the video and do it on your own. All right. So this is relatively simple. All we need to do is have the echo command I'm going to use here double quotes. And I'm going to write here building version. And we're going to use here dollar sign version. And what we're going to get back as a message is building version 123. Very important. If we are using single quotes this variable is not going to be replaced. And instead we're going to get the text as it is here. So that's an important thing to notice for this last task. I want to create a build directory. And inside a build directory I want you to put a file called info dot txt containing this exact text. Build version 123. Of course you want to use a variable for that. So this next task is relatively easy and can be actually accomplished in a single command. All we need to do is reuse the command that already worked. And we are noticing here the double quotes. And all we need to do is use the redirect operator. So what do we need here. Well we need to build directory which doesn't exist. And we need to put inside here a file which is called info dot txt. So this is going to fail because the build directory doesn't exist. But the rest of the steps can be done as you see them right now. I'm just going to demonstrate for a second. So we're going to see here no such file or directory. All we need to do is use the make directory command. Create a build directory. Rerun the previous command. And we can also use here cat to inspect the contents to make sure that everything has been created properly. So there you go. This is how we can do it. Essentially by using a single command.
