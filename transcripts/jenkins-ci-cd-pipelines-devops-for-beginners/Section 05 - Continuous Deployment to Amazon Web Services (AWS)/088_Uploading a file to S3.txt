# Uploading a file to S3

In this lecture we're going to upload a file to this S3 bucket. And as you can see inside there's nothing yet. And we're going to use the AWS CLI. So heading again to the aws cli command reference I'm going to locate here the S3 service. Let's search for S3 and let's take a look at available commands. All right. Here at the bottom there is a list of all available commands. And the command that we're going to use next is CP which stands for copy. And to understand how to use this command we're going to check here the documentation. So what this command does is it copies a local file or S3 object to another location locally or in S3, so it can be used in different directions depending on how we specify different parameters. So as you can see here in the command it says CP. So this is the command that we need to use. Then we have a local path. And the S3 path. It may sound a bit confusing, but if we scroll a bit further down we can also see here some examples. So here's a section with examples. And this is showing here in the example number one how we can copy a local file to S3. So we have AWS S3 copy. And this is the file name. So what we're going to do is we're simply going to copy this command as it is. And inside Jenkins we're going to start working on our pipeline. So within the AWS stage right after version we no longer need this command which is listing all the buckets. So we can remove that. And we're going to paste here the command AWS S3 copy. And here are a few things that we need to change. First of all we don't have a file name. So let's go ahead and create a new file. And we're going to create this file with echo. Let's put the message Lowe S3. So this is the message that we're going to put inside the file which is called index dot HTML. So we're just creating this file on the fly so that we have here file. It's important that we name this file as it is right now, because it's gonna matter a bit later on. So we're copying this local file, which is called index.html, to our bucket. And apart from this, what we also need to do here is to replace my bucket with our own bucket name. So inside AWS, I'm going to simply go ahead and copy the exact name of the bucket. And I'm going to replace here my bucket with this name. So what are we doing here? We are creating a file which is called index.html. And with the aws cli, we are copying this file and we are uploading it to our bucket. Cannot commit these changes. Let the pipeline run and we're going to take a look in a second to see what has happened. So the execution of the AWS stage has been successful. Let's take a quick look at the logs and to see what has happened here. And what we're going to see here is very simple. We are creating this file and then we're using AWS S3 copy to this particular path. This is the path to the bucket. Let's see here that this is a specific address here. And it says here. Upload with providing information about this upload. There are no errors. There's no indication that it didn't work. So all we have to do is to manually check this. So inside this bucket I'm gonna click here on refresh. And now we see we have one object. And this is the index.html file. And from within the management interface we can click on this and download the file. It has a unique address that we can copy. We can delete it and so on. So there are various actions. But in a nutshell this is how we can upload a file from Jenkins to AWS. Now there's still something that I don't like about this configuration. And the fact is I don't like to have here a hardcoded value because it's so easy to miss this. And the first step is always to replace this with a variable. Even if this variable is still defined within the state, makes it a bit easier to understand. So right before the agent here, we can go ahead and define an environment block. And here in the environment, I'm going to use the name AWS S3 bucket. It doesn't really matter how we name this environment variable because we're just going to reference it here. So it's not something that AWS cli is going to look for, but it's still important to have. I'm going to select this here. Copy. And paste it here. So this is the name of the bucket. And now super important, we're going to replace this here with the following and delete it. First add a dollar sign and now paste this value here. So we have here S3 column forward slash forward slash. That's the first part. Now we're using the environment variable. And then we have the name of the file. Now commit these changes and give it a try to see if it works. And just a few seconds later we can see that the state has been successful. Let's take a look at the logs to understand what has happened. And now we're going to see here that the environment variable has been replaced. So this is the full command. The file is uploaded and it works just as before. And you can see here the command that was originally executed. So here you can see the environment variable. But here you can see that it has been properly replaced. So as you can see, using the CLI to upload the file to the S3 bucket is actually easier than you think.
