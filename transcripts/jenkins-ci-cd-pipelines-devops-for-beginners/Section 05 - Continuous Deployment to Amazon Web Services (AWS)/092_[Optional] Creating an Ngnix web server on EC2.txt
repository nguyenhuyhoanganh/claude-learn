# [Optional] Creating an Ngnix web server on EC2

In this lecture, we will create a new virtual server and configure it as a web server using a popular software called Nginx. Our goal is to deploy our simple website application to the server and access it through a browser to see it in action. Although configuring a web server is not strictly required to follow along with the upcoming lectures, I believe it serves as an interesting mini project. However, please note that this setup is not intended for production use. So from the AWS Management Console, let's go ahead and search for EC2. And let's go ahead and launch a new instance. Let's give this a name, like my web server. And from the list of images, I'm going to make sure that Amazon Linux is selected. And I'm going to go here with Amazon Linux 2023. Let's scroll a bit further down. We have the instance type, which is t2.micro. And what is probably most important is that this is part of the free tier. So you have limited usage of this particular virtual machine for free. Let's move further down with this part. And this is relatively important to understand. It is the key pair login. So instead of using a password, we will use an SSH key to connect to this virtual server. Using an SSH key is much better than a password because it is much secure and generally more convenient. An SSH key works like a lock and key system. The server has the public key and you keep the private key. And unlike passwords, SSH keys are protected by very strong cryptography. So they cannot be easily guessed or stolen, which makes it much, much harder for hackers to crack. So it's kind of like an industry standard to use SSH keys for authentication to a virtual server. And we're going to see here that this is required. If we're taking a look here in the list, we're going to see that we have no keys. So I'm going to go ahead and create a new key pair. It is called a pair because it contains private key and a public key. I'm going to name this particular key GitLab so that we know why we have created it. And here are two encryption methods that are possible. Both of them work just as well. In terms of the format, we're going to use the PEM format, which works well with OpenSSH. This is pretty much what we're going to use as well. We're going to keep the defaults as they are. I'm going to create the key pair. We're going to be prompted to download this to our computer or the download will be automatically. And you should have your private key stored on your computer. And this virtual server will have the public key already configured. Alright, so after we have done this super important step, let's move further down to the network settings. And there are a few things that we want to make sure that we have here. First of all, we want to allow SSH traffic from anywhere. So this is the default setting. So we can connect to the server from anywhere. That's a very important setting. After this, we want to allow HTTPS traffic from the internet, but also HTTP traffic from the internet. So there are two more important settings because we're configuring a web server. It's important that this particular virtual machine has these ports open. Now there's still one thing that we want to make sure that it is correct. And this is the auto-assign public IP. You want to see here that this is currently enabled. Just in case this is disabled, simply click here on edit and change this to enable. That's an important setting, it will ensure that when we are launching this virtual machine that we have a public IP to actually access. And in terms of the rest of the settings, we already have what we need. So all we need to do is go ahead and launch this instance. This is going to take just a minute or two. And looking here at instances, you're going to see here that the instance is running, but it's currently still initializing. We're just going to wait for a few minutes. And from time to time, click on this refresh button. So what we should see after a minute or two is that the status check here is two from two checks passed. So this indicates that the machine is properly running. Now the next step is to install the web server called Nginx on this machine. But for that, we're going to need to run some commands directly on the server. Now, luckily, AWS is making our lives a bit easier, because we can do everything from the browser. All we need to do is select this particular instance, and then we're going to see here this connect button. And then we're going to connect directly to this instance that has here a public IP address. And this is the username that we will be using for this connection. In terms of the credentials, this is automatically handled by AWS. So connection has been established, and now we can start typing in some commands. So let's begin with the first command, which will install Nginx. And you don't have to type these commands from scratch. I'm going to attach as a resource to this lecture a txt file with all these commands so you can easily copy paste them. However, I just want to briefly explain what this command does. So the idea is to install Nginx. And for that, we're going to use this package manager, which is called dnf. dnf is a modern package manager for Linux distributions like Amazon Linux, Fedora, or CentOS. It is a successor of the older yum package manager. Anyway, with this package manager, we are running the install command, and we're telling it that we want to install Nginx. So here in the beginning is just ensuring that the command is executed with the super user root. And this is necessary for installing system packages. And last but not least, there's still this why here flag. And this flag ensures that all questions that are posed during the installation are going to be answered with yes. So we don't want to interrupt the installation process. And I hit enter takes only a few seconds. And Nginx has been properly installed. Now just because it has been installed, it doesn't mean that this actually running. So we need to execute another command, I'm going to clear the terminal with Ctrl L. And I'm going to put in the next command, which will actually start Nginx. This is a service that's being started. Now, just because it has been started, probably next time we're going to restart a server, this service is not going to work. So our web server is not going to work. It's not absolutely necessary to do this. But the following command will configure Nginx as a service and it will automatically start when the system boots. So now that we have done all the steps, the final command will just confirm that Nginx is properly running. And what we want to see here is this part active running. So this indicates that Nginx is now properly working. So because we have now configured a web server, let's see it in action. We're going to go back to the instances, click on the instance ID. And this will provide us with some additional information. And what we're going to have here is, first of all, the public IP address. So when we click on this, what we should see is our website. But my browser is trying by default to access this website using HTTPS. And HTTPS is a secure way to connect to the server using this secure protocol. This is currently not configured. So for that reason, the connection is failing. What I need to do in my case is to remove the S from HTTPS, hit enter once again. And what we should see here is welcome to Nginx. And the fact that this is not a secure connection is going to be indicated with this lock. But there's anyway, nothing really secret that we're transferring or publishing on this website. It's anyway a public website. So not using HTTPS is not a problem, it's just that the browser will complain about it. All right. Now there's also a second option that we can use. And this is by using this particular address, which has been generated by Amazon as well. So we can click on open address. Again, we're going to get the same error because we're using HTTPS. So I'm going to go ahead and remove the S, hit enter. And the website is also accessible. So we can either use this address here, which looks more or less like a regular web address, or we can use the IP directly. And this will connect to our server. And what we see here, welcome to Nginx, is just the default page. And it just indicates that Nginx has been installed and it's working as it should. Keep these two tabs open because we're going to need them in just a bit.
