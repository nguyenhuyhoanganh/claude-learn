# Managing AWS credentials in Jenkins

So let's see where in Jenkins can we store this AWS credentials? I'm going to go here inside Jenkins. And let's go to the dashboard. From the dashboard we're going to click manage Jenkins. Within security we have here credentials. We're going to click here on system Global credentials. And we already have here a credential for Netlify. So we're going to go ahead and click Add Credentials. The kind of credential that we're using this time is username with password. Even though AWS does not call this username and password for our use case, this is pretty much what we need. So what we're going to do here is go to Im and copy the access key. This is going to be the username I'm going to paste it here. In Jenkins. We can even treat the username as a secret, so that's definitely a plus. And then we're going to copy here the secret access key. Go back to Jenkins. And paste it here under password. Apart from this, we also need to specify here an id. I recommend using something like my AWS. It's totally up to you which option you choose. So let's go ahead and click on create. On. The second question that comes to mind is how can we provide now this credentials to AWS S3. In a similar way. How we do this with Netlify. We'll do this with AWS as well. So in the case of Netlify, we have inserted here an environment variable, and AWS works in a similar way, but in this case we also need a username and a password. As a resource to this lecture, you're gonna find this documentation page which talks about environment variables that can be used to configure AWS CLI. And in a nutshell, what we need to do is to specify two environment variables. We need to specify the environment variable, which is called AWS access key ID with underscores between words. And the second one is AWS Secret Access key. And they need to be called exactly as it is here. In our Jenkins file. Previously we have used the credentials helper and this works pretty good when we have, for example, a secret text. But when we have a username and password, we're going to try out a different approach. And I'm going to explain in a second what that is. So going back to Jenkins, let's go to our pipeline. And from the pipeline itself, you should see here this option in the menu pipeline syntax. And this is our snippet generator that we can use. And the step that we're going to add here is actually called width. And it should be with credentials. And as you can see here in the description with credentials helps us bind credentials to variables. I'm going to click on this and see what this is. And this is super useful because otherwise the syntax would be very difficult. So this generator that Jenkins offers us saves us a lot of time. So let's see here the bindings I'm going to click here on add. And what we want to use here is username and password but separate it. That's super important. When I click here on separated and we need to specify here the username variable. And this is something that we're going to copy directly from the AWS documentation. So it's going to be AWS access key ID. And paste it here. This is how the variable will be named. And we need the password variable as well, which we're going to copy from the documentation as well. And this will be AWS Secret Access key. The credentials here. You want to make sure that the right credentials are selected. So in this case, the only credentials that we have are this one. So you can check again that this is the access key ID. And it is. You're going to see here that in my case, it's the exact same value that we see here. So we know that we have mapped the right credentials. Let's scroll a bit further down and click on Generate Pipeline Script. So this is the script that is being generated. And whatever commands we wish to run, they need to be within this width credentials block. And this will map here the credential ID you're going to see here the credential ID is my hyphen AWS. And then we have the password variable and the username variable. I'm gonna go ahead and copy this as it is here. And inside the Jenkins file. This is where the magic happens. Pay attention. We're gonna be here inside steps. And I hit here. Enter one. So I have a bit of space and kind of go here right in the beginning. Paste this. And then with a few tabs on to properly indent it to make sure that it's on the same level as s-h. Now, if we want to use these credentials, these are going to be available only in this block. So the block starts here. And ends here. So if we use here AWS Dash Dash version, that will still work. If we use the next command here outside of the with credentials block, it's going to continue to fail. So what we need to do is to move this from outside of the with credentials block. Inside the block. I'm going to make sure here that the rotation stays pretty much the same. I just want to make sure that the code is properly indented. So within steps we now have with credentials. And then inside with credentials we have this SH block. And I commit these changes. And we're going to take a look at the logs and see what's going on there. And now a quick look at the AWS stage will reveal that this stage is now finally successful. Let's take a look at logs to make sure that everything is indeed as expected, and expand here. The shell script and what we're going to see here is the first command. We already know that. Now we see the second command, AWS, S3 ls which stands for essentially list. And this will list here a bucket that we have learned Jenkins. And here the date. So now we have managed to use this credentials within the pipeline. You can also take a look at the log logs. This will provide a bit more information to understand exactly what has happened here. So we've taken a look here in the full logs. We're going to see here the with credentials block starting. We're going to see here some information about the AWS Secret access key. Masking supported patterns simply means that if for some reason we try to print this information, this secret access key is not going to be printed. So you can even try using echo and printing this variable. You're only going to see here like stars like the ones here above. So it's going to be masked. This information is protected by Jenkins. This is why we're using this credentials. So it's only available during the execution. AWS cli gets this information, manages to log in, allows us to interact and manage our AWS account and services. And this is how it appears in the logs.
