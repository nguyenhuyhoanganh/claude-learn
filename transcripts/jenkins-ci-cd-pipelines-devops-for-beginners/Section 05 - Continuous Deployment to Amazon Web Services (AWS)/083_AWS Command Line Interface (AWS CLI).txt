# AWS Command Line Interface (AWS CLI)

So how can we interact with the AWS cloud and for example, upload some files to our newly created S3 bucket? Apart from using this UI that we see here, the answer is simple. We need to use a command line interface or CLI tool. This is how we can interact from Jenkins with the outside world and with services such as AWS. Jenkins cannot really open a browser. Log in and go through this interface and select which files to upload. And even if we try to do that, it will be a complicated setup and highly unreliable. So for the AWS cloud we need a CLI, but from where to get it. So let's go ahead and open any search box. And if we type in AWS cli and hit enter, what we're going to see here is the AWS command line interface. So this simple search has revealed the fact that in fact AWS does have a CLI tool that we can use. And probably here on the right hand side you're going to see that there are different installers for windows, macOS and Linux. That's great if you want to install the AWS command line interface on your own computer. But to install it on Jenkins, we're going to take a different approach. What we actually want to use is a Docker image. So for that we're gonna head to Docker Hub. So on Docker Hub. Let's go ahead and search for AWS cli. I'm going to select here the first result that you see Amazon forward slash AWS cli. And what we're going to see here is that this image comes from a verified publisher. This is what we want to see. And the publisher is Amazon Web Services. That's already great. As usual, when we want to use particular Docker image, it's always a good idea to just spend a few minutes and go through the information that's available here. It's not a lot to read, but it's still super useful. Let's head over to the tags and see what is going on here. And we're going to see here that there are different tags available. For example the latest version is two 1553. But this is also available by using the latest tag. You can even see here that the digest is the same. So these two tags are pointing to the same version at this point. All right, so what do we need to do? Well, we need to get an image so we could use directly this image without specifying a tag. Or we could specify a tag. Now we're going to begin here by not using a tag. So I'm going to just use the latest version. And you're going to see here that this is about 130MB in size. So not the smallest but also not the largest. And all we need here is the first part. So Amazon forward slash AWS cli. We can skip here the latest tag because by default Docker will use the latest tag. So let's jump into our Jenkins file configuration. And now for development purposes we're going to create a new stage. Just going to call this stage AWS so that we can play around with it just as well. We can put it in a separate pipeline just like the editor that we have here. So I'm going to use that. So let's create a new stage and call the stage AWS. And we're going to have here an agent. The agent will be darker because we want to use Docker. And the image that we only use, it's already copied here. And at this point there's no additional configuration. So let's go ahead and also create the steps. So inside the steps I'm just going to create. An SSH step that allows us to add multiple commands. And the first command that we're going to use is something that can help us print the version. But how do we know what to use and how to use this? Well, the thing is, we need to go back to the documentation to understand how to use this tool. I could show you exactly what is to be done, but I want you to understand the process of interacting with the documentation and not just blindly following along what I'm showing on the screen. I think the first part is much more valuable. So I'm going here to documentation. What we're going to find here is the aws CLI reference. And this is practically the manual. Now, when you're opening this for the first time, it can look super, super scary. But again, with a bit of patience and a bit of time, we can manage this. So in a nutshell, what we see here is pretty much how we're supposed to use this command. And the command line interface exposes this command, which is called AWS. Quite simple. And here is the syntax. And we can provide some options. Then we can provide a command that we want AWS to take a subcommand and so on. So it can look a bit complicated in the beginning. But the first thing that we need is AWS. And the second thing optional are the options. So let's see what options can we provide here. So we can see here that we have debug. Output many other things. Let's see if we find version. Oh, here it is. Version dash dash version. It's kind of like standard for all of these commands to implement dash version, but taking it from the documentation is always best. So going back to the Jenkins file, all we need to do is write AWS dash dash version. So the idea with this is that we want to print the version of the AWS CLI tool that we have. I'm gonna go ahead and commit these changes, run the pipeline, and we're going to take a look and see what has happened. A small spoiler alert. This is not going to work. So now I've let this stage run for a few minutes. And as I was saying earlier, the job is still running. And as a spoiler, it's going to continue running indefinitely. Let's take a look at the logs and see what exactly is going on. So we can see here that we have started this container at the same time we're getting here an error. And this is a bit masked. So there is an error. At the same time this stage is continuing to run. And we have here the command AWS dash dash version. And it's just doing something we're not sure exactly what it is doing. So let's take a minute to understand this earth and to see what exactly is happening here. So the error says as follows. The container started but didn't run the expected command. What expected command? We don't know exactly. Please double check your entry point. Does execute the command passed on docker run argument as required by official docker images? This is your link for entry point consistency requirements. Alternatively, you can force image entry point to be disabled by adding the option dash dash entry point. So what is this error or about and what's going on here. This particular Docker image has been configured in a way that when it is started, the AWS CLI is already available, and this is useful when you're trying to run this locally. So when you're starting your Docker image locally and you want to interact with the AWS CLI directly without writing any other commands, but for our use case this is causing an issue. So for that reason we need to go back here to this Jenkins job. And cancel it, because otherwise it's just gonna run forever. Again, going back here to the logs. The suggestion that we're gonna take from this is actually the one with overriding the entry point. So I'm going to go ahead and copy only this part where it says dash, dash entry point. And you're going to see here that there is no entry point. And in order to override this from the Jenkins file, we're going to pass an additional configuration when we're starting this Docker image. And this is done by using args. So args stands for arguments. We want to write here between single quotes or double quotes. In this case because we already have single quotes in this one. So we see here the single quotes. Sometimes we can get confused in so many quotes. And we have here the double quotes at the beginning and at the end possible. Also the other way around that you provide single quotes and double quotes here for this use case it doesn't really matter. So now by overriding the entrypoint, this image no longer has an entry point and then this command will be executed in the image. Pretty similar to how we have executed many other commands in Jenkins before. I'm going to commit these changes, and we're going to take a look together at the pipeline. The second time the execution of this stage has been successful. We can also check the logs and we're going to see here the AWS version. So this will be AWS CLI in version two point 15.53. This is pretty much also the latest version that is available at the time of this recording. So you can see it's the exact same version as here. My recommendation. When you are designing your pipeline and using Docker images is to use either a fixed version, or to use a command that prints the version so that when this Docker image gets updated and something doesn't work anymore, you know that a previous job worked with a specific version, and then the version has changed and that would be a possibility. Second option is to simply hardcode these tags in your Jenkins pipeline. So for example, if I want to use AWS cli, I can simply go ahead and click on this tag. I can take everything that says here and put it inside the pipeline here I'm going to replace this with a specific tag. And this will ensure that whenever we're running AWS CLI, it's going to stick to this particular tag. And for that reason using here AWS Dash dash version makes no sense. So we don't need that anymore because this is documented in the tag. So it's either one or the other. So I'm going to stay here with the latest tag. I don't need a specific version at this point, but keep in mind in general, as a best practice, it's not good to stay on the latest version because sometimes things get updated. For example, we are now using AWS cli in version two something, and maybe in one year from now we'll have aws cli version three and the commands will work a bit different. Keep that in mind.
