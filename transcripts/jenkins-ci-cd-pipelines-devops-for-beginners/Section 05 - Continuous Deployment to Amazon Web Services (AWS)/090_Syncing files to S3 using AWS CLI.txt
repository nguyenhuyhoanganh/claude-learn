# Syncing files to S3 using AWS CLI

In this lecture, we're going to use everything that we have learned to take the website that we're building and testing throughout the pipeline and deploy it to AWS. So essentially, what we're trying to do is to use this AWS state that we put here in the beginning, just for testing purposes, so that we don't have to wait for the entire pipeline, want to put it here. So we're going to merge it here with deploy prod. We're going to consider that AWS is going to be our production environment. We're retiring Netlify and we're going to do some changes here. So let's see how we can combine these stages together and upload the website to AWS. Now obviously there's still a few things that we need to figure out. And what we currently have here in the configuration is that we have a file. Essentially we're creating it and then we are uploading it. Now if you remember in the build stage we're creating here a build folder. And the build folder contains multiple files. So we cannot really go with AWS S3 copy file by file and upload those files. And also to ensure that there are no old files there in S3. We should also first empty the bucket. So delete any files that are there and then only then upload files. It sounds like a bit of work and it is. However, S3 offers a smarter way to do that and this is called sync. So the first step is to look at the aws cli to understand how to use this S3 sync. And inside here we are already inside the copy command. So let's go back to S3. And scroll down to all available commands we will find here. Sync. So think thinks directories and S3 prefixes recursively copies new and updated files from the source directory to the destination. It only creates a folder in the destination if they contain one or more files, so this is the more technical description. Let me put this in plain terms. Sync ensures that both locally and in the S3 bucket, there are the same files, and we don't need to know exactly which files are being uploaded. And we just saying sync this local build directory with the contents of the bucket. And when we're trying to use a command like this. If you're looking at this. I mean, look at how many parameters are here. This is crazy. And I'm going to be honest with you. Documentations for using the AWS CLI is super scary and can be very confusing. Don't get me wrong, I understand this, but I have to stress how important it is to have the patience and just spend a bit of time reading through the documentation. And I'm not talking about this particular use case with singer, but in general, regardless of the documentation and regardless of the tool you wish to use, this is how you master the CLI commands and get most of them by studying the documentation and going through it, and most of the time in the beginning, it really explains from a technical standpoint, how to use all the options and how to set all the flags. And in the beginning you don't really need this, so you need to scroll a bit further down. Until you can find a part with examples, because the examples are generally relatively simple. And in this case here you're going to see here an example on how to use sync. So for example here sync all local objects to a specified bucket. So we're using here AWS S3 sync dot means the current directory. And here is the name of the bucket. So it's not so complicated right. And here there are also other examples that you may need. We're going to start here with this example because this almost contains everything that we need. I'm going to copy this. And go here inside the Jenkins file. And I'm just going to write here below the copy command. So what we have here is AWS S3 sync. We're not going to use the local directory, but we actually need the build directory. So I'm going to right here build. And then we're syncing with our own bucket. And instead of using here my bucket we're going to use here this environment variable that we have defined. And now that we are using sync we can remove the copy command. We no longer need that. We no longer need to create this file in advance. We already have here everything part of the build process. However we're not yet in the build process, so we need to move the entire stage. So I'm going to select the stage that we have here. Kind of cut it. And we're going to go all the way down here. Theoretically, we should go through all the stages that we have, but just for demonstration purposes, we're just gonna immediately deploy here right after the build. So it's definitely not the best approach as there are no tests. But at this point, we can easily see that we have a build and that after the build, we have all the files needed to actually go and deploy. There's not going to be a perfect pipeline from a CI CD perspective, but the goal here is to understand how to use the AWS CLI. So I hope you're going to forgive me. Let's go ahead and commit these changes and see if it works. Let's take a quick look at the logs. And we're going to see here that our stage has failed. So there was a problem. And the error that we're getting back after running this command. So we can see here we have the right command, we have the right directory here we have the right bucket. And it says here the user provided path built does not exist. And the question to you now is, do you know why? Do you know what's the problem here? Oh, I'm going to tell you in a second, but pause the video and think about why doesn't this work just as a quick exercise. So here it is. When we're starting this Docker image, we are not reusing the workspace. Here the workspace exists and it has been used here by this build directory because it's using here reuse node set to true. But here we haven't specified this parameter. So we are starting this AWS CLI container in a completely new workspace which doesn't have to build directory. This is why it doesn't exist. So we're gonna make this change and try once again. So let's take a quick look at the logs to see what has happened. So now the stage is successful. Luckilly. Let's expand this and make sure that everything is as expected. So this is our command. And what we're going to see here is that this command is telling us exactly what's going on. So it's telling us it's uploading this file that you see here or the index HTML. And so on. So it's giving us a full report of what has been uploaded. And if there's something that is deleted, we execute this a few times. We're going to see that files will get also deleted and so on. So it was a good idea to take a look at the logs to understand exactly what's going on. Now from a CI CD perspective, building and immediately deploying is a no no. But our scope here was just to get started with AWS, and I think we have succeeded at if we wanted to have a clean solution, we should have moved this AWS deployment right at the end of the pipeline here. Replace it with deploy prod, run the end to end test on it as well. Now. Just because these files have been uploaded doesn't mean that our website is working. So going back to our address here, let's click the refresh button. And what we're going to see here is. To our website. We also have here the application version. So this is build number 84. And we can check it against what we have in Jenkins as well. So we'll see here. This is build number 84. So we know what we have here.
