# Managing AWS services with AWS CLI

But we have now successfully, let's say, installed AWS CLI inside our Jenkins pipeline, and we can use AWS to interact and manage the different AWS services. But how can we do this? Well, to find the answer to that question, we need to go back to the official documentation to the command reference for AWS CLI. And in this case, after going here over this first part where the global options are being explained, we need to scroll a bit further down. And we're going to see here at one point available services. So these available services are essentially all the services from AWS that we can manage by using the AWS CLI. Let's say that we want to get a list of all the buckets that are available inside our S3 account. So exactly what we are seeing here from this view, where we see a list of all the buckets. In this case I only have one bucket. How can we do such a simple command with aws cli? So in that case, the first step is identifying the service that we need to use. So in this list I'm going to simply use the search function or scroll through this until I can find S3. But slightly further down in this list, one point we're going to see here S3. Let's go ahead and click on this. And it's going to provide us a description. And again, a lot of information. But if we take it easy, we're gonna understand exactly what needs to be done. So let's again go through this documentation and see what kind of things are available. So again, if we go a bit further down, we're going to see all the available commands here. So this is the syntax of the command. So we need to start with AWS space S3 space. And then specify the command that we want to use, and available commands are listed here. So these are all the commands that S3 supports. So let's see for example LZ what does LZ do. So LZ will list as three objects and common prefixes. So it lists some things. And I'm also expecting this to list buckets. So in order to use this, all we need to write is AWS S3 ls going inside the Jenkins file. We'll need to write. Here is AWS S3. LZ. Let's go ahead and commit these changes and see what is happening. So now if we're taking a look at our pipeline, we're going to see that the stage has failed. Let's expand the scripts and understand what has happened here. So we still have the AWS version command. So that is still working. And then we have the next command where we're using S3. And we're getting the following error. Unable to locate credentials, you can configure credentials by running AWS configure. Now we cannot really run commands like this in an interactive way from Jenkins. But if you're thinking about this, what this is actually telling us is that AWS, S3 cannot list any information, cannot give us any information. I mean, how is AWS CLI supposed to know who we are? We have just used a Docker image that anyone could have used. If we could have listed buckets or files, that would be a problem because we could see information from other accounts as well. And trust me, that would be a huge security issue. So we need to tell AWS CLI who we are to be able to execute such commands that fetch information from our account. But this requires a bit of work which we will do over the course of the upcoming lectures.
