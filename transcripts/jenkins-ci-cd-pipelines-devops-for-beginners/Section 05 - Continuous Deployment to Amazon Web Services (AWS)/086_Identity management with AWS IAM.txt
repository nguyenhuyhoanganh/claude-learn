# Identity management with AWS IAM

Going back to the logs, let's try to understand how we can fix this error that we're getting in. Jenkins, when we're trying to interact with one of the AWS services. What can we do? Should we put somewhere in Jenkins our username and password so that AWS cli can find them? Yes, but only kind of. When we're using AWS CLI, we cannot use the email and password we have used to log in into the AWS Management Console. So this is the AWS Management console, this web interface of AWS. And here we're using email and password to log in. We cannot use those in Jenkins. Actually we don't even need access to all services that our root credentials provide. It's just simply too much. If we just want to upload a file or get a list of the files in a bucket, we don't need so many permissions that we have in this account, and any way that wouldn't be a good thing. So it's anyway a good thing that this is not possible because otherwise we would do something super stupid. In this lecture, we will create a set of credentials that have a much narrower scope and which allow us to interact only with the S3 service from the AWS CLI. The idea is that when we're trying to use a service, we should only assign the absolute minimum permissions needed to do the job. This is called the principle of least privilege. And to help us with this, AWS has a service which is called Identity and Access Management or simply IAM or Im, depending on how you want to call it. So I'm going to go ahead here in the search box type IAM. And this is the service that we want to use. This is the service that you have also used to set up multi-factor authentication. But this also allows us to create some users. You're going to see here in our case we don't have any users. But in this lecture we're going to create a specific user that we will use with AWS CLI. To do that. From the left side menu here we're going to click here on users. And currently we have no users. So let's click the button Create User and then create a new user. And I'm just going to call it Jenkins so that I know why I have created it. This user does not need access to the AWS Management Console, so I'm going to leave this checkbox unchecked. Go ahead and click on next. The second step is regarding the permissions. So what is this user allowed to do? And by default the user is not allowed to do anything. And we want this user to be able to upload files to S3 or to delete files if needed. So essentially it needs access to this service S3. What we're going to do next is attach a policy. And a policy is a way of configuring what's allowed and what's not allowed. So a policy can be attached to a user and this decides what the user can do. Let's go ahead and search here for policy, which is called S3. And let's see here a lot of policies that are related to AWS, S3. And we're going to select the policy which is called Amazon S3 Full Access. So we're going to select this policy and then go click next. So now our user has full access to S3 and only to S3. So you can do a lot of stuff in S3, but only there. So for now, this is all that we need from this user so we can go ahead and create it. And now the user has been created. Perfect. But how does this help us? How can we use this user in Jenkins? Well, to actually be able to use the user, we need to go back to it. So again we are here in the users menu. In the IAM service we're going to select the user. We can see which policies this user has. So we can confirm again that it has the S3 full Access policy. And now we're going to click here on the tab Security credentials. And if we scroll a bit further down, we're going to see here this section with access keys. And we need an access key to use AWS CLI to interact with any service from AWS. And it says here use access key to send programmatic calls to AWS from the AWS, CLI, AWS tools and so on. So we need an access key if we want to use AWS CLI. Perfect. So let's go ahead and click on Create Access key. And anyways is trying to educate us a bit and we're going to select here our use case being command line interface. And AWS is going to make some recommendations here for our use case. This is absolutely fine to use the approach. And we're going to click here that we understand the recommendation. But we still want to create this access key for this particular use case. So let's go ahead and click on next. Here we can also provide a description so we can write here. Access key for Jenkins. Super useful later on when you're taking a look at something to understand. Why did you create this. And later on that would be super useful. So now we have created this access key. And we have here an access key which is similar to a username. This is not really a secret. It can be treated as a secret. It's not necessarily a secret. And this access key also has a secret access key. So this is practically the password. So now we have a combination of username and password that we can use with AWS CLI. And this map back to this Jenkins user that we have created here earlier. And the Jenkins user is allowed to interact with the S3 service. Apart from this Jenkins also provides here some best practices. So things that we should know. And there are only things that we know here. For example, never store your access key in plain text, in a code repository or in code. We already know this. In Jenkins, we use a separate secret manager that Jenkins has to manage this secret. Apart from this, it's always a good practice. Like once you no longer need a secret to disable it. Whenever you no longer need a user or an access key to simply disable or delete it. The third one I talk in the beginning enables least privilege permissions. What I would like to add about this is that using the policy Amazon S3 Full Access, it's not fully adhering to the principle of least privilege, as the scope can be narrowed even further, down to a single bucket and even to a specific list of actions. In plain English, this policy still gives this user a bit too much power. But for our use case here, that's fine. While I think it is important to do things right from the start, it kind of goes outside the scope of the course. So if you wish to learn how to do this the proper way, check the resources folder for a quick tutorial.
