# Why do pipelines fail? Exit codes explained

So what has happened with this pipeline? Why does it say here finished failure? And how does Jenkins know that actually, this file doesn't exist? Well, the thing is, so far we have seen the pipeline fail. If our pipeline configuration was incorrect, or if we use the command in a way that it was not intended to be used, so their commands can bring a message saying, hey, something went wrong. You're not supposed to use this command like this or something in that direction. Now, this command hasn't printed any message. It doesn't say that we have used it incorrectly. It doesn't say that the command doesn't exist or anything like that. It's just has it been executed? And then somehow Jenkins knew that there's something wrong. And you're going to see here this says here script returned, exit code one. This is like a secret code. You know, that this utilities have with Jenkins, like Jenkins exit code one and Jenkins like okay, Roger, fail the build. So what is happening here? Can we get into this secret code that Jenkins and these commands use? Of course. Now, exit codes are a way to communicate if the execution of a program has been successful or not. An exit code zero will indicate that the program has been executed successfully. Any other exit code, which can be a number from 1 to 255, indicates failure. Now, in this case, this command test has returned the exit code one. All the previous commands have returned an exit code zero. Now, this is not explicitly mentioned. This is like kind of like expected. So if everything works well, a command just does what it's supposed to do. Prints whatever messages it is supposed to print. And then. Yeah, it tells jenky it strange. Exit code zero and Jenkins like, okay, fine, let's continue next command. But if Jenkins gets this exit code, that is not zero, it will interrupt the pipeline. So it will not execute any other request after this. So if one command fails, if one command tells exit code one or any other exit code that is not zero, then Jenkins will essentially stop the execution. It will fail the pipeline, so the entire pipeline will be marked as failed. Just because one command here has returned this exit. Code one. So when we are seeing here at the end that something has happened, a script has returned an exit code. We need to look. What is the last command that has been executed? From where does this exit code come? And you can see, oh, it's coming from here. And then we need to understand what the command itself is doing. Right. And in this case test is not finding the respective file. So it's supposed to find build for slash computer dot txt. And it's returning one as an exit code. When it does find the file, it means that everything is good so the pipeline doesn't fail. So this is how we can use such commands in Jenkins to test for stuff. And then all we have to do is return an exit code. This is something that the command does automatically. We don't need to tell the command hey return this text so the command does this automatically and then Jenkins will be notified. And then ultimately we will be notified to know, hey, you expected to have an artifact, but that artifact doesn't exist, so do something about it. Take a look at the logs. Try to understand what has happened. Then you can. Oh, here it is. This command is actually removing the file that we need. This is why it's not working anymore. Of course, this is totally simulated, but the idea is the same. So I'm going to go ahead and fix this pipeline. I'm going to remove here. And I just wanted to briefly demonstrate how this exit code works, because this was like in the last stage, it has been the last command. So it's kind of like not always so clear. So we can have here something in between where we can write exit two. So this exit is actually a kind of a command that we can also call. So we're going to simulate here an error somewhere in the middle. So gonna click here on Build Now. And you're going to see this failing again. So if the build stage is failing, you're going to see also the test stage marked as failed even though there's nothing executed right. So we can take a look here at the logs to see what has happened. And we can see here there are a few commands that we have already executed. Created a directory. We have created this file. We have pushed main board to the file. And then we came to the command exit two. Right. So and gonna tell us here error script return exit two. So if we're trying to understand well from where does this exit code to come from. We need to take a look. What is the last command we can see here. The last command is actually this plus nine. In the beginning it is from exit. And then. We're now getting also the message here. Stage test skipped due to earlier failure. So Jenkins is going to skip everything else. Now I just added this for demonstration purposes. I'm going to go ahead and remove it, but I hope you got an idea of how this exit codes work, because it's a super important concept and we're gonna be debugging this pipelines quite a lot. So it's important to have the patience to go through the logs and understand what is happening.
