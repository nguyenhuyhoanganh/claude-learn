# Basic Linux commands recap (practice lab)

Welcome to this first lab, which is designed to get you more familiar with Linux commands. And in this lab, we're going to recap some of the most important commands that we have learned in this section. Now, first of all, how does this lab work? You need to be here in practice mode. So currently we are in video mode. And if you click here we're going to switch to practice mode. And you're going to get here this workspace the most important part is here the terminal. Because this is going to allow you to type in various commands. So it's going to make it a bit larger so that you can probably see it. And I'm also going to go ahead and close this because it's not needed. And what I want you to do is to try on your own some very basic commands. I'm going to tell you what to do. You need to pause the video, work on the respective command. And then I'm going to show you and we're going to proceed to the next task. And so on and so forth. It's not going to be very complicated for this initial lab. It's just to get you started and familiar with the overall interface and how it works. So let's begin. First task create a new directory called build and make sure that it is inside the current user home directory. Now currently we are here inside code. Okay, pause the video. Do this on your own. And after that watch how I did it. Now currently we are inside this code directory here. So what we want to do is use CD and dot dot to navigate one level up so that we are in the proper directory. So if we use pwd which is a Linux command, it's going to show us that we are inside home user. And this is exactly where we want to be. The command that we're going to use is make directory. And we're going to call this directory build. We're going to use the command ls with a flag L to verify that this has been indeed created. I'm going to see it here appearing in the list for this next task I want you to put inside a build directory a file called computer dot txt with the content mainboard. All right. So this is going to be relatively easy. We can use the echo command. And between quotes we're going to put mainboard. And all we need to do is redirect the content to this particular directory which is built. And then inside there we need to specify a file which doesn't exist yet. So we're going to create it on the spot. It's going to be called computer dot txt. After we have executed the command we can use Cat to inspect the contents here. This is going to be relatively easy. We're going to see it has the word mainboard. The next task is to append the word display to this particular file. Now appending is very very similar to redirecting but it is a bit different still. So if we would just bring up the previous command and we would put here display. Well this will actually overwrite the contents of this file. So let me show you kind of right as it is. And then if we're looking with Cat, we're going to see that this file contains only display. So that hasn't been very useful. So let's bring back the previous command with main board that we have executed here. Run it. Once again I'm going to confirm the content is main board. I'm going to bring back the display command that we have executed here. And instead of this redirect we're gonna use the append operator. So this is just going to add to this file. And again with cat we're going to inspect the contents. We're going to see okay we have main board and we have display. So they are all there. All right. For the last task of this lab we want to check if the file computed dot txt exists. And we want to check if that file has the text command by using a specific command without us manually inspecting it. So let's go ahead and see how it works. We're going to use test as a command. I'm going to pass here the F flag which checks for files. So this needs to be a regular file only to specify the path where we want it to be. Well we want to check here for example is build info dot txt file and we're going to get back. Well nothing. This is the part that's a bit annoying right. So what we need to do is display here a status code. And we're going to do it like this. And the status code is actually one. So this is an exit code that we have gotten from the previous command. One means error. Something didn't work properly. This is not being displayed here. But that's absolutely fine. What we are interested in is the exit code. So that's one thing to note that this didn't work. All right how about our file. Well we're going to take a look here at computer dot txt I'm going to run the exact same command. Again. We're not going to get an output. But if you're asking for the exit code we're going to see here exit code one meaning that this command has been indeed successful. So the file exists. Okay. Now let's search for display. How do we do that. We're going to use grep to do a simple search. So we're going to grab for display. And we need to write this properly. If we don't write it properly it's not going to be found. I'm going to search inside build computer dot txt. And we're going to get back display. If we're searching for something that doesn't exist or we are misspelling something for example the or something like that, we're not going to get an error back. But if we're checking for the exit code, we're going to see here that the exit code is one. And in a CI CD pipeline this will fail. So this is just a quick recap of the various commands that we have used up to this point. Do practice a bit more so that you are familiar with them, and better understand exactly what's happening before proceeding. Practice makes perfect, really. Even if you're confused at this time, spend a bit of time. Take a look at this and do your best to understand what has happened so far in the course.
