# Storing build artifacts in Jenkins

Not a problem that we're facing by cleaning the workspace is that we are creating this directory. We are creating the computer dot txt. We're adding mainboard, display and keyboard to it. Everything is inside the file. Everything looks good. But then before we can actually use it, we throw everything away, which kind of like defeats the purpose. So we have solved one problem, but we have generated a new one. Well, not quite. Essentially. Now when we have something inside our pipeline that we want to keep, this means that we have created an artifact. So an artifact simply means that this is something human made. We have created this build directory with a computer dot txt. This is not something that we need during the execution. This is something that we want to keep to something that we want to use later on. So if that's the case, then we need to tell Jenkins that we want to keep this artifact. And this is again something that happens after the job execution. But before we actually clearing the workspace. So let me show you what I mean by that. I'm going to go back to the pipeline configuration. I'm going to scroll here all the way down where we have this post action. Let me zoom in a bit. So what we're going to do here is that we have the post action. And the thing is, we don't want to archive the artifacts if there is an error or something like that. So essentially we don't want to use here this always block because it would mean that we're telling Jenkins whatever happens, you know, keep this. And maybe sometimes it's useful for debugging or something. But generally speaking we only want to do this if the job has been successful, if the pipeline execution has been successful. So this is why we want to create here a new block which is called success. And again between braces we can put stuff. So what are we putting here. Now there's another instruction that we can give Jenkins. And this is archive artifacts. And you need to write it exactly as you see it here. So this is telling Jenkins, hey, I want to keep something and then we need to tell Jenkins, like, okay, this is what we want to keep. So we're gonna use this instruction, archive artifacts, and we're going to specify here which artifacts we're going to write here artifacts column. And then between quotes. And I'll specify here build forward slash. And after this I'm going to add here two stars. So essentially these stars are like wild card. It tells archive this directory and everything that is inside there. This is the main idea of why we're using this. So we're going to go ahead and save this and let's build it once again. So this time oh we're getting an error. So let's take a look and see what has happened. Why are we getting here. An error. What is wrong with this execution. We're gonna click here on the console and we're trying to identify what has happened. So in this situation, what we can notice here is that we have cleaned the workspace actually before archiving happens. So before Jenkins manages to essentially keep this build folder where already deleting the workspace. Right. So again super important to take a look at the logs to go step by step and to understand what has happened. You know, Jenkins is telling us that the language that Jenkins has is, you know, it's not the nicest language, but if we spend a bit of time and take a look at this, we can understand what we did wrong, or at least why this isn't working as expected. Let's go back to the job configuration and make a few changes. Now, one of the first things that we need to understand is what exactly is happening with this post actions. Now, essentially, Jenkins has a specific order how these actions are executed and essentially always is always executed before success or failure, which is another option. So there are multiple ways how we can fix things. And one of this would be for example, to use success and failure or to divide this clean workspace, but for example, an added benefit of not clearing the workspace at the end would be the fact that we can still take a look at all the files that have been generated. This is useful for debugging things. So what we can do, for example, is clean the workspace at the beginning of the job so we can remove this instruction from here and essentially remove all this altogether. We're only going to keep here success. And what we can do here is, as a first thing, we're going to clear the workspace. When we're starting the build, we can add a completely new stage where we're clearing the workspace, but we can just use this instruction here. So let's take a look at how this is working as it is right now. We're going to see here that the execution is successful. And what we're going to see here is that taking a look at the stage. So it starts by first deleting everything that is in the workspace. And then it starts the build process. And then it is archiving the artifacts. So this part with archiving artifacts this is also working. So we have essentially fixed two problems. Now we can see here right on top this new thing is because we have used this artifacts that we have archived. So we're going to get here always like the last successful artifacts. But also if you go to the job itself. So we can click here on this number, we're going to see here that there are some build artifacts. So we're going to see them here. And we can take a look here at the file. We're going to see here. Oh, this is the file and it looks good. But at the same time we can still go to the workspace and we can check how things look like in the workspace itself.
