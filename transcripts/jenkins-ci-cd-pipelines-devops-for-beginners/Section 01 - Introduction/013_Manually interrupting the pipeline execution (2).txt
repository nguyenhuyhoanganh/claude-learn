# Manually interrupting the pipeline execution (2)

As you have seen so far, it takes very little for these jobs to be executed. So we're clicking on build now and essentially in seconds, if not faster, everything is already done. So you can see here the duration of the build is two seconds. And then we also have like creation of the artifact which is taking 105 milliseconds. Now typically jobs do take a bit longer. And sometimes these jobs may misbehave in the sense that they run a bit too long, more than we want to. So let me show you what I mean. I'm going to go ahead and configure this pipeline and what we're going to do here. Somewhere in the middle of our configuration, I'm going to use another command which is called sleep. Now you can have like the idea of what sleep does. So it essentially is telling Jenkins to sleep to not do anything. And sleep takes an argument in seconds. So we're going to set it for 600 seconds. So this will put essentially this job to sleep. So let's go ahead and click on save. I'm going to click here on build Now. You're going to see here the job is starting right. So we can see here the progress of our pipeline. So we are currently in the build stage. And Jenkins is like trying to update us. So there have been seven seconds 13 seconds. So this is executing now for a while. If you're curious to see exactly what is happening like in real time, you can use this progress bar here so you can simply go ahead and click on the progress bar itself. And this will take you directly to the console output. And you will see here this dots here. This indicates that this job is still running. It's still doing something. Now currently the command that is occupying so much time is this sleep command, which is not very surprising. Now of course you can wait for this command to be done. Just simulating here like a real command that takes a lot of time to do things. Sleep is not doing anything, it's just sleeping. But sometimes, you know, you may encounter commands that are just stuck. You know, for some reason the entire process is stuck. And Jenkins is in this state where it is simply stuck. So you can see here at one point Jenkins will kind of like indicate this in red because it's like, oh, this is taking a bit too long. I mean, the previous jobs were like two seconds and three seconds and this is now one minute like I don't think so. So what is happening here is that we may need to manually interrupt this job, because it looks like it's going to keep Jenkins busy for a long while. And this is something that we don't want. So in this case, what we're going to do here is we have this x here. You can see here at the job. There's this x. And also if you're going here to the dashboard. So you are outside of all the jobs you're going to get here, this essentially built executor status. Right? So at one point in time, this executor that we have can run two jobs in parallel. So for example, we could again start our hello pipeline and go back and we're going to see it appearing here. Or maybe it's so fast that we don't even see. But we can also start this laptop assembly pipeline once again. And essentially when we're doing this we have like two pipelines running in parallel, two separate builds, and our executor is busy. So if we're trying to run again, this super fast pipeline. And I go back here to the dashboard and we're going to see here we already have a build queue. So jobs are queuing up. Jenkins is busy with these other jobs. And if you have a lot of projects running in within Jenkins, uh, essentially Jenkins will get stuck at one point. So we're going to be overwhelmed with this. And if that is the situation, we may need to manually interrupt Jenkins. So we're going to click here on this x. So this will forcefully stop the job right. So we're going to select here. Okay. Takes a few seconds. Don't worry about this. I'm gonna do this as well. So let's give it a few seconds. We can also go back. Okay, so the queue has been cleared. There are no more jobs running. So if you're going here to these jobs you can see here that they actually have this X here. So it's essentially aborted. So we can take a look and see here what has happened. And we're going to see here aborted by Valentin. So I manually aborted this. We were still here in the sleep. But then I sent here the interrupt signal to the process, which is a way of telling the process. Hey, I don't want you doing what you're doing anymore. Just try to stop. And then this entire thing stops and it will appear here as a cancelled pipeline. So it will have no build artifacts or anything like that. From this perspective, it has been stopped. So it's something super important because I'm pretty sure you're gonna encounter jobs that misbehave and you may need to stop them. I wanted to show you how to do this early in the course.
