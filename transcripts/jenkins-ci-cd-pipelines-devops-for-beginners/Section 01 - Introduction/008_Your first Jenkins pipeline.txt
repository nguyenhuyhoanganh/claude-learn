# Your first Jenkins pipeline

So what is a pipeline? Allow me to make an analogy to an assembly line used to manufacture a physical product. Any product goes through a series of steps before it is released to the customers. Let's take a laptop, for example. To oversimplify, the assembly line could have the following steps. Take a laptop body case. Add the mainboard. Add the keyboard. Do some quality assurance to ensure it turns on and works properly. Put it in a box. And then finally ship it to customers. Now we are not using Jenkins to produce physical product. We want to build software. However, producing software has many things in common to what I've just described. Before we can ship anything, we need to go through a series of steps. Let's try to build the laptop assembly line in Jenkins using a pipeline. Well, okay. Kind of. Instead of using real components, we're going to use a file folder and some text. One of the reasons we want to use Jenkins is to avoid doing manual work. So let's begin with the first task using a new laptop case. We're going to put this in a stage. But first let's go ahead and create a completely new job. I'm going to create this job and call it laptop assembly. And select pipeline as a style and click on okay. And want to jump directly here to the pipeline. And we're going to start with the Hello World because this already gives us the structure. We're going to put everything that we need to do in a stage because we kind of like considering this, like building part. We're putting all the components together. You want to replace hear the word hello with build so that we know that this is the build stage. Now, inside the steps there are a series of commands we need to run. So for example, we can still use the echo command. And we can have here a message like building a new laptop so that we know at this point we are starting the build process. Okay. So the first part is done. Now let's say that when we're building this laptop we want to store everything in a directory. So in order to store everything in a directory we actually need to create a directory. So we're going to use a shell command which is called mkdir. So this will create a new directory. Of course, to be able to use this command we need to use here SSH. And to run this as a shell command. Now make directory also takes an argument and the name of the argument is build. All right. So the first step is already done. Now let's say that we need to create a new file. So we want to create a file which is called computer dot txt. And this file needs to be inside the build directory. For that we can use another shell command which is called touch. I'm going to use here ssh. So touch. What touch does touch is another Linux command which has a double function. It changes the date if the file exists. So you can see at the file when it has been lastly modified. Or if the file doesn't exist, it creates that file. And this is exactly what we want to do with this touch command. We want to create a new file. So we're going to put this file inside the build directory. And we're going to call it computer dot txt. So it's build forward slash computer dot txt. This is where we're going to store all the components that we want to add to the laptop. All right. Now let's say this first part is done. And now we want to for example add the main board. So what we can do here is we can use the echo command to print the text, and then we can use an operator to append that text to a specific file. So let me show you how it works. So we're going to use SSH. So we're going to use here echo. And we're just adding here the text mainboard. And we want to add it to this file. Right. So in Jenkins we don't have like a file editor Jenkins it's not going to go ahead and open a file and paste some text there. So we always need to work with this Linux command. So we have here echo. So this is printing this text echo. But of course we don't want it printed. We want it to have it inside this file. So what we're going to do here is we're going to use this double greater than operator. So we have this double greater than signs which essentially takes the output of one command. So the output of echo would be the text mainboard being printed. And essentially it appends it to a specified file. So we need to specify where do we want to append this. So we're going to use here this one. So now we are appending mainboard to this file. Okay, I think we already have a few steps. So whenever we're working with, whenever we're building something, it's always a good idea to save the settings, see if everything is working as expected. So I'm going to go ahead here click on save and click on build now. Okay so what we can see here is we have our build history. But there's also built history essentially here when we're opening the pipeline job. So we're going to see here that this build thing, this is the build stage that we have. And if we hover over this, we can also get access to the logs from here. So if I'm going to click on this this additional window will open up. And if we're very careful to click on the right spot on this arrow down, we can expand. So we can see here what has this command did. So we're going to see here. This is just simply a print message. This comes from Jenkins. That's you're building a new lab. Then we can take a look at another one, which is the shell script, which is creating the directory build. So we can see here this command. There is no output from this. So if there is no errors it means that it does work without any issues. And then here on the shelf script, we can see here that we're creating this file. Perfect. No errors. There's also no output. So there's nothing like file has been created or something. So if there's no error, all is good. And then lastly we are using echo and redirecting the output in this file. So you can still see the command here. It's only part of it. But again there's no output. All right. So how do we know that this has been successful. Right. Well there are at least two ways on how we can do this. I'm going to show you the way how we can debug this from within the pipeline itself. So I'm going to go back to configure into the pipeline. So what we can do here is for example use another utility command which is called cat. Oh cat has nothing to do with cats. It actually comes from concatenate. And it is essentially a utility that can be used to create, display or modify the contents of files. Now, quite often this cat command is abused to view the content of a file. So again with ssh we're going to use here cat. And we want to see here what is the contents of this file right now. Okay. We're going to go ahead and save this. Run it once again. And what a bummer. Already. Our job is not working anymore. And you're probably thinking like, hmm, wow, is this from this cat command? It must be the cat command. Why this isn't working anymore, right? Well, first of all, we cannot make any assumptions if something doesn't work anymore. The only way to figure things out is by looking at the logs. I'm going to go ahead and click here on the console output to see the logs to see what has happened. Why did this fail? And it's important to go like from top to bottom to have an overview to understand what has happened. So you're going to see here the first part. We have this echo building a new laptop. And then we have the essentially create directory command. And this is kind of give us here an error. It's going to tell us. Cannot create a directory build file exists. Now this command can only create a directory if it doesn't exist already. So what it means is that we have already created it with the previous job, so it still exists. So when Make directory is trying to create a directory again it's like doesn't work anymore. Now there is a quick fix to this and we're going to go back to the pipeline configuration. Make that change. And this is to push here an additional parameter. We're going to put here a flag which is dash p. Which is kind of like telling McDeere, hey, create this build directory if it's not already created. So this is like what this is telling and this will help get rid of this particular error. Let's go ahead and click on build once again. And we're going to see here everything is working fine. We can take a look at the logs to see what we have there. And we can see here McTeer is still working. We are creating this file. We are putting mainboard. And then with Cat, we are taking a look at the contents of computer dot txt. And we can see now that we have mainboard two times inside. Right. So the first time was from the first execution and the second time is from the last execution that we had. We're going to try to understand a bit later why this is happening, but for the moment we are kind of like happy that it works as it should. So we're going to go ahead and add the rest of the stages here. Let's go back to our configuration. And what else do we need? So for example, we also need let's say a display. Right. So we could also copy this command that we have here. And we can put here display. And again we want to check the file to see how it looks. After this we can use again the Cat command. Or we can also go ahead and for example put here a keyboard. And again, we can check the contents of the file to see if it works as expected. I think we already have all the steps that we need just to understand how these pipelines work and to run them once again. Take a look at the results. And what we're going to see here is projecting the file. Initially we have three main boards. And then after adding the display we have three main boards and a display. Then if we have the keyboard and then if we're adding the keyboard, we also have here the keyboard at the end. So everything is working great. It's not exactly what we wanted, but at least we don't have any errors. And we kind of understood how we can use this Linux commands to at least do some basic stuff with files like creating a directory, creating a new file, adding some file, and printing the contents of the file.
