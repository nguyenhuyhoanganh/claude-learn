# Conclusion

All right, let's recap the most important concepts we have covered so far. We began this section by introducing two types of Jenkins jobs Freestyle and Pipeline. We learned that pipeline jobs are favored in modern Jenkins implementations as they align with the latest best practices. While creating a simple Jenkins pipeline. We have learned several handy Linux commands, such as echo, which is used to print text or inspect the value of a variable. Mkdir used to create a new directory. Touch, which can be used to update the date on a file or create that file if it doesn't exist. R m which can remove a file. Sleep, which introduces a pause in the pipeline's execution. Cat, which has nothing to do with cats but can be used to display the content of a file. Test, which can be used to check if a file exists. And finally grep, which is a super complex command, but we have just used it to search for a specific string in a file. While creating the pipeline, we have learned just how important it is to pay close attention to the logs created by Jenkins during the execution. They are key in understanding what is going on as well as troubleshooting any issues. We have also discussed exit codes, which serve as a way for commands to signal Jenkins whether the execution was successful or not. Lastly, we have emphasized that DevOps is not just about using a specific tool. It's more about the practices employed in building and deploying software. We have the tools to assist us in this process. With that being said, let's take a brief coffee break, stretch our legs, and we'll continue in the next section.
