# Testing build artifacts

In our pipeline configuration, we have now used test. But this test is not some magical command that knows exactly what should be inside this file. So just because the file exists doesn't mean that it has the right contents. We expect this file to have a main board. We have expect this file to have a display and a keyboard. How can we test for that as well? So we know that the file exists, but how can we actually check the contents of the file to see that it contains what it's supposed to contain? Now here in the build we have used cat and it displays everything, right. So theoretically you could use Cat here in the test as well. Well using cat is kind of like useful for troubleshooting purposes. But just because we use the Cat command and there's nothing in the file that's not going to give an error or something like that, it's just going to print nothing. So it's not super useful. What we actually want to use is another command. And this is the grep command. So grep is a CLI tool that allows us to search for a specific string in the file. So for example I just want to check that there's a mainboard. How can we do that. So we're going to go ahead and expand this search here I'm going to write here three. Single quotes. And I'm going to add two more here. So we're testing the file. This is like the first logical step. And then we can use grep. And we can specify here. Hey, we are looking for what are we looking for? We're looking for the main board. I'm going to put that between quotes. And this is like search you're thinking about grep is like search search for main word. Where do you want to search. I don't search here inside build computer. TXT on it. Search that this is inside here. Now grep is a super complex command. It supports regular expressions. But we don't go into this in this course. We just want to search that. This file contains main board. And if it contains you know we're happy. So let's go ahead and click on save. Let's build it. And again everything looks good right. So the tests are passing. Grab doesn't say anything. Oh, it does say something. Oh, look at this. This is interesting. So grab will say here mainboard right. So it will search for mainboard in this. And it will print essentially the output. So it means that mainboard is available again which test. We want to make sure that the test failed. Because if the tests on fail the entire pipeline is useless. And let's try to see what we can do. So for example what we can do here in the script. Is. After creating the file, we can simply go ahead and comment here Mainboards. We're going to write here. Forward slash. Let's see if this works as a comment. So we're going to comment this in the script. Let's click on save. And build it again. It is good that it is failing, but probably not for the right reasons. Actually what we're going to get here is an error. So this is not the correct way to write comment in this. I'm going to get a weird error. Permission denied. And this is just because of this. So we need to understand what we did wrong. So we're going to use here this nice character to make a comment. Let's see if this is working better. And build now. So now we can see here the build is successful. The tests are failing. So what we're going to see here is in the logs. We're going to see that mainboard has not been added. Right. So we see the command with putting the mainboard inside that file doesn't exist. We commented it out. And then we're testing the laptop. And grep is again one of these commands, one of these pesky commands that is then ruining the party and saying, hey, I didn't find the main port exit code one, stop everything. And then Jenkins like, okay, we're stopping. So this is how grep works. It will return an exit code one if it cannot find what we expected to find in the file, and if it finds prints that output. So, yeah, I'm quite happy with this. It seems to be working fine. As always, it's always a good idea. If something fails, you want to also understand what has happened. So you can also manually inspect the artifact or manually inspect the workspace. Now for this artifacts here. These are the last successful artifacts, so don't confuse them. You're going to take a look at it here. It's like oh look at there. There's a main board. No no no these are the last successful. These are like from build number 22. You want to see the artifacts. If you want to see the computer that txt file from this execution the 24th execution you need to click on the number. Go here to workspaces. Go to the workspace. Build. And click on this eye icon and you're going to see here main board is not there. So sometimes when the pipeline is failing. So it's a good idea to inspect the workspace to see exactly what you have there. Is it a problem with the command or is it a problem essentially with what has happened in the sense that this file really doesn't contain main board, the command is actually working fine. We need to fix our build in order to improve this. Okay, so kind of like we're getting the idea how this works. So pretty easily we can expand this for display and keyboard. So let's remove this comment from here. On to remove the comment. And we're going to use here display. And we have to issue here another command grab display. And I'm going to use it inside here. Now duplicate this and we're going to also use here the keyboard. So we have mainboard display and keyboard. We're testing for all. Let's see if everything is there, if everything is working as expected. Everything is working as expected. It's always a good idea. Also to check again the file to make sure that everything is there. Now, the advantage of having tests is that next time when we're making changes to this build, we don't necessarily need to worry so much about testing because we have tests in place. Now, it's always a question like how good the tests are, but so far we are kind of like confident that these tests are doing what they're supposed to do. For example, what if I'm telling you that using this touch command is actually not necessary? So I'm going to go ahead and remove it altogether from here. And you know what? I'm going to rely on this test. Now when I click on save here I'm going to build. Now build is successful. Everything seems to be working well. We can take a look here at the file. The file is still there. We can take a look at the workspace. You will see here the build folder. And also the file is there. So that touch command was not really necessary. But luckily we have tests. So now we can make changes to the build process, optimize it, make it better, and we have the tests in place to ensure that whatever comes out is actually what we expect.
