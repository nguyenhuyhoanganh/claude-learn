# Pipeline stages

So far, every time we made changes to this pipeline configuration, essentially every time we made changes to the build process of this computer, we had to make changes, run them, make sure that the pipeline is running successfully, that none of these commands give an error, but also then go back to the workspace or go back to the artifact and manually inspect the artifact. Now, obviously, this is still a lot of manual work that we need to do. So we don't want every time we make some changes to our build process, to actually have to manually check the file to see if it has been created correctly, and if it contains all the components it should contain. So for that reason, in this lecture, I'm going to show you how to create a test stage. So we already have here the build stage. But we want a test stage to take over this manual process. I'm gonna try to make this as visible as possible. So we are here inside stages. We have the first stage which is build I'm going to right here underneath stage. And this will be the test stage between quotes and the curly brace to start this block, and the curly brace to end this block. I mean, between hitting enter and I'm making sure that the syntax that we have here is valid. Now the stage also contains steps. I'm going to write the steps as well. And we're going to start with a very simple step which is simply echo. And this is echoing, for example, testing the new laptop. We're making small steps. We don't want to do too much at once so that we don't understand what's going on, and we'll save it and run it. And now, every time we're adding or removing stages, things change a bit here in the display. So you'll see here everything that we had before. It's kind of like disappeared because we have added this test stage here. We can take a look at the logs to see exactly what has happened. So we have this build stage. We can see it here. And we're doing all the stuff that we did. And then we're starting here, the test stage. And we can see here our message testing the new laptop. Now as you can see here Jenkins in this console output puts everything that has happened with a job in one place. Sometimes it is convenient because you just have to scroll through this and see if there's anything wrong. Sometimes it's a bit more difficult because you may just want to see what has happened with the stage. So we can still have this possibility of clicking on the stage name and seeing here, oh, we have this print message and we have testing here. The new lab okay. Perfect. Now we have these two stages. But you're not really testing anything, right? So we are just printing the message. So we need to do something more. So what can we do now there is a Linux utility command which is called test. And this command can be used for example to test if the file exists. So we can go ahead and. Check here if this file computer dot txt is inside the build folder. I'm going to see how this works. I'm going to write here ssh. Because this is a shell command and the write test. And we have to pass this minus f or dash f flag to actually test for the existence of this. I'm going to copy here the exact path. Trying to copy paste as much as possible to avoid making mistakes. So let's give it a try and see if it's working. I'm going to click here on save. Build this once again. And the execution is successful. So it seems that this file exists. Now, there's no replacement for really understanding how these commands work. And you always want to make sure that they actually do what they're supposed to do. Because if this command is not really doing what we think it does, then, you know, this entire test and this entire execution is not super helpful. So we want to make sure that, um, what if this file doesn't exist, right? So what if it has been removed or something like this. So we're going to amend here the previous script that we have. So I can use here the command r m which stands for remove. And oops, clicked myself. We're going to copy this. So I'm going to use here are M. To remove this file. Let's see if this is being cut. Click on save build now. And now we're going to see here the build stage is successful. But then we have here the test stage what has happened here inside the test stage. Let's take a look at the logs. And we have here the shell script test minus F. And doesn't say anything, right? But there's an important thing I want you to notice. So we're going to go here to the entire logs that we have, because this is where we get a bit more information. So this is the class command that we have executed. And it is telling us. Error. The script returned. Exit code one. We're going to try to understand in the upcoming lecture what this means.
