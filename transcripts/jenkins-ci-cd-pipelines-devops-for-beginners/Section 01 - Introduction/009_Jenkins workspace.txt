# Jenkins workspace

While our pipeline is working. Honestly, I'm not happy with the fact that this file, our computer contains three main boards, and probably if we run this job multiple times, it will also have multiple displays and multiple keyboards. And this is not okay. Now let's try to understand a bit what is happening here. Isn't like touch supposed to create a new file? Why are we essentially still using the same old file that we had from the previous job? Well, the thing is, touch does not create a new file in the sense that it only creates it if it doesn't exist. But if the file already exists, it just changes the timestamp. It just changes the date when it was modified, but the contents still remains the same. Now why is this happening? Well, essentially when this job is executed, Jenkins uses what is called workspace. So a Jenkins workspace is a directory on the Jenkins controller where the agent where Jenkins runs, builds, and stores the build results like the build directory that we have created and the file that is inside. So we can see that we're here inside the console log. We're going to see here this option which is called workspaces. So we can click on this. And we're going to see here the path to the workspace where Jenkins is saving all these files. So we can go inside here. And what we're going to see here is this is the build directory that we have. And it's also here our computer dot txt. And we can actually also click here on view and see the contents of this file. Now, the thing is, when we're starting this job, once again we're going to reuse this computer dot txt. Now there are at least two ways to handle this. The first approach would be to go back to our pipeline and use, for example, to remove this file or to remove the directory when we're starting a new build. This is definitely one approach, but quite often we may have a lot of files inside our workspace, and sometimes maybe we don't know which files we have. So ideally we'd start with a clean workspace before the build to ensure that there are no stale or unnecessary files left, which may impact other builds. In our case, we are impacted by some file we have created in this initial build. This is definitely not okay. Now there is a Jenkins way of solving this. And this is by using essentially an instruction which is called clean workspace, which is essentially supposed to remove everything that is in the workspace. And we want to execute this at the end of the job. So for example, we're doing something. And then at the end we're going to say okay we want to clear everything. We're going to go ahead and configure this. And again from the pipeline itself. I want you to notice the following. The pipeline begins and ends here somewhere. So this is everything is essentially inside the pipeline. Now after this. This is where the stages come. So we can have here different stages. We only have the build stage. So if you're clicking on this curly braces, you can see where a specific stage begins and ends or where all the steps begin and end. And it's important to have like this syntax. Correct. All right. So this is the first step. Now in Jenkins there's this concept of post build actions. So sometimes we're doing all the things with stages and running commands. But we want to do something at the end. And we're going to do something at the end by defining a post build action. So the first thing we're on the right here is post. And we're going to open and close curly braces. And you can notice here that post is at the same indentation as stages. Right. So that's super important to understand at which level of indentation this is happening. Now inside post. Here we have different options. So for example we can do something only if the build fails or only if the build is successful. But in this case we want to clean the workspace regardless if the build was successful or not. So we're going to use here always. And again between braces and all, this is going to be part of post. So what we're going to do here inside always is clean the workspace. When I right here clean w s and it has to be written exactly as you see it here. So let's save this configuration and see how it works. Initially, what we expect is pretty much the same output, but I want you to notice here we have here the build stage. And now there is this additional stage that appeared here. So you'll see here post actions. This is something that is happening after the execution. So let's take a look at the logs to understand exactly what has happened. So we still have here this is the build stage. So you can notice here we're building the laptop. You're using all these commands. We are still getting a lot of main boards. So this is something that we're trying to get rid of. And then after we are done. So we're printing here like the final output. But here, when we are done, we are essentially using. You can see here these logs. Workspace cleanup. So we are deleting the project workspace and so on. And now the workspace shouldn't contain anything. So if we're going here to workspaces. You're going to see here. There's not even a link. So this workspace no longer exists. So this means that when we're running this job once again. And are taking a look at the logs. We can simply use this feature here. So we're going to expand the last shell script that we have executed. We're going to see that at the end. Our file had a mainboard a display and a keyboard and nothing else because we have cleaned the workspace before actually reusing.
