# Combining multiple shell steps (sh) into one

You can notice in our current pipeline that we have a lot of these ssh ssh whom u. We're using SSH to use the make a line command. Then we're use for touch. Have you using it for echo and and so on. And so forces. Now this is working for it as it is right now because that's not the most issued is way essentially every time there is one of these that elections inside here and new clean workspace enter your or SSH. The Jenkins controller is controlling this, so it's kind of like telling the agent, hey, now go ahead and create a directory. And then the agent reports back, oh, okay. I created a directory and it's telling okay. Now use the touch command to do this and so on. And this can be in some situations inefficient for this super small example it makes no difference. But there's also a different way of writing this. And I just wanted to show you. So what we're going to do here I'm going to write here ssh. Then I'm going to write three single quotes one after the other. Hit enter and then type three more single quotes. And then what we can do here is we can write this CLI commands inside this SSH here on multiple lines. So we don't need to write ssh ssh ssh ssh all over the place. You can simply write them on multiple lines. So let me show you kind of take here this make directory. You can also use this indentation here just to make things clearer. We can also use here touch. I'm going to move it there. And we can also move this main board. It's kind of like easy when you want to change these commands, when you want to work with multiple commands. Kind of like makes everything much simpler to manage. You don't have to add an sh for everything and remove that sleep because we don't want that anymore. Let's also do this. I'm almost done with this. What does a more efficient way to move all this? But anyway, okay, so I removed all this then we only have now clean workspace, then echo, and then in an SSH essentially instruction we are running multiple commands. So we're going to check again if everything is working as expected. Click on build now. Hopefully no errors appear. And look at this. Actually, I would say much more effective. Right. So we had previous executions of two three seconds and now we managed to get it much faster done. So congrats to us. It's also an important way how things are displayed. So here in the logs I'm going to see so much. There is like this. So you see it. It's appearing only once and then it's closing here kind of like and showing oh this everything belongs to SSH. And also if you're looking here at the overview page. So if you're clicking on this and looking at the logs you're going to see here shell script. And this contains everything. So it's going to be all in one big block. You're not going to have shell script shell script shell script. So this is a more efficient way of organizing all this shell commands. Now before we go, let's also check that the build output is also looking good. So I'm going to click here on view. And we can still see mainboard display and keyboard. So at least we know that. And the changes that we made to the pipeline don't impact our final output. And this is kind of important.
