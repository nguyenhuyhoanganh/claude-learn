# Defining environment variables

Let's take another look at the pipeline configuration. I think we made a mistake. We have used this file computer dot txt, but actually we're building a laptop so it kind of makes sense to use laptop dot txt. Unfortunately if we're looking at this we have used computer txt all over our script and in retrospect maybe this wasn't the best idea. Sometimes we need to store information in our pipelines, and some of the details that we're storing are likely to change. So whenever that's the case, it is a good idea not to repeat ourselves like we did here with computer dot txt, but to store this information somewhere where we can change it once and use it multiple times. Now this concept is known as a variable. So we can define variables in our pipeline and then we can use those variables. Let me show you what I mean. So what we're going to do here is below agent any and stages we're going to define here a block which is called environment. To make sure that you are writing this correctly. Sometimes this is like one of the words that is often misspelled, and then Jenkins will not understand what you mean. So double check your spelling there. So the way we're doing this variable declaration is that we want to use as a convention only uppercase. So we can write something like build. Then we're going to separate different words with an underscore file underscore name. So this will be our variable I'm going to write here the equal sign. And between quotes I'm going to write for example laptop dot txt. Okay we have defined a global variable. So this variable build file name is available during the entire job execution here. And it's available to all stages. This is essentially pretty much like an environment variable which is available for the entire system. But more on that later. So we have defined this variable. How can we use it? Well, the way we can use it is by actually adding a dollar sign before the variable name. So let me show you what I mean by that. So inside our script here we can use for example echo and between quotes we can put here build filename. The dollar sign build filename and let's save this configuration. And let's run this pipeline. So if we're taking a look here at the logs. We're going to see here. Print message. And we're going to see here build filename okay so where is laptop dot txt. Well the thing is sometimes these codes can be a bit tricky when working with variables. Now in this case what we did here is we placed single quotes when we're using single quotes with echo. Essentially this string that we're writing here is going to be taken exactly as it is. Now what we actually want to do here is we want to replace this, and we want to have the value of the variable. So we're going to use here double quotes. Let's use again. Apply and take a closer look at this one. Then we're going to take a look here inside the script. So we're going to see here echo laptop dot txt. So now the variable has been rendered. And there's also the possibility of displaying it directly. So let's see if this also working. So I'm going to remove any quotes from here. Click on apply. And rebuild this. And if removing here all the quotes we're going to get here an error essentially. So no such property built finally. So this doesn't work here. So if we want to use echo in our configuration we need to put this between quotes. However let's take this as it is right now and put it here in our SH block. So this is each step that we have. And let's run it as it is right now. See what is happening there. So now the entire execution seems to be successful. Let's take a look at the logs. And what we're going to see here is we have a call laptop dot txt and we can see here laptop dot txt. So inside this shell step it's absolutely fine to use this syntax. So this is essentially executed by Jenkins. But this is essentially echo as a shell command which is a bit different okay. Anyway in case you're getting variables that are not resolved it's probably because of the quotes. So play around a bit with the quotes and you'll figure it out. All right. So now we are printing this here. So we know that this is working. Now the next step would be to replace it all over in our script. So what I'm going to do here is a bit of a copy paste job. Wherever I see computer dot txt I'm going to replace it with this variable name. And this is something we can use directly in the command itself. So not so much an issue for us. I'm almost done. So this is everything that is in the build step. But we also have here the test. So also here inside the test, we need to replace it in all the commands that we have. Right. So we did this again. We can click on save. We have test in place. So if there's something wrong with this configuration we should have some tests that are failing. Right. But sometimes still even if the test passed. So you can see all the tests are passing, it's also not a good idea to do another inspection here on the file. So we're going to refresh this because initially you saw here it was computer that txt. So this was incorrect. Refresh the page once. So now it appears laptop dot txt. It still looks good. Sometimes by major changes of the pipeline. Doesn't hurt to also do an additional manual test just to make sure, but it looks good. And this is how we can use variables in our pipeline and reduce any duplication.
