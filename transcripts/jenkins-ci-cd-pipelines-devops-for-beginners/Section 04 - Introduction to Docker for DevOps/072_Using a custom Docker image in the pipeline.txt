# Using a custom Docker image in the pipeline

So now we have built here a Docker image. Let's figure out how we can use the image that we have created here in deploy staging and deploy prod. Going back to the Jenkins file, let's copy the name of the image and see where in this pipeline have we used the base image with playwright. So this could be one place where we could use it. But just as well it can be inside here in deploy staging. So instead of this image name we can go ahead and paste my playwright and then we can start making some changes. So for example we no longer need npm install. And when we're using tools such as Netlify, these are already globally available. They're not going to be available in node modules that bin, because this is only for a local installation. So we can remove this as well. And Netlify status. It's gonna also not have this entire part here with a directory. So a few things are now much more simpler. Let's go ahead and take a look if we missed anything. So we have here the image. We have changed the path. We have removed the installation part. And we can apply the exact same idea here to deploy prod. Again the same image. We no longer need to install Netlify CLI, and wherever we are using a tool, we're removing here the part with node modules. I think it looks good. Let's commit these changes and take a look at the pipeline. If we're taking a look at the pipeline, we're going to notice something interesting. And what I want you to notice is the execution time for deploy staging. So now we are at 34 seconds execution time. Previously we've been at one minute and 23 seconds. Now of course this varies, but I think we can easily say we have reduced the execution time by at least half from what we had. And even here for deploy prod. Okay. This was already like relatively fast 44 seconds, but still there are 15 seconds for our pipeline overall is faster now and we don't see any errors, which means that everything is okay.
