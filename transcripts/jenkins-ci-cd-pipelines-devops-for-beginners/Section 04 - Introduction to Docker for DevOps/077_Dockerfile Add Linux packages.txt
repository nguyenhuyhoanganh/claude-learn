# Dockerfile: Add Linux packages

Instead of running Docker containers as root within the Jenkins pipeline, let me show you how to install a Linux package such as jq in a custom Docker image that we can build ourselves. Going back to our project file, we are building here our own custom image and we are installing here node jq using npm install. I'm taking node jq only as an example here. Let's assume that we still want to use jq, but the docker image that we want to use doesn't have Node.js installed. At its core, no, GKE is a Linux package, so we can install it using a regular Linux package manager. We don't need npm to do that. We just need to figure out the Linux distribution that is being used. In our case, the Docker image that we're using has this tag here at the end, or this suffix here which is called jammy. And this is a known name for an ubuntu distribution. And this is the ubuntu distribution. So we know that we need to use APT as a package manager. So before or after we use npm install doesn't really matter. We're going to use another run command. In this case what we have to do is run apt update. And another command. It's going to be apt install. GQ. And we can also pass the yes flag. So if we're installing JQ here, we no longer need node jq. We can go ahead and remove that from there. And apart from this, inside our Jenkins file there are also a few changes that we need to do. So we should search for node jq. And wherever we have an occurrence of node JC, we need to replace it just with JC. So that seems to be the only place where we had no JC. Perfect. Let's commit these changes and see if it works. Remember that the Docker image is actually being built by this nightly build. And this doesn't listen to any changes that we make, so we have to manually trigger this. Let's take a look at the logs and see what's going on here. The first command was executed and this is the from command. This is where the second command is executed and everything is done in steps. So this is the npm install command. This will take a minute or two. And let's see what's happening next here. So the third step has been. Update without any issues. And finally we have installed JQ. And we don't see any issues here. So with that being said, all we have to do is trigger this pipeline once again if it's not already starting on its own, we just need to make sure that it's pulling the right image. And we're going to wait here for the deploy staging job to finish. And if this one is successful, it means that we have successfully replaced GQ. All right, so this seems to be working. There's just one additional thing I want to do inside this Docker file. And whenever we're using run commands here or run instructions within the Docker file. If we rebuild this image, the different layers of the image will be reused. And this command in itself is not a good idea to have it alone on a single run instruction, because Docker will cache it. And if we try to install something later, that package may not be available. So for that reason, what we want to do is to combine this on a single line in Linux, if we're trying to run two commands, one after the other. All we have to do is write here. App update. And then comes to ampersand signs. And then we can just add the next command. And this will be always part of the same execution. And it will always be executed. Whenever we're making changes here this will be executed. So we're always running up. Update that's in a nutshell what I wanted to say. But this is how we can install Linux packages inside our custom Docker image. And then inside our Jenkins file. We don't need to worry about starting that container with root. We can add here any packages, any dependencies, anything we need, we speed up the pipeline execution and we can build this image separately and in this way win a lot of time.
