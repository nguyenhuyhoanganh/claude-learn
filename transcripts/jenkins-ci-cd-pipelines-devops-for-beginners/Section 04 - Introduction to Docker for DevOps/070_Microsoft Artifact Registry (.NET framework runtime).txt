# Microsoft Artifact Registry (.NET framework runtime)

How about if you are a Microsoft fan? Where can we find, for example, a dotnet image? Well, let's take a look and search here for dotnet. And if we search for official Docker image. We're going to see here an open source implementation. But this doesn't seem to be from Microsoft. Okay. Let's search for dotnet. Official Docker image. No result. So what's going on here? Are there no Docker images for dotnet? Yes they are, but they are a bit hidden. So I'm gonna link here this page as a resource to this lecture. And this is the official microsoft.net. So dotnet is indeed available on Docker Hub. Kind of like available. It doesn't really work like the rest of the images because Microsoft has its own container registry. So if we go to MCR dot microsoft.com, you're going to see here the Microsoft Artifact Registry. And inside this registry this is Microsoft Primary Registry for all published artifacts. And this also includes container images. And for example for dotnet there can be images for various needs such as development environments or runtime environments and frameworks and so on. So you're going to see here all these images here. They're also documented inside here. So you can see here for example That.net runtime and some additional information. But these are actually not hosted on Docker Hub. They are hosted on the Microsoft Artifact Registry. And how do we know this? That's super easy because what we'll see here in every image that we want to use, we need to specify this full path. You're going to see here mcr dot microsoft.com. And then it starts dotnet runtime and something like that. So the best information comes from the Microsoft Registry itself. This is where we're going to find the tags. But most importantly this is where the about section is up to date. So we're going to find here pretty much the same information as in Docker Hub. But you're going to understand better how to use these images. Which variants are available, how do the tags work and so on without probably noticing. We have already used the Microsoft Container Registry. So if we go, for example, to the end to end stage. Or using playwright. This is actually published on the Microsoft Container Registry, and everything that is not on Docker Hub needs to have this fully qualified path telling Docker from where to get this image. So this is why you're going to see the difference between node and version 18, which doesn't need a fully qualified path and something that comes from a different public registry, such as the Microsoft Artifact Registry. So hope this information helps, especially if you are developing software using Microsoft technologies. This is the place where you want to look for things that Microsoft publishes.
