# Choosing a Docker image (Node.js, Python, Java, PHP)

Let's address a common question why do we need to use Node.js in our Jenkins pipeline? And what exactly does node column 18 alpine mean? This is what we will figure out in this lecture and understand where to look for these Docker images. In a Jenkins stage. We need to choose a Docker image that already has the necessary technology installed for our project. This is crucial for ensuring that our Jenkins environment is ready to handle our specific project requirements without additional setup. In order to select the right Docker image to use, we need to know what kind of a project we have and which version of a specific programming language it needs. In this case, our project uses Node.js. Or if we don't know exactly which image to use, the first stop should be Docker Hub. Docker hub is the default public container registry. This is where most public containers are published, and we can find here a wide range of Docker images. So if our project uses NodeJS, it makes sense to search Docker Hub for an official NodeJS image. So let's go ahead and type here node. And we can even enable here a filter which says official image. So this will ensure that we don't get any images that are not official. And we're going to go ahead and click here on the first result which is node. And this is the official NodeJS image and this is already a very good starting point. Now in the beginning, the information that you see here can be really overwhelming because there are so many things and you can see here all these tags and you don't really know what to do. So here's my first tip. If you want to use a specific image in this case node, skip over this first part with all the tags, which is not really so important, and then go here more towards the end of the documentation where it says how to use the image. And it is explaining here the basic syntax. And it's telling you, for example, that node comes in many flavors, meaning there are different kinds of images that contain node and you may have different use cases. For our project, we need node inversion 18 because the project has been built when version 18 was around. So for that reason, it doesn't make sense to get the latest version of node, which can be maybe 22 or something higher. We need a specific version. And in this case, what this syntax is explaining us is that, hey, if we want to use node in version 18, we should write node column 18. Everything that comes after the column in Docker is considered a tag. It is a way to identify a specific image. So let's go all the way up. And as you can see here, right on top we have tags. So if we click here on the tags. We can search here for 18. And even when we're searching for 18, there's still a lot of results that we're getting. But we're going to be there in a second. So let's say we want to get. Note 18 and these are different versions and different texts. So I'm going to scroll all the way down until we can find only 18. So this is the tag node 18. So if we write in our pipeline node column 18 this is what we're going to get. Let's click on this. And if you want to use it, all we have to do is copy this part here and then put it, for example, in our image definition here. So instead of this we could write your node column 18. But what is this part here with Dash alpine? What does that mean? Well, here's the thing. This image, as it is right now, has a very significant site. So it's almost 400MB. And this means that when we're starting our pipeline, we need to download 400MB almost before we can do anything. And that's quite a lot. So I'm going to go back to what we had earlier. And this was information about node images in general. And so if you scroll again down to the documentation here, you're going to see here also this Dash alpine. So we can use node column 18 Dash alpine. And what this means is that this image is using the Alpine Linux project, which is a Linux distribution which is very, very small in size and is ideal for containers. So just to show you how small in size this is. So remember the previous one was almost 400MB. And now we're going to go ahead and search here for 18 Dash alpine. And let's crawl a bit further down. This is the image. And you're going to see here the size of this image is 42MB. So that's quite a huge difference. So our pipeline can start much faster. This is why you're going to find relatively large images, which have a lot of tools which you may not need. Also smaller Linux distributions like Alpine or Slim which have a much smaller footprint. This is what it means. Essentially you'll see here no column 18 Dash alpine. And this is what we are using. Also in the pipeline, everything that is after the column is considered a tag and is a way of identifying that particular image. But what if you are using a different programming language? Most programming languages or runtime environments like Node.js have official Docker images available. So let me give you just a few examples. We're going to go back here to the start page of Docker Hub. And let's say that you are a Python developer, so all you have to do is search for Python. And you're going to find here a Python image you're going to see right on top here Docker official image. You typically want to work with official images. You don't want something that comes from a third party. And the same idea applies here to Python as well. So we're going to go here over the documentation. We're going to scroll over this part with all the tags and everything, which is I don't know why this is right on top. And here you're going to get some explanations. So really if you spend two minutes looking at this Readme page explaining you what this image is all about and maybe how you can use it as well, that can help you a lot. So you're going to see here for Python you have image variants. For example, if you're using Python three, all you have to do here is probably use three Python column three. Or if you're looking for a smaller version, you can use here Dash slim or Dash alpine. And just to be sure, go here to the tags right on top and search for that specific tab. So let's see if we can find here three Dash alpine. So there are a lot of options. And we can use here three Dash alpine. And we can use Python in version three. If we care exactly about a specific version of Python, we can do that as well. This is why there are so many tags, because sometimes we have a specific tag like three point 10.13. Or we can have a tag like three dash alpine, which means that whatever is the latest version in three, this is what we're going to use. So these images are getting updated when a new version is being released. So it depends on if you really care on a specific version or you just want the major version to be there. Same idea applies, for example, for Java. So if you're a Java developer and you want to use a Docker image for development or in your pipeline. What you will need to do is to search for open JDK. Again, this is an official Docker image. And for OpenJDK, you're going to get here in the beginning, this warning. So this image is being depreciated, but they are here some recommendations in terms of finding a proper image for you. So all you have to do is go a bit through this and understand what's going on. For a long time, open JDK has been the way to go in terms of non-production builds when you need it to use Java, but this is now being deprecated. So look at this alternative options that you see here. Similar idea goes also for PHP. So we can search here for PHP and go directly to the first result. Again we have an official Docker image. And again we have here the overview. Want to go here a bit further down to understand how are these images tagged and how we can use them? We're going to see here image variants one, for example, only provides you the php CLI but doesn't open a web server. So if you need a web server, you also need to add here Apache as well. Practically any programming language or major framework has its own Docker image. Official Docker image that you can use. It just requires you to go to Docker Hub, search for that image, identify that it is an official image, and take two minutes to go through the documentation. Just spending those two minutes going through the documentation patiently. It will save you so much time on the long run. So this is the major tip I want you to get from this lecture. Invest that time reading the documentation.
