# Installing Linux packages in Docker containers

In this lecture, we are going to figure out how we can install additional packages, additional tools or anything that we might need without relying on NodeJS. And I'm just going to use here Python as an example. So we're going to start here a pipeline which is just for learning purposes. So we're going to use the learning pipeline here. So let's click on this configuration see what we need and see what we don't need. In this case we are only going to use one stage. Let me also make this a bit bigger. And what we're going to do here is going to call this simply build, and we're going to remove the script part. So inside this what we're going to have here is the agent definition. And the agent is going to be Docker. Image. The image that we're going to use is going to be Python in version three, and we're going to use the alpine distribution. So typically on an alpine distribution, all we need to do is to have a shell command. And let's say that we need also to parse JSON. So for that we're going to need JQ. But we don't want to use node to install it. And even if we try to use npm install node, that's not going to work. So let me first demonstrate that I'm going to try here npm install node jq. So we have a Python image. And save this configuration. Let's run the pipeline. It's going to take a second or two for this new image to be downloaded. And then if we're taking a look at the logs, we're going to see here we have used this particular image. It has been pulled, it has been started. And now we have the command npm install node dash dash jq. And the error that we're getting here is npm not found. So npm as a command is not installed on this image. So we cannot use npm install. And to install NodeJS it will make absolutely no sense. So the way that we typically go around this is to install jq as a Linux package. And for such an image which is using alpine, we're going to use here the alpine installer, which is A p, k and jq. So this is all we need in order to install JQ on this and I click here on Save Build now. And it's not a problem. We're going to get an additional error. And this error is related to the installation process. You will see here the last command is trying to install JQ. It's getting a permission denied. This permission denied is happening because the user that is starting this container doesn't have the permission to install anything on that particular container because it is not a root user. And we can change that in Jenkins is not a problem and we need to pass an additional configuration. However, I want you to be very, very careful with this configuration because it is very powerful and can do a few things a bit differently. So all you need to do is pass here args. And the arcs that we're going to pass is Dash U, which stands for user. Truth. We're going to start this container as a root user. This is all that this does. And then as a root user we'll be able to install JQ. Let's take a look at the logs. And you will see here finished success. So this time this command has worked without any issues because now we're running this container as a user. However, keep in mind that if you are reusing the workspace and are doing something to that workspace from the container. So if the workspace is shared between multiple containers and you're running this container as root, any file that you change will have the root as an owner. And later on, Jenkins is going to have issues deleting those files because it's not going to have the right permission. So be very careful with this approach. This is not my favorite approach of doing things, but I just wanted to point it out so that you know that it exists. Next, we're going to take a look at how we can install JQ on a different Linux distribution. And we're going to use here ubuntu, because it is quite a very popular distribution and I'm pretty sure you're going to encounter it. So instead of this Python image which is using alpine, we're going to write here ubuntu. And a version of ubuntu that we will be using is called noble. We're going to use the Jenkins tech nobly here. So let's go ahead and run this, and I will warn you in advance. We are going to get a bunch of errors, which we're going to try to understand and fix step by step. So just stick with me for one second. Let's save and run this. Let's take a look at the logs and understand what has happened. And we're taking a look here. The command that we tried executing was. This one for installing GQ and we're getting the error APK not found. And this is expected because ubuntu is using a different package manager which is called apt apt. So we need to use the proper package manager for the respective Linux distribution. Otherwise we will not be able to install any other packages. So let's make that change to our pipeline. So here inside I'm not going to use this tool because it doesn't exist. So we're going to write here app. And the command is going to be installed. Okay, let's see again if this works any better. Doesn't work. You've been warned. So let's understand what's going on here. And it's important to see this particular error unable to locate package JQ. Now there can be two reasons for this. Either this package doesn't exist and it's typically not there because you have misspelled it. Not the case here. Or there is no up to date information about the existing packages. And we need to essentially update the repository information. And for that we need to run an additional command. And this command is app update. So we first need to update. And then we can install additional packages. So I'm going to just write here an additional shell command. And let's give it a try with this one. So we're going to see here app update in action. So it's checking different places for package information. And now we can see that our pipeline is still failing. We see that we are passed now the update. So the last command executed is apt install JQ. But what we see here at one point is this question. Do you want to continue, yes or no? And it's super important to understand that when we're automating commands with Jenkins. Whenever there's a question or a prompt, we cannot answer that prompt. So we need to run these commands in an automated way. And essentially here, if we would run this from a terminal, we would be asked, do we wish to continue installing this tool. And we would press Y on the keyboard and hit enter. It's not possible to do this from Jenkins. So we need to pass here an additional flag in the configuration. And this will look something like this. So it's going to be apt. Install jq and somewhere here we can pass here the flag dash y. And this says essentially if there are any questions just answer with yes, super simple flag, but makes all the difference when installing such things in Jenkins. So we're going to see here the command that we have executed. And we're no longer getting the question. GQ is being installed. And finish successfully. So this is how you can install JQ on different Linux distributions. In a nutshell, you need to know what kind of Linux distribution Docker image has that you're using, and you use the appropriate package manager.
