# Assignment - Solution

So I feel I already told you a lot of the things that you need to do, but let's back up again and see exactly what is the problem and why. Can we not use this image directly? So if we want to use my playwright, my playwright does not have serve. So serve is missing now because serve is missing we need to install it. So let's go back to the docker file inside the docker file here we already have with a global flag netlify cli, node jq and space. We're gonna add here serve. So we're going to make serve also a global tool that we are installing with npm. So this is the first change that needs to be made. The second change is we need to use the right image. That's kind of like obvious. We're going to remove npm install from here because we have it now inside the image. So the image is doing that. And apart from this we don't need this path here. Serve is now installed globally. It's not going to be available there. So that's another quick change that needs to be made. Let's commit these changes and see if the pipeline is still running. So going back to the pipeline, what we can notice here is that the end to end tests that are running locally are successful. Look at this time here, 17 seconds compared to previously 27 or 28 seconds. So that's definitely an improvement. But. This time the build time of the Docker image was one minute. So we have saved 10s here, but we have added one minute. Building this image. This is only the first time. However, in essence we have now used my playwright image in multiple places. So wherever we had this playwright test, now we have my playwright, which is built according to our needs.
