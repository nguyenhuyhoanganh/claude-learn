# Updating the service using AWS CLI

We have managed to update the task definition. So the next logical step would be to also update the service. And for that we're going to use another AWS cli command part of AKS. We're going to see here we're still in EKS. And what we want to locate here is again the section with examples links to the documentation you will find as a resource to this lecture. Let's go ahead and click here on the examples. And we're going to see here pretty much what we need to do. So it's telling us that we need to use AWS ECS update service. We need to specify the name of the service and we need to specify the task definition. So this seems to be very simple and pretty much what we need. I'm going to go ahead and copy this as it is. And we're going to figure out the details as we start configuring this. So back to the Jenkins file after we have registered the new task definition. Let's paste this here and see what we need to do. Now first of all I just wanted to mention specifying the cluster is always a good idea. So if we're going back to the documentation here and scrolling all the way up, there's a lot of documentation, a lot of explanations here. But what we're pretty much interested in is this command here, dash dash cluster. Because this allows us to specify the cluster. So all we need to do here is provide a short name or full Amazon resource name. So we're just going to use the short name of the cluster. If you do not specify a cluster the default cluster is assumed. I don't know which one is the default cluster. So I think it's just much, much wiser to specify a cluster here instead of leaving this empty. So let's begin with the first thing, which is dash dash cluster. I'm going to go here into X. Click on clusters. So this is the name of the cluster that we'll be using. I'm going to go ahead and paste this information here. Make this slightly more bigger so that you can see better. So we have specified a cluster. Then we need to specify the name of the service that we're trying to update. Let's go inside the cluster and see the name of the service. This is our service name. So let's go ahead and simply copy the name of the service as well. Back to the Jenkins file. And instead of my HTTP service we're going to use our service name. Let's go more to the right. And then we have the task definition. So what we want to specify here is the name of the task definition. So let's go here to task definitions. This is the name of the task definition. Go back to the Jenkins file. So now we have specified the task definition. Now the question is how does this command know which version of or which revision of the task definition to use. Because we now have two different revisions. Will it use the latest one or not? This is something that we can find out by looking at the documentation, but actually it would be much, much smarter if we provide the exact version that we want to update. So I'm going to say here, hey, we want this task definition in version two. So we're going to again make a short update here. It's not going to change a lot. We're just going to add here at the end column two. So this is specifying the revision. So it looks like we have everything in place. Let's commit these changes and take a look at our pipeline see if it works. So fortunately we don't have any errors in our AWS stage. So let's take a look at the logs to see exactly what has happened here. We have registered the task definition. Now this task definition is in version three. And now we have used ECS update service. And we have updated the task definition in version two. Still we managed to use now the right container. So now if we're getting back all this JSON it's simply some technical information about what has happened. But if there is no error, we can conclude that this update process has started. Doesn't mean that the update process was completed. We're getting this information very, very fast. We're just sending it. It's pretty similar to using this through the UI in the sense that we are triggering something. Then we have to wait a few seconds or a minute until this is done. If we're taking a look here inside our cluster, we're going to see here that we have this information to from one tasks running. So why do we have two tasks running. Because one of them was started much much earlier. And this one where it says here like 47 seconds ago, this is where one minute ago, this is the latest task that we are now running. So we can even see here if we're taking a look at the information about the task we have here. Nginx in version 1.27. This is the image that we're using. We're clicking on the previous task. We're going to see here that we have nginx in version 1.26. So definitely a difference between this task. What we should see is that once this task is up and running, other tasks will pretty much disappear because we no longer need it. But at one point in time, there will be two tasks running because we don't want to shut off the website and then wait for one minute to see if everything is working properly. So we can click again here on the address that we're getting. Bear in mind that every update that you're doing to this task will lead to a different IP address. This is something that can be resolved with the load balancer, but it's outside the scope of this course. So a page looks exactly the same. But we have used a different Docker image. And we did everything here through two relatively simple CLI commands. So now we understood also the basics of doing this update only from Jenkins.
