# Building a custom AWS CLI Docker image

Waiting for Docker to install. Every time we're trying to build a Docker image is not really the most efficient use of our time, because you can see here we went from having an execution time of two seconds. This is like what it actually takes to build a Docker image. And now we are at 53 seconds, which is not so bad. But still I think we can get this time further down. And the strategy that we're going to deploy is again, by building a custom Docker image with everything that we need so that we don't have to install software while executing the pipeline. So going back to our project, let's see here what we can do. And we already have here this CI directory with a Docker file. So this is going to be the Docker file for playwright. So let's go ahead and rename this I'm going to write here docker file dash playwright. And also within this directory we are going to define another Docker file. And we're going to add here the suffix aws cli. And in this Docker image we're going to start again with from we're going to use here AWS cli. And the next instruction is going to be the run instruction. Because we want to install Docker. We are going to copy this from here. Essentially we practically don't need it anymore. So we can also cut it directly. So we have from this particular image we are running this command. And additionally we can also install JQ for something that we need later on. We're going to see here in the pipeline. At this point we're also installing JQ. Let's do that as well. I'm going to remove JQ from here. So that you can do everything in one step. Pretty much. Let's take a look at our Jenkins file nightly. And we're going to write here that this is for playwright. So in this stage we need to do a few changes. So we're going to say here build Docker images. And we can have two instructions. One of the instructions is going to be for AWS CLI. We can copy the relative path to our Docker file and can put it here. And we can call this my aws cli. This is going to be the new name of that image. And for playwright we're going to copy the relative path as well. And I'm going to replace what we have here. And this is going to be my playwright. Even if we don't have playwright anymore in our pipeline, it's still a good idea to show how we can build multiple Docker images for different use cases. So let's check again everything to make sure it is as we expect it to be. Now that we have this configuration, let's copy my aws cli and wherever we have aws cli, we're going to replace this with our new image which currently doesn't exist. So we need to trigger that build. Otherwise initially the pipeline is not going to find the images. But apart from this let's see what we have. So we have the docker file for aws cli. We have the Jenkins file nightly. So this is something that we need to execute. Does look good initially. And we still have our good old Jenkins file. Perfect. I'm going to go ahead and commit these changes. And we'll see you in Jenkins. So in Jenkins, what we want to do is to open the nightly build for the Docker image. And we'll need to trigger this manually once to be able to build the Docker images. should only take a few seconds, and would be a good idea to take a look at the logs to see that everything has been indeed successful. So this is the playwright image and this is the aws cli image. So these images have now been created. Let's switch to our project. So within our project we can wait for the pipeline to trigger if the pipeline has already been triggered and the images were not found, we would initially get an error. That's absolutely normal. But right after this, these images should work without any issues. So I'm just going to wait a minute or two for the build to complete, and we're going to come back and see if we gained anything in terms of time. So after a few seconds, if we're taking a look at the pipeline, what we can see that the building, the Docker image only took five seconds. And we have reduced this from 53 seconds to initially we had two seconds, but now we're starting a Docker container, so there's a bit of an overhead, but still five seconds. That's pretty pretty good. Now there's still one thing in the Jenkins file that I'm not 100% sure we still need, and I think the best way is to simply try it out. And I'm referring here to this route. So we're still starting this container as a route. And as I said, I'm not sure this is necessary. And the same is here where we're deploying to AWS. We're starting this container as root. And I'm just wondering is this really necessary? Let's give it a try. I'm going to remove here root. And we're going to see if that's important. We're going to keep this configuration and the entry point super important. Let's remove this space here as well. And I'm going to remove here the root just as well from here. And we're going to keep only the entry point. There's no other route that you can see. So I'm going to commit the changes. Let the pipeline run one final time. And I'm going to see if root was necessary or not. We're going to see that the build Docker image stage has failed. We can take a look at the logs and expand this. And what we're going to see in a nutshell is that we're getting here a permission denied because of this Docker sock file. So obviously for this file we need the sudo. So that's definitely something we need to get back inside our pipeline. So I'm going to right here inside the build Docker file dash u root. So we're going to add I'm going to add here the root user I'm going to keep this change as it is. But I'm pretty sure that deploy to AWS can do well without actually being root. So I'm going to keep that as it is. And we're going to see again. So just one more try to get this pipeline to run. So if we're taking one last look at the pipeline you're going to see that the build Docker image stage has been successful. And I'll also the deploy to stage is probably already waiting for the deployment to happen. We can take a look here at the logs, but there's no indication that something has failed at this point. So if the last command is yes, it's x. Wait, so it's waiting. And this is a good indication that everything has worked and will work without any issues here, at least not because of the root user. It's always a good idea to experiment with your pipeline to remove things if you think you no longer need them, and if something doesn't work anymore, then put them back. It would be stupid to keep something in the pipeline and not know if you really need it or not. So it does take a bit of time to experiment with things, but I think that's the best way to learn. And thank you for doing this with me in this lecture. Even if it went a bit longer than it should be, I want to make sure that you understand exactly what we're putting into this pipeline
