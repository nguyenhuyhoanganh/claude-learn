# Building the Docker image

Now that we have a Docker file, let's go ahead and build our Docker image. So let's open up the Jenkins file. And we need to shift around things because first we need to build. So this stage we're deploying to AWS needs to be definitely after the build. So I'm going to move it here. So let's say after building the project we want to build the Docker image. So what we're going to do is simply create another stage. Now we already have this Jenkins file nightly which contains everything that we need in order to build a Docker image. So we can copy that already put it here in place. We no longer need to specify which file this is. So it's automatically going to look for the docker file within the root of the project. I'm going to change here the name. So let's go for the name my Jenkins app So that is going to be the tag that we're going to use for our image. And don't forget this dot here at the end. This represents the build context essentially because in our image we are making a reference to the build directory. It's super important that we specify the right context. Otherwise it's not going to be able to find this. So I'm going to commit the changes. We'll run the pipeline and see if the build is successful. By taking a look at the pipeline. We're going to notice that building the Docker image has been successful. Only took two seconds. So that's great. Let's take a look at the logs to see what has happened. So this is the command that we have executed. And pretty much this has been done in two stages. One of the stages meant that we have pulled this Docker image. And in the second stage, we have copied our files into a specific directory within the image. So that's about it. It's a pretty simple process. However, we're not done with this. We will need to also push this Docker image later on to AWS, and we need a configuration from which we are able to run AWS CLI. So this will definitely complicate things a bit. And this is what we're going to try to do in the next part. Now take a look here at the deploy to AWS. I'm going to copy here this agent configuration. I'm going to paste it right here below. A few important things to note here. We definitely need reuse node. Not sure at this point if we need this route, but we're going to see about it in a second. I'm going to leave it there. And we're also overriding the entry point. I'm going to build this once again, but I'm pretty sure this is not going to work this time when we're using this Docker image. Let me show you. So let's take a look at the build Docker image job and understand why it failed. Obviously we have here only one command. And what does it say? It says docker command not found. So why is Docker not found? Well Docker is not found because the image that we have now used here aws cli does not have docker installed. So in order to use Docker with this image we need to install Docker. When using Amazon Linux, the easiest way to install some software like for example Docker is through the Amazon Linux Extras command, which is specifically designed to manage some Amazon Linux packages. And that's just the best and easiest way to install a software like Docker. So let's go ahead and do right that. Now we're going to use this as the first command. And we can write here SSH on multiple lines. And we can also move this from here a little bit of indentation doesn't hurt. Just to keep things clean. So what we're going to write here is Amazon Dash Linux dash extras install Docker. Right. So that should be it. I'm going to commit these changes. Run the pipeline again and see if it works. So unfortunately our pipeline has failed again. And the question is why did it. Because now we should have Docker installed. Let's take a look at the logs. What has happened here. There's a lot of logs but initially there's no error. So at least the last command should have worked without any issues. We don't see any errors here at the end. And the last command executed is this one docker build. And then we're getting the error. Cannot connect to docker daemon at Unix var run sock is the Docker daemon running. So what is this error all about? Well, what you need to know about Docker is that Docker is made out of two components. There's the Docker client which is the Docker command that we have just installed. And there's also the Docker daemon. This AWS cli image has now the Docker client but does not have the Docker daemon. The Docker daemon is the one that actually does the heavy work. The client is just the one who's taking commands from us. Obviously I'm oversimplifying. Now, our Jenkins installation, as we've seen in the beginning, if we're not in a Docker context, has the possibility of building Docker images so that availability that Docker daemon is available. We just need to create a mapping between our client and the installation of Docker that's available. And I know this may sound a bit technically complicated, but there's a special kind of file which is called Docker dot sock, and this is known as a Unix socket, and it is used for the communication between different software components and to be more exact, in the context of Docker. This socket file, this Docker dot sock, is used for the communication between the Docker daemon and the Docker client. So we need to map this in our configuration. Sounds a bit more complicated than it is actually. I'm going to show you that it's not so easy. I just wanted to briefly explain, like what we're doing and to go through all these errors so that when you're encountering one of these errors later on, you understand a bit better what's going on. So right here after route we're going to right here dash V. And this allows us to map different files or directories. And the way that we're going to map this is we're going to write something like forward slash var run docker dot sock. And we're going to map it to Docker Hub. So essentially we're creating a link between. Our host from where we're running this container and the inside of our container. This is all we need to do. I'm going to add here an additional space. So this is what we have added to our configuration. I'm going to check once again to make sure there are no mistakes. So forward slash VAR. Forward slash run forward slash Docker dot sock. Then we have a column here separating the two. And on the other side it's going to be VAR slash run. Forward slash Docker dot sock. So from that perspective it does look good. So I'm going to commit again the changes. I'm going to take a look at the pipeline. See if it works this time. And finally victory. You can see here now our job is successful. Let's also take a look at the logs to see what exactly happened here? Well, this is the part of the installation where we're installing the Docker client, and then we are successfully executing here Docker build. And these are the two build steps that are occurring. So this is the first step and this is the second step. And yeah the image has been built. There are no errors. So the configuration that we did is now working. So just to recap in a nutshell to be able to use here a Docker image which we will need later on, we needed to install the Docker client. And we also needed to map this Docker sock to our host, essentially to be able to facilitate the communication between the client and the Docker daemon, which is actually building the image. This is why this connection is so important. The configuration is working. So yeah, we did it. Let's move on
