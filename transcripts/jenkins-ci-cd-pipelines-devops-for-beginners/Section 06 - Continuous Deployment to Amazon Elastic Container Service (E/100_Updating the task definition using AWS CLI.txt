# Updating the task definition using AWS CLI

Now we have a cluster and inside it a Docker container is running. But what if we need to deploy a new version of the container? Do we need to create another cluster and go again through the entire process? No. Let me explain. Continuing with our example, if we wish to deploy a different version of nginx, all we need to do is to create a new version of the task definition. and then update the service. So let me briefly explain what needs to be done to find our task definition. We're going to go inside task definitions. And here we're going to find our task definition. It's the only one we're going to go ahead and select it. And what we want to do is to create a new revision. It's possible to create that revision using the UI or simply using JSON. Just going to select here the UI, just to give you a different perspective on how this looks like. And if we scroll here a bit further down we're going to find information about the container. So essentially in this part this is where we need to make the change. Now change in this case would be pretty obvious. So we're not going to do this through the UI. Instead we're going to go back to the cluster. And let's assume that we have now updated the task definition. All we need to do is go inside the cluster, identify the service. So this is the service that we have. And then we need to update the service. And here inside the service what we need to do is to select the task definition. This will remain the same. And the last part here the revision. This is what we are actually updating. So once we have created another revision this will be number two. All we have to do is select it here from the list. I think it will be very boring to do this over the UI. So since our goal is to automate things with Jenkins, I think we should go back to Jenkins and try to do this update process directly through Jenkins. So if you're opening up the project a bit, let's first open up the Jenkins file. And for X, I just wanted to show you a very simplified pipeline. So we're not going to go through all the testing and everything that we had in the past. So let me explain what I mean by that. I'm going to keep here the AWS state, because that is still kind of be super useful for us. But for example, the test stage here, this is something that ideally we should get rid of. So I'm just going to collapse this. And in the collapse mode I can delete the entire thing as same would go here with deploying staging deploying production. So everything that is still inside the pipeline will just go away. So now we only gonna have here the build, which is something that we want to keep. We can also remove here everything with Netlify. Perfect. So let's call this deploy to AWS so that we know exactly what we're doing here. All right. If you remember in the beginning, we have saved our task definition here in the AWS folder. So I'm going to expand this. And what we're going to see here is the task definition. Actually we should rename this into task definition prod just for consistency. Doesn't really matter how it is named, but since we have named the family also task definition prod, we're gonna rename this as well. Perfect. So this is the task definition that we're starting with. And what I want to point out pretty similar to what we did in the UI is the fact that we want to update here the image. And generally speaking, what we want to update is the tag. So currently we are at version 1.26. So let's go ahead and make an update to the next version 1.27. Let's consider this is the new version that we want to deploy. Perfect. Now for now I'm going to go ahead and close this task definition. We don't need it at this point. And going back here to AWS, there are a few things that we need to take into account. And the question is how can we update, first of all the task definition because we know we need to update the task definition and then we need to update the service. So how do we do that. Again using the aws cli I'm going to link here this documentation page. We need to use part of ECS. The register task definition command and these commands. They are not simple. So if you're taking a look here at all the possible parameters that exist here, you can already be super confused about what needs to be done. Trust me, things are much, much more simple. So what I want you to do first is to identify here in the list the examples. So whenever we're trying to use a new command, it's always a good idea to take a look at the examples. Typically the examples are much much easier to understand. So in this case we're giving here an example with to register a task definition with a JSON file. So we have a JSON file. Well what do you know. And all we need to do is pretty much run this command AWS x register task definition. And then with this additional flag cli dash input dash JSON, we are referencing a file. This is what we already have pretty much. So let's go ahead and try to implement this in our Jenkins file as well. I'm going to begin here with only the first part of the command. Don't let this backward slash fool you. This is just to format the command on multiple lines. But we're going to put it on a single line. So I'm going to copy this first. And what do we have here inside deploy to AWS. We're still going to use the AWS CLI image. We have here an entry point. We're reusing the node. Currently we don't have any environment variables. So probably I'm going to go ahead and just remove this environment variable that's hanging around here. We still want to use our credentials because we want to log into AWS. Now let's start running here a new command. So this is going to be AWS X Register task definition and add a space here. Going back to the documentation. And I'm going to copy here the next part. And this is going to be the part where we're referencing the JSON file. And just going to append it here. So it's just going to be one small space here. Adding another space. Let's click here on this file name. And we're going to copy here the relative path. As you can see I'm always trying to copy paste as much as possible because this helps us a lot. Now, in front of this, if we're comparing again what we have here in the documentation, I'm going to see here that we also have this part here with file column forward slash forward slash. We still need to keep that part. So I'm going to add this here in the beginning. All right. There's one more thing we still have here this build stage. And initially we're not going to need the build stage. So I'm just going to do us a favor. Cut this entire configuration and move it here before the build that we're going to execute this as soon as our pipeline starts just doing a bit of formatting to keep everything nice and clean. All right. I think we have now everything that we need going to commit these changes. And we're going to take a look at our pipeline to see if it works or not. Spoiler alert it's not going to work. The first issue that we're getting, we're taking a look here at logs, is the fact that we have not specified any environment variables. So we have here an empty environment block. Apparently Jenkins doesn't like that and was hoping to use it for later, but we're going to have to remove it for now and give it another run. We're not getting any other syntax errors at this point, but let's take a look at why this stage has failed. So why do the logs say I'm going to expand here the last command I'm going to see here AWS version. This is the last command that we have executed. We're getting here the error. You must specify a region. So this is regarding the AWS region that we're using. For example here going into AWS, we're going to see here that my account here is in North Virginia. And this is the code of this region. So this is something that we also need to specify for some services when we're trying to use them in AWS. And this is done by using a specific variable. The name of the variable needs to be exactly as I'm writing it here. So it needs to be AWS underscore default underscore region. And then what I'm going to put inside here is US east one. So this is what this command is expecting. Let's run this again. So let's take a second look at why the pipeline is failing now. And I'm trying to get through as many potential errors as possible, because this is what you're also going to experience when you're using the CLI tools. In the beginning you're going to get a lot of errors. So it's important just to have the patience to go over the errors, understand what has happened. So now as you're noticing, we're no longer getting the error about not specifying a region. So it means that setting that we have provided has worked. However, we're getting a different error. So just again the patience. What does it say here? An error occurred. Access denied. Exception. So when we see here access denied, we need to think about credentials and what we're allowed to do and so on. So when did this access denied exception occurred? When calling the register task definition operation Okay. So we try to register a new task. Obviously this is what we're trying to doing and saying us here that this user, Jenkins, that we have configured in Yum is not authorized to perform a specific action on this specific resource. So it's very technical, but essentially it's telling us this user that we're trying to use is not authorized to do this thing. So there's nothing wrong with our configuration. What is actually wrong here is something with the permissions. So if we're going back into AWS let's go ahead and search for em. We're going to see here our users. Only one user is the Jenkins user that we've been using in the pipeline. Let's take a look here at what is this user allowed to do. And currently this user only has access to Amazon S3. So it's not surprising that if we're trying to use a different AWS service, that we don't actually have access to it. So what you will need to do here is click on Add Permissions and let's attach a policy directly. And the name of the policy will be something with x. So what we will select here from the list is Amazon X Full Access. We are probably attaching here a policy that allows a bit more than what is needed to register a task. But again our focus is on automating things with Jenkins and not a deep dive into AWS policies. So now if we're taking a second look at the policies, we're going to see that for our Jenkins user, this ECS policy allowing full access has been added. So all we need to do is go back to our pipeline. We don't need to make any other changes. We just need to run this pipeline once again. So I'm going to click here on Build Now. And we can even follow along here in the console to see what is happening. And that was a very quick execution here. So we can already see here this command has been executed. We have registered a new task definition with this information and we're not getting back any errors. So all we need to do is go back to X open here. Task definitions. Click on our task definition. And we're going to see here that we now have two versions. This is the previous version that we had that we have created manually. This is the second version that we have which we have created with the help of automation. With AWS, CLI and Jenkins. We can take a look inside it to see the configuration. There's also even here the option of clicking on the JSON so that we can see exactly what we have inside. And what we will notice here is nginx in version 1.27. So now we have managed to do the first step. If we're trying to deploy a new version of a container. And that step is to update the task definition.
