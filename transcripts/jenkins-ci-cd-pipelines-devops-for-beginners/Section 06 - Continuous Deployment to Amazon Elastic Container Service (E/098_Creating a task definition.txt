# Creating a task definition

As I was saying earlier, the cluster is not doing anything as we did not provide any instructions. And in order to provide an instruction, we need to create a task definition. So from this left side menu we're going to identify here in the list task definitions. And we're going to create together a task definition. Currently there are no task definitions. And what we're going to do next is click on this button here. And from the list that appears we're going to select Create New Task Definition with JSON for JSON in this case is used to configure these tasks. And we're going to need this later on for automation purposes. So there's also a user interface that AWS provides us with where we could do all these things, but we want to work directly with the JSON because we want to better understand what we're doing here. So first of all, what we're going to see here in the list is this task definition is compatible with Fargate. This is why it says here Fargate in the list. Then we need to define a family. You'll see here if you're hovering over different properties of this JSON, you're also going to get here additional information. So just in case you don't know what something is, you can hover over this in the interface. And it's going to tell you what this is all about. So we need to specify essentially a name for this task definition. And I'm going to call it learn Jenkins app dash task definition Dash product. Probably the most important part is here under container definitions, because this is where we're going to define which containers we want to run. And at this point, we only want to run one single container. So we can go for example to Docker Hub. And we're going to see here for example in tags we're going to run a specific version of nginx. So we're going to right here together 1.26 Dash alpine. We're taking a look here within the list we're going to find this particular tag. This is what we want to use. So what we're going to do next is simply copy this exact name as it appears here, trying to use copy paste as much as possible so that we don't make any silly mistakes. Go back to the task definition and we're going to specify here the image. So this is going to be nginx. We should also give this a name. And I'm just going to call it Learn Jenkins app for now. So the name of the container is Learn Jenkins app. But as a placeholder we're going to use this nginx image, which is a server perfect. In order to access this container we also need to provide some network configuration in a nutshell. So what we're going to do next is we're going to go to this line with the image right at the end after the comma, and we're going to hit here, enter. And there's a bit of a configuration that we need to enter here. I'm going to also attach this JSON file as a resource to this lecture so that you can easily copy it. But I'm going to write it step by step and briefly explain what we're doing here. So what we're trying to do is to do what is called a port mapping. Want to map the port of the container with a host port. So we're going to write here the configuration or the property port mappings. It's very important how we write this. We're going to see here as I start typing AWS already knows what we're trying to do. So it's offering us the suggestion here the autocomplete suggestion port mappings. It's also providing us with a description. It's a bit long but you know it is what it is. So this will create here a list. So within this square brackets I'm going to create here an object. So we're going to have this curly braces. And I'm going to hit enter here a few times. So let's see once here and here we can start writing our properties. So first we're going to start with a property called name. So that's the name of the port mapping. We're just going to call it nginx dash 80 dash tcp. And because we're adding a new property we're going to add here a comma. And what we're going to specify next is the container port which is 80. Then we're going to also specify the host port which is also 80. The protocol that we're using is going to be TCP. See, the interface is pretty smart. So it's making our life quite, quite easy. And the app protocol. This is going to be HTTP. I'm going to select here HTTP from the list. So now we have defined the image that we want to use. And we also ensured that this image will be accessible by defining this port. Perfect. Now there's still something we need to do. And this is regarding pretty much like the consumption of this particular image that you're running here. You're going to see here that there are already some default values. And they are pretty high, meaning that this is going to drive our cost way, way up. So you want to go with the lowest specification possible that we can define here. You're going to find here in the resources folder a table. And in this table we're going to see exactly which CPU values we can choose and which memory values we can choose. So pretty much for the CPU value we can decide here to use 255 which is point 25 virtual CPUs. We can use any of these writings, so you can use whatever it makes more sense to you. Just going to use here 256. That's super important to put it here in the CPU. I'm going to remove whatever it's inside here I'm going to use two five, six. And next one is the memory currently is at three gigabytes. But if we're taking a look here at the table, you're going to see here that we have different options. So with this CPU value we can also go as low as 512MB. So I'm going to copy that and put it here inside those three gigabytes. Now I'm going to go ahead and select this entire JSON as it is right now. And I highly recommend that you do the same. I'm going to copy this and save it inside a file. Let's go back to our git repository. And what we'll do here is to create a new directory. Let's call this AWS. I'm going to have this AWS directory here. We are inside it. And we're going to create a new file. And let's call this file task definition dot JSON. And we're going to paste here the task definition that we have from AWS. Perfect. So now we have saved this configuration. Let's go ahead and click here on create. We're going to get here an error saying that memory is invalid for this task. So let's see how we can solve this. What we can do here is to simply remove here the unit of measure. And we're going to check later on if the creation has been successful. So let's go ahead and click once again on Create All. It's trying to do our best to understand exactly what's happening. And we're going to get here some information. So we are now want to pay attention to where we are. So we are here inside task definitions. And this is the task that we have created. This is the first revision of the task you're going to see here revision one. And apart from this, what you are going to see here is the task size. So this is where we can confirm again that our CPU is a quarter of a virtual CPU, and that we're using 0.5GB of memory for this. So it means that our configuration has been correct.
