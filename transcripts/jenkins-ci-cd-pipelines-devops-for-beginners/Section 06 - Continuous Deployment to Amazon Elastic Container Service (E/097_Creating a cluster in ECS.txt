# Creating a cluster in ECS

Let's start using X with a very simple example so that we understand the basics. To simplify things, we will deploy a Docker container containing a very popular server called nginx. I know this website doesn't look very modern, but trust me, nginx is a very, very popular web server and often is used as a reverse proxy server. But that's a totally separate topic. Just get this. Nginx is a server super popular, and we're going to consider this to be our application. And we're looking here at Docker Hub. We're going to see here that nginx is also available on Docker Hub. So we already have a container that we can deploy. And this is simplifying things just to get started with ECS. Now back to the ECS interface. The first thing that we want to do is to create the cluster. I'm going to click on Create Cluster and we should provide here a cluster name. Now feel free to name your cluster as you wish. And I'm going to name the cluster learn Jenkins app dash cluster dash prod just to make it a realistic cluster name where we are separating between maybe a production environment and a testing environment. Now if If we scrolling further down, what we want to ensure that we're using is AWS Fargate and not EC2. This is selected by default, but just make sure that you are selecting here AWS Fargate. And then of course more settings that we can do at this point. But this is all we need to do. We need to define a name for our cluster and we need to define the infrastructure. So next we're going to click here on create. And that's it. As you can see this is a very simple process to begin with. So you're going to see here right at the top that the cluster is being created. So what we should see in a second is that the cluster has been successfully created. And here in the list of clusters this is our cluster. But currently we're taking a look here at what the cluster is seeing is essentially there are no tasks running. There's nothing happening. Right. So this cluster is not doing anything at this point. And because we have no tasks running, it's also not costing us anything to have it. But of course we want to change that. Now we could go ahead and also create a cluster for testing purposes, Generally speaking, using the same ECS cluster for both testing and production environments is generally not recommended. This is why we'll consider this cluster to be our production cluster. And if we need also testing environment and we should use a testing environment, we should also create a cluster for the testing environment. But that's totally outside of this. Just as a side note.
