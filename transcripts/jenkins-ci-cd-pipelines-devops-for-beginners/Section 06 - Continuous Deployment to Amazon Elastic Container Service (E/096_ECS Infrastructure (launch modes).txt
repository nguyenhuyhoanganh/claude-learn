# ECS Infrastructure (launch modes)

So let's search for X and see what the buzz is all about. So from the AWS Management Console I'm going to go ahead and type here in the search box x. And what you will see here is the Elastic Container Service. So let's go ahead and click on the first result that we see here. And this will take us to the X starting page. So this is where our journey begins. We were talking earlier that we need to create a cluster. And as you can see here initially we have zero clusters. So what we're going to do next is we're going to go ahead and create a new cluster, or at least start creating one, because there's a bit of explaining that I need to do before we continue. When we start creating a new cluster, we're going to see here a section on infrastructure. So this part here is super important. And this is what this lecture is all about. ACS has two main ways to run containers which are called practically launch modes. These are EC2 instances which you will see here, or AWS Fargate using Using EC2 instances is one way to get started with ECS and to provision an ECS cluster by creating EC2 instances. We have full control over these instances, but this also means we are responsible for managing them. This includes tasks like applying security updates, maintaining the servers to ensure they run smoothly, troubleshooting any issues, and so on. Another, more beginner friendly approach is to use Fargate. Fargate is a serverless compute engine for containers, so you're going to see here serverless near the name. Serverless means that we don't need to worry about managing servers. AWS takes care of that for us. We cannot even see which virtual servers are being used and honestly, we should not even care. The idea is that with Fargate, we focus on building and running our application and care less about exactly on which servers this application is running. While Fargate makes deployment easier, it is important to note that it is not covered under the AWS Free trial. This means that you will be charged for using Fargate based on the resources you consume. AWS provides a calculator which allows us to get a better idea of how much Fargate actually costs. So what you will see here first is the region. So it's important to choose the region where you are using AWS. So in my case I'm going to choose here the region US East one as an example. Apart from this, what you're going to see here is the option is on demand. On demand means that we want to start this cluster now and deploy an application. This is on demand. Apart from this we have here the operating system, the CPU architecture, which don't really makes a big difference. We're going to run only one task. Now here are a few important things to do. We are practically using some resources in the cloud. When one of these resources are the following. First one is the virtual CPUs that we're using. You can see here it's set to one, but we're going to use a quarter of a virtual CPU and this will be enough for our application. Apart from this, our application will also need some memory. Default here is one gigabyte of memory, but we're only going to use half of gigabyte so about 500MB of memory. Apart from this our application also needs storage. So we're going to go here with 20. Let's imagine that we want to use Fargate for one hour. So I'm going to write here average duration 60 for 60 minutes. Or we can also choose a different unit. So we can also go here directly with one hour if we wish. So let's go ahead and click here at the bottom on Save and View Summary. So what we're going to see here most importantly is the monthly cost. So if in a month we're only running this for one hour it's going to cost us $0.38 USD. So probably we're going to need to run this service for a few hours to be able to do all the steps that we're discussing in this section. Just going to give you an overview of how much this costs. Now ideally Fargate should have been covered under the AWS free trial. But fortunately I don't make the rules. So I think for a few hours of running Fargate, that's a reasonable amount. But I leave it up to you to decide. For the upcoming lectures. We will be using Fargate to deploy a Docker container. As for beginners, this is a much, much less complicated process compared to using EC2. I don't want you to spend hours troubleshooting EC2 instances and configurations. That's just not a good use of our time. In the end, our goal is to get something to run and to see how Jenkins can help us facilitate this.
