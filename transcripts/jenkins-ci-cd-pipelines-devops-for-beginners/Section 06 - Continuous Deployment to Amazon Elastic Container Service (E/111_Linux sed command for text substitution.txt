# Linux sed command for text substitution

While our pipeline is working great, it still has a flaw. And the flaw is that in our task definition, we still have a hardcoded reference here, the version. So we need to know kind of like in advance which Jenkins build number we will have. And always keep in mind to update this value here. And honestly it doesn't make any sense. And this is what we're going to change in this lecture. We're going to automate everything. And for that we have to learn another super useful Linux command and go directly here to the Jenkins file. And we're going to do it as follows. We need to do this in the part where we're working here with the task definition. So essentially before registering the task definition we need to make some changes to the task definition file. And we're going to do those changes with the command which is called sed So this command is called like this because it's a stream editor which is generally useful for transforming text, and one of the cool features that it has is that it can do a replacement. You can think of it like when you're in a text editor and you're searching for something and replacing it with something else. This is exactly what this command is doing. So what we need in our file is to replace this with something else. So first of all, we need to get rid of this. And we're going to use a placeholder. I'm going to call this simply app version. And we can also add here something for example an additional character at the beginning and at the end, just to make it a bit more unique so that it doesn't collide with anything else so that we are not by mistake replacing anything else here. This is what we are searching for. This is unique enough so that we are having no issues. When I go here to the Jenkins file and we're going to say here, hey, replace this with what we want to replace it. We want to replace it with the react app version variable that we have here. Because this is holding our application version. We're going to replace it with this. Now the syntax here is super important. This needs to be with a dollar sign in front because it's a variable. And the syntax here is super important. First of all we're going to separate these two here with a forward slash. So this is the first thing that we need. And apart from this we need to put everything between quotes. I'm going to quote here at the beginning quotes at the end. I'm going to provide here an additional flag. Dash G is not necessarily needed. It's just a global replacement. So just in case there are multiple occurrences of this in the file, we want to make sure that we are taking care of all of them. And because this set command is so powerful it knows how to do a lot of things. We need to tell it that it should do a substitution. So we're going to start here with s forward slash. And apart from this we also need to specify where is the file. Where are we trying to do this. I'm going to go ahead and see where the file is I'm going to copy the path and paste it here. This is not the right path just the second Once again, right click copy relative path. This is what we want. Okay. Going to be in AWS. That's the name. And apart from this one final thing that we need to provide is hyphen I. And I stands for Editing in place. We want to take this file as an input. We want to use the set command to automatically place this. And then to be able to do whatever we have configured here. So just again S is for substitution. Then we're separating here what we want to replace with what are we replacing the thing with. And we're doing global search can be a bit complicated in the beginning to configure this. But once you get the hang of it and you know how it works, no problem. And now we have resolved the final piece of, let's say automation that we need so that we can run this pipeline as many times as we need. We can make changes to our project, build it with Jenkins, and not have to worry about updating versions here and there. I'm going to commit the changes. Let the pipeline run, and we're going to check one final time to make sure that everything is working as expected. So while Jenkins is still working on executing the pipeline, I just want to point out again our application version. So it's 114. Want to go a bit and see if everything is working properly. So we're going to look into the container registry. We'll see if the image is inside the container registry. We have here the tag 114 successfully pushed. Let's also go to ECS. And within the cloud we only have the service with one task running. Let's take a look at the task. And if we go to networking and open the address we're going to see here version 114. So our test is successful. We have a fully automated pipeline that does everything that we need.
