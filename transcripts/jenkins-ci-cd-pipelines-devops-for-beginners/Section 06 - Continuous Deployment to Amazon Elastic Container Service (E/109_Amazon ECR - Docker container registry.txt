# Amazon ECR - Docker container registry

As you might have guessed, the Docker image we have built here in our pipeline is pretty much lost as soon as the job finishes, or it will soon be if we do not permanently store this image. Apart from this, the Docker image we have created is not some regular file that we will find in our workspace, or something that we can upload to Amazon S3 or anywhere else. When we want to preserve a Docker image, we need to save it in a Docker registry. For example, Docker Hub is a public Docker registry for Docker images. However, we're making the assumption that our project is not public, and for that reason it makes sense to use a private registry. And AWS has a private registry which is called Elastic Container Registry. So let's go into AWS and search for Easter So what we're going to see here is Elastic Container Registry. Let's click on this. And here on this page we're going to get some generic information about ECR. If you have a bit of time you can simply go over the information presented here. Nothing too advanced. But what you want to do is to create a repository. So I'm going to go ahead and click create. What we need to do here is pay attention to a few settings. So first of all from a visibility standpoint we want to make sure that this is a private repository because we don't want to share this image with anyone else. So this is by default private. We can also make it public. But this is not something that we want. Apart from this we also need to specify the repository name. In our case we will go with a repository name which is the same as our application name. So for example we have here in Jenkins file where we do configure the app name to be this one. Learn Jenkins app I'm going to copy this, and this is going to be the repository name. Let's go ahead and scroll further down. There's some additional settings that we can also use. But in a nutshell we're done with configuring this repository. I'm going to click on create. And in just one second this repository has been created. And this is the address where the repository is located at. So now we have configured the first step. We have a repository with Amazon ECR. And the next step is to take the image that we have built and simply let's say upload it. While the technical term is to push it to this repository. Let's go ahead and copy the address of the repository, because we will need this in our Jenkins file. And right here on top where we have all this environment variables. Let's go ahead and define another environment variable. And we're going to call it Docker Registry or AWS Docker registry as wish and between codes. I'm going to paste here the name. I want you to pay attention to something I'm going to remove here the last part because this is the registry itself. So this is what we need. Perfect. Apart from this there's something additional that we need. When we're building the image. We need to give it a proper tag. And that tag needs to contain also the repository name forward slash app name. This is why you will also see here this being made of two parts. So this is the first part. And here is the second part. We just decided in our pipeline to have this separated because semantically it has a different meaning. So this is in terms of building the image. We have now properly tagged this with the registry, the name and the version So what we need to do next is to simply use the docker push command. And again we're going to specify here the Docker registry. The application name and also the tag. So everything that is inside here we're going to need this. And we're going to use it like that. So just a copy paste from above. And here we are explicitly pushing this particular image with this particular tag. All right. But in order to do this we still need to do a step in between. And that step is just about logging in authenticating against the registry. And for that we are going to use another docker command. So the docker command is simply called Docker login. And this is being used to log in to a Docker registry. The information that we need is the URL of the registry. So we already know that it's the aws docker registry. So we can already append that here. But we need also something else. We need a username. So I'm going to specify here dash dash username. And we don't know what the username is. And we also need a password. But we're not allowed to provide a password here anyway. Now AWS is a bit different from let's say other Docker registries in the sense that we're not provided with a username and a password. And in order to get the password, we actually need to call a service. And I'm going to explain in a second exactly how that works. Now I'm just going to say the following. We have here the username. And the username is going to be AWS. But this doesn't really matter so much. What's most important is this service that we're going to use. And this service is an ECR service. So it's going to be AWS ECR. This is the reason why we are using here an AWS CLI image. Because we need to call ECR. And ECR has a command which is called get login password. So this will give us the login password. Now the idea is that we are getting this login password and we want to directly use it here when using Docker login. So for that reason we're going to use here a pipe. So we are piping the result of this execution to the next command which is called Docker login. This is why I'm going to move docker login to the same line. So we're using get login password. We're getting a password. And then we're using Docker login. We're specifying here the username and we're passing here an additional flag which is called password stdin. So stdin means standard input. And what this actually means is that the password is being provided as a standard input to this command, and this pipe here is essentially getting the result from this first command, putting it into the standard input of this second command. So this is why the second command is able to use the password. And now it sounds a bit complicated. In reality it isn't. Just wanted to briefly explain what is with all these commands and why do we need them. So I'm going to commit these changes. Hopefully I didn't do any mistakes. And we're going to take a look together and see if it works. So I think by now you are already used to my style of letting things fail. So let's go ahead and see what has happened with our build Docker image. I'm going to go ahead and expand this. And let's take a look at what has happened. And we're going to get the following information here. It says here AWS ECR get login password. And then it's piping this Formation to the docker login command. It says here, unable to locate credentials, you can configure credentials by running AWS configure. And this is an error that we had before. You remember this error. We had this error when we're trying to deploy to AWS because we haven't used here with credentials. This is completely missing. So I'm going to go ahead and copy this entire line. And let's put this as a block inside with credentials. I want to make sure that I'm not missing anything here. This would be the beginning and this would be the end. We can select the block, give it a tab for proper indentation. Commit the changes, and let's try once again. Spoiler. It's still going to fail. Switching now to the Jenkins pipeline, we're going to take a look at the job again and see what's the reason for the failure this time. Let's scroll a bit further down. And now this time we're getting a different error. It says here access denied exception when calling the get authorization token. This is our user. Our Jenkins user is not authorized to perform get authorization token on resource. And this is the resource. Again you can probably tell our Jenkins user does not know how to work with ACR with a container registry. And this is expected because we haven't given the user any permissions to do anything. So all we have to do is go to em and see what our user is allowed to do or not allowed to do. Let's open up the user and we can see we have X full access S3 full access. Let's see if we can find anything about ACR. So I'm going to go ahead and search here for ACR And we are going to find anything interesting. So let's go ahead and search for container registry. And one of the options that we're going to find here is Amazon EC2 Container Registry Full Access. So let's see exactly what this contains. So it contains everything regarding ECR for example. So that's already pretty pretty useful. So this is the kind of action that we are interested in. Now we don't want something read only. So that should cover everything that we need. We can also take a look at this one has something with EC2. That's not the kind of policy that we're looking for. So we're going to go with this one again a policy that is a bit probably too generous for what we need, but it should be okay. I'm going to add a permissions. And now we can go back to the Jenkins pipeline. Let's click here on the build. Let's try to restart from stage. And we're going to select here build Docker image. So yes. Finally, success! We have managed to build a Docker image and also publish it in ECR. Let's take a look at the logs to make sure that everything has been as we expect. This is still part of the build and this is the interesting part. We are getting the ECR login password. We are using Docker login. We're getting here a warning but there's nothing to worry about here. It says here login succeeded. And then we finally have Docker push. And we're pushing the image with this tag. And this is done in different layers. So it's taking a bit of time until this is being pushed. And let's verify. I'm going to go here to ECR. And if we click here on the repository name we're going to see here that we have this image tag. So finally success
