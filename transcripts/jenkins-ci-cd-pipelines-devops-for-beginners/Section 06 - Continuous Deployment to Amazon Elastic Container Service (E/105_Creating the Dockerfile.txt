# Creating the Dockerfile

So how do we create a Docker image with our website? A Docker image is created by following a set of instructions similar to following a recipe. We store these instructions in a file called docker file. So let's go ahead and define a docker file in our project. But if we're taking a look in our project we're going to see that we already have a Docker file. And we have used this Docker file to define a custom playwright image that we have used in our previous pipeline. Typically at the project root, if we find the Docker file, we would assume that that Docker file is for building the project and not for building some, let's say, secondary images that we may need in our CI CD pipeline. So for that reason, it would be a good idea to maybe create a CI directory in our project and move this Docker image inside the CI directory. I'm going to click here on move. So now we have this Docker file here. And apart from this in order to still preserve everything as it was, we're going to open up this Jenkins file nightly. If we try to execute this once again this will not work because it will not be able to find the Docker file. So in this case we'll need to specify the file. We're running this command docker build at the root of the repository. And if we don't have a docker file there it will not work. And if we define another docker file there then we'll build the wrong image. So it definitely makes sense to make some adaptations here. So we're going to pass here the F flag. And we're going to specify here that in the CI folder we're going to find here this particular Docker file. So all we have to do here is copy this path. Now select your copy relative path. And we're going to have here docker build. The file is in CI docker file. And then we're going to preserve here the tag. So this should still work just as before. And I'll go ahead and close this and let's go back to creating our Docker file that we now need in the project directory. We want to make sure that we are here at the root of the directory and going to write again docker file. And inside the docker file we're going to start with the from instruction. So the From instruction is allowing us to specify from which image we are starting. In 99% of the cases we're going to start from an image, we're not going to start from scratch. And we already have an image because we've been using here in our task definition, this engine X image. So that's already a pretty good base image because we need a web server to run our website. So nginx is a great choice for that. I'm going to use here from nginx. And we're going to specify here a version The second instruction that we're going to use is the copy instruction. And this will allow us to copy our files from the build directory right here. Build into a location where nginx expects to find HTML files. And this has a specific path. It's going to be an usher which comes from usr share nginx. And the last directory is HTML. So what we're doing here is we're copying from this local directory into nginx here. So that will have this files in our Docker image. And this is all we need. Doesn't need to be super complex. But still we're covering the most important concepts here.
