# Using comments in Jenkinsfile

Through this course will be running this pipeline time and time again. And as you can probably see currently, the build stage is taking a bit longer. So it's like one minute. We need to wait all the time for the build stage to complete. And then for example, we're working here on the tests or we want to see some reports. So we're always have to wait for the entire pipeline to run. But this is not necessarily needed. And in this lecture I'm going to show you how we can use a smart use of comments in the Jenkins file. So comments serve a dual purpose. On one hand, we can use comments to simply comment something in our configuration. Now, typically the configuration itself should be pretty clear and should be obvious what we're trying to do. But for whatever reason, you may decide that you want to leave a comment. So what you can do here is add a new line and write forward slash, forward slash and write something like this is a comment and you will see here the editor recognizes that this is a comment. It's going to print it in green. All right, so this is a way to leave a simple comment about whatever you're trying to do in this pipeline so that later on when you're looking at it and say, oh, this is the part where we're doing this, okay. So that's one option. The other option is to write a blog comment. So blog comment is something that is forward slash star. And this allows you to have multiple lines like line one, line two, and so on. And then you write star and then forward slash again. So you'll see again everything is in green. So all these lines are commented out. Well, typically you're not going to start writing a book in your Jenkins file configuration. But what you could do is, for example, disable a stage. So what do I mean, disable a stage? For example, if we put this entire build stage configuration in a comment would still have it for later on, but do not be part of the pipeline. So if you commit this and let the pipeline run, you will see the build stage disappearing. But the test stage will continue running and it will run. Because first of all, npm test is independent of the build. And apart from this, because we are not cleaning the workspace, the build directory will still be there even if we don't run the build stage. So this is something that can speed up the development time. When you are working on a pipeline, you have some stages that you don't need. This is a quick way on how you can get this to run. The same goes with commands for example. So let's say for example, you have this test command and you are still working on the test stage. You don't want to remove that command and you cannot disable the entire stage. But what you can do is to simply disable the command. So you can use here the pound sign or simply hash in front of it. It will not show up in green because this is essentially a shell script, but this command will not be executed. So go ahead and play around with this and see if things are still working the same. I'm going to commit these changes, and I'm going to take a look in a few seconds at the logs. So what is interesting now to notice is that the build stage has disappeared, but the rest of the pipeline is still working as before. We can also take a look here at the logs to see exactly what has happened. So no sign of the build stage. This is where we're running the tests and you'll be able to see here the container is being started. Only the npm test command is executed. So test minus f is not part of the commands because we have commented it out. And of course, when you're using these comments in your pipeline, you still need a valid Jenkins file, so you cannot comment things that will make the syntax invalid. So that's something definitely to keep in mind. I always recommend only commenting out an entire block and not parts of it, because then things can get really complicated and you may not understand exactly why your pipeline isn't working anymore. Right, so I hope this small tip is useful and that it saves you a lot of time when developing pipelines, but also throughout this entire course.
