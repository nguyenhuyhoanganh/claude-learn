# Publishing an HTML report

Many of the tools that run tests can generate an HTML report. The same goes, for example, with playwright. So when we're running the playwright test, we can also specify an HTML reporter and have an HTML report published. So let's go ahead and try to get this to run in Jenkins. So I'm here at the documentation page which explains how to use the different reporters. And essentially we already have npx playwright tests. All we need to do is append this dash dash reporter equals HTML to our Jenkins configuration. Let's go ahead and open up the Jenkins file. And we're going to add here. The reporter to this kind of commit the changes and run the pipeline. So the execution of the pipeline has been successful. We can take a look at the logs to see if there's anything in particular here that is of interest to us. And essentially you're going to see running three tests. So everything has passed. All right. So let's take a look at the workspace to see if we find this HTML report that has been generated. We're going to see here playwright Dash report. And inside we have HTML. So I'm going to go ahead and click on it. And we're not going to see anything. Of course we can use here. Right click and select inspect. So what we're going to see here in the console, we're going to see an error here. Content security policy. Now in Jenkins there are many files generated by untrusted sources, including those that are found in the project workspace, like this HTML report that we are trying to open. And as you can see, it's not working. The thing is that Jenkins will distribute these files with the content Security policy as an Http header. By default, this CSP header is configured to prohibit various web features in order to guard against cross-site scripting and other possible attacks. So the idea is to protect the Jenkins users who may access these files from some potential security issues. This is why this error occurs. It is just a safety mechanism. Now, in our case, this is not super useful because we really want to see this HTML report. So let's go back to Jenkins and see how we can configure a few things. So I'm going to go here to the dashboard. And from the dashboard I'm going to select Manage Jenkins. And then if we scroll a bit further down, we should see here a section. And the tools and actions we will find here. Script. Console. So essentially this script console allows us to send some very low level commands to Jenkins. You will find the resource to this lecture, this exact script that I'm sending here. Now essentially by running this script you are changing some Jenkins security settings. Well, this is absolutely okay for this test. Jenkins installation. It may not be okay to run this command on a Jenkins installation at work. For additional details, I've added also an article to the resources folder. This can help you get more familiar with the Jenkins content security policy, and better understand what exactly this script does. But for this step, we just want to run the script. You will not see here any result. So the execution is essentially in a blink of an eye. Going back to the dashboard to the job. Now select here the latest build. Check the workspace. Go inside the workspace. Open the playwright report. Let's click on HTML. And now we can see this report here. Now, of course, we don't want to go through this entire path of going to the job and going to all these folders just to find this report. Jenkins also offers us the possibility of publishing this HTML report. Now, in order to publish this HTML report, we need a plugin. We need a publisher. So we're going to go back to manage Jenkins. And from the plugins. We will have to go here to available plugins. And from the available plugins simply search for HTML. So we'll find here the HTML publisher. If you cannot find it, you should check the installed plugins because maybe this is already installed. This case it's not installed. So let's go ahead and install it. It will take only a few seconds. Right. So the installation has been successful. We see here success. Success don't need to restart Jenkins for this particular plugin. So let's go back to our Jenkins configuration. And essentially what we're going to do here is as a post action we always want to publish this report. Now, in this case, this publisher has, say, a bit more complicated syntax. There are a few things to do with it, and I just want to show you a quick way on how you can generate this configuration that is needed. So let's go back to our job. And I'm going to select here configure. Let's scroll a bit further down. And one of the things that we should notice here, right at the end of the configuration, is this thing called pipeline syntax. Let's go ahead and click on this. And essentially this is like a code generator or pipeline generator. So it allows us to easily generate a pipeline script here. What do we have to do here is select the step. So in this case it should be something like publish HTML. You'll see here publish HTML report. And this gives us essentially some additional details about which individual properties that we want to configure. So first of all we need HTML directory. So we need to look inside the job itself and click on the job. Jump directly to the workspace and see what is the full name of this directory. So it's playwright Dash report. I'm going to select it and work with Copy paste. All right. We have the name of the directory. Perfect. What's the name of the report here. So we see the report is index.html. This is already what we have here. Apart from this we can specify a page title. We don't need this. And as a report title we can write here. Playwright. This would be the playwright HTML report. Let's go ahead and click Generate Pipeline Script. And copy this. And here inside the pipeline configuration, inside this block, we can simply go ahead and paste this entire configuration here. Let's go ahead and commit these changes and see if there is anything else that has changed. Execution of the pipeline has been successful. So if nothing shows up on the screen, we're going to hit that refresh button. And what we should see here on the left hand side is this playwright HTML report. So if we click on it we're going to be taken directly here to the report. So we no longer need to click ourselves through this entire process. And we also have a link here so we can go back to the job itself. So this is how you can publish an HTML report in Jenkins.
