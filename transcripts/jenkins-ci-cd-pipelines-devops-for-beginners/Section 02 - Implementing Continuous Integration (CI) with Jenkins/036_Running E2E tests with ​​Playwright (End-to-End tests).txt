# Running E2E tests with ​​Playwright (End-to-End tests)

In this lecture, we will look at how we can run end to end tests written in a very popular framework called playwright. So essentially, end to end tests are a way of testing a running version of our application. In our case we have a website. So essentially to be able to run these tests we need to have this application running somewhere. Now we are still in the CI part of our pipeline. So we currently haven't deployed our application anywhere. But we can still start it in our Jenkins pipeline, run these tests and then see if everything is working as expected. This lecture is technically a bit more advanced, so it may take you a bit more time to do all the steps that I'm doing. And you may also need to go back a few times and rewatch some explanations so that everything makes sense. With that being said, let's dig into it. So here inside the project still have the terminal window open. And I want to go back a bit to the basics. And the basics were simply building the application. So the command that we need to run to build the application is npm. Run, build. Let's run this command once again. And just a few seconds later we're going to get back the following information. So we're going to see here that creation of the build has been successful. And apart from this is also telling us a few interesting facts. And it's saying here the build folder is ready to be deployed and it's giving us an idea how we can actually start a server locally. So here are two commands that we need to run. So npm install dash g serve. This essentially installs a piece of software that allows us to start a very simple web server. But right here locally this will absolutely work on your computer or anywhere else. This is not for production purposes, but this is just enough to actually take a look at a build folder and start a simple web server. So it's going to take a few seconds for this tool to install. And if the installation has been completed, you should see something like this. On the next command that we need to run is this one. So I'll simply copy pasting from the instructions provided here. We're going to get here an error. But at the same time it seems that our application is running. So we're going to get this information here open in a browser. Let's go ahead and try this. I'll take a second or two to load. And this is our application. So we have essentially used this command here to start a very simple Http server that can run our application. Now this is something that we're going to try to do in Jenkins as well. Because in order to run the playwright end to end tests, which are already part of the application, we need a running version of the application. It cannot just work directly with the code like some unit tests or something like that. We need a running application. So this is why it's important to understand a bit the basics. We need a build folder and then we're installing this surf tool. And then we serve. We are starting an Http server that then we can use. So we're going to see here the local address of the server is this one. But here GitHub has also made this available at another address. Okay I'm going to go ahead and press control C to stop the server. So it's saying here gracefully shutting down. So essentially we know that this part is working. So let's go ahead and see how we can get this working in Jenkins as well. We're going to make some changes to our configuration. The build stage is disabled currently because we still have to build files in the workspace, so we're gonna save a bit of time there. We can still keep the stage test actually, to save a bit of time, we can simply go ahead and copy this configuration because many things that we need are already there. So I'm gonna make sure that copying the exact portion that's necessary and going here to the root and then pasting it. So now we have two test configurations. What we need here is actually to rename this in end to end E to E. Then when it comes to the Docker image that we want to use, playwright already provides a Docker image. And this is the name of the Docker image that we can use. And I'm gonna also add this link to the documentation, the resource to this folder, but also this exact image. I'm going to put it there as well so that in case this page changes, you can use the same image as I've used in this demonstration. So let's go ahead and replace this. I'm going to have here this image. That's perfect. And then let's see which commands we need to run. So essentially there are a few commands that we need to run here. And we're going to inspire ourselves from what we have here. So from the commands that we have executed, because the first step is starting the application, and then the next step is actually running the tests. So in order to start the application we're going to use here, we're going to first install serve. So this will be a global NPM dependency. And then we're going to use serve to start a web server in the build directory. And then we're going to write a command Npx playwright. Test. So this will essentially start the tests. Right. So let's go ahead and commit these changes. Run the pipeline and see what is happening. Now jumping back at the pipeline. If you're getting an error in the E2E stage, that's absolutely fine. Let's take a look at the logs and see what is going on. So we're going to scroll down until we can see here actual image that we want to use here. So this is the playwright image. So you can see that the container has been started. And then we are running here the first command, which is npm install dash g serve. We're getting here an error. So when we're getting errors, it's important to go from the beginning to the end and understand what is going on. So we see here we have an E access error something which make directory. And there's also additional information here like this is the create directory command. It's telling us that somehow it cannot write to this directory. And there's also giving us additional information here, including the suggestion running the command as root or admin. And if you search for such an error online, quite often you will get a suggestion. Just run this container in root mode. So let's take a look at how we can actually run this container in root mode and get rid of this error. If we're going here to the Jenkins file, what we can do here is essentially under the Docker configuration we can specify something that is called args. We can write here args and between codes. For example we can specify dash u which will send for the username that will be used to start this container. So you can specify here the username root and the group root. Now don't do this really for this use case. Running this container as a root is not a good idea. This is not the solution. Now essentially you need to remember that we have mounted also the workspace. And we have mounted the workspace from Jenkins, which is running under a different username. And in this container we are essentially running everything with a root username. So if we're making changes to workspace files as a root later on, Jenkins will have issues accessing those files, modifying those files or deleting those files. So for this particular use case using here root to run this container is not a good idea. There are some situations when it may make sense, but typically if you're encountering a solution like this, it may initially seem to work, but then later on you're encountering other issues which will have more troubles understanding. So you're kind of like end up running everything with root, and that's not a good idea. So I just want to show you how you can specify different user. But we're not going to use this user here and we're not going to use this approach. Now the problem here is that we are trying to install serve as a global dependency. Well we don't need to do that. We can also install serve as a local dependency to our project. So let me show you how this looks locally. So here in the terminal kind of simply clear everything with command l and paste this npm install serve. So this will install it as a local dependency to the project. So that's super important to understand the distinction. You see here that the package.json and the package lock Json have been changed. Don't need to run this command because we're not going to commit these files. But there's something else I wanted to show you. If we're taking a look here at the node modules, the node modules will have here a bin directory. And inside the bin directory what we should find is actually serve. Here it is. What we can do here is simply right click this and we can go here. Copy relative path. And now when we're trying to call surf here, instead of calling it like this, I'm going to use here node modules forward slash bin forward slash serve. So we're going to install it locally. And it's going to be available from there as also an executable that we can run in our script. So this is how we can install this locally and not use root. And also get rid of that error that you've seen earlier. So there's an important thing that I wanted to mention is that we don't want to install this as a project dependency. So when we have executed this command, you will see here that the package lock has been changed. And also the package Json has been changed. So we can simply go ahead and revert the changes that we have here. Now we're going to go here to the git commits. We can discard changes for this file and for this file as well. So the only changes that we have made are now to the Jenkins file. So you will see here. Okay, I'm going to go ahead and commit these changes and run the pipeline again. So this pipeline has now been running for almost one minute here inside the E2E stage. So I'm wondering what is the pipeline doing right now. So we can simply click on the logs and see what is happening. So we can now see that we have installed serve no errors. And then the next command you'll see here the plus sign that we're running is this one. It says here accepting connections at this address. So we have started a server. And what you need to understand is that this job is now stuck. Even if we are specified multiple commands. Inside our file here. When we run this command, we are starting a web server and this server and this command runs forever. This is essentially the idea of a server. You're not running a server for one minute or something like that. You are starting the server and it will run until you tell that server to stop. Now, of course, for this scenario, this is not very useful because we actually want to run the tests. So what can we do about this? Now, first of all, when we're encountering a problem like this one, there's no way around this. So this Jenkins job will run here forever. So what we need to do is go back here to the Jenkins job page and manually cancel this job, because otherwise we'll be waiting forever. That's the job of the server. It doesn't stop. Started a web server. It will continue running it. So this is not a proper configuration that we need for our jobs. Okay, so now we have managed to stop this job. Let's go back to the Jenkins file and make a small change to it. So what we need to do here is to specify to tell this command to start the server, but to sort of like run it in the background so that it doesn't block the execution. So what we're going to use here is this ampersand, this end sign at the end. And this will start this command in the background. And it will not block the execution of the rest of the commands, which means that next we can go to this Npx playwright test command. Now, there's still one small thing we need to do here. These commands are executed like one after the other. So when we're running this in this mode, the command will start in the background and will then say, okay, next command, even if it hasn't really started the server yet. So it needs a few seconds to start the server. So we don't want to start running the tests immediately. We want to wait for a few seconds. So this is why we're going to use here the sleep command. We're gonna wait for 10s for the server to start. After those 10s, we're going to run the tests and commit these changes and run the pipeline again. So jumping back at the pipeline, what you will notice here is that all the stages, everything is green. But then the pipeline itself has failed. So what's going on here? First, let's take a look at all the logs to understand what has happened. So we're going to take a look here at the end to end logs to see if everything worked there as expected. So we have here npm install serv. So this is working properly. Then what we're trying to do here is we have this serv command. So we're going to see here this working. We also see here the sleep. So these are not necessarily directly in the order we have written them. But you'll be able to see here that essentially we have this info. So it's accepting connections. And now it's running in the background. And then we have here Npx Playwright test. And we have all these tests being executed. So at least from this perspective, everything seems to be working fine. The output is a bit, let's say, more complex, but. It doesn't seem to be any issues here. There are no errors. And then later on, what we're going to see here is that we have the post actions and recording test results. And then we're getting this error, which is kind of like unusual. No test report files were found. This is a configuration error. So let's take a look at what's going on. Exactly. So within the job itself we're going to go here to the workspace. And according to our configuration here inside test result, we should find our test results. But from the test stage. So what has happened with this? Well, there is a conflict between these tools that we're using because the workspace is now shared between different tools. We have a tool that is running essentially the unit test. And we have another tool that is running the end to end test. And the last tool is essentially overriding this test result. So there are a few things that we can tweak. We can either publish these test results much earlier, but then we are losing these files or we can specify them in a different directory. Now, I just wanted to briefly show you where you can do this. So this is inside the package, that Json file. So here is where this test results are being published. So we can simply change the name here. For example we can call them just results. So we can change the name here. So this will be published under a different path. Let's go back to the Jenkins file. And here this will be a different directory. So that's one possibility. Other possibility. And I just want to show you this as well, would be to move this post block. And to put it here inside the stage itself. So that would be also an option. Just going to leave it as it is. Just want to check again that the path is indeed correct. Don't want to get additional errors here. I'm going to see that the name the directly is this one. So let's commit these changes and run the pipeline again. We are taking a secondary look at the pipeline. We're going to see now that the entire pipeline is successful and that this post action is now also successful. So there are no errors in recording this test results. And we can also go ahead to workspace itself and see what's going on there. So we're going to see here this test results. This is empty. So this is like the default behavior when these tests are successful. So nothing is recorded. It's a bit unusual. But this is also a scenario that you may encounter. But then going back to the workspace we're going to see here also the jest results. These are. Available here. And this is the report that we're publishing. So this way we now have a successful pipeline where we're running end to end tests. And you'll see here that everything is working perfectly. There's no server that's hanging on after the execution has been successful.
