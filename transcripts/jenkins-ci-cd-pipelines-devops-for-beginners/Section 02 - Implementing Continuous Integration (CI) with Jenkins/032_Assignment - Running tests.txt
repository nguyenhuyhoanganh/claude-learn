# Assignment - Running tests

All right, so it's time now for an assignment and for you to get practice working on this Jenkins file and building Jenkins pipeline. So what's the goal of this assignment I want you to create for this pipeline the test stage. So in the test stage, there are two things I want you to do. First of all, I want you to look inside the build folder. And to see if there is an index.html file, that would be the first step. The second step would be to execute some tests that this project has. So in order to execute this test, the command that we need to run is npm test. So I'm going to show you how this command works. Now right here npm test. Now, in this case, this command will detect that it's not running inside a CI CD server and will provide this user interface. But this is something that will not appear in Jenkins. So I'm going to select here A to run all tests. And this is essentially some tests that have been written for this application. So I'm just showing you how to run this manually because it can be useful to know this. But your job is to simply run the npm test command from Jenkins. The following quiz will take you through this assignment step by step.
