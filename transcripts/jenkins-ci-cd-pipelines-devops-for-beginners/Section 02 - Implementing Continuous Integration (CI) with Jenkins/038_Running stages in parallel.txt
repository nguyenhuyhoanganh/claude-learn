# Running stages in parallel

So this is our pipeline. And as you can see, every stage is one after the other. So we're starting here with the checkout of the code. Then we have the build stage which currently doesn't show up because it is disabled. Then we have the test and then we're running the end to end test. And then we are publishing some reports. Now this is all in good, but some of these stages can be executed in parallel to save time. So in this lecture, I want you to show you how we can run the test stage where we run a unit test and the end to end stage in parallel. Let's jump to our pipeline configuration. When the pipeline configuration. What we'll need to do is to define a stage which is like a group stage which contains multiple stages, essentially the stages that we want to run in parallel. So we're going to write here stage. And let's name the stage run tests. This will be the block with this stage. And this is the super important part. This is where we're going to write here parallel. And again as a block we have this stage and then we have this parallel instruction. So essentially we're telling Jenkins everything that is inside here should be run in parallel. So what do we want to run in parallel. Well these stages we're going to go ahead and make sure that we have the syntax and everything correct. So this is one stage and I'll copy it. And this is the second stage when I use here cut. And I make sure that the syntax is still valid. And inside parallel. I'm gonna go ahead and paste here. I'm going to select the entire code. And press tab once to types so that we have the right indentation. So we now have this stage. And then we have the test stage and the end to end stage. We can call this for example unit test. And we can just call this stage test. That would be also fine. Okay. Let's go ahead and commit this and see how it works. Now if we're looking at the pipeline view it looks pretty much the same right? Um, well, it's a bit more complicated. You see, we have here tests, which is essentially an MD stage, which only contains the unit tests and end to end tests. Right now, this view isn't great, but if we're taking a look here at the logs, we'll be able to see that these have actually been executed in parallel. Let's see what is going on here. So for example here right in the beginning, we're going to see here that this Docker image node is being started. And at the same time this Docker image is being started as well. So we'll see here that the logs are combined. So we're doing something with a unit test. We're doing something with the end to end test. So this is essentially a parallel execution. It doesn't look so nice but it is a valid parallel execution. Apart from this, there's also something else that we can do inside our file. And this is related to the post actions. For example, you can go ahead and copy this post block and kind of like make it more specific to the stage that is actually needing this. So let's go for example to the unit test. So we have here the steps. And after the steps we can simply paste this configuration here. Let's go ahead and properly indent this. And I'm going to copy it once again. Then go to the end to end stage as well. Paste it here. All right, so what we need here is for the unit test, we only need the J unit reporter. And for the end to end tests, we need to publish the HTML. So this is what we need in terms of this. And we can use this post actions within the stage. So then we know oh this post action is for the end to end stage. And this post action is for the unit test stage. Let's commit this as well and take a second look at the pipeline. So we're taking a look at the pipeline. There's absolutely nothing different. But the most important thing is to refresh the page and to see here, for example, I'm at build 18. So you will see here that this report has been updated here. So we have an execution kind of go and see okay I have one test passed. And the same goes for the playwright report which is available here. So in this scenario you will see that this post actions still work. So the idea behind this is to simply check the post. Actions still work as before. All right. So in a nutshell, this is how we can run these stages in parallel. You need another stage to wrap these stages that you want to run in parallel. And then also this parallel keyword which is super important. This is what does the magic. This is what tells Jenkins to run this in parallel.
