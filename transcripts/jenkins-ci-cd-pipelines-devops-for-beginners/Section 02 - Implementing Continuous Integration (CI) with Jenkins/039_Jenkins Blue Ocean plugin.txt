# Jenkins Blue Ocean plugin

Currently, the way this pipeline looks like here in the stage view is less than ideal, because if we are running jobs in parallel, we cannot really easily see that. In this lecture, I'm going to show you how we can transform this pipeline representation from this. Into this. So how can we do this? Where does this new look come from? This is actually a Jenkins plugin called Blue Ocean, which completely changes the user interface of Jenkins. Now Jenkins is still Jenkins, and all the jobs and everything that we have will still continue to work as they have before. So let me show you in this lecture how we can do this. I'm going to go here to Dashboard Manage. Jenkins. And for many Jenkins gonna go next to plugins. And from plugins. We're going to go here to available plugins and search for blue. Ocean. I'm going to select here the first option that appears, which is the Blue Ocean aggregator. Let's select this one. And click on install. Blue Ocean is actually a collection of multiple plugins. If later on you want to remove Blue Ocean. Probably it's a good idea right now to make a screenshot or a note of this page with all the plugins that are being installed. Anyway, you're going to see that all the plugins related to Blue Ocean have the name Blue Ocean in them. We scroll a bit down while this is still running. We can activate this checkbox. Restart. Jenkins. When installation is complete and no jobs are running. So this should ensure that once we're done with installing all these plugins, we give Jenkins an opportunity to start again from scratch to make sure that all these plugins are detected and correctly enabled. So installing these plugins will take a few seconds, but we're going to go ahead and skip ahead. So Jenkins has restarted. Let's go ahead and sign in again. And let's go to the dashboard. And what I want you to notice here new is this open blue ocean. So let's go ahead and click on it. And this will open a completely new Jenkins interface. So this replaces the old interface, but you can still go to the old one. And I'm going to show you in a second how everything works. Let's go ahead and check our application to see what the pipeline looks like here. So if we're going here to the latest build that we have. What we're going to notice here is that the representation actually now makes sense. We're starting the pipeline. Then we have to build. Then we're running inside the test stage, the jobs end to end in unit tests. And then we're deploying in the pipeline ends. So this is everything as expected. Apart from this, we can easily inspect here the tests. So if you click on test we're going to see which tests we have and the fact that they have passed. We also have artifacts in case we publishing any artifacts. This will be available here. So this interface in terms of visualizing the pipeline is much, much nicer and easier to understand. We need to run a job. We can click it from here. Or if you want to check the logs, we can access them from here. So definitely spend a bit of time and see how this interface works for you. Some people like it. Others who have been using Jenkins for a long time are kind of like used to the old interface. So at least in this course, we're not going to be using this interface a lot. But I just want to show you a use case when using this interface. Makes a bit of sense. And that is the pipeline visualization. If you wish to return to the previous interface, you can do that as well. There's a button here. So if you're clicking here, you're gonna exit to the classic view. And whenever you want to open the blue ocean view, that's still going to be available here. So hope you find this interface useful. And feel free to jump between the interfaces whenever you want, but especially if you want to make sure that you have configured the pipeline correctly. And if it looks in reality the way you think it should look, then definitely use the Blue Ocean plugin because it makes visualizing your pipelines much, much easier.
