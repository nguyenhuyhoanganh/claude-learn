# Troubleshooting Jenkins while using Docker in Jenkinsfile

At one point during the course, you may notice that the pipelines that you had stopped working, or that there are some weird errors that you're getting. So let me explain how to troubleshoot these issues. Even if right now you are not facing any issues, there is a very high probability that you will have these issues. And I just wanted to let you know how you can deal with them. So first of all, let's start with the pipeline that's already working. So I'm going to start here with this hello pipeline which is very simple and doesn't use Docker. So you will still be able to open Jenkins. You will still be able to see everything. You'll see the pipeline working without any issues. Perfect. But there can be a different type of pipeline pipeline that is actually using Docker. So in this example here, if we're taking a look at the configuration, we're going to see here that we have here a Docker agent that we want to use. So let's go ahead and try to run this pipeline. And what we're going to notice is that this pipeline fails. And taking a look at the error it's going to be a weird error like error during connect. And there can be also various kinds of errors that you may encounter when your pipeline is using Docker. So what exactly is going on? If you are opening Docker desktop, what you should see is something similar to this. So you will have here install Jenkins Docker. You will see here my Jenkins with a status running. And you will see here Jenkins Docker below with a status exiting. Or you may even notice here a different container that is running Jenkins, which has nothing to do with this install Jenkins Docker container. So if you see that container running there outside of this group here, you see this is kind of like a group because we can go ahead and collapse it and expand it. First of all, you need to click on that trash icon under actions to remove that. If you see something similar to what you're seeing here, all you need to do is open a terminal window and run the following command. So I'm here inside my terminal window, and I want you to notice that I'm here inside the install Jenkins Docker directory. So it's super important that you are inside this directory when you're running this command. The commands that we're going to run are actually from the documentation. So first of all what we want to do is go back to the documentation. And this is the documentation that we used in the beginning to install Jenkins using Docker compose. And the command that we want to run is this one docker compose down. So I'm going to go ahead and copy this. And here inside the terminal I'm going to run the command. And this is going to stop all the containers. That's a super important step. So if you're looking here inside Docker desktop we're not going to see any containers running. So this is the state that we want to have. Now going back to the instructions. The second command that we want to run is this one docker compose up d. Let's go ahead and paste this here And now what we will see here is that two containers have been started. If you are taking a look inside Docker Desktop we're going to see here we have two out of two running. And just to make sure that everything is exactly as I'm saying, I'm going to go ahead and expand this, and you're going to see here that both of them have the status running. So this is what we want to see. And then we're going back to Jenkins reloading the page and clicking again on build. Now let's take a look at what is happening here. We're going to see that our pipeline is working without any issues. So that's exactly what we wanted to see. And as I was saying earlier, if you're noticing something like this within your Docker desktop installation that one of the containers is not running, you need to stop all of them using the commands I've shown and then to restart them. This issue is most likely caused when you are stopping Docker desktop without actually stopping the containers first using the CLI, or if you're restarting your computer and then you're starting Docker desktop. Typically only Jenkins Docker will start on its own. And these steps that I've shown you will solve the problem.
