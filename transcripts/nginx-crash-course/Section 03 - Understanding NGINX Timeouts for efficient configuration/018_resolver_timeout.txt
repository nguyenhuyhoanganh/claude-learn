# resolver_timeout

So the resolver timeout is when you when you configure this nginx to connect to a back end. Usually, uh, you need to resolve the DNS for these names, right? And if you, for example, made a request now and the nginx will ask okay, what is my, the IP address of this back end server one dot test.com. This is called resolver timeout, right. Nginx needs to resolve the IP address of this, so it's going to talk to a DNS server. It could be a local one. It could be a public one. How long should I wait? After which I will give up connecting to this back end. So this way, in this case, you will think that you have only 30s and afterward, sorry, you're done effectively. You cannot do anything else. Right? And that is basically. You can you can either move to another back end or just stop the connection altogether. So that is the resolver timeout. Another I think 32nd is too much right for for the default timeout for for to resolve a DNS. I really would would put this as a very very very low number effectively.
