# NGINX Frontend Timeouts

Engine timeouts or any proxy timeouts for that matter, are very critical to control the privacy of the the security really of the proxy, the reverse proxy, the load balancer. It is very critical to ensure efficient use of resources so you don't let a runaway process run for a very long time and consume all the resources and then the client keeps waiting. So these are some of the timeouts that Nginx use to guarantee these things. So. So I'm going to take some time to explain each of these timeouts separately so they'll be available in each lecture so you can jump into the lecture that you're interested in. They are categorized into I categorize them into front end timeouts, which I talked about. Remember, front end is anything that the client talked to the Nginx side. The back end timeout is where the Nginx talks to the back end. Right? The backend servers. So these are some of the times where the back end. These are some of the timeouts of the client. So let's get started with the front end timeouts. So now we're going to talk about the front end timeouts. There are six front end timeouts that we're going to discuss in the coming lectures. And the first one is client header timeouts. Client buddy timeouts send timeouts. Keepalive timeouts lingering timeouts and finally resolver timeouts. Let's start explaining one after the other.
