# proxy_next_upstream_timeout

Proxy next upstream timeout. So now this is defined as limits the time during which a request can be passed to the next server. The value zero turns off this limitation. The default is zero. So what does this do? If let's say I want to send a get request from the client to the proxy and Nginx will turn it on and connect to this guy, but this guy is not responding for any reason. Read timeout. It's no longer reachable anything. Right? What? What should nginx do? You tell me. What should nginx do? Should it just fail? Or should it just try another server? It really there is no right or wrong answer. It really depends on your use case. Sometimes this this request is so precious that okay, let's try another guy. Let's try this. This server. Right? This is the proxy Next upstream timeout. How long should I do this? When do I stop doing this loop? Let's say I have 1000 backend servers. Should I loop through all of them? Or if. If I tried. If I was in this loop for 10s, just go ahead and close proxy Next Upstream diamond is just this amount of time. If you if you have more than that, kill it right so that is the proxy next upstream timeout right so that's only effective if you have like more than 4 or 5 backends. If you have like two backends, I don't know if that's effective. I guess if you're looping, you don't want the nginx to keep looping altogether. This by by default is turned off apparently, which means it's going to loop forever. Be careful with that then.
