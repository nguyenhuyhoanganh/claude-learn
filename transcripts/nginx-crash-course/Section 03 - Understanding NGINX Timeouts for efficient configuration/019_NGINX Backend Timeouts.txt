# NGINX Backend Timeouts

All right. Now that we have discussed the front end timeouts, let's discuss the nginx back end timeouts. So the back end timeouts here is. Proxy connect. Timeout. Proxy send. Timeout. Proxy. Read. Timeout. You can see that everything is appended by the proxy. Keep a lifetime, which we saw in the front end, but that's the back end version of the Keepalive timeout. It's like because we want to keep the connection alive at the front end. Also, we need to keep the connection alive at the back end. Different thing. And then finally the proxy. Next upstream timeout. How about we jump into this?
