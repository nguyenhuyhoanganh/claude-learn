# keepalive_timeout_backend

Finally the keep alive timeout. All right. This is defined as the timeout during which an idle keep alive connection to an upstream server will stay open. Right so we're talking right here now. So if I send a connector, I'm trying to connect with the with the backend server and this connection remain idle. How long should I leave it idle for? Seven seconds. Five seconds. Six seconds. It's not used. Should I close it? Should I leave it idle? What should I do? The default 62nd after 62nd. We're just going to close the connection. That's the keep alive. Very simple. Keep alive backend mechanism. All right. If you you can configure this to be like, okay, very high. So okay I want to keep idle connection. It's okay if they take precious resources, but I want when a client makes a request, I want a connection to be hot and ready. I don't want to establish a handshake every time a client comes, but you can argue with that says, okay, if I have a busy server, then the connections will not stay idle. As a result, they will keep be kept alive. Right. And when? When. So that means I can effectively keep this idle timeout at 60s. Right. But. The problem is, if you have a flux of requests that have a flux of connections and now you kept them idle, if you keep the keepalive timeout like, I don't know, three minutes, then those connections are consuming precious resources on both the backend and the server engine, right. So you need to do something about that as well. Yeah, Yeah. You don't want to consume that much resources, right? You want to shut them down as fast as possible so that, uh, I don't know if you're configuring Nginx to do something else, then you want those resources to move to the other, uh, endpoint effectively. And that concludes our timeout. All the nginx timeouts. Let's jump back to the next lecture.
