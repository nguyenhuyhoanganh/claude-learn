# client_header_timeout

So the first front end timeouts is called client header timeout and is defined as a timeout for reading client request header. If the client does not transmit the entire header within this time, the request is terminated with a 408 request timeout error and the default is 60s. So anytime you see A44XX this is a the client kind of a fault, right? The client took time, the client made an error. The client took its sweet time to respond to us and that is basically when you respond with 400. So the client header timeout, these are specific to only the headers that the Http client sends. So for instance, here is an Nginx server, this is some backend and the client now establishes a TCP connection and sends the get http slash one one and want to go to the root page. Right. And okay, we receive that request and we acknowledge it. But now there are a bunch of headers that the client is responsible to send for us. Like, okay, how big is the content length for this? How big, how, what's the type of the content, what cookies and other stuff like that, Right? So if I send the content length, for example, here and then the client all of a sudden just paused, right? And didn't send headers for a long time, I am allowed to wait. Right. Since that read right then since that header timeout started, we started the header timeout. I'm allowed to wait for x amount of seconds after which I'm going to just kill the connection. Right. And emphasis on kill connection. We are really initiating a close connection. This is not really a forceful connection. Close You're going to talk about this in the lingering section as well. So yeah, Now, if the client actually sends the content type, for example, app Json, I know this is probably wrong because this is a get request, but you get the idea right. This is probably let's change this to post for example. Right? But yeah, content type application Json, well we're not going to really accept it because hey, you're done. Sorry. Right. You took so much time to send this headers, right? And you might say, what's the purpose of this? What is the case where users or clients can take a sweet their sweet time to send the headers? Well, there's an attack called slow Loris that does exactly that. One client can send start sending byte by byte content. Why? Just so that it can occupy resources on nginx or the proxy. Right. And if you do that, if you send one byte a second, then every time you send a byte a second, that timer resets, right? But then you took so, so long to send these headers. I'm going to give you 60s to send the entire headers. Right? If you did not send all these headers, then I'm sorry. You're going to go, son. Right? So you're going to kill it. That's how slow this attack is. Basically prevented by setting the client header and not just that other timeouts as well, which is the next timeout that we're going to discuss. Let's jump to that.
