# send_timeout

So the next timeout to discuss is the send timeout. Send timeouts defines in the Nginx docs as sets a timeout for transmitting a response to the client. The timeout is set only between two successful write operations, not to the transmission of the whole response. So now we receive the request successfully from the client. But now I'm about to respond to the client. So even in the responding aspect of things, we have now a certain amount of time right to respond and not the entire response, just between two writes I'm about to write to the client. That's what's happening here. The default is 60s. Let's take an example here. So let's say I want a get request to Http 1.1 content length is 13 and the content type is text. Immediately Nginx will just forward it to one of the backend. The backend will respond with a very large body or very large content and start responding to nginx. Hey, here is a very, very. So Nginx takes it and just writes it back to the client. So this write a timer starts and now the back end start responding with the continuation of this and then well Nginx takes it and write it back and now. The Bakken is taking a long time to respond to to give me content to send to the client. I don't want to I don't want a bad request like this, taking resources both here and also here. So if this is taken a long time, then I'm going to wait for 60s in the front end to send. And if that takes more than 60s, I'm going to close the connection to the front end. It says, okay, sorry. Whatever requests you send to me, it looks like it's malicious. Or maybe that's one thing. That's one example I have to take example of. Or maybe it's just my back end is just not not happy with this request. So I'm sorry, I have to close this connection. And there are a set of timeout that stops whatever was happening here as well. So we're going to talk about them next time. But yeah, we close the connection, we move along. All right. So any request that comes after the back end takes that time. It will be just a it will be just ignored in this fact. Right? So that is the send timeout. Another kind of important one as well. Let's jump to the next.
