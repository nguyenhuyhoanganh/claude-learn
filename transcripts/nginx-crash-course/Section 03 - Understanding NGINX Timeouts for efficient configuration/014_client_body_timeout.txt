# client_body_timeout

The client buddy timeout is another timeout that is used to prevent the. Attacks like such as slow lyrics and also slow clients that are just taking their time to upload like a large file. Right. This is a specific to the body, not the header. So like, let's say you're uploading a file, right? Or uploading an image or uploading something that is taking so long for you to generate, right? We want to maintain the resources on the Nginx side so that you don't you don't end up consuming and hogging resources. So Nginx doc defines this header timeout as following defines a timeout for reading client request body. The timeout is set only for a period between two successful read. Nginx now is reading the front end right. Not for the transmission of the whole request body so the request body can take a long time. But this this time is just specifically between two reads. Right? Every time a client sends something, this client timeout gets reset. Every time a client sends something we get, we reset it. It's just between two successful writes from the client side and reads from our side and the Nginx is that client timeout is defined right and obviously if it's exceeded we get a 408 timeout error and the default 60. All right so the client sends a post request in this case a http 1.1. The content length is one megabyte million, whatever you want to call it. It's a content type is text. I'm about to send a large body right now, so I send that. Okay. So obviously there's something that I didn't show here, but the Nginx turns it on and send it to one of the backends obviously. Right. So now this, the client starts sending the body. So it's like, okay, this is a very large file. And so every time it's set, it sends the body, the client timeout gets reset. So it sends another again, reset. So now it starts counting. Okay, you send this is the large body segment you send. Now, I have to wait, wait, wait, wait, wait, wait, wait. Once this default 62nd is exceeds, then I am going to close the connection. Okay. So now you can change this to as low as one second or 2 seconds or 3 seconds, because if you don't if you you maybe your nginx says, okay, I don't want to serve client. That takes more than two seconds to send me a body request. Those are bad clients. They are. It's okay if they my uploading clients are usually in a for example, in a high bandwidth network and I don't really trust people to if if someone is doing that shady business taking some time, that means they are an attacker and immediately I'm going to set the attack to the timeout to a very minimum timeout so that nobody can do this stuff. So yeah, if you close the connection, then I'm going to take time. This the request will be blocked and by blocked really what I mean is is going to be read, but it's going to be discarded. It's not going to be sent to the backend. And that's part of the lingering timeout that we're going to talk about. So that is the client body timeout, a very, very important timeout in the front end to set correctly and then jinx, you might not need to touch it but just understand what it does and that is the fundamental understanding of what we need to do as backend engineers. Let's jump to the next lecture.
