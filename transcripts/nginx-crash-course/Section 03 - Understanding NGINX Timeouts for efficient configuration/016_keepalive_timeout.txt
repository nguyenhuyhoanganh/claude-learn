# keepalive_timeout

So the keep alive timeout another front end timeout here and this one is easy to understand but because you see when in the previous days on Http every Http request response was accompanied by a TCP connection, by a dedicated Http TCP connection. And that was expensive because every request you have to do a three way handshake and then send the get request, get request or post request, and then the server will respond. And then you close the connection. And if you do this in a modern website where you have thousands of requests to make, you're going to spend most of the time destroying and opening and initiating TCP connections. So Keep Alive was invented by the Http protocol to keep the connection alive. Right? So when you send a request to get Http one and by the way, keep the connection alive, which I believe now it's the default. It's the default is connection. You have to keep it alive until either the client or the server decides to close it. So now when you keep the connection alive, right, you make this a get request. How long should I wait and keep the idle connection alive before I say, you know what this is? Too many idle connections can also take resources. You need to check these idle connections for idleness and any resource consumption, really. And the proxy side, you need to look at it very carefully. You need to I mean, it doesn't matter if it's one client, but if it's thousands and thousands and thousands of lines doing the same thing, that's when you need to revise the keep alive time, right? Maybe minimize that, let's say. Okay, what is the nature of my request? Am I serving a website? If it's a website, then well, how how often is the user active on this website? Is it just a refresh? Do something for a few seconds and the client and then done. Yeah. Maybe keep the connection alive for for just five seconds. Right. But but the moment they start being active then that keep alive timeout will get reset anyway so it becomes a matter of measuring the use cases and okay if an idle connection was there for 10s, then the second the 11th second someone wanted to use and it's closed. Then they have to establish a connection again. Maybe that's fine. Let them establish the connection again, right? Maybe 75 second, which is the default. I think you can put a zero which disables keep alive clients, says hey, sorry, you can you you always have to establish a connection every time. Right? It's kind of bad if you think about it. But there are some use cases that I've seen that people disabled keep alive clients. If the client stayed idle, if the client connection stayed idle for more than 75 seconds, which I think the default, then what are you going to do is just close the connection. Right? There is also another configuration. Keep alive to set the client side, keep alive as well. Right? Which is something you can you can control if you want to. So this keep alive sets this front end server keep alive connection while the other party which is the timeout the headers is called the response header field where if you respond back to the client, you can also set the keep alive at the client side. How long do you want the client to sit before it closes its connection? And I can't think of an example where this could be different, to be honest. You have to be the same, but might be wrong there. So yeah, that was the keep alive timeout. Let's jump to the next one.
