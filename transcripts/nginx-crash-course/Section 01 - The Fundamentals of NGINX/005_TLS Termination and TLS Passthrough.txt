# TLS Termination and TLS Passthrough

Let's talk about TLS termination versus TLS passthrough. So we start first by defining what TLS is stands for Transport Layer Security. And this is the de facto way of encryption between two parties. If you want to establish encryption, most people use TLS, and every time you see Https on the when you visit a web browser that is TLS and it works by using symmetric encryption for communication. Right. So symmetric means the client and server has the same key. Now, you might have a question. Well, how can I share the key? Right. It's like I cannot send the key. Right. I cannot let one party generate the key and send it plain text because anyone in the middle can see it. That's just bad. And I cannot encrypt it either, because how does the other party decrypt it? It needs some sort of another key to decrypt the key that we encrypted. Right. So that is where there is another method of key exchange algorithm called Diffie-Hellman. And it uses one flavor. It's called asymmetric encryption, which is public private key, Right. So it uses this initially to exchange that symmetric key to a certain extent. Right. Exchanges a master hash that that we use to generate the symmetric key. But you might say I'm saying why can't why are we encryption in this? Why can't we just encrypt with asymmetric encryption altogether? Because we don't have to store anything? Well, this thing is very slow. This thing is very fast. This thing is not designed for long term large content encryption. This thing is. It is. So that is TLS and. These guys do not guarantee the symmetric encryption and asymmetric encryption. You cannot guarantee. Who are you talking to? There is no method of authentication, so anyone in the middle can just stop the key exchange, for example, and then reply back and say, Hey, oh, this is actually the server and intercept the connection and actually decrypt everything by performing a termination of TLS sort of, right. So we needed a way to authenticate that, Oh, is this really the server that I'm talking to? And now you need some sort of a certificate to prove that the server is really the server and. No. And this certificate nobody else has except the server. Right. So, so the server will, during the handshake of the TLS, will reply back with its own certificate that is only signed by a certificate authority that nobody else will ever get. Right. So Google.com will have a certificate authority signing its certificate and nobody, no certificate authority will ever sign like me claiming to be google.com. Right. Unless the certificate authority turns to be shady. Which Guess what? There was a couple of them that got banned. Right. So this is just the server authentication client can be authenticated. And that's when you call this fancy thing that's called Mtls Mutual TLS, but it's outside the scope of this video. So let's talk about TLS formation, which we kind of talked about in the Man in the Middle case, but this is more legitimate, authorized way of doing the termination. So Nginx uses the has the Https, right. Let's assume that has TLS, but the back end is just pure Http. I just decided not to secure my back end. You can choose to do that. Nothing wrong with that at all. If you have it in a privatized on premise, it becomes really questionable if your back end is Http and it's in the cloud, especially with the cloud is everything is shared really and and the networking aspect, unless it's really virtualized and purely secure, then you can start question that you can be safe. But yeah, you can definitely be http on the back end. Now, Nginx, what it does is it actually terminates the TLS. It's like, Hey. Client. Talk to me when you want to negotiate this key exchange thing and I'm going to decrypt your traffic, right? So anyone sniffing from the client all the way to Nginx cannot see anything. It's all encrypted. But I'm going to decrypt the traffic and then send it unencrypted to the back end. Now, anyone between Nginx and the back end sniffing can see it. But who's that? Right? Who's sitting on my premise looking for. That's probably my admin. You might be fine with that. All right. So that's one way to do it. Another way is. An nginx is and the backend is also TLS https and this is what is recommended on the cloud. If you have everything on the cloud then yeah, even backend you should, you should secure that as well. But now nginx terminates TLS right. Decrypts the traffic, optionally rewrite, add headers, add more cookies, whatever and then turns around and re-encrypt the content to the back end because that's a different channel between this channel and this channel is a completely different encryption. So there is decryption encryption going into this mix. There is an additional latency, which means that means whatever ciphers you pick for your algorithm, hey, better be fast. I think Chacha is now the fastest as per a report that I read. A is a is is good, right? Don't use RSA for key exchange at all. I mean, there is an SSL lab which we're going to go through as well. It's going to yell at you. So, hey, this is a bad algorithm. Don't use this, don't use this, remove this algorithm, add this algorithm. Oh, we're going to go through that as well during the exercise. Uh, don't know how I haven't to engine X here, so engine X engine X can look at the this is an emphasis Nginx engine X can look at layer seven data and rewrite headers cache but needs to share the backend certificate or at least has its own right because now you're now hosting TLS uh, session on the Nginx and needs to prove itself. The Nginx needs to say, Hey, this is me. How do you do that? Well, if you have a single domain right then you have to kind of share the backend certificate with Nginx. That is just sometimes a no no for people. It's like, hey, what? What? The certificate That means I should give you my private key. No, Nginx, you're not getting my private key. I don't trust you. Some people say that. Hey, engine X, if you really want to look at this stuff, generate your own stuff. I don't care. I'm not going to give you my private key as a backend. You generate your own private key. You sign your own certificate with some certificate authority and come back. You have your own domain, right? Again, nothing wrong with that. But just just understand that what you're doing is sharing private keys and this is just a no. You never share private keys with anything else except you. Right? So that's that's another thing that you have to look at when you do termination. Right? It becomes a little bit cumbersome with multiple domains. But yeah, that's the price we pay for privacy. So let's talk about TLS passthrough. So if I don't trust Nginx or I don't trust the person hosting Nginx for me. Then you do pass through. Hey, my backend is TLS nginx. You are dumb. You are just a dumb pipe. Just pipe everything through yourself all the way back to me. So nginx now proxy streams a packet directly to the backend. It reads that. Okay, this is a TCP handshake. I'm going to stop here. I'm going to establish the TCP handshake, but if you're doing a TLS, anything that comes with TLS. Hello, I'm not going to respond to you. I'm not authorized to terminate the TLS. I am a pass through, so I'm going to take that TLS request and forward it back to the backend. Just like poop. Poop just like really just is just a pass through, right? And then the backend will receive the TLS and then response back to the to the Nginx. And Linux is effectively a man in the middle here but it cannot see anything. The encryption is end to end. Right. So it can't complete the encryption completely. So the TLS handshake is forwarded all the way to the back end as we talked about. It's just like really, it's a tunnel at the end of the day, Right? But here, when you do this. You can no longer cash because, hey, the moment you establish an encryption between the client and the server, the back end. Now the Nginx cannot really read the content anymore. It cannot see layer seven content. It is now a pure layer for checks only. It can look at the source IP destination, IP, that's it, ports and stuff. You can't look at the content, it's encrypted and you cannot see it, so you cannot cache it. But it is more secure, right? And the Nginx doesn't need the back end certificate. Plus you can there is one disadvantage for this is that Nginx can also because it doesn't have the context and layer seven, it cannot share the backend connections. It has to make private connection for every request and that can be costly. Let's jump into the next lecture which is nginx timeouts.
