# NGINX Internal Architecture

What's going on, guys, In this lecture, I want to go through the internal architecture of Engine X, So this lecture is going to be slightly different. We're going to go deep into Engine X, how the worker processes are sped up, how the connection management is done and stuff like that. Exactly. Understanding how Engine X deal with these large number of connections based on the number of CPU CPU cores that you have on your server. Excuse my voice. I have a little bit of a sore throat fighting it, but let's get into it. So. This is not entirely. I try to simplify this. The architecture. The engine X architecture starts with something called the master process. When you spin up in Gen X, it starts a process that corresponds to the rest of all the processes of Engine X. There are two things that I didn't mention here related to the cash management of Engine X, So when you spin up Engine X, it spins up a process or two processes to manage the cash. That means reading the cash from disk and also refreshing these caches. But this is I thought this is not really what what is more important, what is the most important thing is actually what we call the worker processes or the child processes. So this is these are the processes that actually do more most of the work, you know, and when you sit in Gen X to auto mode, which is the default, right, what will happen is. A number of processes, worker process will be spun up based on how many hardware threads do you have, You know, and this is the hardware speaking. Basically when you have like a processor, it has multiple cores. Let's say you have four cores on your CPU and usually at the hardware level, Intel, AMD or Apple sometimes does time slicing where even the core itself, they split it into something called the hardware threads where they present the CPU to the process, to the application to the operating system as actual multiple cores. So each core becomes two hardware threads, right? Because they can do at that level, they will simulate almost in this particular case, eight cores. Right? So that's why if you have four cores, you're going to have eight worker processes. If you have eight physical cores, you're going to have what, 16 and so on, right? But for simplicity, let's say you have four hardware threads, so that's what, two cords or sometimes even. It doesn't have to be like this, this duality, right? Once, if you call, can have one hardware thread if you disabled was a call hyper-threading. I believe if you disable hardware threading you can have 1 to 1. And this is a choice you make based on performance and stuff like that. So we have now for worker processes and the kernel sits right behind all of this stuff. So if I have a client, this is presented by this laptop, want to connect to Engine X, let's say on port 80, that's where Engine X is listening. I'm hiding so many complexity behind this stuff here. But what's happening is when the client establishes a TCP connection, which is the sense and EQ, EQ, the send request will go through the kernel and the kernel will put it in something called the Syn queue. And and that is kind of managed by the kernel, but it was allocated by engine X when it says, Hey Colonel, I'm listening on Port 80, please. Anything that comes in Port 80, please send it to me. And when that happens, the kernel will reply back with the syntax, basically to complete the TCP handshake and then the client will reply back by an EC completing the three way handshake. And when that happens, the kernel will move that fully fledged connection to something called the accept queue. And again, I'm not showing any of that stuff just to simplify the picture here. Right. And in that queue. So there are two queues, the same queue and the accept queue. And when that connection queue, the accept queue happens. Right. And again it's in the kernel, the engine X is responsible to get the connection from the accept queue says hey, I want this connection back and this is where you need basically, you know, hours and hours to discuss this stuff. You know, how does engine X, how does the process picks up a connection from this connection queue is an art by itself. There are so many methods that are so many discussions, there are so many papers written about this particular problem. How do I accept as connections as fast as possible? You know, so many stuff here, and I think it's a little bit out of the scope of this, you know, introductory course. I might create another course to discuss this in detail if you want. So what happened here? Eventually one of the worker processes will pick up this connection, and that connection will live in the worker process itself, you know, as a pointer, if you will. It's a it's a file descriptor. We call this an integer value pointing to that connection. And the worker now is responsible to read data from this connection. Again, how this connection is picked up by what worker process is is a completely different story. There is load balancing involves at the kernel level right there. Sometimes all the worker processes are listed on the same port and the kernel actually distributes the connection between between other worker processes. Sometimes the the worker processes, actually the master worker processes is what is deliver the connection and it will distribute the connection based on that. But regardless, we have a worker picked up a connection. Now it's ready to read stuff, right? And it's actually consumed the request. So when you send a request, you consume it. You understand it's a request with the layer seven or layer four. We talked about all that stuff, right? And then we'll take that stuff and then it will decide what to do. What can it do with the request they actually sent? And now we have clients sending. Right. So we have the client that's establishing a connection to work or for this client, establish a connection to. Worker three And it started sending a request, write the actual HTTP request, for example. That request will go through the kernel. Obviously, it will go through the TCP connection that we talked about. All of this data will live in that queue. Right. And a specific buffer. And I talk about all of that. And if you're interested to know more, check out my networking course. I talk about the details like these buffers and the window sizes and all of that nitty gritty detail about TCP. The worker process will read this request because it has to actively read it. It's not a push model. The kernel never pushes stuff to to the process. It's not like, Hey, I have data, here you are. No, it's a pull model. If you well now and this is again, there is a venting involved that is asynchronous IO involved is this new thing that's called IO boring which which effectively creates a completeness. Let the kernel does the reading and it will notify the worker process. Hey, I have I have data for you and listen that I said I have data for you. I never said you. I have a request for you. Right. There's a big, big difference between a request and just a stream of whites. You got to understand that that's where layer seven and layer four comes into a picture. And here we're talking about layer seven, right? We need to terminate the data. If there is TLS, there isn't in this case. Right. We'll get a terminated and decrypt it and understand it and then take that data and understand that this is the start of the record. This is the end of the request. Because when you look at layer four, the stream of data is just a stream of bytes, just garbage. The application in generic in this case needs to read, read, read. Oh, there is a set of requests that is the end of the request. It needs to understand how to pass the SDB headers. And this differs from HD one one versus JB two versus JB three. Right? So B three is a completely different protocol. It's a quick. But the cost of this parsing lives right here. So that's CPU. All right. So each worker process is pinned to a CPU, and we never talk about why why we have one worker per view, because we really need that worker process to live in a C in a core and never leaves it as fast as possible. Never leave that CPU core. Why? Because there's something called context switches. And if you how does I know I'm going to go all over the place? But this is all related. You know, if this workers tried to pass this request to understand how it actually how it needs to respond to it, it needs to pass it this person before that, it needs to actually decrypt it. And this is where cryptography comes into the picture. So it needs to use whatever symmetric key encryption is used to decrypt that content and then parse that, find where the request actually is. All of this requires CPU, and when you do this operation, the sets of instructions that you have, this parsing logic is basically just a set of instruction and it lives in the CPU core and the CPU will just fix one instruction at the time and then and keeps executing it. If you have a lot of processes and a lot of threads, then the CPU might kick you out, you know, put you somewhere else and back in memory and then put some other process to execute its threads. That is called context switching. You switch the CPU off to do something else and you are put on hold as a process that is expensive and it's not really expensive for you if you're doing it one or twice or 300 times. But if you're doing it millions and millions of times, especially if you have a lot of threads and they are competing for these cores, then it becomes a problem, right? That's why we have 1 to 1 mapping here so that as long as you don't have anything else but in Gen X running on your machine. Right. That's the goal here. We have to have one process, you know, pinned. Right. With that said, now that we know this stuff, we have a request. Worker three Got a request, right? Worker four Got a request. It has options. What can it do? Well, some requests might be to the web server aspects of Engine X. Hey, go and read the content from disk. And that is that particular request is an IO bound. We called IO bound. Right? Technically the request passing itself and the corruption was CPU bound, but it's not as much. Yeah. Unless you have many, many requests coming in from that connection, then you start feeling it. But there are certain logic in this case. Is I open? I means, hey, I'm sending the request to the desk to read this file, this HTML page, the CSS file, and then I'm going to respawn back to the user. During that reading, I spend, I don't know, 10 milliseconds. I'm waiting. I'm not doing anything because Engine X does always a venting. It has an event right where it will send the IO request and we'll just sit down and do the C. The process can do some something else. Right. And this technically goes through the kernel as well and I just didn't draw it because the IO is a kernel call. Right? Alternatively, you can create something from a backend, right. Where hey go. And I don't know, you're just doing a microservices right where this is a reverse proxy in this particular case where this is terminating the thing, go and make the request on the actual back end. That's also an IO request. It's just that other network, instead of going to disk and you get back the response and then you write back the response and the writing of the response is also, you know, the cost of writing to the socket and the cost of re encrypting, right. The content. Again, this particular is not there is no encryption, but most cases you will encrypt because TLS will be enabled. Right. And the cost of building the HTP response, all of the CPU intensive stuff and then encrypting it and then sending it back to the client. So that's basically how engineers work behind the scenes. And now you can just multiply that by many, many other users, right? Many users connect. They will be load balancer across these four workers in this particular case. So you can have thousands and thousands of connections per worker. So one worker might have, I don't know, 120 or 200 or 300 connection. So it will just flip between connections and to do multiple work. Yeah. So someone just sent me a request. Here, let me pass it. Someone just send me a record here, let me pass it so you can see that the engine X is doing so much. Work, you know, to actually pass these requests. So CPU, you know, finding out the limit here to your CPU and finding out how many processors do you need to spin up, you know, and how what other stuff is running on your engine is really critical. Hope you enjoy this lecture. Got to see you in the next one. Enjoy the course.
