# How many Backends do we need for NGINX?

Welcome to another Q&amp;A lecture for the introduction to Engine X course. This is a very interesting question. I thought I'd make a lecture about it. This comes from has seen if there are a number of clients wanting to establish a web socket connection immediately to Engine X. Well, we need exactly end servers on the back end or a few or servers to serve those in class. And this is a very good question and I answered Many clients can be served with one server. It depends on the nature of the back end load and processing of the resources. You can add more servers on the back. As much as I wanted to expand on this answer a little bit, and the interesting part right here is you have web sockets, which is by design for socket, you have to do a layer for proxy or load balancing. And when you do that, there is a limitation on layer four proxy, which is the moment the client connects to Engine X, engine X, it knows that this particular port is configured for load, layer for proxy. That means all bets are off, it's layer four. So I don't really I don't need to know know anything about this protocol. So my job is whatever stream of data I have, I have to stream it to one and only one backend. I cannot load balance the stream content on this connection on different backend because it will break, right? Why It will break because if you do that then assume it's your streaming HTTP connection and you're in layer four, which is the TCP layer. Right? And the request will become a bunch of segments. So how would you know that? Okay, let's say I'm going to balance by segments, right? So one segment here, one segment here, one segment here, one segment. If you start doing that, then half of the request will become here, not half of the request. An actual half request will go to this innocent and the rest of the request will go to the owners. Because how do you know when to stop reading the request? Because that is a knowledge of the layer seven. The application knows, okay, I'm about to read a request, this is get slash, blah blah blah. Should be one. This is the headers. How do you know where the start, where the request ends and or start and end? This is a knowledge that comes from the application. The moment you said you are a layer four, that means your proxy in TCP. And when you do that you cannot make assumption all the stuff streams goes to one backend for stateful proxy. With that out of the way, what are you going to do? Is you going to reserve a dedicated connection for the client who just connected to you in obstacle? This is important because what will happen is. Let's say I have ten clients. That means ten connection, ten stateful connection web sockets to Engine X. That means I can load balance the connection themselves. Right. So that means let's say I have ten servers, so each one will get a connection. That's fine. But you can definitely share. Right. Let's say we have an 11th client, and the 11th client will go to server one. We can still have the 11 client connection go to the same server. Right. And how many servers do we need? Really? I don't know. It really depends on your workload. You can have one backend and can server a thousand clients. 1000 connections. But it depends like what are these connections actually doing? How much CPU are they doing? Is that IO bound or is it CPU bound? The request type. Rebuilding a game is the game is heavy on CPU processing. Is it using GPU processing? What kind of things are we doing? Are we building a chatting app? Is it IO bound? Is it CPU bound? You're going to think about that because what does it mean to for a backend to be overloaded, right? It's like, how do I determine that? If the CPU is 100%, that means new request cannot be received by the listener thread and as a result cannot be forwarded and accepted from the same queue. Right. Or from the except CPU to the application. So based on that, hey, I want a connection to be accepted. I want connections to be processed. And based on that you can make the guess like, Hey, I need maybe ten requests to serve a thousand clients. That's where trial and error connections and understanding your application and the logic is what matters, I think. So I thought I'll make a I'll talk about that. I think it really depends. The short answer is really depends how many clients I don't think you need. If you have any client, doesn't mean you have to end server. That's just an overkill, right? Unless if you have if you have a limit that you're going to only get 1000 clients and you want absolute services, you know. Right. Absolute 100% service time up uptime you can have dedicated maybe Docker containers. Doesn't we have like a full instance, right. You can share the memory, you can have like a CPU one CPU four gig per container and you say, Hey, this is enough for my app, this is enough for my game. And that's basically comes back to the how your back in the authored you have to write optimized and lean code that can run efficiently. What we enjoy in this lecture I see you in the next one.
