# Spin up a WebSockets Server

All right. Finally to the actual demo work. All right. So what are we going to do here is we're going to spin up a normal WebSocket server using Nginx. We're going to build it from scratch. You can totally skip this lecture if you don't want to know how a websocket server spin up, you can use the code. I'm going to reference the source code in the lectures for sure, but you can totally skip that if you don't want to. So let's jump now. Let's jump to the code and run our own WebSocket servers. And once we do that, we're going to load balance those websocket server with Nginx. All right, guys, how about we jump into it and start spinning up our first web socket server using NodeJS? Obviously, to do this exercise you need NodeJS installed. Just go head to Node.js dot org and install the latest NodeJS and you should be up and running. Really don't need anything else. Pick your favorite visual ide. I like Visual Studio code. It's just really nice. And then create a folder and we're going to get started. I'm going to share the source code at the end of the video for everything so you can find it in the lecture notes, I believe. So let's go ahead and create an index dot JS file here. So what I'm going to do is really just import the. Http. Library. It's very important because the websocket is built on top of Http. So we need really an Http server to get a websocket server. So let's go ahead and just create an Http server object here by doing Http dot, create server. Very simple stuff. We're also going to need the websocket server. So let's go ahead and do a websocket server require websocket. So we're going to require the websocket library and then we're going to get the server object out of that. And you don't really need semicolons in JavaScript, but I'm just getting used to it. So we have the WebSocket server class. We really need to have a websocket server object, right? Just like we did with the Http server. So the way we do it is just do a websocket server, right? And let's just be consistent. I think it has to be like that. Do the camel case websocket server equal a new websocket server and then really just pass in the Http server. And I think kids these days do try tricks like this, right? This is exactly equivalent, right. To having this. But believe me or not, I really like this better. I just find it very difficult to parse these new syntaxes that JavaScript language comes up with every single day. I know it could be just be being a boomer, but yeah, websocket server. We have a websocket server, we have an Http server and there you go. The next step is we need to listen on an Http server obviously. Let's pick a port, random port. We're going to change that later. And whenever we're done we're just going to do console dot log listening on port 8080. We're going to change that later. We're going to make it dynamic. I just want to make sure that my WebSocket server is running first. So up until now we just have a normal Http server that is listening. Right? But what we don't have is an actual event that listens to the upgrade request that we talked about. And to do that we really need to do WebSocket server dot on and when you receive a request you're going to get a request object. And here's what we're going to do with the request object. What are we going to do here is we're going to accept the request to upgrade. And to do that, you're going to do request dot accept, and then you pass in what protocol? Remember when we say chat, when we say chat, super chat, these are like normal protocols now, except basically everything. And then you can also configure what origins and what cookies you want to effectively accept. Not every request like you cannot you don't want to accept WebSocket requests from this particular IP address. But in our situation, we don't really care. So any one request dot origin or you can do a star can send us some stuff and the act of sending a request will give us a beautiful connection. And I'm going to declare this as a global variable. Let connection equal null because I want to reuse this connection as the full duplex one, right? This is what we really need to work on. So now all we have to do is just on we do an event on the connection. If I received a message from a WebSocket message, then. I want to do this with the message. So this is what the core app will do in this case. Let's just go ahead and do console dot log. I received a message. Right. And what is this message? Really? It's the data. But there is a piece of data that we want. This is the the actual object. The data itself is UTF eight data because it's really, really could be anything, right? Not just Ascii though. So emojis, you can set anything. So this will be a converted to a UTF eight and then you can print it. So our servers, what it does is it literally if you send it a message, it replies back immediately with the just say, Hey, I received a message, but how do you reply? You just do connection dot send. The client itself will have a corresponding connection that we is going to listen and we're going to also send data to. So let's go ahead and say okay, A. Hey, client. I received your message. Right. On this port. And this is very critical. I want to specify the port because I want to know which server are we hitting. Right. And so here's what we're going to do. Let's let's actually declare the port right here to be 80, 80, and let's just replace it all over. Because what I'm going to do is I want to make this port the port dynamic. So I'm going to paste here, paste here. And where else did we use the port? Obviously, right here. Okay. And we don't need these guys. Oh, so now they'll have the porch. Awesome. This should be it, to be honest. Let's go ahead and test our server. But before we test it, we have to really initialize NPM and all that jazz. Let's go ahead and do NPM init dash y and then do NPM install. I've got to specify what to install. I guess we only need to install websocket. Http is there by default. Right. And then npm. Not really. Just do node index.js and you can see that we're listening on port 8080 by default. I'm going to change that to make it a parameter because I want to listen on multiple servers, but let's test our server. How do we test it? Just go to the browser. All right, guys. So in order to test this, we really just need you can build an actual client, but I like to test from the browser itself and just open a blank browser page and go to dev tools. And then in dev tools, really just create a variable called AWS and then create a websocket object. This is a built in in every browser, right? And what we're going to do is I'm going to use the WebSocket protocol, I'm going to hit localhost because that's my server. And on Port 8080, that's where we're listening on. Now once you do that, the connection seems to be accepted and we can take a look at the network that we said, okay, look at that 101. We get switching protocol. That's the request that we got in. We requested the upgrade, Right. We requested this and we want WebSocket. Awesome. And we got a response that says, okay, the connection has been established successfully, so our server is actually working correctly. The next thing we're going to do is just do an onmessage This is how you wire an event. So when the client receives a message, I just want to print it, right? And now how do we send the message? Very simple. That sent Hello. And immediately we get a response from the server. Received your message. Hello on 880. Now go to the client. Hi. I received a message. Hello. Cool, cool stuff. So let's just change the port to to make it a little bit more dynamic and as an argument so we can do process dot argv sub two because these are the array of all the arguments you pass to Node. The first one I believe is Node JS itself. The second one is the file, which is Index.js. And so the third 1012 is whatever we want, and in this case it's going to be the port. So I'm just passing out the port. So if I do now, Node Index.js two, two, two two. We're going to be listening on port two, two, two, two. So this exact same thing, but we're listening on port two, two, two, two. And I can do this listening on port. Three, three, three, three. How about I want actually multiple servers. How can I do that? Well, very simple. All we have to do is just do node index dot JS two, two, two two. You do an end node index.js 3333. You do an end index dot js 4444. And let's have another one. Sure. Node index 5555. So we have two, three, four, five. We have four servers. Now once you do that. We're listening on all these four servers. Let's just test one of them and then we're going to jump to the next lecture. Now, if I do. Connect to Port 88 is going to give you an error. So let's go back to three, two, two, two and message. Right. Let's just wire the events or send anything. Hey, I receive a message from two. Two, two. Awesome. So if I do 3333. Awesome message to send. Hi. I receive message from 3333. You might say this is silly, but it's going to be very useful when we put nginx on top. The moment you put nginx or the reverse proxy, you have no idea what backend your token to. This is going to give us a clue what backend did we connect to? And based on that we know how the load balancing is actually working, right? So now we know we have our websockets running up and running. All good. How about we jump into the layer four configuration of my Nginx?
