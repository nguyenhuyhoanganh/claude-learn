# Configure NGINX as Layer 7 WebSocket Proxying

All right. We know how to configure Nginx as a layer for WebSocket proxy and load balancer. We've seen that. How it works. How about we actually do a layer seven websocket proxy? But first of all, what does that mean? In this case, we're going to intercept the path and route appropriately. Here's what we're going to do. If you're going to Http localhost, this is a an index page. We're going to actually show a page. And if you go to the same server localhost, but you're going to slash Web WebSocket app, we're going to actually go into a web socket app. So this is not possible with layer four because port 80 is used for WebSocket. So if you do an http localhost on a websocket connection, it's going to freak out. Wait a second. This is this is not an Http. What are you doing? This is this is an actual websocket connection, so it's going to freak out. But in layer seven, you can actually do these kind of tricks, right? You can intercept the route and route appropriately. So you can you can if you want to. We're not going to do that. But if you want to, you can spin up another set of WebSocket server that does a completely different websocket application. And if you go to slash chat, you can go to that sets of server to that set of backends. This. You cannot do this in layer four because Port eight is blindly tunneling, right? And we only need to. We only know to tunnel to these backends which are websockets. We cannot differentiate enough. There is no enough metadata in layer four to tell us, oh, I want this versus I want this. There is just ports. You can spin up another port if you want to, but it's going to be ugly in layer four. So port 80 becomes a Http port 80. 81 becomes websocket port 8082 becomes the websocket app for chat. You can do that, but some people don't like to do that. How about we jump into the code and let's do this? All right, guys. So in the previous lecture, we learned how to deploy Nginx as a layer for proxy, and we know how we see how sticky this thing is. So let's go ahead and stop Nginx. First of all, you can send a signal to nginx this way nginx dash is stop and this way all the nginx sessions will be stopped. Sometimes times the nginx processes will remain. So you can use kill all. At least this is in mach and you can do kill all nginx that will just make sure everything is killed. But I don't have any matching processes. You can do the same thing for Node, right? I definitely did have some stale nodejs processes. You can kill it this way if you want to. And windows. I believe it's a task. Kill dash im nginx dot txt and the dash f that's how you kill it. And this obviously this is going to get an error here because this is Mac. All right. So let's go ahead and. Create a new configuration and let's call it, I don't know, w aws dot cfg, which indicates that we are a layer seven and we talked about layer seven proxy. It's really we're going to do this through an Http context. And literally let's just let's just copy the entire code really, because it's very, very similar. The only thing we need to change is instead of stream, which indicates that we're streaming to the backend immediately. No, we're actually want to do a layer seven Proxying. So the Http and the the only difference here is what we're going to do when we proxy we when we listen to Port 80, we can't do this anymore. This is not a normal book. We have to specify what protocol is the backend on. So let's go ahead and and delete that line. And I'm going to insert now that we have actually a layer seven, uh, proxy, we can do a location, we can do hey slash websocket app if you actually going to WebSocket app, execute this if you're going to this WebSocket app WebSocket app, let's call this chat chat app and chat and let's say for simplicity, the app is. These two servers and the chat is these two servers. So let's go ahead and do this. So chat and then we're going to copy this thing. So x, y, y. All right. W is chat. So now this is called let's call this W app and this is W chat. So we have two backends I want. This app is just these guys to two and three three and you four, four and five five. So we can actually play some games here, which is awesome. So if you're going to a slash app, I want you to proxy pass to Http. WS app. And when you're going to chat, I want you to proxy pass to what the value is chat. So now this is this has to be a backend that exists right here. Obviously it exists and this backend is already exist. But this is this is not enough because here's what we're missing. You're proxying just http stuff. We need more information on the backend. Now the nginx is actually its own layer seven proxy. We need for Nginx to understand how to do websocket stuff and here are the lines that we need to do that. So I just copy these lines. So let's, let's explain one of them http version 1.1. That means hey, I want to set the Http version. That's very nice. We want to set the upgrade header which we talked about. Just take the variable, whatever the upgrade header is, right? The actual upgrade protocol. So this works actually for any protocol. So this might be actually dangerous if you do it this way. So the more the better way is to effectively just literally just say WebSocket. Right. I remember there was a because of this line, there was an attack that got executed on Http to Https to smuggling have done by this particular line because sometimes you don't really want to upgrade any request. You want Nginx to only upgrade a websockets for example. So you want to hardcode this. This line will upgrade any web web, any any protocol from Http to the protocol. So http to H2 to be specific like that that the H2 c thing, the Http to on clear text, which you should not really use to be honest. Right. And the WebSockets, there are other protocols as well. So make sure that you only upgrade where you absolutely need. But since this is a test, let's upgrade everything. And the third line is set header. We're setting the header for the connection header and we're setting the upgrade. This is exactly identical to the handshake that we talked about, where we're setting the handshake in this case and then proxy set header, we're setting also the host, whatever the host we received, just pass it through. Right. And we'll need to also, uh, copy this same thing here, insert and paste. Awesome. So now that we have all this in place, let's save my project, and then let's copy the path. Then go ahead and run it. How do you run it? Nginx Dash C. Let's see if we didn't get any errors. Oh, we got an error. Unknown directive. Localhost. Okay. Why are we getting an error? On line 27. Oh, why did I call it localhost? It's supposed to be location. All right. I'm just used to writing localhost, I guess. Right? There you go. It's running. Let's test it. Let's go to the browser now in the browser if I go to this guy. Remember, we only have two directives here. We have slash chat and slash app. So let's try the app. And then I'm going to do just on message listening to my message. And then we're getting something. We got a response from two, two, two, two. Let's do it again. This time. I should get a response from 333, three. Awesome. You notice that we're back to two, two, two, two. We never went to four, four, four, four because that's the app. The app is the app. And we go to the configuration. The app only should load balance between this guy and this guy. So it sounds like everything is working correctly. So let's go ahead and do. The birds chat. We'll go to various chat. We're hitting four, four, four, four. So if I do another connection request. We're going to five, five, five, five. So we're now balancing load balancing between four, four, four, four and 5555. Right. There is. No slash, blah, blah, blah, blah. This gives you an error, right? Because it's like, hey, this is not really a valid thing because Nginx only listens to these two. Let's add an index.html page. All right. Now we have two websockets backends. We demonstrated how important the the routing in layer seven load balancing here, but we don't have an HTML page. You can do more than that. Right? So we're going to do here is when someone go to the location slash, we're going to serve them an index.html page, a simple one. That's great one actually index.html. I think you do HTML five and just let's do hello world H1. Hello world. Very simple one. And let's serve it. How do we serve it? HTML pages. I got to remember that. I think you do route the route and then render the path. Right. So let's just go ahead and copy path. And I keep forgetting if this is the actual path or the path to the actual HTML page. So let's try both. Let's try first the path itself. Right? So I'm going to save obviously now that we made a change to the configuration, we can do two things. We can stop Nginx altogether or we can just reload it as reload. Now that Nginx knows this thing, if I go to localhost 880. Look at that. The localhost page. Fetch me a page and we still. Can work with chat. Right. The normal app that we have. And we have another app on the AWS, another WebSocket app. Isn't that cool, guys? Yep, this is the other app and both of them are load balanced. So finally what I'm going to do is I wrote a small HTML page that kind of does everything what we did doing the websocket stuff but through the HTML page. So I'm just going to show you the code directly. So I just wrote this code beforehand because there is no point showing you some normal HTML stuff. This is really the exact same code that we written, except it's in a pretty app, right? It's just a pretty HTML and it has a text box. And when I click on this text box, I start sending messages. That's, that's, uh, you know, uh, and it has a, it has literally a text box and a div element where we show the output. And as I write anything in the text book and I press enter, that's the code 13, we will send a message to the server and we get a get the response back. Really, really simple stuff. So let's go ahead and refresh. Now we have Hello WebSockets. So if I do test enter. This is easier, right, to test. So now if I refresh and I type something else, now we're hitting which the other server refresh, it's much easier than actually typing all this code in the browser. Right? Look at that. We're load balancing between this. But now I want to shift to the other app to the chatting app. Very simple. All we have to do is just change that to a chat, save refresh and voila, now we're load balancing between five, five, five, five and four, four, four, four. And you can see as long as I'm typing now, I'm using the same session, same socket. So it's always going to reply from the same server. Now once I refresh and now hooked into another server. So it's still it is load balancing right at this level. All right guys, that is all the demos we have today. Let's go back and summarize this course.
