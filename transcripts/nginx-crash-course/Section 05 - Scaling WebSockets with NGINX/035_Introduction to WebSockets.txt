# Introduction to WebSockets

Introduction to web sockets. In this lecture, I'd like to talk about what is the web sockets protocol and how does it work. And these two pieces are very critical to understand how to actually scale the thing. You cannot scale something, yet you don't understand how it works. So, um, some people get bored of this, but I like always to go back to the basics and it's like, okay, how is Http 1.0 really used to work the original version? The very first version. There are versions before that, but this is when we first ship it. There's a client, there is a server. We established this lifetime and the client wants to send a request. So what it does, it opens a TCP connection, right? And then it sends the get request. Say I want to index the HTML page on Http 1.0 and the server says Sure. Here's is I read it from the desk or cached in memory and then I'm going to respond to you back. And here's what happens. The design, the original design was since it's stateful, there is no reason to keep the TCP connection. And this was in the 1990s where the web pages didn't have much stuff in it. So you would send one thing and you get it and there was no need to keep the TCP connection anymore. But unfortunately this is not the case anymore. When you want to send another thing, go ahead and reopen another connection. Right? And then send that, Hey, why did you close it? I want image one because I just fished index.html. There is an image that I want to download. Well, okay, you opened. Here's the image one. Okay, let's close it and then do the same thing again. Hey, how much do you get the idea? This is very expensive. Opening and closing the TCP connections. There is an overhead to that and you can add an extra overhead. We didn't have encryption back then, but SSL and TLS in every handshake that is very expensive. There is a sin synack an EC. And then the the TLS handshake, it is very expensive. So we quickly abandoned this model and we added something that called a keepalive header to solve this problem. And we made it kind of mandatory. And one one says, okay, you open a connection, you keep it alive until one of us agrees to close it. So we're going to do okay, give me the index.html page. There is here is it? Hey, we don't close it. Keep the same TCP connection, then send some other data in. Hey, I want image one. Here it is. Here's image two. And here's image three and so on. And then after we are absolutely done, we close everything and we'll be done effectively. So let's take a look at what WebSockets protocol is and what does it do in the same scenario as the Http. So we have the same configuration here. We have a client and a server and HTP server that has the WebSockets capability and we're going to explain what that means in a minute. So what happens here is we open a TCP connection very similar to the Http context, and then we do something called a websocket handshake and we're going to dive deep into what that really means in a minute. And I forgot to turn off my. Laser. There you go. Much better. So we're going to we're going to explain what the websocket handshake is. It is effectively just this thing is just an Http connection on top of on top of it, there is the Http context. And then we do something called the websocket handshake. And this is very critical to understand because engineers need to understand what that is. In order to proxy effectively. And once you finish this handshake, this TCP connection becomes a dumb pipe that is just sending messages back and forth. There are no Http rules anymore. You know, the Http rules are being request response or send a request and wait for a response. No server can send information to the client. Client can send something to the server, clients can send something else without expecting really a response. Well, there is some sort of acknowledgement that the TCP stack, which is the layer four stack and acknowledgement and stuff like that, but at the application level, that's what we care about. There is no expectation the server can send something out, the client can send something back and there is no order. It's bidirectional full duplex communication. That's very useful because in the case of chatting where you don't, you can't predict who is going to send the message that's going to be useful. And the communication obviously can overlap and that is absolutely fine. And there is the headers and specific headers for the websocket messages and stuff like that. But that's out of the scope of this video. And once we're done, we close the connection. Let's take a deep look at what the WebSockets handshake look like. And you might have seen this. This is the protocol prefix which indicates that this URL or I this resource is a websocket resource. And for the secure one, the Https one, which has TLS. So really everything starts with a TCP connection, open request, and then followed by this particular special header, right? Which we send a get request, we send a slash http one one because that's the minimum version of a websocket will work on for for obvious reasons. We need a connection to stay alive. Right. To, uh, to enable full duplex communication. And then we send a special header called connection upgrade. And then that special header contains other information as well that I just didn't find space to write them down. That indicates that this is I want to upgrade to a websocket connection. And if the server supports and understands that this is actually a WebSocket request to upgrade the connection, it's going to it's going to respond back with say, hey, switching protocol, there is a special code, let's go 101. So Http code says, hey, all right, we're going to switch protocol. And one might say, why? Why are we upgrading? The reason why we're upgrading is because this is just a normal connection. And any time you can upgrade an existing TCP connection or TCP, an Http connection to a WebSocket connection, that's what we do. And once you do that, the entire pipe for that particular connection becomes just the websocket connection, right? And then that's what happened. And that's very critical because now we have some sort of a stateful request, right? We just made this connection tethered to this back end. Right? Because now when I send some sort of an information here, I can't really consider this stateless. Right? Because now that particular connection is now stateful. Anything you send always must go to the same server, not just server, same process. Because now we we save the state. We no longer the Http protocol. We are a stateful websockets protocol. Here are more details of how the websocket handshake looks like in the wire. So we're going to send get slash chat for example, where slash chat is the Uri where we support the websocket connection. And we're going to explain that this only works really if you have layer seven proxying because we can't even read what a slash chat really is in a layer four Proxying and load balancing. And then we're going to do Http 1.1. We're going to do, Hey, I'm interested in this particular host in case we have, you know, multiple websites listening on this particular IP address so you can have multiple hosts. I want to upgrade this connection to WebSocket and then the connection header says upgrade and there is a bunch of keys. There is the protocol. This is something that. Is just too internal for you, the user. And the back end says, okay, I want to support this kind of protocol, the sub protocol for the WebSocket. So you can have multiple websocket servers that support like, okay, one supports chat, one support logging, one support superjet one support and on a live feed, you know, and then the version and these are like really optional to be honest, these things, right? Not this, this, this, this key is critical for this. And then you specify the origin and then the server must respond with the following http one one the status code 101. And this is the switching protocol and says, okay, I've upgraded the connection. The connection has been upgraded. I accepted this and this is the key. That is some sort of a variation of this. So we know that, okay, we are who we said we are because we took your key and we did some arithmetics on it. And then here's the things I support. I only support chat. I don't support super chat. So this is the client and the server and all of this really is you can write it yourself from scratch using a normal Http server, or you can just use a library that does this correctly and securely. So what are the websocket use cases? Some of the websocket use cases are really chatting. Right? And it's very important if you want to if you want a chat, right. It's a full duplex communication and each party can send text or images or media in a bidirectional manner. We have no idea who is going to send any data. You might say, I can build a chatting app on an Http vanilla Http, sure, but you're going to have to do polling right? It says, okay, hey, do we have a chat for me? Do you have a chat for me? Right. And then if, if you do, if the server do does, then it's going to respond back. If it doesn't, then it's going to respond back. Nah, nah. And that increased the bandwidth on the network and causes all sorts of latencies and stuff. Right? So bidirectional is preferred if you want instance a almost push kind of a chat notifications live feed like let's say you have a blog or a. Video. Live video is a bad idea on WebSockets just because it's TCP, but you get the idea. You can do something that is doesn't have is not bandwidth hungry. Right. Live feed like you're in a conference and you want to just send messages. It's just a feed of messages. See a feed of news that is constantly being refreshed. So yeah, you can you can replace this with server sent events because it's just one directional technically, right from the server to the client, but you can still use websockets. Multiplayer gaming. Again, it's very similar, right? Especially if the game is built so that we are sending inputs instead of the state of the game. Then WebSockets is a good idea. If it's otherwise, then I would think twice before using WebSockets just because large bandwidth on WebSocket is not a good idea in general. Right. And the only reason is really because of TCP and we don't we don't really build stuff that has like large bandwidth that kind of I'm okay with it being lossy. You prefer to use UDP in that case, like something like WebRTC RTC and stuff like that showing client progress and logging. Right. These are some of the use cases at least.
