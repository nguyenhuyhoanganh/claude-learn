# Configure NGINX as Layer 4 WebSocket Proxying

All right. Now that we have our WebSocket servers running, let's configure our Nginx so that it is a layer four websocket proxy to our websocket servers that we have spun up. And here's what we're going to do for layer four sparking. We're going to listen on Port 80. We're not going to deal with encryption for simplicity here and any TCP connection request is a tunnel that always goes to a WebSocket app. All right. We already know that. But we're going to actually demonstrate it. Very important paths don't matter in layer four. They are layer seven contexts, right? So if slash chat, slash superchat doesn't really matter. Anything you do to a websocket connection in layer four, it will always go to the same server, right? It doesn't really matter. So any request to a websocket connection, regardless of the path, will always be a websocket request. It will be recognized as such and it will always go to the same context, to the same application. Technically might go to different servers for sure, but not a layer four proxying and inject doesn't really understand what is what is it doing in this case. So yeah, if you're doing localhost, this is going to a websocket app. If you're going localhost blah blah blah. This is also a websocket app. It doesn't really care. Anything here doesn't really mean anything on the back end. Yeah, you can do some sort of a small things and, and understand like, okay, I don't really, I don't really care about this. I want to drop this. But Nginx doesn't really care about this. Layer for proxying blindly tunnels, everything to the back end. Any connection request to port 80 will be tunneled to the websocket app backend. Let's jump into it. All right, guys, how about we configure nginx to be a layer four reverse proxy in our case? So let's go ahead and create a TCP dot cfg configuration file for NodeJS. This is a new thing that I didn't show before in the previous section, but you can create a configuration and pass it as a path to Nginx and this is what we're going to do today. So as usual, since we're building a layer four proxy, we're going to start with stream a stream context. And since Nginx for some reason needs this event settings, let's give it this event thing. Now the next thing is we're going to build an upstream backend, we're going to call it AWS backends, right? And here's what we're going to do. The server. 127.0.0.1 on port 2222. These are my backends, so these are my backends. How many backends do we have? Well, why pee pee pee? We have two, two, two, two. We have three three, three, three. We have four, four, four, four and we have five, five, five, five. These are my back ends. So now. The backends. What else? How do I stream to my backend? I'm going to have a server, so we're going to create a server and this server is going to be listening on port. Let's listen on port 80. Sure. And then we're going to proxy pass. There is no protocol. Its blind pass proxy pass everything to backend. Let's go ahead and save my changes. Now let's run my nginx configuration. So I'm going to right click here and then I'm going to copy the path of this configuration. And then here's how you run Nginx. Assume you already installed Nginx. It's a really straightforward process. You can download it directly from the nginx.org. You can do if you have a mac like me, you can do a brew and install nginx. If you have a linux you can just do sudo apt get install nginx. Once we have an nginx you'll have access to this rewrite nginx and then when you do nginx dash c is where you have the configuration. I assume C stands for configuration and then you just do a paste. Right. And just like that. We have nginx now listening on port 80 and. Acting like a reverse proxy. A layer four reverse proxy. Let's go to the browser and test it. All right, guys. So I have Nginx listening on port 80 and ready to accept our WebSocket requests. So let's declare this time our new WebSocket object. And we're going to go to a local host, which is the default is what, port 80 Right now, if I do this and I do a the normal thing on message and then I do dot send. Look at that. We got a request. We got the response from server three three, three. Remember, we have how many? We have two, two, two, two, three, three, three, three, four, four, four, four and five, five, five, five. Right. These are the four servers of socket servers that are running on the back end. Okay, so now another request. If I send another request. What do you guys accept? Expect this. Which server is going to respond back to me? Is going to be the same. Why? Because it's a tunnel. This is. Is a connection now. This is tunneled to a back end connection. If I now establish a new connection, which is doing that, this is now a brand new connection. If I do now this now we got into another server and as long as I have this session open. I'm always going to be connected to that server. And the the thing about this is that I can do really anything here and it won't matter because. I am doing a layer for Proxying. It doesn't really matter. This is absolutely useless. We don't really check for paths. We don't do anything on paths, right? We just literally take whatever we have and then blindly. Go to the back end so you can see that every request to create a new connection to Nginx is load balanced by default round robin to a backend. But as long as we have that connection open, any requests or data sent on that connection always goes to the same server. This is unlike Http which I talked about. I showed that in my previous section. So now that we have an Nginx server listening on port 80 and have our configuration, which is the TCP connection, we can jump into the second lecture to talk about how do we do this in a layer seven configuration and we're going to do cool stuff with that. Let's jump into it.
