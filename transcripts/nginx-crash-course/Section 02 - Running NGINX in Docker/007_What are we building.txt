# What are we building?

Hey, guys. So we have done an entire section of how to run and Gen X locally on my MacBook, and then I exposed the port of Engine X from my MacBook to my router and then I exposed it to the public Internet. When I did that, I was, you know, under the impression that, okay, let's let's build something that doesn't require people to spend money, you know, to spin up with something on AWS or or something like that, to just to have a public IP address. I was like, oh, let's just do some basic networking finesse to to spin that up. But people found that a little bit restrictive because not everybody has access to change their router. So a lot of people asked me to, hey, why just why don't you just do a Docker? Because Docker runs on every operating system. So this is not restrictive to just Mac. It will burn every operating system Windows, Linux, Mac, as long as I have Docker and I think it's a very, very good idea. So this lecture is actually it's a section contain one, multiple lectures. We'll do exactly that. Let me flip to the screen, show you exactly what we're going to build in the coming lectures. Most of you might know already what Docker is. Docker is this runtime that allows a very thin layer on top of the operating system to abstract all the complexity of the OS so that you can put a. An entire almost an entire OS and runtime and frameworks on top of the OS while not changing the route operating system. So it's very useful. So you can spin up multi a container that has no GS version 14 and another container that have Node.js version 13 and another container that have Python version three, Another container has Python two, you know, because we have legacy app and you cannot do this with normal just machines, you know, because you will have to, you cannot install conflicting runtimes. Right. So yeah of course you have you can you can do multiple virtual machines but that's very expensive and thick, you know, because the OS will have expensive, it will have an expensive cost to it. The Docker is a very similar concept, but it's a very it works with something called control groups effectively. It's a process, but the process isolates the process from the rest of the operating system. Very, very elegant design. So what are we going to use Docker for is we're going to spin up a this, this will be my Docker host, right? So go ahead and install. Docker is very straightforward. Yeah. Whether it's open to Linux or Ubuntu, Linux Windows, Mac works with everyone. I'm going to use Docker on top of Mac, but this instruction will be identical for you. What I'm going to do is I'm going to spin up a Docker container and I'm going to literally use the Docker Engine X official image. What does that mean? It means that whoever built this Docker image for me, an image is something you spin up containers from. Think of it like for you code programming, you know, think of it like a class, right? While the container is an object. So your class is a static thing and you can spin up multiple objects, you can create instances of the classes, multiple objects, right? So very similar container is an instance of an image, right? So someone the Engine X team actually built us this beautiful official engine X image and it sits on top of a specific operating system with just the right part, just the things that it actually needs to spin up that thing. Right? And then they installed the engine X in that image. So when I run a container from that image, I will have engine X installed for me just in that container. And that's what that's what I'm going to do. So I'm going to spin up a Docker container. When you spin up a container, you can give it a name, right? Addo-carr name. You can give the host name because it's kind of a machine in itself. You think about it, you can address it by the host name, you can address it by an IP, and by default, when you open up a container, it will sit in its own bridged network with a specific subnet. So 172 blah blah, blah, blah blah. So it will get also a private IP address that you can address from. You can access this private IP address from your host machine. Right. From like in my case or my same Mac, which is where Docker installed. And this will have its own IP address using IP address is probably not a good idea because they change. You know, that's why we like to use hostnames. So we're going to spin up an engine X and we're going to give it ag1 as a hostname and we're going to use the default configuration. We're going to no change a single thing at all. Right, well, guess hey, just spin it up and listen. When you spin up in X as we talked about, it listens on port 80. So we're going to do that. And then what are we going to do is expose because this is completely isolated, right? We have two ways to access this. We can literally use the IP address of engine X to access that, assuming bridging is enabled in Mac OS bridging is not enabled. That means if my Mac right cannot access the internal Docker network while in Windows and Linux, I believe you can. So in my case I'm going to publicly expose Port 80 to my host. That's how you do this? By doing P effectively. I'm going to do all of this, right? So this is what I'm going to do for the first lecture, right? And then I'm going to access the, the content of Engine X as we're going to return this default HTML page, like welcome to an engine X, whatever. So the next thing we're going to do is we'll get to be a little bit sophisticated. What we're going to do is going to actually build a Node.js app, right? In Node.js, of course. Right. And this is going to be very primitive. It's going to just retain its host name, whatever the host name running on Node.js, We're going to return it. Why? We're going to tell you why. Right? So I'm going to use Express to spin up a simple Web server just to return that. Information. And what we're going to do next is to build we're going to build a specific Docker image for my application that I'm just going to build. So it's going to be interesting here. This is slightly different than engine X using a vanilla one, engine X image, Docker image. No, we're going to use Node official node image, right? I'm going to use the lightest version because this is not a very sophisticated version. We're going to use the lightest possible version, pull it, do a little change from the image and then copy my app into the container, and then that will be my image effectively. And then after I build my new image from the node image, so it's my custom image, I'm going to spin up a specific app called Node App one, and I'm going I'm going to give it the hostname node app one and it's going to be listening on Port 88, right? So this is its own container and I'm going to do this same thing right here because now I have an image I can spin up unlimited number of containers from the same image. So I'm going to spend another one node app to beautiful, say, import. You might say, isn't that a conflict? Absolutely not, because this is a completely isolated process. Right? So they are not listening on the same IP address. Right. This is a different IP address. This is its own IP address. This is an IP address. And because I created a specific host name for it, the beauty of this, these three will live in the same network and you can control that. And check out my networking course. I talk about all this networking stuff in details if you're interested. So so here all of these guys can talk to each other because they are on the same network, right? They are on the same subnet. So this can access this and this can of course access this because they can connect to each other, right? So this can of access to Node app and Node app too. So what we're going to do here is going to change. Now we're going to overwrite the configuration of Engine X. When we spin up the container, we're going to actually change the configuration from Engine X, right? We can do it in many ways here. We can build like like we did here. We can build our own Docker app Docker file and we can build our own Docker image from Engine X. That changed that, but a little bit overkill in my opinion. What we can do is just spin up a container and just literally slam slam the configuration file inside the container. So Engine X will spin up with our own config effectively. And in that config we're going to tell it, hey, go ahead and connect make become a layer seven proxy and then connect to node app one and node app two. I want you to load balance anyone going in port 8080, 80 to be specific. Go and load balance them between the two apps. Very simple stuff and the reason I returned the hostname is now when we visit we know which app are we going to be served from. And that's how through load balancing we know that actually it is being load based. And now you guys can I'm going to of course share all the source code. You can change this node app, you can put Postgres in another container here. So all of these guys can talk to another database in the same content. All of this stuff can happen. You know, the world is your oyster, the factory. So finally again, everything is private here. And I'm going to listen in this case. I'm going to change the port 88 because that's not the default anymore, right? I'm going to change 88 hostname. It remains the same final step I'm going to do is I'm going to expose port 8080 to public. Right. That means anyone accessing my machine on port 80 I'm going to forward it to port 88. This is basically some IP table magic, you know not really magic, it's just basic router routing. Talked about IP tables and my my YouTube channel. And finally, final lecture is this is another question we get a lot you go to log and the Q&amp;A section Udemy where hey what if any Gen X is now you know a bottleneck by itself, right. You can spin up another container to to basically load balance requests. Obviously this is a bad idea using just Docker because guess what? If Docker host the host dies, everything dies with it. So technically you need another Docker host to do another load balancing where you can. You want to distribute containers across the host as much as possible, right? That's where Kubernetes comes in. You know, this is where an overkill. You're over killing basically, or your architecture. You install Kubernetes or Marcel's to to, to to orchestrate all these nasty containers. You know, it just becomes very complicated. Very quick. I like. Simplicity, in my opinion. You know, I like things simple. So of course, this is from looking at this. It's very similar, right? The these will be identical is just different config. This will have a different hostname. They will be the same port. And now we're going to expose this to port eight and I'm going to expose that to port 81. Right now. If someone access port 80 they will go to this container. If X if someone access port 81, they will go to this container and the beauty here is you will be load balancing. Now you might say like, I don't want clients to connect to different port. Of course you can. You can control that too. You can specify an IP rule that says, Hey, anyone connecting to Port 80, please load balance them between these two you rules, right? You can make a rule that just or WPF for example to to to distribute the request. You've got to be very careful here. You want to distribute connections, not request. You can't you don't know what a request is at a at a layer four or layer three. Right. There is no concept of a request. You just distributing connections. So let's say you put this behind a DNS, my engine x app dot com and you can have that point to a to service record service one points to port 80, the service to port to port 81. Right. Another trick is to spin up another node instance or another area is then a completely different public IP address and do that exactly again and have DNS have two air records. One points to this public IP address and one points to another. Right. But this is outside the scope of this video. You know, it's just that you can do so many so much strict. And that's where and that's where we're and that's where the art of back end engineering and architecture really lies, you know, can do so much thing. It depends like, what are you trying to do exactly. Not just you're not doing things for fun here. It's, it's practical. You're trying to achieve a point. All right, How about we actually do some work? We'll move to the next lecture. We're going to spin up a very simple thing. Let's get started.
