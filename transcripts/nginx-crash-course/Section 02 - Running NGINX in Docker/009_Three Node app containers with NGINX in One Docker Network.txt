# Three Node app containers with NGINX in One Docker Network

Welcome, everybody. And in this lecture, we're going to spin up as three containers is just one. Right? So in the previous one, we spent up just in Unix as a web server, vanilla stuff, boring. You know, we want some code, we want some backend execution. That's where Node.js come in. Doesn't have to be Node.js. You can do Python, it's identical. However you code yourself, it's identical to spin in a doctor container, right? All you need to know is just which image, if an image such exists, right? And then we can build from it. So what are we going to do is we're going to first build the node app, right? Build an image from it, spin up a container, make sure that container actually works. Once that is done, we're going to destroy that container and then we're going to spin up Engine X, and then we're going to override the configuration. So the can points to this could do two containers. So technically we're going to spin up multiple containers. Maybe we're going to spin up three if we're lucky. Right. And everything will become into inside this Docker host, Right. And we're going to be accessing them from publicly from this. So technically, we never used the host name here, the engine. So you can leave it blank if you want, but it's good to name things. Let's get started. All right. So we have the real me. I'm going to start keeping a good habit to update this. Read me because you guys ask me all the time. It's like, Oh, well, the commands I like to copy and paste. So what are we going to do here? Let's make a folder called App. And inside this app I'm going to build my Node.js app index.html. Yes, let's do a simple express stuff. Express equal. Equal require, express, write and then const app, equal express. Very simple thing here. Abdel, Listen, I'm going to listen on fourth to 8080, and then when it's done, I think this is how you do it. Console.log. Listening on Port 80. 80. And now what I'm going to do here is actually, I'm going to also get the OS. All right? I'm going to require the OS and then I'm going to get the hostname from the OS. I believe it's a function. You do it this way. And finally. We're going to do this plus. On hostname this way it will tell us that I'm listening on this. Right. But that's not enough. Obviously we're not just listening on on a TCP connection to do a CDB, right. We actually do what we want to do more. We're going to do some routing if someone send us a get request on slash on the route, go ahead and take this response. I want you to send back something. What are we sending back? We're sending back hello from hostname. Very boring. And so what this will do is like, whatever my host name is, and the reason I'm doing it this way is because I'm going to get the hostname from the container. Right. Or in this case, if I run it right now or my host machine is going to retain Hussein Mac, whatever the machine name is. Right. And that's enough for us. This will tell us that whoever visited this page, it will tell us where is it visiting from. And that's the reason I wanted to know. No, you can make this app as complicated, as sophisticated as you want. Let's go ahead and save. Of course, we're going to do m a neat dash, a neat dash y, and then we're going to, of course, install express. And this will update my package. Jason Beautiful, beautiful, beautiful. Let's run it and node. What? What am I doing wrong? Oh, I missed a curly braces. Of course. Yeah, I closed the console, but I never closed the lesson. That's what I did. Boom. Okay. Sorry. Index dodgers listening on Port 88 on Hussein Mac. Very beautiful. Now, if I go to again, this is not running in a container. This is running on physically on my machine. Right. Let's go to Safari. And Safari. I'm going to go http Hussein Mac Port 80, 80, and. What? There you go. Hello. Was too tiny. Hello. From Hussein Mack. That's what the response we got. It's just a simple request. So about 80. 80? That's boring, because that's running technically on my physical host machine. Right? And that's why it's telling me, Hey, by the way, this is this is the machine I'm running from. And this is going to change based where it is running from. And that's the fun part. All right, let's kill it. I have my app. It's ready. The next thing I'm going to do is go back up a little bit. And here I'm going to create something called Docker file. No extension, nothing like that has to be capitalized, I think. Right. And then when you do that, you can do from it has this Docker have this Docker file concept where you can build custom images and you can inherit from an image. So in this case I'm going to inherit from Node 12 because I found that it's the lightest version. Note 13 is like a gig for a for no particular reason. I found Node 12 was for me at least this was 600 megs, so I just used Node 12 and you can use of of course the latest. So when you do from Node that tells the whoever when we build that image. Right. At the time when we build that Docker file, which we didn't do right, we're going to show you how to build an image, right? It will inherit from this container, from this story, from this image. Right. From this official image, from not. We could have done engine X, for example. We can have it from engine X, but it doesn't have note. So I want something with the runtime. You could you could inherit from ubuntu if you want directly and then install node. But that's very, you know, that's very heavy. You know, node. Whoever built the node, I would hope that they was a very lightweight operating system, you know, and they effectively built on top of that. You know you can you can build from vanilla, you know whatever Linux distribution, you can do it this way. It's very you can do so many tricks here. Docker is an award by itself, you know, So I'm going to do this. I'm going to insert from note. Okay, what are we going to do? When you spin the container, you can specify where it does what is the main working directory. And that's I want I want you to know this because the working directory here is where your app will run by default. So, so that you don't have to do slash CD go to this directory. No, whatever you run is wherever you run by default. And that's very, very useful. So in my pocket the case is going to be home node app. It could be any directory you want. Right? This is my work directory. Doesn't have anything. Absolutely not. It has. It has zero thing. It might even not exist. Right. So how do we actually put stuff in it? Here's what is interesting. Copy. So again, we're still building the image. When you want to build the image, I want you to copy the content from the app folder, which is in. Remember that folder, right? This is where we had our Node.js app. Copy that. That's where it's very important where you run this command. Right. Copy this folder to home node app, boom, beautiful. You know, and when you do that that's going to copy this folder inside this. And I believe it's not going to create another folder, it's just going to contain copy the contents there and we're going to verify that as well, if I'm mistaken or not. And just like that, we're good. Here is the command that you can execute called run. And I'm going to run NPM install Hosain. Why? And remember, this is a blank node app, right? The blank container has nothing, right? Yeah. You copied content. Yeah, it has package to JSON, but technically express is not installed. Right. You can do this. But that's the very specific. Right. If you do NPM and install whatever is in the package, the Jason will be installed, right? Yeah, I know jazz guys, you know this stuff, right? So in this case, and how does it run a run in inside this folder, Right. Because we said, Hey, this is your working directory. What do you want when you actually spin up a container? Wait a minute. I think I confuse thing, guys. You, when I tell you, run will execute when you build the image. Right. So do you want me building the image, which we didn't do yet? We're going to show it. NPM will install, express or install it. Pretty much everything in package. Yeah you don't you don't want to do CMD right. And PM and install. That's just that will be a disaster because you don't want to install when you're on the container. The container will take ages to spin out. You want to take, you want to install all the dependencies. When you build the image and you need a span of container, you only run the command that you need. What's the command? Well, not inductor, Jack. Right. That's what that's the command is. So to summarize, I know I'm taking a long time to explain, but this is very critical stuff, I think. Yeah, it's unrelated to Engine X, but yeah, it's I think it's very critical. You can skip. Of course, if you don't, if you feel you're not learning anything here, we're going to inherit from node image, make this folder there working directory, copy the app. Right And this is this will happen when I build this image. Right. Copy the app folder into that during the while I'm building, go ahead and actually install. Right. So pull all the latest express version based on the package that Jason downloaded, put it inside this container and wrap it up. You can have multiple run command here. CMD will not execute now. Right? It will not execute if you do run. If you do run node index the Geez, that's wrong, right? The command will run, Node will start spin up a HDB server while we're building the image. That doesn't make any sense. So you don't want to do that, right? So cmd node index dogs, right? This is where we actually spin up the container. Actually, let's stop talking. Actually build something. Right, Right. Save the docker. So how do we do it? Docker build dot. We need to Docker build dot in this folder. Right. That's actually do an LS in this folder. We have a docker file, right? When you do a docker build dot, right, it will look up for a docker file. So and you can do dash T to name your image. So my image are going to be Node app. That's my beautiful image, right? Do it. So now I was going to pull from the node official it will install. Right. I see. Is running the npm install expresses an install with copied and it's run. So now if I do docker images you should have all my stuff here plus latest tool second ago node app. Cool. How about we actually tested test my node app docker run dash dash. I just want to test that here. So I just I don't want to do anything. Dash P 80, 80, 80, 80. Right. And then call the D.A. up. Right. We just spent 48 to 80 and exposed it. Right? So let's test it. Sin Mach 80, 80. Now this is different, right? Look at that. What? Look at this. This is the hostname. But the hostname is just random rubbish. Right. Because I didn't specify a hostname so that's my fault. Let's actually kill this guy. Oh you know what, I don't have a shortage of containers. I'm going to expose another one. These are called hostname, I'm going to call it test node and. So now it's 80, 81. Let's do it. Why are you Googling? All right. Hi. Works all right. We know this stuff works, right? Let's delete all this nonsense containers. Docker, stop. 43, 54. By the way, just specify the first two letters and there is no conflict. Doc are smart enough to stop them. Man still takes a long time. I should have picked up another run time. I was Bao or whatever new run time is, if faster. Anybody knows. 43. 54. All right. Okay. Or I'm not Rahm. Okay, REM. All right, guys, we have a beautiful no jazz container. I can spin it up at any time. Can give it a name. Now, let's actually do the work we want it to do, which is the config. We want to override the config. Right. So. All right, let's build the config first and then let's open up the container and we're going to do the same trick we did with the volume. We're going to map the config from my host down to the container. That's exactly the trick. Right. Not rocket science at all. So we're going to call it engine ex-con. I believe that's the default one. We're going to rewrite everything from scratch. Let's do this. All right. So start with the context. I'm going to build the HTTP context because I want a layer seven load balancer for HTTP. How many servers do we we're going to spin up? I'm going to go with three server node app one on Port 88 semicolon. Server node app two 8080. Server node app 388 Right. Again, guys, remember, this is the host names that remember Engine X will be spent up in its own container. It's going to have a source name and it's going to have its own IP address and node app. It's going to see Node app one, unlike us, which is outside where our outside we cannot see this host name that is local, right? So only an engine X can see it. That's why this conflict should work. Right? But it doesn't make sense now because we didn't spin up these three apps. Right. We should we should probably do that next. But now I have an upstream, right? I'm going to build server right directive. So I'm going to do a simple HTTP server. Here I am listening on Port 1880 and when someone goes to location slash someone, go to look in, slash, go ahead and do a proxy pass. Pass it to HTTP node backend. Is that syntax correct? No. That's a two node backend. Is the syntax correct? The final thing is that we need to do the events thing right, which basically it's we have to set something here. Otherwise in the letters here where you can set the number of maximum worker connection. Right. This is the how many because we talked about this in the architecture lecture. Right guys. But if you're down here, you probably watched that lecture already that when you spin up in Gen X, there is another config that I also didn't specify, which is the the I believe worker processes. I can't remember it right Auto right it's the default is auto right. This tells us like how many processes should engine spin up based on the number of CPU cores that you have? And so this is where it gets interesting. If you run into internet container, whatever the container is assigned to you. This is beautiful, actually, right? So you can have a very beefy machine. You can spin up an engine X with a container and you assign to cores only for this container. So when you do this, engine X will spend up to processes, right? Of course you can override that by by default. It's one CPU core per worker process and that's the default. Right. And then for each worker process, you can specify how many connections you want as a maximum, you know, because you see every worker process has a connection pool to the back end, right? It was going to connect to these guys, Right. So how many what is the maximum? What's the limit? Right. And each worker processor will have its own pool. And by the way, just a side note, that's a one reason that Cloudflare actually recently just moved away from Engine X to their own reverse proxy. They built their own because Engine X had this very restrictive limitation. They cannot share the connection pools, Right? Every worker has its own connection back in pool and it became very chatty on the back end. You're talking to the backend, especially Cloudflare as a CDN. You know, it keeps hammering on the back end. You want as you want to be as chair, as least chatty as possible to the backend. And that's just one reason this is just a side thing. All right, I'll stop. I'll shut up and save this and continue. All right, So this is the file. Let's just pray to God that is actually working, right? All right, so how about we spin up? Right. And in Gen X container Docker run dash dash name and gen x dash dash hostname in G one was a port. I think I changed the port. Right. So I'm going to 80 to 80, 80. So that's what we're going to expose. I'm not going to detach it actually. I mean I might need to detach it. I'm not going to detach because I want to see if there are errors. Right. I'm not going to detach the container and let's do a volume now. We're going to move the volume. And of course, I forgot the path. So let's do this again. This guy. Map this and gen x dot conf to map it to where I believe it's Etsy in Gen X engine ex-con. I think this is the right directory. And then finally do engine X, please run. Ow! Ow, Ow! Doctor Stop in genetics. Doctor Ah, Just because we have an engine X running. Okay, now. Are you guys ready? Let's do it. They'll fail. Hey, it actually, Ron. But that was dumb. I shouldn't. I should actually run it. It's working, but it's not finding those names because we never spun up the actual Node.js. Right. So let's go ahead and so go ahead, Docker, Stop Engine X, We're just going to stop it because it looks like it's working. The reason is just because it couldn't find the back end. Host Right. So what I'm going to do is just do a Docker run dash, dash, host name node app one right dash dash, name node app one I'm going to give it the same name and then now we can detach safely, I suppose, right? Do I need to expose the port? Nope. You should not do that. Actually, this is bad idea to expose the port here. There is no business for these backend apps to be actually exposed publicly. Right? Remember when you expose it, when you do this, if you're doing this on a node or it is going to be exposed publicly to the internet. Bad idea. Don't want to do that, right, assuming your Docker is facing the Internet. Right. All right. So detach node app. Right one. What are you talking about? Conflict. Oh. Docker Stop Node App one. Joke or no? All right. I somehow have it. All right. Start again. All right. Are we ready? Let's do it. One. How do I do another one? Hardest thing in the world. You want to do another one? That's it. Best thing ever. Docker start in next. All this. All right. Now, I think you do Docker logs. It will tell you the. What is going on? He still couldn't find the upstream nor tap one. Why? What does it mean? Couldn't find it. So Engine X says that it could not find hostname node app one. So. This means that although all of them are in a network, technically they have IP addresses, they cannot talk to each other, they cannot see each other for some reason. When I when I actually researched this, it seems like you have to explicitly create a network for all those guys anyway, which is a good thing because this is exactly what we want. Right. Let's not use the default. So if we create a network and put in generics and all the networks, all the node app in the same network, they should be able to see each other. And instead of using the default network, let's let's do things right. Right. So let's do this in order to create a network for all these guys to connect to. We do Docker Network, suppose create Engine X Net or let's call it backend net. Right. So this is my network now. So now I have how many containers do I have? I have Node app one, two and three running and I also have in Gen X, but in Gen X has stopped because it couldn't start. You see in Gen X it has to verify that in the config. Otherwise it will start, you know, yelling at us. So you do this config, let's add it to the read me so you guys can have it. Then Docker network create back and net. That's, that's my network now. So now we're going to do is to go Docker network connect back and net. Right. With Node app one. We're going to just place these containers in the same network so they can see each other. Right? Do that. If you ask me, it should have worked with with the default network. It will probably work on your machine if you use Linux. It's just it's a weird configuration on Mac and Windows. So it's it's better just to use a network so each each other so they see each other. Some you might need to set the DNS, but the default DNS will detect those guys, right? If you create your own network. So now this is the second one, third one. And finally, in Gen X, if you add all of these guys. Let me just add to read me so we can do forget. Cool. Save. So now. If I start in Gen X and I say, okay, in Gen X, what do you tell us? Hey, configuration, ready to start? So that means it should be up and running and it should have an IP address. Let's actually inspect. Do you have an IP address, sir? Yes, you do. And that is the gateway. Okay. What's the IP address, for example, for Node app one. It's two. So they should have everyone have an IP address. Everyone should be able to talk to everybody. So now we have a network. Everybody should talk to each other. How about we actually test it? All right. HTTP colon. Colon. Hussein Mack 80. Hello. Flume Node app, right. Refresh again, no dap to refresh our note F three. Refresh again, no dap. One, two, three. So what happening here is my safari established a TCP connection with engine X. Well, technically it sent the send to my machine Hossein Mac, which then say, Oh, we're going to port 80. Well, I'm not the final destination. I need to forward it to port 8080 on this bridge network on Engine X. So that connection went to Engine X, right. So Engine X establish that connection. One worker, one worker process took that connection and then it just hosted it. Right. And then once we have that. It's a forest since the first HTTP request. Net on that connection. Right. And in Gen-X talk that. Maybe we should show you that it's better to show it here. So safari connected to this. So there is one TCP connection, right? You sent you hit enter and sends the HTTP request. Right. On that connection. It goes here and Gen X reads it and says, okay, this is the first request. It goes to this app, right? If you send it again and if you refresh the page so far, it doesn't re-establish the TCP connection, it just uses the same one, right? So it sends for efficiency reasons, right? So we'll send that request to the same connection. And then Gen X remembers it has a state. That's how round robin works, right? So is all right. I connected to this guy and now I need to send it to this guy. And that's what we're seeing with the app, basically. All right, guys, what a long lecture. He'll do all the stuff, right? Relax and take time to absorb all this stuff. All the source code will be available. Of course, all the things we did will be available in the read be and the details. All right. See you in the next lecture where we're going to spin up 200 X instances. It's going to be easy. That part is going to be easy. See in the next one.
