# NGINX WebServer Container

Welcome. And this will be a lecture where we're going to spin up an ingenious container and give it a hostname that we're going to use. We're going to expose it publicly. What I say publicly to my outside my my network so that at least my machine can access it. Again, you don't have to do that if you're using a bridge network. My Mac doesn't support bridging for Docker, so that's not allowed. But if you have bridge, which is by default and installed for you, if you have Linux, you can just address it as G one and it will find out what energy one is. And if it doesn't work, you can use the IP address of the engine X and then publicly address it from your safari or chrome or whatever directly. Let's get onto it. All that stuff will be in a folder. I'm going to create a folder and this is obviously going to go to the source code. I'm going to share it with you guys. Their code will be let's call the directory engine X, UDP container, engine x, UDP container. Let's go inside and let's start with just spinning up a new container. Of course, make sure you have Docker. How can I make sure that Docker is running? Just do Docker run Hello World. And if you get hello from Docker, you're up and running. So let's go, let's get started. All right. So I'm going to do Docker run. I'm going to give it a name, my container. I'm going to give it a name, I'm going to call it Engine X. And then being because I know that Engine X spins up by default on Port 80, I'm going to expose Port 80. To my host. This is what the on the right hand side is, the what the container is running. And on the left hand side is what you access from your host machine. Right. So by doing that, now it's exposed. Final thing we're going to do is let's give it a host name, dash, dash, host name, and you can call it ng1. That's what we decided to call it. Right. And then. I'm going to add a parameter called dash de, which means detach it so it doesn't actually block this terminal. Right. And I don't want this standard output to be this terminal. I want to do more stuff here. So I just want to detach it. Hey, spin it up and just move on asynchronously, if you will. Right. And then finally, in Gen X, the final parameter is the image that we talked about. Image is the image of this engine x container. So let's go ahead and you can specify also the version of engine X by doing that. And being that I'm building a course, I should probably fix it to a specific version because if you're watching this course three years from now, Engine X could have a breaking change, and the config that I do might be broken. In this case. You come back to this lecture, look at this description and you will see me that Hey guys, whatever. But 1.7 hardcoded to 1.7. But let's let's use the latest. As long as not broken, let me know it's not going to be broken. Right. So run. And just like that, we have a beautiful container. And this is how you see your containers by Docker. I have one container spun up 6 seconds ago, and that's the name of the image. All right, How do I test it? You can do Curl HTTP, Jose and Mac Port 80. All right. Little boring from the terminal. That's all. Let's go ahead and open safari. HTP. Hossein Mac, call an 80. Or you can just choose not to add anything. It works regardless. And welcome to Engine X. This is being served from the default engine X container. So what happened? Really? Really what happened here is I am on my machine. I visited Hossein Mach. Hossein Mach has a rule that says, Hey, someone tried to access Port 80 because that's what HTTP does, right? Default 80. If someone tries to access access port 80, please forward them to this to to the engine X configuration on this particular IP error. So of course, I don't think anybody wants this as a web server. Right. You want to change that, right? That's actually it. So I'm going to make directory called HTML here and. Let's create an index that is HTML and I'm going to create a beautiful HTML page. Here's where my front end skills lie. Basically, this is the extent of my front end, you guys. I'm going to do that. Hello, Ward. And then there you go. I have an HTML file, but that's not enough. This is HTML file is sitting on my host right in a folder called HTML. How do you specify the current directory? Oh, it's pwt. Got it. Okay. BW dd. Regardless, this is the path. That's a long path. But it is the path. So what are we going to do is actually, I'm going to destroy my G1. I'm going to destroy my engine x y did. Can you guess, guys? Why did they fail? The engine x hostname is called G one. These are two different things, right? The engine x hostname is called engine one, but in generic container name is called engine X. These are completely different things. If you want to address the container, you use the image, the container name. If you want to address from a networking perspective, you use ng1 before we delete it, I want to show you that you can actually inspect right the container and show you like this is the IP address, right? So this is the gateway where Docker leaves, right? And this is the private IP address of my container. So, so if I do a kernel http one 72.17 .0.1 on port 80. This doesn't work in Mac, but I bet if you tried it on your own machine this will work because bridging is enabled in in other network. I just. I have no idea why Mac doesn't allow Docker to do this bridging because technically it's just another network. Right. And I want a bridge to this network from my IP address, which is by the way in 192 and 68. something to one or two. So these networks should technically be bridged, but apparently it's not a lot. But regardless, let's stop the networking talk. I'm going to delete my container. Right. And now we're going to do is I'm going to spin up a brand new container, still going to call it Engine X. The host name is still going to be in G one. And here's what we're going to do next. Actually, let's just show the path here because we're going to need it. And then we're going to open up a new container Docker run dash dash, name engine x dash dash hostname in G one, dash P 8080. Right. I'm going to change. Do I change it? No. So I'm going to keep the configuration. But what I'm going to do is I'm going to change the default route for the XHTML, you know, so that that will come instead of using the will welcome. I wanted to use my content and instead. Right. That's the only difference. Right? And I have an index HTML page here in this folder. So what I'm going to do is actually trick it using the volumes. Right. So the volume is. By default, when you spin up a container, it has a volume, right? So it takes a finite amount of space from your host, right. And said, Hey, this is what I'm going to put my stuff in the volume and it creates a volume by itself. You can play tricks with this. You can say, Hey, by the way, don't create your own volume just for this path, right? I want you to dump everything for that path. For that particular part. I want you to list this path instead from my home so you can map volumes. Very interesting. Right. So the rest of the stuff and you can do the volume for the whole container or you can do it per path. So what I'm going to do here is I want the entire this right, this is HTML folder which has the indexed HTML to be mapped, paste it and then do a colon. I want it to be mapped to inside the container where the HTML default stuff lives. Where is that? It lives in folder called a user share. Engine X is HTML. So this means that. Instead of writing your own files, please don't write your own content. Just use whatever I tell you for this particular path. Right. So that's that's that's what we're going to need to do. And I think there is a third parameter that names the volume, but it's not. It's optional. So we're going to leave it blank. So now we mapped it, but what's the rest? Very simple. I want to detach this stuff. And finally the image, Right? Did I miss anything? Hopefully not. If we miss anything, we're going to run it again. And it's running. It's bowling toward 88. But we actually tested its clear before we actually tested. Let's actually show you if we inspect Engine X, you can see that now we have the mount there will be mounted these folders. So my source is this and my destination is this. So actually actually tested in the browser. And be careful now and be careful now. If you visit this, sometimes it's going to be cached. Of course the browser will try to catch it as much as possible. So if you do that, it worked for me. But sometimes the browser will say, Oh, wait a minute, it will still show you the old page. So if it that that happened, just do the old, you know, query parameter trick or is this like just the blah equal blah? This will override the cache, right? The attack cache will be purged effectively. Yeah. So I was just so worked. That's my, my, that's my page last time I checked. And these commands, I'm going to put them in a read me file for you guys so you don't get you don't forget them. So actually, maybe I should do that right now. Read me the text. Inspect content is your friend all right? All of this will be available for you. All right. So now we did this and this. I think we're going to we're going to stop right here because that's that's a lot to digest. Play with this, guys. Try exporting your own whole app. Really. You can just you know, that HTML folder could be a CSS bootstrap, all this fancy frontend stuff that I don't understand, you know, but all that stuff and then expose it to engine X energy X will just read it as is. Right. It's just static files at the end of the day images folder. It should be everything should just work relative path. So technically you should be good. So this is in gen X as a web server. Completely through container and docker. Hope you enjoy it. See you in the next lecture.
