# Working with NGINX - What will we do?

All right. This is people's favorite part. Now, we're going to actually dive into an example, guys. We're going to go into an example. We're going to install Engine X, We're going to configure engines. To be a static observer. We're going to configure Engine X to be a layer server reverse proxy, because technically Nginx is not a forward proxy, it's only a reverse proxy. That's something I learned very recently, right? We're going to proxy the Nginx to four back end NodeJS services using Docker because that's the simplest thing ever, right? To to do testing and we're going to split the load between multiple backends app one, app two. So we're going to do layer seven proxy. We're going to understand or you're going to app to go here, app to app one, go here, and then we're going to block certain requests to Yeah, you can do that in in layer seven proxy. If you do slash admin then Oh block that you cannot go that from certain IP address. You can only do it from other IP addresses for example. Right? Local ones. Nginx is a layer four proxy. We're going to do that, right? We're going to show you how Nginx can perform as a layer four proxy. And what does that mean really? And we're going to enable Https on nginx using letsencrypt fancy stuff. We're going to enable TLS 1.3 because TLS 1.2 is the default in Nginx, right? I believe. And then finally we're going to enable http2 on nginx because http2 is is really interesting technology, right? You really need to think about enabling it on your backend though only enabled when you absolutely need it. And i might need to make another class. Just talking about http because there are so many things buried in that. Before we jump into the example guys.
