# Spin up lightweight Docker Container with your application (nodeapp)

Hey, guys, I'm bringing you a quick video to show you how you can spin up a lightweight node runtime container that contains your application in a fully, essentially encapsulated way where you can stateless Lee, spin up a container, execute your application, and you can spin it down at any time, right? So this is very useful, especially if you're, let's say, running a Jenkins jobs right? Or CI CD pipeline or like you are part of a Kubernetes cluster, you want your application to be an image or a Docker container where you say, hey, execute this, spin up this container, execute stuff, do something and spin down, right? You can do something like that, right? So I'm going to show you how to do that essentially. And there will be two steps to this process. First, we're going to build that Docker image from which we're going to spin up that container. And in order to build that Docker image, we're going to build up our own Docker file, right? And then we're going to pull from the node lightweight Docker image that is already there and we're going to essentially write our own little application like an express application, and we're going to spin it up, right, build the image and then spin up a container running on Port 13 port, right? So that's a very simple thing. You can do the exact same thing with Python, exact same thing with Go and any runtime. So I'm going to show you a node and you can just do that and do it with any runtime you like. So let's just jump into it. All right. So I have Visual Studio code. I obviously I have Docker running here, so I'm going to go ahead and just open a brand new folder and I'm going to my JavaScript folder here, JavaScript background, and I'm going to create a folder called Docker and the code will be available in the description below guys for you. Right? So it's a brand new folder and here's the first thing I want to create. I want since we're building an image, we need to create a Docker file, right? And that Docker file, right? Literally call it Docker file. We have to be the same case as I think as well, right. The moment you do that, right, That's the Docker file. We're going to write code in some some code in order to build our image. Okay. And that code will inherit that image will inherit from the node runtime and specific version as well. So another thing I want to build here is my application. I'm going to create a folder here called App, right? That application will contain an Index.js. Okay, let's start building our application and then we'll get to build our Docker. How about that? Okay, so we have a folder here inside the Index.js. I'm going to go to terminal on my end the app. Let's go to the app. Let's make sure I'm in the app and then just do npm init y and shut up. I know what I'm doing. Just create the package.json and then let's just create an application const app equal require express. Very simple stuff. Right? And then do app dot get when someone visits this thing, let's do a rest dot send hello from a light. Weight container. Right. Very simple stuff. Right. Let's just show you that and then app dot. Listen, I'm going to listen to Port 9999 just for fun. Okay. And we'll do console. The log listing on 999. Simple stuff, right? This is this is my application right there. So let's go ahead and quickly install what do we need express. And the move to install Express. Actually, it's going to be added to our Package.json and that's very important thing. Okay. Right. My package.json requires express and that's important. Okay. So you saw them. What I did, right? I wrote my application, I run NPM install express, and now if I do just NPM install my package.json will see, hey, you require express. I'm going to install express for you. Okay. That's the cool part of this. All right, let's just run the application. How do we run the application? Let's create a script here and let's call the script app. Right. And then when the run the script run, I'm going to just run Index.js. So if I do NPM run app, I'm listening to port 9999 sweet. Let's go to browser localhost. Nein, nein, nein, nein. That's obviously a lie. We're not running from a container. We're running from the machine. But we're going to change that real soon. So let's go to the Docker file now. So what I want to do is build a beautiful image so that. This image will first of all, we're going to inherit from the node image. And now I want a specific version from Node. So if you're watching this video three years from now, you don't like install the latest version of Node, and this breaks the application for some reason. So from Node 12, right. And here's what I want to do. Working the working directory of this thing is I'm going to just make up a working directory in the container itself. I'm going to call it Home node app. This is a completely made up directory that doesn't exist. So the working directory is where my application is running essentially. Okay. And guess what? Up to now that container doesn't have really any code, right? Because we didn't really copy the code. So what I want to do is if, if imagine this application is running right here, right? If I want to copy, how do I copy? I want to copy the app folder to that container. So what do I do? I do literally copy app, which is the location I'm running on because I'm running from this location. Docker file copy the app to that's what home node app. Just copy that stuff there. Copy the content that includes the index that contains the Package.json. Probably we shouldn't copy the node module because we really don't need that. But sure, it's just a test, right? And then here's what we need to do next once you copy it. Now, when we're in a blank container, imagine yourself in this blank container. What do you do next? Guys, We need to install the dependencies, right? And there are two commands. There are command called run and there is a command called CMD, and each one execute on a certain stage. Right? The run command execute. When you build that image from the Docker file, the CMD, execute when you actually run an instance of the container from that image. Okay. And then. So that's what we're interested in running that thing, right? So NPM install. Run NPM install. That's it. I don't need to say NPM install express because from the package.json if I am here and I say NPM run install, I'm going to lock in the package decisions. Okay. What do you want to install? You want to install express. So that will install it for us. Okay. Sweet. That's all what we need. And the final thing is we want to expose. Right? The port. Nein, nein, nein, nein. That's the port we're listening to, right? Sometimes you don't really need to expose any ports. If your application, like, I don't know, executes and computes the first 70 prime numbers and then write it to a rest endpoint and shuts down, That's. That's completely stateless workload. Right. But in this case, when I do expose nine and I, I'm actually listening to a port and I want to expose it to the outside world. Okay. So that's the port. All right. Here's the thing now, guys. This is stuff is missing. Something I want to actually up until now, the application will never run if I run the container. So how do how do I actually run my application when the container is spun up? When the container is actually run? Okay, that's the command cmd. Okay. If I do NPM run app, that's the code, right? We wrote literally here that will execute this. So I think we have everything we need if I come here. And I need to go one level up. I am now in the Docker file. If I do Docker build dash t, give it a name, let's call it node app, anything really. And then dot very important that dot to build the current directory, right? The current doc and we found that thing and we stopped building all that stuff and we're building and everything looks good. Guys, we installed, we built and everything looks good. Okay, so now we have an image called Node app. How do we spin up a container from that? We've done this many times in this channel, so how do we do that? Docker run, right, Give it a name node app. You can call it anything you want. It's just the same. I call it the same name. That's the container. Now, when I do run, write, the cmd will execute, right? When I build the image, the run command was executed. I know it's a little bit confusing, so if I do that and I want to expose which port upon expose 999929999. This could be anything you want. This has to be 99999 because that's the port inside the container. Okay. And finally, node app. And we're listening to Nein, nein, nein. But this is a container, guys. This is a completely write a stateless container in itself. A completely contained by itself. Right? I can destroy it. And I can. I can give you that image. I can give you that Docker file. You guys can. I'm going to push that code. And if you pull it, if you clone that repo and you do Docker, build the same exact command I did, you will be able to do this thing exactly the same. Right? So and we can able to do essentially we'll be able to like spin up a Jenkins instance and do that Docker run and do that stuff as well. Right? So let's see if our stuff actually works, right? And obviously it works, but this time it's actually I am inside the container. All right, guys. So that's that was like a very lightweight video I wanted to make with you guys to share with you and how to make a lightweight NodeJS docker container that have your code and you can do so much stuff with it. Obviously, if you don't care about the container anymore, you can destroy it. And here's how you do it. You do Docker Stop Node app and you can do Docker node app. And that application is gone. Just like that. It's it's killed, right? You can spin up multiple Docker containers if you want to. Right. Let's do that. All right, guys. So the power of this now, now that we have the container running this thing, that application, you can spin up as many containers as you want so I can spin up Docker run dash P like port 8000 that listening to map it to port 99999. And this will be my first container. I'm going to call it node app. Then I'm for fun. I'm going to just detach it because I want I want just this application to run and detach itself, right? And I'm going to create another Port 8001. And another container, 8002. So now let's see if these 8000. 8001 8000 actually working. They should be. They better be, right. So if I do 8000, that works. 8001. That works. 8002. That works. How easy is this, guys with few lines of code. I spun up three web servers running my applications, essentially three containers, and you can put them behind a load balancer. It's as easy as like Caddy or Nginx or Haproxy, which we made a lot of videos on this channel. Right. And you can just load balance the whole thing, right? Which we might do in another video. So we start building all these microservices architecture with containers, right? So this is pretty powerful stuff if you think about it, guys. All right, guys, hope you enjoyed this video. I'm going to see you in the next one. You guys stay awesome.
