# Implementing choreography saga pattern - Part 3

Let's update this function to have some business logic. As the very first step we need to mention @Bean annotation on top of this function, so that this function can be managed by the spring cloud functions behind the scenes. To this function I want to inject IAccountService as an input. Since we are using @Bean annotation. This dependency is also be injected by spring framework during the startup of the application. Now let's go to this IAccountService and try to create an abstract method. This is very similar to what we have done inside the customer microservice. The abstract method name is updateMobileNumber, which is going to accept the mobileNumberUpdateDto object as an input from my function, I can try to invoke the new abstract method that we have created, and to the same I can try to pass mobileNumberUpdateDto. Just above this line, I'm going to add a log statement confirming received updateAccountMobileNumber request for the details provided. Since the execution of the flow is very important inside the Saga pattern, I am trying to add these loggers which are going to help me in my debugging. Next we can go to AccountServiceImpl and here I can try to implement the method which we just now added. Inside this method, I can try to write the same logic that we have written inside the CustomerServiceImpl. . Let me go to CustomerServiceImpl, and here I'll try to copy this entire piece of code and paste it here. Off course we are going to get some compilation issues. Let's try to resolve them. The CustomerRepository should be replaced with AccountsRepository followed by the return type is going to be accounts. Let me update the variable name also using the same variable name I'm going to invoke the setMobileNumber() and using the accountRepository, I'm going to invoke the save() method once the new mobile number is updated inside the accounts microservice. As a next step, these account microservice should publish an message to the cards microservice. We have done the similar kind of logic inside the customer microservice, so it's going to be very similar. First, we need to inject a bean to this impl. Let me mark this as final followed by I'm going to type private final StreamBridge using the same Stream Bridge we are going to send the message. Let me go to CustomerServiceImpl . Here I have a new method. Let me copy the same and paste it here inside the AccountServiceImpl. Once the method is created we can try to rename this to updateCardMobileNumber so that it's going to make sense. The same I'll try to invoke from the upper() method. We also need to add an annotation SLF4J  followed by here I'm going to update the log statement saying that sending update card mobile number request for the details followed by is the update card mobile number request successfully triggered. The binding name we should change instead of updateAccountMobileNumber. I'm going to mention updateCardMobileNumber. The same binding details we need to mention inside the application.yml of accounts microservice. So let me open application.yml. Just after the existing binding details I'm going to add new binding details. The binding name is updateCardMobileNumber-out-0. And this is the destination which is update-card-mobile-number. With these settings, the message will be forwarded to this destination inside the RabbitMQ. Now on the card microservice we need to make very similar changes that we have done inside the account microservice to consume the message that we are trying to publish into this destination. Let me go to the application.yml Cards microservice. As of now, inside this file there are no streaming and function related configurations. We can go to the accounts application.yml and try to copy this entire properties. I'm going to mention the same under the RabbitMQ properties. Here we can try to update these binding name to updateCardMobileNumber. Destination is updateCardMobileNumber and grouping we can use the same property which is spring.application.name for the cards microservice the application name is going to be cards. Since these cards microservice is going to publish the message to the loans microservice, I want to update the next binding details to updateLoanMobileNumber and here also the destination we can mention updateLoneMobileMumber. With these configurations, the card microservice can start consuming the messages from these destination inside the RabbitMQ. Once the message is consumed, we want a function to be executed. That's why let's try to update this function definition to updateCardMobileNumber. Please make sure this definition is same as the prefix that we have mentioned here. If you try to search, you should have the matches in two places. Next we can go to the app package of the cards microservice. Let's try to create a new package with the name function. Inside this new package, let's try to create a new class which is CardFunctions. Inside this class we need to add at configuration package followed by at SLF4J . What I can do is, I can try to copy the code from the accounts functions since it is very similar logic here also, we are trying to build an function that is built based upon the Consumer functional interface instead of IAccountService we need to accept ICardService as an input, followed by I need to update the log as well. Let's try to update the log to received updateCardMobileNumber request. And here we need to use ICardService to invoke the updateMobileNumber() method. Let's open ICardService and we can also open the IAccountService or ICustomerService. Because we have similar abstract method, we can copy them and paste inside the ICardService. Once this method is pasted, we can go to CardServiceImpl and try to implement the method. As of now this method is empty. We can try to write the same logic that we have implemented inside the AccountServiceImpl. Let me copy this entire code and paste inside the CardsServiceImpl abstract method here it should be CardsRepository the same we can update here followed by the return type should be Cards. Let me update the variable name to carts and using this variable name I am trying to invoke setMobileNumber() and the same we need to forward to the save method of the CardsRepository. Next we can try to create a private method which is going to take care of publishing a message to the loans microservice. Let me copy this method from the AccountServiceImpl and paste it here. The method name I'll update to updateLoanMobileNumber. The same I'll try to use here next, I'll go to the top of the class and use the annotation SLF4J followed by we need to inject a being of type StreamBridge. Now let's go to the private method. From the private method, we need to publish the message using the output binding with the name updateLoanMobileNumber-out-0. . The same name I need to update inside the logs as well. Once the logs are updated, we can go to the application.yml of cards microservice. Here we already configured these bindings. These bindings take care of publishing the message to the destination that we have mentioned here. I hope you are clear with the changes that we have done so far. Before you come to the next lecture, I want you to make the changes inside the loans microservice. Go to the loans microservice and create the configurations to accept the message that is being published by the cards microservice. Define a function write your logic. Please try making these changes if you face any issues don't worry, I'm going to make these changes in the next lecture. Thank you, bye.
