# Implementing choreography saga pattern - Part 1

Inside my workspace, I'm going to create a new folder with the name section5, because right now we are inside the section5 where we are trying to implement the Choreography Saga pattern into this section5 folder. I'm going to copy the code from the section1 end folder, because inside the section3 and section4 we have implemented the CQRS pattern with the help of Axon Framework. Since I want to show you Plane Saga pattern without any CQRS and Axon framework, by using spring projects alone, I will consider the code present inside the section1 under the end folder. So let me copy all these projects and paste it inside the section5 . From my IntelliJ I'll try to open the new folder that I have created with the name section5 let me click on this open. Once the project is opened inside the IDE, I need to go to this notification and click on this load button. This is going to detect all my microservices as Maven projects. Once all the microservices are loaded as Maven projects, you can try to do a build just to make sure there are no compilation issues. Inside our microservices, since we are looking to implement the Choreography Saga pattern, we need to make sure all our microservices are communicating asynchronously with the events. To implement event driven style of communication inside our microservices, we need to adopt two different projects from the spring ecosystem. The very first one is Spring Cloud Stream, which is going to help you to stream the events to the message queues like RabbitMQ or Kafka. And the second project is Spring Cloud Functions. So with the help of Spring Cloud Functions, we can convert our Java methods into the rest API components. So as a developer, we don't have to create Rest APIs manually by using lot many annotations. Instead what we can do is, we can simply create normal Java methods and let the spring cloud function expose them as their Rest APIs. This approach is going to be super helpful because inside the event driven microservices, we need to build a lot many APIs which can be invoked by other microservices. First, what I can do is, I'll try to open the pom.xml of one of the microservice. Maybe I'll try to start with the customer microservice. Inside the customer microservice I'm going to add two dependencies. So just after my Eureka client dependency, I have added two dependencies with the name Spring Cloud Stream and Spring Cloud Stream Binder rabbit. Since I'm looking to use RabbitMQ as the infrastructure component, I need to make sure I am adding these second dependency. And we don't have to specifically add the spring cloud functions, because whenever we are using Spring Cloud Stream behind the scenes, Spring Cloud Functions will be downloaded automatically. Let me load the maven changes. Once the changes are loaded, I can click on the spring-cloud-stream. Inside the spring-cloud-stream.pom.xml . You should be able to see the dependencies related to the spring cloud function. If you can scroll down, there is a dependency with the name spring-cloud-function-context. If you click on this again, it will take you to some other pom.xml where you will be able to see all the dependencies like spring cloud function core. So this confirms that whenever we add spring cloud stream, we don't have to specifically add the spring cloud function related dependencies. I hope you are convinced. Now let me copy these two dependencies and paste inside other microservices. The next pom.xml I want to open is from the accounts microservice. Here also, just after the Eureka client, I can paste these two dependencies. Load the Maven changes the next pom.xml I'll open from the cards microservice. And here also just after this dependency, I added these two new dependencies. Let me load the maven changes and open the pom.xml of loans microservice. Here also, I'm going to add the same dependencies and load the maven changes. Since we have only these four microservice which are going to be involved inside the Saga pattern that's why we need to make changes inside these four pom.xml. Let me close all these pom.xml. As a next step, since we are building event driven microservices, we need to set up either RabbitMQ or Kafka inside our local system. Since RabbitMQ is very easy to set up compared to Kafka, I choose RabbitMQ. Let's try to create a RabbitMQ container quickly so that our microservices can connect to this container and use these RabbitMQ container for event driven communication. To start the RabbitMQ container, we need to know the docker command that we can use. To know this command we can go to the RabbitMQ website here you can click on these Dogs. Under this dog's page there is a link with the name Install and Upgrade. Let's click on the same and here you'll be able to see the docker command that we can use. Let me copy this command from my terminal. I am trying to execute the command which is going to start the RabbitMQ container inside my system. Don't try to disturb this terminal because right now inside this terminal only the RabbitMQ is running. If you try to press control C, or if you try to close this terminal, then your RabbitMQ container is going to be stopped. Once the RabbitMQ container is started, we need to mention the RabbitMQ connection details inside the application.yml file. Let me open the application.yml file of Customer microservice. Here just under the spring we need to create new element with the name RabbitMQ. So what I can do is, after this JPA properties I'll try to press enter, and I'll press tab one time so that I am under the spring property. And here I can try to mention RabbitMQ properties. So these are all the RabbitMQ properties. So let me align them properly so that they all present under the spring as a parent. So under the spring there is RabbitMQ. Under RabbitMQ we have mentioned the host port username password and connection timeout. By default the username and password are guessed the same. I'm trying to use here. You can get these properties from the GitHub repo. I hope you are clear. Let me copy them and open the application.yml of accounts microservice. Here also I'm going to do the same. Next I'll go to application.yml of Cards microservice. And here also just after this JPA related properties I'm going to mention RabbitMQ related properties once. This is completed, at last you need to open the application.yml file of loans microservice. Inside this file also we need to mention the same properties. After mentioning all these RabbitMQ related properties, we can close all the files as the next step. I need to create a DTO which can be used as a request to the updateMobileNumber API. So since this Dto is going to be used across all the microservices, I want to create this dto under the common project. So under this common project we have a DTO package. Here under this package I'm going to create a new class with the name mobileNumberUpdateDto. On top of this class first let me mention at data, followed by first I'm going to mention the field name which is private string currentMobileNumber. So anyone who want to invoke my updateMobile number API, first they need to mention what is a current mobile number that is being used in all the microservices. Followed by I'm going to create one more field with the name newMobileNumber. These are the two fields that I want to accept as part of the request. Let me mention some validations on top of these two fields. On this very first field, I'm trying to mention the annotations of @NotEmpty and @Pattern, but for these annotations we are getting a compilation error because as of now, inside the pom.xml of common project, we have not added any dependency related to the validations. So that's why what we need to do is we can go to any of our microservice from our microservices. We can copy these dependency, which is spring boot starter validation. Let me paste the same here under the common and load the maven changes. After this loading is completed, the compilation errors will get resolved. The same kind of validation I want to mention on top of the new mobile number as well. I want to make sure these two fields they are not empty and they are following a  rejects pattern which which accepts only ten digit numerical value. Now we have our Dto class ready. As a next step, I'll go to the customer microservice. Inside this customer microservice, I'll try to create a new API inside my CustomerController class. So as of now here we have only the CRUD operations related API. Inside this class let's create a new API of type patch mapping. The API path I'm going to mention as mobile-number. The name can be anything, but I'm trying to give this name here. Next, let me type public ResponseEntity which is going to accept ResponseDto inside the body and the method name I'm going to mention as updateMobileNumber(). And to this method I'm going to accept a @RequestBody of type mobileNumber, updateDto() since I am looking to perform the validations on the data that I am going to receive inside this object. I need to mention one more annotation as well, which is actually valid from this method only. I need to write all the logic. For now let me return a ResponseEntity object with a status of okay. That means the invocation is successful and inside the body I can try to send ResponseDto object with the status as 200. So if you try to see what I have inside this ResponseDto, I'm trying to populate the status code as 200 and status message. I'll try to send this message which is request process successfully. For now this API is a dummy API which is always going to return this dummy response, but we are going to enhance this API to have some business logic. I hope you are clear with whatever we have done so far. Let's try to continue this in the next lecture. Thank you, bye.
