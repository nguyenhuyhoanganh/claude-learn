# Implementing choreography saga pattern - Part 2

Let's write some business logic inside this method. First, I'm going to create an abstract method inside the ICustomerService. Towards the end I'm going to paste a new abstract method with the name updateMobileNumber. It is going to return a boolean value and it is going to accept a parameter of type mobileNumberUpdateDto. . So this method I'm going to call from my rest API method with the help of ICustomerService.updateMobileNumber. and to this method I'm going to pass mobileNumberUpdateDto object, which I have received as an input. Here I can catch the output using a boolean and based upon the boolean, I can try to send various responses to the client application. For now, let's keep it simple this code should work without any issues. As a next step, I should go to the CustomerServiceImpl class. Let me implement a method that we have inside the interface. So this is the method from this method as of now it is returning false value. Inside this method what I can do is, I can try to load the customer details based upon the given mobileNumber. So here I have a logic let me copy this entire logic and paste it here. First I'll try to pass the current to mobileNumber if there is no customer for this current mobile number, then I want to throw this ResourceNotFoundException. Maybe what I can do is, I can try to create a field with the name currentMobileNum and the value from that dto I can try to assign to this field. The same field I can try to pass to this customerRepository method. And here also I can mention the same in case if there is a valid customer available with the given current mobile number, then immediately using the same CustomerEntity object I want to set the new mobileNumber by invoking setMobileNumber and the new mobile number we can get from the DTO by invoking these getNewMobileNumber() method. And towards the end I can try to invoke CustomerRepository.save(), and to this save() method I can pass the CustomerEntity. At the end we can try to pass the true as a boolean value. With this logic what is going to happen? The mobile number will get updated only inside the customer database, but we don't want to stop here itself. As a next step, we want these customer microservice to publish an event or to forward the request to the account microservice as well to update the mobile number in its own database. How to do that? It's very simple. With the help of Spring Cloud Functions and Spring Cloud streaming, we are going to implement asynchronous communication or event driven microservice. Here what I'm going to do is, I'm going to create a new private method, which is going to return void and the method name is updateAccountMobileNumber(). And to this method I'm going to pass mobileNumberUpdateDto object as an input. So this private method I want to invoke from these upper() method. As soon as we are done saving the new mobileNumber inside the customer database, I want to invoke these updateAccountMobileNumber by providing the mobileNumberUpdateDto as an input to publish an event to the queue we don't have to worry much. We just have to inject a bean into these ServiceImpl class. So first let me add final here, followed by I'm going to duplicate the same line. And this time I'm going to inject the bean of type StreamBridge. So this StreamBridge is coming from the Spring Cloud Functions package. Let me rename the variable name to streamBridge using this streamBridge I can forward the event to one of the QR exchange available inside the RabbitMQ. So let me use the streamBridge.send()method to the send() method. We need to mention what is the binding name along with the what is a payload data that we want to send. The binding name that I want to mention here is, updateAccountMobileNumber-out-0. The same binding name I'm going to configure inside the application.yml as well. I'll share more details about this binding name in few seconds. The next parameter is mobileNumberUpdateDto, which is the payload data that we want to send. I can add some logs also here, just to make sure that my entire flow is working properly. What I can do is, I'll first try to add an annotation of Lombok which is slf4j. After mentioning this annotation, I'm trying to add some loggers. So first I'm trying to add a logger saying that sending updateAccountMobileNumber Request for the details. This entire object is going to be printed onto the console along with this log statement. Next, I can try to add one more logger towards the end of the method. So let me add the logger. So this logger is going to print isUpdateAccountMobileNumber request successfully triggered or not. So to this logger we need to send a variable result. The data inside this result variable we can catch from the send() method output. So let me create var result is equal to StreamBridge.send(). So if you click on the send button you'll be able to see we are going to get a boolean. The same boolean we are going to print onto the console to confirm whether the data is sent to the account microservice or not, with the help of this binding name. With this logic, as of now, we are trying to send our data to this binding name. But these binding details, we have not configured it inside the application.yml. Let me open application.yml of Customer microservice. Here after the RabbitMQ related configurations, I'm going to type cloud please make sure this cloud is present under the spring as a child after the cloud I want to mention stream and under the stream I want to mention bindings. And under these bindings we need to mention the same binder name that we have defined here. So let me copy this entire binding name and paste it here. So whenever someone sends an request to this binding name, I want the data to be forwarded to a specific destination. That's why under these binding configurations we need to type destination. The destination name I'm going to mention as update-account-mobile-number. So with this what is going to happen inside the RabbitMQ there will be a destination created with these name to the same destination my request is going to be forwarded and the account side we need to make some configurations to listen to this destination and read the data from the same destination. So let's go to the application.yml of accounts microservice. Here also I'm going to copy this entire configurations and paste under the RabbitMQ. Just make sure this cloud is started at the same position of RabbitMQ, so that it will be under the spring property. Inside these configurations since account is going to accept the request, we need to update these binding name to -in-0 . So these are the standard naming convention that you need to follow inside the spring Cloud functions and Spring Cloud stream. The same I'm trying to follow here. So the binding name is going to be updateAccountMobileNumber-n-0. . Under this binding name we need to make sure we are mentioning the same destination as we have mentioned here. The destination has to be same in the case of customer, since it is sending the request outside, we need to make sure we are using out whereas inside the accounts microservice since it is going to accept the request, we need to mention in inside the binding name and the destination has to be the same. Once the destination is mentioned, we need to mention one more configuration here group. To this group I want to assign a value by reading the property which is spring.application.name. So this spring.application.name we have already configured here with the value accounts. So these accounts will be tagged to this group. The reason why we need to mention group for the input binding but not for the output binding is it is very simple. In real applications we will end up having multiple instances of microservices. Here my accounts microservice is acting as a consumer. If there are ten different accounts microservice instances running behind the scenes, I want all of them to be registered with the same group name, which is accounts itself. When they register with the same group name what is going to happen behind the scenes is they are going to act as a consumer group. When a consumer group is formed, the spring cloud stream will make sure that the request is only forwarded to one of the instance of the consumer microservice, which is accounts. Whereas in the customer side, we don't have to mention because right now customer is a producer. Producer should not have any group details because while they are producing a message, the request is already being processed by one of the microservice instance. Once this producer publishes the message, it will reach to the RabbitMQ. Since there is no need of load balancing here, we don't need to mention group details here. Whereas on the consumer side, since there is a load balancing is required and since we want to make sure only one of the consumer microservice instance needs to process the request that's why we need to mention these group category. With these configurations, what will happen is the spring cloud stream and RabbitMQ they are going to work together and they will try to forward the request that comes to this destination, to this binding. But with that, the story is not going to end. Once the request is received by this binding, we need to give a clue to the spring cloud stream to which Java method are to which Rest API it needs to forward the request. In other words, we need to convey to the spring cloud stream where is the business logic present to process the request that is received by these binding. So from these destination the message will be read, but the message needs to be executed by some business logic to convey the same what we are going to do is, we are going to make some function configurations just below this cloud I'm going to create a property which is function followed by definition. And this definition I need to mention a value which is updateAccountMobileNumber. Please make sure the function definition is same as the prefix that we have used inside the input binding name. If you follow this naming convention automatically, Spring Cloud Stream will know to which function it has to forward the request that is going to be received by these input binding. So now I have created a spring Cloud function definition. As a next step, I need to create a function which is going to have all the business logic. Let me go to the accounts microservice and inside the accounts microservice I'm going to create a new package with the name function. And inside this function I'm going to create a new Java class AccountFunctions. Inside this class we can create any number of functions. Let me mention the annotation @Configuration followed by at SLF4J. Inside this class we can create a new function or a new method. Let me type public after the public I'm going to type consumer because I want to create a function that adopts the Consumer functional interface. Since I'm going to build a logic which only accepts the input but not publish any output, I'm trying to follow the Consumer functional interface. So what my function it is going to accept it is going to accept mobileNumberUpdateDto as an input, followed by I need to mention the method name. Please make sure you are mentioning the method name same as the definition name that you have given here. So I'm trying to copy the same and paste it here for now my method it is an empty method. Inside this empty method basically I need to return a lambda expression which is going to be executed whenever my function is going to be invoked by the spring cloud stream. So let me type return and lambda expression with the variable name as mobileNumberUpdateDto. For now, I'm going to keep the lambda expression as empty. Since my consumer is not imported, let me import the consumer from the java.util.function. With that the compilation is resolved. I hope you are clear what we have done so far inside this lecture, we are trying to publish an message from the CustomerServiceImpl and the same we are trying to process with this function. So please make sure the method name or function name is same as the definition name and the same definition name you should use as a prefix inside the input binding that you have defined here. To be consistent, even on the output binding that you have defined inside the customer application.yml. . Please make sure you are using the same prefix and the destination also should be same, both on the customer side and the account side. As a next step, let's try to write some logic inside this function that we have created. Thank you, bye.
