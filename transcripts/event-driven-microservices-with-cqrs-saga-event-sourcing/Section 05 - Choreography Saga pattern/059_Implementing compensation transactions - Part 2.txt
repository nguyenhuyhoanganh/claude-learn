# Implementing compensation transactions - Part 2

As of now, we have handled the scenarios of exceptions that may occur inside the customer microservice or inside the account microservice. As a next step, let's try to handle the scenario where a runtime exception is going to happen inside the cards microservice. When an exception happens inside cards microservice, what it has to do, it has to roll back its own transaction on the card's database, followed by it needs to invoke the compensation transaction inside the account microservice and the account microservice then it has to invoke the compensation transaction inside the customer microservice. This is the flow that we want to implement. Let's go to the card microservice inside the CardServiceImpl as of now, this is the logic that we have. First let me mention add transactional followed by I'm going to create a boolean with the variable name as result as false. Let me mention this entire code in inside a try block. Later, I can try to copy the catch block from the accounts microservice. Let me copy this entire code and paste it here. I can get rid of this duplicate return statement. We need to create the new private method, which is going to be responsible to trigger the compensation transaction. Let me copy this method and paste it here. And the method name I'm going to mention rollbackAccountMobileNumber. Because the cards microservice is only responsible to initiate the compensation transaction and the account microservice. That's why I have constructed this name. Let me update here as well. We need to update the log statements and the binding name as well. These binding configurations I need to make inside the application.yml of cards microservice. What I can do is, I will try to copy these two configurations and paste it here. . Let me go to the CardServiceImpl and get these binding name. The same I'm trying to mention here. Now, coming to the destination name, I'll try to mention rollback-account-mobile-number. Now my cards microservice is doing its job of triggering the compensation transaction on the accounts microservice. The loans microservice is not yet come into the picture because the request itself never reached the loans microservice. That's why let's forget about the loans microservice for a few seconds. Next, I need to go to the application.yml of accounts microservice. And here I need to create an inbound binding configurations. Let me copy the existing inbound configurations and these prefix I can copy and paste it here the destination also I can copy and paste it here. Next we need to create a function. That's why let me update the new function definition which is rollbackAccountMobileNumber. If you have multiple function definitions, they should be separated with the semicolon. Once the definition details are updated, I can go to account functions and I'll try to copy this entire method one more time. Let me update the method name to rollback account, mobileNumber and the logger also I'm going to update. And here instead of invoking the updateMobileNumber, we need to invoke the rollbackMobileNumber. First we need to create an abstract method just like how we have inside the ICustomerService, the same abstract method we need to create inside the IAccountService. Let me invoke the same abstract method from the function logic. Once this code is updated, we can go to AccountServiceImpl. Inside this AccountServiceImpl, we can copy the code from the CustomerServiceImpl. Here we have this rollbackMobileNumber method. Let me copy the same and paste it inside the CustomerServiceImpl. Here we need to use the AccountsRepository, followed by we need to update this customer two accounts let's try to update the variable name to accounts next using the same variable name I'm going to invoke the setMobileNumber and set the old mobileNumber with the help of getCurrentMobileNumber. At last, I need to call the save method by sending these Accounts object. With this logic, what is going to happen is the account details are going to be rolled back by updating to the previous old number, which is available under this current mobile number field. But the job of the accounts microservice is only half done here, because once the accounts microservice roll back its own data, next it needs to trigger the compensation transaction of the customer microservice. How to do that? It's very easy. We already have the rollbackCustomerMobileNumber, which we are trying to invoke from the catch block of the accounts microservice. The same method we can try to invoke from this method, maybe after the saveCode(), I can try to invoke a rollbackCustomerMobileNumber and to the same I can try to pass mobileNumberUpdateDto. Now we have handled the scenario of Cards microservice as well. At last we should also take care of loans microservice as well. Let's do this very quickly. First I need to go to the LoansServiceImpl. Inside this ServiceImpl on top of this updateMobileNumber method I'm going to mention at transactional, followed by I'm going to create a boolean variable with the variable name as result, and with the default value as false. Let me create the try block and inside this try block I can try to move this entire code, followed by I'm going to get the catch block code from the CardsServiceImpl. Let me take this entire catch block and paste it here. Let me format the code as a next step We need to create a rollbackCardMobileNumber() method because the loans microservice is responsible to trigger the compensation transaction and the Card microservice. Let me go to the CardsServiceImpl and take this rollbackAccountMobileNumber and paste it here. The method name I'm going to update to rollbackCardMobileNumber. Let me update the log statements and the binding name as well. Next we need to go to the application.yml of loans microservice. Here we need to copy these outbound configurations and paste it here followed by we can copy the method name and mention the name as a prefix here. The destination should be rollback-card-mobileNumber. Once we make all these changes inside the loans microservice, we can go to the application.yml of cards microservice. And here I'll try to copy these inbound configurations and paste one more time, followed by I'll try to get the same prefix name from the loans microservice and mention here the destination also. We can copy the same and paste it here. Once these input binding configurations are completed, I'm going to update the Spring Cloud function definition with the new function name, which is rollbackCardMobileNumber. Let me go to the function class present inside the card microservice. Here I'm going to copy this method and paste one more time. The method name should be updated to rollbackCardMobileNumber. Let me update the logger statement followed by we need to open the ICardsService. Here I need to create a new abstract method which I can get from the IAccountService. Let me copy this entire abstract method and paste it inside the ICardsService. Once the abstract method is created here, I can try to invoke the same, followed by we can go to CradServiceImpl. . Here we can write the logic of the abstract method. Since we have the similar logic inside the AccountServiceImpl. . We can copy the code from here and paste it inside the CardsServiceImpl. . Let me update this two CardsRepository followed by I'm going to update the return type two cards and the variable name also two cards. Using the same cards variable I'm going to invoke the save() method and at last we need to invoke the compensation transaction and the accounts microservice. Because for the cards microservice, the previous microservice will be the account microservice. That's why what we can do is here we can try to invoke the rollbackAccountMobileNumber, which is going to take care of initiating the compensation transaction on the account microservice. With this, I'm assuming we have done all the changes. Let's try to validate all these changes in the next lecture by seeing the demo. Thank you and I'll catch you in the next lecture, bye.
