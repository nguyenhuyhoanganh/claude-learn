# Implementing compensation transactions - Part 1

While designing the compensation transactions, you need to think of all the possibilities and all the places where your code can go wrong, or it can throw an exception. That's why let's try to follow the same strategy. Let's imagine one of the end user is trying to send the request to the mobile number API. The request will be processed by the customer microservice. First, let's imagine that runtime exception can happen inside these customer microservice itself. Before we try to dispatch to the account microservice, the exception can happen here itself. As the initial step let's try to handle the same. I'll go to the CustomerServiceImpl where we are trying to make the database updates. This is the place where we are trying to update the mobileNumber. Let's imagine just after the save() method, I got some RuntimeException. To show you the demo of RuntimeException what I'll do is, I'll try to add the code, which is throw new RuntimeException with a message. Some error occurred while updating mobileNumber, and since there is a runtime exception, all the subsequent code will not be executed. I need to comment them to resolve the compilation error. In this scenario, Spring Data JPA framework can automatically rollback the transaction that happened during this line of execution, but Spring Data JPA won't do that automatically. To make it possible, we need to mention @Transactional annotation. When we mention this annotation, what is going to happen is the logic present inside this method is going to be executed as a transaction. If any runtime exception happens, which results into the runtime exception or its child exceptions, the transaction will be automatically rolled back by the spring data JPA. Let me show you the demo very quickly. Let me save these changes to the build once the build is completed inside the postman I'll try to invoke the API which is choreographyMobileNumber. This time I'll try to mention the current mobile number as 7679 and new mobile number as 7680. Let me click on the send button. You can see I'm getting some error, but let's validate if the mobile number is this new mobile number or the old mobile number. If I go to the fetchCustomerSummary and try to invoke the 7679, you will be able to see I'm getting the customer data, which confirms that the new mobile number, which might have saved with the help of the save() method, is rolled back automatically by the customer microservice. With the changes that we have done so far, we are good with the scenario. If the exception happens inside the customer microservice itself. Next, let's imagine the exception can happen inside the accounts microservice. To write the logic related to the accounts microservice, I need to go to the AccountServiceImpl and here just above the updateMobileNumber I can keep the @Transactional annotation, but this time it is going to be tricky because by just mentioning this annotation, what I'm going to achieve, I will be able to roll back the database changes that are committed only inside the accounts microservice. I can't trigger a compensation transaction to trigger a compensation transaction we need to make sure we are catching the exception and inside the exception handling we need to trigger the compensation transaction inside the customer microservice. I hope you are clear. Let's go to the CustomerServiceImpl and I'm going to comment these throw new RuntimeException code and uncomment all this code. Next I'll go to AccountServiceImpl inside this updateMobileNumber method of AccountService. First I'm going to introduce a try and catch block. I'll try to move this entire code into the try block, followed by I will write a catch block that is going to catch all type of exceptions. For now the catch block it is going to be empty. There is a compilation error saying that missing return statement to resolve this what I can do is, I'll try to create a boolean variable with the name result and with the default value as false. This result I'll try to update to true if all the code present inside the try block is executed the same, I'll try to return as an output from the method. In the case of exception, what I want to do is, first I'll try to log the error details, followed by I need to roll back the existing transaction that updates the data inside the Accounts microservice. Since we are catching the exception, and since we are not going to throw this exception to the parent method, which will be of no use. We need to manually roll back the current transaction. It's very easy, we just have to use the class which is TransactionAspectSupport. Using this class, we need to invoke the current transaction status followed by set rollback only once the current transaction is rolled back inside the accounts microservice. As a next step, I need to initiate the compensation transaction on the customer microservice. Let me create a new private method with the name rollbackCustomerMobileNumber. Let me update these log statements as well. And here the binding name also I'm going to update to rollbackCustomerMobileNumber-out-0. . This rollbackCustomerMobileNumber I'm going to call from the catch block along with the mobileNumberUpdatedDto. Next we need to go to the application.yml. Off accounts microservice and we need to make sure we are configuring the destination name for the new binding that we have created. So let me go to the AccountServiceImpl. . And this is the binding name. Let me copy it here. The destination name I'm going to mention as rollbackCustomerMobileNumber. With these changes the accounts microservice whenever an runtime exception happens, it is going to publish a message to this destination. As a next step, we need to make the changes inside the customer microservice to consume the messages that are going to be published into this destination. Let's go to the application.yml file of customer microservice. Here what we can do is, we can try to copy these input binding names and paste it here. Let me update this binding name to rollbackCustomerMobileNumber the destination also, I'll try to copy from here and paste it here. Next we need to create a function which is going to be executed whenever the message is being consumed from this destination. The function definition we need to mention the same prefix value. Let me copy this value and mention the new function here. Whenever you want to mention multiple function definitions, you need to use this semicolon. Once this definition details are updated, we can go to customer functions. And here I can try to create one more function by copying this code. And I'll try to update the function name to rollbackCustomerMobileNumber. Let me update the log statement also saying that receivedRollbackCustomerMobileNumber request. As of now, inside this function we are simply logging the statement but what we have to do, we have to actually execute a database transaction to roll back to the previous old mobile number. That's why I'll try to inject ICustomerService as a dependency to this function, followed by I'll go to the ICustomerService, and here I will create a new abstract method with the name rollbackMobileNumber. The same I'm going to trigger from the customer functions. Let me write the code with the help of ICustomerService.rollbackMobileNumber and to this method I can pass mobileNumberUpdateDto as an input. Next we need to go to the CustomerServiceImpl. And here we need to implement a method with the name rollbackMobileNumber. Inside this rollbackMobileNumber, what I can do is, I can try to copy this code and paste it here. Because the logic that we have to write is very similar. Let me delete this commented code here there is a trick that you need to be aware by the time this rollbackMobileNumber is executed, the old mobile number is already replaced with the new mobileNumber as part of this method execution. So when you want to roll back, first you need to load the existing data from the database using the new mobileNumber. That's why you need to type get new mobileNumber. Let me update this variable name to newMobileNumber. Using this newMobileNumber, we need to fetch the existing record. Once the customer record is fetched, we need to set the old mobile number by using the getCurrentMobileNumber(). I hope you are seeing the trick here. At last we need to invoke the save() method and we don't have to invoke this private method which is updateAccountMobileNumber(). So this is going to complete the compensation transaction and the customer microservice. I hope you are seeing on how to build the compensation transaction. Let's try to continue this discussion in the next lecture as well. Thank you, bye.
