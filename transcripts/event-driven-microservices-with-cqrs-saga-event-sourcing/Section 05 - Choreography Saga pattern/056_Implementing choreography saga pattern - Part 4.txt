# Implementing choreography saga pattern - Part 4

I hope you are successful in making the changes inside the lone microservice as well. If not, don't worry, you can simply follow my discussion from the cards microservice I can try to copy these entire configurations followed by I can go to application.yml of loans microservice. Here, just under the RabbitMQ related configurations, I'm going to paste the configurations related to spring Cloud function and Spring cloud stream. Let me update the function definition name to updateLoanMobileNumber. The same thing I'm going to mention here inside the input stream binding and the destination should be updateLoanMobileNumber. Group name can be same thing which is spring.application.name. Now let's think about do we need these configurations inside the loans microservice. So what is this configurations for. Usually the outbound bindings we are going to use whenever we want to publish an message from a given microservice to another microservice. But once we update the mobileNumber inside the loans microservice, then there is no next to microservice to invoke, the business flow should end. Initially, the request will be received by the customer, followed by accounts followed by cards, followed by loans so we can stop the entire business flow inside the loans microservice. Since we are done updating the mobileNumber inside all the microservices, that is one option the other option is, we can publish an event into the message queue confirming that loans microservice also updated its mobileNumber, which confirms the entire flow of mobileNumber updation is completed successfully. This event can be consumed by any other client, application or any other microservices. Just to make sure the flow is completed successfully. What I'll do is, I'll try to follow the second option where I'll try to publish an event confirming that mobile number update is successful in all the services. The same message I'll try to consume inside the customer microservice, which is the originator for this entire business flow. My customer microservice can do whatever it wants based upon these status that it receives. Maybe it can update a flag inside the database, or it can provide the same status to the client applications. In case the client applications are interested to know the overall status, they can keep polling the customer microservice, and this customer microservice can update the entire transaction status whether it is successful or not. That's why I'm going to have a outbound binding here, but I'll try to update the outbound binding name to updateMobileNumberStatus. I will get rid of this loan and mention the status towards the end destination also, I'll just mention update-mobile-number-status. As a next step, we need to create a function inside the loans microservice with this name. Let me go to the loans microservice and create a new package with the name function. Inside this package, I'm going to create a new Java class with the name LoanFunctions inside this class as usual, first we need to mention configuration annotation followed by SLF4J. Next I'll go to the CardsFunctions and I'll try to copy this entire function definition into the LoansFunction class. Here first, we need to inject a ILoanService to the function, followed by I need to update the function name also, seems I have not updated properly inside the CardsFunctions. So here the function name should be whatever definition name that we have defined inside the application.yml. of cards microservice. So here this is the function name. Let me mention the same properly. Now I'll go to the application.yml of loans microservice. And this is the function name that I need to mention inside the LoansFunctions. Next I can try to update the logger here saying that updateLoanMobileNumber request for the details. Once this logger is updated, I can try to update this variable name. Now let me open a loan service and inside this interface I need to create a new abstract method just like we have did inside the ICardService. Let me copy this entire abstract method and paste inside the ILoanService. After making this change, I'll go to LoanServiceImpl and try to implement the method. Before we write some logic inside this method, let's try to add the annotation SLF4J followed by I'm going to mark this variable as final. Next I'm going to inject the bean of type StreamBridge. Let me update the variable name to StreamBridge. Next we can go to the CardServiceImpl and try to copy these entire logic. Let me paste inside the LoanServiceImpl. Once this logic is pasted I need to make some changes first here we need to use LoansRepository in all the places followed by the return type should be loans with the variable name as loans using the same variable name I'm going to update the subsequent code and towards the end I need to update the method name to updateMobileNumberStatus. Let me use the same here as well inside the loggers also, I can update the method name saying that updateMobileNumber status and to the send() method also, we need to mention the binding name as updateMobileNumber-out-zero the same we have configured inside application.yml. As you can see here, this is the same binding. We made all the required changes on the loans microservice. As a last step, we need to update the code inside the customer microservice as well to consume the messages that we are going to dispatch to the destination update -mobile. -number -status. Let me open the application.yml of customer microservice here as of now we have only the output bindings. What I can do is, I'll try to copy the input bindings and paste it here. The binding name should be updateMobileNumber status-in-0, and the destination also, we can copy from the loans microservice and paste it here. As a next step, we need to define a function which is going to take care of consuming the message from this destination. Let me copy the function related configurations and paste here inside my customer microservice, I'll try to mention the function name as updateMobileNumberStatus. Next we can go to the customer microservice package structure and here we are going to create a new package with the name function. Inside this function package I'm going to create a new Java class with the name customer functions. I'm going to mention at configuration followed by at SLF4J. Next we can copy the code from the loans function and paste it here. The method name I need to update. Let me go to application.yml and copy this definition and mention here. Inside this business logic, I'm not going to trigger any database changes. I'm going to simply add a logger message for now. But in real applications, there are unlimited possibilities on what you can do by knowing the overall status of the mobileNumber. Update the logger says received updateMobileNumberStatusRequest for the details. When a developer sees this logger, it's a confirmation that the whole transaction of the Saga pattern is completed successfully, and I don't need any input to this function. Let me delete this we should be good with these changes. This is the flow that we have implemented so far. Initially, the end user is going to invoke the rest API that we are exposing in the customer microservice. The API path is mobileNumber. The customer microservice will execute its own business logic, like updating the customer database. Once it is completed, it is going to send a message to the RabbitMQ from the same destination name the account's microservice will start consuming the message. Once the account microservice process its business logic, it is going to publish a message to the RabbitMQ with a destination name. Using the same destination name that used by the accounts microservice the cards microservice it is going to read the message. It is going to execute its logic, next it is going to publish a message to the RabbitMQ followed by the loans microservice is going to read the message from the RabbitMQ. Once the loans microservice updated its database, it is going to publish an message to the RabbitMQ conveying the overall process is completed and the same message is going to be consumed by the customer microservice. So this is the flow that we have implemented so far. By the time I come to the next lecture, I will try to revisit all the code that we have written, and I'll make sure there are no copy paste mistakes. Once we make sure all the code changes are completed, we can try to test the overall flow. Off course, we have implemented only the happy path. We are yet to develop the logic around the compensating transactions. First, let's make sure this happy path is working, followed by we can focus on the error scenarios or the runtime exception scenarios. Thank you, bye.
