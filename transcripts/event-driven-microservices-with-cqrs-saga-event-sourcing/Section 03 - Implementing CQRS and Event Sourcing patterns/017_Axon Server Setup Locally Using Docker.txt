# Axon Server Setup Locally Using Docker

As we are discussing previously, Axon Server is an important component inside the Axon IQ product, which is going to provide the infrastructure capabilities to implement the Event driven patterns using the Docker we are going to set up these Axon server locally. The steps that we need to follow to set up these products are going to change very frequently. That's why what I have done is, I have documented the steps that we need to follow inside the GitHub repo itself. So this is the GitHub repo for this course which is github.com/eazybytes event-driven-microservices . Here, since we are into the section three, you need to click on the section3 folder. If you can scroll down, you'll be able to see the Readme document where I have mentioned all the steps that we need to follow to set up Axon Server locally using Docker. For now, I'm going to follow all the steps that I have documented here, but in future you need to check this Readme document. So if there are any changes, I'm going to update this Readme document by following these Readme documents you need to set up the Axon server locally inside your system as well. Let's try to understand what steps I have defined here as the very first step we need to create the Axon server folder structure. So first we need to create a folder with the name Axon server. So this folder you can create at any place. But preferably I would recommend you to create it on your desktop. Once you have created this Axon server folder inside your desktop, you need to create three different sub folders, namely config, data and events. As you can see here, I have an Axon server folder inside my desktop and inside this folder I have created three different sub folders, namely config, data and events. Once these three sub folders are created inside the config folder, you need to create a file with the name axonserver.properties. You need to follow the same naming convention because Axon Server is going to look for these file name. Once this property file is created, you need to paste these four different properties. Let me go to my config folder and here I have a file with the name axonserver.properties.. Let me show you what do I have inside these properties. I have defined these four different properties. The very first one is server.port. Against this I have defined the port number which is 8024. I'm telling to the axon server to start at this port number. This is the default port number, but I'm trying to re-initiate the same one more time. Next, I want to provide a name to my Axon server here I'm trying to provide the value as EazyBank Axon Server. Most of the properties related to the Axon IQ product they are going to always start with the prefix, which is Axon IQ. But server.port is different because at the end of the day, Axon server is also built by using spring and spring boot. That's why we're trying to follow the same spring boot property here, but other properties which are going to be leveraged by the Axon IQ, they're always going to have a prefix, which is Axon IQ. Second property I have mentioned the name as axoniq.axonserver.name followed by third property is related to the host name. Host name I'm trying to mention local host because we are trying to set up the server inside our local system. Next, we're trying to enable the dev mode by mentioning the true against to this value. Since we are exploring this product inside our local system, we should always make sure we have enabled this. If you don't enable dev mode, then it is going to be considered as a production environment. And with that you may need to handle lot many production ready setup standards in real applications also, this entire Axon server related setup is going to take care by the infra people or DevOps people. As a developer, you don't have to worry how to set up these Axon server. I'm just trying to provide all these steps so that at least you can set up the server inside your local system for your exploration purpose. I also mentioned what is the purpose of these each property inside the Readme document. If you still have any questions, you can read these information that I have provided here. Next, we need to run these docker command to set up the Axon server inside our local system. So the command is docker run -d. . -d indicates that I want to start this command in a detached mode. And using this name, I'm trying to give the Axon server as a name to my Docker container. Next I'm trying to provide the port mapping Axon server is going to start at two different ports. The very first one is 8024. Ports 8024 is going to used for the HTTP interface. For example, if you want to access the Axon server dashboard, we are going to leverage these 8024 port, whereas 8124 is a port used for the gRPC interface that Axon framework application use to communicate within the server. That's why we need to provide the port mapping for both 8024 and 8124, but we are always going to use 8024 to access the dashboard of the Axon server. Next I'm trying to provide the volume configurations. Volume is a concept inside Docker, using which we can mount a local folder to our Docker container. If you see here these are the subfolders that I have created previously data, events and config. These local folders. I want to mount them to the Docker container that I am going to create. So inside that Docker container that I'm going to create, it is going to have these folders like data events and config. But whatever data that my container is going to write into these folders it is going to be written into my local folders that we have created previously. Basically with these volume configuration, what we are trying to achieve is, even if I delete my Axon server container, or even if I restart my Axon server container, I don't want to lose all the data that I have stored inside the container, since with the help of these volumes, we are trying to store all the data inside our local system in future, whenever we set up a new container or whenever we are trying to restart our existing container, it is always going to read the data from our local folders that we have defined here. Under that data subfolder Axon server, it is going to store all its internal data. Similarly, under the events folder, Axon Server is going to store all the events that are going to be published as part of our business logic and config is a folder under which we are going to provide all our configurations. Inside this config folder only we have defined a property file with the name axon.server.properties. Using this properties folder, we are trying to convey our required configurations to the container. At last we need to mention what is a Docker image name. So the docker image name is axoniq/actionserver. So you can copy this entire command and try to execute the same command using a terminal. Throughout this course I'm going to use this beautiful terminal which is war. If you type war terminal inside Google you can go to the website which is Wabedo. This is an intelligent terminal which is going to work by leveraging artificial intelligence. Anytime you have some questions around the commands, you can also ask such questions inside this terminal itself. Overall, it is a nice terminal if you are using Mac OS, you can use this terminal for windows they are planning to launch very soon by the time you see this video if the windows version is also available, I would recommend you to use the same. Otherwise you can use your normal terminals that are available as part of your operating system. Let me try to execute this command as soon as I try to execute this command, you can see a container is created. You need to make sure the Docker desktop is running behind the scenes. Here inside my Docker desktop I have this Axon service container created. Let me click on this container name. You'll be able to see that my container is started. Since Axon Server is also built by using Spring Boot, it is trying to follow the similar style of logs just to confirm whether the Axon server started successfully or not. We can try to access the URL which is localhost:8024. So this is the Axon server dashboard. Since we set up this container very first time, you need to make sure you are clicking on this complete button. Here you have various options. You need to select the start standalone node, which means we are trying to start the Axon server as a single node inside our local. We can also set up this axon server  in a multi node cluster style, which is an expectation for enterprise applications inside their production environment. We don't have to worry much about it. The DevOps people, they are going to take care of it in production environment. So once you select this option please make sure you click on this complete button. And with that, the setup of your action server is going to complete. As of now, you can see there is a context with the name admin and default. So these are the contexts that are going to be used by the axon server itself. But once our microservices start registering with these axon  server we will be able to see our microservices as well here. I hope you are also successful setting up this axon server inside your local. Please, please leverage the steps that I have mentioned here. By following these steps, you should be able to easily set up axon Server without setting up these inside your local system. You will not be able to practice all the demos that we are going to discuss throughout this course. Thank you and I'll catch you in the next lecture, bye.
