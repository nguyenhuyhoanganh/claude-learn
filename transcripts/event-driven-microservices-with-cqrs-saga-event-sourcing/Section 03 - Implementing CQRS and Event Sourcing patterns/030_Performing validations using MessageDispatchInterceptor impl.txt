# Performing validations using MessageDispatchInterceptor implementation - Part 1

When a command is dispatched from the Controller class, the command will be handled inside the Aggregate class. As part of this command handling logic, we can perform some validations. Once we are satisfied with these validations, then only we can proceed with the next set of logic. But this is a very basic style of handling the validations inside the Axon framework. With this basic style, what we are trying to do is, we are trying to perform the validations once the command is dispatched to the handler. So there is no specific disadvantage with this approach, but it is generally recommended by the Axon team to implement these validations. Before dispatching of these command to the handler logic, there is an interface with the name MessageDispatchInterceptor. Inside the Axon framework, you can define an implementation class for this interface, and you can override this abstract method which is handle. So with that, what is going to happen is whenever a command is being dispatched with the help of CommandGateway, just before handing over the command to the command handler logic, whatever logic that you are going to define inside your interceptor is going to be executed as a next step what we can do is, we can try to move all these validation logic to the InterceptorImplementation class. To create an InterceptorImplementation class under the command package, I'm going to create a new sub package with the name interceptor. Under these interceptor sub package, I'm going to create a new Java class with the name CustomerCommandInterceptor on top of this class. First let me mention the @Component and @RequiredArgsConstructor Inside this class, we need to implement the interface which is MessageDispatchInterceptor and to this interface under the generics, we need to type command message which will have question mark under generics. Anytime we are trying to dispatch a command, it is going to be dispatched as a command message. If you open this CommandMessage, it is a interface inside the framework, which is going to have the command name metadata about the command. And if you try to open this message, it is going to have the methods like identifier(), payload(). So basically whatever payload that you are trying to send with the help of your Command object, it is going to be wrapped into this CommandMessage object. So all such CommandMessages we want to intercept. That's why we need to mention these command message under the generics of MessageDispatchInterceptor. Now let me hover on to this compilation error and select these implement methods. Here I want to implement the abstract method. Let me click on this okay. And this is the method signature that we got from the parent interface. If you can go to the parent interface you can try to read the documentation of these abstract method. Apply these interceptor to the given list of messages as of now, from the controller layer, we are trying to dispatch a single command, but this method accepts a list of command messages. The reason is Axon framework also supports batch processing of the commands. So inside the batch processing, since you are going to deal with the collection of command messages to handle such scenarios, this method is accepting list of command messages in our scenario the list is going to have a single message. This method what it is going to return. It is going to return a function that can be invoked to obtain a modified version of the message at each position in the list. So basically we need to return a lambda function that adhere to the by function functional interface. This lambda expression is going to be executed for each of the command message that is going to be received by the interceptor. If needed, you can perform the validations or you can modify the command message it's up to you what you want to write inside this implementation meditation class. Since this method needs to return a lambda expression, I'm going to type lambda and to this lambda as the very first parameter we need to accept the index, which represents the current index value of the command message that is being processed inside the list. Followed by, we are going to receive the command message as an input, the same we need to pass as an input to the lambda expression. Let me mention the parameter name as command. So this is the input to my lambda expression. Now from this lambda expression, I need to return the same command message that I have received after making modifications. If I have some validation failures, then I can happily throw the exception as well. Here inside this lambda expression, I'm going to write a business logic with the help of if condition. Inside this if condition, I'm going to check whether the given command message payload class type is equal to the createCustomerCommand. Let me invoke that .equals to this equals I'm going to pass command.getPayloadType(). So if the given command message is of type createCustomerCommand. I want these if condition to be executed. Now inside this if condition I can do that typecasting of the payload into an CreateCustomerCommand object. That's what I have done here with this line. Once that typecasting is completed, we want to execute our validation logic. We already returned the validation logic here. Let me copy this entire validation logic and paste it here inside the Interceptor. So here I'm getting a error related to the CustomerRepository. I need to inject the been CustomerRepository into these InterceptorImplementation class. Once the bean is injected as a dependency, the error is going to be resolved. As of now, we don't have any errors. Since we have written these validation logic here. Now we can go to the aggregate and we can delete this entire code. But I'm going to keep these as a commented code for your reference. Once we commented we no more needed these method parameter as well. We can delete this, but I'm going to keep this for your reference in case if you want to test this code by uncommenting it. Let's go to the Interceptor and once the validation is executed, if all looks good and if this condition is not met, what I want to do is, I want to simply return whatever command message I have received. So that's what we are trying to do here. If you have requirements to change these command messages to a new version, or if you want to make some modifications, you can happily write all such logic here. As of now, we just implemented the validation logic for the createCustomerCommand. Very similarly, let's try to build the logic for other two commands as well. I'm going to copy the same condition multiple times. So the second if condition I'm going to change to else if. And this time I'm going to check whether the given command message is equal to updateCustomerCommand. If yes, I'm going to do that typecasting to updateCustomerCommand. This time inside the updateCustomerCommand validation I don't want to use this logic. Instead, I'm going to bring new logic. As you can see here first, I'm trying to fetch the existing customer based upon the mobileNumber and activeSw as as true. If there is no existing record for a given mobileNumber, I want to throw an exception which is ResourceNotFoundException for a given customer and mobileNumber. At last we can make the similar changes in the last if condition inside this else if condition we can handle the validations for the deleteCustomerCommand. That's what I'm trying to do here. First let me do the typecasting to deleteCustomerCommand. During that deleteCustomerCommand, I want to execute this code. First, I want to check if there is already a given customer record inside the read database based upon the given customerId and the activeSw as true. If yes, only I want to proceed otherwise I want to throw an exception which is ResourceNotFoundException. But as of now, we don't have any method inside the customer repository with this name. So what we have to do is, we can copy this method and go to the CustomerRepository. Inside the CustomerRepository I'm going to duplicate the same method again, but this time I'll try to change the method name to findByCustomerId() and activeSw and here the parameter name is going to be customer id and boolean active. So this looks good to me. I can go to the Interceptor inside the Interceptor now we don't have any compilation issues. I hope you are clear what we have done so far. Let's try to continue this discussion around the Interceptor in the next lecture as well. Thank you, Bye.
