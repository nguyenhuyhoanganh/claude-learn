# Event Processors in Axon framework - Part 3

By default inside the Axon framework, the streaming event processor is going to be considered. That's why when we are having an runtime exception on the query side, the transaction that happened on the command side is not getting rolled back because the command is ran with a different thread compared to this event handler method. So we want to override these default behavior. We want to leverage the subscribing event process so that we can roll back on the command side as well. So this should be easily achievable because right now we have implemented both command and query sides as part of a single JVM. If you have implemented across multiple Jvms, then it's not going to be a super easy job. You need to follow the Saga design pattern. For now, let's not worry about the distributed transactions. So as a very first step, I need to assign my event handlers to a processing group. So what I'm going to do is, all the events related to the customer I can assign to a processing group to create and assign the events to a processing group we just have to use an annotation which is @ProcessingGroup. To this processing group we can give a name. For example, here I'm trying to give a name which is customer-group. With this, what is going to happen is, all the event handlers that we have inside this class they are going to be tagged to a same group in case if your command is publishing multiple events, since both the events are assigned to a same group, when we switch to the subscribing event processor, both the events, they are going to be processed using a single thread. Once the processing group is created as a next step, we need to make a small configuration on the Spring Boot main class. Let me go to the Spring Boot main class which is CustomersApplication. Here I'm going to copy this method and paste it here I'll try to rename this method to configure() and to this configure() method, I'm going to pass the input parameter of type event processing configure the parameter name I'm going to keep it as config. Now using this config I'm going to invoke the method which is registerListenerInvocationErrorHandler() To this method, first I need to mention my processing group name, which is customer-group followed by I need to pass an lambda expression on how I want to handle the errors. If there is an exception happened during the event processing, let me create a lambda expression which is going to take some input parameter with the name conf from this lambda expression. I'm going to invoke PropagationErrorHandler.instance() method. With this configuration what we are trying to tell is, under my CustomerProcessingGroup, if there is an runtime exception happens while events are being processed. The same runtime exception I want to propagate as it is to the command layer without making any changes. So if you go to this PropagationErrorHandler here, if you try to read the documentation on top of the class, it is an singleton error handler implementation that does not do anything. So it is simply going to propagate whatever runtime exception happened to the upper layers or to the top layers. With this configuration right now, the errors will be propagated to the top layer. But as a last step we need to configure subscribing event process so that rollback also is going to happen automatically because everything is going to be processed using a single thread. So to configure the subscribing event processor we can go to the application.yml of customer microservice. So here just under the Axon we need to type event handling. Under the event handling we need to type processes and under the processors we need to type the processor name which is customer-group. And to these customer group I want to assign the subscribing event processor. That's why under the mode I need to mention subscribing as a event processor. These are the only changes that we need to make. The very first change that we have done is inside the Projection by using these annotation, followed by inside the customers application, we have configure to propagate the exceptions to the top layer, and towards the end we configure subscribing event processing to these customer group. So let me save these changes to the build. Once the build is completed I can try to invoke the update operation. So as of now inside the CustomerProjection you'll be able to see there is a throw new runtime exception is present from the postman I'll try to invoke this update operation. Let me click on the send button and this time you'll be able to see we are getting an exception, which is it is a bad day and with the status as 500 internal server. This response we have coded inside the GlobalExceptionHandler. Inside the global exception handler, we have a method that handles all the exceptions of type exception. So whatever logic we have written here the same is being executed that's why we are receiving the internal server response this time inside the Axon server console. If you try to refresh as of now, you can see the aggregate sequence number is four for this aggregate identifier. If I try to refresh, it is still four that means the transaction on the command side also is never committed. It is only going to be committed once all the events are processed without any exceptions. Here we can do one more enhancement. As of now, whenever an runtime exception is happening on the event side or on the command side, we are going to execute this method because this is a global exception handling that handles the Exception.class. Any exception inside the Java ecosystem is a child of exception that's why this method handles all kind of exceptions. But if you are looking to write a separate exception logic for the runtime exceptions, that happens due to the Axon framework code. You can do that very easily. First, what I'm going to do is, I'll try to keep a breakpoint here and try to invoke the update() operation. If you click on the send button, you will see as of now the breakpoint is being stopped inside this handle global exception where we are trying to handle the exception of type Exception. So let me release this breakpoint. But like I said, we can write a separate logic for all the exceptions that are arising from the Axon framework code. So let me copy the same method. So here instead of Exception.class I'm going to handle the CommandExecutionException. What Axon framework it is going to do is, whenever you're trying to execute a command as part of the command, you are going to dispatch the command. Once the command is processed, you're going to dispatch the events. Once the events are processed, you're going to store the data. Since command execution is a very first step of the CQRS flow any runtime exception that is going to happen inside this flow, it is going to be wrapped as a command execution exception by the framework. This time, instead of this method, this lower method is going to be executed. I can also prove you that. So here instead of just sending the exception dot get message, what we can do is I'll try to append a prefix which is command execution exception occurred due to colon space. And I'm going to append Exception.getMessage(). So since there is a typo let me correct it. Now we are good. Let me save the changes. Do the build. This time I'm going to keep the breakpoint in both of these method . Let's see at which method the execution is going to stop. I'm clicking on this send button, and this time the breakpoint stopped inside the method where we are trying to handle the CommandExecutionException. So the framework is doing a good job here in wrapping the runtime exceptions that are happening inside the Axon framework into a separate exception. Let me release this breakpoint. Inside the response you'll be able to see command execution occurred due to it is a bad day. I hope now you got some clarity around the event process available inside the Axon framework, and how we solve the problem that we have with the help of subscribing event processor. More details around the distributed transactions we are going to discuss in the coming sections. Before I try to close this lecture, let me go to the CustomerProjection and let me comment this through runtime exception and uncomment these actual business logic. Let me save these changes to the build. We are good here. Thank you, and I'll catch you in the next lecture, bye.
