# Building command APIs in customer microservice

Now we need to build Rest APIs that support all command operations like create, update and delete. Under the command package, I'm going to create sub package with the name controller. Inside this controller package, I'm going to create a new Java class with the name CustomerCommandController. Inside this new class we need to build the Rest APIs. But if you recall, we already built all the CRUD operations related APIs inside a class with the name CustomerController. Here under the controller package we have this class let me open this. The same logic I need to repeat. Once we separate these APIs into command and query, we are going to delete this class. Let me copy these annotations and mention on top of this class. along with these annotations, I'm going to mention one more annotation which is @RequiredArgsConstructor. Next I'm looking to build an API to create an customer so let me copy this entire method and paste it here as of now, inside this method, if you closely observe, we are going to accept the data with the help of this CustomerDto where we have fields like name, email, mobileNumber and other two fields are optional. The end user don't have to send. We are going to generate them inside our business logic. Inside this method first we are trying to create a customerId with the help of this UUID class. And later on we are trying to invoke the method createCustomer() inside the CustomerService implementation class. But with this logic, what is going to happen? The customer data it is going to be stored inside the database normally, but we want our data to be stored with the help of event sourcing pattern. That's why here I'm not going to rely on this code. Let me delete the same this line of code also I'm not going to need. That's why I'm trying to delete this as well. Once we receive the data from the end user, we need to create the object of command, and we need to pass the same command to the axon server. So to create the object of Command we already have a associated command with the name CreateCustomerCommand . So let me mention the variable name as createCustomerCommand followed by using the same class I'm going to invoke the builder method. Right now my class is supporting this builder method because I have mentioned this @Builder annotation. So once we have created this builder method, what we can do is, we can try to keep on populating all the required data by invoking the field names. For example, first I'm going to invoke the customerId field. Since the customer is not going to provide the customer ID, I'm going to generate this with the help of UUID.randomUUID.toString(). After the customerId, I'm going to populate the email value. This email value I'm going to get from the CustomerDto input object. Next, I'm going to populate name field by using CustomerDto. After the name, I am also going to populate the mobileNumber field as well. Let me press tab and accept this recommendation. and at last I'm going to populate the activeSw by using a constant which is activeSw under the customer constants file. If I press tab, this recommendation will be accepted, and towards the end we need to invoke the build() method. This build() method is actually going to create the object of this class by using all these data that we are trying to provide. Off course, if you don't want to follow these, you can create the object with the help of constructor as well. Now the command is created successfully. But we need to convert this command to the axon server otherwise it is of no use. How to convey that it's very easy. Axon server has a class which is command gateway. The same I'm trying to inject as a dependency to this class. Behind the scenes, during the startup, the Axon framework is going to create the bean of this CommandGateway. So if you see this is an interface, but behind the scenes it's implementation class bean is going to be created as a bean being by the framework and the same we are trying to use as a dependency for this class. So using these CommandGateway here, I'm going to invoke a method which is send() and wait(). If you want to trigger the command and wait for the command execution to complete, you need to use this send and wait() method. This is going to leverage the blocking style of invocation. The thread is going to be blocked and it is going to wait for the completion of the command execution. So there are multiple overloaded methods of sendAndWait(). If you simply use the sendAndWait() it is going to wait indefinitely until the command is completed, whereas if you want, you can also provide how much time you want to wait by using this third method, whereas the second method, it is going to allow you to provide some metadata information about your command. And the fourth method, it is going to allow you to provide the metadata information timeout information along with the Command object. For now, I want to wait indefinitely. That's why I'm trying to use the first method. To this first method, we need to pass the object that we have created, which is createCustomerCommand. Whereas if you are looking to trigger the command asynchronously in a non-blocking style, you have send methods. These send methods along with the command they are also going to accept the callback function, which is going to be executed once the command execution is completed. But since I don't have any other code to be executed once this command is triggered, I'm trying to use the sendAndWait() method. So once the command is processed behind the scenes, the data will be stored inside the right database and towards the end, I want to return the 201 status to the end user, indicating that the account is created successfully. So now we have created the create API very similarly, we want to create the update API as well. Let me go to the customer control here we have update API. We can copy this method signature nature and paste it here. Now, inside this method, as the very first step, we need to create the object of update the customer command. So let me copy this entire code and paste it here. Instead of CreateCustomerCommand object I want to create object of UpdateCustomerCommand. So let me update the variable name and on the right hand side also I need to correct the class name and this time the customerId we don't want to create from scratch. Instead we can fetch from the CustomerDto class and email, name, mobileNumber and activeSw everything. We can keep it same and at last using the CommandGateway we can trigger the command which is updateCustomerCommand by passing this object and towards the end I can return the same response here also, but instead of created, I can send the response okay and the status as 200 along with the message of 200. With this, the update operation also is created. Now let's go and get the delete operation related code. So let me copy this entire method signature and paste it here. Here I'm trying to use the patch mapping instead of delete mapping. The reason is, we are not going to entirely delete the record. Instead we are going to soft delete the record inside this body what I have to do I have to create the object of deleteCustomerCommand. After the builder method, I need to populate customerId based upon whatever data I have received to the method. And at last the activeSw. We are going to populate with a constant value which is IN_ACTIVE_SWITCH, because we want to mark this column as false so that the entire account is going to be soft delete. Next, using the CommandGateway, we want to trigger this command. That's why I'm trying to use a sendAndWait(). and do the same I'm trying to pass the object of deleteCustomerCommand, and towards the end we can pass the same status conveying that the soft delete is successfully completed. With this, we have developed three kinds of write APIs that are going to be handled by the command component create, update, and delete. If you see what is happening right now is, we are sending these commands to the Axon server but Axon server does not have any clue on how these commands should be handled. That's why as a next step, we need to build an aggregator component on the command side, I'm going to explain what is an aggregator in the next lecture, but I'm assuming you are clear what we have done so far. Since we have this APIs inside this Command controller class, we can delete the duplicate APIs that we have inside the CustomerController. Let me delete this create/api, followed by I'm going to delete the update and delete APIs logic as well. Thank you and I'll catch you in the next lecture, bye
