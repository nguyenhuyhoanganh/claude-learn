# Building query API in customer microservice

To build the read API on the query side, I'm going to create a new sub package under the query with the name Controller. Inside this Controller sub package, let me create a new Java class with the name CustomerQueryController. We can go to the CustomerController class and try to copy all these content that we have here. The same we can paste it inside the CustomerQueryController. After these annotations, we can also mention a @Lombok annotation which is RequiredArgsConstructor. As a next step, we need to build a rest API that is going to support the query or fetch() operation. We already have such operation inside the CustomerController. If you see here we have this method. Let me copy this entire method and paste it here inside the CustomerQueryController. To this controller, I need to inject the customer service as a dependency so that we can use the methods available inside the CustomerServiceImplementation class. As of now, if you see we are directly trying to invoke the fetchCustomer() method available inside the CustomerService. But we should not do this when we are trying to implement the CQRS. We should go with the flow recommended by the framework. As you can see here from the read API we need to create a Query object, the Query object we need to publish to the framework so that it will go to the QueryHandler, where we are going to write all the logic on how to read the data from the read database. We need to follow this approach, as it is going to give more flexibility to the developers to build many queries and query handlers based upon their business requirements. So here, as soon as we receive the input mobileNumber, what we can do is, we can try to create the object of findCustomerQuery(). If you can recall long back we have created a class with the name FindCustomerQuery to the constructor of this class we are going to pass the mobileNumber as the input parameter. Next, we need to publish this query to the framework. So how to do that? It's very easy. As of now you can see I have deleted all the code where we are trying to directly invoke the ICustomerService. . I don't need this dependency anymore. Instead of this, I'm going to inject QueryGateway as a dependency to this class. Just like how we have CommandGateway and the command side. Very similarly we have a QueryGateway. Using this QueryGateway we are going to publish the given Query object. So here we have query() method. Let me invoke this to this query() method. We need to pass the object of findCustomerQuery(). After passing the input parameter, we should also tell to the framework what we are expecting as an output from this query() method. That's why we need to type response types and using these response types class, we need to invoke the instance of method to this instance of method, we need to pass CustomerDto.class. Since we are expecting the output in the form of CustomerDto, which will have customerId, name, email, mobileNumber and activeSw the same we need to provide here and towards the end we need to invoke the join() method. This join() method is from the Completable feature. If you can read the documentation of this method, it says return the result value when complete or throw an exception if completed exceptionally. So whenever we are trying to invoke this query() method behind the scenes, the query() operation is going to be invoked asynchronously in a non-blocking style, but with the help of the join() method, we are trying to wait for the output so that we can eventually send the same output to the end user. You can open this query() method, which will take you to the QueryGateway interface. Inside this interface, you will be able to find many query related abstract methods. Using these methods, you can try to query your read database using various styles. As of now, I'm trying to use a query, but we're going to discuss about the other type of queries in the coming sections. If you read the documentation of any of these methods, you will be able to see some useful information. For example, here on top of this method, we have this documentation which says sends a given query over the query bus. Expecting a response with a given response type from a single source. Execution may be asynchronous depending on the query bus implementation. I hope this is making sense to you. Let me close this interface. Now on the left hand side, I'm going to catch the output with the variable name as CustomerDto itself the same CustomerDto I can return towards the end. So here I'm trying to use ResponseEntity.status to this status() method. I'm going to pass HttpStatus.ok, followed by I'm going to invoke the body() method to this body() method I'm going to pass CustomerDto object. With this logic, we are publishing the query find customer query to the framework by using this query gateway. But we don't have any logic on how these query needs to be handled. To implement the business logic related to this query, we need to create a new package under the query with the name handler. Inside this package, I'm going to create a new Java class with the name CustomerQueryHandler. Let me add @Component annotation followed by required args constructor. To this class I need to inject private final ICustomerService as a dependency. Next, let me write a method which is going to return CustomerDto as an output and the method name I'm going to mention as findCustomer(). To this findCustomer() method, we need to pass findCustomerQuery as an input parameter. Inside this method, using the ICustomerService, I'm going to invoke the fetchCustomer() To this fetchCustomer(), I'm going to pass the mobileNumber that I have inside this FindCustomerQuery object. The output from this method we can straightaway return. But here there is a catch. On top of this method we need to mention an annotation which is QueryHandler. Now this method is going to handle this query of type FindCustomerQuery where we are trying to load the customer details based upon the given mobileNumber from the read database. I think we have implemented all the required components. We should be good to get started with that testing of the entire flow inside the Customer microservice. I hope you are excited to see the demo. Thank you and I'll catch you in the next lecture, bye.
