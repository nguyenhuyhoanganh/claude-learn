# Reading data from EventStore

Inside these Interceptor class we are trying to perform the validations. While we are trying to perform the validations. We are actually loading the data from the read database. As you can see here, with the help of CustomerRepository, we are trying to load the data which we stored on the read site. This may work in this scenario, but in real projects you may get some unique scenarios or peculiar scenarios where you may want to read the data from the event  store to perform some validations. That's why inside this lecture, let me teach you on how to read the data from the event store to demo the same what I'm going to do is, I'm going to perform the validations by loading the data from the event store. I will not disturb these validations code that I have written inside the Interceptor. Instead, I'll go to the CustomerAggregate and here inside the method where we are trying to handle the updateCustomerCommand(). I'm going to write some logic. In order to read the data from the event store first I need to inject a bean into this class. The bean is EventStore. The Axon Framework provides this EventStore to read the data from the right database or from the EventStore database. Using these EventStore, we can invoke a method readEvents(). There are two overloaded read events. The very first read events method. It is only going to accept the what is the aggregate identifier, and this is going to give all the events associated to a aggregate identifier. But if you are looking to read that database from a specific sequence number, then you can try to use the second overloaded method. For our example, I'm going to invoke the very first method, and to this method I'm going to pass the aggregate identifier. So inside the updateCustomerCommand() we will be having customerId which is going to act as a aggregate identifier. After the read events we need to invoke the method as stream() because we want to read all the events as a stream, followed by I want to convert my entire stream of elements into a List on the left hand side. I can try to catch the output by typing list and with the variable name as commands. Now I can simply check if commands isEmpty() If commands.isEmpty(), that means there are no records associated to a given customerId. In such scenarios, there is no meaning of performing the update() operation. That's why I want to throw the exception. So inside the CustomerCommandInterceptor, we have a elseif logic where we are trying to throw the same error. So what we can do is, we can try to copy this entire piece of code and paste it inside our if block. So here I'll just type throw new ResourceNotoFundExceptionCustomer. And here instead of mobileNumber we should say customerId. And here also I'll just invoke getCustomerId(). Towards the end I'm going to put semicolon as a next step. We can keep a breakpoint here and I'll try to do a build. Once a build is completed I'll try to invoke the update() operation. So from my postman, I'm trying to pass the update() operation. So here I'll try to pass the updated name which is Madan Mohan Reddy. So let me click on the send button. As you can see the breakpoint stopped here. Now if I try to debug this line of code, you'll be able to see my list of commands has three elements. So these are the events that are associated to a given customerId. So this way you can always read the data from the EventStore. Here I'm just performing the empty check, but if needed you can try to read the data inside these events and accordingly, you can take a decision whether to proceed with your business logic or not. So let me release this breakpoint. As soon as I release the breakpoint, I got a successful message. I hope you're clear, but since we are trying to perform the same validation inside the Interceptor, I'm not going to keep this code for your reference. I'll just comment this code and keep it inside this class. So in future when you are looking for a reference, you can come and look at these commented code. Thank you and I'll catch you in the next lecture, bye.
