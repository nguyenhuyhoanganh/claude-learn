# Building projection class in customer microservice

To create the Projection class and the query side first, under the query package, I'm going to create a new sub package with the name projection. Inside this Projection class, I'm going to create a new Java class with the name CustomerProjection first on top of this class, we need to mention an annotation, which is component because we want a bean of this class to be created by the spring framework, followed by I'm going to use the Lombok annotation which is required args constructor. Next I'm going to create a method which is public void on customerCreatedEvent() I want this method to be executed whenever the customerCreated event is consumed by the Projection class. On top of this method, we need to mention an annotation which is @EventHandler. Whereas inside the Aggregate class we have mentioned the annotation EventSourcingHandler, which is going to take care of storing the data in an event sourcing style. But here inside the projection, we need to use this @EventHandler, because we want this method to take the responsibility of handling the event that is going to be consumed from the event bus. Inside this method, we need to write the logic to store that data related to the customer into the customer table. We already have an JPA entity class with the name Customer. So first I'm going to create the object of this Customer class. Maybe I'll try to keep the variable name as customerEntity. Next, using the BeanUtil.copyProperties(), I'm going to copy all the data present inside the customer created event into CustomerEntity object. Since the name of the fields are very similar, for example customerId, name, email, mobileNumber and activeSw. Since all the fields names are matching, all the data from the event is going to be copied into these CustomerEntity object. At last we want to save these data into the read database. The read database I want to use normal database itself. Since we are already using the h2 database, I'm going to construct the same history database as the read database. If required, you can use MySQL, Oracle or Postgres based upon your requirements. If you open the application.yml  file of customer, you'll be able to see that I have defined the h2 database related configurations with these, what is going to happen my h2 database is going to act as a read database. In order to save this data into the database. First I need to inject the I customer service interface as a dependency into this production. So let me type private final ICustomerService with the variable name as ICustomerService. Using these I customer service, I'm going to invoke the method which is createCustomer()and to this createCustomer() I'm going to pass the object of CustomerEntity. But I'm going to get a compilation error because inside the ICustomerService, as of now here we're trying to accept the CustomerDto. Instead, what we can do is, we can try to accept the Customer object itself. Let me rename this variable name also to CustomerEntity. I can update the method comments here also I can update. Next I can go to CustomerServiceImpl inside the CustomerServiceImpl also here I need to accept the Customer object. Inside this method I can delete all this code related to the CustomerDto and this customer mapper related code also, I can delete now using the mobileNumber present inside the Customer object I can check if there is already a customer available. If an customer already is, available, I'm going to throw this CustomerAlreadyExistException. Otherwise I'm going to simply save the data into the database. Here you might be wondering why I'm trying to perform the validation one more time, because we already done this very similar validation on the aggregate side. If you go to the constructor of this class, you'll be able to see as soon as someone published this creates customer command. I'm trying to perform these validations since I'm doing this validation already here whatever we are doing here, this may look redundant, but I would always request you to perform the validations on the read side as well. Sometimes if your event is being consumed multiple times for whatever reasons, in such scenarios, you don't want to insert multiple customer details. To avoid such scenarios, you need to perform this validation during the customer creation on the read database as well. With this, now we have completed the logic related to the customer created event. Very similarly, we need to write the logic for other kinds of events. Let me copy this method and paste it here. This time the event that I want to catch is customerUpdatedEvent. Let me change the variable name as well. It is not always mandatory to follow this style. You can always send your event details directly to your service layer as well. Maybe this time what we can do is, I'll try to get rid of these objects, and I'll try to pass these event directly to the service layer. So here I'm going to invoke the method updateCustomer(). Let me go to this updateCustomer(). And here I need to update the parameter name to customerUpdatedEvent(). I'm also going to update the variable name. Let me update the same inside the comments as well as the next step I need to go to the CustomerServiceImpl and inside the updateCustomer() method. I need to update the method input parameter to customerUpdatedEvent(). Once we update this method input parameter details, we need to first check the database if there is already an customer available for a given mobileNumber. So I'm trying to pass the mobileNumber and activeSw as true. If there is a customer already, then I'm going to catch the customer details into this variable. Otherwise, I'm going to simply throw an error saying that ResourceNotFoundException. The resource is customer and the criteria that we have used to search the customer is mobileNumber. Let me update the variable name here. After fetching the details from the database, you need to populate the latest data into this customer object so that we can eventually send the customer object to this save() method. So what I can do is, I'll go to the CustomerMapper class as of now here we have these methods. Very similarly I'm going to write a new method. The method name I'll try to keep it as map event to customer. The very first parameter I'll change to customerUpdatedEvent() and the variable name to event. Since we are giving flexibility to the end user to update only the name and email as part of the update operation, we need to populate only these two fields. So let me go to the CustomerMapper. And here I'm going to type Customer.setName(). And this name I'm going to fetch from the event.getName(). Very similarly I'll try to populate the field which is setEmailBy() reading from the event() method which is getEmail(). At last we need to return the updatedCustomer object. Now I'll go to the CustomerImpl class here instead of map to customer, I'm going to invoke the map event to customer, and the very first method parameter I'm going to pass is customerUpdatedEvent. With this now the updated data will be stored on the read database. As a next step, we need to implement the method to handle the customer deleted event. Let me paste the same method here, but this time I'll try to mention the method input parameter type as customerDeletedEvent. Once we updated this variable name, we can try to invoke the deleteCustomer() method and to this deleteCustomer() method, we can try to pass the customerId() as an input. We already have enough logic around the deleteCustomer(). If you go to the CustomerServiceImpl under the deleteCustomer() first, we are trying to fetch the customer based upon the given customerId. If there is no such customer, we are throwing an error. If there is a customer, we are trying to set the activeSw as false. With all these changes, I'm assuming we should be good from the Projection perspective. So this projection is going to take care of reading the data from the event and saving it to the reader database. As a next step, we need to build the read API's. From the read API's, we need to create a query object, the same query object we need to publish to the framework. And the framework is going to look for the query handler. Inside this query handler only, we need to read the data from the read database and the response we need to send back to the client application. So let's try to build these three components required on the read side in the next lecture. Thank you, bye.
