# Implementing CQRS and ES in accounts microservice - Part 1

As of now, we have implemented the CQRS and event sourcing pattern inside our customer microservice as per our deal that we have discussed previously. What I'm going to do is, I'll try to show you whatever we have done so far by taking one more microservice, which is accounts microservice. So inside this microservice, I'm going to make CQRS and even sourcing changes very quickly. Once you are clear with the steps, I want you to set up the same CQRS and event sourcing pattern under the cards and loans microservice. I'm not going to discuss anything inside the cards and loans microservice around the CQRS and event sourcing pattern. Don't worry, it's going to be very easy to make your life easy I have done something behind the scenes let me show you that. If you go to the GitHub repo of this course, which is event-driven-microservice, right now we are inside the section3 that's why let's go to the section3 as of now, we have implemented the skills and event sourcing pattern inside the Customer. So let me open this customer. If you open this customer, there is a readme document which has all the steps that we have followed. The very first step that we need to do is, to add this dependency inside the microservice, followed by we need to add these properties inside the application.yml. After adding these properties, you need to create these sub packages with the name command and query. You can create all these sub packages under these sub packages you need to create so and so classes to know what is the code that we have written inside these classes you need to obviously open the source code here. I don't want to keep the source code here because that will make the readme document very long. So under the command first we need to create the command classes and the event classes followed by Aggregate class. After the Aggregate class, we need to create the CommandController class, where we are going to develop all the command APIs. Later on to implement the validations, we need to implement the Interceptor followed by Query class after this Query class and the query side, we should also write an Projection class followed by QueryHandler and QueryController. So these are the important classes around the CQRS and event sourcing. Later on under the step four, we have added an abstract method under the CustomerRepositoryInterface, followed by. Under the CustomerMapper we need to add these map event to customer method and under the ICustomerService we need to update these abstract methods to have the correct input parameters. So based upon these abstract methods, you should also update the CustomerServiceImpl based upon the logic that we have discussed. Next, as part of the step six, you need to delete the CustomerController class that we have from the previous section, because right now we segregated everything into command and query APIs. And under the step seven you need to add these method under the GlobalExceptionHandler. This method is going to handle the CommandExecutionException. At last, under the Spring Boot main class, we need to add these two methods, one for the Interceptor and the other one regarding the PropagationErrorHandler. So very similar readme document is present inside all the microservices. For example, if you open the accounts microservice, there is an readme document explaining on how to implement the CQRS and event sourcing pattern. You can get the class names package names from this readme documentation. You can also get the actual source code by looking into the source folder. What I'll do is, I'll try to follow these steps and try to implement these steps inside the account microservice, so that you can try to follow the same kind of steps for loans and cards microservice I want you to implement seeks for loans and cards so that you will be comfortable on what we have learned so far inside this lecture. If you are simply watching the lectures without practicing, That's fine as long as you understand the concept I'm happy with that. So let's go to the accounts microservice inside the pom.xml of all the microservices, we already added this dependency. So I'll try to go to the second step and copy these properties. Let me open the application.yml of accounts microservice and here I'm going to update the properties. If you look here we have a processing group with the name account-group the same name we are going to update inside the Projection class as well. So let me close this application.yml and go to the package structure of accounts microservice. So here first I need to create a new Subpackage with the name command, followed by another Subpackage with the name query. That's what I have highlighted here. First you need to create the command and query. Later on, you need to create all the sub packages under the command and query. So let's try to create all of them. First, under the command package, I want to create a new sub package with the name aggregate, followed by another package with the name event next controller. After the controller I'm going to create interceptor. Now I'll go to the query and here under the query package I'm going to create a sub package with the name controller followed by handler sub package. After the handler sub package, I'm going to create a new sub package with the name projection. After creating all the sub packages, we need to go to the step four. So first I need to create these command classes. So let me create these classes inside the command package. The class name is going to be CreateAccountCommand. Followed by the next class is update account command and the last command that I want to create is, deleteAccount() method. We need to write some source code inside these commands. It is going to take lot of time to type all these code. What I'm going to do is, I'll try to paste the code directly. Since you are already aware about all these concepts, I'm assuming it is fine. If you have any questions, you can let me inside the Q&A, or you can directly refer the code present inside the GitHub repo under this class let me paste the code. As you can see under the account we are going to have fields like accountNumber which I'm going to generate inside my business logic mobileNumber. The end user is going to provide account type I'm going to assign inside the business logic. The same applies for the branch address and the activeSw. So here account number is going to act as a target aggregate identifier because the account number is never going to change. Next let's go to the update account command here. Also I'm going to paste the code. We have the same fields here as a next step let's go to the DeleteAccountCommand inside this class also, I'm pasting the code with the two fields. One is accountNumber followed by activeSW. Next we need to create the classes inside the event package. The very first class is, AccountCreatedEvent the same I'm trying to create, followed by the next class which is AccountUpdatedEvent and the last event is AccountDeletedEvent. Let me paste the code inside all these event classes. Since you are already clear about all these code, I am assuming there are no issues and these are all simple Java code. Since your advanced microservice developer, you should be able to easily understand what is happening here. Next, under the aggregate package, I need to create this Aggregate class. So let me create a new Aggregate class which is AccountsAggregate. And here also I'm going to paste the code we should be able to understand most of this logic. First, we have all the fields that we are going to store using these aggregate. Here this account number is going to act as an AggregateIdentifier, followed by an empty constructor, which is required by the Axon framework. And for the create account command, we are going to leverage this constructor followed by this event sourcing handler which is going to handle these AccountCreatedEvent. Similarly, we have for updateAccount and deleteAccount. Now let's go to the controller package and try to create a new Java class with the name AccountsCommandController. Here also I'm going to paste the code. And inside these controller we are going to have three different operations supporting create, update and delete. So inside these respective APIs we're trying to create the objects of commands. And using these command gateway we're trying to dispatch these commands to the Axon framework and the Axon server. After the controller we need to create a new Interceptor class. So let me create the new class under the interceptor sub package with the name AccountsCommandInterceptor. Here I'm going to paste the code and as usual, it's going to be very similar to what we have discussed as part of the customer microservice. So here we are getting a compilation error because inside the accounts repository we have not defined this method. Let me copy this method and go to the accounts repository and duplicate this line. I'll change the method name to findByAccountNumber and parameter name two accountNumber. And since the accountNumber is of type Long instead of string, we need to type long here so that the compilation error here will get resolved. So with this we have done all the changes required on the command side. As a next step, we need to make the changes on the query side as well. So let's continue this discussion in the next lecture as well. Thank you, bye.
