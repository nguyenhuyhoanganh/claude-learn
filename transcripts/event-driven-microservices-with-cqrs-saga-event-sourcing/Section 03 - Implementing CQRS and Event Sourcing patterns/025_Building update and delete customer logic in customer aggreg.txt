# Building update and delete customer logic in customer aggregate class

Let's write logic inside our CustomerAggregate class to handle the updateCustomerCommand and deleteCustomerCommands while we are trying to handle the createCustomerCommand. We have created the constructor of this class, but we should leverage the constructor only during the create operation. Whereas for the update and delete operation we should define normal Java methods. That's why what I'm going to do is, after these eventSourcingHandler() method, I'm going to create a method which is public void handle(). And to this handle() method I'm going to accept a command which is updateCustomerCommand as an input. On top of this method we need to mention an annotation which is @CommandHandler. Whenever we want to make a constructor or a method to handle the command, we need to make sure we are mentioning these @CommandHandler annotation as usual if needed, we can perform some validations based upon the data that we have received inside the updateCustomerCommand otherwise, we can go ahead and create the event, copy the data and invoke this apply() method. For now, I'm going to skip the validations part inside the update customer command method, because in the coming lectures, I'm going to show you a better way on how to define the validations using an Interceptor class. Instead of defining all the validations related logic inside the aggregate, we can keep all these validation logic in an Interceptor class. That's why for now, I'm going to skip these validations related code. Let me copy these three lines of code and paste it here. This time the event that we need to create is CustomerUpdatedEvent. Into this CustomerUpdatedEvent, we need to copy the data by using these updateCustomerCommand as the source object. At last we need to publish the event which is customerUpdatedEvent. Once this event is published, we need to provide the logic on how this event needs to be stored inside the write database. For the same, we need to copy this entire method and I'm pasting here. This time the input to this method is going to be customerUpdatedEvent. On top of this method, we need to make sure this annotation is present. So during the update operation, I don't want to give flexibility to update the customerId and mobileNumber and activeSw in the coming sections. I'm going to have a separate operation to update the mobileNumber across all microservices like customer, account, loans and cards. That's why I'm going to delete the code related to the mobileNumber and activeSw and customerId. Using update operation the end user can only update the name and email details. That's why I have mentioned only these two field names here with these update operation, what is going to happen inside the right database a new entry will be created into this new entry the updated name and email details will be populated. Whereas coming to the other fields like customerId, mobileNumber and activeSw, all these fields will be copied over from the previous event that we have stored inside the database. I hope it is clear. Now very similarly, we need to create the logic to handle the DeleteCustomerCommand. That's why here let me change the method input parameter to deleteCustomerCommand using the same deleteCustomerCommand I need to publish the event which is customerDeletedEvent. Using this event, the end user can delete the customer details. Since we are only going to soft delete the data, we are going to populate this flag which is activeSw with a false boolean value. Let me close this class and here we need to pass the source object as deleteCustomerCommand followed by customerDeletedEvent. At last I need to publish this customerDeletedEvent. If you can go and check the CustomerCommandController class. Under the delete operation, the customerId we are going to receive from the end user. Whereas the activeSw we are populating with a false value here itself the same we are trying to send to the Aggregate class. That's why here we are simply copying the data from the command to the Event object. Now inside the eventSourcingHandler() method, we need to accept the customerDeletedEvent as an input parameter. This time we are only going to update the activeSw flag. That's why what I'm going to do is,I'm going to invoke this.activeSw. And from the event object, I'm going to get the active switch value and the next line we can delete. With this, we have written the code required to handle the deleteCustomerCommand and the updateCustomerCommand. I hope you are seeing a pattern. So whenever we are trying to handle a command first we can perform any validations followed by we need to copy the data from the command to the Event class and a class we need to invoke this apply() method to publish this event to the framework. Once this event is published, the logic inside the event sourcing handler is going to be executed to store the data into the write database. Once these data is written into the database, the event will be subsequently published into the event bus. As of now, we have completed everything on the command side. As a next step, we should try to build a projection on the query side, which is going to consume the event from the event bus and store the given data onto the read database. Let's try to build the projection in the next lecture. Thank you, bye.
