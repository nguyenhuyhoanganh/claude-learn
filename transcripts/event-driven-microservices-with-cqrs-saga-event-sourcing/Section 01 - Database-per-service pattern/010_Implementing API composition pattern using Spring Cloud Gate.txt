# Implementing API composition pattern using Spring Cloud Gateway - Part 3

So whenever someone makes an API calls, we want this logic to be invoked. As I said before, inside reactive to applications, we can't build the Rest APIs by using RestController. I can't put just a @RestController on top of this class and make this entire logic to be executed instead. Inside the reactive applications, we need to define functions which are capable of handling a specific route. That's why what I'm going to do is, I'm going to create a new package with the name router. Inside this package, I'm going to create a new Java class with the name CustomerCompositeRouter on top of this class. Let me mention configuration annotation to this @Configuration annotation I'm going to mention proxy bean methods as false. This proxy bean methods false is an optimization flag that I'm trying to mention, which will improve the startup performance of my gateway server inside this class, I'm going to create a method which is going to return a bean. That's why I need to first mention @Bean annotation. Below this annotation, let me create a method which is public routerFunction(). From this routerFunction() we are expecting ServerResponse as an output. That's why I need to mention ServerResponse inside the tags followed by I'm going to mention the method name as route() and to this method I'm going to pass the bean of customer CustomeCompositeHandler. So whatever actual business logic that we have defined inside this handler, the same we are trying to provide as a input bean to this method. Now from this method I want to return routerFunctions(). Using these routerFunctions() I'm going to invoke a method route() which is going to accept a predicate and a handler function. So let me invoke the very first method. So here with the help of request predicates I'm going to invoke get(). And to this get I'm going to pass the API path which is API api/composite/fetch/customerSummary. . So with this, what I'm telling is whenever someone is trying to make a Get request to my gateway server with this path, I want my CustomerCompositeHandler to be invoked. So inside my CustomerCompositeHandler I have a method which is fetchCustomerSummary() the same I want to invoke. So let me bring these two next line. So it's very simple I'm trying to define a route of type get with this value. Whenever someone invokes this I want this method to be invoked. But apart from mentioning only the API path, I also want to define more conditions. That's why I'm trying to invoke and() method to this and() method I'm trying to pass a predicate condition with the help of these accept() method. I want my logic to be invoked only if someone sends the input data using JSON format. Similarly, I want to define one more condition. That's why I'm trying to invoke and() method one more time. This time with the help of the same request predicates, I'm trying to invoke the queryParam this queryParam I'm trying to pass some mobileNumber. This queryParam() method it accepts two parameters. The very first one is, string, the other one is predicate condition under which you want to accept this queryParam(). As of now, I don't have any specific predicate conditions. That's why what I'm going to do is, I'll try to write a simple predicate lambda expression, which is always going to return true value. With this, we have defined multiple predicate conditions that a given request has to pass. Whenever someone wants to invoke the logic present inside these handler, they need to make sure they are sending the Get request to these path, which is going to accept the JSON as an input data followed by. It should also have a queryParam() with the param name as mobileNumber. If someone don't follow these conditions, then this logic will never be invoked. with this, I'm assuming we are done all the coding changes to implement API composition pattern inside the gateway. So let me do a build. Once the build is completed we can try to start all the microservices. Let me go to the postman inside the postman I need to create the new data. Whatever data that we have created previously, it is soft deleted and we can't use that data. That's why I'll try to create the customer with the same mobileNumber, since my gateway server still bootstrapping and it is loading the data from the Eureka server. Let me try to send the request one more time. This time you can see the request is successful. Let me go to the accounts here also, I'll try to use the same mobileNumber and create a account. I'll go to the createLoan() and here also using the same mobileNumber. I'm trying to create a new loan followed by I'm trying to create a new card. So now we have data stored in multiple microservices. With the help of Gateway server, we are going to invoke an API which is api/composite/fetchCustomerSummary with a query param mobileNumber and we need to pass the same mobileNumber. If we click on the send button I am getting an runtime error. Let me check what's the problem here? Inside my application.yml I made a mistake, so let me open the application.yml of gateway server. So here under the base-url I need to mention the port number:8072, which is the port number belongs to the gateway server. Previously I have mentioned 8070 which belongs to Eureka Server. We should always mention the gateway server port number, because the whole invocation of the microservices is going to happen within the gateway server. So let me save these changes. Do the build. Once the build is completed, let me send the request one more time. This time also I'm getting a runtime error. Let me try to understand what is this issue. Name for argument of type not specified and parameter name information not available via reflection. I got a hint on why we are getting this exception inside the CustomerSummaryClient interface for all the methods, we are using the RequestParam, but we have not mentioned what is the param name, so we should mention the param name as mobileNumber. I am under the impression whatever method parameter name that we have given here the same is going to be considered for the RequestParam. But it seems that is not happening here. That's why let me pass the param name specifically inside all these methods. So let me do a build. Once the build is completed, let's try to invoke the request one more time. Let's hope for the best! Hooray! We are getting the response successfully. So this is the response from the customer microservice and this is from the accounts and this is from the loans and this is from the cards microservice. We are getting the response within 66 milliseconds. I am sure it is going to take more time. If you have implemented composition pattern in a normal microservice with the help of feign client, because feign client will not use reactive programming, and with that all the microservices invocation, they are going to happen sequentially one by one. But in our scenario, the response, it is always going to be better because we have implemented the API composition pattern inside the Spring Cloud gateway. I hope you are clear on how we have overcome the challenge of cross service queries, with the help of API composition pattern. off course, this pattern has certain limitations, like this pattern can only be used for the read operations and for smaller applications only, whereas for bigger enterprise applications, definitely API composition pattern may not work. They need to consider advanced CQRS pattern. Hold your thoughts around CQRS pattern. We are going to discuss in detail about it in the coming sections. I hope you are clear. Thank you, bye.
