# Implementing API composition pattern using Spring Cloud Gateway - Part 2

As of now, we have developed this interface where we have built various methods conveying the metadata information about all the microservices that my gateway has to invoke. Apart from these metadata information, my gateway also should know what is a root URL like, what is a hostname port number along with the route base URL we're also expecting our gateway server to invoke these APIs parallelly, in a non-blocking functional style. For these kind of parallel invocation, we can't leverage open feign client. The reason is open feign client does not support reactive programming model. That's why instead of open feign client, we are going to use the web client, which is another component inside the spring ecosystem, which can be leveraged whenever we are looking to invoke the APIs in a non-blocking style, and parallelly. So to provide a base root URL and to create the configurations of web client. What we need to do is, we need to create one more package with the name config. Inside this package, I'm going to create a new Java class with the name ClientConfig. On top of this class I'm going to mention configuration annotation, followed by I'm going to create an private string variable with the name as baseUrl. This baseUrl value I want to read from the property file, so that we can change this value dynamically across various environments. So that's why I'm trying to use value annotation. To this value annotation I'm going to mention the variable value as app.base-url. Let me import the @Value annotation as a next step, I need to open the YAML file of gateway server. Here I'm going to create a property which is app:base-url. To this I'm going to mention the value localhost:8070 the same we are trying to refer from the client config. Now we have provided the base URL details to the client config. After this field, I'm going to create a method with the @Bean annotation on top of it this method it is going to return the object of CustomerSummaryClient interface that we have created previously. Let me mention the method name as customerClient() and it is not going to accept anything as an input inside this method. First, I want to create the object of WebClient by invoking the WebClient.builder.baseUrl and to these baseUrl() method, we need to pass the base URL field name followed by we need to invoke the build() method. Now we have WebClient object, which is aware of baseUrl. Once this WebClient object is created, I want to wrap it with the help of WebClientAdapter. Let me mention the object name as Adapter. Later, using the WebClientAdapter, I'm going to invoke the create() method. To this create() method I'll pass the object of WebClient. Basically, I'm trying to wrap this web client into a WebClientAdapter. I'll explain in few seconds on why we are trying to wrap this WebClient into WebClientAdapter, but before that, let me write a few more lines of code here. I'll try to create the object of HttpServiceProxyFactory. The object variable name I'm going to mention as factory itself, and on the right hand side with the help of same class which is HttpServiceProxyFactory, I'm going to invoke the builderFor() method to this builderFor() method I'm going to pass adapter as an input, followed by I'm going to invoke the build() method. At last I'm trying to return the object of customer summary client by writing the code which is Factory.createClient(). And to this createClient, we're trying to pass the interface information which is CustomerSummaryClient. Basically what these three lines are doing is inside our interface we only defined abstract methods in an declarative style. We have not defined any implementation code on how these microservices should be invoked. All the proxy implementation of these HTTP interfaces. It is going to be generated by using HttpServiceProxyFactory. But to this factory object we need to pass the object of type HttpExchangeAdapter. Since these builderFor() it is not going to accept the web client, we need to wrap this web client by using a utility tool which is WebClientAdapter. So by the time this create() method executes, it is going to give a WebClientAdapter which extends AbstractReactorHttpExchangeAdapter. If you open this class again you will be able to see it is implementing ReactorHttpExchangeAdapter and at last it is extending HttpExchangeAdapter. Basically, with the help of these WebClientAdapter, we are making our web client object ready to be used by other framework classes like HttpServiceProxyFactory. I hope you are clear. With the help of this method, we are trying to generate the actual implementation of all the declarative methods that we have defined here. So my web client has the baseUrl information and my HttpServiceProxyFactory. It also has all the metadata information. And inside this metadata we have provided actual path that web client can use to invoke so and so microservice. This is the input and this is the response. I hope you are clear on why we have created this client config. As a next step, I need to create a new class which is responsible to invoke all these microservices and aggregate or compose the response from the microservices. So let me create a new package with the name handler. Inside this handler package, I'm going to create a new Java class with the name CustomerCompositeHandler. On top of this class I'm going to mention annotations Component and RequiredArgsConstructor. nTo this class, we need to inject the bean of CustomeSummaryClient, because using the CustomerSummaryClient bean only, we can invoke actual microservices. Next, let me create a method which is going to return mono of sever response, and the method name is going to be fetchCustomerSummary(). And this method is going to accept server request. So these ServerRequest and server response, they are very similar to the HttpServletRequest and a HttpServletResponse that we see in traditional servlet based web applications. Since gateway is built based upon the reactive programming, we need to use the service request to accept the request and service response to send the response to the client applications. From the ServerRequest I'm going to get the query param with the name mobileNumber, the same query param value I'm going to catch inside a variable which is mobileNumber. Using this mobileNumber, first I'm going to invoke the method which is fetchCustomerDetails(). And to this fetchCustomerDetails() method I'm going to pass the mobileNumber as an input. The output from this method we already know mono of ResponseEntity, which is going to wrap the CustomerDto as an object. I'll just keep the return type variable name as customerDetails. At this line of code, we are trying to actually invoke the Customer microservice. Let me try to repeat the same line multiple times. At the next line, I'll try to invoke the accounts details and the variable name I'll change to account details here and here also instead of CustomerDto, I need to mention AccountsDto. Next, let me invoke fetchLoansDetails()and rename this variable name to loansDetails. And here instead of CustomerDto we need to mention LoansDto. At last I need to rename this method name to fetchCardDetails() and the variable name also I'm going to rename to cardDetails(). And here I'm going to mention CardsDto. With these four lines of code. We are trying to invoke four different microservices. Parallelly in a non-blocking style. When this line of code is executed my thread, it is not going to wait for the response of customerDetails(). . Instead, it is going to simply jump onto the next line of code. This way, all these four microservices, they are going to be invoked parallelly in a non-blocking style. But here at the end of the method, I can't simply return because I need to wait for these four operations to complete once the operations are completed, I want to aggregate all the response into a single object and the same I want to attach to the body of the ServerResponse. So let's try to understand how to do the same to make sure that my thread is waiting for all the above four operations to complete, I need to use the zip method available inside the mono. To this zip() method I need to pass all the four object to details like CustomerDetails, AccountDetails, LoanDetails and CardDetails. With this, what is going to happen is my thread is going to wait until all these objects are completely emitted from the API invocation that we are trying to do here. Once all these objects are available, we can invoke a method which is flatMap(). To this flatMap(), we are going to get a Tuple object. So what is a Tuple object? A Tuple object is capable of holding multiple objects of different data types, usually inside collections like List, Set, they are only capable of storing a single data type objects. But inside reactive programming, this Tuple is capable of holding multiple elements, which belongs to various data types. So here all these elements, they belongs to various data types. So by the time this zip() method completes we are going to get a Tuple object which is storing four different elements customerDetails, accountDetails, loanDetails and cardDetails. So that's why inside this flatMap(), using this Tuple, what I'm going to do is, I'm going to invoke the method which is getT1(), which is going to give me ResponseEntity object that wraps CustomerDto. So from this ResponseEntity object I'm going to invoke the getBody(), which is going to give me the CustomerDto object. The same I'm trying to catch on the left hand side with the variable CustomerDto. Very similar code I can try to write for other objects as well. So here I need to invoke the getT2() followed by getT3() and at last getT4(). So here I need to rename this to AccountsDto. And the variable name also is going to be AccountsDto. And the third one I need to rename to LoansDto. And this one I'll try to change to CardsDto. So let me rename these variable names also. So with this now we have four different objects in our hand. As a next step we need to create the object of CustomerSummaryDto with the help of constructor of CustomerSummaryDto to this constructor I'm going to pass CustomerDto, AccountsDto, LoansDto, CardsDto. At last, from these flatMap(), I'm going to return a ServerResponse object by invoking ServerResponse.ok() method indicating that the invocation is successful, followed by content type with the media type as application JSON. And at last I'm going to invoke the body() method to this body() method I'm going to pass the code which is body inserters dot from value, and to this I'll pass the object of CustomerSummaryDto. So from these flatMap(), we're trying to return the ServerResponse object as an output, the same ServerResponse object we want to return as an output from this method. That's why here we need to put a return statement. With this now our handler it is trying to invoke all the multiple microservices parallelly and aggregate the response into a single object. Here I'm getting a compilation error. I need to keep a semicolon here. Now you might be under the assumption that everything is ready. We can invoke this method from the client applications, but it's not going to work that way. This is not a Rest API, it's just a handler method. Inside reactive applications, we can't build Rest APIs traditionally with the help of RestController. Instead, we need to define routing configurations which are capable of handling a specific API request. Let's try to implement the same in the next lecture. Thank you, bye.
