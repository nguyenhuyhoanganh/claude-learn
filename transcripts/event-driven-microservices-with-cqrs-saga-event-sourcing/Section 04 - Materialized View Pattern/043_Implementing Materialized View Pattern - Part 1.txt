# Implementing Materialized View Pattern - Part 1

To implement the materialized view pattern inside our microservices. I need to create a new microservice with the name profile. Since we are into the new section, what I'm going to do is, I'll try to copy the section3 folder and paste it inside the same workspace. After pasting the folder, I can rename this folder to section four, because right now we are into the section4 and inside this section4, I'll try to look for any hidden folders. So there is a hidden folder which is related to IntelliJ. So let me delete the same once we copied the data from the previous section. As a next step, I want to create a new microservice inside the same section4 folder for this also, I can try to copy one of the microservice which is customer, and paste it here inside the same folder and I'm going to rename this folder to profile. Let me go into this profile and open the pom.xml file inside this pom.xml file, I need to rename this customer to profile. So let me make the same change under the name. Also under the description I will say microservice for reading profile info. So this is the description. Let me scroll back and check if I need to change anything. Seems I'm good here. Let me save this pom.xml. Now I'll go to the IntelliJ and try to open this section4 folder. Here let me click on this open and go to storage, workspaces Event Driven Microservices section4 and click on this open. Once this folder is open I need to go to the notification and click on this load button. As soon as I click on this button, all my projects are going to be detected as a maven projects inside my IntelliJ workspace. As of now, these profile microservice has code related to the customer microservice. We need to make some good amount of changes. Let's do these changes very quickly. First, let me go to the resources application.yml. Here first I need to update the port number to 8084. Followed by I'm going to update the name of the application to profile and h2 file also I'll try to give here as profile. Let me scroll down I am going to keep all these things as it is, and at last I'm going to update this info to profile and the description to Easy Bank profile application. And here under the Axon related properties, I'm going to get rid of these event handling related properties. Because inside this profile microservice I'm not going to build any logic around the commands. I'm only going to focus on the query side. That's why I'm going to delete these properties. And I'll just maintain only these properties using this property. My profile microservice is going to connect with the Axon server. My profile microservice has to connect with the Axon server, because we are going to make other microservice to publish the events whenever the data is changed. And the published events we are going to handle inside the profile microservice. Since we are going to leverage the Axon server as an event bus instead of Kafka and RabbitMQ, we need to make sure our profile microservice is connected to the Axon server, so we are good from the application.yml. Let me close this and next I'm going to delete the schema.sql file, because I'm going to create a profile related table with the help of JpaEntity class itself. So let me delete this Entire.sql file next under the Java resource folder first I'm going to rename the package name to com.eazybytes.profile. So here I need to select all in selected module. So let me rename this folder to profile. And as soon as I click on this refactor, the package name is going to be updated to the profile and at the AuditAwareImpl I need to mention current auditor name as profile_microservice followed by I can delete this entire command package and its subpackages because we're not going to build anything around the command side, we're only going to focus on the query side. So let me delete this command package followed by I'm going to rename these customer constants to profile constants. Once the renaming is completed I'm going to rename the constructor and rename the file as well. So let me close this. And under the dto I'm going to rename this CustomerDto to ProfileDto. And let me rename the file as well. And here under the entity we're going to rename this Customer to Profile. Let me rename the Java file. We'll get back to these files and change the code inside them. For now, let me take care of renaming all the files next. Under the exception, we don't have to make any changes. Let's go to the mapper. Under the mapper I'm going to rename this class to ProfileMapper and get rid of all these methods. For now, let's keep this class as empty. We'll come to these files later. Under the query package we have a ControllerHandlerProjection and query. So this query I'm going to rename to FindProfileQuery. So let me rename the class. And coming to the field we should be fine with the mobileNumber because we want to query the profile microservice with the mobileNumber itself. Let me go to the controller. And here instead of CustomerQueryController I'm going to mention as ProfileQueryController. Inside this ProfileQueryController instead of CustomerDto I'm going to return ProfileDto, and instead of fetchCustomerDetails(), we can mention fetchProfileDetails() as the method name and Request param is going to be mobileNumber and the API instead of fetch, we can mention as profile. So anyone who want to invoke these API, they need to mention the path as /api/profile. Next I want to create the object of FindProfileQuery. Using the same I'm going to invoke the query method of the query gateway to this query gateway I'm going to pass this profile query and the Response type. It is going to be ProfileDto. Let me mention the same. And here also the return type is going to be ProfileDto the same we are going to return as a response from this method. So we are good from the Controller class. So let me rename this file as well. Once the renaming of the file is completed, we can go to the handler And inside the handler we are going to rename the class to ProfileQueryHandler here instead of findCustomer() let me mention the method name as findProfile and this method it is going to accept the object of findProfileQuery() and using the same, we are going to invoke the service() method. Inside this object of findProfileQuery(), we are going to have the mobileNumber. So using the same we are going to invoke the method inside the Service classes. Here instead of ICustomerService I can try to type I ProfileService. What I can do is, I'll go to the service and here instead of ICustomerService I'll rename this to IProfileService later I can get rid of these methods. For now I'm going to have a single fetch() method which is fetchProfile, and the return type from this method is going to be ProfileDto. So let me rename the interface file name. And here also I need to update the method comments. So here instead of account details, we can mention profile details based upon a given mobileNumber. So let me close this and go to the CustomerQueryHandler. And here this time I should not have any compilation errors. So let me import a IProfileService properly to resolve this error, since I'm still getting the error because I'm yet to rename this impl class. So let me rename this impl class by implementing AI IProfileService, followed by renaming this class to ProfileServiceImpl followed by we can get rid of this, createCustomer and updateCustomer and deleteCustomer methods now here what we can do is, we can try to override the method from the interface the method name is fetchProfile(). Inside this fetchProfile() we can try to copy this entire code and paste it here. To resolve these compilation issues, we need to update the CustomerRepository as well. Before that, let me delete this unused method and and go to these CustomerRepository. So here I'm going to rename these interface to ProfileRepository followed by ProfileEntity. I'm going to pass to this JpaRepository generics. Next I can remove this second abstract method and I can keep only the very first abstract method which is findByMobileNumber and activeSw and the return type from this method is going to be optional off profile this now we should be good from the ProfileRepository perspective. Let me go to the ServiceImpl. And here I'm going to inject ProfileRepository into this class. Let me rename the variable name to ProfileRepository. Using this ProfileRepository I'm going to invoke the findByMobileNumber and activeSw. And the return type from this method is going to be profile, a variable name profile itself. If there is no profile, we are going to throw an ResourceNotFoundException with the resource name as profile and with the mobileNumber that we have received and towards the end, using the ProfileMapper, we need to invoke one of the method. But before we try to write a method inside the ProfileMapper, we need to define fields inside the entity class and inside that DTO class. First let me go to the ProfileDto inside this Dto class I'm going to delete this existing code and I'll try to mention the field names which are name mobile number that we are going to get from the customer microservice and account number from the account microservice card number from the card microservice and loan number from the loan microservice. So we are good from the DTO. Next we can go to the ProfileEntity class here also, I'm going to get rid of this code. And just to save some time I'm going to paste some code here. As you can see here first I have created a field which is ProfileId. With this field behind the scenes, we are going to create a column with the name ProfileId under the table profile. This profileId column is going to act as a primary key. Every time we insert a new record, we want the framework to take care of generating the sequence number and using the same during the insert operation. That's why I'm trying to use these two annotations @Id and @GeneratedValue with a strategy as identity. Next, using the name column, we are going to store the name data followed by mobileNumber, activeSw with a default value as false, accountNumber, cardNumber and loanNumber. So these three columns are these three fields. They are nullable so during the insert operation we are going to insert some null values or some default zero values. Since we are trying to store them as long values. Now we can go to the ProfileMapper inside the ProfileMapper, I am going to create a new method with the name mapToProfileDto(). So here we are going to import the ProfileEntity. Once this method is created, we can go to the ServiceImpl class and here we can invoke the mapToProfileDto() and to this method the source we can pass the Profile object followed by a new object of ProfileDto class. So on the return type, let me update the variable name to ProfileDto and the return data type as ProfileDto. The same I'm going to return from this method. So now we are good, but we need to rename the class name file name as well. Let me rename the file with that we should be good from the ServiceImpl. Let me close this and go to the QueryHandler. And here inside the QueryHandler using the IProfileService, we are going to invoke the methodfetchProfile() and since this is going to return the profile data, we need to update the same here. Let me rename the file name as well to resolve this issue. Now we can go to the projection package inside the projection package as of now we have a class with the name CustomerProjection. Let's rename this CustomerProjection to ProfileProjection. And to this class I'm going to inject a IProfileServiceImplementation class as a bean. And inside this Projection class we are going to write business logic to handle various events that are going to be published by the individual microservices. For now, I can delete this entire code related to the customer microservice. We can write our own new event handling logic in the coming lectures. For now, let me rename this file and go to the package structure. And with this I'm assuming we made the all the required changes. Let's go to the CustomersApplication. And here also what I'm going to do is, I'll try to rename this to ProfileApplication, followed by I'm going to rename the class name here. And we don't need these two methods because we are not going to have any Interceptor class. And since we are also not implementing any command set logic, we can get rid of these event processing configure logic as well. So let me format followed by I'm going to rename the file as the last step. We can go to the test folder and the test folder also we can try to rename this class. So instead of CustomerApplicationTest I'm going to mention ProfileApplicationTest. I'm also going to rename this file as well. So the file name is AccountsApplicationTest. I'll just change to ProfileApplicationTest. I hope you're clear. With whatever changes that we have done so far. Let's continue this journey in the next lecture as well. Thank you, bye.
