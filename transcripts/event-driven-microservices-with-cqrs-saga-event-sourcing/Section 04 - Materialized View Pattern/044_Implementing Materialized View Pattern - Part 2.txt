# Implementing Materialized View Pattern - Part 2

Profile microservice is ready as a next step, we can start publishing the events from the individual microservices. Whenever the data is being changed inside these services. To publish these events, we need to create the event classes. As of now, we have created these Event classes directly under the command package because the commands they are going to publish these events using the aggregate lifecycle. Very similarly we have to create these event classes, but we should not create these event classes directly inside the individual microservices, because the same event is going to be used by the profile microservice as well. So since the event class is going to be shared by the multiple microservices, I need to create these event classes under the common project. So let me create a new package with the name com.eazybytes.common.event. Inside this event package, the very first class that I'm going to create is CustomerDataChangedEvent. This event is going to be published whenever the data on the customer side is changing. Similarly, let me create three more classes for the account microservice the class name is going to be account data changed event followed by for loans microservice it is going to be LoanDataChangedEvent. And at last for the card microservice the class name is going to be CardDataChangedEvent. Let's try to declare the required fields inside these event classes first. On top of the CustomerDataChangedEvent, I'm going to mention Lombok annotation, which is data followed by inside this event class the very first field that I'm going to create is name, because we are looking to store this name field inside the Profile microservice. That's why whenever my Customer microservice publishing an event about the data change, it should include the name field after the name field, it should also include the mobileNumber, mobileNumber is very important. We're also looking to maintain this column inside the materialized view database. The last field that I want to create is private boolean activeSw. So these are the three fields that I require from the customer microservice I don't require email and other fields related to the customer microservice. Now let's go to the AccountDataChangedEvent. And here also I am mentioning the data Lombok annotation followed by first we need to create the field which is private string mobileNumber. Because using this mobileNumber only we need to identify the record inside the profile database. Once the record is identified, we are going to populate the accountNumber. So let me create another field which is private long accountNumber. And here I don't want to maintain any activeSw related field because whenever the account data is deleted or not present I'm going to maintain a default value which is zero or null value. Very similarly, we need to populate the data inside the LoanDataChangedEvent. So let me copy paste the same fields and mention the nanotation @Data. And here instead of accountNumber I need to mention loanNumber. Next let's go to the CardDataChangedEvent. And here I'm going to paste the fields and I'll rename this accountNumber to cardNumber. On top of this class I need to mention @Data Annotation. So now we have created the required event classes under the Common Project. Now these event classes can be leveraged by any of the microservices. As a next step, we need to start dispatching our publishing the events from the individual microservices to the profile microservice. First, let's try to make changes inside the customer microservice. Inside the CustomerMicroservice, we have a class with the name CustomerAggregate inside this class, we are handling the commands that manipulates the data of customer. We have a method handling the createCustomerCommand. Similarly for updateCustomerCommand followed by deleteCustomerCommand. So here what we can do is, first let me delete these commented code. So just like how we are creating a customer created event, what I'm going to do is I'm going to create a new object of CustomerDataChangedEvent once this object is Objects created using the same bean utils method. I'm going to copy the data from the CreateCustomerCommand to CustomerDataChangedEvent. Once the event is ready, we need to dispatch the event from the Command class how to dispatch it's very easy. We need to invoke the apply() method and behind the scenes the event is going to be dispatched. Once this event sourcing handling is handled, but with the help of apply() method, we will be able to dispatch our publish only one event. How about the other event that we have? Let me explain how to dispatch multiple events. This apply() method that we are trying to invoke. It is going to return an object of type ApplyMore. Using this ApplyMore, we should be able to invoke many other methods available to dispatch more events. So there are four different methods. You can read the documentation of these methods to understand when to use. But in our scenario I'm going to invoke the method which is and then because I want to dispatch the event without any conditions, as soon as this very first event is dispatched and processed immediately, I want to dispatch the second event without worrying about any conditions. That's why I need to use this method. Now to this method I need to pass the lambda expression and inside this lambda expression, using the aggregate lifecycle, I need to invoke the same apply() method, but this time to the apply() method we need to pass CustomerDataChangedEvent with this, what is going to happen once the very first event is processed? The second event is going to be dispatched. So this is one approach. I'm also going to discuss other approaches as well. Next under the update scenario also we want to execute the very similar logic. So let me copy these two lines and go to the updateCustomerCommandHandler() method. Here I'm going to paste the two lines and the command name here is updateCustomerCommand(). Let me update the same. This time I'll show you the different approach, which is we can try to duplicate the same line and using these apply() method, we can try to dispatch the second event which is CustomerDataChangedEvent. . So this is another style of dispatching multiple events. As long as you are invoking the apply() method multiple times with the different event details, all those events, they are going to be dispatched by the aggregate lifecycle. Here since we are using Axon framework inside our microservices, I'm trying to leverage the Axon framework to dispatch the events. But if you're not using Axon framework, then you know what to do in case if you are using Kafka or RabbitMQ inside your Spring Boot applications, then you can leverage Spring Cloud Stream to dispatch the events to the Kafka topics or to the rabbit queues. Inside my microservice course, I explained in detail on how to dispatch the events to the Kafka topic out to the rabbit queue. In case if you have some questions around the same, please go to the microservice course and complete the respective sections related to the Kafka and RabbitMQ. At last, we need to dispatch an event from the deleteCustomerCommand as well, but we can't dispatch an event from this method. The reason is under the deleteCustomerCommand we are only accepting the customerId but not the mobileNumber. . If I don't have mobileNumber then I can't populate the mobileNumber here. And with that, my profile microservice will not have any clue to which customer so and so even needs to be processed. That's why what I'm going to do is instead of dispatching the event inside the command method, I'll try to dispatch the event directly from the ServiceImpl class. So let me go to the CustomerServiceImpl here inside this class we have this deleteCustomer which is going to be execute to update the flag to the inactive switch. So just after these operation, I want to trigger an event to the profile microservice. But the problem here is this is not an Aggregator class. So that's why we can't use aggregator lifecycle instead what we can do is, we can try to use event gateway that is provided by the Axon framework. If you try to inject this as a bean using this bean, you should be able to publish the a single event, a list of events. Now to publish the event, first we need the event object. What I can do is, I'll try to create the object of CustomerUpdatedEvent. Once this object is created inside this object, I'm going to populate the mobile number by invoking the setMobileNumber() method. So from where I can get this mobileNumber from the CustomerEntity object that is loaded from the database using the customerId provided. So let me invoke this getMobileNumber. Very similarly, I need to populate the active switch to this activeSw. . I'm going to populate the value which is false or we can try to copy these constant. So let me copy this and paste it here and once the Event object is ready we can try to pass the same to the publish() method of the EventGateway. With these changes my Customer microservice is going to publish the events whenever the data is being changed. Let's revise what we have done so far inside the CustomerAggregate class under the createCustomerCommand, we have created the object of CustomerDataChangedEvent the same we are trying to dispatch with the help of apply() method. So this is one style of dispatching. The other style is, we can try to invoke the apply() method multiple times in multiple lines. So that is what we have done under the updateCustomerCommand coming to the delete since the deleteCustomerCommand will not have the mobileNumber. We can't dispatch the command right away here. Instead, we are trying to do that inside the CustomerServiceImpl under this DeleteCustomer. Here we are trying to leverage the event gateway to publish our dispatch the event. The same kind of changes we have to make inside the other three microservices, which are accounts, cards and loans. What we can do is, you can take this as an assignment. By the time you come to the next lecture, please make the very similar changes inside the other three microservices and make sure you are publishing the respective events from your microservice. As of now, we are only publishing the CustomerDataChangeEvent. Very similarly, you can publish these three different events from the individual microservices. I'm also going to make these changes behind the scenes. By the time I come to the next lecture, I'll have these changes inside the other three microservices. So take this as a challenge come on, you can do it. Thank you, bye.
