# Implementing Materialized View Pattern - Part 4

To show you that demo first what I'm going to do is, I'll try to stop the existing Axon server container. Once the container is stopped and deleted, I'll delete the content present inside the data and events because I want to start everything from scratch. Similarly, I'll go to my user folder and try to delete all the h2 database related files. These are all the h2 database related files, which belongs to all the microservices that I have, so let me delete them. Next, from the terminal I'm going to execute the command to start my Axon server container. Once the Axon server container is started, we can try to validate the same by going to the dashboard. And here inside the dashboard, please make sure you are clicking on this complete button. Now we can go ahead and start all our microservices. First I'll try to start my Eureka server. Once the Eureka server is started, I'll try to start my accounts application followed by cards application, customer application and loans application. For some reason, the profile application name is coming as customers application. Let me delete the same and go to the class which is ProfileApplication. From this class I'll try to start my ProfileApplication microservice. Once my profile microservice is also started, I'll try to start my GatewayServerApplication. Now all my microservices and supporting components are started successfully. First, I'll try to test the CustomersApplication microservice by creating a brand new customer. As soon as we try to create a new customer, an event should be dispatched to the profile microservice so I can try to clean the console under the customers application and under the Profiles application as well. Inside the postman, under the customer microservice, I'm going to create a new customer with this mobileNumber. So let me click on this send button. I have received the successful message, but if I check the app profile microservice logs, there is an runtime exception due to which the given event is not processed. So if you see here for the profile microservice, someone is dispatching the CustomerDataChangedEvent we know who is that someone? The someone here is customer microservice, but we are getting an security related exception. I know why this is happening. I'll also show you how to fix it. But before that, let me spend a few minutes explaining about these exception. So as of now what is happening? You are trying to dispatch an event from the customer microservice to the profile microservice. These two different microservices are started under different Jvms or under different Tomcat servers. So whenever you are trying to send an object from one application to the other application over a network. What is going to happen? Serialization of the object is going to happen for security reasons by default, the sharing of the object from one service to other service is not allowed whenever we are using the Axon framework to fix this issue, we need to tell to the Axon server please allow serialization of my event objects so that the multiple microservices they can dispatch and consume the same event based upon the Axon of the end user, so we can try to communicate the same to the Axon server. To configure the same, we need to go to the common project. Inside my common project, I'm going to create a new configuration class because instead of defining these configuration class in all the microservices separately, what I can do is, I can try to create this under the common and the same. I can import inside all the microservices. Here let me create a new package with the name config. Under this new package, I'm going to create a new Java class which is AxonConfig. On top of this class I'm going to keep the @Configuration annotation. Now inside this class I'm going to create a method which is going to return the bean of XStream object. So let me type public Xstream this XStream class you need to import from the package of com.thoughtWorks.xstream method name also, I'm going to keep as xStream() and this method is not going to accept anything as an input. On top of this method, I'm going to mention the @Bean annotation. Now inside this method, first let me create the object of XStreme. Using this XStream object, I need to invoke the allowed types by wildcard and to this wildcard method I need to pass the an array of string. Inside this array of string, I need to pass all the wild cards that I want to be considered as a safe. So here I'm going to mention an pattern which is com.eazybytes.**. With these what I'm trying to say is any class which has a package name that starts with com.eazybytes I want that class object serialization to be allowed. And towards the end we need to return the XStreme object. Off course, here we can try to be more specific instead of mentioning a generic package, we can try to mention the event package name itself like com.eazybytes.common.event. But I want to follow this so that in future if something gets added, I don't want to come here and make changes. This way I can provide list of patterns that can be considered by the framework to allow the serialization of the object. Now we have created a configuration file, but this file we need to use in all the microservices. What I can do is, I can try to open the customers application on top of this class name, I'm going to mention the annotation which is @Import, and to this @Import annotation I'm going to pass AxonConfig.class. . Since we have a single class, we can get rid of these curly braces. We can directly mention the class name since inside the Customers application also the route package is com.eazybytes. The same configuration should work, otherwise you need to mention your customer specific package inside this AxonConfig. Let's go to the Accounts application here also I'm going to mention the same followed by LoansApplication. Once we imported this class into the loans microservice, we can go to the cards application and mention the same here and at last we need to mention under the profile microservice as well. Once we have imported these in all the microservices, let me do the build and see if the dev tools restart is good enough to detect these changes. What I'll do is, I'll try to clean the console of profile microservice and try to create a new customer with a new mobileNumber, which is going to end with the 7677. So let me click on the send button, this time under the profile microservice. Hooray! We don't have any runtime exception, which confirms that the data might have changed behind the scenes into the profile database. We can also validate the same. Let me go to application.yml of profile microservice. Here I'll try to connect to this data source by clicking on this icon. Let me click on this test connection and I'm fine with this username and password. Let me click on this apply, ok. And if I go to the database and here we have a new database connection. Let me open the tables that I have. So under the tables you can see I have a table with the name profile. Let me right click and select New Query Console. Inside this console I'm going to type select star from profile. If I try to execute this query, you'll be able to see there is a record inside the database with the mobile number as 3254537677. The name is also populated successfully. The LoanNumber, CardNumber and accountNumber they are populated with the default values, which is zero. This confirms that the customer microservice is dispatching the event successfully. We can also validate whether the customer microservice is dispatching the customer updates as well. So using the same mobileNumber, I'll try to fetch the customer. I'll copy this body and paste it inside the update customer. Here the name I'll try to mention Madan Mohan Reddy and try to click on this send button. Let me try to execute this query one more time. This time the name you should have as Madan Mohan Reddy. Did you see every change that is happening on the source side? The same is being reflected on the materialized view as well. Let's try to create data of account as well with the same mobileNumber. So I have created a new account. Similarly, I can try to create a new loan followed by new card. So now if I try to invoke this fetch profile API under the profile microservice with the same mobile number, you will be able to see that I'm getting name, mobileNumber, accountNumber, cardNumber and loanNumber. Let's try to delete the card and see if it is working fine or not. So what I can do is, I can try to fetch the existing card details and using this card number I'm going to invoke the deleteCardOperation(). If I click on the send button, I'm getting an runtime exception which says name for the argument of type not specified. Since we have added these extreme related changes, it is creating some problems at the runtime. So to resolve this problem go to the @RequestParam annotation. And here we need to mention card number as a param name. You need to make sure you are mentioning the same in all the places wherever you are using the RequestParam. I'm going to make the similar change in the remaining places and push the code into the GitHub repo. So please make sure you are also making this change. Let me save these changes to the build. Now I'm going to invoke the delete API one more time. This time I'm getting a gateway related error because Sims build is still going on. Let me click on the send button one more time. This time I got a successful message. If I try to invoke these profile API one more time. Still, I'm getting the card number as previous value. I should get the value as zero. Let's try to understand the same. Inside the console of the profile application. I see there is some runtime exception. So what is happening is, we are trying to send the null value from the individual microservice. The same is trying to construct into a primitive value, because inside the database for the card number, loan number and account number microservices, we are using the int data type. That's why null is not acceptable. It is trying to convert these null to some long value. So to resolve this issue, what we can do is, we can try to send a zero value instead of null value from the individual microservice. So let me go to LoanServiceImpl and here instead of null let me set zero. So since zero is not going to be accept I need to mention OL the same kind of setup I can try to make inside the CardServiceApplication as well. et me mention 0L here and I'll go to AccountsServiceImpl and here also I'm going to mention 0L. Once we make these changes let me do a build. I'll try to clean the console of profile application. Let's hope this time we are going to be successful since the card is already deleted on the source database, we can't invoke the same delete operation one more time. Instead, I'll try with that delete loan. First, I need to get the loan details using the fetch loan API. Using the same loan number, I can try to invoke the delete loan API. So the delete operation is successful let me go to the fetch profile and try to invoke the send() method. Hooray! The loan number successfully updated to zero. Let me test the same for AccountsMicroservice as well. First, I'll try to fetch the account details using the same accountNumber I'll try to invoke the deleteAccount API. So the account details are deleted if I go to the fetch profile, the account number is also updated to zero. I hope you are happy with this demo. Inside the next lecture, let's try to do one more end to end demo. And before we try to conclude this section, I'll also share more important details around the materialized view pattern. Thank you, bye.
