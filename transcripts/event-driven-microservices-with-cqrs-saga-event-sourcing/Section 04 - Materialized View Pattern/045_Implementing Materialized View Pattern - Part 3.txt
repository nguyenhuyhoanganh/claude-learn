# Implementing Materialized View Pattern - Part 3

I made the event publishing changes in other three microservices as well. I hope you are also successful. Let me show you one microservice changes that I have done inside the accounts microservice. I have a class with the name AccountAggregate inside this Aggregate if you go to the CreateAccountCommand, just like how we have discussed in the previous lecture very similarly, I'm trying to create an object of AccountDataChangedEvent, and copy the data from the command to this event, followed by this event I'm trying to publish or dispatch with the help of this andThen() method in the same class if you can scroll down under the updateAccountCommand() method here also, I'm trying to create the object of AccountDataChangedEvent the same I'm trying to publish with the help of apply() method. Next let's go to the AccountServiceImpl. Inside the ServiceImpl towards the end we have a method called deleteAccount(). Here also I'm trying to create the object of AccountDataChangedEvent. Into this object I am trying to populate the mobileNumber based upon the AccountEntity that I have loaded from the database. Coming to the accountNumber I'm trying to set as null. Why I'm trying to set null because this account number is marked as inactive on the actual database. That's why inside my profile database I'm trying to maintain a null value. Instead of this null value you can also maintain account related active switch column. But I felt this is very simple when our accountNumber is null or zero, I can assume there is no account or the existing account is deleted. The same kind of logic I have followed for the loans and cards as well. I hope you are clear. So now what is happening? All our individual microservices they will publish or dispatch the events whenever a data is being changed. As an excerpt inside our profile microservice, we need to handle these events by writing some business logic. So let me go to the ProfileProjection class. Inside this ProfileProjection class, I'm going to first write a method which is public void, and the method name I'm going to mention as on and this method it is going to handle the event of type CustomerDataChangedEvent. So inside this method only I'm going to write the business logic. But before that, on top of this method I need to mention an annotation which is @EventHandler. From this method I'm going to invoke the method available inside the ProfileServiceImplementation class. But first what I have to do is, I need to create a abstract method inside the IProfileService interface. So let me create the abstract method which is void handle CustomerDataChangedEvent. So this is the method name. And the parameter that my method is going to accept is CustomerDataChangedEvent. Let me mention the method comments on top of this abstract method. Now I can go to the implementation class which is ProfileServiceImpl. And inside this class I'm going to implement the abstract methods from the interface. So let me select this implement methods and select abstract method from the interface. And you can see as of now my method is empty(). So first using the mobileNumber that I have received I need to identify whether there is an existing record inside my profile database. So that's why using the ProfileRepository I'm going to invoke the findByMobileNumber and activeSw to this I'm going to pass the mobileNumber that I have received from the CustomerDataChangedEvent followed by the second parameter I'm going to mention as true or I can also mention the constant from the Profile constants file. Me type ProfileConstants.activeSw. . In case if there is no record inside my database with a given mobileNumber, I want to create a new Profile object. That's why I'm trying to invoke the orElseGet() method, and from this orElseGet() method I'm going to return the new object of Profile. So let me use constructor reference here. So let me mention semicolon. And on the left hand side I'm going to catch the output with the variable name as Profile itself. So what this line is going to do it is going to check the database if there is an existing record. If yes, it is going to assign the existing record object to this Profile object. Otherwise we are going to get a brand new object next into this Profile object, I can try to set the mobileNumber by reading the mobileNumber from the CustomerDataChangedEvent very similarly I can also try to set the name as well. After the name, I can try to set the activeSwitch as well, so let me invoke Profile.isActiveSw and this activeSw I'll try to populate from the CustomerDataChangedEvent. So here instead of is activeSw I need to invoke setActiveSw. So this code looks good. But there is a some special scenario that we need to consider here. As of now, you can see we are trying to blindly set the name from the event object to the Profile object. But in the scenario of delete operation inside the CustomerServiceImpl if you see here we are trying to send only the mobileNumber and activeSw, but not the name. And with that, this object is going to have the name value as null. And if you try to set the same null value here, the saving of the Profile object is going to fail because name is a non-null column inside the profile database. So to overcome this challenge, what we can do is we can try to have an if condition instead of typing the if condition manually, I can try to use the shortcut if n, which is going to insert some null statement. So let me use the CustomerDataChangedEvent object and using the same I'm going to check if get name is not null. If s then only I'm going to populate the name value into the Profile object. Otherwise I'm not going to make any change to the existing name inside the database. And towards the end, using the ProfileRepository, we can invoke the save() method and to the same method we can pass the Profile object. I hope you are clear what we have written here. let's go to the ProfileProjection and here using the IProfileService I'm going to invoke the HandleCustomerDataChangedEvent and to the same I'll try to pass the CustomerDataChangedEvent as an input. In the similar lines, we need to create the logic for the accounts, cards and loans microservice. So let me go to the IProfileService here I'm going to copy this abstract method multiple times. So in total I'm trying to paste three times. Now I'll try to change this abstract method name to HandleAccountDataChangedEvent to the same. We need to pass AccountDataChangedEvent as an input. So let me update the same inside the method comments as well. The next method name I'm going to change to HandleLoanDataChangedEvent and the method input parameter is LoanDataChangedEvent. The same parameter details I'm trying to update here inside the method comments and the last abstract method. We can try to update this to HandleCardDataChangedEvent which is going to accept CardDataChangedEvent as an input. So once we have created these abstract methods, we need to go to the ProfileServiceImpl. And here we can try to implement the methods. As of now you can see we have to implement three methods. I'm trying to select all three of them and click on this okay. First let's try to write logic inside the HandleAccountDataChangedEvent with the help of ProfileRepository I'm going to invoke the findByMobileNumber and active switch. And the mobileNumber I'm going to get from the AccountDataChangedEvent object, followed by using the profile constant I'm going to invoke the activeSw constant. So with this it is going to check whether a profile record is already present inside the database. If it is not present I want to throw an exception because by the time these AccountDataChangedEvent is being processed, I am expecting the customer data is already present inside the profile database. And that's how the UI application is also going to work. First a customer will be created, followed by the customer can create an account, loan or card. That's why in the case where a profile is not available with a given mobileNumber, in this scenario, I want to throw an exception by using our else throw. The exception that I want to throw is, ResourceNotFoundException of type resource profile with a given mobileNumber, whereas if there is an existing record, I'm going to catch the record with the variable name as profile using the same profile, variable or object, I'm going to invoke the method which is setAccountNumber, and I'm going to get the accountNumber from the AccountDataChangedEvent. And towards the end we can try to invoke the save() method. And to the save method we can try to pass the object of Profile. I hope you are clear. Very similar logic we need to build inside the loan method and card related method. So first here I'm going to get the mobileNumber using the LoanDataChangedEvent. And here instead of setAccountNumber we need to mention setLoanNumber. And here also we need to invoke the getLoanNumber. Let me make the changes inside the card method as well. So we need to read the mobileNumber from the CardDataChangedEvent object. And here using the CardDataChangeEvent we need to invoke the getCardNumber. And here also we need to invoke the setCardNumber. I'm assuming we made all the required changes inside the ServiceImplementation class. Let me close them next. Here inside the Projection we need to copy this method multiple times. So in total I'm going to copy three different times. The second method I'm going to update to handle the event of type AccountDataChangedEvent the same we need to pass as an input to the method HandleAccountDataChanged event present inside the IProfileService. Let's try to make the similar change for the LoanDataChangedEvent as well here also inside the method body I need to invoke HandleLoanDataChangeEvent and towards the end we need to handle the CardDataChangedEvent. And the method here we need to invoke is HandleCardDataChangedEvent. Now we have done all the required changes. The individual microservices they are going to dispatch the events and my Profile microservice it is going to listen to them or it is going to consume those events and update the profile database. As a next step, we can start testing these scenario end to end. If you can recall, we already built an rest API inside the profile microservice. So if you go to the ProfileQueryController here, we have an API with the name profile. From this API we are trying to dispatch the query findProfileQuery(), which is going to be handled by the ProfileQueryHandler. Here we are simply trying to fetch the profile details from the database, but to invoke this API present inside the ProfileQueryController, we need to make changes inside the gateway application. If you go to the Gateway Server application, as of now, we have the routing related configurations only for the customer, accounts, loans and cards. Very similarly, we need to add the routing related configurations for the profile microservice as well. So here instead of cards I can type profile and the same profile I can paste it here and here under the load balancer we need to type profile in capital letters. So we should be good. Let me do a build. My build is successful which confirms there are no issues. I hope you are super excited for the demo in the next lecture. Thank you, bye.
