# Demo of Subscription queries - Part 1

As of now, when we invoke these mobile number API, we are getting the successful message immediately, regardless of the outcome of the Saga pattern. Instead, I want to enhance my code to provide me the overall status of the Saga pattern, whether the Saga pattern execution is successful or if any exceptions occur with this enhancement. The client application also will keep waiting for the overall status until either the Saga pattern is completely successful or completely rollback, the status will not be sent back to the client application. There is a very easy way to achieve this inside Axon framework. If you go to the Axon website and open the documentation under this developers, you will be landed on to this page. You can click on this Axon Framework Reference guide. And here you have all the good documentation around the Axon framework. Right now I'm looking for queries. If you click on this queries and select this query dispatchers, you'll be able to see Axon framework supports four different types of queries. Point to point queries is a normal query which we used in the previous sections. If you open any of the Query controller class like AccountsQueryController. Here we are trying to invoke the query() method using the queryGateway this query() method. It is going to invoke the query handler logic asynchronously, and when the response is ready, it is going to send in the form of AccountsDto. Since I want to wait for the response from the query handler, I am using this join() method. This join() method will force my Completablefuture to wait for the result to come and assign to these variable. Since the variable name is incorrect, let me correct this to account the same account I can try to mention here. So this is a normal point to point query, which is going to help you to query a single record. After the point to point query, there is a scatter gather queries. If you click on this it will take you to the documentation. These queries we need to use when we want responses from all of the query handlers matching your query message. In case if you have multiple query handlers for a query message, and if you want the responses from all such query handlers, then you can use the scatter gather queries. The name itself is self-explanatory. While querying, we are going to query multiple query handlers. That's what scatter indicates. Whereas the responses we are going to gather as a single response. That's what the gather word indicates. After Scatter gather we have subscription queries. This is what we are going to use in our scenario. This subscription query allows a client to get the initial state of the model it wants to query, and to stay up to date as the query view model changes. What this means is when we use the subscription queries, we are going to get an ability to wait for the results that are going to be emitted by the business logic. We can get an initial state response followed by we can keep getting the updated responses as well until the subscription query is completely executed. To implement the subscription queries, we need to use the queryUpdateEmitter. I'm going to show you the demo in a few minutes. And the last query type is streaming queries. This streaming query allows a client to stream large database result sets. The streaming query relies on the reactive streaming model, specifically the publisher type. Instead of loading and handling all the thousands of records at a time with the help of the streaming queries, we can try to stream the query results, just like how we stream the video from the YouTube. When we open a video inside YouTube, it is going to stream the video based upon where the cursor is present. Very similarly, the streaming queries is also going to work. For now, let's try to use this subscription query and implement the enhancement inside our Saga pattern. First, I need to go to the place where we are starting the Saga pattern flow. The Saga pattern flow is going to start inside the CustomerCommandController where we have this API here before I try to write the logic, first let me create an Query class under the customer microservice I can go to the query package. As of now there is a single Query class. Let me copy paste the same and rename this to findUpdateMobileSagaQuery(). In this Query class I don't want to maintain any fields. I want to keep it as empty because technically we're not going to query any database to fetch the results. We simply want to know the overall status of the Saga pattern flow. That's why I'm trying to have this query as empty. Next, I can go to the CustomerCommandController here just before dispatching the command using the command gateway what I'll do is, I'll try to invoke a query, but to invoke a query we need to inject the query gateway here. Let me duplicate this line of code and replace this CommandGatewayWithQueryGateway. Let me update the variable name as well. With the help of this queryGateway variable, I'm going to invoke the subscriptionQuery() method to this subscriptQuery() method first, we need to provide the object of the Query class that we have created just now. The query class that we have created is findUpdateMobileSagaQuery. This is the very first parameter, followed by inside the second parameter. We need to tell what is the return data type that we are expecting as part of the initial state response. Like we discussed previously, the subscriptionQuery is capable of sharing the initial state output followed by the further updates. Under this second parameter, I can mention the return data type that I'm expecting under the initial state response. Let me use response Types class, and using this class I'm going to invoke instance of followed by. I will mention the return data type that I'm expecting, which is ResponseDto.class. This is the initial state response that I'm expecting. Similarly, I can mention that what is a return data type that I'm expecting for the subsequent updates that I'm going to receive from the subscription query. For the subsequent updates also, I'm going to expect the same data type. That's why I'm trying to mention the second parameter also of type ResponseDto.class. Towards the end, we can mention the semicolon. If you open the SubscriptionQuery, you will get to know the details about this method. The very first param indicates the type of the query, and the second param indicates the type of the initial response followed by the third param indicates the type of the incremental update that we are going to receive. I hope you are clear what we have done so far. Now on the left hand side, I need to catch the output with the help of SubscriptionQuery result. Since we are expecting two types of updates. One is the initial update and the other one is the incremental updates. We need to mention what is the types that we are expecting as a return type from the SubscriptionQuery. Here we need to mention ResponseDto only because on the right hand side we have mentioned ResponseDto as the second parameter and third parameter, followed by let me mention the variable name as query result. Now using this query result variable, I can do the magic. Towards the end of this method, I'm trying to return a new ResponseDto object. Instead of this what I can do is, to this body method, I can pass the output that I'm going to get from the query result. Let me use the query result and let me invoke the method which is updates(). After this updates() method, I'm going to invoke the block first command. With this block first command what is going to happen is, the thread that is executing this method it is going to wait until it receives the first response of type ResponseDto. If needed, we can also invoke the block first command that accept duration as an input, which means your thread is going to wait only for the duration that you have provided in my scenario. I want to wait indefinitely that's why I'm trying to invoke the plane() method. As a developer, I need to make sure I am closing the connection of the query result as well before I try to exit from this method. So to close the resource, what I can do is, I can try to use try with Resources block. And inside this try block I can paste all this code and I can move this entire code into the try block. So with this what is going to happen is, whenever my try block execution is completed, the query result resource is automatically going to be closed. So far we have just created a subscriptionQuery, which is going to wait for the very first update. As a next step, we need to publish our emit and first update towards the end of the Saga flow so that we can send the same response to the client application. Let's try to continue this in the next lecture as well. Thank you, bye.
