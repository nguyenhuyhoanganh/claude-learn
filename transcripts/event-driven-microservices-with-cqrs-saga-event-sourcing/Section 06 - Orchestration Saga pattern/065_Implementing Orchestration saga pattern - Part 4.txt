# Implementing Orchestration saga pattern - Part 4

Let's define the business logic inside this new method that we have created. The business logic is going to be very similar to what we have here. What we can do is, we can try to copy this entire business logic and paste it here, followed by I'm going to correct this logger statement with a new value, which is Saga event two received account mobile number updated event for the account number. After this logger statement, we need to write the business logic to dispatch the request for that to the next microservice, which is the cards microservice. That's why here I need to create the object of updateCardMobileNumber command. Using the builder() method of this class, I'm going to populate all the required data like accountNumber, cardNumber, loanNumber, customerId, mobileNumber and newMobileNumber followed by here we need to update this command name to updateCard, mobileNumber command, and inside this command callback logic, we need to trigger the rollback command on the accounts microservice. In case if there are any issues on the cards microservice, it has to trigger the compensation transaction on the previous microservice, which is the accounts microservice. That's why here I'm going to create the object of rollback accountMobileNumber command. Using the builder() method I can populate all the details inside the rollbackAccountMobileNumber command object. Apart from the customerId, we should also populate the account number as well. If you go and check the RollbackAccountMobileNumber command class here account number also is required for the account microservice to rollback both in the read and write database. That's why just above the customerId I'm going to populate accountNumber by invoking getAccountNumber() method. Let me fix this error. So this line is going to dispatch the compensation transaction on the account microservice. As of now we have not written any business logic around the compensation microservice we are going to do that in the coming lectures. For now, let's focus on the happy flow alone. This method is going to dispatch the command to the cards microservice. That's why we need to go to the CradAggregate class. And here we need to copy these two methods and paste it again. This time the command that it is going to handle is updateCardMobileNumber command. Let me mention the same here and update the variable name followed by I'm going to create the object of event which is cardMobileNumberUpdatedEvent. Let's mention the same on the right hand side and update the variable name as well. These two variables I'm going to pass as an input to the copyProperties() method. And eventually I'm going to dispatch the event by invoking this apply() method on the event sourcing handler we need to handle this event cardMobileNumberUpdatedEvent using the same we're going to update the mobileNumber inside the write database of the cards microservice. So using this event I need to invoke getNewMobileNumber(). Next we need to go to the CardProjection class. Inside this class we can copy this method and paste it again. This time the event it is going to handle is cardMobileNumberUpdatedEvent. Using this event only I'm going to pass the both the old mobileNumber and newMobileNumber to the new method that we are going to create inside the CardServiceImpl class. First let's go to ICardService and here towards the end we can copy the new abstract method just like how we have done inside the IAccountService. I can copy this entire method, but before that, let me update this comments here seems I have the copy paste issue. Now I can go to ICardService and paste the abstract method. And here instead of customer I'm going to mention card after this I'll go to the CardProjection. and here I'm going to invoke the method updateMobileNumber. To this method I'm going to pass old mobileNumber followed by newMobileNumber. Next we can open the CardServiceImpl. And here we can try to implement the new method that we have created inside this method let me paste some business logic with this logic we are going to update the newMobileNumber inside the read database of the card microservice. Now we have written all the logic on the cards microservice to update the data in both databases. As a next step, we need to go to the SagaManager class, and here we need to create the new method again, which is going to handle the events that is going to be dispatched from the CardAggregate class. So let me paste the same method. And this time I'm going to handle the event which is cardMobileNumberUpdatedEvent. Let me update the log statement to SagaEvent3 receivedCard, mobileNumber, updated event for the card number. event.get.cardNumber. Next, I need to forward the request to the next microservice, which is the loans microservice, which is the last microservice. That's why here I need to create the object of updateLoanMobileNumber command. Using the same I'm going to invoke the builder() method, and once all the details are populated, I'll try to mention the same command name here inside the command callback implementation. In case of any exception, we want to trigger the rollback or compensation transaction on the card microservice. That's why I need to create the object of RollbackCardMobileNumber command. Let's mention the same command name class on the right hand side. And with that you will see all the compilation issues are resolved. Let me update the variable name to RollbackCardMobileNumber command the same I'll try to pass to the send and wait method inside this command class apart from accountNumber and customerId, we should also pass the cardNumber. That's why what I'll do is, just double the accountNumber I'm going to populate the cardNumber by invoking the getCardNumber() method from this new method what is going to happen a new command will be dispatched to the loan microservice. Inside the loans microservice, we need to handle this command by going to the LoanAggregate. You know the story first we need to copy these two methods and paste it again followed by we need to copy the command name. The command name is updateLoneMobileNumber command. Let me update the variable name to updateLoneMobileNumber command the same variable name I'm going to use here and the event that we need to create is loneMobileNumberUpdatedEvent. The same event object I need to pass to the copyProperties() method. First let me correct the variable name to lone mobileNumberUpdatedEvent() the same variable name I can try to use in these places. With this code we are going to dispatch a new event from this command handler. The same event needs to be handled here inside the event sourcing handler method. Let me mention the correct event type with the variable name as loneMobileNumberUpdatedEvent. Inside this method I'm going to invoke this.mobileNumber is equal to using this variable name I'm going to invoke getNewMobileNumber(). Next we need to go to the Projection class of loan here what I can do is, I'll go to the CardsProjection and copy this last method and paste it here. Instead of ICardService, we need to invoke ILoanService. And the event that we need to handle is loanMobileNumberUpdatedEvent. Let's try to create a new abstract method inside the ILoanService. First I'll go to ICardService and copy this abstract method, followed by I'll open ILoanService. And here towards the end I'm going to paste the abstract method. Next I will update these comments. As a next step we can go to LoansServiceImpl and implement the method that we have added. Let's copy the business logic from the CardServiceImpl. So this is the logic, the same logic we can paste it  here. Once the logic is pasted, we need to use LoansRepository in all the places followed by instead of cards we need to use loans. Let me update the variable name to loans. The same loans variable name I'm going to pass to the save() method. So here what I can do is, I can invoke the LoansConstants. And here also I'll update to loan. And with that we should be good with all the logic that we have written so far the newMobileNumber is going to be updated inside the loans databases. Next we need to go to the Saga class. And here we need to handle the same event that is being published by the loans microservice. What I can do is, I'll try to copy this method and paste it here this time I'm not going to copy the entire method because there is a reason the event that we need to listen here is loanMobileNumberUpdatedEvent. This is the last event that is going to happen inside my Saga pattern after the launch microservice. There is no other microservice to forward the request. That's why I'm not going to dispatch any command. Simply, I'm going to add a log statement which conveys that the Saga event for triggered, which is the end of the Saga, since this is a method which is going to handle the end of the Saga, we need to mention an annotation which is @EndSaga. If required, you can dispatch another event or command from this method based upon your business request. But for now we should be good. As of now, we have written all the logic related to the happy path scenario. Even though we are trying to dispatch the rollback commands. Nowhere we have handled these commands and nowhere we have written any logic. What should happen when someone dispatches this command? Let's try to write the logic related to the compensation transactions from the next lecture. Thank you, bye.
