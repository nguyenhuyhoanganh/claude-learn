# Implementing Orchestration saga pattern - Part 2

We have created all the required command classes and event classes inside the common project. As a next step, we need to create an Rest API that is going to accept the request from the client application to update the mobile number inside all the microservices. To create these new Rest API, we need to go to the customer microservice, because customer microservice is the very first microservice, which is going to accept the request and hand over the response of maintaining the Saga pattern to the Saga manager. Inside the customer microservice, we have implemented all the Rest APIs using the command and query style. So there is a controller with the name CustomerCommandController, where we have created all the rest APIs that are going to change the data. Since updateMobileNumber is also going to change the data inside the same controller class, we need to create a new Rest API that is of type patch mapping with the /mobile-number. Next, let me create the method below this annotation, which is public @ResponseEntity that returns response data as an response method name I'm going to keep it as updateMobileNumber(). And to this method we are going to accept a @RequestBody of type MobileNumberUpdateDto using the data present inside this MobileNumberUpdateDto we need to create the object of updateCustomerMobileNumber command. Let me create the variable name as updateCustomerMobileNumber command. And inside this class there is a builder method. Using the same I'm going to populate all the data. First I'm going to populate the customerId followed by I can populate accountNumber, loanNumber and cardNumber. So first let me populate account number followed by I can populate loan number. After the loan number I can populate cardNumber. And into this cardNumber I can save the data that I can get by invoking the getCardNumber() method. After the cardNumber, I can try to populate the mobileNumber. This mobileNumber data I can get by using the method getCurrentMobileNumber() followed by I'm going to populate newMobileNumber by invoking the method getNewMobileNumber(). And towards the end I need to invoke the build method which is going to create the object of updateCustomerMobileNumber command. Just like how we are dispatching the command here, very similarly, we are going to dispatch our command as well. Let me copy this code and paste it here. I'm going to dispatch the command of type updateCustomerMobileNumberCommand. When a command is dispatched where do we need to handle it. We need to handle it inside the Aggregate class. So let's go to the CustomerAggregate class and here inside this class, what I can do is, I can try to copy the command handler method and event sourcing handler methods and paste it here in this new method instead of handling the delete customer command, I need to handle the command that I'm trying to dispatch here, let me copy this command and paste it here. And let me update the variable name to updateCustomerMobileNumberCommand using the same command. I'm going to prepare a new event which is customerMobileNumberUpdatedEvent. Let me paste the same on the right hand side and update this variable name to customerMobileNumberUpdatedEvent. Let me copy this event object reference to these copyProperties() method. And at last I can try to publish the event by invoking these AggregateLifecycle.apply() method. On the event sourcing handler side I need to handle the event of type customerMobileNumberUpdatedEvent. From this event, I need to populate the latest mobile number by invoking the getter method, which is get newMobileNumber. This event sourcing handler method is going to take care of updating the newMobileNumber inside the write database. Apart from updating the data inside the write database, we also want to update inside the read database as well. We have the logic of updating inside the read database inside the Projection class. Let me open CustomerProjection here just like how we have these event handler methods. Very similarly I'm going to create one more event handler method which is going to handle the event of type customerMobileNumberUpdatedEvent. Once I accepted this event inside this method, I need to invoke the method available inside the ICustomerService. But as of now, I don't have any method which is going to support updating the mobile number. That's why I'll try to open the ICustomerService and create a new abstract method with the name updateMobileNumber. It is going to accept the old mobile number and the new mobile number as method input parameters. Let's try to invoke the same inside this event handler method. Let me invoke updateMobileNumber() method and to this method I can pass the old mobile number followed by the newMobileNumber. Once we invoke this method, we need to go to the CustomerServiceImpl to make sure we are implementing the new abstract method that we have introduced. Inside this method, I'm going to write some simple business logic. As you can see here, with the help of findByMobileNumber and activeSw, I'm trying to verify if there is an existing customer, otherwise I'm going to throw an ResourceNotFoundException if there is a existing customer using the same. I'm trying to invoke the setMobileNumber to which I'm trying to pass new mobile number, and eventually I'm going to invoke the save() method. With this code changes, we are going to save the new mobile number on the read database as well. This completes the functionality and the customer microservice. To accept the request, convert the request into command using the command it is going to create an event. Using the event it is going to store the data both in read and write database. Once these actions are completed, we want customer microservice to handle the execution of the Saga pattern to the Saga manager to take our request further into a Saga Manager first, we need to create a class that is going to act as a Saga manager to the same class we can let the request forwarded to take care and implement the Saga pattern inside the customer microservice. I can create a new package with the name Saga. Inside the Saga package, I'm going to create a new Java class with the name updateMobileNumberSaga. On top of this class I'm going to mention @Saga annotation. This is an annotation from the Axon framework. If you can open this annotation and click on this download sources, you will be able to read the documentation around this annotation. As you can see, the documentation confirms this is an annotation that informs accents auto configure for spring that a given component is a saga instance by mentioning this annotation, we are letting the Axon framework that this class is going to act as a saga manager. After the @Saga annotation, I'm also going to mention a SLF4J annotation. Inside this class, I'm going to create a new method, which is going to handle the event that we are trying to publish from the CustomerProjection class. If you go to the CustomerProjection class, the event that is going to be published is of type customer mobileNumberUpdatedEvent. The same event I want to handle inside my Saga manager as well. Just like how we are trying to mention an @EventHandler annotation on top of these methods. Very similarly here also I need to mention an annotation which is specific to the Saga event handling mechanism. The annotation is @SagaEventHandler. If you can open the @SagaEventHandler annotation, you'll be able to find all the details related to this annotation. This is a method level annotation indicating that the annotated method is an event handler method for the Saga instance. The first parameter is always the payload of the event message. That's what we are trying to accept here. If you have any questions, you can read these documentation. As a next step, we need to populate a property value which is an association property. To this association property I'm going to pass customerId. If you try to read the documentation of this property, it is conveying that this is a property in the event handler that will provide the value to find the Saga instance. Typically, this value is an aggregate identifier of an aggregate that is specific Saga monitors. Basically, as part of your saga, you're going to trigger and execute and handle lot many events to give a clue to your Saga manager to find the Saga instance under which this event needs to be handled. We need to populate this association property. Here I'm trying to use the customerId as the association property because it is unique for each customer. The same association property with the same value we need to use in all the other events that we are going to handle, with the help of Saga event handler. Then only all the events that are going to be processed under a single Saga instance Otherwise the Saga manager will assume they belongs to different Saga instances. I hope you are clear with all the changes that we have done so far. As of now, we build an rest API inside the customer microservice, which is going to publish an event of type customerMobileNumberUpdatedEvent. If you click on this icon, you will be able to see the event is being handled in three places. The very first one is event sourcing handler, which is going to take care of updating the data inside the write database, followed by CustomerProjection, which is going to take care of updating the data inside the database. And at last we have the Saga, which is going to take care of forwarding this request to the next microservice inside the Saga pattern. From the next lecture, let's try to write business logic inside this method. Thank you, bye.
