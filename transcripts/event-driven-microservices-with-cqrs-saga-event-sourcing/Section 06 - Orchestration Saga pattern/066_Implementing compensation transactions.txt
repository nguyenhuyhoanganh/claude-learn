# Implementing compensation transactions

As of now we are only dispatching the rollback commands, but we have not handled them. Let's try to handle these rollback commands as well, so that we are going to handle the negative scenarios also, inside our Saga pattern. We can come in the reverse order while the updates are happening on the loans microservice if there is an runtime exception, then this logic is going to be executed, which is going to trigger the command rollbackCardMobileNumber command. So this command needs to be handled inside the cards microservice. Let me go to CardAggregate class. Inside this Aggregate class I'm going to copy these two methods and paste it here. The command that we need to handle is rollbackCardMobileNumber command. Let me update the variable name as well. While we are handling this command the event that we need to trigger is cardMobileNumberRollbackEvent. . Let me paste the same on the right hand side and update this variable name once the variable name is updated, we can resolve the compilation errors by correcting the object reference names. Towards the end inside these event sourcing handler, we need to handle the event which is cardMobileNumberRollbackEvent. . In this method we need to write some tricky logic. First we need to update the mobile number, but which mobile number we need to update? We need to update the old mobile number, not the new mobile number because we are trying to rollback using the compensation transaction. That's why here instead of get new mobile number, I need to invoke getMobileNumber() followed by I also want to populate the error message inside the write database to understand what is the error that is faced. What I can do is, I can go to the top of this class and I'll try to create one more new field, which is private String errorMsg this field value I'm going to populate here by invoking this.errorMsg is equal to cardMobileNumberRollbackEvent.getErrorMsg(). If you can recall inside the Saga, we're trying to populate this error message with the help of this command commandResultMessage.exceptionResult(). getMessage() the same we are trying to set into the write database. So this is going to take care of reverting the changes on the card write database. As a next step, we need to go to the CardProjection class inside the Projection class we can copy this method one more time. And this time the event that I want to handle is, CardMobileNumberRollbackEvent. From this method we can invoke the updateMobileNumber. But while we are trying to invoke this updateMobileNumber, we need to swap the details. So first we need to populate the newMobileNumber which is already saved inside the database. And this newMobileNumber we want to override with the old mobile number. That's why I'm trying to pass getMobile number as a second parameter. I hope you are clear. There is no confusion with the logic that we have written so far inside the card microservice the compensation transaction is going to be executed. Once the compensation transaction on the cards microservice is completed, we need to trigger the compensation transaction on the account microservice. That's why to make it happen, we need to go to the Saga manager here I need to create a new Saga event handler method, which is going to handle the event of type cardMobileNumberRollback event inside this method. First let me mention the correct log statement which Saga compensation event received cardMobileNumberRollbackEvent for the card number these are just log statements which are going to help in my debugging. As part of the Saga event handler what we want to do, we want to trigger the next compensation transaction by creating the command object of . RollbackAccountMobileNumberCommand. So let me populate the data into this object by using the builder method. Once the builder() method is invoked, I'm going to populate the account number by reading the details from the event. After the account number, I want to populate customerId by invoking getCustomerId() followed by let me populate mobile number by invoking getMobileNumber(), followed by new mobile number by invoking getNewMobileNumber() method. And towards the end I can populate error message field as well by invoking event.getErrorMsg. And towards the end I am going to invoke the build() method. This command object we want to dispatch with the help of Command Gateway, and by using sendAndWait() or send() method, we can use any method. So here I'm trying to use a simple send() method, which is going to accept only the command object as an input. This piece of code, it is going to dispatch a command to the account microservice. As a next step we need to go to the AccountsAggregate class here what I can do is, I can copy the code from the CardAggregate since we have the similar logic, I'm going to copy this code and paste it here inside the AccountsAggregate the command that we need to handle here is rollbackAccountMobileNumberCommand. Let me update the variable names followed by the event that we need to dispatch is accountMobileNumberRollBackEvent. . The same I'm trying to mention on the right hand side. Let me update the variable name the correct variable names I'm trying to update to resolve these compilation issues, followed by inside the event sourcing handler method we need to catch the correct event which is accountMobileNumberRollbackEvent followed by using the same event object I'm going to read the getMobileNumber() and getErrorMsg(). and since this error message field is not present here, we need to create the same. Let me type private String errorMsg. Next we can go to AccountProjection here what we can do is, we can try to copy this piece of code. And this time the event that we are going to handle is accountMobileNumberRollbackEvent. Let me update the variable name to accountMobileNumberRollback event using the same variable name I'm going to first pass the getNewMobileNumber, followed by I'm going to pass old mobile number. With the help of getMobileNumber(). We should always reverse these parameters in the actual successful event. We need to pass getMobileNumber as the very first parameter and the new mobile number as the second parameter. Whereas in the rollback or in the compensation scenario, we need to swap them because we are trying to revert the changes. Now let's go to the Saga manager. And here this time I'm going to copy this entire method and paste it here the event that we need to handle is accountMobileNumberRollbackEvent. Let me update the log statement as well for account number which is event.getAccountNumber() Inside this new method we need to dispatch the command which is rollbackCustomerMobileNumberCommand. Because the previous microservice for the account microservice is customer microservice that's why we need to trigger this command. Let me update the variable name followed by we can try to resolve this compilation errors. First let me delete the account number related logic because we no more needed these. Next we need to pass these rollbackCustomerMobileNumber command object as an input. Once the logic of dispatching this command is completed, we need to go to the CustomerAggregate class. And here we need to paste two new methods. I'll try to get the logic from the Aggregate class of account microservice. Let me copy these two methods and paste it inside the CustomerAggregate class. Once these methods are pasted, we need to update some logic here. The command that we need to handle is rollbackCustomerMobileNumber command and the event that we need to trigger is customerMobileNumberRollbackEvent. Let me mention the same on the right hand side and update this variable name followed by I'm going to resolve these compilation errors. First let me mention the command name followed by event name once this method is updated under the event sourcing handler, we need to handle the customerMobileNumberRollbackEvent. From this event, we need to read the mobileNumber andErrorMsg. Let's go to the top of this class. And here I'm going to type private String errorMsg. Next we need to go to CustomerProjection. And here also we can copy this method and paste again the event that we want to handle is customerMobileNumberRollbackEvent. Using this event we need to first pass the new mobileNumber followed by old mobileNumber with the help of getMobileNumber() method. All this logic is going to take care of roll backing the data on the customer microservice. As a next step, we need to go to the Saga class and here we need to handle a new event which is customerMobileNumberRollbackEvent. Inside this method, we don't have to dispatch any other command, because customer microservice is the last microservice where we need to execute the compensation transaction. That's why I'm going to get rid of all this logic. Instead, I'm going to mention a simple log statement conveying saga compensation event and received customerMobileNumberRollbackEvent for the customerId and since this method is going to handle the last Saga event handler during the exception flow, we need to mention @EndSaga on top of this method as well. Always, you're going to have two methods with @EndSaga annotation. This method is going to act as the @EndSaga in the exception scenario, whereas the other method that we have is going to act as a EndSaga in the happy path scenario, I hope we made all the required changes. Let's try to test these changes in the next lecture. Thank you, bye.
