# Implementing Orchestration saga pattern - Part 3

Before we try to write the logic inside this method, first we need to make sure we are mentioning an annotation on top of this method, which is @StartSaga. Since this is the very first Saga event handler inside my Saga pattern, I need to mention this annotation so that the framework has a clue. What is the very first event handler that is going to start the Saga? And similarly, we should also mention end Saga annotation on top of the method that is going to handle the last event inside the Saga pattern. Once we mention this annotation, let me define a logger statement inside the handle() method. With the help of this logger statement, I'm trying to log a statement Saga event to one start. Received a CustomerMobileNumberUpdatedEvent for the customerId. I'm trying to add this log statements for my debugging purpose from this event handler. What we want to do. We want to hand over the request to the next microservice, which is  account. microservice. Inside the CQRS pattern, anytime you want to trigger an operation that is going to change the data, you need to do it with the help of command classes. That's why here I'm going to create an object of updateAccountMobileNumber command and the right hand side using the builder method. I'm going to populate all the required details. First let me populate the accountNumber. This accountNumber details I'll be having inside the event, followed by I want to populate other details. The next detail that I want to populate is cardNumber. After the cardNumber, I'm going to populate loan number. Once the loan number details are populated, let's try to populate the customerId. Once the customerId is populated, we can try to invoke the mobileNumber. This mobileNumber indicates the currentMobileNumber. That's why I need to use the getMobileNumber(), followed by I'm going to populate new mobileNumber field by invoking getNewMobileNumber() method. And towards the end I need to invoke the build() method. Once the command object is ready, with the help of Command Gateway, we can dispatch the command to use the command gateway first, we need to inject the same as a dependency into this class. Let me mention Private Transient Command gateway. On top of this line I'm going to mention @Autowired annotation here I'm trying to use field injection because my Axon framework, it needs an empty default constructor to make the Saga work. That's why here I'm trying to use the field level Injection. And the reason I'm trying to use transient is I don't want this object to participate inside the serialization that is going to happen between the multiple microservices while we are trying to transfer the events and commands among the multiple services using these command gateway I want to dispatch the command to the accounts microservice. As of now inside the course we are leveraging the send()and wait() method to the send() and wait() method. I'm trying to pass a command object with this what is going to happen is the command will be dispatched to the accounts microservice. But apart from dispatching the command to the accounts microservice, in the case of any exception, I want to trigger the compensation transaction on the customer microservice in case of any runtime exception to have that control, what I'm going to do is, instead of invoking the send() and wait(), I'm going to invoke the send() method, which is going to accept the command object as the first parameter and as a second parameter I can implement the callback logic that is going to be triggered once the command is executed or processed successfully on the accounts microservice. So let me invoke this third send() method. First I'm going to provide command as the very first parameter. As a second parameter, I need to implement the business logic that needs to be executed as part of the callback mechanism. That's why here I'm going to type new command call back with empty generics followed by empty parentheses and curly braces. This is an interface inside this interface there is an abstract method with the name on result the same we need to implement inside our Saga class. Let me go there and here if I hover on this error, it is giving me recommendation to implement methods. Let me click on the same and let's select on result and click on this okay now we can see as part of the second parameter we have complete anonymous class implementation. Whatever logic that we are going to write inside this on result() method will be executed once the command is executed successfully. Let me bring the second parameter of the on result to the next line, so that you can see the complete code. Now let's imagine what logic we need to write inside this method. Inside this method, we need to write a logic to trigger a compensation transaction whenever an runtime exception happened on the accounts microservice because we are trying to dispatch the updateAccountMobileNumber() command using the send() method. Once this command is processed and the account microservice, this command callback implementation is going to be executed. To know whether my command is processed successfully or not, I can try to use this command result message using this command result message I can try to write a if block inside this object of command result message. There is a method which is isExceptional(). So it is going to give a boolean whether an exception happened or not. If there is an exception happen we want to trigger the compensation transaction. Otherwise everyone is happy we don't want to trigger any compensation transaction. What is the compensation transaction that we want to trigger here We want to trigger the compensation transaction on the customer microservice. So there is a command with the name rollbackCustomerMobileNumberCommand. we need to create the object of the same by using the builder() method, and after the builder() method, we need to populate customerId by reading the details from the event object with the help of getCustomerId() after the customerId, we also want to populate the mobileNumber and the newMobileNumber. Let me invoke mobileNumber() method and populate the data by invoking getMobileNumber(). Now I'm going to populate newMobilNumber field by invoking getNewMobileNumber() method. And towards the end I also want to populate error message so that these error message details also we can store inside the write database. So how to know the error message. With the help of these same command result message, we can invoke the method exception result followed by getMessage() which is going to give you the exception details. And towards the end I need to invoke the build() method. So now I have the command ready, which is going to take care of triggering the compensation transaction and the customer microservice. So this command I can try to dispatch with the help of CommandGateway.send() and wait(). I hope you are clear with all the logic that we have written. So as of now, we have dispatched the command to the account microservice. If there is an exception arises, we want to trigger this rollback command on the customer microservice. As a next step, we need to go to the account microservice and handle these command that we are trying to publish. So let's go to the AccountAggregate. And here we need to define a new command handler and event sourcing handler. What I can do is, I can try to copy the code and paste it here. The command that we want to handle is the same command that we are trying to dispatch. updateAccountMobileNumber command. Let me handle the same here with the parameter name as updateAccountMobileNumber command as part of this command, I want to dispatch a new event of type accountMobileNumberUpdatedEvent. So let me mention the same on the right hand side and update the variable name to accountMobileNumberUpdatedEvent. Let's try to fix this compilation errors by mentioning the correct object references. And towards the end we want to publish the accountMobileNumberUpdatedEvent. The same event we want to catch here with the help of event sourcing handler. Let me mention the same here, followed by I'm going to mention the variable name as accountMobileNumberUpdatedEvent using the same I want to update the mobile number by invoking the method getNewMobileNumber(). The similar logic we have written inside the CustomerAggregate as well. If you go here, we have the very similar logic. Next, we need to open the Projection class of Account. So let me open the AccountsProjection, which is going to take care of updating the data inside the read database here let's try to copy this piece of code. Maybe what we can do is, we can try to copy the code from the CustomerProjection. That is going to make more sense because we are going to write very similar code. Let me copy this entire code and paste it here. Now the event that we want to listen is accountMobileNumberUpdatedEvent. Using the same event, we want to pass both the old mobileNumber and newMobileNumber to the method inside the IAccountService. So let's go to IAccountService. Inside the IAccountService we need to create the method which is very similar to ICustomerService. I can copy this entire method and paste it here. Next I can go to the AccountServiceImpl. And here I can try to implement the new abstract method. Inside this empty method, let me paste the business logic. As you can see here, we are trying to update the new mobile number into the read database as well. So all these logic inside the Projection and Aggregate class of account microservice. It is going to take care of updating the newMobileNumber inside the accounts read and write to database. As a next step what we have to do. We need to go to the SagaManager class. And just like how we are listening to the event here, very similarly, we need to create a new method again, which is going to listen to the event that we are going to publish from the account to microservice. If you go to the accounts aggregate, we are trying to publish this event, the same event we need to listen here. Now inside this method, we need to write the very similar logic to forward the request to the next microservice, which is the card microservice. And in the case of any exceptions, we need to trigger the compensation transaction as well. I hope you are with me with all the changes that we have done so far. Let's continue these changes in the next lecture as well. Thank you, bye.
