# Demo of Subscription queries - Part 2

Let's go to our Saga manager class let me type updateMobileNumberSaga. Inside this Saga manager class, there are only two methods that has @EndSaga annotation. This @EndSaga annotation indicates it is going to end the saga flow. These methods are the right places to emit the overall status to the client application. That's why we are going to make changes inside these methods. Before we try to make the changes. First, we need to make sure we are injecting a bean of type @QueryUpdateEmitter. Let me copy these lines of code and paste it here and here. The bean that I want to inject is of type QueryUpdateEmitter. Let me update the variable name to queryUpdateEmitter. Using this queryUpdateEmitter I can emit the response to the subscription query. Let's go to the method where we have the @EndSaga annotation. So this is the method which will be executed in the scenario of Happy Path. If all your transactions inside your Saga pattern are successful, then only this event is going to be emitted. After this log, with the help of queryUpdateEmitter variable, I can invoke the emit() method to this emit() method first we need to pass the what is the query class name? The query class name that we have used while writing the subscription query is findUpdateMobileSagaQuery. The same class name we need to mention here by just typing findUpdateMobileSagaQuery.class and the second parameter we need to pass a predicate, which is going to be executed on the query payload that we are trying to send from the subscription query. If you go to my controller class here, we're trying to send the payload of type findUpdateMobileSagaQuery. . If you have a requirement to send some payload data, you can pass some data inside the object of this query by using the predicate() function, we can apply some filtering conditions and the query object that we are receiving as part of the subscription query. In my scenario, since I'm getting an empty object, what I can do is, I'll try to write a simple predicate condition which is always going to return true. And under the third parameter, we need to emit the response that we want to send to the subscription query. The response type should be of type ResponseDto. That's why here what I'm going to do is, I'll try to create the object of new ResponseDto to the constructor of this class, I can pass two constants from the CustomerConstants class. The very first constant is status 200, indicating the overall Saga pattern is successful. Very similarly, I can pass one more constant conveying the overall message, but I don't have any constant as of now conveying the overall saga pattern status. What I can do is, I'll try to create two new constants here. The very first one is Mobile Update success message, which is going to have the overall successful message. Similarly, the other constant mobile upload failure message, it is going to have the message conveying that the message number update is failed in all services. Let me use these constants inside my SagaManager class. The second constant that I want to send here is CustomerConstants.mobile update success message. that's it. With these changes, whenever these end Saga event handler is being executed, we are going to emit a response to the subscription query that is waiting for my response inside the CustomerCommandController. Since here we are using the block first method, the thread is going to wait until I emit the first response using the queryUpdateEmitter(), just like how we are emitting the response in the scenario of happy path. Very similarly, we need to emit a response in the scenario of failure as well. That's why let's go to the other message where we are trying to end the Saga flow. This is the method which is going to be invoked once all the compensation transactions are executed. I can paste the same code, but here, instead of sending that 200 status, I'm going to send 500 status with the message as mobile update failure message. With these changes now my client application is going to wait for the overall status. If you go to the Controller class. As of now, there is a catch that we need to be aware whatever overall status that we have developed so far with the help of subscription query, it will only work in the scenarios where the Saga flow is triggered using the SagaManager, you need to think about what is going to happen if there is a runtime exception happens inside my CustomerAggregate class itself while processing these updateCustomerNumberCommand. The runtime exception can happen in this layer as well before it hand over the request to the Saga manager to handle this scenario what we can do is, we can try to write the very similar logic that we have written inside the SagaManager. So here, if you go to the top of the class, whenever we are dispatching a command, we are using this kind of logic where we are checking if there is an exception happened during the command processing. If yes, we are trying to execute some logic. Very similarly, I want to implement inside my controller class as well. That's why let me copy this entire code and paste it inside my controller class here just inside my try block I'm going to paste it. Obviously we are going to get some compilation errors. The command name should be updateCustomerMobileNumberCommand. And here also we need to update the correct command class name followed by we don't want to rollback anything here because there is nothing to roll back. That's why what I want to do inside the if block is, I'm going to return the overall response indicating internal server error. If you see here with the help of ResponseEntity, I'm trying to populate the status and the body under the body I'm trying to send the ResponseDto object with the status code as 500, and the message I'm going to send whatever message that I'm going to receive from the exception result of the command result message. This way, the client application is always going to have a proper response. After making all these changes towards the end, we can get rid of these duplicate line of code. Let me save these changes and do the build. Once the build is completed. Behind the scenes, I'm going to restart all my microservices just to make sure that the subscription query results are loaded properly. I restarted all my applications. Now we can go to the postman and try to test the happy flow and the negative flow. First, let me create a brand new data with the mobileNumber that is ending with 7620. First let me create the customer followed by I'm going to create the account. After the account, I'm going to create the loan. After the loan, I'm going to create the card details as well. If I try to invoke these gateway summary API with the 7620 mobile number, you can see I'm getting successful response. As a next step, I'm going to open this orchestration mobile number request and populate these details behind the scenes I have populated all these details. As of now, the current mobile number is 620. We want to update the same with the new mobile number that ends with 621. Let me click on the send button and see what is going to happen. As soon as I click on the send button I'm getting an error. Let me check if the update() operation is successful or not. I'll take this newMobileNumber and invoke the fetchcustomer summary API, since the mobile number is updated in all the APIs. But I'm not getting proper response, I'm getting an response with the error message canceled AXONIQ - 500. Let me see what is the problem? Based upon my analysis, the problem is arising because we are trying to use a query class which does not have any fields. I'm not sure if this is a bug with the AXON IQ framework, or if it is done intentionally to fix this issue what I'm going to do is, I'll try to use some other query class. For example, I'll try to use FindCustomerQuery. This has a field with the name mobileNumber, and for this query we also have query handlers registered. I'm assuming this should fix the problem. Let me go to the Controller class. And here I'm going to create the object of FindCustomerQuery. Since the object requires some mobileNumber, what I'll do is,I'll try to populate the mobile number from the dto class that we have received. Let me format this and bring this parameter to the next line. Since we are using the FindCustomerQuery here, we need to make similar change inside the Saga class as well. Let's go to the SagaManager. And here we need to replace this class with FindCustomerQuery, followed by let me go to the other method here also, I'm going to update the same once I made the changes. I can do the build. Once the build is successful I'll go to the postman from the postman what I'll do is, I'll try to send the request one more time, but this time I need to switch these values. The current mobile number as of now it is 621 and the new mobile number I will move to 622. Let me send this request this time hooray! We are getting the overall message which is mobile number updation successful in all services. If I go ahead and invoke the fetchCustomerSummary with the help of 622 mobile number, I'm getting successful response. Let's try to test the negative scenario as well. To test the negative scenario, I need to open the LoanAggregate class here I'm going to throw the runtime exception intentionally. Let me do the build once the build is successful, I'll go to the Orchestration mobile number API here the current mobileNumber I'll update to 622, followed by the new mobileNumber. I will mention 623. If I click on the send button, you can see I'm getting a message saying that mobile number updation failed in all services. This confirms that the negative scenario is also working fine. We can go ahead and try to invoke the fetchCustomerSummary with this newMobileNumber. We should not get any successful response because the transactions are rolled back. If I go ahead and invoke with 622, I'm going to get successful response. I hope you're clear on how to use the subscription queries to know the overall status of the Saga pattern. Before I try to close this lecture, let me comment these runtime exceptions so that by the time I check in this code into the GitHub repo, you will not have any issues. Let me do the build. The build is successful. I'm assuming you are clear. Whatever we have discussed so far. Thank you, bye.
