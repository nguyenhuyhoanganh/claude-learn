# Implementing Orchestration saga pattern - Part 1

Since we are inside the new section inside my workspace, I'm going to create a new folder with the name section 6. Into this section6, I'm going to copy the code present inside the section3. The reason is very simple. Inside the section4 we have the code related to the materialized view pattern. I don't want to make things complicated by mixing all the patterns inside a single code base. That's why I'll try to take the code present inside the section3, where we have completed the CQRS and event sourcing. On top of this code, we are going to add the Saga pattern as well. Like I was saying at the starting of the course, CQRS, event sourcing and Saga pattern is a deadly combination which is being used by many of the organizations in the real applications. Let me copy all these projects and paste inside the section6 followed by I can try to open the section6 folder from my IntelliJ let me go to storages, workspaces, event driven microservice and select this section6. As soon as I click on this, open all the projects they are going to load into my IntelliJ. Let me click on this notifications and select this load, which is going to load all my projects as maven projects. Once the Maven projects are loaded, we can try to trigger a build. If there are no issues, we can continue to make the code changes. Let me delete this readme.md which is not related to this section, followed by I will open the pom.xml of the common project. Inside this file I'm going to add a new dependency, which is related to the validations, because I'm going to use some validations related annotations inside my common project. That's why I need to make sure I'm adding the Spring Boot Starter Validation project. Let me load the Maven changes and close this file, followed by I will go to the source folder and inside the source folder under the dto package, I'm going to create a new DTO class, which is going to be used as part of the updateMobileNumberRequest. The class name is MobileNumberUpdateDto. On top of this class let me mention @Data annotation. After mentioning the @Data annotation, let me paste a simple code. Hrere I'm trying to accept various fields as part of my updateMobileNumberRequest. Using currentMobileNumber, we want to accept the existing mobileNumber, whereas the new mobile number is the value which needs to be updated inside all the four microservices. Apart from these mobileNumber related fields, I'm also trying to accept customerId, accountNumber, loanNumber and cardNumber. There is a reason why I am trying to accept these fields. If you open any of the Aggregate class, for example inside the CustomerAggregate class, whenever we want to save the data inside the right database. By using event sourcing, it is going to expect the customerId, which is the aggregate identifier. Similarly, inside the loans, cards and accounts microservices, there are aggregate classes which are expecting the aggregate identifier to make the updates inside the event sourcing database. To accommodate the changes of the CQRS and event sourcing pattern. I'm trying to accept these four different fields from the client application itself. Anyway client applications they can trigger this API inside the Gateway server, which is going to give all the details of the customer loans, cards, and accounts. From these requests, they should be able to know these customerId, accountNumber, loanNumber and card number if client applications. If they are not able to send these details, that should be fine. We can run a query inside the CommandInterceptor classes to load these details based upon the current mobileNumber. Since I am looking to avoid unnecessary DB queries. I'm trying to accept these details from the client application itself. I hope you are clear. Now let me close this file and go to the package of Common Project here I'm going to create new package with the name com.eazybytes.common.event, followed by let me create one more package with the name command. After the command I'm going to create one more package with the name config inside the config we need to create a class with the name AxonConfig. Let me mention @Configuration annotation followed by I'm going to paste a method inside this class, which is going to return the bean of XStream. If you recall our discussion of materialized view pattern, we created a very similar method with this method, we are trying to avoid any security related exceptions that may arise while our event objects are being serialized from one application to another application. After making these changes, we need to make sure we are importing these AxonConfig in all the microservices. Let me go to customer application here. I can mention @import followed by AxonConfig.class let me mention the same inside accounts application. After the accounts application, let me open the cards application here as well I'm going to mention the same followed by loans application I'm going to open and paste the same code after config related changes. I'm going to create some command classes and event classes which are going to help me inside my Saga pattern. Let me create a new class inside the command package with the name updateCustomerMobileNumber command. Here I'm trying to use some short words. cus indicates customer, mob indicates mobile, num ndicates number. Once I created this class, I'm going to paste some code inside this code. As you can see from this command, we are going to accept customerId , accountNumber, loanNumber, cardNumber, mobileNumber and newMobileNumber. These fields are very similar to what we have inside the mobileNumberUpdateDto the same and equivalent fields I'm trying to maintain inside my command classes. After this command class let me create next command class with the name updateAccountMobileNum command inside this class also I'm going to paste same fields, but this time the target identifier will be present inside the accountNumber, whereas inside the previous command that we have created, the customerId will be the target identifier. Based upon to which microservice a given command class belongs accordingly, you need to mention the target identifier. Let me create the command class for the card microservice. The class name is updateCradMobileNum command. Here also I'm going to paste the code all this code you can get it from the GitHub repo. Since I don't want to waste time typing all this code, I'm trying to quickly paste the code, but I'm assuming it's a simple code, which we already discussed in the previous sections. Here, the target identifier is a card number. Now let's go ahead and create the next command class which is related to the loans microservice. The class name will be updateLoanMobileNum command inside this class also we are going to paste the code. And here the target identifier will be present on top of the loanNumber. Let me close all these classes. So as of now you can see we have the command classes which are going to take care of the update() operation. Very similarly we need the command classes which are going to take care of the rollback operation or the compensation operation. I hope you remember the CQRS pattern inside the CQRS pattern, everything is going to be triggered with the help of command from the command we are going to trigger the events. Once the events are processed, we are going to store the data inside the database. The very first rollback class that I'm going to create is, rollbackCustomerMobileNumber command. This command class is going to be used whenever someone wants to trigger the compensation transaction on the customer microservice. Here let's try to paste the code. You can see I am trying to use customerId, mobileNumber, newMobileNumber and error message indicating the reason on why the rollback operation has been triggered. If you see here I don't have any fields related to the loan number, card number and account number because my customer microservice never need them, as this is the last compensation transaction that is going to be invoked in the Saga pattern. I hope you're clear. If not, don't worry. When we create the other rollback command classes it is going to make sense to you. Let me try to create one more rollback class with the name rollbackAccountMobileNumber command. Here I'm going to paste a few lines of code inside this class. If you see I have the accountNumber and the customerId as well. The reason why I'm trying to maintain the customerId here is once the compensation transaction is executed inside the accounts microservice, it needs to trigger the compensation transaction present inside the customer service. Whenever we want to trigger a command on the customer service, the customerId is mandatory because it is the target aggregate identifier inside the customer event sourcing database. That's why the rollback of account to microservice, it should have the customerId as well along with the accountNumber. Next, let's try to create one more class with the name rollbackCardMobileNumber command. Inside this class also we're going to paste a few lines of code. As you can see here this class it is going to have cardNumber, accountNumber and customerId. From these Card microservice it is going to trigger the rollback of account to microservice. That's why it needs to pass both the accountNumber and the customerId, because the account microservice, it needs a customerId to trigger the rollback on the customer microservice. That's why we are trying to maintain these fields in this command class. Next, do you think we need to create a command class for the loans microservice? There is no need because loans microservice is the last microservice that is going to be executed inside our Saga pattern. There is no need of executing the compensation transaction separately because the loans microservice for any runtime exceptions, it can roll back the transactions in its own databases. That's why we are not going to create any rollback class for the loan microservice. Once the command classes are created, we need to create associated event classes. I hope you remember inside the CQRS pattern for every command class there should be an associated event class as well. That's what we have been following in all the other microservices. If you go and check the accounts microservice under the command package these are the commands. For these commands we have the associated events. Very similarly we need to create the event classes for these command classes. Under the event package of the common Project. Let me create a new class with the name CustomerMobileNumberUpdatedEvent. On top of this class we need to mention @Data annotation followed by I'm going to mention these fields which are customerId, accountNumber, loanNumber, cardNumber, mobileNumber and the newMobileNumber. Let me create the next event class which is accountMobileNumberUpdatedEvent. On top of this class I'm going to mention @Data annotation followed by I'll try to paste the fields which are accountNumber, loanNumber, cardNumber, mobileNumber, newMobileNumber, and customerId. . Next, we can try to create the event class of Card microservice. The class name will be card mobileNameUpdatedEvent. First we need to mention @Data annotation followed by we need to paste the fields. Once the fields are pasted, we can create the next event class associated to the loans microservice. The class name will be loan mobileNumUpdatedEvent. Let me mention @Data annotation followed by I'm going to paste the fields that are required for these event. So as of now we have the event classes which are associated to the four update commands. In the similar lines, we need to create the event classes for these three rollback commands. Let me create the very first rollback related event class with the name customerMobileNumRollbackEvent. . On top of this Event class, let me mention @Data annotation followed by I'm going to mention the fields which are going to be very similar to the fields present inside the equivalent command. Next, we need to create the event class for the account to microservice. The class name will be accountMobileNumRollbackEvent. Let's mention @Data annotation followed by the field names here we have customerId, accountNumber. After this event class, we need to create the event class for the card microservice as well. The name of the class will be cardMobileNumRollbackEvent. On top of this class we need to mention @Data annotation followed by we can paste the required fields. We don't have to create the rollback event class for the loans microservice because there is no associated command. On top of that, we will never be initiating the compensation transaction on the loans microservice manually, because it is the last microservice that is going to be executed as part of the Saga pattern. I hope you are clear with the changes that we have done so far. Let's continue this code changes in the next lecture as well. Thank you, bye.
