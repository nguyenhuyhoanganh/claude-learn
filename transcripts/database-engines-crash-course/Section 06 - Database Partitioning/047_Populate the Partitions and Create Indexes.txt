# Populate the Partitions and Create Indexes

So now here's what we're going to do. I am going to insert into grade parts select star from. Grates arch. So what will this do? It will essentially copy the entire table into this table. Right. But it will insert one row by row. And here's the beautiful part, guys. The beautiful part on this is every time you insert a row in this table. The database will decide which partition the row goes to based on the value of G. If g is 20 or 20 is between 0 and 35, so it goes to g zero, zero, 35, and so on. So it's going to distribute our beautiful table just like that. So let's see what did I do. Oh that's wrong. Just different syntax. So let's do it again. Boom boom. Keep doing this clear. That's just weird. All right. So this is it. And let's just wait for it. So it's going to insert 10 million rows into this great table. Nice. Done. That's pretty fast right. So now guys if I do select count star from grades parts. That's the same number like 10,000,001. Right? That's extra one. But if I do select max id node id g the grid from grades parts, obviously it's going to be whatever 99 is the max ID. However if I go to the tables now themselves they are populated. But look at this. The maximum value is zero zero 35 is actually 34 right. So let's do select count. So you can see count star from G00 35. This is the my first partition. There only this much rows around. How much is this. 3 million. Is that 3 million? Yeah, 3 million rows. Right. And if I go to 3560, we have around 2 million and so on. It's just a random distribution. So if I do select max G from G 3560, what do you think the number will be? I bet 59. Exactly. Because that's the largest grade we can get in this table and so on. They are just distributed. So now guys, still my tables don't have an index. Right. That was by purpose. So let's go ahead and create an index. And should I create an index on all of the partitions. Well prior to Postgres I believe 11 you you that was the only way to do it in Postgres 12 I believe they added the ability or 11 on and you only have to create it on the on the partition table. Great parts create index. Let's call it grade parts index. Right index on grades parts. On G. The act of creating it on the master or the on the leader partition table. It will create the same index on all the partitions, which is very, very powerful. Let's check this out. So it's going to create that. Obviously there is no index on the master table because it's empty. There is nothing. It's just a virtual table. Almost. Right. So if I do grades ports. There is an index. They say this is your index. But if I go to G00 35. Hey. Where does this index come from? I didn't create that. The database did for me, which is beautiful. If I go to 35. G 3560 we have indexes on all of them. Beautiful.
