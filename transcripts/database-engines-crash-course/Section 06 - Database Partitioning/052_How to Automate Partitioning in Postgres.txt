# How to Automate Partitioning in Postgres

Hey, guys. Some of you asked a fantastic question regarding the partitions topic that I made on my introduction to database engineering course. And the question was, are you saying I have a big table or customers and am I want to partition by ID? How do I dynamically horizontally partition those? So I'm going to create so many partitions. Can you show us a way to automate that? So wrote you guys a script in order to do the partitions in an automatic fashion without you actually manually typing them because it gets really tedious? How about we jump into it? So what we have here is a customer table and let's say I'm going to support 1 billion customers. The default integer field in the ID can support up to 4 billion, which is two to the power 32. But for simplicity reason, let's say it's a billion, a billion customers, a lot of users. Obviously this is like it's a seventh of the world population. All right, So what do we want to do is we want to create multiple partitions and each partition we're going to have, let's say, 10 million each. Okay? And as a result, we we will end up with how many partitions? If you do the math, it's 100 partitions above. We go to the code, show you how to do it. I wrote it in JavaScript, but you can do it in any other languages. You can do it even in PL pl SQL. But I didn't want to introduce you like with a new language that we don't have to learn, so we're going to do it in JavaScript. It's a very with Node.js, so it's a very readable, so to the point and you get to write a client side code, which is like it's going to become your backend. In this case, how about we jump into it? So in this script, guys, I'm going to share with you guys, I have already spun up a Docker container and I showed you many times how to do that is very easy. It's just a one line of code, literally, just let's try it out here so you guys can have it in case I'm going to share this script. So all you have to do is just when you have a docker run dash, dash, name pkg, give it a name and then do dash e postgres underscore password and then equal. This is equal to password. That's the password. And then expose the port. Five, four, three, two because we're going to need to connect to it. Right. And what else? Finally, do detach and then Postgres that will just show you the immediately. So I don't really include the detach unless I make sure that my syntax is correct. But never mind, never mind. So we have now a Docker container. You can just spin it up and now we have it exposed to the, to the your local machine. Now you can use this script to connect to that, right. My machine name just change your host name instead of Jose Mac to yours and voila, you should be able to access that stuff. So here's the script I have this NodeJS will have two scripts, populate customers and create partitions, create partition, will first connect to the Postgres database. Right? So which already exists. And the password of the postgres using the credential Postgres users as we created right here. And then we'll connect to the Postgres database through the client. You're going to create a new database, and if it not exists, I'm going to drop it. So be careful not to hit this script against an existing database that has a customer table. This is dangerous stuff, right? So in a matter of fact, let's just commit that so you guys don't accidentally. Run this against the real life database. I just added it. So if you want to run it multiple times, right, uncomment this code. If you want to do that, then the next step is to create the database customers. And then once we have the database customer, we don't. We no longer need the connection. We can close it. Actually, we now go and connect to the customers database that we just created and connect to it, and then we're going to create a table, right? And this table is going to be called customers. It has two fields for simplicity, ID and name, and it's going to be partitioned by range, which I explained in the partitioning course. We can execute that. Right? But now this is not enough. We need to create the actual partitions, right? So if we did the math, we are assuming that we have 1 billion customers, right? And each customer, each table, each partition should have 10 million. So I did a little bit of a quick loop here to loop to create 100 partitions and each is going to have 1 million, 10 million rows. Right? So I created two variables here. I times 10 million, right? So the first loop, this is going to be zero. And the second this is going to be 10 million, right? Because I plus one zero plus one times. And then you can apply these loops. This is very simple programming. Nothing fancy really here. Then the partition name is going to be called customers underscore ID from and ID two. So that's the the that's the customer. That's the partition name. Right. So every loop you're going to be looping through each of them, then creating all this stuff. So now we're going to actually create the table that's called partition name I just named here. And then we're going to make it exactly look like the customers in terms of indexing of the tables. And you can just play with this, really add more fields and then this code doesn't change because we're going to like really inherit from the table structure. And then that's the beauty. That's what we need. We need to attach the partition that we just created to the customer's table. And this is the range, right? The range is from this and to and just pay attention to the to is actually minus one. So if it's like up until 10 million from 0 to 10 million, that means it's from 0 to 99 million 9999999. That's just that's something I learned the hard way. Okay. And then that's it. It's going to do the second loop, execute the two queries because these are the first query. That's the second query. These are just strings right Now get actually create the partition. And then finally, we're going to close the data. So how about we actually run this so you guys can see we'll run this very simple stuff. Once you clone your. You can you can clone this repo, you have this code, just do NPM install, which I already did to install the pkg library which will allow you to connect to postgres and then do this. Um, zoom in so you guys can see. And node index. Node index calls, create partitions, dot js that's it. Once you do that, we have an error, I think because I'm already connected to the database. Let's just, let's go ahead and exit here and, and let's run it again. I need to comment this out because I already have the database. Postgres That's good. All right, so now we're going to drop and as you can see. Look of that. It's creating all these beautiful partitions and that's it. We're done, guys, and we're going to before we push it, let's just comment this out for you. And that's it. Now, you will need to populate this thing, right? And it's very simple. As you start inserting inserting customers that you only need to target the customers table, that's it. And the database will do its magic based on the ID, it will try to insert to the right database. So I wrote here a small script that creates a 10 million customers in one shot right in a loop. So again, for each for 100 times we're going to do this 100 times. Let's do this again. Node populate. Mr. Myers. All of a sudden, that's it. We're just inserting those customers and that's it. I'm not going to go through all of them and wait because that's that's going to take a long time to complete. But I'm just going to wait for maybe 1 or 2. Meanwhile, while this runs, let's go ahead and show you how to connect to the actual database. Okay. This is how. But let's do it again. So docker execute it. Pkg port. That's my my container name bicycle dash you the user is Postgres and the database dash is customers. Right? Boom. And just like that, you're in the database now. You can do like a select max ID from customers table and tells you what is the maximum ID so it can show you the customers and all that stuff like, All right, so it looks like we okay, we have inserted 10 million customers. Now let's describe this table that we just created. If you do customers, you can see that we have an ID, we have the name, and we also have a partition range ID, and it says I have 100 partitions, but it's not going to show you because there a lot of partitions you can use dash slash d plus to show all of the partitions. So all dash D plus customers. Customers. And that's it. It's going to show you all the beautiful partitions and you can just start querying all of them. So let's query the first one. These are just normal tables at this point, right? They can do select, I don't know, count star from Boom. And is going to query this table normally like just there and you can see like that's the number I talked to you I told you about. All right, guys, that was a quick video showing you how to automate this stuff. I'm going to share this code with you guys. I'm going to see you in the next one. You guys stay. Awesome. Goodbye.
