# Partitioning Types

Partitioning time. So that's what we did. Partitioning range. That's the example I gave. IDs you can partition by log date. Let's say you you have an Iot device that's spitting out measurement or something like that, and there's a log date with every single measurement and you're going to partition like, oh 2012 data 2013 data 2020 data on each year by partition or each quarter or each month, put it in a given partition. So that's range right from this date to this date. Throw it in this partition. And then the beauty of this like, oh, this, this table, this partition is old and nobody uses it anymore. So who's going to query 1995 data anymore? So let's move it into a slower archive. That's like, I don't know, running on tape somewhere. So if it's slow, I don't care. So that's range by lists, discrete values states, for example, California or Rose or any customer in California goes on this table. This is not range. This is a value. So, hey, that partition is for California people that partition for Alabama or zip codes. Right. Or countries the same thing. Right. So that's that's another way of partitioning by hash. Another popular thing especially Cassandra uses that right where where it uses a hash function to to spread the value. So a hash function that gives you a hash. If you got this hash, go to this table. So that's a very, very popular thing. I know in proxies we use IP hash to kind of decide which backend to hit based on the IP. So that's another idea.
