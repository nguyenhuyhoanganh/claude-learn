# Preparing: Postgres, Database, Table, Indexes

Demo. Let's jump into this guy. So what are we gonna do here, guys? We're going to use Docker. We're going to spin up a beautiful Postgres instance, and then we're going to insert around 10 million rows in that table, and then we're going to around create table partitions, and then we're going to join the partitions to the master table and then do some fancy queries, explain and all that jazz. Right. How about we jump into it, guys? All right, guys, let's just jump into the demo guys. So requirement here, just have Docker and that's it because I'm going to spin up a Postgres instance because I play with this is way, way easier than installing Postgres on my own machine. But if you have it, never mind. So once you have that, I'm going to actually run a Docker command here, All right? And I don't need to expose the ports for Postgres because I'm going to bash into the container inside Postgres and I'm going to run the SQL commands inside Postgres container. So I'm going to do a Docker run and then all I have to do is like, basically do dash dash name, give it a name, let's call it PG main, whatever, right? That's the name of the container. And then once I have that, I want to detach this container, right? Because I want to use the same terminal. And then finally. Do an environment variable. Postgres password. That's an requirement. Recent one. I just give it a postgres password. Any any password. And then finally, just do Postgres. This is the image we're going to pull from. And then just like that, we spun up a Postgres instance. If I do Docker, PS, we can have that. We have a postgres running and this is my name. So now what I'm going to do is go Docker, execute Interactive terminal. I want to jump into PG Main inside it, right? I want to run the command bash, right? And then once we bash into the container, I want to do P SQL dash U, which is the username and Postgres. And just like that, since I am the owner of this, it's going to let me in immediately. So now I'm in, I'm in the beautiful Postgres command, right? And I can do stuff right. All right. So what I'm going to do, I'm going to create a table, guys. I'm going to call this create table grades. This is just a bunch of student grades. And I'm going to I'm not going to maintain names of the students or anything. This is just a garbage table where we have literally an ID, a sequence ID that just one, two, three, four, five, six and a grade. That's it. Right. Nothing else. So I'm going to grade. I'm going to call it grades original. Right. And then I am going to give it an ID serial. That means every row I insert will automatically get a sequence and increment. I'm going to make it not null. This is a requirement for partitioning. Everything should be not null, right? Everything that you're partition against should be not null. And then I'm going to do a G, which is the grade itself G and that's also an integer, not null. And just like that, we're ready. So now I'm going to insert around 10 million rows in this table and that's extremely easy to do. So all we have to do is to insert into grades original. I'm going to insert it before I add an index to the G just because it's faster this way, right? Because I don't have to maintain the index while I'm inserting this stuff. So I'm going to insert this select star. Oh, no, star select. Here's what I'm going to do. I'm going to floor a random function which the random function gives me. Fractions like 0.09, 0.1, 0.02.09. That's the maximum number. So I'm going to multiply the random by 100. That gives me values from 0 to 99. And that's exactly the grades right out of 100, essentially. Not quite 100. Not I'm not going to get 100, but who cares, right? So and then I'm going to float the whole thing because I'm going to get the fraction. The floor will give me the function. So now selecting this from Generate series, this is a function that you basically will every time you call it, you specify how many times you want to call it and want to call it 10 million times. And I just added this comment so I can see for myself. So actually like that. And then let's just insert and let's just wait. Oh, bad, bad, bad, bad. Okay, so what I'm going to do, I forgot to specify the column, so insert into so I have to do G. I guess I didn't specify the field itself. That's my bad. So what will happen here is the ID. You don't have to specify because it's it's, it will be auto generated, but the G you have to specify it and the value will go to the G these values. And just like that, we have 10 million rows. Obviously we can we can generate more. But just this just for example, say nice. So now let's create an index on that Create index, let's call it grades original index on grades, what do we call it? Grades original org. Uh, on G. That's it. This will create an index so that create a beautiful index. And we talked about indexing, guys, and that's it. We have an index. So let's describe the table we have here. Boom, Just like that. We have an ID, We have a G. Nice.
