# Full Table Scans

So a full table scan is when you have a large table and you want to find a row in that table or a couple of rows. Not necessarily just one. In order to do that, you need to read the entire table. Reading the entire table effectively consists of reading it page by page. Because what is a table really? It's it's a single data file on disk or it could be multiple data files. And then you need to ask the disk controller says, Hey, give me this portion and then give me this portion and then gives me this portion and that gives me this portion and this portion portions. Really, we can spend hours discussing this stuff, right? If you are looking at the database, these portions are called pages and this is what I'm going to refer to them here for now on pages, read a page, but the page has a fixed size. Some databases have variables. Page size like impulse is always eight K by default unless you recompile the source to be something else. I believe MySQL is 16 K, so when you do that you read these pages one by one, but what does it mean to read eight K from disk? Right. That's could be translated to a single IO. But when you go to the disk there is blocks, there's sectors and in case of a hard disk drive, there is physical SSD page, which is a different size like I believe it's 4k. So that that kind of plays into consideration. And we have spent hours discussing that stuff, right? So it requires a lot of IO to read all this table and then go to the memory and then one by one read these rows and find out, oh, is this my row? Is this my row? Nope. So we need a way to be able to reduce the search space. That's the problem that we have here. So let's take an example here. Find person with ID five and usually when you have the ID five, usually you don't care about the value. Five You want more information, right? So you want to find the ID five so you can pull the name, for example. So in this case, you're going to scan all these raw rows until you find this. And during the scanning, you could have built one page. You could have pulled 100 pages until you reach that. And the cost can vary with the amount of pages you read from disk, right? So if I'm going to search the worst case scenario, if the pages if the row is at the last right of the record and that doesn't necessarily mean anything, to be honest, this statement can be incorrect. It really depends on the Dbms solutions. Dbms are not dumb. Even when a full table scan, when they do that, when they have to do that, when they have to skip the index, they do smart things, they do threading. They split the tables in half and multiple and they do multiple workers that work from the bottom and from the up. And they do all sorts of tricks to find the row as quick as possible.
