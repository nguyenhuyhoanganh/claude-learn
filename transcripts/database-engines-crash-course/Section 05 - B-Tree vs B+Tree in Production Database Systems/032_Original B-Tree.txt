# Original B-Tree

Meet Beatrice. So Beatrice is a balanced data structure for fast traversal. So the goal is to minimize the search space. I don't want to do all these io's to find my rows. Right. Did that rhyme? I think it did. So what? B-tree has a. B-tree has a set of nodes. And I want you to focus on this word nodes. Right. I'm going to reference the paper, the B3 paper written by those two folks in 1970. A b-tree consists of nodes. Okay. And a b-tree have a degree. We call this degree M by default, and you can choose what degree your tree is right. And the choice of how how big, how small really changes the whole equation. And databases pick that automatically, by the way, for you. You don't have to do and you can't configure this, to be honest. So this degree is basically correspond to the number of child nodes each node can have. I said some in this case because not all nodes can have children and I don't really care about these details. These are all theoretical stuff. We don't really care about it, to be honest. We really care about the essence of the tree. To me, the traversal aspect of this node and a node has up to n minus one elements. So if if your node has M children, then the, the it has what we call M minus one elements, right? So if it have five children it must have four. If it have three children must have two elements. So you might say what is an element? This is a good question. So each element has a key and a value, very critical thing. So we have nodes. Nodes have child nodes and each node in in it inside it has elements. And these elements correspond of keys and a value, a key value. The value here is usually the pointer to the row. It's called a data pointer. They call it the value. Sometimes in the in the paper it's referred to as Alpha I. Right. So if you read the paper, I've referenced the paper in the show notes here for you to check it out. And the slides will be in the show notes as well, guys, So you can take a look at that in the description. The value has the data pointer to the row, and the key itself is what you're searching for. And we're going to show an example to clarify all that stuff. So the data pointer, this is this is where databases can shine. Each database system does this differently. A data pointer, which is the value here, can either point to the primary key. If you have a secondary index or a point directly to the tuple secondary indexes in a.B in MySQL and Oracle I believe point to the primary key. Well, while in Postgres all secondary indexes point to the tuple directly and just that I talked about the this in my YouTube channel discussing how Uber moved from Postgres to MySQL. And that is one of the reasons. One tiny reasons. Uh, check out that video if you're interested. Just just type in YouTube, Uber, Uber, MySQL. Hussein you're going to find the video. It's very, very interesting read. So there's also this concept of root node and internal node and a leaf. An a leaf node as well. Right? And we're going to describe what all these means. Like the root node is, is very self descriptive, right? It's the first node at the top. Internal nodes are below it and the leaf nodes that are the at the end, right, there is no child, they don't have children. A very important concept here and note is equal a disk page here in databases. Most databases I know in computer science, they give you this, this, this contrived example where, oh, a degree of three or degree of five and they have some put some values here that doesn't really correspond to anything practical. Right. But practically a node is a complete page. So if a page is eight K, you better find a way to fit eight k worth of elements in it, right? So that's what we try to think here.
