# B+Tree DBMS Considerations

So blustery and considerations. Take a sip of tea. Take a sip of coffee, relax a little bit. I know it's a long video, guys, but I think. I think we need to discuss these in details. You can pause the video and come back later. It's all right. Right. This is there's there's a lot of content there. Ask me questions. Ask questions as you as you encounter them. But this is very, very interesting to understand now. So B+ trees and Dbms considerations. There is an additional cost here, which is the leaf pointer again for, for instance, MySQL, a.B and Postgres, SQL Server all have this leaf pointers that points to the next of the the to the next leaf point in the B+ tree leaf leaf nodes wiredtiger in MongoDB. Yes, MongoDB does use B+ trees through this wiredtiger database engine which I have. I have a lecture about database database engines, right. Talk about that. And Wiredtiger is a B+ tree based engine. And also they have a variant log structure merge tree. That that doesn't have this pointer, right, for for design decisions. Those guys are smart. They only include things where they absolutely need it. So it's like, okay, range queries, who cares? You're not going to do a range queries in Mongo for instance. Okay. You're not going to search for a range of keys. Don't do that. Right. One node fits a Dbms page as I discussed most Dbms do. That can also fit internal nodes easily in memory for fast traversal because the nodes are now tinier I guess. Right? You can fit a lot, a lot, a lot of elements, right? In these internal nodes you can fit these internal nodes in memory easier. So the traversal a single IO will give you way more elements as a result, it's way faster, right? Plus this again really depends on what are you indexing on. This can fit in memory. If it's a grade or Uuid, it's not as easy to fit in memory, but still can. Usually the the internal nodes are not as busy as the leaf nodes in a B+. Trees leaf nodes can live in the data files in the heap. That's absolutely fine. Uh, it is. Obviously you can try to put them in memory, but if the values right, the value that points to is expensive, then sometimes it's not possible. Most Dbms uses B+ trees. So here's an example of the storage cost here. This really I said should between quotes here, but it doesn't have to. Right? The B3 index works best when it's in memory, right? So when you load up your database, let's say you have a table with five indexes, you can try to put all the indexes in memory and you can check the sizes of the indexes by doing a query on the database and say, See, can I fit this entire index in memory? Most of the times the indexes are tiny, like if you have don't have large files, but if it's if it's large, then some databases make the choice, okay, I'm going to only put the internal nodes in memory so that my traversals are so fast because I cache the page. And these are tiny, right? And not very not necessarily tiny because these are pages the same size, but I have way more elements and as a result I can traverse way more. And this could fit in disk, but you can definitely put in memory nothing that stops you from doing that, right? If they fit again, it really depends on on this on this pointer. And also it depends on what you're keying on. Right.
