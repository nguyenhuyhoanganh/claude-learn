# B+Tree Storage Cost in MySQL vs Postgres

Okay, let's talk about this is the final thing. I think the storage cost between Postgres and MySQL. So B plus three secondary index values and I talked about secondary indexes versus a primary index difference is very important to know this differences the two difference between the two. Right. And can either point directly to the tuple. This is an example of Postgres or to the primary key. And this is one of the reasons that Uber moved from Postgres to MySQL. So they. Balskus points directly to pool as a result. Right. Amplifications implode and have a right amplification lecture goes to the go to the section where you have like a various database discussion. You're going to see the right amplification. Very critical to understand that, right? So if you if you have a secondary index and the secondary index point to the tuple, then the tuple size is really not that large. Right. Because it's a fit I believe it's a 32 bit, might be wrong, might be 64 bit. But. That is the pointer. While the MySQL seconder indexes points to the primary key. So if the primary key is large, if it's an integer, you don't have a problem. Really it's just tiny. But if it's a good or a Uuid, that is a really bad idea to put a good or a Uuid as the primary key in a.B MySQL because any secondary key unfortunately will point to the primary key. And plus the whole thing is clustered. So inserts are so slow because of the randomness of the Uuid. It's just not worth it at all. Okay, so as a result the secondary indexes will be so large because they have all these values that points to two primary keys which are effectively uuids, which are these large things, right? And, and you can you can do all sorts of tricks to to, to convert Uuid into instead of using a string which is I forgot what's the lingo for the Uuid but so many bytes. Right? 128 I believe. But you can trick it to use 64 bytes or less than that using the binary representation of Uid. But still you will. It is still large. Right. And if it's large it takes space. Takes space, can't fit the memory. Right. Your memory is precious. You might say, Hey, I'm going to add a one terabyte worth of memory for my primary database. Sure. But do you have to. Right. Think about this, right? Scaling and database engineering is not something that you take lightly. You have to think about all this stuff and it all makes sense when you understand these basic fundamentals. That is what I want to convey in this lecture. The b-trees are not something you do math on. Every decision you make costs you. Right. And it's important to understand how these different dbms's make these design choices. Because every design choice can lead to a completely different outcome if the primary key data type is expensive. This can cause bloat in all the secondary indexes. As I talked about, leaf nodes in MySQL and ODB contains the full row since it's an index organized table or a clustered index too. So that's another thing, right? Clustered indexes in general, SQL Server also have this idea of clustered indexes, clustered index or clustered tables, sometimes called index organized table. So this is an this is an index where the index is the table itself. If you think about it, it's just the whole thing. The leaf node has the whole row and all the columns in it, right? So that everything is is really clustered nicely. It has disadvantages and advantages, right? I'm just not going to go through it, but it's very important to understand that.
