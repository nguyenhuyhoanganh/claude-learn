# Durability (Archived 2022)

Durability. Committed transactions must be persisted in a durable non-volatile storage. That's what it means. If I write and committed a transaction, it better stay there when it's when when I lost power, I. If I committed a transaction, I lost power and then it was shut down. If I restarted, the value better be there. That's what durable durable means. Right. So Redis, for example, is not a durable database. It's an in-memory database. Memcached is an in-memory database. It's not durable and it's up to you if you're caching stuff, you're just doing doing stuff for caching. You might really don't care about being durable, right? So a lot of databases took these four properties and just starting hacking one of them out and then creating a brand new database out of it. So Redis took the durability out and just created this in-memory database thing they have, I think persistence. I didn't work very closely with the radius, but the in-memory they have persistence model. I think Cassandra and MongoDB decided to say, You know what, I don't need consistency. I'm going to give you consistency when you write your query, right? But you don't need consistency and you don't have a schema to begin with. Right. There's a lot of things that got relaxed, not only just the consistency part of it. So the no schema, no referential integrity, you don't get indexes, secondary indexes, all that stuff. You don't get any of that. Okay. So really that that's probably another topic, NoSQL versus SQL. But yeah, that's it guys. Long video. You made it. Thank you so much for watching until the end. All right, guys, I'm going to see you in the next one. You guys stay awesome.
