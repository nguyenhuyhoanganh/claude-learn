# Create Postgres Table with a million Rows (from scratch)

So in many of my videos, I show how to interact with tables with millions and millions of rows in order to test indexing, partitioning and other other features, especially in my introduction to database engineering course. And you guys asked me, how do you create a table with such large quantity of data? So that will be the video to essentially illustrate how to do that. I'm going to do it from scratch. I don't have anything. I'm going to use Docker to spin up a Postgres instance, create a table, create a million row and that's it. Very short video. Let's, let's get into it, make sure you have Docker and let's write this command. Let's spin up a Postgres 13. That's the latest version out there. So let's go ahead and do that Docker run. Guess the only thing we need is is the environment variable Postgres password and we're going to call this Postgres. Just the password. Postgres password. And then I'm going to do. Oscars. That's exactly the image. Before we get the image. Just give it a name. Let's call it PG one because I have something called PG and that's it. It's as simple as this. Create a database. Now I have a database. The next thing I don't really need to expose to the port because I'm going to ssh into the container itself and then run that command and I'm going to just spin up another terminal and do Docker, execute Dash interactive terminal. And then our container name is PG one and then I want to execute the command as P SQL dash U. The username is Postgres, and just like that, we are now into the Postgres command shell. The next thing we're going to do is create a table, let's call it temp. As for temperatures and I don't know what are we going to like? T value t integer I'm going to create. Let's assume this is an Iot populated table, right? With temperatures, I'm going to create a table with just one value, two temperatures, and we're going to insert 100 a million instances of different temperatures from zero to, I don't know, 100, let's say. Is that we have the table. So here's the thing. Here's how you do the trick. We want to insert into temp, and that's the value we want to populate. And the trick here is to do a select and from I'm going to I'm going to leave this as empty here from generate series. And this is a built in function in Postgres and that when you do a select from generate series you can specify from zero to anything really. And I'm going to do a million here, 1000000 that will essentially return for your values from 0 to 1 million, right in a select. But I don't really care about those values because they are very sequential. I don't want random values. So you're going to call a function called random and random are essentially fractions, right? So 0.00, whatever the values. So I'm going to multiply by 100 to get a value from 0 to 100. And since I have an integer, this will be coerced. Nice Lee into an integer. So last thing, do a semicolon and that's it. If you have indexes, this is going to be that's a really guys it's done. That's it. So if I know like a select T from temp. Limit. Ten. You can see I have I have values. And if I do select count star from temp the. Because I have a million rows. That's it, guys. Very, very quick video. So you can populate this database and play. You can essentially change that number to be whatever you want, really, and just play with that. And now you might say, what if I have multiple fields? Just easily just add comma and then add that this is obviously an integer, but you can easily do the same thing to create characters with other functions, right? Convert the character, the number into Ascii, into actual characters and play with that. So that's essentially the core. All right, guys, that's it for me today. Very quick video. I'm going to see you in the next one. You guys stay awesome. Goodbye. All the commands, by the way, are going to be in the description so you don't have to pause and look at this stuff.
