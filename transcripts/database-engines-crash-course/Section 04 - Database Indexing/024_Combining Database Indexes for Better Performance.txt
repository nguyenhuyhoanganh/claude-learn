# Combining Database Indexes for Better Performance

In this video, I want to go through some of the best practices that you can create indexes with so that you can gain the best performance for your queries. I'm going to execute four different queries on this beautiful table that I have here. I have a table with three integers, A, B and C, and I'm going to play with the indexes on A and B So I'm going to create a configuration where I have an index on both A and B, I'm going to create a configuration where I have a composite index on A and B, and I'm going to create a different kind of configuration where I have a composite index and a normal index on B, All right. And I'm going to execute the following queries where I'm going to query only on a and query only on B, and I'm going to query A and B and query on A or B, because that's most of the stuff that we do on the back end. We query with these particular vertices. I understand we have n statements, we have other kind of aggregates, but we'll take it step by step. I hope you learn something useful with this video. With that said, let's just jump into this and play with this thing. All right, guys. So let's start with creating an index on a and another index on B, So I'm going to create an index on this A, by the way, I think I have around 12 million rows in this table. Not much, but enough for us to experiment with. So I'm going to create another index on B. All right, all done. Let's test. So we have an index on a an index on B, so let's check our queries. So the first query I want to do is select C from test where A is equal 70. And I'm going to prefix this with explain and analyze that will tell us how much the analyze take took and the plan that has been used in order to do this thing by jumping to a do it fast, fast, do this very fast. All right. So what happened? Obviously, the postgres decided to use the index. However, it created a bitmap in order to query this index because there are a lot of rows. Look at how many rows we have. 900. 9000 rows. So it has to create a bitmap. Why? Because we have to jump back to the heap to pull c c is not in the index. C is not included in the index. So we have to jump back to the heap to pull the value of C right after locating it. So we are building a map of all the values, the row, the tuples in order to jump back to the table and pull that. And that explains why we have a bitmap index scan scanning the index cell that we just created on a and then we jump back to the heap to actually pull those rows. And we have a cost to 253 millisecond. Not that bad. All right. So that's that's what we have done. And what I want to do here, I'll show you that we can easily change this kind of infer the decision of Postgres by actually limiting the number of cores. So if I say, okay, give me only two queries back, two rows, back, look what the Postgres decided to do because we have only two rows came back. The postgres doesn't need to take the overhead to build a bitmap because building a bitmap takes a little bit of time, right? You have to compile this structure. You have to come up with all values. But it says, Oh, I only found two rows. I don't need to build a bitmap. So I decided to do an index scan, a clean index scan, especially that these rows are are contiguous, they are ordered, they are sorted. So the values that I'm going to get, I don't need I don't need a map for this. I just can go pull the those values directly from the heap. All right. So we use the index. I'm about to go to the other query. My other query is B is equal 100, so I'm still pulling C what? Where B is equal 100. Let's take a look. So I took 250 millisecond. We also use the index. This time we use the B index because yeah, I'm going on B naturally, I'm going to use the index on B and then I hit the table directly with a heap scan using the values that I picked up from B All right. Usually this is also a bad idea, right? Because you're not going to pull 10,000 rows. What are you going to do with 10,000 rows? Unless you're doing some sort of aggregate, that's a different story. But all right. So we have the value. So we're using the index. All right. So let's do this, the query now, but we're going to query on both A and B, So I'm going to do a equal I don't know, 100 and B is equal to 100. Let's see what Postgres decides. So now we're going to do an and between those queries. So Postgres decides says, okay, in parallel, I'm going to scan the A index. I'm going to scan the B index because you gave me two values, right? And scanning those two values, I'm going to scan them and I'm going to build a bitmap, right? And literally you get a bitmap and then literally just end those bitmaps. You're going to end up with a certain values, right? Some values will zero up, some values won't. And then you're going to end up with a set of tuples that you're going to hit the heap and pull the table directly. And that's extremely fast, right? As you can see, it's just happened. Like, how many values did they really return? Right. We return six rows. That's why it's pretty fast. And the ending operation is extremely fast as well. So that's what Postgres do in case of an ad. So. Steiner what would an or do? I'm just going to change this to an or Bu with an Or. It took obviously longer. And that explains a lot obviously because or always brings more rows than end. Right. And now we did exactly the same. We scanned this index, we scanned this index, we brought the bitmap, we ordered them, got a lot more rows than we we wanted, and then we got back the result. So pay attention to what this really happened, right? We're doing double the work we're doing, scanning this index, scanning index and going to the table, giving those rows and then going to the table to pull these roles. All right. So now I'm going to draw. Up these suckers and create a composite index instead. Let's do that. First, I'd just copy. All right. Both indexes dropped. Now I'm going to do is create an index on test a, B. So this is a composite index a little bit different, right? So we have one index that have both values. So it's going to take longer to create and it's going to be more efficient for queries that have both A and B, especially the and cases, right? Because the end cases we did a lot of work and guys don't pay attention to the values that the how long it took because this is really depends on the machine depends on so many things I want you to pay attention to how much work Postgres do. And that's what I'm explaining, essentially. All right, let's do the first query on this. So now I'm going to only query against a and let's see what we're going to do. Look at that. Actually, Postgres. A decided to use the ABX index despite me quitting on a. Why? Because A owns the left hand side when I created that and that dang. That matters because it's in the left hand side. The values can be easily scanned. That's the Postgres rule. You can scan the values from the left, but you cannot scan them from right. Just the index are built left to right. Right. And when you build that query, the index decided to use the postgres decided to use the index and then jump back to the heap. Right. So it took 2 to 50 millisecond to find all those values. Right. And I believe if I do a limit, let's say limit ten, as you can see, it's an index scan instead of a bit bitmap. So it's like a little bit faster. All right, let's do the B. What do Polskas do when I do a, B, 100. Ooh, You felt that, huh? It's a parallel sequence scan. Why? Pollsters will not use the index. Why? Because you cannot use this composite index when you're scanning a filter. That is not the left hand side. It's either A and B or A, You cannot use B, so be very careful. So if you have a composite index on A and B, querying on B will not use the index. So what the table does, what Postgres does is just jump directly to the table and does a full table scan and using multiple worker threads to do that. Right. So it's obviously very, very slow, very careful. Be very careful for that. All right. What happens if I do A and B equals 70 and B, equal 80? Whatever values. Look how fast this thing is. 0.5, half a millisecond. This is absolutely insane that is the best case scenario. If you really all the time, if your queries are all the time like this, have a value and value, it's a great to create an index composite index if if if you if your application only does that and hopefully only does that and you also your query an a best case scenario is to create a composite index. Yeah. It's more costly to create a composite index compared to a single index because it stores more values, obviously. Right. But the performance is remarkable. Right. All right. How about or how about Autodesk? Autodesk. Oh. Nope. Or. And. Or. All bets are off. You cannot use any of the indexes, right? I mean, Polska is decided in this case. It's useless for me to query and try to use that because, you know, it might be faster for me just to go and jump to the table, do a full table scan instead. So that's what Postgres and Postgres does. Most databases do all these decisions on behalf of us. Okay, maybe it's better to do this versus this. I mean, if you look at this, it's like, okay, you have an index on a you can query on a I mean, you have an index on A and B, but you can query, you can use the index for A but Postgres decided no, you know what? It's way slower. I'm going to do this. I'm going to do a bitmap and then go for B, I don't have an index. So anyway, I have to go jump there and do the full scan anyway. Right? What happens if I do a limit limit one sequential scan on this? It's not even parallel, it's just sequential because we know we're going to pull only first row. That's fast. And that's directly on the heap. All right. All right, guys. The first final thing we want to do is. We know. Well, we know that B suffered carries on. B suffered when I have a composite index. So what I'm going to do here is show you that I have a composite index on A and B, what I'm going to do is create an index. On test be an addition to this index and then execute the same query again and see how things are doing. All right. So what will happen now if I query on a we saw this because we're going to use the composite index. That's fine. That's fast. Right? That's nice. Right. How about if I use B? If I use B now. Oh, we're going to use your beautiful index that you just created for us. So that's kind of a compromise as you as a backend engineer, as your database engineer, you can say, okay, I might some queries. I'm going to use the B to query this, so I might need an index there. Right? So you're going to create an index in this case to speed up these queries, Right. And then obviously we're going to hit the table and and the heap in order to do that. All right. Let's do A and B, A and B. Absolutely genius and fast. Look at this. Right. We're going to use the composite index because be right. Right. How about A or B? L or now that we have an index on B and an index on a B now. Oh, wait a second. Actually, we have an index on B, I can query the index on B, do A, build a bitmap and I get a query. Since we're comparing on a, I can still use the composite index because it's a left hand side. I can use the query and the index on that. Build my bitmap beautiful bitmap, build a single bitmap, jump to the heap and then pull the rows that I want. All right, guys, that was some of the best practices that you can do to work with indexes, right? This is just. Just some of them, right, guys? Sometimes indexes can shoot you in the foot. Sometimes they are great, but just learn how to use them. I can't possibly, in ten minutes explain all possible combinations, but hopefully this helps you kind of understand what the database is doing when you actually are querying the indexes. This is very, very critical. As a backend engineer, this is one of the skillset that you have to put on your belt when you're building a database. Applications essentially. Hope you enjoyed this video. Give it a like if you like it, give it a dislike. If you don't like it, I'm going to see you in the next one. You guys stay. Awesome. Goodbye.
