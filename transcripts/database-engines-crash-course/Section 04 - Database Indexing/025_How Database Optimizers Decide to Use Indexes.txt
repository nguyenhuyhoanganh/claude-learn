# How Database Optimizers Decide to Use Indexes

In this video, we'll look through how the database optimizer decides which index to use, especially if there are multiple indexes. This is very useful if you want to know whether your table needs an extra index or not, because if you have multiple indexes, how do you know which index the database is going to use? Is not going to use all of them all the time? It really depends. This video is going to explain these kind of different kind of cases. This video is not going to discuss the combined indexes, you know, one index that have multiple columns. Right. Because I already discussed that. So let's say we have a beautiful table with just two columns. If one if two column one has an index, if one ID and column two has an index, if two ID, and we're going to do a select star from the table where F1 equal one and F2 equal four. So let's go through some scenarios to see how the database optimizer decide to way. Should I use F1 index or if to index. There are so many ways to solve this to look for these values, right? So let's take a lower case number one. Case number one is when the database optimizer decide to use both of them. So what it does, it it queries F1 index, looking for the value one. In this case, finding all the row IDs that matches and tuples in case of Postgres row IDs, in case of Oracle and SQL Server and it collects all these row IDs and then it does the same exact search, but with the value for on if to id so it searches and collects a different set of row IDs and in case of an end it will essentially do an intersection. It's like, okay, what is the values that are in here, not in here, and then essentially merges the results and or it's going to merge the result and essentially union them, Right. So that's that's how it does it. The effective results set, the row IDs are collected. That's the final result. You go to the table if necessary, obviously, and you collect the actual values from the table from the heap. Right. So when does it actually do this? It really depends. If the values are not too small, if we know that we're not going to get so little of a results, because if we're going to get a little of a result, it's not worth searching both indexes. One is enough, right? Especially in case of an end. Right? These cases gets really tedious, right? And if we get too large of a result, if we know that the index is going to turn so many rows, so many row IDs, it's just not worth looking through the index In this case, we're going to go through the table index scan. Right. So it really, really depends on and that's all heuristics the database uses to decide what what of this, Right. So that's case one. So let's talk about case two. The case two is when the optimizer decides to use one index over the other. So let's take an example. We're going to show an example right here. The database decides to use F1 index only, search all the rows for the value of one, and then we collect all the rows and then don't use the second index. Right? Let's just go immediately to the table, fetch those rows and then do a re condition re filter the results based on F2 equal four. Right. So the databases do usually do that when they know that if two index returns so many roles while F1 returns very few rows and the condition is an end because if I know it's an end, then I know of course I'm going to go the smaller one because anything that doesn't match if one right is out of the result set right. So it's going to go if one example is a primary key, if I have a primary key, the database almost always going to use the primary key index in case of an end again or then or it's a different situation, right? Because you get more results. Right. And then go back to the table directly and fetch the results we want to. Right. It's just not worth searching to indexes because b-trees are not cheap guys and even cemeteries are not cheap. Indexes are not cheap to search. Yeah. If you're searching, if you know that you're going to get so, so few of the values, then it's worth searching the index if you know that you're going to get a lot of rows back, then it's not worth searching the index and maybe table scan is actually better. And you might say was saying, how do you know? How does the database know that, hey, this index is going to result so many rows versus this search is going to result into fewer rows? Well, there's something called the statistics, the stats, real, really powerful. The database keeps stats of the table says, okay, here's the table, Here's approximately how many rows there, here's approximately how many ones are there. Here's a proximately how many. Leaves are there and all these kind of values. So they are not 100% correct, but they give really a lot of values. And you can also always update these statistics with a command called analyze or sometimes called, I think, gather statistics in Oracle. Right? So that's the idea. Okay. Really, again, depends on an and or and or here. But here's one example where we use one index over the other. Right. Just one and then filter. So an index with a filter, that's case two. And case number three is just the database decides, you know what? You guys suck, both indexes suck. I'm not going to the search to get for the value f one equal one. And if two equal four is just going to return the whole table, almost the whole table. Three quarter of the table. I did my math and this is what I think. So guess what? I'm not going to use both of you. I'm going to already go to the index because I need to go there anyway, because I'm going to select other columns. The database decide to search. The search will lead so many rows, just like let's go to the table directly and do a filter. This is much cheaper and some databases like Postgres does a threading multithreading and issues so many workers so you can do multiple workers to to scan the table. Right. So table statistics are very, very critical here. So if you got some wrong result, right, let's say here's where things get nasty sometimes I got bit so many times with this. Here's an example. You bring a table very fresh table, right? Empty, right. So that is statistics almost zero. It knows that a table is empty. You insert one row, you insert two rows, three rows, and then the tables get updated every asynchronously. Right now it knows it has only three rows. And then you do an operation to insert 3 million rows or 300 million rows. You just in bulk and you're just immediately after you inserted. You just do queries. You know what will happen in this case, the database, if you don't update the statistics, it will you will execute some something like this and will say, Hey, what should I do? Let's let's look at the statistics. Oh, this table has only three rows, so it's always easier to scan it fully. And in this case, it's going to do a full table scan pulling all the 300 million rows to look through this. I've been I've got bitten so many times by this because I forgot to update this or immediately I, I did my queries after this. So always if you're using Postgres, do analyze what's the other command vacuum full vacuum to clean up all that garbage and then and then after that just do an analyze an oracle use gather statistics, schema statistics and SQL server. I forgot what's the correct execute planner? I forgot what is called for the SQL Server. But yeah, you can do all this kind of stuff guys essentially. And for those who want to advance stuff, if you know what you're doing, if you really know what you're doing, you can use database hinting. You can in the query include some sort of a comments like this one that one you seeing on the screen to kind of force the database to say, hey, your database, you're dumb, you don't know anything, Please trust me. I want you to always use the index of F one or use the index of F two because I have more knowledge than you do because of certain situation. Right. Because my application. I know what I just did. You are not caught up to what I just did as as a database. So trust me, your stats are not up to date to what I'm about to do. Plus, maybe the human in this case are way smarter than machines. Right? And let's say I picks up anytime soon. I don't know. I don't think it will, but anytime soon. So this is going to take time. All right, guys, That's it for me today. Very quick video talking about how these databases use the indexing. Very, very critical. Again, I missed I omitted so many details, for example. So we'll give we'll give this a we'll we'll open it up for questions and we'll have some discussions on on what if there is an or if there is a join. It completely changed the dynamic. Right? There's the building. A database is not something easy. It's really, really difficult. But these tools will help you while you're executing your query. At least you know, Hey, I know that f one is not going to have a lot of value. It's going to have a let's say it's going to have a lot of values. Let's say you have one, two, three. That's only the unique values you're going to have, right? Or I don't know, Let's let's take a real example. Um. Let's say you're building a customer database, right? And you have the states, right, in the United States, like California, Texas, whatever, all of them. Right. And let's say your database is almost. Exclusively all people from California. Right. And you have an index on the state field. Obviously, if all of them are California, that's just that's just a useless index right there, because like, what is going to give me is if I search where state is equal to California, that's the entire table. Right? So but let's say you have three customers on Texas and one from Florida searching that one, that Florida is way faster than searching the 300 million, 300 million. That's the entire United States. But you get the idea, right? If you have one value or few values, indexes are beautiful for this. Right? But if you have so much rows that you're coming back, that's where you play the game of multiple indexes. Okay, you know what? I know that this row, this index is going to give me this much rows back. I'm going to slash it down with another index. So let's put that. But you have to think of how sparse your table looks like. Right. And in these kind of cases. All right. I'm going to leave you there, guys. I'm going to see you in the next one. You guys stay awesome. Goodbye.
