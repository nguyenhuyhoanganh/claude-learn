# Key vs Non-Key Column Database Indexing

Hey guys. In this lecture I'd like to discuss the difference between Qi versus non Qi column indexes. In a nutshell, when you want to create an index in Postgres or really any other database, you specify a field or more than one field to index to create that b-tree on top of right. And that key will be used for searching purposes. And when you do a search on that index, you, the database planner, will use that index and search for that. And the entries that found in the index will contain row pointers to point back to the table to fetch the actual rows. So which we have more than one field there. All right. Non-key indexes are abilities. I don't believe it exists in all databases, but it definitely exists in Postgres where you can effectively create an index and create Non-key include some columns inside the index not to search for, but to include. So in this case, if you select like you created an index on the grades field, right? And you can optionally include other fields in the index like the ID. So now if you select and search by the grade and you want to select the ID, the index doesn't have to go to the table because everything is literally in the index. Let's explain this and see the performance benefits that we get in all of them. Let's jump into it. So I'm going to include in this lecture SQL file that will be creating the students table here and the student table. I deliberately added kind of a big table with a lot of fields to demonstrate the expense of doing this stuff and we have a primary key here and this table actually have 50 million records. And let's do a select count star from students. Boom. Yep, 50, 50 million rows. Make sure if you're using Docker to create to run this container with plenty of shared memory and do a vacuum so that we update all the stuff, make sure the visibility map is up to date. So we get get the optimal query right Now that being said, right, I don't have any indexes here. Right. So we're going to do is do a let's do this query, explain, analyze, explain so we can explain the query analyzer. So tell us how long it took. I'm going to do select ID and grade. I'm going to I want all the students that got results between 90 and 95. Right. So from students where grade is greater than, let's say 80 and less than and G is less than 95. Right? So you can effectively select these range of grades, right, of these students. And I'm going to order by G in a descending manner so that the largest grade goes first and all the way down. Obviously, this is a little bit of an expensive query because you're going to select a lot of rows. Usually this is not recommended, but we're showing the capabilities here. So if I do that, it's going to take a long time and it's going to tell us how long. All right. So the query is finished and it took let's see. It took 21 seconds, 22 seconds actually to finish and look at what it does. It had to do a sort. Why? Because we're doing order by grade, right? Because I want to sort these grades that results, right? It didn't have any index on the G field that I'm searching because I don't have any index there. So it had to do a parallel sequential scan. Sure. Spun up multiple processes to do the hit the disk directly fetching. Look at all this beautiful stuff that is merged from disk. It's so slow. We're going directly to the data file on the table and pulling pages. All the junk. Right. There's so much junk there. There is grades that are ten, 12, 13, and we have to filter this out. Right? Remove and filter this out. And look at this. Look at this. This is what painful rows removed by filter. Right? So we pulled pages and we had to discard what 14 million is that is that 14 million rows are useless. So we pulled pages with rows, but they are just basically filled with stuff that we don't want. And that's what an index help for effectively. So and you can you can you can do something like more elastic you know it's like you know I just want the first thousand students and this should run faster but not always in a sequential scan. This is not nearly necessary to it's going to take faster because it doesn't know it needs to search search and until it gets the results right. So yeah, it took 70s or three seconds faster to pull only 1000 students. All right. So that's that's obviously slow. So the first thing we need to do is actually create an index. How do we do that? Create index on students. G So now this is the fields. I want to create an index on. G so that we can order by the G and filter on the G, because that's what we're doing. We better be a little bit effective when it comes to that stuff. And that's because the indexes are by default ordered. And we love in databases, we love things that are already ordered for us because it's predictable instead of randomness. Randomness is chaos. That was deep. All right. So we have an index. Let's execute my beautiful query again. Let's execute the original one. So what we'll do now, we're querying by G greater than 80, less than 95, order by G that will use the index. We can't guarantee it because the planner has a mind of its own. But let's say they're going to decide to use the index to search for things. But look at what we asked for. We asked for the ID, What is the ID? Well, the ID is actually in two places. The ID is on another index because the primary key and it is also in the heap as another field. So this has to go to the table again to pull the IDs. So you will use the index should use the index to pull whatever it needs to pull. Right. Finds the candidate rows and then jump to the table to pull the IDs of the students. Let's see how fast it is. So it didn't help much. We have an index and we still took 16 glorious seconds to fetch this. All of the information. Well, still better than 21, right? But still slow. Why? And main reason is really we did an index scan backward, you might say. Why? Backward? Because we said order by descending. So which is at the end. So we we did a search for 80. We did this condition on the index and we found one entry. And since this is a P plus three we're going to release, it's all nicely tucked in together, all the values. That's the beauty of the B plus threes, right? So all the values are next to each other. We found all the candidate rows is actually this, this many rows that we found. It took us 42 millisecond. This is not really an millisecond, but this is the cost, 42 cost, whatever this unit is to find the first one and 15.8 to find the last one. Right. So that's like equivalent to a millisecond. But some some people say that it's not, it's not really millisecond. Um, so yeah, it is slow. Still slow. Uh, I'm saying that's a ridiculous query because nobody's going to fetch all these rows. Right. I just want a thousand students. Well, it is much, much faster this time. And it makes sense because it took like 0.5 milliseconds. So bear in mind that we won't be always this lucky to to take 0.45. Right? Because usually this this query are hitting the cache, the operating system cache because we did execute a lot of queries before. Right. So if I do like something like analyze and then did buffers right you can notice that look at this shared hit 20. That means all the things that we queried are cached. That is why it's so fast, right? That's why a lot of you asked me a good question. Hussain, You asked you did this query and it's so, so slow and you did it again so fast. Why? Because the operating system does all the magic of caching all the time. So we need to trick the operating system a little bit. So let's just a little bit change the equation. Change. Let's just do this. Give me all the students from, I don't know, 10 to 20. So this should look at that. We're doing a little bit of reads now. We did hit the end and the end we hit the I o we hit the disk. We no longer do a shared hit. Right. Uh, still, we did a few hit pages hit, but it's better. So that's why got up to six milliseconds, Right. But still, there's a lot of pages that are cached, right? So that's why it's absolutely blazing fast, right? We need to find a way. So if I if I change this to 10,000, it took 25 minutes. Again, I bet that if I remove this limit, it's going to be faster now this time. So a little bit better, right? Look at this. We got 90,000 I o to disk. We went to disk 90,000 times effectively. And we took 11 seconds to do that. Yeah. See. Right. So we know why we understand how the system works because it's just an index scan. And the reason is because you're selecting the ID, which we have to go to the table for, and that's most of the cost, the, the I cost to go to the table while we already living in the index. So the idea is how can we do a pure index only scan? Let's drop this index that we just created and we're going to create a beautiful new index that is on the G, but we're going to include a non-key index here, Non-key column, sorry on students G, but we're going to do include id. Why? Because that's a very common execution path that we're doing. That's where you as a backend engineer, you really need to think like what columns are you selecting? And based on those columns you create your indexes effectively. Let's create that. So it finished. It took a little bit longer and that is makes sense because, well, the index is larger now because we're including more stuff in it. The ID field, which is in a 32 bit I believe. Right. And has to pull this values and store it. So obviously it's going to take more time. But what happened if I execute my beautiful query now? Let's do just the normal one. Look at that. It took only four seconds and we had to go to the disk. You might say, Hussein, you just said that this is an index only scan, right? Do in an index only scan, which is nicely done here. Right. It did said index only scan. So in this index only scan. We didn't have to go to the heap at all. Heap fishes equals zero. So this row you see this line if only when it an index only scan. If you see heap fetches more than zero, that means we have to go to the heap for any reason. Right. One reason is that you didn't run vacuum. The visibility map is out of sync. Right? You you did. You made some changes. You updated the rows. So you have to do a vacuum in order to update all this stuff. So we did a read. For some reason we had to dirty three pages. I'm not sure why. Right. But again, even selects in Postgres sometimes does write all the time. Right? To update these visibility things. The hint bets as they call them. So it took four seconds and I bet if you execute it again and this again guys, this is the entire thing, huh? I'm not doing any limits here. I pulled the everything that's between 10 and 20. Right? And it took one second this time because guess what? We cached more. We hit more pages. Right. And we only had to go to go to the IO 602 times. And you might say, why? Why are we going to the disk? I thought it were just reading the index. Guys, the index is still on disk, right? It's a large it's a very, very large index and we have to hit the i o because it lives on the disk. It will be very beautiful if we can fit the index entirely in memory. That's the ideal situation. But unfortunately, that's that's not always the case. I have a I have an old MacBook with 16 gig GB Ram. It's a Docker container that is not really running with bare bad defaults. So it won't really, uh, and I think I have only one GB shared memory shared with this guy with Postgres. But it's always important to run by the way, always run by Matthew. You run vacuum, uh, verbose, right. And on the student's table. Let's just run it. This will tell us like if there's anything wrong, if you got any errors here and you have to do verbose, it will tell you that, oh, by the way, there is some bad things I have to remove all these dead tables, dead rows. So you guys have if you want to replicate this lecture, you have to run vacuum verbose students, right? Or even just vacuum verbose full altogether. And this will help you update all the pages, the visibility map, anything. It will basically treat Postgres as as as if it's a production ready to be read. Right. And the moment you start updating fields and adding values and inserting rows, then you have to do vacuum again. I mean, vacuum is auto run by default, but it's always good to run this so you can see that we got much better performance. Let's do a limit. Now, this is even a 0.5, right? It's really fast. So guys, at the end of the day, you've seen the performance is not really huge. If you think about it, it really depends on your use case. The larger the table, the better non-key indexes will be. Right. But. Keep in mind that the index will grow larger if you include stuff in it and if you include stuff in it. It can't fit in memory. And if it can't fit in memory, it will be put on disk. And if it put index, that's a read that we have to read the index from the disk to do the queries. So learn about this. You don't have to implement it. Just know that it exists. Put it in your back pocket as a tool. I'm going to see you in the next one. Hope you enjoyed this and all the code. The SQL stuff is going to be included in the lecture resources. See you in the next one. Goodbye.
