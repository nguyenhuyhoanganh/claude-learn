# Create Index Concurrently - Avoid Blocking Production Database Writes

Creating an index on a production database, especially on a large table, can take a long time. And not only that, but it also blocks edits. On your tables, but you cannot really write, writes are blocked when you want to perform these kind of operations. So which kind of sucked write you can do reads just fine. Write for example select from grades where ID is less than ten. That works. You can read but you cannot really write because the index is using that field, that particular field to build the index, right? So that kind of sucks. So Postgres came up with a better approach to create index called Create index concurrently. And this is what I'm going to show in this video. Go ahead and drop this index and do the concept of creating the index concurrently so we can allow write and read while the index is being created, which is very, very valuable. Right? You cannot afford going down or pausing or locking transactions when you when you are essentially creating an index on a production environment. So we're going to do the same thing, create index G on grades. On the field, but we're going to add a word concurrently. What that will do is it will do multiple scans to perform them, and it will wait for all the transactions that write transactions to finish before it actually. And it will pause itself while while there are there are write transactions. So now if I want to insert into grades, write values one for example, I can do that. Can I read? Definitely. You can read. Where it's like, then that's totally fine. You can commit and you can read, right? However, what this will do because of the multi multiple scans, because of the weighting, right. That this operation is going to take way, way, way longer. Obviously compare it's going to consume more CPU obviously, and it's going to consume more memory. So this might take some of the CPU, precious CPU memory from other processes. However. Not only that, it can also potentially fail, guys, Right? Because consider if this index is essentially a unique index and you're inserting duplicate values, what are the index didn't exist. Technically, the index will end up in an invalid state. And in that point, you have to drop and recreate an index. So this was create index concurrently very, very useful tool. And I heard that Postgres is going to make this the default, right? Because when you create an index, you don't really care if it takes a long time, right? You really care about your actual production. Live database writes and reads Be fast, right? And unblocked while in this case you want your index to be created. And it's okay if it takes a little bit long. So this was a very quick video talking about this new feature. Guys, I'm going to see you in the next one. Stay. Awesome. Goodbye.
