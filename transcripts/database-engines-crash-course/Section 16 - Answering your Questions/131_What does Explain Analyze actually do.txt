# What does Explain Analyze actually do?

Hello, everybody. It is October 20th, 21 and I thought it might be interesting for I to go through your fantastic questions on this course. Introduction to Database Engineering as you ask great questions and kind of go through some of them that I got picked up and then answer them in details. I try as much as possible to go through these questions and answer them. You guys ask fantastic questions, but I thought some of these questions it's very hard to answer in text, so might be interesting to make a new lecture called Q&amp;A and answer some of these questions in details. And I'm going to, if you like, these kind of lectures, let me know in the comment discussions. I guess you can have discussions in this lecture. Let me know if you want more and maybe I'll get to do it in a couple of months. Uh, how about we jump into it and see, uh, try to answer some of these questions? So here's a question from a Konstantinos. I hope I pronounced your name right on the getting started with indexing lecture on our introduction to database Engineering course. Getting different results with Explain Analyze. Hi Hossein, and thank you for the direct and comprehensive explanation you get every time. You're welcome. Uh, running. Explain. Analyze Using the exact same syntax with your two queries, I get different results for the first explain analyze select id from employees where ID equal to 1000, I get a value of one he fetches for the later I get a none effectively. Right. And so. So they are effectively saying that you're I'm getting different results with every time I execute. Uh no the results I get every I, I get I get different timings when I execute this query twice. And let's explain a little bit here. What do we mean by heap fetches first? So when you execute a query and specifically in Postgres, right. Um, and that query goes through a path, uh, that is. Exclusively searching the index, which is index only scan. I talked about that in one of the lectures index only scans. That means that it should only go to the index structure. Right? Because we know that there are two structures that is the heap or the entire table worth of data. And there is another data structure called the index that has subset and pointers to the heap with the actual content. So if you're only doing an index only scan, you should only be looking into this structure, which is the index structure, right? Most queries that are index scans, normal scans, they go to the index, find what they want and jump exactly to the page or the block in the table that has the data and pull it up, right? If you don't have an index, you have to scan the heap for the entire table, right? That's that's how basic indexing works. Heap fetches tells us that, yeah, I did an index only scan, but I had to go to the heap to do that. So this is kind of contradictory because hey, if all the results is an index, why are you jumping into the heap? You jump. Postgres keeps these things called hints that tells you if a row is deleted, updated or modified. So you see when when I delete, for example, a row in Postgres or an insert or even update a row you we create a new version of this row because Postgres is an mvcc designed multiversion concurrency control, which means that we also update the index to update the entry. All right. That points to our tuple, right? Because the index also points to the table because you touch the row, we have to update the index to point to the new version. Right. But we also also keep the old version because as some other transactions are also reading, right, the old row because running transaction need to make sure that the row is is running and alive as a result. So because of this all of a sudden now transactions need to make sure that is the row I'm looking at deleted or updated or is or is is it alive to answer this question is it alive it that bits is it alive? Can I see it? Is these kind of bits. The hint bits are stored in the heap and as a result you see these jumps. How do you get rid of these jumps? Well, two solutions. If you have long running transactions, then you will definitely see these jumps, because these jumps means that there are transactions that are very old, very long running, and they they are reading all data that we cannot get rid of. That means, hey, from time to time I'm going to have to jump to the heap to to check if this row is visible or not. But most of the time, if you don't have any running transactions, that means you can freely clean up these old rows and we can freely avoid going to the heap to see if this row is alive or not. Run vacuum. I talked about vacuum and analyze and all this stuff in many lectures in this course, right? So if you don't vacuum after each, after all the updates, then you should get consistent results as of the timing. Right. They did mention that the timing is like 0.004 or 7 millisecond versus 0.17 milliseconds. This is expected because the timing, you only have to worry about times if it's more than, I don't know, ten milliseconds. Right. 12 milliseconds. Right. Six milliseconds. But if it's less than many millisecond, there are so many variables with the hardware, you know, then you're getting into really low level stuff. And this is really not worth going through. Right. You only if you're seeing 100 millisecond, 200, that's where you actually optimize 0.1, 0.2. It's really non-deterministic. It depends on the CPU, it depends on the hardware, it depends on there's other processes running with Postgres, your machine probably having multiple processes that is hogging your CPU, right? So that's kind of answers this question. Let's pick another one. So this is another interesting question from Ihab Ahmed on the acid by Practice examples Lecture Redis Durability versus persistent Hasan In 2009, in minute 20, you said that Reduce doesn't offer durability because of course it is. In-memory database lives in Ram. If electricity goes down, data is lost. And also you, you said. But it has persistence. I need to know what do you mean by that? And I want to understand your point. What is the difference between durability and persistence? They are effectively very, very similar. Right. I might have confused you in that lecture, but let's, let's try to, uh, kind of clarify this. So we're going to come to this, but let's explain. The concept of persistence. To persist is to write to a specific location and have it there, right? So like if you write to a disk, it's persisted to disk, right? And that means if you turn off the electricity and you turn it back on the the the charge will still be there whether you're using magnetic or a hard drive or charge in case of SSD, the data is there. That's called persistence. Now, durability is is a term that database uses. And the idea of if I write and if I said I started a transaction, I did a bunch of queries and I wrote a few things, insert, insert, insert, update, update, update, and then I said commit my transaction is doable if. After the comet. I tell you, it succeeded. If after that, after a millisecond of you telling me that the transaction commit is successfully, I can turn the database off completely. I can unplug the computer and plug it back in and start the database, and the data should be there. Now the durability has to use persistence in order to obviously have the data on. Right. And that's that's that's basically what it is. And now Redis is durable, right? But it depends on the type of durability option that you pick. I don't believe I talked about this in detail, but here is free lecture for you guys. The idea of Redis has, I think, three formats. Effectively right there is the snapshot format where asynchronously when you issue right. This writes actually in Redis goes to memory first and they will commit immediately and they say, hey. The wrote. The write was successful because it was successful writing to the Ram. To the memory. Write doesn't have I say Ram but memory is is very vast write because it could be virtual memory. Write could be memory on disk. That's kind of memory. Write the temporal thing. Ram is the random access memory the slot the dems write. And when I write that you get back commit successfully. But. In that case, if you lose power, you lost your data. But right is what they do with the snapshot is. After a second or two, they take the entire memory and they flush it to disk. They actually persisted to disk. So it is only true that you lose your data exactly after that Second write. If the asynchronous process fails to write to disk and you lose power, then Redis will lose your data. But if you write write, your transaction succeeded to writing to the Ram and then the Redis asynchronous process kicks in and then snapshot the Ram to disk and then it failed. The no redis is actually persistent and durable because hey, it didn't lose your data if it happened in that X amount of seconds and you can configure it, then you will lose your data. So yeah, it is doable, but there is an option that you can lose it. Now you can. This is the fastest write option. There are many other options in Redis that has stronger durability because that that was like a weaker durability or the stronger durability option in Redis called append only file. I think I talked about it in the answer here as well. So the idea here is as you persist, not persist, as you commit your transaction, we write it to the Ram and also we write it to an append only file. Call it this redo log. Some people call it the wall and traditional relational databases we call the wall the write ahead log. And only when that succeeds, Writing to the Wall, which is the journal which is on disk, then we actually say succeed. And this is this is effectively a log of the metadata entries itself. The the actual description of the data includes the data as well, but doesn't have the full data structure of that data. Does that makes sense? Like if you're writing into an index, write the index itself is complex, right? But so the log will have an entry said write x to the index blah. Right. So this is always compact, but writing the actual indexes has to be trees and you have to update many, many locations and stuff like that. Right? So it's not 1 to 1 always. So that's the append only file. And Redis has a stronger durability and it's it's 1 to 1 exactly identical to the, to the relational database. So yeah, this is actually strongly durable if you choose that option. But it's going to be slow in writes because now you're writing to disk with every with every transaction right. That's why Redis is only fast because it's writing to memory, right? But there is always a choice. If you always if you choose a snapshot, you can choose both a off and snapshot as well. To get the best of the world you choose snapshot your writes are fast, but you might lose your data might if that happened, which is very rare that it can happen if you're willing to take the chance you can. Or you can have your writes being slow, but you have higher durability. The option is yours. That's why I like this choice in Reddit. You don't have this choice in other Postgres or MySQL. Don't give you this durability choice. Let's go to the next question. So this is another question from Kanaeru shared on the shared versus exclusive lock lecture, the life cycle of a lock in a transaction. If we update a row during updation, the row acquires an exclusive lock. Does it have an exclusive lock until the commit the transaction? So the question is what is the life cycle of a lock? Right? So this difference from one database to another. But the idea is. If I update one row right, then that row is locked exclusively. Assuming the database supports row level locking. Not all database supports that. Some databases actually, when you want to lock one row, it locks the entire table because it says, hey, it's easier. It's cheaper to lock the entire table instead of locking one row because assume you update 1000, 1000 rows. Do you maintain 1000 locks? Yeah, that's what most databases do. SQL Server has does that actually. Postgres does it differently. It maintains the lock in the table itself. Right. Oracle does a different every database does it differently. But yeah, locks are expensive. So as you update, you maintain a lock and when the transaction rolls back or commit, we release that lock. And the reason we we obtain an exclusive lock on the row itself when we update it so that nobody else actually tries to change it because we don't want the transaction to change it. Sometimes we don't want the transaction to even read it right. The read this version of the row, right. The word exclusive here is a little bit harsh. Exclusive means don't even allow reading it, right? This depends on the type of database. Again, some databases, uh, prevent you from updating it, but allows you to read it. Some database, some databases. Doesn't even have this concept like Postgres. It has multiple versions. Hey, I'm updating this row. I create a new version and locked it. The old one is is free to be read, right? You can still read it. So once you effectively rollback or commit the transaction, this lock is gone. Right? Lock management is very, very expensive effectively. So yeah, that's in a nutshell the life cycle of a lock effectively. But there are many, many types of lock. Hope that answers that question. So let's answer this final question. Index scan versus index only scans. Krishna asks, What exactly is the difference between creating an index on columns directly versus including non-key columns? Right. So they're they're they're they're asking the difference between, hey, what if I create an to indexes here, I create index ID on the ID field and creating another index on the name field. So I have two indexes versus I create one index on the ID field and I include the name. There is a huge difference between them. First of all, this you have two indexes, so you have two data structures. So you have two b-trees, right? So twice the amount of not really stored different storage, but twice the amount of data structures. Right. And well, while the second is you're creating one index and, and that index is keyed on the ID, but you are including with each entry, the name, the corresponding name value not to search for, but to include it. So that means if you search by the ID, you the b-tree, the index will be used to search on the ID and for free. When you find the ID, you can get the name for free without getting to the heap to the table itself to fetch the name column. Right. But if you search by the name column, this will not hit the index because the name column is not indexed on the first example that you showed here when you search by the name. Yeah, there is an index and you can search by the name, right? It's a, it's a little bit of a different thing. And there's another way where you have a composite, right, uh, index where you have one index with two values where you can search by the ID and you can search by the ID and the name, but you cannot search by the name only as well because the name is on the right side of the composite key. I talked about that in another lecture. All right, guys, let's end it here. Just answering four questions. It took us a long time. Let me know if you enjoy these kind of Q&amp;A. I'm going to see you in the next one. Enjoy the course. Goodbye.
