# Multi-master Replication

The other option is to do a multi-master replication, which is a little bit. A little bit complex because you now want to scale the writes, which is something you couldn't do with just the master backup replication mode because the write is always goes to the single instance. So if that instance just couldn't take the writes anymore, you need to scale it. How do you do that? Well, multi master is one way, but it's a very complex way to implement. And let me talk about it a little bit. If if essentially you have multiple master leader nodes that accept writes, but at the moment you say that, what does that mean? We have two people accepting writes and if we do that then there is a possibility of conflicts. Right. That's just very complicated. Like I can write something here and I can write completely something different in another. Database and that that could lead to conflict and you need to do conflict resolution and all that stuff. Some people do that, but most database systems prefer the the original one, which is the one way replication. It's one much, much, much easier to implement, right? And again, you can you can have multiple backup followers nodes. Just each node will follow a different master essentially. But there are multiple nodes essentially.
