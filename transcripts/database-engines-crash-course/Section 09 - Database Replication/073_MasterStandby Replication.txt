# Master/Standby Replication

So the first thing we're going to discuss is master backup replication, sometimes called master slash standby replication. And the idea here is you have one leader database node that accepts, writes and accept DDL operations such as create table or alter database or stuff that you change in the database, create a new database, write versus write where you're inserting new rows in a table or updating a new row or deleting rows. These are called dmls, right? So you do all your writes in one master node and there will be a lot of read only standby or called backup nodes which will receive those writes from the master node. Okay, so that's the idea. You don't write to those nodes yourself as a client. You always write to the master and the master replicates the data to the standby nodes. The beautiful. This is like it's very simple to implement because you don't have conflicts. There's one node that takes writes and that's it. Nobody else is going to to have writes as well, right at the same time. So here's how it looks like. So this is the red one is the master node and the green ones are the standby replica nodes. And if I want to insert or update or even create a new table or even create a database or do anything, I always do it to the master node, the red database instance, and that the act of doing that will propagate those writes to the back end nodes because there is a TCP connection between the replica node and the master node that is like full bidirectional already with a specific database user as well. And this is the same thing here. So anything you do here, this master node will push it to the child nodes or the cold standby nodes and in Postgres. So that's, that's beautiful. So you might have questions like Hussain does. Does the client have to wait for these changes to be pushed to the back end or does it immediately if it's written to the master, can I just immediately get back once it's written to the write ahead log? This is another kind of debate and discussion and people are doing PhDs and what is best. This is called synchronous versus asynchronous one. I'm going to talk about it in another video. So in master backup replication, the beauty of this, if you're doing a read, you can either do a read against the master, you're going to get the fresh all always up to date data, but you can always read against the replicas, against the standby data. That's absolutely fine. You can read against those, but you cannot write to those. The beauty of this is like if this is in a region, a specific region in Asia and this is in a specific region in Europe and this is in America, you can still do that. Yeah, the propagation will take time. But if you if you can take the hit on this consistency, the eventual consistency, that's what the model is, right? Yes. Relational databases have eventual consistency, not just NoSQL. This is a relational database model, yet it has eventual consistency. So those clients will see that, that might see the updates a little bit later. If you can live with that, that's fine. If you don't, there are ways to solve those obviously, but that's the idea here. So you can you can scale horizontally. That's a beautiful of this.
