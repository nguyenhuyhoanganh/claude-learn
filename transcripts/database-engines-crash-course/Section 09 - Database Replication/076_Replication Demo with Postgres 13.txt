# Replication Demo with Postgres 13

So, yeah, how about we guys jump into a demo? So what we're going to do here is we're going to spin up just two Postgres instances with Docker, one master, one standby for simplicity, and we're going to make one as a master and we're going to make the other as a standby. We're going to connect the standby to the master. I'm going to show you how to do that, and then we're going to make the master aware of the standby because we have to do this two way configuration to get this right. I found it I found difficult to actually found a working example on Postgres 13 because it's brand new. Even the one on Percona is just not clear enough. So I had to go through the doc again and make the simplest possible configuration for you. So you get this working right by any by any chance. This is not best practice. I'm not I'm not showing you best practices here. I'm showing you how it works. And I'm going to tell you that, oh, don't do this. I'm going to slap you on the head. Oh, don't use the Postgres user as a replication. Obviously, this goes without saying. But if I if I started doing the create use and replication, this this kind of pollutes the, the goal of what I'm trying to show you. Right. I'm trying to give you what exactly how it works. So simplest way to get it up starting up and running so you can just test and taste this thing. Okay. How about we jump into it? All right, guys, how about we jump into the demo? So I have Docker installed on this machine and I am going to spin up two Docker instances that are each Postgres instance and I'm going to use Postgres 13 because I, I like the risk. I like to I live in the edge babes. I live in the edge. So here's what I'm going to do. So we're going to create a we're going to create two Docker instances. We're going to name the first one, pg P master, and the second one is P standby, right? And based on those each, we're going to have its own port and each going to have its own volume that I'm going to mount on my. Host here. So let's create a folder here. How about we do that? I'm going to call the folder called Rep Replication stands for replication. I'm going to throw my my data for both master and standby here. Why? Because we're going to muck with the data a lot to get this right. Right. First of all, we need to spin up the master and then we're going to spin up the standby. These are completely distinct Postgres instances, obviously. Then we're going to copy the entire data from master to the standby. That's a requirement for to get the replication right. It has to be exactly identical to start with and then you start building on top of it. If those are different, you're going to get a lot of errors. And I got this all the time. So how about we do that? We're going to do Docker run, Dash dash name P master. I'm going to call it the Postgres instance master. I'm going to expose it to five, four, three, two. That's the default port. Five, four, three, two. Right. So it's exposed to the host. That's very important as well. And the master is running five, four, three, two. Right. This is always the same because it's the same it's the same instance that we're running on, right? It's the same Docker instance. And then we're going to do the volume. And the volume here is essentially just inside the replication folder. I'm going to create a folder called Data, right? This will create a folder called Data inside this. And I want to map this to the Postgres data folder, which is in the, which is essentially in the, in the Docker instance, which is var lib I believe, PostgreSQL slash data and that almost it, almost it. So now, now we do a postgres the environment variable password. I'm going to call the Postgres user Postgres that's the same password. Bad idea obviously but we you guys know what I'm doing here. This is not best practices. This is just showing you how to do things. And then if you want to build something massive, this if you want to make this into a production, you actually sit down and think how to do this right? You build an architecture. It's not it's not like that at all. You cannot do it in five minutes. So now the final thing is just do Postgres. And I'm going to detach this so that I'm going to use the same terminal to do other thing. How about we run this? All right. Looks like you have to specify the whole path. So I'm going to do users. Hossein Nasr, Postgres. And then. Replication and then data. Let's review the whole thing again, guys. So. Docker run P master. That's the name of the Docker container, That's the port and that's the path of the volume. What I'm going to do here is I'm going to change this to master data, underscore data. So I know that is actually the master data, right? We want to we want to differentiate between the master data and the standby data. So I'm going to rename this folder to be master data. That's the folder that's going to get created. And then this is what we map to looks good. And finally, once we get this right, we run and. Docker logs. P Master. Looks like we're running. It looks like it's good. Looks like it's good. Creating files configuration files is good. So let's test this thing. B.M. So we have now a folder called P Master Data. Awesome. Okay, so. How about we create the standby replica? All we have to do is just change some variables here. So I'm going to call it P standby. It's now running in five, four, three, two because that's used now. I'm going to five, four, three, three, and then I'm going to match this instead of p master data. No, no. It's going to be P standby data, its own folder and that's it. Just like that, we have two Postgres instances, right? They're not identical right now, but if we go to P standby data, we can see all the data right here, the Postgres data very critical. And then if I go to P master data, you can see it's all there. The next step is I'm going to stop both instances P master and P standby. Why? Because I need to actually copy the content of the master to the standby. And it's very simple to do this because now everything is in my folder right here, right? So all I have to do is just do move p standby data to the P standby data backup. Right. And then I'm going to copy Dash R, which is recursive the master data to P stand by data. Exactly. Copy the whole thing as is and then paste it. And just like that, we now have the same folder, but we tricked the other instance so that it reads almost the same content that's very critical for replication. Another process is to do PG backup. A lot of people do that. I like to do this. That's another example just because it's easier. I don't want to deal with backups and all that stuff. So this is way easier. Now we have two databases now let's start this up, make sure they are working. P stand. P Start. Docker start P master P Standby. Awesome. We're almost there. We just barely created this thing. So the next step here is we need to go to the master. All right, guys. So what we need to do next is update some file only in the master. Right. So just let's go to the master data here, and then let's vim. The PG configuration, very, very critical file. And what we need to do to this file is do an insert and at the end I'm going to add host replication. I want to enable replication for the user. Postgres Bad idea. Everybody tells you don't use Postgres as a replication. Yeah. Also don't use the password. Name password. Right. For your passwords. Obviously everybody knows that. But for simplicity reason I'm going to use the same Postgres user which is an admin. Don't use an admin, use a proper create user and only create so that it only allows to do replication. So you know, you want a user that is allowed to do certain things. Not everything that's a bad idea, but. We're testing. So what's the harm? So and then we're going to do all and then finally MD5, and that will allow the master to to standby, to connect to the master in a replication mode. Awesome. So now the next thing we're going to do is we're going to need to jump into the standby replica. So let's go to the standby replica. And it's very easy because I'm just going through different folders, which is awesome. This is now I'm in a standby data, right? So now I'm going to do Postgres config and now this is the critical part. We want to edit one configuration in the postgres.com file in the standby and that is called I think primary connection info. There you go. We're going to enable that. That will allow us to connect to the master node. And here's the connection string to do this. Application name. The first important thing. Why this is I. This is a unique identifier for each. A backup or standby node. You have to give it a name, so I'm going to call it standby one. Right. That's the first. That's first name that came to my mind. Right. And then you use the same application name. It has to be case sensitive, by the way, and then you reference it on the master so that the master are aware of what nodes it wants to connect to. Right? Otherwise, if you allow anyone to replicate from any database, that's just a security flaw, right? That's why we have this factor authentication here. Then we add a space and we do host equal, which the host name on my master, its host, Mac, which is the same. They are on the same machine. And since I expose them to the port, I can safely use this name. Right. And then what's the port? It's five, four, three, two. Remember, that's the master. Awesome. And then finally, what's the username? Postgres. Well, you can use replicate replica user or replicate or whatever user you want, but for me it's it's Postgres, it's just testing and what's the password. It is also Postgres because we ran, remember? We ran this thing with Postgres. And hopefully we don't have any errors because I ran into errors sometimes here and that's the only change that we do in the configuration on the standby. You can change some other configuration, but I'm not going to go with this in this video. Right? There are some some knobs to turn here. So we save see, it says the standby server this singular only in the standby they are ignored the master server so we save and right awesome. Guys. Did I mention that I have to stop Doc the both standby and the replication. If not, just make sure they are stopped. Well, I don't remember if I stopped them or not. So now the other thing you need to do, which is weird, is. In the standby, which is this you have to create in the same folder. You have to create a file called Standby Dot Signal. This makes this instance a read only instance and a standby instance. Right? So now we have finished with the work in the standby. Now we need to go jump back onto the master. Right. Which is what? What is the master P Master. And now we need to change one slight thing in the configuration, and it's called. I believe, synchronous. Let's search for a synchronous commands by default on that's that's what we discussed synchronous versus asynchronous. Right. What else. Synchronous standby name. That's what we want. So the synchronous standby name, because we were going to do synchronous operations, right? All we have to do is just literally reference here. And that's the tricky part, guys. You have to. This is a syntax thing here. So you do, for example, first one, and then you specify the application names, right? So if you have like a standby, let's spell it right, hopefully standby. Right? So this means give me allow the command as long as one at least one of these servers commit. And obviously this is a little bit silly because there is only one server, right. But if you have like another standby server called standby to the application names, remember here, guys, you put the application names, standby three, Then you say, okay, I want the first two or I want any two for example. Right? This means like this has these two has more priority on the third, right, and so on. But my configuration is only this, so it's very simple. So I have all I have to do is just change this thing. Bum, bum, bum, bum. And then hopefully we don't have any errors, right? And that's it. So now how about we? We. We don't have any problem. Let's start B.M. and P, stand by and let's hope for the best. So let's look at the logs as well. There's another way to look at the logs by doing Docker logs, right? Docker logs for master. Stand by. One is now a synchronous standby with priority one. Look at the beautiful thing. So now the master is aware. Was this the master? Yes, it is a master. And the master is now aware of the standby one replica. Right. And now if I go to the standby. It is also aware. Says database already starts streaming wall from the primary. Right. So wall is the write ahead logs because that's what actually change, right? Write ahead log is essentially is is a very small file that records the transactions. So any transaction on any table or goes to this file first or it's usually in memory and then goes to disk and this is what what what the database is exchange essentially all the walls. Awesome. Looks like we're good guys. So now what do we need to do? Can we test this thing? So far, we didn't actually enter any database. How about we actually. Get into one of them and test. So what I'm going to do here is I'm going to use this terminal, right. And. Execute P master and let's call it P SQL dash u Postgres. That means enter the Postgres mode here. And then I'm going to show you that things are working by doing a select statement, select star from PG Stat replication, and that tells you that, hey, by the way, if this is empty, that means you don't have any replication. But look at this, we have one row, it's a streaming and I I'm streaming and it's a synchronous mode and all this detail and it's called standby one. And this is the user bad idea but who cares? We're testing. So that's the master. I'm going to create another tab here. And this will be my standby. So Docker exec dash it a standby and. Bicycle dash you screws. Now I am in. This is the standby. Yes, this is a standby and this one is the master. So now if I create a table here. Okay. Table test ID integer, and let's say G, Let's say T varchar two. This is not a worker virtual. I just created a table. If I go to this instance and do describe test. Look at this. The table magically appeared on the other instance. Why? Because we're writing to this. However, if I go here and do create table test one ID, I don't know integer and then I do t varchar 200. Look at this. We're getting yelled at. You cannot write to a replica. You cannot write to a standby backup. It's only. It's a read only. So now if I do select count star from test one, it's zero, right? And if I do insert into a test. And I say, I don't know, Eddie And yeah, it's just the ID and then do select Random Times thousand from generate series one 10,000. So I'm inserting random number here. As I query here, it's still zero. Well all of a sudden appeared. That is extremely fast guys because it's it's in the same instance right. But now I am seeing my queries appear here and obviously they are also here. So this is pretty neat. That's exactly what I wanted to show, right, If I drop the table. Drop us a little bit harsh, but if I alter change anything here, that change is going to appear. So let's do alter table test add column. Hopefully I get the syntax right. I'm bad at SQL when it comes to alter. I don't know g varchar or just v integer. Is that how you do it? Oh, how about that? I did it. I got it right. Look at this. It shows up immediately here in the second instance. So that's essentially what we want to show guys. I can delete rows, I can truncate table, truncate table test. Right? And the moment you truncate it, obviously all the rows are gone from the replica as well. Right? So that's essentially replication in a nutshell. How about we jump back into the slides and complete this video? Guys, anything? All the resources and stuff like that will be available below. Let's jump back onto it.
