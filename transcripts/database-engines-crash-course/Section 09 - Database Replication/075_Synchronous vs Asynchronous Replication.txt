# Synchronous vs Asynchronous Replication

Let's talk about a little bit synchronous versus asynchronous replication, which is the same. The problem we originally raised in the first in the presentation. So synchronous replication is when you issue a write to the read master replica master node, the client has to wait until the transaction is written to the master, obviously, right? Because if you commit a transaction, you have to wait for the transaction to commit in the master or in any database. You have to wait write. Once that's done, synchronous replication also forces you to commit to the child replicas or the standby replicas before the client can be unblocked. Cassandra does something similar like this called quorum, and now you get to a confession like, okay, Hussain, I have seven standby replicas on my master node. Which one should I. Should I wait for the seven of them? That's a little bit on overkill, if you think about it. Right. That's why Postgres or other databases gives you these options like, okay, wait for the first two or wait for the first one or whenever any of them completes unblock immediately or whenever two of them or any of them or like, for example, these are more important than those. These two is more important than those three standby replicas like you have priority one on one standby replica is a backup and the other are actually active people actually querying against. So you can have a little bit of flexibility here. So you can control this stuff. You can say, no, I want this as if I am actually want this everything to be fully consistent. So you're going to take the hit, obviously, but you get full consistency, not eventual consistency, because once you write, the transaction cannot fully commit until everything is committed in the master and all the node. If you just turn everything on, you are essentially in a single cluster almost. By the way, this whole thing is called a cluster. Asynchronous replication is what by default? Some of the default is like, okay, once you write to my master, I'm done, the client is done. You just can't unblock to the client. This is done. But the database now now has to do more work on a background. Process that writes periodically to those standby replicas. This obviously. He all of a sudden now you have faster throughput. I have faster writes but the back end I have a little bit some puppet processes that are doing this asynchronous jobs and that could eat off the CPU and do a lot of work, right? If you do a lot of these and it could scale and could slow things down, you could definitely get and get eventual consistency. If you can live with that, you're fine. But it's something people prefer. Synchronous now over asynchronous for for certain reasons.
