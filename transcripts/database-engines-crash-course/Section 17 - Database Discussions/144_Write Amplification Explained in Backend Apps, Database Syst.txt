# Write Amplification Explained in Backend Apps, Database Systems and SSDs

Today's topic is write amplification database. Write amplification. You know, guys, this is one of the reasons that Uber have moved from Postgres to MySQL and we obviously talked about that move that is not entirely approved by the community because Uber did missed a lot of things in that article, but it was a valid reason nevertheless. So write amplification if you don't know guys. It's like a phenomena where your update statement or your SQL or whatever, right? You're performing logically. The actual physical representation of that write is double or not triple or maybe a magnitude more order of magnitude more than the actual logical, right? So you would think like, Hey, I just updated a single field in a single row, but you look at the disk usage at that point and you see it doing thousands and thousands and thousands of kilobytes. Like what? What did I merely updated a 32 bit integer? What the heck happened? The write amplification. You can look at it from three tiers. The first thing which I kind of I saw from building apps, right? And I call it the application write amplification. The second type is the database, write amplification. And the third type is the disk write amplification, which is the SSD. Write amplification. I'm going to talk about the three of them. SSDs are infamous for write amplification, so I'm going to talk about that. And then database, which is something that is hidden from you. It depends really database platform and then the application itself, which is kind of your fault as a backend engineer. So let's jump into each one of them. All right, so let's start with the application. Write amplification. And this is maybe your end user is expecting something, right? But it turns out that the actual work behind that API single call or update is magnificently larger. For example, you're taking your Todo item, Hey, I just done this task, but so to to the, to the developer, the front end developer in this case calling that API to kind of just hey, I just deleted, I just, I just marked this as done. Right. So for the developer might think, oh, this is just an update statement maybe on a single row, but a back end developer could have written this application in a way so that hey, it goes to the Todo app mark that todo as done and then goes to another table, maybe inserts that row into another archive table. Hey, the Hussain have done has washed the dishes for example, it goes into another. So that's two. Right? And then maybe you have another table maintaining the count of the tasks that you've done. So your increment or your update another table with the actual work. So one single API translate to many, many writes and obviously there's this goes downstream and affects that thing. So this is the simplest part of thing you think is a single write, but the actual API implementation, it does a lot of thing and relational databases are designed this way, especially if you have normalized table where you have many, many tables that you have to kind of maintain and keep it consistent. Right? That doesn't mean obviously NoSQL is is infamous of that. Obviously, it has its own limitation as well. But now this is the first type, the application, right? Amplification. Your app claims to do one thing, but actually behind the scene is doing way, way, way, way, way, way more. Another famous thing is delete. Some people implement deletes as a as a as an actual update and an insert. Some people implement delete as a as a logical delete. Right. Where they do more more work at the back end. And so it really depends. So that's an application, right? On application, let's talk about database write amplification. And this is whenever you hear write amplification, it could be either the database, write amplification or the other one, which is the SSD, write amplification. So the database write amplification is is a property that is hidden from you as a database platform. I'm going to give the example of Postgres because the most infamous and it is the exact same thing as MySQL, but with a myisam storage engine, right? They both work almost identically in this case. Almost not. Not quite. So let's say you have a table with six columns, right? Five columns have an index indexes. Each has an index on them. And the last column, which is the sixth column, doesn't have an. So if you execute an update statement, you pulled a single row on this table and then you updated one of the columns, let's say column one, which happened to have an index on them in Postgres. This is what will happen. Postgres will create a brand new row. That's how it works. Always. Any update, any modification on the tuple always creates a new tuple id there is a tuple id that uniquely identifies the rows. Yeah. It will create a new one with the new values that you have. Copy. Exact copy. We have to update the column spawning index because we change the value. So now we update the index. That's something we have to do anyway, right? So that's another. Right. So we inserted a new row and now we want to go to the index and change that tuple ID from pointing to the old row. It points to the new row and we also change the value we introduced the new value that you inserted in that tuple in that, right? So we now it's almost two changes, right? You might say. I'm saying what about the rest of the indexes? These five indexes still point to the old row. Postgres used to update those indexes to point to the new tuple, and that's what Uber didn't like back in what, 91 or 92, I don't remember. And so, so the right value volume that is happening as a result was enormous. Right? Especially when you start you did a single update, Now you're touching all these indexes. And if you have a lot of indexes, all of a sudden these indexes have to be touched despite them not being changed. If you think about it, they were not changed. But Postgres actually solved this problem. I don't remember when maybe it was 94 or 93 by introducing something called the heap only tuple, the hot tuple that essentially it's a trick. So. Okay, uh, since we didn't really change the value, if we didn't really change a value, we don't really go to the index to update it because that's, that's just wasteful, right? So they definitely fix that. The heap only tuple will create a the row, the new row, the tuple ID and then points the old row to the new one. They say, Hey, by the way, if anyone happened to land on the old row, tell them that there is a new version of this. There is a new heap only tuple that points. The only limitation here is it has to be in the same page, right? If the page is full, then you cannot really do that. Right? That's that's why it's called heap and that's why it will fill factor come in. You have to choose the correct fill factor. It's a really complex things to understand. You have to be really careful when you're building this stuff anyway. So it used to do that, but not anymore. But I wanted to give this example. So write amplification is a thing, especially with Postgres, but it could be with any really any other database. Let's say you did another one single update, you updated one row with many values. You changed almost all columns, right? There is no way in MySQL on Postgres or in Oracle we have to do. We wrote one single value, one single row, but as a result we have to touch all the indexes. That's another write amplification. Okay? Because if you look at the update statement, it's a logically it looks to me like updated a single row. But to the database you actually did more and more work. You did a lot, a lot of physical work. Right. And when I say physical here, I mean database physical, right? Because it gets really confusing when I say logical and physical. Where are you really? When I say logical database, right? Is the SQL and physical database rights are essentially the wall changes the write ahead logs, right? All these actual changes that you have the pages. Right. But when it comes to the SSD, which is the next topic, that's a different beast, right? A physical and logical becomes different things. All right. Now, this is the database identification. Very powerful. You have to really understand it, right? So that's the second type of application that goes to the final one, which is a very infamous one with the write amplification. And even if you go to the wiki page, when you write write amplification, that's the only one that shows up anyway, which is the SSD write amplification. So to, to talk about that, we really need to understand how SSD really works, right? So SSD guys have this idea of someone invented something called the charge trap is a technique where you can store multiple level of electrons, right? You can from zero electrons up to certain levels, and the quantity of the electrons determine certain values. And this is the trick we use to correspond to this and this charge trap are put into cells like so. A cell can have a lot of charge traps and as a result, one single cell can have up to the recent newer stuff, up to 16 different charge levels. So if you do the math right with the computer science, 16 is how many bits is four bits, right? So that means each cell can store four bits. And once you charge them, you store them, you can turn off electricity works. It's persistent. It stays for ages. It does not go all it does not get released. Right. And you can. Essentially read those values back. And then based on the quantity of number of electrons, you say, okay, oh, this is 00000, this is 00010, this is 0010. This is oh, this is 1111. Based on this track, they built the entire SSD. So now they took a one cell, they organize it into a row, and then the rows becomes the data pages. Right? So there is a page, has many cells and many rows. Right? So think of a page. Now this have a lot of these charge traps. So a, a page can have a lot of bits stored in it and you can do that. And then pages are organized into blocks, right? And here's the track here. Right? So now you have you have control of a block that has many pages. And this is what we're interested in here at this level, Right. If you love to read more about it, I'm going to reference a video that I absolutely love. It's a very technical, low level thing. If you go on and learn more about SSD, it's a very good animation. So I loved it. So. So now this is what we need to understand. Block and pages. However, nothing is free, obviously. Here's the condition with SSDs. Yeah, you can write stuff for us. You can write in pages, right? As long as the page is empty, it doesn't have anything right. It doesn't have any levels. Okay. As long as the page is empty, you can write anything you want in it, right? And when you write it, we're going to store it. But if there is a page that is already has some content in it and you want to update that content to something else, let's say you have a bunch of rows and you have values. You want to update something, you have to erase this page and write it back. You cannot just overwrite it, right? That's impossible. So what SSD does when you want to update. So what it does is says, okay, let's just write the new value into a brand new page and then leave the old page as a stale market as stale so future people can clean it up if they want to use it for future requests. Right. And here's another limitation in the SSD, you cannot clean up one page at a time. You have to clean a whole block and is you can see some pictures here. A block a block has many pages, right. And a page. I think it's eight K or something like that. If I come with with a payload, I want to update a page. Right. I have a block with three pages. I have four pages. Let's write them A, B, C, d, We just write them back right there. Okay. And then let's follow that. You can see the second picture. I took this from Wikipedia resources, right? And then let's follow it up with writing e f, g, h Right. And then so that's four pages. But I also want to update pages A, B, C, D, So an update. What does it do? It marks those old Abcd pages as stale and then writes new four pages in the same block. So now this block is filled. If you have a new writes that you want to come in and you want to use this block, you cannot use it to the SSD drivers. This is filled. This block is filled. You cannot use it at all. Right? And as a result, it's just filled. You cannot use that. Block So there is this process that's called garbage collection, right? And it runs asynchronously. It cleans up this kind of thing. So so this block has four pages that is in use that it's costing us a lot of precious page memory space. Right? So what we do is, hey, garbage collection, move all the active pages from this block into a brand new block and then just slam that block block, shut clean, flush the whole thing, clean it up so I can use it and I can start using all the free spaces. So now my pages have moved to the block, the second block, and I have now four around four spaces that I can use to write this stuff. Right? That's why it says these do not like updates because of this thing, right? You you're marking this pressure old pages as stale and then writing in the new locations every time as opposed of new fresh nice sequential writes. Right. Every time you just using new pages, you're never touching the old pages. So if you don't touch old pages, if you don't touch old data, you're never going to mark these pages as stale and you're never going to have blocks with stale pages, which is the worst thing you can do if you have blocks with stale pages that have been updated, the garbage collection is going to kick in and the wear leveling. Another thing that's going to kick in, which is it's going to start flushing and moving new pages to to the to new blocks and then flushing blocks. All this work, all the SSDs is going to do as a side effect of your original write, right? So updates are the worst thing you can do to SSDs, right? Like SSDs. Always like beautiful. Just single writes right? That's why B-trees right. If you go to the databases, B-trees causes this kind of behavior over time on SSDs. Because even if you're doing only inserts, b-trees still need to get updated. You're updating in place a b-tree pointer, you're updating in place b-trees locations, you're rebalancing the trees. And when you do that B or B plus three, when you do an update, what do you do with SSD? You go to that page where it exists, slam it as stale, and then take that value, take the new value and write it to a new block. So now the more you do this, the more you're going to have blocks with stale data. And when you have blocks with stale data, garbage collection will get a kick in this going to start moving your precious block to some place, which is an expensive process. It's going to happen again and again and again. Eventually you will get wear down very, very quickly. Right. Because it's going to you're going to start using all your pages in unnecessarily. Let's let's put it this way. Right. And when you start using all the pages in SSD, SSDs have this ergonomic wear leveling where they essentially says, okay, I have this page sitting for years nobody touched this and nobody written in that page and maybe I forgot to mention that, but the page has only limited number amount of time to write, after which it just dies. That page location just dies. You cannot write to it after maybe 2500 times or whatever. There is always the SSD has a lifetime right? So there will be if you without wear leveling, there will be pages that is very busy where you are writing to it a lot, this physical location. Right. And there will be pages that are never touched. So there will be unbalanced. So wear leveling is an SSD concept where it balanced this thing says, okay, hey, this is a cold page from 1998. Nobody touched it. Let me move it to a new location and let me use that precious location to write new pages because you're doing all these updates, right? Eventually. All right. So as the the more you're doing that, the more wear leveling going to kick in and it's going to start using all this pages, all your SSDs in a balanced manner. Right. So updates always kills your SSD faster when I say that I'm talking years and years. Obviously I'm not talking about bounds here. Right. So yeah, that's why. Uh, people came in with log structure, merge tree as an append only so that we are having nice inserts tucked in together. So my blocks don't have these stale pages that always have a consecutive consecutive pages that are never updated. And as a result SSDs love this stuff. You're going to have pages that are never updated as a result, they are clean, right? If you go back to old pages and start updating them, it says you have to move them to a new location. They have this block now all of a sudden have a stale page and all of the other pages are used. So it has to move those pages to somewhere else and flush it and reuse it. And that's a lot of work for this system. All right, guys. So that was the write amplification from the application point of view, from the database point of view and from the SSD. So your single to do marketing the to do app as done if you're doing that single update that the front end developer thinks that you're doing a single update, but you're actually touching all these tables and those tables kick in the database, write amplification, which each table has like, you know, seven indexes on this table have three indexes in this table have, I don't know, four four indexes. Those if you're touching those obviously values with indexes, then those corresponding indexes have to be touched. And here's another one. Write in Postgres, there's this concept of toast tables where a string, if there's a long string, it never stored in line, right? It's stored in another table. That's a toast table. So that table needs to be updated. So the write amplification just triples if you is just like order of magnitude. It's a lot of work. And then now all these writes, which some of them are updated, some of them are inserts. It translates to SSD, write amplification, add the physical disk level, write. So all these pages that you have to update and then move the new page, write mark the old page as stale, move the create a new page, and then have the SSD point, the logical page pointer to the new page physical page, because that that's another thing that the physical and the logical is an SSD concept. I know guys, it's a very complex topic, but I just wanted to make a video raw like that. It's a very, very important topic to understand and then we're going to have to make a new, more, more videos about this. And maybe I can make a practical video discussing this in details. All right. This was a write amplification. Just keep an eye on it. It's really critical. Goodbye.
