# Indexing in PostgreSQL vs MySQL

What's going on, guys? My name is Hussein. I wanted to do a quick video to discuss this topic, which is very interesting to me, and I think. Anyone who works with databases should really understand the underlying data structure of how databases store stuff because it will really become, really handy when you scale. It will really become handy when you design your backend application, how your queries actually access the path of a your path and you're pulling the data, how, how the planner actually makes decisions. And all of this really comes down to indexes. And in this video I want to discuss how specifically just Postgres and how MySQL specifically a.B as a database engine in MySQL stores their indexes. Before we jump into that, we need to understand the basic architecture of tables. So if I create a beautiful table on my database, what does that mean? Right. A table has well, it has columns and and each column will have a specific data type, which is integer string and number dates. Right. Json. I heard the is a good data type these days and you guys come visit me. That's actually a good idea too to store Json as a as a data. So yeah, a table will have multiple columns and each column will have a specific size and that decide the row size and the row size will become either dynamic or static based on how you define it, right? VarChar versus just normal text and so on. And then and that the table is is huge, data structures is big and it's usually stored on disk. And unless you can put it in memory, which is awesome for fast access queries, but usually it's in disk because it's a blob. Huge of just blob, right? So now it comes to indexes index and I made a video about index. Check out this video right here to learn more about them. But an index is basically a another data structure different than the table that tells us, hey, where exactly the rows I'm searching for are. Right. And then the easiest way I understand indexes is like, you know, these alphabetical dividers, binder dividers where it says A, B, C, D, E until Z, right? And then you're searching for a specific like, I don't know, company phone number, right? So if you're searching for a company, uh, zebra, I don't know company. So you will jump to the Z divider and then start searching for that. So you have a smaller subset. So what the index does in this case is actually stores a pointer to the first row in where the Z is starts, essentially. So that's essentially how the index is always like a pointer. So the row, the table, the big stuff and there's another data structure that you can have a quick and access index and it points to the row. Not every database does that. So. How about we start with Postgres? Every index you create in Postgres. Does exactly that. So if you create an index on column A, it's going to create that data structure for you. It's a beautiful data structure. And each leaf, each entry will be pointing directly to the table, directly to to a to a unique identifier in the in the in the table. That's not the primary key necessarily. That's just a row that is most of the time it's invisible. In Postgres I think is called the tuple ID. So that's that's the data structure. So if you create an index points to the row, you might say, what's the problem with that? No, really, no, no problem. But just pay attention to that. If I create another index, Postgres what what is going to do? It also still points to the table. Directly. You see a pattern here, right? The more indexes you create, all of these are actually point to the table, which is pretty neat. So and that's what we that makes sense, right? If I'm searching something in this index, I find it I jump to the table, I pull if I need to jump to the table. That's what's called index only scan versus an index scan. Right? And and if I only need to jump a table, I'm. I know where to find my data. So index is in Postgres all point to the table directly. My sequel and specifically A.B. in MySQL, because MySQL has other database engines that you can swizzle and can change, which is something I love in MySQL and Mariadb. Something you don't see in other databases. We can use swizzle the database engine of a table to be something completely different. My Assam is a is a different database engine that it works exactly. Almost like like Postgres. It points directly to the table. Each index you create, it points to the table. A.B., which is the default MySQL engine doesn't do that. There is always a primary key in the table. And when you create that primary key or if you don't, A.B. or MySQL creates one for you, and then that primary key index points to the table, right? But any secondary index you create, any other index you create does not point to the table directly. It points to the primary key value which corresponds to the table. Multiple hops. There's. There is a design careful design choice for both. And let's let's explore both of them so we know. Postgres. Every index point to the table directly. MySQL. Every index point to the primary key. And the primary key is the only index that points to the table. So now if I did an update in both databases. And I. Update the row. Right or deleted a row. In Postgres if I deleted a row, right? And that road doesn't exist anymore. I have to tell index number one. Index number two. Index number three, index number four, index number six, that that row doesn't exist because guess what? All of them actually are aware are row aware, are aware of this table. So there dependencies. So Postgres, when you update this, it actually goes and update all indexes. That doesn't mean it's slow or bad. It's just that's what it does, right? And MySQL when you update. Add or delete a row. It just updates the primary key. Right, because that's that's the only thing we need to do, because these guys will still point to the to the to the to the primary key and this and that's it. They they don't have they are not aware that a row has been deleted. Right. So that's as long as you don't touch a specific value like for for example if you updated another column in a row that. The index, which on which this is created need to be aware. Right. And that is, in that case you will need to update that index anyway, but only the index that you actually touched for the columns you touched. Right. So that's that's how things work in general. So. Hey, guys. Hussein from editing. And I forgot to mention one thing in my SQL. Remember, every index points to the primary key and primary key points to the table. So if you did a lot of updates on the primary key, guess what will happen? Every single index will need to be updated. So. Really careful while updating primary keys in MySQL. So that's another kind of. Something to be aware of. I'm not going to say limitation. But just something to be aware of. Like the video. And updates in MySQL versus Postgres. You can see that it's a little bit less scattered. Reads, however, if I read. In in in in Postgres using an index. I'm going to find the value I want. And if I ask for a row that doesn't exist in the index, it's not including the index. I have to jump to the table and I can jump very quickly because I immediately look at the at the table. I have the row that points to the table directly and I read it. So reads are in Postgres are extremely fast does not mean that. Breeds on my or slows just just understand how they how things work. Right. So now if I'm reading a particular table in MySQL and I'm using a specific index and I found what I'm looking for, I cannot jump and I need another value to select from the table. I cannot jump to the table directly. I have to jump to the primary key index and then from the primary key index. I get the row and then I jump to the there is an extra hop. So reads are a little bit tiny, bit slower. Sometimes it's unnoticeable. But just understand, if you're doing this a lot, understand what's going on. So if you but but the beauty of this, if you're selecting the primary key a lot in MySQL, that's really beneficial because the primary key is almost always included with every index. So. The choice of what becomes the primary key in A.B. becomes really critical and can boost your performance in a in a in an amazing manner. While in Postgres reads are fast right because and any heap value pulls like pulling from the heap or pulling from the table are also pretty extremely good because you're pulling to the table directly. Right? Obviously doing it over over doing it over doing it. Everything is just basically slower. But that's just very something that's very interesting to understand when you when you deal with these things, right? So Postgres updating a lot of rows or deleting a lot of rows. Might. Trigger a lot of update to a lot of indexes. So just pay attention to that when you do that. And that's it. That's what I wanted to talk about. Obviously, I want to keep this video short. What do you guys think? There is always a choice between Postgres and MySQL on any other database. Doesn't mean Postgres is bad, doesn't mean MySQL is is good, doesn't mean anything. It really depends on what you're trying to do. I believe one of their main concerns of moving from Postgres to to MySQL is exactly that. They had tons of indexes. I was surprised like why do you have this much indexes like too much indexes, too many indexes and as a result touching rows if they touch a lot of rows did a lot of rows just. It just just thrashed all the indexes because it needs to update all the indexes. You need to let everybody that points to the table know that something has changed. Right? Especially if you update if you insert, if you update these, these things are all scattering everywhere. And Postgres is like it's a little bit different when when it comes to updating versus deleting, they they keep the rows alive for MVC reasons. All right, guys, can I give them a video shot? I'm gonna see you in the next one. What do you think about these two beautiful databases? What do you prefer? Let me know in the comment section below. I'm going to see you in the next one. You guys stay. Awesome. Goodbye.
