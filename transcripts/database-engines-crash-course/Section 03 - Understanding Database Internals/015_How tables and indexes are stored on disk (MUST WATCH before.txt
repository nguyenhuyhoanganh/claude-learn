# How tables and indexes are stored on disk (MUST WATCH before continue)

One of the most important fundamentals to understand in databases are the back end of tables and indexes and how are they stored on disk. And you're going to see me mention these fundamental, you know, concepts and pieces, and I want you to understand them before jumping into deep into the course. So this lecture will discuss these different concepts, how tables are and indexes are stored in disk and how they are queried and what is the cost that is associated with these knowing these concepts. Let's jump into it. So here's all the storage concepts and not really just storage. It's just the operations that are performed on the storage itself. So we're going to discuss what is a table from a logical point of view. We're going to discuss the row ID concept, the unique identifier of the row. We're going to discuss what is a page we're going to discuss what's an IO, a heap index data structure, B-trees. And finally, we're going to give you an example of how a query performs on the heap versus an index. Let's jump into it. So we have here a beautiful table with a bunch of columns, right? And so these are the actual fields, the columns. And this is a row, obviously you might say that I work with databases that don't have rows. They have documents. It's the same really same concept. Once you take that idea of row, you can convert it back into at the end of the day, it's all bits, right? And bytes and and the storage model just understands that. And that's where I want you to understand this. This row, this is a column. So what is the concept of a row ID here? Usually databases don't work with the fields that you provide them. Some do, but most databases create their own system maintained. Row Right. And this is the row ID effectively and this is a position in the row itself telling you that uniquely identified this particular row ID right in Postgres. This is also called tuple ID effectively. Right? So this is another important concept to understand, right? Nothing, nothing so special. In MySQL, for example, the primary key becomes that pseudo row id effectively. Right? But in Postgres and some other databases as well, a new row ID is created on your column and that is how it is referenced effectively. So what is a page really? So if we really thought about this table right as we're looking at this logical table, a bunch of columns and rows, what depends really on the storage model, whether it's a row store and a column store, I really want you to watch that other lecture where I talked about rook versus columns or how are they physically stored. This really matter? Rows are stored on this logical pages and a page is really nothing but a fixed size, um, memory location which translates to disk location as well of a of a bunch of bytes. That's it. It's a fixed size. Usually it's fixed size. Right. And then you can put the row in the page. So technically a page can fit many rows. Now it depends how many rows. It all depends on the size of the row. Right. And the size of the columns and the size of the data types of these columns. So the database really doesn't really read just a single row. It's like, Oh, go read, read me row number 1000. Now, it's not like Ram. Ram is byte address where you say, Hey, go give me that particular byte location and just go read it. That's a random access memory, right? Ram No disk, on the other hand, actually just reads a page, right? And you tell me, oh, go, go make an IO and fetch me this much data. Right. And when you do an IO this input output, you get a page or more than one page. Really depends on the the disk partition. How did you partition your, your format, your format, your your hard drive or SSD? And it depends on so many other factors like the the operating system as well. But when you actually read, you get so much stuff, you get one page or more. It really depends. But the beauty of this is one page can have many, many rows, right? Each page has a fixed size. Like in Postgres, the default size is eight. KiloByte in MySQL I believe is 16 kilobyte and you can configure those so if I for. Say each page holds three rows. In this particular example, that means for 1001 rows, you divide it by three, you get around 333 pages. And these pages are stored on disk. And when you look at them in the disk, it's just a bunch of ones and zeros. Right. But they physically have a start and an end, which you define as a database. So this is how you look at and this is very critical to understand. This is not just theoretical stuff. This is what makes some breaks a database. This is what makes a query slow or fast. Understanding How many pages are you pulling? How many rows are you making? Because that's what you what what actually a page is. What is an IO? An IO is an operation that reads request to disk. We try to minimize this as much as possible. This is the currency of a database. The less you make, the faster your queries. That's what we need to make. So what does that mean? An IO can fetch one page or more depending on the disk partitions. I talk about that an IO cannot read a single row. It's just a page with many rows in them. Right. And when you do that, you get many rows for free. You don't get to say, Oh, I want this page, but I don't want this stuff. Sorry, you don't get to do that or I want this page, but I don't want the date of birth. I want this page, but I don't want the names. Now you don't get to do that. That's why Select Star is very expensive. That's why select a name is very expensive as well. Right? Because if you do select name from table, guess what? You're already going to the table. You're doing the page. If it's a row store, you're getting everything right. And we're going to talk more about that in the in the in the course. Then the database has to filter out what you don't want and throw it throw it away. The cost of deserializing the bytes back to the memory and memory structure for the database to process is expensive, right? So you want to minimize these IO. They're very expensive. Same IO and operating system. Some iOS and operating system goes to the operating system cache. This is specifically true for in case of Postgres. Postgres relies heavily on the operating system cache, so an IO doesn't really mean going to disk so it could go to a cache that the operating system defines. So you're going to hear me as well talk about this a lot in the course heap. What is a heap? A heap is exactly what we talked about here. A heap is a collection of pages that represent points to your data table. It has everything about the table itself. So everything in the table is in the heap. We call it the heap because it's a special data structure that has pretty much all the data, everything, right. And that that makes it expensive to query because it has a lot of data, right? So this is where the actual data is stored and everything. And traversing the heap is expensive as we need to read so many data to find what exactly we want. Right? Because usually when you look when you read stuff from the heap, when you try to find something, you find you're trying, you're looking for few things, but the heap gives you everything. And that's costly, right? So this is why we need indexes that help. That help tell you, tell us exactly what part of the heap we need to read. We don't need to read every single page in the heap. I want to know what page exactly to jump to, to read just that page. You can do that in the disk. You say, Hey, go read me page three, three, three, three, three and above. You can do that, but you can't say, Oh, go read me row seven row. If you say go read me row seven, you get a good page two and maybe page three and anything that's next to it effectively. Let's talk about the indexes. Index is nothing but another data structure, no magic. It's just a bunch of other data structure that helps you pinpoint exactly where to go into the heap so you can fetch the information in an efficient manner. So an index is another data structure separate from the heap and has pointers to the heap. Right. And these pointers is just literally numbers that point to the row ID that we talked about and the row ID has more metadata about the page in the heap that we need to pull it. It has part of the data and used to quickly search for something. You can index one or more column in the index effectively to create this data structure. And this data structure uses something called B-trees, which I have an entire section talking about effectively. Good. So yeah, because you cannot index everything, right? You index exactly what the things you need to search for. Once you find a value of the in the index, you go to the heap to fetch more information where everything is there. Right? That's that's the point of the heap index tells you exactly which page to fetch in the heap instead of taking the head to scan every page in the heap. The index is also stored as pages and cost io to pull the entries of the index. Because guess what? When you when you think about the index, it's not something magic, right? It's a bunch of guess what a bunch of another data structure usually b-tree and that b-tree you need to read it in order to understand, to parse this content. Right? And this lives on disk because when you spin up the database, you need to read the index from disk and pull it in memory. Some indexes fit in memory, others don't. If you have an index that is so large, it doesn't have to fit in memory. That makes searching the index to know what to pull right from the heap can be expensive. And this is what I want you to kind of understand and pinpoint here. The smaller this, the more can fit in the memory. Popular data structures for indexes is tree data structure. A brand indexes, you know, many other type of indexes. Learn more on the B3 section. All right. So this is how it looks like on next on disk. This is a very simplified view, guys. So take it with a grain of salt. This is not exactly how it looks like, but it helps understand. So this is the heap. So we have a page, we have a row ID one. This is the probably the employee ID, the name, the date of birth, the salary, and then immediately the next employee row starts, right? It's in the same page, row ID, 220, Adam Knight and then Ali five two. And then this ends Page. Page one. I simplified it. It's exactly identical to this, but I just said row five, four, five, six. Because the three rows in a page, remember that, that that was our condition here in this particular example. Seven, eight, nine page two. So ten, 11, 12 and page three and page 33 will have we'll have three rows, the 1999 and 998. So this is the heap, right. But in the index, if you think about the index, it like let's say I indexed on employee ID, which is this ten, right? So you're going to see only the employee ID here. So ten, 20, 30, 40, 50, 60, 70, 80, 90, Right. And then up until 10,000, which is the last employee employee, 10,000. Okay. I have all the employees here, but that's just their IDs. And look, what do we have here? This is the point we talked about. It says, okay, employee ID ten lives in row ID one has a row ID one and lives in page zero. Right. Employee ID 20 lives on page zero and row ID two, employee number 110 lives on page three. And he has a. ID 11 and so on. Right this way, if you search the index and searching the index is just another i o as we talked about it. Now, it's not sequential as I'm showing it here. It's more a little bit complicated than that. It's a b-tree structure. So it's an actual tree where you go to the left if it's less and go to the right if it's more, I'm going to talk about that in details. But take this with a grain of salt as an example. So let's say I want to search for employee, I don't know, 40, right? So 40 I pull this page. So I did an I on the index. Oh, I found for employee 40. But hey wait a second. I don't want employee 40. I want their name for example, or their salary. Oh you need to go to the heap. What do you do. How, how do I know which page to go to the heap. Well, employee 40 have his row ID four and it's page one. So immediately you do another IO on the heap says oh give me page for page one and give me employee Sorry row ID four. So you go to the row ID four, you discard five and six because guess what? You did an IO, you got five and six. You cannot, you cannot escape that. That gave you all this stuff. Right. And you give you five and six and, and their birthday, you got the salary of six. You get, you get the database of upsert, you get their name of six, you get all that stuff, but you only pick exactly what you want from four effectively. Right. And the database does all this magic behind the scene. So think about just that. So this is much more efficient, right? If this query is fast and if this query is fast, if you're asking for more, it's going to do more jumps here. Right? So let's take an example. So let's say I don't have an index, I only have an heap and I want to do a select star from employee table where employee ID equals 10,000. Well, in this case, guess what? Employee ID doesn't have an index, right? So what do I do? Well, I don't have a choice. I have to go. Query Page zero is is how many rows do we have? We have row ID one, which is ten, 20 and 30, employee ID, ten, employee ID, 20 and 30. So none, none, none. Discard the page. Go to the next page. Page 1456. Nope. 789. Nope. Up until doing a fetching all these pages. This is so expensive. Some some databases does a parallel threading multiprocessing to kind of walk this structure from by using multiple threads from down and bottom, from down to top and in parallel to scan. But in general it is expensive because the last page happened to be the one that had employee 10,000. So we did a lot of queries to fetch that. So that's, that's without index, let's do it with an index. So now the query with the index in this case, we're going to query employee ID 10,000 and here's where the B3 query actually takes place and it goes through a tree until we find exactly the page that we need to look at and one at the moment we find the page in the index. Oh, this is page N, We go to the page and then we find that Exactly. Employee 10,000. Right? And this is a row id 1000 and it's page 333. So once I got that tuple, that information, now I go to the heap and I say, okay, go page three, three, three and pull row 10,000 and return it to the user so immediately we know where to which page to pull, and immediately we get all the information. And then finally, I have some notes here that I just added so we can discuss. And this lecture, sometimes the heap table can be organized around the single index, right? So we did show that index is a separate data structure from the heap, but sometimes the heap, as you can see, as you saw in this lecture, is not really organized. It's not ordered. Right. But the heap can be ordered around the index. And in that case, it's called a cluster index. And I talk about it in this lecture as well in this course. Sorry. And it's called also called the the Oracle terminology. It's called Iot or sorry, it's called index organized table Iot. Right. So just fixed it. So yeah. So this is called an Iot, right? An index organized table where you have the table organized around a single index effectively. And this is called a cluster index, right? And it has a pros and cons can go can I can't go go into this in this lecture, but I'm going to talk about it in other lectures as well. Primary key is usually what is called a primary as a cluster index unless otherwise specified. And whenever you hear the term primary key, that means. Is it a cluster index? Usually unless it's Postgres. Postgres primary keys is just a secondary key, right? So MySQL DB always have a primary key, it's always have a clustered index. So if you define a primary key, the table will be organized around that index. So be very careful about what datatype you pick. I talked about this a lot. The idea of having, for example, a Uuid, which is truly random in a clustered index, just kills the performance when it comes to writing, because now if you insert a Uuid, a random thing, now the heap has to be organized around the index, which is your Uid, which is random. So you're going to be hitting multiple pages because every Uuid is random, so it will l will live in another completely random page. And this scattershot is very, very expensive. And I talk about it in many things and ask questions. Guys, if you want me to explore more of these Postgres have only secondary indexes and all indexes point directly to the row ID which lives in the heap. So this is different than the primary key, right? So in case of if you have a primary key where we have like a clustered index, the primary key itself is a kind of almost like a user defined field, right? But the any other index will point to the primary key here. But in Postgres, everything is a secondary index. We have this row ID, which is a system maintained field that every index point to the row ID, which means if you edit anything in Postgres, all the indexes get updated. That's a cost that you have to be to understand, right? It's just all makes sense once you understand these low level things. That's why I wanted to make this lecture. And finally, let's make a summary. We talked about what a table is. We talked about the row ID, which is really it's a system maintained field that some databases add, some don't, right? Some rely on an existing primary key that the user defined. We talked about page A page has just a fixed size, right? Some pages are dynamic, but let's talk about fixed size. Just easier to deal with. Fixed size pages eight K in Postgres, 16 K in MySQL and you can put rows in them, you can put columns in them, right? You can organize the page around columns, which is something I'm going to talk about in the next lecture. Right? I o gives you one or more pages. You don't, you don't get a row or a byte in an I. You get a lot of stuff. So the key here is if you do an I o and you get everything you want in a single i o that's the beauty. That's the efficient I o you want your i o to be efficient heap data structures. Right. We talked about that index data structure. B-trees We gave you some examples. All right, guys, I'm going to see you in the next one. Thank you so much.
