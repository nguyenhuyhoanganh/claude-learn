# Row-Based vs Column-Based Databases

Row versus column oriented database storages are two style of by which databases use to store their tables on disk. Each have pros and cons. Nothing is perfect. There are use cases for column oriented. There are use cases for row oriented databases. In this video I want to discuss what is a row oriented database or also called row store database, because that's the store type. How is it stored on disk? Right. I'm going to also describe the column oriented database or also known as column store or another fancy name is columnar database because yeah, why not just let's confuse everybody by new by inventing new names. So it's all the same thing. It's just how things are stored on disk. We're going to talk about the difference, these two. And then finally, we're going to discuss pros and cons of both. How about we jump into it, guys? All right, guys. So let's say we have a beautiful table like this, okay? It's an employee table. It doesn't only have eight rows, it has thousands of thousands of rows. Right. But for simplicity, we only have this. These are the columns. Forward. And the first I want you to pay attention to this. This is the row ID. This is how databases it's a unique identifier that identifies the row. It's different than the primary key or this ID, It's just something that is most of the time, most databases have this internal mechanism to identify this row. Okay. And I want you to pay attention to this. So if I'm going to submit a bunch of queries on this table, I want to do this without any indexes because indexes will just complicate the manner. And by the way, we think about column versus row oriented. I don't I want to take them out of the equation and think about them. Just we don't have any indexes. We're just scanning the table to to to answer these following questions. So these are the following queries that I'm going to execute on a row database and then I'm going to execute the same exact queries on column database and see which one does best, where for each style of how we're going to store these tables, right? The same table, we're going to store it on row that same table, I'm going to start on column, uh, storage, and then we're going to look how these queries perform. So select first name from employees where social security number equals six, six, six, the devil. And then we're going to select star from employee where ID equal one. Very simple again. And the final is the select some. We're just going to sum all the salaries of the employees for some reason. Yeah. So the first one row oriented databases, guys. So for row oriented databases, tables are stored as rows on disk. And this might sound a little bit confusing when you first read, it was like, of course, how else are you going to store stuff? And you're going to store row by row, right? So if you think of the disk as a blob, huge blob store, just things that contain by just just bunch of bits and bytes, then the rows are just contiguous one after the other. So the ID followed by the first name, followed by the second name, last name, then followed by the social security number and then comma. And then the second row starts, right? And then so on. Each row obviously has a different size. It could be variable size because I don't know. So like my name is John Smith and someone else name Hussein. Mohammed Nasser. Right. So that's a longer name. So it's going to occupy more storage on disk, obviously, and thus the rows can vary. But for simplicity, let's think about their their fixed size rows. All right. When I want to read a particular row. I go, I do an operation on this called I read when I read that stuff. The controller, whether this is an SSD drive or hard disk reads in blocks, it doesn't read like bit by bit oh bit 0101. It doesn't really like that. It reads the block. So let's shove 512 bytes at a time. And when you or I don't know, 1024 bytes at a time, it depends on the block size. There's something called block size. And once you read that, you're going to get not only one row, you could get one, two, three, four, five rows in a given block. So you get the act of doing one IO can give you five rows and just not all these five rows, five rows and all their columns, all their values. So. So whether you want them or not, you're going to get everything. You're going to get all the columns. So think about that. So technically, you need more rows to find the row you're looking for. So imagine you're doing a table sequential scan on on the entire table. So if you're looking for a employee with Social Security number six, six, six, you're going to have to do a block IO you got, I don't know, seven rows. Then search these rows one by one. You only interested in the Social Security number, so you're going to only pick that field. Unfortunately, you you got all the fields. I'm not saying that's bad. Could be bad. Could be good. Depends what you're doing. But that's what you have to work with. So you have to do a lot of IO to to to actually get to the row that you're looking for because you spent a lot of wasteful reads pulling columns that you didn't need and you don't have a choice. Right? So that's that's just one thing you have to think about with row oriented. Doesn't mean they're bad. No, they are beautiful. They are elegantly designed because they are so simple. They are just rows and you can easily think about them. You can easily implement them. Compare to what we're going to come up next. So let's dive deep into this. So we're going to take our table and then we're going to convert it into a row based on disk. How would how will this look like? Let's take a look. This is how we're going to look like. So imagine this gray box as a block, right? And for simplicity, I put I put two rows in the same block. It could be three, could be four, could be five. Who knows, Right? It depends on the block size. But think about this as as a given block, each gray box takes one. I o jump to the controller to pull that block. So once you did, you ask for this block? You got everything, son. You got everything right. So now this is how we stored basically the row, comma. It's not really comma, it's just literally consequent right values. But I just add a comma for you guys to understand that the ID first name, last name, SSN or the salary birth date I guess when this John the occupation and so on. And so you can see that and this I added this so it can indicate the second row. It doesn't really exist. It's just an identifier like to allow us that. Oh this is actually a second row just, just started and then a row just ended. And you can see that we these are the two rows, two rows, two rows. So let's execute our first query against a row oriented database. I'm looking for the first name for employee where the their Social Security number is six, six, six, the devil. All right, so let's pull it. What do we do? We ask. Hey, because I don't know. I have to start from the top, right? Because I don't know where. Employee six, six, six is something like that. This number, this field has any relation to the row itself. It's just a random value, right? So I have to pull the first block of the table. Does six six, six exist in this? Nope. This is 2 to 2 and this guy is 111. That's the Social Security number. So let's pull the second block, right? Nope, it doesn't exist. This is four, four, four, three, three, three. Right. And yeah, it might look sorted, but it doesn't have to be sorted guys. Right? Just this is just a I'm walking through that query with you. Right. Again, no indexes, nothing. Now. I'm going to. Pull the next block. So I did three reads and then I happen to find that six, six, six. So I found what I'm looking for. Now I want the first name. Guess what? I don't need to do any extra read. Why? Because the first name is right there in the memory now. Because once I pull this block, it's in memory. It's in the Ram. And when I ask for it, I don't have to go back to the disk because. Yeah, guess what? I just pulled it. I found it. Now I just. Hey, this is the first name. And I know because the position, this position number two is the first name. So pull it. So once I find something asking for extra columns are really cheap because we. We already got them right again. Guys, you have to think about this. Whatever I'm going to explain to you, there is no bad or good. It depends on your use case, depend on your queries. It depends on your where clauses. And based on that you pick row versus column based. We're going to come to that. So that's the first query. Let's do another query. Okay. I guess it's executed immediately. All right. So we're going to do select star from employee where ID equal one, right? And since it's ID equal one, this if this is a sequence, the database can do tricks where it can link this sequence with the row ID and it says, okay, ID one is on row 1001, so I know actually where to jump and pull this row, this block, right? It doesn't have to read other things, but it happened to be that this the first block is what we're looking for. So yeah, we're lucky, I guess. But even if we don't have this trick where we link the ID to the row ID, we still have to go through each block. And once we find the block we want, guess what? Now I want all the columns. Can I do this? Absolutely. That is so cheap because all the columns are already in memory. Assuming they are not vertically partitioned into another location. Right. Vertical partitioning. I talked about that in many times in my channels and my courses. So vertical partitioning where we take a column and put it somewhere else. Right. This is not the case where everything is in one beautiful block. So I, I pulled that block. I asked for the ID, I found the ID, and now the user is asking for every field. I always tell people that, hey, select star is bad guys, stay away from it. But it really depends what you're trying to do. If you have indexes, try to do less of that. Select star. Select Star, We're going to learn that it's the worst thing you can do in a column oriented database. Select Star in Row. You can live without it, I guess. Still. Just just be aware of that. So that's cheap. That's relatively cheap because I'm going to pull all the rows and everything in memory. Beautiful. Assume it fits in memory. So could it could could it only be one block? Not necessarily because a row can span multiple blocks. If it's too huge. Let's say we have a, I don't know, some blob here field and then I it's a bad idea to store blobs in line anyway, But let's let's assume you do. But that means this block can this row can span seven blocks for example. So you need to read all the block not necessarily the database are smart. It's going to read the first block, find the ID and only the rows. The the columns you ask for is going to ask for the rest of the block to to fetch her stuff. Anyways, I've been doing this for years. They know what they doing. But I want you just to I want to explain how was doing this stuff. Awesome. So let's let's do the same thing. But in aggregate, this is called an aggregate function, which is an aggregate is just grouping by something, right? So you work on one or a few columns, mainly one or less. How do you work with less one column? That's just idiot. I'm an idiot. Okay, so you work with few columns. All right, let's say one in this example. Okay. So select some salary from employees. Just sum every salary. Let's do this. So we pick the first block. Well, tough luck. I only wanted the salary. Tough luck. I got everything. Again. No indexes here with indexes. This could be extremely fast, but I'm just telling you what the database does without indexes. So you pull this. Oh, pick the salary. It's 1001 and 1002. So we summed those two. Now let's pull the next block so we get the more rows. Okay. 1103, 103 K 104 K sum them and then pull 105, 106 sum them. So, guys, every block we write, we pull, we're pulling row IDs, we're pulling first names and last names. SSN We're pulling salary, we're pulling that which we never use, right? So imagine if I only asked for the for the salaries and if they they were grouped nicely, that would be awesome, wouldn't it? But now, unfortunately, we're asking salary, but we're pulling all the rows, so that's a lot of IO. So again, this is not just one read. It can appear in this screen here as a one read. If the row is long, you can read seven blocks. So that's seven rows, right? I mean, yeah, the databases can do trick where they send one request asynchronously read multiple blocks. But regardless, you're hitting the disk which which every time you hit the disk is a duck dies essentially. Right. So try to save ducks as much as possible. Save the ducks, guys. Save the ducks. So yeah, reading, reading and we're summing we'll summing all of that stuff. That's not so bad I guess. Yeah, but but you read unnecessary information. That means you almost read the entire table. But you used very. Very small portion of it that's extremely inefficient. So raw databases doesn't do well. This kind of queries, if you think about it, it doesn't mean it's slow, really. It depends. What do you mean by slow? I mean, you you've run these kind of queries. The databases do all kinds of tricks to be fast. But if you think about the logic and I'm not saying this is how databases absolutely work, they have all sorts of trick again multithreading they send multiple threads, Postgres send multiple threads to to read asynchronously. So, so they do all sorts of tricks to do fast. But let's discuss how column oriented databases work. So column oriented databases, if you think about it, tables are stored as columns first in disk. So think about. The first name column. The ID column, the last name column. So what do they do is they take the first name and they take all possible values. John, Melissa. Uh, Rick, Paul, Hussein, everybody. And pull all of these into in and save them in disk as consequent value. And then once that table is done and the last value of the first name is done, they start with the second one, right? Last name and then and then so on. So you might say, Oh, why are we doing this? We're going to come to that. So a single block IO read to the table fetches multiple columns with all matching rules, right? So it fetches a column. It could be fetch one column or multiple. It depends. It depends like if the if you have a small table, you can fetch multiple one. If you ask the database to say, Hey, give me this, you're going to get a lot of rows. That's the trick here. So the single column with a lot of rows less Io's are required to get more values of a given column. Right. Because. If you want to get, as we said, the first 100 salaries, that is extremely efficient and column oriented database because hey, just go to the place where we know we stored the salary column and just walk through there because you're going to see the values just consequent. We're going to go in the example and they are great on online analytic processes as we're going to see in the in the coming slides. All right. So let's take the same exact table and start as columns. Here's how it looks like. Again, the row ID is very critical here. The ID field is this. This is the first name. Second last name. I keep saying second name, Social Security number. This is the salary, the date of birth, the the title, I think. And this is the join date. Look at this. So look at this. I want the the ID one. And this. Which row is this. 1001. I'd two. Which row. 3003. Why we're going, we're going to see. Look at this. The row ID is duplicated in every column. So just like that, what is the first thing that comes to your mind? Oh, editing is going to be painful because anything you if you delete row 1004, you have to boof, boof, boof, boof, boof, boof. Do you have to go and mark all these stupid columns, Right? Or these blocks. Remember, these are blocks, right? So. And for fun, I split some of them and multiple blocks doesn't mean they are like, I know this is an integer, for example, or string. I just split them for telling me that, okay, we couldn't store all all four rows here in one block, so we had to split it in another block, right? So I can show you different kind of examples. Again, take it with a grain of salt. I just want to explain these things to you. So this is one block, two blocks, two blocks, two blocks, one block, one block, one block, one block. Okay. Got it. Capiche. So this is how it works. So they just store all this stuff in columns. So all the ID field, the entire. The entire tables here. Right. All the possible values. So this is not just one block. It could be hundreds of thousands of blocks. If you have a lot of rows, remember everything needs to be updated. If you add a new row, you have to turn to to to to update it in all of these logical structures. So what that being said, now we know how this works. Let's put it to the test. Select first name from employee where Social Security number 666. So now the database will say, wait a second, you're looking for Social Security number. I don't need to look in any of other logical structures except for the Social Security, which is this puppy. This is the Social Security data desk. Right. This is how we store. I know this is the location. So in disk, I know where to point my needle and read this stuff. So to search for Social Security number, I only need to pull this right? So now I start reading block by block. I read first block and I got a lot of beautiful values. I got five, five, five, four, four, four, three, three, three, two, two, two, one, one. We did not get six, six, six, so. Okay, no problem. Let's pull the second block. This is almost like the row base, but now at the column level, does that make sense? So now we got it. We found it. But now I need the first name. What? First name is not here. Son. You only got the 66. Now you found that this is actually 1006. That's almost like how indexing works. If you think about it. This is how Postgres stores indexes. It's actually, if you think about this as an index, it's almost very similar, right? It just points to the row now, 1006. Row thousand and six. I know this value. I know the row I'm looking for. This is very critical. And I'm asked for the first name. Where's the first name? The first name is right here. This is the first name. So I'm not going to read this blog. I'm only going to jump right here. Right. So obviously, we found this row, as we said, and then we're going to jump, right? Not the first block. I'm going to jump to this block, you might say, Hussein, how did you know that it's in this block and you didn't pull this block? Because I know the row number and the database does all sorts of trick to two. So, okay, this block has only rows from 1001 to the 1004. Right? So I'm going to jump to block number 703 because it exactly locates the row I want. Right. Because they have all this metadata mumbo jumbo. Right. Again, that's back to our question. The 1006, a link with the block is all there. They know and they pull it and they find it. So we had to do one jump, one block, read, second block read, jumped back to another block read and then we got it. So three na not so bad I guess. Let's do select star from employee where ID equal and let's see how how calm how our beautiful column index column storage do. All right, so ID equal one again, I don't have any knowledge with this, so I'm going to start id. Where is ID? This is the ID, so I need only to read these structures. Right. Let's pull it. Found 1001. It's 1001. So now I have knowledge of the row. I know which block to read, but. The user asked me for everything. Okay, let's jump on. Okay, We found that. Let's jump on to the first name. We know which blog to read. So this one. That's one read. Let's jump on another. Oh. Last name because they want the last name. They want everything read or found it because I know I don't need to read this because I only know which block exactly in each column to read to find my value. All right, SSN fine read. Salary read. Birthdate read. Engineer. I have a bug here. I should have. I didn't stretch it enough. Engineer read. And join date read. What have we done, guys? This is the worst query. There is so much thrashing happening on disk that this is the worst thing you can do. Column databases, just tanks. When you do this, especially if you have a lot of columns and you're asking for everything. And especially if you have like a lot of and queries or queries here where you have to jump to find the multiple rows that will, dude, dude, do that dude and do that. I'm talking to both of you dude, do that. Do not do this. Select start with a row storage column storage. Do not do select star. This is. Do you know how many ducks die when you do this kind of queries? Guys, you go to the database, you read all these ducks, every IO kills a duck. Right? Save the ducks, please, guys. All right, let's do this. Select some salary from employees. Now that we saw how horrible column oriented databases can be, let's see how great they are. I want to sum all the salaries. Yes, sir. What is the salary field? There it is. One read. Done. Well, if you have if you live in multiple blocks, you're going to read the multiple blocks. But done. That's it. And if you have caching and stuff like that. Oh, and by the way, I didn't mention something. If if people multiple people have the same salary, column oriented databases do something called compression and aggregate, they actually if, let's say if if three people have 100,000 salary calories. Okay, all of them have a lot of calories, right? So they have a lot of salaries. And then you can you can just have one entry, 100,000 with a comma base. Right. So 1003, 1004, 1005. All of them have the same one. So this is even more compact. So this will give me a lot of bang for my buck by just doing one block again. Sometimes if I if, if there's like duplication. Oh, this is the best thing for column oriented database, then you just shove it in the same row because, okay, they're going to do it as an array. We talked about that a little bit in my Postgres 13, Postgres 13. They just started doing this index duplication. I believe they called it B3 duplication. Postgres version 13. They start shoving more duplicated values in a single leaf node so they can compress, compress on. So, yeah. Pros and cons. Is this thing perfect? Nope, nope, nope, nope. Nothing perfect. It depends on your use case, guys. If you ask me, I still prefer Ro base to be honest. Just simplicity of writes and reads. But there are some cases where you need column based oriented to summarize, write, write and reads. You want a simple structure and we saw how complex column stores are, right? They're very complex. We have to duplicate. You have to show the, the the the row IDs everywhere. That can be a little bit messy for writes and slow writes and read write. That's why a lot of people use column based and warehouses and stuff like that and lakes data lakes where things are just almost static. They don't edit. They just do a lot of analytics on a single column. So that's powerful stuff, right? So let's go through these pros and cons in general. So for role based, it's very optimal for read and write, right? And well, I'm going to say read and write. I'm going to say transactions in general or online transactional processing, right? They are very great for this because they are very simple in their implementation. So. And if something simple, we can enhance it and make it more and more efficient as we go. Right. That's why rights are fast. Were reads almost fast. It depends how your query looks like. We saw some examples, right? Online transfer, online transaction protocols. Processing transactions are great for row based because you start a transaction. We know what exactly, what rows, what blocks to touch. We can write the wall, the the write ahead log efficiently because we know exactly what we're changing. Right. Versus column. This will a little bit the structure is all over the place so we have to do a scattershot compression isn't really as effective though because. I think about it, right? I'm not talking about the deduplication. That's something you can do in the index. I'm talking about just the idea of having the values in the in the row. The row itself is almost distinct, right? It's almost like a hash because it consists of different fields. Right. So you cannot really easily compress it, right. The compression algorithms is not going to find a lot of tweaks to compress because the values are not consequent right. Compared to the column oriented, where the consequent values are almost the same type and almost the same thing. Right? So you're going to get a lot of chances where of duplication, right? You get a lot of people named John, right? So John's are going to be squashed together. So compression is not as efficient for the rows because you get all these different things that are next to each other and are completely different. So we can't compress as effectively aggregation. We saw how poorly the row based database did with the with aggregation. I mean, we had to query the whole table, but we, we pulled. Lots of information that we didn't pick. So column oriented databases were more efficient when it comes to with aggregation because we fetch only what we need and we we work on that. So if you if you started fetching information or data that that are only what you need, then you're going to start you, you'll be very, very efficient in your query and you'll only fetch the information that you need. Right? So obviously you're going to have a lot and you're going to finish faster than than row oriented databases. Efficient queries with multi columns, right. So if you're picking multiple columns in a query or working with multiple columns usually. The raw oriented or better. Right. And again, it depends. I'm talking only about the queries itself. Right. So you can have a lot of columns if you want, but if you're only working with a few of them, that's absolutely fine with column oriented databases. But. If you're actually working and touching all these columns, that could be bad. Right. With the column oriented database, with with row, we saw it. Right. A fetch on the row. Give us almost all the columns again. Again, if you unless you have vertical partitioning. So with column based writes are slower because we have to update all these beautiful structures. Every column has almost a structure on itself. Think of it this way. How is it stored? So I need to know where is it located? And touch it. Touch it. Every column. Right. Like very similar to indexes, if you think about it. They're perfect for online analytical processing. So if you're doing analysis, you're not touching, you're not writing perfect column oriented databases. It compress greatly because all similar sampling are together. So you can the compression algorithms can do magic on these things and compress them. Amazing. For aggregation, we saw be done right. That was that was that was amazing. I'm going I got to I got to admit and finally it's inefficient for queries with multiple columns. If you're working if you're asking for a lot of columns, don't do a column. Don't do a column. I know this is a little bit weird. You think that column oriented databases or column oriented data stores or columnar will be great if you're asking for a lot of columns? Nope. They are the worst of this stuff. I know guys postgres, MySQL, other databases. Most of them are row based storage. However, SAP, Hana and others have Oracle I believe. Most databases by default go with row, row, row based storage. However, if you think about it, they have database storage engine and we talked about database storage in my database course and my YouTube channel as well. I talked about that, how a database engine you can swizzle this database engine for a given table. So every table you can pick a date table and you store this table as column store and you can pick another table and store it as row store. You can swizzle the database engine, right? And based on that, you can just change your database engine for every table based on the needs. If you have a table that only get queried, make a column, for example. Right? If you're doing a lot of analytics on it, make it a column, right? If you're doing a lot of writes, probably you want to make it a row store, right? And think about all this stuff. Like you cannot, for example, join a row based store table with a column based table. Right? That's just bad. I mean, some databases support it, but give them a give them a break, guys. Give them a break, because that will be really disastrous. I can't even imagine what the database will do. It was going to freak out, I guess. All right, guys, that's it for me today. What do you think about these two puppies? Let me know in the comment section below. I'm going to see you in the next one. You guys stay. Awesome. Goodbye.
