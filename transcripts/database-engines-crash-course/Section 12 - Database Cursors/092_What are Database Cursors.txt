# What are Database Cursors?

So guys, we always talk about how it's bad idea to work with a large set of result set. So if you have like a huge table, like this example, we have a student grades and they are like, what, 12 million students here? Even if you add some sort of filters, let's say, Hey, give me all the students that got grades between 90 and 100, right? And if you do that, yeah, you have a smaller result set. But still, it's a million students. Sometimes there is no option but to work with a larger data set. So if you actually execute, select the student ID from grades and you say, Hey, give me all the students, you can do that, definitely, right? So you can say, give me all the student IDs that got results from 90 to 100 and you can definitely do that. But good luck executing that query. And even if you execute the database, is doing a lot of work to pull, first of all, to do the execution plan to find out which indexes to use, right? And then after that it does the actual fetching, then it compiles the results. And then if you're in another client machine, then that transmits that results into the TCP protocol depends on the database protocol. It will shove that into the network and that is a lot of work. And then the client have to wait for all the results to come back. Right? Obviously I can't it right here. Once you have all the results in the client, then the client has to have the memory to store all those results. So that is absolutely sometimes impossible to do. Meet database cursors and I'm going to talk specifically about server side cursors here cursors that you create on the database and says hey, database please, I'm about to fetch this result, but don't give me the result right now. Create a cursor. And then once you create that cursor, I'm going to ask you to fetch results from that cursor. And only when I do that you go and do the work. Follow your plan, query your do your index only scan or your bitmap index scan or whatever, full table scan. If you didn't have any indexes and then go and fetch those roles for me and then return just the ones I asked for. How do we do that? Let's explain how to do that. Very, very simple. First of all, cursors have to be to work within a transaction. So we do a begin transaction or just begin that does a transaction. And then you can say declare cursor C as type cursor for Select Star. Not just, let's say, interested in the idea from grades where the grade is between 90 and 100. And then when you do that, it says, Hey, I declare the cursor, it's good to go. You saw that it didn't actually execute the query. Technically, it just came up with the map. It came up with the plan to actually plan this query. However, it didn't yet execute it. So now if I do fetch see that act will fetch the first row based on the query plan that have taken place. So if I do that, give me the first row. Fetch. See? Give me the second row and so on. You can go all one by one and you can play with this as you want, right? And you can. For example, there are many, many things that you can do, like, for example, fetch last give me the last entry for this for some reason. Right. And you can give me a fetch the last entry and then you can see that the database is doing more work here. I say, okay, to fetch the last entry, I have to kind of scan through all of that stuff to determine what is the last entry, right? To to pull that last entry. Sometimes the database have knowledge of how to obtain the last entry in a very efficient manner. Sometimes it actually doesn't. Right. And that's when that's when an index backward scan versus an index scan become really, really useful. So, guys, what is the advantages? And this advantages. What is the pros? Of cursors. So the beauty of cursors is you save on memory in the client side, at least in your backend application that connects to the database. Right. So if you're, if you have a spin up Python application or JavaScript or go application and you want you want to process a lot of results, but you want to do them piecemeal, you want to work with them slowly, you can do an open a cursor and then start pulling a number of rows, let's say 100. Let's start working with those 100 and then go move to the next one and then do whatever you want with 100, Right? So you obviously and then discard the memory for the 100 and then fetch the next one. Right? So it definitely saves a lot of memory, especially in the client side, right? Otherwise you cannot pull 100 million rows in the memory. Right. That's why I always get confused of these questions in interviews like, Hey, sort this array. And I don't know, it has like a 1 billion row. It's like, what? Why would where did a billion row came into my memory to sort it. That's a bad idea essentially. Right? So I always like ask the interviewer back. It's like, okay, where are these actual values stored? Are they in a database somewhere? Are they in? Did they came up from thin air? Of course not. Right. So if I can sort them while I'm pulling them up, that's probably a better idea. Right? Then pull all of them in memory and then sort them in memory. So be careful from this question. You can accidentally flip the interview too, on the interviewer. When you when you when you know what you're talking about. So the second pros is streaming, guys. Right? Streaming is a good idea, right? When you want to pull the rows and as you continue pulling the rows, you can stream them to another web socket connection. You can stream to a bunch of gRPC connection and you can just continue pulling values, right? It can also be canceled, right? You can easily cancel a cursor and the beauty of this is like, hey, I'm you know, I know I'm about to pull a million row, but I only worked with 100,000 and I'm good. Cancel that query rollback. I'm good. Now go. Move on. So canceling is a good thing. Paging is another good idea, right? You can page with cursors. However, we'll we'll learn that it's actually it's not as easy to page in a web application with cursors because cursors is stateful we'll come to that also can be used in a stored procedure to write PL SQL programming language a fully fledged programming languages with cursors. All right, so about talking about the cons, what's bad about this? The bad about this is it's actually essentially stateful, right? It's a stateful. That means there is a memory allocated for it in the database. So and there is a corresponding transaction that points to that cursor. That means if you made another request to another server, to another process, that that process has no idea of your cursor, you cannot share cursors essentially because that's, that's just a property of the transaction. So statefulness is a double edged sword. Sometimes it's an advantage, sometimes it is advantage. If you want to horizontally scale, obviously you cannot horizontally scale. You can do some tricks with proxies if you know what you're doing. If you're savage DevOps engineer, you can you can make your web application, understand that there is a cursor involved and send some sort of a variable that will stick you back to the not only to the same server but to the same transaction. However, that is extremely difficult to pull out right Once if you really wanted to do that and you can do paging with that approach, well that is much, much better way of the kind of compared to the current paging that we do, which is essentially a stateless paging. Every request is goes into another server and that server does the query again, which is painful to the database. It's an insult to the database to execute the same query twice, executed once and then loop through it with a cursor. That's even better. But sometimes we cannot. Obviously. Sometimes it doesn't work that well. Another cons is long running productions guys cursors. If you want to iterate through a cursor, you have to do it through a transaction and that means your transaction is going to be running for a long time and transactions running for a long time is is not a good idea for the databases. The databases cannot do indexing properly. If you cannot do DDL on the table if someone is connected, right, you essentially stop people from doing normal work. Some some write operations can be stopped by long running transactions if you have acquired certain shared lock. Right. All right, guys, that's it for me today. That was the pros and cons of database cursors demonstrated. In Postgres. I'm going to see you in the next one. You guys stay awesome. Goodbye.
