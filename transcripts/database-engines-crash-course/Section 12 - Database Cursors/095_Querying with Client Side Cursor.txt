# Querying with Client Side Cursor

All right, guys. So now that we have a million rows, what are we going to do is insert a new file here and then I'm going to call it server cursor. Server side, Curser the Pi. And then I'm going to create another file called client cursor dot pi. So let's start with the client. How do we do a client cursor that actually what are we going to do is query all the rows and then just measure the time for the query to get executed and we're going to fetch the first 50 rows. Okay. So that's that's what that's our criteria and we're going to do it first with the client cursor and then we're going to do it with the server cursor. Again, import cycle and then connection equal. Let's deal. All right, guys, just saving some time here. Okay? I have a connection. Connection dot close. Okay, let's do a cursor. That is client side cursor. So a cursor core connection dot cursor. If you don't specify any parameter here, you get a client side cursor. Okay. Client side cursor. Okay. And we explained what is that? Right? And then what we want to do is cursor, dot, execute, select all from employees. Obviously this is a huge bad mistake to do that. All right. But we're doing some performance thing anyway, right? So even with a where clause, sometimes it gets query can get slow, right? But that essentially will execute everything. And then what we will return will return a bunch of rows for us. Okay. No, that will not return any rows, actually. Okay. So cursor dot Fitch. Many will actually return the number of rows and I want just 50 rows. Okay. And I'm not going to print them or anything. Okay. So now we need to actually measure the time to do that. Okay. And to do that, we're going to import some library called time. All right. I just like to use a T so we can do it simply and then let's do that. Start time. Okay. Equal T dot time. Okay. Make a snapshot before we start the cursor. And after we start the cursor, we will get the time divided subtracted from the start time. All right. And that will be in seconds, essentially. Right? So I want it in milliseconds. So I'm going to just multiply by 1000. Okay. All right. So now we have and we're going to print, uh, f uh, cursor established in E millisecond. Cool. And then we're going to do the same exact thing for the execute. Cool. And then we're going to do. But this is different now. Cursor established the cursor establish a query. All rows will just say, Let's call this, execute the query. All right. And then this is different. This is actually fetching the thing. Okay. And we're going to explain how is this different? Okay. Execute query. And this is actually fetching 50 rows. First 50 rows. Just fetch the first 50 row and tell me how long it takes. Okay. How about we run and see how this is going on? Okay. Oh, we forgot to close the cursor, guys. Cursor to close. Okay, then let's start. All right. So let's zoom in here so you guys can read this. Establishing the cursor obviously doesn't take anything. It's just literally declaring that statement. Right? So .02 millisecond. But look at executing the query, which is we didn't have to actually get the result. It was just executing the query, fetching every single thing into the cursor, took 845 milliseconds and then fetching the first first 50 rows. Nothing, right? Almost nothing because we have everything in the client, right? We now if I look at the processes like the process is huge. It's taking containing 1 million tuple row. Okay? And obviously the more you execute it's a tuple. So it's a it's not an array. So we didn't actually allocate everything. Okay. So it's slightly different. So read only.
