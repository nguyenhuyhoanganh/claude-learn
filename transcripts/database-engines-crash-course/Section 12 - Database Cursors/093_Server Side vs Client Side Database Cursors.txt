# Server Side vs Client Side Database Cursors

In this video, we will discuss the differences between server side curser and a client side cursor with regard to Postgres database and Python. So here I have a Postgres database in my laptop. I have a table here, I created a database and I created a table. It's empty now. With that said, let's just jump into the video. So what's what is the difference between a server side cursor and a client side cursor? So to to connect to the database from Python, you essentially establish something called a cursor, right? You create a cursor and then you either write or read from the database. Okay? And the client side cursor is when you, for example, execute a select statement, let's say select all from employees. Like here, what happens is all the results are executed and delivered over the network to your client, which is Python. Okay. And versus the other approach where server side cursors, where a cursor is created on the database and only when you absolutely need something, the client can say, Hey, give me ten results, give me 30, give me whatever, give me one, give me the next. Scroll through the results and only when you absolutely need it, it will start fetching those results. So the result is actually executed on the server and set there and the client kind of fetches them incrementally or if they want all of them. Okay. So that's the difference. With that said, let's just jump into it. So.
