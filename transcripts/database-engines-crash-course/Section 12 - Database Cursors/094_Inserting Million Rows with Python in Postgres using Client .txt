# Inserting Million Rows with Python in Postgres using Client Side Cursor

To do that, we need actually a large table and to do a large table, we need to actually populate it with, I don't know, like a million rows. Okay. So let's go ahead and do that. Okay. And I'm going to speed up through this process, maybe through the video, but essentially what we're going to do, let's create a new project. And this is called server versus client cursor, right? It's a new project. So we're going to go ahead and then let's go ahead and create a file here called Insert 1000000.py. And that's the file that we will use to essentially create a lot of rows in this table. Okay. All right. So the first thing we need to do is import psycho pg2. And then I want to have obviously establish a connection, right? So that is how you do a connection. What's your host? My machine name is called Hussain Mac. That's where my Postgres instance is. Database equal. Hussain DB And obviously the user is Postgres. The password is Postgres and the port is five, four, three two, which is the default port, right? So obviously you have to close the connection, make it a habit. Okay. And then what we need to do is essentially create a cursor, right? And to create a cursor, you ask the connection, give me a cursor. Okay. Then what do you do is for I in range, This is a million. I think that's a million. Right. Go ahead and execute million. Insert statements. That's the easiest way, right? Obviously there's like a most optimal ways of doing things, but we're just inserting here, obviously, I'm going to skip through this. I'm not going to wait for all the million stuff to get inserted. Okay. Uh, SS Right. ID And then obviously I want I and I don't know the employees name or test I Okay, that's a great name. It's a beautiful name. Okay. And then obviously, once you're done, you have to do connection dot commit. And the reason is you want to commit your changes, right? You don't want to create a million rows and then say that you close it, the whole thing will be rolled back. All right. So let's go ahead and run this, guys. And what did what happened? I think we're going to give it an error here. Oh, no. Looks good. What's going on? It's going okay. 251. Go ahead. 252. Nice. All right, This is chugging. Let it chug. And whenever it's ready, guys, I will come back. All right, guys, we have a million rows. I think. So. Let's make sure that this is true. Oh, that's wrong. Okay, let's do a count. Star. Yep. We have a million rows.
