# Querying with Server Side Cursor

All right, so that's the client side. Let's do the server side, literally copy paste. And the only difference to do a server side, you just give it a name, right? Once you give it a name, it becomes a server side curser But it's behaves exactly the same. Okay. What did we get? Look at the results guys with the server side. Curser. Executing the query took three milliseconds and now fetching the 50 rows actually slightly slower than the client side because we still have to make the round trip to give me the 50 rows. But it's just very it's a very cheap call because I'm getting give me only 50 rows. Right. Give me the 50, give me the 50. And if we do a loop, we can we can see that this is actually we can we can see that. How how long is going to take. Right. Every 50 rows is going to take two milliseconds. All right. So there you have it. Okay. You have client side, right? If you execute, you pull everything to the client. Right? And then if the server side, you leave everything on the server. But you will have to do some round trip going back and forth to fetch the results. Okay.
