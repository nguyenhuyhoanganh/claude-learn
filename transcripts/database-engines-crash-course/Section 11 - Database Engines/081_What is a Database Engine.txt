# What is a Database Engine?

So here's the thing. So a database engine is nothing but a library that takes care of the disk storage and the Crud operations like create and update and delete on the actual disk. Right? Because guess what? When you do insert into a table values one, two, three. Well, what happens here is there are a lot of layers and the lowest layer is we're going to take our data and flush it to disk, right? Whether this is SSD or just a normal hard, hard drive like spinning wheel. Right. There is there is a library that does that. Most of the databases has always merged this concept of a database into one monolithic thing. Right. But recently this has started being broken into. Okay, let me break these libraries into two parts. Right. The data, the actual work to store on the disk versus the actual work to do, for example, client server. That's different, right? I want I want this to be separated from this so I can like a classic classic reusability software, right? This database engine can be as simple as just a key value store, really. It's just example a level. DB Right. So I can put a key. There's a value stored to disk. That's it, right? It could be as rich as complex to support full acid transaction. We talked about acid guys, I'm going to reference it here. Atomicity Consistency, isolation and durability. Right. And supports transaction supports, foreign keys. All these functionality could be moved to the database engine and that's could be could be really powerful for people who want to create a new databases, right? So, so let me just use this engine. I don't want to write my own engine. Right? So yeah, a database could vary. Could be as simple as this. It could be as complex as have all these features. So a Dbms can use the database engine and build features on top of it, right? We might say. Hussain Well, if I thought the database is just storing stuff on disk, right, that's its job. Well, you're wrong because databases support so many other features, and if you and I don't use it, enterprises use it, right? So like server application, right. I want to I want to do a master follower setup where I have a master database or a leader database, and I want this follower node to copy its data that that has nothing to do with storage, right? This is a feature of the database. So you want to build this at the Dbms level, not at the database engine level, Right. So isolation. Some databases do very high level isolation at the at the at the database level, right at the database software level, stored procedures, foreign keys, so many things features that the database can support. Right. But it's independent of the engine storage and stuff. Right? So if you want to write a new database, you don't have to start from scratch. Just pick up an engine, pick up one of those engines. Are we going to talk about and then write your own database on top. And you can be you can you can design your database to fit a single good use case that maybe the general purpose database is out there. Do not fit your use case. Right. So you can start thinking, okay, that's why we start to see a lot of databases, software being being created, right? Because there are specific use cases and to be optimal in this day and age where latency really matter, where where, where performance really matter, Well, I'm going to create my own database and I'm and I'm I am defining my my workload so that it is for example, it's a very high insert workload. But I barely read from my database. Right. I don't know. This could be like a cold storage kind of a thing, like I'm storing logs, just let me store my logs and I don't really care about reading. So that's one use case. Another use case could be, Hey, I'm barely writing to this thing, but I'm reading a lot, so make read very fast. I don't care if you're going to sacrifice writes, okay, so you can play with this stuff, right? Sometimes referred to as a storage engine or embedded database as we talked. Right. Embedded database means like you can take it as it is and run it on your software, on your software. You can run it on your laptop immediately, directly. There is no client and server connection, right? It's a single library that you import in your code. And once you spin up your software, it just exists, right? Very. Powerful stuff. So some Dbms gives you the flexibility to switch engines just like MySQL, mariadb or persona. Another fork of MySQL. I couldn't find another database that I like to switch engines except MySQL, but I might be. I might be wrong there. So correct me if I'm wrong. Some database. Dbmses. Database management system that is comes with a built in engine that you cannot change. Postgres is an example here. You cannot change the engine of Postgres to to be something else. It's always B3, I believe. Okay. And that's it. You stuck with B-trees, right? So. Maybe that's a good thing. Maybe that's a bad thing. So and I'm trying to remember Uber switched from I'm not sure where they switched from. They switched either from postcode to MySQL or from MySQL to Postgres. I need to confirm this information. Right. I they switched for a reason. I think that part that was part of the reason. So yeah, I just confirmed I'm going to link the the article below. So they switched from Postgres to MySQL. Right. I don't know the reasons, but probably one of the attractive reasons for MySQL is the ability to switch engines, the underlying engine. You're not stuck to one engine. You can you can create a table with this engine myisam and then create another table with another completely different engine that does completely different things. So that's kind of attractive. I'm not saying that's the only reason, so I'm going to reference the article below. So they actually switched from Postgres to MySQL, right? All right. So that's that's another attractive thing.
