# Disadvantages of Database Sharding

About cones. Is there anything bad with this? Well, uh, you saw. You saw the code, guys. See how complex our clients get? It's so complicated, right? The client becomes very complicated because they are aware of this sharding thing, and they need to know, like, Oh, this input is in this shard, and this input is in this shard. Well, you can use consistent hashing to hide that, but still the awareness of the shards become really complicated As you start building a huge application, this becomes really nasty, right? It becomes really nasty to have a complex complexity into the client itself. The test, which I think I'm going to make a video about to attack from YouTube that allows you to do sharding on the spot and it's hidden from you. It's a back end sharding, right? And the client just makes the query and it does the sharding for you, which is pretty cool tech, right? So that's that's the tech we're going to talk about that transactions across shard is a problem. How do I do like two inserts that happen to have to the two tables right. And I want them to do atomically. That's almost impossible because you're essentially doing two two database transactions, two separate database, atomic transactions. It's almost impossible. It's very hard to do. Right. So you can't do transactions very effectively with sharding, Right? Right. I mean, if you do it on the same shard, that's yes, you can do that. Rollbacks are very hard. Let's say I want to insert in this table and then I want to insert in that table. But, oh, something bad happened. Let's roll back everything. Well, what does that mean? You can roll back this transaction. Also, you roll back the other transaction despite it being one logical transaction. Complicating stuff. Right. Rollbacks are very hard schema changes. Let's say I want to add another field to my URL table. I don't know, date or something. I have to add it in all the shards, right? It's like I make a change on all the shards and you can just roll it. Roll this, change slowly if you want to, but it's a little bit dangerous, right? If you miss adding one schema and one shard, but you add it to the other shards joins. I have no idea how to do joins across databases. Is that even possible? Right. It just becomes very hard to do joins right. That's why you might use horizontal partitioning instead of sharding. So your stuff that's one benefit of horizontal partitioning, not human resources. Right. So so you essentially have the two tables in the same database, right? And then you can join them, right? Instead of having the tables across multiple databases. And that's the of thing. It has to be something, you know, in the query. So if you're if you're querying for something, the key, the partition key, the sharding key in our case was the URL ID. That's what we know, right? And that's based on that. We know which database to hit. But what if what if I want to query on some other field that I have no knowledge in? So that is expensive because now you have no knowledge, you don't have the key, so you're forced to hit all the databases to know which key. Right. Where where this data reside, essentially. Right. An example would be like, hey, if you have like customer list and customer is on two shards and you're searching by name, tough luck, right? Name has nothing to do with short, so you have to query both shards.
