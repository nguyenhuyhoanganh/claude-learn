# Spin up Docker Postgres Shards

All right, guys. So the first thing we need to do is to spin up the Docker instances for the Postgres. But in order to do that, each Docker instances that we're going to spin up have to create that table, that magic table. So the first thing I'm going to do is create the SQL script that creates the table, the initial table that we will have our stuff. So let's do that. So let's go to the directory here called, I don't know, uh, sharding video, whatever. And then let's just swap into this folder and then we're going to create is let's just create an init dot SQL file. It's just a file that will have our create statement and a little bit SQL here. Let's write some SQL guys. So what I want to do is like I want to create a table URL table, that's what I'm going to call it. And if I still remember SQL, we're going to do the first fields called ID right? And that's serial. That means just incremental. We don't really care about this field much, right? It's not null Right. And it's also a primary key, right? And then the second field is you are. That's the long URL and that's the text. Right? And the third field is a URL, underscore ID, and that guy is just a character of five. I want it to be five character. Exactly. All right. So that's just essentially just the structure, the schema of the table. And because we want to spin up multiple instances, I want this to get executed every time it spin up a new Docker container. Right? So that's the first step. Looks good. The script looks good. There's no errors, hopefully. Right. And let's just write this thing, okay? That's the first step. The second step is we need to write a Docker file because our just random Postgres image won't do. We need to create our own image that essentially executes that script. So how do we do that? Right? So let's go ahead and create a Docker file, then Vim here and then from the Postgres instance, right, the image I want to just literally add one, one line of code. I want to copy the init that just the file we wrote into a special folder called Docker Entrypoint. Init db. God. And what that does is essentially the moment you copy that and you spin up the container, that Docker container, the Postgres image will say, Oh, there's a SQL file here, I'm going to execute it. That's what it does, right? So anything you can write in that SQL file and it doesn't have to be called in it. By the way, anything that you can call it, anything you want. Right. And then it's going to execute it and that's cool. That's what we want, right? So now we're ready. So what we're going to do here is let's build the image, right? And because we have the Docker file, let's build the image from which we can spin up the containers. So the next step is we want to build the image from that Docker file because that's, that's a special image. I don't want to run my postgres from just the vanilla postgres image. I want to run from my this special image and let's build that image because nobody knows about it, right? So what do we do to build that image like you do? Docker build Dash T and I'm going to call it PG Shard, right? And that's how you name tag it, essentially tag the image dash T Right. I don't know why it's called dash dash name, just like the container name, but people like to be confusing, so I don't know. And then once you do that, you do a dot at the end. That's very important because I want to build the current directory and then when you do that you push and that's done. So my image is called PG Shard, right? How about we spin up some container guys, let's spin up some containers. Right? And then finally, once we spin up the container, I'm going to spin up also my PG admin container to administrator those things, right? And then so let's spin up Docker. Run Dash dash name goes. Always a good idea to give it a name. I'm going to call it PG Shard one. Okay. The first Shard. Let me do this so you can see it, guys. The first shard. The port is five, four, three, two, five, four, three, two. And then the image is PG shard, remember? And that's it. And how about we detach it? Because we don't really want like, we don't want to occupy the terminal. So do that. And we have one shard, baby. So let's spin up another shard on port. Five, four, three, three, two. Nice and for sweet. All right, guys. So just like that, we have three instances, database instances. One is running on five, four, three, two, one is running five, four, three, three and three, four. And by default, if I connect, I'm going to show you now all these shards, right? I'm going to use pgadmin Docker container. I'm not going to show you how to spin up a pgadmin because I already did it in other video. Go check it out. I'm going to reference it here, but I already have a Docker image called Pgadmin, so I'm going to just use that one, right? So it's essentially it's, it's listening to port five, five, five five. Let's just check it out. PG admin. That's, that's the one. I have it running for a long time now. It's called PG and it's called PG admin and it's listening on port 5555. So I'm just going to use that to manage my my shards. How about that? Let's just do that. Let's show you that these shards has been executed and then spun up and then have the schema of the table and those tables have have to be empty or obviously. Right. All right, guys. So I have PG admin here. I logged in and then let's go. Let's start adding my servers, my database server. So I'm going to add a server. I'm going to call it Shard one. And the databases. Remember, it's Hussain Mac, that's my machine and it's running on port five, four, three, two. And the default users, when you don't specify anything, the properties, the password and username becomes Postgres and the database also becomes Postgres. So everything in the same thing. All right, so now we have Postgres schema, public tables, URL table. Look at that. That's the table we created, guys. And that's all this stuff we have. All right, let's add the other two. Right. Shard to its Hussain Mac and that's three. Postgres. And let's just add the third one short three. Hussain, Mac and the sport for right, then Postgres and Postgres. And we have three shards, guys. And the same exact thing. Same exact schema. That's what we want. So we are sharding setup is ready. How about we write some code guys?
