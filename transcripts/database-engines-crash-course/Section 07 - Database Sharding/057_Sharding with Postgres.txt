# Sharding with Postgres

How about we build something? Guys, here's what we're going to do. We're going to spin up with Docker. But essentially what we're going to build is we're going to build spin up three Postgres instances running on these guys ports five, four, three, two, five, four, three, three and five, four, three, four. Right. And each of them we're going to create when we spin up that Docker instance, we're going to initialize the database with a table. Okay? And that table is our URL table essentially that has like this short URL shortener, right? We're going to have some fields and all that stuff so we don't have to manually go and spin up that and we're going to spin up a big pgadmin to show you the actual shards and all that. So shard one shot, two shard three, and then we're going to write some code in NodeJS to actually would express the rest API to write to the sharded application. Let's say Hey, shorten this URL for me and we're going to figure out which database to hit and then write to that database and then we're going to read from the sharded database. How about we just jump into it, guys?
