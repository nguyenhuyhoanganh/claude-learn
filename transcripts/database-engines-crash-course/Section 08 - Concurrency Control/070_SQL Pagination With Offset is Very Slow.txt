# SQL Pagination With Offset is Very Slow

In this video, I'll explain why you should avoid using SQL offset when implementing any kind of paging. I'll explain what offset does. Why is it slow and what is the alternative for better performance? This video is inspired by Use the index Luke and I'll have a link to his blog and slides to learn more. And if you're not subscribed to use the index Luke blog What you're doing. This is an extremely mandatory blog to read for all database engineers, so check it out. So let's say you have a web application with an API that supports paging your user. Want to request ten news article in page ten? This is performed via a simple get request as shown here. The API server receives the get request and builds a SQL in order to send it to the database. Hopefully there should be a pool of already established TCP connection to your databases so you don't have to hit the cold start problem here. Page ten translates to offset 100. Assuming each page has ten records and now the database is ready to execute the query against this beautiful table. Offset by design means fetch and drop the first x number of rows. So in this case, the database will fetch the first 110 rows and physically drop the first 100 because guess what? You said it's offset, right? And then leaving the limit of ten rows, which the user will be actually seeing. Right. As the offset increases, the database is doing more work, which makes the operation extremely, extremely expensive. Another problem with the offset is you might accidentally read duplicate records. Consider this the user has now requested page 11, which is now offset 110, and meanwhile someone inserted a new record in your table. So now offsetting 110 rows will accidentally push your existing row that you have read before to your result set accidentally. Now all of a sudden you just read a row that you have read before, which is row 11. In this case. Let us jump to the practical side and test this thing on Postgres guys. All right, guys, I have a Postgres database and I have a table called News Not Test. And there is an index on the ID and I'm going to use the ID here as sense of it as like an auto incrementing ID for my news articles here. And don't pay attention to these three fields. This is the title of the article. Essentially what I want to do here is I'm going to use offset to start paging this and I want to see how the database performs as a result. All right. So let's start with Explain, analyze select title from News order by ID descending, because I want the fresh news first and then I'm going to start with Offset zero and then I'm going to limit the result with ten. And the moment I do that, instantly I get the result. Obviously we have two results. Let's explain what happens here. We're using index scan backwards, right? Because I did the order by ID descending and indexes are already ordered by default. So the index was scanned backward from bottom up, I guess. And then there is this much rows in the index, but this is not really what we want. We actually pulled is ten rows. Why? Because offset zero plus ten rows, that is exactly ten and applying a limit on top of that giving you ten. So that's not so bad. It took 0.2 milliseconds. But let's, let's apply things a little bit. Let's start with offset 1000 and all of a sudden now look at the rows here, guys. We have pulled. From the index, 1010 rows, 1000 plus ten. And that on top of that we apply the limit and then we only pull ten. You're seeing the pattern. Took one millisecond. Right. That's not so bad. Let's make it. A hundred thousand. Oh, you started to feel 79 millisecond. And the guys, I run this query so many times, so there is a lot of page caches. So this might be slower for first run, but you get the idea. So what did what happened here? We pulled 100,010 rows and then only from those stinking 100,000 rows we pulled ten. Can you imagine the work the database is doing? Guys, think about this this much rows. If if this was SQL Server, SQL Server will immediately escalate the lock because as we're touching this much, rows was going to die. But Postgres thank God it doesn't do lock escalation, right? So look, it's going we're working with 100,010 rows. We only actually need ten and that the unfortunate thing the database doesn't know doesn't know to do this because it's too late by then. All right. So you get the idea. Do a million and you can start see feeling. It's going to be slower and slower. Right? And if I restart the database, this is going to go so much slower. 620 And the first time I took like six seconds. Right. But you get the idea right now, what can I do to fix this, guys? So what I'm going to do is I'm going to do a little bit nice trick to actually accomplish that. I'm going to remove the explain analyze and I'm going to remove the offset. And I'm going to simulate an offset by using the ID itself. And then since the ID field has a beautiful index, I'm going to use that as a page. Look at this. So this is not really rocket science. What I'm doing here. I'm going to select the title and the ID and let's say I'm going to pull the first ten rows and you return this to the user. Right? And the user is responsible now to send you what was the last ID? It's so. It's so or sees. It's so it's past tense. Yes. So now what do you do is basically execute the same query, but say, hey. Don't bother returning anything. After this ID? This is the last ID I saw, so I want older pages because you're going back, right? And it depends like how you go forward or back. Right? You can play with this in your algorithm. But if I do this now, I got only these rows. And this is much, much faster. Let's take a look at the explain analyze what is what are we doing? Explain, analyze. And if I do that, look at the number of rows that the actual database worked with. Only ten. Why? Because if you do just limit ten, that's the best thing you can do. This is good. You don't you didn't use offset with it using limit with two parameter like a ten comma ten or offset that kills the performance because now you have to jump and then read limit ten tells the database, Hey, I'm looking only on the first ten rows that you can pull. There's another command called fetch First rows only whatever the first ten rows. That's another thing. You can use it, which is exactly the same as limit. All right. So if you do that, then you get back ten rows. The index will be scanned backward. But wait a second. We only need ten. Plus, we have used an index to actually filter that even down. Even better, right? We just essentially filter down the results, right? We're working with a little bit of a more more niche result, which is awesome. And now the limit applied after the limit only took ten. Now, if I do this, if I if I imagine go went back all the way to page I don't know thousand right page 1000 which is very, very old. That's what an instant. Right. We have worked now the database worked with 89 roles instead of whatever. Right. But still, the actual result set from the from the index came out as ten. Now we jump back to the heap, we pull the title field and then we limit and it becomes ten. So the, the amount of IO is much, much less. The amount of work that the databases do is way less and you saved a lot of CPU cycle and the database will thank you. Hope you guys enjoyed this video. Guys. What are the other approaches that you personally do when paging? I'd love to see them in the comments section below so we all can learn and advance and become better software engineers. I'm going to see you in the next one. You guys stay. Awesome. Good bye.
