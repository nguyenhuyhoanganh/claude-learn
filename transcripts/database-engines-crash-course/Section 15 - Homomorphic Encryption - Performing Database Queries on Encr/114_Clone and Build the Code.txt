# Clone and Build the Code

All right, guys, So I'm going to reference below this GitHub repo that have the source code that have the instructions to get started with this thing. All we need to do is clone the repo. So how about we do this now? I create a folder and I'm about to clone the repo. All right, we have the repo cloned. We basically switch to the directory and there's everything we have. And the next thing we need to do is download the Docker image that has the operating system. Whatever operating system you can pick Fedora, you can pick CentOS, you can pick Ubuntu. I'm going to pick a ponto just because I'm familiar with Ubuntu. So let's do that and then do all ponto and just like that exercise. All right. Downloading all that stuff just like that. We have it ready. In your case, it's going to take a little bit longer to load because I just previously loaded that thing. The next thing we need to do is we want to run the toolkit and do Dash P and then also provide O ponto O ponto. And just like that, this will spin up a web server using guess what uses Visual Studio code and just spin up a web server running on a secure web server here. And then we're going to deal with this on the browser. Obviously it's using a self-signed certificate, so we just need to ignore that. I'm going to use Firefox for that just to access that web page. So using this URL, now we can consume the Visual Studio code interface with everything that is in the container. That's pretty cool. I've never seen this before, so let's go ahead and open Firefox. And run this and you're going to get this. That's absolutely fine. Just go ahead and accept the risk. We trust it's localhost, so we're going to trust that. And just like that, you're going to see it is exactly Visual Studio code and immediately you're going to get a prompt here. Pick the GCC for x86 64 Linux. Now this is the the GCC compiler for C plus plus and it's going to compile the code. And once we have that, the next thing we need to do is click this button build and that will actually build the project. And this will be available for us to run. Then after we build the search, the DB lookup, which is this application, country DB lookup is going to start building that. I'm going to show you the code a little bit in the in a second now. All right.
