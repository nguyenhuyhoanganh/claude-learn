# Database Permissions and Best Practices for Building REST API

What is going on, guys? My name is Hussein, and in this video, I want to do something a little bit different. I built this a little bit of to do application, which has Postgres on the back end and NodeJS on also the back end I guess. And it was a very simple HTML5 app that literally you can click add a to do and then if you click it will literally delete that to do so, it was a little bit of a routing app rest API, nothing fancy. However, I want to point up some a little bit of a not so best practices that I used in this app and I'm going to show you how to do better essentially, especially on the back end. What we're talking about the connection itself, right? And what can we do? And I talked about that where in another video where some of the best practices that you can use when you connect to a database, you need to establish a specific database user with specific permissions for each route. And yeah, it's a lot of work, but it will pay off in the long run. It will give you more security. It will basically group your your routes. Like if you're reading, you don't need to like what I'm doing right now. I'm looking at the Postgres, I'm looking at as admin at the back end and I'm reading, right? That's just a waste. Plus, if someone injected some sort of a malformed SQL to inject SQL, they can drop my entire database because I am. I am admin. Right? I'm logged in as admin, so it's not really a good practice to log in as admin on your web application. So I'm just I'm going to show you how to create a certain users given a certain permissions and create different pool of database connection. And I talked about pooling database connection pooling. Guys, check out this video if you're interested. So how about we jump into this video? This is what I'm going to trying to do in this in this video. All right, guys. So the first thing to explain here, what we're doing is I have a pool, a connection pooling. This is a feature in the PG library that I'm using for NodeJS. And what it does essentially is it starts with a certain number of database connection. And when you issue a query, for example, I want to read these to Dos, you can just use the pool to issue a query. And what that does it, it will reserve one database connection, one TCP connection, send that query right. And then once that query comes back, it will return the result and then release the connection back to the to the pool. And the reason you want to do that and you don't want to use a single database connection and send all your queries in that single client is because you cannot guarantee that which request comes first and which response comes first because of pipelining. And we have the same problem with Http, right? So it's a very bad idea to use a same TCP connection to send multiple database queries because some of them can return before the other. And you cannot guarantee what is a request in this case. Right? So that's in general, that's the idea of this. And and we're using pooling for this idea. So pooling and you can control pooling. And I talked about that in another video. All right. So what I want to do here, what I'm using for the pool is, is the Postgres user, which is so bad, right? No need to use this this God mode to connect to the database. So what we're going to do here is since I have one root that does read the to dos and I think one root that create the to do and one root that delete the to do, I'm going to create three users I can do with two but I want to be fancy a little bit create three write one with only read one with only create and one with only delete. All right. And I'm going to use these each pool create three type of pools and use each for each root. So how about we do the code first and then we create the users? Let's do that. All right. So another thing bad thing I'm doing is I'm hardcoding the passwords in in, in the code. That's a bad idea. The best thing is to use environment variables. And there are many ways to do that. And it's just it's a little bit hectic to for me to use the environment variables for tutorial. But you know the ideas guys is a well known thing. I might create another video and show you how to do that or maybe I'm going to do it in this video. Who knows, right? But it's really simple. You just process dot env and then dot the variable that you pass in. Maybe. Maybe I'm going to do it later. No problem. So what I'm going to do is I'm going to create a DB read pool, right. And this is the user for DB Read is called DB read and the password is Here's the thing guys, about passwords. With database users, you have to create the longest possible, most incomprehensible password ever and store it in some sort of a key vault right where you read that. That's the best practice. Don't store it in the code. Obviously. Don't send it. Just I guess you can store it in a text file somewhere as a config that is not pushed to the to the source source code, but you can use that as well. But I'm going to use db read db read because, because why not? All right. Because if I, if I implemented every single best practice in this video is going to become like three hours, right? You get the idea. I'm going to talk through things that is best practice, but I'm not going to implement everything because it's just too much, right? So I'm going to create three of those. DB read pool, right? And DB create pool. I'm going to create DB create. Let's all use lowercase and. DB Read this, guys. DB Delete. Pull. DB Delete, delete, delete. Right. So three pulls, guys. Three. Different pools in the Connect method where I'm going to connect. I'm going to to actually connect three. All the pools together. Right? Which is the DB create pool, DB, delete pool DB, read pool. And this is awesome guys. Right? Because these pools will start maybe it depends on the number of connections, the minimum number of connections that you start with. And this is configurable and I talked about that in the pooling video and you can start like, you know, let's say you have so many reads, right? So you can start with a obviously more people read than right In general, it depends on the use case. So you can start with that. A lot of reads pooling says I got ten and the delete obviously no one delete. So you can start with zero and if someone attempts to delete, we're going to spin up a new DB database, DB, DB, DB connection and then obviously we're going to keep it until it dies. Keep a lifetime. All right. So three stuff obviously we let's go to each route and start using this stuff. So the read Todos function uses the pool, but we don't want to just use the generic tool. We're going to use the DB read pool to select an ID text from todos. Another thing we don't need to do is this is bad. We should again, guys, I'm just going to talk about the bad practices here and what you can do to to elevate them, but I'm not necessarily going to implement them in this video. So this is bad because this is an called unbounded query. Some some people say, okay, what did you say here? Unbounded, but because of my accent, maybe you can engage it. But it's called unbounded. That means there is no bound for it. That means if if you have like 3 million rows here, you get a return. All the 3 million that's bad. Always limit this stuff. Either use limit zero ten or do some paging or something like that. Right? Don't do that. Don't do paging client side. That's a bad idea. But it's a tutorial. So. So yeah. DB read pool DB create to do a we're going to use the create pool and we're going to create it. This user going to only have create permission. So it's going to be able to insert, but it's not going to able to delete. Does that make sense? Right? So even if you snuck out into the insert that if someone managed to do a SQL injection here, they won't be able to drop my database or they won't be able to delete my stuff. They might be able to insert some stuff, right? The delete route is the is the riskiest here, right, If you think about it. So that's why obviously I don't have any authentication whatsoever on my app. Right? So that's another layer of protection that you can add. Add them. I guess this is a client side front end, right? With JWT or just normal authentication where nobody can actually delete unless they own that stuff. Right. And you can add multiple stuff. I'm just currently deleting this particle or item, right? And I'm using this particular syntax which avoids a avoid SQL injection as well. That's another way to avoid SQL injection. But that function, the creator of this library managed is doing the SQL injection for us and injection is doing the sanitation sanitation for instead of doing like this one. Right. That's. That's just way better. I think that's it. If I restarted this, let's see if we get an error. Obviously, we're going to get some errors. Now it says, okay, what the hell is DB create? I don't I don't have it. Right. So now we go to Pgadmin and we start creating all these beautiful rolls. We're going to create a new user or role called DB read write roles. The label read. Why did we call it de Buried? DB Read. Dibby. Great. Okay, so db read, write, read. Only access to the to the to do table right. And privileges they can obviously log in. Are they superuser? Hell no. Great roles. Hell no credit databases. Hell no update catalog hell no. Inherit rights from the parent roles. Nah. Okay. I don't know. I doesn't have a parent so it doesn't matter. And security, right? Well, that's it. That's all we need. The trick is they can they log in or not? And there is. There is a reason where you you sometimes don't need this role to be log in because you might have just a role with a set of permissions that you can assign to different tables, but it doesn't have an actual user behind it. So we save this stuff and we're going to again create an create DB, create, create user, a user with only create or insert permission and the password is DB create and what they can do, can they only log in right save then we can do boom DB delete a user with delete permissions and db delete. Did I enable permission privileges? They can log in. Awesome save. Now if I go to the Todo database, my Todo database and I go to schema, I go all the way to my Todo table. That's my beautiful table, right click properties. Right. There are sequels that allow you to do the same thing I'm about to do, but. It depends, really on you. What do you prefer? And now I'll go to Privileges. Obviously, this. This. The owner of this table is Postgres. And that's a good thing, right? You want the owner to be someone who has, like, very great permissions so that the measly different other users that consume this table only have so much permission on this table. Right. So they can they cannot, like, drop your whole table or something like that. So I'm going to add a role called pkg create. And this permission is only insert because it's a great right. Can they select? Uh, I didn't really select. So let's try without selecting maybe because I don't know what the internal library does. If the insert does a select, then we might run into an error. But let's, let's give it the minimum permissions here, insert only, right? So a and then I'm going to create another one called DB delete. And this guy is only can delete. I don't have a I don't have an update route. So you might want if you have if your app has an update route, you might do the same thing as here as well. All right. So this is. Good, good. Now we're going to create a DB read which only. Oh, my God. No, this has to be only select. Right? Grantee must be selected. Well, I selected it. Didn't I? They. Debris deletes. All right. Looks like we're good. So we have create. We have create insert. Why does it say a I don't know why it's called a. All right. The read only read. And if you delete only delete, how about we save this puppy? And now if I start my app, let's see if we get any errors. No beautiful errors. So now let's go to my app and let's see if it works normally. If I. Well, it just we know reading works now because I just read my existing row rose right because what do you have rows we have this Asdf thing okay if I insert another thing blah blah blah. Oh, it failed. Okay, let's take a look. Let's take a look. It failed. We failed to insert. As I told you guys, I wasn't sure why why it would fail. So let's, let's take a look. So create to do is what failed, Right? Let's see. I did use the create pool. I am inserting into that. And maybe we just put it here breakpoint and see what exactly the problem. So another thing, if you notice, I didn't return the error to the user because one of the best practices is you don't need to tell the user what what is going on in your database, right? So you need to create your own errors. So if you return E that's kind of bad because sometimes e will contain the source code and contain the line numbers. And, and hackers might use this to give insight into your application, where your code is, where your stuff is. And I run into a problem. So, so that's why I'm returning just false and true. But obviously that's also confusing because you need to tell the user like now, right? We failed, but we don't know what's going on. So you need to specific error exceptions and and stuff like that. Okay. Why do we have here. Oh, come on. It says permission denied for sequence. Oh, that's beautiful. That's beautiful, guys. That is so beautiful. Okay, let me explain what's happening, okay? I completely forgot about that. So. So this is actually powerful stuff, So. If I have two two columns here ID, which is which is a primary key, which is serial behind the scenes, this actually creates a sequence. And a sequence guide is a database construct that basically when you select it, it gives you the A number and you select it. Again, it gives you another number. It's just a sequence of of numbers. Here's the thing. I gave my DB create user access to my Todo so it can insert. But but the the act of inserting a new row in my todo table triggered the sequence. Right. And I need to get a new sequence. So what this database does, it reads from the todo id sequence in order to get the next sequence. But guess what? The user do not have permission on reading that sequence. So we need db create to get access to the sequence. Not everybody else, just a read should be enough. Right? And and hopefully that read should be enough with the select in this case. That's a good practice. That's a good that's good. I'm happy that we failed here so we can learn the process. Let's go ahead and run this beautiful. The code, by the way, guys, will be available in the description below and I'm going to share the source code with you. I need to cope. I actually. All right, let's try this app again. Boom! La la la la la la la. Boom. Oh, another error. Permission denied. Again. I thought I gave you the permission. So is is select is not enough Permission denied for sequence to dos. ID sequence. But I gave you access. That's interesting. What? What what exactly do you need? Apparently, a select is not enough, guys. All right, let's read it again. Despite me giving you the security, I give you a read with a grant permission. No, no, no, definitely not. Maybe usage. That's a different. I don't know what usage means here. There's a difference between select and usage. Let's give it usage. Maybe we'll read about that later. Refresh. And then boom, boop. Okay. It looks like we did it, guys. So the trick was not select per se. It's usage. The idea reading a sequence. The current sequence is different than using the sequence to generate a sequence, apparently. Right. So that's the difference here with. With with the sequence. We need to give them two permissions DB sequence and db grant and select well the I mean usage sorry, usage and select. That's awesome. How about delete? Does delete work. Did it. Oh. I don't think delete is working guys. Let's take a look. Why is delete not working? Because if I click. Let's put a ah, what the heck? Boom. All right. DB delete pool. Boom. Why? Permission. That permission denied for table two dos. All right, guys. So I think I know what's going on, but let's first fix the bug that in my client side. Why? When I click delete and I fail and I return false, nothing happened in my client doesn't tell me anything. So I'm going to go to my index page and see delete, where is my delete method here? And it says, okay, if not succeed, that should have failed, right? But I'm not even getting that, so I'm going to do a little bit debug on the client side here to see why why that's a problem. So let's just do some a little bit of client side coding here. It's been a while. Beautiful code. Let's do that boom and then refresh. And then let's just hopefully I clicked on it. Did I? Yes, I did. Now, this will fail. Go ahead and fail. False. Return. False. We got back here. The success is true. What are you talking about? How is this true? That's wrong. That is wrong. How is the result true? We clearly right. Let's do it again. Let's do it. This side here. I'm going to move my break point somewhere else and let's just remove these. So I did remove these. Okay, so we return false. Let's do to delete. To do. Oh, look at that, guys. We're not using this stuff. That's just dumb, man. All right, so the bug here is. This should be like that, right? Roosevelt sexes equal this? We're not actually setting that thing. That's just bad. Okay. Now if I. Let's try without debugging because I have confidence this will work if I do this. Oh, that's it failed. That's exactly what I want. Okay, now it failed, and here is why it failed. What did we give the delete permission, guys? The db create db delete. We only give it delete, but. If you look closely to what what the delete user is doing. It is not only deleting, it is actually it needs to identify what what are the rows in order to delete it. So technically it needs to find the ID and the database and then delete that. The act of finding is a select. So you need to give it a select. Okay. So that's that's what it was missing here. So we go properties. Sokolow Rotate. DB Delete. Give it a select, which is not a big deal. And just like that, refresh. Now we have refresh here. Boom, deleted, add to do bar to do another doo doo boom, boom, boom, boom. That's awesome, man. Now we have a beautiful application with specific database user for each rest route. How about that guys? Hope you enjoyed this video. Give it a like if you like it, share with your friends. I'm going to see you in the next one. You guys stay. Awesome. Goodbye. Check out the other content on this channel. By the way, guys, we talk about back end mostly in this channel. That's my specialty. All right, I'll see you in the next one. Guys, stay. Awesome. Goodbye.
