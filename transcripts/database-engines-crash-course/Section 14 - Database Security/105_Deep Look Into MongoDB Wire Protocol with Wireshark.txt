# Deep Look Into MongoDB Wire Protocol with Wireshark

What's going on guys? My name is Hussein. And welcome to another episode of Wireshark. Them all, where we wireshark every single protocol in existing and back end engineering. So how about we jump into it today? Today we are wired. Shocking. Why shocking. Wireshark King MongoDB is going to be extreme fun. So MongoDB guys is a NoSQL database that's a key value store. And I already spun up a mongo Atlas database with a user and and all that stuff. They, they give me a free instance with three, three shards, three clusters, one cluster, three shards. And we're going to just basically run this code which connects to the database, does a little bit of sleep just because I want to see what's going on there and then connect to the database called Thunderbolt, which I created there, gets the employee and literally find the employee that's named Hussain. And then execute a search cursor. And then once we get the search cursor we print it. And then after that we close the connection. So I'm going to show you that. And then slowly I'm going to start breaking up the connections one by one after each operation. Right. So how about we do that. So here's a Wireshark I filtered based on Amazon's public IP addresses, which is the which is the provider I'm using for MongoDB Atlas for MongoDB Atlas. So that's the IP address. So now anything going to that IP address will be logged here. And another other thing I did I used SSL key log npm package for NodeJS to spit up the session keys. Because MongoDB is always encrypted by default. Uh golf upload for MongoDB team. That's awesome right? So because it's encrypted I won't be won't be able to see it in Wireshark unless I decrypt it. In order to do that, I spit up the key logs, right. The key log, which I showed how to do that in Http2 video when we Wireshark Http2. And once we do that, I fed the that key to. 222 Wireshark and the TLS option here. Where is it? Right here. And that basically will allow Wireshark to decrypt the keys, because no one in the middle can actually decrypt anything, right? Until unless they have the final exchange keys. And this works on any cipher, whether Diffie-Hellman, elliptic curve, Diffie-Hellman, anything. Right. Because you're at the end, you're at the client. So you do have the keys. This is just an option for, for for for NodeJS to spit out those keys so we can use them for debugging purposes. All right. How about we jump into it. So I'm going to go ahead and run up. All right I'm going to go ahead and run node test.js. And what this will do it will literally connect. Spins. Brent and then close. That's it. So Prince Hussein, which is the employee we found, and then close the connection. Let's go to beautiful, beautiful Wireshark. Look at all this stuff. So that's what I love about Wireshark. It actually know that we're dealing with MongoDB. Look at that. It actually knows the protocol. It does all that stuff. So how about we go through that stuff and then slowly after we go through all the operations until we close the connection. Where's the fin? Right here. I don't see. Finn. What is the Finn? Finn? There is a Finn. Yeah. Until we close the connection, let's go through that stuff. So Mongo uses TCP, so obviously syn ack ack. So that's the three way handshake, which we talked about right here. Go check it out. So every TCP connection has to be first agreed on the sequence number that will be used. So basically a number and you have to to agree for a client sequence number and a server sequence number that will be used to label those packets that you send across the wire. So they can be reordered so they can be retransmitted if they are lost and things like that. Clienthello TLS 1.3 go beautiful one go look at this. Number three. And this is my Mongo client NodeJS right. And so look at all this beautiful cipher suites, powerful suites, powerful suites. Look at that stuff. Elliptic curve Diffie-Hellman. Yeah there is a Diffie-Hellman one right. It's a all right all right. That's good stuff. Good stuff, good stuff. All right. That's not very secure if you ask me. It depends on how how is the bit size for this. But yeah look at this. You can use any of these encryption methods, cipher suites methods. Right. And then since we're using TLS 1.3 server name SNI, we're going to this shard. So shard 0001, that's the primary shard that we're hitting. And then doing all that stuff support support session tickets. That's part of the TLS or is it the part of Kerberos? I'm not sure about that. I don't think you can do Kerberos and handshake supported version. This is the most important thing. We support 1.2 and 1.3 and we send that stuff. Right. I'm not going to go through all this. We went through the details. The server says, yo, I love you a lot. Let's use TLS 1.3 to communicate. But we have to say 1.2. Unfortunately, because there are dumb routers in the web that unfortunately blocks these 1.3 on the version. So this is fake. This is not really 1.2, right? The real version is somewhere right here where we actually say hey, we it is 1.3 supported version. So there you go. It's under supported version. That's why we had to add a new key to just to do that stuff. All right. So we exchange the key and then acknowledge send the certificate. The server sends the certificates and all that stuff. We talked about that many many times in this channel. Verify the certificate that's the server and acknowledge acknowledge some TCP laws. And there is the first thing we send a query. What is this query. All right. Mongo wire protocol. We're using the wire Mongo protocol. So it's a specific protocol for Mongo. It's around 300 byte. Not that big. That's good. And look at this. We're querying a database called admin. Technically I didn't say any of that stuff. The admin database is not created by me. I didn't create it and didn't know anything. And there's no collection called dollar Sign CMD. That's probably some some built in collection name that we just apparently use. All right. And then we query this is the thing we ask for. We ask for so many other information. We ask for the compression type we ask for. That's the SA SSL forgot what stands for this is the the Ldap alternative. I forgot what it stands for. The master is is the one I'm connected to is the master. I want to connect to the master client connected the driver I'm connecting through from NodeJS. And this is the version of the MongoDB client and all that stuff. Look at this compression what kind of compression I'm using and that's it. And the server obviously acknowledges that I received your query and then sends back the reply. And the reply in the reply says, hey, here's the list of hosts that you can connect to. This is awesome by the way guys. So this is the three shards that we talked about. So I connected I believe, to this one. This is the master we're going to find out. And they gave us like by the way this is this is another two shards that you can connect to. So it's almost like a client side sharding. Right. And that's how MongoDB works. The client side is aware of the shard. All right. And set name the atlas we're using Atlas is master. Yes. You're connected to the master or you connect to the secondary. Nope. Are you connect to the primary. This is the primary one is the primary. Right. And that's all that other stuff. Look at that. Electable. Whether this is electable or not is region. Where is it? It's in the east. All the information is there. By the way, this is encrypted, guys, but I have managed to decrypt it because I'm using that we talked about. Right. Look at that. What other stuff. Election ID don't care about that really late last. Right. Max base. And the binary Json is that is that it? I think so I'm not an expert in Mongo so I don't know what what all this stuff is. Most of the stuff I know basic stuff. The versions meanwhile version Max version, this is the response. This is a little bit lengthy response huh. And that's it. So query. And here's the thing guys. You will notice that. After that. The client also sends another query, and that's what left me a little bit baffled. So we're sending another query around 124. And what we're asking here, we're doing the SASl start thing right. So let's let's Google this. All right. So it's simple authentication and security layer protocol. All right. So simple authentication security protocol. So that's the protocol that they're using. Okay scram. That's another hashing algorithm that Mongo uses. And apparently this is they they are agreeing on this stuff. I didn't know what scram is. I had to google. Look at this. Scram. So it stands for Salted Challenge Response authentication mechanism. Scram. So this is apparently an authentication mechanism. And there are two there are many types. There's Sha one. Why are we still using Sha one guys Sha Sha one is dead. It's been proven to be weak. So and apparently what am I using here? I'm using Sha one. Uh, I probably is not a bad idea. No big deal, because this person have to also decrypt in order to get into the Sha one to, to to break it. So I don't see I don't see it as a big deal unless you guys disagree. What do you think? So yeah. So this is the first SASl query. And then it replies back and it says. We haven't conversation. So this is like a conversation. So there's a lot of chattiness just to establish the MongoDB connection. All right. So we have done are we done. False. We're not done. So it's almost like a conversation I don't know the SASl protocol and how it works. But this is extremely chatty. Look at this. We're going to see that this is the first query reply the second query and reply the third query almost the same thing. We're going to the admin cmd collection. We're just having the conversation again there's some negotiation that is going on. And then we get a reply back. And it's not done yet. And then until the fourth query, the fourth round trip, we say, yo, let's continue sailing. And then we say done and then we're done. The connection. So this I believe this is just the connection part. So we're and we're going to see that later when I continue that I think we're just here. That was just here. So a little bit slow, if you ask me. Right? And this is the first request. Request extensible message format is this XML? Is that extensible message format. Is that just XML? That's what XML stands for right. Extensible message Markup Language maybe it's different I think it's a different thing. All right so what we're sending the query the user sending the query to find right. We're sending a find. That's the find right here. Right. So this we're going to find out that this doesn't do anything. This is all client side logic. We're getting the database getting the collection until we want to do something. It's the client. And Mongo is lazy. It's a lazy load approach right where I found out by actually stopping in each step. And there are no network packets sent when you're doing this stuff. Only even here, you don't send anything. It's only here actually. We're going to find out that. Right? So find we're going to find. And on the employees collection did we specify the the collection here, the database I mean the database. Did we specify the database. We specify the collection but not the database. So that's the filter Hussein I want to go to find me Hussein the name Hussein. Return key. Show document. Look at all this stuff man. The timestamp. And there's the DB Thunderbolt. Right. Awesome. And we acknowledge the server. Say I got your request. And now here's what I think. This is just weird. It's not really a request because it's coming from the server. So it should be a reply or a response. So I think this is a problem in Wireshark, King, Wireshark, just expanding the messages and treating it as a request, whereas it is actually a response I think. So. So we're getting cursor a beautiful cursor. This is a client side cursor. We're going to show that because we asked for everything basically. Right. We asked for two array. And we get back an array of every single element which is one basically. And the id the object ID of the element right. And the string which is name Hussein. If you notice here this is five f2 five f2 and the name is Hussein. And then we get the, uh, the collection and the ID and all that stuff. Okay. The cluster time. For some reason we're exchanging the cluster time a lot, and I don't know what's the purpose of this. That's very interesting. I think it's just for syncing purposes. That would be interesting to know. All right, some lost packages. No problem. Another request. Hmm. We didn't send another request. That's weird. So if we look at this request, that request. I didn't do that. It says say in sessions. It's like almost like it's ending the session. Well, there is a document and it says ID and this gives you a value A5, A5, E8 eight. I didn't do any of that stuff. Only thing I did is print it and just close the connection. So maybe that's part of closing the connection and that's the client sending it, right? And then the timestamp and we're getting. Oh look at this admin. I didn't do that. We're doing something to the admin. The MongoDB client is sending a request to the admin. Oh look at that. This is part of a negotiation with the shards. Look at that. Says hey I prefer the primary. Well we are connected to the primary. All right. What's wrong with you, sir? Awesome. All right, all right, all right, all right. This is good. And then we get a response back from the server. Saying what? Server saying, okay, one cluster time. Don't really care about the cluster time now. And the operation. That's nothing. It's just responding back with a response. So I didn't send that as a client, as a user, as a developer, I didn't write that. It's all Mongo. And then obviously this is the close of the connection. The fin. Where is it? Fin EC so the the client initiating the closing of the connection. Look at all that stuff. All right guys, so here's what I'm going to do now. So we went through all that stuff. This is a very basic MongoDB request to pull all that stuff. How about we spice things a little bit and I'm going to copy this thing. And do it right here, right after the connect. I'm going to close the connection. And we're sleeping here. Just because I noticed that. I think that it's almost like a bug here. When we don't close, we close immediately we get a reset on the on the, on the, on the TCP stack. So I'm just adding milliseconds. So we avoid the reset. So let's go ahead and clear which we shouldn't get. If you think about it we shouldn't get this reset lesson. And one second. And then done. Let's take a look. All right. Three way handshake. TLS handshake. All the way we get a query reply a query reply a query reply a query reply. That's where we show it, right? And then a fin. So for round trips that is. Man. If you the next time you establish a TCP connection a MongoDB client connection, think about this as a back end engineer, think about what you're doing right. So pull this connection as much as possible if you can, and and eagerly load them. Because lazy load this thing, the user will feel it because it is extremely slow. Especially if you're if your MongoDB is all over there. And this is another thing, guys, your MongoDB client should be very close to the to the to the MongoDB database. Mine is it, mine is in the west and client is the West. And the database is somewhere in the east, right. So that's bad anyway. But yeah, this is just negotiation and authentication. I still don't understand why do we need SASl. To be honest there's a part of Kerberos authentication. I don't need to do any of that stuff. Right. I mean mutual TLS can solve this problem to authenticate me as a client if that's what it's doing. But I might be wrong. I might be missing something here. All right, so let's move this a little bit. Just this sense for four packets. Yikes. What the hell are you doing? And now I'm going to. Do it all the way here. Right? So this, this and this. And you guys are going to notice that nothing I'm saying nothing will be sent to the to the server after that. It's the exact same thing. Exact same thing. Right. So. Let's go with him again. Query. Reply. Query. Reply. Query. Reply. Query. Reply. That's it. And Finn. So that whatever we did here, all this work, it's all lazy, loaded. All of this is a lazy loading client side, which is good thing. Right? So this is to avoid extra round trips. Right. And DB Thunderbolt this is. Yeah. Give me the Thunderbolt database. But why do you if. No we're not going to use it immediately. I'm not going to send a request to get you the thunderbolt. Right. So let's let's pull this connections the request as much as possible. So okay. You want the Thunderbolt database okay. You want the collection employees okay. I'm not going to send the request yet. You want to find the the employees that name Hussein. Okay. I'm not going to send it yet. Why? Because technically, you didn't consume the search cursor yet. Because what other methods do you have, guys? Uh, uh, engineers let me know. Mongo experts. There is a next. Is it next? Yeah, I think it's called next. So this is next. We'll do something else than to array for example. Right. Once I do next or to array that will do all of this in one trip. So they're kind of optimizing things which I'm going to give them. Uh I'm going to give them the props for that. That's good. But look at this. Look at this stuff. And here's my message. Request. Message. Request. Hear their request. Extensible message format. So that's the extensible message format that we sent, right. That's our query. And the second one is basically the response for this right. Oh that's the that's the first one. Sorry. That's the second request that the client just does send with that. We don't know what's going on there. It's sending it to the admin database. So some sort of a messaging. Right. Just like we saw in WebSockets there is a was it WebSocket. Uh, the heart, the heartbeat thing, the ping pong or something else was it was a Http two, the messaging, the so-called the system stream right. Or the magic stream where they know the magic stream. It's basically a specific stream just for exchanging messages. That's what that's what was it. Right. So this is like, I think the exchanging of information about the cluster, about the stuff like that. But yeah, that's the idea of this stuff. And then finally we close the connection. All right. The last thing I want to do is something I didn't do offline, but I'm going to do it with you because I'm I'm very courageous. Not really because I'm going to try this. I'm going to see what happens when I do this cursor dot next. So this will give me the next result. We'll see what will happen when I do this right. Const result equal this. Let's just do this and see what will happen. And just just. Sure. Let's just print it to that. Oh. That's clear. Clear. Clear and then boom. So. The normal queries. Normal queries. Extensible message format. The same thing. It's the same thing, guys. It's the same thing. Maybe it's not really an array. So this is a request okay. This is the response. Ah look at this a little bit different. It's a cursor. This first batch it is an array huh. I guess it's the same thing. Mm. All right. Never mind. It's just giving you the same results, right? But just one at a time. So, so this is this is a whenever you want something, you just ask for it and you get the second one and second one, second one like that. So there will be a round trip for each one of them. Versus if you do two array they will all be brought locally. So kind of a server side versus client side. Curser. All right guys that's it for me today. That was Mongo Wireshark MongoDB. What should I Wireshark next. Let me know in the comment section below. I'm going to see you in the next one. You guys stay awesome. Goodbye.
