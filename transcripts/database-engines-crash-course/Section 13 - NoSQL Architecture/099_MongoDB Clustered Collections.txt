# MongoDB Clustered Collections

In version 5.3, MongoDB has introduced a new feature called Clustered Collection, and I want to, uh, going into a deep dive into how this functionality work and how it can actually improve the performance of your reads. And if your writes. And before we do that, we really need to explain how Mongo used to store collections and documents in these collections and the the in on disk storage representation, if you will, and then how cluster collection can actually improve that. And of course, everything we do in software engineering that has a cost. So when we introduce a new feature, it comes with its own limitations. And it's good to understand these limitations. Right. So let's talk about clustered collections in MongoDB. So before we talk about clustered collections in MongoDB I want to talk about how regular collections or unclustered collections, those regular collections that you create in version 5.2 or earlier, even. 5.3 you have an option to enable those. Right. The cluster collection. But how does the normal collections work? You see. And in recent versions of version four and newer, uh, Mongo acquired this data storage company called Wiredtiger. It's a database engine that allows you to do this Mongo, that allows you to do acid multiversion, concurrency control and concurrency and other things, you know. And, uh, the way Wiredtiger and MongoDB stores. Thing is, they have a hidden index. Now, the history behind this hidden index I discussed in my deep dive on the MongoDB ODB architecture. If you want to go there, check that video out. But essentially that hidden index, the key to that hidden index is called the record ID. And you. There's no way, as far as I know, to see that ID or that key, that record ID is a 64 bit integer. So eight bytes. And in that hidden index the key is that record id and the value in the leaf pages of that hidden index is the document that you store which is the basin document, the binary JSON and it's compressed and all that jazz for efficiency reasons. So when you submit a document to store in a collection, it goes into that hidden index that you don't see. Right. But then you say, you might say, Hussein, I don't really deal with that record. I have an ID field, an underscore ID field, or I can create my own other indexes. Right. So that other ID field that you actually visible to you that is auto generated for you. It's actually another B-tree index. So there is one B-tree B+ tree index that's hidden, right. And that's technically already clustered around that record ID with the documents, and the documents are live in the leaf pages. Then you have the underscore ID, which is your the object ID of that document. And that underscore ID field is actually around 12 bytes. Right. Um, in size, that key and the value that in that b-trees when I say value that, when you do a traversal on that tree and you find that ID the corresponding value, because B-tree is always a key value at the end, right? The leaf pages stores the value where of that key that you just felt, and the value is actually the record ID we talked about. Okay. So we have when you technically search for a document by its ID, you do the get on mongo the command get on ID, you actually do two reads and MongoDB document is actually called it reads. It's actually kind of an understatement because it's not just reads. It's it's a it's two B-tree seeks. It's not the same as a just a read, a read and indicate that it's a single I o not really write a read here or a B-tree seek you're actually traversing the internal nodes to find on the ID field to read on the ID index, just to find the record ID, then that doesn't give you the document. You have to take that record ID and then go and then go to the hidden index and do another big tree traversal, right? And if you know how B-tree works, you check the root and then you intermediates, and then you go to the leaf and then you fetch the leaf page. And then from the leaf page you find, oh, here's my record ID and here's my documents. And if you're lucky and that page holds many other documents, you cache this whole thing in the buffer pool. Hopefully the next record order you request is in the same document. Right? But unfortunately, it never is because this thing is always random that it causes random I OS. The architecture of non-clustered collection, which is the default, requires you to do a double read or double B-tree seeks. Right just to find the document we're looking for. Same thing for a right when you when you write a document, You write it in the hidden index and you update the B-tree structure for that hidden index, you get a brand new record ID, you take that record ID and insert it into the appropriate location after generating generating a new ID field for you, and then update that index. So you're doing double writes effectively. So to summarize on cluster collection, we have the ID field as an index. And that points to when you do a search you find the ID but then you don't find the document immediately that you're looking for. You actually find a record ID a pointer. And when I say a pointer, I don't mean a c pointer here or a memory point or no, I mean an actual value that you use to do another traversal on another index, which is the record ID, and that will give you the index, the document. So there is two B-tree traversals in Now that we know that the cluster collection, which is in 5.3 and 6.0, actually removes that hidden index altogether and then makes the ID index, your clustered index that becomes your cluster index and means the document ID is stored inline in that ID index. And of course, naturally you only can have one clustered index because that's what you have only one way to store your data. So now when you do an ID lookup, you do you go use the ID index. And then you immediately find the ID key. And the value now is not the record ID anymore. It's actually the whole document, the basin document. Right. And then you return it after you decompress it and do all that jazz. So that's that's what cluster collections is. So you're doing a single read, right? You're doing a single, right? You're changing one thing instead of two. So your queries are faster, your updates are faster, your inserts are technically faster. Even your range queries are faster. Why? If you're doing a range query on the ID, give me all documents between this ID and this ID like. And because the ID field is is incremental, it has a timestamp, right? I think in the first few bytes. Then it's actually very nice to do a range query in a clustered index, because we know that these IDs will live together, will be next to each other. That will give us fewer page IO to disk, and we're going to give and all the documents that we're going to look on the same hot pages that we find. Right. Compare this to the older design, where if you do a range query on an non-clustered index, all you're going to find is record IDs, Right. But this record IDs, can you guarantee that they are actually exactly next to each other in the header index? Maybe. Maybe not. So that will cause actually random reads on the hidden index. While sure that range query was sequential, the randomness to actually get the documents to go to the hidden index that is random and random is bad when it comes to databases, right? We try to avoid random because when you do random, you're paging, you're reading from multiple places in disk. You're lowering the cache cases of logical reads, which is basically page hits or memory. And as a result, things can get slower. And as you pull in fresh pages that are random, you have to discard older pages that you didn't use for a long time. and most of the time, if you discard it all, page chances that you're going to need it again are very high because everything is random and you have limited amount of memory. So with that said, we know what a cluster collection. We know cluster collection. Let's go to the doc and read this and discuss more. All right so this is the MongoDB official documentation. Clustered collections new in version 5.3. Starting in MongoDB 5.3, you can create a collection with a clustered index. Collections created with a clustered index are called cluster collections and you can only have one clustered index. We talked about what a cluster index is is it's when it's when your data is in the index. Right. And it's also called the primary index in other databases. So every time you hear the term primary index it is mostly a cluster index. Postgres being the exception. And I think Oracle also is an exception. Unless you create an index organized table specifically, right by default. And even SQL server like has this idea you can create a primary key, but you can make it a clustered index. So actually clustered your data around that index. Right. As opposed to have it just as a heap table somewhere and point to it. Benefits. Because clustered index stored the documents ordered by the clustered index value. Cluster collection have the following benefits over non cluster collection. Faster queries on cluster culture without needing a secondary index such as queries range. Equality and equality. Comparison with on the cluster index. So if you're doing something on the cluster index it's fast. Because if you're looking for a certain document, it's inline in that document, in that in that index there is no other jumping to another hidden index to get the document. It's right there. And we talked about range, how range scans are really nice. If you go, hey, give me all the documents or just not all the documents, just all the particular fields in a document in this range, right? Again, not cluster. Cluster collection have a fewer lower storage size, which improve the performance of the queries and bulk inserts. So the reason is have lower storage size because we used to store the hidden index with inline the the collection and the data the documents. And then we also had the ID index in a separate structure. So we're storing we have we have two places to store things. We have this additional data structure that we manage. So more size right. Plus the ID index is not is massive right. It's 12 byte key right. So it multiplies right. Plus the record. The record ID was eight bytes. So you're storing eight bytes and storing a 12 byte keys. So for each document. And add to that the overhead of the key management in the B+ tree, the roots, the intermediate nodes. It all adds up right. So then the the storage lower you might say I don't care about storage, but storage kind of almost translates always to I o. Right. Because if you're storing stuff in multiple places, then you need to read multiple places to do an I o in multiple places to fetch the thing that you want. Right? And that's the problem, really. Cluster collection can eliminate the need of a secondary time to time to live index. So they have a feature here called the Time to Live, where you can set a essentially an expire after x seconds on the cluster index. And you can just use that feature to, to delete a document that that expires. I think Reedus has that feature as well, right? Cluster collection have additional performance improvement for inserts, updates and deletes and queries. Here they go into details. All collections have an ID index. A non-clustered collection stores the ID index separately from the document as we explained, right. This requires two writes for insert, update and delete and two reads for queries. They try to avoid talking about this hidden index because this is a public user facing document. But if you go to the for the forums and the threads, you can you can clearly see that details, right? You can see it in conversations, which is which I love. And also I think, I think in the source code which is Mongo is open source, right? A cluster collection stores the index and document together in an ID value order. This requires one right for insert, update and delete and one read for queries, right? Again, when we say two reeds and one reed, we're talking about an actual bee tree traversal. Reed. It's not a single I. Oh no, no, no, it's not a single go to disc. And hey, this is a reed. No, it can cause multiple I os one reed is equal one or more iOS here. So be careful with this terminology. And here. Behavior. Cluster collection. Store documents ordered by the cluster index key value. Right. And the key value for the id. I think you can change it, which is something you have to watch out for. And I'm going to talk about why you can you can have one cluster index in a collection because the document can be stored in only one order. Right? Makes sense. Only collections with a clustered index. Store the store the data in sorted order. You can have a cluster index and add secondary indexes to a clustered collection. Clustered indexes differ from secondary indexes. Now here's the interesting thing. And I think they do mention it here. Hopefully. Hopefully they do. A clustered index can only be created when you create the collection. So there are some limitations I suppose. So they don't let you create an index clustered index after the fact. That's to me that's just a limitation software limitation. There is no technical reason not to do it. They just they just uh, they want to do it later. Because probably here's my guess. I think the reason they don't want to do it is because to to create an index, they have to acquire an exclusive lock, like an almost an access exclusive lock on the whole collection. You cannot do anything to that collection when I add a cluster index. And they didn't want to do that because, uh, it blocks raw, it blocks, reads and writes. Right. So that's why they don't want to do it. This is they just do it. Just just copy the collection to another collection and create the index clustered index there. Right. Because why do we need exclusive access. Because we're reordering how data is stored. Right. The whole thing is changing. It's a massive big change. So cluster index have to be done from scratch or just created from, from the beginning. Uh, cluster collection limitation. You cannot transform a non-clustered collection to a cluster collection or the reverse. Instead you can. So they're talking about this like how do you convert a non-collection non-clustered collection to a cluster collection? You cannot just do it. You cannot just say, hey, add a cluster index because that will acquire an exclusive lock and they will block reads. That's my guess I think. Right. They just don't want to do that. Or maybe they're more technical details that I'm I'm not aware of, at least when it comes to Mongo. Right. but I envisioned them having that feature in the future. Definitely. Because that that's just right. That's just something all databases, almost all databases have. Right. Let me just create a clustered index right now. By default, if a secondary index exists on a cluster collection and the secondary index is usable by your query, the secondary is selected instead of the cluster index. So be careful with this. I still don't understand why would the secondary index be preferred over the, uh, primary uh index? It's just weird because what if I do like, uh, where ID equal blah. And the this field that I happen to index is X, right? It'll be really slow to use the secondary index just to get All these documents write and then filter the documents for that ID. So that is a really something really to watch out because it can really, uh, uh, cause degradation in performance, right? Because, hey, I'm, I'm literally pulling one document and I happen to have another filter. You just happen to use that index just because I had it, you know, a proper, proper plan, I would say, oh, that's a that's a more selective index. I'm going to use that. I don't have details as to why they are doing that. It seems to be a bad idea, but there must be a technical reasons behind it. But so they tell you at least, hey, if you really know what you're doing, use a hint. Which is good. I like that. Uh, Mongo uses hints, right? Mongo supports hints. So in that case, like if you actually doing something like this right? Then probably just hint the planner say, hey, please use this cluster index. So something to watch out for here. The clustered index key must be on the ID field. Sure okay. You cannot hide a clustered index. Hidden indexes. That's another thing. If there are secondary indexes for the cluster collection. The collection has a larger storage size. I'm glad I mentioned this. Let's talk about why. The secondary indexes have a larger storage requirement in cluster collection and here's why. So to explain that we need to know how secondary indexes work in unclustered or non-clustered collections. They do by the secondary index actually points to the record ID back in the older model, right? So both the ID and the, uh, and all the secondary indexes point directly to that record ID, which is just another, uh, you know, B-tree data structure. So if you search by ID or search by the secondary indexes, you are incurring double the double reads. We talked about the double B-tree, uh, traversal. Okay. And the second index in this case include the key that you're indexing. Let's say this is the date of birth or, or the salary or whatever the field is in the document. And then the value of the secondary index is the record ID, which is what we said. It's 64 bit eight bytes right. So that's fixed eight bytes. Never changes. The older in the new model in the clustered collection the secondary indexes point not to the record because that doesn't exist anymore. What does it point to then? You don't have a choice. You have to point to the ID key. So that makes it identical to the InnoDB MySQL model, right. Where now we're pointing to the primary key values. What is the size of the primary key. It is 12 bytes not eight. So that's an additional four bytes you're incurring on the secondary indexes. So yeah. So you're incurring an additional four bytes in this case. Right. Which might not seem much per record. Right. But can add up. Right. So a few extra megabytes 4 or 5MB. Right. And it can add up. Right. And especially if you have a lot of indexes, it can really, really, really add up. And what can make it worse is, uh. Is, is if you did choose to change the ID field content and you're not using the default, that's the default is 12 bytes. If you let's say I, I've seen a lot of people use Uuids, for example, and Uuids. If you do store them, I suppose those are stored as string, right? If you store them as if you store Uuids natively, they are 16 bytes, 128 bits, but almost we rarely store them this way. We store them as a string and with the dashes, and based on whether it's UTF eight or other encoding, that gives you like 36 bytes, that's a lot. That is a lot of bytes, right? So you're jumping from eight bytes to 36. So that is massive per record. So there's like a significant difference. So you're talking about what, 28 bytes extra per record. So that makes your secondary indexes larger. You might say who cares if the secondary indexes are larger. Well the moment things get larger iOS suffer. And the reason is because the page size. What is the page size in MongoDB. It says here wiredtiger is four kilobyte. I don't know if that's correct or not, but. But if it's really that small then yeah, you need to do more iOS to fetch what you need. Write more pages. You need to pull more pages to fetch the things that you want. Right? Because it's just just the things that you want is no longer in fits in one page. It's in multiple pages, right? So the larger the the index, the larger the slower it gets to pull. and you're trying to find what you're looking for. And here's a and of course they go into the details here and how to create it. So they go into details here. So it should be small size as possible talking about the key because very critical the key value selecting that key for the cluster index is critical right. Because it does support up to eight megabytes in size. That just sounds like a bad idea. To have a key in eight megabyte like that is going to be showing up in all your secondary indexes. If you don't have any secondary indexes, who cares? That's fine. But that's just that. Just that sounds like a bad problem to have. So but a smaller indexes is key. Is better. A cluster index key is better. Of course. A large clustered index key causes the cluster to increase in size in the secondary indexes to even get larger. This reduces the performance and storage benefits of the cluster collection, as we explained, right? Secondary indexes on a cluster index with large clustered index keys may use more space compared to secondary indexes were known with non cluster because the secondary indexes non-clustered are fixed to because they are pointing their values. Point to this eight byte uh record IDs, while the secondary indexes and clustered indexes the cluster collections. They point to this massive thing that whatever you have in that case. Right. So be careful with that. Uh, here's a short example how to create a collection. So you would specify a clustered index. And it has to be this key ID one. Right. And it has to be unique of course. And you can give it any name you want. And here they give you how to do this examples. And but yeah so that's what I thought I'll talk about cluster collections I was like an interesting features. If you're using MongoDB or you're planning to use this feature, let me know in the comment section below. I'm gonna see you in the next one. Goodbye.
