# Isolation

All right. Let's talk about the third property in the acid system. Isolation. Very, very critical and really underrated property. Not a lot of people understand. Truly understand the ramifications of transaction isolation. So in this lecture, we're going to talk about isolation. And this is going to be a long lecture. So sit back, relax, grab a cup of coffee, tea or your favorite drink and let's go through it. So to talk about isolation, we have to answer this question. We have transactions, Correct. And if it's a single user, that's fine. But we have many TCP connections to my database, a few sub or remote remote connections, and then each connection execute transactions. So there is a chance where concurrency can happen multiple transactions fighting over to write and to read the same pieces of data. And that's where isolation come into place. The question is, can my in-flight transaction see changes made by other transactions that are also in flight? That's the question that needs answering not only in flight, other in-flight transaction, even transaction that has finished. What if I am running a transaction and I'm querying, reading, reading, reading, reading, reading and some other transaction committed a change? The question is, should I see this change? Well, it depends. Do you want to see that change? Right. There is no answer. There is no right or wrong. So as a result of these, you get a bunch of read phenomenas they call it write very fancy name. And the main understanding of this read phenomenon is, is these are usually undesirable. You don't want these. These are bad side effects. But we're going to go through them. We're going to understand what are they. And if you run into one of them, you're going to see and you understand, Oh, now I see what's happening here. And these are one of the nastiest thing to debug in my opinion. We're also going to talk about isolation levels that were introduced to solve these undesirable read phenomenons. Right. So let's talk about isolation, red phenomenons. So what are the unknowns? So the first red phenomenon is called Dirty Read. And we're going to talk about them in details and with an example. But essentially a dirty read is a read that you as an in-flight transaction currently running. You read something that some other transaction has wrote but didn't really commit yet. So there's a chance that this change that you just read could be rolled back or it could be committed or it could the database could be crashed. Right. So this is called a dirty read. Anything that is dirty means that you wrote something and this write is not really fully flushed, not fully committed. It might change. And that's what it's called. Dirty read Non-repeatable reads are reads that you are in a transaction, you read a value, right? And then in the same transaction you read it again. And that sounds ridiculous. Like what? Why would you ever read things twice? That's just dumb and an optimal. But no, believe me, you do this all the time without you realizing it's not about doing select select ID from account and then doing select ID from account. Not the same exact query. No, we're not talking about that. That's yeah, it's a little bit odd to do that, but there are cases where you actually do the same exact query, but I'm talking about the different queries that yield the same value. And there are thousands of these cases. And one example is you can read a value, but then you do a select some which also reads the value behind the scenes because it does a sum these are different values, but they are dependent on the same entry that is in the database. So an non-repeatable read is when you read something and then you the database tries to read the same value again, that changes. Right. And this is called a non-repeatable. It was not repeatable in my own transaction. So that's what a red phenomena. Some people find this undesirable. Right. I'm going to show you show you an example of that Phantom reads. Phantom reads are things that you cannot really read because they don't exist yet. I'm going to give you an example. So example with this is our range queries like let's say select all the activities between this date and this date, right? When you do this range between this and this, right? You get a number, right, you get a certain values. But then let's say someone inserted a new rose here. It's a completely new role. So it's not really a non-repeatable read. You didn't even read it. It's just something that is inserted that satisfies your range query Now. Now if you do that and you do another select right, you're going to get that phantom new row and you're going to get that read and that things might go really wrong with that. And that's called Phantom Read. So you got you got certain result and the first situation. But the second one you you you got you got a completely different result. So it's not really completely Non-repeatable read it's. It doesn't qualify as an unrepeatable because you didn't really read it in the first place, right? You didn't. You can't quite catch it. I'm doing this with my hand, but you can't see it. I'm doing this with my hand. And the fourth one is really lost updates. That means I wrote something. But before I commit this write, I try to read what I wrote. Well, this is very common because if you're on transaction and you write something and you turn around and read that something, that's completely fine because you are in your own bubble, you can read what you wrote, but other transaction usually can't. Depends whether dirty reads are enabled or not. But. Some other transaction can actually change what you wrote. So if you turn it on a read, what you just wrote is just lost. And that's called Lost Updates. We're going to talk about when that actually can happen and how to circumvent this. All right. Dirty reads. Let's take an example. So we have a sales table here and we have three columns, product ID, quantity, and the price product. One has sold ten quantities and each is $5. So the total price is what, $550? $50. So we sold $50 worth of product one, Product two is 20, quantity $4. So the total is what, $80. So what is a dirty read in this case? You begin a transaction as usual and you start this query, you select the product ID, and then the second column is, is a is an actually expression. You multiply the quantity times the price because you want to see the total right from sales. And that's generally a bad query, not because we're doing an expression, but because we don't have a where clause, but we're explaining things here. So as long as I say tell you that this is bad because if you have billions of sales now, this is going through the entire table, you have to have always a range. You have to have a bounded query. I always say it's bounded and the reason is it's unbounded. This is an unbounded query. That means there is no bounds. It's just forever. It goes forever, right? Yeah. We have two rows here, but when things work with two rows, it not necessarily work with with 100,000 rows. Right. But yeah, we got, we got the idea. Right. So we have what are we going to get is a product one is 50 and the product two is 80. Right. That's what we got. And this is the list. I don't know. Maybe I'm generating a report, a sales report, and now another transaction started while this is actually running. So these are in parallel now, right? So this started and this is now started. We selected this. We read the actual values that are currently in the table transactions to start. And right after this guy read the values, it actually made some more sale, some people more made some more sales on product one, we added an extra five, we sold an extra five products, right? So now what will happen is now it's 15. Okay, now look at this. We're doing another query, but now we're just want some all the, all the sales, right? Regardless of the products, what do you think the value should be versus what do you think the value will be based on a dirty read configuration? Look what will happen. We're going to get 155. Why? Because if you multiply this 15 by five plus 80, which is this didn't change. So the original sum is actually 130. If you think about it, right, 50 plus 80. But we added a $25 worth of sales. So if you add that, you're going to get 155, which is technically not wrong, but if you within the same transaction, this is inconsistent because if you actually print this in a report and you rent it to the manager said, okay, oh, product 150, product 280 and then you printed all the way down in the summary, you printed the sum as 155. They say, What? What are you doing? That's just wrong, Right? And why did we do that? Because we read a dirty read this guy didn't even commit. And yet we read the value. You notice that the line of the commit didn't even happen. Now we commit my transaction, which we didn't didn't change anything. But we're just reading, just to tell you that the transaction has terminated. But now this guy just, hey, you know what? Never mind. I'm going to roll back because, hey, we didn't actually made five five sales roll back. So this is actually worse. You write a dirty read and that read has has gone right. And that is really a problem. You get an inconsistent result. So let's talk about Non-repeatable Read here, same configuration here. 1050 ten 524 And then we're going to begin a transaction. Now we're going to select the same exact thing. We're going to get product 150, product 280. So what is a Non-repeatable read here? Another transaction begins. But look at that. The arrow is short. I made the arrow shorter. Why? Because here's what I'm going to do this time. I'm going to make the sale and guess what? I'm going to actually commit. The change before the second transaction finished or before the second transaction actually made the second query to do the sum. Look at this. So we we actually persisted. This is not a dirty read. This is an actual valid write and valid read. So the value is actually 15, but we read the value B prior to that change. So now what should we do? Now I do a sum count price from sales and guess what we're going to do? We're going to get the exact same wrong result. We get 155 when it should have been 130, right? That's the actual result that is desirable. So now this is not really a dirty read. This is an actual committed read. But the problem is this was a non-repeatable read because you read the values here and you read them kind of again. Right. But you read them through an aggregation function, right? And as a result, you got an inconsistent value. And you might say, in this case, I don't want non-repeatable. Not all the time, because we're going to come to that. But fixing this is expensive. Fixing this so that you don't you read always the same version of the value here is not is not easy. A lot of databases supported through mvcc, but it's not easy because now you have to keep a version of the product one at the original state. That's what Postgres does, by the way. Postgres always. Any updates creates a new version of the row and never changes the same value. While MySQL and Oracle and it might be also SQL Server changes this the final value changes to 15, but it keeps another table called the undo. Right. I'm writing undo here. I don't know if you can see it, but the undo stack. Keeps an actual writing to desk a place where all the previous values. Oh, product one by the way, it used to be ten and it turns to be 15, right? We I don't know. It keeps the delta changes. So now if this guy wants to read in Postgres, it will read the original version. It just it just keeps a version pointer here. So there is another row product one version two write a version B for example. So we don't confuse. But in MySQL what will happen is it doesn't it cannot read this. It has to go and crack open the undo log and then read that values and that could be expensive for long running transactions, right? Because it cannot trust this latest value anymore. So yeah, this was just a short explanation of isolation, but we're going to come to that. Yeah. And then we commit and obviously nothing changes. Bad Non-repeatable read can be bad. So really up to you guys. You cannot just go by this example and say, Oh, I'm fine. No, really. Comes back to what are you trying to build? And then just understand this problem and say, Hey, I'm fine, I'm even fine with Dirty reads because I don't think anybody is fine with dirty reads. Dirty Reads is the worst. But I have actually built an application where I relied on Dirty Read, which was a very bad idea. I regretted that later. Let's talk about Phantom Read. So Phantom reads results from range queries. Anything that can potentially give you one or more results. Pretty much Phantom read always happen. If you think about it. If you have a where clause, I can't think of an example where Phantom could not happen to be honest. So same configuration sales. We begin a transaction, we do a select quantity price sales and this time I'm reading this product 50 and 80. Transaction to start it. Now, it didn't really update. It inserted a new new product, completely new product. Right. So now if you insert a new product, well, that's fine, because in this case, we didn't really read this product. But get this, it actually commits it. So now we have $10, $10.10 quantity of product, three at the price of one. So an extra $10. So now what do we do here in the in the original transaction, you're going to do a sum quantity from sales which doesn't have an unbounded query. There is no there is no bound to it. So that means we're going to unfortunately sum all of them include product three. Yikes. So we get 140, which should be 130. We read a committed value nevertheless not really dirty and that just happened to show up in our range query. Okay. It's not really a non-repeatable read because we never actually read product three to begin with. In the previous one we read these two values and they changed later. Okay, so and why are we differentiating between these two? Is really the implementation when it comes to it. Like if you if you really want to know your guys are interested, it just comes down to locking and Non-repeatable reads When we read something we want to guarantee repeatable reads, anything that the transaction reads, it just obtains a shared lock on it, not really a shared lock. One kind of locks. I'm going to talk about locks in this course as well. And then and then if it just makes sure that this read read doesn't change for that transaction, that's what it makes sure of. But how does it get read of new rows that has never been read? How does it you cannot even see the row to lock it to prevent it from changing? That's what phantom reads. You cannot grab it right? It's Phantom. So last update. Let's talk about the last read phenomenon. So same thing here. We now we're going to begin to transaction in parallel. Concurrently, we're going to update one of these guys, update the quantity to increase. Let's say we added a $10 sale, ten units of sales to product one. Now from ten, it turns to 20 transaction to updated the same quantity to add five. But guess what? This quantity now it's reading the original value because they started the same time. So both of them actually read quantity as ten. So this guy is also ten. So now what it will do is it will take the ten plus five and then store it into the product. So now we have 15. So now we lost the value here. Okay. And that's what happens in this situation. So now we commit the transaction. Transaction two was faster, so it committed and now we have the value 15 and now transaction one assumes that it made the sale. It will do the sum and we're going to give you a completely different result. That is absolutely wrong because it should have been 180, but we got actually less result. Why? Because our update was actually overwritten by another transaction and as a result, it was lost one of the nastiest thing. And this going to be solved basically by actually locking the row. So, hey, if someone row level locks are very popular here, we're going to talk about them here. It's like, hey, I just changed this. I don't want anyone else to change the same row. There is no point of it. Let me finish and then I'll release it. And then once I'm done, you go your own merry way. All right? We talked about rich phenomenons. We need to talk about isolation. I know this. This lecture is a little bit long, guys, but there's a lot of information here. Feel free to ask questions and to understand this better. So isolation levels for in-flight transaction, what are they? So we're going to talk about many, many isolation levels they that were invented to fix those read phenomenons that we talked about. So when I try and start my transaction, you set this isolation level using keyword, I believe set isolation level literally in a transaction. This is the SQL Command for it said, Hey, I want to start transaction with Read Uncommitted. That's the first one which I believe no database supports except SQL Server Read. Uncommitted is just means no isolation. I want to read anything you read is visible by anybody outside that changes this value, right? It just read Uncommitted is related to the transaction itself, so it means no isolation. Any changes from outside is visible to the transaction, whether it's committed or not. That's why it's called read uncommitted. You can read uncommitted values as a result, you can get dirty reads here bad, obviously. But if you think about it, it's technically fast, right? Because I don't really need to maintain anything. It's questionable because the implementation, you don't know what the implementation is. You just assume from theory that it's fast, but it could be not. Read committed. This isolation is one of the most popular isolation levels out there and a lot of databases optimize their engines for read committed workloads, which means each query in a transaction only sees committed changes by other transactions. And we talked about that in the examples. Right. So now if there is a transaction that makes a change, right? While I'm reading something, I'm not going to see this change unless the transaction commits it. Once it commits it while I'm running, I'm still running the transaction. I'll be able to see that change. You might not be happy with that. You say, Hey, this could get inconsistent, but just understand that this exists. And I believe this is the default isolation level for many, many databases. Just understand that if you have a long running transaction, anything that has been committed will be picked up by a transaction. This could be bad. Repeatable read, which is the isolation level was invented to fix the non repeatable read write Non-repeatable read is just bad read phenomena. Repeatable read is the isolation level to make the read repeatable. That means as I read and I read it again, it's not going to change. I can read it 1000 times. As long as I'm in the same transaction, the value will not change. Let's read this definition. By the way, I rewrote this definition based on my understanding. You will not find these definitions in the web because, frankly speaking, computer scientists always complicate things because they added all a lot of jargon for some reason. Right? So based on my personal experience and professional experience working since 2005, this is what I give you. I give you things that are practical, that you can touch and feel right. Fix the typo there here. So the transaction will make sure that when a query reads a row, that row will remain unchanged. While it's while the transaction is running effectively. Right? So that's what it means only when you actually when the correction actually reads a value, reads an object, leads a row, reads a column, then we're going to make sure that as long as this transaction is running, their value will never change. But if you didn't run it, if you if you didn't read it, we're not going to copy a version of that row, if that if you get my point right, because how do you implement something like that? Think about what the database is doing for you. There is cost for everything. And this is a cost If you you can you can take a snapshot of the entire database if you want. And that's another isolation level, by the way, called snapshot. Right. But that's also expensive. So there's the thing snapshot. Each query in a transaction only sees changes that have been committed up to the start of the transaction. It's like a snapshot of the whole database at that moment. This is guaranteed to get rid of every read phenomenon. This repeatable read doesn't get rid of phantom reads because we saw that phantom reads just sneaks in. It's an insert. We never actually read a value to lock it or to to obtain some sort of a lock so that we can version it. We can even get to do that, right? Snapshot isolation. Just hey, let's just hey, this is, this is what I read at that stage. And Postgres repeatable read is actually a snapshot isolation. It's just everything versioned. So if I'm if you start a transaction, you kind of time stamp which version you are in. And then as a result you always going to read this version. So even if you inserted rows show up, you will not pick them up because the database will filter things based on your version effectively. And that's the power of the snapshot isolation. The final one or the basically the slowest, if you think about it, is serializable. That means there are no concurrency anymore. Just each transaction is physically, almost physically database is implemented differently, but as if they are serialized after the other. So if there are three transactions stored in the same same place, they will be serialized based on. Now this depends on the linearizability and stuff, complicated stuff like that. Which one goes first is the question that the database tries to answer so that you get almost the same results. And obviously I talked about that each DMS implement isolation level differently. Don't don't over stress with these isolation levels. Just understand that they exist. Most of the time you're never going to run into these complex situation. But if you do know that these things exist, especially with a lot of users changing and reading at the same time, this is when you start you need to know about this. At least I took this from Wikipedia. I like this table a lot. It doesn't have the snapshot isolation, but if you if you look at this, you can see that read uncommitted. Everything may occur with all the read phenomenons occur. This read committed everything can happen except dirty read. You cannot get dirty because hey, I'm reading committed stuff. Repeatable reads. You don't get dirty reads you don't get lost updates and you don't get Non-repeatable reads as well. All right, because we fixed that old repeatable read, but you still get phantoms. But in serializable and also snapshot, you don't get any of this. It's all fixed effectively. Right? And there is a slight difference between serializable and snapshot, right? Serializable is effectively just slows things down if you think about it. Right? Because it just serializes thing. While Snapshot takes a version of the database at the start of the transaction. Now, when I say version of the database, I'm not talking about actual backup, right? It's the database. Have to implement this in an optimized manner. Make sense guys, let's talk about a little bit of database implementation of isolation. Each Dbms implements isolation level differently. Of course, there is the pessimistic, pessimistic, Pessimistic. Yeah, pessimistic approach, which is this means the pessimistic concurrency control. To be specific. It means I'm going to use locks and I'm going to dedicate an entire section lectures talking about locks. When you get to that, you're going to learn more about it. But effectively I'm going to obtain a row level locks, table locks, page locks, a table locks. That means any change I make, I'm going to obtain a lock in the whole table. Very, very expensive. By the way. I run into a lot of situation where my transactions lock the entire table called lock escalation is escalated the row to a table lock, and then all transactions were just pending, trying to change other values. And it couldn't because the table was locked. Page lock, the actual logical page that contain the changes on lock. This is especially true in case of clustering. If you want to cluster your table again, we're going to talk about that as well. And then these pages that you write to also have certain locks. So effectively there's a pessimistic approach. Lock Hey, I'm changing. Lock it. Don't let anybody touch this. So if you lock a row, only that row won't be able to touch it. You're going to see some examples. When I showed in Postgres and MySQL that when we try to update something, it will succeed. But another transaction will try to update will just sit there and wait because the row is locked. Optimistic usually don't use locks. That's the other approach. That means there won't be any pending transaction. So also not only that, so nobody will wait on the other. And also the benefits of this is the lock management, which is extremely expensive, by the way. May because how do you know that a role is locked? You have to maintain it in memory, right? You have to keep track. The database has to keep track. Oh, this is locked. This is not this is locked. This is expensive, especially row level locks. If you have like 700,000 table rows that you need to lock. This is expensive. So optimistic. A lot of people prefer this approach where, hey, I'm not going to obtain an actual lock, but I am going to just let things play by ear. And when something actually when people start people when transactions start stepping on each other, boom, I'm going to fail the transaction. And the user should just retry. And the error is called usually a serializable error. You get it's like, hey, some something changed at the beginning of the transaction, so just retry the transaction. NoSQL databases prefer this approach to locking because locking is expensive, repeatable read locks. I talked about that a little bit. Repeatable read locks. The rows it reads. But it could be expensive if you reads a lot of rows. Postgres implements repeatable read as a snapshot. We talked about that. Right. A snapshot and repeatable read in Postgres are identical. It's the same thing, right? This is why you don't get phantom reads with Postgres and repeatable reads where in other databases you get phantom reads with repeatable reads and. Uh, spoiler alert. That's part of a quiz. Serializable are usually implemented with optimistic concurrency control. Because if you actually truly do serializable, then your database is going to be so slow. Right? Because what is serializable? Just one after the other. Right. So if you that's why database is implemented with optimistic concurrency control. You can also implement it pessimistically if you want to. Will select for update. I'm going to select this, but I'm going to lock it. That's what you do. So let's summarize the isolation. Isolation is the result of having the transaction run in complete isolation from other concurrent transaction. As a result, we get read phenomenas and we have a lot of isolation levels that have been invented to fix. Read phenomenas. Let's jump to the next lecture. Good job, by the way. Finally finishing this lecture. Good job.
