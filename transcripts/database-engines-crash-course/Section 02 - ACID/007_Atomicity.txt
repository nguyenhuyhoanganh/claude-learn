# Atomicity

Atomicity is one of the four acid properties that defines a relational database management system. And I always say relational, but really it defines any database system, whether it's NoSQL graph or time based really anything. Atomicity is a very critical concept to understand. How about we jump into this lecture and discuss this beautiful concept. So talk about atomicity. Really all the queries in a transaction must succeed. That is the definition of atomicity. We talked about what a transaction is really in the transaction lecture. And we said that all the queries in a given transaction is considered to be one unit of work. And that kind of defines what, what what atomicity really is. Right? It's like an atom. And until now, an atom cannot be split. I know there is nuclear fission that kind of contradicts that, but but that's where atomicity comes into. You treat this as an atom that cannot be split. All the queries in a transaction must succeed if one query fails. What happens if one query fails for any reason? A failed constraint like the balance, goes to minus one, minus negative or a duplicate primary key entry, right? Or an actual invalid SQL syntax. That is a failed query. And what happens when you have one single? Even if you had 100 successful queries in the same transaction, that one failure should roll back the transaction immediately because I said things went wrong. That's the rule of atomicity. So what happens if the database goes down during my transaction. I can't really roll back because the database goes down. What happened in this case? If the database went down prior to a commit, you didn't really commit the transaction. All the successful queries in the transaction should be rolled back, right? Even if you had 100 successful queries in this transaction and you didn't really fail, your query didn't fail, but the database just crashed, right? So the next query didn't really fail. So the database will restart. So once the database restarts, that's when the database should detect that hey, there was a failure technically, right. Because my queries didn't fail. But the database went down and I was mid-transaction and I didn't commit these changes made by the transaction. If the database is actually making changes should be rolled back. That's why I always want you to think about this as you read this as I as I talk. What is the database doing in a transaction? Is it actually writing to disk? Right. I'm not going to commit. If. What if I don't commit? Do we go back to disk and remove these changes? All of this thing really is what makes or breaks a database, right? And this goes back to most of the questions I receive really is what database should I choose? Well, really you need to understand how these databases work and based on your use case, make a choice. There are databases that come before commits. Any right. You make right in the in the queries in the transaction they are actually writing to disk. So say they assume you're going to commit. So they are optimistic in that nature. So when you actually in fact commit I don't need to do any work because I Because I committed. I actually wrote everything to this. It will just write one, uh, one bit that says this transaction is committed and it's so fast, right? Other databases doesn't don't write anything. So the queries are so fast executing because they always write to memory. But the moment you want to commit, they have to go take everything that is in memory and then flushes to disk. And the commit will be slow in that regard. But the rollback will be as absolutely fast. You know, it all depends. There is no right or wrong. There is always a trade off. Let's take an example. This is an example I took in my I talk about in my, uh, transaction uh, lecture atomicity. So I have the same table account table balance 1500 account ID one two. We want to send money from the $100 from account one to account two. What do we do? Very simple. We need to debit this account and credit this account. So -100 this plus 100 this. And we have to do it in a transaction because these two should happen at the same time right. So we start a transaction. We select the balance to check if this guy even have money. And they do because the balance is greater than 100 because it's 1000. Right. Because that's the account one. So yeah let's go ahead and update the account. Let's deduct 100. And what database crashed I did one change and the second change I wanted to do but the database crashed. We restarted the database. And now let's assume this is a bad badly implemented database system. This is what we see when we restart the system. We see 900 here. What? What happened? So in this case, we just lost $100 in thin air, right? That's really bad. Inconsistent. This is an inconsistent view. Not only you just lost actual money, that you. You have no idea where where it's gone because of technology. Effectively. Right? So after we restarted the machine, the first account has been debited, but the other account has not been credited. Very bad. Inconsistent view. So lack of atomicity leads to inconsistencies. And we're going to talk about consistencies in its own, uh, lecture. This is really bad. We just lost data. The information is inconsistent. We talked about that an atomic transaction is a transaction that will roll back all the queries if one or more queries fail. Well, in this situation, yeah, there is a failure, but it's not really our fault because my other transaction, my other query sorry, didn't even execute. So the database got restarted. So how do I know to roll back? I don't know. The database didn't even give it a chance to roll back. I can issue the command roll back. So the database now is responsible to clean up this garbage. It will say, okay, there was a in a running transaction. And it has to know that there is a transaction that has been running and it was crashed. Right. So upon restart, we need to clean up. Right. And let me let me tell you from my professional experience, right. I started working professionally back in 2005. And I've seen rollback. That takes an hour over an hour actually for long transactions, especially in SQL server. But I've seen rollbacks, not actual rollback commit by the user. No, actually the database says, wait, I can't even start the database before I rollback back this, this stuff, because it has to go and clean up all the garbage that has been written, this half garbage that has been written. Right. And this is the garbage that's been written here. So we wrote that debit one account -100. We need to change the 900 back to the 1000 to get a consistent view. And that's where it all play. Some databases won't let you start the database before it's rolled back. They they in recent patches in recent releases they'll say okay it's not really that bad. Let them roll, roll back, let them continue working on other tables other databases. But this database now is just it's in a rollback state. So CPU is getting hammered right. Memory is getting hammered. All these things really. As a result the database should clean this up. After we talked about this and we talked about how long this could take really and it really depends on how much you wrote and how long was the transaction. Long in transactions is generally a bad idea. Because of these things, let's do a quick summary on atomicity. Atomicity is the idea of the transaction being one unit of work, and that cannot be split. That's the property of an atom, at least. At least, uh, back in 1970. If they figure fission, nuclear fission, then this this, this this is broken effectively, but you get the idea, right? So you cannot really, uh, an atom should be one unit and cannot be split, right? So if if you have a transaction with 100 queries, all these 100 queries should succeed. If any of these 100 queries fail, all the 100 or anything that is successful in the query should be rolled back. And not only that, but you should even in case of a crash in the database. So that's not an actual explicit failure by the user. That should also roll back the changes. That was the atomicity lecture. I'm going to see you in the next lecture. Stay tuned.
