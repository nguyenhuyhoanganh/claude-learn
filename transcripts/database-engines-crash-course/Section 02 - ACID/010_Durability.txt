# Durability

And let's talk about durability. Durability, in a nutshell, is the process of persisting the rights that clients make to the database, to a non-volatile system storage system. That means anything that we write is stored so that I mean, even if I lost power or even if I crashed as a client after my committing the transaction, if I come back, I see the change. If the database crashed after the commit, I restore the database. I see that change. I see it persisted in disk. If my computer crashed, I come back. I see this change. That's what durability is. And you might you might think of that as, okay, of course everything is doable. Not really. A lot of database system actually play with that. They play with the durability says okay durability is is slow right. Because to make things durable that means you have to persist them to disk and disk are inherently slow. So a lot of databases play around this durability and say, okay, I'm going to write my stuff to memory and occasionally on the background I'm going to snapshot and write to disk. So I, I'm going to make my rice faster. Can you get the database that does that reads, writes, Does that write as an option? Obviously. Right. But durability is one of the strong relational databases must have. Right. You have to be durable. Right. And we're going to define what that means, really. So durability changes made by a committed transaction, emphasis on committed has to be committed. If it's not committed, really all bets are off must be persisted in a nondurable in a durable non-volatile storage like SSD or hard drive. Durability techniques. There are many. Durability techniques. There is the wall, the write ahead log, which really, you know, when I showed you all these tables and indexes and stuff like that, these are really the representation of the data, which is what people read, what clients read. But these are really large, if you think about it. So if you if you really write these things to disk, which we will durability, committing a transaction takes time because this is a huge these data and the data structures and the b-trees and stuff like that, it takes time. So what database is implemented is a version of the changes that happen only, which is deltas. Effectively you can think of it as deltas. And then this is a write only a write ahead log which calls wall any changes goes to this immediately. First we flush it immediately to disk and this guarantees persistence. So yeah, we didn't really update the data table right on on another data structure yet. But we can if we have a crash, we have this beautiful wall, we can go back and read all the wall entries and then rebuild this state effectively, easily from a persisted state. We still persist this, we still write this durable write to disk like the actual table itself, the changes. But we don't rely on that because that could be slow. And there is another approach which is asynchronous snapshots or even synchronous snapshot for that matter, where where as we as we write, we keep everything in memory. But then asynchronously on the background, we snapshot everything to disk at once. And that is also another concept Reedus uses. Actually, both of these things append only file is another option, which is very similar to the wall really that Redis is uses, which keeps tracks of the changes of what happens and then writes these things. This guarantees that we have a very, you know, lightweight way of storing data in a, in a very fast way, but also in case of a crash, I can always read back and construct the state of the table completely without store, without the need of storing the likes and the ID and the strength and the blob and all that stuff. Right? We change only we store only these changes and eventually we're going to write that as well. So let's talk about the wall a little bit. Writing a lot of data to disk is expensive, as I talked about that a little bit. We have indexes, we have data files, we have columns, we have rows. You know, we have system files, system tables, all this stuff writing. If you imagine writing all the changes and the amplification of the writes itself, that's going to take a long time. You can do it, but it's going to be slow. So we need a better way. We need a more optimized, compact way so they compress this version of of this changes known as well write ahead log segments. So they only write this log log segment. And this this better be persistent because this is what tells us what to change effectively. Right. So these are just tells like, hey, we change this value to this, we change this value to this, we change this value to this, and we even compress that. So we get it in a very tiny segments. So there's something very critical in database systems, and this is one of the problems that operating systems do. And this is called the OS cache. When when you actually write to the OS, you ask the operating system what are the various Windows or Linux When you when the database asks something to ask the OS to write to disk, the OS actually does something shady. It writes not to disk, it writes to its own memory cache. Right. And the reason is because it will it want to batch these writes and flush it to disk at once for for performance reasons because most applications do does a lot of writes and they they want to kind of batch these writes and do it once so because if you batch it you you do less I o and if you do less i o as I always talk about this, i o is better. But what's the problem with that? What if I actually use, say, commit and the database asks the database, ask the the OS to write the wal segments to the disk. But the OS, they say, hey, I'm going to write it to the cache. Instead, the OS will tell the database that hey, I wrote successfully despite it, only it only wrote to the cache. It didn't write to the disk. It will tell the database that hey, database or the application doesn't know what the data. It was just not an application that asked her to write something and it will ask the database, Hey, by the way, I'm the OS and I wrote your changes successfully, but it only wrote it to the cache. So the database will say, okay, uh, sure. The database will say your transaction committed successfully. But the problem is, what if you get a crash there? The database told us it committed successfully. You get a crash. Your computer crash. You restart, and then the OS, obviously. The data was in the cache and the cache when you restart, it was in the Ram obviously. So we lost the data and as a result, the database lied to us. The database in that particular situation is not durable because you wrote the transaction because the transaction told us it was committed, but it wasn't durable. Why? Because if the problem here. So when the writes go to the OS and the OS crashes machine restart could lead to loss of the data. And that's a big problem that databases fight for. So there is this command that bypasses the OS cache and ask the database, ask the operating system, Hey you dumb OS, I don't trust you. I tell you to flush these wall changes. Flush them directly to the disk. So this is called the Fsync command. So if seek command just forces the write always to this. So it's going to be slow. It's going to be very, very slow. So we have to do it with in a smart way because you're bypassing the OS cache and you might say, why? Why this? The OS uses the cache, right? Isn't that dangerous? Well, no. Most of the situations like you don't really get a crash that often. So if you save a word file or a notepad or something, things like that, it's okay to use the cache. Most of the applications use the cache and it's very rare that this crash can happen. Database system is a different they want to guarantee durability and as a result they have to bypass the OS cache and as a result it becomes slow. You see the trade off guys, right? There is always a trade off. So if sick can be expensive and slow down governments, this is another problem, right? If you commit, you want to commit as fast as possible. So let's summarize the final lecture here. Durability is the ability to persist. The changes when the transaction commits, the emphasis on the one transaction commits. So when the transaction commits, I want you to persist it on disk. I don't care. I don't care whether you persist it. Here's what I want when I when you tell me it's commit I can shut off the power at that exact moment and when I come back my data better be there. That's what durability means. I don't care. You're going to store it on on, on the disk, on SSD, on the storage, on, I don't know. In the air. Who cares? I don't care. I want my data back. That's what durability is. A lot of databases play a trade off with this properties because durability is slows rights down because now I'm going through this physical medium to persist my changes. Right? And as a result it will be slower than if you wrote to memory. So Redis, for example, plays some games with that gives you option actually that none of the databases that I know of, even relational gives you that option. Because guess what? It gives you the option. It says, Hey, you want to be completely durable, strongly durable, or you want to be partially durable, not even partial. Partial is the wrong word, eventually durable. I guess I just made up that word, but eventually it's going to persist to disk. So there is a slight few seconds delay that if you lose power or if you crash, you could lose your data. Some people trade that off that three seconds with a fast writes because hey I rather get fast writes and okay my data is not that important. I'm storing just logs of of my application Iot devices. So who cares if I lose a few things. But guess what? Right? Most of the time nobody reports loss, right? Because crashes are very rare. If you think about it, when it comes to the actual databases, those guys build the systems to not really crash. But you cannot take that guarantee because if Murphy's Law right, it says if it never happened, it will eventually happen. Right. All right, guys, that was the last lecture of this. We're going to jump into many other lectures. I'm going to talk about a ventriloquist's I'm going to show you an examples here. So go with another lecture. It's called Acid Example by Practice. I believe that's what I called it and show see how I do this in real time with the actual database, show you all these beautiful properties. I'm going to also talk about eventual consistency and I'm going to take take a take a stab at the quiz and enjoy the rest of the course. Thank you so much. Goodbye.
