# Uptime Monitoring with Heartbeat

Boom, boom, boom, boom. Whenever you build a service and expose a set of end points to provide API access to that service, you'll likely need to track their availability and response times. Aside from ensuring their functionality, but to actually know that something is down or just not performing, you need to consistently monitor your services day in and day out. That is what heartbeat from the Elastic Beat Family helps alleviate. Heartbeat helps you monitor your service availability. It works by defining monitors that check your host to ensure they're alive when discussing monitors. There are three main monitor types to consider. Each one refers to the underlying protocols that's utilized for the monitor. Each of these protocols operate at a different network level, and thus each has varying options of what it can check. So let's go one by one and explore them in more detail. ICMP, sometimes referred to as ping is the lowest level protocol of a three and works by firing raw IP packets echo requests to the end host IP address. It operates mostly at the layer three network of the standardized OCI model. If successful, you basically know that the network device you are contacting is powered and alive, but not much else. We won't focus this lesson on ICMP because it's more for network level monitoring. Nevertheless, all of the principles outlined here are equally applicable. With TCP, we are directing our monitoring requests not just to the host but also to a specific service by defining a port on which it should be reachable. TCP operates at the ozone layer five transport and powers most of the Internet traffic. This monitor works by creating a TCP connection, either unencrypted or encrypted on the host port endpoint, and if something listens to the socket, it considers the check to be successful. The HTTP monitor uses the highest level protocol of the three, which operates at the ozone layer seven application. It is based on a request response communication model. By default, the Monitor validates the ability to make an HTTP connection, which basically means if it receives any status code that is not negative after a request. But it doesn't end there. It offers a wide range of options to define the monitoring logic. For example, we can inspect a return to JSON for a specific value and things like that. Now that we know the tool kit at our disposal, let's dive deeper and see it in action. So let's begin by installing heartbeat, and I'll assume you already have the repositories in place for Elasticsearch in general. You're assuming so just do sudo app get install heartbeat dash elastic equals whatever version of Elasticsearch you have installed right now. So for you, it's probably not 8.1.3. It's probably something newer because you're watching this after I recorded this. Anyway, kick that off and let it do its thing for a little bit. Right. And as an option, we can set heartbeat to start with the boot of the system like this pseudo system control. Enable heartbeat dash elastic. All right. Now that we have the prerequisites covered, we should review the main configuration file for heartbeat and for that will say sudo and whatever your favorite editor is. For me that's nano at the moment. Slash GTC slash heartbeat. Slash heartbeat. Stop, Lionel. All right, so here you'll find a ton of options. But don't worry. We'll manage to get by by leaving most of them on the defaults. First and foremost, let's define heartbeats output, which is the Elasticsearch host. Let's find the host setting here. There it is. All right. So we're okay with this default because we are running on local host on port 9200, so we can just leave that as is. Now, here's a pro tip for you. In a production set up, you'll likely need to pay attention to the SSL and authentication sections as well. Now the second part we need to pay attention to is the monitors directory. And although you can define your monitors straight in the heartbeat Waymo file, it's not a very good idea as it can get messy, so it's better to have them separated in a defined directory where every YAML file will get picked up. So for the configuration we'll just enable monitor reloading by setting it to true. Let's find the heartbeat section. That's up at the top here. We'll leave this path as is for the YAML files that will define the individual monitors. That looks fine. We will set reload on enabled to true. And the reload period. Let's set that to we'll leave that in 5 seconds. That's fine. Great. Now all the potential changes will be automatically reloaded every 5 seconds without the need to restart heartbeat. Lastly, to have our config file clean, we're going to comment out everything under the field heartbeat, port monitors. We're going to define our monitors separately so these can go away. All right, let's see that. A nano that's just going to be control over control X. And we can now start up our heartbeat instance like this pseudo system control start heartbeat. Bash elastics service. All right. If you want to watch the logs of heartbeat, to be sure everything went smoothly, you can do so with the journal control utility. If you're running heartbeat and system D like we like we've been doing pseudo journal. Control. Dash you heartbeat. Dash elastic. DOT service. Dash, chef. And that's how you can keep an eye on the logs there. And you can see that heartbeat is successfully running. That's great. So everything should be ready to define our first monitor now. Let's control C out of this and let's start easy and create a simple monitor of the TCP type. So for the lack of a better shared option in our local environment, will perform our tests against the elastic stack running right here on our own. VM will start by changing into our monitors dot directory and creating a new y ml file like so cd slash etsy slash heartbeat slash monitors dot d. And pseudo whatever your favorite editor is. Nano lecturer, dash of monitors, not Lionel. All right. So let's define a very basic TCP monitor. Let's do a dash ID. Colon elastic stack. Dash. TCP, dash monitor. Type TCP. Hosts. Local host. Ports. 9250 6014 Cabana. Schedule. At every 10 seconds. Okay. So the key parts here are the I.D. That is a unique idea of the monitor within the heartbeat instance. Optionally, you can also give it a name for a more human readable description type. Here we need to define the type of the monitor, ICMP, TCP or TCP and hosts and ports we can pass in an array of items that will later be combined into pairs. We will be checking connectivity with TCP of our instances of Elasticsearch and Cabana running on the default ports on local host. And finally will define how often to perform monitoring with a schedule line. We can either use this logical notation with at every keyword, or we can use a cross expression, you know, like star slash ten star star, star, star star being equivalent to the above example. And that's it. We have a first heartbeat monitor set up. So now we just have to save our YAML file and wait a while. Control O or however you get out of your favorite editor. Control X. All right. So now if we request the list of our indices, curl a local host, call in 9200 slash underscore cat slash indices slash star heartbeat star to pick up any index that has heartbeat inside of the name and then question mark V for verbose. Yeah, so we should now see a heartbeat index and it already has some documents in it. Very cool. And we're going to use JQ as part of this. If you haven't already installed it, you can take care of that with sudo apt get install JQ. I already have it. And let's type in the following query. Curl dashed and silent local host colon 9200 its star heartbeat star again to match any heartbeat index that we have. Slash underscore search. And pipe that into JQ dashes. C single quote. Dot hits. Square brackets. Dot underscore source and pipe again into square brackets. Dot URL dot fold. Comma dot monitor dot status and square bracket and end quote. All right, cool. So with that GQ utility, we were just unpacking two fields from the search query with the URL for the full field for the host port combination and the monitor DOT status. And as you can see, both Elasticsearch and Cabana seem to be up or in other words, they can be connected to at least through TCP. All right. Now, let's test out the TCP monitor, which will likely be the one used with your set of HTTP services. Generally, you need to define what it means for your specific service to be alive and well in order to design the monitor properly. It may be a specific status code or a JSON response with specific contents or all of these conditions combined. To try this, let's define a monitor that will watch the underscore cluster slash health end point of our Elasticsearch cluster. It's a good example of a status endpoint, so we need to go back and edit our monitor file here, which has pseudo nano lecture monitors that Lionel. And we'll add another one. This one will have an idea of Elasticsearch. Dash cluster. Dash health. It is of tape FTP. You're also HGTV local host 9200 underscore cluster. Slash health. Schedule. Every 10 seconds. And furthermore, we will say. Check dot request colon method is get. Check it. Response Colon. Status. 200 Jason Colon. Description. Check status. Condition. Equals. Status green. All right. That's interesting. So you can see that it is fairly similar to our TCP one that we did earlier. But besides the different type, it also adds some extra parameters URLs. So as we are on the HTTP protocol, we need to specify one or more HTP endpoints in the form of a URL check. This is the fun part where we can specify the request properties and expected response under request. To be clear, we specified the get method here, but it's actually the default as well. Optionally, you can also specify various request headers here and in response here we define the logic of the response, parsing and expected results. We're checking specifically for the HTTP status 200. Otherwise, any non 400 or 500 would be acceptable. And we expect the JSON string in the response where the field status complies with the condition of being green. You can combine multiple conditions with or and and not or you can perform various checks such as equals contains regexp etc.. Let's save that yaml file and wait a bit. All right. So afterwards, you can check the monitoring data points, for example, the last 30 seconds with the following command. Karol G and Dash. Dash Silent Look. Host Colon 9200. Slash star heartbeat star slash underscore. Search question mark. And this part is going to be new. Q equals at time stamp colon and inside square brackets in quotes now a dash or on minus rather 30 s and quote plus two plus and inside quotes again now and bracket pipe that in the JQ dash C and inside single quotes dot hits dot hits square brackets dot underscore source pipe and again into square brackets dot URL dot full comma, dot monitor dot status and close off your final square bracket and single quote there. Now note this is the same query as before, but it's adding a time condition. Also notice the dash g flag for curl, which allows us to use the square brackets in the query. This should now yield more results from both of our monitors. Excellent. And as you can see, our HTTP monitor is informing us that the service is down and you can think about why that might be. And as we're running in a single note here, our status is actually yellow and not green. So that's actually an expected result. Now, finally, we can also try visualizing the collected data to save some initial setup work. We will use a predefined heartbeat dashboard that is available as an open source project and the elastic GitHub repo. So first we need to download that heartbeat dashboard file so that we can import it in later. And to do that, I'm just going to download that to my local client PC. If you're on Linux, you could use get for that, but I'm on Windows, so I'm just going to use my web browser. So just enter media dot sundog masslive.com slash s slash htp, underscore dashboard dot and JSON and you should see a file that looks like this. Just go ahead and right click on that and save it to your machine somewhere where you won't lose it. And once you have that, we'll upload it back into Cabana. All right, so now we can go to Cabana and I've already gone to 127.0.0.1 colon 5601 to access cabana running on my virtual machine. We're going to go to the management section, which is just the gear icon here on the left, go to Cabana, saved objects and import and locate that file that we just downloaded. So I put that under documents. There it is. And we'll import it. Great. Done. All right. So in saved objects, you can see what was created via their configuration file. It's a set of visualizations in the dashboard and importantly, an index pattern that is an interface for our data in the heartbeat. Dash star indices. So let's head to the dashboards and play around with what we just imported. And we'll select the Heartbeat HTP monitoring dashboard. Cool. So the out of the box dashboard should look something like this. It shows the distribution of status codes, roundtrip times of requests and other related data. Not bad at all if you want to dazzle your colleagues after only 5 minutes of work. Now, if you need to add or tweak the individual visualizations, you can do so in the visualized section. And also remember that in the Discover section you can inspect the raw data points. Now, as a final step, we should stop the heartbeat instance. Let's close out of our browser. Like so pseudo system control. Stop. Heart beat dash elastic thought service. And if you're short on space, you can also remove its indices to keep your table clean with curl dashed ash request delete htp colons last local host colon 9200 star heart beat star. All right. So very good. You now know how to keep the availability and response times of your services under control and how to quickly visualize the collected data to get valuable insights.
