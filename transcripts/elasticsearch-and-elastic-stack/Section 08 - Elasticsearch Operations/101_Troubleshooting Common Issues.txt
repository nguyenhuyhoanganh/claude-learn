# Troubleshooting Common Issues

Elasticsearch is a complex piece of software by itself, but complexity is further increased when you spin up multiple instances to form a cluster. This complexity comes with the risk of things going wrong. In this lecture, we're going to explore some common issues that you're likely to encounter on your Elasticsearch journey. There are plenty more potential issues that we can squeeze into this lesson. So let's focus on the most prevalent ones, mainly related to a node setup, a cluster formation and the cluster state. The potential Elasticsearch issues can be categorized according to the Elasticsearch lifecycle node setup. Potential issues include the installation and initial startup. The issues can differ significantly depending on how you run your cluster, like whether it's a local installation running on containers or a cloud service, etc.. In this lesson, we'll follow the process of a local setup and focus specifically on bootstrap checks, which are very important when starting a node up. Discovery and cluster formation. This category covers issues related to the discovery process when the nodes need to communicate with each other to establish a cluster relationship. This may involve problems during the initial bootstrapping of the cluster nodes, not joining the cluster and problems with master elections. Indexing data and sharding. This includes issues related to index settings and mapping. But as this is covered in other lectures, we'll just touch upon how sharding issues are reflected in the cluster state. Searching search being the ultimate step of the set up journey can raise issues related to queries that return less relevant results or issues related to search performance. This topic is covered in another lecture in this course. Now that we have some initial background of potential issues with Elasticsearch, let's go one by one. Using a practical approach will expose the pitfalls and show how to overcome them. So before we start messing up our cluster to simulate real world issues, let's back up our existing indices. This will have two benefits. After we're done, we can get back to where we ended up and just continue on in the course and we'll better understand the importance of backing up to prevent data loss while troubleshooting. First, we need to set up our repository. So let's open up our Elasticsearch YAML file using your favorite editor. I like that. No, let's see. Elasticsearch. Why yaml? And we want to make sure we have a registered repository path on our machine. So we're looking for the path dot repo. Don't think there's one in here. Let's go ahead and add one then. Path got repo. Square bracket. Home student backups that should do the job. All right. Control o, enter control x. So that saved. And we might want to save a copy of this config file now as well so we can get back to it at the end of the lesson. So let's make a copy, we'll say pseudo c.p, that's the Elasticsearch Elasticsearch dot y html and let's just copy that into our home directory and that way we can just copy that back when we're done if we need to restore any of those settings later on. Okay. So we need to make sure that the directory exists that we're going to be storing that repository into and that Elasticsearch can write into it. So let's say maker dash p home student backups. And will change the group on that to elasticsearch like so sudo changed group Elasticsearch. Home Student Backups. And finally make it rideable. Pseudo change mod g plus w home student backups. And we need to restart Elasticsearch to pick up that configuration change we made. So let's say sudo in system control stop Elasticsearch start service. And we'll restart it. Okay. So now we can register the new repository to Elasticsearch at the path we configured with Curl request. Put local local host 1900. Underscore snapshot slash backup dash repo backslash. Data. Raw. Quote, Curly type will be filesystem and the settings will have a location of home students backups, backup dash repo, close everything out and looks like a took. Now we can initiate the snapshot process to do the backup with kernel request. Put localhost 200 underscore snapshot. Back up dash repo and we'll call it snapshot dash one. So it looks like that worked. We can check the status of that with a simple get request with kernel request. Get local host native snapshot. Back up repo. Snapshot one. And we'll make a pretty. Looks like it worked. It says the state was to success. All right, cool. Very good. Now that we have our data backed up, we can now proceed to nuke our cluster. So let's get started. Well, let's recap on the basics about logs. So we'll start by looking at the Elasticsearch logs. Their location will depend on the path that logs setting in your Elasticsearch. Why yaml? By default they are found in var log Elasticsearch slash whatever your cluster name is, start log. So basic tailing commands come in handy to monitor the logs in real time. And so say want to keep an eye on these logs off to the side? I'm actually going to start a different terminal window here, so let's go ahead and start a new Telnet client here. It would help if I typed in my password correctly. There we go. All right. And let's see where those logs live. Those are going to be in var log Elasticsearch. So our account has insufficient rates to actually read these logs. Now there are various options to solve this. For example, a valid group assignment of your Linux user or one generally simpler approach is to provide the user sudo permission to run Shell as the Elasticsearch user. We can do this by editing the pseudo file using the pseudo under route. So let's just say pseudo vs pseudo. And we will add the following line. The center to the bottom here. How about username? All equals parentheses. Elasticsearch parentheses. No password. All that should do it. So control O.o. Control X. So after we've done that, we can run the following command to launch a new shell as the Elasticsearch user sudo dash S2 Elasticsearch. Cool. So now we should have the permissions. We need to actually look at these logs, so let's try that again. CD bar. Log Elasticsearch. That's better. And now we can do things like tailed ash and want to look at the last 100 lines in this log file. And our cluster name is actually Elasticsearch start log because we haven't changed it. And there you have it. Or sometimes you just want to look for error messages, right? So for example, we could look at the last 500 log lines and pipe that into grep for error and that would just show us any errors. Fortunately, we don't have any because our cluster is healthy, so that's cool. And sometimes it can also be useful to grab a few surrounding log lines with the context parameter because the messages and struct stack traces can be multi-line sometimes. So we could say, for example, cat Elasticsearch dot log grep bootstrap. Dash. Dash context equals three to get the three surrounding lines for each hit there. So for example, here we have a bootstrap hit and the three lines before and after it as well. So those are some useful tricks for looking at the logs. All right. So let's start talking about bootstrap checks. We'll go back to our primary terminal here. Bootstrap checks are pre-flight validations performed during a node start, which ensure that your node can reasonably perform its functions. There are two modes which determine the execution of bootstrap checks. Development mode is when you bind your node only to a loopback address localhost or with an explicit discovery type of single dash node. No bootstrap checks are performed in development mode, and then in production mode is when you bind your note to a non loopback address like 0.0.0.0, thus making it reachable by other nodes. This is the mode where bootstrap checks are executed. Let's see them in action because when the checks don't pass, it can become tedious work to find out what's going on. So one of the first system settings recommended by elastic is to disable heap swapping. This makes sense because Elasticsearch is highly memory intensive and you don't want to load your memory data from disk. There are two options for this. One is to remove swap files entirely or minimize sloppiness. This is the preferred option but requires considerable intervention as the root user. Or we can add the bootstrapped on memory lock parameter in the last search dot, y'know. So let's try that second option. Let's go ahead and open our main configuration file with pseudo nano and see Elasticsearch. Elasticsearch dot y IMO. And we'll go ahead and find the bootstrap drop memory lock setting. And uncomment that to allow it to be true. Write that out and quit and let's go ahead and restart our service. So studio system control stop Elasticsearch start service. And let's restart it. And after a short wait, we should see some indication of what's happening. All right. So, yeah, we actually got an error as a result of doing that. So let's check our logs and find out what happened. So let's go spelunking through here and see what went wrong. Just got to hit the up arrow here to do a fresh tail of my log. And there we have it. So there's our error and it says bootstrap checks failed memory locking requested for Elasticsearch process. But memory is not locked. But didn't we just lock it before? Well, not really. We just requested the lock, but it didn't actually get locked, so we hit the memory lock. Bootstrap check here. Now, the easy way to fix this in our case is to allow locking and overwrite into our system to a unit file like this. So let's go back to our other window here. Sudo system control. Edit Elasticsearch thought service. And we're going to put in the following config parameter here service. Limit mem lock equals infinity. All right. And let's try spinning that up again. And this time it should be okay. All right. Looks like success. Okay. So let's talk about heap settings next. Now, if you start playing with the JVM settings in the JVM dot options file, which you will likely need to do because by default these settings are set to low for actual production usage. You may face a similar problem as as we just did. So how is that? Well, by setting the initial heap size lower than the max size, which is actually quite usual in the world of Java. Let's open up that option file and lower the initial heap size to see what's going to happen. So sudo nano etsi elasticsearch JVM dot options. VM dot options. And let's go ahead and change these memory settings here. Then a comment, not the original one. So I can go back to them later and we'll set some new ones X and S 500 megabytes and slash x and x one gigabyte. All right. So we've lowered the initial heap size. Let's go ahead and save this setting. And we'll restart our service again. Stop it. And I'll start it. And we'll see what happens as it spins up. Well, looks like we had an error. So let's go back to our logs and see what's going on. So back to the other window and I'll hit the up arrow just to tail the last one lines again. All right. Well, there we have it. Error bootstrap. No validation. Exception bootstrap checks failed. Initial heap size not equal to maximum heap size. So that's telling us pretty explicitly what the problem was there. Now, generally speaking, this problem is also related to memory logging, where they need to increase the heap size during program operations may have undesired consequences. So remember to set those numbers to equal values and for the actual values, follow the recommendations by elastic, which in short is lower than 32 gigabytes and up to half of the available RAM memory. Let's go ahead and change those back before we forget. Yeah. So we'll set that back to one gig for both and I'll just use Control K to get rid of those lines and control o to save and control x, so we should be back in option. Let's try starting it up again, just to be sure. All right. That time has started successfully. So let's talk about some other system checks you may want to perform when things go wrong. There are many other bootstrap checks on the runtime platform and its settings, including a file descriptors check a maximum number of threads, check a maximum size, virtual memory check and many others. You should definitely browse through their descriptions in the docs, but as we're running the official Debian distribution that comes with a predefined system D unit file, most of these issues are resolved for us in the unit file, among others. We can check that unit file to see the individual parameters that get configured. Let's take a look at that unit file to see what's in it. We can say sudo cat user lib system. DX System, elasticsearch dot service. All right. So just take a look at the different things that you have at your disposal here. All sorts of things that could go wrong, but by default, they should be okay in our installation. So just remember that if you run the Elasticsearch binary on your own, you will need to take care of these settings as well. Now, the last check we'll run is the one that will carry us nicely to the next section of the lesson dealing with clustering. But before we dive in, let's see what are the configuration parameters that Elasticsearch checks during its startup with a discovery configuration check? There are three key parameters which govern the cluster formation and discovery process. Let's pull up our wine ML file to take a look. Pseudo nano etsy elasticsearch elasticsearch dot y IMO. All right. So one is discovery dot seed hosts should be down here. Yep. Now, this is a list of ideally all the master eligible nodes in the cluster that we want to join and draw the last cluster state from. Now there's also a discovery dot seed underscore provider setting that you could set here as well, and that would allow you to provide the seed hosts lists in the form of a file that gets reloaded on any change instead of specifying it within the configuration file itself. Also, let's look at the cluster dot initial master node setting here. This is a list of the node names, not hostnames for the very first master elections. So before all of these join and vote, the cluster setup won't be completed. But what if you don't want to form any cluster, but rather just want to run in a small single node setup? Well, you might think you could just eliminate these settings and the y small file. Right. But no, that won't work. After starting up, you would hit another bootstrap error, since at least one of those parameters needs to be set to pass a bootstrap check. So we're going to go ahead and put those back because you can't actually get away with that. So let's see why this is and dive deeper into troubleshooting the discovery process. First, I'll exit out of here. And let's shut down our cluster before we forget. Just stop the service. All right. So after we've successfully passed the bootstrap checks and started up our node for the first time, the next phase in its lifecycle is the discovery process. Now, to simulate the formation of a brand new cluster, we're going to need a clean node. So we need to remove all the data of the node and thus lose all previous cluster state information. That's why we backed everything up to a snapshot earlier. Now, remember, this is really just to experiment in a real production setup. There would be very few reasons to do this. I'm going to go to this other window here where I'm logged in and see Elasticsearch user because I'm going to need its permissions to do this stuff. Armed RF var lib elasticsearch. Last star. All right. We blew away our entire node there. So now let's imagine a situation where we already had a cluster and we just want the node to join in. So we need to make sure the cluster name is correct and linked to some seed host either by IP or hostname and port. So let's go ahead and open up our y am file. We use vim because that's what's installed under this account. That's the Elasticsearch Elasticsearch dot waymo. All right, so we need to make sure that we have a cluster name. Hit. I'd go on to insert mode and now I can edit it. Will change my application to lecture cluster. It would help if I typed it right. And we need to set our Discovery seat hosts. Do. There they are and we'll change that to 127.00.1 Colin 9301 Now this is just a demonstration, so we're using a loopback address. Normally you put an hostname or an IP here and the actual transport port of one or more of your nodes in the cluster. And just to force the failure that we're interested in, I'm going to comment out this line for the initial master nodes, and that way it's not going to be able to reach the master. We'll see what happens when we hit that failure. Let's go ahead and hit escape colon WQ. Exclamation point to right and quit. And now let's start up our service. All right. It looks like it started successfully. Let's check our route, End Point, to see if it really is running. Okay. Curl Local host, coordinated 100. All right. So we did get a nice response with various details here, but something is missing the cluster UUID. This means that our cluster is not actually formed. And we can confirm this by checking the cluster state with the cluster health API. Let's say curl. Local host coordinates 200 slash underscore. Cluster slash health. And after about 30 seconds of waiting will get an exception. Indeed we did, master. Not discovered exception. All right, let's Taylor logs and see that the note didn't discover any master and will continue the discovery process. So let's check our logs and see what happened. Let's look at the past 500 lines here and those that's lecture cluster. Don't log this time because we changed the cluster name. That's the relevant message here, master not discovered. This note is not previously joined a bootstrap cluster and cluster initial master nodes is empty on this node so it's going to continue. Discovery on 120 7.0.1 9301 from the host providers. But yeah, that's basically telling us that we had a problem actually electing a master because we didn't list any master nodes and it couldn't find any makes sense. Right? So these issues are going to be very similar when forming a new cluster and we can simulate that in our environment with the cluster initial master node settings. So again, let's make sure there's no previous data on our node. We'll go ahead and blow away that. Let's stop the service before we forget, huh? So back to this other site here. Stop service. Now we're going to blow away. Var Lib Elasticsearch again. Like so. All right. And now we can edit our way and I'll file again. And now we're going to go back to make sure our cluster name is still a lecture cluster. And now we're gong to set our initial master nodes. So it was complaining before that we had an empty list there. So let's give it a a list and I'll go to insert mode and now we can edit this line, uncomment it and we'll set it to the list of Node one, note two and Node three. So let's go ahead and hit escape. Colin WQ Exclamation point two writing quit and we'll restart the note again. Start the service. And see what happens this time. All right. Looks like it went okay. But again, let's check and make sure. Let's hit the route. End Point. Still we have no cluster EOD. So we didn't actually join a cluster that failed. And if we do a health check again. We'll have to wait 30 seconds for that to time out. All right. Same deal, Master. Not discovered. Let's check the logs again to see what happened this time. So we'll just tail those last 500 lines again. And we're going to look for something about discovering master eligible notes to do. Probably should have crept for Warren, huh? This looks interesting. All right. Node one not discovered yet. This node must discover massive eligible nodes. Node one no to a node three to bootstrap a cluster. We only discovered node one. So, yeah, you can't just specify nodes that don't exist there. All right, so we have performed some experiments here, so we'll need to use your imagination to complete the picture. Now, in a real production scenario, there are many reasons why this problem often appears. Since we're dealing with a distributed system, many external factors such as network communication come to play and may cause the notes to be unable to reach each other. So the problem might not just be that I listed a bunch of fictitious hosts there. It might be that those are valid hosts, but they can't be reached for some reason to resolve these issues. You need to triple check all your settings. So again, let's go back into them. We need to make sure the cluster name, all the notes are joining or forming the right cluster. The no name and a miss type in the no names can cause invalidity for the master elections and the seed hostnames, APIs and supports down here somewhere. Got to make sure those all have valid seed hosts linked and that the ports are actually the configured ones. We need to check connectivity between the nodes and the firewall settings. So use telnet or similar tools to inspect your network and make sure it's open for communication between the nodes, the transport layer and the ports especially. Also check SSL. Intel's communication encryption is a vast topic and we're not going to touch that here, but it's a usual source of troubles invalid certificates and untrusted certificate, authorities and things like that. Also be aware that there are special requirements on the certs when encrypting No. Two, No communication. All right, the last thing we're going to explore is the relationship between the shard allocation and cluster state as these two things are tightly related. But first, we need to change the Elasticsearch y email configuration to let our notes successfully form a single node cluster. So back in our configuration file here, let's just set the initial master as the node itself and start the service. So to take out No to a No. Three and just hit I had to go to insert mode. Forgot I was in vim there for a second. Escape. Colin WQ exclamation point. We wrote that out. So now let's restart our service yet again. Stop it. Started. All right. And again, we'll carry the cluster health API. Let's see what happened. So we can see the cluster status is, in fact, green. That's good. So what does cluster status mean? Well, it actually reflects the worst state of any of the indices that we have in our cluster. The different options include red. That means one or more shards of the index is not assigned in the cluster. This can be caused by various issues at the cluster level, like disjoint nodes or problems with disks and things like that. Generally, the red status marks very serious issues, so be prepared for some potential data loss. It could also be yellow. In that case, the primary data are not yet impacted. All the primary shards are okay, but some replica shards are not assigned. Like, for example, replicas won't be allocated on the same node as the primary shard by design. This status marks a risk of losing data and green means all shards are well allocated. However, it doesn't mean that the data is safely replicated as a single node cluster, since with a single shard index it would be green as well. So now let's create an index with one primary shard and one replica. We'll do that with curl request. Put local host 9200 slash test what's called the index test backslash slash data raw curly with the following settings. Curly bracket number of groups of shards will be set to one and the number of replicas will be set to one as well. Close everything out. All right, so suddenly our cluster will turn yellow because our worst performing index, the only one we have, is also yellow. Let's check our health again. Yep. Now we're yellow. Now you can also check the shards assignment with the Cat Shards API and see what's going on there. So let's say curl localhost 9200 slash underscore cat slash shards. Question mark v aha. So we can see that we have unassigned shards here. Or if you want a more descriptive information, you can use the cluster allocation. Explain API, which provides an explanation as to why the individual shards were not allocated. To do that will say Karl local host 9200 cluster allocation. Explain pretty. And that tells you very explicitly what's going on in our case, as I mentioned before. The reason is due to the allocation of the data replica to the same node being disallowed, since it makes no sense from a resiliency perspective, you wouldn't have a replica on the same node. That's that's silly. So how would you resolve this? Well, we have two options. One would be to remove the replica shard, which is not a real solution. But if you need the actual status, it will work out. Or you could add another node on which the shards could be reallocated. So let's take that second route. So to simulate the following failures, I actually have two different nodes running on the same host here, and setting that up is kind of involved and we're going to do that later in the course as we go into failover. So for now, I just want you to watch and not actually try to follow on yourself. So I've already done some of the grunt work here. Basically, you need to set up a separate system to a unit file for the second node and a server configuration and stuff like that. So just watch from this point on. Okay. So anyway, let's start by reviewing the main configuration file of that second note that I've already set up and will ensure that it will join the same cluster with our existing nodes. So let's say sudo nano etsi Elasticsearch dash node two is where I put that. All right, so we have the same cluster name. We're calling our node here Node two. And we can see that our seed hosts is set to a loopback address, hopefully. Yep. And we can see that our master knows consists of node one and node two. Let's go ahead and exit out of here and start that second node sudo system control start Elasticsearch that's node two dot service. Okay. So at this point I have started up a second node on the same VM. Again, there's quite a bit of configuration behind making that happen, so just watch for this part of it. So now that we have a second node spun up, we should be back in a green status. So let's check. Let's say kernel dashed silence. Local host. 200 slash underscore cluster slash. Health. Pretty. And we'll just grep for the status line. And our status is green. Great. Okay, so we've resolved her issue and the replica shards were automatically reallocated. Perfect. So let's continue with this example. To simulate the red cluster state, let's start by removing the index and creating it again, but this time with only two primary shards and no replica. And we'll quickly see why this is a bad idea. So first of all, delete the one that we have with Curl Bash Dash request delete local host 9200 slash test and I will recreate it with curl request put localhost 9200 slash test backslash. And dash, dash data, dash raw, quick curly settings will set the number of shards. Two, one, two, two, rather four because we have two notes to work with and number of replicas to zero. So this seems like a pretty bad idea. You know, we have our shard split across two nodes, but no backups anywhere. All right, but so far, so good. You know, it's at least storing it. Let's check the shards of salmon to see what's actually going on here. Carol, local host, 9000 underscore cat slash shards, verbose. Okay. So we can see that each primary shard is on a different node, which follows the standard allocation rules set at the cluster level and at the index level. And you likely know where we're heading. So imagine the situation where some network issue emerges and your cluster splits up, resulting in disabled node communication, or even worse, some disk malfunctions leading to the improper functioning of a node. Now, the easiest way to simulate this is to just stop one of our nodes. So let's go ahead and kill No. Two with a pseudo slash spin slash system control. Stop Elasticsearch Dash No. Two dot service. And down it goes. So now if we check our status again to do. We are now in red status. That's a bad thing. So now let's check the explain API to learn more about what's going on. Curl local host. A200 slash underscore cluster slash allocation. Explain pretty. All right. So we cannot allocate it because a previous copy of the primary chart existed but can no longer be found on the nodes in the cluster. Well, that tells you what's going on. It's pretty well described. A node left as we have turned it off. But in the real world that has various potential causes and no valid shard copy can be found in the cluster, in which case we're missing data. Unfortunately, there's no easy solution to this scenario, as we do not have any replicas and there's no way we could remake our data. So firstly, if you are dealing with some network problems, try to thoroughly inspect what could go wrong like a misconfiguration of firewalls and inspect it as a priority, since data cannot consistently be indexed in this state. Now, depending on the document routing, many indexing requests can be pointed toward the missing shard and end up timing out. For example, this to try to insert a document and see what happens. Curl request. Post local host 200 slash test underscore doc data raw and we'll just say a message. It's data. And this should lead to an exception. And after about 30 seconds or so, it finally timed out on me. Now, secondly, if no possible solution was found, the only option left to get the index to work properly may be to allocate a new shard. But be aware that even if the lost node will come back afterwards, the new shard will just overwrite it because it is in a newer state. Now we can allocate a new shard with the cluster reroute API. So here we will allocate one for the test index on the node dash one that operates correctly. Note that we have to explicitly accept data loss. So curl request post local host 9200 slash underscore cluster slash reroute. And we want pretty results. Backslash did all. Quick curly commands. Actually, there's going to be a square bracket. We have a list of them. And curly brackets. Allocate empty primary. Index test. Shard. This one node is node one and except data loss. We'll be true. Cause everything out. I think that's right. All right. And afterwards, we should no longer experience timeouts during indexing. All right, so we're done. But we just need to restore everything from our backup now, because we did do some pretty invasive stuff to our index here. So we're back at the point where you should be following along if you were following along earlier. Okay. We need to restore from that back up and make sure we're not left with any lingering issues that we might have introduced. So we're going to restore all of our original indices that we backed up earlier. Before we can do that, we need to do some cleaning up. So first, we need to make sure that the repository path is registered again in the Elasticsearch dot. As we've done some changes to it during the exercise. So let's go ahead and reference our stored config file that we squirreled away at the start of the lesson, and we'll put that back. So let's see, go back to our home directory. I think that's where we put it. Yep. There's Elasticsearch that Lyonel. So let's go ahead and move that back into position. Sudo move Elasticsearch. So I am going to see Elasticsearch. All right, let's double check that it's there and looks correct. Well, I'm the wrong user sudo. Listen to me and we'll just go ahead and edit it directly at c. Elasticsearch. Elasticsearch why? Email. Make sure things look normal. All right. Things are back to how we started. We have node one. We still have the path to repo set to home student backups. That's very important so we can restore that backup memory lock is commented out again. Everything looks like it's back to default settings, so that's good. All right. Looks good. Now, we do need to make sure that Elasticsearch has permission to read that configuration file we just restored first. So let's go to the SC folder and do a pseudo Alice start. L-A Elasticsearch. See we have. Yeah. So we can see that it's owned by the root group. We need to change that. So, sudo change group. Elasticsearch. But, Lionel. Check that again. All right, that looks better. So now we should be able to restart our main node. Like so. Right. So now we can reregister our repository again to make sure it's ready to provide the backup data. Curl request put will host and a few hundred slash underscore snapshot slash backup dash repo backslash data all. Type is filesystem system. Settings. Location. Home Student Backups. Backup dash. Repo. All right. And we can check the available snapshots in the repository with a simple cash request to our backup repo. And we should see our snapshot one waiting to be restored. Curl Local Host. Note both underscore cat snapshots. Flash Backup. Dash Repo. All right. It's a success. Now, to prevent any rights during the restore process, we need to make sure that all of our indices are closed. So, however, as of Elasticsearch eight, they've made it not that easy. You actually need to disable a safety feature that prevents you from closing all of your indices at once together. So let's fire up our editor and edit our Elasticsearch Dynamo file. And we're looking for the setting action dot destructive underscore requires, underscore name, uncomment that and let it be set to false. And after that, we're going to stop and restart the service again to pick that change up. Curl request post will host a 200 slash underscore all underscore close. And finally we can restore a backup with Colonel request. Post localhost 9000 snapshot. Backup. Dash. Repo slash snapshot. Dash one. Slash underscore restore. And it took it slow after a few seconds. If we check our indices, we should see all the original data back in place. Curl Local Host 9200 Slasher and Score Cat Slash Indices. And there's our original Shakespeare index, for example. So, yeah. Things have been restored. Great. So now that you're armed with foundational knowledge and various commands on troubleshooting your Elasticsearch cluster, the last piece of advice is to stay positive even when things are not working out. It's part of and parcel to being an Elasticsearch engineer.
