# Snapshot Lifecycle Management

Let's face it. Nothing is perfect. The better we architect our systems, though, the more near perfect they become. But even so, someday something is likely to go wrong, despite our best effort. Part of preparing for the unexpected is regularly backing up our data to help us recover from eventual failures. Snapshot Lifecycle Management, or SLM, as will refer to it in this course, helps us fully automate backups of our Elasticsearch clusters and indices. We can set up a policies to instruct asylum when to back up, how often and how long to keep each snapshot around before automatically deleting it. To experiment with snapshot lifecycle management will need to set up a repository where the snapshots can be stored. Configure the repository in our Elasticsearch cluster defined the SLM policy to automate snapshot creation and deletion and test the policy to see if we registered it correctly and it works as expected. The steps will take our easy to understand points. We break them down into their basic actions. A repository is simply a place to save files and directories, just as you would on your local hard drive. Elasticsearch uses repositories to stored snapshots. The first type we'll explore is the shared file system repository. In our exercise, since we're working with a single node, this will be easy. However, when multiple nodes are involved, we would have to configure them to access the same file system, possibly located on another server. The second repository type we'll explore relies on cloud data storage services that use service specific plug ins to connect to services like West three, Microsoft Azure Object Storage or Google Cloud Storage Repository Checks. After picking the repository that best fits our needs. It's time to let Elasticsearch know about it. If we used a shared file system, we need to add a configuration line to the Elasticsearch acquire HTML file. As we'll see in the exercises for cloud based storage repositories. We'll need to install the required repository plug in on each node. Elasticsearch needs to log into these cloud services, so we will also need to add the required secret keys to its key store. This will be fully explained in the exercises. At this point, all prerequisites are met, and we can define the policy to instruct SLM on how to automate back ups with the following parameters schedule what frequency and time to snapshot our data. You can make this as frequent as you require without worrying too much about storage constraints. Snapshots are incremental, meaning that only the differences between the last snapshot and the current snapshot need to be stored. If almost nothing changed between yesterday's backup in today's data, then the snapshot will require negligible storage space, meaning that even if you have gigabytes of data, the snapshot might require just a few megabytes or less. The name defines the pattern he use when naming the snapshots repository specifies where to store our snapshots. Config dot indices is the list of indices to include and retention is an optional parameter we can use to define when SLM can delete some of the snapshots. We can specify three options here expire after. This is a time based expiration. For example, a snapshot created on January 1st with an expire after set to 30 days will be eligible for deletion after January 31st. Men count tells Elasticsearch to keep at least this number of snapshots, even if they're all expired. And Max Count tells Elasticsearch to never keep more than this number of snapshots. For example, if we have 100 snapshots and only one is expired, but Max Count is set to 50, then 50 of the oldest snapshots will be deleted, even the unexpired ones. With our policy finally defined, we can display its status. This will list policy details and settings. Show us how many snapshot attempts were successful and how many failed. When SLM is scheduled to run the next time and other useful information as well. So with the theoretical part out of the way, we can finally learn by doing. Since we're experimenting with a single note here, things will be pretty straightforward. For the repository, we'll just use a directory located on the same machine where Elasticsearch is running. So let's start by creating a mount shared ESR directory. Pseudo mkdir bash p slash mmt slash shared slash s. All right. Now, in a multi node, Elasticsearch cluster, you would then have to mount your actual shared storage on each node using the directory mount. Shared. Yes, as a mount point at this point. So if you're using NFS besides entering the appropriate mount command such as Sudo mount, whatever your NFS server is to this directory, you would also add relevant entries to your ETSI FS tab file as well so that the NFS shares are automatically mounted each time the servers boot up. But again, for the sake of example, we're just going to store this on our local file system in a directory under Mount Shared S and not a shared file system. First, let's make the Elasticsearch username and group the owners of Mount shared. Yes, and then give the user full read and write permissions to do that will say sudo change own dash. Ah. Elasticsearch calling Elasticsearch. Slash. Empty slash. Shared slash. Yes. And Sudo change mod 75 oh slash I'm in t slash shared slash s. All right. Next will either line path dot repo amount shared yes to Elasticsearch start way so that the service knows the location of its allocated repository. Note that on production systems we should add this line to all master and all data nodes as well. So pseudo dash s2 elasticsearch. And now we're Elasticsearch and we can say echo single quote path dot repo. Colon square bracket. Double cooked slash. Empty slash. Shared slash. Yes. Square bracket. Single quote. And we're going to send that off to Etsy, slash Elasticsearch. Elasticsearch. Not why IMO. So this is just going to append that line path dot repo amount share to yes to the end of our Elasticsearch dot line ML file. Great. All right. Now, in every know where we make this change, we need to restart Elasticsearch to apply that new setting. So let's get out of the Elasticsearch account here and we can now say sudo system control restart Elasticsearch dots service. Give that a few seconds. At this point, we can now register the repository in our cluster like so. Curl. Dash. Location. Dash. Request puts. Let's do a backslash there to get a new line. HTTP colon, slash slash. Look localhost on port 9200 slash underscore snapshot slash backup. Underscore repository. And single, quote, another backslash for another new line. We're going to get the header line because our special version of Curl takes care of that for us. So we're just going to jump straight to dash dash data, dash raw, single quote, curly bracket. And now we have the body of our request here. We're going to specify a type of force. So as in file system, comma and the following settings within this block, we will have a location. That is set to slash and anti slash shared slash. Yes. And under there will have a backup underscore repository. Double Coke and close off all of our curly brackets and single quote there. And that should send it off and looks like it went through. We have an acknowledgement, okay, with the next command, we can list all the registered repositories to make sure everything is configured properly. So we'll just say curl local host 1800 underscore snapshot pretty. And there you can see that it did take we do have that now shared. Yes back up repository set up successfully. We can now define our first asylum policy. Let's go through the details of this next action as we go through it. We'll start off by just kicking off the request with Colonel. That's just location. Dash. Request. Put backslash. HTP. Colon slash. Slash. Local host. 1800 slash. Underscore. Slim slash policy. Slash backup. Underscore policy. Underscore daily single quote. That's the format there. Double check it. We'll skip the header line because our curl command actually does that for us. And we'll go straight to dash. Dash data. Dash raw. Single curly bracket. And here comes the body of the request. We'll start with schedule. And so that. 20033. Star. Question mark. Double quote, comma. So what does that mean? Well, we were instructing it to run every day at 3:03 a.m.. This is specified with a cron expression here. And the format here is second minute our day of month, month, day of week and year. Where the year option. Where the year parameter is optional. All right. Next line name is going to be in plenty brackets. Back up. Dash. Curly bracket now slash DX curly bracket, pointy bracket and the quote comma. So all the snapshot names will start with a fixed string back up dash and the date will be appended to it. That's what we're saying here. A random string of characters will be added at the end to ensure that each name is unique. It's usually a good idea to use date math. This helps us to easily spot the date and time of each object, since the resulting names could end up looking like clusters. Six Dash 20 20.0 3.23. Underscore 15. Colon 16. So that would be like easy to read and figure out what that file represents. I still say repository and start that up to back up underscore repository. They will store the snapshots in the repository that we previously registered in our cluster. All right. Next, we're going to say config. And that's going to be a new block here. And we'll say indices. And set that to an inside square brackets, a star character. So by using the special asterisks, wildcard characters star there, we include all the indices in our cluster as part of the back up close off that block there. And we'll say retention. Again a new block expire underscore after and we'll set that to 60 days. So we're just instructing SLM to periodically remove all snapshots that are older than 60 days here. Now we can just close everything off. Like so. And it went through. Great. So let's check out the status of our newly created policy now. We can say Colonel, local host. Coordinator. Slash. Underscore. Slim. Slash policy. Slash backup. Underscore policy. Underscore daily. And question mark human to make a human readable. And looks like it's in there. Let's go for pretty format, actually. Even better. All right. So it looks like that took we have all the parameters in there successfully that we specified. Looks like it's all in there correctly. So besides confirming the policy settings we just defined, we can also see when this will run the next time with next execution. How many snapshots have been taken? How many have failed? And so on. Of course, we may not be able to wait until the next scheduled run, since we are testing and experimenting so we can execute the policy immediately by using the following command. Curl. Dash. Location. Dash. Request. Post. Actually p local hosted NATO. Oops. Underscore asylum policy. Slash back up. Policy daily and underscore execute to actually make it do it. Now, let's see what that does. Well, it came back with a snapshot name and what had actually saved it off, too. So it looks like it ran. Let's check how much data storage is now used by the repository. Let's switch back to our Elasticsearch user. And check out our amount shared. Yes. Directory. And we can say, do you dash h dash? Max death equals one. And looks like we have 20 megabytes of stuff in our backup repository. So for our first run, this should be similar in size to what was actually used by our indices by checking the status of the policy again. He'll show us a new field last success indicating the snapshot that we just took earlier if we were to keep running this over and over and over again. So there you have it. Next, let's set up asylum within a US S-3 repository. Amazon Web Services is very popular in the corporate world, so it's useful to go through an example where we use in a US S3 bucket to store snapshots. Now the following steps are going to require basic knowledge about S3 and IAM. Since we need to configure the bucket and secure login mechanisms beforehand, you're going to need an account to actually go through this exercise and have some familiarity with us. If that doesn't describe you and you're not comfortable with giving us a credit card number because the service could cost real money, just watch and you don't have to actually follow along in this exercise if you don't want to. That's okay. But first, to be able to work with an S3 bucket S3 is Amazon Simple Storage Service. Elasticsearch will require a plug in which can easily be installed first, will go to the user share Elasticsearch directory and from here we'll say sudo bin. Slash Elasticsearch dash plug in install. Repository Dash S3. Just like that. And it should just do that automatically. We will continue and let it do what it wants to do. All right. And we're good now. We just need to restart the service to actually activate that plug in pseudo system control, restart the Elasticsearch start service. And that will take a few seconds and we're done. Great. All right. So next, Elasticsearch needs to be able to log into the services offered by USB three in a secure manner. So to do this, we need to log into our US account and create an IAM user with the necessary S3 permissions before continuing with this lessons. Now again, you're going to need a basic knowledge of IWC and NATO Lewis account to do this, which also involves having a credit card on file with IWC. So again, if you're not comfortable with that or you don't want to spend money, just watch. Okay? So if you do want to go follow along anyway and set up an AWB account, you can follow the official guides out there if you don't already have one. It's pretty simple. You will be asked to enter a payment method, but this particular exercise actually does follow under the IWC free tier. So if you were to set up a brand new IWC account, you could actually get through this without paying any money but still be careful. So I'm going to log into the IWC console. I've actually already done that. So to create the credentials we need, we're going to go to our IWC console and go into IAM. Identity and access management and from here will create a new user that we can use for this purpose. So we'll click on users and add a user and we need to give this user a name. Let's call it Elasticsearch Dash S3. Boxer Tron. Here we go. And now we need to make this programmatic access because we are going to be using this from a program, go to permissions. And from here, we're going to attach existing policies directly and we're going to search for Amazon S3 full access and go ahead and select that and move on to the tag screen. We don't need any review what we've done, so we're creating a new user, Elasticsearch S3 with Amazon, this three full access policy, go ahead and create that user. And now we have the access key ID and the secret access key for this new user. Now as soon as you leave the screen, you will never be able to get that secret access key again. So if you are following along, be sure to show that and copy it off somewhere safe. Same thing with the access kid, so make sure you have those squirreled away somewhere so we can now enter that into Elasticsearch so it can communicate with our S3 buckets. Once you've configured the IAM user and you have your keys available, let's add them to Elasticsearch is key store. So this would need to be repeated for each note if we were on a production cluster. But for this note, let's just say pseudo. Note that I'm in the user share Elasticsearch directory here bin slash Elasticsearch. Dash key store add as three dot client. Whoops. Help if I spelled that right. Client dot default dot access underscore key. Okay. And it will prompt you for the access key. I already have one, but I'm going to go ahead and overwrite that and I'm going to copy that. From the one that I just made. And right click to paste that in. And I'm going to do the same thing for the secret key. So we'll say pseudo bean slash elasticsearch. Dash key store ad as three dot client dot defaults dot secret to underscore key. Make sure that's all spelled properly. And same deal. I'm going to overwrite that and select the new one. Copy it. And right click to paste that and take that in. We can now instruct the S3 client to reload its security settings and pick up the new logging credentials and make sure they look okay. Curl. Dash. Location. Dash. Request. Post. HTP local hosts. 9200 Underscore Nodes. Reload secure settings. In pretty format. Looks like it worked. Successful as one failed to zero. That's all a good sign. All right. So at this point, we can register the cloud storage repository. The only mandatory parameter for registering an S three repository is its bucket name. So let's just do that. Curl. Dash. Location. Dash. Request. Put. HDP. Local host 9200. Underscore snapshots. Slash backup. Underscore repository. Underscore is three. And again we'll skip the header because we're cheating. Dated Ashura. Kick off that block. The tape will be as three and the settings. We'll just consist of a bucket name. And we'll call her a bucket elastic dash slam. Close all that off. And not surprisingly, it gave me an error because that bucket doesn't actually exist yet. So back to the console. So let's just go to S3 within the console here. And from here we'll make a new bucket, create bucket, and you will need to give this some globally unique name. So I'm going to just call this elastic dash and slam a dash f Kane. But you cannot use that same name because it has to be unique. So substitute your own name, organization, just something to make that special. And that should be pretty much all that we need. The default settings should work for us and let's create bucket. All right. So an elastic asylum if Cain is sitting there waiting for us to receive data. And let's try that again. Instead of the classic slam, it's going to be elastic asylum F Cain or whatever you named yours to. It has had to be a unique name and that time it worked. Very cool. So if we now list all registered repositories, we should see both our shared filesystem repository and our cloud storage repository. Let's say curl. Local host, 1900. Slash underscore snapshot. Pretty. And there they both are. Cool. Now let's define our second asylum policy. The policy settings will be similar to before, but will now target our brand new as three repository as a destination for a snapshot. So I'll plod through this as quickly as I can. Curl. Location. Request put. HTP local host coordinator a100 slash underscore SLM policy backup policy daily. As three. Skip the header. Go straight to the data. Rob Lock. We'll start with a schedule indicating that we'll run this at 3:30 a.m.. Just like before. Our name again will just be back up dash timestamp. Just like that. Our repository. Will be set up to our new one backup underscore repository, underscore S3 and our config block will just tell us which indices we want to back up. And again, we'll say all of them. Okay. And the retention will make it a little bit more complicated this time. Make sure you spell it right. Again. We'll have expire after. And set that to 60 days. But we'll also set a min count in a max count just to make things interesting. Close everything off and hopefully I don't have any typos. We'll see if it works. All right. And it's true. So now we can fire off a snapshot operation like so. Curl location, request post. HTP local host 900 underscore a solemn slash policy. Slash backup policy. Daily as three. What's the name of our new policy that we just created? Underscore. Execute. See what happens. Looks like it worked. So the confirmation is displayed immediately. Now, this might sometimes give the illusion that the job is done. However, when a lot of data has to be uploaded, the transfer might continue in the background for a long time. But if we go back to our IWC three bucket console, we might be able to see some data coming in. Let's take a look. So there's that new bucket we just made. And yeah, there's data in it. Really cool. And you can also check this using the IWC command line interface if you want it to as well, instead of going through the user interface on the web console here too. But it looks like it worked. That's awesome. Congratulations on all your hard work. You're now armed with the knowledge to create awesome systems that automatically back up important data and can save the day when disaster strikes.
