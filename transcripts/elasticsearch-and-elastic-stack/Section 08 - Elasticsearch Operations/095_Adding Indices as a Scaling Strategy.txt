# Adding Indices as a Scaling Strategy

Okay. So once you've decided how many shards your index needs, you need to actually create that index and specify the shard count that you want. So let's talk about how to actually do that. And afterwards, we'll talk about how you can use new indices as a scaling strategy of its own. So here's an example of how to actually specify the number of primary and replicas that you want on your new index. And remember, the number of replicas is applied to each primary shard. So by saying number of shards ten. And number of replicas one, that means you're going to end up with ten primary shards and ten replica shards with one replica for each primary. So that can be a little bit confusing at first. Now, in the past, we sort of implicitly created new indices as we've created new data. And we've just been using the default settings for the number of shards because we didn't care. You know, we didn't have a lot of traffic in our little course here and we only had one machine. So it wasn't really worth thinking about too much. But in the real world, you want to think about this a lot. So make sure that any new indexes are formally created before you start inserting data into it with the number of shards that you think you're going to need for the foreseeable future. Now, to make this easier, there's also something you can look up called index templates, and that's the way to automatically apply mappings, analyzers, aliases and settings like that to any new index that gets created. It can save you some time as well. So let's actually get some hands on practice in doing this because it is an important thing to remember and having some sort of muscle memory on. So let's go off and actually create a new index with a specific number of shards. So now that we have Cabana, we can use it instead of a console, which is a little bit more friendly. So let's start by examining the existing settings on the Shakespeare index. To do that, we can just say get slash Shakespeare. You see how it even completes for you if you want underscore settings and hit the play button. So you can see here that by default it only created a single shard with a single replica. So that's the default on this particular system that we installed here. So that's how you can find out how many shards and replicas you have on an existing index if you need to. Now, let's say we're creating a new index, so we want to explicitly set a higher number of shards on. I mean, typically you would want more than one, right, for the ability to actually scale this out at all. So let's imagine that we're setting up a new index called Text's Test Index. So to do that, we would say put slash test index and then open curly brackets and we would say settings. Notice how it's doing all the tabs for me automatically. That's kind of nice number of shards even like lets me autocomplete that it's really handy to use as compared to just typing it in the hard way. Huh. We'll set that to three and we will set a number of replicas also just automatically in there for me to one. So that is how you would go about creating a new index with a specific number of shards and replicas. And remember, you can have multiple shards on one machine. So by saying number of shards three, that does not require me to have three actual hosts, but it gives me the flexibility to do that later on if I want to. So again, you need to think ahead as to how much capacity you think you might need going forward and try to pick the right number of shards based on that. Let's go ahead and hit play just to show you that it does, in fact, work. All right. That got acknowledged. So now we can do that same request again to get that information back, make sure that it actually took test index was what we call that right settings. And there it is. Number of charts, three number of replicas. One, so that is how you go about specifying the number of shards and replicas on a new index. And in the real world, that is something you will want to do before using it.
