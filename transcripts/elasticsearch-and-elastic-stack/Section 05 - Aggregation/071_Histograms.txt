# Histograms

Boom, boom. Another magical thing you can do with aggregations is very quickly generate histograms from your data that's in your search index. So what is a histogram? If you're not familiar with that term, you've probably seen them. Maybe you don't know what they're called, but all they do is display totals of documents bucketed by some interval range. So imagine, for example, we have a search index full of data about flowers. And one of the fields that you have in your documents is the pedal length in centimeters. So this histogram, for example, would let you visualize how many flowers exist for each range of half a centimeter in petal length. Let's say I had a bunch of flowers and some of them had a petal length of 1.23 centimeters and another one had a length of 1.135 centimeters and some had a length of 1.4 or five centimeters. All of those would get thrown into the same bucket of between 1.0 and 1.5 centimeters. So it's a way of visualizing the distribution of values in a given range of documents very easily. So to interpret this particular example here, you can see from this graph that there are about 25 flowers that have a petal length between one and 1.5 centimetres in length. And I'm not sure if that's 19 or 20 exactly, but there are 20 ish flowers that have a petal length between 1.5 and 2.0 and so on and so forth. There's about five that have a pellet lag between 2.0 and 2.5. So what's happening here is that we're aggregating documents together that fall within a given range of values and then plotting how many documents exist within each range. Let's make this real with some actual examples here. Let's say, for example, we wanted to bucket up ratings by 1.0 rating intervals. So instead of counting up all the half star ratings, we want to glom all the half star ratings in with their whole rating counterpart. So this would allow me to see how many one point X and two point X ratings there are, where 1.5 and 1.0 ratings are grouped together, 2.0 and 2.5 are grouped together and so on and so forth. So to do this, it's very simple. The syntax here is pretty much the same as we were doing before we start with an AGS block. Then we give that aggregation a name, whatever you want to call it. In this case, we'll call it whole ratings. And now, instead of saying average or terms, we're going to say histogram for the actual type of aggregation that we're doing. All we need to do now is specify two values for that histogram. The field that we're generating, the histogram on and the interval range that we want to bucket with in. Our histogram in this case would give us back counts of ratings in our ratings index that are bucketed by intervals of 1.0 by whole rating numbers. Another example here. Let's say we want to add up how many movies are in our index from each decade. We could create another histogram based on a year field and set up an interval of ten so that we grouped together all the movies for a given ten year span. 1980s movies and 1990s movies, for example. And count them all up by decade and give you the results. Now, Elasticsearch doesn't give you back an actual graph. Of course, that's what Cabana is for. We'll get to that. But I will give you back the data you would need to very quickly and easily construct an actual pretty graph of your own from this data. So you'll get back a JSON response that contains the counts for each bucket. You can take that to make a graph out of it later if you want to, and later in the course, we'll see how to do that very quickly. But for now, let's dive in and actually run these examples. So let's make it real and actually execute those histogram queries. We'll start by doing the example of bucketing up all of our ratings, our movie ratings data, so that all the half star ratings get bucketed in with our whole star counterparts. To do that will say curl dash x get 1.7.0.0.1 coordinate 100 ratings index. Underscore search size zero. Because we don't actually want query results, we just want aggregations and pretty kick off our request body with an AGS clause as before we'll call this aggregation. Whole ratings called anything you want really. And this will be a type histogram. This histogram will be operating on the field rating and use an interval of 1.0. Close off all those brackets and see what we get back. Cool. It worked. So you can see we have no longer any half star keys here. Only the whole stars because it's bucketed things together by whole reading intervals. So between zero and one we had 2740. So presumably that corresponds to all the 0.5 star ratings. All the ones and one point fives together have 90 to A for all the TOS and 2.5 26 so on and so forth. So yet again, you can see that the most common range here is four point something at 70,000 for a whole count there, but three point something not that far behind. So kind of an interesting distribution here. When you bucket it up this way, people are a little bit picky with their five star ratings, a little bit more than I might think. And obviously, there they really reserve 1.0 and below for the worst of the worst. Let's try that other example on this one. We're going to count up movies by the decade they were released in and you could extend this guys and play around. I encourage you to do so. Might be fun to see what the average rating is by by decade as well. But let's start with this example. Curl dash x gets 120 7.0.0.1 coordinate 100. Slash movies underscore search again, size equals zero and pretty and we'll kick off our body again. Again, we'll do an aggregation aggregations with an AGS clause. We're going to call this release and this will be a histogram on the year field, this time with an interval. Oops from my quote of ten years. Close out those brackets. One, two, three, four and see what comes back. Cool. It worked again. So we have our movie counts broken down by decade here and we could construct a very pretty histogram out of this if we wanted to. But the raw data is easy enough to interpret, right? So you can see that our coverage, the amount of films released increases pretty much every decade, right? So in the 19 tens, we only have seven movies in our database, 37 in the twenties. And by the time we get up to the 20 tens, we have nine 1950 less than the 2000s. But that's only because the decade 2010 wasn't over yet when this data was compiled. Up to that point, though, we just kept getting more and more. And, you know, you know, I could, you know, rant for a while about statistics and how to actually interpret. That doesn't necessarily mean more movies are being made. It just means that more people saw movies that they rated as part of this particular dataset. So always important to understand your data and where it's coming from before you draw conclusions from it. But the important thing here is that we've seen how to create histogram data from Elasticsearch and how to use it to do these aggregations. And really, we're getting into what Elasticsearch is sort of become. It's kind of weird. I mean, Elasticsearch started off as sort of this distributed LUCENE index really intended for powering full text search on big websites. But more and more, it's being used to actually be a really, really quick way to aggregate data and like analyze large data sets on a cluster. I mean, look at how fast they came back. I mean, bang just instantaneously. That's actually pretty amazing milliseconds we got back this these results. So that's really a surprising power of Elasticsearch, if you will, and something that you should use wherever you can.
