# [Exercise] Importing with a Script

If you have any programming experience or Python experience, this next challenge shouldn't be too hard for you. I wanted to basically work from the ratings example that we did in the previous lecture, where we imported all the ratings CSV data from the ML latest small data set into a new ratings index. Your challenge is to modify that script to instead import tag information from the tags CSV file instead. So just open up the CSV file using less or Cat or whatever you want. Understand its contents, what the data is in there and what format it's in. All you have to do is modify that script to parse the tags data out, and import that into a new tags index in Elasticsearch. So should be pretty straightforward, but depending on your level of experience with programming, you may or may not have a harder time with it. Now, if you did download the slides for this course, the solution is in the next slide for you to refer to. So no peeking. Otherwise, hit pause and go give it a try. And when we come back, I'll show you my solution. So let's walk through my solution to the problem. And you can see it's just a modified example of the ratings importer script that I had before in Python. All I've really changed is the read tags function here. It imports the tags csv file that parses that out into a user id, movie ID, a tag, and a timestamp instead of a rating and a timestamp. And it also sucks in the title from the movies dictionary that we loaded up in. The read movies function as the first thing we did. And so that works in a very similar way as before to the ratings example, where we're just denormalizing our data, including that movie title information along with the tags index. Then all we do is change the ratings index to the tags index in the code, and we're done. That's all there is to it. So let's go ahead and run this and make sure it actually works as advertised. So let's go ahead and download my solution to the problem with sudo wget media.com/index tags.py. Pay attention to capitalization. And there it is. Let's take a look inside with less index tags.py. And we're starting off with the same read movies function as before. That's just building up a dictionary of movie IDs to movie titles so we can denormalize our data. Read tags. Looks a whole lot like the previous function that we had. We're just going to be opening up a different file here. Tags dot CSV here to load up that tag data, load up that same dictionary for mapping movie IDs to movie titles with the read movies function, and create a csv dict reader to iterate through that tag csv file. And now for every line in that tags file, we go ahead and extract the user ID movie ID look up the title based on the movie ID, extract the tag and the timestamp and give that up. And finally, to import it we will create our Elasticsearch instance there. And one small difference here from the slides, I do have to specify the actual address of our server within the constructor for that Elasticsearch object. So we're passing in 127.0.0.1 9200 there to make sure it can connect successfully. We then delete any existing index that might be there for tags and then import the new tags index. We also added a request timeout there of 300 seconds, just to make sure that things don't time out on us, but otherwise it's all pretty much the same as before. So let's go ahead and kick that off and see what happens. Q to get out of that and we'll just say Python three index tags.py. And that same warning that the syntax for delete is going to change. So we'll go back and update that script when we need to later on. Let's make sure that it actually worked though. It says that it imported all of our tags pretty quickly there. So let's say curl dash x get 127.0.0.1. Colon 9200 slash tags is our new index slash underscore search to get the first stuff in that index question mark pretty. So we can see it nicely. And sure enough we have tags. So for the movie Gentleman's Agreement from 1947 it was tagged with Judaism and also anti-Semitism. Hmm. That doesn't sound like that. Stood the test of time very well. What else we got? Uh, the lost weekend was tagged with alcoholism. Mutiny on the bounty ships. Makes sense. Um, the movie Pi was tagged. Mathematics seems pretty obvious. But, yeah, there's just people out there who go out there and come up with these tags for movies, and that's what they do with their spare time. So go figure. But it is useful information. If you're trying to analyze this movie's data and understand what these movies are actually about. But the bigger point here is that we were able to apply what we learned and imported the tags data from the Movielens dataset into a new Elasticsearch index using the Elasticsearch module for Python, and just doing that through a client API. So pretty easy to do. Let's move on.
