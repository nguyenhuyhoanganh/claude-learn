# Logstash Input Plugins, Part 1: Heartbeat

In another lesson, we try out a few input plugins like the file input plugin, the TCP, UDP input plugins, etc. for collecting data using Logstash. In this lesson, we'll see a few more useful input plugins like the http, http, poller, dead letter queue, twitter input plugins and see how these input plugins work. So let's dive in and see how they work hands on. So let's start by cloning the repository that we need for this activity. Let's just type in sudo git clone https github.com/resources/logstash input dash plugins dot get. And we'll put that in etsy logstash conf dot d logstash input plugins. And that should clone the repository to the logstash logstash input plugins folder. All right. So let's start with the heartbeat input plugin. The heartbeat input plugin is one of the simple but most useful input plugins available for Logstash. This is one simple way by which we can check whether the Logstash is up and running without any issues. Put simply, we can check Logstash pulse using this plugin. This will send periodic messages to the target, Elasticsearch or any other destination. The intervals at which these messages are sent can also be defined by us. So let's take a quick look at a configuration for sending a status message every five seconds. The configuration for that would look like this. And you can see here there are two important settings in the heartbeat plugin namely the message and the interval. The interval value is in seconds and the heartbeat plugin will send the periodic messages in five second intervals. The message setting will emit the specified string as the health indicator string. By default it will emit okay. We can give any value there that you want though. So let's go ahead and run Logstash using this configuration which we've already cloned from that repository using the following command. USR share logstash bin logstash dash f. And we'll put it to Etsy logstash conf.d logstash input plugins slash heartbeat. Beat slash heartbeat dot conf. And we'll wait some time for Logstash to start running. Once it's done, we'll hit Ctrl C to stop it. And here it goes. All right. You can see those messages being emitted every five seconds, just like we specified. That's pretty cool. Let's go ahead and hit control C to stop this now that it's actually working. And to view a sample document generated by the heartbeat plugin we'll just type in the following request curl x get HTTP 127.0.0.1 9200. Heartbeat is the index we put it into. Underscore search pretty equals true. And we can omit the content type because we're using our special curl. And the body will just be. Size of one to restrict that to one result. And we should see a response like this. And there's one of those okay heartbeat responses there. So you can see the field name message with the specified string okay. In the document.
