# Installing Logstash

So before we can play with Long Stash, first we need to install and configure it. So let's get that out of the way. Fortunately, installing it is pretty easy. All we have to do is say sudo app, get update and then install log stash. And since we already set up the repository for Elasticsearch back when we installed Elasticsearch itself, that will just work. Now, keep in mind, if you're watching this lecture out of sequence, maybe you're referring to it later for installing log stash. You might want to take another look at that Elasticsearch setup video just to make sure that you get all the necessary repository data installed in Ubuntu so it actually knows where to find log stash. But we already do that, so this is all we have to do. We do. However, first need to make sure that we have a Java development kit installed. So while Elasticsearch itself has its own JDK bundled with it, log stash does not. For now, we have to stick with Java eight because they're still working out some bugs with newer versions of Java, but it's just that easy. These three lines should get you going. Once it's all installed, all we have to do is configure it. And this is what a log stash configuration file looks like. This is where all the interesting stuff happens. So as you recall, logs, Dash has a wide variety of input and output plug ins that you can use. And this is how we specify what input and output plugins we are going to use and how they are configured. So just to walk through the sample file here, we're starting off with an input block that defines where Logs Dash is looking for its input data and using a file plugin that's just going to monitor a local file on this system and feed that into log stash. So in this example, that path is pointing to home slash student slash access log, where access log is just an Apache access log that we're going to download shortly to use and play around with. We set start position at the beginning and that will force it to actually start processing that log from the very beginning of it. Otherwise it would just tail that log file and only input new stuff that it sees as it goes. Next, we have a filter block and this is where we actually configure log slash to know how to extract structure data out of this Apache access log file. Fortunately, log slash has a built in pattern called combined Apache log that can automatically extract data from Apache access logs or error logs. So to specify that, we're just going to use that as our grok filter. And finally, we need to specify the date timestamp format. This is going to allow log starts to extract the timestamp from the log lines themselves and use that as the event times that get published into Elasticsearch. Finally, we have the output section. This defines where we're going to send this data to after we've extracted structured information from it and send it to two different places at once. First, we'll send it to our local Elasticsearch server here, running on our local host on Port 9200. And at the same time will output it to the standard output, which will send it to our console using a codec called a ruby debug. And that just defines how that output is formatted to the console. So that allows us to see things get printed out to the console as it's being published in real time. So let's dive in and actually get this set up so far along and let's go ahead and get logged stash installed. First, let's download that access log data that we want to play around with. Make sure that you're in your home directory here and type in W get media dot sundog dash soft akam slash yes slash access underscore log. So that's downloading a real access log from one of the websites that I owned that we can mess around with. Now we need to install a JDK, a Java development kit like we talked about because the log stache is not on or one of its own like Elasticsearch does. To do that will just say sudo apt install open JDK. Dash eight dash jrd dash headless and let that go do its thing after typing in your password. Yes. Continue. All right. I didn't take too long. Now let's go ahead and install the log stash itself. Sudo apt get update ampersand. Ampersand sudo app dash get install log stache. And it should use the repository information that we set up earlier in the course when we first set up Elasticsearch and go grab log stash, which it seems to be doing. All right. So far, this hasn't been too hard. Now comes the hard part, though, actually writing the configuration file for log stash to do what we want. So. So let's say pseudo VII or nano, if you prefer, slash EDC, slash a log stache slash contact B slash log stash. Dot conf. All right, so let's just start by telling it the input path here. So we'll start with the input block. Take it one piece at a time here, guys. Input curly bracket tab. We're going to use the file plug in to just monitor a file on our local file system here and feed that directly into Elasticsearch later on. So again, we're kind of doing this the old fashioned way where we're running log stats directly on the hosts that's generating the log instead of using beats at that layer. But we at least show you how to use log SAS here. So we need to specify the path to the file that we're going to watch for. So we'll say path equals sign greater than sign there. That's the syntax for mapping dictionary entries here and for us that will be slash home slash student slash access log. That's where we downloaded that log to. And we are going to say start position, start, underscore position. At the beginning. That way we'll start processing that data from the beginning of the file no matter what, instead of tailing it for new data, close that off and that's our input section. Next, we need to apply a filter to this data to impart some structure to it and we'll set up a grok filter. And that will have a match entry where we curly brackets quote message or mapping the message of this request to quote present sign curly brackets combined. Apache log. Make sure you spell that correctly. And curly bracket and quotes. And another curly bracket to close off that line. Again, this is using the built in log stash, Apache log filter there to actually pull out the fields of each log line there. Close that off. And we also need to specify our date format that will match square bracket, because you can actually have a list of these things, the time stamp field and interpret that comma, quote as day month. Three M's there. Make sure you get that right because it's actually like a three letter month abbreviation there that's used in the logs slash year. Colon our colon minute. Colon second in Zulu times what that all means and quotes and square bracket. And make sure you get all the right number of letters there in the right case as well. It's all very sensitive to getting that exactly right. We'll close off the date block and close off the filter block and move on to our output block where we see where the state is going to go. Of course, we're going to send it to Elasticsearch. And we need to specify which hosts to send it to. We can actually send it to more than one, but we only have one. And for us, that's going to be localhost on Port 9200. Again, we're using square brackets there because you could have a list of them. At the same time, we're going to send it to standard output. So we can actually see progress as we go using the codec called Ruby to bug. Close that out. And that should be good. You might want to double check everything because the one little typo can screw things up. I actually had to rerecord this whole lecture because I forgot to put that percent sign in there, for example. So be super careful, guys. And if you do run into trouble, we run this. This is a place to look for errors, but it looks good to me. So I'm going to say colon WQ to write that out. All right. So at this point, we have a log stash installed and configured. Next step is to just run it. So we'll do that next.
