# [Exercise] Importing Data with Spark

Okay. So your exercise to close off this section is to write your own spa code that will actually import movie ratings data from the ML latest small data set into Elasticsearch and a new ratings index that you've created. Now, this might sound intimidating if you're new to spark or new to Scala, but don't worry, you can, just for the most part, just modify the code that we wrote in the previous lecture that imported our fake social network data. This will also be in CSV format with the same number of fields and everything, so it's not as scary as it might sound. Just for reference, here's another look at that script that we wrote in the previous lecture where we actually imported the fake friends dot csv file first into a lines already object. So we didn't really talk about the concept of already. These are resilient, resilient distributed data stores and when you load something in with text file there, it's actually putting it into what's called an RTD. That's what lines is. It's sort of a more lower level structure than a data frame, if you will. So it's pretty straightforward. Once we converted that to a data frame, we just use the Save s function there to actually write it out to Elasticsearch. All we need to do is change that text file input to be rating start CSV from the ML Dash latest small data set. So you might need a little bit more information about the path there. And then we're going to change the person class to a rating class that passes every line of the rating CSV file out to its appropriate types and field names and remember the contents of the ratings. That CSV file consists of many lines, each one representing a movie rating for a given user ID, movie, ID rating and timestamp. And those are all integers except for the rating, which is a floating point value in this particular data set. Now, there are a couple of things that you'll need to know about if you're new to spark. One bit of complexity is that the ratings dot CSV file has a hetero on it that contains the name of all the columns. So you can either just delete that header as part of this exercise in the source data. So you have to deal with it at all or you can deal with that specifically in this code. Here's a little snippet of code of how I might go about doing that. So after reading in the Rye already of every line of data, what you can do is assign a header variable that just contains the first row of that already and then filter that row out into a new data already. So what these two lines are doing is saying take the first line out of the data set of lines and then filter out the lines to exclude that first line of information and create a new RTT called data instead that will work with it does not have that hetero in it. So if you want to do it the quote unquote right way, that's how you would go about doing this. All right. So go ahead and give it a shot, if you will. Hit pause and give it a crack. And when we come back to this video, when you hit resume, I'll show you my solution and we'll dive in and run it. All right. So here's how I went about doing this particular problem. As you can see, it's pretty similar to the fake friends. Not serious, for example. All I did was change the path to the input for the lines. Already inserted those two extra lines that actually strip out that first hetero. And from there, it's just a matter of having a rating class instead of a person class that passes out. Each line of rating starts the ASV in the way that I want. After that I'm just using ratings dot save to yes. So all I did was change the name of that dataframe object. And I'm writing that out to this new spark index called Ratings. It's just that easy. So let's watch me actually enter this in and give it a shot. So first, let's make sure we still have that milliliter small data set installed. We did use this in earlier lecture, so if you've been following along, you should have it already. Let's see MLA Dash latest small and we're going to be playing with the rating start CSV file here. If you are taking this course out of sequence, you can head on over to group lands dawg and navigate into the data sets to download that package if you need it. All right. Let's take a quick peek at ratings dot CC. And just to refresh ourselves, this consists of a user ID, which is an integer, a movie ID, also an integer, a rating, which is a floating point number. Actually, you can see these have 4.0, 5.0. Maybe there's a world where there's like half star ratings, although that doesn't really exist in the state, I don't think. And finally, a timestamp, which is also an integer, that's basically epoch. Second, so I won't get into what that really means if you don't know queue to get out of less here and let's KD back up to our home directory and let's go ahead and kick off our spark shell. I'm just going to cheat and use the up arrow to find that command. There it is again. So again, you will need to substitute the specific version of Spark that you installed in the specifics of the Spark Elasticsearch integration package that is compatible with that version. We went through that a couple of lectures ago, if you need to refresh yourself. But for me, this is the path I went on to spark 3.2.1 and I depend on the August Elasticsearch Elasticsearch Das Spark Cadastre three oh package there for spark three underscore 2.12 signifying Scala version 2.12 and finally 8.2.1 is the version number of the Elasticsearch SPARC package itself. Let's kick that off and we can start writing some code. That's going to go off and start my SPARC environment and also load up that integration package. And what we're going to do here is pretty similar to what we did in the previous activity. We're just changing things around a little bit to use different data and put it into a different index. So we'll start out the same way by importing that integration package with the import Elasticsearch dot sparked SQL dot underscore. We will define a case class, and this time, instead of a person, we're going to call it a rating. And that rating consists of a user ID, which is an integer. A movie I.D., which is also an integer. A rating, which is a floating point value as we saw, and a timestamp which is an integer. All right, so now we know what a reading looks like. Let's write our mapper function to actually convert rows from that text file into rating objects. Def mapper. It will take in a variable called line, which is of type string and it will return a rating object and we will define this function thusly. We will have a immutable variable, a vowel called fields. Which will be a list of strings that comes from line dot split. On the comma separator. And then we will create our rating object called rating. By constructing a new rating. I consists of fields of zero. Converted to an integer. Fields one converted to an integer. Builds to. Converted to a float. And Fields three converted to an integer. Double check for typos and whatnot. Okay. Looks good. And finally, we will return the rating that we just created and close off our function. And we now have a mapper function. All right. As before, we will import sparked implicit just because that's what we need to do when we're dealing with conversions of data like this. And we will say vowel lines equals sparked spark context dot text file create the spark context around the text file. EML dash latest dash small slash reading start CSP. Cool. And now we have that RTD object that we talked about called Lines. So first of all, we need to strip off the header from that RTD object that hetero needs to go away. So that's kind of one of the quirks of this activity. To do that, it's easy to say val header equals lines dot first. So it's going to extract that first row of header information into a new RTD called Header that just has that one line in it. And now we will filter that out from the larger RTD. So we'll say Val data, which will be all the data minus that hetero equals lines, which is the complete dataset dot filter. And we're going to pass in a little lambda function here. We say row sequel to row, not equal to header. So basically that applies a filter function that says as long as the incoming row of data does not match that header line, let it through. So that will effectively filter out that first line of data. All right, so now we can save all ratings equals data, dot and map using the mapper function that we defined above, and we'll convert that to a data frame. And finally, we have our data frame full of ratings and we can save that off into Elasticsearch with ratings dot save to E. S and we'll give this index a name of ratings. And again, this is just going to take that data frame that we defined using that case class to define what the field names and types are that we import into Elasticsearch. Let's kick that off. And this will take a little bit more time than before because it is a larger dataset. But it's done. So let's get out of this with Control D and see if it's actually in there with col dash x gets issued a p local host coordinate hundred slash ratings is the index we just created slash underscore search pretty. And there we have it. Very cool. So looks like a user 81 rated movie had 157, whatever that is, 5.0 stars. And user 81 also rated movie 151 five stars as well. But yeah, so we used Apache Spark to pass a large data file structure that data and feed it into Elasticsearch as a new index. And again, the power of this is that you could do this in parallel and handle very massive datasets efficiently this way, if you had a whole cluster and if you did get through this yourself, congratulations. And I hope you find this as a useful tool for managing big data with Elasticsearch.
