# Logstash and S3

Another place where your big data might live is S3. That's an AWS service called the Simple Storage Service, a popular place for storing big datasets. The nice thing about it is that you can just rent the space that you need from Amazon, and it will automatically distribute that data across their entire cloud and automatically back it up for you. So it's a very cost effective and easy way to store your data. And if that is in fact where your data lives that you want to import into Elasticsearch or someplace else using Logstash, well, that's actually really easy to do. All you need to do is in your Logstash configuration file, set up something like this. You just need to specify the bucket name where your data is stored. And remember, in AWS, S3 data is stored into some globally unique bucket name that you just need to provide along with your access credentials for actually accessing that bucket. Obviously, I'm not going to give you my access credentials, but if you have your own, you can use them instead. Now, this is a case where I'm not actually going to have you follow along. If you do have an AWS account of your own, you could set this up for yourself and actually do it for real. But let's just watch as I go ahead and do this within my own account, and you'll see the steps we're actually using to import data from an entire bucket through S3. Creating an account in Amazon to actually allow you to connect to it from Logstash, and then turning around and putting that into Elasticsearch. So let's dive in and I'll show you how that's done. Okay. So again I don't expect you to follow along here. Just watch because I'm going to be using my own AWS account here. If you have one of your own, then you can try and go back and apply this same technique with your own account if you want to, but you're not going to be able to follow along with the exact same credentials and stuff that I'm using here. Anyway, the first thing we need to do if we're going to import data from S3 into Logstash and then into Elasticsearch, is to first create some data in Amazon S3 to import. Right. So, so I'm all logged into my AWS console here. And let's start by taking a look at S3 just from the homepage here. And I've already set up an S3 bucket for this activity and I've called it Sundog es. Let's take a quick peek at that now. Note a couple of things here. I've already put an access log dot text file inside of this bucket. I've uploaded that in there already. And if we take a quick look at that you can confirm that it is in fact an HTTP Apache access log. And furthermore, I have set this up to be publicly accessible. So if we go to the permissions tab here, we can see that I've granted read only public access to everyone. And by the way Amazon S3 makes it very, very hard to set this up while you're setting up a bucket. So you do have to jump through some hoops to actually make that accessible to the outside world, such as our Elasticsearch instance. But anyway, that's sitting there in S3, just waiting for us to do something with it and suck it out and pump it into Elasticsearch. But before I can do that, I also need a way for Logstash to actually connect to S3. And even though this is publicly accessible, I still need to give it some credentials to actually log into AWS itself. And to do that I use something called IAM. So let's switch on over to the IAM console here or dashboard. And from here you can see I've already created a user in IAM called Logstash. And if we take a look at what's going on here, I've already attached the Amazon S3 read only access policy to this IAM user. So I've restricted what this account can do. It can read data from S3 and that's about it. And furthermore I've already generated security credentials for this user as well. So when I created this user, it gave me the opportunity to download the public and secret keys to log in with this user remotely and actually authenticate with Amazon S3. You can see that access key here. And once you actually create it, there's no way to get the secret key back again for security reasons. But I already squirrel that away, so I'll be using that later on when we configure Logstash to read from S3. So let's go back to our console here for our Elasticsearch server. And we're going to edit our configuration for Logstash thusly sudo nano slash Etsy logstash. Slash slash logstash.com. So I'm going to modify this configuration file to not read from a local file but instead from an S3 bucket. So let's get rid of all this file stuff here. Ctrl K will get rid of those lines. And instead I will say our input is using the S3 plugin. We need to give it a bucket name, which in my case is Sundog es. And if you're doing this yourself, you will need to use your own unique bucket name instead. We now need to give it our access credentials for getting in there. So I'm going to have an access key ID. And I already copied that off earlier. I'll just paste that in. And I'm also going to supply my secret access key. Now there are more secure ways of doing this. And actually putting your raw keys in a configuration file in production that's usually frowned upon. But since this isn't actually confidential information, I'm not going to worry about security right now. All right, close off that S3 block and that looks good to me. We can keep these filters because there's still Apache access log. So the grok filter for the combined Apache log is still appropriate. And that date format is still appropriate as well. However, on the Elasticsearch output I'm going to specify an index. So not only are we just going to go into the localhost, but I'm going to specify an index name. So this doesn't get mixed up with the other access logs that I'm importing. So we'll just say index S3 logs and everything else should be good to go. We can also keep the standard output there as well with the Ruby debug codec. That's totally fine. So this all looks good. Let's write that out and exit it. All right. So now we can just kick off Logstash with that configuration file and see if it works. So first we'll CD into usr share logstash and we'll run it thusly bin logstash with the file path to our configuration file which was at c logstash conf dot logstash dot conf. And off it goes. Does get a little bit of time to spin up here. All right. It's starting to chug away. We're just looking for error messages here, which would indicate a typo in our configuration file. Hopefully there aren't any. Ah. It's alive. Let's see what it does. Looks like it's parsing that S3 information right now. And we should be going pretty soon. Yes. Data is flowing. Victory is ours. We'll just wait for that to finish. And a few minutes later, everything seems to have finished. So I'm just going to hit control C to end Logstash here. Break out of it. All right, so let's convince ourselves that it actually did import that data from S3 into Elasticsearch. Let's do a query thusly. Curl x get quote 127 .0.0.1. Colon 9200 S3. Logs, as you'll recall, is the index name that we told it to create. Underscore search pretty. All right. So we have successfully imported data from S3 from S3 bucket just containing raw Apache logs and used Logstash to transform that into structured data stored in our Elasticsearch server. So cool. There you have it. That's how easy it is to import data from an S3 bucket into Elasticsearch. And I should mention that AWS offers some other mechanisms for doing that as well that don't involve Logstash necessarily. But hey, this is a course about Elasticsearch. So we're going to talk about Logstash here. It's still an easy way to do it. And a good tool to have in your tool chest if you have data sitting in S3, data lake, if you will. And with that, let's move on to integrating with other systems next.
