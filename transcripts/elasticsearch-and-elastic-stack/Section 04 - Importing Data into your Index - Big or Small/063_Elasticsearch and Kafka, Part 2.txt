# Elasticsearch and Kafka, Part 2

All right. We successfully created our new topic. And that is basically just a stream that we can publish stuff into. So now we just need to set up Logstash actually listening to that stream let's say sudo nano. Let's see logstash conf.d logstash conf. All right. So now instead of S3 we're going to wipe that all out. Control k control k control k control k. And we will now create instead a Kafka input configuration. We need to tell it the bootstrap server. Or servers which can be a list of servers really. But for us it's just running on localhost. Port 9092 is the default port, and we need to tell it what topics to listen to. We only have one that we set up that's called Kafka logs in square brackets. And close that off looks good. And we can change the output index name to something more distinctive. Instead of S3 logs, we'll call it Kafka logs. Everything else should be okay. Control O. Control x. All right, so now let's kick off logstash. CD and usr share logstash first. And now we will say bin logstash dash f and the path to our configuration file. Thusly. All right. Let that spin up. So now we have a Kafka server sitting on that other session there that's running in the background. And it is sitting there with a Kafka Logs topic that we configured. And Kafka Logs is just sitting there waiting to receive data in Kafka right now. At the same time, Logstash is spinning up and listening to that topic as well. So as we publish data into Kafka on that Kafka logs topic. It should be getting pushed to the Logstash subscriber there, which will then in turn take that and turn around and shove it into our Elasticsearch server. So we're just going to wait for Logstash to spin up here first. All right. It's starting to chug away there. So now we're going to open up yet another session. Log in again. All right. And now what we're going to do from this session is actually publish data into Kafka. And to do that we have a handy Kafka console producer script that we can use to actually just pipe data into it. So again, we'll CD into our Kafka directory. And now we're going to say sudo bin slash Kafka dash console dash producer, shell dash dash broker dash list localhost colon 9092. Topic. The topic name is Kafka Logs, and we're going to feed in data from our access log that's still sitting in our home directory. All right. This should be fun. So we got a bunch of stuff going on here. So in this window we're going to be feeding data into Kafka publishing data into our Kafka topic. In this window we have the Kafka server itself that will be receiving that data and then turning around and publishing it on that topic. And over here we should see Logstash picking up that data from Kafka topic and turning around and putting it into Elasticsearch. So let's go back to this window here and kick that off. And hopefully things will start happening after I authenticate. All right. So we can see that we're pushing data into Kafka there from the access logs. So if everything works properly we should start to see the other windows start to scroll by as well with activity. All right. Something's happening on Logstash here. There we go. Cool. All right, it's chugging away. So, hey. It's working. We're actually publishing data into a Kafka topic. Kafka is then sitting there just publishing that information on the Kafka logs topic that we created. And now Logstash is sitting there subscribing to that topic, sucking data out of it and turning around and indexing it into Elasticsearch. So pretty cool. Let's just wait for that to finish and I'll convince myself that it actually worked. All right. Looks like it finished there. So I'm going to go ahead and control-c out of the Logstash window here. Cool. All right. Let's see if it actually indexed. It kind of a convoluted way of indexing that access log file that was just sitting on our hard drive there anyway. But when you're dealing with big data, sometimes you need to use more complex systems in order to make sure that things are resilient. So let's go ahead and see if that new Kafka Logs index actually exists. We'll say curl x get 127.0.0.1 9200. Slash Kafka logs is the new index that we tried to create their underscore search. Pretty. Hey it worked. So we did it. We actually imported that log file into Kafka, which published it into Logstash, which in turn put it into Elasticsearch. So if you do need to integrate Elasticsearch with Kafka, Logstash provides the glue. You need to do that as well. All right. And we also learned a little bit about setting up Kafka. Theres a little bit of a bonus. All right so let's clean up our mess here, shall we. We can exit out of all these windows here. And we're also done with that Kafka script as well. And here is the Kafka server itself. We're going to control C out of that to shut that down. Knock on Kafka anymore in this course. All right. And let's CD back to our home directory before we forget. All right. We got a nice.
