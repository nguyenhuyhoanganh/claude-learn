# Importing with Client Libraries

Now, while hand constructing those JSON bulk import lines is an option, you generally don't have to do it quite that way. It is easier to use the client APIs that exist for most languages for Elasticsearch. So there are free Elasticsearch client libraries available for pretty much any language you can dream up. For example, Java has an official client maintained by Elastico. Python has an Elasticsearch package that we'll look at. And there's also an Elasticsearch Ruby package. Several different packages for Scala and a Perl module as well for Elasticsearch. So you don't have to deal with constructing your own JSON if you don't want to. Instead of just constructing all that JSON code, we can actually do the same thing that we did in the previous script with just a few lines of code here. Basically, you can see how in one line, once you have an Elasticsearch instance here, we can just call that directly. For example ES dot indices dot delete lets you delete a given index all in one line of code. There's also the little line here that does a parallel bulk import using a given helper function that you provide into a given index. Again, with one line of code, that's all there is to it, so you don't have to muck around with JSON at all when you're using these APIs, which can be kind of nice. Up to this point in the course, we've been dealing with JSON requests and lower level interfaces to Elasticsearch, and that foundation is definitely important to have. But in practice you don't always need it. So let's go ahead and dive in and see what that looks like. Just for reference again, here's the contents of the script that we're about to run. It's still a lot of lines of code, but it's a lot cleaner, right? You know, there's no actual construction of JSON going on here, so it's a lot easier to understand what's happening in this script compared to the one that we just ran. If you look at this in more detail, what we're doing here is we have a function that reads movies into a given dictionary. We have a read ratings function as well. And down at the bottom is where all the magic happens. We create the Elasticsearch instance, delete any existing ratings table that might exist, and then we load up our ratings data into the ratings index using our helper function of read ratings. So you can see that it actually goes through the dictionary reader of the ratings dot csv file, and yields up one dictionary line at a time that maps that to a user ID, movie ID, title rating, and timestamp, and it ties it together with the title lookup. Another concept that this script illustrates is the denormalization of data that we talked about back in data modeling. So instead of actually storing just the movie ID, we're actually looking at the title of that movie and making that part of the ratings index as well. So we don't have to do two different lookups whenever we want to deal with ratings. Basically, we construct an Elasticsearch interface. We delete any existing ratings index that might exist, and then we call this read ratings helper function, which as a first step, calls read movies to construct a dictionary of movie IDs to movie names. And then for every rating, we'll output a new Elasticsearch document that gets an index containing a user ID, movie ID, title, movie title, all being looked up from that movie ID rating, And a timestamp that gets inserted one document at a time. But what's missing here is all that JSON nonsense. So it's a lot easier to read and a lot easier to write. Let's dive in and see how it works. So before we can use the Elasticsearch library in Python, first we need to install it. And before we can install it, we need to install the pip package for Python that lets us install modules for Python. So let's get that out of the way first with sudo apt install python3 pip. And authenticate if you must and down it comes. Yes I want to continue. And this is a pretty beefy package. So it will take a little bit of time. We'll come back when that's done. So now let's install the Elasticsearch package. Now that we have pip using sudo pip three install Elasticsearch and we want to say equals equals nine to make sure that we're getting the version nine package. And then we have a very scary parameter here. Dash. Dash brake dash system. Dash packages. Sounds horrible, but, um. It's okay. What's going on here is that, generally speaking, ubuntu in the current versions of ubuntu really don't like you using Python outside of a specific environment that you've created. And since we're just playing around here and we have a virtual machine here that we're just using for educational purposes, I'm not going to make you worry about creating a virtual environment for Python and switching to it all the time. So instead we're just going to stick with the Python package here. And we're just going to go ahead and use brake system packages saying yes, I know that that could cause bad things to happen if I were in a more complex setting, but I'm not. So it's okay. Let's go ahead and kick that off and we can see that Elasticsearch 9.0.0 got installed. Might be a newer version for you, but it should be nine point something. All right, now let's go ahead and download the script that we need that will take advantage of that package with sudo wget.1.com/is9/index Ratings dot pi. And let's take a look at what's in there with less index ratings dot pi. And we'll just review what's going on here real quick. So we have a read movies function here. All that's doing is opening up the movies dot csv file from the ML latest small data set that we downloaded in the previous lecture, opening that up with UTF eight encoding, and reading that in using the CSV package from Python. And we're going to go through every single row there and extract the movie ID and the movie title, just so we have a way to look up titles for a given movie IDs. We're going to denormalize this data basically as we're inserting it into Elasticsearch. So as we're reading in the raw ratings data, instead of just storing movie IDs, we'll also store the movie title so we can actually make sense of what these things mean when we retrieve the results later on. Read ratings a little more interesting. We're going to go through the ratings CSV file, and we're also going to load up those lookups of movie IDs and movie titles into a dictionary that we can refer to as we load it in. We will then open up that CSV file with a dict reader, allowing us to access it a little bit more easily. And for every line in that CSV file, we'll start off with an empty dictionary and populate that dictionary with the user ID movie ID title using the title lookup function and the rating and timestamp, and we'll just yield up that resulting rating structure for every line. And now we get to actually using the Elasticsearch package itself to import the data. And you can see that's really easy. All we do is start by creating an Elasticsearch instance here with Elasticsearch Elasticsearch giving it the address of where our server is. In this case just the localhost running on port 9200. We will start by deleting any existing index that might already be there, and then you'll see that ES instance that we created being passed into the parallel bulk helper function to pass in all those rows coming in from the read ratings function, feeding in all those individual ratings, all 100,000 of them into our Elasticsearch client there that we created there. And we will specify the ratings index to insert it into. And we also need to specify a request timeout of 300 seconds to make sure that we give it enough time to actually import that much data. Then we just refresh the index to make sure it's all finalized and we're done. So yeah, basically those four lines of code are all it takes to actually import that data into Elasticsearch. So a lot easier using these scripts and these client APIs for different languages. Hit Q to get out of that. And let's actually kick it off with Python three index ratings.py. And we'll let that do its thing. A little deprecation warning there that we can ignore for now. Anyway. Looks like the syntax for deleting an index is going to change soon, so we'll have to do that in our next course update. But yeah it looks like it finished. That was actually pretty quick considering we imported 100,000 ratings there. Let's take a quick look at what's in that index just to make sure it really went in with curl x, get 127.0.0.1 9200. Slash ratings was our index that we just created underscore search to get everything uh, question mark pretty. And we have ratings. Very cool. So yeah. For example, user ID 463 rated alien three four stars. Seems about right to me. And you can see here again that it is denormalized. We were able to use that lookup dictionary of movie IDs to movie titles, to actually put the movie title in each result here without having to go into a separate lookup for it. And if we scroll up a little bit, a couple more examples. Uh, there's some, uh, raunchy movies there. Independence day. Hey, this guy only gave it 3.5 stars kind of movie. Either love it or hate it, I think. And, uh, what's, uh, Schindler's List? That's a classic. Only four stars. Well, actually, I have to agree. I'm not really into depressing movies like that, but, uh, this isn't, of course, about my taste in movies. The point here is that we successfully imported 100,000 ratings just using the Elasticsearch client API for Python, and it was pretty easy to do. So there you have it. Another way of importing data into Elasticsearch without getting down and dirty into the nitty gritty of JSON. And there are some more ways to do it too. So let's keep on moving on with more ways to import data.
