# Logstash and MySQL, Part 2

All right. So before we can actually communicate with MySQL locally, we need the connector for Java. So we can talk to it through JDBC. So we're going to head over to MySQL. That should get you to this page right here. And all we're going to do is steal the download link for that connector package. So to get there let's select for the operating system platform independent. And from there we're going to click on the zip archive. Note by the way that we're getting version 9.3.0 here might be different for you by the time you watch this. Just remember what that version is because you're going to need it later on. For me it's 9.3.0. For you, it's probably something else. Let's click on download. That doesn't really download it though. It brings us to this page here that gets you there. They want your login information but you don't need one. You can just use the no thanks. Just start my download link instead to get there directly without providing any personal information. Now we're not actually going to download this to our PC. We want to download this onto our actual Elasticsearch server. So I'm just going to copy that link off. So on windows I'm going to right click on that link and just copy the link. And now back in putty I have that handy. So I can say now wget I'm in my home directory by the way, and I'm going to paste that in with my right button and let's see if that comes down. All right. So now we have that connector package. We need to unzip it next. So let's say unzip MySQL connector whatever it is. Again for me it's 9.3.0. For you it will be something else. Remember what that number is. All right. So let's go ahead and look inside that folder that we just made. And inside here you'll see there's a jar package. That is the thing that we want okay. So remember where that is and what it's named. You might need to keep this handy in a separate window somewhere. Because we're going to need this in our configuration that we're about to set up for MySQL. All right. So with that in mind let's go ahead and create that configuration file for MySQL. For that we're going to say sudo nano slash Etsy logstash conf dot D slash MySQL conf. And I've already done this. But for you go ahead and type this in. So you might want to hit pause here. Now some of this is going off screen. So the one thing I want to call attention to here is the JDBC driver library setting. That is the path to the thing that we just extracted to the jar file. Okay. So it goes off the side of the screen here I'm going to scroll over so you can see that that is the path to home student. And then the directory that we just expanded again for you that might be a different number. So remember what that was. And then the actual path to the jar file itself. And the naming convention of that changes as well. So do make sure that that matches what you actually downloaded. You need the path to that jar file within the SQL connector. Everything else is pretty straightforward. So again under JDBC here we're saying that we're having an input. And we have the actual string for what database we want to connect to. This is saying that we want to talk to MySQL on the local host on port 3306. Specifically to the Movielens database. We have our student, user and whatever password you intend to set up for that user later on. That might be different for you. Again, the path to the Jar file for the JDBC connector that we just downloaded, the driver class name will be MySQL JDBC driver capital D. And the statement that we're going to execute every time is select star from movies to get back everything that's in the movies table. And then for the output section nothing too unusual here. We're saying that our output is going to be in JSON lines format. And we're going to talk to Elasticsearch on port 9200 on the localhost. And we'll stuff everything into a new index called Movielens SQL. So go ahead and hit pause and type that all in here and then resume when you're ready to move on. When you're done control O to write it out enter and then control X to get out. So next we need to set up that MySQL user that we talked about for the student user account there. For that we can just say sudo mysql Dash u root p. And we're in let's say create user student. At localhost identified by password. And you would obviously use a better password than password. In the real world we will grant all privileges. On star dot star two student at localhost and flush privileges to make that take effect. Now we can quit MySQL and this should just work. Let's CD slash user slash share slash logstash. And from here we can actually run logstash itself bin slash logstash dash f. And the path to that configuration file that we made earlier at c logstash conf dot mysql conf. And now we just sit back and hope that it works. All right. It's kicked off and we're just making sure we don't see any error messages here. These warnings and infos are okay so far. Right now it's trying to make sense of our configuration file. So if there were any typos we'd probably see it pretty shortly. All right still going pretty soon it's going to try to connect to our MySQL database. So hopefully those credentials are working as well. Hey it looks like it's working. Very cool. And you can see that that new JSON lines output format is a little bit more concise than the Ruby debug format we used before. And yeah, it got through it all. So it actually seems to have worked. Let's go ahead and, uh, do a little search on Elasticsearch and see if it's really in there, shall we. So we can say curl x get quote localhost colon 9200 slash movielens. Dash SQL was the new index that we created. Underscore search and we'll do a query line search again question Q equals title colon star and pretty. So that should give us any movie titles with star in the name like we showed before. And sure enough it seems to have worked very cool. So we actually successfully exported data from a MySQL database and into an Elasticsearch index using Logstash. And, uh, you know, the hardest part of all that was just configuring MySQL. Actually, the Logstash piece was fairly straightforward. So pretty cool. So if you do have a bunch of data sitting in a relational database of some sort, that is generally how you would go about sucking it out of the database and pushing it into a distributed Elasticsearch cluster instead. So there you have it.
