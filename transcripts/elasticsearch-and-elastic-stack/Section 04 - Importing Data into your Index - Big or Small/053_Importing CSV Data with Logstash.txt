# Importing CSV Data with Logstash

The CSV file format is widely used across the business and engineering world as a common file for data exchange. The basic concepts of it are fairly simple, but unlike JSON, which is more standardized, you're likely to encounter various flavors of CSV data. This lesson will prepare you to understand how to import and parse CSV using Logstash before being indexed into Elasticsearch. CSV, short for comma separated values, is a popular file format that stores tabular data and is used by spreadsheet software like Microsoft Excel and Google Sheets. Here's a quick rundown of the general rules for the CSV format. The delimiter sets the boundaries between lists of items in the file. The name CSV stands for comma separated values, because these files mostly use commas as the delimiter, CSV organizes data into the same number of items per row, so that every row has the same number of items, even if some of those are empty. This enables a program to reliably parse each item or quickly search across an entire column instead of row by row. Each row in the file is separated by a newline character. Column headers which describe each column's data will typically appear as the first row in the CSV file, but it's an optional row. All other rows that follow include the actual data. Some parts, like the header rows, are optional, and while many csvs use commas to separate the items contained in the file. In reality, this separator can be any character, so the same parsing configuration may not work for all CSV files. Let's take a look at some sample CSV data. The first row here is the header row and defines the names of the fields. The following rows display the actual values of those header row fields. A program that processes the CSV data will associate the first field of name with the first value in every subsequent row after the header. In other words, John, Basil, and Bella would all be associated with the name field. This process of interpreting data and extracting values and converting them in the process to another format is usually called parsing. So let's dive in to a hands on exercise. Let's start by creating a directory for saving the CSV file we'll work on with a mkdir p home student csv data. And next we can just download the sample csv data from GitHub and add it to the folder we just created with the following command. CD, home student csv data and curly bracket curl dash. Big o https colon slash slash raw dot GitHub user content.com/resources/elk course dash samples, slash master slash CSV dash schema dash. Short dash, numerical dot CSV space. Semicolon. Space CD space. Hyphen. Semicolon. Space. Curly bracket. Double check that for any typos. Looks okay to me. Let's go ahead and get that. Looks like it came down okay. So after running this command, our sample CSV file should have been downloaded to the folder home student CSV data. So this CSV file contains an example of payment information log data containing various customers along with their IP addresses, gender, country, and so on. Now let's see what's in that CSV file by typing in the command cat home student CSV data CSV schema short numerical CSV. This will display the contents of the file. Looks reasonable. Let's pull it up in the editor and just make sure everything looks okay with nano home student CSV data. CSV schema short numerical CSV, and I'm going to hit the down button to go all the way to the end and make sure there aren't any empty lines. It's important that there's no empty lines in the CSV file, because otherwise Logstash will try to process them. If there are empty rows, Logstash will try to parse them and the rows would still be indexed, but without associated fields. So in effect the empty lines would create empty documents in Elasticsearch, causing us to unnecessarily increase the document count. But in our case, this file looks clean. I don't see any extra lines at the end here. I'm slamming the down arrow here and it's not going any further. So I'm just going to say Ctrl X to exit and call it good. So in our next step let's look at how a CSV file can be imported into Elasticsearch. By using Logstash. We'll be using a configuration file to instruct Logstash on how to execute the import operation. So let's download the configuration file to the logstash conf folder by typing in the following command sudo wget dash uppercase p etsi logstash conf. Https colon. Slash slash. Dot GitHub user content.com/coralogix resources slash elk course samples master csv read conf. All right all right. So let's open up that configuration file by typing in sudo cat etc. logstash. Cough dot csv read conf. And we can take a look at the contents of the file here. So let's break down how this configuration file works section by section. Let's start with the input section. So up here at the top here the file subsection indicates that a file will be used for the input. And within we use these options. Path indicates the full path to the input file. In our case the CSV file we prepared earlier start position is where we instruct Logstash to read the file from the beginning. This setting only applies to files that are read for the first time. By default, the program would prefer to read from the end, since it expects to process a file that would have data added to it periodically. This way, it could import only the new data whenever it's added to the file at the end of the CSV file. If the file has been seen before, the next parameter will be used to decide what to do. Since Dbpath points to a database file that keeps track of the last line parsed in an input file, in this scenario, the CSV file. The next time the input file would be parsed, the process would continue from the position recorded in the sense db file. Here we set this to dev null so that Logstash won't be able to record the last line it previously read for that particular file. This way it will process the entire file each time from the beginning, rather than continue from where it left off. Now let's break down the filter section of the configuration file. We're going to use CSV as a subsection to specify that this is the type of file we intend to parse. Within that, we declare the following options. Separator where we pass the comma character. This way Logstash will know the commas are used as a delimiter to separate the fields. Skip header is important here, telling the parser to ignore the first line, which only indicates column names and not the actual data fields we intend to import. Columns is where we specify the names of the columns that will be imported. In this case, we are telling our parser that the first field in each line is an ID, the second is a timestamp, and so on until the last one, which is an age. So now let's explore the final section in our configuration file. The output section, Elasticsearch subsection instructs our program that we intend to send the data to Elasticsearch. The hosts option specifies the host and port where our Elasticsearch accepts incoming connections. The index option defines the name of the Elasticsearch index, where imported data will be stored, and standard out will make the import action display its status, output and log information in the terminal. So now that we've seen the different sections of the configuration file, let's run this configuration file with the options we just defined. USR share logstash bin logstash dash f etsy logstash conf dot. CSV read conf. That will take a few seconds to complete. All right, so now that the process has completed, your result should look similar to this. This output shows that our entries have been successfully imported. Now every time we make a change to a configuration and run it, we need to exit Logstash. So let's go ahead and press control C to exit logstash. We can check that the data was inserted with this curl request here curl x get localhost 9200 demo CSV slash underscore search question mark pretty equals true. And your output should look something like this. There's a lot of useful information in this output. For example it shows us the file that was used for importing the data, the column names, the field values and so on. So so far we've only played around with the basics of importing CSV files, but we can already see that it's pretty straightforward. But that's only because we haven't been picky about the exact nature of the data. Other times, though, we may need to be specific with what data we use and how. For example, we may need to skip importing some fields that are unnecessary. Change the mapping of the selected fields, convert the data type of some values, or process them in some other way by changing all characters to uppercase, for example. These kinds of scenarios can be configured by adding another entry to our Logstash configuration located in the filter section, which is called mutate. To make the next part easier, you can download the configuration file which we are going to be working with. You can do so by typing in the following command sudo wget p. Etsy logstash conf d https raw GitHub user content.com slash corelogic's dash resources slash elk course samples slash master slash csv read dash drop conf. And as a quick aside as to what we're trying to accomplish here, you'll see that there's a bunch of metadata fields that have been inserted automatically here, for example timestamp and version and message and host and path. These were not in our original CSV data. They were added automatically by Logstash. Maybe we don't want those. Maybe we want to get rid of those. So we're going to try to do that here. All right. We've got our new configuration file. Let's see what's in it using the cat command sudo cat etsy logstash conf slash csv read conf. And this shows the configuration file. And let's have a look at the mutate section that's new here. So in this example the filter section now has two main entries CSV and mutate. Keep in mind that the order in which these appear is important. In this case, the parameters from the CSV section will be applied first, and only afterwards will the ones from mutate be applied. The convert section is pretty straightforward. All the values in the age field will be converted to integer values. For example, the age value 30.5 would be converted to the integer value 30. Under remove field, we specify all the fields that we want to remove. Those are all those metadata fields that were automatically inserted for us. So now we can run Logstash with these new settings with this command usr share logstash slash bin slash logstash dash f Etsy logstash. Conf dot csv I've read. Drop off. Okay, kick that off. It will take a few seconds to complete again. Okay. Looks good. So after the action is completed, we'll press control C to exit from the execution. And now let's check that the fields were removed as we specified in the configuration file with the following curl command curl x get http localhost 9200. Demo csv drop was our new index that we created. Their underscore search pretty equals true. And dash d single quote curly bracket size one. So that should just return a single result. All right. So part of the response will look like this. And we can see here that the fields we specified were indeed removed as we intended. So all those extra metadata fields are gone. Let's also check the mapping for the index named demo CSV drop with curl x. Get http localhost 9200 demo csv drop underscore mapping field age pretty equals true. A response such as the following shows us that indeed, the age field is of the integer data type that we specified. Notice that it's called long here, but that's just an integer type called a long integer, which can store more digits than a short integer. Well, there you go. You now know how to parse an import data from CSV files into Elasticsearch via Logstash, and modify the data to your needs.
