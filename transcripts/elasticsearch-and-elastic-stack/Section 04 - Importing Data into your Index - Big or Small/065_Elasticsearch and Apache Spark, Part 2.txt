# Elasticsearch and Apache Spark, Part 2

All right. So now that we have SPARC up and running and it is connected to Elasticsearch, we can play around with it. Let's write a script that will just load up that fake friend's data that we looked at in the previous video and pass it out and store it within a Spark Dash Friends Index within Elasticsearch all using Apache Spark. This seems like a very complicated way of doing this, and it is. But if you're dealing with massive data, set spark can be a powerful tool for that because Spark can actually parallelize this processing. So by using Spark, if you are running this on a real cluster, you could have multiple nodes on a cluster and multiple processes on each of those nodes, all chugging away at the same time, processing different parts of that data together and getting it done much more quickly and all feeding it into Elasticsearch. So let's start by importing that package that we looked at earlier to do that integration between Elasticsearch and Spark. To do that will just say import or export Elasticsearch. Not sparked a sequel, not underscore. And I'm not going to teach you scalar or spark in this course, but I'll at least tell you what each line is doing. We're just saying we want to import everything under Spark Dot sequel under the Elasticsearch package. And that succeeded. So that's a good sign. That means that we were able to successfully use that Elasticsearch integration package. If you got an error here, it probably means that you have the wrong version of the Elasticsearch package for the version of SPARC that you installed. So might want to go back and check that. Next, we're going to make what's called a case class. And this is just a scalar way of very succinctly defining a class that has certain members. And for our purposes, we can maybe think of this as like akin to a database table with columns and let's name that thing a person which contains the following data. Collins ID, which will be a type integer. Pay attention to case here, by the way. It does matter. Comma name, which will be a type A string, comma, age, which will be of type integer and num friends, which will also be of type integer. All right, so now we have a person class defined that has an ID name, age, and some friends associated with it. Next, we'll write a little function called mapper, and it will take in a line that is of type string and it will return a person object. And we will define this function thusly. You'll start off with a immutable variable vowel is the syntax for that in scalar called fields, which will be a line dot split on commas. So this line is just going to take each incoming line of that raw file, split it up based on commas, and create a list of strings called fields. Next we'll say vital person, which will be a type of person, and we will construct a new person instance of the person class from fields zero. Oh, and convert that to an integer while we're at it. Fields one. Because these are all strings to begin with. But the name is a string. Fields two will be the age, and we do need to convert that to an integer as well. And finally, Fields three. The number of friends and we'll convert that to an integer. All right. And we will return the resulting personal object that we just created to close out that function. And we now have a new function called Mapper. Very cool. Now we can use it. First, however, we need to import sparked implicit, which is just something we need for dealing with what we call data frames in spark. But that's a whole other course. And now we can say vowel lines equals spark, dot spark, context, dot, text, file. So we're going to create a new spark context from the text file, fake friends. CC I think that we just downloaded in the same directory that we started it from. And that succeeded. So now we're going to take that Lions object, which is actually an RTD, which is a old data structure in SPARC. But again, that's a whole other course and we will say val people equals lines dot map using the mapper function that we defined above and we will take the results of that mapping function and convert it to what we call a data frame in spark. All right. So far, so good. And finally, let's do something with all this. So the cool thing about SPARC is that it doesn't really actually do anything until you tell it to do something with the output. And at that stage, you can figure out how to optimize the processing in the best way. It creates what's called a directed a cyclic graph to try to optimize all the various processing and all the interdependencies of what you've told it to do. And that's what's going to happen right now. And now I will kick that off with people thought safe to eat us and we'll give that index a name sparked friends. And off it goes. And it's done not a whole lot of data there. So didn't take very long at all. But again, if we had massive amounts of data that could actually parallelize all that processing and all that importing into Elasticsearch and how to get out of this, let's say, control D and now we can see if that actually got into Elasticsearch. So let's say curl dash X gets HTP Colon Flags local host colon 1800 slash SPARC dash of friends A-Z index. So we think we just created slash underscore search to get the first results in it. Question mark pretty and let's see what we get back. Oh, my gosh, it worked. Cool. So we can see here that Hugh is 27 years old and has 181 friends. Jadzia is 38, has 380 friends and so on and so forth. So that actually worked. Very cool. So we installed Apache Spark, we installed the connector between Spark and Elasticsearch, and we actually passed some data using Spark and then saved that data into Elasticsearch. Again, pretty complicated way of getting data into Elasticsearch, but the power of this approach is how scalable it is. So I could use Spark to parallel process massive amounts of data and I'll feed it into my Elasticsearch cluster at the same time. So if you're dealing with big data, this might be a useful trick to have up your sleeve. So there it is, Elasticsearch with Apache Spark in Action.
