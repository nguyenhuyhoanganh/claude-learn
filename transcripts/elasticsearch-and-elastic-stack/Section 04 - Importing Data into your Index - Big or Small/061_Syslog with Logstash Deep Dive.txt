# Syslog with Logstash Deep Dive

Syslog is a popular standard for centralizing and formatting log data generated by network devices. In this lesson, we'll learn how to use log stash to collect syslog messages from our servers and devices. We'll explore two methods. One involves using the syslog daemon to send logs through a TCP connection to a central server running log stash. The other method uses log stash to monitor log files on each server or device and automatically index messages to Elasticsearch. Syslog is a standardized way of generating and collecting log information such as program errors, notices, warning status messages and so on. Almost all units like operating systems such as those based on Linux or BSD kernels, use a syslog daemon that is responsible for collecting log information and storing it. Since logs are usually stored locally, but they can also be streamed to a central server if the administrator wants to be able to access all the logs from a single location. By default, Port 5.4 and UDP are used for the transmission of CIS logs. Note that it's recommended to avoid UDP whenever possible, as it doesn't guarantee that all logs will be sent and received when the network is unreliable or congested. Some messages could get lost in transit. For more security and reliability. Port 6514 is often used with TCP connections and TLS encryption. Normally, a six log server is used when an administrator wants to collect source logs from multiple devices to a central location. But obviously we'll learn how to do this with log slash and send it to Elasticsearch as we want to take advantage of its super awesome powers of ingesting large volumes of data and then allowing us to quickly and efficiently search for what we need. So let's dive in and do it. Hands on. Let's take a look at how typical syslog events look like. They're usually collected locally in a file named var log syslog. So to display the first ten lines of ours, let's type in pseudo head. Dash ten var log syslog. All right. So let's analyze how a syslog line is structured. You can see the line starts with a timestamp, including the month name, day of month, our minute and second at which the event was recorded. The next entry is the hostname of the device generating the log. Next is the name of the process that created the log entry. It's process, ID number, and finally the log message itself. Logs are very useful when we want to monitor the health of our systems or debug errors. But when we have to deal with tens, hundreds or even thousands of such systems, it's obviously too complicated to log into each machine and manually look at SSIS logs by centralizing all of them into Elasticsearch. It makes it easier to get a bird's eye view over all of the logged events. Filter out only what we need and quickly spot when a system is misbehaving. In this lesson, we will explore two methods with which we can get our data into log stash and ultimately into an Elasticsearch index. One will be using the syslog service itself to forward logs to logs dash by a TCP connections and two will be configuring logs dash to monitor log files and collect their contents as soon as they appear within those files. Let's start with forwarding CEST log messages to log stash via TCP connections. The Syslog Daemon has the ability to send all the log events that it captures to another device through a TCP connection. Logs dash, on the other hand, has the ability to open up a TCP port and listen for incoming connections. Looking for syslog data. Sounds like a perfect match, so let's see how to make them work together. For simplicity, we will obviously use the same virtual machine to send the logs and also to collect them. But in a real world scenario, we would configure a separate server with logs to listen for incoming connections on a TCP port. Then we would configure the syslog daemons on all the other servers to send their logs to the log stash instance. So this is important. In this exercise, we're configuring the syslog daemon first and logs dash last since we want the first captured logged events to be the ones we intentionally generate. But in a real scenario. Configure log slash listening on the TCP port first. This is to ensure that when you later configure the syslog daemons to send their messages, logs, dashes, ready to ingest them. If Lodge Dash isn't ready, the log entry sent while you can figure it won't make it into Elasticsearch. We will forward our SIS logs to TCP port ten 514 of the virtual machine logs dash will listen to port ten 514 and collect all the messages. Let's edit the configuration file of the syslog daemon with sudo nano. Let's see our syslog rd slash five oh dash default icon. Now above the line. First, some standard log files will add the following. Start out start at at 120 7.0.0.1 colon 10514. Now the start out star indicates to forward all messages and the two at SINES instructs the syslog utility to transmit data through TCP connections. Now to save this config file, we're going to press control X after which we hit Y and enter. Now we'll need to restart the syslog daemon called our Syslog RD so that it picks up on our desire changes. We'll do that with sudo system control. Restart our syslog dot service. Now we installed the git tool in previous lessons, but if for some reason you don't have it available on your test system, you can always install it with sudo apt update and sudo apt install git. Already have it. So now let's clone the repo which contains the configuration files that we want to use with log stash. We'll do that with pseudo git clone https github dot com slash corelogic stash resources slash logs dash dash log dot git and we'll put that in SC logs dash commodore rd logs dash dash sis log double check for typos. I think it looks okay. All right. So now let's take a look at the log entry is being generated by the system d processes pseudo grep system. D bar log. Sis log. All right, so let's copy one of these lines and paste it into the Grok debug dot Hiroko ABC.com website. We open that up. So we're going to put it in the first field in the input section. Let's just take one of those lines that no matter which one. And pasted in that first field. And I'm going to pull up the logs to ask configuration that we're going to be using here. Let's take a look at that here as well. You can see in the highlighted input section how we instruct logs to actually listen for incoming connections on Port ten, 514 and look for syslog data. Now to test how the grok pattern we use in this config file matches our cis log lines. Let's copy it. We're just going to use this within the marks here all the way to there. Copy that out. And then we'll paste it into the Grok debugger in the second field here. In the pattern section, and we can see that every field was perfectly extracted. Great. So now let's run lots dash with this configuration file. To do that, we'll just say pseudo user share log stash been log stash dash off etsy logs dash kafka slash logs dash dash syslog slash syslog dash tcp dash ford conf. Now since logs are continuously generated and collected, we won't stop log stash this time with control c will just leave it running until we see what we want to see. Specifically we're going to be looking for successively started lock stash. And there it is. Great. So let's leave lock stash running in the background, collecting data. So we're going to leave its terminal window open and we're going to open up a second terminal window to enter the next commands. So I'm just going to click on my putty here and open up a new E7 session. Now it's very likely at this point that no syslog events have been collected yet, although it looks like a few just came through. Since we just started Log Stash, but let's make sure to generate some log entries first. So a simple command such as sudo else should do the job. And that will ensure that we generate at least a few log messages and we'll be able to see in the window where Long Stash is running. That pseudo generated some log entries that have been added to the Elasticsearch index. There it is. The output will get will contain something similar to this. So that's awesome. Everything work perfectly. So now let's test out the other scenario. We're going to monitor SIS log files with log stash. So first we need to stop the logs dash process we launched in the previous section. We're going to switch to the terminal where it's running and hit control C to stop it. So now let's take a look at our new logs, task config that we're going to look at this time. And you'll see that the important part here is that we're telling it to monitor the VAR logs, this log file. So let's go ahead and run logs, dash with that configuration. And to save me some typing, I'll just page up and change the actual file for the config to logs. Dash dash monitoring sis log conf. And as usual will wait until it finishes its job and then press control C to exit the process. So let's stop that and let's see the data that has been passed with colonel districts get GDP. Local host 1900. Syslog dash. Monitor underscore search pretty. Vashti. Single quotes and size will be one. And you should get an output similar to this. Looks like it worked. Cool. All right, so to clean up what we've created in this lesson, we just need to delete the two new indexes that we added with a curl. Bash X Delete History. Local Hosts 1900 Syslog Dash Received on TCP. And also syslog dash monitor. And we'll also delete the directory where we placed our this law config files with pseudo our m dash our SC logs dash poverty logs, dash dash syslog. So as you can see, it's fairly easy to gather all your logs in a single location and the advantages are numerous. For example, besides making everything more accessible and easier to search, think about servers failing. It happens a little bit more often than we like. If logs are kept on the server, once it fails, you lose the logs. Or another common scenario is that hackers delete logs once they compromise the machine. By collecting everything into Elasticsearch, though, you'll have the original logs untouched and ready to review to see what happened before the machine experienced problems.
