# Logstash Input Plugins, Part 2: Generator Input and Dead Letter Queue

Let's move on to the Generate input plugin. Sometimes for testing or other purposes, it would be great to have some specified data to be inserted. The conditions might vary, like you might want to have some specific data generated for longer times, or some data generated for some iterations. Of course, we can write custom scripts or programs to generate such data and send to a file or even to Logstash directly. But there is a much more simple way to do such things within Logstash itself. We can use the generator plugin for logstash to generate random or custom log events. Let's dive into an example. First, let me pull up a configuration for this example here, so we can take a look at what it looks like. So the configuration would look like this. In this configuration under the lines section two JSON documents were given. And also for the Logstash to understand it in JSON we've specified the codec value as JSON. Now the count parameter is set to zero, which basically tells the Logstash to generate an infinite number of events with the values in the lines array. If we set any other number for the count parameter, Logstash will generate each line that many times. So let's go ahead and run logstash with this configuration file using the following command. And let's kick that off. Again we'll give it some time for Logstash to run. And after it has started running, we'll wait for some time and then hit control C to quit the Logstash screen and to stop it. Off it goes. All right. Huh? It's definitely working. Let's hit control C to stop it. All right, so now let's inspect a single document from that index that we just created. And again I'll allow to cheat a little bit to find a similar command. All right. So instead of heartbeat epoch the index that we just created is actually called generator. Otherwise it's the same. And we can see that the document in the result looks like this. And I'll call attention to some of those generated fields like last name and first name and gender and email and IP address and ID. And that looks like it worked. So this plugin is very useful when we need to have documents generated for testing. We can give multiple lines and test grok patterns or filters and see how they are getting indexed using this plugin. Next let's move on to the dead letter queue. This is one important input plugin and is an immensely helpful one too. So far we have seen the cases of successful processing of events using Logstash. Now what happens when Logstash fails to process an event? Normally the event is dropped and hence will just lose it, but in many cases it would be helpful to collect those documents for further inspection and do the necessary actions so that in the future the event dropping won't happen. Been to make this possible. Logstash provides us with a queue called dead letter queue. The events which are dropped would get collected here. Now, once in the dead letter queue, with the help of this dead letter queue input plugin, we can process these documents using another Logstash configuration and make the necessary changes and then index them back to Elasticsearch. You can see the flow of unprocessed events to dead letter queue and then to Elasticsearch in this little diagram here. Let me pull this up for you. So noodle on that for a little bit. So now that we've seen what a dead letter queue is in Logstash, let's move on to a simple use case involving the dead letter queue input plugin. Since we are using the dead letter queue input plugin, we need to do two things prior. First, we need to enable the dead letter queue. Then we need to set the path to the dead letter queue. So let's create a folder named Dlq to store the dead letter queue data in. By typing in the following command. Mkdir home student dlq. Now Logstash creates a user named logstash during the installation and performs the actions using this user. So let's change the ownership of this folder to the user named Logstash by typing the following command sudo change own dash big r logstash colon logstash home student dlq. We'll also need to make sure a couple of directories we'll be using are writable. So let's do sudo change mod a plus w slash var lib slash logstash. And also same thing for slash home slash student slash dlq. Now let's enable the dead letter queue and also specify its path in Logstash settings by editing the settings file by typing in sudo nano etsy logstash logstash dot. Now we're going to uncomment the setting dead letter queue if we can find it. There it is. And we will just change that false to true. And we also need to set the path to store the queue data in by Uncommenting the path dot dead letter queue. And set that to home student queue. All right so the logstash. YML file should look like this in this section. After we've made the necessary changes and to save our file we'll just hit control X Press Y and enter. So now let's input a few documents from a JSON file, and let's have a look at the documents in the file by opening the file using this command. Cat Etsy logstash conf d logstash input plugins slash dead letter queue slash sample data dot JSON. And you can see that in the first ten documents the age field has integer values. But from the 11th document it's mistakenly given as a boolean. So what happens is that Elasticsearch will assign a type long data type used for the integers in the age field. This is because when we are indexing the documents, the first document which has the ages integers are indexed first, so Elasticsearch will assign them the age field. Now in the 11th document comes with a Boolean value for age. Elasticsearch will reject the document since the field cannot contain two data types. so it does for all the documents with age as a boolean value. So let's index the sample data and see what happens. For that let's run the configuration file for logstash. Let's have a look at that configuration file first. I'll pull that up for you. And the configuration file would look like this. Note that we've set the index to sample data. And we have the path to our sample data here in JSON format as well. So let's type in this command to run Logstash for indexing the sample data we'll say usr share logstash bin logstash. Dash dash path dot settings Etsy logstash and dash f to Etsy logstash conf dot d logstash input plugins, dead letter queue and dlq Q dash data dash oh one dash ingest conf. Now the command we just typed there is an extra parameter called path dot settings and its value is etsi logstash. This is given so that logstash will understand where the logstash. YML file is located. And we'll take in those settings. So as usual we'll give some time for Logstash to run. And after it's started running, we'll wait for some time and then hit Ctrl C to quit Logstash and stop it. So let's let that run. And away it goes. Cool. All right. So let's go ahead and hit control C. So let's inspect how many documents were inserted into the index. There was a total of 20 documents. And out of it only ten should have been indexed as the other ten would have the mapping issue. So let's check it out with curl dash x get HTTP 127.0.0.1. Colon 9200 Hundred slash sample data underscore search pretty equals true dash d single quote curly bracket track. Total hits will be set to true and size will be set to zero. And we should see a response like this. And in the response we can see that the hits dot total dot value is equal to ten. And now we can see that the remaining ten documents are not indexed. So these documents are dropped. And since we have enabled the dead letter queue these documents should be there. So let's use the input plugin for reading from the dead letter queue. The configuration for reading from the dead letter queue can be seen in this file I'm about to pull up for you. In this configuration, we have pointed to the path where we had set the dead letter queue. Now if we run the configuration, Logstash will read the contents in the dead letter queue and process it and index it into Elasticsearch. For the sake of simplicity in the configuration, I have not added any filters. We can definitely add filters depending on the use case and run the configuration if we want. But let's go ahead and run Logstash with this configuration file using the following command. And I'll just hit the up arrow to go through my history and save me some typing. So this time we're going to set that to dead letter queue just dlq conf. And again give logstash some time to run. Off it goes and looks like it's already done. So we'll hit ctrl C to stop it. And now we can inspect the number of documents that were indexed now by typing in the following request. Curl x. Get HTTP 127.0.0.1 9200 slash zero one slash underscore search pretty equals true and dash d single quote curly bracket size is one. And the response to this request should return something like this. And you can see that there are ten documents that were indexed. And upon inspection on the sample document it returned one document which had the Boolean value for the field age. Note that with this configuration, after running Logstash, the queue contents would not be cleared. If you want the documents which were read not to be indexed again, the flag commit underscore offsets would have to be true just under the path setting.
