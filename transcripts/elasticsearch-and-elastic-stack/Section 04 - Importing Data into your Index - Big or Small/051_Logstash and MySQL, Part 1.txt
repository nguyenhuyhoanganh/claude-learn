# Logstash and MySQL, Part 1

Okay. So let's say you have some data in a sequel database that you want to import into Elasticsearch blocks. Dash can do that, too. And in this example, we're going to use Log Slash with my sequel to actually extract data from a table in my sequel and automatically create an Elasticsearch index from that data. Now the first thing you need to do is make sure you have a G.P.S. driver installed for my cycle on the system that you're running log stache on. That's just a bit of Java code that allows Java to talk to your my SQL database. And you can go to the my SQL website to download the latest one for you. Odds are, if you're on a system that's being used for some other corporate purposes, it might already be installed for you somewhere. But if not, this is how to go about getting it. Just find the latest URL from the dev dot my wsj.com website. Copy that you get it and then unzip it and put it somewhere where you'll remember where it is. At that point, it's just a matter of configuring logs to ask to use that driver and how to connect to your database. You can see here we have a G.P.S. input configuration block here. All you need to do is specify a connection string that specifies where your my SQL host is running and on what port and under what database you're actually talking to. So in our case, we're going to set up a Movie Land's database running locally on Port 3306, and we need to give it the login credentials to connect to that database. The Path to the Jar file that contains the Java Library for connecting Java to my sequel. That's the name of the actual class in Java. And then finally the statement that you want to use to extract data from that database. So in this case, we're selecting every row from the movies database and we're going to turn around and feed that into our output for logs dash which in our example will again be Elasticsearch. But you could do something else here where you could enter any arbitrary SQL. You want to select certain columns from your database or certain rows based on some selection criteria that you might have. So the sky's the limit there. And how you actually want to go about extracting the data from your database that you want to turn around and index into Elasticsearch, you don't have to get every row and you don't have to get every column. You can craft your own SQL there to do whatever you need to do. So let's go see how this all works. So before we can import data from my SQL into logs, Dash and Elasticsearch, first, we need to install my SQL itself. So let's start with that. Let's start by saying sudo apt dash get update just to make sure everything's up to date. In our repositories. And with that out of the way we can install my SQL server with sudo apt get install my sequel dash server. Yep. Go for it. And that is a pretty big package. So that will take a couple of minutes to come down and install. So my sequel is now up and running on our local machine here. That wasn't so hard. So let's put some data into it. First, we need to get some data to put into it. So let's go ahead and download that from the movie lens repository. They'll get HTP, colon, cigar files, dot group lens and doored. Flash datasets. Flash movieland. Slash eml dash 100 k zip and that will get the 100,000 movie lens data set. Download it to our home directory here. Let's go ahead and decompress that with unzip MLA dash 100. Kate it. All right, so now we have our data here just waiting to be imported. So let's sign into my sequel and get that into my SQL table. Sudo my school dash dash local dash in file. We need to explicitly give this thing permission to load files from the local file system equals one dash you route dash p and the default password is password. And we're in. All right. Let's start by creating a database to put our data in with create database. VLANs will call it. And now let's create a table within it. Create table. Villains. Movies. What we're going to do is create a table that maps movie aids to movie titles and their release dates. So open parentheses. First column will be a movie. It will be of type integer and it will be a primary key and it cannot be null. We will also have a title field which is of type text. Don't forget your comms, guys. And finally, a release date. Of type date. In parentheses semicolon. And we now have a handy dandy movies table. So by default, current versions of most people don't allow you to import data locally. So we need to explicitly enable that next. And to do that will just say set global. Local underscore in file equals true. And now we should be able to say load data. Local in file. So we're going to load up that data that we just downloaded. Single quote eml dash 100 K slash UK item. Single quote into table movie lens dot movies. And we need to set the character encoding because otherwise the bad things will happen. Character set. Latin one There are some foreign characters in some of these movie names, so we have to take that into account. Fields terminated. By a single quote, pipe single quote. This is an odd CSV file. It's not actually separated by commas. It's separated by paper characters. All right, hit enter and we'll go to a new line here. Just keep things neat. So what we're going to do is load up those three columns of information separated by pipes into the movie ID column. I'm a tidal column and will move the release date field into variable three at var three and you'll see why in a minute. So now we need to convert that release date from the file into an actual date that my people can understand. So we'll say set release date. That's the name of the release date field in our table. Equal to stir to date. At Bar three. So let's kind of take the raw value from our file that we're importing and we're going to use the following format there to decode it. Percent Small D dash. Percent Big M dash. Percent Big y single quote, close parentheses, semicolon. And if we're lucky, we didn't have any typos and that this will work. And just like that, our date has been imported. Now if you do get an error, sometimes you might just have to quit out of my sequel and go back in and try that low data command again. But for me it worked. So yeah. So now let's see, 13 is actually in there. Let's confirm that it's actually in the table that we expect it to be. To do that, we'll just say use movie movieland. To set that to our current database. And now we can query that database with select statements such as Select. Star from movies. Fair title like single star percent sign. Single quote semicolon and we should get every movie title that has the word star. Sure enough, there it is. Star Wars. Stargate. Star Trek. Starship Troopers. Very cool. And this is a pretty old data set. So a lot of the newer Star Wars movies aren't actually in there. But we can see the query worked. We have data in here. So our next step will be to take this data from my sequel and imported into Elasticsearch via log stash. So let's do that in our next video.
