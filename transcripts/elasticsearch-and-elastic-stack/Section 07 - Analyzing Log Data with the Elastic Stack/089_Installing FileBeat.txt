# Installing FileBeat

So before we can play with Filebeat, first we have to install it. So let's get that out of the way, shall we? Just for your reference, here are the steps that we're about to do live in the console here. But this way you'll have this for your reference as part of the slides and course materials if you need it later. Now remember, if you do need to do this out of sequence, we are assuming that you've already configured your system to have the Elasticsearch repository as part of its configuration. If you need to set that up, go on back to the first setup lecture where we installed Elasticsearch itself at a high level. What we're going to do is install Filebeat from the elastic repository. And in Elasticsearch seven, many of the common plugins you would use are now pre-installed as part of Filebeat. So there's not much that we need to do there. Then we'll download our sample Apache access log and start the Filebeat service to start consuming it and feeding it directly into Elasticsearch. So unlike the diagrams we showed, we're not actually going to be using Logstash as an intermediary here. We're just going to go straight from Filebeat into Elasticsearch because it's all running on one server anyway. There's not much point to having that intermediate layer in our case, so we're just going to go straight from Filebeat into Elasticsearch, and we're going to consume our sample access log after we get it all set up. So let's dive in and take care of it. All right. Let's get Filebeat installed. It's pretty easy, but as usual, we want to make sure we're installing a version that's consistent with the version of Elasticsearch we're currently using. So to check that real quick curl dash x, get 127 .0.0.1 colon 9200. And you can see here that I'm running version 9.0.2 for you. It's definitely something newer because this changes very often. But just remember that version number because we need it to install the correct version of Filebeat, which we will do using sudo apt get install filebeat equals whatever that version is. And again for me that's 9.0.2. For you it is something else and already installed this earlier, so it won't do much for me. But for you this will do something more interesting. All right, that was easy. Now let's download some logs to play with and actually feed into Filebeat. So first let's make a directory for those. And we're currently in our home student directory. Let's go ahead and make a logs directory underneath that and CD into it. And from here let's go ahead and get some sample access logs from Apache. So we'll say wget http colon slash slash media.com slash slash access underscore log. And these are just some sample Apache access logs that came from one of my own websites. All right. So now we need to configure Filebeat to know where that log file is and know how to import it. So to do that we're going to go into the configuration for file beat and mess around. So first let's go to the right directory for that. That's going to be CD slash etc.. Slash Filebeat slash modules dot D. And in here there's a whole bunch of different sample configuration modules for different sources of logs. You can see there's Kafka and IIs and all sorts of stuff, but the one we're interested in is going to be Apache. So there is an Apache dot yml file up there. If we scroll up I guess we'll see it. There it is. Apache disabled. So all we need to do is rename that to take off that disabled bit. So we're going to say sudo mv Apache dot yml dot disabled and move that into a new file called Apache dot yml without dot disabled. And now we can edit that file and set it up the way that we want. So let's say Nano or whatever editor you like Apache Dot yml. And it's currently Unrideable I need to do that a sudo don't I. So sudo nano apache. YML. There we go. All right. So we need to uh first of all enable the access log feature. It's false by default now. So let's change that to true. And we also want to import error logs as well from Apache. We're not going to do that right now but we will later on. So we may as well enable that. And now we just need to set up the paths for where these files are found. This is under the error section. So this will be the path to our error logs. And that will be within square brackets and double quotes. Home student logs. Error. Star and quote and bracket. And similarly, we need to do that for our access log, which is the file that we actually have. So I'm going to uncomment that as well and set the access log path to. Again in brackets because I can have more than one path here if I want home student logs access star. So anything beginning with access is going to be imported into Filebeat. All right I think that's all good to go. Double check for any typos. Go ahead and write that out for me. That'll be control O and control X to get out of the editor. Now we can just start the Filebeat service and it should do the right thing. So let's do sudo slash bin slash system control start filebeat dot service. And that should kick it off. And it will take some time for it to spin up and do its thing. So you might want to go grab a cup of coffee or something and come back in a few minutes. But in time you should be able to see that new index created from Filebeat. One way to check for it is to say, well, first of all, let's go back to our home directory and let's say curl x, get localhost colon 9200 slash underscore cat slash indices question mark v. So we're just getting a list of all the indices in our Elasticsearch instance here. And hey there it is already. So that was actually pretty quick. So you can see there that there is a dot ds Filebeat dash 8.2.0. Whatever it is today's date, and that is our new index that Filebeat has created by importing that sample access log. And actually I can hear my hard drive chugging away right now as it's importing that huge access log file. So just going to let it sit there for a few minutes until it's done. Uh, but yeah, it looks like it's working. So our configuration worked. Um, it looks like we actually created that index successfully. And as we speak, it is populating that index with actual access log data. So that's all there is to getting set up. And we'll move on and play with this in subsequent lectures.
