# FileBeat and the Elastic Stack Architecture

So we can't talk about the elastic stack without talking about the beats framework and fail beat in particular. So file beat is basically this thing that can sit between your log files and log stash in order to provide a more lightweight interface for actually collecting and shipping your log data around. It'll provide an extra buffer layer that's very lightweight so you can just have a file bead running on your individual web servers that just sits there and looks for changes in your log files. And that just shipped some very efficiently and very simply to log stash. Or it can go straight to elastic search as well if he wanted to. The reason this is important is because by having that extra layer in there you get that sort of extra protection. This is called back pressure and we'll talk about that more in a moment. So if logs starts to back up that can exert back pressure on file beat and it can tell it to kind of slow down for a little bit. And similarly if elastic search is getting backed up on inserts it can say OK logs dash you need to slow down a little bit and you need to slow down a little bit on file B and that way you get this more resilient system where you can have these intermediate layers that can cache data and buffer it up for you in case things get bottlenecked at some point. So it provides a little bit more flexibility and it also gives you a little bit more of a lightweight solution on your web servers themselves. Chances are your web servers are busy enough as it is serving their web pages and you don't need some heavyweight thing like log stash actually passing out these log files at the same time that they're being generated. Now these logs can come from many sources. They can come from Apache and G.I. annex or my sequel even. Basically any kind of log you can imagine can be consumed by file beat and a file beats job is just to maintain a read pointer on each log so every log line acts like it's part of a Q and file B just treats it as a queue of events that gets sent off to logs dash or elastic search or whatever you have it set up to do. It's just that simple then logs dash can actually handle the passing of that data and imparting structure upon it and feeding that into elastic search at whatever rate elastic search can handle. And then once you have the data in elastic search you can use Cabana to actually analyze that data. So this as a whole is the elastic stack in a nutshell. This is usually what people are talking about now before beats existed you'd hear people talk about the elk stack which is kind of a cute acronym that's good for elastic search log stash and Cabana. But when people realized that log stash was maybe a little bit too heavyweight of a solution to actually run on your web servers that's when the beats framework got introduced an elastic search log stash file beats and Cabana doesn't really roll off the tongue as well as elk stack. So we just call it the elastic stack now. It's what usually people use when they're trying to use elastic search to actually provide an analytic solution for their log data. Why would you use file beat and log stash and not just one or the other. I mean usually you want to aim for a more simple solution right. Simple is good and generally that's true because it lets things that can break less things that you have to configure things that you can get wrong. But in this case there is a real advantage to scalability and reliability on your cluster to using both like I said final beat is so much more of a lightweight solution than log stash. It's going to put less load on your web servers and that's a good thing because when web servers go down your customers can't access your Web site. It also provides the ability to provide back pressure. Like we said by having this extra buffer layer in between your logs and elastic search that can kind of keep things held up if things get backed up further down the pipeline. It also gives you more flexibility on scaling out your clusters so you can independently scale out your log stash fleet independently scale up your elastic search fleet and independently scale out your front end Web servers whatever makes sense. And before if you were running more than one system on the same machines those two systems would be tied together in terms of scalability. So it can actually save you some money too. Believe it or not but the first thing we need to do is install file beats and we'll take care of that shortly but first let's talk more about back pressure and security. Let's talk about file beats back pressure sensitive protocol a little bit more deeply as it is an important feature. It's kind of the whole reason you'd want to use file beat in connection with logs dash logs dash can be used basically to crunch the data coming in transforming it before setting it off to its final destination. The problem is that logs dash might not have enough capacity to keep up with all the data coming in from the logs. Perhaps there's a huge bursts of traffic or something that could bring down your system if there wasn't a way to control the rate at which it's processed. So starting at the bottom file beat maintains a reed pointer into the physical log files that are appearing on each machine. Really we just have to ensure that file BD isn't moving this Reed pointer ahead faster than logs dash can handle file beat as what's running at the edge on the actual machines where the logs are being generated and then sending that information into a log stash cluster for further processing. This works through what's called a back pressure sensitive protocol between logs dash and file beat. If logs Dash sees that it's starting to back up it lets file beat notes slow down the rate at which it is reading file beat then slows down the rate at which it's moving its Reed pointer ahead through the logs until logs dash catches back up when logs dash those things are okay. File B will resume its usual pace. This basically avoids the need to over scale the machines powering logs dash near system in order to deal with transient peaks of traffic. So not only does this result in a more reliable system it can save you money to here's an example of how you might set all of this up in a real production environment in order to maximize scalability and resiliency. You start with file beat or another data ingestion tool from the beats framework running on the edge nodes of your system. This monitors the data at the point of collection and sends it off to logs dash for further processing beats should load balance across a group of logs dash nodes. So if one logs dash node goes down the system keeps running for high availability. You want at least two logs dash nodes logs Dash has a feature called persistent cuz the data fed to logs dash for processing is kept in memory until logs dash can get to it in a queue. Persistent cues can be used to store this data on disk instead so that if a logs dash node goes down the information in that queue is not lost. Once whatever problem with the node is resolved. Logs dash can resume processing on the data that it still had to do before the crash ensuring that nothing is lost. Persistent queues guarantee what's called at least once delivery. This prevents message loss in the event of a shutdown of a log stash node planned or otherwise. When logs dash restarts it will attempt to deliver messages in the persistent queue until delivery succeeds at least once from their logs dash would feed transform data into your elastic search cluster where you can perform analysis on the data. Generally you'll want at least three dedicated master nodes because you need at least three in order for the system that elects a new master to work properly if the master goes down. And in addition to the data nodes that handle reads and writes on your cluster you might have warm data nodes that are only receiving rights and ready to take the place of a down hot data node immediately in a production setting security is also a concern. Wire encryption will be enabled between beats and logs dash and between logs dash an elastic search. So let's take a closer look at security next.
