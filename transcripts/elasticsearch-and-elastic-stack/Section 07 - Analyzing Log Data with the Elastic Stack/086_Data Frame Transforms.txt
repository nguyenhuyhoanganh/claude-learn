# Data Frame Transforms

From ingesting various events and documents into Elasticsearch is great for a detailed analysis, but when it comes to the common need to analyse data from a higher level, we need to aggregate the individual event data for more interesting insights. Aggregation queries do a lot of this heavy lifting, but sometimes we need to pre-baked the aggregations for better performance and more options for analysis and machine learning. In this lesson, we'll explore how the data frames feature in Elasticsearch can help us create data aggregations for advanced analytics. Data Frame Transforms is an important feature in our toolbox that lets us define a mechanism to aggregate data by specific entities like customer IDs, IPS, etc. and create new summarized secondary indices. Now you may be asking yourself, with all the many aggregation queries already available to us, why would we want to create a new index with duplicated data? Transforms are definitely not here to replace aggregation queries, but rather to overcome the following limitations. Performance complex dashboards may quickly run into performance issues as the aggregation queries get rerun every time, except if it's cached. This can place excessive memory and compute demands on our cluster result limitations as a result of performance issues. Aggregations need to be bounded by various limits, but this can impact the search results to a point where we're not finding what we want. For example, the maximum number of buckets returned or the ordering and filtering limitations of aggregations. Dimensionality of data for higher level monitoring and insights. The raw data may not be very helpful. Having the data aggregated around a specific entity makes it easier to analyze and also enables us to apply machine learning to our data, like detecting outliers, for example. Let's review how the transport mechanism works. Step one Define To create a transform, you have two options either to use Cabana or use the Create Transform API. We will later try both, but we'll dig deeper into the API. There are four required parts of a transform definition. You need to provide an identifier like a name, which is a path parameter. So it needs to comply with standard URL rules. Specify the source index from which the raw data will be drawn. Define the actual transformation mechanism called a pivot, which we will examine in a moment. If the term reminds you of a pivot table in Excel spreadsheets. That's because it's actually based on the same principle. Specify the destination index to which the transformed data will be stored. Step two Run. Here we have the option to run the transform one time with a batch transform. Or we could run it on a scheduled basis. Using continuous transform, we can easily preview our transform before running it with the preview transform API. If everything is okay, the transform can be fired off with these start to transform API, which will create the destination index if it doesn't exist and infer a target mapping. It will perform assurance validations and run the transformation on the whole index or a subset of the data. This process ends up with the creation of what's called a checkpoint, which marks with the transform has processed continuous transforms which run on a scheduled basis, create an incremental checkpoint each time the transform runs. The definition of the actual transformation has two key elements pivot acct group BI is where you define one or more aggregations. Pivot Dot aggregations is where you define the actual aggregations you want to calculate in the buckets of data. For example, you can calculate the average max in some value count, custom scripted metrics and more. Diving deeper into the pivot group. By definition, we have a few ways we can choose to aggregate terms aggregate data based on individual words that are found in your textual fields. Histogram will aggregate based on an interval of numeric values, for example 0 to 5 or 5 to 10, etc. and date histogram will aggregate based on the date and time. Don't worry if that doesn't make full sense yet. We'll see how it all works. Hands on right now. So we'll start with the API because generally it's more effective to understand the system and mechanics of the communication that way. From the data perspective, we'll use an older dataset that was published by Elastic. The Engine X Web Server Logs example. Now these web server logs are extremely simple, but they're close enough to real world use cases. The data sets very small and it's just for example, but the fundamentals really remain valid when we scaled up to actual deployments. So let's start by downloading the data set to our local machine. It's about 11 megabytes in size. And to help you out, I've got a little cheat sheet here for you at media dot sundog desktop dot com slash s slash transforms a-8. text because some things have changed for Elasticsearch eight here. And we're going to start off by creating a transforms directory to keep all of our stuff in one place for this activity. And then we'll copy off this w get command to actually download the data set itself. So back to our terminal, let's create the transform directory. And see the into it and paste in that you get command. And down it comes. And let's take a peek at what's in there. First, let's see how many lines we're dealing with with cat engine x Jason logs, and we'll put that into word count. It's like we have 51,000 Fortune hundred 62 lines. Let's take a look at the new one to see what we're dealing with. And it does, in fact, look like a Web server log. Cool. All right. So we're going to name an index for this data engine X. And next, we need to define its mapping. And for this mapping, we're going to take that time field and map it as a date type with a specific custom format to read that in. We'll also record the IP is an IP type and will also make the bytes field a long value. Everything else will keep is a keyword field. So to define this mapping first, let me get a clean slate here. That's what we're going to do here. And it takes two steps, actually. So the first thing we need to do is enable field data because in Elasticsearch eight, that is off by default. And if we don't have field data enabled, it's going to cause some problems later on in this activity. So let's copy that command off first. It's like that took. And now we will actually define the mapping itself. This command here. If you take a look, it does exactly what I said it would do. We're just setting up one shard. No replicas, because we're just playing around on a local machine. We're mapping the time as a deep tape with that specific format that matches the format in the log. We're gonna make the IP and IP type the bytes along type. Everything else is a keyword. So copy that off and you go. And looks like that worked. All righty. So now we just need to index our data and to practice some valuable stuff. We're going to use the bulk API and index all of that data with just one request. And although it does require some formal pre-processing, it is the most efficient way to index data. And the preprocessing is simple as described by the bulk format before each document. There needs to be an action defined and in our case it's just going to be index colon brackets and then optionally some metadata. So let's do that with this all command here that I'm going to copy off. And you can see it's just putting a line in front of each log entry that says index colon, bracket bracket. All right. So pretty simple. Let's copy that. He that in. My mouse is bouncy. It keeps double pasting. Our analysts take a look at what that did. Fashion. You and your next Jason logs bulk. And you can see it did what I said it would do it put that index line in front of each individual log entry line, and that is the format that the bulk format expects. All right. So now we're ready to index that data. And this is just going to be a standard post request to the bulk end point right here. It's just that simple. So go into bulk. And we just passed in data by an area with a link to that file that we just created with an that sign in front of it. So let's copy that off. And let's see what happens. I take a little bit of time. There we go. Off it goes. All right. So after a few seconds, there we have all of our documents indexed and ready to play with. Let's confirm that it's actually in there. So let's confirm that we have the documents in our index that we expect and that commands is going to be colonel localhost 9000 engine next underscore counts. Once again, my bouncing mouse. Just want one of those commands. Thank you. Here we go. I can see that we do have, as expected, 51,462 documents in that index. Very cool. All right. So now let's just dive right into the preview transform API and the Create Transform API. Now we're going to group by the remote IP to center our transform aggregations around that surrounded three things with this next command. One, we're going to define the remote IP field as someone by which two grouped by and then will define individual simple aggregations. So we're going to calculate the average number of bytes for the IP, the total sum of the transferred bytes and the total number of requests. That is the count of documents. And more powerful and complex, scripted aggregations can also be defined, which we'll do here. We're going to create two scripted metrics that will get the latest and earliest requests by time and return the timestamp. And both are written in Elasticsearch, his own scripting language called Painless, which is basically just a stripped down version of Groovy in Java. Also know that scripted metric aggregations all comply to the same structure. They have an init script. That happens once before every execution where you generally init your variables. There's a map script that's executed once for each document to perform the core calculations. We're comparing the date values in our examples. There's a combined script and that's executed once for each shard. Here you can start aggregating the results and we're just passing on the state here and there's a reducer script that's executed once it aggregated results from all the shards that responded. And that's the final dates comparison that we're doing here. So to define the transform and preview the results of our request, we're going to paste in this command here. It's a big one. Copy that. Back to our terminal. And looks like that worked. The formatting here isn't awesome, but you can see that for each IP address, those aggregations we defined are showing up. So for that IP address, we had three total requests and we have the the last timestamp for that, the first timestamp, total number of bytes and so on and so forth. Wear that out. And to actually create the transform, we just need to send a put command to the transform slash name endpoint. So let's take a look at what that looks like. So just a few considerations to make here. It's gonna look a whole lot like the previous command, but we need to provide a description of our metrics, which is optional down here at the bottom here. And we also need to specify a destination index, which is where we'll store the transform data, we're going to call it Engine X, underscore transformed. Also, if you wanted to run this transform continually, which we're not going to do right now, you could define a frequency and sync clause as well to accomplish that. All right. So let's go ahead and copy this off. And carefully paced that in. Looks like it worked. So after a few moments, we should see the new index created filled with the aggregated data grouped by the remote underscore IP parameter. So let's switch back to Cabana and see if we can find this thing. So I'm gonna open up my browser here and head on over to 1.7.0.0.1 colon. 5601. And we'll go to the default space. And from here, we're going to navigate to management. And under data, we have the transform section here and there is our engine X underscore transform that we just created via the API along with some statistics on its execution, if we were to open that up. Very cool. Now to create a new one, we could just click on the create a transform button here and pick a source index and start defining the transform just like we did with the API. Just be aware that the scripted aggregation would need to be defined in the advanced pivot editor. All right. That's pretty much it. We're not going to go through this example because it's basically the same thing we just did through the API, just in a prettier format. But that is it. So congratulations, you can now start running some truly insightful analysis.
