# "Search as you Type" Field Type

Open autocomplete with search as you type. You may have noticed how on sites like Google you get suggestions as you type with every letter you add. The suggestions are improved. Predicting the query that you want to search for achieving Elasticsearch Autocomplete functionality is facilitated by the search as you type field data type. This data type makes what was previously a very challenging effort remarkably easy building an autocomplete functionality that runs frequent text queries with the speed required for an auto complete search as you type experience would place too much strain on a system at scale. Let's see how search as you type works in Elasticsearch. When data is indexed and mapped as a search as you type data type Elasticsearch automatically generates several subfields. To split the original text into engrams to make it possible to quickly find partial matches. You can think of an end. GRAHAM as a sliding window that moves across a sentence or word to extract partial sequences of words or letters that are then indexed to rapidly match partial text every time the user types a query. The engrams are created during the text analysis phase. If a field is mapped as a search as you type, data type. Let's understand the analyzer process using an example. If we were to feed the sentence into Elasticsearch using the search as you type data type Star Wars Episode seven The Force Awakens The analysis process on this sentence would result in the following subfields being created in addition to the original field. So for the movie title field, the root field is analyzed as configured in the mapping. Star Wars Episode seven The Force and Awakens all its individual words. But for a movie titled Ought to Grab, it splits the sentence up by two words. So Star Wars Wars. Episode. Episode seven seven. The The Force and Force Awakens. And then for a movie titled DOT three Graham, that splits the sentence up by three words. And technically, this is what we call a single token filter. So this includes both the two grams and the three grams. That gives us a set of search terms. Star Wars. Episode. Wars. Episode. Wars. Episode seven, episode seven and so forth. And then we have movie titled Light Index Prefix. This uses an edge and gram token filter to split up each word into sub strings, starting from the edge of the word. So that gives us s as T as a star, etc.. The subfield of movie titled Art Index Prefix in our example mimics how a user would type a search query one letter at a time. We can imagine how, with every letter the user types, a new query is sent to Elasticsearch while typing star. The first query would be us. The second would be S-T, and the third would be stay in the upcoming Hands-On Exercise. We'll use an analyzer with an edge and gram filter at the point of indexing our document at search time, we'll use a standard analyzer to prevent the query from being split up too much, resulting in unrelated results. So let's do this. So for a hands on exercise, we're going to use the same data from the movie lens dataset that we used earlier in the course. So first, let's see how the analysis process works using the Analyze API. The Analyze API enables us to combine various analyzers, tokenize ers, token filters and other components of the analysis process together to test various query combinations and get immediate results. Let's explore edge and Grams with the term star. Starting from Min and Graham, which produces tokens of one character to Max and Graham, which produces tokens of four characters. Now to save you some typing, there's a lot of long commands in this exercise. I've uploaded a copy of these commands to the web for you. Just two media dots. Sundog dog tasks Ofcom slash s slash essay whitey that stands for search as you type dot text. And we'll start with this first command here. Let's take a look at what it's doing. So we're doing an analyze command here on the movies index that we imported earlier in the course. And we are going to say we want a standard token nicer with the text star but with an edge and Graham filter from a mean and Graham of 1 to 4. So let's go ahead and copy that. And go back to our terminal and right click to paste it in and execute it and we can see what happened here. So this yields the following response. And if we look at the start here, we can see the first couple of resulting tokens in the array. So the first token is S followed by T, followed by CTA and so on and so forth, going from 1 to 4 characters because our max gram was set to four. So we end up with a four character word star. Pretty easy, right? So let's further explore these search as you type, data type. Now to do this will create a new index called autocomplete in the put request to the create index API will apply the search as you type data type to two fields title and genre. So to do all that, we'll issue this put request back to our cheat sheet here. We'll copy this. And you can see that we're going to be creating a new index called Autocomplete with the title and genre, having the types search as you type. That's a special type. Copy that. And right click. And let's execute it to create that index with the search as you type. Data type. All right. That worked. So we now have an empty index with a predefined data structure. Now we need to feed us some information. To do that, we'll just re index the data from the movies index into our new Autocomplete Index. This will generate our search as you type fields, while the other fields will be dynamically mapped. So back to our cheat sheet here. You can see here that we're doing a re index command and we're going to be using the movies index as a source and reinforcing it into the Autocomplete Index. And when we're done will grep for total created and failures to get the results from the response that we're interested in. Let's go ahead and copy that. Let's paste that one in. All right. So the response returns. A confirmation of five successfully re index documents. Excellent. We can check the resulting mapping of our autocomplete index with the following command and this one short. So I'll just type it in. Curl localhost a200 slash autocomplete. Slash underscore mapping pretty. And we should see the mapping with the to search as you type fields and there they are. So we have genre and also title set to search as you type. Before we move further, let's make our life easier when working with Jason and Elasticsearch by installing the popular J. Q command line tool, we can do that with the following command sudo app two dash get install JQ. Type in your password. Continue. All right. And now we can start searching. We will send a search request to the search API of our index. We'll use a multi match query to be able to search over multiple fields at the same time. Why a multi match? Remember that for each declared search as you type field, another three sub fields are created. So we need to search in more than one field. Also, we'll use the ball prefix type because it can match the searched words in any order but also assigns a higher score to words in the same order as the query. This is exactly what we need in a autocomplete scenario. So let's search in our title field for the incomplete search query as to. That's what this command here does. You can see here we're just doing a multi match query for stay with the bull free prefix like we talked about against title. Title two, Graham and Title three Graham. Let's copy that. Back to our terminal. Pasted in. And off it goes. So you can see that indeed the autocomplete suggestion would hit both the films with the star turn in their title. We have Star Wars episode seven and also Star Trek Beyond. So that seems to work as intended. So now let's do something fun to see all this in action. We'll make our command interpreter fire off a search request for every letter we type in. Let's go through this step by step. First of all, define an empty variable. For every character we type will be appended to this variable. We'll call it input. Input equals so-called single quote. All right. Next, we will define an infinite loop. These are instructions that just repeat forever until you want to exit and press control C or command C, depending on what platform you're on. The instructions will do the following. And this is going to be back in our cheat sheet here under block five here. So it will read in a single character that we type in, it will append that character to the previously defined variable and print it so that we can see what we searched for. We then fire off that query request with what characters the variable contains. So far, that's what that query dollar sign input is doing. We then D serialize the response, which is the search results using the JQ command line tool we just installed earlier and grab only the field that we've been searching in which in this case is the title field. Then we print the top five results we receive after each request. Let's go ahead and copy that and see what happens. All right, so let's start typing in Star Wars. So I'm going to say capital S. Oh, cool. So we're getting all the things that have an S in it and now a T just like I'm trying to type in Star Wars. So we've already narrowed it down to Star Trek and Star Wars. A R still hasn't differentiated yet space and W and now we're down to the correct result. Notice how with every letter that we added, it narrows down the choice to star related movies. And with the final W character, we get the final Star Wars suggestion. This hit control C to get out of this. Congratulations on going through the steps of this lesson. Now you can experiment on much bigger data sets and you're well prepared to reap the benefits that the search as you type data type has to offer.
