# Sorting

Okay. Let's talk about sorting your results from Elasticsearch. Another very common thing you want to do, sorting by alphabetical order or numerical order, etc.. Normally it's pretty simple. If you're dealing with a field that is numeric in nature or something that's straightforward to search. All you have to do is stick sort equals. Whatever the field is you want to sort by at the end of your or I just like this. So in this example, if I want to sort the movies index and the movies that are in it by the release date, I would just say question marks sort equals year. After the underscore search query there and that's all there is to it. You'll get back a results sorted by the release date just as planned. It's pretty simple, right? But there are some nuances we need to talk about when you're dealing with strings. So here's the thing. If you have a text field like the title in our movie dataset, those are going to be analyzed for full text search so that you can do partial matches and get back fuzzy queries and things like that. You can't use that for sorting documents because the inverted index just contains the individual terms of that title. We can do partial matching, but the actual entire string as a whole is not being stored. So we can't sort by the actual movie title itself. All it has are the individual words that are in the title, and they're order stored in the inverted index. So sorting becomes tricky when you're dealing with analyzed text fields. Now there is a way around this. And here's a little trick that you can use. One thing you can do is set up a subfield that is not analyzed. So if you know that you need to do full text search on a field, but you also want to sort on the entirety of that field as well. You can keep two copies of it around. So if you look at what's going on here in this mapping, if you skip down to the title field here, we're saying that the title field itself remains as a text type. That means that it is analyzed for full text search. But we're also creating a field within that called raw, and that is being analyzed as a keyword, which, as you may recall, is not analyzed. That just draws a straight up copy of the title in the raw field. So what we end up creating here is both a title field that can be used for full text search and also a title raw field that you can use for sorting purposes or any other purpose that might require it on analyzed fields. Sorting is just one of them, really. So now that you have that in place, you can use that new raw subfield to do your sorting by. So if you want to sort by title and using the full title name, you could just say sort equals title dot raw, and that's all there is to it. Now, unfortunately, you can't change the mapping on an existing index, so this is something you need to think about before you import data into a new index. You need to get some thought over. Do I need to sort these text fields? And if so, do I need to keep a raw copy of them around? That's not analysed. Otherwise you would have to delete the entire index, set up a new mapping and re index all the documents all over again, which is possible, but something you'd rather avoid doing. So let's dive in and see how this works in action. Back to our virtual Elasticsearch server. Let's see. Sorting and action. Let's start by sorting by something straightforward like the release year. That's a nice clean numeric term and it should be easy to sort by. So to do that will say curl x gets single. Quote 1.790.0.1 call 1900 slash movies slash underscore search. This should just sort of flow out of your fingers. By now, I'm going to say a question mark. Sort equals year equals year and pretty thusly. So this should give us back the entire index sorted by release year, and fortunately it will automatically truncate those results to us so we don't get inundated. So let's see if that actually worked. Looks like we ended with 2016 and before that we had 2015, 2014, 2008, 1959. So yes, it worked. Cool. So sorting by a numeric field, pretty straightforward. All we have to do is say sort equals whatever the field is as part of the URI and we're good. But let's try searching by a text field, so let's try sorting by the title instead. Let's just do the obvious thing and change the year to title. Let's see what comes back and we get an error and it tells us in great detail why. But fundamentally, field data is disabled on text fields by default. It's telling you that you cannot sort on an indexed field like this on an analyzed field like this, because that title field has been split up into individual search terms. And really those terms in their order are all that Elasticsearch really has handy for sorting. So we can't sort by the title as a whole, unfortunately. One workaround is, as we've talked about, is to create a raw version of that field that is of a keyword type so it won't be analyzed and we can keep a copy of that full original title string for sorting purposes. And that might come in handy for other purposes too, as we'll see later in the course. So unfortunately, that means that we're going to have to re index this data. So we're want to blow away the existing index, remap it, and then re index it all. So let's just bite the bullet and get it done, huh? So let's start by deleting that index. Obviously, in production you want be more careful about this curl dash x delete 1.7.0.0.1 coordinate hundred slash movies. In the real world, you'd create the new index and delete the other one and switch over immediately. Right. All right. So we blew away the movies index, and now let's serve that new mapping so we can have that raw un analyze copy of the title around for that will say Curl Dash Exploit 1.7.020.1 cool and 8000 slash movies is going to be our index name and quote Curly Bracket. We'll set up mappings for this new index with the following properties. And for the title field, we will do the following. We will keep its type as text, meaning that the title field itself will still be indexed for full text search and give you back relevancy and going to the inverted index and whatnot. But under this title field, we are going to create a sub field like so. And we will call this sub field raw. And it will have a type. Keyword. So now titled Da Ra will have an un'analisi version of the string that we can use for sorting quotes off all those brackets. One, two, three, four, five, six. And that took. Now we need to re index our data with that new mapping so that will have those two different copies of the tile keyword. So hopefully you still have that movie star Jason bulk file around and we can just re-import it thusly. Curl dash x put 120 7.0.0.1 colon 1900 slash underscore bulk dash dash data dash binary space at movies dot json. And looks like it took. So everything looks successful from what I can see there. Now we can try that query again, but this time we're going to use Title Dot Raw. So let's try that again. We'll just say Curl X, get 120 7.0.0.1 colon, one slash movies slash underscore, search, and we will see a sought equals titled dot raw this time to sort by that under analyzed field and pretty and see if that works. And here we have results. So let's see if they're actually in sorted order by title. We'll go back up to the top here of what we got back. We started off with Dark Knights, Interstellar Plan Nine, Star Trek and then Star Wars. So yes, it did, in fact, give us back sort of two results in alphabetical order by the title. By using that title across a field we created that was not analyzed. So yay, it all works the way that I said it would. All right. Let's move on.
