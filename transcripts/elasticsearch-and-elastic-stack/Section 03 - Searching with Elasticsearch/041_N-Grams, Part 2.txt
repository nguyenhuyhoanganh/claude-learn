# N-Grams, Part 2

All right. Let's go ahead and play with Edge and Grams and see how that applies to search as you type. As we said, we're going to have to re index our movies data. So the first thing we need to do is delete the existing index curl. Dash X delete 1.7.0.0.1 colon 8000 slash movies. Next, we need to actually create that new analyser we talked about to build those edge n grabs and that we're going to apply at index time. So lots to type here, but let's just get through it. Start with a clean slate here. Curl dash x put single quote 127.0.0.1 colon a200 slash movies question mark pretty D and we will create the following settings. For analysis, we'll create a filter. We will call it autocomplete filter. There will be a tape edge and Graham with a min graham of one and a max Graham of 20. Meaning we can handle words from 1 to 20 characters in life. Close off that phrase of the filter and now will set up our analyzer. We will call it autocomplete. It will be a tape custom. Use the standard. Token nicer. And use the following filter definition. We're actually going to have two of them together. So we're going to use a square bracket to create a list here. It will apply a lowercase at index time so that we have case insensitive results. And we will also apply the autocomplete filter that we just created and that will apply our engrams, our edge engrams at the same time close off the square bracket. And now we can just close off all those curly brackets. Make sure you get the right count here. There's a one, two, three, four, five of them and quote and it took it. There's a lot that can go wrong there. There's a lot of typing to do. So let's test it out, shall we? There's actually a way to make Elasticsearch explicitly analyze a string. Well, let's try it out. Let's type the following kernel. Dash X get 1.7.0.0.1. Call 900. Slash movies. Slash. Underscore. Analyze. With pretty results stashed. Single quote. So what we're going to do is tell Elasticsearch to actually run that analyzer we just made and try it out on a given texturing. So let's say specify the analyzer we want to test out. That's going to be the one that we just made called autocomplete and we will ask it to analyze the following text. So imagine that we're indexing the term as to let's see what it does with it. All right, that's pretty cool. So you can see that it did apply the lowercase filter. So our upper case just got drawn into all lowercase s and the edge and Graham seemed to work, so we ended up with the following tokens out of that analyzer. One is just the unit, gram is the bi gram S-T and the tri gram stay so cool. That seems to be working as intended. So now we can apply this new analyzer to our title field and actually start using that for auto completion. So next we need to map this analyzer to our title field. So let's take care of that. Let's start with a clean slate here. Curl dash x put 1.7.0.0.1 colon 1800 slash movies slash underscore mapping with pretty results, steady single quote. And we will set the following properties as we've done before on the title field. We will make it a type text, meaning it is analyzed and we will specify the analyzer that we want to be, the autocomplete analyzer that we just made. Close out all those brackets. One, two, three. Looks like it worked. And now we can apply that as we re index our data. Curl dash x put 127.0.0.1. Coordinate 200. Slash underscore bulk dash data. Dash binary at movies dot json. All right. Seems to have gone in. So now we can finally try it out. Let's type in curled ex gets 120 7.0.0.1 9100 slash movies. Slash underscore search. Pretty dusty. And this time we will do a query where we are doing a match for the title to. So that should come back with Star Trek. Right? Hey, how come we got, like, plan nine from outer space and stuff? I mean, Star Trek did come back, but I got stuff that I didn't want to. So remember, we mapped the autocomplete analyser to the title field, and by doing so, I query time. Our query is also being split up into unit grams and by grams and tri grams and what not. So by searching for stay that included a search for just the unit grams, which came back with a hit for anything that has a search term that starts with this. To get around that, we're going to specify the standard analyzer on the query side and only use our autocomplete analyzer on the index side. So let's try to fix that. So let's do this query again, this time explicitly specifying the standard analyzer. So the syntax will be slightly different. We'll start off the same crawl x get 120 7.020.19 200 Slash Movies Slash Search Pretty nasty single quotes where we match title. But now instead of just saying Title SCA, we're going to give it a little bit more information. We'll say that the query for the title is stay and the analyzer on the query side must be the standard analyzer and not the autocomplete one that we set up for indexing. What's all that out? One, two, three, four. And this time it worked. So by searching for stay, we got a match on Star Wars and Star Trek, which is exactly what I want. Right. So as I'm typing in SCA, those are two valid auto completion suggestions. Very cool. Let's see, however, where this starts to break down. So let's say that these are kept on typing and instead of SCA they typed in CTA. TR sounds pretty obvious that they're looking for Star Trek at this point. Right? But what do we get back? Star Wars still came back as a result. Star Trek still the top result. So that's a good thing. But Star Wars is still in the running. Why is that? Well, again, this is an analyzed text field. So again, every search term is being treated independently. So we have a hit on Star because we have that try gram match of SGA. But even though TR is in matching anything in the Star Wars title, it's still relevant because we matched at least one search term. So that might not be the behavior necessarily one with a sort of a byproduct of how this approach works on the index side, using engrams. This is an example of where you might want to think about using completion suggestions instead, where you can have more fine control over how this all works. So there are going to be cases like this otherwise. But anyway, that's how engrams work in edge and grams and how you would apply them to a new index and how you would need to use both a separate analyzer on the index side for building those engrams as opposed to a different analyzer on the query side. So you don't end up splitting up your queries in the same way. Kind of an advanced topic here, but hopefully it gives you a little bit more depth into how Elasticsearch works and a little bit of a better understanding of how analyzers work on both the index side and the query side.
