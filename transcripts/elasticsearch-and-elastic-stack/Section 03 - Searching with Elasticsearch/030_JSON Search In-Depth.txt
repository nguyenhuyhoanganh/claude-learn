# JSON Search In-Depth

I hope so. Hopefully I scared you away from using your research in that previous lecture, but you will run across it once in a while. So it's important that you know what it is and how it works. But most of the time you'll want to stick with a request, a body search, where you're actually sending a JSON request as part of the request body to Elasticsearch. And we've seen a bunch of examples of this already in the course. So let's just talk about it a little bit more in depth and take a closer look at them. So here's the JSON equivalent of the request body search for that same thing we did in the previous lecture, just looking for movies that contain the search term star in the title. And you can see here the beginning of the URL looks the same. It's the actual hostname and port followed by the index, followed by underscore search. And I'm sticking pretty on there just to get back nicely formatted results. By the way, in production, you wouldn't necessarily need the pretty argument if you're just dealing with a response in a programmatic manner from an app or something that can just eliminate some overhead that you might not need otherwise. So only use pretty when you're messing around interactively as well. Now the request body is where the magic happens for the query itself. So you can see here that we have a more structured definition of the search query or defining a query block. And within that query block we have a match clause, and that match clause contains the actual match operation of matching the title field to the term star. So figuring out how to structure these requests can get a little bit confusing. Often you need to refer back to some examples or the documentation to get it exactly right. But at least when you look at it, it usually makes sense. And we generally don't have to deal with weird URL encoding because we don't have all those weird, cryptic characters to make up your searches. By the way, you might not have seen this before, but get requests can have a body. We've been kind of doing that throughout the course. But usually when you talk about get requests on CTP requests, we're talking about a web page being retrieved from a web server and usually there's no body with that request at all. But you actually can send a body along with a get request as well that is legal and sometimes that trips people up. It's a legitimate thing to do. Anyway, moving on, let's talk about the things you can do within a query. There's two different things you can do in a query. There are filters and then there are queries. So queries are usually used for returning data in terms of relevance. When we're doing something like searching for the search term track, you would use a query because you want to get back results in orders of relevance as to how relevant the term track was to that given document. However, if you have a binary operation where the answer is basically yes or no, you want to use a filter instead. Filters are much more efficient than queries. Not only are they faster, but the results can be cached by Elasticsearch, so that if you do another query using the same filter, they'll get the back of the results even faster. Let's look at an example of how that might work. So let's do that other second example that we did using your research in the previous lecture and see how you would do that using a request body search instead. So here we are again, structuring a query. And within that query we have a Boolean query, a bool. That means that you can combine things together and the equivalent of an and with a rule the inquiry is called must so by saying must term title track. We're saying that this query must contain the term track within the title to be a valid result. But we are going further to filter that result by having a range filter that contains the year greater than or equal to 2010. So again, the syntax is sometimes something you just have to look up, but this is what it will look like. For this particular example, the syntax does even change from version diversion. This is kind of the new way of doing it that they introduced back in Elasticsearch five, but it's easy to read. So if you have a look at it again, you can see we have a query that contains a Boolean expression where you must have track in the title and you must also have the filter past the condition of the year being greater than or equal to 2010. And those sorts of range comparisons. Muslims cells speed within a range query. So that's what that looks like. Now there are many different kinds of filters. Range is just one of them. You can also do term filters. So if you need to filter by some exact value of a term, you can do that with the term filter. It would look like term year 2014, for example, to filter out only things that contain a year that equals 2014. You can also do terms if you want to match anything in a list of values. So a terms filter could say in this example, anything that has a genre of sci fi or adventure is what that reads out to be. We already looked at range queries in the previous slide. If you just want to do a greater than or less than or greater than or equal to or less than or equal to sort of a thing, that would be a range filter. There's also an existing filter where you can just test if a given field exists at all within a given document. Again, the schema is only loosely enforced in Elasticsearch, so you can in fact have documents that are missing fields entirely. And if you just want to test if it exists or not, the existing filter is what you want and the opposite of exists is missing. If you only want to find documents where their given field does not exist, you could use a missing filter. And finally, there is a ball filter as well. We looked at the must example that's basically the equivalent of and there's also a must not, which is the equivalent of the Boolean operation not. And there's also a should, which is the Boolean equivalent of or so you can do complex Boolean filters where you can have conditions that must be combined with and relationships and not relationships and whatever you can imagine using a ball filter. There are different kinds of queries as well. Just like there's different types of filters, there's different types of queries. Match underscore all is the default that just returns everything. So if you don't specify any query at all, you get back everything. By default it assumes match underscore all. Typically you don't see that in use because it's sort of implicit if you have no query whatsoever. Match is the more common operation for a query where you're doing some sort of a relevant search for a given search term or terms. And you can also do multi match, which allows you to query multiple fields at the same time. There's also a bool query, which is the same thing as a ball filter. You can have, must and should and must not operations in there. The difference is that instead of filtering out results that don't meet the boolean condition, it actually scores the results by relevance. Now a reminder syntax queries are wrapped in a query block. Filters are wrapped in a filter block. And you can combine filters inside queries and you can combine queries inside a filter. Filters can combine to do more complex things, and that's exactly what we're seeing in this second example. Here again, we are wrapping a filter inside of a bully inquiry here to actually achieve the search query of I want things that have track in the title and have a year greater than or equal to 2010. So you can do some pretty complex stuff if you combine these things together in creative ways. Let's dive in and see this in action. Let's just run those queries that we saw in the slides here and see how queries and filters work in action. Clear out this mess. So let's start off with that simple one where we just search for any movies with the term star in the title. That's going to be a query with a match query for the title with the term star. We'll do that. So Curl doesn't get 120 7.0.0.1 coordinate 100. Slash the next name movies slash course score search question mark. Pretty nasty single quote. And now we will put in the body of our get request again, which is a legal thing to do in quotes query. So we are specifying a query as opposed to a filter. You can see these are little bits and pieces of the Jason requests are starting to make more sense now. And we're going to do specifically a match query. As we saw before, there are other types like match all for example. But this is a match query where we're matching the field title to the term star and we'll close out all those brackets and kick it off. So, yeah, we're going into more detail here about breaking down this actual quarry that we've kind of looked at before. Again, we're doing a search with a query, a match query in particular for the title with the term star. And it does in fact work. We got back all the movies with Star in the title. All right, let's do that. More complicated one where we introduce a filter into the mix as well. So for that will say curl dash X, get 1.7.0.0.1 call 1900 slash movies slash underscore, search, question mark, pretty dashed single quote. And again, we will do a query at the highest level here. And this will actually be a bool query because we're gonna combine together two different things. So we're going to have a bool query that is a must meaning an end relationship. It must have the term match the title track. All right. Now, in addition to the term I must having the title term track in it, we are also going to combine in our Boolean query that we are going to apply the following filter. And this filter will be a range type filter and we will be searching for the range on the airfield greater than or equal to 2010. All right. Close out the remaining brackets. And again, let's see if this works. I think it did. So it looks like we have, uh. Yeah. Star Trek films newer than 2010. There's only one of them in our database. Star Trek Beyond. So it works. Cool. Same thing we did before. Just in the more JSON structured syntax and understanding and a little bit more depth hopefully now about what's actually going on within that Jason syntax, specifically in those bodies. Let's move on to do some more complicated stuff.
