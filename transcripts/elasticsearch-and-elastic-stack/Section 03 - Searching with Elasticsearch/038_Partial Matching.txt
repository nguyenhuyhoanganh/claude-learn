# Partial Matching

Okay. Let's talk about partial matching in Elasticsearch. This will be a pretty easy lecture. It's just about what if you want to do a prefix match or some sort of a subset of a string that matches in your search terms. So prefix queries on strings work like this. So imagine, if you will, that we remapped the year field in our movies dataset to be a string value instead of a date. So now if you have a string value, you can just do a prefix query like this. You could just say query prefix, and then the field name followed by whatever prefix you want to search for. So in this particular example of where the prefix queries for the year 201, that would end up matching the years 2010, 2011, 2013, 2014, 2015, any string that begins with the characters 201 So a prefix query is exactly what it sounds like. You provide a prefix in any string where that prefix matches will come back as a search result. An elastic search is actually surprisingly efficient at doing this. It comes down to how the inverse index is stored and sorted, so it's actually pretty quick for Elasticsearch to do something like this. You'd be surprised at how fast a prefix query can return results. Another way to do it is using a wild card query so we can use the wild card syntax using the star character to represent a wild card. So, for example, a wild card query of the year one star would return a hit for anything that starts with a character one and has anything after it. So in this particular example, it would be the same thing as doing a prefix query. But that star can come anywhere in the string. So I could do something else, like for example, year 19, Star three, and that would match, for example, the year 1953, in 1963 and 1973 and so on. So that star can go anywhere in your string and it will match one or more than one characters that belong within where that star is. So that's another way of doing it. And if you know about regular expressions, there's also a RegExp query as well where you can specify a full regular expression for a given field as well. That can also be supported and works just fine. So I don't want to get into the intricacies of regular expressions because that's a whole other course. But if you are familiar with regular expressions, they are a powerful tool as well, and that is also an option for you in doing partial matching. So let's go see these in action. So let's play with partial matching. And again, we need to use string fields if we're doing partial matching. So the first thing we need to do, unfortunately, is remap our database again, our index again, rather to have the year as a text type instead of a data type. So let's get that out of the way. We'll start by blowing away our existing index curl dash x delete 120 7.0.0.1 call 1900 slash movies. And just like that, we're going to blow away our entire movies index again. It freaks me out how easy that is to do. Of course, there are security things you can do to prevent that. We'll talk about that later. And now let's put in our new mapping curl dash x put 120 7.0.0.1 colonised 100 slash movies will be our new index that we're creating a mapping for. Create the following mappings with the following properties. And we will map the airfield thusly with a type of text. One, two, three, four, five. And that took. Now we can re index our data with curl dash put 127.0.0.1 colon 8000 slash underscore bulk dash data dash binary at movistar. Jason. So now we've re indexed our data with the year being interpreted as a text field. So now we can do prefix matches on that field and see what happens. Let's do the following. Let's do a prefix match of 201 on the year and see what we get back. Curl dash x get c 120 7.0.0.1 colon 1800 slash movies. Slash Underscore Search. Question mark. Pretty nasty. And we'll set up a query. But instead of a match query, we're going to do a prefix query and the prefix we're looking for will be on the year fields matching the prefix to zero one. Close everything out and we should get back every movie in our database that starts with the year 201 something. And sure enough, Interstellar 2014, 2015, 2016 seems to have worked. Those older movies from the 1959 or whatever did not come back though, because it did not match that prefix. Pretty straightforward. That's how you expect it to work. Let's see how a wild card query works. So same example from the slides. So instead of a prefix query, we're going to do a wild card query and oops, we'll say year equals one star. So that should give me back pretty much everything because any year that starts with one should come back, but anything from the 2000s will not come back. See if it worked. Sure enough, plan nine from outer space came back and that is the only movie we have in our little database. That was before the year 2000. So our wildcard match of one star actually did the job. Pretty cool. All right, so that's all there is to it. Prefix matches and wildcard matches there as well. And again, array X matches are in there, too, if you have a familiarity with regular expressions in your toolbox.
