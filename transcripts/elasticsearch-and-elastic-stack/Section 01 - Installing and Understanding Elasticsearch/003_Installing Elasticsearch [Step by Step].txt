# Installing Elasticsearch [Step by Step]

Let's dive in and get Elasticsearch installed on your home PC so you can follow along in this course if you'd like to. Now, Elasticsearch is going to be running on an Ubuntu Linux system for this course, but if you don't already have an ubuntu system sitting around, that's okay. What we're going to show you is how to install VirtualBox on your Mac or Windows PC, and that will allow you to install ubuntu running right on your own desktop within a virtual environment. Once we have ubuntu installed inside VirtualBox, we'll install Elasticsearch on it, and after that, we'll install the complete works of William Shakespeare into Elasticsearch and see if we can successfully search that. So that's a lot to do in one lecture, but I'll get you through it. Let's talk briefly about system requirements. Pretty much any PC should be able to handle this. You don't need a ton of resources for Elasticsearch if you do run into trouble. However, make sure that you have virtualization enabled in your Bios settings on your PC, and specifically make sure that Hyper-V virtualization is off if that's an option in your Bios. But just try following along first. You shouldn't need to tinker with your Bios settings unless you run into problems. Also, be aware that the antivirus program called Avast is known to conflict with VirtualBox, so you'll need to switch to a different one or turn it off while using this course. If you're going to be using Avast Antivirus. With that, let's dive in and get this set up. All right, let's get a little Elasticsearch environment up and running so you can use it throughout this course. Now our strategy here is going to be to install ubuntu on a virtual machine on whatever computer you happen to have available. And then we're going to install Elasticsearch from scratch right on top of that. Now there are other ways of doing it. So if you do go to the official Elasticsearch documentation at elastico, there's the full URL. You'll see that there's actually installers for Linux, macOS or windows specifically if you want to run natively on your host system and more commonly in production, you might be using Docker. So if you do want to install in Docker, you can follow those directions to do so. Pretty simple to do, but I'm not going to assume that everybody watching this course already knows how to use Docker. What it's all about. Or you know that they even have it installed at all. So I'm not gonna go this way. But you know, in the real world, this is probably the way you would go. Instead, though, we're just going to set up ubuntu on a virtual machine, which we'll run on pretty much any computer you might have. So to get started, we first need some virtualization software, and a nice free one is VirtualBox. So head on over to VirtualBox and from there you can just click the download button to get the latest version. So I'm going to be doing this under VirtualBox seven. Choose the host system that you're going to be running it on, whether it's Mac or Windows or Linux or anything else. For me, that's going to be windows, and from there you can go ahead and download the installer for VirtualBox. Nothing unusual about it. So go ahead and run that when you've got that downloaded and you should see a setup wizard just like any other application, just run through it, get it installed and you'll be done with that step. So I'm going to go ahead and quit out of that because I already installed that earlier. All right. So now that we've done that we need to first get an image of ubuntu to install on that virtual machine. So to do that, we're going to head on over to ubuntu.com and download desktop and grab the latest long term support version. At this time it's 24.04 25 is on the horizon, so if you're feeling adventurous, you can go ahead and install that if you want to. But this is what we're going to be using throughout this course and what I've tested everything on. So to be safe you might want to stick with 24. Go ahead and download that image. Make sure you got plenty of hard drive space because it's about six gigabytes. They're going to want your information here. You don't have to give it to them at this point. It should be downloading already, but it is six gigabytes from a relatively slow download server. So I'm going to go ahead and pause the video while we wait for that to download. And then we'll come back and use that to set up our image. All right. You should have a giant ISO file six gigabytes or so sitting wherever your downloads from your browser go. And at this point we can now create a VirtualBox image using it. So assuming you've already installed VirtualBox from VirtualBox org, go ahead and open that up and click on new within VirtualBox manager application. and we need to give our image a name. So let's call it something descriptive like Elasticsearch course whatever you want. And we're going to go ahead and change the folder that contains it to someplace where you have plenty of hard drive space. So for me my H drive has loads and loads of room. So I'm going to go ahead and switch that there instead. The default location is fine if you have plenty of room on your default drive, that's okay, but I'm going to select for myself and for the ISO image, we're going to select the one that we just downloaded. So go ahead and click on other and under your downloads folder should be that ISO image that you just got. All right. So this is Linux ubuntu this is all correct and filled in ahead of time for us. So that's cool. Under unattended install we do need to set that up. So you need to have a default username and password. Make that whatever you want. Just remember you just make sure it's something you remember. Make sure your hostname has no spaces in it. And if you have an actual domain name, you can use it, but we're just going to be using this as localhost throughout the course, so no need to worry about that. Let's open up hardware and make sure we have enough memory. You do need at least eight gigabytes of memory here, so I have 32 gigs on my system. Let's go ahead and set that to somewhere in the middle, like 16. You just want to make sure it's in the green zone there. So you don't want to be in the red because that will be consuming too many resources. So similar thing for processors. You can get away with one CPU, but I'm going to set mine to four just to give it a little bit more oomph because I have 16 to spare here. Finally, under hard disk, we can set the default size of that virtual hard disk. Now, if you go through this whole course, you will need at a bare minimum, 30GB. So just to be safe, you might want to crank that up to 100 or so, assuming you have room on your drive in the drive that you selected for the file location. So I do have plenty of room on that drive. I'm going to set that, you know, to around 100GB. And that should be pretty much it. At this point. We can click on finish. That should go ahead and start that automatically. If not hit start to kick that off. And now the installation of ubuntu should just proceed automatically. Let's go ahead and wait for that to happen. All right. That took about ten minutes. But eventually you should see a screen like this with the default username that you set up when you set up this image. So we'll go ahead and click on your username there. And the password that you entered earlier. Hopefully you remember what it was. And we did install the desktop version of ubuntu here. That's that's fine. We're just going to use this to make life a little bit easier for us while we install Elasticsearch. But do keep in mind that after we do this, we will be using this as if it were a server running on the cloud somewhere. So for the remainder of the course, we're mostly going to be just hitting this through a terminal from our local machine here. And it'll be just like using it in the real world. All right. Go ahead and go through these little introductory notes or whatever. Yeah we can skip all this stuff. Just whatever. Fine. Finish. All right. So first thing we want to do is get a terminal prompt. So under here this is the app's menu if you will. From here you can select terminal. And it might be a good idea to pin that to the taskbar here too. So let's pin that to the dashboard so we don't have to go searching for it again. And from here I'm going to use a little cheat sheet that I gave you to make life easier. So if you go ahead and click on Firefox here, instead of typing everything by hand, you can just copy and paste using this handy dandy website I'm about to give you. So just head on over to media.com/install txt and that should give you this page here. So this will give you all the steps for installing Elasticsearch on your system here. Let's start by getting our public sign in key so we can actually download the packages we need. So I'm just going to go ahead and copy that and paste it into our terminal. Boop. Need to authenticate first with that same password that you set up earlier. And with that out of the way, we can go ahead and install the APT transport Https package so we can make sure we can install this stuff from the repository. Again just copy paste that into the terminal. And say yes. Let's go ahead and tell ubuntu where these packages for Elasticsearch live with this next line. It's just saving the repository definition to ubuntu. That didn't take a second. And now that we've done all that, we can actually go ahead and install Elasticsearch. Now before we do that, let's go ahead and make sure that ubuntu itself is up to date. So we want to do sudo apt update ampersand ampersand sudo apt upgrade y would be a good idea. Because a lot has come out since that initial release of ubuntu 24. This will take a little bit of time. So I'm going to go ahead and pause and come back when that's done. All right. That didn't take too long. So let's also do a sudo snap refresh to finish our update here. And it wants to restart but we can put that off for now. We'll say restart later for the time being. That's fine. Now we can continue with installing Elasticsearch itself. So we can see here. That's pretty easy. We're just going to say sudo apt get update and install Elasticsearch. We already did an update, so that's a little bit redundant but it can't hurt. And off it goes. Currently getting the latest package which as of this recording is Elasticsearch nine. And give that a minute or two. All right. That didn't take too long. So what we've done is installed the open source version, the free version of Elasticsearch. It's just to Elasticsearch. We haven't installed any of the other components of the stack yet, like Filebeat or metric speed or Kibana or anything like that. We're starting with Elasticsearch itself, but to finish that we do need to configure it first. So kind of going back to our cheat sheet here, what we're going to do is go ahead and edit that configuration file which lives under Etsy elastic Elasticsearch Elasticsearch. So let's go ahead and edit that. And if you're not familiar with Nano, it's a pretty simple editor to use. If you prefer VI or something, that's fine, I don't care. I won't judge, but it's pretty straightforward. So we're going to go down to node name. Go ahead and uncomment that and we'll call our node here node one. Later on in the course we'll simulate running multiple nodes and doing failover between them. So that's where that will come in handy. Network host is the next thing we want to look for. There it is. And just to get around some uh, security warnings, we're going to set that to 0.0.0. Quick note about security. We're not going to use any because we're using this locally for this entire course. We're just going to be accessing Elasticsearch service running on our local machine. So, um, no need to worry about security in the context of running this course for learning purposes. So I will call out situations where you'd want to do things differently in the real world where security matters. A network dot host, we took care of that. Next is discovery dot c dot hosts. We want to find that one. Where are you? There it is. And we want to change that to a single entry in quotes, which is 127.0.0.1. And I remember seeing down the bottom here x dot security dot enabled. Again, since we're just running this locally, we're not going to worry about security because that introduces a whole lot of complexity you don't need to deal with while you're learning quite yet. We're going to go ahead and set that to false. All right. And we'll go to cluster dot initial master nodes if we can find that one. There it is right there. Remember we named ourselves node dash one. So we're going to change that to node dash one. All right. That should do it. So let's go ahead and nano. You're going to say control O to write that out to disk and control X to get out of the editor. Next thing we need to do is set the file permissions needed to read the logs for Elasticsearch. For that we're just going to say change mod. Again. You can copy and paste this from the cheat sheet if you want to. Actually it's sudo change mod 755 dash uppercase r slash var log slash elasticsearch. Take care of that. And finally we want to make sure that Elasticsearch will start automatically. And it is starting up while we're playing with it here. To do that we're going to say sudo slash bin system control daemon reload to make sure it knows about our new service that we just installed. And now we'll kick that off with sudo bin system control enable Elasticsearch service. So now we will actually start Elasticsearch with sudo bin system control. Start Elasticsearch dot service. And now Elasticsearch should be up and running. Let's go ahead and do a status check just to make sure. So instead of start I'm going to change that to status. It says it's running. That's a good sign. All right. So now in order to communicate with Elasticsearch we need to install Curl. That will allow us to send HTTP requests to our Elasticsearch server. So we're going to be interacting with this just like you would from some RESTful interface from an application somewhere. We're going to do that through most of the course. First you need to install curl. So sudo apt get install curl. I've already done that. But for you that will actually do something more interesting. And at that point we can use curl to test this thing. So let's say curl. And let's go ahead and send a get request with dash X get all uppercase. We're going to send that to the localhost on 127.0.0.1. Colon port 9200. That is the port that Elasticsearch runs on by default. And if everything is done correctly, you should have got something back like this where it says at the bottom, you know, for search. And that's telling you that Elasticsearch is in fact up and running. It's responding to requests. And that is pretty awesome. So let's do something a little bit more interesting with it, shall we? So what I'm going to do, just to make life more interesting in this lecture, is to install the entire complete works of William Shakespeare and do a search on that using the Elasticsearch server that we just set up. So back to our cheat sheet here. If we just copy this line here that will go ahead and download the complete works of William Shakespeare in JSON format, in a format that Elasticsearch can import pretty easily. So go ahead and paste that in. Just getting it from my own server now with this particular file is is the mapping for the data itself. It's not the actual data. So if you take a quick peek at it, you can see it's just saying this is the structure of the data that I'm about to install. So every line has a speaker, a play name, a line ID and a speech number associated with it. So let's go ahead and submit that to Elasticsearch thusly. Go ahead and paste that line in real quick. What's going on here. We're saying we're going to use curl again. And we're going to say we're passing JSON format data. And we're going to use a put action here to tell it that we're putting data into it. We're going to give it a index name of Shakespeare. That's what that Shakespeare indicates. And then dash dash data binary with that at sign says this is the data that we're actually importing. So let's go ahead and kick that off. And you should have got back an acknowledge true response. And shards acknowledge true. And the new index that was created is indeed called Shakespeare. So now it's time to get the actual data itself again. You can just get that from our server here. Copy that from the cheat sheet, paste that in and go ahead and download that data again pretty quick. Uh, Shakespeare wrote a lot, but you know, it's not a big deal for a computer. Let's take a quick little peek at what that looks like. Shakespeare 90. JSON. And you can see that this is standard JSON line format, where every line indicates a line from one of Shakespeare's plays. So let's go ahead and import that next. That's what this next command does. Go ahead and copy that line index data to Elasticsearch. Paste that in and we can see what's going on here. Again we're using curl with a put command in JSON format going to the localhost on port 9200 into the Shakespeare Index. And we're going to use a bulk command, underscore bulk, to index all the data in this JSON file that we're specifying with data binary. Go ahead and import that. And what's happening right now is we're actually indexing all of that data from the complete works of William Shakespeare into Elasticsearch. And it finished before I could even finish that sentence. So let's see if it works. We're going to try to do a Get request on the server. Now to do a search for to be or not to be and see where that line appears in our data. Let's go ahead and copy that, paste it in and let's break this down again. We're using curl to simulate a Rest command here. JSON is the format that we're working with. Later on we'll give you a shortcut so you don't have to type that every time doing a Get request, because you want to retrieve data from our local host on port 9200 from the Shakespeare Index. This time the command is underscore search to search that data. We want a pretty format. And the structure that we're passing in for that search query is a query with a match phrase looking on the text entry field of. To be or not to be. So let's see if we can find that phrase anywhere. Hit enter. Sure enough, it's from hamlet. Speech number 19, line number 3.1.64. Yada yada yada. Right. So there you have it. We actually installed an index, The Complete Works of William Shakespeare into Elasticsearch, which we set up from scratch. You know, we went ahead and set up our own Ubuntu Server instance. We installed Elasticsearch on top of it. We indexed some data. We query that data. We're up and running. That's awesome. Great job. All right. So the dirty work's done. Now we can start playing with this system. If you're done for now though, go ahead and close out of that terminal. Close out of the browser and to shut this down. When you're done, you can just go to the little power icon here. Probably the simplest way to do it and say power off. Power off. And once that shuts down, you should get kicked back to the VirtualBox manager screen. And that's it. All right. Let's keep on going and do some more fun stuff.
