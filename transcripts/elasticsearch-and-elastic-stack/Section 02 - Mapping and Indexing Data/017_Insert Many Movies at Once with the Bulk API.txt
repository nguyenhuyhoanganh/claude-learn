# Insert Many Movies at Once with the Bulk API

So importing one movie was all well and good. And sometimes you do need to do that. But often you'll also have to import a bunch of movies or documents at once. Maybe you're importing some existing data set, or maybe the entire movielens dataset all at once, for example. So let's see how that works and how to import many documents at once over Rest queries and using the JSON format. Well, this is what that would look like. So this is the bulk import format that Elasticsearch uses. So we start off by specifying the Elasticsearch service endpoint there, the IP address and port that we're working on followed by underscore bulk and then dash D for the body of that request. And this is what that format looks like. Now the Elasticsearch documentation says that it's kind of a funny format, but it doesn't look that bad to me. It makes sense. Basically, for every document that we insert, we start off with this create line that indicates the index name that we're inserting this data into, and the ID of the document that we're creating. That's followed by information about the document itself. In this case that includes the ID followed by the title year and list of genres for each movie. and we're going to insert the movie Star Trek Beyond, Star Wars Episode seven, Interstellar and The Dark Knight, and Plan nine from Outer Space all in one request. So it's pretty straightforward, right? One thing that's worth understanding is why they made the format the way it is. So the reason we have all these individual lines here broken up this way, instead of just one giant self-contained JSON request, is because if you remember, Elasticsearch will hash every specific document to a given shard, right? So Elasticsearch needs to be able to deal with these individual documents one at a time. It needs to be able to go through each line and say, okay, I've got movie ID 1355569 here. Which shard should that actually map to? And then it can go off and just hand off that line to the correct shard in our cluster, and then it can process the next line and say, okay, this is going to go to this other shard and this other node instead, and so on and so forth. So this format where things are broken up into these groups of two lines, allows whatever Elasticsearch server you're actually sending this to to say, okay, this is the shard that I need to be dealing with, and I'm going to send this document to that shard, and it just processes it through one line at a time instead of trying to do the whole thing at once. So that's kind of the motivation of this format here. Makes sense when you think about it. It's not that not that crazy. So let's go ahead and run this and see what happens. All right. Now don't worry I'm not going to make you type in all that stuff by hand. We're going to do this the easy way. So again I have started up my Elasticsearch server in VirtualBox and connected to it with putty here, and I'm ready to go. All logged in. All right. Let's grab that JSON file with wget http colon slash slash media.com/9.json. And that should come right down. Let's take a look at what's inside there. Cat movies dot JSON. And sure enough that's the same file that we looked at in the slides right. So we have these groups of two lines where the first one says what index you're going into and what movie ID and after that we have the actual information for the document itself that we want to insert. And when we send this to our specific host on our cluster for Elasticsearch, it will go through that one document at a time and figure out where does this need to go and send that off to the appropriate shard. Now obviously we only have one shard running in our little desktop here, but imagine, if you will, we have a larger cluster and this makes a lot more sense. And let's go ahead and import this with slash user slash bin curl. And we need to use a application JSON format for this particular data. So we can't just cheat and use application JSON. So we're going to say dash uppercase H to specify that content dash type header colon application slash x dash and JSON. Make sure you get that right and export, because we're going to put some data in 127.0.0.1 to our localhost on port 9200 slash underscore bulk question mark pretty dash dash data, dash binary and then app dot JSON in order to get that file imported. Let's kick that off. Looks like it worked. I see a bunch of created results here, so, um, looks promising with the exception of a version conflict on interstellar because we already inserted that one, right? So, uh, that's actually expected behavior there. We can't insert the same document twice. We can update them though, and we'll look at that in the next lecture. But let's go ahead and get this back and see what we got back for results. Again, we'll do a query to search the entire index. So curl dash x get 127.0.0.1 colon 9200 slash movies. The name of our index slash underscore search question mark. Pretty. And that empty search request just says give me back everything. And there we have it. So there is our JSON formatted results from our search query that includes plan nine from Outer Space, The Dark Knight, Star Wars Episode seven, interstellar, still in there from before, Star Trek Beyond, and yeah, it's all there. Cool. So it worked. We were actually able to bulk import a bunch of movies all at once from a pre-generated file that we made, and you don't necessarily have to make that file by hand either. As we'll see later, it's possible to just write a script to generate that. One way of importing all that data into Elasticsearch is to write a script to translate that information into that bulk insert format. But there are other ways to. We'll get there though. For now, though, we're just trying to get some data into Elasticsearch so we can play with it throughout the rest of the course. So stick with it and we'll keep on playing.
