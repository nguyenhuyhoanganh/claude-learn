# Using Analyzers and Tokenizers

Okay. We've talked a couple of times in this course about analyzers, so let's go into a little bit more depth about them and how they can help you to control how your text fields are searched. Now you need to make a decision every time you have a field that contains textual information. You can define it as one of two types, and that determines whether or not your text fields are searched as an exact match or if they're analyzed, meaning that they can be partial matches that are ranked by relevance. Now, if you do want a text field to be an exact match, only you use what's called a keyword type. When you're defining your mapping for your index and that will suppress analyzing for that text field entirely. It will only allow exact matches on a keyword field. So by defining a keyword mapping on a text field, that means that the only return you're going to get on a search on that field will be an exact match. Case sensitive and on all what's in that field. Now, if you want to do partial matches, then you need to choose the text type of matching instead. And that will allow what we call analyzers to be run on that field. Then you can specify what analyzer you want to run on that field. If you're using a text type and there are different analyzers for specific languages, for example. They can do things like make your results for search case insensitive. They can handle stemming, for example, dropping the anger or the edge or the s suffixes on your words and making all those the equivalent from a search standpoint. You can remove stop words like it and add in the you can even apply synonyms and have those treated equally in a search like we talked about earlier. Also on an analyzed field, a search with multiple terms doesn't need to match all of them. If you just have one of those terms, match what's in a text field, it'll come back as a potential result, maybe with a lower, less relevant score, however. So let's dive in and see how this all works. It's best illustrated with an example. So let's just go through a few different examples and get a hands on feel of how the text and keyword field types work and how analyzers work as well. So let's start by doing a search for any movies that have Star Trek in the title. Let's try that. Curl Dash X get 120 7.0.0.1. Coordinate 200 slash movies. Slash underscore search. Pretty results that quote unquote curly brackets, query colon, curly bracket. And we want to match the following title. For Star Trek. Close off those brackets and off it goes. Hmm. Interesting. So we got back to both Star Trek and Star Wars movies as a result of that query for Star Trek. And again, the reason is because when we have an analyzed text field like our titles here, we can actually have partial matches come back. So even though only the words star matched. Star Wars still came back as a result for that, even though we also had trek in the search terms as well. That's how analyzed fields work. However, the score was a little bit lower, so you'll notice that the score for Star Trek Beyond was 3.018, while the score for Star Wars was only 0.87. So, you know, alas, a search just tried to give you. It airs on the side of caution. It gives you more results than you might potentially want through those partial matches. Right. But the most relevant the best matches will be on top of your results. So that's a good thing. So by searching for Star Trek, we got Star Trek Beyond as a top hit, but also Star Wars, because that was a partial hit on the search terms within Star Trek. Again, the search term that we put in Star Trek got broken up into two unique search terms. And looking at the inverted index for the index that mapped back to Star Wars and Star Trek, because those both match the term star at least. All right. So how about genres? How does that work? Do we really get that same behavior there as well? Let's find out. Let's say Curl X gets 127.0.0.1 colon a200 hundred slash movies slash underscore search. Pretty easy. We'll do another query. And this time we're going to say, match the following phrase, please. We want the genre to match the phrase sigh and you would think that match phrase would be a very specific thing. Right? Let's see what happens. In fact, that did pick up all the genres sci fi, even though they search for genre lowercase C.S.I. Again, that's because these genre fields are analyzed. So it's actually going to be normalizing a thing out. It sees that dash and says, these are actually two distinct search terms that I can search for independently from each other. I lowercase do everything to normalize that data. So uppercase are lowercase ci doesn't matter. Is treating it just like the title. Right. So partial hits are okay. Things that aren't quite an exact match are okay as well. So it's still even though that we said we wanted sci and not sci fi, the genre fuel is also being interpreted as a text field that's being analyzed and has, you know, looser constraints about it when you're searching. But I really want that behavior. I want my genres to be more tightly defined. I'm not looking for that kind of activity there. I just want a hit for the actual phrase sci fi and the genre side does not exist. So I would not expect to get any results back for that. So we can fix that. Let's start by just blowing away our index and recreating it, because unfortunately, you cannot change these mappings in most cases once they're in place. But no big deal. We can recreate this thing easily enough. Let's start by blowing away the entire index like so. Curl dash x delete 120 7.0.0.1. Colon 1800. Slash movies that will delete the entire index, which is maybe a little bit too easy to do. But here we go. It's gone. All right? The entire index is gone. So, yeah, let that sink in for a bit. That's easy to do and easy to do by mistake. Even so, make sure you have backups, guys. All right, let's define a new mapping that has the actual behavior that we want, and then we can re-import re index our data with that new mapping in place. So let's say curl dash x puts 120 7.0.0. One coordinated 100 slash movies is going to be our new index dash de hoop and open curly brackets. With the following mappings assigned to this index, we will have the following properties of this mapping. Feel properties in specific. So we'll start off with the ID field and we will say that will be of type integer. Karma. This year, field will be of type. Eight. So far nothing's changed. But now for genre, we're going to say that's going to be of type keyword and that means that we are only going to do exact matches on that. No analyzer will be run on the genre fields at all. If you want to get back search results on the genre, it's going to have to be an exact match. Case sensitive, the whole works. Finally, we'll specify the title mapping and we'll leave that as a text type so that that actually will have an analyzer applied to it. And we can do things like partial matches, you know, normalizing for lowercase and uppercase synonyms, things like that. We can also specify the specific analyzer that we want to run on that text field by saying Comma analyzer. English to apply the English language analyzer. That way you can apply stop words and synonyms that might be specific to the English language. Close that off and close off the other three curly brackets. For curly brackets, rather. And that should be good. All right. They came back successful, so now we can re index our data. To do that, we can just say curl dash x puts 120 7.0.0.1 call 1900 slash underscore bulk question mark pretty dashed ast data dash binary. And hopefully you still have this file sitting around. Movie star Jason after the assign. So that will take our bulk import file there and everything is back in. So let's try some more searches and see how it works. Now, let's try that one that we tried earlier of the searching for Cy for the genre. Let's say curl dash x gets 120 7.0.0.1 call 1900 slash movies slash underscore search question mark pretty dash dashed quote curly brackets. I do another query matching the following terms where a genre matches lowercase cy. So this worked before because that was an analyzed field, but this time we got back nothing. So that's exactly where we want it because that is now a keyword field. Only an exact match will work on the genre now. So let's try changing that to sci fi. You think that's going to work a little bit? A trick question? It didn't. The reason is because it has to be an exact match and that means that it has to be case sensitive. Even so, let's fix that. Capitalist's Capital F now we got some hits. So if we search for the specific genre sci fi and it's an exact match because that is a keyword field and on analyzed now it's working for us. Let's try searching for a title as well and just make sure that still works as intended. So I'm going to change the genre to title Star Wars lowercase. Let's see what happens. And as expected, we're getting both Star Wars and Star Trek movies, but the Star Wars results are going to be more relevant because we are analyzing that title field. So partial matches are okay. Case doesn't matter. I typed in lowercase Star Wars. No big deal, but the Star Wars movie did come out on top with a much higher score than the Star Trek movie. So it works as advertised. That's very cool. So again, the moral of the story, if you want a field to be analyzed, make sure it's a text field and that will allow you to do partial matching and be a little bit more forgiving on your search results. But if you do want exact matches for search terms, make sure you make your text fields a keyword field instead. All right. Let's move on.
