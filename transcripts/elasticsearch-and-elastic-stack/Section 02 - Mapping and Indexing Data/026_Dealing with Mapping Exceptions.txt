# Dealing with Mapping Exceptions

Mapping is an essential foundation of an index that can generally be considered the heart of Elasticsearch. So you can be sure of the importance of a well managed mapping. But just as it is with many important things, sometimes mappings can go wrong. We'll take a look at various issues that can arise with mappings and how to deal with them. Before delving into the possible challenges with mappings, let's quickly recap some key points about mappings. A mapping essentially entails two parts the process a process of defining how your JSON documents will be stored in an index and the result the actual metadata structure resulting from the definition process. If we first consider the process aspect of the mapping definition, there are generally two ways this can happen. An explicit mapping process where you define what fields in their types you want to store along with any additional parameters. A dynamic mapping Elasticsearch automatically attempts to determine the appropriate data type and updates the mapping accordingly. The result of the mapping process defines what we can index via individual fields and their data types, and also how the indexing happens via related parameters. Consider this mapping example here. It's a very simple mapping example for a basic log collection microservice. The individual logs consist of the following fields and their associated data types. The timestamp of the log is mapped as a date service name, which created the log is mapped as a keyword IP of the host on which the log was produced is mapped as an IP data type, port number is mapped as an integer. The actual log message map is text to enable full text searching and more as we have not disabled the default dynamic mapping process. So we'll be able to see how we can introduce new fields arbitrarily and they will be added to the mapping automatically. So what could go wrong? There are generally two potential issues that many will end up with facing with mappings. If we create an explicit mapping when fields don't match, we'll get an exception if the mismatch falls beyond a certain safety zone. We'll explain this in more detail later in the exercise. If we keep the defaults dynamic mapping and then introduce many more fields, we're in for a mapping explosion which can take our cluster down. Let's continue with some interesting hands on examples where we'll simulate the issues and attempt to resolve them. Let's get back to the safety zone we mentioned before when there's a mapping mismatch. We'll create our index and see it in action. We are using the same exact mapping that we saw earlier. And to save you some typing, I've uploaded some of the larger commands in this exercise to the web for you. So just head over to media that some dog tasks Ofcom slash s slash exceptions dot text and you'll see this cheat sheet that you can just copy and paste from. So we'll start by creating our index. We're on a call it microservice dash logs containing the following properties. And although we're defining the port as an integer type, that will be important later on. Going to go ahead and copy that. And back to our terminal and right click to paste. All right. Now a well-defined JSON log for this mapping would look something like this and block to note that the port is defined as a integer one, two, three, four or five, just like it should be. But what if another service tries to log its port as a string and not a numeric value? Notice that the port is in quotation marks here. I mean, that's actually a string containing the string. 15,000. Well, let's try it out. Copy that. And pasted it in. Great. It actually worked without throwing an exception. This is that safety zone that I mentioned earlier. But what if that service law does string that has no relation to numeric values at all into the poor field, which we earlier defined as an integer? Well, let's see what happens then. So on this one, our message is I am not well because the port is actually the string none. That's not a number at all. Well, let's see what happens. Copy that. Number format section under a map or parsing exception. Hmm. So we're now entering the world of Elasticsearch mapping exceptions. We've received a code 400 and the map or parsing exception that is informing us about our data type issue, specifically that it failed to pass the provided value of none to the type integer. So how do we solve this kind of an issue? Well, unfortunately, there isn't a one size fits all solution. In this specific case, we can partially resolve the issue by defining a ignore or malformed mapping parameter. Now keep in mind this parameter is non dynamic, so you either need to set it when creating your index or you need to close the index. Change the setting value and then reopen the index, which is what we're going to do right now, something like this. So let's run the commands in block five here, one at a time. First, we'll close our index. And then we'll set index mapping ignore malformed to true. And will reopen that index. All right, so now let's try to index that same document again. That's what's in BLOCK Six here. All right. That one actually worked. Now, if we check the document by its ID, it will show us that the poor field was actually omitted for indexing, and we'll see it in the ignored section. Let's see how that works. First, we need to copy that ID that we got back after inserting it and we'll type in kernel ATP. Local Host 9200 slash microservice dash logs. Slash underscore doc slash right click to paste in that ID question mark pretty. Single quote. So note here, it's telling you that the port field was ignored due to that rule. Now, the reason this is only a partial solution is because the setting has its limits and they are quite considerable. Let's reveal one of the next example. A developer might decide that when a microservice receives some API request, it should log the received Jason payload in the message field. Now we already map the message field as text and we still have the ignore malformed parameter set. So what would happen? Well, let's see. We'll copy block seven here. That is putting some JSON data within the message. Let's get a clean slate here. And we got an error. So we see our old friend the mapper parsing exception. This is because ignore malformed can't handle JSON objects on the input, which is a significant limitation to be aware of. Now when speaking of JSON objects, be aware that all the mapping ideas remain valid for the nested parts as well. Continuing our scenario after losing some laws to mapping exceptions, we decided time to introduce a new payload field of the type object where we can store the JSON at will. Now remember, we have dynamic mapping in place so we can index it without first creating its mapping. Let's go ahead and try that. See, we have a payload field now that contains that JSON data. All good. All right. Now we can check the mapping and focus on that payload field. To say, Colonel. That's his request. Gets a copy. Local host 1800 microservice dash logs slash underscore mapping pretty. And let's find that payload field. There it is. So it was mapped as an object with sub properties defining the nested fields. So apparently the dynamic mapping works, but there is a trap. The payloads are and generally any JSON object in the world of many producers and consumers can consist of almost anything. So you know what will happen with different JSON payloads, which also consist of a payload dot data that received field but with a different type of data. Let's try that with BLOCK nine here. Well, you see, we're just sending a slightly different payload here. And again, we got the map or a passing exception. So what else can we do? Well, engineers on the team need to be aware of these mapping mechanics. You can also establish shared guidelines for the log fields. Secondly, you may consider what's called a dead letter queue pattern that would store the fail documents in a separate queue. The C there needs to be handled on an application level or by employing log stache DL. Q Which allows us to still process the failed documents. Let's clear and start with a first slate here. So now the second area of caution in relation to mappings are limits. Even from super simple examples. With payloads, you can see that the number of nested fields can start accumulating pretty quickly. Where does this road end? Well, at the number 1000, which is the default limit of the number of fields in a mapping. Let's simulate this exception in our safe playground environment before you'll unwillingly meet it in your production environment. Let's start by creating a large dummy JSON document with 1001 fields, post it and see what happens. So to create the document, we're going to use the example command below with the JQ tool. And if you don't already have JQ installed, you'll have to do that with sudo apt dash get install JQ. And once you have that, you can create the JSON manually or if you prefer. This is a little bit easier, actually. A lot easier. Just go to BLOCK ten here and we'll set up a variable called thousand and one Fields Jason that contains the following stuff using JQ. Copy. Based. And you can see all this is doing is echoing that 1001 times to that environment variable and we can echo that to take a look at what's in it. Oh, yeah. 1001 things. So we can now create a new plane index with a curl that says a location request to put. A sleepy local host 1900 slash will call this one big dash objects. And we'll post in our generated JSON. Big dash objects. Underscore. Duck. Poshmark. Pretty backslash. Dash. Data. Dash. Raw. Quote. Dollars 9001. Fields. Jason and I will import that the contents of that into our big objects index. And you can guess what happened. We went straight to the illegal argument. Exception. Exception that informs us about the limit being exceeded very explicitly. So how do you handle that? Well, first, you should definitely think about what you're storing in your indices and for what purpose. Secondly, if you still need to, you can increase this 1000 limit. But be careful. As with bigger complexity, you might come a much bigger price of potential performance degradations and high memory pressure. Changing this limit can be performed with a simple, dynamic setting change. We can just say curl location. Request put HP local host 1900 slash big dash objects slash underscore settings. Data raw. It would be index mapping, not total underscore field start limits. And we could set that to 1001. And that would get around that particular issue. All right. So now you know that you're more aware of the dangers lurking within mappings and you're much better prepared for the production battlefield.
