# Import a Single Movie via JSON / REST

Okay. So we have a mapping defined within our movie index. So let's go ahead and show you how to actually import a document into that index. And for the sake of illustration, we'll import one of my favorite movies, Interstellar, and the information associated with it. So here's what the format looks like for that request. And again, I just want to back up a little here. We're using the Curl Command just for the sake of illustration. It's an easy thing to use while we're messing around and learning. But remember, in the real world, probably you won't be using Curl to do this sort of stuff. Usually we're going to be doing something like interfacing with an Elasticsearch cluster from some application that you're writing. So in the real world, you'll probably be issuing arrest queries over HTTP from Java or some web framework that you're developing within or some application that you're writing. We're just going to use Curl here and we're going to create a put request and we're going to send that to 120 7.0.0.1 colon 9200. Of course, you would be sending that to the hostname of the actual endpoint for your real Elasticsearch cluster in the real world. For us, that just happens to be how we reach it on our desktop. Followed by that, we have Flash movies, which is the name of our index, and we'll follow that in turn by the unique document ID that we're assigning to this movie. It turns out that in the movie lines data set, the movie ID for Interstellar is 109487, so we'll stay consistent with that. Next, we need to specify the body of this request and to do that from the Curl command line, we say Dash D and then a single quote which allows us to start typing multiple lines of the query in JSON format that we want to issue. Again, this is a put request or we're telling it to add the following information into our index. We start off with the genre, which is going to be a list of genres assigned to this movie, and we're going to specify that list as square brackets containing quoted strings within it, separated by commas. So Interstellar will belong to both the IMAX and sci fi genres. We're giving it the title of Interstellar, and the release date was 2014, which is going to be in date format later on. So let's go ahead and type that in and see what happens. Okay. So I've gone ahead and started up my virtual Elasticsearch server within a virtual box and waited for that to spin up. And then I connected to it using putty on my Windows system. So we're all logged in here and ready to go. Now I'm going to spare you some trouble here. We're going to take a little diversion here to actually set up a little script that will save us from having to type in the content type headers every time that we use the curl command. So let's get that out of the way first. Okay. We're going to make basically our own version of Curl that will save us some typing. So first of all, make sure that you're in your home directory here, which is probably home student and we're going to create a bin folder within that with the Durbin command. And let's see the into that change into the bin directory and let's create our own curl script here. Let's use the nano editor because that's a little bit more friendly nano curl like that. And now we're in the Nano Text editor and we can just start typing. So we're going to build a bash script that wraps the built in Curl Command, and this automatically adds in that content type header for us so we don't have to keep typing it in. So type in the following hash, exclamation point, slash bin slash bash, and then we'll say slash user slash bin slash curl bash h double quote content slash type colon application slash JSON quote and then quote dollar sign at sign quote. All right. That's all there is to it. So all we're doing is making our own little kernel script that calls the existing curl script, but puts dash age content type application JSON at the beginning of every request for us automatically. So that's just going to save us a lot of typing later on in this course. Let's write this out. We'll hit a control. Oh, to write that out and then hit enter to accept the file name and control X to exit out of the editor. Now we need to make this executable. So to do that will say change mod a plus x to grant execution permissions to everybody curl just like that. All right. Let's see if it works. Let's go back to our home directory, CDD, Tilde. And now to pick up that change, we're going to say source dot profile. And now that should put our new curl into action and that will automatically happen every time that we restart this machine as well. So let's make sure we're picking up the right one. Let's say which curl. And sure enough, curl is now pointing to the one in our home directory because there is an entry in dot profile that says anything in the bin folder of our home directory should take precedence over anything else. So now when we type in curl, it will automatically append that header type to to it for us, which is super handy. All right. So now that we have that out of the way, let's actually start using curl and import that single movie for Interstellar like we were talking about. So type along with me if you wish. Where I say curl z put 127.0.0.1 curl in 9200 and that's the IP address and port that we're talking to you slash movies which is the name of our index and then dashed single quote. And now we're going to start typing in the body of this request that will start with a open curly bracket because we're in JSON format and a type of tab. If you want to keep things neatly formatted, you don't have to do this, but I'm going to do it at first, at least just to make things more clear, there's a little bit of a trick to that. You have to hit control V and then the tab key to actually get a tab in there. So we'll say, quote, mappings colon and then another curly bracket and control V tab control V tab again just to keep things nice and organized. So we're creating a new mapping for this index that will contain the following properties. Thusly. And control v tab control the tab control v tab. Again, we're three deep now and we're gong to set up a property for the year. Field Control V Tab Control V Tab Control V Tab Control v tab type. Colon date. So basically we're saying that we want to set up a mapping to ensure that the year field is interpreted as a date type and not just a number, which is what would happen by default if we were just to give it the value 2014. It doesn't know that's an actual date unless we tell it. Let's close off all those brackets, control the TAB, control of the TAB, control TV tab, and we'll just keep doing that all the way down. Until we're back at the outer layer. And to close that off or put in a final single quote to say that we're done with that body and off it goes. All right. We got back in acknowledgement saying that that actually worked. It came back with it all is true. So everything looks good. Let's just make sure that that actually took. So to do that we can actually issue would get request on the mapping and see what comes back and see if that information is actually in there. So let's say curl dash x get this time 120 7.0.0.1 colon 9200 slash movies slash underscore mapping. All right. And sure enough, there is our mapping all in one line there. It's not exactly pretty to look at, but we'll see how that can be made to be a little bit more easy to look at shortly. But yeah, looks like it's in there. So our mapping is part of the index now. That's great. So let's go ahead and insert our movie and apply that mapping to it along the way. So instead of making you watch me type, I'm just going to throw this up here and you can type it in yourself as we go kind of walk through it here, curl dash expert and by the way you could use put or post both would be legal in this case but because we're actually specifying the movie ideas part of this request and not asking Elasticsearch to generate it put is actually probably the better choice. So put 127.0.0.1 call 900. Just our server and port slash movies is our index slash underscore doc and that's kind of a holdover from the older API for Elasticsearch. The type used to be in that spot, but types don't exist anymore. Slash 109487 which is our movie ID for interstellar question mark pretty to get back a nicely formatted result dashed single quote curly bracket genre and double quotes colon and then in square brackets IMAX colon, sci fi and double quotes and your square bracket and through a comma next line title and double quotes colon interstellar in double quotes comma and finally year in double quotes Colon 2014. And because of our mapping that will be treated as an actual year and not just a number and curly bracket and quote and let's kick it off. Looks like it was successful. So it came back with a created results with a successful shard there. Nothing failed. So it looks promising. Let's go ahead and retrieve that back and see if it actually is in there. So let's ask Elasticsearch to give us back everything in this index in a nice, pretty formatted result. To do that, we can say curl dash x get 120 7.0.0.1. Colon 9200. Slash movies. Slash. Underscore. Search. Issue a search query. And we're going to say question mark pretty to say that we want the results formatted in a nicely readable format with new lines separating everything else and tabs and all that good stuff. And there we have it. So we do get back one hit because we only have one movie in our index so far with ID 129487 as it should be. And our two genres in there, IMAX and sci fi, the title Interstellar and the year 2014. But yay success. We actually inserted the movie into our Elasticsearch index and was able to search for it successfully. So we are actually using Elasticsearch already. Guys, that's pretty awesome. Throughout this course, we're just going to keep doing more and more complicated stuff with Elasticsearch. Obviously, this is very basic, but stick with us and we'll make it a lot more interesting going forward.
