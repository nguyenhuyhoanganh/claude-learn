# Dealing with Concurrency

But boom, boom, boom, boom. So when you're dealing with distributed systems like Elasticsearch, sometimes you can run into really weird problems with concurrency. What happens when two different clients are trying to do the same thing at the same time? Who wins? These are concurrency issues. Let's talk about how to deal with that with Elasticsearch. So here's the problem. Let's say that we have two different clients and they're running a web application on a big distributed website, for example, and they're maintaining page counts for any given documents that are viewed. We'll call those documents pages on our website. So let's imagine that two people are viewing the same page at the same time on our website through two different Web servers. So basically at this point, I have two different clients for Elasticsearch retrieving a view account for a page from Elasticsearch. And since the both are asking at the same time at this point is trying to get the current count for that page, let's say it comes back with the number 1010 people came over to this page so far. Now, both of these clients want to increment that at the same time. So they're going to go ahead and increment the view, count for that page and figure out that I want to write a new update for this document that has a view count of 11, and they will both in turn write a new document update with a new view out of 11. But it should have been 12 because we had two incremented requests happen there from ten and ten plus two is 12, not 11. So you see, there was this brief window of time between retrieving the current view count for the page and writing the new view count of the page during which things went wrong due to this concurrency issue. And this is a very real problem if you have a lot of people hitting your website or Elasticsearch service or cluster at the same time. This sort of weirdness can happen. So what do we do about it? Well, there's a solution called Optimistic Concurrency Control. So let's walk through how this would work. It's similar to how the version field was used that we talked about when we talked about updates. This is where that concept comes in handy. The difference is that instead of a single version field, we now have a sequence number and the primary shard that owns that sequence by taking the sequence number and primary term together. We have a unique chronological record of this given document. So in the same situation here, we have two different clients that are trying to retrieve the current view account for a given page document from Elasticsearch, and they both get the number ten back. But when he requests something from Elasticsearch, it also gives you back a sequence number for that document. So I now know that The View kind of ten is associated explicitly with a given sequence number of that document. And that sequence number in turn is associated with a primary term. Call that sequence number nine just for the sake of argument. So now when these guys say that I want to write a new value for that page count, I can specify that. I'm basing that on what I saw in sequence number nine from primary term one. So when you do an update, you can specify the sequence number and primary term that you want to update explicitly. We'll see what that syntax looks like shortly. So what would happen then if two people try to update the same sequence? Is only one of them would succeed. Let's see. The first one actually successfully wrote the count of 11, given the sequence number nine. The other one would try to say, okay, I want to update this document explicitly for sequence nine two. And Elasticsearch could then say, Hey, my current sequence number is actually ten, not nine. That's not going to work. Something's wrong here. You're basing this update on the wrong information, and at that point, you could try again on that particular client. So I would just go back to try to reacquire the current view, count for that page, start over basically, and then I'd get back sequence ten of that document, which contains 11, and I could then increment that to 12 and write it again, hopefully successfully this time. Now you don't have to necessarily do this all by hand. There's a parameter called retry on conflicts. When you do an update that will allow you to automatically retry. If this happens, that's kind of a nifty feature. So that's optimistic. Concurrency Control. You might want to stare at this slide for a little bit if it doesn't make sense. But again, the idea is that if you have many Web servers or many clients that are trying to talk to Elasticsearch at once and trying to update the same document at the same time, you can use these sequence numbers in order to ensure that you're not stomping on each other and retry on conflicts and using an explicit sequence number. And your updates are ways to work around this issue. So let's just go and see how it works in practice. So let's get hands on and illustrate how avoiding concurrency issues works in practice here. Let's start by reminding ourselves what the document looks like for Interstellar in our movies index. So let's say curl dash x get 120 7.0.0.1. Call 9200. Slash Movies slash underscore doc. Slash 109487. Question mark pretty. And we kind of ignored these fields before. But notice that there is a signal and a primary term field in there that's automatically maintained. So the current revision of the Interstellar document is actually sequence number seven and primary term one. This is separate from the version number that we used for updates. So this is something new in Elasticsearch seven that gives you more finer control over concurrency control. So what we can do is say that I want to update this document, but restrict that update to this particular sequence number so that if someone else is trying to do the same thing, that won't work to do that. Here's what the syntax looks like. So make a note of the sequence number seven and primary term one, the combination of those to indicate the specific revision. Let's say curl dash x put to do a an update here. And we're gonna use double quotes this time because we're going to have some fancy characters in here to deal with. 120 7.0.0.1 colon 9200 slash movies. Slash underscore doc slash 109487. And there were I say question mark if underscore Q underscore no equals seven. So we're explicitly saying that we only want to update sequence number seven in turn from primary term one. So we'll say if primary term equals one. Okay. So that's how we've extended our update request there to actually be specific to that revision dashed single quote, curly bracket genres. We'll keep that as IMAX and sci fi. We're doing a full update here and we'll change the title to interstellar food. Or whatever you want. Really. And we'll keep the years 2014. Close that off thusly. And you can see that that did, in fact, succeed. And we have back a new sequence number 12 this time. Right. Primary term is still one because we only have one chart anyway that owns that sequence. But our new sequence number that we got back, it's been updated to 12 at this point. So let's try doing that again and see what happens. So let's say that I'm some other client trying to do the same thing at the same time. And if I just hit the about a couple of times there to go back to our original request, we can try again to specifically update sequence number seven for primary term one. Let's see what happens. That gives this back an error. Okay. So if you imagine that we did both of those updates concurrently at the same time, only one of them would have succeeded, which is exactly the behavior that we want. The other one gave us, gives us back a version conflict engine exception, saying that our sequence number is incorrect. Our current one has a more recent one. We wanted to update sequence number seven, but in fact, we're up to sequence number 12 right now. So that means we have to go back and retry with the correct sequence number. So to do so, I can just hit the up arrow and go back the back arrow here and change the sequence number to the current one, which is 12. And let's see if that one succeeds. Indeed it did. And our new sequence number is now 13. So very cool. That's exactly how it's supposed to work. So if you're managing this by hand in your code, for example, you would specify the exact sequence number from the current version that you got back. If that came back successfully, then great. If not, you have to. You see, you have to get the new sequence number and actually try again. So that's kind of the hard way of doing it where you manage the sequence numbers by hand, but you can also ask Elasticsearch to do it for you with the retry on conflict parameter. Let's see what that looks like. So let's say curl dash x post 120 7.0.0.1 cool and 9200 slash movies slash underscore update because we're doing a partial update this time. Slash 109487, which is our movie ID and we're going to say retry on conflict just like that. And we'll say we'll retry up to five times automatically with the new sequence number that we get back in the event that we get a concurrency conflict dashed quote. And this time we'll just do its partial update of the title. Remember, the syntax for a partial update is dark and then the field you want to update and we'll change the title this time to Interstellar or typo was everything off and that was successful. We're now up to sequence number 14 automatically. Let's take a look at the results right now. Make sure that that change actually took to scrolling up to our search term there. CURL except for that document ID. And sure enough, our new title is Interstellar Typo. So let's do it one more time and just change the title back to what it should be. Again, I'm going to just use the up arrow to go back here a couple of commands and change the title to Interstellar, which is correct value. Again, just doing another partial update here, this time with retry on conflicts set to five again. And since we don't actually have any concurrency going on at the moment, that succeeded just fine. And we are now up to sequence number 15, a primary term one. But anyway, you can see that it all just works. It is something you have to think about if you're going to have a lot of concurrent rights to your cluster. So if you are in that situation, make sure you're either checking for the current sequence number and specifying that with your updates or use the handy dandy retry on conflict feature to manage all of that for you automatically. All right, let's move on.
