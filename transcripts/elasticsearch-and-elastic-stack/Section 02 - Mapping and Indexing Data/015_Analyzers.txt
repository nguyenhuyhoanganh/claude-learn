# Analyzers

So now that we've seen some data that we would like to import into elastic search let's talk about mappings which tells elastic search how to store that information what is a mapping. Well you can think about a mapping an elastic search as a Schema Definition. It's telling its elastic search what format to store your data in and how to index it and how to analyze it. Elastic search usually has reasonable defaults and will infer from the nature of your data the right thing to do more often than not it can figure out if you're trying to store strings or floating point numbers or integers or whatnot but sometimes you need to give it a little bit of a hint. Here's an example of where we're going to import some movie lines data and we want the release date to be explicitly interpreted as a data type field. So here's what a Jason request would look like to do that. Now we're going to be using the curl command throughout this course remember that's just a simple Linux command that allows you to send an H TTP request to the server that's running a plastic search. So in that case that's going to be our local host running within our virtual machine. The syntax of the curl command is that you need to specify the content headers using the dash H command. So we're going to send that along as part of the HDTV request a header that says content dash type application slash Jason which tells a server that we're sending Jason formatted data and that's a new requirement as a Valasek search 6. I'm not actually showing it on the slide however because I'm about to show you a shortcut that will save you from having to type that every time. Now an elastic search five and earlier you didn't have to type that at all but unless it's are six and up you do anyway. Once we have our curl command kicked off we'll send an X put which is the verb for the HDP request the HDTV action if you will. We're going to do a put command to put information into our index the actual address we're talking to is 127 not 0 0 dot 1 which is the loop back address for the local host and colon ninety two hundred indicates that we're running on Port ninety two hundred. Then we say slash movies which is the name of the index that we're manipulating. And after that in curl will say dash D. And then a single quote that says that we're going to send along the data as the request body following this quote those single quotes will enclose the Jason data that's actually the body of this message that we're sending into elastic search that Jason body contains a mapping section which specifies that for this index what the properties of the following fields should be that year type date is what's telling a search that I want you to explicitly interpret the year field when it comes in as a date type and not just a string full of numbers and dashes. Mapping can do a lot more than that though so mappings can define field types like we talked about in other field types besides date can include strings bytes short integers integers long integers floating point numbers double precision and floating point numbers and boolean values as well. So those are all types that allow us to search recognizes and can handle. You can also specify whether or not you want to feel to be index for full text search or not as part of your mapping. So for example you might say index colon not analyzed. If you don't want that information to be part of full text search if you just want that to be sort of ancillary information that gets tagged along in the results instead. More interesting are the field analyzers and analyzers have multiple different things they can do they can have character filters. So for example you can remove H2 e-mail encoding or convert Ampersand to an words you could do token izing with token ideas. That's basically specifying how you split strings based on whitespace or punctuation or non letter characters. There are also token filters where you can do things like lowercase all the information for research purposes do stemming synonyms and stop words we'll talk about that more in a bit. There are many different token filters to choose from for example standard was split on word boundaries simple splits on anything that isn't a letter and also lowercase is for you and then it's a simple whitespace token either that just splits on whitespace but doesn't convert to lowercase you can also specify the language that you want to use for this index that will be language specific stop words and stemming rules that get imported as a result of what language you're using. Go through that a little bit more depth here shall we. So basically there are three things in analyser can do. One is character filters so an analyzer can remove h t email encoding and do things like convert Ampersand to the word. And for example the idea here is that if you apply the same analyzer to your search query and the information that's indexed you can get rid of all those discrepancies that might apply between the two. So if I apply the same analyzer that maps Ampersand to the word and on my search query and in the data that I'm indexing. That means that it doesn't matter if I'm searching for Ampersand or and I'll still get Ampersand results back from my inverted index we also have token Isaias which actually splits your strings up in certain ways. So how do I know what makes up a word. How are search terms broken up. Your choice for tokenism determines that you can for example split strings up just based on whitespace on punctuation or on anything that's not a letter. There's also language specific ways of doing tokenization as well. That's how you specify how a string gets broken up into search terms that get indexed. You can also do token filtering again if you want your searches to be case insensitive which you usually do. You might want your token filter to lowercase everything again if you apply the same analyzer and the same token filter to your search queries and what's indexed you can get rid of any case sensitivity if they're both lowercase in both what you're searching for and what you stored for things to search for you can also handle stemming for example if you want the word box and boxes and boxing and boxed to all match for a search for the term box. You could do stemming to normalize all those different variants of the given word to the same root stem synonyms or also a language specific thing. If you wanted to search for the word big to also turn up documents that contain the word large your token filter could normalize all those to a given synonym or to make sure that those all at work as well. You can also specify stop words so if you don't want to waste space and store things like the end and in a in little words like that that don't really convey a lot of information you might want to apply a stop word filter as well to all of your searches so you don't end up indexing all that stuff to now there are reasons to actually not use stop words particularly for phrase search. Imagine that to our first example that we did back in lecture 1 when we search for to be or not to be. All of those might be stop words right. So if you would actually filter out all of the stop words like to be and not an or it wouldn't work at all you would build a search for that phrase so you have to think closely about whether you want to use stop words or not. So don't enable stop words lightly. Sometimes it has side effects that you don't really want. For analyzers themselves there are several choices. There are standard analyzers the one that you get by default it splits everything by word boundaries and removes any punctuation and lower cases everything for you. So if you don't know the language that's a good choice to stick with and that's what we want to do to stick with for our movie titles because we do have some foreign films in there as well. In the case of our movie lend status standard is actually a good choice. There is also a simple analyzer that just splits on anything that's not a letter and lower case is everything and nothing more than that and there's an even simpler whitespace analyzer that just splits on any whitespace and does not lowercase so any punctuation will be preserved for example and that that may be something you want or don't want. Depending on the type of data that you have cause sometimes punctuation is important. There's also language specific analyzers you can specify your text is in English or any other common language that allows search supports which is pretty much all of them. And then I could do language specific things like handling stop words and stemming in synonyms which is pretty cool. So if you do know that you're dealing with full text to data and that you want to do full text searches on it and you know what language it's in you can use language analyzers to do that. Sometimes you can even mix and match different languages in the same index and there are some tricks you can do to actually run different analyzers on the same text and store those as different fields. I could have for example an English version and a French version of the information in my index that I can search separately. So with that let's actually create a real mapping for some movies data hands on.
