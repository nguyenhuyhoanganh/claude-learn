# Connecting to your Cluster

So let's go over how to actually connect to your cluster. Now that we have it set up and running on a virtual machine on your desktop. Now, if you're running in an ubuntu desktop machine that you had lying around, obviously you already know how to connect to that. You just sign in and you go to town. But earlier back in lecture one, when we set things up in a virtual machine, if you did that, you know, we just signed right into the console on that machine once we started up. And that's not how you would do it in the real world. Typically you would connect to your cluster or machine on your cluster using SSH from some remote location. That's how we're going to do it here as well within this course. So let me show you how to actually connect to your Elasticsearch server using SSH on your own PC. First thing we need to do is open up port 22 on our virtual machine so we can communicate with it via the secure shell or SSH protocol. And while we're at it, we'll open up the other ports to Elasticsearch we'll need as well. Then we need to install some sort of terminal application on our desktop that we can use to communicate with our virtual machine. Now, if you're on macOS, you already have a terminal application and you can just use it. Just type in SSH 127.0.0.1 and that will connect you to your local machine that you're running there within your virtual image. But if you're on windows. Windows does not come with a built in terminal application. So we'll have to go install something called putty in order to do that. And that has a nice little side benefit to it has better support for things like copying and pasting data from your windows clipboard, as opposed to just logging directly into your machine from the virtual machine. Finally, I'll show you how to actually connect to your so-called cluster, which is really just a single Elasticsearch host running within a virtual machine on your desktop. And we'll practice that because we're going to have to do that every time we log in and try to do something in this course on your cluster. So let's dive in and see how it all works. All right. So back to the VirtualBox manager here. What we're going to do is open up the ports. We need to communicate with Elasticsearch and some other associated applications from outside of our virtual machine. So go ahead and select the image that you created earlier. We call that Elasticsearch course in our setup here and click on the settings icon. From here select network. And then we're going to click on Port Forwarding. Pretty simple. Go ahead and click the plus icon to add a new port forwarding rule here let's call this one ssh. Because that's the first thing we're going to do. We need to set up an SSH port so we can actually communicate to this thing from outside of the virtual machine. So again we're going to be communicating with this server running as if it were a remote server on the cloud to kind of make things a little bit more realistic throughout this course. So the host IP is going to be 127.0.0.1 because it's running on the local host, the host port will be 22 and the guest port will also be 22. Now, if you're on macOS or Linux, you might already be using 22 because you might have SSH already set up on your host system. If so, you can use a different number like two, two, two, two. Just remember what it is when you connect to SSH. Let's also open up Elasticsearch itself. We'll add another port forwarding here for Elasticsearch, and that will also be running on 127.0.0.1. This host port will be 9200. And we'll also have the guest port set to the same as 9200. Finally, we'll open up a port for Kibana, which is the user interface for playing with Elasticsearch. Hit the plus sign one more time. We'll call this one Kibana host IP again is 127.0.0.1. Port for Kibana is going to be 5601, and we'll set the guest port to the same 5601. That should do it. Hit okay and okay to get out of the settings. And let's go ahead and start this up so we can start playing around with it and see if we can connect to it. Now like we said, if you're on Mac or Linux, there's not a whole lot you need to do. You can just use the built in terminal application and use the SSH command to connect directly to the localhost on 127.0.0.1 0.1 port 22 for SSH by default. However, if you're on windows, there is no built in terminal that can talk to Linux. So to do that, we're going to have to go and download a terminal application for us specifically to do that. Head on over to Putty org. And from here you can download a pretty simple and pretty popular client for telnet and SSH. For us it's going to be SSH. Click on Download Putty if you're on windows and select the flavor of windows that you're on. Most likely that's 64 bit x86. But if you're on an ARM system, they've got a build for that as well. I'm not going to show you how to install that, because I'm pretty sure you know how to download and install software. Pretty straightforward. But when you're done, go ahead and start putty. It should look like this. And what you need to do is type in the hostname, which is 127.0.0.1. The default port of 22 is correct and make life easier. You can give it a name like uh, Elasticsearch and save that session so you can easily open it up later. All right. Meanwhile my virtual machine has spun up for ubuntu. So let's go ahead and log in with OS boxes. Org and if you did install the desktop version of ubuntu like we showed you in the setup lecture, unfortunately it's not running the SSH server by default. We're going to have to install that. If however you're running the server version of ubuntu, then that's a different story. It probably has everything already set up for you already, but let's assume that you followed the instructions and you're actually using the desktop version. So in that case, we're going to have to open up a terminal and install SSH. The server for SSH. And that's pretty easy to do. We're just going to say sudo apt get install OpenSSH server and type in our password which is OS boxes.org. Yes. And we'll just let that do its thing for a moment. All right. So that should be pretty much it. Just to be sure let's make sure that this is set up to start automatically on startup. So we'll say sudo system control enable ssh. All right. So we should be good to go here. So next let's create a new user that we can connect to externally using SSH. And that's pretty easy. We'll just say sudo. Add user. Let's call it student. And make up a password that you won't forget. And type it again. And you can fill this out if you want to. But none of these are required. I'll just hit enter to get past them all. Yep. Now we also need to add that new student account to the sudo group. So it has permissions to do more interesting things. And to do that we're just going to say sudo usermod dash little a big g sudo student and authenticate again if you must. And that should take care of that. All right. So now we should have a new student account that we can log into. So let's go back to putty and load up that Elasticsearch session that we created earlier and click on open. And the first time you do this you'll probably see a warning message about some security warning. Don't worry about it. Just click accept. It just does that when it hasn't seen a host before. So now we should be able to log in as student with the password that we created. And we're in. Very cool. All right. So now you know how to connect to your ubuntu instance. Running Elasticsearch externally using putty on SSH. Now you don't have to do it this way. You could actually just spark up the graphical interface for ubuntu and use the terminal on it directly. This is just a little bit more of a realistic situation where you'd have the server version of ubuntu running remotely somewhere, and you'd just be logging into it remotely using SSH or something like that. So this is going to be a little bit more realistic here, but you don't have to do it this way. So that's how to do it on windows on Mac OS it's a little bit different. So I don't have a mac system handy here. But basically on Mac OS you would spark up a terminal and you would use the SSH command from the command line to get into this host. So I will spark up my command prompt in windows here, which is in the same spirit as the terminal on Mac OS. So here you would just enter SSH student at 127.0.0.1-P 22 for port 22 and again on Mac OS. You just do this in your terminal. And if you do get a warning about security again, sometimes the first time that you connect to a host with SSH, it will warn you about it. We'll just say yes, we do want to continue and enter your password. And we're in. All right. So now you have the ability to connect to your virtual machine from an SSH client, be it putty on windows or a terminal using the SSH command on Mac OS. With that in hand, remember how to do that because we're going to come back and do that before we start every exercise going forward. And let's start to do some more interesting work coming up next.
