# Installing Kibana

So if you are sick of looking at JSON commands being written in a terminal, I have good news for you. We're about to install Kibana, which is a much prettier way of interacting with Elasticsearch. With Kibana, it's a web UI that just sits on top of the aggregation framework for Elasticsearch, and it can use elasticsearch's aggregations under the hood to create beautiful charts and graphs like this. So instead of crafting JSON requests by hand, you can interactively create the aggregations that you want on the fields that are in your index and visualize them pretty much in any way you can imagine. It's very fun and very cool stuff. But before we can play with Kibana, first we have to install it. And just for reference here in the slides, here are the steps we're about to do. All you have to do is use apt. Get to install it. Since we've already installed the necessary package repositories way back when we first installed Elasticsearch, and then we just need to change the configuration for Cabana a little bit. There are a couple of settings that are specific to running Cabana on a virtual machine on a single host, like we're doing here. After that, it's just a matter of reloading the service and enabling and starting it. And at that point forward, you can hit Cabana on port 5601 through a web browser, which we already opened up through VirtualBox earlier back when we first set up VirtualBox, you know, way back in the first lectures. So let's go ahead and do this. All right. So let's start by making sure we're installing the right version of Cabana to match the version of Elasticsearch that we're running. So let's check that version by running curl dash x get 127.0.0.1 colon 9200. And it should say there the version number. In my case it's 9.0.1. For you it's definitely something else because that's already obsolete even when I'm recording this. So just take a note of that number. Remember it for me that's 9.0.1. For you. It's not. It's something else. Just remember it. So let's see what versions of Cabana we have available to install. We can say sudo apt list cabana. And we can see that right now, 9.0.2 is actually available, even though I'm running 9.0.1. So I could update that if I wanted to, but I don't because I'm still running 9.0.1 right now. Now, if you want to see what other versions are available and not just the latest version, you can say sudo cabana dash a for all. And you can see that in addition to 9.0.2 and 9.0.1, the original 9.0.0 was also an option for me if I so chose, but I want to keep things consistent. So since I'm running 9.0.1, I'm going to install 9.01 of Cabana. To do that, I'm just going to say sudo apt get install cabana and then equals the Elasticsearch version that you have installed from above. So again for me that's 9.0.1 for you it's not. And I already did that. So it's not going to do anything. But for you it will do something a little bit more interesting there. Okay. So once you have the correct version of Cabana installed to match the version of Elasticsearch you have running, we need to change the configuration a little bit. To do that, we're going to say sudo nano slash Etsy slash Kibana slash Kibana. Help! If I spelled it right. Dot. YML. And what we're going to do is change the server dot host from localhost to 0.0.0.0. That just gets around some security problems that we'll have otherwise running on a single host. And I'll write that out with control O and control X. All right. Now we just need to set up the daemon to run Kibana. So we'll say sudo bin slash system control daemon reload first of all. And then we'll do sudo bin system control. Enable Kibana dot service to make sure that it comes up every time that we start up automatically. And finally we'll start that for the first time sudo bin system control start Kibana dot service. And off it goes. Now it'll probably take a few minutes for that to spin up. So you might want to go grab a cup of coffee or something, but eventually that means Kibana will be available on port 5601 for us. And assuming you opened up that port in your virtual machine way back in the setup lecture, that should work. Let's just type in 127.0.0.1 colon 560 one inch our browser. And again, it's probably not ready yet. Oh no. Hey it is ready. Cool. That was quick. All right, well, we're in Kibana. How cool is that? So you can see here we have this really pretty graphical interface now for interacting with Elasticsearch. So we don't have to deal with curl commands anymore and JSON. You know, this is all really aimed more at not the developer but the business analyst or the data analyst here. So with Kibana, you'll see that we can open up our data for analysis by people who are not engineers. And it's also going to make our eyes feel a little bit better going forward. Because all those curl commands, they're not really fun to look at, are they? But with Kibana. Sky's the limit. So let's start diving into playing around with this in our next lecture and see what we can do with it.
