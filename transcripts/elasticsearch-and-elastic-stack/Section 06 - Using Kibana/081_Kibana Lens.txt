# Kibana Lens

Millions of people already use Kibana for a wide range of purposes, but it was still a challenge for the average business user to quickly learn. Visualizations often require quite a bit of experimentation and several iterations to get the results just right. Visualizations in Kibana paired with the speed of Elasticsearch is up to the challenge, but it still requires advanced planning, or you'll end up having to redo it a few times. The new kid on the block, Kibana Lens, was designed to change this, and we're here to learn how to take advantage of this capability. So let's get started. Kibana lens is changing the traditional visualization approach in Elasticsearch, where we were forced to pre-select a visualization type along with an index pattern in advance, and then be constrained by those initial settings as needs naturally evolve. Many users have wanted a more flexible approach to visualizations. Kibana lens accomplishes this with a single visualization app, where you can drag and drop the parameters and change the visualization on the fly. A few key benefits of Kibana Lens include convenient features for fields such as showing their distribution of values, or searching fields by name for quickly tracking down the data you want. Quick aggregation metrics like min, max, average sum count, and unique count. Switching between multiple chart types after the fact, such as bar area, line and stacked charts. The ability to drag and drop any field to get it immediately visualized, or to break down the existing chart by its values. Automatic suggestions on other possible visualization types showing the raw data in data tables. Combining the visualization with searching and filtering capabilities, combining data from multiple index patterns, and quickly saving the visualization for allowing for easy dashboard composition. Okay, let's see how Kibana lens works. So first we need to have something to visualize. The power of lens really comes into play with rich structured and time oriented data. Now to get this kind of data quickly, let's use the Metricbeat tool, which will enable us to collect dozens of system metrics from Linux out of the box. Now, since we've already installed a couple of packages from Elasticsearch APT repository, it's very easy to add another one. Now, we do need to make sure that the version of Metricbeat we install is compatible with the version of Elasticsearch we're running. So just like we did with Kibana, we're going to do a quick check as to what version we need to install. So let's just say curl x get 127.0.0.1 colon 9200. And that should tell you what version number you're running for me it's 9.0.1. For you. It is definitely something newer because that's already out of date even as I'm recording this. So to install the matching version of Metricbeat for whatever that is, I would just say sudo apt get install metricbeat equals whatever that version is. Again, for me it's 9.01. For you it is definitely something else. And I already installed it. So it's not going to do much for me. But for you it will do something more interesting. And we're good. Now we can start it with sudo system control start metricbeat. And that will start publishing stats about my system into a metricbeat index. So now all the rich metrics like CPU and load and memory and network processes and stuff like that, they're all being collected in ten second intervals to our Elasticsearch instance. Now, to make things even more interesting, let's perform some load testing while we collect our system metrics to see some of the numbers fluctuate when we visualize them later on. So we'll do this with a simple tool called stress. And to install that very easy you just say sudo apt install stress. And before we start using it, let's check out how many cores and how much available memory we have. And that will inform what parameters to send to stress. So let's start with nproc. So I can see I only have one core in my virtual machine here. And from memory I can say free H and see that I've got about, um, not a whole lot. 159 megs of memory free right now, but a little bit more swap space. So let's run two different loads. First, we're going to spin up two workers, which will max our CPU cores for two minutes or 100 and 20s. So to do that, let's do stress CPU one because I only have one timeout 120 and if you did come back with more than one processor, just use that same number there for CPU. So for me Nproc said one, I'm passing in one for CPU, and let's just let that run for a couple of minutes and I'll come back when that's done. All right. Two minutes have passed and I've finished. And now for my second load I'm going to stress memory usage with stress VM. Five. Timeout 180. So that's going to run five workers. That should allocate about 256MB of memory each for three minutes. And we'll let that go off. And once again I'll come back when that's finished. All right. Three minutes have passed. It's done. And my office is noticeably warmer now from all that computing. All right. So let's go back to Kibana Lens now. So we're going to create our visualizations using lens. And feel free to follow along here to get the basics around lens. But once we're settled feel free to just click around because lens is exactly the tool with experimentation pre-baked into its very nature. Now, before we start, we need to create a data view that will point to the indices that we want to draw the data from. So let's find our way to stack management here. And we'll go to Data Views under Kibana and create on create a data view. And we'll give it a name of Metricbeat star. and also an index pattern of metric beat star. And there's our metric beat index. Way cool. Go ahead and save that to Kibana. And now we can use it. All right. All sorts of information here. You can see there's actually 590 pages of different statistics here we can play with. So the sky's the limit here for playing around. All right. So let's create a visualization. Let's go to visualize. And under the main menu here that would be Visualize Library. And now we're going to create a new visualization and select lens. All right so now we have this welcoming empty screen telling me to drop some fields to start. So let's drop some. So first let's make sure that metric beat is selected here. And from here we can use the field search on the left panel to search for process CPU. And there's quite a few of them here. So we're going to be looking for system dot process dot dot dot proc. Where are you? There it is. System dot process dot dot dot percent is what we're after. So let's go ahead and drop that. Just tell me what percentage of the CPU is being consumed. So now we're going to switch the aggregation we have on our y axis. The default averages are not really meaningful. In this case what we want is the maximum. So let's click on the aggregation we have in our right panel here on the vertical axis. And let's go ahead and change that to maximum. All right. Already things are looking a little bit more interesting right. So now let's split the chart by another dimension which is going to be processed executable to see what binary was running in the process. Now the technique here is the same. We're just going to search for the field over here on the left panel. So that's going to be process dot executable. There it is. And we could also filter for just string fields first with the filter by type if we wanted to. If you then click on the given field you will find a nice little overview of the distribution of the top values for the selected period. So in our case, we'll see which executables had the highest count of collected metrics in the period. Now to use this field we're just going to grab it and drop it into the main area. So we're starting to see it come together here. But let's iterate further. As would be typical when creating such dashboards for a business. So. So let's increase the number of values we can see in our chart from the default 3 to 5. We'll just click on breakdown by here and bump up 3 to 5. And since more than 15 minutes have passed since I started this. I'm going to change this to go back a little bit further back in time. Let's look at the last 30 minutes instead. All right. So now that's getting interesting right. So now we can see the top five processes that consume the most CPU for any given time slot. And we can see very clearly that while our stress tool was running that had that big spike right there. And we can see clearly that that portion of the stat graph is attributed to user bin stress. So very cool. So let's click on the save link and save this as a new visualization. And let's give it a name like I don't know lens uh top five processes. And I'm not going to associate with the dashboard yet. But if I was building a dashboard I could. So let's save and add that to a library. All right. It's perfect. So to test out some more lens features and to have some more material on a dashboard, we're going to create later, we're going to create another visualization. So let's repeat that procedure by going back to visualize and create a new visualization and pick lens again. So now we're going to search for the memory actual fields. And we're going to drag system dot memory dot actually used dot bytes and actual dot free over here. Now this creates another stacked bar chart. But we're going to change this to a stacked area chart. So we'll just click on the chart icon here and pick the desired type which is going to be stacked area. Area stacked rather. And we can also customize the granularity of the display data which is by default 30s. Now our data was actually captured in ten second intervals. So let's switch that interval by clicking on the time stamp in the x axis. And from here we're going to customize the time interval. And we can change that from 30s to 10s. And close that out. All right. So our new chart visualizing the memory usage should look similar to this one here. Let's just refresh that to make sure it's up to date and make sure we're looking at the last 30 minutes. So yeah. So while we were running that stress command aimed at memory, we are seeing some sharp spikes here where we, uh, allocated that memory from the stress tool. So let's save our progress here again and we'll call this one lens memory usage. And again we'll not associate that with a dashboard quite yet. All right. The last feature we're going to try out is the ability to stack multiple layers to combine different types of charts in the same visualization. So again let's create a new lens visualization. And this time we're going to search for socket dot summary. And we're looking for system dot socket. Dot summary. Dot all dot count. Let's drag that over. And we're going to change the chart type to line chart. All right. And we're going to change the time interval to one minute. Pretty easy right. So now let's add a layer. And we're going to add a new visualization. And we'll change that to a bar chart. And drop in the time stamp for the x axis. And for the y axis we'll add in a bunch of stuff. Listening. From summary dot tcp dot all. And we'll also throw in established. And time. Wait. Looks like I inadvertently wiped out listening. Gotta watch where you're dropping things. And let's also do close. Wait. All right. Cool. Now, optionally, I could also add systemd.socket dot summary dot udp dot all count to see all the UDP protocol sockets. Uh, but lastly, let's change the time granularity to the same value as the second layer. So let's change that to a custom time interval of one minute again. Close that out. And I'm going to change that first layer back to a line. All right. So yeah our visualization should now look similar to this. And we can see the average of all socket connections in the line chart. And the TCP open sockets in various states in the bar chart. So let's go ahead and save this as lens sockets. All right. So naturally, the final step is combining everything we've done into a single dashboard to monitor our vitals. So let's create a new dashboard. Like so. And now we can say add visualization from the library and just select all the ones that we created earlier. All right. And we're done. It's just that easy. So now we can play around with the dashboard or add more visualizations if we want to. If you want to play around try adding a data table so you can view the actual raw data in here as well. But you are now well prepared for any data exploration and visualization in the wild. So use lens whenever you need to perform some data driven experimentations with various metrics and dimensions that you have in your data to tune your dashboards for the most effective storytelling.
