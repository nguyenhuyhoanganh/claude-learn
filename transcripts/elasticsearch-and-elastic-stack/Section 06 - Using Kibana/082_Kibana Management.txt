# Kibana Management

Kibana is considered the window to Elasticsearch, and indeed it's a powerful UI for searching, filtering, analyzing, and visualizing Elasticsearch data. But it's also used to configure, administer, and monitor the Elasticsearch cluster. In this lesson, we're going to explore how Kibana can help administer Elasticsearch for our users. We're going to be exploring the following Kibana management features in this lesson. Spaces enable you to organize various dashboards, visualizations, searches, and other so-called saved objects on your Kibana instance into distinct spaces where different user groups can access what they need. This can be especially useful for segregating various business users according to departments like marketing, security, development, operations, finance, etc. you can also assign access to multiple spaces for specific users, in which case they'll see their accessible spaces when logging in. In relation to spaces, Kibana allows you to transfer a selected set of saved objects, either by copying them between spaces within a single Kibana instance, or by exporting them in JSON format from one Kibana instance, and then importing into another instance. This can be useful in multi-cluster environments. Lastly, we'll look at some settings related to user setup in spaces, such as setting a default landing page when entering a space or switching to dark mode. Without further ado, let's jump right into spaces. All right, so on the Kibana home page on port 5601, let's go to Stack Management. And from here under Kibana we're going to select spaces. And you can see that by default there is a default space that's there. Unless you explicitly disabled spaces somehow that's shared across all users. And it's just always going to be there if you need it. But we're going to create a new space here. So click on Create Space And let's give it a name like DevOps, because that'll be our little use case here. Creating a space for ops. If you wanted to, you could provide a description of that as well. Don't need to. So I'm not going to. You can also change the URL identifier for it as well if you want. Now under Solution View you can customize what features the people see in this space if you want to. So you could limit that to Elasticsearch observability features, security features or classic. I'm going to go with classic because well I want everything. And also in the same spirit we can set the specific feature visibility on these different sections as well. So while the solution view kind of controls the UI and how things are prevented, the feature visibility here is more of a filter. And this will actually make sure that these things are actually removed from the menus in Kibana for people. So if I was, for example, making a space for, you know, market research or something like that, we'd probably want to disable everything except analytics here. But for DevOps, it probably makes sense to have everything enabled. So I'm going to go ahead and keep that all enabled for my DevOps space. If you want to, you can define a custom color for your space as well to differentiate it or give it a little custom letter there, or a special image if you want to. Even so, if you want to get all fancy and pretty, you can do that too. But that's pretty much all there is to it. So I'm going to go ahead and click on Create Space to make the DevOps space. And well done. Your first custom space is ready. So now we can see the new space in the top left menu by clicking on the spaces icon next to the elastic logo. And that menu is great for just quickly jumping between different spaces. So let's switch to DevOps now. The next time you or other users log into Kibana, you'll be prompted with selection boxes to pick from the existing spaces on your Kibana instance. And now we're in our DevOps space, so perfect. Now just to complete the picture for you. There's also an API to perform these administration tasks of spaces. So for example let's go back to our terminal. Here we could say something like curl. localhost. Colon 5601. Hitting the Kibana port. Slash API. Slash spaces. Slash space slash DevOps. And that will give us the basic information about that space that we just created. And just to give you a sense of what's possible with this API, you can create, update and delete spaces as well as copy saved objects between spaces. So now let's take a look at how to migrate Kibana objects. We're going to talk about migrating saved objects now. Now the primary function of the spaces is to separate saved objects between various user groups. So to see it in action we need to have some saved objects created first. So let's import a premade dashboard and a couple of related objects so we don't have to create them manually. So if you're following along you can go ahead and download this dashboard from the. Dashboard. JSON and. On Linux you can just use wget to get it. Or if you're on Windows or Mac using a browser, you can just right click on this and do a save as to someplace where you want to save it to. So now we need to make sure we're in our DevOps space and head back to the management app. And then we'll head over to the saved objects section. So we're going to click on the import icon here in the right corner. And then we'll pick the HTTP dashboard dot JSON file that we just downloaded. And that was in my downloads folder. If you don't see it by the way it's possible windows tapped a dot txt at the end. So you might have to go in there and take that dot. TXT extension off. All right let's go ahead and click on import. And we imported eight new objects. So that should import eight saved objects altogether. The dashboard a couple of visualizations an index pattern and one saved search. Now we don't have any data for any of these, so we can't actually visualize anything with them. But for our demonstration, we only need them to be present in our DevOps space. Now, just to reinforce the concept of spaces that we touched on earlier, let's switch back to the default space like so, and then navigate back to Stack Management and the saved objects section. And note that you don't find any of those imported objects that we just imported. You will neither see them in the dashboard or visualize sections either. So this is a great demonstration of how Kibana spaces allow you to organize your data according to your user needs. But now let's jump back to our DevOps space. And navigate back to saved objects again. And we'll simulate what it would be like to transfer objects between different spaces. So we have a couple of options here. So first we can copy any object between spaces within the same Kibana instance. This can be done by clicking the three dots symbol next to any saved object. And from here you can select Copy to spaces. And afterwards you can pick one or more spaces and finish by clicking on copy. Now that will duplicate the entire saved object and others like associated index patterns or all the visualizations on a dashboard. Now there's another way to do it if you want to migrate Kibana objects between different instances, for example, from your testing environment to production or something, you could do so by exporting the objects to a file, and that can be done easily by selecting one or more objects, and then using the export button up here in the corner. So that will download a JSON file that's newline delimited. And you can import that anywhere just like we imported those dashboards earlier. And for our last expedition here. Let's head over to the Advanced Settings section in the management app where many settings are concentrated. First of all, bear in mind the warning box that is displayed on the top of the page saying caution, you can break stuff here. This is true. All sorts of settings, both high and low level, are found here in one place, so always double check what you're about to tweak. Secondly, these settings are mostly applied to the active space, not globally, which is useful for our case. So for example, you can change the default landing page to which the users are redirected when they switch to a given space. You can do that with the default route setting. There it is. So for example, I could switch that to the monitoring app for my space there. And that would aggregate information about our Elastic Stack components. So our operations team could go straight to the overview of the cluster. Similarly, I could guide marketing users straight to their main dashboard here. Also, if you prefer, you can change your Kibana UI to dark mode, with black and gray as the base colors. Where is that? There it is, dark mode. And that can help reduce eye strain in low light conditions. And lastly, we'll take a quick look into various time related settings that are available. Back up to the top here. Now these settings only affect how the dates are displayed in Kibana and not their persisted form, which is stored in Elasticsearch. So in this section we can change the display formats of timestamps to have them prettified in the UI. We can change the starting day of the week. That might be different depending on your region. And we can change the time zone used to recalculate the displayed time. This defaults to your browser settings, which adopts your OS time zone, but you can change it, for example to UTC or some specific time zone. And this can be useful when you need to troubleshoot an incident with colleagues from across other time zones. So that about sums up the more common management tasks that you'll be performing in Kibana.
