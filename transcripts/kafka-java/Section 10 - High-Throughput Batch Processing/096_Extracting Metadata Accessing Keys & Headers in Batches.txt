# Extracting Metadata: Accessing Keys & Headers in Batches

This will be a quick lecture and I want you to be aware of this. In the previous lecture, we were consuming messages in batches like this along with some properties. But this is how we had exposed our bean and everything was working great. Now here we are getting the list of events or the list of payload objects, right? Okay, perfect. What if I want message keys? Now you might be thinking or you might be wondering, maybe I have to expose a bean like this list of message of key, list of message of string. So that spring will give me a list. Then I can iterate the list for each message. I can get the key and I can get the payload. You might be thinking like this, but actually it does not work this way. If you check the spring cloud stream documentation, they had explicitly mentioned that they can support only these types, list of key or message of list of key. List of message of key is not allowed if we are going to use in the batch mode. Okay. So because of this reason, right? That approach, whatever we were thinking, this approach, it will not work. So what to do if I want the message keys? So what spring says is that they can do this way. That is, they can give us the message of list of key. So this is how it is going to be. It's going to be a little bit ugly, but this is how they can give. So what they are saying here is they will give us one message object. In the message object, we will be having all the list of payload, all the list of events. If you access the key, they will, they will give us list of all the keys. If you access the source attribute, they will give us the list of all the source attributes. Okay. So this is how they are going to give us. I don't like this, but this is how it is because of this reason, right? When you use a batch mode, if you want keys, then this is how we have to do. So we will be exposing a bean like this consumer of message of list of tea like this. We will be getting one message object from the message object. We can get all the payloads list of string. This is our entire payload. Okay. Then we have to get all the keys list of keys like this. Then we have to iterate. So for each, okay. So using the size of the payload or size of the key, then we have to iterate. Then we have to access the key and the payload like this. Okay. So this is how we have to do if you really want those keys. However, the consumer applications, oftentimes they will not be depending on the key. So this is not really common for the consumer application to access the key. So this is not really a big issue, but if you really depend on those keys, then this is how we have to access.
