# Batch Project Setup

In this lecture, let's explore our project setup Let's come back to our IDE. Let's go to section 12 package under that. We have two packages consumer and producer Let's first take a look at our producer Our producer is a spring component. It's a command-line runner We are auto wiring stream bridge and this is our binding name demo out Okay Then when since it's a command-line runner When our application starts this run method will be invoked, you know that Inside the run method. I have one simple for loop here We are going to produce 1 million messages. So using the stream bridge I Send messages like message 1 message 2 message 3. So this is what I am sending that messages to our binding Okay If I try to log every message, it will be too much. So I so I do not want to do that This is why I am having this block that is for every thousand messages. We are going to log this message saying that Total messages produced so far thousand two thousand three thousand. So this is how it will print Okay, otherwise it will be like too much noise in the console. So this is why Then for every thousand messages, we intentionally sleep for 10 millisecond Okay, just for demo purposes as you see the producer is very simple. That's it Let's take a look at the configuration Go to resources section 120 to producer yamal here We have everything as usual But I have added these producer properties linger dot Ms 10 millisecond and the batch size is 32 KB This is super important, okay, then Under consumer we have the consumer config. So now we are going to consume list of messages list of string Then whenever I get the list, I am just a simply printing How many messages I have in this list and how many messages I have consumed so far So this is what I am logging. So batch received list dot size Total processed every time I get the list, right? I am I'm using atomic integer and I keep on updating this counter Okay So this will be printing the list contains 500 messages so far you have consumed 2,000 messages and so on So it will be printing continuously like this Then if we check the consumer configuration 0-1 consumer yamal Here I am adding this consumer batch mode true as we had discussed. This is very important Then I am adding these consumer properties the fetch minimum bytes. I said that to 1 KB This is 500 is a default value Then the maximum pole records 500 is the default value. So I am just updating the 2000 here
