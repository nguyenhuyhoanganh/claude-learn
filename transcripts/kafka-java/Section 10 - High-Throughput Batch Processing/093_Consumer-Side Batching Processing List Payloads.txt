# Consumer-Side Batching: Processing List Payloads

In this lecture, let's talk about the consumer side Consuming messages in batches We have these properties The first one is maximum pole records This is the upper limit That is, our consumer will be asking the broker Hey broker, send me the messages The broker might have a lot of messages to give it to the consumer So we set an upper limit The default value is 500 So we are saying that Do not give me more than 500 messages in a single call So that is what we are configuring here Then fetch maximum wait millisecond This is the deadline for the broker To understand this better Our consumer asks broker Hey broker, give me the messages I am expecting at least 1 byte Maximum 500 records So this is how the consumer asks the broker Now if the broker has messages It will give it to the consumer No issues But what if the broker does not have any messages To give it to the consumer Maybe the producer has not produced any new messages So in that case What do we expect from the broker So we are giving this wait time That is, the broker has to wait for this much time To see if the producer is giving any new messages If the producer gives any new messages within this time If we get at least 1 byte Then it will give it to the consumer But even after 500 milliseconds Default value is 500 milliseconds So even after 500 milliseconds If the broker has not received any new messages Then it will notify the consumer saying that Hey, I do not have anything Then the consumer will ask again Hey broker, give me if you have any messages So this cycle will repeat again and again The default values for maximum pull records Fetch maximum byte millisecond They are actually good The first property I would tune is Fetch minimum bytes The default value is 1 byte We can configure that to 1 KB to 16 KB So that the broker waits until enough data is available Before returning records to the consumer And we have to understand one thing This is something we might often miss or forget that When we say fetch minimum bytes We often think that it is just payload bytes The actual event, the message bytes But remember that Kafka message contains attributes Your key, the timestamp, offset, the partition information All those things So it includes all those Everything Actually it includes all those things So we have to consider the headers information as well So when we say fetch minimum bytes It is not just the payload bytes alone It includes the headers as well Now there is an important problem on the consumer side Let's discuss this Sometimes people will be configuring all these properties And they think that the consumer is more efficient This makes the communication between Kafka broker And the consumer application more efficient of course However there is a problem For example if you are going to expose our bean like this Consumer of string If you are going to expose our bean like this Then the spring cloud stream will be delivering the messages One by one to our function Internally the spring framework might be consuming messages in batches So this process could be more efficient However the spring cloud stream will be delivering The messages to our function one by one Because that is what we are saying consumer of string But internally as part of the message processing You might be inserting that record into our database So if it is going to The spring framework is going to call our consumer like this one by one Then you will be inserting these records one by one Into our database table right So this is not efficient So this is not true end to end batching So to enable true end to end batching Along with these properties We also have to expose our bean like this Consumer of list of T Consumer of list of string With this approach the spring cloud stream will be delivering The list of messages to our application This will allow us to process them in batches And persist them in batches if you want We also have to include this property in our binding configuration Spring cloud stream bindings consumer in zero So the consumer batch mode is true Why we need this property is mainly because Otherwise spring will not know the difference between Whether your single message itself is a list Or you are looking for list of messages So this is why we need this and this Only then we can get the list of messages We can enable true end to end batching
