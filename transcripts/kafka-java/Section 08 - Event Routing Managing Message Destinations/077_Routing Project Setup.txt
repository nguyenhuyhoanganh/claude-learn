# Routing Project Setup

In this lecture, let me explain the project setup for content-based routing. Whatever we had discussed, this is what we are going to do for the routing demo. We have the order object and here we have two types of products. Our producer will emit order objects via this topic. Our processor will be consuming these events and it will check the product type. If it's a digital product, then it will build a digital delivery object with the order ID, customer email, etc. Then send it to this topic so that the digital delivery consumer can handle this order delivery. Similarly, if it's a physical product, then the processor will be building the physical delivery object, provide the order ID, customer address, etc. Then send it to this physical delivery topic so that the physical delivery consumer can handle the delivery of this order. Simple setup. This is what we are going to do. As we show here, we are going to run two different consumers. I mean two different JVMs or instances just to mimic the production setup. Okay, let's take a look at the code. Let's come back to our IDE. Let's please go to section 09 package. At this point, you should be already familiar with the structure. So let's check the DTO package first. Here we have the product type. We are reusing whatever we had done earlier. So product type, we already know. Order object, same order object, nothing special here. This time we have two delivery objects. One is for digital delivery. The other one is for physical delivery like this. So this is our DTO package. Let's check the producer. The producer is same. I did not do any change. So same producer. I simply copied and pasted here. Then under resources, section 09, 04 producer, same producer. So producer out 0 goes to order events and we are using the default 1000 millisecond. So every second the supplier will be invoked. So we already know this. Let's check the processor. Please go to processor config under processor package. So this is our processor beam delivery processor. Here we receive the order object. Whenever we receive the order object, I simply pass that to this dispatch method. This is just a helper method. I could have simply done this here, but I thought of separating this like this. So this is why it's here. OK. So when we receive the order object, the first thing we check the product type, digital or physical. If it's a digital, we build the digital delivery object like this. Again, it's just a demo. So we build a digital delivery object. If it's a physical, then we build a physical delivery object like this. So once these objects are built, then we call the two message method basically to wrap that message. So we pass the payload and we pass the destination. So here if you scroll up here, I have some string constants. So this is the header spring cloud stream send to destination. This is the header. And we are having two custom bindings, digital out, physical out. So we are saying once the digital delivery object is built, build the message object with this binding. Similarly, once this object is built, build the message object with this binding. So that is what we are saying. So message builder with the payload. We provide the payload. We set the header and we provide the binding name. Build. So we return the message object. So that said, that's what we return here. OK. Very simple implementation. Let's check the corresponding YAML configuration right away. So this is our Bean. So if you go to section 0903 processor YAML. So that is what we have here. Then as part of the bindings, we have the delivery processor in 0. And we consume from order events. We give some group name. And we do not have delivery processor out 0 because we are using header. And here we have two custom bindings. So ensure that we have those here. Digital delivery out. This is the topic. Physical delivery out. This is the topic. Everything else. Nothing special. We have already seen all these things. Then let's go to the consumer package. Here we have two different configuration class files. So this one is for digital delivery consumer. We consume the digital delivery event and we simply just print it on the console for now. Then similarly for the physical delivery, it consumes the physical delivery event and it prints that on the console. Now if you go to section 0903 resources, here we have two different YAML configuration files for these two consumers. So the digital consumer, that is the Bean we activate here. Digital consumer in 0. Digital delivery topic. We give some group name. Similarly for the physical consumer, physical consumer Bean. Physical consumer in 0. Physical delivery topic. Okay. Simple setup.
