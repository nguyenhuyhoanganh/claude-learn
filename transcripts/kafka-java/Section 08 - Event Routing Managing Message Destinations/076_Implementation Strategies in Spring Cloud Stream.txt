# Implementation Strategies in Spring Cloud Stream

There is a challenge in implementing this event routing with the Spring Cloud Stream. If we have a low-level API, we can do whatever we want. But when we use a framework with some contract and assumptions, the framework has to support this behavior somehow. Let me explain the challenge. This is the function contract, right? You have the input type T, output type R. We map that with the two different bindings here. This is the input topic and this is the output topic. This is what we would use for most of the cases, so no issues. But if we are going to route this message to two different topics, how do I do that here? You see the problem, right? Spring Cloud Stream provides two different options to support this behavior. So the problem can be solved easily. The first option is to use stream bridge, which we are already familiar with. This is how we are going to implement. First, as part of the binding configuration, we will be having the processor in zero, destination and all as usual. Here there is no change. This is for the input topic, okay? Then we will be creating two custom bindings. Give any name you like. It's up to you. So I specify physical delivery out, destination is physical delivery topic, digital delivery out, destination is digital delivery topic. So two custom bindings, okay, perfect, right? Then we are going to auto wire stream bridge. Do not create the function bean. It's a processor, but do not create the function bean. Instead, just to create the consumer bean like this, your processor is a consumer. So we can create the consumer bean like this. We will be consuming the order event, right? Then check the product type, depending on the product type, build the delivery object, then send it to the corresponding binding. If it's a physical product, send it to physical delivery out binding. If it's a digital product, send it to digital delivery out binding. That's it. Very simple, right? Another option is to use message header. The configuration YAML is going to look exactly the same as we had just discussed. Then you do not need stream bridge here. If you like to create a function bean, just create it as usual. Here we have to return the message object. In the message, we can add a header, right? We have already seen that, you know that. So just add one header like this, spring.cloud.stream.sendTo.destination. So this is the header and give the binding name, okay? So give whatever the binding name you have configured here, okay? So what it means is that this message, whatever I am building, it has to go to this binding. That is what you are saying, okay? So that's it. Spring.cloud.stream will check this header, then it will deliver the, or it will send the message to the corresponding topic, okay? So very simple, right? Okay. We have two simple solutions. As we had already discussed the stream bridge in the previous section, let's explore this message header approach for our event routing.
