# Filtering: Conditional Message Processing

In this lecture, let's talk about the filter processor. You know the project setup, right? So our order service is going to continuously emit this order event. Now this time we are going to activate a different processor, shipment processor. So this processor is going to check if the product type is physical, then it will be emitting this shipment event. If it's a digital, there is nothing to emit, okay? So it will not emit. So only for physical product that has to be shipped. So it will be emitting shipment event like this. So nothing else, very simple. So let's take a look at the code. Let's come back to our IDE. This is the shipment processor. As usual here, we are going to receive the order object and we have to return one shipment object. As part of the processing logic here, we check if the order product type is physical. If it's physical, then we return the shipment event like this. Otherwise we return null. We have to return something. So we are going to return null. Spring cloud stream will understand that. If it's null, then it will assume that there is nothing to emit. Let's check the corresponding YAML configuration for this processor. Go to resources, section 07, shipment processor YAML. So we activate that shipment processor bean and it subscribes to order events. Shipment processor out 0 is shipment events. Just for this demo, I did the docker-compose down and up. Please do that. Otherwise you will be having some existing messages in the Kafka topic. So the output might not look good, confusing. So to have a clean demo, please do that as part of every single demo. Now let's go to section runner. The consumer producer, that configuration is going to be same. We are not going to change that. But for this processor, now we have to point to 04 shipment processor. Check the spelling, ensure that you are pointing to this shipment processor. So now let's run the consumer and let's run the processor. Let's wait for the partitions to be assigned. So here everything is assigned. Great. Okay. So things are good. Now let's run the producer. Here it's emitting order ID 1, 2, 3, 4 and so on. But if you check, only the even numbers are physical product. So then we should be getting the shipment events only for the even numbers as you see. All the digital products are dropped. That's it. Now we can stop these.
