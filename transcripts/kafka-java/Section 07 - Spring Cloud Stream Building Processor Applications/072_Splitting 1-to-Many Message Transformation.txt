# Splitting: 1-to-Many Message Transformation

In this lecture, let's focus on the one-to-many mapping processor. Here the setup is same. Here we are going to activate one notification processor. We have two notification channels, SMS and email. So our processor for every order event it receives, the processor will be emitting two events for the consumer. So one notification event for SMS, another notification event for email. So one-to-many. Now, this is important. I want you to listen carefully. This is the Java function contract for processor. So for one order event, if we have to return multiple notification events, we obviously think that we have to return a list, right? So naturally, this is what we will want to do. Function receives order, and here we are going to return list of notification. Perfect. But the Spring Cloud Stream will interpret this slightly differently. That you are returning one value, that one value is list of notification. Obviously it will think that it's a message of list of notification. So it will assume that the whole list itself is one event. So this is how it will send. If this is what you want to do, then this is correct. We can do this way. But I want to treat every single item in the list as a separate message or event. In that case, we have to tell Spring Cloud Stream explicitly that I am returning list of message objects. So we have to mention like this explicitly. Only then Spring Cloud Stream will understand that, oh, it's a list of messages. Then I will have to send them as separate events like this. The difference is very important, and I want you to be aware of this. This is what we are going to do in this demo. Later, we will be talking about router. So that time, this concept, you can understand and where it will be helpful. Let's come back to our IDE, go to processor config. So this is the one-to-many mapping processor. Here for every single order we receive, we send two events, notification events. One is for SMS notification, the other one is for email notification. So here I am coming up with some phone number for SMS notification and some email ID like this for email notification for the demo. And here, if you notice, we are sending list of message of notification like this. Only then Spring Cloud Stream will understand that and it will be treating these as independent event objects. Let's check the corresponding configuration YAML. This one notification processor. So we activate notification processor Bean, it will be subscribing to the order events topic and whatever it emits, it will be going to the notification events topic. Let's run this first, do the docker compose down and up. Then let's go to our section runner, the consumer producer, no change, but for the processor that should be pointing to 05 notification processor, ensure that. Let's start the consumer. And let's start the processor as well. The partitions are assigned perfect. Then let's run the producer. So for now, every single order event, we should be getting two notification events. Let's check that. If you see already one, one, so here we get two notification events. That's it. Simple demo. Let's stop these.
