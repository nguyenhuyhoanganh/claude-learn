# Transformation: 1-to-1 Mapping Processor

This is going to be our project setup. This could be confusing. So please be patient. We have a producer and a processor and a consumer. And we are going to use Java records this time. Instead of emitting message one, message two and so on, that is boring actually. So let's use Java records. The producer is going to emit order objects via this order events topic. And here we have three types of processors. So one processor is for one to one simple mapping, the other one processor is for filtering. And the third processor is for one to many. So we will be enabling one processor at a time, one by one, that is what we are going to do. Depending on the processing logic we are going to enable, it will be doing some action and it will be emitting corresponding events in the appropriate topics. So we have three output topics. Our consumer is going to consume all these topics and just print the events on the console for the demo purposes for now. So this is the setup we are going to have in a very high level. First let's focus on the one to one mapping and this is what we are going to do. Here this is our order object and we have two types of product to keep things very simple, digital and physical. And our order object will be looking like this. Our producer will be emitting this order objects continuously every second. Our processor is going to consume this order object and it's going to emit one payment object via this payment events. So for every single order object it receives, it will be emitting one payment object via payment events. Our consumer is going to consume these events and print it on the console. This is what we are going to do, very simple. Let's please come back to our IDE and go to section 07 package. Here we have these consumer, processor and a producer. Now we have one DTO package. Under DTO I have this product type enum, then this is our order object. This is our payment object. So now let's discuss one by one. Let's go to producer. The producer is going to be very simple as usual. Instead of emitting message one, message two, now we are going to emit order object. So the counter I'm going to keep on incrementing this whenever this get method is invoked. So I am going to use that as ID. Then I'm simply just checking if the ID is even number, I'm going to assume that it's a physical product. If the ID is odd number, then it's a digital product. Then I build one order object. So basically I'm just simply just passing this ID, nothing else. Order ID, customer ID, basically they all will be same. Again, just a demo, just to keep things simple. Then for the amount, I'm going to generate some random number between one and thousand. Then we pass the product type. So this is how we simply build one order object. We are emitting this order object. Then under section 07 resources, section 07, I have this. I have many YAML files, but let's focus on the producer YAML. So this is our producer beam, our polling interval, like one second, producer out zero. It goes to order events topic, nothing special. Then let's check the consumer, go to consumer package, our consumer config. Here we have multiple consumers. One is for consuming the payment events. One is for consuming the shipment events. This is for consuming the notification events. So whenever we receive any event, we are going to simply just print it on the console. Hopefully no confusion here. If you have any questions, please let me know via the Q&A section. Let's check that corresponding the consumer YAML file. We have only one consumer YAML. Here we activate all the beams, payment consumer, shipment consumer, and notification consumer. The payment consumer in zero that is going to observe or subscribe to this payment events topic. Similarly, shipment consumer is going to subscribe to shipment events. Notification consumer is going to subscribe to notification events. That's it. Now as you understand what our producer is doing and what our consumer is doing, let's focus on the processor. Let's go to processor package. Here we have one simple class. If you, okay. So here we have multiple bins. Let's not focus on other things. Okay. So this is what we are going to focus for now for this demo. Here this is one to one mapping, very, very simple processor payment processor. So for every single order is going to receive. So it is going to emit one payment event like this. Very simple. This is what we are going to do in the real life. When you receive the order, you will be probably passing that order object to your service class and get that processed. Then you will be returning whatever you are receiving from your service class. But again, this is a demo. So we are keeping things simple. Okay. So now let's go to our resources section 07. Let's check the corresponding configuration YAML file. So 03 payment processor YAML. Let's click on this. So here we are activating only the payment processor bin. Our processor is a producer and a consumer, right? So it will be subscribing to a topic that is order events, payment processor in zero order events. So we are giving some consumer group name. Then it's also going to produce an event. So payment processor out, zero destination is payment events. I just did the Docker compose down and up to have a fresh Kafka container. So please do that. Let's go back to our IDE, go to section 07 section runner, ensure that the consumer is pointing to section 07, 01 consumer. The processor is going, processor should be pointing to 03 payment processor and the producer is going to point to 02 producer. Okay. So this is the setup we want to have. First let's run the consumer. So here we are activating all the bins. So it will be subscribing to all the topics. So let's be patient. Okay. So here they are subscribed to all the topics. So this is great. So the consumer is ready. Now let's run the processor as well. Okay. The processor is also ready. Now let's run the producer. Okay. So now it's emitting its order events. Okay. Java object. Actually, I'm not logging anything. So if you want to add some log statements, feel free to add, okay, sorry about that. Now let's go to consumer. Okay. So now if you see here, we are receiving the payment event. So the processor is processing and here for every single order, the producer is emitting here we receive the corresponding payment event, right? That's it for the one-to-one mapping demo. So let's stop this.
