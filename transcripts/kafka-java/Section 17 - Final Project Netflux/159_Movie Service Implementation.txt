# Movie Service: Implementation

In this lecture, let's work on the service class methods When we invoke this endpoint that time we will be calling the movie service get movie method. So let's focus on this So for this we need the movie repository. So private final movie Repository movie repository like this When we save the movie that time we will be using the application event publisher as we did earlier for customer service So let's inject that as well application event publisher even publisher like this So let's inject these When this method is invoked using the movie repository We are going to say fine by ID so provide the movie ID When the when we have the movie entity object, we are going to use the movie mapper To movie details. Okay. So this is what we are going to return if it's not found. We are going to throw DTO Movie not found exception pass the movie ID When this method is invoked we get the movie details That is a DTO. Basically, we will not be having the movie ID and all at this point. So we are going to Use the movie repository to save Okay Okay, so we have to provide the entity object. So from DTO we have to create the entity So we are going to use the movie mapper To movie. Okay. So this is the entity object. So pass the details movie details object. Okay Perfect when you say this will give us a movie object With the ID set. Okay. So here the movie object will be containing the movie ID after saving Perfect at this point we can use the event publisher to publish this right movie added event. So this dot event publisher Publish Event. Okay, so we are going to create the movie added event, right? So movie Mapper to movie added event. So if you provide the entity object from there From that they can create the movie added added event. So provide the movie object So we are publishing so this is great. We have to return the movie details Okay, so now once again, let's call the movie mapper To movie details from the movie entity object. So provide the movie object perfect and Let's add the transactional annotation Let's focus on the messaging now When this method is invoked We can build the message object and we can send via stream bridge at the logger and we are going to inject stream bridge Like this at the constructor Parameter Okay, perfect. We also need the binding name So, let's go to source main resources application yaml movie events out. So that is the binding name So let me copy this and we are going to send the message with the key integer the movie ID So let's use that private static final string Movie Events. Oh, let's keep it like this Var Message message builder with the payload movie added event Set Kafka headers dot key we are going to add The movie ID as the key. This is Integer so this is good Dot belt and Using stream bridge we can send use the movie Events out binding and Provide the message object. So that's it then use the logger To log this message published
