# Movie Service: Data Initialization

In this lecture, let's focus on the MovieData initializer. The whole purpose of this class is to help with the demo nothing else. We have the huge, the movie's JSON line file, right? So we are going to read this file line by line, each and every line is one JSON object. So we are going to read that. By using the movie service, we are going to call the SaveMovie method so that the movie will be saved and it will also be publishing the movie added event and we are going to observe the recommendation service, the real-time notification, etc. So this is why we are doing this. So we are going to inject the movie service like this and we also need that file, right? Sorry, this movie, the JSON file. So private final resource, Spring Framework Core IO resource like this. So let's add these as constructor parameters. We have to tell Spring that we want this file as a resource. So we are going to do that by using this value annotation. So classpath movies JSON L. This is the file we want that as a resource. We are going to read this file line by line. So for that, I am going to have one private helper method. Let's call this load movies. Let's keep it like that. I'm going to use buffered reader. So var reader new buffered reader like this. This buffered reader, okay, so we will be accepting the reader object. So we are going to use new input stream reader. So now we need the input stream. So we can create that by using resource dot get input stream like this. So now we have the reader object. I am going to use try with resources like this. So this will be throwing exception. So let me catch the exception and we will throw run time exception. Using the reader, we can use the lines method. So that will give us the stream of string. That is, it will give us the stream of this. So this will give us the stream of line object. So each and every line represent one movie object. So currently we get that as string. So let's say line each and every line is one movie. So we are going to use the JSON mapper to deserialize. So our mapper, JSON mapper from tools Jackson dot shared. So using the mapper, mapper dot read value, provide the line and we want to deserialize that into movie details class. So now we are getting the stream of movie details. So for each the movie details, right? I am going to use this dot movie service dot save movie. Perfect, right? This will run super fast. Probably this will be loading all the hundred thousand movies in one second. That is not the intention. We want to import slowly. So I am going to have another method to delay. So I'm going to call this sleep if this is going to accept long millis millisecond. So I'm going to say thread dot sleep millis like this. And let's use try catch around with the try catch like this. So now I'm going to say this dot sleep three thousand, three seconds. So we will be importing a movie every three seconds like this. So this is what we are going to do. Everything looks good. So now as part of the run method, we can call the load movies method. However, here we block the thread, right? So we can use virtual thread, thread of virtual dot start. Then we can give the load movies method as runnable like this. So that's it. When we run our application, if this property is set to true, that time this run method will be invoked using the virtual thread, we will be importing one movie every three seconds continuously.
