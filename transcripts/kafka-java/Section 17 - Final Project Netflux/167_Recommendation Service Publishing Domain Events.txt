# Recommendation Service: Publishing Domain Events

In this lecture, let's focus on the recommendation stream service. Its main responsibility is to provide real-time recommendation to the customers. Newly added movies and personalized recommendations. This is what it's going to do. But in order to do that, the recommendation stream service should know when we have some updates. So when the movie service and the customer service, when they receive the events, they update the tables, right? Once it's done, then they will notify recommendation stream service that, hey, we have just updated. So you can provide the recommendation. So this using the application event publisher, they are going to notify the recommendation stream service. So let's focus on this part first. Let's come back to our IDE. Under BTO, you can see one class called a recommendation events. If you click on this, here we have a couple of records. Basically, the movie service and the customer service need some object. Using that object, they will notify the recommendation stream service using the application event publisher. So that is the idea. So we are modeling that using these two records. I have just added one new movie. So this is the movie ID. I just updated this customer genre. This is the customer ID. So this is how they are going to notify. So we have one class called recommendation events. It basically acts like a namespace. Under that, we have two records. So why am I creating like this? Why can I not create these two records as two separate Java files? We can actually do that. If you want to have these as two separate Java files here, you can absolutely do that. But in the real life, you might be having multiple similar small Java records like this. For example, in our case, we have only new movie and personalized. Maybe in the future, you might want to have something based on your watch history, all those things. So if you have multiple recommendations, then you might be having multiple Java records. So you can organize all these records using one single Java class file like this. So this class acts like a namespace. Under that, you keep all your Java records like this. These are all related. So this is why we are doing this way. However, if you want to separate. If you want to have a separate Java class files, we can also do that. Let's work on this first. Let's go to movie service. Once we have inserted the new movie, then using the application, the event publisher. So let's inject that private final application, event publisher, event publisher. Let's keep it like this. Let's add the constructor parameter. Now using this event publisher, we are going to notify other spring components that I have an update. This is what we are going to do. So we are going to say new movie added event. So we need the movie ID, right? So movie added event that movie ID. So we are going to notify this way. Similarly, let's go to customer service here. Let's inject the private final application, event publisher, event publisher like this. Let's add the constructor parameter. This dot event publisher, publish event, new personalized event for the customer ID. Okay. So genre updated event, customer ID. If you go to the recommendation stream service, now here we have the event listener, right? So when these services, when they publish the events, the recommendation stream service, these methods will be invoked. So using these as triggers, we can send the real time recommendation. And if you notice here, I have this async annotation. Why we have this async annotation is mainly because to better understand this, these are the two Kafka consumer bins we have, right? So we have one Kafka consumer thread. That thread will be consuming the messages and using the customer service. It will be updating the database tables, right? Once the database update is complete, basically we have consumed the message and this is the primary responsibility that is done. So we can acknowledge the message. So this thread does not have to waste time to build the real time notification and sending the notification. This Kafka consumer thread doesn't have to do this. So this should be the main responsibility. As soon as this is done, it simply just notifies other spring components that I updated the database tables. If you have something to do, please do. So it simply just notifies other thread pools and it goes back to consuming other messages. Okay. So this is why I am doing this way.
