# Customer Service: Testcontainers Integration Test

In the previous lecture, we tested the genre-updated event using the test binder. In this lecture, we are going to use test container. As I had mentioned already, it is not like you have to repeat the same test using both the test binder and the Kafka binder. So it is not really like that. Since we are practicing, I want to do both, but in the real life, prefer test container. Okay. That is what I would say. Okay. So in this, the starter project should be already including everything for you. So since our application is producing a message via the customer events topic, our integration test will be acting like a test, acting like a consumer. So it's going to consume the genre-updated event. So we have the blocking queue. Whenever we receive the message, we are going to add it to the queue. Okay. Since this is a beam, I can inject that in our test class like this. We also have the rested test client. Okay. So when we send the patch request, our application should be producing the message. It will be added to the queue and we are going to validate that as part of our test method. I have already included the properties. The spring cloud function definition is test consumer, and we do not have the spring cloud function definition here. Okay. So, so this is good. And the, and the test consumer in zero destination is customer events. And we have the key deserializer, which is the integer deserializer. Okay. And auto offset reset is earliest. To save some time, let's go to the event, the test binder test. Let's simply copy all these things and paste it here. So this part is going to remain same, right? So we are building the genre update request. Okay. Then we send the patch request. At this point, it should be triggering the genre updated event and our test consumer will be able to consume that. So we can access that using this queue object. So this dot queue dot poll. So for the timeout, let's give like five seconds and okay. So five seconds, let's keep it like this. So there is a checked exception. Let's add this exception to the method signature. Let's ensure that this message is not null. So assertions assert not null message. Let's do that first. Then we do not need the JSON mapper message dot get payload. That will give us the customer genre updated event. And for the key, right, we have to use received key because we are using Kafka binder. So it's a received key. That's it. It looks good. Let's run this. Now it uses the test container. Wow. Look at that. The test passed.
