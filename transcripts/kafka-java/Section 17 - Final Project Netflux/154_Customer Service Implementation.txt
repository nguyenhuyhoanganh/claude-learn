# Customer Service: Implementation

let's come back to our IDE let's work on this API first when this endpoint is invoked for the given customer ID we return the customer details right so for that let's finish this first so we need the repository so let's inject that private final customer repository like this so let's add the constructor so now using this customer repository this dot customer repository find by ID, let's pass the customer ID, then dot map, when it returns the actual customer entity we are going to use the customer mapper so customer mapper so two customer details there is a method to create the customer details object the dedue if it's not present we are going to throw exception saying that we do not have such customer, no customer sorry, customer not found exception so new customer not found exception let's pass the customer ID so that's it we are done if the customer ID is present we are returning the customer details otherwise we will be throwing exception that time the controller advice will catch that exception and it will return proper 404 response ok, perfect now let's focus on this endpoint, the patch request so here we get the customer ID and the request body the genre update request so let's focus on this so when this method is invoked, so that time again we are going to get the customer entity find by ID provide the customer ID if the customer is not present again as usual we are going to throw exception new customer not found exception, pass the customer ID so as part of this here we are going to get the actual customer object so var customer, so this is the customer entity so when we have the customer entity then we can use the set method to set the favorite, customer's favorite genre from the request let's not forget the transactional annotation from Jakarta transaction when this method executes successfully spring will be updating the database table automatically for us at this point we might want to emit the customer genre updated event to the customer events Kafka topic so we might want to inject stream bridge and we might want to do this way but instead of doing this way we have a separate class under messaging we have the customer event publisher so this class will be responsible for publishing the various events to Kafka topics why can we not use stream bridge in the service class itself why do we need a separate messaging class no special reason actually just a simple separation of concern our customer service is simple so it is nothing wrong if we want to use stream bridge and directly send the message to Kafka but in the real life I assume our application could be bigger with more functionalities and features so you might be having multiple service classes like this we will be using stream bridge everywhere and build a message objects with the keys and other header information etc so I feel like we would be polluting our service classes with the unnecessary messaging related complexities instead of using stream bridge everywhere we could use application event publisher like this it is a spring component we can auto wire this in our service classes using this we can notify other spring components that the customer genre is updated our messaging class will be listening to this event using this event listener annotation so when the application event publisher publishes this event this method will be invoked automatically in the messaging class then it will build the message object with the keys headers etc then we will send this message to the Kafka topic using stream bridge so that our service classes can focus on the actual domain logic they do not have to really worry about the messaging related complexities this is the actual idea if you do not like it no worries you can actually send it using stream bridge in the service class itself it's up to you but I think that it would be good to separate the messaging concerns so this is why I am doing this way let's come back to our IDE customer service class after updating the customer's favorite genre we are going to notify other spring components that the customer genre is updated so for that we need the application event publisher let's let's add the constructor parameter spring will automatically inject this for us then using the event publisher we are going to publish the event so we need the object so we are going to build the new customer genre updated event but it's already there if you go to customer mapper if you provide the customer ID in the favorite genre this will build the customer genre updated event so this is what we are going to use customer mapper to genre updated event provide the customer ID and provide the genre when we publish this event this method will be automatically invoked so that time we are going to build a message object message equals message builder with payload genre updated event that is the payload I am also going to set the key kafka headers dot key since this is the customer related event I am going to use the customer ID as a key customer ID so we are using integer type so we have to set the integer serializer so let's not forget that as well so dot builder so we have built the message object now we can send using the stream bridge I do not have stream bridge so let's please add that private final stream bridge stream bridge like this let's add the constructor parameter now we can use stream bridge to send ok we need the binding name as well in the application yaml I have already added that customer events out that is the binding name ok so the destination is customer events that is the topic this is the binding name I have already included the integer serializer so that is good ok so private static final string let's say customer events out let's keep it like this now let's provide the customer events out and send the message that's it let's add one last message saying that the message is published like this at this point our customer event publisher the customer service everything looks good this configuration also looks good in the next lecture let's focus on the integration test
