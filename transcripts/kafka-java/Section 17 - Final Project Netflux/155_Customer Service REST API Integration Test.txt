# Customer Service: REST API Integration Test

in this lecture let's focus on writing the integration test for our customer service application for that please go to source-test-java.com-vinsguru-customer under that here we have we have few test classes the test methods are not implemented we are going to implement that okay so this test class for testing the API for example we have the get API to get the customer details right so that is what we are going to test that here then when we have the when the customer ID is not present that time application will be throwing the customer not found exception our controller advice should be giving us the proper 404 response so that is what we are going to validate that here we also have one more API to update the customer genre so we will be sending the patch request right so we should be adding the test here however I am not including that here is mainly because we have a separate test to validate the event so we can include that here that is we have one test class to test that customer genre updated event so to generate that event we have to send the patch request anyway so we are going to test that as part of this so okay so this is why I am not including that here okay so we are going to test that using test binder here we are going to repeat the same test using Kafka okay I have already included the test to consumer configuration I have added all the properties we just have to add the test here now you might be wondering why do we have to repeat the or why do we have to do the same test using test binder and Kafka in the real life you can just use the test container test okay so you just need one integration test that's it you do not have to repeat every single test using as binder and a test container you do not have to do that since this is a demo project we are practicing and I thought of doing both but in the real life you can just have the test container test let's go to customer API test as you see this is a spring boot test under here I am using the auto configure rusted test client so when you add this annotation we can inject this this rusted test client was introduced as part of spring boot version 4 so this is a replacement for the test rest template so using this we can test our API's in a declarative state so that is the idea so let's test our customer details this endpoint this is what we are going to do okay so this dot test client we are going to send the get request okay then what is the URI so we do not have to provide the localhost colon 8080 all those things because the spring will automatically a setting up the base URL for us we have to provide only the path because as part of the test you might want to use random ports and all right so spring will be setting up the local host colon port etc automatically we just have to give the path alone so in that case it will be API customers 1 okay so this is the endpoint we are going to use and if you remember when our application starts it will be automatically setting up some users for us so Sam is the customer ID 1 this is 2 this is 3 so let's test this information okay so when we send this request okay so this is the URI exchange so now we are sending the request we are going to validate the response so we can write that in a declarative style like this okay so expect status dot is 200 successful it should be 200 successful response that is what we are saying then expect body now how how do you want to validate the body so we are going to use JSON path if we have not used the JSON path before I would like to give you some information probably this will be helpful for you let's imagine that you have one API when you invoke that API you are getting one JSON response like this okay so order ID some nested customer details some array list of items okay something like that you have some response so this is how we will be using the JSON path to access the information from the the response body the dollar is the root object okay so when you say dollar the root object when you say dollar dot order ID that that will give you the 101 similarly when you say dollar dot status this will give you the status conformed okay similarly when you when you want to access this name Sam right dollar dot customer dot name so this is how you get that information when you say dollar dot customer dot ID that will give you 7 when you have the list of items like this then if you want to get the book that is the first item in the list under that you want to access this field product then dollar dot items 0 array index 0 dot product that will give you the book similarly if you want to access this quantity 3 right dollar dot items okay so this array index is 1 right dot quantity that will give you 3 so this is how you use the JSON path to extract the information from the response body you can go to this GitHub project to see lots of examples like this to get a better idea if you have a very complex JSON response when we send this request API customers one that time the endpoint is going to give us the customer details object if you check the customer details we have these fields ID name favorite genre it is not like you have to validate each and every field in the response that is not the expectation since this is very small we can validate all but it's up to us actually okay if you want we can just validate only the ID part alone it's up to us so dollar dot ID should be equal to 1 it's a number in this case there should be 1 so that is what you are validating if you want you can keep chaining actually for example ID should be 1 the name should be Sam okay like this so if you if you want to finish with this you can just simply just do it like this way or if you want to validate more fields you can again keep chaining like this so then the favorite genre should be action right so that is what I am validating here so that said if you do not like this way of doing the assertion that is if you do not like the JSON path no worries I am going to show you another option let me just remove this okay after validating this 200 successful we are going to say return result and we are going to provide deserialize that response into customer details class so that is what you are saying now get response body will will be giving you the customer details the DTO okay so the DTO is customer details now you can use the regular Java the J unit assertions assertion asset equals the DTO dot customer ID okay so you can do you can assert in the regular style okay so we can also do this way if you if you prefer this but I kind of like the JSON path it looks neat in a declarative style we can keep chaining so this is why I am doing this way however it's up to you for the customer not found okay let me simply just copy these three lines and paste it here so using the test client we are sending one get request for the URI API customers let's give some ID which is not present let's say 10 then if I send the request expect status should be a 404 client error okay so this is what we are validating if you want to exact HTTP code validation then you can simply say is equal to 404 something like this you can validate this way or if you are looking for any 4xx client error you can simply just use this way okay let's quickly run this okay great instead of Sam let's let's keep it as Mike okay let's see if it fails okay so the customer not found test passed the customer details test failed because we expected Mike but the actual result is Sam okay so this is cool and our implementation works great
