# Customer Service: Architecture & Project Structure

In this lecture, let's focus on the customer service project structure. Let's come back to our IDE. Let's please go to customer service and check the form XML. Here I would have added the Netflix events as dependency. Then this customer service module will be including like the spring, the data JPA module, Kafka, cloud stream, etc. And here we will be using H2 as our database. If you expand source main resources as part of the data SQL. So we are going to have one table called customer and we will have only these columns. ID, name and favorite genre. When we start our customer service application, I want this application to have some customer information. So we will be automatically inserting these records like three customer information like Sam, Mike, John and their corresponding favorite genre. Then if you expand the source main Java, we will see the calm wins guru customer and under that we can see all these packages. As I had already mentioned, the Netflix starter project includes the boring setup. So this is our table, right? So to represent that, we have this entity class, ID, name, favorite genre with all the getters and setters. Then we have the repository, customer repository, which extends the JPA repository. The entity represents that database layer. The DTO will represent the API layer. So when we invoke the customer, get customer endpoint, that time we will be returning the customer details, right? So this Java record will represent that. So ID, name and favorite genre. Then we need a mapper to convert the entity into DTO or vice versa. So for that under mapper, we have the customer mapper class. So we have this. But as I said, the service, if you check, there is a class, but currently it's not implemented. So similarly for the messaging, we have one class, but there is no implementation. We have to work on this. But if you check the controller, it's already done. So these are the endpoints we are going to use. One get customer endpoint based on the given ID. We will be getting the customer information. Similarly, the patch endpoint to update the customer's favorite genre. The patch endpoint will have one request body, the genre update request. And if you expand the DTO here, we can see that this is the patch request, the body, and it will contain favorite genre. When we invoke the get customer by ID endpoint, there is a very good chance that we might provide the ID which is not present, right? So in that case, we will be throwing the customer not found exception. And we have the controller advice to catch that exception to return 404 response. Hopefully you understand the project setup. Please pause this lecture and explore the packages yourself so that you become comfortable with the project structure. Feel free to change the package names, etc. Or if you do not like certain structure, right? Again, feel free to change it. Do not change the API endpoints. So because we are going to have one demo, so that time I do not want things to break. Do not change the API endpoints. But if you do not like the project structure, feel free to change it. That's up to you. In the next lecture, let's focus on the service and the messaging layer.
