# Movie Service: Architecture & Project Structure

In the next few lectures, we are going to work on the movie service. We have one table called a movie and we have a couple of endpoints. One get endpoint to get the movie details by the movie ID. Then one post endpoint to add a new movie. That time our application will be triggering the movie added event via the movie events topic. As we did the customer service, right? So we will be focusing on the service layer, messaging layer and the integration test. Let's come back to our ID. Let's please go to movie service. If you check the palm XML, it will be very similar to customer service dependencies. This includes the Netflix events and the spring data, JPA, Kafka dependency, etc. And we are using H2, everything will be exactly same as the customer service dependencies. If you expand the source main Java under that convents guru movie, so we will be seeing similar packages. If you check the data SQL, now here you get the table structure. So here we have these many fields actually. I was able to get some test data. So the movies information here, it's a little bit difficult to see. So let me open that via the visual studio code. So okay. So here each line represents one movie object. This is JSON L, JSON line, okay? So each and every line is one JSON object. And I have close to 100,000 movies, okay? So we have 100,000 movies, that's a lot. As you see, we have title, vote average, vote count, release date, revenue, runtime, budget, overview. So along with that, we have genre, right? So we have a good amount of information here actually for each and every movie. So that is what we have here actually, okay? And our entity object will represent the database layer. So we have all these columns, okay, with the getters, setters, everything included. Then we have the repository. Then we have the corresponding DTO, the movie details with the same fields, whatever we have in the entity, but this record represents the API layer. We have the mapper, movie mapper to convert the DTO into entity, okay? So that is what we are doing here. And here, if you give the movie entity object from there, we can also create the movie added event object. If you check the exception here, we get, we have the movie not found exception. And if you check the controller, here we have the two endpoints. This is to create or add a new movie. This is to get the movie details by the given movie ID. When we throw movie not found exception, that time this controller advice will catch the exception and it will return 404. So if you see the whole project structure is very similar to customer service. As usual, the messaging, this class, we have to implement and the service, the movie service, we will have to implement. We also have one more class called the movie data initializer. This is something I just added, okay? So we have 100,000 movies, right? So what I am thinking is that when we start the application, we can use this class to initialize the data. That is, this is the command line runner as you see. So we are going to have one property called app.importMovie, something like this. So if you do not like this, you can disable this. So it's up to you. So what I am thinking here is by using this property, when we run the application, this will start. So that time, what we can do here is we can just import all the movies. Basically, we can load all the movies. Then we can insert the movie one by one slowly, okay? Every one second, one movie, every one second or so, something like that. So that the movie service, right, will keep on emitting the movie added event, okay? So that we can see how the recommendation service behaves. Otherwise, we have to use this data, we have to copy this and use postman to send a post request and all, right? So I thought we could use this to automate that. So that is the idea. If you are not sure, just ignore this for now, okay? Just this class, just ignore this for now. You will get it, okay? So when you see the demo, you will understand that time.
