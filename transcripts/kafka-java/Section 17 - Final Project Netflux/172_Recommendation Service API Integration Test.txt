# Recommendation Service: API Integration Test

In the next few lectures, let's focus on writing the integration test for our recommendation service. Let's start with the testing these APIs first. Let's come back to our IDE. Please go to recommendation service, source, test Java. Under that, here we have few test classes, right? Here I have the skeleton class, the test methods are not implemented. Let's focus on all these test classes one by one. As part of this test class, our goal is to validate the recommendation API and the recommendation, the stream API. To validate this, when we run our application, we should be having some data, right? Only then we can validate the API. So I am using the testdata.sql. It's in the source test resources, you can check. So when we, as part of the test, we basically insert these records, these nine movies with some genres. And here we have a couple of customers, customer one, customer two, these are their favorite genres. Assuming we have the data like this, how the API is going to behave. So let's test that. I am enabling the test binder and auto configure REST test client. So I can inject these two beans here. Now using the test client, let's test the recommendation API. So this.testclient get for the URI, API recommendations, customer ID, let's say one, exchange, expect status is 200 successful. Then return result. If you know, we are, we will be getting the list of movie recommendations, right? So new parameterized type preference, I will be expecting the list of movie recommendations. So this is what I am going to get, that get response body. So this will give us the recommendations like this. This could be null. So let's ensure that it's not null, assert not null, let's do that first. If it's not null, then we have the list and the list size should be two, because we have two different types of movie recommendations. One is the newly added, the other one is personalized. So let's validate that. So we have the recommendations size should be two. So that is what we are checking. Then in the list, we have only two items. The first item is for newly added. So let's test that recommendation type should be newly added, recommendations, get first that type and how many movies it should return. Actually, we, it will return 10, the newly added movies. In our case, we have only nine, right? So it will, it will be returning all the nine movies. So if you check recommendations, get first dot movies dot size. So this should be containing all the nine movies. Okay, now let's, okay, let me copy this and paste it here. Let's validate the second movie recommendation, which is personalized. So get last dot type should be personalized and get last movies size. This should be the, we are testing for the user ID, customer ID one, right? So customer ID's favorite genre is action. So this will give us all the action movies. So in our case, we have four. So this size should be four. Hopefully this is clear. And please let me know if you have any questions. So let me run this. Okay, perfect. Now let's focus on this.
