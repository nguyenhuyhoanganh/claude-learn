# Recommendation Service: Consumer Test Binder Test

In this lecture, let's focus on the next test class which is RecommendationServiceBinderTest using the test binder, we are going to emit these two events CustomerJournerUpdatedEvent and the MovieAddedEvent and we are going to check if our service class is updating our database tables properly so that is what we are going to do as part of the previous test, yes we emitted the CustomerJournerUpdatedEvent however, our goal was to test the streaming API, we did not test the database tables so here our main goal is to verify the database table update using these repositories let's focus on this test and to save some time let's copy these two lines and let's come back and paste it here so let's emit the CustomerJournerUpdatedEvent for the CustomerId1 and let's imagine that the customer's favorite journal is Action, let's keep it like this as part of this test, we are not using the testdata.sql here so when we run the test, we will be having the tables but there will not be any records so empty table, when we emit this event, our service class will be consuming this event and it will be inserting this record into the CustomerJourner table okay, so now using the CustomerJourner repository, we should be able to get the CustomerJourner entity for the CustomerId1 or else throw, it's optional, so we are getting the entity CustomerJourner like this, then using the assertions we are going to validate the journal is Action CustomerJourner.getFavoriteJourner, let's see if it works great, let's copy this and paste it here for the MovieAddedEvent, let's modify so as part of this, we are going to emit the MovieAddedEvent, so we have a few fields, so okay, MovieId is 1, the MovieTitle, let's keep it as Inception like this and the Runtime, maybe it's 100, let's okay, VoteCount 110, then ReleaseDate, I'm going to keep it as Null, doesn't really matter, then List of Journals, so we can say Action, so give some journal here, then okay, Null and then the Instant, Instant.Null, okay, so now we have the MovieAddedEvent and we are going to send that to MovieEvents topic, then using the MovieRepository, FindById, the MovieId 1 or else throw, so now this is the MovieEntity, okay, so now let's validate the title, okay, so it should be InceptionMovie.GetTitle, let's run these two tests, received MovieAddedEvent, received CustomerJournalUpdatedEvent.
