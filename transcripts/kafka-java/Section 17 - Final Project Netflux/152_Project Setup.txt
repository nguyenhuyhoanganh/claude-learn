# Project Setup

In this lecture, let's talk about the Netflix project structure. We have three services, right? In the real life, we will be creating three different Spring Boot projects. But for this course to make our life easier, we will use one Maven Spring Boot project with multiple Maven modules. Each module will represent one microservice. So let's keep it like this. As we had discussed, these services emit events and the recommendation service will be consuming these events. So I am going to add one more Maven module called Netflix events. Here we will have the Java records to represent the events. Then this module will be added as dependency by the other microservice modules like customer service, movie service and recommendation service. We are going to build this Netflix application together. So I want you to write code with me. If you have two monitors, you can use one monitor to write code and use other monitor to play the lectures for reference. You do not have to set up the project as I have already done that. If you check the GitHub repository for this course, or if you had already cloned that, I would have shared this here, 06 Netflix starter. This is the multi module Maven project we will use. If you are not interested in writing code, then you can get the completed the final code as part of this 07 Netflix final. At this time of recording this lecture, I have not yet done that. But I am sure by the time you watch the lecture, it will be here. The Netflix starter project already includes the boring setup. For example, we would need packages like the controller. If you are going to expose endpoints, we need the DTOs entity, the mapper to convert the entity into DTO or vice versa, etc, right? So I thought this could be boring. So I have already done that. It's part of the Netflix starter project already all these packages. Our main goal here is to focus on the service layer, messaging layer and integration test. So this is the main focus, not to spend time on DTO or creating the mapper classes, etc. This is already included. Assuming you are going to write code with me, please import the Netflix starter project into your favorite IDE like this. Once imported, this is what I want you to do first, right click on the main, the root pom xml, right click, scroll, maven sync project. Please do this without fail. Then open terminal, go to the project root directory, Netflix here, let's do the maven clean compile. It should be success like this. Assuming everything is good so far. Now let me explain the project. As we had already discussed, this is a multi module maven project. Here we have customer service, movie service and the recommendation service. We also have Netflix events. If you expand that, this module, it will have just a couple of Java records to represent the event like customer, genre, updated event here. As part of this, we will have a customer ID, favorite genre and updated ad. That's it. Then as part of the movie added event, we will have the movie ID, title, runtime, genres, etc.
