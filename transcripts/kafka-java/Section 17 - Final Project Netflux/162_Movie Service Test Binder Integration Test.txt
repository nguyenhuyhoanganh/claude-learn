# Movie Service: Test Binder Integration Test

In this lecture, let's write the integration test for the MovieAdded event using the TestBinder. So for that, I am enabling TestBinder. I am also auto-configuring REST Test Client. So we will be having access to these two beans. Using the REST Test Client, we will be sending the POST request with MovieDetailsRequestBody. That time application will be saving the movie and it will be triggering the MovieAdded event. We can validate that by using the output destination. So that is the idea. We do not want the MovieDataInitializer as part of the test, so we are disabling that. To send the POST request, we need the RequestBody, MovieDetails. So for that, I am going to use one movie. So let me simply copy this, 8 Mile. I like Eminem. So feel free to copy whatever you like. So here, let's come back, so var json. So let me paste that. So this is one movie object, right? So by using the var request, using the JSONMapper, Tools, Jackson, DataBind.SharedReadValue. Provide the JSON, the string. We are going to deserialize that as MovieDetails class like this. Then by using the testClient, we can send the POST request. So this.testClientPOST, the URI is going to be slash APA slash Movies like this. Then dot the body. Then send the request dot exchange. Then expect status is 200 successful. And we want the response body as well. So I am going to use return result. I'm going to say deserialize that as MovieDetails class dot getResponseBody. So I'm going to say var, this is response, right? Then we are going to assert this, assertions, assert not null. Let's ensure that the response is not null. Why we are not using JSONPath is mainly because this response object will be containing the movie ID, right? I want the movie ID so that we can validate the movie added event ID as well, OK? So because this ID is dynamic in this case, so this is why we are doing this way. First let's ensure that the response is not null. Then let's also add one more assertion that let's validate the title. Assert equals the title should be 8 mile like this. So response dot title. I want to add one more assertion here that the response ID, the movie ID should not be null. OK, perfect. Now at this point, we can validate the event using the output destination. So this dot output destination dot receive. So let's wait for like one second movie events. So that is a topic name, right? So that should be giving us the message of byte array. We are going to get the event by using the JSONMapper dot shared read value, provide the message dot get payload byte array. And we want to deserialize that as movie added event dot class like this. Now let's add a few assertions. First I'm going to validate the message key that should be same as the movie ID. So response dot ID should be same as message get headers get Kafka headers dot key. We are using test binder, so key and it's integer class. So these should be same. So this should be same. Similarly response dot ID should be same as the event dot movie ID. So this should be same. Then let's validate the movie title event dot title. So that's it. Everything looks good. We send the post request, we validate the movie response, then we validate the movie added event. OK, perfect. Let's run this. Great.
