# Implementing Real-Time Recommendations using Flux

In the previous lecture, we were talking about the sync and the flex objects, right? We are going to use that in our recommendation stream service in this lecture to implement the real time recommendation. So let's work on that. Let's come back to our IDE. Please go to DTO movie recommendations. This is one Java record. If you are going to send recommendations to the customer, then we need an object to represent that, right? So that is what this Java record is about. Here we have few fields. The first one is recommendation type. So it's an enum. So we have two types of recommendation, newly added, personalized. So that is what we have here. Then integer customer ID. If it's a newly added movie, it's a new movie recommendation, then the customer ID will not be set. It will be null because the new movie recommendation is for everyone. But when we say personalized recommend, it's for one single customer. So in that case, we will be setting up the customer ID. So for newly added recommendation, it's null. For the personalized recommendation, we will be providing the customer ID. Then the list of movie, the DTO objects, the list of movies you want to recommend as part of the recommendation type. So these are the fields we have. Here these are the static factory methods to create the instance of the movie recommendations. This is the object we are going to emit via the sync. So let's go to recommendation stream service. Here let's create the sync object, private final syncs dot many movie recommendations. So let's call this recommendation sync. Let's keep it like that. Recommendations dot many multicast on backpressure buffer. I am going to use the small queues size, queues, small buffer size. This is what, this is how we create the recommendation sync. Then let's create the flux. Flux of movie recommendations. Let's call this recommendations. Recommendation flux or recommendations flux. So recommendation sync as flux like this. We also need the recommendation service, private final recommendation service, recommendation service. So we are going to add this as constructor parameter. So Spring will inject this for us. Now let's focus on this method. When this method is triggered, that time the movie service would have updated the table with the new movie, right? So that is what it tells us. So when this method is invoked, we are going to ask the recommendation service, hey recommendation service, can you find the movie? Can you give me the movie information? So that is what we are saying. So event dot movie ID. So we provide the movie ID. So this will give us the movie summary object or movie object, right? So bar movie, okay. So now using, okay, using the movie recommendations, we are going to build the newly added. So in our case, it's a list of just one movie, just one, okay. So we have the recommendation here. So now I can, using the sync, I can simply send it. So this dot recommendation sync dot try emit next, I can simply just send the recommendation. So this is how you are emitting. Now this try emit next, right? In case of multiple threads, if it's not able to emit this item successfully, it will simply silently fail. So it will give you the emit result if you notice, okay. So what you can do here is there is also another option, emit next here, emit failure handler. So if you want the guaranteed delivery, so you can go for this option, that try emit next will be useful. If you think that when I emit multiple items using multiple concurrent threads, if it's okay to drop in that case, you can use for try emit next like this, okay. So if it's okay to drop an item in case of multiple threads, if it's okay, then in that case we can use try emit next. If you want guaranteed delivery with the internal retry, then we can use emit next. So let's use that recommendation. So emit failure handler. So here there is an option called busy looping, okay. So duration of seconds one. So what you are saying here is, try to send this. If it fails, then again retry, just to keep retrying again and again for one second. So that is what you are saying, okay. So keep on retrying again and again and try to send this, that is what you say. So when you say guaranteed delivery within this time limit, it will try to send, okay. So this is how you are sending the movie recommendation for the newly added movie. Okay. Now let's work on this method. When this method is invoked, the customer genre would have been updated. So the customer prefers a different genre now. So we will have to provide the corresponding recommendation for the customer, right. So for that, we are going to ask the recommendation service find personalized. So event dot customer ID. So now this will give us the list of movie summary. So let's call this movies, let's keep it like that. Then movie recommendations dot personalized. So event dot customer ID, then the movies, okay. So we build the recommendation like this. Then using the try, sorry, using the emit next, we can simply just send it. So I am going to simply copy that and paste it here. So using the sync, we emit the items, right. So why are the other end, which is this flex, the users will subscribe. Now when the users, when they invoke the API, the stream endpoint, right, that time this method will be invoked. So we have to give them the flex object so that the users can subscribe. That is the idea. So we have to return the recommendation flex. Now there is a small problem with our implementation. So this is our sync and this is our flex, right. So whatever I emit via the sync object, everyone will receive. That is what we had said. So which is okay for the newly added movie recommendation. So if there is a new movie recommendation, if you emit, everyone should get it. So that is fine. But if I send a personalized recommendation, not everyone should get it, right. So it's personalized. So it is for one that particular customer, right. So if I emit one personalized recommendation for customer ID too, only this customer should receive that. All other customers should not receive that. So with our current implementation, everyone will get it. So we have to filter in a way that the personalized recommendation for the customer ID too, only that customer should receive that. Other customer should not receive that. Luckily this is very easy to implement that. Here we are receiving one customer ID as the parameter. So that this flex reactive pipeline has one operator called the filter, which is similar to our Java stream pipeline filter operator, okay. So here you are receiving the recommendations, right, movie recommendation. So we are going to check objects is null recommendation dot customer ID. If the customer ID is null, we assume that it is a global recommendation, it is for everyone, okay. Or the recommendation customer ID equals whatever I have received as part of the input parameter. So this is what we are saying. Hopefully this is clear. Basically, when the customer ID too, when they subscribe to this flex, right, we will be receiving two here. So we are checking the customer ID should be null. In that case, we are allowing the recommendation or the recommendation customer ID should be 2. In that case, we will be allowing the recommendation. In all other cases, we will not allow. So that is what we are saying.
