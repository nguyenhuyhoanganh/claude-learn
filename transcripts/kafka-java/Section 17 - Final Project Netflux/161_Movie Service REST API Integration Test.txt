# Movie Service: REST API Integration Test

In the next few lectures, let's write the integration test for our movie service application Whatever the process we followed for customer service, right? That is what we are going to do here as well Please go to source test Java under CalmVinsGuruMovie here We will be seeing the three test classes as part of the movie API test We will be testing the movie details the movie not found 404 response, etc This is using the test binder and that this is using the test container. Okay same process Okay. So now if you notice I use the auto configure a rest test client so that we can use this as part of the spring boot test properties, I Set the app dot import movies as false. So basically we do not want the movie data initializer So this is there just for the demo purposes so for that test we can simply just set that to false Then I have the SQL annotation is mainly because okay If you see right as part of the data data dot SQL we have the create table But we do not have any data. So when you start this application The table will be empty basically, right? So how can we test the movie details endpoint so we cannot test this or if you want to test this First you have to do the same movie first only then we can invoke the the movie get details You're getting right. So For this what I am doing here is I have one test data dot SQL file under source test resources, so When we run the test this will basically just import a couple of movie records We are just setting only the title and runtime field. All other fields can be null. So no big deal we just want a couple of fields so that This we will be importing these two movie records into our movie table So that we can use this movie day we can validate the movie details endpoint. Okay, so this is why we are doing this Assuming everything is clear so far. Let's focus on this test. If you have any questions, please. Let me know via the Q&A section Okay, so this dot test client we are going to Send a get request for the URI. It will be like API movies one if you check It's a ID right? It's a primary key serial. So when you import When you insert this will be movie ID 1 this will be movie ID 2 so Okay, so this is what we are sending movie ID 1 so we should be getting inception exchange expect status Is 200 successful Expect body. Let's use the JSON path as usual Dollar run ID is equal to 1 JSON path Dollar dot idle is equal to Inception like this then JSON path you can also validate the run time if you want is equal to 150 For the movie not found. Let me simply just it copy these and paste it here So if I send the request for the movie ID 10, that should not be present. So I should be getting Okay, so client error perfect let's quickly run this test
