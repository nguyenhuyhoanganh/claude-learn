# Recommendation Service: Consumer Testcontainers Test

In this lecture, let's focus on the recommendation service Kafka test class. This will be same as the previous one, but instead of using the test binder, we are going to use the Kafka binder with a test container. So that is the idea. So again, to save some time, I'm going to simply copy these two test methods. That is what I'm having here. If you see customer genre updated event and the movie added event. So simply let's paste here to save some time. Now let's update. Instead of using the input destination, we are going to use the stream bridge to send that the event. Okay. So this start stream bridge. Okay. So the stream bridge accepts that topic first. Then here you provide the, here actually you can, you don't need to give the message object. You can directly give the event so we can simply just to keep it like this. Okay. So let me copy this and paste it here. So here we are going to send that event to movie events. As I had mentioned the multiple times as part of the previous lecture, you definitely do not have to repeat the same test using multiple binders as I am doing here. I am doing this as mainly because we are practicing. So I am, I like to show all these things to you, but in the real life you can just prefer that test container test. Okay. Now you understand what we are doing here is right. So we are emitting customer genre updated event. At this point we are querying the customer genre table using the repository and we should be getting the customer genre entity and it should be containing action. Very simple test, right? But if you run, it might not work here. We are launching test container. See it failed. As you see here, it throws this exception. So the fine by ID returns an optional, it's empty. So it throws this exception here. Everything looks good. However, this time we are bringing the actual Kafka container, the Kafka broker. So when you run the test, that Kafka container is starting. Our application is connecting to the Kafka broker. But if you remember, whenever we run the consumer applications, right, we will be waiting for some time. We will wait for the partitions to be assigned, right? So it takes some time. We send the event to proper Kafka topic, but the application will not receive that immediately. In the previous test here, everything happened super fast because there is no Kafka broker here. It's an in-memory queue. So whatever you send, application will receive it immediately. So you should be able to validate as part of the next line, next statement like this. But here it does not work like that. Here we have to wait for some time. We have to wait for the table to be populated. So only then we can validate. So it will take some time. For that, we are going to use the availability library. So I'm going to have one helper method, private void waitUntilAsserted. So here I'm going to accept the throwing runnable. So this is what the availability uses actually, throwing runnable like this. So waitility of wait at most, let's say a duration of 10 seconds, but we are not going to wait for 10 seconds. It's at most 10 seconds. That's what we are saying until asserted pass the runnable. Then let's cut these two statements and I'm going to use this.waitUntilAsserted and I'm going to provide that as runnable. Now let's run this. There is a very good chance that it will still not work. First let's run this. Okay. In your case, maybe it works for you, but if you scroll down, right, I'm still getting the no value present exception is mainly because this waitUntilAsserted, right, or this throwing runnable, right? It has to throw assertion related exception. So the no such value, no value present, all those exceptions, it will not accept that. It has to be a assertion failure exception. So we have to say in that case, throw assertion failed, okay? So this is what we are going to say, assertions failed, okay? So this is the exception we want to throw. So now if you run this, it will wait properly. And if you see now it takes some time. See received. Okay. It works great, right? Let's rerun this. Now, if you notice that this took three seconds, it was waiting for the partitions to be assigned. And it received the event properly. Then it validates, okay? So it works, right? Let's do the same for this test as well. This dot waitUntilAsserted. Let's keep it like this. Now let's run these two tests together. Great.
