# Recommendation Service: Server-Sent Events (SSE) Integration Test

Using the REST test client, we can test the stream APIs. However, we will be facing some challenges. If you have very simple requirements as we have, yes, this will work, but if you have more complex streaming requirements and all right, this might not work properly. In those cases, I will highly recommend WebFlux and the corresponding web test client. So they support streaming very well. Streaming is a first-class citizen in WebFlux, but with the REST test client, we only have very limited support. As part of this stream API, as you know, we will be getting an item or the recommendation only when the recommendation service consumes an event, right, like genre-updated event or movie-added event. Only then this will be triggering one recommendation. So we are going to create one event. So let's generate customer-genre-updated event for the customer ID two. And let's use some genre. Currently, this is, okay, the customer's favorite genre is adventure. Let's say now the customer changes the genre to the science fiction, okay? So let's use that. Okay, so this is how we are going to provide, oops. Okay, oh, we also have to provide the instant, instant dot now. Let's keep it like this. Now we have the input destination. So using that, we can emit this. So this dot input destination, send. So we have to use the, we have to build the message. We have to send the message object with the payload event dot build. Then we have to provide the topic name. So this will be customer events. At this point, our service class will be consuming this event. It will be updating the database table and it will be triggering the real-time recommendation, right? So we should be able to get this dot test-client-get-request. The URI is going to be API recommendations. Since we emitted this event for customer ID, so let's use, customer ID two, so let's use that dot stream like this, okay? Then exchange expect status is 200 successful. Is 200 successful. Then the return result. If you remember, we were returning the flex of movie recommendations, right? However, you cannot use movie recommendations here is mainly because again, the test-client does not have the proper decoder. To better understand this, let's use string dot class. That is how we are going to decode this. I'll explain what is going on. Get response body. So let's say var response. Let's keep it like this. And let's simply just print the response. I want to show you this. Let's run this. Okay, so we have received that event, but I'm getting null. API recommendations. Actually, it kind of happens too fast. Maybe it's because we are running in the asynchronous mode. Thread dot sleep, let's use thread dot sleep, okay? For 100 milliseconds, let's keep it like this. Okay, perfect. So we have got the response, which is good, but let me go to the recommendation stream service. Let's test this theory. Let's comment this async. Let's come back quickly and let's comment this as well. Let's test this. Assuming now we are not using async mode, now we get the response properly because now everything is getting executed in the, using the main thread. Okay, perfect. Since we are using a separate thread pool, so we might need thread dot sleep here. 100 millisecond, it's decent, so we can keep like this. This is one challenge. The rest test client exits immediately. The web test client doesn't do that. So we have to delay this by 100 millisecond so that it sees some response. So this is why we are doing this. So this is one problem. Another problem here is we are using the string dot class. Why? It's mainly because if you run this, now, if you see the response, right, this is not proper JSON. So this is JSON. And if you see, this is how we get data colon, then you have the JSON, right? So you cannot decode this into movie recommendations. So it happens, it's mainly because this is part of the server sent event, the SSE specification. This is how we are supposed to receive the response, actually, or this is how the server is supposed to send the response. So the data colon will be there always. The web test client comes with the proper message converters so it can understand this, but the rest test client does not understand this. So if you use the movie recommendations like this, right, if you try to deserialize this response into movie recommendations, this will fail, saying that, hey, the response is not in the proper format. I cannot decode this. That's what it says. No suitable HTTP message converter found. So, okay, so these are some of the challenges we have with the rest test client. So what we have to do here is, first, we are going to ensure that the response is not null, okay? Then we have to remove the data colon. So this is how we are going to do. So, JSONMapper, tools.json, share, read value. We are going in the response, right? Substring, index, fine. Basically, we are removing the, to remove data colon, okay? So this is what I am doing. So to remove that in the response, I am using index file. It starts from index file. That is what we are saying. Then deserialize that response into movie recommendation class. So by doing this way, we will be getting the movie recommendation. So let's print that. Now, if I run this, now this works. I get the movie recommendation. Now we can add assert statements as we have the recommendation object. Assertions, assert equals. First, I'm going to check the recommendation type. Since we emitted the customer genre updated event, so the recommendation type should be personalized. So let's validate that, okay? Then, since it's a personalized, we should be having the customer ID as well. In that case, the customer ID should be two. So let's test that. Then how many movies? Okay, so movies.size. Since this is a science fiction, right? So how many science fiction movies are there? Four. So we should be having four movies here. Okay, so let's validate this. Let's run this. Great. I have added some comments here. Here we are using thread.sleep is mainly because the recommendation stream service uses asynchronous mode to publish the event and the test client does not understand that properly. Also, here we are using string.class is mainly because this test client does not have the proper HTTP message converter for the streaming response. Since it does not have the message converter, then it becomes our responsibility to deserialize this. So the response format, it will be like this. So we have to remove the data colon. So this is why we are using substring. Then we deserialize this.
