# Customer Service: Test Binder Integration Test

In this lecture, we are going to write the integration test for the genre updated event. That is using the REST test client, we are going to send one patch request. So that time our application should be triggering the genre updated event. So we are going to validate that by using the output destination. So that is the idea. We are going to do that by using the test binder. First we have to build a request object, new genre update request. It has one single field. So it's very simple. Let's use Mike currently, currently his favorite genre is comedy. Let's imagine that Mike wants to change that to thriller. So let's keep it like this. So now we have the request object. So using the test client, we are going to send one patch request for the URI API slash customers. The customer ID is 2 for Mike and genre. So this is the URI body provide the request, then dot exchange, expect status is 200 successful. If you check it's 204. So if you want to validate exact 204, you can simply say is equal to, you can validate like this is no content. You can also validate this way. Maybe if you want to use this, let's use this. Okay. It's no content. We can simply just validate like this. At this point, we should be expecting the customer genre updated event in our output destination. Right. So let's do that. This dot output destination dot receive. So let's say we are waiting for like thousand milliseconds for the topic name customer events. So this will give us the message. So the message of byte array, that is what it's going to give us. Right. So we want to extract the customer genre updated event, that payload. So var event equals jsonMapper, tools, Jackson, data bind, json, please use that, shared read value message, get payload, provide the byte array. And we want to deserialize that into customer genre updated event class like this. Okay. So if you do this way, sorry, read value, not values. Okay. So now this will give us the customer genre updated event. Now we can assert assertions dot assert equals, okay, let's say to event dot customer ID should be two. Right. Assertion, the favorite genre. If you check the favorite genre, that should be thriller, because that is what we should be seeing in the event. We are also emitting the message with the key. So let's validate the key as well. Let me do that first. Okay. Assertion, assert equals, the key should be two, right? So message, get headers, get Kafka headers, dot received key, we should not be using this is the test binder. So we should be using key, remember that. And it's an integer class. Okay. So let's get that. So first we are validating the key, the key should be two. Then the event customer ID, that should also be two and the event favorite genre should be thriller. That is how we send the request. So the event should be containing the thriller. Everything looks good. Now let's run this. Wow. Great.
