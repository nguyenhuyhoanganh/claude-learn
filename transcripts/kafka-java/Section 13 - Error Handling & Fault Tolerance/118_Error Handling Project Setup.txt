# Error Handling Project Setup

In this lecture, let me explain the project setup to play with the error handling using spring cloud stream If we imagine we are exposing a bean like this then under the hood This is what the spring cloud stream does in a very high level again. It's a pseudocode Spring will create an instance like this. Let's call that handler then spring will fetch Messages from Kafka and gave them to the handler like this If the message is processed successfully the offset is committed in case of exceptions Depending on the retry configuration in the YAML file. It will retry Again, this is what happens in a very high level So to play with the error handling our consumer bean is going to be like this We are going to consume messages of integer type. Let's say These are all order IDs If the order ID is lesser than 1 then we will throw input validation exception Okay, so it's a non retry able error If the order ID is greater than 5 Then we will throw some service unavailable exception at random to simulate intermittent issues In all other cases the message is processed successfully we will test this by producing messages via console producer Let's come back to our IDE. Let's please go to section 16 package here we have one consumer application and we have one exceptions package for the input validation exception and Service unavailable exception if you expand the consumer package Let's check the order service It's a spring component. It's very simple here. We have one public method save order and here we pass some ID order ID First we check the order ID if it's greater than 5, we will be throwing some exception at random So it might throw the exception or it might not throw the exception If you are not going to throw the exception Then we will be printing this message saying that the order is saved successfully something like that as you see it's a very simple Class, let's go to the consumer config here. We auto wire the order service This is where we will be receiving the messages whenever we receive the message We give that give the message to this private helper method So the first thing what we do here is to print the message with all the headers I would like to show you certain things. So let's do this Then we get the payload We call that order ID, right? If the order ID is lesser than 1 then Okay, so we are going to throw this exception input validation exception Okay in all other cases we can save this order So we are going to use this service class to save the order ID. So this is how we are going to play with the error handling
