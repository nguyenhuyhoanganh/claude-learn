# Demo: Fixed Delay Retry Strategy

In this lecture let's see a demo of how retry works using a fixed delay strategy the number of retry attempts the delay etc will be part of the configuration in our spring cloud stream configuration for the consumer in zero binding here we provide the destination group name etc right then we provide the retry configuration for the binding so the maximum attempts three this is the number of retry delivery attempts back off initial interval it's in milliseconds so as soon as the exception is thrown we are not going to retry immediately so we might want to wait for some time before we go for the retry attempt so this is the way time before the first retry so in this case we are going to wait for two seconds then the back of multiplier when you have multiple retry attempts here we provide only the initial interval right so what about the subsequent retry attempts wait time so that is when we use this if it's one then it's fixed so all the retry attempts will be delayed by two seconds for this based on this configuration if you give two here the back of multiplier if it's two right so the first way time will be two seconds the second attempt wait time will be like four seconds and the third attempt wait time will be like eight seconds and so on so that will be like exponential back off as usual I have shared some instructions via this readme file for this demo please ensure that you are having the fresh Kafka container the first demo is going to be about retry with the fixed delay in our section runner configuration ensure that you are using this this is the config file we are going to use then we will be opening two terminals one is optional let me explain one by one the first terminal is for console producer using that we will be producing messages like 0123 and so on and we are going to observe the consumer application behavior when it throws exception how the retries are applied the wait time etc all those things we are going to observe by using the console producer then the second terminal again it's optional you can use this to see when the message is getting acknowledged or when the offset is committed you can see in case of success it will be acknowledged immediately in case of failure during the retry attempts the offset will not be committed you can see all those things by using this first I am ensuring that I am having the fresh Kafka container by doing the docker-compose down and docker-compose up let's please go to section 16 section runner here please ensure that you are using this configuration let's please go to resources section 16 retry fixed delay yaml whatever we had just discussed right so that is the configuration I am having here however the back of initial interval I'm going to change that to 5 just for demo purposes you can have this as 2 but for me I want to show you certain things so I want some time so I am keeping this as 5 seconds okay perfect now let's run this here I am having two terminals one is for console producer then another terminal to check the the consumer group status so for us since we have not produced any message it's zero so let's produce the first message let's say one they say one they are received successfully and if you check the status that the current offset is one the lag is zero so let's clear this okay then let's produce messages like two three four etc as you see they are all getting saved successfully and we do not have we should not be having any issues here so the current offset is four if you check the message headers if you scroll to the right here you can see the delivery attempts right so here it says one because this is the very first attempt for that message and the message is getting processed successfully so the delivery attempt is one but in case of failures right spring will be retrained that time we can see that the delivery attempt is increasing okay so we can see that now let's try to produce zero so the order ID cannot be lesser than one right so in this case we are going to throw input validation exception however we are going to retry I know that we had discussed that it's a non retryable failure but we have not provided the configuration so by default spring will be retrying all the exceptions okay so this is intentional later we can modify this behavior but for now we are going to retry this let's publish this here we are not getting the order saved successfully and if you check the delivery attempt it will be one two and so on as you see meanwhile if you check the consumer group status right here we see the lag so after four okay so now let's check the lag it should be zero what happens here is during the retry attempts spring will not be committing the offset of course but after all the retry attempts it will not know what to do okay we have to go to the next message right so it will be committing the offset by throwing the exception so this is what we said after all the retry attempts so we have to take some decision so in this case we are simply throwing the exception and we are committing the offset we can also send that to a dead letter topic we will come to that later but for now this is what we are doing if you check the console output here we can see the invalid order ID exception and if you scroll up we can see this exception multiple times because we were retrying multiple times right so spring gives all those exception details so scroll up so here for the message the payload zero right this is the first attempt okay so then we had a three retry attempts after all these attempts if you are going to still get get the same error then it will be throwing the exception saying that retry attempts exhausted so it will be simply throwing this exception and it will be committing the offset now our consumer application is ready to consume the new messages so let's try to produce more messages okay let me scroll down so we can produce messages like one two we can see all those things now as you see okay four five let's try to produce six now when you produce six you will be having the intermittent issues right so let's try to produce six now okay so the very first time itself it worked okay service unavailable now as you see we are going to retry after five seconds okay so now as part of the retry it worked so this is great
