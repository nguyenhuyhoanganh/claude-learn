# Demo: Exponential Backoff Retry Strategy

In this lecture, let's see the retry with exponential delay, for that this is the configuration we are going to use. Let's check the resources. Section 16, retry exponential delay. Here everything will look exactly same as the previous one. Maximum attempts 3, initial interval 2 seconds. The back of multiplier, it's 2. That is what I am using. It's a double value, you can give whatever you like. So what it means is that the first retry attempt will be happening after 2 seconds. The subsequent retries like the second attempt, third attempt will be multiplied by 2. That means the second retry attempt will be happening after 4 seconds. The third retry attempt will be happening after 8 seconds and so on. So it will grow exponentially. You can also set the upper bound. You can keep on growing exponentially, but not more than 10 seconds. So that is what you are seeing here. Let's run our consumer application. Let's produce some messages, 1, 2, 3, 4, OK. So everything is good so far. Now if I produce zero, you know what will happen. This time we will be throwing exception and if you check that timestamp, right? Here you can see the exponential delay. So we have one more attempt and if you see it takes actually more time. Let's check the message timestamp, scroll up, OK. So this is the very first attempt, OK 4630. So now at this point we are throwing the exception, right? So the first retry attempt happens after 2 seconds, OK. Then the second retry attempt happens after 4 seconds. Then the third attempt happens after 8 seconds, OK. So the delay time grows exponentially as you see. Once the retry attempt exhausted, Spring will be committing the offset and it will be ready to consume the next message. If I produce 1, 2, 3, 4, etc. Now here they are all getting consumed, OK. How about 6, service unavailable, OK. Very first time itself it works. Service unavailable. Now we should be retrying again service unavailable, OK. So third time, wow, finally it works. So this is great.
