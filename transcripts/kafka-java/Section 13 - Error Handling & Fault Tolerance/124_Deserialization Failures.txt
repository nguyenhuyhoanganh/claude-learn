# Deserialization Failures

This will be a quick lecture. This is something I wanted to demo, but I forgot as part of the previous lecture. So I'm going to do this now. That is, sometimes the message, whatever we try to produce, spring might not be able to decode or deserialize properly. That is, when we produce 1, 2, 3, 4, everything gets processed successfully. When I produce minus n, it could be an invalid order ID. So we get this minus 10 as invalid already in the DLQ. But still, minus 10 is an integer, right? So spring will be able to deserialize this message properly and give it to the application. Application throws invalid order ID, so this is why we are seeing that in the DLQ. Sometimes the message could be correct in a way that spring might not be able to deserialize this at all. For example, if I say Vinod, how will you be able to deserialize this into an integer you are getting, right? So in this case, it's a problem, right? So in this case, spring will be throwing that exception. So even that message will be going to the DLQ like this. So here we see Vinod and the issue is mainly because it receives one byte array and it's not able to cast that into an integer type. This is what I wanted to show. That's it for the demo. Let's stop this application.
