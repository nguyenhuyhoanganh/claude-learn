# Pause/Resume Project Setup

This will be a quick lecture and in this lecture let me explain the project setup what we have for the dynamic pause and resume of consumer bindings. In our project we are going to have one producer that will be producing messages periodically like one message per second and so on depending on the configuration. We do not have any external dependencies for our consumer application. Our consumer will be having one health check manager. So we are going to simulate like every 10 seconds or so it will pause the binding and it will resume the binding. That is first it will allow the messages for like 10 seconds then after 10 seconds it will pause. Then after 10 seconds it will resume and so on. So this is what it's going to do. Let's come back to our IDE. Let's please go to section 17 package. Here we can see the consumer and the producer packages or our consumer producer applications. If you check the producer config this is nothing new. We have seen this many times. So we are going to produce like message 1 message 2 message 3 and so on. And if you check the producer configuration this is nothing new. I'm using the fixed delay 1000 millisecond. So every one second we are going to produce messages to our demo topic. Then if you check the consumer application and the consumer config this is very simple. They have seen this many times right. This as well. And if you check the consumer YAML the configuration YAML demo topic demo group nothing special. Then we have the consumer lifecycle manager or health check manager whatever we want to call this. The name doesn't really matter. It's a spring component. Here we are injecting the bindings lifecycle controller. This is our consumer binding right consumer in zero. So I am having one string constant for this. I'm calling this consumer channel. Then here I have a couple of methods. One is for pausing the binding and the other one is for resuming the binding. I'm using the change state method. Provide the binding name and provide the state like paused resumed. Then we have one method. It's going to be scheduled or it's going to be executed every 10 seconds. Ideally as part of this method we should be doing some health check etc. But this is demo purpose. This is we are doing for demo purposes right. So what I am doing here is currently I am checking the binding state. If it's paused then I resume. If it's if it's in the running state I pause the binding. OK. So basically we are going to toggle. As you see it's a very simple implementation. Now you might be having one question here that. Here as part of the change state we provide the binding name and we provide the state. Right. It's a very simple but the query state right. It gives us the list of binding. How come that is the consumer in zero is like one single binding. Right. So we should be getting one single state. So why are we getting a list here. OK. If you check the configuration configuration YAML. So here we are having only one consumer in zero. Right. But this is a logical consumer. OK. It's a logical binding. However at runtime we could have multiple instances. So this is why we are having the list. OK. If you are confused. Think. We have used concurrency. If you remember. Right. Concurrency 3. Concurrency 10 and all. So the consumer in zero is one logical binding. When you use concurrency you are having multiple instances of this. So this is why it's a list of bindings. OK. So basically by querying this binding name we get multiple instances. We check if they are all paused. If yes then we resume. Otherwise we pause.
