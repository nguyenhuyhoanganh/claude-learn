# Kafka Message Attributes & Message Builder

In this lecture, let's discuss the Kafka message attributes The Kafka message we send can contain additional metadata along with the actual event These are the attributes normally we might be interested in The message key we have discussed this then value is the actual payload or event Then we can set additional headers any key value pairs you are interested in sending Then I am stamp the event time To support that spring has one message builder object using that we can build a message of type Using this message builder We will be setting the payload the event then we will be providing the key Order one something like this Then we can also set Any other information like the order sources web and so on additional headers Then we will be building the message object. So we will be building one message of order event like this Then our producer side instead of sending the order event Directly, we will be sending the message of order event The message contains the event key, right? So this is how we will be sending the message Similarly the consumer can also receive the message type if it wants like consumer of message of payment event Similarly the processor can receive the message type and it can return Another message type with the key like this Please do note that the producer has to send the message type because This type has the key. This is just the event actual payload, right? So this is the type which has the key and additional metadata. So the producer has to use message type But the consumer might not be interested in the key all those things Okay might not be interested in that case. The consumer can still directly receive the actual event if it wants So the consumer side the message type is optional similarly for the processor At the receiving side that this message type is optional But if you are going to set key, then you should be returning the message type
