# Optional: Creating a Reactive Producer

This lecture is optional. This lecture is for developers who currently use reactive programming in their application. To create a Kafka producer application, as usual, we will be creating a supplier bean like this. The supplier will be returning a flux of T. Since flux represents a stream of data, the framework does not need any pooling interval. The flux can emit data whenever it wants. It's completely up to us. The framework will be internally subscribing to the flux and it will be emitting data whenever it receives data in the reactive pipeline. Let's come back to our IDE. Let's take a look at some code. Let's go to section 0.5. Here we have consumer and the producer. This consumer we have already seen the reactive consumer. Nothing special here. If you go to producer, we are returning supplier of flux of string. I use the flux interval so I can emit data periodically so we can build a message one message to something like that. Then using the do on next operator before I send the message to Kafka, I simply print it on the console. Nothing special. So this is the reactive producer. For the section 0.5, I have corresponding configuration YAML files. One for consumer, reactive consumer and one for producer. Again this configuration YAML files, they do not care if you are using reactive programming or not, they will look exactly the same. So now let's run this. Let's go to the section 0.5 section runner. Ensure that the consumer is pointing to 0.1 consumer. Similarly, the producer should be pointing to 0.2 producer. Perfect. My Kafka server is up and running so I'm going to run the consumer. OK, so it's ready. Now let's run the producer. The flux interval, it starts from zero. So because of that, I am seeing this, but it's not an issue. So we are emitting data. So here we are receiving the items. Perfect. What if I want to send an event with the key? Same concept. Instead of returning flux of T, we will be returning flux of message of T. The message contains the event key and other attributes as you know. So this is what we will have to return as part of the reactive pipeline. How to produce messages dynamically? So do I need a stream bridge? Actually, you can use stream bridge if you like it. OK, so the stream bridge is not blocking API. So you can absolutely use stream bridge if you like. But you do not need it. You can simply use the sync object as we normally use in the reactive programming. For example, let's imagine that you are creating one sync bean like this. Sync.ManyMulticastOnBackpressureBuffer. This is how we create the sync object in the reactive programming. OK, now Spring Framework wants flux. So from the sync object, we create the flux and we simply return our producer, order event producer like this. So Spring Framework will be subscribing to the flux. Now we have the sync. You can simply auto wire the sync object in your service class wherever you want. It's completely up to you. Then we will be saying sync.TryEmitNext. Then you emit the order event. Now Spring will be getting that event and it will be sending that to Kafka topic. So producing messages dynamically is very easy in reactive programming. We do not need a stream bridge, but if you like the stream bridge concept, you can absolutely use that as well.
