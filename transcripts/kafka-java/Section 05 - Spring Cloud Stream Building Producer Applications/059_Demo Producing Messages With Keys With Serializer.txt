# Demo: Producing Messages With Keys With Serializer

In this lecture let's run our section 03 setup to produce and consume messages with keys. Everything is ready at my side. So first let me open the terminal. I am going to delete that demo topic. So if you have that topic already, you can delete this first. We can recreate that topic with two partitions. So it is not like you should have a topic with two partitions. It's not really like that. Since we are producing messages with keys, why not? So let's create a topic with two partitions. So that is the idea. I have shared these commands via the readme file. Then as I had mentioned already, the delete is an asynchronous process and it won't let you delete the topic if the producer or consumer is still connected to this topic. So remember that. Let's run the consumer first. Wait for a few seconds. So now we see the two partitions assigned. OK, so let's go to the producer and run this. Look at that. Now we are able to produce messages without any issues. OK, perfect. You can observe the message, the structure. So this is the payload. It has some unique message ID. This is the actual message key, whatever the producer is trying to send. And we are setting some trace ID, right? That timestamp is automatically set by the client. OK, perfect. So great. Now let's go to consumer. If you see here, we are getting the key 32, payload 32, message 32 and so on. And if this is the actual the message object, it contains the payload and the headers. The delivery attempt and all, we will be discussing that in detail later. So no worries. So. The producer was setting message key, right? But here, if you see, it's Kafka received message key. So this is why we were using that string constant. So it also contains from which topic the message is coming from. So when you subscribe to multiple topics, right? This will be helpful. And the offset. It says offset 45, 34, because remember, we have two partitions. OK, so we get the trace ID as well at the consumer side. Great. And here we are getting the partition ID, partition information as well. Perfect. Let's stop this. Let's have another quick run. That is, the producer is going to produce messages with the keys, custom attributes. But consumer might not be interested in those things. OK, so for that, let's point this consumer to use 01 consumer YAML. So that it will be using this. We are not using this. But the producer is going to produce messages with the keys. That is what we are going to do. So let's quickly run this and see. OK, so this is something we stopped the consumer first. Then we stopped producer later because of that the topic had some messages. OK, so it's already confirms that it's working. But let's run this one more time quickly. So now if we come back and check here, we get the messages as usual. OK, so it is not like you have to consume the message of type T. You can also directly ask for the payload. Spring will just give the payload to you directly like this.
