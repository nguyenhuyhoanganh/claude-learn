# Creating a Simple Producer

So, this is the demo we are going to have, we already know how to develop a consumer application. So, we will be running one consumer application, then we are going to have one producer, we are going to run this as well, it will be periodically sending messages to a Kafka topic and this consumer will be subscribing to that Kafka topic, we are going to see if it gets all the messages ok, so that is it, very simple demo, let us do that. Let us come back to our IDE, let us go to section 03 package, under that I have two packages as you see, one is consumer, the other one is producer, I want us to see these two packages as two different applications, that is how we are going to run. The consumer package is having the Kafka consumer application implementation ok, this is something we have already seen, we know this. Then under producer package, we have the Kafka producer, the implementation is very simple if you see, here we are returning a supplier of string, I name this method as producer, so whenever the supplier.get method is invoked, I want to return message 1, message 2, message 3 and so on. So far that I need one counter, so I create one counter here, then whenever the method is invoked, we are incrementing the counter, so that it will be returning message 1, message 2, message 3 and so on, very simple implementation.
