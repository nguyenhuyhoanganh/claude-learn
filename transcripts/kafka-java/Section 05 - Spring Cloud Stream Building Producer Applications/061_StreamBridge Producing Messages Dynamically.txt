# StreamBridge: Producing Messages Dynamically

In this lecture, let's discuss how are we going to solve this problem in the synchronous style programming The Spring Cloud Stream module provides a spring component called a stream bridge You can auto wire wherever you want in your application using that you can send messages for example In a service class, I can simply auto wire stream bridge like this Then I can simply say streambridge.send I can have to provide the binding name then I can provide the my event object Very simple, right? So whenever we think that the supplier or function model is not Sufficient so that time we can use stream bridge to send the messages Just to give you an idea. Let's say we have one rustic controller like this Here we have two end points One end point is for viewing the product. The another end point is for placing an order just to keep things simple I am auto wiring stream bridge So whenever this method is invoked with the product ID, we are emitting an event streambridge.send Here we provide the product view binding name then we give the object Similarly whenever the place create order method is invoked customers place order Stream bridge dot send here. We give the binding name for our event. Then we give the the event object That's it. Very simple for the producer configuration When we were using the supplier type the binding name will be derived It will take the B name out zero then we will be providing the destination like this Okay, perfect. If we are going to use stream bridge instead of this supplier type then what is the binding name? Okay for this there is nothing to derive from so we can Create or we can use our own binding name like this Spring cloud stream bindings order events out product view out So we can come up with our own binding name like this then using this binding name we can send the event Stream bridge dot send product view out product view event You will be giving the object like this the stream bridge it will take this name and it will check Oh, I have to send that event to product events topic. This is how it will send Similarly for the order events out it will check this it will take this topic Then it will take this event and it will send to this topic Now, let's consider this scenario in this case the stream bridge It will come and check here, but the name is not defined here in this case The stream bridge will assume that this is the topic name So it will take this event and it will be directly sending to this topic I Would personally not recommend this for actual production application However, this will be useful to write integration test If you want to produce messages with the keys as usual You can use the message builder to build the message of type T then you can send that using stream bridge
