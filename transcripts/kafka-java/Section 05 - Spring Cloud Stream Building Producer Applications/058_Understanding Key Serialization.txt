# Understanding Key Serialization

In this lecture, I'm going to show you one problem. So, please do not run this just Observe what I am doing. Okay. So at this point we have the consumer and we have the producer so to run these I Have created two new config files under section 03 03 message consumer yaml 04 message producer yaml Okay So if I if you check this basically same as the previous one But this time we are activating this message consumer been whatever we have created here Message consumer message consumer in 0 same topic demo group everything else remains same Similarly for the message producer. We are activating this bean Message producer out 0 same demo topic Okay, so everything remains same then the section runner Here the consumer is pointing to 03 message consumer The producer is pointing to 04 message producer. So hopefully you get the setup, right? Okay, I have just restarted my Kafka server now. Everything is ready From my side. So I am going to run the consumer So let it run let's wait for the partition the assignment, okay, so this is done now if I run the producer I Will be facing an issue. I just wanted to show that first Okay, so this is exactly what I wanted to show so we will be getting some exceptions like this We try to send a message more or less like this With a key some custom attribute or header and a payload We had already discussed that the broker stores Everything as bytes the serialization and the deserialization should be handled by the applications Let's take this message the spring framework tries to send this right The spring does not have any issues in converting the payload into bytes Because it knows that its order even type payment even type or string type and so on We explicitly say message of type P. So no issues here For the custom attributes spring can treat this as string values and they can be converted to Bytes as well not a problem here But when it comes to key spring does not do any guesswork No assumptions because the message key plays an important role in determining the partition And it could affect the message ordering so we must explicitly mention how to deal how to serialize at the producer side and How to deserialize at the consumer side? To better understand this let's say we produce a message With a key like this. The value is one But what is its type should we treat this as short or int or long or? string Because depending on the type we will be getting different byte Representation and that is what is going to be used to determine the partition as you see So the spring framework does not want to guess this It expects us to provide either raw bytes. You can directly provide bytes as your key or We have to set the serializer and deserializer here We had seen this slide in the previous section if you remember the Kafka library comes with Some serializer and deserializers we could use since our key is a string type We will be using the string serializer at the producer side and the string deserializer at the consumer side But if you want to use in long double, etc. You can use the corresponding serializer deserializer Let's come back to our IDE So to fix the problem for the message producer yaml since we produce messages with a key string key Then as part of the Kafka binder producer properties We are going to say use this string serial serializer. The string serializer is part of this package So key dot serializer is the producer property. We are saying that use this string serializer Similarly for the message consumer we are explicitly saying that hey consumer you are going to get a key and You can deserialize using this string deserializer
