# Demo: Producing Messages With Keys

let's come back to our IDE in this lecture let's produce messages with the keys let's go to section 03 our producer config class so I am creating one helper method called a build message it's going to get some integer input we are going to return message of string so the message type right okay so if I type message you might be seeing a lot but particularly we are interested in the spring framework messaging message so that is what we are going to return return message builder we are going to use message builder again you will be seeing you might see more than one in this case we are interested in a messaging support this is builder with payload so here I am going to say message like this okay then set header we are going to say Kafka headers spring framework Kafka support it has the string constants so Kafka headers key okay for the key we can say key 1 key 2 and so on this is how we are going to set then we can also set few other headers if you are interested in for example let's say trace ID you are interested in the observability and all right so maybe we can set some trace ID something like that request ID whatever you want to call this okay dot that's it now I can easily modify this but I do not want to touch this so that I can simply commit this code you can use this as a reference so I am going to create another bean just by copying this and I am going to call this message producer like this and it's going to return message of string okay then I am going to modify just this part that is whenever the supplier dot get method is invoked I am going to call this dot build message that time I am going to increment the counter okay so that's it now we will be building message one message two and we will be sending the message with the key like this then let's go to the sections 03 consumer config here I am adding one helper method private void handle message let's imagine that we are going to receive the message of string like this so how to access the message information so that is what we are going to see at the consumer side so first I am simply just printing whatever we have in the message then let's imagine we are interested in extracting the key and the payload so our key equals message dot get headers then get it's simply a map so Kafka headers now you will think that we have to use key right because that is what we heard a set at the producer side you might think like that but actually we have to use received key it might look weird at first that way we have to use a different header to access the key there is a very good reason for that first understand that it's not modified by the Kafka broker it's done by the spring team but why oftentimes in microservices architecture the messages might include some attributes for observability purposes like race ID span ID etc along with other information like this source let's take a processor it receives a message and it returns another message right so this processor might want to include all those observability attributes as part of the message it returns payment event so the message builder has some helper methods to build messages like this message builder create provide the event then whatever the the input message whatever the headers it has right we might want to simply just send those for the payment event as well so what will happen if the input message this message contains the key like this now when you use this way you automatically you are inheriting all those attributes and you are going to set all those attributes for the payment event you are seeing right so now the payment event will be having the order ID as its key but this is not intentional that is when you return this message the framework will not know whether you said this intentionally or this message got this key because of this so this will be causing some confusion you are seeing right so because of this reason the spring team what they are doing here is then they give the message to you that time they change the key name to be like this so that even if you do this way even if you are going to set this accidentally as part of the payment event they will know that oh this is the received key so this is not for the output message so we can remove this so they will simply just remove it hope now you understand why we use received key here to get the key okay then our payload equals message get payload that's it so now we can simply just print the key and the payload again just to be very clear this log message itself will be containing the key the message the payload etc I am intentionally printing this one more time to show that this is how we have to access okay so that is why nothing else okay perfect then as we had created the message producer I do not want to touch this so let me copy this and let's have a separate consumer to receive the message of string let's keep it like that and let's call this message consumer okay so whenever I get the message I am going to simply just call this handle message so I am going to use method reference like this that's it
