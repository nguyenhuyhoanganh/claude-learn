# Demo: StreamBridge

in this lecture let's have a quick demo of stream bridge for that I do not want to emit message one message two etc just for this demo let's have something different to keep things simple I am going to use this ping command for example ping-c 10 google.com something like this so it will be checking if google.com is reachable by sending an echo request then it will give some output that is what we are going to emit let's quickly check this command let's open the terminal and let's enter this Linux Mac users please use this Windows users it's instead of minus C you have to use minus M I am using Mac so if you are having Windows the output what you see could be slightly different but the output doesn't really matter so I want us to receive something and we are going to send that information to a Kafka topic so let's not really worry about the output as long as we get something we should be good let's open the terminal enter this command hit enter now I get something right so whenever we get this we will be sending that to Kafka topic using stream bridge so that is the demo we are going to have this is slightly better than the message one message two right so that is why we are doing this let's come back to our IDE let's please go to section 04 package as usual here we have consumer and producer our consumer is the usual consumer nothing special here so let's go to our producer here I have ping producer it's a spring component so this can auto wire stream bridge for us so that's what we have here it implements the command line runner so we have the run method as you know when the application starts the run method will be invoked so as part of that using the Java process builder I am basically entering the command ping minus C 10 google.com whatever we just did okay so this is what I am doing if you are a Windows user please use minus n okay do not forget that do not forget to change this then we saw some output in the terminal right so whenever we get that output right we can access that actually the reader dot lines as you see it's a stream so for each line we can emit that line using the stream bridge Kafka topic that is what we are doing we need the destination so for that we are having one destination called a ping out so under source main resources section 04 I have two configuration YAML files one for consumer the other one is for producer the consumer same nothing special demo topic demo group okay nothing special here for the producer we do not have the function definition and also it's very simple spring cloud stream bindings ping out this is the name I am using okay ping out and you have to give the destination which is demo topic let's please go to section runner before running this please ensure that you have created a fresh Kafka container just to have a clean demo nothing else okay our consumer is pointing to section section 04 under 0 1 consumer perfect the producer is pointing to section 0402 producer okay let's run the consumer let's run the producer as soon as I run this it will be entering that command and it will be getting that output then it will be sending that to a Kafka topic the consumer should receive that you should be able to see okay so now it's sending 64 bytes all those things whatever it's receiving and consumer should be receiving that okay perfect okay that's it the producer is done at this point okay so it had received that output and it had sent everything so basically the command-line runner exited that's it we can close this
