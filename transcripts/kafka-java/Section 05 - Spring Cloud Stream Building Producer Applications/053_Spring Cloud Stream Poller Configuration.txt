# Spring Cloud Stream Poller Configuration

In this section, let's see how to develop a simple Kafka producer application using Spring Cloud Stream. We can also discuss how to produce messages with the keys, then how to produce messages dynamically. At this point, you already know how the producer application will look like when we use the Spring Cloud Stream framework. We will be using the Java Functional Interface Supplier to build a Kafka producer application. We will be exposing a Bean like this. For now, let's use string messages. Later, we can use Java records. I want to keep things simple now for us to become comfortable with the Spring Cloud Stream. Then we will be providing the configuration like this. Spring Cloud Stream, Bindings, the Producer, Out, 0, Destination, we will be giving some topic name. Very simple. There is a small challenge with the supplier type. To better understand this, when we used the Java consumer type to build a Kafka consumer application, internally, this is what happens. Please understand that this is pseudocode to give an idea, to create a mental model. That is, in the Spring framework, internally, it creates the instance of this Bean, so payment event consumer like this. Then again and again, in a loop, it gets the message from Kafka and give it to the consumer like this. So, this is what happens again and again. Very simple. I hope no confusion here. Now, let's check the producer. Here we are exposing a supplier type, so Spring framework internally creates the instance of the supplier like this. Then it will invoke the supplier.get method to get the item. Then it will send it to Kafka, right? OK. Is that it? Will it just send one item? So, the Spring Cloud Stream framework needs a mechanism to invoke the supplier again and again to send the messages. We can provide this polar configuration. So using this, it will invoke the supplier periodically to send the messages to Kafka. Again, it's pseudocode. So this is the configuration for polling. By default, it invokes the supplier every thousand millisecond or one second to send the message to Kafka. First let's, let me demo this. Then let's discuss a few other options one by one.
