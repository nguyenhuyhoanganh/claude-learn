# Cluster Configuration Properties - Part 1

In the next lecture, we are going to set up a Kafka cluster using docker compose We will be running three Kafka containers like this. All these containers will have to talk Among themselves to make our cluster work, right? So we have to provide some properties for these containers I am going to name these containers as Kafka 1 Kafka 2 Kafka 3 like this Also our application has to talk to these Brokers, right? So we will be doing the port mapping like this Okay. So now just because We run three Kafka containers. We do not automatically form a cluster We have to provide some information in a way that we create the cluster. So we have to talk about some properties Some properties will be common some properties will be different depending on the The container or the node so to understand this better Let's take Kafka one container Kafka to container and let's compare these properties so that we can understand What are all the properties which are common and what are all the properties which are going to be different? depending on the container We are going to compare properties side-by-side like this, this is Kafka one container properties This is Kafka to container properties First property node dot ID. So we have to give some node ID here So here I am giving one for this container. I am giving two So if you are going to have thousand brokers in your cluster right thousand instances in your cluster So each and every node should be having one unique ID like this Then the cluster ID the cluster ID should be same for all the nodes Basically, we are telling this node that hey node you are part of this cluster Okay, so they all have to have the same cluster ID. This is important I cannot simply use we know like this for my cluster ID Okay, so the cluster ID has to be in a specific format. That is what Kafka wants So to generate the cluster ID we you can go to the bin directory And if you run the script, this will be giving you one random ID. So that is what we have to use as cluster ID Okay. So in this case, we you can just use this whatever I am giving you here You can simply just use this I generated this by I created this by running this Okay, so you you have to generate one ID then that is what you have to use as cluster ID for your cluster Then process dot roles. This is something we know already here. I am providing both broker and controller roles for every container then the listeners If you check the Kafka one container property here, this is going to use 9092 for internal communication 9093 for controller communication and 8081 for external communication So when we spin up this Kafka container, it will be opening Or it will be listening on these ports This is for Kafka one if you check for the Kafka to container property It's mostly same 9092 for internal 9093 for controller But here I am using 8082 for external because we are going to do port mapping. So this is why okay So we are running three containers. So we need a different port Right for port mapping for the host port the container port can be same But I want to keep it like this to avoid confusion Now this is going to be the interesting part here. We are Opening three ports, right? But Kafka does not know which port is for which communication You might say this is internal. This is controller, but Kafka does not know all these things These are all just simply names that is What we are saying here is that you you have to give some kind of a label for the ports Then you have to specify which label you are using for controller communication and which label you are using for The data plane broker communication So here we say internal, right? This is just a name. I can even simply say, you know like this and I Can make it work So the Kafka cluster will be just working fine like this But do you really understand what it means? It's kind of confusing, right? This is simply a label So to provide meaningful label, so we use Internal like this, but Kafka does not know what internal is. It does not know what controller is so you have to say that Controller listener name is controller. That is I am using one label called controller Go and use that port for your controller communication Similarly, I am using one label called internal use that for Use that port for the data plane communication. That is what you are saying The remaining port right 8081 that label the 808 That label the 808 this is this will be used for external communication Hopefully up to this point. It's clear, right? So far the node id That is different Node id is different and the listeners here. We use 8081 for external communication here We use 8082 for external communication All other properties. They all have the same value as you see
