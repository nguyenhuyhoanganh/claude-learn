# Multi-Node Cluster: Docker Compose Setup

In this lecture, I am going to share the Docker Compose YAML for our Kafka cluster. If you had cloned my GitHub project, please go to the project 04 Kafka cluster. Under that, you can find the Docker Compose YAML and the server.env environment variable files. If you check the Docker Compose YAML, I will be creating three containers Kafka1, Kafka2 and Kafka3 like this. So it's going to be a three node cluster. And in the previous lecture, I had mentioned that these containers, they will have some common properties, then they will have some container specific properties. Okay. So I am keeping all the common properties in the server.env file. So we have already, we have already discussed all these things. I am providing the server properties as environment variables using the rules we had discussed in the previous lecture. Let's check our Docker Compose YAML. Here we are creating three Kafka brokers using Docker containers. So I am naming them Kafka1, Kafka2, Kafka3 and so on. If I expand this, this is something we have already discussed as part of the Kafka setup. So same image, I'm giving the container name as the service name Kafka1 and the working directory. Okay. So now I am providing the server.env file. Okay. So basically this container, when it starts, it will be using all the common properties from this file. Okay. Then I am providing additional environment variables like this. So for this container, the node ID is going to be one and the listeners. These are the ports this container is going to use 9092 for internal communication, 9093 for controller and the external 8081. Then it's advertising that I can be reached at Kafka19092 for the external communication. I can be reached at localhost 8081. We are also doing the port mapping as you see here. Okay. So if you understand this, we already discussed all these properties. So if you understand this setup, then that's exactly what I'm doing for Kafka2, Kafka3.
