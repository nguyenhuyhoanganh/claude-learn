# Cluster Configuration Properties - Part 2

Now, let's focus on this property, this could be a little bit confusing, so please be patient. A Kafka broker might be opening multiple ports for multiple reasons. But it has to advertise which one it's using for the internal communication and which one is it's using for external communication, how to reach. Basically by using this property, this broker tells others that how I can be reached. That is what it says. Now it says that my name is Kafka1, for the internal communication, I can be reached at Kafka19092. That's what it says. For the external communication, I can be reached at localhost 8081. Now this localhost, it's kind of confusing. Ideally, we should be providing some public IP address, but here we are giving localhost. So it's weirder, right? We have to see this property value from the caller perspective. That is, this Kafka1 broker or container tells that for the internal communication, I can be reached at Kafka19092. So what it means is that since we are using Docker Compose, since they are all in the same network, this container can reach out to this container by using the service name. So Kafka19092, by using the service name or the container name, this container can reach out to this container. So they are all in the same network. This is how we are going to set up. So for the internal communication, this tells others that I can be reached at Kafka19092. For the external communication, that is, the producer application or the consumer application, they are not part of the Docker Compose. Since we are doing 8081 as the port mapping here, right? So what it tells is that the producer application can reach out to me by using localhost 8081. Because we are doing port mapping from the producer perspective. This application perspective, it's localhost 8081. So we have to see from the caller perspective. This is how it provides the property value. Hopefully this is clear. If you check for the Kafka 2 container, it will be, it provides Kafka 29092 for the external communication, localhost 8082. Then controller quorum voters. So who are all eligible to vote, to select the controller. So in our case, all the three Kafka containers, they all have the controller role. So we provide that information. So node ID, the host name, then the controller port 9093. Similarly, Kafka 2, node ID 2, 9093, Kafka 3, node ID 3, 9093. So that is what we provide here. Then the listener security protocol. So you know, we have these listeners, right? So here we provide the security protocol information. First, let's take a look at this. Here I say controller plaintext, internal plaintext, external plaintext. So what does it mean? These are the security protocols we have plaintext, SSL, SASL, plaintext, SASL, SSL. So this could be confusing. Let's take this slowly. So we have multiple communications here, right? Communication between brokers, communication between controllers, and the communication between the application and the broker and so on. So here, when we use plaintext, what it means is that there is no authentication involved and the connection is not encrypted. That is, the brokers, when they talk among themselves, when you use plaintext, what it means is that they all can freely talk to each other. And it's something like a HTTP, there is no encryption. So we are not using SSL, it's not encrypted. So that is what we are saying. When you say SSL, it's still we are not authenticating, like there is no username password, but the connection is encrypted. So that is what we are saying. When you use SASL, plaintext, what it means is that the connection is not encrypted, but there will be some username password authentication. That is, the producer is going to talk to the broker, it has to use, it has to provide some username password. So we will be doing some authentication. So this is what we are saying, authenticated, but it's plaintext. So no encryption here, the connections. SASL, SSL, it's authentication is, we have authentication and the connection is also encrypted. With that information, we can understand this better. For each listener, like controller, internal, external, these are the listeners we have, right? For each listener, what is the security protocol, provide that as a map, like a key value pairs. That's what it says. So controller is going to use plaintext, internal communication, that is also plaintext, external communication, that is also plaintext. That is what we are saying. We can also say, controller is plaintext, internal is plaintext, but external communication, it can be SSL. Something like that we can specify if you want, okay? But to keep things simple, since this section is about cluster, let's focus on, let's keep things simple, let's use plaintext. The security part, I'm going to cover that later, so that time we can play with the SSL. We are almost there. We have only two more properties to cover. So this one is very simple. I'm just simply disabling the topics auto-creation. So this is why I'm setting that to false, because we are going to have one demo and I do not want our application to automatically create a topic because I want to create a topic with a replication factor. So I am intentionally changing that to false. Then the last property, this is very important. That is one internal Kafka topic called consumer offsets. This is something you would have seen if you have a consumer group and if you try to list the topics you have in your Kafka cluster, that time you might be seeing this topic. So using this internal topic, Kafka tracks where the consumer groups are now, that is up to which offset they have seen the messages. So I had told you that Kafka is tracking where the consumer groups are. So for that, it uses this topic. So by using the replication factor, what we are saying here is for this topic, how many copies we want. Since we are running a three node cluster, we are saying that we want three copies of that topic. Why? This is mainly because remember that in the cluster, the nodes can go up and down. So if you use one, right, what it means is that we will be having only one copy. If that node goes down, then that's it. Kafka cannot track where the consumers are, the consumer, right? So this is why we say that it's three. Now hopefully I think you have a decent idea about these properties. Okay. So if you try to compare this and this, so the node ID is different. Node ID is different. The listeners will be different because here we use 8081, here we use 8082. So the listeners will be different. Then the advertised listeners, that is also different because the ports are different because of this reason, this property will also be different. Other than that, everything else will remain same.
