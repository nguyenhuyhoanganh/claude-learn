# Demo: High Availability & Fault Tolerance in Action

Let's come back to our IDE. Let's please go to section 11. As part of this project setup, we are going to see what happens to our application when we run our Kafka in the cluster mode and when the node goes up and down, if there is any impact, if our application is getting interrupted. That is what we are going to observe. For that, I have a very simple producer and a consumer application. If you check the consumer, nothing special. We have seen this many times. If you check the corresponding consumer YAML, again, this is nothing special. But as part of the bootstrap server, even though we are going to have three nodes in the cluster, I am intentionally giving only this as bootstrap server. Then if you check the producer, here, again, our same supplier producer Bean, we have seen this many times. If you check the corresponding producer YAML, I am giving localhost 8081 as our bootstrap server. The fixed delay, I'm using 50 millisecond just for the demo purposes. I have already shared some instructions in the readme file, so please refer to that. Please go to the Kafka cluster directory. So let's do the docker-compose down. I have already done that, so please do that. Then let's do docker-compose up. Let's start the cluster from scratch for this demo. Once the cluster is up and running, let's access the Kafka 1 container. Then let's create the demo topic. One partition and three replicas. So this is how we are creating. Okay, so now the topic is created. Then let's please go to the section 11 section runner. Here we have the consumer and the producer. Ensure that it's pointing to section 1101 consumer, section 1102 producer. First, let's start the consumer. Do not start the producer. If you run the producer, then the consumer will be printing all the log messages. It will be very difficult to observe certain things. This is why I did not want to run the producer. Now let's do, let's search for localhost colon 808. Let's keep it like that. Okay, so 8081, this is something we had provided in our bootstrap server itself. So this is fine. However, there are 17 results, right? So let's check. Okay, now this is interesting. Even though we did not specify 8082 in our YAML file, right? Our consumer application comes to know about this instance. This is interesting, right? This is cool. Anyway, let's clear this. Let's scroll down. Okay, let's keep it like this. Let's run the producer. So the producer keeps on producing messages every 50 millisecond. Here we are continuously consuming messages, super fast. Now let's go open the terminal. Here we have three Kafka containers up and running, right? In the background, if you see the messages are being consumed continuously, super fast. Okay, now I'm going to stop our bootstrap server, Docker stop Kafka one. Let's see what happens. Let's hit enter. Now we saw some output. However, the application is continuously working. See the bootstrap server is down. Whatever we had provided in the YAML file. Okay, so if you see, it knows that node one disconnected. However, it was consuming 633 and 634. It, okay, so we did not lose any messages. That's it. It's continuously consuming messages and the producer is producing messages. Okay, perfect. Now let's start Kafka one. Great, now let's bring Kafka two down. If I hit enter, again, there was some messages. If you scroll up, okay, 14, 13, 14, 14, 18, 19. So we did not lose any messages. We are continuously consuming messages. As you see 52, 53, it says that node two disconnected, but however, this is continuously working, right? And the producer is also producing messages. So this is cool. So again, let's now, let's bring Kafka two up. Let's stop Kafka three. Okay. So again, this is continuously working. So this is node three disconnected 22, 18, 22, 19. So perfect, right? Let's stop this. This is what I wanted to show.
