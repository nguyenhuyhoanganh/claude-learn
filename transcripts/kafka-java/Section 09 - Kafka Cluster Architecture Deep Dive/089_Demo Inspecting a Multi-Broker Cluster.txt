# Demo: Inspecting a Multi-Broker Cluster

I have added this readme file under the 04 Kafka cluster. Let's use these instructions for our cluster demo. The very first thing what I want you to do here is, you might be running the Kafka container as part of this 01 Kafka setup, okay? I want you to go to that directory via your terminal or command line. Go to that directory, please do the docker-compose down. Let's ensure that we are not running any docker containers. Let's start from scratch. Then once that is done, that is the very first step. Once that is done, then go to this directory, 04 Kafka cluster via your terminal. Let's do the docker-compose up. Open the terminal, go to the directory where you have the docker-compose yaml for the cluster. So this one, then let's do docker-compose up. Let's wait for like 10 seconds. Once the logs are settled, then open another terminal and issue this command, docker ps-a to ensure that all the three Kafka containers are up and running. Kafka one, Kafka two, Kafka three, they all should be up. Then let's access the Kafka one container. I have already shared the command, so please use that. Now I'm inside the Kafka one container. Now let's create the topic with two partitions and three replication factor. So let's hit enter, the topic is created. Let's describe the topic. Hopefully you can see this better, I guess. I reduced the size so that we can show the output in the proper tabular format. This is why I had to reduce the font size for this. Okay, so now when you describe here, we can see the demo topic and the topic ID. It shows that we have two partitions, yes, and three replicas, right? So replication factor three. The last time we did not discuss all those things, mainly because that time we were not talking about this. Now we have the knowledge, so we can discuss more. So we have two partitions, so partition zero and partition one. And if you see, the leader is one. What the one means, the node ID one, okay? So the Kafka one is a leader for partition zero and the Kafka two is a leader for partition one. And here it says replicas, one, two, three, two, three, one. So what it means is that we have three Kafka nodes, right? Node ID one, node ID two, node ID three. So all those instances have the replica of partition zero. Similarly, the nodes two, three, one, they all have the replica or the copies of this partition one. So that's what it says. The ISR means in sync replicas. That is currently these nodes, they're all up to date. That's what it says, okay? So two, three, one, all these nodes are up to date. So we have three copies in these three nodes and all the nodes are up to date. So that's what it means. We have already seen how to scale by creating multiple partitions. We had a demo for that actually, but we have not seen high availability. So let's do that now. Since we have multiple copies, so let's bring the node two down. So if we bring that down, what will happen to this partition one? Let's open another terminal, another window like this. So I'm going to bring the node ID two, which is Kafka two down. Let's see what happens. Docker stop Kafka two. Let's bring that down. Okay, so what will happen to this partition one? Let's describe one more time. If I hit enter, this command could be slower now. Okay, so in my case, it's kind of quick, but in your case, it could be slightly slower. So no worries. Initially, the node two was the leader for partition one. Now the node three became the leader for partition one. And we have replicas in node one, two, three, but only one and three, they are in sync. The node two is currently, it's down. Let's bring the Kafka two up. So let's start Kafka two. So now if you describe one more time here, now they are all in sync. Please ensure that at least two containers are up and running in the three-node cluster to make the cluster work properly. Because when you have a single node up and running in a three-node cluster, and the two nodes are unavailable, right? The single node cannot select itself as the controller. So we need a majority to own. This is why we need at least two nodes should be up and running to elect the controller. That is the concept. Okay, so in our case, I'm having all the nodes up and running. So let me clear the screen and here, let me clear the screen, describe one more time. Now I'm going to bring the Kafka one down. So when I bring Kafka one down, this could probably exit this, because I have accessed the Kafka one container here. Okay, so this is fine. Let's do Kafka. Okay, let me access Kafka two here. Let me try to describe. Let me copy this command and paste it here. If I try to describe, right? Now the node ID two became the leader for both partitions and currently only two and three are in sync. Also, this is normal for a node to be the leader for more than one partition. The way in which it works is that if you check the previous output here, if you check the replicas one, two, three, right? So it will be selecting the leader in this particular order. So one was the leader that time, when node one goes down, then it will check the next node, which is two, that became the leader. So this is how it works. Now you might think that why one single node has to handle all the partitions, but no worries, there is a background thread in Kafka, which will periodically check and depending on the load, it will be redistributing the partitions.
