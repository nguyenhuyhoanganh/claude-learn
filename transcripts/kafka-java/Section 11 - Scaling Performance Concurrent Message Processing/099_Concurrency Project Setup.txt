# Concurrency Project Setup

In this lecture, let's explore the project setup. As we had already discussed, we will be reusing this example whatever we had done as part of our content-based routing. However, here we are going to make few changes. First our processor is going to be slow. It will be taking 200 milliseconds to build one delivery object. So remember that. Then we need multiple partitions if you are going to enable the Spring Cloud Stream concurrency. So we have to have multiple partitions, otherwise it will not work. So we are going to assume that our order events topic has multiple partitions. So the producer will be producing messages with the keys so that the messages can be distributed across partitions. Here we will not be making any changes. Let's come back to our IDE. Let's please go to section 13 package. Under that we have consumer, DTO, processor, producer, etc. First let's check our consumer. We had already said that we will not be making any change. So I have the digital delivery consumer config, physical delivery consumer config. So no change here. And I have the corresponding YAML under section 13. Again, no change. So let's focus on the producer. If we check our producer, here we are supplying the order objects. Now we are producing our order object with the keys. So previously it was supplier of order. Now it's supplier of message of order because it has keys. So whenever the supplier get method is invoked, we build this message object. So basically we build the same order object. Now we wrap it with the message and we provide the key. The key is nothing but the input like 1, 2, 3, that is the key. So we are using integer key and we build the message of order and we simply return that. Basically you remember this, when you have a key, you have to provide the serializer. Otherwise it will throw exception. You know that. So ensure that in the section 13 package, right, as part of our producer YAML, we should include this producer properties, key serializer, it's integer serializer. Everything else remains same mostly, however, the order interval is going to be 100 millisecond. That is, we are going to produce one order event like every 100 millisecond. So every second we will be producing 10 orders. This is how, this is the setup we want to have. Hopefully this is clear. Assuming you understand the producer, you understand the consumers. Now let's focus on the processor. Under processor here we have a couple of classes. Let's focus on the delivery service class first. This is a spring component. So here we have many private methods. So this is the only public method we have. This method accepts the order object and returns one object. So the idea here is this delivery service class is responsible for taking the order object and it has to build the delivery object. So that is the idea. So when it builds the delivery object, it will have to make some database call or a microservice call. So that call takes some time. So here I am using thread.sleep, but this is to simulate the network call. Let's imagine that it takes 200 millisecond. This is what we are doing here. So whenever you pass the order object to build the delivery object, we have to make some network calls. It takes some time. Then depending on the order type, we build either the digital delivery object or the physical delivery object and we return. So this is what we do here. If this is clear, then processor config, it's very simple. Here we have the string constants. This is the destination header. These are our bindings. This is what we had done earlier as well. So nothing special. So this is our processor bean. Here we get the order object and we return the message object with the header. So since the delivery service is a spring component, I am injecting this here as method parameter. So whenever I get the order object using the delivery service, I build the delivery object here. Once I build the delivery object, then I create the message object using the two message method. So we build the delivery object, then we give that to this method. So depending on the delivery object, if it's a digital delivery, then we use digital out. Otherwise we use physical out. That is a destination. We build the message object with the header and that is what we return. Very simple. Now let's check the processor yaml. Go to resources, section 13, processor, delivery processor is our bean name. So delivery processor in zero, this order events, give some group name. Now we use the concurrency option. We create three threads. You can even put 300 here. It doesn't really matter. But if you are going to create a topic with only three partitions, then only three threads can be used. Right? Okay. Remember that. And these are our custom bindings. So when you use this binding to send the messages, it will be sent to this topic. When you use this binding, the messages will be sent to this topic. We know this. Okay. Since at the producer side, we are producing messages with the integer key. Now at the consumer side, I'm using key deserializer, integer deserializer. However, if you check our processor configuration, I'm not accessing the key. So I do not need deserializer, but it's good to include that if you, if you ever want that. Right? So you have to include the deserializer. So this is why I simply thought of adding this anyway.
