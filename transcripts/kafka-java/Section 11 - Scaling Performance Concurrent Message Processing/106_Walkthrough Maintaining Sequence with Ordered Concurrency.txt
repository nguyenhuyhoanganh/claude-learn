# Walkthrough: Maintaining Sequence with Ordered Concurrency

In this lecture, let's discuss how to do application-level concurrency when the message ordering is also important based on a custom key. Conceptually, this is very similar to what we did in the previous lecture. There is one additional step in this case. Basically, we have to create a custom partition in the memory. We start the same way. The processor will be consuming records in the batch mode, so we will receive the list of order. But before processing, we will do a custom grouping step by using this grouping by. Here, as you see, we iterate the list using the Java stream pipeline. Using grouping by, we group all the orders that belong to the same customer into the same bucket. For example, let's say we have a list of 500 order objects. This is the received list. And let's assume that there are two different orders. This one and this one belong to the same customer, C1. Let's keep it like that. When we apply the grouping logic, those orders will be grouped as a list like this. Let's call this grouped list buckets. So once you have these buckets, then the concurrency model becomes clear, right? Now, all these buckets can be processed concurrently. But items within the bucket, they all have to be processed sequentially one by one. So concurrency across buckets, sequential processing within the bucket. Now, let's come back to our IDE. Let's please go to ordered message processor config. So here this class or configuration will be activated. Then the processing mode is ordered. OK, so this is same as the previous one. But as I had just explained. Here, the very first step is doing the custom partition. So I partition by the customer ID. This is just an example, but you can feel free to change this to something else like instead of customer ID, maybe order ID, maybe message key. Currently, we are not accessing the message key. But if you have that, you can also partition by that if you want. So these are all up to you. So we when we get the list of order objects, this is what we do first. So we partition by the customer ID. So now here this will give us the stream of list of order. So the each list will be containing the orders belong to one single customer. This is how we group. Now, if you call this as a bucket, just to avoid confusion, if you call this as a bucket. So now all these buckets can be executed using stream gatherers concurrently using virtual threads. However, each bucket right items in the bucket. It has to be processed sequentially. So that is what we are doing. Bucket that stream here. We are not using parallel and all. OK, so here we process everything sequentially one by one. Perfect. Now, then this will give us the stream of list of message. Sorry, this will give us a stream of list of delivery objects. We flatten that using flat map. Then we build the stream of message of delivery object. Then we create the list and that is what we return. Hopefully this gives you some idea. Please let me know if you have any questions via the Q&A section.
