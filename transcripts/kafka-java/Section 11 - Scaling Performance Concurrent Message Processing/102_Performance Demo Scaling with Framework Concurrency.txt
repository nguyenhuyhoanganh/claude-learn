# Performance Demo: Scaling with Framework Concurrency

Let's come back to our terminal. In the previous lecture, we had a demo with the concurrency level one. Now this order even topics has some messages that are not consumed by the processor because the processor was slow right with the concurrency level one. So this order events topic will be having some messages. So I am going to delete this topic. Remember to delete this topic, ensure that you have stopped all the producer consumer application. So let's delete this. Then let's recreate that topic with the three partitions. Now we have the new topic with the three partitions. Let's go back to our IDE. Let's change the processor concurrency level to three. Let's change that to three. So now let's start the digital delivery consumer. So these topics had some messages. So now it's consuming that. So this is fine. Physical delivery consumer. So it might be having some old messages that is fine. So this is good. So now let's start the processor. So the concurrency level is set to three. So now we have three threads. Okay. Let's run the producer. Producer is super fast. And if you check the processor, right, since we have three threads now. So the processor will be able to keep up with the producer. Let's wait for some time and see how it goes. I waited for some time. Now the producer is producing the order IDs like one, one, five, zero in that range. Now if you check the processor, yes, the processor processor is able to keep up with the producer and we should not be seeing any delay here as well. So this is great. Now let's stop the producer. I only stopped the producer. The processor consumers are, they are all running. However, there are no events to process since the producer is stopped. Okay. Perfect. The concurrency level is set to three at the processor side. Now as you see, the processor is able to keep up with the producer. Perfect. Okay. Now let's do this. In the producer YAML, what will happen if the producer is going to produce one event every 10 millisecond. In that case, it will be producing like a hundred events per second, right? So if I try to run the producer with this configuration, it's super fast. Okay. Now the processor here, the concurrency is level is set to three. However, if you see the producer while it's processing, producing 1100, 1200, okay, 1300 super fast. Now it's like 200, 300. This is what it's processing now you are getting, right? So even when you have the concurrency level is set to three, there is no way for this processor to keep up with the producer with this setup. So the framework cannot help. Even if you run multiple instances, there is no way because we are processing per partition level, right? So because of this reason, there is no way for our processor to keep up with the producer. So how to fix this problem? Okay. Let's talk about this in the next lecture. For now, let's stop all these applications.
