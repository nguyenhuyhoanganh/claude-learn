# Framework Concurrency: Scaling via Consumer Threads

In this lecture, let's discuss how Spring Cloud Stream helps us with the concurrent message processing. By default, when we expose a Bean like this, the Spring Cloud Stream uses a single thread for message consumption. Under the hood, the framework code looks something like this. This is just a pseudo code to give you the idea. So, when we expose the Bean like this, the Spring will be creating a handler like this. For order even consumer, they will be creating one handler like this. Then, it creates the Kafka consumer instance, connects to the broker. Then, it will pull records from Kafka broker. It will get the records, then it will give it to our handler. So, this is the default behavior and the messages are processed sequentially one by one. Again, this is just a pseudo code to give you the idea. We had discussed earlier that when a topic has multiple partitions and when we have only one consumer in a consumer group like this, Kafka will be assigning all the partitions to that single consumer. So, to scale, the usual approach is to run multiple instances of the consumer like this. Now, think about this. Do we really need to run multiple instances? Why can we not simply create more threads inside a single application? This is where the Spring Cloud Streams concurrency option comes in. Let's imagine a topic with three partitions. We expose a consumer Bean like this. And in the Spring Cloud Stream configuration for the consumer binding, order even consumer in zero, we provide the destination, we give the group name, right? Then we can set a concurrency for the consumer binding like this. Then we configure this concurrency like this. What Spring will do here is, as usual, it will be creating one handler for our order even consumer like this. Again, this is pseudo code. Just visualize to give you the idea. It will be creating one handler like this. Then based on the concurrency value we provide here, Spring will be creating one executor service. Executor service is nothing but a thread pool. You know that. So, using the executor service or thread pool, Spring will be submitting multiple runnable tasks like this. As part of the runnable, we create the Kafka consumer instance, each running in its own thread, joining the Kafka broker and pulling from Kafka independently. So, we have a single consumer application, JVM, but we create multiple threads, T1, T2, T3 like this. So, each thread will be trying to connect to the Kafka broker saying that, hey Kafka broker, I am a consumer from this consumer group. So, from Kafka's point of view, now it sees three consumers from the same group. Kafka does not know or does not care whether they are coming from three different application instances or just three threads inside the same instance. So, it does not care. It simply assigns one partition per consumer like this. So, we do not need three separate instances. If a single instance has enough CPU and memory, we can process the partitions concurrently using multiple threads at the framework level. No code change is required. It's just a simple property update. It's pretty cool, right? Now, let's see this in action.
