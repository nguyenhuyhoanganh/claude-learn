# Demo: Unordered Concurrency in Action

In this lecture, let's see how the unordered concurrent message processing works. First, let's go to the section 14 resources. Let's check the configuration YAML. Here, we do not have any change in the digital consumer, physical consumer, and all. For the producer, we are going to produce one item every 10 milliseconds. So super fast. This is what we are trying to simulate. And the framework level concurrency is not helping much here, right? So this is what we want. And for the processor, we are not using the framework level concurrency. We do not need that. But we have enabled the batch mode. Everything else remains same. So this is super important. Please go to the terminal. Ensure that you are having the fresh Kafka container. For me, I just did the Docker Compose down and up. We do not need to create the topic with three partitions. One partition is enough, actually. If you want, you can create. But since we are not using the framework level concurrency, I think we do not need that. We can just have one partition. Then let's go to the section runner. Ensure that everything is correctly pointing to section 14, corresponding configuration YAML file. For the processor, this is the very important thing. Along with the section 14 processor, we are also doing the processing mode unordered. We want that beam to be activated. So this is what we want. So ensure that you are having this. Everything else remains same. So let's start that digital delivery consumer, physical delivery consumer. Let's run the processor. Ensure that the partitions are assigned everywhere. OK, it's good. So now let's run the producer. Now this is super fast. Let it run for some time. Let's check the processor. OK, so it's continuously sending output messages. So OK, let it run for some time. Currently it's like 1,300, 1,400, 1,500, right? OK, so it's continuously sending. Let's check. And if you see, we get everything. OK, so 2,200. So we are also like 2,300, 2,400. So our processor is able to keep up with the producer now. I waited for some time. So now here we are like 12,000 in that range. So everything seems to be good. We can stop the producer and all other applications.
