# Walkthrough: Implementing Unordered Parallel Processing

Let's please come back to our IDE, let's please go to section 14 package, here as usual we have the consumer, video, processor, everything. I simply copied everything from section 13 and pasted here. So same consumers, digital delivery, physical delivery consumer, there is no change here. Same producer, no change here as well. The only thing is we have the delivery service and again there is no change, we are not going to touch this. We have already assumed that the delivery service, it takes time, 200 millisecond. So we should not be touching this. So let's keep it like this. Now you will be seeing two classes, ordered message processing and unordered message processor config. So this is what we are going to focus first. Basically here we are not touching anything, the way in which we are going to build message general, everything is going to remain same. The only thing is now our processor beam is going to accept the list of order and here we have to return the list of delivery object. Let me quickly show you this, here we get the list of orders, I can iterate the orders using orders.stream, then for each order I can use the map operator to give that to delivery service like this to build the delivery object. Once I have the delivery object, I can use another map operator to give that to this method to build the message of delivery object. I am using method reference. Now I get the stream of message of object, I can use to list. So that's it, there is no compilation issue. However, here I am processing all the items sequentially one by one. If the list contains 100 items, it's going to take 100 times 200 millisecond, right? Because each and every item processing will be taking 200 millisecond. So this will be processed one by one. Some of you might think that, hey, we can use dot parallel to create the parallel stream so that we can process everything in parallel. You might think like that, you can try, but it will not work. Because as part of the thread pool, whatever the Java team using behind the scenes for the parallel stream, here we have only very few number of platform threads, depending on the number of CPUs you have. So this dot parallel should be used only for CPU intensive operation, not for the network calls. So this is not the correct way to solve this problem. So please do not use this. In the Java stream pipeline, this is the time consuming operation and it involves network calls. So this can be solved using virtual threads. For that, as part of Java 24, they have introduced the stream gatherers. I have one dedicated course for this. So please check that out if you are interested. So using this dot gather operator, we can say gatherers, java util stream gatherers dot map concurrent. So here it will be expecting two arguments. The first one is concurrency. What is the maximum level of concurrency you want? I'm going to say 500 maximum concurrency. Why it's mainly because I assume that we are consuming in the batch mode and we will be using the maximum pole records 500. So that is why, but you can say 1000, whatever you want. So maximum concurrency is 500 and what is the operation you want to execute concurrently? So the delivery service to delivery. So that is what we are giving. By using stream gatherers, now each and every order will be processed. This particular operation will be executed concurrently by using 500 virtual threads. So this is how we are going to solve this problem and we can remove this. As part of this message processing, we do not care about the message ordering. So we process all the 500 items concurrently. That is our assumption. We have already said that. And I have also shared the code for ordered message processing. So I want to, I want to have only one bean at a time, either this or this. So this is why I am adding one conditional on property. So the processing mode unordered, when we have this property, this bean will be activated. When you have ordered that time, that bean will be activated. Okay. So let's take a look at this one by one. Let's focus on this first.
