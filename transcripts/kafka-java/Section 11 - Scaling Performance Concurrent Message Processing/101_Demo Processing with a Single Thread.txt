# Demo: Processing with a Single Thread

I stopped the processor, we are going to start from scratch First Concurrency, I want that to be 1 For now This is the default value So let it be 1 for now For this demo first Go to section runner Let's start everything So digital consumer first Let's run this Then physical consumer Then the processor Remember that the concurrency is set to 1 So digital consumer It's good Digital consumer is good Physical consumer This is also good Processor It has 3 partitions assigned This is perfect Now the producer As you know at this point The producer is going to produce 1 order event Every 100 ms The processor is going to process But it will take 200 ms So basically the processor is slow Remember that Let's run the producer Ok So it's producing super fast Now the processor is processing 1 by 1 Great Physical delivery It gets the events Digital consumer It gets the events Perfect Remember we have 3 partitions Now if you check The producer is producing ID is like 240, 250 in that range Now if you check the processor We are still in the 130, 140 in that range So the producer Now it's like It's 350 Now if you see So if you see At this rate The processor can never catch up The producer is super fast But the processor can never catch up I waited for some time Now the producer is going to produce Ok Now it's 1000 If you come and check Here we are processing like 600 in that range Since the processor is slow The Our digital and physical delivery Consumers since they depend on processor They will also be slow So we do not have Any issues with the consumer side Since the processor is slow So they are getting The events like this as you see So how to speed up the processor Now we can increase the concurrency Level so let's do that Ok let's stop the producer Let's stop the processor Let's stop all these Consumers
