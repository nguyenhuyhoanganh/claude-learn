# Demo: Thread-to-Partition Assignment

I have shared some demo instructions as part of the readme file. As usual, please create the fresh Kafka container. Then let's create this topic. We want the topic with the three partitions. So please do this. Then we will be starting the consumer producer applications one by one. We are going to observe certain things. Okay. First, let's start with this. Let's create this topic. Let's go to the terminal. I just did the Docker compose down and up. My Kafka server started. Let's open another terminal. Access the Kafka container. Then let's create this topic order events with the three partitions. It's created. Let's please go to section 13 section render. Ensure that all the consumer processor applications, they are all correctly pointing to appropriate YAML files. Section 13, digital consumer, section 13, physical consumer, and so on. They all look good to me. So now let's check the processor YAML. I want to show you something. The concurrency is three. Let's keep it like this. Let's come back to section render. And I am going to run the processor alone. You do not have to do this. You can simply just watch me doing this. I want to quickly show you something. Okay. I'm just running the processor alone. I am not producing anything. I'm not consuming anything. Just I want to observe the log. Since we have set the concurrency to three. Now, if you see, we print this partitions assigned three times. And if you notice here, these are like thread names. These are like thread names. So if you notice here, we have three different threads. And each thread is assigned with one partition. Partition zero, partition one, partition two. So this is cool, right? So now let's go back to the YAML, processor YAML. Let's change the concurrency to two. And let's restart the processor application. Again, let's observe the log. Now, since we set the concurrency to two, right? Okay. So now we have only two threads. So one thread is assigned with the two partitions, as you see. And another thread has only one partition assigned. Okay. So this is great. Now let's set the concurrency to one. You know what will happen now. So we will be having only one consumer thread. You see, here we have only one consumer thread. And it's assigned with all the partitions, zero, one, two. So this is cool, right?
