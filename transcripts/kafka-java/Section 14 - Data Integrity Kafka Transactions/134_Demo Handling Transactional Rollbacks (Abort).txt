# Demo: Handling Transactional Rollbacks (Abort)

In this lecture, let's have the demo for the key 2. And as you see, this will fail always. That is, first we will be emitting the credit request. After 3 seconds delay, then we will be emitting the debit request. Then we will be throwing exception. So, credit request and debit request will be emitted via stream bridge. The read committed consumer will not be seeing these because the transaction fails. However, the read uncommitted consumer will be seeing these two requests. Interestingly, since the transaction fails, spring will retry again and again. So, because of this reason, this will be emitted again and again as part of the retries. So, the read uncommitted consumer will be seeing these multiple times as part of the retry attempts. However, the read committed consumer will not see any of these. So, this is how it will work. So, let's go to the terminal. I cleared the terminal just to have the clean demo, nothing else. So, this is read committed, this is read uncommitted. So, copy the message for the key 2. Let's go to the console producer. Let's produce that message. Let's go to the consumer immediately. And here you see the credit. Here we are not seeing anything. We see the debit. Now it should fail actually. Okay, exception is thrown. Spring retries. So, another credit, another debit. There will be another exception. So, spring will retry. Credit and debit as you see. So, here we see these multiple times. But here we are not seeing these, right? If you check the log, here we can see that spring would have rolled back the transaction. Okay. So, transaction rolled back and all we can see. And after all the retry attempts, spring would have sent the message to the DLQ as well. We have not subscribed to the DLQ. But if you subscribe, you can see that message. Let me quickly show the DLQ as well. I have opened another terminal. And here if you see, I'm using console consumer. I'm subscribing to the DLQ topic. Consuming from beginning. So, we should be able to see that message. I'm not using the print headers and the print keys and all as you see. So, we are getting only the value. But if you want, you can also add all those options to check the exception details, etc. But anyway, so when the transaction fails, spring retries. Then finally, it sends the message to the DLQ.
