# Demo: Successful Atomic Commits

In this lecture, let's have the demo for Kafka transaction as usual. I have provided the instructions where the readme file First let's ensure that we have the Kafka fresh Kafka container. So please do this Then I wanted to share this information. This is not something very important, but this is something good to know So in during the retry behavior, right? So that is a when the Kafka transactions fail spring will retry as usual However, the delivery attempt header whatever we were seeing as part of the previous section, right? so this time we Will not see that actually that is the delivery attempt header will be always one Why it's mainly because as part of the previous section we were not using transactions That is spring will be doing the retry in memory So it will be updating the header for us and that is how it was delivering the messages now when you use Transaction it does not do actually Okay, so it basically it will be aborting the transaction and it will be pulling the messages one more time So it will be doing a fresh re-delivery of the message So because of this reason right the delivery attempt header will be always one. So You might find that a little bit weird. So this is something I wanted to share Then other than that it will be sending the messages to DLQ and also Everything else will be working properly, but the delivery attempt header That might not be updated. So be aware of that Then this is the transaction ID prefix. We are currently using so this is fine for the demo But if you are going to be running multiple instances and all in production So probably to just to make this unique you can just include the application name some random ID Okay, so that you can have some unique prefix We discussed this in the previous lecture So let's scroll down and this is how we will be starting the processor application by using this configuration we will be opening three terminals one for console producer like this and We will have two console consumers one console consumer will be using the read committed option The other consumer will be using the read uncommitted option. This is the default option we do not need this, but I Included this just to be consistent with this and we will be producing messages like this. I Just did the docker-compose down and up so I am having the fresh Kafka container Let's go to IDE section runner. Let's start the application Then go to the terminal copy the console producer command and paste it here Okay, so this is good then Open two more terminals. So here I am going to use Read committed consumer. So this is a read committed consumer Okay, so the topic is not yet present. So this is why it's throwing this warning But is actually it's not a failure. Okay, so the first time it would be creating the topic if it's not present So this is why we are seeing this So if you are worried just simply just kill this clear the screen and paste this command one more time now The topic is present. We will not be having this warning. Okay, so then Let's copy the other console consumer command for uncommitted option hit enter Okay. So now we have two console consumers Now copy the message with the key one and we are going to use the console producer To produce that message just paste it here. Do not hit enter. Okay, so we are producing this message with the key one Do not hit enter. I want to tell this first Okay so here we have two consumers right and Our application is going to process and it will it will be producing two output messages As you saw we are simulating some delay red dot sleep like three seconds That's what we are using right? You can see that the read uncommitted will be getting the messages Immediately as and when the stream bridge produces the message, right? So we will be seeing that immediately as part of this read uncommitted consumer However, the read committed consumer will not receive those messages Only after the transaction is committed then it will be seeing those two output messages together Okay, so that is how it will work. You can see that Let's hit enter and go to the consumers immediately Now if you see here, we are not seeing anything, but here we see credit and debit Okay, so at this point the transaction is committed so here we are seeing credit and debit records You
