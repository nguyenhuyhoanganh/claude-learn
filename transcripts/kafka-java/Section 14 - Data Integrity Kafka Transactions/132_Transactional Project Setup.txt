# Transactional Project Setup

In this lecture, let me explain the project setup for Kafka transaction This is what we are going to do in a high level We will be having one processor application we are going to use console producer to produce that Transfer request like this Okay, we will be producing our messages with the keys and our processor application is going to use the keys For some logic, okay again, this is for demo purposes If the key is one is used the transaction will succeed If we use the key to the transaction will always fail if we will be throwing some exception Okay to see how it goes Then if we use key three, it might succeed or it might fail. It's a random Okay, so this is how we are going to simulate very simple and we are going to have two Kafka console Consumers one will be using the read uncommitted option. The other one will be using the read committed option So we are going to observe what kind of messages these consumers are consuming Okay, so this is what we are going to do in a very high level to understand the Kafka transaction As usual spring will be doing all the heavy lifting we will be focusing on our business logic We will be exposing our beam like this spring will be handling the beginning transaction committing transaction or in case of error Aborting the transaction, etc Let's come back to our IDE Let's please go to section 18 package under that We have two packages DTO and processor if you expand the DTO here We have the transfer request from account to account amount. So this is the request we are going to produce via the console producer our processor application will be consuming this message and it will be producing to transaction request messages account amount and the transaction type One is for the credit request. The other one is for debit request. Let's go to the processor Let me explain the setup Here I am auto wiring the stream bridge. We are going to produce two output messages So I am using stream bridge for that for the output binding. I am having one string constant here So this is where we are consuming the message the transfer request First I am extracting the key Depending on the key, we are going to simulate different behavior as we had already said if the key is going to be 1 It will be success if it's going to be the key is going to be 2 it will fail always If the key is 3 then it might be it might succeed or it might fail at random In all other cases, we are going to throw invalid key exception This processor method internally uses this private helper method, right? So let's check this The handle transfer request method accepts three arguments the transfer request Then it accepts two runnable, right? So before I explain this, let me explain these methods first So here we have one method No, ah, basically we are returning one now we are returning one Runnable, which does not do anything. Okay, so this is intentional Then here we are returning another runnable Which is going to throw exception at random as you see It's going to generate one random number between 1 and 100. Let's say you are passing 80 So what it means is that 80% of the time it will be throwing this exception Okay, so that is how we have created this Okay to runnables like this Then we have another method run with delay. So here we will be passing one runnable So it's going to simulate some delay Okay, so we will be sleeping like three seconds. Then it will be executing the runnable. Okay Three simple Helper methods like this. Let's scroll up. Let's go to the handle transfer request method now Here we are going to receive the transfer request, right? Then we are building two Transaction request like this from the transfer request very simple one credit request one debit request Okay, first we are emitting the credit request using the stream bridge Once it's emitted I am running the runnable with the Runner using the run with the delay method. I am running the runnable Okay, then we are emitting the debit request Then I am running the runnable to okay. So this is what I am doing now. You might be asking what is this? Why are you doing this? You might be asking like this because here by using this I am kind of trying to simulate some processing time Some random failures, etc. So this is why I am doing this way Now if you scroll up if you check this method, right? We can understand this a little bit better. It is when the key is one. We are calling the handle transfer request method Message dot get payload it that will give us the transfer request So that is what we are sending here as part of the runnable one and runnable two. We are using no As part of the runnable one and runnable two we are using no op so basically no exception because of this reason The credit request and the debit request will be sent successfully without any issues. No exception will be thrown perfect in case of key two We send the transfer request as part of the runnable one We are using no op. So basically here the exception will not be thrown But as part of runnable two hundred percent we are throwing the exception so after emitting the debit request Here the exception will be thrown when the key is two Okay, when we use Um the key three, right? So that time we are simulating failure 30 percent of the times either 30 percent Okay, so either We will we might be throwing exception here or we might be throwing exception here Hopefully you understand the setup now, right in case of any questions, please feel free to ask me via the q a section Then let's check the corresponding the configuration yaml go to resources section 18 processor yaml here The cloud function definition processor. We know this as part of the bindings Processor in zero the processor is going to consume So processor in zero The destination is transfer request I give some group name then using stream bridge. We are producing the output messages. So this is the output binding name Okay, so this is the corresponding that topic Okay, perfect then The transaction might fail right? So spring will retry after all the retry Attempts exhausted spring will be sending the failed message to the dlq So that is what i'm enabling for this consumer binding Enable dlq and I provide the dlq name here As we had said we will be using the console producer to produce messages with the keys Our processor application will be consuming the messages Since we are using the console producer. It will be always using the string serializer internally So our processor will be using Key deserializer string deserializer. Okay to deserialize the key Okay When the transaction fails when the processor application when it sends the message to dlq Sends the message to dlq that time it will be producing the message with the same key actually so for that we also have to Have this producer property. So key serializer string serializer We have two more properties to discuss Transaction id prefix As we had already discussed we need to set the transactional id property, right spring will be Doing that internally for us. However, we have to give some prefix for our application So since it's a demo application i'm using a simple prefix like this But for your actual production grade application, um, you might want to include the application name as well Check the readme. I just updated this here. So you should be able to see okay, so That is what I would recommend Okay Then the producer configuration acknowledgement all you remember the the replication factor Right. So we have one, uh, the leader node and we might be having couple of follower nodes So the data will be continuously replicated, right? so Whenever we produce messages, we want to ensure that the data is replicated properly. So This is the this configuration ensures that okay So it prevents data loss on broker failure. So this we need this For kafka to provide the transaction guarantee So this is important
