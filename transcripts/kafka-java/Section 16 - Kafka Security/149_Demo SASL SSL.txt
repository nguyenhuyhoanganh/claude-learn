# Demo: SASL SSL

In this lecture, let's talk about the setup for Sassel SSL. That is, as part of this, we are enabling authentication and we will also encrypt the communication. This will be more or less like the previous lecture. We have one additional step for SSL. First, we do not have any change here. It is about the authentication, so we are not changing this. This is good. Then, as part of server properties, we will enable Sassel SSL for the external communication. We also have to provide a couple of more properties we will discuss in a minute. Then we have to provide the Java keystore file which contains the private key and identity certificates. Once you have all these, if you start the server, then it basically enables Sassel with SSL. On the application side, along with the username and password, they will contain the trust store to connect to the broker. Very simple. Let's please come back to our project workspace. Please go to the cloned project, Kafka Security, Sassel SSL. If you expand here, we have the Docker Compose YAML. As usual, here we have the jazz kind of file. So we have that we have discussed. So let's ignore that. As part of the volume mapping, I am mapping one search directory. It is here. So here I have the the keystore and the trust store files. So I am mapping that with the Docker container here. OK, so let's discuss the server ENV file. If you open this. Everything remains same. As I said here, we are doing Sassel SSL. Now, the Sassel authentication mechanism is playing, so there is no change. So this is good. Now, as part of the SSL, we have to tell the Kafka broker that in this location, I have the keystore file and the trust store files. So we have to provide the location. OK, so that is what I am sharing. And if you remember, when we create the Java keystore files, we have to give some password. So I am just using the change it the default password. So this is what it is. Please open the terminal, go to the path where we have the Docker Compose YAML for SSL. So for me, this is the location. Before you do the Docker Compose up, please ensure that you do not have any Kafka containers up and running. OK, so let's do the Docker Compose down if you have something running. OK, so for me, everything is good. So I'm going to do the Docker Compose up. OK, so the Kafka server started. Let's come back to our IDE. If we go to section 20, section runner, and if you use the Sassel plain text consumer YAML, if I run this right, it should fail. It should not be able to connect because now we are enabling SSL, right? So the username password alone, it's not enough. So if you see, it's not able to connect. So let's stop this. We also have to provide the trust store. Only then the consumer will be able to connect to the broker successfully. I am having that as part of source main resources. Section 20 under that. Here I have the trust store. And this is the Sassel SSL consumer YAML. So here we are using the trust store. OK, along with the username password, we also have to give this. This is what I am doing for the producer as well. OK, so now let's use this. So 03 Sassel SSL consumer 04 Sassel SSL producer. OK, so let's run this. It should be able to connect to the broker successfully. Wait for the partition assignment. OK, perfect. So let's run the producer application. So it's able to produce messages and here we should be receiving the messages. Perfect. So that's it for the demo. Let's stop this. Then let's please do the Docker compose now.
