# Demo: SASL Plaintext

In this lecture, let me explain the Kafka broker setup to enable Sassel plaintext. Please go to the cloned project, 05 Kafka Security. Under that, we will be seeing two directories. Let's focus on the Sassel plaintext. If you expand here, you will be seeing the Docker Compose YAML. Open the Docker Compose YAML. Up to this point, we should be familiar. We have already discussed as part of the Kafka setup earlier. Let's focus on these. Here, as part of the volumes, I am mapping one file with the Docker container, right? So, this file is here. Conf, the jazz, Conf. So, here I am using the plain login module to enable simple username and password authentication. This is the username, app-user. That is the username our application has to use, and this is the password. Basically, this Kafka broker will allow the applications when they use this username and password. Otherwise, it will reject the connection. Okay, we have this file, and we are doing some volume mapping. Perfect. But when the Kafka broker starts, it does not read this file automatically. So, we have to set this property. We have to tell explicitly that the config file is present in this path. So, we do that by setting this environment variable. So, we simply provide the username, password, login credentials in the jazz config file. However, we have not enabled sassel plain text authentication. So, we have to provide some additional environment variables, which I keep as part of the server.env file. Now, it might look like we have many environment variables to enable sassel plain text, but actually it is not. Unfortunately, this Kafka docker container, right, it expects minimum set of properties. You have to give all these properties if you are going to use environment variable. Otherwise, don't give anything. This is how it works. Yeah. So, this is why we have to give all these things. For example, Kafka process rules, broker, controller. So, even this we have to give. The node ID one, we have to give this kind of information. So, if you do not use this, node ID one is the default. However, if you are going to use environment variable, this docker container expects give all these things. Only then I will start. Otherwise, I will not start. So, that is how it works. So, it's kind of annoying. So, we have to give all these details. So, first we set the process rules, broker, controller, node ID, cluster ID. Now, I'm sure you will be having this question. We are going to run one single Kafka broker. Why do we have to set the cluster ID? Remember that Kafka is always designed to run as a cluster, even if you are going to start one single broker. So, it basically, it runs like a cluster with one node. That is how it runs. So, we have to provide cluster ID. Then we provide the listeners. This is the listener name, external. We are going to listen on 9092 for controller 9093. So, the controller listener name, controller. The interbroker listener name is external. Again, in our case, right, there is no multiple brokers. So, there is no brokers communication. You can argue like that. I totally get it. But this is the mandatory property. You cannot ignore this. You have to give something here. So, this is why I'm giving this external. Okay. And Kafka advertised listeners. So, how the external application can connect to Kafka broker? So, we are going to say that localhost 9092. Okay. And who will be voting? So, in this case, it's a single node. So, this node, the container name is Kafka and the node ID is one. So, one at Kafka 9093. Again, all these things we have already discussed as part of Kafka cluster section anyway. So, the important part here is the listener security protocol map. So, for the external communication, we are going to use Sassel plain text. So, here we are enabling the Sassel authentication. Then we have to say Sassel enabled mechanism is plain. Sassel mechanism interbroker protocol plain. Please open the terminal. Go to the directory or go to the path where we were discussing the Sassel plain text setup. Okay. So, this is the path where we have the docker-compose-yaml environment directory, con of directory, et cetera, for the Sassel plain text. Okay. So, now we are going to do docker-compose-yaml. Before that, I want to issue this command, docker ps-a, to ensure that this is how you see. There should not be any containers running or even stopped state. Just ensure that you have the clean state like this. If you have some containers still running for you, probably this could be because of the Kafka setup. You might be still running. So, please do the docker-compose down here. Then we are going to do the docker-compose up under this directory. Okay. So, please do that. Now, let's do docker-compose up. Now, we are running the Kafka broker with the Sassel plain text. The Kafka server started. Now, let's come back to our IDE. Let's please go to section 03. Here, normally this is how we will be running our consumer application, right? If I run the consumer application, it will be connecting to the Kafka broker. Now, if you see, it cannot connect because as part of this section 03 module, we did not have the username, password and all. So, because of this reason, this is how it will be. Okay. So, it might not be able to connect to the Kafka broker as it used to do before. So, let's stop this. Now, let's go to the package section 20. So, scroll down section 20. It's same as section 03. I simply copied and pasted everything here. So, we have the same producer and consumer application. Whenever we get the message, we are going to simply print it on the console and the producer will be producing messages like message 1, message 2, message 3 and so on. If I am using the same code, then what is the difference? Okay. The difference is different configuration file. If you go to source main resources, section 20. So, now the consumer application will be using this Sassel plain text consumer YAML. Again, same YAML, but we are enabling the security protocol. Sassel plain text, Sassel mechanism is plain and our application will be providing the username and password. App user is the username, secret123 is the password. Okay. So, now this is the consumer YAML. Similarly, I have the producer YAML. Okay. So, we have to add this Sassel plain text, app user, secret123, etc. Okay. So, this is what I am using. So, in the section runner, now the consumer is pointing to section 20, Sassel plain text consumer, section 20, Sassel plain text producer. Okay. So, now if I run the consumer, right, it should be able to connect to the broker. The partition should be assigned. This is cool, right? So, let me stop. Let me change the password for the consumer. Let's say secret1234. Let's keep it like that. This is the wrong password. So, if I run the consumer application, it should fail actually. Okay. So, now invalid username and password, right? So, this is great. Okay. Let's correct the password. Let's run the consumer application. Wait for the partition assignment. Okay. Perfect. Now let's run the producer application. Okay. It's able to produce messages. And here we are consuming messages. This is great. That's it for the demo. Let's stop this. Then please do Docker Compose now.
