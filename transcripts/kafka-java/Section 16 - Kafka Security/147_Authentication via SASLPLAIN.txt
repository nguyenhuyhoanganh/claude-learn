# Authentication via SASL/PLAIN

In this lecture, let's talk about SSL plaintext. That means we enable authentication but no encryption. No worries, we will also enable SSL later. Let's do one by one. As part of authentication, we are going to use simple username and password authentication. These are the steps we have to do. First, we have to create one jazz configuration file like this. There is an interface called login module. This is an implementation class for Kafka. So we need to provide that. Then you have to provide the username and password in this format. User underscore username equals password like this. So user underscore order service secret one. User underscore product service secret two. So basically here, the order service is the username. This is the password. Similarly, product service is the username. This is the password. So you can have multiple username password as like this. Once you have this file, then we need to enable SSL using this property. SSL enabled mechanisms. Here we are giving plain. The plain and plain text, they might sound similar and it could be confusing but they are different. When we say plain text, we are saying that the communication is not encrypted. That is, we are not using SSL. That is what we mean by plain text. When we say plain, here we are using simple username and password authentication. So this plain is about SSL authentication mechanism. This plain text is about the communication encryption. Then we have to provide the listener security protocol map. The listener name colon corresponding security protocol. We have to provide this like a key value pair like this. We have already discussed as part of the Kafka cluster section. Please go back and refresh if you had forgotten. So again, this external is just a listener name. It can be anything. So by providing these properties and providing the JAWS configuration file, we will be starting the Kafka server. This is how we will be enabling the SSL plain text on the server side. Once you enable SSL here, the applications cannot simply talk to the broker after that. Applications must use credentials. This is how we will be providing the credentials in the Spring Cloud Stream configuration YAML. Here I directly give the username and password in the file. This is just an idea. But in the real life, as part of your application minus prod YAML, you can get the credentials from the AWS secrets manager, whatever you are using, and you can parameterize this.
