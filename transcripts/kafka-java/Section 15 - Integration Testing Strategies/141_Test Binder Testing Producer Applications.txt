# Test Binder: Testing Producer Applications

in this lecture let's write that test for our order producer application so I am creating one test class like this lecture 03 producer test I am adding this spring boot test annotation like this so section runner producer class from section 19 so that is what we load here then the properties section is section 19 for the config we are going to use the 04 producer for this application okay perfect now when we run the test at that point it will be starting the producer application and that the producer application will keep on emitting messages every second right so using this output destination we should be able to receive the message okay so very simple so you have to give some time out let's say one second sorry thousand millisecond okay so that is what we are giving and the name the topic name is order events right so that is what we are giving here however this is going to give us the byte array okay so we have to deserialize that into our order object actually so I am going to have one helper method in the abstract test so protected which is going to return type sorry message of type T like this let me add some space here okay so the method name is going to be received like this here I am going to provide the topic name I'm going to call this destination then the class okay I'm going to call this type like this then using the output destination this is the timeout and here we are giving the topic name or a destination so I will be getting the message this is this is message of byte array right then using the JSON mapper you might be seeing multiple JSON mapper we are interested in this tools Jackson data bind that JSON mapper dot shared dot read value so here you can provide the byte array so message get payload and provide that type so now this will give us the actual the object the order object okay whatever it is so let's call this payload now I'm going to return this message builder create message provide the payload and whatever the original headers right I'm going to simply just add everything and return now using this helper method in our test method we are going to receive couple of messages so message one equals this dot receive so let's provide the topic name which is order events so our producer is going to produce messages our test is going to messages from order events so we want the order class okay order from section 19 so let's do this one more time okay message 2 if it is confusing what I am doing here just to keep all of us on the same page when I invoke the receive method we internally call output destination dot receive right this method will be waiting to get one message okay so that is what it gives us so this method will be waiting to get one message maximum it will wait for one second so first we wait to receive one message the very first message then we wait for maximum another one second to receive the next message okay so we are receiving two messages and we are going to validate that that's enough for our test then we can use assertions to assert assertions dot assert equals let's add that our producer is producing messages with the keys so let's start with that let's validate the key so for the very first message the key should be one okay that is what I am going to validate so message one get headers get Kafka Kafka headers dot now we want we might want to use received key that is what we had discussed earlier right but remember that that is done by the Kafka binder for your actual consumer application but this is test and we are not using Kafka binder our test binder will give exactly the way in which the producer produced okay so you should not be using received key so here we should be using key okay remember that this test binder and you are going to get the message whatever the producer produced right you are going to get that as it is so we will be using key here remember that okay and key type is integer class okay so this is how we are validating oops sorry I have to add this okay so this is exactly why I modified the producer to produce messages with the keys for this test so that I can explain this to you okay so hopefully this is clear so let's add a couple of more assertions so then let's validate the payload get payload so let's validate the order ID okay so that should be one as well okay then let's add one more assertion here we can validate the product type so product type it should be digital right so get payload and the product type should be digital I have added this note and I have added the same assertion for the message too so the key should be to the order ID should be 2 and the product type should be physical so that's it I think we are good let's run this test perfect sometimes this logs could be confusing that here we see only the order ID 2 but it looks like the order ID 1 is not even produced right but actually if you check here it should be there
