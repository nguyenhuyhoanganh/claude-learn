# Testcontainers: Testing Producer Applications

In the previous lecture, we tested our consumer application by using stream bridge. It was very simple and straightforward, right? Now in this lecture, let's test our producer application. So the producer application is going to produce messages to the order events Kafka topic. Now our test will be acting like a consumer, right? How are we going to consume that? When we used a test binder, we had input destination, output destination. So that was very simple actually, but when we use Kafka binder, how to receive the messages? So is there anything like stream bridge? Actually we do not have any, but it's easy for us to expose one bean like this. So this is the Kafka consumer, right? This is how we have developed a multiple consumer application so far, right? So our producer is going to produce something, our test can act like a consumer. So to consume messages from order events topic in the test class, we will be exposing one bean like this so that it will be subscribing to the order events topic and it will be getting all the messages. Then we can use that to validate. So it's very simple. Let's come back to our IDE section 02 under this, I simply just copied the lecture 03 producer test from section 01 I pasted here. So here we are seeing compilation issues is mainly because we do not have this method and all as part of our abstract test. So this is fine. We are going to make this work by using test containers. Our test class is going to consume messages from the order events topic. So for that, we are going to expose one bean here. So for that, I need one configuration class. So let's create test consumer configuration like this. Let's not forget that test configuration annotation to consume orders, we have to expose a bean. So let's do that bean public consumer of message of order from section 19. So let's call this test consumer and let's keep it like this. Now here I will be receiving the message object. What are we going to do with that? So how can we validate that here? So for that, I am going to expose another bean, please be patient, everything will be very clear. So here I am going to use blocking queue. It's a java util concurrent blocking queue. So this is going to be message of order type section 19. Let's call this orders. Let's keep it like that. Return new linked blocking queue. So this is thread safe. So since this is a bean, spring will be able to inject this. So we can get this instance here. So whenever I get the message, I am going to simply just add it to the this blocking queue. So this is how I am going to do, I can use method reference. So let's do that. Since this is a bean, we can also inject this bean in our test class here. So I can say auto wired private. I can just inject this bean here. So now using this orders instance, we should be able to get. So orders dot four. So we can say like this. So here you will be giving some time out. We are saying that time unit seconds like maximum. I would like to wait for five seconds to get the first object. So that is what we are saying. So it's going to throw exception. So add exception to the method signature. Let's copy this and paste it here as well. So here we are waiting for two messages. So that is what we are doing. Now there is a very good chance that it might return null. So we are going to ensure that assertions assert not null message one. So let's add this validation for message two as well like this. Then here we are using the Kafka binder and our test is a consumer. So we should not be using key, we should be using received key. So let's ensure that as well received a key, received a key like this. Now at this point, everything looks good, right? Do you think that we can run? Actually no, it will not work. That is, this is our producer application. Our producer application will be loading this file to run, right? So we will be creating the producer bean, etc of it. Now exposing this bean alone, it's not enough to consume the messages, you know, right? So we have to provide which topic, all those information we have to provide that. So we need to do that. As we had discussed earlier, just exposing the supplier bean alone does not create the Kafka producer automatically. So you need to have the bean plus you need to have this configuration along with this binder. So they all together create the Kafka producer application, right? Similarly, we have just exposed one bean. That's it. What about this configuration, right? So we have to have this configuration as well as part of this configuration for the test here, spring cloud function definition. I have the test to consumer. So that is what we have. Then spring cloud stream bindings test to consumer in zero is order events topic. Then here, the producer is producing with the integer serializer. Since we are going to validate the key as part of the test, we have to deserialize. So we have to include the key deserializer as well. Okay. Remember that. Then auto offset reset earliest. So this is something we know already. So we have to have this configuration as part of the test. Let's come back to our IDE. Now I'm going to do a couple of things here. Let me do one by one. We have the test configuration, right? We have to import that. So let's add the import test consumer configuration class lecture zero three producer test. Okay. So we have to import this first. Okay. Then as part of properties, I'm going to add these properties. No worries. I'm going to explain all these things one by one, whatever I had explained earlier, the properties for the test consumer. So that is what I have given here instead of YAML file, it's in the properties format. So okay. Let me explain the spring cloud function definition as to consumer. But if you notice, I am giving producer semi-colon test consumer. Why? This is what we had discussed, right? So why am I adding the producer as well? Okay. If you remember that test, when you give properties like this, right here, you are saying section 19 zero four producer. So as part of this spring will be loading this file first. Okay. So here you have this spring cloud function definition producer. Now, if you remove this, right, you are, you are saying that I am overriding the spring cloud function definition to test consumer. So this is what you are saying. If you, if you mention like this, the producer bin will not be activated at all. So you are not creating the Kafka producer at all as part of your application. So what we are saying here is I want the producer along, then along with that, I also need the test consumer. Okay. So this is why we are saying that we have two beans. One is producer as part of the application. Then I have test consumer as part of the test. This is the only conflicting property, all other properties are specific to test consumer. So we are not going to impact the producer or we are not going to override any of the producer properties. So no worries. So here I am giving spring cloud stream bindings, test the consumer in zero destination is order events. Then consumer properties deserializer is integer deserializer, then consumer properties auto offset reset is earliest. Okay. So these are very simple as we had just discussed. At this point, we have the configuration, we are importing the test consumer configuration. We have the code ready. So everything looks good. We can run the test. Let's see how it goes. Look at that, that test fast, but when it passes like this, I don't trust this. Okay. Let me quickly test. Let me change that to digital. Let me run this. Great. So now if you check, it says that you expected digital, but actually we got physical. So this is cool.
