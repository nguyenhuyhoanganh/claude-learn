# Integration Test Project Setup

As you know, we have different types of applications, like producer, consumer, and processor. To learn and practice writing integration tests, we need some applications for all these types. So, I am going to reuse the project whatever we had done earlier for content-based routing. Here we have the producer, processor, and a couple of consumers. So using these, we can see how to use test binder and test containers, etc. Let's please come back to our IDE. Under source main Java, please go to section 19 package. So this is where we are going to have the producer, processor, and the consumer, etc. We can use the old, the previous project, but I did not want to mess up that project. So whatever we are going to do or modify for integration testing, we can use this project. Let's quickly check. The producer will be producing order objects like order 1, order 2, order 3, and so on. The product type can be physical or digital. I produce the order object with the keys, as you see. So I am using the counter as the key. Then we have the processor. Here we consume the order objects. We check the product type. Depending on the product type, we build the delivery object. Then we send that to appropriate output topic. Then we have two different consumers to consume the digital delivery objects and the physical delivery objects. And currently, we simply just print them on the console. Then under source main resources, under section 19, we have the corresponding configuration YAML files for all these applications. We have different types of applications, right? Producer, processor, and the consumer. So how do I know that my producer application will work correctly? Does it emit order objects every one second, or if I give one order object, will the processor give me the corresponding delivery object? So this is what we are going to test. Similarly, if I emit one physical delivery object to a topic, does this consumer consume the message properly? So this is what we are going to test using test binder and test container.
