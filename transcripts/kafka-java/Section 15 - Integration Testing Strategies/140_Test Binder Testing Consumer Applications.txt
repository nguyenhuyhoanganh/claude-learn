# Test Binder: Testing Consumer Applications

Let us come back to our IDE, under source test java, I am having two packages, section 01 and section 02. Section 01 I am going to use for test binder and section 02 is for test container, ok. So here I have one abstract test. Because we are going to write multiple test classes like one for digital consumer, one for physical consumer and we have producer, processor and all, right. So all these test classes will be extending this abstract test. They are all going to use test binder. So I am having one abstract test like this. I am adding this enable test binder. So I should be able to inject these two beans in our test class, ok. Then I am creating some, I am having some string constants for the topic names. Remember these are all topic names, not the binding names. So if you check the processor yaml, right, we are using order events, digital delivery, physical delivery. So these are the topic names. So this is what I am having here. Then I have one digital consumer test class which extends this abstract test. I am keeping the name as lecture 01, lecture 02, etc. to keep things organized in an order. So this is why I am doing this way. So we have one test method. We are going to write that test together. So no worries. Now there is a challenge in testing our digital delivery consumer application. Using the input destination, we can send the digital delivery object to this topic. That is not the challenge. It is very simple, but how are we going to validate that? If you see our consumer application simply consumes the messages and it is going to log on the console, right. In the actual production grade application, you will be having the repository. You will be inserting the records into the database table or you might be updating records, etc. So in our test class, we can auto wire repository and we can validate if the record has been inserted successfully or not. So that is how normally we will be writing our test. Unfortunately, our digital delivery consumer, it simply logs the object on the console. So for us, the only way for us to validate this by checking the log. Interestingly, Spring Boot provides one extension for us for the JUnit test. So let's go to the digital consumer test here, extend with output capture extension. So by using this extension, we can check the log. With this extension, we should be able to receive the captured output object and using this object, we can access the log get out. This will give us the string using, then we can using the contains method, we can check if the log contains some text, whatever you want to validate. So we can validate like this. Now just to be very clear, I am not saying that this is how everyone should be writing the test. So I am not saying like that. Since we are facing this challenge, we are going with this approach. However, later in this course, we are going to develop an application with the proper databases. So we will be writing better test. No worries. With that information, let's test our digital delivery consumer application. So for that, first we have to build this object. So new digital delivery from section 19. So this accepts the order ID. So one and some email address, sam at gmail.com. Let's keep it like this. Then using the input destination, we can send. However, if you see it accepts only the message type. So let's build the message. Message builder with the payload digital delivery dot build. So now we have the message. So let's pass the message and we have to give the topic name, which is digital delivery. At this point, you are actually sending the message to the consumer application here. So this consumer application will be receiving the message and it will be printing this on the console. So this is what we are going to validate. So we can use the assertions dot assert true output dot get out dot contains, we are going to check if it's like this. So this is how we are logging. So that is what I'm going to check, digital delivery object like this. This is a spring boot test. So let's not forget that spring boot test annotation. However, here we have to specify the classes. Normally we do not do that. Here we have to do is mainly because under source main Java, we have multiple spring boot applications. So normally in your application, you will be having only one, but in our playground project we have many. So we have to tell spring that this is the application we are testing, the digital delivery consumer. So we have to explicitly mention that. So section runner from section 19 dot digital delivery consumer dot class. So this is the main spring boot application. And our application expects two properties. Let's not forget that as well. One is section, which is section 19. The other property is config. We have to mention that as well. So that is 01 digital consumer. That's it. At this point, we can run this test and see how it goes. Perfect. And you see, we sent the digital delivery object right. And our consumer application is able to receive that and it logs that. And that is what we are validating here. So if I remove this colon, now that test should fail actually, because we do not log like this. Okay. So because of the reason it fails. So now it should pass. Perfect. Right. I also created one test class for physical consumer. Same concept. Now we are building the physical delivery object. I am sending that to this topic and that is what I am validating. Let me run this as well quickly. Okay. So that also passes.
