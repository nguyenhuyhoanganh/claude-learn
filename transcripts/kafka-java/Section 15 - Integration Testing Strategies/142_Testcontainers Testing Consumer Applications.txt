# Testcontainers: Testing Consumer Applications

In the next few lectures, we are going to write the integration test using test containers for our application. We are going to use the exact same application, but we will be writing the integration test using test containers. As I had mentioned already, test binder is optional, good to have, but test containers is must have. Please ensure that you have at least few end-to-end test using test containers for your application. If your application is very simple and you have only few tests, then just use test containers, you can ignore the test binder. This would be my recommendation. When we use test containers, we are running the real Kafka broker using Docker container and we will be using the Kafka binder as well. So the setup will be more or less like this. That is, if your application is a consumer type application, then our test will be acting like a producer because this consumer application will be subscribing to a Kafka topic, right? So our producer application will be producing messages to this Kafka topic so that our consumer application will receive. We will be testing that. If your application type is producer, then our test will be acting like a consumer, okay? So that it will be subscribing to the topic to see if you are receiving the messages. If your application is a processor type application, then our test will be acting like both producer and consumer. Basically, we will be producing messages via one topic, the processor will do some processing and it will be emitting another message for the input message, right? So our test will be subscribing to this topic to see whether the processor is working properly or not. Let's write that test one by one as we did earlier. Let's start with our consumer application. So for that, we will have to produce messages in our test class, right? So that our consumer application can receive that. So how are we going to produce messages to a Kafka topic? Okay. So this is not test binder. So how are we going to produce messages to a Kafka topic for that we can use stream bridge. It's very simple. So let's do that. Let's come back to our IDE. Let's please go to source test Java section zero two. This package is for test containers, okay? So here we have one test containers configuration class. This is something the spring initializer itself will create this. We do not have to do anything special when you initialize or when you set up your project using spring initializer. So that will be creating this for you. So as you see, it's a beam, we will be importing this configuration in our test class like this import test containers configuration. So when you run that test, then this will be automatically starting the Docker container. Okay. So as part of this section zero two abstract test, as usual, I am having the string constants for our, the topics we are using. And since we are not using test binder, I cannot use the input destination, output destination, et cetera. Okay. So these are all part of test binders. So now we are writing a better test, actually real test. We are mimicking the production setup so we can use stream bridge as part of our test to produce messages. So I am keeping this as part of abstract test. Then I have the lecture zero one digital consumer test, which extends this section zero two abstract test. We have the same extension, the spring boot test. Okay. Everything looks exactly the same. Even the first two steps, right? That is also same actually. Okay. So now we will have to send the messages to the digital delivery topic, right? So we can use stream bridge, this dot stream bridge dot send. So we can say digital delivery, right? So provide the topic name and provide the object message like this. So that's it. So instead of output, sorry, instead of input destination, we are using stream bridge to send the messages. So that's it. Very simple. Now we can run this at this point, this should be creating the docker container. Okay. So I have already pulled the image. So because of that, it quickly ran for me. Probably if you are running this for the first time, it will be pulling the docker image. So please be patient. We seem to have done everything correctly, however, that test failed, right? So why? Okay. So when we use, when we used the test binder here is everything in memory here. So as soon as you send the message, the consumer will get it and you are able to validate immediately. But as part of this, we are bringing the test container. So it's not like as soon as you send, you can immediately validate. Okay. So it might take some time. It might take a few milliseconds. Okay. So it's not like it will take one hour or so it's like it will take a few milliseconds because they are using the real Kafka broker. So it will take some time. We have to account that as well. So we can use thread.sleep to sleep for a few seconds so that we can validate later. Otherwise, we can use a vitality library. Okay. So this is something we can use or a vitality. Okay. So a wait. So that is here, what we are saying here is at most. So, okay. Let me write this first. Then I'll explain until asserted. Okay. So here we are going to add this assertions here like this. What we do here is we expect this assertion to pass. Okay. But it's not going to happen immediately. So we are saying that this is what we want this to happen, but it might not happen immediately. So maximum, I would like to wait for five seconds. Okay. At most, I would like to wait for five seconds. So this library, what it will do is that it will check periodically. So if it fails, then it will wait for some time and again, it will check. Okay. So maximum, it will wait for five seconds, but it will check again and again. And when it passes, then it will exit. Okay. So this is exactly what we want. We can use thread.sleep, but it will be like hard-coded sleep, right? So this is actually better. You are not really sleeping for like one second or two seconds. So within like 10 milliseconds, if we got this output, then the test will exit successfully. So this is why we are using this availability library. The default interval to check this is 100 millisecond. If you want to adjust that, there is an option polling, poll interval. So here you can provide the duration. Okay. So if you want to change that to 50 millisecond, 10 millisecond, et cetera, you can also give that, but we can go with the default value. So no big deal for us. Okay. So let's run the test. Same test. So now we have added this availability, nothing else. So let's run this. Now look at that. So the test passed. I added the steps for the physical delivery consumer as well. So let me run this.
