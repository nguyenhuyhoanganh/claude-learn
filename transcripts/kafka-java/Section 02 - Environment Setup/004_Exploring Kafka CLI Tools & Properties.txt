# Exploring Kafka CLI Tools & Properties

I have one readme file as well. I would have shared some instructions. So let's do this one by one Open another terminal first ensure that your container is up and running the Kafka container is up and running for me then We are going to use the exec command to launch Shell in an interactive mode inside the running container. So that is what I am doing Docker exec minus IT Kafka is the container name bash Shell, so if I hit enter now, I am inside the container and We will be landing in this path. This is our working directory in the docker-compose-yaml If we run this command to list the files ls-l in this path Here we can see many Command-line tools like we will be using some of these actually So for example the Kafka topics, so this is something we will be using in the next section. You will be seeing Even though this Kafka topics a sketch. It's part of this path in the in this directory. I cannot simply Run it like this if I It will say that command not fault. Okay. So the main reason is mainly because if you check the path This OPD Kafka bin it's not part of this path variable So because of the reason right so it cannot execute like that So whenever you try to execute any of these commands you have to always Add this dot slash in this current directory we have this Kafka topics Tool I want to run this you have to say like this if you hit enter Then you will be seeing some output like this. Of course. I am not explaining what this is for and all no worries I'm going to cover that later in the next section first I want you to ensure that you are able to invoke this command etc. Nothing else Let me clear this So currently we are in the bin directory, right? So let's come out of that and here We have another directory called config under OPT Kafka. So let's Go inside and check what we have here and as you see Kafka has several properties file and the each property file will contain several properties Particularly we will be interested in the broker properties Consumer properties controller properties producer server properties, etc Again, if I try to explain now, it will be too much so let's take this slowly, but I wanted to show that this is where we have the property files and The bin directory is where we have the command-line tools nothing else If you need more details about the properties I Would have shared this link so you can check here. For example, you are seeing the producer properties, right? Let's say you have you are seeing one property and you want to know more about this in that case You can check the producer configuration. You can click on this and it's a huge list They will provide all the details about the property. Okay, so similarly consumer Properties you can if for that you can come and check this and so on
