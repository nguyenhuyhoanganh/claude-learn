# Demo: Scaling & Partition Rebalancing

In this lecture, let's have the demo. I have already shared the instructions via the readme file. First, we need a topic with the three partitions. So access the Kafka container, first delete the topic. We will have this topic actually, so delete this first, then recreate this topic with the three partitions. Let's start from scratch. If you have, if you face some issues, probably just to create a fresh Kafka container, okay? So let's start from scratch. First, let's do this. Open the terminal. First, let's delete the topic, then recreate the topic with the three partitions. Let's come back to our IDE. Let's start the consumer one. Now, if we can wait and see, okay, we can see that all the three partitions are assigned. Partition zero, partition one, and a partition two, right? So this is great. Now let's imagine the second consumer joins. I know we have not started the producer, but I want to see this first, okay? Otherwise it's very difficult to see this if it's going to log messages continuously, okay? So this is why I want to show this first. So when you have only one consumer, Kafka assigns all the three partitions to this single consumer, okay? Now let's imagine we have the second consumer joins. So now Kafka will be doing the partition rebalancing. So it got the partition two. So now this will be having partition zero and a partition one. If you see, initially this was the, okay, initially it had three partitions. Later Kafka revoked the partitions, okay? Then assigns new partitions. It got zero and one. This consumer got two, okay? So now let's imagine third consumer joins. So we have three consumers running now, okay? If you see now, if you check, it revoked the partition and reassigns new partitions, okay? Perfect. Similarly, if you see this consumer now, it has only one partition. Initially it was having two, it got revoked. Now it's having only one partition. So this is how Kafka assigns the partition. Now let's kill one consumer randomly, okay? Let's stop consumer one. If I stop this, now if you see the pre-destroy hook prints total messages consumed, right? So this is why I wanted to have the message counter, okay? So it will print like this. Currently it has not processed anything. So it prints zero, but this is good. So now we have stopped one consumer. So again, so now Kafka will do partition rebalancing, okay? So now it got two partitions. This has one partition. So perfect. Let's kill this consumer. I mean, stop this consumer. Consume total messages consumed zero, that's correct. Now consumer three should be having all the three partitions. So this is cool, right? Let's stop all the consumers. So currently I do not have any consumer running. Let's keep it like this. Let's start the producer. The producer is going to produce one item every one millisecond. So let it run, okay? So this is super fast. It's fine. Now let's start the consumer one. Okay, so now it gets the messages, okay? So the producer keeps on producing items. The consumer keeps on consuming all the items. Let's imagine this is a lot of work, okay? So too much load. So the autoscaler spins up another instance. So the consumer two joins now. Okay, look at that. Okay, so now we have two consumers, perfect. Okay, again, let's imagine that this is too much work. So the autoscaler spins up another instance. So consumer three joins now. Perfect, right? Okay, so now let's imagine that the autoscaler scales in. That is, it's stopping the consumers because we do not have too much CPU utilization now. So it reduces the number of instances currently running in production. Let's imagine like that. Let's stop consumer one. You can stop anything you want. Okay, let me stop consumer one. Okay, stop. Total message is consumed. Let's keep it like this. Now let's stop consumer two. Okay, this is perfect. Now the producer is running. Consumer three is running. Let's stop the producer. Okay, now let's stop the consumer. Then for every consumer, make a note of this number. For consumer three, we have this. Consumer two, we have this. Consumer one, we have this, right? Then how many messages we have produced so far? If you scroll up, okay, so 83343. If I add all these numbers or the number of messages processed by these consumer instances, I get 83343, which is same as the total number of messages produced by the producer.
