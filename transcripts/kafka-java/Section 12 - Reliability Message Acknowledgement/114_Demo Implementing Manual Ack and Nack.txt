# Demo: Implementing Manual Ack and Nack

I have already shared some instructions via readme file for this demo, so please refer to that. As usual, ensure that you are having the fresh Kafka container. I just did the docker-compose down and up, so please do that. Let's come back to our IDE, go to section 15, ensure that it's pointing to section 15.01 consumer. So let's run this. Then go to the terminal, ensure that you have opened two terminal windows like this. In one terminal, I'm going to use the Kafka console producer. So here we are going to manually produce messages. Then in another terminal, I'm going to describe the consumer group, the demo group. So if I hit enter, currently it shows that we have the demo topic, partition 0, so only one partition. Currently, we have not produced any message. So this is why it's like this. Now let's come here, let's go to the console producer. Let's produce message 1 first. If I hit enter, the message is delivered here. So here we can check the payload 1, and if you scroll to the right, we should be able to see the acknowledgement object. So this is the acknowledgement object. So here we can see the message. Now if you describe the consumer group here, we can see that the current offset is 1. And the lag is 0. Let's produce more messages, let's say 2, 3. So all those messages are delivered. And if you check, the current offset is 3, lag is 0. So our application is also acknowledging all those messages, right? So everything is going well so far. Now what happens if I produce 4? So we get 4, but we are not acknowledging the message, right? If you describe this, now here you can see the lag is 1. That is Kafka delivered the message, however, our application is not acknowledging the message. So only when our application acknowledges the message, right, only that time Kafka will be updating the current offset. Let's go to the console producer. Let's produce 5, 6. So all those messages are delivered. Let me clear the screen. It's confusing. Okay. So now if I describe one more time, here the lag is 3. So the 4, 5, 6, they are not acknowledged. Now let's imagine our application crashed or if you try to restart this application, something like that, right? Since those 4, 5, 6 messages, right, those are not acknowledged. Now Kafka will be delivering those messages. Okay. So if you restart again, we will be seeing those 4, 5, 6 again. So we have to acknowledge these messages. If you describe one more time, the lag is still 3. Okay. Now it is not like you have to acknowledge 4, 5, 6. Now if I produce one, our application will be acknowledging this one. Okay. So when it acknowledges this one, that implicitly means the 4, 5, 6, they all have been processed. That is how Kafka is going to assume. Okay. So now you will see that, okay, currently we have the lag 3, right? When I produce one, now this message will have been acknowledged, right? If you check, okay, let me clear the screen and describe one more time. If you check now the lag is zero, the current offset is set to 7 now. This is cool, right? Let's go to the console producer. Okay. Now let's produce the message 7 and see how the NAC works. Since we are going to generate the random value, you could be seeing a different behavior. So maybe the very first time itself, the message might be acknowledged. In that case, try to produce the message 7 again and again to see how the NAC works. Let's send this. Okay. So random value 9. Okay. So the very first time itself, the message got acknowledged. Great. Let me produce again. Okay. So temporary failure, it will be retried. See random value 6, again, temporary failure. You can see that, right? So the message is getting re-delivered again and again after five seconds. If you check the timestamp, it will be re-delivered. It's getting re-delivered every five seconds. At some point, we should be having the random value 9 or 10 that time. Okay. It succeeded. That's it for the demo. Let's stop this application.
