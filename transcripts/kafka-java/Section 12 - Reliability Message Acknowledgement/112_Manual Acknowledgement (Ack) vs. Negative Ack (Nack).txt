# Manual Acknowledgement (Ack) vs. Negative Ack (Nack)

In this lecture, let's talk about manual acknowledgement. So far, we have been saying that the Spring Cloud Stream handles the message acknowledgement for us. And honestly, that is what we will use and prefer in the real production applications. Let's take Spring Data JPA. When we save an entity like this, we do not explicitly commit the database transaction ourselves. The framework does it for us. The message acknowledgement in Spring Cloud Stream works in a very similar way. Remember that. But sometimes you might feel that you need to have more control. In that case, Spring provides an option for you to acknowledge the messages yourself if you want. Let's see how to do that. First, instead of exposing a consumer which accepts the order event directly, you have to expose your Bean this way like consumer of message of order event. So you have to accept the message type first. So this is how you have to create your Bean. Then in the Spring Cloud Stream configuration YAML file, here you will be providing all your configuration as usual. Then you have to enable the acknowledgement mode manual. You have to have this. Remember that this is not a consumer property. This is a framework feature for the Kafka binder at the binding level. OK, so Kafka bindings or even consumer in zero for this consumer. I am enabling manual acknowledgement mode. So that is what you are saying here. By doing this way, you are basically telling the framework that a spring. You do not acknowledge the messages. I want to control that. So that is what you are saying here. OK. When spring sees this. Then it will be delivering the message objects to you. Right. So that time what they do here is that in the message object, they add this header acknowledgement. And here they will be adding the acknowledgement object. Which you can extract message dot head and get headers. Get acknowledgement. You can get the acknowledgement object. Then you can acknowledge yourself. At this point, a common question will be, does this acknowledgement go to Kafka immediately? No, this acknowledgement object. It's a spring level abstraction. Spring tracks the acknowledgement you provide, and then it commits the offsets in batches because committing offsets. It's a network call. Remember that. So even in the manual mode, spring will try to be efficient. Just to be very clear, our application uses the framework. So our application talks to the framework. The framework manages the Kafka interaction. So this option gives us the control. But spring manages the interaction. If you really want this acknowledgement to go to Kafka immediately, there is an option for that as well. Instead of manual, there is an option for us to use manual underscore immediate. So in that case, this acknowledgement, whatever you acknowledge, right, that will be sent to Kafka immediately. There is something called a NAC, Negative Acknowledgement. It's not a Kafka concept. It's a framework feature. To better understand this, let's say we get the message object. We access the headers to get the acknowledgement object. So this is our acknowledgement object. Let's imagine like that. Then using the try-catch block, we process our message. It's a pseudocode to give you the idea. We process the message. If the message processing is successful, we acknowledge the message like this. In case of exception, we say acknowledgement dot NAC. Then we give some duration. So what it means is that the message is not acknowledged when you do NAC. So offset is not committed. But the message will be retried after a given duration. That is, when you do this right, Spring will be invoking the method again with the same message. OK. After this duration, we will be talking about the error handling and all as part of upcoming sections. But when you want to have more control, when you enable manual acknowledgement, if you want to control, retry everything like this, you can do. So when you do this way right, Spring will be re-delivering the message again and again for you until you successfully process this message.
