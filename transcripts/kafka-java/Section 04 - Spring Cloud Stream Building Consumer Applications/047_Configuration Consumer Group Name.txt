# Configuration: Consumer Group Name

in the first two demos we did not provide the group name because of this reason our consumer application became part of the anonymous group if you check the log every time you restart this ID anonymous ID will be different okay so new consumer groups will be created every time you restart that is how it works we know that okay so this time let's provide the group name basically same YAML we have to mention that this consumer is part of this group okay so for that I am having separate YAML okay same YAML only difference is this binding is part of or this consumer is part of this group so we have to give some group name I am just giving the name demo group now let's use this configuration let's go to section runner let's use that config 03 consumer group is what I am giving now if we run this what do you think that it will happen so a new consumer group called a demo group joins for the first time and if we are using the from beginning right so because of this reason we will be consuming all the five messages if I restart now we can check the log be patient now if you notice right it's part of the demo group now okay demo group group ID demo group okay so now we are seeing all the five messages right if I restart what it will happen since Kafka has the tracker for this demo group and it sees that all the five messages have been delivered to this consumer even if we have this property for this group Kafka will not deliver the messages so now it's waiting for the new messages you can also see that in the log if you scroll to the right here if you see committed offset offset 5 that is the offsets 01234 all those offsets are the corresponding messages have been delivered now it's at offset 5 waiting for new messages let's go back to the terminal let's produce a couple of messages say 6 now if I hit enter okay so now I am seeing received 6 right perfect 78 okay
