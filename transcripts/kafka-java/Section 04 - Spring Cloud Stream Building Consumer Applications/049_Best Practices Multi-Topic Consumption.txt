# Best Practices: Multi-Topic Consumption

Even though we have this support, I personally will not recommend this approach for production grade applications. Maybe it's good for testing purposes, but not for production grade applications. Why it's mainly because it might look good at first, but oftentimes it will lead to problems. That is, for example, you cannot have topic level override, we can have bindings level override, but not in the topic level. For example, what if you want to change the maximum poll records for one topic, but not for other topic. Now you do not have the flexibility. So it's always better to have one-to-one mapping, that is one binding, one topic. So it's better to have one-to-one mapping. So not mapping like this, you're getting right. So when you have one-to-one mapping, then it's easy for us to provide binding level overrides. The recommended approach will be, let's go to the consumer config. Here we have one bean, let's say this is consumer one and let's copy paste, let's create consumer two. Now here we have two beans, this is what we are going to do. Consumer one in zero will be consuming from demo topic one, consumer two in zero will be consuming messages from demo topic two. So you have one-to-one mapping, you have two bindings, now you can have bindings level override. But you might think that, but we will end up having duplicate code or redundant code, right? Actually, these beans are simply entry points to receive the messages from Kafka. We are not going to write the entire business logic here. That is, once you receive the message or the order event, let's say this is the web order event. In this case, we are going to call the process order method from your service class, then we will be passing the message, okay? Similarly here as well, you are going to call the process order method, you will be passing the message. So in that place, we do not have any redundant code, we are calling the same method, right? So we are reusing the method or service logic. So these beans are simply entry points.
