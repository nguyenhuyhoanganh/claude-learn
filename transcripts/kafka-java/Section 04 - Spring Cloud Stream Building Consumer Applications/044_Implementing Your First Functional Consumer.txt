# Implementing Your First Functional Consumer

In this lecture, we are going to create a Kafka consumer using Spring Cloud Stream at this point You know how it will look like For that I am creating one package called consumer under section 01 and I am creating one class called consumer config This is a spring configuration class and because we are going to expose a bean This is the pattern I am going to follow Across all the packages as part of upcoming sections That is whenever we create a consumer I will be creating one consumer package and I will be keeping this class under this Because as part of the upcoming sections, right one single section might be having producer consumer Processor DTOs, etc. So to keep things organized and follow the same pattern, so I am doing this way Let's create the very first consumer public consumer Java util function consumer For now, let's use string type message later. We can use Java objects For the the bean name. I'm going to simply use consumer like this So whenever we get the message, I am going to use the logger just to print the message Let's not forget the bean annotation That's it. Can you believe that we have developed one Kafka consumer application now, this is cool Okay. So now we are going to run this with this spring cloud stream configuration here Spring cloud function definition consumer that is the bean name Okay, then spring cloud stream bindings Consumer in zero the destination is demo topic Let's run this in the next lecture
