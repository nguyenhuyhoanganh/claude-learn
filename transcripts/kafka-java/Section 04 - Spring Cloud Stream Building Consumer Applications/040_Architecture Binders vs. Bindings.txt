# Architecture: Binders vs. Bindings

In this lecture, let's talk about couple of important terminologies The first terminology is binder As I had mentioned already Binder is a driver like Postgres driver. The name binder came up because it binds the application to the messaging system We will be developing our producer application consumer type application like this using Java 8 8 Functional interfaces and expose the beams You do not have to be an expert in functional style programming. It will be very simple actually This is what we would write as as a developer Then we will be including Kafka binder as a dependency in our Application so the binder is part of the application. So let's assume that the outer layer Is the binder let's imagine like this So far, it's clear, I guess The second terminology is binding binding is a mapping between your beam and a Topic in Kafka or any messaging system we use It's like whatever the supplier provides it goes to this topic Similarly, whatever the message this topic has it goes to this consumer So we as a developer we need to provide this configuration or mapping we call that bindings We provide all these mapping details in the application YAML or application dot properties file When the application starts the binder gets these bindings configuration then it understands Whenever we get the message, it will be invoking this consumer Similarly, whatever the supplier provides it will be sending that to This topic and so on very simple But how do we really map this beam with the topic? How does the configuration look like For that spring uses some naming convention. It could be weird or confusing. Please be patient the naming format is function name out index function name in index Here the out in Basically represent the output input What it means is that this function? Gives an output. It's a producer This function expects an input. It's a consumer What is this index? This is the annoying part. It's going to be zero for most of the cases. This is how it will be Function name out index. So order event producer is the function name or bean name? So order even producer out zero similarly payment event consumer in zero You can find this information in the spring doc for the most of the applications Index is going to be zero So we will be using zero Now I can totally guess your question that if it's going to be always zero way to include this, right? Yes, I totally get it Remember that spring cloud stream is a generic framework. They support multiple binders They try to support different different use cases So there is an use case for which the index could be different not zero But as I said, it's not very common. I Wish the spring team had designed this slightly differently, but it's fine I'm going to talk about that use case later. It will give you an idea No worries, but for now it will be zero here
