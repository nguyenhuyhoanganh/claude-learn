# Configuration: Auto Offset Reset

If we go to the source main resources section 01 from beginning yawal, here we can see that I am providing the Kafka broker or bootstrap in the information endpoint. Even though in our case, even if we ignore that the binder can assume this, I want to mention this explicitly so that we know where to update. Then this is the actual Kafka consumer property for the from beginning, that is auto offset reset the value is earliest. What it means is that you know that Kafka has the offset tracking, right? When it's not present, that is a new consumer group joins, what is bad to you, which offset is what the property says, we are saying that earliest start from the beginning, okay? That is what we are providing. Then what is this? This is what we were using, right? Yes, the console consumer was expecting this from us so that when we use this, it will be automatically using this property internally. It seemed like the console producer was using timeout to set the linger.ms, right? It's similar to that. The console consumer was using this to set this internally, okay? But this is the actual property. Now let's use this YAML in our section runner. Let's go to section runner. Let's replace this config with this, okay? Zero to from beginning. This is what I am using. So now let's run our application. We are still not giving the group name. So it's going to use anonymous group, okay? However, this time we are using the from beginning because of this reason. Now every time you restart, you will be getting all the messages we have produced in that topic.
