# Optional: Building Reactive Consumers

This lecture is optional. If you are comfortable with the reactive programming currently using Spring Webflex or Spring Data, R2DBC, etc, please watch. Otherwise, please skip this lecture. Spring Cloud Stream supports reactive programming. So, our consumer producer beans can use reactive publisher types like Flexofty, Monofty, you know that. It gives us the back pressure support. But there is one important information. The Kafka binder is not a real reactive binder like R2DBC. R2DBC, the postgres driver was created from scratch. But for Kafka, we do not have any such binder. But it's not a problem because under the hood, we use dedicated threads for Kafka. So, it will not affect our NetEase event loop thread. So, it is safe to use with Spring Webflex, Spring Data, R2DBC, etc. In the traditional consumer, we created a bean like consumer of string, right? Here, we will be using consumer of flex of string. Spring Cloud Stream will be giving you one flex and it will be streaming messages via flex whenever we get messages from the topic. It also gives us the back pressure support as we just discussed. Once you get the message, if you have to call a remote API using web client, using the flat map operator, all those things normally you do can be done as usual in the reactive pipeline. But there is one small problem. If you are going to use the Java consumer type to represent the Kafka consumer here, you will end up subscribing the flex yourself. This is a bad practice in reactive programming in general, you know that. Why? It's mainly because as you know, we do not normally throw exceptions in the reactive programming. Instead, we will be emitting error signals. So, if you subscribe yourself, the Spring Cloud Stream framework will not know about that, right? So, it is recommended to let the framework subscribe. That is, Spring Cloud Stream should be doing the subscribe. But how will it do? Because we are using the Java consumer type, right? Yeah, that is the problem. So, instead of using the Java consumer type, we will be using the Java function type. That is, here we will be getting the flex, we will be doing the processing as usual, and we will be using the then operator to return the mono of void. So, this is the reactive void, you know that, right? So, now this is the publisher type. Spring framework can subscribe to this publisher. Now, you might be wondering, but function is a processor, right? Please do not take that blindly. That is, we consume an event, and if we return another event, yes, it is a processor. But in our case, we are not returning anything. This is void actually, right? So, but it is a reactive type. So, it is still a consumer. Let us come back to our IDE. Let us use section 02 for the reactive consumer. Whatever we had done as part of section 01, right? I simply copied and pasted under section 02. I just updated this for the reactive types. Here we are getting the flex of string, and we are returning the mono of void as we just discussed. Nothing else. I changed the bean name to reactive consumer. Then under resources, we have the section 02 directory. Under that, I have one spring cloud stream, the configuration YAML. Actually, this YAML, it is not anything to do with the reactive types, okay? So, if you are using traditional programming or reactive programming, your YAML is going to be same. But I updated here for the bean name, okay? Since we are using reactive consumer, that is what we have to use here, and for the bindings name, reactive consumer in 0. We are still using the same demo topic and demo group, etc. Everything else remains same. Let us go to the section runner, section 02, 01, reactive consumer. Let us run this. Now, the demo topic partition has been assigned. So, let us open the terminal Kafka console producer bootstrap server demo topic. Okay, we have already done this. So, let me quickly produce a couple of messages to see how it goes. 1, 2, 3, 4, 5. Okay. So, whatever we produce here, we are able to consume.
