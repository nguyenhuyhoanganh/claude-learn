# Configuration Property Hierarchy

In this lecture, let's discuss how the binding configuration will look like in the application YAML Assuming this is very clear to you This is how it will look like spring cloud stream bindings Order event producer out zero. So here we have one order event producer. So whatever The supplier provides it will be going to this order events topic Order event producer out zero is the binding name destination is order events. That is what we are saying here Similarly, whatever message we have in the payment events topic will be going to this payment event consumer payment event consumer in zero Destination is payment events. Hope you get the idea. Now. Let's take a look at some examples one by one For the producer configuration Assuming we have one bean like this supplier of order event order event producer This is how we will be configuring spring cloud stream bindings order event producer out zero because it emits data The destination is order events. Okay, so this part is clear We also have to include spring cloud function definition. We have to include the bean name Like this why it's mainly because the spring cloud stream is one module It uses another module under the hood that module is spring cloud function so for that That module expects the bean names to be listed like this. It's semicolon separated when you have multiple beans I'll show you some examples. No worries. So we also have to include this in our yaml For the consumer configuration, let's imagine you have one microservice called Notification service which consumes payment event. So you will be exposing one bean like this consumer of payment event payment event consumer spring cloud function definition payment event consumer spring cloud stream bindings payment event consumer in zero the destination is payment events and The group name consumer group name we will be giving our microservice name Okay, it can be anything but normally we will be using our microservice name notification service like this. So this is a consumer group name Our application could be processor type application. That is let's imagine payment service Which consumes order events then it emits payment events So we might be exposing one bean like this called a payment processor a name can be anything here We are using payment processor. So it's a function consumes order events returns payment even Okay, perfect. Now, this is how the configuration will look like spring cloud function definition payment processor bean name spring cloud stream bindings Payment processor in zero the destination is order events Since here we are consuming messages, right? So we have to give the group name. So payment service Okay, so the same bean payment processor also emits data returns data, right? So the payment processor out zero here. We are using payment processor in zero This is consuming part here we emit data payment processor out zero that destination is payment events There is a very good chance that our application could be consuming multiple events For example, let's take inventory service, which is interested in consuming both order events and payment events So we will be exposing two beans like this order event consumer payment event consumer For that this is how the configuration will look like spring cloud function definition order event consumer Semicolon payment event consumer. So here we have two beans Spring cloud stream bindings order event consumer in zero. The destination is order events here We give the group name inventory service similarly payment event consumer in zero. The destination is payment events Group name is inventory service same group name, but remember this is for different topics Whatever the properties are the configuration we have seen so far These are like generic properties which will work for any messaging system The spring cloud stream supports like Kafka Pulsar rabbit MQ, etc But what if we want to override? Some Kafka specific property for example linger dot Ms We know that this is a Kafka property for producer configuration It might not be applicable for rabbit MQ. So where to configure that? This is how it will look like. These are like generic properties the binder specific properties It will be like spring cloud stream Kafka binder related properties. So these are your bootstrap servers So brokers like you can list your bootstrap servers like this Then producer properties linger dot Ms is a producer property, right? So here you are using 100 millisecond Similarly batch dot size 5000 bytes So if you want to override any of the producer related properties here, you can give Similarly the consumer related properties like maximum pole records so you can configure like this so these are all Kafka binder related properties as Part of this configuration what we are really seeing here is in my application If I have any producer that will be using this property similarly in my application if I have any consumer That will be using this property. Okay, perfect Now what if we have Multiple consumer like this order even to consumer payment even consumer. So here we are having two bindings for each Consumer right? Okay, perfect Now what if I want to have? Maximum pole records n for this under 24 this so that is also possible spring cloud stream supports that That is this is these are like generic properties, right after that here They are going to say Kafka. This is the Kafka bind related property However, I am going to have override in the bindings level Okay So Kafka bindings for the order event consumer in 0 for this binding I would like to change the consumer maximum pole records property to 10 Similarly similarly for this binding. I would like to change the consumer maximum pole records property to 20 So we can also do this You
