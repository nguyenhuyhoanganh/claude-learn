# Setting Up The Playground Project

In this lecture, let me quickly show you how I am setting up the playground project. First, I go to Spring Initializer Maven Project Java I select the latest stable version For the Artifact And the name I give Event Driven Playground But it can be anything you know Then here I go with the YAML configuration I choose the latest version of Java For the dependencies, we want Spring Cloud Stream This one Framework for developing event-driven microservices But you need Apache, Kafka, other binders, one of these binders So ok, let's select this first Then Kafka Apache, Kafka, select this as well This is enough for now But later we will be writing integration tests and all So for that we need Test Container So let's add that as well right away Test Container So that's it, we can generate this project Unzip this, then import this project into our favorite IDE Then I import this project like this Whenever you import a project It does not really matter if you are creating a new project yourself Or if you are opening the project I have shared The very first thing what I want you to do is that Go to pom.xml, scroll down Maven Sync Project without file So it will be downloading the dependencies and set up the project for you Then under Source Main Java We have this com.bins.guru.playground Feel free to change this name Then under that I am creating section 01, section 02 and so on As I was explaining earlier As I said, each and every section is independent Every section is a small independent application Ok, so probably if you had cloned my project You will be seeing several packages But this is what I have as of now This is the main Spring Boot application runner If it is here, if I run this By default, what it will do here is It will be scanning all these packages and it will be creating the bins But in our case, each and every section is independent For example, I might be having one order event producer bin here I might be having another order event producer bin here To demo a different thing So, if the Spring runner is here What it will do is, it will be complaining that Hey, you have duplicate bins So, I do not want this There is also one property So, we can use the scan packages If you remember So, we can also update this, go under scan only this package Go under scan only this package We can also configure this way But this could be a little bit annoying So, I want the ability to run quickly So, what I am doing here is I am going to simply just move this under section 01 Like this Then change the name to section runner like this By doing this way Now, if I run this This will be scanning only the section 01 package or anything under It will not scan other packages like this Right And each and every section will be having its own section runner So, we have the ability to start the application quickly Assuming this is clear Then let's go to source main resources Here I have one application yaml Then for each and every package here I will be having one corresponding directory here as well Section 01, section 02 and so on Ok So, whatever we do as part of this section 1 So, the corresponding spring cloud stream configuration yaml files Should be present under section 01 like this So, here as of now At this time of recording this lecture I don't have the content But by the time you watch the lecture You can see all those things Basically the idea here is Now, if I am going to develop one consumer application under section 01 What will happen if I use this configuration file Ok What will happen if I use this configuration file So, this is how I am going to demo We have so many things to learn And we cannot use this one single application yaml throughout the course Because we will end up keep on updating this yaml file again and again We will make mistakes and we will be losing the history I do not want all those things By keeping separate spring cloud stream configuration yaml files like this We can simply point our application to use one of these yamls By doing this way we can always go back and revisit anytime what we did Now, there is a challenge here That if you are going to have several spring cloud stream configuration yaml files like this How are we going to point our application to read one specific yaml file Ok So, for that Under application yaml I have this Spring config import And I am using two parameters Section Config This section refers to Section 01, section 02 and so on Which section we are using Then under that Which configuration file we are planning to use So, that is what these two parameters represent Ok So, the section runner This is how we are going to run We will be providing those two parameters Section 01 Config is 03 consumer group yaml Ok So, now when you run this Application yaml will be using those parameters Section and config And it will be reading this configuration and it will behave accordingly
