# Demo: Console Producer

In this lecture, let's try to produce messages to a topic. For that we are going to use another CLI tool called Kafka Console Producer. It's mainly used for learning, testing and debugging purposes. I have created some notes for you. It's shared under 02 Kafka Fundamentals, 02 Console Producer. So please refer to this. Run the terminal, ensure that your docker container is up and running, ensure that you are in the bin directory. So now let's create a topic. I have already shared that command. So let's create a topic called demo topic. Okay, so now it's created. Now I'm in the bin directory, right? So we are going to use the Kafka Console Producer. So this is the tool we are going to use. First let me do this console producer. If I hit enter, I just want to show you the options. So minus minus topic, this is the required option, the topic name to produce messages to. And the required option is bootstrap server, okay, only these two, nothing else. There are many other options if you want to use, but let's not worry about all those things for now. Okay. So let me clear this screen. I have already shared, given the command, right? So just to copy this and paste console producer minus minus bootstrap server localhost 9092 topic is demo topic. If I hit enter, you will be seeing this arrow or greater than sign, right? So now we can produce messages. Again this console producer is a simple tool for learning, testing, etc. So here you can type whatever you want. And if you press enter, then whatever you had typed, that will be sent as the message, okay, or event, simple plain text, nothing else. So let's follow the software development tradition. So first let's use hello world as the first message. I hit enter. Now the message is sent to the topic. So I can type whatever I want, let's say 1, 2, 3. So now I have produced four messages. In the next lecture, let's see how to consume these messages.
