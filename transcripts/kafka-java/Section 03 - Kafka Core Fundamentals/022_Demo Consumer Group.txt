# Demo: Consumer Group

In this lecture, let's have a demo. Let's see how the consumer groups work for that I have created one document you can find this under 0 to Kafka fundamentals 06 consumer group Okay, so this is what we are going to do as usual I would suggest us to start with the clean state. So go to 01 Kafka setup Let's do this Docker Compose down and Docker Compose up Then please ensure that you have this topic demo topic. So let's do this I'm not going to record this because we have already done this So I assume you know this if you face any issues, let me know. Okay Then we are going to open multiple terminals So first we are going to have two terminals Open two terminals in each terminal. We have to access the Kafka container Okay, so in the first terminal we are going to start one consumer group Console consumer same command whatever we had done earlier Okay, so the only thing is we are going to pass the group name option. So I am going to provide payment service here Then in another terminal we are going to start another console consumer Same as this one only thing is the group name is different. This is inventory service So to console consumers we are from different groups, okay, so this is the setup we are having here Okay, then another terminal third terminal So we are going to access the container as usual. We are going to start the console producer We are going to type something and we are going to observe the consumer. I Already did that Docker Compose down and up. I created the topic Okay, so now I have opened three terminals as you see I am I have accessed the Docker container Okay. So now here I am going to start the payment service consumer group Just copy the command hit enter now. It's waiting for messages let's start the the the other Consumer group inventory service. So now this is also listening, right? Okay. Now, let's start the console producer Copy the command Now I am ready to produce some messages. Let's send something one two three Four something like this now we can see that both consumers. They are getting the messages, right? now I Have opened another terminal for the terminal Okay, so we are going to start one console consumer another console consumer from the payment service group Okay, so let's access the container and whatever the command I have shared right for the payment service consumer group simply copy this whatever we have here and Paste it Okay. So this is my producer This is one console consumer from payment service, this is another console consumer from the same payment service This one consumer from the inventory service. Okay. So now you see the setup, right? Okay So now so far I have produced four messages. Okay, let me produce like more messages now five Okay. So now if you see the payment service, right, it's not getting five. So now this Console consumer from the payment service group gets the five inventory service as usual. It's the message Okay, because we have only one consumer from this group. So six seven eight nine If I do this, so now only this consumer gets all the messages and this consumer does not get anything As usual here we are getting all the messages. So let me enter some messages fast like this So now if you see This consumer gets all the messages this consumer gets all the messages But this consumer completely stopped getting the messages, right? The good news here is when we have multiple consumers in a consumer group Only one consumer gets the message. So we do not do redundant processing. So that is a good news actually, right? Then when we have multiple consumers from different groups, so in this case we have different groups so Both of them get the messages, right? So that is also good actually But the only annoying behavior here is that when we have multiple consumers in a group in like this We could have probably expected The messages should have been distributed between these two consumers one message here another message here and so on However, it does not seem to be behaving like this. Only one consumer gets all the messages, right? So actually there is a very good reason for Kafka to do like this So we can also make it work the way in which we want but the default behavior of doing this Right this way, right? There is a reason behind it. So for that we have to talk about a concept called partition So we will be talking about that in the next few lectures
