# Demo: Multiple Consumers

in this lecture let's have one producer and let's have two consumers so let's have three terminals open one for producer and we are going to have two consumers so now as usual the producer is going to produce some messages and we are going to observe what is happening here and we are going to have some discussion whether the behavior is correct or not let's start the consumers here so for that the command we can simply just copy this we do not need everything just this part is what we want so let's simply copy this and paste it here we do not need the from beginning and all okay so now here the consumer is ready listening for new messages similarly here as well the consumer is ready okay so now let's start the producer and let's produce some messages observe the consumer if I type one okay I'm seeing one here okay in both consumers I am getting both consumers are receiving one two three four five six here if you see I'm getting all the messages okay so both consumers are getting all the messages now this behavior is correct or not depending on our expectation it could be either correct or it's not correct for example let's imagine that this is order microservice order service so order service keep on producing events like order events now if you consider this as a payment service if you consider this as inventory service yes both micro services might be interested in the order events so both of them should be getting all the events so in that way yes this behavior is correct but what if you run multiple instances of payment service for example in the real life to handle the load you will be running multiple instances of payment service right so this could be payment service one this could be payment service two we have two instances if both instances are getting the same message then we will be doing redundant processing so it is not correct in that case right
