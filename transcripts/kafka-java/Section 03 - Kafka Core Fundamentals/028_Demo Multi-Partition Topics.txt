# Demo: Multi-Partition Topics

This will be a quick demo lecture for this lecture, please go to workspace 0 to Kafka fundamentals 07 topic with the two partitions. First we are going to start Kafka with a clean state. So please do the docker compose down, docker compose up again just to have a clean demo. Okay. Assuming you have done this, now let's access the Kafka container as usual. Then we are going to create a topic with the two partitions, same command. Now this time we are passing the partitions option. Then we are going to do the describe to see the details about the topic we have created. I am opening the terminal. I have already done the docker compose down and up. Okay. So now I'm going to create the topic just to copy the command and paste it. So now the topic is created. Let's do the describe, copy the command, paste it here. And as you see, now we are seeing partition 0 partition 1. So we have two partitions for order events topic. As we just saw now that we can create a topic with the multiple partitions. And now we understand that topic is made up of partitions. Now I would like to quickly rephrase what I had said earlier, because when we started this section, that time we did not know or we were not aware of these partition concepts. Now we are aware of the partitions concepts, right? So I would like to clarify a few more things. If you remember, we had said that in the cluster, when we create the topic, one node could be the leader. The other node could be the follower and all, right? So this is actually correct for a topic with the one partition, when you create a topic with only one partition. So then there will be one node that could be the leader for the topic or leader for the partition. There could be another node follower for the partition. Okay. Now when you create, since we can create topic with the multiple partitions like this, it is not like all the partitions will be part of a single node. The controller might distribute these partitions across different broker nodes. Okay. So even if you create one topic with the two partitions, as we had already said, topic is just a logical construct. So the order events partition zero can be part of this node. Order events partition one can be part of this node. Since we have two different partitions, this is the leader for partition zero and this is the leader for partition one. Okay. Hopefully I'm not confusing you. Now this could be the follower for partition zero and this could be the follower for partition one and so on. Okay. So hopefully now you understand this better. Okay. Topic is just an abstraction. Okay. That is what I want you to remember. Topic is nothing. It is a logical construct. Partition is the physical construct. So the partitions can be distributed across different nodes. Okay. So because of this reason, one node will be the leader for one partition. Another node could be the leader for another partition. Again I am saying could be. Okay. So in a cluster. Okay. Let's come back to the terminal. So here we are doing the describe for order events, right? So in our case, we are not running our Kafka server in the cluster mode. We are using docker container. So we have standalone Kafka server, one single node. So here, what is this leader one? Okay. So what the one means here. So to understand this better, I have opened another terminal. Either you can also do as I do, or you can simply just watch me doing this. If you go to OPT Kafka config directory. So here I'm going to print one property called node ID from the server dot properties. If you can read this file, if you see the node ID is one, basically when we run the Kafka server, right? In the cluster mode and all, each and every single node will be given one unique ID. Okay. So this docker container by default, it uses one, but if you are going to run multiple Kafka servers, multiple instances, each and every instance will be having one unique node ID. So what it's prints here is that who is having order events partition zero. So node ID one, that is the leader for order events partition zero. Similarly, it says that the node ID one, this is the leader for order events partition one. That is, we created this order events topic with the two partitions. Since we are running standalone Kafka, both partitions are part of the same node. Okay. So that is what it prints. But later when we run the cluster mode, right, we can see this better, but hopefully you get the idea now.
