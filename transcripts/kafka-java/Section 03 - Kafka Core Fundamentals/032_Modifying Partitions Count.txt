# Modifying Partitions Count

Now, let us consider this scenario, we have a three partitions topic, and we have already produced some messages to keep all of us on the same page. And for better understanding, let us imagine that this is how we find the partition, some pseudocode key mod number of partitions. So the 1, 2, 3, this is the key, let us imagine like that, again, for better understanding. So message with the key 1 goes to this partition, message with the key 2 go to this partition, and the message with the key 3 goes to this partition, and so on. So the key 4 lands here, perfect. This is how we have our setup in production, and everything is working great. Let's say over time, your application started getting more traffic. So you want to run one more consumer in the group. So for that, you need one more partition, three partitions are not enough now, it will happen, right? There is nothing wrong with that. Okay, there is an option to increase the number of partitions, Kafka topic, bootstrap server, order events, alter option, this is what we have to use, then you have to provide the number of partitions. For that, using this option, we can only increase the partitions, we cannot decrease, we can only increase the partitions. So there is an option if you want to do that. Now you got one more partition and you have one consumer in your consumer group, that's it, this is the perfect setup, this is what you wanted, and everything will look very simple and easy. But there is a problem. Since the number of partitions got changed, since we have more partitions now, the partition calculation will return different values for the same keys we had used earlier. For example, if the application is going to produce a new message with a key 4, this function will not be returning, this function will be returning a different partition. So the 4 will not come here, instead, the 4 will go to this partition now. As you see, this message was produced after this message. Because of the setup now, there is a very good chance that this message will be delivered to this consumer first, and it will get processed here before this message gets processed by this consumer. So we are losing the message ordering guarantee temporarily for some messages. This could be a problem we have to be aware of. How to solve this problem? We have to design upfront properly. That is one option. Okay, what do I mean by that? If you ever think that we are running 3 consumers now, but in the next 3 to 5 years, we will be running 6 consumers in the consumer group to handle the load, etc. Just create a topic with 6 partitions or create a topic with 10 partitions. It is okay to have more partitions, it is not a problem. But when you have less partitions, compared to the number of consumers in the consumer group, that is when the consumers are going to be sitting idle. But it does not mean that we should be like, okay, you know what, I am going to create a topic with 10,000 partitions. So lifelong, I do not have to worry about this. Please do not go to that mode. There are some challenges in that approach as well. We have one dedicated best practices section to talk about this. But this is one option. This is what I wanted to share. But for now, in the next 3 to 5 years, if you have an idea about the growth, use that information for the partitions. Or we can simply accept the message ordering issue for a short period of time. Not every single application is a mission critical application or a banking application. For example, this topic, right, this could be a product view events topic. The customer ID 4 viewed iPhone first, the customer ID 4 viewed iPad next. So that is what this message is represent. In that case, what is going to happen if we process this message first, it is not going to have much impact. In that case, yeah, we can simply just accept this and do this way. Okay, so it is not a problem. Another option is stop the producer, run the alter command to increase the number of partitions. So now you have 4 partitions, but the producer is stopped. If you have some existing messages in these partitions, let the consumers run, drain all the messages. Once everything is empty here, then start the producer. By doing this way, we will not be having any issues, but we will be having temporary downtime. Okay. This is the last option. First of all, the topic, right, it is simply just a configuration that is our application is not married to the topic. So the idea here is you have an order events topic with the 3 partitions. Now, do not try to change the partition count and all, instead, create a different topic, order events v1 or order events v2, just to create a topic with the 4 partitions now. Okay, you have a different topic. Okay. So now, what we are going to do here is, we will be updating our producer application. So I mean, not the code, we will be updating the application.properties because the topic name and all is going to be a configuration. So we will be just updating the application.properties, we will be restarting the application. That's it. So now the producer application will be producing the messages to the new topic. Okay. While the consumers will be consuming messages from the old topic. Once the consumers have drained all the messages, now we will be updating consumer application.properties to consume messages from the new topic.
