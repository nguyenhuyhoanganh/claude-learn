# Demo: Kafka Topics

In this lecture, let's play with the Kafka topics. Let's create delete topics, etc For that, I would have already shared some notes So please go to workspace 0 to Kafka fundamentals under that I would have shared this file 01 topics so you can use this for this demo Open the terminal first to ensure that your Kafka container is up and running Then let's access the container. We have already used this command Hit enter ensure that you are in this bin Directory as you know if you can list the files, so this is where we have the Kafka topics So this is what we are going to use So let me clear the screen dot slash Kafka Topics if you hit enter, so now here if you scroll up Here we can see some options like create delete describe change a topic Okay. So if you see here we these are the list of options. There are many options actually So minus minus bootstrap server. This is one required Option we have to give so the Kafka server to connect to you can say that hey But the Kafka server is running inside the container, right? You can ask like this Yes, but this this are the CLI tools, right? They do not do this kind of assumptions If you are ignoring this then it will learn it will not assume that it's localhost. So it does not work like spring boot Okay, so we will have to give the bootstrap server details even if it's localhost we have to explicitly mention that Okay, so this is one required option. We have to give then scroll down So minus minus create to create a topic minus minus delete to delete a topic and so on So this is how we create delete etc Minus minus list to list all the topics Okay, and what is the how do I give the topic name so this is how you give the topic name minus minus Topic so you give some name. Okay. So these are the options we are going to use. Okay, so first let's create the topic dot slash Kafka topics minus minus bootstrap server So For us it's a localhost colon 9092 Then we are going to create a topic So minus minus create then we have to give some topic name. So minus minus topic then give your name. So We can call this order events. Okay some name So if I hit enter now it created the topic order events. Let's create few more topics So let's call this payment events then Shipping events feel free to give any name you like doesn't really matter Then we might be interested in seeing all the topics we have in the cluster So for that, we will be using the same Kafka topics CLI, but this time we will be using Kafka topics bootstrap server list all the topics we have Okay, so we will be using minus minus list if I hit enter it will be listing all the topics we have Okay, so we might be interested in okay. I would like to know more about this order events topic So in that case we can use minus minus Describe option to get more details about the topic order events If you use this command with this option If you hit enter then it will give you more details about the order events Now, I'm sure you will be having lots of questions. Like what is this partition count? What is this replication factor? Why it says 0 what is what is 1 here? What is 1 here? So you will be having this kind of questions No worries, I'm going to explain all these things later I can open a slide now and talk about this for next 30-40 minutes But definitely I'm sure that you will not like it. It will be very boring Okay, so I will explain all these things when the time comes So this is how you get the details about the topic if you are interested So I just wanted to show this minus minus describe option nothing else To delete the topic you will be using the delete option. So now instead of describe if I say Delete now I am deleting this order Okay, so if I list now It's gone we have only payment events and shipping events You
