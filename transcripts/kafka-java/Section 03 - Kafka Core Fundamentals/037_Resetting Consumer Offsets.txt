# Resetting Consumer Offsets

In the previous lecture we had discussed that how Kafka tracks the offset for each consumer group for each partition Now let's imagine that this is how Kafka has delivered the messages so this is how it has tracked That is the current offset is 96 and the log end offset is also 96 So the lag is zero basically from Kafka perspective for this topic all the messages have been delivered So Kafka will not deliver the messages to you for these consumers it will not re-deliver those messages In some cases you might want those messages to be re-delivered May be you had some bug in your application now you have fixed it so you want Kafka to re-deliver those messages In those cases there is an option to reset this ledger so that Kafka can re-deliver those messages So these are the options we have actually There is a command I am going to share that we are going to have a demo no worries But these are the options we have so let's discuss the options first So if you say shift by 3 what it means is that move forward Let's say the current position is 5 let's imagine like this current offset is 5 If you say shift by 3 so it will update the offset to 8 So move forward that is what you are saying If you say minus 2 then move backward So let's say you are here right if you say minus 2 then it will just put you here actually So that these messages will be delivered to you So this is what you are saying actually Also these options you cannot do for one single partition so it will be applied for all the partitions Remember that You can also say that by duration Just re-deliver last 5 minutes messages You can say like this or to date time Can you re-deliver all those messages added after this date and time So you can mention like this Or you know what just go to 0 Just give me all the messages from the beginning You can say like this Or we can say that just reset the lag to 0 I want to see only the new messages You can also specify like this As usual for the demo I have documented the steps You can find this under 02 Kafka Fundamentals Offset Reset But in order to do this you should have done this offset tracking demo Because we need the topic with the two partitions And we need the consumer group Kafka should have tracked so that you can reset Right So we have some prerequisite So ensure that you are doing this first Only then we can do this Ok So these are the options we have discussed I will come to this So this is the command Kafka Consumer Groups Bootstrap Server This is the topic CG the group name they are giving Then Reset Offset So this is what we have to say Reset Offset and this is the option Ok So these are the options we have This is what we are saying Actually shift by minus 3 Ok So we have two different options Like Dry Run Execute If you say Dry Run it does not change the offset But it will show the new offset it would apply Execute will actually reset the offset Ok Hopefully this is clear Let's come back to the terminal Ensure that your consumers are stopped Because if the consumers are running Kafka cannot reset the offset So ensure that they are not running Now let's Ok let me clear the screen Let me describe Whatever we had done So for the offset tracking topic So currently this is the current offset For partition 1 it is 9 For partition 0 it is 6 Now let's imagine that we want to shift by minus 3 Ok So let's do the Dry Run first Copy the Dry Run command If I copy and paste it here So here I am doing Reset Offset Shift by minus 3 Dry Run So what it will do is that It will Ok Let me show the output Easy for us to understand So the current offset right So this will be the new offset actually 6 and 3 That is what it would apply So that's what it says Dry Run If you say Execute It would actually apply Ok So now it's done If you describe So the current offset It became 6 and 3 So now the lag is 3, 3 If I start the consumer Now this consumer will be getting Those messages The last 6 messages we had produced So that is what we have received here Again if you go back And check Describe right So the lag is 0 now
