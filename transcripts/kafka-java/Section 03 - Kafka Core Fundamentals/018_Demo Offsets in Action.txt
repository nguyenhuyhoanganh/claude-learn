# Demo: Offsets in Action

Let's go to our project workspace, 02 Kafka Fundamentals, 04 Print Offset. I have already shared some instructions and we can use this for our demo. So for us to have the clean demo, let's please go to this Kafka 1 setup directory. So this is where we have the docker-compose yaml, right? So let's do the docker-compose down, then let's do the docker-compose up. Because we have already created some topics, we have produced some messages and all. So I want to have a clean demo. So let's do this so that we can get rid of the existing state in the Kafka container. Then let's, by doing this, we are creating the new Kafka container. We are not doing any volume mapping, so we are starting from scratch. Then as usual, we are going to access the container. Then we will be creating a topic called a demo topic, we have already seen these commands, so nothing special. Then open two terminals, okay, so side by side, one for producer and one for consumer. So this is for console consumer, we are going to consume this demo topic messages. And here, if you notice, I am adding this property, print.offset equals true. So this property enables the offset printing along with each message, okay, so we can see. Then another terminal, again, we are going to access the container, we are going to start the console producer to produce some messages so that we can see the offset at the consumer side. So that's it, very simple, right? So let's do this. Go to the terminal, if the Kafka server is up and running, press control C, let's do docker compose down, okay. Then let's do docker compose up, ensure that the Kafka server has started. Then let's have two terminals side by side like this, access the container, okay. So one for producer and one for the consumer. So in one of the shell, let's create the topic, demo topic, let's create that, okay. So now it's created, perfect. So then let's start the console consumer. So here I am going to start the console consumer just to copy that command and paste it, hit enter, okay. So now it's waiting for new messages. Then let's start the console producer, copy that command here. Let's paste this, hit enter. So now I'm ready to produce messages. Let's say, hi, now if you see offset zero, hi, something like this, hello, okay. Now you can see the offset right, zero, one, two, and so on. Let's try to produce more messages, if I keep on entering like this, now you can see the offset.
