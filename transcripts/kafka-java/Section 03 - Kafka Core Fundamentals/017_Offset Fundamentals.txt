# Offset Fundamentals

In this lecture, let me quickly demo a concept called offset in Kafka topics. There is also another concept called partition related to Kafka topics. But I am going to cover that in another few lectures. Let's go one by one. There are so many things to discuss and demo. So let's take this slowly. Let's imagine this is our Kafka server and here we have one topic. This is our producer and here we have one consumer. We have to see this Kafka topic as append only immutable structure. That is whenever the messages are sent to a topic, Kafka will be storing the messages in the order it receives. It is super important for Kafka so that it can deliver the messages only in the order it received. For that, just imagine this topic, uh, like an array like data structure. Okay. So it assigns one unique number. We call that offset like array index. It starts from zero. So whenever Kafka receives the message and it will keep on appending the message and it will be increasing the offset. So the first message offset zero. Okay. Next message offset one. Next message offset two and so on. Then the consumers ask for messages. So using the offsets, the messages are delivered in the same order in which they were added to this topic. Okay. So let's have a quick demo to see this offset. Just a quick note. If the offset is going to keep on increasing, what is the maximum value? So the offset can go up to long dot maximum value. Okay. So that's a lot. It's huge. So even if your application is going to produce 1 million messages per second, okay, that's a lot. 1 million messages per second. The application has to run for like 292,000 years to reach this long dot maximum value. So it's almost impossible to reach.
