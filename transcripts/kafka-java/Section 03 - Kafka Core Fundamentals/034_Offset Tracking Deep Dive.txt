# Offset Tracking Deep Dive

In this lecture, let's talk about offset tracking. This is something very important We already know what offset is. We had a discussed this a few lectures ago Kafka assigns Unique number to each message within a partition This is offset It does not matter if we have one partition or hundred partitions each partition will have this offset It starts from zero. We know that Kafka internally maintains some kind of a ledger. This ledger is not an official Kafka term I am just saying this to give you an idea visualize Okay, whenever a consumer group Subscribes to a topic for the very first time Kafka starts tracking using its ledger in this ledger Kafka tracks In this partition. I have offset up to 10 For this consumer. I have given messages up to 3 and so on Why it's mainly because in the micro services architecture anything could happen This machine or consumer could crash due to out of memory error Restart and join the group once again Okay, so Kafka should not be delivering all the messages from zero once again So it it wants to remember it remembers that Yes, I have delivered the messages up to three. I know that Okay, take four five six now, so this is how it will be delivering the messages This is called offset tracking Kafka does that for each consumer group and each partition You
