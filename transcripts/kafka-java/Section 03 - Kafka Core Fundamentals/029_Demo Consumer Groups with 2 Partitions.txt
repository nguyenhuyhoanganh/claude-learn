# Demo: Consumer Groups with 2 Partitions

In this lecture, we are going to have a demo. This is our goal. We are going to create one consumer group with the two consumers. Then we are going to have one topic with the two partitions. And we are going to see if Kafka assigns each partition to one consumer in that consumer group. For that, please go to workspace 02 Kafka fundamentals 08 consumer group with the two partitions. So I have already documented, this is the instruction we are going to follow for this demo. First, please ensure that you have completed this demo 07. So here we are creating a topic with the two partitions. So ensure that you are, you are, you have done this. It's better to start from the clean state. So please follow all these things. Ensure that you have created this topic, order events topic with the two partitions. Ensure that assuming you have that set up, then they can start from this. As usual, we will be opening multiple terminals in terminal one, access the Docker container. Here we are going to start one console consumer, bootstrap server, order events topic. We are going to print offset, and we are also going to print the key. We will be producing messages with the key. We also want to see the key as part of the consumer. So we are also printing that. And this console consumer belongs to the payment service group. Perfect. Then in another terminal, terminal two, we are going to do the same, access the container, exact same command. Similarly, we are, we are, we are having two console consumers belong to the same group. Okay. Perfect. Then third terminal, we are going to start one console producer, access the Docker container as usual. As I said, we will be producing messages with the key. So we will be using this format, key colon value. The colon is the key separator. So console producer, bootstrap server, order events topic. Now this time we are going to provide a couple of properties. Basically we are saying that I'm going to use colon as the key separator. So parse that. Basically this is how I am going to produce the message. Use this as the key. And after colon, that is the value. That is what we are telling this to our console producer. I have opened the three terminals. I have accessed the Docker containers. I already have the topic with the two partitions. Now copy the console consumer command. So here I am going to start the console consumer one, payment service group, hit enter. Here console consumer two, okay. Same payment service group. Perfect. Here we are going to produce messages. So copy the console producer command, paste it here and remember that we have to produce messages with the key now. For the keys, I'm going to use numbers one, two, three, et cetera. So one is the key, colon is the key separator for the message A, that is the message. If I hit enter, now this consumer gets this message and this consumer does not get the message. So which is good actually. So only one consumer gets that message. Perfect. Okay. Two is the next message. Sorry. Yeah. Two is the next message key, colon B. Now if I hit enter, now this consumer gets this message. This is cool. Right? Okay. So three C. Now this consumer gets this message. Perfect. Okay. So now I'm going to use the key one. If I send a different message, okay, so this consumer gets the message. So see, this is the key. This is offset. Okay. So this is the message. So whenever I send different, different messages using the same key, okay, so the same consumer gets all the messages. Now, if I send any message using this key two, okay, this consumer will be getting those messages. Okay. Perfect. Right. As you see, this is how we will be solving both the scaling issue and the message ordering issue. First, we have to have a Kafka topic with more than one partition. Then you will be running multiple consumers in a consumer group. In our case, we are having two partitions and two consumers in our consumer group. We are producing messages with the key. By using a key, the Kafka client library will be distributing the messages between two consumers in the consumer group. In our case, this is what is happening, right? So the load is distributed. Okay. The message ordering is also guaranteed by using the key. For example, this message was, sorry, this message was produced after this. So only in the order it was delivered, right? Same. This message was produced after this. Okay. So this is how the messages are getting delivered. Okay. So by using the key, the order, the ordering is guaranteed.
