# Cluster Mechanics: Leader & Follower

Let's continue our discussion. I hope now you have a basic idea about the Kafka cluster Let's imagine we want to create a topic called order events The controller it is the boss, right? So the controller will find one broker Let's say this broker and it will ask this broker that hey broker you own the order events topic You handle the read and write request So it will ask like this and this broker will happily accept that So whenever the producer application produces the order events So this broker will write that in the machine I mean in the disk when the consumer application asks for the messages the broker will Deliver those messages Okay, perfect Then again, we have the same old question. So what if this mission dies? Now, this is a problem right because we will be losing the data Whatever the producer has produced so far because it's part of this mission disk, right? So we have lost the data and it's not good This is why then the controller finds the broker It will also identify few other nodes to be the backup I'm trying to explain the concepts in a super high level to give you an idea to have a mental model But we will have a better understanding later when we have the actual hands-on Okay, so this controller will identify this node and it will instruct that hey broker You are the primary for the order events, but I want to have some backups So when you get the messages for when you get the order events, can you also Give those events to these two nodes. They are your backup So the cons and the controller will instruct this broker like this So this broker Will give whenever it gets the order events, right? It will write in its disk and it will also give those events to these two nodes Like this, so the data will be replicated to other instances as well This way so when this broker dies It's not a problem since the data has been replicated So one of these node will become the primary for the order events So the producer can still produce the messages as usual and the consumer can still consume the messages as usual without any interruption I used the term primary backup, etc to explain the concepts But the correct Kafka terminology is leader followers So the leader is the primary broker which handles the read and write Followers get the data from the leader It's backup when the leader dies one of the followers will become the leader for the topic again to Keep things simple Also, we can have multiple topics right, so For an order events, this node can be the leader these two nodes could be the followers For the payment events, this node could be the leader and these two nodes could be its followers and so on Basically the data is distributed among all the brokers in the cluster and we will be having some kind of Backup in the cluster. So we should be able to safely produce and consume messages without much interruption Again, I will be demoing this later and you will be seeing yourself Let's quickly summarize whatever we had discussed in the last two lectures Single Kafka node is okay for development purposes But it's not good for production environment where we need high availability and horizontal scalability So we will be running multiple Kafka servers in the cluster mode We developers we simply call them Kafka servers or Kafka node, etc But they can play specific roles in the cluster they can be just brokers to handle read and write request or they can be just Controllers to manage the cluster operation or in a small cluster in the standalone Kafka instances like docker containers, it will have both broker and controller roles At any given time we will be having only one controller if the controller goes down for some reason There will be another controller eligible node. It will be ready to take over immediately Similarly when the leader broker dies for some reason there might be there might be another follower Broker, it will be ready to take over immediately So there will not be any single point of failure and our application will not be interrupted
