# Demo: Tracking Consumer Offsets

To understand this better, we are going to have a demo for that, please go to workspace 02 Kafka Fundamentals, then Offset Tracking. Here as usual, we are going to create a topic with the two partitions, the topic name is this offset tracking topic, something like that. Then we are going to start a console producer. And we are going to produce five messages with a key, these are the messages I am going to produce actually. So we do not want consumer at this point, we want to produce messages first. Let's imagine there is an existing topic, the producer has already produced some messages, that is how I would like to simulate. So this is what I am trying to do now. Then after producing the messages, we are going to start two consumers from a consumer group. Okay, same command, we will be using two terminals. They will be subscribing to the topic for the very first time and they are not using from beginning, intentionally we are not using, okay. So at this point, since the producer has already produced the images, sorry, messages, as you know, when you run the console consumer, they will not be seeing any messages, you know this right, okay. Then there is another command describing the consumer group. So CG is the consumer group name. So that is what we are describing here, okay. So when you run this command, it will be printing some output, this could be a little bit confusing. So first let's do this, then let me come back and explain this so that we will be on the same page. Open the terminal. First, let's create the topic, copy that command and paste it. Now the topic is created with the two partitions. Perfect. Then copy the console producer command and paste it. Now we can produce those five messages, 1A, 2A, 3A, 4A, 5A, okay. So we have produced five messages now. At this point, we have a topic with the two partitions, we also have some messages. However, there is nothing to track from Kafka side because we do not have any consumer group so far, right. So there is nothing to track. Now let's start our console consumer, copy the console consumer command, paste it here. So now we have one consumer group. At this point, Kafka would have started tracking. Since we are not using from beginning, right, so what it means is that these consumers, they are telling Kafka that, hey Kafka, we would like to subscribe to this offset tracking topic, and we would like to see the messages, only the new messages from now. That is what they are saying. Now let's copy the consumer group describe command. Let's open another terminal. This is where we are going to run. Here the font size could be a little bit smaller because the output will be in a tabular format. It's very difficult to see. So that is why I have adjusted the size. Probably if you do that in your machine, you can see that better, okay. So let me run this command here, we are describing the consumer group CG, that is the name we have given. Okay, so the output could be a little bit confusing, please be patient, let me hit enter. Okay. So here, what it says is that, let me explain this way so that it's easy, okay. So we have two partitions, partition zero, partition one, okay. So partition zero, partition one, and if you remember, we produced five messages in total. So partition zero, log end offset, what this says is that in the partition zero, I have written two messages, next offset is two. So I have written the index offset zero, offset one, next offset is two. That is what this two says. For the partition one, I have written three messages in the partition, next offset is three. So that is what it says, okay. Perfect, okay. So the current offset is, it basically represents where the consumers are now. So what Kafka tracks is that I have to give this message, whenever I get this message here, I have to give it to this consumer. Similarly, whenever the offset two message, whenever I write, I have to give it to this consumer. So that is what it says, why? It's mainly because these consumers, when they join, they did not use from beginning, right? So what they are saying is that, hey Kafka, just give me the new messages going forward. So that is how they are joining. So Kafka assumes that they have already seen all these messages, even though these messages were not delivered, since they were not interested, Kafka assumes that, okay, so I have delivered all these messages. That is how it internally tracks. So this is the position for this consumer and this is the position for this consumer. This is how it tracks and the lag is zero. What it means is that basically there is nothing to deliver. So that this consumer has seen all the messages, okay. So that is what it means, lag is zero means basically the consumer has seen all the messages. Basically you get the idea, right? Now let's go back, the consumers are running, let's produce five messages now, okay. Let's produce same messages, 1A, 2A, 3A, 4A, 5A, okay. So here we have delivered three messages and here we have delivered two messages, perfect. Now let's come back here and describe one more time. As you see, since we have produced more messages now, the log end offset, three became six and two became four. The current offset is also six and four. Basically all the messages have been delivered to this consumer. So the lag is zero, perfect so far, right? Now let's go back and stop the consumer, both of them crashed. Let's imagine like this, meanwhile, the producer is producing more messages. Let's use the same key, but this time let's use B so that we can see that it's a new message. So 1B, 2B, 3B, 4B, 5B. So we have produced five messages, but these consumers are not there, right? So let's go back to this terminal and describe one more time. If you check the output, it says that the consumer group CG has no active members. For the partition zero, the log end offset became six now. It was four, now it became six because in this partition, we have added two more messages. Similarly, for the partition one, we have added three more messages. So six became nine, however, if you check the current offset remains six and four because we do not have those consumers. So lag is three and two, basically we have to deliver the last three messages from this partition to this consumer and two messages from this partition to this consumer, okay? So we have this lag. So this is how the Kafka tracks. Now if we go back and start this consumer, probably as soon as I start, there is a very good chance that Kafka might deliver all the five messages to this consumer, okay? So if I run this, okay, let's wait and see, okay. Now if you see all the five messages have been delivered to this consumer, 1B, 2B, 3B, all those things we can see now. Kafka will not have to wait for all the consumers to join. As soon as the consumer is, one consumer is available from that consumer group, it will be assigning all the partitions to that consumer. So all the messages were delivered to this consumer, okay? Now if this consumer, if this consumer joins, again, Kafka will do the partition rebalancing. You know that we have already discussed. Now if you go back and describe, right, the current offset is 9696, the lag is zero. So we do not have anything to deliver. So the consumer is up to date. It has consumed or it has seen all the messages.
