# Bootstrap Server

In this lecture, I'm going to quickly explain an important thing called bootstrap server. If I run a Kafka cluster like this with the hundreds or thousands of nodes, how can I talk to a specific broker? Because this broker does not have data. This broker does not have data. Only this broker has the order events topic, right? So my application, if it's going to produce order events, how can it directly or correctly go and talk to this specific broker? How do I know, right? So let's discuss that. If you remember, I had told you that these brokers are like a family. They all know each other very well. So it does not really matter how many instances you have in your cluster. You do not have to know which node has order events topic. What we really need is one single machine IP address. That's it. You do not have to remember 1000 IP addresses. You just need to know one machine detail. That's it. So if our application, let's say that, you know, this machine IP address, okay? You are hostname, something, you know, this machine IP address. As long as if your application can connect to this broker, then that's it. The very next second, your application will come to know about the entire cluster. This broker will give all the details to this machine that this broker has order events. This broker has payment events, etc. So it will provide the entire cluster metadata to this application. Actually we do not have to worry about writing the code to get that cluster information and managing them and all in our application. It's already handled as part of the Kafka official client library, which we will be adding in our application. So even if you have thousands of servers in your cluster, as long as you know the one single server connectivity details, your application can work just fine without any issues. We call that bootstrap server. Any broker can act as a bootstrap server to provide the initial metadata and it is not a special role like a broker, controller, etc. We can ask the same old question again. That is, my application is trying to connect to the cluster using this IP address. But if this node is down, what will happen? Okay. So if our application is trying to connect that time, if this node is down, then our application cannot connect because that is the only IP address we know, but that node is down. So our application cannot connect. It is a problem. In those cases, you can identify a few more machines, like set of machines, like these machines or these machines, again, anything can act like a bootstrap server. So you can identify a few more machines and you can provide them as a list in your application.properties. So as long as your application can talk to one of these servers, then it will work just fine. Thank you.
