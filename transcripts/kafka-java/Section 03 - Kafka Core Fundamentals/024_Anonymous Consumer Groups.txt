# Anonymous Consumer Groups

This is going to be another quick lecture. You do not have to do this. You can simply just watch me doing this. I simply thought of sharing this information. Nothing else. In the previous lecture, we had created a couple of consumer groups and we were able to list. Right. Payment service. Inventory service. Okay. This is good. Now I have opened two terminals. I am going to start a console consumer. So let me copy this command and paste it here. This is nothing new. You have already seen this console consumer. Goodstop server demo topic. Right. So now. Okay. Let me do that. Okay. I have not yet started the console consumer here. Let me list the consumer groups. Once I have payment service. Inventory service. Okay. So now I am going to hit enter. Now the console consumer started. If I list. Right. You will be seeing one new group. Console consumer 91555. Something like this. Okay. So let me start another console consumer here. I am going to hit enter. Okay. So now let me list this. If you see. We have one more consumer group. For Kafka. Every single consumer should be part of a consumer group. If the group name is not specified. Then Kafka will assign a random group. We call them anonymous consumer groups. So if you start multiple console consumer. We might think that we might accumulate a lot of consumer groups. Right. But we do not have to worry about that. Kafka will usually remove the inactive groups after seven days. It's based on this configuration. We can adjust this if you want. So this is why when we start two console consumers. Without specifying the group name. Then Kafka will assign one random group names for this. Okay. You belong to this group and you belong to this group. So it will assign some random consumer group names. Now Kafka will see them. Consumers from two different groups. So this is why whenever we produce messages. Kafka sends all the messages to both consumers.
