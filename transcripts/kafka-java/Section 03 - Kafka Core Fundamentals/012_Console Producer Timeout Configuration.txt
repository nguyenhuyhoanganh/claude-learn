# Console Producer Timeout Configuration

this will be a quick lecture let's have another demo and i want to show you something for that i have opened terminal side by side like this ensure that you have accessed the kafka container and you are in the bin directory so here i am having the console producer kafka console producer bootstrap server demo topic this is what i have we have already seen this right okay so here i have the kafka console consumer bootstrap server demo topic okay so now let's here the producer side i am going to hit enter so now i am ready to produce messages here here i am not using from beginning okay so we are going to consume only latest messages let me hit enter here as well i want you to observe this okay then i'll explain what is going on now i'm going to send messages very fast here and see how it behaves so let me type something if i type something right if you see as and when i type it's not getting delivered immediately however the message seems to be delivered in batches in bulk that is how you see right again you can observe this one more time you see this is how the message are getting delivered eventually you get all the messages for example if here if i hit enter the v that will also be delivered eventually you get all the messages however it is not like if i type one see i did not get one immediately as soon as i entered two it's i seem to be getting one and two right in batches so this is how it seems to be working right it is not like the consumer is slow or the kafka server is slow it is not like that instead it's a more of a the console producer behavior it's overriding one property because of this reason it behaves like this so to fix this okay so let me press ctrl c okay so let me create the console producer one more time by using this timeout option okay so again i'm going to explain by default the timer value is one second so what it means is that it will wait for messages okay whatever you enter right it will keep everything in the memory for one second then only it will deliver actually okay so by default the value is one second like thousand millisecond we can set that to 10 or you can just set that to zero so that means do not wait as soon as i give the message to you just deliver it immediately so that is what we are saying okay so you can adjust that to 50 millisecond 100 millisecond whatever you like we can also set that to zero to send it immediately okay so let me also press ctrl c here let's clear this again let's start from scratch so console consumer i am not using from beginning let me hit enter okay so here timeout zero that is what i am using hit enter okay so now i am ready to produce messages so now if i say one if you see it gets is delivered immediately right you see three as soon as i enter here we here we see the messages so see now this is fast enough let me quickly show you the timeout option so the timeout this one the default value is one second so it is the maximum amount of time a message will queue awaiting sufficient batch size basically internally it's the console producer keeps the messages in a queue then it delivers them in batches okay in a high level so mainly i wanted to show you this to talk about this property linger.ms so this is the option to control linger.ms in producer configuration so let's talk about this
