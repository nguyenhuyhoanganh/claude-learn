# Message Serialization & Deserialization

this will be another quick lecture let's discuss how Kafka stores the messages internally Kafka stores and it transports everything as bytes it does not understand your message it does not care as well its job is to accept the messages from the producer and deliver the messages to the consumer so the serialization and deserialization it's done by the individual applications not by the broker so Kafka will be expecting the messages as bytes this is what this is how it works so the producer API the Kafka client library it has to be configured how to serialize the messages similarly the consumer side we have to configure how to deserialize the messages Kafka client library comes with some basic serializer and deserializer for example how to serialize the integer how to deserialize the integer and so on so very basic so we have to ask the producer API to use this to serialize and deserialize but what about complex java objects because this is okay if you are going to send an integer as message yeah we can use this but in the real life we will be sending some complex Java objects right order events payment events etc so in those cases we can use Jackson library to serialize our object into JSON then the JSON can be converted into byte array right so that is how we will be sending anyway the spring framework will be doing all this heavy lifting for us but this is the idea I want you to know how Kafka store the messages who is responsible for serializing and deserializing so this is what I wanted to share with you now if you are wondering how did the console producer and the console consumer work then without providing the serializer and deserializer etc ok so this console producer and the console consumer these are like simple tools for learning and testing they always treat the messages as string by default so whatever you type right the console producer when you hit enter it will take the information as string and convert that to byte array and that is how it sends similarly the console consumer it will take that byte array convert that to string and it will display so this is how they have been configured so this is how it works
