# Demo: Handling Null Keys

In this lecture, let's have a quick demo. I had mentioned that we can send messages without a key, right? The key can be null. Let's imagine a topic with the two partitions. And let's try to produce messages without a key or null key. In that case, let's see what happens. This is the demo. So far that we are going to create a topic with the two partitions. So I'm creating one new topic called a null key topic, something like that, with the two partitions. Then we are going to have two terminals for consumer group with the two consumers, console consumer, null key topic, and the null key group. This is what I'm having. In the terminal two, I'm doing the same, two consumers from the consumer group. Now we are going to start a producer. I do not want to type one by one. So I have this script, simple script, as you see. So every 50 milliseconds, we are going to produce one message, message one, message two, message three. So this is a loop. So it will continuously send messages via the console producer to the null key topic. So this is what we are going to do. And let's observe the behavior. I have opened the three terminals. I have access to the docker container first. Let's create the topic. Okay. So the topic is created. Now in these terminals, I'm going to start the console consumer, copy that given command. Okay. So this is ready. And again, here, I'm going to start another console consumer. This is also ready. I have cleared the screen here. Copy the producer script and paste it here before hitting enter. Please check that here we are sending the message every 50 milliseconds. It will be super fast. Let's observe the behavior. Let it run for some time. If you hit enter, now if you see this consumer gets all the messages, right? This consumer seems to be sitting idle. And if you wait for some time, be patient. Now this consumer gets the message. This is how it works when you send messages with a null key that it does not do the round robin for each and every message. Instead, it will send some messages to this consumer first, then it will switch to this consumer, then it will switch to this consumer and so on. So this will be the behavior when you send messages with a null keys. I simply just wanted to show you this, nothing else at this point, we can simply stop this.
