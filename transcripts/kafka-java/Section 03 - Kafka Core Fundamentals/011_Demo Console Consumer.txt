# Demo: Console Consumer

In this lecture, let's see how to consume messages from a topic for that We have another command line tool and this is mainly used for learning testing and debugging purposes This is very similar to console producer so, please go to 03 console consumer And here you will be giving the bootstrap server and the topic nothing else So, let's simply just copy this whatever we have here and let's go to the terminal and I have opened another terminal. So this is our Producer I have already produced the messages and whatever I was showing you in the previous lecture Hello world 123, etc So now I have opened another terminal and accessed the docker container. I am in the bin directory So I am going to simply Paste this command console consumer bootstrap server Demo topic etc. If I hit enter Now even if I am going to wait for one month, I am not going to see all these messages It's disappointing, right? That's how I also felt when I was playing with the console consumer for the first time Okay, so this behavior is expected that is the consumers by default They will be consuming only the new messages So whatever the message is produced by the producer, right in this case We have these four messages the consumers will not see it's an option We can adjust this but this is the default behavior You might be wondering why it's like this, but if you think about this, right? Maybe some applications might want to behave. Maybe this is what they want Who knows for example, let's imagine that we have one producer application which has produced the weather information Probably they might be running for the past three days So the consumer application wants to consume the weather information But it might not be interested in the the day before yesterday data and done Okay, so it wants to know what is the current weather? So it might be interested in the only the new information, okay, so it might not be interested in the history so in those cases The consumer application This is the option they might want particularly the stock prices and all you might be interested in the the new data Might not be interested in the older data. Okay So this is why it's behaving like this now For the producer right if I press sorry if I enter for The consumer sees four, okay, if I produce five the consumer sees five However, it's not seeing the hello world one, two, three and on So any new messages the consumer will see but it will not see the old messages by default This is great. So now what if I want to see all the messages? Okay, so for that The consumer side right plus sorry press ctrl C Okay, so let's come out of this then Same command, but one we have to pass this option from beginning Like this. Okay, nothing else. So now if I press enter Enter So this consumer will be seeing all the messages And as usual any new messages, whatever you produce That will it will also receive those messages
