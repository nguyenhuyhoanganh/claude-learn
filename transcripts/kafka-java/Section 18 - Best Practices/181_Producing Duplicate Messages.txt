# Producing Duplicate Messages

Now we understand what Idempotent producer is, right? Now, let me clarify one more thing related to that. And this is important. Okay. So the Idempotent producer, right, is only we are talking in terms of like the client library retry. Okay. So that is you as a developer or the application when it generates duplicate messages, let's imagine that you as a developer, you created these messages twice. Okay? You already sent these messages. They were written but by mistake. Your application created another set of same messages. Okay. Somehow some bug in the code. Okay, let's imagine so. So what will happen now? So they enable idempotence. Will it help me? Actually, no, it will not help you. And those messages will be sent to Kafka broker. And the Kafka broker will write those messages as a new messages. New set of messages, actually. Okay, so why is mainly because these messages will be having a different sequence ID? Okay, so remember that the sequence IDs are generated by this client library and it send those messages right? So whenever it receives new messages from the application, it assumes that those are like new set of messages actually. Okay. So the enable idempotence we are talking in terms of this library and the retry perspective only. Okay. If you are if your application itself generate duplicate messages, then you have to be careful. That is not this library's problem and it's not Kafka brokers problem. Okay.
