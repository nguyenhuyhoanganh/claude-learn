# Type Inference with Generics

In this video, I want to point out one or two corner cases around generics, specifically inside of classes. All right. So down here, we wrote out new array of anything. And when we created that instance of array of anything, we specified a type for T inside of that class definition. So when we put string right here, we can imagine that we went through the class definition and we replaced T with the word string. Like so. So that's what we can imagine is going on behind the scenes. Now here's something really interesting. If I delete that specification right there of angle brackets string, you'll notice that it appears that everything still works out just fine. If I assign this instance of array of anything to some variable, I'll call it say RR like so and then mouse over. Ah, you'll notice that it has already filled in for me a type annotation that says array of anything with angle brackets of string, even though you and I did not actually specify that. So how did TypeScript figure out the type of array of anything for us? Well, this is another incidence of type inference. So here's what's going on behind the scenes. When we create a new instance of rape, anything we pass in an array of strings right there. And TypeScript knows that is an array of strings. Remember, TypeScript is very intelligent. So we passed in that argument right there, and that shows up as the variable or the argument to the constructor of collection. So TypeScript is going to look at the type of collection that we just passed in, and it's going to say, Oh, it is an array of strings. So even though you and I did not specify a generic for type T, TypeScript is going to assume because we just put in an array of strings. Well then t must be string. So again, that is type inference. We spoke about type inference way long ago when we were discussing type annotations and it applies to generics as well. So with type inference around generics, it turns out that we've already been making use of generics quite a bit throughout this course, even though you might not quite have realized it just yet. In particular, you might remember the very first application we worked on involved Axios. It turns out that when we made use of Axios, well, Axios is a generic class. So when we made use of Axios, we had some specific return type, but we didn't actually have to annotate it when we made use of Axios back on that very first application. And that is thanks to type inference or type inference around generic specifically. Now understanding type inference around generics can be a little bit tricky. You really need to look at a couple of different examples of it. So let's take another quick pause right here. We're going to come back the next video and talk a little bit about how we can use generics with functions in addition to classes. And we'll see another good example of type inference over there as well. So another quick pause and I'll see you in just a minute.
