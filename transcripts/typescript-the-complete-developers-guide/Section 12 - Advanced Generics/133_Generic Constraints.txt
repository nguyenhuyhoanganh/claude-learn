# Generic Constraints

In this video. I want to show you one last quick example around generics. So at this point, we've got a function right here called print anything and print anything is going to call console.log with some element inside that array. I want to try building another print. Anything function, but rather than directly console.log in it like so I want to try to call some like built in print method on this element right here like RTI. So that's going to imply that our argument of RR is going to be an array of objects that have some print method on them. That's kind of confusing for me to say out loud. So let's just go through a quick example. So I'm going to put down another section header down here, and I'm going to call it generic constraints because that's what we are really going to be talking about. So then down here, I'm going to define two very simple classes. One is going to be called class car, and it's going to have a print method. Any time we call that, we're going to console.log out. I am a car. And then going to find another class called House. This thing is also going to print method. Any time we call it, I'm going to console.log. I am a house. All right. So I now want to define another generic function that will take an array of either cars or houses, loop through the array, and then call the print method on each element. So let's try doing that. Right now, I'm going to define a new function called print houses or cars. I want this to be a generic function. So I'm going to put on the DT right there inside those angle brackets. And then I'm going to say that this first argument is going to be called RR and it's going to be an array of type T, and then I'll put on my return type annotation of void as well. So now we can iterate through R and for every element on there, we're going to try to call that print method. All right. So I'm going to say four let I equals zero less than error length plus plus. And at ah at I I'll call the print method like so now as soon as I do that we get a nasty little error message here and it says property print does not exist on type T. So what does this error mean? Well, this is saying TypeScript saying, hey, you're going to say that we're going to dynamically specify type T. So this element right here that we are trying to reference will be of type T. And right now, there is no guarantee that this type right here of T is going to have a print method available. So, for example, if we tried to call print houses or cars and pass in an array of numbers like so this could very easily lead to an error when we actually run this code because we're basically going to have a number and try to call the print method on it. Numbers in TypeScript and JavaScript as well do not have a built in print method, so this definitely would lead to an error. So what we need to do here is add on a constraint to type T. A constraint is going to essentially tell TypeScript that we promise that there will be a print method available. So that's going to make sure that whenever we try to call or otherwise reference print houses or cars, we're always going to feed in a type that will have a print method available. So to do so, I'm going to define an interface right above the function definition. And I'm going to call the interface printable. And then on the interface, I'm going to say that there's going to be a print method that returns nothing like so. And now here's the magic. When I specify type T right here, I'm going to say T extends printable like so. So this is the actual constraint right here. This is us telling TypeScript that whatever type T we put in here, it's going to have all the properties that the interface principle has. So in other words, whatever type T we put in here, it will have a print method that returns nothing at all. As soon as we add that in, you'll notice that the code down here now shows us an air around each of those different numbers. And if we mouse over them, the air says that the type number cannot be assigned to type printable. In other words, a number does not satisfy interface printable. So we are not allowed to pass in numbers to print houses or cars. Again, this is called a generic constraint and it's going to limit the types that we can pass in for RT right here. So we can't pass an array of numbers again because they don't satisfy that interface. However, we can call print houses or cars with an array of houses or cars. So for example, I can call this with new house and new house and so on. And there's no air anymore. There's no air because an instance of a house has a print method, so it satisfies this interface. We can also call print houses or cars. With an array of cars, just array of car like so as well. We could even go so far as to put on the actual generic annotation right here. We should do that because I just told you in the last video that we are going to generally add them on. So I can say this is going to be print houses or cars and the first argument is going to be an array of houses or the same thing with car as well. All right. So this idea of using a type constraint or generic constraint is going to be really helpful because any time that we want to try to call a method or maybe reference a property on some value that's going to be marked by the generic type, we might want to make sure that that property or method or whatever actually exists. And we're going to do that by defining the separate interface and then using that interface when we define the actual generic type itself. All right. Now, it might seem like this is kind of like a rather esoteric or hard to understand topic, but believe it or not, it actually comes up all the time and we're going to see an example of it on the very next application we work on. So with that in mind, let's take a quick pause right here. We're going to come back to the next video and start working on our next project.
