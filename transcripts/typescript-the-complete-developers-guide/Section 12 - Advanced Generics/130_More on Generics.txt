# More on Generics

In the last application we worked on, we discovered the topic of generics. Before we move on to our next application on a very quickly covered generics again and go into a little bit greater detail just so you understand how they work. So to get started, I'm going to change out of my project directory and I'm going to go back into that features directory that we were working on way earlier on inside this course. I'll then start up my code editor inside there and we're going to write out one or two quick examples. All right. So to get started inside of my root project directory, I'm going to create a new file called Generics Dots. Then inside of here, I want to first begin by writing out a class that kind of represents a simple array of numbers. So I want this to have a simple property of maybe something like collection to reference an array of numbers, and then maybe one method that we'll just call Git that will allow us to get some specific element out of that array of numbers. So I'm going to make a class array of numbers. I'm going to define the constructor method and say that this thing will be coupled with some collection of data, and that has to be an array of numbers like so. And then we'll say that this thing has a single method called Git. And whenever we call Git, we have to pass in the index as a number and that should receive or retrieve, I should say, some element out of our collection of data. So this is going to return a number. Then inside of here, we can say return this collection. At Index. Well, pretty much simple enough, right? Nothing too crazy here. So let's now try to create an identical class. But rather than representing an array of numbers, we'll instead have it represent an array of strings. So right underneath, I'll say class array of strings. This thing will also have a constructor with some modifier of public on the first argument that will call a collection. This is going to be an array of strings and we'll do an identical git method on here as well. But instead of returning a number, we're going to return a string because now we are dealing with an array of strings. So let's say get index at number that's going to return a string. And to actually return that will do this or simply return this collection at index like so. So this is an example of very similar to the one that we just saw in the application we went through. We have two classes that are just about identical in nature. The only big difference between the two of them is the presence of the type of collection. So in the first one, we've got a number. Any time that we get an element out of that array, we will be returning a number. And likewise, down here we have an array of strings. Anytime we get an element out there, we're going to have a string. So ideally we would be able to condense both these classes down to one. And one way to do that would be to make use of generics. So let's define a new class down at the bottom of the file now, and we're going to make it a generic class. So I'm going to say class array of anything. Right after the name of the class, I'll put down those brackets and then t inside them. Remember, we think of T as being just like an argument. There's no special meaning to the word T or the letter T here. We could call it type in this class. You can call it anything you want. But again, by convention we usually refer to it simply as t like so. So now we can look at either of our implementations up here, and we're essentially going to say everywhere that we see a number or specifically number that's referring to the array that we are working with. We're going to replace it with T. Now, do you remember when we see index right here that's going to stay index as a number because the index coming into the get method, well, it always needs to be a number. So we'll say. Constructor. Public collection. This is going to be an array of TX type like so. We can then define our git method. So that's still going to be called with a number, but now we're going to return an element of type ht from it. So then we can return this collection at index like so. All right. So that's our generic class right there. Once again, just picture this as being like an argument any time we now reference this class in any way, shape or form. So if we want to create an instance of it or refer to it as a type or whatever it might be, all we have to do is write out array of anything. And then those brackets and then the type that we want to have specified for t inside that body of the class. So for example, if we now want to have an array of strings, we could say new array of anything and then put in just the word string right there. And then pass in are starting collection. So that would be an array of strings A, B, C and so on. Okay. So this example that we just looked at is basically similar to what we just saw in the application we just worked on. So of course, there are a couple of other elements that I want to show you here. So let's take a quick pause right here. When we come back, the next video, we're going to continue on with generics and learn a couple corner cases around them. So a quick pause and I'll see you in just a minute.
