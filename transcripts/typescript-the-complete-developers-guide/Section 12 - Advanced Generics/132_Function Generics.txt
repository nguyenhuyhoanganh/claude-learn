# Function Generics

In this video, I want to show you an example of generics around functions. So we're going to write out one or two examples and we'll also see another instance of using type inference around generics as well. So let's get to it. So I'm going to make a new section header down at the bottom of my file and I'm going to say example of generics with functions. Then down here I'm going to write out two separate functions. And these are both going to be very simple. I'm going to call one print strings and the second. Rent numbers. So each of these functions is going to receive either an array of strings or an array of numbers. And both of them are just going to iterate through that array and attempt to console.log out each element inside of that array. So for print strings, I'll receive an argument that I'll call simply, ah, and that's going to be an array of strings like so, and this is not going to return anything. So we'll give it a return type of void. So then I can set up a simple for loop inside of here. I'll say four let equals zero. I less than array length I plus plus. And then inside of here we can do a simple console log with RR at I like so. So now, as you might expect, well, the implementation of print numbers is going to be just about identical. So we could actually just copy paste over that for loop like so. And then the only thing we'd have to update is the arguments on print numbers. So I'd say, well, this thing is going to be coupled with an array. That's going to be an array of numbers and it's not going to return anything. So once again, we can very easily see that there is 100% code duplication between both these different functions. So a very easy way to fix this up would be to make use of generics. We could create one new function that could receive any type of array. So an array with anything inside of it. So to do so, I could write out a new function down here called print. Anything. It's again, going to have a return type of void. And then to specify that this is a generic function right after the function name we can put in those brackets and t once again. So then we can say ah and say this is going to be an array of t types. But inside of here we could have the exact same logic we had previously. So for let I equals zero. I less than RR. That length. I plus plus like so and a quick console.log as well. So now, once again, just like with functions, we imagine that this is like an extra argument right here. Whenever we attempt to reference or call print anything, we're going to specify the type of T and that's going to tell this function that it's going to be eventually called with an array of type, array of whatever T is. Once again, t is not some special identifier. We can call this anything we want to call it like type of array or something like that. But again, by convention we usually refer to it simply as T. So then if we wanted to call this function, we could say print anything which down are angled brackets like so and specify the type of that array. So we could say simply string. Now we can pass in array of strings. So like A, B and C. Now, one thing that's really easy to get confused on is whether or not right here in these angle brackets we should put string or string with square brackets like so we would not put in square brackets like so because that would indicate that we're going to pass in an array that is a two dimensional array of strings. Remember, t is going to be replaced everywhere inside this function definition so that t right there would be changed to array of strings like so. So we would not put in string with square brackets right there in the actual generic definition. Now once again functions make use of type inference when we are making use of generics as well. So we could technically drop off those brackets right there and everything would still work out as expected. We can now mouse over, print anything and you'll notice that TypeScript has already added back on those angle brackets in that function type annotation. And it also understands that the array argument is going to be an array of strings as well. So once again, TypeScript has inspected this element right here. It said, Oh, okay, this is going to be an array of strings. So TypeScript can now figure out by itself what the type of T is going to be. So it's going to go back through the entire function definition and replace T everywhere inside there with string. Now this topic is kind of very similar to using type inference around return values as well. Remember we spoke about technically when we write out a function, we do not specifically have to add in an annotation for the return type because TypeScript can figure it out for us. However, it's very helpful for you and I to add in that type annotation manually on return types all the time because it helps us check errors. For example, we might accidentally write in a return statement somewhere inside of a function, and if we add in a manual type annotation, as we did right here, TypeScript will help us decide that, hey, that's probably an error. And we probably did not mean to do that. So in a very similar fashion, whenever we make use of generics around classes or functions, I still recommend you add in the type annotation on the generic as well because it will help you catch errors. So for example, if we said that we wanted to call print anything with something that is going to be an array of strings, well, that's going to help us catch a scenario where we accidentally then pass in an array of numbers. If we pass an array of numbers, TypeScript is going to say, Hey, this was supposed to be a string, but you put in a number right here. So in general, I still, you know, for the most part, recommend you add in a type annotation for generics with classes and string or scammy functions, even though strictly it can sometimes be ignored. Okay. So let's take another quick pause right here in the next video. There's one last quick thing I want to show you around generics. So quick pause and I'll see you in just a minute.
