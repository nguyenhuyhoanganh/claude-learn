# Prototypes Reminder

In this video. We're going to get a quick reminder on what constructors and prototypes are in normal JavaScript. All right. So we're going to write out a quick code sample using the TypeScript Playground editor. I've got a link to it right here on the screen. And so once you navigate to that link, you should see a page like this. Quick reminder, if you write out some typescript code on the left hand side, you're going to see the compiled JavaScript output on the right hand side. So we're going to write out a quick code sample here in just a minute. But before we do, let me give you one quick diagram. So throughout this course, we've been making use of TypeScript classes. And chances are if you're watching this course, you've probably seen S 2015 classes as well. Now, as a reminder, we say that classes in JavaScript are syntactic sugar. In other words, in JavaScript classes don't really exist. Classes were added in just to make working with inheritance and objects easier for engineers who are used to working with, say, Java or C sharp or other object oriented programming or class based languages. But in JavaScript, the idea of true classes was never actually implemented. Instead, when you write out an IS 2015 class or a TypeScript class, behind the scenes it is executed by JavaScript using the prototype inheritance system. So even though we think we're making use of classes, yeah, we're still using prototype inheritance. So let's get a better idea of what this prototype inheritance stuff is. All right. So we're going to write out a quick code sample here. I want to write out a class to represent a simple boat. And maybe a boat has a color property and like a pilot or kind of like navigate method attached to it as well. So we're going to write out a TypeScript implementation on the left and see what JavaScript we get on the right hand side. So I'll write out Class Boat. And this is going to have a color property that is a string and I'll initialize it to red and it will also have a pilot method that doesn't return anything. And when I console or see me when I call it, I'm going to do a console log of swish, which I think is the sound that a boat makes. All right. So now we can take a look at what got output on the right hand side. So you'll notice that there's a reference to that prototype right there. Clearly, there is no evidence of a class being the output coming from our typescript class. So this is how we would implement this kind of class right here using the prototype system. I can now copy all that JavaScript code right there. I'm going to open up my browser console, I'll paste it in, and now I can use that to make an instance of a boat like so. You notice that now my boat has a color that's a string of red. And if I wanted to, I could call new boat pilot. And I'll see the console log of switch right there. Okay. So there's a couple of things I just want to make sure is really clear about this prototype inheritance system. First off, we refer to the function defined as both as our constructor function. So this has a very similar purpose to a normal constructor function that we are used to seeing inside of a class. We use this constructor function to initialize different properties that we want our object to have. Inside that constructor function, we are generally only going to associate different properties such as strings, objects, arrays, booleans and so on with the value. This, if we want to associate any different methods with an instance of the boat, we're going to assign those different methods to the prototype property. Now that prototype property right there, that is the real magic of what is going on here. And that's what I really want to focus on. So any time that we call a different method on an instance of a boat, we make a new instance of a boat right here. So I'll say our boat is a new boat. If I then try to call pilot. Yup. No issue. I call that method successfully. However, I want to show you something interesting. I'm going to write out just vote by itself and when I do so I can then expand it. And you'll notice that we don't see any reference to anything called pilot. Like there's no pilot method here. Well, in reality, the pilot method is buried inside this underscore underscore proto property. If I expand that, I'll then see the pilot method right there. So what's going on with this proto thing and how is it related to prototype? Let's take a look at a quick diagram. Okay. So in this diagram right here, we've got our instance of a boat tied to this instance of a boat or all the different plain property assignments again, like numbers, strings, booleans, arrays and so on. But any methods that we want to assign to our boat get tied to that prototype property. The prototype is actually invisible, more or less on our instance of boat. I know that you and I can see this underscore underscore proto thing right here, but in the actual JavaScript spec there is no underscore underscore proto property. Instead, that property right there is added in solely by my Chrome browser just to help with debugging. So as far as JavaScript is concerned, that proto property does not actually exist. So any time that I try to call the pilot method on my instance of boat right here, here's what happens behind the scenes. JavaScript is going to look at this object and it's going to say, Okay, do you have a pilot method attached to you? In this case, no, we definitely don't. So behind the scenes, 100% invisible to you and I. JavaScript is going to look at Boat's prototype property and remember this prototype thing right here is invisible. JavaScript is going to look at the object referred to by that prototype property that is boat's prototype. And on there it's going to find the pilot method and invoke it. So this entire kind of lookup process goes on behind the scenes without us even knowing about it. And there's a couple of things I want to really stress around this prototype thing. First off, a prototype property can be easily modified or looked up. We can add methods to it. We can do anything like that that we want to do. So for example, let's imagine that we want to add another method to class boat. One way to do that would be to just go back over to our class definition right here and add in a new method like, let's say, float. And if we call a float, we'll do a console. Log of boat is floating. So now if we run this code. I'll create a new instance of boat again and I can successfully call float like so. So we can easily attach as many methods to prototype as we want. However, the really interesting thing and kind of unexpected thing about prototype inheritance is that we can add prototypes or methods to this prototype even after an object has been created. So for example, let's say I create a new boat right here and I'll say concept boat without some new property or method name, let's say sink. So boat without sink, that will be new boat like so. So now if I try to call boat without sink. Dot sink. I'll get some undefined air. So let's now say that we want to go back to that prototype and add in a method definition to it. So we would define boat. Dot prototype. Dot, sink. I'll say that is a function. And any time I call it, I'll do a console. Log of boat is sinking. So now if I go back to my boat without sink. But without sync. I can now call that sync method on it and I will no longer get an error. And this is really unexpected behavior right here to a lot of people coming from other languages. So you can actually look back at a prototype and add methods or remove methods or change methods on the fly. And that's going to essentially modify every active instance of boat that we have inside of our application and of course, every future instance as well. That's kind of weird, kind of unexpected, but actually can serve in our favor in some regards, particularly when we start looking at decorators. Okay. So in this video, essentially all we really accomplished was to say that when we write out classes, we're still making use of the prototype system. Every class we create creates something called a prototype, and that stores all the different methods associated with our class, or in this case, really our object that we're trying to create. If we're looking at it from the JavaScript side and we can easily modify a prototype even after this thing has been defined and potentially even after we've created an instance of an object as well. So now that we've got all that stuff out of the way, let's take a quick pause right here. When we come back in the next section, we're going to have a quick discussion on. Well, where is it? Here we go, Decorators. So I'll see you in just a minute.
