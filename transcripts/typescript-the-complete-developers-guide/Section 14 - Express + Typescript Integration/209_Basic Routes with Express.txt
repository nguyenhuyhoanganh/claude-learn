# Basic Routes with Express

All right, now that we've got our project set up, we're going to start to install Express and then get a very simple server started. So let's get to it. To install Express. I'm going to flip back over to my terminal and inside my project directory I'll hit control C and then do an NPM Install Express. At the same time, we're going to install a couple other middleware that we're definitely going to need to build this application. So first off, we're going to need a middleware to parse all the text inside this form. We're also going to need some kind of middleware to track whether or not a user is logged in. So to track that, we're going to use another middleware called Cookie Session that allows us to store some information in a cookie about a given user. So I'm going to install express body dash parser and Cookie. Dash session, I'm going to zoom out so that all gets on one line, hopefully. There we go. All right, I'll install that. That'll take just a minute or two. So I'm going to let it do its thing, and then I'm going to immediately start my server back up with an NPM start. Okay. And PM start and there we go. All right, so let's flip back over and get started. So inside of my index file, I'm going to import Express and start up a server. That's it. We'll start off really simple. So I'm going to first begin by deleting the console log. I'm going to import Express from Express. Now, as soon as I do so, I'm going to see very predictably an error message right here. And the reason for that, remember, usually when we make use of third party JavaScript libraries, we have to install a type definition file. So we just installed the actual libraries, but we didn't get the type definition files. So let's go back over to our terminal and get those type definition files. So I'm going to change back over, stop my server a second time and then we'll do an NPM install. Or at types express. At types. Cookie session. So we're just going to get all these type definition files in one go and at types slash body parser. All right, so we'll run those. These will be really, really quick to install. And then we can start everything back up once again with NPM start. There we go. All right, so now I'll flip back over, and that error message has gone away. All right. Now that we've got Express inside of here, we can use it to create an app. So I'll say const app is express. Now, remember, if you're watching these videos, I'm assuming that you have a basic understanding of express. So I'm not going to really explain what is going on with lines of code like that right there. Next up, I'm going to attach a single root handler to that app. So I'll say app target or add slash. And then remember, we've got a root handler function right here, so we need to receive a request. And Rez object from it. Now, here's the big question. Here's like where the typescript stuff comes into play. We can add in optionally on this thing type annotations to this function. It's not strictly required, and it really comes down to how far or how deep with TypeScript you want to go. So personally, I want to show you like the most TypeScript stuff as possible in general. So I do want to add in type annotations for this request argument and this response argument as well. So to do so, I need to import a type from Express that represents exactly what a request object is and what a response object is to do. So if we want to import these types, we can do a command click on Express. And this will open up a file that tells us about all the different types that express exports, or more specifically, the express type definition file. So inside of here, we can do a search for request. Actually, let's just cut right to the chase here. We're going to look for interface request. So you'll see that exported from Express is some kind of interface called request. So this interface right here describes all the different properties and functions and whatnot that a request object is going to have. We can get the same thing for a response object as well. So right here I can see an export for interface response and this interface is going to describe all the different properties and functions that are response has as well. So we can import both those interfaces. With import. Request and response. And now we can use those two types right there, those two interfaces to annotate rec and rez. It's all put on request. And response. Again, pretty much optional, but I'm going to show you the more typescript side of things just to make sure it's really clear that, hey, we are using TypeScript and not just writing out plain JavaScript here. So now inside of here, we can do a resend and attempt to send back some plain HTML. So let's do a multi-line string right here. And I'll do like a div. That maybe has an H one. But says simply, Hi there to get started. Then finally, to make sure that our server is listening on some port and ready to serve out this HTML. I'm going to add in at the bottom apt listen on port 3000. And then we'll pass in a callback function. And inside the callback I'll do a console log of. Listening on part 3000 like so. Okay. So I'll save this. I'll flip back over to my terminal and I should see listing on port 3000. Now, if you don't see listing on port 3000, remember Port 3000 might be in use on your machine. So if you see a big error that says like E adder in use or something like that, well, chances are you've already got a process running on Port 3000, so make sure you stop that server, whatever it is, or alternatively, just change port 3000 right here to 3001 or whatever other available port you have. So once I've got that server up and running, I should then be able to open up my browser. Go to localhost 3000 and see hi there appear on the screen. So easy stuff so far. So let's take a quick pause right here. When we come back, the next section, we're going to start implementing all this authentication stuff. So I'll see you in just a minute.
