# Parsing Form Bodies

Now that we've got our router all set up, let's start speeding through our implementation. So the first page that I want to really implement is the login form. So if a user makes a get request to login, I want to show a form with an email password and submit button. So let's get to it. I'm going to flip back over to my login routes file. I'm going to reuse the current route definition that we have right here, but I am going to change the path on it to slash login. Next up, I am going to import the request and response types and annotate the rec and raise arguments to my request handler. I'm going to do the same thing we just saw inside of indexes. So I'm going to import your request interface and response interface at the top, and then I'll annotate as a request. And as response like so. Remember, not strictly necessary. Just want to add in a little bit of additional typescript, that's all. All right. So now as my present, I'm going to rewrite that with a multi-line string. So, remember, use your back ticks there. So we're going to send back a form that has a method of post, because whenever someone submits this form, we want to make a post request. Then inside the form I'll add in a div. Wear that div I'll add in a label. Of email. An input with a name of email. And I'll do another div right underneath that div. On this next live, I'll do a label. Password. An input with a name of password and to make sure that the input is actually obscured. I'll put on a type of password as well. And we'll close off that tag. And then finally after that dive, I'll put down a button. With the text submit. And that's pretty much it. So we've now got a basic form put together for our login. Let's go and take a look. Just make sure that everything is currently showing up correctly inside of our browser. So I'm going to flip back over. I'm at localhost 3000 and I'm going to navigate to 3000, log in and there's our form and you'll notice that I've got these two icons on here. Those are just because I'm using a password manager inside of Chrome right now. So the password manager that I use automatically adds on those icons. Chances are you are not seeing them, and that's totally fine. So if we now fill in this form with anything at all and click on Submit, that's going to make a post request to slash log in. Right now we are seeing this error message because we don't have a route handler on our express server. So let's flip back over and add in a handler for a post request to slash login. All right, back inside of login routes underneath our current route handler, I'm going to add in a new one, so I'll call this router post to log in. Once again, this thing is going to take a request, object of type request and a response of type response. So inside of here, we need to inspect our request and somehow get our form data. Now, just a little bit of trivia here. Remember, when we're making use of express, Express is not going to automatically parse the contents of a form for us. Instead, to parse a form, we have to use that body parser middleware that we installed just a little bit ago. So remember, we did an NPM install for body parser. That's the middleware that is going to parse the form for us and then make it available to us inside of router dot post. All right, so inside of index. We need to wire up that middleware. So back over here at the top, I'm going to import. Body passer. From Body dash passer. And then to actually wire this thing up. I'm going to go right above the current use statement. Make sure you add this above. If you add it after, this is not going to work correctly. So we'll do use body parser, URL encoded. That's what's going to parse the form. So we're going to call that and pass in an option object with extended true like so. All right, so we'll save this file. I'll flip back over to login roots. And now inside of here we should have access to a new property on that request object. A new property of rec dot body. So that's going to contain the email and password out of that form. And the name of those properties is going to be email and password because that's the names that we provided on those two inputs, email and password. So we can pull off those two properties with a little bit of D structuring. So I'll see e mail and password like so. And now just as a quick test to make sure everything is working as expected, I'll just do a resend and we're just going to send back the email. And password. We'll just join them together as strings just to test this thing out. All right, so I'm going to save this. I'm going to flip back over. I'm going to go back up to my address bar. If you hit refresh right now, it's just going to do another post request. So I want to test the entire flow. So I'm going to go back up to the address bar and hit enter. That's going to make sure that I make another get request to that root. So now put in email of email and just some random password click on submit and I see the email and the password that I entered sent directly back to me. Okay. So we're definitely parsing the body correctly now, so let's take a quick pause right here. We'll come back the next video and we'll continue working on our login logic.
