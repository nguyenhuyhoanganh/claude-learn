# Wiring Up Sessions

In this video. We're going to continue working on our post request handler for login. So at this point, we've done a lot of clarification around what's going on with record body and whatnot. So now we need to actually fix this thing up and make it do what we originally wanted it to do. So remember, the goal here is that any time that a user attempts to log in, if they log in successfully, we're going to send them back to the root root and we're going to set some flag on their session that says, Hey, you are now logged in to our application. The other thing I want to remind you about is that our application is not going to have any idea of sign up. So there's going to be one single combination of email and password that can be used to actually log in to our app. So let's fix up a request handler and make sure it actually does this stuff. Okay. So back inside of post login. I'm going to delete that, if else statement just because as a lot of stuff inside there that we don't need like the email to uppercase. So now I'm going to write out the if statement. We're going to first check to make sure that the email and password are defined. And then if they are, we'll make sure that they are equal to some pre coded or hardcoded set of email and password credentials. So I'm going to say if email and password. So that's our type guard that's going to make sure that email and password are defined. And we'll say if email is equal to any hardcoded email you want. So in my case, I'm just going to say the only valid email is high at high. And then I'll make sure that the password is equal to some hard coded password, which for me is just going to be simply the string password. Okay. So if we get inside if statement that means that the user has successfully logged in and we need to somehow mark this person as logged in. And then redirect them to the root. Root. And then if we fall into this case, that means that either they did not not provide an email or password or the email or password were invalid. So if that's the case, we know right away that we just want to send back an error message and say invalid email or password. Okay. So now we need to actually mark this person as being logged in to do so. We're going to set a property on the user's session. Now, once again, if you are here in this course or specifically watching the stuff around express, I'm going to assume that you are familiar with Sessions in general, so we can use a session to tie some piece of data to a user or someone who is making a request to our server. When we set a property on a session, that information is going to be stored in a user's cookie, which gets handled or managed automatically by that person's browser. So every follow up request they make to us will have this information that we attach to their session. We're going to use that to mark that person as being logged in. Now to make use of a session, we first have to set up sessions inside of Express. So you might recall inside of our package JSON file, when we were doing our initial setup, we installed something called cookie dash session. So that's what we're going to use to manage sessions. This is a middleware that we need to wire up inside of our indexed file. So let's do that right now. Yes. I'm going to go back over to index. Yes. At the top, I'm going to import cookie session. From cookie dash session. And then right above my obtuse router, make sure this is above. If you put it underneath, this is not going to work correctly. We're going to wire up that middleware. So let's say app use. Rookie session. We're going to call it and pass in an options object. It's going to have a keys property, which is going to be an array of strings. So this keys thing right here is some information that will be used to actually encode the session or encrypt it. The actual key we passed in here doesn't matter. It just has to be a string, that's all. Okay. So that's going to add in sessions to our request object. Now something I want to mention here, because it's entirely relevant given the discussions we just had. Remember by adding in the body parser, that meant that we were actually adding in a body property to our request. We went through all that discussion about how the type definition file was not super well set up to accommodate that body property. But Cookie's session is very different. Cookies sessions type definition file actually handles the property that it adds to the request object much better. So let me tell you exactly let me give you some background here really quickly. Normally, like Normal Express, if we were just using Express by itself and we tried to refer to record session, we would get undefined from that by default. A request object does not have a session property. Just like normally a request object does not have a body property. It is only the fact that we added in this cookie session middleware right here that a request object is going to get a session property attached to it. That's the only reason this session property is now going to exist. So we just spoke about how to type definition file around body parser and express kind of handles all that stuff in a kind of weird way. However, Cookie Session actually pulled this off correctly. The type definition file for cookie session is much, much better handled. So I'm going to do a command click on cookie session really quickly. I'm going to scroll up to the top of this type definition file. And here's the really important thing. They essentially added in a line right here that says Go and find express because they are assuming that you're using cookie session with Express. They are finding the interface request inside of Express and they are saying that that request interface is going to extend from cookies session request. So this is some way that these cookie session type definition file is saying, hey, go find that request interface. We want to add in an additional property to it. That's what they're doing right here. They're saying we want to add in some additional property to that request interface and the additional properties are contained inside of cookies session request. So we can do a command click on that interface right there and will tell us about the different properties that cookies session is going to add in to that request object. And in this case, they're adding in a new optional property called session. And it's going to be of type cookie session object. So if we now do a command, click on that. It is going to tell us that these are the different properties that are going to be inside that session property. So it is changed is new and most importantly or right here. Property name coal and string any. So that right there is the magic ticket. That's the thing that you and I had to add in manually with body parser is they did not really do a super great job with the type definition file, but cookie session took care of all this stuff for us. So the long and the short of it, what I'm really getting at here is if we now go back over to log in roots and we'd write out REQ Session, we're going to see a reference to a property that was added by the cookie session middleware or the cookie session type definition file. So if we now hover over this, it's going to tell us, okay, rect session is going to be a cookie session object or undefined. So once again, cookie session, their type definition file took care of all this stuff for us. Whereas with body parser and the express type definition file, it was not quite so well put together. Okay. So that's pretty much it. So now this is our session property. Remember, we can use this to store some information about the person making the request. So I'm going to say that next session is going to be some object with a property called Logged In and I'll just set it to true. This right here is what's going to mark this person as being logged in to our application. And then finally we'll do a redirect. So we'll do a rez dot redirect. We're going to just send this person back to our root root like so. Now I'm going to remove those two comments just because we don't really need them. And that's pretty much it. So let's now test this out. Now, quick reminder, we don't actually have a route route just yet, so we're probably going see an error message when we test this. But let's just do it anyways. All right, so I'll go back over. I'm going to revisit, slash, log in. I'm going to put in the correct email and password of hi at haiku and password. I'll submit and I should get redirected back to local host 3000. All right, so that's pretty much it looks good. So now we can start to work on our route. Route? That's this thing right here. Remember, it just needs to state whether or not the user is logged in and then show a link to actually log out the user. If the user goes to that route when they are not logged in, we need to show the appropriate message and some text that says, Hey, here's a link to log in. So a quick pause and we'll take care of that in the next video.
