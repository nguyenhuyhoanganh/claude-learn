# Protecting Routes

In this video. We're going to add in this new route that a user is only going to be able to see if they are actually logged in to our application. Now, of course, we could add in a very simple request handler that just checks the user's authentication status. But a much better way to handle this, like a more express style way, would be to create a middleware that checks to see if a user is logged in before allowing them to go to some actual given route handler. So I'm going to flip back over to my editor and at the top of our login routes file. Right underneath our router or something, right above the router definition. I'm going to create a new middleware and I'm going to call it require off like so. So require auth is going to be coupled with some request, object, some response and some reference to a next function to run. So the type on next over here you might have you might recall that we've seen this a couple of times inside of all the express type definition stuff. If I do a command click on request again inside of here, we're going to see that there is an interface called next function. So this is essentially a reference to a function that's going to be like a next function or a reference to the next middleware that we want to call. So the type of next over here is going to be of type next function. So I'm going to make sure that I import that interface at the top. And then annotate the next argument right here. Like so. Okay. So now inside of this middleware, we can check to see if the user has a session and is logged in. If they are, well, then we're going to let them proceed to the next handler. Otherwise, we're going to set an error status on the request and send the response back to them with some kind of error code or error message. So inside of here, we first need to set up a type guard. So we'll say if. Rect session if that is defined and if wrecked session logged in is true. Then if that's the case, we want to call next and immediately return. Notice how I'm not putting return next like so. That's because we are not really supposed to return anything from a middleware function. And in fact, I didn't even annotate that on there. I'm going to do that right now. So I'm going to annotate this as returning nothing at all. So if we put on return next like so well, in this case, a next function doesn't actually return anything. So this doesn't actually cause us to violate this void annotation. Nonetheless, I would still really recommend you put return on the next line down. Just so it's really clear to other engineers that you are in fact not returning anything whatsoever. Okay. So now if we get past this, that means that the user doesn't have a session or is not logged in. And so down here we need to somehow tell the user, sorry, but you're not allowed to progress and see this protected root. So I'm going to set a status code of 403 of forbidden and I'll send back some message of like not permitted or something like that. Okay. So that's pretty much it. So now we can create a request handler that makes use of this require auth middleware. So I'm going to go down to the very bottom of this file once again. And I'll say rootkit slash protected. And then I'll set up that middleware that we just created called require auth. And then the actual request handler that we want to run if the user gets past require off. So I'll say req is request. And Rez is response. And then inside of here, we can just assume that the user actually is logged in so we can send them back some like protected information. So in this case, we want to say something like Welcome to Protected Route or something like that. So I'll do a simple resend. Welcome to protected root logged in user. All right, so let's do a test. I'll save the file. I'm going to flip back over. Right now, I'm not logged in. So if I try to go to slash protected, I'm going to expect to see an error message. And yep, I definitely do. If I open up my request log, I should also see that that request or the response as a403, which means I am totally forbidden. But if I now go back to my root route, attempt to log in, enter in a valid email. And password. I'm now logged in to the application. And so now I should be able to go to slash protected and see the protected route. And that's pretty much it. Okay. So that's pretty much this whole express application. We've now gotten through all these basic routes we want to put together. So let's take a quick pause right here. We're going to reflect on the code that we've written so far and kind of how we've integrated Express or Semi TypeScript, and we'll start to figure out how we can rewrite all this stuff in a really typescript fashion. So a quick pause and I'll see you in just a minute.
