# Using an Express Router

Now that we've got a very basic server up and running, we're going to start to speed through the actual implementation of all these different routes. So we want to have a route, route that detects whether or not our user is logged in with a link to the login page, then the login form. We need to make sure that the root route can detect whether or not the user is logged in. We need to implement some log out functionality and then we need to have this protected route that will reject a user if they are not actually signed in. So this is going to be basically a lot of typing. So let's just get to it. All right. So the first thing that I think is probably going to be pretty obvious here is that we're going to have a couple of different route handlers, like maybe five, six or seven in total. We could toss all those directly into this indexed file, but well, this file would probably get really large really quickly. So instead, I'm going to make a new folder inside my RC directory called Routes, and then inside there I'm going to make a new file called Login Routes. It's. You'll notice I'm using a lowercase l on here. Remember, in TypeScript, whenever we have a capital character as a file name, it means that we are exporting a class. But in this case we are not going to be exporting a class from the login roots file. Instead, we're going to create an express router. We're then going to associate a bunch of different routes with that router, and then we're going to export it from this file to our index file and associated with our express application. So in other words, we are not exporting a class from login routes. Remember, right now we are trying to figure out how we can implement this application using just a very normal JavaScript style approach, just sprinkling in a little bit of typescript here and there. So what we are doing right now is pretty much a typical approach for building something like this with Express. All right. Inside of login routes, I'm going to import. At the top router from Express. Now, if you've never used the router before from Express, it's essentially just like the app object we were looking at. We can associate route handlers with that router and then later on attach all them directly onto this app object. So for example, we could do const router equals router. Notice how there is no new keyword on there. It's just router. And then to associate a route with that, we would say router git or word slash. And then this would get our normal route handler with a request and RTÉ's argument. And then inside of here we could do a resend either, just like we have inside of indexes right now. So then at the bottom of the file we will export that router like. So notice how we are using curly braces here because we are not doing a declaration of router and export on the same line. Instead we are trying to export a variable that has already been created. So to do so we have to use those curly braces. So now we can save this file, pull it back over to index dots at the top. I'm going to import the router that we just created from the Roots log in roots file. And then I'm going to delete that request handler we already put together. And I'll add in app dot use router and that's pretty much it. That's how we use a router object. So I'll save this file. Pull it back over. We'll do a quick test. Yep. I can still see that text right there. Okay. So we're going to continue working in this log in roots file. We're going to associate some more routes with this router thing. So let's take a quick pause right here. When we come back, the next section we're going to add on some of these other route handlers so we have to deal with. So I'll see you in just a minute.
