# Project Setup

Now that we understand what we are trying to build, we're going to go through a little bit of setup in this video. The project setup is going to be identical to what we've done in the past, so we're going to generate a package, JSON file, install a couple of dependencies, write out one or two scripts to get our TypeScript compiler and Node one running, and that's pretty much it. So let's dive right in. To get started, I'm going to flip back over to my terminal. I've still got the two processes running from our previous application, so I'm going to stop both those with a control c. Like. So I'll then go up one directory back to my workspace folder and then inside of here I'm going to make a new directory. And I'm going to call this one server. I'll then change into that new directory. And then inside of here I'm going to generate a new package json file with npm init. Why? After that, I'm then going to also immediately generate a config file with TSC dash dash in it. And then finally I'm going to install those two dependencies that we've been using quite a bit node on and concurrently. So I'll do an NPM install and currently. And Nordman. So I'll let that go. Should take just a second or two to actually install these things. So I'm just going to let it do its thing. As soon as that's done, we're going to open up our code editor and write out a couple of scripts, update our config file and create a couple of directories. All done. So I'm going to start up my code editor and let's get to it. So to get started inside my root project directory, I'm going to make an Z folder and I'm also going to make a build folder. As usual, please make sure that you create the build directory outside of CRC. If you just use VS code as usual, chances are you're going to accidentally create it inside of SC. Next up inside of our config JSON file, I'm going to find the outdoor and router options and uncomment both them. So my output directory will be filled and my root directory will be sc like so. I'll then save this file and then go over to my package json file. And then inside of here, we're going to add in those three different scripts. So I'm going to delete the initial script that you see right there, and I'll replace it with a start. Colon build. That's going to run. Tsk w. Well then do a start colon run that will do a node on build slash indexes. And then finally a start script to get all this stuff running, which will be concurrently. 9 p.m. Colon Start. Colon Star. All right. I think the last thing we need to do is create a starting index file inside of our CRC directory. So I'm going to open up my CRC folder. I'll make a new file inside there called Indexes and I'll do a simple console log of Hi there like so. Okay. That should be it. So to test this all out. I'll flip back over to my terminal and I'll run 9 p.m. start. Now, remember, the first time we run this, we might see a nasty error message. That's totally fine. I'm just going to hit control C and restart and PM start. And there we go. I've now got high there appearing. All right, so that's our project set up. So let's take a quick pause right here. We'll come back the next video and start working on the actual express application. It's a quick pause, and I'll see you in just a minute.
