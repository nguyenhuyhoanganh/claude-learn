# App Overview

In this video, we're going to start working on our first application to understand how to get TypeScript to work nicely with some JavaScript, library or framework. The first application we're going to focus on is going to be all about express. JS So we're going to learn how to get Express to work nicely with TypeScript. If you're going to watch this series of videos like specifically about Express, just so you know, I'm going to assume that you already have a basic understanding of express and that, you know, things like, say, route handlers and middleware and so on. If you're not familiar with those concepts, well, I would encourage you to get a little bit of background on Express first. This is first and foremost a course about TypeScript. Of course. Okay. So let's talk about what we're going to be building. So we're going to make a very simple kind of like authentication style application. So using EXPRESS, we're going to try to build some website that we can visit different routes on and see different content up here on the screen. We're going to have a route, route that tells the user whether or not they are currently logged in and then shows them a button to actually log in to our application. Whenever a user clicks on that login link will take them to slash login as a root and we'll show them a very simple login form. Now this is all going to be very basic HTML that we are sending from our express server down to the browser. So we're no longer talking about like those view classes or models or anything like that. This is just plain old express. Now as far as the actual login process goes, we're not going to have any actual backend database or anything like that. There's not going to be any sign up feature. It's just going to be a plain hardcoded email and password. So our application is just going to have one valid login credentials set. Once a user enters that and clicks on submit, we'll then tell them, okay, you are now logged in. If they go back to that root, root, and once they are logged in, they can also go to some routes called slash protected. If a user is logged, logged in and they go to this root, we'll show them some information saying, Hey, you are logged in. You are allowed to see this root. And if someone tries to go to that root of slash protected when they are not logged in, then we're going to give them some error message and say denied or something similar to that. So in essence, it's going to be a very simple express application that we're going to put together because we really just want to focus on understanding how to get TypeScript and Express to work together nicely. Now, in the last video, I told you that we were not going to focus too much on this first way of getting TypeScript to work with some outside library. Nonetheless, I figured, Hey, what the heck? We got some time, right? So we're going to build two different versions of this application. We're going to first build a pretty simple and straightforward version of it, and we're just going to like speed through the implementation basically as fast as we can go, just using express normally and adding in the bare minimum amount of typescript specific stuff as we can. So basically really just type annotations and that's it. Once we get through that very simple implementation, we'll then refactor it into this much more complicated version. And honestly, when I say much more complicated, I mean that relatively it's really not going to be that bad at all. Okay. So that's the idea, implementation number one. And then very quick refactor into implementation style number two. So let's take a quick pause right here. When we come back to the next section, we'll do a little bit of basic project setup. So I'll see you in just a minute.
