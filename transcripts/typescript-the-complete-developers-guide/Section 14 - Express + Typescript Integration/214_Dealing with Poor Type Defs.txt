# Dealing with Poor Type Defs

We've now spent a lot of time talking about this wrecked body property and how it is not very well handled inside of our type definition file. So in this video, we're going to figure out some solution. Here's my idea. We've already got an interface for type request, and we've seen that the only real issue with this interface is that that body property is not very well typed. So one way we could fix that is by opening up the type definition file and changing the type directly inside there. But as I said, we never change a type definition file manually because it is a dependency to our project. And as soon as we reinstall our dependencies, well, all of our changes get wiped away. So instead, we're going to create a new interface that is going to extend type request. So we're going to essentially borrow all the different type definitions that are defined already inside that interface. And we're just going to override or kind of rewrite that one body property and give it a more appropriate type for our application. So to define this new interface, I'm going to go up to the top of the file and define it up there. The only reason I'm defining this at the top of the file is because I expect to probably end up using this interface several times throughout this file. And so I just want to very prominently display it right at the top. It's up here. I'm going to create a new interface called request with type, request with body. And like I said, we want this new interface to essentially extend or borrow all the different properties that have been defined on the request interface. So I'm going to say extends request like so. So this now means that the type interface of request with body is going to have all the same properties as request. And now inside the interface we're going to override or change that one single property of body. So I'm going to say that body is going to be some object with some unknown keys that are strings. And their values are always going to be a string or undefined. And that's pretty much it. So we've now created our own custom interface to define exactly what a request is when we expect it to have a body. And now we can use this new interface in all of our different request handlers whenever we expect a body. And it's going to force us to make sure that we check to see that any property that we look up on that body object is actually a string and not undefined. So to use this, we can now go back down to our request handler and we can update the type of this rec argument right here. So I'm going to say that this is going to now be request with body. And now if we mouse over wrecked body right here, you'll see the definition. From the interface we just put together. So rec dot body is going to be some object that has some unknown keys and the values are going to be strings or undefined. And the end result is that we end up seeing a air around this reference to email because we're trying to call it to uppercase on it, but email might be undefined and so we need to actually take care of that. So to solve this, we would write the same code that we saw just a moment ago. We just have to set up a very simple type guard. So I'll say if. Email. That's our type guard that's going to make sure that email is defined and then we can move our resend into the if statement like so. So if we now mouse over email and of the type card we will plainly see. Yep. At this point we definitely know that it is a string. Okay. So now we can also add on in case. We're going to say if email is not defined, well then let's send back some kind of air and just say you must provide an email. And once again, ideally, we would probably set back send back some appropriate status like a 422 to indicate that the record that the user just submitted, so the email and password were not valid and a 422 would be appropriate for that. Okay, so that's pretty much it. So now we can test this out. Now, remember when I saved this and I go to test it? We still have an incorrect input name inside of our template for the actual form. So at this point in time, when we try to submit the form, we will not have an email property because on the record body object there's going to be M and pay. So I'm going to expect that we fall into this case right here. All right. So I'm going to flip back over. I'm going to do a revisit to Local 3000. Log in. I'll enter in whatever email and password and then I'll see that error text. So if I now go back over. And update my template. So now we are submitting a object or a encoded string with the correct property names of email and password. Now I can save this. I'm going to go back to slash login. I'll enter my email and password and I'll see the capitalized email appear. Perfect. Okay. So that's going to be like a pretty ideal solution or I should say like kind of a low friction solution to dealing with this kind of poorly typed, wrecked body property. So this is a low friction solution because it doesn't really require us to kind of twist around, express too much. We didn't have to write a ton new code. All we did was we defined an interface with a more appropriate body property, and now we can use this interface throughout this entire route handler file. And if we ever create a second route handler file maybe to handle some other routes inside our application, well, we can move this interface definition into like a more centralized location and then use it throughout all of our different route handler files. Okay. So this, like I said, is a good example of how we can just kind of do the bare minimum with TypeScript. Right here, just do like the bare minimum with TypeScript and just add in some of these basic type annotations to fix things up and help TypeScript understand what is actually going on inside of our code. So let's continue on. We're going to implement the rest of this application now and then we're going to eventually come back and figure out some ideal solution or hopefully better solution, where maybe we're going to really tear apart some stuff on the express side and figure out how to really get the stuff working very nicely with TypeScript. Because right now this is I don't know, it's definitely a solution, but perhaps it's not the most ideal one because now we are essentially saying that every time we write out a request handler that expects to have a body property, you and I, as engineers, we have to remember to say that rec is going to be of type request with body. So I would not say that this is a perfect solution yet because it really requires us to remember that we have to write that out, which is not super ideal. Okay. So let's take a quick pause right here and continue in the next video.
