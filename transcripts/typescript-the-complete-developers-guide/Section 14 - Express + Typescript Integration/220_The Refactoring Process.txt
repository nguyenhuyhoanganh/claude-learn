# The Refactoring Process

In the last video, we started to discuss what it takes to get TypeScript and Express to work together. So remember just throwing code into classes, but we don't necessarily gain anything from it. So in our case, to actually make refactoring our application worth it, we're going to first stick a ton of our code into classes and then we're going to use some really advanced features of TypeScript to get some very high level of developer productivity. So in other words, we're going to try to significantly enhance the developer experience. So here's what we're going to do. I've got the documentation for a library that we looked at a while ago. Open right here. This library is called Express Decorators. You might recall we very briefly took a look at this documentation when we were talking about how we could use a adapter library of sorts that has a bunch of helpers for working with our library and typescript together. So what I want to do is essentially re implement some portions of this library inside of our project. So we're not going to use this library directly. We're going to instead re implement it. So that's going to require a lot of pretty hard work on our side. Now, to give you an idea of how we're going to do this, I want to first scroll down to the bottom of this documentation page and give you a quick example of exactly how this library works. All right. So I'm going to scroll down to a code snippet down here. If you're looking at this documentation, too, I'm looking at this section called Create your First Controller. So when we start to make use of this library, they introduce the idea of something called a controller that is essentially a class that has some number of root handlers tied to it. So here's the code for a controller. You'll notice right away that there's some syntax inside of here like that at Symbol right there that we have definitely not seen inside of TypeScript before. So that is something called a decorator. And you can see that these decorators are kind of spread around inside of this class. So in this particular case, the US Express Decorators project significantly enhanced developer productivity through the use of number one classes. And number two, this is the advanced feature of typescript decorators. So decorators are a feature in TypeScript. That is how this library really works and like I said, improves the developer experience. So I want to essentially re-implement a lot of these different decorators inside of our project. These developers are simply these decorators are going to give us a way better code structure than we have right now or I guess debatably would give us some better code. So let me tell you exactly how we're going to take this project. All right. So here's the general idea. Understanding decorators by themselves is pretty challenging. And if we tried to just dive right in and use decorators inside of our project, it would definitely be really, really hard to understand. So here's how we're going to cover decorators. We're going to first get a quick reminder on some basic JavaScript concepts that are really important for you to understand in order to understand decorators. After that, we're going to do a quick little side demonstration with decorators, and then we're going to come back to our project and refactor it to use these decorators in classes together. So that's the general idea. So let's take a quick pause right here in the next video, we're going to get a quick reminder of some basic JavaScript concepts. So I'll see you in just a minute.
