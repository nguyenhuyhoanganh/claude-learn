# Typescript with JS Libraries

After many, many, many, many, many sections, I think that we've now got a pretty solid basis or grasp of the fundamentals of TypeScript. So now in this video and these coming sections, we're going to start to focus on how we actually use TypeScript today. Like right now on current projects that you are already working on that are might already be written using basic JavaScript. So here's the thing. Throughout this entire course, I've shown you a very specific style of using TypeScript, really the intended goal of TypeScript, and that is to use classes and to make these classes work with each other primarily through the use of interfaces. Now, I got a quick question for you. How much JavaScript have you written that relies 100% on classes and getting those classes to work together with something like interfaces? Chances are not much. Maybe in the world of React, where we have class based components, you might say, Oh yeah, hey, I used to react with classes. Well, you did. But there's kind of a couple of caveats even there, where the React code that you're writing doesn't quite mesh that well with the world of TypeScript. So in this video and the next coming, I want to understand how we can take everything that we've learned so far and apply all these ideas to libraries that you are already working with right now. So here's the real big problem. TypeScript has a very distinct programming style. We are writing essentially object oriented programming code. We are making heavy use of classes, heavy use of inheritance and sometimes composition as well. The big issue is that a lot of these popular JavaScript libraries that were making use of were written way before JavaScript itself even had the idea of classes with is 2015 and remember classes in S 2015 aren't like conventional classes in the original programming sense. So a lot of these popular JavaScript libraries were written before it was popular to make use of classes at all. And really a prime example of this, what I'm really thinking about when I say that is the express framework on the Node.js side of things. So because of this kind of mismatch right here, sometimes integrating TypeScript into a popular JavaScript library or a project that uses a popular JavaScript library can really be challenging. And so that's going to be our big goal. How do we kind of mesh typescript together with these frameworks that we are already making use of? So in general, there's going to be three distinct solutions that we can use whenever we want to use TypeScript with some JavaScript library. So the first solution is to just use the library. Normally, we're not going to worry about creating classes in general or making use of interfaces or stuff like that. So we're just going to use the library normally and we'll add in maybe some like very basic type annotations wherever we can. So stuff to say like, Oh, this function is going to be called with a string or a number or an object with these given types. But we won't really go overboard with trying to create classes or meshing that library or twisting it to work nicely with TypeScript. In other words, we're just going to use TypeScript where it is convenient and otherwise we're just going to kind of fall back to whatever programming style that library demands. Now, the second option here is to try to use a TypeScript adapter library that someone might have already written to work with the library you are trying to use. So again, a prime example of this would be express. So I've got a page open right here, a package on AMPM called Express Decorators. So if you read the description here very quickly, it says essentially a TypeScript framework on top of express. So this is a pre-built library to work with Express and essentially get Express in TypeScript to work together as best as possible. If you take a look at some of the code inside of here and if, you know, express at all, you'll notice that this doesn't look very much like typical express code. Instead, we definitely have a class definition right here. We definitely don't have a lot of stuff that looks terribly familiar with Express. I mean, here's some middleware setup right here, but definitely you can see like a lot of methods, a lot of classes, all that kind of stuff that is very typical of TypeScript as opposed to express. So the second way that we can get TypeScript to work together with some library is to use some adapter library that might already exist out there. And then the final option is to twist the heck out of your library, whatever library you're trying to use, and make it work with TypeScript classes, that is the like most extreme option where we might be working with some library that doesn't really take any type of class based approach, and we're just going to really twist the heck out of it and make it fit and make it work with classes. So for you and I, for the most part, we're going to take approach number three down here. And the reason we're going to take this approach is that it is definitely the hardest. And so I think that it makes the most sense in the context of a course to show you how to do the hard stuff, because figuring out how to do this other stuff is generally going to be a lot easier in the case of using the library normally and just adding in basic types, I'm very confident you can figure that out. And in the case of using some adapter library, well, you've always got the documentation or that adapter library to fall back to. So you can always use like the documentation on express decorators to figure out exactly how this thing works. So in the context of this course, I think it makes by far the most sense to show you exactly how to essentially mangle a library and make it work with TypeScript classes. Okay. So with that in mind, we're going to take a quick pause right here in the next video. The first library we're going to focus on is Express. So quick pause and we're going to work on an Express project in the next video. So I'll see you in just a minute.
