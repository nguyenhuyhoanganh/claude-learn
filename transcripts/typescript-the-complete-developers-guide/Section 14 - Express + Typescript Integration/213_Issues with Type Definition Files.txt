# Issues with Type Definition Files

In the last video we spoke about two distinct reasons that getting TypeScript and these JavaScript libraries to work together can sometimes be challenging. So the first issue is that type definition files alone don't really express what is going on inside of a JavaScript function. It only declares that a JavaScript function might exist. So a big reason that this is a problem is middleware as a middleware function might be adding on properties or removing properties or otherwise changing them on request. Object in TypeScript has no idea that that is occurring. And so that can very easily lead to issues where we are trying to refer to properties inside of our TypeScript code that we think exist when in reality they might have been removed or we might be trying to refer to a property that we know has been added by a middleware. But TypeScript is saying, Hey, I don't think this object has that property because TypeScript just doesn't know what is going on inside that function. Now the second issue we discussed is that these type definition files to us are not always accurate. And a good example of that was the request interface. So whoever wrote that request interface made a really big assumption there, assuming that we are always using the body parts or middleware. And that by definition means that our request object is expected to have a body property. But if we aren't using that middleware, there is no body property. And that means that the type definition file would therefore be incorrect. So now the third reason we're going to discuss that getting TypeScript to work with Express is not that great sometimes is the fact that whenever we have some outside source of information being provided to our server, we don't know that that information is correctly formed. So in other words, when we receive a form submission, we might be assuming that it has some set of properties when in reality they might not have been provided. Now, a quick note here. When you read this, you might be saying, Stephen, what are you talking about? That's true of any program or any server, and that's correct. Any program that takes in outside information cannot assume that that outside source of information has all expected properties 100% true. My claim is that this problem is exacerbated. Or made worse by the type definition file. So we essentially have three issues here that all relate to type definition file. And it might be sounding like I'm just complaining about type definition files in general. Well, remember, the entire discussion here is about how to use TypeScript now with popular JavaScript frameworks. And the tool that we have to get TypeScript to work nicely with JavaScript frameworks is type definition files. So my whole goal here is to help you understand how these type definition files sometimes come up short. So we're going to see that our server has an issue accepting invalid inputs and this problem is not helped. We get no help from our type definition file when we definitely could make a tiny change to the type definition file and deal with this problem way, way better. So let me give you an example. Let me show you what I'm talking about. Okay. So I'm going to flip back over to my editor. Inside my index file, I'm going to uncomment the middleware right there. I'm then going to go back over to login routes. And I'm going to say, rather than sending back email plus password, I'm going to just try to send back the email in all capital letters like so. Okay. So we're very clearly making a huge assumption here. We are assuming that email is definitely going to be a string and therefore has access to the two uppercase property. Now, just so you know, the property names of email and password on body only. Ah, email and password, because that's the names that we put into our two inputs in the form. Any time we submit a form inside the browser, the browser is going to look inside the form and find all the different inputs. It's then going to look at these inputs and see if they have a name property. If they do, the browser is going to submit the value inside of this input and this input using those name properties. So in other words, email and password down here are only email and password because that's what they were on these inputs. So let's imagine for a second that we get a submission of some information with just a totally invalid form. Maybe it doesn't have any properties. Or maybe it has some incorrectly named properties. Like let's change email to just m and password to just pay. So let's now try to save this and now submit the form because we've changed these two names. There will definitely not be an email and password property on record body. So let's just see what happens. So I'll flip back over to my browser, I'm going to refresh the page, I'm going to enter in some email and some password and click on submit. And sure enough, I now get an error around that two uppercase call. There is no email property on the body object. Email is undefined. And so we are trying to call a function to uppercase on undefined to see what properties were actually submitted. We can open up our request log click on that request that was just issued. Go down to the bottom and click on View Source. So that is the actual body of the request that was sent inside that HTTP request. So the actual property names were M and PR. Now once again, this is a problem that all servers deal with. All servers have to make sure that the correct properties actually are provided. My issue here is that right now, TypeScript gave us no indication that there might be an issue with this code. And that's what I really want from TypeScript. I want TypeScript to tell me when something might not be right. And so ideally, I would want TypeScript to understand that rect body is going to contain an object that might or might not have the properties that I expect. So let's investigate a tiny change that we can make to the type definition file that would make this entire issue not a problem whatsoever. So I'm going to again, command click on body right there. And it's going to take us back to the definition of body on the request object that we were just looking at in the last video. So right now, the type definition file is saying that body is going to be any and that's not super helpful for us. Remember, whenever we have a type of any generally a indication that something is not quite right. So I'm going to change the type definition file here. Normally, we never make a change manually to a type definition file because this is technically a dependency. But I just want to give you an example of how a better type definition file could have solved this entire issue. So I'm going to say that the body property is instead going to be an object that is going to have some keys of unknown value. Like, I don't know what the different keys are inside this object because we could be getting any keys, right? We just made a post request with like M and PA and we could easily change our form and change these keys again. So we have no idea what keys are going to be on this object. So to express that, we would use that syntax we've seen a couple of times before. We're just going to say this object is going to have some keys that are strings. We don't know what they are, but they're definitely going to exist. So then, as a value for all these different keys, I'm going to put in string or undefined. And that right there is the key. So now we are telling TypeScript that we're going to eventually try to access some property on this object and its value will be either string or undefined. We have no idea it's going to be one or the other. So if I now save this file and remember in general, we do not modify type definition files. I'm just trying to prove a point right now. And go back over to logging roots. You'll now see that we've got an error around email and it tells us the exact error that I would want to see. It says, Hey, email is going to be either a string or undefined. You better handle this because if it is undefined, you're going to get an error out of this line. So to fix this, we would have to set up a type guard. Remember, a type guard. We would do like if email. And then inside there we can make the assumption that email is now definitely a string. So now this pattern right here would essentially force us to deal with this issue by having a better type definition of what that body property is going to be. I've now solved an issue thanks to TypeScript that I would otherwise definitely have if anyone ever submitted an invalid form. And so now it's like really fix this up. We would probably put on like an LZ case and maybe send back resend. You must provide an email property or something like that. And so this is a great example of how TypeScript can help us as opposed to just being like normal JavaScript that we'd write on any given day. So if I now save this. I can go back over to my browser. I'm going to go back to Local 3000. Local 3000 log in. And now if I submit an email and a password, I get a much more reasonable message than the server error that we saw before. And again, this is all thanks to that type definition file. So again, it might sound like I'm just railing and complaining about type definition of files. Well, maybe I am. But the whole point here is that type definition files are how we get TypeScript to work with JavaScript. And so if we work with a not great type definition file, we're going to run into problems. Again. In general, we do not modify type definition files. So you might be thinking that the solution that I just showed you is not appropriate, and that's 100% true. I would not in any way, shape or form ever modify the body type right here. So I'm going to change it back to any. Again, I just want to show you how we could have fixed this problem. If the type definition file was better. Of course. Okay. So clearly that's not a solution. So let's take a quick pause right here. We're going to come back in the next video, and I'm going to show you a real solution that we can use if our type definition file is basically just like not up to par, which is definitely the case here. Now, before we pause, I'm going to make sure I change my code back to how it was before so we can make sure that we actually handle the case in which email is undefined. So quick pause. Come back the next video. Show you a better solution to this problem.
