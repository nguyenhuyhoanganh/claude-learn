# Oops, My Bad

Oops. In the last video, I said that there was something going wrong. There's nothing going wrong. We don't get any output at our terminal when we are using the HTML report. My mistake. Okay. So just one more quick example of these static methods. Let's try doing the same thing for the match reader as well. So right now our match reader only ever gets used with the CSV file reader. So let's add a static method to match reader and just say, hey, this thing is always going to be created with a CSV file reader. So to make that happen, I'm going to open up my match reader file. Here it is right here at the top. I'm going to import. CSV file reader. From the CSV file reader file. And then I can add on a static method. So I'll say static. And how about for this name? Here again, we could do something really descriptive. Well, from CSV, I think that that makes sense. Now we'll make sure that we pass in the name of the CSV as an argument. So I'll pass in file name as string and this thing is going to return an instance of match reader. So then inside of here, we can return a new match reader. It gets pre-configured with a CSV file reader. Let's get that new keyword and then make sure that we pass in the file name to the CSV file reader. Here we go. Okay, so we can save this and then back inside of indexes rather than writing out all the stuff you see right here. I'm going to delete those comments. I'm going to delete the declaration for CSV file reader. I'm not going to call a new match reader anymore. Instead, I'm going to try to call that static method that we just put on the class. So I'm going to say match reader dot from CSV and then I'll pass in the name of the CSV file. So football dot csv. Okay. So now we can save this again. And this time I learned my lesson. No output at the terminal. Yep, that's right. We get the report html file here. So if you really want to test, make sure that's still being generated correctly, I could delete that file and then just make, like, an arbitrary change to this thing. Save it. And I should see the file regenerated again. Perfect. So now the real benefit to this approach. Look at how many import statements we no longer need inside of here. So we can now delete the import for HTML report. We can delete all those other imports as well, and we're left with just match reader and summary. All right. So that looks pretty good. In fact, you know, if we really want to condense this a little bit more or make a little bit nicer on the I, I might move the summary declaration next to the other declaration and then put those two commands next to each other. Wow. That's a big improvement. Okay. So that's the last thing I want to show you. Just those static class methods. Again, we can use those to return in Pre-configured instance of the class. We don't have to use static class methods for returning an instance of the class. We can use these for anything, but in general using them to kind of generate a pre-configured instance of match reader. That's definitely a good place to get started with them. Okay, so let's take a quick pause right here. I swear that's the last thing. So we'll do a quick review of everything we accomplished here in the next video.
