# Type Assertions

In the last video, we started going through our row of strings and we're converting each value inside there into some other appropriate type. We left off on the match result value right here. So we currently have a string which is good, but we need to tell TypeScript that this string is going to be one of the possible values from our match result enum. In order to do that, we need to get access to the match result enum or that type inside of our CSV file reader. Currently that enum is defined inside of our index file. So we could export it from this file and then import it inside of CSV file reader. But usually by convention we do not export anything out of our indexed file. So to fix this, I think we should move the enum definition into a new separate file. We can then import this enum into any other file inside of our project. Let's do that refactor right now. Inside of CRC, I'm going to make a new file called Match Results. I'll then go back over to index points. I'm going to cut the enum out of here. And then I'll paste it into match results. And finally, I'll make sure that I export it from here as well. So now we can import this enum into both index notes and CSV file reader as well. Over inside of indexes. I will import match result. From match result. And then I'll do the same thing inside of CSV file reader as well. So at the very top I will import match result. Rum match result. So now finally we can go back down to where where we are writing all this road conversion logic. And we're going to take row at five, which you will recall is the match result. So like this string right here. So what we are trying to do right now is tell TypeScript that this string is going to be one of the possible values inside of the match result. Enum. To do so we're going to find row at five right here and then say as match result. So this is what is known as a type assertion. A type assertion is where you and I, as the developers, are trying to override type scripts, default behavior or kind of tell TypeScript that, hey, we know what is going on here. So at present, TypeScript only knows that row at five is a string. We need to override that behavior and we need to tell TypeScript, hey, believe us, trust us. We're the developers. We know what's going on here. We want you, TypeScript, to consider row at five as one of the possible values out of that enum. So now TypeScript knows that that index right there is going to be H, a or D. We're going to see a lot of type assertions later on inside this course. So for right now, we'll just kind of leave that as is. As a quick aside, I do want to mention that you can use type assertions with any kind of type. It's not limited just to being used with enemies. Like I said, we'll see some more kind of usage of type assertions later on as well. Okay. So that looks good. So now the very last value we have to work with is the referee, which we're just going to leave as a string. All right. So the very last value inside of here will be row at six. Like so. And that's pretty much it. So we are taking in the array of strings and then we are doing some conversion process, sticking them all into an array and then returning it. So now the only big issue we have left inside here is that on this mapping function, we annotated its type as any, which is definitely not appropriate. So let's take a quick pause right here. We're going to come back to the next video and discuss how we're going to update this type annotation right here to some more appropriate type. So I'll see you in just a minute.
