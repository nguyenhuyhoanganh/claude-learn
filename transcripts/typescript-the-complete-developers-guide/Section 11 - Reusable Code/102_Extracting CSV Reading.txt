# Extracting CSV Reading

Now that we have a better understanding of our enemies work and we've solved the issue of comparison with some mystery strings, let's move on to the next big issue with our program. So our next big issue is that our source of data is hard coded. What do I mean by that? Well, if you go back over to our index file at the very top, we've got that logic to open up the CSV file and parse all the data inside of it. So I want you to imagine for a second that we decided to get our football data from some outside API rather than the CSV file. If that were the case, we would have to come into our indexed file and comment or otherwise delete all that CSV related stuff and then replace it with some logic to make a network request to get some data and then operate on that instead. So when I say that our source of data is hardcoded, I mean to say that if we wanted to ever change our source of information to our program, we would have to make a very significant change to the structure of our program. We would delete like over half of our source code or otherwise commented out. So in this video, we're going to figure out some good way to somehow extract this logic and somewhat make it at least somewhat reusable. The logic we have right now to open up the CSV file is actually a common logic that we could use on any type of CSV file. So we can take any type of CSV file and essentially beat it through these lines of code right here and we'll get some representation of the data inside of that CSV file. And so I think that beyond the fact that we don't want to delete some code that we spent a lot of time writing, well, this actually is some pretty good code right here that we could easily re-use on a future project. So how are we going to extract this? And number one, keep us from having to delete it if we decide to have an alternate source of information. And number two, make it available on future projects as well. Well, here's my idea. I think that we should create a separate class called a CSV file reader. The CSV file reader class is named such because it's going to literally open up a CSV file and read it. So the word reader right here indicates that this object is going to attempt to read or capture some information. I think that this class could have two fields. One field would be maybe the file name or the name of the CSV file that we want to open. And then the second field where it maybe be like some data variable that would eventually be all the parsed information out of the CSV file. So data would start off empty when we first create the class and then once we actually open up the file and capture some information from it, well then assign all that information to data. And then finally to actually open up that file and parse all the information inside, we should create maybe like a read method. So any time we call read, we will actually open up the file, grab all the information out of it, parse it all, and then assign it over to data. So if we create some class like this, then we could very easily reuse it in the future. And in addition, if we create a class like this inside of a separate file, then we can easily decide to change the source of information inside of our program without deleting all that very valuable CSV reading code that we already put together. So let's give this a shot. Let's try creating a separate class inside of a separate file that's going to have these fields and that read method as well. All right, so to get started inside my SRC directory, I will make a new file called CSV file reader. Then inside of here, I'm going to create a class and immediately export it. So I'll export class. CSV file reader. Now, like we just said, I think this thing is going to have to fields one is going to be our file name, the other is going to be the actual reference to the data that we captured out of the CSV file. So I'm going to first initialize data. I'll say data is going to be a two dimensional array of strings and I'm going to initialize its value right here in line and say it's going to start off as an empty array. So eventually we will throw some actual data inside of here. Now for the file name. We could initialize that right here, but instead we probably want to pass in the file name as an option to our class. So chances are if we ever use our CSV file reader class on a future project, we will probably not always be opening up something called football CSV. Instead, we should make the file name an option so we can customize it and use our class to open up different CSV files. So within mind, rather than declaring this property right here, I'm going to instead define the constructor and we're going to receive. File name as an argument to our constructor. Now, notice I'm using the public modifier right here. All that's doing is automatically defining a property on our class called file name. And so whatever we pass in as the first argument when we create a CSV file, reader will be assigned to a file name property on our object instance. I don't think we actually need any logic inside the constructor, so I'm going to shorten up those curly braces to just be on the same line like so. All right, so it looks good. So now last thing we will do is put together a read method. And this is going to be responsible for actually opening up that file, parsing all the information and so on. So I'm going to define a new method called Read. I don't think it's going to return anything at all. So I'll give it a return type of void. And then inside of here we want to open up that file and so on. So I think it's going to be this exact same logic that we already have inside of indexes. So inside of here, I'm going to copy and paste all that stuff from FS, read, file, sync, all the way down to the bottom of our map statement. So I'm going to cut all that. I'm going to go back over to my read method and then I'll say this data is going to be the result of all that logic. And then finally we need to make sure that we import the FFS module at the top of this file. So I will import. PFS from PFS at the top. And. Okay, I think that's it. Everything inside of here looks good. So let's now try to use out this brand new class right away. Back inside of indexes. I don't need to import FS anymore. Instead I'm going to import CSV file reader. From. CSV file reader. Then rather than declaring a variable called matches right here, I'm going to declare a variable called about reader and this is going to be a new CSV file reader. Don't forget, we have to pass in the name of the CSV file. We want to open as an argument to the constructor. So I'll say football csv. And by the way, that reminds me, I think I made a little typo inside the class back inside of our class definition right here when we call it read file sync, we're still hardcoded to open up football CSV. So rather than using that hardcoded string, let's instead use that property that we're now passing in. So I will replace football CSV with this dot file name. That's better. Okay. So now back over inside of indexes after creating our reader well then call reader read to actually open up that file, read all the information and then assign it to the data property. And so now finally down here where we try to iterate over our matches, we can instead iterate over reader dot data like so. All right, so I'll save this file. I'm looking at my file folder. Explorer over here. It looks like I don't have any errors, and so I'm ready for a quick test. I'm going to flip back over to my terminal and just make sure that I can still see that man United has won 18 games. Yep. Still works. All right, so this is definitely an improvement. So now if we ever decided to use an alternate source of information inside of our program, like, say, some API, we would not have to delete all that very valuable logic around opening up that file and then parsing all that information. Instead, we would only have to delete or otherwise comment out the line of code that created an instance of the file reader and the actual read method. That's it. Deleting these two lines right here is way more palatable. Definitely way more preferable than deleting all of this rather customized logic right here. So we would just have to delete these two lines and replace it with some other code to make some API request to fetch our data. In addition, we've now also got a class that we could very easily use on future projects as well to open up and read data out of CSV files. And I really mean that like you could use this class right here on your own projects in the future to open up CSV files. So I think there's a huge benefit here in extracting that CSV reading logic into a separate class. All right. So I'd say this is definitely an improvement. So let's take a quick pause right here and move on to our next issue in the next video.
