# App Wrapup

All right, my friends, I know we spent quite a bit of time on this project, but I just want to do a quick recap and discuss some of the really important topics we covered. So first off, insight of match results. We created an enum. Remember, enemies describe a set of values that are all closely related. Enemies don't really carry a lot of intrinsic value inside them. Their real goal is just to communicate to other engineers that, Hey, here's a set of values that are all closely related. We also saw a very practical example of using tuples, so we created a tuple to describe a single row of a CSV file. Remember when we made use of the tuple? There was kind of two separate steps going on. On the one hand, we had to actually convert the string, so we're coming in from that CSV file to the appropriate type. Once all those values were in the appropriate type, we could then describe the entire row using the tuple. So just creating the tuple didn't actually convert any values for us. We had to do the conversion manually. Making the tuple just allows us to describe the ordered types inside of the array looking thing. I don't want to use the word tuple again. The next big thing we saw was inside of the inheritance directory. So remember, these two files are kind of backups right now. We created the abstract class CSV file reader and we made this thing a generic class. We only spoke about generics very briefly, so we're still going to do a lot more with them inside this course. We used a generic here because we wanted to configure the types inside this class definition on the fly. Remember, you can think of this generic definition as being like a function for types. So any time we reference CSV file reader, like any time we have to pass in essentially an argument for t, remember. T. There's nothing special about calling this thing. T Really? You can imagine this thing is instead called like type or data. But convention is to just call it. So that's how you can imagine it. But all the documentation, all the blog posts you're going to see, there's going to call it. RT as a shortcut. We eventually made use of this abstract class inside of our match reader inside the inheritance directory. So inside of here we extended CSV file reader and notice that's how we provided eventually a type to our generic. And we eventually decided that, hey, you know, this approach was good, but we want to try out something different than just inheritance. And so that's when we started looking at composition. And so we had several different examples of composition going on inside this project. First off, we had our. Matt Schrader. Here it is right here. So the match reader, we said we're going to provide a data reader to this thing when we create it. So the data reader was going to be the source of information. So data reader could have been coming from CSV file or an outside API or even like direct user input, whatever we want it to be. So match readers job was to load up some data. It delegated the loading of actual data and match reader only worried about providing an implementation or converting that incoming data into the match data tuple that we just discussed. The other big example of composition that we saw was inside of summary notes, and we just did a ton of stuff around this file. So I don't think I really need to repeat anything inside of here because you probably just heard me talk about this several times over. Okay. So this was a really awesome project to highlight some benefits of composition. And even though it was a really long project, I hope you enjoyed getting a very practical comparison between inheritance and composition. So let's take a quick pause right here. We still have got a lot of work to do, so I'll see you in just a minute.
