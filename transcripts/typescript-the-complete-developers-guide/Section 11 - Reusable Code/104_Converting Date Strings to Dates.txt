# Converting Date Strings to Dates

In this video, we're going to work on a new function called Parse State. The goal is function is going to be to take that string with the day, month and then year and then return an actual date object with it. Before we start writing any code, I want to give you a quick review on how we create a date object in JavaScript in general. So I've got my JavaScript terminal up here. I can call New Date when I do. So we can pass in some arguments to customize what day, month and year this date object is going to point to. So the first argument inside this constructor can be the year that we want to set the date object to. So for example, I can pass in 2020 and that should create a new date object that's going to point at the year 2020. As a second argument we can put in the month that we want the state object to point to for the month we have to do it, use it zero indexed. So in other words, January is reflected as zero. February is one and so on. So I could put in say zero right here. And that will point to January 2020. And then the third argument is going to be the day. So if I want to do like January 1st, 2020, I would put in 2020 zero and then one where I could do like 15. And that will look at January 15th. So in effect, all we have to do here to create this date object is split apart. The string right here extract the month, day and year. We're going to subtract the month by one, and then we're going to feed all those different values into the date constructor. And that's pretty much it. And to create this part state function, we're going to make a new, separate file to store this code. This code that's going to convert that date doesn't really have anything to do with anything else inside of our current application. So I'm going to put it inside of a new file that I'm just going to call utilities. Utils being short for utilities. So then inside of here, I'm going to create an export, a new function called date string to date. And then I'm going to annotate this thing. The first argument is going to be something I'll call date string and it's going to be of type string. And then this overall function is going to return an object of type date. All right. So then inside of here, we're first going to take that date string and split it apart based on those forward slashes. So to do so, we'll write out something like. About date parts is date string. Dot split on Ford like so. So let's imagine for a second that the input to our function is something like, oh, about let's use one of the actual dates rather than making one up. So how about right here we got 28, ten, 2018. So we've got 28. Ten 2018. Does that means that the output from this line right here will be an array of strings with 28, ten and 2018? These strings are not super useful to us because when we use that date constructor, we have to pass in actual numbers, not strings. So we now have to iterate through the array. We get back, take each of those strings and turn them into the respective numbers that they represent. So to do that, I'm going to just chain on a simple map statement right here. So this map statement is going to be coupled with something else called value as just an arbitrary argument name here, and that's going to be a string. And then our goal is to return a number. I'm going to zoom out for just a second, actually. How about better than zooming out? Let's just new line this statement so we can get everything a little bit more legible like so. All right. So now inside the map statement we can return pass int with a value like so. So Parson is a function built into JavaScript and therefore TypeScript as well. It takes in a string and returns a number out of it. So now date parts is going to be an array of numbers. The first element will be the day, then the month, then the year. So we can now pass each of those into a date constructor and return it. Sol then return a new date. I'm going to first put in date parts at two because we want our year first. Then we'll put in the month, which will be date parts at one minus one. Remember, the minus one is on here because our month inside the date constructor is zero indexed. So January is supposed to be zero. And then finally state parts at zero. That's our day. All right. And that's pretty much it. Okay. So let's save this file right here. When we come back in the next video, we're going to be able to make use of this date string to date, function to convert. That date string into an actual date and will also handle conversion of a couple of other elements inside of each row as well. So a quick pause and I'll see you in just a minute.
