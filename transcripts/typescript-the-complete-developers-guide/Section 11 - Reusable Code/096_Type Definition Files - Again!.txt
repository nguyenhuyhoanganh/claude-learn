# Type Definition Files - Again!

In the section right before this one, I put a football CSV file, so this has all the raw soccer or football data inside of it. Remember, each row represents one single match. Please make sure that you download this file and put it into your root project directory so you can see right now it's inside my root directory. It is not inside Z, it is not inside build. It's in the root project folder. Okay. So now we've got this file, we can start to think about exactly what we're going to do with it. Remember, our goal here is to take all the information in that file, load it into our application, then parse it, analyze it, and generate some kind of report. So in this video, we're going to figure out how to load up information out of that file using the Node.js Standard Library. Let's take a look at the documentation for Node.js and understand how to open up and read the contents of a file. You can find the documentation for Node.js at this link right here. So I'm going to copy that link and open it up inside of a new browser tab. On the left hand side, you'll see all the different modules that are available inside the Node Standard Library. I'm going to scroll down just a little bit and find a section called File System right here. So in the file system module we can then scroll down a pretty good amount. We are looking for a function called FS, read file sync. So here it is right here. Read file sync. So I'll take a look at the documentation for that. We can call, read, file, sync, and then pass in the name of the file that we want to open. So in this case, it would be the football CSV file. When we call it Reed File Sync, it's then going to return back to us the entire contents of that file as a plain string. And so we're going to essentially get back one big string with all of our data inside of it, like one big string with all this information. So obviously we're probably not going to work with want to work with this information as a big string. And so that's where we're going to have to do some kind of like parsing logic or something like that to take that big string and put it into a much more usable data structure. But we'll figure out how to do that in just a little bit. All right. So now that we understand how to load up the contents of a file, let's flip back over to our editor and get started. Inside of my index file, we first have to import the PFS module from the Node Standard Library. To do so, we can write out import FS from FS like so. Now right away we see a nasty little error message here. So it says, okay, PFS is declared but never read. That's fine because we're going to use the library in just a second. But more importantly, we see an error that says cannot find module FFS. So where is this coming from? Just to be clear, this FS thing right here stands for file system. This is the module in the Node Standard Library that we are just looking at. In a typical Node.js application, we can write out a required statement for the FS module without having to do any like pre installation step of the FS module or anything like that. And so it seems like this air right here is a little bit out of place and I don't quite understand why we are seeing it. Well, it turns out we know exactly why we are seeing this air. The issue is that TypeScript is reporting the error incorrectly to us. Let's take a look at a diagram and I'll tell you exactly what I mean by that. All right. So as a quick reminder, we spoke a little bit ago in a previous application about type definition files. Remember, when we work with TypeScript, we can freely work with different JavaScript libraries. The only requirement is that we have to install something called that type definition file. The type definition files tell TypeScript about all the different objects, properties, functions, and function arguments and return values inside of these JavaScript libraries. So any time we want to work with a third party JavaScript library, we have to install that type definition file unless it is included by default with the library itself. So it turns out the same is true of Node.js as well. Whenever we work with Node.js from TypeScript, we have to install a type definition file. The type definition file tells TypeScript about all the different modules inside the node standard library, all the different properties they have, all the different functions and all those functions signatures as well. So you might think that TypeScript should ship with support for Node out of the box, or at least these type definition files. Unfortunately, no, not the case. Any time we want to make use of the node standard library, we have to install that type definition of file. Now, one thing to point out here. Normally when we install a type definition file, we use that formula that looks something like. Let me pull up my editor here really quickly. Normally we did like an NPM install with something it looks like at types and then like the name of library, right. And so in this case, you might be thinking that we would do an NPM install at types slash FFS because that is the name of the module we are trying to pull in. Well, no, not quite. Lots of big surprises here. Whenever we want to get the type definition file for any node module, like anything inside the standard library, we're always going to install these same type definition file. So let's flip back over to our terminal. We're going to install this file and you'll figure out what the name of it is. All right, so back inside my terminal, I'm going to start my running development process here with a control C and then to install that type definition file will do NPM install at types slash node like so. So anytime we are using anything inside the node standard library, we are always going to install at types slash node. That's pretty much it. All right, so I'll run that. I'm then going to start up my amp, start script again. I'll then flip back over to my editor, and now you'll notice that error message has gone away. So, like I said, this is another issue around type definition files. The thing that is really unfortunate here is that if you recall back when we were working with type definition files before TypeScript gave us a very clear error message, it said, Hey, make sure you install a type definition file, but when we make use of a node module or anything out of the node standard library, it instead just says Hey cannot find module FS, which is super not helpful at all. Nonetheless, that's how it works. So just remember, any time you use a built in node standard library module, you have to install that type definition file, and that's pretty much it. All right. So let's take a quick pause right here. When we come back to the next video. We're going to finish writing out our code to open up our football CSV file and read the contents of it.
