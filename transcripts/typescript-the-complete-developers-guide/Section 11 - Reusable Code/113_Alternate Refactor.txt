# Alternate Refactor

In the last video we finished up our first refactor of CSV file reader, so we achieved code reuse here by making use of an abstract class and generics at the same time. This was definitely a pretty tricky implementation. I think it definitely works, but yeah, it's definitely kind of confusing to understand what's going on. So in this video, we're going to start to refactor our CSV file reader once more. We're going to use a very different approach and I think in some regards is easier to understand and in other regards is a little bit more challenging. And this is why I wanted to show you both approaches. So to get started, we're first going to do a little bit of work on our code base. I want to keep a copy of this file CSV file reader around along with the match reader that we created as well because we're going to do a direct compare and contrast between both these approaches at the end of this application. So to keep a reference to both these different files, I'm going to create a new project or simply a new directory inside of CRC called Inheritance. Because that's essentially what we use here to solve this whole big issue and then going to drag and drop CSV file a reader into that directory. And if it asks me if I want to update any references. No, I don't want it to do any automatic updates. And then I'm going to drag and drop match reader inside there as well. I'm going to move again and I'm going to say, no, do not update any imports. All right, so now I've got a backup of both those files inside of inheritance, and now I'm going to go back to the backup of CSV file reader that we created a while ago and I'm going to change the extension on there from dot back to Dots. Okay. So now we're essentially back where we were several videos ago before we started this refactor. So we've now essentially got a clean slate. So now that we've restored our project, let's take a look at a diagram to help you understand what the second refactor we're going to do is going to look like. Okay. Here we go. So with this approach, we're going to lean heavily upon interfaces. We're still going to create a class called Match Reader, but it's going to have a very different relationship to the CSV file reader. So before CSV file, the reader was a parent class and we extended match reader using it. This time around, Match Reader is going to essentially stand on its own. It's going to be its own separate little class. Matt Schraeder is going to have a property that we will call Reeder, and it's going to be a type data reader. Data reader is going to be the interface you see up here. So remember when we make an interface, we are creating a new type and other objects inside of our application can opt in to satisfying this interface by implementing the same properties with the same exact types. So data reader is going to say, okay, to be a data reader, you must have a read function that returns nothing and a data property that is a two dimensional array of strings. You'll notice that those two properties right there look very similar to the properties that we have on CSV file reader. So CSV file reader is going to be essentially a data reader that can work with that data reader interface. So the idea here is that we might have eventually several different readers inside of our application, like this one is called CSV File Reader, but maybe we've got another one at some future point in time called like network. Reader. Or how about API reader? So maybe this class CSV file reader gets information out of a CSV file, but API reader can get information from some remote API. Both of them satisfy the data reader interface so we can assign either of them to this reader property. So then on Match Reader, when we eventually want to load up data about all of our different matches, we can call some function on there called like load or something. The load function is going to look at the reader and it's going to call the read method on it. After completing the read method, we can then access all the data that has been loaded on that data property and we'll do the actual data conversion process where remember, we turn the date into an actual date and those numbers or those strings into numbers and so on. We can do all that inside of this load method. Okay. So that's the general idea. So let's take a quick pause. We're going to come back to the next video and start implementing this solution.
