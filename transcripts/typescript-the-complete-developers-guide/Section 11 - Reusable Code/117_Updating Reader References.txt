# Updating Reader References

The last thing we have to do is make sure that our indexed file attempts to create an instance of match reader. Now, the important thing to keep in mind here is that when we create an instance of match reader, we have to pass in some value that satisfies the data reader interface. And so right now we only have one class that satisfies that interface. It is the CSV file reader. So that means that inside of our indexed file we are creating an instance of match reader and CSV file reader as well. Okay. So that might. Let's flip back over to our indexed file and we're going to do a couple updates inside of here. So first off, I'm going to delete these two lines right here because if we try to update these in place, things are going to be just a little bit confusing. I'm then going to import the CSV file reader at the top. Like so. And now we can actually make use of this stuff. So the first thing we need to do, once again, create an object that satisfies the. Data reader interface. So that's step one. And then step two is going to be to create an instance of match reader and pass in something. Satisfying the data reader interface. So again, it's a two step process. Now, before we were just creating one class, now we are creating two separate classes. And the idea here is that by creating two separate classes, we can kind of customize where the match reader is getting its information from on the fly. All right, so let's get to it. First, create the object that satisfies the data reader interface that is CSV file reader. So we'll say const csv file reader. Notice the lowercase characters. There is going to be a new CSV file reader and we once again still have to pass in the name of the CSV file we want to open, which is football. CSV. So now that we've created our source of information, we can now create the actual match reader. So we'll say const match reader. His new match reader. And we're going to pass in to the constructor the source of information to use, which is the CSV file reader. So I'll pass in CSV file reader. Okay. So we've now got the match reader. But remember, just creating it is not enough. Once we create it, we then have to call the load method. The load method is what's going to actually tell the reader to get all of its data and then transform all that data and assign it to the matches property. So now we're going to call Match Reader. Scott load like so. And then finally now anywhere beneath this we can now reference match reader. DOT matches, and that's going to be the information that we want to actually iterate over for our analysis. And so that means down here inside of our for loop, we are no longer iterating over readers data. Instead, we are iterating over match reader matches. So we'll update update that to match reader matches like so. Okay. So let's save this file. I'm going to flip back over to my terminal. And we should still see. Man, United won 18 games. Awesome. Now, if you don't see anything here, just hit control, see, and restart that process. Remember, you can restart it with an RPM start and you should see Man United won 18 games like so. Out. There we go. All right. Perfect. Okay. So let's take a quick pause right here. And we're going to do a big retrospection on the two factors we've now gone through. So quick pause and I'll see you in just a minute.
