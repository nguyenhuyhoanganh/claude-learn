# Building the Reporter

In this video, we're going to start implementing one of our report classes. So remember, these things need to satisfy the output target interface. Their general goal is to take the report that we generated during our analysis and then just send it to that target. So like in this case, maybe the console. So we would do a console log if we had a HTML report class. Well, it would probably generate some HTML file or something like that. So let's get started on generating first our console report class. All right. So back inside, my editor, I'm going to make another new directory inside of CRC called Report. Targets. And then inside this directory, I'll make a new file called Console Reports. So I'm going to use that same importing the interface thing that we used just a moment ago. Remember, we just imported an interface into Win's analysis just to help us implement that interface inside the file. Again, not required. It just makes life a little bit easier. So in this case, the interface we care about is defined inside of summary. We want to import the output target interface so that we make sure that we import it or implement it correctly. So inside of council report I'm going to import. Output target. Rum up one directory. And then we're going to get our summary file. So then inside of here, we can do an export class console report. Implements that interface. And so, again, we are told right away. Okay. Hey, TypeScript understands you're trying to implement this interface. Well, right now you're doing a pretty bad job of it. You need to make sure you implement the property print. So let's do that right now. We're going to implement print. If we go back over to our interface definition, we will see that it's supposed to take in a string and return nothing. So this is going to take in a string and return nothing. So like we said in the case of Council Report, well, this is pretty much just going to take the report and console.log it so we can just do console.log report and that's it. That's the entire implementation. So I think you can agree with me that this case well, this is a really simple and straightforward class nonetheless. Hey, it's a valid use. We will do the HTML report as well. So you can see one that's just slightly more complicated. But to be honest, at the end of the day, it's going to look very similar to what you see right here. Okay. So we've now got something to actually do the analysis. We've got something to actually run the report. Now, last thing we have to do is make sure that we actually put in some implementation inside of our class summary. So that needs to have a build and report method that's going to take in all that match data it needs to take that match data and pump it into the analyzer. Remember the analyzer has the run method that needs that match data to run correctly. The analyzer is then going to return a string. To use as the report. So then build and print method will take that string and toss it off to our report or our output target, I should say. So let's now implement, build and print report inside of class summary. So back inside of summary. Underneath the constructor I will define, build and print report. This thing needs to take in our list of matches, which is going to be an array of match data tuples, and it's not going to return anything. So then inside of here, we're going to take a look at the analyzer we were provided, which is available as this analyzer. And on that thing, we're going to call a run and pass in the list of matches that we were given to this method. So now Run is going to give us back a string, and that's going to be the actual report that we now want to somehow give to the user. So I'm going to save that report. Or maybe I could call it report or output. I don't know either one. I'll call it output here. And then with that output, we can now hand that off to our output target and have it be reported to some given target like the console or HTML file or whatever else. So to access that we would do this dot output target. This dot output target will call the print method and we're going to pass in that output from the analysis. And that's pretty much it. So again, the big thing to keep in mind here, this is like a perfect example of real composition. Our class summary doesn't really have a lot of behavior to it, like really just basically nothing. Instead, it has references to other objects. Whenever we send a command or call a method on summary, summary is not really responsible for doing anything. Instead, it is going to attempt to use these different objects that it has references to to actually do the heavy lifting. So summary is really like a coordinator of sorts. And the great thing about this approach is that we can freely swap out the analyzer or the output target and get different outputs of information or generate different kinds of reports. Okay. So this looks pretty great. This is a really strong structure here, a really strong pattern. So now the last thing we need to do is actually get this thing running inside of our index file. So let's take a quick pause right here and we'll fix everything up inside of indexes in the next video.
