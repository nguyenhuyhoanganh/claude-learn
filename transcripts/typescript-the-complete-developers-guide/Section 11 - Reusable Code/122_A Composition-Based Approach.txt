# A Composition-Based Approach

All right. In this video, we're going to start doing a big refactor once again. So we're going to add class summary, add some interfaces, add some other classes. You get the idea. One thing I want you to notice right away, however, is that just about every one of these different things has a reference to that match data tuple type. Right now, our match data tuple is defined inside. Of our match reader file. So here's where we define that tuple. We're definitely going to have to refer to this tuple type in other locations inside of our project. And so I think just to make life a little bit easier, we might want to move this type definition into its own separate file so we can very easily import it. And all these other files won't have a dependency upon Match Reader. So let's try that out right now. So I'm going to create a new file inside of my Z directory called Match Data. It's. And then inside of my match reader, I'm going to cut the match data type. And paste it inside of match data. I'm going to make sure I export this type. And then you'll notice this thing also has a reference to match result. So of course we need to make sure we import match result into here as well. We'll import match result. Firm match result, and then I'll save this file. Okay, that looks good. So now everywhere where we need to make use of match data, we can just import this type from this file. We need to make sure we do that right away because we just remove that definition from match. Reader So I'm going to go back over to Match Reader at the top, I will import. Match data. From match data. Well. Okay. So now we can start with the actual different classes we need to build. So we're going to first start off with class summary. So I'm going to make a new file. Inside of CRC again called summary. It's. Inside of here, we're going to define two interfaces and one class. Let's start with the interfaces first. So the first interface we need to define is analyzer. So I will do an export interface analyzer. One thing I put in that export keyword, we don't actually have to export an interface. Remember, other objects inside of TypeScript can implicitly satisfy an interface. However, in this case, remember, we can optionally use an interface definition to help us actually satisfy that interface when we write out a class. And that's what we're going to do this time around. To do that, we need to export the interface. I know that probably sounds really confusing. I said it all very quickly, so let's just throw on export for right now. Later on in just a little bit, when we start putting together these classes on the right hand side, I'll show you exactly why we are exporting that interface. It'll be more clear at that point in time. Okay. So our analyzer is going to have just one method. It needs to have a run method that's going to take in a list of matches that's going to be an array of match data tuples. And then it has to return a string. That's the actual data from running our analysis. So I'm going to say this thing has to have run. It's going to take an argument that we'll call matches. It's going to be an array of match data tuples and it has to return a string. Now right away we get an error around match data. That's why we just extracted that to its own separate file. So let's import that in here right away. Cool. Okay. Now we can define our second interface of output target. So I'll do export interface output target. This thing needs to have a print method that takes in an argument called report as a string and doesn't return anything. So print. Report that's going to be a string and doesn't return a thing. And then finally we can set up our actual class definition or the summary. Now again, summary is a class name here. I really don't like that much. I had previously called this thing analysis. Like so. But then we had a class called Analysis and Analyzer Inside and I was just really not clear. I felt like so calling it summary just kind of helps give a little bit differentiation against the analyzer property. Again, if you want to call this something else, absolutely. Feel free to do so. So I'm going to create a class called Summary. I'm going to make sure I export it. Now, here's the key the total key to composition. When we define an instance of class summary, we are going to pass in an instance, to use an instance of an object to use as the analyzer and an instance to use as the output target as well. So in other words, when we create an instance of class summary, we have to pass in to the constructor. One of these different analysis objects over here and one of these different reporting objects over here. And those are going to be used to be assigned to the analyzer property and the output target property. This is the key to object composition. We're going to configure summary when we actually create an instance of it. So with that in mind, I'm going to define the constructor function. And we're going to use that shortcut for defining a instance variable. So I'll say public and then we're going to pass in. Our analyzer. And it needs to be of type analyzer. And then the second argument will be public and we're going to pass in our output target. And it needs to be of type output target like so now I'm going to save this. It's not going to wrap the line, but I'll do it manually for you here. Just you can see this entire constructor definition. So now as an example of how we would actually use something like this, we would say news summary. And then we would have to pass in something that meets the analyzer interface. So maybe a new average or how about a new Windows analysis? So new Windows analysis. And as the second argument something that satisfies the output target interface. So a new. Like console report. That's what this is going to eventually look like. Now, as you might imagine, this kind of approach right here of using composition and passing in these instances when we create the like parent instance starts to get really verbose and there actually are some third party tools to make this entire process a little bit easier. We may or may not take a look at them inside this course because they are ridiculously complicated. So we'll see how far we get. Okay. So now we understand the general goal here. I'm going to save this file. We'll take a quick break and we'll continue in the next video.
