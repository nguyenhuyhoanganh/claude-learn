# Project Setup

In this video, we're going to do a little bit of project setup, so we're going to go through that same package, JSON file, the config file, all that kind of good stuff to actually compile our code and run it with one single command. Now, as a quick reminder of how we did that on the sorting project, remember we've got our package JSON file here. We created three different scripts, one to compile our code, one to run our code and then one to run, build and run simultaneously. We also made use of those dependencies concurrently to run multiple scripts with one command and Node Mond to automatically restart our project any time we recompiled it. And then finally remember, we also set up the CRC directory to be where all of our source code is going to live and the build directory to contain all of our compiled source code. So we're basically going to set up that exact structure one more time. It should go a lot faster since we now know exactly what we are doing. All right, so back at my terminal, I'm going to go back to a workspace directory of sorts. And then inside of here, I'm going to make a new project directory that I'm going to call stats. I'll then change into stats and I'm going to generate the packaged JSON file and the config file. Well, first do an excuse me npm init dash. Why? So that's our package chase on file and I'll do a tsk dash dash init so that will generate RTC config file. Once I've generated both those files and then going to install Node Mond and concurrently. So I'll do an NPM install. Nordman. And concurrently like so. Double check your spelling on concurrently. Make sure you've got two R's in there. So that should be a really fast install. I'll just let it do its thing. It should take just a couple of seconds and there we go. Once the installation is complete, I'll then start up my code editor inside this directory. Once I have my editor open, I'm going to create the package, the SRC directory and the build directory inside of my root project folder. So here's my RC folder and then I'll make sure that I also create, build and once again, remember I told you last time, make sure you do not create build inside of RC. If you just create the RC and then immediately click on new folder again, you will probably end up creating build inside of RC, which is not what we want. Once I've created CRC, I'm then going to make a new file inside there that's going to be eventually the root of our application. So I'm going to create indexes. And for right now, I'll do a quick console log. Like so. Next up, let's do a little bit of configuration of the TypeScript compiler. So we're going to make sure that we tell TypeScript that RC contains all of our source code and build is going to contain all the outputted compiled code. We are going to take care of that configuration inside of RT config json. Remember this file has a ton of different config or TypeScript compiler settings. The two that we care about right now are the output directory and the root directory. I'm going to uncomment both those settings and for root directory I'm going to say look for all of our source code inside the CRC directory. And then once everything has been compiled, put it inside of the build directory like so. I'll then save this file. Close it and then open up package JSON. So inside of here, the last thing we have to do is set up our three different scripts to build, run and run those two commands simultaneously as well. So inside the script section, I'm going to delete the existing command that we don't need and then I'll replace it with Start Colon Built. Any time we run this command, we actually want to run our TypeScript compiler in watch mode. Well then do our run command. So start colon run. Anytime we run that we'll execute node man build index DOJ's remember Node Man is going to automatically rerun that file any time we recompile our code. And then finally, one more to run, build and run simultaneously. So I'll call this one start. And any time we run an RPM start, we'll do concurrently. Once again, check your spelling. End PM Colon start. Colon Star. Once again, 9 p.m. Colon right here is telling concurrently to look inside of our scripts directory and ask me our packaged JSON file and find the script section. And then it's going to find all the scripts that have a pattern of start colon and then anything indicated by the star. So that's going to find, start colon, build and start colon, run and execute both those commands. All right, so I'm going to save this file and we can do a quick test. I'll then flip back over to my terminal and I'll do an NPM start. Now, the first time we run this, you might see an error message, which is totally fine. It's a very first time we try to run this. We might see the error message because Node Man will try to run our indexed JS file before TypeScript has successfully compiled it. So if I see an error like this right here, that's totally fine. I'm just going to stop the process with a control C and then run it one more time. And then I should see everything compile and run successfully. So there's my console log out of the index file right there. All right, so this looks good. Now, at the end of this video and as you go into the next video, you're going to see a quick lecture that has a CSV file, that's the CSV file, has all the football match stats. You're going to want to take that file that you're going to see in that next lecture and just make sure you drop it inside of your root project directory. We're going to use that as the source of all the data for the application we're going to work on. So just make sure you grab that file that you're going to see in a minute and drop it directly in here. All right. So quick pause and we'll start working on our application in the next video.
