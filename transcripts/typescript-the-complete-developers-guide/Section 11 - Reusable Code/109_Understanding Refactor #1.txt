# Understanding Refactor #1

In this video, we're going to kick off our first refactor of CSV file reader. Remember, we are going to do two separate refactor here so that you can compare and contrast different styles of code re-use in TypeScript. Before we talk about the first refactor we're going to do. I want to take a look at our read method inside of our read method. Notice how the first half of the method has a bunch of logic that will work with any type of CSV file. So it doesn't matter what type of CSV file, what the different columns are, how many columns there are, how many rows there are, or anything like that. That logic right there will always work correctly. It's only the second half of the read method that has some logic that is customized for our football CSV file. So it's really just this anonymous function right here that has that custom logic. Before we start talking about the big refactor we're going to do, I want to extract this function right here into a helper method on the class. The reason we're doing this is just to help you understand the refactor. We're going to do a little bit better. So, again, this anonymous function right here, I just want to extract into a helper method. So inside of my class definition, I'm going to make a helper method called Map Row. So the idea here is that we're going to take in one row, we're going to kind of convert the information inside of it and then return it again exactly what this anonymous function is currently doing. So SHAPIRO is going to take in a single row that is going to be an array of strings and it's going to return a match data tuple. So then we can go back up to the anonymous function. I'm going to cut and paste the return statement right here. Into the helper function. And now rather than passing in an anonymous function to map. I'm going to pass in a reference to map Roe instead. Notice how I did not put any parentheses on map row? We are instead just giving the map function. If I can get one set. There we go. We are just passing a reference to map row into map. So we are not trying to invoke map row. So this has an immediate impact here. Now, our read method is 100% reusable, more or less. It doesn't have any references inside of here, like very distinctly to any specific CSV file. And we've now isolated all the custom logic into Map Row instead. Okay, so now we understand that refactor. Let's talk about the big refactor to CSV file reader so we can restore some amount of code reuse. All right. So here's the idea. We're going to turn CSV file reader into an abstract class. Remember what abstract classes are in TypeScript. There are classes that we're never going to use to create an instance of an object. So we're never going to create an instance of CSV file reader anymore. Inside of an abstract class. We can have some methods with real implementations and then we can have some methods that we are going to promise to implement at some future point in time. These abstract methods that we're going to declare are going to have to be implemented by some child class. So the overall idea here, to really sum this up, we're going to have CSV file a reader that has still the read method that has some logic that is going to be appropriate for parsing any type of CSV file. Then whenever we want to parse or extract information from some very specific CSV file, we're going to subclass or extend CSV file reader. So this subclass right here, which we are going to call a match reader, is going to have some information about how to convert a very specific row of data. And so this match reader would be customized specifically for football CSV. So in other words, Match reader knows exactly how football CSV is structured, but CSV file reader does not as no idea. It only knows how to generally parse CSV files. So this is the first big refactor we're going to do. And this would be one way to get some amount of code reuse because now we can reuse CSV file reader as much as we want. Let's imagine that maybe we want to parse some CSV file. There has information about movies. We might create a new class called like Movie Reader. This would extend to CSV file reader and all we would have to do is implement a map row function that like returns maybe something of type, I don't know, movie data or something like that. So now we would be able to re-use all the logic inside of Reed, and all we have to do is customize the conversion of a per row basis or on a per row basis. So I think that this would definitely be a pretty good refactor. So now we understand what's going on here. Let's take a quick pause right now and we'll start this first refactor in the next video.
