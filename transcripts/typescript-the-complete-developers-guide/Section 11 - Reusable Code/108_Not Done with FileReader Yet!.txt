# Not Done with FileReader Yet!

At the end of the last video, I said that we're going to move on to our next big issue inside of our code base. But before we do, there's one quick thing I want to point out here. I want you to recall why we created the CSV file reader class. There were two big reasons we created this thing. The first one, we had said that we wanted to have a reusable source of information into our program. So at some future point in time, we might decide to get information about these different football matches from some outside API. If we had placed all of our CSV file reading logic right here, we would have had to delete that logic, which would have definitely not been that great. We would have deleted it and then replaced it with the logic to get information from that outside API. So as a solution, we extracted all that information or all that CSV parsing logic into our CSV file reader. The other big reason we created this class was so that we would have a class that could be used on future projects as well to open up CSV files and read information from them. So we wanted to be able to use this class on different types of CSV files as well. So I now have a big question for you. Do you think, given the refactor we just did around match data and this mapping function down here, do you think this class is still reusable? Well, I would definitely say no, without a doubt. No, this thing is not reusable. And the reason for that is that we now have a ton of references to match data. And this conversion function right here as well. So this class is now only going to work when we have a CSV file of exactly this structure, namely a date, a string, a string, a number, a number and then a character that is either an H and A or a D and then a string on the very end. If our CSV file structure ever changes from this, we would have to open up this class and start making some changes. So I would say that our class is no longer reusable. We cannot make use of this on future projects. So in this video and the next couple, I want to start to refactor this thing and make it reusable again. I still want to have the idea of match data like that's very valuable. I still want to have the match data tuple and I still want to have this conversion logic, but I want to have it somewhere else. That's all I really want to do. Now to do this refactor and make this class reusable again, we're going to take a look at two different ways of approaching this. Two very, very different ways. So this is going to give you the ability to compare and contrast different ways that we can reuse code inside of TypeScript. It's going to be a lot of fun. Now, before we get started, I want to do one quick thing here. I want to make a quick backup of our CSV file reader. We could do this using git, but honestly it would be a lot easier if we just created a new file and did a little bit of copy paste. So inside of my CRC directory, I'm going to make a new file called CSV File Reader. Back. Stands for backup. And then I'm going to copy paste all my typescript out of the current file into this dot back file and save it and close it. We are going to eventually use that code later on as a starting point for the second refactor or the second version of this stuff that we're going to do. Okay. So now we understand the goal. Let's take a quick pause right here. When we come back in the next video, we're going to start to refactor this class into the first reusable version. So I'll see you in just a minute.
