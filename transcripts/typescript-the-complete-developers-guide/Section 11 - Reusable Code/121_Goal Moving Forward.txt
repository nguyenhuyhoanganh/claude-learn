# Goal Moving Forward

In the last couple of videos, we've gotten really, really far off course. So in this video, we're going to start working on our big list of issues. Again, the conclusion of this application is going to come very quickly, so we're going to finish up this entire app rather fast now that we've covered some of these really big topics. So the last three issues on here are essentially all very closely related. The first issue is that we had a variable named after a specific team, so that was inside of our indexed file. We had a variable called Man United Winds. This variable really only made sense when we were looking at the number of times that some team called Man United won a game. And if we ever wanted to find out the number of times some other team won again, well, chances are this variable name of Man United wins would not make a lot of sense. After that, we've got a fixed analysis type. So right here, we've got only one type of analysis that we can really do. And if we ever want to have some different kind of analysis, well, we would have to delete or otherwise comment out this analysis, which remember usually is a sign that we cannot easily swap in or swap out different code. And then finally, we don't really have the ability to output our report in different formats right now. Our format or our output is a very simple console log. And so I don't have the ability to print out maybe like a PDF report or an HTML report or anything like that. So we're going to essentially fix all these three issues in one go by making use of object composition. Remember we just spoke about object composition in great detail. The idea is that we're going to give one object references to other objects and then the main object is going to essentially delegate certain behaviors out to those third party objects. So let me show you a diagram of how we're going to try to structure this next big refactor of our application. All right. So here's the idea. Now, we're not going to create every one of these things you see right here. Some of these are just four examples. So you would understand why this approach is going to make a lot of sense. We're going to create a new class called Summary. I think the class name is probably not the best and we could probably figure out a better class name here. If you can think of a better name, certainly feel to feel free to do so on your own project. The goal of class summary is essentially to coordinate the analysis phase and the reporting phase. So class summary is going to have a reference to some object that satisfies the analyzer interface. The idea here is that the analyzer interface is going to require some other class to implement a method called Run. Run is going to receive all that big list of matches that we've analyzed excuse me, that we've parsed. And then it's going to have to return some string that represents the output of that analysis. So an analyzer or something that implements this interface is essentially responsible for running some analysis on our data. So for example, we might create a class called Average Goals Analysis that implements that interface. And as you might guess, this class would have a run method that would try to figure out the average number of goals scored for maybe some particular team or for every single match. We might also have a class called like Wind Analysis, and maybe that's going to determine the total number of goals that some particular team scored or like the or simply the total number of wins that some team earned. So by creating this class analyzer and using composition, we're going to be able to easily swap out these different types of analysis. Now in a very similar thought, we're going to also have a property called output target. And this is going to have to be a object that satisfies the output target interface. The output target interface is essentially trying to specify some reporting output. So once we get some output or some report of sorts from our analyzer or some actual data, we need to actually put that information out somewhere. So something that satisfies the output target only has to have a single method called print and it's going to take in some report that's a string. So the output target might be satisfied by like a class called console report. And so console report, as you might guess, might just take a report as a string and console, log it out to our terminal and that's it. We might also create a class called HTML report as a substitute that would take in a report string and then save it out as some HTML file or something like that. So the entire idea here is that summary is kind of like the coordinating object. It's going to have a reference to some object that's going to be the analyzer and implement some very specific analysis. And it's also going to have a reference to some output target that's going to take a report and try to report it to some very specific channel. To actually start up that entire process of building the report and then sending it somewhere. We'll probably put a method on class summary called Build and Reprint Report or something like that. So when we call Build Imprint Report, it's not up to the summary to actually build and print the report. It's going to delegate those behaviors to these objects that it has reference to. So this is another example, like perfect example of how delegation and composition really is going to work. All right. So now we understand what's going to go on. We're going to write a whole bunch of code rather quickly, because I think that we've got a pretty good idea of what's going on here. So let's take a quick pause right here, and we're going to start banging out some code in the next video.
