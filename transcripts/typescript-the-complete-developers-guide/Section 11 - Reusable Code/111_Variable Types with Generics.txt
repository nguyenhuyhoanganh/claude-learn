# Variable Types with Generics

In the last video we spoke about how our CSV file reader still has a reference to match data. We don't want to have any references to match data inside this file because match data is 100% based upon football. CSV football. CSV is only going to exist inside this project. So if we want this class to be reusable, well, there can't be any reference to that csv file which match data essentially is. So we need to remove or ideally replace match data with some other type inside this file. One option would be to change it to any, but that's not really ideal because we want to avoid the any type as much as possible. So how are we going to fix this? Well, we're going to use a new feature inside of TypeScript that we've not yet discussed before called generics. Let's take a look at a quick diagram and get a better idea of how generics work and how they're going to fix our issue. Okay. So when we make use of generics, we're essentially working with something very similar to function arguments. When we work with functions, we pass in arguments to customize how the function body behaves. With generics, we are going to define classes and functions and we're going to customize the different types inside of those class and function definitions by passing in a type, as though it kind of were somewhat like an argument. Generics are going to allow us to define the type of a property inside of a class. The argument to a function and the return type of a function at some future point in time. And we're going to use generics very heavily whenever we are writing reusable code. Now, this diagram right here is not super helpful. So let's take a look at a quick code sample to give you a better idea of what is going on with generics. Back inside of my code editor, I'm going to make a new file. This file we're not going to save. I'm just going to do some quick little examples inside of here. So first I want to write out a little bit of code that has nothing to do with generics at all. So I'm going to say right here. Nothing to do with generics. I just want to write out a quick code sample to help you understand how we really think about function arguments. Okay. So I'm going to write out a simple function called add one. So ad one is going to take some argument that's going to be a number and then it's going to eventually return a number as well. So inside the function body, I'm going to say a plus, as you might guess, one, because the function is called add one. Now let's imagine that we have some other requirement inside of our application where sometimes we need to add one to a number and sometimes we need to add like two or three. Well, one really silly way to accomplish this would be to just duplicate the add one function. We'll call it say add two and maybe add two is going to take a and add two to it and maybe we need to add three as well. So we do something like add three and this one is just going to add three. So notice a pattern here. Every time that we want to change some value inside the function, we are duplicating the function and then just changing some hardcoded value inside the function. Now of course, this is really silly to do a much better way to approach building out this collection of functions right here would be to just build one function called simply add. And then rather than having a hardcoded value right there, we could accept that as a second argument. So I'll call it like B, that's going to be a number. And then we can do return A plus B. So now rather than having three different function definitions, I have one and I can call add with like ten and one. Out with one and two and add with ten and three. With each of these function calls, we can imagine that this argument of one right here is being received as the argument of B. And so inside the function body, we can kind of imagine that we are replacing B right there with one. Right. That's not quite what is going on. But we can imagine that is what is happening. So the entire point here is that when we make use of function arguments, we customize how the function behaves when we actually make use of it. So with that in mind, let's now take a look at generics. And I really want you to understand that all this generic stuff is very, very similar to how arguments work inside functions. Okay. So I'm going to delete all that stuff and I'm going to write out a new class definition inside of here. So I'm going to call a class or create a class called Old Number. This thing is going to have a data property that is a number. So then I would create an instance of this by saying something like Old number is a new old. Number. And then if I wanted to assign that data property, I could say hold number. Data is one, two, three. Now, let's imagine very similar to the collection of functions we just put together. Maybe we have some other requirement inside of our application where we also have to have a class that has a reference to a string as well. So one really silly way to accomplish that would be to create a second class called Hold String. And maybe this has a data property as well that's going to be a string. So then we can say Hold string. It's going to be a new old string. Now we can say hold string data is some string like so. So just like that function example. This is silly right here. We would never define like duplicate class definitions and just sub out one single property inside there or one like series of characters. So rather than defining these separate classes, we could instead use a generic. The generic is going to customize the definition of this class in the exact same way that that be argument in the functions we are just looking at. Customize the body of the function. So I'm going to delete this code one more time and I'm going to write out a single class that's going to basically do the same thing that these two right here are doing. But I can use a generic to customize the types inside of it on the fly. So I'm going to make a new class called Hold Anything. It's still going to have a data property. But then for its type, I'm going to add in a little additional syntax right after the class name right there, I'm going to put in a set of angle brackets and then I'm going to receive a type as an argument to the class. So the type I'm going to put in here, I'm going to give a argument name to. I'm going to give it a name as something like. Type of. Data. So again, we can imagine this right here as being an argument just in the same way we had that B argument into those ADD functions. So now inside of my class definition, I can freely refer to type of data in any location where I need to refer to a type. So for data, I'm going to say, Oh yeah, this data is going to be of type, type of data. So now I can create an instance of hold anything. And when I create the instance, I'm going to pass in essentially an argument for type of data right here. Remember, when we call a function, we have to pass in a value for each argument in the exact same way. When we define a generic, we have to pass in a type for this generic value right here. And we are treating this as though it were an argument to the entire class body. So underneath the class definition, I'll say const hold number. So as you might guess, I want to create a copy of hold anything that's going to specifically be made to hold a number. So I'll say new hold anything. And then I'm going to put down another set of angle brackets right here, and I'm going to provide a type that's going to be used inside of the class body. So I'm going to say right here, number, and then I'll put down my usual set of parentheses right after that. So number right here is doing the same kind of thing that a function argument is doing. We can imagine that the class definition is receiving number and then inside of the class definition, everywhere where we have type of data, it gets replaced with number instead. So this generic right here is saying, okay, give me a copy of hold anything that is customized to work with numbers. So once again, I can't say it enough. When we work with generics, just view it as though it were exactly like a function argument. When we call a function, we pass in an argument to customize how the function works. When we use a generic, we are saying when we create an instance of the class, we're going to pass in an argument of sorts to customize how the class works. So now underneath this, I can say hold number data is going to be some number. And if I hover over dot data, you'll notice that there's an annotation on here that says data is of type number. So when we see type of data right here, every time we see type of data, it's going to be replaced with number. So now we can reuse this class definition as many times as we want with different types. So I can say hold string. Is going to be a new old string. And for the generic type right there, I'm going to say, yep, this thing is going to be a string. Oop. Sorry, I called it hold stream and hold anything. So now we can imagine that type string is going to replace type of data everywhere inside the class body. So like that's going to be a string and that's going to be a string as well. And now I can say hold string data will be a string like so. And of course, I need to actually change that back type of data. And when I do so, I can now hover over data and I get a type annotation saying, Oh yeah, in this case, data is a string. If I can actually hover over it. There we go. There is the type annotation. Okay. So can't say it enough treat generics like function arguments at the end of the day very similar in nature. So hopefully you can kind of understand how we might be able to use this strategy right here in solving our match data issue. So rather than having a hard coded match data and match data right here, we could instead turn our abstract class CSV file reader into a generic class. So we can add on a generic annotation right here of something like type of data. And then we can use type of data wherever we have match data. So right there and right there as well. So now whenever we create an instance of CSV file reader or otherwise make use of it because remember this is an abstract class, we can't actually create an instance of it. Whenever we make use of CSV file reader, we're going to add in those little angle brackets or we're going to specify the type to use inside of the class in place of type of data. All right. Now, one thing I am going to change here, whenever you start working with generics by convention, rather than giving this thing a really long name, usually we will refer to it simply as T instead. So you'll see like T right there. T right there and T right there. Now this is just convention. There's nothing special about calling type of data. T And as a matter of fact, there's nothing special about calling it type of data. Anyways, we can call this anything we want about like anything we want, literally, just like a argument name. It can be any type or something, any name that we want it to be. But again by convention usually we refer to it as TW like so. So when you start looking at some typescript source code written by other engineers, you're going to sometimes see a lot of references to something called T. So whenever you see PT, that is a reference to a generic type. Okay. So now we've got a better idea of what's going on with generics. I'm going to save this file right here. We're going to come back in the next video, do a quick recap on generics and then make sure that it's really clear that you understand why this is going to solve the issue inside of our CSV file reader.
