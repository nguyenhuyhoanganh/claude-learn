# Using Enums

In the last video we spoke about how the presence of these different identifiers or lack of them is just a little bit confusing. So in this video, we're going to figure out first a JavaScript solution to this problem and we'll talk about an equivalent TypeScript style solution. Okay. So once again, extracting these hardcoded strings into variables is great because it gives us a little bit more context about this comparison. But in doing so, we might possibly lose information about our data set because we would be very tempted to to delete the draw option like so. So to fix this up, I'm going to replace all those identifiers with a new variable. I'm going to call this match result. This is going to be an object. And inside of it, I'm going to encode the three different possible outcomes of a match. So they will be home win. That would be a capital H, a way win which will be a capital A and draw, which would be a capital DX. Notice how I used variable names of capital match result, capital, home, win away, win and drop. That's definitely not convention right there, but you'll see why I am using those capital letters in just a moment. So now for our comparisons, we would instead do something like match result dot com win. And match result shot away win. So let's take a look at this approach. Now, our comparisons down here and right here are still crystal clear. And I think other engineers would be able to say, okay, we're checking to see if there is a home win or an away win. The other improvement is that our object up here very closely represents the three possible outcomes from a match either a home away or draw. And the benefit to this style is that now we can put this drawer property in here and it's never going to be marked as unused. And so no other engineer should ever come in and start to delete options out of that object. So I would say that this is definitely a big improvement, but still one tiny little issue here. What is the last issue? Well, the last issue is that in typical JavaScript, I know we are writing TypeScript here, but in typical JavaScript, objects have a ton of different purposes. In JavaScript, we use objects to represent records or like pieces of information. Other times we use them to store a collection of different methods or different properties. And in other times, like this scenario, right here, we are using them to store a record or a set of possible outcomes of a match. So the only issue with the current object based approach right here is that, well, other engineers might look at this and say, why is this thing an object? Like, does this represent something important? Its purpose is just a little bit unclear. So rather than using an object like you see, we're going to use something inside of TypeScript called an enum. The word enum. And I'm going to type it out for you really quickly right here in a comment. Enum short for enumeration. Is has a purpose just about identical to what you see right here. It is essentially an object that stores some very closely related values. And these values are always going to be either numbers or strings. So I'm going to refactor this object right here into an enum definition and we'll talk about exactly what they are a little bit more. So for this refactor, I'm going to change the word const into enum. I'm then going to remove the equal sign. And then for each of these colons I will replace them with equals as well. Like so. All right. So that right there, that is the syntax for an enum. We use the enum keyword, the name of the enum set of curly braces and then a collection of different properties inside. Now all these different properties inside are going to be a set of very closely related values. And so using them to represent the outcomes of a football match would be a perfect use of an enum. There's a fixed set and a small set of possible outcomes of a match either a home, win away, win or draw. Now, something that's really hard to understand about enemies is why we would use them over the object we just had. Well, the purpose of an enum, more than anything else, is signaling to other engineers there's not necessarily any big improvement to our code from like a performance perspective or the ability for TypeScript to do any checking or anything like that. By using an enum, we use it simply to signal to other engineers that this is a collection of very closely related values. In order to reference any of the values inside of here, we would write out the name of the enum. So like match result and then the name of the option that we want to reference. So away win. Or home win. Or draw. When we create an enum, we also create a new type inside of our application as well. So the type would be match result. So we can now declare some kind of function that may be returned one of these match results. So for example, let's say that we had some function call like print match result. Or something like that. And then maybe inside of here, we had some logic where we check to see if some condition was true or false. Like maybe we checked to see if match at five was the home team. If that was the case, then we could return match results at home win. And if that wasn't the case and we only had kind of like a two scenario system here where either you have a home win or an away win, maybe our default case would be match result, away win. So now inside this function, no matter what, we are returning one of the different values that is contained inside that enum. So to properly annotate this function, we could put a colon right here and then say whatever we return from this function is going to be one of the match result properties like so. So this is saying that whatever we return is going to be either home, win away, win or draw. All right. Now, like I said, emotions are very subtle. So I want to walk through a couple of different examples with them to help you understand when we should be using enzymes in general. So let's take a quick pause right here. We'll come back in the next video and go through a very quick exercise.
