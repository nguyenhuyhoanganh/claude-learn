# Implementing an Analyzer Class

In this video, we're going to start working on some of the different analyzers that we're going to have inside of our project. Remember, something that meets the analyzer interface is going to be a class that actually runs the very real analysis for us. So it's going to find maybe the average number of goals per game or the number of wins per team or whatever it is. So let's start working on our first analyzer. We're going to implement our wins analysis class. The WINS Analysis class is essentially going to be the same exact thing we are currently doing inside of our index file. We're just going to give it the name of a team. So for example, maybe Man United and it's going to tell us the number of times that that particular team won a match. That's it. So pretty much the same thing as what we have right here. But we're going to wrap it up inside of a class. So to get started inside my CRC directory, I'm going to make a new folder and I'm going to call it analyzers and I'm going to eventually put all of my different analyzers inside this directory. So the first one we're going to create wins analysis. So inside that new directory, I'll create winds analysis. Acts. Then inside of here. I'm going to export. Class wins analysis. Now, remember, this class right here needs to satisfy. The analyzer interface. So the way that we can satisfy that interface is by obviously implementing that run method. Or remember, if we want to, we can have TypeScript give us just a little bit of help with the implementation. If we want to get just a little bit of help, we can import that interface and then use the implements keyword when we define our class. And that means that TypeScript is going to check our class to make sure specifically that it successfully implements that interface. We've done this once before, remember, it is 100% optional. You don't have to do it, but it's nice to have TypeScript kind of check your code for you if you want to. So just to be clear, that is also why we exported. Our. Analyzer interface in the output target interface so we can import these into these other files and use them while we are writing our code to make sure that we are implementing the interfaces correctly. So back inside of Win's analysis. At the very top. I'm going to import analyzer from up one directory because remember right now we are inside analyzers and I want to take a look at that summary file. I want to get the analyzer interface out of there. So now here's the optional step we can say implements. Analyzer like so and that right there is what's going to tell TypeScript, hey, we're trying to get this class to implement this interface. Give us some help, check our class for us. So right now, if we hover over it, it says, hey, you're not implementing this interface correctly. You have to have that run method. Again. Can't say enough. 100% optional. Totally up to you if you want to use this on your own projects. Okay. So inside of here, we want our winds analysis class to take in the name of a team and find the number of times that that team won a match. So we need to provide the team probably as a constructor argument. So I'm going to define the constructor. I'll use that shortened syntax and say public. We're going to get the team and that's going to be a string. And of course, if you want to, you can call this like team name. That might make it a little bit more clear. I'll just call it team, though. So now that we've got the name of the team that we want to find the number of wins for, we can define our run method. Remember, the run method is what is required by our interface. So if we go back over to our interface definition, it's going to tell us that the run method has to take in an array of match data tuples and then return a string. Remember that the argument name inside the interface that's only there to give you and I as developers some context. When we implement the run method, we don't have to call that first argument matches. If we don't want to, we're going to use matches just because, hey, it makes a lot of sense, but we don't have to exactly match that name. Okay. So back inside of Win's analysis. We're going to get matches. That's going to be the array of match data tuples and then we eventually need to return a string. That's going to be the actual result of this analysis. Now right away we get an error around match data because we have not imported that type into this file. So I'll do that at the top. We need to go up one directory again. So notice the dot dot slash and then we're looking for match data, which is where our tuple is defined. Okay. So now inside of here, we can do the actual analysis. Now, just to save a little bit of time, I'm going to go back over to my index file and I'm just going to cut and paste the existing analysis we had done over here. We're going to need to change a couple of things, but this will definitely work to get us started. Okay, so I'm going to cut that stuff. I'm going to go back over to Windows Analysis and paste it inside of Run. I'm going to reformat just a little bit and now we're going to do a little bit of renaming inside of here. So first off, yeah, calling this man united wins. Probably not a good idea. I'm just going to call it wins instead, so I'm going to update their there and there. So I should now see wins three times one, two and three. Next up inside of our For Loop, you'll notice that we are still trying to pull data off our match reader that doesn't exist inside this file. Now all of our different matches are going to be provided as an argument so we can update the for of loop to iterate over simply matches like so. And then finally we've got a air down here around the match results. Remember, that is our enum. Enum that has either home win away, win or draw. So to make that error go away, we can just import the enum definition at the top. Up one directory again. Match result. And those errors go away. All right. So now the very last thing we have to do is actually produce and return a string using the analysis that we've conducted inside this method. So after the for loop, I'll do something like return and then a string, a string describing the result of this analysis. So I'll say like team, this team. One, and then I'll print out the number of wins this team won. Like so. Okay. So that looks good. That is our first analyzer. So this is a standalone class that's going to take all of our match data. Run some analysis on it and then return a result. So in theory, we could create as many of these classes as we want to and each one could implement a different type of analysis. Remember back in this diagram over here, I gave the example maybe we would create a separate class that tries to find the number of average goals per game or something like that. Now, one thing I just want to mention really quickly here as well, we are not restricted to just the run method here. If our class needs to have some other methods inside of it to actually complete an analysis, that's really fine. We can actually add on helper methods. Remember, an interface like the one we are implementing right here doesn't restrict the number of methods we can have inside of a class. It only requires that we have a certain number with some given name and signature. That's all it does. Okay. So this looks good. So let's take a quick pause right here. When we come back. The next video, we'll start putting together the console report and we'll be able to start testing out our project again. So a quick pause and I'll see you in just a minute.
