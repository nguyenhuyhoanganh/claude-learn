# Generating HTML Reports

In this video, we're going to put together another reporting class that's going to take the result of an analysis and just throw it into an HTML file. So let's get right to it. Inside of my report targets directory, I'm going to make a new file called HTML reports. Then inside of here, I'm going to import the output target interface that we had defined inside of summary. Remember importing the interface and using it inside of an implements clause not required. Totally optional. So inside of html reports I will import output output target from up one directory summary. Then I will export class HTML report and I'm going to say this thing needs to implement the output target interface. And once again, we get some information here telling us, hey, this thing better have that print method. So inside of here we can define our print method. It's going to be given the report as a string and it's not going to return anything. So then inside this method, we're going to generate a tiny little HTML snippet and then write it to an HTML file. So to generate the HTML snippet, I'll say const HTML is going to be a string template. So notice the back ticks here because we want to use a multi-line string. If we use single quotes, well, we're going to get a syntax error very quickly. So please make sure you're using those back ticks. Then inside of here, I'll put down just a little bit of very simple HTML, so I'll place a div. How about an H one? That says analysis output. And then a div and inside there, I'll just dump the report that we were given. Okay. So this is going to generate some HTML and now we want to stick it into an HTML file. So to do so, we're going to use that FS class that we spoke about or the FS module from the Node Standard Library. Again, remember, we use the FS module inside of our CSV file reader to read a file off our harddrive. So now we're just going to do the exact opposite. We're going to attempt to write a file. So back inside of HTML report at the top, I'm going to import FS from FS. And then right after we create our HTML string, I'll call ZFS, write file sync. The first argument to this? Well, I can tell you or you can command click on this or even just hover over it and get some documentation. So if I hover over it, the tooltip right here says that the first argument is going to be a path. So essentially the name of the file that I want to generate or actually write to my harddrive. And then the second argument is something called data. And I bet you can imagine. Yeah, data is probably supposed to be like the actual information we want to write into that file. So once again, we don't have to look at the documentation. We can use these type definition files to understand what we are doing here. So for the first argument, I'll say reporter html. It would be nice to be able to pass in the name of a file as a constructor argument, but I'll leave that as an exercise up to you. And then the second argument is going to be the actual data we want to write into the file. So in this case, this snippet of HTML. So let's say HTML like so. Okay. So that's pretty much it for our HTML report. So now the last thing we have to do is flip back over to indexes. At the top. I'm going to import. The HTML report that we just created. From our report targets directory. HTML report. And then finally down when we create our summary, rather than passing in an instance of council report, we will instead pass in an instance of HTML report instead. Okay. So now the instant I save this file, remember, our code is going to be reran at the terminal, and so we should see a HTML file generated inside of our root project directory. So I'm going to save this file. Very shortly after that, i'll see reporter html generated because well, you know, we just generated the report and so now we can open up that file inside of our browser. If you are on macOS, you can open up that file very easily by opening up your terminal and running open. Then the name of the file which is report HTML. So open reports HTML. If you're on windows, you can just open up your older explorer, find your project directory, and open up that HTML file. And so now I can see analysis output as an H one and then a div that says Team Man United won eight games. Now, obviously, this is a super, super boring report. But again, the point here is that by making use of an interface and object composition, we can very dramatically change the behaviour of our application without having to make dramatic changes in the actual code. Okay. So I think that we've spoken quite a bit about composition now, so I'm not going to repeat any of the stuff that you've heard me say many times. So let's take a quick pause right here. This application is pretty much all wrapped up, so let's do a quick wrap up and talk about some of the high points of this app in the next video.
