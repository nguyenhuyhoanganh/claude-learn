# Data Types

We've now extracted all the CSV file reader logic, but we still have a lot of work to do. So let's go back to our list of outstanding items. Okay, here we go. So the next thing we're going to work on is the fact that our data array is all strings. Even though there are definitely some different types of values inside there. So to help you understand what I mean by this, I'm going to flip back over to my index file. And then right after we read the contents of that CSV file, I'm going to add in a console.log or reader. Data like so. All right. So I'm going to save this flip back over to my terminal, and now we can see all the information coming out of that CSV file. So notice how we have some numbers inside of here that are still wrapped inside of strings. So I think that we need to somehow parse those numbers out and put them into the overall data structure that we're getting back at that CSV file reader as actual numbers as opposed to the strings that they currently are. In addition, you'll notice we also have some date strings inside of here as well. And they have the format day, month, year. Having the date inside of here as a string is not super helpful. So I think we should also try to parse that string and turn it into an actual date object as well. Now something I want to clarify here and make sure it's really clear is that we're kind of talking about doing two operations. So first off, I want to take these different values that are currently being treated as strings and actually are strings. And I want to parse them into some appropriate value. So in other words, I think we need to take this string right here that represents a date and turn it into an actual date object. And then in the case of the number of goals scored, those are currently strings. So we need to parse those strings and get numbers out of them. So at the same time, not only are we doing this kind of parsing logic, but we also need to kind of update the types that these different values are going to be stored as inside of our data array. So the first element should be an instance of a date object that should be its type. And then for the number of goals scored, those should be number types. And for the result of the match, we should make sure that it's really clear that this is one of the different values inside of our match result. Enum. So we're not only talking about parsing these values and getting a different value. We're also talking about updating their type definition inside of the CSV file reader as well. Okay. So let's take a quick pause right here. When we come back, the next video, we're going to start working on a function that's going to take that date string, parse it, and return an actual instance of a date object. So a quick pause and I'll see you in just a minute.
