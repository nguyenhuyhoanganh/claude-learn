# Applying a Type to a Generic Class

In the last video we spoke about how we can make use of generics to customize the different types that are flowing around a class. Now, we spoke about changing the type of the data property right here. But just to be clear, we can use the generic for anything inside the class body that we want. So for example, we could have a function inside of here called like ADD and we can say that maybe ADD takes a argument of type T and it returns an argument of type T as well. So now in the case of hold number, we could call hold number dot, add and pass in some number. Oops. One thing I forgot, we need to make sure we actually return that argument a like so. So now by putting together a instance of hold, anything that is customized to work with numbers, we can once again imagine that everything that says T inside the class capital t. Like right there. Oh can't do my multi select. Here we go is going to instead say number. Okay. So we're going to use this feature to solve all of our issues with CSV file reader. So CSV file reader is now going to be a generic class. So whenever we make use of this class in any way, shape or form, we're going to have to pass in the type to customize CSV file reader. So that means we're going to say, okay, when we use CSV file reader, we're going to pass in match data. And we can imagine that match data will then go around and replace all the capital t's inside of here like so. Okay. So now we understand what's going to solve this issue. I'm not going to take our type data definition because we don't need this inside of this file anymore. I'm going to cut it out and I'm going to put it over inside of Match Reader. And then likewise back inside of CSV file reader, we now have a unused import for match result. So I'm going to delete that out of there as well. Then back inside of Match Reader, we now have an air around CSV file reader and it says, okay, you've got a generic type right here and if you want to use this type, you have to provide a type argument to it. So at CSV file reader, I'm going to put in that generic notation. And inside those English brackets, I'll say the type that I want to be applied to t inside of csv file reader, which is going to be match data. And that's pretty much it. Okay. So now we've got a very reusable CSV file reader. There's no reference to anything about football, CSV or the different types inside of it inside of here. Instead, it is a generic class, so we can customize the different types that are flowing around it on the fly. In this case we are customizing the CSV file reader over inside of Match Reader. So we're saying give us a copy of Match Reader that's going to extend CSV file reader that has been customized to work with match data. So now everything inside of our application should work as long as we fix up a little bit of code inside of indexes. So I'm going to flip back over there. We still have a reference to CSV file reader here. Remember, we are no longer going to create references or instances of CSV file reader. Instead, this is going to be a match reader. So I'm going to update the import statement. And update the file as well. And now we can create an instance of match reader like so. And that's pretty much it. So now we can save everything, flip back over to our terminal, and we should see. Man United won 18 games. Awesome. You'll notice I still have that big console.log here. I'm going to fix that up really quickly. Where is that coming from? That's somewhere around here. We just try restarting this really quickly. Yeah. There we go. Okay. Man, United won 18 games. Much better. All right, so it's definitely works out pretty well. And this finishes up version one of our refactor. This is version one where we are making use of CSV file reader in a abstract class plus generic to achieve some amount of code re-use. So let's take a quick pause right here. When we come back to the next video, we're going to start to refactor CSV file reader a second time and take a look at a very different way of getting some amount of code reuse without using an abstract class. So quick pause and I'll see you in just a minute.
