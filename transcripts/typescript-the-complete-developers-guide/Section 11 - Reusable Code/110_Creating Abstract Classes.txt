# Creating Abstract Classes

All right, now that we understand the refactor, let's get to it. We're going to first create that class match reader and make sure that it extends the abstract class CSV file reader. So back inside of my code editor, I'm going to create a new file inside of my CRC directory called Match Reader Dots that inside of here. I'm going to import the CSV file reader. From the CSV file reader file. Well then define and export class match reader. And that's going to be extending our CSV file reader. So inside of here, we're going to eventually have to define the map row function. Let's do that right now. So I'm going to define map row. This thing is going to take a single ROE. That is a array of strings and it's going to return something of type match data. Because, remember, that's the entire goal. This thing, this is where we are wrapping up the reference to match data. So this is going to return match data. And then inside of here, we need to return something of type match data. So rather than rewriting all that stuff, let's just go back over to CSV file reader. We'll find the existing mapper over here and I'm just going to cut and paste that entire return statement. I'll cut it, pull it back over. And based. We immediately get a couple of errors inside of here. So let's take care of, say, date string to date. I'll import that at the top from. Our Utils file. Next up, we have a reference to match results. Remember, that is our enum and we define that inside of our match results file. So let's import that as well. Cool. And we still have an error here around match data. We're going to take care of that area in just a moment. But first, let's do a little refactor on CSV file reader first. So back inside of CSV file reader, we can go up to the top. We don't need the date string to date function anymore, so I'm going to delete that import. Next up, we will mark our class as being abstract. And then inside of this class, we need to mark well, we don't have to per se, but we want to mark our map row function as being abstract. Remember, we put the definition for that at the very bottom. So to mark a method as abstract, which indicates that it's going to be implemented by our child class, we just put the abstract keyword in front of it and then we remove the curly braces for the function itself. Now. Personally, I like to put abstract methods right underneath the constructor so that they're really easy to see. So I'm going to cut that definition. Input it right underneath the constructor. Okay. So this looks pretty good. But there's one really big issue that we've kind of been skirting around. And he might be actually kind of wondering why we didn't discuss this in the last two videos. We've said several times now that the entire goal is to make CSV file reader reusable. Right. That's the idea. And to really make that occur, we need to make sure that this file, the entire file has no reference whatsoever to anything called match data because match data is customized to work only for the current CSV file we are dealing with. Match data is saying that every row is going to be a date string, string number, number, and so on. So match data is talking specifically about football CSV. So we need to somehow extract that type definition out of this file. And not have any reference to it inside of our class. But that's going to make life just a little bit challenging. And why is that? Well, it's because our data property right here, we're saying that this is going to be an array of match data tuples. Likewise, map row right here. We are saying that this is going to return a match data tuple. So on the one hand, we're saying that we want to remove these references, but the big question is what do we replace it with? Like Map Row has to return something if it's not going to be matched data. Literally. What are we going to put right there? We could really cheat and say this is going to be of type any but that's probably not a good idea because remember we want to avoid any as a type as much as possible. So let's take a quick pause right here. When we come back, the next video, we're going to really outline what the issue is here and why we don't want to have match data. And we're going to discuss how we're going to fix this up. So quick pause and I'll see you in just a minute.
