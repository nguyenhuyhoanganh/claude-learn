# Converting Row Values

In the last video, we put together our date string to date function. So now we can go through our CSV file reader and after we read some information out of the CSV file, we can now go through each individual row and convert each value inside of each of those rows into some appropriate type. To get started. At the very top of this file, I'm going to import that helper we just created. So that's date string. To date. From the utils file. And now inside of our read method, I'm going to chain on another dot map statement. So we could definitely add in some logic to the DOT map that already exists to take each element inside of each row and convert them. But I think that it might be just a little bit more clear to just use a separate map statement, because then we would know very well that this MAP statement is first going to split a single string row into an array of strings, and then the next map statement will do the actual conversion process for each of those values. So I'm going to remove the semicolon right there and chain on another map. I'm going to pass this thing a function. So our incoming value is going to be something that will come call row and that's going to be the array of strings. So just to make sure it's really clear row right here that represents. One of these single rows, so an array of strings. And we want to go through that array of strings and convert each of those values into some other type if it is appropriate. And then from each of these, we're going to return well, something we'll get around to that in just a second. So right now, I'm going to cheat just a little bit and I'm going to put on any right there. Remember, in general, we don't ever want to use the any type, but I just want to get something working for right now and we're going to come back to that and really decide what we are returning from this function in just a moment. Okay. So now inside of here, we're going to take that row of strings. We're going to go through each value inside of it and do some processing. So I'm going to eventually return a new array that's going to represent all the items inside there. So our first element is going to be the converted date string. So we want to take in that first element, run it through that function we just put together and spit out the date on the other side. So I'm going to take date string. To date. And put in row at zero. And that's pretty much it. So now we're going to repeat that same process for all the other values inside the row. And then if we need to convert it, well, we will convert it on the fly. So the next two elements we're not going to do any conversion on those are going to stay as strings. So the next element will be row at one. Then row it to. Then three and four are going to be the number of goals scored. Right now they are strings. We need to turn them into numbers. To do that, we'll just use the parsing function again. So we'll do a pars int with Rowe at three. A percent with Roe at four. And that's looking pretty good. Now the next one is going to be a little bit more challenging. We currently have a string inside of our row and we want to make sure it's really clear that this string is going to be one of the possible values of our match result. Enum. So to convert a string into one of the values from our match result enum, we first need to get access to the enum type definition inside this file. Now that's going to be just a little bit of a process. So I'm going to save this file right here for right now. We're going to take a quick pause and then come back in the next video. So quick break and I'll see you in just a minute.
