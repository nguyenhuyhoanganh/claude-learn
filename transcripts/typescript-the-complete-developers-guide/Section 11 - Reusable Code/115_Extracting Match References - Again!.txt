# Extracting Match References - Again!

In this video, we're going to start refactoring our CSV file reader. There's a lot of stuff inside of here that needs to be fixed up, so we're going to do it step by step, line by line. So first off, everything having to do with a match needs to get moved over to the match reader. Remember, the entire goal of all this refactoring is to make sure that CSV file reader is 100% standalone and reusable, so it should have no reference whatsoever to anything having to do with a match. Does. That means that we're going to move the entire conversion process of all of our data over to the match reader. That means that our CSV file reader no longer needs the date string to date function. So I'm going to cut that import and move it over to the match reader at the top. Next up, match result once again, should have no references to match inside this file. So I'm going to cut that out and move it over to Match Reader. We then have our tuple definition. Well, same thing. Should be no references to match data inside this file, so I'm going to cut that and move it over to match reader as well. All right. Now, going down the list here, our CSV file reader is no longer going to be dealing with a data property that is match data. Remember CSV file reader, we want this to satisfy the data reader interface. So data needs to be a two dimensional array of strings. So I'm going to update data right there to string two dimensional like so. Next up, if we scroll down a little bit, we've still got that mapping statement right here where we do the actual data conversion process. So again, noted data conversion inside of here whatsoever. So I'm going to cut out that dot map. We should still have the original map right there, and I'm going to paste the rest of all that stuff, everything. I just cut directly inside my class body. We're going to reformat that in just a second. So for right now, I'm just going to paste it and comment it out. So now if I go back over to CSV file reader, I should see no errors inside of here whatsoever. In fact, I should see 100% generic code. Nothing related to matches. All we have is some CSV file reading logic that we can actually use on future projects as well. Okay. So this looks pretty good. It's now the last thing we have to do. Is make sure that match reader has a load method. Any time we call load, we need to attempt to read data from our data reader and then we're going to do that conversion process on data. So a quick pause will take care of that in the next video.
