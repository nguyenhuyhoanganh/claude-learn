# More on Inheritance vs Composition

In the last video we introduced the idea of inheritance, verse, composition, and I had said that inheritance is characterized by is a whereas composition is a has a relationship. Now this isn't super useful right here. So let's walk through another quick example to really help you understand what's going on. All right. So I want to imagine for a second that we're going to work on a very different application. We're not going to actually write any code. This is just a quick experiment. And I want to imagine that inside of our application, we have to somehow model a window like a window in a house. So we might say that, okay, you know, let's try taking some inheritance based approach here and we're going to try to create a class of window and maybe it's going to have some properties like the one you see right here. So we might have like an open flag to indicate whether or not the window is open and then some method to change the open state. And then of course, a window has some dimensions, like a height and width, and maybe at some point in time we would want to calculate the area of the window as well. Now, let's imagine for a second that in addition to a class called Window, maybe we also want to have a class called Wall. Well, when you look at wall over here, I think a wall would probably have a color that's a string and then it would probably also have a height with an area. So notice that both window and wall share some common characteristics. They've both got a height, they've both got the width, and they've both got an area as well. And so chances are that we're going to have some amount of code duplication between these two classes. So to avoid any amount of code duplication, we might decide to create some common parent class for both them. So maybe some relationship like this. So we might create a class called a rectangle, because a rectangle kind of represents the shape of both a wall and a window. Rectangle would have a height with an area of method, and then we would create wall and window and make sure that they extend rectangle. So Wall and window would then borrow all of these different methods and properties. So in this relationship we would say that wall is a rectangle and that window is a rectangle as well. We're using classic inheritance. Here we are borrowing properties and methods from some parent class. Now this might seem like it works out pretty well, but there's a really easy way to throw a wrench in this whole system. So let's imagine for a second that we have a window that is not rectangle. Maybe we have some other kind of window inside of our house that is actually circular in nature. And this is entirely common. I don't know if you've ever seen one before, but houses all the time have circular windows. So we would not be able to use our class window that we already created right here to represent a circular window. Instead, we would have to create an entire nother class hierarchy in parallel. So we would continue to have our class rectangle with height, with an area and that would be used as a parent class to wall. And then maybe we would rename our window to Rectangle Window to indicate that this is a rectangular flavor of a window. And then to represent a circular window, we might create another parent class called Circle, and this might have a radius and an area that returns a number. So now we would create a circle window using the parent class of circle and circle window would have to have its own separate definition of whether or not the window is open and also some method to toggle whether or not is open as well. So now right away we can see very easily that we've got some code duplication going on. These are both windows and they differ in a very, very slight fashion. Just the shape. That's it. But we would end up having to duplicate this open property and we'd also have to duplicate the toggle open method as well. Between those two classes now, chances are the implementation of Toggle open and open would be very simple. But hey, the point here is that we would have some amount of code duplication. So one way that we could solve this whole problem is to abandon inheritance entirely and try using out a composition based approach instead. So if we used a composition approach, here's what we would do. We would create a class to represent a wall and a class to represent a window. Both wall and window would have a reference to some other object that matched some interface, and maybe we would call that interface shape. So then, rather than having class and window inherit from some other class, we would say dimensions. Are you going to reference some particular shape? So I can say that. Okay, class wall. I want to have a wall with a rectangle or rectangular dimensions. So when I initialize or create an instance of class wall, I could give it a reference to class rectangle. I could just as easily give class wall a reference to class circle instead. And then on class? Well, I would probably add on an area method and any time that we called area, rather than trying to compute the area ourself inside of class wall, we could instead kind of delegate that area calculation. To whatever our dimensions object was. And so that means that eventually we would be calling area on the rectangle or area on the circle instead. And the same thing with the window as well. We would have a reference to some object that matches a shape interface. And so this would be a reference to either class circle or an instance of a circle, to be precise, or an instance of rectangle. And then any time we call area on class window, we would delegate that calculation. To our dimensions property. And so we would still be up to the shape to reflect the area of either the window or the wall. So the benefit to this approach right here is that by using composition and giving a object a reference to another object, we can much more easily create some more complicated looking objects without having to completely duplicate a class hierarchy. So this is way more closer scenario to our second refactor that we put together. That's exactly this relationship right here. We had class match reader. It had a reference to some other class or some other object. On Match Reader. We had a reader property and we said it was going to be a type data reader that was an interface. So because we're using an interface here, we can freely swap what object we assign to reader. As we said several times, right now we're making use of CSV file reader, but we could easily create some other type of class like maybe API reader that got information from some totally other different location inside of our application. And so we can easily swap out these different objects to distinctly change the behavior of Match Reader without having to actually really change any code. So that's why we generally like to make use of composition. It's sometimes a lot easier to make an object with a lot more reusable code, and it keeps us from having to duplicate code all over the place if we just want to make like one very small change to some particular attribute. Now we're going to see instances of composition throughout the rest of this course, and we're going to continue to look at some more instances of inheritance as well. So we can continue to get a little bit more of a distinction between both approaches. So let's take a quick pause right here. There's one last topic around composition and inheritance that I want to talk about in the next video.
