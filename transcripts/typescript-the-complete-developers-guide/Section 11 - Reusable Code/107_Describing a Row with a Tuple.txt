# Describing a Row with a Tuple

We've converted all the values inside of our row array into the appropriate type of values. But now we are kind of stuck with this any definition right here. So we need to replace any with some type annotation that's going to describe this array right here. Now, the only issue is that if we treat this thing as an array, we would have to add an annotation that looked a little something like this. We would have to write out something like this is going to be date or string or number or match result. And then we could put on square brackets like so to indicate that this is going to be an array that has a date or a string or a number or a match result inside of it. This is not super ideal. You might recall that we spoke about this annotation type right here a little bit earlier on inside the course. If we ever wanted to access an element inside of this array now, we would have to do some type of type guard, which we discussed earlier as well, that would allow us to treat this first element as a date or these two elements as strings or these two as numbers. So we did talk about another data structure available to us inside of TypeScript that can address this issue very well. You might remember that we spoke about tuples much earlier. So remember a tuple looks very similar to an array, but it's meant to organize elements inside of it in a very specific order. So the example that we went through with tuples, we said that we might have an object that looks like this, that represents a drink and a drink object had color. That was a string carbonated. That was a boolean and sugar. That was a number. Another way that we could represent this object right here was with a tuple. So we would take Brown and stick it in as the first element inside the tuple. Then carbonated was the second element and sugar was the third element. When we were making use of a tuple, we're saying that the first element is always going to be a string representing the color. The second would always be a boolean representing carbonation. And then the third was always a number representing sugar content. If we ever scrambled this order, then our tuple would not make sense anymore. So we always had to leave the tuple in this exact order. So it kind of seems like we are in a very similar case with the row of data that we are now working with. We now are working with some row where each element inside of here is at a very specific position. The date is always first. Then we always have the home team, then the old team and so on. So I think that rather than treating this thing as a plane array that might have a variety of different types, we should instead create a new tuple definition. And this tuple will describe this data structure right here where we first have a date, then string, string, number, number and so on. So let's try going down that route. Let's try using a tuple to describe this row. Now, you might recall the first thing we have to do to make use of a tuple is add in the tuple as a new type. So at the top of our file, we need to add in a new type definition or a type alias that describes the structure of the tuple we're going to be working with. So I'm going to go back over at the top of the file. I'm going to add in type match data. Equals and then the tuple structure. So remember this defines a new type called match data and we're going to say that a match data tuple is going to have this very specific ordering of types inside of it. So the first element will be a date. Then a string, then a string, a number, a number. One of the possible values out of match result, which we would express by just writing out match result. And then finally, the string for the referee. So string. Like so. All right. So we've now defined a new tuple inside of our app, and we're calling it Match Data. So now we can go back down to our mapping statement. We're going to update this definition or this type annotation right here. So we're going to say that we're going to call this mapping function. We're going to take in and a row of or a array of strings. And then we're going to return something right here that has the exact same structure as the match data tuple. Okay. So now TypeScript is going to treat this thing. We are returning as though it were the match data tuple. It knows that this row or this array or this tuple, however you want to refer to it, it knows that the first element is going to be a date, then a string, string, number, number, and so on. Okay, so this looks pretty good. However, we still have a little air up here. So if you take a look at this air, it says that we are now trying to. Well, it's kind of hard to read this air, but essentially what it's saying is that we are returning an array of tuples right now. So we have an array and inside of it are a bunch of different tuples that conform to the matched data type. And we're trying to assign that to a property that is expecting a 2D array of strings. So now the last thing we have to do is update our data property right here and change its type annotation because we are no longer going to be working with data that is a 2D array of strings. Instead, we've got an array of tuples. So we can update our type annotation right there by saying match data and then square brackets like so. Notice that we'll only have one set of square brackets right here. The reason for that is that when we say match data, we are kind of implicitly saying that match data is in array. So this is saying we have an array of arrays and those inner arrays have a date string, string, number, number and so on. Okay. So this looks pretty good. If we now flip back over to index, it's. We can take a look at, say, reader, actually, let's write out a council log right here. We could do something like reader data and then put down our index notation. We can do something like zero. And when we write that out and hover over it, TypeScript should be telling us, okay, it's not quite telling me here. Let's try doing it right out this syntax just a little bit differently. Let's try doing like const. Date of first match. How about that? A little bit more detail than you did, but let's try it out so we can then do a reader data at zero. At zero. Oh, that's why I didn't show up. I was not putting in the correct number of indices. So we're now getting the very first match inside there, and we're pulling out the first element which we know is going to be a date. So if we now hover over date of first match, we will see type inference kick in. TypeScript knows that readers data at the first row at the first index is going to be a date and so it correctly annotated date of first match as being of type date. All right, so this looks pretty good. So now whenever we start to work with our data property and the elements inside of it, TypeScript has types for all those different elements and understands what different types of values we are working with. So now we can try to access that data array, reach into it and maybe do some like numerical calculation using all the different goals that were scored. Or we can access that data object and call some different methods on it to try to pull out like some very precise date related information or something like that. All right. So this is definitely a good improvement. So let's take a quick pause right here. We're going to come back to the next video and move on to our next big issue.
