# Re-Integrating Attributes

Now that we've got class attributes all put together, we're going to wired this thing back up to our user class. So I'm going to flip back over to user and I will import it up at the top. Okay. So now we can wire this up to class user. We can do it in exactly the same way we did before. So I could say something like Public attributes is going to be an instance of attributes and we need to tell this thing what the generic type is going to be. So we need to kind of preload it or specify the type T as being the user props interface. So then we can create a new instance. Of attributes. We can again specify that generic type. And there we go. Now, just one issue here. You'll notice that we call the constructor, but we are expected to pass in one argument. And that one argument needs to be the initial properties of our user, because remember, we're going to take that initial argument. It's going to serve as our data property. So that kind of forms a little bit of an issue inside this file. You see, we've been using this kind of pattern of setting up these hardcoded sub modules inside of here. But now one of these sub modules is going to need to get an argument. And that argument in theory needs to come from the constructor of class user. Why is that? Well, remember, our original goal was to call new user and pass in some starting properties for the user, like my name, age and so on. So it's clear that in some cases we can kind of hard code these different sub modules, but in this case we need to specify new attributes from the constructor so we can receive this argument right here and pass it through to new attributes. So we're going to set up attributes just a little bit differently. All right. So I'm going to delete the initialization. And now we can define our constructor. We're going to receive some starting attributes for this user. So refer to that as simply about adders, and it's going to be of type user props. And then inside of here we can say this attributes is going to be a new. Attributes. With a type user props loaded in and we can pass through adders like so. Okay. So that definitely works. Now you can probably tell that we've got two styles of initialization going on inside of here, so sometimes we don't need to receive anything coming through the constructor. So doing this same line initialization works out just fine, but we've now got some properties that have to be initialized through the constructor. So as you might imagine, well, this could possibly get really nasty really quickly. For example, we could just plain forget to initialize one of these different little sub modules that we have set up inside of class user. Super easy mistake to make. And as you can see, I don't immediately get any feedback from TypeScript around this problem. So it's clear that maybe this pattern that we're using of an inline initialization will not work out quite as well as we had expected. Nonetheless, let's kind of stay the course here until we see that this really is a very big issue. Okay. So we've now wired up attributes to our user class. So let's take a quick pause right here. And in the next video, we can start getting used to our user class and understanding how to still call all the previous methods. So we were making use of on it. So quick pause and I'll see you in just a minute.
