# An Advanced Generic Constraint

All right. Now that we've got a deeper understanding of those two different rules, I want to show you one more quick diagram and then we're going to refactor our Git method. So here's a quick diagram. Okay. So remember, we've got our user props type. The user props type is an interface and an interface in general describes the structure of an object. In the case of user props, we are seeing that there are going to be three key value pairs in an object that satisfies this interface. There is a key of name that's going to have a type of string, a key of age that's going to have a type of number and a key of ID that's going to have a type of number as well. And then remember what our ultimate goal is from our git method. We essentially want to say that whenever we call a jet, we want to pass in a string. And then ideally what we want TypeScript to do is use this string right here as a sort of lookup inside of that interface. So we really want TypeScript to somehow take that string, look up that string inside of user, prop the interface, and then understand that the key of name corresponds to a string. And so we want TypeScript to somehow understand that whenever we call get with a string of name, we will return a string and then something similar for age, but with a number and with ID as well. So essentially we want to have a very close relationship between this variable right here, the argument and whatever gets returned. So with that in mind, let's update our git method. All right. The first change I'm going to make here, I'm going to change the argument name of prop name to key instead. Now, this is no significant refactor whatsoever. There's nothing important about this. This has no change on the behavior of our application. The only reason I'm doing it is to use an argument name that has a little bit more meaning to it. Having the word of key in there is just going to make understanding some of the syntax we're about to put in here a little bit easier to understand. Again, no functional change. So here's where the real refactor starts. We have seen generics in use of with classes and we've also seen generics around standalone functions as well. But we can also use generics with method definitions too. So to do so, I'm going to add in a set of angle brackets right after get. And then inside of here, I'm going to start to write out the crazy part. So I'm going to say K extends key of T. Then on my argument right here for key, I'm going to say that I'm not passing in a string. Instead, I'm going to pass in something called K or something of type K. And then finally for my return type annotation, I'm going to put in T of k like so. All right. So here's the first thing I want you to understand. The first thing to understand is that the letter K inside of here is not some special operator, just like tea. It is meant to represent some kind of type. We can use any identifier we want in place of K. So rather than putting in K, we could say like key of object or something like that, any name that you want whatsoever. But usually by convention we will refer to this as K. Okay. So with that in mind, let's now break this down part by part. So the first little bit right here sets up a generic constraint. So remember we spoke about constraints before. A constraint limits the types that K in this case can be. We saw a constraint back over on sync. So here's sync right here. We set up a constraint and setting up the constraint limited the types that t could be. So we're doing the same exact thing with Kay right here. So in this case we are saying that the value of K or the type of K can only ever be one of the keys of T. So in total, this step right here is saying that the type of K can only ever be one of the different keys of T. If we pass in a T right here of prop types or some user props, then K can only ever be the strings, name, age or ID. So again, that segment right there, that is our constraint. We are limiting the different types that can be. So now inside of our argument, we're saying that whatever argument we're passing in is going to be of type K. Because K can only ever be one of the different types of tea or different keys of tea, which in our case right now is only name, age or ID. That means that we can only ever call get with either name, age or ID. That's it. Remember, we can treat strings as these strings right here on this object as though they were types. So between this first constraint right here and this type annotation right here, we are saying that we can only ever call git with name, age or ID as strings. And then finally, this part right here, our return type annotation is essentially the same as a normal object lookup. So remember whenever we have an object like again, let's say. Colors. And maybe this has a property red. That's red. We can look up something in there like so that does a lookup and then returns whatever that value is right there. So we're doing the same thing right here, but with a type. So we're saying look at the interface of T, which in our case we are imagining is only going to be user props. So look up that interface T and then return the value at the key of K. We've already established that K can only ever be name, age or ID, so TypeScript is going to look up name, age or ID and return the corresponding type, which would be string or number. And that's pretty much it. Now, I know this has been pretty nasty and I know the syntax right here is pretty nasty. But if you really break it down part by part, I'm convinced that it does make a decent amount of sense. So let's go through it one more time. The first part right here, we are setting up a generic constraint. We are saying that k can only ever be one of the keys of T. She is going to be a sort of variable. So remember, it's not only ever going to be user props, but right now the only really interface that we have inside of our application to use with this class is user props. So we are imagining that DT is going to be this interface right here. So with this constraint we are saying that K can only ever be name, age or ID. We then try to make use of that type right away inside this argument. So we're saying that key is going to be of type K, which, like we just said, can only ever be the strings of name, age or ID. And then finally, as a return, we are saying take that type T, whatever we passed in when we are referencing attributes and look up the type K inside there. So take name, age or ID. Look it up inside this interface. And that right there is the type we are returning from Git. All right. So there you go, two times through. So with all that in mind, let's now try to write out a little bit of code inside this file to actually test this thing out. So I encourage you to write this code along with me so you can hover over some of these different definitions and see their different types. We are going to delete all the code, this like test code that we're about to write. Nonetheless, I do encourage you to write it. Okay. So up at the top. I'm going to import user props. From the user file. Then down at the bottom. I'll say Konst Adams is new attributes. We're going to plug in a type for T of user props and we'll put in some starting attributes. And again, I'm going to new line this just to make it easier to read and actually the proper way of new lining. This I'm kind of taking a shortcut here. There we go. That's much better. Okay. So we'll have an idea of five, an age of 20. And a name of whatever. So now I can create a new variable called name and I can call adders target name like so. If I now hover over name, you'll notice that it has an annotation of string. And so we have now achieved our original goal. We can use the Git method and get back some variable that has the correct type annotation on it. Likewise, we can do the same thing for age as well. So adders don't get. Age. We now hover over this, we'll be told. Age is definitely going to be a number. And then finally with ID. It is definitely going to be a number as well. And we got this behavior all because of this ray syntax on here. All right. So that's pretty much it. That's how we got attributes working, just the way that we would probably expect this thing to work in general. So if this syntax right here is still confusing, I encourage you to kind of rewind this video and watch my explanation again, because I know I just repeat it two times in a row, so I'm not going to just repeat the same thing again. Again, the whole thing we're doing here is setting up the constraint. We are limiting the different type that we can pass in as the argument. And then we are looking up the given key on the interface of T to understand what type of value we are returning. That's what's going on with each partisan tax inside there. Okay. So now that we've got this all fixed up, I'm going to delete all the sample code, which is the stuff down at the bottom. I'm going to delete the import statement at the top as well, and that's pretty much it. So let's take a quick pause right here. We're going to come back the next video. And just like those other two classes we created, we need to integrate attributes back into our user class. So quick pause and I'll see you in just a minute.
