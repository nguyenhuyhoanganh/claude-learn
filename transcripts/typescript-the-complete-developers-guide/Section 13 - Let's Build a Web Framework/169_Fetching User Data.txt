# Fetching User Data

In this video. We're going to move on to doing our fetch method. Remember, the idea here is that fetch is going to first need to somehow get the current ID off of attributes and then only if we have an ID are we going to call the fetch method on sync. We're then going to wait for our request to be resolved. We're going to get a response back from our JSON server. We're then going to take that information we get and set it on our attributes instance. There's definitely going to be a very high level of coordination between attributes and sync in this case. So let's get to it. Back inside of class user, I'm going to define my fetch method. And I don't really expect this thing to take any arguments and I don't expect it to return anything. So I'll give it a void, return type and no arguments listed. So now the first thing we need to do is get the ID of the current user. So I'll do const ID is this dot attributes target id like so. So now we need to make sure that ID is actually defined. Remember ID is an optional property, so sometimes id might be defined. Other cases we might not have an ID. We're never going to have an idea if this model is brand new and it's solely the presence of this ID that indicates whether or not this instance of a user has a representation on our backend server. So in other words, if we don't have an ID, we do not want to attempt to fetch any data. So I'm going to say if type of ID is not number. In other words, if this thing does not exist, if it's not a number, if it's actually undefined, then I want to throw a new error. That says cannot fetch without an ID like so. Now, if we get past that if statement, we can then go ahead and do our actual fetching. So to do the actual fetch, we're going to use the fetch method on our sink module. So I'll say this async fetch, I'm going to pass in our ID and remember when we call fetch, we return a promise, a promise that's going to eventually resolve with the data related to this user. So we need to chain on a dot then statement. And this then is going to eventually be called with some response. And it's type is of Axios response. And we immediately get an air around that because we don't have an actual value inside of here for Axios response or an import for it. So I'm going make sure that I import that type at the very top. So I'll import. Axios response from Axios. So now that air has gone away. And then I'm also going to annotate the return type of this era of function as void as well, because I definitely do not expect to return anything. So now inside of here, remember, a response has a data property that represents the actual information that was given to us coming back from our JSON server. So we're going to take that information and just throw it directly into set. So I'll call. This dot set with response data like so and I just did this set technically we might want to do this attributes set instead to directly reference the attributes property. Now this is kind of an interesting thing here, an interesting question. When we start making use of this coordination between different methods, do we want to use like our implementation of set inside of class user or do we want to reach directly into our attributes class or the attributes object? And that's kind of a question that I've struggled with myself in this particular case. If we reference this attribute set, then we're going to skip out on that trigger change event. And chances are we always want to trigger a change anytime we update our data. Because remember, that's going to be the primary tool that we use to tell other parts of our application that some information about this user has changed. So I think that in this particular case, we might actually want to rely upon the set function inside of user as opposed to the one inside of attributes. So we might want to just put this set after all, because now not only are we going to update our attributes of the user, but we're also going to automatically trigger that change event as well. Now you can definitely make the case to say maybe now are get of ID should also reference just simply this dot get as well and hey you know it's up to you in the case of Git well we don't really have any special coordination here. So whether we reference this target or this dot attributes target, well, it doesn't really make a big difference in the case of getting the ID. So in this case, totally up to you. All right, so that looks good. So now I want to test this out, so I'm going to save the file. Now I'm going to open up my DB JSON file. Remember, that's where all of our users are stored. And I'm going to find the ID of an actual record because I want to test this out. So it looks like there is an ID of one and it has a name of new name and an age of 9.99. So let's go back over to Indexed. We're going to create a user with an ID of one and then intend to fetch some data about them. Okay. So inside of indexes, I'm going to remove all the properties on my user when I create it and I'm just going to give it an ID of one. I'm then going to delete the console log for user name. I'm going to update the user on change callback right here to instead print out console.log. I was going to print out the entire user after it has been changed. And then finally down here, I'm going to update the user set to be a user fetch instead. So now, in theory, we're going to attempt to get the idea of the user, make a request to JSON server with an idea of one. We're then going to update the properties on our user via the set method and whenever we change some data tied to our user, that should trigger this change event. And so we should see a console log of the entire user appear and we should be able to manually inspect that user and see our relevant data on it. All right, so let's save this and see what happens. So I'll flip back over to my browser. Here's the console log for the user. And if I expand it, open up attributes and open up data. I'll see the age of 999 and the name of new name. So I definitely means that this entire system of fetching data and using it to update information about our current user is working perfectly. Awesome. It's now the last thing we need to work on is our safe method. So quick pause right here and we'll take care of save in the next video.
