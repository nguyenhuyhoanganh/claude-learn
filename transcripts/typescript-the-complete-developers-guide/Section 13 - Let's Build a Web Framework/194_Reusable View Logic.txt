# Reusable View Logic

Our user form is now in a pretty good state. It's now in this video. We're going to start to think about extracting some reusable logic out of this thing. Now, remember, originally I said that we were going to work on figuring out nesting of different views within each other, but in this case, we only have one view right now. And so if we wanted to wanted to figure out that nesting stuff, we would essentially have to figure out how to nest user form inside of user form, which would probably end up being really confusing. So we're going to instead focus on giving ourselves the ability to easily create views, and then we'll start to worry about nesting one view inside of another. Okay, so then mind, let's get to it. We're going to discuss how to extract some reusable logic out of user form. And we're going to worry about how we're going to think about how to do this extraction first by using composition and then think about how we could do it using inheritance. All right. So a quick diagram here. So just a quick reminder of all the different methods and properties that currently exist inside of user form. We've definitely got a lot of different properties and methods right now. Some of these different methods are absolutely reusable in nature. So for example, if we think about the render method towards the bottom. Right now, this method is absolutely reusable in nature. There's nothing inside of here that's 100% custom tailored towards user form. All the logic we have inside of here probably needs to be done for any type of view we create. On the other hand, there's definitely some methods that we have that are not reusable in any way and should only belong inside of class user form. So for example, on set name click or onset age click, these are both two functions that really only belong inside of user form and we probably don't need to worry about extracting them in any way. So we're going to first think about how we can separate these two pools of methods using composition. So here's my idea. I think that we should possibly create a new class called HTML render. This class right here we would rely upon to take some HTML and put it on the screen. Essentially, it would encapsulate all the current reusable view logic that we have inside of user form. Our user form would then have a reference to an instance. Of that HTML render. So maybe we would add on a new property called like render and it would be of type HTML render like so and so. This would be a reference to an instance of that class. So then any time that we try to render class user form, class user form would delegate the actual rendering responsibility off to its instance of HTML render. So essentially, we would just have to teach or put functionality inside of this render to tell it exactly how to form up some HTML, put it into the DOM, respond to user events and so on. So let's try going down this path and then figuring out whether or not it makes a lot of sense. So we're going to go through each of these different properties and methods one by one, and figure out which of them should be moved over to HTML render. The real decision here on whether or not the different methods and properties should be moved over is essentially a question of whether we expect to be able to reuse these different methods inside of some other view. So let's go through each of them one by one. All right. So first off, parent of type element. Well, everything that wants to be shown inside the dome needs to have this parent reference. So chances are we would want to move that reference over to a HTML render. How about model user? Well, this one's kind of confusing because on the one hand, we definitely want every HTML render to have a model that it can bind to so that it can automatically render itself any time that some data tied to that model changes. But on the other hand, right now, the property model is of type user. So that's definitely custom just to the user form. We could definitely figure out some way, however, using generics to make that type right there a little bit more flexible. So I think that we should move over this model property to the HTML render so that now 100% of the time we will know that yes, we can re render our HTML whenever that model changes. Okay. How about template? Well, this one is also a little bit confusing. Without a doubt. The HTML renderer is probably going to have to have a template method or some method that returns a string to be turned into some HTML. However, the template method itself is going to be very custom to each view that we put together. So user form is going to have one template that shows some input elements and buttons, but user show is going to have a very different template that's going to have an H one and name and age and so on. So I think that the template method would stay with user form and we would have to somehow make sure that this HTML render can access that template method. Okay. Let's keep going. How about the render method? Well, without a doubt, everything that needs to be shown on the DOM needs to be contained or get access to essentially this render method. So I think we would definitely move that one over about events map. Remember Events map is the method that's going to return an object that describes all the different events and elements to bind those events to inside of this specific view. So this is another one where the HTML renderer is going to be responsible for binding these events, but the actual event definitions are going to be very different for each view class we create. So I would probably use that or leave that with user form. All right, how about blind events? Well, that's the actual event. Binding and logic. Definitely. Move that over here. Find model. Yep. Definitely move that over. Every view we create is going to have to bind to a model. And then how about onset and excuse me, onset named click and onset age click. Well, those are definitely both methods that are custom for user form, so I would probably leave them over here. And then finally, yeah, the render reference that's going to stay with user form. Okay. So this would be one possible way to split up our composition. So the only issue with this approach is that the render method and the bind events methods expect to eventually call something called template, or essentially they need a template to work correctly. So render essentially needs access to that template method and bind events really needs access to the events map as well. So not only do we have user form that needs a reference over to this HTML render, but I'm kind of imagining a scenario where for our HTML renderer to work correctly, it would also have to have a reference back over to our user form. So maybe we would give our renderer a view property and it would be of some like type. That's an interface that we would call a view, and this interface would say that you have to have a template method that returns a string and an events method method or an events map method that returns an object as well. So we're kind of talking about like a bidirectional relationship here. Now, here's the thing. Any time that we end up in this kind of scenario where we have this bidirectional relationship, it's usually generally a sign that maybe composition is not the best idea, or at least the division of methods that we have between the two is not the best idea. So that's what I'm kind of thinking here. I think that maybe composition in this particular case is not a great idea because to make this stuff really work appropriately, we would have to make sure that we've got this, like, bidirectional relationship. Now, one way to get around this would be to say that our HTML render, maybe its render method has to be called with a template that is a string and maybe bind. Events has to be called with some object that specifies all those different events that need to be bound. So if we set this up, then we can make sure that we had some kind of like delegation inside of user form. So maybe we would like re render or re implement a render method over here and a bind events method over here as well. These would not take any arguments. All these things would do. It's called either template or events map and pass the results of those onto the render method over here on HTML render. So this would definitely be one way to solve this problem. Now we only have a one way relationship. However, I want you to keep something really important in mind. We're trying to build up a like web framework here, and generally with a web framework, we want to make building out views and building out these different components as easy as possible. So if we start to talk about how class user form has to implement, render and bind events and then put together that delegation, well, that means that we are essentially expecting our users to implement these different methods for us. And personally, I don't really think that's super realistic. And just to be clear, when I say our users, I'm talking about like the users of our framework, the developers who are going to use our code. So we're now expecting that they're going to implement these two methods and set them up to correctly cull, render and bind events. And honestly, like I said, I just don't think that's super realistic. So once again, I'm not sure that composition is the best solution here. Maybe there is a better way to divvy up these methods, but to be honest, I think that inheritance is a pretty straightforward solution here. So how would we do this if we wanted to use inheritance? Well, essentially we would have the same division of methods that we just saw. We would probably create a class view rather than calling it like HTML render. We just call it like class view or something like that. And then we would have user form extend, class view. So then all of our different reusable logic that's currently on user form, we would move over to class view. So that would be like parent model, render bind events and bind model like so. Okay. So now if we did this, just one other very small issue. If we set up this kind of relationship, remember render, let me finish this diagram really quick. There we go. Our render method and our bind events method as well. Try to call some methods that presumably are not going to be implemented by class view. If I go back over to render inside of my user form right now, here's my render method. Remember, this thing is going to call this DOT template and if we go to bind events right here, this thing is going to call events map and it's going to try to access those methods on itself. But class view should not be in charge of implementing templates and events. Map Those are going to be two very custom methods that have to be defined by user form. Nonetheless, we're going to have references to them inside of class view. So if we just try to call those methods on like this without them being defined, we'll very quickly run into an error message. Now, this might be a problem that sounds really familiar. Essentially what I'm saying is that we would want to set up class view over here as an abstract class so we're not going to ever implement or instantiate view directly. Instead, it is only ever going to be used to extend another. By implementing this thing as an abstract class, it would allow us to add in some abstract methods to its definition. So we would want to add in an abstract template signature and an abstract events map as well. And so that would solve any issues with render or bond events trying to refer to this template and this DOT events map. So in this scenario, if a developer wanted to now use our framework, all they would have to do is extend class view. And the instant they did, they would get a big error notification saying, Hey, your class user form has to implement a method called template that returns a string and a method called events map that returns some kind of object as well. So on many levels, I think that using inheritance here definitely just makes a pretty good amount of sense. Okay. So that's my thoughts on the topic. Now once again, I want to say that you could probably sit down and put a lot of a lot of thought into this and think of a really elegant way to separate some kind of like renderer out from our user form. I'm positive that's got to be possible. Nonetheless, remember, when we start to think about composition versus inheritance, we should not always just target composition because like, Hey, that's the cool way to do things today. We always have to pull in the right tool or the right design pattern for the job. In this case, I think that we've laid out a couple of reasons to say that inheritance just plain makes a decent amount of sense here. Okay. So all that in mind. Let's take a quick pause right here. When we come back, the next video, we're going to start to set up abstract class view and work on its implementation.
