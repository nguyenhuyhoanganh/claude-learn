# Setting Data While Triggering

In this video, we're going to start to re-implement these three different methods that need some coordination between attributes and sync and eventing. So the first one we're going to take care of is set. Now, you might be thinking that set would be a simple pass through method. Well, normally it would, but we're going to add in a new requirement of set. You might notice that several times we have bound to an event name on our eventing class right here or eventing object. And I've been using an event name of change over and over and over again. So it turns out that we're going to make the change event a very special event inside of our class of user. We're going to say that any time that we set a property or we essentially update some data on our user, we're going to trigger an event named Change. The goal here is that we're going to allow other parts of our application to essentially listen to our instance of user and get some notification whenever users data is changed. So any time we call set, we're not only going to update our data on attributes, we're also going to trigger a change event. And that's what's going to allow other parts of our application to automatically update any time we update users information. All right. So let's get to it. I'm going to flip back over to my editor. And inside of class user at the very bottom, I'm going to define a set method. Now this is going to be an instance. We're going to have to take in the same arguments as we would normally would for our attributes. Method of set right here. So any time that we call set, we expect to receive some update object and it's going to have inside of attributes class. TW But inside of users we know essentially what TT is. It's always going to be over here, user props. So we're going to call set with some update that's going to be of type user props and we're not going to return anything. Then inside of here, we're going to take this attributes set. We're going to call that with the update object that we just received. And then, like I said, simultaneously, we're also going to trigger a change event. And this is what is going to tell other parts of our application that, hey, something just changed about our user. So we will call this dot events, dot trigger and we're going to trigger the change event. All right, so that's it. I'm not going to change back over to indexes again and we'll do a quick test. So I'm now going to delete the manual trigger right here. I'm going to leave behind the on change. Now I'm going to try to update some property about my user. So I'll do a user set. Name and I'll set it to new name. So as soon as we update that property on user, we should see a console log of user was changed. So I'll save this. Look back over. And I'll see user was changed perfect. So now the entire idea here in the like kind of context of our greater framework that we're working on is we're going to eventually put in some code that says essentially something like user was changed. We probably need to update some HTML. That's what this change event is really going to do for us. We're going to use this change event to get some notice that, hey, we need to update some HTML to show the new name to our user on the screen. Okay. So that's pretty much it for set. So let's take a quick pause right here. When we come back in the next video, we're going to move on to fetch and save. So quick pause and I'll see you in just a minute.
