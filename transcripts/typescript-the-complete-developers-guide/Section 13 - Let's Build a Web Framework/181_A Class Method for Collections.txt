# A Class Method for Collections

In this video, we're going to try to move this new collection stuff into a better location than our index file. So, to be honest, I think the best location to create a new collection is going to be inside of our users file. I think that we should create a second static class method inside of here that's going to return a collection of users. So inside of class user or inside of user excuse me. At the very top, I'm going to import collection from election. And then I'm going to add on a new static class method. Called Build User Collection. This is going to return. An instance of collection. We have to specify the generic types for this thing. So the first is going to be our user class and the second will be user props. And then inside of here we can return essentially everything we just put over here inside of indexes. So I'm going to copy all this stuff or cut it out of here. I'm going to go back over to user TS. I'm going to return that big block of code. And now in this case, we've already defined our route URL inside of here. It's right there. So I can reuse that root URL variable. All right. That's pretty much it. So now I can save this file. I can go back over to your indexes. And now to create my collection, I would call user dot build user collection like so. So now I no longer need to import user props. I no longer need to import collection, and that's pretty much it. So we can now save this file. Flip back over. There's our collection again, and it still has all of our different users inside of it. All right. So that's pretty much it for collections. We've now got a pretty solid base class to use for representing a collection. It is generic in nature, so we can specify the type of model that it's going to contain and also the type of JSON data that we're going to get back from our API. So this looks pretty great. Let's take a quick pause right here. And in the next video, we're going to finally move on to actually producing some HTML and getting that HTML visible on the screen. So I'll see you in just a second.
