# Saving Data

In this video. We're going to move on to our last big method of save. So this one's going to be a little bit challenging because we essentially need to pull off all the different properties of our user. We're going to kind of amalgamate them all together and then eventually call Save. And that should save all of our information about this user to our JSON server. So let's get to it. So I'm going to flip back over to my editor and I'm going to go to my user file at the bottom of my user class. I'm going to define my save method. And when I call save, I don't really expect to pass in any different arguments this thing. And I also don't really expect it to return anything either. So I'm going to give it a return type of void and no arguments. So the first thing we need to do is attempt to pull off all the different properties off of our attributes property. Right now we only have a method on attributes to pull off one property at a time. Now I'm going to suggest that maybe that is super not useful. Maybe it would be a lot better if we had a method on attributes to just get all of our different properties in one single go. Because that means that we could just call this new method, get all of our properties, and then pass them all directly into the save method on sync. So let's open up attributes and add in a new method that's just going to return all the different properties that it has. So instead of attributes, I'm going to add in a new method called Get All. Now. I don't expect this thing to take any arguments, but I definitely expect it to return some value. So it's going to return in our case, user props, which is represented inside this file and or inside of attributes as t remember, t is specifying the interface. That is the shape of data that we're actually working with or all the different properties that we're trying to store. So I'm going to mark this thing as returning Type T. Then inside of here to return all of our data inside of attributes. Well, it's just going to be return this data like so. And that's pretty much it. Okay. So now we can close this file after saving it and then back inside of class user. I'm going to go down to the bottom. So now to actually save this user or save all their properties that they have, we can reference this sync save. We have to pass in all the data about the current user. So to get all that information we can call this attributes get all. And that's pretty much it. So now any time that we call save. Yep. Get all the properties, throw it into save. That's all it is. Now, the one thing I want to point out here is that it might be kind of useful to tie an event to a successful save. So we might add a new trigger to our class. Or to say right here, we can say after successfully saving all of our data, let's trigger some like save event so that other parts of our application know when a user has been successfully saved. So to do so, we could chain on another dot then statement. This will also be called with some response argument that will be an instance of Axios response. And I don't really expect it to return anything. It's all put on void on there as well. And then inside the dot, then statement, we can do a this trigger. Save like so. And the other thing that I want to mention here is if we wanted to do any air handling, we could also chain on a catch. And inside of couch we could do a this trigger. Air or something like that. Now, these events, I'm just kind of making up on the top of my head, but it means that if someone starts to listen to an instance of a user or they call on which we defined up here, they call on and say that they want to listen to the save event or the air event. Then any time we save successfully, that save callback will be invoked. And the same thing for air as well. Okay. So let's now test this out as well. So I'm going to save the user file. I'll flip back over to indexes. At the top. I'm going to add in some new properties to this user, so I'll give it a name of about newer name at an age of zero. I'm then going to set up an event listener for Save. And then after that, I'm going to attempt to actually save the user. So now in theory, when we save the user, that will make the network request to our JSON server. If it is successful, we're going to trigger the save event, which should call our callback right here and we should see our user console logged out to the console. So let's try this out. I'm going to save the file, look back over my browser and there's the console log of the user. If I expand attributes, I should see zero and name newer name on there. Perfect. And if I open up my DB JSON file, I should see that information reflected inside there as well. So here's DB JSON and I see name of newer name and age of zero. Cool. All right. So that's pretty much it. I think we've now reimplemented all of our functionality back on class user using a composition style approach. So this is looking pretty darn good. But now we're going to very quickly run into the final big issue with the data side of our framework, and that is we now have a class user that is fully functional and it has a ton of functionality tied to it, but it's only good for users. So in the next video, we're going to start to think about how we can extract functionality from class user right here and use it for creating other types of models as well, like a model for a blog post or an image or a comment or whatever else. So quick pause and I'll see you in just a minute.
