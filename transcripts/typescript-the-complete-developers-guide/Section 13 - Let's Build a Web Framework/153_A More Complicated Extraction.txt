# A More Complicated Extraction

In this video, we're going to start working on another extraction of functionality out of class user. So we're going to try to create a new property on class user called Sync, and that's going to be an instance of a class called Sync. And this sync thing is going to be a class that gives us the ability to save and fetch information about a particular user from our back end server. So essentially all we're really saying here is that inside of class user, here it is right here. We want to scroll down to the bottom. Here is the fetch method and the save method. We want to extract both these models excuse me, both these methods into a separate module. Now, you're going to very quickly find that extracting these methods is going to be way more involved than the eventing stuff we just did. So this will be a really fun extraction. We're going to learn a lot along the way. So to get started, I'm going to first create a new file inside of my model's directory called Sync. It's. Then inside there we'll define a class and export it called sync. And now right away. Well, let's just do the same thing as what we did last time. I'm going to go back over to users. I'm going to find the fetch method, cut it and paste it over inside of sync. Then I'll go back over to user dots again. I'll find the save method. Cut it. And paste it inside of Sink as well. Like. So it's now inside of class sync. I've got fetch and save. Now right away, there's a couple of errors around Axios. We could probably fix those up really easily. I'm going to go back over to user dots one more time. At the very top, we've got a now unused import statement for Axios and Axios response. So I'm just going to cut that. And paste it at the top of class sink like so. Now just about half of our errors go away. And here's the thing, the remaining errors. Well, this is where our source of trouble is going to come from. So take a look. We've now got an error around this set. We've got an error around this dot. Get another one around. Get right here. And this data in this data. So a ton of code inside of class. Sink is now trying to refer to different properties that are only on our user class. So this means that we're not going to be able to do a very simple cut and paste job for class sync instead. We're going to very clearly have to have a very well defined relationship between class sync and class user. Because class user cannot work correctly without sync and sync very clearly cannot work correctly without class user. So to fix up these kind of dependencies that both classes have on each other, let's take a quick pause. When we come back in the next video, I'm going to show you a couple of diagrams to lay out different options of how we can get these two classes to work with each other very nicely.
