# Triggering Event Callbacks

All right. In this section, we're going to make sure that our model has a trigger method assigned to it. Once again, this is going to allow us to trigger all the different callbacks registered to some particular event. As a quick reminder, we don't have any special events inside of our application yet. We're building out this system first and we'll very quickly see why we are adding in this functionality in just a little bit. Okay, so let's get to it. So inside of my user class, underneath on, I'm going to add in a new function called trigger. We're going to call this thing with some event name and that's going to be a string. I don't expect this function to return anything, so I'll give it a return type of void. So now inside of here, we first need to see if we have some registered events with this given event name. So I'm going to pull off that array of callbacks. I'm going to call it handlers again and I'll say this dot events. At event name like so. So now remember events right here at event name that can be an array or undefined one or the other. So we need to check to see if handlers is actually defined and if it is an array. So to do so, we're going to say if there are no handlers, like if that variable just doesn't exist, if that is undefined or if there are no handlers inside there. So handlers length equals equals equals zero, then we will just return early. So then if we do have some defined handlers array and we do have some number of handlers in there, then we will call them all, call all those callbacks right after we have that early return. So to do so, we can say something like handlers dot for each. And we'll say for each callback inside their. Just run the callback and that's pretty much it. All right, so I'm going to save this. And now we can do a real test of the whole venting system. So I'm going to flip back over to indexes. We've already got some registered events on here, but now to make sure that they're actually working correctly, when we trigger an event, we need to make sure that these callback functions actually have some implementation on them. So on the first one, I'll add in a console log of change, number one. On the second one, I'll add in a console log of change. Number two. And then on this last one, I'm going to give it a more reasonable name. I'll call it something like, I don't know, save. Maybe we have an event called Save for some reason. Who knows? And inside they're all added console.log of save was triggered. And then finally, instead of the console log at the bottom, I'm going to delete that console log. And now we can try calling trigger and triggering a change event or a save event. So I'll call a user trigger change and that's how we would use that. Okay. So now I can save this flip back over and I should see two console logs. Yep. Sure do. Change number one. And change number two. Likewise, I should be able to trigger a save event. So I'll change. Change, change, change to save. Yeah, that makes a lot of sense. Save that. Flip back over. Save was triggered. And then finally, we will test the case in which we are trying to trigger an event that has no callbacks whatsoever. So like some random event name. Like so. So I'll save that look back over and we get nothing at all. And most importantly, we didn't get any crashes either, which is good. All right. So this is looking pretty great. So we've now completed our eventing system. So now the last thing we are going to work on that is tied to a user is an actual sinking system as well. So we need to have the ability to make sure that we can fetch some data from some outside server about a user and also save some data about a user as well. So a quick pause and we'll start to take care of those in the next video.
