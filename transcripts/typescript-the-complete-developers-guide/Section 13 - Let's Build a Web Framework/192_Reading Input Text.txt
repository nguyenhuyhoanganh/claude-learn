# Reading Input Text

At the end of the last video, I told you that we were going to try to figure out how to nest one view class inside of another. But before we do, there's one last thing I want to do inside of user form. Notice how inside of user form we're supposed to have a text input that shows a placeholder of the current user details name. So we want to have the ability to type in a new name to this thing and then click on update name and have that update the name property of our user model. Let's add in all that functionality first and then we will start to worry about nesting one view inside of another. Okay. So to get started, we're definitely going to have to first make sure that we've got an input and a button and that we've got some click event handler on the button because that's what we really care about. We want to know when the button gets clicked. That's going to be our signal that we need to update the name of our user model. So I'm going to flip back over to user form. I've got my template method right here inside my template method. Yep. We've got an input right there and we've got a button right there. The button currently says Click me. So I'm going to change the text on there to just be like change name instead. So now we can set up an event handler for this button. Remember, if we just added an event handler and gave it a selector of button, it would find both this one right here and the one right underneath it. So to be a little bit more specific, I'm going to give my button a class of change dash name. I guess technically it should be set name just to be consistent with the button underneath it. Whatever. We'll go a set name. Okay. So now we can go back up to our events map. Here it is right here. So I'm going to watch for a click event on the dot set name. And any time that we click on an element that has a class name of set name, we will run a helper method called this dot on set name. Click. So now inside my class, I'll add in a helper method called on set name. Click so on set name click and remember we want to set this up as an arrow function so we don't have to worry about any value of this inside of here. The arrow function is probably not going to return anything, so we'll give it a return type avoid like so. So now inside of here, we need to do a couple of different things. Remember, this event handler is triggering off a button click. If you're coming from the world of Vue.js or React, you might be used to receiving an event object inside of your event handlers, and we could definitely do the same thing here as well. But the issue is that right now we've got an event handler on the button being clicked. So if we did receive an event object, it would not be a reference or would not contain a reference to the input element. So we cannot get at the text inside the input element by using an event object. Instead we're going to have to reach directly into the DOM minus input element and then attempt to read some text off it manually. It won't be that much harder. It's just like one or two extra steps now to reach into the DOM itself and find that input element. We can use our reference to the parent element. Remember, the parent element is essentially the HTML element that contains all of the different HTML. Tied to this current view. Every element inside the browser has a query selector method tied to it that we can use to search through all the child elements to this parent. So in other words, inside of on set name click, I'm going to call this parent query selector and I'm going to search for an input element inside there. And then I'll sign that to input. Okay. So now to actually pull some text out of that thing. We can call input value. That's how we normally get some text out of an input element. Like so. Because that's going to be the actual new name. So I'll assign that to name like so. And then we can update our model with this model set with that new name. And remember, any time we call set, we're going to trigger a change event. And so that should automatically update our template as well. Okay. So I'll save this and we'll do a quick test. So I'm going to flip back over. I'm going to put in a new name of like new name and then click on change name. And sure enough, we get the new name in there. All right. So that looks pretty great. One thing I want to mention, if you're getting a lot of errors around set name, click right here. You might see something that says like input type is not appropriate or something like that. If you see that error, that's totally fine. We're going to fix up some errors like that in just a couple of videos. So just hang on if you are seeing that error. Okay. So now that we've finished up more or less our user form, let's take a quick pause right here and continue in the next video.
