# Binding Event Handlers

Now that we've got our events map put together, we're going to go down to our render method and start to add in some additional logic. That's going to call that events map method. Take the object we get back, iterate over all the key value pairs inside there and set up all these different event handlers. Now we're not going to set up this logic directly inside of the render method. Instead, I think it would be really nice to make a helper method so that we don't kind of clutter up the render method in general. Before we set up this helper method, there's one more thing I want you to know about that template element that we are currently making use of. So the template element that we're making use of inside the render method has a content property and we just saw that content property inside of render right here. So the content property is the actual reference to the HTML that is inside of that template. So in this case, content for us might be a form with a label and an input or whatever else. The one thing I want you to understand is that this content property is of type document fragment. This is a type or an object that is included inside your browser by default. A document fragment is essentially an object that can contain a reference to some HTML, a document fragment in general. Its purpose is to kind of hold some HTML inside of memory before it actually gets attached to the DOM. So essentially, you're supposed to use a document fragment for exactly what we are doing right now. We are kind of prepping some HTML to be inserted into the DOM. Okay. So let's make a little helper method that's going to take in a reference to a document fragment. And then we're going to iterate through our events map and try to bind all these events to the HTML inside that document fragment. So right above render, I'm going to make a helper method called bind events. This thing is going to take a reference to a document fragment. So I'm going to call the argument fragment and it will be of type document fragment. And then I don't really expect it to return anything. Sole market is void. So now the first thing we need to do is get a reference to our events map right here so we can now iterate over that object and set up all these different event handlers. So I'm going to say const events map is this dot events map. So now we can set up a four in loop to iterate over all the key value pairs inside there. So let's say four let event key in events map. So Event key is going to be the strings. So like flick button or hover h one or drag div or whatever else. So the first thing we need to do is split event key in half based on that colon so we can get the event name and the selector for the element that we want to attach our event handler to. So to do so, we could say something like we'll use a little bit of D structuring here. I'll say event name and selector. Is going to come from event. Key split colon like so. Now this is some S 2015 syntax right here. When we call the event key, we're going to expect to get back an array with two elements inside of it. So we're using D structuring to get a reference to the first element inside there, which we're going to call event name and a reference to the second element inside there, which we're going to call a selector. So in our particular case, if we now did a console log of event name right here, I would expect to get back click. And if I did a console log of selector, I would expect to get a console log of button. So now with that information, we can now set up our actual event handler. So I'm going to look at that fragment that we passed in which, remember, is a reference to our HTML that we're trying to get ready to be inserted into the DOM. And I'm going to try to find every element inside there that matches this selector. So I'm going to say query selector. All We're going to find all the different elements inside that fragment, that match that selector. So I'm going to pass in this lecture. And that's going to give us back an array of elements that match that selector so we can then iterate over that array. And for every element that we match, we will attach whatever event we had referenced or whatever event handler we had a reference in this case, like on button click. So I'm going to do a for each statement over all those different elements that we return. And I'm going to say for each element and I'll zoom out for just a second so you can see the whole line for each element that we've found inside that snippet. We'll set up our event handler. So I'll say element dot add event. Listener. We're going to watch for the event name. Remember, that is coming from the first part of the event key, which in our case right now is simply click. And then as the second argument, we'll specify our callback function, which is the value inside of events map. So to get the appropriate value, we would say events map at event key like so. And that's pretty much it. So we can now call find events from our render method after we set up the template element so inside of our render method. Here's we set up our template element. Right after that, I'm going to call this dot bind events and I need to pass in the document fragment from the template element that we just created. Remember, the document fragment is essentially a reference to all the HTML that we're trying to get ready to insert into the DOM. So on blind events right here, I'll pass in template element dot content and if we hover over that it will actually even tell you, yeah, this thing is a document fragment. Okay. So that's pretty much it. So this is kind of a clever way to handle events if you don't have something really fancy like Gsx or something like that, to bind all your different events for you. So like I said, this is how we basically used to do it back in the day. All right. So I'm going to save this and we'll do a quick test. So I'm going to flip back over. And now I should be able to click on my button and see. Hi there appear. Yep. Let's do the same thing and we'll try doing like maybe a hover on the H one. So I'll add in a new key to my events map right here. I'll say find a hover on the H one. And any time that event is triggered, I want to call this dot on header hover and I'll add that in as a helper method inside my class. So I'll say on header hover. Oh, Mark, this says void. And inside of here I'll say console.log. Each one was hovered over. So now I can save this again, flip back over, I can hover over the H1. And you know what? I think I probably did that incorrectly. Let's change it rather than hover. Let's do like a mouse. Enter. I'm probably just making up event names here by mistake. Let's try a mouse enter instead. Yeah, there we go. Okay. That's probably why I was thinking of when I was saying hover mouse over. Okay, so it looks like that works and we can still click on the button that works as well. So that's pretty much it. We've now got any venting system built into our view. So let's take a quick pause right here and then move on to our next feature in the next video.
