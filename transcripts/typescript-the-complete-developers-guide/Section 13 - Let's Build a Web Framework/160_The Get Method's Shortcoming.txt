# The Get Method's Shortcoming

In the last video we spoke about how the return type signature of our JIT method is not quite appropriate. So right now it's either a number or a string that we are returning. For just a second, I want to imagine that maybe we put in a limitation inside of our application and we say that we can only ever have attributes that are maybe like a number or a string or a boolean. So we basically have the basic types covered here. So maybe we just run with this and we say, okay, we can only ever store these types of values, and then boom, all of our code would work just fine. Well, for the most part, just fine. If we went down this route right here and I do not recommend it at all, I want to show you a little bit of code that we would have to write any time that we want to access a property through this git method. Once you see this kind of difficulty, you'll then have a better understanding of why we want to get away from this union pattern that we currently have. So I'm going to write out a little bit of sample code. I encourage you not to write out the sample code just yet because I'm going to delete it very quickly. All right. So I'm going to try creating an instance of attributes with user props, serving as our type. And then we'll take a look at how awkward it would be to make use of the current git method. So at the top I'm going to import. User props. And then down at the bottom of the file, I'm going to create an instance of attributes. So I'm going to create a variable called adders short for attributes. I'm going to say this is going to be a new instance of attributes. I'm going to give the generic a type here. So I'm going to say find all instances of T inside this class and replace T with user props like so. And then I'm going to also pass in a starting set of properties or as we call it, data. So the initial data for user props here or attributes is going to be an ID of five about a name of whatever. And an age of 20 like so. Now I'm going to new line. This just you can see this a little bit more easily. There we go. That's better. Okay. So now let's try to access, say ID through adders or specifically through the git method. So to get access to ID, we would do something like const ID is adders git and then we would pass in the string of the property that we want to retrieve. So now here's the issue. If we now mouse over ID, we get back a type annotation on ID of string or number or boolean. Remember, that's what our type annotation for the return value right here says. Any time we call Git, we're going to return one of these possible types. So before we can access any properties on ID that only belong to a number, we have to set up one of those type guards. Remember we spoke about type guards a little bit ago. If we try to access any properties on ID right now, we will only see the properties that are common between number, string and boolean. So to treat ID truly as though it were a number, we'd have to set up a type type guard by writing out something like if type of. ID is number. Then inside of here we could treat it as though it were a number. If I mouse over ID, you'll see that I get the annotation. Yep, it's a number. And so now I can access all the properties on there that are common to numbers. So we would have to repeat this same kind of type guard every single time we try to access a property off of our attributes object. And clearly, this is not really a pattern we want to go down. It would be much better if we could somehow just call adders, get and then have whatever value we get back be of the correct type. That would be super ideal. So ideally, I would love to be able to just say, okay, this idea right here, if I call get with ID, ID will be a number 100% of the time without any extra hassle or anything like that on my side. Now, one way we could do this would be to put on a direct type annotation like so. But if we try to do that, we're going to very quickly see a message that says, hey, you have one of these possible values and you cannot just assume that it is of type number. So that would not quite work out. The other thing we could possibly try doing is using a type assertion. Remember, a type assertion is where we try to override type scripts behavior and just say, Look, TypeScript, we know what we are doing. Just believe us. Believe us when we say that ID is a number. So we could add on a type assertion by writing out at target and then as number like so. So that right there is the type of assertion we are trying to just override TypeScript and tell it. Look, whatever we get back from this function call, it's going to be a number. Just trust us. And so if we now mouse over ID, yep, it's a number. But obviously this approach has a downside. If we ever come back through our code and decide, Hey, you know what, instead of ID, we actually want to get our name. So we put a name right here, name right here. Well, name is definitely not a number. So chances are if we wrote out some code like this, TypeScript would think that everything is okay. But when we try to run it, chances are something would not be working as expected because we are now treating name, which is definitely a string as though it were a number. So it's clear that all these different possible solutions have, well, some really big downsides. So we need to figure out a way. To just be able to call. Adders, get a name and have TypeScript really understand what we are trying to do with just this code right here. We need TypeScript to understand that name is 100% of time going to be a string. So that's the big goal here. That's what we are trying to do now. To make that happen, we're going to have to use some really complicated syntax around generics. So rather than just writing out the syntax, I just want to build up kind of an understanding for what you and I are going to try to do. So we're going to take a quick pause right here. When we come back in the next section, I want to point out two really important things about JavaScript and TypeScript. And once we have those two important facts as a base, will then be able to be able to start writing out some kind of advanced syntax around the Git method right here and have this line of code just work correctly. Okay. Now, before we take a pause, I'm going to delete all the sample code I put on here because I told you not to write it. And I'm back to how it probably looks for you. So I'm going to save this file. I'm going to take a quick pause and I'll see you in the next video.
