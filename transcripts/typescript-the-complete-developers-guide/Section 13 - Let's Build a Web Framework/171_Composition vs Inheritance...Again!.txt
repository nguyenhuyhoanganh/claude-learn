# Composition vs Inheritance...Again!

We've now finished up our implementation of class user by using composition. So now in this video, I want to highlight a couple of issues with our current implementation and talk about how we're going to possibly fix them. So let's get to it. All right. So here's the first big issue I see in class user. Right now at present, we've got a lot of nested properties like events, sync and attributes, and they're currently all marked with the modifier of public. Chances are these should not be marked as public in order to interact with events, sync or attributes. We really want other developers to go through the methods that we've set up, so get set on trigger and so on. So that's an easy fix. We could do that rather quickly. The next big thing I see is the fact that those are all hardcoded object references. So remember, the entire point of composition is that we should be able to swap out these different nested properties to create new, different functionality. When we first set up events, we had said, You know what, hard coding events is totally fine because realistically we're probably never going to want to swap out events. The same is probably also true of attributes, but I would probably not say that it's true of sync. I could very easily imagine scenarios where I want to might want to swap out an implementation of sync. Right now, our sync module always reaches out to some outside API. So for example, maybe we want to make a version of sync that instead syncs instead syncs data with some like local storage on a user's device. So right now these are all hardcoded. That's probably not the best. We probably eventually want to build to swap these different modules out. The other big issue is that the definition or the type for each of these different properties are actual implementations or classes. So eventing, sync and attributes. Ideally they would instead be interfaces so that we could easily swap them out. Now, if these are hardcoded, we don't really need to make the interfaces because hey, it's always going to be attributes, so why bother trying to type it with anything else? But if we're going to think about allowing these different classes or objects to be swapped out, then yeah, we're probably going to want to set up those interfaces. Okay. Now, here's the last big thing and we kind of alluded to this at the end of the last video. Right now, we've got a tremendous amount of functionality tied to class user. But remember, we are trying to create a framework here. We want to create some reusable elements that can be used by other developers to create wildly different applications. So we might eventually have another model class inside of our application, like a blog post or something like that. If we create a second class called blog post to represent literally a blog post, chances are we will not want to have to re-implement all those methods again on class blog post. So it's clear that we still have a little bit of work to do in that regard as well. All right. So that's the three big issues that I see right now. We've got some public properties that probably shouldn't be public. We've got some hardcoded properties, and they currently have defined types that are classes, which means we are always locked into using in sync and attributes, and those should probably be interfaces. And then finally, at a higher level, class user has a ton of super useful code and we probably want to make that stuff all way more reusable than it currently is. So let's talk about some possible solutions to this problem. All right, so here's one possible solution that I see. The first solution would be to create a class user and have it be very simple and straightforward in nature. So we're going to take kind of a composition style approach to getting some amount of code reuse out of all these different methods we've now created. So class user would now be very, very simple. It would have a single property called model. That would be a reference to an instance of a class called model. And class model would be essentially the current user we have. So we'd have git set on trigger, fetch, save and so on. Now, this would make re-using logic very, very easy. Let's imagine first the case in which we want to add some custom functionality to class users. So maybe like a Gitter, a full name or a method to determine if this user is an admin user or something like that. We could easily add those functions directly onto class user and that would leave our class model totally untouched. In order to set up a Gitter for full name or set up some method of like is admin user, we could allow class user to reference this model property and essentially call git on that model to get some required information or maybe call fetch to make a request to some outside API to determine if this user is an admin user or not or something like that. So this would definitely be a good approach because it would allow us to add on these different methods very easily only to class user. So we can very easily imagine that if we started to have other types of resources in our application or other model classes like say, class image, this could also have a reference to an instance of a model. And we could easily add custom model or simply custom methods to class image. So we are definitely getting a great amount of code reuse here, but we still have places to attach methods that are custom just for class image or intended just for class user. Now there is a downside to this approach, a very, very big downside. If we go with this approach, remember, we kind of ran into an issue earlier on when we were talking about, well, we don't want to have to write out like user events, icon or user attributes, target or user attributes set and so on. So we went through an entire series of factors to add in. Jeter's essentially pass through methods on class user to expose all those methods to the outside world. So we did not have to do stuff like user attributes, dot set and so on. So that kind of implies that if we're going to have model now be a composition style class, we would have to once again re-implement this entire level of different methods on class user to expose the different methods on model. If we didn't do that, then we would instead have to allow access to user model. So any time someone wanted to change some data on a user, they would have to write out user model set. So either we do that pass through level once again, or we allow access directly to the model. Now, if we allow access directly to the model, that starts to put us in a really weird scenario. The reason this is weird is that we're going to start to have some properties or some methods directly on this model thing that we're going to want to access all the time. For example, if we've got class user and user has like a first name and a last name to access first name and last name, we would have to call user model git. Well, let's then imagine that we've got some method that returns the full name of a user. So to access that method, it would be simply user full name. So in other words, to access information on a user, in some cases it would be user full name. In other cases it would be user model git, first name or user model git last name. And so to access to different information, we'd have to use completely different syntax. So this is kind of where composition in TypeScript starts to break down. Using composition to create generally reusable classes is fantastic. But as soon as we want to like customize a class, well, life starts to get really confusing, all because we've got these nested properties. So this is one scenario where personally I think that inheritance is the correct solution. Just my opinion. I think that if we instead of setting up this model property relationship, if we instead say that user is going to inherit from class model, it solves just about all of these different issues because now we don't have to worry about doing user model to reference some data versus like user docket or whatever it is to access other properties. So this would absolutely solve that issue simply by using inheritance. So like I said, this is one location where I feel like composition starts to break down, specifically inside TypeScript. Now, I want to tell you right now that you might be a person who says, Oh, yeah, like we want to favor composition as much as possible. Well, that's great. But this is one scenario where I think that just sticking to that principle has a net harm. You don't always have to use composition 100% at a time, just as you don't always have to use inheritance. It's all about, of course, using the right tool in the right situation. And in this case, my opinion is that using inheritance is the right solution. So we're going to use an inheritance style approach here to set up class user. We're going to say class user is going to get an instance or inherit class model. So class users can have all these different methods. And we're not going to have to worry about setting up any method, delegation or anything like that. Okay. So that's the general idea. So let's take a quick pause right here in the next video, we're going to start extracting all these methods from class user into a new class that we're going to call a model. We're then going to set up class user to extend model. And then inside of user, we'll have the ability to add a bunch of new custom behavior that they'll be separate from other data models that we might have inside of our app. So let's take a quick pause right here and start that refactor in the next video.
