# Binding Events on Class Name

We now have the ability to print out information from a model and show it inside of our template. But now we need to figure out how to do the opposite of that. I want to make sure that we can take some user input, like maybe a button click or user typing or something like that, and use that information to somehow update our model. So let's set our sights really low or just do something really easy to get started. I want to try to add a button to my template called Set Random Age, and any time we click on that button, I want to try to set a random age on our user model. And of course, once we change the age, I would also expect the HTML on the screen to be automatically updated as well. So let's get to it. The first thing we will do is add in a new button to our template. So I'm going to go back over to my template inside my user form. And underneath the existing button, I'm going to add in a new one. And I'm going to give it some text like Set Random Age. Okay. So now we can go back up to our event handler stuff. Here's the events map right here, and we need to make sure that we add in a click event handler for any time a button gets clicked on. But you'll notice that we actually already have one right here. We already have a click event handler that is going to bind to all the different buttons inside of our template. In fact, that includes the button that we just added as well. So for example, if we go back over to our browser, we'll see that new button. And if we click on either button, we'll see the text. Hi there appear. So that's kind of an issue with our current eventing setup. If we always bind to events. Based on the tag name, we might accidentally end up binding a event handler to multiple different identical elements. So we need some better way of specifying exactly which button we want to bind to. Well, this is actually really going to be really easy. So all we have to do is add in a class name or an ID to our different button elements or in general, any elements inside of our template. Let me show you what I mean. I'm going to add in a class name. Of set age to my button right here. Then I'm going to go back up to my events map and I'm going to add in. A new event handler of click pollen dot set age. And then any time that we click on an element that has a class of set age, I will run this dot on set age click. Now, a couple of things here because we kind of jumped over a few elements really quickly. First off, this dot right here means try to find an element inside of our template with a class name of set age. This is just going to work naturally out of the box. And the reason for that is that when we put together our Bind Events method right here, we are trying to find an element inside of our template using query selector all. And so effectively what we would now be passing in as the selector is something like set dash age with a dot in front of it. So this right here is going to look through our template and find all the different elements that have a class name of set dash age. And again, the DOT means to indicate bind elements with a given class name. So we can just reuse all the same code we already have. Just fine. All we have to do is say that now. Instead of trying to select a very specific element by using its name, we can instead use a class name or an ID or anything else like that. Okay. Now, the one downside is that this event right here that we were sending up before is still going to bind to both buttons. So I'm going to just delete that first event set up right there because we're not actually using it at all. So I'm going to delete that one. I'm going to delete the H one as well, because we don't really need that one either. And I'm going to delete the respective handlers right here and right here on header hover and on button click as well. And so now we can define onset age. Click. Okay. So I'll define on set age click. Like so. And now just to test everything out, let's do a quick console.log and say button was clicked. So now whenever we render this to the browser, this class is going to try to find some element with a class name of set age. It's going to watch for a click event. And any time someone clicks on that, we're going to run on set age click. So let's now test this out. So I'll flip back over. If I click on Click Me, which doesn't have a class name, we don't see any event triggered, but if I click on set random age. Yep. Button was clicked right there. Okay. So that's how we're going to ultimately really be setting up events inside of a view class rather than trying to specify an element based on it's element name or the tag type will instead use a class name or an ID or something like that. This is going to allow us to be a lot more specific when we try to bind event handlers to different elements inside of our template. All right. So now that we've got that all fixed up, let's take a quick pause right here and we'll resume with trying to set a random age in the next video.
