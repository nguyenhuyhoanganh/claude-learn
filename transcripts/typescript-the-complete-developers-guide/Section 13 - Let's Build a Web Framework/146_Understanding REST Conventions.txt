# Understanding REST Conventions

In this video, we're going to get a better idea of how to interact with our JSON server. As I mentioned in the last video, the JSON server is going to have a big impact on how we design our user class. Now, before you start thinking that we're going to design our user to only interact with the JSON server, no, don't sweat it. It turns out that JSON server follows a lot of very popular conventions for working with individual records of like JSON information. And so in the future, we could very easily swap out our JSON server and replace it with any of another other implementations. For example, like maybe Ruby on Rails or something like that. Or some Express server. And as long as those servers follow the correct convention, it's going to work with our user class just fine. So to get a better idea of how these conventions work, we're going to write out a little bit of code to interact directly with the server and take a look at one or two quick diagrams. So diagram first. All right, here we go. So right here on the screen are what we call restful conventions. These are conventions for making network requests to some outside server. And based upon the type of request or the method of the request and the path that we make the request to, it's going to affect exactly what that backend server is going to do for us. So these are the conventions that JSON server follows. And if we just follow these conventions, we're going to have the intended impact every single time without doing any additional setup of the JSON server. So in this example, I want to imagine how we would work with some resource called posts. So maybe we've got our back end server or the JSON server that manages a list of posts for us. If we wanted to retrieve all the different posts being served by that server, we would make a git request to our server slash posts. Right now our server is running on localhost 3000, so in total this would be something like that. But in general, when we were talking about these restful conventions, we are only concerned with the path on the very end. If we ever wanted to get a very particular resource or like a very particular post, we would make a get request to slash, post, slash, and then the ID of the post we are looking for. If we want to create a new thing or a new post, we would make a, well, a post request to slash posts and we would also include the JSON information for the post we're trying to create. If we ever want to update a post, we'd make a post request to slash, post, slash ID and if we ever make want to delete a post, we make a delete request over to slash post slash ID where again the ID is the idea of the post that we're trying to delete. So JSON server follows all these conventions right here just to figure out how they work. Really quickly, let's write a little sample code inside of our editor. We're not going to worry about working with Axios or something, working with our user model just yet. We're just going to interact directly with Axios. So to do so, I'm going to flip back over to my editor and find the index file. And we currently have a little bit of sample code inside of here. I'm going to delete all this sample code and instead at the very top I'm going to import Axios. From Axios. So let's first try to create a new user. Remember, JSON server has already created an endpoint to handle users because we put slash users right here inside of our DB JSON file. So if we want to create a new user, we could say Axios post, then we would put in the URL that we want to make this request to remember the JSON server is currently running on on local host 3000. So if we want to make our network request, we would make it to local host colon 3000 users. So you can either copy that URL right there or just type it out. So I'm just going to type it out. And then as the second argument, we'll put an object that represents the properties that this user is supposed to have. So to stick in form with our current user class, we would say maybe this user has a name of my name. We use single quotes here. Bad habit. And an age as well. So now if I save this, it's going to automatically refresh my browser, and the browser should make this request to that JSON server, and the JSON server should create a new user with that name and age. So I'm going to save the file. I'm going to flip back over to my browser. In my chrome console, I can open up my network tab. And then if I sort by. Czar requests. I should see the request appear in here. And you'll notice in my case I don't see it appear. So I'm going to refresh really quickly. There we go. Now I can see it perfectly. So we first see a request that is of method options. The reason that's there is because we are making a request to a URL endpoint with a different port on it. If you've ever dealt with Corsair's errors or RUS origin resource sharing, that's what this option is. Request is talking about. What we are doing here, it's really not relevant. So then the second request is the actual post request that we're trying to make. So if we click on that, we'll see the response we got back from our server. So our server said, okay, it has created a new record that has an age of 20 and a name of my name. And then here's something really critical to notice in the response that we got back from the server. Our user that we created was assigned an ID of one. So with JSON server, any time that we create a new record, it automatically will be assigned a unique ID. We can now go back over to our DB JSON file and if your code editor supports live reload, you should see the user inside of here. If you don't see the user inside there and just close that file and open it back up. So we'll see the name of the user that was created their age and the ID of that user as well. So now if you want to, we could retrieve information about this user by using that ID. So for example, if I go back over here to my index file and if we follow restful conventions, which you'll recall, if we want to get some particular resource, we need to make a request to slash the name of the resource slash and then the ID. So if we want to get information about the user with ID one, we need to make an Axios dot get request. Two. Slash users slash one. And then with a gift type request, we are not going to provide any information with it. So I'm going to delete that second argument like so. So now I can save this file again. I can flip back over to my browser. And I'll see the network request that was made right there. And so the server sent back some information that said, okay, we found a user there, age is 20, ID is one and name is my name. So all these other restful conventions are going to work identically. We're not going to go through all of them because I think you get the idea now. And honestly, if you're here on this course, I kind of expect you might already know a lot of this stuff that I'm talking about. Now, there is one extremely thing, important thing to keep in mind here. As you can tell, that ID property that it gets assigned to a user when it's created on the server is super important. If a user model or like a user class that we have has an ID, there's actually a really important, important ramification of that. So if we ever have a user that has an ID property, that means that this user has been saved to our back end server because we're going to say that ID's are only generated by that server. So if we ever see a user with an ID, that means that it must have a server side representation. However, if a user does not have an ID, that means that this user must be brand new and that we've never saved it to our back end server. So that's how we're going to use our single user class and kind of differentiate between a user instance of a user that has been saved to the back end and one that has not. Okay. Now, the other big impact here is that we need to make sure that that data property on all of our users has the potential to have an ID as well. Right now, our interface for it. Inside of user TZ says that the user property or data property right here is only ever going to have possibly a name or a string. So we're going to have to update this and make sure that it's clear that this can also have an ID as well. Because if we ever try to fetch a user or we want to fetch a user, we have to put the ID inside there and then we're going to use that ID to actually retrieve a user or save it or whatever else. Okay. So there's been good progress. Let's take a quick pause right here, and we're going to start working on some methods. To support saving a user and fetching them on our JSON server. So quick pause and I'll see you in just a minute.
