# Adding JSON Server

Now that we finished all of our eventing stuff, we're going to move on to the last piece of functionality around class user, which is the ability to save information to some back end server about a user and then fetch it at some future point in time. Now, when we talk about saving information or fetching it, we're really talking about that data property. Remember the data property of a user stored the name and age of a user. So I want to have the ability to save information about a very particular user to the back end and then eventually fetch it at some future point in time. Now to actually save and fetch information. We of course need an actual backend server. So we're going to have to very quickly implement a backend server. Don't worry, it's going to be really fast. We're going to use a third party package that's going to be just perfectly tailored for what we are trying to do here to host our server for us. So here's the idea. Here we go. Inside the browser, we're going to have our user instance like the class user we are working on right now. This user instance is going to be making use of that axiom library that we saw much earlier on inside the course. Remember, Axios can be used to make network requests to some outside server. So whenever we try to save some data, Axios is going to make a request to this outside server that you and I are going to set up. The package that we're going to use to set up this server is called JSON server. This is a third party package that we can install via RPM to stand up a server very quickly and very easily. It has the ability to receive information and store it into a plain JSON file and then eventually serve that information right back to us. So once again, it's a really easy way to get a server stood up in a development environment. So let's try installing that package and then going through a little bit of setup with it and getting a better idea of how this JSON server is going to work. All right. So back inside of my terminal, I'm going to find where I'm currently running parcel. I'm going to stop this process with control C and then I will install JSON server with NPM. Install Dash G, JSON Dash server. Okay. So we're going to install this as a global module. Just installing it isn't quite enough. We also have to do just a little bit of setup inside of our project directory. Whenever we run JSON server, we have to point it to an actual JSON file or a file that contains some amount of JSON data. This file essentially acts as a database of sorts. So that file is going to be where JSON server attempts to save all of our information about various users inside of our app. So I'm going to flip back over to my code editor and create a JSON file that's going to serve as the database for our JSON server. Okay. So back inside of my editor, I'm going to create a new file inside of my root project directory. Make sure you are creating this file inside of your root project directory as well. And I'm going to call it DB JSON. So inside of here, we're going to eventually see a ton of data up here. And all this data is going to be information that we are trying to save about the different users inside of our application. We're not going to, in general, write any information into this file manually except for right now when we do some initial setup, all the data inside of here is going to be managed by JSON server for us. So the initial setup that we're going to do inside of here is going to be to create a object. I'm going to add in a key, repped by double quotes called users. And I'm going to give that a value of an array like so. Make sure you have double quotes around users. If you don't have those double quotes, this is not going to work correctly. So now when we start up JSON server, we're going to point it at this file. JSON server is going to look at this object right here and it's going to see that it has a key of users. So that tells JSON server. Oh, okay, it has to serve up a resource called users and JSON server is going to automatically create a handful of different routes that will allow it to serve us some JSON data about users. It will give us the ability to save users to this back end database as well and then fetch them at some future point in time. So I'm now going to save this file. And then to actually start up the JSON server, I'm going to go back over to my terminal. I'm going to open up a second terminal window also inside my project directory. Inside the second one, I'm going to run it JSON, dash server, dash W and then DB JSON. Let me resize this thing. Just you can see the whole command very easily. Okay. So we'll run that and then we'll see a message up here like so. So under this resources section, notice how it says users. So JSON server has parsed that JSON file and it understands that it's supposed to serve and receive requests around some resource called users. Now that doesn't really quite tell us what give us like any functionality right now, but we'll talk about what that means in just a moment. First, I'm going to go back over to my first terminal window over here. We just spoke about how we need Axios to communicate between our user model and that server. So inside of my project directory, I'll do a quick NPM install for Axios. And then after that is installed, I'm going to start parcel backup with parcel index HTML. Okay. So that looks good. Now, at this point in time, we have two different commands to actually start up our project. If you ever stop either of these running processes, you're going to have to start up two terminal windows. One is going to run parcel index.html and the other is going to run. JSON server dash w db json and it's really easy to forget these commands. So to help us, I'm going to flip back over to the package JSON file and I'm going to create a script to run that command and that command. This step is entirely optional. I just want to do this so that if you ever stop these running processes, you don't have to come back to this video to remember what to run. So I'm going to go to my package JSON file. I'm going to add a script section inside of here. And I'll add in a script called Start Colon DB and if I ever run this, I will actually execute JSON Dash server. Dash W db json. And then right after that I'll do start, pull and parcel. And if I ever run that script I will do index partial index html like so. Oops. I forgot a comma right above scripts. There we go. That's better. Okay. So now as you watch me in this course, I'm generally just going to run these commands directly because I know them off the top of my head. But if you ever forget them, just come back to the package JSON file and you can use these as the reminder of what command to run or alternatively your terminal. You can now just do NPM runs, start pulling DB and then a second window npm run start colon parcel slowly up to you. Okay. So we've now got our JSON server up and running. So let's take a quick pause right here. When we come back, the next video, we're going to discuss how we interact with this server because it's going to very deeply impact how we actually design our user model to save and fetch information. So quick pause and I'll see you in just a minute.
