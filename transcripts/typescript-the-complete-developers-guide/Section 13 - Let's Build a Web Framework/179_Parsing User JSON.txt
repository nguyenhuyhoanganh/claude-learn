# Parsing User JSON

In this video, we're going to make sure that our class collection has a fetch method that will make a request to users to retrieve all the different users that we have. Now to get started, I first want to use Axios to manually make this request. Just you can see the exact information that we're going to get back from JSON server and so we can understand how we need to parse this stuff and turn each object that we get back into an instance of a user. So to do so, I'm going to go back to my index file. I'm going to delete all the code that I currently have inside of here, and I'll replace it with a import Axios from Axios. Then I'll use that to make a get request to http colon slash slash local host 3000 slash users. I'll then chain on a dot, then statement. This will be called with some response object that is of type Axios response. And inside here I'll console.log response data, which is the actual information that we get back from JSON server. I'm going to make sure I also import the type Axios response as well. So Axios response. All right, so I'll save this. I'm going to flip back over and then inside my browser, there's the console log. So you can see that when we make a network request over to that endpoint, we're going to get back an array of objects and each object is going to have an ID, a name and an age. So the object that we get back essentially conforms to our user props interface. So we want to essentially take objects that conform to that interface and use them to create different instances of users. You might recall that our user class inside of user dots has our static build user method and it accepts an argument of user props. So user props. Yep, that's that interface that has the ID name and age. So essentially we just need to loop over the response. Take each one of these objects and throw them into build the user. That's pretty much it. All right, so let's get to it. I'm going to go back over to my collection file at the top. I know I'm going to need to use Axios, so I'm going to import Axios. Then we'll go down to the bottom of the file. Actually, one other thing really quickly. We're probably going to have to make reference to the user props interface as well inside of here. So I'm going to import that right there as well. Okay. So now down at the bottom of the file, I'm going to add in a new method called fetch. I don't think that this is going to be called with any arguments, and I don't think it needs to return anything either. So I'll mark the return type is void. Then inside of here we need to make a network request with Axios dot get. The argument to get is going to be the URL that we want to make our request to. We could hard code in localhost 3000 users, but as you might guess, well, we're going to have to definitely dynamically specify that root URL on the fly. So I think that whenever we create an instance of class collection, we should have to pass in the root URL that we want this thing to make a request to. So then we can very easily reuse it for other types of records in the future as well. So I'll add in a constructor. Please the shortened syntax to receive a public variable called root URL. Ask me if type string. Okay. So now we can reference that root URL inside of our axios request. So this dot root URL like so. All right, so now we can chain on a dot, then statement, receive all that JSON data and turn each of those objects into an instance of a user. So to do so I'll chain on a dot then. This will be called within Axios response. That will call response. Here we go. I'm going to make sure that I import type Axios response at the top. So import Axios and Axios response. So then we've got a response data property here. So we want to run a loop over response data because that is going to be the array of records that we get back from the JSON server. So we're going to do a for each on this thing and for every object inside there. We're going to turn it into an instance of a user and add it to our model's array. Okay. So we'll say response data for each value. Value. There we go. Which is going to be of type user props. So we're going to take that value right there. We're going to stick it into user build user. That's going to create a user. And then I'll take this models and I'll push in that user like so. And that's pretty much it. So now I can save this file. And we can test it out very easily back inside of our index file. So back inside of indexes, I'm going to delete all the test code we had put inside of here with Axios. I'm going to import election. From models. Collection. I'll then create a new instance of a collection with concert collection is new collection. And remember we have to pass in our route URL right here. So I'll do. HTTP colon slash localhost 3000 slash users remember extremely common to make typos inside of here. So please triple check your URL. And then we can call election fetch. Oh, one last thing. Right now, we are not returning a promise or anything like that from collection. Fetch. Remember, we are going to rely upon this eventing system to get an idea of whenever the collection has changed. So after we run the entire for each loop right here, right after that, I'm going to make sure that we trigger a change event to tell everyone that, hey, we have changed this collection somehow. So I'll say this dot trigger change. Then back inside of indexes to get a notification of when the fetch has been completed. We can add on an event handler, so I'll say collection on change. After the collection has changed. Let's just console.log out the entire collection so we can inspect it inside of our console. Okay. So I'll save this. I'm going to flip back over. There's the console log. I should be able to expand it, open up models and see all those different users. So the users that I see in here are all the users that are reflected inside my DB JSON file. So I should have inside of here a user with ID one. Yo, there it is. A user with ID to the user with ID three and so on. All right. So that's pretty much it. That is our users collection. Now, of course, the only issue now is that this collection only works with users. So let's take a quick pause right here. When we come back, the next video. We're going to turn this into a generic class. So I'll see you in just a minute.
