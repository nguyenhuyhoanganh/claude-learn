# Generic Constraints Around Sync

In this video, we're going to start to refactor class sync and turn it into a generic class. So the overall goal here is to remove every reference to user props from this file. So to get started, I'm going to first find class sync and I'm going to add on those angle brackets with a T inside them. Remember, we've said it many times, PT is not some special identifier is just convention. So now we can go through this file, we can remove every reference to user props and if we see any reference to user props inside the class, we will replace it with that capital T. So to get started at the very top, I'm going to remove the import statement for user props. Then inside of the safe method, we've got the argument type of user props right there. So we're going to replace that with a capital TX like so. All right. So as soon as we do so, we're going to see a little air here. So we now have an air around ID. You might recall that before we started this application, we had a quick side discussion on generics. And when we spoke about generics and that side discussion, we said that sometimes we have a hard time referring to properties on a value that is a generic type. So the reason for that is that in this case, we are seeing that data is going to be some generic type, some type that has not yet been provided. So we're trying to read a property off this type or off a value with some unknown type. Remember, any time we reference any property on any value whatsoever, TypeScript wants to check to make sure that that property actually exists. So in this case, TypeScript has no idea what type T is going to be and therefore it doesn't have any idea if data is actually going to have an ID property. And that's why we are seeing this error on here. It says, hey, I don't know if this type T is going to have an ID property. So you might recall that we saw this on that previous discussion we had around generics. We saw it specifically around when we were trying to call a method on some class. Remember, we were talking about that like print method on either a car or a house. And we said that we could fix that up by making use of a generic constraint. A generic constraint is where we try to tell TypeScript that whatever value or whatever type T that we pass in for our class, it will always have some given property. And we did that by defining an interface. So let's do that right here. We're going to define an interface and we're going to use that interface to guarantee to TypeScript that whatever type we use with class sync right here, it will have a defined ID property. So I'm going to say right here, interface has ID. And I'll say that it is going to be a number. And then to make sure that TypeScript understands that whatever type we use with class sync is going to satisfy this interface, will say t extends has ID like so. So now we have a constraint on here. We can now only use class sync with some type that will have an ID property that is a number. As soon as we add that in, you'll notice that the error message down there goes away. Because now TypeScript understands that whatever type data is, well, it's going to have a defined ID property and it's going to be a number. So that looks like it solves the issue. But it turns out, well, there's kind of a side issue going on here. So let's take a quick pause. We're going to come back in the next video. We're going to try to use class sync and we're going to see a little air come up that is related to this type constraint stuff. So a quick pause and we'll see you in just a minute.
