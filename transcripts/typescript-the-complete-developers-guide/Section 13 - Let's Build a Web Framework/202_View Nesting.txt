# View Nesting

In this video, the last thing we have to do is actually execute the nesting process. Now, in this case, there's not going to be really any automatic system. So let me tell you exactly how we're going to accomplish this. Inside of my viewpoints file. I'm going to go all the way down to my render method. So after we have called behind events and map regions and before we call a pen, this right here is essentially where we want to set up our view nesting. So we want to start to nest all these views together before we actually append anything to the dome. So right here, we need to somehow make sure that everything gets nested together. Now, the actual nesting process is not going to be something that is done automatically by the view based class. Instead, we're going to call a helper method that we can choose optionally to implement inside of any of our different view classes. And inside that helper method is where we're going to do our actual nesting process. Once again, I apologize. This is one of those scenarios where you just got to see the code because honestly, I don't think any explanation is going to really make this super clear. Now, doing the implementation is going to be a few very short steps. So let me show you what I'm going to do. So right after we map all those regions and right before we render the stuff to the DOM, I'm going to call a helper function called on render. And the idea here is that this is kind of like an event handler for render. So it's going to be a method that gets called right before our element gets shoved into the dom. Now right away we get an error message. So I'm going to define a default implementation of on render inside of view. So I'll provide a default implementation like so we don't actually have to do anything inside of here. We just don't want to see that air. The other reason that we are adding in on render right here, we could define this as an abstract method, but remember, if we define it as abstract, then all child classes would be required to implement on render as well. But I don't really want every view class that we create to have to implement on render. On render is essentially going to be the prime location to do view nesting and because not every view we create is going to want to nest anything. I don't want to require anyone to have to define on render. It's completely optional. Okay. So now I'm going to save this file. I'm going to go back over to user edit. And now inside of here we can define an on render method and remember this thing is going to be called automatically. So inside of here is where we're going to do our nesting. And how are we going to do it? Well, we're going to do it in a very manual fashion. So we're going to import in user form and user show. We're going to create instances of them. We're going to pass in our user model. And remember, we just went through all that hassle with regions to get a reference to that. Dave Right there and that Dave right there. So when we create user form and user show as the parent elements will pass in that div and that div. So let's see what that would look like. At the top. I'm going to import the two classes that I want to nest inside of here. So user form. From user form. And user show. From user show. And now inside of On Render, we're going to create instances of both those classes in attempt to render them. So I'm going to say new. User show. Remember the first argument to this thing. It takes two arguments. The first one is going to be the parent element and the second is going to be the model to use. So the parent element is going to be our reference to that. Dave right there, which we just got through setting up that region system. Remember, the actual div reference is going to be on user edit regions user show. That's going to be the reference to the element where we want to show user show. So as the first argument I'll pass in this dot regions dot user show. And then the second element or second argument will be our reference to the user model which is available inside of user edit as this dump model. Now, right after creating this instance, we can assign it to a variable like so. And all we really want to do with this thing. Estella to render itself. That's pretty much it. Now. We don't actually need to use our show for anything else here. So actually, you know, we could just not assign it to a variable at all. And after we create an instance, just immediately call render on it like so. Okay. So now we're going to repeat that same process for user form as well. So I'm going to say new user form. I'll pass in the region of where I want to render this thing. So this dot regions dot user form. And then the second argument will be this model. And then I'll call the render method on it. And that's it. That's nesting. That's all we got to do. I know. Super nasty. Really. The big leap of faith I want you to take here was that we needed to provide some way for our view class to automatically find these different elements that are going to show up in the template. If we didn't go through this region system, then we would have had to write some code inside of here to essentially root through our current template and attempt to find like that. Dave Right there and that. Dave Right there. Maybe it would have been a little bit easier to understand if we went through that process. But I figure at this point we spent a lot of time on this project, so might as well kind of get it all closed up. So anyways, once we set up all the regions stuff, whenever this component is rendered to the screen, it's going to call on render. And inside of here we're going to create our child elements or child view classes pass in the reference to the div where this should be rendered model as the second argument and that's it. So now we can save this file, look back over to the browser and you'll see both user show and user form are both visible at the same time. Awesome. So now we should be able to change the name of our user, like new name, like change name and see the new name appear. And I should be able to do a set random age as well. And the really interesting thing here is thanks to all that inventing stuff that we put together inside of our user model, remember, there's like no direct tie between these view classes and the model directly. Like nothing really to say. Not a whole lot of infrastructure there. The only kind of connection between all of them is that eventing system. On the model where we said every single time that we update the user model, we're going to somehow notify these views that they should re render. And that's exactly what you're seeing here. Okay. So this looks great. Now, let's take a quick pause right here. We're going to add in like one more quick feature, maybe just to show you how we could use our framework. And then we're going to wrap up this entire project because like I said, we spent an awful lot of time on it. So quick pause and I'll see you in just a minute.
