# Extending the User

In this video. We're going to continue working on class model. So we're now going to set up a constructor function inside of here that's going to take an instance of something that satisfies model attributes, an instance of something that satisfies sync and same thing for events as well. And it's going to assign them all to private properties. So I'm going to set up my constructor. This thing is going to take three separate arguments. The first one is going to be a private property of attributes, and this is going to take something that satisfies interface model attributes. Now when we write out model attributes, we immediately have to specify a generic type for this thing. Now to get a generic type, we need to make class model into a generic as well. I know that stuff sounds like really weird. Just bear with me for a little bit during this refactor and you're going to very quickly see how all this stuff comes together. So on class model, I'm going to say that whenever we want to create a model at all, we have to pass in type T. That's going to specify the actual data structure that we're going to work with inside this thing. And we're going to pass through type T to model attributes like so. Now. I still have an error here. It's just because I need my curly braces after the constructor. Okay. Now, I'm also going to designate that we need to take in some events property as well. So private events. This needs to satisfy the events interface. And then finally private sync. And this needs to satisfy the sync interface. And just like model attributes, this thing also needs to know what type of data it's going to work with. So we're going to pass through the PT type like so. Okay. So now the next step, we're going to take all the implementation from class user, cut it and paste it into model. So I'm going to go over to user. I'm going to take all the implementation inside of here, except for the constructor and these private property or the public properties we established up top. So everything from get on all the way to the bottom. I'm going to cut it all. And then paste it inside of model. Now, right away inside of here, we've got a couple of errors, mostly around user props. So the type of data that we are working with, like if this thing has a name that's a string and a H, that's a number and so on is now going to be specified through this type T So we're going to go around and replace user props with T. There's one on set. I think that we just need to import at the top of the file for Axios response. So I'm going to import that from Axios as well. And then I think that's the only other error. So now we've got one more air inside of here around ID. If we mouse over it, it says that argument of type ID is not assignable to parameter of type key of T. Now, you might recall that we saw an air just like this a little bit ago. It's because TypeScript just doesn't know that our type T is going to have an ID property. So we saw this previously inside of I think it was attributes. So no, it was inside of Sink. So to fix it up, we're going to add in a type constraint to this generic type right here. So we're going to say that type T is always going to have some kind of ID property. So we can say interface has ID, we're going to say this is going to have an optional ID property that is going to be a number and we can add on the constraint t extends has ID like so. And as soon as I do that, the error goes away. Okay. So that's pretty much it, believe it or not, or the entire model file. So now we can go back over to user. We're going to set it up to extend class model. And we're going to make sure that we kind of just while you're up everything correctly, more or less. So I'm going to go back over to user. It's. Back over here. I'm going to delete the constructor. I'm going to delete all those additional properties as well. At the top, I'm going to delete all the import statements and the only thing we need to import is the model from the model file. So now we can set up our actual inheritance. We're going to say class user extends model, and when we create model or we specify model right here, we need to designate the generic type that should be used throughout there. So this is where the actual connection happens. We're going to say that for type T and model, we want to use this structure, an ID, name, an age that's a number, string and number for say model. And then inside they're user props like so. So now, once again, we can imagine an inside of class model. She's going to be used or kind of replaced with user props. And so we can imagine that we go around and everywhere where we see. DT, it gets replaced with user props. Okay. So now the very last thing we need to do is make sure that we've got some ability to create an instance of a user. And whenever we do so, we should automatically create all these different sub modules to use. Because remember right now, if we wanted to create an instance of a user, we looked at this on a previous application, we would have to write out something like new user and then pass in something to use for attributes. And then pass in something to use for events and then passing something to use for sync. We don't really want to have to do that every time we create a user. So let's take a quick pause right here when we come back. The next video, we're going to set up one of those static class methods on class user that's going to give us a pre configured version of user. All right. So quick pause and we'll see you in a minute.
