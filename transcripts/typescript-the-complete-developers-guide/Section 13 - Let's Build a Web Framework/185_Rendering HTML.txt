# Rendering HTML

Now that we've got a user form put together, we can test this out inside of our index file. So I'm going to flip back over. We've got a lot of stuff about user inside of here right now. I'm going to delete all that code and I'm going to instead import user form. From View's slash user form. So now we can create a new instance of user form. And remember, when we create an instance we have to pass in a reference to an HTML element, and that element is going to be where we place all the content from user form. So I'm going to open up my indexed HTML file. And I'm going to add in some div that is going to serve as the absolute parent element of my entire application right now, just the user form. So I'm going to add in a div right above the script tag and I'm going to give it an ID of root like so. So now we're going to try to get a reference to this div with ID root and pass it into the user form as the parent. So I'll save the index HTML file. I'll then go back over to indexes and into the constructor. I'm going to pass document query selector. LB root or something. We don't want to do a query selector here. Be much more appropriate to do a JIT element by ID. Root. That's much better. Okay. So that's going to create an instance of user form. Now to actually get some HTML to show up, we'll call user form render. And that's pretty much it. So I'm now going to save this to make sure that parcel sees the update to our HTML file. I'm going to go back over to my terminal and restart my running parcel process. So I'll end it with a control C and then rerun parcel index html. Okay. So now we can go back over to our browser and retest this. All right. As soon as I flip over, I can actually see the user forms HTML appear on the screen. So I've got user form and I've got an input right after that. Cool. So we can now show some really basic content. So let's take a quick pause right here. We're going to come back to the next video and start adding in some additional functionality.
