# Composition is Delegation

All right, my friends, we've now pulled a class user apart, extracted three separate classes, and then kind of reintegrated all of them back in. So I think that we're now ready for a test of class user inside of our indexed file. So let's get to it. All right. So I'm going to flip back over. I still have a little bit of code in here from when we when we were testing events. I'm going to delete the event specific code and I'm going to leave behind where I created a user. So maybe as our first test, we'll say that we'll try to take all the attributes that this user has and attempt to actually save them to our JSON server. So how would we do that? Well, I guess it would be something like what do we do before we did? Like a user save. Okay. Well, I'm getting an error there. Oh, that's right. We don't have a safe method on user anymore. It's called sync or it's inside the sync property. So I guess we have to do something like user sync save. Well, okay, we got another error here. Let's see what that is. Oh, that's right. When we call save, we have to pass in the data that we want to save. So to actually get the data we want to save, I guess we could do like user data. Oh, that's right. It's a that is a property that we extracted to that attribute sub module. So we have to do something like attributes dot data. Oh, wait. Okay, there's another error. Well, if you recall, data is a private property, so we can't extract it directly. The only way we can get at our data is to have like that git method call where we pass in either id name or age. So I guess what we'd have to do here is really call that three times, collect our ID name and age, and then pass all that information off to save. Well, let me just pause right here. I think you kind of get the idea here. Using this new refactored version of user is really a pain in the rear. In order to do something as basic as saving user information, we might have to do potentially three different calls and then pass all that information off to save. Whereas before. You'll recall the code we had before we did this refactor. All we had to do was something like user save. Very clear. Very easy. Not clear. Not easy. So even though we have separated our user class out into these three separate sub modules, I think it's clear that we still have a little bit of work to do from the usability side. So let's take a look at a diagram to understand what we still need to work on here. All right. So I'm going to flip back over. So in this diagram right here on the left hand side, we've got something called the caller. By caller, I just mean someone who wants to call a method on class user. That's it. Now remember when we start to use composition, the idea for implementing composition is delegation with delegation. We don't really want a color to reach into class user and reach through to like say the the sync instance directly. We don't want to color to reach over to eventing or to reach through to attributes. So in other words, having code like this right here where we try to reach into the attributes module or sync or so on, not quite so great if we are trying to implement delegation. The idea with delegation is that our class user is still going to have all these same properties or all those same methods and the color is going to call those methods. And then inside of the method save our user class is going to use these different sub modules to actually implement some behavior. So save isn't really being done by class user. Class user is still going to rely upon, say, sync and attributes to implement that behavior. So that is the delegation part here, and that's the part that we are currently missing inside of our current implementation. So in other words, we now need to go back through class user and we're going to re implement all these different methods, but we're going to implement them very quickly because we're going to be making use of these instances that we've already got available inside of class user. Now in some cases, when we try to re implement each of these, it's going to be very easy. For example, in the case of calling Git, whenever someone calls git, well, it's basically going to be a direct pass through to the Git method on class attributes. So that will be really simple. But in some cases, like calling save, we need to make sure that we first call Git on attributes to get all the different properties tied to a user. And only then after that Queen, take that information and pass it off to save. So in some cases very easy pass through. In some cases, we're going to do some amount of coordination to get all these different kind of some modules working correctly. So we still have a little bit of work to do. So let's take a pause right here. When we come back to the next video, we're going to re-implement each of these methods on class user, but they're going to be way easier to put together than in our last iteration. So quick pause and we'll start that in the next video.
