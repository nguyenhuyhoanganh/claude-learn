# Nesting with Regions

In the last video, we sped through our implementation of user edit and user show. So now we need to start to focus on how we're going to get user form and user show to be nested inside of user edit. At the end of the last video we created those two divs inside of user edit with very special class name. So one is user show and the other is user form. So as you might guess, here's what's going to happen whenever we render user edit. We need to somehow get a reference to this div right here. We're then going to create an instance of user show and we're going to pass in this div as user shows, parent element. Remember, the parent element is essentially how we decide where something gets rendered into the DOM. So if we pass in a reference to this div right here to our user show, when we create it, then all of user shows, HTML should appear appear right there. So in other words, we should eventually have like that h one inside there. And then the div with the name and so on all inside of that div. So the whole trick here is to somehow figure out some automated, reusable way to say that any time that we render user edit, we should somehow very easily get a reference to that div. Okay. So let me tell you about how we're going to do this. Now, I'm going to tell you right now, understanding how we're going to do this is going to be a little bit confusing because it's like two or three stages. But as soon as we walk through one full example and actually write all the code for this, I'm really confident you're going to have a good understanding of what's going on, as with most things. Okay. So let's get to it. First, a diagram. So we're going to open up class view and we're going to add in one method and one property to it. The first method we're going to add in is something called Region Map. Region Map is going to be very similar in nature. Through the events map method that we already added in to class view. Remember, class view is a method that can be implemented by some developer like you or me when we extend the view. And the purpose of events map is to somehow provide a collection of different events that we want to watch for inside of the template produced by a given view. So this is an optional method that is giving some information to class view to process. That's going to be the same thing with the region map right here. We're going to implement region map as kind of a optional method to be implemented whenever you extend or subclass view. So our developers who use our framework are going to implement region map. Region map is going to return an object that has some key value pairs where the keys are, what we're going to refer to as regions. We won't worry about what those keys really are just yet. What's much more relevant is what the values are. So the value is going to be a selector to find some element inside the current template. So for example, if I wanted to create a region which is our term for some place where we can nest a view out of those two divs that we just put inside of here, I would add in a key value pair with a value of user show or dot user form. And ideally, that should get me to select that element or that element. So once our view class has called Region Map got in the subject, we're then going to have to iterate over all these different key value pairs. It's then going to find some element with that selector or that selector. And then this is the critical part. Once it finds that element inside the template, it's going to assign that element to this regions property, which is going to be an object. And so this regions property is going to eventually have a user show that's the name of the region and a value of the element that it found in the template. And then the same thing with user form as well. Now let me reformat that so you can see a little bit more easily. There we go. So this region's property is going to start off as an empty object. And once class view has processed the region map and found some corresponding elements inside the template, it's going to add those into regions. Once we have all of these element references inside of here, we're then going to be able to use these to actually nest some views. All right. So I know that this part is like a little bit complicated, especially since it's pretty far removed from TypeScript in general. Nonetheless, let's take a quick pause right here. We're going to try to just implement this portion. This doesn't really answer exactly how we're going to render some child views into these different elements. Nonetheless, let's start implementing just this portion, and we're going to figure out how we can actually nest up inside these different elements really easily once we get this part done. So quick pause and I'll see you in just a minute.
