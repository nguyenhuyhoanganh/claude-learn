# Dynamic Array Creation

In this video, we're going to start implementing our own method. But before we do, I want to take care of a couple of housekeeping items. First off, we define the on method, but I neglected to put on a return type on here, so I'll make sure that I do that like. So I don't expect to return anything from this method, so I'll annotate it with a type of void. Next up at the top of the file, I made a little typo here when I defined the type alias. I said that we were going to define a function type that takes no arguments and returns nothing. I used the incorrect syntax. The syntax we put right here is actually interpreted by TypeScript as being a function that takes no arguments and returns an object. So when you put down those curly braces inside of a function annotation, TypeScript thinks that we are saying, Oh yeah, this is going to return an object of some kind to actually indicate that we want a function that returns nothing, nothing at all. We would put in void like. So that was a typo on my part. I apologize for that. Okay. So that's much better. So now we can get back to our on implementation. Okay. So inside of here, we're going to have to handle two different cases. Remember, I just told you at great length how the event object, when it gets initialized, when we first create our user, is going to be an empty object that's going to eventually have some keys as strings or strings as keys and an array of callback functions. But when users first created, we have an empty object. So there's actually two cases that we need to consider inside the on method when we start to think about registering a callback function. So inside of here, if we do this events at event name that can have two possible values. It can be either an array of callback functions or it can be undefined. So when we first create our user, this line of code right here is guaranteed to be undefined. But as we start to add in some actual events here, eventually some of these keys might give us back an array of callback functions. So we need to basically just handle both cases appropriately. So to do so, here's what we're going to do. I'm going to say cost handlers is going to be this dot events at event name or empty array. That's the trick. So when we first create our user and we look at this dot events and look up event name that's going to give us possibly undefined. If it does, then we will just fall back to assigning an empty array to handlers. But if this dot events is defined or something, this dot events ad event name is defined, then we'll take that array of callbacks that we've already created and assign it to handlers instead. Either way, handlers is guaranteed to be a array. So after we get that array, we can then say handlers push and we'll add in the brand new callback that was passed into our on method. So I'm going to push in callback. And then finally we're going to take the handlers array and assign it back to the this event object. So we'll say this dot events at event name. Is handlers like so. And that's it. Okay. So let's now give this a quick test. Now, when we test this, we don't actually have the ability to invoke any of these handlers just yet. We only have the ability to just register them right now. But that's fine. We can still write out some code to make sure that they are being registered correctly. So back inside of my index file, underneath our user declaration right here, I'm going to delete the user set and the console logs. And then going to register a new event with my user. So I'm going to give it an event name of like change. It honestly doesn't matter. We can register any event right now that we want to. There are no meaningful events inside of our application yet. And then I'll pass in a callback function. The callback function doesn't have to do anything at all right now because once again, we don't have the ability to trigger any events. So to actually make sure that something here is actually working correctly, well, we can't just look at our entire user. Through a console.log, we can look at its events property and just make sure that it has a change property on there with at least one registered function. That's how we can test that this thing is actually working. So I'm going to save this file. I'm going to flip back over to my browser. I see the console log of user right here. So there's the events property. I can see that it has a key of change and it has one registered function with it. So we're definitely registering functions correctly. We can now try to register a second function. So I'll add in a second one. I'll say like user on change a second time. And then just for fun, I'll try registering some other event name as well. Like so. Okay. So we'll save that again. Split back over. And now my user event object has that random key. It also has the change property with two functions in there. So now in theory, if we had the ability to trigger some event like trigger the change event, we would look at this array of functions right here and run each of those functions. Okay. So this looks pretty good. So let's take a quick pause right here. When we come back in the next video, we're going to do exactly that. We're going to make sure that any time we call the trigger method, we can pass in the name of an event. And then we'll loop through all those different callbacks and call them so quick. Pause, and I'll see you in just a minute.
