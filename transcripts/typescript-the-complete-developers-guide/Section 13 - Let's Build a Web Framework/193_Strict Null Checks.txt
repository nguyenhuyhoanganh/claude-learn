# Strict Null Checks

In the last video at the very end, I said that you might be seeing an error message around on set named click. So in this video, I want to show you how you can get an error message to show up here very easily by just changing one or two settings inside of TypeScript. So to get started, I first want to generate a config file inside my project. Remember, a config file is used to customize exactly how the TypeScript compiler works. Right now we are running our project using parcel by default with parcel notice config file gets generated. So I'm going to flip back over to my terminal. Inside of my project directory of web, I'm going to generate a new config file by running TSC init like so. So now I'm going to flip back over to my editor and I want to open up that file and point out one extremely important setting inside there. Soon as I flip back over, you're going to see an error right there. That's totally fine. I want to open up the configure JSON file. And then inside of here, I'm going to scroll down to a section called Strict Type Checking Options. When whenever we generate the config file, all these different settings inside of here are by default turned to true or turned on, I should say this one setting right here that says strict true essentially governs all of these other options inside of here. So by adding in just the config file by itself, we automatically are enabling some more strict options around TypeScript. So let's talk about what that really means. Well, the easiest way to tell you what that means is to just point back to the code that we just wrote 2 seconds ago. So if I flip back over, you'll immediately notice we've got an error around input. And if I hover over it, it says object is possibly null. So I don't want you to think about TypeScript right now. I want you to think about just the normal operation of a method like query selector. In fact, I'm going to flip back over to my browser and I want to show you how query selector usually works. All right. So I'm going to flip back over. I've got my browser console open right here and I'm going to call. Document dot, body, query, selector, and I'm going to put in like a garbage selector to this thing, something that's clearly not going to match or find any element inside the DOM. And then when I run that, I get back a value of null. However, if I call document body query selector and try to find an element that's div, yep. I definitely get something back. So it's very clear that the default operation or behavior of query selector is to return either an element or null. When we are not operating TypeScript in strict mode, TypeScript by default doesn't really worry about the null case. And this is not just for the query selector method. This is for any other method that might return a value or null. So without the config file, TypeScript just says, hey, you know what that method call it might sometimes fail more or less by returning null, and that's okay. We don't really care about it, but as soon as we create that config JSON file, strict mode is turned on. And now all of a sudden TypeScript wants us to consider every case where we might get back a value of null. And again, this is not just for query selector, this is for all different methods that we might make use of any function that might return a value or null. So to fix this up. We can go back over. If I hover over query selector, you'll notice that it says right here, HTML element input is going to be the return type or null. So if we now hover over input, it's going to tell us that its type is HTML element, input, element or null. So that's essentially what we need to fix up. We need to eliminate that null case inside of our type union. Remember, any time we have a type union, we can resolve it by adding in a type guard. So in this case, we can say if. Input. Then go ahead and do all this stuff. So essentially we are saying if input has a value, then go ahead and treat it as though it were a value and as though it's not null. So inside of the if statement, which is our type guard, if I mouse over input, we've now removed that null case and we can now treat input as though it were strictly an HTML input element. Okay. So this looks pretty good now. Now there's one other error that was brought into our application when we enabled strict mode. If you flip back over to indexes, you're going to see another error over here as well, tied to basically the same thing around this git element by ID method. So get element II ID is now considered to be returning either in element or null. So we have to go through the same operation and make sure that the result of that method right there is some defined value and not null. So to fix this one up, I'm going to extract that call right there. Into a helper method or something. A helper variable. So I'll say const route is that request or that get element by ID. I'm going to make sure I pass in root as the first argument to user form. And now to make sure that we take into account the case that route might be null. I'll add in another type guard, so I'll say if root. Let's do this. And then in this case, we might want to also add in an LS case, if we don't have that route element available, maybe we should just throw a new air. And say something like route element not found or something like that. So if we ever accidentally render our application or attempt to render it when we don't have a root element, well, we'll know right away inside our console, which would be kind of nice because it'll tell us why our HTML is not showing up on the screen. Okay. So that's pretty much it. So remember, there is at the end of the day, just a really big difference by having this config file inside of your project. If you generate it well, you're going to be inside of strict mode and now you're going to have to consider a lot of cases where some variables might actually be null. That is specifically the setting right here of strict null checks. Okay. So let's take a quick pause right here. Now that we fix this all up and continue with our user form in the next video.
