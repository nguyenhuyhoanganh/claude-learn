# Defining an Events Map

We can now show some HTML to our user, which is a great step forward. But now I think that we need to work on the ability to have some event handlers tied to this HTML as well. So let me give you a quick example. Let's say that we add in a button to our template. Like so. And maybe that button has the text click me. If I then save that, I can then flip back over to my browser and I'll see that button up here. If I now click on this button, absolutely nothing happens and we don't really have any easy way to somehow add an event handler to the button. So I want to have the ability to detect a click on that button and run some custom code any time a click occurs. So let me give you a rough outline on how we're going to accomplish this. All right. Here's my idea. This is a timeline describing everything that happens inside of our render method. So we're going to add in one additional step along the way. So whenever we call our render method, we call the template method, we get back that HTML string. And remember, at that point in time, it is just a string to actually turn it into HTML. We insert that HTML string into a template element. That's when it actually gets turned into something useful for us. So right after that would probably be a really good time for us to attempt to bind some event handlers to the HTML that is inside of that template element. So then right after that, we can then continue to take that content of the template and insert it into the DOM. So we're going to essentially add in this one additional step right here. Now, one thing I want to mention to you, you might be thinking, hey, why don't we just do something like maybe react or Angular does? Why don't we just add in some funny looking event handlers directly to our code right here, something that says like on click or something like that. And that would be a reference to some on Click Function or something like that. Well, this is actually a lot harder to implement than you might think, because we would have to implement some ability to parse this HTML string and detect that we were trying to bind to some event handler and then actually set up that association. So that's really challenging to do. So we're going to take a much easier approach. So here's my idea, and it's kind of hard to explain in diagram format. So we're just going to write out a quick example of it right above my template. I'm going to add in a event handler that I want to run any time that I click on this button element. So I'm going to call the event handler on button click. So this is going to be a method with a return type of void. And for right now, I'll just do a console log of hi there. Like so. So now I need to somehow tie this method to that button element. I remember this HTML string out here is basically worthless to us. It's only useful once it has been inserted into that template element. So I think that what we should do and this is the part that's really hard to diagram, so just bear with me. I want to add in a method to this thing called events map. Any time we call this events map method, I'm going to return an object with some really special keys and values. The keys inside of here are going to be a string and the string is going to contain first a event name like Click or hover or change or something like that. And then right after that will be a colon, and then the name of the element or a selector for the element that we want to bind the event handler to. So let me show you what I mean inside this object. I'm going to give it a key of Click Colon button. And then the value for that key will be this dot. On button, click. So what we are trying to express here is that we want to somehow set up a click event handler to the button inside of our template. And any time that thing gets clicked, we want to run this function right here. So essentially the keys inside this object are going to have a ton of meaning associated with them. So we need to somehow parse this key right here and understand that we are trying to set up a click event handler on the button. Likewise, we might also set up something like maybe hover over the H one, and if we do that, we might call an event handler called like on Hover H. Or how about on hover header or something like that? And we could also set up maybe another event handler. There was something like Drag Div. This dot on drag div. So like I said, we need to somehow parse the key right here, separate the colon or split based on the colon. And we're going to try to set up a drag event handler on the div. In this case, we try to set up a hover event handler on the H one. In this case, a click event handler on the button. So that's my idea. This is actually how a lot of event handling use to be taken care of way back in the day. Well, these days we've got way more fancy technologies like React and Angular and whatnot. Like I said, this used to be basically how we did it. So I think that we can probably just use this same pattern inside of our framework, and I expect it'll actually work out pretty well. Okay. I'm going to delete those extra two key value pairs on there because right now we only want to set up a click event handler on the button. Now I'm also going to set up a return value on events map as well. Putting together a return value for this object right here is going to be just a little bit nasty because essentially we've got some keys where we don't really know what those keys are going to be ahead of time. So we need to somehow express to TypeScript that, hey, we're going to return an object and we don't necessarily know what the keys are going to be. So to do so, I'm going to put down an object literal right here. I'll put in square brackets and I'll say key colon string. So that tells TypeScript that, hey, we're going to return an object. We don't really know what the keys are going to be, but they're going to be strings. And then we're going to say that the value for everything inside that object will be a function that takes no arguments and returns nothing. As is the case with on button. Click right here. Okay. So this events map is essentially how we're going to relate the different events that we want to watch for inside of our HTML snippet to the different functions that we want to run. So let's take a quick pause right here. I'm going to save this file. When we come back to the next section, we're going to somehow get this events map right here or this object, parse that key and then bind all of our events with it. So quick pause and we'll finish this up in the next video.
