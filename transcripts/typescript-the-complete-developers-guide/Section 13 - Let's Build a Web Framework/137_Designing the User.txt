# Designing the User

In this video, we're going to start taking a look at some of the different properties that a user is going to have. So first off, a user is going to have one distinct piece of data called simply data. And this is going to be some object that stores all the different properties that describe a very particular user, such as their name, their age, and so on. Now notice that I've marked this property right here as being private. Remember, that modifier means that we cannot access that data property from outside this class. So the whole idea here is that we don't want other developers kind of reaching into a user and messing around with its data manually. Instead, we want our different developers who are going to use our framework to eventually use some other methods to manipulate the data. Those two methods are going to be called get and set. We are not going to use type scripts built in getters and setters in this particular application, and there's a very good reason for that that we'll discuss later on through the implementation of this thing. So for right now, just understand that any time we want to change some data related to a user, we're going to use the Git method or the set method. The purpose of Git right here is to pass in the name of some property that we want to retrieve. So if a user has some data tied to it, like a name and an age and we're calling them specifically name and age, we could retrieve the name by calling git and passing in a string of. Name. Oh don't want to delete that much name like so. And that would retrieve the name of a particular user. And then if they wanted to get the age of a user, we would pass in age like so. So because jet can return different types of data. Notice how I annotated the return type of git right here as returning either a string or a number. This is not going to be a long term solution right here. We're going to eventually have a long discussion about that return type and how we can kind of improve it. But we'll get around to that later on. So then to change any data related to a user, we're going to have a set method. The set method is going to take in some object that describes how we want to update information about a user. So for example, if we want to update a user's name, we would pass an object that had a name property and the new name. Like my new name. Like so. And if we wanted to update the age of a user, we would pass an object with an age property and then the new age, whatever that might be like. So. Okay. So those are the first three things we're going to focus on. We're just going to focus on making sure that we have the ability to create a user like an instance of a user class, that a user has some data tied to them, and that we can get those different pieces of data and eventually change them as well. So now that we understand the first goal that we're going to try to achieve, let's take a quick pause right here and then start the implementation in the next video.
