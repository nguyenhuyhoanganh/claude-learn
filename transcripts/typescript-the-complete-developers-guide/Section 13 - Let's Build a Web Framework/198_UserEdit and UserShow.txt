# UserEdit and UserShow

All right, my friends, in this video, we're going to take a stab at user show and user edit. Then we're going to figure out how to nest all these things together so that they show up at the same time. Now, user show and user edit should be pretty straightforward in their initial implementation. So let's just dive right in. I'm going to flip back over to my editor and inside my views directory, I'll make a new file called User Show. It's. So now, inside of here, I need to import my view based class. From The View File. When I set up my view, I'm also going to have to specify the type of model and the properties that that model is going to have. And I'm going to do that by importing. User and user props. From up one directory models user. So then inside if you're I can export class. User show, and that's going to be extending through the extends keyword view. And remember, when we specify view, we have to pass in the type of model that we're going to specify and the properties that that model is going to have. So I'll put in user and user props and then I can define my actual class now right away, because view is an abstract class, it expects us to have certain methods defined inside of user show, and so we'll see some errors around event map and also template. So even if we don't have any events, we're still required to provide the events map function right now. Now, that might not be like the best thing in the world. Maybe rather than requiring that we always have an events map function, maybe we should instead provide a default implementation of it inside of our view class. Because right now I don't really expect user show to have any events tied to it. And so that would mean nonetheless that we would have to define an events map function and return basically an empty object like so in order to satisfy the view base class. It's like I said, probably don't want to have to put this requirement in even if we don't have any events so we can remove that requirement by going back over to viewpoints. And rather than saying that events map is going to be an abstract function, we can define it as a normal function inside of your or like an implemented function and just give it some default functionality that can optionally be overridden by the child class. Okay. So I'm going to move this function definition underneath template. I'm going to take off the abstract keyword. And then I will provide an implementation for the function right here. So I'm going to return an empty object like so. So now we are saying that events map is no longer required to be implemented in a child class. It can be if it wants to, and it will be used if it does, but it doesn't have to be implemented anymore. And remember, we are doing this because we want to make sure that you don't have to implement this method if you decide that you don't want to have any events. So now if I flip back over to user show and I hover over that error, it now says that the only issue is that we don't have a template. So we can now create classes that are just presentational in nature, like they just returned some HTML to be shown on the screen. All right. Speaking of which, let's return some HTML via a template method. So I'll define template that's going to return a string. And I'm going to look at my mockup really quickly. So it looks like we should still have an H1 along with a name and age of our user. So I'm going to return a template string. Remember, we define this with back ticks because we want it to be a multi-line string. So inside of here, I'll put a div. I'll give it an H, one that says user show or user detail. And then I can put in a div. With the username. And I can access the user name because remember, the expectation is that we're going to pass in an instance of a user whenever we create user show. So I can do some string templating right here and refer to this model git name. Now I can duplicate this down. And get the age instead and I'll update that div as well. And that should be it. Okay. So we don't really have a great way to show this on the screen just yet. So let's first start working on the other big class we have. User edit and then we're going to figure out how we can show these all on the screen together very easily by nesting them together. So for user edit, I'm going to open up my views directory. And I'm going to make a new file called User Edits. Then inside of here once more, I'm going to import my view based class. From The View File. And my user and user props methods. From up one directory models user. Then I will export class. User edit that extends view and once again we're going to pass in the model type and the different properties that that model will have. And now inside of here, I'm going to define a template that's going to return a string. And for right now, this is going to kind of lead us in to how we're going to handle all this nesting stuff. I'm going to return a div and inside there are going to be two other divs. Now, why are we going to return some nested divs here? Let's put them in really quickly. I'm going to put div one and another Div right there, and I'm going to give this a name. So I'm going to get the first one, A or B, not a name, but a class. So the first div inside there, I'm going to give a class with a name like I don't know, how about something like user dash form? And then on the next one, I'll give it a class of user dash show. And you know what? Now I think about it, let's actually swap the order of those two. That's better. So user show first, then user form. Okay. So what you see right here is going to be some of the base mechanism that we use for doing some nesting. So let's take a quick pause right here. We're going to talk about how these two divs are going to eventually function as our primary means of nesting views together. So quick pause and I'll see you in just a minute.
