# Adding Methods to the User

In this video, we need to make sure that we actually set a random age on our model. Any time that we run the helper right here on set age, click. All right. So to get started, we first need to think about whether or not this is the best location to actually write out some logic that's going to set a random age. Now, the reason I say that is that we are talking about modifying a user model in a very specific way. There might be other applications or other classes inside of our app where we want to set a random age as well. So usually any time that we try to write out some logic that directly modifies a class or something, a model will usually tuck that into the model class itself as a helper method. So essentially what I'm saying is that I think we need to open up our user model file and add a method to it to set a random age. So then we can have some reusable set random age logic tied directly to the model instead of being tucked away inside of our view class. So let's open up our model's user file. I'll then go down to the very bottom, and I'm going to add a new method inside of here. But I'm going to call set random age. So then inside of here, we can compute some random age between like zero and 100 or zero and 99, whatever, and then set it on our model. So I'll say const age is going to be math round and I'll pass into that math at random times 100. So that will give us our random age as a positive integer. Now we can set that age on the model. And to do so, we'll call this set. And pass in our update. Object of age is age like so. Now we can use a little bit of as 2015 syntax here to shorten that down to just age like so. Okay. So now we've got a additional method on user that we can reuse from any different view at some point in the future if we want to. So now I'm going to save user. I'll go back over to user form. So here's my view class and I'm going to update the implementation of Set Age Click. So any time we click on this button, I want to call this Top model, which remember is an instance of a user. And now on that model we have the set random age method, so we're going to call it, and that should update our model and change the age on it. Okay. So we can now test this out. I'm going to save this flip back over. I'll click on set random edge and sure enough, we end up seeing an error message. So this right here is a super classic error. Super classic. We've kind of already ran into it in the past. Essentially, the issue is the context of this inside of onset age, click. So remember down here, inside of Bind events, we eventually set up an event listener on the element that we find. So this is going to be that button and we pass in the function that we want to be called whenever we pass in a function to be called by an event handler about 99% of the time, the value of this inside that function is not going to be what we expect it to be. So this is a classic issue in React in a lot of cases with Angular as well. All we need to do is bind the value of this inside of the function. So to do so we can define our event handler set h click right here as an arrow function instead of a normal function. So to do so I'm going to delete those prints. I'll say equals and we'll set this up as an arrow function like so. And I'm still going to specify a return type of void. That's all we have to do. So now that we're using an arrow function, the value of this inside of here should refer to our view class of user form, and we shouldn't see that error anymore. In general, every time that we define an event handler or anything that we add to our events map, we're going to have to use an arrow function to avoid the same exact error. All right, so I'll save this. I'll flip back over again. I'll click on set. Random age and no error. Fantastic. So that's good. But the downside is that when I click on the button, I don't see our text over here update. So presumably we are setting a random age. You know, I assume that's working, but I don't see any update inside of our actual HTML. Well, the reason for that is really simple. We haven't implemented that yet. This is one of the nice things about building your own framework. When something goes wrong, you can kind of say, Oh yeah, have I even built that in yet? In this case, no. We don't have anything inside of our view class that says, Hey, any time that the model changes, try to re render. So that's clearly a feature we still need to add in. So let's take a quick pause right here and add in that feature in the next video.
