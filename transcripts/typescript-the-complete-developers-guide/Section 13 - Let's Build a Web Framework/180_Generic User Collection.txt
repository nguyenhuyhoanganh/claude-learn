# Generic User Collection

In this video, we need to turn our collection class into a generic class and essentially remove every reference to user from it. If we have any references to user or user probs inside of here, that means we're not going to be able to use class collection for other types of data inside of our application as well. So I'm going to first delete our import statement for user and user props at the top like so. And then right away, we're going to see a couple of errors up here. Okay. So let's go through each of these. The first thing we need to do is specify collection as a generic class. So I'm going to say that this thing is going to take type T. So tea will eventually be for us, probably like the user type or maybe in the future a class image or blog post or comment or whatever else. RT Essentially is the type of whatever collection or whatever collection of things we're going to store inside of here. So I'm going to say that Models is now going to be an array that contains element of type RT. We can then go down. And down here we now have a reference to user props that we need to replace as well. Now, this is kind of a little bit of a troubling note right here because right now type T is user like the user class. And the user class doesn't necessarily have a reference to type user props. So we need to essentially add in a second generic type to our class to allow us to specify the type of user props. So to do so, we can just add on a second generic type up here at the top. I'm going to abbreviate that second type as K. So remember with functions in general, we can pass in one argument to arguments, whatever. Same thing with generics. So all we are doing here is we are saying that whenever we refer to collection, we're going to pass in a type to be used for PT and a type to be used for K as well. In this case, K is going to be the type that specifies the structure of JSON data that we get back. So now for value. Right here I'll specify k like so. Now the next big issue is that we've got a direct reference to a function right here that's going to attempt to create a user out of some JSON data. So we cannot replace this with our generic type because remember, this is a reference to an actual class or like an actual object, not a type. And right here we are attempting to call an actual function. So we cannot just arbitrarily stick in t like everywhere that we want to t and k only go in locations where we are attempting to specify a type. So to solve this issue right here, I think that we essentially need to pass in when we create an instance of our collection, a function that will be used to take some JSON data and return an instance of a model. So I'm going to add on another argument to my constructor function. I'm going to call it D serialize. So the idea here is that the serialize function is going to take in some JSON data and then turn it into an actual instance of an object and we will need to pass it into our collection any time we create an instance of it. So this is going to allow us to reuse our collection for any type of data we want. So now that type of serialize is going to be a function that's going to take in some JSON. So I'm going to put in a type annotation here for a function. So it's going to take in some JSON and the JSON is going to be a type K because remember, essentially the JSON is the user props looking object. It has the name ID and age. So it's going to take an argument of type K and then it's going to eventually return something of type T, which is the actual thing that we're storing inside of our model's array. Okay. So that's pretty much it. Now we need to make sure that we call it serialize with each piece of JSON Hassan, each JSON model or each JSON record and then get back our instance of a model. So to do so. I'm going to delete the entire const user line right there. And I'm going to push into this models or this model models. I'm going to push in this serialize. Value like so. And that's pretty much it. So we're going to stick in the JSON data we get. We should get back an instance of a model which we are going to add into our array of models. Okay. So let's now save this. I'm going to go back over to my index file and we're going to try this thing out now right away. We've got an air around our collection. I'm going to put a new line inside of here just so we can see the additional arguments we have to pass in. The first thing we have to do is provide some types for the collection itself. So in this case, the first type that we have to put in here is a reference to the user class. So I'm going to import. User. And user props at the top. So the first type is going to be the class that we are going to eventually produce, which is user. And then the second is going to be the description of that JSON object that we get back from the API or the array of JSON objects which will be user props. And then as the second argument, we have to pass in a function that's going to take in an object of structure, user props and return an instance of a user. So I'm going to pass in a function here that's going to take in some JSON that is of type user props and return. User. Build user with JSON. And that's pretty much it. Okay. So I'll save this. I'm going to flip back over to my browser again. We'll do a quick test. So I got the collection again and I still have inside of my model's property and array of records where every one of them is an instance of a user that has the appropriate data attached to it. All right. So that looks pretty great. And the only issue here is that we probably do not want to do all this customization every single time that we have to create a new collection of user. In other words, every time we try to create a collection of users, I don't want to have to pass in the URL and I don't want to have to define this serialized function and so on. So I think that we need to figure out a better place to essentially put this code right here, then directly inside of our index file. So let's figure out a better place for this in the next video.
