# Building the UserForm

In this video, we're going to start working on our user form class. Now, I sat down and came up with a rough plan of how we can implement this class and have some really reusable logic inside of it. So let me tell you about my idea right now. Okay. So I think that inside of class user form, we're going to start off with first one property called parent and two different methods. Let's first talk about these different methods. So the first method is going to be called template and this is going to return a string. The idea behind this method right here is that it's going to return a string that contains some amount of HTML that we eventually want to show on the screen to the user. So in the case of user form, the template method might return a text input a button, button and a button. In addition, we'll also have a render method as well. And any time we call render, we're going to want to take that template and somehow show that HTML inside the DOM. So the template is about defining what HTML this class is going to show. The render method is about actually taking that HTML and inserting it into the DOM. To make sure that we actually can insert this into a DOM or in the DOM. We should also have a parent property on here as well. And parent is going to be a reference to some HTML element that exists inside the DOM. Let me show you another diagram to help you understand what all these different properties and methods are going to do together. Okay. So here's the idea. And as diagram is a little bit complicated. So let's go through it step by step. So first off, we're going to call render and truly render is going to call template and template is going to return a string that contains some amount of HTML. So we can imagine that template is going to return like maybe a form with a label and an input or something like that. The render method is then going to take this HTML and append it as a child to the parent property. So this parent property right here is going to be a reference to some HTML element that already exists inside of our DOM. So we can imagine if this is the DOM over here, maybe the parent element is going to point out that div right there. So when render calls template and gets this HTML snippet right here renders then going to take that HTML and append it as a child to that parent element. So we can imagine that it's going to be inserted like right there and added in as a child. And that's the entire idea. So it's going to be really simple and straightforward in nature, but it's going to work out pretty well. Now, one thing I want to point out here is that we're not going to do any like fancy react style dom dipping or anything like that. We're going to be basically inserting and removing snippets of HTML. So this is pretty primitive, but it's going to be very easy for us to implement, certainly much easier than taking kind of like a React style approach. All right. So let's get started right now. I'm going to flip back over to my code editor and we'll get started on class user form. All right, so inside my C directory, I'm going to make a new folder called Views. And then inside there, I'm going to make a new file called User Formats. Then inside this file, I'm going to create an export, a class called user form. So this thing is going to have a parent property. That's going to be a reference to an HTML element. Now, as soon as I write out HTML right here, you'll notice that we get Autocomplete appearing. So by default we can reference a ton of different types inside of TypeScript without having to actually import them into this file. These are all considered to be inside of the global scope of TypeScript. So whenever we start making use or reference of these HTML specific types, no need for import statements in general. So parent is going to be of type element like so. If we want to. We could always do a command click on element right here and will tell us about exactly what an element is. So you'll see, even though it's kind of hard to read, your element is the most general base class from which all objects in a document inherit. So an element is basically a reference to any HTML element. Next up, we can define our template method. You'll notice I'm getting some autocomplete there that is not super helpful. So let me try that again template and then I'm going to hit escape. So it doesn't try to like autocomplete that this is going to return a string and then right now I'm going to return a multi-line string which we form by using back ticks. Make sure you are using that ticks as well, not single quotes and inside of here in a place a div and for right now we'll just do something simple like an h one. That says user form. And then maybe some kind of input like so. Okay. So let's take a pause right here. When we come back in the next section, we're going to make sure that this thing also has a render method and that we can use that to actually call the template and append it to the parent. So a quick pause and I'll see you in just a minute.
