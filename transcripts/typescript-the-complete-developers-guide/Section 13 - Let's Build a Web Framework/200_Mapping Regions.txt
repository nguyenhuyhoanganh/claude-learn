# Mapping Regions

In this video, we're going to start to implement this region system inside of class view. So let's get to it. All right. So the first thing I'm going to do is add in a new property to my view class at the very top. So I'm going to say that this is going to have a new property called Regions. This is going to be an object. So I'm going to add in an annotation. I don't really know what the different keys of this object are going to be right away. So I have to add in that kind of generic key definition and say that this thing is going to have some number of keys that are going to be strings. And each of those keys are going to reference some element. And we're going to initialize this property as being an empty object like so. So next up, we're going to define a default implementation of region map. We might call this regions map since events map is plural as well. Let's just call it regions map just to stay consistent. So going to add a new method inside if you're called regions map. This is going to return an object of type. Object. It's going to have some unknown keys as well that are going to be strings. And then the values are going to be strings because they're going to be HTML selectors like the string of dot user form or dot user show or whatever else. So then, since this is a default implementation, I'll return an empty object like so and oops, I put it in strings right there. I meant just string. Okay. So now the idea is let's flip back over to a quick diagram. Here's what we were trying to do. So once again, inside of our render method, after we bind our event handlers, we're going to add in a couple of extra steps. So we're going to first call that region map method that we just added in to get a list of all the different regions that need to be created. Now, remember, a region is essentially a reference to some element where we want to nest a view. After that, we're then going to write in some code that's going to loop through all those different regions that we've specified and try to find some element inside of our template that matches each selector. And then we're going to take those elements and stick them into this region's property. Once again, I know this is nasty stuff. Let's just write the code and get through it. So down inside of my render method. I'm going to add a call to a helper method that I'm going to call map regions. So I'm going to call this DOT Map regions and I'm going to pass in a reference to the current template that we are working with. So template element. Not content. And then I'll define map regions right above. So this thing is going to take something that we'll call a fragment as a argument, and that is of type document fragment. Remember, we spoke about what these were much earlier. We were talking about bind events. And then as a return value, we're going to not return anything. Now. I've got a missing G in there. There it is. All right. So now inside of map regions, we're going to first get a reference to our region map. So I'll say regions map is this dot regions map. Then I'm going to iterate through that object and find all the different selectors inside there. So I'll save for let key in regions map. And I'll say the selector is regions map at key. So that's essentially going to be the actual selector that we want to look up inside of our current template to find the element where we should insert this given nested view. So now I need to actually find that element inside of my template. So I'm going to say this dot regions. At Key. So we're going to populate that regions object. Remember, it starts off completely empty. We defined it up here. So we're going to add in all the regions that we find. So I'll say this Regions at key is going to be fragment. Query selector. And we'll pass in the selector that was provided. Now right away we see an error message. This is going to be for a somewhat familiar reason. So any time we call a query selector, we might get a result. In some cases we might find an element. In other cases we won't find an element. So query selector returns either element or null. So we can either update the type of this regions to reflect the fact that we don't know if there's going to be an element there. But a better way to do this would be to say that we're going to only add in an element or a key value pair of regions if we actually found something with query selector. So I'm going to add in a new variable here called Element. I'm going to assign the result of fragment query selector to that. And then I'll say if we found an element, so if it is defined, then insert it into regions. So I'll say this dot regions at key is element like so. And that's it. And once again, I know we're going through this rather quickly. The real takeaway here, the only thing that we really are trying to convey is at the end of the day, we're now going to have an object. Where the keys are going to be the name of some region where we want to nest a view and the value is going to be the element that is going to serve as the parent element. Let's take a quick pause right here. When we come back, the next section we're going to try actually making use of this map region stuff. And I'm pretty confident that as soon as you see this running inside the browser, you're going to say, oh, okay, I get it, I understand what's going on. So quick pause and we're going to actually make use of this in the next video.
