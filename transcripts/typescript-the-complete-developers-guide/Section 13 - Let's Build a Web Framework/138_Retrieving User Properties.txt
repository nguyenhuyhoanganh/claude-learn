# Retrieving User Properties

In this video, we're going to start implementing our class user and giving it the data property get and set as well. So let's get to it. I'm going to begin by flipping back over to my code editor inside my CRC directory. I'm going to make a new folder called Models. This folder is going to eventually hold all the different models of our application or essentially all the different classes that represent some data. Then inside the models, I'm going to make a new file called User Dots. And inside of user dots, I'm going to export class user like so. So the first thing we said was that this thing is supposed to have some data property tied to it and that data property is going to actually represent the information about some particular user. It would be really nice if we could assign some properties to a user when we first create an instance of it. So in other words, it'd be really nice if we could say like new user and then right away pass in an object with this user's name and their age, whatever it might be like. So. So we not only want to have a data property tied to a user, we also want to be able to specify that data property when we create an instance. So that means that we should define our constructor function and make sure that it can receive a first argument. That is an object that looks like this right here. And we need to take that information and assign it to some property on the user. Okay. So I'm going to define my constructor. I'm going to give the first argument a modifier of private, because remember, we don't want other engineers reaching in directly to our class and modifying data. I'm going to say that this property is going to be called data, and then I'm going to put in a type annotation for that object we were just looking at. So it has to be an object with a name that's a string and an age that is a number. So we could express that in a type annotation by writing out an object and name. That's a string. An age that is a number. Now, remember we discussed. This type of annotation much earlier inside the course. And we had said, you know what? Writing out a type annotation for an object literal directly in line like that is really not the best thing in the world, because it means that, well, if we ever add in more properties, we're just going to have to add them on to this line and the line is going to get longer and longer and longer. So one way to save space is to extract that definition of that object right there into an interface instead. Remember, interfaces are not only used to somehow get some amount of code re-use between different classes, they are also used to create types that describe object literals. So let's create an interface at the very top. And I'm going to call it user props. So the purpose of this interface is to essentially describe that type right there, the different properties that a user is going to have. So it's going to be a name that's a string and an age. That is a number. And now we can delete that long form type annotation right there and replace it with only user props. And then finally after the constructor, I'll put in my curly braces like so. Okay. So that's a pretty good start. So now the next thing we want to do is make sure that this thing has a JIT method that we can use to retrieve some information off of that data property. Remember, we don't want other developers reaching into our user and messing around with the data property directly. There's a very good reason for that that you're going to see in a little bit as we start to change and add some more functionality to our application. So I'm going to add in a JIT method to my user class. This thing has to be coupled with some property name or prop name, as we will call it. And that is the name of the property that we are trying to retrieve. So I'm going to receive that as an argument called prop name. It's going to be a string. And then from this, we're going to return either a string or a number because someone who calls git can be requesting either name or age. So we're going to put in a type union right here. We can express a type union by using the parentheses and then saying name or assuming not name, but number or string. Remember, that is a type union. It means that we're going to return either a number or a string from this method. So now inside of Git we can return this data and we will look up the property crop name like so. All right. So that looks pretty good. So now, in theory, we can actually test this out by exporting. Well, we've already got the export right there. We can flip over to our exports file. We can try to create a new instance of a user and then try to test out the code that we've written so far. So let's do that right now. I'm going to flip back over to indexes. I'm going to delete the console.log statement at the top, and I'm going to import the user. From the models slash user file. So then I can create an instance of a user and I can pass in some initial properties for this user. So I can say, okay, first user is going to have a name of my name and an age of 20. And then if I want to retrieve one single property off the user, I could say user. Target and I can get their name. Or I could do a user docket and get the age. To make sure that we can retrieve those correctly. I'll put in a console log for both them. All right, so I'll save this file. I'm going to flip back over to my browser. I'm at localhost. One, two, three, four with my console open and I can plainly see the name of the user and their age as well. So that looks pretty good. So let's take a quick pause right here. When we come back to the next video. We're going to make sure that we give our user class the ability to change that data as well. So quick pause and I'll see you in just a minute.
