# Users Collection

Our class user is looking great. Our class model is also looking great. But there's one big hole in our entire setup right now. Remember our fetch function and how it works? I'm going to open up my API sync file inside of here. We've got the fetch function. Remember we said that we can only call this thing if we have the ID of the record that we want to fetch. And this is actually a really big problem because it introduces a kind of chicken and egg scenario inside of our program. The problem is that when we first start up our application, we don't actually know what IDs are available. So when we write the code for our app right now, we have to hard code some IDs in as we are doing right here. If we have some big collection of users stored inside of our JSON server, we cannot currently somehow get a list of all those different IDs and know what different users exist. So we would have to actually write out some code that maybe creates a user with ID of one and then a user with ID two and then maybe three and so on. And then fetch each of these. And then only after attempting to do a fetch and receiving some data. Would we know that a user with an idea of one exists or one with two exists, or three and so on. So that's clearly not a good solution. So we need a way, better way of somehow figuring out what different users exist on our back end. So to do so, we're just going to use some of the built in functionality of JSON server. I'm talking specifically about those restful conventions that we spoke about before. So remember this, we had said that one of the different convention methods on here. What's to make a gift request to slash posts or in our case, slash users? And if we do that, we should retrieve a list of all the different users that exist on our back end server. And so we can essentially use this root right here as a way to figure out what different users exist on our back end. So with this, if we ever wanted to show a list of all of our different users, well, we can make a request to this endpoint, get all of our users and to the we now have all of our users and we can render them all out on the screen somehow. So in order to build this out, I think that we need to make a collection class of sorts, something that we can call like class user collection. Maybe we'll have a models property that will be an array of the users. Maybe it will also have access to that events thing that we put together and then also a fetch method. So if we call fetch user collection will make a request to slash users, get all the users, get a ton of that JSON data and then take all that JSON data and turn them into instances of users and then to make them all available to us on the models property. So let's try putting this thing together. Now, one thing I want to note that you can probably kind of guess right now is that we're going to build this thing out and call it user collection, but eventually we're probably going to want to make this thing reusable in nature. So I expect that eventually we will do a refactor and call it something like class collection instead. And then we will somehow have to specify this type of user using a generic, because we will want to use this class collection to represent many different types of records, not just users. So just keep that in mind. That is a refactor that we'll do at some point. But just to keep things easy, we will first start off just trying to build something to work with only users. Okay. So now that we understand what we are doing, let's take a quick pause and start working on this new class in the next video.
