# Saving User Data

In this video, we're going to work on our safe method inside the user class. So I'm at the very bottom of the user file. Here's the fetch method right underneath it. I'm going to define save as well. Now, just like fetch, I don't think that save really needs to return anything just yet. So I'm going to give this a return type of void like so. And then in addition, I don't think it really needs any arguments either. So I'll leave those off to. Okay. So now inside of Save, it's not going to be quite as easy as just making a network request. You see, there's actually one consideration we need to have. We start to think about saving data tied to a user. There's actually two scenarios we have to worry about. Remember, we had said that there really is a very strong responsibility of that ID property. If a user has an ID, that means that a representation of this user already exists on our back end server. So that means that if we are trying to save a user that has an ID, we are trying to update an existing record. And remember, we just spoke about those restful conventions. If we are trying to update a record, we want to make a put request to slash users slash and then the ID and attach all the update information along with it. However, if a user does not have an ID, that means that this must be a brand new user. They're not persisted on the server. And so when we make this network request, we want to create an entirely new record. And if we are following our restful conventions, that means we have to make a post request along with all of our information about this particular user. So in other words, inside of our safe method, we have to check to see if our user has an ID. If they have an ID, we're going to make a put request. If they don't have an ID, we're going to make a post request instead. So with that in mind, I'm going to flip back over and we're going to put our save method together. So inside of here, I'm going to say if this dot get ID. Then we need to do a put. Else we need to do a post and that's pretty much it. Okay. So let's try taking care of our request first. So I'll say Axios. Dot put. And I'm going to use a template string here again. I'm going to make a request to a HTTP colon slash slash local host colon 3000 slash users. And then we want to put in the ID of the user we are trying to update right here. So I'm going to say once again, this git ID now right away you'll notice that we've got a duplication of this target ID. So I'm going to just extract that up here. I'm going to say const ID is this git ID and now we don't have to have the duplicated function calls so I can update the if statement to if ID. And then inside of our link right here, I can say simply ID. All right, so it looks good. So now it's the second argument we're going to pass in all the information about this particular user. So all the information is essentially that data property. Remember that data property alone has all the custom information about this very specific user, in this case, very simply, the name and age. And so I'm going to pass in as the second argument this dot data like so. Okay. Now, I'm going to remove that comment right there. I don't think we really need that anymore. And then we'll take care of the Post case. So this is the case where we don't have a user yet, so we need to create one by making use of a post request. So in this case, I'll do Axios. Dot post. I don't need to use a template string this time because there's no idea that we are trying to refer to. So I can use a plain string here and I'll go to http localhost, 3000 users like so. And then as a second argument, I'm going to pass in all the information I want to send, which is this dot data. Okay. So it looks pretty good. Now, one thing I want to mention here, something that I'm sure you can guess. We are currently hard coding this local host string inside of our user class, so it's entirely likely, even almost definite. I would say that this big URL right here is not always going to be local host 3000. So at some point in time, we definitely are going to have to figure out a better way of handling the URL. But for right now, it'll work out just fine. Okay. So to test this out, there are two scenarios we have to deal with. First, we need to try to create a user that already has a backend representation. We then need to update some information on that user and then attempt to save it. And the second case is we're going to create a brand new user without an ID, give it some information, and then try to save it as well. So I'm going to save user dates. And then to test out those two scenarios, I'm going to go back over to my index file. Now back over here. I'm going to delete the user fetch and the set timeout. So this right here is a user that already has a back end excuse me, a backhand representation. So I'm going to try to update some information on the user, like set a new property on it and then I'll try to save it. And once I save it, I should see my DB json. I should see this record right here change. So notice how currently I have a name of my name and an age of 20. So I will expect to see those two records change once I save my user. Okay. So right here, I'm going to do a user set. I'm going to set the name. Oops. Set the name to something like new name just to make sure it's really obvious. And then I'll also update the age as well. So I remember I just did these in two separate set statements. You don't have to do that. You can do the combination or the update in both or just like one single line like so. And actually, just to be clear, I'm just going to stick with that route. Now, another thing I just realized I didn't actually change the age there, so I'm going to put it to like some really big number like so. Okay. So now they've updated this user and this user has an ID I should be able to call user save on it and then save this file, let my browser refresh and then I should see an update inside my DB JSON file to that existing record. So I'm going to save this. I'll flip back over to my browser. I should see a put request which indicates that I am updating a record. So that right there is the response that I got back from the server, and that means I should be able to go to my DB JSON file and see the new name and the new age. Perfect. So now the next thing we need to test out, we need to make sure that we have the ability to create a new user without an ID. And if we try to save it, you should create a brand new record inside of DB JSON. Okay. So I'm going to delete the user set and I'm going to change my user constructor, so I'm not going to give it an ID. But I will give it a name of new record. At an age of about zero this time. So in this case, we're creating a brand new user doesn't have an ID, that means it doesn't have a back end representation. So we should be making a post request to create a brand new user on our back end. So I'll save this flip back over to my browser. Yep. There's the post request. Go to my JSON file and I should see a new record inside of here with the name of new record, an age of zero and an ID of two. Awesome. So this is looking pretty darn great. So we now have the entire spectrum of abilities tied to our user. We've got the ability. To represent properties of a user so we can get them, we can set them. We have some like venting functionality here to set up callbacks and whatnot that we're not quite using just yet, but we will in a moment. And then we've got the ability to fetch data tied to a record or a user and to save information tied to a user as well. So this is looking pretty great, but at this point in time we haven't really written a lot of brand new typescript just yet. I guess we kind of have. We've learned a couple of things. But, you know, as we're going through this, it definitely feels like, as we mentioned earlier, we're kind of creating a mega user class of sorts. Remember, that's what we spoke about earlier on. I told you you're going to build out this like mega user class. And that's what we are definitely at right now. We've got a user class that does a lot of very, very different things. So I think that we're at a really good point. But right now I want to try to start refactoring our user to instead make use of composition. So just to give you kind of a different flavor on how we could potentially build up this user by generating separate different objects for each of the responsibilities that a user currently has. So let's take a quick pause right here and come back in the next section and start doing our first big refactor.
