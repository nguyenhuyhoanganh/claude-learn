# Final User Refactor

All right, my friends, this is going to be the last little bit of refactoring we have to do. So the last thing we have to do is add a static class method to user that's going to give us a pre-configured version of the user. So it'll be a copy of the user that already has an appropriate attributes, copy loaded up and events and sync and so on. Before we start that, let's do a quick refactor here. Remember, I had said that we probably should give sinc a more appropriate name, so I'm going to do that right now really quickly in class or the file sync, I'm going to rename it to API sync. I'll then open up that file and I'm just going to rename the class to API sync like so. Okay. So now that we've got that wired up, I'm going to go back over to user dots and I'm going to import attributes, eventing and API sync. So at the top, I will import attributes. I'll import API sync. And I'll import. Eventing. Then inside of class user, we will set up our static class method method that I'll call build user. So this thing is going to be called with some starting initial properties for our user. So I'll call them adders short four attributes and it's going to be of type. User props. And then from this static class method, we're going to return a pre initialized user. So then inside of here, we'll say return a new user. And inside there, we're going to specify the three different classes or instances to use for those different properties. So the first argument needs to be something that satisfies the attributes or model attributes interface. So that's going to be a new attributes. So I'll say new attributes we're going to pass in the starting attributes, which remember we received as adders. And we also need to make sure that this gets specified as a generic as well. So inside of your new attributes, I'll put down my ankle brackets and I'll say, use their props. Then the second argument is going to be a new eventing. What do we call that thing? Yup. Eventing. And then finally new API sync. This thing is also a generic, so we need to specify user props and we also need to give the first argument right here of a route URL. So let's say route URL like so. And that is it. Believe it or not, that is it. That's all we got to do. So now any time that we want to create a user, we'll call a user build user and passing pass in the starting properties for them as the adders argument right here that will then create a new instance of a user. Initializing these different modules to use. So those will all get passed into model. They'll get picked up by model and set as attributes, events and sync. So because last user extends model, we can now create a new user. You mean not new user like that. It would be user build user and then pass in our starting arguments. And then on that user, we can do all the stuff that we've spoke about for so long. Like, say, get the ID we could do a user get. I don't know what else do we want to do on here? Like get a name. Yet age, we could do a user sync, a user fetch, whatever else we want to do. Now you will notice we've got a little air here. The air is build user does not exist. Oh, my mistake. I called it build, build, build user. That's better. All right. So that's pretty much it. So let's take a quick pause right here. We'll come back in the next video and we will test this thing out. So I'll see you in just a minute.
