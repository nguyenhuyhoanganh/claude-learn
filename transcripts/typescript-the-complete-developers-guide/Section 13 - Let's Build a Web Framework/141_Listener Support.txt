# Listener Support

Now that we've got a better idea of what On and Trigger are all about, let's start writing out an implementation for them right now. So I'm going to flip back over to my code editor. I'm still inside of class user, so I'm going to first stub out the on method and the trigger method and give them some working signature that we can kind of get started with. So I'm going to say that this class needs to have an on method. It's going to be called with some name of event that is a string. How about rather than calling this name, because that might get confused with our user name property. Let's instead call it like event name. That's probably a little bit more clear. And then the second argument is going to be some callback function. So I'm going to receive that as an argument called callback. And then we have to write out a type annotation right here to indicate that this is going to be a function. So to indicate that this is going to be a function, I'll write down parentheses early brace. First me hero curly braces like so. So that's how we indicate that callback is going to be a function. In this case, we are indicating that it is a function that's going to receive no arguments and return no value. We might eventually update that in the future, but for right now, this will work out just fine. Now, if this syntax right here looks a little bit nasty, we can create something called a type alias and essentially pull out that type annotation to somewhere else inside this file. Let's try that out right now just because it might come in handy in just a little bit. So at the top of the file, I'm going to create a type alias or a empty function or essentially a function that has no arguments and no return value. So to do so, I'll write out type, then the name of this type that I want to create. I'm going to call it callback. And then I'm going to write out the type that this is going to refer to. And so it's going to be a function that takes no arguments and returns nothing. So now, rather than writing out this kind of function type right here, I can instead just refer to callback. Setting up a type of alias like this right here for a function is really nice because when you start to write out aero functions inside of your actual implementation code, life gets kind of confusing really quickly. Some people might look at this and say, Oh, callback like, is this function right here? Why is it an empty function? Well, that's not a func we're not creating a function there. That is a type annotation. We are saying that callback is going to be a function with no arguments and no return value. So creating a type alias like this helps to add just a little bit of clarity to our code. So now down here, rather than putting in that type annotation, we can instead refer to the type alias we just created by saying, Well back. Okay. So this right here, all we're saying we are going to call on with a string as the first argument and a function as the second. That's it. Nothing more. Okay. So now here comes the kind of hard part. We need a way to store all these events. So when we call on, we're going to pass in the name of the event that we are listening for. You and I do not really yet know the different names of events that we might have. You just saw a moment ago inside of my browser console. I showed you an example of. Attaching a click event handler to the body element. So I see the first argument to this thing I passed in Click is that meant that I was watching for the Click type event. There are many other types of events that we can listen to that are built directly into like normal HTML and JavaScript. So we can also listen for, say, a hover event or, I don't know, a drag event or a mouse up or a mouse over or whatever else. These are all different types of events that we can listen for. So we need to make sure in a very similar fashion that our on function has the ability to receive many different types of event request listeners. So we need to be able to receive all these different strings and kind of like hold on to them all or kind of catalog them and get them ready to be called at some future point in time when we actually want to trigger this event. So that's going to be just maybe like a little bit confusing. So let's take a quick pause right here. We're going to come back to the next video, and I'm going to show you a diagram to really help you understand what we're trying to do here with this next step and show you exactly how we're going to solve this thing. So a quick pause and I'll see you in just a minute.
