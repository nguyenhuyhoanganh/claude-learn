# Optional Interface Properties

In this video, we're going to make sure that we have the ability to change the data that is tied to a user. So to do so, we need to implement a set method. Remember, when we call set, we're going to pass in some object that contains all the different updates that we want to make to our user. So the object might have a new name with a new name that we want to assign to our user like so. All right. So I'm going to flip back over. And inside my class user, I'm going to define a new set method. So when we call set, I don't think that we need to return anything at all. So I'll give this a return type of void. And then as my argument, I'm going to receive that as something that I'll call about, just like update as it is basically an update to the properties of this user. I guess that makes sense. And then the subject can be or excuse me, this argument can be an object that has a name that is a string or a age that is a number, because those are the two properties that we can possibly update on our user. Now once again, we've written out a type annotation for an object literally right here, so we can replace that object literal. Type definition with the interface that we just defined instead. So let's do that just to save a couple of characters. So I'm going to replace that type annotation with user props. Okay. So now inside of here, we're going to look at that update object and use it to update all the information that we have on data. We could do this by trying to iterate over the update object and taking a look at each property one by one. Or we can do this entire update in one single step instead by using a function built into JavaScript called object assign. The object assign method is going to take two objects. The second object that we pass in is going to have all of its properties taken and copied over to the first and study essentially does exactly what we are trying to do here. So we're going to say. Take our this data object. Remember, that is the object that records all the information about this particular user. And then we want to take the update object that we just passed in and pass it in as this second argument. So this basically says take all the properties on update and all the values on there and just copy, paste them over onto this data and override all the properties on this data. So whatever is on there is going to get overwritten and replaced, which is exactly what we want. Okay. So let's now save this. We can flip back over to index.html and we'll try to write out some code to now make use of our new set method. So back inside of indexes, I'm going to. Add in some new code right after we create our user to update the name and the age. So let's say user. Set and then I'll pass in an object to update the properties of this user. So I'm going to say that I want this thing to have a new name of new name and an age of about 99. So we're going to create our user with one set of properties. We're then going to immediately update them and then we will print out the name and then the age. So let's save this flip back over to our browser and test once again. All right, I can see new name and 999. Perfect. Well, almost perfect. You see, this actually has one little downside. Take a look at this. Back inside of indexes, I'm going to try updating only one property. You know, chances are we're not going to always want to update the name and the age at the same time. We probably are going to more frequently want to update just the name or just the age. So what would happen if I try to only update name? When I try to only update the name, I very quickly get an error message so we can take a look at the error message or I can just tell you what's going on. So here's what's happening. Back inside of user dates, we said that the set method was going to be called with some object that conforms to the user props interface. And inside of our interface we said that an object to satisfy this interface must always have a name, property and an age as well. So the problem here is that if we want to only update one property, we no longer are conforming to that interface. And that means we can't just update one property at a time. So in order to fix this, we're going to make a little change to our user props interface. We're going to say that both the properties on here are optional. I can mark them as optional by putting a question mark after each of the property names like so. So now this says In order for an object to satisfy this interface, it can have a name and it can have an age. But if it doesn't, that's totally okay. So now that we've made that change, we can save this file, flip back over to the indexed file, and you will see that that error has now gone away. So the question mark makes a property optional, so we no longer have to provide both a name and an age when we try to pass in an object to set. We can provide just one or just the other. It's like I can change this to age with an age of nine, nine, nine, or we can still do both. Technically we can also do no properties at all. Now, that's actually kind of weird behavior, but it's not strictly forbidden. Like this actually won't cause any issues inside of our code. It is weird and we probably wouldn't want someone to do this, but nonetheless, it's technically not going to break anything, so we can't allow that. All right. So now we've got a lot more flexibility. The other thing that is nice about having these optional properties on that object type. Is that when we create a new user, we do not have to initially specify a name in an age. So now that the name and the age on the user object is optional, we can create a user without any properties which actually would be very useful. For example, sometimes if we want to present a form to a user that asks them to sign up to our application, we might want to create an empty model and then have a user enter in an email and a password. So sometimes we do have to create some object like this without any initial values inside of it, and then only assign or initialize those at some future point in time. Okay. So this looks like a pretty good improvement. So I'm going to save this file. We'll take a quick pause right here, and we're going to continue working on our user class in the next video.
