# Connecting Sync Back to User

In the last video, we defined class sync as a generic class. This class now has a constraint on it, so whenever we reference, sync or use it as a type or do anything with sync whatsoever, we have to pass in a type right here that's going to have a defined ID property. That is a number. Now that we've got class sync all put together, let's flip back over to our user file and try making use of it inside there. As soon as we start making use of class sync inside of user tests, we're going to run into another error. And this is going to be a really interesting error because it's kind of, in my opinion, a weakness of TypeScript. We're going to introduce an error that we have to fix. And in fixing it, we're going to essentially introduce another bug into our program that TypeScript isn't going to find. So this is going to be an interesting little video here and the next one. So let's get to it. So we need to wire up, sync back into our user, because remember, the entire idea right now is that we're trying to use a composition style approach. So at the top of the file, I'm going to import sync. From the sink file. And now we need to somehow create an instance of sync and assign it to class user. Remember, we had a discussion about a couple of different ways to approach this. Right now, we had settled on assigning a hard coded copy of, in this case eventing to the events property of user. So whenever we create an instance of user, we're always going to get a copy of eventing. Remember, this is kind of a not super great approach right here because it means that we can not very easily swap out different sub modules inside of class user. And that is one of the goals of composition. Now when we did this with eventing, we said, well, that's probably okay because realistically we're probably never going to swap out eventing. But the same is not really true of class sync with class sync. We have an implementation of a sinking class that's always going to reach out to some defined API with a request to retrieve information or put in post to save it. So in the case of class sync, it is very easy to imagine a scenario where we might want to sync data from a model to some outside source besides an API. So a very easy way or a very easy case to think of would be if we wanted to make some class that instead saved data about a user to local storage on a user's device. So in the case of eventing, well, probably not going to swap that out any time soon. But in the classic case of sync, well, it's way more likely that we might decide to swap this out at some future point in time. Nonetheless, we're still going to define a kind of sync property on user, and we're going to hardcoded an instance of sync to it just to use the same technique as we did with eventing, even though, like I said, way more likely that we might want to swap out sync at some point in time. Don't worry, we are going to come back later on and kind of address this shortcoming. Okay. So I'm going to say. Inside of class user. Whenever we create an instance of a user, we're going to get a public property that we're going to call sync. It's going to be an instance of the sync class. As soon as we put down sync, we get an ear right here because we have to specify a generic type for it. So we want to create an instance of sync where we're going to use user props. So remember that means that inside of class sync, wherever you see PT, we're going to replace it with user props just like we had before. So I'm going to put inside of here user props. Then this is going to be an instance of sync. That is configured to use user props. And remember, any time we create an instance of sync, we have to pass in a URL to use as the root URL. So this would be way better. Remember, we had set this up as a constructor property because we want to make sure we could customize the root URL to save some data to when we actually created an instance of sync. So we just have to pass in essentially our local host path right there. So I'm going to define this as a separate variable above the class. I'll say route URL is http colon slash slash localhost, 3000 slash users like so. And we can pass that into sync. Perfect. All right. So I'm going to zoom out here. And now here is the interesting error that we have to deal with. So this is the error that we're going to fix up. And in the process, we're going to kind of introduce another error into our application that TypeScript is not really going to detect. So let's take a quick pause because we're going to need just a little bit of time to understand exactly what's going on here. So quick pause and I'll see you in just a minute.
