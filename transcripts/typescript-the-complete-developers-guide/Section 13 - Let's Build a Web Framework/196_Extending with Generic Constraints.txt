# Extending with Generic Constraints

In this video. We're now going to continue by making sure that class view does not have any references to a user inside of it. Remember, a reference to user means that this view right here can only ever be used with a model of type user. And chances are, we're going to eventually want to model out comments, blog posts. Blah, blah, blah inside of our application as well. So we need to turn view into a generic class. To get started, I'm going to first delete that import statement for user at the top and then I'm going to go look through this file and make sure that this is the only reference to type user inside of here. So I can take a quick glance through. And yep, that's the only reference to user so we can now turn view into a generic class. So I'll add on those English brackets with T and then we can go through the class and instead of referencing user, we'll reference T instead. Now, as soon as we do that, we're going to see another era up here, around here on this model. So remember, we've seen this with generics several times now. Any time that we use a generic typescript is still going to attempt to check all the different references that we have to properties and methods on different objects. So in this particular case, we're saying that model is going to be of type T. So TypeScript does not know, it has no clue whatsoever what different properties and methods model might have. So then later on we have some code right here where we try to access an on method on model. And TypeScript is complaining saying that we don't have a property called on on type T because we just don't know what type T is. Now a better way to phrase this. Their message might be something like property on might not exist on type T that Mike might make like a little bit more sense. So type T might have a property on, but it might not. And that's why we're seeing the air. So just like before, to fix this up, we have to add in a type or simply a generic constraint. Remember, a generic constraint is going to be applied right here. We're going to essentially guarantee the type typescript. That type T is going to have a certain set of properties and methods tied to it. So one way that we can implement a generic constraint is to build up an interface directly inside this file. And inside that interface we can list out all the different properties and methods that we would expect to you to have. Now, in this case, DT is going to be the type of our model. So really the interface is going to describe the different properties and methods that a model should have. So the first thing we have to do here is think of a name for the interface. We could use an interface name of like model because that's literally what we're trying to describe here. But that might get a little bit confusing because we've definitely already got some stuff in our application called model, specifically class model. So maybe a better interface name, as bizarre as this might sound, would be like model for Vue or something like that. You know, kind of implies that this is going to be an interface that specifies the different properties that a model must have in order to be supplied as a type to a view. I don't know, maybe that's not a good name, but honestly, I sat here for like 5 minutes before this video trying to think of a better name, and I just couldn't really think of one. So now inside of this interface, we could list out all the different properties that we would expect type T to have. So I can say that this should have an on method. That takes an event name as a string. And then some callback function. That returns nothing. And then we would put on a return type for the on method as well. Avoid like so. So now we can apply this interface as a constraint to our view or to our generic. So let's say TT extends model for view like so. And now that error message goes away. Okay. So that definitely works. One thing I want to point out here is that we technically kind of could also use the model class directly as a generic constraint. So remember, when we create a class as in the case with model right here, here's class model. When we create this class, we are technically creating two things. On the one hand, we are creating a class that we can create an instance out of. On the other hand, we are technically also creating a type or an interface, so we could use our model definition as the type constraint on T right here. And that would make sure that whatever we pass in as type T would essentially be an instance of a model. Now, honestly, that might be the best solution, because if you think about it, the path that we're going down right here where we're specifying this interface, we're going to eventually have to add on all the different properties that a model has directly to this thing. Now, if we make use of this interface based solution, it means that we're going to have really good separation and no direct coupling between view and model. However, at this point, we've said very clearly that we expect every view to have a reference to a model. So if we continue to go down this interface route, we're going to eventually just add on back all the different properties that a model has to this thing anyways. You might be saying, Stephen, why would we add on all the different properties? Well, remember, our references to this model are not only inside of view, we're going to eventually also have references to our model inside of user form as well. And as a matter of fact, if you flip over to user form right now, you can see some errors around this model. We're technically seeing these errors for a different reason. In this case, we haven't specified a generic type on view. That's why we're currently seeing this air. But even if we fixed up that generic type, this would eventually show an error as well. Because if this was a type T, then we would TypeScript would have to make sure that that type has a set property assigned to it. And to make sure that it has that set property, it would look back to this generic constraint over here. And to make sure that the generic constraint described that set method, well, we would have to add it on to interface. So like I said, we would eventually just be adding back on all the different properties that a model has to this interface anyways. So maybe not the best solution in this case. You can definitely go down this route and you would have a really good separation between your view, class and model. But once again, I just don't know that there's a good reason for that. Okay. So for us, I'm going to delete the interface and I'm going to import model. From up one directory models slash model. And now I'm going to use model as my generic constraint. Now just one issue. We've now added on the generic constraint which essentially says that type T is going to have all the different same properties as model does. So that includes like on and whatever else. But now the new issue is that remember model itself is a generic class. Here's class model, model is a generic class. So in order to ever provide a reference to class model, we have to provide a type for T. So that kind of puts us in a bind here. And if we're going to specify model right here, we need to simultaneously give model some type as well and say like for example, the appropriate specify or the appropriate thing to put in. There would be something like user props. Remember, that was the generic type on model we were discussing or assuming we were designating all the different properties that an instance of a model would have. So for example, we would pass in user props like so. However, we obviously can't put in user props right here and it kind of makes you wonder, well, what in the world would we put in here? Well, let me just kind of cut to the chase here. Essentially, the best solution is to say that whenever we create a view or specify view in any way, we're going to pass in to generic types. We're going to pass in T, which is going to apply this generic constraint of model. And then we're going to also pass in a second type that we'll call K. And then we're going to make use of K not inside of the body of the class. Instead we're going to make use of K right there. Now, this is incredibly nasty, in my opinion, because this looks very dissimilar from any other syntax you would ever really expect to see. So remember, we've been saying that we want to think of these generics as being like arguments, but for types instead. So that's what we're still doing here. But the reason this is so nasty is that in this first argument, so to speak, we essentially have a reference to the second argument. So this is saying that t is going to have all the same properties as a model with type K loaded into it. And where is the definition of K coming from? The definition of K is coming from the second generic type being passed in. So whenever we make reference to view, we're going to pass in a type of model. And then the second type that we're going to pass in is going to be the set of attributes that are going to exist inside of that model. So. Kay, we can imagine. I wish I had, like, a little diagramming tool right here. What I would do is I would draw an arrow from this K over to that K. That's what's happening here. This type K is being like copy pasted over there. So if we passed in user props like so. It would be user props right there as well. That's essentially what's going on. So that's how we're going to fix this issue around model, also having to have a type specified for it as well. Now, I know this looks super nasty. I would encourage you to just like take a look at this for a little bit, maybe even pause the video. The most important thing to understand is, remember, every single time that we reference model, we have to pass in the set of properties that we expect a model to have. And usually like for right now in our current application, that is the user interface. So we specify that it's going to possibly have an ID, a name and an age. Okay. So that's pretty much it. Now, the benefit to going through all these different hoops here, let me maybe that would be the best thing to do. Let me tell you, like the benefit to all this, let's finish wiring this stuff up. So I'm going to save you points and I'll flip over to user form and then inside of user form, I'm going to add on additional import to user, I'm going to add in user props and now I'm going to specify my generic type on view. So on this thing, I'm going to pass in a type for T, which is going to be user and then it type for K, which will be user props like so. And now you'll see that all these errors go away. Now, like I said, maybe the most effective way to help you understand why we are going through all these hoops here is that now inside of class user form, TypeScript understands that this model is going to refer to an instance of a user. And furthermore, TypeScript now also understands the different properties that user is going to have. It understands that it's going to have the set of properties of user props. So that means that anywhere inside of our class we can refer to. This model, we can do a JIT, we can try to retrieve the name and notice how TypeScript is 100% aware of what a git with name is going to return. It's going to return a string. And it only knows that because we passed in user props. All right. Again, I know this stuff is like a little bit nasty. This is like one of those kind of barriers that you have to jump over with TypeScript, where, honestly, my mind, I'm like kind of at a blank of how to clarify this further. And this is one of those things, unfortunately, you just kind of get have to get like a little breakthrough on your own to kind of internalize. I know that's not helpful. I know it. I know that's not helpful at all. But honestly, trust me, as soon as you start writing some code and you run into this stuff around generics, I'm pretty darn confident that you're going to come up with solutions exactly like what you see here. You really just have to be presented with a problem yourself before you can really understand what's going on. So unfortunately, it's kind of one of those things where you have to experience it for yourself. It's not something that can quite be so easily taught. Okay. So enough of all this, we've now got class user form all set up. It extends our view and we've loaded in the definition of what a user is and the different properties that user is going to have. So let's take a quick pause right here. We're going to come back in the next video and we'll talk about how we're going to do this nesting stuff so quick. And I'll see you in just a minute.
