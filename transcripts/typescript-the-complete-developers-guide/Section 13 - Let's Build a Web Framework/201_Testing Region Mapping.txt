# Testing Region Mapping

In this video, we're going to test out this map region stuff. So the first thing you have to do is go back over to user edit PTS. Remember inside of user edit, we want to nest user form and user show. And I want to nest them inside of that div and that div right there. So here's the process we would normally go through inside of user edit I would add in a method to override. The method that we added over here of regions map. So remember, just like with events map by default we return an empty object. But optionally a child class can choose to implement events map and actually return an object with a set of events to bind to. So same exact thing here with regions map. We have a default implementation, but if we want to we can override it inside of the child class and return an object that contains all the different regions we want to look up. So inside of here, I'm going to define a method called Regions Map. This is going to return an object and we can add on the annotation here. It'll be the exact same thing. So it's going to be an object with some unknown keys. That are going to point to strengths. So then inside of here, I'm going to return an object. I'm going to give it a key of user show and that's going to have a selector for that div right there. So I'll write in a selector of dot user show. And then I'll do another one for user form and I'll provide a selector for that element right there. So that would be dot user form. Okay. And that's pretty much it. So now as soon as we render user edit to the DOM, the render method that we just put together, here's the render method is going to call map regions. Map regions is going to run it's going to look up the regions map that we just defined inside of user edit and it's going to loop through both those key value properties and attempt to find some element in the DOM that matches both those selectors. Once it finds those elements, it's going to add all of them to this regions property, this regions object. And remember, this is kind of like step one of nesting one view inside of another. So at the end of the day, all we're trying to confirm right now is that if we now console.log out an instance of user edit, we should see a regions property that is going to have a user show and a user form property and they should point to actual divs that exist inside the dom. Okay. So to test this, I'm going to save this file. I'm going to go back over to indexes. Over here. I'm going to delete all this test code we put together. Actually, how about let's not have to rewrite all this. Let's do this intelligently. So I'm going to update the import statement for user form. We'll get user submit user edit instead. And I'll get that from the import name and the file. And then when we actually create an instance down here, I'm going to make an instance of user edit. I'm going to update the incidence variable name just for fun. And then after we rendered that thing to the DOM, I'll then do a console.log. And remember, the goal right now is just to make sure that we've got a regions property that is an object with references to DOM elements inside of it. Okay, so I'll save this. I'm going to flip back over. You know, open up my console. Here it is. So here's user edit. It has a regions property. Has a key of user form that is a reference to an element inside the DOM. As a matter of fact, I can mouse over it and see that it's a essentially div with no height right now. And the same thing for users show as well or user show. So we've now got references to these two elements. So now the last thing we have to do is somehow put together a system that's going to create an instance of user form, an instance of user show and pass both those things in as parent elements. And that's it, believe it or not. So a quick pause right here, and I'll see you in just a minute.
