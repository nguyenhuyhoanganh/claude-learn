# Options for Adapting Sync

In the last video, we started extracting class sync, but we very quickly ran into some issues. Our fetch method is trying to refer to this target, this set, and basically same thing for down here on save as well. So how are we going to somehow resolve these references to methods that don't exist on sync? Well, there's definitely many different ways to solve this. In this video, I'm going to show you three possible ways we could solve this issue. One way is not going to really work out so well. The other two are definitely serious contenders. So let's get to it. Okay, so here's the real issue. Our class user is going to have a sink property that's going to try to point at an instance of class sink. So at some time, class user is going to receive a method call or some kind of request that says, hey, user, save your data, class user is not going to be responsible for that. Instead, we are making use of composition and the way that we are implementing composition here is through delegation. So class user is going to delegate that save request to class sync. So on class sync we're going to eventually call save. And the expectation inside of method save is that we're going to take some data from the user and somehow save it to our outside API. So now that we understand those kind of ground rules, let's talk about these three different ways to solve the issue. Okay. So here's option number one. Option number one is going to be that any time user gets some kind of save or fetch request in this case, we'll talk about save for the most part, class user is going to pass all the current data properties over to save along with some ID to use as well. So in other words, the sync is sync class is going to get some function arguments of the ID that we're trying to save this record as and the actual data to save to that outside server. Now, this approach right here absolutely, positively would work. The big downside to it, however, is that class sync will now be configured to only work with class user because it's going to have some argument on here that says specifically look for some argument that satisfies the interface user props. So class sync is only going to work with class user. And that means we would have to have a separate class sink for every different type of data inside of our application. And it all is because we expect this data argument to be some object that has a very specific type. Now, of course, we could probably change that and we could probably just say, Oh yeah, data can be like anything or something. But remember, as much as possible, we try to avoid type any. So I'm sure we can probably think of a better solution here than that. Okay. So that's the big downside to option number one. This definitely would work. However, it would really isolate class sync and mean that we only works with this one class right here. So let's take a look at option number two. Now, option two is going to be a little bit complicated, but we can get through it. First off, I want to take a quick side note here and tell you about the terms serialize and de serialize. So what do those terms mean in general? Whenever you see the term serialize, we are generally talking about taking some data off an object and converting it into like some savable format that can be easily persisted. When we have an object inside of JavaScript or TypeScript, it's kind of hard to like take an object in memory and just somehow save it to the hard drive. So we almost always first convert that object into some other type of data, usually for our case with TypeScript and JavaScript into JSON. So when we talk about serializing something, we are talking about taking some properties off an object and converting it into JSON. Whenever we talk about D serializing an object, we're talking about taking a blob of information like some JSON or something like that. And we're going to somehow parse that JSON, take the properties out of it, and then put them on an object. So Serialize is talking about saving something. De Serialize is talking about taking something that was saved and putting it back into our application. So now that we know those two terms, let's go back to this option. So at this option, we could try setting up class sync. Let me remove that little typo right there. We could try setting up class sync to also define two interfaces, maybe an interface called serialize able and an interface called DC realizable. So the idea behind Serialize is that some other class or some other object would have to implement the serialize method. Any time serialize gets called that other class. So in this case our user would have to return some kind of object. Like we're not going to specifically say any here. We can at least say like, you have to return an object. So with this we would say that the input to save would be first an ID and the second argument would be some object that satisfies the realizable interface. So whenever we call save last sync is going to take in that second argument right there. It's going to call the serialize argument on it. We're assuming the serialize method on it, and it's going to expect to receive some object that's going to then send to that outside API. Now, the reason we're seeing just like some object right here is to intentionally leave this vague. So if we just say some object right here, that means it could be an object with, like, your name. That's a string and or an age. That's a number. So by saying just some object, it leaves the option open to try to re-use class sync with other types of classes. And we are not suffering from that same problem we had back over here where class sync was like totally locked into user props. Then any time we wanted to fetch some information, we could require that we pass in something called De Serialize or some class that satisfies that interface. And so with this interface, we require that other class to have a D serialized method that's going to take in some JSON data. And presumably it would then be up to that other class, in this case the user, to take in that JSON data, parse all that information somehow, and use it to restore all the properties on the relevant user. So this approach is pretty good. But the downside is once again that we've got the output from serialized right here at best inside the interface. All we can really say is this is going to be an object with some properties. We can't specifically say what properties are going to be on there, because if we do, all of a sudden we're going to lock in this interface or class sync to be only good for class user. And the same thing is true of disk realisable down here as well. So this is a good approach. It makes heavily heavy use of interfaces, but it's still not great because, well, we lose out on some type safety here. We are not fully describing the types of information that are flowing around our app. Okay. So that's option two. Even though I called it option number three, I reorganize these options at the last second. So now we're going to take a look at the final option. Option number three. Okay. So this one is probably going to be the most complicated by a large margin, but it's also probably debatably the best way to go. So in this case, we're going to turn class, sink into a generic class. Remember what a generic class is. We're essentially turning all the types inside this class definition, or at least some of them into kind of like being a function argument. So this means that whenever we make use of class sync, we're going to specify the type of T and then that type T is basically going to flow down and define all the different types inside of our class. So that means over here in class user, when we actually make use of sync, we're going to customize sync by specifying the type of T. So that means right here we would say, okay, wherever you see T inside of class sync, replace it with user props. And so that would allow us to describe very precisely the input arguments to save. Data of tea would be essentially replaced with user props like so. So this way is debatably probably the best way. But like I said, it's also kind of the most complicated. One thing I want to clarify about this approach is that if we created a second class meant to represent some data like let's say blogpost, we absolutely could continue to reuse class sync over there because when we define blogpost sync properly, we would say that sync was going to be a generic and we would insert the type of like blogpost props or something like that. And so we can customize class sync depending upon the needs of whatever class is making use of it. So again, I would say, yeah, this is probably the most appropriate solution, but we're definitely going to have to learn a lot more about generics along the way to really make this thing work. The other kind of interesting thing here is that it doesn't quite solve the scenario of fetching information terribly well. Well, I should say it does solve it, but it still puts some like demands back on whatever class is using class sync. So in this case, remember, the idea of fetch is that we're going to receive some information and then hopefully deliver it back to whatever class is making use of sync. So in this case, to get some information back from fetch over to our user fetch would have to probably return a promise that's going to eventually resolve with user props. That object that we just talking about now, I know that sounds really nasty, but we'll dive in really deep to that. I just want to mention right now just to say, yeah, that would be kind of a requirement here. That would have been a lot easier on this kind of interface with serialize able and de serialize able approach. Okay. So now that we've kind of taken a look at some of these options, like I said, we are going to go with option number three because it really is the most appropriate. But this is not without its own set of challenges. So let's take a quick pause right here, and we're going to start the implementation in the next video.
