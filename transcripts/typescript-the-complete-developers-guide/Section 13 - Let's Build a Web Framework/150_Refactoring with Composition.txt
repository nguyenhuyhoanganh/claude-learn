# Refactoring with Composition

All right, my friends, we've got a great implementation of our user class put together. And now in this video, we're going to start to, well, tear it apart. So I want to take our user class and refactor it into some pattern that tries to use composition to add in functionality to it. So the big issue with our user class right now is that it's got a ton of different functions that do very different things, which is kind of okay. Remember we had said that was kind of like the initial goal of our user right here. It had these three very big responsibilities. But I just want to try to refactor this thing using composition, and I essentially want to extract that behavior into a separate class that into a separate class and that into a separate class as well. So I want to have one class that's going to be responsible for storing data related to a user. I want to have another that's responsible for all that eventual logic. And then one more to handle the actual like persistence of data and whatnot. We're still going to tie all these different systems together inside the user class, but the actual functionality is going to be completely delegated to these outside objects. So to see an example of this, the easiest place to get started is going to be that eventing logic we put together. So the on method and the trigger method, nothing else inside of our user class is calling those two methods right now. So it should be really easy to extract them into some separate class and then wire them somehow back into our user. So let's get to it. All right. So I'm going to flip back over to my editor. Inside my model's directory, I'm going to make a new file called Eventing Dots. So the goal of this file right here is to implement a class that's going to be responsible for handling all the different events tied to a user. We don't have any events right now, but, you know, we've got the on and trigger methods. So inside of here, I'm going to export a class called eventing. And then going to go back over to user. I'm going to find the on method right here. I'm going to cut it. And paste it into eventing. I'm then going to go back over. I'm going to find Trigger. I'm going to cut it. And I'm going to paste it inside of you venting as well. And right away you'll notice we've got a lot of errors inside of here, so we got to fix a couple of things up. The first big one is that we've got a broken type reference right here of callback. So remember we had set up a type alias for that. We define the type alias inside of user dots. So I'm going to flip back over, fix up some white space. And then at the top of the file, I'll find the type alias that we had added right there. I'm going to cut that type alias. Flip back over to events and paste it at the top. Next up, we clearly have a ear on here around events as well. So we had to find events as a property on our user class. So now I want everything tied to handling events to be tied to last eventing. So I'm going to go back over to user dates. I'm going to find where we defined or declared that events property. I'm going to cut it out. And then paste that inside of eventing. As well. Let me close that sidebar just to make this a little bit easier to see. Okay. So believe it or not, well, that's pretty much it. So we've now extracted everything around handling events into a separate class. So I'm going to save this file and I'll go back over to user. So now here's the key. We want to make sure that whenever we create a user, we have the ability to essentially throw in some module or some class or some object to use for handling events. So let's take a quick pause right here. Now that we've extracted all that event logic, we're going to figure out how to wire it back in to our user. So quick pause and I'll see you in just a minute.
