# CollectionView Implementation

All right. Hopefully you had some success in building that collection view. Like I said, it was a very challenging little exercise. Nonetheless, I want you to had the opportunity to kind of try some of this stuff out for your own. All right. So let's get to it in this video. We're going to speed through the implementation of view. Now, just to be really clear, we are going to go through this very quickly because a lot of the topics we need to cover here and understand around building a collection view are not really super relevant towards the world of TypeScript, and that's what this course is all about. Instead, a lot of the stuff in this collection view is going to be about understanding just like plain HTML and how the browser works. So not super relevant towards the course. Nonetheless, I want to give you an actual solution here, just in case you tried this exercise and got stuck somewhere. Okay, so I'm going to get started right now in my views directory, I'm going to make a new file called Collection View. It's. So inside of here, we're going to declare and export an abstract class called Collection View. So I'll do an export abstract class. Election view. I'm going to immediately define the things constructor. Whenever I define or create an instance of collection view, I'm going to expect to pass in two arguments. The first is going to be a reference to some parent HTML element, which is going to be where we're going to render our list of views. So I'm going to define a public parent property that's going to be a type element. And then secondly, we need a collection of records, like a collection of users that we want to pass into this collection view. And I want to attempt to eventually render out one view for each model inside that collection. So my second argument will be public election. And it will be of type collection. Remember, we created that collection class way long ago inside of our models directory. Here's collection right here. So we need to make sure that we import that class into this file, because right now we just can't find that type. So at the top, I'm going to import. Collection. From up one directory models collection. Whoops. Forgot my curly braces. Now, even after we import that, I've still got an air around collection, and that's because we had defined collection as a generic class. I remember way long ago when we put this thing together, we said that this thing was going to take in two generic types. The first one was tea, and that was going to be the type of model that we were passing in. And then the second one was K, and that was going to be the interface that specified the different properties that Model T would have. So in our example, we would have passed in like the user model as T and user props as K like so. So whenever we try to reference in any way collection, we need to make sure that we specify these T and K types. And so that includes back over here inside of our new collection view. So we can't just like magically put in T and K right here and expect everything to work out. In addition, we can't just put in like user and user props because that means that our collection view would only work for user and user props. So instead we're going to turn CollectionView into a generic class as well. So whenever we reference or create an instance of a collection view, we're going to specify the type of model that this thing is going to try to render and the different properties that that model is going to have. So I'm going to turn CollectionView into a generic class, and then I'm just going to take those Types 10-K and pass them directly through to collection. Like so. Okay. So that looks good. Now, our constructor doesn't really need to do anything else, so I'm going to shorten the curly braces down to a single line like so. All right. So now the next thing I want to work on is an abstract method definition. Remember when we were looking at this diagram just a second ago, we had said that the collection view is going to be like kind of an overarching superclass that can be used to create any type of collection view. So it's going to have a method called render item that's going to take some instance of a model and a reference to some HTML element. It will then be up to a child class to actually implement that method. And again, the goal of render item is to take in one model, generate a view out of it, and render that view to this item parent element. So we need to define this abstract method. So back inside of CollectionView, I'll say abstract. Render item. This thing is going to receive a first argument of some model that we will refer to as being of type DT because remember, that's going to be the type of model we're trying to render inside of here. And then the second argument will be an item parent, which will be some HTML element. And this is going to be where we want to render this particular view. For this model to. Now I don't expect this render item to return anything, so I'll market with the return type of void like so. Okay. So now the last thing we really need to do inside of here is build a render method. Inside the render method, we're going to iterate over our collection. And for every model inside of that collection, we're going to call a render item. So we get a chance to build up a view for each single model and render it to the screen. So the render method honestly really won't be that bad. So I'll define render with the return type of void as well. The first thing I'm going to do inside of here is clear out. The parent element is remember, we might be re rendering our collection view and every time we re render it we want to clear out all the HTML that the parent element has. Otherwise, we're just going to be duplicating HTML over and over and over again. So I'll say this dot parent dot inner html is empty string. Next up, just like on our normal view, I'm going to create a template element. Like so. And now we're going to iterate through all the different models inside of our collection. And for every model inside there, we're going to call render item. And we're going to pass in a given model and some parent to render this view, too. So let's say for let model of this collection dot models because remember we want to iterate over every model that we fetched inside their. Okay. So now inside of here, we're going to build up some parent element, which is going to like create it from scratch and then we'll pass it into render item. So I'm going to first say wrapper element. Is document create element. Div so this right here wrapper element is going to be the parent element that we're going to pass into render item. Maybe a better name for it would be simply like item parent just to match that argument name right there. So we're going to pass item parent into render item. This thing is going to render a view directly into that element. So then inside of here, right after that, I'll say this render item. I'll pass in the muddle that we're currently iterating over and that item parent. Okay. So the result of this line right here, if render item is implemented correctly, we should create a view and to render some HTML into item parent. So now we've got like that item parent element right there, essentially a div that has some HTML inside of it. And now we need to append that HTML into our overall template element. Template element is going to hold the big list of all the different views HTML. And that's going to be what we eventually append to the DOM to represent the entire collection view. So right after that I'll do a template element content. Dot append item parent. So this right here is what is going to actually build up the list. So once we've iterated through all those different models, well, then take the template element and append it to the collection view's parent. So I'll do this parent append template element. Content like so. And that's pretty much it. So this right here was definitely the really challenging part that honestly, I don't really expect you to have gotten through if you did. Hats off to you. Nice job. Okay. So that's the entire implementation for CollectionView. So now to actually make use of this, we're going to subclass it by creating a class called User List. So the idea with User List is that we're going to create one view for every user that we fetch from our JSON endpoint. All we have to do inside of user list is define that abstract method. Render item. So inside my views directory, I'll make a new file called User Lists. Oop. Little typo there. You know, take the E off the end. So inside of here, I'm going to import the collection view that we just created. All then export a class. Of User list and this is going to extend collection view. Now, remember, any time we refer to CollectionView, we have to pass in a type four, T and K. T is going to be the type of model that we are trying to eventually render, and K is going to be the different properties that that model is going to have. So at the top, we need to make sure that we import our user class like the user model and user props as well. So I will import user and user props from up one directory models user. And then right here next to CollectionView, we can specify our generic types, which will be user and user props. As soon as I add that in, I'm going to get an air around user list. Let's just because we have not implemented our abstract method of render item and that's really all we have to do inside this class. So inside of here, I'll say render item. This thing is going to be called with some model that's an instance of a user and some parent. Actually, we called it what we actually call this thing back inside of CollectionView. We give it the name item parent. I'm going to use the same argument name, even though we don't actually have to. We can call this argument anything that we want. We spoke about this several times, but I'm just going to stay consistent and call it item parent and then this is going to be of type element. All right. So now inside of here, we're going to create a view we're going to pass in this model, and we're going to get that view to render to item parent. So now we need like a view, a standalone view to actually take in that model and render something with it. So in this case, I'm just going to reuse the user show class. So we already put together. So in other words, for every user we fetch from our JSON server, we're going to render out one user show class. So I'm going to import user show. From user show. And then inside of render item, we will create a new instance of user show. We're going to pass in the. Item parent as the first argument and then the model as the second, and then we'll call render on that instance. Remember, whenever we call render, that's going to cause user sho to call its render method. Well, obviously that's what happens here because we're calling render. Let me rephrase that. Let's just take a look at render. So remember, whenever we call Render, we're going to create our template, right? They're going to build up that template and then we're going to eventually append that template to the parent element of the view, which in this case is the item parent. Okay. So that's pretty much it for user list. It's not. The last thing we have to do is actually build up a collection and pass it into user list. So to actually test this stuff out, we're going to flip back over to indexes and write the code to actually glue all this stuff together. So back inside of indexes, I'm just going to delete everything that's inside of here for right now. So we can see what we have to do from scratch. So at the top, I'm going to import the user list, which is our collection view. I'm going to import my collection. From Models collection, and I'm going to import user props. From models user. So remember how we make use of collection right here. Oops. Little type right there. Remember how we make use of collection? Any time we try to make use of collection, we have to pass in some types T and K. And then we're going to call fetch. Fetch is going to reach out to the root URL, attempt to fetch some data. It's going to turn all that JSON data into an individual model and add each one to the model's array. And then when that entire process is complete, it's going to trigger a change event. So we now need to create a new collection. When we do so, we're going to fetch some data from our JSON server. And then once we've seen this trigger event or this change event triggered, we can then go through the process of trying to create an actual collection view and render out this big list of models. So back inside of indexed I'm going to create my collection that I'll call a user's. I'm going to pass in the URL to get our list of users from which will be HTTP, colon localhost 3000 users. And then remember that second argument is the serialize function where we're going to take in some amount of JSON data and then return some kind of model to use. So I'm going to pass in an arrow function. This thing is going to be called with some amount of JSON data. And that's going to be of type user props because remember, that is the information or the type of information that we're getting back from JSON server. So I'm going to annotate it as user props like. So I'm going to just new line that argument so you can see this more easily. There we go. That's better. So now inside of here, we're going to return. User build user. We're going to pass in the JSON for specifying this new user model. Okay. So that's going to build up the initial definition of our collection. So now we're going to attempt to fetch all the different users from our JSON server endpoint. So I'll call users fetch. Once we call fetch and successfully get back some JSON and turn it all into that list of models, we're then going to trigger that change event. So right above this, I'm going to put on an event handler on the collection and watch for that change event. So whenever the collection is changed, we're then going to attempt to create our user list, which is the collection view and render that thing to the DOM. So to render this thing to the DOM, we first need to get some root element to render the collection list to remember inside of our index HTML file, we had created that div with ID root, so we need to get a reference to that element right there and pass it in as the parent element to our collection view. So I'll say construit is document dot get element by ID with root. And then remember Root right now is either each HTML element or null. So we have to make sure that we actually got a result back. So we'll say if root then create a new user list. That's our CollectionView. The first argument is going to be the parent element that we want to render this thing to, which will be root. And then the second argument will be our collection. Which is going to be users. And then after we create that thing, we'll call the render method, which should trigger the entire rendering sequence. All right. So that's pretty much it. I know that's a lot of nasty code, but like I said, this is pretty much how we used to like build web applications back in the day before the time of React. It really was a lot of what you see exactly right here. So I'm going to save this and I'm going to flip back over to the browser and we'll do a quick test. So once over here are going to see a bunch of different copies of user detail up here. So that is our collection view in operation. If you look very carefully at all the details for these different users, they should be matching up to your DB JSON file. So if you open up DB JSON, you'll notice that I've got a name of user number one with an age of zero and then Bill with ten and Jill with 20. And so if I flip back over user number one, Bill and Jill, age of ten, age of 20. So that's pretty much it. All right. I know this was a nasty little exercise, but hey, hopefully if you actually got it together, maybe learn something during the process. Okay. So let's take a quick pause right here and we're going to do a big wrap up in the next video.
