# Implementing a Users Collection

In this video, we're going to start working on our class user collection. So I'm going to flip back over to my editor. I'm going to find my models directory and I'll make a new file called simply Collection Dots. Now, the reason I'm going to call this collection is, remember, we're going to pretend that we are building it specifically for users, and then we're going to eventually refactor it to be fully reusable in nature. So we're going to really call it collection all the way through. But right now we're just pretending that it's going to be only for users. So inside of collection, I'm going to create an export, a class called Collection. Now I know that this thing is going to definitely have to have some list of all the different users that we've fetched. So I'm going to give it a property called Models. I'm going to say that this is of type array of user and I'm going to start it off as an empty array like so. Oops. That should be Nicole's. There we go. Now, right away, we get an error around user. That's just because we have not imported it. So I'm going to import user at the top like so. Now the next thing I want to do is wire up that events module inside of here. The reason we're going to wire that up is that we're going to use that same kind of venting idea to kind of broadcast a signal out to the rest of our application any time that we have successfully fetched some data. So I'm going to import in events. Or eventing, which is what we called it from eventing. And rather than trying to set up some fancy constructor and receive eventing on the fly, we're just going to hard code eventing in because remember at the end of the day, very unlikely that we're ever going to want to swap out eventing. So I'm going to give this thing an events property. It's going to be a type of venting and it's going to be initialized right there in line like so. Now in the case of eventing, we probably just want to set up some pass through methods with this thing and make sure that other classes or other locations inside of our code can directly call on and trigger on collection. So I'm going to set up a Gitter inside of here called on. Whenever someone calls this we will return this dot events on. Now. Quick question here. Why are we not using the shortened syntax as we just saw inside of user or excuse me, inside of muddle? Remember, we just did this refactor right here. Well, remember when we did that refactor, we had a long discussion talking about how if we are initializing a property in this fashion, this line of code. Is going to run after this kind of deal right here. And so we cannot use the shortened syntax if we are initializing properties in line like so. So that's why we are setting up a gitter to expose on to the outside world. So we can do the same thing for Trigger. And I can return this dot events trigger. And that's pretty much it. That looks good. All right. So let's say take a quick pause right here. When we come back in the next video, we're going to start to set up a fetch function inside of you as well. So I'll see you in just a minute.
