# Saving Data From a View

Now that we've separated user form from our view class, we're going to first do a little bit more customization of user form. Specifically, I just want to make sure that user form only shows what we show here in the mockup. After we do that, we'll then create user show by using that new view class that we just extracted. So let's get to it. All right. So I'm going to flip back over to user form and I'm going to find the template method. So inside of here we've got an H, one that we probably shouldn't have in here because remember our mock up showed that H one being displayed by user show. So I'm going to delete the H one. I'm going to delete the user name and the user age. And now the next thing I'm going to do, I'm going to make sure that the input has some placeholder text in there that shows the user's current name. So to put a placeholder there, I'll find the input element. I'm going to give it a placeholder property. I'm going to put in a set of double quotes like so, and then we're going to use some string interpolation here. So I'm going to put in my dollar sign curly braces, and I'm going to print out the user's current name inside. So I'll say this model, get a name like so. So if I now save this, I can flip back over. And I should see name because my default name is capital name. If I change it like so, I'll then see that become the placeholder text inside there as well. So that looks good. So now the last button we want to put inside of here is the save button at the very bottom. And as you might guess, that should persist this model to our server. So this one's going to be actually pretty straightforward. All we have to do is call save on our model. That's it. In order to test it right now. All we really have to do is make sure that we see a network request occur inside of our console. So I'll pull open my network request tab right now. All right. So now to put in a save button, I'm going to first add in a button to my template. I'll give it a class so I can reference it from my event map. As a class, I'll give it. How about just save dash model? And we'll give it the text save user like so. So now we can set up a new event inside of our event map using the class name save dash model. All right, here's my events map. I'll add in a new click on the dot. Remember, DOT indicates class and then it's save dash model. So any time that we detect this event, I will run this dot on save. Click. And then I'll define that as a helper method inside of my class. So on save click. And remember, we're going to set up all of our event handlers as aero functions so we don't have to worry about the value of this inside them. I don't expect this function to return anything. So a market is void. And then finally inside of here, we'll do this dot model. Dot save. And that's pretty much it. Okay. So I'm going to save this file. I'm going to flip back over. I can see the new button right there and I'll click on Save User and there is my request right there. Of course this request comes up as a post request because we are currently creating this user. They don't start off with an ID and remember, if a user doesn't have an idea or a model in general doesn't have an ID, that means that it has not been persisted to the server before. And so we should try to create this record from scratch on the server by using a post request. So it looks like it was assigned an ID of seven. I can flip back over to my DB json file. And inside there I should see an ID with seven. Okay. So that looks pretty great. So we've now got our user form 100% complete. Believe it or not, after all this time as all the correct HTML inside of it. So now we're going to take one more quick pause. When we come back, the next video, we're going to start to wire up user show and user edit and then figure out how to take care of all this nesting stuff. So quick pause and I'll see you in just a minute.
