# Re-Integrating Eventing

Now that we have extracted our class eventing, we need to somehow wire it back into our user file or specifically the user class. So let me first give you a quick overview of what this class is going to eventually look like. So our end goal here through using composition is to have our class user and rather than having a ton of methods inside of it, we want to have a couple of different properties that class user can use to delegate out some very important operations. So we just extracted out some like eventing class, and now we want to eventually assign it back to maybe some property on class user called events. And then hopefully we will eventually have some attributes, property and sync property to take care of handling properties tied to the user and take care of all that server saving stuff and whatnot as well. So we're not only talking about like integrating back in this eventing class, we eventually need to integrate back in attributes and something to handle sync as well. Okay. So with all this in mind, I now want to give you a quick reminder of how we handle this on a previous project. So I'm going to pull a code editor on the screen really quickly. All right. So this is from that CSV project we worked on. Remember, this is our match reader file. The match reader made use of the CSV file reader. So we made use of composition very heavily on this project to get some amount of code re-use. Remember CSV file reader had all the logic of how to open up a CSV file and extract some plain text out of it and then do some very basic parsing on that text. So our whole idea here was that when we created an instance of Match Reader, we would pass into the constructor some property or some object that satisfied the data reader interface. So whenever we created a match reader, we could pass in a CSV file reader or maybe like a network API reader instead. So we can very easily customize where match reader gets information from just by passing in a different argument to the constructor. Now to save a little bit of typing and avoid having to say basically that exact line right there. Every time we wanted to create a match reader, we had created a class method. Remember the class method we called from CSV and that essentially gave us back a pre-configured copy of Match Reader that already had a CSV file reader loaded into it. So now when we start talking about assigning that eventing class back into user, we essentially have to ask the same kind of question how are we going to really get that event in class into the user? Well, I think that there are a couple different options. This is not quite as easy as a decision back on Match Reader for one really important reason. All right. So I'm going to go back over to my real code editor. Here we go. So the reason that it's slightly different this time around is that we already have a constructor and the constructor is going to take in some user information or some data about this user. So if we're talking about providing some dependency to the class through the constructor, well, we're going to kind of have to work it in with some existing code. So to take a couple of look or take a look at a couple of ways of handling this, we're going to look at three different options in this video. So I'm going to very quickly write out three different ways of handling this, and we'll just kind of do some analysis on each one and decide which one might be the best for us. So I've copy pasted the code inside of user tests into these three separate files and I'm going to very quickly go through these three different options. Now, don't write out any of the code I'm about to write. These are all temporary files. I'm going to delete all of them and then we're going to re implement whatever we do back inside a user. Together in the next video. So the first option to wire venting back into our user would be to still accept these dependencies as like just a second argument to the constructor. So the first argument is always going to be our user data or kind of like the initial data for our user. And we're just going to say, okay, the second argument is going to be dedicated to taking in some dependencies to make our user work correctly. So as a second argument, we could say on here something like. We new line this just to make it a little bit easier to read. So inside of here, I would say, okay, now we've got a private property called Events and this is going to be an instance of the event in class like so. So now what that means is every time we want to create a user, I'm going to show you an example at the very bottom of the file. I would have to do something like new user is going to be some starting attributes, you know, maybe an ID or whatever. And then the second argument would be new venting like so. And this right here, it just kind of feels nasty. You know what? Every single time we create a user, we have to create an event in class as well. And that means that, like when we start to refactor out some different pieces of functionality, we're going to have to add those on as different arguments. So I don't know, maybe this is maybe just not the best approach just yet, but I just want to mention that this could be an approach. So now the second way of doing this would be to only accept dependencies into the constructor and define a static class method to pre configure user and assign properties afterwards. So this would be way more similar to the match reader example we just looked at. So we're going to say that the constructor is always dedicated 100% to receiving configuration or kind of like sub modules for the class. So we're always going to make our constructors inside of our project 100% about receiving these kind of composable subclasses. Now, I don't want to use the term subclass right there because we're not talking about inheritance. I just mean to say we're going to receive in these other classes to make our class work correctly into the constructor. And the constructor will never really be about taking in some initial data or something like that. And so this just kind of clarifies the purpose of our constructor a little bit. So let's take a look at what that would look like. Okay. So inside of my constructor, I would say the first argument is always going to be. Something that I'm going to use or maybe like an events property and that will be an instance of eventing. So then I can define a static class method to give me like a preconfigured copy. And I'll say maybe how about like from data or something like that? So then we'll take in some properties for the user as an argument to this class method. So I'll call this about data and that needs to satisfy the user interface. So then inside of here, I'm going to return a new. Excuse me. I don't want to return it to see that I'm going to create a new user. I'm going to say this is going to be a new user and I'm going to pass in a new copy of events like so. And then after I create the user, I'm then going to take that data argument. I'm going to take all the properties in there and set them on the user. So I'll say user set data. And then finally I will return. User. Now to really make this work, I need to add in a annotation right there to say it's going to return an instance of user. And then also I need to make sure that when I set that data, we've now lost the kind of declaration of the data property property that we previously had inside the constructor. So I would have to separately define it or declare it as a property on the class right here. So I'd probably say like private data. Is going to be some object that satisfies user props like so. Okay. So that would be this kind of second approach. So I think that this approach is pretty good. But, you know, at the same time, having to do this user set data stuff right here feels kind of nasty in this case, initializing a user with the correct set of properties that we get off of data. Well, it's only like one line of code, but I think you can kind of imagine that we might have some more complicated classes in the future that require many lines of initialization. And so we would potentially have to like add in a ton of configuration inside the static class method. And if we ever wanted to have some other configuration option, like maybe instead of using the eventing class, maybe we had like a different venting module called like really cool events. Or if we wanted to use that we would then have to duplicate. Our static class method called this one like super events or whatever it is. And then we would have possibly many lines of initialization code right here as well, and those would be easily duplicated between both the classes. So I don't know, this still doesn't really feel that great as well, but maybe it's at least an improvement over what we just spoke about this option down here as well, where we had the first argument of data and the second as the actual dependencies to our class. So let's now talk about the third option here. This one might actually make a lot of sense in this case. So in this scenario, we're going to say that the constructor is still all about receiving user properties. So doing some like pre configuration of a user. And then here's the important part on this. Option number three, we're going to say that we're just going to hardcoded our dependencies into this class as properties. So inside of my actual class user, I'm going to say that this thing is going to have an events property. That's going to be of type eventing. And I'm just going to initialize it right here on the same line. I'll say new eventing. So what does that do? Well, that means that if we now go down to the bottom of the file, so I'll go all the way down here if I create a user. Like so and I pass in some initial data for it. Maybe in this case just an empty object. This thing is going to have a preconfigured events module inside of it. So events is already here. I don't have to do any further configuration. Now the downside to this approach is that we really lose out on some of the benefits. Of composition. Essentially right now, every time we create a user, we're going to have the same exact class serving as our events sub module inside of here. So back in our match reader that we were just discussing, remember we said that one of the big strengths of using composition is that we can swap out what source of information we are using on the fly. So back on this project, having the ability to swap out that sub module of sorts really quickly, it made a lot of sense. But on this project, well, you know what, honestly, our eventing class like this implementation right here is basically how all eventing modules or eventually libraries are put together. And I kind of have a really hard time imagining that we would ever want to replace our eventing class with some other alternate implementation. So on this project in particular, and most especially with this particular class, it kind of doesn't really feel that risky to just say, Yeah, it's always going to be classy venting because what else would it be like? What else are we going to put in here? What other copy of some eventing library are we going to use? Is there really such a big difference between different implementations of eventing libraries that we would ever want to swap this out? I don't know. I kind of have a hard time believing that. Now, even if we hard to code this thing, we still retain some of the benefits of composition. We can very easily reuse that event in class, in other classes inside of our project. We can very easily test our eventing library in isolation because all this code is just right here inside of one file and it's a self contained class. So we still get a lot of the benefits of composition. We just can't easily swap out eventing on class user, that's all. So on this project, as you might guess, we're going to go with option three. Eventually, we are going to possibly have to swap some things out and you'll see why in a little bit. But just because I really don't see us having to replace a lot of these different modules are going to put together, we're just going to say that we're going to hardcoded them in here. Now, if we wanted to, we could always come back in and say like, okay, project wide, we are no longer using eventing, we're going to use this new like super. Events library that we are just installing into our project. That is always an option. We could like replace this thing on a global level. So every user has a different copy of events or some new eventing library. That's still an option. But again, at the end of the day, I just don't really see that happening. All right. So that's pretty much it. I just want to give you a couple of different options of how we can integrate these extracted classes back into a central class. So we had the option number one, just continue to accept these things as a constructor argument. Option number two, only accept the sub module of sorts as a constructor argument and then do all the other configuration side of a class method. And then finally, what probably makes a lot of sense for us, just say, yeah, you know what, it's hardcoded. So what? So I think this way will work out generally pretty well, although, who knows, we might eventually have to change it, maybe even sooner than you would think. So let's take a quick pause right here. Continue in the next video and we'll continue extracting logic out of our user class. And oh yeah, we need to actually wire up eventing because we were just looking at some examples. All right. So quick pause and I'll see you in just a minute.
