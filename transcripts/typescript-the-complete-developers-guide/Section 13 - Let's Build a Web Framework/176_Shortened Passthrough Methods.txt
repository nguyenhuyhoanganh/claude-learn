# Shortened Passthrough Methods

We've now wrapped up our model class. But before we move on, one last little change I want to make in here. So you might recall that I mentioned a while ago that we had set up all these jitters inside of our model. At the time it was actually the user. And I told you that we could actually use a even shorter syntax than what you see right here to kind of relay or pass through all these different methods. So here's the shortened syntax in place of saying that get it right there. I'm going to instead say on equals this events on like so. So these two are effectively equivalent for what we are doing right here. We're going to add on a property to our class of on and it's going to be a direct reference to the on function from this DOT events. So I can now remove that jitter. Now I can repeat that same process for the other two getters as well. So I can say trigger is this events trigger. And remove the guitar. And I could do a get and say that is this. Attributes get like so. Okay. So that's much shorter than what we had before. And you might be kind of curious, why did we not just do this initially? Well, it really comes down to kind of an order of operations thing. I'm going to flip over to a TypeScript playground really quickly. We're going to write out some code to show you exactly why we could only do this now and why we could not quite do it with the structure of code that we had previously. Okay. So back inside of a typescript playground. I'm going to create a class called Engine. And my class engine is going to have a single start method and anytime I call that I'll do a console log of started like so I'm gonna zoom in here just you can see that code a little bit more quickly. Unfortunately this this editor here doesn't really scale too well, but I think we can still see what's going on. Okay. So now to make use of this class engine, I'm going to create a second class called Car, and this is going to have a reference to an instance of an engine. So I'm going to define that as an instance variable. They'll say engine is going to be of type. Whoops, engine. Now here's the key. I'm going to set up a constructor method, and inside the constructor, I'm going to initialize the engine instance variable. Essentially, I'm going to assign an instance of engine to it. So I'll say this engine is a new engine like so. All right, so let's now try doing that kind of pass through syntax that we just saw inside of our editor. So I want to add start as a method to car so I will write out right underneath the constructor start is this engine start like so. Okay. So let's now take a look at the code that was generated for us here on the right hand side. I'm going to zoom out just we can see that. All right. There we go. So our class car was turned into a function. Inside of that function is a constructor function. So that's the actual constructor that is coming out of class car. Now I want you to take notice of the two instance variable assignments inside of here. The first thing to notice is that the syntax right here where we just say star equals blah, blah, blah. That essentially gets translated into a direct line inside of our constructor that you see right there. Now, the issue here is the order of these two lines. Notice the order of them. So we first have this engine start is going to be assigned to this start. So we are essentially trying to use the this engine property here before it has even been assigned. So any code that we add in the constructor is going to be executed after any assignments like this one you see right here. The problem is that we are trying to access this engine before it has been initialized. And so if we actually ran this code right here and we tried to call the start method, we would probably see an error or actually, heck, even this over here, when we first created an instance of CCAA, we would probably see something that said cannot read properly. Start of undefined is at this point this engine is undefined. So that's the problem. We can't use this syntax. We're doing method pass through if we are assigning any of these different properties inside the constructor. Now if we are assigning them using that modifier shortened syntax inside the constructor arguments, we can do this perfectly fine. These different initialization, as you see right here, take place before anything else inside the constructor. So let's now try refactoring this to assign an initialize engine inside the constructor argument list instead. I'm going to delete that argument or excuse me, the body of the constructor. I'm going to delete the initialization or the declaration right there. And then in the constructor argument list, I'll say public engine. Is engine like so. So now in this case, we're going to take that engine argument, assign it to the engine, and then we're only going to access this engine after that. So previously when we first wrote out our user, we had some code inside of our constructor and that's why we were not able to use this shortened syntax. But with the form we have right now. Back inside of user. With the form that we currently have where we're doing all of the assignment of user inside the constructor of our model constructor or the argument list right there. It means that all these things are going to get assigned before we actually run these lines of code right here. And so events and attributes will already be defined. So that's pretty much it. Now, this is definitely some really brittle code. It's brittle because we ever decide to move these initialization and and assignments into the constructor itself. Well, some stuff here is going to break. Definitely a downside to this approach. Nonetheless, I want to show you the shortened syntax so we could possibly use in some scenarios. All right. So let's take a quick pause right here and continue with our framework in the next video.
