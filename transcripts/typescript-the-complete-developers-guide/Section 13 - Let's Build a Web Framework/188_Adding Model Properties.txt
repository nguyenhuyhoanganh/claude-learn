# Adding Model Properties

We now have the ability to handle events inside of our class or our view class. Now, the next thing we need to work on is making sure that we can print up information from a model inside of our template. So our user form doesn't really have a lot of direct references to a user model. It does print out the user's current name. The user show definitely has a lot more information that needs to be printed out from a user model. Nonetheless, I think we should try to implement this behavior or this functionality inside of user form and then eventually extract it so we can reuse it inside of user show as well. So the goal here is really simple, really straightforward. We want to make sure that whenever we create an instance of a view, we pass in a model and then we want to somehow print out properties from that model inside of our template. So let's give this a shot. To get started, I'm going to flip back over to my user form class. At the top, I'll find the constructor. So I'm going to say that whenever I create an instance of a user form, I want to pass in a second argument. This will be a public property. Called model. And I'm going to say that it's going to be an instance of a user. So I'll write out user like so. Now we get an air around that just because we don't have that type of available inside this file. So to fix that, we can import. User, and then we have to write out the relative path right here. So we are currently inside the views directory. We have to write out dot, dot slash to indicate that we want to go back up to Z and then models to go into the models directory. And then user. Okay. So that's pretty much it for user form. We now have access or are expecting to get access to a user model whenever we create an instance of this user form. So now we can go back into our indexed file and make sure that we create an instance of a user and pass it into our form when it gets created. So I'll go back over to Index. We now have an error around the constructor. That's just because we are only passing in one argument instead of the two that are now expected. So we can import. User. From models user at the top and then create an instance of a user. Remember to create an instance of a user. We said that we're now going to use that static class property called build user because build user already has all those kind of predefined or wired up sub modules inside of it. So when we call a build user, we just have to pass in the starting properties for a user. So I'll give this starting user a name of name about all capitals and an age of 20 like so. So now I can pass in that user as the second argument to my user form constructor. So I'll go all the way over here to the right hand side and pass them the user as the second argument like. So I'm going to zoom out just you can see that whole line. All right, so that looks good. So now the last thing you have to do is go back over to user form and make sure that we somehow reference the different properties that a user has and attempt to print them up inside of our template. So I'm going to close the indexed file, go back over to user form. I'm going to go down to my template. Here it is right here. And how about right underneath the H one? I'll just try to print out a div inside of here, and I want to print out the user's name. Remember that this template or this string that we're currently operating on is a template string because we used back two characters so we could put down a dollar sign. Really braces. And inside there reference this model and to get a property off of our model, remember what we do. We call Doggett and we pass in the string to indicate the property that we want in this case name. So now we can repeat that same process to also print out the user's age. So I'm going to duplicate that line down, change it to user age, and change the git to age as well. Okay. So it looks pretty good. Let's save this. I'm going to flip back over. And I can now see my user's name and their age as well. Perfect. All right. So let's take a quick pause right here. When we come back. The next video, we're going to start to work on making sure that we can actually update properties tied to a user from the HTML on the page. So a quick pause and I'll see you in just a minute.
