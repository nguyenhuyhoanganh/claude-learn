# Optional Properties

In the last video we added in a generic constraint to class sync. And now when we specify sync right here for our sync property, then eventually create an instance of it. We pass in user props for that generic constraint and now we're seeing an error message around it. And the error message says same thing in both cases. Essentially something about how user props does not satisfy the constraint has ID. And then if you read really closely, it says property ID is optional in type user props are required in type has ID and that's pretty much the whole issue here. Right now we are passing in interface user props or our type constraint, but inside a user props ID is marked as being optional. Over inside of Saints has ID says that whatever type comes in for RT has to have a required ID property. That is a number and that's the source of the error that we are now seeing. Either we have optional over here and required over here. So in one case, TypeScript is saying, hey, you know what, if you're not going to have this property, I don't want to deal with you. So that's pretty much it. So in order to fix this, all we really have to do is mark the ID property inside of our interface as optional as well. Okay. So that's pretty much the fix. And as soon as we make that change, the air goes away. Back over here. But this idea around optional properties kind of brings up a more interesting topic. You might recall that down inside of our save function right here, we had a reference to data ID and we made this whole big idea around IDs, sometimes being defined and sometimes not being defined. And depending upon whether or not ID was defined, we wanted to have some very significantly different behavior. So now the thing that's kind of awkward here, if you mouse over ID, you might notice that it has a type annotation of being number. So what you're seeing right here is the default behavior for optional properties in TypeScript. And we've been using the default behavior of TypeScript throughout this entire application. But in some other locations or other projects we've worked on inside this course, we've had TypeScript kind of inadvertently set up in a slightly different mode. So let me show you what I'm talking about. I'm going to flip back over to my terminal. And inside my project directory I'm going to generate a TZ config file using TSC dash dash init. Like so remember what that does, that creates a config file. And inside that file is a lot of different configuration where the typescript compiler. So I'm going to flip back over and very quickly open up that file without worrying about anything else that just occurred. So all we just did was generate this file. Nothing else whatsoever. Now, I'm going to scroll down this file a little bit, and I want to point out one very important section inside of here. And that section is strict type checking options. And one setting inside of here says strict. True. Now, if you read the description or comment for that setting right here, it says Enable all strict type checking options. So by just having this config file existing like the default config file inside of our project, we are changing the behavior of the TypeScript compiler by default. Strict right here normally is false like so. So by just generating this file, we have changed how the TypeScript compiler behaves and now all of a sudden we are doing a bunch of additional checks on our code base. One very important check that was just enabled by having this file inside of here is a rule called strict null checks. So what strict nul checks does is it takes a lot of scenarios where types are kind of gives us the benefit of the doubt and instead requires a lot more rigor in how we treat different values and different properties inside of our app. So to give you a really good idea of what that rule right there truly impacts, I'm going to flip back over to my Saints file and now I'm going to once again mouse over my ID property. And remember, at this point in time, the only thing we have changed is to generate that file that has that one setting enabled. And so when I now mouse over that property, you'll notice that the type of ID is now very, very different. Now it is either number or undefined. And this is a way more better way, in my opinion, of reflecting optional properties. So again, type scripts, default behavior. Even if we mark a property as being undefined or should be optional, it kind of gives us the benefit of the doubt. It says, okay, you know, this is an optional property, but we're just going to assume that it's always going to be defined. And that's what you saw just a moment ago when we mouse it over ID and we had an annotation of number. So by just turning on that one setting, which we did, by just generating the config file, we have now gone into this very different mode where now optional properties are going to be either a number or undefined. And so now the if statement that we have right here makes a lot more sense. The if statement where we check to see if it exists is essentially functioning as a type guard type script, understands what we are doing with this if statement we are checking to see if that variable is defined. And so inside of the statement, if we write out ID and now mouse over it, it will have a type of only number because TypeScript understands that in this scenario there's no way we could enter this branch of the if statement if ID is undefined. Likewise if I write out ID down here in the LS case, TypeScript is going to say well okay, maybe not quite so smart in the LS case this time around. I guess it only works correctly on the if statement, but now you can see that we kind of lost that type guard. Okay. So the big takeaway here, what I really wanted to convey is that when we use optional properties, we're going to see that our code behaves very differently depending upon that one single setting inside of our config file. So by not having this file inside of our project before, well, it made a TypeScript compiler treat our code very differently. And by just generating it now, all of a sudden we're going to have some very different experiences around types inside of our app. Okay. So now I am going to save this file and believe it or not, I'm actually going to delete the config file. So right now, with the version of the application we're putting together, I really don't want to have strict mode turned on just yet because it's going to show us a lot of other errors that are pretty unimportant as far as the application is stands as it is right now. So I'm going to now delete the config file. As soon as I delete it. I can now wait a second or two, and if I now mouse over it again, you'll see that we've now fallen back to the default behavior where number or schema ID is now considered to be a number and nothing else. All right. So now, just so you know, later on inside this application, like this app we are currently building, we are going to regenerate that config file and handle all these different kind of errors that are going to pop up in our code once we enable that strict mode. But right now, like I said, we're not going to worry about that strict setting just yet. All right. So quick pause right here and we'll continue in the next video.
