# Adding Fetch Functionality

In this video, we're going to start working on an implementation of our fetch and save methods. So let's get to it. I'm going to flip back over to my code editor. The first thing we need to do inside of our interface user props is that in a new optional property of ID, and that's going to be a number. Remember, this ID is going to be tied to every user. If a user has an ID, that means that they have a saved backend representation. If they don't have an ID that indicates that this model is brand new and there's probably not some backend representation for them, that ID is also going to customize the request that we make using Axios to retrieve or save some information. Okay. So now that we've got the ID inside there, I'm going to go down to the very bottom of my user class and we're going to first define our fetch method. Now for fetch. I don't think that we need to really add in any arguments to this thing. And as far as the return type goes, well, I'm not sure that we really need to return anything from this either. So I'm going to say a return type of void. You might notice that back on the diagram we were just looking at, I said that we were going to return a promise. Well, it turns out that the return value from fetch and save is a little bit nuanced. So we're going to first just start off returning nothing at all, and you're going to see that start to change over time, but we'll eventually get there. Okay. So now inside of fetch, we need to make a request over to our JSON server. And we need to make sure that we attempt to make a request that has the ID of our user tied to it. So in other words, we need to do this one right here. We're trying to make a request to get some information about the current user with that given ID, and we need to make that request to users and then the ID. Okay. So to do so, I'm going to first go back to the top of the file. And import Axios from Axios. Well, then go back down to the bottom and side a fetch and I'm going to say Axios target and we need to make a request to that local host slash user's slash one. So I'm going to use a string template here because we do have to inject our ID for the current user. So I'm going to place a set of back ticks and I'll say http, colon, slash, slash, local host, 3000 and then a slash, and then we're going to put in users and then we will inject our current users. ID So I'm going to put in the dollar sign curly braces and then this dot git ID. And I'll zoom out so you can see that whole line. Now, this requesting right here is really nasty, so please triple check this. If we start to test out this code and it just doesn't work, I can just about guarantee you, you probably made a typo inside this string right here. And one thing I want to point out. Notice how we are using that git syntax right there. We access properties on our user by using that git method. Okay. So now writing that request line right there is going to return a promise. So we're going to chain on a dot then statement so we get some notice of when we get a response back from our server. The response we get back, I will receive as an argument called response. This thing is going to be of type Axios response. And from this function, I'm not going to return anything, so I'm going to entertain it as void. Now you'll notice on here we've got an error around Axios response. So I just know that type off the top of my head. Whenever we chain on the dot, then statement, the argument that we get back here or this first argument is of type Axios response. So this is an object coming from the Axios library that contains the response information that we got back from that endpoint. So I'm going to go back up to the top of the file and I'm going make sure that I import this type from Axios as well. So at the top of the file, in addition to importing Axios into this file, I'm going to also from Axios import the type Axios promise like so. Okay. So now if we go back down. Oops, sorry, not Axios promised Axios response. All right, better. There we go. Axios response. Okay. So now if we go back down, that ear is now gone. So now inside of response is the JSON data that we got back from our API and that should contain the ID and the name and the age of the user that we just fetched. So inside of the DOT then statement, I'm going to take that information and set it on our user and I can do that by writing out this set. And we're going to take response data like so. Okay. So response data is the actual JSON data we are getting back from our server and we're going to take that big object that represents that user. It's going to essentially be exactly this thing right here, and we're going to just set it on our user model and update all of its current properties. And that should be it. Okay. So I'm going to save this right here. Now, we don't quite have the ability to test this just yet too easily. So let's take a quick pause right here. We're going to add in just a little bit more code so that we can test this and then we'll actually do a test. So quick pause and I'll see you in just a minute.
