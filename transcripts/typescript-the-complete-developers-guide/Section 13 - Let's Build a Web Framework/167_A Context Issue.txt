# A Context Issue

Now that we've re-implemented the get on and trigger methods through those getters, let's do a quick test to make sure they are working as expected. All right. So once again, I'm going to go back over to Indexed and then delete the user statement right there. We're going to start out with a simple test to the Git method. Now we're going to find a little error around this, and the error is going to be pretty perplexing. Nonetheless, let's give it a shot and I'll tell you what's going on. Okay. So I'll do a user dot get and I'm going to try to retrieve the user's name and then let's do a console log of it like so. Okay. So notice how when I write this out, I don't see any errors or anything like that, so I'm not going to save this. I'll flip back over to my browser. And sure enough I get a really nasty error message. It says cannot read property name of undefined. Well, gee whiz, this seems like it's pretty nasty. So what's going on here? Well, let me give you a quick reminder of how this works in JavaScript in general. Once I tell you this, you'll probably get a really good idea of why we're seeing this error. Okay, so to get started, I'm going to first comment out that console.log we just put together and we're going to do another quick code snippet right here. I'm going to put a section header that says reminder on how this works in JavaScript. Okay. So now down here, I'm going to create a object literal called colors. It's going to have a colour property of red and then I'll give it a print color method like so inside a print color, I'm going to do a console log of this color. So now, in theory, any time I call a print color, we should see red console locked. Let's now test that out. Okay. So I'll do colors, dot, print color. Like. So I'll save the file. I'll flip back over and I see Red up here. Totally working as expected. No mystery here whatsoever. Now, here's the rule of thumb to use. Any time that we were thinking about this inside of JavaScript, the rule of thumb is that this is going to be equal to whatever is to the left of our function call. That sounds like a weird rule, but let's try actually testing it out here. In this case, what is left to print color or excuse me, what is to the left of print color? Well, it's the colors object. So that means that inside of print color, everywhere that we see the word this, we could imagine that it gets replaced with colors. So we could imagine that in this case, instead of this right there, it's color, stock color. And so this code right here would still work perfectly. In fact, I could save it and I should still see red at my console. So that's the rule of thumb. We always think what is still left of our method call. Well, that's going to be what this is equal to. So now to really show you that in action, I'm going to change the colors back to this like so. So to really show you that in action, let's try structuring off the print color method from colors and then calling it. So I'm going to say const print color. Is colors, print color. And now we will call print color directly. So I'll say print color like so. So now what is left to the left of print color? In this case, nothing like undefined. So if we now save this file and run it, we could imagine that we are trying to reference undefined color, which is definitely going to result in some kind of error. So let's now try saving this and see if that is what happens. So I can flip back over and sure enough I will see. Cannot read property color of undefined. So this is just a quick explanation of what's going on with the keyword this in JavaScript. So now I want to take this knowledge that we just got a reminder of I'm going to delete this sample code. I'm going to uncomment the console.log we just had. And now I want you to think about what is really going on with our getter method right here. We are calling Git and what is still left of it. Well, it's user. So that means that if we open up our attributes class, the git method is going to be executed with this equal to our user. That's what we can imagine is really happening behind the scenes now, and that's why we are seeing an error. So if I save this once again and flip back over, sure enough it says cannot read property name of undefined because we are trying to access some like data dot name property. Right. We can imagine back inside of attributes that we've got user data. User does not have a data property. And so we are trying to access something like user data. Name data is undefined. And so we see the error message that we just saw in our console. So that's what's going wrong. All right. So how do we fix this? That's a real question. Well, it's a very, very simple fix. I just want you to understand why we're doing this fix in order to fix this. All we have to do is set up our get method right here as a bound function or essentially as an arrow function. So I'm going to find my method and I'm going to say get equals. And I'll put an arrow right before my curly braces over here on the right hand side. So we've now turned this into an arrow function. In an aero function. This is always going to be correctly bound to our instance of attributes that we create. So this, no matter what, this kind of overrides the role that we just discussed. Now, no matter what, this will always be equal to our instance of attributes. And so that's going to fix up the error entirely. If I now save this flip back over, I'll now see new record appear perfect. All right. So sure enough, the same problem is actually an issue with our eventing class as well. So on eventing. Ideally we need to set up on and trigger as bound functions as well because they have references to this inside them too. So to fix this up, I'm going to find on I'm going to put an equal sign right after it and then an arrow right before the curly braces. And I'll do the same thing for Trigger as well. So there's the equal and arrow. Now this kind of begs the question, how often are we going to want to define methods normally versus define them as arrow functions? Well, in my opinion, really, you should be using arrow functions like 100% of the time. There's just about no scenario ever where we want to somehow have a mystery value of this. However, you will not see a lot of style guides that recommend that for whatever reason. A lot of times people will say, Oh, well, there's a performance penalty. And my answer to that is, well, you know, a performance penalty is like 100% worth not ever having to deal with any ambiguous value of this or anything like that. Just my opinion. But other people will, of course, probably disagree with me. All right. So that's pretty much it. We've now fixed up all of our pass through functions so we can now go back over to indexes and do a final test of these pass through methods. So I should be able to set up user on change. And do something like console.log. User was changed. And then user trigger. Change like. So if I now save this, I should be able to flip back over and I'll see. User was changed. Awesome. Now, one quick thing that I want to mention here. In case you are kind of a exploration guy or kind of an adventurous type, you might try to run on and trigger without binding those functions inside of you venting. So you might try to change these back, right? So instead of on equals, just make it on and then remove the arrow like. So the same thing with trigger as well. And if you do that, you might actually see that your code still works as expected. But in this case, it's just totally by chance that your code would work as expected. The only way or the only reason that it would still work is the fact that we are storing our events on an events property inside of your venting and inside of users. It's. We are referencing events and using that for event. I'm not going to go into too much detail, but essentially if you don't bind on and trigger in this one particular case, they're still going to work. But it's just 100% by chance. And it really comes down to the names of the properties that we used for storing all these different events. And I'll let you do a little bit of exploration on your own to figure out exactly why that is. But again, it's just by chance. And if we called events right there or events over here, anything different, it would no longer work as expected. So just want to throw that in really quickly, because I know in courses like this, people like to adventure sometimes and try out code without doing like pretty much what I say. All right. So now that we've got this fixed, we've now set up our three different paths through methods successfully. So now we're going to take a quick pause right here. When we come back, the next video, we're going to start to set up those second class of methods, the ones that need coordination between these different objects inside of user. So quick pause and I'll see you in just a minute.
