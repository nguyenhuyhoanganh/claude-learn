# Successfully Fetching Model Data

In the last video, we added in some code to actually fetch some information about a particular user and update our current user model using it. So now the last thing we have to do to actually test this out is make sure that we can eventually assign an instance of a user, an ID, because it's that ID that's going to actually tie a model or an instance of a user to some record on our back end server. So to do so, I'm going to flip back over to my index file inside of here. I'm going to once again delete everything inside of here. I'm going to import my user from the model's user file. And then to get started, I'm going to first create a user and I'm going to give it an initial starting ID when I create it. So I'm going to say const user is new user, and I'm going to say, okay, this things ID is one. So I've now created a new user that has an ID and that indicates that this record right here has some backend representation and that we should be able to make a request to our backend server and kind of fill in the information about the user. Now you might notice this kind of introduces a chicken and the egg problem with our application. We are saying, Oh yeah, you can't fetch information about a user until it has an ID, but then simultaneously you kind of ask yourself, how are we going to know what ID to put in here? Don't worry, we're going to kind of solve that in the future. For right now, I just want to make sure that we can hard code or just basically throw in an ID on a user and then have our user instance right here, use that ID to actually make a request and fetch some information. Now, one other thing I want to mention really quickly. We are only able to pass in an ID here now. Because remember, inside of our constructor of user, we are taking in that entire first argument and assigning it to the data property. So our data property is going to have that ID and then down inside that request that we just authored right here, when we made the request, we said, Oh yeah, look at the data property essentially through the Git method and get that ID. So the idea is now like kind of being passed through all the way here to fetch. Okay. Anyways, let's come back over here. So after we create the user and then going to do a user fetch. And I'm just going to basically like kind of leave it there, to be honest. We don't really have any mechanism yet to kind of get a notice of whenever we successfully fetch data. Remember, inside of Fetch, we are not returning anything. We're not returning this promise right here. So we don't really have any good way of saying, hey, we've just fetch some data about a user and here it is. We could, of course add in a console log to this thing, but as far as our index file goes, we don't really have anything or any mechanism to get some notice of when fetch has been successfully completed. So obviously, that's kind of a shortcoming that we're going to have to deal with in a little bit. But right now, we can at least just look at our request log and make sure that the request to fetch information about the user with ID one is actually being issued. Okay. So anyways, I'm going to save this file. I'm going to flip back over to my browser. And I should be able to refresh and in my network request log see a get request with an ID of one and yep. There it is. So now in theory. This user model. After completing the fetch request right there, we should be able to see our given properties on their. If we really want to test this, one way we could fix it is to do like just honestly, this is going to be a total hack, but we could set up a set timeout. We'll just do that to make sure everything's working. So I'm going to call set, timeout, pass it a function. I'm going to give this a timeout of like 4 seconds. And then inside there I'll do a console log of user. So by the time this console log gets executed, we should have already completed the actual network request inside of fetch. And so we should see the actual information on our user instance. So I'll save this. Look back over and then after 4 seconds, I should be able to expand data and see age and name on there. Awesome. So just to make sure you understand why that's so significant, when we created our user model back over here, we didn't provide it a name or an age, the name and the age was extracted or essentially obtained by the user model itself by just using that ID and calling fetch. All right. So this looks good. Definitely a step forward. So we'll take a pause right here. When we come back in the next video, we're going to move on to our next piece of functionality around these network requests and make sure that a user model has the ability to save itself to that backend server as well. Because right now we're just working with one user that we had created inside of our JSON file by manually making a network request with Axios. Like two videos ago. So quick pause and we'll take your save in the next section.
