# Extracting a View Class

In this video, we're going to start to work on our abstract class view. So let's get to it. All right. I'm going to flip back over to my code editor and in my views directory, I'm going to make a new file called Viewpoints. Then inside of here, I'm going to create an export, an abstract class called View. So now this is going to be a pretty straightforward refactor. We're going to essentially just pull over a ton of different properties from user form into view. So it's going to basically be all the different properties and methods that we just spoke about. So just a bunch of cut and pasting. All right. So inside of user form, I'm going to first take over the entire constructor and bind model. So constructor and bind model. I'm going to cut and I'm going to paste those into class view. Now right away we get some errors around user and render. We'll take care of all the errors at the very end. Let's just do the copy paste right now. So back inside of user form. The next thing I'll do is go on down to. Find events. So I'm going to pull, bind events and render. We're going to cut both those out of user form. And pace them into view. Okay. So that's just about it. Let's now go through it and fix up some of these different errors. So first off, we've got an error in class view around the existence of this type user. Now, as you might guess, we're probably going to want to turn class view right here into a generic class. Let's first, though, before we worry about that additional refactor, let's just import user for right now just to get everything working, we're just going to assume that our model is always a user. So at the top I will import. User from up one directory models user. Next up, we've got some errors around events, map and template. So right now we are trying to call those methods even though they are not defined in sight of class view. And our expectation is that those are going to always be defined by a child class. So we need to add them to our view class as abstract methods. And remember, that's essentially telling TypeScript that, hey, don't worry about it, these methods will eventually exist. So I'm going to add in abstract events map. So that's going to take no arguments. And remember that kind of complicated signature we have to put on here. So it's going to be an object literal that has some keys that are going to be strings. And then the value for every one of those keys is going to be a function that doesn't return anything like so. Okay. So next up, we're going to take care of the abstract definition for template as well. So template will be. A method. That just returns a string, and that's pretty much it. All right, so that's all we have to do. Class view is now complete. So we can now flip back over to user form. We're going to import class view and we're going to extend class view. So back inside of user form at the very top, I'm going to import. View. From view. And then I'll say that user form extends view like so. And that's pretty much it. So we can now go through both these classes and just make sure that we don't have any errors. I don't see any inside of here and I don't see inside any excuse me any inside of here either. All right, so let's try saving user form and I'm going to flip back over to my browser, test this out and just see if everything still works as expected. Okay. So back over here. I can refresh the page. Yep. Looks like I can change the name. I can set a random age. Everything still works. So I think you got to admit, definitely a very easy refactor compared to going with the composition style approach. All right. So now that we've got this thing all put together, the last thing we have to do is turn view into a generic class. Because, remember, we don't want our view to always be working with the user. We want it to work with any arbitrary type of model. So let's take a quick pause right here and start working on that in the next video.
