# Parcel Setup

In this video, we're going to do a tiny little bit of project setup. So you'll notice I'm still inside of my set stats directory here. In the last project we worked on, I'm going to go back up one directory, back to a workspace folder, and then inside of here I'm going to make a new folder called Web. So that's going to be the name of the project that we're going to work out of. So I'm going to change into that directory. And then inside there, I'm going to start up my code editor. All right. To run this new project, we're going to be making use of parcel once again. You might remember that we used parcel earlier on inside this course to get TypeScript running easily inside of our browser. So if you skipped over that video, you're going to need to run a very quick command. You're going to need to run NPM, install dash G parcel, dash bundler if you are already installed bundler, as I showed it earlier on inside the course, you don't need to run that. All right. So once you've got bundler installed, we can then start to do a little bit of setup over inside of our code editor. So inside of my root project directory, I'm going to make a new file called index.HTML. And remember what we put inside of here. Whenever we are working with Arcel, we're going to write out just a little bit of HTML. So an HTML tag, we're going to put down a body. We're going to put a script inside of here. It's going to have an CRC property and it's going to point to our root typescript file. So I'm going to say, look, inside of our root project directory, find the shrink folder and then find the indexed file inside there and I'll close off the script tag like so. Okay. So let's now create that CRC directory and get an indexed file inside there. So I'll make Z and then inside there I'll make index dots. And then once inside there I'll do a quick console log and just say hi there to make sure that we have everything working. Okay. So I'm now going to save both these files. I'm then going to flip back over to my terminal and inside my project directory. I'll do parcel index html like so. And we should see that little pop up that says installing TypeScript. And then after just a couple of seconds, we should see servers running at local host one, two, three, four. If you see an error right now saying something about a port in use, chances are you might have the parcel server that we were running earlier, still running. So make sure you stop that other running server before we try running it here. So now we should be able to open up our browser. Go to localhost. One, two, three, four. Open up our console and see. Hi there. Inside the console. Perfect. Okay, so now that we've got something to work out of, let's take a quick pause right here and we'll start working on our project and writing some code in the next video.
