# Re-Rendering on Model Change

We can now set our user's age to some random number. But like we just said, the HTML is not actually being updated. So in this video, we're going to figure out some way to say that any time that our model gets updated or changed, any time we want to re render all this HTML, essentially reproduce our template and stick it back onto the page. All right. Now, to do so, you might recall that when we put together our user model or more specifically now the model class itself, we added in that events property. Remember we had said in addition that inside of our set method, any time that we change any properties on a instance of a model, in this case, like our user model, we would also trigger an event called change. And this change event right here was what was going to allow us to tell other parts of our application that some data has updated. So the solution here is going to be really straightforward. When we create an instance of a view class, we're going to listen to the model that gets passed in and we're going to listen specifically for a change event. Any time that change event occurs, that means that we must have set some new data on our model, and so we should attempt to re render our view. All right. So I'm going to go back over to User Forum and I'm going to add in some code to my constructor to make sure that any time we receive this model right away, we're going to start listening to its change event. And any time that change event is triggered, well, we're just going to basically call render and render is going to re render the template, rewind all the different events and then add that HTML to the page. So pretty easy solution. Okay. So inside my constructor, I'm going to expand the curly braces. And I'll say this model, because by this time model has already been bound. So we refer to it as this model and we'll say on change, any time the change event occurs, we're going to put in a callback right here and inside the callback. We'll say this render like so, and that's pretty much it. Now, one thing that we might do to slightly improve this is maybe just extract this into a helper method that really would not be the worst thing in the world. So I'm going to add in a helper method called bind model. Like so, and I'm just going to move that logic directly into it. And then inside the constructor, I'll call this dot bind model. Now, there's not necessarily a great reason for doing this. I just like to keep the constructor as simple as possible just to make it easier to understand exactly what happens when we create an instance of a user form. Okay. So let's now save this and we'll test it out. So I'm going to flip back over. I'm going to click on set Random Age and. Well, it kind of works. I guess you'll notice that we've now got a brand new instance of user form on the page. It has a different age, which means that we did set a random age. But obviously the fact that we are rendering a completely separate block of HTML is not super ideal. If I click on set age again, well, we just keep on adding on new user forms. The good news once again is yeah, they do have random ages. So the next thing we need to do here is figure out exactly how we can avoid duplicating all this HTML. The solution that we're going to use is going to be really straightforward. I'm going to flip back over to my render method. Inside of user form. So we're going to say that the instant we start to render a second time, we're going to look at our parent element and remove all the HTML inside of it. So essentially every single time we call a render, we're going to look at the parent element that contains all this HTML right here, remove all this stuff, essentially delete it, and then we will replace it with our newly rendered template. That's all. So to empty out that parent element, we can do this. Parent which remember is the reference to the parent element that kind of is holding this entire template. We'll say inour html. Notice the capital html on there. Equals empty string. And that's all we have to do. So that's going to empty out the parent element. We're going to re render our template behind our events and then stick it in at the parent element. So I'll save this and we'll test it out a second time. Okay, so back over here. I'll click on set Random Age and there we go. So we're technically replacing all of our HTML here with brand new sets of HTML. It's not necessarily the most efficient way to do this. Certainly more recent frameworks like once again react in Angular, take a much more elegant approach to this, where they try to compare the different HTML that's in the DOM versus what we are trying to update it with and then only make the updates necessary. But you know what? This is going to work for our framework and it is far more simple. All right. So this is looking pretty good. So we've just about now got everything we need for a view class. The last big thing that we need to add in is the ability ability to nest one view class inside of another. So let's take a quick pause right here and we'll start to wrap up a couple of different elements in the next video.
