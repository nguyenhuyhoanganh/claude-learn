# Storing Event Listeners

In the last video we left off on this on function right here. And I kind of cryptically said, Oh yeah, we need to have support for different kinds of events. So in other words, when we create an instance of a user, we're going to want to be able to add a bunch of event listeners and then only at some future point in time are we going to want to actually trigger or actually run those different event listeners. So we need some way to kind of store all these callback functions until it's time to actually call them. So to do this, we're going to use an object to store all the different event names with all the relative callbacks. Let me show you a diagram that I think is going to really clarify what I'm talking about here, because it's kind of tough to put into words. Okay. So here's the general idea. Inside of our class user, we're going to create an object that's going to store all the different event listeners that get added to a user inside this object. We're going to have keys that are going to be the names of different events. You and I have not yet discussed the different event names that we're going to have tied to our user right now. We'll just imagine that we're going to have like similar events that are related to normal HTML events. So the keys of this object are going to be event names, then the values are going to be arrays that store all the different callbacks that have been registered for each of these different events. So in other words, if some other developer makes use of our user and they call on with an event name of click and then pass in some callback and then do the exact same thing a second time, we're going to take both the callbacks they pass in and we're going to add them to an array. Where the click property inside this object points at that array. When we eventually decide to trigger the click event, we will iterate through this array and call each of those callback functions. This is a pretty standard eventing style setup right here. So you'll actually see this kind of implementation with many other styles of eventing libraries in JavaScript as well. Now, in some cases, we might have some events that don't get any callbacks registered. So this would just be an empty array. We could still trigger the hover event and we would just have no callbacks to run. And then same thing with Mouse over. If we trigger Mouse over, well, we will iterate through this array and call the one callback that has been registered. So that's the general idea. So let's try actually writing out some code to make this happen. All right. So the first thing we need is some property on our class user that's going to store all the different events that get registered. So I'm going to add a new property definition up here. We do not need to pass this in when we create an instance of our user. So I'm not going to add it to the constructor. We're only going to allow events to be registered after a user has been created. That's why we're going to add it in as a separate property. So I'm going to say that there's going to be some property called events on our user. Then we have to put in a type annotation here. Now, the type annotation is going to be a little bit tough, so let's just write out the syntax and we'll talk about what it means. So I'm going to say events is going to be an object and all the keys of this object. Are going to be strings. And then the value for each of those keys is going to be an array of callback functions. And then finally, I'm going to initialize this as an empty object like so. Okay. So what does this mean right here? Well, remember, usually when we write out a type annotation for an object, we just write out the name of the property that's going to eventually exist here. So we might have something like, say click or something like that. But in this case, we don't know the names of the different events that we're going to have. We have no clue. We might have eventually an event tied to a user called Click or Hover or mouse over or maybe like change or sync or delete. The point is, you and I just don't know yet what the different event names are that we will have tied to a user. So to solve that, we're going to say, hey, TypeScript events is going to be an object. We don't know what the different keys are going to be. We have no idea whatsoever. But we do know that they will be strings. In all these different keys are going to point at values that are an array of callback functions. So we use this syntax right here any time we are trying to tell TypeScript, Look, we just don't know what properties this object is going to have. We know it will be an object and then it will have some properties, but we just don't know. So now we can add any property we want to to the event object and always point it at a callback or an array of callbacks. So for example, inside of on, just as a quick example, this is not the actual code we're going to write inside of here, but we can say this dot events and then use the bracket notation and say like anything you can possibly imagine is going to be an array. Like so it can be a empty array or, you know, technically these are supposed to be callback functions. So we could put in like say an empty callback function like so. So we can continue adding more different properties to this object. And this is 100% allowed because we specifically told TypeScript, look, we don't know what keys this object is going to have right now, but we will in the future. Okay. So that looks good. I'm going to delete that quick example right there. I'm going to save this file. We'll take a quick pause. And when we come back, the next video. Now that we have somewhere to actually store these events, we will add them in any time that we call the on function. So quick pause and I'll see you in just a minute.
