# Collection Views

All right, my friends, we spent a ton of time on this project, and I think that our Web framework is now in a really good spot. So there's one last thing that we need to do here that we haven't really taken care of. You'll recall that when we were talking about our data layer, specifically all of our models, we created that generic collection object. And the purpose of this was to fetch a big list of all of our users or really any type of resource from our JSON server. So we have the ability to have a collection of data, but we don't have anything right now that can actually render a collection. So the last thing I want to do and I'm going to leave this up to you to try it on your own, is to create a sort of collection view. So the idea here is that we should have some kind of collection view as a class, and chances are it should probably be an abstract class because, well, we might want to have some subclass of it that implements some special functionality. So the goal of collection view is to take in a collection of models. I imagine that this class would probably have a render method that is going to attempt to render out a view for each model that we have fetched in order to render out a view for each model, I think that we might have an abstract method called render item. So this would be abstract right here. I guess I just couldn't quite fit it on the same line. So then any time that we subclass Collection View to create a class like user list, we would require user list to implement render item. And I would imagine that this method might take in an instance of a model and a reference to some parent element, HTML element. And then inside of render item, we could attempt to render a single view with that given model and pass in a reference to that item parent which remember is an element. So this is just one possible way to implement a collection view. There is definitely some other ways we could approach this as well. So like I said, I would love if you took a shot at implementing this collection view. Now, this is 100% optional. You do not have to do this if you don't want to. And I'll be totally honest with you, it is a pretty challenging little project, so you could easily spend a pretty good amount of time putting this thing together. It's definitely not for someone who doesn't have kind of a background with web development and HTML already. So if you want to take a shot at this, go ahead. We're going to come back in the next video and I'm going to walk through a solution that kind of takes this approach right here. So quick pause and I'll see you in just a minute.
