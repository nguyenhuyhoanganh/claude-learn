# Two Important Rules

In the last video, we established that we need to do some major work on our JIT method. So I told you that we're going to fix this and make it really nice using generics. But before we do that, we need to build up some understanding around two really important topics. So let's get to it. Okay, so here's the two really important things for you to understand. First off, in typescript, strings can be types. Now, this sounds a little bit crazy. So basically we're just going to write out a little bit of code to explore this idea. So I'm going to flip back over to my code editor and down at the very bottom of the attributes file, I'm going to write out just a little bit of sample code. Again, we're going to delete this very quickly. So you might recall that back when we created a tuple much earlier, we used some syntax. We refer to it as a type alias. So when we created a tuple, we wrote out something like type, what was it? Match data, that's what it was. And we wrote out an array and all the types that we expected to be in there at their precise indices. So we had like string number, number, whatever it was. So we can use this type alias syntax to also create new types using other values besides just an array like the one you see right here or technically a tuple. So we can use this type alias syntax to create a type out of a string as well. To do so, we could write out something like type. Then the name of the type. Maybe I'll put down like best name. And then the type is going to be a string. In this case, the best name is going to be. Well, of course, Stephen, I'm just kidding. But just as an example. So even though it looks like we are doing a variable declaration here, we are actually creating a type. So I can use the word best name in any location where I would normally put a type annotation or example with a function call. So I'm going to define a function here called print name. We're going to culprit name with some string. But more than just being any string, I'm going to say that the only possible type of value that you can pass into this function is a type of best name. So I'm going to call that argument name and then I'll annotate it with the type of best name, and then it's not going to return anything. So if we normally built out a function like this and put in a type annotation of string, that means we could pass in any string here that we want whatsoever. But by putting in best name with best name being a type, we are essentially saying you can only call print name with a string of Stephen. If I try to call a print name with any other kind of string, I'm going to get an error. So if I call it with any gibberish like so, nope, not allowed because this string right here is not of type. Stephen That's essentially what we're saying. So the only value that I can call print name width is a value of type. Best name. And for a value to be of type best name, it must be a string and it must have the characters. Stephen. So I can call a print name. With Stephen like so. Notice how that call succeeds. All the others fail. And this might seem like a really crazy thing to have inside a typescript, but it turns out there's a lot of scenarios where we kind of do want to create a type out of a string. The the actual use case we're going to use it for in just a moment. Well, obviously, it's going to kind of solve a problem like this, but there are many other applications where we would want to create a type out of a string. And if you're at all familiar with, say, redux, well, it turns out that being able to type your actions, which remember, have a type property that is usually a string, it turns out that this kind of feature is really nice with Redux. I just want to throw it out there really quickly. Okay. So now that we understand this idea that in TypeScript a string can be a type, I'm going to delete the sample code and we're going to go look at the second big thing to understand. But here's the second big thing in JavaScript and therefore TypeScript as well. All object keys in reality are strings. Even if you actually think it is a number. So for this point right here, I'm going to open up my browser console. I'm going to write out a quick code snippet. All right. Here we go. So I'm going to create a object called I don't know, let's do colors. That's my favorite one. So I gonna create an object called Colors. I'm going to make an empty object and I'm going to say I want to add a property to this thing and I want the property to be like, how about read whatever? I assign that a value of red. Okay. Nothing too crazy yet. If I print out colors, I get red. Red. Let's now try doing the same thing with a number or using a number as the key. If I want to use a number as a key in a JavaScript object, I have to use that bracket syntax so I can say like colors at five equals the string red like so. So now it would appear that I'm using a number as a key. If I hit that and then print out colors again, I can even see the key right there. And it really looks to me visually from this debugger as though the number five is a key. And heck, I can actually even verify that I can put in colors at five, and if I do so, I'll get back red. So once again, I've got some kind of thought process here. I got some evidence that makes me think that the key is possible to be a number, but in reality that is not what is really going on behind the scenes. You see, whenever you use a number as a key like this, behind the scenes JavaScript takes that number and it first turns it into a string. And that happens any time you try to access a property or set a property on an object. So in reality, even though we really think that we are using a number here as a key note, it is a string and to prove it we can write out colors and then five as a string. And if I run that, I still get back red. So even though you really think you're using a number here as a as a key know internally it is first being converted into a string and then that string is being added to the object as a property. All right. Now, that might seem like it's really irrelevant, but I want you to think about the impact of these two rules together. So in TypeScript, a string can be a type. And then we are secondly saying that in JavaScript and therefore TypeScript as well. All object keys are strings. So if we kind of put these two facts together, what does it tell us? Well, it tells us that on an object or the keys of an object can actually be a type as well. Now that might sound really strange, but I just want to throw this sentence out once again because it's kind of hard to picture in your head in TypeScript. The keys of an object can be a type as well. Now just saying that like so many things inside this course, just saying that doesn't really give us any further understanding. So let's take a quick pause right here. When we come back in the next section, we now have enough knowledge to put together this kind of crazy generic syntax on our JIT method. So we're now going to start to add in the crazy generic syntax, and then we can go over the syntax and really understand what is going on. So quick pause and I'll see you in just a minute.
