# Extracting a Model Class

In the last video, we had a discussion about how we should probably create a new class called model and then use that to extend user. So this new class called model is essentially going to have every last line of code that is currently inside of user. So let's get started on class model right now. Inside my models directory, I'm going to make a new file called model. Then inside, if you're going to create an export, a class called model. All right. Now, the first thing I want to do inside of here is define three different interfaces. Remember, we spoke about how inside of user we said this several times. Very unlikely that we will want to replace the venting and attributes, but definitely likely that we will want to replace sync. So we're just going to stay consistent and either use interfaces for all properties or no properties. In the case of model, I think it makes a lot of sense to set up these three different properties as being typed with interfaces so we can allow them to be dynamically exchanged. So if we ever want to create a user that gets backed up to local storage instead of some outside API, we can very easily make that swap. So in class model, we're going to take the interface style approach. So we're going to define a interface called modal attributes. This is going to be for the attributes property, as you might guess. We'll do an interface called Sync. That's going to be for sync. And notice how the name is identical here. I think that part of all this refactor, we might want to also change the name of class sync into like API sync to make sure that it's really clear that this is going to sync some data to some outside API. So given that we are probably going to end up naming renaming Sink, I think calling the interface simply Sink makes a lot of sense. And then finally we'll need an interface for venting, which we'll just call. How about like events or something like that? Kind of hard to put a name to this particular one. Okay. So now we're going to have to set up an interface for each of these. Now, in some cases, it's going to be really easy, like in the case with events, in other cases where we're using generics, well, it might be a little bit more challenging. So let's do events first. Now to get a guide on how to implement this interface, I'm going to first pull up my eventing file. So eventing should have that on function. That's going to take those arguments and trigger as well. So I'm going to say that there needs to be a function called on. One needs to take an event name that's going to be a string. Remember the name of the argument doesn't matter. And then it also should take a callback that's going to be up type callback. We could define this function in line here or the type signature in line. So if we wanted to do that, we could write out simply like that and say void like. So remember inside of events when we set up that type alias right there, we could use a type alias here as well. It's totally up to you. And I'm going to say that this is going to not return anything. So then I'll also set up a trigger. And this is going to take an event name. That's a string. And return nothing. So I'll market as void as well. Okay. So there's one interface down. It's now the next one that we're going to work on is model attributes. Now, just like classes and functions can be, generics interfaces can as well. And we want this interface right here to be a generic. The reason we want it to be generic is that remember our current attributes class is a generic because we don't know what type of data it's going to be returning or receiving in several of these different methods. And so we need to reflect that kind of uncertainty inside of the interface definition as well. To write out a generic interface. We're going to use the exact same syntax we see inside of a class. So for model attributes, right after that, I'll put my angle brackets like so input down a T. So now inside of our interface definition, we're going to use a T in every location where we are kind of uncertain about the type or we kind of want the type to be a variable of sorts. So the easiest method to get started with would be our set method. Set is going to take some update object of type T and then not return anything. So we're essentially going to write out the same exact thing inside of our interface as well. So I'll say set value of type T and return nothing. So now if we ever wanted to designate a model attributes interface that works with user props, we would just say like model attributes and then those angle brackets and then user props inside. That's pretty much it. So now we can repeat the same thing for get all and get let's do get all first. So get all is going to take no arguments and it's going to return an object of type T. And then the JIT method is going to be a little bit challenging because we need to use the same syntax you see right here. So we're going to say essentially get and we're going to specify that there's going to be a generic inside this function and the generic K is going to be one of the possible keys of T. We're then going to set up the constraint and say that that argument key is only ever going to be one of those keys of T. And then finally, the return type is going to be a lookup on type T for the given key. Okay. So get. Will say K extends key of T. That's going to take a JFK and it's going to return some type where we need to look up the T type. And find the k property on their. All right. So that's pretty much it. So now we can do sync as well. So sync is going to be very similar. Or sync. We're going to say type T. And inside we're going to do a fetch method. Remember inside of sync. Let's open that up really quickly. Beck takes an idea that it's a number and then returns some kind of promise. So right now, it might not be the best idea to say that fetch is always going to return a promise. For example, if we think about sinking information to a local device, we can technically make use of local storage inside the browser, and there's no asynchronous nature about that. So it might not be the best idea to always say that we're going to return a promise from fetch, but we'll just stick with that for right now. Okay. So for fetch, we're going to take an ID that is a number, and then we're going to have to return some kind of promise object. So I'm going to say this is going to be an Axios promise and we're definitely going to have to import that type of the top. I have an add on that automatically import stuff for me so you can see that that was imported automatically when I wrote that out. If your editor did not automatically import that, you'll need to add in the import manually. So now we can do our save method as well. So Save is going to be called with some data that we want to save of type T and we're going to return a promise. So we're going to save. Some data of type T and then also return a promise like so. Okay. So that's it. So we've now got these three different interfaces and they mean that we can now swap out implementations inside of class model. So now in theory, we could make a user who has model attributes in events and some sync object that saves data to a remote API. And then we could potentially also make a image model and the image model might have the same model attributes and events, but it might have a sync class that instead syncs information to local storage. So really easy to swap out these different objects based upon the type of model that we are working with. All right. So let's take a quick pause right here and continue working on class model in the next video.
