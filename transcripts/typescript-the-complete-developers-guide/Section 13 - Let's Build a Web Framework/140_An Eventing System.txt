# An Eventing System

We've now finished up our first part of class user. So we have the ability to have some information about a user. We can retrieve those properties and set them as well. So we're now going to move on to our second little part of functionality around our user, which is to make sure that whenever we change something about our user, we can notify other parts of our application. To do so, we're going to set up a little mini eventing framework of sorts. Now, when I use the term eventing in JavaScript, I'm talking about a very specific programming architecture, kind of a style. If you've ever worked with HTML before, you're probably very used to setting up event handlers. So inside of my JavaScript console, really quickly, I'm going to get a reference to document dot body and I'm going to try to add an event listener to it. To do so, I can call add event listener and then pass in the name of the event. I'm looking for like maybe a click, and then I can pass in as a second argument, a function to run any time that event is triggered. So then inside of this function, I can do like a quick console log and say hi there or whatever else. So the whole idea here is that we are adding an event listener, we are watching for a very specific event to occur. And whenever that event occurs, our function right here gets invoked. The function right there is going to be invoked by the browser. So that is some like built in functionality. So we want to set up a system just like this right here to work with changes to data on our user model. So it's going to be very similar to this add event listener type function. The only big difference is that we're going to call the function that sets up the event listener on. So rather than calling it add event listener, we're going to call hours on. The first argument is going to be a string of the event that someone is watching for. And then the second argument will be a callback function. Now remember, when we make use of browser based events like click events, it's usually the browser that eventually says, Oh, something was just clicked. I need to call all the callback functions that were registered. But in this case, you and I are building the actual framework. And so you and I have to build in the actual ability to say, Oh, something just happened. We need to run all the callback functions related to some given event. And so you and I are also going to build a function called trigger, and this trigger function is going to run all the callback functions that have been registered by on. All right. So that's the idea. Remember, the entire goal of On and Trigger right here is going to make sure that other parts of our application can react to some changes in our user data. All right. So now that we've got a better idea of what On and Trigger are all about, let's take a quick pause right here and start implementing them in the next video.
