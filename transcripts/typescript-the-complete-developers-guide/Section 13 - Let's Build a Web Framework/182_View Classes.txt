# View Classes

We've now wrapped up the data side of our application so we can create collections and models. We can use these to represent different resources inside of our app, such as blog posts or users comments, images and so on. So now that we've got the data site all wrapped up, it's time to think about the view side or the side that's going to actually produce some HTML and show it on the screen. So to approach this problem, we're going to take a really common approach that is used in a lot of other frameworks, such as React or Angular. We're going to make individual classes that are responsible for showing very discrete sections of HTML for our app. So here's the general idea or the little test application we're trying to put together. We're going to segment this thing into different classes. So, for example, showing the text user detail and some information about a particular user is going to be the responsibility of a class that we're going to call user show. Then down here at the bottom, we've got a form being displayed. We're going to make the responsibility of all that HTML inside of a class called user form. To make sure that we can show both a user show and user form at the same time. We're going to somehow nest these inside of another class called user edit. So this is a really common approach. We're going to basically say, yep, all this segment of HTML is being produced by one class inside of our application. We also want to make sure that we have the abilities to nest one object or one class is HTML inside of another's. So in this particular case, we would end up with a hierarchy that looks like this. We're going to have a user edit class, and inside of it we're going to show a user show and a user form. All right. So now we get that understanding. Let's talk about some more specifics of what we have to do here. Okay. So a couple of notes on each of these each of these different classes we are going to refer to as a view or a view class because it is responsible for showing some HTML. So we're going to say that every view class we put together must produce some HTML and show it on the screen. We definitely want to make sure that we have the ability to nest one view's HTML inside of another so we can actually show user show and user form inside of user edit. Inside of each of these view classes, we're definitely going to have to have some way of handling user events. So in other words, handling the case in which a user clicks on one of these buttons or selects that text input right there and types in some new text. One thing I want to mention here is that we're probably going to have a really tight coupling between these different views and some model. So in other words, whenever we look at, say, a user show right here, it's very clear that we need to show information about a very particular user. And that user is provided probably to user show as a kind of model. So there's definitely going to be a very tight coupling between the models and views of our application. And then finally, well, at some point in time, we're probably going to want to have the ability to actually like reach into the HTML produced by view and trying to mess around with the HTML inside there. So, for example, any time a user types in that user input right there or the name input, we're probably going to want to have the ability to kind of like reach in to our HTML and read out the new text that a user has entered inside their. Okay. So that's just some high level notes on some things we need to think about as we put this class or this kind of view stuff together. So here's the overall goal. We're going to take an approach very similar to what we did with our user. Remember, when we put together the user model, we initially put together just a user class and then we started to extract logic from it and eventually turned it into something reusable. So we're going to do the same thing here as well. We're going to focus on just implementing one class called user form. So remember, that's going to be this little section, the form down here. So we're going to get user form working really well. And then once we do that, we're going to then extract some reusable logic from it. And once we have that reusable logic, we can then use that to implement user edit and user show as well. All right. So now that we understand the plan, let's take a quick pause right here and start writing some code in the next video.
