# Reminder on Accessors

In this video, we're going to start to restore access to a couple of different methods on class user. Out of all these methods that we're going to implement, there's really two types of methods. So the first type of method that we need to re implement are going to essentially be direct pass thrus of arguments. So in other words, any time we call on en class user, we're going to take whatever arguments are provided to on and just turn right back around and call the venting with those arguments. Same thing with Trigger. Anytime someone calls Trigger, we're going to take the arguments to trigger and turn right back around and call Trigger on eventing with those same arguments. Same thing with Git as well. So we're going to turn right back around and call it Git with whatever key was passed into the Git method. So that's the first type of method that we need to re implement. The second type is going to be a little bit more complex. So each of these methods are going to need some level of coordination between the different objects we have that are backing class user. So for example, any time we call fetch, we're going to have to first do a JIT on our attributes class right here and attempt to get the current ID of the user. Once we have that ID, we can then call the fetch method on class sync. So that's going to be some amount of coordination between these different objects. Same thing with save as well. Any time we call save, we're going to first have to get our attributes, get method and pull off all the different properties we want to save. We can then take that information and call save with it. So clearly there's going to be some amount of coordination required between these different methods. They might be thinking that set doesn't really fall into this category. You might be thinking that any time we call set, we're just going to turn right back around and call set on our attributes instance as well. But in fact, our set method is going to be a little bit more complicated than that. You'll see why in just a little bit. Okay. So now to actually re implement each of these methods, we're going to use a little bit of fancy syntax. So I want to first give you a quick reminder on a topic that we discussed much earlier in this course around assessors on classes. So first, a very quick example. Okay. Inside of my index notes, I'm going to delete all that sample code. And then inside of here, I'm going to put a quick reminder on accessories. All right. So I'm going to put a little test class right here. I'm going to call this class. How about person? I'm going to say that whenever we create a person, it's going to have a constructor and we are expected to pass in a first name. That's going to be a string and a last name. That's going to be a string as well. I'm going to collapse my sidebar so you can see that whole line. So now imagine what we could do if we wanted to say, print out the full name of this person. We could define a method called like full name. And this thing would return a string. And then inside of here, we could return with a little string template, something like. Dollar sign curly brace this first name space and then this dot last name like so. So now we can test this out very quickly. I'll say person is a new person with the name of, I don't know, first name, last name like. So now if I wanted to print out their first name, I could do a console.log. Person. Dot full name. This code right here. 100% works. No issue whatsoever. I can now save this. And if I flip back over to my browser, I should see first name. Last name, print out like so. So that's one way to write this out. But there is another way that we could write this out to avoid having to treat full name as an actual function. So we could set this up as a Gitter instead. To set this up as a Gitter or an assessor, we could add on the git keyword right in front of full name. And we do. So if we now want to reference the full name of this person, we no longer have to treat that as though it were a function call. So we would now remove those parentheses afterwards and we would just write out person, full name. So the reason that we set up get properties like this in general is to just make it clear that, hey, you know what, you're not necessarily calling a function here. Whenever we call a function, the kind of thought process that goes on in a lot of engineers head is that we're trying to like make a change or somehow affect some property on our instance of person. So we might set up a getter property right here any time that we are just retrieving information so that it seems more like we are accessing a property and not necessarily changing anything about our instance of person. So we do this again by setting up an assessor. And the key to it is using that git keyword right there. So if I now save this, I can flip back over and I'll see still. First name. Last name. Now, the thing that's really important for you to see here is that when we reference full name, we are not putting any parentheses on there. When we just reference full name, it's going to invoke this function for us and return that string. Okay. So now that we got that quick reminder on accessories, let's take a quick pause. When we come back to the next video, we're going to start re implementing those different methods on class user and we're going to use a JIT accessory to do so. So quick pause and I'll see you in just a minute.
