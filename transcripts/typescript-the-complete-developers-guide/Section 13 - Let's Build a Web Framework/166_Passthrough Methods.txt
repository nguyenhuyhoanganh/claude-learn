# Passthrough Methods

In the last video, we got a quick reminder on how accessories work. Now we're going to start to re-implement all these different methods on our instance of user. We're going to first begin with the on method right here. And I first want to show you how we could implement on on our instance of user without using an accessory. So I want to show you what we would have to do if you were not using a Gitter. And you're going to see that it would be really nasty. So once we implement it and we see what it looks like without an accessory, you're going to say, Wow, this seems like it would be a lot of pain. And that's when we're going to refactor the thing to use a git access or instead, which is going to be a lot easier. So let's get to it. All right, so I can open up my user file. Here we go. And inside of class user, I'm going to define an on method. So our goal with this on method right here is to take in some arguments and then just turn right back around and call the on method on our events property. So in order to do this correctly, we first need to understand what the events dot on method expects to be called with. So I'm going to open up my events file. And we can find the odd method right here. And clearly it expects to be called with an event name that's a string and a callback function, and then it's not going to return anything. So we're going to essentially use this same method signature on our on method right here. We're going to take those arguments and pass them directly through to eventing on. So we're going to take an event name that's a string, a callback. That's a type callback. And then we're not going to return anything. So then we'll turn right back around and call this event on and pass. Passing the event name and the callback. Okay. So that's what we would have to do if we were not using a jitter or an assessor as we were just talking about. Now, the reason this is not good right here is that it means that every single time we want to pass through some function call or pass through some method call, we're going to have to first look up what different properties are expected by this on function, and we're going to have to manually type them all out right here. So that means that any time we happen to change this on method over here, we would have to go back over to user and update that file as well, which could potentially be a real nightmare because hey, in theory, if we start changing this method right here, we would have to go to every separate class inside of our application that made use of eventing and do a refactor there as well. So that is definitely not ideal, this approach right here. No, we probably don't want to do that. So rather than doing this right here, we're going to instead try using a Gitter. The idea with this Gitter is that any time someone references the on property on an instance of user, we're going to directly return the on function from this events. So we're not going to try to call on. That's not the goal here with this Gitter. The goal is just to return a reference to the on method on events. Let's write out the code for it and I think you'll get a better idea of what's going on. Okay so I'm going to go in to define a jitter of on. And then any time someone references this on property, I will return this dot events dot on. Now notice how I'm not calling on here. So the real leap of faith you need to make here, the real important thing to understand is that I am not trying to call a function right here. Instead, I am trying to return a reference to the events on method. So what does this mean? Let's go back over. Let's save this file and go back over to indexes. So back over here. Remember we said that whenever we use a guitar, we no longer make use of those parentheses. If we just reference that guitar, it's going to run that code right there and we're going to get some value back. So let's keep that in mind. I'm going to delete that sample code and then we will make use of that new guitar. So I'm going to say user dot on. That right there is going to return the en function from the events class. I can assign that function to some variable like so. So now on is the actual on function from the eventing class. You can even see the signature right there. So now if I wanted to, I can call on like so and I can pass in all the on arguments that would normally be expected, like the event name and a callback. So I could say on change. And that passed in some callback as well. So again, the real key thing to keep in mind here is that when we reference users on, we are getting back a reference to the on method on the eventing class. Now, we're probably not going to want to have to write out these two statements every time we want to call on and we don't have to, we can instead simply write out user on and then call it right away. So I'm going to call this with change. And then I'll put in something in here, like console.log. User was changed. And that's pretty much it. I just want to repeat one more time. The real leap of faith here is understanding that that little segment of code is going to return the on method from inventing the parentheses that you see right there are intended to invoke the on method on inventing the parentheses are not invoking this right here by just referencing user on we're going to run that code and return the on function. So the parentheses that you then see are intended to invoke this function, not this one. Okay. So that's pretty much it. So let's now do this same process for our trigger and get methods as well. All right. So I'm going to define get trigger. And whenever we call this or reference this property, we will return this events trigger and then I'll do the same thing for get as well. Notice the funny syntax here. It's like literally get git and in this case we will return this dot attributes target and that's pretty much it. So this is how we're going to implement pass through methods, how this definitely works right here, but it still seems like it's kind of a lot of code to have to write out just to re-implement these methods. We are going to look at another way of handling this later on. That's going to even further condense the syntax a little bit more and will make life even easier than it is with the Skitters. However, we can't use this syntax just yet because of some really funky stuff around how we are initializing these different kind of like sub module properties inside of here and how our constructor is implemented as well. So we will do it eventually, but right now this is going to work out just fine. Okay. So that's pretty much it. Now it looks like everything's going to work correctly here. But let's do a quick test in the next video we're going to test out, say, our JIT method and we're going to very quickly run into an error message. So we're going to figure out why we are still seeing an error and we'll figure out a fix for it. So quick pause and I'll see you in just a minute.
