# The UserForm's Render Method

Let's continue working on our user form. So we have got our template and a property called Parent which we have not initialized yet, but we will in just a moment. So then I'm going to add on one more method here called render. This is not going to take any arguments and it's not going to return anything. So remember the goal of render. We want to take our template and somehow append it as a child to the parent. Now, the important thing to understand here is that right now our template is a plain string. I've been saying, Oh yeah, it's going to return a string that contains some HTML. Well, this is not technically HTML yet. We have to actually take the string and turn it into HTML. So to do so, we're going to use something called a template element. A template element is a type of HTML element. So you can actually create an HTML element that is a template like. So I'm just writing this out as a quick example. We don't actually want that inside of our template. Template elements like the one you see right here are can be used for exactly what we are doing. Basically turn some string into some actual HTML that can be stuck into the DOM. Now if you want to learn more about these template elements, I encourage you to read the Indian or Mozilla Developer Network article on it. This is of course about TypeScript, so I'm not going to tell you too much about this like really intricate details around HTML or anything like that. So I'm just going to tell you basically what we have to write right here. Okay. So we're going to say. Template element is going to be document create element of type template. And let me fix that typo. Then we're going to stick some inner HTML inside this template element. So I'm going to say template element dot inner HTML is this template like so. So this right here is basically the step where we're going to take that string and turn it into some actual HTML that is contained inside of this template element. So now the last thing we need to do is take that actual HTML that we now have and stick it in as a child to the parent. So I'll say this dot parent dot append template element dot content like so. So the dot content property is the actual HTML that we have stuck into the template element. So we're going to take all that and append it to the parent and that's pretty much it. Now, last thing we have to do, we did define the parent property, but we never initialized it. So whenever we create an instance of user form, we're going to pass in the parent property. We're going to essentially tell this class where it should insert its HTML. So rather than defining this as a property, I'm going to instead define the constructor so we can initialize it and declare it at the same time. So let's say constructor. Public. Parent is going to be of type element. Yep. And that's pretty much it. Okay. So believe it or not, this is really all we need to get some HTML to appear inside the browser. So let's take a quick pause right here. We'll come back to the next video and we will test this thing out.
