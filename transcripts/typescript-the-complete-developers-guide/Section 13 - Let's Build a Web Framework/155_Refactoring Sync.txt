# Refactoring Sync

All right, my friends, in this video, we're going to start to refactor class, sink into a generic class. Now, usually creating a generic class is a little bit challenging, but this time we're going to use a little trick just to make it easier. So here's the idea. We're really doing two different factors here. On the one hand, we're saying that we want to turn this thing into a generic class. On the other hand, we also are going to update our save and fetch methods to receive a lot of information about what we are saving and fetching coming from arguments. So in order to make this a little bit easier, we're going to first refactor our class just assuming that we are using option number one. So we're going to refactor class sync to have many different references to user props that's going to allow us to avoid having to do anything related to generics upfront. So we're going to get save and fetch working, assuming as though they were taking in arguments from some outside source that'll be refactor part one. And then once we complete that, we can then very easily go through class sync and essentially replace any reference to user props with a reference to the generic type that's coming in from the class definition. Let's give this a shot as soon as we start writing code. I think you'll understand what I'm saying. Okay. So I'm going to flip back over to my editor and find my Saints file at the very top. I'm going to immediately import that user props interface from our user file. So I'm going to import user props from user right away. We will see an error message. That's because we are not currently exporting that interface from that file. So I'm going to flip on over to user. I'll find my interface user props and it's going to make sure that I export it like so. Right now, I go back over instincts. Air is going to go away. Okay. So now we're going to rewrite sync, specifically fetch and save, assuming that we're going to have a ton of information coming from some object of type user props into both those functions. The first refactor we're going to do inside of here. And this refactor is kind of unrelated, but it's going to help out a lot with the later stuff we have to do. Remember how we said that we've got these really long URLs inside of here. Let's just fix that up right away. So I want to say that we're going to pass in the root URL that we're making these requests to as an argument to class sync when we create an instance of it. So I'm going to define the constructor function. And I'm going to say that this thing is going to be coupled with some public property that we're going to call Root Earl, and that's going to be a string. So now when we're whenever we create an instance of sync, like, say, new sync right here, I want to require us to pass in some route URL. Like that. So now everywhere we wear we have that really long route URL, we can replace it with a reference to route URL. So I'm going to delete that little sample code up there. I'm going to get started on the Axios target statement right here. So I'm going to take everything from HTTP all the way to the end of Slash users. I'm going to delete that and replace it with dollar sign. Early braces this dot root URL. Now something really important here. Notice how I still have a forward slash in here. So the assumption is that when we create an instance of sync, we're going to pass in just everything from HTTP. To users, and that's it right there. We're not going to put on an ending forward slash. If we put in an ending forward slash, then we would end up too in the URL that we're using for the request, which could definitely have a really bad impact. So again, when we create an instance of sync, that's what we're going to do. No last forward slash. Okay. So it looks good. Let's fix up the other two URLs. So they're right here. So down here, I'm going to update that. I'm going to take everything from HTTP to slash users and replace it with this root URL. And same thing right here in this case, we don't actually need any string templating. We can just replace the entire string with root URL like so. Okay. Now, I beg you, please check your spelling. Check all the characters in your URL right there and right there as well. If you have any errors on either of these, it's going to be really hard to debug. Later on, you're going to end up getting a 400 for error and you're going to think to yourself, Why am I getting a 400 for here? Okay. So now we fix that, let's start working on our save method. So remember, we want to pretend that we're going to receive all the data that we're trying to save as an argument into save. So I'm going to receive that as an argument. Then I'm going to call data and it's going to be of type user props. So again, the idea here is that we are receiving all the data to save from outside source. And for right now, until we start to add in the generic code, we're just going to pretend that it is of type user props. So now we can take that data and throw it in as a second argument to our put statement. And the second argument to our post as well. All right. Now, just one more air inside of here. Notice how we've still got an air around this target. So the assumption originally was that this method would have direct access to this target on our user model, but that's no longer the case. Instead, we now want to try to get our ID out of this data property. Remember, user props potentially might have an ID property. It might. It's an optional property. So we want to look into that first argument. We want to see if it has an ID property. If it does, then we'll do the put. Otherwise we will do the post. So I'm going to say that ID. Is going to come from data ID like. So now I can use a little bit of S 2015 D structuring here and refactor that like so. Okay. So that looks pretty good. So I'm going to save this. And I'm going to go back up to my fetch method and we're going to start to refactor this as well. So first off, the first air I see inside of here is something around this git ID. So chances are whenever we call fetch we are probably going to want to say that you have to pass in the idea of the record that you want to retrieve. So I'm going to say that whenever we call fetch, it must be called with an ID. That has to be a number. So now I can fix up that statement right there rather than calling this get ID. I'm going to instead just reference that element or that argument that we just added in of ID like so. Okay. So now this is looking pretty good. Now, here comes the kind of hard part. Right now, after we make the request, whenever we get a response, we're going to execute this inner function right here. So we're going to take that response and update our model with that data. So this is now going to be a little bit more challenging because fetch obviously well, it doesn't have any access to this set. That is a function that is implemented inside of user, not inside of sync. So we need to figure out some way of getting this response to be available to class user. To do so, I think that we should just change the signature of this method. And we should say that any time we call fetch, we're going to return a promise that it will eventually resolve with the data that we get back from the API. So we're essentially going to say that now our user file has to call fetch. It has to chain on a dot then statement, and it's going to be up to class user to take this response right here and update all the user properties with it. So we're essentially changing the level of delegation here. We're saying now class sync is only going to be responsible for making the request, but as far as taking that data and doing something with it, well, that's now up to class sync. So I'm going to take that entire then statement. I'm going to delete it. I'm going to make sure that I return that promise as soon as I do. So I get an air because we had said that we were not going to return anything. So we need to update our return type right here. So to express the fact that we are returning a promise from the axial library, we're going to say that we're going to return an object of type Axios promise. That's what we are now returning. If you hover over that? Well, we never actually imported that inside this file, so we need to import it at the top. So where I'm importing Axios, so I'm going to add in another import of Axios promise like so. And now that air goes away. Now you will notice that we are no longer making use of Axios response. So if we want to, we can definitely delete that import. Now, I know this Axios promised thing right here is probably just a little bit confusing. The only reason it's here is because when we call Axios, we get back a promise and it is a promise that is implemented by the Axios library. So we're saying, hey, we're going to return an object that has all the properties of an Axios promise, and we provide a reference to where that type is defined. It is defined inside of the Axios library. Okay. So that looks good. Now, just one more very small issue. I'm now looking at fetch right here and I'm saying, okay, every time we call fetch, we're going to return a promise that's going to eventually resolve with some data that is presumably customized to our user. So the last thing I'm kind of thinking about here is, well, what is the deal with Save? When we call save right now, we have absolutely no indication whatsoever that the user has been successfully persisted to the back end. So when we call save, that's pretty much it. It's like a dead void. Nothing actually bubbles out of this method. We return nothing. No other part of our code base gets any indication that the user was saved successfully or that the save failed. So I think that we need to do something very similar to save as what we just did with Fetch. I think that we need to return a promise so that whenever you and I call save, we will get back some object that we can use to determine whether or not the user was correctly persisted. So to do so, I'm going to update the return type of save and I'm going to say that this is now going to also return an Axios promise. And then I'm going to make sure that in either of our if statement cases, we're going to return a promise like so. All right. And that's pretty much it. Okay. So that's class sync. So we've now written this thing out, assuming that we are always working with user props. So let's take a quick pause right here when we come back. The next video, we're going to turn this into a generic class. And when we do so, we're going to see a couple of really interesting errors appear here. So we're not quite done with this thing. It's not quite as easy as replacing user props with a T. So let's take a quick pause right here and we'll continue in the next video.
