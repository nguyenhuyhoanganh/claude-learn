# Model Wrapup

All right, my friends. Time to finally test this thing out. Now, I just want to point out one little typo I made in the last video that I fixed at the very end. I called the static class method. Build, build. My mistake. It should be build user like so. All right. So now to test this out, we'll go back over to indexes. We currently have an error on this line of code right here because we're not going to use the new user constructor anymore. Instead, to create an instance of a user, we'll call a user build user. So I'll do a user dot build user and I'll pass in some starting arguments like say an ID of one. So then as a quick test here, how about we set up our event listener to watch for a change event and then we'll call user fetch. So as usual, as we've seen many times, this code right here should attempt to fetch out of my DB json file the information about the user with an ID of one and we should see a console log. And inside that console log we should be able to explore the user and see that it has a name of newer name and an age of zero. So I'm going to save indexed. I'll flip back over to my browser. Yup. There's the console log. Now I should be able to expand attributes. Take a look at data and yup, it has an age of zero and a name of newer name. Perfect. All right. So I know that refactor that we just did was really, really fast, but essentially we are just doing the same exact patterns that we've seen many, many times throughout this course. So we've seen more than once using composition or creating a composed object through a static class method. So the idea here is that we don't want to have to write out all these different kind of like sub module classes to use inside there every single time. So instead we create one static class method and then serve back a preconfigured user with those different subclasses inside there. I shouldn't say subclasses. I'll say like sub modules. That's kind of the term we're using for these composition properties. So now if we want to have a user that was linked to some other source of information instead of an API like maybe local storage, we could add in a new static class method called like build local user. And this would have the exact same implementation. But maybe instead of using API sync, we could instead use some other module called like Local Sync, and maybe Local Sync would have a save and fetch method that saves information to local storage instead. So that's the entire idea. We can now create different versions of class user by adding on these different static class properties. All right, I'm going to delete that. The other thing I want to show you is that we can very easily add on methods to customize the user. So in addition to having all the behavior that a model normally has, maybe we also have some specific things that only a user can do. So like maybe we say is admin user. Maybe that's some method we want to add in for some reason and maybe this is going to return a boolean. And we could say that, I don't know, maybe if the user has an ID of one, they must be the first user and therefore they are an ID. So we could say something like return this dot, get ID. And we'll say like equals one. Like so. So if the idea is equal to one, yep, they must be an admin user. So now to make use of that method, we would do something back inside of indexes like user dot is admin user and that's pretty much it. Okay. So let's take a quick pause right here. We've now got a really solid data layer to build upon. I'm going to clean up that one example method. So we've got a really solid data layer to build upon. So let's take a quick pause right here. When we come back, the next video, we're going to start to implement the next phase of our framework. So quick pause and I'll see you in just a minute.
