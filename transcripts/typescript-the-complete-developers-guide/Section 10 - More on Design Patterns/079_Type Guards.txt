# Type Guards

We just spoke about the result of using a union operator between an array of numbers and a string. Now I want to make sure it's really clear why the results of this is very significant. Remember, inside of our for loop right here, we have some existing logic that is only going to work if we get to treat this collection as an array of numbers. So I'm going to add on another comment right here. I'm going to say all of this only works if collection is an array of numbers like so. And then presumably down here when we eventually implement some stuff to consider collection as a string, that logic is only going to work. If collection is a string. So that means that, like all the code we have right here, it's just plain not going to work as expected because we are using this union operator. I'm saying not work as expected. It's not going to work at all. TypeScript is not going to allow us to compile this code. So how are we going to fix this? We need to somehow essentially trick or tell TypeScript that if we are getting to this block of code right here, we want TypeScript to allow us to treat collection as though it were an array of numbers. And then if we get down here instead to some future logic that we're going to write out, we need to somehow convince TypeScript once again that down here this collection is a string and it should allow us to do all the things that are normally associated with the string. Once again, the problem is that right now, as it stands with this collection, we are limited to only doing the things common to an array of numbers and a string. Remember, eventually we also want to add in support for sorting a linked list as well. And so if we continue down this approach, we are going to be further limited in the number of properties we can access to the different properties that are common between an array of numbers, a string and a linked list. So chances are we are not going to be able to manipulate this collection when we get to that point. So we need to figure out some way of convincing. TypeScript, Hey, this right here, just let us treat it as though it were an array of numbers. So that's what we're going to figure out in this video. How are we going to somehow convince TypeScript to allow us to run this code right here? So to do so, we're going to use something called a type guard. A type card is going to be a check on the type of this collection. Once we run a type guard, we are essentially going to clarify the type of value we are working with. By clarifying the type we are working with. It will restore access to all the different properties associated with this collection with an array of numbers, and we will not have to be limited to just the properties that are common between a string and a number. Now to help you understand exactly how that's going to work, let's write out the type card, because at the end of the day, the syntax for this is really straightforward and simple. It is the concept behind it and understanding what is going on that's a little bit more complicated. So I'm going to wrap this if statement right here with another if statement. It's going to write out the if statement first. Like so. So you can see I've now wrapped the inner if statement with another one. So inside of here we are going to write out our type guard. We're going to say. A ray or something. This collection instance of capital a ouray. Make sure you get that capital on their. And as soon as I write that, you'll notice those two errors right there. Go away. So again, this right here is what we refer to as a type guard. We can use a type guard to restore access to one of the specific types that we are dealing with inside of a union. Operator. So take a look at this. Really interesting. If I now write out right here this collection dot, once again, we're going to see the list of different properties that we currently have on this collection. These are the different properties that are common between the array of number and string. But now inside the if statement we can write out this collection dot and we are back to seeing the list of properties for only an array by itself. So the type card shows you something really interesting in TypeScript. It shows you that TypeScript is way smarter than you would have ever expected. TypeScript can read this statement right here and it understands what we are trying to do. TypeScript understands that if we get into this, if statement collection must be an array. So if we get like right here, collection must be an array. And it's going to have all the properties and all the features normally associated with an array. And so this union restriction is going to be lifted right here, like inside the statement right here. At this point, the type of collection is an array of numbers. That's what it is in. TypeScript is 100% aware of that. So again, we can make use of a type card. That's what this is right here to check to see if we are actually working with the array of numbers or string. And once we put in that type card, we restore access to all the properties associated with an array. So I'm going to clean up that up. So now I want to do the same thing down here as well to check to see if we are working with a string. Now, if you want to add an A type guard to see if we are working with a string, the syntax is going to be just a little bit different. So I'm going to say if type of this collection equals equals equals string like so. So then inside of here we can write out this collection dot and now we're going to see all the methods that are associated with a string. So we just added in another type guard right here, but this type guard clarified what collection was. So now inside the SIP statement, TypeScript understands that collection must be a string, and we have restored access to only the properties associated with the string. And so inside of here, we could write out some logic to do this comparison that is appropriate for a string and then do the swapping logic that would be appropriate for a string as well. All right. And I'm going to delete that. This dot right there. There's one quick thing I want to mention about these two type of or these two type guards. So a quick note here. Here we go. So you'll notice that the syntax we used in those two type guards was just a little bit different. Any time that we want to set up a type guard for a primitive value, like a number string boolean or a symbol, we haven't really spoken about symbols, but I'm going to throw it on here because it's technically accurate. We're going to use that type of operator. So if you want to set up a type card and figure out the type of a value that is a number string or boolean, we're going to write out this right here. So for example, if you wanted to clarify if this collection was a number, all we would have to do is update that to be number. If we want to clarify to see if this collection was a boolean, we would say Boolean like so. This type of check is only going to work in this fashion. Like to restore access to all the properties in a union for number, string and boolean and symbol. And that's it. Even though it type of technically works for arrays and objects and whatnot inside a JavaScript as well, it does not function as a type guard in TypeScript. So we could technically check to see if type of this collection is equal to say object. Just to make sure that part is really clear. If I open up a console really quickly right here, I can do a type of an array in JavaScript and I get a result of object right here. So this is valid syntax, but this is not going to function as a type guard. TypeScript will only allow you to restore access using the syntax right here for a string, a boolean and a number. That's it. So now the other form of syntax we use in a type guard is any time they want to narrow down any other type of value. So if we are not trying to restore access to a number, string or boolean, we're going to instead use that instance of syntax. So we're going to write out. This dot collection instance of and then whatever the constructor function is for the type we are trying to check for now, that's kind of a tricky little thing that constructor function for whatever thing we are trying to check for. So for example, if you and I define a custom class, let's say, how about Sorter? And we want to see if this collection was a of type sorter. We would say this collection instance of sorter, if we wanted to see if this collection was an instance of a date, we would say instance of date. So in other words, we would put a reference to the constructor function right there to use a type card with anything that is not a number, a string or a boolean. Okay. So that is a little bit more on unions. And a little bit more on type cards. Again, we use type cards any time we want to restore access to a set of properties in a union type, which is what we have right here. All right. So let's take a quick pause right here and we'll come back to the next video and kind of discuss where we are now at this point, because we've gone a little bit off topic here from our original goal. So quick pause and I'll see you in just a minute.
