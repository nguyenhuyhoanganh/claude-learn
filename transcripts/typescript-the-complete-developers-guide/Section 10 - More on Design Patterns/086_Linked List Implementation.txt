# Linked List Implementation

In the last video, I repeated probably I think three times total that by using this interface we don't have to memorize a sorting algorithm. Instead, we just have to know that we have to implement these three functions. I know, like I said, I repeated that probably at least three times. Again, that is the entire significance of using interfaces. You're going to see a lot of documentation around TypeScript out there saying, Oh yeah, TypeScript is great because we can make these interfaces to describe the structure of an object or something like that. And that's technically true, but that is like really playing down the useful enough usefulness of interfaces. You interfaces are useful not because we can describe a type, it's because we can set up a contract between one class and another class and say, Hey, if you do X, Y, Z, imagine all the functionality I'm going to give you. That's why we care about interfaces in TypeScript. All right. So having said all that, one other quick thing I want to mention about this interface here. Something that's kind of interesting to keep in mind is that the interface only specifies the property names and respective types of some other class or some other object. The Sortable interface doesn't actually make any claims about these methods doing the correct things. So in other words, we can actually define a class that has a compare function with these arguments and that return value and a swap method with these arguments that return value. And both those methods could do the totally incorrect thing, like maybe compare returns always false or always true. And maybe Swap doesn't actually swap anything at all, but nonetheless has the correct arguments. If we had incorrect implementation of these methods, TypeScript is not going to help us out with that at all. So the interface does nothing to guarantee that we are doing the correct thing inside these methods. It just makes sure that these methods exist and have the appropriate types. That's it. Just something important to keep in mind there. Having said all this, let's talk about what we're going to actually try to achieve in this video. So in this section, we're going to focus on our linked list implementation. Now, the only reason we are doing this linked list stuff is to really prove to you that by using this interface, you don't have to memorize these, like super nitty gritty algorithms of how to like source something. Once again, I got to tell you, I don't think the majority of engineers out there could tell you how to sort a length list off the top of their head. However, if we have this kind of interface set up, we turn the problem from sorting a linked list into comparing two elements in a linked list getting the length of a linked list and swapping two elements in a linked list, which is way easier to figure out how to do then actually having to implement sorting on a linked list. So again, that's the only reason we're going to go through the link list stuff here. So with that in mind, we're not really going to be doing a lot of TypeScript specific stuff in this implementation. And so if you don't care about linked lists at all, and I really don't blame you if you don't, then feel free to skip this video. We're only going to put together a basic implementation of the link list right here so you can skip to the next video. I'll give you all the source code we're going to write and so you can skip all this extra stuff. Okay. So totally up to you. Like I said, pause here if you want to just get past the link list. All right. So if you're still here, let's talk about how we're going to do this. So in the link list data structure, we've got a series of different nodes. Every node contains one value. A value can be of any arbitrary type, but we are going to build our linked list to store numbers. So every node is going to have a single number. It's also going to have a reference to the next node inside of a chain when we get all the way down to the last node over here. The very last one is going to reference a value of null, and that means that we've hit the end of our chain. So we're going to have two classes in total. We're going to have a node class that represents each individual node. And we're also going to have a linked list class. The link list class is going to have a reference to the head node, and it's also going to have a bunch of methods associated with it to manipulate the overall list. So the link last class will have maybe a method called length to return the total number of nodes. It'll have an add method to add in a new node. We'll have an app method to return the element at a very specific index and the chain will have of course compare swap. And we'll also implement a print method as well to iterate through the whole list and print out every value inside of it. And the only goal of print is that we can actually verify that we have correctly sorted the list at some point in time. So with that in mind, let's get to it. All right. So I'm going to flip back over. Inside my CRC directory, I'll make a new file again called Linked Lists and then inside of here let's first put together our implementation of a node. We're going to put both the node class and the link list class directly inside of here. Usually we would probably create a separate file for the Node class, but we're not going to actually use it anywhere else inside of our project. So we're just going to stick both classes directly in here just to save a little bit of time. So at the top, we'll add in a new class called Node. It's like we just said, a node is going to have two properties. It's going to have a value that's going to be a number and that's going to be the actual number we are trying to store in a node. And then next, which is going to be a reference to the next node in the chain, or it might possibly be null. And if it's null, that means that we have hit the very end of the chain. So our node is going to have two properties. I'm going to define the constructor. I'm going to use that shortened syntax. So I'll say public data. And that's going to be the number that we are trying to track here. A note is also going to have a next property. We're going to define the next property with the standalone notation up here because I don't want to define the next node in the chain when we create a node. Instead, I want to build to create a node first and then associate it with some other node in the chain later on. So that's why I'm not sticking next inside the constructor. So next is going to be of type note to say we are referencing another node or it's going to be of type null. And by default when we first create this thing, it's going to be null to say when we create a node by itself, it's not going to be attached or referencing any other node. So by default, next will be null. Okay. So now we've got that put together. Honestly, that's the total implementation of a node. So now we can start to put together our actual length list as well. So I'm going to export class linked list. Notice that we did not export node again. That's because we don't really need to use this anywhere else inside of our project. So then inside of here, we're going to have our link list, have a reference to the head node. So head is going to be either a reference to a node or it might be null when we first create the linked list. If it's null, that means that our link list is totally empty. So I'm going to say that this thing is going to have a head property that is going to reference a node. So it's of type node or it is null. And just like up here, we're going to default it to null. So the link list starts off totally empty. All right. And that's the only property that a linked list is going to have. From here on out, we're just going to add all these different methods. So the first method we need to add is the well add method. So this is going to take in a number. It's going to create a node out of it. Well, then add that node to the very end of our chain. So if we call, say, add with a value or a number of how about two would essentially find the last node. And we'd stick on to the very end like so a new node with a value of two like that. And this new node would now reference null. So I'm going to do my ad method. This is going to take in some number that we'll refer to as data. I don't expect it will return anything, so I'll annotate it with the return type. Avoid. We'll also going to zoom in here really quickly, just we can read this a little bit more easily. So then right away, I'm going to create a new node out of that number data. So I'll say const node is going to be a new node and I'll pass in data like so. So then inside of here, we need to consider two different cases. We need to make sure that we actually have a head. If we don't have a head yet in our list is entirely empty, then we're just going to take the head or assuming the new node that we created and assign it to the head property. So I'm going to say if there is not a head, if that doesn't exist, then this head is going to be the new node we just created and we're going to return immediately so we don't run any of the other code inside of here. Now something kind of interesting here. Notice how we have a return type of void, but we put a return statement on here anyways. So when we have a return type of void, we can still have return statements. Void just means that we're not going to be specifically returning any useful value, so we can still return early if we want to. We just can't try to return like an actual thing, like say a number. If we do that, well then very quickly see an error message. Okay. So now after that down here, we will be in the case where we already have a head node. And so we need to find the last node in our chain and add the newly created node to that. So to do so, we're going to write out a little while loop right here that we're going to use several times throughout the rest of this class to iterate through our chain. I'm going to say let tail is this dot head. So this is going to give us a reference to the first node in our chain. Well then say while tail next. So while the head node has a next property, then advance that tail variable. So we'll say tail. His tail next. Now this little statement right here is just a little bit challenging to get your head around the first time. Like I said, we're going to repeat it several times. So let's flip back over to that diagram and just make sure it's really clear what we're doing there. So we're going to create a variable called tail. I'm going to reflect that right here. And by default, Thale is going to reflect or point at. The head node. So, like this thing right here. We're then going to say, okay, look at tail. When we say look at tail, we are really saying, look at this. If tail has a defined next property. So in other words, if next is referring to an actual node, then update our tail reference and say that tail is now referring to the next node. So then we're going to look at this next node right here. And as long as this thing has a defined next property, then update our tail reference again and go to the next one. And we're going to repeat that same process until we hit the very last node in the chain. So once we get here, we say if tail next refers to something, in this case it doesn't refer refers to a value of null. And so that's when we are going to exit our wild loop. So this last case down here when we hit our very last node will be captured by this right here. We're saying while tail has a defined next property, when tail is eventually that very last node, next is going to be undefined. And so the while loop will not continue after that. Okay. Like I said, we're going to repeat the same kind of while loop right here several times. So I just wanted to cover it really quickly. So once we find that very last node, we're going to take the node we just created. And add it as the next property to the tail. So we'll say tail next equals node like so. So just to make sure that's clear as well, we're saying, okay, once we get to this last node, take the node that we just created. Like, let's imagine it's this one right here. And have the next property of that last one referred to the node we just created. Okay. So that's pretty much it. That is our first method of AD right here. So for this first one, I just want to go through a little bit slowly, just make sure everything was clear. Now that we've got this put together, we can go through all the other methods pretty quickly as well. So the next one we're going to do is how about length? So remember our length list has to have a length property if it's going to satisfy our interface. So down underneath that, I'm going to add in a new jitter. So I'll say get length. This thing is going to return a number. And so this time our length getter is actually going to be kind of complicated. Unfortunately, the reason it's going to be complicated is because there's going to be a lot more logic going on than back with characters collection. Back over here, length was really straightforward and you were probably asking yourself like, Stephen, why are we even adding in a separate method for this? Well, this time around, calculating the length, length of a length list, much more complicated. That's why we created this separate property to find the length of a collection. All right. So inside of here, we're going to first check to see if there is not a head. So if not this dot head, we don't have a head at all, then we're going to return zero because our list is empty. So if we then get past that, we're going to set up another little while loop right here and iterate through our list. So I'm going to create a counting variable called length and I'll initialize it as one and I'll get a reference to the first node in our chain. So I'll say, let node is this head. So we'll once again say while. No. Not next increment or length counter. And then update the node reference right here to point at the next node in the chain. So once again, we're going to iterate through the entire chain. Once we get to the very last node, next will be undefined. And so the while loop will breakout out. And at that point in time, blank will then reflect the total number of nodes that we've crawled through. So then after the while loop we can return length. And that's our length of a length linked list. Excuse me. All right. So now the next method we're going to put together. Is at. At is not required by our actual interface. The reason we're creating at is that in both of our interfaces right here and I think maybe even in print, we actually need the ability to reach into our linked list and get the node at some very specific index. So the goal of at right here is to receive an index and return the node at that index. This is essentially equivalent to the square bracket access or notation that we were using around the array and the string as well. Yeah. So like I said, like with length, this one's going to be a little bit challenging as well, but we can get through it rather quickly. So I'll define my method. This is going to take some index. That's a number. And it's going to return a node. So once again, we're going to do a quick check here. We're going to say if we don't have a head property, then something is off here. In other words, if our link list is empty and we're trying to get some element inside of it, well, this is definitely not expected. So I'm going to throw a new error in this case. I'm going to say index out of balance like so, because essentially someone is asking for a node at an index that definitely does not exist. So then after that, if statement we can set up another little while loop, just like the one we did right there. So let's say let counter this time we're going to initialize it at zero because we want to count from index zero for that first node node. Will then create a node variable to refer to this head. And we'll say while node. So while we have a node if counter equals I like so so or not I but index. So if we have counted all the way up to the appropriate index, that means we have found the node that we are looking for. So in that case, return that node. Otherwise. If we do not satisfy that if statement, then we need to continue iterating. So we'll say counter plus plus to start to move on to the next node and we'll update the node reference to the next node along the chain. So notably node next like so. As a quick note, you might see an error message right now around let node right here. So the reason that you might see that is just reflect the fact. We go? Oh, excuse me. Right here. This node right here. Sorry. So right now we've got an error around Node right there. That's because we have a expectation for returning something. But if we don't ever satisfy this if statement, then we're going to end up with an error message. Or essentially we're not going to actually return a node. So if we eventually get out of this loop without ever hitting the return statement, I'm going to throw an error down here. And say, once again, index out of balance, because essentially we went through the entire list of nodes we had and we never found an index equal to the one we were looking for, which means someone probably was asking for an index that is just too great or larger than our actual link list. All right. We got one other ear right here. So we are trying to assign something as of type, node or null, because remember, node next can be either a reference to a node or a reference to null if it's the very last node in the chain. And we're trying to assign. A variable of type node ordinal to a variable that is supposed to be of type node. So this is where type annotation is coming into play. We're looking at this head right here. This dot head is going to be a reference to a node. And so TypeScript is going to say, oh, this node variable right here must be of type node as well. And so the issue here is that we are trying to assign a type of null or node to something is supposed to be just node. So to fix this, we're going to add on an annual or a manual type annotation. So we're going to say that node can be either a node or null like so remember, this was actually one of the cases that we ran into when we were talking about where we use type annotation. We said that sometimes we have to initialize a variable, but then we only know the actual value or the actual type for that later on. And if that's the case, then TypeScript type inference system is not going to quite work correctly. And so we have to add in that type annotation just to help it along. All right. So that was a nasty one. We are now complete with the app method. So we're now going to move on to our next one. Which is compare, then swap and print and we're all done. Remember, if this is too much for you, you can always take a pause and get the completed code in the next video. I'm just showing you this code for like completion sake. All right. So we'll do compare here. So this is going to take the left index. That's a number and right index. That's the number. And return a boolean. So this is where we're going to immediately use the app method we just put together. So we want to find the node at left index and the node at right index and compare them. Before we do that, however, let's just do another quick check here. We're going to say if we don't have a head element and the entire list is empty, just return false early. Or probably even better yet, we should probably just like throw in air here. So if we don't have anything inside of our list, calling compare doesn't make any sense whatsoever. So I'm just going to throw it in the air here that says list is empty. Just so you know, all these errors that we're adding in. I don't really expect us to hit any of them. This is just kind of covering some corner cases that we might run into if we try testing out some, like, weird inputs to our code. All right, so then we can return this dot at left index. So that's going to get us the index or excuse me, the node at left index. We want to reference its data property to find the number inside that actual node and then compare it and see if it is greater than the node value at the right index. So we'll do this at right index data. All right, one more here. I'm going to zoom back in. I say one more, but it's actually two more. We've got to do swap and print. So this one's going to be swap. So we'll define swap. It's not going to return anything. This will take a left index. That is a number and a right index. That is a number. Now, swapping two elements in a linked list can be really, really, really complicated. So we're going to kind of cheat here and we're just going to not actually swap the nodes, because if you have to swap the nodes, you have to repair all of the different references that might be broken. So like, if we want to swap this node right here and this node right here, we're not like just swapping the node arbitrarily on a diagram, but we really have to do is find the node before this one right here and tell its next property to look at that other one over there. And so that starts to get really complicated. So we're just going to take an easy out here and we're just going to say, rather than actually swapping the nodes, we're just going to swap the values, which is way, way, way easier. So if we want to swap this node and this one, we're just going to take the three and put it over there and the zero and put it over there like so it's good enough for what we are doing. Okay. So inside of here, I'm going to get a reference to the node on the left. So I'll call this left node is going to be this dot at. Left index. In a reference to the right node. And then just as we've done several times, we'll write out some basic swapping logic. So I'll say like left value or something like that. I think we use left hand before, let's call it left hand as well. So that's going to be a left node data. So then we'll take the right hand side and throw it into the left. So left hand data will be right hand or left hand, but. Left node dot data. Is right. Note data. And then a left node. Now write node dot data. Is right node? No. Oh, my gosh. I have failed you horribly here. I failed horribly. Right? Not. No data is going to be left hand. That's it. Okay. Much better. I apologize. Long video here. We're almost done, though. Okay. So that's our swapping logic. And now the last thing we need to do is our print method, just so we can actually print out all the different values that we have inside of our link list and make sure that they were sorted appropriately. So I'll define print down here. I don't expect to return anything, so I'll put on a return type of void. Now, once again, we'll do a quick if we don't have a head and this thing is empty. I'll just return right away. Otherwise we're going to iterate through this list. So I'll do a let node. Is this head and will once again set up a while loop to iterate through the list. So let's say while we have a node, while one exists, do a console log of node data and then update our node variable to the next node along the chain. So notably node dot next like. So once again, we get an error here because node as defined up here is going to every always reference node, but no dot next can be either a reference to a node or null. So we once again have to put on a type annotation on this variable right here to say, hey, it's cool. Like Node can be either a node or null. So node or null. And that's it for real this time. All right. I know this was nasty. Like I said, I just want to speed through this implementation because all this list stuff is super irrelevant for TypeScript. I just want to give you the full implementation to show you. Yes. This, like, actually is doable. Now, once again, I think that putting together compare right here was super easy. Like if you understand what's going on with the link list and once we had the app method as well, of course once we had all that stuff putting together compare no problem whatsoever. Swap. I definitely had a mind blank in there for a second, but we eventually got the implementation put together. And so I think you can agree with me unequivocally that sorting a linked list by just having to implement these two methods and length as well was way easier than having to figure out how to sort the elements in a linked list. And again, that's the entire point of interfaces. All right. So let's take a quick pause right here. We're going to come back the next video and actually test this thing out. So I'll see you in just a minute.
