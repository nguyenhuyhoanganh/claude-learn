# Sorter Scaffolding

Now that we've got a better idea of how bubble sort works, let's write out an implementation of it to work with a very simple array of numbers. So we're not going to worry about any code re-use or anything like that just yet. Let's just get an implementation put together. So I'm going to flip on over to my editor right now instead of my index file. We've got a lot of log statements inside of here. I'm going to just go ahead and delete all this stuff. I'm going to replace it with a simple class definition. So I'm going to make a class called Sorter. And the idea here is that this is going to be a class that's going to take some collection of data and sort all the elements inside of it. So I'm going to say that this sort of class is going to have a property that I'll call like collection. Collection is going to be a type array of numbers. So collection is going to be the data that we want to sort remember. For right now, we are just worried about setting everything up to work with an array of numbers. We'll worry about strings and linked lists later on. So collection is going to be of type number of array. Now we can either initialize this on the same line or we can define the constructor function so that we can pass in a array of numbers when we create an instance of our sorter. I think that when we create an instance of the sorter, we will want to specify the collection as like an argument to the constructor. In other words, we want to pass in the array of numbers we want to sort. So I'm going to define a constructor. To take in that collection. So I'm going to say any time we create an instance of a sorter, we have to pass in something we're going to call a collection, and it must also be an array of numbers. Then inside the constructor, we can say this collection is going to be that collection of data we just passed in. So now as an example of how we would use this, we could say Collins disorder is going to be a new disorder. And we can pass in our array of numbers. So like ten three negative five zero like so. Now, as a quick reminder, we can shorten up the syntax you see right here by using a modifier inside of our constructor argument. So what you see right here inside of our class body is 100% equivalent to putting public right there and then deleting the field definition at the top. And deleting that assignment inside the constructor as well. So 100% equivalent to what we had before. All right. So now that we've created our class and it has some data that needs to be sorted, let's try creating a sort method on here. So whenever we call the sort method, we're going to take this array and sort all the numbers inside of it. I'm not going to have the method return any value, so I'm going to annotate it with a type of void. So now, for example, we would call sorter sort down here. After calling that, the collection should be sorted. So I should be able to do like a console log of sorter collection and see our sorted array of numbers instead of ten three negative five zero. It should be negative five zero 310. So this looks good. So now all we have to do is write out our implementation of sort right here. Let's take a quick pause and then write out that implementation in the next video. So I'll see you in just a minute.
