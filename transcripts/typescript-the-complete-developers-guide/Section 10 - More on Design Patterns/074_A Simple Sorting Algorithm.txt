# A Simple Sorting Algorithm

We've now got a pretty solid build set up put together. So let's start actually implementing our project. Quick reminder, we want to write some extremely reusable code to sort some different data structures. So in this section, I want to tell you a little bit more about the basic sorting algorithm we are going to use. Now, I'm going to be honest with you, we're going to use one of the simplest sorting algorithms around, because honestly, the algorithm implementation here is really not that important. What's much more important is for us to understand how we're going to reuse this code as much as possible. So in general, I don't really recommend you use the algorithm we're going to use, which is called bubble sort. Nonetheless, it's what we're going to use because it's pretty darn appropriate for the project we are working on. So we're going to walk through the bubble sort algorithm and understand how it works by considering just like an array of very simple numbers, like the ones you see right here. So here's how bubble sort works. Bubble sort is going to be a double nested for loop that's going to iterate through our entire collection several times. For every iteration, it's going to consider each pair of elements inside of our collection of data. So it's going to first consider these two elements right here, then these two elements right here, then these two elements right here and so on. At every pair of elements, bubble sort is going to ask a very simple question. It's going to say, is the element on the left hand side of this pair greater than the element on the right hand side? If the element on the left is greater than the one on the right, then bubble sort is going to swap these two elements. In this case, zero is not greater than ten, so no swapping will occur. After this pair has been processed. To me, bubble sort is going to move on to the next pair. So we then move on and consider this pair numbers right here. So we're then going to say, okay, it's ten. Greater than negative one? Well, in this case, yes, it is. So that means we are going to swap this pair of elements like so. We'll then move on to the next pair. So we're now going to consider is ten greater than five? Yep. Once again, ten is greater than five, so we're going to swap these two elements. And that concludes one iteration through our array. After we have completed one iteration through this entire collection of data, bubble sort is guaranteed to have the greatest element all the way to the right hand side. Because remember, we are comparing each pair in sequence and so whatever the greatest element is inside of our entire array, after the first iteration, it will be all the way on the right hand side and therefore in the correct location. So that means that we never have to consider this very last element over here in any future iteration of bubble sort. So now we're not going to have to worry about this element. Instead, we're just going to worry about sorting everything else before it. So now we're going to repeat that same comparison process. But once again, we're going to ignore the last element. So once again, we're going to look at these first two elements and we're going to say, is zero greater than negative one? Well, zero is greater than negative one. So we're going to swap these two like so. Then we're going to move on to the next bear. And we're going to say is zero greater than five or to me, yes, is zero greater than five. In this case, no, zero is not greater than five. So no swap required. Now that we have completed another iteration, because remember, we don't consider that last element anymore, we are now guaranteed on the second iteration to have our second to last element be in the correct location as well. So five is now guaranteed to be in the correct spot. So we don't need to worry about five for any future loops. And now once again, we're going to reset and consider these first two elements right here. And we're going to say is negative one greater than zero. In this case? Nope. So no swap required. Now, once again, the third to last element, or I should say second to last verse. The last, second to last. Yeah, second to last. Right here is now guaranteed, guaranteed to be sorted. So we are not going to have to worry about that element anymore. And we're now left with just one element, which means we don't have to do any more sorting. And as you can see, this collection of numbers has been properly sorted from least to greatest. So that's pretty much bubble sort, in essence, kind of a simple algorithm. Now, it's not the fastest sorting algorithm out there by far, but like I said, it's going to work very well for our purposes. So let's take a quick pause right here. When we come back in the next section, we're going to start to write out an implementation of bubble sort that's going to work only for an array of numbers. So quick pause and I'll see you in just a minute.
