# App Overview

In this video. We're going to start talking about the next big project we're going to work on. So let's get to it. All right. So quick diagram. In this diagram, I've got a couple of different data structures, an array of numbers, a string of characters, and a linked list where each node contains a number. If you've never worked with the link list before, that's totally fine. We are going to discuss exactly how they work. So the goal of this project is to take each of these different data structures and sort them simply from least to greatest. So, for example, an array of numbers should be sorted with the lowest numbers in front, highest numbers in back, a string should be sorted by lowercase character order. So a would go to the front, then capital B, j o, capital P and a link list. Just like the array of numbers, lowest values should come first and then go up to the greatest. This might seem like kind of a silly project, but the entire goal here is to give you another really strong example of how we can effectively use interfaces and classes together inside of TypeScript. So we're going to write out some sorting logic, and if you've ever written out a sorting algorithm before, you know that sometimes they can be just a little bit of a pain. So the goal for this project is only to write out a sorting algorithm one single time. And we're going to make that sorting algorithm algorithm work equally well for all of these very different data structures. So our goal is going to be to figure out how to implement an algorithm one time and reuse that code as much as possible. So that's idea. So let's take a quick pause right here. We're going to get started in the next video, so I'll see you in just a minute.
