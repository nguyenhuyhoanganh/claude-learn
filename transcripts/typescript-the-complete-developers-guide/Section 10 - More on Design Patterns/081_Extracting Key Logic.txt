# Extracting Key Logic

In the last video we spoke about why this was not that great in approach. I know that it might seem now like we just wasted a lot of time writing out all this code, but I just want to point out that we now understand a lot better exactly what the union operator does and how to use type guards and whatnot. At the end of the day, we could have used this as a solution. All the code you see inside of here, we 100% could could have gotten it working this way. But once again, I'm just suggesting this is not the best way that we could have implemented this. So let's now figure out how we're going to really solve this issue. As you might guess, once again, we're going to fix it eventually using interfaces. But before we just dive directly into the interface based solution, we're going to do a little bit of an intermediate refactor. So the intermediate refactor is going to help you understand the general strategy we're going to use to fix this problem. And then we'll start to introduce interfaces and really understand the full big picture. So let me show you a quick diagram to help you understand the intermediate refactor we are going to do. Before I show you the diagram, however, one really important reminder the entire key to everything around this bubble sort algorithm and the whole reason that it doesn't work that well with these very different types is that we've got two operations that are going to have to be customized depending upon the data structure we are working with. So operation number one that has to be customized is the comparison between two elements. Operation number two that has to be customized is the actual swapping logic. So again, that's a super important thing to remember, and that's really the key to understanding the refactor we are about to do. So here's the diagram. Okay. So like I just said 2 seconds ago, the entire key to all this stuff is that the comparison and the swapping has to be customized based on the type of data we are working with. So we are going to do exactly that. We are going to try to extract that comparison logic into a helper function in a different class. And then we're also going to extract the swapping logic into a different class as well. So our sorter is no longer going to contain a direct reference to an array of numbers. Instead, we're going to have our sorter have a direct reference to some new thing that we're going to create called a numbers collection. The numbers collection is going to hold a reference to the actual array of numbers that we want to sort on some data property. Now, the name data here is not special. We could have called this like, you know, numbers two sort or something like that. So data is going to be the actual array of numbers we want to sort. And then inside of our class, we're going to have a couple of different methods that describe how to execute those very special operations, particularly for an array of numbers. So remember, like we just said, the comparison for an array numbers is a very special operation that's going to change depending upon the type of data we are working with. And the same thing for Swap as well. So we're just going to wrap up all this like custom stuff or all the stuff that needs to be custom into its own standalone class. We're going to say, Hey, you want to sort an array of numbers? Great. Here's a class that has an array of numbers, and it also has a very custom function that describes how to swap two elements in an array of numbers. And we're also going to have a function that describes how to compare two elements inside of an array of numbers. So all this custom logic is now going to be wrapped up inside of numbers collection. And as you might guess, this is going to be step one to getting our sorter to be fully reusable with different data types. All right. Another way of looking at this kind of just to rephrase it a different way, is that we want Sauder to be 100% generic and work with very different data types. So we're going to try to make Sauder as generic as it possibly can be. Some stuff inside of Sauder already is generic, like those for loops right here. These for loops are going to stay exactly the same no matter what data structure we are using. The only thing that's really going to change is the comparison and the swapping. And so those very specialized pieces of logic are going to get entirely wrapped up inside of this new class. Okay. So that's the general idea. Now, one last thing here. I want to repeat just one more time to make sure it's super clear Sautter is no longer going to have a direct reference to the array of numbers we are trying to sort. Instead, Sautter is going to have a reference to the numbers collection, and the numbers collection is going to have the actual array of numbers we care about. All right. So now that we have some vague idea of what's going on here, let's start to do this refactor. So step one, I'm going to take this sort of class and extract it to its own separate file called Sorter. So inside of my SRC directory, I'll make a new file called Sorter. It's. And then I'll flip back over to index notes and I'm going to cut and paste the entire sorter. So just the sort of class by itself, none of the stuff down there at the bottom. So I'm going to cut that. I'm going to flip over to sorts and pace the class. So now inside of here, we're going to do a little bit of cleanup. First off, I'm going to put the export statement in front of class order so we can make use of this class in other files. Next, I'm going to remove all the type annotations on public collection right here. Remember, no longer are we going to have collection. Have a direct reference to the array of numbers. Instead it's going to wrap up a class or reference a class that has some information about the number or simply the array of numbers. We want to sort and very specialized information on how to swap and compare different elements inside that array. I'm going to leave the type annotation blank for right now. So I'm going to say like to do fix me. And I'll make that like a segmented comment like so. So I don't break the syntax in this entire file. So we need to put a type annotation in here right now. The only reason I do this is just just to make sure it's super clear that sorter is no longer going to be directly working with that array of numbers. Okay. So next up, we're going to fix up our for loop just a little bit. So I'm going to remove the first type guard right here. I'm then going to remove the comment that we had above the array stuff. I'm going to remove the if statement that was our type guard to resolve that union type. And I'm going to remove the closing curly brace that we had on the type card as well. And then finally, I'm going to indent the remaining if statement that has all of our array logic at this point, please double check your curly braces. Make sure you do not have an extra curly brace right here. You should have just the one curly brace right there that closes off the if statement. And then finally I'm going to delete the comments and the type guard for the collect where collection was a string. And there we go. Okay. So this looks good. Now, remember the entire goal here. We no longer want to have these specialized array logic inside of this if statement. We want to extract that and the swapping stuff into our separate class. So we are going to come back and do an update to that if statement and the swapping in just a moment. But first, let's start working on that actual logic inside of our numbers collection, which we're going to create right now. Actually know what before we start that, right now, this is getting into a long video. So let's take a quick pause and we'll start creating this numbers collection in the next section. So quick break and I'll see you in just a minute.
