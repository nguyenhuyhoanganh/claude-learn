# Separating Swapping and Comparison

In the last video, we started to extract a bunch of logic out to the numbers collection. So we're going to create that class right now and start to implement some of these very specific methods that tell our sorter how to somehow swap elements in that array or compare elements inside that array. So let's get to it. All right, inside of my CRC directory, I'm going to make a new file called Numbers Collection Dots. Then inside this file, we're going to put together some scaffolding for class. So I'm going to create an export class numbers collection right away. Remember we had said that this thing needs to have a reference to the array of numbers, so we actually want to sort. So I'm going to give this thing a property that I'll call data. Again, we can call it anything we want. Just in this case, I'm going to call it data because, you know, whatever. And it's going to be of type number, array of number. Now we're going to have to eventually initialize this. Chances are we're going to want to pass in the array of numbers. We actually want to sort to our numbers collection when we create it. So to do that, we would have to define our constructor. Receive some argument that we might call data and it's going to be an array of numbers. And then we would take that argument and assign it to our data property like so. All right, quick reminder, the exact syntax you see right here, we can condense down by just putting it on a modifier in front of data of public like so removing that assignment and then removing the initialization up there, the declaration up there. Like so 100% equivalent. All right. So now we have a class called Numbers Collection that is going to have a reference to the data that we want to eventually sort. So now inside this class, we're going to define some different methods that are going to contain the super customized logic to compare to elements and decide if they need to be swapped and then to actually swap them. So let's first declare a method inside of numbers collection called Compare. So I'm going to define compare. This thing is going to return a boolean to indicate whether or not this pair of elements need to be swapped. So again, if we go back over to our dates, this method we are defining, it needs to do exactly this right here. This is what we are trying to replace. We want to compare the two elements next to each other and decide if they need to be swapped. So instead of compare, we're going to take in two arguments. The first argument is going to be the index of the left hand side of this pair. So I'll say left index is going to be a number. And then the second argument is going to be a right index. We make sure it's really clear what the purpose of these two arguments are. Like we just said, the goal of this overall method right here is to decide if we need to swap two elements in a pair. And when I say two elements in a pair, remember, the entire point of bubble sort is that we compare each little pair of elements all the way through the collection of data. So like compare those two, then compare those two, then compare those two and so on. And so we need to know at all times what two elements we are trying to compare. So that left index is going to be the index of the element on the left hand side of the pair. And then the right index or the second argument is going to be the index of the right hand side of that pair. So in this case, it would be zero and one. So index zero, index one. And we then moved on to the next pair. It would be index one, index two. Now you're probably thinking we don't actually need both indices right here because the right index or like the right hand side, the pair is always just like the next element over. We could potentially just take left index and then add one to it. So in other words, we don't need to know that we need to compare zero or index zero in index one. We really only need to know, we need to compare index zero and then we can just add one to that and that would tell us, okay, we need to compare that with the next one over so we could do that. We could shorten up the argument list right here to be just one element or one argument, but we're just going to receive two. Just so it's really clear to everyone here that we are trying to compare two elements against each other. Okay. So now inside of here, we're going to do the actual comparison logic and then return the Boolean to say whether or not these elements need to be swapped. So I'm going to return this data at left index. And if the element on the left hand side of the pair is greater than the element on the right hand side of the pair. Right index, then yes, we want to swap these two things. So if compare returns true, then yep, we need to swap these things. Otherwise, if we return false, it means they are already in the correct order and no swapping is required. Okay. So we have now extracted out compare. That means that back inside of Sorter, we have now taken this comparison right here and extracted the array specific implementation of it into that helper method. So let's now do a very similar thing. We're going to extract the swapping logic over to a helper method on numbers collection to. So back inside of Numbers Collection, I'm going to define a method called swap. I don't expect this to return anything. And as arguments, we're going to receive the left index and the right index that we are trying to swap. So again, this would be the case in which we are saying like is let me just update the diagram quickly. So the entire point of bubble sort. Once again, we are trying to say is. Zero. Greater than negative one. If it is, then we should swap these two. So the comparison right there was the compare function we just put together. So now if we succeed in that comparison and we do need to swap these, we now need to actually swap them. So we're going to take in the index of the two elements that need to be swapped. So first argument will be the left index. And we'll get our right index. And once again, those are the two elements that we're trying to swap. So then inside of here, we're going to do these swapping logic specifically for an array of numbers. And so this logic is going to be just about identical to what we have back over here. So we'll say left hand. This is the left hand value. Notice I'm saying left hand here, not left index. So left hand is going to be. This data at left index. So that's a reference to the element on the left hand side of the pair. Well, then take the right hand side and throw it into the left hand index. So I'll say this data at left index is this dot data at right index. And then finally we'll take our reference to the left hand value that we are saving here and throw it into the right hand spot. So I'll say this dot data at right index is left hand like so. A lot of places inside of here that you can make a typo. Chances are, if you make any typo, your editor will tell you. But just to make sure it's really clear, if I click on left index, I should see left index highlighted. Left index. And if I click on right index, I should see. Right, right, right. And if I click on left hand, I should see a left hand and left hand like so. So make sure you've got all the same references, same spelling, everything inside of here. Okay. Now, the very last thing we have to do here. Remember back inside of our sorter to figure out how many elements we are iterating over, we pulled off the length property from our collection, so we need to make sure if we want to like keep this same syntax right here, we need to make sure that our collection, which is going to be our numbers collection, has a length property available on it. So this is going to be the number of elements inside of this data array right here. Again, this is to tell our four loops exactly how many times they are iterating over our collection or how many steps of iteration there are, I should say. So to do this a couple of different ways we can do it. We could add on a new method here called length, and this would return a number. And then from it we could return this data length like so. So this is definitely one way to do it. But notice how in this case we would have to call length as a method. Another way that we could do this, that would be just a little bit more natural, because usually whenever we get the length of like an array or a string, it's just a property, not a method we have to call. So another way we could do this is set up a jitter or something called an assessor. Let's write out some syntax and I think you'll get the idea of what we're doing here really, really quickly. So in front of length right here, I'm going to put the keyword, get like so you'll notice nothing has changed. The only thing that is now changed is how we would now call this method if we now created a new instance of numbers collection. So I'm going to do that really quickly down here. And I passed in some like array of numbers like so. And assigned it to some variable. Now to get the length property of that array right there, I could do collection dot length like so. So when I say length, I'm referring to this function right here. But because I put on the keyword git, I do not have to call length as though it were a function. So I do not have to put on a set of parentheses right there. As a matter of fact, if I do, I will see an error message. So the point of this little git keyword right here is just to make sure that we can reference the length in the way that we are usually used to doing. So whenever we've got an array or a string, all we have to do is reference the length property and it's going to reach into our data array and return the length of data array. Okay. So I'm going to remove that test code down there really quickly. I'm going to save this file. And now the very last thing we have to do in this very long section, I apologize for the length. We have to go back over to our sorter and we're going to wire up the numbers collection to it. So I'm going to go back over to Sorter. Remember we had said that the big thing we had to do here was eventually annotate the type of collection. So at the top, I'm going to import numbers collection. From the numbers collection file. And then I'm going to update that annotation, and I'm going to say that this collection instance property is going to be an instance of a numbers collection like so. And now you'll immediately see up see a ton of errors up here. So we need to essentially go back into this implementation right here. And rather than trying to work with this collection as though it were a plain array. Remember, the entire idea here is that we are delegating out all those very complicated operations to this outside class that knows exactly how to deal with a comparison for a number or array of numbers and how to deal with swapping in an array of numbers as well. So we essentially need to just add in that code right there. So rather than us trying to do this comparison right here directly inside of our sorter, we're now going to say, just call this dot collection compare. And we're going to pass in the two indices that we want to compare. So that's going to be J and J plus one like so. And we'll do the same thing for the swapping logic right here as well. I'll say this collection swap at J. J plus one. All right. And that is it right there. That's everything. So the entire point here, one more time. I know I repeat this many times, but the entire point here is we've now abstracted out all that very array specific comparison and all the very array specific swapping into a separate class. So now the very last thing. Let's test this out and make sure that everything still works as we would expect. So I'm going to save both sorter and numbers collection. I'm going to go back over to indexes. And then over here, I'm going to import sorter. From Sauder. Numbers collection. From Numbers collection. And then going to create a new instance of a numbers collection. I'll call this thing like, how about just numbers collection? No need to be fancy. So this will be a new numbers collection and I'm going to pass in the array of numbers. I want to be sorted. So I'll do like ten three, negative five. And zero again. Now zoom out just for a second. And now when we create our instance of disorder, no longer are we attempting to sort or pass in the array of numbers we want to sort directly. Instead, we are passing in an object that has the data we want to sort and those two very specific implementations for those two steps that are going to be very different for every type of data we try to sort. So now it's a new sorter I'll pass in numbers collection like so. We'll do the sorting step. And then finally, rather than trying to console, log out, sorter collection, let's just console.log out the array of numbers directly which is available on numbers, collection data. So I'm going to console log numbers, collection data like so and that's it. Hmm. As a long refactor. So I'm going to save this. Let's make sure it worked. Flip on over to my terminal and I'll see this sorted. Numbers? Sure enough, no errors whatsoever. Just make sure everything actually, like, did work here. I'm going to change one or two of these numbers. Like, I'll put, like, 100,000 right there at the very start. I'll save it again. And. Yep, there we go. All right. So I've now spoken a ton about this. I've repeated, like, some concepts so many times, so I'm not going to do any review here. Let's just stop right here. We're going to come back the next video. We're going to point out one deficiency, one last deficiency, and then start the last refactor. We have to do so. Quick pause and I'll see you in just a minute.
