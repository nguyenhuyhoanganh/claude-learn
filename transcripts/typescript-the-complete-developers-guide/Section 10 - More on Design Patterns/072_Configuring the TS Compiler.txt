# Configuring the TS Compiler

Now that we have a better idea of what we're going to build, let's start doing a little bit of project setup. Now, on the last project we worked on, we made use of parcel. Remember, parcel took in an HTML document, saw that we were trying to include a TypeScript script tag and it automatically compiled that TypeScript code for us and made it available inside the browser. In this application, I want to try doing things just a little bit different. So instead of running our code inside the browser, I want to try running all this sorting stuff with Node.js instead. So we're not going to use parcel here. We absolutely could if we wanted to continue working in the browser, but instead I'd want to just figure out how to work more directly with the TypeScript compiler. So like I said, we're going to do things just a little bit differently here just to learn some more. To get started. I'm going to first stop my running parcel server with a command C and then going to go back up to a workspace directory and I'll make a new project directory called Sort. I'll then change into that directory and start up my code editor inside there. Once my editor has started up, I'm going to make a new TypeScript file inside my root project directory called Index Dots. And then inside of here, I'm just going to add in a quick counsel log that says hi there. All right. So I want to first begin by just giving you a quick reminder on how the TypeScript compiler works. So now we've saved this file. We can flip back over to our terminal and inside the sort directory we can run TSC and then the name of that file indexed. So when we run that, typescript is going to compile that file and then spit out a indexed JS file. So essentially it just takes the name of that file copies. It turns all the code in it into JavaScript and then turns the extension into JS instead. So now inside of our root project directory, we can see indexed JS and it contains the JavaScript equivalent of our TypeScript code, which right now is pretty darn similar. Now, as you might imagine, every time we compile this indexed file, we're going to end up with another JS file inside the same directory. Chances are if we just had all of our source code inside this root project folder, things would get really messy really quickly. So let's try organizing our project a little bit better. I'm going to try creating a directory inside of here called CRC, and I'm going to put all of my typescript source code into that folder. So I'm going to move indexes inside their. Then I want to try creating a new folder inside of our project directory as well called Build. Make sure you create this directory inside the root project folder. It should not be inside of CRC. If you just created that build directory using vs code sometimes vs code kind of stick stuff into the existing directory that you might have already selected. So see how I have it selected right here. If I create a new folder right here, it's going to be stuffed right into CRC. So again, just make sure you did not put build into CRC. All right. So the idea of build is just to organize all of our compiled code. So once we compile indexes, rather than sticking the result into the CRC directory as well or into our root project folder, I instead want to get the compiled file placed into the build directory instead. Like so. So that Bill Directory will contain all of our compiled code and we will basically know we don't really need to ever mess with that folder. And we are only going to edit the code inside of RC. So let's figure out how we can figure can configure our TypeScript compiler to now work with these two separate directories. To do so, we need to generate a file called a config JSON file. This is a TypeScript compiler configuration file that we can use to customize how the compiler behaves to generate a a config file at the terminal. We will run tsc init like so. And then we will be told that we created a config file. So let's go open that up and see what we are working with. So here's that file inside of here. You are going to see a ton of different options. So these are all options we can use to customize the compiler. Any time that we run the TSC compiler at our terminal, it's going to automatically check to see if we have a config inside of here. If we do, it's going to use these settings to customize the compiler. So in order to tell TypeScript or the compiler to get all of our code out of the SRC directory and then place all the results inside of the build directory, we're going to find two settings inside of here called out dir and Root Dir. So Route Des is going to be a relative path reference to the directory that holds all of our source code. So I'm going to uncomment that line and say all of our source code can be found in the DOT slash CRC directory. Then the outdoor is where we want to place all of our compiled code. So I'm going to uncomment that and I'm going to say take all of our compiled code and put it into that build folder like so. Once I've done all this, I'm going to save this file. I'm then going to flip back over to my terminal and then once over here I can now just run tsk by itself. If I just run, tsk, that is going to find that config file that we just generated and it's going to say, Oh, okay, I need to take all the code out of this directory as RC. I need to compile all of it and put all of it into the build directory. So if we now run, tsk, like. So we'll get kicked right back over to the command line. But we can now look into the build directory and there still is that indexed JS file. Well, I kind of had dragged ours inside there, so I'm going to delete indexed edges really quickly. Let's try running that command one more time and just make sure that the compilation actually worked as expected. Okay, so there's one more time. And now inside of Build. Yep. I see my file right there. Now, a very easy way to make this a little bit easier on ourselves would be to run tests with one additional flag. Of Dash W Dash W means watch all the files inside of the root dir or essentially the CRC directory for us. So any time we change any of our code inside the CRC directory, the compiler is going to automatically recompile everything and stick it all into build for us. So that just means that we're not going to have to run. Tsk again and again. And again and again. So I'm going to run that. And now we'll see a very different interface here, we're being told, okay, we're going to continuously compile all of our code. So now if we flip back over to index.html and start making some changes inside of here, like let's say, how about add a function called log something? And, you know, whatever. Just put some code in there. If we then save that, we can go back over to our terminal. In the background when I saved that file. This thing automatically recompiled you can actually see that it says file change detected. And if we now flip back over to our build index JS, we will see the updated source code inside of here. All right, so that's pretty much it. So once again, we can configure the TypeScript TypeScript compiler. Is there a lot of tongue twister words by creating a config JSON file and we create that by running TSC dash dash init at our command line. So now we've got a little bit better build process here. Let's take a quick pause and do one last quick update in the next video as well.
