# Concurrent Compilation and Execution

We've now got a pretty automatic process for re compiling our code, but I want you to think about what this really means on a very practical level. We're now basically saying that every time we make a change to index, it's like, let's say let's now try calling that log something function. We can save this file and now the build index JS file will be updated, but that's pretty much it. We now have to actually figure out how to run this thing. So to run that file we have to go back over to our terminal. We can now either stop this running process with Control C, or we can open up a second terminal window inside the same directory. So I'm going to do the second option. I'm going to open up a second terminal window in the same directory. And now inside of here I can do node build, slash indexed JS And that's going to run my JavaScript files. Now as you can kind of imagine, having to rerun node build index JS every time we make a change would be a little bit of a pain. So I think we still need to do a little bit of setup here and kind of automate the process of running our code every single time we make a change. So to do so, we're going to set up just a little bit of additional tooling to get started. I'm going to close that second terminal window and I'm going to stop the running typescript process with the control c. Then inside of here, I'm going to generate a package JSON file because we are going to install two command line tools to help us automate all this code running stuff. So I'll do an MPM init dash dash or something. Dash y like so to generate a package json file. Once I've done that, I'm then going to do an NPM install for a module called Node Mod and then another module called concurrently. So Gnomon is going to allow us to rerun Node any time a file changes inside of our project. So Norman is going to be responsible for actually executing our code once it has been compiled. And then concurrently is going to be responsible for helping us run multiple scripts at the same time. In particular, we're going to want to run a script to start up the TypeScript compiler, number one. And then number two, we're going to use Node Mod to actually execute that code. So again, concurrently is just going to help us do both those steps at once using one single command. Okay. So going to install those two modules. And while that's going, I'm going to flip back over to my code editor. So now inside of package JSON, I'm going to open that thing up. We're going to see our two dependencies added in here really quickly. Once that's done, I'm then going to find my script section and we're going to write out two scripts, actually. Excuse me, three scripts total. To compile our code and then automatically run it. So I'm going to delete the existing script that's there and we'll add a first one that's going to start up our TypeScript compiler in watch mode. So I'm going to add a new script called Start Colon Build. And anytime we run that script, we're going to actually execute tests. So again, that's going to start up the compiler in watch mode. Next, I'm going to add a script that's going to actually run that compiled code. I'm going to call it Start, Colon Run. And for that I'm going to run node man build slash index. JS So. And then finally. I'll add in a stark command, and the only job for start right here is to run, start, build and start run at the same time. So rather than having two terminal windows open, one running this process and the other running this process, we're just going to run start and that's going to start up our entire project for us in a development mode. So for this command, we'll do concurrently. 9 p.m. Colon Start. Colon Star. So again, check your spelling there end PM Colon start colon star. So this is going to tell concurrently to look at our NPM scripts. So that's our packaged JSON script section right here and find every script that begins with Start Colon and then anything. So that's going to find this script number one and this script number two, and it's going to start them both up in parallel. Okay. So now we can save this file, flip back over to our terminal one more time. And then over here we can do NPM start like so. And once again, that's going to automatically compile our code and then rerun it every single time we make a change. So that's pretty much it. If we now flip back over to our editor and open up indexes, we can now change anything inside of here. So maybe instead of a console log of Hi there, I'll do like one, two, three. I can save this file now and if I flip back over, I'll see that console log right here. Cool. So we've now got a much more automated building and running process and we're going to use this little setup right here quite a bit throughout this course, any time we need to work on a Node.js based application. So let's take a pause right here again, and we're going to start actually working on the implementation of our project in the next video.
