# Just...One...More...Fix...

In the last video we put together the entire length list class. If you sat through that implementation, I am very impressed. I also apologize for how nasty that was. But hey, it's all done. If you skipped over that video and hopefully you did make sure you copy, paste the code out of the last lecture and throw it into a linked lists file inside your RC directory. Now inside of here, I just want to point out, if you did not go through the implementation with me, we added in a length property that has a getter. We implemented a compare function and a swap function. So the length list has all the implementation required of our sortable interface. So we're now going to create a linked list and then use our sorter to actually sort it back inside of indexes at the very top. I'm going to import the linked list. Rumblings list. I'm then going to comment out the character collection stuff just so we don't have a bunch of different console logs. And then right here, I'll create a new link list. I then need to add in a couple of elements to it. So I'll do a linked list ad and these are the different numbers we're going to add into the list. So I'll put in some stuff like how about 500? Then how about negative ten? Negative three. Why not? And we'll just do one more. Five or four, I guess. All right, so there it is. Now we can create our sorter. So we'll say sorter is going to be a new sorter and we're going to pass in our collection that satisfies the Sortable interface, which is linked list. It's going to pass in a linked list and now we can call sorter, dot, sort, and then finally we can print out all the different values inside the length list with length list, dot print. And that's it. So I'm going to save the file once again, flip back over to my terminal. And sure enough, I'm going to see all the numbers from Link List printed up in the sorted order. Now that is pretty awesome. Again, I don't know how to sort a linked list. I've got no idea how to. Definitely not off the top of my head. But I think that at the end of the day, I could probably figure out how to compare two elements inside of a length list, and I can probably figure out at least a kind of cheating way to swap two elements inside of a length list as well. And so this entire idea of using the interface has saved me a ton of complexity inside of my application. And it means that I don't have to go install some like third party library to figure out how to sort a linked list or even really look at Stack Overflow, for that matter. All right, so this is awesome. But sorry, but I can't resist it. Sorry. Like one last little issue here that's just not quite so great. So what's the issue? Well, let's be honest here. The code we have is awesome. But there's one big problem. Every time that we want, we want to sort a collection. We have to create the collection. We then have to create a new thing called disorder, feed in the collection and then call sorter sort. I don't really recall seeing any programming language that quite as laborious as this is right here to sort a simple collection of records. What would be way more obvious for sorting than this and a lot more usable is if we could simply create some collection like let's say numbers collection right there, and then call numbers collection sort. That's way more obvious to other engineers. I think other engineers would understand like yeah, of course we call numbers collection sort. It is a method on the collection as opposed to us having to create that separate class. So what we have right now is good, but I don't think it's ideal. I think this is the kind of interaction we want right here. So like I said, just one more little. But here, let's take a quick pause. We're going to come back the next video and figure out how to refactor our sorter and make it available, kind of like as a method on these different collections. So I'll see you in just a minute.
