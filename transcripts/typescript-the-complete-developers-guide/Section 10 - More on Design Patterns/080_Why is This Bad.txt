# Why is This Bad?

In the last two videos. We went just a little bit off topic here, discussing some more round union types and how we can restore access to one set of properties in that union by using a type guard. Remember, type guards are essentially very intelligent snippets of code. TypeScript really understands what is going on here. TypeScript understands that if we get into this if statement, then this dot collection must be an array and we are going to restore access to all the properties of an array on this collection. All right, so this kind of concludes our bad code right here. We've now written out all the bad code we are going to write. Let me tell you why this stuff is bad. Well, the reason this is bad is the exact same reason we ran into on that last project we worked on. If we ever want to add in some additional things to be sorted here, we would have to go back over to our class sorter and add them into this union. So for example, if we also wanted to deal with sorting a linked list, we would have to say, okay, collection is going to be an array of numbers, a string, or it's going to be a linked list. And then inside this for loop right here, we would then have to add in a check to say, okay, is this collection an instance of linked list? Now, once again, we have not done any implementation on linked list. I'm just kind of using this as a temporary stand in. So then inside of here, we would put our logic to kind of sort or do some sorting stuff around a linked list. And as we started to add in more types of collections inside of our application, we're just going to have to come back to this sort function every time and add in some additional if statements. And as you might guess, that is not ideal at all. So again, this concludes the bad code. The reason it is bad is that if we ever want to add support for sorting some other type of data collection, we would have to come back to our sorter and start to really change a bunch of data in here. If we eventually end up with like 50 different types of things that were supposed to be sortable, well, then all of a sudden we're going to have 50 nested if statements inside of here all essentially maybe doing like the exact same thing. So clearly this solution is not going to quite work out. So let's take a pause right here. We're going to come back in the next video and discuss how we're going to fix this with good code. So this is going to start like the series of factors that we're going to go through to really solve this issue in the correct way. So quick pause and I'll see you in just a minute.
