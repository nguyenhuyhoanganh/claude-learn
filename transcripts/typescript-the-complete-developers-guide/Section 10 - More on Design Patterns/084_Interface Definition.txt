# Interface Definition

All right. Definitely had enough lecture inside this series of videos, so let's just start writing code right away. The first thing we need to do is define our interface inside the sorter. So inside of sorts at the very top, I'm going to define a new interface called Sortable. Now the name of this interface, maybe it's not the best, but the idea here is that I'm trying to say you can be sorted if you implement these different properties. So in that context I think of Sortable, it kind of makes a little bit of sense. So then instead of here, we're going to list out all the different properties. You must have to be used with our sorter. So in order to be used with our sorter, you have to define a length property that is a number. You have to define a compare function. That takes two indices or two numbers as arguments so we can reflect those as like left index. That's a number and. Right. Index. That is a number and that needs to return a boolean. That's going to tell us whether or not those two elements need to be swapped. And then finally, we'll define the actual swapping function, which again is going to take left index. That is a number and right index that is a number and that's not going to have to return anything. So we'll annotate it as void like so. So now when we create an instance of sorter, we no longer need to say that you have to pass in a numbers collection. Instead, you can pass in any object, anything whatsoever that has these three properties. So I'm going to update the type annotation right there to sortable. And I no longer need that import statement at the top like so. So this is it. This is the final form of our sorter. Well, at least for now, we are going we are going to refactor some other stuff in the future. But for right now, this is like the good version right here. This is like really, really good stuff right here. So again, we have directions at the top on how you can be an argument. As long as you have these properties, I'm going to sort you no problem. So if you now go back over to indexes, you'll notice that we can still pass in numbers collection without any sort of error to our sorter. And that's because our numbers collection correctly satisfies the sortable interface. Our numbers collection has a length property. Remember, we define this as a jitter. And so to actually access this thing, we can just write out like dot length without having to put on this set of parentheses. So if we define a jitter to reflect that inside of an interface, we just pretend as though it actually is a property. Numbers collection also has our compare method that takes two numbers and returns. A Boolean and Numbers collection also has a swap method that takes two numbers and doesn't return anything. One very important thing I want to point out here. You'll notice that my compare method I gave argument names of left index and right index, and when I did the actual implementation of numbers collection. I gave them the same names. The argument names between an interface and the actual implementation do not have to line up. So in other words, this argument right here does not have to be called left index. I can call it a PDF if I want to. So that name of the argument right there does not have to be identical to the actual implementation. They don't have to match up. The only reason that the interface arguments have names at all is to give them a little bit of context and help other engineers read this argument name and have some vague idea of what its meaning is. So by putting in left index, I think other engineers could look at this and understand, okay, this is going to be a number that is supposed to be an index and like something on the left hand side. Well, okay, I know that we're using bubble sword. Maybe it's supposed to be the left hand index of some pair of elements or something like that. So again, the only reason we use any names here inside the interface definition is just to provide context of what their purpose is. All right, so let's save everything one more time, and then I'm going to flip on over to my terminal. Do one last quick test. Oops. Let me just change one of these numbers one more time. I'm going to do like 50 right there. I'll save this again, flip back over and I've got my sorted stuff right there. And there's the 50 on the far right hand side. Okay. So we've now refactored, we've got the interface in place. This means that we can now start to define other types of collections of data inside of our application. And as long as they satisfy these properties right here, we can use them with their sorter and they will be properly sorted. So let's take a quick pause right here. When we come back, in the next video, we're going to start to implement our characters collection. That's going to represent a string. So this right here, cactus collection, is supposed to be a string of characters and we're going to implement swap compare in length inside there and then use that class along with our sorter to actually sort our string. So quick pause and I'll see you in just a minute.
