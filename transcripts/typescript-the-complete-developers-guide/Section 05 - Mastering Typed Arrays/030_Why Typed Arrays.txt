# Why Typed Arrays?

Now that we've seen a couple of examples of res, I want to walk through some kind of corner cases around them. So essentially I want to discuss why we care about these arrays. Like what so big a deal about arrays in TypeScript that we're making this into a separate topic. Well, here's why. When we work with the RES in TypeScript, we got a couple of different advantages in a couple of different downsides as well. So first off, TypeScript can help us when we make a typed array by helping us do type inference whenever we are pulling a value out of an array. Let's see a quick example of that. So back over inside my code editor. I'm going to put a little comment down right here and I'll say help with inference. When extracting values. So in other words, if we look at our carmakers array right here, we know that it is an array of strings. TypeScript knows that as well through type inference. So that means that if we ever pull an element out of this array, either by using, say, the pop method or by direct index access, TypeScript will know that we are going to be pulling out a string and help us with type inference with the new variable that we're assigning this this value to. So let's look take a look at an example of that. Let's say that I'm making a new variable right here and I'm going to call it something like car. If I then take car makers and do an index access to it, I can now hover over car and you'll see that type inference has come into play and TypeScript knows that this variable is going to be a string. It knows this because it knows that car makers is an array of strings and we just pulled one value out of it. Likewise, this also works if we try a pop method. So let's say we've got my car. We do like carmakers pop like so to remove the last element in the array. Once again, TypeScript knows that carmakers has an array of strings. So when we call pop, it's going to return an instance of a string so we can once again hover over my car and we see. Yup, TypeScript has correctly inferred that my car is going to be a string. All right. So that's one benefit of making use of arrays in TypeScript. Secondly, TypeScript can help us prevent or help prevent us from adding in incompatible values into an array if we have correctly type the array. So for example, back over here once again, we can say prevent incompatible. Values. So once again, if we take our carmakers array right here, that only contains strings and we try to add in some element that is not a string like, let's say a number, we'll very quickly see an error message type script is telling us here, hey, you're trying to put in a number to a collection that is only supposed to contain strings. So that's going to help us from putting in dissimilar elements into one single array. And once again, if we want to have multiple different types in an array, we definitely can do that. But we have to use some special syntax to do so. We'll take a look at an example of that in just a moment. All right, third. Any time we declare an array, we get a lot of help with built in functions like map for each reduce and so on whenever we are iterating over a collection of elements. So for example, if we flip back over once again. We get help with map or for each and reduce. So for example, if we do car maker's map, we can add in a function right here that will be called with every element in the array. So I'm going to receive each car as a string and from this I'm going to return, say a string as well. We'll just do a straight pass through here. So I'll take the car and return it immediately. So TypeScript is going to make sure that the value that we are putting into this function right here will be a string. And so because of that, because TypeScript knows that car is going to be a string, we get access to a lot of different autocomplete on the variable that comes in here. So for example, if I put in a dot right after car, I can then see the autocomplete appear with all the different methods that belong to strings. So for example, I can do a to upper. Case like so. So any time we start working with MAP for each reduce or any similar helpers, TypeScript is going to give us autocomplete on the variable being passed into this function right here, which is a nice little benefit. All right. Now, the last item we're going to talk about right here is that we can, in fact, put different types of elements inside of an array. Let's take a quick pause, though, because there's a couple of different corner cases around this and some interesting syntax we need to understand. At the same time. It's a quick pause and we'll see you in just a minute.
