# Wrapping Methods with Descriptors

In this video, we're going to use our property descriptor to somehow wrap our pilot function and catch any error that it throws. All right. So I'm going to delete the two console logs inside of here. And then first off, I'm going to pull a reference to the existing pilot method like this actual function right there with that code inside of it. So I'm going to say const method is a property descriptor. So DSC value. So now method is going to be a reference to that exact function right there. And now I'm going to go back onto my property descriptor and I'm going to try to update its value. So I'm going to say desk value is going to be some new function that we're going to define right here. So inside of here, we're going to put our custom logic to try to run method. And if it results in an error, we'll catch that error and do some kind of like nice console.log instead. So I'll say try inside of the try statement. I'll call the method. And then if anything goes wrong, I'll catch that air and I'll just do a nice console log and I'll say something like, Oops, boat was sunk and I'm just doing that message because remember we are working with class boat here. All right, so let's try saving this and running the file again. So I'm going to run at Node Decorators again. I've got to actually create an instance of boat and call it. Let's make sure we do that at the bottom. So at the very bottom, I'll make a new boat and call pilot. I'll save the file. Flip back over once again. And there we go. So now we've intercepted that call to the original. Pilot method right here, and we wrapped it inside of our own custom function. So now, in theory, if we ever expect anything to go wrong inside of some given method of a class, we could just throw on the log air decorator and hey, we're going to automatically catch any errors inside there and log them out appropriately. So this is just one example of how we could somehow get at a function and wrap it with some additional functionality. So let's take a quick pause right here and continue discussing decorators in the next video.
