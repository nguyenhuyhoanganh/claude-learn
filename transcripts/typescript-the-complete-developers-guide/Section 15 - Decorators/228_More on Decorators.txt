# More on Decorators

At this point, we've seen decorators in use around methods and around properties on a class as well. So in this video, I want to show you a couple other locations where we can easily make use of decorators too. So the first one would be an assessor. I can very easily reuse the test decorator on this assessor right here, so I could say right above it at test decorator. And then maybe we will reformat our test decorator right here. I'm going to change it back to being a console log of just the key. And if I now save this file. I will flip back over to my terminal, rerun that thing, and now I'll see the key of color and the key of formatted color as well. So we can easily use a decorator on a property inside of a class in assessor and a method as well. There are a couple other locations where you can use decorators too. Now, I'm not going to really show you a very clear examples right now of why you would want to use these other types of decorators for right now. I just want you to know that they exist because you're going to sometimes see the syntax in the wild and the syntax is going to look kind of crazy. So the first additional location that we can use a decorator in is on a proper and a argument or a parameter to a method. So let's say that our pilot method right here has some argument to it that we'll call like speed, and that's a string. And then we'll say maybe inside this method if speed equals fast. Then we'll do a console.log of swish. Else will do a console log of I don't know nothing because the boat's not moving. So if we wanted to, we could actually apply a decorator directly to that argument. I'm going to first write out the decorator definition and we'll figure out how to apply it to that argument directly. So I'm going to write out a new decorator function underneath the class. I'm going to simply call it parameter decorator. You're going to very frequently see in the documentation that they refer to the arguments to a method as a parameter rather than an argument. And that's why I'm calling this parameter decorator. So now the first argument to this decorator, when we apply it to that argument right there of speed would be a target, which again would be our class. We can give it a type annotation of any. The actual type here is going to be the prototype of class boat. So we could leave this as any. Alternatively, we could refer to it simply as boat like. So now referring to it as boat is actually a little bit disingenuous. And the reason for that is that we are receiving the first argument as the prototype, not actually an instance of class boat, which is what this actually implies. So you can say either boat right there or just leave it as any again, for right now, we're going to go with any. Then the second argument we're going to refer to as key once again, that's going to be the name of the method, which in this case is pilot. And then finally, the last argument is going to be an index. That is a number. The index is the actual index of the argument that we are applying this decorator to. So now inside of here, I'm just going to do a simple console log of key and index. All right. Now, here's the weird part. Here's how we make use of this decorator when we want to apply it to an argument. Right in front of the argument we'll write out at parameter decorator like. So we then put in a little space and then the actual argument itself and as usual, the type annotation on the method or the argument. Okay. So now we can save this. Flip back over once again and we'll see that we get a console log of the name of the method that these argument is in and the index of that argument, which in this case is zero. So if we had multiple arguments going to pilot right here, they would have incremented indices. So, for example, let's say that we have another argument called like. I don't know. Generate wake because that's what boats do and that'll be a boolean. And now we can apply that same decorator here as well. So I'll say at. Parameter decorator. Oh, a little typo there. Let me fix that up. There we go. Now I can say this once again, you're going to notice that my code formatter very quickly kicks in here. As soon as you start using parameter decorators, you're going to end up with some really long lines. And so inevitably you're going to have to reformat them to have them kind of fit nicely on the screen. All right. So I'll run this again and now we'll see. Pilot one and Pilot zero. So Pilot Zero is the first parameter decorator that we applied to the first argument, and then pilot one is the second. That would be this one or right there. Now you might be kind of sitting here saying, okay, when in the world would we ever, ever use this? Well, there's some really advanced scenarios where you might want to have some idea of the different arguments that are coming into a function. Once again, I'm not going to show you any examples right now. I just want you to know that this type of decorator exists. That's it for right now. Okay. Now, just one other decorator I want to show you really quickly. This is going to be a decorator that we apply to a class definition. So we're going to apply a decorator to class boat itself as opposed to some thing inside of the class. All right. So I'm going to first write out a decorator this time around. I'm going to call it constructor. Actually, let's just call it class decorator. So when we apply a decorator to a class, the only argument to it is going to be the constructor function of that class. So we can either annotate this as a type function or alternatively, we can say type of boat, which is a reference to the constructor function of that class. So then inside of here we could do a console log of that constructor function. And now to actually apply this decorator to the class, we would go back up to the top and right above it, say at and then we'd reference class decorator. So now I'll save that. Once again, I'm going to flip back over, run that file, and I'll now see a console.log of that constructor function that's called the boat function. All right, once again. Yeah, I know. When would you use this? Well, same answer. There's some really advanced scenarios where you might want to have some ability to look at a class decorator. Although we are going to look at an example of using a class decorator in a little bit. And the reason that we're going to be using it is not to somehow mess around with a constructor function. Instead, we're going to kind of use a class decorator to coordinate a lot of decorators inside a class definition. And I know that sounds really weird right now, but hey, that's something we're going to see in just a little bit. Okay. So last thing I want to tell you here is that we have seen a decorator on a couple of different properties inside of a class, so an actual property and assessor and a method. And these are all different things tied to the class or more specifically, instances of the class. So we can actually apply decorators to static methods, static properties and static assessors as well. I'm not going to write out any examples of this because at this point it's just kind of going overboard and I'm showing you a bunch of syntax without any immediate use to it. But I just want you to know that we can use decorators on static methods, assessors and properties as well. So we're going to basically take a pause right here. We've now got some real solid bedrock to understand decorators with, but we don't still have a lot of very practical examples to understand where in the world we would ever want to use any of these things. So let's take a quick pause right here. We're going to continue in the next video and start to take all this decorator knowledge back over to the world of Express.
