# Property Descriptors

In this video, we're going to see a quick example of why decorators are sometimes really, really useful. So I want to change up the decorator we've got right here. I'm going to change the name of it, first off to Log Air. I'm then going to delete that sample code we had at the very bottom, and I'm going to once again apply this decorator to the pilot function. And then inside of pilot, I'm going to make sure that I always immediately throw an air. So I'll say throw new air like so. All right. So I bet you can guess what we want to do here. We want to somehow make sure that whenever we call pilot and we inevitably throw an error, we want to make sure that our decorator is going to somehow catch that error and report it to us at our console with some kind of like nice console log. So we need to figure out some way of essentially wrapping up this function right here and making sure that whenever we call it well, we're going to run some custom logic that we're going to find inside of our decorator. Now in doing this, we're going to have to make use of the third argument to our decorator function. So let's have a quick aside and discuss exactly what that third argument is. Remember I told you that the third argument was something called a property descriptor. So I'm going to add that third argument in. I'm going to call it DSC short for descriptor, and it's going to be a type property descriptor like so. Now you'll notice that we did not have to import that type or anything like that. The property descriptor type is globally available to us in TypeScript. So what exactly is a property descriptor? Well, this is an object that has some configuration options around a property defined on an object. It is not actually a part of TypeScript, it's a part of S5 JavaScript. So you can actually make use of property descriptors inside of Node or your browser right now without making use of IS 2015 or TypeScript or anything like that. So let me tell you a little bit more about property descriptors with a quick diagram. All right. So a property descriptor is essentially an object that is meant to configure a property on another object. Whenever we are working with a property descriptor for a method, specifically the property descriptor is going to have these following options. So first, it's going to have a writable flag that's going to be a boolean of either true or false that describes whether or not the given property can be changed. It's also going to have innumerable excuse me, that's a hard word, innumerable property that describes whether or not that property will be looped over in a four in loop. It has a value that's the current value of the property and it configurable flag as well that describes whether or not we can change the configuration of this property and whether or not the property can be actually deleted. Now I'm just basically using the word property like over and over and over here. So let's write out a little bit of code and get a quick example of what's really going on here. All right, I'm going to open up my JavaScript console. And inside of here, I'm going to define an object that I'll simply call car. And maybe this will have a make of Honda. And how about a year of 2000 like so? So now I can use the built in object or simply built in function object dot get own property descriptor. So again, this is built into iOS five. Don't need any use 2015, don't need BABBEL, don't need TypeScript or anything like that. Then as the first argument to this function will pass in the object car. And then as a second argument, we'll pass in the key that we want to get a property descriptor for. So in this case, let's start off with make. So I'll put in a string of make whoops, make like so and that will give me back the property descriptor object or the make property on the car object. So you'll see that it has a value that describes the current value of the make property. We've got configurable enumerable and writable is currently true, which means we can update that make property. Let's try to change this property descriptor and you're going to very quickly see some interesting behavior. So to change a property descriptor, we can use object dot. Define property. We'll pass in the object of car the property name which is make I keep on putting in Mark for some reason. And then finally the new configuration object or the new. Property definition right here. So the new property definition or make I want to just update this thing and say that writable is now false. So in that object I'll put in writable of false like so. So now it looks like the object car is still the same it was before. But now the big difference is that the make property is no longer rideable, which means that I essentially cannot update its value in any way. So if I put down car make. Why do I keep writing mark whatever and change that to how about Chevy? Well, it's going to look like that update went through. But if I print out car once again, you'll notice that it's still Honda because this make property is no longer rideable. So this whole property descriptor object is going to be the key way in which we modify different properties inside of our descriptors. Now you might think that we could just look at, say, Target at Key or something like that and change its value to be some new function. Well, unfortunately, no. Remember in the last section I showed you that underscore, underscore, decorate function and I told you that I trimmed out a lot of functionality. One of the pieces of functionality that I updated inside there was essentially after our decorator gets called TypeScript internally is going to try to apply a property descriptor back to the prototype. So unfortunately, we cannot just overwrite methods directly on the prototype. Instead, if we want to change them, we essentially have to go through this descriptor or this property descriptor thing and try to change or kind of wrap existing functionality or existing methods using that property descriptor. Okay. So let's take a quick pause right here now that we've got a better idea of how property descriptors work. We're going to try to use that thing in the next video to implement our actual log air. Decorator So quick pause and I'll see you in just a minute.
