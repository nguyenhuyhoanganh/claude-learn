# Decorator Factories

Now that we've got our decorator working, I want to make one little change to it. Right now, any time that we make use of this decorator, it's always going to console.log. Oops. Boat was sunk. Well, obviously, we're probably not going to always use this decorator only with a class called Boat, so it would be kind of nice to be able to customize that error message whenever we actually use the decorator. So in other words, it would be kind of nice to be able to pass in an error message right here if something like oops boat was sunk in ocean or something like that. As soon as I add in those prints and add in the string argument, however. Well, very quickly, see a nasty error message here. So how are we going to fix this up? Well, we can use something called a decorator factory. A decorator factory is essentially a decorator that returns a function. Really not the worst thing in the world. Just the syntax looks sometimes a little bit nasty. So here's what we're going to do. I'm going to find my log air decorator right now. I'm going to remove the log error name on it. And then I'm going to wrap that function with another one called Log Air. So I'm going to wrap the old log here like so, and I'm going to return the old log air just like that. And then I'm going to receive that potential ear message as an argument to the new log air. So I'll say error message is going to be a string. And now when we do our console log, I'm going to attempt a console log out error message instead. So what's going on here? Well, pretty much when we call log air right here, we're going to pass in some initial configuration argument to it that's going to run log air right here. And then Log Air is going to return this function right here that is going to be applied as the actual decorator. Of course, whenever we actually run this function, it's going to have a reference back to error message, which was our original argument back into the top level function. That's pretty much it. So any time we want to like configure a decorator, we're going to use a decorator factory, which is just a normal function that returns our decorator. That's it. So I'm going to now save this. I'm going to flip back over to my terminal. I'll run test no decorators again and now I see. Oops boat was sunk in ocean. And of course we could customize that as much as we want but can say, I don't know, something bad. Save the file. We run it, and we're good to go. All right. So we're going to most frequently see this kind of decorator pattern where decorators are being used as factories. Because very frequently, when we want to use a decorator, we probably want to customize it in some way. It's a little bit less frequent that we want to write a decorator that doesn't take any arguments at all. All right. So let's take another quick pause right here. Come back in the next video.
