# Decorators Around Properties

In this video, I want to show you one or two more things around decorators, specifically around our color property up here. So I'm going to define another decorator inside this file called Test Decorator for lack of a. Better term. And then inside of here, we're going to again have a target of any remember using that any annotation not super ideal but around decorators. We're just going to relax that requirement for just a moment just to make this a little bit easier and we'll do a key of string. And then inside of here. Well, let's try to just print out the target and the key. So I'll do a console log of target. And a console.log of key as well. And then going to apply that decorator to our property up here. Like so. And then finally, I'm going to go down to the bottom of the file. You'll recall that we created a new instance of boat down here and called the pilot method. We don't need that anymore because I just want to take a look at how that color property works with a decorator. Okay. So I'm now going to save the file. I'm going to flip back over to my terminal and I'm going to run that file again. And so back over here, I see the boat prototype and the key of color. So let's now try to print out the value of that color property. How would we do that? Well, if Target is like the object, I guess we could do something like Target at Key or something like that. Maybe that would make sense. Let's try this. I'll save this. I'll flip back over, run the file again and I get undefined. Huh? Okay, well, maybe something's not quite right. Let's take a more direct approach. Let's just do target dot color like so. All right, I'll save that. I'll flip back over, run again. And sure enough, I still get undefined. So what's going on here? Well, remember, the first argument to a decorator function is always going to be the prototype of this class. And remember that prototype inside a JavaScript generally only stores method definitions actual instance properties such as strings, numbers, booleans arrays and so on all get defined inside the constructor. And finally, remember, all these decorators are running when we define our class. So our decorator is never going to be able to access any instance properties on an instance ever unless we start to get like really, really, really, really clever. Essentially this very simple example right here. If we try to write out some very simple code like this, we cannot read the property of an instance of boat. It's just not available to us. So this is a huge limitation and it's just something I want you to be aware of. The only reason that we can use decorators on properties at all is more just for us to have the ability to know that, oh hey, like there's a color property and we know that because the key is going to be color. That's like pretty much it. That's the really only usage of a decorator around our properties inside of a class definition. So we can't try to like modify those properties. We can't try to like change them or try to wrap them with some function or anything like that. Well, technically we could wrap them with the function, but we're kind of getting in the weeds here. Essentially, all I really want you to understand is that when we are using decorators around a property definition inside of a class, we cannot get direct access to that property is the direct excuse me, the decorator is being executed before we ever create an instance, number one. And number two. The only argument that we get to our decorator. Is the actual prototype as opposed to like the actual value or anything like that. And we cannot use the prototype to look up a value on an instance or something, something like that. Okay. So just a little side thing I want to throw out there so that you are aware of this. All right. So let's take a quick pause right here. Come back the next section and continue with decorators.
