# Decorators in Typescript

In this video. We're going to continue with a quick aside on decorators, so let's get to it. First off, plain definition a decorator is a function that can be used to modify, change or otherwise mess around with different properties or methods inside of a class. Now these decorators that we're talking about around TypeScript are very different than JavaScript decorators. So if you've ever made use of JavaScript decorators, they are somewhat similar, but a lot of the rules are very different. So you should not be thinking that this is a topic that you already have mastery of. Chances are there might be a new thing or two that you need to learn. In TypeScript. We're only going to use decorators around classes, so you're never going to see a decorator kind of floating off by itself. It's always going to be very tightly coupled with some class definition. The key to really understanding decorators, in my opinion, is to understand the order in which decorators work. So back up here at the very top, I had told you that we can use decorators to modify different properties or methods around a class. We can use them to modify properties or methods or static methods or accessories or classes themselves as well. So you're going to see decorators used inside of a class in some very, very different locations to modify some different properties of a class or different parts of a class. So to really gain mastery and understanding of these decorators, you got to understand the order in operations around them. We're going to spend a lot of time to really clarify that point. And now finally decorators in TypeScript 100% experimental, which means that the things that I'm telling you right now might be changed at some future point in time, but hopefully it'll all be the same. Okay. So we're going to write out a little bit of code to understand how decorators work. To do this, we're going to open up our code editor for a previous project that we worked on. So I'm going to go back over to my terminal. I'm going to go back to my workspace directory, and you might remember that we created that folder called Features. So I'm going to go into that folder. I'm going to start up my code editor inside there. And then inside of this code editor, I'm going to create a new file called Decorators. It's. All right. So we're going to do our little side experiment with decorators inside of here and then take all this knowledge back over to our Express project. So inside of here, I'm going to create a new class. I'm going to also call it Boat, just like the project we just worked on. So I'll say Class Boat. I'm going to say that this thing has a color property that is a string. I'll also give it a pilot method that returns nothing. And if I call it, I'll do a console log of swish again. Why not? And then finally I'll add in a jitter here as well. So my jitter, I'm going to call formatted color. So any time that we reference this Gitter, we're going to return a string that's essentially just going to print out our color in a nicely formatted string. So I will return a template string. So don't forget the back ticks right there. And I'll put in here this boat. Color is this color like so? All right. So now inside of here, we have an instance of a property. We have an assessor, which in this case is a getter, and we have a method as well, in addition to the overall class definition. So we can create a decorator and apply it to every single one of these different things. So let's define our first decorator at the bottom of the file. So down here, I'm going to define a function that I will call test decorator. Now the arguments, this function are very important. The first argument is going to be something that we're going to always call target. And if right now I'm going to leave it's type as any. Next up, the second argument is going to be something called a key, and that's going to be a string. And then this function right now is not going to return anything. So I'll annotate it as void. So right now, let's just do a console.log of target. And a console.log. Of key. Now I know I've told you several times that we avoid the any annotation as much as possible, but this time around we're just going to ignore that rule for a little bit. So this function right here is our decorator, and now we need to apply the decorator inside of our class. So to apply the decorator, I'm going to find my pilot method and I'm going to write out at test decorator right above it. Like, so notice that I did not put on any parentheses or anything like that. I just wrote out Test Decorator. As soon as I add that in, we get an error around pilot. It says that experimental support around decorators is not here or full support is not here just yet. So if we want to use decorators, we have to enable the experimental decorators option. The Experimental Decorators option is set inside of our config file. So inside of this project, we've not yet created a config file. So I'm going to go back over to my code editor. I'm going to generate a config file, and then we were going to use that to enable support for decorators. So I'm going to go back over to my terminal. Inside of features. I'll do tsk. Dash, dash in it. Remember, that's how we create our config file. And then inside of my config file, I'm going to scroll down to the very bottom. And I'll find a section down there called Experimental Options. Now, if you do not see experimental options down here, that's totally fine. Chances are you're watching this when decorators have actually been added with full support, which is totally fine. But if you do see this section, then we're going to make sure that we uncomment the bottom two lines there, which are experimental decorators and emit decorator metadata as well. Now I'm going to get an ear right away. That's just because my commas inside this file are now kind of unbalanced. So I need to make sure that I go back up to the previous uncomment line and add a comma after it. And then I'm also going to make sure that I remove the comma on that very last line as well. Right after emit decorator metadata. So now I should be able to save this file. And then go back over to my decorators file and that error message should be gone. Okay. So this looks good. It's now the last thing I want to do really quickly is run this file and just see what happens. We still don't really know what a decorator is or what it's doing for us. Nonetheless, let's just run this file and see what console logs we get. All right. So I'm going to flip back over and I'm going to run that file with TZ node. So I'll do t node decorators. It's. All right. And there's my console.log. So I've got the target and the key. So let's take a quick pause right here. We'll come back in the next section and talk about what is going on with these things.
