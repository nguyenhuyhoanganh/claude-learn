# Details on Decorators

In the last video, we wrote out a little bit of sample code that included a decorator and then we executed it. We've now got the output right here. We'll come back to that output in just a moment. But first, I want to show you a quick diagram to explain to you a little bit more about decorators. All right. Here we go. All right. So these notes right here are applicable to when we use a decorator on a property method or an assessor. Just to be clear, inside of our class, we have written out one of each of those. So we have a property, we have an assessor, and we have a method as well. So we can use this decorator on each of these three different things. All right. Back over to those notes. It's the decorator function itself that is the test decorator function right here is going to be called with a couple of different arguments. The first argument is going to be the prototype of the object. So in this case, we're talking about the prototype of class boat. Remember, we saw just a moment ago in that last review on JavaScript prototypes, whenever we create a class, it also creates a boat constructor function and defines a prototype on that constructor function as well. So the first argument to our decorator function right here is going to be the prototype of class boat. So that means that it's going to be an object with all the different functions that we defined inside of class boat. We can actually see that in the output right here. You can see that the target is something called boat. In this case, it is the prototype of boat. So it is listing out only the different functions and accessories that we defined on there. So you'll notice that our property definition doesn't exist inside of here because a property definition gets moved to the constructor function. And like I said, we saw that just a moment ago when we put our class boat into that TypeScript playground. Now the second argument is going to be the key of the property method or accessory that we applied our decorator to. So in this case, we applied our decorator to the pilot method. So we're essentially saying, well, whatever the name is of the thing, we're applying the decorator to that name right there, like pilot will be the second argument to the decorator as a string. So if we move test decorator to get formatted color, the key would now be formatted color. And as you might guess, if we moved it to color, the key would then be color. All right. I'm going to move the decorator back to pilot. So when we ran this file, we had the decorator on pilot, and that's why we saw it inside of our output. The Kiev pilot. Now there is technically a third argument on our decorator function as well. We didn't actually receive that third argument inside of our decorator. So remember, we only receive the first two arguments. We just ignored that third one. But if we had to fine that third argument, it would be an object called the property descriptor. We'll talk more about what a property descriptor is later on. All right. Now here's the most important note by far around decorators super key to understand right here. That decorator is only applied. In other words, the decorator only gets executed one single time, and that is when we first define our class. So in other words, when TypeScript first takes a look, or I should say when JavaScript first takes a look at this code, after we've piled it into JavaScript, we define class boat. At some point in time when we define class boat, this decorator gets executed. The decorator does not get executed when we create an instance of the class. And we can actually I can very easily prove that to you. Take a look at this file. At no point in time inside this file did we actually make an instance of class boat. Nonetheless, our decorator definitely ran whenever we ran the file itself. So again, a decorator only gets executed one single time when we define the class. Now decorators at the end of the day are like way more simple than you would ever really guess if you kind of look at the code that is implementing them. Let me show you what I mean. I'm going to take this class definition and the entire file really that we just put together. I'm going to copy the entire thing and I'm going to go and paste it into that TypeScript playground tool that we were using just a moment ago. Okay. So here's the playground. I'm going to paste in that class definition and the test decorator on the left hand side, and then we'll see the code that we get on the right hand side up here. So notice that as soon as we use a decorator on the left hand side in our output, we get in a added function definition right here called underscore, underscore, decorate. This is a function that gets automatically added into our code by TypeScript to actually implement all this decorator stuff. If I remove the decorator. Then that function goes away. So that underscore underscore decorate function only exist to actually implement this decorator stuff. Let's try to find out when that function actually gets used. If I scroll down just a little bit, we can see that function being used right here. So right above, we've got our pilot method definition. So we're still adding on the pilot method to the prototype. And then immediately after we add that method to the prototype, we call underscore, underscore, decorate. The first argument is an array of all the decorators that we're trying to apply to that method. The second is the boat prototype, and then the third is the key of the property that we added the decorator to. In this case, it's simply pilot. And then we're not going to worry about that last argument on there too much. Again, that's related to the property descriptor that we'll talk about later on. So I want to show you exactly what this code right here does. In other words, what does decorate really do for us? Well, I took this decorate function right here. I copied it out to a separate file. And then I essentially removed all the extra fluff. So here's the real decorating function I went through here. I removed a ton of extra stuff, kind of like air checking stuff, some stuff to handle when we pass in a reference to something besides the method, I removed all that and I just reduced it down to the absolute bare minimum. And here's essentially what the decorate function does. First off, it gets a reference to the property descriptor on the object. Remember, we're not going to really worry about property descriptors right now. And then all it does is loop through all the different decorators in that array, and it calls each one with the prototype, with a key and with the property descriptor. That's it. Nothing else. So all this decorator stuff is really just kind of like syntactic sugar. That's it. At the end of the day, we don't need to have any fancy definition of of decorators on here. We could delete that and we could try to just run this thing manually. To do so, I would call test decorator. I would pass in the prototype of boat. And I would pass in the key on that object or on that class I care about, which in this case is. Violet like so. If I now save this and flip back over to my terminal, run this again, you're going to see the exact same output. So I can't overstate this enough. All this decorator stuff is really just fancy syntax and pretty much nothing more. So all we're doing here over and over again is passing in the prototype in the name of that key on the prototype that we care about. The real question is why do we care? Like, who cares that we can call a function really easily with the prototype in that key? And that's the real question. Understanding the mechanics here, not so bad. The real question is why do we care about using decorators at all? So let's take a quick pause right here. We're going to come back the next video and we'll talk about possible things that we could do with a decorator. So quick pause and I'll see you in just a minute.
