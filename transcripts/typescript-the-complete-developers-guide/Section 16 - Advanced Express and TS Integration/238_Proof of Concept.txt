# Proof of Concept

Now that we've set up our controller decorator, the last thing we have to do is take this route configuration information and associate it with some router. So for right now, we're just going to create a new router directly inside this file and then associate all these different routes with that router. We're going to eventually come up with a better solution than this, but this will work for right now because to be honest, right now we're just kind of trying to get things actually working. Okay. So at the top, I'm going to import Express from Express. I'll then define and export a new router. So I'll say export const router is expressed router. Notice how ROOTER has a capital R. And then inside of our for loop, after we pull off our handler and our path, we're going to associate that root handler with our router. Now, the one thing to keep in mind is that all the different methods or keys that we iterate over on the target prototype might not actually be a root handler, so it might not have some path information to find. So we need to make sure that we only attempt to associate a route handler with the router if we have successfully found a path property. So let's say if we have a path property, then let's go ahead and do that association. So I'll say router Doggett We need to make sure that as the path that we pass in here, we provide both the root prefix and the path as well. So I'm going to put in here route prefix. And then we can either join these two strings with just simple string concatenation, or if we want to be fancy, we can use a template string instead. Let's just use a template string. So I'll put down my back ticks. I'll put down root prefix. And then right after it. Path. And then as the second argument, we'll put in our route handler. Like so. Okay. So that looks good. So now I'm going to go back over to login controller. We need to do just a little bit of housekeeping inside there. So back inside of log in controller, I left on the slash author right here. So for this first example, I don't really want to have any prefix whatsoever actually. You know what, let's leave the prefix on what am I saying? We can actually test this thing in whole, so to speak. So I'll leave on that prefix. So just note that when we actually attempt to navigate to this route, we're going to have to go to slash slash login. Now you'll notice we have an error around controller. That's because we need to import that decorator into this file. So I'll say import controller from decorators. Controller. And then one other thing. I've got a tiny typo inside this file. When I copied in that git login method, I've neglected to actually remove the semicolon and parentheses. Down here at the bottom. So I'm going to clean those up. And now this file should be good to go. Okay. Now, there's just one little thing we need to be aware of. Right now, our controller decorator file is exporting a router. We need to make sure that we hook up that router to our express application. The other thing we need to be aware of is that right now our logging controller is not exporting anything whatsoever. However, we still need to make sure that we import this file somewhere inside of our project, because right now it's just kind of like a single file hanging by itself. And nothing else inside of our application actually imports this thing, which means that if we try to execute this right now, the log and controller file would never actually be executed. So to fix both those problems, I'm going to flip back over to indexes. I'm going to add in an import statement. Four controllers. Log in controller. So that's going to fix the second issue. Now, that's going to make sure that we actually include and import the code inside that file. And then secondly, we need to make sure that we import that router that we just created inside the controller decorator file. So we need to import that router right there and associate it with our app. We've already got an import statement right here for router from our login routes file. So to avoid any naming conflict, we're going to do a little bit of a rename on the fly. So I'll say import router as how about controller router? So that's going to rename router on the fly so we can associate or simply access controller router anywhere inside this file now. And that's going to refer to the router that we're going to get from the controller decorator file. So I'll go to controllers decorators controller. It's. And then finally we need to make sure that we do an apt use. Controller controller router. Okay. So that should be it. Now. I'm going to save this file. I'm going to flip back over to my terminal and just make sure that I don't have any errors. It looks like I'm all good to go right now, so that means I can test this out by going back over to my browser. I'm going to find localhost 3000. And I'll try going first. Let's just do the root route. Make sure everything still works. Yep, looks good. So now we'll go to auth, log in and hey, I see my form. Awesome. So at this point, we've now got some like early proof of concept that this idea is going to work out. But there's definitely a couple of rough edges inside of our application right now. Let me point out a couple of them in the next video. So quick pause and I'll see you in just a minute.
