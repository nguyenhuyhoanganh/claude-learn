# Closed Method Sets with Enums

In the last video, we made sure that every different route handler that gets associated with our get put, post delete and patch decorators is going to have a method metadata property assigned to it. So now for every route handler, we know the path that we want to register against in the method as well. So now we need to go back over to Controller TS inside of here. At present, every single handler we set up is always going to have a method of git, which is definitely not what we want. So now we can pull off that method piece of metadata and associate the correct method with each of our different route handlers. So to get started right after path, we're going to pull off the method that we just stored on metadata as well. So I'm going to pull off method. This is going to come from Target prototype and we're going to look up the key on there. All right. So now in theory, method is going to be get post put and so on. So now to make sure that we associate the correct method here, we can do a simple lookup on the router using this method string so we can delete the dot and get and then replace it with square brackets. Look up that method and then call the function that gets returned. Just one issue. We're getting an error around that code. So if I mouse over that, we're going to see a very familiar error message here. So it says that the return from router at method has any type because router has no index signature. So what does that mean? Well, essentially means that TypeScript is saying, look, I have no idea what method is going to be. So when we plug in router at method, we could be returning anything. We could be returning a string or a boolean or hopefully a function because we're trying to call it immediately. But even if it is a function, we don't know what arguments it takes. So TypeScript is just complaining here saying I have no idea what is going on with your code. So in the past we always fix this by adding in an index signature. But in this case that wouldn't quite be super ideal because remember, the router definition is provided to us by express and we do not change type definition files. Now we could create a kind of custom interface inside of our app router file and say that this router thing is going to match that interface. But there's actually a little bit more eloquent way to tackle this. So we're going to do a little side refactor here very quickly. And then when we come back to this file, we're going to update that code right there. And you're going to see how this little side refactor is going to work everything out perfectly. Okay. So let's go back over to Roots really quickly. Now, inside of here, I want you to notice how we've got a bunch of hardcoded strings in here, like git post, get patch and so on. And you might notice that, hey, wait a minute. One of those calls was completely incorrect. I set up Del right here, which is supposed to be for the delete method, but I called it root binder with git, so that means that I put in an incorrect string when I called root binder. That was just a totally innocent typo, but I bet you can imagine how whenever we have hardcoded strings like this that we might have to type out multiple times inside of a project. It's extremely easy to make typos exactly like that one. So it would be great if we had some way in TypeScript to kind of express all these different methods inside of some kind of like closed data structure. So a perfect thing and actually a perfect thing in TypeScript. I know I just said express. I meant to say TypeScript, perfect thing to use here would be to create an enum to represent all the different methods that we want to handle. Then we can do a much easier job of avoiding typos, just like the one you saw me make right here. So let's very quickly make an enum to represent all these different methods. When we do that, we're then going to very quickly see how we can get this error message to go away using that enum. All right, so inside of my decorators directory, I'm going to make a new file called Methods. I'm using a capital IM on this name because we are exporting an enum from this file. So inside if you are going to export enum methods. And then I'm going to define all the different methods I want to have access to. So it'd be get. We'll do a post. We'll do a patch. Adele, which we actually delete. And let's see a put I think that's the last one that we are worrying about right now. Okay. So we've now created an enum to express all these different methods. So now let's import this enum back into roots and use that for all these different root binder things right here. Okay. So at the top, I'm going to import. Methods from that methods file. And now in place of each of these hardcoded strings, I'm going to use that enum. So I'll say methods target. All due methods put. Methods. Post. Del. And patch like so. Okay. So now we are guaranteed to know that whatever string we put in as that method right there is going to be one of the values out of our enum. So now let's go back over to our controller file. And now here's going to be the magic. I'm going to import that enum at the top. So I'm going to import methods. From methods. So now once again when we pull out method using the get metadata. Excuse me, get metadata. All right. Here, we know that this string is going to be one of the values out of methods. So we can add on a manual annotation to method right here. Right now, if we mouse over it, we see that TypeScript thinks that method is any and that's because a git metadata call returns any type. So we could narrow down this type right here by saying typescript, hey, look, this variable method, it's going to be one of those values out of methods. So I'm going to put in a manual annotation here like so. And then as soon as I do, you'll notice that error goes away. So why in the world is that? Well, it's actually pretty simple. TypeScript now knows that the method that we put inside of here is only ever going to be one of those strings that we just define inside that enum. And I'm going to very quickly click through to our definition of what a router is like the type definition file for a router. So I'm going to do a couple of very quick clicks here just to show you the explanation. Okay. So here's all the different methods that a router is going to have. It's going to have git post, put, delete patch and so on. So TypeScript now understands that method right here is only ever going to be get post put and so on. And so it knows it can look at that enum. It can say, okay, out of all the possible values inside of here, what different methods can be returned from this lookup on router? And as soon as it does that, it's going to look up that appropriate string on the router definition and say okay, if that enum value is git, well okay. Yeah, we're definitely going to return a function. What if it's post? Oh yep. It's a function. How about put it's a function as well. And so we essentially have this closed set of possible values. And that's why that error message went away, because now TypeScript is 100% sure that no matter what lookup we do on there, we're always going to get back a function that is used for setting up our root handler. So like I've said a couple of times in this course, TypeScript is actually pretty darn smart. Okay. So that's a good improvement right here. So let's take another quick pause and I'll see you in just a minute.
