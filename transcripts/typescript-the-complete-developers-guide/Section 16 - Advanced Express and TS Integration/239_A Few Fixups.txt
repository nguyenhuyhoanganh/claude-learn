# A Few Fixups

In the last video we got our application working with this new kind of decorator based syntax. But we definitely have some really rough edges inside of our application right now. So in this video, I want to point out a couple of these different issues, and we're going to fix them all one by one. So first off, inside of our log in controller, we've got some unused imports at the top. That's a really quick fix. So I'm going to delete router and I'm going to delete next function up there as well. Next up, you'll notice that we've got two import statements for these two different decorators. I don't know about you, but I kind of dislike having to have a billion different import statements at the top of every file. So we could actually kind of condense those two imports down to one if we make use of an index file. Let me show you what I mean. This is kind of a throwback to is 2015 modules as well. So if we want to, we can optionally create an index file in any directory of our project and then use that as kind of like a central import point for everything inside that directory. So inside of Decorators, I'm going to create a new file called Index Dots. Then inside of here we're going to import and immediately export everything from controller and roots. So I'll say export star from controller. An export star from Roots. So now any time we want to get at our controller or the get decorators, all we have to do is import the decorators directory. If we import the decorators directory, TypeScript is going to automatically assume that we want to get the indexed file. Okay. So now back inside of log and controller, we can condense these to import statements. I'm going to delete both them and I'll condense it down to just get and controller from decorators like so. So that's much more compact than what we had before. Okay. So next big issue, I'm going to go over to my controllers file inside of here. We are creating a router directly inside of this controller file. Now, I don't know about you, but something kind of feels off about this. We're creating a router right here, and it's potentially going to be a kind of a little bit difficult to get access to this router in other places inside of our application without having to reach into some directory called like controller or simply decorators. So I think that this router right now is really not being defined at a appropriate location. So I want to try to extract this router definition into some separate file. And we're going to say that we only have one single router inside of our application. So we're going to essentially turn this router into something called a singleton, which means that there's only ever going to be exactly one router inside of our entire app. So to do so inside of my CRC directory, I'm going to condense a couple of directories here. Just you can see this more easily inside of CRC. I'm going to make a new file called App Router. So inside of here, we're going to define a singleton class that's going to wrap up the one single express router that's going to exist inside of our app. To make a singleton at the top. I'm going to first import Express from Express. Well then export a class called App Router. Now again, I use the term singleton and that means that we're only ever going to have exactly one copy of a router at a time. So to make sure that's the case, we're going to set up a kind of peculiar, peculiar little structure inside this class. So I'm going to say that there's going to be a private property that is static. Remember that static keywords means that we can access this property without having to create an instance of app router. The private keyword means that no nothing or no code outside of this class can ever access this property. I'm going to give the property a name of instance and a type of express router. Then I'm going to define a single static method on the class. I'll get instance. Remember, static once again means that we can access this method without creating an instance of app router. Any time that we access this method right here, we're going to return. Something of a type express router. So now inside of this method, we're going to check to see if we have an instance available. If we do, we're going to return it automatically right away. Otherwise, we're going to create an instance immediately and return that new instance we just created. So let's say if we do not have. An instance. Then we're going to create one. So I'll say app router instance is express router. And immediately after that we will do a return app router instance like so. So this right here is how we can very easily set up a singleton inside of TypeScript. So once again, the idea is that we want to only ever have one single router available inside of our application. So if we ever want to access that router, we're going to import app router and then use the git instance method. If it's the first time we are ever calling get instance, we're going to create a new router right away and assign it to instance. Otherwise we'll just return instance right away. Okay. So now we can use this app router class throughout our entire project and we'll know that we are only working with one single router. So let's now go back to our controllers decorators controllers file. Inside of here. We're going to make sure that rather than creating a router right here, we're going to use our one singular app router instead. So I'm going to delete that export statement. I'm going to delete the import for EXPRESS. And then I'm going to import. App router from up one directory up another directory app router. And then inside of here we can access our router inside of our return statement for that function right there and right before the for loop we can easily get our at our router by doing font's router is app router dot get instance like so. Okay. So now we're going to associate all of our different routes with that one single router. Now, one thing I want to mention here really quickly is that we could potentially use a jitter for getting this instance instead. That would be really easy. We would just have to put the git keyword right in front and then maybe give it a name of like instance or something like that that would conflict with that. Well, we could definitely do a rename if you want to use a Gitter here, go for it. I kind of like using a method in general just because I don't know. I'm not a huge fan of getters and setters personally, because they kind of hide the fact that I'm doing some or executing some logic and not just doing a direct assignment of some variable or retrieving some property or something like that. Now, I'm sure a lot of people would say, Hey, Stephen, that's the point. You don't want to know that that logic is occurring behind the scenes, but nonetheless, I don't know. It just scares me a little bit. So I personally don't really like to use accessories that much, but this would definitely be a very good time, a very good place to make use of an assessor. Okay. So now to fix everything up back inside of indexes, we are trying to import router as controller router. Well, that thing doesn't exist anymore. Now we only have our app router. So I'm going to remove that import statement and instead I'm going to import app router. From App Rooter. And then I'll associated that with my app. And specifically, I need to make sure that I get the instance out of there first. Okay. So that should be it. So let's save this really quickly. I'm going to flip back over, refresh the page, and yup, everything still works just fine. I'm going to also check my terminal and just make sure I don't have any errors. Nope, definitely don't. Okay, so this looks pretty good. Now, I know we're going through some of these fixes really quickly, but remember, I had said that if you're watching this, I'm kind of assuming that you've got a really good idea of how to work with Express and that you feel fairly confident with TypeScript. So in general, we're going to keep up the same speed for the rest of this express related stuff. All right. So let's take a quick pause right here now that we've done a whole bunch of fix up. Let's come back the next section and point out one last little awkward thing about our application. So I'll see you in just a minute.
