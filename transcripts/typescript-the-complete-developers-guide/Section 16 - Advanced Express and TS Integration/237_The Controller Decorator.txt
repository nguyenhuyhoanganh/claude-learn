# The Controller Decorator

In this video, we're going to put together the implementation for our controller decorator. Remember, the controller decorator is going to look at whatever class we are applying it to. It's going to iterate over all of the different properties of that class's prototype, and it's going to check to see if those methods have any metadata associated with them. If they do, it's going to take that metadata information and associate it with some express router. All right. So to get started, I'm going to create another file inside of my decorators directory. I'm going to call this one. Controller. Yes. Then inside of here, we're going to once again define and immediately export a function. So I'll say export function controller. This thing is going to take some route prefix. Remember the idea there was that this would be a prefix that could be applied to all the different routes that we define inside of here. So for example, if we want to make sure that all the different routes inside of here have a prefix of auth, and we put that in like. So then the end route for that handler right there should be slash auth slash login like so. Okay. So inside of Controller, I'm going to take in that first argument as something that I'll call root prefix. And it's going to be a string. And then I'm going to return. Our actual decorator function. And remember, this thing gets called eventually with some constructor function. Now we don't really know what constructor function we're going to be putting in here. We just know that it's going to be a function. So I'm going to give this thing a name of Target and I'll annotate it simply as function like. So. Remember, whenever we apply a decorator to a class itself, like the login controller right here. That's going to be our constructor function as opposed to usually when we apply a decorator to a method or a property, it's going to be the prototype instead. Okay. So now inside of here, we can write some code that looks very similar to what we just put together. So we'll say four let key in target dot prototype. Like so. So now for every key. We're going to look up Target prototype at Key. We're going to try to see if that's a method that has some amount of middleware. If it does, we're going to pull off that route property and associate it with some router. So we'll say. Target DOT prototype at key. I'm going to sign that to some little temporary variable here. I'm going to call it simply method. Actually, let's call it root handler instead. I think that makes a little bit more sense. We're then going to check to see if we have any metadata available on this root handler. So I'm going to say specifically, we want to look up that path property that we hopefully set in the other decorator we just put together. So the PATH property right there. So we'll say const path is going to be it reflect dot get metadata. We're going to look up the path metadata key on target dot prototype at key like so. Let's make sure that we also import reflect metadata inside of here. You'll notice that we don't get any error message when we use reflect right here. That's kind of a mistaken lack of error message right now TypeScript is detecting that we imported reflect into another file. If we ever decide to delete that other import statement, well, we will end up with an error over here. So in general, we kind of like to either import reflect at one central location into our project or alternatively just import it into every file that you need to make use of reflect. So I'm going to import reflect metadata at the top. And now we can essentially take all this information, both our handler and the path and associate it with some router. The only question is what router we don't actually have like a router inside this file. So we need to make sure that we import, express, create a router, associate all these different routes with it, and then eventually export it and associate it with our indexed DOJ's app. Remember, the app right here gets to be indexed. We're not writing JavaScript here, of course. So let's take a quick pause right here. We'll come back the next video and create that router inside of here. So I'll see you in just a minute.
