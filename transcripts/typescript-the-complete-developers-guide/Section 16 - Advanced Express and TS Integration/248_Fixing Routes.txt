# Fixing Routes

All right, my friends, in this video, we're going to pull apart the rest of the request handlers inside of login routes and make sure that we add all these different request handlers to the appropriate controller. Right now we've only got one controller called log and controller and something called login controller probably is not super appropriate for like this root route right here. So maybe we will also make a new controller as well. But to get started, I'm going to first move over that slash log out route right here. So I'm inside of logging routes. I'm going to find git slash log out. I'm going to cut that, move it over to log in controller. I'll go down to the bottom, I'll paste it right here and once again we'll go through that same reformatting process. So I'll call this get log out. I'll remove that fat arrow, remove the extra semicolon and parentheses, and I'll say that I want to access this root handler any time someone makes a request to slash, log out. Okay. That looks good. So now let's put together a new controller to handle our protected and route routes as well. So inside of my controllers directory, I'm going to make a new file called about route controller. Why not? Then inside, if you're I'm going to make a new class called route controller. I'm going to go back over to logging roots and I'm going to take these to last request handlers. We'll do them one at a time. So I'll take the first one. I'll cut it. I'll paste it. Once again, I'm going to format the name a little bit. So we'll do get root. I think that's reasonable. I'll remove the arrow on the end. And then remove the semicolon and Perin at the bottom. We need to make sure that we import the request object. Or the request type. From Express. And response. My bad. Both those. There we go. That's better. One other thing I want to mention is that our log in and log out links are now outdated. Notice how they are trying to go to just slash log out and slash log in when in reality we are now using that root prefix. So they're going to be slash auth and slash auth like so. Okay. So let's now import our decorators and start to fix this thing up. So I'm going to import. The gift decorator from decorators. And I'll apply it to get root so I'll do get slash like so. And then we need to do the same thing with controller as well. Now remember, when we apply these two paths together, the path prefix and the actual path right here for the individual root handler, we just join them together. So if we put forward slash in both them, we would end up with a route of slash slash which could have some bad side effects. So we could solve that by just calling controller with an empty string like so. Okay. So that looks good. Let's now move over our last handler. So back inside of login routes, I'm going to copy over or cut over the slash protected route. So at the bottom of route controller, I'll paste that. Once again, I'm going to go through that same refactor. So I'll say get protected. Delete the arrow, delete the semicolon and parentheses. We want to access this thing any time we make a get request to slash protected. And then remember, we also want to have a middleware on here as well. So back inside of login routes at the top, we had set up that require auth middleware. So I'm going to cut that. I'll paste it at the top of route controller. To wire up a middleware, we need to make sure that we import the use decorator from our decorators directory. We also have an air right here around next function. Let's make sure that we import that from express as well. And then finally we can wire up require auth using that used decorator. So say at use. Require us. All right. So this looks pretty good. I'm going to go back over to login routes. I think that we've pulled out everything we need to pull out of here. So I'm going to save this file and delete that roots directory because we're not using it for anything anymore. Now we need to wire up a couple of things back inside of indexes. First off, we're getting an air around importing router from login routes. Well, that makes sense because we just deleted that file, so I'm going to delete that. We no longer are going to wire up that router to our app object. I'll delete that as well. We have a new controller, so we need to make sure that we import that file just so the code inside of it actually gets executed. So I'll import controllers. Route controller. And I think that's just about it. Is there anything else? I don't think so. I think we need to do a test because if there is something else, I'm definitely forgetting about it. All right. So let's go back over to our terminal. I do not see any errors. That's great. So let's test out our application. All right. I'm going to go back to my root. Root? It looks good. You are not logged in. I'm going to attempt to log in. I'm now logged in awesome. And I should be able to go to slash protected and I can now to do a full test. Let's also make sure we can log out so I'll go to auth slash log out. I'm not logged in and if I now try to go to slash protected, I should see an error. Awesome. All right, my friends, I know this project has been a little bit intense, but that's pretty much it. So that's a demonstration of how we can wire together TypeScript and some other JavaScript library using classes, but also using some advanced features along the way. So let's do a quick pause right here and we'll do a quick wrap up in the next video very quickly. So I'll see you in just a minute.
