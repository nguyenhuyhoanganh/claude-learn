# Let's Refactor!

All right, my friends, time to get started working on our application now that we have a better idea of what's going on. So to get started, I'm going to first close that terminal tab where we were running a little bit of code out of our features project. I'm going to go back over to my server directory and just make sure that I'm still running my server. All right, so now I'll flip back over. And I've got a ton of code inside of my project right now between both login routes and the index file, where I wrote out a bunch of code and said, Do not write this stuff. So I'm going to very quickly do just a little bit of cleanup here inside my indexed file. I'm going to delete all that stuff that I had said, Do not write. I'm going to do the same thing inside of log and routes as well. So I have this entire do not write section. I'm going to delete all that stuff. Okay. So now I'm back to how I was before. If I flip back over to my terminal, I should still see my server successfully running. All right. So let's get start with this refactor. The first thing I want to do is create a new project or assuming a new directory inside of our SRC folder. So inside of SRC, I'm going to make a new folder called controllers. So here on forward, all the different route handlers that we put together, we're going to put into some classes that we're going to call controllers. And these different classes are essentially going to have some methods that just serve as request handlers inside of controllers. I'm going to make a new file called Login Controller. It's. And then finally inside of the controller's folder, I'm going to make one more folder. Called Decorators. Inside that decorators directory, you going to end up putting a ton of different files that are related to helping us with all these different decorators we've spoken about putting together those three or four different ones. So to get started inside of log in controller, I'm going to define a class called Log and Controller. And for right now, we're going to move over exactly one method from our log in roots file. Remember, at present, log in Roots has all the different handlers of our application. I want to move over just that get login route handler right here. So we're going to focus first on making this route handler work perfectly with this new kind of decorator structure. So I'm just going to take that entire router dot jet statement. I'm going to cut it. And then paste it into the login controller class and we're going to do a little bit of formatting to get this thing working as we would expect. Okay. So right away we see a ton of errors because well, we've got some really bad syntax here. So I'm going to define a new method inside of here called git log in. And then I'm going to essentially just delete that router, get slash login. So that everything kind of lines up like so and then just delete that arrow. And I think everything's going to be just about fine. I think I need to add on a return type annotation there. Avoid. And the other thing we need to do is make sure that we import request and response from express. Right now we don't see an error, but that's because requests in response are matching up with some global variable tied to the fetch API instead. So in order to fix up that error, I'm going to go back over to logging roots. I'm going to find the import statement at the top that has a bunch of imports from Express, and I'm just going to paste it directly into logging controller. And now that error message should go away. Okay. So this looks pretty good. Now, one other quick little piece of housekeeping inside of login routes. When I was writing out all that sample code, I had autocomplete pop up a couple of times and that autocomplete accidentally imported a couple of things here that are totally unnecessary, totally unused. These were just automatic imports from all that sample code I put together. So I'm going to delete those as well very quickly. Okay. So now we're going to focus on logging controller. So remember, we want to have some kind of decorator inside of here, something like at Git. And if we ever make a request to slash log in, then we want to somehow run this request handler right here. Once again, you're going to see that autocomplete added in an import statement. Freebie automatically. Kind of annoying. I'm going to delete that once again. And there we go. The other decorator that we want to have inside of here that we need to write is the decorator that's going to actually take all this routing information and merge it all together on some router. So I'm going to write in our kind of definition of that for right now or kind of a stub for it. So I'll say that I want to eventually have a controller. And once again, autocomplete is really thwarting my day. So I want to have a controller decorator and instead of here, we're going to put in some kind of like root root of sorts that's going to prefix all the different roots that we define inside this controller. Okay. So this is kind of what we have to work with. This is the code that we need to make actually work. So I'm going to save this file. We're going to take a quick pause right here. When we come back, the next video we're going to get started on defining this git decorator. So quick pause and I'll see you in just a minute.
