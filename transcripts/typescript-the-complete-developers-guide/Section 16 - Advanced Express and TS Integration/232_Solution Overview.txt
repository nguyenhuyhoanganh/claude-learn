# Solution Overview

Now that we understand the big issue in setting up all these different decorators to attach different handlers to our router, let's talk about how we're going to solve this. Before I show you the solution directly, I want you to know that the solution we're going to use is very, very similar to the solution that that TypeScript Express Decorators project uses as well. The way they solve it is just a little bit more, I don't know, well-formed, I suppose. But it's the product of some engineers who have had many months and possibly even years to work on the solution. So we're going to do something very, very similar in nature. We're going to use essentially the same toolset, but we're going to write some slightly different looking code just because we want to put this together much more quickly. Okay, so let's talk about what we're going to do. I'm going to show you a diagram that's going to lay out the entire process of how our application is going to run and somehow collect all these different pieces of information and then associate a given method with our router using this configuration. So here's the plan. So we're going to write out all of our code. We're going to eventually run it with Node.js. So Node is going to execute our code. And remember, when our class definition is read in and the class itself is defined, that's when all of our different decorators are executed. So we're going to have a bunch of different decorators like our post, our validate body and our use. Those are going to associate some root configure information with a given root handler by using something called metadata. We'll talk about what metadata is in just a moment. But right now, just understand that we're going to have each of these different decorators somehow store some configuration options in this kind of little metadata thing. So all of our different method decorators are going to be executed immediately with our class definition. So that means that like this one and this one and this one, all those are going to execute. And then the at get right there is going to execute as well. We are always guaranteed that the very last decorator to be executed around a class will be the class decorator itself. So in this case, our controller decorator. And that's going to be the key to all this stuff. So that controller class decorator is going to be the last one to run. So by the time that one runs, that means that all of our other little tiny decorators have been executed and provided some root configuration information. So inside of our class decorator, we're going to add in some code that reads out some metadata information that's been associated with each method. And that metadata information is going to contain all this different root configuration stuff, like the different method type to use, the different root names, the different properties to validate in the different middleware to use for each of our different class methods. So that we're going to read out all that information and we're then going to take that information and add the complete root definition in one single go to our router. And that's the key. So we're going to essentially collect all this root configuration information as the decorators run. We're then going to store that information somehow. And then at the very last step, when our very last decorator runs, which is always guaranteed to be this controller class decorator right there, that's when we're going to take all that root configuration information and associate with them with the router as a given root. Now, as you might guess, the real key to all this is this metadata thing. So I know we already did like one aside for prototypes. We did another side for decorators. Well, here's our last aside. We're going to discuss what metadata is. So what's metadata? All right. A couple of notes on metadata. So first off, this is a proposed feature to be added to JavaScript. Not necessarily TypeScript alone. So we're going to use a feature that is definitely experimental in nature. So who knows? Some of this stuff might break someday, but it's definitely been around for a while so far. Hopefully it's going to stay that way. Metadata is essentially a little snippet of information that can be tied to a method on a class A property, on a class or a class definition itself. Essentially, any kind of object we can associate a tiny little bit of extra information with. Metadata can be very frequently used for super custom stuff, like exactly what we are trying to do right now. However, TypeScript itself also has some pretty close integration with the metadata system. So for example, metadata, TypeScript optionally can export some information when we convert our TypeScript code to JavaScript. And this metadata information can describe some of the different types that exist inside of our application, inside of our JavaScript. Now that's really important to understand, remember. Normally when we convert TypeScript to JavaScript, all that type information inside of our TypeScript code gets 100% wiped away. But there are some scenarios where you want to kind of preserve some of that type information into your JavaScript code, and you can do that by using this metadata system. Now I one thing I want to mention here is that that is one very optional feature of TypeScript and this metadata stuff. So we can have TypeScript export some type information. We don't have to. And if we do or don't, it doesn't make a difference for if we want to use this metadata system on our own for our own custom stuff. All right. And then finally, to work with this metadata stuff, we're going to install and make use of a package called reflect metadata. So you can find this package on NPM, reflect ASH metadata. You want to you can look up some of the documentation here around it. But of course, I'll tell you just about everything you need to know as we go through one or two quick examples before we actually write out our code for the Express project. Okay. So now we have some vague idea of what metadata is. Let's take a quick pause right here. When we come back, the next section, we're going to write out some test code back inside of our features project directory. So you get a better understanding of what this metadata stuff really is. So a quick pause and I'll see you in just a minute.
