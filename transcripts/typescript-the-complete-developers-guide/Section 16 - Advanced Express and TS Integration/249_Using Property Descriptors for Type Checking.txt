# Using Property Descriptors for Type Checking

In the last video I had said that we were all done. Well, I did actually forget one tiny little thing, so this is entirely optional. I just want to throw this out there as one thing we could do to easily improve our decorators. So here's some interesting behavior inside of my log in controller. I'm going to define a new method called AD, and guess what ad is going to do? It's going to take a number and another number and return a number. So inside of here, I'll say A plus B like so. And now I'm going to add on this the interesting part, my decorator of agate, so I'm going to say at git slash like so. And I see no air whatsoever. Would I expect to see an air? Well, yeah, probably so. Think about what's going to happen with this code. Now we're essentially saying register this function right here as a request handler any time someone makes a request to slash off. So if we take in some request because that's what this argument is, can actually be a request in response, we're then going to try to add those two objects together, and that's pretty much it. Well, if we did that, our server would probably just hang because we are not sending back some kind of response to the browser. So even though our server would hang and not give a response, I think, well, the fact it's hanging is bad obviously. But beyond that, the bigger issue is that we see no air here whatsoever. So I want to figure out some way that we can somehow tell our decorator that it can be only used on a function that takes in a request and a response and then maybe like doesn't return anything. Probably ideally. So how can we do that? Well, let's go back over to our roots file from our decorators directory. So remember, this is where we set up like our git post put and all that kind of good stuff. So I want you to remember how on the inner function here, the innermost one, which is the actual decorator, we've been talking about that third argument, quite a bit of property descriptor. So property descriptor, remember, that's going to be an object that has a writable property, a configurable, a what else, enumerable? And finally, the most important one for us, a value property. And that value property is going to be a reference to some actual function, like the actual function that we applied our decorator to. And that's going to be the key for how we assert or put in some type checking with our decorator. So it's kind of hard for me to just explain what we're going to do here. It's a lot easier to just write out the code. So let's just do a little update here very quickly. At the top of this file, I'm going to define a new interface called Root Handler Descriptor, and this is going to extend the property descriptor interface. Now a property descriptor has all those different properties we just listed. So one of them is value. Value is actually an optional property. So if we want to extend this interface and add in some new definition of what value is going to be, we have to market as optional as well. So I'm going to say that the value property has to be a request handler as defined by the Express Library. So I'm going to import request handler. Rum Express at the top like so. And then finally, as a third argument, rather than saying D.C. is going to be a property descriptor, instead I'm going to say it's going to be a root handler descriptor. Like so. All right. So what does that do for us? Well, I want you to remember the entire point of an interface. An interface says if you want to qualify as a route handler descriptor, you have to satisfy all these given properties. So in this case, it's saying you if you have a value property, it is optional. So if you have a value property, it must be of type request handler. And request handler is a function that takes a request and a response and then returns anything in this case. So that means that if we're going to call this function right here, we can only pass in a something or essentially apply our decorator to a property that satisfies that interface. So it has to be a property that satisfies the request handler type. So if we now save this and flip back over to log in controller, you'll see that we're now getting an error. And if I mouse over that, it'll tell us why it says okay, we're trying to say type property descriptor of a that takes a number and B that takes a number and returns the number. Well, that function we provided is not assignable to a root handler descriptor. That's the interface we just put together. So if you scroll down a little bit, it'll give you a better description of what's going on here. So essentially we're saying, let's see, is there any very clear definition here? This is kind of like garble stuff, but you kind of get the idea we're trying to take in or provide a function that takes two numbers and returns a number, and we're trying to pass that off as though it were a request. HANDLER So after all this time, after all me saying, oh, yeah, we're going to do something with that property descriptor, well, that's how we can actually use it. We can use that property descriptor to limit how we actually use a decorator. So all we have to do is define a interface that extends property descriptor and then customize that value property. Okay, so that's the end for real. So I'm going to delete this. And we're all done. Okay. So let's take a quick pause and now a real wrap up in the next video.
