# The 'Get' Decorator

In this video. We're going to start working on this git decorator. The goal here is to somehow define some metadata on git log in. That's going to say that this method should be called any time we make a git request to log in. So to get started, I'm going to find that decorators directory that we just created. And inside there I'm going to make a new file called Roots. So inside of here, we're going to define all those different decorators like git post, delete, any other different method. We're all going to put them together inside of this file and then export them for use inside of the rest of our project. So inside of here, I'm going to define and export a function right away that I'm going to call. Simply get. So this is going to be a factory decorator. Remember, that means that we're creating a decorator that's going to return a function. So I'm going to take in an argument here that's going to be the path. So I'll say this is going to be a path that's going to be a string, and then we're going to return a function. So this function right here is going to be the actual decorator thing. It's going to be what gets called with our target, the key and that file descriptor or excuse me, the property descriptor as well. So we'll take in a target of any. Now I know I've really kind of laxed here quite a bit in saying, oh yeah, don't use a annotation of any, but we've been using it non-stop throughout all this decorator stuff. Don't worry, I'm going to very quickly tell you how we're going to kind of fix all this. Any stuff really soon. Then the second argument will be our key. And then the third is going to be our property descriptor, and we're not going to use the property descriptor right away, but we will in just a minute. So I'm going to add that in as an argument called DSC short for descriptor and it is of type property descriptor like so. And this line is really long. So I'm going to zoom out for just a second so you can see that whole statement. Okay. It looks good. So now we're going to do the same exact thing that we saw previously on the project with that metadata stuff that we just went over. So we want to define some metadata on the target at Key that says, hey, we're trying to associate some like path to be kind of wired up to our router at some point in time in the future. So inside of here, we need to make use of that metadata package. But remember, we have not installed metadata into our server project just yet. So let's very quickly change back over to our terminal. We're going to NPM install the reflect metadata package. So back inside of my terminal, I'm going to stop my server. And I'll do an NPM install reflect dash metadata. And then once it is complete, I'll start my server back up with NPM start. One other thing we need to check very quickly. Let's just make sure that we enabled decorators. Inside of our config file. So here's my test config file. Looks like we never turned on experimental support for decorators or emitting metadata. So I'm going to fix that up really quickly. I'm going to uncomment both those rules. I'm going to delete the comma after emit decorator metadata, and I'm going to make sure that I get a comma on the most previous property up here. Okay. So that looks good. So now we can go back over to Roots and we can start to put together our actual metadata stuff inside of here. So at the top, I will import reflect dash metadata. And now we can use metadata inside this inner function. So I'll say reflect. Dot define metadata. We're going to try to define a path. So I'm going to give this thing a metadata key of path. I think that will be clear enough to us in the future. We're going to give it a value of whatever path we just passed in. And then we need to define the object and the key on that object that we want to assign this metadata to. So our target is going to be target. That's the object we want to assign the metadata to. And specifically on that target object, we want to look at key. Like so. Okay. That's pretty much it. So now we can save this. We can go back over to login controller. At the top I'm going to import get from decorators slash routes like so. And now we should see the error message around that get decorator go away. All right. So it looks good. Let's take a quick pause right here and continue in the next video.
