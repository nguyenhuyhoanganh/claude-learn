# The 'Use' Decorator

In this video, we're going to start working on our next decorator. So inside of our log in controller, as a quick example, remember we had said that we wanted to have a decorator called Use and we should be able to call this thing to wire up some given middleware to just this request handler. One thing to be aware of is that we might want to have the ability to wire up multiple different middleware. So like one and two and three, like so. So we need to make sure we have the ability to support, calling, use multiple times, storing a reference to each of these middleware, and then eventually wiring them all up to this handler when we run our controller decorator. Now the general pattern for implementing use is going to be just about identical to what we've already gone through with the git post, put and delete and all those other decorators as well. We're going to store some SKUs, we're going to store some metadata on this request. Handler And then wired up to our router from the controller decorator. Okay, so let's get to it. Inside of my decorators folder, I'm going to create a new file called Use Dots. Then inside of here, I'm going to import reflect metadata at the top. We definitely need to set a new metadata key on some property here. So remember we created that enum just a second ago called metadata keys so that we don't have to type out manually any hardcoded strings that we can very easily make a typo on. So I'm going to assume that we're going to need to set a new metadata property to store all these different middleware for a given request. Handler So right now, I'm going to go ahead and just add in a new key to this thing of middleware, and that will be a key of or a hardcoded string of middleware like so. So now back inside of used yes I will also import that enum. So middleware keys me metadata keys. From. The metadata keys file. So now inside of here we can define and export a use function. So I'll export function use. This is going to be a factory decorator. Remember, a factory decorator is one that returns a function. We use factory decorators any time that we want our decorator to take an argument. So in this case, our argument is the middleware that we want to hook up to our request. Handler So I'm going to receive that as an argument called middleware. And I'm going to give it a type annotation of request. Handler Remember, that type is coming from the Express library itself, so I can make sure that I import that type as well. Okay. So then inside of here, we're going to return a function. This is the actual decorator that's going to be called with some target that is going to be the object's prototype. A key that is a string. And then remember our property descriptor that we call DSC? We're not making use of the property descriptor just yet, but we will in just a moment. So I'm going to continue to add in this argument. So property descriptor like. So I'm going to zoom out so you can see this entire line. There we go. Okay. So now the key thing to remember here is that the only difference between this middleware or assuming this decorator, I keep on mixing those terms. Unfortunately, the only difference between this decorator and the ones we've already put together is we want to have the ability to call this thing multiple times. So there's a really easy way that we can handle that. When we initially call the decorator and get into the actual decorator function, let's try to pull off the metadata middleware property off of whatever given property we're supposed to be looking at. You'll see why in just a second. So I'm going to say const middleware is reflect dot get metadata. On the key of metadata. He's got middleware. And then we want to look at that target object and specifically the key property. And I'm going to save this. So it just holds that line like so. And now I'm going to add on in our case after that function call of empty array. So that's a secret essentially, rather than storing a single middleware. We're going to look at this metadata key property of middleware. We're going to store an array there, and that array is going to store a bunch of different middleware. So every time that we try to add in a new middleware, we will look up that property. If it exists, we will assign it to middleware. Otherwise, if we've never stored any middleware as before, we will assign an empty array to middleware as. So then we can add in the middleware that was just passed in to our decorator. So I'll say middleware, push middleware. Notice the S on here. So middleware is going to be the array of middleware. So we just pulled out. So we're adding in that new one that was just pushed in or added to our decorator. And then finally we're just going to take that middleware array and assign it back to the metadata object. So we'll say reflect, define metadata. Metadata keys middleware. And then we're going to assign the value of middle wears. And the target and key like so and I'm going to save this so you can once again see the entire line is that's a pretty long line. If we want to, we can use a little bit of compacted syntax. So rather than manually pushing in that middleware like so we can delete that line. And optionally when we pass in a value to store with our metadata rather than referencing middleware is right there. We can say create a new array, take all the different values out of middleware and throw them in there and then add in the new middleware that we want to throw in. All right. Going to zoom back in and there we go. There's the finished product. Okay. So I think this looks pretty good. So now that we have stored all of our middleware and metadata, the last thing we have to do is go over to controller TS. When we run the controller decorator, we need to make sure that we pull out these middleware cars and wire them up to our request handler. So inside of controller TS Here's my controller file right here. I'm going to go down just a little bit. So we are currently pulling off path and method right here. I'm going to also pull off. Middleware. Yes. And that's going to be reflect get metadata. Metadata Keys middleware and I'm going to new the line this right away because we know this is going to be a really long line of code. So look at that middleware key. Get the target. And key like so. Now, remember, we might be trying to add in a request handler that has no metrics, I mean, no middleware whatsoever. So we need to also handle the case in which this might return undefined. So to deal with that, we can just do once again a simple or statement and say if we don't actually pull out an array of middleware right here, no problem. Well, let's just assign an empty array to middleware as. Okay so now we can take this middle wears away array and wired up to our router call right here now to wire up middleware. In general, this is a little bit of express knowledge. We just pass them in right before that root handler. And to do so we can use a little bit of that spread syntax because remember we are working with an array of middleware here. So I'm going to say. Dot. Dot, dot. Middle. Wears like so. And I'll save this. And once again, I'm going to zoom out because we've got some really long lines of code. And that should be it. All right. So let's take a quick pause right here. We don't quite have anything set up in our application just yet to actually test out this new use decorator. So let's take a quick pause. When we come back, the next video, we'll create a new, very small temporary middleware and attempt to wire it up to our request handler that we have inside of our login controller. So a quick pause and I'll see you in just a minute.
