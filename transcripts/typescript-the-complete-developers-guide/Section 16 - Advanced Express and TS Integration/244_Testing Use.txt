# Testing Use

In the last video, I made a tiny little typo inside of our controller file. So inside of here, remember, we are iterating over all the keys in Target prototype and then we eventually try to pull off the middleware metadata property. But the issue is that right here we are looking at just target. Remember the target inside of a controller decorator or a decorator that is being used directly on a class. The target is going to be the constructor function. So in this case, rather than referencing Target, we need to look at the constructor functions prototype because that's where our actual function is. So that right there should be targeted prototype like. So my mistake. I'll zoom out once again, just you can see that entire line. Okay. So now that we've got that fixed up, we need to actually try to test out this use decorator. So to get started, I'm going to first open up my decorators indexed file. Remember, this is kind of the central import statement or import source for all this different decorator stuff we are doing. So inside of here I'll do a export star from use like so. Then back inside of my login controller. I'm going to import that used decorator and I'm going to wire it up to the git login root handler. But before we do, let's create a little test middleware. So I'm going to define a little middleware right here that they'll call simply logger. This thing will be called with a request. A response and a next function, I'll make sure that I import next function from Express. I know sometimes it seems like I'm making up these different import types right here. Like next function. Honestly, the only way to get experience with these different types coming out of these JavaScript library type definition files is to work with them yourself. So it would probably not be too efficient if I sat down and said, Hey, whenever we're trying to define a middleware, make sure you annotate next as next function. Instead, you kind of have to take a look at that type definition, file yourself and understand what's going on inside there. And that's why earlier on inside this course, I made a big deal about going into those type definition files and exploring them a little bit. Okay. So inside of here, we'll do a simple console. Log of request was made. And then we'll call next to call our next middleware on the chain. So now finally, we can wire up this middleware to our request handler. So we'll say at use logger like so. And that's pretty much it. Well, hopefully. So let's say this file. I'm going to go back over to my terminal. Just make sure I don't have any errors. Yep. Looks good. I'll then go back over to my browser. I'm going to refresh the page. And then if I go back to my terminal, I should see a request was made. Yep. There it is. Okay, so this looks pretty good. If I now refresh every single time that I do a refresh, we should be calling that middleware. And so I should see request was made over and over again. All right. So this looks pretty darn good. I think that we've got middleware hooked up successfully using that used decorator. So I'm going to very quickly clean up what I just did inside this file because I do not want to leave behind this kind of useless middleware that we have right now. So I'm going to delete that middleware. I'm going to delete the at use statement and then remove use from that import list. All right, so this looks good. Let's take another quick pause right here and we'll come back in the next video.
