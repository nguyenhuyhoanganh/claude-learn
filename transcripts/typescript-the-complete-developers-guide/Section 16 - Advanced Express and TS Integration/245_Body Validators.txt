# Body Validators

In this video, we're going to start working on our very last decorator. So if I open up my login roots file, remember we went through all that discussion and I've referenced that discussion like 15 times now around making sure that our body coming to the post request for login had the email and password properties. So we want to put together another decorator that's going to essentially automatically check to make sure that these different properties are available inside of body. The general strategy excuse me, the general strategy here is going to be just about identical to the other decorators we have put together. We're going to create a new file inside of our decorators directory. We're going to call it something like Validate Body or something like that, and we'll define a decorator inside there that's going to attach some information to a request handler, and then we're going to wire all that stuff back up together inside of Controller. TS So let's get to it inside my decorators directory, I'll make a new file and honestly, I've been using different names for this so we could call this like body validator in another iteration. I call it required props, like required request properties or something like that. Right now I feel like body validator kind of makes a lot of sense, so I'll call this body validator. All right. So once again, the first thing I want to do is import reflect metadata. We know that we need to store some new information using the metadata system. That means we're going to have to create a new metadata key. So we'll add a new key to our metadata key's enum. Here it is right here. So I'll add in a new key to this thing that I'll call. How about, like, validator? Okay. Then back inside of body validator, I will import metadata keys from that file. And then we will define and export a function that's going to set up all these different metadata keys so we have to check for. So we'll export function. Body validator. This is definitely going to be a factory decorator because we're going to want to put in some list of strings to check for inside the request body. So because we do not know the number of arguments that this function is going to be called with, remember, we might call it with just like one property that we want to ensure is present or maybe many. I'm going to receive this argument as dot, dot, dot keys, and it'll be an array of strings. So now we can call that. Ads like, yeah, check for email and password and maybe like password confirmation or something like that as well. And this list of strings right here will show up in that key's variable as an array of strings. Okay. So then inside of here, we can return a function. Once again, this will be coupled with our target that's we're going to mark as any for right now. With our key. That will be a string and our property descriptor. Then inside of here, we'll do a reflect. Dot define metadata. We're going to define metadata. Keys. Dot validator. The data we're going to pass in here is going to be that array of keys. So that's what we want to check for. And then we're going to assign it to Target at Key. All right. So that looks good. Very similar to the other different decorators we've put together so far. So now I'll save this and we can flip back over to our controller file, which is where we have our controller decorator, and we're going to pull out this list of keys and make sure that they actually exist inside the request body. Okay. So in sight of controller ts. At the very top. Up here, I'm going to first make a function that's going to automate all the checking process for us. So our controller decorator is already getting pretty bloated right now. We're going to pull this thing apart in just a second. So if right now let's just define a separate function, it will actually check to make sure that all the keys, given keys are actually present inside that request body. So I'm going to define a function. Called Body Validators. This thing is going to take an array of strings. I'll call it keys, and eventually it's going to return. A single request handler or. So to actually check to make sure that the body has all these different keys, we're going to essentially just build a little middleware right here. So we're going to essentially return a middleware that will be hooked up to our request handler, along with all those other middleware. And so the middleware that we're going to formulate right here is just going to make sure that all these different keys are present. So inside of here, I'm going to return a function that's going to serve as our actual middleware. So this thing is going to be called with some request. Some response and again, some next function. And chances are we're going to do some imports inside of here from express to get all these different types. So let's take care of that right now at the top, I'm going to import request, response request handler. And next function. All right. That looks good. So now inside the body of the middleware, we're going to look at record body. We're going to make sure that there is a body in the first case. And then we're going to make sure that all these different keys exist inside there. If any of them don't, then we're going to make sure that we immediately return the request or respond to the request with some error message. Otherwise, we'll call our next function, and that's pretty much it. Now, this video is running on just a little bit long, so let's take a quick pause right here and actually put together the implementation in the next video.
