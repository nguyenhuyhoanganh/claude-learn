# Practical Metadata

In the last video, we saw how we can use metadata around a very simple object. So let's now try to figure out how we can use metadata with a TypeScript class instead. I'm going to take just about everything inside this file and delete it if you want to. You can certainly comment it out. I'm just going to delete it so you can very easily see what I'm doing inside this file. Then I'm going to replace it with a class definition. So I'm going to create a class called Plane. I'm going to give playing a color that's a string of red. I'm also going to give it a method called fly. And any time I call fly, I'll do a count log of vir like so, which I guess is the sound that a plane makes. Okay. So then let's try to now use a decorator and metadata together to put some metadata property on the fly method. So to get started, I'll first write out a plain decorator function. So I'm going to call this decorator mark function because essentially we're going to add some information to a function inside of class plane. I'm not going to create this as a factory decorator. Remember, a decorator factory is one that returns a function. Instead, we're just going to call this decorator directly. We're not going to worry about passing in any custom arguments to it. So that means that mark function itself is going to take in the arguments of target of the plane prototype. And a key that is a string, and that's going to be the actual name of the method that we're going to apply this decorator to. So then inside of here, we can once again call, reflect, define metadata. I'm going to try to set some key on this fly method of how about just like secret? And I'll give it a value of about 1 to 3. Then the object that we are trying to apply this metadata to is the target. And finally, the key that we want to apply it to is going to be key. Like so. So now I can use this as mach function on fly. So that's going to look at the fly method where the fly property on the plane prototype and it's going to define some metadata called secret and it's going to give it a value of 1 to 3. So now to retrieve this information, we could reach directly into Plains Prototype and try to print out the information associated with that fly method. Let's try doing that down here. So I'll say const secret is reflect dot get metadata. We're going to try to retrieve the secret metadata property off of the plane prototype and on the plane prototype in specific. We want to fly find that fly property. So I'll put in. Why as a string like so. So now we can do a console log of secret. At the very bottom and we should see. One, two, three. So I'll save this. I'm going to once again flip back over, run it. And I see. Sure enough. One, two, three. So that's essentially how we're going to very easily tie some amount of information with a method. We're going to create a decorator that looks like this one right here. We can associate some information with that method and then retrieve it at some future point in time. Now to just make this a little bit more realistic, let's try making Mark function into a decorator factory so we can apply some custom information to each different function that we mark. So to turn this into a factory decorator, I'm going to wrap that thing with another function. That's going to be called Mach function. I'm going to indent the inner one. I'm going to remove the name off the inner function. So it's now an anonymous function and I'm going to return it like so. So now as the argument to mark function, we can pass in some secret information that we want to associate with that fly method. So I could say something right here like, I don't know, secret info and maybe that's going to be a string. And now when we try to associate or define metadata, we're going to make sure that the information that we define on there is going to be secret info instead of the hard coded 1 to 3. Okay. So now whenever we make use of mark function, we're going to call it and pass in our secret info as a string and I'll put in like hi there because clearly I like that string a lot. Okay. So now I'll save this. I should be able to flip back over to my terminal again. Rerun that file and see. Hi. There appear. Perfect. All right. So clearly I think we got a good job on adding information to a method inside of a class. So let's now try to figure out a little bit better way to retrieve information rather than reaching directly into the prototype of that class. So something a little bit more effective than that right there. So I'm going to try to define a second decorator function. This will be just a plain decorator. It will not be a factory decorator. And I want to apply this decorator to the class itself. When I define or apply it to the class, I want to then loop through all the different properties on this class's prototype and retrieve and attempt to print out all the different metadata that each property has. So to do so, I'm going to first write out a function called print metadata. So this is going to be my decorator and I'm going to apply it to a class because I'm applying it to a class. Remember, the arguments are going to be changing. So we're still going to have a target argument, but its type is going to be different. Whenever we apply a decorator to a class, the type argument is going to be of type well, constructor function. So whatever the constructor function of plane is. So to refer to that type, we can write out type of plane. That's a reference to the constructor function of the plane class. So then inside of here. We can write out a loop to go through all of the different keys on the plane objects prototype. And so that would include right now, just the fly method. So inside of here I'll say for let key in target dot prototype. So we're going to walk through all the different keys inside of that prototype. Right now, when I say key, I'm talking about like normal key. I'm not talking about a metadata key. So this loop right now is only going to run one single time. It's only going to find that fly method right now. So now inside of here, I'll do a reflect dot get metadata. I'm going to try to look up the what do we call it, secret property. From metadata of every different key inside of that prototype. So I'm going to look up the secret. From Target prototype. And specifically inside that prototype, I want to look at the key property. Now I know I'm using the term key an awful lot here. So let me just explain this last step one more time. Our for loop is going to iterate through all the different keys of the prototype. So the word key right here is going to be first equal to fly. So then we're going to look at getting metadata off of Target prototype and we're going to pass in as the key right here. The property that we're trying to get our metadata off of is going to be fly. And inside that fly property, we're going to try to find the metadata information called Secret. Okay. So let's now assign that. It's a secret and we'll do a quick console log of secret like so. So the last thing is to actually make use of this decorator. So I'm going to apply print metadata to the class definition itself. Like so. Now. I'll save this file. I'm going to flip back over to my terminal. Run it one more time and I'll once again see. Hi there. Perfect. Okay. So this file right here in its current state is essentially exactly what we're going to do inside of our express application, 100%. This is exactly what we're going to do. The only difference is that our mach function, rather than being called mark function, it's going to be something like get to associate a route or post to associate a root or use to indicate that we want to use a middleware or what was he the one like validate body to validate some information in the body. So for each of these different decorators we create, we're going to take in some customization and we're going to associate it with that property on the class's prototype. Then once we finally run the print metadata or that controller. Which is what we're going to call it. Remember, we said we were going to call it comptroller. When we finally run that top level decorator, that would essentially be this one right here. We're going to loop through all the different keys of the prototype. We're going to look at every single one of those methods and see if it has any root configuration data. If it does, we're going to then going to pull it out and associate it with our router. So let me give you a really practical example of this. We're just going to change a couple of names inside this file, and you're going to very quickly see how this all works out. Okay. So I'm going to change the name of mark function right here to get and I'm going to imagine that I'm trying to associate the slash log in root or I get request to slash log in with this fly method right here. So I'll then change the name of this decorator from Valerie body to get. It's going to take an argument of how about we just call it like path? And then when we associate our metadata, rather than calling the metadata key secret, let's instead give it a more reasonable name. It's like the path name, so I'll call it simply path. And the information we want to associate is going to be the path that we passed in. So now instead of calling this decorator print metadata, let's call it controller instead. I'll change the name of this decorator down here to controller as well. And so now we're going to look at every method inside that class rather than pulling out a property called Secret. Let's pull out the path property from that data. I'll assign it to a variable called path. And then let's imagine that we've got some router inside of here, some like express router. And so we would do something like router gadget path. And then as the second argument, we would pass in the actual function. So that would be. Target prototype at key like so. I know you see some errors inside of here. Don't worry about the errors. We will fix those up. But this right here is essentially everything we're going to do. Now, remember, we also have to associate some middleware and the validate stuff as well. We're going to do that all at the same time. So we are going to create a second decorator called like use. And when we start to pull out information about the given function inside this loop right here, we're going to pull out that different middleware in addition to the path. So right here, maybe we'll pull out like the middleware. That we want to use. And then we would just expand this router, call right here to K whenever someone makes a get request to this path, run this middleware and then eventually that route handler. So this is pretty much it. This is how we are going to exactly achieve our goal inside of our express application. All right. So now that we've got a better idea of what we're trying to do, let's take a quick pause right here, and we're going to start implementing all the stuff in the next video.
