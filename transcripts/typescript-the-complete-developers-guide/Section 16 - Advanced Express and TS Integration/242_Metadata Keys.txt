# Metadata Keys

In the last video, we defined that methods enum to solve a little problem in our code. Now, while we are talking about enemies, I want to point out one other location that we could definitely make use of an enum to absolutely improve the quality of our code. So notice how inside of our controller file we're doing a git metadata call with a string of path and a string of method right there inside of our roots file, we are doing a defined metadata with those exact same strings. Now, what would happen if I put in defined metadata right there with like a string of method one or something like that? Well, if I put that in, then when we do our metadata lookup, we're going to get undefined because we defined metadata as method one, but we tried to retrieve it as simply method. So in that case, method right here would come out as undefined. Now that's super ideal and un ideal. And once again, we're seeing a possible air that would be generated by a very simple mistyped string. So I think that we might want to create a second enum inside of our application, an enum to define all the different metadata keys that we might be defining. So then we only have to write out a string one single time inside that enum definition, and there's zero chance of us accidentally making a typo in the metadata key in some other location. So let's try creating an enum to represent all the different metadata keys we're ever going to define. So to do so inside of decorators, I'm going to make a new another new file and we'll call it metadata. Keys is. And then inside of here, I'm going to define and export another enum called Metadata Keys. So I'll give this a key of, like. HANDLER Or how about just, like, method? Let's keep it straightforward. So method will be method and we'll do a path of path like so. And I mixed and matched my capitals here. Let's just stick with lowercase in typescript. We're actually supposed to use uppercase characters on our enum rules or our enum properties, but we just broke that rule inside of our methods. Enum So I'm just going to go with lowercase keys here, just for the heck of it. Okay. So now we can make use of this enum in both routes, RTS and controller, RTS as well. So inside of Roots TS at the top I'm going to import metadata. Lowercase D. Metadata keys from metadata keys. And then, rather than referencing the hardcoded path right there, I'll instead use metadata keys. Path. And same thing for method as well. Okay. So now we can go and update controller pts two. So I'll save this file. Go back over to controller ts at the top. I will import metadata keys. From that file. And then rather than referencing the hardcoded path and the hardcoded method. We'll instead do. Meta data qis path. And instead of method metadata keys. Dot method. Okay. So much better. You'll now notice that we have essentially zero hardcoded strings inside of our app, which is usually a good sign because that means that there's that much less chance that we're going to accidentally make one of those typos. Okay. So everything's looking pretty good here. And we've now essentially got full support for setting up different route handlers on a given method. However, we still need to set up decorators to handle body validation and also make sure that we can handle middleware as well. So quick pause and we'll start to take care of those two things in the next video.
