# Why is This Hard?

In this video. I want to help you understand why setting up these different decorators is going to be rather challenging. So to understand why this is going to be hard, we're going to have to apply some knowledge of decorators and also apply some knowledge of the express router as well. I want to remind you that at the very top of this file we defined a variable called router right here. So we associated all of our different route handlers with that router. So I want to think about how we would associate these different methods inside of class login controller with that router by using decorators. This is just going to be a little thought experiment. Now I'm going to very quickly write out some code. This is where some of this, like really tough stuff I mentioned is going to kick into gear. So I'm going to very quickly write out some code to give like one possible implementation of how to actually put all these different decorators together. So I'm going to first think about how we would implement, post, validate body and use on the post login method. All right. So I'm going to write out some code here. Once again, I'm inside the section called Do Not Write because I am going to delete this very quickly. This is just a quick example. So I want to have a decorator called Post. Whenever we call a post, we're going to pass in some route name. And remember, if we want to use a decorator factory, we have to return a function and that will be coupled with some target. That's a prototype. I'm going to annotate it as any for right now, the key, which is the name of the method. In this case, it'll be post login. And finally, our desk or property descriptor. As well. Okay. So now inside of here, we're going to have to essentially take that root name. That is a string. And our. Name post and the actual method right there and associate all these things with our router. So to do so we could write out some code that looks like this. We could say essentially router. At or senior at her post. And we want to associate the root name. With Target at Key like so. So think about what that code right there is going to do. That's essentially going to say, okay, look at the router. We want to associate a post request handler. The route we're watching for is route name. So in this case, it'd be slash login. And then the actual route handler itself, like the actual function that's going to handle the request is Target at Key, which is essentially a reference to post log in right here. So this code right here would probably work just fine. I'm going to be honest with you. This is probably going to work A-OK. But now here's the issue. The issue starts to come up when we think about how we would implement, validate, body and use as well. So let's think about how we would implement those. I'll first start off with the use because that one's a little bit more obvious. So I'll say that we need some use decorator. This thing is going to be called with some middleware. And for right now, I'm just going to annotate it as any. The type of that thing should probably be like request handler or something like that. But also annotate is any for right now. And then, because this is going to be a decorator or a factory decorator, we need to once again return a function that will be called with our target key. And property descriptor as well. Okay. So here's the problem. We need to essentially say, hey, remember that request handler that we associate with the router just a second ago with that post decorator right there? Well, we want to tack on a middleware to that. So what we are essentially saying here is that now with only this information, with only that middleware function that we're passing in, with only the target and the key, which is essentially a reference to this method right here, we essentially have to now look up that request handler that we just associated with our router right here, and we have to somehow inject a middleware inside of here. So in other words, we need to kind of communicate some information from this function right here. Back to that statement right there. That's the big challenge. That is what is really hard about this. So the possible ways we could do this would be maybe to somehow access the router. And maybe we could say, like, I'm going to call some imaginary function here. I'm going to say like router dot add middleware to handler, we just registered or something like that and then pass in the middleware. Now, as you might guess, that method does not exist at all, but that's essentially what we're trying to do. So we need to kind of reach backwards, find whatever root handler we just associated with the router and tried to like inject a middleware back into that thing. That's what the big challenge here is. So this is a great example of where you get to see very much in action. Why sometimes it's hard to get TypeScript to work with some JavaScript libraries in order to do something like this. Right here, we need to have some expert level knowledge of express. That's one possibility. So we would need some expert level knowledge where we could somehow look back into the router, find the last handler that we just registered, and then somehow, like, associate this middleware with it. That's option number one. Option number two is to take a little bit more general approach, something a little bit more flexible, something that might work with other different routers besides just express where we don't have to reach directly into the router and try to like backwards inject a middleware or something like that. Now, one thing I want to mention here really quickly, you might know that Express has the ability to generate like a route object and we can associate middleware with a route object. Again, I'm kind of assuming you have a lot of expert knowledge of express here. Unfortunately, even if we did that, it wouldn't quite work as expected because whenever we register route handlers or middleware with a router. We have to first register the middleware and then the root handler. So we are essentially saying, okay, we would have to somehow like first register the middleware and then only later register the actual root handler, handler itself. So it starts to get kind of confusing and it means that we would have to have like some distinct order of operations of how we are applying all these different decorators, which would definitely be confusing for people who are trying to use these decorators as well. So in other words, rather than trying to do this right here, rather than trying to reach into the router and find the last handler that we just registered with it and tried to like inject a middleware to use, I want to think of some more general solution, something that's going to be a little bit more obvious and work 100% of the time and not really rely upon some inner workings of express or something like that. So once again, this is the big challenge. This is what is so hard about getting a router to work with these different decorators. And this is essentially the problem that we're going to be struggling with, and it's going to essentially shape how we structure this entire project and writing all these different decorators. Okay, so that's enough of that. Let's take a quick pause right here. When we come back in the next section, I'm going to give you a general solution of how we're going to approach this problem. It's a quick pause, and I'll see you in just a minute.
