# Project Overview

All right, my friends, if you're still here, well, no further waiting needed. Let's get to it. Let's talk about what we're really going to be doing here. So I've got the documentation from that library that we looked at a while back to us express decorators on the screen. I'm going to scroll down to the very bottom and down here at the very bottom there is a section called Create Your First Controller. So with this library you create something called a controller. It's essentially a class meant to handle a bunch of different route handlers. So here's the definition of a controller. You'll see that they are using a class decorator at the top, and then for each method inside of here, they've got some number of actual method decorators as well. So there's one, there's another right there and here's like two being applied to this post method. So we're going to take a pattern very similar to what you see right here. And this is how we're going to get some really good, very solid coupling between TypeScript and Express inside of our application. So to get started, I'm going to flip back over to my editor and I'm going to write out a quick code snippet to tell you exactly what we're going to try to achieve. All right, so here's my server code editor. I've now closed the code editor we were working with on that features directory. So here's that like decorators file we were just working out of. You'll see. Yep. I'm tossing it off to the side. So I'm back on the Express project now. So you should see your index file and your login routes file as well. So we're going to get started inside of our login routes file. Inside of here. I just want to give you a quick reminder. We defined a router and then we associated a couple of different route handlers with that router. Now, this approach right here, yeah, it definitely works. There are probably millions of lines of code written with this pattern in the world today, and all that code probably works just fine. Nonetheless, it would be kind of neat if we could use some syntax like the one we just saw inside of this Express Decorators library. So let's write out a quick example inside this file at the very bottom. This is not going to be real code. I'm just going to give you an example of what we're going to try to achieve. So I'm going to go down to the very bottom and I'm going to write out a quick example here. I'm going to say, do not write because I'm going to very quickly delete all this stuff. So I think it would be really neat if we could somehow make a class called something like login controller or something like that and have all the routes that we placed inside this file into this logging controller class. So for example, maybe we would have some method called like git log in because this is making a git request to slash log in and attempt to find the log in form. And then maybe we could have a method called like post log in and get root, root and stuff like that. I know some of these different routes inside this file aren't necessarily coupled to logging in. Nonetheless, let's just throw them all into this class. So I think it would be really neat if we could have a method called like get login. And if this thing was called with some request and response objects. And then inside of here, ideally, maybe we could say, you know, a present and we'll send back our form or whatever else it is. So if we just define the method like this, you'll notice that we have no mention to any router whatsoever. There's no nothing here to actually associate this method with the router. There's nothing to say. Okay. If anyone makes a get request to slash login, run this method right here. So we're going to try to create a decorator that's going to work like this. I won't be able to say at Git. And then the route right after it. Like so. And so I want to have a decorator that's going to essentially read this method right here and take that method and associate it with some router. So if we ever make a gift request to log in, it should attempt to access this method. So same thing with maybe our post request for the login form as well. So maybe we'll have a method like post login. That takes a request and response. And inside of here we can do all that same logic we had above that we went through. Remember, we went through all this pain right here with the post login form where we had to make sure that the body had an email and password and so on. And so I want to imagine that we could take all that logic and stick it directly into this method like so. And then to associate this method with a post request to the slash log in root. I want to have a decorator like at post. I got my autocomplete kicking in here at post login like so. So if we ever make a post request to slash log in. Yep. Run this method. So I think that you get the idea of routing. I think that's fairly clear. I think that the next thing that would be really cool to have is the ability to maybe have a decorator that's going to automate, checking to make sure that we actually have some properties inside the body of a request. So remember, we went through all of that hassle talking about how we have to make sure that the body of the request has an email and password. And then we had to actually write out some logic right here and then do some very manual air handling around that. So I think it would be awesome if we had some kind of decorator call like Validate Body or something like that. And if we could just pass in. The different properties that we expect that body to have, like email and password. And then if a user did not, or if the request did not have an email and or an email and password in the body of the request, then we should automatically send back some kind of error message to whoever made this request. So once we have this middleware hooked up, we can then be somewhat guaranteed that inside of this method right here, email and password are actually defined. The last decorator I want to put together is going to be something around making use of middleware. So remember at the very top of this file, we defined that require auth middleware. So I think that it would be really neat if we could also automatically wire up some middleware to one of these methods as well. So if I could do something like at use. Require off like that and have the require auth middleware run automatically before executing this method right here. Now, the last thing that we need to do and this one is going to be a little bit mysterious, but I'm going to tell you that this is something we're going to have to do. I want to also have the ability to mark a class distinctly as being a controller by putting it on some like at controller. I keep on getting that autocomplete at controller decorator like so and maybe we could pass in like a prefix and this prefix would be applied to all the different routes inside of here. So maybe this would have like an auth prefix like so. And that means that all these different routes inside the class would then be accessible through slash auth slash login. All right. So that's this right here. This is the general idea. This is what we want to achieve. So we're essentially talking about putting together four different decorators. Well, maybe slightly more. But the reason I say slightly more is that you'll notice that get and post are essentially doing the same thing. We're attempting to associate a method with a like a method on a class with a router. So they really do the same thing, but they do have different names. Nonetheless, we essentially are talking about four different types of decorators. So we got one decorator to associate a method with a router. We've got one to do some validation, one to set up some middleware, and then one to mark a class as being an actual controller. So that's essentially the project. That's what we're going to try to do. Now some of the code we're going to write for this. Like I said, it's going to be a little bit crazy and it's going to require you to kind of put on your thinking cap in some locations because I'm not going to necessarily walk you through every last tiny little aspect of this, because if we did that well, this would be like another ten hour section of this course. And I want to get this done a little bit more quickly. Okay. So now they understand what we're trying to do. I want to take a quick pause. We're going to come back the next video. And I want to highlight one or two issues with all this code that I'm saying that we need to write. And these this highlighting of these issues is going to help you understand why this is going to be something that's rather difficult to do. So quick pause and we'll see you in just a minute.
