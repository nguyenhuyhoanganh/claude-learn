# Defining a RouteBinder

In this video, I want to point out one last little awkward thing. We've put together a decorator called Git, and we define that inside of our roots file in the decorators directory. Now, the only issue right now is that we only have one method, right. A git request. Chances are whenever we build a server, we're going to want to have many other different route methods available to us as well. So like a post or a put or a delete or whatever else. And so right now, I don't think that we really are doing a great job of making sure that we can associate these other types of route handlers. Now you might be thinking, okay, well, this is easy, Steven, let's just duplicate this function, change the name to post and there we go. Well, not quite. Remember, we are defining some metadata on our route handler right now of only path. So when we get back over to our controller file and we pull off that path property, well, that's all we know. We only know the path. We don't actually know what type or what method of route handler. We want to assign this to our router as we have hardcoded in git. So we essentially need to add in a second type or a second piece of metadata to all these different definitions. And that second piece should indicate the method that we want this root handler to be assigned to our router as. So to do so well, we basically just have to define a second piece of metadata. That's pretty much it. So we can very easily just copy and paste, ah, reflect, define metadata. All right here and I can change this second one. I'll change the name to like method. And because I'm inside of my git decorator, I'm going to change from path right here to get. And then we can repeat that same process for post as well. So the second one will go to method and then I'll change that value right there that we're storing as metadata to post. And now, as I'm sure you could guess over and controller PTS, in addition to pulling off path, we can also pull off method and then associate the correct method with our router. Now. This is definitely going to work. I just want to point out that we've now got two like very identical function definitions here. And if we wanted to also have delete and put and options and head and all these different methods, well, we're going to have a lot of super duplicated code inside of your. So we're going to do a little bit of a refactor here, and the refactor is going to look just a little bit nasty, still a little bit nasty, but I'm sure you'll understand what's going on. So I'm going to delete the post function. And then rather than exporting, get right here, I'm going to remove the export keyword. And I'm going to wrap function git with one more function. All right. So that's the kind of the hard part here. So I'm going to say function and I'm going to give this thing a name of how about like root finder or something like that? And then I'll indent the inner function. I'm going to make sure I return the inner function. I'm going to turn the inner one into an anonymous function by deleting the keyword git. And then finally, I'm going to add in the method that we want to create this decorator for. As an argument to root binder. They'll say method is going to be a string. And now here's the key to all of it. At the bottom, I'm now going to list out all of the different methods that I want to build a decorator for, and I'll use this root binder function to make them. So I'll say expert cons target is going to be root binder with Git and I'll repeat that same process for all my other methods as well. So I'll do one for like put one for post, one for delete. We can actually have a variable name, delete and JavaScript that is actually a protected keyword. So I'm going to change it to just be Del instead and let's do one for like Patch and I'll bet you can guess you can continue on that list if you want to. Okay. So think about what's going on here. Now we're calling root binder with Git that's going to return a factory decorator. And that factory decorator is going to have a reference back to that method argument. So method right there is going to eventually be get or it's going to be post or it's going to be patch or you get the idea. So now we're going to take that method name and we're going to define that as our method data for that method piece right there. So I'm going to update get to instead be method. And that's pretty much it. So the only reason we created this root binder thing right here was to avoid having to duplicate this same function over and over again. Okay. So this looks good. So now, if we ever wanted to add in more method types, well, we would just expand the list down here. All right. So I'm going to save this. I'm going to once again go back over to my terminal and just make sure I don't have any errors. And then I'll go back over to my browser and do a quick test. So it looks like my application still works just fine. All right. So that's a pretty good fix right there. So let's take a quick pause and come back in the next video.
