# Automated Validation

In this video, we're going to finish up this middleware that's going to take a look at the request body and make sure that all the given keys are actually present inside there. Now, we're not going to try to do any validation on the format of those keys or what their values are or anything like that. We're just checking to make sure that those keys are available. That's it. All right. So inside of here, we're going to first make sure that the request actually has a body property. So let's say if there is no body, then let's immediately send back some kind of error message. So to send back the URL, I'll do a rez status or 22 and I'll send back the text. You have it just like we'll do it very directly here. We'll just say invalid request. Okay. So now we can iterate over all these different keys and make sure that they are actually present inside that body. One little thing. Sorry. Let's make sure after that error gets sent back, we immediately return. That's better. Okay, so now we can iterate through that array of keys. I'll say let key of keys. And then if rec dot body at key, if that does not exist. So we'll say if not rec at body at key, then we'll do the same exact thing. So we will do a response of 422 with invalid request and immediately return. If we then get through the entire four loop, then we'll move on to the next middleware by calling the next function. And that's pretty much it. Okay. So now to make use of this, we just have to make sure that we pull off that list of keys that we need to validate from our metadata. And then we'll make sure that we call this thing and wire up the function that gets returned into our request handler middleware list. All right. So down inside of controller. We're going to find where we set up our actual route handler. Right above it, I'm going to pull off the list of required body properties, so I'll say const required body crops. And this will come from reflect get metadata. We're going to look at metadata. KEYES Dot validator. At Target. Prototype at Key. And I'm going to make sure that I definitely zoom out here so you can see that whole line. Now, once again, we have to make sure that we handle the case in which we never set any validators on here. So I'd be the case in which we get an empty array back from reflect metadata. So I'll do an or empty array like so. After that, we can take that array of keys and pass it into the function we just put together called body validators. So I'll say Collins validator is body validators and we're going to generate that using required body props. And then finally we're going to wire that up to our request handler. So right after middleware, I'll say, then run it through our validator like so. All right. I'm going to zoom out one more time here just so you can see this entire statement. So it looks pretty good. Let's take another quick pause right here. We'll come back the next video and do a quick test.
