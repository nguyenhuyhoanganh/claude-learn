# Basics of Metadata

In this video, we're going to write out a little bit of code to get a better idea of how metadata works. So for this code snippet, we're going to go back to our features project directory. I apologize for flipping between these different code editors over and over, but I really want you to have this features directory full of a bunch of very simple code snippets that you can refer back to in the future. That's why we are putting all these little side topics into the features directory. So inside of here, I'm going to start up my code editor once again. And here it is right here. So I'm going to close that decorators file that we had open previously, and I'm going to create a new file inside of here called Metadata. US. All right. Before we can actually make use of metadata, we have to do just a little bit of setup. First off, I'm going to go inside of my config JSON file, which we generated a little bit ago. And I'm going to make sure that both experimental decorators and emit decorator metadata are both on commented. If you don't see these options inside of here, that's totally fine. It just means that there's now prime support for metadata and decorators. Okay. Once I've got those both enabled and set to true, I'm then going to go back over to my terminal and I'm going to generate a package JSON file. To work with metadata, we have to make use of that reflect metadata package. So we have to make sure that we actually install this thing. So at my terminal I'll do an MPLM in it. Why? And then once I've generated the package package JSON file, I can do a NPM install reflect dash metadata like so. All right. And that's it. So let's go write some code back inside my code editor. I'm inside of my metadata file. Inside of here. We first have to import reflect metadata, so I'm going to import reflect dash metadata. Now you'll notice that I did not actually assign this import to any variable. We didn't say like import something from reflect metadata. When we import this package, it automatically adds a single variable to global scope. That variable is reflect. So we should now be able to right reflect in here and if we put a dot right after it, we'll see all the different methods that the reflect metadata package brings to us. So the big ones I want to point out here are defined metadata. This function allows us to attach metadata to an object or a property of an object and get metadata, which you might guess allows us to retrieve metadata from an object or a property as well. All right. So to get started. Let's figure out how this stuff works. I'm going to define a little object. Called Blaine. And this plane object is going to have a simple color property of red like so. So now I want to associate some metadata with this object right here. Remember, metadata is like some secret information that doesn't really show up anywhere except through the use of this reflect metadata package. The information that we associate with this object is going to be a little object of its own. I'll show you a diagram in just a second to make sure it's really clear what's going on here. But right now, let's just define some metadata and attach it to that object. So I'm going to say it reflect define metadata. Then we have to pass in the key for metadata. So I'm going to associate some metadata property called note with this object and the value for that, I'm going to put as simply hi there. And then I want to associate that metadata with the plain object like so. So you can kind of imagine behind the scenes that this is like adding in a new property of note with the value of hi there to that plane object, except this property is never going to show up in any debugger or anything like that. It is essentially an invisible property. If we now print out plane, you're not going to see any mention of. Hi there or note whatsoever. Let's now save this file and run it just to make sure that's the case. So I'll flip back over to my terminal and I'll do a node metadata as. And so I see only colors red. I don't see any mention. Of note or hi there. So to retrieve metadata off an object we can write out reflect dot git metadata. And then pass in the key that we're looking for, which is note and the object that we want to get the metadata information from, which is plain. So I'm going to assign that to a variable called note like. So I'm going to delete the previous console.log just so we don't get inundated with console logs here. And then I'll do a console log of note. So I'll save this, pull it back over, run the file. And sure enough, there's high there. Okay, so let's take a look at a diagram to kind of get a better idea of what's really going on here. All right. So we've got our object plane as a color property of red. So we're essentially imagining that we are getting an additional property or kind of like a pointer to another object attached to plane. So this is very much like that proto property that we saw before when we were talking about prototypes. This metadata thing is totally invisible. And if we try to console.log out our object plane, we're not going to see any mention of metadata whatsoever. This metadata thing is a reference to another object that has a bunch of different key value properties on it that essentially describe some special stuff about this object. So we just added on a note property of Hi there, we could add on as many properties as we want, so I could have another property called like I don't know about height, like the height of the plane or something like that and give it a value of ten like so. To actually reflect that in our code. We would come back over and we would set up a second defined metadata function call. So I would just copy paste that down. I would change the key that we are setting on metadata to height and then the value to ten or whatever we want to put on there. So now to retrieve that information, we would again do a get metadata call. So I'd say Collins height is reflect dot get metadata. We want to find the height metadata property on the plane object and then I could do a console log of height as well. All right, so I'm going to save this, run it again. And now we see both. Hi there. And then. So the real lesson here is essentially metadata just lets us attach little pieces of information to an object. We can also use this metadata stuff to attach information to a property on an object as well. So in other words, we could attach some little special piece of information directly to this color. Property color is still going to have a value of red, but we can associate some additional information about it as well if we so chose. So to do so, let's delete a little bit of code here first. If you want to, you could, of course, just comment that out so you can reference it later on. I'm going to delete it just to keep my screen nice and clear for you. So to associate a metadata property with color, we would say reflect dot define metadata. We then put in the key for this metadata, which I'll do note once again and once again I'll give it a value of Hi there. And we're going to say, look at the plain object and then find the colour property on there. So this is going to associate a note property with a value of hi there to the plains colour property. So now we could retrieve that information in a very similar way. So we could say note is reflect dot get metadata. We're going to look for that note property. On plane and specifically on the plane object. We want to find the color property. So I'll then do a console.log of note. Save this. I'm going to flip back over to my terminal once again. Run it and we once again see. Hi there. So in this case, we've got something a little bit more like this right here. So here is. Object plane. Object plane has a color property. Normally, it points at the value of red that nothing has changed in that regard. But we've also attached some kind of metadata information about that color property. So this metadata property is its own object of sorts that has a note. And I said in this diagram by there we actually did. Hi there. But you get the idea. So once again, we can associate a little kind of hidden object with another object or a property of an object as well. So now that we've gotten the basics of this reflex stuff down, let's take a quick pause. When we come back in the next section, we're going to learn how to work with this metadata stuff around TypeScript classes as well. So a quick pause and I'll see you in just a minute.
