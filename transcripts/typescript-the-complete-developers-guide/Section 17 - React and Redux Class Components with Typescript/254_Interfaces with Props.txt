# Interfaces with Props

Now that we've got a basic component put together, let's do one of the most basic things that we can do in React, which is to pass a prop down into the app component. So I'm going to find where we make an instance of the app component right here, and I'm going to add in an additional prop to it. I'll say color is red like so. So let's try to reference that prop inside this div right here. So I'm going to put inside of here my curly braces, as usual, and say this prop dot color. Now right away, you're going to notice we have two error messages going on here. So there's an error message around app and one around color as well. If you mouse over the app, one in particular, you'll notice that this error message really does not look that helpful at all. So here's the issue. One of the most basic ways that we worked with components in TypeScript is to create an interface that describes the different props that we're going to pass into a component. Let me show you how we do that right above the app component. I'm going to create a new interface called App Props. So the goal here is to create an interface that's going to describe all the different props that we expect to pass into our app component. So right now, the only prop that we ever expect to pass in is color. And I expect that to be a string. So now I'm going to annotate. Component right here. Component is actually a generic class. And if you hover over it, you'll notice that there's kind of some interesting notation right here. So this is saying whenever we make use of React component, we can specify the structure of props that we expect to pass in by passing it in as a generic as the first argument. So I'm going to put in right here app props like so. And as soon as I do that, both those error messages go away. So this pattern that you see right here is going to be repeated for just about every single class based react component you ever create. You're going to define your component. You're going to define an interface right above it that describes the structure of props that you expect to pass into the component. And then you're going to reference that interface right next to react component right here. So let's now save this file, flip back over to the browser and yep, we can see red appear. Now something that's kind of interesting is that many times in React components, we have props that might be optional. So for example, I might want to show the app component and pass in a color, but then in some scenarios I might not want to pass in a color at all. So let's say that we want to render app without any color property. If we try to do so, we're going to get an error. And the reason for that, well, it comes back to basic interfaces. Right now, color is a required property. So if we want to show app, we are required to pass in some value for color if we want to mark a property as being optional. Well, just like with the normal interface, right after that key, we'll put down a question mark like so. And that means that is an optional property so we can show app either with the color prop or without it. Now it doesn't matter. So if we now save this, we can flip back over. And now we see nothing on the screen because we passed in no color prop at all. Okay. So like I said, this right here when the most basic things to understand with TypeScript and react together, we're always going to define our class based components with an interface right above that describes the structure of the props we're going to pass into that component. So let's take another quick pause right here and continue in the next video.
