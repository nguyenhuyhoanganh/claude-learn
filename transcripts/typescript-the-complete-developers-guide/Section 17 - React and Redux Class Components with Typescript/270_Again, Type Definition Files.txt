# Again, Type Definition Files

In the last video we tried wiring up our delete to do action creator, but we saw an error around our app component being passed into the connect function. Now if you mouse over this, you can read through the error message. Essentially what's going on is one of the same issues we ran into back on the express application. The issue here is kind of disagreement of the type definition files. So here's a problem the connect function expects to receive as a second argument an object that is going to have a bunch of different action creators inside of it. The problem is exactly what Redux or React Redux or specifically this connect function thinks that an action creator is. So Connect thinks that an action creator is a function that returns an object and nothing else. Now, if you mouse over underscore app right here, you can scroll down to the very bottom and you're going to see something that says like type promise void provides no match for dispatch with promise void. Essentially what the issue here is, is the fact that we are using redux skunk with fetch to dos. Fetch to dos is a redux thunk type action creator. It does not return a normal action object. Instead it returns a function that's going to eventually dispatch an action. So if you mouse over fetch to dos right here, you'll see the type annotation of what fetch to dos is. So this is a function that is called with dispatch and it eventually returns a promise because we set that up as a async await function that resolves with nothing at all. However, if you mouse over delete to do, you'll see that this has the annotation of a normal action creator function. So it takes in some kind of argument and just immediately returns one object and that's it. So once again, the problem here is that React Redux just doesn't know what a Redux Thunk type action creator is. And that's why we are seeing this error message. Now, unfortunately, at this point in time, there are no easy workarounds for this problem. So unfortunately, the really work or the best workaround we really have here is to just say that fetch to dos is going to be a function like that and just leave it right there because we don't really have an effective way of telling Redux or simply React Redux exactly what Redux thunk is. So we're just going to kind of cheat here and say, Oh yeah, fetch dos is going to be a function. That's it. And as far as the connect function is concerned, it is 100% satisfied with that answer. And so that error message goes away. Kind of unfortunate, but it's just the reality of having to work with these type definition files. All right, so let's now save this and we can test out our application inside the browser. So over here I'm going to click on fetch. Now, I should be able to click on it to do and see it disappear from the list. And yep. Looks like that works perfectly. Okay. So that's pretty much it with deleting a to do. Now the last thing I want to take care of. Remember we had said if we could find our diagram here. Here we go. While we are fetching our list of tattoos, I want to show a kind of loading message on the screen. So let's take care of that in the next video.
