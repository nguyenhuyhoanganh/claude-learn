# Tracking Loading with Component State

In this video, we're going to make sure that we can show the loading text on the screen whenever we click on that fetch to do button. Once we successfully fetch our list of to dos, I then want that text to go away. Now, normally the right way to do this is to manage this loading piece of state right here with Redux. So with Redux, we should have some piece of state that says, Hey, we are attempting to fetch the list of to dos. Once we get them, we should dispatch an action saying, okay, we're all done fetching the list, so that's the right way to do things. But in this one particular case, we're going to instead use component level state inside of our app component. And the only reason for that is that I just want you to have a reasonable example of how to use component level state. That's it. Okay. So we will flip back over to our component. Now to get started right above my class, I'm going to define an interface that describes the state that I expect my component to have. So I'm going to say interface app state. And I'll say that this is going to have a fetching flag that's going to be a boolean like so. We can then add that in as the second argument to the generic. And then remember, if we take this approach of defining this interface, we are essentially saying that we want to initialize our state object inside the constructor. So we would say, Here's our constructor function. That's going to be called with some props object that is of type app props. We would then call super with props and then initialize our state to be fetching false because by default we are not fetching anything. So remember, if we did not define this interface right here and did not pass it in as a second argument to the generic, then we could instead simply do state is fetching false. Remember this type of approach right here is just going to override the state property of their react component based class. So it's either approach. Totally up to you. Okay. So now as soon as we click on a the button to fetch our list of to dos, let's update our state to say yes, we are now fetching. So I'll say inside of on button click this dot set state. Fetching is true. And then once we receive our list of tattoos, we want to make sure that we flip this flag right here over to false. So to do so, we can define another lifecycle method of component did update. This is not going to return anything. And remember. This life cycle method is called with our previous set of props from the last time this component was rendered. So I'm going to receive that as an argument called previous props and it's going to be have type app props. So then inside of here we can say if our previous props had no tattoos. So notice the not right there. So if our previous props had no tattoos and this current props does have some, that must mean that we just successfully fetched our list of tattoos. So I'm going to update my state to fetching false. And now finally inside my render method, we can add in a little conditional to see if we are currently rendering or simply fetching anything. So down inside of my render method. I'm going to add in a little conditional. So I'll say this state fetching. If we are, then I'll return the text loading. Otherwise I'll just return null. So we show nothing at all. Okay. So I'll save this file and I'm going to flip back over to my browser and we can test this out. Now, one thing to be aware of fetching that list of to dos is generally very fast. So the loading text is probably going to only display for like a fraction of a second. So going to click on fetch and I definitely solve for like a fraction of a second the word loading right there. If I refresh the page again, I can click it again. And yep, we're definitely showing that loading flag appropriately. Okay. So that's pretty much it. That is our application. So I hope that you've enjoyed learning how to put React and Redux together. Let's take a quick pause in right now and we'll talk about some of the overview of this entire application, some of the big lessons in the next video.
