# Action Types Enum

In this video, we're going to start making a couple improvements to the action creator we just made. So the first thing I want to do is try to apply a type to the response data that we get back from our API request. So to do so, I'm going to create an interface inside this file that describes the structure of A. To do that, we get back from our API. So I want to create an interface that has an ID, a title and completed once again, we're going to totally ignore user ID, so we're just going 100% ignore it and not add it into any interface because we never really intend to make use of it. All right. So I'm going to flip back over. And right underneath my import statements, I'm going to define an interface called To-Do. So this is going to have an ID. That is a number. A title that is a string and completed that is a boolean as well. So now in order to help TypeScript understand what type of information is going to be inside of response data, remember that git right here is a generic function as well and you can tell that if you mouse right over it, you're going to see Axios instance. Doggett and then that classic generic syntax right now. So right now TypeScript assumes that we're going to get back the any type from this request we are making. So we just want to help out TypeScript a little bit. So I'm going to put on here my angle brackets and say to do array like so. So this tells TypeScript that inside of response data we're going to have an array of objects that satisfy the to do interface. So they're all going to have an ID that it's a number, a title that's a string and completed that's a boolean. So if I now mouse over response data, you'll see that the type annotation on here has been updated. And now TypeScript understands. Okay, response data is going to be an array of to dos. Perfect. Guess that was an easy fix. So now the next thing I want to do is create an enum to represent all the different types that I expect to have inside of my application. Now, a quick note here, because very quickly, we're going to start using the term type quite a bit. Remember that in TypeScript when we say type, we were referring to the type of an object or a property or a function. When we say type in Redux, we are talking about the type that an action object has specifically like its type property. So I'm going to be referring to type type quite a bit. I'm going to do my best to make sure it's really clear when I'm talking about a TypeScript type and a Redux type. So I want to create an enum to represent all the different types I expect to have inside of my app. To do so, I'm going to find my actions directory. I'm going to make another new file called Types. It's. And then inside of here. I'm going to create and export an enum called Action Types. And then inside of here, I'm going to add in an action type of fetch to dos like so. Now, one thing you might notice is that on every other enum we've ever created, we've always said something like Fetch Dos is a string. Like so. Well, it turns out that with an enum you don't actually have to do that. You can just say fetch to dos and leave it right there. If you do that by default, TypeScript is going to assign a zero to that first type right there, and then a one to the next one and a two to the next one and so on. In Redux. We don't really care about actually assigning a real string to this thing. Remember, the only requirement of a type in a redux action is that it must be some unique value. In theory, a unique string. But once again, it really doesn't make a difference at all. It just has to be something unique. So if we rely upon the default behavior of this being a zero, that's pretty much good enough for us. Okay. So there's our action type. So now back inside of index.html inside the actions directory, I'm going to import action types. From types. And then right here, instead of saying type, we can put in action types dot vegetables like so. So no longer do we have a hardcoded string that has some very special meaning to us. Instead we've got an enum and TypeScript can do a very good job of taking a look at that value right there and using it to check out some different types or values that we reference elsewhere inside of our application. You're going to see in just a little bit that this action type's enum is like a super pivotal thing inside of our Redux app, and there's going to be some really great coordination between this enum and the different reducers we create that's going to end up giving like great functionality to our Redux code, but we'll see that in due time. So we've now improved our response object and we've added in a better type here for our action object. So I want to do one more thing around this dispatch function. Let's take a quick pause right here and we'll tackle that in the next video.
