# React and Redux Overview

In this section, we're going to move on to understanding how to get TypeScript to work really nicely with React and Redux. Before we get started with anything, I want to just have a quick disclaimer here. In general, I'm going to assume that you are very much familiar with React and Redux, so we're not going to cover React or Redux from scratch. Again, I'm assuming that you've got some kind of like intermediate level amount of knowledge. I will do a lot of reminders as we go through a lot of this code, just to remind you about how React and Redux works. But again, high level concepts, you better have them. Okay. So that out of the way, let's talk about how we're going to get these things to work together. Now, I want to first begin with a kind of a pros and cons list of getting TypeScript and React and redux to work together. So pros first first thing here is that making use of TypeScript eliminates entire classes of very easy to make mistakes when working with React and Redux. For example, a classic mistake whenever you're working with Redux is to make a typo on action types. When we're making use of TypeScript, well, that problem goes entirely out the window. So when we start making use of TypeScript, a lot of very basic errors start to fall away, which is really nice. Next up, TypeScript gives us a much better idea of the information that's flowing around inside of our application, inside of a normal JavaScript, React and Redux app. You have Redux kind of flowing all over the place and you don't always know exactly what type of data is coming out of your Redux store. So when we start making use of TypeScript, that compiler and the type annotations and the entire type system, it's going to give you an excellent idea of what all the different types of data are inside of your app, and you're in general never going to have a lot of questions around, Hey, what props are flowing into this component or something like that? And then finally, it makes refactoring any type of React and redux code really, really, really easy and straightforward. You're going to have a much better job or way more efficient time trying to say change the structure of some state being returned from a reducer inside of a normal JavaScript application. If you start to change reducers, well, you're going to be doing a ton of very manual refactoring with your JavaScript code. It's not really a nice experience, but with TypeScript, as soon as you start to change the structure of your store, well, you're going to get a ton of notifications, essentially a ton of errors inside of your editor to tell you about all the different changes you need to eventually make. Okay. So that's kind of the pros now. There's definitely a pretty big list of cons here that I want you to be aware of. Now, the only reason I'm going to show you these cons are just to point out some things that you need to be aware of. I'm not saying that you should not use TypeScript with React and Redux. I still think 100%. Using TypeScript with both is a fantastic choice and personally I plan on all my projects. From the time that I first learned TypeScript all the way into the future, I'm always going to use TypeScript as much as possible with React and Redux. Nonetheless, I just want you to be aware of some of these really big issues. So the first big issue is around type definition files we already saw back on that express application. If you work through it, that sometimes type definition files are kind of unreliable and you're going to see that, yeah, that's very much the case here with React and Redux as well, especially around the Redux library. Maybe by the time you are taking this course, maybe there will be some updates to those type definition files. But I can definitely tell you that as of right now, well, there's still kind of some work to be done on that front. Next up, you're going to see that there are going to be a lot of generics flowing around as well. Maybe not a ton per say, but definitely more than say on the express application we were just working on. If you found all that generic stuff to be a little bit confusing, well, unfortunately, React and Redux might be a little bit confusing as well. I do want to say that as far as generics go, you're going to mostly be seeing them around Redux, mostly around React. You're not going to really see a lot of generics. Technically, every component we make with React is going to be a generic, but those are very easy to work with compared to some of the generics we're going to run to on the redux side. Now, the next issue here is one of the biggest problems that I've had personally, working with TypeScript with React and Redux, mostly on the Redux side in particular. So we end up having a ton of imports flowing all over the place. We've got a ton of different files, a ton of different pieces of code that are highly interconnected. So we get a real tight coupling between different things we create inside of our app, which is pretty darn unfortunate. But in reality, that's just kind of the situation that we are confronted with when it comes to Redux, because everything inside of Redux basically needs to know everything. An action creator needs to know the payload type and action well, that needs to know the payload because that's the actual thing that carries the payload. The reducer needs to know about the payload type, the store needs to know about the payload type. And our app components are react. Components also need to know about the data coming out of Redux as well. So as soon as you start to introduce new data types that maybe represent some JSON data that's coming in to your application, that information has to be flowing everywhere, everywhere, like to almost every single file. And that can definitely be a pretty big pain, unfortunately. Now, the last big issue here is that redux and absolutely react as well. But it's not quite so it's such an issue with React Redux in particular is incredibly functional in nature. In other words, Redux works through functions, right? We create functions. That's essentially how Redux works. Well, TypeScript, as you well know by this point, is all about classes. And so we've got a very functional style library and we've got a very class based or object oriented language, essentially, that we are working with here. And so, unfortunately, getting these things to work together, sometimes it just doesn't quite feel right. It feels like we're not quite using 100% of the power of TypeScript. TypeScript is definitely going to amplify our redux code and make it better, but it feels like it's kind of hard to really use every feature inside of TypeScript appropriately. Okay. So that's some of the big issues I just want you to be aware of. And we're going to absolutely be seeing instances of every single one of these problems, and we'll have a discussion about each one and how to deal with each one of them as well. All right. So let's take a quick pause right here. We're going to come back the next section and start to discuss the app that we're going to work on.
