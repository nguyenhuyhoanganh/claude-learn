# Validating Store Structure

In the last video, we finished up our tattoos reducer, and now all we have to do is hook it back up to our combined reducers call. So inside of reducers indexed, it's yours. Combined reducers. I'm going to import our reducer at the top. From the Tutu's file. And then I'll assign that to the combined reducers. Call under a key of to dos. So to dos reducer like so. Now, just for a moment, I want you to remember exactly what this is going to do for our redox state. This means that we're going to have a redux state that looks exactly like this. It's going to be some object that has a to DOS property and that's going to be an array of To-Do objects like so. Okay. So with that in mind, I want to now show you some little optional step that we can add in to the combined reducers. Call this little extra optional step is going to be very, very similar to what we just did inside of actions indexes. So remember just a second ago on our dispatch function. We added a type right there, and that type was used to essentially validate the object that we are passing in. So we can do something very similar with with our combined reducers call as well. Combined reducers is actually a generic function. And if we want to, we can pass in some type or some interface that will be used to essentially validate that object. Let me show you exactly how this works. Right above my reducers call. I'm going to add in an interface that's going to describe the entire state of my entire store. So remember just a second ago, I told you that our store or our state object was going to look like that, right? Well, essentially, we want to express this inside this new interface. So I'm going to call this new interface store state. It's going to have a to DOS property that will be an array of tattoos. Right away we get an air around to do because we have not imported that into this file. So I'm going to make sure that I very quickly import that at the top from actions indexes. Okay. So this interface right here is going to describe the entire state, the entire state inside of our Redux store. And now I'm going to provide that interface as the generic argument to combine reducers. So let's say right here, store state like so. Now it looks like nothing changed, but something really important is going on behind the scenes now. TypeScript is now taking a look at the object that we are passing into combine reducers. It's going to take a look at all the different properties and then for each reducer that we pass right here, here's the kind of hard part to understand. Normally with an interface, we just say, Oh yeah, we check to make sure that the properties and the values match up with the interface. But in this case, TypeScript is making sure that this function right here returns a value of type array of to dos. So that's the validation that's going on. TypeScript is making sure that whatever our reducer is actually doing is lining up with what we say that we want our store state to be. So that's really fantastic news for us. It means that if we ever accidentally return some inappropriate value from reducer, we're going to catch it automatically inside this file as an error. Let's try testing that out right now. I'm going to go back over to do tattoos. That's our reducer. I'm going to comment out the entire switch statement. And instead I'm just going to turn the number five. So now today's reducer is always going to return a number instead of an array of to dos. If I now flip back over to indexes, you're going to very quickly see an error message right here. So TypeScript has said, okay, you expect your store state to be an object with a to DOS key. That's going to be an array of to do objects. However, your to dos reducer is returning a number, which is not the same as a reducer that returns an array of to do objects. So like I just said, this is a fantastic feature. It's going to help us very easily catch errors. If we ever have a reducer that fails to return a value or returns an incorrect type of value as well. The other thing that we get by creating this interface right here of Store State is essentially a living document of sorts. This interface right here is always going to describe the entire state of our store redux store. Excuse me. So that means that if another engineer starts working on our project and wants to understand all the different values and types and properties inside of our store, well, they just have to look at this interface and they will immediately understand all the data that exists inside of our Redux store, which is a fantastic feature as well. Okay. So I'm going to go back over to my reducer and undo that change. And now you'll see that the error message has now gone away. Okay. So this is a pretty great feature right here. Definitely one of the kind of killer features of integrating TypeScript into a Redux application. So that's pretty much it for our reducer. So let's take a quick pause right here, and then we're going to wire this thing up to our app file in just a moment.
