# Confusing Component State!

In the last video, we got a quick example with State put together. And at the very end of the video I told you that there was something a little bit mysterious about the code we just put together. So in this video, I want to point out that when we start making use of TypeScript with React, we're going to get some kind of interesting behaviors. And unless you are kind of sharp on your typescript skills, sometimes you're going to end up getting some error messages that are just plain mysterious. So let me show you a quick refactor that's going to result in a really strange error message. So I want to point out right now that as I've written out my code, everything looks 100% A-OK. No issues whatsoever. Now I want to point out the state definition up here. Remember, this is a entirely normal way to initialize a property on a class in TypeScript. If you've ever worked with S 2015 classes as well, you might have also seen that we can use this kind of syntax with S 2015 classes as well. So this is a very normal way to initialize state in either a JavaScript React application or a TypeScript one as well. Another way that we can initialize state is by defining the constructor function. Let's try doing that right now. So to find my constructor, this thing is going to take some props as an argument. This is 100% the same as it is in the JavaScript world with React. The only difference this time around is that we have to provide a type annotation for this props and as you'll recall, they're going to follow the type of app props, so we'll use that annotation like so. Then inside of here, just like with is 2015, we have to call super with props. Nothing different there. So now inside the constructor, we are free to initialize our state object. Let's try doing that right now. So I'll say this state is counter zero like so. So in the JavaScript world, this is 100% equivalent to the code we just had on the screen a moment ago. But you'll notice that even with this code, we still have an error around this statcounter. And if I mouse over it, you'll notice that it says property counter does not exist on type read only object. What's going on there? Well, this is what I'm talking about. So this is some kind of unexpected behavior that you might be really surprised at when you start working with React in TypeScript together. If you're just trying to take all of your JavaScript knowledge and throw it at React irregardless of the fact that we're using TypeScript. So let's do a little bit of a dive here and try to figure out what's going on. To get started. I'm going to do a command click on React component so that we can look at its type definition file. So I'll do a command click. I get a little pop up window right here if you see the same one. Kind of hard to read the code inside of here. So you can just double click or single click on interface component over here and it will pull that open in a separate window. So we are extending class component. So let's let's look at its definition right here. First off, I want you to take notice that component is a generic class, right? So that class component is the same one we are extending right here. But you'll notice that the generic actually takes in two separate arguments. The first one is called P and the second one is S. So we already made use of that first generic argument. We said that it was the prop, so we expected our component to receive and we defined our interface at props right here. So the second generic argument is s which stands for State. So optionally we can provide an interface as the second generic argument any time we create a react component as a class and that's going to describe the state that we expect to have inside of our component. Now, how is that related to this problem that we're running into right here? Well, let me make it a little bit more clear. The current definition of class component, if you scroll way down. Says that state is going to be of type read only as that means that this is going to be a read only object of type se. And like we just said, S is going to be some generic type that we pass in. At this point, we have not passed in any type for SE. The default for SE inside of here is an empty object with no properties whatsoever. So that kind of partially explains the error message that we are seeing right here. The default state object inside of a component is an empty object. So there is no counter property there. There's no color, there's no number, no counter, nothing whatsoever. It's a plain empty object. And so if we try to access a property on an empty object, TypeScript is going to throw an error, which is what we're seeing right here. So why did our code that we have before work? Fine. But now this is not working correctly. Well, remember how extending a class works in TypeScript. Let's look at a quick diagram. Okay. So we've got our app component and previously we had a state property being defined inside of it. The component class that we are also extending has a state property as well. When our subclass component or when our subclass right here of app extends component, it redefined or it overrode the state property of component. So when we had that inline property definition like right here, when we said state equals counter zero, like so we were redefining the state property that had been already set up or defined over here inside of class component. So when we use this syntax, we are completely redefining the meaning of state inside of our component. We are 100% overriding this definition right here. So that's why we. Everything worked when we had this code. So why doesn't it work when we have just this? Well, because in this case, this state is understood by TypeScript to be that empty object. It's this right here. And we are not trying to define or override this property. We are trying to assign a new value to it. As a better way of taking a look at this. Let's try printing out this state like so now mouse over it and you'll see that it's printed up as being an empty object. So essentially an object with no properties to be assigned to it whatsoever. We can't assign any properties. We can't read any properties. There's nothing on this object whatsoever. But if we once again override the States property on our extended component or our subclass right here of EP by saying counter is zero like so if you now mouse over this state, we have now overridden the definition of state and side of component and now TypeScript understands that state is going to be an object with a counter that is a number. So this, in my opinion, has really surprising behavior. It makes a ton of sense. Don't get me wrong. When you really think about what's going on behind the scenes here, it makes a ton of sense. But you might recall that if you learned React back in the JavaScript world, you were probably told, possibly even by me in another course, that defining your state object with that syntax and that syntax is 100% equivalent. Well, that's not true in the world of TypeScript. In the world of TypeScript. These are two very different statements, and they're going to have two very different impacts. So you as developer essentially need to decide whether you want to use the constructor method of initializing your state object or using the property method of initializing a state object. It's totally up to you. You just have to pick one way or the other. Now I want to show you an example. I think that we already know how to override the state property easily enough. Let's take a look at an example of how to get this stuff to work. If we use the constructor method. So now that I deleted that state property. If we take a look at what this state is, again, it's back to being that empty object. And rather than really just being an empty object, remember, the React component wants state to be whatever that generic type se is that we passed in. We haven't passed in S yet, we've only passed in E, so let's try passing in some type of SW as the second argument to the component generic. So back over here. I'm going to define a new interface to describe my state object. So I call it App State. Is going to be a counter property that is a number. And now I'm going to pass that in as a second argument to the generic. So I'll say app state like so. And as soon as I do that, the error goes away because now if we write out this state one more time and mouse over it, TypeScript says, Oh, okay, this dot state, no problem. I understand it is an interface that follows app state or it is of type app state, I should say. So it has a counter property that is supposed to be a number. Okay. So essentially the lesson here once again is that we have to make a decision. Either we take this route of defining an interface to describe our state, has it in as a second argument to the generic and then initialize our state object inside the constructor. Either we do that or. We set up our separate state property like so and that overrides the state property defined inside of component. Now, the important thing to kind of take note of here, you can actually get into a funny scenario if you do both these things. So if you define the state property inside the class and also pass in this interface as the second argument to the generic, well, we're not using this interface anymore inside of class app, the definition of what state is or its type is going to be the property we provided right here. So let's say that we make this state object instead of being counter, let's say like number. Instead, as soon as we do that, we're going to see a ton of error messages appear because now TypeScript thinks that this state is an object that has a number property. So because we once again overrode state, the app state interface we just passed in as a generic is not being used. It's getting tossed out the window. So this entire system can really lead to some nasty bugs. So at this point, I don't really expect you to memorize all this stuff. I just want to point out that you have to choose one method or the other. And if you start to mix these different methods of defining your state on a component, well, you're going to run into some trouble. Okay. So enough of this. We're going to go with the easy route in general. So I'm going to delete the constructor. I'm going to delete the app state generic. And the interface as well. And then I'm going to fix up this number property to what it should be, which is counter and everything's back to working as usual. So like I said, there's some real complexity hiding here inside of our component. All right. So now that we have a better idea of how state works, let's take a quick pause and continue in the next video.
