# Connecting a Component to Redux

Now that we've got our reducers all set up, we need to make sure that we can make this list of reducers available to our app component so we can eventually render them on the screen. So inside of my components directory, I'm going to find the app file. So we need to do a couple of things inside of here. First off, we need to make sure that this component has access to the fetch to DOS Action Creator so we can actually fetch our list of to DOS when this component displays on the screen. We also need to make sure that we have a map state to props function that can take the list of to dos out of our Redux store and provide them to our app component as props. So let's get to it. We're going to first begin with a couple of import statements at the top. So we're going to import our connect function. From React Redux. We're going to import our to do. Interface and the fetch to dos action creator from up one directory actions. And then finally I'm also going to import store state as well from up one directory reducers. Remember we just created this interface right here. That interface describes our entire redox state. You'll see why we are importing that in just a moment. Okay. So now eventually we want to provide some different props to our app component. So to get started, let's define an interface that describes all the different props that we expect this component to receive. So right above it, I'm going to say interface app props. And then inside of here, we'll define all the different props. So first off, I definitely want my app component to get a list of all those different to DOS. So I'm going to assume that our props object is going to have a to DOS property that is going to be an array of to do objects. Second, I also expect my app component to get the fetch to DOS action creator. So I'm going to add in fetch to DOS. This is going to be a function. And for right now, I'm going to just give it no arguments and return type of any. We're going to come back and fix this up in just a moment. But for right now, all I'm trying to do here is indicate that fetch to dos is going to be some kind of function. And I don't really know what it's going to return. So like I said, we'll come back to that in just a moment. Now that we've defined an interface or the props that app is going to receive, we're going to wire that up to react component as our generic. Remember, the generic right here is going to help TypeScript understand what different properties we expect to be on our prop object. So I'll say app props like so. And now inside of my component, I should be able to reference stuff like this dot, props, dot and then see the fetch duties function and the to dos array as well. Okay. So let's make sure that that action creator and all that all those tattoos actually exist on the props object. So to do so, we have to create our map state to props function and wired up along with our action creator of fetch tattoos using connect. So down here I'll start with my map state to props function. So this is going to be coupled with our entire Redux store. So we need to provide an annotation for that right here. Well, remember, we just put together an interface called Store State, and that interface describes our entire Redux State object. So that's why we imported it into this file, because this first argument is going to be the entire state object. So I'm going to annotate it as so I'm going to say the state object is going to be of type app, state or excuse me, store state. So then inside of here, we want to pull off the tattoos property and return it inside of an object. So I'm going to say return to dos is state dot to dos. And if we want to, we can provide a type annotation on this function as well. So we could say that this is going to return some object that has it to dos property and the value of it is going to be an array of to dos. Or excuse me, just to do like so single. Let me collapse my sidebar so you can see that whole line. If you want to use a little bit of structuring out of this argument, because that's a really popular thing to do with our maps data, props function, we could change that to be simply to dos and then inside of our function body. This would be simply to dos like so. Okay. So that looks good. So now the last thing we have to do is wire everything up together using Connect. So we'll stay down here. Export. Connect. We need to provide a name for this. Let's not forget that we need to use some name that's actually different than app right here. One way to approach this would be to either just fall back to using export default or remember we try to not use that in TypeScript. So maybe a better way would be to call class app right here or app component underscore app like so just to avoid a name collision because remember we don't really want to export class app. As a matter of fact, I'm going to remove that export statement right now. Instead, we want to export the connected version of app. So let's do export const app down here and that's going to be the connected version. Now remember the crazy signature of Kinect. We've got the first set of parentheses for configuration, and then the second set for our. Underscore app component like so. Then the first argument I'm going to do a new line here is map state to props. And the second argument is going to be an object. Containing our fetch to news action creator. So we'll say fetch to tattoos like so. And that's pretty much it. So now we should be able to, inside of our component, define a lifecycle method and call fetch to dos and then see all those to dos show up on our prop object. Let's take a quick pause right here and take care of that in the next video.
