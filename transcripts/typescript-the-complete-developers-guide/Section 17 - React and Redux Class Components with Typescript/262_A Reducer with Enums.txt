# A Reducer with Enums

Now that we've created our action creator, we need to make sure that we have a reducer that's going to handle an action of Type two abuse action and essentially store a reference to all the different tutus that we've fetched. So inside of my reducers directory, I'm going to make a new file called to DOS Dots. All right. So inside of here, we're going to write out what looks like a very normal more or less reducer. So I'll say export cost to dos reducer. All right. Remember, the first argument is going to be our default state argument or state object for just this reducer. Chances are, I want this reducer to return an array of to do objects. So in order to express an array of to do objects, I need to first import that to do interface that we just created inside of actions index dots years that interface right here. So I'm going to import to do. From up one directory actions like so. And I'm getting an air around that, which is very unexpected. Oh, you know what? I think I forgot to put a expert on on that my mistake are going to make sure that I export, that I'm going to make sure that I export this other interface we just created as well, because we're going to need that in just a moment to. Okay. So that looks better. It's now back inside that reducer. For my default state argument, I'll say state is going to be of type to do array. And remember, this is only specifying the annotation or the type of state we still need to provide some default value for it. So to provide a default value for an argument, at the same time that we do a type annotation, we'll just say right after that equals and then the default value. So this is saying that state is going to be an array of to this. And if we don't provide in a state, then just use an empty array. Okay. So now next one is going to be our action. Now, here's where things start to get pretty darn interesting. Remember just a second ago we created inside of our actions index file an interface called fetch to DOS action. And this describes the exact structure of what we expect in action to be. It's going to have a type property that's fetched to dos and it's going to have a payload that is an array of to dos as well. So we could say right here that action is going to be of type, like object, know, like maybe we don't know. It's going to be an object. It's going to have some properties, but that's basically all we know. That's not super ideal. It would be super awesome if we could somehow tell this reducer that that second argument is always going to be an object of this structure right here. So let's import this interface into that reducer as well, and we'll use this interface as the annotation to that action argument. So back over here. I'm going to import. That's today's action. And I'll use that as the annotation for action. And when I save the file, I get the two arguments. New line just so they are easily legible. Okay. So this is pretty neat because now we know that that action argument is always going to be some object that has a payload that is an array of to dos. But there is one slight downside to this. Eventually, we're going to have a lot of different action objects or action types like different action objects really inside of our application. So right now we are saying that this is always only going to be fetch action or fetch to DOS action. So this might not be super ideal for the long run, but it's definitely going to work out right now. We're going to end up having to come back to this and make a little tweak to it in just a little bit. But we'll get to there in due time. All right. So now inside of a reducer, we can write out what we normally see inside of reducer bodies, which is a switch statement. So I'm going to switch over. Action type. And I'm going to say if action type, remember type right here, we know that right now it only has one possible value action. That type can only possibly be fetched to dos from our action types enum. So we essentially need to import that action type's enum inside of here. And we'll add in a case statement to watch to make sure that action type is actually equal, to fetch to dos. So at the top, I'm going to also import. Action types. From actions types. And then we're going to do case action types. Fetch to. So then inside of here, we are 100% confident that our action is going to be a fetch to DOS action because it has the correct type. So if that's the case, we're just going to return the array of to dos that we just fetched. So I'll say action dot payload like so. Now we need to also handle our default case. If we don't match with some given action type that we've listed out inside of here, then we're just going to return our state like so. All right. And that's pretty much it. Now, one thing that I want to point out here is that our typescript code is kind of lying to us at this point in time. Let me tell you what's going on. Remember that when Redux first boots up, it sends a series of kind of warming up actions to all the different reducers. It sends a couple of different action objects that have some randomized types on them to first test to make sure that none of our reducers respond incorrectly to an action, and secondly, to get the initial default value of state for each of our reducers as well. So in other words, redux despatches its own type of action to our reducers. But at present, we have applied a kind of incorrect annotation to this action argument. So we are saying that this action is always going to be of type fetch to DOS action. And like I said, that's not really super accurate because there will be other types of actions coming through here as well that are dispatched automatically by Redux. So the annotation on there really is kind of lying and we probably could do a better job of it. Nonetheless, we are going to handle those kind of extra extraneous types through the use of our switch statement. So it just kind of something to be aware of here where the invitation is not quite really describing 100% of reality. Okay. So that's our reducer. So let's take a quick pause right here. When we come back, the next section, we're going to wire this up to our combined reducers call and then get some information from our reducer into our app component. So quick break and I'll see you in just a minute.
