# Handling Component State

Now that we've seen props in action, let's try putting together another quick example and take a look at component level state as well. All right. So I'm going to try to change out this app component with a kind of simple counterexample. So I want to show two buttons on the screen, one to increment to counter in, one to decrement it as well. I'm going to first begin by initializing a state object inside of my class. So I'll say state is going to be an object with a counter property that's going to start off as zero like so. Next up, I'm going to refactor my render method. So I'm going to remove that div. I'm going to make a multi line expression. And inside of here is going to be a div with one button that says increment. In a second that says decrement. And then underneath that we'll print out the current counter. So this state counter like so. Now let's add in to event handlers to handle clicking on increment or decrement. So for the first button, whenever we have a click event, we will run this dot on increment. And for the second button on click will be this dot on decrement. So now we can add in those two method definitions. We're going to add these into the class as arrow functions because we need to make sure that as event handlers, these functions are bound. So let's say on increment equals like so. I'm going to make sure I put on a return annotation here. Avoid. And then inside of here we'll do this set state counter is going to be this state counter plus one. And now we can do the same thing for UN decrement as well. So we'll do a this state counter is this state counter minus one. Okay. So it looks simple enough. But I got to tell you, there's actually some really hidden complexity going on here. So let's save this really quickly. We're going to test out inside of our browser and we'll take a look at this code again and notice that something's a little bit strange in here. So I'm going to save this. I'm going to flip back over to my browser. There is my increment in decrement. Now remember, I run my browser really far zoomed in. So you probably see something like this right here. And I can click on increment decrement. Everything looks good. Okay. So like I said, there's some little hidden complexity inside of here. So let's take a quick pause and we'll come back in the next video.
