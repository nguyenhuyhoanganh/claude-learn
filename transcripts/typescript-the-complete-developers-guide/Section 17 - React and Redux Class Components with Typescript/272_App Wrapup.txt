# App Wrapup

Now that we've finished up our application, let's do a quick review of some of the high level concepts around getting React, Redux and TypeScript to work together. So the first thing that we learned is that any time we create a component, we will also define a interface that's going to define the different props that we expect our component to receive. So in this case, this interface is saying we expect our component to get a list of to dos, a fetch to dos, that is a function and delete. To do that is going to have that very specific type of delete to do. Remember that because we are using Redux and React Redux and Redux link together. All these things don't really behave that nicely together when it comes to those type definition files. And that's why we just use the keyword function right here. Next up, we also learned much earlier and we got a quick review of it in the last video, that there are two different ways that we can initialize state inside of our React component. So we can either set up an interface describing the structure of state and then pass that in as the second argument to our generic right here. And third, define and initialize our state object inside the constructor. Or if we do not want to define the constructor at all, we do not have to define that second interface. Instead we can just initialize our state object right in line. Either way is 100% appropriate. Totally comes down to what you want to do. All right. The next big thing we saw was the division of responsibility inside of our actions directory. So inside of here, we had a types file that listed out, as usual, all the different types of actions we have inside the action types enum. We're using an enum here. I want you to know that the official Redux documentation around working with TypeScript recommends using a type alias with some hardcoded strings. In my opinion, that's not a super great approach. Instead, using an enum is super appropriate for this particular use case because remember what an enum does. It contains a set of different values or properties that are all very similar in nature and are all somehow closely related. And we also know that they are a fixed set, and that definitely matches all the different action types that we expect to have inside of a Redux application. Inside this file, we also exported a type union between all the different action types we expect to ever have. And when I say action types, I mean like the actual action object itself. So any time we add in a reducer, you're going to definitely import this action type right here and use it to annotate your actual action argument. So with that in mind, let's take a look at our reducers to do its file. So inside of here, this is where we imported action and we use that to annotate the action argument. So now inside of the body of this reducer, TypeScript only knows that action is possibly any of those different actions. So in order to figure out what type of action it is, we used a type guard in the form of this switch statement right here. So inside of each of these different cases where you're essentially narrowing down the possible type, that action is to either a fetch to news action or a delete to do action. And because of this, inside of each of these different cases, we have 100% certainty about all the different properties of that action object. So, for example, action, not payload right here. We are very much confident that this is going to be an array of to dos. Okay. So that's pretty much it. That's the highlights of getting React and Redux and TypeScript to all work together. So once again, I hope you enjoyed this application and hope you learned a lot about working with React, Redux and TypeScript. So let's take a quick pause right here.
