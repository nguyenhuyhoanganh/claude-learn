# Expressing Actions as Type Union

Now that we've created our delete to do action and the appropriate action creator, we can flip back over to our reducer and make sure that it watches for that action. So I'm going to find my reducers to do its file. Here it is right here. The first thing we need to do is make sure that we import the appropriate action at the very top. So remember, we are creating an interface to describe each of the different actions we expect this reducer to see. We already created Fetch Studios Action and just a moment ago we also created Delete to do action. So this right here is not an actual object, it's not an actual action. It's instead a type. It's an interface. So now we can use this additional interface to annotate the action argument of our reducer. Right now, our reducer is only ever going to expect this action argument to be of type fetch to DOS action, but that's no longer the case. Now that action argument is going to be either fetch to dos action or delete to do action. So we can express that using a type union. Remember, we can set up a type union by saying Fetch to dos action and then saying or delete to do action like so. Now, as soon as I save this, we're going to get a big multi line. There we go. All right. So now inside of our reducer body, the action argument can be either of type fetched to dos action or delete to do action. Now that part is not that bad, but what is kind of bad about this is notice how this line right here, this type annotation, well, that's definitely going to start to grow in size very quickly. So let's imagine that we start to add in other types or other types of actions into our app. Maybe we would have like another or statement here of. Edit to do action and then create to do action. Yeah. That line would start to grow very quickly and be very, very large. So rather than expressing this type union right here, I think that we need to express the type union in some more elegant way, some location where we're not going to have like a really, really, really long line of code. So to do so, I'm going to flip back over to my actions. Typists file. Let me show you how we're going to fix this. Okay. So back over here at the very top, I'm going to import. Both of the action interfaces we've now created. So that's fetch to DOS action and delete to do action from the to DOS file. And then at the bottom, I'm going to export a new type alias called Action. And this is going to be our actual type union. So in other words, we're going to express the type union between all the different types of actions we have right here inside of this one type file, rather than trying to spread them out to all the different reducers we have. So let's say that type action is going to be either vetch to dos action or delete to do action. If we started to have additional types inside of our application or additional actions like let's say we had fetch blogs and maybe delete log. Then we can just add them as additional cases on this union right here so we could say or fetch blogs or delete blog like so. But in this case, we just have those original two. So now we can use this new type alias of action inside all of our different reducers. So I'll flip back over to do two to DOS. I can delete those two separate interface imports right there and replace it with simply action. And now action as an argument is going to be of type action. Okay. So that's a lot easier to read inside of our reducer. And it also means that as we start to add in more action types to our application, we don't have to flip back to every different reducer we have and start to update that type union. Okay, so now that we've updated this with our type alias, let's take a quick pause and finish up the actual implementation of our to DOS reducer in the next video.
