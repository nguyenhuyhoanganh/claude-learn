# Action Creators with Typescript

Now that we've got some base set up for our application, I think that the best place to get started would be to create an action creator that's going to reach out to that JSON placeholder API and attempt to fetch that list of to DOS. So let's very quickly go and review that API documentation, even though we've really definitely used it quite a bit throughout this course. So quick reminder, here's the link here on the screen. I'm going to navigate to that page. Here it is right here. I'm going to scroll down a bit. And find the resources section. There it is right there. And so inside of here is the to DOS endpoint. So we'll click on that and here we go. So if we make a get request to this URL right here, we're going to get an array of objects for each object has a user id id title and completed as well. So we need to use Axios to make request to this URL. Once we get a response, we need to dispatch an action from an action creator. Now, once again, I've said this about three times so far. I'm assuming that you've got a background in Redux, so hopefully none of this sounds too crazy. So let's get started. I'm going to first find my Z directory once again. And inside there I'm going to make a new folder. Called actions. And then inside of actions, I'll make an index file. So this file right here is going to eventually hold all of my action creators. So let's get started. Nice and easy. We will import Axios from Axios. And we're going to just pretend like we are writing out a normal action creator. Let's not try to overcomplicate this or anything. So I'm going to export constant batch to dos. Remember, we are making a network request here, which means this is going to be an asynchronous action creator. That means we have to make use of redux thunk. So rather than returning it in action from this function, we're going to instead return a function instead. Remember when we are making use of redux thunk. This function will be called with some dispatch. And that dispatch right there is the dispatch function from Redux, which allows us to arbitrarily dispatch actions as we please. As soon as we add in that argument, we're of course, going to see an error message because there is no type annotation for dispatch just yet. So we need to apply some type annotation to this and thus begins what is probably the most challenging part of getting React Redux and React Redux to work nicely with TypeScript. Here is the big trouble. Here's like one of the hardest things about it. You really have to study some of these type definition files and understand some of the types that are provided to us by these different libraries. So we need to provide an annotation or dispatch. Remember, dispatch is a function and off the top of my head, I sure don't know what different arguments dispatch takes and I don't know what types it takes either. So either we try to add in a manual type annotation right here for a function which would be probably pretty challenging. Or we have to go root through the type definition file of Redux or Redux thunk, and try to figure out exactly what the type signature of dispatch is or some type for it to use as an annotation. So to do so, let's add in an import for redux right here. And then I'll do a command click on it, and we can explore that type definition file. Okay. So here's the type definition file for Redux. So instead of here, we want to find some annotation or some type to use for the dispatch function. A very easy thing to do inside of here will be to just search for dispatch. And if we do so, I'm going to see very quickly that there actually is an interface for it. So inside the type definition file, there is a definition of exactly what the dispatch function is. So we don't have to try to rewrite this type ourselves. Instead, we're just going to import this interface right here and use it to annotate dispatch. So let's do that. I'm going to change that import statement to dispatch. From Redux like so, and I can use that as my annotation. All right, so that looks good. Next up, I'm going to mark this function as async so we can use async await inside of it. And we'll say Collins response is await Axios target. And then we need to put in our URL right here. So I'm going to flip back over to my browser. Here's the URL we want to make a request to. I'm going to copy it. I'm going to assign it to a variable inside of here called about simply URL. And I'll make the request to Earl. So once we get a response back, we can then do a dispatch. With an action object. So this thing is going to have some type. Let's try doing something like, I don't know, fetch to dos. And then we'll do a payload of response data. Okay. So take a look at this file right now. Besides this one single annotation, I just want to point out that it pretty much looks like normal JavaScript, which is kind of nice, but unfortunately that has some really big downsides. So at present we don't really have any type safety just about whatsoever going on inside this file. So let me point out a couple of things that are not quite so great. First off, we're getting back a response from that JSON API, but we have no idea exactly what structure of data got returned. So, for example, if I mouse over response data, we'll get an annotation of any. So we don't really know what data we are working with. Next up, take a look at the type we assigned right here as well. So we said fetch to dos as a hardcoded string. Remember, in just about every single application we've worked through in this course so far, we've been trying to avoid strings as much as possible because it's really easy to make a typo with them. So instead, maybe we should try creating like an enum or something like that to store all of our different action types. That sounds like a pretty good idea. And then finally notice how we're calling our dispatch function right here by mouse over it. You'll see that it looks like there's some big assumption about what we're going to pass in. Well, the dispatch function is actually a generic function. So when we call it, we can actually pass in some arguments or some generic type that's going to essentially describe the action object that we're passing in. Now, those first two things I mentioned, the response data and the string right here might sound reasonable. Maybe that thing I mentioned around dispatch doesn't quite make any sense just yet. So let's take a quick pause right here. In the next video, we're going to try to apply a type to our response data and create an enum to represent our action type. And we'll start to discuss this dispatch generic thing just a little bit more. So a quick pause and I'll see you in just a minute.
