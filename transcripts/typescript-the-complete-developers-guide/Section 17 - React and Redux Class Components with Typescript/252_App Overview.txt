# App Overview

In the last video we spoke about some of the pros and cons of using React and Redux together with TypeScript. Now, as a quick aside here, I'm only going to mention this one time. If you go to the Redux documentation, there's actually a guide over here about using Redux with TypeScript. I'll let you find this page on your own if you want to. I just want you to be aware that some of the recommendations on this page, in my opinion, are not super great. I guess. I don't know. I'm not really super excited about some of the recommendations they give you on this page. So in general, we're going to do a lot of things very similar to this guide, but there's going be a couple of other things that we do slightly differently, in my opinion, a better way. I'm going to leave it up to you to kind of compare and contrast the different ways, because this page very likely will change over time. So I don't really want to point out anything in particular on here. But again, I just want you to be aware that in some cases we're going to follow the recommendations here. In some cases, we're going to do things just a little bit differently. Okay. So with that in mind, let's take a look at a mockup of the application we're going to build. Now, this app is going to be super simple in nature because remember, this is, of course, about TypeScript, not React and redux. So we're going to be using just enough React and Redux to essentially understand how to work with TypeScript. So here's the app. When it loads up, we're going to show a single button on the screen. It's just going to say fetch. Whenever a user clicks on that button, we're going to display the text loading as we are making an API request. So whenever a user clicks on the button, we're going to make a request to fetch a list of to dos from that fake JSON API that we've made use of several times throughout this course. Once we fetch all those to dos, we're just going to show them on the screen. And then as a little twist here, any time we click on it to do, we're going to delete it. So if I say click on to do three, I just want to delete it out of that list. Like, so it's like I said, very simple application. However, the goal here is to understand how to make TypeScript and react and Redux work together. And this application is definitely going to fit the bill for that, even though it's pretty simple. All right. So let's take a pause right here. We'll come back the next video, and we're going to generate our application with Create React app and then start working. So I'll see you in just a minute.
