# Functional Components

We've now seen an example of growth, props and state instead of a class based component. And before we move on and actually start our application, there's one more quick thing I want to show you. And that's just how to define a functional component in general. It's going to be very similar to a normal JavaScript style functional component. The only difference is we're going to add on a couple of type annotations to it. So we're going to do a quick example here and then again actually start with our application. To get started. I'm going to take my entire class app and I'm just going to comment it out for just a moment. And then down. How about right above? Actually, because we're going to use this interface. Let's define a functional component called app. So let's say const app is going to be a function like so. Now remember, the argument to a functional component is going to be some props object. So it's same thing here. We're going to take in that props argument. We need to provide a type annotation. Once again, we expect this props object to satisfy the app props interface. So I'm going to annotate it as type app props. Like so. And then with a functional component, remember as much as possible, we'd like to not rely upon type inference, and this is something we spoke about way back at the very start of this course. So we can rely on type inference right here to figure out what we're going to return from the function. But I would highly recommend that you actually annotate the return type instead. So in this case, we need to essentially put on a return annotation here that indicates that we're going to return some JSON element. So to do so, we can add on our usual annotation. And the type that we're going to refer to is JSP X dot element like. So that's how we indicate that we're going to return some JSX from a function. We get an error as soon as we use that. That's just because our function is not actually returning anything just yet. So this would be our sign that we need to make sure that our app component is returning something. So let's add in a return and we'll do something simple here. Like props, dot color, like so. Okay. So that's pretty much it. Let's save this. I'm going to flip back over and I don't see anything right now. Oops. Little mistake on my end. Let's make sure back down here in our app component that we pass in some color property of like red. And once I do that. Yep. There we go. Okay. So again, I just want to very quickly show you a functional component definition. What we're going to do is define a normal functional component. We're just going to add in an annotation for that object. That's usually going to be an interface. And then we're going to add in a return type annotation that's usually going to be X element. All right, so this looks pretty good. So I'm going to delete this app component if you want to. You can comment it out or save it. But we're going to end up doing a lot of stuff inside this indexed file. So I would kind of recommend, just delete, deleting it for right now and I'm going to uncomment class up and we're back to what we have. All right. Just so you know, we are going to take another look at functional components later on as we start to work on this application. So don't worry, that's not the only taste of that stuff that you're going to get. Let's take a quick pause right here. When we come back in the next section, we'll start actually working on that original app that we discussed. It's a quick pause, and I'll see you in just a minute.
