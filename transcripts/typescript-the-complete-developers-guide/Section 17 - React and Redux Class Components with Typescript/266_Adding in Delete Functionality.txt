# Adding in Delete Functionality

Now that we can show a list of to dos to our users, we need to make sure that we can click on a single To-Do and have it be deleted off this list. So let's get to it. I'm going to flip back over to my code editor. We're going to go through the usual process of wiring up an action, an action creator, modifying our reducer and then attaching our action creator back to our app component. So it's the entire usual redux flow. So I'm going to first begin by closing down the APTs X file because we don't need to access that file until we eventually have our action creator ready to go. I'm then going to find my actions type file inside of here. I'll find the action type's enum and to it I'm going to add in a new action type of delete. To do so. Now we can create an action creator that's going to have this type. So I'll flip back over to my actions index file. Right underneath my interface of fetch tattoos, action. I'm going to create a very similar interface. Called Delete to do action. Remember the goal of defining these different interfaces that essentially model or set up an expectation of the structure that an action should have right now inside of our actions indexed file, we're using this fetch to use action interface to do a little type checking on what we call dispatch with. So the entire goal of setting up these interfaces is to make sure that we are returning actions from our action creators with the appropriate properties and the appropriate types. So my delete to do action will have a type of action types dot delete to do. And then a payload. I think that as the payload property, we don't need to provide the entire to do that we want to delete. Instead we can just provide the ID of the to do that we want to delete. So it's going to be a number like so. Okay. So now inside this file at the very bottom, I'm going to create a new action creator. Called Delete to do. I don't think that this needs to be an async action creator, so we're not going to use Redux Thunk in this case. Or the argument we're going to receive an ID that's going to be the ID of the to do that we want to delete and then we're going to eventually return an object that should conform to the delete excuse me, delete to do action interface. So delete to do action. Then inside of here we can return an object with the type of actions delete to do and a payload of the ID like so. And as soon as we put that stuff in that air message that we saw around, that expected return value goes away. Okay. So this is a good start. Let's take a quick pause right here. When we come back, the next video, I want to point out something kind of off about the file we are working in right now and we'll continue working our application. So quick pause and I'll see you in just a minute.
