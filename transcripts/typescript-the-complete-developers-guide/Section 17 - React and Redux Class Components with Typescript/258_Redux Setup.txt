# Redux Setup

All right. Now that we've got some of the basics of react with TypeScript put together, we essentially know how to define a class based component and a functional component. Let's start working on our actual application. So remember the overall goal here is to have that fetch button show the loading message and then show all those to do's. We're going to implement this using Redux. So we need to make sure that we install a couple dependencies as well. We're going to have to install Redux. We're going to have to install React Redux. And we're going to need Axios two in order to actually make the actual request to our API. So I'm going to change on over to my terminal and we're going to grab a couple of dependencies. Okay, I'm inside my project directory. I'm going to do an npm install. We're going to get Redux, react, Redux, Axios and Redux dash thunk. Redux thunk we're going to use to actually make that API request inside of an action creator. Now remember, if you're watching these videos, I'm assuming that you are familiar with Redux and Redux thunk. And like basically all these things. So if you're not familiar with them, well, I'm going to show you how to use TypeScript with them, but I'm only going to give you kind of a light discussion on how each of these things work. All right. So I'm going to install that. While that's running, I'm going to flip back over to my index.ts file and we're going to do a little bit more setup. So rather than defining a app component inside of here, let's instead just do our normal Redux store setup. So we're going to have to create a couple directories, create a couple files, create a dummy reducer, all that kind of stuff. And then we're going to coordinate it all inside this file. So we're going to have to do a pretty big amount of code here. Like lots of flipping around to different files. So let's get started right away I'm going to first begin by taking my interface app props and the app component. And I'm going to delete all that stuff. Next up I'm going to import, create, store and apply middleware from Redux. I'm going to get the provider tag from React Redux. I'm going to get thunk from Redux. Thunk. And I think that's just about good enough for right now. After that, we're going to do a store declaration, and we don't have any reducers just yet, but we will create some and import them into this file in just a moment. So this will be create store. With some reducers. Again, we don't have those yet, but we will in just a moment. And then I'll do apply middleware and pass in redux thunk like so. And then after that I'm going to find my app component right here. I'm going to wrap it with a provider. By doing a little multiline expression here. So here's my provider. I'm going to pass in the store as a prop. There we go. That's looking good. Okay so a couple errors inside of here. First off we got an error around reducers just because we don't have any yet. We've got an error around app because we need to recreate that app component. And then I've got an error around React Redux as well. And it has what's probably going to be a pretty familiar, uh, error message right here. So here's the story on type definition files. Remember some libraries out there? Some JavaScript libraries include type definition files. Examples of that would be like react redux Redux thunk. And we know that because we just installed Redux thunk. But we didn't install a separate type definition file even though we didn't install that separate file. If I do like a command click on thunk right now, it's going to show me a type definition file. So this means to me that we got a type definition file by default. So react Redux is one library where we need to separately install that type definition file. Remember how we go through that process. You can always mouse over this error message and I'll tell you exactly what to do. We have to do that npm install with at types slash. And then the name of the library, which in this case of course is react redux. So I'm going to copy that command. Flip back over to my terminal. And run that really quickly. These type definition files are always super tiny, so this should install rather quickly. And then once it is installed, we should be able to flip back over and see that error message go away. Perfect. All right. So now we have to create a couple more files. We got to create some reducer. We need to make an app component as well. To get started inside my src directory, I'm going to make a new folder called components. And inside there I'll make an app dot tsx file. Then inside of here we can import react from react. And I will export class app. Extends React.component. I'll define my render method inside there, and I'll return a div that once again just says hi there like so. All right. So that's going to be a good dummy application or app component to have. So let's make sure we import that back into Index.ts. Back over here. I'll do an import of app. From components. Okay. Looks good. Oops. Component slash app. My bad. I've still got an air around app right here. That's because I'm still passing in a prop of color red, even though our component doesn't expect to receive that. So I'm going to make sure I remove that prop. That's much better. Okay, so now the last thing we have to do is create some starting reducer for our application. So inside of my src folder I'm going to once again create a new directory. And I'm going to call this one reducers. Then inside of reducers, I'll make a new file called index.ts. Notice how we're just using a RTS this time, because we don't need to have any JSX inside of here. Inside of here we're going to import combine reducers from Redux. And then I'll do an export const reducers. And that's going to be combine reducers. And I'm going to pass into that some object with a silly starting reducer. So I'll say something like counter is going to be some function that always returns one. Now, this right here is just a kind of dummy reducer of sorts to get our application started. We're not going to have this reducer in the long run. I just want to start off with some silly reducer. Just so Redux thinks that we've got at least one reducer. If we don't have at least one, Redux will throw an error. Okay. So last thing we have to do is save this and then import that reducer back into our index.ts file. So I'll flip back over. I'll do an import reducers. From reducers, and that's pretty much it. Okay, lots of typing. Hopefully we didn't go too quickly, but just about everything inside of here is like normal React Redux application setup, so hopefully there's nothing too crazy that we just did. So now let's do a quick test. I'm going to save this. I'm going to flip on over to my terminal just to make sure I don't have any errors that my code editor is not surfacing. To me everything looks good. So I should be able to flip back over to my application and see. Hi there on the screen. Perfect. Okay, so now that we've got everything set up, we can start to focus on how to use TypeScript with Redux. So let's take a quick pause and start to tackle that in the next video.
