# Rendering a List

Now that we've got our connect function all set up, let's define a lifecycle method inside of our app component and make sure that we can actually call our fetch choose action creator. Remember, we eventually want to only call fetch dos once the user clicks on a button. But right now we're just going to use a lifecycle method to do a little bit of testing. Okay. So inside of my app component, I'm going to define component did mount. And then inside of here we will call this dot prop dot fetch to dos like so. So now let's save this. We will flip back over. I'm going to open up my request log right here and I'm just going to make sure that I see a request going out to that JSON server. And there it is right there. So I should see a big old list of to dos inside there in total 200. That's a lot of to dos. We'll eventually work on rendering them onto the screen. Now, this only proves that we are making a request to get that list of to dos. We also have to make sure that they are being stored inside of our Redux store. So to make sure that inside my render method, I'm going to add in a console log of this prop to dos. Now I'll save this flip back over once again. And if I open up my console. Okay. There we go. Right there. So without a doubt, our Redux store is making the request. Or I should say the action creator is making the request, and then we're catching the list of to dos inside of our store. So now that we know that we've got some bit of code on the Redux side that is working well, let's work on fixing up our app component just a little bit. So we want to show a button inside of here that's going to have a click event handler whenever we click the button. Like I said, we want to call that action creator. So to get started, I'm going to clean up that console log. I'm going to clean up the component didn't mount. And then I'm going to replace the text of Hi there with a new button element. And we'll give the button element the text fetch. All right, now we can wire up a on click event handler. So I will call this this dot on button click. And don't forget, this is an event handler. So we have to set up this method inside of our class app as a bound function. So I'll define on button click as an arrow function like so. So I don't expect this function to return any arguments. So I'm going to edit annotate it as void like so. Then inside of here, we can call this props. Fetch to dos. Okay. So that looks good. So now we know, while we are reasonably sure that that's going to fetch our list of tattoos and give us access to the list of tattoos inside this component. So now the next thing we need to do is make sure that we can render this list inside of this component as well. So I'm going to add in another helper method inside of here and I'm going to call it Render List. Now, here's the interesting thing. In this case, we need to add in a helper method that's going to iterate over that list of to dos and return a JS element for each one. And so in other words, I think that we want to return an array of JS elements. So as our return type annotation on here, I'm going to say JS X element and we're going to return an array of them like so. Then inside of here, we can do a return. This dot props to dos dot map. So we're going to iterate over each to do that is a to do. And inside of here we can return a div. And we'll just print out to do. Oh man that autocomplete really kills me sometimes to do. Dot title. There we go. And I'll make sure that I get a key property on here as well. Let's not forget that. So I'll give this thing a key. Of to do dot I'd. And I'll make sure that I reference that helper method of render list from right underneath that button. So I'll call this dot render list. Okay. So it looks pretty good. I'm going to save this. I'm going to flip back over and we'll do a quick test. Now, you'll notice that I've got a little warning right here. That's just from the autocomplete that accidentally kicked in there. I'm going to clean up the import statement that was added from that autocomplete, and now that warning will go away. That's better. All right. So to test this, I'll click on fetch and there we go. I can see my list of to dos on the screen. So that's pretty much our basic functionality. Everything put together. All we have to do now is add in the ability to click on it to do and delete it off this list. So let's take a quick pause right here and tackle that in the next video.
