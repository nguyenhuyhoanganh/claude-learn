# Simple Components

In the last video we generated our new project. So I'm not going to change into that new project directory of Rrts and then start up my code editor inside there. Now. Right away you'll notice that our project looks like a normal React project, but if you open up that CRC directory, you're going to see a couple of files inside of here with a weird extension. So I see APTs. What is that? A file is any typescript file that also contains some amount of sex inside of a react project. Any time we create a component file. So like a file that is meant to create a react component, we're going to use that extension of TSX. If we don't expect to place any JS in a file, we'll use the usual dots instead. To get started. I want to delete the entire CRC directory because I want to start 100% from scratch. So I'm going to delete that folder. And then I'll create a brand new CRC directory and inside there I'll make an index file. Let's just try to create a very simple component and get it to show up on the screen. So inside of here, I'm going to import React from React. I'm going to import React Dom. From React Dom. Well, then create a class based component that I'll call app. And as usual, this is going to extend React component. I'll give this thing a render method that returns just a little bit of plain. So I'll just do a div that says Hi there. Okay. Now that we've got our component, we can attempt to render this inside the DOM. So we will call, react, dom render. Our first argument is going to be our app component. And then the second argument is going to be a reference to the DOM node that we want to render our application to. So as a quick reminder inside of the public industry indexed HTML file, there's a different side of here with an idea of root, and that's where we attempt to render our application too. So we just need to get a reference to that element. So as a second element or second argument, I'm going a new line. This just you can see it more easily. We'll pass in document query selector lb route. And that's it. So this is another great lesson in getting TypeScript to work with some JavaScript library. Take a look at this file. Do you see anything inside of here that looks typescript specific? No, not really at all. Now, TypeScript is running inside of here. We can mouse over different methods and we'll find a definition or a type annotation of what a different method is, or maybe what the react component class is. Or even react dom render right here as well. So we don't always have to go 100% overboard with the typescript. And we've seen this many times throughout the express application we worked on as well. It's a really quickly, let's now save this file. I'm then going to flip back over to my terminal and start up the development server just to make sure everything works. So I'll do an NPM start. That should start up my React development server now as a quick reminder if you are still running. The Express application, you might see a message saying, hey, port 3000 in use. So if you see that message, you can go stop that running express server and then restart the React application. If you don't do that, then. Create React app is just going to take a different port instead. So for me, Create React app is available on localhost 3000. So I'm going to go over to my browser and sure enough. Yep, I see. Hi there. Okay. So let's take a quick pause right here and continue working on our setup in the next video.
