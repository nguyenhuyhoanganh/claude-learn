# The Generic Dispatch Function

In this video. I want to address the dispatch function right here. So I'm going to show you an extra little very optional step that you can add in. And this optional step is going to give you a little bit better ear checking inside of your action creators so you do not strictly have to do the step, but I recommend that you do it right here. Actually, I'm going to tell you, please add in the code we're about to add in because we're actually going to use it for something else in just a little bit. But in your own application, this will be something that is optional. So we're going to add in a new interface inside this file. And the goal of the interface is to describe this action object right here. And then once we create that interface, I'll very quickly show you why we are creating it. So I'm going to make a new interface right underneath to do. So let's say interface fetch to DOS action. And again, this interface is meant to describe this action object right here. So it's definitely going to have to have a type property. And we want the type for this particular action to always be 100% of time. Fetch types. Fetch to dos or schema action types. Fetch to dos. So I'll say action types. Fetch to dos. Like so. And then on the payload property, first off, we expect there to be a payload property and we really want the value of that or the type of that property to be an array of to dos. So we'll say payload is to do array like so. All right, now here's the magic. We've created an interface to describe that action. Here's the optional step. If I want to write after dispatch, this is a generic function so I can pass in an interface that describes the argument that I expect to be passed into dispatch. Now that's a lot of words. So let's just add in the code and I'll tell you about exactly what this is doing. So I'll say fetch to use action like so. So why did we do that? Well, the answer is very simple. Sometimes when you work on an action crater, particularly with Redux Thunk, you might end up with a ton of code inside of here. And it can very quickly become really confusing about exactly what you're doing. So you might end up having like several requests being made and then maybe like an if statement or something like that and maybe some other async stuff or a wait stuff inside of here. Who knows what else? Essentially, at the end of the day, you can have some pretty confusing action creators. So what this generic type right here does is make sure that you are always passing in an object with the correct types and properties. So let me clean up all this extra stuff really quickly and show you how this works. Remember, our interface is saying that this thing must have a type and a payload that is an array of to dos. So what would happen if I accidentally made a typo and instead said, Well, I want to return a payload of some like random string right there? If I did that, I'm going to very quickly get an error message and it says, Hey, you provided a string, but we expected an array of to dos. Likewise, if I accidentally. Enter in some kind of other action type, like let's say we put in some other action type like I don't know. Remote tattoos or something like that. Now, if we do that, we'll get an error around the enum. Let's imagine I'm going to flip over to my enum file really quickly and just add that in as a new enum. So now if I go back over here once again, we'll see an error message and it says, hey, we were expecting fetch to dos, but you put in promote to dos. So the goal here or the lesson here is that if we add in this very optional generic right here, TypeScript is just going to make sure that our action object has the correct properties and the correct types as well. It's just an extra little step to make sure that we are doing the correct thing and inside all of our different action creators. And this is particularly useful if you have really complicated action creators that, like I said, are making multiple requests or something like that. Okay. So that's what's going on. Now really quickly, I'm going to change back my action type here to fetch to dos. And I'm going to remove that promote tattoos from the enum as well. Like so. All right, so I can't say enough. It is optional. You don't have to do that if you want to. Don't want to. But I highly recommend you do it. The reason I highly recommend will obviously you get some additional type safety, but as you're going to very quickly learn, reading this interface to describe an action is something that we really want to do anyways because we're going to use that action or excuse me, this interface right here in just a moment inside one of our reducers. And this is going to be the thing that really ties together our different reducers and these action creators in a really neat way. So let's take a quick pause right here and continue in the next video.
