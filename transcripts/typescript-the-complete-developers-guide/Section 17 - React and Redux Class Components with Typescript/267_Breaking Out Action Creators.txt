# Breaking Out Action Creators

I want to point out something kind of interesting about our actions index file right now. Inside of a normal Redux JavaScript application. So no TypeScript at all. We would only have a bunch of action creators inside this file. So for example, we would only have inside of indexes fetch to dos and delete to do. But when we start using TypeScript in Redux together, our indexed file is going to instead balloon in size and start to be just about twice as large as our normal definition of what a bunch of different action creators are. And that's because we have to define all of these different interfaces. So as you can imagine, when we start using TypeScript and Redux together, the size of our indexed file is going to grow to be quite large. And with that in mind, I highly recommend trying to break apart all the different action creators inside of your application into separate files. A very easy way to do this is to create a separate file for each different kind of category of action creator you have. Right now, our application has a single resource called to DOS. So I think that we should create another file inside of our actions directory called to DOS and we should throw inside of there all of our different interfaces and action creators related to to DOS. So let's try doing that right now. Inside of my actions directory, I'll make a new file called to DOS. It's. I'll then flip back over to index keys. I'm going to select everything inside of here. I'm going to cut it and then paste it inside of. To. Like so. So essentially all we did here was remove all of our logic over to the second file. Now the benefit to this is that now we can use our indexed file as kind of a central export point for everything inside of our actions directory. So inside of here we can say export star from the to DOS file and export star from types. So now if any other part of our application needs anything related to actions or action creators, all we have to do is import the actions directory and that will give us access to everything inside of today's TVs and typists as well. To show you exactly how that works in action. Let's open up our reducers to News File really quickly. So here's our reducer right here. Now, rather than having these two separate import statements, we can condense it down to just one import statement from the actions folder. So in other words I can move this action type's import to be also coming from the actions directory. And then I can delete the second import statement like so. Okay. So that's a pretty good improvement. Now in this application, it's kind of hard to see exactly how this pattern would scale because we only have one type of resource. But you can kind of imagine that if we had other types of resources like, say, maybe users or blog posts or images, we could create a separate file inside of our actions directory to work with each of those different types of resources. And each file would have its own separate set of interfaces and action creators, and we could use our index file as the central export spot for all those different files. Okay. So now that we fix this up, let's take another quick pause and I'll see you in just a minute.
