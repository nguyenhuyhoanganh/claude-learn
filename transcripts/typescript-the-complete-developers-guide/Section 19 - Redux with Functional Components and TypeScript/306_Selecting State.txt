# Selecting State

We need to get access to some state that is stored inside of our Redux store, inside of our repositories list component. To do so, we are going to use another hook from the React Redux library. At the very top I will import use selector from React Redux. This hook is similar in nature to the mapstatetoprops function that you might be used to if you have ever made use of class based components before. So as a very quick example of how we use this hook, we could write out inside of here, const State is use selector. I'm then going to call that with a function that is going to receive all the state out of our Redux store. And I'm going to call that simply state. I'm then going to immediately return it from that function. After that, I will then do a console log of whatever we get back. So let's just run this and see exactly what happens. So I'm gonna do a quick refresh and will immediately see a console log. So this is all the state that is inside of our store. It, of course has the repositories key and there's the output from our repositories reducer. So we have no data error of null loading a false. I'm going to try to search for React. So I'll type in React. I'm going to clear my console and do a search. Here's my console log. I'm going to take a look at the last entry right there. And now I can see my data property has a list of 20 different packages that we can print up onto the screen. Naturally, we probably don't want to get this entire repositories piece of state. We probably just want some individual pieces. So in other words, inside of our component itself, we don't need this entire giant object. We really just need these pieces of state just loading. Just air and just data. So to make sure that we only get those very small pieces of state that we really care about, we can select some piece of state. That's what this function right here is really all about. This function is going to receive our entire state object out of Redux. Then whatever we return from, here is what we're going to eventually get out on the other side. So again, you can really think of this as being very similar to the map state to props function. So in our case, all we really care about is state dot repositories. That's it. We don't really care about anything else. But as soon as we add that in, unfortunately, we get a little error message. So what's going on here? Well, for just a moment, I'm going to first annotate state as any. I just want to make sure that this code works and then we'll start to approach the TypeScript side of things. So I'm going to save this now. Flip back over. And now if I refresh, I will see that now we are only getting that repositories piece of state. So just loading error and data. It's now just really quickly, we can destructure out data error and loading and then eventually make use of those different properties inside of our component. But before we do so, we probably, of course want to understand why are we seeing an error message whenever we try to reference repositories? Well, I'm going to remove that type annotation of any really quickly and then we'll take a look at the error message again. So I'm going to hover over repositories and you'll notice it says property repositories does not exist on type default route state. So this right here is a little bit of a pain point around using the React Redux library. Whenever you make use of use selector. Use selector really has no idea whatsoever about what type of data is inside of your Redux store. No information right now is being communicated to TypeScript from our Kind of Redux side of things over to this React Redux side of things. So you and I have to write in a little bit of additional code to help React Redux, understand what type of data or really what type this argument is right here. This extra little bit of code is just a little bit frustrating to have to write because it's a little confusing exactly what we have to do and it kind of feels like something that should be kind of done behind the scenes automatically. Unfortunately, automating this process would be really challenging. So this is why you and I have to write out just a little bit of additional code to get everything working as expected. So let's take a pause right here. Come back in the next video, we're going to write out this additional little bit of code and really understand what it is doing behind the scenes.
