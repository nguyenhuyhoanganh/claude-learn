# App Overview

In this section, we're going to start to take a look at making Redux and TypeScript work together. We're going to be working on a very slightly larger application in this section. It's still a very small app, don't get me wrong, but we're going to be very much focused on the Redux side of things, and I'm going to try to give you kind of a project template that you can very easily use in future projects as well. So let's first begin by taking a look at a very quick mock up of this very small application we're going to build. All right, here we go. So like I said, it is very small, very simple. I don't really care about the UI side of things. This is really all about understanding Redux and TypeScript together. So our application is going to show a very simple text input to a user. A user is then going to type in some package name inside there and then click on a submit button. Once they click on that button, we're going to take that search term and we're going to do a search of the NPM API. We're going to try to find all different packages named React. We'll then get all the different packages with that name or somehow related to that name, and then we'll just print up the name of all those packages on the screen. So essentially, just a very simple let's make a request to get back some data, take that data, show it on the screen, and that's it. Now, again, I know this seems like just the smallest, most inconsequential thing, but trust me, along the way, you are going to be shocked, absolutely shocked at the amount of code that we're going to have to write if we want to really implement this correctly in the Redux side of things when it comes to kind of TypeScript. Okay, so let's go back over to our terminal. Let's generate a new project and then start installing a couple of dependencies. So I'm back inside of a workspace directory of sorts. I'm going to run Npx, create React app. I'm going to name this Project Redux Dash TS. And then a dash dash template TypeScript like so. As usual, this is going to take a couple of minutes to install, so I'm going to let it do its thing. Come back in the next video. We're going to add in a couple more packages to kind of handle Redux and all that kind of stuff. And we'll start working on our application.
