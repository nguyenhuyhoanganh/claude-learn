# Quick Wrapup - Final Lecture

All right, my friends, that's it for this project. So I just want to wrap up on it very quickly. A couple of big items I really want to point out around some kind of awkward points when we start working with Redux and TypeScript together. And some of this is not from Redux directly. Instead it might be from the React Redux Library. So the first thing I want to point out is that we did a really kind of precise job of segmenting off a lot of different things inside of separate directories, whereas previous Redux projects you might have worked on with just JavaScript, sometimes combined stuff like action types, action creators and actions all into one single file. So we created all these separate directories just to make sure it was really crystal clear what was importing, what, what was exporting what, and so on. We also made sure that whenever we needed to get access to some redux related stuff from our React application, we made sure that we imported all that stuff from the root index file inside that state directory. So that was here's the state directory Index.ts right here. We did not import anything directly from files or folders inside of state. Instead, all of our imports for redux related stuff were coming from Index.ts right here. Next up, we saw that communicating some type information over to our components could be a little bit challenging. And that's where kind of ranted for just a little bit. I apologize for that. Ranted just a little bit around how we had to write out just that extra little bit of code to communicate some type information about the structure of data inside of our Redux store. Over to React Redux. There's not much more to say than that. Pretty much if you want to make use of TypeScript in React Redux together and use that use selector hook, pretty much you're going to want to create a file that looks like this right here. And then finally, last thing I want to mention, we didn't really go too much into depth or too much in depth on some of the type definition files for Redux and React Redux. But if you start going into those type definition files, in some cases they're kind of like this type stuff, just a little bit overengineered. So reading those type definition files is sometimes a little bit challenging. One thing that we kind of sidestepped here in particular that I want to point out was inside of our action Creators Index.ts file, we made use of an import directly from Redux. So we imported the dispatch type from Redux, and we use that to apply a type to the dispatch function right here. Another way that we could have accomplished this was by importing. Redux, thunk. I'm just going to write that out like so, and then do a command, click on Redux Thunk and it will take us to the type definition file for Redux Thunk. So inside of here are a couple of different types to describe action creators that we might write out using Redux. Thunk. So these are pretty much types that we would apply to like search repositories itself. But I'm going to be honest with you, understanding what these types are doing is really challenging and debatably maybe just a little bit overengineered, you might say. Kind of. So using the Redux thunk type definition files is kind of challenging. So a workaround is to either just, hey, say I'm going to annotate everything as any not really recommended. So you can either try to kind of break things down into smaller types, which is what we really did by importing dispatch from Redux. Or alternatively, it is also technically possible to write out our own types to describe a lot of things that are going on here. So that's it. Just some high level items I want to mention very quickly around this project.
