# Calling an Action Creator

Inside of our onsubmit function. We now want to take this term piece of state right here and use it to call our action creator of search repositories. So that's what we're going to focus on in this video. We're going to first focus on how we just do this with a kind of normal JavaScript approach. And then as usual, we'll take a look at some ways to improve it with some typescript. So I'm going to first get started by adding in an import statement at the very top of use dispatch from. React redux. Now, if you are not familiar with this hook right here, this is a hook coming from the you guessed it, React Redux Library. It gives us access to the dispatch function directly inside of a component. So we can use this function to manually dispatch an action creator. So just importing use dispatch by itself isn't enough. We also have to also import our action creator too. Remember, we have already exported all of our different action creators from the state Index.ts file. We exported something inside of here called Action Creators so we can import action creators into repositories list and that will give us access to all the different action creators we have created. So back inside of our component, I will import. Action creators. From up one directory state. Then inside of our component, we're going to first use the use dispatch hook to get a reference to the dispatch function. So for that we will say dispatch is use dispatch like so. So again, this is the same kind of dispatch function that we have access to inside of Redux Thunk functions so we can call dispatch and provide whatever action creator we want to eventually invoke. So I will do a dispatch and I'm going to pass into that action creators, dot search repositories and then pass in term. And I want to point out right away how crazy this line of code looks. This is just way just a lot of code to just dispatch an action. So this is probably going to work just fine. But again, I just want to point out, this doesn't look great. And surely we can probably think of some way to somehow condense this and make it just a little bit more convenient to eventually call some kind of action creator. And yeah, don't worry, we're definitely going to do that. First, however, let's just save this and make sure that we can actually call this action creator, make sure we can see some network request going off to the API. Make sure that we get back some information and make sure that we do not get any kind of big error messages. So I'm going to refresh the page. I'll search for React, and I'm going to also pull open my network request tab. And I can very easily see. Yep, there's my request right there. Off to NPM and presumably I'm going to get back some response that has a bunch of all the found packages inside of it. So I would say that definitely looks pretty good. It looks like we are able to call the action creator. But as I mentioned, yeah, this code almost laughable. It's just not great. So we definitely need to somehow improve this a little bit and make sure that it's just a little bit more convenient to actually call one of our action creators.
