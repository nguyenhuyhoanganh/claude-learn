# Adding Request Logic

Let's add in some logic inside of our try block to use Axios to make a request over to the API. To do so, I'm going to get some data back from Axios. I'll do an await Axios dot get and I'll put in our URL right there. So for our URL we'll write in https colon slash slash registry, NPM js.org and then a slash. A dash. Another slash. V1 search. We then have to add on a query string to indicate what library we are actually searching for. So as a second argument, I'm going to put in an object. I'm going to add in a params property to it. And then inside there I will provide text and the value for text is going to come from term. So again, params right here is what's going to get our query string into that URL. Now we can take this data right here and do some processing on it and pull off all the names of these different libraries. As a very quick reminder, the information that we get back from this API, let's just make a request over once again, if I can find. Browser window that still has that request inside of it. Here it is. I'm going to pull it on the screen. Here we go. So here's the response. We get back from the API. So we want to go into that data property that we just destructured off that object that we get back from Axios. I want to get the objects property that's going to be an array of objects and each of those objects has a package property. So off of that, that package property we want to get off name and that will give us the name of the library itself. So. Back over inside of our code. We really want to take a look at data. Dot objects. I want to map over that. The map statement is going to give us this object right here. So out of that object, I want to get the package property name. So I'm going to get an argument that will call result. I'm then going to return result.package.name. I'm getting an error from result right here. I'm going to annotate this one as any for just a moment. Then we'll take this entire mapped array right here that should have all of our package names. I'm going to assign it to a variable that I'll call simply names. And then finally, we will dispatch an action. So in this case, we want to dispatch an action with type action type dot search repository success and our payload will be names. And that should be it. Okay, so let's save this. This action creator looks pretty reasonable. I think the last big thing we have to do inside of here is just make sure that we export the action creator itself. So I'll put in an export const search repositories. You will notice that we are currently not making use of this action import. We will in just a moment. So let's take a quick pause right here. Now that the action creator is all put together and want to take a look at some ways we can improve this action. Creator in just a moment.
