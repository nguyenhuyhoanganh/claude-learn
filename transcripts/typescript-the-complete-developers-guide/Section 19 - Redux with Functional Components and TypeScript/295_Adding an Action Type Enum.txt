# Adding an Action Type Enum

The solution we have put together now allows TypeScript to take a look inside of each of our different cases and make sure that we are using the action in an appropriate way. So, for example, inside this search repository success case right here, TypeScript knows that the action is of type search repository success action, which means it has a payload property that is an array of strings. If we take that payload property and try to assign it to air right here like so, we'll get an error because now TypeScript is saying you are trying to take an array of strings and assign it to a property that is supposed to be either a string or null. So this is pretty good. We have added in some reasonable amount of type checking inside of our reducers. There's just a couple of kind of usability issues inside of here now. First off, adding in this gigantic type annotation for our action argument is going to get pretty tedious really quickly. So another way that we might decide to write all this stuff out is right above our reducer. Say type action is going to be and then copy over or really cut and paste this kind of or really it's a type union is what we call this. So I'm going to cut all that and paste it in like so. So we've now got a new type inside of our application called Action, and this represents all different possible actions that can be processed by all of our different reducers. Now I'm going to use that add action like so. And that's pretty much it. The other thing that I would really like to somehow improve just a little bit, just like inside of a normal Redux JavaScript project, we have now duplicated some strings inside of our reducers and up inside of our interfaces as well. And whenever we start to put together our actual action creators, we're going to need to use these same strings over inside those action creators, too. So I think that duplicating all these strings and all these different locations probably not a good idea. So to handle this in a slightly better fashion, once again, right above the reducer, I'm going to add in an enum called Action Type. If you're not familiar with an enum, it's essentially kind of like an object. It sets up a variety of different properties that all have a very closely related definition or very closely related meaning. So inside of our enum, we're going to list out all the different possible action types that we're going to have across all different actions, all different action creators and all different reducers. So inside of here, I'm going to write out search repositories and if we want to be like, Really redux here, we would kind of put in search repositories, all capitals. Like. So it is not required to do all capitals. Again, that's just kind of convention and I'm going to set that equal to search. Repositories comma at the end. We'll then repeat that for our other two action types. So search. Repositories. Success. And then finally search. Repositories. Air. As well. Now again, just a little bit more on enums. If we ever want to refer to these different properties and access the strings we've assigned to them, we would do something like action type dot search repositories or action type. Dot search repositories success. So now we can use this enum to access all these different action types rather than writing out the raw strings, which again is usually bad because we're just duplicating these strings all around and it's very easy to make a typo inside them. So to make use of that enum, we could then go down to our switch statement and replace all these strings with something like action type dot search repositories. Action Typekit Search Repository Success. And then action type search repositories air. I'm also going to clean up that comment right there because that was just there for informational purposes. Okay, that's definitely a good improvement. So now the last thing that we can do. Is referred to the enum for finding the actual values of these strings inside of our three different interfaces as well. So again, rather than hard coding in these strings to these interfaces, for the first type, we could put in action type dot search repositories. Action. Typekit Search repositories Success. And action Typekit Search repositories air. All right. That's pretty much it. So this file right here is a really good example of how to set up a reducer, how to type our different actions. How to apply those action types inside of a reducer. How to set up an action type enum and make use of that enum inside of a reducer. And our action type interfaces as well. So this right now is a pretty, pretty good example. I'm pretty happy with this. The one improvement that I would like to make to it is to extract these interface definitions and extract the action types, enum into separate files just to make sure that we could reuse this enum inside of other reducers very easily and also make sure that we can access this enum inside of our different action creators as well. We will eventually want to also make use of these interfaces inside of our action creators to. The last thing we're going to do is take all the stuff that we have now created and split it up into a couple of different files and folders based upon exactly what part of our redux side of things it actually implements. So let's take care of this little reorganization in the next video.
