# Creating a Typed Selector

All right, Let's take care of part two of making sure that we communicate some type information over to React Redux. Once again, we're going to be essentially doing what it says inside the official documentation to apply some type stuff. So a lot of this code is going to be kind of confusing, kind of weird, going to feel a little bit awkward. But again, it's just kind of what we have to write out. So to get started inside of my hooks directory, I'm going to make a new file called Use Typed Selector Dot ts. Then inside of here we are going to import. Use selector. And. Typed use selector hook. From React Redux. I'm also going to import. That root state type that we just put together a moment ago from up one directory state. And then finally I'm going to export const. Use typed selector. I'm going to put on a type annotation to it of typed use. Selector hook. Angle brackets. Root state. Equals. Use selector. Yeah, it just feels awkward. I'm pretty convinced there's a better way to do this. But this is what the documentation says. And in general, I like to kind of stick with the documentation. So now, rather than ever using use selector to try to get some state out of our store, we're going to instead use this thing called use typed selector. Its name is just meant to imply that, well, this has some type information applied to it. We do not really have to call this use type selector. We could call it use selector and then just import this thing up here as something else. If you don't like having to call this thing, use type selector. That's personally what I do. I personally rename use selector to just something like as underscore use selector. And then use that over here. And now I can call the thing that I export use selector. So now I don't even really have to worry about the fact that I'm using use type selector because that's just kind of an awkward name. So if you want to do this, totally fine. But again, documentation is going to say something closer to what I had to start with, something like this right here. Okay, So now we're going to go back over to our component. And instead of making use of use selector, we're going to use use type selector. So back over inside of our component, I'm going to delete the import for use selector and instead get use typed selector. From hooks use type selector. And then I'll use that thing in place of use selector right here. So now, somewhat magically, if I hover over state, it correctly describes or is annotated as the type of state inside of our Redux store. So it's something that has a repositories key that points at some object that satisfies the interface of repositories state. So now I can very easily take a look at state dot repositories, return that entire object and then destructure out of it data error and loading. And if you mouse over data, air and loading, you'll notice that they all have the correct type annotations on them. So data is an array of strings. Air is string or null and loading is a boolean. So that's it. Again, I know this like this video in the last one. I really wish that I could make a little bit more compelling and wish I could better explain what's going on. But there's kind of not a lot to say beyond, well, this is what we got to do to communicate some type of information over. So I apologize for kind of whining about this a little bit, but it's just kind of a sore spot for me just because, again, I feel like some of these libraries kind of make typescript and integration just a little bit unnecessarily difficult. But that's just life. It's just what we have to deal with in the JavaScript world. Anyways, back on topic. So last thing we have to do is make use of data error and loading inside this component. Let's take care of that in just a moment.
