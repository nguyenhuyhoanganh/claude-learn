# Adding Action Creators

The next big thing we're going to take care of is making this action creator that will make a request off to the API. So we're going to call this thing search repositories and we're going to pass to it some search term. That's going to be a string. So let's get to it. Back inside of my code editor. I'm going to find my state directory. I'm going to make a new folder inside there and I will call this action creators. And then inside there, I will make an index file. Now, once again, we're going to write our action creator directly inside this file. But if we started to grow our application in size, I could very easily create another file inside this directory called something like Repository action creator or something like that. Something that kind of indicates what's inside there and then start to create all these different action creators we want to have inside of separate files inside that directory so we don't necessarily have to create all of our action creators inside of Index.ts just in this case, because we've only got one kind of makes sense to just write it out directly inside of here. Okay. So then inside of Index.tsx at the very top, I'm going to import a couple of different things to get started. First, I'm going to get Axios from Axios because we're going to use that to actually make our network request. I'm then going to import. Action types. Or maybe it's just action type is what we called it from up one directory action types. And then finally. Action from up one directory. Actions. So then inside of here, we're going to write out our action creator. We are going to be making use of Redux Thunk in order to write out an asynchronous action creator, because of course, we do have to make a network request. So I'm going to write out search. Repositories. That's the name of our action creator. It's going to take some kind of search term. I'm going to annotate that as a string. So that's going to be the name of the library that we are searching for. So react, react, Dom, whatever else. Then, because we are making use of Redux, Thunk, I'm going to return an async function. That will be called with the dispatch function. And for right now I'm going to annotate dispatch as any. So remember dispatch right here, this is how we manually dispatch actions directly into the Redux store and get those all processed by our reducer. So now inside of this function we can write out as much async await logic as we want. We can use as much async code and then eventually when we get some kind of response or possibly error from the API, we can manually dispatch some kind of action. Now inside of here, the first thing I want to do inside of this function is make sure that we immediately dispatch an action of type. Or guess satisfying the interface search repositories action because again, this is going to be the thing that indicates to our reducer that we are about to make a request to go and find some list of repositories from NPM and this is what's going to turn our loading flag over to True. And hopefully eventually we'll update our UI to say, Hey, we're currently making a request. So in here, I'm going to immediately dispatch an action. Of type. Action Typekit search repositories. After that, we can then start to make our actual request. I'm going to wrap up this request inside of a try catch block. So if anything goes wrong with the request itself, we'll end up in the catch statement down here and we can dispatch another action to indicate that something just went wrong with our request. Let's handle that case first. Let's handle the catch case or something going wrong. So down here, if anything goes wrong, I want to dispatch an action. And that action should satisfy the interface of search repositories error action. So it should have a type of that right there and a payload describing exactly what just went wrong. So I'll put in dispatch. Type of action type. Search Repositories. Error. And then my payload is going to be the Errormessage property. So that error object right there, all error objects in JavaScript have a message property. We're going to take that and provide it as a payload. That's going to be a string describing what just went wrong. Okay, so this is a really good start. So now we'll take a very quick pause and then start to write out our actual request logic inside of our try block in just a moment.
