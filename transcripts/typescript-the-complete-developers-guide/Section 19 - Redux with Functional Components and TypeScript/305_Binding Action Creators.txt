# Binding Action Creators

This line of code around dispatching an action is really long, way longer than I would ever want to write to just dispatch one single action. So let's figure out a way to condense this just a little bit. To do so, we're going to create a hook that is going to automatically give us access to all of our different action creators inside of our components. So to get started inside of my SRC directory, I will make a new folder called Hooks. Inside there I'm going to make a new file called Use Actions Dot ts. Inside of here. We're going to add in an import for that same use dispatch function or that same use dispatch hook that we just made use of. And we will import that from React Redux. I'm going to also get Bind Action Creators from Redux. And then finally, I will import that big action creators object, which technically just has one action creator inside of it. Right now, our search repositories one from up one directory state. Then I will define and export a function called use actions. Okay, so here's the general idea. We have use dispatch. The use dispatch function is only used to get access to the dispatch function itself, and that is the function that is a part of our Redux store. It is what allows us to dispatch an action into all of our different reducers. So the first thing we're going to do is make use of that to get access to our dispatch function. Next up, Bind Action Creators. You might recall how this function works from the Redux library. This bind action creators function is going to be called. With a first argument of an object containing a bunch of different action creators. So that is our action creators input right here. Remember, this is an object technically that has right now our one action creator of search repositories inside of it. Then as a second argument. We provide the dispatch function. Find Action creators is going to give us back an object that contains all the different action creators that we provided in as the first argument. But now whenever we call these bound action creators, the return value from them will be automatically taken and provided to dispatch. So we can kind of imagine that we are getting back an object that kind of looks like this. We're getting back something that looks like search repositories. Dispatch search repositories. Repositories, if I could spell that correctly. Positas. There we go. And really just kind of like that part right there. Not quite exactly that, but you can kind of imagine that is what we are getting back out of Bind action creators. So we are going to return that object from this function. So that's all we have to add inside of here. So let's save this and then see how we're going to actually use this inside of a component. So we'll go back over to repositories list. I'm going to delete the use dispatch and action creators import and replace it with use actions from up one directory hooks Use actions. I'll then clean up our use dispatch line. I'm going to instead call. Let's do the call first. Use actions. And then out of there I'm going to destructure search repositories. So this version of search repositories like we just saw is pretty much already bound to that dispatch function. As soon as we call this. It is equivalent to doing this entire long line of code right there. Now inside of our onsubmit we can instead only do search repositories and pass in term lot easier to use than what we had before. Okay, so let's save this. Do a quick test. If I once again search for React? Yep. Looks like I am able to successfully reach out to that API and get a list of all those matching packages. Now the last big thing we have to do is somehow reach into our store, get some piece of state, and then iterate over all the fetch packages we found and print out a line for each of them inside of our block. So that very last step in the next video.
