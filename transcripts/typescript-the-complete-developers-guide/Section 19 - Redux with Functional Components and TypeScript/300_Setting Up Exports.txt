# Setting Up Exports

Well, we've just about got every last individual piece of our redux application put together. The last thing we really have to do is start to wire all this stuff together and create an actual Redux store. I'm going to first begin by going into my reducers directory. Inside there, I'm going to make a new file called Index.ts. Inside of here, I'm going to import all of my different reducers. Right now there's only one, and we're going to use that combine reducers function that we get from Redux to combine everything together. So at the very top I will import combine reducers from Redux. I'll then import my reducer which we had called. Oh, I think we just exported as default. That's totally fine. So I'm going to import repositories reducer. From the repositories reducer file. I'll then create a new variable called reducers, and that's going to be the result of calling combine reducers. I'm going to say that we want to have a reducers or not reducers, but repositories, there we go. Piece of state. And the value for this piece of state is going to come from the repositories reducer. And then finally I will export default reducers. So nothing very crazy inside of here. This looks like what we would normally write out inside of a JavaScript redux project. All right. Well, that's the last kind of little part we have to put together. So now we can combine all these different parts into our actual Redux store. I'm going to create our store inside of a new file. I'm going to create inside the state directory. I'm going to call this new file store. It's. Inside of here, we will import. Create store. Apply middleware. We're going to get both those from Redux. I will get thunk from Redux. Thunk. And then finally our reducer. So we just set up from the reducers directory. Then finally at the very bottom I will export const store and the result of that is going to come from Create store Pass passing our reducers pass in an initial state argument of an empty object. And I will apply middleware. And I'm going to apply the Redux Thunk Middleware. Let me zoom out just for a moment so you can see that entire line. All right, let's just about it. So remember one thing we discussed just a little bit ago? We had said that it's really nice if we do not try to pick things one at a time out of our Redux directory whenever we're trying to wire it up to the React side of our application. Instead, if we have a central index.ts file inside of our root Redux folder, in our case we called it the state folder, we can then import everything from our action creators and middlewares all these different locations into this index.ts file and then export them from there. And it just makes sure that we're not trying to import stuff from these individual files one at a time, which again generally ends up being just a little bit confusing. So the last thing we have to do on the redux side of our project inside of state, I'm going to make an index dot ts file. And then from here, like I said, we're going to export everything that we might need to get access to from other parts of our project. The first thing I'm going to export is star from our store file we just created. So that means now that exported from this file is going to be a store variable. I'm also going to export Star. As action creators. From actions which mean action. Readers that's saying go into our action creators index.ts file Find all the named exports inside of here and just export all of them as something called action creators. And I think that's pretty much it for right now. There's going to eventually be 1 or 2 other exports we add inside of here, but we're not going to add them in just yet because they're not going to quite make sense right now. So eventually a little bit more is going to be added in to this file. All right. So it looks pretty good. Well, we can take a pause right here, come back in the next video, and we're going to start to wire up the redux side of our application to the React side.
