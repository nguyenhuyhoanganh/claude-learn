# Wiring Up to React

As I've mentioned a couple of times now, we have finished up more or less a lot of the Redux side of our project. So we're going to start to connect a lot of that to the React side of things. I'm going to first begin by going into our components App.tsx file at the very top. I'm going to import provider from React Redux. Remember, this is a react component that we're going to use to get access to our Redux store throughout all of our different components. I'm then going to also import store from up one directory state. So this is kind of the nice thing by exporting all of our redux stuff from that Index.ts file, we don't really have to worry about digging through that state directory now to find our store or anything like that. We can just get all the different things we ever need to import from importing state. That's it. Once we've got all that stuff imported, I'm then going to replace that one right there with provider. I'm going to provide a store argument or a store prop to it. Of store. And then we can start to show some content inside of here. Right now I'm going to show How about some text of. And one that says search for a. Repository or let's call it package, because again, that's technically what it is. And then after that, H one, we can show some kind of new component, maybe we'll call it repository search repositories list or something like that. Let's just call it repositories list. We'll call it Repository's list. So this will be a new React component and that's going to contain all the logic around text input, searching, getting the information back out of Redux and eventually rendering a list of repositories onto the screen. I'm going to import that component even though we haven't created it just yet. From repositories. List. Well, then let's go and create that file right away. So inside of our components directory, I'll add in repositories, such a tough word to spell lists. There we go. Inside of here. I'll create. Repositories list. I'm going to annotate this function as react. FC functional component and then I will export default. Repositories list at the bottom. Right now inside that component. I'm going to return a div. Put a form element inside there. Why not? Let's just kind of get started on this thing. Inside the form. I'll add a text input. A button right after it. And I'll give the button, the text search. Okay. A lot of very fast typing there, but a lot of this stuff is kind of rote, you know, just putting basic component stuff together. So let's save this, go back over to our browser, Just make sure everything is showing up as expected. All right. So it looks like we might have a little error here. I completely made a little mistake. There was one last thing I wanted to talk about around the reducer, and I really just missed out on that. So let's take a quick pause right here. All we have to do is go back over to our reducer and add in a initial state for our state argument. That's all just a little thing, a little technicality that I just missed out on entirely. I apologize for that. So let's take care of that in just a moment.
