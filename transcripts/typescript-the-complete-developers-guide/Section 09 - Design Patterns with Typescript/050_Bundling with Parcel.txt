# Bundling with Parcel

All right, so let me tell you exactly how Parcel works. It might be kind of surprising. All right, so we're going to create a single file inside of our project directory to start. The single file is going to be an index.html file. Inside this HTML file, we are going to include a script tag for a file called index.ts. Notice the extension on there is .ts. Remember, we can't have raw TypeScript code running inside the browser. So how does this actually work? Well, we're going to start up that Parcel tool and we're going to feed the index.html file into it. Parcel is going to see that script tag with a source of index.ts. And as soon as it sees that .ts extension, Parcel is going to say, oh, I see, it is a TypeScript file. I need to parse the code inside there, compile it and turn it into JavaScript and then load that into the browser as opposed to the index.ts file that we requested. So like I said, Parcel is an awesome little tool. It has a lot of magic in it like this right here that makes setting up new TypeScript projects really, really easily. So let's get to it. Back inside my editor, I'm going to create a new file called index.html. And then inside of here, we're going to put down some very, very basic HTML. I'm going to put in an HTML tag. I'll put a body inside there. And then inside there, I'm going to put in a script tag with a source of .src index.ts. And I'll close off that script tag like so. All right, so we are going to create an src directory inside of our root project folder that's going to hold all of our source code. So inside of the maps directory, I'm going to make a new folder called src. And then inside there, we're going to create our index.ts file. So inside of src, I'll make index.ts. And then to get started inside of here, I'll do a quick console log of hi there, like so. Now I'm going to save this file. Okay, so like I said, we're now going to take parcel at our command line, and we're going to feed in this HTML file. As soon as parcel sees this src right here, it's going to find our index.ts file. It's going to notice that it is a TypeScript file. And so it's going to convert it into JavaScript for us automatically. After it does that, parcel is going to automatically update the script tag and change index.ts essentially into index.js for us before we access this HTML document inside of our browser. So let's try that out right now. I'm going to flip back over to my terminal. Inside of my maps directory, I'll run parcel index.html. Now, as soon as I run that, you'll notice very briefly something right there that said installing TypeScript. That means that parcel saw that we had a script tag that include that .ts file, and it installed a couple dependencies to work with TypeScript for us automatically. Now you'll notice I have a message right here that says my application is at localhost 1234. If you see any error message right now, and you see anything that says like e-adder or error address or something like that, chances are port 1234 on your machine is in use. So you'll want to make sure you don't have anything running on port 1234, because if you do, you will probably see an error message. As a quick aside, if you ever want to stop this running server, you can just hit control C, and you can always start it back up very easily by running parcel index.html again. All right, so to test out this application, I'm going to open up my browser and go to localhost 1234. So localhost 1234. Once here, I'm probably going to see a blank page, but if I open up my console, I can see hi there right there. So that's the console log I had put into my SRC file. So it looks like everything is working as expected. Awesome. One other quick thing you might find interesting, if we go to the elements tab right here, and then open up body, you'll notice that our script tag that we had written inside that HTML file did get updated. So rather than referencing a .ts file, parcel injected this new SRC tag right here, the new SRC property. So that's the JavaScript file that parcel created for us out of our TypeScript code. All right, so now we've got everything working. Let's take a pause right here, and we'll continue working on our application in the next video.
