# Google Maps Integration

In the last section, we installed Google Maps into our project. As one side note here, just to really make sure everything is set up correctly, you can open up your console inside the browser and just write out Google like so, and you should see an object like that appear. If you don't see that, once again, it means something was not set up correctly. Alright, so now we've got everything going, we can start to think about how we're going to add in Google Maps support to our project. So this is kind of an interesting scenario. Usually when we make use of TypeScript, we install dependencies using NPM, and we import them with normal import statements like the one you see right here. However, this time around, we have added in a script directly to our HTML file. And so that script is going to be added into our project as a global variable. It means that essentially, inside of our console over here, you notice how I just typed out Google by itself. This is a global variable that is available everywhere inside of our project without import statements or anything like that. And then once again, inside of my index.ts file, I'm going to write out Google like so. Remember anytime that we want to inspect a third party library, or even something that we wrote out as well just to be technical, we can hold down that command key or control key and then click on that thing like so. So here's the type definition file for the Google Maps JavaScript SDK. Once again, we can use this type definition file to get a better idea of how to work with this library. We can pull up the documentation, or we can just try to read this stuff and figure out what is going on. All right, so now that we've got access to Google Maps like 100% for real this time inside of our project, let's take a quick pause right here and continue in the next video. We're going to finally get a map to appear on the screen.
