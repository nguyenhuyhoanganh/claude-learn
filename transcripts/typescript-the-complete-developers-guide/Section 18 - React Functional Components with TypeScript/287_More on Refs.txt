# More on Refs

We've created our input ref, as I mentioned. All we have to do now is make sure that whenever our component first shows up on the screen, we attempt to focus that text input by using this ref. So to make sure that we can run some code when our component first is rendered on the screen, we'll make use of the use effect hook as well. I'm going to import Useeffect at the very top. Then down in my component right above on click, I'll add in use effect. I want to make sure that this inner function is only invoked one single time. So as a second argument, I'll put in an empty array. Then inside of here we can take a look at input ref and get a reference to our input element and then attempt to actually focus it. So you might be tempted to write out something like input ref dot current, which be a reference to the actual input element and then try to focus it by calling something like focus, which is how we generally focus an input element inside the browser. We'll notice as soon as I add on that dot focus, I get an error message. If you mouse over it, it says object is possibly null. So once again, remember, TypeScript is pretty well convinced that you might accidentally not actually assign this ref to any element that is being returned from your component. So TypeScript is very much convinced that input ref or input ref current might sometimes be null. So before we ever try to access input ref dot current and then try to look at any property or any function on there. TypeScript wants us to make sure that this is actually some defined value. It wants us to make sure that this is actually pointing to some element, right? So to add in a check for that, we can do it very easily. We'll just add in if. There is no input ref dot current value. So if that is not defined, we're going to return early from useeffect. So we refer to this as a type guard. It makes sure that we're not going to get any further down into our function if we do not have a value assigned to input current. If you now mouse over input current underneath the IF statement, you'll see that now TypeScript is 100% sure. Yes, this thing is definitely going to be referring to some kind of input element because we already checked and made sure that it's not null up here. All right, so let's save this file. We then need to make sure we wire it up inside of our app component before we try to test it out. So I'll go back over to TSX. At the top. I'm going to replace that import statement with user search from refs, user search, and I'll show that component instead. Looks good. Back inside of our browser. Quick refresh and you'll notice now when I refresh that text area right there, the text input is automatically focused so I can type in there immediately without having to actually select the text input. And that means that our component definitely works as expected. Okay. So the big takeaway here is that whenever you define a ref, TypeScript is really paranoid and it really thinks that you might not assign that ref or bind it to any actual element. So you are going to add in the type of element that you're going to provide the ref to or null and then null as a default value. Then whenever you are trying to access that ref, you are going to first check to make sure that ref current is actually defined. You're going to most frequently add in a check like this inside of a use effect function. I do recommend that you do that check and then an early return if input ref dot current is not defined. But of course another way you could define this is to say if it is defined then go ahead and attempt to focus the input. So that's another way we could write that out. But in general, I personally prefer doing the early check and the early return for the negative case. Where we have the not like so. Okay. So that's pretty much it for refs. Again, really just comes down to remembering that line of code right there. Last thing I want to mention around this. If you're ever in a scenario where you just don't remember this type and you don't want to deal with it at all, you can always just put in any like so and just tell TypeScript, you know what? Whatever. It's any value. Don't try to do any actual checking here. Don't try to make sure it's an input element or anything like that. So this is kind of an escape hatch right here. If you do not want to put in the full type and sometimes do that myself, if I'm just kind of quickly prototyping a component or something similar to that. All right. So that's it for us. Pause right here. Move on to our next topic in just a moment.
