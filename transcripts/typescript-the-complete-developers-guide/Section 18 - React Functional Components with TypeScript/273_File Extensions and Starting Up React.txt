# File Extensions and Starting Up React

I have generated my project, so I'm going to change into the new directory that was created. I'll then start up my code editor inside there. We're going to take a look at some of the different files and folders that were generated for us. Now, at first glance it appears that Create React App has given us all the normal files and folders that we would expect to see. However, if you open up your SRC directory, you might immediately notice a very small difference inside of here where we would normally have dot JS files for a normal create React app project. In this case we have some different files with an extension of dot TSX. So first a little lesson that we see right away without even having to start up our project or anything like that. When we start to make use of TypeScript and react together, we're always going to start to think about the extensions that we apply to all of our different file names. If we ever create a new file and we want it to contain a react component or really any kind of JSX at all, we're going to name that file with an extension dot TSX. This indicates that this is going to be a typescript file that contains, you guessed it, some amount of JSX. If we create a file inside of our project that is not going to contain any JSX, but maybe it's a file for Redux or reducer or something like that, we'll instead only use dot ts. If you do not follow this rule, then TypeScript is going to start to complain inside of your editor very quickly. So let me show you a very quick example of that. I'm going to open up my app dot TSX file. Inside of here is the app component. I'll then rename that file. To just apts. And then immediately we start to see a ton of different error messages appear on the screen. So very clearly, if we want to create a react component, we must place it inside of a file with a file extension of dot TSX. And I just messed that up. There we go. Much better. All right. So that's simple enough. Let's start to kind of dial things up here a little bit. We clearly got a lot of different files created for us. Whenever I make a new React project, I don't really like to use these default files because there's essentially just a lot of code inside there and don't really necessarily know what any of that code does. So inside of my SRC directory, I'm going to select all the different files inside there and then delete them. I'm then going to create a new index.ts file from scratch and we'll get started inside that file by writing out a little bit of code. So I'm going to write out index dot TSX. Then inside of here, I want to create a very simple component and display it on the screen. So the entire process for this is going to look very familiar, not really anything new. Just because we were making use of TypeScript or anything like that at the very top of the file I will import react Dom. From React, Dom. I'll then create a component that I will call app. And inside there will return about a div. With an H1 inside of it. That says, Hi there. And then at the bottom of the file, I'm going to try to use React Dom to render that app component onto the screen. We'll do a very simple react Dom dot render. I'll show an instance of my app component and then I'm going to select that HTML element that we always get whenever we use Create React app, we're going to do a document. Where your selector. Pound root. So notice right away that just because we're using TypeScript. Well, nothing necessarily has to change. This is the first real kind of big realization I want you to have around React and TypeScript working together. Just because you are introducing TypeScript into a project doesn't necessarily mean you need to start adding in a ton of fancy new syntax. TypeScript and react by itself. They work together fantastically. And so even though you might not see it. TypeScript really is working behind the scenes here and making sure that you have applied some different appropriate types to your code. Let's try starting up our project at our terminal and just make sure that this code actually works as expected. So back at my terminal. I'll do an NPM run start. That should open up a new browser window at localhost 3000 and you should see some content like this appear on the screen. And at present I do not have any warnings or any errors or anything like that. All right, So good start. But obviously just saying, hey, we don't necessarily have to add in any special code isn't super helpful. So let's start to investigate some different features and some different things we can do with React and TypeScript together in the next video.
