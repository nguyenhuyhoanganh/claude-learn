# State with TypeScript

Let's start to get a better idea of how to work with state and TypeScript together. To do so, we're going to build out a very small little application. So I want to build something that looks like this. We're going to show some kind of imaginary party guest list. This will list out a variety of different names of people who are invited to some imaginary party. So we're going to eventually collect a list of names and then render them out as a list onto the screen. In order to add in more names to the list, we'll put a text input down here towards the bottom and a little button after it. So we're going to type in a name right there, click Add and then expect to see the name added into that list. We're going to build this entire application inside of one single component. Okay. So to get started, I'm going to go back over to my editor. I'm going to make a new folder inside my SRC directory called State. Now, the reason I'm calling the state is just because it is an example of using state normally inside of a real React project. I would not build some folder called state or anything like that. Then inside that folder I'm going to make a new file called Guest List dot TSX. All right. So we'll first begin by just writing out a very simple component. I'm going to annotate it's type as React dot FC. I'm not going to put in any angle brackets here because we're not going to expect to receive any props to our guest list. If we do not expect to receive any props, we do not have to add in those angle brackets right there or adding any kind of interface or anything like that. I'll then put in a very simple return. Of div and I'll just print out guest list and then a simple export default guest list at the bottom. Once I've got that all put together, I'm going to try to show it inside of my index.tsx file. So back inside of Index.tsx I will import guest list. From state guest list. And then show that component in place. That one. All right, let's go back over to our browser and just make sure everything is showing up. Where's my browser? Here it is. All right. Simple enough. So let's go into that guest list component and start adding in a little bit of indentation or implementation. So first, I probably don't really want to show just simply guest list inside there, so I'm going to expand that div. I'm going to wrap guest list with an H3. We'll eventually put in a little bit of logic right here to render out our list of guests. But for right now, I'm going to first begin by adding in an input. And a button. Right after it for the button. I'll give it the text. Add guest. And that's pretty much it for static content that we need. So let's now add in a piece of state to track first whatever user types into that input. So we're going to set up that input as a controlled input. Remember, a controlled input is one where we are going to designate an onchange callback and a value prop as well. To do so, we first need to add in a piece of state to our application something to track whatever user types inside there. So to add, in a piece of state, we will import the use state hook from React. Then right above our return statement, I'll add in a new piece of state called Name. Set name. Call your state and I'm going to provide an initial value of empty string. We can then start to wire up some props on the input element itself. So I'll give it a value of name and an Onchange or Onchange will provide a callback function that will receive an event object. Whenever Onchange is called, we're going to update our Setname piece of state. So we'll do a setname. E which is short for event.target.value like so. Now, at this point in time, we have a fully functional component that makes use of some state. This absolutely works. Everything we have inside of here. And I just want you to note once again that outside of this little bit of type annotation right there, there's nothing inside of our component that looks special just because we are making use of TypeScript. So at the end of the day, you can really get pretty far with TypeScript and React put together without really obsessing over any additional details. We only need to start worrying about TypeScript if we want to add in a couple more checks to our different components. So let's take a look at how we might improve some of this code and also add in some more functionality to it, like the ability to actually truly add a guest in the next video.
