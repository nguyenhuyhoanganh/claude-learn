# Typing Standalone Event Handlers

In this video, we're going to try to figure out why we are getting an error message from this line up here, but not if we define the same function directly in line on the onchange prop. The reason we're seeing this error message is all because of that type inference system in TypeScript in how it works. Remember, TypeScript is always trying to figure out the different types of values flowing around your application. One tool it has to do that is the type inference system. If we take a look at this input element right here and then mouse over Onchange, you'll notice that TypeScript is 100% aware of what that onchange prop really is. It understands that it is a prop. It understands that we are required to provide some kind of callback function to it. This right here says that if we decide to provide an callback function to Onchange, it's going to have a first argument that we might call event and its type will be that right there. So a type for that event object. If you mouse over right here in line, you'll notice we get the exact same type. So that is the type inference system in TypeScript being applied to this onchange callback. However, the type inference system is not applied if we define the function ahead of time. So even if we define the function ahead of time, even if we pass down onchange like so type inference will not be applied. Type inference is only applied inside of our JSX when we define that callback function directly in line. So how are we going to solve this? How are we going to define onchange ahead of time and still somehow annotate the type of our event object right here? Well, it's going to be pretty straightforward. We're still going to have an event object. I'm going to very quickly rename it just to make our code a little bit more clearer, because E is not very clear. Then to apply a type two event right here. Let me show you a little trick. I'm going to mouse over on change. I'm then going to find the React dot change event right here, highlight it, copy it, put a colon after event and then paste that type in. So this is essentially the exact same thing as type inference, where we're kind of taking that inferred type or the required type of onchange. We are just manually placing that type right there. So now our event object is properly typed and we can refer to all different kinds of properties on it, such as event, target value or whatever else we want to access. Now, you will notice that if we look at this event type right here, it says change event. But there are other kinds of events that we handle inside of React code, such as maybe a hover or a drag or other stuff like that that we might want to annotate as well. So let's take a quick pause, Come back in just a moment. And I want to show you a couple of other types of events that you might want to be aware of.
