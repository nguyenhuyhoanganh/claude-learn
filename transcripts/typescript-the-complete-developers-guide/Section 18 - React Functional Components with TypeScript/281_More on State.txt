# More on State

Let's try to build out one more small component to get a better idea of how state works with TypeScript. So for this component, I want to make some kind of user search component. The goal of this component, we're going to show a little title at the top, a place where a user can enter a name and then a little find button. We're then going to take whatever the user types inside their attempt to find that person and then print out some user or some information about them at the very bottom of the screen. So if I were to search for, say, Sarah, I would expect to see that result right there. And if I ever try to find someone who doesn't exist, I would expect to see something that says null or user not found. Or something like that. Once again, we're going to build all this stuff up inside of one single component. So I'm going to go back over to my code editor inside of my state directory. I'll make a new file of user search dot TSX. Then inside of here, we know right away that we're probably going to have to add in some piece of state. So will import use state? From React. I'm going to make my user search component. And again, I will annotate its type. As React Dot FC. I'll then export default user search at the bottom. As usual. For right now I'm going to place a div that just says user search. Save this and then go back over to our app component and just make sure we can see this thing on the screen. So back inside of. I'm going to update the import statement to import. Guest list from State. Guest list or not? Guest list. I just did the same exact one again. No, we want user search. I was looking at the words guest list. User search. There we go. And then finally show that inside the app component. Kwik Save. Kwik Test. I can see user search on the screen. Looks good. Okay. So back inside of our component. Let's first begin by adding in maybe a hardcoded list of users that we want to search over. So right above my component, I'm going to add in. Users as an array. And I'm going to have every element inside this array be an object that has a name and an age. So first I'll add in Sarah with an age of 20. I'm going to add in Alex with an age of about 20. And then finally, a name of Michael with an age of 20 as well. Everyone is 20 in this world. Who knows why? So now we're going to add in a text input. We're going to somehow store whatever user types into that text input once again inside of a piece of state. Whenever a user clicks on some button, we're going to iterate through our list of users and try to find some user inside there. If we find a user, we'll display details about them on the screen. Otherwise we show could not find user or something similar. Okay. So to get started inside of user search, I'm going to first begin by adding in a name. Piece of state. Inside this div, I'm going to give myself a little bit of space. Add in an input. I'm going to set its value to be name. And whenever someone changes this thing, I'm going to provide a callback function. I'm going to receive event as an argument that I'll call simply and then we'll update once again our name, piece of state. I'll put in set name E target value. Then right after the input, we'll place a button once again. That says find user or it can say search or whatever else. And then finally, we'll put in an onClick prop to this thing as well. I'm going to define on click up here. And so now inside of this function is where we can somehow take a look at all of our different users and find one with a matching name. Before we do that, however, let's just take a quick break and then wrap up this example in the next video.
