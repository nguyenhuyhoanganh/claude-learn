# Inline Event Handlers

In our last two examples, we put together input elements that had onchange callback functions. In both cases, these callback functions were written inline. So in other words, we define the function right here as opposed to writing out the function separately as we did with onClick. So it turns out that if we define these onchange functions separately inside of our component, life gets just a little bit more challenging. So let's write out a very quick component just to kind of explore this idea of defining an onchange callback where we define onchange itself as a separate function rather than making this change inside of our user search file. I'm going to instead create a new component inside of my SRC directory. I'm going to make a new folder called Events, and then inside there I'm going to make a new file called Event Component because I don't have a greater name than that in this case. So event component is going to be essentially just a very simple component that we're going to use just to explore events. It's not really going to do anything per se, and that's why we've got a very boring name for it. So inside that file. I'm going to declare a new component. Of event. Component. I'm going to annotate it's type as react dot FC. I'll make sure I export that thing at the bottom. And then inside the component itself, I'm going to place a div. That complaint that contains an input element with only onchange defined. I'm then going to add in a very similar callback for Onchange as what we had in the previous two examples. So I'm going to receive some kind of event object that I'll abbreviate simply as E and then do a console log of E like so. Let's save this and test this thing out very quickly inside of our browser, back inside of our main TSX file at the very top, I'm going to delete our user search import and replace it with event component. From events. Event component. And then I'll show that component on the screen. All right, quick save, quick refresh. And as soon as we type inside of here, we very clearly see our event console logged. Without a doubt, everything is working as expected. So let's now go back over to our editor and we're going to make a very small change to our event component, as I mentioned. All I really want to do is not define this callback function inline. Instead, I want to define it separately inside my component. So I'm going to put in a const. On change. We are once again going to provide some kind of event object as the argument, and I'm going to abbreviate that as E, I'll do a console log of E inside of here and then to the Onchange prop. I'm going to replace that with the reference to our onchange function. And then right away we see an error message around event. So it says in this case parameter E has an any type. Remember TypeScript in general using the any type. Generally we want to avoid that if we possibly can. But my real question here is why are we now seeing this error here? But we didn't see it with this other implementation just a moment ago. So that's the big question. We're going to answer that question in the next video and then figure out how to get this error message to go away.
