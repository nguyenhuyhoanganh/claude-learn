# Project (Build A Task Scheduler)

Okay. Welcome back. In order to wrap up this section of the course, I want us to build a task scheduler. So create a new folder five scheduler. You can name it whatever you like. All right. And then let's open it up. So how would it work. We are going to keep it really simple. Basically we are going to have a map. Let's say let me show you an example. So map we're going to have a map like this task map is going to be a string. And then in it let's say hello one. Then we give it a function. So we are going to store different different functions like this in a map. And then based on how the timing is configured, like if it's every minute, let's say if we configure this to run every minute, every minute we fire this, every minute we fire this. If we configure it to run once, we are not going to add it to the map at all. It's as simple as this. So in the in this code we are going to use the some of the time methods that are time functions that we've learned. So let's go ahead and work on it. Let's start with the task itself right. The task is going to have an ID is going to have a name is going to have if it's recurring. Recurring bool then we need a stop channel. We are going to run each of the task in a different go routine. So we need this channel to stop it in case we want to stop that go routine. So I'm using the struct um instead of bool because struct an empty stroke doesn't take memory. Then wait group is going to be sync. think? Um, well, let's have a wait group that is pointing to the thing that wait group. The reason we have wait group here is so that we can wait for things to be done before we we know when this specific task is completed and all that. All right. Then the scheduler itself is also quite easy. That map that I showed you here earlier, we are going to have them in here. So map and that's it. Then we need a mutex to control it. This mutex is going to control read and write as um access to this map for us. And then we need a global wait group the global wait group. We are going to use it to wait for all the tasks. So all the goroutines that are running these tasks, all the tasks to be done before we can exit, okay. After that, let's create a small constructor And we just make the map and that's it. Nothing else needs to be made, just this one. If you don't make this, if you don't call, make to make it, to initialize it, you cannot use it. This is going to be Neil. Okay. With this let's create one function to generate task IDs for us. Because the string here should be the task id. So let's call it generate task id and then we return a small string. So tax ID is just Unix nano from the current time. And that's it for the basics. All right. Yeah I don't think it's complicated but yeah you'll be the judge. All right. So the first method we are going to add to the scheduler is to add the ability to schedule a task once. That means it's honestly it's the same thing. Like let me show you time dot after func this this after func. Let's say if you give it five times time dot seconds and you give it like this after test. After five seconds is going to run this function something similar to this is what we are about to reinvent. So I'm going to take it out now okay. So schedule once. Let's look at it. So we need the name of the test. We want to run the delay. How long before we execute the task. And then this is the the action that we are going to run. So we are going to run this function. It doesn't take any argument doesn't return anything. You can do everything inside. All right. With that in place let's generate an ID and let's print out a message. I'm going to be printing out a lot of messages to give us visual insight what is going on inside the scheduler. So I'll be printing out a lot of messages. The code itself is super simple, but the messages I'll be printing out can make it look a little bit, um, lengthy, but the is important. If you want to debug things, you you need the messages. So we just generated an ID here. We haven't done anything. Now think about it. We only need to run this action once. That means it doesn't make any sense to us if we don't need to add it here, we don't need to add it to the map. So we are not going to add it to the map. We just schedule it as soon as the time elapsed. We run it and that's it. So but we need to make sure that it finishes running. So let's uh, increment the global wait group. Now that we've incremented the global wait group we call that Time.after func. Again. You see we pass it the delay right then in in it we can make sure to defer the global wait group. All right. Then we should print out a message that we are executing the one off action. All right. And then let's now execute the action itself. So let's call this functionality. It's a plain function. So let's call it. After that we print out another message again. And after the printed message we should come here and return the task ID to the user. And that's it. So I think I can take that access out. All right. It looks if you take out these messages that we are printing, let me delete them for a minute. If you delete all the messages that we are printing, you see how simple it is? We generated an ID, incremented the global wait group, and then we call this after func. And then we just call the task inside. So it's super super easy. So far you haven't seen the use for this. But it's okay because we don't technically need that in this schedule once we are going to need it in the second method. So let's undo all the messages and printing out. We can test it. So scheduler scheduler. Then scheduler dot schedule ones. Let's say um test. And then the duration will be two times seconds. And the action we want to run here just a plain function. That's what we accept. And in it here you can do anything you want. I can, for instance, say print um running test. Okay, so. But how do we run it? We need to. We need somehow to wait. Let's let's do something. Let's let's do let's pretend not to wait. So go run. Uh, 155 and run it. You see, it says scheduling the task, but it never get a chance to run. Because think about it, and I am taking this time to explain, not just to rush through the code. Right? We we incremented the global, the wait group here, we schedule, we enter this place. But because this, um, the the main goal routine exited this never get the chance to execute. That means somehow we need this wait group to wait. So if we come here and then we say global wait group wait, we should be able to wait for it. Good. We are waiting. And now it got the chance to execute. And that's fantastic. All right, with that out of the way, let's add the the big method which is to schedule tasks at intervals. So let's call it um schedule interval. Let me explain the signature a little bit. So the same thing here name is name here delay. We have initial delay which I'm going to explain now. Then the action is the same. Then the interval. Right. That's what we have for now. The initial delay is the how much we want to wait before we run it for the first time. This is important because think about it. If we put in a task or we schedule a task to run, when do we start running it? Do you run it immediately or you want to wait for some time? So you give the user the opportunity to determine that. So initial delay is how long we have to wait before we run it for the first time. When we run it for the first time, we then schedule the interval. The interval is how frequently we want to run it. And the action is just a plain function, just like we have here. Okay, with that out of the way, let's now make sure that the interval is not zero or minus. So let's check for that okay. After that let's generate an ID. Let's create a task. Now that we have the tasks, this is the first time we are using the task object. The most important thing here is that you have to make the channel for this task, right? If you don't make the channel and you try to use it, it's going to panic. Now that we have made the task, let's acquire a lock because we need to add the task to this database that we have. So let's acquire a lock. Let's put it in there and then release the lock. Basically we are using the task ID to put it. Of course you can if you like. You can come here and say task ID is the same thing, right? Now that we have put the task in the database in that map. What? Let's print out a message. Right. The message is just for us to understand what is going on. I mentioned it previously. Now that we've printed out the message, what do you think is the next thing to do? Of course we need to increment the global weight group and the tax weight group, right? We increment them. We need to increment them because we are about to start a new goal routine. And then when we start a new goal routine, we also need to signal the decrement right. For both of them. So let's go into the routine. Okay. So this this is a go routine function. And then we have to signal the decrementing of this. And that's it right. Next thing is we print out another message that we are in the go routine. Now that we are in the go routine, it is time for us to determine when to run it based on this initial delay. And the best thing to use for that is the timer. We have seen it before. The timer would fire after this delay has elapsed, so let's use the select to do that. We have this. So we have two different channels. One is coming from the timer. Another one is this top charm. So if we receive, let's say, before we get the opportunity to run the code, let's say we receive a message to stop the go routine. So we want to stop it. If we don't receive any message here and the initial delay elapsed, we run the code. And that's exactly what we are going to do. Okay. So let's go into select. Now in the select we try let's put let's pull out a message from the initial timer. Basically this is us pulling out a message from this channel. When the message come from the channel what we want to do here let's let's allow allow it to to flow down. I want to allow it to flow down because we when we pull the message from here and we don't do anything, the flow, the code is going to continue here. Right, then that is the first channel. The second channel is this the stop channel for the tax? This one. So let's wait for that as well. So this is the task this task that we have dot stop channel. Let's try to pull a message. If we receive any message from the stop channel that means we have been asked to stop it. So let's stop this timer and then let's print out a message and then we return. This would return us from the goroutine and terminate the process for this task. So for any time, any time this function is called, we are going to start a goroutine. And that goroutine is controlled by the um by the wait group. And also this channel, this channel can stop it. All right. Now let's assume that we didn't receive any message here. Right. And we received a message here. We received a message here. We didn't do anything with it. So we let the code flow down here, which is what we want. Now the code has come down here. Let's print out a message saying that. Well, we are about to execute the tax for the first time right now that we have printed out a message, what we want to do, we can go ahead and execute the tasks. That's it. Simple stuff. Exactly what we did earlier. Now the tax has been executed for the first time when they could return to this point after the function returned here, we can now schedule the interval, which is super, super easy to do. We can use the ticker for that. So let's use the ticker. And don't forget to defer the stop from that ticker. Now we have the ticker. We need to go into an infinite loop right now. So before we go into the infinite loop, let's print out a message one more time. And then we can start the loop. In the loop we need to select again. Why do we need select. salad. Because don't forget, we have two channels. Again, one channel is from the. Another one is also from the channel. If we receive any message in the channel before the next sticker, we want to stop the go routine. So let's go ahead and do that. So pull message put the current time. Put the current time from the ticker. And then let's print out a message. And then we run the action. And that that's that's the whole job. After that let's also do the same thing that we did here. Right. Print out a message and we return. So this path is the same as this path. This path. The only reason why of course, we could have asked why I try to put the code in here, but it's not as clean as doing it down here. So that's why I didn't do it in there. And I let the code flow down. Okay, after that whole party, we should return the task ID and nil and that's how our scheduler. So let me scroll to the top and then do a walk through down. It's quite lengthy. We can it's possible to break it apart. So we we did a simple error check here. Created a task. We acquire mutex to make sure that we can concurrently add item to the map without problems. We just printed out messages. If you take out all the messages I am printing out, the thing is quite simple. Then we incremented our global wait group and the tax wait group. Of course, we also have to defer them. The most important thing that you should take away from here is this for each task they want to schedule, we are starting the go routine that will be waiting and be executing the tasks in the background until the wait group. Either we send a stop to stop that specific task or. Somehow the system run out of memory. You know, if not, it's going to continue running, right? And then the whole job is just done here. Using the select select is so cool because it allows us to, um, listen on multiple channels like we are listening on two different channels here. And that's how it works. So that's it for schedule interval. Now time for us to test it. So we have this one that is going to run once. And then I'm going to come here and do schedule interval. So let's call it test interval. Initial delay. Let's say it should wait for one second before it start to run. And then after that it should be running every three seconds. So what we want to run. Let me put a message here. Running database backup in. Real time. Honestly, it can be any kind of job. Don't forget, we are returning the task ID and a possible error that we are not necessarily using for now. So I'm going to comment them out and then the wait is still fine. Okay. Let's see. So the way I think it should work if, if, if the goroutines are behaving as expected is I think we should see the first run of this one. Then we see some, we see this running this one, then we also see this one. But how quick how the goroutine is going to schedule them is, is that the runtime is responsible for that. But we know that, uh, at the second, the second with two seconds, this is going to run, this is going to wait for one second, then run, then after the one second wait, and then this one will be scheduled and then start running every three seconds. So let's wait for it. Okay. You see? And then you see, this is exactly what I was talking about. I'm going to stop the program now so we can analyze the output. The test, first of all, of course, that was scheduled. Then this this one was also scheduled. Right. And then the running database back up. First of all, ran before this one because it waited just one second and this was waiting for two seconds. Then after that running database backup, the next second of it before the next iteration of it ran, this one ran after the wait. So this makes a lot of sense. Then after the the three seconds wait, then it started running every three seconds, every three seconds, every three seconds. So look look at it. 2730 33. So you see that it's very sequential. It make a lot of sense. So this is super powerful and that's it. But here is a problem. One little problem that we have that I want to solve before I end the video is how do you stop a task. You know, now I had to I had to press Ctrl C on my keyboard to stop it, but we need a better way to stop it. So here let's add a small function called stop task. And then, um, let's acquire the lock. Let's make sure that the task is in the map. So if you give us a task ID we want to make sure the ID exists. So let's check two things we want to check here I want to make sure that the A is exist is not false. And the task is also recurring. So if it is not a recurring task we unlock, we put out the message and we return false. So there is nothing we can do if we can't find it or it is not a recurring task. If not, let's delete it from the map. Let's unlock the mutex. Let's print out a message. Let's close the channel. Then we wait for the wait group to for it to finish doing its thing. Then we print out another message and then we return true. And that's it for for the stop the stop task. The most important thing here for us is to close the channel is super important. As soon as you close the channel, everything shuts down. Okay then lastly, we need to stop all the stop all is super simple. It's going to use the same stop task, but we are going to collect all the ideas of the tasks we have and then loop and call this stop tasks on each of the ideas. So let's do it right. Print out a message that we are going to run the, um, shut down for all the task. Then let's acquire a lock. Let's create a slice of string to hold the ideas. Let's loop and append all the ideas that we can find here. Now the next thing is to unlock it. Any time that you are in a concurrent environment and you are interacting with a critical data data point, that is that multiple go routines can change, you always want to use the mutex. Now basically we are going to loop over these ideas and call the stop task. So let's loop over it. Let's call the stop task on it. And then we can print out a message here. Let's make sure we wait for everything to clean up and then print out a message. And that's about it. Even though we have these two um, so you can use this from any, any go routine or even manually calling them after, after some kind of time out. So let's start with one. Let's, let's try to use it in a go routine. And I can show you how to use it manually. Let's add one more method called auto Stop all. Then start a go routine. In the go routine, let's use that our famous timer. Then we define the stopping of the timer when we receive a signal from it. So when this time elapsed. So after the time they are going to pass, when it elapsed, we print out a message and then we just call stop all. And that should stop all the tasks. So let's go and try it. Then instead of this thing running forever until we press Ctrl c, I'm going to say schedule auto stop after five seconds. The number should be on this side. I don't know it. It just read better it. Of course this is multiplication. This side doesn't matter. But he just looked better. All right. So let's run it. Boom is running, running, running and boom. You see, after five seconds, the auto stop was triggered. You know, after five seconds. Initiating shutdown for all the attack, he sent the the stop signal to the test, um, interval and everything shuts down. And that's so fantastic. This is so good. Of course, you can also try to do this. So. You can. Let's give this a shot. Uh, time dot after chunk. Then you can say five times time dot seconds and you can do something like this. But this is not this. This. Yeah. So you can do something like this doesn't take any argument. And let's let's give that a shot and see. answer. You see, it got initialized and everything was shut down. So if you if you don't want to use this, honestly, you can use this as well. And that should work okay. And that is it for the video. And that's it for this section. Maybe I can leave this one in the code base as well. You can use any of them. Uh let me scroll to the top. I'm going to scroll down gradually if you want to pause it to see uh, everything that we have done, then you can pause it, type along, copy the code, run it to see how it works. It's a fantastic code. It's a fantastic code, you see. You see? So, something I want to mention, Mentioned for the sake of correctness. This is I am calling tax. I'm closing it with an assumption that. So I'm assuming that nothing else is going to close this, and it is only here that we are going to close it. So just to have that in mind and then stop all and then the auto stop that we wrote and then I gave you a different option here in case you don't want to use this auto stop. All in short, if you want to stop a specific task, you can use the you can put in your own callback here and then put the ID and it will stop after that time as well. And that's it for this section. I enjoyed this section so much because I love time and I love to do things that are concurrent, so I have a lot of fun to work on this section. See you. Bye.
