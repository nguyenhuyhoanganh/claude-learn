# Understanding the Go Toolchain

In the last video we worked on this program. Hello world program. It works great and you have seen some command. You saw the go run command that I executed and the go run command executed the program for us. In this video, I want to talk a little bit about some of these tools. It's important because I don't want us to rely on IDE all the time. I want us to be able to run these commands by ourselves. We want us to know what the commands does, and everything else would become easier as we code along. So the run command go run helps you compile the program and execute the program. We also have Go Build that you can use to build the file as well. This is going to build an executable for this operating system a hello program. So I'm going to run it. Now when I say LLS Alice. You see, just ignore this one. This is the executable and we can run the executable by just typing. Hello. And you see this is a compiled program. So if you want to compile the program is go build. We are going to see like a more complex build process later. But I just want to show you the basic command we have seen. Go run run the program, go build compile the program for you. So what is the difference between the run and the build? Run does two things. Go run. Hello. Go for instance. So basically it compiles a program but into a temporary directory and executes the program. So it does two things. It's a convenient way to it just helps you instead of doing build and then running it by yourself. This does it for you. Most developers just use go run Then they go build itself. Just compile the program. It doesn't run it. So that's the difference. We have other commands too. Like for example, let's say let me open the program that we wrote. This is a program. Look at what I'm going to do. You see the formatting is a little bit messed up. I can try to mess it up a little bit more and I will type go and then I can give it the file name. Now I'm going to open the file again. You see this, the program, this FMT program, which is for format that we executed, has helped us to format the program nicely according to convention. So we can use this go format. The name of the file or the package to help you format your file. Then there is a very important command. Again, go test that you can use to test your Go program. Currently we don't have any tests so this is not going to work. Then we also have go install to install packages. We have go get also to install packages. We are going to see the difference between go install and go get later. But these are the few commands that I want us to learn. Now and later we can learn how to use them more thoroughly. So let me just go over them again. Go run for running program and go build for compiling a program go format for formatting the program. Go get slash install for installing an external package. So these are the basic command. And then I will come back later to explain them more. See you in the next video.
