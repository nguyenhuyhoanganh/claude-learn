# Build A Custom Email Library

Hello. Welcome back. In the last video we saw how to work with the SMTP service was very good. Now it is time for us to add a small image sending library that we can use to send emails. Okay, now in production you want to use um, something like this. So there is a well tested email program, but I want to show you how to do it by yourself in case you want to do it. This is way, way more secure. This has a lot of settings that you can use. So in production you should just download one of these. You probably don't have to do much and then use them. Um, then I wanted to show you something in the last video before I ended it, which is how to use the SMTP command. Honestly, this is something you probably never have to do, but if you are interested in those commands I was typing, you can come here and see it. This is you. Remember? This is exactly what I type, and this is how you can learn it if you want to learn it. It's a good thing to learn. All right, with that out of the way, we'll keep our mailbox open. Let's go back to our program. And, uh, let's start working on the program. I think I can come here. Let me see where we are. I sort of, I mistakenly check out too much, I think. Okay, I can go to checkout. Git checkout. Okay, I think git status. First git add git commit and picks sm smtp program. I think that's it. Then git checkout b 33 dash add image. Library. Something like this should do. Okay now where do we want to add it. We can add in internal. I'm going to add a new folder called notification. Notification. And then we add Mailgun email dot go actually. All right. And that's it. So what we want to write let's create a config for our SMTP program. You know SMTP requires authentication most times. But for this local installation that we have we don't need that. But we're going to keep it. Then when we want to send an email this is the payload for the email image, the person we want to send to the subject and the body. So far, so good, right? Then the program that we're going to use to send the email, the library, we are going to call it email notifier. And then we can embed um this SMTP config inside. So let's do that and then let's add a constructor. All this is super easy super simple. Something you have seen a million times. Then we are going to add just two methods to this type. So the first method we are going to add is to send simple email. And this method does what its name says. It's going to send very simple emails. So the address of the server we are going to construct it which is basically the host colon the port. All right. Now that we have the address of the server we are going to dial a connection to the server. So it's like a phone call. It basically like similar to what I was typing in the terminal. Okay, now something I want to mention before I dive. The connection is that there is no TLS, so this is not secure, but it's enough to show you how it works. So now let's use the. Net package to dial a connection. If the connection is not successful we want to return an error. So it's always important to check for error. And don't forget to defer closing of the connection. So far so good. We haven't done anything tricky right now. If we have the connection, let's use our connection to create a new SMTP client. And with that we also check for error. If there is no error, let's defer the closing. This is one way you can defer the closing. Then you can call quit to get out of the SMTP connection. If someone is trying to use authentication by setting their username and password or All password. We want to check for that as well. Now we can call the SMTP dot plain. Ought to do the authentication. Let's check for the authentication and error here. And that's it for that. After the authentication um the next thing we want to do is to do what I did in the terminal. We first of all set the sender. So you're calling the this is the client and SMTP client. May we set the from. After that we need to set the recipients. And we need to start doing the data. The data we need to acquire a right closer from it. So this data client or data give us a right closer. That means we can write all the payload that is the email we want to send. We can write it to it. Okay. So let's construct the message here. And then we write it And then we check for error. And then we close. And that's it for it. So it's very very sequential. Dialing the connection to the server and using that connection to create an SMTP client. Then if there is a username or password set we want to try and authenticate. And then after that the basic stuff that I did from the command line setting the sender, the recipient then packing the message that we want to send and then writing it to it and that's it. Now that we have that out of the way, I'm going to add a method that technically doesn't belong here. But I'm going to add it anyway because that is the only method we are going to use in the course, which is to send login notification. Ideally, you should have this somewhere else that you can use it, but I want to add it here anyway just to keep the whole thing in one place. And it's simple. We need to construct. um, simple email, and then we can pass it to, uh, our function to send it to construct simple email. This is to email simple email, the email address and then the title, and then just the message that we want to send. After that we just call our function passing it in for it. And that's it. Very simple very straightforward. And I hope this is not confusing, but it's a good exercise that we are doing it by ourselves. Actually, the libraries on the internet are doing the same thing, but they have more configurations to them. Go already provide SMTP library, so we are just using it to do what we want and that's it for now. We are not going to run it yet. In the next video, we are going to start working on how we can take this program and use it in another program, the microservice that we are going to write. So see you there.
