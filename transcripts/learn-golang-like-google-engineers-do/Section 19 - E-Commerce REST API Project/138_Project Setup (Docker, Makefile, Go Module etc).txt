# Project Setup (Docker, Makefile, Go Module etc)

Hello. Welcome to the section. Now, in this section I want us to start setting up our project and then let's start building our e-commerce. I have gone ahead to create an empty folder in my computer called Learning Go Shop. This is what I have presently and I want you to do the same thing. Just this folder. You can use your VS code terminal anything. Create a folder anywhere on your system. As you can see, there is nothing inside the folder. I'm going to attempt to open it in my text editor which is goland. This is what I use and this is what we have been using. Let me make sure that I expand it correctly. I don't want all this pop up and this is it. So there is nothing here. Still fresh. Let me open my terminal quickly. I'm going to switch to bash. This is how I have been working from the last project. Chat now. What is the first thing we are going to do when you are starting a new go project? The first thing you do is to initialize your goal module by typing go! Now I am making an assumption that you have go installed on your system, and before I even do the go module, let me show my my let me show you my version of go, which is go 1.24.4 as at the time of this recording. Now that I have confirmed that I have go installed, I need to initialize go module by typing go mod init comm. This is my GitHub username. I'm going to name it learning. This is the go. This is the module part. This is my GitHub username. So you want to replace this part with your GitHub username. Of course if you're on bitbucket GitLab. You can use anything you like. Some people don't even put a full URL, but most professional projects use exactly something like this. So you go ahead and press the enter key. And we have just created our go module. Now what that means is this directory that we have now is managed by the go runtime as a module. And that is how we are going to start working. The next thing I like to do is to create a git ignore, so that we don't mistakenly commit files that we don't want in our repository. So I'm going to of course I can come here and right click if you want to do it and type dot git ignore and that's it. You we can start putting the files inside the first thing. The first set of files I want to ignore like binary. Maybe things that have been compiled like your exc five things that we don't want in our repository. So I'm going to ignore all that and this beam folder as well. After that any test files. We also don't want coverage output in our repository. We don't want the vendor folder in our repository. We don't want the go workspace file. We don't want the env file that is going to hold our environmental variables in our repository. Let me close this down. We also don't want IDE settings. You. If you want you can download this um git ignore file from the repository immediately. This all the source code would be on GitHub and you have access to it from day one. Then some other files that we don't want. Our upload folder don't want it, then some database in case you want to use SQLite. And lastly logs that we don't want. So this is our git ignore. It is simple but very important now that we have our git ignore. I want to commit this to git. So I'm going to type git init. I'm going to type git status to make sure that there is nothing else there. It looks good. Let me check inside the gold module. It looks fine as well. So I'm going to type git add git commit and I'll say initial commit. And with this we have just made our first commit. So this looks really really good. After this first commit I want us to do something. I want you to type git checkout dash B one project setup this, become the branch that we are going to work on for this first video. So I'm going to type git branch just to make sure that we are in that branch. And this looks very good. Okay. And next we want to I want to show you something very important here in the browser. We need to install these two softwares. The way they are installed is a little bit different. So this migrate and this um CLI linting program. What is go go lang ci lint. For example let's go back to the IDE. Let me show you something quickly. In my terminal you can use any terminal you like if I type migrate you see I have it already installed. So I'm going to type like you see it's installed. If I type go lang cli lint, I don't think I typed it very well. Go lang ci not cli. If I put it, you see I have it installed. Now let's say I want to know where they are. In my in my computer and I type. This is where they are currently installed. And this is something that you have to do by yourself. So let me explain why. Let's say if you go to let's start with the linter. If you go to if you come to the website and you go to install, you scroll down and then you start seeing different options. Option number one, which is what I used, is you download a binary if you have core on your system. I think one windows you should have core using. But um, the other terminal not, not uh not cmd the PowerShell. I think there is core in PowerShell if you can and then execute it. But for windows this is not going to work because I think this is a a Linux command. But if you scroll down This is for windows people. Now just scroll down a little bit more on windows. You see there is also a different instruction here. You can run the above command with git bash. So which is very good. That means if you have git bash you can just do it using git bash. But if you don't have git bash, keep scrolling down for windows for now and you can use any of this option. This um this package managers that they have for windows to install it. Now for Mac OS x, please use homebrew to install it. So you need to come here on this documentation. I'm going to leave a link to it directly. You can then install it similarly go migrate. Go migrate. Let me scroll down a little bit. Go migrate uses um I'm going to scroll down look at how we assess it. Right. So I'm going to scroll down. Greetings. You click on Getting Started and look for the documentation for how to install it. Okay, it's not there. Let me look for it. There is. You should go under releases. If you go under releases, look at the under assets. They are here. You see there is version for Linux. This Darwin is for Mac. If you are on windows, you probably can get away with one of these. Right. But I'm going to show you another way that we can execute it without installing it directly. But let's for now, if you can install this tool, these are just the two things that you need to have installed on your system. So use the documentation to install them. If you have any problems installing any of the software, please send me a message. Don't say the instructor didn't explain it well and then just go to comment or leave me bad feedback. Just send me a message and let's do it together. I promise you, it's quite easy to get installed all right after that. The rest is really, really simple. The next thing I want us to do is to work on the structure of the project. So the way I want us to structure the project this time around is going to be a little different from what we did in the last video. This is the last project that we did. So let's start like this. Let's create, um, let's create a folder cmd in the cmd folder. We are going to create API. Then we are going to create another folder. This is on the same level with cmd. We are going to create a db. Then we are going to create migrations. Okay. Then on the same level, you see the same level. Let's create Docker where we are going to put our Docker files. Then in the API folder create Main.go inside. And this is your main.go. So we have the main package and just main function. Nothing special yet. Let's print something to the screen. Hello world okay. And we can go ahead. You can type go run from your terminal cmd API just exactly like this and press Hello World. This is the beginning of our e-commerce project. Now if you have this ready, the next thing I want us to do is to set up some make commands. Make doesn't work on windows, but the commands that we have on the mic itself would work on windows. So I want you. If you don't have make on windows, you can run the command directly by yourself. So let's go ahead and set up the mic commands for the make commands. And this is very useful for non-windows people. But all the commands we are going to have inside for sure you can run the commands on window. So the first command I want us to set up is the build command. So we're going to type build. And then this is what we're going to do. So when you run make build it compiles the app and put it in bin slash app. So I can make build. And then it does that. So we should have a bin here. Now with the compiled app. Like I said we can just delete this. It doesn't go into our git. If you want you can just type go build our Output. Bin slash app. CMD slash API. You see, this is something I have to do over and over and over again. Imagine typing it all the time. This is why make is very useful. Now I press it the same effect, but just more typing. So this is why I love make. All right. The next one is for run. That's very similar. Uh, good. Then we have one for dev, which is exactly like run. Then I have one for the linter. Okay. And then we're going to have one for migrate up this one. Um, let me set how I want the URL to look. So the when we start our Docker for with Postgres, we are going to have the username as Postgres the password will be password. This is going to be the host. So port 5432 must be available on your system. Then we are going to have e-commerce shop as the database. And that would be it. Then with this up command. Similarly we're going to have make migrate down. So when we run make migrate down it will run this command and very similar five four the same thing just the down. The reason why I put this database connection here, this is no problem. This is only for Docker. The reason why I have it in here is because I want us to be able to run this command easily without suffering. Okay. If I run. If I run this one now, some of them will fail. For example, because there is no migration yet. Migrate. Migrate up. You know it's fine. We're coming to deal with this later. All right. After that, I want to add, for example, if you run make without typing anything, I like it to run a different command. This help. And then something like this the help basically allow usage sort of a short documentation for someone that just came to the project that want to know what it does. Then we have to do a small thing called phony. So, uh, dot here, if you notice the dot phony has help build run dev lint and then our migration command. Now, I can explain that in a little bit. It's sometimes if you run make if there is a folder here called run. Let's say I create a folder here called run. And I don't have this for this phony. This run here in the phony make will not run this command make will ignore it because of the folder that is there or a file. So having this phony you are telling basically making it very clear to make that, hey, there is a real command here called run. It helps to manage the clashing of files and your make commands. So this is very, very good. The next thing I want us to set up so that we can end this video is Docker compose. And that is very straightforward to set up. So I'm going to come here. Create docker compose dot y. Okay. All right. So let's start with the version. The version is not according to the documentation. The version is no longer important, but I'll just put it like that. Services. What do we need? Postgres for sure. So we need Postgres. I need the Postgres. Let me choose the version that I want this 12 and the container. I think I don't really care much about the container, but this is fine. The user. Remember we said that the user was going to be Postgres and the password was going to be password and the database was going to be, uh, e-commerce underscore shop. That's it. When Postgres is starting, it will create this database for you. And the port is correct. Also this, um, volume and the path is fine. Next let's do for. local stock. So local stock I'm going to accept everything now. So local stock. Local stock. Latest. This is very good. Container name is fine by me. Environment. Now the services that we want to support for the start. Let's keep it on S3 and SQS later. I will explain why I'm putting SQS there then for the region US east one. By default it doesn't really matter. Um, I can if there is default region and if there is also AWS default region. You can set both if you want, but it doesn't really matter. Access key AWS access key and test test. In case you want to do that then the pots are. This is the standard pot for local stock. I don't I'm not going to accept this one. Okay. And then volumes uh, can just name it like this Lucas local stack underscore data and we map it to. It should be mapped to var lib local stack. Okay. Then we just come outside here and define the volumes and local stack. Okay. This is it for the Docker composed. I'm going to go to make and add it. So I'm going to come here and say uh docker up like this. Docker down, you see that we are referencing the file from our docker folder. I intentionally don't put it here. I like to have my things organized after that. The last thing that I want to do is to create another file, our env dot env example. This one will be in the repository. This is where people can copy to make the env file. So let's start with port for server 88. It's always a good port. And then g mode depends. It can be released. Can be test can be debug. For now I'm going to keep it on debug then db host db port db user which I'm going to put as postgres db password will be password. I want to have it to match as close as possible. DB B name would be e commerce underscore shop and then the database SSL mode will be disabled. Right. What else. JWT which we have not done yet but I want to keep it here anyway. JWT is something we need to put a very long key there. Then J-w key expires in let's say 24 hours or more if you want, and refresh. Refresh token expires in let's say 72 hours, something like that. Okay. Then for the AWS, AWS region. US East one AWS access key, which we have already has test the secret. So this is the key ID and then the access key itself. Test test. That is what we put in localhost. Now when you make an upload the bucket the AWS S3 bucket I'm going to call it e commerce something like this. Let's say e-commerce uploads. Then what else AWS endpoint AWS S3 endpoint like this I think that's about it. Then finally when if we are going to upload to local system, maybe upload path. This is what we ignore this part in our git ignore. So this is good. Max upload. Sykes. Max upload size. I want to put this in a byte, not a number like that. So 1048457 60. You can put anything you like about 100. All right. So this is what we have. What you can do now is to copy is to just, um, make a copy of it. So I'm going to go to for my ID, I just go to refactor. Then I make a copy as dot env. So this is my real env. This will be in the repository. Um, honestly that's about it for this video. Thank you for watching. It's quite a long video, but we've done so well. And in the next video we continue to work on the project which we add the linting, um, configurations.
