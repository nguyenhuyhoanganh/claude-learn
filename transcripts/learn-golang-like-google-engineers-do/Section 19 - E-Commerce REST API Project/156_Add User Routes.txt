# Add User Routes

The last video we created the two handlers. Now let's commit them. Okay, so git status. Now we need to do git checkout dash. B15 add user handlers git status git add git commit add user handlers. Okay, while we are here, let's just check out git checkout dash B16 add user root. So and I'm going to make sure that we commit this one before we end the video okay. Let's add the root for these two methods. Quite straightforward to do actually. So I'm going to go here open server go. So server slash server dot go. And here so this is the end. And here we can put a protected, um, method. So let me come here. Since we are here I can we have the auth here now. Great. So I'm going to say protected. I don't want anything to. So it's going to be like this. Now that we have it let's say protected. Now we need to add the middleware because without the middleware the user ID won't be injected. We won't be able to check for authentication. So middleware and then like this. Okay. Since we have the protected route now we go to users. We do the same thing. Sign so users dot get. Slash profile. So let's get profile. This is the handler that we just added. And the second one is going to be a put request and then update profile. Okay I hope this is not confusing. Of course our linter is going to scream at us. So I think it's a good time to lint. Let's lint it make lint. And whatever problem we have, we can use this. We don't. Don't worry about this. We can. We're going to fix this later. But for this, we can fix it. Line 52. This one, this is what it's saying. So let's do this. We can say user root like this. I think like this. And then so this is some sort of a cheat to get the linter out of our way. And you see that it doesn't complain anymore since we have a variable here. This is what it's looking for. So that is another way. If you don't want to continue including this signal message, you can just do something like this and that will get it out of our way. Okay, with that in place I'm going to say make run. And now that is running. Let's bring up our postman. Let's log in. This I'm able to log in successfully. Now I can forward it. Let's go to users. This is Get profile for now I'm just going to make the request. You see invalid token. Uh, bearer token is set here. About the token here is no longer valid. So I the one I copied from there. I'm going to paste it in here. If you don't want to use this variable, you can delete it and put your token directly if you want. And then the request that I made to get my profile I was successful. This body is the relevant. You don't need anybody's a Get request but it returned the profile okay. What about update profile. This is the update profile. In that authorization I have the bearer token set here. And then here I can make it. Uh. Mark. Mark, look, I don't I'm just typing anything that comes to my mind. Then I press it and you look at it, it was updated correctly for the phone number. Maybe we can put something more dramatic update was also updated. So it's working. No errors, no problems. Everything works as programmed. And you can see the SQL out here. Something I don't really like about this is that we are seeing here is this. Let's go to database. And here that we're connecting to the SQL here. This is where it's this is where it's coming from. If you want you can change this to error something like this. And then it only print out errors. So when I do the update now you see we no longer see the SQL. But for development you may want it. So some maybe we can put like a. We probably want to put like conditional statement here to do it for during development. You want to see the SQL. During production, you don't want to put the SQL because of password and things like that. So I'm going to ignore it for now because I really want to be seeing the SQL. Okay, so quick recap before I end the video for this one, what did we do. So let's go to git status. And the only file that we changed was server slash server. So let's open it up server slash server dot. Go and look at the other changes we made. We added a protected route by including this middleware. Very important. And then we created the routes. And we introduced this variable inside this bracket just to make sure that it doesn't continue to bug us. So that will be it. And then I'll go ahead and commit my changes and user routes. And we have tested it and it works very well. So thank you for watching and I will see you in the next video.
