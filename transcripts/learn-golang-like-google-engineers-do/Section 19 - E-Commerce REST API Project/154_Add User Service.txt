# Add User Service

Now that we have tested our API and everything is working, let's continue adding more features. Now let's work on the user service in the user service. We are going to have two features which is the Get Profile and Update profile. So let's start by creating a user underscore service in our services folder. So you go to Internal Services. Just create a file here called user underscore services. Go. I always miss putting the dot go. Okay. Now let's put it in the services package. Okay. Just like the first service we are going to create a user service struct and then inject our database connection. Very simple. Now let's create a function that help us create this one. This value. Good. After that, we need to create a get profile, which would take a user ID and help us find the profile for that user. So get profile. This is the model that we are going to use. So this is user model. Let's try to find the user using the ID. If we don't find the user will return an error straight up. If we find the user let's just return it and that's it. If you notice we are returning the D2. This is very important when in the at the service level we are dealing with the D2, we are not dealing directly with the model in terms of what we return to the user. We want to return D2 to our application that time. All right. The next is update profile. We take the user ID that we want to update. And then the new payload they are sending in like the the first name last name and phone that they want to update. And then we can return user response which is this one again and a possible error. Okay. So the model we can try to find the user. If we find the user Let's now update the user. So we update these three values. After that let's save it back. So db dot save. And then if there is any error we return the error. If not we return get profile. So basically we are using this. One question though is wouldn't it have been possible if we call this one here first. Well if you call this one here first then the the value that you get back is the D2. And we why we probably can work with that. Um this is actually fine because you also this is just a single call as well which we are still making here. So if you call it, you're still making a call. All right. Um, and that's it. So the good thing here is when you make the update and you make this call, you fetch the updated data. So this is very good way to do it. All right. And that's all for the user service. The next video we are going to work on the handlers for this user service. So see you there.
