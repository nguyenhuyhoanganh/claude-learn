# Add Password Utility

Hello. Welcome back. In the last video we worked on the JWT utilities. Now I want us to do the same or similar thing for password, a way for us to hash password and also check for password. Okay. Create a new branch type git checkout dash B8-8- password dash utility and then press enter and make sure that you are in that branch. And let's start coding. All right. Go to utils where we have JWT and response and create password go. It's going to be two simple functions. So func hash password. And in the hash password we are just going to take password. We're going to return the hashed password or possible error okay. And that is it. So that's all for the function. We can add a little bit of text here. Hash password using B Crypt at default cost. That means I'm going to change this to from 14 to Bcrypt the default cost, which is ten. Func check password. And then I'll take the password and the hash. And then we check just a boolean. I'm going to return boolean. So if there is none we know that it's not correct. And that's it. Good. And that's it for the password utilities. Let's lint it and see. Make lint. No errors. So git status good. Git add everything git commit add password utilities or something like this. And that's it for the video. See you in the next one.
