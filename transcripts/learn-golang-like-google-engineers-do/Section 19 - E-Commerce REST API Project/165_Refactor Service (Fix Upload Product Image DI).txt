# Refactor Service (Fix Upload Product Image DI)

Okay. I want us to start. Hello. Welcome back. I want us to fix something. All right. So in product underscore handler's go, which is in the server folder. So here this file let's go to upload product image. You know we worked on it just recently. That handler we need to do something. Look at what we are doing here. We are creating the file provider here. We are creating the service inside it. It's not nice. Let's refactor it quickly. The first thing I'm going to do is to delete the both lines. So delete them. And then I'm going to put s dot here. That means we assume that we are going to have a field called upload service. We don't have it yet. So let's go to server. Now there is an upload service here that we mistakenly put in the last video and is pointing to the interface. So let's change it to point to the right service. So services dot upload service now is pointing to the right service. Okay. And let's check the handler quickly. You see the error has gone away. Now let's go back to the server struct which is here. So click on server Go. And this is where we were. All right. Now that we have the field let's add it to the function. So this new let's add it to the arguments as part. So it becomes a parameter. And then let's add it here as well okay. So when we go to main we need to construct this service. So let's go to main. So cmd API the main dot go and look at here we have an error now because the Upload service is missing. Okay, so let's create it. Upload service equal services dot new upload service. But then we are going to have a problem because where is the upload provider. Now we need the upload provider. We for sure we can create one. So providers dot new. Let's import the package. New local upload provider. And it takes the path. And we can just give it the path from config upload dot path. Okay. You see we brought out the construction to main so that we can control it. And this is dependency injection. So here we are injecting all the dependencies using a A constructor style, you can use method injection, but this is a constructor injection. All right. With this small refactoring, we can attempt to run the program to make sure that the program is still running. Good. Let's bring up, um, let's bring up our postman. I can send and the upload still works. So fantastic. That's it for the video. Make sure you commit your changes to git status. Git add I am still in this branch 24 add dash upload dash handler. So the same branch add everything git commit refactor. Handler. And that's it for the video. Thank you for watching. I will see you in the next video. Let's start doing AWS stuff.
