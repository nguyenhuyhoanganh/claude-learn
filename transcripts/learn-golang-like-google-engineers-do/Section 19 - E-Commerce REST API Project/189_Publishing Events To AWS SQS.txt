# Publishing Events To AWS SQS

Okay. Welcome back. In the last video we added this implementation for our event publisher. Now it's time for us to set up how to use it. And we can use it any way we want. So let me go to the services and then the auth service. This is the only place I'm going to implement it for now. Then I can say let's call it event publisher. Event publisher and event publisher is coming for event that publisher. So this one remember we are using the interface. This is important. We're using the interface. That is why I'm not putting a pointer here. You shouldn't do that because this is an interface. So we're pointing to the interface. Now that we have it in there I need to also include it in the constructor event publisher, and we can come here and say event. Publisher. All right. Then we need to also set it here. Of course, setting it here doesn't solve the problem since we are using this code I think it's in main. So go to. This is where I am cmd slash api slash Main.go. This is where we are initializing our service and we need it here now in order to create it here I'm going to come here and say um, maybe we have the database already initialized here, but we don't need database for this, so we can do it anywhere we want. But since everything is up here, let me keep this too close to each other and I can say event publisher. Equal to events. Dot. What is the. Let me see. I need some help with the import here. And this is it. So new publisher. Now what? What does it take? Takes a context. And then app config AWS config okay. So what we want to do let me look for the. Let me look for how what we can do. We need the context defined. Let me see. Um we don't have any context anywhere here. So that is fine. I'm going to come up here see. Like this. and then we're going to pass it. And after that what do I need. I need this config dot aws. And that should be it. Or we should check for error. And I'm not going to use theta I'm going to use error. Because if we theta then the the files are not going to get the opportunity to execute. After that we can return if there is any error. Okay. At this point everything looks good and we can just pass in the event publisher. This helps us with initialization. Um, let me run the linter quickly to make sure that no mistakes. Great. The linter run no errors and it looks like we are good to go. Now with that Initialize. I want to open the file browser again and go back to the auth service. And then here. We can do we can use it here. So let's say this is login generate response. Let's open this function. This is the last place that we use um we generated the response and the generate response returns um an error. So I think maybe in here uh we can call the event that publish. So what does it take. It takes an event type. I'm going to just hard code it here. Now say um let's call it. Login. Um. Let's call it login I think. I think user logged in or something like this. Maybe it's fine. What else do we need to pass? Payload. Um, what do we have here concerning possible payload? The user itself. We can pass it. And what else? Metadata. I'm not going to pass any metadata. So if we want, we can pass just an empty map like this. And what does it return a possible error. So we can go to error like this and check if error is not need we return the error. But instead of just returning the error, I want to at least wrap it like this. And this is how you put message in the queue. If everything goes well, we should see this message in the queue. But this is not going to work yet. Where is the queue in AWS? We haven't created a queue. So let's go to our AWS config in the docker in it. Remember in our local stack init. Let's add the code here to help us create the queue when the code is starting. So you see we have uh, I can move this code up here like this. I thought when I, when I, um, copy it like this, it moves everything. It should be able to do it here. So I put a code up here. I don't need this one here. So when we start, we create the that queue and that should be it. Let's give it a shot and see. So Docker let me. So I want to say make. Let's make sure this is safe. Okay. Make Docker down. Okay. Make Docker or. Local stack is started. But let's check something. Do we even have. Where is the docker compose? Um. Local stack. Okay SQS. So we have SQS as part of the services. Make sure that you have S3 and SQS enabled. And that is what you are using here SQS and S3. Okay that looks like we did it. But I want to go. Let me bring up my docker. This is my docker Docker. Let's open it up. This is a different thing. The latest one out quickly. So this is our local stack Castor container. I'm going to click on it, then execute. This should put us inside the container. So I'm going to say AWS local. This is inside the local stack container. Now I run the SQL command and then list. And look at the queue was created when it was starting. So this is really really really good. Um, I can put another command to attempt to see all the messages in the queue. And there is seem like there is nothing yet. Okay, now let's do something then go run or make run. It's looking good. Okay, everything is running. I'm going to go to login. Try to log in and see. At least we are not seeing any errors here from the publisher, so that's a great sign. Let's go back to Docker and try to catch all the messages again and look at the message. Think about this. This is so good. This is look at the event type that we put and user logged in that we put water. May added their own sort of metadata where the the Uuid and all that. This is okay. But look at our body the body of the message. This is the user data that we put in the queue, the I am email ID, first name, last name and everything. But you notice that the password is not here. Password is not here because we say that whenever we are doing JSON encoding, don't put the password and that is coming from where is the model user that is coming from, where is the password. This dash that we put here is you see how fantastic that is. So we have been able just with few lines of code, we have been able to create an event publisher that we can use to publish events to the queue. And the next step, next level for us, is to get these messages from the queue and start processing them and send emails. So honestly, for me this is fantastic and thank you for watching. And we are going to take the um, we're going to take it to the next step by consuming these messages and sending an email. We're doing everything locally. You are not going to spend any money and that's it. See you soon.
