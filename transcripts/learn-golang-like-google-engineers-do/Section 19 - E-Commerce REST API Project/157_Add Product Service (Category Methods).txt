# Add Product Service (Category Methods)

Okay. Welcome back. Now let's work on the product service, everything related to product. Let's work on it. So git checkout B 17 add product services something like this. And then we are in a new branch. Okay let's open our file browser and then go to services. Right click new go file product underscore service go okay. Very similar to what we've been doing here for example that we have something like this. Create a constructor and then we add everything is very similar. So the first thing we want to do is to have a product service look at it almost the same thing. Then the constructor and that's it for that. Next is we want to be able to create categories. So let's add a create category to the product service. So we're going to add a create category method. Okay. The create category method will take a request. So which is create category request from the D2. Look at the payload. So just name and description in order for you to create a category. And then we'll return a create category response which would contain this data. All right. Now let's create the data that we want to put in the database. Basically the model name and description that we receive from here. And now let's attempt to create it in the database which is super easy. You call DB create and pass it the address of this model. And then you check, you know if there is an error we return the error. If not, if everything goes well, we construct this response now and return it right. So far so good I hope no confusion. Right? Right. One of the one of the greatest things I like about the AWM is that you don't have to write SQL, and we can change database anytime we like. Honestly, we can just change the database now from Postgres to SQLite or MySQL and everything will still work. So it's quite an interesting, um, program. After that, let me make some space down here. After that, we want to add a get category. Um, I think we should add a get category here now. Okay. Forget category. What we want to do. We want to find a list of all the categories and just return it. But we need to convert it to this D2. Like I told you, we want to always return it to you. But first of all let's create a slice of category so models or categories. So we create a slice of it. And then we query from database and map to it, which is very powerful. You don't have to do the scanning by yourself. Now let's make response that we want to send out. I want to explain this a little bit since we when after this query here, we are going to have a some sort of a list. Let's say if the mapping here returns 20 result here, that means we this line will be 20. And they make here that we are specifying. If you look at make right, uh, we are able to specify the, the capacity that we want it to have since, since we make this we are going to eliminate a further allocation. Right. If we don't do this, we don't put this pattern, we just make, uh, and leave it or you put zero and leave it. Then there can be re-allocation if try to think about it like this. If they are array, start with ten Internally and you the data that you want to fit into it is 50. What would happen is go would first of all, check the size of the data and start filling it. Filling it in. As soon as you reach the maximum, it creates another portion of memory and then copy the old one there and try to fill it in so that copying and reallocation is what we are trying to avoid here by specifying this lens. So it's very, very important. After that we just loop and then we fill it in and return it. Now there is something I'm doing here which um, if you in case you don't know, there are two ways to loop this thing. So since these categories we can also loop it like this for. Let me ignore the index and say maybe category like this. This is another way to sorry categories. This is another way to loop and this is also very common. But here is the problem with this. In each iteration of the loop, the go compiler would copy this. So this is going to be a new copy that it will copy from here. So in this loop you are going to have a copy. So if you have 1000 items here it's going to make 1000 copies depending on the fields. So if one item is like 500 bytes then you're going to have 500 bytes times 1000. But for this this flavor of the loop that we are using index, it doesn't copy it because you still have access to the old one. So here we are using the index to sort of repopulate it using the. So it's um, this is better for things like this. That can be really large. Okay. Now after all that talk, let's add uh, the update category service. And then we need the, the model that we want to update. Okay. We try to find it first. If it doesn't exist we return an error. We cannot update what we don't have right now. Let's update just the name and the description. Okay. After that, if it's active is not new, let's set it. If it's active is not set. So basically we de-reference it and set it here. All right. After that we should attempt to save it which is calling by calling DB dot save and giving it the model that we want to save. If there is an error, we return the error. Classic stuff, right? And after that we need to return this response with the updated data. And that's it for the product service actually. So let me scroll to the top We created a struct and a constructor. We added a create category and then get categories. Update category. And that's it for the product service for this video actually because don't forget um, this is I'm preparing the groundwork for um for products like ability to create products, do all those things. So this is a sort of an entry point into it. Now we can do categories. We need to also do products. Then we can do cats and all that. But for now this is where we're going to leave this video okay. And then git status git add. Git commit um product. Maybe we should call it that category services for even do. Yeah. So that's a good commit message for this. And I will see you in the next video. Bye for now.
