# Add Auth Service

In this video we are going to start building the services. Now, before I start writing code, I want to explain something to you. And this what I'm about to explain is very important. What I'm about to explain you would find in all go applications and even beyond. Here is how most go applications are organized irrespective of the software architecture they are using. Most times here is this. I'm going to delete this, but please, um, let's focus on the idea here. Most times you have some data, right? Data then you have model. The model is directly coupled with this. Right. Then in between these data. So this between your this is your data here. So your model is mapped to the data right now. How do you fetch this data from the database? When you fetch data from the database, you have the mapping here. Some people here, they use something called repository. The repository is nothing but repository. It's nothing but a struct or a class or something that you use to fetch something. Now this repository can use for example, the repository can use let's say uh go or it can use plain SQL anything. It can even use MongoDB. Right. Can use MongoDB, whatever that you can use. Repository can is responsible for fetching the data and Returning, um, the model to whoever that is interested in the database. So when you need something from the database, you ask the repository if you are doing domain driven design. Sometimes they put this repository in like an infrastructure folder. It doesn't matter where they put it, you know, um, they have repository. Now after the repository some people add another layer here called services. And the services right. The services is responsible for doing the business logic. And here is why. If you imagine you have something called login service and you want to use it when on the HTTP context, like on the web or maybe on the command line, it's easier than let's say you put the whole code in a handler in order for you to use a handler from the command line is very tricky. What if you want to do GraphQL? What if you want to do gRPC? So what they do is they encapsulate all the business logic right here in the services. And then from services from services they can have uh let's say HTTP. Maybe there is HTTP here. In the case of go we have handlers right. Then you can have if you want CLI you can have uh graph. You can have G and many many many more. For us we are going to do it a little bit different. And here is why. For this project there is I don't see a lot of benefit adding this repository layer, but we are going to have services, but they they the database assets, we are going to call go directly inside the services and do everything from there. Then our handlers would use HTTP, handlers would use the services. That is the only difference. These are a simple idea but very powerful. Please don't take it for granted. Whenever you see a program in go, you look for some kind of repository or some kind of database access, you know, or what, whatever they are doing. But there is always some kind of encapsulation going on that they are using to encapsulate all the database operation. You look for that. Then there is another layer where they are doing some business logic, and then there is another layer that is exposed to the world, like the handlers and everything. All right, with this out of the way, let's go ahead and start building our first service. So I'm going to git checkout this time around early. So git checkout dash b 11 add services. Good. The first thing I want to do is to go to our folder structure. Make sure we have everything lined up correctly. Then in internal we add another folder called services. Right in it we add another file called auth underscore service okay. With this we can put the first service that we want to create which is the auth service. So it's just a simple struct. And what are our dependencies. Let me close this side. We are we depend on like I mentioned we depend on the db and also the config, we are going to need them. So that is it. Now let's create a constructor service to allow us construct this outside. Okay. And that's it. Nothing difficult so far. This is the exact same thing that we have been doing all over and over and over again. Next let's work on the registration service. So let's come here and say func or service right. What services about input and output for your service. What what do you want for your service to receive. What kind of input and what kind of output it should provide? Always focus on input and output. This is where our DTO shines. You don't want to expose your models at this point. So we are going to receive a request of DTO dot register star request, and our output would be a pointer to auto response and a possible error. You see how simple the input and responses input output. That's it. Now let's do the job. First I want to write out some things. We check if user exists in the database. We hash the password. Then we create the user. Maybe we can create a cat for the user as well. And we generate a token. Okay. Let's make sure we don't forget. So let's find a existing user. Now let's use the this db to query the user. And this is where we are going to start using ORM. So I'm going to say db dot where we are going to find by email like this. You see equal to just this placeholder. Then we are putting the request dot email. Okay. Then we find the first one. Now this idea of first and then you try to bind it to this is why is powerful. But because we can easily bind it to this and we don't need to do much. We can get the error. So we return an error like this. That means we can we can change our expression to be. If l equal this error, not ni, we know the user doesn't exist and we can return name and errors. Dot. User not found or like that. This is it. And this is why the ORM is powerful. Remember all those scanning that we used to do with SQL? We no longer have to do it here. This will take care of it for us after that. At this point we have the user. So hashed password would be utils dot. Remember we worked on this function together. Now we check for error. Good. After. If everything goes well let's create the user. The email. The password will become the hash. Password. First name. Last name. The phone number. By the way, there is a library you can use to format the phone number in case you want it to roll. We can do models dot user to be the user by default or this one user wrote customer yeah. Then after that we create the user, which is the same thing very similar to this kind of operation. You see we call db dot create and then pass the user object that we want to create. If we can create it, we return an error. After that we create a cat. When the user exists in our system, we want the user to have even an empty cat by default. So we create the cat, give it a user ID which is this user, and then we call db create again this time around. Honestly, if we are unable to create the the cat, I don't want it to fail. We can just put, uh, we can just put a log here. We may need to bring in the logger, but for now, let's just say log dot. Uh, maybe let's print a message out here for now. Print airline. I'm going to import it later. Unable to create Cat, but we don't want to fail it that way. So after that we can do something. I'm going to return a function that doesn't exist yet because, um, I encapsulate that into a function because we need it. So let's just return s dot generate or response. And then we pass it the address of the user. And this is the user service. Let's see. We have a typo here and hash password. And that's it. We fix it. So very simple and a read rich treat. Don't worry about this function. We are going to do it. Now let's do for login. So this is for the login. Very similar. We try to find the user and make sure that the user is active. You see we are passing we have two placeholders here. Now we are passing two arguments to to it. Let's check for password. Now if we find the user. And if that's the case we should return the um this response. That's why you see the reason why I encapsulated it. Because we're going to need it at multiple places. After this service we have the register and the login service. Now let's do the refresh token service. Okay. The refresh token service is also very simple. First of all, we we validate the refresh token from the request if A refresh token is valid. We try to. Find the token in the database. Very important. If it's not valid, we don't need to stress ourselves going to the database. If we find that it's valid, then we go to the database and try to find it. Then we can now. After that the user itself. We find the user. If we find the user, we delete it. We delete the refresh token not the user. Very important. And then we can return the new token. Because when you refresh your token you want to get some new tokens back. All right. For logout logout is very simple. We are just going to delete the refresh token from the database. And that's it. So you pass us the refresh token. We delete it from the database and that is logging out. And then let's create this function now. Okay. For this let me. We have, we remember we worked on this together, so let's fill it in. Let me pass the pointer to s dot config. Dot jwt and then think user.id user dot email and then string user dot rule. I think that should be it. Now let's not forget the comma here, except you want to bring this over here. Let's check for error. If error is not near will return. If not. Now let's store the refresh token in our database. Okay, we store the refresh token in the database and we will return the response to the user, which is very basic stuff. We prepare the user and then the access token and refresh token and that's it for the service. Right now we don't we are not able to test it until we build the handler. So we need to build the HTTP handler that can use this service. And that will be the next thing that we'll do in the next video. So I want to scroll a little bit so that you can in case you want to type everything. We created this service a constructor function. We implemented a register service which basically tries to find a user. If it found a user, we hash the password. Then we create the user in the database. We create an empty card for the user and send the generated token response here. So login as well. We try to find an active user with using their email address and is active. If we found the user, we will check the password if password is valid. Will return response that contain access token and refresh token. When you refresh your token, we validate it with make sure that it exists in the database. We make sure that the user that we extracted from the client also exist in the database. And then something that we might want to add to this is maybe you even want the user to be active because you know, anything can change, right? If that is the case, we delete the refresh token and then we give you a new pair. All right. And then uh, log it, log out. We just delete the refresh token that you want to log out. Then the last one is just the response which we use to generate the token pair. And then store the refresh token and return response. So that's what we did in this video. Thank you for watching. And in the next video we are going to continue from, um, from where we stop here now. Bye for now.
