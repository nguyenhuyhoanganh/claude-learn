# Setup Database Connection and Logger

In this video, I want us to start setting up database connections and everything related to database and the logger. So let's go. Open your terminal and type git checkout dash b put two config. Database logger. Something like this. Now we are in another branch which is the second branch of our project. Good. Okay. Before we can connect to the database we need to install some things. For now I want us to type I want you to type git sorry I want you to type go get and then let's install gym. You see it's installed gym. So let me check here. There is, uh, a lot of things already pulled down. Okay. That is good. Let's go back here. Go get. So let's install go the go dot EMV. Good. You see the versions that have been installed in case you have a different version later in the video, you want to stick with these versions so you can check the repository for the version before you install a newer version. Okay, so go get let's install zero log. Very powerful logger. Let's install the go library and go get. Lastly the Postgres driver for go. And that's it. Okay. For all the libraries that we installed I'm going. You are still going to see them in the import. So there is nothing much to, um, write out. Then the next thing I want us to do is to set up the file, the config and everything. Let's do it. Let's go over to our file layout here. So I'm going to right click folder. Let's create internal. Okay. And then in the internal we create config. Okay. And in it you create config go. Okay. Make sure it is like this. So far so good right. Let's make some imports. So we're going to use the go env and all this. I'm going to try not to save anything because as soon as I save this is going to disappear. This is how my IDE works. So I'll try. Now let's create a config for the server. So server config the port and the gym mode. Remember here let's go to the env quickly. So basically this is what we are going to map to the two variables we have here. So we are going to map the port and the Jim mode to these two. Both of them are strings as you can see. Right after that the database config. So host port user password name and then SSL mode. Look at it. Look at this is these are the variables we are going to map there okay. Then we need for the um we are going to need for the JDBC. We are going to need for the Amazon which and then upload. And lastly you see we have for each of the configuration group we have different struct for them. These are basic data structure that we have learned over and over again. Now let's I want to put one over at the top here that uses everything. We don't need this for now. So when you go to, let's say when you want to access a config, you can type config dot server config then dot host. That is how you can uh sorry dot port. That is how you are going to access something like this. So this is going to be very powerful. Now when we are, when we want to load our config, I want us to I want us to do a little bit of, um, what do you call it? Uh, I want to have a function that help us manage the data. So we are going to first of all, look for the data in environmental variable. If it is not there, we will return the default one. So let me do something uh get env. Okay. And the key and the Default value. And of course it returns a string. The autocomplete is getting more crazy and crazier every day, so we've tried to get it from the environment. If it's not there, we if it's there, we return it. If it's not there, we return the default value. That's it for this one. Okay. Next let's work on the function that does the loading of the environmental variable. So basically a constructor function the constructor function should be very very simple. I want us to pour it. Um maybe we can put it down here just just above the get env. So func the id already sort of know what I want to do load. Then I'm going to try to get a pointer to the config and a possible error. Okay, now the first thing I want to do is to try and load the, the the variables. So using the go env call dot load. Now let's check something quickly up here. You see we have it. But it's looking like we don't have it. We need to run go mod tidy later. Um. So just keep it this way. Now go. Env load returns an error which if we try to use check for the error and return nil or exit the program. What it means is whenever there is no env here, we won't be able to run our program. But because I want you to be able to run this program in like a Docker environment where you may not have this env, you probably are going to have your environmental variables in the environment, which is fine as well. So we are going to ignore the error. And then uh, the next thing I'm going to do is to the JWT, the 24 hours and everything we have, we're going to pass them. So I'll pass the JWT expiration and the refresh token expiration and the maximum upload size, um, size as well. Okay, good. Let's closing. That part makes the screen a little bigger. So basically we are extracting the same variables that we have up from there. Next is um let's return. Let's do something I'm going to return like this. Then the possible error. Now our program should. Looks good. It's looking good now. All right. So we have here uh, in the config up here. If you come here we have server to say database jwt AWS and upload. Right. That is what we have to set. So let's start setting them. The first server. Then I put the server variable. You see after that we have database. I want to make sure there are no errors. Put it put all the database variables okay. After the after that. Uh. After that we should set for JWT. Okay. Let's put the JWT variables. You see we are using the Ktnv right after that we set for AWS. If you want to make sure you you don't get error, you do it on a single line. Then you expand it and put the variables. is easier then I'll upload. And that's it for the config. Let's look at it. Okay, just to fix that error go mod tidy and then my ID should reload and do it. But it doesn't. So let's see. Let me go to my IDE settings go go go go modules. And you see I need to enable this. So if you are using VSCode I don't think you're going to have that issue. So this is good. It looks good now. No more errors. So after this let's go to let's do logger. So here in internal I'm going to create another folder logger. And then in it we create logger. And let's start bringing things in. What do I want? Um, let's create a simple function here called name that returns zero log. Zero log. Like this I don't want all this autocomplete yet okay. And that zero log we're going to need the OS I think from zero log again we are going to also need the log. And probably we're going to need a time as well okay. So we remember we're returning zero log dot log. Okay good. So let's start it zero log dot time. Time field format. I want a different time, which is the ah 3339 naught nano. This one. You can click on it to see other options there. Look at basic. This is how the time layout is going to be. If you want this you can choose any one you like. Okay, let's go back. Don't worry about this. When we run, go more tidy. Everything become nice. Okay. After that we try to check for something info as dot. If they get um. Gene mode is not a release. We there is. We want to, um, set the output to console. You know, uh, let's let's do it. Always dot get e. Gene mode. Not equal to. Release. Okay. Log dot logger. And this is what we are going to have. But I want this to go to stdout is fine. And uh, after that we can return. Log dot logger. Okay. Now with that I'm going to run go mod tidy quickly. And it's bringing everything in now. Okay. Looks good. So let's hope the IDE reload quickly. All right. IDE reloaded. But, uh, the name that I have here is not correct, so that's why. Good. It now looks like, uh. What it should be. The next thing I want to do while we are here is go to internal, create another folder called database. And in that we create database go. Now let's connect to our database system. So the the thing I want to do, the first thing I actually want to do is to make sure that we have the import. So I'm going to come here and say import. Let's have it like this. We are going to change it shortly. So new let's create a new config CFG is coming from our config package dot database config this one. And what we want to return, we want to return the Gorham pointer or some of the resources or the connection and the possible error. Okay, so far so good. Don't worry about the squiggly line. We'll deal with that shortly after that. Um, we the database connection string is something that can really make mistakes. So let me bring it over and show you what it is. So the DSN host is equal to this user. Equal to this password equals this and very straightforward um, thing. And then we populate it with our config variables that we already set up coming from the env file. All right. So far no confusion I hope then this is where we do the real connection call. Um we call Gorham Dot open because we want to use Postgres. That's why I brought in Postgres here. So we call the Postgres driver and pass it the DSN. And then we have some we have access to some config here which we can look at shortly in the config is possible for us to set logger, but I'm not going to set it now. We'll come back to it later. So let's check for error. Pharaoh is none here. We return nil and then the error. If not, we return the DB. Okay, now I'm going to run. Go mod tidy. And wait for the IDE to reload. Good. Now that the ID has been reloaded, I want to set a little bit of, uh, logging here like this. So this can give us some small, um, logging information. With this in play, I want to go to main and then let's initialize this. So let's open the folders, go to cmd API Main.go. And here let's initialize our logger. So I'm going to call log. So this is going to come from. Let's import it. This is our logger is not new is coming from here. So this is the import path. Now there can be some time if you import the standard logger from this from the standard libraries clashes. So careful with this. We are replace. This is the logger that we want to use. We don't want to use the standard one. So config. So we're going to load our config check for error. If there is an error look at how we are calling the logger. We call theta because we cannot continue with the program at this level. If not we try to connect to the database and then we check for error. Again. If there is an error, we theta. If not, there's something I want to do. I want to be able to close the database and go or mix it a little bit tricky. So here's what I'm going to do. Main DB we can obtain the main DB and if there is an error we filter and if not we can defer the closing. And main db dot close. After that we can set the gym mode, gene dot set mode, and then we log that we're going to start server. We haven't started any server so we are only a few things here in the import side. Let's we need to import gene which we don't have yet. So let me do git more tidy sorry. Go more tidy and wait for it to reload. Good it reloaded. We don't need this one and Everything looked good before I tried to run the program. I want us to do a recap. So let me open the folder structure. Most of the time in this video we work on this. Internal, you know, um, starting with config which is quite straightforward. We created different struct and then we use this load method to map all the data from the env file into the variables here. So all the fields here now point to our environmental variables which is very good. Okay. After that um, database which we just did just make connection to the database then logger which we also just initialize an instance of zero logger. And then lastly go to main dot, go inside the API folder. And then we try to set everything up with the logger, the database using the config, and then printing out a message. So let's try to run it. I'm going to write make run. And then um, even though we are seeing an error it doesn't look that bad. So fail to initialize the database. And what does it say? Fail to connect? To fail to connect. So it look like, um, it's a database. So e-commerce shop not found which is okay. Let's look at the docker open docker docker compose. Let's make sure that this match okay. But we didn't get it created. So there's no problem what we can do. Let's say I'm going to attempt to connect to it. Now I want to use my ID to do this. But honestly you can use any other GUI program that you have to connect to the database. In short. In the next video, I'm going to try to check out some that I can share with you. Okay. So I'll go here. Data source Postgres. Then this is uh, I know username was uh, Postgres password or password test connection. Look it I'm connected, but this database is not there. So what I'm going to do, I'm going to leave the Postgres database that is there or use that one to get in. Okay. Now that I'm in, I probably can right click new database and then I can you see look at the SQL that is going to run. With that I click okay. And now that I have that database created, I'm going to remove this now or go to property, edit it and put the new database that I just created. And now I am connected. So let's run it again. And then you see connection to the database was successful. Our our application ran successfully. Okay. And that will be all for this video is quite a long video. But very soon you're going to start enjoying the coding part and running things, you know. But so far we've done some setting up. I want to try and um, the config is probably the longest, so I'm going to scroll it down gradually in case you want to pause the video to type it out. But I encourage you to probably just copy it from the repository and then use it. All right. See you in the next video. Bye for now.
