# Add Product Handlers With Category Endpoints

In the last video, we worked on the product service, but as always, I made another mistake. I skipped one service, which is the delete product service. Very simple. So why we are still here in this branch? Let me go ahead and add it. So I'm going to come down here. So under the update product. So I am in product underscore service. So don't forget. And then under the update product I'm going to add another method called delete product. All right. So and it's very easy. It's everything that we needed to do that I forgot. So that's it I'm going to now commit it into the same branch. Add delete product service okay. Now that we've done that let's check out of this immediately to add Category handlers. I'm going to go here to servers and then right click New Go File. Let's call it um product handlers dot go. Okay. We need to add all the handlers. Honestly, I was thinking maybe I should leave this one for you as an assignment, but let's do it because the handlers are so easy to do. So let's start with the create category. It doesn't, you know, remember it's just a gene context here. And then we accept the DTO and map the data. So we should bind JSON and then create a service call the create category with the request. And that's it. So it's really easy. So we we are going to we create a request here bind it and the service. Now we call the service check for error. If there is no error, we return response. That's it for this handler. It's very easy, right? I wish everything life is like this. So that's it for the create category. Let's do get categories very very easy. We create the service for this one. Get categories doesn't need any parameter because when you just call the get endpoint will return you the list of the categories. So let's try to call it. If there is any error we return an error. If not you get all the categories right. Very good. Update categories. So let me make some rooms for myself. For the update category we want to do a little bit more. We need to convert the ID so the ID that we are going to receive. If there is any error we can return an error. So look at it because we need the id in the. Unsigned integer because our id is an unsigned integer unsigned int. I could have used an integer, but an integer can be negative positive, and an id in the database cannot be negative, which is unsigned. That is why I use uint which is unsigned. It to sort of match that you cannot it cannot be negative. So um, when you cause three conf dot pass you int, you give it a string that you want to pass. Then the base in base, what do you want to pass it to base ten. And then the bit size is 32 bits which is enough for us. Okay. After that we can now create the object category request and then bind the JSON. Okay. After the JSON is binded we create the service. We can call the service. Now you see. And then we check for error. If everything goes well we return. We don't have to do any much to work here because the whole job is done in this update category. Here, for example, the updating itself. Okay. After the update category, let's work on delete category. I usually forget the delete. To delete a category we need also to convert the ID right and then check for error. After that we create the service, call the service and check for error. So basically instead of doing it in two different lines, we can do it in a single line this time around. And then if everything goes well they get their response. And that's it for this. Um, adding all the things that relates to category. Before we add the ones for product, I think we should add handlers and test them out. Right? That's the fun of what we are doing. So I would see you in the next video where we add the handlers for these ones. Bye for now.
