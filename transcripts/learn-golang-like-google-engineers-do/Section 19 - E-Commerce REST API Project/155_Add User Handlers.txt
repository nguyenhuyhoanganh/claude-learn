# Add User Handlers

Welcome back. In the last video we added this user service. So let's do the git path which is git checkout. I think is 14 add user services git status. This is the file that we added. So git add. Git commit add user services. Right. So far so good. In this video I want us to add the user handlers. Let's go here in the server folder right click. Let's create a new go file auth. No not auth handler. So we should put this in. Um yeah. We can just put everything in the auth handlers. No problem because it's just two methods right. We worked on this one previously. So we don't need to create new files. It's fine. Everything is in the same package so it doesn't really matter. Okay let's create a git profile handler. And then in it we grab the user id from the context. We create the service. Try to find a. Let's call the service to find the user and then check for error. If there is any error, we know that the user cannot be found, so we return the 404 here. If everything goes well, let's return the user's profile. And that's it. This is the handler. Next we create, um update profile. Let's grab the user ID from context. Let's create the request which is update profile request. This is coming from the DTO. You know. Now let's bind it from Gene which is a classic way of doing it. Create the service now and let's call the service. Let's check for error. And then we return success. Very very sequential and very beautiful. Something that in case you're wondering where this is coming from, why we are able to do it here. Remember, let me switch here quickly. Go to server made well. And in our art middleware this is where we are adding it. You see dot set and from the claims we have the user ID we've we have the email and the role here in the context as well. So that is how we are able to grab it from here. Um, when, if you want to make some improvement, each time we type this thing, we risk mistyping it. So ideally you can refactor this into a constant and then you can, um, just use the constant or you have a helper method like, uh, get user ID or must get ID something that can panic. So but um, yeah, this is it. This is the handler. And in the next video we can add the routes.
