# Order Service With Order Mapper

In this video, I want us to write this get order response. But before we write that, let me explain it a little bit. You know, here we are returning the data from DTO, but when we are going to get the data from database, we are going to get the model. So we need to convert the model to this DTO type. You know. So we need a mapper which is very practical thing to do. It's something that we've been doing before. So I'm going to scroll down here. And then we are going to write one method called Convert to order response. So let me come here. This convert order response method is going to take the model and convert it to this DTO that we have. All right. It's what I just explained to you. So let's create um the order items. Um let's use make to make them. So basically this is what we are our output and the, the number of the items that we want to create is going to be the length of order dot order items. Then after that, we go into a loop and then, you know, I like the classic loop. We grab the current item, then I'm going to append, I'm going to create this type and append it to this object that we have. So the code is going to look a little bit weird but it's nothing. It's honestly um we are just appending right. So other items you know we set it to append here. This is it. We're adding to it. Then we are creating this DTO that we have here and the D3, I'm going to click on it so we can look at it. This is a DTO. It takes there's an ID here and a product. Product respond is here. Let's click click on it. This is the product response. And we have like images and every other thing inside. Let's go back now. So that's what we are going to form. After that we can come here. So outside of the loop, we come here and we can create the response itself. Let me talk a little bit about this date format. If you notice, it looks like you are typing a date and this is how they go date formatting work. It's a standard. Um, there are many, many like built in formats that you can use, but I left this one here for to show you something. Each time you have a string like this, don't leave it like this. Try to refactor it into a constant because it's probably not going to change. But leaving the string like this in your code is called magic strings and is prone to error. You people can mess things up. So we're going to refactor this now. Let's go ahead with this method in in play. We can go and use the ID because what we take here we take the ID. So we need to fetch the order from database to use it to form the response. So let's create this method now. So we create the method. So this is a get order response method. We're taking the DT here and also the order id. Let's create the model then fetch it from database. And after that we do the response and then we return it. And that's it. It's quite simple. Basically we take the ID we fetch it from the database, use this method that we created earlier to convert it to the other response which is the DTO. And then we return it and that's it for the video. And that completes the implementation of create order method with all the utilities that it needs to work. But before we go, let's not forget the refactoring that we want to do. So I'm going to cut this out. And I say that um date format something like this. Maybe we can call it default date format. For us. Okay. And now let's scroll to the top. Here we can come here const default date format is. This is what I cut earlier. And that's it. So this helps you avoid magic numbers. Magic strings. So don't leave. Don't leave numeric or strings literals in your code because then people are going to get confused. But when you give it a name like this, it reads better. People can look at the variable name and try to infer the meaning of the variables from the name. And that's it. So I'm going to scroll down a little bit in case you want to pause and then type the code. You can do that. So this completes the implementation of Create Order.
