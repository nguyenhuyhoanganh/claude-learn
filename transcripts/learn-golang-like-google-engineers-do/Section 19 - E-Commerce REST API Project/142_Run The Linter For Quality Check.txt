# Run The Linter For Quality Check

In between our coding. Sometimes we are going to slow down to do some quality check. So in this video, what I want us to do, since we have been adding a lot of code, we have of course, we've been able to run, let's say make run to run the program. No problems, but we haven't run the linter. So I'm going to run, make lint or let me open the make file to show you the command, just in case you don't have make you can run this command. You don't have make you can run this command directly on your terminal. But make sure that you have this program installed on your system. Now run make lint and let's see what happens. You see, we have an issue here that we need to fix. So what is the problem. You see huge param cfg which is which is the config that we are passing to this new function. You see it is heavy that in total it's about 96 bytes. So consider passing it by pointer which makes sense. If you have a huge data like this and you pass it by value, the entire data is copied into that function stack. That means if you have it in a million places, you're going to copy it in a million times. But if you pass the pointer, the pointer is copied in there, but it's referencing. The data will be stored in a different place, probably on the heap. So this is um, from a from a data standpoint. Um, this is a better idea. So let's go there. It's suggesting that the why we should change is on line 12, column ten in database or go. So let's go and look for it. So database dot go line 12 is down here. So we say we should do it like this okay. But if we do it like this things are no longer going to work. So let's run it one more time. Now we say cannot use database variable of stroke. Type this as this. Yes, because we change it to a pointer. We need to go to main and pass a pointer. So go to cmd API and then main dot go and change this to an address of. So we are passing the address of database to this function now because that is what we said we want here by using this asterisk. So when you have an asterisk like this. When you want to pass the data, you have to pass the address, not the copy of the data. With that, let's go and run the linter one more time. And no more errors. No more complaints. Congratulations. You have just done your first linting successfully and everything looks good. With that, I am going to say status. We are still in the old branch. You see with this Add model. So I am going to commit it in there. So you might um, when you pull the code from the git repository, you might not see the error because we are you are going to see it in the same branch. But yeah. So git commit uh add lint. And. Fix linting uh with the config database config. Something like this okay. So that's it for the video. And then see you in the next one.
