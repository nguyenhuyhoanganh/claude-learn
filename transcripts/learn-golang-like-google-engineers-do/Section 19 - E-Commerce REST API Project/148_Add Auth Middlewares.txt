# Add Auth Middlewares

Hello. Welcome back. In the last video we added this hash password and check password functions. This time I want us to add a middleware that would allow us check the JWT when it is sent to us in the authorization header. It's very simple to do actually. So let's go and do it. Go to server and then create middleware middleware like this that go. So I am in internal slash server and then middleware go the same place you have server go okay so what we want to do we want to create middleware. So I'm going to say func. Let's add it to the server struct. So this is the server struct. Let's let me show you where it is. So this is the server struct. So we are going to add it to it what we want to call it auth middleware. And it return gin dot handler func. Then we return it like this is the signature all the time. So it's simple, right? Let me show you what we are going to do. The user from the browser or like postman or API when they are calling our API, they are going to send something like this, a header like this. They are going to send an authorization like this. Then they're going to say Bera. Bera token bera. Then JWT would follow. When we receive so many things can happen here. Don't don't worry about the the the spelling is when we are going to fix it, all right? When we receive this, first of all, we want to make sure that this header even exists. That is one two. We want to make sure that it's in the right format. If we receive the right format, that means we need to split it and extract the JWT from this part. So if you use the String.split, you should at least have two parts. So the parts must be two in order for us to be sure that the JWT is is here. All right. And we can also check that the first part is bearer to make sure everything is in the correct format. Okay. So let's start with the header itself. We try to get the header which is this authorization right. And then let's check if it exists or not. If the header is empty that means this authorization was not sent at all. We can abort the operation and send out this message. You remember the responses that we created in some videos ago? Look at how we are using them now, right? Very useful. After this, let's split. Let's split it now. That means the value we are going to get here will be this. So we need to split it. Now we split we spin split it. So ideally it should be two. So let's check if it is not two or the first part is not a Bear like this. And we know that this is not a valid token. Um, sort of a valid format, but if everything pass from there, then we need to now check the JWT itself. Okay. We extract the second part. The reason why the index is one here is because index zero would be look at index zero would be bearer index one would be the JWT that is sent to us. It can be empty. It can be random string. But then this is our validate token that we implemented. Would check for it using our secret key. And if everything goes well, the claims that it will return to us would be the claim that we put in JWT, that we have the user ID, the email and the role of the user. Now let's add everything into context. So we add the three of them into context and then forward the request to the next handler. And that's it. With hot middleware it's quite straightforward. The only thing we did most is extract the JWT from the header and just do some checks here, and then use our utility validate token to check for it. And then we forward the request. Now something very important. You must not forget the return here. The return is too important. If you don't abort or you don't return, the flow is going to continue downwards and it can create unexpected results. So do not forget to return and do not forget to abort the request when your checks are not valid. Okay, the next thing I want to add is, um, an admin middleware, which we should have, but we are not going to use it right away. But let's go ahead and add it anyway. Let me come down here. This is a very simple function. Basically we want to make sure that the user role that is in the context that we set here is admin. And in order for us to use this admin middleware we must force to use the middleware because you have to pass through the auth middleware. Then you we can check if you are an admin or not. So I'm going to say server then admin middleware like this, then return and then let's start. So let's grab the role from the context. Let's if it doesn't exist we just return. Sorry I made a mistake here. Yeah. Let me use util. Now you dot bidding. Okay. Passing the context. And then I'm going to just say forbidden. I won't put any message or that will signal that you are an admin or not, so that they don't get the idea. Then we are bought and don't forget our return. That's it for that. Next we want to check, um, that the role that is there is actually an admin role right. So if role is not equal to strings let me Models that roll out mean. So let let's import this quickly. You know that I think we call it user role admin. Yes. That's it. If that's not the case we can do similar thing with utils. Again abort and return. If everything goes well we forward the request to the next handler. Now let's look at what we have here that is giving us um, it's a string and I'm wrapping it and let's see why it's not accepting it. So the s that we have here is for sure shouldn't be there. And that's it for the middleware. In the next video we can register the middleware with some of our routers. So and then we can start using them. So far so good I hope they are not very complicated. It's actually very straightforward code. So I hope you don't find it confusing. Now let's lint make lint okay. We have some error but it's fine. You say that uh, hot middleware is not used and then admin middleware is not used. It's fine. Let's keep it like this. We'll fix that later. So git commit add middle west git branch and we are in the right branch. Oh no we are not in the right branch. We committed this into our password utility. So I'm going to check out git checkout b. Hmm. Well I need a way to fix this right. Um b nine. Let's see. Let me find out of middleware. Okay. Git status anyway. So basically what we have in the password util branch is going to be the same here. It's okay. Um, I'm. When I'm pushing, I make sure I push push all the branches. And you can find the code here. So see you in the next video.
