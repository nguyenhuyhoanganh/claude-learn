# Build Email Notification Microservice

In the last video we updated our Docker compose works great. So I'm going to say Docker. So make Docker down. And that should shut down everything that we have currently running. Now our git status. So working tree is clean so we don't have any problem. Now git checkout dash B 30 8-R. Micro service something like this. Actually we have most of the code. So this is going to be really really easy to do. Um this is where we are going to work from. So this time around in CMD inside notifier Main.go. If you don't have it yet, remember to create it. Ideally you should have it, because if you don't have it, then you have missed out in some of the great things that we've done. But you should have it. I expect you to have it. All right, let's bring in some of the most important imports, and I will explain them. Nothing special here. Some water mill import. This is our internal config. This is the model package, the notification package providers. We worked on everything here. So, um, this is. This shouldn't be strange at all. And with that out of the way, what we want to do, the first thing is to print out a message to the screen that we are starting a notification service. This is something that was showing up in the Docker previously because when I was testing it, I put this message there. Now let's create a context. The context would allow us to control signals like, um, when something should time out or not. All right then let's load our config. So the configuration that we want to load would help us, um, utilize the environmental variables that we have. Let's check for error here. So far so good, right? No confusion. This is our internal configuration. This is what we are calling. And we are doing the same thing in the API as well. We are loading the config there. All right. After we have loaded the config. Now let's use that configuration to initialize the email config, which is what we need from the notification package the smtp config there. So let's do it. So from the notifications package we have smtp config that we have. There we are initializing each of the field with the config that we have at the application level. This is why we added it to the application level to have this control. All right. After this we need to create the email notifier instance which requires this config. And we did email notifier instance in play. The next thing is to create the AWS config, this exact same thing that we did in the API. So let's go ahead and do it. You see you remember that when we created this. All right. After that what next. If you have AWS config you just create the subscriber. How does the subscriber work? Very simple. The subscriber continues to run and listen to messages in the queue. As soon as any message gets in the queue, the subscriber pull it and then your program can process it. When you finish processing it, if everything is good, you acknowledge it that you are done processing. If it's not good, you do not acknowledge it and then the program can know that, okay, this is not processed and another consumer can pick it up. Or if you configure your queue to have a dead letter queue, it can go into what they call dead letter queue on Amazon as well. So the next thing that I want us to do is to now try to create the instance of the subscriber. So now we call water main new STD logger, just the same thing that we do, because we are going to pass the logger to it and then create the subscriber. So this is the subscriber that we've created. And then we are passing our AWS config that we have created here earlier. All right. Now that the subscriber is in play, it is time for us to start subscribing to messages. So let's create um, let's check for error first of all. And after the error check we can now start, uh, pulling messages from the queue. And this is the queue that we pass. All right. If you think about this, it reads very sequential. All the everything that we have here are just settings. We have we just pass our create our AWS config, create the um the subscriber instance. And now we're about to use the subscriber to subscribe to messages in the queue. So it's quite readable, very sequential. Now let's check for errors. If we run into any error we want to call the subscriber dot close. This is super important. Now why we can go ahead and just start an infinite loop and start waiting for messages on this. Um, on this channel that we have. You see that? It's a message channel. Why? We can do that. Something can happen. And this is something that you shouldn't take for granted when you build command line applications. This is when, for example, the program is running and someone press Ctrl C or another program, send you an interrupt to quit or to do something, you want to make sure that you close your resources. It is not good enough for you to just exit the program. So in order for us to do that, let's handle some. Let's create a signal channel and listen for these two signals. And in order for us to do that, listen to the two signals we need to use the select. Select will help us listen in any of the channels. So we have one channel here now and then another one here. We did this thing in the concurrency um module. So we have messages channel that we can listen to messages from the queue. And we also have another channel that we can listen for interrupts. Now let's print out a message that we are about to start listening. And then we can go to Infinite Loop. And then in the infinite loop I can go to select. Now the select we can we can start listening to messages. And then when we get any message from the queue we are going to send it into another function called process message. Now the process message. We are going to write it shortly. If if we run into any error here, if we run into any error, we just send this not ack back. So send messages negative. So that means we did not process it. Else we should send back that okay. Everything went well and that's it for that. The next is to. So this is the case for that. And the next case is to listen on this um channel which is for the signal. And if we receive any message on this channel, let's print out a message that we are shutting down and then we can close the subscriber. Did you see how sequential it reads? Right. It reads really well. Don't forget to return to get out of the loop. This is how everything is in the main. um, function so far. This is how you, uh, listen on multiple channels. And the good thing this is a design in goal that I really love, that people use channels very well. When you subscribe to the queue, it gives you a channel to listen on. And this is really, really very good. Like it's called a dramatic goal. All right. Now let's create the process message function which is super easy. We grab the message type and then we can switch that type. So if we say user login type we call another function which is handle user login message. And um we after that we we return. And by default we can put a default. Here we can say we are not um, we don't want to process this one and then we can just Return me now. If you want you can come and add different switch statement or add a little bit of a sort of routing here. But for now we only want to process user underscore logged underscore in message type. And then after that let's create this method as well or function rather. And that one is super easy. We create a a variable user from the model. Because remember we put the entire user object in the queue so we can unmarshal it back into the object. So we have access to the variables and we try to unmarshal it. Of course, if it fails we want to return an error. If everything goes well, let's try to form the user name, which is sort of like full name that we want to put. If it is empty, we just call the user user. Then we can print out a message and call the email notifier to send the Send a message and that's it for our microservice. Let me scroll to the top. At this point, we no longer need this. So what did we do? Um, I print out a message, first of all, created a context, then loaded our config. Um, we created our, um, the smt config from our config. Right. Then we create the notifier which is what we use for email notification AWS config, the same one that we created in API. Then the logger from water mill, then the subscriber instance. After the subscriber instance, we start subscribing to messages, which returns us a channel. If you look at the signature for subscribe, it returns a channel. So it's a receiving channel that you can use to receive data. That's why you have this arrow pointing out that you can receive from it. You cannot send to this channel here. It's for you to receive from it. Okay. If we are unable to create the subscriber, we just sort of call close, which is a cleanup function. Then, uh, one very important thing we also are doing here is to create a signal channel that allow us to listen to those, these two signals in case someone wants to shut down the program. And after that, basically is for us to go into infinite loop and start listening for messages and then process the messages. So as you can see, everything looks very, very sequential. Now, before I run do anything, I want to take this user underscore logged in. I want us to go to Internal Services auth service and check here to make sure that whatever we have here this message type matches. Another thing you can do is to take this. For example. Let's go to notification. Maybe we can say like message. Underscore types. And then I can do like this user logged. And we you have something like this. Then in your auth you can do notifications. Like this. And then in here you do the same thing. Like this. And this eliminates some possible typo that can mess up your program. And honestly things like this can be very difficult to debug. So we can run make lint. No linting error. Good. Um, now the docker is there. Let's go to images I want to I want to delete some of the images. Notifier. Let me delete the image to rebuild it. Okay. Good. And um now let's do make. Make Docker. Um. Is rebuilding. Let's give it a moment. Okay. It's done building. Now that is done building. We should our notifier. Let's see if it is running or not. Okay. Look like we have everything running. Let me open it here. Now it's time to test it. And, uh, let's open our email box here. And then bring our postman to this side. Just run it. Wow. Think about this. It's so cool. It came instantly. Look at our message. Hello, John. Do you see? This is so good. Our fully containerized application using AWS locally. Cuz everything is so fast, so powerful. Wow. On a single shot. It worked. This is good. And that means the only thing that you need to do now is just to add more notifications as you wish. And you already have all the capabilities to send email to do everything. You see, our program is fully dockerized where we no longer have to run anything locally. Everything now is in Docker working together. You know very, very well. Man, this is really, really good. I'm very excited about this achievement, what we've done. So let's um git status. Git add. Git commit. He's. Emailing or notifier. Microservice notifier. Micro service. Wow. And that's it for, uh, sort of everything that I want to do in terms of immunofixation and, uh, and, uh, yeah, and all that. So we still have more to go regarding this e-commerce. We are not done yet. We need to do, um, bringing more stuffs. But honestly, if you have watched the video to this level, think about how much we've been able to do really, really well and look at the microservice that we've written. It looks simple, but it's very powerful. Trust me. This is production level software and you can really, really use it. The only thing that you want to change in production is to make sure that your email is using, um, the the email library is using TLS, but aside from that this is really good. So thank you for watching. And then I will see you in the next section of the course. Bye for now.
