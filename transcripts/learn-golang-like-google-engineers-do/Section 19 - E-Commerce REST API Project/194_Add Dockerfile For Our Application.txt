# Add Dockerfile For Our Application

Okay. Welcome back. Before I or I put the new Docker file, I want us to fix the name for that branch. There was a branch that we didn't name properly and now I can't find it okay. Is this add multi binary build. So git checkout add multi binary build. So why I'm in that branch. Now I can do git branch m I think should be what. What is it. I think it's 34. Yes 34. Mad multi. Binary build and that's it okay. So git 35. Something like this. Good then git that b. This is a new branch now 36. So add docker Car. Okay so open our Docker and then right click Docker file. And I'm going to paste the docker file. Then I explain it okay. Now what is the most uh the most complex thing here is the that same command that we put in the makefile is what is here to help us build binary. So what are we doing? We have two stage build here. One is a builder which we use to compile the program. Then we copy all the program that we compile from there into another another image, another image which would be smaller. So what are we doing. We are we are building from go 1.2, 4.4, which is the sort of the latest version as at the time of this recording. So if you are watching in the future, you can adjust this number gradually and see how it affects the program. But most times go would always compile at least as long as you're in version one. Okay, so our working directory is going to be up inside the container. And then we copy every. We copy our go mode and the go.sum. And then we run download to download all the dependencies into that container. Then we copy everything which is the source code you are seeing here. And from there we do the same thing we did in make, which is to run the command that does the building of binaries. It builds all the binary, as we have done exactly in main to that bin folder. And after that process, this build stage ends. And then the next stage starts, we grab plain Linux. Alpine um image has no goal installed inside. Then um, we just do the normal regular, uh, no cache, add certificates, and then set our working directory to slash routes. This is not super important. You can do anything you want to do. And but the most important thing here is that we copy all the binaries into the image. And then by default, we are starting the API. Now there is something here I want to mention. Should we copy all the binaries? If we want we can have different different docker files for each of the binary and copy and use that instead. But for for the sake of convenience I'm copying everything here. So the other ones, like notifier for instance will be dormant, will just be sitting in the image. Not started, not used yet, but we are going to use it soon. I'm going to show you how we are going to use it, but for now, whenever you use this docker file is going to start the API by default. And that's it for the Docker file. So I'm going to go ahead and git add git commit add docker file okay. And in the next video we can update our docker compose to see how it works.
