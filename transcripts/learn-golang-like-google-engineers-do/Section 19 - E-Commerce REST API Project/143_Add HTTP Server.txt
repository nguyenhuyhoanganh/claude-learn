# Add HTTP Server

Okay. Welcome back. Now that we can connect to the database, we have our logger and we have our models. The next logical step is to add the HTTP server. I'm going to go to my terminal. Open your terminal let's say git branch to know where we are. Now we need to add another branch. So git checkout dash. B add HTTP server. So we're going to work on this branch. All right good to add the HTTP server. Go to internal. Create a new folder called server. Then in it you create a new file called server dot go. This is where we are going to add our server information. In the previous project we used the HTTP library from the Gold Standard library this time around. Like I said, we are going to use gym. It's nothing much. It's not even that much difference. But it's a powerful way, way more powerful than the default one. I'm going to start by creating a server struct, and then I add the. We need to add three fields the access to the config db and logger. Remember how we did in the first project? We had an application structure that we put all the dependencies. So this is what we are going to have in it config db and logger. After that we create a function new. So server dot new. And in it we are going to basically return an instance of this server. So config s fg. All right. Basically, this is what we are going to initialize in Maine. After that, we need to set up a route. How we did, um, in the project as well. This is where we are going to set up new routers. So I'm going to say func. This is going to be attached to the server. So server. Setup route. Is going to return dot engine. Okay. And in it let's create an instance of gene. So this is router dot new. There is also gene dot default that has some sort of default configuration. But I want to use this new because I want to show you how to add some middlewares from Gene Engine itself. With that in place, you can add middlewares. We need the middleware. This logger middleware is coming from. Jim. This is not our own logger. This is coming from Jim. So the logger middleware. Let's add recovery in case the go routine crashes. It can it will not crash the entire application. Then we're going to add another middleware which I have not created. We are going to create it shortly which is this for course in case you are building JavaScript application this is super important. If not, it's not going to work. After we have added middleware. Let's start a let's start adding add our add routes. The first route, which is the only route that we are going to add in this video, is a health check, basically to know if the application is up or not, I'm going to create it down shortly, and then we can, um, we can return the router like this. Okay. This is very, very good. Now let's create the health check first. Very simple. I'm going to say server what we named it health check. Like this. And look at the look at the difference between a handler for gym and a standard library. Handler is for gym. It only takes this context, which most times we we name it that we just see or we name it CT. So the. But if you are using the standard library, this would have been uh, it would have taken two arguments, one http response writer and a pointer to HTTP request. But for gym Gene. It is a single context and it's funny that look at the HTTP response right time requests. They are embedded inside. You can have access to them somehow. Then in order to write data to the screaming gene is very simple. You just do see which is the context dot. For example, we want to write JSON and then you can type 200 here. But I like to use a constant from the standard library. And then this is just a map gene dot h is the map. Let me hover it. And you see, you see a map of string to anything that you want to print. And for that we can just put status okay here. And that's it. That is this is a complete HTTP handler for gene framework. This won't work for the standard library. But we are using gene here. So this is perfect okay. The next thing I want us to do is to write this middleware. So this is a middleware. Now, before I continue with the code, I want to show you something. The difference between our our old middleware for the standard library. And this one is that for Jim, your middleware always return this same handler func and you can just return it like this. If you see the function I return is exactly like this, you see. So that means this function is is technically a handler func. And in it you can do anything you want to do just like you did here. Right. So I'm going to first of all add some headers. You know access control headers. You know, all that thing that you need to do for call and all the headers that we want to allow, we may need to come here and add some times. Maybe you have like X uh or something whatever the headers I want to allow from the user. Sometime we can add more here, and then after that we want to check if the HTTP method is options. We want to send the 204 status code. This is very important for preflight when you're in JavaScript application. And that would be it after that basically we can just return. We just return nest here and that's it. That's it for setting up the HTTP route. The rest is basically for us to go to main and mount it. But before we go to main and mount it, maybe we can take a minute to do a add some other great stuff. Right? First is I want you to be this is already done. I want you to go to internal. And add a folder called utils. Here is why. What why I want to do what I want to do. Any time you build API, you want to make sure that the response which you send to your user is identical all the time. If you don't, if you don't ensure that your your API. For example, have you noticed an API where if the if the response is successful, the payload is a little bit different? If it's an error, it's a little bit different. That is very difficult to manage when you're building JavaScript application. So in this video I want us to create some helpers, some helper functions that keep our response very, very similar. If there is an error, you would see an error. If there is no error, you will see the success, but the payload is the same. So in the fire this is the folder we created. it. I can create the file here called response dot go. And then let's start bringing them in. First of all let's create a response structure. So this struct type of is just a struct. So in it we have success message data error. And look at what we've done here. Even if there is no error this will be empty. But if there is an error you found it. But the most important thing is that the structure is very similar. What I want to take out from here is this I want I want the data to be always available. All right then after this I'm going to create different flavors of responses. We are going to have paginated response. Sometimes we are going to have response. Um we are going to have success response created. They are not in there just struck. So let me show you. So this is paginated um, response. And this is the metadata for the pagination If we want to send out success response, we use this function. What is the difference? Nothing much. You just give it the gene context, the message you want to send. And if you have data and then you send it like this. Also createdat which is very similar to this aside from the status code, this one is where this is 201 and this is 200. Nothing. So just plain function. We also have error response. Almost very very simple. We created the response check. If error is known then we set the error to whatever error that the error message we have here. And then we send out the JSON. Okay. We have bad request very plain function as well. We have our unauthorized response forbidden not found then internal server error. wrong. And then they paginated the rest. Success response. If you look at it, there are just I think the biggest difference is just the status code, which is something we don't want to repeat a million times. That's why I brought them in. Okay. Let me go over it again. You see the struct this is for pagination and this is metadata for pagination. Then for success createdat error. Bad request. Unauthorized forbidding not found internal server error and then paginated success response. So this is very simple and straightforward. All right. The next thing I want us to do is to set up our server in main. So go to main.go. So in your cmd slash API Main.go open it up. I'm going to go to the terminal quickly to write Make run to make sure that things are still working good. No errors. This is good. Now after here, just here, we can start. Let's bring in the server. So I'm going to call this sr v for the server. Server dot name. We need to import the package from our. So it's been imported. So server dot new. We need to pass it the config the database and the logger. So cfg I think it's we need to pass the pointer. The database which is db and the logger which is log. Let's see. Good. So this um already let's see if it's the pointer. Yeah it's a pointer. Perfect. Okay. And then what else we need to set up. Now that we have an instance of the server, we just call s s v dot set up routes. This is going to set up the route for us immediately okay. And after that I'm going to create a HTTP server instance. So HTTP server we have done this before. Address. The address would be the server port that we have in the config. Remember it right. So this is supposed to be a string not an int. So this is percent s okay. Good. Next thing I want to set is the handler. And the handler is becoming the router. So here we should actually get the router back and put it here Remember this returns the gene engine. Gene engine qualifies as the HTTP server mocks it is the implementation. So that is why we are able to use it like this. Of course there are things you can set like a read and read timeout. You know, something like that. You know let's say five times seconds right. Timeout, things like that, maybe ten. You can set all these settings there. After we have created the server for this time, I'm going to start the server in another go routine. So I'm going to say go func. This is the function. Then we are going to run it in a different go routine. In it we are going to start the server. But before we start the server I'll put out a message and then we can do error check. I want to make sure that the I want to use the E. So I this Import the errors. Good. So basically we we attempt to start the server. If there if the server cannot start we just put out an error message. Now we can log theta here and able to start the server. Hope this is not confusing. We could have of course just called this outside and not in a different goroutine. But the problem is if we do that, we don't have the opportunity to do a graceful shutdown because we want to be able to shutdown the server by ourselves. And that's what I'm going to show you next. Now, you know, if you start this in a goroutine, you need somehow you need to wait. If you don't wait, this server is going to exit when main returns here. So in order for us to wait, we're going to listen on the signal. For example, when you press Ctrl C or when the program receives some kind of signal from the operating system to quit, then we have the opportunity to do some clean up. So we are going to say quit. We make a channel of signal. So this is the channel of signal. It's a buffered channel. So we now do the we do signal dot notify. So on quit we are going to receive a signal here. And we can listen to Cisco. So we are going to listen to these two signals. If you want you can put these two on Google just to see the kind of interrupt that it sends. So you see you can hover on it to see them. But if we listen on these two signals and here is how the waiting is going to happen here. This is where the program is going to block here until it receives a signal to quit. This is why this, um, this is very important. This is why we are able to do this. Okay. After that, um, if if we receive the signal here, then we can say the server is shutting down. And then let's create a context with timeout. So we are going to timeout after after five seconds or it depends on what you are doing. Maybe we can say 1520 second or whatever. Um, after that let's define the closing of the um. Closing of the context. And now we then we are going to finally shut down the HTTP server itself. This one give us the opportunity to do any kind of database, clean up anything that we want to do, and then we can say server exited here. All right. You see it's a little bit of code. We started um we created the server instance. We grab our route. Of course if you want you can actually put this directly here since we don't need it in two different places. And then we created a HTTP server, which is something you have seen before. I think the only thing that you really haven't seen is this, um, sort of graceful shutdown that we are doing, starting the server in a different goroutine and waiting for us to receive a signal. Then we can signaling to the context. Remember, the context is being passed to this shutdown, which is going to know that, okay, it is time for me to shut down and stop receiving requests. So this is very, very good. All right. Now that we have this in place, I'm going to go and run, make run or just for the sake of the people that is not on on Linux or Mac that doesn't have make. This is a command that I'm going to run. So go run like this. In short, we can run it. Go run c d life. This is the command I'm going to run. Don't forget my docker is running. So we have the health endpoint. Let me try to bring my browser up in incognito mode And I can be somewhat 80, 80 and I can say health I think good. And we have the status. Okay. Congratulations. You have developed your first HTTP program using Gene in go. And we have done so much. The setup we have is very, very good. Let me go to the terminal and show you you don't have to do this one. I'm going to say three. And I want to do this to show you our program structure. You see how we structure the program. This is our output. This is not in our git. This is our executable database migration is going to be here Docker our go module. And in internal we have this great structure that we have. So this is very good. This is very professional for a small to medium sized project. Thank you for watching I will see you in the next video. Bye for now.
