# Event-Driven Programming: Event Publisher Using Watermill

Now that we have the interface, let's create an implementation for that. So water may create water. Milk go in the same folder where we have the interface. So watermill dot go basically an implementation that uses water me. And then let's bring in some imports. Now I want to explain the import a little bit. First of all I'm importing the provider that we did earlier basically here so that we can use the AWS config function. Then I have these three water mill imports here, which we are going to tidy up soon. And then um, there is a blank import here to allow us to use the AWS endpoint. And all of this is going to come in as soon as I run go Mod tidy. And finally, because we have, um, uh, I want us to make, I want to make sure that the config we are using here is we want it to be distinct from the AWS config. I aliased it to app.config. This is our internal config, so I name it app.config and that's it. So let's create a publisher type. So this is called event publisher. Um this one is going to use water Me for sure. Now the message that publisher is actually what we really need. All the code that we want to write is for us to initialize this message, that publisher, which is coming from water me because this is what we are going to use to publish messages to the queue. So all the code that we are going to write is to help us. At the end of the day, we just want to initialize this publisher and then we start using it to publish messages to the queue. Remember that we have this interface here. Now we need to implement this method. All right. So let's go ahead and implement the method. Now implementing the method is quite easy. This is our payload. Let's turn it to JSON because it's JSON that we want, right? Now this is super simple. We attempt to turn it into a JSON. If it is not possible, we return an error. That means this data is a byte slice that we can easily use. Now let's create a water message. All right. The water mill message is, um, a sort of an envelope that you use to pack the message that you want to send over the wire. Now, with the water message created, we need to create metadata. Even this event type is a metadata, but we are sort of going to set it by default. So let's create some metadata here. Um, something I, something we have here is ability to add additional metadata aside from the event type. So let's just loop over this map and set each of them as mass metadata. And that's it. After that, what else? We can return, um. This publisher. Publisher. And pass it. The queue and the message. All right. Now that we return it. See here. Think about it. When we want to create an instance of this event publisher, we need to initialize this in order for us to be able to use it like this. But overall, we just created our payload here and pass the name of the queue and the payload and that does it. The close is super simple. The close. We just need to close this publisher itself. And it has a close method. So we implement a close method easily like this. And that's it. So with this in play we can go ahead and run go mod tidy and let's see what happens. Go mod tidy. And this is going to download all the libraries that we have imported for us and that's it. So let's wait for my ID to do some indexing. And ideally we shouldn't see errors anymore. Now these squiggly lines are not the things you are still seeing. It's okay now because we are going to get them off right now. All right. Let's create another function this time around called New Event Publisher, which we are going to call to initialize this event publisher. And in it we are going to initialize this one. So let's go ahead and do it. So I'm going to close this on the side to give us more space. And let's put the function here. And now what do we need. Water main has its own logger. I'm going to sort of establish them. Initialize the logger here. It's not um it has debug if you want it to output some debug info and tracing I turn them off by default. If you like, you can turn them on. Then we need to do two things. We need to create. We need to grab the config that we created earlier using um in our provider, the create AWS config. We need to grab that config, and then we use that config to create a publisher config. And that's it. So let's grab it. We check for error quickly. If there are some any error we want to return that error. Now that we have our config let's now create the config for the publisher. So basically you need the aws config to configure the publisher. This publisher is what we are about to initialize, which is coming from. I've told you before, everything that we are doing here is just to initialize this publisher. Now that we have we have the publisher config, we need to create the publisher here. Now this is an attempt for us to create a publisher. If there is any error, we want to make sure that we return that error. If not, it is time for us to return this our event publisher. Passing this publisher instance and then the name of the queue and the possible error which we have set to NE here. Since there is no error after this point. And that's it for the implementation. It looks really simple straightforward. So we have the type here. Then we implemented the method from that interface that we have. And this is super easy. We mash out the payload into JSON. Create an envelope here that see that um watermill also have a Uuid just to create an ID for the message. So each message is going to have an ID, which is a Uuid if you don't want to use the Uuid. Of course you can use something else if you want, but this is just a string. All right. And then here, um, after that we do the metadata. But look at this, our metadata. There is a small flaw here that I want to point out. So I don't want to look foolish if we have this here, if someone if if someone pass events underscore type as well in the metadata is going to overwrite this. So if you want you can do delete to delete, um delete any possible event type from this metadata. But I'm not I'm just going to keep it this way. I just said let me point it out for you. And maybe maybe overwriting this is also a desired behavior that you want. So we keep it like that. And after that we just return the publisher that publish. And then we created this, uh, constructor that we are going to use shortly. And that's it for the video, probably the longest video so far in this section. And then in the next video we are going to learn how to start using this. So see you there.
