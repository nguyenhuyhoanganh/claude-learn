# Fix Append Issue In The Order Mapper

Okay, now let's fix the problem where we had initially. So you remember this is the I mean order underscore service dot go. That's the order service. Remember I mentioned that I think the bug is in the mapper which is this convert to order response. The function takes a model and then convert it to order response. But something we are doing here that somehow I don't know how I ended up doing it a little bit silly anyway, is that here we are initializing the data already. That means we have the sort of the array pre-allocated in memory, and when the pre-allocation happened, we have access to each of the slots of the array already that we can just put the items that we want. Right. Then I, I went ahead to use append for nothing. Then append is basically adding to the end of that and just messing up the whole thing. So this is where the problem is. So first of all, we don't need append. I'm going to take out the append and use the id here and set it and then remove this. And that's it. You know, it was a very silly mistake, but pardon me. Sometimes you you can just get a little bit overwhelmed with everything. But that was it. So make sure you go to order underscore service go. And then in the convert to order response change from append to use the index of the array. Because remember we already done the pre allocation here. That means we can use the index to access each of the slots of the array. And we can go ahead. Now I'll run the program. Then I can let's go to get order I see as I do get order now you no longer see the empty ones up there. And then the same thing with orders. We no longer see it. Everything is looking very, very good. We can do a quick test. One more quick test to add an item to the cart. You see, it comes back nicely. We can create an order. It come back nicely. So that's it. What I'm going to do now is to do git status. Um, so we can do. Git add. Git commit x. Um slice initial. It's not initialization. So fix uh slice up append something like this. And that's end the that ends the sections for order management. The next thing I want us to do is to add some documentation, which is something super important. Um, especially if you are building API in a team. Imagine if you have a lot of people in your team and they are changing the data structure. They are changing the the fields or the data type. You know, most times people don't go to postman to update it and that's a problem. So in the next video, I want us to focus on adding documentation to make it look good. And then we can take it up from there. So see you soon.
