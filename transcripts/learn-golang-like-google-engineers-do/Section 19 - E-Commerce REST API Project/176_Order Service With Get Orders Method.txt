# Order Service With Get Orders Method

In the last video, we added the methods to allow us get order response and convert model to D2 response. Very powerful and we are going to continue to use these two methods. They are very very helpful method. But in this video we want to we want the ability to get all customer orders. So given the customer ID or user ID, we want to be able to get all the orders. And aside from getting all their orders, we want to make sure that we add some pagination. So if they have 1000 items you don't want to retrieve, 1000 or 1 million, you want to make sure that you can paginate the values. And, um, not to put a lot of pressure on the response you are sending to the browser. All right. Um, so I'm going to come down here and add a new method, right. This I'm making sure like so the method is get order. So it takes the user ID page limit all of that this these two two types are int. And. Of course, we have been using an unsigned integer for our user id since. And we return the same response. But this time around, because we are trying to retrieve all orders, we are returning the slides of order response from the DTO package. And then remember the utility that we had before this meta pagination metadata that we had. We are going to need it here as well. Okay. Now that we have the method signature in place, let's add some checks. First of all, we want to make sure that the page is not less than one. So if it is less than one we want to make sure we set it to one. So nobody is going to give us zero or anything minus one to that part. Like this. This is not a valid page number. So we want to make sure we set it like this. The same thing with the limit. If the limit is less than one we want to set it to ten by default. and for this limit as well. Maybe we can even do if it's like greater than 100, we want to set it to 100. You know, um, it's a way that you have to protect your system. You don't want someone typing 10 million as the limit. So this can be very, very important. And of course, these two can be combined, you know, uh, but let's keep it like this. All right, after that, uh, let's calculate the offset, which we're going to use for the pagination. Now, the data itself, when we fetch the orders from the database, we need to bind it to a slice of models that order. So let's create that and then the total number of items in the database. So if you have 50 orders remember it's not this is not money. It's just the total number of orders that you have placed. We need this to calculate the pagination. So that's why we're having it. So first of all let's get the total number of orders that you've placed, and then we bind it. All right. After that, we can now fetch the orders itself. And the fetching the order itself is nothing special. We've seen this query over and over and over again. We are just preloading the order items, the product and the category. This is the power of ORM, right? And then we find it by the user. We also order it. But we are this time we are passing the offset and limit which we already calculated here. So limit is something that we pass. Offset is something we calculated here. We find and bind it to the orders that we have. And of course it returns an error. And then we are inlining the error. So this thing if it confuses you a little bit let me show you something. If I say s dot db dot find just for now. Find here. Like this. Let's do something like this. Okay. Well, I'm going to delete it. It returns if you look at it. Let's click on it. You see it returns this uh DB but in it there is error. So and the error is what we are checking. So instead of us doing it in multiple lines we just do it in line here, which is. So don't don't be confused. All right. After that, um, let's make, let's create, uh, a slice of um, other response. We are going to use the make method. Okay. And then we loop. Now in the loop we are using this convert to order response that we have. Again just to make sure that we can um, form it into the response type that we want this D2. All right. After that we can calculate the total number of pages. Which is what we are dividing it by the limit. All right. And then we should do the metadata pagination meta. Now. So we pass the page the limit the total items in the database or the orders that we've placed. And then the total number of pages that you possibly can have. So the total number of pages is determined by this limit. So if you have 50 items in the database and we fetch 50 items, then you down, the total number of pages is just going to be that one page. You don't have multiple pages, but if you have 50 items and you want to see ten at a time, then you're going to have like five pages or so. So that's why we have that there and that's it. You know here we're just going to return the response. Meta. And then since we don't have any errors here so far. So this is it for the get orders. It's quite These are like important checks. We calculate, um, offset from that, find the order from the database. And then this is mapping everything here is data mapping because we have here the order is um slice of models. And we want to return D2. We need to do this mapping here to convert them. And you saw the method earlier and that's it. Now after that let's add one more method to get a specific order. And then that that's the end of the implementation of the order service. So we get the model, find the order from the database. If you look at the the query you see is very similar. We are preloading the same item. The only thing is because this is one item we don't need to order. We don't need to order, we don't need um offset. We don't. That's it. But here we see need the where to find for the specific user, and then we find it. And of course we check for error. After that, we need to convert this model to the response that we want to return because it's detail. And then we return the response and that's it. So that concludes uh, let me scroll to the top, and I want to fold the code and make sure we can focus on these two methods that we just implemented. You've seen this right. We talked about this a lot already. And then they get older that we just implemented. It's also very simple. It's actually just a call to the database. It's like uh finding the data and then putting it together for you by the ORM. Then we check for error. If there are no errors, then we convert it to the DTO response that we want and that's it. Um, yeah, I think that's everything for that. What else? I hope I didn't miss anything. This is. This is a create order that we did in the other video. This is the gate order that we just added as well. And that's it. So let me. So for our public API we have these three methods. This is the three actions that we are going to give to the user. And we shall see how to use them when we start working with the handler. So see you in that video. I'm very excited the progress that we've made and I hope you are excited too. So see you in the next video. I'm going to open them up, open them up, open them up so I can scroll a little bit. Good bye.
