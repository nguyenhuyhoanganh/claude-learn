# Multi-binary Builder (Update To Makefile)

Okay. Welcome back. In the last video we added a new email library. And now I want us to use that email library to do some stuffs. But before we can do that, we need to we need to change the strategy which we used to build our email program. So let's first of all check um here and go to build build basically build a single binary and send it to this bin slash app. We're going to change that. So all right. But before I do that let's commit the code from the last video. Git add git commit. Add email program or email library. That's what we call it. Good. Now with that out of the way I can say git checkout dash b r. Uh. Binary build something like this. Add multi binary. Okay. First of all let's go to cmd right click or however you want you whatever ID you are using. And you can add a new folder called node to file. Right. This is a new program entirely. And then you put Main.go inside and we can put a font hello World from notifier okay. And that's it for now. So if I go to my make. You two possible ways to build this. We can put a note and then notifier to build both of them. And then if I type make build. We see that uh, both programs have been built and for sure I should be able to run bin notifier. And we see our hello world. And we can also run the bin slash. up. Which is our program is also running. Okay. So far so good. No. No surprises. But the problem with this is this. If I have let me delete the binary folder. If I have, let's say different microservices here, let's say ten of them. I don't want to add ten lines here. I want to change the behavior a little bit. So I'm going to delete everything here. Also I won't call the this API. I won't call it up the binary, I won't call it up again. I'm going to call it API actually. So the first thing I want to do is to echo our building. Building all binaries. The code I'm about to put. If you're on windows, it's not going to work for you. So if you can do that, multiple lines that I showed first. And that is why I always show a sort of a manual way to do it. This is first of all, creating the bin directory in case it's not there. And then I'll put the program that can loop over all the folders here, and then use that as a base name to create it. So you see that it's the most, the most, um, sort of complex thing here is just this simple loop that we are doing. So it does a very simple loop. And it takes the, the, the file name from there and use it and use it to, uh, form the name of the binary and then the directory here. So there is no, nothing complicated here at all. With that we can just call make build. And you see it building API building notifier. And both of them have been compiled and put in here for us. We don't even have to think about it. The good thing now is if I come here, add another one. Let's call this a jobber. And I put a a minute. Go inside. I won't even type anything. Run it again. And then you see, it builds job as well. So this is really good. You don't need to do them manually anymore because of that simple program that we just put on. We can delete this one now. And this is the only thing that we modify here. So git status and the notifier is there. No problem. I'm going to keep the notifier because we need it as a multi. Build builder or something like this. And that ends the video for now it's very simple. Like I said if you want windows and you can run this program, this command, please use the multiple lines that we had earlier. Just do two lines, one for the API, but rename it to API instead of app and then another one for notifier. It works as well. This is just for convenience and for me, honestly, it's better. Okay, see you soon.
