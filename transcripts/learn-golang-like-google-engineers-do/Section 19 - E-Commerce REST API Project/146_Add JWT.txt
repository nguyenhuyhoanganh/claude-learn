# Add JWT

Now let's work on J.W., because we need that in order for the user to log in. Currently we are in this branch. So this is serve six. Now I'm going to do git checkout seven. First of all dash B7-R. Okay. Looks good. Let me bring close that out and then YouTube. So internal slash YouTube is where we have response. Let's create a. Simple file then import. We are going to import the JWT package that we'll be using this package. Don't worry as soon as we run go mod tidy this is going to fix the error you are seeing. Scene. JWT deals with claims. Claims are like what you give to the customer. So let me give you an example. Let's say you have a club. When a user buys a ticket, the ticket becomes the claim. So that ticket, when they want to have access to the club, they bring you that ticket and show it to you. And you can verify that ticket if you should admit them or not. So the ticket is the claim. So JWT works in similar manner. So we are going to have some claims. The claims we need the user ID in the claim. Which I'm going to make. Are you int. We can make it an int as well if you want email. Role. Then there is a J.W.. Claims which we are embedding. This is going to come from this. I'm going to save this file. Now go to my terminal run go mod tidy to download this library. Now it is downloaded. So this is the version that we're currently using. Good. After that let's create the function to generate the JWT token. So we're going to generate access token and refresh token func generate token pair. What do we need cfg. We need access to the config the jwt config this one. We created it User id you int email role. And here. It's not like this I'm coming. What we want to return. We want to return the access token. Refresh token a string and a possible error. So the two different token. Let's spell it correctly. Good access token refresh token and a possible error. Good. Now let's create our access token here. This is the claims for our access token. Basically, this claim is what we created here. Passing the user ID email and role. Then the registered claim is basically that's where you put your expires at an issuer. You can of course open it up to see what it, um. The things you can set. Look at them if you are. These are the things that you can set. Right after that, you need to create the new claim now, which is this is access token. Then we need to sign it with our secret key which is going to come from this config. Look at the key here. We're going to use the key to sign the string. And now when after the sign we check for error. If there are if you have any errors we just return empty strings and the error after that let's create for refresh token as well. Very almost the same thing. You can copy and paste it honestly. Then we sign check for error. Now we can return but that's it. So this is it for the riff generation of Tolkien. So here we generate access token. So. And here we generate the refresh token. The question is, you know, um, people usually wondered maybe the refresh token is so special. There's nothing special about. It's about the same thing. Honestly, it's the context which you use them and also the validity. We are going to study refresh token in database. We are not going to study access token. This access token is going to be short lived. Refresh token is going to stay longer. But uh, both of them, we need them in order for them to renew their token. All right. After that, we need to work on the validity. How do we check the token when they send it? Let me scroll down a little bit here. Let's create a function called Validate token with this signature. As you can see we are going to accept any kind of token, refresh token. Access token. And the secret? The secret. We have it. This never goes to the user. Never, ever put the secret on the internet. That is the encryption key. If the encryption key is compromised, everything else is compromised. Very important. Okay, then we can use the JWT pass with claims to try to do the, um, the passing of the token that they sent to us. It has a word signature, but it's not that complex. If you look at the function, it takes the token and the claims and the claims. We just kept it empty because it's not as we don't. We have everything in here already. So we kept it empty. And then they call back. And then this is the callback takes the sort of the object here, the token. And basically this is what we do. All right then let's search uh A check for error. After that, we try to check for the validity and then we can return an error if it is not valid. All right, that's it. Honestly it's a very simple function. And sometime you should always add validate token check if J.W. or data token is valid. Right. You can always add this comment. Generate token pair. Generate access token. And refresh token. And then if you want here as well you can say claims. Con tains the data for the user. Something like this. Claims we didn't spell it well. Okay. And here also spelling error. But it's okay. All right so we have um we can we have everything here. And that's it for the video. The next video we are going to start um, incorporating most of this thing that we've built. So it's very straightforward and very good. But before I commit, let's run, go make lint. So I'm running make lint to lint. Okay. So we have some issues. Comment format input space between and okay. So let's go and fix that. So we should put a space here. You see that the lint can be very petty sometimes say param type combine for and then could be replaced. So where is that. So on line 20. On line 20. So say we have a parameter that can be replaced. When you see this message. Sometimes it can be overwhelming. But let me explain it to you. Say that param type combine. Look at here. Email and role are both strings type. That means we can omit this one. That is all it's saying. And then I run it again. And beautiful. Our linting pass. So great. Git status. Everything looks good. Git add git commit. Then add jwt utilities. Just two function that we added. And then let me check our branch. Good. We didn't make any mistake this time, so see you in the next video.
