# Update Product Service With Product Methods

Welcome back. In the last video we added most of the category service, but we missed one. So let me quickly add it and then we commit it to this same branch before we branch out. So let's go ahead and add a delete and then we return. So basically a simple delete category that we needed to add and that that's it. There's nothing much there. And then git add git commit. Um add delete category service something like this okay. Since we can then branch out git checkout b. Add product service maybe product. Now ideally we should have named this one. Um maybe we should do it. Git branch m. We can say 17. Add category services that renamed it. Now we can say git branch out 18. Add product services. Good. So we are in a new branch now to add a product service. But we are going to keep everything in this file. So no problems. Let me fold everything I hope you've been able to type everything. All right. The first thing I want to do, I want to create a helper function that convert the product model to respond something really simple. So this is the function we are going to loop. So let's create all the sort of make all the possible images that we can find a loop and then turn it to image response something like this. Then we return the D2. If you just want to take a few seconds to talk about something, if you come from dotnet, there is a a mapper that does this automatically for you. And then I think in other languages there is always something in between the model and the D2 that know how to translate the model to dtos and from dtos to the model. But in go this is most what this is what you're going to find. Most people do it manually like this. Like you have helper functions and know how to. For example, we went from models to D2 and maybe you have D2. You can take it to models just having helper functions like this. So there is no a lot of uh, magic. Everything you are doing, you are seeing them. So it's very, very, very important. All right. Let's now work on the. Let's start adding the rest of the service. Let me scroll down here. Let's start here. Okay. The first is to create products. So let's add a create product. Okay. Now when you want to create products we are going to receive the create product request from the DTO and return product response right. Now let's create a model here that we want to persist to. Products equals models dot product. And then we fill the data that we got from the D2 okay. So far so good I hope no confusion. The next thing is just to create we call db dot create and pass the pointer and then check for error. And that's it. Next we return get product. So x dot get product is something we don't have yet. And I'm going to add it shortly but just keep it that way. Remember, we did something like that sometime. I think it was during the gait profile that we pass the ID to help us return. So it's a reusable function. Okay. After this. Now let's work on get products, which is a little bit big because we need to add pagination. Let's let me come down here I hope let me open it a little bit. I hope you have seen it. I can close it now. So let's create a function signature and then I can talk about it. When you want to get products from the service you can, you have the opportunity to pass the page and the limit. So basically uh, page is sort of the offset. And limit is like the page size, like how many you want to fetch from the database. And when we find the record where we promise to return you three things. The product information which is in encapsulated in this product response. We give you pagination metadata, which is something that we created some time ago that has the page, the limit, the total, and then total pages that you have. This can be used on the front end to create pagination. This is very good information to use on the front end. And of course a possible error. Okay. With this let's add a little bit of guide. If the page is less than one let's set it to one okay. If limit is less than one, let's set it to ten just to create some kind of guides to make sure that we are good. Now we can calculate the offset, which is the classic way to calculate offset when you do pagination. After that, let's create slices of product and then the total. Let's set it okay. After that let's try to find all the products that are currently active and then count it and we map it to, um, total. So basically we find all the product that is currently active, count it and add it to total. Now why this? Why this might work sometimes this is tricky because let's say if you add a different clause to the next query that we need to populate the output, then this might have maybe more data than that. But for this course this is perfect for us. Okay. The next thing that I want us to do we are going to fetch products. So you see we have the products here. We are not using it yet. We need to find the data and map it just like we're mapping like this. But here's what we are going to do. Aside from the products, we also need to find the categories and the images. So go or allow us to use something called preload, which we can use. So let's let me show you what it is. We call db dot preload category and images. If you go to product here you see we have category and images. So that's exactly what we are preloading here. Basically you are loading both record at as part of the product. Then we check the we are only interested in product that is active. And then we add the offset and the limit and we bind to the product. So it's going to fill this up. And then we check for error right after that. Every other thing is something that we've seen before is basically transforming it to this response here. So we are going to loop using index based loop. And then we transform it. This is where we use our wonderful helper function that we created earlier. Right. And what next? We calculate the. Total pages from the, um, from the, uh, data that we got from here. So we use it to calculate the total pages. Okay. After that we do the metadata. Let's construct the meta. So this um pagination metadata is basically simple. We we the total pages will be here if you think about it. Total what is total pages. The the total number of records. Let's say this is 500. You know, the um, added the limit plus one then divided by limit again. And this gives you the total. So it's sort of help you, um, balance it out. And then after that what just return. We need to return the response the meta and NE. So that's it for this. I hope this is not confusing at all. Don't let the pagination thing confuse you. The the most important thing here is this query, which is very simple. We did I think the the thing that is new here is the fact that we call preload twice to preload these two associated types. And then we added of course you have seen the where before. Offset and limit also are new but they just very basic. And then we call find. Find can pull multiple records and we bind it to this. And that's it okay. Now we still have one more method to add okay. Since we are done with this I can fold it and then come down here. The squiggly line we have here is because we are here to create the get product. I think let's go ahead and create it here now. So get product. Remember it takes an ID of the product and then we try to find it. Let's create the model and then let's attempt to find it. We are going to preload as well the category and the image. If we can't find it we return an error. If we at this point we should have found it. So we create the response and then return the response and knee and that's it. Okay, since the video is not that long yet, I think we should create the update product as well. So if you have typed this out, let's make some rooms here. Okay. Let's create this one update product would take give us an int. So we take an int and IT request to update product request from the D2. Okay let's find the product that we want to update. Very important. If we can't find it we return an error. If we found it let's update all the fields that we can. We want to update using the data that we got from the D2, starting with the category ID in case they want to change the category of the product. The name. Let me make sure that I type the product correctly and then the description we are going to do the price. The stock if it's active is not near. That means they are changing the product. If it is active or not, we want to set it and after that let's attempt to save it. If we are unable to save it, we will return an error. Else at this point we have saved it and we should return get product with ID. All right. So this is very, very good. You see we are using Getproduct at two different places. And that was why I put it in a dedicated function. So this is good. Before I end the video this entire code I want to show it to you completely one more time so that in case you need to retype it, you can pause the video gradually, and it also gives us an opportunity to review it quickly before I end the video. I don't want us to rush through things because I don't want the videos to be too long, but sometimes, you know you want to review your code. All right. We started the product underscore service ago with simple struct, which is this product service. We added a constructor. We added some methods to create category. Nothing tricky about them. They are very basic thing. The only thing that we use from the go here is this. Create here and pass in the model. Then here as well we use db dot where and to find the data that we want to return for this gate categories or in update we find the model as well using first first returns a single record, find returns multiple records, and then we also use the save here to save the record. Delete. We use delete to delete the record. So we pass the identity here. Okay then for products creating product is also very similar. We we we created a model and then pass it to DB create. And after that we call this get product which we'll look at shortly. I think where we have most code is in this Getproducts because we introduce pagination, we needed to count the records that we have in the database first, and then we went on to execute this query which does the preloading of category and images. And then of course we set offset and defined which would find all the records that match this criteria. And the criteria is very simple is active is equal to true. And then it fetches them with their categories and images that belongs to them. And here it is just classic. We want to transform the records, which is at this point what we have is models because we don't want to return models to the customer. We transform them to product response here using this convert to product response function that we created. And then we calculate the total pages. And then we create the pagination metadata. And then we return it right after that they get product is very simple. We find the product in the database. We preload it with the get category and images. And then we convert it to the response that we want. And then we return it. And then the update product is very very similar as where we find the product where we want to update. We set all the fields that we want to update, we attempt to save it. If everything goes well, we return it. And then this one is the helper methods that know how to convert a slice. Sorry, I don't know how to convert a model, just a single product to product response. And that is what we use in the loop to do our conversion. I hope I am able to explain this clearly without confusion, so I'm going to go ahead before I commit. Let me run the linter. Okay. The 61 error with which is this admin middleware. So we have no problem. So I'm going to go ahead git add git commit add product. Services okay. And that's it for the video. Um it's quite a long video but yeah that's what it is. So thank you for watching and I will see you in the next one.
