# Add Auth Routes

Hello. Welcome back. In the last video we added a file called auth underscore handlers inside the server folder. And then in it we created a register handler which is just a HTTP handler. We created a login handler. We created a refresh token handler. We created a login handler. So far we've not been able to execute the code. So in this video I want us to fix that. Let's go to our terminal let's git status. And then I'm going to add git add git commit and auth handlers. So we commit our changes. Now let's branch out of this to 13. Uh add or shoot something like this okay. Now that we are in a new branch let's open our folder structure and add in the server that go. So this is server. Server that go. Let's open it up. This is our route here. So let's add more route. So I'm going to come here and say API router dot dash group and then API slash v1. Then we are going to do something like this. Now linter doesn't like this but yeah we're going to do it. I just want to use it to organize it. You know it makes it cleaner in it. Let's create another one or group handler. So this is for authentication. Then we do something like this again. And in it we can do other post. So when we receive a Post request to slash register we want to call the register handler which is which we added to the server struct. So I hope you're not confused right. We added this register here to the server. Let me show you this is it that we just did it some a video ago, then I we can do the same thing. Let me just duplicate this. So I go to I can have login I'm going to have refresh for refresh token I'm going to have logout. Then we change this to login. Let's change it to refresh. Refresh token. And then this is logout. And that's it I think we mistakenly deleted something here. Right. So this is this is our root. Now I want to go to the terminal to attempt to start the program. And let's see what happens. Make run. Let's wait for it. No errors and it's running. So look at the routes that we have already. You see congratulations. We've done a great job. So with this in play A, we should at least run our linter. So make lint. Good. Now we still have the auth middleware and the admin middleware on use. There is no problem. Then it's complaining of line 40 that is unused. So there is multiple multiple ways to fix this. If you want a if you want the linter to ignore something, what he's trying to say is that we didn't declare any variable in this block. And what we have here, they are not variable declaration. We are just calling functions here. So he's complaining about that. It's an empty block that we could have as well. Taking this out. But I like the grouping. So what I can do I can cheat. We can say uh, go no lint. And what is the rule? You say unnecessary block. So if we want, we can say let let's try this go critic. I think this is the name of the rule. Then I can put a comment. Uh, and maybe I need this for readability. Something I do when you are working in a team and you do something like this, always have a comment to your to tell your team member why you're doing it. Um, it doesn't it doesn't remove it. So let me see what is going on here. Let me see why. So that it says line. What? Line 40. Okay, so maybe we should put it up here. I think it's line 40. Let's try. Good. So you see that it works. So it's ignoring that this whole path, this is one way to do it. I'm going to show you another way to do it later. But this is one way to ignore it. So in the next video we are going to test what we have done. See you in that video.
