# Add Auth Handlers

Okay. Welcome back from the last video. I want us to commit what we did. So let's first of all do git status git add git commit auth services okay. Now this new video do git checkout dash b one to add auth handlers. And now we're in a new branch okay. Now let's go back to our file list. And in the server folder right click. Or you can use the terminal however you want it. Just create a new file called auth handlers dot go. And in that let's start creating our auth handlers. All the handlers would be attached directly to the server so we don't have to do much I think I didn't put that go right. So let me rename the file But go. Okay, now let's put a package server. Okay. For the first one is register of course. So let's put a server register since we are going to have the route here too. We can keep everything lowercase register. And you know this is Gene. So we can do gene dot context. So it's a handler and it doesn't return anything. After that. We need to we everything we deal with here is a D2. You are not not going to see model. So let's say the request is going to come in as um register request. Now we are going to use Gene to extract the data and bind it into this request. So let's use that. So Gene is going to use this should bind JSON into this. And this is how it's able to do some of the validations here. So let's go back. After that. Let's create the service, the auth service here. And after that let's now call the register service from the auth service. You see, remember what I said that you have a service and you can call the service from your handlers or your GraphQL resolver or from your gRPC handlers wherever. So this is our HTTP handler. We are calling the service from here okay. And now we can check for error. If there is an error we return bad request. And then we return with. This return statement is very important. Don't forget to do it. If everything goes well, we just return, not return. We just, um, we just end the process with this, uh, response, which is the created response that we created. Remember, we are not returning anything like this. You cannot return anything like this, but you can have an empty return, which here is irrelevant. So this is how it's done. Now look at how simple our HTTP handler is because we we have the the responsibility is well divided. The model layer is handling. It's thin, the layer is handling it's thin, the service layer is doing its thing. And then we have the smallest possible cause that we need to make in the HTTP handler. And that is why it looks so simple. So that is it for the register handler. Now let's do login. Login is also very similar. Every this pattern that we are seeing is going to be everywhere like that. You bind you get this your request you bind to it. You can then um create the service, call the service that you want to call from the service. Now here is login. We check for error again. And then if error Ferrer is, if we have an error, we return unauthorized. If not, we just return success with the response. The response from the auth service already contains our JWT, and every time we don't need to even think about it, think about it. You can access them in the response. For example response dot access token if you want to do something with it. So this is why, um, this service oriented architecture is very powerful. Okay. Next one is to uh do refresh token. So function. Server refresh token. Then we do the same thing bind to it, create the service, call the service, check for error and return. And that's it. If you arrange your program very well, it becomes like a poem. It reads so well and people love it. Next let's do logout. Logout is probably even more simpler. Sorry. Um, we don't refresh token. We don't. Let me see. We don't refresh token. So for logout. Let me see. For logout we can. Let me see what we have here. Yes. So this is fine because we are going to receive the refresh token that we want to log out. Yes. Now let's bind to it. Create the service, call the service. And then we also output a success response. And that's it for logout. Okay. After this logout I want us to end the video here. And then in the next video we can add the routing and we take it up from there. Thank you for watching.
