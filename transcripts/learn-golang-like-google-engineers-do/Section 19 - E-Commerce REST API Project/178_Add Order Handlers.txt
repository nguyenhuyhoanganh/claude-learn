# Add Order Handlers

In this video I want us to add the add order handler. So basically when the user wants to create an order, this is the handler that we are going to call. So let's check out a new branch 29 add order handler. All right that's good okay. So what do we want to do. Let's go to server. This is where we are doing all the handlers. So I'm going to create a new file add order handler go. And in the server itself let's add the dependency. So order service very important because we need the order service in the handler. And here we need to add order service services dot order service. And then let's add it to the constructor. Of course this is going to make this method to fail. So let's go to main and fix it. We come up here. all the service I can take out one of the D, and this looks good now, so we can add it here. Now that the line is getting too long, we can break it down. Break it down like this to make it look nicer. Sometimes you want to make your code look beautiful. You see? Good looks better. And now we have the dependency injected. We have the file created in server. We created a new file order underscore handler. In short, I'm going to go ahead and run the linter to make sure that the modification that we just made didn't break the code beautifully. The code didn't break. So we are doing great. Now the handler is going to be very, very easy to create. So let's add a new method to the server create order. So let's do it. This method is going to help us to create an order. In order to create an order, what do we need? We just need a user ID because the order information is in the cart. We are going to use the service that we created earlier to grab them from the cart and do all that. So we are going to call the order Service Create Order method to create the order. So let's grab the user ID from context. You know we have been doing this many, many times already. And then we just try to create the order. With that we check for error. If there are errors or any error. In particular, we try to return the error to the user using this bad request. Bad request response that we created is in the utility package. If not, we can return the um, the response okay and then pass the order along with it. And that's it for the create order handler is very simple. Now in order to get all the users order, let's add another method. Of course we still need the user id. Now we need to grab the page and the limit from the query string. You've seen similar thing before. Yes, we did it before. Then we try to grab the orders using the services. Here the most important thing here is to check for error first, because if there is any error then these two fields would be invalid to use. So always check for error. All right. So far so good. If everything goes well we just return the data and that's it. We have used this before so there's no huge explanation needed for this. But if you want you can open it up and see what it looks like. So this is it. And that's it for the get orders handler. Now to get a specific order let's add another method. So I'm going to close this side out. So let's grab the user ID from context again. Then let's try to when you the pastor pass us the id we want to make sure that it's an unsigned integer. And after that we check for error. Then we try to grab the order from database. Also check for error if there are no errors, we return the order and that's it for the handlers. The next thing we need to do is to map these handlers to route so we can start executing the operation. But before I do that, let's try to run the lintel and see what we have linked. Okay. There is a lot of um linting error. But these are not technically these are not errors. They say that the methods are unused, which we know because we haven't used them in the route yet. So this is quite good. So I'm going to run a format to make sure that all the codes are well formatted. But this linting error would go away as soon as we start using this method that we just created. So I'm going to go ahead and um, I think we can keep the commit in the next video when we add the routers. Also. Let me see. Yeah I think we should do it in the next video. See you there.
