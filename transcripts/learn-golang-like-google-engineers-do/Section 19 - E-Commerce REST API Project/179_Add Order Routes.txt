# Add Order Routes

In the last video, we created a create order handler, get orders handler, and then the get order handler. All right. Now we need to add some routes that would point to each of these handlers. So let's open our file browser. Go to server. So I'm in server then server dot go scroll down a little bit to where we are doing all our routing. So this is for cats. So I'm going to put a space and then add one for orders. So let's create the orders route okay. We need to do this because of the linter. We always have some kind of variable here to just sort of make sure that it doesn't scream at us. So let's add for orders as well. And then we add for the three methods. So when you go to slash, when you post data to slash orders, we create the order. When you go to just slash orders, we give you the list of all the orders for that user. When you go to slash orders, slash an order ID, we give you that specific order and that's it for the route. So I'm going to run the linter. Okay. Linter looks good. Git add git commit add order handler. Good. Now, um, the next thing I want us to do is to test this order implementation. And we're going to do that in the next video. But so far I think we've done great. We've added all the routes, we added the services handlers and everything. So let's test it in the next video to see if we have any bugs or not. See you there.
