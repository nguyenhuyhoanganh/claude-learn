# Add File Upload Service

Hello, hello and welcome back. In the last video, we did a little bit of refactoring where we change all the services to from creating them in the handlers to dependency injection. So that is what we have here right now. Time to work on file upload. So let's do git checkout I'm going to call this 23 five upload service something like this okay. And then we close this one out with this. Let's go to internal. This is our folder. And now create interfaces or contracts whatever you want to call it. But I'm going to call it interfaces. And in it I'm going to call write upload dot go. So in interfaces create upload or that. Go there and let's create a simple interface for our file upload service. Think about this because I want us to be able to upload to our local file directory and also to AWS. We need some kind of interface to allow or switch the upload source. And this is where interface becomes useful. It's not the having an interface for the sake of interfaces. This is a very useful interface that we want to have. So let's create an interface called Upload Provider Interface. And in it we are going to have two methods upload file and delete file. This is going to be a multi-part file header which is going to contain the content for us and the path. And then we return an A string, which is like the path of upload and a possible error. This the path and the error. So this is a very simple interface we need to implement for local file storage and AWS. So let's go ahead and start with the local file uploader. In internal. So this is internal. Let's create another folder here called providers. And in it create a file local provider. Okay. So far so good. Let's create a new type. Local upload provider is going to be a struct with the base path right. Let's create the constructor. For this. Okay next let's implement one of the methods which is the first method upload file. So I'm going to say func. This is the method that we have from the interface. Right? With um, let's go back to make sure that we are all in sync here. Where is the interface? It matches it multi-part a pointer to multi-part file header and then a path string. All right a pointer to multi-part file header part string. And then this is very good okay. Now let's form the full path. So basically joining the base path and the path we are going to receive. With that we can attempt to create the path. Remember uh, let's say if you are going to have uh upload maybe two for product, uh, ID, maybe we can even say images if in order for you to create all this nested part, we have to use the make Mkdir all method in the OS package. Something like this. This is what we. We have seen this before. Okay, after that we can let me write something out. We can attempt to us first. We are the file that they are sending in which we have here. We are going to open. Let's say open source but not open source software I mean the source of the stream. Then create destination. And then read from source to destination. Something like this. So this is the three steps. You open this one and get the content from there. You create the destination file. You read from the source into the destination. And that's it. So let's go ahead with this first one. So we open it. Let's check for error. If there are no errors let's defer the closing of that source. Very important. Then here we attempt to create the the destination. We check for error. If there are no errors we defer the closing as well. Then here we can read from destination. So destination will read from source basically. And you can also use copy to copy it. Then lastly we return the path like this. And that's it. The next one is the delete file. And that one is very easy. We create the full path and then we delete it and that's it. So if we go to the form if you go to the upload interface now you see we have one implementation my I'd usually show the implementation, so I can click on it and jump to it right here. Very simple code, but also very powerful. All right. The next thing I want us to do, since we have the interface and an implementation, let's now add the service itself. Okay. Let's open our services. Where is it. So this is services new file or upload service. Okay. With that in place let's create a new upload service. Trucks. Okay. And then in it we have a field called provider which is pointing to the interface. And we are pointing to the interface because we want to be able to switch out the implementation. Very important. Let's create a constructor for that. Basically a simple constructor that accepts the upload provider. And that's it. And then let's create an upload product image method, which is what we are going to call from our handler. So let's add it. And you see the signature look a little bit similar. We just need a file this time around. And the product ID that I want to upload for. And then let's try to extract the extension. You can use this file path dot to extract an extension. And we just lowercase everything just for consistency. Then we are going to call a function here called um is valid image extension which we haven't written but we are going to write it. So let's work on that. Um, basically we want to make sure that the extension is among the allowed extension. Of course it's not a great validation, but it will do it for now. Now let's create a path that we want to return, and then we return the provider and that part and that's it. So basically we are sending the upload, uh, provider the file and the path. Okay. Then last thing, let's implement this function. If your IDE can allow you to create the function internal um inline here you can also do it. But let's do it. And then let's create all the extensions. So we want dot, jpg, jpeg, png, gif and then the web p. All right then basically let's a loop. Um the loop is checking if this extension match anyone. If we find a match, we return true. If we don't find a match, let's return false here. And that's it. For the upload service, we need to add an upload handler in order for us to test it. And let's do that in the next video.
