# Add Database Migrations

Now that we have the middleware, I think it's time for us to add some database tables. So I'm going to open the file browser here. So let's put everything together nicely. Okay. You look at the structure that we have. So in DB we have this migration. So let's go ahead and create the first migration file. The way it's going to work is this. Let me come down here and write something first. We are going to have let's say migration for users table. That is to to do something up. Then for users table we have another one to do to reverse it. So up and down. When we run migration up this we execute this. When we run migration then we execute this. You can use the migrate command to create the migration files. But I want to do it manually just in case you were not able to get the migration program to run, and we can also execute the SQL manually. So let's go ahead and do it. Let me create a file. So 001 underscore create users underscore table. So this is the first migration file we are going to create. Now I want to create an enum. Enum is a some sort of a list that is constrained that would have values. So in this enum type called user rule I want to have customer and admin. So a user in our system can either be a customer or an admin. Then the users table the users table. Let me explain it a little bit. It has ID email password, first name, last name, phone is active role which is going to come from here. And these are timestamp. Nothing. Nothing very complicated here. This is the same structure. And if you look at what we have here, it matches the model. If you go to the users model you see that it matches what we have here. The phone number. Everything is a direct match. So this is why the model is always, uh, your model must have tight coupling with the database tables. And that is why we separated that the communication between the customer and us with the D2. So it's a very important concept that you need to understand. All right. Now that we have the users table I want to add some indexes to allow us um speed up the query. So because we are going to be searching by sometime email and then deleted that, I want to make sure that we have indexes on them and that's it. Now let's do the reverse which is basically deleting the table. And then we delete the user stable and also with the type that we create. That's it. Then we add another one. This 1002 for refresh token. Let's create the refresh token table. The nothing special in it is just very similar. Um, if you are, if you are using MySQL then you can this can be a little bit different. It can be um int primary key autoincrement something like that. And then let's add the indexes. So we're adding index on the token because we are going to search by token user ID and also deleted that they deleted it that we have here. Maybe I should explain it a little bit. I put deleted art to introduce soft delete. When you build software, sometimes you don't want your users deleting things. You want to have a soft delete because when you are auditing the system, you want to have records. So this is some sort of open secret actually, whenever you sign up on any website and you. And you delete your account, I don't think anybody deletes any account. They just do a. Soft delete. And maybe they randomize your data a little bit, but your data is there. All right. Now let's do the reverse for this refresh token. And basically we drop the table. Okay. For categories very similar. So this is the categories table. Nothing special is just. That and then let's add some indexes for its active and deleted that again. After that let's do the reverse. You can pause the video of course if you want to type it out. So. Let's do the reverse. And in the reverse, we just drop it. Okay, now let's do for products. This is a products table. It's almost the same thing. Let's add some indexes for because we can search product using category ID the SKU the is active and deleted. Maybe this can be indexed as well. But for now we don't need it. So let's skip it. And then let's add for the down. So let's do the reverse. And that's it. What else let's do for the product images. So this is 005. Let's add it and also add some indexes. Good. Now let's do the reverse. And it reverts. Just drops it. Let's do for the cat stable. 006 let's also add some indexes as well after I formatted it. Good. Now let's add the reverse. After that, the cat items stable. Let's do it. Now let's add some indexes. And then let's do the reverse. Of course you have access to the SQL so you can copy it and execute it from there in the repository. Let's do for the other stable. Okay. The other stable as well. We have an enum which is basically the other Order status. Now let's create the table. And we can add some indexes. Okay. Let's do the reverse a lot of tables right. Okay. What else. We have uh order items let's do for order items which is 009. Let's create some indexes as well. Let's do the reverse which is the last one. And that's it. Everything that we have here, these are all the tables. It matches exactly the model that we have. So all the models that we have here, everything that we have here matches them. All right. So what next is time to execute this SQL. Here's what I'm going to do. I want to let me bring over, um, p admin. You see, this was a program that I mentioned last time we went to my second screen. This was the program I mentioned last time that if you cannot get the if you don't have a GUI, you should download this one for windows. I downloaded it even for my Mac as well, just so that we I show you how to use it so you can go here. So these are let me remove what is here now. For now right click Recreate Server I'm going to say Docker. Instance. Now I have the docker running I think. Then you come to connection here. My host is localhost. The port is correct. The user is correct. right. This database is the default database. Password is password. And I can say save. And now I click. You see all the databases that I have. This is the one we are currently connected to. So I click on it. Now the way Postgres is arranged if you're not careful, if you have not been working with Postgres it can be confusing. But if you come to schema here you see this public, then you see tables. Currently there are no tables so everything is empty. Now we are going to try and execute the query. So let me move this away. If I open my mic. This is my mic. This is the make file that we have here right here. Look at the migration migrate up. We have the connection to the database and the date. This is database. So let's try and run it. If you have my if you have it installed you can run it. If you don't have it Then. Then you can copy all the SQL from the OP. For example, you copy this one, you paste it in the IDE. Um, not IDE, the GUI. Let me show you how to do it. You can come here, I think. Uh, no. Not this. Let's say I'm looking for a console. Okay. Query two. Yeah. You can come here and paste it. In short, let let me do it for you to see. You see, I come here, I paste it, look at it, you execute script. You see it was executed successfully. If I go here now, right click refresh. You see, users is there now with a lot of if you go under the indexes you see all the two indexes that we created. So this is good. But I need to do the reverse. So I'm going to go to the down, copy it. Go back to our GUI. Now paste it run it again. Now this I'm going to refresh. The table is gone. So if you don't have the migrate installed this is how you can execute it. So copy everything in here and execute them one after the other and then you have the same experience. I'm going to move this away. Now go to my terminal. Make. Migrate. Up. And it worked. If I bring the the program over here and I refresh, we should see all the tables. You see all the tables are here. I'm going to run migrate down. Yes. And everything went down. Refresh again. It is gone. Do you see how powerful migration is? This schema migration. Leave it there. the. The migration program is using it to track the versions of migration, so you don't need to worry about it up again. Good. And now that is how you do database migration. Um, in go. So that would be all. But before I forget again this time around, I want to make sure we get the git right. So git checkout dash b ten add database dash migrations like this. So git status is just one thing. We added the db folder. Uh, that is the only way we change. Let me make sure that we're on the right. We are here add git commit. Add all the tables. Good. Now in the next video we start with an authentication service. See you there.
