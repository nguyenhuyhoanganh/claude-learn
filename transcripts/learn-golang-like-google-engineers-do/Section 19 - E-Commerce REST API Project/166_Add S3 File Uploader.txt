# Add S3 File Uploader

In this video, I want us to add AWS S3 file upload to our project. The first thing I want us to do is to go and create a new branch. So git checkout 24, add S3 file upload provider something like this. Okay, this is the name. What did we do? Okay, I didn't type it very well. I think I missed the dash B when I was checking out. Okay so git checkout dash B now we are in a new branch. Good. Okay let's go to the file directory and then open this local provider. Let me show you something before we start working on the S3 version. So we have two methods that we need to implement for the S3 as well. The same signature because we need to satisfy the interface. So these two methods right. The we have delete here and also this one. But notice what we are doing inside. This is the basic operation. We open the source with the destination. It was created here. But for AWS we need to we need to pass the data to the uploader. Then the uploader know how to grab the bytes and transfer it to AWS for us. And it's almost very similar. The most complex part of uploading something to AWS is the credential. Just setting up the client, because they sometimes this can be tricky. You know how if you have done anything with AWS before, sometimes credentials can be managed from environment variable. Sometimes it's from a EMV file or JSON file, or even given the virtual box and access to a particular resource. So it can be really complicated, but we are going to try to keep it very simple in this video. All right. Now with that out of the way, go to providers and create an S3 go file. And then let's start coding. The first thing I'm going to do is to bring all the imports okay. All the imports. I want to explain everything we have here. These are these are from the standard package go standard library. We don't need to worry about this. You have seen all of that before. The AWS is the top level SDK. Um, from the SDK, this is a top level object. Then we have config, config and credentials working hand to hand because you need them. You need both of them to create the credentials. When you want to make an upload. This manager is actually what you use to make an upload. And you see it is coming from the S3, um, sort of sub package. And then we have S3 as well here. So this like these two goes together, these two goes together. And then this one controls everything now because from the standard um, sorry, from the AWS SDK v2, we have config and we also have config from our own project I need, I added this um prefix so that basically I am renaming the import from config to App.config. I could have as well just name this one aws config. But you know the package is for AWS. So let's make let's give AWS a priority here. Alright, after that let's create a struct called S3 provider with some fields. Basically, uh, the client is the S3 client that we are going to construct. Uploader is the uploader that we are going to use to upload to AWS. And then the bucket is like the folder in S3 where we are going to put the content. Endpoint is our AWS endpoint. But for this video we are going to use local stack. So this is going to point to our local stack. All right next let's add the constructor. The constructor is very simple. Basically we need to construct this. Write the code that we are going to have inside. Everything is just setting up AWS something that I'm going to put inside. Let's start by creating the config. Now all these red lines you are seeing, we are going to fix them in a second when we run git sorry. When we run go mod tidy, we are going to fix it with go mod tidy shortly and if you want to do it, you can pause the video now and run go mod Tidy to fix it for you. All right, now we check for error. If there is an error, I want to panic. Why am I panicking here? This is. This can be controversial. Should we panic or we should return the arrow here? It's your choice. We are going to call this new S3 provider from Maine. That means the question is, if you were unable to create this config, do you. Do you still want to run the program? Do you want to continue running the program? Or you want to panic and tell the developer that you were unable to make the configuration? It's your choice. Some people want to do it like this. Some people want to return the error. For us, I am going to panic here and it doesn't mean that you cannot return the error. You have the choice to return it if you want. All right, now, because of local stack, I need to make sure that we can configure the we can configure the endpoint for local stack. All right. Local stack. Use this endpoint. And this use is very important. So we bring in this configuration and wrap it here. All right. After that we just return. We just return this object. All right. And that's it. Let me now run go mod tidy and good. Everything is downloaded. Now. All the the libraries that we use are downloaded and everything is green for us. So this is the most complex part of this whole AWS implementation. Every other thing is easy. Now that we have the AWS configured. Now let's add the methods that we want to add. So two of them. This is the first one upload file which is the same signature that we have in the interface place this very important. All right. The rest is like what you saw before we open the we open this file. We check for error. If there is an error, we return the error. If not, we defer the closing of the file. Right now we need to do the re upload to AWS. So let's do the upload. And doing the upload like I said is super simple. Remember we created the uploader here in our config. Now we just call the uploader upload. And then we pass it the object that we want to upload. Look at the body. The body that we have here is an IO reader. And that is why we could pass the source directly to it. The the key is the path that we are passing. Bucket is coming from the config and that's it. The rest this resort has a lot of things inside. We are only interested in the key and then we can return in. So first let's check for error and if there is no error we can return the result dot key. and then nail. Now if you notice something here this asterisk it because key is a pointer. Look at it field key. It's a pointer to a string. So we need to dereference it in order to get the string value which is what we need for our program. All right. After that let's work on the delete. The delete is even way way more simpler. It's almost one liner. You just call client delete object and then pass the object that you want to delete. And then we can return the error here. That's it. We are done with AWS. Implementation honestly is very easy because the SDK does most of the magic for you. And this is really powerful. Before we start integrating this into our code, I want to point something out, something that we are doing that is not good. If you look at our code, look at what we are doing with the context, context, what to do here, context or to do here. This is not very good. This is not a very good programming in go. When you have context like this, you should pass the context according to convention. You should pass the context as the first argument. And I would love to do this in another video so that we can clean this up a little bit. Don't forget you can just do it here. If you want to do it, you have to change the interface signature. We have to change both implementation to adapt to these new changes. So I won't do it in this video. All right. With that out of the way. And now that we have our both methods implemented let's go to main. So main dot go. And how do we use it. There is something I want to do. We I want us to be able to switch between these uh Luca. Luca. Upload provider and this new AWS provider. So let's do something over here. I'm going to say, for example let's say upload provider. We know the interface is here. So it's an interface. All right. We can say if config dot upload dot. We don't have any field to use to to do the switch. So we can say okay upload provider we are going to create it. Let's just assume what it will be called for. Now, if it is equal to S3, then we can set this to providers. Dot new S3 provider. What does it take? It takes the App.config so that's the config. Else we set it to this one. And then we can pass it like this. Look simple. No magic. The only thing that we are missing here is that this upload in config doesn't have this. So let's go and add it quickly. So let's go to our file browser inside internal config config. Go. This is our upload. So let's scroll where we have it here. This is the upload config. So let's add it here. Upload provider will be string. And we can just add a comment here. Upload provider can be can be S3 or local something like this. Now let's go scroll down and set it. I'm going to copy this and paste here and we can say upload provider. Let's set the default as local. Then we need to go to our dot env file. So scroll down to the bottom of your project dot env file and look at it. I have it here so you can set it to S3 or whatever you want. You can come here and add it to the example. So let's just say local. But for my env I'm going to set it to S3 because I want us to test it. All right I hope you have done everything. Go back to Main.go to make sure there are no errors. So what we did basically we declare the variable here. This is a variable declaration of type and interface. This is variable initialization. So we are doing conditional initialization here. If the upload provider is set to S3, we initialize it to this one. Else we fall back to this. So as long as as long as it's not S3, it's just going to fall back here. The locale is not really useful, just S3. That is important. Okay, with this out of the way, I would like to run the project and see what happened. Uh, so make run and then the project run. So this is fantastic. Fantastic. Now, um, I want before I do anything, I'm going to do make, uh, Docker. Make Docker down. So let's shut down the docker. Make Docker all good. Everything is back up. But now that we have think about this, we have the we have the S3 working. But how about the bucket that we want to upload. So let's do something. Let's go to um dot env to make sure that we are pointing to localhost. So make sure that your AWS endpoint or AWS S3 endpoint is like this. The port number is very important 456 is that is the right port number to localhost. Now that we have the port number to localhost we need a way to create the buckets. Here's what I want to do. You see inside Docker folder here. When when local stack is starting up I want it to always create the bucket ready for us to use. And the name of the bucket would be this. So let's go ahead and do that. Okay. Right click. Let's create a folder here called init. Let's create another folder here called AWS. And then in that AWS folder create a file called init. Okay. If you give it a different name you have to remember the name because we need to do um we need to reference it in the docker compose. Then here you should put this content very simple. It's a batch script basically that inside the container there is a program called AWS local that would call us execute the S3 command and and create this bucket for us. And that's it. Now that we have that, open your command or your terminal. And then you need to make it executable by running this program, change mode plus x and then the full path to this file which is here. So just press enter. And that's it. Now that the program is executable, go to your Docker compose. And then in this is your local stack. Here add one more entry. So you add this entry. Basically you are mapping the file we just created to this path in the container local stack. Know how to use this program basically running it when it's starting. That is why we are using it. That means when local stack is starting, it's going to run this bash script, whatever you like, you can put here. Actually, it doesn't matter. It doesn't have to be. Creation of buckets can be creation of an SNS topic. It can be a queue. Subscribing to an SNS topic. It can be you putting content in a queue. Anything you can do in here. All right. After that, let's make sure that, um, local stack Postgres. So we have all that everything. I think everything is right as it should be with this in in play. I want to go now and run docker. Com um. Sorry. Make make docker down again so that we shut everything down. Okay. And then make Docker up. Good. Let me run make run to run the program. Now everything is running. No errors so far. Now let's try to upload an image. And it was uploaded. But how are we really sure that it's the S3 version that it's using and not the local one? All right, let's do something to it, just to be sure. So I'm going to go to um I'm going to go to our handler. Here. So so not the handler. So the provider actually so S3 provider we called here. Right. So let's come here and put a log dot something like this. Uploading file, this part using S3. I'm just going to make it later. All right. Log. Let's make sure that the log is correct. Let's run it one more time. And then I run it again. And let's look for the log that we just put. Um where is the log. Look at it. Uploading file this using S3. So no no games, no fluke no lie. So very very good. We did it. And that's it for the video. I still have a little bit more that I want to show you. Um, for example, how do we serve the content that is in the S3 bucket to our end user? Let's work on that in the next video.
