# Event-Driven Programming: Add An Event Publisher

I want us to talk a little bit about asynchronous programming, where we process some task in the background. Go supports high concurrency. We can use goroutine to do that. But what if we want the the task, the data that we want to process? What if we want to do it in a more robust way? For example, email sending. When you want to send an email, you want to be able to process it outside of the HTTP context because it takes time. You probably want to process some PDF or create the templates you want to dial in. Um, SMTP server and all that can be really, really, really long. And this is something you don't want to you want you don't want your user to wait for when the user clicks on, let's say, register. You want to take that task, put it in a queue and then a consumer take over that process and process the message for you. This can be done in a lot of different ways. For example, some people can use Reddis. Some people use this AWS queues and that is what we are going to do in this course we are going to use AWS. I am introducing AWS here for a reason. Number one, most people in production out there, the job you are going to work do every day. Most people, most companies use AWS and already we have started using AWS. We use it for our buckets where we store the static assets. Um, earlier and now we are about to work with queues using, uh, the SQS program from AWS. So in go, um, AWS already have SDK, but SDK doesn't give you everything. I want to make your life a little bit easier. So there is this program called Water Me. This program it it make your life like ten x easier by. It gives you everything that you need to create a sort of a pop song. Almost anything that you want to do using um, in order for you to design event driven programs in go. So this is what we are going to use to achieve our objective, which is to be able to send messages, um, in the background using Amazon. Q all right. Now, with that out of the way, I want to open the program. This is our e-commerce program. I my I committed the changes from the last video, and now I am going to check out a new branch, uh, let's call it, uh, what what did I call it? Um, three one. Ah. Event publisher. Okay, so this is going to be our new branch. Now, what do I want to do in. The most difficult thing probably is to create the config. But because we are using local stack, we are going to keep the configuration super, super easy. And if you want to use it with AWS directly on the internet, you can modify it a little bit. I want to go to internal then providers, and I'm going to create another. File here called AWS. All right. We already have this S3 here that we have. You see we have the AWS SDK version two already in here. And this is important a lot of people are still using version one which is AWS SDK lids. But I'm using the version two just to make sure that you are up to date. It's a little bit more difficult to work with, but for me, I don't care. No matter how difficult it is, I can try to make it work and then keep you up to date. So let's bring in some imports, which I just want to show you. The most important thing here is the config. This is what we are going to use. The credential is what AWS used to manage credentials and config is what we want to create. Okay. So we are going to create a function very simple function here which is create AWS config. It takes a context and endpoint and region and the endpoint. This is a URL of our local stack. And we are going to keep the endpoint config very basic. I'm even going to leave some sort of static values in there. So let's create a config we want to return and a possible error. And I'm going to come down here and return the config and the possible error. Good. At least this put us in a clean state and we can put the code in between. So what we want to do, we want to check if the endpoint is not empty. We want to do something else. Maybe we should start with doing something else part by saying cfg which is the config and the error is equal to. So we are going to call the config dot load default config, pass it the context and the region and that's it for this. This one it means AWS can use this um, sort of the default to manage the config and get it loaded for you. It uses all these environmental variables to pick things up and all that. All right. For this we want to do a little bit more what we want to be able to fetch the config using the, um, the local stack and the URL and everything. So let me bring it over and just show you it's also a very similar function call. But this time around we are using the endpoint. And then we are using a credential provider which is this. So if you are using this endpoint approach for your AWS you can go to AWS. Of course. Let me show you what you can do instead of just accepting this, um, endpoint and region, you could accept this is our config. You can come to the config and add, uh, you see you can accept this AWS config right there because we have it, you can accept this AWS config and use it. But for me this is very easy. I don't really care for it. For now I'm going to pass the config over there and then we can use it. But honestly this is everything that we need to do for the AWS config. And in the next video we are going to continue to update it, add more stuff. But for now just want to keep this video short. So you take this one right here. And then let's continue in the next video. So before we go I'm going to make um make lint. Write the linter work and that's it. I'm not going to change anything. I won't commit the code yet. And when we come in the next video, we can do that and continue adding more changes to it. See you there.
