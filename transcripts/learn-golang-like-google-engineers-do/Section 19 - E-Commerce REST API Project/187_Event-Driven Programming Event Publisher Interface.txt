# Event-Driven Programming: Event Publisher Interface

Now I want us to first of all, commit the code we wrote the next last video. So git add git commit r aws config. Now we are still in the same branch I'm now going to check out yet, so we should be in the same branch now that we have the AWS. Sorry. Now that we have the AWS config, let's add some configurations. So the first place I want to go is to go back to that config where this is internal config, then config go. So let's go to AWS config here and I want to add event. Name. So this is the name of the queue that we are going to use. And then where we have the load where is it this one it's going to be event queue. And then we just set it like this. So that means we can set the queue using this environmental variable. Of course, by default this is going to be the name of the queue. Then the next thing I want us to do is to open the env file. Let's add it here. This is let's add it here. So this is our env data. And also add it here somewhere I want to check as well is the docker. I'm not sure if we have anything in the docker. So docker docker compose. And for sure we have um some of the data here. Um, so I think we should add it here as well AWS what is the name AWS underscore. Oh no this is not this is not where we supposed to put it. I think if we want to put it it would be it would be the app itself, but at the moment I think we can skip it. All right then. Um, after that, I want us to create the interface for the publisher. So what is the what is a publisher? The publisher is the program that puts data in the queue. So you are the producer. You are the one that puts can do something and put data in the queue. Then we are going to design another program, which is the consumer or the subscriber that takes data from that queue and process them. So for now we are going to have a very simple interface. So in internal here let's create another folder called events. And in events we are going to create a publisher dot go. Okay. With the publisher Dot go let's add the type publisher. And then there is a publish method and a closed method. Publish method is simple. It takes an event type, the payload and then any metadata that they want to provide for that event, and also close, which we can use to shut down or do some cleanup. And that's it for the publisher. In the next video, we are going to write an implementation for Watami that is going to implement this interface, and we start using it. But for now we can end the video here. First I want to try to lint. Good. No errors. So git add everything git commit and aws config no add event name config and publisher interface okay. And that's it for the video. See you in the next one.
