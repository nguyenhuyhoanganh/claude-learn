# Add Swagger Documentation

In this video, I want us to add some documentation to the code. It's quite easy to do and sometimes I don't know why people don't even do it. So let's go to the browser and look at this slash swag, which of course I'm going to leave the link for you now. It's um, it automatically generates the documentation from the code and publish it for you. There are many ways to theme it, to give it beautiful looks and all that. And, uh, you can scroll down to see how to install it, go to Getting Started and then you can run this, go install this, go install I've talked about it before is going to put this Swag binary in your path. So it is very important for you to have your goal. So let me go to. So for example let me type echo go path. It's very important for you to have like your goal path slash bin, for example. Now. So to have this in your path so that you can easily type. For example, if I type swag, I have swag. I run that same command to have this for me. If I don't run that command, I would be forced to say let's say go path slash. B slash log. Something like this would have been the full path to it. So you can find the path to your goal being and run the program that like this if you want. But honestly it's more convenient to do it like this in that bin folder. So let me do let's go path slash bin. And you see everything that I have installed from go You see they go path slash bin. I have so many things installed. Even this is, uh. Somehow, I don't know, PM Python stuff got in here, but, you see, I have, uh. This is for debugging. These are for for import, for security. This is the vulnerability check and all that that we are using. You see, this is swag. Everything is in here. If I want, of course I can, you know, point to them directly. Like this. So let me go do that. Do it one more time. Because I think that I suspect that this is probably going to be confusing to some people. If I want, I can run any of the program by typing bin slash, uh, any of them, let's say pip, for instance, and it would run, but it's not as convenient as just typing In Pip, for instance, or swag, for instance. And in order to do that, you must have that path, that directory that I just showed in your path variable, which is usually, um, if you are, it depends on what you are using. If you are using bash, it will be in your maybe dot like dot uh profile or I think it's bash. Let me you can see something like uh. Let I want to do something. Let me do ls like this I'm going to view I want to attempt to show everything and it's like this, but I'm going to grab it to do bash. You see something like this, then there. If you are not using bash, uh, you think is Z like this to show for the. So it will be this one. The Z arm is inside one of them, but on the Mac by default, the latest version of Mac not like for some long ago. They the the shell. You can type echo shell to know what you are using. See I'm using the Z. If you are using bash it will be bash here. So make sure that you have your path correctly set. Then for windows you have to do it from the um there is an environmental variable settings uh, that you can search for and then just set it as a windows is GUI. It's actually way, way more easier I think. Okay. The first thing I want to do is to open our main.go. So this is our main. And then I'm going to add some comments here. And this is not just a comment but actually the beginning of our documentation. So we are saying that this is important to you add it to the main. We are saying that this is the title of the documentation, the version, some description like terms and conditions or terms of service. If you have, you put the URL. I don't have all that, so I just put some data from myself, my LinkedIn and my name and some just, you know, random email that doesn't it's not valid. But more importantly is the port and the API path. So let's go to postman and check that okay. Our API is in API slash v1. You know, um, if we need to adjust it to come and adjust it later. But you see that we have all that. Then in the At security definitions dot API key we set it to bearer auth. Very important. How do we want to pass it in where the header and what is the header name. Authorization. So this is very important. Then we added a description here. This is the simple sort of the bare minimum that you have to provide in order for you to get a documentation. By default it generates a swagger documentation interface, which I don't really like because I think it's I don't think it looks that great. So in order for me to trim it a little bit, I'm going to come here right click, let's create another folder. Then in it I am going to create a rapid. Dot HTML and then not dot go. I need to rename it from go. The idea is assuming we want to use go. Then here I just paste the HTML. That is very simple and there's nothing there. This is just the JavaScript is going to inject everything in here for us. And so this rapid doc we are going to use, you can copy this rapid doc on the browser for it to give you, um, sort of what you can set here. So I just found like I went on the internet as well to find some default values. And then I have them set up. Now after that we need to set up the main and we need to bind the root. We need to do a couple of other things. But this video is getting too long. So I want to give you the chance to just do these two things that we did. So let's go back again. In our CMD main go. We added these comments. You see I haven't imported anything yet, but we are going to do that in the next video. I just added a comment, then in the I added a folder docs and then I added this rapid doc dot HTML, which is this. So that's it for now. In the next video we are going to add more information. See you there.
