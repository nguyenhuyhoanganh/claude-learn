# How to work with Arrays

In this section, we are going to talk about complex data types like array slices and many, many more. In this video we are going to start with arrays. Arrays are fundamental building blocks for collections in go. Um, in computer science, arrays are one of the most important data structures and they are everywhere. And an array is a numbered sequence of elements of a specific length and type. Very important. The length of an array does not change. The data type must be the same. Unlike Python where you can create an array, for instance, or JavaScript and you add different items in go. When you create an array, two important things two important array attributes is the type must be the same. That is, the item type that you want to insert must be the same and the size does not change. The size does not change because when the memory is allocated, they allocate contiguous block of memory and it's almost impossible to change the size because, for example, let me give you an example. Let's say, um, let's create a different uh, section for this. So this is going to be three section. And I will go in here. Now create a folder one. And in that I will create a minute ago. Now when let's look at let's imagine this is memory right. If you allocate uh let's say var numbers. And I put two int. Let's say the compiler allocates this contiguous block of memory for two items. So one and two and put one and two here. If you attempt to expand it, what that means is you want to want to expand it to this level. But what if after this location, something else is using this block of memory? That means it is now risky. You don't want to overwrite data for other programs, right? Because you are not the only one in the computer. So when an array is initialized, the size cannot change. There is a different approach. If you want a an expandable array like slices or in Java array list, the way compiler treats them is a little bit different from classic arrays. This is a classic array that is very efficient, but then they are not very flexible. So let's look at how to work with arrays in go up here I just declared an array to possibly have two slots right? And we can go ahead and print what could be in those slots. But this I want to put the plus V to print the internal structure. This time around we are going to three section and then one array main. Like I mentioned previously I'm going to continue to use go run for as long as I can, just so that if you're on windows or on Linux, you are using the same command and it should work for you perfectly. Please, if you run into any problem, do not hesitate to send me a message or use the FAQ. Send me a message on LinkedIn anywhere and I will gladly support you. So I will run the program now. And as you can see, two slots were allocated the default data inside just zero zero. So we've not put anything inside. Now we can change that by coming here and saying at index zero, sorry, uh, numbers at index zero, we want to put one, and at index one we want to put two. Then we can print it again. And you see the content of that array has now changed. Very important here when we declare the array we specify the size. You can also declare an array let's say prime numbers. We can say prime. And then uh maybe we have four this time around 223, four, five, seven for instance. And we can print it out, see if we are able to declare initialize using this short declaration, um, approach. At the same we declare the variable and we initialize the content immediately and this is the result. You can do that as well. The reason why you can also change the value. So let's go back let's say prime at index. Um so this index zero index one index two index three for instance we can change it to let's say 11 and print it out again and run it. And then you see the value is now 11. Right. We can loop. You can use the loop. So we can say for I equals so I. Equals zero I lent this is Len I less than Len of prime. And then we print it this Len Len function. Um it takes a type and gives you the length of the items. We're going to look at it later, um, in and then we can use the index now to print the prime. So I don't know why it's printing prime prime here. So I can just take out, uh, one. And I can print just one here. Let's run it again. And you see, uh, two, three, five, 11. This quote is not important. See? Two, three, five, 11 because remember, we change the value in the at index 3 to 11. We can do matrix. We can do var matrix two three. So this two by three right. And we can print it out. You see. So two rows and three columns you can put items inside. So let's say matrix. 000. We can put like this like this. And then the last one let me make sure 00010001 let's say 122 something like this. This is okay. This is not everything. But at least this gives you how we can set this so we can set data on them. Um, this is 2d, 2D array. You can do 3D arrays. You can do how many do you want? But don't forget it gets too complicated. Then it's a lot for you to manage. So this is an array. When you pass an array to function and you return them, you always have to carry along the size. They are not very flexible, but that is what an array is in go 90% of the time you are going to use slices something else, not an array directly. So let's talk about slices next.
