# Working with pointers

One of the first things I noticed when I started learning go many years ago was pointers coming from Python or Java. Nobody really talks about pointers. You only hear about pointers from C, C++. Of course, if you've studied computer science or you've done any low level programming, you have dealt with pointers before. Then when I started doing go, I started hearing about pointers. Now, the reason why I personally like pointers is I think the things that people say about pointers are sometimes over exaggerated a little bit, because I think pointers is the key to memory. It gives you access to memory. And what do we do as computer scientists? We manipulate memory. That is what we do. We push data from one portion of the memory to the other, transforming it and doing something with it. That is what we do. So having access directly to the memory. For me, it's an advantage. It's not. It's not technically a bad thing. You know, in go we have pointers, but the pointers we have in go are not like the pointers in C++ or C in go pointers does not support arithmetic. That means we cannot increment or decrement the pointers like we can do in C and C++. But they are also quite powerful because they can give you the memory address and you can use them to cause side effects. Something that we are going to see shortly in this video. Now let me create another file for this. So I'm going to go for pointers. And now we create a main dot. Go for it. There are many many cool stuffs you can do with pointers. So what is a pointer. A pointer is also a variable. But instead of storing value it stores an address of another variable. Let me give you that example. Let's say you have age. Let's say age is ten age. Here this data ten is stored somewhere in memory. How do you know where it is stored? Well, you can know by printing out the address. And here I'm not going to print. I'm going to just print the address instead. And instead of that as int uh. But it's okay if your pointers mean. And this is the memory address of this variable. This is not the value. This is the memory address printed in decimal number. So pointer is pointer. Store this address instead of this. So let me give you that example here. Let's say age. Age would now store the address. And when you print each tr like this. Uh h I'm going to age h p t uh, age address. See what I'm going to do and print age address like this. Both of them are the same. The values are the same. This is just pointing to the address of this. And this is powerful because with this you can change this value without touching this variable, because you know where it is in memory is like, uh, it's like an account number, right? If you give me your bank account number, I can pay money into the account number for you. That means I can cost an effect on that account using the account number, because the account number is a reference to that account. So this is what pointers are. So a pointer is a variable that holds an address of another variable. This is important and that's all in go. Pointers are very powerful, but they don't expose you to the danger of pointer arithmetic that you have in C. Because the problem in C sometimes you you do the pointer arithmetic matrix and you get it wrong. And then things can go really, really, really crazy. So let me give you an example of some of the side effects that pointers can cause. Let's start with a function here let's say modify value. And I'm going to say int here. And I would say var equals um value equals value times ten or something like that. Right. We are we are basically printing out. So I'm going to come here. Now let's comment everything here out for now. Or we can delete them actually If I create a value number here called. Let's use the short declaration. Let's put ten for now. And I call modify value I print the number. You see the number is still ten even though we try. We set it to this. We multiplied it. But look at inside the function the number is 100. We we call the function before the printing. But it doesn't affect it. And this is where pointer comes in. Let's create another function. Func modify pointer. Something like this. This time around we are going to take the address of the variable. This is very important and I'm going to do something before I print it I'm going to say if var equals NE we don't want to do anything. Or we can print something outside that uh modify value. You modify pointer or vice name. And I don't need this. In short, we can use println here. Okay. When if the var because it can be nil then we're just going to print this. Else we just let's put an empty return statement here for now to exit the function. If not we come here and we do the same thing. But this time around we are going to go into that address in memory. So. We are going to go to the. Value value. And then we multiply it. You see that we dereferencing. So star value is called you are dereferencing the referencing referencing You are dereferencing the pointer. That is what it means. So let me put it here for you. We go into that address and we say go into that address. So we are going to set it now to. So go into the address and multiply the data by ten and assign it back to that address. And then we print the value. So what I'm going to do here now modify pointer. You see in order for us to pass the address we put this ampersand. I showed you earlier when I printed um the age, you know, this ampersand would give us the address. So we pass the address. Now we are going to print and we you will see that it has changed the number ten now to 100. Look at this. The var here that you are seeing because this is a memory address. Look at what it looks like if you want to see the value itself. You have to dereference it and it will become 100 as well. So this is 100. But if you want the value here is a memory address that is stored. This is why pointers are so powerful but also dangerous. Because what are we saying here. This is a side effect. What are the things that are happening in the function here is affecting the variable outside of the function. Why? Because the function know the address of that variable. This is the difference and this is in your mentally. You should understand this that each time you are given a function, an address of your data, that function can change that data using that address. Most likely the data might be on the heap. That means memory management, garbage collection need to take place and all that. But very important here is that each time you pass a pointer to a function, Know that it can possibly cause side effects, right? If you are not very, if you don't have this mental model of of side effect, and you have ten here and you have called this function somewhere in your code already, the value has changed, but you are still thinking that, oh, the value is still ten, but the value is already 100 because someone changed it using the referencing. So this is the power of pointers in go. I have a video on YouTube that shows, um, complex usage of pointers. For example, let me show you something that is a little interesting. Let's say, uh, grade. Right. Um, 50. Okay. Uh, grade p tr. Grade p tier. So this is a pointer, right? But what is the pointer to this one? This grade also has a point. It has an address. So I want to print out instead of printing out. Um, let me see. Um, grade this grade TR I'm going to print the address of that function. Not not the address of this, but the address of this one. So I'm going to show you. And you see this address that you've seen here. Let's duplicate it. This is the address of this. So if you the address of grade itself this is the value here. Uh grade. So let me you have two different addresses. The value stored in grade B, tr is the address of grade. But don't forget this is also a a variable stored. That means this variable that even though it's a pointer, it has an address on it of its own. So the address second address here is the address of this variable. So if you dereference let me see if I can wrap it like this. And try to dereference it. So if you dereference it this way you should get this value back. Let's see it. Pointer magic. You see now you have the same thing. You know, pointers are so powerful. Trust me, you can do amazing things with them. But trust me, there can be also complicated to manage. And what you know. God has made it super, super fun to work with pointers. And I am going to teach you a whole lot about pointers as we build our project. So thank you for watching and see you in the next video where we do some advanced stuff with slices one more time. See you there.
