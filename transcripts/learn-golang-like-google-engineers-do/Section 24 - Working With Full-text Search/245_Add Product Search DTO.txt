# Add Product Search DTO

Now that we have our database migration in place, it is time for us to start building the search functionality. Now, what I want us to add is to update this DTO. So go to internal DTO, then go in there. You want to add this to um structs and then let me explain them. So we added search product request and then product search result. So let's start with the explanation of search product request. I think the most two important things I'm doing here is this I put this bind in here so that Jim can help us with some validation. The validation error on the client side is not that friendly, but I don't want to waste time doing additional validation myself. So I added this binding. So basically it's required. And then the minimum length is one. That's it. Then every other thing is basic for what you have seen before. But something that worth talking about is these pointers that we are seeing here. Whenever you see pointers added to a ditto. Ditto is the domain transfer object. This is the data that goes through the wire. Whenever you see pointers there, you should slow down to understand why. Now if without the without the pointer, it would be impossible for us to distinguish between when the category was not sent versus zero value. Let me say that one more time, the zero value of an int would be zero. But if we don't for because we have to make it a pointer, if we don't set it at all, it will be nil. That means we can distinguish between, for example, when the minimum price, let's say when they are there, when someone is looking for a product with a price of zero versus when they did not send the minimum price at all. Maybe when when the minimum price is near. When they don't send a minimum price. We probably don't want to include the check for minimum price in the query and that is it. So this basically we are using this technique of a pointer here in the D2 to distinguish between zero values and the absence of the field itself when they didn't send the field at all. Because when you come to the postman or when you want to send a request, it is possible for you to send. The only required fields are you don't need to send this one, you only need to send a query. That means every other thing is optional for the limit. We are setting some default limits so it doesn't matter if you send it or not. All right. And that's it. So and that's it for the search product request for this we just added a single field and wrap the existing product response. And that's it. So that's all for the D2. It's super simple straightforward. And in the next video we start incorporating this into the rest of the code base. I will see you there. Bye for now.
