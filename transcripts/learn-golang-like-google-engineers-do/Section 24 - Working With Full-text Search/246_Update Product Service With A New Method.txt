# Update Product Service With A New Method

Okay. Welcome back. Now that we have our data in place, it is time for us to go into the services. So internal services and then open the interface. So let's look at the product service interface. And we are going to add one more method. So let me put the method here. So the method is search products. Basically it takes a new detail that we created. So you remember and then it returns a product search result which we also created in the last video. And then we are as part of what we are returning uh pagination metadata and the possible error. This is very similar to what we have here in. So the code will look almost the same. Aside from the SQL. It's going to look very, very similar with this in place. Let's go and now implement this method. So I'm going to open product service. Uh let's fold it. So I'm in product underscore service dot go. Now let me fold everything here. Okay. If you look at what we have here this is almost what we are going to do. We're going to do all this. We are going to do everything like this. We are going to find um like the total. We are going to do it like this as well, the counting, because without the total we can't do pagination properly. And then, uh, we're going to find the main, um, data like the products itself and bind it. And then we convert it to the right output like this loop and then build the pagination. So this is what we are going to do. Very similar to this. The only difference is that the query itself will look different. So that's it. So I'm going to I think just right here we can start here. So let me start with the method name itself. Okay. And it will make sense to close out that part of the IDE so that we can see it properly. And maybe I can zoom the IDE a little bit. Right. Good. Okay. So this is the this is the method we have the detail of course. That is the request that is coming in. And then the data that we want to return. So far nothing. Um strange. Like I said, the page let's say the page and the limit. Let's do the check and set the meaningful, um, default values. You know, this is what we have done in the other method that I just showed you. And then let's calculate the offset here. Right. And that's it. Then the query itself, even though it's going to look a little bit, uh, weird. It's not that bad. Let me explain it. Let me put a comment here. Build query. Basically we are searching in the we are of course selecting everything that we can find. Right then we are searching in the search where that new search vector field that we added, that is our index. We are using the query that is going to come from the front end. Use this Q we are using that one to search and this know how to do that. This is why it's very important. And this um, this is a combination. So if you look up this, uh, search and the vector, there are multiple ways to do the queries and it's so powerful. But this is how we are going to do it. And of course we are only interested in products that are active now. The result here is a query object. So the this is what we are going to have. So we have we this is the beginning of the query. We have this query object in place. And then we can continue adding other conditions to it. For example now if they send us a category ID we want to include it. You know. So if the category ID is not nil, then we say query is not equal to query dot, where category id is this. That means we only want to find the product that the query matches this query, and the category id matches the category ID. We did the same thing for the minimum price and the maximum price, right? This is the power of ORM as well. Using ORM. This is really, really easy to do compared to normal SQL. This would have been a lot of SQL that very difficult to read. And after that we I think here let me put a comment. Put some comment here. Count total result. So let's create a count total variable here of int 64. And then we count the data that we have from this query. We still have access to the query. We just call count. Now to count it. You see we have not reset it. So the query object is still valid for us to use. After that we need to now create the data. So execute query with um with ranking and uh maybe create a product something like this. Maybe I think from an English standpoint should be this. All right. Uh, so let's create the product slice so products and slice. And then we will execute the query. Executing the query is honestly is not different from what you have seen. We order it here. So I can say order by relevance here. And and that's it. So we basically order it by the relevance. And then we preload the category. We preload the image. We have done that before setting offset and limit. We have done it before the find. Of course we've done it. And then dot error so that we can check for the error in line here. And that's it. So if there is an error we return nil nil and the error if not at this point for sure we probably have something you know. So that means what we can do. Now I can say um build output response here and then I'll create the result. So so result would be uh, let's make it so this is memory allocation here is super important. You're locating a chunk of memory all at once at. So if this is 50, you are going to have 50 slots of memory to put this data. And then we just loop, which is something we've done. So we just loop. And then at that index we set it. And then the the ranking here I set the ranking to uh zero zero. Um, and we can talk about it later. Uh, because there is a, um. There is a way there is, uh, a way for us to, uh, pull it out, you know, and then and set it. But let's just set it to zero zero for now, and then I can hopefully come back to it later, I hope I don't forget and I don't want to forget, but after that I like to build, um, pagination and then total and then the metadata, and then we return it and that's it. So we've done the pagination before. The only thing that looks strange for us here is the ranking, which is okay for now. Let's keep it the way it is for now. And then we come back to it, um, for the implementation. That's it. It's quite long, but honestly, if you think about it, this is the basic we've done this before multiple times. The query is also doesn't look that bad. Conditionally we are adding these ones. Then we are counting. And then here we are executing the query. This place we are building the output and then constructing the pagination and returning the data. And that's it. So this gives us the ability to search for products in our database. In the next video we can incorporate this into our handlers. And then we map it and we begin to test it from postman I will see you there by.
