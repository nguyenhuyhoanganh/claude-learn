# Fix The Database Clean Up Function

Before we start writing tests for the middleware, I want to do a little bit of cleanup. So let's open this cleanup test data here. Look at this code. This is a mess. What are loops for. So I'm going to say tables and I'll do it like this. User comment votes I think is five. Um what what are we missing profile I think we start with the profile. So we delete the profile, we delete the user post. Ideally we should delete it. Like let me do it the right way. Delete user comment. Vote belongs to. You can delete the delete the vote delete the comments, then delete the post. Then we can now delete the users. Because if you don't do it like this. If we add database constraints, you start getting errors. So here basically we can now loop for like this and then delete. Uh is the same thing that we have here. Just copy it in here. And it will be. Table delete from table okay. And that's it. Goodbye. That was a mess. So I'm going to run the code one more time to make sure things are still working. Good. Let's run it again. Things are still working to be double. Sure. That's it. That was a one minute fix to this function. It looks so, so much better.
