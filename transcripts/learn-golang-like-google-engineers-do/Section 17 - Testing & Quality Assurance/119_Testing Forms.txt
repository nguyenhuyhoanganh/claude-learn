# Testing Forms

Welcome back. Now that we have learned the basics of testing, Ingo, I want us to start writing real tests in a real project. Sometime ago we worked on this project, which, um, which we all enjoyed, but we didn't write any test. So it is time for us to start adding some tests. Now open up the project. If you don't have it, you can clone it from the repository. I'm going to leave the link in the resource. Now when I pull down the project, I went ahead to install the testify package, which is go get like this and that's already there. Okay, good. Now that we have the testify package installed, let me switch to bash quickly. By the way, you can, in case I press, uh, any key, you can see what the key that the key combination that I press with. I'm on the Mac. So you can always see the combination here. Like, for example, I recently just press command one. You know, like this. Okay. All right, I have that. So what? What how where should we start? We have all the code right here. Where should we start when I'm in new, when I'm new to a project, or I want to just start writing tests for a project, what I do is I pick the simplest thing to start writing. Test for simplest things mean code without dependency, and the form is a typical example of that. This form doesn't have any dependency. It is a very basic code, and I'm going to go ahead and create a forms dot test like this. With this in place, let's start writing some tests. So I'm going to say func I know we have this type form here and then it has a lot of fields attached to it. So let's go ahead and test some of them. We are not going to cover everything but you will see. So test new form. And this is the signature I testing dot two pointer to testing t. Basically values is url dot values. I showed this before that this is what a form is actually is just this. And this is also very simple. Data structure is a map. All right. Now that we have that we can add some things to it. So. Add email. test.com I think that will be all. Then we can create our form with the values. Now that we've created our form with the values let's make sure this is not ni assert. Not ni. Okay. The form we want to make sure that it's not name, but somehow the asset library is now getting imported, which is a typical problem of this Side. It has nothing to do with the code, so let me go here and import it. I think it's like this, but it's not going to. It's sometimes it doesn't just work. You need to clear the cache. You need to do many things. And I don't know if it's going to ever work, but no problem. What we need to do, of course, is just to run the test. So go, run, go. Maybe we should use the other command. Go! Test. Run. Run this. And it passed. You see, we don't have any errors, so we can try to make it fail shortly. Uh, if I put Dot fail here. It failed. So we know we're running it, so it's not a fluke. Okay, now that we have that, let's check for more data. I can put a equal here. But this red line and everything doesn't make it look pleasant. So in when I finish this video, I can try to fix it and then we come back to it. But right now I think I need to go here, maybe repair. I'd invalidate cache, then start showing where. But I really don't give. I don't care about it. Um, I can use notepad if I want. So let's continue writing. We want to make sure that we have this test at Test.com when we call form Dot get, you know, remember that, uh, we have the form here. And then we also want to make sure that, uh, uh, error is not me. Not me. So we can check for this. Form dot errors. Basically we want to make sure that it's initialized and we don't have any errors. So the length we assert that length of format error is zero. Okay. And that's it. Now let's run the code. Everything looks good. How can we check to make sure that it's actually good? Let's put something here. And if we don't get an error, then our code is not working. Of course we got an error. That means our code is working properly. So the test is working good. I want to attempt if I know the IDE is not working very well, but I want to attempt to run the coverage here and see what happened with coverage. You see, it doesn't work. So my ID is really, really dead. All right, so this is the first test for the form and we can do more. Let's write one more for the form. And that will be it for the form. So I'm going to come here duplicate this and say test. So let's test form. Maybe we should test the required which is something that we use a lot and we put the same image, but here we can say empty. And just put something like this. Okay. This is an empty. Just have to be like this. All right. So we create our form with it and then let's validate it a little bit. Form dot required email. We can put a password. What is empty. You know empty is actually not empty. It has some characters here but they are invisible. So we should know. First I want to make sure that the form is not new. That is still fine. And now let me see. Let me cut out this. I want to check for something else. So let's check for the password is required. At least it's there. So I Assad contains. This is the okay contains that forms of error dot set up password. Because we know that the password was not passed and we can say password I think that is the message that we are sending is required. Something like this. Let's run it and see. This time around we can change it to this. Okay. It works. So this is the message that we are passing. And you can test it by going to this required. And you see this is what we are checking. That password is required. So if we check change it let's say we remove the required. Maybe someone recently in the morning and was angry and remove required. Now you see our test is catching it. That is why you write test now. Restore it. Run it again to make sure you are green. Fantastic. After that, let's also check. Um. Let's see. Check for the empty and see. Even though we. We sort of. It's an empty space. And I can say empty is required. It's not technically empty, but ideally we shouldn't allow this. Right. So let's run it. And it passed. So we are definitely treating this as empty which is good. Okay. Um, I think that's it about it. Uh, if we want, maybe we can check for the email. Let me see. I can come here and say assert. T. Form there is at least there the image is valid. So we don't expect any message there. For the image. The error message should be blank. Okay, let's run it. Good. Now we've written test for the format list. This is very, um. It's not the most complicated text in the world, but it gives you great idea, great foundation to finish it up. And you can use the same approach to write for valid marks. They are quite straightforward, actually. No dependencies. So you should be able to finish it up. And that will be it for this video. Like I said, I need to fix this my ID problem. And then we continue in the next video to start testing more. We are going to test most of the files here, especially this database operation. I want us to test it. All right. See you there.
