# Test The User Repository

In the last video, we set up the test database connection, which we want to start using now. So let's start with testing the repository. Um, don't worry about this, uh, squiggly line. It's trying to it's the IDE. This is an IDE problem, so let's ignore it for now. And, um, what I want to do is this. Come here. Let's let's start with the user repository. Let's start testing that. So I'm going to right click and say user repository underscore test. All right. And that's it. Let's start testing. So which what do we want to test. First let's test the create user method. First I'm going to say func test SQL User repository. This one. Let's test this one first. So I want to close this out so you can see very well. Can this thing just close? Okay, maybe I use this button. Okay, good. The database. Clean up. Let's divide clean up db. And then we pass T before. So you know how before our work right. So the repo itself let's create it new SQL user repository. And we have our testdb to pass to it. Good. So we have our repository. And honestly everything here is just like we have been doing in main in the handlers. So let's say user id. Is repo dot create user. You name uh John Doe. That is the most popular guy in the computer world John Doe. Com What else? Test password. And what else? Avatar. I think the line is too long, so I'm going to break it into New line. Be careful doing this. Then the comma must be here if I want to do this one as well. I can do it. But if I want to do this one here, I need to put a comma here, even if there is no other argument. So it's important, but it looks cleaner. So let's keep it like this. First we need to assert that we don't have any error. So assert error is nil. That is the first assertion. We also want to assert that the user id is greater than zero. So assert. Greater than zero. And that's a good test so far. What I'm going to do now I'm going to run the test using my ID. So I'm going to run it and it's a font was imported and never used. So here, let's clean it out, then go back to our test again in admin user underscore repository underscore test. Go and run it again and it passed. Don't forget if you can run it also from the command line by typing I want to go back to bash again because of the autocomplete I have here by typing go run sorry go test run. And we give it the name of the test that we want to run, which is this one, and it's the same experience. So if you want you can put the dash V as well to give you a verbose output. So you can do this as well. For me, running it from the ID is just for the sake of convenience. We have been doing it from the command line all along and it works both ways. So let's run it again. No matter how many times we run it, it's going to continue to work. So great. Now that we check for that, um, we can also verify that the user has created. But that's no need. Now if I, if we want this is I want to just if you want to go extreme case because you have access to these. If you want you can try to get this same user. So user error. Um yeah I think user error dot user by email. And then you try to get it. First of all error should be near and Users should not be near, and we want to make sure the two user ID matches and run it again. And you see it works perfectly well. Our test passes. With this I can try to run with coverage and show you how the coverage looks on the IDE. Of course it has some information here, but I want us to go to the code itself and see how it is. So open the repository file which is user underscore repository dot go. You see everywhere that is green. We covered you know um we just covered this. We covered everywhere. That is green. So mostly we've not covered the sort of the error parts in the code. And it can be tricky to cover because think about this, the skills right in here in order for, for example, in order for this to fail, the SQL must fail. But for here, let me show you one trick that we can use to cover this part. You see this generate password. If you pass it, let's open it up. It's in the Bcrypt package. Let's open it up. I'm going to click on it to open it up. You see that if the password is greater than 72 characters, it returns an error. That means we can pass a very large password. Um, let me see. Generate string. Uh, and and then I can say. Something like this. And then return it. And what we are going to do here, we can come here now. I'm going to do very similar thing. Paste it here. We just paste it here. Here is the password. Of course we need to change this to two. And then the password is we're going to make it super long. Let's give it 73. Something like this. And at this point error should this should be error. And you need user should be ni. Sorry ID should be zero equals I use id should be zero. We can change it like this. And let's run the code and see. And it passed. And let's run it with coverage now. And let me replace the active suit. Go into the code again. So let's open user underscore repository dot go. So I'm going to click on this one to open it and come and look at it. You see we've covered that part. So sometimes you need to tweak the code you need. And by going into some of the standard libraries you can find a way to tweak the code to make you have a higher percentage of tests. So basically, we've been able to cover this by inspecting the code from the standard library, using this path to force generate from password to return an error, which is basically it cannot handle more than 72 characters of string. So that is very, very, um, creative. And you can do more, right? You can try to find a way to see how, for example, how this exit can fail and all that. But most times it doesn't really matter because you just test the most important, um, part Parts for your code and that's it. You don't need to do anything tricky. You just do the most important part. And for that, we are going to write just one more test for this repository. Uh, and then we can stop, we can stop for this. So we can say for example func. So let's check for the duplicate email in case you've registered. And you want to register again, which I think is very important. So is going to be the same thing. But this time around it will be duplicate email where we do the defer we create it. And let me copy this first and paste it here. So we have this. The first one should be successful like this. The second one Shouldn't be successful, so we should receive an error. And honestly, we don't care about this. We make sure we receive an error that we cannot create it. And I'm going to run it. And it worked right. It worked. So that means we are preventing against creation of double account using the same email. And that works perfectly well. Okay. Um, if you want honestly, you can just test the rest of the method. They are quite straightforward to test. The most tricky part in testing is actually doing this setup that we did in setup underscore test, like setting up your dependencies and having access to them. And for this we've been able to use this same Testdb and SQLite to do the testing. And it works perfectly well. So thank you so much for watching. I will see you in the next video.
