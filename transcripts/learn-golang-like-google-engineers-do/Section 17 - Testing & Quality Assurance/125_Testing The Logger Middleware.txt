# Testing The Logger Middleware

Now that we have done the cleanup, let's start writing tests for our middleware. So I'm going to go here create a new file middleware middle West underscore test okay. What we want to test. First let's open middleware go I think we should write test for the logger first. So what do we want to know. We know that this logger is being used right. So I'm going to click on this usage to see look at where we're using it. So every request that we make it goes through this logger right. So let's now go back to the logger. So I'm in middleware that go and we are looking at the logger now logger middleware. That means ideally when we make the request this data should be written to the log. So the remote IP the remote address which is usually IP the protocol method. And this. But the thing is this. Sometimes this, this one changes a lot. So we don't really care about this. We just want to make sure that it has at least the basic data that we are looking for. So let's do it. How do we do it okay. So let's say func test logger. Is not showing uh application like this because it's sort of a hidden method. So but anyhow so test logger let's create a fake test handler. Test handler. And this is where go is interesting. So http func like this. So this is a handler that we are going to use. Whatever you write to it we can read it. So let's write header make sure it's 200 okay. So actually the default anyway we just write okay. And I'm just going to write okay. So this is our handler. We need the handler to use the logger. Right. That's why. Okay. Now we have a handler I can say handler test app. Remember we initialize the test app recently then logger. And then we pass it this. So this now becomes a real handler that we need to pass to our we need to call seven okay. Let's create a request. You can create test. Test actually. So this is a request. Whatever you want you can put on it I can say test. It doesn't matter. And then our response writer is like this. I'm going to use W so that it doesn't confuse you. But most code base uses this error. But I told you before is nothing but the HTTP response, right? So I'm going to use w. So HTTP test recorder. And honestly that's the this is the toughest part. We are done basically just to call handler dot server and pass it the response writer and the request so we can inspect the response writer. Now assert that we have okay. And then we want to also assert that the body uh dot string. Is equal. That what we got is, uh, like this. This is what we wrote to the body. Okay. What else do we want to check? But how do we check the log? Let's run it. Actually, I'm going to say run, and it passed. I want to run with coverage. And here's where it gets tricky. If I go to the middleware look, it's saying that we cover the logger 100%, which is somehow true, but we are not checking this. It doesn't care if we are checking this or not, but it's it noticed that we are calling it and it's a single path code. There is no if else statement that we have to touch it. We have to test the different branches. So it's saying that we covered everything. This is where test coverage can be deceiving. When someone says that hey, my code have 100% test coverage, are they really checking things? So this is where it gets interesting. For me. This is unacceptable and we have to do better. Now let's go and improve it. One of the problems we're going to have is when we set up the test application in setup underscore test go. This is it. We discarded the log. That means we don't have access to it but we can overwrite it. So let's go and do that. So go back to Middlewares dot sorry Middlewares underscore test go. And before you use before you use the test app, let's create a buffer and then test app.info log. And we are going to set it to this. So we are overwriting what we did in that setup. Now instead of using OS dot stdout let me type it correctly. Which can still work. This is going to print it to the screen. But if we put the buffer here the data is going to the buffer for us. And we can inspect the buffer here by saying assert. Assert equal. So we can say buffer dot string here. Of course it's not going to be quiet, but at least it gives us something you know, to look at. Now let's run it. And the test should fail. Now it failed because this is what we got from the, uh, this IP address and everything. And honestly, we don't care. We want to just check for this, that we this was in it. So we are going to say if the buffer a set contains a set that the buffer contains. And for this we need to switch things around because we are using contain. So we are saying that assert that the buffer that we have contains this test. And now we can run it. This won't change. The code coverage count is still 100%, but this is testing the real thing. And you can go to the logger, for instance. And uh, let's remove the method and remove one thing from here. And now the code. The test is going to fail. So let's go back to middlewares underscore test and run it. And now it failed. So this is testing the real thing. It's not the fake coverage test coverage that was reported earlier. So this is fantastic. And this is how you should write test. You should check for the real edge cases that you want to check for. I want to run it one more time to make sure we are agreeing. Um, I made I missed that, yeah. So we are back. I'm going to run it again. And we are agreeing. So I'm going to just clean out my coverage and the code is back clean. And that is how to test Middlewares. We are going to test some more middleware and let's see how we go from this. Thank you.
