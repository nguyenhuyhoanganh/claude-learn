# Adding Test Setup

Welcome back. So I managed to fix my ID problem. Let me show you what happened. You go to go to Goland here, at least on the Mac. So you go to the settings. And for me it was this this enable go modules integration was not checked. So for example if I take it out and I press apply okay. We are back to that problem right now. If I go back there settings and I go to under go I go to go modules. And then I click on enable go module app apply okay. And then it's back. Um I know because when I open the go module and I discovered that it was not recognizing it, that was how I just had the hint that it's probably the go module setting. So that is how I fix it. All right. Now we are back here. Something I have not mentioned about testing in go is what if? What if we want to do something before we run this test? Let's say we want to set up something like database connection or something. Well, go provide some more like the way you have main. Now here we can have a main for test. So let's go here and create um setup underscore test and put down main here. So I can say test mean but the signature is a little different. It takes this M which is testing to um is a pointer to testing dot m. That is what it takes. And it, it takes that because technically this is what runs the test. There is a run here m dot run. This is what is going to run the test. So you can do something before the test is executed. And you can do something after. So for us here, what we are going to do for all the test files. Let's create a yeah, let's create a test DB connection here. And then I'm going to come here and say var error and test db. This is what we just declared. And error equal SQL dot. Sqlite3. And we can use the in memory for this okay. And what else we can check for error here. If there is an error we have to panic. There is nothing you can do after that if there is an error here. And after that I want to run a database migration. So basically creating the tables and all that. So we are going to um, have to um, I'm going to put a function here shortly that creates that. So let me bring in a function that does that. So I have these two functions. They are not really interesting thing to write all over. Or C let me come here to make sure I'm not missing anything out. This is our table. So we can copy everything from here. Go here. Let's go to this test underscore set up underscore test dot go. And I'm going to paste it in here. So this is what we are going to all the time. We create this. And in the clean up I need to delete all these tables. So the way to do that it can be funny I think I don't see a better way. So I'm going to just duplicate this, just sort of duplicate it. So we delete users profile Five post comments to profile users. Post comments. Notes. What else? Comments. 12345. One. Two. Three. Four. Five. And that's it. Basically. It's not the cleanest thing because we don't have some sort of automatic migration happening. I'm going to just use it that way. All right. After the test, the connection to the database, we can, um, if error, we can try to ping the database. But I'm going to. Yeah. Let's ping the database. If we cannot ping we do it. Then I'm going to come here. Now if error equals setup test schema, pass the test database that we just created. If there is an error, we panic. If not, this run is going to return an integer which we can return to the operating system. Let me show you why. Any time you run a command, you always get, uh, the status code back, which you can access like this. For example, the last command that we executed was successful. If I do something, um, probably. Maybe I do go test now. Maybe it's going to fail, right? No, it's passing because we have done everything right. Let me do something to make it fail. Okay. And then I do it. Good. It failed now. And I try to get the last command. You see, it is now one because the last status code was not successful. So that the here in setup underscore test, we have access to that code here. And we must send it to the operating system in order for us to be consistent after that. uh, after the test finished running, we closed the database, and then we do OS dot exit with that code. So the question is this, uh, when can we call clean up? All right. We can as well do the clean up. Um, maybe we can do clean up here. Um, testing, I think. Uh, let me see. What did we pass? We just give it a test, okay? We can try to do the clean up in each of the tests so that we are, um. Yeah, let's let's hold off on the cleanup for now, because sometimes it gets really messy with these things, and, um, we can have a problem with it. So let's let's continue now, um, I'm going to run the test. And it is failing because we we changed something for sure. So we change here. We change here. Now let's fix it. We run the test. It's still passing. Let's open up underscore test dot go. And I want to show you something. I'll print out a message here. Uh, this is before the tests run. Um, form dot, this is after the test run. I think we need to put the dash V to see that message and look at it. This is before the test run, and this is after the test has run. You see that? It works exactly how we say it was going to work. Okay. So I'm going to remove this code. Remove this. And this is what we did in this video. Basically setting up our database connection so we can start testing the repositories. And let's start doing that from the next video. Bye for now.
