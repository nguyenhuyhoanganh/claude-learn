# Create A Test App Instance

Okay. Welcome back. Now that we have some tests for our repository, it is time for us to start thinking about the handlers. You know, look at the handlers and maybe the middlewares. But there is something very common between the handlers and middleware. Most they both belong to the application object. And if you go to Main.go before let me hide run hide coverage to make it cleaner. If you go to Main.go, you see we created the application instance here. We need something like this in test. So let's do it now let's go back to our setup underscore test and add a function called. Let me come up here first I can say test is a pointer to our application. All right. So I'm going to come down here and say funk setup application or setup app. Anyhow, you want to call it what we need. We probably going to need the DB and we need to return an application. I don't want to return any errors, just the application here. And what we can do actually after we have the database connection here, you can say test us and just do it like this. This would initialize it. All right. Now in here we can do the sessions I'll call it says session. Sessions or new. And I'm going to pass it bytes. Just for us to give it the secret key. Secret session key. Something like this. Very long. Maybe 32. By something like this, I don't know. But I think if I hope this one works, I think it should be 32 or something. But let's keep it like that and says that, uh, lifetime equals to maybe 24. That time doesn't really matter anyway. And let's return application here. Error log for this. Um, we can use a log dot. I think there is a. Log dot new. I think there is an OS dot discard or something. Or IO dot discard. Let me Let's say I dot. Can it work like that? Um. Okay. And let me see. There should be a way for us to discard it. So I'm going to look this up and then tell you in the next video. I don't want to print any, um, logs out, so. But let's do it for now since I can't remember it. Yeah. This is the format. And for the first time, this autocomplete helped. So it's actually iOS. Okay, I was right. Actually. I ordered this car. Then the prefix and the flag that are missing. Okay good. Then info log the same thing. I don't care about the log. So user repo new SQL user repo. Initialize it repo. Initialize it with the DB that we receive here. What else? Templates. This is the same thing templates Da and then public path like this. And session is set. I think that's about it. We have not we're not going to use it right away. But I just want us to set it up now. And I can run the test again to make sure that things are still working. Okay. Nothing is broken, and that's a good sign. So in this video basically we sort of initialize our app. So I created a test app and then it is initialized here. So this is it. And this is the function that we used to set it up. It's super similar to what we have in made. But instead of us being it in main this is for the test and that's it for the video. In the next video we start testing our middlewares.
