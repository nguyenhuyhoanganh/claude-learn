# Testing The Auth-Required Milddeware

Now let's go to Midwest test and write test for the authenticate middleware. Okay. So I'm going to come here. Probably can copy this one and paste. Maybe we can reuse some of the setup. I'm going to say um okay I think let me see. Let me check what we are. We should start. Um, so we have this authenticate and we have this. Let's start with this one is simpler. And then we can do this later. So require test require auth. Require auth. Maybe authenticated. So this is a success case okay. And for this we We have to, we can do two things. One is to let me think about it a little bit. What do we need? We need a protected endpoint. And. Yeah. Probably can do the same thing. So let's let's do a test handler. Test handler. Let's copy it from here. Let's put a test handler. There's nothing here I can just call it protected here. That's what we are sending out okay. Then we basically can call it uh for sure. We we are not going to do this. Let me make the request for. So this is a prepared request done. And the recorder. But the request this time around won't be just like this. We now need to do request. So this become request with context. And then contest without contest. Let me. Let me do it right in one context with all. Request context. And then I pass through. Okay. So with that, what I want to do with that function is to wrap. Um, basically let me show you I can just create it right here for a very simple function. It's a one liner, but can be we can we're going to need it to reuse it. So our context. And Ease or whatever we want. But I can just give it an interface. And it returns context. That's how we're able to wrap it. So what is going to happen is, is we have. We return context. The context this becomes the current context. Now then I need the context. This is context. Or key. And then it's basically a way for us to set it to true false true false in the in here. Okay. So this helps us to prepare the request. After we've prepared the request, the next thing we can just make the call right. We have the recorder here and then handler. So I can say handler. Blah. Let me make the handler here now so we can call the. Test app. Then it is authenticated. Authenticated is handler. Then we pass it the test handler. Okay. Let me see what I did wrong. Can I use test handlers of type handler functions of type requests? No, this is the wrong one. Require what? Sometimes when you record for too long, you start having, like, a fatigue. So I need to take a break. But yeah, after the handler, we just save it here and the rest is just regular checks. So of course this should be status. Okay. And here we should get this protected back if we had access. Because if we because we set it to true here, we should have access. If it is for we shouldn't have access. And then I think we set the no no cache header on the cache control. Something like that. Cache control. Let's run this. Good. It passed. We didn't make any mistake. So let me run with coverage and then we can see what we covered. So open middlewares go. And as you see we cover the happy part. So if not authenticated we haven't covered it because we covered the happy part. So let's cover this part as well. Which is basically we need a way to we need we can set it to false and then we can cover here. But the good thing is we need to check for the redirect. And the redirect is setting the header. So it's setting a location header. Um here look at what is setting so we can check for that headers. All right. Very simple stuffs okay. Let me close everything out I copy this and I'm going to paste and I will say not authenticated. This is still the protected URL. I don't want to change anything there. And at this point we can since we know for sure we don't. We don't even care about this actually. So I'm going to have my handler like this. I don't need to wrap the request, and if I want I can say the where you make the the call to quarter doesn't matter, actually. So you keep it like that. And then this one I'm going to say see order. Since we didn't set anything I think it should work fine. See order then this is going to be we need to know I think we set it to log in and the location header. That's what we are sending. We don't care about this. We are not going to see it because it's going to redirect. Let's check it. Run the test. And it failed. Yeah. Because this the redirect was wrong. So let's copy and paste it here. And that should make it now pass. Good. So we fix it I'm going to run it again. Run with coverage now and go to the. And you see we cover it good good good code Making a lot of effort. So I'm going to go here. Now I can right click and go to. There is a way to run the coverage from here as well and the bulk here here. So just that file and you see we have 54% coverage now. And it's not a baby coverage. It is real coverage. So we cover this cover this we cover this completely. And this is covered as well. This is covered because we are using it inside here. So we don't need to write a separate test for this. Um, the only one that is pending is this one. And I'm going to go ahead and write the test for it in the next video to cover it, because it has some really interesting things for us to do. So I want to make sure that, um, we have a test for this. Thank you for watching.
