# Testing The Authenticate Middleware

Okay. Welcome back. In order for us to test the authenticate middleware, we need to do a little bit more. And because I know there are some complexities involved, that is why, um, I make sure that we test it together. Okay. So let's start with the function itself. Uh, so we are going to test authenticate and then with valid session. And let's go ahead and do defer. Defer clean up. Data because we are going to use the database here. We need to do some setting up. We need the user in the database. We need the data in the session. We need to also run an internal middleware that the session library depends on. So it's a little bit of um uh complexity here. So let's start with the user ID and then test app dot user repo dot create. And we have done this many, many times. So session user is the name I'm going to give to it. And then session at test.com and password. Good. And then Everton. Okay. And that's it for the creation of the user. Let's make a little bit of a session here a session no error. And I want to make sure that we are set. Greater than zero. I don't want this wrapping. Okay. So far nothing strange. This is what we've been doing. In short, we can go ahead and run this test. And it's passing so far. Very good. Okay. Let's continue. Let's set up our test handler, which I can take from up here and bring it down here and make a little bit of modification. So inside the test handler we can check for a few things. We can Let's put the let's put something inside the session here. So I'm going to say test app. Now this is a trick in order for us to test app dot session dot put. And we request. The user key. And let's put this image in it. I want when we are done with the code I want to explain something to you. Because the way I'm testing this middleware, it's um, it's not like how we tested the other middlewares. And because of how the library this session library behaves, it has some setup that we need to do. Okay. So I'm going to put a data in session and just write. Uh we write okay. And that's it. We don't need to do anything. This I'm quite in short let's call it setup Handler. So we are using it to put data in session. And that's all for this one. Okay. Next step is we can copy this again and bring this one over and test handler. And in the test handler we can do a little bit of session. Now this is think about this like a when you authenticated okay. So I said true. So then test app. At least we know that test app dot authenticate is authentication should be true for sure. When you are here we need to pass a T here. Then what else we want to check. Let's get the user from session. So test app dot get user. So the user that we put in the context will now come. We can extract it there. We can check to make sure that that user matches the user that we created, that we also put in session. Okay. What else we want to check. We want to also check for the email. And we want to do a search. Let me duplicate this. This is the email that we have. Of course checking the ID already probably is enough. But just one more checks and we can w the right. Okay. And that's it. So this is the setup handler. And we got to run them differently. So this needs to run first. Okay. The next thing I want to do is to sort of change the request. So let's go here and say um I can call it setup chain and test app dot session enable And then we pass the setup handler. This one. So we are going to enable the session. And then when we execute this uh this chain then this handler will be also executed. And then we put the data and we can copy it from there into this phone. It's a trick but that is how we have to do it. So first let's make after that chain. Let's make a first request. And it doesn't really matter. You can call it anything you want to call it. Let's call it setup. And then W1 which is the recorder for this. And then let's call the serve. So now we are going to execute this and this. When these fires we are going to put message in session. Now that the message is in the session we can do something. Let's do something now. Now the the what we want to test. Let's call that one test chain. And test app. No, we don't need this anymore. Now we need to call. Um, we already tested it, so. Yes. So actually, let's let's enable it. But what I want to do this time around is we need to we are going to enable it. Then at the same time test app we call authenticate in there and then pass the test handler. We pass in this one. Now the reason why we still need it is because if we don't do it, we cannot, um, run the some of the code here. That depends on the session. So it's very important to enable. All right. After that we need the second request which honestly we can just grab like this and bring it here. I'm going to just call this one test. It's not that important. Now before we make the final request Let me do something. I want to copy cookies from the first one into the second one. So if cookies are w1. Dot result dot cookies if we have it right like this, I can say for I don't care about these cookies. Where where is the second request? Let's call it request to. Request two dot add cookie. And then we add the cookie there basically copying it into this request. Now that we have there we can make this is the second reader W2. We make a second reader. And then this test chain we run it serve. We are serving it with this um the response write a second response writer and the request to request. We copy the cookies from the first one and put it in the request to. That is how we can continue the flow after that. Honestly, nothing else remains and we can just check for data here and that's it. It's a little bit of code. Let's run it and see if it passes or not. And wonderful. This is wonderful. I want to run it one more time. Good. What? I've running it from the command line. Let's run everything. And all our tests are passing. Okay. Why did we have to do so much? Why did we take this route? Or first of all, calling this one and putting message in the queue. I want to show you something in the this library that we downloaded from the internet. I want to show you something that it does. If we follow, if you want to do it sequentially by first of all, creating the user, setting up this setup route, set up handler just like we've been doing. And then outside here you call that port and then you call. You make your call like this, and then you save. It's not going to work because until you call the handler dot save http, the middlewares have not been executed. That means even though you do it like this, the code is not running yet. You are only setting up the middleware to execute when you are calling the server. What that means is enable here has not gotten the chance to run, but you already call put but put depend on enable being executed for so I can show you. First of all let's go to put. If you go to put it's saying it's trying to grab something from context that is using internally. And if it can't find it in in the context this is very key here. It panicked. It can panic. Now if you go back, if you go back and you go to enable, if you click on enable. You see enable is checking here and enable is responsible for setting that key in the context. So that was something that I learned. And then from the code just by reading the code basically. And then I came up with this idea of copying the cookie. So it works really well and I love it. All right. Thank you so much for watching. I we haven't we won't have 100% coverage here yet, but I think you've learned so much that honestly testing this one is super easy. You don't even need to do much. And even this part, you don't need to do much. It's basically giving you the wrong user and all that. So, um, you can take that as an assignment. We've done the most complex part and then that's it for the video and that's it for testing. Middlewares we are going to write 1 or 2 tests for handlers, and we can say goodbye to testing, and we can start talking about another section of the course. Thank you for watching. Bye.
