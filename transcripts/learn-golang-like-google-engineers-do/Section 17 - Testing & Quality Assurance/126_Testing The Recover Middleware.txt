# Testing The Recover Middleware

Welcome back. In the last video, we tested the logger. Now I want us to test the recover, which is something that we used to recover from panic. So how do you test a code like that? It's actually not that difficult. Let's go to Middlewares underscore test go. And let's duplicate this function. Paste it. And I'm going to fold this one to give us some room. And instead of test logger we can give it test recover okay. All right. We still need a handler. But this time around I'm going to call it a panic handler. Because remember when I just when we discussed the handler when we discuss panic, I told you that because of how go routines work, when one go routine panics, you don't want to crash the application, you want to catch it and send something back to the browser. That is what the recover is doing for us. And now we want to test it. Okay, here we are. Of course we have to panic for real. And then this is a test panic. After the panic. I don't care about this buffer anymore because we are done with this. And what else? We can pass the panic handler here. This is going to panic. And then we need to put recover here. And recover should recover from that panic. We are going to make the request exactly as we did. But this time around this is going to be internal server error. That is what we expect. And definitely not nobody here what we want to check for. If what we want to check for, we can. The body is going to have some data, but I don't know exactly what is going to come because let me show you what I'm saying. This this is what we are printing, is basically going to have the panic as part of it. When I see it, then we can use contains to fix it. So let's run the code now. And internal server error. So as expected okay. But this was what was sent to us so I can put it here. So it's basically internal server error that was sent. Okay. There is a new line in what was sent. Good. So I thought was um, a not panic. Um recover. Going to recover. So we recover. We write this if we want. We can also check for. So the error here was actually internal server error. Okay. So if we want we can check for this header as well as because look it said this connection. Let's check for it. Um. I set dot equal um I think is it closed. What is it. And header w dot headers dot get connection. Let's check it. Okay. It's closed not closed. So that's correct now. Good. So if I go to the recover now and we don't send the header, for example. Let's see. Maybe the web server is sending it anyway. No, it doesn't send it. So it's a very good thing that we sent it and we are testing for it. Do it one more time. Good. You see how we have been able to test these two powerful middlewares recover and logger. And you can go here, run with coverage or run from the terminal. And it's going to run all the tests that we have here. And you can see the percentage of coverage that you have. For example, phones with 30% hyper is 32%. Our middleware is at 25% already. Because we've done logger, we've done recover. We haven't done require auth and authenticate. So um, for maybe we just do one of these and you can handle the rest. You are safe. But I think we should do one of these. So in the next video we'll pick one of these. And then we write test for. Thank you for watching. Bye.
