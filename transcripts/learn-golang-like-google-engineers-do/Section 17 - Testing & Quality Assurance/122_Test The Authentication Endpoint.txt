# Test The Authentication Endpoint

Okay, even though I had a plan to just move on from this repository. But I want to add one more test, uh, to for the authentication. It's a very important thing to test. So let's add a test for it. And we can call it a day for this. So test user repository authentication. Let's put sources here. And for this I want to copy over a little bit of uh the setting up like here. And maybe we copy this as well. Copy all these ones okay. You see that there's a lot of copying going on. That's how it is for test. So we have this setup now okay. With this setup we can try to log in the user. So I can say user ID error equals repo dot authenticate. Give it Uh the email journal.com and password is test password. Right. And what we want to check very simple stuff that no errors. The errors or you can there is also this no error. Can use that. And then you can use equal. Let's use assert equal to make sure that the user id matches. So assert dot equal. So this user ID that we got here match. Maybe here's what I'm going to do. This is let's name this one uh current user ID and or user ID. This is better more descriptive. So the current user ID is what we pop in here. We want to make sure that the current user ID we have. That is what we have that we want to check against. Matches what we got. Always remember that. Okay. And of course we already check for error here. So let's run it. And it worked. Then I'm going to just copy the whole thing here and paste it. And then change this to. Wrong password. The reason why the name of the test can be sometimes very long, is because it's also a documentation for someone that is new to your code base. To try to read them here, I'm going to just put a wrong password here in the authentication. And in short, let's type it out boldly. Wrong password. And for this we expect an error. We don't Um, really? We expect an error. We don't care about this one. Honestly, we only care about the error this time around. And here I want to do something. Since we have the error, we want to make sure that the error is this. I think it's invalid credential that we call the error. Right. Error. Invalid credential. And then we pass error here. Good. And let's run it. Everything passed. We can go to the terminal as well and run. I want to run all the test. Can we just put Dot good and everything run very well. No errors. If we check the return data, you see zero. So this is very, very good. Thanks for watching and I will see you in the next video.
