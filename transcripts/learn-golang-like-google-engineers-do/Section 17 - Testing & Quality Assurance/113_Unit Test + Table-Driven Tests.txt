# Unit Test + Table-Driven Tests

This is a brand new section that we are going to talk about testing. I consider testing as the most important aspect of software development. I don't play with it and I want to make sure you learn it very well. Now why we are going to do a lot in this section. I want to give you access to additional material. When you go to my YouTube channel. So YouTube slash ABBA Joseph, so is my son name and then my first name. But you can just search for my name like this. When you come to my channel, there is this video here that you can use to build complete API using test driven development. It went in really deep and it's almost about three hours. Of course that is completely free and it's available on YouTube. You see, I recorded it about a year ago and it has about, um, almost 13 K views here on YouTube. And the good thing about this video is it it teaches you the test driven approach, which is you write a test and you write a code. We are going to learn all that, um, in this section. But this is this has a lot. And if you don't mind, please come to the channel and then learn from there as well. All right. Let's go to go dev. On this page, if you go to Docs Standard Library and you search for testing, you should find this testing. And this is the library for testing. Everything is built in in go. We technically don't need an external library, but we are going to use one just to make our life easier. But you have everything right here. So in case you want to read up on it, you can read up on it. Especially like this false and a whole lot more that it offers. So let's go and start digging. This was where we left off last time. We no longer need a browser, so I'm going to come to our project. Create a new folder 14 section. Then in it I'm going to just create like a user package. And we can put user underscore test. Now that we're here let's write our first test. Test valid user name. And this is it. So this is the most basic signature of a test function in go. Every test function in go has the signature. So the test starts with test. And then it takes one parameter, which is a pointer to testing T, and that's it. If this is a valid test. So if I go to my. If I come to my terminal here and I run the test, you see it should pass. And that's it. The test passed because there is no failure message here. So it is great. What if we want to make it fail? Well, that is super easy. You just fail. And that's it. Then you run the test. You see, now it failed. So very straightforward to run the test. You type go test and the package that you want to run. If you want to run a specific file let me show you. Then you run that specific file. But there is a reason why I'm running the package instead of the file. I will show you shortly. So let's go ahead and make it pass again. Now it passed. So how do we check for code? Okay. Let's say let's say got. Username I'll check username. You see the function that we want to test I'm going to rename it now called check username. So this is going to be the name of the function. And we can pass it a username let's say test as a username. So if if the if what we got if it is not true we know that it's an error and that's it. There is no success message, so if it fails, you send out an error like this or nothing. So that's it. And I can run it again. Of course it's going to complain that we don't have check username which is fine. Now we need to go and create check username. So I'm going to add another file user dot go. And then let's copy the method or the function name. And what we want to check for. Just little thing here. Um if Len maybe if it's less than six. Or strings dot contain. Maybe if it contains admin or something we will just say it's not valid. Something like this. That's it. Now, if I run the code, we're still going to get the same error. You see why? Because we are running this specific file, and somehow the compiler hasn't found this one. So we need to run the package. And we got false. But the error message has changed. So this is our test failing. So let's go ahead and fix it. So if we go to our test it is failing because this the length is too short. So test 123. And now it passed. Congratulations. You have written your first test and it passed. For tests like this that maybe we check for this. Maybe we also want to check for that. This admin thing is, uh. It's like that. So ideally I can. This is supposed to be for. So if God not equal to false, I can say something like, uh. Wanted for false God. True. Then run it and it passed. But look at the repetition. So let's take this one from here and do and do something. I'm going to copy and paste it. And then I'm going to just name this table. Ideally it should be one you know. But I just want to keep this one around for you. So let's open this up. Here's what I'm going to do. I'm going to create um, something called test cases. So it's just a struct the name of the the test. So for for instance maybe string no admin whatever uh expect so input and then want what we want back and that's it. Right. So now let's, let's prefill our test. So the first one is this. So test one name is uh two short. And then we want force this one uh. Empty. We want force. This one contains admin admin user. We want false. This is valid. Great username. And here now we want true okay. So with this I can come here and save for. I don't care about the index, but I'm going to say test case and then range over the test and then pass the input and then check. So if what we got is not equal to what we want, we know that it's an error and that's it for that. So we go ahead and run the test. And the test passed. This is what in go. This is what they call us table. Table driven test. So this is table driven testing. Go. Basically this is like a table. And then these are rows that you have inside the table. And then you loop. Each row is a unique test. But you don't have to do this. Remember what we were doing here before that I had to copy and paste and check. Sometimes we have to do it anyway. But if you can use table driven tests, do table driven tests. It's quite good. All right. The way we run the test, we can have more information. For example, if I come here and I put that view, we see that it gives us more information and look at what is going on. Test check user. Um, in short, let me see something. What if the V is capital? Can it give us okay. It has to be. I think it has to be lowercase. Okay. You can also add the race flag. If you are testing concurrent application like this. It takes that introduce a little bit of latency. But yeah so we are not testing any concurrent application. We don't have problem with data race. So I don't care about that. Now you see what we have here? There is another way to do it. Something called subtext. So in order for us to do that, I'm going to copy and paste this one. Check username a table with subtext. And there is nothing. Don't let the name, you know, table driven test with subtext don't. Even though it sounds a little bit scary, but it's nothing. What? Instead of putting the code like this, here's why we have to do subtext before I change it to subtext, I want to show you something. Let's say instead of me just saying error, here I do t dot theta. So the it's going to stop. And now we run it. And I want to. I want one of the tests to fail. So I'm going to I'm going to make sure this one. Let's let's make it fail. Yeah. This this is going to make it I want to show you. Okay. You see that if you look at, uh, just just hold on a little bit. Let me. I want the output to be cleaner. So instead of calling this a test, I'm going to just. I won't run that test anymore. Even this. Okay. There is a way to filter the test, by the way. Um. And I'm going to just say something like this just to make sure we get out of it. So just to focus on this one. So I'm going to run it again and look at what happens. You say run test check username. So I run this one, but as soon as one of it failed, it didn't run the rest of the test again because we failed to hear. And this is where subtest is now important. Let's change it to subtest and see you come here and you say t dot run inside here and then you pass it this signature. So I'm passing it the name. And then it takes a callback. And that becomes the test that you are going to do. So basically the inner test here now is going to sort of shadow this one. And now this is inner test. Now let's run it to see the difference. You see that even though one of them fatal, one of them actually fatal, but the rest still ran. This is why subtest is super important. If you want that kind of behavior where even the one of them fatal, you still want to run the other ones, then subtest is a way to go. So that is why we do subtest. So subtest is super easy. You just do test run to run. Give it a name and the name that we give it. So if you come here now you see the name that we give. It has been added to for example the first one test table with subtest, then two short empty. The empty is the one that is failing. You look at it is the one that is failing and then this one contains admin is valid. The names have not been prefixed to all of them. So we can go ahead and remove that and run the test again. And great. If you take out the V that we put in the, the flag here is we're not going to get that large output. So if you need that kind of output you have to include the V. And now that you are learning you should always have the V on so that you can see what you are running. Okay, that makes it super, super great. So I'm going to come back and restore what we did here, bring back our test. Bring back our test here. And then. I think test check something like this okay. Everything looks great. And then I'm going to run it one more time and everything is still passing. Good. Now that we have learned the basic of testing, you know how to write basic test how to change it to a table driven test, how to go from table driven test to using subtest. In the next video we are going to continue with testing more advanced stuff. So see you there.
