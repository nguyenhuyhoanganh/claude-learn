# How To Test Confusing Code

In this video, I want us to deal with how to test confusing functions. So let's start with the new file and I'm going to say greeting. I'll give it a prefix name string and then print it out. And we also need to have the prefix as part of the print. How do you test this? This is confusing because number one this function returns nothing is printing to the console. And when you want to test it, that means there is nothing returned to you that you can check. That's a problem. Well, we have two choices here. One is we can be very creative and try to find a way around it. The second option is to refactor it. I think we should do both. All right, let's start. So how do we test for this? I think, um, if you imagine you are in an interview and the interviewer asks you, how do you test this function? Of course, you can tell the interviewer that, well, this is a bad code. If you want to test it, we can refactor it or whatever, right? You know, there are many ways you can test it, but sometimes you just want to take a step back and ask yourself, what exactly do I want to know about the code? Well, we want to know that when you call this function hello, followed by the prefix, followed by the name, and then um, this uh, letter and the new line is printed. That's it. That's all we want to check. It's very sequential. So basically the string match exactly what we are expecting. And right now we have a problem that this format printf is printing to standard output. So it's going to this OS dot stdout. So the first thing I want to do, one of the first creative thing I like to do is to maybe we can take over sort of hijacking. They always stood out creatively and use that. So let's go ahead and do it. So I'm going to keep the test, the code that we are testing and the test in the same file for now. And I can say test greeting the same signature. So let's let's get the original. Original study out and then set it to the this is the original one. Then I'm going to use a pipe. And this um what do you call it? The autocomplete is quite smart. He knows exactly what I'm driving at. There is an error check here that we need to do if error is not me. So what I'm going to do is to use the assert no error so that we can start getting used to the assert now because we need. When you call form dot printf, now that we are using, the pipe is going to write into the um, it's going to go into the writer. So we are going to set. Oh, as to that. So when you set this so our writer is now where the data is going to go. Now we need a way to read from it because they are connected. So using a pipe. So the writer and the reader is connected. So if you have something here you can read it from here. So great. Now after that basically if we want to when we finish running this I like to restore it back. If not it's going to affect other tests. So I don't want to forget that at all. It's very important each time I use this technique I do this the far, very, very early here so that I don't forget. And then I just do it like this. This is going to make sure that by the time we are returning from this function, we restore the OS dot stdout back to the original, um, sort of pointer or descriptor, whatever it is. Now, if we call greeting and we say Mr. Joseph is going to print. Now, we can read from here, you know. So I'm going to close the reader. And now we need the buffer. I had I want to have a different kind of buffer. Strings dot builder. Then I copy everything. I need the arrow like this. Good. So basically we're just going to copy from here into the buffer that we have established. And we check for the error using assert. And that's it for that. Now in order for us to check let me say what I wanted or want. This is what we want we to be. Hello, Mr.. Joseph. And then I think like this. This is what we intend to get. None got. Basically, we turn the buffer into the string, and then we can check assert equal. And that's it. Here we don't have to do it like this. So this is it. Now, moment of truth. Let's run the test and see. And it passed. Do you see the trouble we went through just to test this? So how do we know it's working? Let's take out hello from it, for instance. And now it failed. That means, you see, it has a very good output for us to see here. It's saying that. And this is why I also love this. Assert it say not equal expected. This is what we are expecting. Hello Mr. Joseph. This is what we are expecting. Actually this is what we got that it is just Mr. Joseph and then a new line that we got and then it shows a diff here for us in case we want to fix it. So it's very descriptive, very detailed. Now we can go and restore it. And the code is now passed. Okay, as you can see, it is very, um, a little bit technical because of all the pipe that we are using. But if you think about the pipe thing, you see that the reader and the writer are both files that are sort of connected to each other. So if you put something here, you can read it from here. You know, basically it's a connected pipe. So it's very powerful feature that God provides for us. And you can use it to do a whole lot. Okay. Now this is one way to do this thing. How about we refactor the code? Since the code is the same I'm going to create another version here so we don't break the first test. So this is the second one. And then I'm going to say test two. What if we refactor it. Maybe instead of here instead of printing like this. What if we say uh out os.io Io dot write. Something like this io dot writer. And then we can say dot right? Maybe something like this okay. And we take it. So that is a buffer now. Right. So we I can remove everything here. And what if we come here call it and we pass it the buffer. And then everything like this and every other thing remain the same. So let's run the code now and see. And you see that it passed. So look at what we did. But I'm able to do this for one reason because. This string builder implements Io.writer brighter and our function can accept it. If we want to print it to the console as well, we can just do osd dot osd stdout, which also implements Io.writer. So by accepting the generic interface here that we just call write, we make the function super flexible. This is easier to manage than the first version, but like I said, if you are in an interview and you demonstrate these two sort of solutions and explain it clearly, honestly, the interviewer will be very happy with you. It it shows two things. One is, you know, the language. The second one is, you know, by technical trade offs which of it you want to use. You can decide based on maybe what your your team preferences and all that. But for sure this is like there are multiple ways to do this thing and this is very, very good. So that will be it for this video. Thanks for watching and I will see you in the next one.
