# Using The Assert Library To Simplify Assertions

In the last video, we started learning how to do testing in go. And because testing is something you do for production, I want to make sure you have the right tools as you are learning. I don't want to just show you a little part of it. Then you know, you start struggling when you want to do it in production. Okay, so what I want to do now is to go to the browser and we need to download this library. Here is the problem this library solves for us two problems for me two major problems. One is smoking and the second one is access. So let me show you instead of I don't like this if statement that we have. Ideally I would have loved to say assert assert that, you know, um, maybe what do you call assert equals? Like this that what we got is and you can add message. You can add you can add this message that we have here that, uh, what we have here, you can add it. Okay. This is, this is what we want. Simple. More straightforward. And you see, it's already importing the library because I have it downloaded before. But let's go to the browser. Let me show you. If you scroll down, it's super easy. Just like every go library, you just copy it and then you can do go get with it. Um, it has a mock. We're going to take a look at the mock later. How to do mocking. It has something. It has an assert and it has a require. I always use the assert. I don't use the require. So if you want to install it, look at it. You come here go get. So I'm going to the terminal. I copied it and then installed. That's it. You don't need to change anything in your code again. You can still run the test. So let's see why it fails. Missing some okay. Let me. I want to go mod. Let's see. Go mod tidy and see if it gets tidy up for us. Then run the test. And now it works great. So we went from instead of us doing this, let's convert this as well. So we expect this to be true. It's easy as that. True. So if you want there is a cert true like this. And you don't need the true here anymore. You just pass the gut and you can do away with this. In short, with that, you don't need the guts. You can put it like this. You see, it gets very, very short. What else? Here we are doing the same thing. Assert equal. Got one. There is also assert same, which you can take a look. What argument must be. Pointer variables. Sameness is determined based on the equality of both type and value. So actually this is more strict. And I like this. And what else I think that's about it for for this let's now run the test one more time to make sure we are still passing. Something is failing. Now see both argument must be pointed. So okay, since we didn't pass a pointer, we can go back to assert equal And then let's run it one more time. Got it? None passed. Okay, beautiful. We are going to use the assert library a lot. It has some really, really nice methods like this equals. It has. Let's let me give you a demo of one. And let's say. Let's say login. This is a simple message. So this will return error and boolean something like this. If username if is a check username is one. I'm going to return like this. And here I'm going to return true. So here's where we're going to test this now. You see we have error and we have boolean. I'm doing this just to show you a little bit of data set. We can come here and say test login okay. And then we see this is the error we're going to get back. And this is maybe if it is okay and then login. And we pass it test as the username. Let's see why. Not enough argument. Why. Okay there is a password here. Honestly I don't care about the password. And then that's it. Now with the assert you can do assert no error first no error. So that if the message is not required you can just take it out. Like I said, that there is no error you can Assert that, uh, this okay is for is true or false, but for this we know that the username is too short. So what? I'm going to keep it like this. So we see how it fails. You see it failed. Now let's go and fix it. To receive invalid username should be true. So this failed. Now let's fix it. Um test username which is valid. So I set no error and then assert okay should be true. Now when you run it all our tests should pass and it passed. Indeed. So these are two different methods that we've looked at as well. And there is also a set contains assert not nil assert nil. Things like that. We are going to learn how to use everything about the assert library. Not everything, but most things. So yes, thank you for watching and I will see you in the next video.
