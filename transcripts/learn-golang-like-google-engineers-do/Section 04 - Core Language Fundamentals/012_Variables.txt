# Variables

Now that we have seen some values, let's see how to store them in variables. A variable is a named storage location for a value. In go, variables must be declared before their can be used. Now there are several ways to declare a variable in go. We can use the var keyword or use the short variable declaration approach. This is where we left off in the last video. I'm going to delete everything that we've done before and let's start learning how to declare variables in go is quite easy if you are coming from Java. The type is on the other side. You know, in Java for example, the type is on this side in go. So, um, just forgive me if you don't know Java, but in Java you said string name for instance. So the type is on the left side. But in go the type is on the right side. I think, um, most programming languages that I know that does type in the type is usually on this side, but in go is on the right side. So to declare a variable in go using the var keyword, you type var. The name of the variable, for instance greeting and then the type. This is declaration. It hasn't been initialized yet. Now what does that mean when you declare a variable and it hasn't been initialized? That means the memory hasn't been allocated for it, but it depends. The compiler can choose to allocate memory and give it a default value. In go, something called zero value is assigned to your variable that hasn't been initialized. So for string the zero value. The zero value is an empty string. This is important. This keyword zero value. You can pop it on Google or something and ask what's a zero values in go? When you declare a string, this one you haven't. Initializing the zero value is an on empty string. So this is an a declaration. And in order to initialize it you can do it this way. Hello world. So this is declaration. This is initialization or initialized. Basically we now allocated memory. And this variable now is pointing to this location of data in memory. All right. Now we can also print it out this way. And let me open my I'm going to open my terminal here. Let's keep using go run. The data is in. Remember section one. Let me open here. So one section. That is it. And then we have the main dot go there and run it. And you see we have hello world. So close that side. Now this is a string. We also can declare var count for instance an integer and set count to maybe Pen and print it out and I'll run it again. This is how to declare a string. We can declare boolean. Let's say is running and var is running bool and we can set it to true. So and then print it out. I'm going to explain something to you soon. And that's it. We print it out. What else. We can also declare multiple variables on the same line. So let's say var first name last name you see. And then each of them can be assigned separately like this. And we can print them both out. You see now this the approach that we've seen here, these are the standard sort of long variable declaration approach. There is a shorter version of this that we can do, for instance, um, email, let's say email test at test comm and we can print it out. So this colon equals is a short variable declaration and initialization at the same time. Each time you have a variable you want to declare and initialize at the same time, this is a preferred option instead of doing it. These two steps. The reason why this is is of course is shorter, obviously, but not just that you are able to initialize it. And of course you are. You also eliminate the type. You see, we are not saying string here because what is going to happen? The go compiler is going to infer the type from the right side. It can know that of course this is a string, isn't a quote, you know. And we can do, for example, age 24 and print it and you know, it works. Go is going to know that this is an int. But sometimes you also want to take control of the data type. That is, when you up, you go for this, uh, this approach. Um, let me close the terminal a little bit. Um, come here and I'll show you, let's say here, for instance, it is possible to say var e int equal 2025. You see and you print it, look at what is going on is saying that this is redundant. Our ID is telling us that INT here is redundant, because the compiler is going to know already that this is an integer. So it is not important to provide the type all the time, especially when the type can be inferred by the compiler. So these are the basic um goal, uh, mostly primitive types. These are not complex types. Here we are going to look at more complex types later like arrays slices and many, many more. So see you in the next video and we'll talk about constants.
