# Enums

Hello and welcome back to the video. In the last video we looked at constants. In this video I want us to look at something called enums or enumerations. I'm going to create another folder here called three. Section three. Section enum I'm going to rename it to, you know, three e norm something like this or enums. Right. And let's create a new file inside main dot go I'm keeping the file separately. So in case you want to pull them from GitHub you can download them from GitHub. All right. So go doesn't have an enum keyword like some other languages like Java, C or C++. However it provides a mechanism to create enumerated constants easily using the Iota identifier. So in go, when you want to create a constant as an enum, let's say const I showed you before that we can group your constant this way. So let's say Sunday equals iota. And so what happened? As you can see from the IDE by default go is going to assign this zero right. And then Monday. Tuesday Wednesday Thursday Friday Saturday. And you see each of them has this value. So this becomes the enums. And you can print them out. Let's let's let's print them out. Let's say println Sunday. I can Monday Tuesday Wednesday Thursday Friday Saturday okay. Let's open the I'm going to open here now three enum and may not go okay. You see 0 to 6. six. So this is the basic way to do enums in go. Um, you can what if we want the numbers to start from one instead of zero? Well you can come here and add it. Add something to it. You see I added one to this. So it's like zero plus one. Then every other thing starts counting from that. So that is how you do basic enums in go. Uh, let me give you another example that would be more practical. For example, let's say const I want to do um, we have not talked about defining our own type. But let me give you a peek ahead of what is coming. Let's say type log level. And it's of type int. And then I can do const now. Const okay. Sometimes the ideas are too smart. So let's say error. Um, we can start. This is a log error level and then maybe warm low info debug something like this, maybe theta or something log for something like this. You see we also have from zero down. Something like this can be really practical when we start talking about, um, how to use, uh, enums and to structure our code a little bit better. So this is how you do enumerations in go. Um, you don't have we don't have enum keyword like Java. You can do a little bit of validation. For example, having this type defined this way helps you to, um, compare the types easily. And we can look we're going to look at that later. So thank you for watching. I'll see you in the next video where we are going to work on our first go project. See you there.
