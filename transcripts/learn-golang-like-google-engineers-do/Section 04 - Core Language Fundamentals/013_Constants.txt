# Constants

After variables, we have constants. As the name suggests, a constant is a value that, once defined, cannot be changed during the program execution. This is very important. In go, constants are declared using const keyword. There can be character, string, boolean, or numeric values. Let's look at some example constants. So this is the video. Sorry. This is the program from the last video. I am going to um, I think I should create a new, um let's create a new one here. I think one way to arrange this is um, instead of just section one, let me do it. Like, say, just give me a minute. Let me rearrange this a little bit. So zero one. So one of these will be variables. Variables. My spelling is where variables. Okay. And then we put this one in here. Then we can create another one and two constants. Then in it we can create a main dot. Go. Good. Now let's look at some constants. Constants can be declared um almost anywhere. So we can declare it here. For example um let's say host string. Um. Localhost for now. And I can come here. And print it out. So this is a constant. Let's open the, the terminal I, I am executing the program in the terminal all the time because of platform compatibility. I don't want to use the IDE. And then some people, um, some people start having different experience. If not, I could have as well just click on this button. So if you have this feature on your ID, please go ahead and click on this button. So I'm going to run. Go. Let's look at the path run go go run. Um one section and then two. And we mean so this is the program. You see we have localhost. Okay. Let's close this part. Now this is a constant. It cannot be changed if we try to say k equals something else. This is going this is an error. Let's look at it. And good. At least you have seen your first error. So declared. And you see let's say um. Yeah something like this. Let us pretend that we want to concatenate and you see cannot assign to host neither addressable nor in mapping this. Something like that. It means we cannot change the value. You can the constants. Let's have a. Let's declare some constant here as well. So const let's say app name and go let's print it out. Um const pi for instance is float 64 something like this. Print it out. And what else we can. Let's look at some more data type. For instance var sorry const say rate 32 for instance. And I don't want to just say 5.1 or some point two something like this. And then we print it out. So this are the are constants. Okay I'm going to run it again. You see everything is printed out nicely. So basically that's all about constants.
