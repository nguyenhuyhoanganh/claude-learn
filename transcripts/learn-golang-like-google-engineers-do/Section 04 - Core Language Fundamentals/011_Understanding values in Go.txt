# Understanding values in Go

In this video I want us to talk about some values in go. We're going to talk about strings. Integers float many many things. All right. So because we have started the course officially I'm going to create open my IDE. This is Golang. You can use Visual Studio Code if you wish. And I will create a directory here called Running Go. Open it and then expand my IDE. This is the beginning of the journey and there is something here called Go mod. Please ignore it for now. We are going to update it later. But here I'm going to create a folder. I'm going to call it um section. One. I'm going to put one section because I want the I want just the folders to be to be sorted. And I will create a file here called um value. So okay we can create a main inside. Actually, let me close this out. Open everything. Let me check if copilot is on. I'm going to disable it because I don't want the distraction now. Welcome to welcome officially to your go programming course. So this is the first official video where we talk about some atoms, some of the values that we have in go in go. We have um strings. We have numbers, floats, many things. So let's look at some of them for now. The what I want to import for you. We have used this before. Don't worry. We'll talk about import later. This squiggly line that you are seeing simply means we imported the file, but we've not used it. So in go, when you import a library, you must use it. All right. So let's go I'm going to use print f and like this. So this is the same Hello World program. I'm going to open my terminal here. So this is my terminal right down here. And we should be able to type go run. The file is in one section. And we run it this way. All right. So what I'm what am I going to do in go. This is a string. What we have here is the regular string. And of course you can concatenate them for example hello plus word for instance. We can concatenate it that way I run it again. Still looks good. We can put this new line character to print it. So this is a string in go I'm going to duplicate this line to print number is very easy. I can use println to print new line at the end of it. Numbers are also very easy. Let's say one plus one, for instance, which is two. That is a number. This is an int in go. I'm going to duplicate the line again. We have booleans which we have true. That is true or false for instance. Okay. And you can print that out as well. You say true or false. So these are boolean. We also have um other data types like let me use printf like an array or slices as an example. I'm going to use this printing format. And I put something like this. And then we print some numbers. So this is something called slices. We'll talk about it later. Uh Santa is just a fancy array. It's an extendable array. We look at it later. What else do we have? Um, we have, uh, something called interface, which is nothing. Maybe. Let's print it like this in is, uh, let me create a type of it for now. T is nothing, and. Z is nil. Right. So this is a new type. So these are really the basic type. There is also float. Let me let's not forget float uh let's say 3.1. Four. Something like this. We also have floats you see. So these are the basic values real data that we have. We are going to combine them to do great stuff. In the next video we will look at more complex types with we start talking about variables. So see you in the next video where we talk about variables.
