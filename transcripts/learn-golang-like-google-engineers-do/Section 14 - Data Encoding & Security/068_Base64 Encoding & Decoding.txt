# Base64 Encoding & Decoding

Welcome back. Before we wrap up this section, I want to show one more example which is base64 encoding. Base64 encoding is super important when you're developing an application. So I think base64 encoding. And then we put in main dot go in here. Beautiful. Now the encoding package has so much for us. Aside from what we have seen we have seen JSON. But there is so much more we can do. So let's start with something. Um, let's start with some kind of data. Welcome to the wonderful. Okay, so this is my text. How do I transform this to b64 encoded string. Okay. Encoded. And just like everything in go, this base64 dot std encoding dot encode to string. Takes a slice of byte and each time a function takes a slice of byte. You can always wrap it like this. Another way is for you to pass the bytes directly. Which I can show you later, but this is how most people do it. And we should print it out. And after that we can run it and see. Let's make sure we know where we are. 11 five. Main.go. And this is the base64 encoded string. I'm going to copy this. This is the encoded string. Encoded string. Okay, I won't print it. Okay. We can just keep the printing. But this is the encoded string. We can try to decode it as well. And decoding is the same thing. It's like almost the same signature. I can come here and do the decode string. And I say I'm going to say encoding decode string. And you see that it takes a string. So we have to take this one out and pass it the string. Okay. Let me make sure that I get the returns an error as well. With that we can check for the error. And now we should have this same data. So let's check for equality. If it's not, then we know that the encoding failed. If they are not equal, then the encoding errors we can just print out. Uh, it doesn't really matter what we print here, but we can run it now. You see the same data. We didn't get this message. That means it is correct. If I put something else, let's say I put, uh, you hear something to make it invalid. And you see, decoder string does not match encoded data. See? And that's base64 encoding. Uh, before I wrap it up, I want to show you something. Let's say, you know, this takes, um, a slice of byte. This is something you can do, let's say raw data. This is slice of byte. Instead of me doing this. I'm not wrapping it. I want to construct the hexadecimal myself. So you can type some hexadecimal by yourself here. Let's say D whatever you can come up with. And so this is raw data. Now you can put raw hexadecimal and. Um, and uh and just put some more data C or something. Then we, we print it out. So this is raw data in case you are dealing with, um, uh, maybe some kind of audio file, whatever you're dealing with. Um. You see, with the same thing now that we had here. And now we can pass the raw data. You see, this time around, we are no longer wrapping it because we have the raw data, and you don't need to wrap it in like this. And that's it. With this, I can replace this with and then print it out. And this is the base64. We can copy it out. And 64 string or something like this. And we can decode it. The decoding is like this. So we can copy this over paste this here. We don't no longer can do this. Then print it out I want to print it out instead of, uh. And this is a decoder string print. You see, because of the distance. No problem. Because of the same hex that we type here, it's binary data that is not really printable, but it's, um, you can if you want, you can check for the value directly. Um. This string like this is not going to work because of how, uh, the binary data is. So this is cannot you can compare it to remember when we printed it out, it's um, it's a little bit gibberish like, uh, unprintable characters. So if you want, you can go on the internet, construct some, maybe turn something like this into this encoded, for instance, into Eczema, and then you can pop that in here and all that. But the takeaway is that you can construct your hexadecimal or your bytes directly. Then the number here doesn't have to be in hex. It can be in, um, in regular, uh, decimal or whatever number that you want to put. Um, it can be in base ten instead of, uh, this. So, uh, that's how it works. Um, just quick recap. Encoding is very straightforward. You call the encode to string and you pass it the slice of byte decoding, you pass it the string. And if you want to work with raw data, you can construct your byte and pass it in. And that's it for base64 encoding. We are going to use it a lot with um when we are working with URL and everything. So see you in the next section of the course, uh, where things start getting more interesting and we start doing database.
