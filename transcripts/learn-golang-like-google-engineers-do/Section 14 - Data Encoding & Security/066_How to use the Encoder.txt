# How to use the Encoder

Even though we can use the marshal and marshal function. There is another way. I want to show you that by going to the folder. And then let's create another one. I'm going to call it encode something like this encode and I would create a main dot. Go inside. This is going to be very small but it's also very good. Let's go here and copy the struct. I want to copy this. It's good. And I paste it here I think that we don't we no longer need most of these things okay. And let's start with um creating a user. Let's add some random data. I think this is even enough. If you want, you can put this active. It's true. Okay. Now how what what do you do as before. If before. At this stage, we use the masher to turn it into JSON. All right. So here is the difference. So here I'm going to say enc for JSON dot new encoder. Now if you notice we pass OS dot stdout which is an output which is a writer. So this takes an interface. So it takes io.writer. That means you can specify where you want your data to be written to. And this is the difference between the format Marcia Marcia give you the data as bytes. This one, you can pass it a destination, and your destination can also be like in a buffer. You can write this to a web response. It is way, way more flexible and even for I don't know the difference in performance, but this is way, way more flexible and you can easily put it anywhere you like. So I am going to now call the encode on it. So if error we use this format. If not we just log theta. And at this point you don't need to do anything anymore because your data is already in the standard output. So let's run it. Um, the data will be printed in the standard output. You can if you don't want the standard output, you can use a different, uh, buffer. Buffer buffer. New string buffer. And then pass it. It's going to be there. But of course because this is a buffer you need to print the buffer now. I think yeah that's it. If the most important thing most important difference is just the flexibility that it provides, which is basically when you take an encoder, you need to give it a destination where you want to encode the data to. And that's the difference. So it's basic. I want to take it back to using the stdout. And that's it. All right. Thanks for watching this one. Let's do the second version. Um sorry. The the opposite, which is to decode in the next video.
