# Marshalling Data

When you build APIs in go, you need a way to encode or decode some data like JSON, XML or whatever data that you want in go. It's actually super easy to transform your data to JSON or from JSON to your struct. What I'm going to do is to create a new section. And in this one, let's start with it. Let's use a go language called marshalling. So go instead of encoding and decoding go use marshalling and or marshalling. When you're learning a language, try to use the same kind of terminologies that they use. All right. Let's say let's create a struct called user and give it a name. Give it age. Maybe phone. I can make it a string and I can say it's active. Okay. Now let's create. Um. Let's make it like this. And a phone. Something like this. How do we turn this into a JSON? Like I said, super easy. Something. You can let me write it out like this. Bytes or byte slice and then slash masher. And you pass the data that you want to masher. Um, before I, before I continue, I want to click on this, uh, martial function to show you the signature a little bit. Look at it. It takes any. That means it can almost on Marcia or Marcia anything. I think the second the counterpart is also like that. So it's good check for error. And if there is an error with theta, of course, if you are in a function, maybe you want to return the error instead of theta over here in mean we can use the log dot theta. And then you have the return type is a slice of bytes. And you can turn the slice of byte by wrapping it around a string function to turn it into a string. So we can do this like this. You see, we use this to turn it into a string. Now let's run it. Okay I can this was the. Go run section ten that we did. And one more. Let's see what we have here. 11, actually. 11. One. Marshalling and main dot. Go. And you see. Beautiful. So we have the name, age. Phone number is active. Is Boolean. That's it. Okay. Before we do another example, let's take care of some things here a little bit. We have the JSON name is almost like this. Like is active another what if you want it in a different format okay. Here in the struct you can add something called JSON tag. So you see it's you use this Backtick. And then the name of the tag which is um sorry like the tag group, uh, JSON for instance. And then the name of the tag, you can do the same thing here. We can do the same thing here. We can do the same thing here if you like. whatever you like this time around. In short, you can. You can name it like this. You can give this one full name or whatever you put there. Okay, let's run it now. You see name, age, phone, active, whatever you want to name it, you can name it here. You can have multiple tags. For example you can have XML as well. You can have multiple tags. But the XML doesn't affect the output of JSON. I can name this one uh one number instead. And even though I type it phone number in the XML tag, it doesn't affect the JSON. So that's basic. Uh, that is the basic. The output can be a little bit, uh, more beautiful. So instead of this masher Sure I can change it to partial indent. And then I pass how I want the indentation to work. I also put some space and then I run it and look at it. If you can, you can. If you want some prefix, you can. Let's say I put dash. You see of course I don't want the prefix, but I just wanted to show you that. And this, uh, martial indent gives you, it does some formatting for you, but from a processing standpoint, it's a little bit more expensive than the masher. If you don't need it to be readable, please always use the masher one. It is cheaper from a computational standpoint compared to the, um, martial indent. Okay. And that's the basic of marshalling turning your data into JSON in go. Thank you for watching and I will see you in the next video.
