# Custom type with Structs

Hello and welcome back. In this section we are going to discuss some complex data types in go. And from here we are going to start creating our own custom types. In the video you have seen it before. Let me scroll to the top. You've seen this keyword before that I've used struct, right? That's what we're going to learn in this video a little bit more in go. We don't have classes. So the struct is the short word for structures. This is how we group related fields together. And it is very powerful. This is something you are going to use all the time. It's like using classes in Java. Everything in Java is a class in go. Most teams are structs. So I'm going to create a new section called five section. And in it I will create a folder one struct, then main dot go. Okay. Let's start by creating a type called employee. Which is struct. Employee is a custom type that we can add custom fields. So it's like grouping a type. It's now a complex type right. And we can have first name last name maybe the position of the employee the salary. And if it's active or not. The reason I am using all this is to show you that we can use different types. We can even joined at and then time, the time which is coming from the standard library. When you have a struct like this, you can create a value type. You can create a type of it. So this is an employee we can create. Let's say that is a type of employee. Now go is not really an object oriented programming. That means the idea of an instance is something that sometimes we use. But that is not really true because when you create, you are just creating a value of this. This is a structure that you've defined and you can create a concrete value of it. So this is a real data. All right let's see. Let's say Jane and we assign an ID Jane Doe position whatever. And that's it. Now this is a type value type concrete type that we've refused and we can print it out. I'm going to open my terminal. We now go to the section that we just created it. Is it 8 or 7? Let's see. No, it says section five and one. Okay. Struct the me. We print it and look at the data. If you want to print it to look better we can come here and do printf. And we'll do percentage v like this. Print it. Look. Doesn't look much different. Let me add a plus in between here. And run it. Now this is better I think I should put a new line. Okay look you see the ID and everything they join. That is quite long, you know, Salary. Everything all right? So. You can let me delete this. You can print individual fields like gender ID so it uses dot notation. I'm glad I chose that. In C if you are coming from C this is how it is in Java is dot notation in PHP is something like this I think. Um. And you can use first name, last name just like that, you know, and then run it. You see, that is how you access the fields inside the struct. Sometimes when you see a struct like this, let me scroll, let me go back up. You can create a function called new Employee. And the new employee can return to you can return it as a pointer, or return it. Return the concrete value itself so you can come here now and fill uh, let's say id int. Uh first name. Last name. Position. They are all string is active is bull and joined at. I'm going to skip this one and just use noun. Let's fill it out. Join at none. Then we come. You can come here now and create. Uh. Turn. Let's say Joe new employee, you see. And then that's it. You can print Joe. This is more like a constructor function or a, a factory function that you use to create, uh, the type of employee. Let's run it. You see, it works great. You can as well update the field yourself. So I can now say, Joe, the salary, maybe, uh, something like this, I, I wish, and then I run it. This time around, I'm going to say, Joe, the salary. And you see this much salaries. I don't know if you earned this much. Please DM me. I would like to like to partner with you. All right, so that's the basics of, um, struct. You We can. We are going to see more. But let's maybe before I end the video, let's do a little bit of pointers to it. So Joe, Peter and this is a pointer and we can assess the data using the pointer as well. But here is an interesting thing. If you notice this Joe is a pointer. That means what we have this Jupiter. Let's print it out is a memory address. And I can print it out to show you. Um, the go is doing some magic here that instead of printing the memory address is is giving us the, uh, the structure instead. Right. And it's not allowing us to ideally, this is how you should we should do the dereferencing. Uh, you dereference it like this to get the value out, you see. But even if you don't put the dereferencing go does it for you. So it's like magic, you know? Um, with the pointer in place, you can change the value. So I can do this. That is active equals true pointer. Your pointer. Dot. Um, let's change the last name to um g g h n John Adam. Something like this to make it obvious. And I am not going to print a pointer again. I'm going to print Joe. And you see, it's now changed what you. Because we are this is a pointer. Whatever you do to it affects the original type. This is important. So you know. All right. Um, this is the basics of struct. In the next video we look at methods. So I don't want to pack this video. Let me scroll just to the top and we can look at what we did fully. So you see we created a type of employee. We have a factory method here. Sorry, a factory function here that allow us to create the type in main we created gin which is of type employee. We printed out some of James attributes or you can call them fields as well. Here we create a job, but this time we use a factory function. We printed some of Joe's attributes or fields. We we set some salaries here, and we also printed it out to see that we can set the value from outside. We created a pointer to Joe. We modified Joe's value using the pointer, and then we saw that it reflected when we printed Joe. That's what we've done in this video. Thanks for watching and see you in the next video.
