# Interfaces

In this video we are going to talk about interfaces. An interface is a contract. And in software engineering, contract helps us create software that are loosely coupled interfaces help us to also provide a way for others to interact with our program. And when you talk about interface, it's not just the interface keyword in in the programming language, interfaces are everywhere. It's just a contract. In APIs you can have an interface in your programming language, you can have interfaces. So when you expose a set of methods or a set of operations, you are exposing an interface for others to interact with your software. Let's start by saying, um, maybe we should continue using the employee example, um, from the last video, you. Since we have already seen it, I'm going to copy it over. But this time around, I would like to keep it really short so that the video doesn't go too long here. This time around, we can just eliminate everything here and just have name. There is something I didn't talk about last in the last video about, um, struct the capitalization. I will talk about it later when I talk about, um, packages later, but just keep doing it this way for now. So we have um, let's create a function here called Display person. Okay. And what I'm going to do, maybe instead of this being an employee, let's, uh yeah, an employee is fine. And then we can give it P and give it an employee. We are not going to do anything special. I'm going to say display p dot get name. Now because we don't have a great name attached to employee here. This is going to show an error. So I will just click here to create the this method for us. We've seen it in the last video, so I don't I should let me not waste your time by typing all the time. I'm just going to return the full name or which is name here okay. It's nothing special yet. So let's create Joe and ID and name Joe and we call Display Joe. Then let's go to terminal. Let's open the terminal here three interfaces. And we see Joe. We say display person but we're calling an employee. So let's say we have another type. Uh, maybe this is a business a business person a business person or something like that. And I go here again, I create a maybe is a business person. Okay. And here's the problem. We want to use the same function, but it's not going to work because we are type hinting. Uh, display person to display employee. Of course, if I change it to business person then it will work for the business person but not work for the employee. This is where you need an interface. Now what do we want? We said we want to say that display person as an operation should depend on a contract. Not a concrete type, but on a contract. Good. So we can come here to the top now and define. It doesn't matter where you define it actually. But I want to keep it at the top. We can say interface. We can say person is of type of interface, and in it we. The interface is a person should have gate name, and already the id is telling you that you have one implementation which is employee. Now here is the catch in go implementing an interface is implicit. It's not like for example, this is a little bit of uh let's say uh, it's not like you can't do class, uh employee. And then you say implement person. This we don't have any we don't have any keywords like this. In short, if you are looking at the employee type and what it has, it doesn't have any mention of the person interface. It's implicit. The only thing that the employee is doing is it has the same method signature that the interface wants. That means if an interface has a method and your type implements that method, your type has automatically satisfies that interface. That is the catch. So we have a contract here now of a person. Then we can say display person. Don't depend on a concrete type. Depend on the contract. That means whoever satisfies this contract is who we want to deal with. We can print the name of that person. So when I go here now, you see, it still doesn't work for Jane because Jane does not satisfy the contract. If I want Jane to satisfy the contract, I need to add this method to to the business person struct as well. So I'll copy and paste it here and just add it. Both of them now satisfy the interface of person. And as you can see from our I'd say we have two implementations. Sorry, that jumped it. I don't like to click on this because we jump too fast. So Okay. So we have an interface and then we have two implementation. Now if we go to main you see both of them would work now. And we have Jane Joe and Jane. Fantastic. This is what an interface is. You can add. And the good thing about go is this. You can you can have your, um, your interface and the many, like if there is any code in the, in the code base that have this function like this, that already satisfy the interface. If of course, if I add another thing, then get email for instance get ID let's use get id then we don't. It doesn't satisfy the interface anymore, because we are now saying that the interface, the contract to be qualified to be for us to call you a person according to this program is you must have a great name and a great ID, and none of these types currently satisfies that, so they cannot be called a person. So this is what it is. This is an interface. I'm going to take this out so that we we are back to the good state. And you can try to play with this. We're going to learn a whole lot more. It is possible to if you have one interface, it's possible to have that interface, uh to implement multiple interfaces and do all that. In short, you can have something like this. Let me show you, uh, a some interfaces in the code base just for us to use that idea to learn about the go code base itself. Let's start with this. Um, let's say I open, um. The IO dot writer. This is the very popular interface that says that the writer interface. Any type that have this method satisfied. This is another interface. Closer. Just one then if you keep scrolling down, this is another interface seeker. Look at this interface read writer. It just embedded two interfaces. So both methods. That means in order for you to satisfy the reader and writer interface, you should implement whatever method that is required here. And here you see read closer, write closer. You see how the interfaces are structured in the core code base. Look at another one. They are everywhere you see very very useful. So this is why you should also sometimes read a code base as well. Yeah. All right. And that's it about interfaces. Uh we're going to start using them very very soon. And I will see you. I want to talk about one more important interface that is already in the code base, which is a string interface in the next video. And then we can um, look at generics after that. See you soon.
