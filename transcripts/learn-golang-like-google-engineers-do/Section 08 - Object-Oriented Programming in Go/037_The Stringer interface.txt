# The Stringer interface

In the last video, we looked at interfaces, right? And in this video I want to show you something. Notice when let's go back here and run the code from the last video. Notice when I go here, let me comment out these two lines. I don't want to delete them. I want to just comment them out. When I go here and I print gin, look at what happens. This is the code from the say Geo declare not used so we can comment out Geo. For now this is the code from the last section. So you should you see what we have displayed. Well what is behaving like this. Because we do not provide a stringer. We did not implement the stringer interface which is in. I'm going to show you let's create another folder quickly. Um, you can say for stringer String a little bit. And I'll put in main.go. Then copy what we have from this section in here and I will delete most of them. For example, this um, we're no longer using this employee. Delete them. Let's keep the interface. We can delete this one. We're no longer using this. Delete it if you want. You can keep them. But I just don't want us to have too many code. All right, so if we go into full formed, there is a string. This is an interface. The format. The formatting is usually the code that formats your code. And printing to the screen usually look for this method. So if you have the string method in your type this is what is going to be called. That means if we go to business person. And what is the interface? Again let's go to the interface. Sometime. You go to the interface and you copy the method. And let's go back. Let's implement that same method. So instead of let's go back to the console look at how it was printed. Now we can change how that printing happened here. I'm going to say okay print it like this. Maybe something like this to make it a little bit fancy. And then we call it need to change the path to fall. And the main dot go we call it and look at the printout. So each time you print uh a type and you don't have the stringer interface implemented, go is going to try its best to print it the way it likes. You know, just basically how it can present the best it can. Right. But if you don't want that standard printing, you can implement this stringer interface, which is a very basic function, is called string that returns a string. But the capital letter is important and that's it. With this you can take charge control how your types are printed. You can add it to any type. In short, uh, let's say let me come here and say type id int okay. And I would add it to this. And I'm going to say I'd. And because this ID is an int I'm just going to say e. The e is doesn't really make a it doesn't matter. It can be anything. It can be ID ex or something like this doesn't matter. But then when I come here I can create a, my ID is uh, we call it ID, right. Let's say 30 too. And I would print my ID and then you see that it's using this method now to print it just for it to be shared coming from here. And I've print it again for it. You see you when you can create add methods to any type. You see this is a almost the built in type. But we aliased it and then we added the method to it and then look at how I'm creating it. You can define it like this as well. Uh. This ID and set it like this. Uh, if you try like this it work as well because it's an integer. So it works as well. See the same thing. So really, really cool stuff. I hope you are enjoying the course. Thank you so much for watching. In the next video we look at something that was recently introduced in Google generics, and I will try my best to explain it to you. Let's see you there.
