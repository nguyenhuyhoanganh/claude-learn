# Introduction To Forms

In the last video. We did so much, we did the redirect, we did so much. And in this video I want us to start working with forms. Forms are the way for us to extract data from customer and in go. Working with forms is quite simple. I want to start slowly in this video by showing you the basics of working with forms, and then in the next video we can write a small library that just make our lives easier. Let's go to the login form, for instance a very small form if you remember. Let me show you what the database look like. You remember this database that we use from the other video. We still have it. So we have email and hashed password. That means what we're going to do is to restructure the form a little bit. So instead of this being username it should be email I'm going to say email address. Is. Then ID email name would be email. And here for now we don't really this is not important. For now. We can actually take this out. Then password is password. Then the button is login. Then we want to maybe we should send it to a different route called uh process logging slash process. You can choose to do this or do it in the same handler. But since the login is small if we can as well keep it in the login handler. And then we do something like this. So we have already modified the form. Now let's go to the handler. This is the login handler right? Right now we can comment this line out. This is what we did to test the session earlier. We also comment this line out here. Now let's focus on the login handler. We can check if request dot method. If it is a Post request method post that we want to process the form, we can write out the code here. Now, in order for you to process the form, the first thing you must do is this. You must pass the form from the request so we can say if error. If it fails to pass the form, then we know there is a problem and we can return. We can return some kind of error. You know, for now I'm going to just do this HTTP error, something like this. Bad request. If the passing of the form fails, the passing process is like decoding HTTP protocol into a structure that we can use. It's very maybe some kind of map or something, but it's a very important process. We can now extract email. You can use, um, instead of doing this. There are two ways to do it. This is one way to do it. You can also do form value sorry form value. This is how I like to do it. It's shorter then a password comes from password. Now we have the data with this. Honestly we can for example now uh I'm I'm just going to log it on the console info log and something like this I don't really I'm not going to log the password okay. Let's log the password. Because if we don't we can now run the program. So let's put it like this. After this we don't do anything. That's it. Right. We just want to test that the form is working. Now we run the program. go to login. And I am going to move it to the side like this. Now. Let's refresh. Okay. Now time to put some email address which is let me just put like demo@localhost.com. And then password. With this we can check it to login with password the email and then the password. So fantastic we can capture the data. With this we login. The user in is actually very straightforward at this point but we are not there yet. I only wanted to show you this, so that when we write this small library that can allow us, you know, for example, if we want to add validation, we want to, um, print out the validation message. We need a simpler approach to do it. And the next library we are going to write would help us do that. So see you in that video and thanks for watching.
