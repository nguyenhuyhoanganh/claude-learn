# Post Repository: Get All Method

Welcome back. Let's write the next big one, which is the get all method. So let's start with func. And then I bring the signature and that's it. Now let's check the validation. Because when the validation fails we don't want to do any work at all. There's no need to do that. We can return nil return nil empty meta. And the error that we received from the validation after that, the next thing is the base query. Now, because the base query is very similar to what we wrote here. Um, here where is the where is the. This one is very similar to this. I'm not going to write this from scratch to waste your time. So let me just bring it over and then we can talk about it. You see, this um, count over is basically going to count total number of records that we have. And you see how it works. this. We need this for pagination, right? Okay. So after that I need to capture the arguments because I want to do a little bit of um if statement. For example, when people want to sort, when they want to search, when they want to. For example, if you say you want to order by popularity, then we want to order by the total number of votes and then maybe the time it was created. Uh, something like that. Just add basic functionality to it. So let's track the arguments in, uh, in an empty interface. Okay. By the way, there is also like any if you want to use any any is an alias of empty interface. Okay. Good. And then argument index. This is what we're going to use to do the binding. Now let the let the flow start. So if the theta dot query is not empty. That means someone wants to query for something. Okay, um, base query, I'm going to add something like this. Say where? Okay, p dot title. Um, like I think like this is fine. Okay. Then I will say args. And equal to append args. This is going to build it out myself. Okay. Percent like this strings to lower. And then filter that query. Okay. And then arg index plus plus. That's it for that. Then um We can do a group by. Base query. Say group by. Let me take all the autocomplete and see what I can take out by ID, by title, by URL, by user id, by Createdat, and of course by username. Let me make sure that, um, the user table has a name field. Yes. That's very correct. Okay. Yep. So far so good. What else we want to do? Um, let's say if the filter dot order by if someone say they want to order by popular. Right. This is something you can do differently. But let's keep it simple. We say order by. Of course, there is nothing like popular. Let's order by vote on the vote count in descending order. And then p dot. Um createdat. I think descending or something like this should do it. Else. And this is the ugly part of, um, working with SQL this way. So most people prefer ORM, they don't want to see SQL. Instead of that, I only want to do this one. I think order by p dot created dissent. Okay, that's it then what else? Let's start working on the limiting. Uh, limit. Dot page size. This is our limit. Like how many do you want to see? That is the size of the page. Let's calculate the offset. Offset. Of course we know that is theta dot page minus one. Then we multiply it. Okay. Then base query. Of course we can add limit and offset now. Limit. Limit offset. Like this. Then don't forget. Since we have done the args we can say args. Append arg, limit and offset. Something I don't understand is of course. So I watched. This is a digression a little bit why we have this append instead of the way slices work. I wish we could just x dot append and then call this, but slice doesn't really have any methods. Um, there is a new slices that like this one now, a new package, which basically has a collection of functions that you can use to search and all that. So it doesn't offer so much. But anyhow, it works. So we did that happen. Now let's query actually Rose. So I'm going to this is passing the whole query and then spreading the argument. And we can um. Let's check for error. Good. If there is some error we just want to return it quickly. And if not remember you need because you are querying for multiple rows, you need to defer the closing right. This is no longer needed for you. So we can do the post. And each time I create something like this, I like to, uh, return it for now. So A post meta. And like this. Even though we need to change the meta, but it gives me a happy path. Okay. VAR total records and that's it. Then we have to do the loop here for rows or nest. So this is going to continue to advance the pointer. And in it this is where we are going to scan all the records to an error. We do the scanning. And I hope that we don't have any error during the scanning. So let's check. Let's take a look at it very carefully. Where is the selection. So we selected two um total records. Man it's the the place is too far from each other. So let me copy this up there and let's match it first. Okay. Let me paste it here. So we selected total record. So you see. So that needs to change. So I need to do like total records. This is we created this variable. After that the post ID, the title, the post URL, the user id for sure, and the post createdat. Then the. Username which we are getting from the user into the post username and then post the total comment. We have comment count here and then vote count. So this is the correct one. All right great. Don't mess up the scanning. If that happens it's not going to work. It's going to throw an error at you okay. Now if we receive an error of course we want to return the error and some empty data. If not I'm going to say, uh, this set the total records. This total record, though, is only one time we need it. We are still going to have it in all the posts. We say it's a price that we have to pay somehow. Maybe there is a different way I can do it next time, but that works for now. Okay, after that, of course, we still have one more error check for the rows. So let's do that outside. Uh, if error like this and then we return. Okay. What if we don't receive any. We don't have any post at all. Then we can still return an empty data. So if Len post is zero, we can return this. But should we want to return nil? That is a question. Maybe we should return an empty slice of post like this. This is an option, you know. It depends on what you want, but this is something you can also do. After that, let's do meta data and then finish this process. Meta data. We already have the function, so we are going to fill things in total records and theta dot page theta dot page size. That's it. And we return it here. That's it for this method. Okay. It's quite a long method. Let me scroll to the top and let's look at it. Um, we started with the validation to make sure that, um, if the data is not valid, we just want to get out of it quickly. Then a simple query very similar to what we did earlier. And then we start tracking the um, index. Um something though. Why why did why did we even track the index since we don't need it? Um, I think I wanted to use it for. Yeah, maybe I don't need it, so I think I can take it out for now. I wanted to use it. For what? Actually to know if I need to search or not. Right? Anyhow, yeah. So after that, of course we did the we set up the pagination, um, requirement which is just limit and offset. And then we did the scanning which is very simple. You've seen this before and a little bit of error checks here and there and then calculate the metadata okay. That's it. Now we've written this code so far we've not started writing unit testing or integration tests for our code. That means the only way we can know if the code is really working is when we execute the code, which is not great. But, um, testing, you see, at the at the front of this course, you're going to learn how to do a lot of testing, but just bear with me in the next video, we we should add the last method I think, which is add comment. So see you there.
