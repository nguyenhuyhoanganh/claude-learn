# Post Repository And Interfaces

Now let's start working on the post repository which is going to be here. And so we have a repository here. Here's what we are going to do. Let's rename this to user repository. Okay. And now we need to create another file post. Read good. This is it. So what we want to do let's create an interface post repository. What should be inside. Let's have a create post. And it's going to have a title. Very. You saw the table right. Title URL. We need a user ID that would be title and URL will be a string. And. Title URL will be a string. This is interface not a struct Title URL will be a string, the user id would be an int, and we can return a possible integer and an error. So this can be default value. Value will be zero if we have an error. Okay. What else. I'm going to duplicate it and say add comment. That would take the. Let me just switch this like this user and post ID. Then the body that's the comment itself and will return the same thing. What else. Let's add vote a way for us to vote post id user id return just error. What else? We can add a get all, which is something really interesting. This is actually this is the most interesting method we are going to write. For now we are going to have something called theta, which is going to be a struct that we need to decide. Maybe we want to query or want to search for the post. We want to do pagination, something like that. Let's define that shortly. Then we can return the slice of post which you have not defined. We are also not we have not defined this one as well. Metadata. This is where we are going to stop pagination information and then a possible error. So we need to define this this and this. Okay. Patient little patient. We are there. Almost there. Get by ID that would be id int. And then we can return the pointer to the post or an error. Okay. We haven't defined the post yet. So then getting our comments or comments we need the post ID for sure. And we can return the comment which we have also not defined and a possible error. Now we we may need to introduce, um, what do you call it? Um, pagination here later, but let's continue for now. I want to come to the top here and start to work on the some of the types. So we need the id int and then Jason is int. We need the title the URL. We need the user id. Something funny I'm going to do here. And then then the user name which is going to be the name of the user. Remember we don't have this. We don't have this in the table. But when we join the data from the user and on the post table, we can have all this information, then createdat this for sure is time or time. And what else do we need? The comment count. I'm going to preload this so we can use it in the GUI and then the vote count as well. And total records. This is something we need for presentations okay. And that's it for the post. How about the comment. So you see now that that is now very colorful. So comment for comment. We need the ID of course we need the body of the comment. We need the user ID. The post id. We need the user name and the createdat. Just take those two fields and bring them over. And that's it for the comment. What else do we need? The filter itself. Filter struct C struct. Um page. It's going to be an int. We actually don't need the JSON here but I'm going to keep it. Page size uh all the all the bye. Yeah I think all that by string is fine. The reason why I say we don't need this is because we are going to use a query string. So we need to we are going to extract it from like for example page equal to one. So and then query a way for them to search you know. Okay what else we need. The meta num. That's what is pending meta struct. Good. What do we need. Um current page. And page size. Somehow it looks like. Oh, why are we repeating it? Well, this meta is going to be returned from. uh, this gate. All right. Look at. So we need to return this from here. The filtering is coming in. This is going out. And then we can add, like, are you on the first page, the next page number, the previous page, the last page and the total records. Total records. Something like this. Good, good. Now let's scroll to the top and then look at it slowly. We have the post type, the comment type, the theta metadata and our interface. Right. And that will be it for this video. If you've completed this then let's continue. In the next video we are going to add a little bit of uh method to the filter, just validating it. Then we can do the calculation for the meta. And then we continue adding um we can implement all these methods. See you there.
