# Introduction To Routing

In this section, I want us to focus on building web applications in go. We are going to focus on the core web concepts using the. Net HTTP package. Go provides us this package to allow us build almost any kind of web application that we want to build, and it is very powerful. Before I start, we start coding. I want to explain something a little bit, something to help you understand how most web applications work. Let me start by creating a new section here. I'm going to call this 13, but instead of section I'm going to call it web. And in it I would have that go okay. The most for me when I am. When I'm new in a language, I want to understand some of the sort of what it provides in and the way I do that is what do I need to give to get something? For example, what is the input and the output? What output do I expect? Let me explain that every web application rely on routing. In go they call it mocks or a multiplexer is the same thing. Then this routing somehow is mapped to some kind of handlers in Java. Net they call them controllers express also call them handler I think. So it's different terminology that that mean the same thing. So routing can map to this. So for example, we can say when a request a get request, go to the home page. Execute this function. Home page function. Let's say home page. We can say when a Post request go to slash users I want you to execute a, let's say create user function, something like this. And this is the simple concept. But what is responsible for this mapping is the mocks. So there is a every web programming, uh, every web programming framework like um JS. Laravel, Symfony, whatever. They all have this routing. So let's take a quick look before we start coding. This is web Jean very popular web framework in go. It is small. We are going to line it as well. But it's the same thing. It has routing and you can use the. Let's let me go to the quick start and see you see you create an instance of it and do all that. The same thing with express. If you go to Java, it's almost the same thing. It doesn't really matter. Sometime ago, let me show you, this is four years ago. This is my GitHub in. I was teaching PHP and I in the class. I taught how to build a route for the routing from scratch, and it's a simple code that uses regular expression to build the routing, uh, HTTP routing. So it's in the core, I think. Where is it, uh, app core data. Let me see what is here HTTP Read. Request and response. Contract routes. Okay. You see so this is what this was done four years ago. And and I did an implementation of it. It's quite easy to do actually. But I'm showing you this to tell you that when you come to a new language, a new framework, just learn how the routing works, every other thing, you can be bringing them in gradually. So now with that in place, so how does it work in go? The networking in go generally networking generally in go is done using the package. So if you want to write a networking application like socket application and everything is the. Net package, then there is because if you think about it, the HTTP is based on that. And they added the HTTP sub package. And with this you can build web applications and that is what we are going to do. So how do you do it? We can use the HTTP to create a server. And it's a production level server that is so good. So I'm going to go ahead and do it. So let's say I go here now and then I type HTTP or let's call it server HTTP server. This is the server and the server. It has so many things you can do listing and serve and all that. But there is a better way to do like sort of a shortcut. We can if we need more customization later. We use this. But there is a shortcut. Here's what we are going to do. I'm just going to come here and type HTTP dot listing and serve to listen on a specific port. So the port we are going to listen on is 80. 80. And here I'm going to pass nil for now. And we return an error. We can do the regular error checking and theta. Now we have we are listing. We have a server running. Let's go to the browser. Where we have a server running. But it doesn't do anything because nothing is binded to it yet. 80. And that's it? Nothing is there yet? No. Handler. So what we are going to do now? We can say. We can say my max new server Max, and then we add some handlers to it. So we are creating an instance of a max. And then I can say handler funk when they hit the home page. I want you to execute this function home. But what is the signature for this function? Because it's not it's not like every other function. It must follow a specific format. And let's write it inline. The function must take in a response writer and a pointer to httprequest. I want to say that again, it doesn't return anything, but it takes. The first argument is HTTP response writer. The second one is a pointer to Httprequest this writer, you can write to it. That means I can come here now and do write. We can write. Hello world. And what I'm going to do now is to run the server. Actually, that is the first mistake we made. We never run the server. Now we can go here I think is 13 web and main. So I started it's running now. Then I refresh and it says 404 page not found. Think about it. This makes sense. And here is why we created the mocks and added the routers to it. But there is no way that we have told the server to use these mocks. And that is what we can do here. We pass it the mocks. Now we need to restart the server. Each time you do that you have to restart the server. And we can go to the browser. Now run it and hello world. Congratulation, you have written your first web application in go. Now let's make it a little bit interesting. Instead of writing just plain text here. Let's write some let's say header. So we are writing a http HTML header. We want to send our HTML. If you want you can write add the encoding like the charset and all that. Then the content that we want to send out. And I can write it a multiple ways to write it. One way is to use the let me show you how this is. We can do this. You see that we are passing the writer here. Uh, let me click on it briefly. It takes a writer. And this is also a writer. That means we can pass the writer and then it will write, take this content and write it into this. And that's it. That is one way to do the writing. Let's open the browser again and welcome to your website. Another way to do it is also to call the they write itself. Which writes bytes. And you can wrap it around like this. It works as well. Nothing is going to look different. It returns an error. So you can, I think, return an error and in total number of bytes. Right. Yeah. We can ignore the total number of bytes reading and then check for error if there is an error. Of course you don't. Probably don't want to filter here because look this is going to crash the application. So you can do something you want to do with it there. But let's keep it like that for now. And it is running. We run it okay. And that's your view building your web application in go. I want to reiterate some of the things that we talked about here quickly. If I open listen and serve. You see that here. It says handler. It didn't say mocks It says a handler, but at the top of it it says the handler is typically NE, in which case default server max is used. It's because the handler is just an interface, and I want us to implement that interface now and see we can come here and say type default handler. I'm not going to do anything here. And this is the interface. And we come here right okay. And hello world. And we no longer need this for now. We can come here and use this. Marks is our is this one. This is our handler. And we pass it like this. And it's going to use our server marks the new one we just created now. And look at it. You see how powerful this is? And this is why I love go so much. Because think about this. Everything is transparent to you. It looks like a lot of work, but it is transparent. You are seeing. You are seeing everything being constructed from almost from scratch. You know what is happening. There is no magic, no pre-processing. You are doing everything by yourself. You can control the code, you control your writing and however you want to do it. And trust me, when we start using framework, some of these writing processes and everything become easier. But it is really fun. What if we want to write JSON? For sure? Let's, uh, let's do something. Let's do a little bit of routing. I can, uh, let's say maybe we just keep it like this. Now I can come here and do. Um, let's come here. To map. We haven't done. We haven't played with map lately, so let's use map. Um. User. A, uh, Joseph. Sorry about the typo. Okay, the undo is fine. Uh, age 40 something. Um. Height, I don't know, whatever. Seven, six. Um, location. Uh, can say United State. Okay, then we can turn this into JSON and how, you know, how we do that already. And Um, if we if we are unable to turn it into a JSON, we return an error. But if we turn it into a JSON successfully, it's a byte. That means we don't need to wrap it. We can just pipe it around. And here we need to change this to application slash JSON. And then we run the server. Boom. And you have your JSON. Very lovely very simple you know. And let me stop the server and go back over there and see what we have seen. But when if you have to create your you don't if you have to create your own server mocks is you really now can do it like this here. Here is the problem with the server mocks. We are not doing any routing. If I go here and I say slash about is the same page, if I say slash whatever is the same, it doesn't. There is no routing going on. So you have access to this information and for sure you can do some from the request. You have access to path like this. And maybe for now let's just log what is being assessed as a path on here. So watch out on the console. Let's let's put this away here and look at the path. So you can break this up and decide on what to execute if you want to do that right. Maybe users like can be API slash v1 slash users, whatever you want to imagine. And this can be split up and you make some routing with it. So this is just an introduction. In the next video we look at how to organize it a little bit better. We can do multiple routing and then we start putting together our applications gradually. Thank you for watching and I will see you there.
