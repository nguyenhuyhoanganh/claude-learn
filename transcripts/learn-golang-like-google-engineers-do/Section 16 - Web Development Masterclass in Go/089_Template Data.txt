# Template Data

In the last video, we used the foam wrapper that we wrote two videos ago to do validation for our form, which is working, but unfortunately we don't have a way to display the error message in our template, and there is also no way for us to repopulate the form in case there was an error. And then we don't want the user to retype everything, so we need a way to maintain state. Now if you go into our template, this is the renderer or everything about our template is here. Currently, the only way we can pass data to our template is through this data interface, empty interface that we have. Why this might work. It's a little bit of a struggle. You know, in order to work with the maps and all that. So what we are going to do, let's come here and type, um, let's say template data and what we are going to do, for instance, form, we are going to have a form and pass it our form which the form that we created here. We are going to be adding more things later. Then um, maybe we can add is authenticated boolean later as well. We are going to need that. We are going to add a flash message. I think maybe flash message. Um we could have as well maybe flash type or something. But let's keep it this way for now then. What else? I think I'm going to keep it simple for now, and then later we can come back to it. All right. Now, with that in place, we need to change how the render method works. Before we can update the render method, let's find a way to initialize this data okay. And that would be let me add a small method here. Um default template. It's data, something like this. And that would give us the template data. Okay. Um, how do we want it? We of course we need the data. We want to take the template data. You can can think about this like a middleware for the template data. Template data. And then the request we need the request. Here you will see why. Okay good. With this in place we check. First of all if the data is not initialized we want to initialize it. So we don't get a panic here. We can do anything that we want to do. You know um, for example we can say data dot flash. We can get app session, we can get the flash from the string. So we can um, this is a little bit of trouble here. Maybe I can, since we are going to use this in a single place. I want to do something. I need access to the session and I don't have it here. So we have, um, two ways to do it. Let me see. Let's think about the one way is to embed app directly in this. And then we can access it. But let's go to main first of all. So our template was initialized with this. I can imagine just passing only the app here. Again we have it's possible to do that actually. But now instead of doing that we could have as well pass a pass the session. Um, I think I think we should pass the session, which is of type. Let's this is the session type. Let's pass the session. Um, let me go to the template renderer. This is our template renderer. So session and then here. I think we should pass it as the second argument. And I name it session. Okay. Then we come here and do it this way. Of course this is not going to work anymore since we need to pass the session here and we can easily pass it from session here. Okay. That solves it. Now let's go back. In the default template data method. We can now have access to session and we can pop it. User want to initialize the flash message. There are multiple things that we still need to do. Um, this is a little bit, uh, This is not that great, I think. Maybe. Here's what I think. Maybe instead of us doing that and forgive me, but I don't have this prepared beforehand. Because if I do that, then the whole recording would be like a movie and it doesn't make sense. I'm thinking at the same time I'm recording maybe this default template data doesn't belong here. Think about it. We need it. But maybe it doesn't belong here. Maybe we should do it in the render method that we have. So let's cut it out from here and go to handler. Let's go to render. So let's add it to the application itself for now. We add it to the application because think about it. If we need to access like if the user is authenticated middlewares and everything, they are directly in the application. And we don't want to move all that to templates. So let's keep this one here for now. And of course, this make our life easier with this. Okay. And then after that, we just return the data. For now we are going to come back to add more things, but let's return it for now. Then the next thing I want to do is to work on this render. The render method no longer have this signature alone. We need to include the. We need to include this now http request pointer. We need to include it. And then we need a way to. So this is render and data. So let me send me do the data like this. So data is no longer going to be this data is going to be for example data is going to be exactly this now. Then we can say see a template. Uh, maybe we can call it match data or something. We add the default one. Like this. And then pass the data and the request. Don't worry. The request. We are not. We're not using it yet. Um, in short, we're using it here. But we are going to use it more. Then we can pass this here. This worked, but we could have as well just paste it like this. And that's that's it. Okay, good. With these changes, I want us to go back and undo some of the things we did in main. So first let's go to main. We no longer we don't have to pass the session here again. I am so grateful for that. I never wanted to do that. And then with the session no longer passed, we can also remove it from here. We can delete it from here. Delete it from here. And that's it. With this, I would like to run the program to make sure nothing is broken. Okay. For sure things are broken since we change the signature of the render method. So let's go and fix that. We need to pass the R everywhere. Everywhere that we have used the render. The signature changed. So we have to do it everywhere. Everywhere. And that's it. With that we can try again. Good. It is running. The good thing about go is if it is working, it is working. No surprises. Maybe one surprise. Okay, let's see why. Well, let's see why. Why? It's failing, right? We have, uh, it said that, um, template login, executing content error. Can't evaluate feed error in type. Good. Now um let's go to that's coming from login. Right. I can go to template login for now. Let's just cut this out for now. This is what is failing. Error message. Before we didn't pass anything so it was working. And now we have some data passed. It can't find it in there. And then it's screaming at us, which is fine. Now look at it. This is error message and we are going to control it soon. So this looks super, super good. And I want us to now work on the how to display the error message. So that is the next thing that we are going to work on. See you in the next video.
