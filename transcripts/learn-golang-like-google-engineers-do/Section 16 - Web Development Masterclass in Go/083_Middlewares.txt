# Middlewares

So we have been mostly working on templates for the past maybe 2 or 3 videos. And I want to change that a little bit. I want us to start working on the middleware bringing in session management, because we're going to need that when we login the user, we want to use the session to control the user login. The first thing I want to do is to open handlers, go and clean things up, delete the HTML, delete this logger. Everything is clean here. Now let's check main. What do we have that we don't need in main? I think everything in main looks okay. Good. And then. Shoots. Everything looks good. Great. Now let's start working on middleware. What is middleware? A middleware is, um, think about it as a function that needs to. You can run within before a request or after a request. So it's something that happened in between the calls. So let me give you an example. When I come to my the homepage and I make a request, let's start the server. When I make the request, if we want, we can do something before the response is sent to us. And then, you know, we can also do something after the response has been sent. Anyhow, you want to do it, but middleware allow you to take total control of how the requests move from function to function. So let me show you. In go is super easy to write a middleware. The signature is simple. So let's create a new file called redo. With. Okay. In this in this file let's start the first middleware which I'm going to call log. So let's say function we're going to add it to the application. It's definitely not save. It's going to be log or log something anyhow whatever we want to call it logger. This is a logger middleware. It a middleware must the way it is structured in go. If you are using the standard HTTP library, it has to take HTTP handler and return the HTTP handler. This is the signature for middleware and you will see why soon. Usually for by convention this is called nest. Here if you if you don't you can actually just return next. You know if you don't have anything to do this is a valid middleware. But what usually happens is this. That you, you you do, let's say here, this is what we want to do. We want to log something to the screen. So I can say return http handler func. And this is what I want to do. The handler func allow us to do something to have access to the response writer and the request. So here now we can say app.info log dot print f. This is our login. What we want to do let's say percent s this is going to be for the host. This is the IP address or the domain. This is going to be for the protocol like HTTP or or whatever they're using. And this is going to be for the HTTP method. And this is going to be for the http url that was requested. And this is it remote address, the protocol method and the URL that request URI. This is it. This is what we are going to print every time that a user request a page. And you must not forget to call next, which serve HTTP like this. This is a very powerful middleware. Now in order to use the middleware you go to your route. Since we have a mux already, I am going to say handler and then app dot logger. This is the middleware and pass it the mux. Now we can return the handler because remember the reason why you have HTTP handler as an input and output is because of this. This is our mux is also HTTP handler. And so you can pass it and return it. Now let's go and test it. If I go there and refresh the page, you see what we did for the time. This is the the this is the address. This is because we are um this is localhost. So this is the address. This is the protocol. This is the um the method and then the path. We can go to contact. You can see this is it. So this is our our login. And you can go to many other pages for us to see more logs. Beautiful. Now there is another problem. This is so let's go back to middleware now. This is the first one we've written. But there's another problem. Let me show you the problem. Let's say I go to handlers dot go. And here something panic. We panic. Something like this. And let's start the server. Let's go to home page to panic now. It crashed. So what's the problem with this? When we panic. So when something panic like this, you don't have control over it. That go routine crashed. You know the way, if you have done any socket programming before, each time a client is connecting, you start a new go routine or a new thread to handle that client. So each client connection is in a different go routine because the go routine just crashed. We we didn't have the opportunity to capture the message. We can't put it in a log. We can't send a useful message to the user. And that is not very good. So we need to solve that problem by introducing a way to recover from the panic. And we can use a middleware for that as well. So let's go to our Middlewares that go. And I can start another one. So let me copy this and paste it. And I can say recover. We don't need the login here anymore. What I want us to do here is to do the we you have seen recover before. So we do the defer work okay. And in it we do this right. What we want to do we can do anything anything you want to do here. So first I want to set some header that the connection was closed. And then I'm going to add a a a small you can write anything here but I'm going to add a small helper server error something to print error to the screen from the server. Then let's give it the writer. And then let's format the error like this. All right. And that's about it. After that, after the this is it. Let's call the function. After this we just call next and that's it. Of course we are not using it yet. So let's write this one quickly I can put it in the past. Go. And this one will be super easy to write. Application. And that's it okay. So what we want inside let's have a we're going to use the the stack. We need to we need to we need to print the stack ourselves. So let's go to trace okay. We have the Let me because I want. Let's do like this. I'm going to print the stack trace itself in a new line. So the so we can see it very well. So new line just go to a new line. And then this is error dot error and then debug dot stack. So we're going to put this in a new line the debug information in a new line. Then the main error message will be in a different line. Now we have the trace we can call the error log I will just I think let's call output because it can print out some depth. We print like two level deep and that's the trace. After that um we just write HTTP error. So it's a internal server error and then internal server error. This one is going to print internal server error. Look at what they did. So it's very straightforward. It's just a switch statement okay. And that's it. Now let's wire up, go to rootstock. Go and wire up the recover. So we have the logger here. You can just come here and say recover. This is it. And pass it inside like this. And that's it. Now restart the server refresh and look at it. We have our internal server error and we also have our stack trace. Look at the message right here. See we panicked. This is the message that we put in the panic. Look at you. See where it's coming from. Right. And you can look at it for example you can look at the log. We know for sure this is where the panic is coming from. And then you can solve it. Beautiful beautiful beautiful. So this video let's go back to Middlewares and go and look at what we did. We created two different middlewares. And I told you that every middleware has the same signature. If you are using the standard library. If you are using a framework like gin, the signature is a little bit different, but they all behave the same. Middleware is something that can happen in between and in the standard library. It's always this. It takes a HTTP handler and return a HTTP handler. Then in the first middleware we created was a simple logger that helped us print logs to the console. If you want, you can turn this to JSON and print JSON to the console as well. Then the second one is a way for us to recover from the panic inside the go routine, which gives us the opportunity to print something to the screen for the user and also log it. And that's it for this video. In the next video, I want us to bring in session management. We need to be able to put data in session. We need to be able to check if the data existing session to control logging. That's what I will do in the next video. See you there.
