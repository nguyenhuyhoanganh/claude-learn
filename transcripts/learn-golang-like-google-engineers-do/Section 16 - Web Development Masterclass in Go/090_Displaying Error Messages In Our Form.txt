# Displaying Error Messages In Our Form

Welcome back. In the last video, we set up everything so that we can, um, pass messages to our templates. Something I did above this, um, before I started recording was this I want to show you so you can make the changes. Now go to the forms dot. Go. You see that this is our form, right? This data, we are passing it to the template. So I just want to make sure that you know where I'm starting. So this is our form. Now go to the renderer this template data the form. Before this video it was like this. It wasn't a pointer. So change it to a pointer. That's it I think that's the only changes I made here then in handlers. Let me close up this in handler. So this render this one during the get request. When you just open the form you should pass the template data and set the form to an empty form like this. It's important because if you don't do this, when we write the code, we are going to write next. The form is not going to show up. That's why I'm doing this. So not inside the check for the post request outside of it. This is a default. This is what we are using to render the Login.html. Just pass in the new form and then empty form like this. All right, that's it. Now that we are here, we want to go scroll down here and set some things up. So let's pass this same template data again. And then form would be form. So we are passing the same form back to the login dot HTML. Now in the login dot HTML file we want to do a couple of things. I'm going to use some keywords that we've not used before. Since we have. There is something right now that we have form in the context. This is how to access the data and everything there. So you can type like dot form, dot errors. This will give you the errors object. You know um if you want to access like method and everything there, there is a get the method and then you can pass value and all that. But this is going to be a lot of typing. So what I'm going to do I'm going to wrap the form. This is our form and this place I'm going to wrap it in the form context. So in order to do the wrapping you can do like this with form this. That means we no longer have to type form dot like we no longer have to type this when we are referencing the context. I need to end this. Don't forget, if you don't end it, it's going to crash after that. This error message that we are want to display here. We don't have it yet, but I'm going to also wrap it. I'm going to wrap it in width. So width. Now basically it's like what I'm about to type like typing dot form dot errors, but we have the dot form already, so I'm going to type errors. Then dot get. This is the method that we added to it. Then we are going to look for generic message. If we have the generic message is going to be in context. So we just print it out like this and then end. Don't forget to end every width that you start. So every width that you start must be ended. In case you find in this syntax a little bit, um, strange. Honestly, I don't know why they chose this syntax. And if you do anything like infrastructure as code, like Helms chat, this is the same template system that is used for hem. Uh, so it's good to know it. You know, it's very powerful template system, but I don't really like the syntax, to be honest with you. Now we need to add this generic message when they submit the form. We also want to. So let me put the brackets there. We want to call the get. This is the get that we have on the form. And then we call, we pass this argument to it that we want to get the value for email. And that becomes what we set as a value here. We don't want to do that for password. So this is the complete setup on how we can retain the data when it is submitted. When there is an error message we also display it. But it is also possible for us to display the individual error messages here. Maybe we can come back and do it later. But you can also you can take that as an um, what do you call it as some sort of assignment. It's not hard. Let me show you an example of how to do it. Since we are here. You are. You can do um. This is errors, dot. You don't even have to do this. Like, you can just do it, like, um. errors dot get. Errors dot get dot. Or you can do the width and then end. I can do. Let's say PR class. I don't know if I have a I think I have error message here as well. And then you can do this. But then the error this will be email something like this. So you can take this as an assignment. And you can extract error messages for each of the field. Okay. Let's go and start the program. And then I'm going to refresh. Just type some random stuff and press enter. You see it looks like nothing is going on because the generic message that we intend to display here, we haven't added it. So let's go to the handlers Us dot go and add it. So many handlers that go inside the login method where we are checking for the form validity. If it is not valid. Before we send the form to the browser, I want to say form errors dot add, and then I add a generic message that the form um, the data you submitted, uh, was not valid, something like this. And that's it. And that should solve the problem. Now I run the code again. We can type the bad data that we typed earlier. And then you see the error message coming in very very powerful. But the good thing is also that look at we are returning the submitted data. That means the user don't have to do it over and over and over again. This is really good. This is classic. At this point. The next thing I want to do is for us to process the form and login the user in the next video. See you.
