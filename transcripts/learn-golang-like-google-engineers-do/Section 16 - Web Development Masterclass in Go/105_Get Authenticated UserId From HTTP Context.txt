# Get Authenticated UserId From HTTP Context

We left these 14 in the from the last video. Now let's fix it. What we want to do. Let's go to the. There are two ways to do this. One is to put the user ID in the session. Another one is to put something in the context. I want to put something in the context. And I think we should put the user object in the context. So let's go to middleware. And here I'm going to create um another one context user key or something like this. And this is going to be for the user. In short I'm going to call it user just like this. All right. We need this key to access the user. Now scroll down to the authenticate method. This is where we're doing the authentication. Remember here we have access to the user. Let's let's call it you. And over down here I'm going to duplicate this line. Then I'm going to use this existing context as a parent context here. And we can change the key here to context user key and pass it the you that we are we got. So this is the user. And of course we just set it like this and that propagates that into the context. I think that's it for the Or middleware modification. Now we need a helper function to help us extract the user from context. So let me come here to help us. And I would create a new helper here. And let's name it get user from. Context okay. And we can give it a context. There are two things we can give you if we want. We just give it the It said. But I think just the context is fine, honestly, and it should return the user. Make sure it is this user. You see, I didn't import anything because the user is right here in the same package, and finding the user in context is quite easy. Make sure that the context is context user key and not the other one. If not, you are going to get a panic because I'm about to do some panic here and the user not found in context and we can return you. Now this can be a debate of if we should panic here or not, but think about it, without the user in context, there is nothing else we can do. So if the user is not in context, we seriously can panic. That means there is something fundamentally wrong with the code. And this is for me. This is a good reason to panic because think about if you return an error here, what do you do? The user doesn't exist. Who do you send the error to? So this is a good way to panic. Now that we have this, we can take this. Go to handlers, go and then insert it. Here is where we have the 14 I'm going to say user and then app dot. Get user from context and pass it the context. Now we can call user.id. And if there are no errors this should fix the id problem and make the id dynamic. So I'm going to start the server now. So now that the server is started. If we don't get panic then it should be good. I hope we don't get panic. Go. I think two and then I'm just going to copy the URL and paste and submit. Boom. And it worked. So now the idea is dynamic. We can log in with a different user to check. So let me open my database quickly. All the IDs are at 14 here. Let's open a different user. Maybe this one, this demo two I just have com and I can login password. Okay. This is the password is not correct. None is logged in. So I can submit a post. And um I can paste this one too and do the same thing I did submit post creation failed. And why is error creating post duplicate post title? So let's do another one three and great this now created. So I refresh the database. Is there a way to refresh for post and look at the ID for this is 13. So this the context work. Everything works perfectly. No errors. And that is how we are seeing the post. Now that we can create post, I think it's next thing is for us to display the post that we've created. See you there.
