# Simple Template Rendering Engine

Welcome back. In the last video, we added the roots. We restructure the project a little bit. Now I want us to start working with templates. The first thing I want to do is to add another method that we can use to render templates. I'm going to go here and create a new file render dot go. Okay let's start from there. Now this is a plain function. Since we are going to need the render only in the handlers, I think we can attach it to the application itself. So we're going to do this as well. And I'm going to do something show you something soon. Why I am attaching it. Okay. Now what do we need. We need to write the the HTML content of the template into something. So we need we can accept the HTTP writer response writer here. Then the file that we want to render. And data. If we have anything that we want to pass to the template. And that's it. Okay. So the template you've seen this before. We did it in a few videos back I showed you how to work with templates. Now when you want to import the template package you want to make sure that you are importing HTML template. Not this one. This one. The HTML template is built on top of this, but this is very important for security reasons. Now we check for error and if there are, if we run into any error, we just want to return the error to the browser and then we return here. We don't want to do anything else. If not we need to execute the template. So error. We execute the template with the data. We check for error. Again we return if there is any error. And basically that's it for the render method. It's very simple. So let's go and use it. Here I want to create a new folder called templates. And in it I'm going to put a very simple template index.html. And in the index.html let's put a simple header from home page. Home. If I can write home well home page from template. Okay then let's go to the home handler. So this is the home handler. We no longer need this. We can now use the app dot render. And then we give it the right response writer which is this. What else. We need to give it the name of the file that we want to render, which I'm going to call, I think we name it index or HTML. Now we can pass name for the data. This is not going to work yet. Let me show you why we're going to start our program, then refresh. And then you see open HTML no such file directory which which makes sense because this is our let's look at our folder structure. We have. This is our root directory. So when you run the program this is where Go started working from. So this is the current working directory. So we need to go into the one three web in order for us to access the template folder. But what I want to do instead is to go to our application structure and add a template that like this, something like this. Then we can come here and say it would be one three web. So this makes it configurable for us. Don't forget, if you are moving the folder around, don't forget to update this. Then in the in the render method, this one we can come here. Remember we use we know how to join files. So instead of it like you can say full path for path we join it. So template directory with the file name. And then we go with full path here. Now let's test it. Go to the browser. Run it and voila we have it. So beautiful. We can now render templates. So let's go back to add something to the template. Now this is our template here. Open it up. We can here maybe we can add a div or a paragraph. Let's add a paragraph. Welcome to our simple blog something like this. ladies now go to the browser refresh and you see the changes immediately. We didn't we didn't have to restart the server. While this is good for development, this is also a big problem when you are in production because as soon as you start having visitors, you don't want to run this rendering process every time, like the passing of the files. Ideally, we want to pass this. Store it in a cache and then we execute from the cache. But right now we are doing this all every time that someone hits the page. And this is not very very good. So I want us to write a complete template rendering shield that will allow us to render templates easily. Now if we want to for example, add layouts, partials and all that, it becomes a little bit tricky. And then um, we won't be able to scale our application. Very. So in the next video, I want us to work on a more comprehensive template structure. See you there.
