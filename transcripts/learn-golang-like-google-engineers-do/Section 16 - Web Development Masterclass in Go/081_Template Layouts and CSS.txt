# Template Layouts and CSS

In this video, I want us to look at how we can do the layouts and every other thing that we need to continue building our web project. But before we do that, I want to point out something in the renderer that when you open the renderer, this method. The this one, the gate template method. Because we are only reading here, we can use the R lock and. Unlock. It is cheaper than the full locking that we are using before for this block. It is fine to do this, but for here this is correct. All right, both of them would work. But from a from an optimization standpoint this is better. So that says something very simple that I wanted to add. Okay. Now let's go back to our template. This was our a template. I'm going to make some changes there shortly. Now let's go to routes and I want to add a way for us to render static assets. So let's go to let's say static directory something like this. I'm going to use the file path dot join. We are going to join the current directory the this one and public something like this. Then we call mocks dot handle. Let's say public like this and then like this. This is what we need to render static assets over here. Let's create the public dot public folder. and in it I'm going to create a CSS folder. Within the CSS folder we are going to create a style dot CSS. Okay, if there are no errors, let's say this was something I was playing with earlier. I think we can do like this CSS and then style the CSS. Okay I need to start the server. And the server is started and that's it. It's currently blank. Let me close some of the tabs to make things cleaner. Nothing here yet I want to copy over the CSS. It's um, it's a simple CSS, but we don't want to type everything because this is not a CSS class and I don't want us to waste time typing CSS and HTML. So let me just bring the CSS over. I'm going to copy paste it here. Let me minimize this also. Close this tab and scroll to the top. I'm going to scroll a little bit fast. Good. So this is where it starts. And then it's just had basic stuffs. It's going to look like a Hacker News clone. That is what we're going to do now. When we go back here we should be able to see the CSS. So this is good so far. I'm going to come here now. Now when you go back to the roots that go what we did here, I think we can take advantage of something else. So instead of instead of hard coding this here, we probably can do app dot static path or public path, public path, something like this. Then let's go into app And we set the public part like this. And over here, we can set it like this. I think this this should be fine. Let's test it. Okay. Something declared and not used. Uh, which is, uh. Here. So we no longer need this one. Let's run it again. Go back to the browser. Um, we need to do public good. And it works. So this is better. It's configurable. Everything is well organized in main, and we can easily make changes over there. Now that we can bring in CSS, it's time for us to do a little bit of layout, which is how we we can bring in additional pages, open templates. So we can go into template and say, first of all, let me, let me be sure of the name. So we called it, um, layout. Right? Yes. So layouts. I create this directory here and we also need to create pastures. Good. In the layout I'm going to put a base dot HTML there. This is the base layout. Then I'm going to bring in the HTML for this very basic so basic stuff here we need to change this to public. And we need the header this block. This is a content block. So we are going to render the the sort of the for example home. We're going to wrap it around content. Then we inject it into this place. Then we need the footer as well. So let's create the header. Um, maybe before we create the header, we can test it and see what happens. Okay. But here is where we're going to have a problem right now. Let me show you something. If you go home, let's say we go to handlers. And this is what we are currently rendering. Right? It's this index. When we render the index, there is currently no relationship between the index and the layout, even though the layout is available for us. But currently there is no way we are telling this index to use the layout as the base template. That's a little bit tricky, right? So what I want to do, let's set up the let's just go ahead and set up the pastures, the footer and some and the header for now. I mean let me bring over the Footer is very simple, simple HTML. I'm going to leave all this HTML for you in the code, then header dot HTML as well. Okay. And I bring the header as well. Good. So this is the header is a lot of content but we can reduce it. I'm going to add a conditional statement to hide some of this menu. When you are logged in. Then what I want to do is in the this is the index. We paste this we replace the index with this content. Here's what's going on. This this loop here I added this loop just to duplicate. I have this five times in the page, just to give us a view of what our content should look like. But the most important thing here is this define that we are saying that this is the content of the page. So we are we wrap it in this define and then there is an end here the range. And then there is end here. And that's it for and that's it for the template. This is a pagination that we are going to have. Now that we have this. We still have not solved the problem because let me run it and show you why. When I go to the home now you see nothing happens. Nothing happens because this content is there. But there is no way we are printing it. The if you go to handlers, we just call it index dot HTML, trying to render it, but the content is wrapped in the content block. If I want to show something, let's say I put something here test. Which is outside of it. You see, you can see the test because that is outside of the content block. Now, how do we connect this to our base template? That's where the trick is. So we need to go into our renderer and in the render. Let's go to render render. When we we get the template we can do this. Instead of us calling execute we can say execute template. This one takes an IO writer. It takes the name of the template which is the base template. Now that we really want to execute with this, and this should solve the problem for us. Wow. And that's it. This is the page. If everything that we've just done now is a little confusing, I'm going to take a step back now and re-explain it. So let's go back. I'm going to re-explain it. Then I'll end the video. And in the next video we start bringing things in more. So let's go back to explain it a little bit. What what exactly did we do? Let me fold everything here. We started with the we started with the roots. We started by adding a small handle up root. We added this root to allow us access the public data which we have here. Okay. That's what we did. That is the first thing we did. Then the second thing was adding these templates, the layouts and the partials. And then we edited the index.html. And for the layout we have based HTML which is very simple it this template is when he finds a content block it injects it right in here. This place where including the headers HTML and footer HTML, both of them are coming from pastures and they are able to come from pastures because in the renderer we are passing all the content in in layout and pastures, right? Then now that we have this, we need to execute this. And also the any of the pages that they want to use. And that is exactly what we did. So when we I can fold this now you go to index. This is the main content that is wrapped around the content block. So if you want your if you want your templates to be rendered or your page to be rendered, it needs to be wrapped around the content block. After that we just we modify renderer, we modify renderer and then renderer instead of renderer using the execute renderer now is using the execute template and executing the base template. That's what we've done and that's it. So thank you for watching. In the next video we add more things. See you there.
