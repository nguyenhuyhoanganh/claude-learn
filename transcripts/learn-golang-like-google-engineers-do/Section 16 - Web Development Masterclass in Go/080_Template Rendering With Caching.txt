# Template Rendering With Caching

Okay. Welcome back. In the last video, we worked on the rendering method, which we basically used to render the page so we can refresh, you see. But there was a problem. The problem was now the server is still running. If I go in here and I type test, test, test and I go back to the page and I refresh, you see it shows up immediately. This is not what we want for production. You want to add some level of caching. This is good for development. But when you're in production you don't want to do this immediately because this is going to overwhelm your server. You have to like pass the template over and over again. And that is very, very, very resource intensive. So what I want to do in this video is to add some caching. Basically when we when we render our template, once we don't want to pass it again. We can continue to execute that same template and that eliminate the parsing process. So let's go ahead and work with that. We have the rendered go here. This is where the method is I won't touch it. Let's create another file here called. Something like this. The names are similar but uh this is a renderer. This is a render itself. So let's keep it that way for now. What I'm going to do is to type, uh, let's say template renderer. I'm going to have a cache, which is going to be a map that points to template. Let's import the template package. After that we need a mutex I'm going to use the RW mutex okay. Then I need a flag for if we are in development or not. Because if we are in development, I don't want to cache or we can choose to cache or not to cache. Let's add that flexibility. Then the template the that we something similar that we had in main last time. All right. So this is the basic this is what we need the sort of the construct for it. Now I need to have a constructor method. And the constructor method. This is a I want uh the template directory here. And then we can have is dev boolean something we can use to set it. We just come here and return. Now let's set it now don't forget each time you have a map like this you have to make it. The zero value of a map is new. So if you don't do, if you don't make it and you try to set something and it you're going to get a panic. Now after that for mutex you don't need to do anything. We just need to set this mutex is usable by default. So we don't need anything there. Now that we have that let's work on we need about three methods or so. So but let's come let's start with the first one I can from here. I'm going to we need the render method that is very similar to what we had the other time. You see we we need the response writer where we are going to write it. And something interesting this you can use the IO writer as well. You can use IO writer here as well. Maybe we should use IO writer. So in case we want to write to a different destination as well. Okay. After this, this is the render method will be very simple. But why where the where we have problem with this IO I right? Is. Um, let's let's try something. It's not. It's not a big deal. But I wish, because we are going to use this only in HTTP context. I think we should just keep it with, um, with HTTP response writer. Uh, yeah. Like this. Because I want to return something to the browser here. Let's say, uh, template. Okay. Um, the template here, I'm going to add time to get the template. Get template. And then pass to the the name of the template. So here that's template name okay. We're going to pass it like this. We need to create this method this template this template. Basically try to get the template from the cache or not something like that. And um this is error. So we check for error. If error is not new, we do the same thing that we did here. Let me go to render. Let me copy something from render here. Something like this. And we paste it. We just paste it here. You see the same thing. The only thing now is the reason you're seeing this error here is because we have not read. We need to write this template. When we finish writing it, everything goes away. After that, we do the same thing. We execute the template which go back to render. This is what we need to do. Again nothing different. The same thing. Okay. That is it for that method. This method is done. Next let's work on the gate template. There is a way I write code. Sometimes I take things in reverse order. Like now we are going to write gate template. Gate template may depend on another method. You know we keep going in reverse direction until we're done. So basically I write the thing as I want to use them, you know. So let's go here. Now we know that get template returns, uh, a template and an error. So it takes the name of the template and return the template and an error. Good. What I want to do first is, is if we are not in development mode, let's take the lock. Then let's try to get template from the cache. If we find the template in the cache, we unlock and then we just return the template and that's it for this. Nothing else to do here. If not here we just unlock the mutex. But if we are not, if we can't find the template in the cache, we need to get the template and then cache it. So for that we do the same thing here or similar thing. So it's not get template I'm going to call this one pass template something like this. Then we check for error. Simple process right. Very simple. Okay. Now that we have the template we can catch it if we are not in. If we are not in dev we want to cache. Okay I'm going to lock it. Do the caching which is three dot cache like this template name like this unlock. And then over here we can return. So return template and then name. Very beautiful code. Let's say We see in the render, we get the template. If we if we run into an error, we return the error. Else we execute the template, then the get template. Basically, if we are not in development, we try to find it in cache. If it is in cache, we return it. If it is not in cache, we pass the template, use the parser here, and then we catch it and return the template. Very simple. So next is to work on the template method which is also quite straightforward okay so let me come here. Copy the signature shot. We can fold this one like this. So we need the template name. It returns the same signature with this actually so we can copy it like this I always try to save time by copying and paste. Okay now what do we want to do? we need a path to the template. So template path. You see we join it. Same thing that we did. The reason why this pattern is important is because of operating system compatibility. You don't want to do it manually. And then on windows is different. On Mac is different or Linux is different. So always use this helper method to do it then uh files. Let's put all the files here. Then put the template path okay. Because there is a way I want to structure it. Inside the templates we can have two subfolders layouts and partials. So layout is like you need a base and maybe for authentication and then different layouts for the partials we can have like headers footers maybe menu or however you want to structure it. So let's start with the layouts. The code here is going to look very similar, but somehow, um, let me say layout path. Let me do it like this. Path dot join. So this is the template directory would be temporary directory slash layout. We are going to say everything inside layout dot HTML something like this. Then I can now say layout error like this. Let's check. Now this. If we don't, if there is an error, we don't care because we can choose to have the layouts folder or not. So we can say if error. Is equal to ni, you know, if error is equal to ni, we want to just append to the files and that's it. The reason why we we don't want to check if error is not near and then return, because that way if they don't put layout you. You're not rendering the page and that's not nice. This is optional. That's why I'm doing it like this. We need to do the same thing for partials. I'm going to copy and paste. This can be refactored into a single method that you give a directory. And it does the passing for you. But hey let's do everything here. Partial path. And I can say partials. Maybe the layout. We should name it layouts like this. Yeah. And then partials. And I put this one and that's it. So we can pass data or templates from pastures and then from layout and pastures. Okay. Not so far I hope. No confusing code here. This is very similar. We can this is nicely we can refactor this but let's keep doing it like this Then. Um. Template Tmpl. Template dot. Dot. Pass files. We want to pass the entire files. Okay, good. We are spreading it. And the reason why we can spread it. Look at the signature so we can spread it like that. Then we check for error. Of course, if there is an error here, there's nothing we can do. We have to return the error. And after that, we can return the template. Good. And that's it for the method. And I think that that's us. That's everything that we need to do. Um let's start let's take a quick look. This is the base for it. This is template renderer. We set up everything here. The constructor A simple render method. Then the get template that checks if it is in cache or not, and then the pass template. Okay. Um, with this we can go ahead and use it actually. So what I'm going to do is to go to my main right. We can let's keep this one for now. We can say maybe let's call it TP and then template renderer. Or then here we can say app.tp equal to new template renderer. So it's app dot template template directory is dev. For now let's put it true for now. Um something is not working here. So let's go to this one and see what we are doing Yeah, I've seen the problem. The problem is that we need to make this a pointer. Okay. And that's it. Um, then now that we set the template instance here, we can start using it. So we go to one of our handlers for example the home. Instead of us calling render maybe we are going to do let's go to render. And we can delete the code here and return app dot t. Dot render w file name and data are. Okay I think it doesn't return anything. That's why. So it's fine. Uh, that's why we had that error. So we just call the render from the template. But let me add a little guy. Let's say there's something I would like to do if Aptp is near here, I can say, um, let's add a HTTP dot, uh, error or something. Let's see what we can do. Let me go to the renderer. Let's go to the renderer and copy something like this. Handlers render. So here is now we can paste this code and we just say errors dot new uh template rendering engine is not set something like this okay. And now use okay. If this this can be a regular string. We don't need error here. So that's it. Say text okay. Fantastic. That means in our handlers let's go to handlers. . This is home. We no longer have to change anything. Everything should continue to work. Now let's run the code. And I go to home refresh. Okay. Index dot HTML not found or something like that. Okay, let's see why an index.html is there. Okay. So time to debug. Let's close everything. Go to main. This is our templates directory which makes sense. And then templates correct. So from that's no problem there. Let's open this okay. See where the problem is. We didn't set this. So template there and set this good. That should be the problem. And then we run it again debugging good. Worked great. Now let's do one more thing. Now the server is running. I'm going to type hello world here. I think I have SS tags. Refresh. You see that the Hello World showed up, right? Let's go back to Main.go and turn this dev to false. Okay. Restart the server and let's see if our cache work. Now see, this is hello world. I'm not going to restart the server this time around, but change the template instead. Hello world changed. Refresh the page now. Nothing. You see, we didn't see the changes because we are now caching. We cast the template and the cache works great. And that's it. And we can do. We can do multiple times. It doesn't change anything. So this is really, really good. Now there are multiple ways to do this caching thing, but most times it is similar. You have some kind of mutex controlling the map and all that. Um, and that's it for this video. In the next video we can work on some of the layouts, some of the templates, and then we can start bringing in the database operations and continue to work with our web application. I hope you enjoy the video. Before I end the video, let me go back to the renderer and then do a quick recap. We created a structure here which is very basic. The cache is the most important thing here probably. And then the mutex that we use to control the locks. After that there is a constructor. Then this render method that we use to render the template is very similar to what we did in the last video. Then there is a gate template which actually does the caching. It tries to find the templates in the cache. cash. If you didn't find it, we pass a new template and cache it. And then the last method here is basically passing the files and returning the template. So that's it. And then in main we added the teepee which is basically our template. And then the pointer to it. Then we initialize it here. Notice that here I didn't do teepee here and then do new. The reason I didn't do it like this is because I won't be able to do app dot this. This is not going to work because at this point app has not been initialized. So and I need to use the app template directory instead of duplicating it I need to use this one. So this is why I set it up here. And it is normal. It is normal to do things like this. So and that's it for the video. Thanks for watching. I'll see you in the next one.
