# Post Repository Implementation

Okay, let's start working on the big one, which is the repository itself. So I'm going to fold everything here. Then under this place, let's start with very similar to what we did here. Let me open this and show you. So look at what we did. Something like this is what we are going to do. So we can copy it. And we can call it SQL repository return new SQL post repository. And I can say. Uh, what am I doing SQL post repository create new post type. Okay, so I'm supposed to do this and then we return this okay. Hope I don't have, uh, like this. Let's say SQL. Okay. Um, let's see why cannot use the DB type or subtype. Let me see what's the problem. This is why copy and paste can sometimes is annoying. New SQL repository. Hold on. Okay. Return. I need to put a pointer here and. Like this. Good. Yes. That's it. Okay. What is the. Which of the method should we start with? Let's start with, um, let me take the type for sure and I'm going to say func R for repo. And let's start with the create post, since that is the. So we need a tie to the URL. Both are strings and the user id. Very simple. Then the s the statement is for SQL. So I always like to call it like that Stmt Stmt. Okay, so insert into post the title, use url user id and this very very straightforward no complications. So result. Or you can call it R we execute okay. And then we check for error okay. Now when we check for error maybe we can do a little bit more with this error message I want to show you more things. Let's say I want to come here and define the type. Let's go to the top gently and say var I'm going to do like this. Let's say error. Duplicate post title or something like this. And this. We can keep it like this. Then what is going to happen is this I'm going to come here and say if strings contain and what the error message is usually something like uh, you need constraint. And. Then it will put like post or title something like this. If that's the case we want to return zero and then the error duplicate post. This is one way to do it. But we are going to test for this. In case the string error string is different we can adjust this. If not, we can return the plain error as we received it. Okay, after that let's look for the post ID that was inserted and we get it and check for error again. Now this should return int64. Yes. All right. So we need to cast it to an int, which is very simple just like this. Fantastic. We just created our first method. All right. Let's go ahead and do the second one. I am coming here like this. Then I can say add comment. Now let's make sure that we match the signature the user id post id. So let me go to the interface to make sure we are matching it. Look at it. So this is a comment. Yes. Match. Okay. Fantastic. So we're going to do the same very similar thing here inserting two comments. Okay. And body. So this is the body. We need the like this. Okay. And then the third one like this. All right, that's it. User ID post id and body. You can switch it around if you want. It's no problem. It's still going to work. But I think this is fine for me. Then. Um. Result. We executed check for error, and I don't want to do that anymore. I'm just going to return the error as I received it and we check for the last ID, the same code here we can copy and paste it, but but we need to change this to comment ID. You know, and then do it like this okay so far so good. And we are doing very very well. Next is this um our vote okay. So let's go. I think as soon as I start typing. Autocomplete gives me the the signature, which is very good. But I like to double check. So I'm going to highlight it like this. And then we go over here at the top. So add vote. And this is not very good. Um correct. Because to add vote the adding of the vote does not require body. So let's take out the body. So we only need a user ID and a post ID to add a vote. That is that was my error and we fixed it. And let's scroll back down and do the same fix here in the main implementation. And then let's start writing the code. The code is also super, super easy. So insert into vote user id, post id and this is the two records. Now we I don't really care about the result this time around. I'm going to do it like this. But now I want to do a little bit more with the error check because, um, if you voted before, that's if you voted before. I want to show a different message. So let's say if strings, uh, like this. So unique constraint failed this one. We know that the only unique constraint that we have is the user id and the post id. So I don't really care about the field or I want to do something or. Or strings that contain I can say primary key constraint field. If we receive any of these errors we are going to return. Let's return. Um, and we're supposed to return just an error and not error an int. So let's also adjust that. So I'm going to say error. Duplicate vote. Something like this. So let's create this one. While we're on our way up. We can remove this and make it only error as a return value. Then let's scroll to the top and while we are here add vote is only error here. So this is great. And then I go all the way here and add this error message that we want to do duplicate vote. That works. So let's jump to the code here. And that's it. If not sure we can return the error as we receive it after that. Honestly, there is nothing else to do. We just return nil. And that's it. Very simple, very straightforward. Okay. The next one is to do get by ID, I want to go to the interface to see what we have get by ID so this is it. And let's try to see do it. This one is going to be a little bit more complicated than we have seen so far. Because think about it when we are retrieving a post. Let me come to the SQL quickly. When we are retrieving a post, we also want to retrieve a few things with it, like um, maybe how many comments it has, who made the post and all that. So that is going to cost us to do a lot of join here. All right. So let's work on it. So I'm going to call this query now because this is going to be a little bit big. It's not just a statement. And then I'm going to do this. If you are not very familiar with SQL, well, this is an opportunity to learn some SQL. So I'm going to start very simple line by line. If you find this confusing please you can Google it or just send me a message. Let's start with this. So I'm going to select um the post I'm going to say I am. I am going to alias the post table this time around because we are going to concatenate um, other tables too that might have ID, so you can just say ID that would cause, uh, an ambiguity. That is the English. So Peter. Title. Um, Peter. Title p dot url Peter user id and uh dot. Body. Um. Not my body. P dot creator. That's. That is what we need. Okay. Then you dot name as user underscore name. Remember in this we have a field that we named like this. So this is where when we're going to martial we martial it into this the JSON don't worry about the JSON but just focus on the name in here. Okay. So you dot user underscore name. And then I'm going to another line. The line I'm just going to different lines so that it doesn't look too long and confusing to you. So I'm going to say count right. I'm going to say distinct I'm going to count p.id which is comment.id as comment count. Then I'm going to also do like this to do v dot user id to count their total vote v dot user id. Vote count. After this we can go from from post which is now alias as P before you can say from post as P, but you no longer have to do that. You can just front post P, you know, that's fine. Then let's start joining the table. So I'm going to left join. US users u on p dot user id equal to u.id. This is very basic join and we are going to do the same. Join on comment. But I want to make sure that there are no errors. So comment is see for sure on p.id that is equal to post id. Then lastly we join the vote. Let's join the vote which is p dot. We are going to judge the p.id and then vote vote dot post underscore id. And that's how we are going to join it. Okay. Where p equal to this. So this period equal to this is where we are going to send this guy in. And then we do a little bit of grouping here. Group by group by. I'm going to do group by p.id. I think I can say by the title uh url user id createdat and um url user id createdat. about, um, you name? Okay. If there are no errors, there is a way to check it. Let's go to. Even though we are not going to receive any record, I want us to try it in the console. If you have a console, you can try. Let's just put one here and run it and there is no error. So this is good. That means our no error in our code. So we can always test in isolation so that when you put it in code you don't put something with defect in your code. So this is great. Now let's go back to the code and then we can finish it up. Right then let's come here. And I can do the role that I want to retrieve from the database and query rule. This is a simple query. And then via the post that I want to return is here. In short, I want to come down here and return that post that we've created now just to satisfy the Signature. Okay then what next is to scan. So error we are going to do the scanning. I'm going to come back later for that. And I check if error not will return. Um so if you if we can't find a post, we are going to see the very similar error that we were seeing during the login. So if we want we can do something about it here. But for now I'm just going to return nil, return nil, return nil and error for now intentionally. We can come and fix it later. Okay, now let's work on the scanning. First we need to scan the post ID. We need to scan it to the title. It must match exactly how you select it. You can't mix it up. So we scan this post posted. Posted. Post. Idol. Post. Idol url url user id user id Createdat createdat. Okay, that works so far, so I'm going to put them in a new line. Remember we are passing the address here, so address and not the value. So we stopped at uh we stopped at createdat. Yeah. So we need to do the user name and then comment count and the vote count. And I think that's the last one. Okay. Good. That that's it. So that's it for this uh, method. We've done so much in this video and I would like to stop here. Uh, just let's take a quick scan of what we did, and I can stop the video. So we created a type which is struct and very similar to what we did with the user repository, and then a little constructor here. Then we added the create post method. Very simple SQL and then we just execute. Okay. If you want as an assignment you can use Preparedstatement. You know I explain why prepare statements are very good in in our database section. After that we added the add comment method and then add vote method. And lastly this get by id which has a little bit of uh, complex SQL. But trust me, it's just this basic joins that we have nothing special about it. And then the regular scanning and that's it for this video. In the next video we write what I consider the most complex and sort of the biggest method, which is the get all which we have to do pagination and all that. So let's save that great one for last. See you there.
