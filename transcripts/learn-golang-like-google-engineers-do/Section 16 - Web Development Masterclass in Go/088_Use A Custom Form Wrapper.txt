# Use A Custom Form Wrapper

In the last video, we did the form library, a very small library here that we wrote in the last video. Okay, you can take a look. Now we need to use it in the handlers here to work with our form. So how do we use it? It's quite simple. We come here and we say form, um new form. We know where it is. And then we pass the form. Look at it. You see, we pass the pass this post form from the standard library into a that now convert it to our form. So what we did was a wrapper very very important. And everything else becomes super easy. So we can say uh required what is required email and password. And this is why I did the method chaining. Then I can say that again, I can go to a new line and say max length, max length which feed maybe the email, the length to be, let's say two, five, five for instance, and then for the password as well. It's 255. So. Max length min length as well. We can add a min length for the password to be. Let's say. Um min length for the email. Min length for email would be let's say three. You know, just something like this. What else do you want to add for the email? Again we want to add the matches. So the feed and then. Radius. This is the regular expression that we pasted. Okay. After that we can actually this is still valuable. We can do if the form is not valid. What we want to do this time around we still want to render this, but this time we want to render it with a form error. So I'm going to return here. When we render it with the form error, we need a way to pass the errors to the template which we don't have right now. But what I want to do is to print it to the screen. Let's um app dot log dot printf. And for now we can say invalid form. And print the form dot errors. For now we are going to fix this in the next video template data in the next video. Now let's run the code. And see if there are no errors in our implementation. We should be able to validate our form. Okay so I'm going to just do like this press. Let's see what is coming over on the console. You see, we have this message here and look at it on line 30. It says invalid form email. This field is too short. Minimum of three characters. Good. And then password. Password is also too short. So let's go to I can say test and then maybe pass and look at it again email email. This is not a valid email. And great now let's make it valid all around. And then password. And then you see the logged in with this. So if our validation worked great. Two improvements I would like to make. One is somehow we need to retain the message here as well. The form data that was submitted. We need to retain it. So let's work on the template data in the next video. And then we can see how to display the error message and also retain the form data. See you there.
