# Add JSONRPC Server Implementation

Okay. Welcome back. I want us to check out a new branch, um, for our, um, Json-rpc server. We are going to continue working in this branch for maybe 1 or 2 videos, so let's keep it open. The first thing I'd like to do is to go into the JSON RPC folder, which is inside internal, and create a server in it. We need a way to structure the handler. So let's create a new handler type. This is really simple. Basically a handler is any function that accepts raw message, which is this is just bytes that accept a byte and return anything and possible error. It's a little bit, uh, it's very generic, but we want to keep it that way. So handler. Is a function that handles a JSON RPC method. And that's it. Now after the handler, let's create a server type the server type. In the server type, we have a map for the handlers and then the logger. And that's it for the server. This is don't forget this is the core protocol itself. We have not done any MCP. We are still trying to see how to interpret the JSON of our JSON RPC itself. So we are going to register the handlers here. With this we can create a new server. And um let's return. So let's make it and then this and that's it for for that after this uh So server. So we created a new server. We initialize it. The make here is very important. If you don't make it you can use it. That's very important. Always have it in your mind. If you have a map. If you have not initialize a map, not map channel slices, if you don't initialize them and you try to use them, you are going to get a panic. Okay, I'm point us anything like that, you must initialize them. So let's add another method to the map struct to register uh to be able to register method, basically adding something to the map. So we have the map already. So putting a string and a function. So register this. And then in it we just add this is the name and then the handler. And what else. Let's put some log and that's it. What else. How do we handle the request when the request come? We want to fetch the handler from the method. We want to call validate and we can execute the handler and return it something like that. So let's go ahead and work on it. So this is handle request. Request is going to come. The request is basically our RPC request. The method is going to be here. Some parameters are going to come as raw string. And that's why we are accepting there. So we have everything the client the like Claude there will be the one to send this to us. And of course ID. All right. So let me put out a message in the log that we have received some requests, and this is the ID and we are handling it. We haven't handled it yet but we are about to handle it. Then let's validate the request. We want to validate it because we want to make sure that the request has the method. And, uh, let's let's go back to the validate. Actually, what are we checking here? We want to make sure the RPC is 2.0. And at the same time the method is not empty. So let's call the validate. So we validated the request. If we run into any error we return the error. Basically if we have our own type of error we can return the error here like this. If not, we return a more generic error message. If not, after the validation, it's time for us to, let's say execute a handler like this. And then I can do this. This is the method I will receive. You see, we are not trying to like maybe make it all lowercase or anything. It has to match method for method. Now we check if it is okay. That means the method the we are able to find the handler. If it is not okay, we didn't find a handler. It's very important to check for that. So we check if it is not okay. New method not found. Error will be returned and if it is okay, that means at this point we have the handler. So we can now go ahead and do the execution by calling it so handler passing it the parameter. And then we check for error. The error check is very similar to what we have there. If we can convert it to our own error then it is written like this. If not, we return internal server error passing the error. If not, we return response success response. You see, the code to handle the request is actually super easy, but the error handling is where we have like most of the most of the code. If not, you are basically off by. In short, I want to show you. Let's say we are. We want to be silly enough not to do error check and we delete this. And, uh, maybe we just delete this as well. So the code will come down to something like this. But it is always better to have the error checks. Okay. And that will be it for the video. I let me go to the top. We added the handler, the server type, a new, um, a new server function, um, functions to help us create an instance. Register method to allow us register some handlers and then a handle, um, request to allow us to handle JSON RPC request. And that is it. I am not going to commit or check out yet. In the next video we continue because we still have a we need to be able to start the server. So I'll see you in the next video.
