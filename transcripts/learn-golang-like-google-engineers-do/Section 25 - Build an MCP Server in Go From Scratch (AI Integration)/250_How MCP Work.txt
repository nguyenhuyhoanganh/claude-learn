# How MCP Work

In the last video I showed you what we are. We are going to build in this section of the course. And uh, before we start coding, I want to explain basically what we are going to do now MCP model, context protocol. It's, uh, it's a very simple protocol. Um, if you have done any sort of HTTP implementation before by yourself, that means you should be able to do this. The reason I choose to implement a subset of it, um, we won't implement everything end to end, like following the protocol into details, but we are going to implement it good enough for the AI to be able to use it. Any client can use it. So that's the most important thing for me. One of the most exciting aspect of the protocol is that it uses RPC. The part is, uh, is really, um, something you also hear in G. You know, that is the the procedural core aspect of it is you call methods in the system. So there are functions that are registered for you to call to be able to call them over the wire. The JSON aspect is the difference between this, the JSON that the MCP uses and the regular JSON that we have is normally, JSON doesn't have any kind of restriction. That means it is possible for me to present my JSON in any how I want it. But in a protocol design, you want the data structure to be the same all the time. Like you need some kind of headers, you need some kind of formatting. You need a sometime typed, um, typed fields. That means when something should be an integer, it should be an integer, not a string. Things like that. So the, the JSON, uh, allow us to do all that. So when you come to this documentation, there are like a lot of places to look at. If you if this is your first time and you don't mind coding in Python, maybe you can go to this build and MCP server and then you can look over what they have. I checked out some courses on Udemy, and I noticed that some of them just took this tutorial and turned it into a course. And they are selling and quite interesting. A lot of people are buying, but I don't do things like that. I want to make sure that we go into it in detail and see. Then another important place you should check out is this SDK. Case there are as this case here even go there is an SDK for go, but I'm not going to use the SDK. The SDK is going to make your life easier. It's probably more mature, but you won't understand what is going on. And I want you to understand what is going on. That is why I want us to implement it then. Tools. This MCP inspector is very useful. It is very useful because it's, it's it's way better for debugging than using the AI. With the inspector you can run the tools by yourself. So we are going to take a look at how to use it as well. Now on the architecture side it is um, how do I explain it? The most easiest way to think about it is this your AI systems, like Claude? Claude is your is an AI system, but at the same time, Claude is also active in the cloud desktop, not the cloud AI. The cloud desktop is acting as an MCP client and a client. Basically, just like your browser, know how to construct a request and send it to the server and then the server interpret that request and respond to the client. It's as simple as that. So Cloud Desktop, the host system know is an MCP client that know how to communicate with the servers. Your server can provide access to any kind of resources. Like for us, we are going to provide access to our e-commerce system. You could have as well be providing access to maybe a database or something. Now, how does this communication happen in the context of web? When we build rest API, everything goes over HTTP. Yes. It is also possible for your Um MCP server to work over HTTP is called streamable HTTP, but more commonly is we also have this stdio transport which is standard input and output transport, which is what we are going to use. Basically what it means is this. Let's go back to our code. For instance, when you when you run an application, if you for example, when I type make run here your software have three files open. So I'm going to take this out. But you have three three open files. The the zero is um input I think zero is stdin. Then one is stdout and two is STD error. This is very important for for designing. Let's go back to the browser for, for using Stdio um transport. We are going to rely on this stdin and stdout. When your client want to send message to the server is going to write the message into the server stdin. When your server want to respond to the client, your server is going to write the message to the stdout. If you have any logs, anything you want to print to the screen, use std error. Because even if it is not an error, because if you write anything to stdout, you are going to corrupt the JSON data. This is super important. Very important. It is in the specification. Let me take this out. So when you go into the specification The the thin. The layer is very easy. This is the PC version is 2.0. You can click on it. But like I said I'm going to break everything down. Now go to the specification and then you can um check up check the transport, say STD um communication over standard input and output like I mentioned. Then it explain it. Here we say the client launches the MCP server as a subprocess. That means it is running it as sort of part in its own space. Then the server reads JSON rpc message from its standard input and sends message to its standard output. As you can see, this is what I just explained a message are individual JSON RPC requests notification or response? Now he said that messages are delimited by new lines and must not contain embedded new lines. This is very important. Then he said, the server may write you can write anything you want to write to your standard error for logging purposes, even though it is not an error. Doesn't matter. Just write it to this. Clients may capture forward or ignore this logging. And here is a very powerful warning the server must not write anything to its standard out. That is not a valid MCP message. The client must not write anything to the server's stdin. That is not a valid MCP. You see. So the warnings are very clear. And this is it. I'm going to ignore the streamable http because we won't use it in this course. And after that, the rest is really, really simple. In order for you to um, use it, you will construct Jason that look like this. So basically it's going to have this. Then an ID can be a string or number. The method that they want to execute and then some parameters which we are going to do. Then the response also look like this. It's going to have the JSON RPC version and ID this ID if it is a notification type of message, the ID is optional. And then you can also have possible errors. The errors can have code message and optional data. The result can be optional as well. Then like I said notification doesn't have an ID and then that's it. Um, the lifecycle of it is uh, basically uh, there are different phases to the operation. So client make an initialization request to the server. Server will respond to the client that it can initialize and then initialize the notification is sent. After that, the normal process of help me um client will send a list of tools to execute, then the server execute it and respond to the client. If they keep going back and forth like that. And that's it. So let me show you an example. So this is an initialization request that the client sent. And then this is an initialization response that was sent by the server. So as you can see it's just JSON back and forth back and forth. And that's it. In the next video we are going to start building the project itself. And basically that's it. I won't be referencing this. Um, I won't be referencing the website too much so that I don't confuse you. But in case you need to quickly check out anything that you think, ah, I didn't get right. Or maybe I miss you can just come to this website on Modal Context Protocol. I'll see you in the next video.
