# Add A REST Client

Okay. Welcome back. In the last video, we finished the implementation for our MCP server. In this video I want to add a very small HTTP client. So we are going to be using Rest. The reason I chose this Rest framework for our HTTP client is um, just for the benefit of those coming from like Python, Java, C sharp, um, in go as well. We have a lot of libraries, great libraries. This is one of them. You can go to the this is the path in GitHub. But this is the main website. As you can see it offers so much very beautiful um HTTP client. But of course what we are doing in this video, you can use the standard library to do it. There is nothing special. We are only going to make, um, get and post request to our e-commerce endpoint, which we know for sure we can use the HTTP library in the standard library, but I wanted to show you this, so I am going to use it now in order for us to use it, I want to write a very small wrapper that is specific to our project. So we are going to use almost everything from here. But I mean when when I say everything, I mean the things that we need. So let's now come here, check out a new branch seven add HTTP client like this. And I would come here in the client let's add client or just let's call it HTTP actually. So internal client create HTTP. The first thing I want to do is to create. The first thing I want to do is to create the client itself. So let's call it Rest client. in in it. Let's add the rusty client. So client is, um, this is the rusty. I'm going to bring it in. Don't worry. Then the base URL for our e-commerce API default token. This is the JWT that we're going to use. And optionally if we want to use the JWT or not. So use token is boolean. And lastly the logger. Um, since we don't have this included, the code is not going to work yet, but um, we can actually go ahead and bring it in. So let me open the GitHub. How do you install it? Okay. Let's go to the website. Uh, upgrade. Okay, here's how we can install it. So go get since it's in GitHub. Git hub.com/go/resti. And let's bring the version two. And then it's been downloaded and that's it. Our IDE helped us to import it automatically. All right. With that in place let's add a constructor that we can use to create this client. And in it let's create the Resti client itself. So rest dot new. And then let's set the base URL to this URL that we are passing in. And let's set a sort of a global content type which is going to be JSON. And then it supports like events. So on after response. So after the response has come back from the request, we want to log something to the screen. So like this. In it I'm going to log it log add a log here and then return. Uh I think we should also close this out. This is very useful because when when you get your data you can print out things, uh, to look at in the console. And after that we can just return the client instance and that's it for the constructor. So far so good okay. Let's add another method called prepare. Um, a small method called prepare request that we are going to use in the post and get method. So let's come here. What do we need. Let's let's get the request from the client. You remember, this is our client, and this is the request object. If if use token is true and the default token is not an empty string, we want to set the auth token. And then we can return the request. The only reason why I have this method is because of this logic here. So conditionally we can use it. So now let's add the width token. So anytime you want to use the you want to make a request with token you have to call this with token first. And look at we are returning rest client so that we can do method chaining. You will see how it works. So it will be possible for you to call the client. Then with token for example. Then you can now say get something like this. This is how it's going to work you know. When you call with him, I want to clone the client. I am cloning the client because if we don't clone it, when you set the token already and you want to make another call that doesn't need the token, then you are going to use a stale token, something that you have set in the context. So the clone allow us to use a fresh version of this client. So we set it and then we return the clone. After that let's add two methods get and post. So we are only going to make get requests and post requests for this, um, video, um, this program that we are building. So let's come here. So for the get you need the path we want to call for example slash orders then parameters. And if you have any parameters you can pass that. So let's grab the request from the prepare request If the parameters someone passes a parameter, we want to set the set query params on the request. So. And then we can just make the request. So response and error equal to request dot get like this. And then we check for error first. This response can also have error. So let's check for it. And then after that we return the bytes. So the body is a byte you see. And that's it then for post post post is also very similar. This is the body and the path. Then let's grab the request. So we grab the request. We set the body. And honestly this is not super important since we set it up there. But for clarity I want to make sure that we emphasize that is JSON that we are sending in the body. Um, so I put it here for clarity. And then of course after that we can make the request and then the same error checking pattern that we have here. That is what we are going to do as well. So we check for error. The response already can contain errors. So let's check for that as well. And then we return the bytes. Honestly that's everything about the rest client. It's not complicated at all. We could have as well just use the rest client directly. But this wrapper especially with the with this with token that we are doing, it's very good that we are wrapping it like this and that's it for the client. So I'm going to say git status. Git add git commit. And then add rest client okay that's it. This is very small but quite elegant. And that's it for for it by.
