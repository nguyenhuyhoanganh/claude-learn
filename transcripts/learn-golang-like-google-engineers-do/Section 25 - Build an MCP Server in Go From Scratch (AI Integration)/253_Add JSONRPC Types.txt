# Add JSONRPC Types

Now we are going to start, um, implementing the core protocol itself, the RPC itself. So before when you come here, you search for error handling, for example, you are going to notice that the protocol defines some error constant that look like this. So all the errors that have codes like basic error types like well maybe it's invalid JSON, it's an invalid request object. If method doesn't exist it's maybe it's an invalid parameter etc.. You have error codes like this. So that is where we're going to start. Here in our terminal I'm going to check out and um to add JSON RPC types. I think it's a good name. Then over here in internal inside Json-rpc. I'm going to right click and then let's start with Arago. And in the air let's make it call it errors. Yes. So errors.com. So let's create a constant and add some error code. So for error path for passing error this is the error code. And I got all this from the documentation there. So we have four invalid request. Let's add another one for when the method is not found. And then let's add another one when the invalid parameter. And lastly if it is internal server error or internal RPC error, however we want to call it they are all negative numbers. Okay, with this we are still going to come to these errors that go to add maybe some helper methods later, but uh, let's keep it that way and create another file here called um Types and for the types. When you make a request. Let's go back to the. I promise I won't be going back to the documentation too much, but I, you know, let's, uh, let me put a request. When you make a request, there is always this JSON RPC method and then parameters that can be sometimes they are not sometimes not there including id id will not be there because sometimes when it is a notification and then it is important. I left a comment here to know that this ID it can be a string number or no, no fractional numbers are allowed like to 1.2 or something like that. Okay. And how do I know all this. Look at it here. You see this is it. And an ID. And of course error can be part of it as well. So it's uh, if this is really where you can find all the details. The parameter? It matches exactly what we are having. Let me move it out like this. You see, this is a JSON RPC, the id, the method that we are going to use parameter this part. Protocol version capabilities and all this ones and the client info we are going to this is this is when with connection has been established and the one will start making two calls. Then we construct this. So we are going to have this part, this internal part in the MCP server itself. So let me minimize this. And then we continue here. Then the rest one as well is very similar. The error. Um let's create the error type here. The error has called message and data I showed you. And then after that this error is something that we also want to make it compatible with go error. So let's implement the error interface which is this. Okay. So far so good. Basically you see we are only creating structs that will help us produce the JSON that we need. And there is nothing technical. There is no mathematics involved, nothing. We want to know when a request is a notification. When an ID is nil, that request is considered a notification, right? And that's it. And then let's add a method to validate the request. What do I want to check in the validation I want to make sure that the RPC the JSON RPC is 2.0. And I also want to make sure that the method is not empty. So that's it here. So I check for the RPC. And then I also check for the method. And that is the only validation I want to do. There's probably more you can do. Then let's add a function here to create new response. And create new response. This is always a constant. Then you just pass the result if you have any error. And then an ID and that's it. And the new success response is success response that uh, that um, you want to send, you can have an ID and the error will be here. The result will be empty. I don't need a result inside. This is like, uh, sort of, um, sorry. I say response, error, response. So for the success is this. So let me put a message there. So create and then. Sometime when you record for too long you start having fatigue. So for the success of course we need a result the ID and no error for the error. We don't need the result, but we need the ID and the error and that's it. So these are just helper functions. So, uh, let me scroll to the top. We have the request. We have the response. We have error. And these are this is to conform to the go error interface. And this is for us to be able to check if a request is a notification. Basically if an ID is nil and a little bit of validation that we did and then some helper functions here. And that's it for the types. So and that will be it for now. Um git status. Git add git commit JSON r c types I hope so far it is not complicated yet. Like I said, it's super easy to do and and I'm happy with the progress we are making. So in the next video we continue.
