# Add MCP Server Types

In the last video, we forgot to commit our changes, so I'm going to commit it. So this is the file git add. Git add uh JSON rpc server. That's it. Now we need to start working on the MCP itself. The first thing I want to do is to create a new file. By the way we need to check out the branch. But let's go ahead and create a new file. I think we should create types dot go. Before I start writing the code, let me bring over the browser again. Basically, we need to create types that we can use to create this internal. After the after the connection has been made we need types to produce the JSON for everything here. So initialization then for two. So let me show you an example. For example this protocol version capabilities client info. Then if you go under tools we need to. For we need to have uh we already have this one wrapped from, from the JSON IPC. So for example let's say you want to execute these two we need to be able to produce this JSON like name title, description, the input schema and things like that. That is what we are going to do. So I'm going to put all the structs and then we see how it goes from there. So let's go back to the IDE and start working on it okay. Let me check out a new branch I don't want to forget. So I'm going to say git checkout dash b mcp. This is five MCP. Are types like this. Okay. Let's start. The first one. Um, so every all the structs I'm about to add, don't think about it too much. All of them is just to produce the JSON that we we want. That's it. There is no logic, just structs. This is the initialize request struct. The protocol is going to be there. So we need to now add a client capabilities and then client client info. What I just showed you. And this is a client capability. So um there is also two can be in here but I omitted it. Most of these we are not even going to use it. But I want to have I want to be as close as possible to the full implementation. So we just have to worry. So client info, client info, basically house name and version. I think I want to switch one more time to the browser for that client info. Let me go back. Uh, for the client info you see name, title and version. So if you like, you can add all the fields that you want, but some of them are optional, so it will work. And then what again the response. So initialize um results. So we need to add a server capabilities and server info. And this is the interesting part right. You do something client send some kind of request server response. That's it. So let's add for that. When client when the server is responding to the client, the server usually respond with the capabilities. What it can do in terms of the tools it supports where the resources and prompts. But we are only going to need tools for this um. Tutorial. And the last one is prompt capabilities, the prompt and the other resources. They are quite interesting naturally, but if we want to have everything, I don't have the need for all of them, because this is for our e-commerce and we're only going to need the tools for the e-commerce. So if you want, you can actually take all this out. And then after that, after the client prompts capability, we need the server info. We need the two. This is for me. This is the most important thing for this video. The tool. So we need to specify the name of the to the description and the input. So let me put the input schema here. Input schema usually have the type properties. And if it is required and then the property type description items. And that's the item. What else. The some of the responses uh. Toothless result. And then. Call to requests. I think the last one would be to resort and the contents. This is everything. If you are confused of anything at all, if you just check the documentation, you find that, for example, let me go to the tools now and show you something. Let's open, let's go to the tools here. Let me let me try to reduce it. I want you to understand it as much as possible. If you go under tool. This is in order for us to list. So this is the tools right. We are going to see the two. For instance this is this object. It has a name um description. Then the input scheme the title. And I didn't put the title. But if you know the name is more than a name and description is more than enough, you can if you want, you can put the title if you want. Then the input schema. You see the type properties and then require the require this an array. The same thing that we have here. You see it's just that. So you can choose to meet some things and it is fine if you like I said, um, just do as much as you can do to make it work and that's it. All right. So that's the types for the server. We need to start implementing the server. The server itself is super easy. But before we write the server we need some kind of registry to register tools. Um and then we can come back. So type everything out and then see you in the next video. Let me commit the changes first. Then. Add the MCP types okay. That's it for the video. Bye.
