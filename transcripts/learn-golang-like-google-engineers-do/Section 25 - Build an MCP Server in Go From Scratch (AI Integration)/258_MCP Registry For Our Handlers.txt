# MCP Registry For Our Handlers

Okay. Welcome back. Now let's add the registry for our tools. So I'm going to git checkout. Slash MCP reg three. Then let's come here. Registry. Okay. Here. So what is it two. In the context of our MCP server I defined it two to be any function that let me show you any function that takes a context, an argument like this. And then it returns a the call to result and a possible error. This is the signature of a tool. All the tools we are going to design. Like for example let me show you, let me give an for example list Released products. Uh. Add to cart. All of them. They are going to have this signature that we have here. They are going to have the same signature. Now that we have the two signature, let me comment this out. Let's add a register. So for the registry we need to add the tools. And then we need to add the handlers. And let's add logger as well. Because the logging is very important we can't print anything to the screen. We can't use printf or or log dot print something. We need to take total control of where we print our stuffs. After that, let's add the. We need to be able to construct this so we can initialize this, uh, important uh maps. So new registry and I can A return. So let's make the two and like this very easy so far, right. We need to be able to add to this map. So if we create a two we need to be able to add a two to the maps. So let me come here. Uh another function called register. So we take the two and the handler function. And then let's add a log and that's it. So why do we have map for twos and handlers. Honestly separation of concern here. For example I'm about to write another function to list all the tools. And I'm only interested in the tools. I'm not interested in the handler, the function itself. So I just intentionally separated them. Let's add the method now to list or to be able to list all the tools. Um, in order to list the tools, let's make this is what we are going to return. So anytime I do something, you can just pause. And if you want to think about it a minute, you can think about it. So let's just loop and then we append. We append and then we can return. Right. So far so good. Since we are only returning the tools we don't care about the the function itself. That is why I separated them. So it's uh, this is like our memory where we keep our things database. What next? We need to be able to execute the tool. So we need to be able to call this function that they register. So this function that is being registered we find it in the map and just call it that is so easy to do. So execute. The execution it takes context the name of the tool. We are going to find it in the map like this. And then the args we know that we can pass the eggs to. It is so easy. So handler and the comma okay pattern. So handler okay. If we don't find it we return an error. So we return an empty call to result and error message. Else we just call it and that's it. This is it for the registry. Super simple. There is no no kind of mathematics or complex documentation that we need to read. This is it. So basically we are registering a function in the map and then we are executing that function when the time comes. That's it. Now with that in place I can add them to git git commit add mcp mcp registry. Right. Then in the next video we continue the implementation of our MCP server. See you there.
