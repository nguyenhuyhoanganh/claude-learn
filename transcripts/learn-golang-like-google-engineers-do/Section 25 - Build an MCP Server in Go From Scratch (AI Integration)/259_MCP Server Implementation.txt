# MCP Server Implementation

Okay. Welcome back. Now let's add the server itself. So let me git checkout dash b seven mcp add server and then create a server dot. Go inside the MCP folder. All right. Before I put the code let's look at the protocol version is 2025 618. So that is a constant. We need that. And then um the name of the server is going to be I just call it uh, e-commerce MCP server. You can give it a better name. Um, and what else. The version of the server itself is AV1. So this is our version 1.0. This is the first version. So if you like you can give it anything. Now let's define the server struct itself. Uh, we need we are going to need the JSON RPC server here. So let's bring it in. We're going to need our registry that we just created. And we're going to need our logger. After that we are going to need to create new server which is which will be. Basically we create the RPC server passing it the logger. And then we can return the server like this. Let me let me for now I'm going to just return the server. Here we need to add a method that we haven't created so that this when you create a server you can register the the the handlers that you want to execute. So all the methods that you want to execute. So let me come here now. Um, this is our server. So I'm going to say s. Register handlers. So the handlers are the initialize. Initialize. Initialize initialize the two lists and the call. So we are going to have the four of them here. And we need to call it here. So immediately you create this you have uh sorry I need to call server like this. And that's that registers it. But we need to create this this for um this for method. So handle initialize. When the client send an initialize request we are going to call this when the client send initial uh initialized. We call this when the client asks for the list of tools available. We call this one when the clients want to make a call for a tool, we call this and that is very straightforward. So let's implement this for tools now. So let's come here. Let's start with this one. This one is almost an empty method. So let's start with this. So what we want to do we need the we need the initial initialize request. We then we need to unmarshal the parameters into this. Basically the data that is coming will martial it into this. You see this is the payload that I showed you earlier. And if we are unable to do that, we can return an error. But if it succeed let's print out some logs. The logs are very important when you are dealing with this because debugging can be tricky. And after that we are going to return. We're going to return the initialized result. So let me return it here. Look at what we are building. Initialize result is basically the is uh where is it. I think we can go to lifecycle. Initialize the client must. So the client will send this and then initialize result. So let me put it by side like this. You see protocol version capabilities. We are only adding like tools here. The tools if it has changed or not. This is something you can make dynamic if you want. But you see we don't even we are not adding this for other ones. But server information is important. We added it and that's it. And if you want, you can add additional instruction. This is it. It's just JSON that we are returning. Okay. Let's hide it. And then the next one is we don't I'm not going to return anything to the client. Um, I'm just going to put a log and then like this. And that's it for that. Um, initialize when it is initialized. What else we need when the call to list the tools. Remember we added the. Let me go back to the registry. We have this to do that for us. So let's use it. Okay. Let's call that registry then add some logs and then we can return it and that's it for that. The last one is to handle the call. So when they The call. Um, when they say two slash call, we can handle it. So it's also not difficult. Let's create the call request. You see the name and then the arguments. Then we have the parameters into it. After that, let's log something to the screen. Don't don't take this login for granted. It's super useful. You will see now because look at the. Let's go back to the types quickly. Sorry. Let's go to the top of this page. I think the definition is in the type right. Where is the registry. Yeah sorry it's in registry. So open registry when you want to call this function we need a context and then args. I think we already have the args. We just need the context. So let's go back to the server. We can come here. Let's create a context. Then the result of the execution. So basically we call the this is the args. We have the name. And then we pass the context. Let's check for error here we check for error. If error is not nil we are going to return the call to result. We are going to return the call to result. But we are going to mark it as if error is true. So basically if error is not near, this is where it ends. Then we added the log. Then we return this this JSON that we are returning this call to result. So the content here is a slice. But you just set the message and then you mark this as error. True. The reason why we could have return an error. But I want you to differentiate between this one and this error when we return an error, it shows like this function was unable to execute completely, for example when JSON failed. But for this it's a. It's not a failure from the data that came in, it is basically a failure to execute the two. Maybe the API cannot was not responding or something. So this is a better way to return that error. And after that of course, we just return the result and name something that I felt I should explain is this. So let's go to registries. So why do we have a registry here and then. So why do we have this one here this two func and then types. Um let's go to the server. Why do we have it. And and this handler. Think about this. The one inside JSON RPC. PC. It handles the core protocol level like, uh, least two, least two, uh, initialize things like that. That is, that is what we have registered here. Let me go to the MCP server again. Sometimes when you switch around, it can confuse people. So just pardon me for that. That is what we have. You see our PC server register method. So it handles this initialize initialize listing of tools and all that. That is the core protocol. But when we want to start executing the tools for example calling API, if if they made a call, if they make a call to call a specific tool, this is where the MCP comes in. It will bring in at that point. We are now in we are now using this, uh, signature to call it. That is the difference. So I separated the MCP operation shown from JSON, RPC, which is the core protocol, and that's it. So I don't want it to be confused. All right. Okay. And then honestly that's everything about the the server. Um, I want to pause a second. I will scroll back to the top. And then I'm going to scroll down gradually so you can copy everything or type them out. Of course you have access to the code. And then we can continue in the next video to wrap most of the things up. We are almost done. Okay. That's it. So let's uh git status. Git add git git commit r mcp server, something like that. And that's it for the video. I see you in the next one. Bye.
