# Add Configs

The first thing I want to do is to do git init so that we are in the git repository. This is everything we added. I'm going to do git add git commit m initial commit. When you are looking at the git hub, the main would always contain everything. I'm going to merge everything back to main, but for now let's keep branching out. So uh config. Okay, let's add some configuration. Now um here in uh let's add config folder configs and add a config dot. Go inside. Okay I hope so far no confusion. And then in the in in this place let's add a config struct. So this API url will be the URL to our like. For example url to our api v1 to our e-commerce auth token. Um, this will be JWT for the current currently logged in user. Uh this one. When we are going we have to just for the sake of this, um, course we are going. When we log in, then we copy the JWT into our environment for cloud to have access to it, and then some error, some log levels that we want to use. Um. Yeah. The transport for now since we only support, uh, stdio or we just keep it for now. No HTTP for now. Okay, then let's add the load. We have seen this loading method before. So basically we call dot load to load the config for us. And in in cloud we can name the environment variable auth token or jwt. The I, I choose the I have auth token and jwt because I'm still thinking of adding um, OAuth two authentication, but the cloud desktop currently does not support it. So if I find a client that supports it then I'll add water. So let's make it future proof. So basically what are we doing if we find the token from here great. If we don't we check here as well. So in your environment you can set auth token JWT underscore token. After that we build the config and then we attempt to return it. This get env is basically small function down here. And then we can An attempt to grab it from the environment, just like we have here. If we don't have it, if it is empty, we can return the default. If. If not, we return the value and that's it for the config, right? I hope it's not confusing yet. This is just a struct that we are constructing here. Returning it. And the reason we are calling this get get env here is because we want to be able to pass, uh, we want to be able to pass default values like, uh, like we have mentioned. And that's it for the config. So I don't want to forget I call git add git commit. Add config loader. And that will be it for this video. In the next video we start messing with the rpc, JSON, rpc types itself. See you there.
