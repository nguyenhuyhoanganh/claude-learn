# Formatting strings

In this video, I want us to start looking at formatting strings. We've seen it before with printf s printf, and in this video I want to show you a little bit more. Let's start by creating a type called config I struct okay. And the key is going to be a string value. Now the value I want the value to be anything. And if you set is going to be bool. One interesting thing sometimes this can be like you can use interface. This is a type which means anything. Don't over use it because when you want to use the type, you need to cast it to the concrete type that you want and it can be a little difficult to manage. Now. Now I'm going to add uh, so I call this C config item. Let's add a small print out. Now this is where the string formatting comes in. You've seen it, seen some part of it before. I'm going to return format dot print f. Well this printf formats the string and return the string. So sprintf there is a printf that print to the screen, but this one format the string and return it. So we have the key. We have the value. And I want to add the is set which will be of type C. The t that we are using here is for us to print for is for boolean. It shows true or false for boolean. And I can come here and add that as c dot is set. So this is for string. In short let me make a list here quickly. Um you have you have V which is uh the default formatting. It prints are for is for struct. It prints the general object. Sometimes, uh, if the type implements the stringer interface, it calls the string on it. V is sort of. If you don't supply anything internally, they use it. Then there is also like this plus v this one. When printing struct it adds the fields like you can see the name of the field. I think we've used it multiple times today. And then there is a uh like this percent dash V is a is a this is a ghost syntax representation of value. Um, you can play with that as well. We if you want to print a type to know if it is for example a string, bool, integer, whatever. You can use this percent t capital letter t. And you've seen this before for string and for integer. I don't know why the integer is d, not I. Okay. And then for float this is f for float. It can be interesting. For example, if you want to print two decimal places you say percent dot two f. This will give you two decimal places. And then for boolean which I just showed you now is percent t. And for the double quote string you can use Q. And if you want to print percent itself inside the quote, you can escape it using to doing 2%. So that's it. Uh, let's go now to main and do um, let's create some of the values. So I'm going to add this code and then print the status. So you see we have um. C is always true. Doesn't matter. So we have the the what you they take away from the video is basically we have string here. This is going to print one decimal place and then integer which is a pot. And this is boolean. We can print it out. Let's run the code. This is formatting. Let's go to two main and print it. You see um the application this is the string. The version you see is 1.2. If I change this to two then it will be one to show 1.1 instead. You see. So we can use this to control the, the precision. And the integer was printed here. And the boolean okay. Um what else. We can create some. Let me create some of the config about three of them and let's print some of them out using what we have explained. So I'm going to bring this one here. Print it out for you since we need the two. So let me print everything out. Um and I'll bring this other one out as well. So let's print them now okay. This the value of v is used here directly. Then this 1% percent. So this is percent that we have here plus this. So this one you see we are able to print the percent because we are escaping it here. And then to show you this value we're printing this out here like this. And look at it. Then lastly this one, it shows you are that is coming from Maine. This is richer for like debugging, as if you want to do that. This one. If we want to print the struct field I told you. Yes. Here. We can do it this way. So one now um, is behaving that way because of the stringer interface that the stringer method that we implemented. If we take it out, you see the field. So that's it. Um, yeah. When you are, when you start working on the code base, most times you only going to be using, um, this is very common. This is also common. The string the the integer this and this. Most of the time this is what you use. Sometimes you are going to use this as well. Something that I didn't mention I want to just show you quickly is this, um, let's say we have this error. I can say error. Errors dot new. I can just doesn't matter. The message test. Okay. We can do font dot error. F it accept all the same printing format and we can say error here. Here is the error on port and then integer. And then after that I'll put this w and then pass it like this. It doesn't like the capitalization here. So let's see. So you see that this returns an error but accept formatting as well. This is the integer the w is for wrapping. That means we are wrapping this error into this one. So you have it wrapped. Thank you for watching. And in the next video we are going to talk about something called runes and then Unicode.
