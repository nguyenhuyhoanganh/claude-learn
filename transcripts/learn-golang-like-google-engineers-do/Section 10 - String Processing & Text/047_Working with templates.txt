# Working with templates

Hello and welcome back to the video. In this today's tutorial we want to look at text template. So I am going to create a new folder text template. Text template is for us to format our um output in a certain way. It can be HTML, it can be XML, whatever you want to use. But today we're just going to use plain text. So I am going to create a main dot. Go in there. And the package we are going to use is coming from let's say import text slash template. This is it. First let me create some data. Here we have this email data recipient name sender name subject body. These items is a list. And then just a little flag here the demo here we basically want to deal with different data types. So we have a slice. Or you can call it a list. And this can demo. We can use this to demo a loop. And this one is just an int. Okay. Looks good so far. Next let me print something to the screen here. Um. Something like this. Now let's create the template itself. Image template. If you want you can use double quotes like this. But because I want to write multiple lines I'm going to use the Backtick. And then I can go to New line. Here you can type HTML whatever you want to type or even read from a text file. I'm going to format. This is the subject. Let's format the body. And here before I type more, let me explain something here. You see this syntax you are seeing here? Um, the data is going to come from here. This object is going to come from here. And the dot is the current context. That means if I pass this object, the dot becomes this object. If you are looping, for example, if when we loop through this object in the loop, the context will become the object in that iteration. So that is the only I think that probably the most confusing part of using the go template. So this dot is the current object you are passing. Okay, now let's do some if statement. If dot you see if we have items. That means if the if the length is greater than zero. Now I can do loop. It doesn't matter if you put indentation or not. It's is going to work. So let's say related items something like this. And here we loop range. And here you have access to the item. So inside the range I can put a maybe just dash and print the item. You see we are using just the dot because at each iteration you are going to have access to the current object and the value will be dot after the range we end. And we need to also end the loop. Sorry, we need to end the if statement. So this first end is for range. The second end is for the if statement. That is one. Then this one I want us to also look at uh if statement again. So let's say if. Greater than. And this is the interesting part. So this is already defined in the template is greater than. If is greater than on read zero. And this is how you put it. This doesn't use the regular construct that we are used to. So just know that it's as if if this is greater than this. A little bit strange that they chose this syntax. Uh, you have then you can put uh, like this on read and then we can end as well. Or maybe before we end it, we can say else. You have no messages. You know, something like this. And I can say thanks. Put a sender name. I think I have the sender name up there. Good. And that's the template. You define your template and you can, you know, pass data as you wish and send out or store or do whatever you want to do. Like I said, it can be HTML anything. All right. So let's process it. How do you process it? Very simple. You define the template the template object which I'll call tmpl. Then pass it like this. Template dot new. This will become the name of the template. The name doesn't really matter. Email, message or whatever I want to give to it. And then pass. This would pass the test that we defined up there. Now this become your template object. Of course, it also returns an error, so we need to check for it. Because we are in main, we can panic or exit the program using exit. You know there is nothing to do if we are unable to compile the template and we can. I can print out the error message here. You also have access to this error function on the error object if you want to use it. Now that you have the template object, you can pass any data to it. Let me create a sample data and with this sample data we can go ahead and do something. Let's execute it. And the question is where do you want to print it? When you say when you call template object dot execute you have two options. One is This one is to just send it to the output screen that is your console. Another one is to pass an object to it, like, um, maybe let's do this one first. Then I'll do the second style. We check for error again and we can exit. So if error is not, knee is going to exit. Else we are going to see this template with this data outside here okay. So I run it. And look at it. You see. Very good. Look at the list. We have 500 messages. If we change this to zero we're going to see different message. You see you have no messages. What if you don't want to print it out and you want to maybe, um. Just do something to it again. Let's say let's come here. Let me show you something. Open this. In the the function, the template that execute it takes an IO writer, which is an interface. That means any object that implements this interface can be passed in there. All right. Good. There is one object that we have used before that I know for sure, which is the Stringbuilder. So var output. Strings dot builder. We know that this has. This implemented it. And then I can pass it like this. Now our output contains the template for you to be. For us to be super super certain I'm going to turn everything to capital letter to uppercase. And that's it. And run it. Run it now. You see everything has been turned to capital letter. So what have we done? I want to explain the most important steps. First, you need the data. Just your data. And it doesn't have to be, um. It can be. You can have an interface or whatever here, like, uh, uh, sample something. You know, it can be an interface like this. It can be embedded. Struct can be almost anything. Right. First you need your data. You need your template. You build the template object, you pass your data value that you've created, and then you execute. During the execution process you can choose where to output it one. You can put it in a buffer like we've done here. Another option is to also send it to the output which is OS dot. Um that would put it on the um console. So it's your choice right now. We've, we've, we've done both, but this is the basics of go template. Now, before I end the video, I want to show you one last thing up here in the import. We are using text slash template for command line application. This is good, but if you want to deal with HTML you have to change the import to like this. Honestly, this is not going to change our code. It's going to work. You see, nothing changed. It still work. But the good thing is when you the the HTML version, uh, provides some security around HTML and all that. So if you are dealing with web application HTML, please use the HTML version. If not, use the text version. And that is it. For this video I want to scroll down carefully so you can see it. In case you type in along, you can copy everything out you see. And that's it for this video. Thanks for watching. I'll see you in the next one.
