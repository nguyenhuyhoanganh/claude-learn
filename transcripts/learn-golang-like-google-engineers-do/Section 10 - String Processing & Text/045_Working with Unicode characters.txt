# Working with Unicode characters

Hello and welcome back to the video. In this video I want us to talk about go support for Unicode. You in order to deal with Unicode in go you can go to the documentation. This is pkg.go.dev/unicode. Everything about Unicode is here. All right. These are all the functions. This digit is graphics. Everything you can use to work with Unicode. They are all here. But here is the most important part. Now let's let me create a new folder three dot. Code. And I would create a main dot. Go inside okay. When you create a plain string in go let's say username and test. For example this is Ascii. This is an Ascii characters. Each of them is being encoded using a single byte. So this is four byte. That is why when I print the learn the length would be four. I can go to Unicode three more. Then the length will be four because it is four bytes. Don't think in terms of characters, think in terms of bytes. This is four byte string. If I replace this that with um, let's say I pick this. Uh, I think this is Japanese word, I pick it. This is three. Right. So it is one three. This encoding here is different. So because it's encoding using UTF eight this might be three bytes three by three byte making it nine bytes. If you are thinking in terms of length, you think that, okay, maybe this should be three as well. But this is not doesn't count character. It's counting the total number of bytes. If you want to count the characters it's a little bit different. So this is bytes that is being counted in Ascii. The reason why if you for example, you say people should type username and if it is a character it's up to four. The reason why the four characters matches the four bytes is because of single byte encoding that Ascii is using, but UTF eight is a variable length encoding. That means it starts with a single byte up to four bytes depending on the language. For for the. This one that I pasted is this using three bytes, and that is why we have nine. Some would use four bytes or one byte in the case of Ascii. So why? Why is this a problem? It's a problem a little bit because let's say you come here, I'm going to comment, duplicate and comment one of it out. I when I come to this place I say test okay and username when I do a slice of it like this. You see it gives me 116. If we want to know what that is print f. I think we can print the character itself. One one says let me put a new line, but you can see that it's t you see the first one. The second one. Is e it is possible for us to index into it, but we cannot do the same thing here. And this indexing is working because it's a single byte, but for here it doesn't know how to do it. It doesn't. What is printed doesn't even make sense. You know it doesn't. You can you can try to slice it. You see, it doesn't make any sense. So in order for so any time that you want to index into the string, you don't you cannot do this. You want to do the range. For example let's say I equals zero I less than the length of username I plus plus. And we print out this is not a good idea in go because of the Unicode code point. So we are converting to a string printing. And it doesn't make sense if you are want if you want to loop over the string and print out each character, the best way to do it is to use the range. So for. v equal to range username. And then we can print the v here. And this gives you of course you need to. But you see that is three. And that gives you the. Let me try to use string and I give you the right output. This is very important. Each of these is called the rune. So if you want to define each of them I can come here now and say uh, let's say data, I can do a slice of rune and take each of them. And put them like this. And this is going to work. Come here like this. You see? It works. This is the best way to process your data. Your data in go is when you are dealing with Unicode. The Unicode package itself has so much. So we can go call the Unicode package Unicode dot and it has some cool package. For example is you can see is digit. If this is a digit and what does it take. It takes a rune so we can pass one of it. Then you know the one at index I. And there is we are not printing anything out. In short, maybe we should, uh, add it to this place. Yeah. Um, I want to pass the V inside now and print it. And none of them is a is digit, but I think it's letter is also there. Is later. You can even check if it is a lowercase or not. They're all letters. Are they lowercase? Since I don't speak the language this is how to they are not lowercase so they are all uppercase. So is it upper? Yes. Oh it's not lower. It's not upper. Interesting is the letter. You see. Yeah. So you you can use each of um you can use this method, the methods from there or the functions. And to Play with the Unicode. But the takeaway for you is this any time you want to interact with the individual room, use range. Don't use the classic C style for loop that's going to corrupt your data. If you are checking for length of username. Uh, this just checking for length is not going to work because this is three characters, but the NUM, the what you are going to get will come back as nine because of the encoding. So you need to use a different technique to check which we can look at later. There are libraries that help you accomplish that, to count these, to sort of know. For example, if you know that this is encoded using three byte each, you can divide this by the by the byte and then you get the real character count. That is how to do it. So thank you for watching and I will see you in the next video. I will show you a little bit of regular expression and then we can move on from there. Bye for now.
