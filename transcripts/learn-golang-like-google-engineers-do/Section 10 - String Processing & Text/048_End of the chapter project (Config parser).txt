# End of the chapter project (Config parser)

Hello and welcome back to the video. In this video, I want us to work on a small project, and it's a way for us to solidify everything that we've learned in this section. So I'm going to go to the folder and I would create a new folder and the main dot go, that's it. Now I'm going to paste a config here. Let's paste this config. I want us to write a program that pass this config into a map for us. It has some restriction. It must be in quote and you know but we are going to support like comment. You see comments can be here and look at this long password. But there is also comment and something like this. So let's look at how to write a program that pass it into a map for us. This is something you can use for your program to handle configuration. Um, now during the regular expression section. I mentioned that when I am working with rigorous regular expression, I like to use this website to at least visualize what I'm working on. I will be using regular expression to extract some of the values, and I have spent some time playing with it just to understand it visually before I implement it in code. It's an incremental progress. This is how I work. Let's go back here and um, let's start working on the function itself. Maybe we come here. I'm going to fold this. Let's come here and say func. I can say path config something like this content would be string. And we can return the map map of string. And a possible error. First thing I want to do is config and I'll make the map and return it. I config. And I'll return for the error is going to be ni for by default at least this give us a a set of a good state to work. No errors yet. Okay, I'll paste the regular expression, then I'll start working on it. I want us to read the text line by line, and decide if we want to take the line and pass it or not. And the way to do that, you can use a scanner. I can come here and say scanner. And there is a scanner in Buffalo. New scanner. Now you need to specify a source where you want to scan from. If we want to scan from standard input we can put OS dot std in here. For instance. This would have allowed us to scan from the standard input, but we want to scan from a text the string that we have and there is something in the strings package strings dot new reader. This allow us to scan from the string itself. For the sake of reporting errors, I'm going to count the lines that I am on line. Norm, this is zero and let's start scanning. Go on infinite loop and we can increment the line count. And then let's get the line. And I would dim the line. So let's say trimmed trimmed line this one. Trim the line I am using the trim line because when I want to report an error, I can just use this raw data that I have now changed to report an error. Let's check if it is an empty string or. Or it starts with that hash. That means it is a comment or an empty line that we don't want to process. What do you do? You type, put, continue and we skip that iteration. All right. After that, the regular expression. Of course. We've seen this before. Remember we have the regular expression here. So re find string sub match. We call this function to help us find it. Now something I want to mention here before I continue writing code is you see this keyword must. Anytime you see this keyword must in a function name, that means that function would panic. It's a convention in go. It's not a compiler feature they put it must mean something must happen or else the program would crash. So this must compile means if anything is wrong with this regular expression, our program is going to panic at this point and the program would crash. And if maybe you are thinking why would it do something like this? Well, think about it. If the regular expression is invalid, there is nothing else makes sense here than here. So really, you should make sure the regular expression works first before you do anything else. Okay, And we have done some matches with Wheaties, Neal. That means nothing. And I want to return. Um, let's. We can skip it, but I want to also print a message to this console if it is need. We want to try. Um. Want print? We can say line number and this, uh, maybe line number. This is output is invalid. Something like this. Okay. That means we didn't match it on the line number. And, uh, I want to use the line instead, not the train one. Okay, after that, let's let's get the key from the first index and the value we need to do a little bit with that value string. We have to look for it. If, if the second index is not an empty string, that means the value is there. We can grab the value from there. Uh, else if. But the third one is not an empty string, then the value is there. Not a very good way to look for values, but it works. Else we find it at the fourth position. I can do it like this. Okay, now we can come here to add the key to it. We are not trimming or anything. We just leave it that way. After that, I think that should be it. If there is no error with the regular expression, maybe it can work. Um. We pass it, check for error. If we. Yeah, we can exit, or else I want us to loop now. And then print each of them out. Let's see it. Okay. It looks good. Um. another keynote value. The bug is true. Um, let me use, uh, printf because I want to say the key equals value. Maybe there is. You can use this q something like this. Let's put new line. Okay. Good. You see the key will allow us to add this, uh, quote around them. Um, so app name we have them. Looks really good. You know, um, let's go back to the function itself and see what we did. This is a function. The most, I think unusual thing here. Maybe 1 or 2. One is a regular expression is quite long. Like I said, I use regex to play with it. Uh, it took a while actually, to get it right. And then we use the scanner, which we haven't seen before. But it's an interesting idea that it takes an IO reader. And we can read from any source that implements that interface. So we are currently reading a string like. But like I said, we could have read from the standard input then this. These ones are normal. We just use the scanner to scan line by line. Here we grab the line and then you can do anything that you want to do with the line. And we try to process each of the line by looking for the value and putting everything together. So that's it for this project. I think the takeaway for the for this project is this, um, incremental development. Start small with what, you know, debug, make sure you're printing output to see the result of what you are doing, and then you refine as you go along. And that is it for the video. Uh, for this one. Thank you so much for watching.
