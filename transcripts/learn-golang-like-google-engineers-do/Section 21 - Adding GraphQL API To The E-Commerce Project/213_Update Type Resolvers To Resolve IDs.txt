# Update Type Resolvers To Resolve IDs

Welcome back. Now let's update some of the resolvers starting with the ID. So git status where we are already committed. So git checkout for the three graph. So that should be 43 graph dash update ID resolver. Something like this will do. And then I'm going to open. Let's scroll to the top. Open our file browser. Let me close out this close out this. And then let's focus on the. Schema generator types. Let's see what was generated for us. So these types dot resolvers or go this is where we are going to focus. So let's update it. What does it return. Returns a string. We know that for sure. Um this is a cat resolver. We know we can do this. So basically you can return sprintf. So this is going to be num um, convert this to a string and then object ID. The ID is here. Okay. And then for the card resolver user id there is a user ID here. And then we do the same thing. So return sprintf. And then you put the numeric formatter object dot user id is here also is user ID. All right. This one as well. Very similar. We have the ID here as well. For the category resolver ID we also have ID here. So I'm going to copy this one here and paste here I think. Yeah. What else. For the order resolver ID we also have an ID there. Um and then which one order resolver user ID. There is a user ID here. So we copy this one. Okay. So far so good. Order item resolver I'd there is an ID there. So we copy this one and paste here. This the behavior is predictable. Product resolver ID is just object or ID as well. Category ID category product resolver category id. There is a category ID here. So I'm going to do it like this. But here would be category ID. Okay then product image resolver. There is an ID in there. So we use the ID. And then use the resolver ID. There is also an ID just copy and paste it. Which one again I think that's it. These ones are getter methods that are more like helpers that was generated for us. You don't need to do anything here. Just leave them alone. And that would be all. I think I'm going to scroll back up slowly, and if you are typing, you can pause it. But I'm going to scroll back up slowly just to make sure we review, um, id id okay. And the category id and then object ID object id okay. Looks good. So far I don't see any errors. User id user id okay id id user id id okay. It looks perfect. So I'm going to go here git status. Git add. Git commit r. Or not add update type resolve as id something like this. And that's it for this. In the next video we're going to do the dependency injection itself. So I will see you there.
