# Add Middleware Helpers

Welcome back. In the last video we added a schema and we generated our code and that was great. We also sort of looked around to see what was generated. And everything looks good. So I'm going to git add everything. Let me expand this a little bit more git commit add operations something like this. Simple. Then I'm going to do git checkout 42 graph. This time around let's say add helpers. Um, the reason why I name it helpers is because I want to add a little bit of, uh, helpers, something like a middleware. It's not a middleware per se, but I want I want us to be able to extract the user ID, the user role from the context. So let's go to resolver here and create a new file. Click on resolver. Create a new file. Helpers go okay. Um what happened? Why does it give that squiggly line is because of the package name, so let's give it a package resolver. Okay, let me close this side now and increase the font size. Okay. First thing I want to do is to create a new variable called unauthorized. So when you try to access an endpoint that we can't find your ID in the context, that means you somehow not logged in. So we know that you are not logged in. So let's put it like this. Okay. This is it. So this is a custom error that we've created unauthorized. Now we're going to add one function called get user ID from context. It's super easy. So it takes the context and return a uint and an error. Okay. So with text context let's import context here. So this one. And then how do we do it. Let's say let's attempt to extract the user ID from the context. If it is nil we know that the person is definitely not logged in. If we are able to convert this to a uint, then we are good. We return it and then error should be nil. If not, we can return zero and unauthorized. And that's it. Okay then let's add another. A very similar function. But instead of user IDs for the user role. So. So get get user role from context. The same thing is you return the role and the possible error. So grab the role. We check if it is nil. We know for sure the user is not logged in. If we are able to convert it to a string. So if we look at what we did here, if we can cast it to a string, we return it. If not, we know you are not logged in and we return the same thing like this here. Lastly, one more method to check if you are an admin. So is admin from context, so we can grab the role now using this function, we check for error and then we return if it is an admin. But this is a magic string that is not very good. So I can say admin. And let's go up here create a constant admin role equal to admin. And that's that is better than having the admin type here okay. And that will be it for the helper. So if you in case you want you are typing along I want to scroll back down a little bit um for you to type it out. Very simple functions. And that's it for this. So in the next video we are going to start updating the ID resolver. So I'll see you there.
