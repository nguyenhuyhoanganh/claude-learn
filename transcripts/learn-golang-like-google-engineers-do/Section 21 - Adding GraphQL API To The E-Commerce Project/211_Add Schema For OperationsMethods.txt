# Add Schema For Operations/Methods

Hey. Welcome back. Here is where we left off. In the last video we added the types which is user auth, payload and so on and so forth. And I can scroll down to see I haven't changed anything. So let's go to the terminal and do git status to make sure we are good. So 41 add. So we are going to git add git commit add schema for types I want to use the shorter English and dash b 42 graph add operations. I call it operations because this we are about to specify a list of functions that the user can call on the API. So like calling get product. You know get your profile Getting categories, things like that. So those are the operations that can be performed. Some of them are we have two types of operation. One is querying like asking for things. Another one is changing things which is called mutation. So most times it's two. Uh, but GraphQL allow you to do more like subscription. But we are not doing subscription in this course. Uh, subscription is actually quite cool. This is a digression, but it's an important concept in case you want to Google it. It allows you to connect the client to the server in real time, and the server can stream data to the client using WebSocket. And maybe different kind of bot subscription is cool. Okay. So let's go to our folder again schema right click and add schema okay. So this is where we put the operation. Let's start with the type. So like I said we are going to have query and we are going to have mutations. So all the queries will be here, all the mutations will be here. Queries return data, mutations changes data. So for the query, the first query we are going to have is called me which which is basically like get profile. So a way for you to get your profile. So after that we're going to have products. These products function is going to take arguments like the page and the limit. And we are even going to set some default arguments. Then I will explain why I introduced that product connection in the types. So this is products function where two arguments so page and the limit with default value of one and ten. And then is going to return product connection. Now why did I introduce product connection type. Let's go in. Let's click on it and go there. What does it contain. It contains product edge and page info. Now when you have when you want to return a composition of data like, um, you have data that you want to compose in a specific format, you are allowed to do it anyhow. You want it. For me, this is like a node and it's it's nothing special. This is not a special GraphQL type. It is me that named it Product Connection. And then edges will contain the product edges. Product edges is just a node to product and this is how you can change things up. And of course lastly we have page information which is nothing but the pagination data that will be part of it as well. So this is quite, quite powerful. Okay. Let's go back to schema. And then so we are back to the schema dot graph QL. And then here inside the query let's add for getting a specific product. After that let's add a way to get all the categories. So when you go to let's say you type query and then you say, uh, maybe I should type outside here. Let's say you type queries and you say categories, and then this will return you, uh, array of categories. But of course you have to specify the fields you want. So categories and then cut all those orders is very similar to this product as well. And lastly order to get a specific order. That's all for query right? If you are right typing along you can type it exactly like this. And I have explained this. The same explanation goes for this. So I think this is good. Now in the mutation, what people can do, what they can perform, they can of course register. We know that the registration takes a register input which we have defined already and returns out payload which we defined earlier. Right. They can also log in. Again, they can refresh their token, they can log out. Right. And don't forget some of the some of the functions or the methods or the operations, whatever you want to call it. But most times I call them GraphQL operations. Some of them can return boolean like this, which is really, really cool. And then after that, uh, update profile, we want to give the user the ability to update their profile. You see that the difference between query and, uh, mutation, most of the data input to mutations. They are actually like complex types. Y query mostly receives simple types, but honestly nothing stops you from having let's say uh, product request here that has a field and all that. But most times this is how it is done. Okay. Update profile. We have done that. Categories we want to create. Be able to create categories, update category and delete category. That's it. Similar then for products we have the create products. We have the update product and delete product. Add to cart. Update cart item and then remove from Cat. And the last one is creating order. So create order. When you create click on Create Order it returns you the order. But think about it, there is no argument. You don't need to specify anything and this mirrors almost what you have in the rest version. If you go to the rest version, create order. It takes nothing. It doesn't take any argument. Internet is a little bit slow, so this is not loading very well. But if you look at it, it doesn't take any argument at all, which makes it really, really good. Okay, let me try again. If it loads. Okay. The postman doesn't load so we can leave it alone. And with this in place, uh, I think we should go ahead and run it. This is how I make. This is a command we want to run. If you want, you can run this. So see here. Look at what I'm going to do before I run this command. I want to go ahead and run this one. Okay I'm going to go ahead and paste it. Run it. And it ran. No errors. But sometimes when you run it it will tell you to first of all run this one. So this is not super compulsory except when you get the error. But in order not to even get the error at all, I just put both of them in the same make target and then you can just do this. And it worked. Um, by the way, you can run the generate multiple times. It doesn't overwrite anything. Your code should be there. Before we end the video, now let's look at what was generated for us. So in here, like I said, if you go to. Remember this file that we specify. Look at it here. Right. And the package will be graph which is here. Then model we say is going to be model models underscore gen is here. What else we say that the graph should be in. The resolvers should be in graph slash resolver in the resolver package. And it should have the type dot resolvers or go. And we should have for we have the main resolver we have for schema and we have for types. And all of them match here. So this is the types and this is the schema. This is the schema. So you have this one this one and this one. There is nothing um this is just inputs here we have for the types and then the schema that needs it. All right. What else. Uh let me open it again. What else did we say with the rest. Is this mapping. So if you open this you see that it's going to reference the audio package. And then nothing much is here because it's using the mapping and the node is pointing to things here. So this is super powerful and I am very happy with what we have done. So um, if you open the resolver it's currently empty. This is where we are going to have our dependency injection here. This is where we are going to implement all, All of our resolvers. And if you look at the input and the output, all of them are pointing to our dtus. Are dtus. Everything here is pointing to our database. You see D2, d2, d2, everything so far. D2 d2. This is the product connection which uses the D2 internally, so it's fine d2 d2. This is the other connection. It uses the D2 internally. This is fine D2 and that's it. Honestly this is the most difficult part. We are almost done actually because think about what is going to happen next. We inject all our services in here, and then we just call the services from each of these functions. And that will be our GraphQL API. So see you there.
