# Implement Order Resolvers

Before we continue, I want us to add and commit our changes to git. So git commit R implementation for. Resolvers. Okay. And that's it. For that I need to go back to my terminal again and add and then create git checkout 49. This time around 49 graph. Simply implement um all the resolvers like this okay. With this let's start with the create order. I'm going to scroll to the top somewhere at the top. Uh, here. All right. What do we need? It's very simple. We grab the user ID, right. The same thing that we have been doing. If we have the user ID, we can attempt to use the user ID to fetch. Sorry. We can attempt to use the user ID to create an order. Remember, in order to create an order, you must have a cart. So we assume cart exists and the service know how to handle all that. We check for error. If there are no errors, we return the created orders. If we have any error, we return these errors here and that's it for the create orders. The rest is the orders and order. So orders is for us to get all the orders for the currently logged in user, or that is for us to get a specific order based on the ID that belongs to the currently logged in user. So let's go ahead and take care of that. The orders would look like this. So we need to copy this code which I want to make one more change to it. But we'll come back to that. We need to copy this code. So keep scrolling here. All right. So we first of all grab the user ID and return the right error type for us. Okay. By the way I kept changing that error because I don't want the wrapping anymore. I changed my mind because I think this is enough. So we just return unauthorized. If you want, you can leave it. It's no problem. Okay. This is for the pagination. The same thing that we did with, uh, with the products. So if you want, you can just copy and paste it from there. Um, it's repetitive, but it's okay. After that, we can attempt to get the orders. We check for error. And then if there are no errors at this point, you see we have other connection. Remember we had product connection with AG. So we had to create the edges. So we allocate memory for it. Again we loop to fill up the edges, and then we return the data here and that's it. It's simple, almost identical to the product. Um, I don't think it should be confusing because this is just a struct. And then we are creating the the edge. Edges is just a, a slice of pointers of other edge which if you click on it, other edge is a node that is pointing to our other response, which is this because we are reusing the DTO. So it's not it shouldn't be that complicated. All right. Before um, I okay, let's do the last one, which is this one here. So let's grab the user ID. All right. And then after that we pass this ID here to an unsigned integer. Okay. Now that we have the unsigned integer we can use it to get the order. If we run into any errors, we want to make sure we return that error. Else we can return the order that we just fetched. And that's it for this function here. I am satisfied I don't want to make any changes here, but in the orders and in the product, I want to show you something. Logically, we say that the default of p and L should be one and ten, and then we rely on this value. So what happens if this value is zero. Then we are going to pass zero and whatever number here. So we need a way to we need to apply some guard on this. There are two ways to do it. One way is to add a small function that know how to sort of normalize this input for you. If not, you can come here and say if um, if p less than equal to zero, we set p to one. If limit. If L is less than or equal to zero, we set it to ten. And that way we protect ourselves. We can take this same code, copy it and go to. Products here and put it right here. It must be down here, not up here. It has to be here because we are setting it here and we are checking again. Like I said, this is very repetitive. In short, let's not be lazy. Let me refactor it quickly. So what? We need to refactor it. We can um we need to get these two inputs which are pointers. So I'm going to come here. Let's go to the bottom of the page. Uh function. Get uh paging numbers something like this. So this is the p is int. And the l is also int. And we want to return just two numbers int and int. Okay I'm going to copy paste it here I will return the return p and L okay. And okay because we named this parameter p and l. So let's name them page and limit. To be clear actually here um these are the defaults actually here I don't care about the default anymore. I'm going to say. Um okay. So let me do it like this. I'm trying to reduce the code here and I know it's possible. So let's do something like this. And can't say. Your P, the number here doesn't matter, actually. But anyhow, let's keep it for now. Let's keep it so P one and ten. So we did this. We checked for this as well. We do the guard and we return it. This works. But if we want if we want we can probably do the know what. Let's take the challenge. But we need to have the variables. Are p l equals zero zero okay. And then yeah but we need a guard here as well if uh yeah we need a guard. So honestly nothing much has changed. So let's keep it that way. Now we go to the top. We go to the top here. I no longer need everything here, and we don't have to repeat ourselves. And then we can just say. Equals this and pass it. Page and limit. And that's it. Then we copy the same thing to products. And remove everything here. Come on. Why should we repeat that? So that's it for the video. All right. And then I'm going to commit the changes this time around once and for all. Adding Pim. Resolvers. Okay, see you in the next video.
