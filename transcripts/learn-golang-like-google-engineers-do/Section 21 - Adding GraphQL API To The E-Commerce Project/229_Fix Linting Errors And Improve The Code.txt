# Fix Linting Errors And Improve The Code

I want us to make some improvements. So if you run make lint, which is this command, basically we have some errors, right. So I want us to fix it. So I'm going to say git checkout dash B 57 graph fix linter issues okay. Now let's run the linter again. Make lint and let's go to the top and start. Um first of all this one looks like something we can do right away. So inside services go to the order underscore services go which is here we are now using this one. Let's see is there anywhere we are doing date formatting anymore. No. So we can comment that out okay. That is one. So that was in order underscore service or go. We remove the default date format that we are now using. I run the linter again. And wait for it. It says that in the helper. So inside graph resolvers helpers go. Remember that method that we wrote the get paging numbers. Open it up so that we can change it like this. Because it's the same type. All right. So let's run the linter again. This is how you fix linting problem. You do it, you run it, you do it. You run it and say consider giving a name to this results. So which of the result is it talking about. Let's see. So say on unnamed results. Page lint I think it's talking about this because it's look at it. It has int here in here. And that might be confusing. So maybe we can name them a page limit. No. So page let's say page number and then page limit something like this. And if you want you with this one too in place you don't necessarily need the return. But just naming them. Let's see if that's enough. Yeah. Good. So it said that uh, could be replaced with uh okay. This is really interesting. So we should take this one out as well. So sometimes the linter can be really petty and you want to follow it, uh, as as much as you want, You know, if you are tired of it, you can get out of it. But most times the suggestions are great and we is happy now. Okay, the next is in graph QL dot go, which is something I know was going to come up. Let me show you. Remember, technically you shouldn't do this just using a plain text as context key. When you do this. If any other part of your code is using the same key, then you have a clash. Basically what you should do is you create a type and then you use that type as a context key. That is what we are going to do. So in order to fix this code, let's first of all go to let's go to YouTube and create a new file. Let's call it context. I'm putting it in utils here because I want to share the code between you two. So we are going to use the code in our server. We're also going to use it in, um, I think in the server and in the GraphQL as well. So let's create a context type. So this new type here, the context key is just a string. But we are aliasing it. And after that let me create a constant. So we add for user ID user email the role and for gene. So instead of it just being a string here like this it is now a new type. This new type is even though it's a string alias it doesn't equate to it. It's they are not the same thing anymore. So this is why it's good. Now that we have that in place, let's go back to where the linter was complaining, which is in internal server GraphQL. So this is where it is. Open it up. And then this is where we are. So basically we need to replace this right? We replace this with the U2. So the user ID here, we need to replace this one as well with the one from the U2. We need to replace this one as well. And then that should be it I think for let me make sure that we didn't mess it up, you know. I think we let's add the one for Gene as well. All right. So with this just make these changes with these ones in place we are ready to fix the last one. But because we've made these changes let's let me let's run the ring and see what's going to happen. Because we made these changes. The code is not going to work. You don't have a linting problem again, but you have a logical problem. The problem is in the in the helper function where we extract the user ID since we no longer use the plain string. It's not going to work. So if you go to graph, let's close everything up here now and then graph. We remember resolvers helper. This is where we were. This is no longer going to work. Look at it. We are just calling the string the plain string. And it's not going to work. So we need to replace it with the one from util. Basically just the key. And then the last one is this role. And we do it like this. And that's it. That should fix it. I'm going to run the linter one more time to make sure that we haven't messed up anything. Good. No linting error. But how about the code working? Can it work? CMD api. All right. The code is running at least. So let's run it. Now invalid token. So let me log in. That one is not a problem. Let's copy the JWT here. I'm going to come here and paste it. Paste the JWT. It is important to test with the protected endpoint because that is where we need the user ID from the context. And that works fantastically well. Raoul. And I created that. The things we added. Everything is working fantastic. So we made that improvement. Something else I want us to check is let's open. Let's open the server. Open server. Go. Here we have this database which I don't think we are using. I'm not completely sure, but since we have all the services Injected, we probably don't need it. So I am going to delete it here, delete it from here, and then delete it from here. So I deleted it from the struct, deleted it from the arguments and I deleted it from the initialization here. But of course this is going to cause an error. Since when we go to main. Now let's try to run the code and see what happens. For sure. So you need to go to API slash Main.go. And here we need to take out the database. Okay. Let's run it one more time. Okay. Everything is running. We can attempt to run some query and that's working very well. So no issues. Let's run the linter as well. And the linting passed. Okay. What of, uh, what else can we check? How about the logger? We're using the logger. Let's open one of the services. Open services auth service. We really now using the logger that much. But I'm going to keep it anyway because ideally you should use them. And if you are going to use. So here is the interesting thing in the server. In the server itself I don't see a lot of places that we need the logger, but most people are going to need it. So let's keep it in there. Yeah. And that's it for the improvement. You see I love quality and I'm not going to leave things, um, properly tested. So this is it. Um git commit. So git add git commit linting issues. And that will be all for this video.
