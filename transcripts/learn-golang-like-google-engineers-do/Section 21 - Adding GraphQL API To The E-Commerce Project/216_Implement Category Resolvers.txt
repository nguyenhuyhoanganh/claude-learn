# Implement Category Resolvers

Before we continue with the implementations of the other resolvers, I want us to commit the changes. You want to do git status. All right git add git commit and then add all resolvers. Something like this should do. Then git checkout dash b 46 half implement category resolver. Something like this should do for this one. Okay where do we want to start. So I am in schema dot resolvers dot go. And we are looking for. Let me scroll to the top to see where we are. We've done for register login refresh token logout update profile. This is where we are going to start. Create category resolve. I'm going to close this side of the IDE so you can see everything. This is an admin action. So the first thing we need to do is to check if the user is an admin. Remember we wrote this function earlier that we can use to check if the user is an admin or not. Let's open it up just to refresh your memory. See. And then it's using this one. With that we can actually, um, it is safe to return this because it returns a boolean. And you know, it is either a false or true for function. So we can return this error if the user is not an admin. After that let's call the create category um on the product service. So we call the product service dot create category. Passing our input. Let's check for error. If there are no errors we can return it. It's very simple. Most of the work is done in the service, so we don't have to do so much. This is also an admin action. So the code is going to look very similar. We check for the admin. We pass the ID. Remember the function that we wrote last time that will take a string and return us a you int. So this is it. Now check for error. If there are no errors we can go ahead and try to update the category. Then we check for error as well. If there are no errors we can return the updated category. You see it is super super simple. And then next is the delete which is very similar and also very straightforward. Let's make sure I mark it admin action. This is just a comment. So for people to know let's make sure the user is current user is an admin. Right. And then there is something I see that we are doing that is inconsistent. Now let me unauthorized this better. And let me make sure that we have the same thing. All right. After that of course we we can pass the ID as well. Check for error then call the delete category. Also check for error and then return true and nil. That means there are no errors. All right. Good, good. The next one is um categories basically for us to fetch all the categories. It's a public method and we don't need any kind of check inside. So let's look for it. Keep scrolling down I think it's somewhere down here. Look at it here. Categories. So we grab all the categories from the database. Check for errors. If we run into any error, we return the error message and nil if not. Let's create a result slice of the data that we want to do. Then we loop. Basically, we build it up and then we return the result. And that's it. Super straightforward and simple. Most of the work is done in the service, so we don't have to do so much. All right. And that will be it for this video. Before I end the video I want to scroll back up. So if you are typing along I hope you have typed this out. You can pause the video briefly to type it out. Then I'm going to scroll to the top just to review what we did. Delete category as well and then update category. Create category. That's it for the video. That's everything we did here. Actually, if you think about it, if you have seen the way we did this, you you can probably do everything by yourself because it's quite straightforward. We are only calling the services checking for error and returning data and that's it. All right. See you in the next video.
