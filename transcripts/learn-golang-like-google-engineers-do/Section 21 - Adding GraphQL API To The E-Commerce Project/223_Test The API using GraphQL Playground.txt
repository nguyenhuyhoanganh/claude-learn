# Test The API using GraphQL Playground

In order for us to test the project, I want you to do a couple of things. Uh, I don't think we are going to write code in this video, but I want us to just run the code and see. So, um, open schema, GraphQL. So to see the mutation, these are the mutations that you have, you know, and then these are the query. So we can query for me products product categories cat orders and order. And then we can run this mutation. So these are the functions that we have. There is a problem. The problem is in the middleware. But we're going to we're going to take care of it I want to show it to you in order for you to run the server first of all type go mod tidy. All right then if you have make you just run make Docker up or you can run Docker compose. All right. So I'm going to run Docker up. Now one of the problem with this is the server is already started in here. So this is it. And this is not going to work. So I'm going to stop it. It's not going to work because we didn't rebuild the thing from scratch. We are now rebuilding the product from scratch so it won't have the latest changes in the Docker container. We need to rebuild the image, which I'm not interested in doing right now. What we need to do, make sure you stop that one. That container, this app won't stop it. You can leave every other things running. Then here you can either run go cmd slash API or type make run and it looks like everything is running so far no errors. We can see all the routes. Then we have playground and our GraphQL routes that we added. Okay, so now we can open the browser. And this is the interface you see slash playground. But look at what's going on. Immediately we opened it. It says that authorization header required and why we are in a public endpoint. Okay, so how do we send the authorization headers in this playground? You come to these headers you type out put an object here. This is JavaScript authorization bearer. And then you put the the JWT token here. If I send it it says the token is invalid. I cannot do anything even when I try to for example query for me. I can query for me because of this. But what if we take our postman and we log in? Right. And then we copy the JWT from our postman. Let me copy it very well. So I use the postman to login, copy the JWT and paste it here, and then send it and look at it. It worked. So with this, we can access protected endpoints. You know, um, let me bring another one so we can get profile. We sure can update the profile with this. So let me bring the the GraphQL query for updating profile. So this is just the name. This is the query itself that we define. So let's update our profile. This is just the name. And this is the mutation. The input is the input and then the object. So first name last name and phone I'm going to send it. Uh see the phone is different and it has been updated. So it's super fast. Very good as well. What else can we do. We can try to get our categories. So this is the query. And this one returns an error say created at within. We didn't define the createdat we didn't do it. So let me take it out. We're going to fix that later. But aside from that, it should work and it works great. So technically our API is working, but we have the problems that we cannot log in from here because of the middleware. And it is really tricky to fix. Um, and I want us to fix it. That is what we are going to do. We can log in, we can register from the GraphQL. And I want us to be able to do that. So we need to perform some magic. If I go here now um it says invalid credential. But solely hitting that level is is reaching this level because we have this. If I take it out, then we have a problem. Imagine in you you don't need a JWT to log in. You should log in to get JWT, not the other way around. So let's fix that in the next video. But here's the takeaway for this video we tested. The code is working. We don't have any errors so far, but we know that we need to fix this public and protected operation. So let's go and attempt to fix that in the next video. I will see you there.
