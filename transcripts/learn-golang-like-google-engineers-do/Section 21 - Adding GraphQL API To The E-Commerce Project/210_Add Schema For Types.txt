# Add Schema For Types

Okay. Welcome back. So I fixed it. I installed the GraphQL plugin into my IDE and the view is way, way better now. So this is where we stopped in the last video. We have the inputs. All of them okay good. Now um let's add let's say git checkout dash b for the one graph. And this one would be for types add types schema something like this. And then we come here right click and types dot graph q lls okay this is it. So let's start adding the type. Let's first of all start with a scalar of time. This is because we are going to need it. But then let's add the type for the user okay. And then let's show basically the user is this is the response. Now what we are sending out when someone make a request, this is what we are sending out. So this matches the user response correctly. And if you come here you should see everything that we are naming their match. This one here it matches this model here. So basically this is what this that this definition that we have. It matches this here this user response and this this is why the name is user. So you see the connection right okay. And then after that we add for auth what payload then category. What else. Product image. Let's add for product image I think I can close this side actually close this one down. Yeah. And then products. Okay. You see, I want to slow down and explain a little bit more because product images is an array of product image. You see how we structure it. So it is something that can contain um product images. And I told you this pound sign that we put here is to say it is compulsory, you know, in the data. So it has to be there. And so far every other thing is flat. The data structures are flat. Flat. This is just the first array that we have introduced. So type cat item is cat item is product here. This is why. This is why GraphQL is very powerful. You see cat item. The product is a product. So when you want to fetch it you can when you fetch cut item, you can fetch its products and then you can also fetch its category. And then you can fetch product image, you know. So it's really powerful. Then the cart itself and cut items is also an array that should contain cut items. So this cut underscore items an array of this so far. Honestly it's not complicated at all. And then other item is similar. We'll see how funny the other item is. So when you fetch another item, you can get the product. When you have a product, if you go to the product, for instance, you can get the category, you can get the product image on a single call. You can do this. Then the order after the order we we need something called product connection that they are going to use. We are going to use to join all the product. So let's define the edges. Okay. And then other edge and then page info for pagination. This is for us to connect the edges together. All right. Um honestly the schema is quite self-explanatory. If you are typing along I want to scroll down slowly so you can type everything out. If you have any questions, please. And please don't forget to let me know. And again, you can copy paste something to some of these new AI stuffs to explain it. But um, there's not it's not more than this. When we generate the data, you are going to have go types that represent each of this. All right. I'll see you in the next video where we add, um, operations like what people can actually do in the API.
