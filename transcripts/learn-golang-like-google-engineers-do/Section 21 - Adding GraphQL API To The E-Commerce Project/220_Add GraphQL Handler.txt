# Add GraphQL Handler

We have made a lot of progress. We have implemented all the resolvers before we do the next thing, which is to mount the resolver. So create GraphQL server and register it with our server. I want us to review this code a little bit. Um, then I can mention something. So I am in schema resolvers go which is here. So inside graph resolver schema dot resolvers go. You know, um, we did the registration logging refresh token and everything. So I'm just going to scroll down for us to see everything that we've done, including checks for admin and then everything. You see. We completed all the resolvers. Okay. And that's it. So this is this was the helper function to help us create version number. So we create we completed the reservoir. This is a huge step for us. The next thing I want us to do is to create the GraphQL server. The same thing that we did in the last section of the introductory to introduction to GraphQL. The same thing we are going to do here. So I'm going to now say checkout. Dash. B 50. What a golden number. And then create. Something like this. Okay so where do we start. Let's go into servers I'm going to close the graph. Now go into internal server. Right click new file new go. File graph go. We are going to put the server information here. So let's create a function called create GraphQL handler. It's a GraphQL handler because the GraphQL server is technically HTTP handler. That's it. So we need to map it right. The next thing is to create our resolver first. So our VR that's what I'm going to name it. And then I have resolvers I think is Dot. Yeah. And let's see you see all the arguments that we need to pass. So I need to put it a new line and then we can start. I think the first argument is the auth service which we can get from this. So auth service. What nest auth service. User service. Product service. S dot cat service. S dot order service. I think that's it you see. And if you are wondering where this is coming from, remember we injected all the dependencies in here. So that's it. With this in place the next thing is to create the executable schema. So schema dot graph which is coming from here. And then we pass in our resolver here. So this is how it finds our resolver. Um, if you want to take a look at this config you can take a look at it. There are some other things that are also configurable in there, but this is the most important one. Now that we have the resolver let's create the handler. So you call handlers new to create it. Now before there used to be another method here new default server. But you are not to say you shouldn't use it anymore, that's why it is deprecated. See? So don't use this function in case you see it. Now that we have this the handler, we just add some transport to it. Just exact same thing that we did in the other video. So we added all the transport set the query cache um limit. So let me import this I think this import is coming from GraphQL handler. This one. And then the last one. Maybe. Yeah. Then there is one more. So we they use extension this the automatic persistence for the query. Let's set it as well. And that's it. So this is the GraphQL handler which then we can return it here. Very similar to what we did in main of the introduction to GraphQL. And after this because if you look at this I want because I want you to understand what we are doing. I'm going to click on this new right. When I click on the new it will take us to this server which is in the GraphQL package itself. When you open the server, let's go into it. Actually, I click on it when you open the server. If you scroll down here, you would see an implementation of the HTTP um box. That's the HTTP handler interface. So it has this server HTTP. This is what makes it HTTP handler. Normally if we are using the standard HTTP package we would have been able to call. For example, we would have been able to do HTTP dot handle like this and pass it. Um, maybe you can call it API or GraphQL or whatever. But since we are not doing this, we are using Gene. We need to wrap it with a gene function. So let's create a function called GraphQL handler. Then we create our we create this handler now that we have and then we return a the gene function like this. And that's it. So this this rapid. So this rapid as a gene handler now. All right then the next one is to for the playground we are going to have a playground where the one the browser interface. So let's create one for it. And then we create the playground. And then we return the gene wrap as well. And that's it for it. So it's very simple very straightforward. What all we did was setting up the resolver creating the schema executable uh or executable schema. Then we created the handler which is aka the server here. And we added some transport. These are like middlewares basically. Uh, so Middlewares and then we return it, and I have shown it to you that it is just a HTTP handler. There is nothing complicated about it. And then in order for us to use it with Jim, we created these wrappers to wrappers that we have here. And that's it for the video. In the next video, we add some middlewares because think about it. Let me show you one more thing before we go. If you go to graph, you don't need to type this one. You just need to watch it. You go to graph resolvers. If you go here, um, get user ID from context. Look at we are trying to get the user ID from context. So how would the user ID get into the context. So there should be a middleware that does that. When you are authenticated check your JWT and get your ID from there. Put it in the context before this function can work. So that is why we need middlewares. All right, I see you in the next video. Bye.
