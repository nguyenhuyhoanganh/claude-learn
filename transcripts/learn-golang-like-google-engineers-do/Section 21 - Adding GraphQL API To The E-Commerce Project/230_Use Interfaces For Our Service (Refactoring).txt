# Use Interfaces For Our Service (Refactoring)

One of the most powerful thing in software engineering is incremental progress. Whenever you are starting a new software, whenever you want to solve a problem, try not to be too complicated when it is too complicated. You are probably not going to solve that problem. My approach has always been start simple. Get it to work. Improve it. You know, what we have done so far in this course is we've been building incrementally. We test it and then we go back to reiterate, you know, that's what we've been doing. If you look at our services so open server go go to the server. Go look at some one of the problems we have. The server currently have these dependencies injected. But the dependencies are concrete type. That means it's a little bit difficult for us to sort of swap this concrete implementation with a mock. So if we want to test, for example, the handlers, we will be required to have like a real connection to the database. Or you know, you have to do way, way more in order to test your handlers. But today I want us to swap out this with an interface. So let's start. Let me show you what we are going to do. Open auth service. So go to auth underscore services or go all the methods that we have on the struct. We are going to extract them each and every one of them into an interface. So we are going to have auth service interface and it will contain all the methods in the service. So let's go here now in the services. So we have interface like this. Then the first thing I want us to create is the auth service interface. And in the auth service interface we are going to start by adding register method. If you look at the signature of this register method it's exact same thing that we have here. Look at it. It takes a pointer to DTO register request and it returns a pointer to DTO or to response an error. Right. The same thing we have here. And from the ID you can see that it has one implementation already. So this is good. Then add the add this one login add refresh token and then log out. And that's it. And you see that we already have one implementation. The next one we are going to add is the user service interface For that one. We need to get profile update profile and that will be it. We already have one implementation we don't need. We're not going to write much code. We just extracting an interface. Then let's do for product service interface. Okay. We need a create category. We need a get category update category delete category. Then we create product get products get product update product and then delete product. Let me close this on the side. As you can see um this is everything here has an implementation already and is coming from of course this product service. All right. Let's go back to it and um, add one last one here, which is the add product image. Okay then cut service interface. With the cut service interface we need to get cut. Of course we need the add to cart. We need the update cart item remove from cart. And that's it for the cart service interface. So far it's looking good. We haven't done anything strange. We are just basically copying the methods that we already have implemented down here. And this is a powerful way to refactor your code. Then other service interface we need to create order the get orders, the get order and that would be it. Then lastly upload service interface which has a single method of upload image and that's it. So we have this interface set up like this. How do we use it? Well before we do that, let's do something. Let's make sure that each of the services actually implements this interface. So let's open auth service I'm going to come to the top here just right here. And I would say we can do it. We can write it here. Actually at the top var I don't want to name the variable, but I'm going to say auth service interface, which is the type. We are going to say that our service is equal. We assign it to it that basically this is for. Let me show you the impact of this. Let's say we come here now for some reason I put a name string it no longer this hot service no longer satisfy the interface. And right there, you know, you don't even need to compile the code. Your idea will show you. And this is a very powerful way to get instant feedback on if you are truly implementing the interface. So because we are not going to use the variable on this side, that's why we don't name it and we put underscore. All right. Now let's take it out with 100% sure that we implemented the interface. We are going to do the same thing for the CART service as well. So scroll to the top of the cart service. And then I'm going to say var as well cart service interface. It's going to be equal to like this. No errors. So that is a very good sign for the order service. The same thing. No Know that this is why the autocomplete can be really annoying, or that service should be here, and then other service interface should be here and that's it. Okay. Product service. The same thing. You see how the autocomplete always get it wrong. Let's say we put the product service and then upload service. Yeah. Upload service interface equal to this. And that should be no. How about the user service. Yes. Let's scroll to the top. User service interface and the user service 100%. We have the interfaces covered. Now we need to update our server. Basically I will open the server. Go open server here. We no longer need to depend on concrete types, so we need to change it to add interface to each of them to depend on the interfaces instead. All right with depending on the interfaces. Instead we then here as well. We have to. Do for everything. Good. One of the advantages of what we are doing is if, for example, um, this is the name here is not very it's not correct. So let's fix it. Good catch. If, for example, we started with this interface, you'll probably be more confused. You know, it can be confusing sometimes when people just jump straight to an interface and, you know, start implementation, start all this jargon, and then you get confused. But with this, you know exactly what we are doing. And don't forget, now that we are depending on the interfaces here, we also need to go to the resolver. So open graph and then resolver resolver. Go. And let's change it here as well. Interface. And to change them here as well. Right. Interface okay. And here of course. Change everything from the concrete type to an interface. Okay. And that's it. Now we have to run the code. First of all, run the linter. Good. We have some errors. So let's look at the errors. Okay. We need to fix some of these errors quickly. And then let's see. Um say that um auth register undefined is pointer to interface not. Okay I see I know why. I know why. So when we were doing this because we are pointing to an interface here, we no longer need the asterisk because it is a pointer. So that was my mistake. Please take them out so it's no longer a concrete type. Take all. Take out all the pointers, please. Take them out. Okay. And that's it then let's make. Let's check here as well. Server dot go. We have to do the same thing here. Okay. This is why the linter is also very helpful because. Good. Then lastly here. And then. Good. Now let's run it one more time. Good. No linting errors so far off. Go! Run! Run. CMD slash API. And let's go to the browser. Run it. Your code is working. You see, we made a huge improvement to our service. We went from depending on concrete type to an interface. And now we are able to run the code. We can mock. So if when we want to test it let me show you one last thing before I end the video. So for example we need in our this is internal server in auth handlers. For instance in order to test this out and this one this interface. This is auth service. You see it's an interface. That means you can put in a mock version, inject a mock version and then mock the return type of this register. And that is how we can test the handler. And that will be it for this video. Thank you so much for watching and I will see you.
