# Fix Public and Private URL Issue

Okay. Welcome back. In the last video, we tested the API and we ran into an issue with the authentication everywhere. We are protecting all the endpoints, including registration and logging. And that needs to be resolved. Um, in the server. This is the problem right now. All the endpoints are um they are protected with this auth middleware that we have. So there is no way for us to bypass authentication for logging in registration and other endpoints. That doesn't require authentication. And this is one of the downside to GraphQL because it's a single endpoint API. If it was um, rest, we could have been able to choose the endpoints we want to protect and what we don't want to protect. But this one is a little bit tricky. So I am going to duplicate the code a little bit in the GraphQL. So inside server GraphQL, um, the server where we created the server. So server slash GraphQL dot go open it up. We are going to keep the default behavior I won't change anything. So we keep what we have here I'm going to sort of duplicate this to add two additional ones. One for public, one for protected only for testing for the sake of testing. So this is for the public. They are going to go to the public, um URL like this GraphQL slash public to access this one. Then this is for protected. And then the slash GraphQL go to the protected endpoint. Now we just added these two. So in case you want to type it out you can pause the video here and type it out. All right now let's go to the server. Go here I'm going to delete everything here and map all the Get requests. So we map the three Get requests that we have. This playground would be the default. This is the public. And sort of explicitly we are saying this is a protected um endpoint. We can take this one out. This is all right. Then we add for the public, um, post request. So this is the Post request for the public notice. What is going on here. This is the public URL. We are only using the GraphQL middleware which is this one. Even though we are calling this remember the IDs. Since we are not relying on the ID in the public resolvers, there is no problem. We can get away with it like this. But we we didn't call the authentication, um, middleware here. So this is really, really good. And lastly we can add for the protected ones. So the protected path which is this default one. We have the auth middleware now to protect it. And again adding the GraphQL middleware which we are using to extract the user id, the user role and the user email. And lastly, we mount the post request endpoint because all GraphQL requests are Post request. That is why we have to add the Post request. The Get request is for us to access the playground where we can send the, um, the payload and that's it. With this in play, we can attempt to run the server and see what happened. I'm going to go right. Go on. Slash cmd slash API. And let's wait for it to run. With this in place, we can open Chrome. Now look at. Before we were unable to log in, we got this authorization header required. I'm going to delete the authorization headers completely. What I need to let me copy this. At first I need to go to slash public. And we are in the slash public now I, I press it. It's a login failed invalid credentials. Well this makes sense. We no longer hit the authentication middleware which is a significant step that we've, um, taken. So let's use this credential to log in. And then password. And look at it. It works great. The beautiful thing about GraphQL is your ability to choose your fields. For example, let's say when a user logged in, when a user is logged in, we only want the access token. We don't care about anything else. Then you can delete every other field that you put in here to only ask for the access token, And we can send it. And this is the access token. This way you are fetching the specific fields that you require for your application. So it's really, really powerful. And that's that's it. Now that we have the authentication let's try it so we can go back to the default. You see the default behavior is still there. This one requires authentication. So I'm going to put. All ization. Bearer. And then paste our token. Then we can query. Let's query me. And we can query for ID and first name I think. Let's run it. And you see, we got the data back. Last name? Last name. Is there something that we need to we need to take care of? Let's say I put createdat. We don't have it. And for me this is an important fields. You know when you have when you are dealing with, uh, data, you want to know when the data was created, when it was updated. So in the next video we are going to refactor the code to add Createdat and Updatedat. At the same time, we are going to modify the schema definitions. We are going to regenerate our schema. We are going to update our DTO. So it's like a complete refactoring to include these two important fields. I intentionally saved this for the last, because I want you to also learn how to go back and change your schema, regenerate, adjust your code, and see the um, the result. So see you in the next video.
