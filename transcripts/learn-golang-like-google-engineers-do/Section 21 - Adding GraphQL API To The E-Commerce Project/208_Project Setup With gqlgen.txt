# Project Setup With gqlgen

Hello. Welcome back. In this section of the course, I want us to add GraphQL to our e-commerce application. We have learned how to use GraphQL. But how do we apply that to a real application? Let's say you have an existing Rest API and you want to introduce GraphQL. Two things I want you to learn from this section. One is code reusability, the ability for you to use your existing code instead of writing things from scratch. And the way we have structured our project so far, we are able to reuse the same logic without reinventing the wheel. Then the second thing I want you to also learn is the ability to look at your systems as a module. So that is um, it's an important engineering concept. Think about what we have done incrementally. We have been building the softwares little by little, step by step, making mistakes along the way. Refactoring our code. Now we are about to add GraphQL. So you want to look at the GraphQL as an independent module so that that way you are not mixing things up. What you have with the rest is okay. That is the base system, but you are about to introduce another module that of course would use an existing feature. So let's go ahead and start working on it. The first thing I want to do is to first of all, we should run the project to make sure that nothing is broken. I'm going to go ahead and do make Docker up. And just to make sure that we are on the same page, the command I just ran is basically a docker compose up. So this is it. So if you don't have make just run this command right. With that we should open our postman and then I can try to log in and you see login is working. I go to register. Registration worked. Okay, so the project is still working. Now I can do. Make Docker down and everything is down. Good. Okay, I want to go to the terminal and do git checkout dash b. This time around I'm going to say three line graph. Then I will say add setup something like this okay all the GraphQL branches will start with this graph prefix. So where do we start. We want to install the GraphQL program that we use in the last video. The same thing. So if you have it installed already you probably can maybe just skip ahead a little bit. So this is the library. Go install GitHub 99 designs. They are the author of this um g generator. And this is the version that we are going to use. So I just type I just press enter key. Now it is installed if I say which. Again um you see it is currently installed on this path. So you should have that path on your system path. If you're on windows this is automatically done for you. So you don't have a lot to worry about. After that. I need to also install it as a module because when we want to run it, I want us to run it as a module. So you have to run, go get and then this path, don't forget the version. The version is important and that's it. With that out of the way, the next thing I want us to do in the root directory of your project where you have make go module where you have all this in you right click new file. Then you create g q g y. This is the config in the last um GraphQL video that we did. We we use a initializer to do this for us. We didn't create it manually, but this time around we are going to do everything ourselves because it's an existing code and I don't want the generator messing up with our code. So I'm going to do everything by hand. Now this is where we put all the configurations. Somehow I'm going to make it a little bit verbose by adding comments and um, yeah, just for you to when you come back to it next time, I want you to be able to follow it again. So let's start all of, um, let me start with, uh, maybe we should quickly before we put before we put the content content here. Just a minute. Go to your make. Let me add one target here called aim. What should we call it? Graph. Generate. Then we are going to add this two commands here. Basically this is what we are going to run to generate the code from the schema. We are going to add the schema in the next video. And this is the make target. If you don't have make you first of all run this one and then you run this second one. We are using this approach to run it okay. It is also possible to run like this if you have it installed in your path without running the module, but if you don't have it, you can try to run it from the module. Like this. Now, sorry for switching on you. So let's go back to the GQ yml. Now in the GQ dot y, you want to start with the this is the top level config. So let's start with GraphQL schema. The schema config stays where you are going to have the all your schemas defined. So we are going to create a folder here graph. So graph like this. Good. And then in the graph we are going to have schema. And then in it we are going to have other things so the the generator can find it okay. After that we remember in the other video the GraphQL video we have that um generated go file that is quite big. We are going to put it in it. So under the exec we are going to have it there okay. This is where all the interfaces, all the configuration and everything is generated and kept. You don't edit this file, it is managed by the generator. You don't even care about it. So leave it out. Leave that fire alone then models The model is like the DTU for GraphQL. So all the input and output is usually managed via the model. But what we are going to do in this section of the course is we are going to reuse our existing dtus. We have all the details that we are using because they are all here in auth order and product. They are here and we are going to reuse them. We won't let GraphQL recreate them because come on, that's a waste of time. Okay, after the model, the resolvers, where do we want to put the resolver and what kind of configurations do we need? Okay. Let me explain this a little bit. Now when you generate the resolvers you have two options. One option is to make it a single file. So everything is one file. Another option is this follow schema which I have used here. So it generates separate files per schema. So this is for me this is a preferred option. But if you are working on a basic project, you probably can go with a single file. Okay, then after that, where are the resolvers stored? Is it going to be in graph slash resolver? Then the package go package will be resolver. And the file name format would be the name of the resolver dot resolvers dot go. And that's it for the resolver. So far so good okay. Now when the when the generator is scanning the code, I want it to try and check the DTO package to compare the types. If it finds a type in the DTO package that matches our schema type, it shouldn't regenerate it. It should use it, so should reference it. That's why I'm doing this auto binding okay. It's giving some kind of a say in auto bind. Some kind of typo, but it's not typo, so ignore that. Um. Squiggly line. Okay, so far, so good. Now the biggest part is the models, the configuration, which is basically aliasing all the models. So all the models we are supposed to have in GraphQL, which we are going to define soon in the schema, we are going to alias all of them so that GraphQL use our own DTO and not the GraphQL one, which is code reusability. All right. So this is for user. You know where it is. You have been working with it already. So the user model would point to this user response. So whenever we want to return user to the user like when you call the API and we want to return a user, we are actually going to be returning this DTO user response. So I'm going to quickly add the rest. It's quite number a number of them so I won't explain everything. again. Let's just add the rest. It's quite easy to add. Okay, for cats we added for cat a cat item. Let's add for cat item. Let's add for the order. Let's add for order item then product image okay. After that let's add um register input login input refresh token input. And we also have refresh token input. Um. What else do we have. Update profile input category input. Update category input. Create cat item input. Um. And then let me move this out like this. Very well. Also, make sure they are aligned under the model. That is where you can make a mistake. Add to cart input of course. Then the id we are going to use the one from GraphQL. Then I'm going to add one more alias, which is super important for UI because we use a UI a uint we use uint. So I am bringing you int here and this is the config look a little bit lengthy, but think about it. Aside from these aliases that we have, everything else is super basic, super basic. And that will be it. For this video we have set up our GraphQL and we are ready to fly. Trust me, this is super powerful. In the next video, we add the schema and then we can attempt to generate the GraphQL code from it. See you there!
