# Understanding Go Module

Hello and welcome back to this section where we discuss Go modules. Go modules are Go's official dependency management system. They address how go code is organized, versioned and distributed. Very important feature of go! Every go project these days uses go module. There was one old way to do things using go path, but you don't even need to know that anymore, since go module is the standard way of doing things. In order to know about the module system, you can come to the documentation. Here I am on go dev. You go to docs user manual and you see tutorial. Create a go module if you want. You can read this in your spare time. What I am going to do in this course is for us to for me to show you how to organize your code into modules. So let's go to our IDE. I'm going to since I have access to my terminal here. You can do this anywhere. This is the for. I have a folder here already, but I want to do it outside for a reason when I created this or okay, we can also do it in here. But this is going to mess up how the IDE behave. There is a go.mod here already which somehow make this project a module, but I'm not going to use this folder structure. So let's go here. I'm going to go out of it. Let me show the full path of my project. So I'm in this learning go. I'm going to go back and then I will create. This is the start. We want to create a new go module. Now I'm going to say make their. Just assume that you are going to start a new project. I'm going to write mkdir for make directory. If you are on windows, the command might be a little bit different, but you can just use the GUI to create a new folder. Let's call it a go module example. And I am going to switch into that directory. Now that I'm in that directory, I'm in order to initialize it it as a go module I type go init. Then I put the module path so I can um, let's say example. For instance, before I press enter I want to explain this path a little bit. The goal the module path which is this path is when you import your package, this is going to be the prefix. For example, if this example that I have here would be something this. If we have a strings um package inside it, it will be example slash string. All right let's go ahead and press enter. And now in that folder we are going to have a go dot mod okay. Let's open it in IDE here. New window. And in my IDE this is it I see I have just a module example. Now for best practices. 99% of the time people use the repository. So people put like github.com. For example this is my GitHub username and they put the name of the project let's say go module module Example, this full set of a namespace is is important for to provide uniqueness. Most times this is what people do instead of this example that I am putting here, and that is what we are going to do. So I'm going to leave it like this. Nothing has changed. You can modify it. And of course the goal that we are using is version 1.24.1. When I start installing external libraries from the internet, you are going to see a list here. So let's go ahead and do something. Let's say I create a folder here cmd. And in the cmd I create a API. And then in the API I put main. So this is one program. We can come here now and print out something. Hello world from API okay. now I can create another folder here. Let's say let's call it app. And then maybe in the app we have models. And in it we can have user. Maybe this is user. And let's create a type user. I will come to this shortly if when we go into our main and we want to use that user for instance user user let's call it Jane. We have been using Jane. You see look at. This look at the import prefix. It has the module path that we use and followed by the folder. And then the package models is the package and models dot user. This is how you can create an internal. By the way, I just want let me use Jane here. This is how you can create internal packages. And I will go ahead and run the program. I need to okay I'm going to use bash go run cmd slash APIs. Notice what I'm doing here. Before I was running a single file. But now we are going to run the package itself. So this is the main package. This dot slash is important for it to find it. You see hello world from API models. Dot user is currently empty. There are no fields inside and that is why it is like this. Now what has changed from what we've been doing previously in since we started the course? Everything that we've been doing, we have been doing. Then in the main package. Now we have another package that is outside of the main package. So there is a small problem, but don't be worried. There is also a solution. The problem is this. Let's say I come here and I type name and then we go to main. Remember I'm in the models package. Now I'm in models package and I put name field in the struct when I go to main and I try to set name. This is now going to work. If I try to compile it boom name undefined but it is defined. Let me show you how. If we go here, let's say for example. Um, you let's do it the same way we did it over there. User. Username. Again. But it is working here. Well, this is access control. In go. In order to export any value or type you, the type name must start with capital letter. This is very important if you want your type to be exported from a package that it was created. If you want that type to be accessible from outside of that package, the name must start with a capital letter. There are no keywords like protected, private or package in go. There are no keywords, no access control keywords. The only thing that we have is this convention of not necessarily convention is enforced by the compiler as well. The name must start with capital letter. So I have to make it like this. And with this we can now go to API and set it like this. And then it works and I'm going to run it now you see boom we have Jane. This is how you work with internal packages. We are going to create so many of them later. And this is how you import them. What of external packages. If we want to import different different functionality from the internet. Well that is also simple. Let's take for example this library called Table Writer. Um, let's scroll to the bottom of it to just show you what it does. So it prints very beautiful output to your terminal using a table. What if I want to use it in my project? Well, if you come to any library that you find, you can see the installation instruction here, say go get. And then the package. So let's copy it. Go to our IDE or and then terminal and paste it. Before I execute it I want to show you something. If I want I can omit this at the version and then run only this path. This would give me the latest version. But specifically, if I want a version I can put at and put a version and the version must be available in the repository. So I'm going to install this external package. This is like pip install npm require or if you are using Java maven install or something. So this is very easy very easy to do and very straightforward. Now that we have it let's look at our go mod. It has been imported even we haven't used it yet, but it has. It has been imported to our project and it is now available for us to use, and let's go ahead and use it because you are. As you are developing, growing, you must learn to read the documentation of any library you want to use. We are going back to GitHub. And I'm going to scroll. And let's take this example that we have here. Basically copy it and we come to here I'm going to paste it and look at we imported it. See table dot header the data and table dot book and this. But it's interesting that uh on reserve that means there is no header. Let's see. And for from the documentation it seems like they are the their documentation is also not very recent. So look at the right header to set. So I can set um The data like this. So this first rule is what we want to use as the header. And this is interesting this we took everything from their documentation. And yet you know it's not up to date. And what there is a bulk I don't see that bulk. So let me say this um set. Is there a way to append bulk append. Maybe we can use append. And let's put the data this way. So basically you remember what these slices are. We do let me see I think we need to spread it right now. Let me see what it takes. It takes a slice of string. So this is not going to work. Maybe this ideally this should be in a loop. Um let me do this whole row and then append row. Yeah. This looks okay so far. Now let's run it. And look at how it prints out nicely. Package version status. And we have it now. This is, um, not very good. So we can we can do like this from there to we want to take from one up. We don't want the first rule now. Great. So it works. This is how you use external library. You see it's very similar to our own internal library as well. And there is nothing much to learn anymore about go module. The rest is let's say we let me do something else again. Let's bring in, um, let me bring Gene. I'm going to let me go. Lang gene and I will. This is a HTTP framework called Gene. Look at what I'm going to do. I will scroll to the bottom. I won't install it yet because I want to show you another command. I'm going to copy everything that is here. And I would paste it here, overwrite this one and look at we haven't installed, we haven't installed it. And of course when if you are using an using an IDE you can click on this to try and resolve it. But look at what I'm going to do. I would go to my terminal and run go mod tidy and this would pull down Gene for me and this squiggly line should go away. My ID is reloading and because goland is a little bit slow, um, if you are using VS code, you won't even notice it, you see, and the errors are gone. That means I can now run the program Rum and gin will run. And this is starting the HTTP server for us on port 8080. So I can go. And this is, uh, ping slash ping that we put. You see we have the data really easy. Um, the go module is very powerful. Whenever you run, go build, go run, go test is going to try and download any module that you have used. Um, that hasn't been installed for you, but of course it must be imported. So you should know the import path. And basically that's it for go module. Um, after this video we are going to start doing more things by using go module for a lot of external libraries that we are going to use to build our project. Thank you for watching and see you in the next video.
