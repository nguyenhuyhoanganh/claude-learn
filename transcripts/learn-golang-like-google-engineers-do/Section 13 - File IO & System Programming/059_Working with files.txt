# Working with files

Welcome back to this new section of the course where we talk about files and directories. Files and directories are super useful in programming. So we are going to tackle some of them here. And you have seen, I think a lot of them already in the HTTP downloader that we created. We use directories, we use files, we created new files and everything. But this section is dedicated to explain a little bit more of how to work with files and directories. So let's start by creating a new section. And I'm going to create a new folder called zero one. So this is ten section ten and then one. And let's call this just files. And in it I'm going to put a minute go okay good. So to I want to be a little bit faster, because I think you've seen most of it here already. Let's start with writing a file in go. Let's say we have this data. Let's say this data. Um welcome to go programming language. This is the data we have. I can write a file like this using OS dot write file. And I can name it as um let's say text dot txt. And now this is it. So you pass the data let's open the font. Look at the function signature. It takes the file name. It takes the file name the data you want to write in bytes. So it's a slice of bytes. And then the file mode the file mode. Let's click on this. The file mode is something you can command. You can take a look at later. There are different modes here. They are just numbers. For example, this is a pen. You can look at it. You can come and take a look, take a look at them later. But that's just the file mode. And then we just check for error. If if error is not new we we log theta. If not, we just print something to the screen. Right. This is how to write a file. Now I'm going to run it. Let's switch to we don't. We no longer need the race flag. Let's switch to section ten the first folder and then main dot go. And I'm going to run it. And you see done was printed out okay. Let me close my terminal. Go to this place and look at it. Actually we didn't name it text dot txt we name it text dot txt. So let's name it like this. I'm going to delete this one. Open my terminal, run the code again and we should see it here. Now let's open it to see the content and look at what we have written. It's quite easy very straightforward. I'm going to keep the terminal open here for now. And also this part, since we are dealing with files so that we can see what we are writing. Notice something? Even though I am here in section ten, the fire was written out here. It is written out here because this is the root directory of our program. This is what Go considers the entry point. This is the running um working directory of the program. So it's important if you want to write it let's say inside section ten. We would have done this way ten. So we start from the working directory. We go to ten section and then files like this way. And we can run it now. And the file would be in here you see very important information. Now we can delete both and move on to the next which is how to read it. Now we can actually write it. Maybe I should create something called file path here. Like this. And pass the file path. If you run it again you are going to overwrite it. Welcome to the go programming language. Look at the text inside okay, now let me write something. I will say welcome to the go programming language. Lots of love. For go! Okay, now run it again and let's check the content. And you see, it's a new content, so it's important. To read the file. It's also very simple down writing. So content error. You see this is why the autocomplete is a little bit um I call it crazy. This I owe you to. It's a deprecated library. It was there before, but we no longer use it because it has been deprecated by go. But the auto suggestion is suggesting it, so be careful. So read file. Oh s dot read file is a good one. In short, let me show you why I'm saying that I o u t. Is this is this is. Then click on it. You see deprecated as of go 1.16. This is how long it has been deprecated and they point you to this right one. Know your language. We check for error. Now this content is slice of bytes. That means we can print it out turn it to a string like this. Just like this. You wrap it in a string this way and it turns to a string, and we can print the content out and that's it. So we wrote and we read. Since the content is already in the file, we can even comment out this one. Sorry, I can comment out this section that we used to write and just run the read. And then it read the file and that's it. So I'm restoring everything back. The next thing is for us to look at here we what we wrote the file. So this operation created The operation did two things. It created a file and wrote to it. But this time around, let's do it a little bit different. Let's call it New File two or something like this. And the possible error or dot create. This is this is going to create a file and return you a file handle. And you can do anything you want to do with it. Now remember I mentioned before, each time you are dealing with file operation you should always close them. Since this return a file, we should close them. Of course when we are done. But sometimes you can also close this way. Let me show you another. You can define a custom function here and uh, like this. And in it we can say because this returns an error, you can do this or maybe not theta, maybe something else. This is another way to do it as well. Uh, people just use the people. Just call close and ignoring the error because close can return error. So, uh, this is a good way to do it. I'm going to just defy it. I don't think anything can fail in this small tutorial that we are doing. All right. Now that we have the file, what do you want to do with it? We just defer the closing here. We haven't closed it. So that's that's very important. We defined it. If it hasn't been closed let's look at some of the function there. We can. Right. You see you have a right that you can use to write slices of bytes. You can write at a specific offset. You can write a string. You can write two, you know. So let's just take this one and uh, we can. Just let's write something else. Uh, welcome. Welcome, all. Java and Python and JavaScript and PHP and Ruby developers. Okay, now this returns an error. So you should always handle it. And can I think a return an error and the total byte site has written. Yes. So I'm going to ignore that and only care about the error. And we can check for the error. Now the file we created I don't want that same file. So this time around I'm going to put a message here file via create dot txt something like this. Then we run it and we should see the file right here. This new one, which is content is inside. Fantastic. What else should we look at? Open. You can open a file and do something with it. So let's say let's let's do something like this. Um, let's say list. PHP we have this file now Java. Python. Sharp. My first love Delphi C++ okay. We have this. What if we want to read this line by line? Okay. What I'm going to do now is to. I don't really care about this anymore. We can take it out or I can comment it out. In case you need to look at it in the code. Okay. Let's open the file. New file. Let's call it new file. OS dot open. And I would uh, this the name of the file is file via create. Dot txt. Okay. Let's check for error. Quickly copy and paste it down here. And now we have the file open. You do. What did I say. You defer. Very important. Now that we have the file open we can read from it. Let's look at some of the methods that are available. You see you can read and write to it actually. So you can read read that and do many things. Okay. But um let's use the scanner. We've seen this before from both IO. And what is going to happen is going to wrap this. So it's going to give us a better way to read from the file. I want to do line by line line number. Let's say line one. Then we scan and then we increment the line number here. Um, after that we can just, um, maybe we print out the line, actually. Okay. That's it. Then we check for error for the scanner. If this error is not ni, we're we log theta. But, um, something else that can happen here I want to mention is if, if it reach, um end of file, it can return an error as well. So maybe we only want to filter if error is not EOF. So IO dot eof. Good. I think that's it. Let's run it and see you see it, put it. And if we want we can print the line number as well. Let me do it. So I'm going to since we are starting from one we print the line number before uh we increment it. You see from 1 to 7. And if you open the file for sure 1 to 7, that's what we have. Isn't this great? Right. It is so good when I show them over here, when we run the file, it was overwriting the previous content. So what if you want to append. End. Well, let's do it with this file. So I'm going to maybe let's create a function and call uh print content. And I would take this and paste it here. And I will say file name or file path, whatever you want to call it. Where did we cut the code from this one. Like this. Let's bring it in. And then file name. So we we move everything to a function. Then we can come here and say print content. Nothing has changed. And then let's run the code. Ah. Let's see. We're making a mistake somewhere. Oho! This one. Good. We forgot to pass the file name. Run it again. Nothing has changed. The same thing. So let me put the file name here. Good. And I'm going to put it like this. All right. Let's close this one down for now. And then in main I want to append more data. There we I want us to. When after this file will be closed, I want us to reopen the file. A new file or dot open file file. And this is where it gets interesting. Let's pass the file name. Then we need to pass some flags. We are going to start with OS dot append. This is like this. Sorry. Okay. I'm going to change this to append. I'll change that to append. I'm going to could have just added the append here. Doesn't matter. I also want to be able to create. And, uh. I think this too was actually okay. You can open you can open the package to see, uh, to see all of them if you want. And that's it. This is going to allow us append to it, and we check for error quickly. Since we have this over here, we do the same thing. Now we we differ. Now we can write to it anything we want. So I'm going to look for the right string. And I would uh let's say I'm going to write a line. I'm going to put a printf. What language have we now added C. And maybe we can call it multiple times. I don't really care about error and everything for now here. So I'm going to ignore them I think. What is the it returns. Yeah. Like this I don't care for now let's duplicate. We could have done it in the same code, but yeah. Um, maybe I don't know if we have added Ruby. Um, maybe for Fortran, Ada, something like that. And this, this. Okay, now let's run it and. Okay. And let's open the file. Look at the new ones that we added. See we are able to append and that that's that's it basically. Um, yeah. This is like the basics, you know, the few important things here. If you want to append, you can look at the flags. They are all written here. The meaning open the file, write only uh, open the file. So you want to support different uh, combination. You can um I think they are using some kind of bit mapping or something here. So if you want to support different combination you can list them over here. And don't forget you see we are using open file not open. Open doesn't accept these flags but open file does. So when you use open you just open to read and write. And uh, that's it. I think there is a default combination that it has. It has made look at it. So you can read up on this later. Yeah. That's it. So thank you for watching this video.
