# Working with directories

Welcome back. Now let's look at how to work with directories. Directories are how you group your files in your system and in go. You would have a lot of utilities that helps us to work with directories, from creating directories to removing them and so on and so forth. So I'm going to create a new path three dir for directory. And then I would put a minute go in there. Now let's start coding. The first thing I want to show you is how to make a directory. And we can do it using this from the OS package make dir. Okay. And we can just call this um maybe downloads. Okay. And then we pass the permission. Now it returns an error. Look at the signature. It returns an error. That means you must handle the error. Good. We can do if. Let's use this format. I've shown you this format before. And then we can log theta. Since when there is an error here, there is nothing you can do. So it is safer to do this log theta here. Let me open my terminal and I want to switch to that directory. This is three and main that go and run it now. No output. That is because we didn't print anything now. But for sure we should have this directory since we didn't get any error. So let's check and look at it. It is right here. Downloads. Now I'm going to do something else again. Let's delete it. I'm going to do something else again. But this time around we should see an error. So I'm going to say downloads maybe static slash images something like this. And then we run it and you see make the image is no such file. So this fails. What that means is this we are trying to tell you to create this directory, this one, but this doesn't exist. And in it we don't have this one. We also don't have this one. And this is this is not not real. This is not unique. To go. Let me show you on the command line. Pardon me if you're on windows. I don't know how to do this on windows, but I will show you here. If you do make dir and you pass this path here, this is going to fail. It's going to fail because it doesn't know how to create all these paths together. But there is also a way to create everything together, um, which you can say basically create everything together. Like everything I did. Sorry, I made a mistake. I'm supposed to pass lowercase p, and you see, now you see, it has the everything together. Now let's delete it and create it. Do the same thing from go in. Go. There is a make the. K or this one, um, the same signature. But this time around we are going to have all the internal folders created as well. So that's the difference between the just the make the make the. So if you know you're going to have like deep or nested folders that doesn't exist or some might exist, some might not exist. The safe option, the safer option is this. But remember if you are if you are there, let's say like this. If you are taking this from outside, if you are taking this from outside, remember to always clean it. So instead of just passing it like this, you call us dot clean. Sorry, not OS is file path dot clean. We looked at it in the library or always do this. So just to avoid issue security problems. Right. And basically that's instead of me deleting it here, let's let me delete it here myself. So I'm going to say um o dot remove. You see remove is also the same thing. That is remove to remove a single file and then remove all we can do remove all here. And it also returns an error. We can do the same thing that we did here. And log theta man. It's probably like almost like nothing happened right. Because we create we are going to create it and then delete it immediately. Okay I'm looking for the program I see and. I deleted it, but um, the image was not the whole path was not deleted. It deleted the image. So let's see how we can delete everything. Maybe we need to use recursion. Um. Let's let's look let's go into the, the the package itself. Remove remove part. And any children it contains. Okay. Good. That means yes. This is what we're supposed to do. So we're supposed to remove this part and all its children and that's it. So it's gone. But what I want to do, since we are creating this just to give us show you, uh, that that's how it works. I'll do this. Remember, we have this children, but then we're going to remove the parent. And then from the parent removes everything okay. It's nothing showing. Nothing is showing here because immediately we're creating it and we're removing it immediately. So it looks like nothing happened. But I'm going to let's comment this code out for a minute. I will create it this time around. I'm going to comment this out, then comment this one. Remove the comment and now run it. So you watch here. Now run it and boom it's gone. Okay, so let's let me do a quick recap if you want to. When you call remove or remove or remove the folder and all its children that's important. The the first time that I passed the dir it this was the parent. This is the end and that's why it removed only that one. So if you want to remove everything you have to pass the from the it's a tree. So if you are going to um cut down a tree you need to go to the branch. And then everything that is on that branch will go down with it. Um, of course, I'm not saying you should go and cut down any tree. I'm just. I'm just giving an illustration. All right. Um, that's it for directory. Basically. Now there is a temp directory that in case when you are writing tests and you want to store something in a temporary directory, let's look at that in the next video.
