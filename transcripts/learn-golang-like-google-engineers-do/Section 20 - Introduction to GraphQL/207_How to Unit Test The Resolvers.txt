# How to Unit Test The Resolvers

How do you test the resolvers? If you want to test this resolver, how do you test them? Now, whenever you want to test any kind of code in go or anywhere at all, the question you should be asking yourself two very important questions. One is what do I want to test? That is, what kind of input and output do you want to check for? That is number one. Number two, what are my dependencies? Most times when people cannot test a piece of code, it's actually due to dependency management. Because when you depend on some complicated, um, library settings, signals or whatever it is, you don't know how to bring those dependencies into your test, then it becomes really confusing. But if your if your mind is clear about your dependencies and the input and output you want to check. You probably can test any piece of code and I want to show you. So let's start by going to graph. So let me git checkout dash. B seven add. Resolver tests. Okay so in graph. Let's add a new go file resolver underscore test. Okay. In the resolver underscore test let's test the create todo in method. So we are going to test the create todo method. But the question is this. Let's go into our resolver. What are our dependencies. From here we see that we only have one dependency which is a store. That means in order for us to test this resolver, we have about seven methods or so. In order for us to test those methods, we must get this store in place. If you are confused a little bit about how do I know that this is our only dependency? Well, you can look inside the code yourself if you come here. You see, this is the receiver. How many places do you call receiver dot. So r dot what. For example here we only call r dot store. So the store is what we need to make sure that it is set on this R before we can do our testing. So that out of the way we can go here now and create the store initialize our resolver set setting that R. So R dot store is no longer needed because we are setting it here. If you use a Postgres or MySQL or any relational database management system, you probably have to get a connection to the database and initialize your repository and pass the connection here. Okay, with this out of the way, honestly, everything is super easy. You need the input which we are going to create plainly like this. Then we call that method. So our resolver dot mutation. Then you call the method on it. Let's import this context quickly and then you pass the input. We are passing the input. Because if you see here the arguments are a context and the input. And that is exactly what we have constructed and passed along here after. After this you can just check for things. Let's check for error. Make sure there are no errors, then we can do some equality check to make sure that what we got back matches what we had. Remember, the expected is what you currently have that you want to check what you got against. Then we check to make sure that the status is false. Because when we create a new todo, the status should be false. And we want to make sure that the ID is not empty. And that's it for this test. I can run it by clicking on this to run it, or I can run it on the command line by typing go up test. And then I can run like this and we see what happens. We see no errors. If I put some data here. Okay. So this is really not working very well. So what I'm going to do is to come here and run it. Okay I need to run go mod tidy first and with that go more tidy. I can click on this button and everything looks good. No errors we can to just just to be sure we put some tests here, run it again and now we have an error, which is fine because we can undo it. To go back to the no error state. Now let's add another test for our validation so you can run it from the command line like we did in the previous, um, chapter. But I'm going to just keep running it from the idea here. All right. Let's create the story again. Let's create our resolver. Let's create the input that we want to put. We know that we have a validation. When the input is empty we expect an error. So let's call the method and assert that the to do list nil because error shouldn't be name. All right we have an error, and we want to make sure that the validation error matches what we are receiving from that. And that should be easy for this level. We are going to add a little bit more. But let's run this and see okay. It returns an error but the error message didn't match. So valid actual this is what we're expecting. So let's fix it. Validation failed for text and then text. Test cannot be empty so we fix that error message there. Fantastic. How about that 255 length thing that we did? Okay. Here I'm going to create an input I will call a function that we can write shortly okay. So we are going to create an input with a very long text. Then we record it to do again this time around. We should check for error very similarly. And then we check for the error here. Now let's write this function. This generated string here. Generate string is quite easy. We just make the the the byte that we have we range. Then we can put anything that we want to put in it. So I'm going to put B at the index of I. And then I can put some just random thing here and return it as a string. And that's it. So let's run the code now. Okay. Let's look at the test now. Validation failed for text. So let's make sure that the error message is correct. Test cannot. Exit 255 characters. That is the error message that we expect from it. Good. So that works great. Okay, let's go back. I want to type go test again to see V. Okay. Um, so let's go to where are we? Graph. Good. So that package you see that we have everything here. Uh, you can run from the command line. The dash V is for the verbose. That is how we are seeing all this one, just to make sure that we are not being misled. We see that it failed very well. Okay, great. So it's working all right. Um. What next? Let's test the gate to dos. We just add one more and like always, we do the store. Create the resolver. Let's create two to dos. And then we just assert that, uh, there is no problem that we have to do uh, created. So we have we created two todos. There shouldn't be any errors. And the length of the to dos that we got when we call todos should be two and that's it. So we can run from the command line again. Good. So works great. Now for my IDE. I can come like this and say run with coverage and then I can see some coverage data and open it. You see, if you go to the resolvers you see that the create todo. We covered everything. But we didn't write for this, this and this. We just. We covered this one as well. We didn't write for this one. So I want to leave this for you as an assignment. It's super easy. You almost can duplicate everything. It's super easy. Single dependency. Try to make sure you cover every line of code here because it's really easy to do and I am trusting you if you have watched the course till this level for sure. I have no doubt doubt that you can do it. So thank you for watching. Um, in my ID I can go to Ron, which is a little bit off the screen for you, but I can go to Ron and click on this hide coverage to clear out this. So that will be it. So git status everything looks good. Git add git commit add test to resolvers. And that would be it for this video by.
