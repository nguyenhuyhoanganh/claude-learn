# Define Project Schema

In this video I want us to define our schema. So go to graph. Then inside graph open schema dot graph QL. So this this file open it. And I'm going to delete some of these things that I don't think we need and probably can delete everything I'm going to delete all these types. Let's delete them. Even delete everything from here. Yes. Let's keep it like this okay. Okay. Let's start with some comment a little bit of comment then um, or inside the query. We want to support the ability to get all to do. So we are going to call it to dos. Very similar to what I showed you earlier. And it's going to take a type to do which we removed, but we are going to bring it back. All right. Then another operation to get it to do with a specific ID, you know? Um, so when you, when you query to do you need to pass an ID then the last one is to do's by status, which I also demoed earlier. Okay. So far so good. That's it for the query. Then for the mutation we are going to add and add an operation to create. To do so we need to add this type as well. New to do then another one for updating it to do, another one for deleting it to do, and then toggle it to do so it will do that have been completed, can be toggled from to become a to do again. All right. Basically these are the seven operations we are going to support three for querying data and then four for mutating data for the types. Let's put them here. This is our Todo type. It has an ID text dawn and createdat That the ID is a built in data type. And then uh, string boolean and string. Of course they are the safest planetary. And then we need to this new to do type. When you want to create a to do we need that type. Just the the test of the to do you want to create. And then when you want to update it to do we need another type here which basically the test and the status of the to do. And lastly after this, after this schema modification we need to regenerate it. And this is the mistake that sometimes I make right? I modify my schema, then I forget to regenerate it. Then I start struggling. I was going on and not saying my type. So don't forget now when we did let me open my terminal when we generated the schema the first time we did it, we used this init. Right now don't use the init anymore because we already Initialize the project. It is now time for us to just generate. So you just type generate. We replace the init keyword with generate and then you press enter. Now it says that it's saying that um because of what we are doing, we say we should run this command first, this one, which is okay. And this is something you probably have to do a lot. So instead of me doing it multiple times, let me do something. Let me copy let me copy this out. Right. I'm going to I'm going to create a make file. You can if you don't have a touch command you can just right click and create it by yourself manually. And I'm going to say go. Like this. I'll put I'm going to paste this one first. Then there are other command that we run. I am going to also paste that command. So basically we we are going to run both of them to stop seeing that message. If you don't want to do it like this, of course you can run this one first and run the second one. So but I like to save time. I don't want to repeat that next time. So I can type make GraphQL generate. And now it has regenerated the the program. So let's look at the model. That is one good place to check to make sure that your data is there. At least, if nothing else is here. We know that we didn't have update to do that was like this before. And then they have this new to do, and to do now includes, um, the createdat. So this is very good. What else it is generated file. You usually don't come there. It's always going to be recreated. So ignore it. The reservoir still remains clean. This is the schema that we modify about ourselves. If the program is not going to change it, and then this is where we need to come and implement our, um, our resolvers. You see, now that we have more methods now create to do, to do, delete to do, everything is now well outlined for us to work with in the next video. So this is really good. I haven't, I haven't initialize the git repository here yet. So let's go ahead and do that. And then I can touch dot git ignore. I like to use the touch command a lot. So please in case you don't have it just right click and then new file. Then you can type the name of the file there. Right. Um, this one I think is okay for now. We are going to come back in the next video and then we can commit our changes and then we start coding the rest from there. See you there.
