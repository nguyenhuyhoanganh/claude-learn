# Implement In-Memory Database

In the last video we created the. Gitignore. We initialize the git repository with a couple of other things. So now I want to paste the. Gitignore data. Um, this is exactly what we have in the e-commerce project here. I don't think we need the upload so we can take that out, but this is just standard and nothing special here. So I'm going to keep it. And I can scroll a little bit for you to um, see why if you want to copy it or type it anyhow you want it. So this is for the. Gitignore with that in place, I'm going to do git status just to check everything that we have. This looks good to me. I'm going to do git add git commit. M initial. Commit. With schema Then I'm going to do git checkout. Uh, schema. Schema setup like this. Then I'm going to do another git checkout. And in memory store something like this. Okay so this video I want us to add the in-memory store. Uh we don't have a database here like MySQL or SQLite or Postgres, anything like that. So we're going to keep the data in memory. But when you restart the application you are going to lose your data for this project. That is more than enough for us to do what we want to do. So I'm going to go ahead here right click create a new folder called model. Then maybe I should name it models. Good. Would so create a new folder. Models. Then you create a file inside. Let's call it to do. And that's it. I'm going to expand it to make sure that everything is clean and clear. All right we need a type. So let me explain what we want to do. Basically we are going to have a map of for example todos like this. There's going to be of type map. And then I, we can have a string here which is going to be the ID that would point to a pointer to, to do so the the key of the map, the data will be pointer to to do. It's a pointer. Because sometimes I want to be sure that the todo is there or not or and if the todo doesn't exist I want to get a needle back. So this that is the trick. Basically this is the data structure we are going to use. But because HTTP calls in go is concurrent we need to have a mutex. So we are going to protect this map with a mutex to avoid clashing you know to avoid data risk. So that's all. So it's a very simple approach. Let's go ahead and bring the type that we want to put in the map. This is the todo type right ID text done. And createdat. That's it. The createdat if you want you can use the time dot time. But I just I just want to use string this time to show you. We have been using time dot time in a lot of places, so I want to show you that it's possible for you to use a string as well. Then you can pass it. So if you want you can change it. Let me add a comment to do. To do type. S the type we want to store in the map. Okay, then we need one for to do store is the um. The to dos in memory. Memory. And this is the store. So we have the mutex the to do which I just showed you now. And then next is the next idea is just a way for us to track the ID we want to put next. So it's going to control this field. Okay. After that we should add the constructor. So new. New to do store Create a store in memory. Like this. And that's the most important thing here, is that do not forget to make it very important. Without making it, you cannot use it. And then after that, we increment the next ID to be one. So when we are starting, the id will be one. But when we create a to do we are going to increment this id to next ID plus plus okay. So far so good. Now let's add the first method which is getting all to do. So get all returns all to do's. With this of course we need to get a read lock. We are reading. So we need the R lock and we can define the closing. After that we make the list of to do's that we want to return, then we loop. And then the loop. Basically we add this to DOS and we return it and that's it, right? It's not hard at all. The next method is, uh, get by ID uh, returns a todo by id, okay. And then we do the same thing with the lock again. Then we just return it like this. We are able to do this because this is a pointer to todo. And look at here. Look at what we have here. So if we find it great we returned it to pointer to to to. If we don't find it it will be nil. So this is perfectly fine. You don't need to check if it exists. And that's why it took a pointer instead of just the struct. The concrete type. Okay, good. The next method is, uh, get to do get by status returns. To do based on the done status. Okay. This one we need. We also do the same thing with the log. This is reading. So we are using the R lock and the R unlock. When we want to mutate the data we are going to use the full duplex for the mutex. Now let's create a variable for the theta to do. Then we loop in the loop. We check if the todo is equal to the status. This this status they pass in here. If that is the case we append it to the filter. So we append it here and then. if after that we can return it. And that's it. Quite simple. Right? Next method is to create a to do finally create create um add new to do like this is super simple. This time around we need a full duplex for the log. Now we need to create the type itself that we want to put in the map. So I'm going to put it here like this okay. So because the id is an integer we are passing it through a sprint f. And to make it a string. And this is very normal when you create it to do the status will be forced by default and created that. We format the date nicely and set it here and that's it Okay. And then after that we remember I said, when we create the to do, we have to increment the next ID. So let's increment the next ID here. And then we add the to do to the map using the id and we return the to true. That's it. So far so good okay. The similar method is the update. Update update modifies an existing to do okay. Now before I continue with the code, I want to mention something here. Test is a pointer to string and Don is a pointer to Don. I'm using a pointer here because I want to distinguish between the zero value and the absence of concrete value. Let me say that one more time, the zero value of a string is an empty string. The zero value of a boolean is false. So what is going to happen is if the date if this data is false, or is an empty string, I still want to set it. But if it is nil, which means they don't want to set it, then I won't set it. So I basically for partial update. So it's more like a patch. All right. We take the full duplex again with a lock to lock. And then we define the unlocking. Now please don't forget to do the file is very important. And after that we can try to grab the to do from the list. Then we check if it is as if it exists. Remember I said that when we grab it like this, it can return either the to do pointed it to two or a nil. So we check if it is nil. We don't want to do anything. We can return NI, right? Then this is why we are using a pointer here. We check if the text is not DNE before we update it. Then we can update it like this. Now remember when we are updating the text we have to dereference here the referencing, basically unwrapping the real value from the pointer and then setting it to the text for us. We do the same thing with the dawn as well and that would be it. Then we return the to do. Right. So far so good I hope no confusion, right? Please. If you are confused for any reason, do not hesitate to send me a message or post a message in the Q&amp;A section. Okay then of course the almighty delete delete removes a to do. Basically, you give us an idea of what you want to delete and then we have we find it. We check, first of all, if it exists. If not, we return false. That is why we don't care about the value. Here we are only using this sort of the common pattern right now. At this point it should exist. We just call delete. I. Then we return true. And that's it. Okay. Last method that I like to add is a toggle which basically change it. If it is if it is done it changes to back. Change the done back to false if false if done is false, it changes back to true. Just a toggle right and toggle. Switches the done status between. Slash false. Something like this. To uh, make it, um. Okay, now we take the full duplex again. Then we try, we find the to do. Try to grab it. We check if it is nil. There is nothing we can do. We have to return nil. If not, we set the toggle here. The most important thing here is this pan sign at the back of it. So if this dawn is false before and you negate it, it becomes true. So it's a negation sign. If you've done anything in Boolean logic, uh, logic gates, you know, uh, negation is very important and useful thing here. So then after that we returned it to do, but we just mutated and that's it. Let me scroll to the top and then we see what we have done. So we have the to do type it the store that manages to do a simple constructor, and then we add some method to the store so they get all they get by the get by status they create, which basically creates a new todo or updates. The todo does partial updating, which we are setting pointers and delete. And of course lastly is our toggle method. So this is very beautiful what we are going to do I want to give you just a glimpse into what we are going to do. Or let's go ahead go to server. This time around I'm going to rename it from Server to Main. Okay then here. Don't worry about this. Um, everything here, let me go to my ID, so I'm going to my ID and go to settings. And then in my settings I can go to go Project Structure. Let me look for module. So this is go module. So I need to enable this. This is why it's showing that way. So if you use Go Land and you have that problem just search for go modules and you click on it it's going to refresh. Good. You see we are back to good thing. All right. I rename the server go to main.go which is way way better to me. Then what I'm going to do here. This is the resolver. Let me come out here and name it resolver. And copy it from here. Bring it here. And put it like this. So. All right, this resolver is here. So go to Graph Resolver. This is it. So inside graph resolver dot go you can come here and type um to do store. To do store would be a pointer to to do store like this okay. So we are injecting this dependency. Now when you go to main we can set it to to do store like this we bring in use a constructor. So the significance of this is this. Now if I go to my schema dot resolvers dot go. This is the template that we need to implement. For example here r dot todo store create. This can already for them we can put for example. Test with this or with this we can create a todo already. So but we're not going to work on this one for in this video. But I'm just giving you a glimpse of what we're going to do. So here we've been able to create a store and inject the dependencies. This is dependency injection. Very important. This is all it is to dependency injection. But in go you do it by yourself in other programming language or framework like Spring Framework, Symfony, Laravel and the rest. There is a way to help you resolve the dependency automatically. But for go, we usually don't need any third party library to do it because it's quite easy to do right write just to give you a sort of a recap. Let's go to a model. We created a to do in-memory store here. Then I went to graph slash go injected the dependency the store, not the to do. Then I went to main, created the resolver and then initialize the store. And then I put the resolver back into this handler which was created for us by the initializer. All right. And that's it for the video. We can type git status. Git status. Git add git commit. Add in memory store. And that's it. I would see you in the next video. Bye for now.
