# Better Error Handling

In this video, I want us to add a little bit of error handling. I want to show you how you can do that. I'm going to stop the program. We started it in the last video to test the API, which was great then. Now I'm going to check out Dash B for custom error handling. Something like this should do. Okay, let's open it up. I'm going to add another package for errors, which um, sometimes this can clash with the internal stuffs but it's okay. And let's put error dot go inside. I'm putting everything just here. Nano complex stuff here. Let's add two custom error types. We looked at error. Custom error type some time ago at the towards the beginning of the course. So I'm going to define a type for not found error which is very basic. The resource name that was not found and then the id. And in order for this to become an error, you must implement the error interface, which is very, very simple. To implement you need an error um method attached to this and it should return a string. So that's it. So we attach this method to this type. This makes it an error. So this can be returned or when anywhere you return an error from a function, a method, or even when you accept which I don't see anybody accepting errors. Right. But anywhere you have error interface, this satisfies it and you can use it. Then let's add validation error and at the same time implement the interface. And that's it for this error. With the custom error types in place I want us to go to the resolvers. So open graph go to schema dot resolvers or go. And then in the Create. You see, we are not doing any kind of validation here. So I want to add a little bit of validation before I create it to do what do I want to validate? If the test the length of the test after we stream the space is zero, we know that in type anything and then we can return a validation error. So like this. So we return validation error. That's it. All right. Make sure that you have this imported. Right. So this has to be imported for it to work. After that we also want to make sure that they didn't type too long a text that is too long. So maximum maximum of 255 characters. So 255 characters max. And then we just put it like this and that's it. I'm only doing this as a demo to show you how to add custom error or custom validation. Another way to do the validation is to add validation directly to the model, which is because the models are your detail. They are the data that is going over the wire, and you can attach some validations to this model to do, for instance. So maybe it will be possible for you to say do need to do dot validate. Or something like this. Validate maybe something like this you can do. But let's keep it simple okay. After that what else. Um if the so here basically if the to do is not found instead of false returning this error maybe we can return errors dot not found then resources would be. To do. And ID will be ID something like this. Work as well. And we can copy this anywhere we are doing not found. We can replace that with this small, elaborate error. Where else were we doing that? I think that's about it. Now, with that in place, we can run the program. Go, run. And open the browser since we are here. Query. To do for sure. We know that the todos are no longer in the database because we restarted the server. I'd. And this should give us that beautiful error that we have. So this is we didn't update that. So let me check. Uh create todo update delete toggle todos to do. Yeah. We didn't update it. So let's go and do that I can copy. Let me scroll up and copy the not found part. Why is it to do so? Here? This this this a question of do we want to return an error or we want to return? No. So here I think we should return an error. Change my mind. Why not? And then we go back to the browser and we hit it. And you see, to do with ID one not found path and that's it. So it's quite a better error message and that's good. And if you want to mutate delete. If we put some kind of stuff you know this one. We are not handling that which is okay toggle I can put some random ID. You see the beautiful error message that we are getting. And that's it for the error handling. Let me scroll to the top. Go open the file browser, then do a quick recap. Open errors dot error go. We added two custom error types. Then we added the implementation to make it satisfy the error interface and the message that you are seeing. There is what we have put here. So with the custom error types you can add your own message, your own data. And this is why it's so powerful. Then we went into the resolver. So schema the resolvers go and we start returning them from the code. When we hit an error condition we did that in a couple of places. So that's it. So adjust your code to look exactly like my. And the feedback on the browser should be the same. All right. And that's it okay I want to scroll down one more time slowly in case you are typing along. And that's it. Bye for now.
