# Add Middlewares

Okay. Welcome back. When you open that go you see that the server is just HTTP server. That means we can add middlewares. I think some of these things are actually middleware. So let's go and add some middlewares. All right let's go here. Before I do that I need to check out a new branch actually. So git checkout dash five meadow where our middleware. All right. With that in place I can right click now say middleware. I hope I spell it correctly. And the first one we're going to do a request ID middleware. So each request are coming to the server would be assigned an ID and it's quite easy to do. Let's start with some type that we want to put in the context. Okay. Type context key and const. Request id key context key and request ID. And that's it. Something else I also want is to import um a Uuid that we are going to need from Google github.com slash Google slash Uuid. It's currently not there. So I'm going to open my terminal to type go mod tidy. Okay I hope it it could disappear, but let's let's keep going. And we need this Uuid for sure. Uh let's create a request id middleware. You know what a middleware is? Middleware is a function that takes a HTTP handler and returns a returns the HTTP handler. We send it before. So and that's it. So basically we need to return this. And in it we are going to attempt to grab a key from the header which might not exist. So if the request ID exists already we grab it. If not we generate one. And the generation. Basically we set it to uuid dot new dot string, which is going to be uuid value from the same library that Google publishes on GitHub for us. Okay, after that we let's grab a context from the request and set a header with that data. And then we save. And that's it for this. So remember to import context like this. That's it. This is it. Now let's add one more function to help us get this data from the from the context. Okay, so we check if it exists and we are able to convert it to a string. Then we return it. If not we return an empty string. Let's return an empty string here. Okay, so far I guess no confusion, right? Good. After that, let's add one more. One more middleware. Um, so go to middleware and we can right click and say login. Okay. Let's call this one login and return the HTTP handler func again. So when the request starts to before let's say you want to fetch a to do when the request come in, we put a timer. When it ends We can then print something out the printing out. We can also put like total time it took to process that request. So let's put a start which is time dot. Now I'm going to create a function here which doesn't exist yet. Um, not a function, a type called response. Right? I need to wrap the response writer because I want to inject a few things myself. So let's put a wrapper here and then we serve the request, then put the message we want to put in the log. So this is a message we want to put in the log. Right. And that is it. So let's put this response writer wrapper here is a small type here. And inside it we embed the original one and additional status code that we want to add. That's it is because of this status code actually that I put it. Then we add one method to write the header. And that will be setting it. And that's it. So I need to write it. And that's it. So basically we set the header and then we call the internal one. So this is our middleware. We have the two middlewares in place. So we need to go to main go. And in main dot go down here let me say set up custom do where. I can say var. This is our handler http dot handler equal to s. So this one this is http handler because I want to attach our middleware I am reassigning it here. So I'm going to say middleware. Let's make sure that there is no middleware imported from anywhere. Okay. Good. So middleware login. We pass the same handler. Then we do the same thing again middleware like this. And with that in place we can replace this with this one. So this should attach the middleware for us. Time to run the code to see what happens. If everything is all right we should start seeing the request IDs. Let me move my browser in order for us to see the output. You see? Look at the request I just made. It's here. And it's super fast. Super fast. One millisecond mutation. Super, super fast. It doesn't even matter what we are doing. They are super fast. This is not even up to a millisecond. So of course the data is in memories. It's also very small. So go is super fast. And and this is how you attach, um, middleware to your GraphQL API. I want to go back to the middleware folder again. This is the one for login. So in case you want to type along with pause the video, then this is the one for request ID. Super easy. After that we went to main and then here we just we reassigned a server the existing server attached the middleware to the new variable and then inject the new variable here and that's it. So that will be it. So git done and done git commit r. Do where. Okay. And that's it for the video. Something I don't remember. Git branch. Okay. The custom error handling is there but I don't remember. Did we commit it. Let me do git log. Yeah. We didn't commit that custom error handling code so. But anyhow the data is in this branch so it's fine. I can try to fix it off camera, but everything is here. All right. Thank you for watching and I will see you in the next video. Maybe we can look at some best practices.
