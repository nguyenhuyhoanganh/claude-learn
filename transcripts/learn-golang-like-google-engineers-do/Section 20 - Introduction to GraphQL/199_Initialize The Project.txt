# Initialize The Project

Time to code. Now let's open our IDE. This was the e-commerce project. We are now going to work on this. For now I'm going to go back. So basically I want you to create a folder anywhere in your system. So I'm going to create a folder here called. Intro to graph QL calls. And then I'm going to switch into that folder immediately. Then I will let me open it in go land. With that I'm going to replace the window. Okay. So open my terminal again. I don't want anything here. So good. You know, this is a fresh go project. Yet nothing is inside. The first thing I want to do, of course, is to do go mod init github.com my username intro to GraphQL cause something like this. With that in place we need to install. This is the framework we are going to use. It's very popular. It's a schema approach. Uh that means you have to. So it's a it's based on schema first approach. So you define your schema and then you generate your code based on the schema. There are some other frameworks that is code first. So they don't deal with schema directly. Or you can you have to define the schema in the code and all that. But most of that is the is the same thing. All right, let me go back to the IDE and then let's start with, um, go let's install. So I'm going to install it. So you need to type this command. Now this is something that in case you don't have the go part on your system this can be tricky. But just do go install this for now. It's installing. After that we do go get you see the command right. Of course you are going to have access to the code so you can copy it from there. Now after that two command we need to run the binary. So even though I have it I don't need to do this. But just in case you don't get, uh, gold in your path. You can run this as well. So you run, tap, go run the full module path and then init. This is going to generate some files. And that's it. So let's look at the generated files. So the first and very important file is this config. L g n y. This file provide the sort of the mapping the config for the generator. Your code would need the file when you are going to production, but just for development. It uses this information to generate files. All right then this is the server itself. It's listening on port 8080. And then um everything is set up like this. Okay. Then in this graph package you have uh, let's look at, um, the schema itself. So this is the schema. You see the to do user some query. This is an input. This is a special type for like a DTO input and then mutation. So for example when you open the documentation you are going to have inquiry inside query are going to have to do's which you saw how I used it earlier. And then under mutation you are going to have a single operation. Then there is um schema dot resolvers dot go. This is the this is the boilerplate for your resolver. Actually this is what where you need to write your code. This is where you need to write your code for this, this one that we've generated so far. We just we have to want to create to do and then to do so we're going to leave it like this for now. Then if you have dependencies that you need to inject for your resolvers, this is where you inject them. For example, you like your let's say user auth. You know, things like that are going to be injected here. And then there will be accessible inside this place. There is no magic. Everything is quite straightforward. Then the models that was generated, they are here which is the package is model. Okay. Now with this in place, sometime when I do something like this I try to run, go mod tidy, uh, just to make sure that things are, well, um, tidy. And um, even though we have like some of these errors and the rest, I don't want you to worry about that. We can try to run it and see what happens. And it's running, you know? Um, it's not going to work very well because there is panic in the code, you see. So let's go here. Now under query, we have this to do under mutation. Let's go to doc. And we have just one as well. Like this is exactly what I said right. I think uh for, for this for the query is going to panic. You see internal server error because there is a panic in there which is fine. But overall this is very easy as you have as you see, we did we run the installation from the command line. Then we call the init and that's it for the video. Next video we are going to start defining our own schema. We are not going to rely on the schema that is generated for us, so that we can take control of the program. See you there.
