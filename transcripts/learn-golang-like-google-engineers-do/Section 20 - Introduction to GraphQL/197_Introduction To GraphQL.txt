# Introduction To GraphQL

Hello. Welcome to another great section of the course. Now let's talk about GraphQL. GraphQL is a query language and a runtime for building modern web APIs. It's so powerful. Unlike Rest, GraphQL, um, give you a very flexible single endpoint where you can query for the data that you need. Now let's look at some benefits. GraphQL offers a single endpoint that means you don't need to remember many endpoints. It just offers some flexibility so clients can specify exactly what data they need. Type safety. You can define the schemas of your data and the structure exactly how they would be. And it supports real time. That means clients can subscribe to data and you can change the data, and the client can see the changes in real time if you want. And lastly, GraphQL also is Self-documenting. That means you can you can inspect the operations that it supports, the fields and everything that it contains in GraphQL endpoint that you provide. Now let's compare it with rest a little bit. Unlike unlike Rest, GraphQL offers a single endpoint. You can name it whatever you want, maybe slash API, for example, in Rest you can have slash user slash post. But GraphQL, like I said, is single endpoint in Rest. The data structure is fixed. In GraphQL, you can define the shape of the data that you need. In rest. It is possible to over fetch or under fetch. What does that mean? For example, when we are fetching the user's API in rest, sometimes you have to. All the fields that you fetch are just too many and you probably don't need everything. Sometimes you need some things. They are not there, so it can be a problem. So sometimes you over fetch, sometimes you under fetch. But for GraphQL you can specify exactly what you need. So just ask for exactly what you want. All right then lastly multiple requests for related data in GraphQL. You can send a single request for related data. Okay. Now let's talk about some of the important keywords. Some of the things I think are are confusing about GraphQL resolvers. Resolvers think if you are coming from rest API, think of resolvers as a handler or a controller. It's a little bit more than that. But honestly, that is the sort of the simplest way to think about them. Resolvers are responsible for providing the data. Okay, so I show you some examples here. Let's go back. And you see when you issue a query like this say we want to find a specific user with an ID of one. We are saying that we want the name. This is the name field. The fields are important. We treat fields fields individually, then posts. Then you want the user with the users post and the post fields. You need the title and the createdat. And in GraphQL of course the resolver execution would look like this. You call the user resolver with an ID, then it returns the data that is going to look like this. And but internally it created something like of this structure. But you don't have to worry about that. Most times you are going to use a single resolver to resolve all the fields. So how does resolvers work? Well, This is if you if you are going to take anything from this video, it is this page three steps very three important steps one you define your schemas. You generate the resolver interface from the schema. Then you implement the interface. You know whenever you have an interface you can implement that interface. These three steps are what you need to build GraphQL APIs in go. Sometimes the number two can be done manually depending on the framework you are using, but it's important that you you always have to define your schema. Some kind of resolver interface is generated for you, either explicitly or implicitly, but then you write your resolver implementations by yourself. So this is an example of a schema for our Todo application. You see, it has an ID that is compulsory with this path sign that we have a text that is a string that is compulsory with this pint sign, then a done field which is a boolean. It's also compulsory with this sign. So this um sign, it means it is required. The ID is a boolean data type that we can look at. But string and boolean are self-explanatory. So this ID sometimes you can control. It can be an integer. It can be a string but it's controllable. Then after that after the after the schema generation you you can generate your interface. Now when the interface is generated the interface always looks like this. And the number of methods you are going to have in the interface depend exactly on the definition of your schema. So for example, to do's and to do that we have for query. If you go to your schema we have them here. So this type of query, this is like a function. You are saying that users can call this operations to do's and is going to return a an array of to do's, which is compulsory. The same thing here. Another function called to do that will take an ID, but return or possibly return a to do because you can give an idea of A to do that doesn't exist. And that is how we got this interface. So you only need to implement this to other methods. Now if you think about it there is model Todo here model or to do here. Where do they come from. Let's go back one more step. That model that todo that you are seeing when you run the generator. This type would become a model. So it's like your DTO for GraphQL. So this is this becomes the argument that is taking here is taking the todo as an argument. So this is become model dot What to do. All right. I hope that my explanation does justice. So let's continue to talk talking about how to implement the interface. You are going to have sort of a scarred, uh, a boilerplate that looks like this query resolver. You know, most times it's empty. And then you can inject your dependencies in here and use the dependencies. For example, this my resolver that I have here, even though I didn't put a dependency in there for sure we are going to depend on the to do store, which we are calling from one of the implementations. Right? And honestly, that's it. Just three important steps. You know, you you define your schema, you generate your code and implement the interface is very easy, very, very fast to do. So it's time to code. I'll see you in the coding section. Bye.
