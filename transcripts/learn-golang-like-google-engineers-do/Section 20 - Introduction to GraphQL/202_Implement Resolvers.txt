# Implement Resolvers

Okay. Welcome back. In the last video, we added the in-memory data store. And I want us to start implementing our resolvers. So we are going to git checkout dash B three implement. Implement resolvers. It's so easy to do now that we have the dependency injection in place okay. So open inside graph go to schema dot resolvers dot go open it up close everything out I can let's start with um the create to do which I demoed briefly but is nothing. So first of all, what we are going to do is to just pass the input. Now focus on the input and output. Input is for this. Is this new to do, which we have access to the text field from there. Then output must be either nil, which GraphQL handles gracefully, or the type that would have been a value of. To do so, you must return model. To do that is a pointer to model to do. All right. Let's go ahead and call the store and pass it. Input dot test. And then we we know for sure that this will return us the to do. But we need to convert that to do to this GraphQL type. And then we just return it this way. So it's super easy. Now the conversion between our store to do and GraphQL type, we probably need to refactor the code later to have some kind of mapper that does it. But now this is fine, and this is all that we have to do because we done most of the work in the store Then in update. Update is a little bit more code, but it's super easy. First of all, we attempt to update the to do with the data from input, right? Then we check. We know if the Todo does not exist is going to return NI, so we check and then if it is near, we return an error that says to do not found. If the todo was found and updated, we're just going to do the same thing that we did here. And that's it. All right. Delete is just a single line. We call the delete method from the store passing it the ID and that's it. Then uh toggle is very similar to update. So the toggle and update method are super similar. We we we attempt to toggle the todo. We check if it is ni. We know the order to do does not exist. If not, we return this data. Look at how duplicated this is. You know we are doing it multiple times, but when you are starting something, just keep it simple. Don't rush to refactor to make fancy program. Just keep it going. Get it to work as soon as it's working. You can come around and refactor it. Okay. After the toggle we have to get the todos, which is super easy as well. But the only difference? The only thing that will be complicated, is that we need to return a slice of this type, which is super easy to do. First of all, let's grab the todos from the store. You see, I have all the todos from the store. Now let's make a result that we want to send to the front end. So basically a slice of pointers to todo, and then the length of the capacity of the of the slice that we are making is the total number of. To do so, we are allocating the memory. This is an important step. The rest is just to loop. We loop and then we set the each of the indexes. So this is really really good. After that we return it. And that's it for the to do. It's also super easy. We grab the to do. If it is nil we know it doesn't exist. If not we do the same thing that we did here. And that's it. All right. So far so good. Now the todos by status would be very similar to this. Be very similar to this. The only difference is this call to the model. If not every other thing is the same. So let's go ahead and do it like that. Then we make the result again. We do that loop and then we return and that's it. You see how simple, For how elegant it is. It is. It reads like a book. It's very sequential. There is no trick. Okay. And that's it. This one is managed by the runtime. You don't have to worry about them. You only focus on the boilerplates that you need to implement with this in place. I am eager to test it, but we are going to do that in the next video. So let's go ahead. Um, just to make sure that our program is running, I'm going to type go run. Then I put Dot okay. The program runs so I'm going to stop it now. Status git add git commit implement. Resolvers. And that's it for the video. In the next video we just test the program to see if it works or not. If it doesn't work, we refactor and that's it. We debug. We try to make it work.
