# Add Mapper Functions And Adjust Security Settings

Okay. And welcome back. In this video I want us to make some improvements. So I'm going to check out um six so six dash improvements something like that. It's a little bit vague but it's what it is. So the first thing I want us to talk about is, um, some of the things that we are seeing here, uh, for, um. Here. So let's start with this, uh, use introspection. What does it mean? So it enables the GraphQL engine to it allows when we go to the client, the data that we are seeing, those information that I was able to click on query mutation. And you can see the list of what's available. This is what is making it happen. This is why it's happening. All right. So this is super, super important. Then, um. This one automatic um persisted query. So what does it mean? Now the way GraphQL works is um, so this basically allows client to send a query hash instead of the full query string. It's an optimization thing. So server catches query by hash. And that reduces the network payload. So if you have sent a query before instead of resending the same query, you can send the hash of that query and the server know the query to execute. So it says it's specifically beneficial for mobile clients and large queries. Um, for this it can store the way we have configured it. Now it can store up to 100 cards and then it's using, um, LRU eviction, uh, policy. Uh. What? One more thing we can add is, uh, this is super important. GraphQL allow you to execute really complex nested queries. So the query can be really large. And people can use this to overwhelm your system. So for security reasons, you want to make sure that you add that up. So we can add this fixed complexity limit. In short when you are just starting out you should set it to something really low. So this is a security and performance protection extension that prevents expensive queries from overwhelming your GraphQL server. It works by number one, assigning a complexity cost to each field in your schema, calculating total query complexity before execution, and rejecting queries that exceed the configured limit. So you need to test out with your query and come up with a number that makes sense for your program. Then the second improvement I want us to make is this. If you go back to graph slash schema dot resolvers or go we. This problem exists. Look at what we are doing here. The data that we are returning here, they are almost the same thing everywhere. And for me this is not really nice. So let's come here and create a a helper function that we can use to eliminate that. So let's create a folder called helpers. Or let me put it in the GraphQL because that's where it should be. So help us dot go no I don't want a folder I want a file. So inside graph we create helpers. So it's in the same place where you have the schema resolvers. Everything is in the same path. All right. Now the first one is the convert. To do so we are going to convert to do from models. So this is our own model to the GraphQL to do. All right. So okay I made mistake with the other one. All right. So basically we take the models of to do and convert it to regular to do. This is what we've been doing right. And that's it. If it's empty or it's near sorry we return nil. If not we just return it like this. This is the exact thing what we have been doing here. Then the second one is convert to do so. This is plural. Now this one we are going to use it for the get all and They get to do by status, I think. So we first of all remember we made the result, then we loop and then we return and that's it. So here in our resolver we can afford to return. Convert to dos. Now let me see. Um. Update to do so models dot to do. And then we return nil here. Code is cleaner. Let's go to this. To do so. This will return a single todo as well. So we remove it convert to do so. Then we pass it. The todo code is getting cleaner and cleaner here. There is nothing to do in the delete the toggle. It's the same thing. Remove this one. Convert to do. To do. Where else? Here. Of course. We have all the to do's here. And then convert to dos. This time around. And that's it. In short, if you like, you can do like this here. And you put it in here. Delete this line. Become very clean. What else to do? This return is single to do. So we can do. Convert to do. And that's it to do by status returns a slice of that. So we delete everything we can convert todos to dos. This time I'm going to just leave the variable. I'm just because I think this is longer. And that's it. You see, it's our reservoir is cleaner. The only thing we have here is the validation that makes it a little bit big. But aside from that, it's way, way cleaner. We can we should run it and see. Okay. And then I go to the browser. So run it. Okay. No errors, mutation no errors. Let's look for another mutation. Mutation. I'm looking for a mutation that we used to create something okay. And there's nothing there. Let's create one. Mutation. Create to do an input. Texts are line Java. ID and that's it. And it was created and fetch answered. It's working. And we didn't break anything. We made huge improvements. Which is great. So git add git commit. Add helper functions and helper mapper functions. Are are just uh performance and security settings. Good message. So this is really good. And that will be it for the video. I'll see you in the next one. We're almost done with this project.
