# The Repository Pattern

If you look at the code that we have written, um, I wouldn't I'm not very proud of it because it's a little bit everywhere. In a real world application, you want to be a little bit more organized. And that is what I want to do in this video. In this video, we are going to restructure the code a little bit, not necessarily adding new features, but a little bit of refactoring. And I would show you how we use the repository pattern and dependency injection in real world application. So let's get into it. I create a new folder here six. I can call it repository. And in it we create a Main.go. Aside from the main.go I'm also going to create another folder here called models. And I would put models. Or maybe we can call it user. Okay. So it's in a different package now. Um I'm going to copy everything into this main.go and we start moving things out. Let's scroll to the top. The first thing I want to move out is for sure we have the tables and even data inside. We no longer need this text so we delete that. We need to move out these two into the model. Okay. We want to make sure that you have when you have a type that is exported you want to document them. So we can say user. The comment must start with the name of the type. User is user repre represent uh a customer in database with a backing table of users in the database. Okay. We present. We can fix the. All right then the same thing here. Profile belongs to a user. Something like this. This is one on 1 to 1 relationship. You cannot have two profiles. You cannot have two users. They just 1 to 1. All right. We move that out. Then let's go back to main. What else can we move out. Maybe I'm going to keep the connection here for now. We no longer need this function. But we have this these other ones that we need to move out as well. So we have the models. I'm going to create another folder here called um maybe we can call it domain, something like this. And if you want, you can put everything here. But in short, let's, uh, let's remove let's remove it. And, um, we call it um. Repository. Repo. Okay. And user okay. What I want to do is this I'll create a type here called user repository user repo like this. It's an interface for user repo tree. It's an interface. And we can before I put anything inside I'm also going to create another type here called maybe. User. repository. You probably can have like a NoSQL user and all that. So for this we have a DB instance and we can do a funk. Like this. So this is like a constructor for us. You can add a you can add comment create new user repository type okay. So far so good. Now let's set the data here. Good. So what next. Let's go back to main. This is the function that we have I'm going to copy this. Go into the repository the interface and I will put it like this. Well we no longer need to take this. We need everything here. And that's it for this one. Then we also need this one. Go to the the interface. We paste it. We no longer need this. This is going to come from models. This is the import part. This model is what we just created here. Let's go back to main. Get all users the same thing. Put it in the interface, remove this one. And of course we have to replace this like this okay. What else do we have? Okay. That's all beautiful. So far so good. Now we need to implement all these three methods on this type. So I am going to use uh if you want, you can use your ID to generate it. But since I'm trying to avoid magic, let's, um. Let's do it this way. So like this. That is the first one. This is the second one. And this is the third one. At that error that you've seen now is gone. I'm going to just return zero and nil for now. Here's where I'm going to return. Nil nil. And this one nil nil as well. This I don't want to return a slice of pointers to users. I just want to return the slice of users. Okay. And we need to also update that. Okay. Beautiful so far. No more errors here right? We still have things to do here. And then in Maine, let's start moving the code out. Gradually. This is the create user. I'm going to copy the whole code. Then we fix we delete the function from here. Let's delete this one. Go back to the repository. Let's paste it and start working on it. Context is here. Ideally we should accept the context, but I'm going to keep it the way it is for now. Remember we have this DB embedded. So we can now call DB that give us the transaction. We have this. We don't need to change anything here. The TX we already have it. We don't need to change anything. Nothing needs to change here. Nothing else needs to change here. Everything so far looking good. And that's it for that function. It's been implemented super easy, right? The only thing we change is the reference. Okay, let's close this one and go back to main. Let's copy the get user by email. And we delete it again this. Get user by me I will paste okay. We have the query. Now we need to also call r db. Now this is models dot user. And that's it. Nothing. We didn't change anything. Here we are querying. We now using prepare statement for this. But it's fine since we are only using it once. This also protects us from SQL injection. So this is fine. And then we return. Okay, let's go back to do the last one. Which is? Get all users. Copy everything or cut however you want to do it. But for sure, delete it. And then we go back to the get users inside the repository. Paste it. Let me scroll to the top. We reference the DB that is embedded. And of course here we need to change it. We need to also change this one. Models dot user and that. Clear out all the references. Nothing else need to change. And that's it. Let's go back to main. In main let's for now I can create another function here called func connect. Connect to database. Name. String. And what we need to return SQL that we can return this. This. The same thing that we've done here in main is what we are redoing. I'm going to do the ping as well. Return. Connect. Okay. What else? Hope I didn't make any mistake. Okay, then we go back. Go here. I connect to the database. So we use the function here now instead. All right. That looks good. We check for error. This error checking main. Let me write a small function here called check error. If error is none we panic of this one. And so instead of that this should only be using main. After that we have access to the db. Now I just want to run the code, you know, to make sure that uh, nothing much has changed. So I can delete this. Delete this. We are not using this okay. Let's check the repository import. Everything looks good so far from an import standpoint okay. Looks okay or main. We have the SQLite imported. Okay, so let's run it and see what happens. Six right. Repository main. Okay. Looks good. We are able to establish connection. So how do you use it. We can now say repo. Uh new. User repository. And then we pass it the DB. Let's see what it takes. This is the DB. We pass it and that gives us access to. Now do this. Let's say user error repo dot. You see you can create now. User from repo. User from repo at IMI and outline this and password avatar. Okay, we check for error and we can print out the user. Let's run the code. You see, now we have, uh, we didn't restore the table name last time when we did the test testing. So let me go to the implementation and fix it quickly. We intentionally did this to test the defer statement. So I'm back here to main. Let's run it now. Good. We are able to establish the connection. We're able to create a user. And just to make sure that we are really creating the user, let's check the database. And it's in the database. No magic. Everything we are doing is real. So what have we done? Think about this. How beautiful it is. Let's say you have an app. You can your app now have access to this repository. That is an implementation of the interface. That means, um, let's do a write a function here quickly. Just something really small function print users. And I am going to say repo. Um. This is not what we are going to use. We're going to say user repository. So users we check for error. Then we loop and we print the users. Maybe we just print the name or the image of the users, probably with the ID. Okay. Think about this. We this this function that we have depends on an interface. Look, it's an interface. And the beauty of this is this. You can you can implement that interface to talk to Postgres, to MySQL, even to MongoDB and produce the same results. So here what we are going to do I'm going to just come here. Now we have the repo already. I'm just going to come here and pass it this implementation and run it. And we should see the result. This is the power of interfaces. And at the same time, what we are doing here is dependency injection because we have been able to use, uh, this is sort of the constructor to inject this dependency. It's a powerful idea. If we have a different data source, it can be maybe be MongoDB or Mongo. Whatever. And then you like Mongo connection as long as it implements all the required methods. It's fine. And that's it for the video. I hope you've learned something. We are going to use this concept a lot. I want to go back to just scroll down to show you everything. We we change the connection, refactor the code, move something to the model here. Then the repository is here as well. And look at how we move the code from very ugly code to well-managed code. We can. This is testable. Now we can really test things as the way it is. And that's it for this video. Thank you so much for taking the time to finish this section. I will see you in the next section. We need to start building APIs and many, many other things. Thank you. See you there.
