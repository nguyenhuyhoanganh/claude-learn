# Fetching records

In the last video. We use this function to create some data in our database. Very simple insert statement. We encrypted our password and then we executed the query. In this video we are going to retrieve data from the database. But before we do that I want to emphasize on something. If you look at so far from the SQL library, we have used just one function. directly from the SQL itself we use very few functions and that's the power of go. You don't need to know so much. You don't need to remember so much. You can do a lot just by following simple convention. We open the database and just with the execute that we have here, we are able to insert the data and bind our record as well. So this is really important. And then this video that we are going to retrieve data from database. You're going to see that we don't have to do so much as well. So let's do it. Don't worry about this query line is trying to get me to choose a schema. Do some other things, but it's no problem. As an ID thing. So function we want to maybe get user by by email something like this. Okay. What? But how. Where is the user. Good. So what usually happen in go is let me scroll to the top. When you have a table like this, you also have a struct that map closely to what the table is. So we can have id. Int int or int 64. Whatever you want to use. And the name email. This DB tag is not important. If you are using Aurum, you might want to use it, but for sure the DB tag is not important for this video. When we use an Aurum later, we can learn how to use some tags like that. We have the hashed password for the hash password in case we want to turn this into a JSON. For instance, I don't want, um, the hash password, and I'm not sure if I explain this. Omitempty Omitempty means do not do not include email in the JSON. If the value of email is empty, that is what it means. But for this we don't need them as well. So I'm going to take them out. Then lastly, let's add an Createdat and bind it to this. It's a time so I can put a time dot time. Beautiful. Now we have the user struct. This is what we are going to return from our function. So I'm going to scroll down now to our function. This is our get user by email. We can return a possible pointer and. B complain. Let's let's make this exported as well. So it's fine but it doesn't. It's not a problem. Since you are in the same package you have access to all the variables. So let's do it. Statement. This is what we did earlier. Select from users where email equals this. But careful if you have a big table you want to name them like this like ID email. In short let's go ahead and do that because I think it's better ID email. So let's say name ID email and hashed password and then createdat. It's important because the order is important for the next thing that we want to do. After that, we just do the same thing. We execute, we sorry, we do. We query. I still have this in my brain. We queried for a single role. This is important query role. So we are querying for a single row. And then we are binding our email here to this. And honestly that's it. If you think about something doesn't return error why. Well it's a little bit strange. But when the data doesn't exist for example, if you try to use this and the data doesn't exist, then you're going to get an error. And here's how to do that. Now that we have the rule, we have to do something called scanning. And this is the difference between what we have in and maybe some other language. You need to scan the data back into this truck. So let's create a var user. And this is what we want to return. So let's go ahead and return it here. Now I don't want this uh what the ID is doing here. Um, so this is what we are going to return later. But here, now we need to say error equals scan. Then we scan the record into this. So we scan the id in this order. After the id we scan the name we scan the email hash password and createdat. Now this is where you can have an error in case the record doesn't exist. So if there is an error we return the error. But sometimes this error can be something called SQL dot error no row. So this can be the error as well. That means you didn't find the record. And maybe you want to do something with this error, like returning a 404 or something instead of like you need a way to distinguish between this and a real database errors, like maybe an error in SQL or something. So this is an important error type that you should take, um, use. Now that we have this, we have scanned the records into the struct. We return it. And that means here for example, we can come here now and say, maybe Jane, we can get this by email and pass. Jane, I need the I need the data, I don't okay, let me go here. Is it possible to copy. Good. So I copy the email and put it here. And of course we need to pass the database handle. And let's check for error. If there are no errors we can printf the data. And that should be it. We can now run the code. Uh this is where am I query. I made a mistake again. I didn't create a new, um I didn't create a new folder for it, so I need to do that. But this is the record, so let's quickly do that. I'm going to undo. I copied it already, and just. I'm just going to undo everything quickly. Okay. That's the last. I cannot undo more than that. Okay. Great. Okay. And I will come here. Three. And I will say, instead of naming the other one, ideally this should be instead of query should have been insert. So here I can say three. Select something like that. Then main dot go and we copy paste it here I want to make sure that there are no errors in this one to disturb us. Okay I think that's fine. Now we also don't need this here anymore. And that should be it. So we have the select here. Then I'm going to go ahead and run it. Three mean. Good. We have the record here all the time. Look at the password and everything. And you already learn how to do JSON. So if you want you can turn this into a JSON by saying um byte slice. Dot. In. Then. And I can do this then print it out. You see, this is the record we fetched from the database. We just turn it into a JSON. But you notice something in our JSON, the password is not there because we put that dash is very important. It's a security trick that you should always do. And this is how to retrieve a single row from the database. And you can use this query. What if you want to retrieve multiple rows. It's very easy as well. So I can go here and say funk get users and I will do this. So we email, grab everything from the database. Then this time around we're just calling query. We are not having any clots here. That is why we are not passing anything. If you see you see you can bind parameters here, but we don't have any parameters to bind. So we check for error. If there are errors we return, else we defer the closing. This time around, we have to defer the closing of the rows because there's multiple rows and it's using some kind of mechanism to, uh, sort of causal scanning going on in there. And then we create the record that we want to send out. Then we start going through each row and then we scan. So we have to scan doing the same thing that we did earlier here. And we are going to scan everything there. So we have it I can see error. And then rows dot scan And we scan everything. If there is an error, we return. So the scanning is very important. The order is very important. After that we can append to this. Um, this one the want me to use like an empty slice like this. So it's fine. You can do this or you. Yeah. This is actually more beautiful after the append. So each time we scan a row, we append to this. Each time we scan, we append to this. And after that we we return. But there is something else you can do in between this, these rows. If you look at the rows itself it can have an error. Sometimes it returns an error. So you need to check for it here. Error. And then you can do this. If there is any error you're going to find it here. You know during the fetching of the row. So and this is how to fetch multiple records. If you look at the code, it looks a little bit, um. It's like you're probably thinking that this is a lot of code just to fetch data from the database, but honestly, it's not. I think most of the data is in this scanning. Right. And this is why, uh, we have some other utility to help us do this, scanning automatically. But when you are learning, learn the low level stuff, learn how the thing is done internally first. But this is how you do it. Now let's take this record and go to main instead of Gene. We can call this one users and I'm going to call it and let's call it here. I'll pass it to the JSON. And then we are going to have the JSON of the users. And this is it. You see. Think about it. This is almost like an API right. You have very good record coming from the database directly. I don't know about you, but honestly this is so cool. Um, you have two functions to help you do that. And that's it. For this video, I want to scroll down back down slowly just for you to see the code. And then in case you want to type along, which I encourage you to do, you can type along. So I'll scroll. You know this is a database scroll. We retrieve the data. We created this function. If you look at these two functions they are very similar. The only difference is that the scanning for this is in um these two functions are very similar. The scanning is done in a loop, while this one is done outside because of a single query. And that's it for the video. Everything that we've done here is applicable to other database systems like Postgres, MySQL, whatever you want to use is the same idea. In short, when this code the way it is now, if you take the same code and you change the connection to Postgres, it should work or MySQL actually should work. I think for Postgres. The only thing you have to change is this instead of this question mark, Postgres uses number. So Postgres it will be like this dollar sign one. And then here will be dollar sign $1 sign two. Two and three. This is what you're going to have in Postgres. This is for Postgres syntax. But aside from that most of the SQL syntax standard is the same and it should work for all of them. So thanks for watching and I will see you in the next video. I want us to talk about something very important called Prepared Statement. After that, we can do some kind of design pattern that is very common when you are building microservices. Thank you for watching.
