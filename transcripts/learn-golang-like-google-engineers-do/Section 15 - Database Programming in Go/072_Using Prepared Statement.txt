# Using Prepared Statement

Hello and welcome back. In this video I want us to talk about prepared statement. Now, um, I already created a folder which is this one for prepare and then Main.go. So what is what is a prepared statement? In the simplest term, a prepared statement is a precompiled SQL statement that can be executed multiple times with different parameters. So technically what does that mean. So instead of you because when you execute SQL statement, just like our programming language, it goes through different phases like passing, compiling, then maybe of some optimization. All that different phases needs to happen. When you use a prepared statement it happens once. That means you prepare the statement, it gets passed, compiled, optimized, and then it is stored. Each time you pass a parameter, that same statement gets executed, which honestly, from a performance standpoint it is better. But even more importantly is security. The security SQL injection is one of the most common web or web application vulnerabilities, and you want to try as much as possible to avoid them. Prepared statement helps with SQL injection because. Parameters are sent to the database separately from the SQL. So basically what happened is because you have the prepared statement, the parameters will be sent to the database separately from the SQL query. So this is an important. Feature that helps with SQL injection. And sorry I'm explaining a lot, but this is a very important topic and the code doesn't look that much different from what we have been doing. So what I'm going to do now is to go ahead and let's use the prepared statement. Let's go here. This was the last query I'm going to copy everything. Bring it here and paste it. And this here is where we are. We have the table. We already created a table so I won't I don't need this one anymore. We need to keep this. We need to keep this path. And this is the function we wrote last time. We can delete this one now and keep these two functions okay? Three actually. All right. So this is what we have. In short in order to make sure that the code is still working I'm going to run it. Good. Our code still works. So how do we change this to prepare to a prepared statement. First let's start with the create user. Maybe we can change the function to create user with Prepared. Prepared. Something like this. Not much is going to change, but if you notice what we are doing here, the this this this SQL query, we are passing it directly here. We are going to do it a little bit different. Now instead of just passing the query like this here look at what I'm going to do. I'm going to now say error because it returns an error. The DB dot prepare and then paste it. And this converts it to a prepared statement. Now we need to check for the error. We check for the error. Now because of how prepared statement work you need to defer the closing. So statement dot close should be deferred after that. This is the regular password hashing that we are doing. Nothing has nothing. Nothing has to change. And then we have the Here. But what happened now? You don't need to pass in the statement again because you can execute the statement. And this is what I was explaining when I started that. When you have the statement precompiled, you can execute it multiple times. You can take the same statement again and execute it with different parameters. This is super good, especially when you are doing something in a loop and the rest of it remain the same. Not much has changed, but very, very important that we have been able to transition this code from, um, maybe a good code to a great code. You know, relatively. And you can now go and let's use it. I can I want to maybe comment this part out. Passing the DB. And name Serve us email. At localhost. Don't try to send an email to it, please. Test password or something. And I think that was the last. Uh, and here I don't really care about the return data. I'm going to do it like this. Or maybe we should check for the error. At least error? Always. At least when something doesn't work, we should know. Now let's run the code. Encoding. Imported and not used. Go doesn't like that. So let's delete it or comment it out for now. Run it again and good. It looks like nothing happened. But for sure something happened. Let's open our database. Don't forget, if you are using, uh, Visual Studio Code, I showed you how to use it. And here this is the data. I look at the data that we just created. So prepared statement it works. Looks like nothing much change. But trust me using prepared statement is way, way better than what we did initially. And that's it. About prepared statement. You can prepare, use it to prepare any kind of statement and then execute it something quickly. Before I wrap this up, um, some time context is important. I'm going to show you later, but let's say CTO create user with context. And then here I can take context dot context. Sometimes you see this like this. Then you can see execute with context. Or execute context. This is it. Sometimes you see it like this as well and not much has changed. If we want to use it, let's go here. We can replace this with it, but we need to pass a context. You have two ways to do it. One, maybe you can pass it directly here, but let's just create it out here context with background and pass the context. If you are in a web environment, the context would come from the request. So but this is very good, good practice. You want to always pass around the context in go. Nothing much would have changed. I can say two two and that doesn't need to change and run it again. Good. And the data would have been created as well. Look at it. So fantastic. All right. Um that's it for prepared statement. The next thing I want to talk about is transactions. Um, transactions are very important. And we talk about it in the next video. So see you there.
