# Update AuthService To Use Our Repositories

Okay. Welcome back. Now that we have our implementations for Cat repository and user repository, we should use them. So what I'm going to do is to create a new branch. So git checkout dash B 61 replace. DB query with. Cause something like this quite a long branch name. And then how do we want to do it? I'm going to for this one let's rely on the intellisense auth underscore service. So let's close this up as well. So open auth underscore service. And then let's start from here. Let's start with register. The first we should um let's first of all remove this code. So you cannot register with this email. So what I'm going to do with this is to come here. Maybe something like this. So first of all we need to delete this for sure. We we don't care about the user but we care about the error. So we are going to replace this one with call to use s dot user repo dot by email. And then we pass it um request dot email. And that would solve this problem. So if there is an error. Um we know that they cannot use the email. Right. And that will be it for this. Then here we don't need to change anything for his for sure. Here we need to change something. We need to change the call to. use a repo here. And then let's see what why do we have this error since here if we can, of course, if we cannot create the user we want to return an error. But I want to know we don't. This is not. Yeah. So this is it for the cart okay. Let's see. We're going to call the cat. Card repo create. Of course we don't need this one. And here basically the we there's nothing we can do just because of the linter. Maybe we need to do something like this to make the linter happy. Uh, this you can put a inject that our logger, to print a message to the logger. But even though we cannot create the cart, you want to make sure that the registration still continues, you know, and then I think that's it for that, for this. Uh, how about login? Login is also quite similar. So let's come here for the login. We are going to need uh here. So let's say user an error. So we are going to get it by email and active. And then we pass it the email. And true we want to make sure that the active is true. Here we are just going to check for error. So we remove everything here and check for the error. Like this. This is good. What else here. Um password check. We don't need to change anything there. And this place, since the user that we return is a pointer already, we don't need a reference here and that will do it for that. Nothing else have to change. okay for the refresh token. Very similarly here, this is where we need to change the code. So here I can take this var out. And do this. So let's dot user repo get valid refresh token request dot refresh token. So we check for error we run into an error. We return this error. If not let's try to find the user. Very similarly. Let's take out the var and then error. So we want to find the user by id okay. And then we check for errors. Well good good. So far so good. Here we can call the user repo delete refresh token by I. I. D. So what I'm going to do is to do if. Equal to s is a repo the refresh token by id passing the refresh token id. And then we check for error here I don't want to I'm going to ignore it. But don't forget to print a let's for now. Since I didn't include the log, I'm just going to put a message in the log here. Uh, maybe. Where is the log we can put like, uh. Yeah, I think this is enough for now. There's no problem here. We can. When if you inject the logger into the service, you can use the logger here. Then we just pass the user here. And that's it for that for log out. It's quite A straightforward noun, we just call s dot user repo dot refresh token. And then for the generate response, let's see where we need it in the create refresh token model. So we are passing here so we can delete this line and say do that same thing S dot user repo dot create refresh token and pass it the. Reference to the refresh token model that I have we have prepared here if we cannot. So this is the response that we want to generate if we cannot create the refresh token. Now the question is we cannot create refresh token. Do you want to generate the response or not? Honestly I think for this it depends on um It depends on what you want to do. I think if we cannot create this refresh token, I'm going to return an error. And then here I think that's it for for this. Um. Let me see. But before let's let me copy, cut, copy the code and then undo it. Let's see. Let me see what we did before. Okay. Here we were not returning anything before. So let's let's do let's ignore it for now. And we can use the logger. I don't I'm trying to make sure I don't change the behavior of the program, that the refactor doesn't change the behavior. Continue to work. Of course, there are a lot of rooms for improvement here and there, but let's make sure that the program still works. So everything that we have adjusted, let me scroll to the top. We have used the user repository and the repository. In short the cat, we didn't type it well. So I'm going to use a Refactoring power of my IDE. So say Cat in case your IDE doesn't support this, you have to do it manually by yourself. All right. With this, I have renamed, uh, all the instances, and then we replace our call to the DB with this, uh, repository methods, or I use this repository method. We no longer depend on the DB anywhere, and there are no squiggly line. That means we look good. So far as you can see, everything is looking good. And that's it. All right I'm going to go ahead and commit the code git status git add git commit and then update auth services to use repositories. I think this is a good one. All right. In the next video we are going to test this to make sure our code still works. So I will see you there.
