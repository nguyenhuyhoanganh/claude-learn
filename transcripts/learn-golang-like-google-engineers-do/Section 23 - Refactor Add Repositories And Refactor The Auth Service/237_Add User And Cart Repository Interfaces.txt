# Add User And Cart Repository Interfaces

Now that we have extracted interfaces for our services, I want us to try and do a little bit more on the service layer. What we did here. The handler is the one benefitting from it. So for example in our auth handler when you come here, this auth service that we have is an interface. Similarly if we go to let's say auth underscore service go look at the problem we have. Let me scroll to the top here. If you come to the register method you notice that we depend on the object relational mapper the ORM. We depending on it directly here. This is the reference here called db. And we are making the query right in the service. Look for a small project. This is fine. You don't need to complicate it. You don't need to refactor for this at all. But I want to show you how the possibility. So we are going to do for the auth service and you can do the rest by yourself. What I'm going to show you will be very easy to do for the rest of the services. So let's focus on just the auth service. The first thing I want to do is to open my terminal. And do git status to make sure that um, I have all my changes committed. Now, if you have committed your changes from the last video, you can just branch to create a new branch. So I'm going to just commit my changes first and then extract interfaces and fix linter is something like this. Okay, now we are no longer working in GraphQL. So I'm going to just say git checkout dash B 58 dash. What do we call the call. This um let's say add user Um. Cut. Something like this. This is what we are going to do. So let's think about it for a minute in the register. So inside auth underscore service in the register method we have a way here to. So we are trying to find a user by an email here. All right. So we need a method for that. And here we are creating a cat. So we are creating a user. We are going to need a method for that. We are also creating a cat. We are going to need a method for that. We are going to need a method for finding a user by email. And this is active field. And then for Refreshtoken as well, we're going to need a method to find a refresh token. So you see how I am sort of um, trying to understand the code and pull out potential methods that we can use. So let's go to internal and then create a new directory called repository. So inside the internal create a new repository there. Or let's make it plural repositories. Okay I made it plural. And then in it let's create um auth or user repositories I think we can create user repo. Z3 something like this should do. Okay. Um, one one way we can do this, since I'm going to put all the interfaces in the same file, maybe first of all, let's name this to interfaces. Pardon me for the, um, I'm just thinking as I'm doing it. So the first interface I want to create is the user repository interface. So this is the user repository interface. And remember we are going to need a method for get by email. This get by email would accept an email address and return us a pointer to models of user and an error. Okay, we are going to implement all this, but we are just defining the interface. We haven't implemented it yet. Just definition for now. All right let's add another method for get by ID which is going to take the user ID and return us a pointer to user and an error. I'm going to go over it a little bit more quickly now than I can explain anything that we that might look confusing that that would be it for that then for the refresh token as well. And that's it. Okay. This is for the user repository. Basically everything that concerns the user. Honestly, I don't see anything that should be confusing here. These are just methods. We haven't implemented them. You know, they're just we just specify the signature and that's it. So let's add another interface for cat repository. So let's call it cat repository interface okay. Let's add the git by user ID. So if we want to get a cat by user ID and then we want to create a cat, we want to update the cat. We want to delete a cat. Basically this is a Crud operation. So if I move the see up here at least like get um, this is a reading part. This is the updating part. And then this is the delete part. We have a similar situation here. The crud is also happening here and here as well. So this is the interface for the user and the cat repositories. All right. With this out of the way I'm going to end the video for um this video so you can have the chance to type it out in the next video. We are going to start implementing these interfaces. I'll see you there.
