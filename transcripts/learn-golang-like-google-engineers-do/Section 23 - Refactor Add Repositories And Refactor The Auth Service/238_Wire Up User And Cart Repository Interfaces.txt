# Wire Up User And Cart Repository Interfaces

In the last video, we created these two interfaces, and in this video I want us to wire them up. So the first thing I want to do is to I'm going to keep it in the same branch actually. So the first thing I want to do is to go to services. So this auth underscore service. So go to auth underscore service and then scroll to the top. Just scroll to the top of it. So here where we have the type auth service the struct type. What I'm going to do now I'm going to delete this. Of course this is going to make the code to not work anymore, but it's fine. Then I am going to also delete it from here. You see, I deleted the field and I also deleted the parameters from the constructor. We no longer need that db. The code no longer going to work, but it's fine. That is why we are engineers. So what do I want to do? I want to start by creating the, um, the field here. So user repo and that user repo will come from repositories user. So let me import it. So where is it here repositories then this is user repository interface. And then similarly cut repo. And we have the repository interface. So we have these two interfaces here. We need to include them here in this parameter now as well. What I'm going to do let me break this down to a new line. So I'm going to add them to the end of this. And then add user repository here and cut repo you. Most times you can just copy from here. And so this is the function parameter of course. Don't forget to add a comma there. And let's come here and initialize them right where we are calling this still not going to work, but look at what we're going to do. We are going to if we go to the usage which is in the main dot, go basically let's, let's click press on it. So inside our API slash Main.go open it up. This is where we have our OTT service um created. Now we need to pass these repositories which we have not um, which haven't created. So we're going to leave the code like this not to work. It is fine. Right. We are going to leave it like this. Then in the next video we you know what? Instead of waiting till the next video, look, let's let's go, let's go in here so that we can put it in a working state and commit it. So let me say a user repo. This is what I wanted. And then what I'm going to do is type OUP user repository like this, then I'm going to say font new user repository. And for now we are just going to return this. But since we have not implemented the interface, even though I'm going to here, we are going to need that DB that we removed. So let's go here and copy it. So I'm just going to I'm going to copy something like this into here and put it here. So this is the DB. We are going to accept it here as well and then have it in here. This is the user repository. We are going to duplicate this for the repository as well. Um but there is a small little squiggly line here which is saying that since we are creating a constructor here that this should be exported. It's fine. We can take that advice, right? It doesn't hurt. Um, but this is, this is it. So can we use this in place of the user repository? Well, the answer is yes and no. Yes, we can use it. But no, it's not going to work. So let's go ahead. Create the one for the card quickly repo. You know and then I'm going to copy this and just paste it here. And we just change it here like this. Cat cat repository. Replace this with cat new cat. Repository okay. And of course we should be returning this here. And this are the two different repositories we are going to do. Now let's open the main back again. So main here. Let's come to this place and say user repo equals repo. Pause dot view user repository. And of course it takes a DB. We have it. We do the same thing for cat. And that's it. So we have created a two dependencies. And if we hover let's put this in a new line. So this is the auth service now. And if you hover over this you know that we accept the user repo. Then the Cod repo. So let's pass the user repo. The error is now going to go away. It's going to complain. And that's it. So what is the problem is saying that we are using these two repository which has not implemented the interface. So what I'm going to do I'm going to stop the interface. So let's go here quickly and stop them. Um let's go to interfaces. So this is for the user repository. Just copy everything. We won't write the implementation yet and copy it. Bring it here and what is going to happen. Let's copy this. I'm going to say func for each of them. What what should we name it. So let's name it R like this. Then pointer to this and like this okay we need to import this model package. Import it. And that looks good. I'm just going to return ni ni for now just to put the code in a good shape basically. So I'm going to copy this paste here paste it here paste it here. This is something we have to do really fast. Paste it here, paste it, paste it, paste it. We won't write any code to implement it. We're just going to create a happy path for ourself. And then we get out of it. So open it up and then return where you need to return or where you need to return. Just name. Do it. Everything is to create a happy path for us. And this is how incremental development work. You give yourself a happy path and then you come back to work on the main thing. Okay, that's that creates, um, a happy path for us. So let's go to the interface again, copy all the method for the cat repository and then paste it. We do the same thing. I'm not going to type everything so I will copy this from here. Go to cat repository. Paste it behind this one. Then copy the name and put it here you know then copy everything paste paste it paste this right and that's it. So we do this Return nil. Return nil. Here. As for good. Now let's go to main in API slash main dot and let's see what's going on. We no longer need this DB. And look at it. Everything looks good. We don't longer have those squiggly lines. Our service. We configured our service with the two repositories. Even though the implementation there's nothing in there. But look, there are no squiggly line and we look good. All right. So this is the hard part for us. And what I'm going to do now, um, I'm going to say git add git commit um add user and card repository interface in the next one. In the next video we are going to implement each of the methods. So I will see you there.
