# Implement CartRepositoryInterface

Okay. Welcome back. In the last video, we worked on the user repository implementation. Right. Um, this is the code. It looks very, very simple. All right, now open the file browser here and then open the cat underscore repository. And then let's start implementing this. This is super simple. There are almost one one liner. The first thing you need to do here is to of course create the the model that you want to return, which is the cat. Then we do we run the query. So basically we check if user ID is equal to this, pass the user ID and then bind it, check for error. And then if there is an error we return nil and the error and that's it. Just return and that's it for that. This one there are one one liner are to create here is for update our DB. So we just save it. And then the last one of course the lithosphere is one liner and then we delete. And that's it for the Cat implementation. So easy right? So open the terminal. Since we have not checked out let's check out and commit it. So I'm going to say 60 dash B 60 add in okay. Maybe I think we named it implement. Um cat jittery. Interface something like this. Now with that out of the way git status git add git commit and then implement. That. Repository okay that's it for the video. In the next video we start using these methods in the code for sure. All right. See you there.
