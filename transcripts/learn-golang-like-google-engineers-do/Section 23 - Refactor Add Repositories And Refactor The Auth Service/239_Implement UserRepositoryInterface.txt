# Implement UserRepositoryInterface

In the last video, we created the happy part for ourself by adding some stubs, just, you know, empty methods. Today two interfaces. This time we are going to implement the user repository interface. So let's check out a new branch 59 and um implement user repository interface something like this. That looks good okay let's open it up. So in repositories open user underscore repository. And then let's close out that part of the IDE. All right. Let's start with the let's start with this um gate by email. The first thing I want to do is to create an instance of what I want to return. So this user model, this is what I want to return. And then with that in place what do I want to do is very easy. So you can let's say this is R dot DB EB dot where and then you put the email just by email. Right? Then that is the email. Then you call the first, and the first is going to bind the data to this variable that we have here. But of course this first returns an error which we need to check. So if you if you can assign an error like this and then like this, and then you check for error. If error is not this, return this. Else we want to return this. So this is classic. There's nothing complicated here. But I want to simplify the code a little bit. We can use the long approach that if error not needs then we return it. Then we can delete this part. And this is how we are going to do for most of them. And that completes the implementation for the gate by email. For the gate by IDs I almost identical. So let's put the instance here. And then we do the same thing. And then we return the user. You see it's very very similar actually. If you want you can create a helper method to do it. But let's not be too tricky. Let's do things simply, in a simple manner. Alright, this one as well is also not that difficult. Almost the same thing. That is a powerful arm. Don't expect to write a lot of code. So here basically db db dot where m equal to this and I and is active equal to this. This is important that you use this question marks. Very important. Don't try to concatenate your SQL query. You are going to have SQL injection, the ORM and the libraries that protect us by using this query binding. So it's super important we bind it again and then we check for error. And that's it for the create a super easy one liner. So we just call the DB dot create and that's it. Pass in the model that we want to create. Update is also the same thing. And then what else. The for the delete as well is about the same thing. So these are just for one liner the powerful actually. And then um What? Which one create? We have to create the for the refresh token as well as one liner. So don't forget to pass the model, the right model, then the get valid refresh token. Let's work on that. Okay. And then we find it and then we return it. Basically we make sure that the token is what we pass in and then they expired at is greater than now. That means it's in the future, right? Future is greater than now. Simple logic. Right. And that's it for that. Then the deletion here is also super easy. Um, basically we we first of all find the token, then we delete it. And lastly we also want to delete by the which is similar to this code here. And that's it. All right. And that is it for the implementation of the user repository. As you can see the code is super simple. And this is why I don't want to do the whole thing. Because if you can do this for products you can do this for Cod. And let me show you something. Let's take, um, let's take this, uh, gate by email and active as an example. Note the query email equal to this and active. So if you go to the auth service and we go up we scroll down to here look at this is where we are going to need it. You see that the query is the same. The query is 100% the same. And this is why we are just going to replace this man with a call to the um repository instead of running the query here, but we won't do it in this video. We are do it in when we have implemented the one for cat in the next video. So that will be all. So basically just check out now sorry. Commit your code to git add git commit and then add implementation for user repository. And that's it. Okay I will see you in the next video.
