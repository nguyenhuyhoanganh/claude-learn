# Install Go and VS Code

Let's install go on a Linux system. I am currently on a fresh installation of ubuntu and there is nothing inside the system. So let me open my browser and go to go dev. And open it. All right then open vs code as well. VSCode I don't know the website so I'm going to just type VSCode go to Google. All right. And that's it. And we are on this website. All right. The first thing I want to do before I install go, I want to install VSCode because I want us to have access to VS code to edit some files. All right. If you are on an AMD system or Intel you want to download, um, this, let's go to other platforms and show you. So if you are this x64 is for AMD and Intel processors. For me, I am currently on ARM because I have this virtual machine I'm using on an Apple M1 chip. So this is for ARM. So you probably have to download one of these, but the installation is the same. So I'm going to download my here and wait for it to download. After the after the download just go to Setup Linux and the instruction is super easy. It's a single line of command. So sudo apt install. Then you put dot slash the name of the file. Let's go and write that exact command. I'm going to copy the command. Let's open my terminal. And the terminal is here. Let's switch to my download folder. So download unless this is the file that we downloaded I'm going to copy the paste the command dot slash code and then I press enter. Let me type my sudo password and wait for it to perform the magic. Right. Press yes. And that's it. Look, it shows this sort of error and all that here, but don't worry about it. The installation was good. I'm going to, um. I'm going to remove this file. Just dash f code just to keep our assistant clean of what we don't need. Now, if I go to the list of my application, you see, VS code is installed. All right. Now that we have VS code, it's time to install Google itself. So let's go back. I want to open the command again. When I type go let's say go version. You see, it's not there. But we can install go by running this command. But let's not do that. Let's go to the go website to install it from there, because I want you to get into the habit of installing things by first reading the documentation. It's important. It's very important. All right. So we are on go dev I'm going to go to download. And then on the download page all these ones are not going to work for us. This is an AMD version for me. It's not going to work. This is probably going to work for you. So if you are on AMD or Intel computer just download one of these. For me, I am on a. So I need to look for an ARM version like this one and download it. And the download is there. It doesn't matter if you download the right version. The installation instruction is the same. Now that we have it downloaded, Go to the top after downloading a binary release suitable for your system. This is very important word. Please follow these instructions here and the instruction is super straightforward. We have this long command here that technically running it once would have installed go. But let's decode it. Let's not run it at once. Let's start gradually. The first command try to remove any any old go installation. So basically, uh, they are removing ah f recursively local go. So if you have an old go installation this is going to remove it. Important. Then the next step is to um basically um sort of unwrap this archive and put it in this directory. So that is what we are going to do. But in order for it to work, they didn't put what I'm about to say. They didn't put it in this command. But I know for sure, in order for it to work, you need to start this command with sudo so sudo tar capital letter c. Then you are local. We are going to put the folder that we are going to extract from here x. So this is dash x z and then go remember I am currently still in the download directory. That is why I am referencing it that like this. So if I just press enter we wait for it to happen and it has happened. The goal is already installed like this. Um, I'm going to do less and also remove this file that we no longer need. Right now we have installed go, but it's not going to work yet. So if I type go version, we are still going to see the error message. That goal is not found, no problem. That is because goal go is currently not in our execution path. That is the next command here. But in order to in order to do this, you need to know your shell. If I type echo show it. This is telling me that my shell is a bash shell. So if you have if you see an output that says, uh, let's say it says like bin slash, ssh. If you see an output that look like this, that means the name of the file that you have to modify is different. But for bash. So if you see an output that is x z you will probably going to modify an output that look like this. Uh. And not bash RC. All right. But because this is bash I can type code. Now this is why I say we should install vs code first and I can type, um, rush hour now in the instruction here, they said we should update this dot profile, which you can do as well, but I always put my thing in the same place. So I just open this one. It's going to work with the file opened I'm going to scroll to the bottom of it just down here and I'll type export path equals the existing path. Then just colon slash USAR local go slash bin. This is the bin folder where goal is installed. After this command you go to file and then you save and we can close it. Now you expect it to work you need to restart your terminal. So if you want you can just start a new tab like this and then type go version. And that's it. So goal is currently installed. So how do we test it. Well it's by writing programs. So let's go write some go program. And I'm going to the desktop. I didn't type desktop well. Now I'm on the desktop I can make a new directory mkdir example. Then I'm going to CD into that example. And then this command I'm typing. Now honestly they are basic commands. But if you if you're not used to them just type it and you get the same result. And with we already installed go we just want to test it. So example while we're in the example folder on the desktop, we can open it in VS code by typing code dot to open the current directory. And in that example folder I let me expand it. I can put it a minute. Go in there. We can just say new file main.go. Um, package with AI team will not let someone type code. Actually. So annoying. This this. Func main. And I'm going to show you a little problem shortly. All right. So we I saved it by using ctrl s. And we can click on this place that says problem or something. Or you can uh view I think terminal here if you want open the terminal. And actually you can type go run main dot go here. And then if you say hello world What that means. You are good to go. All right. It's hello, world. But in order for you to use VS code with go programming, um, you know better. You can. You need to come to extensions and search for go. And this is going to give you the go um plugin. Install it. It's about 17 million downloads as at the time of this recording. So it's quite popular. With this we have a lot of opportunities with go. You know, um, we can do more, uh, syntax highlighting, debugging, so many things. You know, you can put breakpoints and do a lot, a lot, a lot with it. And honestly, that concludes, um, that concludes the installation for good on the Linus system. So let me now close, uh, vs code. We no longer need it. And um, and that's it.
