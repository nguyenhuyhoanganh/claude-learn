# Functions

Finally we are going to start talking about functions. We have a lot to do in this section because functions are fundamental building blocks in go. They are used to group a sequence of statements to perform a specific task. Because go technically is not an object oriented programming language, we rely on function to group together other sequence of statements to perform tasks. That is why functions are so important in go, and you must learn them very well. They can make your code modular. They can provide reusability. They make things easier if you design them well. Now we have seen functions a lot in this course already. These are functions that we used in the last, uh project. And I'm going to start talking about functions from scratch again. So let's start a new section here called for um for section. And in it I'm going to create one function and I will create a main dot. Go inside. Okay. If you don't like creating many files, you can of course use the same file. But this makes it more organized as you have seen, to define the function in go, the keyword is func just like this. Then the name of the function let's say grid, and then you can specify function parameters. And for example the name and the the type of the parameter is string. And that's it. This is a full function in go. Basically the func keyword, the name of the function, and it is enclosed with this curly bracket. This is optional. Functions can take arguments or not. So if I take this out, these functions can be this function can be called without an argument. Right. So it's a grid function. That means we can go in here now and try to greet someone with it. Hello. Now this is called function definition. We just defined the function but it does nothing yet. It's different now when you call, when you want the function to do something you have to invoke it. You have to call or call the function. So we can call it. Now with this great name, Bob Wonderland, the brother of Alice and I. Come here section four. One and main. And we should see hello Bob Wonderland. So function declaration or function definition. Sorry. And then function invocation. We can for example func add. A. Int. And we just do it like this. And of course we come here and call it ad one and two, which the answer is three. Obviously when you have variables in the function parameters, if you have variables that have the same type, you can omit the type like this. Just separate them with commas. So you are saying basically a and b are int and that doesn't stop. So you can go here and put name string. You can do that as well. But I'm going to keep it like this. So this is called the function signature. So the signature doesn't change. All right. So far so good. And inside the function you can do anything you can do. If statement you can put any kind of statement that you know already in the function. Functions can also return you data. What we have now. They don't return anything. But functions can also return data. So let's say func calculate area. Give it the width Float64. Right width and height. And we we can add some checks here if it's less than zero which doesn't make sense. Or height less than zero. I'm going to return zero. But at the same time I also want to print out a message here like this with error width and height must be positive, but if not we just multiply them. I can return the statement like this. That means when we want to call this area, I call. I can come here and say area and then I can just say 4x4 for instance. Comes and we print it out. Print out, okay and run it again. We have 16. So this is this function now because when we, when we when we declare the define the function we specify these two parameters. When you are passing it they become arguments. People. People sometimes miss miss miss the miss those two words, two key words up the function parameters is part of the function signature. When you are passing value into it, the values are passed as arguments. I hope I am not mixing them up as well. So this is returning a value for you. It can return pointer, it can return anything. It can return custom type, but your function can return values in short function. Single return can return multiple values. So we are going to look at that next. Yep.
