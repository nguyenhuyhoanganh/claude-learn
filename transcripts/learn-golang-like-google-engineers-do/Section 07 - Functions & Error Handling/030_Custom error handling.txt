# Custom error handling

In the last video, we looked at functions ability to return multiple values, and we wrote this divide function and the split function. Both of them return multiple values. One thing that we can do to make this code a little better is, for example, this error is generic. If we want to test for it, it's a little bit tricky to test for. Let me take this code as this one and let's go to a new create a new folder here five I can call it um custom error. And create a main dot. Go inside this I paste the function. This is what we wrote in the last video. It's very simple function. Here's where the problem is. Let's say for some reasons. Um, I just want to demo something. Now if if B is zero, we want to return this error. But let's do another thing again. Let's say if a greater than. I think there is um, let's, let's put just um greater than 1000 for this is a silly example. Uh, we can say uh, a is too large. Okay. Something like this. Now let me come here and say value error and I will put this check for error. Then print the error. Else you can do this as well. Let's say let's just return and print value here. This is another way to do it. Instead of putting an else statement I'm going to open my terminal. I will go to the folder now which is five main, then run it. The answer is two. Well how do we know for example, if I put zero here? There. We see divide by zero, but at the same time, let's put a 1001 here and make this one one. Instead we are going to still see that error A is too large, but we are printing the same error. Let's say if A is too large, for instance we want to do something different this way. One way to do it is to check for this specific error, right? Like say come here and I say okay, if error is not ni and if error is like this. We can do something else, you know, uh, from print, uh, do something else. Right. Uh, else. Else. Do we want to print the standard error. Right. And we can go ahead and run the code, do something else. But here is the problem. If for some reasons they maybe if you are not the author of this code, or someone from your team changed the error message that maybe they change it for a must be less than 1000, then this code now is broken, right? You see, they do something else that we rely on no longer work. The. We are now getting this generic error message down here. So how do you solve this problem? Well, this is where custom errors come in. You can come here for and say error divided by zero. This is divide by zero. You can create one. Then error are not too large. And something like this right. And we can replace these errors with this. We replace them. So what happened is this when you come here now, you can do if errors. Is this you do something. If error is this, you can do something, you know. Then we come here and run the code number too large and we get. We got the number too large here. But the good thing is this. You will no longer rely on the string value. You are relying on the custom error type. We can go here anytime and change the this the string. It doesn't matter. You are testing for this equality. So this is why custom errors are very important. This is one way to create your own error. But what if you want to have a custom error type with more information? Well, I know we've not discussed structs, but we can create a type, let's say uh, up arrow, something like this. And we can say maybe the operation. The operation. I can put like code. For instance, I can put message, for instance, or maybe the time the error happened and time or time we haven't discussed structure, but just bear with me why you can turn this into an error. So let me come here and say func new new up arrow and we can return. Just fill out the struct like this. Okay, here's where at the moment we cannot return it. At the moment let's say I come here for instance. Um, maybe we should write another function. Test? Uh, no, let's not name it test. Um, example or. sorry. Let's call it, uh. Up up, up. Do something. Okay. And we want to return new, uh. New error. Right. Uh. Do something. Let's say the code is 100 and message is, uh. Do something failed. And the time. Now, you see, this is not a valid error type yet. Why? Look at the error message. Let's start using the error message to help us. You see that cannot use. Put our type here as the type error type does not implement. And it shows the method that we have not implemented. And it's saying that because error is an interface, it's not a concrete type. That means I want to click on it to for us to see the interface. You see look at the interface. That means this is our custom type of error that we have. Must implement this error interface in order for it to qualify as an error. So let's copy that interface here. Like I said, we haven't discussed this part, but I want to show it to you anyway. So I'm going to do it like this or P and we can return maybe just a message. As you can see, just by implementing this error interface, we can now return this as an error. But it's a richer error, um, to return because we have a whole lot of information. We added our own information. So later in the course you are going to see how we can use this in our web applications to track different kind of errors from different levels of the program. So thank you for watching and see you in the next video where we talk about different.
