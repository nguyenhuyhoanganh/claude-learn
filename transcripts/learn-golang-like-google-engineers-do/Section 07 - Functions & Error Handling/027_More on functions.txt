# More on functions

Welcome back. Because functions in go are like every other type. You can pass them, you can return them from other functions and they are quite flexible. So I want I want us to look at that as well. I will change right this um. Two I'm going to call this one, um function as well to something like this. Not very good naming, but let's keep it that way. Okay. Let's say you have um in you can do recursion. Let's start with recursion. So a function can call itself in go. It's normal in most programming language allow you to do that. So we let's do the classic factorial function factorial. And that's it. So If. Let's do the base check. Do the base case first. This is called base case. If it's less than one, we return one. This is a base case. This is how we are going to get out of the recursion. If not, we return n times factorial n minus one. This is classic classic. In every recursion class you do this and uh we can do factorial of five for instance. And then let me print it out. Yes. Like this. So let's go to two print run it in um, 120 which is standard without when you are doing recursion, basically when a function begins to call itself, you have to be careful without the base case, this is never going to exit until you fill up the stack and your program would crash. You know how this stack frame works. Let me explain that a little bit. Well, the first time when you call this function, this function is pushed onto the stack. And then it gets to this place another function, this same function again with a different copy of data, get pushed into the stack like that, like that. So if you don't have the base case, then keep filling up the stack until you exhaust the stack and then boom, you have a stack overflow. So this is a basic form of recursion in go. Another thing I want to talk about is, uh, closures. Um, you can do closures in many ways in go. Like I said, you can return function from a function. You can pass a function as an argument and many, many more. Let me show you one from let's say int sequence, Maybe it went something like this. Okay. And then this will return. You see, we are now returning a function that returns an int. And we can say zero. So we are returning a function that returns an. So what we want to return we want to increment. Uh sorry I want to increment the I and then return it. So basically what is going to happen is next let's keep this one like this. Let's say next sequence next int. And then I come here I can print many of it out. So in each iteration we have the the increment 1234. Because when we call this we are under this function with this data. And you keep calling it like this. So in the code here as well, let's say you have I think you've seen it before in one place that I use the switch statement where I did, uh, let's say. Let me do something like this func logger, um, message string, something like this. And you see we just created a variable that is of type function. And I can call logger see hello world twice. And you see so functions are very powerful in go. You can pass them. You can return them. You can do anything you want to do with them. You can take them as an argument. Um, anything. You know there are like every other types. So enjoy function. In the next video we look at something called variadic functions and I will see you there.
