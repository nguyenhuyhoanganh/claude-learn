# Panic and recovery

In this section, I want us to discuss something very useful. It's called panic and recovery. So I'm going to name this panic recover. So I just created a new folder and I will put a main.go inside. All right let's open it up. We've seen panic before. You can panic the program manually by typing something like this. You know, something bad just happen. And I'm going to run it. And this is going to crash the program. So let's open the terminal, go to panic and recover. And the main.go. And this is the message something bad just happened. Panic and recovery is not exception handling. So if you're coming from Python, Java, JavaScript, HP there are no exception handling in go. But we have panic. The panic is an exceptional situation. Ideally things shouldn't panic, but we usually get panic and most times you don't want to recover from the panic. If you start panicking, you should solve the problem because errors are values in goal. That means when your function return an error, you should handle the error carefully and react based on the error. But when you start seeing panic, then you know there is something wrong, generally wrong with your code. But sometime we still have to use panic. But let's say if something panics, how do you recover from it instead of crashing the program? That is what I want to explain. Like I mentioned, it is not exception handling. Don't use it as an exception handling mechanism. So let me show you how to use panic. Let's create a fake function here called might panic. And should panic. And I will put something like this. If we should panic and then we are going to just call panic. Something went wrong in. Let's put the name of the function here. This is a way for us to just sort of mimic panicking. Uh, Um, this function executed without panic. Without a panic. Okay. And we come here. Now we can run the function output first so that we don't get panic. I run the code and it executed without panic. Let me change this to true. And then it's going to panic. And we say panic, including the stack trace of the panic. Okay, but when it panics, how do we recover from it? This is the thing that we want to we want to handle. So let's say I have another function that is using this one. Let me think let's say recoverable or something. I, I, I yeah recoverable. And this is where the defer statement is also useful. We are going to say defer. We defer the function here like this. Then in the default. You see that we call this function called recover. And whatever we want to do here we can do. So if the recover is not near that means the recover has picked up a panic. Um, event. We can come here and say that a print we recovered from panic. And, uh, I think that's it. We don't want to do anything we call the the function that can panic here. And I'm going to just make it true for now. Then we come here, we call it recoverable. You see. Recover from panic. Something went wrong in my panic. This defining the function and calling recover inside. And if you can do whatever you want to do so you are guarding. So you provide a guard to this function. This is going to protect your program from crashing. But like I say don't do this all the time. When you expect a code to panic, or as soon as your code start panicking, you go and wrap it with this function. No, you should fix the panic. Most time panic is as a result of maybe out of resources programming error. Maybe when something is not supposed to be nil, it is nil and you are calling a method or nil, then it would panic. You know there can be anything you know, but you try to solve the source of the problem instead of trying to recover from a panic. In a nutshell, this is how panic and recovery works. You have a function that can panic, and before that function is called, you have a default func, and then in it you call recover. You always want to check that the recover is not nil before you. If it is not near then you know you are recovering from a panic. So thank you for watching and I will see you in the next section where we build, um, in the next video actually where we are going to build our another project. It's all about project. In this course we're building another project. All right. See you there.
