# Understanding Hard Dependencies

Hello and welcome back to the section. Today we are going to start talking about mocking how to mock some dependencies in your test. Mocking is an important technique. Mocking is super important to learn, and I am going to make it so easy for you to understand. So let's start with some examples. Here is my browser. I have this, um dummy JSON com slash post and then there is a limit here. So this website provides a dummy API for you to use in your program and what you can do. Basically you can change this limit for instance to fetch different numbers of posts. And so we have a list of posts. Right. We are going to write a program that rich this website to get a list of posts for us. And we are going to try to test the program. I want to teach you mocking using different styles, but at the same time, I also want to make it as real as possible. This is an external dependency. This is a network called in your test. If you make the call directly to this endpoint, then your test sometimes will fail, maybe due to network timeout, maybe when the service is no longer available or something like that. So today we're going to learn how to mock this behavior. All right let's go ahead and start. I want to start by creating another folder. Maybe we should call this 116 section. And in it we're going to put a main maybe not a main.go. Let's start with the we can put another folder inside called um one gateposts something like this. All right then in it let's put a minute. Go inside, okay? Because you have seen a code like this multiple times. I'm going to go over this really quickly. So let's create a struct. Write the struct. If you look at this truck that I have let's look at one post. So this is a post ID matches the ID title, matches the title. Here. Look at the JSON tag body matches body and then the user id matches this user id which we had probably not going to use, but I just want to have it there anyway. So we need we are going to interact with just four fields from this struct. All right. So let me take it out now. Now we have the post. Now let's create a post response that we are going to use. So the post response would be this whole object. Here you see this whole object that we have here the whole object. because think about it post. Look at it. Post is here is an array which you have here slices. It's an array. And then there is a total which we are going to pull from here. And that total is here. If you want to add something like skip and limit as well, sure you can add them. But this is more than enough for our demo. So let me move it away one more time. All right. Now with that in place, let's try to write a function that can help us fetch this post. So I'm going to go ahead and put the function signature. The only parameter that we are setting is limit which can be one, two, three and so on and so forth. Then we are going to return a slice of posts, right? You probably thinking why don't we return post response? Well, the post response is something that we are going to decode the the data that we receive from the internet into I'm not in. I'm not interested in. Any other thing here? In short, we could have removed this. But like I said, I'm just structuring it to give you different perspectives. All right, with that out of the way, let's construct the URL and look at the URL is super easy. We're just using Sprintf here and passing the limit here to for example this would be two three and all that. All right. Okay. With the URL let's make the call. We can use the HTTP package to make the call. And then let's check for error. The HTTP package returns a response HTTP response and an error. So we must defer the closing of the HTTP response body. All right. After that we want to also make sure for example when let me inspect here. Go to network and refresh the page. You see that here the HTTP status you see it is 304. So we want to make sure. Let me go here one more time. And now it is 200. So that is the 200 we are interested in. So what I'm going to do is sometimes it can be 304, sometimes it can be 200. But let's, let's focus on the 200. Now let's check to make sure that the status is 200. If it is not 200 we are going to return an error. And then after that let's attempt to decode the response that JSON. Let's decode it into post response. So let's create a variable of post response type here. And let's call the JSON decoder and decode it. So we call JSON new decoder then pass it the body. This is the data. And this is where we want to decode it to. If it fails we return an error. And then if everything goes well, we return the data. Okay. Our program is quite straightforward, very sequential, very classic. This is how most go, um, network calls are made. With this in place, let's go to main and try to use it so we can call this function with um, let's call it with the argument of five. And then let's check for the error. Make sure that there are no errors. If we have an error we can print it out to stderr. So f printf it takes an io.writer. So take an IO writer and then you can write into this writer. So here we are passing the operating system standard error um as an input. If you want you can pass a buffer and the message goes to your buffer. And you can do whatever you want to do with it. But this will go to our console. You won't see the difference, but this is definitely a very good practice. Now with that out of the way, let's just look over the post and that will be it. Okay, so let's run the program. Go! Run! I know his system section, then one, then min. I just run it and let's wait for it to run. Okay. If there are I think let me try to turn off my. Okay. Sometimes the firewall can prevent things from working. I'm going to stop it and do it again because of my internet look. Internet was a little bit slow earlier, but now the post are here. 12345. Okay, let's change it to two and run it again. See we have two. Let's do three. free. We have free. So we know the code works by testing it using the real data. Now let's go back to the program that does this. This is the most important piece of the software. The question is if I give you this, let's say you are in an interview and someone said, how do you test this code? What would be your response? Look at it's quite easy to test, but we have a problem here like I've been mentioning in throughout the course. Anytime that you are writing program, always think about your dependencies. Don't take them for granted because they can either make your program awesome or break it. Our dependence very hard dependency that we have is this. Had the pen then see it is a hard dependency because each time we call this it goes directly to this website. We don't have any way to replace it. It's directly called being called in the code and there's no way for us to get out of it. There are several ways to if you want to. Most there's a lot of techniques that you can use, you know, um, to make this test work without mocking. But today I'm going to show you how to use mocking to test this code. And when you look at your code that you have hard dependencies like this, you most likely you want to refactor. So refactor for testability. Refactoring doesn't necessarily change the behavior of the software, but it can make it easier for testing. And that's the step that we are going to take. So in the next video, we are going to refactor this just a little bit to allow us test it easily. All right I will see you in that video. Bye for now.
