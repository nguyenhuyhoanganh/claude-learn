# Using Testify Mocking Library

Okay. Welcome back. In this video I want us to learn how to use testify mock. It is so easy to use. So let's go ahead and copy and paste this uh one. So it's going to be six. Um it's no longer manual mock I'm going to say test five. I think that's how to spell it. Right. And here let me close this up. Client will remain as it is. This is not going to be as it is anymore. But I'm going to do is to this is going to be very similar. We can still use the table driven test by the way. But when you want to start something, um, afresh, you don't start with complexity. You want to try and start with the very bare minimum so that you don't get confused. I'm going to keep this one for now. Maybe we need it, maybe we don't. But let's keep it for now. And then here we are going to do something different. We no longer need this one, but the signature should remain the same. All right. So I'm going to say mock doc mock here. So we are going to embed the mock from testify. This has make this our mock HTTP client. Now a an instance or sort of has a testify mock. You know it it's almost like the same thing now. But we are embedding it. And this is um, a little bit of object oriented programming. So how do you use it? There are many ways to use it, but let's get the argument. And the argument here is going to be URL. And then let's check um, if the first argument is is knee is is knee, we want to return an error. So if argument is knee we want to return an error. And then if not, we can return the first argument and cast it to HTTP response. And then the second one can be an error. This. These arguments are going to be we're going to start passing them shortly. So. This is all you have to do to use the mock. You know um this is the simplest structure. Okay. So what would a success case look like with this. All right. Let's go ahead and create the mock client I'm going to use new now. So the new give us an instance of it. So this give us a mock client and then the the post we still have the post here. Um honestly I, I think we can I don't know why we didn't re reduce what we have here. Honestly it's overwhelming. We probably can do. Yeah. If I hope I don't blow anything up. But I think we can just deal with one now. And skill limit is one something like this. We even don't we don't even need the limit. I think that's all. All right. Nothing much has to change. Um, so the more the we can use this to create mock response, so that's fine. Um, what I'm going to do now is mock client. Okay. Um, so first of all, let me create the response that we want to return. Mock response. Uh, let's because the function is mock response and this one is mock response. So let me call this mock res response 200 okay. And then let's pass it the dummy post okay. And that's it. Now here's the there's a different way to think about how this testify mock work. So what are we going to say. You have to read it. Read this with me. Say when they. When they mock. Client. So this is, um. Sorry. Let me take this. Yeah. When the mock client. So this is our HTTP client. Now, the mock one. When they mock client, you say on on get when they get method is called with a example.com slash post with a with an argument that is this. It should return the mock response and nil. You see how it reads like English. Right. It's you are saying now. So when anytime we pass it on we call the get. This is going to trigger the so on on get with this argument, return this. So this is how we configure it. And with that we are almost there. We can now say okay our client now new client passes the more client quickly. And then the base URL. Don't forget we don't need this one. With that in place post error we can call client dot, get fetch post and then let's pass the limit of one. Let's just do a certain. Now assert assert no error. Assert. The post is not me. I start the length is one of the post is one. We also want to assert. If we want, we can assert the the title of the post equal. So I'm going to say the title of the post. This is what we have versus what we got. And that's it. Okay. Let's see. He said that something is missing from a go model. Let's make sure that nothing else was imported. So I'm going to do go mod tidy. Let's see if if we tidy it let's run the test again. Okay. Let's see what the problem is. Say fail to decode respond invalid so that JSON that we modify. We definitely messed it up. Um so an array you know what? Let me copy a different one that I trust and paste it here because look like we got it completely messed up now. That means we can just, uh, do this. Let's copy this and paste it here. Or with this. I like this style too, so I copy the title here instead. Let's run it one more time. Good. It passed. Now look at how simple the test looks. And test testing the other cases. It's even easier. So, for example, first of all, let me run with coverage and let's let's be looking at them. If you look at here how do we test. Here we are. We're going to test it say on get when get is called. Instead of it returning a knee for error, it should return an error. That means if an error is returned, this part of the code will be covered. So let's go and do that quickly. So I copy it and then I paste here and I can say network error. So instead of here I said it should be NE and here error should be network error. Everything else. We just need to flip the assertions. So here there should be error. Post should be NE and we don't need anything else because post is NE. Don't can't check length on that. We don't care about the body response body. See how simple it is compared to the other one? Now let's run it. It passed. But let's run it with coverage. I have that this is nonsense. Keep coming up and we go out. We go again. Open the testify. Mock where you have your client. We have it. Now, you see, we covered it. How about this? It's also super easy to do by now. I'm sure you should be able to figure all this out by now, because we just need to change the status code and also change the body. So here we go back here into the test. Uh, what we want to test. We want to test the status code. So let me copy this one. And say not okay. What what what if it's not okay then internal server error here. We don't really care about the body. Honestly. We can put an empty string here. Then error should be we just accept the same error thing here. If you want you can check for the specific error let's say. Um, a set contains. So something like this. Let's save the error contains 500 internal server error or something like that. So what it contains is now the um, one of the special status code 500. That is what we are returning. So we should expect it to contain that. And that's it. And that passed. Then last one is the JSON. Okay. For the JSON for the JSON body invalid JSON. Of course we need it to be okay for us. Here we put invalid JSON instead. So we are going to pass no network error here. No network error. This is the response body. And then we got to this place. Everything is going to fail with the invalid JSON, but the error message will be a little different. Let's copy it from the console. Let's run it. So this is the error message that came. Paste it and then run it again and everything passed. Now when I run the entire code. Run the test. We are 100% covered. So question is, which one should you use? Well, honestly, if you ask me, I say use this testify, mock more, use it more than doing it manually because it gives you a lot of benefit, a lot of benefit, a lot of benefit. And that's it for mocking this approach. You can use it to test anything, anything if you have a for example, if you have, um, let's say this is something silly that I want to show you. Um. Does Masha something like this? You can mark this. Look at the interface. You can. The API. Sorry. Look at how how it looks. You can mark this. You can create, extract some kind of interface to mark it. And there's a lot of strategies. But I hope you have enjoyed this mocking section. I enjoyed this section a lot because I was able to present, um, different perspective, different techniques. And I think with all this, you are armed with all these techniques and everything. You should be able to test anything. Whatever code you write in, go. In short, if you are struggling to test any piece of code, I want you to take a step back and try to refactor that code. Maybe you're making it too complicated. Maybe you are doing something wrong because if it is not testable, then it's not maintainable. Bye for now.
