# Manual Mocking With Interfaces

Hello and welcome back. In this video we are going to write our first test using some kind of mock. This time around we are going to use do a manual mock manually mocking our responses, our status code and everything for now. Later I'm going to show you how to simplify that. Okay. So let's start by copying this. Actually copy and paste it. Or you can create a folder and do it manually if you want. So I'm going to say manual mock right. Then in the manual mock let me create another file called client dot. Go. Go into the main and copy everything from here or court rather. So I cutting everything. Go into the client and paste in there so we can free up main. Now main is way way way smaller. Then here I'm going to create a client underscore test dot go. This is where we are going to put all our tests. Now in main sorry now in client or go we have this interface method that we need to implement. So let me copy it. Go to client underscore test. Go and type mock. HTTP client right I'm going to keep it empty like that for now. Then even though I'm not going to use this I can paste that method that we copied from the interface down here. Now this is our implementation. For now let's return new ni ni ni. This is definitely not going to work. But let's go to client. Go and see what's what's going on. You see that the the count for implementation has now reached four. If I click on it you see we have this mock client already implemented it. So this is a very good sign that we are doing the right thing. In case your ID doesn't show you this, that's no problem. Just make sure you have the same method with the right signature. Okay. We have the method. So what do we do with it? We can use it. So I can go func test fetch. So and then let's say let's pretend to test the success case. Uh what is the success case. So open up the client. Go. What is our success case? Our success case is that we we may we are able to make the call. We receive 200 status code, and we are able to decode the response and return some posts. So if we pass two here, we should receive two posts. That is the success case. So let's attempt to mock that this is not going to be easy, but we'll see. So Mark client then client new post client. Let's pass it. The client okay. Then we pass it the base URL which we can. It can be anything this time around example.com. All right. Now we have the client and the client itself. Notice that we no longer pass HTTP client default client here because we cannot use this in test. This is going to make an external call. That was why we did a refactoring to accept or inject the HTTP client implementation. All right. With this we can say post and then arrow call client dot fetch post. Let's give it two as the limit. Now we can check for a referral is not new. We want to we can't dot theta here because if there is no need, there is nothing we can do. Then we also want to make sure that if posts is needed, we want to say that post shouldn't be near. Then we also count if length of post is not two, then we have a problem after that. Uh, I think that is the most important thing that we want to check. Ideally, this should give us the success case, but this code is not going to work because we are returning nil here. I'm going to click here, run it and we should receive a panic. Definitely. We are receiving a panic because in the client here, this body that we receive body here is nil. And we are calling Dot close on it then that's why it's panicking. So nothing strange. Let's go back to client underscore test and then close everything to give us more room. Let's try to return something here. We can use the IO dot. don't know. So let me import the IO package. Nope. Closer. Right. Then pass it. Bytes dot new. Let's import the bytes package as well. But those new buffer I think new buffer string will do with this one. So it's trying to say that. So it's no problem. What we can. Let me let I need to do it like this. So http. Dot response. This is the HTTP response. Then the body is what we need to set to like this. Now we need to pass what we want to return. If we go to the web page. Let's add a limit and make it two. Okay. So we have just to have the two posts here. I'm going to copy it like this. I'll just come to somewhere down here that will not disturb us. So I'll say const dummy post. And then just put it like this. So here I don't want to say it, just fold it out. And what I'm going to do I'll put it in here okay. But then we need the status code to be 200. So the status code can be 200. And then the header. Let's make sure we make it uh, we can leave it like this. Or you can do make HTTP header. Any of it would work. This give us a sweet path. So I'm going to run the test again. And our test passed. Right I want to run the test with coverage so that we can see what we covered. And you're going to notice that we have covered only the sweet path. Let me open the code. Now you see exactly what I told you. We were able to make the call. Of course, we were able to define then the test. The code was 200. Okay. That means we didn't enter here. So good. Now we're able to decode and we we return here. So success case. Very powerful. Mocking has worked. We have been able to use the mocked version. So you see that the instead of the response coming from the internet we are controlling this. Right. This is a very powerful idea. And I want us to expand on this idea, um, so that we can test all the possible cases, the error cases and all that. We are going to just we are using the same code, but refactor it a little bit because think about it. If instead of let's say expected body for example, can be string expected code, here we if we do it this way. And let's say here I put a c dot expected body and then C dot expected code. And here we are testing for success. So expected body would be the dummy body that we put here. Then expected code would be 200. Nothing has changed. The test is going to work right. Test is still going to work. We still doing the happy part. But then here is a little bit it's now possible to do let's say. Instead of uh not and not okay. Instead of uh okay we can say bad request. And what would happen with bad request? We know for sure that if error is nil, we should have a problem. So let's let me go here quickly. If error equal to we expect error should be shouldn't be near. If post is not knee, we expect the post to be near. So I'm going to run it now. And then it passed. But that's not the most interesting thing. Now when I run it with coverage and I replace the suit and open the code up, you see that we've been able to cover this part already. Do you see how powerful it is? You can do the same thing with this. So how about how how how about the. How about this this part this also easy, right? Uh, let's say let me copy and paste the code again. Do you see how powerful mock is? Uh, failure. Failure means this. Um, this should return an error. But how do you return an error here? Because here we made it a constant. We made it a constant. That means we in order for us to control what we return here, we need to do something different. It's possible we can come here, add another field and say expect error and do some kind of a thing here, but we won't do it now. So. But for the JSON it's possible to do JSON error. Instead of that we can say that uh, we can put something invalid and then oh 200. Okay. And here of course we expect an error. So let's run it. It passed. Now let's run it with coverage again to see what we covered. And if you go back you see we've covered the JSON part as well. So this is the power is It's too much. You have. You almost control what you can return and everything. And it's it's a complete, a complete workflow that gives you the opportunity to do anything to test almost any part of your code. If I go ahead and if I run the entire suit, our test coverage is closer to 100 already. You see, if you go to look at the client go, we have 91% test coverage. Everything is covered except this part. And what I'm going to do, we are going to do another so manual mock again. But version two I'm going to rewrite the code to be a little bit cleaner, a little bit more professional, like what you would see at the at your workplace, what you can show off in an interview instead of this manual thing I'm doing. It's still going to be manual, but the code will look a little bit more professional. Let's do that in the next video. But I'm sure you'd appreciate the power that we have now with the mock. All right, I will see you there.
