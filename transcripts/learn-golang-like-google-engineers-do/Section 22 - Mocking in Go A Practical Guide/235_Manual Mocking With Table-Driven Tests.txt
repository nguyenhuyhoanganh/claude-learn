# Manual Mocking With Table-Driven Tests

I'll come back. Now let's convert the test into table driven test. So the first thing I want to do since we have a go.mod here, let me check inside already. We have the testify package installed, so. But if you don't have it, you can simply run, go get like this and then it will install it for you. We're going to need it. So just do that quickly. And then I want to go to tools then sorry, run on my IDE in case it's hidden outside of the screen. Just go to run and then I want to hide this um, code coverage numbers. If you don't use Golang then you don't have to worry about it. I have also shown you how to run the code from the terminal, if you want to do that as well. Okay, let me copy and paste this. I'm going to name this file and then table. Driven like this. And with this, nothing has changed. The same code from the last video. But instead of having, um, how many, how many functions do we have? Instead of having these four functions, we are going to have just one. And then we we use table driven approach to um, do the testing. So let's do it. I want to start by deleting three of them and also delete the sources that I put here. I think I'm deleting too much. Good. And we can remove everything from here for now okay. So table driven test in go follows a very almost similar pattern from uh everyone you in the test you create a table which is just a struct, and then it's an array of fields that you just have to combine. There's nothing special about it. So let's say test right. And then I put the code. And what is this. This is a struct plain struct. But here's an array. So we can put multiple cases in here for now. All right. The first field is the name. This is our function that we have there that get func. Remember we were calling it. We're configuring it for each of the test cases. So the secret to knowing how to refactor into table driven tests is each everything that you are repeating in each of the different functions that you are testing probably can go into table driven configuration. Then the limit if we want error. If the error contains a specific string, um, if we want the post to be nil or not, if the specific post length that we want, the expected URL and the expected get core count. And that's it. No. So let's add the four cases now. So this is the success case. You see success case for get func. Of course this is the same thing that we did right. We with a status okay. And then we pass the dummy post which is still here. The limit is two. We don't want any error here. We don't want post to be nil because we this is the success case. We want post length to be two and our expected URL should look like this. And the total number of method calls we should have here should be one. Okay, let's add another one. The network core case. You remember we we return nil for the body and return the network error. And the rest is the limit. Of course, everything else sort of remain the same. But what is important here is for this we are expecting an error. We also want the post to be, uh, Neil and we want the error should contain network error as part of it, but the URL remains the same and the call remains the same. Then the third one. Let me close this part. So for non 200 status okay. Of course we need to return a status that is not 200. So we return internal server error with an empty body because we won't get to that JSON decoding before it fails. So we don't care about the body actually. And of course this has to be nil error. The URL and the call is still the same. We want post should be none because we are going to have a non status. Um a non 200 status and an error should contain this. And that's everything that we were checking is what we just reconfigured. After this we put the last one. The last one. You see the most important thing here is this invalid JSON that we have. And for that after that that's it. The rest is we can go in here. This is the slice. The slice of this that we have. I can fold it, actually. We can try to. Let me see if I can fold everything out. Yeah. And then we have some space here and we can type. We just loop. And then you cut and run. We have seen it before. I think in the one of the section that we've learned testing, we're calling that run because this is a sub text. So this is a sub test. If you call theta in here the theta is only going to filter the sub test. But it's going to allow the other test to run. Okay. So what we want to do we create the client. Of course we create the mock client. I want to um if you can create a mock client like this if you want. And I think, let's go ahead and do it so we can pass the get form. The get func is coming from this test cases now. And that that should be it then. The client for the post new post we pass it the mock client and then http example.com. There's no post here. Then let's call the client dot post. Now post like this. You see that it's the same thing that we've been doing now. This is where it gets interesting. We have to now start remembering what we put here. If we want error and the error is we don't get the error, we want to, we want to make sure we produce an error. So here's where is it if. If TT dot want error, we. If we say we want error, that means we are in that state. Now let's assert that error is not NE or we are. We can assert error like this. So assert error like this. We are using the assert library now just to make the code cleaner. If TT dot error contains is not empty, we want to assert that it contains assert contains that error. So we want we can. If you want you can call error dot error. Or you can do like this. If you want you can do it like this as well okay. And that's it. If not here. If not. So if we don't want error then there should be no error. Like if we say we don't want an error and we we, we run into an error here, then we should yield some kind of message. Okay. If we want post to be near, then post should be NE. If TT dot one post to be NE post should be NE. If not. We we have to check for one more thing. Again. If I want to do a little bit of um, if I set not NE. If the post is not NE, we can assert the length. Assert length post and then the land that we want, and that will be it. Uh, last thing we want to check is the core. If TT dot um expect get call. This one is greater than zero. Let's check. Let's check for the length first. Assert. So mock client dot get calls should match expected call. That is one. Then if TT dot expected URL is not empty and Len want to measure the length of the call is also not zero, then assert equal the expected URL and the first one the same thing that we did. Now there's a lot of um, let's run it. First of all, before I give my comment about it. And it passed. And then let's run it with coverage and let's check. And then it's, it's 100% coverage. Coverage. All right. So let me go to run hide coverage data and then come back to the code so that there is room for improvement here. This if statement in in in in test. I am not a big fan of it, but I'm showing you that it's possible to configure your test cases as complex or complicated as you want. But just how go how the designers of go which things are always keep things simple. Some of the things that we are checking here for this, for this small code, we really don't care about this method calls and all this thing that we are checking, but I'm including them because this is this is a lesson. This is showing you it is more of showing you opportunities than just testing the code. If not, the first thing that we did, that first approach that we I implemented the code and were passing, um, the just different responses and checking that that almost did it. We got around 91% or 92%. That is more than enough. But I am showing you this different perspective, different configurations, so that as you are growing in the industry or when you when you see a code like this, you are not intimidated. You understand what they are doing. There is nothing strange about it and you can really follow it and understand it. And that will be all for this, um, manual mocking. There is still more we can do with mocking. I want us to learn, test this package, testify it, provide it some mocking library that would make your life ten x simpler. And I want to make sure I show you that before I end this section for mocking. Like I said, mocking is an important skill to have. Don't take it for granted. So keep learning and I will see you soon. Bye for now.
