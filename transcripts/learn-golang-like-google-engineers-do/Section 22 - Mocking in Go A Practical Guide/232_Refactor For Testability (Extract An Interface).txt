# Refactor For Testability (Extract An Interface)

In the last video we wrote this program that has this hard dependency. And in this video, I want to teach you something that is very important, which I call, um, refactor for testability. Okay. So let's go ahead and do it. So I'm going to come here and um, let me create another folder to refactor for testing bility. And then we just put main that go in there. The first thing I want to do is to copy this code. I think we can copy everything that we have here, here and paste it here. Nothing much has changed, right? We still have the same code. Now, one one technique that I find very useful is when you have an external API. So this is an external library. This HTTP dot get is an external library. Does it matter even though it's in the standard library? It is external to your code. You don't control it. You don't. You don't even know how it's implemented. Anytime you have something like this, one technique that you can use is I call it an interface. So interface interface extraction simply means what if we turn this into some kind of interface so that this HTTP get becomes the default implementation of that interface? Let me show you what I mean by that. I'm going to come up here. Let's close this side of the it is to see things very well. And I'm going to say type HTTP client. And then interface. And I put this method in. So get look at the structure of it. Get. Don't worry about the package. Just focus on the signature here. Get it takes a string. Yes. And then what does it return? Let me hover it. It returns a pointer to a HTTP response and an error. That's exactly the signature that we have here. So. So what can we do with this. Well here is the fun part. Let's say we create another type here called Posts Client for that dummy API that we have stroked. Then we can put the HTTP client interface that we just we just created. Let's embed it here. Is this interface then maybe the base URL. Okay. With this in place let's put a small um function here to help us create this. And then we set the we set it like this. So this is a little bit of a constructor. All right. We have this interface. This interface we know how interfaces work in go. Implementation is implicit already is telling us that there are there are programs that have already implemented it. So look at HTTP client already satisfies it. So this is good. With this in place we can change this. Let's make this a method. Now let me call it C and let's use this one. And I'm going to make this a method here okay. The signature remains the same. We're not going to change anything. But what is going to happen here. We can this is a base. So I can take this one out now and put C dot base URL and limit okay. Now we no longer need this. This hard dependency will be broken. Now depend on interface. Instead of calling Httpget, we call this our HTTP client, the embedded one, this one so it will be C. Dot http client dot get. And that's the only thing that we need to change. Every other thing remain the same. Then in our code here base URL I can just set this one here quickly. Then let me go and copy the what we did in main. From here I'm going to copy everything here. Bring it over here. But let's call it a post client. We created a post client using the constructor. Now we pass it the default HTTP client and the base URL. Here we can call this. Okay I just need to delete most of it. All right. And everything else remained the same. So let's run the code. I'm going to go to two. Run it. If there are no bugs, it should run the same behavior. But way, way, way more testable code that we have written. And with this in place, we can actually start mocking it. So what we have what have we done? Before I end the video, let's go over it again. I know this might look primitive. You are probably in a hurry that just show us the mocking. Why are you showing us this? But this is an important, um, foundation. Don't rush through things. So what did we do? We saw an opportunity here for an API that looks like this. That takes a string and returns a pointer to HTTP response and an error. And what we did with that is we came here to extract an interface. Right. Extracting an interface helps us to embed that HTTP client. And here that where we used to have had dependency, we no longer have that hard dependency. We could come here and call the embedded HTTP client method, which is an interface. If you click on it now, it goes to the interface. Right. And then every other thing remains exactly as it was. But the advantage of this is this. You can you come here if you have if you want to test this, which we are going to do in the next video, we are going to start testing it. We instead of passing this default HTTP client, we can pass our own, let's say mock. We can pass a mock. HTTP client and the mock HTTP client can. For example, it can return an error for us to test here. It can return a different status code. It can return an invalid instead of JSON. It can return HTML or some kind of invalid JSON for us to test. And it will be so simple. That's why I love this approach. All right, I'll see you in the next video where we start testing this very code. See you there by.
