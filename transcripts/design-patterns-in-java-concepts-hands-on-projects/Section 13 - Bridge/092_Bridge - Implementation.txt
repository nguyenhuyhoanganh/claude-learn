# Bridge - Implementation

All right. It's time to see how we can implement a bridge design pattern in Java. So I'll switch over to Eclipse now. And as usual, I have implemented some of the classes that we are going to use so that we can save some time while implementing this design pattern. So these are the same classes that we saw in previous video in the UML diagram. So we have a Fifo collection here that is a first in, first out collection, a generic interface, and this is what our abstraction is. That means our client is going to deal with this particular interface and implementation of this particular abstraction. Then we have linked list, which is also an interface, but this is our implementer. That means linked list provides operations or defines operations that can be used by our abstraction to provide its functionality. Then we have a queue which will be our concrete or refined abstraction. So we're going to implement this queue class using one of the concrete implementation of linked list. And to do that I have created here a singly linked list class which implements our linked list interface and provides implementation for methods defined in that interface. So let's start implementing queue. Remember, this is our refined abstraction and as we saw our abstraction works or uses our implementer in order to provide its functionality. So I'm going to start by saying it implements sorry implements. Fifo collection and we're going to keep the same type parameter. Then we're going to generate stubs for the methods that are defined in Fifo collection. Now we saw that our abstraction uses composition and it uses an object of implementer. So for our linked list implementer we have singly linked list, which is a concrete class. So I'm going to have a private linked list, sorry. So I'm going to have a private linked list called as list here. All right. So this list is the instance. This list is going to hold the object of implementer using which we can provide our functionality. So let's say we want to provide implementation for our offer method. To do that, we're going to simply say last dot, add last and element. All right, so we're going to add the element to end of our link list. And when it comes to get the element from our linked list, we're going to say list. Dot remove first. All right. So this method remove first returns. The first element that is present in our link list and removes it as well. All right. So this is how we can provide the functionality in our abstraction using the implementer. Now, one thing that remains is we need to define a public constructor that can be used in order to. In order to get hold of our link list instance. So I'm going to say we need an object of link list and we're going to simply say this dot list is equal to whatever is given to us in the constructor, and this is how a bridge works. So this particular composition that we have here is what bridge is all about. Using this list, we are providing the functionality that is defined in our abstraction. Now how do we use it? So we are going to jump to the client class and we are going to say, let's create a Fifo collection of, let's say integers, and I'm going to call this simply a collection and we are going to create the queue and we're going to give him new. Singly linked list. All right. So now our collection is ready to be used and we can use it using the method that are defined there so we can add call the offer method and add some integers. I'm going to copy paste this method, add some another digit, then add another number and then we can check whether our collection returns these values. All right. So we wanted a first in, first out collection, so we added ten. First we want to get ten back as the first element. I'm going to call this method three times. Let's call it one more time to make sure that we have handled the null issues. Okay? And if we run this client code using run Java code, you will see that we get our first element that was added, then the second and then the last. Lastly, we get null. All right. So whether to return null or throw an exception is up to the implementation and that decision can be taken either in the collection, the abstraction or an implementer can throw an exception as well. Anyways, the point here was how we implemented our abstraction so that it uses composition and one of the concrete implementer to provide functionality that is defined here. All right, so this is how you can implement bridge design pattern in Java.
