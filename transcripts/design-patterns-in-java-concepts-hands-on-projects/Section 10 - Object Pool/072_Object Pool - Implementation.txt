# Object Pool - Implementation

All right. Let's jump into clips and see how we can implement the pool object pool design pattern. So I have a few classes here that I have already implemented. These are exactly the same as we discussed in the UML diagram. I will quickly go over them. Once again, we have an image interface here and this is going to represent an image that can be rendered in our application either on a canvas or directly on the screen. We are not actually going to do the rendering. Once again, this is a hypothetical application, so let's look at the methods of this interface. So it has methods that allow someone to set and get the location of this image and an operation that actually draws this image on the location that this image object will have. Then we have a bitmap class here, which is an implementation of the image interface. And this class represents a bitmap file that is stored on our disk. Now we'll see that in the constructor of this class we are expecting a string which represents a file name on the disk. So hypothetically, what is going to happen here is that if somebody creates an object of this class, we are going to take this name. We're going to go to the disk, read that file, allocate a new byte array, and store the file content in that array. We are not actually doing that here, but you get the idea that this is what this class is actually doing, hypothetically. All right. And we want to pull objects of this class because every time we create a bitmap object, it's going to trigger a file IO, which, you know, is going to block and it's going to do a memory allocation and then it's going to store all the file contents in that array. So what we're going to do is we're going to pool objects of this class so that we will have some objects in reserve. So somebody if somebody needs an object of this bitmap, we can give it an already created object so that it won't trigger a file io. So that's what we are going to implement in this example. So to begin with, we are going to code or implement our object pool using an interface. So this is the pool interface. So we are saying that any class which implements this interface can be pooled in our object pool. So what is the contract of this pool interface? Well, there is only one method that we're going to add called as reset method. And the contract here is that we want all the implementing classes to reset their state when this method is called. And this is to ensure that there is no leftover state values that will affect the client. So if we pool this object and if it has some leftover state values, for example, if we have a location that is a that has some value, and if an object gets it and tries to draw it, it will draw in the old location value. So reset is a way that our object pool can ensure that the state leftover state is reset. But now it's time to have our implementation bitmap class implement this pool level interface. So instead of having our bitmap implemented, we are going to make our image interface extend from the pool level interface so that all images will become playable in the bitmap class. Now we have to provide an implementation for this reset method, so I'm going to generate a method stub here and let's see what we need to reset. So we have two state values here. We have a location and a name. We know that the name of the name represents a file name and that won't be changed by the client. We only have two methods that allow the client to read a state value and change the state value and that are related to location. So in our reset operation, our reset implementation, I'm going to simply say location is equal to null. All right? So that takes care of resetting our state. And then I'm simply going to print out a message saying that the bitmap is reset so that we get some indication that our reset operation was called. Now let's jump into the object pool. Okay, So this is where we are going to implement our object pool. So I have created an empty class here and this class is going to pool anything which extends from pool level. So we are going to write it in a generic fashion so that it is more reusable. So first order of business, while creating an object pool is to implement or provide a way to cache objects in memory to simplify things. What we are going to do is we are going to use a collection that is provided by Java and the interface is blocking queue. So blocking queue is an interface that is provided in concurrent package of Java. We are going to use the generic variable and we are going to say available pool, right? So this class, this reference available pool is going to represent our in-memory cache. So blocking pool allows some level of thread safety so that we can get away with using only the blocking queue. Now let's write the constructor for our object pool. In the constructor we are going to initialize our in-memory cache and we are going to use the linked blocking queue implementation of the. Uh, blocking interface. Okay, so this implementation will allow us some level of thread safety when we are adding and removing objects from this queue. All right. Now we need to provide methods that allow someone to get objects from the thread pool. Sorry, from the object pool. So we're going to declare a method called as get. And this method is simply going to try to take the object from. The available pool. So available pool. So available pool deck. Okay. And we're going to return that. Now, this method throws an interrupted exception because it is going to block if the queue is empty. All right. So if the object pool is empty and if we're trying to take an object out of it, it's going to block. And if it is blocked and somebody interrupts that blocking, it is going to throw an interrupted exception. All right. So in the catch, we're going to simply say we're going to print a message on the error stream. We're going to say system error, dot print. Ellen. And we're going to say our take. Sorry. It was interrupted. Okay. And if that is the case, we're going to say return null. Okay. Now, you have to take a decision here. Okay. We have simply we are simply waiting indefinitely if our object pool is an empty. Okay. But in real life example, you have to decide what happens if the object cache or in-memory cache of the objects is empty. You may want to create new objects if the pool is empty and then add those objects in the pool and return. Or if if your resource if the object represents an external resource which is limited, for example, like a connection database connection which is limited by, let's say, licensing requirements or sockets which represent limitation by operating system. In those cases, you may want to wait if you have reached the limit that is allowed by the external resource. In this implementation. However, we are simply going to wait until some object becomes available to us. All right. So this is the get method. And using this method, somebody can ask for the object from this pool. Now, the next operation that we need is the release operation. Okay. So when somebody asks for an object from the object pool, we will return it. They will use it for some time and they must return that object to our object pool so that we can add it back in our in-memory cache. And to do that, we have the release method. So in this method, our only the most important job is to call the reset operation so that the object state is now clean and it is reusable so that we don't have to do anything in the get. Once our reset operation is complete, then we are simply going to say available pool. So. And we're going to put the object that is given to us. All right. So this method again throws an interrupted exception. So we are going to simply add the same method catch block here and we're going to say our put was interrupted, right? So we now have our object pool ready. We have a gate operation. We have a release operation, and we have in-memory cache. Now, how objects are going to be created for the first time in this pool? So in this example, we are going to create some objects in our object pool is created. So we are going to say, give us a supplier, okay? And we're going to call this create an account. Okay. So I will explain what I did here. So I declared two parameters for our object pool constructor. The first parameter is the supplier, which is a functional interface, and it has a simple method called as get. So we can call the creator and get objects of bitmap. And the count represents how many objects we are going to create when we are initializing our object pool. Okay, so once that is done, let's loop over. So we're going to write a simple loop while I is less than count I plus plus. For each of these we are going to say available two and we're going to use the offer method because it doesn't throw any operation and we're going to say creator dot get. All right. So in this loop in the for loop, we are looping over an integer and we are creating the creating these many objects and we are using the creator, which was the constructor argument supplied to us to create initial objects in our pool. So we're going to recreate some objects in this implementation. All right. And now our object pool is implemented. Now let's jump over to the client and let's try to use this object. So first of all, I'm going to create a object pool. And this object is going to be formed bitmap and I'm going to say bitmap. All right. Oops, sorry. You need to initialize it. All right. So this will be your final variable or final static variable that will represent our object pool. And then we have to provide two arguments to our object. First of all, we need to supply a, we need to supply a supplier, basically, and then we have to provide the number of objects that we want to recreate. So to supply, to give the to create the supplier, we're going to use a simple lambda expression and I'm going to simply say new bitmap and give it some image name, logo, dot, sorry, logo, dot, BMP. And let's say we want to create five objects here. All right. So this lambda expression is our supplier. So our constructor object will constructor is going to call this lambda expression in order to get some objects and add them into pool while it is initializing and here we are indicating that we want five objects to be created in our object pool. Now let's use our pool to get some bitmap images out of it. So let's say bitmap is going to be sorry. Bitmap pool, bitmap pool, dot, get as we got some bitmap. Now let's change its location. So I'm going to say set location and let's use a point 2D. So this is from the Java effects. So let's set location two and ten. Let's get another object from our object pool. So map, pool, get. And change its location using the set location method. And give it a point to the. Let's give it to minus ten. Zero. All right. And then we can call the draw operation on this. So this draw operation is not going to actually draw any bitmap. It's simply going to print some messages. And finally, we need to release these objects to the object pool. So we're going to say bitmap pool dot release, and we're going to give it be one. I'm going to copy paste it. And B2. All right. So this is how you are going to use your object. So once the object pool is created and there are some objects in it, we can simply call this get method to get hold of the pool object. We can use that object as a regular object. We can call the methods that we want on that object. And once we are done, our the client's job is to release those objects back into the pool so that they can be reused. So let's run this example now. So I'm going to say run as Java application and you will see that we get a message saying that drawing logo dot BNP at the location that we set. The second object is also drawn. And when we release these objects, our reset operation is called All right, So this is how you can implement the object pool. So you create some in-memory cache, use any data structure you can ask for method or a way to create initial objects in the pool. And then you apply these methods that allow anyone to get the objects from pool and release them back into the pool. All right. So this is a simple implementation implementation of object pool design pattern.
