# Proxy - Summary

All right, Now let's look at a summary of proxy design pattern. So whenever we want a standing object or a placeholder object, we want to control access or we want authentication done on a method level basis. For many of these use cases, we can use the proxy design pattern. Now, one important point is that proxy implements the same interface which is implemented by the real object and it delegates the actual functionality. The actual operation is delegated to the real object, and proxies provide their functionality independent of that method or functionality which is provided by real objects. Now, depending upon the type of proxy that we are using, they are given real object at different times, or they can even create the real object on their own when they see fit. Some branches do not even need a real object and instead the talk with remote servers. Whenever a whenever our client code needs or calls an operation on. Now, some important uses of this design pattern are we can have a protection proxy that controls access to the methods that are called on the object. We can have a remote proxy which provides the client code with an object which can be interacted with as a regular object and the proxy handles the network communication with the remote service behind the scenes. We can have a virtual proxy and this is also called as a standing object or placeholder object. And this proxy holds on to loading or creating the real object unless it is absolutely necessary. In Java. We can also have what are called as dynamic proxies. So for these dynamic proxies, we only provide the code that decides what happens when a method is called. The actual proxy class is created at runtime by Java. Now let's look at the UML diagram for a proxy design pattern. So this is an example of static proxy design pattern. Of course we have an interface which is needed or used by the client, and this same interface is used by the class, which is going to create the real object for us. Then we have a proxy class which implements the same interface and it optionally has an has a reference to the real object. Okay, so proxy. Whenever a proxy object is returned to the client, client will not know that he is dealing with the proxy object. And when client calls an operation on the proxy. Proxy will provide its functionality, which can be an authentication check auditing or it could work as a stand in for a real object whenever needed. However, Proxy will delegate the actual functionality to the real object. Now let's look at how we can implement proxy design pattern. So to start off, we have an interface here which represents an image that is used by our application. Then we have a concrete subject. So this is the class which represents the real object. We have a bitmap image class which implements image. And if you look at the constructor of this class, as soon as we create an object of this class, we are going to load the actual image from the disk. That means a disk IO is involved. Whenever we create a bitmap image object, then we have our proxy. So we have a class here called as Image proxy, which implements the image and you will see that it caches some properties on behalf of the real object. Since creating real object involves disk IO, we want to hold on to creating the real object unless it is absolutely necessary, so it will provide some functionality which is possible by proxy object itself. So it will cache some properties on behalf of real object. And in the render we need the actual object now because we need to render the actual bytes. In that case, it will create the real object and delegate to that object the actual functionality. Now we have an image factory here. And this is typically one of the pattern that is used when we are working with proxies so that our client doesn't need to know that it is getting a proxy object. So here we have a simple static method. So we are using a simple factory here and we get the file name, we accept the file name and return the proxy instance to the client. So client will simply use the factory and it will use the returned object as if it was a real object. And when it calls a method which proxy cannot satisfy, which is in this case, render or proxy is going to create the real object behind the scenes. All right. So this is the proxy design pattern. We also have in Java what is called as a dynamic proxy. So in this case, we do not implement a proxy class, but we provide implementation for invocation handler. So invocation handler is an interface that is provided by Java and it has defined a single method called as invoke. Now this method is called for every method invocation that happens on the proxy object, and depending upon the actual method that is being called, we can take different actions in our invocation handler. Now, to create the actual proxy, we have to use another class which is provided by Java, and that class is proxy class. So it has a static method called as new proxy instance, which we can use in order to create the proxy object which is used by the client. So any method that is called on this return object, we get a call to the invoke method here and the client can transparently use this object which is created using dynamic proxy. The same way which it was doing previously.
