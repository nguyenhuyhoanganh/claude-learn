# Proxy - Implementation & Design Considerations

All right. Now let's look at some of the implementation and design considerations when working with proxy design pattern. So first point is that how the proxy gets hold of the real object? It depends entirely on the purpose of the proxy. For example, if we have a virtual proxy or creation on demand type of proxy, our purpose is to hold on to creating the real object unless it is absolutely necessary. So in those proxies, what happens is that we try to handle requests in our proxy itself. And whenever there is a request or method to call, that proxy cannot satisfy, only then we are going to create the real object authentication proxy or protection. Proxy, on the other hand, has a purpose of simply checking whether the caller has proper permissions or if the correct role is present in the authentication context. If that is the case, then it's simply going to call the real operation on the object. So that means protection proxies need a real object from the moment they are created. The proxy itself can maintain some state or cache some state values on behalf of the real object and then provide those values when the real object is created. Of course, this is applicable where we are using the virtual proxy or creation on demand type of proxy. Now always pay attention to the performance cost of using proxies in your application. And you should also pay attention to synchronization issues that are added by the proxy. Okay, so if your real object has handled synchronization, that doesn't mean your proxies automatically threads it. Okay. Especially if your proxy is accessing some resources you need to handle its synchronization separately. Now let's look at some of the design considerations while using proxies. Now, proxies typically do not need to know about the concrete implementation of real objects. That means in your application you can start designing proxies, which may be a proxy for protection authentication. You can define design proxies for remote access, and you don't need to know or you don't need to have the real object ready and implemented to do that. Using Java, we can use its feature to create dynamic proxies, and this feature allows you to create proxies for objects at runtime. They are. This design pattern is really great for implementing security or a virtual proxy and many of its uses that we have seen. So if your application needs or can benefit from these types, you can always try to look where you can use the proxy design pattern. All right. So these are some of the points that you should keep in mind when working with design proxy design pattern.
