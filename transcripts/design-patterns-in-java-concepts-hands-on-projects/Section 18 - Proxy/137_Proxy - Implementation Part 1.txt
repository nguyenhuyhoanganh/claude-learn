# Proxy - Implementation Part 1

All right. It's time to jump into Eclipse and see how we can implement the proxy design pattern. So I'm going to switch over to Eclipse. Now, I've written some interface and a concrete class that we are going to use while implementing this proxy design pattern. So to start off, we have image interface here. This interface defines an image that is used by our client and client application can render an instance of image object anywhere on its canvas. Then we have a bitmap image class. So I have written this class and this class will periodically load a file from the disk so it has a single constructor that expects a file name. And as soon as we create an object of this class, this class is going to load a file from the disk and load the image data. Of course, it's not doing that here and we have some placeholder code, but you get the idea that creating a real object involves using disk IO, which is a slow operation. So we do not want to create this object. As soon as somebody creates its instance, we want to hold on to object creation until we absolutely need it. Apart from this constructor, it has a property called as Location, which represents the location on canvas where this image is rendered. All right, then we have image proxy. So this is the class where we are going to implement our static proxy. To start off, we are going to first implement the interface that is expected. So we are implementing the image interface. Next up, I'm going to provide method stubs for methods that are defined in our image interface. All right, so how should we implement this proxy? So this is going to be a virtual proxy or object of this proxy will be a standing object. That means we want to use this proxy object as long as possible, and we are going to create the real object only when it is absolutely necessary. So first of all, we need a reference to the real object. So I'm going to declare that. So I'm going to declare a field of type bitmap image. And this reference is going to hold hold a reference to or pointer to the real object. Now we need to provide a constructor that matches with the real object or real class. So we have an image proxy constructor here and I'm also going to accept the file name that would be needed by constructor of Bitmap Image. Now, since we are not creating the real object in the constructor itself, we need to hold on to this file name. So I will declare another field here of type string and that will store the file name image file name that is given to us in our constructor. So I'm going to say this dot name is equal to name. All right. So this is how this virtual proxy is going to work. It's going to try to provide functionality for methods where it can. And there is a method called as render. Now, in this method, we need an actual image object. All right. So for set, location and get location, our proxy can provide this functionality on its own. So what happens in set location? So this is the setter method that sets the location of the real image. So what we need to do is we need to first check whether we have created the real object. If we have a real object, then we are simply going to delegate this method call to our real object. So we are going to say image, dot, set location, and we're going to pass it. The parameter value that we got. However, if we have not yet created the object in that case, we still want to hold on to this value so that we can give this value to the real object when we create it. So I'm going to declare another field called of type point 2D, and that's going to store the location value. All right. So we're going to use this field if we don't have real object with us. All right. So we're going to store this value in our field. Same thing goes for get location. This is simply a getter method for the location property of real object. Again, we're going to say if we have a real object, that means if our image field is not null, then we are going to return whatever is given to us by its get location method. However, if we don't have the real object, then we are going to return whatever the value that is cached or stored in our proxy. Okay. So see how we are using the real object if it is available. If not, this proxy is taking care of providing functionality of these methods as long as possible. Now we have the render method. Now this is where actual render is going to happen and proxy cannot satisfy functionality of this method. So now we need to actually create the real object. So again here we're going to check whether image is still null. If that is the case, we need to first create the real object here. So we're going to call image equal to new bitmap image. Now it needs a value of the file name, which we have stored previously. Next step, we need to make sure that our location, which might have been set by client previously, we need to make sure that we are setting this property on the real object. So I'm going to say if location is not equal to null, that means if client has set the location value, then we are going to call image dot, set location and give it the location which was given to us by the client. All right. So once this is done, our real object is now ready. So we are simply. Going to say image, dot render. All right. So this is how we develop our implement our proxy virtual proxy in Java. All right. So we hold on to creating the real object until it is absolutely necessary. So in the render method, we need a real object now. So we check whether the object is present or not. If not, we create it. We set any properties on that object which we have cached and then we're going to call its real method. All right. So to use this proxy, we we are going to need an image factory, okay? Because we want our client to not know about existence of image proxy. So this is going to be a simple factory. That means we are going to have a simple public static method that is going to return. An object which implements image interface. I'm going to say get image. And this factory expects the file name on which for which this bitmap image is created. And we're going to simply say return new and we are going to return an image proxy here with the name value. All right. So this is our image factory ready? Now, in the client, what we are going to do is client is going to simply use our image factory. So it's going to say image factory. Dot get image and it's going to give us some name, for example. A1, dot, BMP, And then client can use this particular object as if it was a real object. So I'm going to say set location and we're going to create a new point 2D here. And let's say we want to render this image at location 1010. We are going to make sure that our property is set. So I'm going to say our image location is and I'm going to call the getter method on this image image dot, get location and we're going to print the value. Then we are going to call the actual render method on this image. Okay, So what we expect is that whenever we are using set location and get location, we expect that our real object should not be created only when we call our render method, our real bitmap image should be created. So I'm going to add a separator here, some message saying that rendering image now. So okay, so we get an indication that we are about to call the render method on our proxy. So I'm going to right click and say run as Java application and you will see that we get the image location here and we get a message saying that rendering image now. And then we get a message saying that loaded from disk. So this message loaded from disk is coming from the Real Objects constructor. Okay. So you will see that when we call the render method, then only the real bitmap image is created. All right, so this is how you can implement a static proxy in Java. Next, we are going to see a feature that is provided by Java, which is Dynamic Proxy.
