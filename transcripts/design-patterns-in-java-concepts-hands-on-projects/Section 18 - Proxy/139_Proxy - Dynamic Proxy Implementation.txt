# Proxy - Dynamic Proxy Implementation

All right. Now we're going to see how we can implement a dynamic proxy in Java. So I'll switch over to Eclipse now and we are going to use an interface and a class from our previous lecture. So I will briefly explain them once again. So we have this interface image and this interface represents an actual image which is stored on our hard disk and the client is interacting with this particular interface. So it expects an object which implements image interface. It has a few methods defined in it. And then we have a class called as Bitmap Image. Now this is an actual class which has implemented this method and is providing the real functionality. Of course this is not doing anything here, but a real class would have loaded the file from disk in its constructor and it would have rendered the content of that image when its render method was called. Okay, so this is the real object. And our goal here in this assignment is that we want a virtual proxy or a stand in proxy for our bitmap image class because we don't want to load contents from disk. Every time somebody creates an instance of our bitmap image, we want to hold on to the disk IO unless it is absolutely necessary. So we want a proxy that will stand in place of the real object and it will only load the real object if somebody calls render method on our proxy. Okay, so this is the mission brief that we have gotten and now we are going to implement that proxy using Java's dynamic proxy API. So when using Java's dynamic proxy, we do not write a proxy class because that's what dynamic Proxy are. This is one of the feature of Dynamic Proxy is that they can create a proxy class at runtime. However, we still need to provide a code which will be executed when somebody runs or somebody calls a method on that dynamically generated proxy. I will switch over to the static proxy example from our previous video. Okay, so in the previous video we wrote this class Image Proxy, which was our static proxy and it implemented the image interface and it provided implementation for all these methods. Okay. So when somebody called a set location method on our proxy, we had this code which was running if somebody called get location, we had this code, so on and so forth. But what happens for a dynamic proxy because Java is creating the proxy class for us. But what about the code that we want to run when a proxy method is called? Well, that code is written in our invocation handler. So we're going to write this class image invocation handler, and this class is going to implement interface called as Invocation Handler. Invocation Handler. Now this interface is part of Java's Reflect API. Okay, so the package for this invocation handler is Java.lang dot reflect. Now this interface has defined a single method in it, so I'm going to generate the stub for that method and the method name is invoke. Okay. And the API is that anytime anybody calls any method on our dynamically generated proxy, okay. So if somebody calls, get location, set, location, render any method, if it is called, our invoke method will be called by the reflect API. Okay. So this is the single point where you have to write your code about what happens when any one of these methods is called. Okay. So instead of having three methods that correspond to the actual methods in image, we now have to write a single method that takes care of all this logic which we have provided in these three methods. Okay, so how can we do that? First order of business should be to identify which method is being called on our object. Okay? On our proxy. So to do that we have a method parameter in this method signature. Okay. So the second parameter of this invoke method is this method. Now this method is from the reflect package again. So we have java.lang.reflect.method.invoke. So what we can do is we can get hold of the image. Sorry, get hold of the method set location for example, that is defined in our interface and inside our invoke. We can compare that method to whatever is given to us. And if that matches, that means our set location method is called. So let's write some code for doing that. So I'm going to do what I'm going to do is I'm going to create a new reference of method type, and I'm going to call this set location method. Now what we're going to do is we are going to use our interface image interface, use its class attribute, and then call get method on this. So using get method, we can get hold of the actual method object which represents our set location method. So for this method we need to provide name of our method and then we need to provide the parameter types that are expected by our set location. So if we look at set location method, you will see that we need an object of point 2D, which is part of Java package. Okay, so let's create an array of class class and we are going to need. A class object of point 2D. So point to this from Java f x. Okay. So make sure that you're using Java f x and we can get hold of the class for that particular point 2D. Okay. So now we have an object of type method set, location method. We want to check whether the method that is given to us is exactly same as our set location method. So we can use our equals equals method set, location, method dot equals and the method parameter that is given to us by reflect API. Okay. So if this condition is true, that means somebody has called set location on our image proxy. So what can we do with with this information? Well, we can write the code which we have written in our set location. But before we do that, what I'm going to do is I'm going to simply get hold of the argument for our set location. So to do that, we have a third parameter in our invoke method called as args and which is an object array. So this array will hold the parameters that are passed in our set location method call. So what I'm going to do is I'm going to again declare a point 2D reference point 2D, and since this method has only single parameter, that particular value of that parameter will be at zeroth location in this array. Okay, so this will be the first element in our args array. So I'm going to say args zero. We need to cast the value that is returned because this is an object array. And what I'm going to do for time being is I'm going to simply say sysout and whatever print the value of point 2D. Okay, so that's what we are doing right now in this invoke method. So if somebody calls set location on the proxy, we are going to check whether the set location was called. If that is the case, we are going to get hold of the parameter value of point 2D and print that out. Let's check this much code. Okay. So we have invocation handler now, but what about the proxy? Okay, we haven't yet created a proxy, so we're going to do that in our image factory. Okay, So this will be a simple factory. So I'm going to write a simple public static method that represents our that returns an object which implements image interface. I'm not going to take any argument here. So now we want to create the proxy. Now, we have known that the Java Dynamic Proxy will create the proxy for us. So how does it do that? We have to use a class called as proxy. Now this class is part of Reflect API. Okay? So make sure that you are using the correct class in the correct package. So it is java.lang dot reflect. There is another proxy class in the networking package of Java, so don't use that. So this class has a static method that we can use and the method is new proxy instance. Okay. So using the static method, we can dynamically generate a proxy class. First argument for this method is the class loader. So I'm going to use whatever class loader has loaded our image factory. So I'm going to say image factory dot class dot, get class loader. Next, we have to provide an array of class objects which represent the interfaces that our new proxy is going to implement. So in our static proxy, we had to manually implement the image interface. In this case, we can simply give it an array of class objects and the content of this array will be the interface interfaces that are implemented by the proxy. So I'm going to say image dot class. Okay, so now we have a proxy. We will get a proxy which implements image interface. You can give multiple interfaces here and the returned proxy would implement or can stand in for any of those interfaces. Okay, so that's it. Sorry for the last argument I'm going to hit. Enter. We have to provide an object of invocation handler. Okay. So I'm going to give it image invocation handler that we have just written. All right? And this is the this is all the information that is needed by the proxy. Now we are going to return the proxy that is created by Java. But of course, we first need to cast it to the interface that we want. Okay. So we are going to cast the return type of new proxy instance to image and return it. So now our proxy is created, we have our image invocation handler and now we are going to use this in our client code. So we have a main method here. So I'm simply going to say image. IMG is equal to use our image factory dot get image and I'm simply going to call the set location method on this object. So I'm going to say new point to D and point to this from the Java effects. And let's say I'm going to give it a coordinate of minus ten and zero. Okay. So we have a proxy here and we have a we have called a method on that proxy set location. Now let's run this code and see what happens. Okay, so we get a system out which prints the point to D value. Okay, but where is the system out coming from? It's coming from our invocation handler. Okay. So we can add it from invocation Angular. And just to make sure that. Okay. Save. Okay, so we are getting it from the invocation handler. And this is how you can intercept the methods that are being called on your proxy and provide a code inside this invoke method. Okay. So what we need to do is we need to compare every time this invoke is called, we need to every time check which method is being called. And based on that method call, which is actually happening, you can take some action here. Okay. So what I'm going to do here is I'm going to cheat a little bit and I have written a code for this actual invocation handler, so I'm going to copy it from my second monitor and I'm going to paste it. Okay? Don't worry. I'm going to explain everything. We are going to do the exact same thing that we have done so far. Okay. We are going to check which method is called and we are going to take some action based on the method that is being called. Okay. So I'm just going to replace this part with the. Copy paste. So I'm going to quickly go over to what happens. Okay. We still have the invoke method here and we are still going to check which method is being called. Okay. So if set location is called, we have a simple method here. Private method called as handle set location. So handle set location is going to cache the value of the location in this particular instance. Or if we have created the real object, then we are going to use that object and set location on that real object. Same thing is done for get location. Okay. So if get location method is called, we are going to check if we have a real object. If that is the case, then we are going to return from it or we're going to return the cached value. Okay. Same thing goes for render. Now one thing you will notice is that I have written a static block here, okay? And I have written a couple of static constants and these are method type references. So instead of calling image class and get method every time in invoke, what I've done is I have called these methods in a static block so that we don't have to do it in every invocation and we have cached the objects which represent our method so that we can use them here. All right. One thing you will also notice is that I have given a constructor here Image invocation handler, which accepts a file name because we need a file name when creating the real object. Okay. And there is one. Okay, real object. So we are creating the real object in the render method. So if our image is null, we are going to create the bitmap image and I need to check whether our location is not null. If location is not null, then we're going to set the location on the real object. All right. So why? For creating real object. We need a file name. So we write our invocation handler with same number of arguments in its constructor. Now, inside our image factory, we need to provide this image name. So I'm going to accept a parameter here in our Get Image Simple factory and pass on that name to our invocation handler in the client I'm going to pass the value of. Eight old BNP. We can call, set location, we can call, get location. And then finally we can call the render method. And we know that when we call render, our real object will be created. Again, I'm going to add a separator. And if we run this class now. You will see that we get our set location, get location calls, and when we are actually invoking render, then the real object is created and it is rendered. Okay, so this is how you can use dynamic proxy. Okay. So I have provided this code as an attachment to this video so you can go through this code. It's pretty simple. Whatever we discussed is exactly the same thing that happens here. We call implement the code of our proxy inside the invoke method.
