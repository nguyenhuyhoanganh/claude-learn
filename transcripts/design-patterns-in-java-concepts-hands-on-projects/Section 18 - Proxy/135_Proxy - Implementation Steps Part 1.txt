# Proxy - Implementation Steps Part 1

All right. So let's look at some of the steps that you would follow when implementing a proxy design pattern. So we start by implementing our proxy. And the requirement here is that our proxy class is going to implement the same interface as implemented by the real object. Now we can either create the actual object later when we actually need it, or we can ask for the real object in constructor of our proxy. It totally depends on the type of application that you're working with and type of functionality that you're looking for. Now, in method implementation of our proxy, we implement our proxy functionality with whatever is provided in our proxy so we can do some authentication if it is a protection proxy. And before delegating to the real object, we carry out our functionality. And if the result is okay and if we want, then we can proceed to the real object. Now how to provide the client with instance of proxy. Okay, so we have a proxy class. We have a real object. Now some code wants to use our proxy. Then we have to decide how that code gets hold of our proxy instance. We can provide a factory which is used by client, and that factory will transparently create a proxy object instead of real object and give it to the client. Or we can compose the client. So when we are creating object of a certain class which needs our proxy, we can give our proxy in the constructor of that class. Now what we are implementing above. So whatever we have seen, the steps that we have seen so far that implements what is called a static proxy, that means we are statically defining, defining a class, a proxy class, and then we are statically defining which what happens in the methods of that proxy. Java also has a provision for creating dynamic proxy. So that means all you need is an interface and you can create a proxy while your application is running.
