# Decorator - Summary

All right, Now let's look at the summary of decorator design pattern. So we use this pattern whenever we want to add a little bit of functionality on top of the existing functionality that is provided by the object. Now, decorator has the exact same interface as the object that it wraps, decorates or contains. So this is an important point and decorator allows you to dynamically construct behavior at runtime using the composition so you can decide which object is wrapped or which object which object is decorated. So that decision can be taken at runtime. Now a decorator can wrap another decorator. That means it supports recursive composition and the original decorator can have the actual object concrete object within itself. Now, client of a decorator object is unaware that it is using a decorated object, and that is possible because decorator has the exact same interface as the object that it decorates. Now let's look at the UML of this design pattern. So we have a component or this is the base class or interface which is implemented by concrete concrete component and objects of this class provide some basic functionality. Then we have a decorator which also extends or implements component, and it also contains an object of concrete component within itself. And whenever the decorator implements this operation, it will delegate the operation to the object which it contains. And then on top of that, it will provide some additional behavior or additional features. All right. So it's quite a simple design pattern. And this is the Yaml for decorator design pattern. Now let's look at the implementation of this design pattern. So to begin with, we have a message interface. So this is our component interface. It has a single method Get-content which is used by client. Then we have our typical concrete component text message which implements this message and it stores a string message within itself which it can return to the client. Now we have a decorator. So here we have a class called as HTML encoded message. Now this is the decorator. So you will see that it implements the same interface as the concrete component. However, in its constructor it needs an object of concrete component or object which implements this message interface. In the implementation of Get Content method, it will first delegate the method call or first of all, it will get the behavior from the original object that it contains. So we call message dot, get content so it provides the original message. And on top of that we provide some additional functionality. Now, to use a decorator in our client, we are going to first need original object, a concrete component object. So here we create a text message and then we can create our decorator instance and provide our original object in its constructor. All right. After that, we can use our decorator object as if it was a regular object. All right, so this is the decorator pattern in Java.
