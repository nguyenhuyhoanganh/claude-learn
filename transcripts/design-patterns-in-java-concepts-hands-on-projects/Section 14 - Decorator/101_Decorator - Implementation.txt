# Decorator - Implementation

All right. Now we're going to see how we can implement Decorator in Java. So again, we have some simple classes that we are that we that I have already implemented. And these are exactly same as the UML that we saw in previous lecture. So we have first a message interface. This is our component interface which provides a single method called as getcontent. Then we have text message. So this is our concrete component which implements message and it simply holds a simple text message within itself and it provides implementation of get content method by returning that single string. So if we jump back to the client, so this is where we are using our message object, you will see that we are creating a new object of text message, providing it with some string. And then we are calling its get content method and printing it. So if we run this code as it is, remember we have not yet introduced decorator. Okay, this is simple plain Java. So we have a message interface which is implemented by text message and it provides some method and we get the output from this object. All right, now it's time to go and implement a decorator. So here I have created an empty class called as HTML encoded message. So this will be our decorator that encodes our string that is stored in text message, and it encodes using the encoded it so that it is HTML encoded. So to begin with, we are going to implement the message interface that is used by our concrete component. However, so I will just quickly generate this stub for the get content method so that we can remove this error. Now we have seen that decorator also needs an object of the actual concrete component. So what we are going to do is we are going to declare a field here of type message and this message will be the concrete component that we need. Of course, we're going to need a constructor that accepts this object because without this object, we cannot work. Our decorator cannot work without presence of this object. So we're going to define our only constructor that accepts an object which implements message, and we're going to simply store it within ourselves. Remember, I have not used text message here because we want to allow our decorators to be recursively. They should be able to be used recursive composition. That means we can pass another decorator here and our code will still work. All right, so now let's implement the get content method. First of all, we can use the we can use the functionality that is provided by our original object. So we're going to call the get content method here. But now it's time to add on to that functionality. So to to do that, I'm going to use a common slang library and use a class called as String Escape Util. So this class is part of our Apache Commons library and we are going to use that class in order to encode our escape HTML. All right. So I'm going to simply say HTML four and I'm going to pass it the string that is returned by our message. All right. So this is how decorator works. It first of all, calls the original object and get its behavior. In this case, we are getting the string that is the message or content of our message object. And then we are going to add on to that message. We're going to then escape that string so that it is HTML encoded. So now let's jump to our client. So how we are going to use this decorator, we are going to create another message object. So I'm going to say decorator. And this time we are going to create HTML encoded message and we are going to pass our existing object, which is M to this decorator. Now we are going to use this decorator as if it was a regular object because it implements the exact same interface. So I'm going to say decorator dot get content. All right. Now let's run this code as Java application and you will see that we have the original message that is printed from line number seven. However, from line number ten, we are printing the message. We are getting an HTML encoded string back. So you will see that these less than greater than signs are replaced by a character that is compatible with HTML. All right. So this is how we can use decorators. I have another decorator, which is which I have already implemented called as Base64 encoded message. And again, it works on the similar lines. We are going to implement the message and we are going to get the content from the object which is contained within it. And then we are going to do base64 encoding. So to use this new decorator, what we can do here is I'm going to simply say we are going to take our existing decorator and then we are going to initialize our base64 encoded message and we are going to pass it our decorator, which is HTML encoded message. Okay, so that is what recursive composition is. So we are going to give an already decorated object to this decorator and then we can call this or use this new decorator as if it was a regular object. So I'm going to say get content. And if you run this code now, run as Java application, you will see that we get a Base64 encoded message which was previously HTML encoded, which was originally a plain string. Okay, so this is how you can implement and use decorator. This is quite powerful design pattern and once you start using it you will see that it has many things to offer. Okay, so that's all for implementation of decorator design pattern.
