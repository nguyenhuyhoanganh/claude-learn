# Visitor - Example UML

All right. Now it's time to look at the example that we're going to implement in the next video. So to begin with, you will see that we have an employee interface here and multiple classes which are extending from the abstract employee which implements the employee interface. So these classes, programmer, project lead manager and vice president represent different roles in a typical organizational department. So we will have programmers who are reporting to a project lead project leads, reporting to manager and managers, reporting to vice president. So we're going to build a tree structure using these classes so we'll have a single vice president representing a department, and under him we will have all these different roles. So once we have this object structure, let's say we want to add a new functionality to these classes. So without modifying these classes, we can add a functionality using the visitor pattern. So we have an interface here called as Visitor, and we are going to see two implementations of this visitor interface. The first is a print visitor, which is going to navigate the organizational structure and print out some informational messages for each rule. Then we are going to see how we can write an appraisal visitor, which is going to visit all the classes or all the objects in our organizational structure and carry out an appraisal. Implementation wise, you will see that we have an accept method defined in our employee interface, and this method expects an object which implements the visitor interface. So all these classes have provided implementation for accept methods. So we have a programmer, project, lead manager and vice president providing implementation for Accept and they are going to get a visitor object in the Accept method visitor itself. This visitor interface has defined four methods where each method accepts an object of these concrete classes. So we have visit method which expects a vice president, a manager, a project lead and a programmer. All right, so this is how we have our visitor pattern implemented here. We have accept method in the objects and in the visitor method we have a visit method which is specific to these concrete classes whose objects our visitor is going to visit. So once our visitor is accepted by these objects, they are going to call the visit method, which is applicable to that particular class. And inside that visit method we are going to provide the functionality as per the implementation that we are trying out. So print visitor will have a functionality that will print information about the object that it is visiting. Appraisal visitor will provide implementation for carrying out an appraisal of the object which it has just visited. All right, so this is the example. It's going to be a bit lengthy to implement. So we are going to follow our usual pattern. We are going to see an implement one of the concrete visitor and we are going to see how the second visitor is implemented. All right. So see you in the next one.
