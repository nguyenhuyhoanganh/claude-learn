# Visitor - Introduction

All right, now let's discuss visitor design pattern. This is one of the most versatile design patterns that you will see. You can use this design pattern in very different types of situations, and it has great potential to solve some critical problems. So what is this design pattern? So this design pattern allows us to define new operations that can be performed on an object without changing the class definition of that object. Right. It almost sounds magical when you first time hear it, but this is an pattern delivers on this promise in a very clever way. So let's try to understand this design pattern. And I'm warning you ahead that unless and until we see the UML of this design pattern, things are going to sound a little bit confusing. So please bear with me. The UML is coming very soon. So you can think of this design pattern in this way. So let's say we have an object, a different object, and we're going to call that object visitor. Then this visitor is going to visit all the objects in our application or in our module, and those objects are arranged in some kind of object structure. So our visitor is going to navigate that object structure or it's going to traverse that object structure and it's going to visit each node in that structure. So each time our visitor visits a particular node, then an object which is sitting at that node is going to call a very specific method on the visitor, and it's going to pass itself as an argument. Okay, so our visitor object has methods defined which are specific to a particular class. So all objects of that class know about that specific method, and those objects are going to call their respective method and pass themselves as an argument. And once they do that, our visitor is going to get a chance to provide some functionality on that object. So it's going to sound a little bit confusing, so please bear with me for a few seconds. So each time we need a new operation or a new functionality on our existing object, we are going to create a subclass of our visitor and provide the implementation of that new operation in that class and visit our object structure using that newly created class and its object. Now object themselves. They only implement a very simple accept method. So in this method we are going to pass our visitor as an argument. And once we call that method, all objects know about a method that is present in visitor, which is created specifically for that particular object, and it's going to invoke that method and pass itself as an argument. All right. So this is how this design pattern works. Now let's look at the UML. So that will make some things very clear. So first of all, we have a visitor interface here, okay? And this interface defines methods that are specific to classes that are present in our application or in an area of our application where we are using this design environment. So we'll see. We have method visit Concrete element A and it is expecting an object of concrete element, A so concrete element A is a concrete class in our application. All right. We have another class here called as Concrete Element B, and we have a specific method defined in our visitor visit, concrete element B, which is going to be specific for this particular class. So if we had a third class, we are going to have a third method which is specific to that class. Then we can have multiple implementations of this visitor. All right. So in each implementation is going to implement some different functionality. Okay. So each visitor is providing a particular functionality which can be invoked on these objects. All right. Next up, you will see we have a simple element interface, and this interface defines the accept method. And this method will be given an object of visitor. So this interface is then implemented by all the objects or all the classes which are participating in our design pattern. So a concrete element will implement that, accept method. And inside this method, concrete element B is going to call this specific method in our visitor. So as we already know, for concrete element class, we have a method visit concrete element B and it is expecting an object of this class. So you can clearly guess what is going to be the implementation of accept here because we are going to get a reference of visitor. So this method will have code saying visitor dot visit concrete element B and it's going to pass this here in the similar way we have we will have implementation of accept method in concrete element, concrete element A and in this class we are going to call visit concrete element A. All right, so this is how this design pattern is arranged. Each class, each concrete class, which will be part of the object structure, will have a specific method defined in our visitor interface so that this class knows what method to call on the visitor. Once that is out of the way, we can create multiple implementations of our visitor and each implementation defines a specific operation. We can have a visitor that prints out our object structure. Another visitor can do maybe some calculation on the object so we can have multiple functionalities implemented now and each class will implement our visitor interface. Lastly, we will see that we have something here called as object structure. So this is nothing but an object that allows our visitor or that enumerates our object structure. So objects of these classes are going to be arranged in some type of object structure, like a tree and object structure is going to enumerate that object structure and pass the visitor to each of those objects. Now, one thing I want to clear here that it's not mandatory, that our objects must be in some type of a structure. Okay? The design pattern defines it in a way that we start to think that we need to have some kind of a binary tree or something like that. It's not like that. It is actually an additional feature of this design pattern that if you have an object structure, then this design pattern works much better. If you don't have an object structure, if you have one single object, then you can call the accept method on that single object and pass your visitor. So object structure is not something which is mandatory for this design pattern to work. Object structure is an is a feature that is provided or that is supported by this visitor design pattern. All right. So next we are going to see how we can implement or what steps we need to take to implement this design pattern.
