# Visitor - Implementation

All right, Now it's time to jump into Eclipse and see how we can implement visitor design pattern. So here we have some classes. These are exactly the same as we discussed in the previous video while discussing UML of this particular example. So let's go over these classes one by one so that we will have or will get some idea about what we are doing here. So we are trying to represent a department in an organization in this example. So a typical organization will have different roles which belong to a particular department. Now each role or employee of a particular role is going to report to somebody else. And that way we have our organizational structure. So we'll have somebody who is at the top, people reporting to that person and other people who are reporting to those, so on and so forth. So let's look at the classes themselves. First of all, we have an interface here called as employee. And this interface is going to represent all the employees that are part of this particular department. This interface has provided some methods that are common to all these employees which work in that department. Next up, we have an abstract employee class, so this is simply an abstract class which implements that employee interface and provides implementation for common behavior. For example, it provides a way to store the employee name. And we know that this is going to be a common logic across all implementations of employee. It provides the employee ID and it has a getter method to get that employee ID. All right, so this class is nothing but a common base class where we have gathered together the common functionality which is needed by all implementations of employees. Then we have multiple classes that represent different rules that are present in our department. So for example, here we have a class called as programmer, and this represents a particular rule in our department. So objects of this class will be employees which have programmer rule. Now programmers are going to report to project leads. So Project Lead has a collection called as direct reports inside of it, and it's going to store references to those programmer objects. A project leads are going to report to managers and managers are ultimately going to report to a single vice president which represents head of that particular department. All right. So these are the classes which are which we are going to use in order to build an organizational structure. So now let's look at the client class. So here we have a class with a main method and you will see that I have written a simple method called as build organization. This is a private static method. Here we are going to create few programmer objects. Okay, so we are going to create some programmer objects, populate some test data. Then we're going to create a couple of project leads and assign these programmers to those project leads, then create a couple of managers assigning each project lead to a manager. And finally we create a vice president where these managers will report and ultimately we're going to return that vice president object. So in this way, we have our data structure, our object structure ready. Okay, so we have a VP. And if we use methods that are defined in that vice president, we can navigate all the employees that are working under vice president directly or indirectly. All right. So this is the object structure that we're going to use. Now. We have a hypothetical requirement that we want to print messages for each of these objects that are part of our object structure. For example, when we visit a programmer, we need to print something like, for example, Andy is a programmer and he has a Java as its as its skill. When we visit a project lead, we need to print out that Tina has two programmers that are reporting to her. Okay, so these kind of messages that we want to generate, maybe because we want to create a nice looking report or we want to generate a page on our local Internet site. All right. So how can we achieve this functionality? Well, in absence of visitor design pattern, what we have to do is we have to go to all of these classes and add a method that will recreate that message based on what rule that method is written in. So programmer will have that method project lead are going to have again the same method, but it will create a different message. All right, so this is how we typically implement this type of requirement. We modify all these classes. Now, since we are using using visitor pattern, we are going to make use of a mechanism or another way that is offered by this design pattern. Now, if you remember from our discussion so far, you will see that we need an accept method in all of these classes where we can pass our visitor object. So let's take care of that problem first. So first of all, we are going to go to our employee interface since this is the common base or common interface for all the roles in the organization, we're going to add the accept method here. So I'm going to create a new method which returns nothing called as accept, and it is going to expect an object of visitor type. All right. So this is the visitor that will be passed to employees. Now, if you look at the visitor interface, it is currently empty. We simply have a placeholder here. Now, this accept method needs to be implemented by all of these classes. All right. So before we do that, let's go back to our visitor. And if you remember, we saw that visitor interface defines methods that are specific to these implementation. So we are going to need a method in our visitor, which is specific to programmer class, specific to project lead, so on and so forth. So we're going to do that next. So I'm going to define here a visit method. And this visit method is going to expect an object of programmer to be passed. All right. Now in the similar lines, we are going to have another visit method which expects a project lead and another one for our manager manager. And lastly, we need a visit method for vice president. All right, so once. Wrestling. Let's call that VP. All right, so now we have these methods, and as per our visitor design pattern, these classes are going to implement the accept method and call a method on our visitor, which is specific to those classes. So let's implement our accept method here. So I'm going to generate a method stub for this accept method and inside this accept method we are simply going to write visitor dot visit and we're going to pass this as an argument. So since we are writing this or implementing this method, accept method in programmer, this reference is going to refer to programmer and this is the reason we are not implementing our accept method inside our abstract employee. Because if we implement accept here and pass this, then it's going to mean that we are going to have a reference of abstract employee. Okay, Because this reference in this class means abstract employee. However, inside programmer this means programmer and this is how our method overloading is going to work. Now one thing that I wanted to point out that it is not necessary to use method overloading. You can create a method called as visit programmer, visit project lead visit manager, so on and so forth. Okay, so that way you can avoid some confusion if you are not comfortable using method overloading. All right, so we have implemented now accept method here in the programmer. Now this same code is going to be needed in our project lead. Okay? Because we are again going to override our accept method and we are going to pass this here. But since we are in project Lead class, this visit will be referring to the Visit method implementation or visit method in our Visitor, which expects a project lead object. All right, so I'm going to paste that method in the manager class now. Okay. So we are in the manager class and we are calling the visit method from this class. Same thing in the vice president class. All right. So we now have accept method implemented in all of our concrete classes. All right. So now our visitor pattern is basically done. Now we have all we have to do is provide an implementation of this visitor interface and provide or implement the required functionality. So as we discussed, we have a requirement that we want to print informational messages for all the objects in the object structure. All right, So this is the object structure here, and we want to print some messages for each object. So we are going to create a new class called as Print Visitor. And here we are going to implement our logic. So this class is going to implement visitor interface and I'm going to generate the method stubs for all those four visit methods which are overloaded. And inside this implementation we are going to write code which is specific to programmer, specific to project lead manager and vice president. All right, So let's begin our implementation. So it's it's going to it's going to be pretty simple implementation. We are going to create a string here and let's generate a message. So we are going to say programmer dot get name and we're going to say that he is a let's use the skill programmer dot get skill and add. Sorry. All right. So this message is going to print something like Andy is a Java programmer, so on and so forth. All right. So this is the custom message that we wanted to generate. And lastly, we're going to simply print out this message from our visitor. All right. In real life application, you may need a file handle or you may be collecting these messages in one of the properties of this print visitor. And at the end of visiting all the nodes, you will be getting that map outside and creating your final report. All right. So I'm now going to provide an implementation for the remaining visit methods. They are going to follow the same logic. We are simply going to print some message. So these A so let's say he's a project lead of with let's sorry, let's report how many employees or how many programmers are reporting to this project. So I'm going to say that, for example, Tina is a project lead with, for example, two programmers reporting. Okay. And then we're going to print out the message. So remember, we are able to hard code parts of our string where we are saying directly that this is a project lead because this particular visit method will be called by the project lead. So Object of Project Lead is going to call this method. So that's why we are sure, that's why we are able to hard code this message where we have this project lead in the message. So I'm going to quickly implement the same method for manager and we're going to say that manager. He's a manager with. Let's get hold of its direct reports. Get direct reports. Dot size. Leads route. And then we're going to print out the created message. Same thing for vice president. So it's a string message. We're going to say v dot name is a vice president with. Dot get direct reports dot size. And since we know. Okay. And finally, print out the message. All right. So this is how you will be implementing your visitor. So in each visit method, we are going to provide the same functionality, which is here to create some custom message for that particular object and then printing out that message. So since our visitor is now complete, let's go back to our client and see how we can use that visitor. So first order of business is that we are going to need access to our object structure. So I'm going to call our static method that we have written called as build organization, and we are going to get hold of our organizational structure or object structure. Once we have that structure, we now need to iterate over that object structure, passing our visitor to these objects. So let's take care of creating the visitor first. So I'm going to create a new visitor object which will be print visitor. All right. So we now have our object structure and a visitor here. So how can we implement this logic where we are iterating? So let's create a simple method here. So I'm going to collapse this one and create another static method, private static void, and we're going to call this visit org structure. And this method is going to expect an employee as MP and a visitor which will be passed to that employee. So it's going to be visitor. All right. Once that is done, we are simply going to say MP That is the employee that is given to us and we're going to call the accept method and pass our visitor here. All right, So we now have visited the node which was passed to us. Now the next job is that we need to access the report or other employees that are reporting to this MP and pass our visitor to those objects. So to do that we are going to simply call MP, dot get directory ports. So we now have a collection which contains all the employees that are reporting to this particular employee and we're going to use our simple for loop and we're going to say for each employee again, do visit org structure and we're going to pass E and the Visitor. All right. So we are using recursion here. So for each directory ports, we are again going to call the same method passing the one of the directory ports to this method as the first argument. All right. So this is the method that is going to iterate over object structure and passing visitor to each of these objects and calling accept on those objects. So now let's call the static method from our main method. So we're going to call visit org structure and we're going to pass it. The VP, which is the employee that is written by our build organization method, and then we're going to pass our visitor. All right? And now let's save this class and run as a Java application. All right. So once we run this application, you will see that we get an output saying that Richard is a vice president with two managers reporting Chinese manager with one leads reporting, so on and so forth. So we get this functionality where we printed custom messages for each object without modifying these classes. Okay. So of course we had to add this accept method, but once we have that method, we can now create any other visitor and provide a different functionality in that visitor. All right, so this is how we can implement our visitor pattern. Now, as promised, we also have another visitor here called as Appraisal Visitor. So I'm going to quickly uncomment this code so it's a little bit involved code. However, it's still going to demonstrate the exact same thing that we have already seen in the print visitor. And what this appraisal visitor is doing is that it is visiting each node and it is calling their individual rating and it is checking the rating of the people who are reporting to us. So, for example, when we are carrying out a appraisal of Project Lead, what we are doing here is that we are giving 75% weightage to his personal rating and 25% weightage of the final rating is given to programmers who are reporting into this project. Okay, So this is something that I have implemented in this class. Of course it can be any logic that you want it to use. All right. So this way we can implement another visitor which carries out this appraisal. Now, one thing that you. With notice here is that in this appraisal visitor, we are collecting final ratings inside of a hash map. And this is what we call as an accumulating visitor. So this visitor is going to visit each node in our object structure and carrying out after carrying out the appraisal, it is going to cache that final value, the final rating inside of our ratings map. And this is another use of our visitors so they can accumulate state. And since we are saving that hash map in terms of the employee ID and his final rating, we effectively have a state for that particular object. Okay. Since we are using hash map, we have an identity and a value corresponding to that identity. So this is how we can use or create an accumulating visitor. All right. So I have attached code for this completed example. You can download that and look at how we can implement this type of visitor. All right. So that's all for implementation of Visitor Pattern.
