# Visitor - Comparison with Strategy

All right. Now we're going to look at comparison of visitor design pattern with strategy. Now, visitor is actually quite different design pattern than rest of the design patterns so far that we have studied. So it is not fair to compare visitor with these patterns. However, we are still going to see one difference between visitor and strategy, and that is because I have seen these two. There is some confusion between these two design patterns to someone who is new to this whole concept. So the difference between visitor and strategy is that all visitor subclasses provide possibly different functionalities from each other. So if we have a visitor and we have two implementation implementations of our visitor interface, then there is a guarantee that these two implementations do different things. They provide different functionalities in the strategy design pattern. We also have subclasses. However, each subclass represents a different algorithm them that solves the exact same problem. And this is the difference between visitor and strategy. Okay, because it is often confused because in strategy also we create subclasses and we implement different algorithms in each subclass. The difference here is all those algorithms in strategy are all strategy implementations solve the exact same problem. However, using different steps in the visitor, the functionality provided is completely different. That means two visitor implementations will do completely different things. One will be printing out the object structure, another one will be maybe carrying out the averages or applying some discounts. Okay, so this is the difference between visitor and strategy design pattern.
