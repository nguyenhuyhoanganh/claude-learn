# Strategy - Implementation

All right, Now let's see how we can implement the strategy design pattern in Java. So I'll switch over to Eclipse now and there we are going to start implementing this design pattern. So to begin with, we have few classes with us. These are exactly the same as we discussed in the previous video where we discussed the UML diagram of this particular example. So I will go over these classes quickly. So first of all, we have a class here called as Order and Object of this class represent orders that are placed through our application. We have a couple of fields here. We have an order ID, the date on which the order was placed. Then we have a map which contains the items that are part of this order. And finally the total cost of this entire order object. So in the client. So I'll switch over to the client class. This is the simple class. We have a static collection of orders, so we have a link list here. And inside the main method you will see I'm calling the Create Orders method. So this is a static method, private static method. And this method is going to create some test data for us. So it's going to create few order objects, add some test items in it, and we are going to end up with the orders collection with three different orders inside it. Now the requirement for client is that it wants to print those orders which are present in this collection. And to do that it's going to use another class called as print service. It has defined a method called as print orders, where it is going to give the collection of orders. Now let's switch over to the print service class. It's a simple class. We have no code here. We're going to implement this class in this example. So in the print service class, we have a print orders method that will get hold or that will get access to the collection of orders given to us by the client. Now, this class is going to be the context class in our strategy design pattern. That means we are going to configure an object of print service with a concrete implementation of strategy. All right, so what is the strategy here? So I'll switch over to the order printer file and we have an empty interface here called as Order printer. And this interface represents our strategy interface. So we are going to define method here that will be implemented by all the classes of this which implement this interface. And this method is simply going to be called as print method and we are going to accept a collection, a collection of orders and implementations are going to print this collection of orders using different algorithms. Okay. So one thing that I wanted to make clear is that algorithm doesn't mean only searching and sorting algorithms, any logic which has certain steps, which takes an input and produces an output is basically an algorithm. So in this example, the way we print this collection is going to be defined by a specific algorithm. So to begin with, we are going to first look at this class called as summary printer. So in this class we are going to create a concrete implementation of this strategy interface. So to begin with, we are going to first implement the order printer interface, which is our strategy interface. I'm going to quickly generate the method step for the print method. All right. So inside this method now we are going to implement an algorithm that prints this collection of orders. So it's going to be a pretty simple implementation. We are going to start by printing some header that will indicate that this is actually a summary report. All right. Once that is done, we are going to iterate over these orders. So I'm going to get pulled off the iterator which iterates over order collection. I'm going to call this iterator and this is going to be given to us by the orders collection. All right. Now we want to use this iterator to go through all the orders and print only the summary of the orders. So what we're going to do is we are not going to print the item details for a particular order. We are simply going to print the order number, the date on which the order was placed. We are going to print the number of items in that order and the total. We also want to have a nice serial number on the left side of our printer. So we are going to use a for loop here. So I'm going to use a for loop. Initialize it with one. Our condition for for loop is going to be iterated dot hasnext and then we're going to increment our index value. Once we have that, let's print out the details. So we're going to start by printing out the index number. Then let's add a full stop after that and then print the order number. So first of all, we need to get hold of that order. So I'm going to say order, order is equal to iterator dot next. All right. So now we have an access to the order object. So once we do that, let's do order dot get ID. So we now have printed the order ID, then I'm going to add a simple tab here and then print the date on which order was placed. I'm not going to use any date formatter. Let's keep this simple. After that, I'm going to print the number of items in this order. So I'm going to get hold of the items collection using get items, get items method on our order and I'm going to call the size method on it. After that, I'm going to print the total cost of this order. All right. And at the end, let's print out some separator that indicates the orders are finished. So we have printed all the orders. Okay. So this is the separate. So we are printing the order summary on this particular line. Let's bring the grand total of the order here. So I'm going to take a create a local variable here outside of my for loop, initialize it with zero. And while iterating over this collection, I'm going to add that individual orders cost into the total. All right. So when we are done with all the orders, we can print the total. Value of these orders, I'm going to say total is equal to total. Okay, Let's add a few tabs here so that the output appears somewhat formatted. All right. So this is how we can implement an algorithm in our concrete strategy. Now, the next part of this job or the next part of this implementation is to make sure that our context service or the context is able to be configured with one of these implementation. So we are going to declare a field here which will be of type order printer. And this reference is going to hold a reference to the actual concrete strategy object. Now we want our client to have a freedom to choose the strategy that it wants, so we are going to accept the order printer object in our constructor. So when creating our printers, print service client is going to decide which type of printout it wants. If it wants a summary printout, then it will create an object of our summary printer and give that in the constructor of print service. So we are going to save this reference in our field so that when client calls print orders, we are simply going to delegate this operation to our printer. So we're going to call the print method and pass the orders. All right. So our context class is now using a strategy design pattern. Now the last thing to do is coming to our client. We now need to provide a concrete strategy, object to our print service. So I'm going to simply say new summary printer. All right. So this is the concrete strategy that our client is creating and it is configuring our print service with that strategy. So now let's save everything and let's run this application. All right? So you will see that in the output. We have the summary report, we have details, summary details of each order. And finally, we have the total. All right. So in the similar way, we can create another implementation of our strategy that is going to use a different algorithm to save us some time. I have already created the detail printer class. All right. So let's take a look at this class. So this class again implements order printer. It provides an implementation for print method, but you will see that we are going we are using a different algorithm here in order to generate our output. So here we are going to print all the items that are present in the order. I'm not going to go into details. It's pretty simple code. We are iterating over the collection and also iterating over the items inside that order. So let's use the detail printer. Now, if we run this class as it is, you will see that we are getting an output for summary report. If our client wanted a detailed printout, it will give us our configure our print service with detail printer object. And now if we run this class, you will see that we get a different output. All right, so in this output, we are also getting the details of items in that order. So this is how we can implement different algorithms in different implementations of our strategy. We are going to configure our context with the appropriate strategy, and our context is simply going to use that algorithm to carry out its tasks. All right. So this is how we can implement strategy design pattern in Java.
