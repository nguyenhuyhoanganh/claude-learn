# Strategy - Example

All right, Now it's time to look at some real life examples of application of strategy, design pattern and the Java.util dot comparator interface is a great example of strategy matter. In fact, this is the perfect example of how a strategy pattern can be used or how it can be useful while developing applications. So we can implement this comparator interface and provide multiple implementations where in each implementation we will use a different algorithm to perform comparison between two objects of same class, and then we can provide these implementation or plug these implementations in various methods like the sort method that is provided to us by various collections. So here is an example of comparator design pattern or comparator interface in action. So here you will see that we have a user class with two simple properties called as name and age. Now we can provide an implementation of comparator where we will first compare using the name property and we can provide another implementation of comparator where we will be sorting or comparing users based on the age. So here are those simple implementations of these comparator. So first of all, we have a sort by age class which implements the comparator, but in the compare method it is going to use the age property of users for sort by name implementation. We are comparing using the name property of these users. So here you will see a clear example of strategy design pattern. The comparator interface will basically the strategy interface in this case sort by age and sort by name will be concrete strategies that can be implemented now when we are using these implementation. So here is an example code. So we will create, for example, an array list, we will add some users and then we can call the sort method and plug the strategy implementation that we want. So here we are going to provide with concrete implementations of comparator. All right, so this is an example of strategy design pattern in real life. Now another example that can be thought of of this strategy design pattern is basically the implicit naming strategy and physical naming strategy. Interfaces are contracts that can be found in hibernate. So if you are not familiar with these classes and what they do, these classes are used for converting the logical names or domain specific names of entities and fields on those in those entities to the actual database table name and column names. All right. So implicit, implicit naming strategy. There are multiple implementations of this naming strategy provided with Hibernate. And we can use properties file to configure a specific implementation of this naming strategy to be used. And we can also create our own class and provide our own algorithm to map the entity names to the actual database names. All right. So the implicit naming strategy and physical naming strategy are in the hibernate are examples of strategy design pattern.
