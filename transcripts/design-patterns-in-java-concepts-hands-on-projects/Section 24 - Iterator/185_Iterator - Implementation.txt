# Iterator - Implementation

All right, Now let's see how we can implement iterator design pattern in Eclipse. So let's switch over to Eclipse now. And this is quite a simple pattern to implement. There is nothing to it. So let's discuss what we are going to try here. So we have an enum here called as Theme Color. And what we are going to do is we are going to create an enum that will allow anyone to iterate over the enum constants that are present in this theme color enum. All right. So that's our requirement. So let's start by creating an interface for our iterator. So I'm going to create an iterator as a generic interface so that multiple classes can implement this interface and provide their own iterator implementations. So to begin with, we are going to provide a method called as Hasnext, and this method is going to tell if there is another element available in the sequence in the underlying collection or sequence of elements. And finally, we are going to need a method to get the actual element, and that method is called as next method. All right. So this is the interface that is required for using the iterator design pattern. Now let's implement this interface. So you will typically find that we create the iterator implementation class in the aggregate object for us. Theme color is the aggregate object here. So let's start by creating a class. So I'm going to create a private static class here because we do not want to expose or allow anyone else from outside this class to use it. So I'm going to name this class theme, color iterator. It's going to implement our iterator interface and we are going to return theme colors inside the next method. So we saw that we need a state in order to maintain the or maintain the position or remember the position that this iterator instance is using. So we're going to declare a private INT position here. Okay. So we will know at what index we are currently in. I'm going to generate stub for the methods that are defined in the iterator interface. So we have hasnext and next method. All right. So how should we implement this? Well, whenever an instance of theme color iterator is created, we are going to have our position at default value of zero. Now what is the collection that we are going to iterate over? So these are the constants which represent the collection. So how can we get hold of all these enum collections? Well, we can use the values method that is available in every enum. So in order to implement our Hasnext method, what we need to do is we need to check whether our position that is the current location that our interface is located at, if it is less than theme color. Values and Values method returns an array of theme color enum constants, and we're going to check if it is less than length. Okay, so as long as our position is less than the length of values length of value array, we are going to say that there is a next element. So how should we provide access to the next element? We can simply use the same method. We can say theme color, dot values and return whatever is available at position. Okay, So we want to also increment when we return the element, the next element in the sequence. So I'm going to add a position plus plus. So what we're going to do here is we're going to take the array of the values or enum constants, return whatever is at the position. So our position will begin at zero. So for the first call of next, we're going to return red and then we are going to increment our position so that it will become one. All right. So this is all there is to it. Now, we need to only provide a method inside of our aggregate class to. Get hold of this. Operator So I'm going to declare a method here as public static and we are going to return an iterator. Okay? So remember, we are our client is only going to use the iterator interface because our actual class here is private. So we are going to return an iterator that allows our client to iterate over theme color collection. And I'm going to name this method, get iterator. All right. Inside this method we are simply going to say return new theme, color iterator, and that's it. Okay, so anybody can call get return method on our theme color. They will get hold of an instance of theme, color, iterator. And using these two methods they can iterate over the enum constants which are defined in this enum. So let's use this iterator in our client. So to begin with, we are going to declare a reference of type iterator which allows us to iterate theme color constants. I'm going to name it and let's call the method on the theme color enum. Sorry. Let's call the method theme color dot, get iterator. So this was a static method so we can call this on the enums class name. All right, so once we have an enum, sorry, Once we have an iterator, we can use a simple while loop saying that while Iterator has next, we are going to print out the elements that are returned by our iterators next method. All right. And let's run this code now so we can say run as Java application and you will see that we get all the constants that are defined in our theme color. So if I add another color here, let's say green. And if I go to the client and simply run my code once again, we're going to green at the same location where it is declared in theme color. All right. So this is how we can create iterator design pattern and use it in our clients.
