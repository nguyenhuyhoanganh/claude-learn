# Iterator - Pitfalls

Look, now, let's look at some of the pitfalls of this is apparent, there are not that many pitfalls here. The first obvious one is that we don't directly get access to an index. Why are you using the Étretat? And you might have faced this problem when using iterators with Java collection glasses. If you're using a for loop or even a for each loop, you can create an integer and use that as an index. OK, so this is one minor pitfall, if you can call it that. One additional pitfall here is that it drippers are quite sensitive to changes to the underlying collection because as soon as the collection is modified, you londoño, even though it is not clear now what that means. If there is, somebody adds an element that still makes our iterator state invalid. I guess what we need to handle those kind of scenarios here, and that can be a pitfall for this design better.
