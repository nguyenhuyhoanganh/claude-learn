# Iterator - Summary

All right. Now let's look at the summary of iterator design pattern. So whenever we want to give access to elements of an aggregate object or whenever we want to provide a sequential access to information, we can use iterator design pattern. Now, Iterator needs access to the internal data structure of our aggregator or our aggregate object to provide the iteration, functionality or whatever functionality of our iterator provides. And this usually means that it is a quite common practice to have iterator implementations as inner classes because then they can access our internal collection, which is part of our aggregate object. Now Iterator allows the client code to check whether there is an element available to consume, and it provides another method that actually gets or provides that next element to the client. Okay, so this type of implementation allows to write simple while loops which can check if element is available to read. And inside that loop we will access that actual element. Now, iterators typically are implemented as sequential sequential ways to access the data, but it is not mandatory that you should only provide a forward reading iterator. You can provide a reverse iterator if your aggregate objects collection supports. Now you can also provide bidirectional iterators, so you can call the next method on iterator. You can also call a previous method and it will provide a previous element in the collection. All right. So there are multiple ways that you can implement iterator and customize them for any particular application or particular use case. All right. Now let's look at the UML diagram of iterator design pattern. So we have this aggregate object and it represents an object which has elements or children inside it. And our client wants to iterate over those elements or access those elements one by one. Aggregate defines a method to create an iterator so we have a create iterator method here, which is used by client concrete. Aggregate object actually provides an implementation for this iterator and it will return an instance of iterator interface or object of a class who implements iterator interface. Next up, we have the actual iterator interface that has simple methods like Hasnext, which tells our client whether it can, whether there is an element available for consumption and a method called as next that gives out the next element to the client. Concrete iterators implement the iterator object and they have a state in them that allows them to remember their position in the underlying collection, which is part of their concrete aggregate object. All right. Now, one thing I want to highlight here once again is that you don't need an actual collection like array to use iterator design pattern. You can provide iterator for something like file reading or providing access to resource which is available on network. Now let's look at a sample implementation of iterator design pattern. So here we have an interface called as iterator. We have defined this iterator as a generic interface, so that allows this iterator to be implemented or customizable for any datatype. It has defined a method called as hasnext and it returns a boolean telling whether there is an element available for consumption and in next method that returns the next element in collection and it advances our iterator position. Then let's look at an aggregate object. So we used an enum in order to demonstrate that our iterator design pattern so enums have constants defined in them and our iterator is going to allow our client to iterate over these constants one by one. All right. So our theme color enum has a method called as Get Iterator, which returns an object whose class implements the iterator interface. So let's look at this implementation of concrete iterator. So here we have a class called as theme color Iterator. You will see that it is an inner class and we have this class inside our theme color enum and it provides implementation for the next method as well as has next method. You will see that we maintain some kind of state in our concrete iterator to remember the position while we are iterating over our inner collection. All right. Inside the client we are going to call the get. Rhythm method on our enum and using Hasnext and next method we can iterate over child's elements of our aggregate object. All right, so this is how you can implement an iterative design pattern in Java.
