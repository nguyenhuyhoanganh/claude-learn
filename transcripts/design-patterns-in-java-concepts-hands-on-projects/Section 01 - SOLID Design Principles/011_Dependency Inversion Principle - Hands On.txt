# Dependency Inversion Principle - Hands On

All right, Now let's see the dependency inversion principle in action using some Java code. So we have a class here called as message printer. It's a simple class with a method called as write message. And the idea here is that this method will get a message object. And right now it is also receiving a file name. And this class will basically print the message object by first converting it to a Json format and then writing that Json message to the file with the given name and it is using print writer to do actual file writing. Okay. So if we come to our main method here, we have we are creating a message object and then we are creating the message printer object and calling the right message. We are passing the message object here and the file name. So if we run this code right now, you will see that it completes its run and then it creates a file that you see here. Okay, so we get a message here. So if we change this to this is a new message. And if we run this code, once again, you will see that our file is updated. Okay. So this is the code that is currently present here. Just to be complete, I will quickly show you all the classes that are participating here. We have a message class here. It is simple class. It has a string which stores the actual message content. It has a timestamp property as well and it is initialized with the current time as soon as the message object is created. Okay, we have some getter methods here. Then we have Json formatter and it is implementing a formatter interface. Formatter interface is simple interface that has a format method and Json formatter implements that message. Here and it is using the Jackson library in order to carry out the Json serialization. All right, so this is the code that we have here. And as you can see, the dependencies for the right message that we can immediately find out is that it depends on the Json formatter and it depends on the print writer. Now this print writer is part of the Java's i o package, but nonetheless, this is a dependency of this particular message. Now what are the problems or what are the issues with the current implementation? Well, let's say if we want to print this message on console, then the current method is not going to work unless we go and write another method where instead of using print writer, we use system dot out. That is the first problem. Second problem is that if we want to change the format of the message, then we have to again modify this particular method and change the Json formatter to the new class that we want to use to format our message object. So these are the problems in traditional software development methodology that we use because we have to know about these concrete classes and that creates a tight coupling between our method, our code and these objects. Dependency inversion will allow us to remove this dependency. Okay, so our write message should not ideally be tightly coupled with Json formatter. It can depend on abstraction. So what is an abstraction here? Formatter which is an interface, is an abstraction. And on this, if we depend on this abstraction, then we can change the object that is actually used. Okay, without needing to modify this code. So how can we do that? Well, to do that, we have to change how we write our or how we implement our method. So instead of creating an object of Json formatter here, what I'm going to do is I'm going to remove this line entirely and I'm going to accept. Formatter as a method parameter, and you can guess. We can also accept Printwriter as a method parameter too. So now, instead of creating a printwriter ourselves, we can simplify our method code to these three lines. All right. We don't even need to close the printwriter. Okay, Let me just. Change that. All right. Okay. So now instead of creating the Json formatter, we are accepting a parameter. So we are declaring our dependency and we're asking our caller to give us the dependency. Okay. So somebody else is now giving dependency to this method, and that is the inversion now. Okay. So the responsibility of creating the actual object of the dependency is now with the client or caller of this write message. So if we come back to main method now, instead of simply passing the message and file name, we also need to pass the actual formatter. And we also need to pass the object of print writer. Okay. So we are going to for right now use the same method file writer. Okay. One thing that I missed out is that since we are accepting sorry about that. Since we are accepting a print writer, we no longer need the file name. Because if you look at the method code, this file name is nowhere used. Okay, so we can safely remove the file name because the actual file name now can be specified in the client code or the code which is calling our method. Okay. So I'm going to pass the test message dot txt file name here and I'm going to remove this argument. Okay. So I need to import this file writer. And there we go. Okay, so this is what we have done. Okay. Once again, we go back to our original method. We were creating Object of Formatter. We were creating object of print writer. We are now accepting those as arguments to our method. Okay. And in the client code now, instead of simply calling the method, we have to provide the dependencies for this particular method. So our method now contains only the business rules. Okay, there is nothing here, but in real life you will have only the business logic here and now. If we run this code again, let's change the message here and run our main method again. So we're going to run as Java application. And if we go to the test message, you will see that our message is updated and timestamp is updated as well. Okay, so this is how we use dependency inversion. And the benefit of this is that instead of print writer, maybe we can pass something like system dot out. Print writer start out. Okay. And now if we run our code run as Java application, you will see that we get our message on console. Okay, so this is how and this is the power of dependency inversion. Actually, once you see how easy it is to change the actual objects that are used by our main method or the original method, you will see the power behind the dependency inversion. We didn't need to modify our write message method. You can implement this method, test it once and then you can keep it aside. Okay. And we can change the objects or the dependencies that we are supplying to this particular method and we can get different type of behavior or different kind of output. Okay. On the similar lines, we can also change our formatter to something like a text formatter and we will get message in the text format. Okay. So this is how dependency inversion principle is implemented and this is how it helps us. It's a really powerful mechanism and if you are using something like spring or spring boot, you are already making use of dependency inversion, the auto wiring that happens, the spring beans, those are all utilizing the dependency inversion principle. Okay, so that's it.
