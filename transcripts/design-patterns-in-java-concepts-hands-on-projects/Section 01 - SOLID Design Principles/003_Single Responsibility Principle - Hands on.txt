# Single Responsibility Principle - Hands on

All right. Now let's look at an example where we can see the single responsibility principle getting violated. And we are also going to see how we can fix such kind of problems. So let's take a look at the code here. This is a simple application. We have a class here called as User Controller. Now, from the name itself, you can guess that what we are trying here is that we are trying to simulate something, a class which you will write if you are developing an MVC web application or a rest web service using something like spring. So although this is not an actual web service or an actual three tier MVC application, you will write something similar in those applications in real life. Okay, so user controller is the class that we are going to focus on, and this class will basically get requests from the client. In this case, we are simply going to call its methods. But in an MVC application or a rest web service, a controller will receive requests and then it will process those requests and send out the response to the client. In case of our example here we have a method here called as create user, and this method will receive a string which is a Json string that contains user information. This is the user that our client wants to create and create. User will return a string as a response. In our case, it is simply going to return a simple plain text string and it will return success. If it was able to create the user object or it will return an error if there is any issue with that. The user itself is a simple class. It has only three properties. Okay, and we have a store class here in order to simulate something getting stored in a database. In our case, this is just a HashMap. Okay? But you get the idea how you will have an actual JPA entity here and you will have a persistent context and everything else. Okay. In this example, the store is a stand in for that entire persistence system. Lastly, we have a main class here, so this class is going to we are going to use this class to test our user controller. Okay. So first of all, let's go ahead and run our main method. So I'm going to right click and select Run as Java application and you will see that we get a response that valid Json was sent and we received success in the response. If we send invalid Json, we get an error response. Okay. So our example is now set and I hope you get the idea how this example works. Now let's focus on the user controller class. What whenever you are trying to test whether an existing code or existing class satisfies the single responsibility principle, you have to ask yourself what this class is supposed to do and what exactly it is doing right now. A controller in an MVC application is supposed to be the. Controller. Well, it will receive the requests from the client and it will hand that request to the rest of the application and it will get the results of that processing and will send the response to the client. A controller itself is not supposed to have any business logic in it. Okay. That is what the rest of the application is for our class. Our user controller here is doing multiple things, though it is getting the request and creating a user object from that request, it is validating that object and it is also taking care of storing that object in our HashMap. So now. You can see how the single responsibility principle is getting violated, because now there are multiple reasons for this user controller class to change. If our validation logic changes or if we add a new field in our user, then user controller needs to change because it needs to modify all these validations. If the way we store our user object changes, let's say if we start using an actual database or if we start using something like NoSQL databases like MongoDB, our user controller needs to change apart from these additional responsibilities. The original responsibility of this class, which is actually accepting the request. If that changes, then of course this class has to change. So now you see there are multiple reasons for this user controller to change and hence it is violating the single responsibility principle. All right. So how can we fix that? So whenever you find something similar in your own application, the first thing that you need to do is to make sure that you have a good test case ready to test your existing code. So our main method here is going to serve as a test case for us. But in real application, real life application, you will have something like JUnit or testing test cases. And these are very important because we are going to refactor that existing code and we do not want to introduce new defects as a result of that refactoring. Okay, so we have already run our main method and we have seen the output of this test results and we are going to keep running these tests whenever we do or whenever we finish part of our refactoring. So how to refactor, how to refactor or fix something like this? Well, the solution is quite simple. You have to identify a responsibility that this class is having, which it is not supposed to have. For example, I see that validation is something that controllers should not bother about. It should not have the implementation of all of these validations because these are going to change. Okay. These are going to change. The rules are going to change. The fields are going to get added or removed as the application is evolved. So to do that, we are going to create a new class here called as User Validator. I have an empty class here, so let's write a method here. I'm going to write a public method which returns a boolean, and this method will validate a user object. All right. And our user controller here, instead of doing the validation, we are going to create the user validator object and we are going to call its validate method. So I'm going to say validator dot, validate user and pass the user object to it. This method will return us the result of validation. So let's store that in a variable called as valid. All right, so now user validator should carry out the validation. We have a if check here that we are actually calling a method here is valid user and we are returning an error response. So instead of this method call, we are now going to simply check the flag that we have created here. Okay, so if the validation has failed, then we're going to send the error. So now we need to implement this user validator. Typically it should be a straightforward process. All that we have to do is we have to take out all these methods that are carrying out the actual validation of user objects. So I have just simply removed those methods and I'm going to add those methods in my user validator class. Okay? And here I'm simply going to say return is valid user and pass the incoming parameter. All right. So now our user controller should work as it was working before the refactoring. And to test that, we're going to go back to the main method and we are going to run the main method. All right. So we get the exact same results from the main method we get when we send a valid Json, we get success. When we send invalid Json, we get an error. All right. So now we have reduced or we have taken out at least one responsibility from the user controller so that we have a separate class. And if you look at this user validator, you will see that this is a good example of single responsibility principle because this class is in charge of validating user object. So this class will change only if something changes in our validation logic. Okay? It will not get affected if we change, for example, how we store the user object. This class is not going to change. Okay. Our user controller, however, is still going to get impacted if we change how the user is stored. So to do manage the persistence, I have created another class here, an empty class called as User Persistence Service. Let's add a simple method here called as save user, which will accept a user object. And since our store bridge is a simple class, we are going to create that class object here. New store. All right. And in the save method, we are simply going to hand that object to store method. All right. And this is our user persistence done. So again, we are going to go back to the user controller and instead of creating the store, we are going to create the object of user persistence service. Let's call this service and. Create the object and instead of saying store dot store, we're going to say service dot save user. User. Yeah. Let me just rename this to persistence. Service. Okay. So that. It is more readable. All right. So now we have taken out the persistence responsibility from the user controller and put it in a separate class. Okay. So this class will change only if there is some change in how we store our user object. Okay. So now if you look at the user controller, it is only taking the incoming requests. It is still doing the parsing of incoming string from Json to Java object and it is up to you what you want to do. You can move this logic as well in a separate class and that again will be a good design, but I will leave that to you. Okay, but you get the idea how we are going to fix the violations of single responsibility principle. I'm going to run the main method one last time to make sure that our tests are still passing. Okay. In a real life spring application, you will have persistence service as well as the validator injected in your controller bean automatically. We will not be creating these kind of objects in a spring application. All right. So this is how we check whether a class is violating single responsibility principle and this is how we refactor it so that it complies with that principle.
