# Interface Segregation Principle - Hands On

All right. Now let's take a look at a code example that will help us understand interface segregation principle in detail. Okay. So this is a very simple example. We have a class here called as Entity, and you can think of this class as something which is a base class for your JPA entities, okay? And we have two entities here, actually one called as user. It has simple name and last login as properties and getter setter method for those properties. And we have a another entity called as Order. Now in a typical application, you will have a persistent service as well as a data access layer which actually communicates or uses persistence service in order to connect to back end. But in this example, we are going to simply stop at the service layer itself. So we have a user persistence service and as you can guess from the name job of this class is to help us perform operations on the persistent store specific to user entity. I'm not using layer, so I have simply declared a map here which will store the user entities. And this service has methods that you will typically find in any persistence service you have Save method, delete method, find method. And we also have a method called as find by name. Now, one common way that we run into violations of interface segregation is that when you're trying to extract an interface from an existing class. So let's say someone who wrote this code didn't actually thought ahead and he directly wrote the User Persistence Service. And now somebody said that you should have a common interface for all of these services. So what he does is he extracts an interface and this interface is persistent service. So it contains all those methods that are defined in user persistence. Okay? So it contains all those methods. Now, as we saw, we also have an entity named as order. And let's say we want to now create a service persistence service for this particular entity. So how should we do it? We will go and create a new class and we will call this class user persistence service. All right. And we are going to implement the persistence interface. Okay. So Persistence Service is using generics, so we are working on the. Working on the order entity. All right. So now I will simply generate the method stubs for this particular class. So I will say implement methods and whatever is present in the persistence service we are going to implement. I will begin by declaring again a map that will that we will use to store order entities. Order! I will call this orders and we are going to immediately initialize it. So that we can start using it. All right. So we come to the first implementation save. All right, This is fine. I'm directly using Synchronize block, which is not super performant, but this will do for us. So we can just say orders dot put and we're going to get the ID from our entity. So entity dot get ID and we will store the order. All right. We can similarly implement delete. So to save time I will copy paste this code and I will simply replace the put call with call to the remove method. We pass the key and since we are using the sorry it needs to be entity here we are using the ID of our order as the key. We are passing that to the remove method on the similar lines. We can also implement the find by ID method. It is very simple. As you can see, since we have a map that is keyed on the ID itself, I can simply say orders dot get and we are going to get the ID, All right. And we need to return the order. So, so far, so good. Okay. We don't see any problem. We don't see any violation of any design principle here. And now let's go to the next method. Now we have a method called find by name that is defined in the persistence service interface. If we go to our order, you will see that we don't have anything like name here. Okay. Name doesn't make any sense for an order entity order can have an ID or can have a date. It can have a value, but we don't have names for our orders. A user, on the other hand, makes perfect sense because it has a name. So having a find method that searches based on the name of a user makes sense here. But our order persistence service cannot provide any implementation for find by name. So what we do in this kind of situations, well, typically what people do is they will say throw new unsupported operation exception and they will pass on some methods saying that find by name is not supported. All right. And this is here is a is an example of problem. If we do not follow interface segregation principle because we have an interface that contains methods that don't make sense for some of the implementing classes. So this is how we run into violation of this principle and how will we solve this problem? Well, the idea here is that to break your interfaces, that is one solution. Second is that if a method in an interface is applicable to only a single class, then we can either remove that definition and put that only in the class for which it actually makes sense. So I will simply remove the override method here. And since we know that name makes sense for only user persistent service, anybody who wants to use that particular method can cast or use reference to this user persistence service so that our order persistence service is not forced to provide dummy implementation or empty implementation for methods that are defined in persistence service. All right. This is a very simple principle, and that is why the example itself is very simple, but it is really a powerful design principle. And you will find this principle violated a lot of times in real life projects, because one example that I showed you is that sometimes people write actual implementing classes and then they extract an interface out of it so that the code looks nice on the top, that, okay, you are using an interface, especially someone who is starting out. They may try to go this way, but make sure that you are not adding methods in your interface which do not make sense for all other classes. Okay, so this is the interface segregation principle in Java.
