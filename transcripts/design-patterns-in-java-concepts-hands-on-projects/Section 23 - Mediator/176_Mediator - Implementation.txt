# Mediator - Implementation

All right, Now let's see how we can implement mediator in Java. So as we discussed in the example human video of this particular topic, we are going to use Java effects in order to demonstrate how a UI mediator or how a mediator design pattern works. All right. So to begin with, we are going to implement our mediator first. This is going to be a concrete class because we want to keep things simple. When additional point that I will quickly highlight is that I have already created an interface called as UI Control and we have discussed this interface in the video. I will explain it again. Our mediator doesn't need the participating objects to belong to a specific type. It can work with different types of classes. What benefit we are getting by having all of our objects implement a common interface is that our mediator becomes easy to implement and so that you will understand the principles of this design better instead of focusing on the actual implementation. All right, so we have the UI control interface which will be implemented by objects that are participating. So here we have a class called as text box, which extends from a text field class, which is a UI control in Java effects. So we're going to have an object of text box on our screen and we want this text box to participate in the collaboration. So we want to know when somebody types a value in a text box and we want to be able to tell this text box that this is your new value, or we're going to tell this text box somebody else has changed its value. Do your own thing. All right. So this is the text box and this is going to implement our UI control. So the objects are going to implement UI control. And the benefit of this is that our mediator can work on this interface, so it will work on this interface and make our implementation easy. So let's start implementing our UI mid-year. So we discussed that our mediator, concrete mediator, needs to maintain a list of objects that are participating in collaboration. So let's use a collection, a simple list. And since all of our objects are going to implement UI control, we can use this interface. So I'm going to name my collection colleagues and we're going to create a simple array list here. It's quite simple, nothing complicated. Next, we're going to provide a method using which objects can register themselves with our mediator. Now, during registered they are going to pass themselves. So we are then going to get a reference of UI control. Now, once they register, we're going to add them to our collection. So I'm going to simply say Click, start, add. And whatever control has registered with this, I'm not doing any duplicate checking or any other knowledge checking and all that. I'm going to keep this implementation simple. Know, the next method we need in our mediator is a method that is called by these objects to tell mediator that their state has changed. So they are going to notify to a mediator. So let's call this method value changed method. So we're going to call value change and our mediator must know which object has changed. So it is going to pass itself. The object is going to pass itself in this method so that our mediator can use this reference to figure out which of the colleagues has changed. Now, the job of mediator here is to tell remaining objects that this control is changed in its state. So we're going to iterate over our colleagues collection. We are going to remove this object which has just told us that its state has changed because that would be a redundant notification because this object already knows that it state has changed. And for the remaining UI controls in this collection, we're going to send out a notification saying that control state has changed. So I'm going to use a stream stream API here so that our code is simple and easy to write. So once we have stream of colleagues or UI controls, we want to filter out this particular object. So we want to filter out the control. So we are going to pass on only those controls which are not this object. Pretty simple. We are filtering out this control from our collection. Once that is done, we are going to simply iterate using for each and we're going to say for every UI control call there. I have made a mistake. Syntax error C is not equal to control. All right, so we have filtered out our control. Then we're going to simply iterate over remaining objects using four H, and we're going to call the control change method that is defined in our UI control interface. And we're going to pass the control here. So this is the UI control interface and this is the method control change which is called by UI mediator to tell these objects that somebody else has changed its state. Right. So this is how we can implement our UI. Mediator or a mediator. All right. This is the only thing that mediator does. Now, let's jump into our text box class, and this is one of the participating object. So let's implement our remaining mediator design pattern here. So first of all, we know that this text box is going to need our reference to our mediator because it needs to tell the mediator whenever it state changes. So we are going to accept a mediator object or reference to mediator object in the constructor of our text box so that we have access to this mediator as soon as our object is created. I'm going to store this reference store this reference in our class as a field so that we can use it anywhere in the instance methods of this class. So I'm going to say this dot mediator is equal to mediator. The subtext method that you're seeing is going to set the default value that is displayed in our text box control. So when our application runs, you will see that we have this default value of text box. That is what set text method does. It is provided by the text field class, which is provided by Java Effects API. All right. So now we have a mediator here. So first thing we want to do in every object is that we want to register this object with the media. So I'm going to say this dot mediator dot register and we need to pass UI control. That means this class needs to first implement the UI control interface because in the register method of our mediator, we are expecting an object that implements this interface. So I'm going to simply pass here this and to remove some errors I'm going to generate the method stubs for UI control. So these are three methods that are defined in UI control. All right, so now where text box object is created, it knows about the mediator and it has registered itself with the mediator. Now let's implement the remaining functionality one by one. So let's first take care of notifying the mediator that this text box has changed. All right, so how can we identify that? So the next few lines of code are specific to Java effects, so don't worry if you do not understand, I will explain them briefly. So in Java, if we have a text property on a text field and we can add a listener on this property, so we can add, we can say add listener and this will be a change listener. So whatever we provide here, whatever expression we provide here will be or the class object provide here will be called whenever a text, whenever text property of our text field changes. So this is what this code is doing. It's adding a listener on the text property of our text box. So let's implement this listener. So we are going to use the Lambda expression here because this interface has only one method. I will show you what the interface is so that. Java Fix being value. Just ignore it. I'm just going to jump into the definition of this interface and you will see that we have only one method in this interface. It is a functional interface. It has a method called S changed. So this is the changed method and we are going to get three different things here. We're going to get the observable that is the property whose value was changed our text property. Then we're going to get the old value and new value as parameters in this change. So we are going to provide this change listener in our text box as a lambda expression. So we'll remove this part of code. So the value, then the old value and then the new value. These are the things that we're going to get here. I'm going to write our Lambda expression now. So what we're going to do is we're going to say this dot, mediator, dot. Value changed and we're going to pass ourselves here. All right. So once again, we are adding a listener on our text property because that is what our user is going to change. Once he changes that property, we're going to call our mediator and tell the mediator that my state has changed. Now do whatever you want with this information. So this is what is happening in this three lines of code. All right. So we have now notified our mediator that our value has changed. What is remaining now? Well, we need to make sure that what is going to happen in the control changed method. So control change method is called by mediator when somebody else changes the value. So what we're going to do is we're going to implement control change method next. So here what we're going to do is simply call this dart set text method. And you have seen this method call here using set text. We can set the text property of our text field and the value we are going to get from the control dot get controlled value method. So this is part of our UI control interface. This method get control value which will return the string representation of that controls current value. So that brings us to our own implementation of control value. We need to return whatever is our text. Oh, Uh, what is our text value in the text field? Right. The control value. The control name. I'm going to simply return a hardcoded value because this is used only for debugging purposes. So. Sorry. Excluding. Text box. So this is text box. All right. So you might think that our implementation is complete. But there is one caveat that I wanted to highlight in this implementation. So once again, I will quickly explain. So whenever our text property changes, we are telling my editor that our value has changed. That's fine and our mediator is taking care that it is not calling our control changed method here. All right. But if let's say somebody else changes, so let's say a slider is changed and it calls control change method on our text box, we are going to call set text. Set text is going to invoke this listener. We are then going to tell mediator that a value is changed and we're going to send out one more notification to all the remaining controls. So you see what is going to happen here. So as soon as somebody as soon as our UI mediator or mediator calls control change method, our set text method is going to change is going to be called and this set text invocation is going to result in a notification sent back to the mediator. So this is the loop that we need to break because mediator tells us somebody change, so we change our own property and that results in we sending mediator once again that okay, we are now changed, but this is redundant. To solve this problem, we are going to use a simple boolean flag. You can implement this in a better way. So I'm going to say we have a flag called as mediated update. Mediated update is going to indicate that the property update is because of mediator. And in our listener, we are going to check if this is not a mediated update, then only call the mediator. So if this is a mediated update, then we do not want to tell mediator. Okay, my value change because mediator already knows that in our control change method, which is called by our mediator, we are going to say our mediated mediated update has begun. So we're going to say it is true. And once our set text is done, we're going to say it is false, right? So we are going to use this Boolean variable to mark the boundary where a property change is happening because of mediator. So this marks the boundary. And inside this boundary we can change the property, which is a result of mediator telling us a value has changed. So this is how you can implement an R class whose object is going to participate in the mediated or mediated collaboration. In a similar way, we are going to write a slider class, so I have already written this class here, so I'm going to simply remove the comments from this class. This class also works exactly same. We are going to get a mediator here. We are going to save the mediator reference. We are going to register ourselves with that mediator. We are going to have a listener on this slider property and we are then going to check If this is not a mediated update, then notify mediator that our value has changed in the control change method. Again, we are going to mark boundaries saying that the next property update is because of mediator. Then we're going to set the value, new value and marks that our mediator update has finished. So we need these boolean flags because the set value method, we don't have access to the actual field because these are Java effects controls and that's the reason in your real life implementation. You may not need this type of implementation or this Boolean variable. We need this because we only have access to this setter method that is provided by Java Fix, and this setter method always calls this listener from which we are sending the update. So if we don't guard against the mediated update, we would run into this loop where mediator tells us somebody changed, We change our value and we tell mediator back, we changed. So that's it becomes it's a little bit ugly and not that much clearer, but I hope you get the idea what we are doing here. The next control is label. Now this is a simple control because a label is a read only text. UI control is only a read, only control. So here we only have a mediator. We are going to register ourselves with mediator, but we don't have any change Listener because this is a read only control. However, if somebody else changes their value. We are going to update our text that is displayed in this label. So these are the three controls that form this collaboration group. So if text box changes, our slider and label is going to update. If slider changes, text box and label are going to update. Now our job is to run into jump to the client class where we are going to create this UI application. So there are two parts. First of all, we are going to create our mediator. So we're going to create a new object of UI mediator and then we're going to create three controls or three objects that form this collaboration. So each object is provided with this same mediator object. So we have a slider object here. We are giving it the same mediator, text box, same mediator and label. So these objects are now going to register themselves with this mediator and form a collaboration group. Now the next bit of code is Java Effect code. What we are doing here is we are simply. We are simply going to create a pain, a great pain to which we are going to add our controls. Then we are going to create a scene. And in this scene we are going to set our grid and add that scene into our primary stage. So these lines, of course, you don't need to worry. These are Java fixed lines of codes. What is important is this. Here we are creating the objects and creating relation between these objects and mediator. All right. So let's save this class, client class, and let's run it. So I'm going to say run as sorry, run as Java application. All right. So this is the application. It's quite ugly. So you will see that we have this default value of TextBox. Our slider is at zero position and our label is showing its default value. Now, let's change this slider. Okay. You will see that as soon as I change my slider. All these two controls are changing. These two controls are changing. If I change the text box, let's say I typed 20, you will see that all the remaining two controls will change to reflect that value. So we can use mediator in these type of situations and you will see the benefit. A huge advantage you will get by using this design pattern. So all these objects are telling each other whenever the estate changes and the remaining objects will update their state and reflect it in the UI to reflect the new value. All right. So this is how you can implement a mediator design pattern. I have attached again the code for this example to this lesson. You can download it and study how we are implementing this mediator design.
