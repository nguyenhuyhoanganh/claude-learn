# Mediator - Example

All right, Now let's look at some examples of mediator design pattern. So before we do that, I want to again reiterate one important characteristic of mediator, and it is that mediator streamlines the communication between multiple objects. So if a class simply calls methods on multiple objects, it cannot be called as a mediator design pattern. And this is to highlight that some of the examples that you will find online for mediator design pattern are not actually examples of mediator pattern. Just because a class can call some other class after some time elapses doesn't mean that it is a mediator. So what are the examples of mediator then? Well, we have to go into a little bit deeper and look into places like Swing and JWT in order to find the real example of mediator design pattern. So the Button group class, which is present in the Java dot swing package is an example of mediator because it takes care of making sure that only one button or only a single button in a group is selected at a time. So participating buttons or classes which represent buttons, they notify this mediator when they are selected. So let's look at the code snippet from this, these particular classes. So first of all, we have the button group class, which is our mediator, and you will see that it has a method called as set selected, which is called by these buttons. And this method takes care of notifying or making sure that if the new selection is not or is different from the old one, then it will unselect the old selection and mark the currently selected button as the selected button. The right hand side. You will see that we have a code from toggle button. So we have a set selected method here and using the get group method it will get access to the mediator because remember button group is the actually mediator in this example. So whenever it selected a method is called, it will notify the mediator that its state is now selected and mediator can take some action on that particular selection. All right, so the button group class is an example of mediator. Now sometimes you will see that a friend controller, which is used in many Java applications, is also given a given as an example of mediator. And one good example of this design pattern. Friend controller is the dispatcher servlet in spring. So I have taken an example or diagram from the spring website and I will briefly explain what is happening here. So friend controller is basically a class which is, as its name suggests, sitting in front of your entire web application. So all incoming requests will land on this particular controller and this class will then forward that incoming request to an appropriate class. So it will pass it on to the actual page controller. All right. So if you don't know about spring, then this may sound a little bit confusing. However, you can at least remember that. Dispatcher Servlet in spring can be given as an example of mediator. Now purpose of friend controller actually is to act as a central point where requests from outside world will land and then they are forwarded to appropriate page controller. Okay. And the controller which is actually used is basically looked up by using some kind of URL to class mapping. So friend controller pattern can be thought of as a specialized version of mediator pattern because friend controller satisfies some characteristics of mediator design pattern that it acts as a central hub for communication between objects, but it is also a specialized version of mediator because it also handles requests that are coming from outside system. First difference and second difference is that it performs some kind of lookup in order to find out which object to notify. Okay, because in a pure mediator implementation, the mediator will notify all the objects. Whenever somebody tells it that it has changed his state, then it will notify all the objects. However, in front controller we use some kind of mapping to find out the correct page controller. So that is why I'm saying that. Yeah, you can say that it is a mediator, but it is a very specialized version of this mediator design pattern.
