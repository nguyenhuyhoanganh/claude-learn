# Adapter - Implementation - Class Adapter

All right. It's time to see how we can implement the adapter design pattern in Eclipse. So I'll switch over to the eclipse. And again, here I have some classes that I have already written to save us some time while implementing the adapter design pattern. Now I will go over these classes one by one to see what they are. So first of all, we have an employee class here and this is our adapter class. That means object of this class can work or can provide the functionality that is needed by our client code. So what is this client code? So here we have a business card designer class, and this class is supposed to print business cards for its customers. It has a method called as design card, which expects an object which implements this customer interface. And it's going to call some methods on this object in order to print the business card. So what is customer interface? So this is the customer interface, which is the target interface. If you remember from our UML diagram. And it has defined a few methods that are needed by our business card designer. Now, looking at our employee class, we can see that the information required by customer like name, designation and address is available in our employee class, but the that functionality is provided with a different method. So we have a gate full name which can work where we expect a name, we have a job title method, get job title that can work where a designation is required and we have a method called as get office location which can work where get address is required. So now we have a situation where we need a class adapter or sorry, we need an adapter in order to work with this code. So to start off, we are going to implement the class adapter, which is also called as a two way adapter. So in order to implement the class adapter, we start by extending from our existing class, which is employee. So I'm going to say extends employee. Then we need to also implement the interface that is expected by our client. So I'm going to say implements customer. Now it means that we now have to provide implementation for methods that are defined in our customer interface. So I'm going to quickly generate stub for these methods, get address, get designation and get name which are defined in the customer interface. All right, so these are the method steps. Now all we need to do is to translate these method calls to an appropriate method in our adapter. And we have already seen that get method. We have a method called as get full name in our employee that can work here. Now we are able to call this dot get full name because our class extends from employee. So we have get full name method inherited in our adapter. On the similar lines, I'm going to quickly provide the methods that work. For the customer method implementations. So git address. We have a method called as git office location and this can work as an address. All right, so this is our class adapter completely implemented, so we extend it from our existing class, implemented the interface that is required by client and we provided the interface translation. So whenever we get a method that is called on the customer interface, we translate that method in a in a to a correct method which is already present in our adapter. Now let's see how we can use this class in our main method. Now you will see we have, I have defined a method here called as populate employee data. This is a helper method that expects an employee object. And what it does is that it can populate some sample data into that object so that we can use it to test out our code. So to begin with, we are first going to create an object of our class adapter. So I'm going to say employee class adapter adapter is equal to new employee class adapter. We don't need any constructor arguments here. Then we are going to make sure that we have an object of the client code, which is the business card designer. So if you remember, this is the class that we want to use, which expects a customer object. So designer, we're going to again create a simple object of this class and we are going to call a method on this designer designer, which is the design card, and it expects a customer object so we can pass our adapter here. All right. And then we are going to simply say print the card so that we can make sure that this adapter worked. Now, this is what adapter does. It can work or it can be used. Object of our adapter can be used where a target interface object is expected. But since we are using a class adapter, we can also use our adapter object where our adapter is expected. So if you look at this method, it expects an object of employee class and our adapter since already extends employee. We can also use our adapter to call this method populate employee data. And this is why class adapter is called as two way adapter because if you look at line number eight, we are using our adapter where an adapter is expected and we are using our adapter on line number ten where we have a design card method and here customer interface was expected there. Also, we can use our adapter. So that's why this is called as a two way class adapter. Now let's run this code and make sure that it works as intended. All right, So we have our business card card created and we get we can print that string in order to get the output. All right. So this is how you can implement a class adapter or two way adapter.
