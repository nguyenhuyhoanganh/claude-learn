# Adapter - Implementation - Object Adapter

Now we're going to see how we can implement an object adapter in our Java code. So I'll switch over to Eclipse again. So in the previous video, we have seen how we can implement a two way class adapter and now it's time to implement an object adapter. So here I have already created an empty class called as Employee Object adapter. So in order to use or in order to implement an object adapter, we simply need to implement the target interface that is expected by our client code. All right. So we simply implement customer. We do not extend from our employee class and then I will simply generate the method stubs for methods that are defined in our customer interface. Now, an object adapter needs an object of our adapter. So I'm going to declare a private field where we are going to store the adapter. So I'm going to name this object as an adapter. All right, so this is the adapter object that we have in our object adapter. And the method implementation itself is quite simple. Instead of using this, which we did in the class adapter here, we are going to call the same method but on the adapter object. All right, so this is the only difference between class adapter and an object adapter. So for designation we are going to use the gate jobtitle method. And for address we have an office location, get office location. All right. So this way we can implement our object adapter. Now the question remains how we are going to get hold of this object which we need. The best way to implement an object adapter is to write a constructor for your object adapter class. Sorry object adapter class and expect accept an adapter object as your constructor argument. So I'm going to say we need an employee object in our constructor and we are going to say this dot adapter is equal to whatever is given to us in our method parameter. All right. So now we have an adapter object that we can use while implementing the methods that are defined in target interface. All right. So you can see that this is quite simple. There is nothing to it. And the only difference between class adapter and object adapter is we're going to use a composition in order to implement our functionality. So adapter is compose or is using composition in order to provide the customer interface method implementation. So let's jump over to main method and here we're going to use our object adapter. Now first of all, we're going to need an employee object. So I'm going to create an object of employee class, which is our adapter, because remember, this object is needed by our object adapter. Now, before we can pass the employee object to our adapter, I'm going to call the populate employee data method. So this method is going to take an existing employee object and populate some information that we can use. Next up, we need to create the object of our object adapter. So employee object adapter. I'm going to name this as object adapter. Object adapter is equal to new employee object adapter, and we're going to pass the employee object that we just created. All right. So this object, we created that object here and we need that object in the constructor next step. The only thing that is remaining is we're going to again call the designer method. Sorry, the client code, which is going to expect a customer object. We're going to call the design card method and we're going to give it object adapter. All right. Again, I'm going to save the result of calling this method in a string object, which we had to use, and I'm going to print out whatever is present in that string before we run this code. I'm simply going to add a line break here so that we get a clear separation between these two outputs. All right. So once again, we simply created an adapter object. We get data populated in that object. We created our object adapter instance by providing it with our adapter object. And then we called our client code and passed on the object adapter instance. All right. This is how we can implement and use object adapter. So let's run this code now and you will see that we get again the same output. All right. So this is how you can implement an object adapter in Java.
