# Observer - Implementation

All right. Now we're going to see how we can implement observer design pattern in Java. So let's switch over to Eclipse. And here we have a class called as Order. Now, these are exactly same as we saw in the previous video where we discussed the UML for this particular example. So we are going to have an order object which is going to be our subject, or as it is sometimes called, observable. And whenever state of an order object changes, there are a couple of objects which want to get notified about that state change. So let's start implementing our observer design pattern. Before we do that, I will briefly explain what state is present in the order object. It's nothing complicated. It's pretty simple. We are not even recording the item names. We are simply recording the number of items that are present in this order and the total cost of these items. Okay, so these are the only two properties that are going to change or these are these two properties constitute the state. That is going to change in our order object. We have an ID here which represents the order number and it is given to this object in its constructor. All right, so we have Add item method here and we are simply going to specify the price of the item that is being added. And in this method we are going to add that price to our total item cost and we are going to increment our count. We have few getter methods that return the number of items in the order and the current item cost. All right, so this is our order object, our order class, and this is going to serve as our observable. Now, we are not going to create an interface here because we have a very simple application and our observers are not going to listen to any other objects. Okay, So let's begin. So first of all, what we need to do is that we need to define an interface which will be implemented by all observers. So even though subject interface or observable interface is optional, we still need an interface for actual observers. Okay, because order object is going to use this interface to tell objects other objects that its state has changed. So let's define this interface. We are going to add a simple method here called as updated and we are going to get pass it on object of order here. All right. So this is the observer interface and that's all there is to it. Whenever our order changes, it's going to call the updated method on all registered observers. So that brings us back to our order. So here we need to provide methods using which observers can register themselves and do register. So we're going to add two methods here. We're going to create a public method, public void, and let's call this method attach. And we are going to expect that an observer reference is going to be passed to us. All right. So this is the attach method on the similar lines. We are also going to declare another method, define another method called as. Detach. And here also we expect an object of observer. All right. So we have these methods which can be called by other objects. Now, what's happening or what's going to happen in the attach method? Well, we need to hold on to this reference that is given to us because this is an observer object that that is saying to our order object that it is interested in listening about its state change. So to hold on to these observers and there can be multiple observers, we need some sort of collection. So I'm going to declare a field inside our order object. And let's use a simple list here and the list will hold order observers. I'm going to call this list observers. Let's initialize it on the same line. Use the array list class. All right. And in the attach method, we are simply going to add the incoming observer to our observers collection. So when it comes time to send out notifications, we are going to use this collection. And this collection will hold every object or every observer that has registered with our order in the detach method. We are simply going to say observers dot remove and we're going to remove the observer which was given to us as an argument. All right, so this is our attach and detach methods. And here is a collection of observers. Now, one thing that is remaining here is we need to actually send out notification when state change happens. So if you look carefully, you will see that we are not going to send notification when somebody creates our object because you can easily guess that there is no point to that because our observers collection is going to be an empty collection. So the only method which causes our state to change is the Add item method. So in this method at the bottom, we are going to get hold of our collection, which is observers, okay? And in this collection we're going to iterate over all the observers that are present in that list. And on each observer we're going to call the updated method. Okay, so this updated method is defined in our order observer interface. And we're going to pass our order object as an argument to this updated method. All right. So this is the line which sends out notifications to all observers that its state has just changed. All right. So this is all there is. There is needed for a verb subject or observable. Now we have this interface. Now let's create some actual observers. Concrete observers or listeners which can listen to our audio object. So I have an empty class here called as Price Observer. And the hypothetical mission brief here is that this observer should listen for state change on our order object. And we want some kind of discount to be given if the item cost exceeds certain limits. Okay. It's a simple requirement, but we are going to use observer design pattern to do that. To begin with, we are going to implement our order observer interface. Okay. So it implements order observer. I'm going to quickly generate the method stub. For the updated method. All right. And now let's provide some implementation. So whenever updated method is called. So whenever we create an object of price observer and it's updated method is called, we know that something has changed in the order, but we don't know what is the current value of the item cost or what is the current value of number of items is to do that, we are going to call the order object back and ask for the state that we are interested in. In this case we are interested in item cost. So I will simply create a simple local variable doodle and we're going to call the get item cost method on our observable. All right, So this is how you would typically implement an observer. You will call back the subject and ask for the state that you are interested in. And let's implement some logic here. So we're going to say if our total is greater than or equal to 200, then let's say we want to provide some kind of discount here. So how should we provide that discount? Well, we'll jump back to order class. And let's add another field here that will record the discount that is applied to this particular order. So I'm going to create a field here called as Discount And. Let's generate the getter setter methods for this. Field. So we're going to generate get discount and set discount. All right. So we now have these methods and from the Price Observer, we. We are saying that if the current item cost is greater than or equal to 200, then we are going to say order dot, set discount and we're going to give it $10 discount. Simple. Let's add one more condition here so we can test it out thoroughly that if the total however, if it is greater than or equal to $500, then let's give them discount of. $20. All right. And this is how you would implement your observer. You will get a notification. You will check the current state in the observable for whichever property that you are interested in. And based on the value of that property, you would take some action. So here you will notice that I'm modifying the observable again. But if you look at our order class, we are not going to send a new notification if our discount changes and this is what you should keep an eye on, that you should not enter into an infinite loop if you simply added notification to each and every setter method. And if one of the observers goes back and modifies our observable, then it's again going to send the notification. We are again going to calculate the same discount value, update the observable again and we will get notification back. So this will be the infinite loop. So to break that, we have added the notification, the code which sends out notification only in our Add item method. All right. So we now have a discount. So what I'm going to do is we have this method here called as get total. So I'm going to deduct my discount from the total item cost. All right. So this is how we are going to apply the discount to our order. And this is how you will implement a observer. You will see that we also have another observer here called as Quantity Observer. And this is again implemented in exactly same manner as we implemented Price Observer here. However, we are checking what is the count or what is the number of items in this order and depending upon the number of items, we are setting the shipping cost. So we need to provide another field here that will record the shipping cost. So I'm going to create a simple double here. And let's generate the getter setter methods for this newly added shipping cost field. All right. And our quantity observer is now able to set the shipping cost based on number of items in that order. One final thing we will do is that while calculating total, we need to add the shipping cost. All right. Should be will be added. One last thing I'm going to do is I'm going to generate a two string implementation for our. Order method so that we can print it. All right, I'm going to remove the. Observers from this list. All right. So once again, we added a new field for our quantity observer, and that observer is updating the shipping cost. We added the shipping cost to the order total because that will be additional cost. And we generated a simple two string method so that we can print our order in our client code. So let's jump to our client and let's start using these observers. So I'm going to create an order first. So let's create a new order. And let's give it a value ID of 100. Now what we're going to do is we are going to create our price observer. All right, let's call it price. Create new object price Observer. Now we need to attach our observer to the order object because then and then only it is going to get notification. So we are going to do is order dot, we're going to call the attach method and we're going to pass the price observer here. All right. So this is the line where we are creating the relation between observer, which is price and the subject or observable, which is the order object. So we have our Price Observer. Now let's add some items here. So let's add just a simple item which is worth $50. And what I'm going to do is I'm going to print out the order details now after adding an item which is only $50. So let's run this code, run as Java application and you will see that we have a order here. We have ID of 100 item cost is 50 and you will see that we don't get any discount here. Okay, so discount is zero. Now let's add another item here. So I'm going to add another item, which is, let's say $179. So that is going to cross 200. Let's run this code once again. All right. And you will see that we get a discount of $10. So this is how our observer is working and it is listening. Whenever we add an item, it's going getting a notification on each notification it's checking what is the current value of the cost. And depending on that value, it is updating the discount amount that is given to customer. All right, So this is how you can implement and use observer design pattern in Java.
