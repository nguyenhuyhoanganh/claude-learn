# Observer - Comparison with Mediator

All right, Now let's compare our observable design pattern with mediator. Now, we have already seen these differences when we were discussing mediator design pattern. These are quite similar differences here. But now you have an advantage that you are now familiar with, both observer and mediator, and it makes sense to compare only those design patterns which are somewhat similar. There is no point in comparing Observer to Memento just for the sake of having a different comparison. Okay. Observer and mediators are frequently complementary. That means you can you are going to choose one of these if you are solving a problem related to object communication. So whenever there is a collaboration between objects, then depending upon the pattern of communication, whether we have one to many communication or many to many communication, you are going to choose either observer or mediator. So we are going to see differences between these two design patterns here to help you choose in a better way. So first difference is that observer provides one to many communication pattern or relationship between objects. So we have a single observer observable, which notifies multiple observers when its state changes. Mediator, on the other hand, defines or it represents many to many communication. So when an object change changes its state using mediator, it is going to notify many other objects. And if any of those objects change, changes its state, we are going to get a notification. So mediator is for many to many communication pattern observer is for one to many communication pattern. In observer. The communication is simple and we can even describe it as a publish subscribe model of communication in mediator. Communication is not that simple. We have this web of communication pattern that is present in this group of objects. All right. So these are some differences between these two design patterns.
