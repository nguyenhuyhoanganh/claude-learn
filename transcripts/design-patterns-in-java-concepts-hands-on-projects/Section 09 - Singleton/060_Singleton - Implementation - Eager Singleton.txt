# Singleton - Implementation - Eager Singleton

All right. It's time to jump into Eclipse and start implementing Singleton design pattern. So first of all, we're going to start by implementing Eager Singleton. Now, this is one of the simplest way that you can implement Singleton design pattern. As we discussed, the requirement for Singleton design pattern is that we want to make sure that there is only one instance of our class ever created. That means we want to prevent anybody from creating instance of our class. And to do that we are going to declare a constructor in our class, which is a private constructor. All right. And this will take care of inheritance as well. So since we have a private constructor we cannot inherit from eager registry. Next up, we want to create our singleton instance. So to do that, we are going to declare a private static final field, and it will be of type eager registry, which is this class itself. I'm going to name this variable static variable as instance. And on the same line we are going to initialize our singleton instance. All right. So this instance variable now holds the only instance that we are ever going to create. Lastly, we need to provide a publicly accessible static method that returns this instance to outside world. So I'm going to say return. Instance. All right. Sorry. The method name is get instance. All right. So anybody can call our get instance method and get access to our singleton instance. To check this, I'm going to go to another class called as Client. And we have a main method here. So to do that, first of all, I'm going to make sure that I cannot create instance of this class using the constructor. All right. And this is because the constructor itself is not visible or since it is private, we cannot access it. So how should we get an access or get hold of our singleton instance that is using the static method that we have provided called as get instance? All right. And this is the only way that we can get hold of our singleton instance. Now we can call this get instance method multiple times, and each time we are going to get the exact same instance. So I'm going to call get instance again. And if we print the sorry, if we print the result of equals Operator So I'm going to use the equals operator. And as you can, as you already know, equals operator checks the references and it will return true only if both these references point to the exact same object. So if I run this class, our client class, you will see that we're going to get true. All right. So here we have an eager singleton implemented in Java. The only things that you need to take care of is making sure your constructor is private. Creating your singleton instance while initializing your instance static variable and provide a public static method in order to get that instance out. All right. So this was the eager singleton implementation.
