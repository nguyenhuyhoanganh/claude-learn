# Singleton - Example

All right. Now let's look at the example of Singleton design pattern in Java class library and great example of this pattern where it is actually good to have or use the Singleton design pattern is the runtime class which is present in the Java class library. So it is part of the standard Java API and this is the code snippet from that class. This code is taken from runtime class in Java file and you will see that in this class we find the typical characteristic of singleton pattern first characteristic being that we have a private constructor. Second, you will see that we have a static variable private static variable where our singleton instance will be stored and a static method which is used by the external code to get hold of that singleton instance. So you will see that the runtime class is actually implemented as an eager singleton. All right, so Java Lang dot runtime class is an example of singleton design pattern in real world.
