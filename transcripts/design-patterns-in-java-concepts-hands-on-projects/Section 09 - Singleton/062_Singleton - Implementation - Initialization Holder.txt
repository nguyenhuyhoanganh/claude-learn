# Singleton - Implementation - Initialization Holder

All right. In this video, we are going to look at another way that we can implement Lazy Singleton. In previous lecture, we saw that we can use the double check locking and use volatile keyword in order to create our lazy singleton. But there is another way that we can implement this lazy singleton, and that is using what is called as lazy initialization holder idiom. And we're going to see what exactly is that as we implement this. So to start off again, we're going to make our constructor private. Okay? So that takes care of inheritance as well as making sure that nobody can create instance of our class outside. Next up, we are again going to need a variable that holds our singleton instance. And if you remember from our previous lecture, we are we were declaring this instance variable in our class. But instead of that, what we are going to do is we are going to instead declare a static inner class and let's call this class registry holder. All right. So we have an inner class in our registry lazy registry. It is a private static class. And inside this class, I'm going to declare my variable static variable. Sorry. Instance, which will be our variable that holds on to our singleton instance. Now we are going to initialize this variable on the same line. All right. And then we are going to have our public static method as usual, that returns our singleton instance to outside world. Method name is get instance and we are going to say return register holder. So remember, we are referring to this inner class now Dot, and we are going to return the instance that is declared inside. All right. So what is happening here? Okay. Let's take a quick look. So you remember when we were implementing the eager registry that since our instance was initialized as a initialization of static variable, as soon as somebody loaded caused our class loader to load this class, then the class loader itself was going to initialize this static variable with our new instance. All right. And that's why it was called as an eager singleton, because before anybody could, anybody called our get instance method, our instance was getting created. Now, here, if you look at it, we we have not declared the static variable in our main class. Instead, we have an inner class, a private static inner class, and that has this static variable. So applying the same logic, as soon as the class loader loads our registry holder class, it will initialize this instance variable and create our singleton instance. But the only way anybody can access this class or make the class loader load this is if somebody called our get instance method. Because inside this method we are referring or making the first reference to our registry holder. And that way we are indeed using lazy initialization. So this class won't be initialized unless and until somebody calls the get instance method to prove that what we are going to do is in our constructor of this singleton, we are going to print some message. So I'm going to print in Singleton. All right. So anytime our constructor gets gets called, this system.out will be printed. Now let's jump to our client code. I'm going to remove all the existing code, and let's say I'm simply going to make a reference to our newly created singleton. All right. I'm not going to call any method on this. I'm simply going to make a reference so that the class loader now has to load our lazy registry class. So let's run this code. So I'm going to say run as Java application and I'm going to bring out my output. All right. So you will see that nothing has been printed in our console. Just to make sure I'm going to add one additional line here and I'm going to simply say print done. All right. So I'm going to run once again. You will see that we get done printed, but our constructor didn't print anything. That means this constructor was not called. Now watch what happens as soon as I call the get instance method. So as soon as I call get instance method. Sorry. I need to. Sorry. I need to call on the class itself. Get instance method. All right. And I'm going to save this reference in my singleton. Singleton is equal to sorry. Singleton is equal to lazy registry dot get instance. All right. Now let's run our class and you will see that. Now, our constructor was called because we see this line in the output. All right. And you can keep calling this get instance method multiple times. Our constructor will be called only once. All right. So this is called as. So this is called as lazy initialization holder idiom. And here we make use of an inner static class, which is private. And that way we make sure that even though we have initialized the static variable with our singleton instance, it won't be created unless and until somebody calls the get instance method. One thing just I wanted to quickly show that you cannot make reference to this inner class from outside. All right. You don't get to access this class. So if I try to say, let's say registry holder. Older, you will see that this code won't compile because it will say that the inner class, since it is private, is not visible. All right. So there is no way for anyone to refer to this inner class. And that way we get benefit of lazy Singleton. One quick change that you will see from our singleton with double checked locking is that we don't need to provide any synchronization and perform any double check locking or use volatile in order to get the benefit of Lazy Singleton. All right, so this is a singleton implementation using lazy initialization holder idiom.
