# Null Object - Introduction

All right. Now, this is the last design pattern that we're going to study. And it is very simple design pattern called as null object. So this is a behavioral design pattern because it provides a certain behavior to our objects. So what is a null object? So you will see that we typically use literal null as a value that indicates an absence of object. So while writing our Java code, you might have used Null as a value which indicates that this particular reference doesn't have or doesn't point to any object using null object pattern. We can provide an alternate representation to indicate an absence of object. So using this pattern, we don't have to perform the null checks wherever we can expect a null object to be our null reference to be present. So most important characteristics of that null object that we're going to implement is that it will basically do nothing and store nothing when an operation is called on it. So what happens in your code? If you try to remember any typical code that you write, we check that if an object or if an reference is null, then we are simply not going to do any operation on that object. Same thing is going to happen here using the null object. The only benefit is that the code which is using our null object pattern won't have to do or won't have to consider a possibility that it's going to get a null reference. Okay. Because we're going to achieve the exact same thing using null object. Now null objects seems like a proxy because it stands in for a real object. However, there is an important difference here. A proxy at some point will use real object or it will transform to a real object or even in absence of real object. For example, if we are using remote proxy. The proxy itself will provide some behavior that will have some side effect. So if we are using a remote proxy where we don't actually have a real object, the proxy still connects to a remote server and carries out some work when we call a method on that remote proxy. Null object, on the other hand, will not do any such thing. Null objects do not transform into real objects anytime. Okay, so this is the most important difference and the most important point that you should keep in mind when using this design pattern. All right. Now we use this pattern when we want to treat the absence of a collaborator transparently without using null checks. Okay. So if somebody is asking us to provide an object and we cannot provide a proper implementation or correct implementation, then we can return a null object and the class or the code which has asked us to provide that object will work flawlessly without using any null checks. Now let's look at the UML diagram of this design pattern. It's a pretty simple design pattern. First of all, we have some abstract class or a base class which has some operations defined in it. All right, then we are going to have some concrete class which is actually going to provide implementation for that operation. And then we are going to have a null class here. Okay. So null class is going to define our will be a null object here and implementation of operation in this class will do nothing. All right. So the concrete class will have the actual implementation for Operation Null class will provide or implement the operation in such a way that nothing happens when our client calls the operation on object of null class. Okay, so that's all there is to this null object design pattern. Next, we are going to see steps that are needed to implement this design pattern.
