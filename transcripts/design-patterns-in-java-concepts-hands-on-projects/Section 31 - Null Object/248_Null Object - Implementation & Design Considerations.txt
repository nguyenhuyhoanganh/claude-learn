# Null Object - Implementation & Design Considerations

All right, Now it's time to look at some implementation and design considerations when working with null object design pattern. So the first point is that the class or the code which is using our null null object, it should not have to do anything special when it is working with this object because this is the whole point of this design pattern that the code which is using our null object is not aware that this is a special implementation of the class that it is expecting. Now what do nothing means for an operation depends entirely on the context or the application that you're working with. And this is especially true where methods in our null object are expected to return some value. Okay, so if our method is, for example, expected to return an int, then you should look whether returning zero makes sense as a default value or whether returning one makes sense for the default value. Now, if you find a need where your null object has to transform into real object sometime in future, then that is a good indication that you should use something like a state pattern where we will have a state object, which is which will be a null object and that will do nothing. Okay. So as we saw in the state design pattern state, the context object delegates, the operation to its current state object so we can have our regular behavior as one state and the null object will be another implementation or another state in whichever context object can exist. Okay, so whenever you see a need that your null object is going to transform, do not use null object pattern. Instead use state pattern and have a null state there. Now let's look at some design considerations. So since null objects do not have any state and they do not perform any complex behaviors that results in side effects, they are pretty good candidates for singleton pattern. Okay, so there are not not many cases where Singleton making using Singleton makes sense. But this is one scenario where I think Singleton can be a good choice because these objects do not have any state. They do not have any implementation in their methods, which has some side effects. So go ahead and create a singleton for or when implementing the null object design pattern so that we are going to use the same instance of that null object everywhere. Now null objects are actually very useful in other design patterns. For example, the example that we just discussed in the state design pattern. So we can have a null object as one of the possible state values. And whenever object context object is in that state, any operation called on that will not do anything. So if you remember from our implementation of state design pattern, we had this order which our customer can cancel after cancellation. Our order was going into canceled state. So that cancel state is a good example of null object design pattern. So once our object is canceled, we do not want anyone else to touch that object until cancellation is done. Okay. So we do not allow anybody to cancel cancellation. That means if somebody calls cancel, it should not. Nothing should happen. If somebody says our object is dispatched, that is not a valid state for canceled order. So that is one example. Another example is that inside strategy design pattern. We can have a null object that represents basically a null strategy. That means whatever is given to us, we are going to return that as it is and you will see that we need these kind of implementations to represent a special scenario related to our input in strategy design pattern. All right. So there are multiple applications of this null object design pattern in real life.
