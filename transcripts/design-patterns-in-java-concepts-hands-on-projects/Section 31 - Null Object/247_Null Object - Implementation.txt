# Null Object - Implementation

All right. Now it's time to look into implementation of the null object design pattern. So I'll switch over to Eclipse now. This design pattern is quite simple to implement and there is nothing to it. As we discussed in the previous video where we saw the example UML, we are going to use a couple of classes to save us some time. So we'll go over these classes one by one. So first of all we have a class here called as Complex Service, and this is a dummy object or dummy class that is going to simulate that. We are creating some kind of a report. So it has a method called as generate report. And here we are going to simply sleep for three seconds in order to simulate that. We are doing some work here. And finally we are going to save a file called as report file using the storage service. So you will see that in the complex service constructor here. We need an object of storage service. There is another constructor here that expects a report name as well as storage service. So once we have that storage service in the constructor, our generate report method is going to call the same method on that storage service. So now let's look at the storage service implementation. This is again a simple class. Here we have a save method that expects an object of report class. Here we are simply going to print out that we are writing a report and then we are going to use a print writer and write out that particular file with TXT extension and we are going to simply write the report name again in that file and finally be done with that report. All right, So this is our implementation here. And if you look at our client client is going to create a very complex service. It's going to give some report name and it's going to create an instance of storage service and then call the generate report method on that complex service. So this is how our application typically works. Now let's say we want to implement null object pattern here. So to do that we are going to create a new class. We are going to call that class null storage service. So to implement a null object, the first thing that you have to do is you have to provide the exact same interface as is expected by the user of this class. So complex services our user for our null object. So it is working on the storage service interface or whatever is exposed by that class. So we are going to extend from that storage service extends storage service and I'm going to override all the methods that are present in that storage service. So remember, we are not simply going to override any abstract methods. We are going to override every method from our base class in the null object so that we will not provide any implementation. And whenever null object is used by our complex service, it won't do anything. So here we have an empty method to indicate that our method was called. I'm simply going to print out a message here that null object save method and we're going to say doing nothing. All right. And that's the reads. That's all there is to it. Null object implementation. So we will extend from your existing abstract class interface or even a concrete class override all the methods and basically do not or not do anything in those implementations. Lastly, I wanted to show you what is present in the report class. There is nothing to it. We simply have a single field called name and we're going to accept that name in the constructor. And we have a getter here. So let's go back to our client and let's run this particular class client class as it is. So here we are not using a storage service here. Okay? So if I run this class as a Java application, you will see that we get a message saying that a complex report is being generated. It is saying that done with the report and it is saying that it is writing out that report. So if we look at the. Sorry. So if we look at the project structure, so I'm going to refresh my hierarchy, you will see that we get a simple, simple report dot txt file that is created as a result of running this application. Now let's replace our real object with null object. So we are simply going to give it an object of null storage service and our complex service is not going to behave or is not going to handle that object differently. Okay, so this code works regardless whether we are passing a real object or a null object. Now coming back to client, let's run this code now. So I'm going to change report name here. Let's say simple report two and say run as Java application. We are again going to get a message from our complex service that it is starting a generate starting generation of the complex report. It's going to say done with the report. However, at the end you will see that we get a message that null object save method is called and we are not doing anything. All right. So if if you check the file system, you will see that no new file is now created and that's all there is to implementing a null object. The problem with this design pattern is not the implementation or that is not the main concern when using this design pattern. The main concern or main point that you should remember when using null object is to how to define do nothing operations. Because if our method was complicated and if it was returning some other object, then it would have been a lot more complicated to implement this null object. All right. But since we are learning about this pattern for the first time, we have kept things simple.
