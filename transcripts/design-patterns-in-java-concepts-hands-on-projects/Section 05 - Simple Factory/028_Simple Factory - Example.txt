# Simple Factory - Example

All right, Now let's look at some real world examples of Simple Factory and you will find that there are no problems finding examples of simple factory in almost every application or every framework out there. In fact, I would like to warn you that many examples that you will find online about factory method are in fact examples of simple factory. So let's look at one of the examples. So we have a class here called as number format, which is present in Java's text package. Now this class has a gate instance method, which is an example of simple factory. So let's look at the code of this class. So this code is taken from the number format dot class file in the runtime dot jar file which comes with your JDK. So you will see that we have here an get instance method, which is a static method that is used internally by the number format class and you will see that we have our familiar code snippet here. We are checking an incoming parameter for our simple factory method and we are based on value of that choice. We are going to instantiate a particular class and then we are going to return that class to the outside world. All right, so number format class is an example of simple factory.
