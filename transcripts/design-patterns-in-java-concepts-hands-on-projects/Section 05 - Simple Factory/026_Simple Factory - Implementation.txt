# Simple Factory - Implementation

All right. Now we're going to see how we can implement Simple Factory in Java code. So I'll switch over to Eclipse and you will see that I have written a couple of classes that we are going to use while implementing Simple Factory. So I will go through these classes one by one and explain what their purpose is. So first of all, we have a post class now. This is our abstract product class. So this post represents an article that is published on a website. Now there are multiple subclasses of this post. We have a news post and this class represents a news article that is published on our website. On similar lines, we have a blog post which represents an article in our blog that is published. Then we have a product post which represents a product information page on our website. So Post Factory is a class where we are going to implement a simple factory. Now, in order to implement a simple factory, we are going to start by creating a static method. Public static. And we are going to return an object which is subclass of our post class. I'm going to name this method as create post. Now we have discussed that while declaring simple factory or while implementing simple factory, we need a criteria, a variable whose value decides which object we are going to create. So I'm going to accept a simple string argument string parameter here called as type and based upon value of this type, we are going to instantiate one of these three subclasses of post. So I will start by writing a switch statement and inside this switch statement I'm going to compare value of our type parameter and in case the value of type is blog, we are going to return a new blog post. Okay. On the similar line, if the type variable is set to news, then we are going to return a new news post. All right. And you will see that how easy it is to implement a simple factory. And lastly, we are going to say that if the type is product, then we are going to return new product post. All right. So this is how we can implement a simple factory in case we get a value that is not known to us. For example, then we are going to add a default case here and here we can simply say new illegal argument exception. And we are going to provide a message saying that. Post type is unknown. All right. And this is how you can implement a simple factory. Now, in order to use this, we are going to create a new class here. Let's call this class client. And we are going to have a public static void main method here. Now, inside this main method, we are simply going to use our simple factory. So we are going to say we want a post and we are going to use our post factory class and use its static create post method. And let's say we are going to check for blog. Then we can print whatever object is returned to us. So if I run this class, run as Java application, you will see that I get an object of blog post. If I change my type to news and run my class once again, you can guess we are going to get a news post. All right. So you get the idea how we can implement a simple factory in Java.
