# Abstract Factory - Example

All right, Now let's look at some real world examples of abstract factory design pattern. So the document Builder Factory, which is part of Java API for XML, is a good example of abstract factory design pattern. However, there is one issue with this example in that this class implementation doesn't match 100% with the UML diagram that we have seen in the previous slides. Because this class has a static method called as new instance method, and that method returns the actual factory object that is used for creating different products. So maybe wondering if this is a static method that means it is going to have some static definition or static code which determines that these are the possible examples of factory, but that is not the case. The new instance method uses classpath scanning, it uses system properties. It will also check for a particular property file in order to find the actual implementation of factory. And using that information it will instantiate that particular class and return us. So in this way we have the ability to change the actual factory that is used by our client code. And I maybe, I'm sure that you are a little bit confused. So what we are going to do is we're going to jump into Java code and see what this document builder factory is so that you will understand how it is an example of Abstract Factory. All right, So I'm now going to switch over to Eclipse so that we can see this document builder factory in action. So we have a simple class here called as Client and we have a main method. We are only using the classes which are given to us by the Java API. We are not using any custom classes in this example. So if you look at the code, you will see that we are first going to call the new instance method on the document builder factory. Now new instance method. If we go into the source code, you will see that this is a static method. Okay? And this is what I was talking about when we when I said that new instance is a static method. However, we still can change the actual factory that is being used. So document builder factory dot new instance is going to return us a factory object. And using that factory we can build a document builder and using that document builder we can create a document. So document Builder Factory is an abstract class. I will jump back. The document builder that we get from the factory is also an abstract class, and the document is actually an interface which is created by the document builder. Okay, So right now if we what I'm doing is I'm simply going to print the actual class that is returned to us by these methods. Okay, So we are not actually populating any data. We are only checking which objects are given to us by as a result of calling these methods. So I'm going to simply say run as Java application and you will see that we have an output here. You will see that for our factory class we are getting document builder factory impl from the Jaxb package. So this is the implementation of the document builder factory that comes with JDK. All right. And since we are using this factory, we are going to get a document builder from the same package and then we are going to get a document impl from Dom's package. So these are all implementations which are provided by default in Java class library. Now if we want to use a different factory, okay? Because remember in Abstract Factory we can change the factory class and we will get a different set of classes which work together. Okay. So here the document builder and the document these are the classes which work together. So they form a set or a kit to change the factory. We have to modify the class path of this example. So I'm going to switch to the run configuration and in the class path in the user entries, I'm going to add some jar files. So I'm going to use a third party library called as Shiny Parser. And I'm going to add these three jar files in my classpath. Okay. So I have simply added these three files in my runtime class path and I'm going to say apply and remember, these are the classes which we got if we had not configured the classpath. And if I run this same code again, you will see that I get a different factory. We get C document builder factory, we get C shiny dom parser and C dom. To HTML document. All right. So this is how we can change the factory. That is return to us, returned to us by the document builder factory, new instance method. And since this is an example of abstract factory pattern, once we get a different implementation of factory, whenever we create an object using that factory, we are going to get a different product. Okay, so this is an example. The document builder factory in Java API for XML is an example of abstract factory design pattern. All right. So coming back to our presentation, we saw that document Builder Factory is an example of abstract factory design pattern. Now, in the document Builder factory, the way in which we configure a different factory for our client to use is slightly different. We use a path, or we can set a system property in order to configure our client with a particular implementation of factory class. But even so, this is an example of abstract Factory.
