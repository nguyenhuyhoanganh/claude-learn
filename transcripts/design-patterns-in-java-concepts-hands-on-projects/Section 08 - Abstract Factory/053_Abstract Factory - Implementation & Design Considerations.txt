# Abstract Factory - Implementation & Design Considerations

Now let's look at some of the implementation and design considerations while using Abstract Factory. One point that you may have noticed is that we can implement our factories, concrete factories as singletons because we typically need only one instance of our concrete factory and we can use that instance everywhere because they don't have state of their own. But before you do that, make sure that you are familiar with Singleton design pattern and many of its drawbacks. Now, whenever you want to add a new product type, for example, in our storage, in our example, we used instance and storage. Let's say we want to provide or provision a network that requires changes to our abstract factory and all implementations of that abstract factory. All right. So you should be familiar that adding a new product type requires changes to all these factory classes. Why using abstract factory, we typically provide the client code with the concrete factory. So we pass on the object of one of the concrete factories and then the client code can use that factory. Now let's look at some of the design considerations. So whenever we have a situation that we want to constrain which type of objects are created in our application, and the reason for that constraint may be that these objects can work together or they form a family or set of objects. In that case, you can use the abstract factory method. Now Abstract Factory itself uses factory method pattern. So the methods that we provided like create instance and create storage. These are examples of factory methods which are implemented in subclasses. If the objects that we are creating in our concrete factory, if those are expensive, then we can transparently switch how our factory creates these objects. So we can use a prototype or we can use a singleton inside our factory. All right. So these are some of the points that you should remember when using the abstract factory.
