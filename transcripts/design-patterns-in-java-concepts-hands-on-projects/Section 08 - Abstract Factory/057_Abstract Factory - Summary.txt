# Abstract Factory - Summary

Let's look at a summary of abstract factory design pattern. So whenever we have a set of objects that work together and they form a family or set, then we can use an abstract factory to create those sets of objects. So our client code doesn't have to worry that it is creating the correct object. Our abstract factory implementations take care of creating only those objects that belong to one set or one family of products. Now Abstract Factory itself uses factory method design pattern, and you can think of Abstract Factory as a class which has multiple factory methods defined in it. Adding a new product to our existing implementation of Abstract Factory requires changes to the base class or our interface of the abstract factory as well as well as all of the implementations of that interface. Now concrete factories can be singleton, and the reason for that is because we are going to need only one instance of an abstract factory, and typically they don't have any state within them. We provide the client code with the concrete factory instance. And one point that you might have noticed is that we can change factories at runtime so we can initialize our client with one instance or one implementation of our factory and at runtime based upon some criteria, we can switch that factory out and our client can start creating objects from a different product family. Now let's look at the UML diagram of Abstract Factory. So we have an instance interface here which is an example of an abstract product class. And then we have a storage which is another abstract product class. We have two implementations, for instance, EC2 instance as well as Google Compute Engine instance for storage. Again, we have two implementations. The point here, important point to note here is that EC2 instance, which is a child class of instance and S3 storage, which is a child class of storage. These two classes or objects of these two classes work together. So these objects form one set and Google Compute Engine instance and Google Cloud storage form another family or another set of classes or objects that work together. Then we have Resource Factory, which is our abstract factory. And this abstract class or interface defines factory methods to create objects of instance and storage. We implement our abstract factory in one or more concrete classes. So here we have Resource Factory, which provides implementations for the factory methods that are defined in our abstract factory. And these concrete implementations of factory take care that they are creating objects that belong to one single family. So Resource Factory will create instance of EC2 instance class and it will, when we call, create storage on our resource factory, it will create S3 storage. All right. So the logic of making sure that the objects that are being created belong to same families handled in our concrete implementation of Abstract Factory. All right. So this is the UML of our abstract factory design pattern. Now let's look at the implementation of Abstract Factory. So here, using the UML diagram that we just saw here, we have two interfaces that represent the products that are used in our application. So we have instance and storage. Then we have these set of classes which belong to a family. So we have EC2 instance, which is type of an instance and S3 storage, which is type of storage. And these two classes belong to one family called as Amazon Web Services. On the similar lines, we have implementation of instance called as Google Compute Engine Instance and Google Cloud storage, which implements storage. And these two classes are objects of these two classes belong to one family called as Google Cloud platform family. Then we have our abstract factory. In Abstract Factory we define these methods called as create instance, create storage. And these are actually the factory methods that are provided or defined in our abstract factory. Then we provide concrete implementation for our abstract factory. For example, we have AWS resource factory, which creates instance and storage objects which belong to Amazon Web Service Product Family and Google Resource Factory will create objects that belong to another family. Now, in order to use our abstract factory, what we do is we create our client with a concrete instance or instance of concrete implementation of one of these abstract factories. So we give it an instance of resource factory, and the client will keep using this factory to create objects that it needs. All right. So this is all about the abstract resource factory design pattern.
