# Abstract Factory - Implementation

All right, Now let's look at the implementation of Abstract Factory design pattern in Eclipse. So I'll switch over to Eclipse now, and as usual, I have implemented some of the classes that we are going to use while implementing an abstract factory. So these classes are exactly similar to what we discussed in the example UML that we saw in previous lecture. But I will go over these classes so that we have better understanding of how we are going to use them. So first of all, we have an instance interface and this class represents a compute resource that is available from cloud providers like Amazon and Google Cloud platform. So this instance interface defines some generic methods that can be called regardless of which implementation we are going to use. So we have an instance interface and there are two classes which implement this interface. We have an EC2 instance which represents a compute resource on Amazon Cloud. Of course there is nothing inside it that actually connects to AWS, but you get the idea on the similar lines. We have Google Compute Engine resource, which is again a compute resource and it implements the instance interface. Then we have a storage interface and this class again abstracts the cloud storage that is available from multiple cloud providers. And implementing this interface we have S3 Storage, which is on Amazon and Google Cloud Storage, which is available on Google Cloud platform. All right, So these are some of the classes, sorry. So these are some of the classes that we are going to use in our abstract factory. So here we have resource factory and this is the interface which represents our abstract factory interface. So we are going to add methods here that create objects of these types. So we have a create instance method that is going to return any one of child classes or any one of the classes which are implementing instance. And we are going to accept a capacity of the instance, for example, a large or micro instance, then we are going to have another method that creates storage on the cloud platform and we are going to take our capacity that is needed to be provisioned on the cloud storage. Now we are going to implement this interface for each of our providers. So I have already written AWS resource provider, AWS Resource Factory. So this class implements our abstract factory. So I'm going to implement methods called as Create Instance and create storage. And this class is simply going to create a new EC2 instance and we're going to pass on the capacity. And for storage, we are going to create the object of S3 storage because this is the storage that is available on AWS cloud service. All right, So this is our AWS resource factory. We also have Google Resource Factory. So this is another concrete implementation of our abstract factory. And here we're going to create objects that are available on Google's cloud platform. So I'm going to say Google Compute engine resource with the given capacity. And as to the storage, we are going to say. Google cloud storage with the capacity that's given to us. All right. So this is all there is to it. Now we have two concrete implementations of our abstract factory. And these classes, these factories create classes or objects which are related or which are meant to be used together. So if we are using Google Compute Engine Instance, we want Google Cloud storage. Now let's jump into the client and see how we can use our abstract factory. Now, one typical usage you will find when working with Abstract Factory is that we compose classes using the concrete instance of our abstract factory. You will get an idea when I actually type it. So inside this client class I'm going to declare a private field which will be of type resource factory. Then I'm going to sorry. Then I'm going to declare a constructor of our client, which. Accepts a factory resource factory implementation, an object of resource factory, and this is going to simply store that reference. All right. So now we have our client class, which is composed with a concrete implementation of resource factory. Now, inside this client class, I'm going to write a simple method called as public instance and create server. So we are going to give it a capacity and. Let's see the storage that we need in megabytes. So this method create server is going to provision the compute resource as well as storage. And both of these will be created on a single cloud provider based upon the factory instance that we actually provide to it. So let's create something here. We are going to create an instance and we're going to use our factory factory dot create instance. We're going to give it the capacity. Then we are going to provision a storage and we are again going to use the factories, create storage method with the capacity that we want and let's say to simulate that these objects work together. We're going to call the attach storage method on our instance. All right. And finally, we're going to return our instance. All right. So attached storage is a method that is defined in instance. But you will see that I haven't defined that method in the. Sorry. So I have defined this method in the instance interface and it accepts a storage. There is nothing to it. In order to use this client, we are first going to create an object of our client, let's say AWS, and we're going to create new client object and we're going to give it AWS Resource Factory. All right? And let's say let's say we want to create an instance called as Ivan. We can say AWS, create server, let's say capacity of micro, and let's say we want 24, 80, 20 and 20 GB. All right. Now we can call the methods on this storage on the instance that is written to us, stop. And if we run this class using run as Java application, you will see that we get a message saying that it has attached S3 storage to an EC2 instance and we get a message saying that EC2 instance is started and stopped. So you see we have an S3 instance that is created when we called factory dot create storage and EC2 instance. So these two objects, EC2 instance and S3 work together and this is the use of using abstract factory just to make sure we are also going to create now our client object, another client object using Google's cloud platform. So let's say new client. And we're going to give it new Google. Resource factory. Okay. And we're going to I'm going to copy this code here. And I'm going to simply change it. All right. And if we run this once again, you will see that we first get EC2 instance and S3 as as it was the case previously. And then you will see that we're going to get. Oh, sorry. Uh, I made a mistake. Sorry. I need to call. This is the drawback of using copy paste. Okay, so I'm going to call run this program once again. So first of all, we. We get classes which are related to AWS, and at the bottom you will see that we are going to create the classes or objects of Google Cloud platform. All right. So this is how you can use an abstract factory and implement it in Java.
