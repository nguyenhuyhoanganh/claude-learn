# Composite - Implementation

All right, Now it's time to jump into Eclipse and see how we can implement composite design patterns. So I'll switch over to Eclipse. Now, I have written the abstract component class that we are going to use, so I will explain what that class is doing. So here we have an abstract class called as File, and this will be the component in our composite design pattern. That means our leaf node and the composite node is going to extend from this file class. In this file class, I have a single field called as Name. We have a constructor that accepts the file name here. We have a getter method here and the setter method. Then we have defined an abstract operation called as LHS. So this is the operation that our client code is actually going to be interested in. I have added the children management operations, which is Add file, get files and remove file which are only applicable to composite here, just to show you how you would implement such a type of implementation of composite design pattern. So let's jump to binary file and this will be our leaf node in the composite design pattern. So let's implement binary file. So we're going to extend from our file class, which is our component. Uh, in the binary file, I'm going to add a property called as Size. So this will represent the size of our file. Okay. Then I need to declare a constructor. So I'm going to say binary file, and we are going to accept a name for our binary file and the size information we are going to pass on the name to our base class. And then we're going to set up our size, which is equal to whatever is given to us in the constructor. Next, we need to override all of these methods because they are abstract methods. So what I'm going to do is add file, get files, list, remove file. So these are the methods that we are going to override in order to make this class non abstract. So what happens in the operation? Well, we are simply going to provide a name of our file. So I'm going to say get name and I'm going to append the size information for this particular file. Okay. So whenever somebody calls us on our binary file, we are going to print the file name and print size. Now we have these operations called as Add file, and this is not applicable to our leaf node. And this is the problem that you will face if you move all of your operations into the base component. So add file has no meaning for this particular class. Okay. But since we have it, we are going to throw an exception called as unsupported Operation Exception and we are going to print our pass on the message saying that the leaf node. Doesn't support Add operation. Okay. And this is what we are going to do in all of these methods which are actually meant for our composite. So git files, we are going to say we don't support the git operation and we are also going to say we do not support the remove operation. Okay. And this is how your leaf node is going to look if you add your child or child management operations in the base class. Okay. Whether this is correct thing to do or not, I will leave that decision up to you, but you will end up with classes which have these methods which should not be there. Anyways, moving on, we have a directory class, so this is our composite. So in order to implement this we are again going to extend from our file class, which is our base class. And here instead of size, what we are going to do is we're going to maintain a list of. File objects and we're going to call this collection children. Okay, So this is the composite class in our composite design pattern. I'm going to initialize it with ArrayList all as always. So we will we will have an empty directory. If we create a new directory. It won't throw an exception since the since there are no files in it. Next up, we need to implement our constructor, so I'll move it one line down. So we're going to have a constructor here called as directory. We only need name of our directory and that too we are going to pass on to our base class using super name. Next up, we need to provide the implementation for the abstract operations that are defined in our base class. So I have just generated this stub. Now let's look at the operation here. So what we want to do is we want to call this operation on all of our children. So if you do an LS on a directory, you will see that we get the information of files which are present in that directory. Okay. So to begin with, I'm going to simply say that we are going to print name of our directory and on the next line we want to print information of our file. So I'm going to use the children dot and I'm going to the going to use the for each operation which is added in Java eight and we're going to give it the operation. Okay, so what's this going to do is that it's going to go over the children collection in our directory class and call LS method on each of our children. Okay. Next we have the Add file method. And this is simple. We are simply going to add. Whatever is given to us in our collection of nodes. Similarly, when somebody calls the get file method, we're going to say return. We need to return an array. So I'm going to say children dot to array and we are going to give it an array of. File and the size of this array, I'm going to make it equal to the children size. Okay. And lastly, we have an operation called as remove file. So we are simply going to use the remove operation that is available on the list class. And we're going to say remove file. Okay. Of course this works if we have correctly implemented the equals method. Okay. I'm going to leave that for now. But this is how you would implement your composite. So we would have a collection which contains the child or children, and then we provide the children management operations here. And in our actual operation, we are going to delegate to our children. Okay, So now let's see how we can implement or use this composite design pattern in our client code. So what I've done here is I have written two private methods that create our tree structure. And one thing that you should note is that this design pattern doesn't say anything about how you're going to create your tree. Okay, So what I've done here is I have created a couple of file and directory objects and I have created added these file objects into one of the directory objects. Important thing to remember here is that I'm returning an object of directory. So dir two is an object of directory. So this method will create a tree of objects and it will return a composite. The second method create tree two. It returns simply a leaf node. And what we're going to see here is that let's say I have a file class and I'm going to call this object root one and I'm going to say use the create tree one method, all right? And I can invoke the operation on this particular node. Now, if I run my class, you will see that we get an output here saying that we have a directory two and then we have these many files in the directory. Two we have this file in directory one. All right. So our client was able to use our composite. Now, if we use or if we use the second method that we have written. So I'm going to call create tree two. So this is the second method that we have written. And if you remember, this method returns a binary file, which is our leaf node, but still our client is able to call the method on it. And if we run this program, so I'm going to add some separator here so that we can differentiate in the output. All right. And if I run this code now, sorry. Run as Java application. You will see that we simply get the output of that particular file. Okay, so this is what we mean, that our client should not worry about which object it is using. It is still able to call the operation on it and get the desired output. All right. So this is how you can implement a composite design pattern in Java. Okay. In this approach, what we have done is we have added the children management operations on the file. But you can also remove these operations from here and keep them only in directory. Both ways are valid. Do what you feel is the correct way.
