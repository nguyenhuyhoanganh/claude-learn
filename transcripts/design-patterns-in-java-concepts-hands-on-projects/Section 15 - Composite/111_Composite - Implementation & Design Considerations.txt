# Composite - Implementation & Design Considerations

Now let's look at some of the design and implementation considerations when working with composite design pattern. So one point is that you can provide a method in your class that allows you to traverse back to parent of that node, and this will make the traversal of your tree structure easy. Again, it is up to you whether you need this convenience in your class or if you are not traversing your tree structures at all, then it doesn't make sense. However, there is an option for you that you can add a node. Sorry, Add an operation that allows any node to give back its parent on the similar lines. You can have the collection fields which hold the children of composite. You can define that field in the base component and that will make it somewhat easier to provide some default operation implementations or default implementations. However, the leaf node is again not going to need that collection field. Okay, So again, it's up to you where you define that collection field. Now last point is suppose you have a tree structure in your application and that has thousands of nodes in it. And if you find that some of these nodes can be cached, that means we have a node which is immutable and it is repeated several hundreds of times in our tree, then you can cache that particular node. So you will have one object and which is referred from hundreds of times or hundreds of places in your tree structure. So that will save you on to some memory. But again, it depends on how many number of nodes you have in your tree. If there are very few, then caching is actually going to cost you more in terms of performance. Okay, but this is an option. The point of these slides is to highlight some of the points that you can take into consideration while you're working on your specific problem. Now let's look at the design considerations. And again, I'm going to highlight here the point that I made while we were discussing the UML for the composite design pattern. And it is where the operations which deal with change management or whether where to put those operations. All right. So that is the decision which needs to be made. The design pattern suggests that you should have it in component so that the client doesn't ever need to know that there is a class called as composite. However, there is another viewpoint that says that we are forcing our leaf node to provide implementation for a method which it doesn't need or it doesn't make sense for a leaf node to have that operation in it. So again, it's a decision that you need to make based upon your preference and based upon the problem that you are dealing with. Overall, the goal of what this design pattern is, overall goal of this design pattern is to make sure that the client code or the code which is working with our tree structure, is able to work without doing any casting up, casting down, casting, whatever you need. So it should not have to deal with those type of issues. That's the whole purpose of composite. If you can achieve that, then the design pattern is implemented or fulfilled its purpose. Okay, that's the end goal here.
