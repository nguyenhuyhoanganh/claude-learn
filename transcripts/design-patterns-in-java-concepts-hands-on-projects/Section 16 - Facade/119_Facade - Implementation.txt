# Facade - Implementation

All right, let's jump into Eclipse and see how we can implement the facade design pattern. All right. So again, we have I have gone ahead and implemented some of the classes that we are going to use in this example. So in the email package, you will see that we have multiple classes which are part of our email subsystem. All right. And using these classes, we are going to send email. Of course, these classes don't do anything much, but they are placeholders in order to indicate the amount of classes that you would typically interact with if you wanted to send an actual email. All right. So if you look at our client code, which is using this system, we have a main method here and we want to send an order email. So we have written a written a method in our client which interacts with these many classes. So it uses template factory to create first a template, then it uses stationery factory to create stationery object. Then it gets email builder and builds the actual email using the template and stationery, as well as the order object for which we want to send an email. It finally builds that email object, uses Mailer and sends that email and it returns whether the email was sent successfully or not, and it prints an appropriate message. If you even look at the import statements that are needed in this class, you can see that we have a tight coupling between our code and all of these classes, which are part of our email subsystem to solve this problem. So I'm going to close other windows. So to solve this problem, we are going to implement an email facade. So here I have created an empty class where we are going to implement our facade design pattern. So to begin with, we don't need to implement any interface or any extend from any class while writing facade. You are in control of how you are defining the operations. The only goal here is to make it simple for our client code to use our subsystem. So what I'm going to do is I'm going to define a same method called as public Boolean, and we are going to name this Method Saint order email, which accepts an order object. And using this order object it can send an email. Now I'm going to cheat cheat a little bit and I'm going to take the code that is needed to send email from this private method and I'm going to stick it here. All right, so that you won't have to see me typing all of these things together, because our focus here is how email facade makes it easier for client code to use our subsystem. All right. So we now have our facade ready. It's as simple as that. Now we are going to jump into our client code, so I'm going to simply comment out this method. All right. And I'm going to organize my imports. And instead of using method, which was defined here, what we are going to do is we're going to create an object of email facade, sorry, facade, equal to new email facade. And using that facade object, we are going to send the email. All right. So sorry. The method that we are going to use is send order email. All right. And this is what is needed for the client to send an order email. If you look at the import of this, you can quickly see that we are now only interacting with a single facade class from our email subsystem. All right. And we can still get the exact same result that we were getting when we were using the classes ourselves. And this is the benefit of the facade design pattern. It makes it easy for the client to use our subsystem and it also avoids tight coupling with all of these classes, which are part of our subsystem. All right, so this is it. There is nothing to implementation of a facade. Only goal is to make sure that you are sticking with the use cases and sticking with the point that it should make life easier for the client code.
