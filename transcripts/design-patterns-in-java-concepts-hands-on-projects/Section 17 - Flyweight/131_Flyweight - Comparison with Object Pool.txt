# Flyweight - Comparison with Object Pool

Now let's compare flyweight with object pool. And you may have noticed there is some similarity in the purpose of why these design patterns can be or need to be compared. They tried to solve kind of similar problems, but there are some differences between them. Flyweight Actually, the state of the flyweight is divided. We have an intrinsic state which is actually stored in the object and an extrinsic state, an object which is part of an object pool. However, has all of its state contained within that object. We do not store object state outside if you're if we are using object pool. Now, in typical usage, the client will not change the intrinsic state of flyweight. And that's the reason that we are able to share that single instance in multiple contexts. Object pool. However, since it is a separate object, clients will change the state of that object and use it as they would any regular object. Okay, so there is no change for the client. If we are using object pool for flyweight pattern client has to take care of storing or calculating the extrinsic state for the flyweight shared object. All right. So these are the this is the comparison between flyweight and object pool.
