# Flyweight - Implementation

All right. It's time to jump into Eclipse and see how we can implement a simple flyweight design pattern. So I'm going to switch over to Eclipse now, and I have written a few classes already which we can use while implementing this design pattern. So first of all, we have an interface called as error message, and this is going to be the interface which is used or implemented by all flyweight classes. All right. It has a single operation defined as get text and it needs a string which represents the error code. So the idea here is that in our application we are going to need a lot of error messages to be shown to the user, especially if we have a web application and there are multiple users who are accessing our web application. Then for any error that a user encounters, we may need a class to represent the message that is shown to user. All right, so this is what our hypothetical problem is in order to implement our flyweight design pattern, we are going to now jump into another class called as System error Message. So this is an example of a concrete flyweight which is shared in our system to begin with. We are going to say we are implementing the error message. Sorry. Error message interface that we have defined. I will quickly generate the method stuff for the only method in this error in this interface called as get text. All right, so now we are designing our flyweight or we are implementing our flyweight. What we need. So we have access to error code. So this will be the extrinsic state that is given to us so we can use that state in order to create our final message. But we need something more than the error code and we need some kind of a template which we can use in order to generate that message shown to the user. So what I'm going to do is I'm going to declare two properties for this class, which will be our first of all, message template. And then another string which will represent the base URL for our Help document. And these are the these properties are part of our intrinsic state. That means we can share the message template property and we can also share the base URL of our Help document. Okay. Any of these two properties cannot, cannot be changed or they are not changed. It doesn't matter where this object is used. Now, in order to provide the implementation for our operation, we are going to make use of intrinsic state as well as whatever is given to us in the method parameter, which will be our extrinsic state. So what I'm going to do is I'm going to simply say I'm going to say message template. All right? And before we do that, I will just inform you what we're going to do is we are going to make use of a special string in the template which we can replace with the error code. So we will have some kind of error message, some error message. And wherever we find a string dollar error code, we are going to replace that string with the actual error code on the help URL. Our job is going to be only to pass sorry, only to pass the error code at the end of our URL. So we will have some URL like some domain.com. Then we will have help. This is the error. Okay. So this much information we are going to get in the help URL base. Our job is to append the error code to this URL. So I'm going to say dot replace. And the string that we are replacing is dollar error code. And we are going to replace that with the actual code. Then we are going to append the help URL base plus again, our sorry plus our error code. All right. So this is how you can combine your intrinsic state. So we have used our message template, which is our intrinsic state and the extrinsic state which is given to us, which is code. Okay. And. All right. So next, what we need. We need a factory which can be used to get objects of this class. So we here have error message factory. So some of the code that I have already written, I will explain. So we have a simple class here. Error message factory. We have an enum called as error type and we have defined predefined some of the errors that our system can encounter. So we have a generic system error page, not found error server error. So anytime an error occurs which is of this type, client code can ask our factory for an object that presents a readable message. For this error, we are implementing this error factory error message factory as a singleton. So here we have a eager singleton. So we have a factory instance which is initialized. As soon as we load our class we have a static method called as Gate instance, which returns the error message factory. We have a private constructor and we're going to see what this method is. Okay, so for now we need some way to cache our error objects. So what I'm going to do here is I'm going to create a private map. Uh, from the and then I'm going to say for each error type, we are going to catch an object of system error message. Okay? And I'm going to call this error messages and I'm going to initialize it to HashMap. All right. The simple HashMap, Remember, we are not going to worry about synchronization here just to keep things simple. And we are actually focusing on the design better. So in the constructor of our error message factory, what I'm going to do is I'm going to populate this map with some objects. So in the error messages, I'm going to say put and I'm going to add a generic. System error. And I'm going to say we have a new system error message. We need to define a constructor in our system error message. Sorry, which I forgot. So we have a public system error message and this constructor will accept the message template that will be used by this error message and it will also accept the help URL base which is the base URL for our uh, sorry, base URL for our error message. So I'm going to say this dot message template is equal to message template. And this start help base is equal to help earn base. All right. That's it. I'm going to go back to my error factory, and we're going to simply provide some sample message here. Okay. So I'm going to say the message is a generic. End of night. And remember, we need to provide the dollar error code. Occurred. I'm going to do I'm going to put it on. A new line occurred. Please. Can I just say please refer to. I'm going to put a new line here and in the URL, I'm going to simply say google.com is equal to whatever. Okay. Then I'm going to copy this and I'm going to add another object of page not found error. Okay. So I'm going to say. Page not found. An error of time occurred. Please refer to. All right. So you get the idea of how we can create objects of our flyweight. Okay. We can create we are creating multiple objects of flyweight. By the way, this is one important point that you should remember, that it is not a singleton, okay? Even though it is shared, we are sharing an instance of this class. It's not required that we have only one instance. We can create multiple objects and share those objects. Okay, so this is one point where a lot of people get it confused with a singleton. Why not use a singleton instead of flyweight? Well, we can create multiple instances of our single flyweight class and these objects will have different intrinsic states. Okay. And we can still share these objects whenever required. Then I'm going to provide a method, a public method, which returns one of the objects of our system error message. I'm going to say get error and we need an error type error type. And we are going to we are simply going to say return. Error messages. Gate and I'm going to pass the time. Okay. I'm not going to do a lot of validation and error checking here, but you get the idea that we have a map here and somebody can pass us the type of error message that we that he wants and we are going to return the correct type from our map. So now let's jump into our client and start using this particular object. So how do we use it? Well, simply we will say we have a system error message. Message one, and we are going to use our error message Factory. Error. Message Factory. Dot get instance, dot get error and let's say we want a generic system error. All right, then we can say this out. MSG one dot get text for let's say we have an error. Oh five, six, anything. All right. And if you run this code, run as Java application, you will see that we get a message saying that a generic sorry, I made a typo here. A generic error of type 4056 occurred. Please refer to this document. Okay, so this is how you can use the flyweight design pattern when you have shared object. Okay, so our operation in our flyweight needs intrinsic state, which is defined here and it also needs an extrinsic state. Okay. One thing to remember, we have we can have and we will have multiple objects of this class which are shared. It's not required that you have only one instance of system error message. That's not what flyweight is all about. Okay, So I told you about one additional class that we are using, so I have already implemented user man error message here to save us some typing. So this is an example of unshared concrete flyweight. So as you can see in this class, we have a completely different state. And when we are creating an object of this class, we need a case ID, okay? And depending upon case ID, we are going to create our state. So object of this state, object of this class cannot be shared. And typically you will see that in our error message factory. Wherever you have your flyweight factory, we have a separate method to get these unshared objects out of it. Just just to make clear that get user banned message requires something else as its input. All right. And you will use this in a similar way. So, for example, if I want to use a user banned error message object, then I'm still going to use the error message factory. I need to call the get instance and I'm going to say get user banned error message and I need to pass some case ID here. All right. And since this is again an object of error message, I can again call the get text method. Okay. But you will see that we still need to pass something where the extrinsic state was required, so I'm going to simply pass the null. And our object, which is unshared concrete, private, still works. Okay. So this is the message that is printed by the user banned error message. All right. So this is how you can implement a flyweight in your code. Now, I agree that this example doesn't truly bring out the brilliance of flyweight. And the reason for that is because this flyweight design pattern is really useful. If you have graphical applications, for example, like games or graphical editors, where you have this large graphics information that is loaded and that forms your intrinsic state and extrinsic state in that case can be a location. So you want to render an image at two places in your application on your canvas. Then you can really make use of a flyweight because you save loading that graphical information every time anyways. This still shows you how a flyweight, what flyweight can do and how you can implement it in your application.
