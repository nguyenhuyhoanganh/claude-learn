# Flyweight - Example UML

All right. Now let's look at the diagram of the example that we are going to implement using the Flyway Design factory. So what we have here is we have an error message and we can have thousands of error message objects in our system if it is used by thousands of users. Now, a typical error message will have a template so that we can inform our user that an error has occurred. But you will see that we can actually divide the state of our error message in two types. We have an intrinsic state. The state which is not going to change no matter what error occurs. So we have this message template. We have the help URL, which is the part of help URL which is always going to remain same. And then we have the actual error code which is going to change based upon what actual error happened. All right. So this is the object that we can define its state in two parts. So we have a message template and help URL base. So these are the intrinsic states. And then we have an error code which will be provided by the client at runtime. And using information from these two types of values, we will be able to give out the exact error message that can be shown to the client. All right, so error messages are going to be our abstract flyweight. We have a system error message, which is our shared flyweight. We can have a type of an error message which is not shared. So we have a user band error message class here. And what we're going to do is we're going to need a lot of information in order to create this error message which is not shared between two objects of this class. So we have a case number, case remarks, the duration of band. So it will depend on what violation the user has done in order to create or craft the correct error message. So we have this user band error message, which is going to be an unshared flyweight. Then we have error message factory and this factory is going to provide a message called as a method call as get error message. We're going to type accept the type of error message that the user wants. We also provide a separate method to get band error message, and we need a case number in that case. All right. So using this case number, we maybe we're going to pull up some data from the database and then create the object so that we can give out a meaningful message so our client will work with error message factory now. All right. So let's see how we can implement this in Java code.
