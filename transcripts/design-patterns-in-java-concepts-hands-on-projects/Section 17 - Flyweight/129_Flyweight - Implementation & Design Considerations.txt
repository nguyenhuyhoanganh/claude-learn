# Flyweight - Implementation & Design Considerations

All right. Now let's look at some of the implementation and design considerations when working with flyweight design pattern. A factory is always necessary when you're working with flyweight design pattern because the client code needs a central place where it can ask for a shared flyweight object. Also, since the number of shared instances can be quite large, a central place to store all of these instances is always a good idea. Now flyweight intrinsic state. It should always be an immutable for successful implementation of this particular design pattern. Now let's look at some of the design considerations. The ability to use flyweight in your application. It depends entirely upon presence of a sensible extrinsic state, which you can then move out of your class and then the client is able to provide that extrinsic state. Okay. If you cannot find an example or if you cannot find such a state which can be moved outside of an object, you cannot use this design pattern. Now, some other design patterns like state and strategy design patterns, which we are going to see in future lectures, they can really make use of this flyweight design pattern. All right. So these are some of the points that you should be aware of when working with flyweight.
