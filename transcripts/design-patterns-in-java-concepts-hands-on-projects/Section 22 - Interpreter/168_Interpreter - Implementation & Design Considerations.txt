# Interpreter - Implementation & Design Considerations

All right. Now let's talk about the implementation and design considerations when working with Interpreter design Pattern. So first of all, apart from simply interpreting the expressions using our abstract syntax tree that we built, we can also do some other things, like pretty printing is another example. So in pretty printing, what we do is we print our expression with some formatted in a formatted structure. Okay, so that's one thing that you can do using your interpreter design pattern. Now one thing that you might have already noticed and I have already talked about is that this pattern doesn't tell you how to do the actual parsing because the sentence that you are interpreting is coming in as a string. So now you need to parse that string in order to interpret it. Okay, so that's completely up to you how you do that. You can use regex or regular expressions or you can do plain old string tokenization. Anyway, the point here is parsing is completely up to you. Now the context object object that we saw, we were using an existing object in our application. So we had this user object which has roles in it and we are using we were using that object as our context, but that doesn't have to be the case. You can create a separate class for your context, set that user object as one of its properties and add any additional fields that you need in order to use that context while interpreting that expression. All right, so you can use context objects to do your state keeping. Now let's talk about some design considerations. Okay? So instead of relying on the interpreter pattern itself, what we can do is we can build our syntax tree using the guidelines of our interpreter design pattern, and then we can use another design pattern called as visitor. Okay. So one benefit of visitor is that if you look at our expression abstract, abstract expression interface, you will see that we have simple method called as interpret. Now using visitor. What we can do is, apart from interpretation, we can keep on adding multiple methods or multiple things that we can do with our syntax tree. Okay. And once you learn about visitor design pattern, you will see how we can combine these two so that we can add more functionality to our abstract syntax tree using visitor. Now you can also use flyweight design pattern for you for the terminal expressions because you will often find that the terminal expressions can be reused. Okay, so for example, in our example we had these permissions which were terminal expressions. So let's say you have ten permissions defined in your system. Then you don't need to create a new permission expression every time you can create these expressions one time. And since these expressions typically represent constant value like a fixed role or a number, if you are parsing some numerical expression. So these are typically, you know, immutable objects. So you can use flyweight design pattern for terminal expressions. Okay, So these are some of the design considerations while working with interpreter design pattern.
