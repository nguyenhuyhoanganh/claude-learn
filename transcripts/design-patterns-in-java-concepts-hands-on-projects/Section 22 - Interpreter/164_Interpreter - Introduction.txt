# Interpreter - Introduction

All right, now let's learn about interpreter design pattern. Now this design pattern solves a very specific type of a problem, and unless you face that problem, you won't have much use of this design pattern. However, if you run into that particular problem, you will be glad that you know about this design pattern. So what is that problem? So we use interpreter when we want to process a simple language with rules and grammars. For example, here is an example of such a language. So here we have a statement that says file access requires user rule and admin rule. Now this particular sentence is part or written in a language that can be interpreted by our interpreter. So there are some rules or grammar rules about this particular sentence. So we have a and here and then we have user rule and admin rule so we can interpret that we need user rule and admin role in order to access file. Okay. So we can determine that file may refer to an object in our application. Access can refer to the read access and user rule and admin rule are role objects which are part of our authentication authorization system. All right. So we can use these kind of statements in order to define some language or define some statements. And our code will use or will interpret these statements and carry out some meaningful action. And that's what this interpreter design pattern is. However, before you start planning on using it for very complex language like natural language processing and all that, that is not what this design pattern is. It solves very simple statements like the one you see on screen. Now. Interpreter allows us to represent rules of the language or grammar, as it is called, in a data structure, and then we can interpret sentences that are written in that particular language. Okay? So the grammar rule of having and, and we have a special meaning for that word. Okay, So these kind of rules can be represented as a class in our interpreter design pattern. Now, the classes also provide a method to interpret the actual sentence. Okay, so we will build a data structure that represents grammar of our rule. And then we'll we will give that data structure a sentence to interpret and our interpreter design pattern will interpret meaning of that statement. So now let's look at the UML diagram of this design pattern. Okay, so first of all, we have an abstract expression. So this is an interface that defines or that forms the basis for all of our classes that are part of interpreter design pattern. It defines a simple operation called as interpret, and it needs an object of context. So we're going to discuss what context is. But the important point is we have this interpret operation here. Next up, we have terminal expression. So terminal expression represents leaf node in our data structure and it implements the interpret operation and terminal expressions do not have any child expression since they are leaf nodes. On the other side of spectrum, we have non terminal expressions and you can think of these as composites. So they have other expressions which can be non terminal as well as terminal in them. And when we call interpret on our non terminal expression, it's going to call interpret on all of its children. Then we have the context. Okay, So each interpret operation that you see here needs access to the context. So context is an object that holds the global information which is needed by interpreters. So what does this global information mean? It could be the actual statement that we want to interpret. That object can also hold partial results of our interpretation. Okay, so we can use this global object as we choose. And then lastly, we have our client. So client calls our interpret operation on the interpret interpreter data structure. Okay. It also optionally builds the syntax tree based on the language rules. Now, one thing that this design pattern doesn't tell you is how to build the syntax tree. It doesn't tell you how to parse statements. It simply allows you to use an already built tree in order to interpret meaning of a sentence. Now let's see how we can implement this design pattern.
