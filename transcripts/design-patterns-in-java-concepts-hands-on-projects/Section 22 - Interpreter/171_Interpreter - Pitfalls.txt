# Interpreter - Pitfalls

Okay, Now let's look at some of the pitfalls of this interpreter design pattern. So class per rule, this mechanism quickly results in a large number of classes being created, even for a moderately complex grammar or even for a language with moderately complex rules. This pattern is really not suitable for languages with complex grammar rule. The example that we saw where we define simple permission expressions as a as an English language sentence, that is the complexity that you can easily handle with this design pattern. This is not mean to parse, you know, a natural language like English or any other language for that matter. And last pitfall is that this design pattern is very specific to a particular kind of problem, which is of interpreting languages. Okay. So there are not that many uses outside of this particular purpose in the interpreter design pattern. All right. So these are some of the pitfalls of interpreter design pattern.
