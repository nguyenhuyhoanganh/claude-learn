# Interpreter - Summary

All right, Now let's look at a summary of interpreter design pattern. So whenever we want to pass a language which has some grammar rules defined for it, we can use our interpreter design pattern. Each rule in that language becomes a becomes a class or an expression. In this interpreter, a terminal expression is an expression that provides implementation for interpret method. A nonterminal expression contains or holds other expressions in it, and its interpret operation is going to be delegated to whatever children it has as well as it can optionally do some interpretation of its own. Now this pattern doesn't provide any solution for actual parsing of the string that you get. So the actual parsing has to be done by your own. Now let's look at the human diagram of this design pattern. So first of all, we have an abstract expression. So this is an interface that defines the simple interpret operation, which expects an object of context For this expression. We have two implementations. We have a terminal expression. This expression represents a leaf node in our abstract syntax tree. Now interpret operation implementation in this terminal expression will carry out the interpretation and return its result. Non terminal expressions are expressions which contain other expressions in them, so their interpret operation will call interpret on whatever children it has, and it can also optionally do its own interpretation. Maybe on the results which are returned by its children. Context represents a object, any object that can be used by our interpreter. So we can use this object to provide input which needs to be interpreted. We can use this object to store intermediate results while we are performing. The interpretation client is actually going to use our abstract expression, abstract syntax tree and interpret any sentence. Now client has a responsibility to build a syntax tree so client can do that himself, or we can provide a separate class that builds our syntax tree using the interpreter design pattern. Now let's look at a sample implementation of this design pattern. So here we have a simple permission expression, and this represents our abstract expression. We have a simple method here called as interpret that needs an object of user. So we are going to use an object of user class as a context in this particular implementation. Next, we have a terminal expression. So we have a class called as Permission, which implements our permission expression. And this class is going to tell whether the given user has a permission. Okay, so we will create objects of this permission, and each object represents a particular permission that we want to check our user against. All right, then we have the nonterminal expression. So here we have an example of our expression. So this class is a nonterminal expression. That means it has two other expressions inside it, and its interpret operation is going to first call interpret on the on its children. So it's going to call expression one dot interpret expression to interpret and it's going to use results of those interpretation and perform its own interpretation. So it's going to use the or operator on the result of these two expressions. All right. Now we need to build our abstract syntax tree. So there are a number of ways that you can achieve this. There is no fixed way that is provided by this design pattern. So here we have an expression builder and we are using a simple stack in order to implement our parse operation. The result of this expression builder method is that it is going to create our abstract syntax tree. All right. Lastly, we have a client, so our client is going to use our expression builder in order to build the abstract syntax tree. Once a syntax tree is built, client can use that in order to interpret any number of sentences written in that language. All right. So this is the interpreter design pattern in Java.
