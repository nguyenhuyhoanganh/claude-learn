# Interpreter - Implementation

All right, now let's see how we can implement interpreter design pattern in Java. So I will jump into Eclipse now and I have a couple of classes here and I'm going to explain what these classes do. These are exactly same as we discussed in the UML diagram video for this example. So let's go over these classes one by one. So first of all, we have a class here called as Report. Now our application has these reports created in it and each report is represented by an instance of this class. Okay. So we are going to have multiple reports here and we are giving access to this report based on permissions. So so each report object has a permission expression defined in it. So here we are going to have values like not admin, and it indicates that any user can access this report. If he is not an admin, we can have a permission expression saying finance user and admin. That means this. This particular report object can be accessed if the user has finance user as well as admin role. All right, so this is the string permission and this permission is going to form our rules or it is going to give us the rules for interpreting users permission. So we are going to use this permission expression or permission string to build our abstract syntax tree. All right. So we are going to transfer this or transform this expression into a tree of expressions. And if somebody asks us for a permission or when somebody asks us to check whether he can access this particular report object, we are going to check that user against this expression. So what user has so user is the class that represents someone who is going to access our report object. So this user has a collection of permissions in it. So we will have typically a user object and we are going to check some permissions in the UI and those are going to be stored in the database. So user object has this list of permissions which will be checked against this expression. So if our user has an admin role and our expression is not admin, that means that user object cannot read our report object. All right. So we are going to interpret this list of permission. With the rules given by this permission string. Then we have this permission expression. So this is our abstract expression interface and we are using user object. So it has a single method called as interpret as we have seen in the discussion of this design pattern. And you might remember that interpret needs a context as an, as an argument. So here we are using user object itself as context, because the only requirement for context is that it should provide everything that is needed by our expressions to perform interpretation and our expressions are going to only need access to users so that they can read its collection of permissions so that they can return true or false based on that list. So we are going to use our user object as our context. And as I just said, our interpret operation is going to return true or false? And the final result will indicate whether our user can read our report object. All right, So now let's start implementing this particular design pattern. So we begin with permission. So this class represents a terminal expression. So what is a terminal expression in our application? So we have these roles, admin role, finance, user role. So our permission class is going to represent that single particular role. All right, So it's going to represent role like admin, it's going to have a value like finance user, so on and so forth. So to begin implementation, I'm going to say it implements our permission expression interface, permission expression, and I'm going to quickly generate a stub for the interpret operation. All right. So what our permission expression needs, well, it needs to have the actual permission that this object represents. So I'm going to have here a string called as permission, and I'm going to declare a public constructor here permission, which is going to accept a string value as parameter, and we're going to store that value inside of our field. All right. So this is what our permission class needs. It needs an actual value like admin or finance user while creating it. And whenever we give this permission a user object, it's going to check the collection that is inside this user and return whether this permission is part of that user's permission collection. All right, so how should we implement this method? Well, we first need to get access to the permissions that are stored in this user, get permissions and to simplify things, I'm simply going to use its contains method which needs an object. And this method will return true if the permission list contains this permission. Of course, this is not a quite a robust implementation. We could have implemented a different way, but to save some time and to highlight only the important points of our design pattern, we are going to check the permission collections using the contents method, and once it finds it, we are going to say return that result of whatever contains return. So if our permission is found, we are going to say our terminal expression has evaluated to a true value. However, if our user lacks this particular permission, our contains method is going to return false and we are going to say our permission expression is also false. Now I'm going to also override the two string method so that we can print this class. So in the two string method, I'm simply going to return the permission so that we can print out our abstract syntax tree once it is done. Now, one issue you will see with this contains method is that we do not have a way to do a string case insensitive comparison. Okay. Because contains is going to do a simple equals comparison with this object. So that means if our permission and whatever is stored in the user have same string but in a different case, then we are not or we are going to get wrong result here. So what I'm going to do is whenever I'm storing permission, I'm going to convert them to lowercase in every place. Okay, So now our permission that is stored in my permission expression is going to be a lowercase value of the actual permission. I will just check the user object and you will see that while we are adding permissions to users collection, you will see that I'm also calling the lowercase method on those strings. So this collection also holds all the permission strings. Lowercase strings. All right. So this is our terminal expression completely implemented. Now let's implement a non terminal expression. So here we have an expression, and this is going to check whether our finance user and admin expression is true. Okay, so this is an example of non terminal expression because it is going to contain child expressions in it. So to start the implementation, I'm going to say it implements permission expression is going to implement that interface. I'm going to quickly generate the method step for interpret method. So whatever an expression needs, well, it needs at least two expressions to work. So we are simply going to stick with two expression limits. We are going to say we need a terminal expression. One, I'm going to say this expression one. All right. We are also going to need another expression in order to check whether these two are equal. Now, I'm going to write a constructor for my and expression and we are going to accept sorry, we are going to accept two expressions. Okay. And I'm going to do store references to those expression. One is equal to. Expression one. And expression. This expression do is equal to expression. Okay, So what I'm simply doing here is I'm asking in my constructor to provide us with these two expression in order to create our non-terminal expression. Okay. So you need to have two expression before you can create or end expression. And in the interpret method, you can guess that it's going to be quite a simple implementation. We're going to say expression one dot interpret because remember we saw that interpret Non-terminal expression will call interpret on their child. So we're going to call interpret operation on Expression one. Then we are going to use that result, perform an operation, and also call the interpret on expression two. Okay. So once again, what is happening here? So we have these two expressions which are child expression of our and. When we when somebody calls interpret on this and expression and gives us a user object, we are going to call interpret operation on our child expression. So we have we have this interpret method call here for expression one and another interpret method call on expression two. Once these two expressions are interpreted and they return some boolean value, we are going to use that value and perform some additional interpretation in our and expression. All right. So you get the idea what is going to happen here. So this is what Non-terminal expression does it calls interpret on the child expression and if needed, it will perform some additional interpretation based on the results of the child expression. Now, what I'm going to do is on the similar lines we have. Sorry. On the similar lines, I have already implemented not an expression, so we will quickly go over those. So here we have our expression. It is exactly similar to and in terms of what it needs and how it works. So it needs two expressions. So we are going to accept those in the interpret operation. We are going to again call interpret on the expression one as well as expression two, and then we are going to perform a logical or operation on return values of these two expression in the not expression. We are going to need only one child expression and we are going to call interpret on that expression and negate the result. So if this expression returns true, we are going to return false. All right. You will see that I'm implementing two string method everywhere, so I will just jump back to an expression and I'm going to override the two string method. Okay. Do string. And I'm going to do is I'm going to say expression one plus and. Expression. Okay, so we are effectively calling to string of expression one concatenated with and and then to string of expression two. All right. So we now have our nonterminal and terminal expressions built. Okay, So now we need to actually go and parse whatever is given to us. Okay? So we are going to get an object of report and based on this permission string, which can have a value like finance user and admin, we have to interpret this string, parse this string and create our expression objects and build a tree using that. So we need to create an expression builder for that. So what I've done is I have already implemented an expression builder to save some time, but I'm going to explain that how we are going to implement this. So I'll quickly go over three, go over this expression builder. So why we need it in the first place. I hope you understand we are going to get an object of report and we are going to parse the permission string which is written here, and then we are going to create our permission expression based on that string. Okay, so design pattern itself doesn't provide us with any definite way on how to do parsing. We have to implement it on our own. So I have created this parse method, so I will quickly remove the comments. So in the parsing you will see that I'm using a string tokenizer and we are assuming that. So we are using the default separator for string Tokenizer And you can remember that the default separator is a space. So we are assuming that our report condition will be written as finance user space and space admin. All right. We are not doing a lot of error checking or edge case handling here. I hope you understand this is not a production code anyways. So once we tokenize our permission string we are going to iterate over its tokens and check what value we are getting. So if we get any of and not or or string, that means we are parsing this particular bit in the string. So this means our nonterminal expression. And what we are doing here is if we encounter and in our string, then we are going to push that operator on our stack. So I have these two stacks here, operators and permission and we are going to use these stacks in order to build up our expression. All right. If we found anything else then and not and or so we only support these nonterminal expressions. If there is something else that means it is actually a permission like finance user or admin. If that is the case, we are directly going to push a permission on our stack. So we're going to create this nonterminal expression in the build expression. Once our parsing is done, we are going to call the build expression. So I will remove the comments here in the build expressions, we are going to iterate over our operations array, we're going to pop out the operator and then we are going to build the expressions that are needed for creating our. Uh nonterminal expression. So if it is a not operator that we popped out, then we are going to pop out one permission from our permission stack. And because our not permission, not expression only needs a single child. So we're going to build that expression, build that permission and create our not expression in the similar way. I'm going to use two permissions in order to build our and expression. All right. So this is how we can build our expressions. I'm not going to explain these in much more detail. The code is attached to this video because we could spend a lot of time here. Our goal here is to only look at the interpreter design pattern. All right. So what you need to know here is we are going to build an expression or abstract syntax tree using our expression classes. Once that is done, we are going to jump into our client and use this. So we are first going to create a report object. So I'm going to create a new report. Object sorry, new report object. And the constructor for report needs two arguments. It's going to need a name. So I'm going to say cash flow report and it needs a expression that indicates who can access this report. So to begin with, I'm going to simply use a simple expression like not admin. All right? Now what we need is we need an expression builder, we need an expression builder. So let's create it. Sorry, expression builder builder is equal to new expression builder. Then we are going to give this report, give this report to our builder in order to build our syntax tree. So this build operation in the expression builder is going to return us with a single permission expression that represents the root of our abstract syntax tree. Okay, so we're going to say permission expression. It's X is equal to builder dot build and we are going to give it report one. All right. So now we have our abstract syntax tree here and let's first print out whatever is the content of this expression tree. All right. So we are going to print out the expression tree here and then we are going to create a user. So I'm going to say let's say user user one is equal to new user and let's say the name of the user. And then we have to provide the permissions that it has. So let's say we're going to give it a user permission and let's say admin permission. All right? And let's print out. The result User Access report. Sorry. And I'm going to say exp, which is our expression dot interpret and we're going to give it our user one. All right. So what happened here? We created a report object. Then we built our expression using expression builder. Then we got a hold of the user which is actually trying to access this report. And we're going to interpret this user, interpret its permissions and check whether they match with this expression. So we're going to interpret the user's permission and check whether they satisfy this expression. All right. So let's run this example now, Java application, and you will see that, first of all, we get this result printed saying not admin. And this is the expression that we print. And once we check whether the user can access our report, it says false because our user has admin permissions. Now let's change this expression. Let's say, let's say we have only terminal expression here. Let's say our report says that anybody with admin role can access it. So we will write simply the admin. Now let's run our application application once again. So I'm going to say sorry, run as Java application. You will see that our expression tree only prints admin and we say that user can access report. That is true. Now let's give some complicated expression like say finance. Admin. Okay. I'm just making this up as I'm going. Or admin. All right, so we are using our non terminal expression here once again. So I'm going to say run as Java application and here is our expression tree admin or finance admin and our user has access is true because our user is admin. All right. So let's say if I remove the admin permission from my user and then run the application, you will see that we don't get access to this report. All right. So this is how you can implement interpreter design pattern. I know it's a little bit. It went a little bit fast in the expression builder part because we are already over 20 minutes in this video. I wanted to keep it simple and focus on the design pattern. However, don't worry, the finished code for this example is attached as well to this video. You can download that and study this example on your own. All right. So thank you.
