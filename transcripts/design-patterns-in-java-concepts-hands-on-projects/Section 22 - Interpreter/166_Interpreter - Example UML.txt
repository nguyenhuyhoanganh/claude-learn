# Interpreter - Example UML

All right. Now, let's look at the of example that we are going to implement in our Java class. Okay. So this is going to be a bit involved implementation. So bear with me for any complexity that you may encounter. I will try to explain all of these classes one by one. So our goal here is that or the problem that we are going to try here is we are again going to consider a hypothetical situation. And let's say we have objects of reports in our application. Okay? So we have a class report. An objects of that class represent a report that is stored in our system. Now, each reports, each report has a permission attached to it. Okay. So that who can view this report or not? Now, these permissions are written in normal English language. Okay. So let's say you have used some designer workflow designer or permission designer with a UI, and non-technical users have defined permission on that report. So our permission in the report will read something like user needs admin role or user needs finance admin role in order to use or read that particular report. Okay. So the permission statement in our report is going to be our rule. Okay. Then we are going to use that rule in order to and build our interpreter. So this is the report class which has this permission. Now coming back to the center of our diagram, we are going to have an abstract expression as we saw in our previous UML, we have a permission expression. So this is going to be our abstract expression and, and it defines an interpret operation. As we know for context, we are going to use a user object. So user object is another class. So user is another class in our system and a user has multiple roles assigned to it. So we are going to use this object as our context. So permission expression is our abstract expression, and then we are going to have a terminal expression, which is the permission. Okay. And after that, we have multiple terminal expressions. For example, we have our expression, not expression and and expression. So these nonterminal expressions will have other expressions in them. So an expression needs two other expressions, not expression needs one other expression in order to work. The permission expression, which is our terminal expression, is simply going to return the string value of the permission or the role that the user has. All right. And lastly, we have this expression builder. So this is a separate class that reads the permission statement from the report and it builds this abstract syntax tree. Once our tree is built for this particular report, we can give this tree any user object, and this design pattern or this syntax tree will tell us whether the user can access the particular report or not. Okay, so this is how we are going to implement the interpreter design pattern in Java.
