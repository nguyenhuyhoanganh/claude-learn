# Factory Method - Implementation & Design Considerations

All right. Now let's look at some of the implementation and design considerations while implementing factory method. Now we have seen so far our creator being abstract class, but that doesn't have to be the case. Your creator can be a concrete class if you can provide a default object in its factory method implementation. Now you can also use the simple factory way of providing additional arguments or criteria in your factory method. So if you remember our Simple Factory, it was a very simple class with a static method that took a single argument, which was the criteria based upon which we decided which object to create. Well, we can use that same logic here. So our factory method currently doesn't take any argument, but that doesn't have to be the case. We can accept a criteria in our factory method and using that criteria, we can decide which object to instantiate. All right. So you can extend your factory method using that particular logic. Now let's look at some of the design considerations, the creator hierarchy that we see. For example, in our example, we had a message creator and then we had two child classes. Jason, Creator and text message creator. This hierarchy reflects the product hierarchy. That means the objects that we are going to create and the creators are. They have similar hierarchy and we typically end up with one concrete creator per concrete product that we add in our application. All right. The only point that I wanted to highlight simply is that the two hierarchies of the product classes and their creators reflect each other. Now you will see that template method is a design pattern also makes use of the factory method and another creational design pattern that we are going to look at called as Abstract Factory also makes use of factory method design pattern. All right, so these are some of the points that you should be aware of when using factory method design pattern.
