# Factory Method - Implementation

All right. It's time to jump into Eclipse and implement the factory design pattern. So, as usual, I have written a couple of classes that we are going to use while implementing this design pattern, and I'm going to go over them one by one. So here we have a message class, and this is our product class and this is an abstract class. So we are going to create objects of subclass of this message. Now we have Jason message, which is a concrete class and which extends from our product class, which is message on the similar lines. We have a text message, then we have message creator and this is going to be our abstract creator and this class is going to have the abstract factory method. So let's start by implementing our factory method pattern. So to start off, I'm going to declare a method that is going to be used by client in order to get object of message. So our return type is going to be message. And this message refers to the abstract product class. So I will switch over to it. And what we are going to do is I'm going to write now the abstract. Factory method called as create message. All right, so this is our factory method, which is an abstract method. Now we are going to get the message object from this create message method. Remember, this method will be implemented. This create message method will be implemented by one of the concrete creators. And our abstract creator is going to use that method in order to create the message object and then it can do some additional processing on this. For example, it may say add default headers to our message. It's going to, let's say, encrypt the message and finally it's going to return that message. All right. So this is how you will implement an abstract creator in factory method. So abstract creator will typically perform some additional processing on the object that is returned by subclasses. Now, this is not mandatory, but this is typically how the abstract creator works, so it will use this abstract method, get the object, provide some additional properties or perform some additional operations on this object and finally return that to the client. Now it's time to implement our concrete creators. Now, as you can see, implementing concrete creators can be quite easy since we have handled all the processing that is needed on the product In our abstract creator, this class is simply going to override the create message method. And since this is a Json message creator, we are going to simply return new Json. Message. All right. So this is going to return a simple Json message. So on the similar lines, we are going to have a text message creator. And as you can guess, this class is going to again override the concrete message method and return a text message object from it. Right. So now we have our abstract creator and then we have our concrete creators and each concrete creator refers or creates objects of one of the product classes. Once these creators are implemented, we are now going to jump into client, which has a main method. So in order to use this pattern, what we are going to do is we are going to call our static method that I have defined here. And as you can see, this method expects an object of message creator. So if we give it Jason, message Creator and inside this method, let's say we get the object, the message object from our creator, get message. And then we are going to simply print out whatever the contents of that message is. All right. So this is how we can use the factory method design pattern in our client code and just to make sure that we are going to get a different object, we can again call the print message method. And here, instead of Json creator, I'm going to give text message creator. Sorry. Sorry about that. Text message creator. All right. And if we run this class, you will see that in the output we get first an object of Json message, and then we get an object of text message. All right, so this is how you can implement your factory method design pattern. You will have an abstract creator and it will provide the factory method. So this is the factory method which is then implemented by subclasses of our message creator. All right, so this is all about implementing factory method.
