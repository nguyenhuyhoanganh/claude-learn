# Memento - Implementation Steps

All right. Now, as is the case, we are going to see how we can implement a momentum. So we start by studying our originator object. So we'll study this class and find out which state needs to be stored in our memento so that it can be restored back in its previous state. Once we determine what actually needs to be stored, we can implement our memento. The requirement here is that this class, the Memento class, should not expose anything that is stored in it, because if we allow anyone even to read this stored state, that means we are creating or we are breaking the encapsulation of originator because typically the state of originator is going to be private to that particular class. If we allow this state to be read from Memento object, that means we are exposing this state to somebody else and they can start depending on this state. So Memento should not allow reading of this state only originator. That is the object which is creating Memento should be able to read what is stored in the memento and modify or create a new memento object. Now, originators should provide a method that can be called, which takes a snapshot of its state and returns an object of memento. Another method should be provided in originator, which will take an existing Memento object and restore itself to the state that is stored in the memento.
