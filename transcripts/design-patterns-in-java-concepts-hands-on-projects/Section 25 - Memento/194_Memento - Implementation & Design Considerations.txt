# Memento - Implementation & Design Considerations

All right, Now let's look at some implementation and design considerations when working with Memento Design Pattern. So first, implementation consideration is that you should always keep an eye on the size of the state that is getting stored in Memento. So frequently, if you have a large array that is stored each time we take a snapshot, then your memento objects will start consuming a large amount of memory. If you are running into these type of problems, then you have to design some solution. For example, you can discard older states so you can keep a limit on how many undos a user can perform. Okay, so that is a great way to take care of these kind of solutions or these kind of problems. So the point here is always keep an eye on how much object, how much memory your memento is consuming. Now Memento class itself ends up being the inner class in the originator, and this is because of the requirement that it has to encapsulate all the details of what is stored in its instance. Okay, so you will end up creating private getter and setter methods. And that means the Or it has to be an inner class of the originator so that it can access those private getters. Okay. Now resetting to a previous state when we reset our originator to a previous state using a memento, you should also consider what effect that is going to have because let's say you execute an operation on the originator and its state changes. But during that operation, if your originator communicates with some other object whose state is also now getting changed, then simply resetting the originator is not going to be enough. Okay, so let's say you have an Add operation on our workflow designer and our workflow is going to add this newly created step. But let's say it's also contacting some queue which is external to our workflow designer and sending some data there. And as a result of that message that is sitting on queue, somebody else changes their state. So if we now only reset our workflow designer or only our originator, then those objects are going to be left in the invalid state. So you should always keep an eye when you're resetting your originator that what effect our originator is going to have on the external objects. Okay, now let's look at some design considerations when working with momentum. Now if our workflow, if our originator has a definite fixed way in which it is modified and there is now a fixed way where mementos are getting created, then we can only store incremental state that is state that is changed from last time the moment was created and this is quite possible if we are using command design pattern. So if our originator object can modify its state only because of some command, then we can store only the changed state in that particular command. So we can create this incremental version of mementos instead of storing entire state each time, we are only going to store what has changed from the last time the memento was created. And that way we can save on to the memory consumption of mementos. Now women Jews themselves can be stored internally. So that way you don't have to worry about having this memento class, which is going to be stored somewhere external to our originator. You can store that memento internally so you will not expose even the class itself, which is used to create mementos. But that adds to the complexity of your originator because now it has to juggle this additional data structure inside itself and that can complicate your implementation. But there is but this is a way that you can also implement Memento.
