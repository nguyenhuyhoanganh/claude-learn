# Memento - Summary

Alright, so let's summarize memento design better so we can use this design button in order to take a snapshot of our object state and we can store that snapshot and use it in the future to reset our object to that saved state. Memento itself is created in such a way that it doesn't expose any value or any state that is stored inside of it. Because even if we allow it read only access to over mementoes, stored states, then other parts of our application against on depending on presence of those values. That means we are going to break encapsulation off our region ever. So mementos do not allow any type of access to what is stored inside of them or is never object whose snapshot we want to take. These objects provide methods in order to take or create a memento from them. And they also provide a method where we can give an existing memento so that the original object resets itself using the given memento. Now mementos need to be saved for them to be of any use, because the whole purpose of taking a snapshot is that we want to revert to that snapshot in future. When we went to war works well with common design, but because we have a fixed way in which our originator is getting modified. So if our originator only changes because of execution of commands, then each command can take a snapshot before it executes. So that we can use a new feature using these stored momentum. Now let's look at the human diagram of this design. It's quite a simple design. Better. We have an original right here. So this represents an object whose snapshot we are going to treat. This originator provides two important methods. All of that are of interest to us. The first method is creating a window using the calling. This method will give us an instance of momentum, so it will create or take a snapshot of the originator state and give that a store that in a moment to object and give that object to us. Another method is set memento and this method expects an existing moment object to be passed and using that memento originator is going to reset its state to whatever is stored in that moment. Memento itself is a sealed object. It doesn't expose anything to the outside world. We simply get this get hold of this moment to object by calling Create Memento. We store that in caretaker and whenever we want to reset originator, we're going to ask the caretaker to give us a stored moment to object. And we're going to call the set moment to method on originator. All right, so this is the human moment to design better known. This event is sample implementation of this design. We're. All right. So, first of all, we have a moment of class here. So you'll see that we have some state values that are used to store our originators state. Now you will see we have the constructor, the getter methods and even the which is everything is private in this class. And this way we are ensuring that nobody outside can use our read values that are stored in them. And to modify the workflow designer is our originator. So in this example, we are going to store a snapshot of this object. So you will see we have a method here called as get momentum, which is going to create amendment to object and it's going to store its state inside that memento and return the object to outside what I said. Memento, on the other hand, is going to take an existing moment to object and it's going to reset itself. So it's going to reset its workflow state using the values that are stored in the moment to object. Now, Memento is an inert class of this workflow designer, and that is why our workflow designer can access these private getter methods on private constructor. All right, so this is how we can typically implement Momenta. Now let's look at the create a caretaker of our moment to December. And in this case we are using a command. So we have add step command. And in this command why we are if somebody calls an execute operation on every command, we are going to first get hold of the momentum. So we are going to call the get moment method on workflow designer, save that moment inside us and then execute or perform the operation that this command represents. If somebody calls in and do operation on this command, we are going to call the SEC momentum method and give it over already stored state. And so this is how we can combine command and momentum together to provide the undo functionality. All right. So this is all about the momentum design better in job.
