# Memento - Implementation

All right, Now let's see how we can implement Memento design pattern in Java. So I'll switch over to Eclipse now and we are going to work with a lot of different classes here just so I can demonstrate how to use Memento with command design pattern as I had promised when we were studying the command design pattern. So I will explain these classes. These are exactly same as we saw in the example video. So let's go over these classes one by one. So what we are designing here is a hypothetical application that means which doesn't have any GUI or anything. We are designing or creating a workflow designer. So workflow designer is typically a part of your application, which has some user interface attached to it. It could be a web UI and people can access this designer in their browsers and define some workflows. Okay, so workflow is a class in our application and it represents s number of steps or a sequence of steps that need to be followed in order to complete a business process. Okay. So we have this class here. It has a single collection called as Steps. It has a name so we can create a workflow with a name and certain number of steps associated with that workflow. Okay, so this is the workflow class. And we are we have this workflow designer that is going to create this workflow. Okay? So workflow designer has methods like Add, remove step, which are going to add or remove steps from the workflow. And our designer state currently only involves or only consists of a workflow object and this class workflow designer is going to be our originator. So we're going to take snapshots of our workflow designer's state. Okay, so that's why we have methods here called as Get Memento and Set Memento. Okay, so these are the methods using which we can take a snapshot of our workflow designer. So currently these are unimplemented methods, but you will see that we have a memento class. So let's go to that class. All right. So you will see we have a memento class here, so we'll add a comment memento. And let me bring that up so that you can see it. All right. So this is the class, which is an inner class in our workflow designer that will represent a snapshot of our workflow designer state. All right. So what we need in this class are all the fields that we want to store so that we can recreate our workflow designer. Now you will see that we have this workflow reference here, and if you are thinking about putting that straight up here, it's not going to work. Why? Because unless we make a deep copy of our workflow and then store that deep copy in Memento, what we are going to end up with is if we simply store reference and if somebody adds a step, then our memento itself is going to get changed because it only has a reference to this single object. So what we're going to do is we are going to store the actual steps, which are strings. Okay? So we are going to have an array of string here called as steps, okay? Because strings are immutable so that we can store references to those steps here, but we are not going to store reference to the workflow or a collection like linked list. We're going to store an array inside our momentum. Then we are going to store the name of the workflow that we are working with. All right, String name. Now these are private. Then we are going to write a constructor, so I'm going to create a private constructor here. And this is so that nobody from outside this class can create a memento. We want only the designer workflow designer to create this Memento objects. So I'm going to accept all the information that is needed. So I'm going to say string name and I'm going to say this dot steps is equal to whatever is given to us, and then this dot name is equal to whatever is given to us. All right? So we now have provision to create a memento and store our steps and name of the workflow that we are working with. Then we need methods to get these information out when we are restoring our workflow designer state. So we need a getter method. So I'm going to have a method here called as. Get snaps. Okay. And in this method we are simply going to return whatever steps array that we have, but we are going to mark this method private, all right? Because we do not want anyone to even read the state that is stored inside of momentum because in future we may replace the string array with something else and then our getter method is also going to change. And if somebody outside was accessing this method, then that change would break the code. All right. So to avoid these kind of couplings with the internal values or internal structure of our momentum class, everything inside this class is going to be private. In the similar lines. We are going to add a getter method for getting name from this memento. And here also we are going to simply return name. All right. So we now have a class whose object we can create in our get Memento method. So first of all, what we are going to do is we're going to check whether we have a state or not. Okay? So if our workflow is null and if somebody calls a memento, get memento on our workflow designer, what should we do? Well, we still have to return a non null object because remember, mementos are stored externally in this particular application. Okay. So they will be expecting a non null object. So I'm going to say if workflow is null, then we should create an empty memento. But if you look at our constructor, we don't allow creating a empty memento. So I will add a default constructor or no argument constructor memento. And this is simply going to leave it as it is. All right. So if workflow is now, we are going to return a empty memento object. All right. However, if there is a workflow created, then we are going to say create new memento and then we're going to say workflow dot, get steps and workflow dot get name. All right. So this is the memento that we are going to return. If we have a state, a valid state or a valid workflow in our designer. So that's one part of the puzzle. The next part is to restore our state again. We need to first get the Memento object and check if there is any state stored in it, because as we have seen, if our workflow was null, then we were returning an empty memento. So that means we have to check whether the memento is empty so we can say if Memento Dot. So you get stamps. If it is null. And Memento dot get name is also equal to null. Okay, So these two conditions will indicate to us that we have we had created an empty memento because there is no setter method here. So instead of having these two checks here, what I'm going to do is I'm going to copy these and create another method here which returns a Boolean. And let's add a method here called as is Empty. Okay. And this is going to simply return the result of this same thing. So it's going to say if this dot get steps is null as well as get name is null. All right. So I have simply removed the check inside momentum so that we can have a cleaner API and we can simply call if Memento is empty. That means our workflow was null, remember? So if our workflow was null, we were returning an empty memento. So I'm going to simply say this dot workflow is going to be null now. Okay, so we reset our state. When this memento was created and since this memento was an empty memento, which can only happen if our workflow was null when resetting using that memento, we are going to set our workflow field to null. Otherwise, if the memento is present, then we are going to create a new workflow object. We're going to create a new state. And while creating this memento, we are sorry, creating this workflow. We are going to use the information that is stored in our memento. So Workflow constructor. We have a constructor in our workflow. I will highlight it. It takes a name and the array of steps. Okay, so we're going to pass it on Memento Dot get name and then we are going to pass Memento. Dot get steps right. And this way we now have our workflow designer state, which is our workflow object, reset to the exact values stored in the Memento instance. All right. So this way we can implement our momentum. So remember, this was only a demonstration. So we had a very simple example to work with. But in real life, you may have to do a lot of work in order to create a memento and restored from a memento. Okay. And always keep in mind that simply copying the reference is not going to work. We had to actually copy the deeper state or we have to make deeper clones of our state. All right. So this is our originator, which is our workflow designer completed. We have our Memento class implemented and we have methods in our originator to get a memento and set a memento. Now, how we are going to use this, as I told you previously, we are going to use a command design pattern. So let's look at these commands first. So I will briefly explain what those are. So first of all, we have workflow command interface. Okay. So this is the abstract command interface. We already know it has a method called as execute, but now we also have a method called as undo. So calling this method is going to reset whatever changes were done by this command. And our receiver, which is in this case is workflow designer, is going to revert to its state before this command was executed. All right. Then we have an abstract class called as Abstract Workflow Command, which is implementing this workflow Command interface. And the reason we have this abstract class here is we are going to implement our undo operation in this single place, okay? And all the commands can extend from this workflow command, abstract workflow command and have this implementation available to them. So let's look at this class in detail. So we are accepting workflow designer, which will be our receiver. So we have a protected variable here to store our receiver. We also have another field to store a memento given to us by workflow designer. And if somebody calls undo operation, we are going to call the receiver, which is our workflow designer, and set Memento with the object that we had stored. So how are we going to get hold of this memento? So let's look at a simple command here. So we have add step command. So this command will add a step to the workflow that we are editing. And this step command needs a parameter or an argument of string type, which which is the name of the step that is added in the workflow. So in the constructor we are giving the designer to our base class so that it will store it as a receiver. And then we are storing the arguments that are needed to execute this command, which is the string field name as step in the execute. However, you will see that we are calling the get momentum method first on the receiver, get momentum. So it's going to return us the current state of the workflow designer. We are going to store that in our command and then we are going to carry out our commands operation. All right, So this is how all the commands that are present or that are implemented here are. Executed. So in every command before we carry out our actual work, we get a memento from the receiver and store it in the command object itself. Okay. Same thing for remove step command. We get the memento, store it, and then execute whatever our command wants to do. All right. So this is how you will combine Memento and Command Design banner. Now, let's see this in action. So what I have done is I have created the client code to use already in this client code. I will explain this. What is happening here in this client code, you will see that we are creating our workflow designer and we are calling a method called as run commands. Okay, so this is the static method that we are calling in the workflow in the run commands. What we are going to do is we are going to create several commands, okay, commands, and then execute them while collecting those commands in a collection. So this is how you would work with the undo functionality. So you will create a command, execute it and add it into some collection. Okay. Some ordered collection like linked list, which will maintain the order in which objects were inserted. So once these commands are executed, we would have our workflow designer reach a particular step which reach a particular state. And after that we are going to print whatever workflow designer is doing and then undo these commands one by one. Okay, So what I'm going to do is, first of all, we are simply going to run our commands and then print our state of workflow designer. So I'm going to right click and say run as Java application. All right. So you will see that we have a workflow called as Leave Workflow created where we begin and we have these many steps in this workflow. All right, So what I'm going to do now is I'm going to undo one of the commands that are present in the collection of commands. So I'm going to say commands dot sorry, commands dot. I'm going to say get last or let's call remove last. Okay. So that we don't call undo on the same command once again. So we're going to remove the last element from this collection and we're going to call the undo operation on it. Okay. So the last command, as you will see, was the application approved command. Okay. So we added a step application approved, which was the last command executed. And you can see that in the output that the last step was application approved. So let's undo that command and I'm going to again print whatever the state of designer is. All right, so now let's execute this code once again. So we'll see that we begin with whatever we had. Okay, So we have these many steps. Once we undo our last command, you will see that the application approved state is now removed. Okay. We now have only two steps here. The last step, which we did was the application approved step that is now removed. So our originator reset it to a state that was present before we executed that last command. Okay. So you can keep on doing this. We can then again remove the last command and call an undo operation on it. And let's print our workflow designers once again if I run. All right, You will see that in the last. We also removed the submit application step as well. All right, so this is how you can combine Memento and Command to implement an undo functionality. And we also saw in detail how we can implement a memento design pattern where we give out a sealed object to the outside world. So just to show that this object is really sealed, if you call if you try to call any method on the memento, you will see that we do not have any getter methods accessible to us. Once we are outside of that workflow, designer. Okay, so this is what we mean when we say that we are going to return a sealed object so that we are sure that nobody's depending or using the contents of our Memento object.
