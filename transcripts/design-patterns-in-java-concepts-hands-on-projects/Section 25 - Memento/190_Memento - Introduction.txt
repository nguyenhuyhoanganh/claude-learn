# Memento - Introduction

All right. Now let's learn about women to design better. Now, this is one of those design button that solves a very specific problem, and you will use this to solve that problem only. So what exactly is the woman to see whenever we want to store our objects? And we want to ensure that nobody can modify or read that internal state of an object. Then we can use memento. So what does this mean? So let's say you want to take a snapshot of your object state. You want to record all the values of the state at this particular moment in time. And you want to use this same state to perform some operation in future. A great example of using such a snapshot is if you want to provide and do functionality. So in word processors or golfing applications, you may have used this undo feature. So the way this feature works or the way this design pattern allows you to provide this functionality, is that you will keep on taking snapshots of your object state. And if somebody calls you to undo the operations that are happening on the object or that have happened on the object, you will restore that object using the snapshot that we have already taken. And this is what women do is it allows you to take a snapshot of your object. See, it keeps that state field inside of it. That means nobody can read whatever state we have saved in Memento. And of course, they cannot modify it. The only object that can read or create a memento is the object whose snapshot we have taken. And this is the main intent behind this design about it. Okay. And the reason for using this is an apartment is if you want to restore over object state to a previously saved state. Now using Memento, we can ask an object to give it state as a single sealed object. So we will get an object outside when we call for a memento. And we can take this memento and store it somewhere else until we need it to restore over object. Now this pattern is often combined with the command design button in order to provide the undo functionality in our application. Right now, let's look at the human diagram of this design. It's quite a simple design button. First of all, we have a rule called US Originator. So this is an object whose state we want to store. We're going to take a snapshot of originator state. So it has provided a method called Let's create momentum. And this method is going to return us an object of momentum. So Momenta class represents a stored state of this originator. We also have a method here in the originator called a set momenta and it will accept an object of our momenta class. And this originator will reset itself using the values that are stored in the given object. We have another roll call, less caretaker. And this is simply a collection or another object which holds on to these mementos as they are created. So you can see that whenever we want to take a snapshot on the originator, we are going to call the Create Momentum Method. It's going to return us an object of this class, and we are going to give that class the object of this moment class to the caretaker, which was keep on storing these mementos. So we can ask the caretaker to give us a memento which is stored in it, and we can get that, give that momentum back to originator to reset the. All right. So this is how all this is how the momentum design button works.
