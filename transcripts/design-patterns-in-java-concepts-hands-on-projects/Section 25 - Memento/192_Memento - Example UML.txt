# Memento - Example UML

Now let's look at the HTML of the example that we are going to implement in Java. So we are going to use this memento design pattern with command, and I will show you how you can use command pattern along with Memento to provide a new functionality. So the problem here, the hypothetical problem that we are going to solve here is that we are going to create a workflow designer. Of course, there is not going to be any graphical user interface or it's not going to design anything special. We are simply going to have a placeholder called class called as workflow Designer, and this class can create workflows. So typically you will see that these type of classes or these type of usage in enterprise applications, where you give the user a graphical interface in the browser and using that, they can create different workflows. So we are going to assume that we are creating such a workflow designer and we are going to provide the undo functionality on this designer using Memento and Command. So workflow designer is going to be our originator. So we're going to take snapshots of this designer so that if, let's say, user adds a few steps and then he wants to undo that operation, we are going to use our previously saved memento in order to reset our workflow designer. Designer Memento is our actual Memento class. And whenever we call a create momentum method on the workflow designer, it's going to return an instance of this particular Memento class. Now, who is going to call this Create Memento method and who is going to call the set Memento? This is where command design pattern comes into picture. So our workflow designer is going to be driven by commands. So if user clicks an add button on the workflow designer, we're going to create an Add command and we're going to execute that command against our workflow designer. So this now becomes an ideal point to take a snapshot of our workflow designer. So before actually adding the newly created step, our Add command is going to call the Create Memento method. It's going to get a Memento object and it's going to store that state in itself. So Add Command is now going to store the Memento that it just created. Then it's going to actually add this step and its work would be done. We are going to store this Add command now in a collection. So we're going to create a linked list where all the executed commands will be stored. And if a user calls or if user says that, undo the last command, we're going to take the last object in the list and call the undo operation on that. So now this command object is going to use the saved memento and call the set Memento on Workflow Designer. Okay, So this is how an undo operation is implemented in applications. All right. So let's jump into Eclipse and see how we can implement that in Java.
