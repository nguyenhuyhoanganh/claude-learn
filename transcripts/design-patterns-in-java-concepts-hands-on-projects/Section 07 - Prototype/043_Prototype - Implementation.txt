# Prototype - Implementation

All right. Now we're going to see how we can implement prototype design pattern in Eclipse. So I'll switch over to Eclipse. And as as usual, I have written some code that we are going to use while implementing this design pattern. So we are going to go over the classes which have already written. These classes will correspond to the example that we just saw. So first of all, we have a game unit class and this is a base class for all of our prototype classes. Now, game unit, this object of this class represents some unit on a game which is played on a map. This class has a single property or attribute called as position, and this represents the position of this particular game unit object on a map. Then we have two child classes of this game unit. First of all, we have a Swordsman class which is extending from game unit. It defines its own property called as state, which refers to the current state of this particular swordsman. And we have another class called as General, which is again extending from game, right? What we are going to do here is we're going to say that general object of this general class don't support cloning. Maybe the general unit is a unique unit in our game and we do not want to accidentally create copies of this particular object. All right. So let's start implementing our prototype design pattern. To begin, we are going to implement a marker interface called as Cloneable. So Cloneable is an inbuilt Java interface, which indicates to the user of this class that this particular class supports the cloning operation. Now the actual method which clones our object, we are going to override the clone method from object class and this method is going to provide the copy of this particular object. Now we're going to do two changes to this method. First of all, we are going to make this method public so that it can be used by classes which are outside of this particular package. Then we are going to use the covariant return type, which is a feature of Java, where we can return a subclass or child class of object. And here we're going to return game method. This is going to save cloning on the in the code which is using our clone method. You will see we have a throws clause here and we are saying that clone not supported exception is thrown from this method. We are going to keep this throws clause so that we can use that in classes which do not support cloning. Now, for time being, I'm going to say return null here so that we get rid of this error message for time being. Now let's start the cloning operation. The first thing that you need to consider is whether you need a shallow copy or deep copy of your state. If you look at the state of our game unit, we'll see that we have only one proper 80 in our class called as Position, which belongs, which is a type of point 3D. Now Point 3D class, which is from Java package is an immutable class. That means once you create an object of point 3D, you cannot modify it, which means we can get away with shallow copy. Now to do a shallow copy, we are going to use the clone method from the object class. So we are going to simply say super dot clone and this will call the clone method implementation that is available in object. Right? But we need to store the reference to the returned object here. So I'm going to call this method unit. Now the clone method returns an object, so we need to cast the return value to game unit. All right, so now here we have another object called as unit, which is copy of this object and it is a shallow copy. Next, we need to even though this position is a shallow copy, we need to make sure that our object state is reset when we are returning a clone. All right, so this game unit may be located to some location on the map. And when somebody clones this object, we want to make that copy located at the origin. So in order to reset our state, I'm going to write another method here, a protected method called as initialize. So initialize method will have one job, and that is to reset the state of current object. So it's going to say this dot position and you are to get 0.3 D which is located at origin. We can use a constant which is defined in the point 3D object called as zero. All right. So this way we have now reset our position to origin. Only thing is we need to call this method on. The story on the cloned object. So I'm going to say unit dot initialize. And here we have reset our cloned object and I'm going to return that to the outside world. Now, you may think that this is enough for it, but if you look at our child classes, you will see that they have their own state. So we have a property called state here and they need a chance or they need a way to reset that property because we can be cloning a swordsman. And the default implementation of clone method, which is provided in game unit is resetting only the properties or state which is defined in the base class. So one way is that they can override this initialize method, but that way they need to now handle the resetting of state that is defined in base class. Instead of that, what we're going to do is we're going to define one more method, one more method called as reset. And inside this reset method, instead of providing an implementation, we're going to declare it as an abstract method. All right. So this way the child class has to now provide an implementation for reset, and we're going to call reset from our initialize method. All right. So once again, we used the clone method from the object class because we wanted a shallow copy. We wrote a new method called as initialize, which would reset the state that is defined in this base class. We have given and we have defined an abstract method that subclasses our child classes will override. And we're going to call that reset method from our initialize. All right, so this is how you can implement Clone. Now let's go to the Swordsman class and here we need to now override the reset method from the game unit class. So inside reset method, the only thing that we need to do is to make sure that the state that is present in our swordsman is reset. So I'm going to simply say state is equal to idle. All right? So even if somebody calls an attack method on our swordsman, his state will change to attacking. And if he calls clone on our swordsman, it's going to create a new swordsman which is in idle position. Uh, same thing into general, but we discussed that we do not want to support the cloning operation in general. So how do we do that? Well, we are going to override the clone method from our game unit. We also need to override the reset because that is an abstract method. But in the clone method we are going to throw new clone not supported exception and we can pass along a message saying that Sorry. Generals are. Unique and in the reset method we are going to throw unsupported. This method should not get called, but we still are going to throw an exception to indicate that reset not supported. All right. So here we have unsupported Operation Exception. All right. So in this way we can have a class that extends from our prototype base class but still decide that it doesn't want to get cloned. In that case, it will override the clone method from our base class and throw the clone Not supported exception. And this is why we had this throws clause because the client, which is going to use these classes, is aware that there could be a subclass which can decide to throw this exception. All right. Now the only thing remaining is to go into our client code. We have a main method here and we are going to first create the initial instance of our swordsman by hand using the constructor. So this is the first instance that we are creating using constructor. Then we are going to, let's say, change some of the properties of this method. So we are going to, let's say, move our swordsman into sorry, move our swordsman into, let's say negative X direction or minus ten, zero x, y, z. And the distance that it travels is, let's say 20. All right. Oh. Right. All right. So we have now moved our swordsman and let's call the attack method here, and then we are going to print the current state of our swordsman. So if I run this program now, run as a Java application, you will see that we get a message saying that our swordsman is attacking at this particular position. Now let's create another swordsman. S2 But here we are going to use our existing instance S1 and call the clone method. Now, remember, we need to cast the return type or return value of our clone operation to a swordsman. And we also need to handle the clone not supported exception. So for time being, I'm going to add the throws clause in my main method so that we have a much cleaner code and let's check the default values or the state of our cloned object. All right. And to differentiate, I'm going to add a message. So saying that this is a cloned swordsman. And let's run our application one more time. And you will see that we still have our original swordsman, which is in attacking state. At this location. However, the cloned swordsman is in idle state and is located at Origin. All right, so this is how you can implement prototype in your code.
