# State - Implementation

All right, Now it's time to jump into Eclipse and see how we can implement state design pattern. So I'll switch over to Eclipse now. All right. So we have some classes here. These are exactly the same as we discussed in the previous video when we discussed the UML of this example. So to recap, we are going to use an ordering system and the order class is going to be our context class. That means we are going to represent State of our Order class in a using a state design pattern. So each possible state of our order is going to be a separate class in our state design pattern. We have few operations here. We have a cancel operation payment, successful, dispatched and delivered. So these operations are called on our order object from outside. So our payment gateway, the code which interacts with our payment gateway, is going to call payment successful when the payment is actually done. Our code, which talks with the courier service, is going to tell us whenever order is dispatched and when it is delivered, customer is going to call cancel method on our order if you want. And we are going to handle what happens when our order is canceled and the behavior of cancellation is heavily dependent on which state our order object exists in. For example, if our order is a new order or the state of order is new, that means payment has not been done. And of course we haven't dispatched anything. And if customer cancels that order, then there is no processing needed or no communication needed with external systems. However, if the order is in a paid state and we are preparing for dispatch and the cancellation happens, we need to contact the payment gateway to rollback the transaction so there can be multiple things that need to happen whenever order object is canceled. And this is a great example where state design pattern can be used because the behavior of cancel is entirely dependent on which state our order object exists. What happens in a paid order state is different than what happens if our order is in a new state. All right, so let's start implementing our state design pattern here. To begin with, I have created an empty interface here. Order state and this will be our abstract state. So we are going to have multiple classes implementing this state interface, which represent multiple values for our order State. So since we are only going to handle the cancel method, we need to provide a method in our order state that will handle the cancellation. So I'm going to name this method handle cancellation. All right. So coming back to our order object, which is our context object, we are simply going to add a field here, private field of type order state. And I'm going to name this field current state. All right. And in the cancellation, what we are going to do is we're going to say current state dot handle cancellation. So we have delegated the entire cancellation to the current state object. I'm going to store the return value here, and this will be the cancellation fee that is charged to our customer or customer who is canceling this order. All right. So this is how we can delegate to the current state operations or behavior of our context object. So first of all, let's see what happens in multiple states. So let's take a new state here. So I have created an empty class, new. And this class represents a state value that our orders object can exist in. So our object order object can be in a new state. So to begin with, we are going to implement the interface order state so that this class becomes a qualified object or qualified class, which can be set in our order. Now I'm going to generate the method stub for handle cancellation. And now here we're going to write code which describes or which carries out things which are needed when we are canceling our order. So what happens here? So we are not going to do any actual processing. Of course we're going to print different messages to indicate different things are happening in the cancellation. So in the new state, we are going to say that it's a new order. All right. And no processing is going to be done. Okay. So we're going to say no processing done and we're going to say that the cancellation fee is zero because this is a new order. We are not going to charge anything to our customer if the state or the cancellation happens before doing any payment. So this is how we can implement a state. So we have one step ready which can set which can be used in our context object. Now let's move on to the paid state. So we have again, another class called as Paid. And this is again going to implement our order state interface. I'm going to generate the method stub here for handle cancellation and in if our order is paid. So this class object of this class will represent a state which means that our orders payment has been done. So we need to contact our payment gateway. So we are going to simply print a message saying that contacting payment gateway to. Rollback transaction. In actual real life example, you will be actually creating some message, passing it on to the message queue where another piece of code will be handling the actual cancellation. Here, however, we are simply going to print messages saying what we are doing now. So we have handled the paid state and let's return some value which will be the cancellation fee. If cancellation happens whenever order is in a paid state. All right. So you will now see how we can have or how we can get different behavior, because if our current state is set to an object of new order or the object of new class, then our cancellation will not do anything. And we will simply say that you have zero cancellation fees. If our current state is set to an object of paid class, then cancellation will actually contact payment gateway, or at least it will print a message saying it is contacting payment. Gateway and customer will be charged $10 for cancellation and so on and so forth. We can keep adding multiple states. But one thing that you should so that takes care of one aspect of the state implementation that what happens in different states. One important aspect that is still missing from our implementation is how the states transition from one state to another. So our order will obviously start in a new state. So when should our current state get set to paid state? So we are going to do that in these methods. So we have a method here called as payment successful. So what we are going to do here is we are going to say current state is equal to new paid. All right. So this is how we are going to change our transition from one state to the next state. So if our order was in a new state and payment successful is called, then we're going to move our order object to paid state. Similarly, I have already written some code for in-transit state. So this is again a very simple class. I will explain again. So here we have an in-transit class that will simply print a message that we are going to contact the courier service so that your shipment can be stopped. And it's also going to contact the payment gateway for rollback of the transaction. And we have a different amount as our cancellation fee. On the similar lines, we are going to have a delivered state. All right. So let me remove this. So in the delivered state, we are going to contact Courier Service for picking up the delivered items. And once the items are with us, we are going to inspect them and then we are going to initiate the payment rollback. However, this time we are going to have a different amount for cancellation fee. So this is how we can keep on adding new states in which our context object can exist and define behavior which is specific to this particular state value. So as we transition from new state to paid state in payment, successful in dispatch state, we are going to transition from our paid state to the in-transit state. All right. And in the delivered method. So this method delivered method will be called whenever Courier service notifies us that the item was successfully delivered. So that means our order state is now delivered. All right. And since we are setting the state to the current state value, if somebody calls cancellation while these steps are executing or if our payment successful is done and somebody calls cancellation, our implementation of cancellation from paid class will be used. Same thing happens if our order is dispatched but not yet delivered. That means our state is in transit and if somebody calls cancellation, the cancellation process or implementation, which is specific to in transit, will be executed. All right. So this is how we can add new states to our application. Sorry, add new states to our application and we are going to use these states. So let's go to the client code here. So before we do that, let's talk about the cancellation state. I forgot about that. So we have another state called as cancelled and cancel state is what happens whenever cancellation method is done. Because remember, currently when somebody calls cancel, we're simply going to execute the code for cancellation, return the charges. But our state still remains to whatever state it was in. Okay. So if somebody calls cancel and if we are in paid state at the end of cancellation, we are still in the paid state. So let's change that. To do that, we have another state class called as Canceled. So let's implement this class so it again implements the same interface or state and let's handle the cancellation here. So what happens if somebody calls cancel on already canceled object? So we have a couple of options here. We can have a null state. That means this state will basically do nothing. Okay. Or we can throw an exception indicating that whatever code called cancel on our cancel state may be having some defect or it is not checking the current state of our order. So depending on your specific needs, you can implement this cancel state. We are simply going to say that. All right. We're going to say we are going to throw new illegal state exception because we expect that nobody should call cancellation on already canceled object. And if that call happens, that means there is some logical error in our application. All right. So the code, which is displaying the option to cancel to our user is probably wrong because if the order is cancel, then user should not see the cancel button. And to find out these types of bugs, it's always a good idea to throw exception so that that problem is highlighted. Okay, so we're going to send out some message here. Canceled order. Can't cancel anymore. All right. So this is our canceled state. And lastly, we need to come back to our order state. And whenever cancellation is done, that is our handle cancellation method is executed. We are going to set our current state to the canceled sorry to the. Cancelled state. All right. So once our state is cancelled and if somebody calls cancel, then we are not going to call any handle cancellation once again. All right. So this is how we can use or implement state design pattern. Now let's jump to the client code. So here I have a simple main method. So what I'm going to do is we're going to use this order class. So let's create a new object of order class. All right. Now here is our first one more point that we should consider that who gives our object state? We can have our client, which is creating our order object, give the initial state or our order can decide what is its initial state. We are going to go with the second option. So our order object itself is going to say that when it is created, it is going to exist in a new state. All right, so order object is now created and let's call order dot cancel here. All right. So our order is going to be in a new state. So let's see what happens. So I'm going to run this class as a Java application and you will see that we get a message saying that it's a new order, no processing done. Now, next step, what we're going to do is instead of canceling immediately, we are going to say order, dot payment, successful. All right. So this means on this line, we are going to transition to new state, which is the paid state, and let's cancel after our payment is done. So I'm going to now call order dot cancel. So let's run this class once again and you will see we get a different message saying that it is contacting payment gateway to rollback transaction. So you can in this way you can keep on calling different operations which will result in our order object moving from one state to another. And when we call the cancel operation, again, depending on what is the current state, we are going to get a different output. Okay. So if our order is in dispatch state, we're going to say that we are contacting courier service and your payment will be rolled back. All right. So this is how we can implement and use state transition state design pattern. Now, one thing I would like to highlight is that in real life implementation, a lot of things and a lot of corner cases need to be handled. For example, we are not checking if our current state is paid before moving to the in-transit state. All right. So you need to handle those kind of validations while you are triggering state transitions. We haven't done that here to keep our implementation simple. But once you understand how a state pattern works, you can easily implement it in any kind of situation.
