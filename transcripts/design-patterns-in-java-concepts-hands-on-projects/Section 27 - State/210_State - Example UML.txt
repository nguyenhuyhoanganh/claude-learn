# State - Example UML

All right, Now it's time to look at the HTML of example that we're going to implement in Java. So we're going to use the same example that we used when we were discussing steps of this design pattern. So first of all, we have a class here called As Order, and this is going to be our context class and the order is going to exist in multiple states, and these states are represented by these concrete classes which all implement our order state interface. So we have these four distinct values for our order. State order defines multiple operations in it. So we have cancel payment, successful and many other operations defined here and we are going to take care of only the cancel operation here. So if somebody calls cancel on our order object, then depending on the current state, one of these implementations of handle cancellations will be called. So cancel is going to delegate to the handle cancellation and different things are going to happen based on which concrete class or which object is currently set as our order state. The three methods below payment successful, dispatched and delivered. We are going to use these methods to trigger our state transition. So in this example we are using context object to transition to next state. But as I already explained, you can as well use these states themselves to transition to next state. So there are two ways that you can do a state transition. We are going to use a way where context class decides when its state changes to the next value. All right. So let's jump into Eclipse and see how we can implement this design pattern.
