# Command - Implementation

All right, let's jump into Eclipse and see how we can implement command design pattern. So I'll switch over to Eclipse now, and I have written a few classes here that we are going to use while implementing this command design pattern to save us on some time. All right. So command design pattern is one of those pattern, which is pretty easy to implement and it still provides a tremendously powerful feature while implementing or designing your application. So let's look at these classes that I have already written. So first of all, we have service. So this is a dummy class that would contact our exchange web service in order to carry out the commands that we give to this service object. All right. So we have a Add member method here that needs an email address and the distribution list. And we are going to add this email address into the distribution list by contacting exchange. Of course, there is not an actual exchange server running here. We're simply going to print a message saying that we added this contact to contact group. So this class of object of this class is going to be our receiver object. So remember in command object, we need a receiver on which the command will call operations. Next up, we have the command interface. All right, so we're going to start here our implementation. So as we discussed, command interface is typically quite simple. It simply provides a single method called as execute. And the reason it is so simple and the reason our execute method doesn't take any argument is because all the information needed by a command implementation is given to that instance when it is created. And one another reason is that the invoker, that is the code which is going to actually execute this command can be somewhere else. Okay, So it may not know what the receiver was or what the arguments were supposed to be. All right. So we have this command interface and that's it. We are done with the interface. Now it's time to implement this. So I have created an empty class here called as Add Member Command, and this class represents a Add command on our exchange server distribution list. All right, so let's implement this class. So we'll start by implementing our command interface that we just created. I will quickly generate the method stub for the execute method. All right. So as I told you, command should have everything it needs in order to execute itself. Okay? It should not ask for anything in its execute method. That means we need to find out what we need. So if we look at the service. So this is our receiver object. So you will typically study your receiver object, find out which method you are going to use, and get all the information that is required to call this method in our concrete command. So I'm going to quickly declare a few fields here. I'm going to have an email address that will be added to. The distribution list. And so I'm going to have a list name. So we now have the arguments that we can use to call the Add method of service. Last thing we need is the actual instance of the service. So remember, command object also gets reference to the receiver object. So I'm going to declare another variable AWS service and let's name this receiver so that we are clear on what this does. All right. So now we need to get all this information in our constructor because that is the only place that you should typically ask for information needed by your command. So I'm going to add a public constructor. We're going to declare we're going to expect an email. We're going to expect a list name. And lastly, we are going to need reference to the AWS service. All right. Typically what you can do is I might have already said this, that the service that we are using, we can actually locate that at runtime. Okay. So our command can locate this service using facilities of the container in which it is running. So for example, if you are running in a Java enterprise container, then you have Jndi or you also have a CDI. If we are running in a spring container, spring IOC container, then we can use the application context and use that to get hold of the bean that we need. Okay, so receiver object can be optional but is not optional is the arguments because these arguments will be will have some value which are given to us by the client anyways, in the constructor we have a simple code. We're going to say email address is equal to email, we're going to store the list name and then we're going to store a reference to our receiver. So in this example, we are not going to use any lookup to locate our service. We're simply going to ask our client to give it to us. Now let's implement the execute method. So what this what this command represents is an Add operation on a distribution list. And to do that, we are going to use our service and we're going to call the Add member method here. So I'm going to simply say in our execute method receiver dot add member and we're going to pass it the email address and the list name or the distribution list name. All right, So this is our concrete command ready to be used. All right. So what I'm going to do is I'm going to explain to you this one piece of code here, which I have already written. So this class mail task runner, this class represents an invoker. Okay? So what this class does is that when we create an object of this task runner, it's going to start a thread which is private to this class, and this thread is going to check the pending commands on a particular list and it's going to execute those commands one by one. Okay. So if you look at our run method that this thread is going to use, it's going to wait until some command becomes available. Once it is available, it's going to call the execute method. So this is right here. So it's going to get that command from list and call it's execute. Okay, so this is our invoker. Now I have purposely written it as a separate thread so that you get an idea of how this design pattern is typically used. Okay. So having command pattern in a multithreaded environment is common design pattern implementation that you will find. All right, then we have provided a simple method called as Add command and client is going to submit command to our invoker using this method. And here we're simply going to add that command to list. One thing you will notice is that. Uh, while we are running, um, uh, this thread loop, we need to use synchronization because we have now multi threaded environment. We have our main thread and this worker thread which is running. So that's why you will see that I'm using the synchronized block here. That's the only purpose here. Okay, so it's not part of the command pattern. Okay. So that's why I have already implemented this, implemented this class so we don't spend much more time on it. All right, so. We have this execute command. One thing I'm going to do is now I'm going to jump to the client and we are going to start creating our command. So in your client code, you will start with creating a command. And we're going to say we want Add member command. We are giving going to give this command some email address. There is no validation there. But. Okay. Okay, maybe Sam and we need a service instance, so I'm going to create my service instance here. It's a simple class. You would typically use a locator of some type like jndi lookup or something like that. All right. So we now have created our command, so you will see that the client is providing all the information. So we want to add this email address to distribution list named as spam. All right. So once our command is created, client can send this command for execution to different part of our code and we're going to simulate that using our mail task runner class. So we have a sorry, mail task runner tasks. Runner get instance is the singleton and we are going to call it add command method and going to give it C1. All right. On the similar lines, we can create one more command. So let's create a C2 command. And this is again going to be at member command, the ADB and. Let's add it to spam again and use the same instance. We are again going to use mail tasks runner get instance dot add command C2. All right. So we can keep on doing this. Typically you would have a user interface or it could be a controller in your MVC architecture, which is creating these commands based upon the input that is received from user and submitting those for execution to a different part of your application, maybe submitting it to a different thread, or maybe it is sending those objects to some form of a queue. All right. So once this is done, our our task runner is going to run. What we're going to do is we're going to simply call Thread.sleep here to give some time to our worker thread to finish its job. Okay, so I have added Thread.sleep. I'm declaring this throws exception throws exception clause. All right. And then we are simply going to say, you know, shut down our worker thread. So I have given a shutdown method. All right. And now let's run this code as a Java application. All right. So you will see that we simply created the command and we submitted those commands to our worker thread and our worker thread executed those commands. All right. So this message that you are seeing added this or this to spam, it is coming from the service. Okay. So if you remember, I showed you that we had only system out here. So our command is actually calling the Add method add member method on the receiver. All right. And you will see that we have these two commands that are executed. And at the end we are interrupting our runner and stopping our worker thread. All right. So it's quite simple to implement this command pattern and it's quite powerful. You can do a lot of quite interesting things using this command pattern. All right. So you can keep on adding several commands. You have to pay attention that all the information that is needed by the command should be provided while we are creating this instance, because that gives you a flexibility of passing this object. So the C1 object that we have created is now a complete object. Okay? It's an immutable object in the sense that whatever it needs is already given to it. We cannot change its arguments, okay? Or we cannot change the receiver of that command. And one benefit of having such a complete object is that we can send it anywhere. And the code which is going to execute this command simply needs to call the execute method. Okay, this code doesn't know what is happening in that execute method. All right. So this is the command design pattern in Java.
