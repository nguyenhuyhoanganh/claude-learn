# Command - Pitfalls

All right. Now let's take a look at pitfalls of command design pattern. Now, there is only one area of this design pattern where things become a little bit tricky, and that is about when, when it, uh, when we have to handle errors and return values because of or that are resulting because of execution of our command. The things we get a little bit controversial on how to handle errors that are thrown when we execute a command so our receiver might throw an error. Okay. Or we may want to get hold of the return value of an operation that is run by our command. So how can you do that? Well, error handling is difficult to implement without coupling your command back to the client code, which created that command. Okay. Because the question now is who wants to know about the error? Typically it is the client, it is the code which is creating these commands who may want to know that one of its command failed and the return value for return values. Again, it's going to be the same situation. Client may want to know what happened and what is the result of the commands that I sent you. And this logic is a little bit difficult to implement without coupling our command back to the client code. But the whole purpose of this design pattern was to decouple our client code, which is creating the command and the code which is executing it. Okay. So error handling without coupling back to the client is a little bit difficult. And especially if we are using Multithreading and our commands are executed in a separate thread. And believe me, that is quite a common scenario when using the command design pattern, because this design pattern naturally lends itself to those kind of situations where we have a worker thread, where we have a user interface thread and user interface is creating these command instances and sending it to the background runner, which is executing these commands. Okay. In those type of situations, error handling and returning values can become quite difficult. Okay? So you can brainstorm how you can do it. Maybe you can use some kind of event handling mechanism. You may use queues in order to decouple the invoker and client. Once again, use the command IDs to find out. But the point still remains that it's not that easy. Okay, Unless you actually have a reference of client in your command, which would be idiotic and call the command, call the client code to tell that, okay, I'm done finishing. Okay. That's bad design because it defeats the whole purpose of how command design pattern is meant to be used anyways. These are some of the pitfalls and these are some situations you should really think about before you start implementing this design pattern in your application.
