# Command - Comparison with Strategy

All right, Now let's compare our command design pattern with another design pattern called as strategy. Now it is discussed in a next few lectures after next few lectures. So you can watch that lecture and then come back to here. Or if you want, you can continue as it is. So command design pattern contains which operation is executed by the receiver. So if you looked at our design pattern, we need an axis or we need a reference to the receiver receiver object which actually contains the functionality. Command itself only knows which operation to call and it knows what arguments to pass. It doesn't implement any logic in it. Okay, so the commands are pretty simple strategy Design pattern, however, contains how the operation is to be carried out. Okay, so strategy in strategy design pattern, we actually implement the logic. Okay. The command design pattern encapsulates an action. Okay? So it represents an operation or an action that is called on the receiver strategy design pattern encapsulates a particular algorithm that can be used to solve our problem. Okay. So this is these are a couple of differences between command and strategy design pattern.
