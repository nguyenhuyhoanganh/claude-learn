# Builder - Implementation Part 1

It's time to go into Eclipse and implement this builder design pattern. Now, what I have done here is that I have written some of the classes that we are going to use while implementing our builder so you don't have to see me writing all of this code. So I will go through all these classes one by one to explain what these are and how we are going to use them. So first of all, we have a user class here. You can think of this class as an entity class, something whose object you will get from a persistence layer and which is stored in a database. And we are going to use this object in order to construct our data transfer object. Next up, we have address class. Now, this is a regular value class which is used inside our user entity to store the address information for our user. Next up, you will see we have user web DTO. Now this is the product class in our builder design pattern. That means we are going to create objects of this class in our builder. We will see that we need a name of the user, we need address and age of the user in order to create objects of this class. Next we have builder. Now this is the abstract builder. Now here you will see, as we have discussed, that builder provides methods that allows the user to build parts of our object. So you will see we have these methods with, with first name, with last name, with birthday, with address. So these are the methods that build up part of our user web. DTO One thing you will notice that all these methods return reference to builder itself. So we are using this type of implementation which allows us to do method chaining. In case you don't know what method chaining is, I will explain it. When we are actually implementing the director, you will see we also have a build method. Now this is the method which assembles our final object and this method returns the object that it has built. We have also provided a get user method. So this is the query method and this method will simply return the already built DTO object. User web builder is the concrete builder, and this is what we are going to implement in this video. So to start off, we are going to first implement the Builder Abstract Builder interface. I will quickly generate the method stubs for the unimplemented methods. Now first method that we have is called as with first name. So we are going to get first name of the user as a parameter in this method. But if you look at our product class, you will see that we need a complete name. So we have no use of first name only. We need both first name and last name. So whenever you have such a situation where you cannot directly build the part, what we do is we store this information that is given to us. And inside our build method we are going to create our name string using both first name and last name. So to hold on to this parameter, I'm going to declare a field inside of our builder. I'm going to give it a name called as first name and inside this method, since we cannot build the name right away, we are simply going to store the past parameter. Now, how should we return the builder instance? We are simply going to say return this in the same way we have a with last name method. So we are again going to store the incoming value of last name so that we can use it later in the build method. And we're going to say last name is equal to name. Again, we are going to say return this Now we are we have a method called as with birthday. So we are going to get a local date which represents the birth date of the user. But if you look at our product class, you will see that we don't have need for the birth date. We need age of the user and that is in string format. So here is a chance that we can build part of our final object. So to do that, we need to calculate the difference between the user's birth date and the current date. To do that, we are going to use a class called as Period, which is inbuilt Java eight class, and we are going to say age in years and this is going to be sorry. This is going to be a difference between the user's birth date and the current date. So we get hold of the current date using local date Dot now. So this is a static method. Now is a static method that returns the current date. So now we have the difference between two dates. Now we want our age to be expressed in terms of years only. We are not really worried about the months in that difference. So what we are going to do is we are going to only take the years, the difference in terms of years. So I will say that I'm going to first declare the variable or the sorry, the field which will store the age of our user. And I'm going to say age is equal to now if you directly say get years, so get years is going to return us an int. So we need to convert that into a string. So I'm going to use the wrapper class integer and convert the difference in years to a string. And lastly, we are going to say return this. Next up, we have a method which gives us the address object. Again, if we look at the final product, you will see that we have a string address. So we need to build a string using this address object. So first of all, I'm going to say that we are going to have an address field inside of our builder. Next up, what we are going to do is we're going to simply build up the so I'm going to say this Dot address is equal to let's use the fields inside this class in order to build our address. So I'm going to say house number. And restored. Market Street. Then I'm going to. So I'm simply building up the address, final address string that we are going to use. Get city. Good. All right. So now we have built another part of our final object, and that part is the address string. And now again, we are going to say return this. So now we have the build method. Now this is the method that is going to create the user web object. So we are going to store this build object. So I'm going to say private user web DTO, and I'm going to simply say DTO and we are going to say sorry. D2 is equal to new user web D2. Now you will see that we don't have yet created the name. So what we are going to do is we are going to now use the first name that we have stored previously and the last name in order to create the user name we have already have access to address. So we have already built this part and we have also built the age part. And this is our object that is complete. Now, remember, this method returns the build object and not the builder instance. So I'm going to say return D2 last method get user D2 is simply going to return whatever we have already built. All right, so this is how you can implement the builder. We are going to build up the parts of final object where it is possible in case we are getting partial information. For example, in the first name, last name methods, then we are going to store that information and create it, create that particular part in the build method. So our builder is now implemented. So now we are going to use this builder and to do that we are going to need a director. So this is the client class and you will see that we have a main method here. I have written a create user method, so this method simply gives us a user object that we can use in our example. So how to do, how to write the director. Now we are going to not we are not going to create a separate class for the director. We instead what we are going to do is we are going to create a private static method that works or that plays the role of director. Now this method is going to return the DTO that is built and I'm going to name this method direct build. All right, I'm going to simply put return null for the time being so that we get rid of the errors. Now this director needs a concrete builder instance, so we need to have a parameter. We're going to declare it in terms of the abstract builder. And we are also going to need the user entity because remember, we are going to build the DTO from an existing entity object, which is the user object. All right, So now we have all the information that we need in our director. We have a builder and we have the information that we need to create the object. So what we're going to do is we're going to start using our builder. So we're going to say with first name and for first name, I'm going to get the first name from my user entity. Now remember I said that we are going to use method chaining. And that's that was the reason, given that we have these builder as return type of our methods inside the builder. So what is method chaining? So since with first name returns the builder instance itself, we can directly put Dot here and call the next method on the builder. So this ability to chain one method after another is what is called method chaining, and it makes it easier to write a lengthy piece of code so we can now build the next part, which is the last name again, I'm going to get the last name from our user entity. I'm going to hit Enter and continue on the next line. We are going to next provide the address, get, address and. Sorry. On the next line, we are going to give the birthday and that is the user dot get birthday. All right. So now we have provided we have built all the parts using the builder. The last step is that we need to now build or assemble our final object. And if you remember build returns, if you go to the abstract builder, you will see that build method itself returns the final build data transfer object. So we are going to simply say return and return the output of build method. All right, so this is our director using the builder and the user entity in order to create our web DTO. Now all we need to do is just use this directory. So to start off, I'm going to get a user object from our create user method. Typically you will be getting this object from your persistence layer or from a service. So we now have user. Next, we need to have a builder that director can use. So I'm going to say user builder. Builder. Is equal to new user. So we need to give the concrete builder class name here. All right. So once that is done, we can directly call the direct build method and give it the builder and the user entity, and the output will be the DTO object. All right. So this is how we can use the director inside of our client code. Lastly, we are going to do a system.out in order to see what our DTO has inside of it. All right. So to run this, I'm going to say run as Java application and you will see that inside this console, you will see we have the web DTO object completely built and we have all the properties of the user entity populated inside of this object. All right. So this is how you can implement a builder pattern in Java.
