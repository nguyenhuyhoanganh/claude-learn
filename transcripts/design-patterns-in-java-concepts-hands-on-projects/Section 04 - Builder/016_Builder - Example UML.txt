# Builder - Example UML

All right. So before we can start coding in Eclipse, let's first look at what we are going to build. So here is an UML diagram of the example that we are going to try our hands on. You will see that I have here an interface or an abstract class called as user DTO. So DTO stands for data transfer objects and user. DTO is a data transfer object for user entity. Now user Web2 is an implementation or a concrete data transfer object, and this is what we are going to build. So user web DTO is our final project to create that web. DTO we are going to implement user web DTO builder. So this is our concrete builder that we are going to write in our code. User DTO builder is an interface or an abstract builder that has simply defined the methods that correspond to each part of the user. DTO object. Client will be simply a main class with a static public static void main method and client itself will play the role of director in our example. These classes that you see in orange color are examples of how we can further enhance this example. So as we have provided user web builder, we can also optionally provide another builder which implements our user DTO builder and build another type of object. So this is how you can extend your builder design pattern. All right. So this is the example that we are going to follow in the next lecture.
