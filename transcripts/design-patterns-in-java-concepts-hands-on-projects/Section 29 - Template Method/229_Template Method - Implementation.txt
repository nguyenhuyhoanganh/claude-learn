# Template Method - Implementation

All right, Now it's time to jump into Eclipse and see how we can implement template method design pattern. So we are going to use a couple of classes that I have already written. These are exactly the same as we have seen in the example UML video. So I will explain what these are. So first of all, we have an order class and this is the object that we want to print using the template method design pattern. So this class has some information in it that we want to print so it has an order ID, the date on which this order was placed, a collection of items where we have an item, name and price that is associated with it. And finally a total which represents the total amount that this order amounts to. All right. So we want to print all of this information of our order in a particular file. And to do that, we are going to use and use a template method design pattern. So here we have a class called as Order Printer, which will serve as our abstract base class, which will define this template method. So to begin with, we are going to write our template method here, which will be called by the client. So we are going to have a public void print order method. Now this method is going to need the order object, which is to be printed and we are also going to need a file name where this order will be exported to. So this is the file name which will be created by printing this order. Now let's implement our template method design pattern. So first of all, we are going to need a file writer so that we can write out our contents of our order object. So we are going to use the try with resource mechanism of Java. So we are going to use print writer. Writer is equal to new. Brink writer and we're going to give it a fine name. Okay. And let's import this class. It is in Java package and we are going to simply add the throws declaration in our method. So we're going to throw ioexception and let the client handle or take some action. If our order printer is not able to create or create this file. So now let's start printing our order. So first of all, we need to print some sort of header or we want to give our give out some initial header information and we're going to write that header information using our writer. So I'm going to create an abstract method here. And this will be a protected method because we want our subclasses to use this method and we're going to call this method start. So job of this method is going to be to return the header for the file that is being created. So we need to declare this method as an abstract method because we don't know what each file type may want to put there. All right. So this is a since this is an abstract method, our class also needs to be an abstract class. Sorry. Abstract class. All right. So let's use our writer and we're going to say writer dot print Ln and we're going to call our start method inside of it. All right, so this is how this template method typically works. So we're going to break down our algorithm into multiple steps, and each step is going to be an abstract method in this order printer class. So moving on, we are going to next print the order number. So we're going to say print Ln and let's call these methods format because the subclass is actually going to return us some formatted string that we are actually going to write. Okay, so the printing is actually done from this template method. So let's call this methods format and we're going to pass on the order object here. So let's create this method format, order number, method. We are going to create a protected. Method. Abstract Method. Which is going to accept the order. All right. So here we have our format order number method. So I'm going to just move this down so that it is easy to read. So we're going to arrange these methods in the same way we are calling them. Next up, we need to print out the actual items in our order. Okay, so we're going to create a method called as format items and we're going to give this method again the order. So let's declare this step again as an abstract method here. We're going to expect a single string format items and we are going to pass on the order object. All right. So we have printed order number, we have printed the items. Then we we are going to print the total print line and we're going to say the method name is format total. Okay, let's create the abstract method here. Protected abstract string format total. We're going to create the order. All right. And lastly, we're going to call the end method that will allow our subclasses to give us some footer for this particular file. So the method name is going to be simply end. And this will be, again, an abstract, abstract method, right end. And that's about it. All right. So this is our template method design pattern in action. So we have this print order method, which is our template method, and this method is using or is declaring an algorithm. And in this case, the algorithm is pretty simple. It is simply taking an outer object and this algorithm is simply printing out the contents of that order object. However, the steps inside of our algorithm are implemented as abstract methods, which are subclasses are going to implement, and we are going to use that implementation. We can also declare our print order method as a final method because we do not want our subclasses to change how this algorithm is implemented. We want to be in control of about the algorithm. We are only giving the freedom about how the steps are implemented. All right, so now our order printer class is ready. Our template method is ready. Now let's create a subclass to implement these methods one by one. So. We have a class here called as text. Sorry. Text printer. All right. And in this class we are going to implement our template method design pattern. So we are going to extend from our order printer, right? And now we have to provide implementation for these methods. Okay. So I'm going to generate stubs for these methods and let's implement these methods one by one. So first of all, we need to return a string that indicates that our order is going to start. So I'm simply going to say that we are going to print order details. Okay, So let's make it a capital case. Okay. So this will be the header of our text file. Next. In the next method, we have to format the order number and we are getting an object of the order. So we are again going to return a string here. Return we're going to say order number and just append the ID of the order. Right. In case of format items, we have a map inside our order which contains these items. So let's declare a for loop here and we are going to say so the loop that we are going to say map dot entry. And this collection is of map is of string and double time. And the collection is get items. All right. And we have to call the reset method on this. All right. So we are iterating over the entries in our items map, and we're going to print these entries one by one and create a string that will be returned to the base class. So we are going to need a string builder now because we are concatenating across loop boundary. So let's create a new string builder. All right. And inside the stringbuilder, let's give it some header so that we will know that this is the items that are being printed. So I'm just simply going to add a couple of dashes here. Slash n. All right. Now using our string stringbuilder, we're going to append the item name, which is the key in our map, and then we are going to append the price. So let's add a dollar sign and use entry dot get value, which is going to return us our items price. And I'm also going to add a new line at the end. All right, So here we are, concatenating all the items that are present in the order into our stringbuilder. Finally, we are going to say filter, dot, append, and we are going to add this same line here. All right. So that it marks the end of the details. Order details and we're going to return builder dot to string. Okay. Next we have the format total method. Here we are going to simply say that print total. And let's give a dollar sign and append the order to. All right. And at the end, we do not want any footer here, so I'm going to simply return an empty string. All right. So this is our subclass, which is actually implementing steps that are needed for the algorithm, which is defined in our template method in order printer class. Now it's time to use this particular text printer in our client. So our client is going to be simply a simple class with a main method in it, and we are going to create an order object to begin with. So I'm going to create order equal to new order in the constructor. We need to give the order ID and let's add some couple of items here. Um. I mean, maybe a soda. 50. And I do. Let's see. Sandwich and add some price to it. Add another item. So. All right. So we have added a couple of items in our order so that our order print will look will have some content in it. Next, we need to create our order printer, so we're going to use the base class reference. However, we need to provide the actual concrete class in order to create the object of object which extends from order printer. And finally, we're going to simply say printer dot print order. We're going to pass the order object and let's create a file with name 1001. txt. Now print order is throwing an exception. So instead of handling, I'm just simply going to throw it back to the JVM. So if some error occurs, our program is going to terminate. But this works for us. So let's run this example now. So I'm going to say run as Java application. Of course we are not going to get any output here, so let's check our project directory. So I'm going to refresh this. Okay. And you will see that we have this file that is created. Okay. So 11. txt and you will see that it has all the items that we had added. We have a total here and all the steps that we had implemented in our text printer. So this is how you can implement an order printer. To further demonstrate this design pattern, I have already created another class or another implementation of this order printer. And that is. The printer. So I'll just quickly explain what is happening here. So here we have a class called as HTML printer that extends from our order printer and we are again implementing these methods. However, we have now something to return in the header. So we want an HTML header. Then we are going to use HTML elements to format the contents of this file and finally we are going to terminate that so we can use this class instead of text printer. So we can say order printer is equal to HTML printer and let's give this class a file name 1001. HTML. And if we run this, run a Java application and. Let's refresh our project. So I'm going to refresh it and we'll see that we have this HTML file created in our project directory. Okay. So you will see this is the output which is generated by HTML printer. All right. So this is how you can use a template method design pattern.
