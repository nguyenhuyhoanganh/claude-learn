# Chain of Responsibility - Implementation

All right, Now let's see how we can implement chain of responsibility in Java. So I'll switch over to Eclipse now. And here we are going to implement our chain of responsibility design pattern. So we already discussed the mission brief here, so I will briefly mention it once again. So if you are watching this video on its own, you will get some context. So we are trying to write a leave management system. Employees will apply for leave in this system. We have multiple approvers in our application, so we have different roles like project lead manager and director, and each role can only approve a certain type of lead. A project lead can only approve a sick leave if it is less than or equal to two days. Manager can approve the paid time off type of leave or a sick leave, which is more than two days. A director can approve a leave of leave of absence or loss of pay kind of leave. Okay, so that is our mission brief. So we are going to implement the solution for this problem using chain of responsibility. So to begin with, I have a class here, simple called as leave application. Now, object of this class is is going to represent the actual leave application created by our employee. So it has information about what type of leave this is, the duration of that leave. And it will also have a status for telling whether the leave application is approved or rejected or is still pending. Okay. We also have a field here called as processed by that is going to record which role approved or processed this leave application. Okay. We are not going to complicate our system by checking with leave balance or having an or something like that. Okay, let's keep it simple so that we can focus on our design pattern. So an object of application is going to represent a request that needs to be processed. So let's start our implementation. I have created an empty interface here called as Leave Approver. So this is going to be our abstract handler and this interface is going to define a simple method called as, let's say, void process leave application. Okay, So this method will be called by our client in order to process the leave application made by client, it is going to need the request object which, which in our case is an object of leave application. I'm going to name it application. I'm also going to add a method here to get the role of our approver. So I'm going to say get approver role, something like get approval role. Okay. So that we can find out who processed our leave. Okay. So we have our abstract handler ready. Now. We have already discussed that chain of responsibility handler or in the chain of responsibility design pattern. If a handler is not able to process a request, okay, If it is not applicable for that particular approver, he's going to pass it on to the next approver in its chain. Okay. So we need this code where we are checking if we can process the request. And if not, then we are going to pass that request on to the next approver in the chain. And this logic is going to be repeated in each handler. Okay? Each handler needs to check whether there is a successor. If it is present, then let's pass it on. If I cannot process it and this code is going to be common in all handlers. So what we are going to do is we are going to create an abstract class to hold this logic. Okay? So we are going to create an abstract class called as an employee. So I'm going to call make this class an abstract class because we don't want people creating just employee objects. We want them to belong to a particular role so they can be a project lead or manager or director. Now this abstract class is going to implement our leave approver interface. We are going to. Provide implementation for these two methods here and you will see how we can do that. Okay. So what I'm going to do is first, let's take care of the approval rule here. So what I'm going to do is I'm going to have a private field here called as, let's say, rule, and I'm going to return that rule in our get approval method. Okay. It's simple. A getter method. Now, how we can get hold of this rule, we are going to declare a constructor employee a public constructor, and we are going to accept the value of rule in our constructor. So this rule is equal to rule. We do not have any setter method because once a rule is assigned we do not want it to be modifiable. All right. So we have taken care of get approval rule here. Now we are going to need a successor. Okay. So we are going to need to know what is the next object in the chain. To do that, I'm going to declare another field here called as private. And this next handler is also going to be of type approval. And I'm going to call this next object successor. All right. Next, I'm going to accept the successor in my constructor. Okay. And I'm going to equate it. This not successor is equal to successor. All right. So we now have all the information that we need. Okay. We have a rule name and we know who is going to be next object in our chain. Now let's implement our process. Leave application method. Now, this particular class cannot check whether it can approve or not because that logic is going to be contained in the child objects of our employee. So Project Lead knows what it can do. Employee, which is an abstract class, doesn't know. So we need another method in our child classes which we can call from our employee. So I'm going to create a protected, protected method here that is going to return a Boolean and we are going to name this method process request and we are going to pass our existing request to this method. All right. And I'm going to mark this method. Abstract. Sorry. Abstract. So what is going to happen now is that any child class which extends from our employee has to provide a method implementation for process requests. It doesn't need to override the process, leave application because we're going to provide implementation for that here. So in our process request method, we are first going to call our process sorry, process leave application. We're going to call the process request method, which is implemented by child. So if our child object is able to handle the request, then we don't have to do anything. However, if it is not able to handle the request so child child class is going to return false if it cannot process our application and we need to check whether our successor is present. So if our successor is present, that means if its successor is not null, then we are going to hand this request over to the next object in our. So I'm going to call it's process leave application method and that's it. All right. So this is how we can implement the abstract lever prover. Okay. Once again, I will go through this method once again in case it was not clear. So we have implemented process lever application in this abstract class. But what we have done is that we have created another abstract method called as process request, which is returning a boolean. So we will call this method and child classes will have implementation of this method. And if they are able to process this request, that means if value of process, request return value of process request is true, we are done. However, if they are not able to process, then not they will return. False. So not false is true. And if there is a successor which is next to us, then we are going to hand our request to that next object. All right. So this is the simple implementation for our abstract handler. Now let's implement some concrete handlers. So I will jump over to Project Lead. So this is a concrete handler and I'm going to extend from employee. Now we need to provide method implementation for method implementation for our process request method. All right. So we need to implement this method. But first of all, we need to take care of our constructor because the employee constructor needs a role name and it also needs the successor. So I'm going to write a constructor here, public constructor for Project Lead, and we are simply going to accept the next approver in line, I'm going to say. Give us the next successor because we already know the rule name for this class. So we can call Super Project Lead and pass it the successor. All right. I have. Oh, sorry. All right, So this is our concrete class ready? Now we have to provide the implementation for process requests. So our mission statement is that this class is able to process a live application if the type of leaf is sick, leaves and the duration for leave is less than or equal to two days. Okay, so it can approve, leave sick leaves up to two days. So let's implement this logic. So we are going to say if. Our application. Has a type which is sick leave to leave application dot type. Dot sick, then we are going to consider whether we want to process it or not. Okay. If the application is not sick live then we are returning false. Okay. We cannot process that live. Then we need to again check whether our live application is less than two days or less than or equal to two days. So we have a method called as get number of days. And if those are less than or equal to true, then we are simply going to approve it. Okay. We are not going to check the live balance and all that. So we are going to say application dot approve and we are going to call the get approval from the base class. And finally we are going to say, okay, we handle this request. All right. So this is the implementation for Project Lead. It's quite simple. We're going to check for the criteria which qualifies this class to handle it, handle the leave application. If that criteria is matched, we are going to approve that leave and return. True telling our base class, telling our employee class here that, okay, our process request was successful and that is the end of our chain. However, if the leave application was not sick leave or if the number of days in the leave application were greater than two, we are going to return False. That means we are going to jump to the successor in our chain. So after Project Lead we have a manager and after manager we have a director and there is no one above director who can approve the leave. So next we need to implement the manager. So what I've done is I have already implemented the logic for manager, so we are going to go over that. Sorry. So we are going to go over the logic of this class. So let me remove the comments. All right. Implement. All right. Okay. So I have changed the. Method name. So it is now a process request method. All right. So let's look at the implementation of this class. So again, we have a constructor which needs a successor and we are calling our base class with roll manager and we are giving it the next approver in line. Then we have implementation of process request. Here we are going to check if it is a cyclic manager is going to say, okay, if it is a cyclic, I'm going to simply approve it. If it is a paid time off, then it's going to check whether it is less than or equal to five days. Okay. So this is another part of our mission brief. If that is the case, then it is going to approve and say return true for any other scenario, whether it is a loss of pay kind of leave or whether it is a paid time off for more than five days, we are going to say return false and the next object is going to be a director. So I will. I have already provided implementation for director here. I will simply remove the comments and I need to change the method name process request. All right. Again, it is a simple implementation. We are going to say the role is director and if there is any approver in future, if we add, let's say a CEO above that or CFO, that person is going to be the next approver. Then we are going to check whether the leave type is a PTO. Then we are going to say, okay, approve, okay, we don't have loss of pay approval here. Okay. I was going to add a CFO class, but I think our point here is to understand the chain of responsibility. And I think these three implementations are enough to demonstrate how a chain of responsibility works. All right. So we now have our concrete handlers ready. Now our job is to create a chain of these concrete handlers. So to do that, we are going to use the client class here. So I will switch over. So this is a class with simple main method. First job is we are going to need a method that creates a chain of approvers Leave Approvers and it returns the first object in chain. So I'm going to call this method, let's say create chain. All right, Private, I'm going to make it static so that we can call it from main method and we are going to begin creating our chain. So first of all, we are going to create an object of director. So director is equal to new director and there is no successor for our director. Then we are going to create our manager new manager and we can give our director as a successor to the manager. Okay. If you remember from our problem statement, if manager cannot approve the leave, it will go to the director. And lastly, we are going to create our project lead. And he's going to be having manager as its successor. All right. So this is how we can keep on creating chain of our handlers. And then finally, we're going to return our lead object. Okay. That is the first object in our chain, and that's all our client needs. It needs access to the first approver in our chain of responsibility. And once it gets to access to the lead, he can our leave application will be processed by all of these handlers. So in the client class, we first need to create an object of lead application, okay? Because we want to create request first. So I'm going to call this leave application and I have written a builder in our Leave application, get builder. And then we have. Different types. So let's say we are going to use a sick leave. And I'm going to hit enter and come to the next line. Okay, So I'm simply building the leave application here. I'm going to say with type from let's say today, local date dot now dot to, let's say local date. Dot from. Okay. So not from it is off. And let's say it is 2018 and let's say we are going to use. Month is two and this is two. And then we are going to say. Okay. I made a mistake here. I'm using local date and not local data. Sorry. And after that we are going to simply say dot build. All right. So I made a mistake here. I mistyped the local date. Okay, so we're going to create a local date, and then we're going to say, build our live application. All right. So we now have an object of live application. What we're going to do is we're going to get hold of the first object in our chain of approvers. So I'm going to say approver and I'm going to call my static method, create chain, and we're going to pass our live application process, live application and call it application. Lastly, we're going to print out the status of our application. All right. So first of all, I'm going to write or print out the live application before it is processed so we can see the difference. I'm going to add a separator here so that we can see different difference in the output. And this. All right, now let's run this class. So I'm going to simply say run as Java application. Oh, sorry. I made a mistake in passing the day first and then date. Sorry about that. Let's run this once again as a Java application. All right. So we have a sick leave for ten days and it is the status of that leave is pending when we created it. After that, once we go through the change, you will see that we have our sick leave for ten days approved by manager. So remember, our first object in the chain was lead object, but it couldn't process that application because it was for more than two days. So it went to the manager and he approved our leave application. All right. So this is how you can implement a chain of responsibility design pattern in Java. Okay. Typically you will have a base class because this logic of calling on the successor can be encapsulated in that central location or one location instead of repeating it in every handler.
