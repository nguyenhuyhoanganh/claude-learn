# Chain of Responsibility - Implementation & Design Considerations

All right, Now let's look at some of the implementation and design considerations while using or implementing chain of responsibility. Now, as is a general advice for any type of programming, always prefer interfaces. The same guideline applies here as well. If you have your handler defined as an interface, then any class in your application can implement that interface and act as a handler. And this will allow you to use your existing classes, which may be already extending from one of the class in your application act as the concrete handlers. Now handlers can allow the request to progress to the next object or the next handler, even if they are able to handle or process on that incoming request. And servlet filters typically follow this pattern. So even if a handler in the chain filter chain handles the incoming request, the next handler is still called. Okay, So that way you can build up your functionality, your desired end goal in multiple steps. So one handler will provide a lot of functionality. The second handler will do further processing on that request, so on and so forth. Okay, so passing on to the next object is completely in your control and you have to decide what happens if the request is handled. Now chain. The chain of handlers can be described using one of the XML or Json files, and this provides you with a great flexibility that allows you to create these chains handler chains dynamically at runtime. Okay, so in your running application, you may change up some configuration and the chain will be created with the newly added or removed handlers. Okay. That provides you with a great flexibility in how your program works at runtime. Now let's look at design considerations. There is only one. Sometimes you can think of using existing connections that are present in your classes or objects. You can take an example of composite design pattern. Now here we have an object with multiple children inside it, so you really readily have a object structure which has connection in them. So you can give the request to the composite and then it will call its children for handling that request. Okay. So you can use these existing connections which are present in your application code to implement the handler chain.
