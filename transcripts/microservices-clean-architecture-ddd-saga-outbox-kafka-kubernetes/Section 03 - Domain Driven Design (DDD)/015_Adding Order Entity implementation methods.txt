# Adding Order Entity implementation methods

Hi everyone. Let's continue with the implementation of order business logic. I will edit the public initialized order methods in the order class. First, I will set the orderID by calling super classes, setID methods, using a random UUID. I will use UUID, randomUUID methods for this. Then I will set the trackingID, again using randomUUID. Then I will set the initial status as pending, this is the status of an order when it is first created. I also need to initialize the items or that let's create initialize order items private method. In this method, I will use a local long variable starting from one, and use it to set the itemID. I will loop over the item list, and to initialize, it will make sense to add a new method in the order item class with a name initialize order item. So I will add this method in the order item with void return type. And pass orderID and order itemID as the parameters. In this method, I will assign orderID and call super class setID method to set the itemID. Then I will go back to order class and call initialize order item method for each item, and each time, I will increase the itemID by one. Actually, I can make this initialize order item method package private in the order item class, because I will not reach it outside of the package. It should only be called from order entity, so I remove the public access here. Second method I want to add in order class is validate order. Here I will check if order object is in correct state for initialization. I will do this with method validate initial order. Then I will check the total price with validate total price method. And finally, I will add validate items price method to check the order item price information. Let's create these three methods and implement them. I will use (indistinct) to create the first method. In validate initial order method, I will check if order status or identifier is null. These are the fields that I will set in the initialize order method, so I expect that they don't have any value prior to initialization. If order status or ID has value, I will throw an exception. Notice that this order process will be triggered by customers. So I will throw a domain exception here in case the business logic fails, and I will send appropriate error messages to the customers. For this, I will create order domain exception class in the exception package. To use a basic exception class, at this point, I will go to common domain module and create a new package exception, and then create domain exception class, extending the round time exception. And then I will create two constructors with string parameters, and with string and throw parameters, and then call the superclass constructors in these constructors. Then I will extend this domain exception in the order domain exception class. So I will have a base domain exception class, but also I have a specific exception for order domain. By creating a specific exception, I will even add context to the exception following domain driven design principles. And I look at the codes or when I see the exception in the logs, I will understand that this exception comes from domain logic. So here I will throw exception with an error message in case order is not in the correct state or initialization. Then let's implement validate total price method. Here I will check if price is not null and greater than zero. And throw an exception otherwise. Finally, I will implement validate items price method. First, I will create a stream from the items and use map method. Get to order item. Here I will define lombok inside map method and the input will be order item. And for the body, I will call another method here, validate item price. And I will send order item as the parameter to these new methods. I will implement these methods in a bit. Then in the map, I will return the sub total of the order item. I will then call reduce method here. With the reduced method, I want to start with zero and add all the sub totals to get the total amounts of all items. To do that, I will go to money value object in the common domain module and create a constant field for money, named zero. So I define a static final money field and created by sending the big decimal zero to the constructor of money class. Then I will return to the reduce method, and I will start with this money zero and call money add method. So basically, this map reduce operation will use the sub total for each item and adds them to each other to create the order items total. So I will assign the results to a money object named order items total. Then I will check if the price of the order, which is the total price of each item equals to these order items total. If these are not equal, I will throw order domain exception with a proper error message. In the validate item price method, I need to validate the item price by looking at the product price that sits on the persistent store. For this one, it will make sense to delegate this check to the order item entity. So I will create is price valid method in the order item class. This method will return a boolean and check if the price of the item is greater than zero. I'm using the methods from the money value object, as you can see here. Then I will check if the price equals to the product price. As you see, the product is the entity that order item has a reference. And finally, here I will check if price multiply by quantity equals to sub total. Now I can simply call this method in the validate item price method, and in case it returns false, I will throw an order domain exception with a proper error message. Great, I have completed the validation and initialization business logic methods for the order entity. Let's continue with the remaining method implementations of order entity in the next lecture.
