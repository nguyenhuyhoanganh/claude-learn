# Implementing Order Domain Service

Hi everyone. Let's continue with implementation of domain service. I will create order domain service in full class. And implement order domain service interface. In the validates and initiate order method implementation. First, I want to validate restaurant with a validate restaurant method. I will pass the restaurant object as parameter. Let's create these methods, I will implement it shortly. Then I will create a method called setOrderProductInformation and pass order and restaurant information. Remember that in the order item we have a reference to products and this product entity has name and price. I put that entity in order item just to be able to check the real price of a product. Remember that I get the product ID and the item price from the client, from the customer, but to be sure that this price is really the price of the product I will get the product information from the database using restaurant entity and then I will crosscheck the product information here. I will implement this one shortly right after implementing the validate restaurant method. Here you see that the domain service uses multiple aggregates to check some business requirements. Then I will call validateOrder method on the order object and then call initializeOrder method again on the order object. If everything goes correctly I will then return an order created events and pass order and current time with UTC on as the parameters. I use zonedDateTime now method here and use the zoneId of method to use UTC zone. As you may know, UTC stands for universal Time Coordinated. And it is the primary time standards by which the word regulates clocks and time. Great. Now, before implementing remaining methods I want to add logging in this class and this will be the only place that I will use a library in the domain module, but only in the domain service not in the domain core. I will go to Base Palm XML file and add provided long book dependency. And also springboard started logging dependency in the dependencies section. Here I use dependencies section instead of dependency management because I will use these dependencies application wise even in domain service. Then I will let SLF 4G annotation to the domain service implementation. After remaining reloads, The annotation should be available to use here. And then I will simply use log info method to log information before returning from the validate and initiate order method Now, in the one date restaurant method, I will simply check if restaurant is active using the volume property on the restaurant entity. If it is not active I will throw an order domain exception here. Then in this set order product information method I will get to order items and use for each method. Then I will use the current order item as the input to do Lambda definition in the for each method. And on the implementation I will get restaurant products and use another for each this time for the restaurant products. Then I will get the current product for the order item and find the corresponding product in the restaurant products. Once the iTune product equals to do restaurant products I will update the current products with the name and price from the restaurant product, which comes from the database. To do that, I will add a method to do product entity and let's name it as updateWithConfirmedNameAndPrice and pass name and price as the arguments. Once this data is available, it'll be checked in the order item validatItemPrice method which is called from the order entity during the validation as you can remember. In the updateWithConfirmedNameAndPrice method, I will simply set the name and price of a product. Just note, you may want to optimize this set order product information method. Currently, it runs in N-square time complexity in big notation because of the two inner loops. If you use an additional data structure here for products such as Hash Map you can reduce the time complexity to a linear time with a small space cost. I will leave it to you and continue with the implementation of the domain service. In the pay order method I will simply call pay for order, and then log info message. Then I will return an orderPaidEvent. Again with order and zonedDateTime parameters. In the approveOrder method I will call order approved method and log a message. As you see, these methods doesn't return any events because this is one of the last step in order processing and after approval, I don't need to fire an event. Instead, the clients will fetch the data using the get endpoint with tracking id. If you will the client that has capable of capturing events. You can hire final events even here that will be consumed by the client to continue with the delivery process. However, I will not implement the client as an event consumer. As mentioned here, I just use a HTTP client with Postman for the sake of this course. In the cancelOrderPayment method, I'll call you need cancel method and pass failure messages as parameters. And I log info message and return orderCanceledEvent. And finally, in the cancelOrder method I call cancel method on order object and log info message. Again, here, I don't return an event because this is another possible last step in order processing like to approve state. Let's create a final field for this UTC and reuse it instead of hard coding multiple times in this class. Great, I have finished the order domain core. Just to summarize, we have created entities, value objects, domain events, and the domain service. We also used that common domain module for common classes. Now, this core module is ready to be called by the application service, which will be the first contact point of a client request. Notice that in domain driven design concepts I have created a domain service to drive the business logic in the domain core module. If you think in terms of clean architecture from Uncle Bob these domain service could be matched to use cases because use cases described as the components that drive the business entities and domain service as the similar. I have seen discussions that matches the application service of a domain driven design to do use cases in the clean architecture. I believe this is not true because application service has no business logic insight and it is exposed to outside through an interface. While the use cases are not exposed if you think of clean architecture. All right, let's continue with the application service implementation in the next lecture.
