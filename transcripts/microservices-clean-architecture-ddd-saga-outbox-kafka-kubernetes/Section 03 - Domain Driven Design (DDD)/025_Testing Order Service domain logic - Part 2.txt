# Testing Order Service domain logic - Part 2

Hi everyone. In this lecture I will create the test methods. Let's create the first method testCreateOrder with test annotation. Here I will call order application service createOrder method with createOrderCommand object as the parameter and it'll return the createOrderResponse. This will be a successful createOrder so I will check if the response has pending order status. As you can remember, when I create an order the initial state should be pending. Here I use assertEquals method from JUnit Library. I will use another assertEquals to check the message. And check, if trackingId is not now, using assert not now. Let's run this test method to see if it'll pass. I will right click and run the test. As you see, it's all green and my assertions were correct. Now I will create another test method testCreateOrderWithWrongTotalPrice. Here I will use createOrderCommandWrongPrice object. So I will expect an exception. I will use assertThrows() method from assertions class and expect order domain exception in the first parameter. And as the second parameter, I will send Lambda with no inputs on the left side of the Lambda and I will call CreateOrder() method with createOrderCommandWrongPrice object on the right hand side of the Lambda expression. This will return an order domain exception. And so I will assert that the exception message is total price is not equal to do order items total. As you see, this is the error message I throw in the business logic. The third method is the testCreateOrderWithWrongProductPrice(). Here again, I will use assertThrows() this time with createOrderCommand. And the message should be order item price is not wanted for product as you see in the business logic. I will add the final test method, testCreateOrderWithPassiveRestaurant(). Here I will create a new restaurantResponse object with the active field set as false. Then I will return this when findRestaurantInformation is called on restaurantRepository mock object. So in that case, when I use assertThrows() I will expect an orderDomainException. Its Message Restaurant is not active as stated in the business logic. Great. So let's run all tests together. I will right click on the class and run all tests. But before that I want to fix a small error. Although it doesn't change the results is assertEquals method actually expects the expected result as the first parameter and the actual result as the second parameter. -: So I will simply revert to parameter orders in each assertEquals method. And I can run all tests. As you see, all is green so all tests are passed. You can run your test with coverage with intelligent as you can see here and see the college percentage for your classes and lines. And you can add more tests to have better test coverage. All right, after showing how easy to test that domain layer using a clean architecture let's continue with Kaka in the next lecture which will be our event store to using saga and outbox patterns. So I will see you in the next section.
