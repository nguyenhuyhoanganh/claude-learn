# Testing failure scenarios

Hi everyone. Let's continue with some failure scenarios. First, I will try to order an unavailable product. Here in restaurant container module, in the initState of a SQL file. I see a product which is not available. Not that in this scenario, I assume that the product is just updated on restaurant service tables, but not updated in client sites. So the product is allowed to be ordered, so simply I assume it is sold out and not available in this case. Let's try to order this unavailable product. The price for this product is 25. I will just add a new product to my item list with Price 25 and update the total price. And set the post request to create an order. Now I will first check the get endpoint using the tracking ID. As you see the order is in canceled status and the failure message mentions that the product is not available. What I expect is that order will be created and then the payment will be done successfully. Then in the order approval step, I will get an error from restaurant service. And the order service will call the compensating transaction and tell payment service to rollback the changes in payment service local database. During that rollback process, the order status will be in canceling state. And in the end if payment service returns a payment canceled event, my final status will be canceled, which is the status we see here that returns from the get endpoint of order service. Let's check the logs. In order service logs, I see order is created and then payment is completed in order payment Saga class. And the order created event is published into the topic payment request as you see here. Payment service is listed in this topic. So if I look at the payment service logs I should see a payment completed event. Indeed I can see that and payment service processes the payment and returns a payment completed event and publishes a payment response or model object into Kafka topic payment response, to notify the order service. Then I switch back to order service logs and I see that this time an order paid event is created and sent to Kafka to notify restaurant service. Using the restaurant approval request topic. This topic is consumed by restaurant service, and if I look at restaurant service logs, I see processing order approval event message and after that, in restaurant domain service in pool class, I see that the order is rejected. Is rejected order status and the failure message is encapsulated in the order rejected event, as you see here. And restaurant approval response, our model object sent to Kafka, this time into topic restaurant approval response, which is the topic that order service consumes. So this will notify order service. Let's go back to order service logs. I see processing rejected order for order ID message, in restaurant approval response Kafka message. So in each order, service is processing the rejected order. In order approval Saga, I see canceling order message, and in order domain service input, I see that order payment is canceling in local database of order service. At this point, order status will be in canceling state. Then an order canceled event is created and published to topic payment requests, as you see here. I see that a payment request our model object is created and sent to Kafka. The payment service will list in, that's payment request topic, and get this order canceled event. Now in payment service order canceled event is received as you see in the logs. And I see received payment rollback event message in payment request helper. And then see the payment canceled message in the payment domain service input. So in local database of payment service, the changes are rollback. And then a payment canceled event is published using payment response our model, into topic payment response. This Kafka topic is decent by order service. So one final time I will go to order service logs. This time I see that the order is canceled, so the order status sets to cancel status and then I see order is rollback, message here. Now one final scenario that I would like to process, is insufficient credit. So I will remove the unavailable product and use the original JSON request. This time I will try to create a new order, with a quantity 10 for the second product. So the sub total will be 500 and the total will be 550. Remember that this customer had a 500 credits and I already spent 200, so I shouldn't be able to create this order. Let's try it. Order is created in pending state and I will query the get end point with tracking ID. And as expected, I got canceled status for this order. And the failure message mentions that the customer credit is not enough to complete the payment. In the logs of order service, I should see an order created event that is published to trigger payment service. As you see, order created Kafka message publisher, publishes this order created event in the payment request topic. And payment service listens this Kafka topic and starts payment complete process. However, during domain logic checks, it fails with insufficient credits, as you see in the payment domain service input log messages. Then a payment failed event is published into payment response Kafka topic, which is listed by order service. I also see the failure message in the response, so the order service will get the reasons of failure. And indeed order service logs show that orders rollback with the failure messages, returned from the payment service. And order canceled messages in the order payment Saga and order domain service input indicates that order status set to canceled in the order service local database. Alright, we have a working food ordering system application with Saga pattern. And it seems to be handling failure scenarios as well. However, when we look closer, we will see that this application might still fail, because the local database transaction and event publishing operation are not running in a single unit of work. Remember that I commit to database changes to be sure that the changes are there and then I fire the domain events. But what will happen if the publishing fails, or the consumers of the events will fail before processing the required business logic. As I mentioned, to make this application more resilient, I will also apply all sparks pattern next to the Saga pattern. So let's add all sparks pattern into the application in the next section.
