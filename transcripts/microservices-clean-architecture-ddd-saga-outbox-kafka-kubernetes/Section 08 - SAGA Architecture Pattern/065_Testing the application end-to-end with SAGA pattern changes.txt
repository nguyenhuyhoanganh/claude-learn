# Testing the application end-to-end with SAGA pattern changes

Hi everyone. Now let's test the SAGA flow in our application. For this, I will start Zookeeper and Kafka using docker compose. But first I will delete the old data in the volumes folder to start with fresh data in Zookeeper and Kafka. So these folders should be empty. And I will open a terminal lone docker compos folder and start zookeeper using docker-compos -f common.yml -f zookeeper.yml up command. And I will go and start Kafka cluster in a new terminal window using docker compos and this time I will use Kafka cluster yml file. Finally, I will run in Kafka.yml file. Using docker compos -f common.yml -f in it's Kafka yml commands. As you remember, this will create the Kafka topics. Now I will go and check the Kafka manager web interface to see if the cluster is up and running. After adding the cluster manually, here I can see that cluster is up and running and I can also see the payment and restaurant approval Kafka topics. And then I will start order, payments and restaurant services. I will use the application classes under the container modules for each service. I will also run the customer service. For now, it'll simply create a customer database objects and then exit. Remember, I will update the customer service with CQRS pattern in the coming sections. Let's open PG admin and check the four database kmas. Customer, order, payments and restaurant. As you see, I have some initial data for customer, restaurant and products. Those data is initialized using to in each data SQL files in each service. As you see, I don't have any order in the orders table yet. Now let's create an order. I will open Postman and create a post request using local hosts 81 81 orders endpoint. Remember to order service rounds on 81 81 port. As defined in the application yml file. For the request buddy, I will use this JSON data. As you see, I sent customer ID and restaurant ID, which must be defined in the initial data SQL files. And I sent in others using street, postal codes and city. And then I have the total price here and then the items. In each item, I have product ID which is again must be one of two correct product IDs defined in the in each data SQL file of restaurant service. And I set quantity, price and sub total fields. Again, this price must be a correct price otherwise the domain logic will reject this order. Here I have another product in the items list. It's actually the same product but this time I add three products in this item. This JSON data is the equivalent of to create order commands, as you can see here. So this JSON data will be converted to this create order commands when I sent this request to the spring rest controller. All right, let's send this post request to the order controller. As you see, I got a pending order with the tracking ID. Now I will open a new Postman tab and query the tracking endpoint using CAT methods. I will pass the order ID as the path variable as it is defined as a path variable in the controller methods. As you see, these returns to order already in the approved state. So let's go and check the logs. As you see, when I first send the post request in the order service logs, I see the order saved with ID lock message in order creates help per class. So the order is persisted into the local database and then I see sending message in the Kafka producer inpl. And then the payment requests our model sent to Kafka for order ID message in the order created Kafka message publisher class. This message is published to be read by payment service because it uses the payment request topic, as you see in the logs. So I will see this message in payment service logs. Let's go and check that. In payment service, in payment request Kafka listener class, I see the order is received message and it is processing. And I see the payment is initiated for order ID message, which updated local database on payment service. Then in the payment request message listener inpl, I see that publishing payment event message. So the payment service is publishing a payment completed event using payment completed Kafka message publisher class. The payment completed event is published into payment response topic, which is consumed by the order service. And finally here, I got a confirmation from Kafka for this payment completed event in the payment service logs. Let's switch back to the order service logs because the payment completed event supposed to be consumed by order service. In payment response Kafka listener class, I see that the payment response is received and processing successful payment message. And in order payment saga, I got the completing payment for order with ID message. Then in the payment response message listener inpl class, I see publishing order paid event message. And order paid Kafka message publisher publishes this order paid event. As you can see here, restaurant approval Request Avro Model is used as the Avro type for this order paid event. This time, restaurant approval request Kafka topic is used which is listened by the restaurant service. And then I see received successful response from Kafka message, so the acknowledgement is received from Kafka cluster. As mentioned, this order paid events will be consumed by restaurant service. So, let's switch to the restaurant service logs. Indeed, in restaurant approval request Kafka listener class, I see one order received and processing two order approval event messages. And in restaurant domain service Inpl, I see that the order is approved, so the changes are persisted into the restaurant service local database. Then, an order approved event is published with order approved Kafka message publisher class. And the restaurant approval response Avro model object sends to Kafka. Into topic, restaurant approval response, as you can see here. Then I also see the acknowledgement from Kafka with the received successful response from Kafka message here. Finally, if I switch back to order service logs, I see restaurant approval response message in the restaurant approval response Kafka listener class. Then in the order approval saga class, I see approving order with ID message, and in the restaurant approval response message listener inpl class, I see that the order is approved for order ID message, so the order is persisted as approvals in the local database of order service. Great, we have reviewed the happy flow in logs. Let's also check the database records in PG admin. In PG admin consult, if I check the orders table, I see that the order is persisted with approval state. And in the payment credits table, I see that the original credits 500 is reduced by the order price, which was 200 in total. So here I have 300 credits left. And also in the credit history table, I see the payments for 200 amounts in the last records. Finally, in restaurants scanner in the order approval table, I see the order is approved. Let's also check the Kafka topics. You may use Kafka cat commands line tool to consume the payments and approval request and response topics. Plus this time I will use Kafka tool. If you have not installed it, please go and install it in your system. As you can remember, I installed Kafka tool in the setup section. Here I can connect to my Kafka cluster and then check the records in topics. The data is hex encoded, but you can decode it using a hex decoder. I will use onlinetools.com, and after decoding you'll see the serialized data, which is in Avro format. But the field values are still readable, as you see. In Kafka tool, you may even see your consumers with offset values, as you can see here. Okay, now I will run two failure scenarios. Let's continue with that in the next lecture.
