# Using Order Payment Saga in Messaging & Implementing Order Approval Saga

Now in payment response message listener impl class, I will inject order payment saga object using constructor injection. Then in the payment completed methods, I will call process methods of order payment saga. With payment response model parameter and get the order paid events. Then I will log an info message. And then fire the order paid events, using the fire methods of these domain events. Remember that I have the fire method implementation in the domain itself, and it simply calls to corresponding published methods to trigger the next step. In the payment cancel methods I will call the order payment saga rollback methods and just log an info message. I will use string join methods here with the failure message delimiter constants and create a string from the failure message list. As you see, I don't need to fire any event here. Let's also add the order ID to the log message here. Great, I have completed the order payment saga implementation. Now let's implement the next step which is order approval saga. I will create order approval saga class. Then @slf4j and component annotations, as in the order payment saga. Then I will implement saga step interface, and this time I will use restaurant approval response for the data generic type which is used for the parameter type of process and rollback methods. And I will use empty events for the return type of process methods and use order canceled events for the return type of rollback methods. Let's override the process and rollback methods with these generic types. Now I will let three fields, as in the order payment saga. Order domain service, order repository and order canceled payment request message publisher. As usual I will use constructor injection to inject those fields. Then in the process and rollback methods I will let transactional annotations. So here in these methods, Supreme will start a database transaction and commit the changes before returning to the color of these methods. This is important, as mentioned previously, because I want to commit the database operations before firing the domain event. Then in the process methods I will log an info message. Here I will change the parameters as restaurant approval response. And then I need to find to order as in the order payment saga. I will need exactly the same implementation, so it will be better to create a new class with the common methods of order payments and order approval. Let's go and create a new class order saga helper. I will let slf4j and component annotations also here. And then I will let order repository final fields and inject this with constructor injection. Then I will create final order methods and pass the string order ID parameter and return order object. Here I will get the implementation from the order payment saga. It'll be the same implementation. And I will create save order methods and pass the order domain object as parameter. Here I will call the order repository save methods with the order object. Now in the order payment saga I will let order saga helper field instead of the order repository fields and inject this helper object in the constructor. And then I will simply call the find order and save order methods in the implementation. So I can delete the find order methods here because now I am using the order saga helper class for the find order and save order operations. Similarly, in the order approval saga I will replace order repository with order saga helper field. And in the process methods, I will log an info message and then get to order using the defined order methods of order saga helper class. And I will call order domain service, approve order methods with the order parameter, and then call order saga helper, save order methods to persist the updated order objects into database. Then I will log an info message and return an empty event 'cause there will be no next step in the saga flow after approving the order. In the rollback methods, similarly, I will log an info message. And find the order using the order saga helper find order methods, and I will call order domain service, cancel order payment methods. With order, failure messages of restaurant approval response and with order canceled, payment request message publisher. This will return the domain events which is an order canceled event. Then I will update to order in the database using order saga helper, save order method. Finally, I will log an info message and then return the order canceled domain events from this methods. Now I need to call this order approval saga methods in my saga flow and to do that I will go to restaurant approval response message listener input class, which implements the input ports. Restaurant approval response message listener, which is the interface that the order messaging module uses after consuming restaurant approval response Kafka topic. Now I will let order approval saga fields and inject it with constructors. Now in the order approved methods, I will call order approval saga process methods with restaurant approval response parameters. And then log an info message. In the order rejected methods, I will call order approval saga rollback methods with restaurant approval response parameter. This will return an order canceled event. Then I will log an info message. Here I will use string join methods to get a string from the failure messages list. As you see, I used the failure message delimiter constants which is a comma, and then fire the order canceled events using the fire methods implemented in this domain event. All right, so we have completed the saga steps for order payments and order approval. Let's run Maven clean install commands and wield the application. Now we are ready to test whole saga flow in our application end to end. In the next lecture, I will send an order create post request to the application, from postman and then check logs, local databases and Kafka to see the happy flow as well as the failure scenarios. So I will see you in the next lecture.
