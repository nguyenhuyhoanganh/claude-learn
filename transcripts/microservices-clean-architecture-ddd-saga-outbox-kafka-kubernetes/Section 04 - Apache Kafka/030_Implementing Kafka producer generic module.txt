# Implementing Kafka producer generic module

Hi everyone. In this lecture, I will implement the Kafka producer module. First in the Palm XML file, I will add Kafka model. Kafka config data, order domain core and common domain dependencies. And also I will let Sprint Kafka, and Kafka our serial dependencies. I will create package config ordering system Kafka producer and create tool sub-packages, service and exception. Let's first create Kafka producer config class and mark this class with sprint configuration annotation. Here I will use two generic types, K and V for key and value. Key should be serializable and value should be a type of specific record base, which is the abstract base class of our type. I will inject Kafka config data and Kafka producer config data classes using constructor injection. Then I will create producer config methods with bean annotation and return a bean off-type map with string key and object value. Here I will hold to producer properties in a hash map. I will put two configuration codes and go over it. Here I said boot strap servers, scam registry, GN value, sterilizer clauses, batch size, linger MS, compression type, acknowledge, request, time out, and re-trace. Again, I will explain these properties in detail when I write them on the configuration file in the coming lectures in the order container module. Then I will create another bin with Methods Producer Factory and create a default Kafka producer factory with producer config map that I just created. So I passed the result of the producer config to the constructor of the default Kafka producer factory here. Finally, I will create a Kafka template bin and pass the producer factory as parameter. So in the end, I will use this Kafka template to send data to Kafka Brokers in this producer module. This Kafka template is a upper class to send data to Kafka cluster. Next, I will create a Kafka producer exception class in the exception package. Here I extend round time exception and I will simply create a constructor with strength message and call the Superclass constructor with this message. Then I will create Kafka producer interface under service package. I will use those same generic types J for T and V four value as in the Kafka producer config class. I will have a single send method here with void return type. As the parameters I will use topic, name, key, and message and a callback with type of listenable future callback. Let's implement this interface in the impl package. I will create Kafka producer impl class and implement Kafka producer interface. I will keep the generic types as it is so that I will be able to use this class from any service with any model. So it'll be a generic class. I will mark this class with component annotation so it'll be a sprint bean. I also use slf4j annotation to create a log variable automatically. I will inject Kafka template using constructor injection. Remember, we have created the Kafka template bean in the producer config class. In the sense methods, I will first log an inform message. And then call sense methods on Kafka template object. I pass the topic name T and message as the parameters. Send methods will return a listenable future. And I will use add callback methods on this future object to add my callback methods. So the color of these sends methods will be able to have the results of these send methods. Why do we use a callback here? Because the send methods on Kafka producer is a non-blocking asynchronous call so it will not return a results immediately. Instead, it requires a callback methods to be called later asynchronously. Here I will catch the Kafka exception, log an error message and throw a Kafka producer exception. Finally I will also add a closed methods which pre-destroy annotation. In these methods I will call Kafka templates, destroy methods, after logging an info message. These closed methods will be called when application is shutting down thanks to this producer annotation. So you can write any cleanup codes in a methods with this annotation. Great, producer implementation is completed. Let's finish this section with the Kafka consumer module in the next lecture.
