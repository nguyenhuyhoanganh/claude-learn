# Running Apache Kafka using Docker

Hi everyone. In this lecture, I will run a Kafka Cluster using Docker. For local developments, I will use Docker compose and run ZooKeeper's Kafka Cluster and it's schema registry together. Later in the course, I will replace this with cp helm charts in Kubernetes, then I run everything in Kubernetes. All right, so let's first create a new module called Infrastructure. I will put the source code of Kafka producer and consumer here in the next lectures. Let's delete the source folder as I won't use this module to create Java source files. Now let's just create a folder, Docker Compose, and then at, that's end file to set some global variables for Docker Compose. Here I'll set Kafka version as 701 and I set network name as full ordering system. And finally set the group ID com foods ordering system. I will use these variables in the Compose files. I will start with the common YAML file, but be sure that you have the Docker running in your local system. If not, you can check the setup section and install Docker in your operating system. Now, in the common YAML file I set the Network's Food Ordering System driver as reach. This network type will enable reaching to other containers by the container names that shares the same network. I will then create ZooKeeper YAML file. Here I will run Confluence ZooKeeper Docker image. As you see, I use Kafka version and global network variables from the environment file. For the port of ZooKeepers I use default ports 2181 and set the environment variables. Here you see I edit a Kafka ops environment variable and white listed ruok command. This will have to check the health of ZooKeeper using ruok command, as you will see shortly. And also I have the volumes for data and transactions of ZooKeeper. For this, I will create a volumes folder and create data and transaction folders under ZooKeeper folder. Since I use the volume maps, the data will be persisted to disk, so when I restart the Docker Compose file, I will not lose any previous data. I used volume mappings because while demonstrating the (indistinct) situations in SAGA and Outbox patterns I will need the previous data when I shut down and start the Kafka Cluster again. Great. Let's also create the Kafka Cluster YAML file. Here I have a schema registry and three Kafka Brokers. In schema registry, I use Confluence schema registry, Docker image. As you see, schema registry depends on the Kafka Brokers. I use 80 81 default ports and set the environment variables such as host name schema registry, listener, ZooKeeper URL, and bootstrap servers. Again, I use Kafka version and global network variables from environment file. In Kafka Brokers, I use Confluent Kafka Docker image using ports 1 90 92 and I set the environment variables like ZooKeeper address and listener names for the Kafka Broker. As you see, this broker is accessible using Kafka Broker van host name with ports 90 92. And also from outside of Drucker, it is accessible with local hosts using 1 90 92 port. Again, I use Kafka version and global network variables from environment file. Also, I use a volume mapping for Kafka Brokers as in the ZooKeeper. I map Kafka data folders to a local folders volumes Kafka Broker one. So let's create these folders and I will also create the folders for broker two and three. For broker two and three, I have this similar Docker Compose definitions. On the ports, broker ID, and volume mapping folders are different as you can see here. As you see, I also have a Kafka Manager Docker image which will run on 90 0 0 port. So I will use this web user interface to see my brokers and topics on Kafka Cluster. Finally, I will add init Kafka Docker Compose file. I will run this once, just to create the required topics and I first run the Cluster. As you see here, I use CP Kafka Confluent's Docker image and use Kafka version and global network variables again. I use entry points bin SH with minus C option and run commands to create the Kafka topics in the commands property. First, I run Kafka topics list commands to wait until the brokers are ready. Then I delete the Kafka topics if they're already created using the Kafka topics command with delete option. As you see, I have four topics: payments request payment response, restaurant approval requests, and restaurant approval response. And here I have Kafka topics commands which create option and I create the four Kafka topics with three partitions and three as the replication factor. Remember with three partitions, I can insert my data into three different partitions concurrently, using three different consumers. And with replication factor set as three, I can increase resiliency as my data will be held in three different partitions in three different brokers. Since I already created three Kafka Brokers in the Cluster, setting the replication factor as three is perfectly fine here. Then I list the topics that I created and exit this container as the sole purpose of this container is just to create the topics and then exit. Great, so let's run this stacker Compose files. First, I need to run ZooKeeper as the Cluster requires an healthy ZooKeeper when persistent volumes are used. So I run (indistinct) Compose minus F common YAHL minus F ZooKeeper YAML up command and run ZooKeeper container. As you see, I pass the common YAML file as part of the Compose command to include the network driver. Now to test the health of a ZooKeeper, I will use ruok utility that's enabled in the Docker Compose file as you can see here. I will open a new terminal and then I run echo ruok and then and see local host 21 81 command it should return IMOC text if ZooKeeper is healthy. As you see in this page, these four letter commands ruok can be used to check if server is running in a non-error state. Then I can run Kafka Clusters using Docker Compose minus F comma YAML minus F Kafka Cluster YAML up command. And then let's also run the init Kafka Compose file using Docker Compose minus F comma YAML minus F init Kafka YAML up command. As you see the topics are created. Let's confirm this by opening Kafka Managers user interface. I will open a browser window and type local host 90 00. As you see, there is no Cluster listed here because this Kafka Manager has no discovery and I need to add my Cluster manually. So I will go and add a new Cluster with name Food Ordering System Cluster and enter a ZooKeeper 2181 is the ZooKeeper address and port. Now, if I list the Clusters, I should see the three brokers and the topics that are created using init Kafka YAML Docker Compose file. As mentioned, now this data is persisted to the volumes folder as you see here in ZooKeeper and Kafka volume mapping folders. So when I shut down ZooKeeper and Kafka Clusters and start them again, I should still see the Kafka topics there. Let's shut down ZooKeeper and then Kafka Clusters and start them again. As you see, I can see the topics in the Kafka Manager console. Remember, when you use volume mappings you should first start ZooKeeper and then start Kafka Clusters. Because Kafka Clusters checks the health of a ZooKeeper at startup and fails if it is unhealthy. As mentioned in the Kubernetes section, I will replace Docker Compose with CP helm charts to run Kafka Clusters, so stay tuned for that. Great. We have the local Kafka Cluster functioning properly so we can start writing the Kafka generic modules to use them from the microservices. I'll start doing that in the next lecture.
