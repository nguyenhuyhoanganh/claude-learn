# Updating Payment database schema, config and package structure for Outbox

Hi everyone. In this lecture I will refactor payment service for outbox pattern as I did in the order service. I will go to payment service and first I will change the schema squared file in payment container resources folder. I will drop type outbox status if exist and recreate it. Remember that I create this type also in the order service so to be sure it is created. I drop and recreate it here because I created on the public schema. I have started, completed and failed values for outbox status. Then I will create order outbox table with fields ID and saga ID UUID and create that and process that timestamps and with type character fields. This is the name of the Saga. Then I have Jason Fields for payloads which will be the Jason representation of the domain event and I have outwork status and payment status fields. And finally, the version fields to use an optimistic locking. ID is the primary key here and I create an index on type and payment status fields. Finally, I create an uric index on type saga ID payment status and outwork status fields. Then in the application YAMA file, I will add two configuration values, outwork scheduler fixed rate and initial delay. Under the prefix payment service. I'll set 10,000 which is 10 seconds, so I will use this 10 seconds rate for scheduler implementation, and I will pull the outbox table for each 10 seconds for the payment schedulers. Remember that I set this same rate for order service scheduler also. Then I will go to payment application service module in payments domain layer. First, I will update the palm XL file with springboard starter Jason dependency. I will need this to serialize the domain events into Jason object and I will also add starter test dependency to duplicate payments. So I will write some test and I implement the outbox pattern to test these duplicate payments. Also, I will add a dependency to outbox module here I will create a package outbox and create sub packages model and scheduler. In model package, I will create order outbox message and put Getter, Builder and AllArgsConstructor LOBO connotations. Then I will have ID and, saga ID UUID fields and zones and date time fields for creative ads and process that. Then string type and string pay logs. I add payment status and outbox status using alarm types and finally, the integer version fields to version the database objects and use it for optimistic lacking. I will create setter methods for outback status field because I will update the outback status after getting a successful response from the message pass which is Kafka. Then I will create all advance payload class. Again with Lobo connotations, Getter, Builder and AllArgsContructors. Here I will add payments ID, customer ID, order ID, price createdAt, payment status and a list of failure messages. As you see, I use Jason property for each field here as this class will be used for the Jason representation of domain events, and I will set the Jason representation of these object in the string payload field of order outbox message object. Now, before the scheduler package, I will go and create the output parts for the outbox objects. First, in the repository package, I will create order outbox repository. Here I will let save methods with order outbox message and return order, outbox message object. Then I will let find by type an outbox status methods with type an outbox status parameters, and return an optional list of order outbox message. Then I have find by type and saga ID and payment status and outbox status methods and past the type, Saga ID payment status and outbox status parameters. This will return an optional order outbox message. Finally, here I hit delete by type and outbox status methods to delete the outbox objects with type and outbox status. Then in the message publisher package I will let payment response message publisher interface. Here I will have a single publish methods with order outbox message, and by consumer parameters. In by consumer, has order outbox message and outbox status input generic types so it'll be a callback method with these two parameters I will not use the old publisher interfaces so I will delete them. Right now, I will go back to scheduler package in the outbox package. Let's continue with this scheduler implementation of payment service in the next lecture.
