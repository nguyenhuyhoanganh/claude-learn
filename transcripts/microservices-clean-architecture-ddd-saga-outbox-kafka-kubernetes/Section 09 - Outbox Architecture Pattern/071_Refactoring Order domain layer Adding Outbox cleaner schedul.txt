# Refactoring Order domain layer: Adding Outbox cleaner scheduler for Payment

Now when the outbox status is updated as completed, I can actually delete that data from the outbox table. This will prevent the outbox table getting too large, which could affect application performance. So to clean the outbox table, I will create another scheduler class. Payment Outbox Cleaner Scheduler. I will mark this class with Slf4j and component annotations as usual. Then I will implement Outbox Scheduler and override process outbox message methods. Here I will inject payment outbox help project using constructor injection. On the process outbox message methods, I will use scheduled annotation and set cron midnight. That means these methods will run every day at midnight which would be a good time to do the cleaning operations on this table. Here instead of deleting the data directly, I want to have more control on what I am deleting. So first, I will use payment outbox helper, get payment outbox message by outbox status and saga status methods, and pass the outbox status as completed as the first parameter. Then I will set saga status as succeeded, failed, or compensated as the second parameter. So here I will get all completed outbox messages but I also do a double check that the saga status of that messages is one of two ending states and not starting your intermediate states like status, processing, or compensated. This method returns an optional list of order payment outbox messages. If the optional object is present, I will get the inner list objects, and log an info message with the size of the list, and also with the payloads of each message. Remember that the payload has all the detailed information like order ID and customer ID. So in the logs I will have the details of the deleted messages. I will get the payloads using the stream map methods and collect them as a string using a new line limiter. Then I will add the new methods to payment outbox helper class. Lead payment outbox message by outbox status and saga status. I will use outbox status and saga status wall arts as parameters in this methods. Here I will simply call the payment outbox repository delete by type and outbox status and saga status methods and pass the order saga name, outbox status and saga status as parameters. Finally, I will add transaction annotation to these methods as in the other methods of this class. Then in the payment outbox cleaner scheduler class I will call this delete methods and delete the completed outbox messages. Then I will log an info message. Here before deleting the data from the original outbox table, you might also create an archive table and move the data there. Then you can use that table along with the logs for analyzing the system. I will leave it to you for now and continue on the outbox implementation. All right. Now similarly, I will create a restaurant approval outbox scheduler. Let's do that in the next lecture.
