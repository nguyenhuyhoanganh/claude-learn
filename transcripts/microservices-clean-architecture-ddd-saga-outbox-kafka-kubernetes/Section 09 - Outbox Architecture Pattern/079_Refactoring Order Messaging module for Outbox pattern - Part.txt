# Refactoring Order Messaging module for Outbox pattern - Part 1

Hi everyone. The last part to refactor in order service, will be the order messaging module. So I will go to 'order-messaging' module and open publisher Kafka package. Here first, I will delete all the previous publishers. Because remember that bit outbox pattern, I have only two publishers in output parts for domain layer, as you see here. So I just need to implement them and override publish methods. Let's create order payments events, Kafka publisher class and use SLF4J and component annotations. And I will implement payment request message publisher interface and override to publish methods. The payment request message publisher interface is the output parts defined in order application service to publish the events. And I will inject required fields, order messaging data mapper, Kafka producer with string and payment request Avromodel, generic types, order service conflict data, Kafka message helper, and finally, object mapper. I will inject these fields with constructor injection. In published method, I will create and call get order payment event payload method with order payment outbox message payload is the parameter. And return order payment event payload object. In get order payment event payload methods, I will use object mapper and use read value methods to convert the stream payloads into an order payment event payload object. For this, I send a second parameter with order payment event payload class. I will return to order payment event payload object and also will create a tricatch block here. And in case of error I will log an error message and throw order domain exception. And in published methods I will assign sagaID to a local variable, and then log an info message. And then I will go to messaging data mapper, and create order payment events to payment requests Avromodel methods. Here I will create payment requests Avromodel objects using sagaID and order payment event payload objects as the parameters. I will use Avromodel newBuilder methods and set a random ID and then set sagaID, customer ID, order ID, price create to text and payment order status fields. Then in publish methods, I will call these methods and get payment requests Avromodel objects. Then I will call Kafka producer send methods. For the first parameter, I will get the topic name from the order service configdata using the get payment request topic name methods. Then I will pass sagaID for the second parameter, which is the key for the Kafka message. And I will pass the payment request Avromodel object which is the value for the Kafka message. Finally, I need a Kafka callback for the last parameter. Remember I have a generic get Kafka callback methods in the Kafka message helper class. This class is in the Kafka producer module. This methods is used from all messaging modules. Now I will change that generic methods A bit to be used with the need publisher classes that have the outbox pattern changes. Let's think about how to use this generic methods with outbox pattern. Now I will need a Kafka callback methods that takes topic name, payment request Avromodel, then order payment outbox message and byconsumer interface with generic types, order payment outbox message, and outbox status, which is the callback that I got here for the outbox operation. Out date, I have the by consumer outbox callback parameter in the published method. I will still return a listenable future callback with it's sound result and with string and payment request Avromodel generic types. So I will add a second generic type U and add two more parameters to this generic methods, get Kafka callback. So I will let U outbox message parameter and also byconsumer outbox callback parameter. Here I use U and outbox status as the generic types for the byconsumer. For the outbox status, I need to add outbox dependency here in the Kafka producer module. So I will go to palm examine file to add the outbox dependency. In the own failure methods, first, I will fix the Avromodel name, actually I can use a replacement with parentheses for that. And I will let the outbox message class name to do error message. After that, I will use outbox callback methods and call accept methods with the outbox message and with outbox status, failed. So here, if Kafka producer gets a failed response I will set the outbox message as failed on the local database using the callback parameters that comes with byconsumer interface. Similarly, in the on success methods I will call accept methods on the outbox callback with outbox message and with completed outbox status. So this way I will be able to update my outbox status by looking at the response of the Kafka producer. Thanks to this callback methods. Now I will just call to get Kafka callback method of the Kafka message helper. It's payment request topic name payment request Avromodel order payments outbox message, outbox callback order id, and Avromodel name parameters. Again, the outbox callback parameter comes as a parameter to the publish methods as you see here One thing to note here is that I set the sagaID as key here so the messages belongs to the same sagaID, will be in the same partition and they will be ordered because Kafka guarantees ordering in a single partition. Finally, in published methods, I will log an info message. And also I will use a tricatch statement and catch generic exception class, and then log an error message. Then I will create another class for approval publisher. Let's continue with approval publisher implementation in the next lecture.
