# Refactoring Payment domain layer: Adding Outbox schedulers

All right, as in the order service, in the payment service, I will have outbox scheduler interface and configuration. So I will use the outbox module to use that common interface and config. I will also add Saga module dependency to use order Saga name constant. I will first create OrderOutbox helper class to use it from the schedulers. In this class, I will let SLFRJ and component annotations as usual and I inject two fields, OrderOutboxRepository and ObjectMapper. I will use constructer injection to inject them. And I will create get completed OrderOutbox message by Saga ID and payment status methods with Saga ID and payment status parameters and then return optional OrderOutbox message. Here I will call corresponding methods of OrderOutbox repository, find by type and Saga ID and payment status and outbox status. As you see, I need to type for the first parameter, which is order Saga name. Remember I had the Saga constants class in the Saga module with the Saga name. Note that this constant class is in the order package because this constant is specific to the order Saga, but the other classes, Saga status and Saga step, are more general types that can be used from any Saga implementation. So I use this order Saga name as the first parameter, then pass Saga ID, payment status, and outbox status as completed. Here I want to get the completed outbox status records. Finally, I use read only transactional annotation here because it's a read only operation and I will create get OrderOutbox message by outbox status methods with the outbox status parameter and call OrderOutbox repository find by type in outbox status methods with order Saga name and the outbox status. This will return an optional list of OrderOutbox message objects, and again I will use read only transactional annotation here and then I will create the delete OrderOutbox message by outbox status methods with the outbox status parameter and use the delete methods on the OrderOutbox repository to delete the OrderOutbox message objects. This time here I use transactional annotation without the read only option 'cause this is a state changing method. And I will create save OrderOutbox message methods with OrderEventPayload, PaymentStatus, OutboxStatus, and UUID sagaId fields. I will use transactional annotation also in this method. Here I will create and call a private method save and will then send the OrderOutbox message as a parameter to this save method. To create the OrderOutbox message, I use builder and set ID with a random ID because this is a new outbox object. Then I said sagaId, created it, and order Saga name. Then I need to set payloads, and for this I will create a JSON object from the order advanced payload. Let's create createPayload method for this. Then I will set payments and outbox status values. Now in the say method, I will call OrderOutbox repository say method and get a response OutboxMessage. If the result is null, I will log an error and drop a payment domain exception. Otherwise, I will log an info message. In the CreatePayload method, I will use objectMapper writeValueAsString method and create a JSON text representation from the old advanced payloads. Here I will use try-catch statements and catch JSON processing exception and log an error and throw payment domain exception in case of JSON processing exception occurs. Here I will also set the processedAt field with current zone date time with UTC zone because here we process the event. Now I will create OrderOutbox scheduler class and add SLFRJ and component annotations as usual. I will implement outbox scheduler interface from outbox module. Then I will inject OrderOutbox helper and payment response message publisher. Then I will add that scheduled annotation to the processOutboxMessage method and use fixed rate and initial delay configuration variables. As you see, these variables are set on the application YAML file. Then I will also add transactional annotation on these methods. Here I will get the started outbox messages using orderOutboxHelper getOrderOutboxMessageByOutboxStatus method. If the result list size is greater than zero, I will get the list and load an info message with the ID of the messages. As you see, I create a stream on outboxMessages, get the ID, and collect them to a string using collectors joining method with a comma as the delimiter. And I will call publish methods for each orderOutboxMessage And for the second parameters, I need to pass a methods reference. Let's create these methods with orderOutboxMessage and outboxStatus parameters in the orderOutboxHelper class. In these methods, I will update the outbox status of the message and then call outbox helper save method and log an info message. I will also add a transactional annotation so that Spring will start a database transaction for this operation. As in the order service, this helper is called from schedulers and I have the transactional annotation also there, but just to be safe in case this helper is called from somewhere else, I will also add the transaction annotation to each method here. Remember that default propagation method for a transaction is required, so if there is a transaction opened and the method is called, that transaction will be used. Otherwise, a new transaction will be opened. All right, now I will pass the updateOutboxMessage method reference as the second parameter of publish method. Finally, I will log an info message in the processOutboxMessage. Now I will create OrderOutboxCleanerScheduler class. I will use this to clean the outbox table at midnight every day, as we did in the order service. Let's add SLFRJ and component annotations, then implement OutboxScheduler interface. I will inject OrderOutboxHelper object and add scheduled annotation with crom midnight. Then I will call orderOutboxHelper getOrderOutboxMessageByOutboxStatus method and get the outbox message list for the completed status. If the response list is present and the size is greater than zero, I will get the list from the optional object. Then I will log info message for received outbox messages with the size of the list and then call orderOutboxHelper delete method and delete the completed outbox messages. Then I will log another log message and finish this method. Now I will also add the transactional annotation to this method to start a database transaction for the cleanup process. Great. Now I will go to PaymentRequestHelper class and remove the old publisher classes. As mentioned, I'm not using these publisher classes anymore because I will not return the domain events anymore. Instead, I will persist them into an outbox table locally and publish them later by reading them with schedulers, and I will make the return types of the methods void in the PaymentRequestHelper class. Also, I will go to PaymentDomainService in DomainCoreModule and delete the publisher parameters. And in the payment domain service info, I will do the same and remove the publisher parameters from the method definitions, and also I will fix the method calls for the domain event constructors. Finally, I will change the domain event classes and remove the publisher and fire methods. Remember that we don't use them anymore. Instead, we use outbox table to persist the domain events. So I change PaymentCompletedEvent, delete the publisher object, and remove the fire method, and I will do the same for PaymentCanceledEvent and also for PaymentFailedEvent. And let's go to payment request message listener info class and delete the fireEvent method and not call it from the completePayment and cancelPayment methods. I will also delete the assignments for the events. Great. Now I will go to paymentRequestHelper class and update it to use with the outbox pattern. Let's do that in the next lecture.
