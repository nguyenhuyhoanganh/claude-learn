# Refactoring Order domain layer: Adding Outbox scheduler

Now I will return to the scheduler package in the outbox package, but before that I will go to outbox module. I will update the pom XML file with springboard starter dependency to get the required annotations to configure and use a spring scheduler here in the outbox module. Then I will go and create a new package here config. In the config package I will create scheduler config class and then I will add configuration annotation to make these class a spring configuration bean. Then I will add enable scheduling annotation. This will enable scheduling in my application. I will not create any bean here but we may create a specific ObjectMapper bean, actually, if you need custom object mapper configuration. Remember that I will use ObjectMapper for JSON serialization of the domain event. With springboard starter JSON dependency a default object mapper bean is created automatically and using this default bean will be enough for me. In case you need to add some specific properties of the ObjectMapper bean, you can create it here and for example, at fail on unknown properties, false, which will prevent failing if it encounters unknown JSON properties during the serialization. As I told, I don't need to customize any bean for this application. Then here I will create an interface outbox scheduler with a single methods voids process outbox message. Now in the scheduler package, in the order application service, I will create tool subpackages, payments and approval. Now in the payment package I will create payment outbox scheduler class. I will add SLF4J and component annotations to this class and implement outbox scheduler interface from outbox package and then override process outbox message methods. To do payment outbox repository operations I will create a helper class instead of injecting repository directly here. So I will create payment outbox helper, class, and use SLF4J and component annotations as usual. Then I will inject payment to outbox repository using constructor injection. Here I will create a public methods get payment outbox message by outbox status and saga status. It's the outbox status and saga status works parameters. And with return type optional list of order payment saga outbox message. Then I will call payment to outbox repository find by type and status and saga status methods. This method requires type, which is the saga name. For this I will create a saga constants class in the saga module under a package order. And add a static final fields for order saga name with value order processing saga. I will make this class final and also add private constructors as a best practice. This will simply hold the constants with static final fields, so I don't want anyone to create an instance of this class, which is unnecessary. Then I will pass order saga name constant as the first parameter. So it's simply the name of this saga flow. And then I pass the outbox status and the works saga status. This will return an optional list of order payment outbox messages. Then I will add get payments outbox message by saga ID and status methods. Here I will pass UUID saga ID and saga status works parameters and return optional order payment to outbox message object. Here I will call payment to outbox repository find by type and saga ID and saga status methods and pass order saga name, saga ID and saga status works as parameters. For both of these get methods, I will use transactional annotation with read-only true option 'cause these methods doesn't change the state and only gets data. So I can use read-only true option for this transactional annotation. I will come back to this helper class to add more methods for the payments outbox repository operations. Now in the payment outbox scheduler I will inject two fields, payments outbox helper and payment request message publisher. In the process outbox message methods I will use transactional annotation. So I want to run these methods with a spring managed transaction and I will use scheduled annotation which will make this method a scheduler and for fixed-rate and initial delay I will use outbox scheduler fixed-rate and delay properties. And remember that I set them on the application yaml five. It is 10 seconds, so for each 10 seconds this method will run. Here in the process outbox message methods, I will pull the outbox table. Actually I will pull the domain errands that are persisted into the outbox table. For this I will call payment outbox helper get payment outbox message by outbox status and saga status methods. As the first parameter, I will send outbox status started, because I want to get only outbox messages with started status. Then for the saga status works I will pass started and compensating saga status values. So what do these saga status parameters mean? In the payment outbox table I will have the domain events for two type of events, order created and order canceling. Remember that the order service triggers the payment service for these two type of events. And in the order status to saga status methods of order saga helper, I set the saga status as started for a newly created order, which is in pending states and I set the saga status compensating for an order that is in the canceling status. So when I pass started and compensating saga status objects to these methods, I actually ask for order pending or order canceling events, which are the two types of events that can be in the payment or outbox message. This will return an optional list of order payment outbox message. If this optional object is present and size is greater than zero, I will get the list of these payment outbox messages. Then I will log an info message and I will list the idea of each messages in the logs. To print the IDs, I will map the outbox messages and get the ID as a string value and then use collectors joining methods with comma as the delimiter. And then for each outbox message I will call payment message publisher publish methods. As you see, the second parameter requires a bi consumer with order payment outbox message and outbox status generic types. It will actually accept a method with these two parameters with void return type. I will create a new private method here called update outbox status and use order payment outbox messages and outbox status as parameters. In these methods I want to update the payment outbox table, so I need to use payment outbox repository interface. Remember that I have created payment outbox helper class with the outbox repository already injected so let's use that class here also. I will add the same methods in the payment outbox helper with order payment outbox message parameter and call payment outbox repository same methods using the order payment outbox message object. It will return an order payment outbox message as a response and if the result object is null I will understand that the save database operation was not successful, so I will log an error message and throw order domain exception. Otherwise, I will log an info message and return. Then in the update outbox status methods I will update the outbox status of order payment outbox message with the status parameter and then call same methods then I will log an info message here. To start a transaction for this save methods I also added a transactional annotation in the same method as you see here in the payment outbox helper so that spring will start a database transaction for this operation. This helper is called from schedulers and I add the transactional annotation there, but just to be safe, in case this helper is called from somewhere else, I also added transactional annotation to save methods here. As you see for get methods I added a read-only transactional annotation. Remember that default propagation methods for a transaction is required. So if there is a transaction already open when the method is called, then that transaction will be used. Otherwise a new transaction will be opened. Now in the process outbox message method of payment outbox scheduler, in the second parameter of the publish methods, I will simply use method reference this update outbox status. So this method reference will be passed into the published methods and I will be able to call these update outbox status methods with the completed or failed outbox status in the published method implementation. Remember, the implementation will be in the order messaging module and I will implement that in the following lectures. Then I finally log an info message here and complete the process outbox message methods. Again, please note that when I pull the events from outbox table and publish them successfully, I will mark them as completed or when it is failed, I will mark them as failed in this update outbox status methods. I will do that in the order messaging module, because only then I will know that if the publish operations are successful or not, but the update method is already implemented here. It is update outbox status methods. Therefore, when the outbox table is updated as completed in the next scheduled call of this process outbox message to find by type and outbox status and saga status methods, will not return the same outbox message, because as you see I use outbox status started in the condition of these methods. So this way I will prevent pulling the same data twice from the outbox table. However, note that, depending on the response time of Kafka cluster to producer, these update methods may not be called before the second run of the scheduler methods. So it means the same outbox message could be published more than once in some rare cases when the execution is delayed because of CPU decides to run some other trace or processes or because of a network delay from Kafka cluster to producer. This cannot be avoided with strict lock and wait implementations. But those type of locking will also slow down the application and is not acceptable in distributed applications. Because of that, I will also be cautious on the consumer side which is payment service in that case and I will eliminate duplicate messages. This will make sure to have (indistinct) messages. Stay tuned for the implementation of this. I will do that when I refactor payment service for outbox pattern. All right, payment outbox scheduler is completed. Now in the next lecture I will continue with another scheduler to clean the data from outbox table so I will see you in the next lecture.
