# Refactoring Restaurant Service for Outbox pattern - Part 2

Now I will continue with message listener implementation class. In the restaurant approval request message listener input class, I have removed to event firing as we don't fire the events direct anymore. Instead the events are persisted into our outbox table and processed by schedulers. And in the restaurant approval request helper in persist order approval methods I first call, publish if outbox message process methods. So I simply check if the message is already processed. In that case, I publish the message again and let the order service consume it. As you see I call order outbacks helpers, get order outbacks message by saga ID and outback status methods, and use saga ID and status completed for the parameters. If I get this completed outbacks object, I call restaurant approval response message publisher, publish methods, sending a callback also with the second parameter. If the message is already processed, here I just log an info message and return after publishing the message. If I cannot find a completed outbox message for this saga ID I process it as I did previously, and then call order outbacks helper, save order outbacks message methods. For the first parameter I have edit restaurant data method order approval events to order event payload methods and create an order event payload from the order approve events. Then I send order approval status and started outback status. And finally the saga ID, using Q U I D from sync Methods here. Remember as in the payment service in the outbacks table I have a unique index from type saga ID approval status and outbox status. So adding the same outbox message is not possible with these methods. And I will get a unique constraint violation exception here, in case of duplicate message. I will check this exception in Kafka listener and prevent retrying to duplicate messages. Kafka listener is in the messaging module. So let's switch to there. Here in the messaging module in the restaurant approval request kafka listener, I catch this data access exception and log an error message for unit violation. I also catch restaurant not fun exception and log an error message. As you see, I don't catch other runtime exceptions so it'll cause to retry those messages. But for this unique constraint and restaurant not fun exception, I don't want to retry as it doesn't make sense in those cases. In the restaurant approval event, kafka publisher, similar to the publisher class in payment service I implement the output ports, restaurant approval response message publisher from the domain layer. Previously here I had two different publishers for order approved and rejected, but now I only have a single publisher, and handle both statuses here. Here I have four fields injected, data methods, Kafka producer, restaurant service config data and Kafka message helper. And in the published methods I call Kafka message helper get all event payloads methods and get an order event payload object. Then I get the saga ID and then log an info message. Here I need the mapper methods to get an restaurant approval response server model object. So here in the mapper I have this new methods, order events payloads to restaurant approval response server model. And then I call Kafka producer sent method here with parameters, restaurant approval response topic name, saga ID, restaurant approval response order model and Kafka message helper, get Kafka callback method reference. For the Kafka callback methods I sent restaurant approval response topic name, our model, outbox message and the outbox callback method reference. And finally the order ID and the auto model name string parameter. Then here I log info message, and as you see I catch the exception and then log an error message. Great. So I have completed all required changes to use outbox pattern with this restaurant application service. So I can test the whole application, and in the next lecture I will do end to end test of this application with the outbox pattern implementation changes. So I will see you in the next lecture.
