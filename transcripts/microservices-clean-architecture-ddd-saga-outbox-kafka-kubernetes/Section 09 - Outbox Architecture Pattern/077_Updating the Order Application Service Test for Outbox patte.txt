# Updating the Order Application Service Test for Outbox pattern changes

Hi everyone. Let's fix the compile errors that are caused with the outbox pattern implementation changes. In the order test configuration, I don't have these publishers anymore. Instead, I will mock payment request message publisher interface, and also mock restaurant approval request message publisher interface. Then I need two more mock objects for repositories. Payment outbox repository and approval outbox repository. Remember that those are the repositories. It comes with outbox pattern. Also, I will add an application Java file to the test resources folder, and add outbox scheduler fixed rate and initial delay configuration variables. These are required by the scheduler annotation so I need to set them to run the test successfully. Great. Then in the order application service test, I will auto wire payment outbox repository and then object mapper, and I will create saga ID constant using final. Here in the in method, I will use van methods for payment outbox repository, same methods, and return an order payment outbox message and this methods calls. For this, I will let two new methods get order payment outbox message and create payload. Here I will create order payment event payload objects using wheeler pattern. I will set order id, customer ID, price, created at, and payment order status with pending state. Then I will use order payment outbox message builder methods and set a random ID and then set the saga ID and created at fields with current zone state time. I will set the type as the constant order saga name. I need to set the payload string, and for this I will create a methods to use object mapper write value as string methods and return adjacent text from the event payloads. So the return type here will be a string and I set order status as pending and saga status and outbox status as started. And set the version as zero. And I will return the order payment outbox message object. Then I will also delete these empty events in the common domain as it is not used anymore because we are not returning events anymore. We are persisting them in the outbox tables and use schedulers to read and publish events. All right, let's run these test with these changes. As you see, all tests are passing. Great. All right, I have finished refactoring order service domain layer changes for outbox pattern. Let's continue changing the order data access module for outbox pattern in the next lecture.
