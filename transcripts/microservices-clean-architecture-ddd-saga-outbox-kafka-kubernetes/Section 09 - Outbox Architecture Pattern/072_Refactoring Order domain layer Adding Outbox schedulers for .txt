# Refactoring Order domain layer: Adding Outbox schedulers for Approval

All right, now I will go to scheduler approval package. Let's first create approval outbox helper class here. As in the payment outbox helper, here I will do the repository operations. I will let SLF4J and component annotations and then add approval outbox repository as a field and inject it with constructor injection. Here first, I will create get approval outbox message by outbox status and Saga status methods. With parameters outbox status and Saga status war arcs, and return optional list of order approval outbox messages. And I will call approval outbox repository methods with order, saga name, outbox status and Saga status war arcs parameter and return. I will use transactional annotation with read on the option in this method and then I will create get approval outbox message by Saga ID and Saga status method. With parameters UUID, Saga ID and Saga status war arcs, and return optional order approval outbox message. Here I will call warrant by type and Saga ID and Saga status method of approval outbox repository and pass the order Saga name, Saga ID and Saga status parameters. Again, I will use transactional annotation, with read on the option here in this methods, as it's a read on new operation. Similarly, I will create save methods with order approval outbox message parameter. I will call approval outbox repository save method and if the result is now, I will log an error and throw order domain exception. Otherwise, I will log an info message. Then I will edit the transactional annotation also in this methods. But this time I will not set read on the option because this method is a state changing method. Finally, I will create delete approvals outbox message by outbox status and Saga status methods, with outbox status and Saga status war arcs parameters. And then here I will call approval outbox repository delete by type and outbox status and Saga status methods with order Saga name, outbox status, and Saga status, war arcs parameters. Again, I will add transactional annotation to these methods. Then I will create restaurant approval outbox scheduler class. I will let SL4J and component annotations, then here I will implement outbox scheduler interface and override the process, outbox message methods. I will create two fields, approval outbox helper and the restaurant approval message publisher and inject them using constructor. Then on process outbox message methods, I will let scheduled annotation and set fixed rate and initial delay configuration variables from the application conflict as in the payments outbox scheduler. And I will let transactional annotation to this method also. I will call approval outbox helper, get approval outbox message by outbox status and Saga status methods, and pass the outbox status as started and Saga status as processing. Remember that, processing Saga status corresponds to the order paid status. So this is the state that an order requires an approval from restaurant service. This returns an optional list of order approval outbox message. If the list is present and size is greater than zero, I will get the email list and log info message. I will list the idea of each message in the lock message. Then for each outbox message I will call restaurant approval message publisher, publish methods. Not that the second parameter of published methods is a by consumer, it's order approval outbox message and outbox status, generic variables. So I will create a private method here with these two parameters. Then I set outbox status of order approval outbox message and then call the same method of approval outbox helper to update order approval outbox message. Then I will log an info message here. Now I can send these methods as the second parameter of to publish methods, using methods reference. Then I will log an info message. As you see, this is a similar implementation as in the payment outbox scheduler. Now, I will create a cleaner scheduler for approval outbox, also. Restaurant approval outbox cleaner scheduler. And add SLF4J and component annotations. Then I will implement outbox scheduler interface and override the process, outbox message method. I will inject approval outbox helper fields here, using constructor. Again, as in the payment cleaner scheduler, I will use scheduled annotation with Chrome midnight. So these methods will be triggered every day at midnight. Then I will call approval outbox helper, get approval outbox message by outbox data and Saga status methods for the completed outbox messages and for Saga status, I pass succeeded, failed, or compensated here. Remember, Saga status parameter is a war arcs so I can pass multiples Saga status values. This will return an optional list of order approval outbox messages. If this optional object is present I will get the list object and log an info message with the payload of each message. This payload includes all details of the event by order ID and price. I will collect each payload into a string using comma as the parameter here. Then I will call approval outbox helper, delete approval outbox message by outbox status and Saga status methods and delete the completed outbox messages with the Saga status succeeded, failed, or compensated. And then I will log an info message. Not that for the failed outbox messages, it would be a good idea to create a dashboard possibly with alerting option and list and analyze them. I won't delete them here, as in the payment outbox table. As you see, I only delete completed outbox messages because failed operations are not in the final state. It is a situation that needs to be resolved because outbox status is only set to fails in case Kafka Producer send methods is failed. That could be a network issue or an issue on Kafka cluster. All right, so I will continue with refactoring the application service implementations for order create comments handlers, order of payment saga and order approval saga classes. Let's do that in the next lecture.
