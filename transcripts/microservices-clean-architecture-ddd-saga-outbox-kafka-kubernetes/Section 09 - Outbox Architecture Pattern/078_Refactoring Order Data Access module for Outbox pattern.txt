# Refactoring Order Data Access module for Outbox pattern

Hi everyone. Let's add the outbox repository adapters in the order data access module. In the order data access module I will create an outbox package and create payment and restaurant approval sub packages as I did previously. Since these data adapting implementations will be straightforward, I will put the classes here for these adapter implementations and go over and explain them. Now, in the payment package I have the payment outbox entity which is the JPA entity class. Here I have the AllArgo connotations as well in the other entity classes for getter setter methods, welder pattern, and for constructors. Then I have entity and table annotations with table name, payments underscore outbox. I have the ID annotation for primary key ID. Then I have saga ID, created that and process that and then type and payload fields for saga order and outbox status fields, I have enumerated annotation with string and AM type. Remember that I have created and AM types in Postgres for those fields. Finally, I have the version fields with version annotation. This is important to enable optimistic lacking for this entity class. Then I have equals and hash code methods using the ID field. Then in the exception package I have payment outbox not found exception class, which extends round time exception. In mapper package, I have payment outbox data access mapper class with order payment outbox message to outbox entity methods that creates an payment outbox entity from the order payment outbox message. And also I have payment outbox entity to order payment outbox message methods that does the other way around and creates a order payment outbox message from the payment outbox entity object. Both methods here users will their pattern to create the objects as you see here. Then in the repository package I hear payment outbox JPA repository, which is annotated switch repository spring annotation by extent JPA repository here with payment outbox entity object and with UU ID for primary key. Here, find by type and outbox status and saga status in methods takes type, outbox status, and a list of saga status and return optional list of payment to outbox entity. Then the fund by type and saga ID and saga status in methods takes type, saga ID, and list of saga status and again, return optional payment outbox entity. Finally, delete by type and outbox status and saga status in methods takes type, outbox status, and list of status to delete the payment outbox entity. Then in the adapter package, I have the adapter implementation, payment outbox repository input. This is spring managed bin, which is component annotation and it implements payment outbox repository interface which is the repository output part defined in the order service application. I have payment outbox JPA repository and the data mapper injected here. In the same methods, I simply call JPA repository save methods and use the data access mapper to convert the order payment outbox message to entity objects. Similarly, I convert the result entity to order payment outbox message using mapper class. In find by type and outbox status and saga status methods, again, I call JPA repository find by type and outbox status and saga status in methods using the type outbox status and saga status list parameters. If no outbox object found, I throw payment outbox not found exception and in the end I use data access mapper payment outbox entity to order payment outbox message methods and collect the results map to a list of outbox message objects and return it with optional wrapper. In find by type and saga ID and saga status I call the JPA repository find by type and saga ID and saga status in methods of repository and use the payment outbox entity to order payment outbox message methods to get an optional outbox message object. Finally, to delete the payment outbox message I use the JPA repository delete by type and outbox data send saga status in methods in these final methods. All right, then in the restaurant approval package in entity I have a similar entity class. The table name is restaurant April Outbox and I have the same field as in the payment outbox entity, including the version fields mid version annotation to enable optimistic locking. Then in exception, I have April Outbox not found exception which is a runtime exception and in the mapper package I have the approval outbox data access mapper component with two methods for the created outbox message to outbox entity that converts approval outbox message to approval outbox entity and the second method approval outbox entity to all the approval outbox message to convert the entity to outbox message. Both methods again uses builder pattern to create objects. In repository package I have approval outbox JPA repository, which is annotated with repository spring annotation, and it extends JPA repository with approval outbox entity and UU ID generic types. Here I have two find by methods. The first one uses type outbox status and a list of saga status to return a list of approval outbox entity and the second find by method uses type saga ID and list of saga status to return an optional approval outbox entity. Finally, the delete methods delete the outbox messages using type outbox status and list of saga status parameters. And then in the adapter package, I have the approval outbox repository impl components. This class implements approval outbox repository and injects approval outbox JPA repository and data access mapper classes. In same methods I call approval outbox JPA repository save methods and persistent approval outbox entity. I use data access mapper to create an entity object from the outbox message, and also I use the corresponding methods to create an outbox message from the result entity that in the find by type and outbox status and saga status methods, I call the corresponding JPA repository methods to get a list of other approval outbox messages. If not found, I throw approval outbox not found exception and then use data access mapper to convert entity objects to outbox objects and collect the result to a list and return as optional list. In the next method, I call the JPA repository find by type and saga ID and saga status in methods and use the corresponding data access mapper methods to create an optional order approval outbox message object. Finally, in the delete methods I delete the outbox messages using the JPA repository delete by type and outbox status and saga status in methods. Great, so after adding this data access module in the next lecture I will continue refactoring all the messaging module or outbox pattern, so I will see you in the next lecture.
