# Refactoring Order Messaging module for Outbox pattern - Part 2

Now I will create order approval event Kafka publisher class in the publisher Kafka package. Here again, I will use SLF4J and component annotations. I will implement restaurant approval request message publisher interface this time and override publish method I will inject the same five fields as in the order payment Kafka event publisher and I will only change the Kafka producer message to restaurant approval request Avro model. Now in the publish methods again I will have a similar implementation as in the order payment Kafka event publisher. In fact, I can create a helper method with generic types for getting the order event payload. Let's do that first and then I will complete the publish method. Now in the Kafka message helper class in the Kafka producer module I will add a new generic methods. Let's move this get order payments event payload to the Kafka message helper and make it public. First I will create object map fields and inject it. Then I will rename these methods as get all the event payloads to make it generic. Then I will add a generic type T and return it. Also, I will add a second parameter with type class and with generic type T and name it as output type. Then in the method I will use the output type as the second parameter of read value methods which requires the type of the object to be created. Then I will update the error message and exception message. Great. Now in order payments event Kafka publishers I will remove object mapper and instead use Kafka message helper class to create order event payloads. I will delete get order payment event payload methods and instead call Kafka message helper get order event payloads methods with a second parameter order payments event payloads class. Now in order approval events Kafka publisher I will remove object mapper field. Then in the published methods, I will call Kafka message helpers get order event payload methods and pass payload as the first parameter and order approval event payload class as the second parameter. This will return an order approval event payload object. Then I will assign saga ID to a local variable using order approval outbox message get saga ID methods and then log an info message. Then I will go to messaging data mapper class and add a new methods order approval event payloads to restaurant approval requests Avro model. Here I will create Avro model object from saga ID and order approval event payload objects. I will use the new order method of Avro model class and set a random ID and set saga id, order ID and restaurant id. Then I will set restaurant order status and then products with idea and quantity. For this, I use product need boiler methods and get the ID and quantity from the order approval event product object. Finally, as you see, I set the price and created at fields. And will that return the restaurant approval request Avro model object. Now I will call these methods and get a restaurant approval request Avro model object in the published methods. And then call Kafka Producer sand methods and pass restaurant approval request topic name, saga id, and Avro model. Finally, for the callback methods, I will again use Kafka message child class, get Kafka callback methods and pass the restaurant approval request topic name restaurant approval request Avro model, order approval outback message, the outbox callback methods, and then the order ID and Avro model name. These outbox callback methods will be called on the Kafka callback methods to set the final status of the outbox message. And I will log an info message. And then use try/catch block to catch generic exception and in case of exception, I will log an error message here. Again, I set to the saga as key here. That means the messages belongs to the same saga ID will be in the same partition and they will be ordered because Kafka quantities ordering in a single partition. Now in the messaging data mapper class I will delete the unused methods. Those methods belongs to the previous publisher classes so I can safely delete them. Finally, I will go to listener package in payment response Kafka listener class. In the receive methods, I will let a try/catch block. And I catch optimistic locking, fade or exception. Remember that I have enabled optimistic locking and it can be thrown from the order payment saga process or rollback methods. In case this error is strong, I will log an error message and prevent throwing an error in this Kafka listener methods. because as mentioned, the optimistic lock exception is not an exception that is a required retry. It's a result of duplicate message, so logging and ignoring it is enough here. So I will mention this. We make comments here saying that do not take action on optimistic lock exception. Now when I look at the domain logic, I see that order not found exception can thrown in the order saga. If an order cannot be found, then saga cannot continue. In that case, I should not retry the operation so I will also catch on the do not find the exception in the Kafka listener and make it a no op and only write an error log here instead of propagating the exception. Now, what about the other errors? What will happen when some other exception is strong during saga operation? Since I don't catch the other exceptions the exception will be propagated and spring will assume that the event listener method call is failed and then it'll read the same message again from Kafka so it actually does retry for a failed message automatically, which is great. Again, for optimistic lock exception I don't want this behavior, so I catch that exception and prevent propagation of that exception. Similarly, in the restaurant approval response Kafka listener, I will use a try catch block with optimistic locking failure exception and also it's order not fund exception and prevent propagation of those exceptions. Instead, I will hook an error message here. All right, we have completed the order service changes for outbox pattern. In the next section, let's write a test for order payment saga to test the concurrent access and optimistic locking. I will see you in the next lecture.
