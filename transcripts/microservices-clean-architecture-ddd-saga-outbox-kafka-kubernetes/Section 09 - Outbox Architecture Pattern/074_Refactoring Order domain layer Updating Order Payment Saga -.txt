# Refactoring Order domain layer: Updating Order Payment Saga - Part 1

Hi everyone. Let's continue refactoring order payment Saga class for outbox pattern. Here, I will first remove the publisher field because as mentioned, I will not fire events directly with outbox pattern. Instead, I will use outbox table to hold the events and fire them later with a scheduler. I will also remove the publisher parameter in order domain service for pay order methods. Actually I can remove publisher from all methods here in this interface. Then I will fix the implementation class that uses this interface. So I will remove the publisher from methods in the order domain service in full class. And I will go to event classes. First, order created events. I will remove the publisher from this event class. I can also go and remove the fire methods in the domain event interface. Similarly, I will remove the publisher from order canceled events. And then from the order paid event. Now in order payment Saga, I will update process and rollback methods with outbox pattern. I still need to input parameter payment response. However, the return types are not necessary anymore because I will not expect an event from these methods to be fired in the color methods of the process methods in payment response message listener input. As you see, I call fire methods in payment completed method. I will remove that. And change the log message here also. Then I will go to a restaurant approval response message listener input class and remove the order canceled event fire method call. And change the log message Again. Since I don't need the return types, I will also go to Saga Step interface and make it simpler. I will only leave the type T and delete the other generic types, and make the return types void. Now in order payment Saga, I will only use payment response as the generic type, and change the process and rollback methods to return void. In the process method of order payment Saga, I will first get the current outbox message from database. Note that this process methods is called from the payment response message listener input. So it'll be called when payment service returns a response using the payment response Kafka topic. That means there must be an outbox message that is created previously. Now to using reading the outbox message from database, I will first create payment outworks helper field here. and injected in the constructor. Then I will call payment outbox helper, get payment outbox message by Saga ID and Saga status methods with Saga ID and the Saga status parameters. As you see, I got the Saga ID from the payment response and use UUID from string methods to convert it to an UUID. Here for the Saga status, I pass started status. This will return an outbox object for the specified Saga ID, which is the started status. Remember that I saved, an outbox object in order created command handler with Saga status started because this order status Saga status methods will return started Saga status for the pending order which is the initial state for an order. Now this helper, returned an optional order payments outbox message. If this optional object is empty, I will log an inform message, and directly return. And I will write a message on logs that, the message is already processed. Because to process these methods, I need to have the Saga to be in the started state. And when this process methods triggered for the first time, I shouldn't see this message as empty. So when this message can be empty? If the same message is produced twice on payment response Kafka topic from the payment service, and the payment response message listener input class will be called twice for the same message. Remember that, this payment response message listener input, is called from the Kafka Listener Method in messaging module. So in that case, the first message will find the record with started Saga status, and then it'll process and update the status according to the saga flow. In that case, the second message that comes from Kafka will not find the Saga ID with started status because it'll be updated, when the first message is processed. So this is one of the each cases, that we need to cover and I do that by checking the outbox records with the Saga status at the start of the process method. And when can payment service create two messages for the same Saga ID? Actually, it could happen when order service sends the same outbox message to the payment service. And as I mentioned, this can happen if the scheduler methods runs more than once before the outbox message set is completed. Another scenario could be when you have multiple instances of the order service, the same message can be sent twice from each instance to the Kafka topic. In the end, in this distributed architecture, sending the messages multiple times could be possible. So I need to take care of this case, before processing data, right? Then I will get the order payment outbox message from the optional object then the rest of the body is the same. I find the order and update as paid, and I have the returned order paid events here before logging the final message. Now I will update the outbox table for payment outbox. This way, as I mentioned I want to prevent processing the same message twice. Because at the start of the process methods, I check the Saga status. If I update the Saga status here, the next threat that users duplicate Saga message, will not process the data. Now first, I need to get the Saga status. For that, I will inject order Saga helpers and use the order status to Saga status method. As you see, I used to order object inside the domain event to get the current order status. Remember that, this domain event is created with the order object parameter. Then I will update the payment outbox table. For this, I use the payment outbox helper object. Remember that, it has the payment outbox repository so it can handle updating the outbox table. I will call payment outbox helper save method. It requires order payment outbox message. But before calling, I need to update this data with new status values. So I will create, get updated payment outbox message method with parameters, order payment outbox message, order status and Saga status. I sent the new order status reading it from the order in the domain event object. Then here in this method, I will set the processed at field with the current time with UTC zone and I set the order status and Saga status with the new values and returned the order payment outbox message. Here, since I sent the order payments outbox message as parameter reference, even without return statements it will be updated on the caller method. However, to make the caller code more fluid, I return the object here and use it on the payment outbox helper save methods directly. Then how do I proceed the Saga flow? Now let's check what we have done so far. I have created a payment outbox object from the order created event. In order create command handler with started outbox status. Then the payment outbox scheduler, fetched this data and published it to Kafka topic, then payment service read topic, processed payments and then write results to the payment response topic. Then I listened payment response topic, and this process method in the order payment Saga is called. I updated the order as paid in local database here and gets order paid events. And then I updated the payment outbox object with the new order and Saga status values. Now in this Saga flow, I need to fire an event to trigger the restaurant approval phase. Remember, previously, I was firing an event in the payment completed method after the process method completed transaction of operation and returns. Now with outbox pattern, instead of firing the events directly, I need to save it to local database and to save the approval event, I will use approval outbox table. For this, I will inject approval outbox helper and create and call, save approval outbox message method. This will be as similar method as in the payment outbox helper. I will have parameters, order approval event payload order status status, Saga status, outbox status, and Saga ID. Then I will call save methods with order approval outbox message parameter. I will create the order approval outbox message using builder pattern. I will set random ID, and then set the Saga ID, then set the create ordered field. Then I set the type field, and it'll be a constant order Saga name. As mentioned, currently, I don't have any other Saga in this application. Then I'll set the payload using a new methods create payload. In this methods, I will take the order approval event payload parameter and return a string. Here, I will create a JSON text representation for the order approval event payloads. For this, let's inject object mapper using constructor injection and then I will call object mapper right value as string methods to return the JSON text for the event payloads. In case of error, I will use try-catch statement and log an error message, and then throw order domain exception. Now in this save approval outbox message method, I will set the remaining fields, order status, Saga status, and outbox status. Then I will build the object and return. I will also add transactional annotation to these methods. Now in order payment Saga, I will call save approval outbox message of approval outbox helper. For the first parameter, I need order approval event payloads. For this, I will use order data mapper. So let's inject it first. Then I will create a new method, order paid events to order approval event payloads and convert order paid events into order approval event payload object. I will use order approval event payload builder methods, and set order ID, restaurant ID. Then for restaurant status, I will set paid, and for products, I will create order approval event product with IDN quantity. Then I will set price , and create added fields. Now I will call this order data mapper method, and get the payload for the first parameter. Then I will pass order status, Saga status, then for outbox status, I will pass starter status and then set the Saga ID using UUID from string methods and getting the ID from payment response object. Now, if you think the Saga flow again, I have created the approval events in the outbox table. When the approval outbox scheduler runs, it'll read this event and publish into restaurant approval request topic and then it'll be listened and processed in restaurant service. And again, all the database operations will run in the same asset transaction here, which was the purpose of using outbox pattern. Alright, so let's continue refactoring order payment Saga in the next lecture.
