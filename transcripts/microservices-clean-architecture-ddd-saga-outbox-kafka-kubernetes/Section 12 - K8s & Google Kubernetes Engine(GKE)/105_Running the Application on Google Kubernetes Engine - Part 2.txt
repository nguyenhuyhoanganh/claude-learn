# Running the Application on Google Kubernetes Engine - Part 2

Now I will run kubectl cp, which is copy, create-topics.sh kafka-client:/kafka-client-storage command. This is to copy the Create Topics sh file ,which is an executable file, into the Kafka client container, which is running in Kubernetes pod. Remember, I configured this mount path in the deployment file of Kafka client. Then I will exec into the Kafka client port using kubectl exec command. It's minus IT option, giving the Kafka client container name and using the bin bash share. Now I will go to Kafka client storage directory which is mount into this container and then run the create topics sh file with the zookeeper host parameter. This will create the Kafka topics that are required for my application. Remember that I have the zookeeper host name as a parameter in the create topics sh file. And I first delete and then create the topics as you see here. Now my Kafka topics are created. I will exit from this container and then clear the terminal. Let's also run postgres deployment file. As you see, I use the latest postgres tucker image in this file. Let's wait until the postgres container is in running state. Great. Now I want to connect to this postgres instance from my local machine using PG admin to check the data in the tables. For this, I need to use the external IP. If I run Trip CTL get all commands, I will see that there is an external IP assigned to the postgres, because I have a lot more answer configured. As you see here I can also see the details of this service in services and ingress in the Google console. Now, I will use this external IP, with ports 54 32 and create a new connection in my local PG admin. I will use the admin password as defined in the postgres deployment file. And then as you see I am connected into the postgres, running in the Kubernetes. Now I can check the tables when I run the application services. All right. The next step is to run the application deployment file. I will run kubectl apply commands for the application deployment yama file and then check the ports. When all services are in running states that speak the order service and check the logs using kubectl logs command. Everything looks good. Order Service is able to connect to postgres and Kafka cluster that are running on Google Kubernetes engine. Now I will run kubectl get all commands and get the external IP off the loadbalancer of customer service. If I go to postman and use this external IP of customer service with the ports 81 84. I should be able to create a customer. I will create the user under score one as I did previously. Then if I go to PG admin and connect to remote postgres I will see the newly created customer in the customer's table of the order schema. Great. Let's also get the external IP of the order service, loadbalancer, and try to create an order through postman. Let's track the order using the tracking ID. I will use the same external IP for the get endpoint. And as you see, the order is already in the approved state so the saga follow is completed. I will go and check the logs of the order service in Google consult. Here I can see that the application runs correctly and all saga steps completed. First, payment is completed and restaurant service provided approval and communicate between the services. Here I have used events that are persisted in the outbox tables, and in the end the events are published and consumed to calca cluster, that is running on Google Kubernetes engine. Let's check the remote database also. In the orders table, I can see the approved order. You may also inspect the Kafka topics using the Kafka client part. Just log into Kafka Client using kubectl exec commands and run the Kafka topics commands. I will leave it as an exercise for you. All right, great job so far. Now we have the application running on Google Kubernetes engine. In the next lecture I will add horizontal scaling to the application services, so I will see you in the next lecture.
