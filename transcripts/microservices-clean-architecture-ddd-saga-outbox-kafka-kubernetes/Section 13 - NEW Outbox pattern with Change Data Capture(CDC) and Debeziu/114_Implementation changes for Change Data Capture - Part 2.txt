# Implementation changes for Change Data Capture - Part 2

Hi everyone. In this lecture, I will continue with restaurant service changes to use Debezium and change data capture. Let's open restaurant approval response Kafka listener in order messaging. Here I will use the envelope class from package debezium.restaurant.order_outbox. I will update this log statement to print the size of create operation request as I did in the payment response Kafka listener. Then in a for each loop, I will get the Avro Model and check if before object is null, and the operation is a create operation. For this, I will add common messaging module and use DebeziumOp enum class. After that, I will log a message here. Then I will get the value object using getAfter method. At this point, I need restaurant order events payload class. This payload should have the same fields of the order event payload class from restaurant service. Actually, this payload class will be used in both order and restaurant services, and if I look at the payment order event payload that I copied from payment service, that also has the same fields with order event payload class in the payment service. So here, instead of creating duplicate same classes, I will go and create these payload classes in the common domain module. So in common domain, I will create events payload package, and first move the order service payload classes here, order payment and payment order event payloads. I will fix this problem. Let's just continue with refactoring. Then let's do it with now. So here I need the Jackson annotations dependency to use JSON property annotation. Then I will copy order event payload from restaurant service and rename it as restaurant order event payload. Finally, I will move the 2 order approval event classes for order approval event payloads and order approval event producer from order application service to here in the common domain. Now, in restaurant approval response Kafka listener class, I will inject Kafka message helper and call kafkaMessageHelper.getOrderEventPayload method. with payload and the restaurant order event payload class parameters. Then I return a restaurant order event payload object here. Now, I will change these if statements to use the string order approval status with payload to get order approval status. I will then change the method approval response Avro model to approval response in order messaging data mapper and pass restaurant order event payload and restaurant approval response Avro model parameters. Here I will create a restaurant approval response object and set its fields from the input parameters as we did before for payment object. I just need to use instant parse method for created at field and use order approval status directly instead of using the enum. Then I will use payload and value objects in the parameters while calling the mapper method. Again, I call restaurant approval response message listener order approved method without doing any change in the application service module. Only changing this order messaging module is enough to switch to change data capture, as you can see. This is, again, thanks to the ports and adapters and clean architecture principles that we have applied. Then I will change the log statements to get the order ID from restaurant order event payload. Finally, I will catch data access exception here, as in the payment response listener, and handle unique constraint violation case here. Let's copy the catch statement from there. I will just change the log message and the payload object in that log message. All right, now let's go to restaurant messaging module. I will update restaurant approval request Kafka listener and use the envelope class from package debezium.order.restaurant_approval_outbox in the generic type definition and also in the method parameter for payload. I'll copy my log statement from payment response Kafka listener and change the message here. To use DebeziumOp enum class here, I need to add a dependency to common messaging module. In messages for each loop, I will get the Avro model and then check my condition for a new outbox record with get before is null and operation equals to create. Then I will get the after object as a value. Remember that I already created order approval event payload in common domain event package, so I can use it here to create an instance from the JSON payload. Let's first inject Kafka message helper here. Then use get order event payload method of Kafka message helper with the Avro model get payload and order approval event payload class parameters. Here I will get an instance of order approval event payload object. I will change the method in restaurant messaging data mapper restaurant approval requests Avro model to restaurant approval to convert order approval event payload and restaurant approval request Avro model to a restaurant approval request object. I use the order approval event payload here along with the Debezium value object to set the fields of restaurant approval request object. I will then update the log statements to use the new payload object to get ID information. Now, I will also go to application YAML file in the restaurant container module and change the restaurant approval request and response topic names. I will use Debezium order restaurant approval outbox for request topic name, and Debezium restaurant order outbox for the response topic name. Then I will delete the scheduler config values, as we don't use them anymore. Remember that we have the corresponding Avro classes for these topics in the Kafka model module. All right, Finally, I will go to payment messaging module. Here I will do similar changes. However, because of a scenario that I will use in the JMeter load test, I will need to make some more changes also here. Let's continue changing this listener in the next lecture.
