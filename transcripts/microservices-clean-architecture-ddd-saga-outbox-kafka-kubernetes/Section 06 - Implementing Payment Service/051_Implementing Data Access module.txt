# Implementing Data Access module

Hi everyone. In this lecture, I will complete payment data access module. As you know, this will be one of the infrastructure modules, and it will implement the output interfaces from the payments domain with adapters. Here I will have payments, credit entry, and credit history repository adapters. It will be similar to the order data access module and here I will have a straightforward classes and interfaces. So I prefer to copy and paste the implementations into this payment data access module, and go over them briefly. This way, I am hoping to be less verbose, preventing repetitions. It's not that you can also download this data access module in the resources section of this lecture. Now, first, I will add the dependencies to the pom.xml file. I need payment-application-service dependency here. Okay, now I need to add this dependency to the base pom.xml file with version. Then I can use it in the data access pom.xml file without the version. And I will let spring-boot-starter-data-jpa. And postgresql dependencies. And then I will create the base package, com.food.ordering.system.payment.service.dataaccess. Now I will pass the payments, credit entry, and credit history data access implementations. Here on the payments package, I have the JPA entity class. I have entity and table annotations from JPA library. As you see I set the table name as payments. Then now here the usual Lombok annotations. Getter, setter, builder, no args constructor and all args constructor. As you see I have the ID fields with ID annotation to indicate this is the primary key. Then I have customer ID, order ID, price, status and created at fields. For payment status, I used enumerated annotation to indicate this is an enum type in Java. Then in the equals and hashCode methods, I only use the ID fields here, which is the primary key for this PaymentEntity. In the exception package, I have PaymentDataAccessException, which extends runtime exception class. Here I use the constructor with a string message and call the superclass constructor with that error message. Then I have PaymentDataAccessMapper, which is a string component. Here I have two methods, first is PaymentToPaymentEntity which simply creates PaymentEntity JPA object from a payments domain object. And the second method is PaymentEntityToPayment, which creates domain objects payments from the payments entity database objects. In the repository package, I have PaymentJpaRepository, which extends JPA repository interface from Java X persistence library. And I use PaymentEntity and UUID generic types for the entity and primary key types. Here I need to use repository annotation to mark this class as the repository Spring bin. And finally, I had the findByOrderId method here, which takes order ID parameter and return an optional payments entity object. And finally, in the adapter package, I have the adapter implementation, PaymentRepositoryImpl. This one implements two payments repository from the payments domain module. And authorize the methods, save and findByOrderId. You now use components annotation to make this class spring managed bean, and as you see I injected PaymentJpaRepository and PaymentDataAccessMapper objects using constructor injection. In the same methods I simply call JpaRepository sale methods and use DataMapper methods, PaymentToPaymentsEntity to convert payments domain objects to a PaymentJpaEntity. Similarly, I use PaymentEntityToPayment methods of PaymentDataAccessMapper to convert the result entity to payment domain object because I need to return domain objects from this data access module. As the domain layer shouldn't know anything about the JpaEntity. Similarly in the other methods findByOrderId, I call findByOrderId methods of JpaRepository and map the results to an optional payment object, using paymentEntityToPayment methods of the DataAccessMapper class. Great, now in the credit entry. I have the similar class and interfaces. I have the JPA entity class with the same annotations, only the table name is credit_entry here. Then I have ID, primary key fields with ID annotation. And have the customer ID and total credit amount fields. Finally, I have the hashCode and equals methods that use the primary key ID. Then I have CreditEntryDataAccessException class, which extends RuntimeException. And then I have CreditEntryDataAccessMapper class in the mapper package, which is a spring components, and has two methods. CreditEntryEntityToCreditEntry and CreditEntryToCreditEntryEntity. To convert JpaEntity to domain object and also, domain objects to Entity. Then in the repository package. I have the CreditEntryJpaRepository interface which is marked with repository annotation and extends JpaRepository, which CreditEntryEntity and UUID generic types. Here I have findByCustomerI methods, to return an optional CreditEntryEntity that takes customer ID as parameter. Finally, I have the adapter implementation, that make use of the classes and interfaces that I just relieved. The CreditEntryRepositoryImpl class, implements credit entry repository interface which is an output port defined in the payments domain layer. Remember, in hexagonal architecture terms, an output port is an interface that requires an implementation which is an adapter. You know I use components being annotation and inject CreditEntryJpaRepository and CreditEntryDataAccessMapper objects. And simply implement save and findByCustomerId methods by calling the JpaRepository methods and using the DataAccessMapper to convert to and from the payment domain object and paymentJpaEntity. As you see we have the same pattern in all data access implementations. If you compare adapter implementation of payments and credit entry, you will see the pattern. Now, finally, in the credit history. I have CreditHistoryEntity which table named CreditHistory. I used the same annotations as in the other entity implementations. And I have a primary key ID and customer ID, and amount, and the transaction type. For transaction type, I use enumerated string and use transaction type Enum as you see here. It can have debit or credit values. I have the equals and hashCode methods that's used the primary key ID. And as in the other entities I have the CreditHistoryDataAccessException that extends the RuntimeException. And then I have CreditHistoryDataAccessMapper. In the mapper, I have components annotation of spring, and have two methods. CreditHistoryEntityToCreditHistory to convert JpaEntities to domain objects and credit history to CreditHistoryEntity to convert domain objects to JpaEntity. In the repository package, I have CreditHistoryJpaRepository interface. Which is again a repository as you see with this spring annotation. Then extends JpaRepository interface, it's CreditHistoryEntity and UUID generic types. And I have a single method here, findByCustomerId, which takes customer ID, UUIDs parameter. And return an optional list of credit history entity. Finally, the last adapter is CreditHistoryRepositoryImple, which implements CreditHistoryRepository, from payments domain layer. Here now I have components annotation again. To make this class, it's Spring-managed bin. I injected CreditHistoryJpaRepository and CreditHistoryDataAccessMapper using constructor injection. And in the same methods I call JpaRepository save methods. After converting the domain object to CreditHistoryEntity. And also, I convert the result EntityToCreditHistory domain object as you see here. Similarly, in the findByCustomerId methods, I call JpaRepository, findByCustomerId methods and get to optional list of CreditHistoryEntity. And I make these optional objects get a stream from the inner list. And for each item in the list, which is a CreditHistoryEntity. I create a new CreditHistory domain objects using CreditHistoryDataAccessMapper, CreditHistoryEntityToCreditHistory method. And then, I collect the result objects to a list of CreditHistory domain objects. Alright, that was the data access implementation for payment service. Let's implement the messaging module for the payment service in the next lecture.
