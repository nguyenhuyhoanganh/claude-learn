# Application Service domain module: Refactoring fire event process

Hi everyone. Now I will refactor this fireEvent method. Let's think about it. How to get rid of these if conditions. In each if condition, I use the payment events which implements the domain event interface. So I can use this common points. I will go and add a simple fire methods to the domain event interface with void return type and without any parameter. Now, in the fireEvent methods, I want to call this fire methods without checking the type of the event. But for this, I need to move the quotes in the if blocks to the fire methods, and it'll require to have the publisher in the event classes. So let's do this change. I will go to do PaymentCompleted event. I need to have a reference to the publisher for payment completed event which is PaymentCompleted message publisher. However, we have a problem here. Publisher is in the payment application service. And the domain events are in the domain core. I cannot put a dependency to the application service from the domain core because it's the core domain, and I want it to be the most independent module. Also, even if I decide to put that dependency, it'll create a cyclic dependency because payment application service already has a dependency to the payment domain core. All right, I have a solution for this since the payment completed message publisher extends the domain event publisher interface, and this domain event publisher is in the common domain module, I can use this domain event publisher in the event class directly because core domain has a dependency to the common domain. So in payment completed event class, I can create domain event publisher fields with generic type payment completed events, and add it to the constructor. Now, I will implement the fire method here, note that this fire is the new method, I just edit to the domain event interface. In the fire method, so I can simply call payment completed message publisher, publish methods, and pass this reference as the parameter because these published methods will require a payment completed event, and I am currently inside a payment completed event object. So here this refers to the payment completed event object. Then I need to pass this publisher to this event constructor whenever this constructor is called. Let's check the usages of this payment completed event. As you see in the payments domain service impl class, I call the constructor of the payment completed event. I will use Intelligy shortcuts to add the domain event publisher parameter, and I will choose adding it to the base methods because I have an interface defined for this payments domain service. As you see, the domain event publisher is added to the payments domain service interface as well. Now, I need to check where this validate, and initiate payment methods is called. As you see I call it from the payment request helper class in the application service. This is the place that I can use Spring Bean injection. So I can inject the publisher object, and pass directly to the validate and initiate payment methods. Let's create the field payment completed message publisher , and inject with the constructor injection, and then I simply pass it to do payments domain service as a parameter. Now, if I check the fireEvent methods of payment request message listener impl again, when I call the fire methods on the event, it'll find to overwritten methods on the payment completed events. Since I passed the publisher reference while constructing the payment completed event, I can simply call the publish methods using the this reference, which simply refers to the current domain event which is here payment completed event. Now, I just need to apply the same approach, and refactor the quote for the other domain events. I will go to payment canceled events, add a domain event publisher field with payment canceled events, generic type. Then I will set this field with constructor. And in the fire methods, I will use this publisher to publish the events using this reference, and I will check the usages of this constructor, and add this parameter to the method. Then, I will go to the place where I call validate and canceled payment methods, and simply inject the payment canceled message publisher using constructor injection, and pass this publisher as a parameter. Similarly, I will go to the payment failed event at domain event publisher with payment failed events, and use constructor to set these fields. And then I will call publish methods in the fire methods. Then I will go to payment domain service impl, and at the domain event publisher as parameter to both validate and initiate payments, and validate and cancel payments methods. Then in the payment request helper, I will inject the payment failed publisher object using constructor injection. And then pass it as a parameter to the payments domain service. Now, in the payment request message listener impl class, I can safely deletes the if conditions in the fireEvent method and simply use the fire methods on the domain event object. As you see, I got rid of the if conditions here. I will do this change also for the order events. So let's go to order created events, and add domain event publisher with order created events generic type. And use constructor to set these fields. Then I will override the fire methods which is defined in the domain events interface, and then I will call publish methods in the fire methods. I call this constructor in the order domain service impl as you see here. So I will add the domain event publisher as parameter here. And go to the Caller methods which is in the order create helper class. Here I will inject the order created payment request message publisher, and pass this publisher as a parameter in order domain service validate and initiate order methods. Similarly, I will go to the order canceled events, and then add domain event publisher field, then I will set this field with constructor, and then I will override to fire methods, and call the publish methods in the fire methods. I call these methods from the cancel order payments methods as you see here, in the order domain service. So here I will add new parameter for the domain event publisher. As you see, there is no reference to these methods because I haven't implemented the code that we call this cancel order payment method. It'll be the compensating transaction methods in the saga pattern, and I will implement it in the coming sections. Finally, I will go to the order paid event. At domain event publisher with order paid event, generic type, then I will use constructor to set this publisher, and finally override the fire methods. And call the publish methods in the fire methods. As I did previously, I will pass this reference, which is the current domain event object. I have a reference to this constructor in the pay order methods of the order domain service, so I will add the domain event publisher, here as a parameter. Again, I don't have a reference for this method yet. I will be implementing it while completing the saga pattern. Finally, I will remove the publisher fields from the payment request message listener impl class. Great, we have completed the domain module for payment service. Let's continue with data access infrastructure module in the next lecture.
