# Application Service domain module: Implementing input ports - Part 1

Hi everyone. In this lecture, I will continue with the implementation of the input ports in the payment application service. So I will go and create payment request message listener impl class and implement payment request message listener interface which is the input port as you see here. I will implement the methods and use the service annotation here. So this class will be a Spring managed bean. I will also use Slf4j annotation for this class so that I will create a log variable automatically. Here I will create a helper component to call the repository methods and then call to helper class to persist data on local database. Then I will fire the domain events when the database operations are persisted. So let's create payment request helper class. I will use component annotation to make this class a spring bean. Then I use Slf4j annotation to create Slf4j log label to use in this class. I will let the final fields here to inject payments domain service, data mapper, payment repository, credit entry repository and credit history repository. I will use constructor injection to inject these fields and I will create persist payment methods that returns a payments event and takes a payment request as parameter. I will let transaction annotation to these methods so that it'll create a transaction for database operation and commit to the changes when I return from these methods. I will first log an info message here and then create and call payment request to payment methods on payment data mapper class. Let's create these methods in the data mapper. I will get a payment request object and return a payment domain object. So I will use payments builder methods and set customer id, order id and price using the domain objects. Then in the persist payment methods, I will create and call get credit entry methods. I will pass the customer id here from the payment object and return that credit entry. In this get credit entry methods, I will call credit entry repository find by customer id methods and get an optional credit entry object. If these object is empty, I will log an error message and throw a payment application service exception. Otherwise, I will return the credit entry object from this method. And similarly, I will create get credit history methods and pass the customer id as parameter and return list of credit history object. Here I will call the credit history repository find by customer id methods and pass the customer id as parameter. This will return an optional object for list of credit histories. If this optional object is empty, I will log an error and throw payment application service exception. Otherwise, I will return the list of credit history here. Then I will create failure messages list and call payment domain service, validate and initiate payment methods. With payments, credit entry, credit history, and failure messages parameters. These methods will return a payment event. Then I will call payment repository save methods and persist the payment object. If failure message is empty that means there is no business logic error. I will save the credit entry and credit history using the respective repository interfaces. For the history, I will save the last records, which is edit during the business logic as you see here. I will not update the credit entry and history in case there is a business logic error. However, I persist the payments in any case because it'll update the payment as failed. As you see the status is updated in case it is failed. Finally, I will return the payment event. Then I will create a persist cancel payment methods with transactional annotation. Here again, I will log an info message and then call payment repository find by order id methods and pass the order id as parameter. This will return an optional payment. If it's empty, I will log an error message and throw payment application service exception. Otherwise, I will get the payments from the optional payment object and get credit entry and credit history using the methods that I created before. And then I will create failure message list and call to payment domain service, validate and cancel payment methods. Here I will pass payments, credit entry, credit history, and failure messages as parameters. These methods will return a payment event. Then I will save payments and also credit entry and history in case there is no failure messages. As you see, this code is common in both methods so let's create a new methods, persistDbObject and use from both methods. I will create persistDbObject methods and move this code here. Then I can call these methods from persist cancel payment and persist payment methods. Finally, I will return to payment events from these methods. Actually, you can also move this part to the common methods as it is also mostly the same, but you need to handle these different methods called in that case. You can pass a function that takes four parameters and return a payment event. I will leave it as an exercise for you. Please try to move this part to a common private instance methods also. Now I will return to payment request message listener input class. Let's continue with the implementation of this class in the next lecture.
