# Domain core module: Adding Exception and Domain events

Now I will create an exception class, payments domain exception and extend the domain exception from the common domain module. I will also create payments not found exception to use when no payment is found. This class will also extend the domain exception class. Finally, in the events package, I will create payment event class, make it abstract, and then implement domain event interface. For the generic type, I will use payment entity object. I will let three final fields, payments, zone state time for created at, and a list of strings for failure messages. I will create a constructor with these three final fields and then create the other methods. Then I will create payment completed events and extend the payment event class. And I will call the payment events constructor using super keywords. Similarly, I will create the payment canceled event and extend the payment events and again call the super classes constructor with all parameters. Finally, I will create payment failed events, and extend the payment event class, and then call to payment event constructor using super keyboards. Similar to the events in the order domain, here in the payments domain, I have created specific events, but I checked the common quote in the base event class, which is payment events. All right, let's continue with the domain service implementation in the next lecture.
