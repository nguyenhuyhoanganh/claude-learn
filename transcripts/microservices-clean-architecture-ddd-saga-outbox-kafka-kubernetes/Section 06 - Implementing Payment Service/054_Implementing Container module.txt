# Implementing Container module

Hi everyone. Let's complete the payment service with the container module. I will first go to Pom XML file. Remember, the container module is the dirtiest components. That means it has dependency to all modules to be able to run all of them together. So I will let payments, domain, core, and application service. Then I will let data access and for this one I need to add to dependency to the base Pom XML file with version so that I can omit the version here in the container Pom XML file. Then I will let messaging components. Again I will add this to the base Pom XML file which version and use it here in the container module Pom file. Without the version I will also add spring boot starter dependency because this container module will start a spring boot application. Finally, I will add spring boot's maven plugin. It will create a runable bundled jar file from this container. And here I will also add the docker image name and use build image call to create a docker image in local docker registry when I run the Maven install commands, great. Let's create the base package now com food ordering system payment service domain and I will create payment service application class and then add spring boot application annotation with base packages property com food ordering system. Then I will let entities can and enable JPA repository's annotations with the base packages ending with payment service data access. As you see, this is similar to the order service spring boot's main class but I adjusted the package names. Then I will create main methods and call spring application run methods to run a spring boot application with this class. So by adding spring boot application annotation and having these main methods I will be able to start the service. Then I will add be configuration class and add configuration annotation. So this is a sprint configuration bean. Here I will add the methods payments domain service which be annotation and it'll create a sprint bean from the payments domain service implementation and return a payments domain service interface. Remember, I haven't put to spring dependency in core domain layer of payment service, but I still want to inject and use this payments domain service from other modules. So I create a be configuration here in the container module for this purpose. Now I will go to resources folder and create application YML configuration file. Here I will edit the config data for payment service. I will set the port as 8182, then set to log level debug and then I will create payment service prefix and its payment request and response topic names. Remember, in the payment service config data class I use this prefix payment service. So actually these topic names will be mapped to an instance of this payment service config data class and I will use it to consume and produce on Kafka topic. Then I have this spring JPA and data source properties. If you look at the application YML of order of service actually I will need to do same conflict for JPA and data source. So I will copy that and just change the schema name as payment. Also, I will add a data property which in its data is square. In order service I had no in its data square file, but in the payment service I will create some data at startup to use in the demo. I don't need that in the order service cause the input comes from a REST API. In order service for the explanation of these JPA and data source properties you may check the lecture for order service container while implementing the order service container. I have gone through these properties with explanations. Then I need Kafka config Kafka producer and Kafka consumer config properties. Again, I will get them from order services file and just remove the restaurant approval consumer group ID property as I don't need it here. Again, instead of repeating the same explanations for these properties I would rather continue with the next steps here. Please check the lecture of order container module if you haven't come through the explanations of these properties. Now I will create init schema SQL file I will create the database objects for payment service. You can download this file from the resources section of this lecture. Here I create the payment schema and create UUID extension. As you see, again, most of the things are similar to the order service schema file so you should be familiar with the commands. I create a payment status type as ENUM which completed canceled and failed values. This is the way of creating an ENUM type in Postgrace. If you check the payment status Java class you see that I have the same values there and I create the payment table. In the payment schema, I have id, customer ID and order ID fields. These are the fields fit new ID type. Then I have price, which is a numeric fields and I use 10 digit precision for this value and use two digits after the decimal points as you see here. Then I have timestamp created that fields and the status column with the new payment status and I type that I just created here. Primary key for this table is the ID field. Then I have credit entry table with id customer id and total credit amount fields and then I have the credit history table. For this, I will create a new ENUM type transaction type with values, debit and credit. This will be the equivalence and I type in Java. As you see here in the credit history table. I have ID customer ID amounts and type fields and for the type field I used to ENUM transaction type that I just created. Great, let's create the init data is grad file. Also, here I will insert some initial data for credit entry and histories. Normally you would create an endpoint here and update the customer's credits data but for the sake of this application I will just in some data into the customer credit here. As you see, I use the same customer ID of the data file in the customer service cause I will use this customer ID while creating an order through REST API. On order service, the ID fields are just random new IDs and for amounts, I put some initial amount here for credit entry and history tables. But be careful to have the correct sum for debit and credit entries in history table. It should be equal to the amount in the credit entry table as it is checked in the business logic. So here in the history I have 700 credits, minus 200 debit and in the end I have 500 credits for this customer in the credit entry table. All right, with is container module payment service implementation is completed. Now I will let the restaurant approval service to the application and we will be ready to apply SAGA and Outbox patterns. Let's create the restaurant approval service in the next section.
