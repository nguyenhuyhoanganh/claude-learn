# Implementing Messaging module: Adding Listeners

Okay, now I will go to the listener Kafka package and create payment request Kafka listener class. I will add component annotation to make this class a spring theme. And @Slf4j annotation to create a log variable. And then I will implement Kafka Consumer interface from Kafka Consumer module. And use payment request avro model as the generate type. For the autotype for this Kafka consumer. And I will create two final fields. And inject them using constractor injection. Payment request, message listener and messaging data mapper. Not that payment request message listener is the input parts and has the adapter implementation in the payment application service module. Here, I just need to listen to Kafka messages and trigger that adapter implementation. On the receipt (indistinct), I will list Kafka listener annotation. Which is the spring Kafka annotation to create a Kafka consumer from a simple matplot. I will give the consumer ID using two configuration parameter. Name it consumer group ID. And give the topic name using payment request topic name configuration variable. I will define this configuration variable in the next lecture, in payment container module, application (indistinct). I will use payload annotation for the list of messages. And use header annotations for keys, partitions and offsets. Then I will log an info message. And for each message, I will get the payment request avro model. And check the payments order status. Remember that this payments service can be called for two different scenarios. Either for a pending order, just to complete a payment. Or to cancel a payments. In case the order I pull has failed in avro interface. I will check these two conditions, here. In case of pending payments, order status, I will log a message and call payment request message listener, complete payment (indistinct). For this, I will create a payment request object using to payment request avro model to payment request (indistinct) of messaging data mapper. Similarly, in the cancel state, I will log a message and call cancel payment (indistinct) with the payment request object. As you see, the complete payments and cancel payments (indistinct) are (indistinct) in the payments domain layer. In the payment application service module. And they file the corresponding events after persisting the changes, in the payments local database. And those events will be used by the order service to continue with the operations. It will see how this data flow works, shortly with SAGA pattern in the following sections. All right, we have a final module left for the payment service. Which is the payment service container module. Similar to the order service, the container module will include a spring boot starter class. So, it will start the microservice itself. And it will have all the dependencies to run the payment service, domain logic. Together with the adapter implementations, which are data access and messaging. I will see you in the next lecture to complete the payment service.
