# Implementing Customer Service modules

All right, let's go over the customer module implementations. I won't have a complex business logic here, so I put the implementations and now I will go over it, instead of repeating the similar implementations, that we have already done in the previous microservices. First in the customer domain core, I have customer entity, which is actually the aggregate root. Here I used customer ID as the generic type, for aggregate root. Then I have username, first name and last name string fields. As you see, I have one constructor and the getter methods for the fields. Here, there is no business logic methods. I will focus on the implementation, of CTRS pattern in general here, but feel free to add any validation or initialization logic as an exercise. Then in the event package, I have customer created events. Similar to the events in the other services, here I implement domain event interface, with the customer entity generic type. Then I have customer and created at fields, with a constructor for both fields and also I have the getCustomer method here. In the exception package, I have a single customer domain exception class, which extends domain exception, from the common domain module, then I have the customer domain service interface, with a single methods, validate and initiate customer. Here I get a parameter customer entity and return customer created events, then for the implementation of domain service, I have customer domain service input class. Here I just log an info message and return customer created events, passing to customer object and the current zoned date time. As you see again, I don't have any logic here, to keep it simple. Next, I will go to the customer application service module. Here in the config package, I have customer service config data. This object holds to customer topic name and it uses customer service prefix. Remember, for this, I have edits the customer topic name, in the application YAML configuration file and I have create customer commands, which has customer id, user name, first name and last name fields. These commands will be sent from the rest API, into the application service domain layer. Here I have notNULL validation annotations and also the usual Lombok annotations, getter, builder and all argsconstructor. Similarly, I have create customer response here, in this class I have customer id, UUID and the message string field to return to the client. In customer data mapper spring components, I have two simple methods, to convert a create customer commands to customer entity and to convert a customer entity, to customer response object. Then I have the port definitions, which are the building blocks, of an hexagonal architecture. For input ports, I have customer application service interface. Here I have create customer methods, parameter is create customer command and I have the valid annotation, to enable validation of the fields using spring validation. Then I return create customer response here. Remember that an input part is implemented, in the application service itself. Indeed, the implementation of these interface is here. I will come to this after reviewing the parts. Now for the output parts, I have customer repository, which create customer methods. Remember that this interface, is implemented in data access module, because the data access module is an infrastructure module and it implements the output parts using an adapter. Then as a second output port, I have a customer message publisher and here I have a published methods, with customer created event parameter. This is implemented in another infrastructure module, in customer messaging. Now here I have the customer create command handle class, which is a spring component and has three fields injected. Customer domain service, customer repository and customer data mapper. Here I have a transactional annotation on methods, create customer. These methods takes create customer command object and return a customer created events. Here I first create a customer object, from create customer command using the data mapper. Then I called domain service, validate and initiate customer methods, with the customer object. This simply returns a customer created event, then I call repository create customer methods, to process the customer object. If the method returns a null object, I log an error message here and throw a customer domain exception. Otherwise, I log an info message and return the created event. Finally, in the application service input class, I implement the application service input parts. Here I have SLA4J annotation to use an implicit log variable and then I have validated annotation, to enable spring validation in this class. And finally, I have the service annotation, to make this class as spring managed bin, in the service layer. Then I have three fields injected, customer creates command handler, customer data mapper and a message publisher. In the overwritten create customer methods, I first called the customer create command handler, create customer methods. Remember that this is a transactional methods and it persists the customer into database and return a customer created event. Now after being sure that the customer is persisted, into database here in the Create customer methods, I published a customer created events, using the message publisher published methods. Again, remember that this publisher is implemented, in the messaging module. Finally, I use data mapper customer, to create customer response methods, to create response objects and return. Please note that again, I kept the thing simple here, in this service to simply focus on, CQRS idea, but actually for a strong consistency, between local database operations and the data publishing operation. You still need to use outbox pattern, along with CQRS pattern as in the SAGA pattern, because here after persisting the data into database, you cannot be sure if the publish operation, will be successful and if it is not successful, you will have inconsistency, between the customer tables in customer service and the order service. All right, now I will go to the data access module. Here I have customer entity with JPA entity and table annotations. The table name is customers and I have the ID, username, first name and last name fields. As you see, the ID fields is marked with ID annotation to indicate that this is the primary key. Here I also have the usual Lombok annotations and I have customer data access exception, which is a runtime exception, which can be used in case of data access exception, is strong. Then I have customer data access mapper spring components, here I have two methods to convert customer domain object, to customer entity and customer entity to domain object. Next, I have customer JPA repository, in the repository package, this interface extends JPA repository, with customer entity and UUID generic types. Finally, in the adapter package, I have the adapter implementation customer repository input. This is a spring components and I implement the customer repository, from the domain layer here. Two fields are injected here, customer JPA repository and the data access mapper. In the implementation of create customer methods, I simply call JPA repository save methods and use data access mapper, to get a entity from the customer domain object. Again, I use customer data access mapper methods, to convert the returned JPA entity, to the customer domain object. The next infrastructure module is customer messaging. Here I have customer messaging data mapper, spring components with a single methods, payment response avro model, to payment response. This method returns a customer avro model, from the customer created event object. Then in the customer created event Kafka publisher class, I implement the output parts, customer message publisher, from the domain layer of customer. This publisher is a spring components and has SLF4J annotation, to use an implicit log variable as usual and three fields are injected here, data mapper, Kafka producer with string and customer avro model generic types and customer service config data. In the published method implementation, I log an info message and then create customer avro model, from customer created events using the data mapper methods. Then I call Kafka producer send methods and pass the topic name, reading it from customer service conflict data object and pass customer avro model ID as the key and the customer avro model itself is the message. For the last parameter, I get a callback method using topic name and customer avro model message. As you see this callback, returns a listenable future callback and has own failure and own success methods implemented. In case of failure, I log an error message here and in case of successful message, I log an info message. Note that since I haven't used Outback's pattern here, for simplicity, I cannot reuse the Kafka message helper, get Kafka callback method here. As I set up that methods to use outbox pattern. Finally, I log an info message in the publish methods and also use a try-catch statements and catch and log the error condition. The final customer module is customer application. Here I have customer controller. As you see, this is a rest controller and I set the request mapping as customers. One field is injected here, which is customer application service and I have a post mapping here, in the create customer methods, which request body objects create customer command. I simply log an info message and call application service, create customer methods and then wrap to create customer response object, to response entity with ok methods. I also have a controller advice here, with customer global exception handle class. I simply extend the global exception handler and catch customer domain exception, in the handle exception methods. Then log error message and return error DTO using PET request status and with exception message. Remember that I had a similar global exception handler, in the order service application module and this global exception handler, is from the common application module. All right we have completed the customer service, again using clean and hexagonal architecture principles. In the next lecture, I will update the order service, to complete the CQRS pattern and update the customer table in the order service, with the events published from the customer service. So I will see you in the next lecture.
