# Data access module: Adding Customer and Restaurant implementations

All right, now I will go to customer package and create customer entity class. I will add the same annotations from the order entity classes. For the table name, I will use order customer m-view and also set the schema as customer. As mentioned previously for the early examples, I will use a materialized view from the customer database schema in the order service. Here I will only add ID fields using the idea annotation. As mentioned, I will use this entity just to check if the customer exists or not, so I don't need to add any other fields for now here. Then I will create customer JPA repository interface in the repository package and extend JPA repository with customer entity and UUID generic types. And I will add repository notation to make this interface as sprint managed JPAB. Let's also create customer data access member class and add component annotation. And then I will add customer entity to customer methods. I will create a customer domain object from the customer entity here. For this, I will add a constructor with the customer ID fields into the customer domain object. I will also achieve the no argument constructor for customer here and create the object in this data mapper class. Finally, in the adapter package I will create the adapter implementation customer repository input class and implement customer repository interface from the order domain layer. Again, this is the output port for customer repository. I will add component annotation to this class to make it a spring-managed bin and I will create customer JPA repository and customer data access mapper and inject these fields using constructor injection. Then in the find customer methods, I will call customer JPA repository, find by ID methods and map the optional entity result object to an optional customer object using customer data access mapper, customer entity to customer methods. All right, then I will go to do restaurant package and create restaurant entity class. I will add the same annotations and say is the table name I will use order restaurant m-view and use schema restaurant. So as in the customer entity, I will use a materialized view from the restaurant database to query restaurant information in the order domain layer. I will create restaurant ID and product ID with UUID data type. I will put ID annotation for both of these fields. So the primary key will consist of those two fields. For this, I will go and create restaurant entity ID class and implement serializable interface. Then I will add getter, setter, welder, no args constructor and all args constructor annotations from Lombok Library. I will add the same restaurant ID and product ID fields here in the ID class also. Then I will also add hash code and equals methods to this ID class using the restaurant ID and product ID fields. Then I will use the ID class annotation in the restaurant entity class and referring to the restaurant entity ID class. This way I will set the restaurant entity ID as the primary key of the restaurant entity using this ID class annotation. Then I will add four more fields, restaurant name, restaurant active, product name and product price fields. Finally, I will add hash code and equals methods with restaurant and product ID fields which are the primary key of this table. Next, I will create restaurant JPA repository class and implement JPA repository with restaurant entity and restaurant entity ID generic types because the identifier of the restaurant entity is this composite class, restaurant entity ID. I will use repository annotation to create a JPA bin from this interface and then I will add find by restaurant ID and product ID in methods and return an optional list of restaurant entity object. And as parameter, I'll pass restaurant ID and a list of product IDs. Since I pass a list of product IDs, here I use product ID in, in the method name, so that means this will be converted to an SQL query using the in-statement. Let's also create restaurant data access mapper and use component annotation to make this class a spring managed be. I will create restaurant to restaurant products methods to return a list of UUID values for the products and pass the restaurant object as the parameter. Here I will get the products and get the product IDs and return a list of product ID from this method. Then I will create a restaurant entity to restaurant methods and return a restaurant object, and pass a list of restaurant entity objects as the parameter. Here, first, I will check if restaurant exists otherwise I will throw restaurant data access exception. So for the restaurant data access implementation, here I will create an exception package and create this restaurant data access exception class, and then I will extend round time exception. I will use this exception class in the data access implementation for restaurant. Then I will create a list of product objects from the restaurant entities. Here I will create the product objects using the products and price from the restaurant entity. As you see, I created product and money value objects here. Here I have a mistake on restaurant entity. The price field should be big decimal instead of string, let's fix that. Next, I return the restaurant domain object, creating it with the restaurant ID products and restaurant active fields. Again, here, this active fields should be Boolean instead of string, so I will fix that. Finally, I will create the adapter implementation for the restaurant repository from the domain layer. So I will create restaurant repository input class and implement restaurant repository interface. This restaurant repository interface is an output part in the order domain layer. I will add component annotation for this adapter and make it a supreme P. Then I will let restaurant JPA repository and restaurant data access mapper fields and inject them using constructor injection. In defined restaurant information methods, I will call restaurant data access mapper, restaurant to restaurant products methods and return the product idea list from the restaurant object. Then I will call restaurant JPA repository, find by restaurant ID and product ID in-methods and pass the restaurant ID and the product ID list as a parameter, and return optional list of restaurant entity objects. Finally, I will map the entities to an optional restaurant using restaurant data access mapper, entity to restaurant methods. Alright, we have completed the order data access module implementation. Let's continue with the messaging module of the order service in the next lecture.
