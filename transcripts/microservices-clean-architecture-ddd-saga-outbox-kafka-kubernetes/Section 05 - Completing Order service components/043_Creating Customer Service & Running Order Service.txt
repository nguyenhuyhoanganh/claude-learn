# Creating Customer Service & Running Order Service

Hi everyone. In this final lecture of this section I will create customer service and then run order service, Andslant. As mentioned, for now I will only create a Springboard main class and create database objects for the customer service. Then I will use a materialized wheel to create a customer data from the order service. Later in the course, I will come back to do customer service and use CQRS pattern to use customer data in the order service. Now let's create a new Maven module and name it as customer service. First, I will go and add the required dependencies in the Palm XML file. I will add Spring boot starter Spring boot starter data JPA and PostgreSQL dependencies. With these dependencies I will be able to create database objects and insert some initial data into the customer database. Let's also create Spring boot Maven plugin as in the order service. I just need to set the name of the image as customer service. Later, I will create a background image from this service but as mentioned for now I will just run it once and create database objects. Then I will go and create a package, com food ordering system customer service. And then create customer service application. Here I will use Spring boot application annotation with scan based packages com food ordering system and in the main methods I will call Spring application run methods. When I run this class it will start a service as a Spring boot application. Now I will create application yml file and set the configuration data. First, I will set the port as 8184 and then set Spring JPA and data source properties. I will copy these properties from order service and just change current schema as customer in the connection url. I will also create a data property and set the inits data SQL file. So here I will need two SQL files: one to create the schema and one to initialize the data on that schema. I will create init schema and init data SQL files here. In the init schema file, I will create customer schema and create UUID extension to use UID data type in Postgres. And then I will create customer's stable with ID, username, first name, and last name, and with primary key id. Then I will create the order customer materialized view it's name order customer m view. In this view, I will use ID, username, first name and last name columns from the customer table. But you can also use only a subset with the required fields in this materialized view. Remember, I said this (indistinct) name with the customer schema in the customer entity. I will call refresh, materialized comments to create the data in the materialized view. A materialized view is a physical representation of a query that might include one or more tables with a rare condition. The difference from a normal view is that with materialized view, the data is physically stored in disc instead of running the query each time. So performance-wise, it is much better as it caches the results of a query in disc and return it. So if that's the case, half this data could be refreshed, if the data is changed in the tables that the view is using. For this, I will create a function refresh order customer m view, and call it for each data manipulation object on the customer table. So in this function, I will call refresh materialized command. Now I will create a trigger refresh order customer m view on the customers table for each insert updates and delete operations and call execute procedure command, using the function refresh order customer m view. This will refresh the data in the materialized view whenever data is changed in the customer table. Now, I will also create the inits data SQL file content and insert two customers. I will use this data to test the functionality of the order service application. Let's run this service and just create the database objects. Now if I go to Postgres, I will see the customer schema and the customer table and also I need to see the data in the customers table. Great. Now I will run the order service using the local Kafka Cluster and see if the functionality that we have implemented so far is working. So I will run ZooKeeper. And then I will run Kafka Cluster. But before running Kafka Cluster I will be sure that ZooKeeper container is started. Now I will open Kafka Manager using local host 9000. As you see, I have the topics for payment and restaurant approval here because I have created them previously and I have a persistent store to hold the topics and the metadata. For now, order service, we'll only send data to payment request topic and I will check that topic data. In the coming sections, I will continue with the rest of the implementation with payment and restaurant services. Let's run order service using the Spring boot main class in the order container. Be sure that you have the local Postgres running and you set the correct username, password and it's schema name in the application yml file. I will first go and check local Postgres database. There should be the orders schema next to the customer's schema. All right, I see the orders, order items and order others tables. Now I will go to Postman and make a post request to the order service rest API using the input JSON that I have mentioned before. As you see the order returns in the pending state. And in the (indistinct) of the order service, I can see that the order is created on local database. And also the order created event is sent to Kafka. Let's first check the Postgres table. As you see the order is created here. Then I will use Kafka CAT utility to check if the data is on the Kafka topic, payment request. Remember, this Kafka CAT installation is covered in the setup section. If you don't have it, please install it first. When I run Kafka CAT minus C minus B local host 19092 minus T payment request command I should see that a single result is returned, which is the order created event that I created with my post request. All right, in the next two sections I will create payment and restaurant approval services. After that, we will be ready to create a robust and resilient distributed transaction using SAGA and Outbox patterns. So I will see you in the next section.
