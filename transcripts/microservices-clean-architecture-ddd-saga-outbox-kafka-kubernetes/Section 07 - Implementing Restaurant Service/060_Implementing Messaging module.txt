# Implementing Messaging module

Hi everyone. In this lecture, I will implement the restaurant messaging module. In this messaging module, I will use generic Kafka modules to implement the adapters for the publisher's output ports of restaurant domain layer. Also, here I will create the Kafka listener implementation to be used by the domain layer. Let's go to do pom XML file and set the required dependencies. In this module, I need the restaurant application service dependency which is in the domain layer and holds to output ports, and here I will implement these ports with adapters so I need these dependency. I will also add the Kafka producers, consumer, and Kafka model dependencies because I will implement messaging using Kafka modules here. Next, let's create the base package com food ordering system restaurant service messaging. Again, this will be very similar to do order and payment messaging modules. So I will put the classes and interfaces of this restaurant messaging module and then go over them. Here I have listener Kafka, publisher Kafka, and mapper packages. In the mapper package, I have the restaurant messaging, data mapper class marked with component annotation. I will inject and use this data mapper class to convert domain and messaging objects. Here I have three methods. First one is order approved events to restaurant approval response Avro model. It will create an Avro model object from the order approved events. As you see here, I set ID with a random UUID and set this SAGA ID as an empty string. Again, I will fill this SAGA ID when I implement this SAGA pattern in the next section. As you see here, I set the fields of Avro model by simply reading them from order approved event objects. Then I have the mapper methods to convert order rejected events to restaurant approval response Avro model objects. Then I have restaurant approval request Avro model to restaurant approval methods to create a restaurant approval request object from the restaurant approval request Avro model object. This method creates a restaurant approval request from the Kafka object to send to data to domain layer on the restaurant service. As you see here, I set ID, SAGA ID, Restaurant ID, and Order ID and then the restaurant order status, products, price, and created a set fields. For each methods, here I have used to build pattern which is implemented in the data classes. Great, then I will go to do publisher package. Here I have two publishers for each restaurant event. First one is order approved Kafka message publisher. I have SL4J annotation here to create a local variable and component annotation to create a spring managed pin from this class. This class implements order approved message publisher interface from the domain layer of restaurant service. Remember, this order approved message publisher interface is the output ports and I implement it between adapter here in the messaging module. I have data mapper fields then Kafka producer fields with string key and restaurant approval response Avro model. Is Kafka producer is the interface defined in the Kafka producer module? Then I have another field here, restaurant service config data. I use this config data to read the Kafka topic names. Finally, I have the Kafka message helper fields to use the call back methods. As you see here, I used constructor with these four fields and inject them using constructor injection. In the published methods, I get to order ID from the order approximates and then login info message. Then I convert the events to restaurant approval response Avro model object using mapper class. And then I call Kafka producer send methods. I get the topic name from the restaurant service config data for the key of to Kafka records, I sent the order ID and for the value, I sent restaurant approval response Avro model. Finally, I called the Kafka message helper get Kafka callback methods. This methods will return a listenable future callback and it will be called when the Kafka producer gets a response from Kafka cluster. Remember, Kafka producer sent methods is an asynchronous methods. That's why I need a callback here as I will not get a response immediately from Kafka. In the published methods, I log an info message and also use a try catch statement to log an error message in case the Kafka producer sent operation is failed. Then I have order rejected Kafka message publisher similar to the order approach message publisher. Here I used order rejected message publisher interface and used the events order rejected events in the published methods. Right, now I will go to do listener Kafka package. Here I have restaurant approval request Kafka listener class. I use SL4J to create a log variable and component annotation to make this class as spring pin. And then I implement Kafka consumer interface from the Kafka consumer module and use restaurant approval requests Avro model as the generic type for the Avro type for this Kafka consumer. Then I have two final fields here and I inject them using constructor injection. These fields are restaurant approval request message listener and restaurant messaging data mapper. These restaurant approval requests message listener is the input ports and it has the adapter implementation in the restaurant application service module. Here, I just need to listen Kafka messages and trigger that adapter implementation. On the receive methods, I have the Kafka listener annotation, which is the spring Kafka annotation to create a Kafka consumer from a simple method. Here I give the consumer ID using the configuration parameter, restaurant approval consumer group ID and I give the topic name using the restaurant approval request topic name from the configuration variables. I will define these configuration variables in the next lecture in restaurant container module application yml file. Then I use payload annotation for the list of messages and use the header annotation for keys, partitions, and offsets. And then I log an info message here and then for each message I will get the restaurant approval request Avro model, and again I log a message and call restaurant approval requests message listener approve order methods. To create the parameter, I use the restaurant approval requests Avro model to restaurant approval methods of the restaurant messaging data mapper. As you see, the approve order methods is implemented in the restaurant domain layer, in the restaurant application service module and it will find the corresponding events after persisting the changes in the restaurant local database. Then that event will be used by the order service to continue with the operation. Alright, the last module of restaurant service will be the container module which will include a Springboot starter class. So it will start the Microservice itself. And it will have all the dependencies to run the restaurant service domain logic together with the adapter implementations. So I will see you in the next lecture to complete the restaurant service with the container module.
