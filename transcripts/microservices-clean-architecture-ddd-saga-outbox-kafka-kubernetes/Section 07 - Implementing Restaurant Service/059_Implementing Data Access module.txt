# Implementing Data Access module

Hi everyone. Let's continue with restaurant service data access module. I will first add to dependencies restaurant application service, Springboard Starter data JPA, and Postgresql. I will create the base package com food ordering system restaurant service, data access, restaurant. Again, I will put the implementations here and go over it for the data access module because it has straightforward classes and interfaces. Now, here in the anti package, I have order approval anti class it's getter, setter, wielder, no axe constructor and all axe constructor annotations. Remember, these are lombox annotations to get rid of all your related Java courts, to create getter, setter and contractor methods and also to create a wielder pattern. Then I have annotation to make this class a JPA entity. And also here I have table annotation with the database stable name order underscore approval. In database kmar restaurant. I have ID fields with UUID type and use ID annotation to make these fields the primary key of the table. Then I have restaurant ID and order ID fields and finally order approval status with enumerated string type. So I will create an annam type one database as you will see in the schema SQL file in restaurant container module in the coming lectures. And here I also need restaurant entity but I would like to use the same entity class from the order data access module using the materialized view. So instead of repeating the quote here and creating the same entity I will create a common data access module and put my common classes in that module and I can use it from order service and restaurant service. So I will go to the common module and create a submodule common data access. Here I will let Spring data JPA and Postgresql dependencies in the Palm XML file of common data access module. Then I will create a package for restaurant data access, com food ordering system, data access restaurant. And then add three sub packages, entity, exception and repository. In the entity package, I will go and get the restaurant entity and restaurant entity ID from the order data access module. But I will add a new field also, volume products available because I will reuse this entity. But in the restaurant service I will need this new product available field also. And I will copy restaurant data access exception from order data access restaurant package. And finally I will get restaurant JPA repository. Then I can delete these three packages, entity, exception and repository, in the order data access module on the restaurant package. After doing this change, I need to go to the base palm XML file and add common data access dependency with project version. Now in the order data access, I will let this common data access dependency. One important thing now is to change to order service springwood application class and adds com food ordering system data access to the repository and entity base packages. Because if we use this package as well to use the restaurant repository and entities, I will add this base package also to the restaurant service springwood application class in the coming lectures. Now, I will also add this common data access to do the restaurant data access module cause I will use the entities from this common module in the data my implementations. Now after adding the common data access module to do order data access and restaurant data access, let's rebuild the project now, and see if there is no compiled time error. Here I will get some import statements errors and I will fix them in the order data access module. Now after creating the common data access module and reusing it from restaurant service and data access I will continue reviewing the restaurant data access implementation clauses. I will go to do mapper package. And here I have the restaurant data access mapper clause. This is again a similar data mapper clause as in the other data access modules. Here I have restaurant to restaurant products methods to create a list of product IDs from the restaurant objects. Here I use to order detail object in the restaurant object and get the products as you see here. Then I have restaurant entity to restaurant methods. Here I create a restaurant object from a list of restaurant entities and I use the restaurant entity list to get the product list and create a list of products with ID, name, price and a availability. Note that these two methods are similar to the ones in the order data access restaurant package. I have slightly different objects, so I prefer to use different methods in different mappers for them. And note that this restaurant entity is defined in the common data access module that I just created. And I have order approval to order approval entity methods which simply creates an order approval entity from order approval domain objects. And similarly, I have order approval entity to order approval methods, which creates an order approval domain object from the entity JPA object. Next, I have the order approval JPA repository interface, in repository package. Here there is a repository annotation to make this clause a spring data bean. And here I extend JPA repository with order approval entity and UUID generic types. Order approval entity is the entity for this repository and UUID is the primary key type for the entity. Finally, for the adapter implementation, first I have the order approval repository input clause, with component annotation. So this clause is a spring bean and here I implement order approval repository interface from restaurant domain layer, which is the output port, defined in the restaurant application service. I have two fields here, order approval JPA repository and data access mapper. And I inject them using constructor injection. Then in the same methods I call JPA repository same methods and use data access mapper methods to convert the order approval domain objects to JPA entity, to send in the parameter of those same methods and also to convert the return to JPA entity to order approval domain objects. Second, here I have the restaurant repository input class with spring component annotation. This one implements restaurant repository interface from the restaurant domain layer. I a restaurant JPA repository and restaurant data access maple fields, injected using construct injection as usual. Then in defined restaurant information I call data access mapper restaurant to restaurant products methods and get a list of UUID values for products using the parameter restaurant. Then I call JPA repository, find by restaurant ID and product ID in methods. And then I use data access mapper, restaurant entity to restaurant methods, to create an optional restaurant object from the list of restaurant entities. Again, this implementation is the same of the one in the order data access, but I have different objects and so the data map implementation is slightly different. And because of this I used a different restaurant repository input class here. Before closing, I will fix my package name here. I have a typo. I will rename the package name to use the data access. Here I used more a's than necessary. Great, after completing the restaurant data access module let's continue with the messaging module in the next lecture, which is another adapter implementation module.
