# Implementing  Container module

Hi everyone. In this lecture I will complete the restaurant service with the container module. I will first go to POM XML file. Remember that the container module is the dirtiest components. That means it has a dependency to all modules to be able to run all modules together. So I will add restaurant domain core and application service and also data access and messaging dependencies. Also, I will add spring boot starter dependency because this container module will start a spring boot application and I need to require its annotations to use from that dependency. Finally, I will add spring boot Maven plugin. It will create a runnable jar file from this container and here I also the docker image name and use the build image call of the spring boot Maven plugin to create a docker image in the local docker registry. This Docker image will be created when I run the maven install commands. Now I will create the base package com food ordering system, restaurant service domain and I will create restaurant service application class. I will add spring boot application annotation with base packages calm food ordering system and I will add entity scan and enable JPA repositories annotations with base packages ending with restaurant service data access. And for this JPA repositories and entity scan, I will also add com food ordering system data access package because I will use the common restaurant repository from that package in the common data access module, remember that I dID the same in the order service spring boot main in class. As you see, this is similar to the order service spring boot main class, but I just changed the package names. And I will create the main methods and call spring application run methods to run a spring boot application with this class. And finally, I will add bean configuration class and add configuration annotation. This is a spring configuration bean and here I will add the methods restaurant domain service with bean notation and it'll create a spring bean from the restaurant domain service implementation. As you see here I return a restaurant domain service interface. Remember, I haven't put the spring dependency in the core domain layer of restaurant service as in the payment and order services, but I still want to inject and use this restaurant domain service from the other modules. So here I create a bean configuration in the container module for this purpose. Now I will go to resources folder and create the application YML configuration file. And I will add the config data for the restaurant service. First I set the port as 8183, then I set the log level as debug and then I create restaurant service prefix and add the restaurant approval request and response topic names. Remember that in the restaurant service config data class I used this prefix restaurant service. So actually these topic names will be mapped to an instance of this restaurant service config data class and I will use it to consume and produce on Kafka topic. Then I have this spring JPA and data source properties. Now if I look at application YML order service actually I will need the same config for JPA and data source so I will copy that and just change the schema name as restaurant here. Also, I will add the data property, which in its data is squared. Here in the restaurant service, I will create some data at startup to use in the demonstration for the explanation of this JPA and data source properties. Again, you may check the order container lecture cause during the implementation of order service container I have gone through the explanation of these properties and again, I need Kafka config producer and consumer config properties and I will get them from the order service configuration file and here I just keep the restaurant approval consumer group ID property as a consumer group ID. Please check the order container lecture if you haven't gone through the explanation of these properties. Now I will create init schema file. Here I create the database objects for restaurant service. First I create the restaurant schema here and then create the UUID extension to be able to use an UUID type in Postgre's. Then I create restaurants table in the restaurant schema. Here I have ID, name, and active fields, and the primary key for this table is the ID field as you see here. Then I have approval status and enum type with approved and rejected values, As you see this enum type corresponds to the order approval status, Java enum type. Next I'll create order approval table. Here I have ID, restaurant ID, and order ID, UUID fields and then I have the approval status using the enum type that I just created: approval status. Finally here I have the ID fields set as the primary key of the order approval table. Then I have products table with ID, name, price, and available fields. Here again, the ID is the primary key. Then I create restaurant products table with ID, restaurant ID, and product ID. In this table, I will keep the product of a restaurant. Again, the ID field is the primary key here. Here I have two foreign keys for restaurant ID to restaurant table and for product ID to the products table. Finally, I have the materialized view its name or the restaurant M View. Materialized View is a database view that persists the results of a query on disk instead of running the query each time it is called. Here I joined three tables restaurant, products and restaurant products and gets the ID, name, and active fields from restaurant table and then gets ID, name, price, and available fields from the products table. I refresh the materialized view here and also create a function refresh order restaurant M view to refresh the materialized view, and I call this function in a trigger when the restaurant products table is inserted, updated, or deleted. So I will keep the materialized view up to date and the data in the restaurant table changed. Okay, let's create init data SQL file also. Here I will insert some initial data for restaurant and products. As you see, I have two restaurants, four products, and I put two of the products to the first restaurant and two of the products to the second restaurant. All right, init container module restaurant service implementation is completed. Let's build the project and then run maven install command. As you see, I have a successful build with maven install commands. Please note that this Maven install command also creates the docker images for the services using spring boot maven plugin. I will add and commit all my changes here and create a backup branch before Saga to create a branch. I will use git checkout minus B command and give the name of the branch. Then I can switch back to the master branch using git checkout master command because I will continue using master branch for the saga pattern implementation in the next section. So I will see you in the next lecture.
