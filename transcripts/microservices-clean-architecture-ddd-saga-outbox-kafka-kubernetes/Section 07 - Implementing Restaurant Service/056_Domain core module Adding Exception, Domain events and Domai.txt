# Domain core module: Adding Exception, Domain events and Domain Service

All right. In this lecture I will finish the restaurant domain core module implementation. Now let's create an exception class restaurant domain exception in the exception package and extends domain exception. I will use this for the exception situations in the restaurant service. Here I will also create RestaurantNotFoundException and again extends_DomainException. Then I will create two domain events. First, I will create order approval events class and implement domain events with order approval entity as the generic type. I will make this class abstract as I will use it as the PACE event class. I will let four final fields; order approval, restaurant ID, list off string failure messages, and create that zone date time. I will create constructor with these four fields and create to get your methods here. Now, I will create order approved events and simply extend the order approval events. In the constructor, I will call the super class constructor. As you see here, I need to override the fire method also from the base domain events interface. Remember that I have created this fire methods to easily fire any events from the application service. To publish an event in these fire methods, I will let the field domain event publisher with order approved event generic type. And then set this in the constructor. Then in the fire methods, I will call publish methods of the domain event publisher and use the current event with these keywords. Similarly, I will create order rejected events. Let's just copy the order approved event and change the generic type to order rejected events in the domain event publisher. I will also rename the field to order rejected. Now I will create restaurant domain service interface and add validate order methods. It'll return and order approval events and take parameters restaurant and failure messages. Also, I will add order approved message publisher and order rejected message publisher using the domain events publisher interface and setting the generic types here. Then I will implement this interface with restaurant domain service impl class. Here I will use Slf4j annotation to create a log variable. Then in implementation, I will call restaurant validate order methods and pass the failure messages as parameter. I will log an info message here and then check if there is any failure messages. If there is no failure message, I will log a message and I will set the order approval status as approved by creating order approval with the construct order approval methods and then return and order approved events. I will pass order approval, restaurant ID, failure messages, current zone date time with UTC zone, and order approved message publishers. For UTC, I used the constant from the common domain and here I pass the domain event publisher as the parameter to the order approved event. As you see, I pass failure message as it is, but it is empty in case of approved order. And if there is any failure, I will log a message and set the status as rejected and then return an order rejected event. In the order rejected events constructor, I will pass order rejected events domain event publisher as the last parameter as you see here. Great, let's finish the domain layer with the restaurant application service module in the next lecture.
