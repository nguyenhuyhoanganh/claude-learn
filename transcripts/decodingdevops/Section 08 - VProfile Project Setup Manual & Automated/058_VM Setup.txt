# VM Setup

(letters beeping robotically) Instructor: All right, in this lecture we are going to set up virtual machine by using vagrant file. All the information is available in the source code. So first open the browser and go to this URL, github.com/hkhcoders/vprofile-project. This is the source code for our project. First, we need to clone the source code in our local machine, so I'm going to use VS Code. If you have any other preference, you can use that. First, let me copy the URL here. So click on this dropdown. You see HTTPS URL. Click on the copy and I'm going to open VS Code, Visual Studio Code. Again, you can use any other editor of your choice. So to clone the source code, you need to click on this button over here, source control and say Clone Repository. Paste your URL over there and hit Enter. Select a path. So I'm going to go to F drive. You can clone it anywhere you want. I'll create a folder over here. I will name it as hkhcoder and select this as the destination. Okay, I would say that open in the same window. Okay, there it is open. Now, before we do anything, it's very, very important that you switch the branch, source code will have different branches, we'll cover all that in the GIT section of this course. But for now, what you need to do is just click on this button over here where it says main. It's too small, I know. And select origin/local. Yeah, make sure you switch that. You should see local over here and nothing else. All right, so let's look at the source code. So we will be using this folder over here, vagrant and it has sub folders. First, we will be doing manual provisioning. That means we'll bring up the virtual machine and execute all the steps one by one. So manual provisioning Windows and Mac OS with Intel chip, need to use this folder. If you have MacOS with M1, M2 or M3 chip, then you use this folder. Okay, so with the folder you see over here a vagrant file and a PDF document. Let's go and check this PDF document. So I'm going to open the file Explorer, F drive, hkhcoder. This is where I code. I see here the profile-project and vagrant. And I'm going to go to this folder over here and there's the PDF document. I'm going to open this. You can download this from the resource of this lecture as well. So let's see what we got. So prerequisite, Oracle VM virtual box and vagrant. This we already have and a vagrant plugin. I'm going to explain about this plugin later, but all you need to do is execute this command and they should install the plugin, vagrant-hostmanager. Now, the word, plugin, most of the time means an extra feature. So we use this tool, vagrant, and this plugin will add some extra feature to our vagrant tool. What is that feature? We'll see that in some time, but all you need to do first is copy this command, open Git Bash or your terminal if you're using MacOS. So I'm going to open Git Bash and execute this command. Very important. We'll see what is the benefit of this in some time. Okay, that is installed. Okay, so then it says VM setup. We clone the source code, we switched the branch local. We go into a right folder and we say vagrant up. That should bring up all the virtual machines. Before we do that, let's take a look at the vagrant file. So go back to your VS Code, manual provisioning Windows, Mac, Intel. If you're using MacOS M1, M2 chip, use that folder and open the vagrant file. Okay, so this is a multi-VM vagrant file and we have five VMs over here. DB VM, Memcache VM, RabbitMQ VM, Tomcat VM and Nginx VM. We have here two global setting, config host manager enabled. I told you, I'm going to explain this later. When we bring up the virtual machine, I'm going to show you what this does. So this is just any other multi-VM vagrant file that see few differences like we have here, hostname mentioned. So when we log into our VM, we will see hostname here as db01 for this machine and the IP address and the memory size. We've seen all this in vagrant section. So I have given here just 600 MB of memory for DB, Memcache, RabbitMQ, Tomcat, 800 MB, Nginx 800 MB. Now, this is the minimum that we can go, but if you have a better computer, let's say you have 16 GB of RAM, or more than eight GB of RAM, you can go till 1,024 MB. That will be one GB per VM, but I have kept here minimum. So majority of the people can use this vagrant file. Okay, so the first four VMs, we use CentOS9, for Web01, which is Nginx VM, we use Ubuntu. Same in the vagrant file for Macs M1, M2 chip, we have CentOS for first four VM and Web01, we have Ubuntu. So there's not much difference except the box name is different. Let's bring up these VMs and then see the settings that we have mentioned over here, how does it take effect, so I'm going to open Git Bash to execute my vagrant command, MacOS people can use terminal. So here's my Git Bash and I'm going to switch to the folder where I have my source code. So mine is in F drive, I have hkhcoder, in this, there is vprofile-project. Hit Enter. Do ls, and you should see the source code. I'm going to switch to vagrant and ls, right, the four folders. So I'm going to go to manual provisioning Windows, Mac, Intel, this folder. So choose the folder based on your operating system. So people with Windows, MacOS, Intel chip, even Linux users like Ubuntu should go to this folder except MacOS with M1, M2, M3 chip, everyone should be using this folder. Okay, let me clear the screen. Okay, let's do ls. We see here vagrant file and we see a PDF document. We'll go to the document. By the time we'll bring up the virtual machine, it takes time to bring up these VMs, by the time we'll go through the documents, but before I bring up the virtual machine, I would like to check if there are any other VMs already running so I can clean it. So vagrant, space, global-status. Okay, so I don't see any VM, but if you see any VM, go to that folder. It'll show you the directory over here. Go to that directory and destroy vagrant, destroy, delete those VMs. And once you are done cleaning up everything, you run this command, vagrant global-status--prune. So the prune command, if it has any stale entries, it'll remove those entries. You'll have a clean setup and then you can do vagrant up. Okay, for some people, this might ask a password to enter. That will be your own computer password. This is not the VM password. It is going to ask your own computer password. Most of the people it won't even ask. Just do vagrant up, again, make sure you are in the correct folder and do vagrant up. This is going to take some time, by the time, let's go back to a document and take a look. Okay, so here it says, all the VM's hostname and etc host file entries will be automatically updated. We'll see what is this when we bring up the VM and why it is important. In fact, this is the reason why we installed this plugin. We'll come to that in a moment. So we have the architecture diagram that we did in the previous lecture. So these are the services that is in this project. Nginx, Tomcat, RabbitMQ, Memcache, ElasticSearch, MySQL. Now we are skipping ElasticSearch, because this needs lot of amount of memory. So you're skipping this service. Okay, so it says the setup should be done in this below mentioned order first, MySQL, then Memcache, then RabbitMQ, then Tomcat, then Nginx. Now the VMs are coming up, but they're just bare operating system. We are going to install these services and there is a specific order mentioned over here. In real time, also, you should follow similar order. First, you set up the database and any other backend services that you have that uses the database like Memcache here connects to MySQL database. So first we need to set up MySQL. If it's up and running, then we go to Memcache. So Memcache can connect to it, one by one everything is getting validated and finally we come till Nginx. So once we access the Nginx service, all the other backend application service already are up and running. Now this is a good practice, not mandatory. Some places it is mandatory. Some application depends on the database to come up. That will be mandatory, but this should be anyways a standard order and when we shut down, the order should be reversed. First, we need to cut the user access. So frontend service, we need to stop first like Nginx, and then application service like Tomcat, then RabbitMQ, then Memcache, then MySQL. Again, a standard practice, not mandatory. So first, we have the MySQL setup and we'll come to all this commands. Let's see if the VMs are up or not. Okay, so I have RMQ01 coming up. So I'm going to pause the recording and resume once all the VMs are up. Okay, all my VMs are up. If you see some other error like timeout, or something like that, then just do vagrant reload. You can just do vagrant reload that VM name to reboot that, and then you can just again do vagrant up, or you can just say vagrant reload to just reboot all the VMs. But if it comes up clean, no worries. First, I'm going to log in to Web01, vagrant ssh web01. Okay, so once we log in, we see there is the hostname configured web01. I'm going to switch to root user and I will show you one nice entry that is created automatically by vagrant just do cat/etc/hosts, and you should see this entry in the host file. Okay, so the host file of any computer works like this. If you try to access a machine with the name app01, it is going to look for it's IP. First, it is going to look for the IP in the host file. If it's not finding in the host file, then it'll go and check the DNS of your ISP. But here we have the entry, app01 means this IP address, db01 means this IP. We did not add this entry, vagrant did it by using the vagrant host manager plugin. So this is the plugin that we installed initially, if you remember, and let's go to the vagrant file. In the vagrant file, we made this two entries, config.hostmanager.enabled true, config.hostmanager.manage_host=true. Now, this is just available in it's documentation in the GitHub repository. If we just Google, you'll find it. It's no big deal. Just install the plugin, add these entries, and automatically your VMs will have host name to IP mapping. Okay, you can check that in login and check in every VM in etc hosts file, you'll find this entry. Now, this is how it'll work in our configuration files, we'll have names like the name of app01. Right? Now, if we do ping app01 -c 4, that means it is going to first resolve app01 to an IP address. You see that? That is happening through the host file, -: c 4 means count for four packets. It's going to send four packets, and if you get a reply back, that means we are able to connect. This is just a simple sanity test. If you're not getting any reply, you're getting timeouts, or something like that, then you need to reboot that machine, coming from app01, then reboot app01. Okay, now, I give you one exercise over here. Log into every machine, exit from here. Okay? Log into then app01 and ping the backend services like MC01, RMQ01, DB01. Right? Try that, if you see anything does not give a response back, then you need to reboot that machine. Not a mandatory test for now, but just an exercise.
