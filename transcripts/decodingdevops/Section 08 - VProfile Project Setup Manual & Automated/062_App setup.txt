# App setup

Instructor: Okay, so the Tomcat setup is two step setup. First, we're going to set up the Tomcat service. Now this is same as what we did in the background and Linux section. We set up Tomcat service. In this lecture, Systemctl &amp; Tomcat 10. In this, I have explained the setup of Tomcat in detail. So if you have not gone through this, then check this lecture and then come back to the project, Tomcat Setup. Okay, so if we are going to run dnf update -y, install epel-release, install JDK 17, install git, wget, and download the Tomcat binary. We do not have a direct Tomcat installation, like dnf installed tomcat10. We're using Tomcat 10 here. So we download directly the binary file. We extract it, we add a user called tomcat with a home directory, /usr/local/tomcat, without any login shell. So this command is going to create a user called tomcat and also going to create this folder. Then we're going to copy the extracted Tomcat 10 binary into the folder that was created in the previous command. We give it, the tomcat, ownership. We create a Tomcat service file, and because of this, we can then manage Tomcat with systemctl command. Then we say systemctl daemon-reload, start and enable Tomcat service. So same thing what we did in that Tomcat setup lectures. So I'm just going to execute all this, all this command one by one over here. Install epel-release, install JDK 17, install git and wget. Move to Tomcat, sorry, move to temp folder. Let me clear the screen, and download the Tomcat 10 tarball. Okay, I'm mistaken. Copy paste. Could copy it again from here. Okay, that should be just one single URL. And in front of this, there should be wget. So wget and this URL. Okay, the tarball should be available over here. Let's extract it. So tar -xzvf apache-tomcat. Extract it. You should have the folder. Okay, next, with this command, we are going to create the tomcat user and that home directory for it, which will be the folder to live, or home directory of tomcat. Okay, next step, we are going to copy recursively everything from that folder to /usr/local/tomcat/. And in /usr/local/tomcat/, we should have all this content. If you do ls -l in this folder, or on this folder, then you should see everything is owned by root user. We have to make sure everything is owned by tomcat user. So tomcat user, tomcat group, recursively, give permission to /usr/local/tomcat/. Okay? Let's do ls -l once again. Everything is owned by tomcat now. Okay, then we are going to open or create this file, tomcat.service, this is a systemctl file. Insert mode, and we copy all this content from here, all this black section, just copy that, just paste it here. Okay, let's give some proper space over here, just to look better. Okay, this is good. And let's just save and quit it. Then systemctl daemon-reload. Whenever you make any change to systemctl files, you need to run this command. Then we are going to start the Tomcat service. That will be the name of the service, tomcat, because this file name is tomcat, tomcat.service. Okay, next, let's enable it. Okay, let's check its status. All right, Q to quit. Okay, so that was Tomcat setup. We did this already, and this is the second time we are doing it. Again, the firewall rules. So we don't start the firewall, anyways, there is no firewall rules. So you can run those commands or not run, does not matter for this project, as I said, we're going to cover that later. Okay, now this is the main stuff. So we have our source code, right, in branch local. That is the code, that does not run on the Tomcat service. You need to compile it, create a package, and then Tomcat understands that, that package, Tomcat can run or host. So in order to do that, in order to compile and build the package, you need a tool called Maven. We have a separate section for Maven, and there's lot of Maven and build tools in Jenkins lecture, lot of CICD. So all these things will be crystal clear later. For now, all you need to do is download and run Maven. Download the source code, build your source code with the Maven command, and then you need to deploy the artifact. You need to remove the default Tomcat application and copy your build artifact into Tomcat and start the Tomcat service. I know this will be too much for now, build and source code and stuff. Trust me, everything will be clear. It'll be more than clear. We are going to do a lot of CICD work later. For now, understand this much. We are going to pull the source code, build it with Maven, and deploy it on Tomcat, three steps, right? So in order to build with Maven, first we need to have Maven. So we're going to download Maven first. So this is the command. Okay, with wget command, I'm going to download the Maven binary. Okay, it's a zip file. So we're going to run unzip apache-maven. So Maven is also from Apache, and Tomcat is also from Apache, and also httpd is also from Apache. Okay, there we have the Maven folder. So we're going to copy this Maven folder into this location. Nothing specific, just we need a location. We're just copying it. So when we run this Maven command, it is going to fail, saying out of memory. So we're just giving this command that, you know, the Maven has 512 MB of memory, actually it does not have, we're just fooling it around. So there is low memory in this VM itself. So it cannot allocate 512 MB. So by running this export command, we are setting this variable and we are saying that you know, hey, you have 512 MB of memory. Well, it does not have, just fooling around, but if it needs it can go till 512 MB of memory. That's what we are seeing here. Okay, now the critical step. We are going to clone the source code, right? The branch is local, okay? We should have the source code here, vprofile-project. Let's get inside this. Let me clear the screen, do ls. Okay, so we have seen this src folder while we're setting up MySQL, right? So /src/main/resources/, over here, along with this SQL file, we have one very important file, application.properties. This is the file that our applications will use, or VProfile application will use to connect to backend services. Like how does it know where is database, what is the username, what is the password, where is RabbitMQ, what is the user? All that information will be in this file. So let's open this file. Okay? So to do vim src/main/resources/application.properties. Okay, make sure you are in this vprofile-project folder first before you run this command. Okay? So here it is. So jdbc.url, right here it says db01:3306, that's the port number. And it is connecting to the database with this name, db01. And this will be resolved to an IP address, because in the host file we have that entry. We've seen this in the beginning in the VM setup, right? So db01 will be resolved to an IP address. And this the database name, accounts. That's why we created a database named accounts. Username, admin. And password, admin123. That's why we had those admin user, admin123 password. If you change it over there, you have to make sure you change it here, right? It's not a hard and fast rule that you need to have this user, this password, this DB name. If you want different, you can create different, but make sure then, you update application.properties file. Same then, you have memcached, mc01, it's port number 11211. Then you have rabbitmq, rmw01, port number 5672, username, test, password, test. And this file, we will be changing later. In AWS, we'll have project to set up all this move, migrate to AWS, we'll be making changes. And then in Docker, in Kubernetes, various other project that we'll do in this course, we will be making changes to this file. So nothing over here, same thing what we did. So we don't need to make any change, but I want you to remember this path very, very well. In the source code at this location, we have this file. Also in real time you need to be sure about where is the configuration file, where this DB information, backend information mentioned, right? Because when you build the package, right, we're going to build it with Maven, this configuration file will be in that package, or sometimes they are set with the variables also. In any case, you should know where are the backend configurations. Okay, so nothing to update over here. We have all correct information. Now we are going to run this Maven command and build our source code. Okay, so this is the path of where the Maven binary is, /usr/local/maven3.9/bin/mvn, 'cause this is where we copied the Maven binary, space, install. So this command is going to basically take our source code and build it into a package. Once this command completes, we'll have something called as artifact. So let's run this command. Once again, this will be much more clear when we do Maven, continuous integration, continuous delivery, okay? And this is going to take some time. So again, I'm going to pause my recording and resume once this is completed. Okay, it's completed. Let me clear my screen, do ls. And you should see here a new directory called target. If you do ls target/, you should see here artifact, vprofile-v2.war. Like we have tar.gz, like we have zip, war is an archive in Java. So this is the package. Now we need to copy this package into the Tomcat home directory at this location, ls /usr/local/tomcat/webapps/. In this, you have a folder called ROOT. We are going to remove this one. This is the default Tomcat application. We'll remove this one. Instead of that, we're going to copy our artifact. So let's remove, -rf. Make sure you are over here, or for this, you can be anywhere. We're giving absolute path. So rm -rf /usr/local/tomcat/webapps/ROOT. Okay, hit Enter, remove that. And then make sure you are at this location, vprofile-project. You have the target, cp target/vprofile-v2.war, copy to /usr/local/tomcat/webapps/ROOT.war, capital ROOT.war. So we copy the artifact with this name. So that becomes a default application, okay? After we do that, we are going to change the ownership, because once we extract this ROOT, it will have different user ownership. We're going to give it to the tomcat ownership. Okay, now let me show you something. You do ls /usr/local/tomcat/webapps/, and you copied it with this name ROOT.war, but you should see a folder called ROOT. So Tomcat extracted that into this ROOT folder, okay? And that is it. It is done. You can restart the Tomcat service if you want, systemctl restart tomcat. It's not mandatory, because Tomcat will, on the fly, extract the artifact, create the folder for your artifact. Okay, so the Tomcat service is restarted. Let's check its status. Okay, active running. You use Q to quit. Okay, so the Tomcat service is also up and running, and our application is running on that. We can validate that, but let's set the last service in our project, which is Nginx. So exit from here, complete this, and join me in the next lecture where we'll set up the last service, Nginx.
