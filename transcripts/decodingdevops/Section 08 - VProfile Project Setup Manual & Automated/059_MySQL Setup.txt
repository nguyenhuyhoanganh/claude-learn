# MySQL Setup

Instructor: All right. In this lecture, we are going to do the database setup. If you are just continuing from the previous lecture, then you can continue the rest of the step. But if you took a break, a long break, then I recommend you run a vagrant reload command to reboot all the machines. Make sure you're in the right folder and just run vagrant reload. But if, if you're just continuing, then no need. Just let's go to the document and start with the MySQL setup. We are using MySQL for the database for the, for this V profile project. So let's check the steps. First we need to run DNF update command. That is to update all the softwares and apply the latest patches. This is always recommended when you bring up any computer, new installation, bringing up a VM. You make sure you update the operating system. Then we install EPEL release repository. We talked about this in the Linux lectures and then we install Git and MariaDB server. We start the service and we enable it. You see here the package name is mariadb-server but the service name is MariaDB. So sometimes the package name and service name are same. Sometimes they are different. How do we know? We just check the documentation or we can use Google, Chat GPT to find it out. Okay. Next we run a command, my sql secure installation. This is going to ask us a series of questions like set the password, disable remote login, stuff like that. We'll go through that. Once we do that, we are going to log into the database. We get a MySQL prompt and then we are going to execute some MySQL queries. That will create an account, a database first, then create an account, give it some privileges. Then we are going to deploy an SQL file. We are going to run some queries, SQL queries that is available in our source code. This is provided by the developers to us and it is important to set this schemas for this V profile project. In real time, you will get this kind of DB dump. Usually call them as DB dump or it'll be automatically done by the application service itself. When you bring up the application, then automatically it'll log into the database and initialize it. This is also called as initializing database. After this, your database should be ready. So let's get started. Let's execute all the steps one by one, first starting with DNF update. So I'm going to log into db vagrant, ssh db01. Okay, I'm in the DB VM. I'm going to switch to the root user, sudo-i. Let me clear the screen and I'm going to run the command dnf update- y to update all the packages to its latest versions. Now this will take time, lot of time, so I'm going to pause my recording and continue once this is completed. Okay, so this is completed. Next step is to install EPEL release repository. That is done. Next, installing Git and mariadb-server. So we did all these things, installing packages and all in Linux and Vagrant lectures. So you can also copy from the documents. Simply paste and run it. You, you don't need any extra practice to type the command. These are the say similar kind of packages that we are installing over here. Okay, the package of MariaDB is installed. Let's start the service. Let's enable the service. Let's check its status. Hopefully, it'll be running. Active running, Hit queue to quit from here. Let's clear the screen. And now we are going to run a command. My sql_secure_installation. This command is going to ask us some series of questions. We are going to answer that in order to secure our database. So let's hit Enter. Enter current password for root. Now this is not the root user of Linux. This is the root user of the DB, MySQL DB. So we don't have any password. We are just going to hit Enter. It's just a new database. Okay, it says switch to unix authentication. Just hit Enter. It'll take Y as the default. Change route password. Hit Enter. Yes, you want to change the route password and I'm entering here a simple password admin one, two, three. Just to keep things simple here, but in real time the password of database definitely should be long and complex. But here I'm using admin one, two, three as I have mentioned in the document. One more time. Admin one, two, three. Remove anonymous users. Yes please. So without password, nobody will be able to log in now. Disallow route login remotely? Yes, so it's not, does not allow root user to log in remotely from other machines. Yes, remote test database. Yes. Reload the privilege. Yes. Okay. So DB is secure. Now it's time to deploy our schema into the database. So we're going to log in with root user because that's the only user we have now. So we have to run the command mysql-u root, that's root username -p and the password without any space. Now this is not at all recommended, mentioning password like this in the command line. The best way is just you say -P and hit Enter, it ask the password to you, but just for the sake of showing you the setup, I'm typing the password over here. Okay? Otherwise it should be like this. You hit Enter and it asks you the password. You enter the root password admin one, two, three which we did, and we log in. We have to create a database here. So this is the command create database and the database name will be 'accounts'. This is very specific for a V profile project that needs the database named as 'accounts'. So this is the skill command for this- create database accounts followed or ending with a semicolon. All the SQL queries like this will end with semicolon. Okay, this is the MySQL prompt. This is not the Linux shell. Okay, after doing that, we are going to execute this command. I will explain this once I paste it. Let me just copy it first. Okay, so this command says grant all the privileges on the database named accounts.star. That means all the tables, everything inside that to the user admin. So along with this, command is also going to create this user admin at local host. That means locally. Identified by admin123. This is the password now. This is the password again for admin user. We set the same password for root user. DB root user. And followed by a semicolon at the end, hit Enter. That is done. The next command is similar except with one difference that I will show you. Here it says admin at percent. Percent means remotely because our application, V profile application will be living in app zero one, the Tomcat machine. From there, it'll connect to the database. That is a remote login. With this user and with this password. So we are allowing that access. Hit Enter. That worked. Okay, next is flush privileges. You know this is like a reload. So just run this command and then we exit. Oops. Exit. Okay, next we are going to clone our source code and we are going to mention this branch local. And we go into the source code and we run this SQL query. Okay, let's do this. I'm going to copy this Git clone command. I'm going to go to the temp directory, clear my screen, execute this command, which is going to clone the source code. -: B means branch local. Don't worry about Git for now. We are going to have a separate section on Git. Okay, clone the source code, then do cd vprofile-project. You get into the source code. Okay, in the source code, at this location we have the DB file, src main resources. You do LS and you see here db backup. sql file. So we have to run this command MySQL- u user, which is the root -p and the password admin123, and the database named accounts that we created. Input redirection. And we give the path src main resources and db_backup.sql. You can do a CAD to this file. You will see there are a lot of SQL queries in that this will be executed on this database accounts by using these credentials, of course. Let's hit Enter. Once we do that, let's log into the database. So just up arrow, go to the same command, just remove it till here to input redirection. And this will log in directly to the accounts database. There you see. Database named accounts. And here you can say show tables and you should see this schema over here. Okay, so accounts is the database. Like that, there will be other databases also if you just do show databases. You should see other databases like, you know, MySQL information schema, performance schema. For our V profile project, we need a database with this name. You'll understand that when we set up Tomcat servers, but for now here we are done with the database. And here there are few firewall related commands and it is too early for you to understand what is a firewall. In AWS section, we are going to cover what is firewall, how it works, the port numbers, because you need the knowledge of networking plus all these things to understand how firewall works. But these are the command, you execute them or you don't execute them. You'll get the same result in this project. So we are skipping it for now. Later we are going to talk about it, okay? So complete this setup and then join me in the next lecture where we will set up memcached servers.
