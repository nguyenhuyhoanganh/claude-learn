# Automated - Code

Instructor: Okay, so, in our repository, we have a "vagrant" folder. "Automated provisioning." Choose the one based on your operating system, Windows and MacOS with Intel chip, use this folder. And MacOS M1 and M2 chip, use the other one. Now, we had already cloned this source code in VSCode. So, let's look at it through VSCode. Automated provisioning, and let's check the vagrant file. Now, this is the same vagrant file which we used previously in manual provisioning, except you have one extra step in this. For every VM, there is a part of a script that we'll be executing. For DB01, the path is mysql.sh, and in the same folder, you have mysql.sh. It's a Shell script. Don't worry, it's a pretty simple Shell script. So, same command copied from the document and pasted in that file with some tweaks. If you're doing this in decoding devops codes, we have bash scripting sections separately, where I'm going to train you in bash scripting. But even if you don't have much idea about bash scripting, you can understand this file is very simple. Let's see. Mysql.sh. In the beginning of Shell script, you may have this hash exclamation, slash bin, slash bash. This is to open the bash Shell interpreter. It's the same black screen where we execute the command. So it's going to open a new Shell and execute all the command. First command here is a variable, and its value will be this. This is the database password that we are setting. Again, pretty simple for convenience purpose only. And similar command, install "mariadb," start and enable mariadb. We clone the source code and deploy the schema. Manually, we will log into the sql command prompt and then run the sql code. If you want to run it from the Shell, then you can give the log in command. Oops, yeah. "mysql -u username -p password -e" to execute this query. So, you can do the sql queries from the Shell, but then you need to pass in the log in detail. So, similar commands to run the sql queries. At the end, you will see some firewall commands. You don't need to worry about that. We'll talk about firewalling AWS security groups. So, like that, every VM has its own Shell script. That's for memcache, memcache.sh. Take a look. It's pretty simple, same commands. And then we have a rabbit mq. Once again, pretty simple, same commands. Then we have tomcat.sh. This also tubuntu, not tubuntu, sorry. Ubuntu. Tomcat Ubuntu. That's a tongue twister, man. Tomcat Ubuntu script, that is for the Ubuntu. It's simpler than this one. So, here we have to set up tomcat systems manually. So, again, similar commands except with few tweaks, like variables and creating this file through cat Command. Because in the Shell script, we cannot use VIM editor and copy paste, and so with cat Command, we create this file and all this content will be injected into this file. This EOT you see, EOT or EOF, something like that, you will see and between that all the content will be there. All the content in between that will be injected into that file. So in the Shell script, if you need to create a file with a lot of content, this is the way. So, we created the system file here and then we start and enable tomcat, we clone the source code, run mvn install, deploy the artifact, same steps what we did manually. And, finally, in nginx.sh, that is also pretty simple. Install nginx, create that configuration file, and disable the default website, enable our website, start and enable nginx. So, without knowing bash Shell script, you will still be able to understand these scripts, these files, but in Decoding DevOps course, we have bash scripting section where you're going to learn much more advance in bash scripting than this. This is pretty basic. So, in the next lecture, let's bring up all this VM automatically and set up all the services automatically, and test our application. See you in the next lecture.
