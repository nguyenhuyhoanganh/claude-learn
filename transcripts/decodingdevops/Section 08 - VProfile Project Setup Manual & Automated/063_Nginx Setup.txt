# Nginx Setup

-: All right, the last service in the stack nginx. Now this is pretty simple. This just acts like a load balancer. It takes the request and just forwards it to the Tomcat servers, and this will be, web01 will be open to machine. So all open to related command. So apt Update and apt Upgrade first. Update and Upgrade Command are different in Open2. Update means it is going to check for if there is any update, right? And Upgrade Command is going to apply those changes. You know, upgrade, actually do the update, right? Apt update is just to check. It's not going to make any change. Okay? And then we install nginx Service apt install nginx and we create a configuration file, okay? nginx configurations or nginx host can be created by creating a file into this location etc/nginx/sites-available in this location you have to create configuration for your host. So we just create a file with name vproapp, not mandatory, you can give it with any other name, but this content matter over here. Okay? There's two parts over here. This section is server, which says here, Listen on Port 80, location/ route. That means you know, just the host name. There is no, you know, slash any other extra URL after that. We'll learn about this in AWS also. So this simply means that if the request comes on port 80, if somebody's, you know, putting the IP address or the host name of this machine and access it on port 80, that is from the browser, right? HTTP, blah, blah, blah 80. Then it says that forward the request at this location. So proxy pass. So what is this location? http:vproapp. So here's the section we created for vproapp. upstream vproapp. So that will be the server app01:8080. That means it's going to forward the request to app zero one on port 8080. So this is the configuration. We're going to talk about the load balancer stuff in detail in AWS for now. Just understand this much that it takes the request on port 80 and forwards the request to app01 Port 8080. nginx has a default configuration file with this command is going to remove that default configuration and we are going to create a link, ln-s command. We are going to create a link of our configuration file at this location. Now, when you create this link and restart nginx service, your site will be enabled. So nginx is going to read everything from this sites-enabled folder and activate it. Basically activate this configuration and that is it. Let's go ahead and do the setup. Let's see. Vagrant ssh web01. Let's switch to root user, clear the screen apt update. I'm going to run both the command together. apt update &amp;&amp; apt upgrade. So once, right. Upgrade. So once the apt update completes it is going to then execute the next command, which is apt upgrade. I'm going to say -y. So it does not ask me any question. Okay, this is going to take some time. Alright? You might get this screen, just hit a space bar just to say, okay, and then we might get this again. So just use tab to go to okay and hit space bar. Okay, the upgrade is completed. It's time to install nginx now. apt install nginx -y. Okay, nginx is installed. Now let me show you one thing. ls/etc/nginx. Okay? In this right, you have sites available and sites enabled. In this, you're going to create the configuration file and in this location you're going to create a link of that configuration file. Okay? So, okay, so in sites-available, we create a file called vproapp insert mode, and then we just copy paste this content. Alright? I will make it look little better. I'll give some space here. Yeah, this looks much better. Let's save and quit. Then nginx has a default page. We're just going to remove that page. Okay? That's the default website of nginx. And then we are going to create a link. Alright? This is the file that we created. We're going to create its link in sites-enabled. Hit Enter. All right, and then we are going to restart nginx. If everything is good, nginx should restart. If there is any problem, you should get some error. Or when you check the status, you should see the error. Mostly the error will be the wrong configuration. Copy paste mistake. So if you get any error, make sure you open this file and fix those, you know, verify with the document if everything is correct, and fix those things. And then restart nginx servers should work. And with that one last command, the entire setup is completed. You see how quickly the time flew. Ah, maybe not so quickly for you, but we'll validate everything in the next lecture. We'll see our V profile application looks. If you have come till this far, you've done a really good job. You don't need to remember all these commands and all the steps. You're going to set up this project many times in the course and in real time also, you don't need to remember the commands, we have the documentation, we have Google, Chat GPT. You know, if you start remembering everything you know, you will overload yourself very quickly. So please don't try to memorize anything, just go with the flow. Just do the setup. Again, we are going to do many times in the course, so it'll be very easy later for you. All right? I know now you will be relaxed after hearing all this. All right, then join me in the next lecture. Let's validate this project.
