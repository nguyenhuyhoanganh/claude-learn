# Memcache Setup

Instructor: All right, in this lecture we are going to set up Memcache. This is pretty simple. We just need to install Memcache, start the service, make some configuration change, and restart Memcache service. That's all we need to do. So let's log into Memcache. So I'm going to exit from db01 because I'm connected in that. So I'll say vagrant, vagrant ssh mc01, that's the name of our VM. All right, I do sudo -i to become the root user. Clear the screen. Let's check all the commands and execute them one by one. So, once again, first, dnf update -y. Now this takes a lot of time. If you really want to skip this command, you can, this won't make any difference in this project setup. If you want skip it, skip it, but I'm going to execute this one. So I just copy this command and just run it. Again, I'm going to pause my recording and resume once this is completed. The update completed successfully. Let's go to the next step. Install epel-release repository and installing Memcache, dnf install memcached, that's the name of the package. Okay, memcached is installed. Let's start the memcached service. So systemctl start memcached. And we'll enable the service. Enable. Okay, now the next command, that's a sed command. We know this, search and replace. So it is searching for this 127.0.0.1 and replacing it with 0.0.0 and it is doing it in this file. So what does this mean and what does this do? Well, this is very important for you to understand. Whenever you set up a stack, now this is a stack, combination of multiple services working together as a single unit. It's a stack. Your multiple services, these services are designed to connect with each other. Like in our case, we'll have the Tomcat, our application running on that, and that will try to connect to Memcache service for the caching requirement, database caching. Now that's a remote connection, right? Memcache is on a different machine and Tomcat is on a different machine. Now many of the services are designed to, by default, not listen to remote connection. They don't listen to remote connection, only local connection. So every service will have its configuration file where it's picking up things like this file over here is for Memcache to pick up some settings when Memcache service starts. So let's take a look at this file. Hmm, just some variables. Some configuration files will be huge. Here you see OPTIONS -l 127.0.0.1. This is the local IP. Every machine refer to itself with this IP address. And when the Memcache starts by default it is designed to listen to only local IP, not the remote IP. So we need to change this. Either here we mention the IP address of the Tomcat machine, or we say 0.0.0.0. That means all in networking. Like in Linux we have star, that means everything, in networking 0.0.0. Did I say four zeros? Yeah, yeah. So IPv4. All the IPv4. It can listen to all the IPv4. That's what it means. And that's really difficult to remember. Really, I see all the zeros or not, but you know, let's just run this command and make the changes. Copy this and paste it. And let's look at the file again. There. Or you can just manually, you know, just vi this file, open this file and make the changes. You can do that also. It's a very good lesson for you. If a service is not able to connect to a remote service, even though the service is running, everything is fine, network is fine, everything is fine, make sure you check whether it is designed to run remote connection or not. Most of the services are not designed like this. They will be mentioning local IPs for security reasons. Anyways, we're done here. So let's restart Memcache service because we made a configuration change. Whenever we make configuration change, what is the rule? That's right, we restart the service. So restart memcached. Again, the firewall rules, you can execute them or you can skip them. As I said, we're going to talk about them later, but you have to execute this command. It's not a firewall command, it's a memcached command, which says, you know, listen on port 11211 and UDP port 11111 again. Did I say five times 1? Anyways, you know that, right? So -u memcached and -d means DAMON, run it in the background. And that's how we set up Memcache service for vprofile-project. In the next lecture, we will set up RabbitMQ service. So finish this and make sure you log out from here and then join me in the next lecture.
