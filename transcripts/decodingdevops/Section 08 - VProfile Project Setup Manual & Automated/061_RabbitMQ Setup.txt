# RabbitMQ Setup

Instructor: Okay, in this lecture we are going to set up RabbitMQ server queuing service. So once again, "dnf update hyphen y", "install epel release", "install wget" and then "install rabbitmq", sorry, this is the repository of four RabbitMQ. It's going to... with this package, it's going to set the repository, so from where we can install RabbitMQ and then "dnf--enablerepo" enables this repository which is installed and "-y install rabbitmq server", right? Don't get confused, it's just one single command wrapped up, no, two commands wrapped up into one. One thing it is doing is it is enabling this repository and then it is installing RabbitMQ server, all in one command, like that. And then "systemctl enable --now rabbitmq server". So you can do "systemctl start" and the service name and then you can say "systemctl enable" or together you can do like this, combination. And just thought, you know, you should know different ways. So just included that. Otherwise, "systemctl" or "enable", that also, start and enable, both commands, also, you can do. Okay? So once you have RabbitMQ up and running, then we are going to make some configuration change, add some user, execute some permission, and then RabbitMQ servers will be up and running. So let's do this one by one and let's see how it goes. Okay, so, "vagrant ssh rmq01". Make sure you are in the right folder. Okay, so switch to root user, "dnf update -y". Once again, this will take time. I'm going to pause my recording and resume once this is completed. Okay, the update is completed. Next step is installing EPL release repository and installing wget and wget would be already installed. Yes, it is already installed. Okay. The next command is installing the centos RabbitMQ repository. So once we install this package, it's going to set the RabbitMQ repository from where we can download all the RabbitMQ packages. So with this command it is going to enable this repository and install RabbitMQ from that. And you see, it's checking that repository. So RabbitMQ package plus 19 other repository dependencies. Okay, RabbitMQ is installed. Let's do the configuration changes. Let's copy this command and I'll explain you while I run it. Let me clear the screen. Okay, once again, this is a configuration change, just like we did it in (indistinct). So this is the file, "etc/rabbitmq.config". So we ran this command, "echo", we printed this message and redirected that into this file. Okay, after this we are going to "restart rabbitmq-server", "rabbitmq-server", that's the server's name. Okay, then we are going to execute a RabbitMQ command that is going to add a user called "test" with the password "test". This is a RabbitMQ user. Just like in mySQL. We did add the admin user. Like that, here, we have this test user in RabbitMQ. Then we say, "set_user_tags test administrator". We give this test user administrator tag and then we are going to run this command to set permission to this test user. Once again, let's restart the RabbitMQ service, "systemctl restart rabbitmq-server". One last time, let's check its status, "systemctl status rabbitmq-server" and it's active running, Q to quit. And that's all we need to do in RabbitMQ for this project. So complete this setup and join me in the next lecture, where we will set up Tomcat along with our V profile application. Exit from here and join me in the next one.
