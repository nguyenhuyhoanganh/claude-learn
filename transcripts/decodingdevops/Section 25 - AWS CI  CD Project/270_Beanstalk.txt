# Beanstalk

Instructor: Hello, and welcome. In this lecture, we are going to set up Beanstalk environment where we can run our Vprofile application. But before we launch the Beanstalk environment, we are going to create key pair, which we will use to log into our Beanstalk instance. We have a use case for that, and we'll do that in some time, but before that, we'll create a key pair. So click on Create key pair. Give key pair a name. So I will say here vprobeankey, then format, and Create key pair. Okay, now let's create Beanstalk application. So find Beanstalk service, and click on Create application. Web server environment, we'll select. We'll give application a name. I'm naming it as a Vprofile. Let's come down. Environment name, we'll say Vprofile-prod, and I'll add some numbers just to make it unique, so I'll say just 11. And for the domain, we will give the same name, and we need to check the availability because this will be the URL, and we have to make sure it's unique. So you need to add a different number or just make the name unique. You cannot use the same name that I have given. And let's choose the platform, so platform is Tomcat. Our Vprofile application runs on Tomcat, and we are going with Tomcat 10 with Coretto 21. That means JDK 21, and the instance will be Amazon Linux 2023. Okay, let's come down, and let's select configuration as custom. We'll make changes. We'll have load balanced instance, multiple instances with the load balancer. Let's go Next. Service role, we are going to say Use an existing service role, the service role we created in our re-architecting project where we created Beanstalk. Before that, we created this role. So if you don't see this, make sure you watch that lecture and create this role, and then say Use an existing role, select this role. Key pair, we already created, so we'll select that key pair. Instance profile, also we'll say vprofile-bean-role. All this we covered in a re-architecting project, so I'm just selecting the existing one, and go Next. Okay, from the dropdown, select your VPC, and instance settings, I'll say public address Activated. So instance will have public IP address , and in this VPC, we have these many subnets. We are going to select all the subnets. So when the instance gets launched, it can go to any of the zones. So we will have minimum two instances. So it's going to divide these two instances in two Availability Zones. Let's come down, all the way down. Add new tag. We will give tag as Project vprofile. You cannot use a name tag because that tag is already taken. When we create application, it anyways uses that tag. So you cannot have name tag. We are creating just Project tag. Go Next. We will change the root volume from Container Default to General Purpose 3, GP3. There's some recent issues with Beanstalk. It is trying to use Launch Configuration, which is outdated, and you'll get an error. That Beanstalk should be using Launch Template. So if you use GP3, it will use Launch Template, but if you select here Container Default, it'll use Launch Configuration, which is outdated. So make sure you select GP3 over here. Scroll down. Security group, so it will create its own security groups, and we'll keep it as it is. In Auto scaling group, we have to change from Single instance to Load balanced and we will see minimum 2 instances and maximum 4. You can keep maximum as 2 as well. We are anyways not going to scale out. We'll just deploy the application on multiple instances. So make sure it's just two instances. So let's come down. Instance type, remove this t2.small, We'll go with t2.micro because that comes under Free tier. So search for t2.micro. Okay, then come down. Scaling triggers for Auto scaling group, we'll keep them Default. Load balancer visibility, public. Load Balancer Type, Application load balancer and Dedicated. Listeners, we'll keep Default. Processes, we'll make one simple change over here. Select that and Action and go to Edit. And in the Sessions, you make sure Session stickiness is Enabled. We will have two instances. So what stickiness will do is when we access our application through the load balancer, and let's say we go to instance number 1, so stickiness will hold that session for us. If we don't have stickiness, then it is going to bounce between these instances we want for our application. The Vprofile application is very simple. It doesn't give any token. So for authentication, we are to make sure we stick to one instance. Otherwise, if it jumps to another instance, we'll lose the authentication. So this is the requirement just for the Vprofile project, which is very simple. So let's save this, and then let's come down and go to Next. Health reporting, we'll keep it Default, and everything else, Default. Rolling updates, we will do Rolling. So one instance gets upgraded at a time. For that, we are going to give the size as 50%. So by Default, we will have two instances. So 50% means one instance at a time. You can give a fixed number also. In real-time production cases, you will have multiple instances, and percentage of deployment should be not more than 25%. Mostly in production. it's just 10%. Let's say you have 10 instances, so 10% means one instance at a time, but we just have two instances, and so we'll keep it as 50% one instance at a time and then everything else Default. Let's come down and go to Next. Okay, we selected everything. Review it once again. Make sure you have selected the same option that I have selected. Otherwise, you can edit it. And once you are done with all your reviews, then you can click on Submit. So it is going to take some time to launch this application. By the time, we will create RDS. So I'll see you in the next lecture.
