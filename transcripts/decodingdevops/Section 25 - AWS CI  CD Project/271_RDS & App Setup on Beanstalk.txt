# RDS & App Setup on Beanstalk

Instructor: Alright, so we did set up our Beans Stack application environment and now we need a database to store our vprofile application tables. So we are going to create Amazon RDS for that. So search for RDS service and open it and then click on create database. Okay, keep standard create and select MySQL. Come down. MySQL engine version 8.035. And then in templates, just make sure you select free tier so, accidentally, you don't select any paid service, and come down. Let's give DB instance a name. So I'm going to call it as vprords. Username, we will keep it as admin, and for password, we will say auto generate password. Instance configuration, make sure you select the instance type as T3 Micro or T2 Micro. If you selected free tier by default, it is going to select T3 Micro or T2 Micro, which comes under free tier. Scroll down storage. Also, it'll select minimum which is 20 GB, general purpose storage. Connectivity, keep it as default. We will do our own connections. Public access, no. Security group, you have to make sure you click on create new security group. We will give it a name, vprords-sg. Availability zone will say no preference. Any additional configurations. We have port 3306. That's default. Let's keep it as it is. Database authentication will keep password authentication. In additional configuration, you have to give the name of our DB which is accounts. So we vprofile application needs the DB name as accounts, so we need to create that. And then we are going to run some SQL queries which will create tables in this accounts database. Alright, let's come down. Keep everything default. Don't worry about this cost. We are going to create everything in free tier. So click on create database. Okay, close this popup if it appears. And here you will see view credential details. Make sure you do that and you copy this password and store it somewhere because we are going to put this information in our bill file. Okay, so I'm using sticky notes temporarily to store the password and also I will store the username and then we can close it. The problem is if you do not save this now and if you close this then there is no way of retrieving the credentials back. You have to reset the credentials. So make sure you get that, and if you accidentally close it, then wait for this instance to become ready and then you can go in the settings and you can reset the master credentials. Okay, so now you need to wait for five to 10 minutes for this RDS instance to become ready. And once it is ready, we are going to go to our next step, which is DB initialization. So I'm going to pause the recording for now and resume once this instance is ready. Okay, after some time, my RDS instance is in available status and my Bean Stack instance is also available. So Bean Stack environment would have created two EC2 instances. If you go to EC2, EC2 instances, make sure you're in the same region. You will see two instances over here. And when we deploy our application, it is going to run on these instances and these instance needs a connection to the RDS instance. Our Vprofile application needs the database connection. We are going to give information about RDS instance that we created. So our application is going to try to access the RDS database instance, and for that, first we need to make sure RDS instance security group. Let's go to the RDS instance. And you should see here security group. This security group should allow connection from these instances. So we will go to security groups and you should see here the RDS security group and you will see here two Bean Stack security group. One is for the load balancer and this one, this is for the instance. So make sure you select this security group, the RDS instance security group and copy its security group id. Then we will go to the RDS security group. Click on that. And in the inbound rules, we'll say edit, and we'll say add rule. So we are editing RDS security group, and we will say 3306 incoming connection is allowed from this security group right where our instance is running. So our Bean Stack instance can connect to RDS instance on port 3306. Save rule. Okay, let's go back to your instance. Now select any of the instance. Now we will do two things. One, we will test this connection that we are able to access the RDS instance from this instance from the EC2 instance. That will check one. Second thing, we have to initialize our database, the accounts database living in RDS instance. We need to add tables in that, the schemas. So for this purpose we will SSH into any one of this instance. So let's copy the public IP of any of the instance and open git bash for Windows user terminal for MacOS. And we created the key initially key pair. So mine is in downloads. Okay, downloads/vprobeankey, that's where I have my login key. So I'm going to see ssh -i, downloads vprobeankey. Oh wait, before that, we have to make sure we do this, chmod 400 downloads/vprobeankey. We give this permission to the key and then we can use it. Otherwise we might get an error saying permission denied. And the username is EC2-user because it's Amazon Linux instance. And let's paste the, oops, not this one, the IP address. Let me copy the IP address of the instance once again. Let's paste it here. I say yes. Okay, so we are in the Beans Stack instance. Let's do sudo -i. Let's clear the screen. This is Amazon Linux. So to test the connection to our RDS instance in our accounts database, we need MySQL client. So let's search for DNF, search mysql. Let's search if we have any MySQL client. So that's the name of the MySQL client, mariadb105. This is in Amazon Lenux instance. So we'll say DNF install and the name. Now, let's say -y. Alright, we have that. Let's clear the screen. Now we have to give this command mysql -h, the host name. So here we have RDS instance, so let's get the end point of RDS instance. So go to your RDS instance and here's the endpoint. Copy that and let's paste it here. Space username. We have a user admin in our database. -: P, password. So I stored my password in the sticky note, so I'm going to copy it from there and I'm going to paste it here. Now, yes, I am showing you the password of this RDS instance, which is very bad, but I'm going to delete this instance as soon as this lecture is completed. And also giving password like that in the command line is a very bad idea. But this is just to show you in real time, you should not give the password, just say -p. And you can give the database name, accounts, and hit enter. So if you're not given the password in the command line, it'll ask you the password. Then you can paste the password, which will not be visible. So as you see we are connected to the RDS instance in the accounts database. You can say here, show databases. All right, so we have the accounts database where anyways we are logged into that and few other database. Now let's exit from here. So that was our one task. We verified the connection. If you're getting timeout, something like that, some error, you have to check first the security group rule, which we did to make sure we add the rule over there. And also check the username and password if it's correct, and also the DB name in this account. Just look for the error, what exactly error it is giving you, invalid credentials or timeout. Based on that, you can do the troubleshooting. Now we need to run the SQL query on this RDS accounts database. So we have that in our source code. So open the source code, vprofile project hkhcoder vprofile project. Right, that's the URL. Open that and switch to the branch, AWS CI. Go to SRC, main, resources, and the db_backup.sql file, this file. So I'm going to directly download this file in the instance. And for that I'm going to click here on raw. When I click on raw, it's going to gimme a URL of the file. I'm going to copy this URL, go to my instance, and I will say wget and paste this URL. So what it'll do is it is going to download this file. Okay, for some reason, if you're not able to do this, you can just simply copy the content of this file and paste it in a file in the instance. You can do that also, or you can download this file as well. In any case, we need this SQL file over here. Now let me clear the screen, ls. Now let's do up arrow and go back to a login command there. And same over here. We have to give input redirection and the file name. What will happen is it is going to log into the accounts database and execute these SQL queries. Now let's log in again to the RDS instance and verify. So cancel this, just keep the command log in command with the database named accounts. Hit enter and say show tables and you should be able to see these tables. If you don't see these tables created, make sure you check the file content. You have to download the same file as it is and execute the same command. You will see the tables then. Okay, let's exit from here. So now our database is ready with the tables in it. So now we can move on to the next lecture from where we are going to create CICD pipeline where our source code will be automatically fetched, built, and deployed to the Bean Stack instance. Our application will be running on Bean Stack instance, and application will be accessing this RDS database. So wrap this up and join me in the next lecture.
