# Build, Deploy & Code Pipeline

Instructor: Welcome to the last lecture of this section. In this lecture, we are going to create a CodePipeline that is going to build the artifact from our source code present in Bitbucket and then deploy that artifact to Beanstalk environment. But first, we have the build project. We need to test this project. So open your build project and click on Start Build. As soon as you do that, click on Phase Details because if you stick to build logs, it might hang your browser. So we will first test whether this build completes or not successfully. This one is going to fail because I have made one mistake and I want to show you how you can check and fix things. Once we fix this, we will create the CodePipeline. So I'll pause the recording now and resume when this is completed or when this fails. Okay, so as you see, this failed in the pre-build stage and you see some message over here which might tell you a bit about why it failed, but the details will be in the build logs, so click on Build Logs and we'll click on this Tail Logs so it goes till the end. So whenever the build fails, look for the error in the logs and then check where it starts from. So in our case, you see till here everything is fine, says running command, and then here it failed, sed hyphen e expression, unterminated s command. So there is some problem in the sed command. So let's take a look at that. Now from here itself, I can show you sed works like this. We have seen the Linux section, right? It searches for this and then replaces with this, right? And the search and replace terminates when it sees the backward slash, sorry forward slash. So between these two forward slash, this is the search criteria. And in between these forward slashes, which at the end there is no forward slash, there should be the string that will replace it. So let's close this. So this information we have given in build spec file, right? If you remember, so let's go to build spec file. Let's go back to build job. Click on it, go to Edit. So you can make changes like the source code and stuff. Many things you can change. And that's our build spec file. We are going to put a forward slash there. And once we fix this, we are going to say Update Project. Now let's run it once again. Once again, click on Phase Details and wait for some time. Okay, so the build completed successfully. Now let's create the CodePipeline, right? So we have the source code which is in Bitbucket. We have a Beanstalk environment where we want to deploy the artifact. We have the RDS. So our view profile application will connect to RDS by using the application properties file information. And that file is in the source code, which will be fetched by our CodeBuild project. It's going to build the artifact. And all this we need to now combine with CodePipeline. So scroll down here and you can see Pipeline. Pipelines. So these are all developer tools over here. You can either go to Pipeline by clicking over here or you can search for CodePipeline here. You need to click on this Getting Started in the CodePipeline. Okay, so AWS CodePipeline, visualize and automate different stages of your software release process. Same like what we do in Jenkins. We create pipeline in Jenkins. Similar way in AWS, we use AWS CodePipeline. It's a continuous integration and continuous delivery service for fast and reliable application and infrastructure updates. So whenever there is a code change, CodePipeline will detect the code change, it is going to fetch the code, build it through CodeBuild, and deploy it in the target. In our case, the target will be Beanstalk. So let's click on Create Pipeline. Okay, so choose Creation Options. So you have deployment, continuous integration, automation, build custom pipeline. So we're going to do build custom. We check in deployment, you have push to ECR. So if you have Docker build process, you can host it on ECR. We have ECS where we can run our containers. We have CloudFormation, Terraform. And in continuous integration, you will see build tools. But we're interested in this one, build custom pipeline, so click on that, go Next. Let's give pipeline a name. So I'm giving it as vprocicdpipeline. So this is going to create a role, a service role that should have access to all these services like CodeBuild and Beanstalk because we're going to do deploy it on Beanstalk, but there's one problem. Recently, this role does not create any permission for Beanstalk or deployment will fail. So after we're done creating this pipeline, we're going to find this role in IAM and we'll add permission for Beanstalk. So for now, let's just keep it as it is. Let's go Next. Source provider. Now, you need to select the source code over here. So our source score is in Bitbucket and you should see the repository over here, the connection name first of all, and then you should see the repository because we already connected it while we're doing the CodeBuild project. Branch, aws-ci. Now this source code, we need to provide this information so it detects the commit, and so that will be an event, right? It detects a commit as an event and that event will trigger this pipeline. Okay, let's go Next. So what is the build option that you're executing over here? Do you want to execute some commands or you want other build providers? Click on other build providers and we're using CodeBuild, so we are going to select CodeBuild, but you see here you have option for ECR and Jenkins also. We are going to select CodeBuild and see our project name over there. And then you have some options like it's a single build or you want to like build multiples, batch build. You have the region name. Let's go Next. Test provider, you can select where you want to test this code. You can have another CodeBuild project that can test your code or do code analysis or any other thing. You have other options also over here provided by AWS. So we do not have anything over here. We're just going to go Next, skip to the next stage. Deploy provider, so this is where we select the Beanstalk. So search for Beanstalk, AWS Elastic Beanstalk. Select your application and select your environment name from here and then go Next. Okay, let's review everything, if you're good with all this. Now before I complete the review, I'm going to get the service role name. Okay, I just copy the service role name because we need to edit this and add Beanstalk permission over here. So copy the service role name and then say Create Pipeline. As soon as you create a pipeline, it will start the trigger, it will start the pipeline. Okay, there it started the pipeline. You know what, I'm going to stop the execution and I will say stop execution, which is in progress, this one and just say stop. And the reason I stopped it because it will fail in the deploy stage, that's why. Even if it fails, you can just retry after adding permission to the role. So go to IAM service here, IAM, go to Roles. In the search, paste the pipeline name, which you copied, sorry, the role name. Once you find your role name, or let's say even if you missed it, you can go back to your pipeline, you can click on Pipelines, go to your pipeline, can go to Settings, and here also you can find the role name, right? This is the role name. Okay, so find the role name, search it, click on it. So you see it has some policies. This is for CodeBuild, this is for CodeConnections, this is for CodePipeline, nothing for Beanstalk, so we are going to click on this dropdown and say Attach Policies. Search for Beanstalk. AdministratorAccess-AWSElasticBeanstalk. Put a check mark on that. Scroll down, say Add Permission. So that's done. We can trigger the pipeline now. So go back to your pipeline and click on Release Change, Release. Some error I got on things related to the pipeline. So open the CodePipeline from somewhere else if you face this error. Let's check the pipeline. It says it's in progress. After some time, the build completed and the deploy stage has started. Let's check the Beanstalk environment. You see the Batch 1: Starting application deployment command. So we have rolling update, we have selected while creating Beanstalk, and we have two instances and we selected 50%. So that's one out of two instance. So one instance deployment is going on. Once it is good, it's going to move to the next instance. So I'm going to pause the recording and resume when the deployment is completed. Okay, so the deployment is completed and is successful. Let's go to the CodePipeline. Now, here also it says the deployment is completed. Let's go to the environment and let's find this URL. Yeah, this one. Let's click on this. That should redirect to the load balancer of the Beanstalk and that should show us the application. So enter the username and password, same, admin_vp, and same password, admin_vp. Login. Now, this login should prove the connection to the RDS instance, and we do not have anything else. We don't have RabbitMQ and Memcached or Elasticsearch, anything. We just wanted to prove the CodePipeline deployment, which we have proved by doing this project. In DevOps projects course, we have this much in detail, we have many more thing that is more close to real-time environment like testings, repositories. But I wanted you to have a taste of AWS CodePipeline. And that is why this section. Now our CodePipeline, as we see when we make a code change, it is going to trigger the pipeline. So let's test that last thing and then we're going to do the cleanup. So either from the command line you can open your terminal, you can go to that folder where we cloned the source code from Bitbucket. You can make any commit over there or you can just do it directly from the Bitbucket UI also. So let's go to the branch aws-ci and let's open README.md file. Let's click on Edit. Okay, don't make any meaningful change. You know, just add or remove some hash over here in the second line and make a commit. I can give some commit message. Let's click on Commit. Now, this come commit should trigger the pipeline. So let's go to CodePipeline. Let's go back. You see that was fast, right? As soon as I made a commit, triggered the pipeline. Okay, and I already went to the build stage and then it's going to go to the deployment stage. All right, so you can wait until this deployment is completed. And once it is done, you should do the cleanup, starting with the database. So go to the RDS, find your database and delete it. Action, Delete. Uncheck create final snapshot. I acknowledge. To confirm, type delete me here and click on Delete and then you can delete the environment. But before you delete the environment, we need to update the security group rules of RDS. So go to EC2, go to Security Group. Okay, the security group of RDS is anyways gone because we deleted the RDS instance. Oh wait, it's here. Okay, so edit the security group rule of the RDS and remove this line because this is connected to the Beanstalk, right? So when you try to delete the Beanstalk environment, it's going to fail because it will say that the security group is referred somewhere else and it will stop the deletion process. So make sure the RDS security group rule, you delete. Save and you go back to Beanstalk environment and delete it. So Elastic Beanstalk, go to Application, select your application name. In the actions, delete application. And the application will delete the environment first and then it will delete the application. So once it is deleted, or even if it's not deleted, you can still delete the pipeline. So go to your CodePipeline, select it and say Delete Pipeline. Delete. Then you can go to your CodeBuild build project and you can delete it. It's not necessary because as long as you don't run the build, it's not going to have any charge, any kind of bill. But just for the sake of cleanup, you can delete it because we have the build spec file, we can anytime quickly create the build project. Okay, so we cleaned everything except the Bitbucket repository. That we'll keep it for now because whenever you need, you can create the build project and pipeline anytime you want. I hope you have learned something new and you gained some more confidence on CI/CD. Once you know how to do CI/CD from Jenkins, you can do CI/CD from any other tool, any other cloud. And that was the whole purpose of adding this section in this course. So that was all in this project. I will see you in the next one.
