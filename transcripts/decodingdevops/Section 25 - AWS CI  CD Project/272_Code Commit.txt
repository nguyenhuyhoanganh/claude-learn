# Code Commit

Instructor: Hello and welcome. In this lecture, we are going to create a Bitbucket repository. So here are the steps that we are going to execute. We are going to log into bitbucket.org. We are going to create an account and then create an empty repository over there. This is, again, it's a Git repository, same like GitHub. Then we will use key-based authentication. So we will generate SSH keys in our local computer, which should give us public key and private key. We will copy the public key to our Bitbucket account. We will create an SSH configuration file, which will hold the information that if we are trying to push it on bitbucket.org, then it is going to use the keys that we have defined. So once we have authentication, then we are going to take our GitHub repository source code. So basically we will clone vprofile application from GitHub repository, we will remove the GitHub URL from that, and add the Bitbucket repository URL that we have created in the first step. Then we are going to push this code to Bitbucket repository. So in short, create a Bitbucket repository, use SSH keys for the authentication, take repository from GitHub, and migrate it to Bitbucket repository. Let's get started, then. Okay, so first we will create account on Bitbucket and I'm going to use my Google account. So you can also use a Google account. Sign into your Google account, and in the same browser, in another tab, open bitbucket.org. Click on Log In, or you can also click on Get It Free. And I'm using Google, so once you select your Google account, it is going to give you this window to create a Bitbucket account, and we'll just say create account. Okay, let's close this popup here. Alright, so first thing we have to do is we have to create a workspace. So workspace is like kind of a placeholder, which will store our Git repository, and I'm going to give my workspace a name. I just said devopscicd here to make sure this is unique. You can give something else. I'll say agree and create workspace. Okay, see that workspace ID is already taken, so let me give a different name. Okay, so I have given AWSCICD45 just to make it unique. You have to give a different name than this. You cannot give the same name. So I'll say agree and create workspace. Okay, so the workspace is created. Let me close this. Next, next. Got it. Okay, so we created the account, we created a workspace for us, and in this, we are going to create a repository. So just click on here, create repository. So first we need to give project name, so I'm just going to say here, vprofile, and repository name, I will just give as vproapp, and I'll make sure it is a private repository. And anyways, we are going to use authentication from our local system to Bitbucket repository and also from AWS services that we will see in further lectures. Now I want to create an empty repository because I'm going to push my code from GitHub repository over here, so I need this repository to be completely empty. So include a read me file, no, and include a Git file, no, and I will just say create repository. So this should create a branch in the repository. There, it created a repository, and there is a branch called Main, and you will see here the command to add this URL. So in an empty repository or in any Git repository, you can run this command. It will add this as the URL for the remote Git repository. We'll see that in a moment now. Okay, next step is we are going to create SSH keys. So for Windows user, you can open Git bash. For MacOS, you can open your terminal. Okay, let's do ls.ssh. So there are no keys. We will get into this folder, cd .ssh, and we will run here a command ssh-keygen. Hit enter. So it is asking the name or path to the repository, to the keys. So if we just give the name, it is going to create it in this folder, .ssh, and that's where we want, and I'm going to give name as vprobit_rsa. And then just hit enter, enter, enter, and let's do ls now. Now we should see here two keys, a private key and a public key. Now let's do cat to this public key, vprobit_rsa.pub. Make sure the public key. So that's the public key. We will copy this public key, and we'll save it or store it on our Bitbucket account. So let's go back to Bitbucket. Okay, so you see the symbol over here, this gear cog symbol. Click on that and go to Personal Bitbucket Settings. Here you should see SSH keys. Click on that and say add key. Let's give a label, as in the title to your key, and then just paste it over here and say add key. And let's go back, and we have to create a config file. So when we do git push or git pull in Bitbucket repositories, then it should use these keys for login, this private key to login to our Bitbucket account. We will mention that in the config file. So in the same folder, .ssh in your home directory, you have to create a file called config. INSERT mode. Insert this content in this file. I will put this in the resource section. You can download it from the resource and copy paste it, or you can just type it, so it's pretty simple. It says if the host is bitbucket.org, then the preferred authentication method is by using the public key, and this is the login file, authentication file. You can say this is the private key. It's going to use the private key to connect to bitbucket.org where we have stored the public key. Okay, let's save this information. Okay, now come to the repositories. You can just click here on the repositories, select your repositories, and here you should see the command to clone the repository. Make sure SSH is selected over here, SSH, and that's the command to clone our repository by using SSH. So let's copy this command, and before running this command, before cloning the repository, I have to run one more command. So open the terminal and execute this command, ssh -T git@bitbucket.org. So when you run this command, it should ask you a prompt. Just say yes. So it is going to store the fingerprint of Bitbucket in the known host file. That's the key fingerprint. Now, when I was running the git clone command for our repository, it was not asking this prompt. It was just stuck at one point. So then I figured out the problem is that it was not invoking this prompt for some reason, and so I execute this command. This is also a test to check whether our keys are authenticating or not, and you can see you can use Git to connect to Bitbucket, but the shell access is disabled, of course. Now we can copy this command. So copy the command, and let's move to temp directory, cs temp. Because this is just a test, we are not going to clone this or push our code like this. This is just a test whether we are able to clone our repository by using SSH or not, so let's execute this command and figure out. So cloning into vproapp. You appear to have cloned an empty repository. So that worked. We know repository's empty, but if you do ls -ltr at the end, you should be able to see here the repository. You can do like this. Also cat vproapp/.git/config file in that repository. You can see here the remote URL. So it successfully cloned the repository. So that was just a test. Now we are going to clone our vprofile project source code, remove the GitHub URL from that, and add this URL in our repository. So it's migrating code from GitHub to Bitbucket. Okay, so come to github.com/hkhcoder/vprofile-project. That's our repository, and let's clone this source code. So get the HTTPS URL, copy that, open your terminal, and I'm going to go to D drive, so choose the location where you want to clone the source code. So I'm just creating it in D drive. You can do it somewhere else if you want. So I'm creating a folder in D drive /d/aws-cicd and then I do cd into that folder, and I'm going to once again copy this URL and paste it. Right, I had to run the git clone command. So git clone and the URL. We clone our source code. Okay, and then we get into our source code. Let's clear the screen. Okay, so I'm in the repository. We can do cat .git/config. So we should see here github.com. We have to remove this URL, but before that, we need to check out to a branch. So do git checkout aws-ci. So I check out to the branch. So this repository has many branches. I want to check out aws-ci branch and the main branch. These two branch I want to check out. That means I want to move it to Bitbucket. So whatever branches you want to migrate, you need to do this checkout. You need to check out to those branch, and those branches only will be moved because if I do git branch -a, you should see a lot of other branches, but I'm going to check out only this. I'm going to move only these two branches main and aws-ci. So execute this command git checkout aws-ci. Also, if your repository has some tags, you can execute this command to fetch the tags, git fetch --tags, which will fetch all the tags as well. So git checkout branch name, and fetch the tags, and now let's execute this command, git remote rm origin. This is going to remove the GitHub URL. You can do cat .git/config. You can see that the remote URL section is gone completely. Now execute and wait. First git remote add origin, and then you need to give your URL over there where you want to push this code. I guess so from here, just copy this URL, just the URL. Don't copy the git clone command. git@bitbucket.org, and that's URL, and paste it over here. So this is going to add now this remote URL. If you do cat git/config, you should see here Bitbucket URL. Okay, now we do git push. So git push origin --all. That means all the branches on which you did checkout, all those branches, so we have two branches, main and aws-ci. These two branches are all branches. It is going to push to this repository. Okay, that's done. Let's go and check it ourself. Let's refresh the page, and then you should see here code over here, main and aws-ci branch. Okay, so now let me run the history command and show all the steps to you once again. So we create a folder, get to that folder, and we clone our source code, the source from where we want to migrate. We clone the source code, we get into the repository, and we do checkout to as many as branch we want to push to the remote, to the target repository. Do a checkout, and we can fetch the tags. We don't have any tags in our repository, but if you have, we can do fetch the tags. We remove the URL, and then we add the target URL, and then we push all the branch. So these are the steps to migrate Git to Git repository with all the histories. Now in the next lecture, we are going to use AWS CodeBuild service, which will fetch the source code and build it into an artifact, so wrap this up and join me in the next lecture.
