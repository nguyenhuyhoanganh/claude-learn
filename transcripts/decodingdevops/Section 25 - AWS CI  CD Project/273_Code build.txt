# Code build

Instructor: Hello and welcome to the CodeBuild lecture. In this lecture we are going to use AWS CodeBuild service to fetch our source code and build it into an artifact. So search for CodeBuild and open CodeBuild. So I really don't need to explain you CodeBuild from the scratch because you already know Jenkins, it's the AWS cloud version of Jenkins. The difference, or the major difference I would say is Jenkins, we need to launch an instance or you need a virtual machine or a computer to run Jenkins service. CodeBuild is different than that. It's pay as you go service. So you create a CodeBuild project which is like a Jenkins job and when you run that project or when you run the build, the compute resource that it uses, the ram, memory, CPU, whatever it uses, only you pay for that, only for that much amount of time. Like as long as your build is running, you only pay for the build time. So you don't need to continuously be running an EC2 instance. If you see here it says AWS CodeBuild is fully managed continuous integration service that compiles source code run test produce software packages and says pay only for the build time you use and it's pretty simple. The main thing in CodeBuild is the build spec file. So all the build information you give in a yml format and select the other details like the source code and stuff. We'll see all those things. Now here also it shows source code. So this is an example of a build spec file. Pretty simple. We'll get into that, we'll see it from the documentation also. Now before we create this CodeBuild project, we need to create an S3 bucket because that's where we are going to store our artifacts. Just search for S3, open the S3 bucket service, create a bucket, make sure it's in the same region where you're using CodeBuild, give bucket a name and just create it. That's all. And just giving the name awscicdvproartifact. Give a different name. The bucket name needs to be unique. Add some numbers to make it unique. Give the bucket name and simply scroll down and create bucket. That is all. Okay, let's learn this service by doing it. So click on create project. Think of same like creating a Jenkins job or Jenkins pipeline, give project a name, I'm just saying vproappcicd, I'll just say vproappbuild. Source provider. We need to select the source provider from where the CodeBuild job is going to fetch the source code. We are using Bitbucket, so from the source provider we can select here, Bitbucket, we have here other options like GitHub and GitLab, as we are using Bitbucket, so we're going to select Bitbucket. Once you're connected to a Bitbucket account, you should see your repository over here and you can mention the branch name. But this job is not connected to any Bitbucket repository and you should see this red cross mark over here. We can click on manage account credentials to connect it to our Bitbucket repository. So click on this, this should open a new tab and you can see here different types of mechanism for credentials which you can use to connect to your Bitbucket repository. The first one is Bitbucket app. And the last one OAuth app. They work similarly and this is completely managed by AWS Bitbucket app. And then you have personal access token, which is a premium feature of Bitbucket. So we are not going to use that one, but this is the more recommended way. The next one is app password where you mention the username and password of your Bitbucket repository or your Bitbucket account, which is not recommended. No, passwords are generally avoided for security reasons, they can be exposed easily. But I'll show you how you can generate a username and password in Bitbucket account. So you can click, you can go to your Bitbucket account. So to get the username first, click on this gear symbol over here and go to personal Bitbucket settings. And here you can find the username, if you want you can change the username. Actually I did change the username because it was something different so I just changed it. You can do that as well. And then here you have the option of app passwords. You can go there and you can create the app password. Okay, so that is one way of connecting. Okay, so let's come back here. So as I said, we will be using Bitbucket app, but let's take a look at OAuth app also, it's almost similar, just click on this. The extra feature that it gives is you can store this fourth token in secret manager. Now, secrets manager is not a free service from AWS and it says it's recommended. You can also use CodeBuild service. So you can click on CodeBuild and you can click on connect to Bitbucket. If you're already logged in you can select your workspace and you can select your repository. If you're not, you can select the account and log in and then you can connect. As I said, we are going to use Bitbucket app. So let's click on this and you will get this. You can create a new Bitbucket connection over here. So click on that and you should get a new window. You can give connection a name. So I have given name here, bitbucketvproapp and I'm going to say connect to Bitbucket. Okay, so it automatically connected because I'm logged in from the same browser to my Bitbucket account. But if you're not logged in from the same browser, then you can click on install new app or it'll throw another window where you can select the login detail. Like we used a Gmail account, like I have used the Google account to log into Bitbucket. So you can select your Google account and you can sign in from here. But if you're already logged into Bitbucket account from the same browser, you should see or hear the new connection. You can select that and you can say connect. Now here you should get your connection name, select that and say save. Okay, once you do that, that tab should get closed. If not, just come back to your tab from where you're selecting the Bitbucket and here you can select the repository and you can give the branch name over here. So we are using awscicdbranch, so I'm going to give here aws-ci. So this should be enough. Then scroll down. So provisioning model on demand, like pay as you go model, not the result capacity and environment image. So we'll just say AWS managed image. You can create your own custom docker images also to build and you can specify that. And EC2 is fine here for the compute Lambda is much faster and cheaper option. But we are good with this for now. Operating system. Let's select here Ubuntu, and this is fine standard. You can select the image as 7.0. Now as you go, I'm recording this lecture in August, 2024. As you move forward, AWS will remain, will release different version of the images. 7.0 works fine for us. This is the latest for now, for source code, it works. So you can select that, don't select any latest version, just go with 7.0 If any new version comes, I will be making test. If everything is working fine then I'll release the new lecture. But as long as you see 7.0 over here, select that and let's come down. New service role, let's say now create a new service role, additional configuration, we don't need anything else. So you can select the compute resource, how much it should use. So 3GB memory, two CPUs the minimum. So we have to select that only, coming down. Now here you have to mention the build commands and the build spec. So if you know we just have one simple command which does everything, like for example if we install and you are good with that, so you can mention that command, but we need to, you know, feed more information. We have to specify the JDK version 17 or 21. We need to change the RDS detail in the application properties file, the database detail. So we need to run some commands also. So we need to use a build spec file for that. It's a file in the yml format. So use a build spec file, select that, oh wait, sorry, instead build command is fine, just switch to editor. And here itself we can the build spec file. Okay, the ideal way will be to in your source code itself you have a build spec file and you mentioned its path over there, right? Right in your source code. So from the source code you mentioned the path of the build spec file and it'll use that, that's the better way. But we are going to go with here switch to reader. So from the resource section you can download the build spec file and copy it over here. After making the changes, I'll show you what changes we need to do. I have also kept the build spec file in the repository. So you can go to HH code or you can go to your Bitbucket repository, but make sure you switch to branch AWS-CI and here you select AWS files and buildspec.yml file, this file, right? So this is the whole file. So let's copy this. We need to make some changes so you can use an editor for that. Okay, I'll just click over here and say open raw and I will copy all this content and I'm using the sublime text editor and I'm just using it to show you that's all. So you can use the editor of your choice. So build spec file, it's in yml format so no surprises for you. We already did it in Ansible and before that also we use yml file. So it's key and value. So version 0.2, that's the current one right now for this one. And you can mention environment variable. These are all commented, you see hash in front of them. But you can set environment variables. Also you have to say ENV variables, key and value. Key and value. Now also you have parameter store. Now there are many other things in the build spec file in DevOps project scores we have covered that in detail. Here we are keeping it simple, it is showing you, you can mention variables over here that will be exported. And then you have phases in that you have various kind of phases. One of the phases, install phase. And in install phase you can mention commands or you can also mention the softwares that you want to install. So runtime-versions. Now AWS CodeBuild service knows how to read this and execute this. So you mentioned Java and you mentioned the version coreccto17. That means JDK17, provided from AWS. So by using runtime versions it'll execute its own command and install coreccto17 for us like that. There are many other, there's a huge list. We'll see that in the documentation in a moment. But just quickly understand this build spec file for now. So in the phases, you have install phase any tool that you need. So we are mentioning that pre-built, pre-built commands, you know we can mention commands in that it is just before the build commands. So our main command is here, mvn_install. So we say build commands and mvn_install, but before that we want it to use maven 3.9 and we want to change the database username, password and URL in application.properties file. In our source code we have this application.properties file. We want to search there the password username and the URL of database and change it to the RDS that we are using, right? So let's come once again from the beginning. So pre-built commands. So we see app-get update, JQ java query, we download maven double get command with maven with double get command, we download maven 3.9, tar extract command, we extract that tar ball and we create a link, ln-s, you know from this we change it to the name maven. So we will have a folder called maven. So when we done MVN command, it's going to look for that maven folder from that, it's going to use the maven binary, right? So maven is set over here and then we are using the sed command to search for jdbc.password. This text equal to admin 123. You know, we search for this text and replace it with jdbc.password is equal to the password of the RDS. So the RDS username password and the URL that you have saved, you need to change that over here. I will just do that in some time. Same way we search for jdbc.username equal to admin and change it to jdbc.username admin, okay it's the same thing. And then search for db01:3306 to the URL of the RDS there, :3306, right? So in application properties file, we search for these three things and we replace it, right? So let's go to the repository. Let's see, once again branchaws-main. So SRC, main resources, application.properties file, right? So it's going to search for this and replace it with the RDS endpoint. Search for this, replace it with the username of the RDS, search for jdbc.password and replace it with that database password. Okay, so we are going to change these details in a moment. Let's look at the other settings of buildspec.yml file. So install phase, pre-build phase and then you have build phase. Build phase, we just have a command. So we are saying commands and we say mvn install. So it's in a list format. You can give the list of commands just, you know, with a hyphen in the next line. But we just need one command, mvn install. And then you have post build and I have given here commands mvn package. Now this is really not necessary for this, mvn install is going to give us the artifact, we don't need to package it again, but this is just two extra steps just to show you. And then finally we have artifacts. So in phases, you have these phases. Install, pre-build, build, post-build, that's all we need. There are many other phases as well, but this is all we need. And then after this phase is completed, then artifacts. So what's the artifacts? So we're seeing files and we are seeing **/*. That means you know all the files. Base directory, this is going to be the base directory. So this is the artifact. We are not mentioning the.v, we are just mentioning the entire directory name, target/vprofile-v2. So that directory will be after the install completes, that directory is their final artifact. And if we mention a destination like an S3 bucket, it is going to copy this content to the S3 bucket. Or if you're saying deploy it somewhere else, it's going to deploy this entire folder over there. Of course it is going to archive this folder and then move it to the destination. Now I know there's too many things over here, but do not worry. There's proper documentation for everything from AWS. So let's go back and just Google CodeBuild build spec file documentation, search for it and over here build specification reference. So all the information is given. Do not panic, it's just when you need it, you use it. You don't need to read this entire thing, end to end and understand everything, just ask per your need. So let's scroll down. Build spec syntax, right? So that's the syntax, okay? So in this file everything is given and when you click on something, it's going to send to its documentation. So env. So you've seen that variable parameter, store exported, variable secret manager and DevOps project scores, we have covered many of them. Proxy, batch, phases, right? So install phase we have used. So you know, you can mention other things like run as what username, many other things, commands. Basically you'll need command most of the time. Pre-built, again, you have commands, build, again, you have commands. Post-build, again, you have commands. It's again a series of command, but it's just divided into different phases. You see, it's pretty simple. Just commands. Reports. So we do have reports, you know, we can also export our reports, unit test reports from this location to some other destination. So you can mention all the details over here. S3 bucket, where you want to send and all. Okay, artifacts. Now if you want to understand this, for example, I want to understand artifacts, I'll just click on that and tells me what is this all about. And you know, everything is mentioned over here, how you can, if you want to, you know, represent all the files, this is what we did. Or you can mention a subdirectory inside that, all the files. Artifacts name, if you want to change the name, override the name, default name from, you know, let's say from V profile, have V2 to something else. We can do that also as example given over here, okay? So as you need it in your project, you look for those option and then use it. Experiment with that. You don't need to know all this by heart, okay? You know, when you keep using it then you'll slowly become master of it. But don't need become a master of it for now, just go through it and just keep in your mind that you have all the necessary information. Okay, so now it's time to change our DB details into application.properties file. So we are going to mention all that in build spec file. So make sure you copy the RDS endpoint URL, the username and password. So I'm going to do it one by one. So let's let me copy the password first. I already saved it in the sticky notes as I told you previously to save it somewhere safe in the sticky notes or somewhere. So take a build spec file either from the resource section of the lecture or from the source code and just change the DB password carefully. Password equal to, and the database password, username, you don't need to change the same username, but if you did change username while creating IDS instance, then make sure you give the right username over here. Endpoint. So that's the endpoint. If you're not saving in sticky notes, copy it from the RDS page, carefully copy it, just the endpoint. Okay, from here, till .com, okay, paste it, okay, make sure it's like this. Okay / the endpoint, :3306/. Okay, save this. You don't even need to save this. Just select it, copy it, and go to your CodeBuild project. Okay here, select everything and paste it here. Make sure there's no error over here, no syntax error or anything. If you see, fix it now, just copy the build spec file once again, make the changes and then paste it over here and scroll down and select the artifact location. So we have to select Amazon S3. This is where we want to copy our S3 bucket. And from that, select your bucket name. So mine is this one, awscicdvproartifact. That's what I have created. Just give the bucket name, don't need to mention any folder name and then come down, no artifacts packaging is required for us. Scroll down, CloudWatch logs. We definitely need the logs when we build the job because it's not a Jenkins instance where we can see the console output all the time. You know it's pay as you go. So as the build runs that time, if we miss the information that said it's gone. So we definitely need to store the information somewhere. And that is CloudWatch logs. So you can mention the group name and then you can mention the stream name. Then you can later also check in the CloudWatch logs. So I've given group name as vprofileproject and stream name as vprobuild. So later also I can go to CloudWatch, look for this group name inside that, look for the stream name and read the logs. And then can take many action on the logs later also. Okay, so that is all in this CodeBuild project. Let's just create this build project. And once this is created, in the next lecture, we will execute this build job, test it, and also create a pipeline from that. So create the build project. Complete this one. You don't need to run this for now. Complete this and join me in the next lecture.
