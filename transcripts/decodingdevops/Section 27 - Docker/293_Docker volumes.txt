# Docker volumes

Containers are generally known for its volatile nature. That's because containers are disposable. The way you make any change in a container is always through the image. So if you want to add a package or a configuration file or any serious change you want to do in the container, you don't log in and make changes. That's because they're disposable. We delete them and we create an updated container from an updated image. And for that reason they are called as volatile. If you store any data in the container and if we have to replace it, we'll see in Kubernetes when we do upgrades, rolling upgrades, it's going to remove the container and create a new container. So when it removes the container, all the data will be also gone with it. But what if you have a container that is stateful like MySQL that needs DB, it stores databases and reads from the database. If you happen to replace that container, all the data will be also gone. So for that, we have container volumes so the data in the container does not persist for a longer time. Moreover, it will be difficult to get the data from the container if you're using it. Like you're making code changes live, right? And we have seen it's in a separate directory, it stores its data so it's tightly coupled with that directory file system structure. So if you want to move that data somewhere else, it's very difficult or like let's say you can't do it. So Docker has two options for persistent data. One is volumes. It's a wrapper where in the docker's volumes directory, it will create a directory and then you can attach that to your container. So whatever data you store in a container at a separate directory, you will attach it to a directory. All the data from that container directory will be going to the volumes which is in your host machine. So it's safe on your host machine. So all the volumes will be in this location. /var/lib/docker/volume location in Linux, another option is bind mount. This is same as Vagrant Sync directories. We have seen in Vagrant sync directory, where you can take any directory from your host machine and map it to a container directory. And this is mostly when you want to make changes to the container data. You can do it from your host machine directory and that will be reflecting into your container directory. So you can select any directory from your host machine and map it. By the way, volume is also similar, but volume is managed directly by the docker. It will put the directories in this /var/docker/volumes. OK, so let's see both the options with an example. For this, we will take a MySQL image. Let's find an official MySQL image in Docker Hub, you can just make a search search for MySQL. Okay, there it's an official image. Let's click on it. The benefit of official images you can trust on it, One. Second, you have the documentation to read from. So what is My sequel? Blah, blah blah. How to use this image? Yes, so docker run as a name, name of a container. E option is to export variables. So you can set this variable MySQL root password with a value. And you definitely have to set that. Otherwise your container does not run MySQL container D. And then you give the image name. Right? But what about volumes? It's going to store some data somewhere, right? MySQL process. So let's find that. Okay, here we have caveat section where to store data. You can read through it. It says V option. You pick up a host machine directory, map it with /var/lib/mysql. So that's the container volume. That's the container directory where it's going to store the data. Now already, don't come to an assumption that if MySQL by default stores data at this location, it would be same in the container. It could be or it could be in a different directory also. So one, if it's an official image, you can find it directly like this. But if it's an unofficial image, you can also run inspect command and find where it is going to store the data. So let's pull this image docker pull MySQL and I'll go with the tag five seven, just some random tag. Okay, so the images here. Now I want to know what is the volume directory, and also I want to know what port number the process runs. So you can run a command docker inspect and you can give the image name. Oh, wait a second. I have to give the tag, I think. Okay, so some information in JSON format like that's. The ID when it was created from what container it was created showing the container ID also hostname few other information exposed ports 3306 and 33060. So there are two ports that will be running if you run the container from this, some environmental variables. CMD, this is the command. When you say docker run, it actually is going to run this command, which creates the process. With CMD, you could also have entry point. We'll find that. But yeah, volumes. This is what we're looking /var/lib/mysql right? So that's how we can find out entry point. Now, entry point CMD works together or sometimes individually. If you see both, entry point will have higher priority. It will run that script and then it's going to run this command. Okay, so now we know details about this image. So we can run the right command. So let's say docker run. Wait, image name, five seven tag. Okay, docker run name. Let's give a name. I will say DB. Just DB. Now let's give some different name like V profile, DB, something like that. I have to export a variable that is mandatory to run this container. So the variable is MYSQL_ROOT_PASSWORD So which is going to set the MySQL root password and we give some value. I will say secret password, something like that. Okay, port number. We know it's going to run the service on 3306. So I'm going to map some port with 3306 port. So I will say 3030 of my host machine maps with 3306 port of the container. Okay, now volume V. Now, first we're going to see bind mount. We'll specify a directory path in our host machine that will be mapped with the container directory /var/lib/mysql. So we need to create the directory and I will do it here itself in the home directory. I will say vprodbdata. Okay, I created that directory. Okay, now let's take the command once again. So the host machine directory and complete path /home/ ubuntu/vprodbdata give the complete path and the container directory /var/lib/mysql and then the image name, which is MySQL the tag five seven. Before we execute we're, we have to make sure it is going to run in the background. So we have to give hyphen d option so it runs in the background detached mode. Otherwise it's going to take up our shell and will not be able to do anything. Okay, let's run it now. Continues running. Let's check the directory vprodbdata. And you should see now all the data from /var/lib/mysql directory of the container. You can log into this container docker exec and let's give the container name bin. So if you log in, we go to /var/lib/mysql. You see exactly same data, right? So this directory, this data is now stored in the host machine directory with pro DB coming out. So this is bind mount. Now, even if I happen to delete this container, so I'll stop the container and I will also delete the container. And let's check vprodbdata still there. Bind mount is mostly used to inject data from host machine to the container, like code developers are writing. So while the container is running, they can do all the code changes in the host machine directory and that will be reflected in the container. But for preserving data purpose, the better option is volumes, docker volumes. So docker volumes if you run volume, yeah, it should show you other options. Docker volume create. For example, let's give a volume name. I'll give my DB data, which creates a volume. You can say docker volume LS. All right. Now let's take our command once again back. Now. I have removed the container. Okay, if you have not removed the container, then while creating the container, you make sure you give a different name and different host port. Otherwise there will be a conflict. I removed the container, so it's fine. I'm rerunning it. It's going to create a new container now with the same name and host port for volume. Instead of giving the entire path of the directory, we'll just give the volume name. So I'll just give my DB data that's more convenient. Let's run this. Okay. Docker PS okay container is running now as I told you, it should have stored the data in VAR lib docker volumes Directory. Okay. In the Volumes directory, you should find your volume directory with the same name. And inside that you should have underscore Data Directory. And inside this, you can see your data from your MySQL container. Again, if I happen to delete that container, the data is still in my host machine in a volume, I can attach it to the new container when it gets created. And this use case for the volume option, most of the use cases you want to preserve data of the container to our host machine. Now, about docker Inspect Command docker Inspect Command you can run it also for your container web vprodb that's our container docker inspector on a container will give almost similar information with more about container in JSON format. So the container ID when it was created path so this is the script it's running arguments MySQL D status is running running true. Some other information process ID in your host machine from which image it got created. So image ID log bar that's where it's storing the log. So we can really retrieve the log of this container. If you run the command docker logs and the container name, we can see the logs of the container. These logs are basically the one that is getting generated by the process we run. The process generates some output. That's what you're seeing when you do docker logs. Command docker logs and the container name, which is good for troubleshooting. Right? Let's go back to the Inspect Command. Okay. Binds so my DB data volume is bind with /var/lib/mysql when you're troubleshooting or when you want to know where is it really storing the data, you can find that out through Binds, port binding likewise, right? 3306 is mapped with host port 33rd. There's no quota, nothing set. So memory zero means it can use as much as memory, as much as CPU. It wants more about mount option amount, information, type, volume, volume name, location where it has storing destination that's source, that's the destination. Read write mode true exposed ports again, environment variables that are set and you can see all the environment variables. The one that we have set CMD what command it is running entry point. This is the script that it runs first and then it passed this as an argument. So when you say docker run, it's going to actually run the script and pass this as an argument to it. That's how the entry point and CMD works together. And also it has the IP address. As you're talking about a container having an IP address, and you can really ping to this IP also Also that's replying back. Okay, so this IP you will not be able to connect out of your host machine. This is a network inside the host machine. So if you're already in the host machine, you can use this IP to connect to your container. Also, it's running MySQL service, so we can use MySQL client, MySQL -h Let's give the IP address of the container. The user is root, not admin p. And we have given password. Secret. I have set my Secret Pass. Okay, let's log in. No, I think I've given the wrong password. I need to check the password. Wait a second. Let's do Docker Inspector. It Secret Pass. No, it was just Secret Pass. I was giving wrong password. Okay, let's try it once again. Secret Pass. Yes, I'm in show databases. Right? Okay, so you have not only just learned docker volumes, you have learned Docker Inspect command. How to read the data, how to find the right command, right option for your container to run. You Docker Inspect to the image. You can find the volume, path, port number. All right, once you're done with this exercise, do the clean up. Make sure you keep cleaning things. Otherwise you'll end up having many containers and images. And easy to instance. Just eight GB volume and T two micro may go out of resource. So I'm going to clean this docker, stop the container, and let's remove the container. And let's remove the image. MySQL five dot seven. Alright, that's all in this video. I will see you in the next one.
