# Docker commands & concepts

First we are going to see a website called As Hub. docker.com and I think it's really the backbone of Docker. This is the place where you have images, lots of images. Official, known official. You can create your account, you can put your own customized images. So there are images for Mongo traffic, postgres node redis, Ubuntu. These are official images. You should see their official image means that is directly from the vendor. MariaDB, Alpine, busy Box, docker, docker in Docker. Wow, that's funny. Nginx, memcache, httpd, registry python, hello World, RabbitMQ. The list will be on and on. Okay. You can create your own images and upload it to Docker Hub as well. So Docker Hub is called As Registry. Docker Hub is the registry for Docker images. So what release Docker image. We have seen one simple example hello world image and the container got created from that image. So a docker image is really a stopped container which is archived. Okay, just like you have VM image from where you can create VMs or like AMI, but cannot directly compare that virtual machine image. Are we talking specifically about container images? Image consists of multiple layers. Each layer will be some data. Like when you're building your own image, let's say you have installed a package after install Git that will create a layer. After that you created a directory that will create another layer. So like there will be multiple layers. It has its own file system format and all the layers will be in read only mode. And we mostly bundle our app in the image. Container runs from the images. No, literally they runs from the images. They are connected to the image. Unlike VMs. When you create VMs from an image like we create from Vagrant box, the box and VM are separate then okay, the VM will be complete clone of the image. But containers are not like that. They are running from the images. Like directly. You can't remove image if the container is running. Images are called as repositories when we are talking in the registries, right? In a Docker Hub, if you see Tomcat, that's a repository. Tomcat repository in Docker Hub registry. So Docker images really the images becomes containers when they run on Docker engine. That's the simplest definition. And Docker Hub is not the only registry. You have other cloud based registries. Like you have GCR, Google Container Registry. You have Amazon ECR where you can create your own images and you can upload there your own private registry. You can build your own in house solution to store your images. You can use Nexus Three as a Docker registry or J Frog artifactory or DTR which is Docker trusted registry. It's container based registry. You can find its image also on Docker Hub registry. That's kind of funny. Okay, any case, so container runs from the images guys So you have an Ubuntu image and you're running containers from that. So as many containers you run from it, you can. OK. Containers will have a thin read write layer. All the data that you see will be from the image. containers. Sorry. Image layers will be all read only mode. Okay. Maybe there is a directory opt data directory. That's a layer and container referring to that. If you get into the container, if you go to that directory, you are actually accessing the image data. So containers are not going to replicate the entire image content. And that's how it saves a lot of space. It uses some aufs file system. There's lot of technicality also in that. But for a higher level understanding. Container is just a thin read write layer. And all the data you see in the container is actually from the image. To create a container, just a docker run. Docker run will create the container from the image. You have to specify the image as we did docker run. Hello world. Okay, so there are a lot of docker commands. We'll see them now, but we'll go to Docker Hub first and find some image for us. Okay, back to Docker Hub. Let's take a simple one. You can go with NGINX. Okay, let's go with NGINX. NGINX is a web service. We know it runs on port 80, right? So let's take this. So every official image will have nice documentation. Read it. If you want to just pull the image, you can say docker pull nginx. There's the command. Okay, so I'll say here docker pull nginx. This is going to pull the image from Docker Hub by default. By default it will go to Docker Hub. Okay, now you should run the command docker images. Right? We see two images. Okay. One is the nginx that we have just pulled. The other one was Hello World. Okay, you see, it's called a repository. They'll have a tag. Every image will have a tag. One image will have multiple tags. Tags are mostly used for versioning latest. It's just a name. It really doesn't mean it's not a guarantee that it's the latest image, but majorly for official images, latest will really mean the latest image. Image will have an ID. It will have the time when it was created and size of it. One three three MB. That's it. Imagine you create a VM image which has NGINX. If you take example of AWS itself, AMI that's Linux is minimum eight GB size. Compare that with 133 MB. What is it? Nothing. Okay, now let's run container from that. Okay, so let's read the documentation. Let's keep reading it. So we have different tags. It is telling us you have a tags section. You have different different tags that you can pull any image. You just have to know any tag. You just have to say colon and the tag name. Like I will do now. I'll pull some image with different tag. I will say this one. Okay. I'm just doing it just for fun. Okay. Nothing. Okay, now it's pulling some layers. I think it's done already. Oh, okay. Let me clear the screen. Now, you should see nginx here two times. One with this tag mainline Alpine Pearl and one with latest. That's how you can mention the tags. Okay, what is nginx and stuff? How to use this image. So every official image will have proper documentation of how to run it. It says Docker run, f name, name of the container. V that is for volume option. We'll see that later. D nginx. That's the name of the image. Okay, so let me do it my way. Now, so far we know docker run and the image name docker and creates the container. That's the image name. I'll give a name to this image. I'll say name. I will say my web. Okay, we have seen previously, when we just run the container, it runs on the foreground, it runs on the shell. If you want to run it in the background, you give D option detached mode. So it runs in the background so it doesn't capture our shell. Also, now, this is going to be continuously running process. Not like Hello World, hello World runs on the screen and that's it. But this is going to be continuously running. If you don't want it to run, it on a shell So Hyphen, D, run it in background. Now, nginx runs a web service, a process we can access from port 80. Now, that's what we know. NGINX runs by default on port 80. But you should also check from the documentation. Or you can run the Docker Inspect command also that I'll show you. So you should be sure your container is running on what port. The process is running on what port in the container. So you can map a host port. You cannot connect to this container directly because it's going to be in a private network. In the host machine, there will be a private network. It will be inside that. Think of this like the private subnet instance. Running in a private subnet. You cannot access it like that. This container will be running in a private network. If you want to access it from the outside, you have to map a host port. So you access the host on that port, and then it is going to route the request to the container on port that you specified. So I will say -p. Let's take some examples. 70, 90. That's the host port. Container port is 80. This is called port mapping or port forwarding. You map a host port with the container port. Now, host port should be free container port. You need to know what process the process runs on what port. Okay, let's hit enter. Okay, it returns a long number. It's actually the container ID. You can say docker ps. Okay, so that's a small container ID. It's running from this image, and it is running some command, which is actually a script that runs the nginx process and you can access it from any IP address on port 7090. Okay, that's the host port. So we have to access our host machine. Ours is easy to instance, let's copy its IP address and give the port 70 90. Okay, security group. EC2 instance security group. I told you we have to add a rule. So add rule and I will say all traffic from my IP because we are going to run many, many containers here. Okay, let's save this and let's come back. Let's refresh and there you go. An nginx service running from a container. It's continuously running. If you want to stop it, you can say docker stop and you can give the container ID or you can give the container name also. Okay, it is stopped. You say docker PS. It's not running docker ps -a You should be seeing it in the exited status. Okay, if I want to start it, I'm going to say docker start and again container ID or the name this time let's give the name docker PS. Now it's running. Okay, let's do a little investigation on our host machine. Let's run the process command ps -ef on my host machine. As I told you, container is just a process running from a directory. So I need to prove that point. So ps -ef I do and I should see it there. That's the process ID of your container. That's actually your container. And this actually created one more process. So there are two processes running from the container. Now I told you a container running from a directory. Yes. So let's be the root user and we are going to get into this directory /var/lib/docker. you see different directories here, there's a container directory in that. Let's get into that. And you should see here two directories because we have two containers. Okay, match that number. That's the directory. That's the complete container ID. So let's get into that big directory and you should see some data. Okay, there is resolve.com file for resolving container names, its host name and stuff. Okay. I also told you one more thing that containers runs directly from the images. So what are the page also we are seeing our web page. It's actually coming from the image. The containers does not have any data mostly if you're not storing anything. So let's check the size of it. du -sh And let's check the size of this directory and you see that it's just 40 KB. There's just some configuration files you have. The data is from the image directory. Okay, but image directory you will have distributed data. If you do ls image, you should see overlay two and you should see different directory structure. There I prove my point. Container is just a process running from a directory and all the data is coming from the image. Containers can store data. You can have dynamic containers or stateful containers that stores data or reads the data. And if you're going to do that, you have to make sure you connect volumes to it. We'll be seeing that next. But let's do a little more experiment with this container docker PS. So just try to become comfortable, right? So you understand the technology, nothing real time and all sorts of things. For now, we're going to see that in Kubernetes. For now, we're just getting comfortable with this technology. Okay, if you are having an urge to log into the container like a VM, you do login like we do SSH, but there is no SSH running in the container. So you can't really do SSH. So how do I log in? Well, basically there is no login because it's a process. How can you really log into a process? But you can attach to this container, or if you don't want that, you can run commands also. On this, there is an option docker exec and you give the container name or the ID and then you can run command like I would like to run ls / Okay, so that command got executed in this container. And you see that you should see similar directory structure like you see in any operating system or Linux operating system because it's a Linux container, right? Okay, how can we get into that? How can we attach not login attach so same docker exec command. So there's a trick. You can do /bin/bah. So if it has bash shell, of course it will have Bash shell most of the time, or you can have other shell also. But mostly you'll find the Bash shell in this path. If we can do this, we can attach it. But the problem is when I do that, it just runs that command in the container. I'm not connected to that shell. So if you want to connect to that command that you're running, you can say -it interactive and T is for the TTY. So what I'm really saying is run this command in this container and attach me to that command to that process. And when I do that, I'm in the container I have attached to that process. Now in the container, the container is really a stripped down version of an operating system. So you'll not find many things. Like if you try to run, if config command, it will not be there. You try to run PS command, most of the containers will not have that also. But let's install this. Let's install a package that will give us PS command because I want to show you something. So this is actually debian based container. So I can run apt command apt update. See that? That's working. And I'm going to install procps Okay, this should give me the process command. Install it. So installation is happening in the container. Now. We are creating some data in the container that's not recommended, but we are just learning for now lets run ps -ef And you see now the process of the container only PID one. The first process is the nginx process. And that started this process. Okay? But we see one more process, bin bash. That is the command that we executed. And that's how we are attached. We are currently connected to that process 34. Okay. Now, if I type exit over here, this process will be dead. I'm attached to this process. If I type exit, this process will be killed and I will be thrown out. Okay, I'm out. I'm in the host machine back to my host machine. Okay. So that's how you can get in and get out of the container. Some containers will not have bash shell. So maybe you'll have just sh the shell. Okay. Okay, so I have a container running, and I have one container that is dead. And I have three images. Okay? So now I will remove the image docker RMI and I'll remove NGINX image with this tag. Okay, I can remove that. That's nice. Okay, now let's also try to remove docker images. Now let's try to remove the image RMI to remove the image. The latest one. Okay? You don't need to give tag. If you don't give, that means latest. Anyways, it's going to throw me an error. It is saying container with so and so. ID is using it is using its referenced image. That proves the point again, containers running from the image. So if I want to remove the image, first I have to remove the container. So if I try to remove the container, let's see what happens. Okay. It says you cannot remove. It is in a running state, so you have to stop the container. So first is I stopped the container and then I remove the container. And then I can remove the image. All right? Okay, let's test one more image. There is an image called Ubuntu. Yeah. It's not going to be an entire Ubuntu operating system. It's going to be a very smaller version of Ubuntu in a container. Okay, so let's pull that image and the latest tag. Okay, so I want to now run a container from that. Let's see what happens. docker run ubuntu OK, let's say. Docker PS. There's nothing. What happened? Docker ps -a You see, that container is in exited state. So it started and it's dead. That's because it is running bash shell. And you need to interact with the bash shell. Otherwise it cannot be running like that. So if you want to run it, so you have to say hyphen it and you give the bin bash. Okay. I mean the container, if you check the process. And now yes, PS command is here in Ubuntu container in Ubuntu image. So if you see the PID is the bash shell. Right? And now if I exit, which is going to kill this process, that's the main process. The first process, if that is dead, the container will be also dead. I'm out. Let's check docker PS. It's not there. ps -a You see that one? Okay. It's an exited state. Because we actually killed that main process. So that PID one is actually your container process. For NGINX, it was NGINX process, but for here, it was our bash shell. Okay, let's do the clean up. Docker RM. Let's remove it with its name. I will remove multiple like all the containers. Since they're in a stop state, exited state, I can remove them. Check the images. Let's remove the image. Both the image one, two also and also this. Hello, world. Okay, so clean slate now. Okay. So by running those commands, we got comfortable with the containers. Docker containers. So we see images. Let's list the images locally. Docker run is to create the container. PS is to list the containers. ps -a is to list all the containers. Docker exec is to run the commands on the container, and we can use it also to attach to the container. We can start, stop, restart or remove a container. Docker RMI is to remove the image. Again, there is a dependency. Remember, you have to remove the container. Docker Inspect will be seeing that in the next video. Check the official documentation for more commands. We'll be seeing more and more commands. And we'll continue learning all this from the next video.
