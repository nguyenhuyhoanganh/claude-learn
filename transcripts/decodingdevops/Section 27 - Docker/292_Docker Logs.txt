# Docker Logs

Hello and welcome. In this lecture, we will talk about docker logs. To be specific, logs of your container. Docker container. Now, there is a simple command, docker logs. And you give just the container name or its ID, and you can see the logs of that container. But you really need to understand what are these logs. So before we run that command, let me run a command docker run capital P NGINX. Even before I run this, I'm going to fetch this image. Docker pull NGINX. I have cleaned everything. I don't have an image, any container. So I'm pulling a fresh image. Okay, we have the image. Now we can see detailed information about an image by using a command called docker inspect. Now, this is not logs, this is metadata of the image in JSON format. Let's run this. There's a lot of information about this. We're going to see this in coming lecture. But I want to just show you one thing here, two things, CMD and entry point. So when you say docker run and you give the image name, it basically runs first this script, which is an entry point, and then it is going to run the command which is in the CMD. Whenever we execute any command, most of the command that we run returns an output. This docker entry point sh and this NGINX g daemon off both this command will return output and we really don't see them when we run them in the background. Here it is, docker run -d that is running in the background, detached mode. I ay Capital P is for the port mapping. So automatically it will pick up the host port. I don't need to mention host and container port. Both. Anyway, let's run this. Okay. Docker PS and the container is running. The container name is this and it has automatically done the port mapping and it executed the commands, which is an entry point and CMD. But we don't see its output. Well, we can see its output by running the command docker logs. I see docker logs. I can give the container name or ID and it shows the output. You see that it ran that script and that script returns some output till here. Then here it is running another command, NGINX G daemon off. And that's the output of the second command. So that is logs the output of the command executed in the container. Now, if I do not give Hyphen D option, this is another container. Next container I'm running. If I don't run it in the background, then obviously it is going to run in the foreground and you see all the output right on your screen. But then this is a continuously running process, nginx process. And it's just taken my shell, right? So if I want my shell back, I'm going to do control C. When I do control C, it will also kill that process. So container will be also dead. Docker PS and you see that exited container 8 seconds ago. This is our container. We did control C. It killed the process. Okay, but why I'm really showing you all this? What's the purpose? Well the purpose is troubleshooting, when you build your own images, which we will be doing. And when you run containers from your own custom built images, you might make some mistake and your container won't start. How do you figure out what is the problem by looking at the output of the process and that you can do it through docker logs command. I will show you an example here. I'm going to run one container in the background and this is going to be MySQL five seven. That's the tag name. So this image does not exist. First it is going to pull the image and run the container in the background. So let's run this. So it's fetching the image and it has run the container. That's the container ID. We do docker PS and we should be seeing a MySQL container, do we? No, we don't see it. We do docker ps -a and see, that is our container. It has given this name. It was running from the image. That's the container ID. Check that and it is in status exited. Why it did not start well, how do we find out? Simple. Docker logs. And we see the logs of this container. So you see it says error entry point. It was running something in the entry point. And it says database is uninitialized and password option is not specified. You need to specify one of the following. This is variables. You need to export a variable, either this or this or this. Then only it will run. Now, we'll see detail about this in the next lecture, in the docker volumes lecture. But I will show you how to really run this one. Let's go back to our docker run command and we need to export a variable that we can do it with. -e. MySQL underscore root password is equal to My. So the logs told us that you need to set one of these variables. To set a variable in a container, we need to give an E variable and its value. And there you see it's running now. Now I need to specify this password and all of that that will be seen in the next lecture. In this lecture, I want you to have understanding on what basically logs means in a container. The process that starts the process output is the log. So if something goes wrong while the process starts, you can check docker logs and you can find out the problem. So in the upcoming lectures, when we'll be building our image, where we'll be containerizing our project, we'll be doing a lot of things and things might go wrong. And you will need docker logs command. One last thing. Let's run docker logs command to a running container. All right, so it's all good is anyways? Running. But that's all the output when the MySQL process started. A lot of things right? So fetch different images of your choice. Run docker logs before you run docker logs, run docker inspect. See what command it will be running. And with docker logs, you can see output of those commands. Before you move to the next lecture, do the cleanup process. Remove all the containers, remove all the images, and join me in the next lecture.
