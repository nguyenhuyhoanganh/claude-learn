# Docker Compose

Hello and welcome. In this lecture we will talk about Docker Compose. Now, Docker Compose is another utility. Like we have docker engine which is to run our container. And when we install docker engine, we get docker engine, docker CLI. But Docker Compose is a separate utility that you have to install. And we will install it. We'll see the steps, but let's understand what is Docker Compose first. Okay, in short, docker Compose is a tool to run multicontainers together. Like our view profile project. We will be containerizing it. We will be writing docker files for those. And when we run the container, we know our project containers needs to talk to each other. Like we had multiple VMs or multiple EC, two instances or multiple AWS services that talk to each other in the same network. Like that when the container is, we will have multiple containers and we need a tool which can run them together and we can manage it centrally. And Docker Compose does that very well. You have multiple containers, you don't need to do docker run docker P, E. There are so many options that we specify while we run the container. And if you have like that six, seven containers to run together, there is a huge chance you will make mistakes while running the container. Docker Compose, you have a YAML file where you mentioned all the container information and you said Docker compose up just like vagrant up. Yes, we can compare Docker Compose with Vagrant, what vagrant is for VMs, same way Docker Compose is for containers. This is one example here. This is the YAML file. If you quickly go through, it's pretty simple to understand. We have a version here, services. We have services means containers. So we have two containers here, web and Redis. Now Redis container, there is an image called Redis. So it will be same as we say docker run redis, which will run a container from the image Redis with the name redis. But then we have a web container here. You have an option of build and there is a path dot in current working directory. So this is expecting a docker file in the same directory where you have the compose file. This docker-compose.yaml file in the same place you should have a docker file. So Build Instruction will build an image from the docker file and it will run the container with the name web. It's going to map the port 8000 with the port 5000 of the container. And there is a volumes option here. And its value is a list. Two volumes we have, the first one is the bind mount. dot is the host directory. That means the current working directory will be mapped with the container directory /code. That's bind mount But we have seen volumes where we create volumes separately. And then we map with the container volume. Like that you have here volumes section where you create a volume with name logvolume01 , which creates a volume. And this volume is mapped here. Now this is the same thing what we do in command line, docker run, docker logs, sorry, docker volumes, those same commands. Instead of running commands imperative, we are going to put it in all that in docker compose file declarative benefits. Well, first thing is we can avoid a lot of human error. Second, this docker compose file will be part of your repository. So developer can use this docker compost file to run containers locally or wherever they want. And it will be version control. Whatever changes you make, everything will be tracked. Infrastructure as a code. Now there is a very neat exercise with docker compose in the Getting started section here. And step by step you have exercise. We'll go through this one. It's really neat. I really want you to do this entire exercise. But before that you have to install the compose. So let's go to install compose. And we have here Mac, Windows, Windows Server, Linux. So I have an EC2 instance where I have installed docker engine. In the same instance I'm going to install the docker compose. Okay. So that's my docker engine and it's an ec2 instance. You can launch a VM also if you want with vagrant I'm using here. EC2 instance there's already docker engine running here. I will install docker compose. So that's the curl command. Let's copy that, which will download the compose binary and it's going to put it in this location. The next is we have to make it executable. So here you have the command ch mod to make it executable. And now you should have docker compose. All right, so there are many options. We'll go through this. But first we will need to write the docker compose file and we're going to follow that getting started guide the entire process step by step. It's a very good flow. So let's go to Getting started here. So step one, create a directory. Go into that directory. Let's do that. So this will be like our project directory. Okay, I'm in this. We have to create a file app by this is a Python application. You really don't need to understand this. This basically is going to be a web container when we run it. When we access from the browser, it is just going to return this text in the browser. You don't need to understand this. If you're a Python guy, then obviously it's simple application written in flask. Otherwise just create the file and put the content. So I'll do that, just copy all this content. Okay, I'm going to create the file app by just going to save this file. That's all. Okay, then you need to create this file requirements TXT. Make sure you don't make any mistake. So I'll just copy paste and you need to have your flask and redis. So these are basically the dependencies, the python application dependencies. While we build it, it's going to install these dependencies. Okay, step two is create a docker file and put this content. Let's copy this. So let's create a docker file. Paste this content and let's just go through it quickly. So this base image is here, python work directoriescode. We're setting two environment variable over here. This is for the Flask. Then we have an EPK command here, which is for the Alpine. Alpine is very, very lightweight. The next container. OK, let's not get into that one. The copyrequirements TXT. Two requirements TXT. This will be in the container. It will be in code directory because that's the work directory. So the path of this file will be coderequirements TXT. Then we run the command pip install on this file. It's going to install those dependencies. Expose it on port 5000 copy to dot. That means current working directory content will be copied in the container directorycode. So slashcode in the container will have the current directory content which will be app puarements and then finally Flask run. This command will start the Flask project. So let me save and quit this. Now. We are not going to build this one. We are going to do everything through docker compose file. Created that file. Let's go back. Okay, here also all the steps in the docker files are explained. Next. OK, now we're writing a docker compose file. So that's going to be the name of the compose file and this will be the content. Let's copy this. Now that's the default name of the compose file. It can be different, but then you need to specify the path. If you give a different name, this is the default name. Let's go with the default name. Okay, pretty simple. Compose file. Two containers, web and redisbuild. So we have a docker file. It's going to build that. Run the container and map port 8000 with the port 5000. And then the redis container which runs from the redis image. Let's see when quit this. Whatever I said, same things are written over here. Now just run docker-compose up. So let's do that docker-compose up. Now when I run this command, it's going to read the docker compose file and just execute all the instructions. And everything will be on the foreground. It will be on our shell. Okay, so first it creates a network and then it starts building your container. Okay, all this is running. It's running on the foreground. Now I'm running it on the EC2 instance, I have mapped port 8000 of the host machine. So I'm going to access my EC2 instance on port 8000. And I should see the web page. Okay. So that's my ec2 instance running docker engine. I'm going to go to the security group and I'm going to add a rule here. Basically, I just see all traffic from my IP. Let's save this. Access the instance. Okay, there. Hello world. I have been seen two times. So that's what our container returns on the web page. Okay, let's go back to this Getting Started guide. So we're still here. So as many as time you refresh, it's going to just increment that number. OK, I would say switch to another terminal window and type docker image LS to list the images. And you can do docker inspect. You can do docker-compose down to bring down all the containers. Just kill that container just to control C. It was running on the foreground, right? We do control C and it just brings down everything, stops the container. That's why you should not run it in the foreground. docker-compose down. This command is going to stop and remove the containers. Yes, it will remove the container docker images. Okay, so that's one image. And this redis is another image. Now I did down so it removed the container. Also, you won't be able to see the container after you run the command. Now let's go back to again to this tutorial and it says edit the compose file and add add volume. So let's do that. Okay, there is variable also. Let's do both. Just copy this. Let's go to our Docker compose file. Okay, indentation matters a lot. Now you must have learned all that in Ansible YAML file. So all these are in the same line. And let's save this. This time I'm going to run it in the background. So docker-compose up -d that will run it in the background detached mode. It doesn't print all those things. You can just docker PS or we can docker. Okay, I moved some extra containers. So I just wanted you to have a clean terminal . So docker-compose ps. This is docker-compose ps, almost similar. You also have docker-compose top which will show you the PID, PPID and it will also show the command that these containers are running. Okay, so it has more things over here. It is saying update the application. So open app PY file, make the changes to the text. You will see it gets automatically refreshed. So you can keep experimenting with this. We're going to write a docker compose file for our vprofile project. So by now, you are prepared to containerize a project which is basically two things, docker file and Docker Compose. Whenever you hear the word containerizing or dockerizing, that means you have to write docker file and you have to write docker compose file. Most of the time, docker file is mandatory. But now there are options or alternative to docker Compose as well. But mostly these two things. Build the images docker file and run the containers together which we'll see how to do in the next lecture which is containerizing our project. So wrap this up. Once you're done, clean everything and join me in the next lecture.
