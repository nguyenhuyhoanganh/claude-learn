# Entrypoint and CMD

Hello and welcome. In last lecture, you have seen how to build a docker image, and we have seen many instructions. In this lecture, I am going to talk about two instructions from the docker file, ENTRYPOINT and CMD, because usually there is a confusion in between these two. So I have created a separate lecture for these two instructions which will help you understand and grasp concepts behind command CMD and ENTRYPOINT. So let's go to my docker engine and let's look at three different docker file that will be using CMD and ENTRYPOINT. I'm in the docker engine. I've just logged into it and I have a directory here called entry CMD and with the wrong spelling. Let me correct the spelling here. Okay, there, I fixed it. So I have three directories in this CMD, entry and entry CMD, and each contains a docker file. So let's look at CMD docker file. It's very simple docker file we are using from instruction, and we are using Ubuntu latest image and CMD instruction. So we have seen in the previous lecture, also CMD is the place where we give the binary or the command which will start the container process. So here I'm saying echo hello. So echo is the command shell command, and hello is the argument to that command. So that's how we give command comma argument in double quotes. Now let's build this image. We'll say docker build t. I will call it as printer. I need to go inside that. I'll just give the path CMD docker file, so I'm not in that directory. So I directly give the path of the docker file and it's going to build let me just remove the name. Just give CMD. Okay, yeah, that's building it. Okay, so I should have an image now, docker images, and that's a printer image. Let me clear the screen. Let's do a cat once again to that file and the docker image that we have from this. So I'm just going to say docker run, and I give the image name so we know what this will do. This has the command echo hello. So it's just going to run that and it should be printing the text hello there. It worked. Okay, so that is in CMD, command and argument both. Now, another docker file. Here we have the instruction ENTRYPOINT and we just have the command. We do not have any argument. Let's build this docker build -t printer v2. I'll give this tag as v2. I will give the directory path where I have the docker file and let's build it. Okay, the images built docker images, and we have printer v2. Now, if I run this docker run printer, what do you think will happen? It will run the command echo. That's right, but there's no argument to it. And if you just run empty echo, it's just going to print an empty line there. Right, but if you have it like that. You have command in the ENTRYPOINT and you do not have argument. Then that means a user has to pass the argument. So I'm going to pass your argument, hello. So now it's going to run the command echo hello. So in a docker file, or when you docker, inspect any image and you see ENTRYPOINT have command but does not have any argument. That means the user needs to pass the argument. Okay? Now the third one, entry CMD, docker file. So here you have ENTRYPOINT and CMD together. I think you would have understood. Now the ENTRYPOINT, we have command and CMD. We have the argument. If they are used together, that will be mostly for two purposes. One is what you're seeing right now. ENTRYPOINT will have command and CMD will have argument. That is one purpose of using both together. The second purpose is you maybe need to have some script that executes and initialize something. And you want to run it first so that you give in the ENTRYPOINT. And then your actual command that starts the container process will be in CMD, right? So we can use ENTRYPOINT and CMD together. Now, in this case, let's build it docker build printer v three and the directory. Okay, now let's run it, docker run printer V three. Now we know what's going to happen. It's just going to say echo hello. So just prints hello. You will say, what's the big deal? I mean, we could have given just CMD and come on an argument. Same like our force docker file. But watch this. Now I can now override the argument. So argument is hello. But now I can overwrite it. I can say hi, or maybe I can say hello world, right? And if I do not give any argument, then it will just run, use the default argument. So now you can say if ENTRYPOINT and CMD are used together, CMD will have default argument, which user can override. If this is still confusing, I'm uploading all this into the resource section in this lecture. Download it, build each and every docker file and run it. Just keep one thing in mind, ENTRYPOINT will have higher priority, and then comes CMD. And that is all in this lecture. I will see you in the next one.
