# Multi Stage Dockerfile

Hello and welcome. In this lecture we will be talking about multistage docker file. We will understand what is that and why is it really required. So first let me clone the source code code and I will be mentioning the branch that I have used. You can also use the same branch. Okay, so this is going to specifically clone this docker branch which has the docker files. So you just clone this and you have the directory repository. Get into that. And inside this we have docker files. Now we will be looking at this app directory where we have the application docker file. Let me clear the screen here. Okay, so we have this docker file here, which we use to build our app image. Now I did not cover multi stage in the containerizing project just to remove little overhead. But now we need to talk about this multi stage concept. This docker file has one problem. The problem is it's not completely automated. We have to generate the artifact in this step. It copies the artifact. But this artifact needs to be there in this directory. We need to manually build this artifact. We have to do MV and install which creates this artifact. And then we copy this. How about we put the MVN install command build commands in the Dockerfile, then we don't need to do it manually right now. Sure, we can do that. I can just open this docker file and we can just give the commands. I'm not going to do it, but I'm just showing you. I'm going to run the git clone command and specifically the branch -b vp-rem this branch we have our source code and then amperson amperson CD into the repository. And then we will say MVN install. Now for this we also need to have JDK. So I can say here apt. Just before this you can say, okay, so apt update, then apt install, openjdk I have given ampersand in here two times and then -y Okay, so that should do, right? But the problem with this is this tomcat image will also have the repository that will add to the storage. Then we have MVN install which is going to download lots of dependencies that will be also in the image. We will have target directory that will contain more than just the artifact. So we have many things into this image which is not even required. We just interested in the artifact. If we do build in the same image, then this image size will be huge in GB's, which will be difficult for shipping. It is possible to ship, but the size will be huge and we should have as small footprint as possible for the docker image. So now you will say we can go back to our previous method. We can just build the artifact separately on the host machine and then copy. But then that is manual. So we cannot do this and we cannot go with the manual step. So what is the solution? Yes, it's multi stage docker file. We'll see what is a multi stage docker file. But before we see multi stage docker file, let me tell you, you don't need to build this image. This is really not the real time. Production use cases should not have build and the app image in the same docker file. Or should I say you should not have build artifact and the artifact in the same image. You should not have this. This was just to show you the problem. So I'm not going to save this. I'm going to quit from this file. And I'm going to create a directory here called multistage. And I'll go inside this directory and I will write a docker file that will be multi stage docker file. Okay? So let's write Dockerfile. Now we need maven to build our artifact. And for maven we need open JDK. And there is already an open JDK image on docker hub. We can use that, which is this one. And we are going to use the tag eight. Because we need OpenJDK eight. Now, I'm going to give a name to this image and that we do it like this. I'll give the name as build underscore image. So we say fetch this image and give this docker file name as build image. And in this one we are going to build our artifact. Okay, so three run instruction, app update, app install, maven, clone the source code, go into the repository and MVN install. This will build our artifact. You will say what's the difference, it's the same thing. Hold on, this is our build image. But our app image will be different. So there will be one more instruction now from tomcat 8-jre11 And we are going to remove the default app from that. So that run instruction should remove everything from web apps directory. Now, we want to copy our artifact from this build image to this image. For that we use your copy instruction as usual and we see from equal to this build image. So copy from this image, copy what all the artifact which will be in this directory. We have target and in that we have artifact copied at that location. And then your usual instruction expose on 80, 80 and cmd catalina run on whatever instruction you have, volumes or work directory, anything. So you see now when we build this image, this will be the output image. This image is just to build the artifact. And when we copy from the build image, you have to understand this. Whatever is the work directory for open JDK work directory, it is going to create this repository in that directory. So let's say it's root directory just slash. So the path will be viewprofile repo. And in this image, we don't need to specify the work directory here. We can just give the directory inside that or artifact copy to this location. So let's build it and see how it works. Let's save and quit. And we'll say docker build. I'll say app image v one. Let's hit enter. So first it is doing the build image. There it's running MVN install and you see it's downloading so many dependencies. All that will be in the build image. Okay, that's completed. Let's clear the screen and let's look at docker images. Okay, now check this. This is our output image and its size is 374 MB. This consists the artifact and this one where you do not have any name. This is our build image. Look at its size. If you do both in the same image, then just add one GB plus 374 MB. So we reduced the footprint of our app image. And we have also automated the entire build process. And that is multi stage Dockerfile. So keep this in mind when you work in any project. Whenever there is a build for any image, app image, front end, back end, wherever there is build, you should have a multistage docker file. I will upload this in the repository and also in the resource section. All you need to do is create this docker file in docker engine and build it and see it yourself. That is all in this video. I will see you in the next one.
