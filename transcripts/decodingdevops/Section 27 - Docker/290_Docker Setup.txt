# Docker Setup

Okay, let's launch an ec2 instance and set up docker engine on that. We'll be using Ubuntu 18. Feel free to use your own platform. You can use virtual machines or whatever. It's just going with Ubuntu. Okay? Any place is fine. VM cloud, anything. So launch an Ec2 instance going with open to 18 t two micro and all defaults should be fine. Name it docker engine. Let's create a security group for it docker from Myip. We need to add a few more rules. Actually, one more rule will add all traffic from my IP because we'll be accessing docker engine on different different ports when we run the containers on them. Let's create a key pair and let's launch it. So again, we will be following the official documentation. So docker documentation you have. Get started. Download and install guides. Really nice document. Improved a lot, actually. And then there was a time when it was so difficult to read, but now the documentation is really amazing. So download and install. Okay, so you have docker for Mac, for Windows, for Linux. We're going with Linux. All right. Different platforms. So centos, debian Fedora, Ubuntu. Let's go with Ubuntu 16. All this platform, they should be fine. If you have any older version of docker like docker or docker engine docker IO. So it recommends that you remove those things. Okay, let's go through the installation update, installing some dependencies there, installing the GPG key, adding the docker repository and then installing docker. Okay, let's follow this. Let's log into the docker engine. Okay, I'll just switch to root user and let's take all our commands and execute them. So, installation is completed. We have installed docker ce community edition. There is also docker EE enterprise edition. Okay, now let's check the docker service. There should be a service running with the name docker. That's your docker daemon, And you can use commands to connect to it. Like for example, docker images is going to list all the images which is in your local machine. There is nothing now. Okay? Now if I try to run same command with ubuntu user, currently I'm ubuntu user, who am I, ubuntu, docker images. Okay. Or any docker command for that matter. It's going to throw you this. Permission denied. Okay, so by default, only root user can connect to the docker daemon by CLI. If you want any other user to run docker commands, then you need to add that user in the docker group. There is a group called as docker group. Okay, if you check the docker group file sorry, the group file, you should see the docker group and you can add your user either. You can just do here ubuntu, or you can use the user mod command. Let's do the user mod command. So user mode capital g, docker, the group and the user ubuntu. So one to user will be I'm doing it with ubuntu user only. So I have to do it through sudo command. Okay? Now if you check, Ubuntu should be in the docker group. Okay, you better log out and log in to the Ubuntu user back and then try that docker images command. It's working. Okay. Okay, now let's really validate that. Let's make a test. And actually there is an image created by docker. To test this image. You pull the image run, it just says hello. That's all. Just to validate. Okay, there is some post installation steps for Linux. Check that manage docker as a non root user. Yeah, we already did that. Configure docker to start on boot. So in Ubuntu anyways, if you install a service, it gets enabled automatically. Okay, so there are really a lot of steps. Most of the defaults should be fine. But if you're getting into any issues while you're setting up docker engine on Linux machine, I recommend you go through this. See if you have all this already set or not. There's some troubleshooting steps also there and here at the end you have get started. We'll be doing all this, just showing you in case you need to watch any video from docker directly to understand why. Docker to have much better answers if someone asks you, right? So there's some steps, also some tutorial. We'll be doing all this. So just in case, if you need, you can follow this. Okay, so I am going to run some docker command now, which is going to make a test docker run. Hello world. This basically is an image. So docker run means actually create container for me from this image. So you don't have an image. So what happens? It says, I don't know where is this image. So it will go and find it at docker hub. There is a place, docker hub where all these images are hosted. So it's going to pull that image, download it and then run the container from that. And all this container does is just print this message, that's all and its work is done. This is a very short lived container. Its job is to just run a script that prints a message that's it dead. You can check the running container by running the command docker PS. You should not see any container. But if you do docker ps -a, you should see a dead container. Status is exited. Okay. And you have a container ID there. It was running from this image commands. It is running a command slash hello. That means it's some kind of script, right? You can check that also. I'll show you how you can do that later. It was created 35 seconds ago and its name was modest Query. So if you don't name a container, it will give a name, adjectuve and celebrity name combined together. Okay, so sometimes, yes, you have such short lived containers also. And there are sometimes continuously running containers. Take for example, you want to archive your logs and send it to logging dashboard, maybe using Elk stack or Splunk. Anything to do this activity. It's a shortly done activity, maybe scheduled task. You need to have a container that runs. Does that work? And then it's dead or continuously running container like you're running Apache service or Tomcat service or my SQL service. It should be running continuously. Okay, so in next video we will see lot of docker command docker commands and we'll get comfortable with most of the CLI. So wrap this up and join me in the next video.
