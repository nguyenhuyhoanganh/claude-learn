# Monitoring and Observability Tools

All right. So in this lecture we are going to discuss about the tools that we will be using in this section starting with Prometheus. Grafana. Loki. Node exporter. Cromdale is outdated so we have removed it. Alloy alert manager. And there are few more. Let's understand the use case of these tools. First of all, the Prometheus. So Prometheus is the open source monitoring and alerting toolkit. That is also one more reason why it is so popular because of its open source nature. You can customize it a lot. So this basically collects the metrics. You can think of it as a database in the monitoring. Not exactly database, but it stores all the information, all the metrics. It pulls the metrics. And this process is called as scraping. So system exposes endpoints. And Prometheus is going to go and scrape all the information, all the metrics and store it into its database. It stores all this information into a time series data, which is very helpful in charting graphs and bar charts and etc. the many amazing things that you can do with that. So Prometheus, in order to get the information from Prometheus, you need to use prom QL, its own query language. You can integrate this with the Alert Manager service to send notifications. But Grafana is better and we're going to use Grafana to integrate with Alert Manager. This is ideal for any cloud native application, especially Kubernetes. You can monitor Kubernetes, you can run it on Kubernetes. So Kubernetes, all the pods, containers, all that monitoring comes with it, plus infrastructure monitoring and all the different kinds of monitoring you can achieve with Prometheus. So one example metric we want to check total CPU seconds or better I'll show the metrics or checks from Prometheus UI. So this is Prometheus dashboard. Nothing fancy. If you're thinking about fancy dashboard and all that is in Grafana, this is going to collect and store the metrics. Like if you go to the status and you check the target health, you can see different endpoints that it is collecting the metrics from. You can see its state. You can see last scrape when it's scraped the metrics. So these endpoints will be exposing metrics that Prometheus is going to collect. This is called as scraping. Once we set up this you can understand much better. It has its own query language. We're going to talk about this in detail. But just few examples like there is a metric called up. You can execute this. So it's going to fetch the information like one means it is up, zero means it is down. And you can mention labels here like app is equal to node one. So it shows only the matched label. There are many more things many more queries. We're going to learn that later. Let's talk about the next tool, Grafana. So all that fancy thing which you must have visualized, the charts, graphs, notification alerts, all this is in Grafana. It's an open source dashboard and visualization tool. So it can connect to Prometheus Loki which we're going to talk about. So log management and there are many other data sources that you can mention. So it can fetch all that information from these monitoring and logging tools. And based on that information you can create panels. You can create graphs stuff like that. So features will custom dashboards. There's many things you can customize in this. Seriously I have never seen any such flexible monitoring tool. You get many many many options. Anyways, we'll see that later. So alerts and notification it can send. It can integrate with alert manager and send notifications. You can see different kinds of data and you can visualize in different format graph charts, bar charts, pie diagrams Arms, and mostly mostly for metrics, logs and traces or golden pillars. So you have the monitoring stack. Stack. You have Prometheus, you have Loki, many other things, and think Grafana as the front end of all this. So that's how Grafana looks. You usually access it through the browser but changes customization. You can do it through JSON as well. So I go to dashboard. I see here not team. I see the project. I can see the graph. This is for disk I o. This is for disk space utilization. You can see the graphs. You can see the thresholds, the different kinds of visualization not just graphs. You have things like gauge, pie chart and many other things based on the requirement. You can configure it for your metrics. So this is for metrics. The Prometheus collects the metrics and Grafana access that and can create the charts and send the notifications. So that's what Grafana is in short okay. So that was about metrics and the graphs or the panels. Then we have Loki. We talked about logs, right? So Loki is a log aggregation system. You can push logs to Loki and it can store it efficiently using labels, which you can use to sort find things. And once you integrate it with Grafana, like we integrate Prometheus like that, we can integrate Grafana with Loki. Then you can see the visualization. And of course you can create the alerts notifications also. So again this is for modern day applications. Cloud native containers. Kubernetes. So like Prometheus is for metrics Loki is for logs. Now these are all servers. Grafana. Prometheus Loki. Now let's talk about the client side node exporter. So you need to have this agent in your system which can expose the metrics. So node exporter exposes metrics and Prometheus is going to scrape it. So it's a very lightweight agent which is for system metrics like CPU utilization, disk memory. The infrastructure monitoring we talked about. So this is the agent for that. So it runs on Linux or different kinds of servers exposes different kinds of metrics CPU memory disk network. And this this metrics will be scraped by Prometheus. Now promptly is outdated. But I'm going to still talk about it because many organizations still use this. Prompted to push logs to Loki like node exporter exposes metrics to Prometheus. Prometheus is going to scrape it promptly, is going to push the logs promptly is going to push logs to Loki. So logs from containers or servers, any text log files, it can send it to Loki. So it reads the logs or any standard output, adds labels to it and sends it to Loki. Now Grafana is going to get it from Loki and it will be structured and the structure is important. So we can create the graphs, the charts, the notifications. So this basically is a log forwarder for Loki, but it is outdated very recently. Its support is also stopped. So what do we use alloy now. Alloy is all in one. If you use alloy you don't even need to use node exporter. It's called as the unified agent from Grafana Labs. It can collect metrics. It can collect logs traces, profiles. So all in all in one you can replace it with node. You can also replace the node exporter but current state I'll tell you node exporter is still used a lot. Promptly is phasing out instead of prompt. We are using alloy slowly node exporter will also phase out and alloy will be used for everything. Once again. Remember alloy is an agent that can collect metrics, logs, traces and send it to Loki and Prometheus also. And of course it works with Grafana cloud or even self-hosted. Yes, there's Grafana cloud also, but we're going to set up everything our own. We need to understand the entire infrastructure. So we're going to set up everything. We're not going to use self-hosted, sorry. We're not going to use cloud. We're going to do the self-hosted. So alloy is one agent for all observability data. Then you have alert manager works with Prometheus to handle alerts can send notification to slack, email pagerduty or any kind of webhooks. So let's say the CPU spikes above 6080 or this getting full or X amount of requests are failing for user some HTTP request. So for all this we can create alert in alert manager and we get notifications. So this basically is a delivery system alert delivery system for Prometheus. Now let's take a look at the architecture diagram and understand the setup that we will be doing in this section Okay, so Prometheus, Grafana, Loki, these are the servers and application server or any kind of server database container, pod, Kubernetes, anything. This is what we are monitoring. So alloy or prompt you can read the logs, structure it, label it and send this logs to Loki. So Loki is a log aggregation tool and Loki receives log from alloy or prompt tail. Then we have node exporter that exposes metrics and Prometheus is going to scrape it. So this arrow is wrong just for visualization. Prometheus on the other hand, is going to go to the metrics and scrape it. So this arrow will make more sense. Now node exporter is not the only way to expose the metrics. Node exporter is for the system metrics. Usually developer create their application and exposes Is matrix, which Prometheus can scrape. Now Prometheus and Loki both can be integrated with Grafana, and Grafana can show the dashboard, the panels that we have seen in previous lectures. Sorry, in the same lecture. So this is the whole architecture. These different services runs on different ports or exposes different ports which are default, which can be changed very easily. But as a DevOps engineer, when you're doing the setup or when you're doing automation of this entire setup on Kubernetes or on any kind of server, you should know the port numbers, which will help you decide the firewall rules and accessibility. So let's take a look at the port numbers, the default port numbers of these services. So you see Grafana at the top which we can access its dashboard. We can access on port 3000. You see here that's the Grafana IP. And that's the port number 3000. Now again this is default. We can change it. Prometheus we can access on port 1990, and Grafana is also going to access Prometheus on port 1990 to get all the data so we can create the dashboard panels. So basically Grafana executes Prometheus query from QL to fetch the information. We're going to understand that in the next lecture or next to next lecture. And those queries it's going to run on port 1990. You see there Prometheus. We are accessing here on 1990. Once again it is the default port which we can change. We are going to set an application on an EC2 instance and install node exporter. In this. This node exporter is going to expose the metrics on port 9100. So Prometheus is going to scrape the metrics from this application on port 9100. We will be hosting the application, and the application will be also exposing the metrics that will be on port 5000. Again these are the port. We can change it. But understand this is the access. These services connect each other on ports. As a DevOps engineer, we should be aware about this. Now prompt tail or ally is going to push logs to Loki. Now Loki will be listening on port 3100 and logs will be pushed to it. Unlike Prometheus, Prometheus is going to scrape the metric. Loki is going to receive it. So prompt tail or ally is going to push the logs to Loki, which can be collected by Grafana. So Grafana will connect to Loki on port 3100. Grafana will connect to Prometheus on port 1990. Prometheus is going to scrape the metrics on port 9100, and application metrics also can be scraped that will be defined by the developers. So download this document this image from this lecture resource and take a look. In the next lecture we are going to start the setup of these tools these services. So wrap this up and join me in the next one.
