# Building Grafana Panels and Dashboards

Okay. Finally, the moment which you've been waiting for. Panels. So keep this one handy. This sheet we're going to use these queries. Just make sure you have the right label names over here. Compare and check from your Prometheus what name you have given. Make sure you have that label. So that's how we create panel. Let's go to dashboard. And dashboard we created folders. Folder name is Titan monitoring. In this we have Titan prod. That's the dashboard name. So this belongs to all the production systems. In this we have two rows application and system. So we are going to create first panels for system like CPU utilization. Also make sure on your web server node right on this one SSH to this check if load.sh script is running or not. If it's not running, you need to run it. So I see my web server node siphon f grep load.sh. I see it's running in the background. If you don't see it running, you need to run it. This is the path where the script will be copied. So no hub. No hub. Enter. Enter the name and back. Sorry. Ampersand to run it in the background. Because if you rebooted this machine then this will not be running. So run this command only if you don't see the process running. Run this command and wait for a minimum ten minutes before you continue in this lecture. Okay, so mine is already running. So I'm going to go back. And here I'm going to say now add visualization. Click on that okay. So here you have visualization type time series. Most of the monitoring panels you will see related to our softwares DevOps application. You will see mostly time series. But then we have bar chart also which will be helpful. Gauge different kinds of pie chart that is also useful. So first we're going to start with time series only. The panel that we are creating will be for CPU utilization. So let's give it a title. So I have given CPU utilization and here I have given percentage because the query that we are going to run is going to return it in percentage. And let's get the query. So over here if you scroll down it's not coming down. Okay here. So data source should be here. Prometheus selected. This is the first query we are running. It's just named as a. You can change if you want. We can run like that multiple queries. We'll see that example also. But over here you might have selected here builder. So instead of that select code and paste your query here and say run query. Okay. So that is for last six hours is selected. Click on drop down I'm going to click last five minutes okay. So this is last five minutes. You know every 15 seconds the Prometheus is going to fetch the metric. So if you look at this point, you see here 2119, 15 seconds. If you see here, this is going to be 2119. Now here you see these names. These are called as legends. And you can change that. So if you click here on the Options Legend Auto instead of that you can select custom and you can give your own label also. So I'm giving here this is the variable in curly braces I've given instance. So that returns the value of instance instances a label its value is returned. That's the IP address and the port number. And I've just given hyphen CPU usage. That's just a string. So you can customize based on your requirement. And if you scroll down here there are many options you can tweak. Like for example here in the graph styles in lines I can select this kind of smooth kind of lines or this pattern or this pattern. Let me keep this one default. And so we'll see more options in this in other metrics. Right now we're just going to save this one. So say save dashboard say save okay. Now if you go back to our dashboards go here to your dashboard see here metric. Let's pull this one and drop it in system. Now that's going to only work if you are in the edit. If you're not in the edit. Let me save this and go back to Exit edit. You know now I cannot drag and drop this. That will only work if you are in the edit. So if you go enter edit mode then only you should be able to drag and drop this. Okay, let me save the dashboard once again. Okay, nothing to see. I'll just say update default time range. Okay, so that's one panel. You understood how to create a panel. Now follow me and create all the panels. Then we're going to work on other stuff like notifications okay. Next we're going to create for the available memory. So copy this metric say add visualization in the query a go to the code. Paste that query over here. Run query in the legends shows the all the labels. So I'm just going to go to here options legend Custom. And I'm going to say you know what here I'll just say app instead of instance. Okay. So the value of app which is web zero one system and memory available. So panel name memory available in MB. I usually give this unit which is very essential. So this is visualization is in time series. If we go to gauge. So that shows in the gauge format like we have the speedometer. And this might not make much sense for now. But if we use percentage wise and we can set different thresholds with different colors. So let's tweak the query a little bit. So the second query returns it in percentage. Now you can do this with the same query also, but percentage will make more sense when you are visualizing it. And here I'm going to call it here also percentage. Now you can create the second panel also with the MB also. No problem. I'm going to click here on Transparent Background and let me scroll down here. Or in the standard option you can find you can search for percentage 0 to 100. Yeah. Started making some sense okay. Once again if you scroll down. So here we have the threshold. Let's add the threshold. So different colors. And then you can see it is at what color. So this is the available memory. And let me add some threshold. So I will say 20 for red color. You can change the color from here. And if the available is He's maybe 40. Then it is orange. You know what 5050 is orange. Let me select the orange. Hopefully this is orange. And then more than that let's go here like 70. That will call it as green okay. So like this you can play around with the threshold. Not so good at this. This takes me some time to set this. But this is the total available memory in percentage. You can keep refreshing this. So I just want to say you know, that this is uh, if it's more than 70% it's green. You see that? If it's less than that, it's orange. If it's less than that, it's green. And the base color. Also I'm changing it to dark red. This one is slightly red. So if it's below 20 then it's extremely dangerous like that okay let's save this dashboard. Save okay. Let me go back to dashboards. And again I had not selected. I need to drag and drop in the system. Okay, so now I have two panels in the system and I can bring them together also here. Yeah like that. And I'll say save dashboard. Okay. Next panel I'm going to create for application the HTTP request. So this one traffics and request I'm going to copy this one and visualization. Now let me run this and then talk about it. So just in the code run query. Yeah sorry there run query okay so we have seen this previously HTTP request total total request of HTTP coming to our web server. And we have our two scripts running one for root and one for payment running in the background. If it's not running for you, then switch to Prometheus server, switch to root user and check in the process if you have these scripts running or not. Because if you rebooted, it will not run. So these two scripts that we downloaded over here from our GitHub repository. Now I'm running it from Prometheus. You can run it from Grafana also or from anywhere. We just need to access those URLs. So you can download this script from anywhere. Just make sure you give the right IP address of web server in this script and then just run it. No hub dot slash script, path and ampersand do it. Same for both the script for main and payment. So you get the metrics collected in Prometheus, and you can see that here in the Grafana dashboard. So this is for last five minutes. I'm going to check for last three hours because I have it running for a long time. So that's why I'm seeing it for three hours. You might not see this for long. Now I need to separate these two with proper name. So this green one is for the slash endpoint. And this one is for slash payment. So in Options Legends custom I'm going to give here as endpoint endpoint I'll just say minus or hyphen. And in again curly braces. Here I'm going to say app. So this should return the endpoint. Yeah like that. So last 15 minutes. Keep it time series I'll give the title panel name HTTP request rate and requests per second. We are using the rate function over here lines I'll keep this one. Okay this is good. And now let me save this okay. Again I need to move it. And this one goes in application okay. Let's see few more things quickly. So so far we're just running one query. But we can use multiple queries like for example if you want to see one one of this line that shows the current timestamp and one that shows probably last five minutes one so we can quickly make a comparison from the graph. It's pretty easy. Just add another visualization in the document. Find this. We have two queries over here. The last one. So copy this. Read HTTP request. Total curly brace was missing anyways okay so this is for the root endpoint the main website. So put this here. This is going to be the query a. Once again you can rename this if you want. But I'm going to keep it as it is. Run this. So this is for the current one. Now I want to see the last five minutes or maybe last day. Yesterday is also. So I'm going to add here one more query add query. Now this query be. Now in this I'm going to just mention the same query. But I'm going to offset it by five minutes. So here itself in the document you have the second query. So it's just the same query. But we have given just offset by five minute. I'm going to run this query. So both the metrics are same for the same endpoint. Just one is the older one. So in the legend also make sure you mention that. So now I need to understand which one is which. The query b I'm going to hide. So the yellow one was the query B which was offset. So green one legend I'm going to customize endpoint and app. And I'm just going to say this is current. Now let me just copy this and scroll down here. Go to legend. So this is for the query. Be custom to paste it here. And this one I'm going to say five minutes Older ups five minutes older or whatever. Your query is about 5 minutes or 1 hour or one day. Two days. And I'm gonna say show response here and hide this. All right. This makes more sense. Give a name to the panel. This is given endpoint request now and older. Now this could be better I know that okay. But the idea was to just to show you how you can use two different queries. And this is not only for offset. You can use it for other like one query you can have for slash, the other one can have for slash endpoint sorry slash payment. So this is both for the slash. You can add here more queries for slash payment. So you can have query a, b C, d for in one graph. So play with it. And once you're done save the dashboard. Let's go back to the dashboards. Once again moving this to the applications and bringing both together Other. Okay, this looks good. Okay, one last one. Then we're going to move on to the next lecture for notifications. I want to show the variables which is quite useful. So from the dashboard here itself click on settings. Now I want to have a graph that can show me both the endpoint slash and slash payment. But I should be able to select between them. So I need to have a variable a dynamic variable that shows me the endpoint or not just endpoint. You can have anything. Just take a look first how to add the dynamic variables. So just go to Settings Variables add variable. So name of the variable I'm going to say this is going to be endpoint. And label this endpoint as E capital have given okay. This variable is going to query from Prometheus and select the query. So the query will be executed. The output will be returned. And that output is the variables value. So that is dynamic variable it generates in the runtime. So I'm going to say classic query execute the query and get the output. So that's my query label. Underscore values is the metric. And it returns me HTTP request total and endpoint. Now when you do this and if you scroll down you should already see the value over here. Or you can say run query and you can see the value. So the value of this variable, this variable end point will be this dynamically. So and you can select which value do you want in the panel. So save dashboard here. Okay. Now let's create a panel. Same for HTTP request with a slightly different query. So I'm just going to say add visualization. So get this query dynamic variable panel query. Copy this query. So this is the same query by the way little tweaked HTTP request total. And we're given here the label and endpoint. equal to dollar endpoint. That's the variable name. So run query. So now when you do this you see here you can select the endpoint either payment or route. So this endpoint variable that we have created that this was the label name we have given. The variable name was this endpoint and label is this endpoint. And now in the graph you can select. So like that you can have multiple variables or multiple value to a variable. And you can select that. I'm just going to simply name this panel as dynamic variable panel. It is just for learning dynamic variable. This name doesn't make sense actually in the monitoring it should be HTTP total request count like that. But just for learning purpose we're giving that name. Okay. So go back to your dashboard and put that into application row. And that should be all in this lecture. In the next lecture we're going to talk about notification.
