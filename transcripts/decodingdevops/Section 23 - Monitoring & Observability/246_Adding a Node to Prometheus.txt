# Adding a Node to Prometheus

Welcome. From this lecture onwards, we are going to start exploring Prometheus. So once again, Prometheus is an open source monitoring and alerting toolkit. It collects metrics from systems and application. This process is also called as scraping and it stores this metrics in a time series data. To retrieve these metrics, you need to execute Prom shell. Prom shell is Prometheus query language. Prom itself is a vast subject, but we're going to learn enough of prom shell so we can start monitoring the systems. Create panels and dashboards in Grafana, and send alert notification. But please keep in mind this course does not teach you complete Prom shell. The complete training material is available in Prometheus website. This training program to teach you complete prom shell if you're interested in learning the entire prom shell. So before starting with Prometheus and started learning the prom shell, we need to do few things. We need to generate some load on our system, on our web server, so we can collect information in Prometheus and see by executing the prompt QL queries, I should just say prompt ql because q itself means query. Okay, so open your AWS console and power on Prometheus and web server. So when you power on Prometheus and web server, if you if you're doing it in the next day. So when you power on Prometheus and web server, you should update the security group rules if your IP address changed. So go to security groups web server, edit inbound rule, web server, security group and wherever you see your IP. Just updated my IP, my IP. Now my IP is already updated. Just save the rules. Same do it for Prometheus also. And if you're doing Grafana also today, then update the rules for Grafana as well. So here 22 from my IP 1990 from IP for Prometheus save rules okay. First let's log in to the web server. So copy its public IP. So SSH hyphen I downloads and the key path username ubuntu at the rate the IP address of web server. Okay we are in. You can see the hostname. Also. We did setup last time. So it has the hostname as web server web zero one. Now in this machine you should have this script already there. Do a cat to this script or you can access it from the GitHub repository. You need to execute these two commands not to just this one for now. This is for logs that we'll see later. So only this command you need to copy and paste. So that's going to run this load script that is going to generate random load. So just copy this command and execute it. I already did this. And I should see siphon f grep load.sh. So I already see this script running. And if I run top command oops top command. I see a load average of 3.39 something and you see the some stress commands executing. So it's going to be random load generation. So now CPU is 74%. It's going to spike up and down Q to quit. So this is one thing that you need to do. Then the other thing is we need to send the website request to our web server. So if we go check its public IP we should see the website running. Sorry colon 5000 is running on 5000. So that's the main page. And then we have slash payment. It's another page. Now Prometheus is also going to collect the queries. I'm going to say the access the hits on the website that also it's going to collect that information. So we have two scripts that we are going to execute which will generate random access. So it's going to fake the website access. So we can collect this metrics and see it in Prometheus. This is just for learning purpose. This is not part of Monitoring stack. We're just trying to replicate the real time behavior, how the users access the website and how do we track that information. Okay. So go to github.com slash V profile hyphen project. Switch to monitoring branch. Here you see two scripts. One is main dot. The another one is payment.sh. If you click on this so here there is a curl command that is getting executed. Now this is getting executed randomly. So we can see spikes in the graph. You need to download the script. Change this IP to your website IP. In our case in my case it will be this IP. Sorry, not this IP, the private IP we will be using private IP of web server. So let's do this. Let's run it from Prometheus itself. So SSH to Prometheus. So ssh key path ubuntu at the rate the Prometheus public ip ssh switch to root user. Clear the screen. Run the command wget space and let's copy this. Click on raw. And get this link. Let's download it. Okay. Once you download it open this. And you make sure you give here private IP address. So shift insert sorry just insert. Get into insert mode and update over here the private IP of your web server your private IP. Because if you're running it from Prometheus, because Prometheus and this web server is in the same network. So anywhere if you want to run it from Grafana, Prometheus, Loki, anywhere, if you're running, then just use the private IP. If you're running it from locally, from your computer, then you need to use the public IP of the web server public IP. Okay, so the reason we are running it from Prometheus, because it's just one instance that is up and it's in the same network. So that is the reason we are running it from Prometheus. Otherwise you can run it from anywhere. So at both places mention the Prometheus. Web servers. Private IP, web servers, private IP. Take a look. This is the web server. Private IP and save and quit this file and download the another file which is for payment. This one payments. You need to do the same thing. Payment go to raw. Copy the link paste it. Download it. Open the file website hyphen, payment.sh and update here the private IP of web server at both places. This is for the payment link. Oh, also I need to change here to the port 5000. Make sure the port here is 5000. I'll update this in the script though I think while editing I made the same mistake. It should be 5000 or application is running on 5000. Okay, save and quit and run both the script the same way. No hub dot slash or first we need to make it executable. So see chmod chmod plus x website. Means you execute permission to this and also to payment.sh. And then you need to execute it like this. No hub dot slash. First the script ampersand. So it goes in the background and gets detached from the shell. Same way you run the payment dot, run both the scripts. Okay. And when you run the both this command, you should be able to run syphon f grep and find this website. You should see both the scripts running. Now this will generate some fake access on our website which we can track. Okay. Please make sure to run it for like five ten minutes at least and then only go to the next lecture so you can see some actual data from Prometheus. Okay, so if you go to Prometheus now, get the public IP of Prometheus And it's running on port 1990. Prometheus public IP port, 1990. Okay, first, let's check the status. Target health. So now this is showing only for Prometheus. We are going to add our web server in the Prometheus. So then Prometheus can start monitoring our web server also. And then you should see this information about Pro. Sorry about the web server over here. So in order to do that open this file etc.. Prometheus Prometheus dot. YML open this. You see here some global settings. Scrape intervals 15 seconds. So every 15 seconds Prometheus is going to check its target and collect the metrics. That's called as scraping or scrape interval. Here evaluation period is also 15 seconds. Now evaluation is checking its rules that we define in it every 15 seconds. Now this might be a little confusing. For now you can just leave evaluation rule for now. We'll talk about it in Grafana. So scrape interval every 15 seconds. Then you have settings for alert Manager, which you're going to manage through Grafana itself. So scroll down and here you should see the scrape configs. So config these are the jobs. So there's only one job over here Prometheus. And if you see it's configuration target is localhost on port 9090. So it's monitoring itself. And that is happening because of this configuration labels app Prometheus. So if you check here you see here app Prometheus. And there are some other app some other labels you see that is defined by Prometheus itself. So you can give your own labels. Prometheus is also going to give labels to the metrics. Now we need to add our web server. So what I'm going to do I'm going to copy these ten lines from here. So I'm going to say ten. Why why go at the end I'm going to say P to paste. So job name I'm going to change it with I'm going to say here is web server web server hyphen system. Term. I'll tell you that. Why I have given the name system here. Because we are going to monitor the system metrics. There's a difference. First let's add this one. So static config in the targets we need to give here the private IP and the port number of our web server. So let's check our web server. So there is node exporter service running on web server. We did set that up right remember. So we use this script right. Web node setup.ss script. To set up our web server. In second stage we set up node exporter right there. We download the node exporter extract it right. We set it as a service. So it is running as a service over here in the web server. And this is running on port 9100. Right. 9100 we allowed it node exporter port 9100. Now we can also access it right from the browser. So if we take its public IP node exporter and if we give the port number 9100. We see Prometheus node exporter and the metrics are exposed at this path slash metrics. Okay, this is the web server, not the Prometheus server. This is the node exporter generating and exposing the metrics. And these are the system metrics like the CPU disk memory stuff like that. So take some time and go through this. These are different metrics. Lot of metrics. See if something makes sense like CPU memory disk will quickly make sense. But there are many more metrics. So these are system metrics not the application metrics. We are also generating application metrics on port 5000. Right. Our application is running on port 5000. And through Python flask application we are exposing metrics for our Python flask application also. So both of these metrics we need to monitor from Prometheus. So that's what we have to do here. We have to update this private IP. Let's get the private IP of web server now. Prometheus and web server are in the same network, so we're giving private IP mostly. That will be the case. There will be always in the same network. So this is the system matrix. Let me change here the label to. I'll give label as app zero one system. Now we can give many more labels. Let's just keep one for now. Now that is going to monitor the system metrics the application metrics. Also we are going to monitor. So again copy these lines. Once again you can do ten by one and then paste. Paste it over there. So job name or here I'm going to change it to web server hyphen. App stat like application statistics like that. And here this runs on port 5000. 5000. And here the private IP again of our web server. App label this I'm going to change it to web zero one hyphen app stat okay. Now label helps us differentiate between metrics and find and get the right information. You need to learn that in from shell. Let's save this one. You know, once again just take a look at this file. So you make any mistake you can just check it. It's in YAML format. So make sure the space and everything is exactly same. So we added these two settings web server system metrics and web zero and web server abstract. These two. That is the name of the job. The label is web zero one app stat and web zero one system. After doing this you need to restart Prometheus service. Prometheus. Okay, I think there are some changes that happen. We need to do systemctl daemon reload. I don't remember making any change to the system level. Systemctl daemon. Reload. And then let's restart the Prometheus service. Okay, wait for a minute or so. And after that, come to Prometheus server port 9090. Go to targets and click on the status target health. Okay. There is one mistake that I did. So the metric is not exposed on port 9090. That is on port 90 100. Yeah 9100. That's always confusing. So I'm going to update the file once again. So where we are monitoring the node right. The web server there you change the port number 9100. Do not change it for Prometheus that keep as it is for our web server. Change the port to 9100 for the web server system. Metrics for web server. Web start that is port 5000. And then save and quit. Restart Prometheus service. Wait for some time and then you can refresh this and few seconds. You should be able to see this as up. Okay. So that's what we wanted to do in this lecture. So once you have this up and running you can join me in the next lecture. We're going to run some from Kql queries and get more information about these systems. If you see some problem restarting Prometheus service or you see something here down some issue, some error, make sure you check your configuration. It's usually the configuration file. Mistake the YAML file. It's in YAML structure. So make sure the structure is correct as per YAML format. Check all the port number the IP addresses. Make sure everything is correct. Restart Prometheus service. It should be working now. Also take care of the security group. Rules 9100 5000 security group for the web server. You can open it for anywhere if things are not working. And once you get this, you can join me in the next lecture. See you in another one!
