# Understanding PromQL (Prometheus Query Language)

Hello and welcome. In this lecture we are going to learn from Prometheus Query Language. So open your Prometheus server on port 1990 from browser. Check the status of your instances. So that's Prometheus, that web server app stat. That's web server system. Now all these metrics expose the endpoint. Sorry, all these endpoints expose the metrics which Prometheus has collected and stored it. And we can access those metrics by running query. So click on query. One simple example is up is a metric. If you just run it you can say execute and you will see the result of that query. Yes, that's a query and that's the result. So here up label app Prometheus. That's one. Its output is one and that's an integer. Next one you see app web zero one hyphen abstract. You see that in double quotes. That's a string. Now this output that is returned is called as instant vector. It will have values like one zero. So if it's up, you see here one. If it's down, you see here zero. I have created a sheet over here. You can download it from the lecture resource over here. These are the queries in the in this first column. And the column B shows what it returns when you execute these queries. So first query that we executed up returned an instant vector. But if you give a range like five that means five minutes in square bracket that is going to return range vector. So we say up square bracket we say 5MM is for a minute, D is for days, H is for hours, Ms. is for milliseconds. So we're going to go here five M and execute this. So you see the entire range over here. Now what. This is the one represents here the value that it is up at this time. This is time now. Why so many times? Because we are taking a range of five minutes and Prometheus scrapes every 15 seconds. So every 15 seconds for five minutes, that will be 20 values that we are getting now. It doesn't make sense to read this, but when you create a graph by using Grafana, you can see proper timeline. When was this term up? When it was down, at what time? Now this time is in Unix format. So this timestamp is basically the number of seconds that has completed from January 1st, 1970. That's Unix timestamp. Now, I know it's slightly weird, but it helps store the data properly in proper structure. And when you use Grafana it will properly show you the current timestamp. Everything. So first was instant vector, then it was range vector. These are the different types of data. Now I'm showing you a count function count parenthesis. And in this I have given the metric up. It's going to basically just count how many of those values it has returned, and that is three. But with this it also returns the label. Now there are no labels over here, but it returns this empty curly braces. If you want it in proper scalar format, you need to give it in scalar function scalar parenthesis, scalar parenthesis count up that also returns three, but it just returns three. Doesn't return any labels. Now again, doesn't make too much sense. Now we are seeing three with count function, also with scalar also. But when we use it in Grafana this will make more sense. So for now just have patience and just execute all these queries and check the result. First we check the data types instant vector, range, vector, scalar and string. The string is basically the text. And we also have arithmetic operators like plus minus multiplication division modulus. So for example, this metric node memory mem totals bytes. It returns in the bytes. So we only have this for the web system web zero one system because that is created by node metrics. Node metrics generate the system metrics. System metrics includes this memory information. So this is the total memory in bytes. Now we also have a metric to check the free bytes. Free memory bytes. And we have. If we subtract this we'll get the available memory. So if we put here minus and node memory free bytes and we execute it we get here the we get here the output that shows us the available memory. So from total we are subtracting free we get the available. Now like this I have given here a few more examples. How to get free disk utilization. So we divide the available bytes of the file system with the total byte size of the file system, we get an output. We multiply that with 100, we get the output in percentage. Again, the reason I'm showing you all this is so when you create metric panels in Grafana, you will not then be confused with what is this division multiplication additions subtractions. So these are arithmetic operators. And this another sheet I have more examples over here. So just check it from the lecture resource. It is useful. Okay let's check the comparison operator. Like equal to greater than less than or less than equal to. So we sing up. And if its value is one then only return the output. So execute. We see all the output. We say if up value is zero, show me all the nodes that means which are down right up zero equal to zero. Nothing because all the nodes are up. Let's check another metric node node node node one. Execute this. So let's say we want to check all the system whose load average is greater than three or greater than two, we'll say. Let's give a space here. Execute okay. For few minutes. For few seconds. It did not show because we have a script running in our web node. Right. Which is sometime pushing the load. Sometime it's bringing it down. So it was not showing it, then executed a few times. After few seconds it started showing it. If we say show me which is which has load average greater than three, we might not get anything right now. Okay. Likewise up not equal to zero. That means this query is going to show all the node that are up okay. Now this one let's take this one okay. Let's understand this one. So give me any up metric. Its value is equal to one. And this is the ending operation. And the load should be also greater than two. So ending means both the values should return. No should match. Then only we get the output. We say here load is greater than three. We might not get anything because. Oh, I think now node value. Load value is greater than three say four. Yeah. So load of our system is going up and down. So that's why we see this. So the node was up but its value load value was less than four. So it did not return any output. Now we can use or operator. Now show us the node that is up or any node. Its value is greater than four. So we might get one or all three right. So these are called as logical operator and or okay. So first we have seen the data types arithmetic operators comparison operators and logical operators. Once again execute all this. Look at the output. Learn by doing it. And not just by watching. By just watching you'll just get confused. And this will all make more sense when we go into Grafana. Okay. Let's take example of offset. Offset is showing the history. So we just execute the metric. It shows the current value. But if you want to see the previous values like what happened like last five minutes. So let's say we want to see node memory free bytes. So it's going to show me the current one. But if I'm going to say offset five m that means last five minutes. Sorry five minutes back. What was the free memory bytes. So it might show us a different value. So five minutes. M is for minute. Then we have days. We have hours. We have seconds. We have milliseconds. So based on your requirement you can see that. So memory six minutes ago up status five minutes ago. So what we want to do again that we'll understand in Grafana that what what we want to see based on that we're going to write the query. Okay. So so far we're checking the metrics from the node exporter. We have also the Python flask application Titan that is also exposing metrics that is mostly related to HTTP. So let's search for http http request total. Let's execute this okay. So here it says web zero one app start endpoint slash which is the main. There are total 8900 requests that came. And for payment. This is only one I think there's something wrong with the script that we are running because this should also have spiked. But but before that, let me just take the public IP of the web server and access it on port 5000 slash payment. Right. So that page is up. No problem. Let's check once again executing it few more times. So I should see the result in the query. The Prometheus query. Let's wait for a few seconds 15 seconds it takes to scrape. Right? So we need to wait at least 15 seconds. Okay, I see now that five total. Right. But the script that we are running I think that is not working. So let me switch to Prometheus and check take a look at that script. Okay. PS hyphen f group was web something. Okay. It's running. Let me kill it. Kill 2363. Okay. It's here. The script is here. Let me take a look. Website payment dot. SH. Everything looks good. Let's do a curl manually. Yeah, it's returning the HTML page. Let me run the script once again. Dot slash web payment and person. Okay, now it is increasing. Something wrong? Why was that script not running? I did not make any change. It was same as it is. Just killed the process and started again. And I'm going to kill this process for which checks the main page and make sure there you know at least similar range. 2331. And after few minutes I'm going to run it once again. So when I get into Grafana I should show you graphs matching going up and down. Right. So to show that visual representation I'm just trying to make sure that they are in thousands. Okay. Again now back here so we can see get me a range of five minutes. What happened in last five minutes. And that was in thousands. You know take a look over here for payment. It was was one. Then at this time it was there were two requests then five requests for this time, then eight, then increased to 35. Right. So this shows the range vector. So there are many interesting function that Frankl provides. One of them is rate. We'll be using this in Grafana also. So put this all in rate and execute this. So this shows the request per second. So for this one request per second was around like three seconds sorry three. And for slash payment it is less. We know that you know it was not working. It was not running. So like that there are many helpful functions that we will be using in Grafana. Let's take a look. So we have I rate that shows instant rate network for network throughput helpful for network throughput. Actually not so much for the HTTP total request. There is increase function that can show, you know increase in last. Here it is like one hour. You can just put five minutes also and see. So last one minute or sorry one hour. What was the increase in this total request sum that will just sum add everything. So here this basically means, you know, when we run up we get three values, right. So sum is just going to add them three. So if you do this you just get the value three. And by job here means this is for the label. So you know Prometheus configuration we have mentioned jobs or three jobs. So let's run this okay. So for the job Prometheus like if you have multiple configuration in one job like in one like Prometheus job, we just have one configuration or here also we have one configuration. Let me show you. So when we open the Prometheus configuration file. So this job right web server app start it has just one config. Just one target mentioned. We can have multiple targets also especially when we group things together. So let's say here we have ten targets. So job web server app start has ten target. Now out of that ten target how many are up. That's exactly the query we have executed. Right now we just have one target per group. So it is showing its status that it's just up. It's just one target that is up here, one target here, up here, one target up like that. Then we have average. We have delta. The difference. And then there is a huge list of the functions in the another sheet I have given here a lot of other functions. Minimum maximum average counting top k top three. So this command is going to return the top three load values. Bottom k bottom three load values. Count values is going to count based on the labels. So take a look at this list. You can execute. You should actually execute these queries. And also make sure you look at the description while you're executing the query. Some might make sense. Some might not make sense. Once again, once we put this in Grafana, all will start making sense. So there are many functions. There's many. Operator. We cannot just execute all the queries. Understand the entire prompt QL. That's not going to be possible in this lecture or in this course. But this much will be enough for you. All the sheet if you execute like look at the output. Then when you get into Grafana you will not be confused about the queries. In any case, you can use ChatGPT or any other AI tool to give you a query that you're looking for. That's what I do. I have a requirement. I need this kind of graph or bar chart or pie chart. So I just open ChatGPT. I just mentioned my requirement, get the query, put it there, and after some tinkering I get what I want. So for now, take some time, execute these queries, get your results and be prepared for next lectures from QL. Query is not over. It just got started. We're going to have this throughout the course. Sorry throughout this section of monitoring and observability. But that should be all in this lecture for now. I'll see you in the next one.
