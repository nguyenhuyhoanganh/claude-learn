# Connecting Grafana & Prometheus

Hello and welcome. In this lecture we are going to get started with Grafana. So once again what is Grafana. Well it's a visualization tool we have executed from QL and we see the output is not really readable. It was not designed to Prometheus store metrics in structured format, which Grafana can read and show you visualization dashboards, graphs and you can set alert notifications. Also, there are numerous settings in Grafana to create your own dashboards. So based on the requirement of your project, your use cases, you can create your own dashboards from where you can monitor your entire infrastructure. In fact, the NOC team or SOC team who is going to monitor 24 by seven will usually have these on their screens. So let's power on Grafana Grafana EC2 instance, and let's do some customization first before we get started and create the dashboard. We're also going to take a look at the Prometheus query that we will be executing from Grafana. And once we are set with everything, we'll start creating panels. Okay. So let's go to EC2. Let's power on Grafana. If you are doing it after one day or few days, make sure you update the security group rule of Grafana because we allow 3000 from our IP. IP could change. So just to refresh this once again my IP save the rules, wait for a few minutes after powering on Grafana and then you can access it. It's dashboard from the browser on port 3000. Okay, so the login user was admin and we did reset the password. And I told you to remember the password. Okay. So the first thing is to connect Grafana with Prometheus server. And for that you click here on connections and click on data sources and say Add Data Source. So you see Grafana, you can select data source as Prometheus, Grafana, InfluxDB, Loki. Elasticsearch. Jaeger. Tempo. Zipkin. There's a huge list. SQL cloud we have Azure CloudWatch. This is the monitoring service. Amazon AWS Monitoring Service CloudWatch. You can fetch the data from there. Also from Grafana Cloud. So we have a cloud for Grafana. So instead of you setting up your own Grafana, you can access Grafana Cloud. You know you pay the subscription and start using it. So there's a huge list. It's really very flexible. You can use it not only with Prometheus but many other data source. But for now we're going to select Prometheus. You can give your own name is helpful when you have multiple Prometheus servers from where you're fetching data on your Grafana dashboard. Connection. This is going to be the private IP, because Grafana and Prometheus are in the same network, and usually that will be the case. So http colon slash slash get the private IP of Prometheus private IP, not the public IP. Because we shut down the power on public IP will change. So use private IP. So HTTP colon slash slash private IP of Prometheus colon port 1990. We see it's listening on port 1990. Now just make sure Prometheus Security Group allows 1990 from Grafana security Group. We did this already. If you have some issues with that just allow 1990 from anywhere on Prometheus Security Group. But the best and secure way is to allow 1990 from Grafana security Group. So this is the Prometheus Security Group 1990 allowed from Grafana Security Group. This will work. This will definitely work. But in case if you have mentioned a wrong security group, there might be some issues. So just try with 1990 from anywhere. You know, just create a new rule 1990 from anywhere. Only if you're having problem. Otherwise just keep it from Grafana security Group. Now we do not have any authentication with on Prometheus. We just access and we get the page. But in real time that won't be the case. There will be authentication and you need to select what kind of authentication is just basic authentication username or password. Or it will be OAuth token based. So right now we don't have any authentication. So we're just saying no authentication. You simply scroll down and you just say save and test. So I get a successfully successfully queried Prometheus API. And next you can start. So if you get any error, check the security group, check the port number, the IP address. Mostly the issue is somewhere here or the security group of Prometheus. So. But if you get this tested then it's already saved. The connection is already saved. Now all you can do is you can you run your own test also. So just once this is successful, just click once again on data sources. You should see Prometheus data Source. Click on explore and you see here the Prometheus is selected as the data source. This is where you run the query. You see here a this is the A query. You can have multiple query. It's the name does not matter. You can change that name also. So usually it will be like this. So you can select on the drop down. So one is builder and the other one is code. Usually we use code that's much better. But you can find it from here also. So let's say HTTP request total we select here select label and select the endpoint. We'll select the value as payment. You see showing all that. Let me refresh this run query. So you see the graph. We're going to build these kind of panels. These are called as panels. So different kinds of panel. This is just a graph based. And this was just a test to see if things are working or not. I'm going to come back to from QL once again in few minutes. Let's click on explain here. Once I click on explain you get the actual query. You see this. Let me copy that and let's go to code. Okay. This is builder. This is code. So you see it's already there in the code. And instead of this I'm going to remove the payment and then run the query once again. Or I can add one more query here. Let's go click on code. Let me copy this and let's put here payment. And let's run the query. So if you run the query you should see two lines over here in the graph. And you can see the details about this is for the green line is for the endpoint route and the yellow line. This is for payment. You can see all the other labels and you can see it in different format. This is lines. You can see it in bar format. Also these are the bars points stacked lines stacked bars. There are many other options. You can customize it heavily. So we'll come back to this Prometheus query from QL. But before that let's connect slack as a notification system as a contact point in our Grafana, so we can use it later to send notifications. So wrap this up and join me in the next lecture.
