# Loki and Web Server Setup

All right. So we have set up Grafana and Prometheus. Now it's time to set up Loki and the web application server. Let's take a look at the Loki setup script. Loki is our log aggregation server, so it's setup is similar to Prometheus. We download the binary, we extract it, we set up the systemd file and start and enable Loki. So Loki version. So some variables that we are setting first is Loki version the download URL. This is from GitHub. And once we download we get this zip file with this name. When you extract, you get a binary file with this name. So setting all this as a variable which we can use in our script. It's a temporary folder that we are setting where we are going to download all this configuration directory. So HTC Loki is the configuration directory. Data will be stored at var lib. Loki binaries will be in usr local bin. And this is the service file which will help us enable systemctl service. So again similar process we set the directory. We go into that directory. We're installing unzip first, then we download Loki. We unzip it, we move the binary to usr local bin with the name Loki. We give this binary file executable permission and then you can run the Loki command. Loki hyphen. Hyphen version should tell you the version of Loki. So that's the binary. We need to run it through systemctl. So we need to set up Loki user. We need to. Yeah. First this the group, we create the Loki group. Then we create the Loki user. Add it in Loki Group, we're creating two folders in the data directory chunks and rules. We give data directory ownership of Loki User and Loki group. We create the configuration folder, etc.. Loki. And in that folder we create this file config.yml. This will be the Loki's configuration. You don't need to understand anything for now, except few things like it's going to be running on port 3100. We're going to take a look at Loki. When we do log aggregation in detail, we'll take a look at this configuration. So that's the configuration file of Loki, and the configuration directory also should have ownership of Loki user and Loki group. Then we set up the systemctl file. So Execstart. You see here is the binary path and the configuration path mentioned over here. Once we do this we are allowing firewall. Rule 3100 is the Ubuntu Firewall. UFW allow 3100 systemctl daemon reload, enable and start Loki. Now once you start Loki, after a few minutes, if you run this command, you should get output as ready. If you get that Loki is running and this command should give you the metrics for Loki. We're dropping this query from here and we'll see the output. So once we run the script, we can see the output. So first let's launch the EC2 instance. I'm going to say launch instance naming it as Loki Server Ubuntu. Make sure the instance type is T2 micro same key pair network setting edit create the security group for Loki. So Loki, hyphen, SG and same name in the description I'm giving. And the security group rule 22 from my IP so we can ssh. So Loki is going to run on port 3100. We need to allow this port from our application or web server. So it can push logs. Alloy should be able to push the log to Loki on port 3100. So 3100 allowed from our web server and 3100 allowed from Grafana. So we do not have our web server yet. So for now I'm just going to say 3100 allowed from anywhere. Definitely not production use case. We are going to change that later anyways, but for now just keep 3100 allowed from anywhere and say launch instance once you see Loki running as such to it as such, hyphen I key path one two at the rate the public IP of Loki. Switch to root user sudo hyphen I oops. Sudo hyphen I. Clear the screen and let's download the setup script. So go to GitHub to our project repository. Switch to monitoring branch locate setup script. Click on raw. Get this link wget and paste this link. Shift insert. Make this executable. And let's run this. So here it is going to wait for one 20s that much I have seen, it takes time to make the service ready but it could take little longer also. Anyways, let's wait and see. Okay, it completed but it still says Ingestor is not ready. Let's take a look. So let's take a look at that command. Let's run it once again. And now it's ready. It takes more than one 20s. Maybe you can set it for one 80s. We can also check Loki version. Loki hyphen. Hyphen version. Okay, so here we can confirm Loki is set up and running. We can shut down this EC2 instance. Let's move on to the web application server now. Okay. So once again come to GitHub. Go to monitoring branch. Here you have a script web node setup dot ssh. Now this is pretty long. But most of the things are not relevant to monitoring stack here except the node exporter and alloy. The other things are just to generate metrics and logs. So we can monitor. We can do log aggregation like I have set up a Titan application just to name that I have given. I just fetched a template from 2.com, and I am hosting it on a Python flask app, which is also generating metrics. So application metrics also we have we also have the system metrics through node exporter. Then we are running some script that is going to generate log files. So alloy can push these logs to Loki. So let's take a look at the script. So this is a log script. Please do not worry. Most of the things are just relevant to generating metrics and logs. Only node, exporter and alloy is the thing that will be relevant to the monitoring stack. So first basic system setup. Let's take a look. So we are setting up hostname over here apt update apt upgrade. We install zip, unzip and stress a tool that we are going to use to stress our system. So we get some metrics. Now here we are installing node exporter Prometheus node exporter and same story. We are going to download the binary, unzip it, set up the service file for it system file with the user. Of course the user is going to be Prometheus. Group is Prometheus var lib node is the folder where it is going to store the data. So that is node exporter exec. Start just running. This binary will start exposing the metrics the system metrics CPU memory disk. So we start and enable node. The service name will be node. Node because that is the name of our service file node. Okay. That sets up node exporter. Again you can check that in the Prometheus download section. You have here node exporter Linux amd64. So we just extract this binary. It has just this binary. That's it. The zip file will have just this binary. We are running this binary as a service. Simple. So that's node exporter. Now this Titan app, the name that I have given because the template of 2.com is Titan. This I'm hosting it as a Python flask application. So only if you're interested in this you can take a look. Otherwise you can simply skip it in real time. You will have real applications that is going to generate metrics and logs. All right. But if you're interested. So we install Python Python three which will be installed in ubuntu 24 Python virtual environment. We clone our repository code of profile project repository, and we switch to monitoring branch after we switch to monitoring branch. And this will have a folder called Titan here. So we create a folder of Titan and move this Titan folder all the content to opt Titan folder. Here I can show you better. So this is the Titan folder. So in this we have this template from two play.com. And one more HTML file that I created is a payment link. Nothing fancy, just the page and the JavaScript CSS from play.com. This is the main file App.py. If you're interested in Python flask application, you can take a look over here. This is to create logs. This is also going to generate logs and some metrics. So we're hosting the two Play.com template here and the payment HTML file here. This is the routing rule and metrics. This is where we exposes the metrics of the application. And we run it on port 5000. So that's what we are doing in this script. We are hosting it as a Python application. We activate the Python virtual environment, install some dependencies and just execute. Permission to this. Now we can simply run this Python three app.py. We can run this, but we run everything as a service because we are DevOps, we should manage everything as a service. So again same thing. We are creating a system file for Python application. That's the command that will be running when we run systemctl start Titan. This is the path it is going to run. So again it's not much related to the monitoring stack just some application. Now load generation script we are running two. So we're going to run two script over here. One is load dot and another is generate multi logs. This is going to use the stress command and stress our system. So we get some metrics on CPU and even disk IO. And this script is going to generate logs in var log Titan folder. So we download this script and we run it. If you are interested you can take a look at this script. So this is load dot s h. So a script that runs a while loop that runs continuously. While true. So this is the stress command. It is running. And it's going to be doing it in random duration. So we can see spikes going up and down. And CPU worker. The number of CPUs that needs to stress will be also random. So we see the proper graph that we can use it as a metric. And then the generate multi logs. So it's going to create this folder var log Titan. And store some fake logs into these three files. App one dot log app two dot log app three dot log. So do not panic. I have created this script by using GitHub copilot. Of course I made many changes, but the ultimate aim was to, you know, have some log files which we can fetch and display on Grafana. So once again not part of monitoring stack, but you need to have some log files, some load, so we can scrape them and visualize it in Grafana. Okay. Now here we are configuring an alloy. Alloy is the agent like we have node exporter. As an agent that exposes metrics. Alloy can expose metrics and push logs to Loki. Installation is pretty simple, just apt install alloy. We need to of course set the alloy repository. This. All these commands are from the alloy documentation directly. Alloy configuration will be at this path etc.. Alloy config dot alloy. And we're going to take a look at this configuration later when we do log aggregation. But few things you need to know because you need to change these IP addresses. The Prometheus IP you need to mention over here. This is where alloy is going to push the metrics to Prometheus. And our flask app is exposing metrics over here. Slash metrics on port 5000. So this you can leave as it is. No need to change anything over here, because locally itself, it is going to get the metrics from the Titan Flask app and expose it at slash metrics. This for default metrics here. Actually we should have the port number 9100 the node exporter. But anyways we'll change that later. For the log it's going to push to the Loki. So mention Loki Private IP over here. We're going to download the script and mention these IP addresses. So once we download this script in the EC2 instance we'll update this and then we'll run this. So alloy also has a web page which you can access and see the health of logs and metrics whether it's pushing or not. And by default it runs on port 123, four, five. And by default it listens only on local. So if you want to access it from the browser on EC2 instance, you need to mention this setting in etc.. Default alloy configuration file. You need to add this argument server listen address 0.0.0. That means listens on any IP on port one, two, three, four, five. And then we simply restart alloy. So once again if we check alloy we are just installing alloy from the repository. APT install alloy. But we are making the configuration changes with these two files and restart alloy. So that will set alloy and then the firewall. Rule 22 for my IP 9100. This is for the node exporter which is going to expose the metrics 3100 for Loki is actually not required because we are going to push it to Loki, not the other way around. So this is not really required. Port 5000. The Titan app is going to listen on port 5000 and alloy port 12345. Yeah, I know developer should have got little more creative with the port number. But anyways, let's keep the default. So once again we are going to set up the application server on EC2. So there will be Titan app, there will be node exporter and there will be alloy. And we are running some script in the background to generate logs and generate fake load. So let's set this up. So launch instance. Let's name it as simply web zero one ubuntu 24 T2 micro instance type same key pair security group. Click on edit naming it as web hyphen SG 22 from my IP so we can SSH. It's running the application on port 5000. So 5000 from my IP. And Prometheus is going to scrape it. So 5000 allowed from Prometheus. So Prometheus Security Group Prometheus Security group. And the description Prometheus not scaring scraping I know it's scaring now, but don't worry. We're going to fix that by learning it. By knowing it. Okay then. Node exporter port 9100 so Prometheus can scrape it 9100. Zero. Now we also want to see it from the browser. So we're going to do my IP. And then once again 9100 from Prometheus source Prometheus security group Prometheus keyring. Sorry, scraping so low key. We don't need to do anything because low key we are going to push the logs to low key right on port 3100. We push it to low key so we don't need to add anything over here. This should be it. Let's launch it. Okay. It's in running status. Now let me copy the public IP and ssh ssh hyphen a key path ubuntu at the rate the public IP of web server. Let's switch to root user. Clear the screen. Let's download the setup script. So go to monitoring branch web node setup script raw. Copy the endpoint. Wget and the URL shift insert. Let's make it executable. Now, before we run it. We need to change the IPS. So go to the alloy section. Right. This is where we are creating at this line number. Line number could change when I make the changes. So Prometheus endpoint here you need to give the private IP of Prometheus not the public IP. So go to insert mode and remove this section which says Prometheus IP. Oh get the private IP of Prometheus. So go to instances as the Prometheus. It's off. But we should have the private IP as the private IP of Prometheus. Okay, shift insert to paste. Make sure you make no mistakes. HTTP colon slash slash private IP colon 1990. Okay, let's get the low key private IP. Also go to low key. Make sure you select low key here. Get the private IP of low key. Sometimes you select the wrong instance, so make sure you selected correct instance. Low key private IP. Let's come down. We should find low key somewhere here. Aha. Found it. Oh. You had one job. Yes. You're right Loki. We'll do our job. We'll change this Loki IP string to the actual Loki IP. Make sure the syntax is correct. And save and quit this file and then run it. Okay, it took some time, but it got completed successfully. Let's take a look from the browser. So, web zero one IP, you exit on port 5000 and you should be able to see the Titan app. This is the template I got from two 8.com which I have hosted on Python flask app. And also one more page is slash payment. And also we are exposing Metrics. So 5000 slash metrics. This is the Titan application metrics, not the system metrics. You see here. This these metrics all related to HTTP. The node exporter metrics will be on port 9100 slash metrics. Yeah. This is the metrics exposed by the node exporter. So you can find here CPU disk all the system related metrics that Prometheus is going to scrape. Now also if you run siphon if grep load.sh you should see a script running in the background. This is running stress command. So if you run top command we should be able to see some load. Average CPU utilization is 50%. You see it's down. It's going to spike up and down. Now it's dead I think. So it's going to run the stress command and stress the load Q to quit from here, you can take a look for some time. And then we also have a script that generates logs if you generate. Okay. So that's the script. And this should be generating logs in var log log Titan. See here. App one log two log three log. Now these logs access error and info. This is created by the Titan Flask app. Now when we power off this this script are going to be dead. They're not going to come up when we power on again. We have to run them again later. But all those nice things in the later lecture, we are done here, we are going to shut down this instance of zero one instance state stop instance stop. Now remember, when you power on these instances it's public IP will change. And also your public IP might also change. So later you need to update the security group. Also we'll talk about all that in the next lecture. So wrap this up and join me in the next one.
