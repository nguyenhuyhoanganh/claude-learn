# Setting Up the Monitoring Environment

Welcome. In this lecture we are going to set up Prometheus, Grafana, Loki and an application server. All these four will be on an AWS EC2 instances. All these tools comes as a binary based on your operating system. You can download and run them, but that is not the right way to run in real time. In real time, we should run as a service. So Prometheus as a service which we can manage through systemctl command. In Linux, we use systemctl to manage the service. Right now, if you have followed this course from the beginning, you know what I'm talking about. And Linux lectures in Vagrant and Linux server section we set up Tomcat service. Similarly we are going to set up here Prometheus, Grafana, Loki and also alloy and Node Exporter. On the application server our application will be running Python flask application which is going to generate metrics and logs that will be pushing to these servers. First we are we're going to set up Grafana. Second. Prometheus. Third the application server. Fourth Loki. Now, in real time, the sequence does not matter. We are going through this sequence because we are just learning. We need to make sure everything is accessible, but in real time you will have definitely first your applications, your systems which are exposing metrics and generating logs. And then you set up the monitoring stack, the logging stack. So the sequence really does not matter. But once again we're going to first set up Grafana Prometheus node and then Loki. So let's get started. So I have logged into my AWS account. I'm in North California region over here. I'm going to set up these EC2 instances for EC2 instances. For now we have bash scripts to set up these services. So go to github.com slash slash v profile hyphen project switch to monitoring branch. First let's take a look at the Grafana setup. SH script. Nothing fancy. In this script we are setting some variable Grafana version. The download URL. This is going to be a Debian package because we're going to set or run this on ubuntu machine. So ubuntu package, Debian package. That's the URL from where it's going to download Grafana. And that's the Grafana version that we have mentioned here, the Debian file name and the service name. So we run apt update, apt upgrade, install a few dependencies, download Grafana Debian package and install it with dpkg hyphen I command and then we enable and start the Grafana service. Once we have this installed, we can access it on port 3000. The default username and password is admin, so click on this raw button over here. Continue to this website and keep it like this. Let's launch an EC2 instance. So launch instance naming it as Grafana hyphen Server ubuntu. Ubuntu 24. We are going with the free version. Scroll down. Make sure the instance type is T2 micro. Let's create a key pair. Now we'll use one key for all our instances. This is definitely not production style. You should have different login keys, but for our entire monitoring stack we'll just use one key for convenience while we're learning. So create new key pair. I'm going to give it name as Monit stack key PEM format create key pair. Now should download the private key now network settings. Click on edit and Create security group. Name it as Grafana Hyphen SG. Make sure you give some meaningful name because we will need to add this security group information in Prometheus. Also 22 from my IP I'm doing if your IP. If your public IP is continuously changing. You can keep it anywhere or you can keep updating. This one rule that we need to add is for 3000 port so we can access it from the browser. So I'm going to say from my IP. Take a look at that port number diagram. Grafana is going to listen on port 3000. We're going to exit from the browser. So that is the reason 3000 from my IP. And just launch the instance. We're going to download the script and run it. Launch instance okay. My Grafana server is running. I'm going to do SSH to it. So I'm going to copy its public IP, open the terminal, switch to it, mention a hyphen I the path of the key. So mine is in downloads. So monitor stack key dot pem. That's the name. The username is ubuntu because we have used ubuntu Ami login. Yes okay I'm in I'm going to switch to root user clear the screen. And we are going to download the script from GitHub. So copy this raw link of Grafana Grafana setup script shift insert to paste it. Okay, this is downloaded. Let me give it executable permission and then let's just run it. Okay the setup completed. Let's access it from the browser. So take its public IP and access it on port 3000 okay. Initially it might take a little while for this page to load, and once it's loaded it's going to be pretty quick. So the username is admin and password is admin by default. And it's going to ask you to reset the password. So I'm going to give it a password. Make sure whatever you give you remember this. So keep it simple for now okay. So Grafana is set. Let's go to the next server that is Prometheus. Let's take a look at the Prometheus script. Also you can shut it down for now if you want. now. In fact, I'll also shut it down because we're going to use it in the next lecture. So just set it and just shut it down. Now make sure when you power on you use its new IP because this IP will be gone. You'll get a new public IP. So make sure you check the new public IP when you power on this IP won't work. Okay so let's go back to our monitoring branch and let's take a look at Prometheus setup script. I'll just copy that script into Sublime Text editor. Just looks better. Okay. So first thing is we set the hostname as Prometheus. Then few variables are defined like Prometheus version the download URL. Now this download is going to be the binary. It will not give us that systemctl command. It's just a binary we download and we have to run it. But we're going to run it as a service and it will be similar as we set up the Tomcat service. If you remember download the binary extracted, we set up the systemctl file and then we run it through systemctl command. So if you want to just find and download you can just search download Prometheus. It will take to this URL. Here you have not only Prometheus but other things like alert manager, black box exporter, node exporter. So currently I'm using this 13.5.0 Linux amd64. So there's windows package. Also there's Darwin package is Linux. We are using Amd64. Here. You have packages for alert manager black box exporter console graphite memcache. These are the exporters by the way like the agent memcache exporter MySQL exporter. If you're monitoring MySQL servers you can install these exporters. And it will expose the metrics which Prometheus can scrape. Node exporter. This is one we will be setting up on our web server. Okay. Let's go back to the script. So download URL is going to have the download URL that we have just seen. Tar file variable is just going to be the name of the package that will be downloaded. Extract folder. So when we extract it this will be the name of the folder Older working directory to store temporary files that we are going to download. Configuration directory. So this is the configuration path of Prometheus. Etc.. Prometheus. This is where we store the configuration data directory. By default Prometheus is going to store its information over here var lib Prometheus binaries are going to be in the bin usr local bin directory, the Prometheus binary file, and the service file that we are going to create to manage Prometheus with systemctl command that will be this path. So again it's pretty simple. Then we create the folder. We go into that folder we download Prometheus. We extract it. Now to run it as a service we need the user and the group. So we create the Prometheus group. We create the Prometheus user and add it into Prometheus group. Then we create the data directory where Prometheus is going to store the data. We give this data directory the permission of Prometheus user and group. That's the ownership. And this is the permission. So full permission to user and full permission to group. Then these are some subdirectories that we need to create in the configuration folder in etc.. Prometheus rules rules hyphen s files underscore SD. So once we download and extract Prometheus we go into that folder. In that folder you will have this file Prometheus and prompt tool. These are the binaries. This we are moving it into the usr local bin folder. That's the binary. That's where we move. You can then check Prometheus hyphen hyphen version. You should be able to see it's version. Prometheus dot yml. This will be the configuration file. This file we are going to edit and mention the information of the node that it needs to scrape. So in that tarball you'll have the configuration the default configuration. Also you need to move it to the configuration directory which is slash slash Prometheus. You can just download the tar file, extract it and see the content to understand it better. So this tarball I'm going to not download it here I'm going to copy this link and download it in the command line on one of the EC2 instance. Okay I downloaded this and now let's extract it. And let's get into this folder. Okay. Here. So these are the two binaries that we copied usr local bin. And this is the configuration file we put it in etc. Prometheus folder that we are creating. We'll take a look at it in detail. For now let's focus on the setup script. So once we have that tarball extracted we create the systemd file service file at this path. And we put all this content. So take a look over here at Execstart. This is the binary path we are mentioning. Here. We are setting the configuration path storage location var lib Prometheus. It's going to listen on port 9090. If you wish to change you can change it here. And one option over here. You see this? You should take a note off as from the beginning, I'm saying that Prometheus is going to scrape the matrix, but from alloy, you can also send the metrics to Prometheus. If you want that to happen, you need to enable this option when you start the binary web dot enable remote write receiver. So when we run systemctl start Prometheus it's going to execute this binary with this options okay. So we create the systemd file. And then we set the permission to the configuration directory to the data directory. Of course the configuration directory should have permission 775. All for Prometheus user. All for Prometheus group systemctl daemon reload enable Prometheus start and checking its status. Okay, now let's launch the EC2 instance. So launch instance name Prometheus or Prometheus server. Ubuntu. Ubuntu server 24 Ami Instance type T2 micro is fine. Key pair use the same key that we created earlier. Security group. Let's click on edit. Let's give security group name as Prometheus security group. Same in description I have given. We need to do ssh. So I'm going to say 22 from my IP. And Prometheus by default we have seen is going to listen on port 1990. So 1990 from my IP. So we can access it from browser and 1990 from Grafana. So Grafana can run the from shell and fetch the data. So add port 1990 from my IP and 1990 from Grafana security Group. So search for your Grafana security group name. Select that and make sure you mention the description. Allows Grafana to run query or just allows Grafana and say launch instance. So by the time it is launching Let's go to Prometheus, setup script on GitHub, click on raw, copy that endpoint. I should copy it later because I need to copy the public IP also. Okay, Prometheus is running. I'm going to copy this public IP as such. Hyphen a key path ubuntu at the rate the public IP of Prometheus. Switch to route user. Clear the screen. Let's download. Prometheus. Setup script from GitHub. So go to Prometheus setup script. Click on raw. Copy the endpoint. Make it executable. And let's run this. Okay Prometheus is running Q to quit this, I should update this hyphen F and no pager option so it doesn't stop here. I'm going to edit this file. You don't need to do anything. Systemctl status. Prometheus. Hyphen hyphen. No. Hyphen pager. So when you run this command with this option, it's not going to stop. It's going to simply print the information and go to the next command. So let me commit this. Okay. So Prometheus should be running. Now let's copy its public IP for in browser port 1990. So we have Prometheus running. If you go to status target health you see it's scraping its own metrics. Now here it has given the endpoint as localhost 1990 slash metrics. But here itself we can access it if you just type here metrics 1990 slash metrics. So these are the metrics. So Prometheus server is monitoring itself. And these are different metrics. We're going to take a look at this metrics later. But just wanted to show you that's monitoring itself. So we have Grafana and Prometheus setup. You can shut them down. In the next lecture we are going to set up the web server. So wrap this up and join me in the next lecture.
