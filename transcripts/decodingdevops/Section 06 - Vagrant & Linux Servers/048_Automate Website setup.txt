# Automate Website setup

Instructor: Hello, and welcome. Now, previous two lecture, we did the setup of a website on CentOS, and we set up the WordPress blog on Ubuntu. In this lecture we will be doing all this automatically, we will be writing a vagrant file that can provision website automatically, and then we will do the same for the WordPress template. So let's get into it. So for this lecture, I recommend you open a VS Code, VisualStudioCode. If you don't have it, download, install and open it. And then once you open it, click on file, open folder, go to the vagrant VMs folder where you have your vagrant VMs. Okay, mine is in F:\vagrant-vms, and I'll say select folder. Now first we will be automating this finance website, so I want you to right click on this and say copy. And again, right click over here and say paste. And then right click once again on this finance copy, rename it, instead of copy, rename it as IAAC, or just IAC infrastructure as code. Because we'll be putting everything in the vagrant file. Our entire infrastructure, the website will be in just one file, it will be code based. This is called a infrastructure as code. We'll be talking about this infrastructure code as coding in detail later. So expand this, if you have .vagrant folder over here, remove that, delete that .vagrant folder from finance IAC, and you have the vagrant file. This is from the previous lectures. First thing we'll look into this is the IP address. So I'm going to change the IP address. If you have this VM already running, or created, then the IP will collide. So I'm going to change it to 28. Anyways, I have deleted all the VM, but still, I give different IP. And then come down, and through provisioning we are going to enter all the commands that we use to set up the finance website. So uncomment this line, and this one, all the lines below it uncomment. Okay, let's write all the commands one by one. First was, not tum, yum install httpd wget unzip vim -y. Make sure you enter -y. In the provisioning, if it is asking you question, the provisioning will fails. What is the next step, to start the service, systemctl start httpd, and then, systemctl enabled httpd, and download the data from tooplate.com, but before that, we'll create a temporary folder. We'll call it whatever name you want, mkdir -p, -p if that folder already exists, it is not going to fail, but if the folder does not exist, it'll create it. That's why -p option. we do cd into that, /tmp/finance, and then here we'll give the command wget, and the link from tooplate.com. So let's go to a browser, right? So select the template, whatever you want, go to its page, click F12, go to network, and click on the download link. When you click on the download link, you should get the zip link over here. Go to the headers, and copy that link from the header, request URL. And then paste it, CTRL V, and then we are going to say unzip. And this will be the name of the zip file. Now, when you unzip, if there are files that already exist, and you want it to overwrite, you can use -o. Now first time, obviously this file won't exist, but if you're doing provisioning multiple times, there will be file that, those files may exist over there and it should not ask you questions. So -o to override if the file exist. Okay, then cp, and paste that zip file name, remove that .zip, /star. So when you unzip, it is going to create that folder /*. Make sure you remove that .zip from here, right? Also, we have to give -r. So recursively, copy everything to /var/www/html/, and then restart the service. And these are the only command that we need to run, right? Now, how do we know these commands? Because we did it manually. Remember the rule that I told you, if you want to automate something, you should know how to do it manually first. Verify all the steps, once again, look for any typographical mistake. And also we can give one more command to clean up. Now this folder would have created, and we really don't need it once it is created, so we are going to remove that. We are already in that folder, we did cd, so we'll hit the command cd/tmp to come out of that folder. Otherwise, we'll be cutting the same branch of the tree on which we are sitting. So come out of that and then cut it, delete it. Okay, I think this is good, let me save this. CTRL S I did, once again, file, save. Now let's go to Git Bash. Clear the screen cd /f/vagrant-vms/, and I should see their cd financeIAC, as code. There's a vagrant file, and it's showtime now. Let's run vigrant up. Okay, now the provisioning will start, in a moment. Okay, started. So if you make any syntax error, this will fail. If you make any logical mistakes, the provisioning will go, but you might not be able to access the website, or there will be some problem. Okay, at the end we see it's unzipping, and then the other command after that doesn't print any output, so we don't see any output, just till the unzip we see the output. Now let's get the IP address of this VM, or you can do vagrant, ssh, find the IP, the public IP, or this IP, and then let's go to the browser. Time to test it, and there we go. How was that, right? Now you do vagrant destroy, as long as you have that file, you can provision this website whenever you want. So infrastructure in code. Now destroy that VM, but keep the vagrant file, especially the provisioning command, we will be using that later also in AWS, while we are testing the same thing. So anytime you want to set up the website, you have the scripts with you, you can anywhere, anytime, set up this website. This is also called reusability, we can reuse the code, but this is pretty basic of what scripting and reusability is. We will be learning more in detail, in scripting, and other sections. Now let's do the same for WordPress.
