# Vagrant IP, RAM & CPU

Lecturer: Hello and welcome. In this section of the course we are going to learn little more detail about Vagrant. And it's not just about Vagrant. Through Vagrant, we are going to learn a lot of concepts that is very important for cloud computing, Docker, Kubernetes, and other technologies. In this section we are also going to leverage AI tool, GitHub Copilot, and see how we can use AI to write our code or even modify it. So we already know Vagrant through VM setup section. We have seen how we can use Vagrant Cloud to fetch VM images or VM boxes. We talked about Vagrantfile where we can mention virtual machine settings. And we have seen some simple Vagrant commands like Vagrant up, halt, destroy, et cetera. Now in this section we are going to get little more detail into Vagrant. We are going to cover networking concepts, we're going to talk about the resource RAM and CPU, we'll see how we can provision servers on VMs automatically. We are also going to see the multi VM concept. And along with all this, we are also going to leverage AI to write our Vagrantfile and fix it and make changes to it. So, let's get into it. Okay, first few thing before we get started. Open Terminal for macOS and Git Bash for Windows and execute the command Vagrant global status. This command should show you the status of all the available vagrant VMs. So, I have two VM. One is open, two other one is sent to us. And they're in running states, so I'm going to delete them. We are going to do cleanup and then we will start working on our Vagrantfile. So one way is you can do CD into these folders and delete the VM or you can just queue their ID, and also delete them. So, we'll do both. First, we're going to get into, so I'm going to get into F drive, vagrant_vms_ubuntu. So, I'm in this directory. I'm gonna run the command vagrant status. This shows the status of the VM in the current folder only. And I'm going to do Vagrant destroy --force. Now if you observe here it says the VM is powered off, but here it says in the global status it says running. So global status is not very dependent command. It can show you the stale information. And I'll show you in a moment how it can refresh that information. So first, let's delete this ubuntu VM. Okay, it's deleted. Now I'm going to run the global status command once again. Okay, now it shows the center's VM running. But I'm going to run now one option with it, --prune, which will remove the stale information. Okay, now I'm going to show you how to remove the VM. Without getting into the folder, you can do Vagrant destroy and you can mention the ID of the VM. Now this is also not so dependent command because we don't know whether that ID is real or it's the old IP which does not exist. ID I mean to say. So it's always better to get into the folder and delete the VMs or not take actions into the VM. Let's run global status prune command once again. So, we are all good. So in short, Vagrant global status. Check the VM, delete the VM, make sure everything is clean up. Okay, so in the VM setup lecture, I have created VM in this folder in my F drive in Vagrant VMs folder. If you have created anywhere else, just check the folder where you have created this ubuntu and sent to this folder or directory and then open VS Code. Yes, VS Code, Microsoft Visual Studio Code. We are going to use this code editor to manage our Vagrantfile and even Vagrant VM. First, we are going to set our default terminal into the VS Code. So Windows user use the command, Control + Shift + P, for macOS, use the command, Command + Shift + P and you should get this dialog box over here or here you have to type terminal colon, select default profile. When you find it, you can click on it or you can hit enter and select your default terminal. For Windows user please select Git Bash. Now macOS user, you don't need to select anything. Anyways, your default terminal is the terminal. So for Windows, just select Git Bash over there. So when we execute command from VS Code, it's going to execute on Git Bash terminal. As I said, we are going to manage our Vagrantfile from VS Code editor, so we need one extension. So click on the symbol, this is extensions. And over here search for a Vagrantfile. Okay, let's click on this. So it says, this provides syntax highlighting support for Vagrantfile. Useful if you use Vagrant. And let's install it. See trust publisher install. Okay, very good. Now we are going to open the folder where we have placed our Vagrantfile previously. So I'm going to click on file and say open folder. So I go to F drive, Vagrant VMs. Please select the right folder and say select folder. Okay, now you can see here open to folder and sent to this folder both has the Vagrantfile. Click on any of the Vagrantfile. You should see its content. Let's understand the syntax of this Vagrantfile, or any Vagrantfile for that matter. You have the syntax. First we are going to talk about the hash. Hash is a comment. Wherever you see hash, that means anything after that we'll be ignored. Now this is used for writing comment or even disabling any setting. The code is actually starting over here from line eight. You see this, click to collapse. Let's expand this. So, vagrant.configure, and then you see something called as config in between this two pipe symbol. And if I click on that, you see over here config is highlighted everywhere. And every setting you will see starts like this, config.vm.box. That's the box name. And you see some other things like config.vm.box check update equal to false. But this is disabled because there is a hash placed in front of it, right? All the settings will be like this until you reach, and this is where the code completes or the VM setting completes. Now this is just a text and whatever you write over here, all the settings should start like this. Like for example, if I write here, I say DevOps, then all my settings should start like this. DevOps. Okay, for programmers, this is a class object where we have many other methods and functions. For non-programmers, just remember whatever you have over here, whatever text you have over here, all the settings should start like that. So I'm going to do Control + Z and go back to config. Just keep it config. Okay, so let's say now I want to enable bridged adapter. You remember in the VM setup, in manual VM setup, we talked about bridged adapter. So your VM will have an extra network adapter that will get the IP address from the router. If you want to do same setting in Vagrant, you need to find this setting, config.vm.network. This one especially, config.vm.network in the double codes nets public network. So if you uncomment this one, right? So once we do that, we need to save the file and we need to do Vagrant restart. I mean to say Vagrant reload. Or if the VM is not started, we just do Vagrant up. So only at the boot time these settings will be applied. If you want to give your own IP address to your VM, there's another setting over here. You see config.vm.network, same thing. But with that you have here private network, IP and the IP address. And make sure the syntax is as it is. Here, you have comma. With IP, you have a colon. I don't try to understand too much about the code. We are just talking about the settings and how to achieve a particular simple things in the VMs. We are going to learn about scripting in Bash and Python in later lectures. For now, just understand the settings. That would be enough. Okay, so you can give your IP address over here. I mean the IP address of your VM, that will be a static IP address. With this your Wi-Fi router or whatever router you have in your network, we assign IP address to your VM. It'll be a random IP. So, this will get a random IP to your VM. And this is going to get the private static IP to your VM static. It'll remain until unless you don't change this. Okay, so that was about IP address allotment. And we have other settings like memory, RAM memory, but there's this section, config.vm.network if you uncomment this line. And this starts a new code block. So this is the main code block which starts over here and completes over here at the end. In that you have other code blocks like this one. So when you uncomment this, when you remove hash from it, make sure you you remove the hash from this also. And then vb.memory, remove hash from there. Now, check this out, config.vm.provider, virtual box, that's the name of the provider, do vb. Now the text inside that or the settings inside that will start like this, vb..memory = 1024. If you want to add extra CPU, you can say vb.cpus, vb.cpus = 2. Now as I do that, you see my code is automatically completing. And the reason behind that is GitHub Copilot, okay? So let's say I do not know what is the setting for the CPUs, right? So I just come to this line, vb.memory. I just hit enter over here and it automatically suggests me. You see that, vb.cpus. And you can use tab to complete this. Tab, complete this. And then make sure you remove the hash. Now let's say I do not understand what is this setting vb.cpu. What do I do? Well, I'm going to select this and I'm going to run the command, Control + I, or in macOS it's Command + I. Select the text force, then use Command + I or Control + I. And you should get this. Now here, type, /explain, and hit enter. Enter. And it'll start explaining you this line. So GitHub Copilot is telling us what this is about. Now I know this what this setting is, but I recommend you read it or any line of code if you don't understand like this, you can just select that and you can do again, Control + I or Command + I, and say /explain. And once you're good, you can close that. Okay, this is all good, but now we are going to do Control + S, save this file, and we are going to use GitHub Copilot to write entire Vagrantfile. We'll provide information to it and it'll write its own Vagrantfile. So for that we are going to click on this Vagrant VM folder and create a new folder. I'm going to name it as my VMs. You can name it anything you want. And in that we are going, wait. Just created in the wrong folder. I'm going to drag it and drop it at Vagrant VMs. Yeah, okay, yeah, just drag it and drop it over there. And in this I'm going to create a new Vagrantfile, so I click on this new file and see Vagrantfile. Make sure you give it capital Vagrantfile, V capital. Okay, clearly says there open chat through Control + I or Command + I. I'm going to hit, Command + I, and I'm going to write my own Vagrantfile with GitHub Copilot or GitHub Copilot is going to write it for me. So I'm going to get the open to box name, so I can get it from the Vagrantfile, which we already have used. So for Windows, it'll be something like this. For macOS M-series chip, it'll be box open to something. So just copy the box name, go to the new Vagrantfile that you have created. Use Control + I or Command + I. And write it with me. Write Vagrantfile with box name. What are your box image? Just paste it there. And I'm going to give comma use virtual box provider. This is for Windows virtual box provider. For macOS, you need to write use vmware_desktop. Okay, for macOS M-series chip, your provider is vmware_desktop. For Windows, it'll be virtual box. Virtual box. Then I also need some memory, like 1GB of RAM, two CPUs and I need IP address to my VM. So I'm going to write a few more things like first use virtual works provider or vmware_desktop provider based on your operating system of your computer. Memory 1GB, CPU 2, bridged adapter and static IP 192.168.56.17. Just make sure to keep 192 to 168.56. After that, you can give any number of your choice. Now let's hit enter and see what happens. Okay, so almost right, but except we needed a different public network. And for private network we need the static IP. So either we can accept it or we can modify this. I will say, static IP for private_network. For public_network, no static IP. Okay, nice. I think this looks good. I'll say accept. Now see this is AI, you know, it'll be learning and it'll be learning more. You may get a different result than what I'm getting right now, or same, but you need to interact to it or sorry, interact with it frequently so you understand its language. If you keep talking to it regularly, you know how to talk to it. You know, we are humans, we know how to talk. That's the best thing that we do. We communicate. So we just need to learn how to communicate properly with these AI tools, which is also called as prompt engineering. Okay, then now once we are good with all the settings. If we are okay, we can save this file, Control + S. I'm going to save this file. And then I'm going to open the terminal and run this Vagrant command to create VM with all the settings. So I'm going to go over here and click on terminal, new terminal. Now already have selected Git Bash as my terminal, but if you see a different terminal than what you don't want, then you can anytime click on this dropdown and select your right terminal. Okay, so from the terminal, if you do run PW, I'm in Vagrant VMs folder. I'm going to get into my VMs folder. Clear the screen. So my Vagrantfile is in my VMs folder. And from here I'm going to do Vagrant up. Now either you can create your own Vagrantfile like I did and bring up the VM or you can download the Vagrantfile from the resource section and then use it. Okay, open virtual box and double click on your VM if your VM is taking a very long time to pull and it's not coming up or if it's doing a timeout. That's one quick fix to it. Okay, you see now it's coming up. Okay, VM is up. Let's do Vagrant ssh. And let's check if everything is correct or not. So I'm in the VM, let me do sudo -i. Clear the screen. Let me check first the IP address, IP address show command. So VM got an IP that I have assigned. That is the static IP and the brief from the bridge adapter, that's the IP from the Wi-Fi router. Let's check the memory, free -m. So, around 1GB of RAM. That's what we wanted. And let's check CPU. So to do a cat to this file, cat/proc/cpuinfo. And here you should see CPU cores 2. Okay, this is all well and good. Now you can also apply all this setting to your existing Vagrantfile, like open to Vagrantfile and send Vagrantfile. Just do it for the practice and keep talking to the GitHub Copilot to generate your own Vagrantfile. Give different, different settings and you will be amazed what all it can do for you. Now, before you move to the next lecture, do Vagrant halt to this VM. Once this VM is off, then join me in the next one.
