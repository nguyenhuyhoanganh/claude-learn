# Systemctl & Tomcat 10

Instructor: Hello and welcome. In this lecture we are going to learn about systemctl by using Apache Tomcat. We have previously seen (indistinct) service and we know we can manage the service with systemctl command, but there are some services for them you don't get systemctl by default, so you should know how to build it for them. Tomcat is a service from Apache, and let me make this clear. Apache httpd and Apache Tomcat are two different services. Tomcat is open source Java servlet container. That means you can host Java-based web applications by using Tomcat service, and we will be seeing Tomcat service so many times in this course. So let's take a look what we are going to do in this lecture. First we are going to download and start Tomcat service manually. We'll see what are the problems with this. Then we will see systemd file, which is the configuration file for systemctl command. We'll see the requirement for it, the users we need to create the folders. And finally we will write our own systemd file for Tomcat service. The main focus of this course is systemctl and not Apache Tomcat, so I want you to focus on the process, the concept, and not the commands. So let's get started. Okay, we will need a CentOS VM for this lecture and I'm using the same old CentOS VM that we created earlier. So you can use the same one, bring it up and log into it. Okay, then switch to root user. Let me clear the screen and let me show you once again, this is a CentOS VM, CentOS operating system. And I am going to install here httpd first. Okay, httpd is installed and we know we can manage it through systemctl command, systemctl status httpd. So let's see the configuration file for the systemctl command. If you do ls to this particular directory, /usr/lib/systemd/system/ you should see here so many files including httpd.service. So this is the configuration file. Let's take a look at this. So I'm doing cat /usr/lib/systemd/system/httpd.service. Okay, in this file we have four, or sorry, three directives, unit, service, and install. I want you to just focus on this setting in the service directory, ExecStart, /usr/sbin/httpd $OPTIONS -DFOREGROUND. So this basically is the command to start httpd service. So when we say systemctl start httpd, it's going to execute this command and we'll look into other options later when we write our own Tomcat systemd file. So we install this service through package manager DNF. And when that happens, this will create all these files automatically. Its configuration file will be in etc folder, right? Httpd here. Its logs will be in /var/log/httpd, right? Httpd, right? And there's a systemd file. And different location, it will create different files for it. The libraries will be in /usr/lib like that. So that is managed by package manager like DNF. So we want to install Apache Tomcat service and it's not available through DNF command. And moreover, we want to install a specific version. So I simply Google download Tomcat 10 and it's the first link from Apache Tomcat itself. And there should be a download link if you come over here, binary distribution core tower, download gzip format. We want to download, so we can say right click, copy link address, and let's do wget and the link is going to download the tarball of Apache Tomcat. And that's the folder. Let's extract it, tar xzvf extract. And if we do ls, we should see the folder here. And if you do ls to that folder, we should see here the binaries, the configuration, logs, lib, temp, and all the other things in just this folder. So complete binary, complete package is this directory or this folder. Okay, now we can start by using the binaries in this, but Apache Tomcat needs a dependency and that is Java. So we are going to first install Java for it. So just say dnf install java-17-openjdk and say -y. Now, don't worry about Java JDK. We will cover all these things later. Just focus on the service management systemctl. So this is the dependency we need to install Java 17 and we are installing Java 17 for this. It's going to take some time to install Java 17. Okay, that's installed. Let's check its version, java -version, and it's open JDK version 17. Okay, so that's the dependency and we have the Apache Tomcat service. And to start it, we have to do this. Well, let me show you the file first, Apache Tomcat. In this folder you have bin. In this you have startup.sh. So that is a script, a shell script to start the Tomcat service. So you have to just simply give its path. Now we'll just first get into this folder and it'll say bin/startup.sh. Okay, it says Tomcat started and you can check the process, ps -ef | grep tomcat, and you should see Tomcat service running, our Tomcat process running. Now, there's a default webpage also for Tomcat. Just do ip address show. Get the IP address, the one that starts with 192 or whatever bridge network you have used or private network. Take that IP address, put the IP in the browser, :8080. Hit Enter and you should see a default Apache Tomcat webpage. So it's running, right? So what's the problem? Problem is first to start with how do we enable this service at the boot time? You know, if the computer reboots, if there was reboots, the Tomcat service won't come up automatically, right? You need to like this, run this command startup.sh, the script to start Tomcat service. To stop also you have a separate script. The other problem is in DevOps, there are so many automation tools and these tools will execute commands remotely to start a service. We don't mention the command in these tools. We just say, you know, start a particular service, start Tomcat service. So in the background it will execute systemctl start tomcat. And since it's not available, we won't be able to start the Tomcat service or manage it. So as a DevOps engineer, it is definitely not recommended to run a service like this. Okay, let's get the process ID, ps -ef grep tomcat, and we have to stop it. See, this is how we have to stop it, kill. And we will give the process ID. For me it's 31251, kill it. Now, this is simply just one process, so it was easy. If there are many other processes, just forking processes, then we need to kill all those process as well. Difficult, doable but complicated, right? Okay, now let's get started with our main work, writing systemd file for Tomcat service. So Tomcat, like any other service, should be run as non-root user. So we need a user that is going to run the Tomcat processes and we also need a home directory where we can keep our Tomcat data. So I'm going to create a user, so useradd tomcat, and the home directory I'm setting as /opt/tomcat. This is where we'll copy all our Tomcat binary files. And since this user Tomcat is just to run the Tomcat process, we do not want it to have any shell, but there's a spelling mistake, no login. Yeah, okay, this is for security purpose. So should not have a login. Let's create this and it should have created this folder as well, /opt/tomcat. And now let's do ls. We have all the content in this. We're going to do cp -r apache-tomcat/* to /opt/tomcat. So we copy all the content in this location and we have to make sure all the files that we copied is owned by the Tomcat user that is going to run the process. Otherwise you will get permission denied. So /opt/tomcat we have to give recursively. So I'm going to say here -r. Let's verify that, ls -l /opt/tomcat. Okay, all the files copied and they have ownership of Tomcat. Okay, now we have to create the systemd file and it will be at this location, /etc/systemd/system, and you have to give the name. This will be the name of the service, Tomcat. If you give any other name, that will be the name of the service, systemctl, start, stop, Tomcat, like that. Extension should be .service. Let's create this file. Let's get into insert mode and let me first write the three directives, unit service install in square bracket. In the unit we give global things like description and the dependency. So description, Tomcat. After, this is used as a network.target. So after all the network service gets started, then only our service can be started. So that's a dependency. So with after we creates a dependency. And all this I'm taking from the documentation, which I will show you at the end of this lecture, so just bear with me for some time. Now, in the service, we have to mention first the user that is going to run this service. So we're going to say user that we created, user equal to Tomcat. Make sure don't do any spelling mistake, capital, small, be careful with everything. Working directory. So we have working directory and that is /opt/tomcat. Okay, environment variables. Now, these variables that you see over here, it has nothing to do with the systemd file. These are the requirements of the Tomcat service. So if you want to start the Tomcat service and we want to use a Java of a specific version or location, you can do JAVA_HOME equal to this path or wherever your Java is installed. Our Java by default will be installed at this location, /usr/lib/jvm/jre. So how do we export variable here? We say environment equal to variable name equal to its value. Also we need CATALINA_HOME or CATALINA_BASE that is the home directory of Tomcat. We do it through this variable. Again, as I said, this is the dependency of Tomcat, not systemd. So in systemd, if you want to export variable, we say environment and that. Okay, now the main thing, ExecStart, the executable file, right? Let me fix this first. Yeah, the executable file to start or stop your service. So ExecStart, you need to give the location. So this is the startup.sh file that we used initially to start Tomcat service. And this is the location of it, right? We created /opt/tomcat folder. In that we have bin/startup.sh. And to stop, that's the path, shutdown.sh. Okay, now the last thing, install section, wanted by multi-user.target. Now, this is really system admin stuff, but quickly, let me explain this to you. So we have kind of a safe mode and then we have kind of a non-GUI mode where all the services are started except the GUI. And then there is a GUI mode. So multi-user.target is the non-GUI mode of the operating system where all the services starts. It's not the safe mode. It's not the GUI. It's the complete operating system startup, but minus GUI, graphical user interface. No graphical user interface. So we want this service to run in this, which is what when we do (indistinct) update the VM starts, the operating system starts. There is no GUI, but all the services starts. That is the multi-user.target. You know what? Don't worry too much about this for now. Okay, I want to do one more thing here, service type forking. Forking in process means the process that starts child processes. So the target service can start those child processes. And I will also mention group tomcat. Wait, not capital here, it is small t. You can use this file from the resource section if you don't want to write it, and it's okay not to write it. I never write from the scratch. You know, we have many available systemd file like that on the internet, on GitHub repositories. I have my own so many, so I just reuse them. Don't need to write it from the scratch. Okay, so let me verify it once again and then I will save it. Okay, I think I'm good. I'm going to save and quit. And when you add a, when you make any kind of change in the systemd file, you have to run this command systemctl daemon-reload. Okay, just make sure no Tomcat service is already running. Oh, sorry, the process, no Tomcat process is already running. If it's running, just kill those process. Clear the screen and let's do systemctl start tomcat. If you make any mistake, you should see it over here. Looks like it got started. And I will say status, yeah, it's active running, right? And the best part is I can enable the Tomcat service. Enable. All right, so when we reboot this VM, Tomcat service will come up. Let me quickly show you one use case from a DevOps perspective of using systemctl. Okay, this is one Ansible code. Don't worry about what it does and how it works. We are going to learn Ansible in detail later. Just wanted to show you this, right? So Ansible uses a module called service, just mention the name of the service and we mention the state. When we execute this, what happens is it is going to contact the systemctl to start or stop or manage the service. So if you don't have systemctl for a service, you won't be able to manage it like this. So there won't be any standard basically to manage your service. So as a DevOps engineer, it's very essential for you to write systemd files for services or processes that doesn't have systemctl. Don't worry about the complication. We can do this multiple times in this course. I have given the systemd file in the resource section and all the steps to set up the Tomcat service. You can check those steps as well. That will be all in this lecture. I will see you in the next one.
