# Provisioning

Instructor: Hello and welcome. In this lecture we are going to talk about provisioning. In short, this means executing commands when the OS comes up or after the boot. Mostly it's the first time boot when the first time the VM or the operating system comes up for the first time executing commands on it. This is also called us bootstrapping in general. In vagrant it is called US vagrant provisioning. So make sure all the VMs are powered off or deleted. Go to CentOS folder, Vagrant file and scroll at the end. And before I go there, okay, just make sure you have some private IP, private network IP given over here. Also uncommand the public network setting and scroll till the end where you see this setting. Just uncommand these all these lines, line number 75, 76, 77, 78. So config dot VM dot provision. And in double codes you'll see shell. That means provision on the shell. So here the provisionary is shell, there are some other provisioner also in vagrant, comma inline. And you see two input redirection over here. This is append input redirection, the opposite of output redirection. And in between this minus shell and shell, you will see commands. So these commands will come as an input redirection on the shell. So in short, if you want VM to execute commands, you can just mention all the commands over here in between this minus shell and shell. We can also ask co-pilot about this. So just select this setting, command I or control I and say slash explain. Slash explain command can explain the settings. Okay, I'm just going to click here, view in chat. So it just appears over there. You can read it easily. So I'm not going to read it line by line. You can just read it and try to understand like that. Any setting that you don't understand in any kind of code, you know, Python, Bash, anything that you write, Yammer later, you're going to write a lot of different kinds of code, Terraform, Ansible. So you can just select it like this and say, slash explain, and it'll explain it to you. So in short here it says, you know, the selected code defines a shell provisioner for the VM specifically, it tells vagrant to run a shell script When the VM is created or provisioned. It's only the first time when the VM is created. If you want to again repeatedly ask to execute provisioner, you have to mention vagrant provision. We'll see about that. First, let's just make sure we give the right command. These are open to commands. We are in the center is vagrant file. So I'm going to give equivalent. Okay, I've mentioned some commands, installing some packages, creating a folder, and running free hyphen M command and putting its output into this file. Running df hyphen h, that shows the disinformation of the Linux operating system and putting dis information in this file. Basically just some shell commands you can just mention over there. And let's save this, and let's open the terminal, new terminal. Make sure you are in that folder. In this case, the centos folder. Check the vagrant status. Okay, it says not created. So this should be in this state, because we need to create it for the first time. So vagrant up is going to create this VM now and execute the shell commands. Okay, so the VM is up, and you see it executed the commands that we have mentioned in provisioning, right? So it installed the package and these commands also executed, but we have sent the output to a file. So if you log in, you can see the output in these files as well. Now provisioning is once for the first time when the VM is coming up. That's by default nature. If I add here more commands, like I'm going to start httpd service, you see it already trying to complete, and I'm going to hit tab to complete it. That's copilot in action. And then next line wait, system ctl enable httpd again tab. And that is all. I'm going to save this one, and then I'm going to reboot this. Vagrant reload. So what do you think? Will it execute all this commands? Remember the VM is already up. Provisioning is for the first time. Let's test it. I'm doing a reload to an existing VM. Now let's see whether it executes the provisioning commands or not. So the VM is up and you see it did not execute the commands because provisioning is only for the first time. But if you want to execute provisioning again, you can run vagrant provision or use hyphen f and provision option with vagrant up or vagrant reload. You can say vagrant reload, hyphen f and provision. Or if you just want to execute the scripts, you can just run vagrant provision. So I'm going to run vagrant provision and you see it is executing the scripts that are mentioned or that is mentioned. I'm going to add a few more commands over here. System ctl stop firewall d and system ctl disable firewall d. And then I'm going to save this file and execute vagrant provision once again. Now let's get the IP address of this VM. So my IP is over here, 168 56 15. Let's open the browser. So let's do http colon slash slash put the IP address. Hit enter and you should see the default Santos webpage coming from H 2D PD, Apache H2 DP service is running well and good. So now in the next lecture, we are going to set up our own website on this VM, or we'll create another VM and set up a website on that. We are going to use the provisioning also. So once you're done over here, delete the VM, delete all the VMs, and then join me in the next lecture.
