# Website Setup

Instructor: Hello and welcome. In this lecture we are going to talk more about Linux. So, far we have learned basics of Linux. We have seen the file systems, filters, users and groups sudo, software management, services and processes. In this lecture we will be talking about server management. How do we install and manage a server as service on a Linux operating system. This is to boost your confidence. You get more practice. And then similar thing we will be doing on AWS Cloud as well in many of the coming sections. So first, we will be setting up httpd service on CentOS and we will deploy some HTML templates. This is a very basic website. A ready-built website will download from the internet and run it on a CentOS operating system. Next we will be using Ubuntu operating system and we will set up a LAMP stack. It's long form is Linux, Apache, MySQL and PHP. There are many application that runs on LAMP stack. So, we'll set up an Ubuntu VM, set up LAMP stack on that and deploy WordPress template. WordPress is a very popular blogging application that you can run on your Linux machines. And all these we will be doing manually first. And then we are going to automate these setup by using vagrant provisioning. So, at the end we will have two vagrant files, one to setup HTML templates automatically, the other to set up WordPress template automatically. Now let's see this in action. Okay, so we want to set up a website on CentOS and there are ready-made templates available on the internet. So, I recommend you use a Brave browser and open this site: tooplate.com. Now, Brave browser because if you open it in other browser, it'll show you a lot of marketing popups. So, use Brave browser, easy to get the download link, also. There are many ready-made templates available here. You can choose whatever you want. Right now I see this one Mini Finance or finance. So, I'm going to use this one. So, our project name will be finance or finance. So, I'm going to create a virtual machine with CentOS operating system. Before I do that, I'll see if there are any other VMs running. So, I don't have any other virtual machines running. If you have: go there, delete those virtual machines. I'm going to create a folder in F drive. vagrant-vms with the name finance. And get into that, you can choose any folder of your choice. vagrant box list. We have a CentOS box in our machine. This one we'll be using eurolinux. So, we'll run the command vagrant init. And the box name, I just selected that, it copied it. I'm going to do Shift + Insert so it paste there. It creates the vagrant file. We'll open this vagrant file. You can use Notepad or VIM, whatever editor of your choice. We're going to make some changes. We need a static IP or a public IP is also fine. So, I'm going to give the IP as 56.22. Make sure this IP doesn't collide with other existing VMs. That's why I told you to even delete the VM. And also, we can use the IP that we get from our router. So, you can use any of this IP to connect to this VM later. And we'll keep RAM as 1 GB. Okay, save and quit. And let's bring up the VM. Okay, the VM is up. I'm need to log in inside, okay, and switch to root user. The first thing I want to do is I would like to change the host name here. So, via etc/hostname and give host name of your choice. I'm just going to give it as finance and run the command hostname finance. And then you have to log out and then log in again. Okay, now this is not mandatory. If you want to do... You want to skip this also that is fine. Okay, so, in order to set the HTML template, we need httpd service. Now since this is CentOS, the service name is httpd or also the package name is httpd. If it's Ubuntu, the package and service name is Apache2. We have seen that, right? So, we have this as CentOS 9. Okay, now let's install httpd and we will need a few other things: wget, vim, unzip and zip. Zip is not necessary, but let's download it. So, httpd, we know that's the website package. wget, we are going to download the HTML template by using wget command. vim because we're going to edit some files. So, and the file that we are going to download from the internet the HTML template. That will be in zip format. So, we need unzip to unzip it. Let's install it. So, these are the dependencies. So, first thing always is to install the dependencies. That's what we are doing. Okay, it's installed. Let's start the service, systemctl start httpd. And we would also like to enable the service because if you reboot the httpd service won't come up. So, you should enable that. And now it'll come up if even if we reboot this OS. Okay, let's check the IP address, ip addr show. And any 192 IP except NAT use. You can use this IP also 56.22. And let's check it from the browser, http:// and you paste the IP address. Okay, so you see a default httpd webpage. It's the default. When you don't set anything it just shows that, you know, httpd services running fine. Okay now, before I deploy my HTML templates from tooplate.com, I want to show you one thing. Read this: For systems using Apache HTTP server: You may now add content or content to the directory /var/www/html/. Okay so, if we add content over here it's going to show over here, but the content needs to be in a specific format that we will see. So, go to /var/www/html/, ls, it will be empty. We are going to create here a file index.html, exactly same name. That's the file it is going to throw on the browser. And let's put something over here. This is my first website setup. And so I'm not writing really any HTML content, just some text. If you know HTML you can write it but really don't bother. So, we created a file exactly in this folder with exactly this name. And after that we are going to restart the httpd service. So, whenever you make change to the service, make sure you restart the service or at least reload the service. Okay, keep that as a thumb rule, remember that. Okay, let's go back to the browser. And now let's refresh it again. You see that? That's really a very dumb website, I know that, right? But we are practicing here. We understood index.html file is pushed to the browser, right? Now, coming back to here, tooplate.com. So, select whatever template you want, right? You can scroll down. If you click on Download, it is going to download a zip file. And download. Right, a zip file. See that. And this contains all the data, including index.html of this website. But we need to download it in our VM where we can unzip and we can deploy this file to /var/www/html. And in order to do that, we need the download link. Generally, what we do is we do Right Click, and we say Copy Link Address. Right? Now let's open another tab. If we paste this link, this really is not a download link, okay? This is not going to download this zip file or it may download. But anyways, what I wanted to show you here is finding the download link from the browser, and use Brave browser for now, F12. Click on F12. Okay? Make sure you are in the network tab, right? And then click on download. Okay, it'll start the download. You can save or cancel it. Cancel it. Okay, now here you see two links. Okay, let's expand this. Okay, you see there, right? So, click on that. Click on Headers, make sure you click on Headers. Once again, Network, click on that link, zip one in Headers. And here you see the complete download link and you can copy it from here. Okay, let's do Control + C to copy and let's go to our terminal. Now don't download it in HTML or other places. Just go in tmp because this is going to be temporary file. We don't want to put it in other places. wget paste the link and this wget command is going to download that zip file and store it in our local machine. Sorry, in the VM, right? So, that's the zip file. And now it's time to unzip it, unzip give the name. Do ls. You should find a folder there, a directory. Get inside that and clear the screen. Right, make sure you're inside this unzipped folder. Do ls. And now you should see all the content including index.html. Now, all this content, we need to copy, cp -r *. -: r because there are folders, right? We want to copy all of the folders *, everything to /var/www/html. Right? So, make sure you are in that folder. So, it's asking do you want to override the existing index.html file. Yes, because we created it just for testing. Right, now do ls /var/www/html. And you should see all this content, same content, right? And the next step, I think you already know. We are going to restart the httpd service. Okay. Very well. Okay, now before we check from the browser I want you to validate few other things. systemctl status httpd, the service should be running. You should have the content in /var/www/html. Also, I want you to check one more thing. That's the firewall service. So, systemctl status firewalld. Now if you are running a CentOS VM on Windows machine, this will be not active. This will be inactive. But if you are running Fedora on MacWise, then this will be running and you have to stop it. systemctl status stop firewalld. And you have to also disable it. So, it does not come at the boot time. When you reboot the machine, it should not come up. In real time, this is not a good practice to shut down the firewall. But we are going to learn about firewall later in real time use cases. For now, we are just learning provisioning. So, we are focusing on Linux virtualization and we'll see some automation in next lectures. You can also check the service. Once again, httpd. Q to quit. Right, pretty basic validation. Get the IP address. ip addr show. Right? Not this 10.0 IP. 192, this IP or this IP, right? If you already have it in the browser. So, I'm going to go back to the browser and I'm going to simply refresh the page. And now you should see the website, right? So, index html file is pushed on the browser and that file shows the other content, images and all the other things. Okay so, if you want to quickly reiterate everything, right? What we did, we created the VM CentOS, right? And let's look at the history. We installed the dependencies or packages then we started the service. Or you can say we managed the service, right? We made some configuration. We did not make actually any configuration change. But the third step will be configuration changes if anything is required. And then the final fourth step is deploying the data which was, you know, downloaded from the internet. We did unzip and copied it, restarted the service that is deploying the data. We are going to reiterate all this in proper method and make you aware about server deployment steps after we do WordPress. So, once you're done here, exit from the VM and power off. Or if you want, you can delete this also. We don't need it now. Also, I destroyed the VM. Deleted the VM. So, complete this exercise, delete the VM, and then join me in the next lecture where we are going to get more into detail with WordPress block.
