# Multi VM Vagrant file

instructor: Hello, and welcome. In this lecture, we are going to talk about Multi VM Vagrantfile, what is that, and why do we need it? Well, if we look at our previous Vagrantfile, we have a folder and we placed a Vagrantfile in that folder. So you can have just one VM per folder. If you want multiple Vms, then you need to manage different folders. You need to manage them with different commands. It'll be very convenient if we can manage multiple VMs together from one single Vagrantfile. This would be specially for an application stack that needs multiple services running in multiple Vms, like database on a separate VM, application service on a separate VM, frontend service on a separate VM. But together they work and form one single application. In such cases, you need to create a multi VM Vagrantfile. So I am in the Vagrant documentation. You can simply Google Vagrant documentation. You can come to this link, and there are all the steps, different settings of Vagrantfile, and many other thing that you can learn from the documentation. Like in CLI, if you click here; we have the box command, Vagrant box add, Vagrant box list, Vagrant box prune, remove. You can check in the init. Vagrant init box name, and you can say vagrant init -m box name, which creates Vagrantfile without any comments, -: f to overwrite an existing Vagrantfile, and many other options. See different arguments that you can provide to the Vagrant command. And also let's look at Vagrantfile; config.vm, what are the other different settings you can give with config.vm? So you have section separate for different different settings. I wanted to come here to Multi-Machine. You can read about what is a multi-machine since we're already discussing about it. If you come down here, you have an example of a multi VM Vagrantfile. You can simply copy this, paste it in a file called Vagrantfile. You have to create a file with the name Vagrantfile, V capital, paste this content and then you can make the changes. if you look closely, the setting, and then it closes over here. And then inside that, you have config.vm.define. And then a name web do web, this will be the name of this VM web.vm.box, where you can mention the box, and an end keyword. So this is one VM and this is the other VM like that. You can have multiple VMs in one single Vagrantfile. Of course, you can mention other commands, or other settings like the config.vm.network. Provisioning, hostname also you can set. All the settings that we have seen previously, you can define for separate Vms. And that is some work, really. But that is only at the beginning, once you have a one single multi VM Vagrantfile, you can just copy paste that file, make the changes, and create other Vagrantfiles also. And now this is a very perfect place to introduce ChatGPT. Yes, you heard it right, the famous AI tool that generates text. So if you already have an account on ChatGPT, just open it. If you do not have ChatGPT account, just sign up. It's pretty simple, mention your email address and sign up on ChatGPT, and open it like I did. So that's the UI of the ChatGPT. We'll ask ChatGPT, what is vagrant or Vagrant? It's thinking. And it's started printing sometimes it's very slow. I thought I will show you the power of ChatGPT but it's actually very slow today, I don't know why? It happens sometimes. So ChatGPT generates text. And we will be writing a lot of text in this course and in your DevOps career. Most of the time that text will be the scripts, the configuration files. Now, instead of you writing it all one by one, you can ask ChatGPT to do it for you. But do not always depend on ChatGPT at least in this course, do not use ChatGPT, just test it and that is fine. If you start using ChatGPT for the convenience in this course, then you will not learn. As a DevOps, you should be able to write each and every line of your script and code. I said you should be able to, but that not necessarily means that you should be always doing that. Yes. For the help, you can use ChatGPT. So watch now. So I will ask it, Sample Vagrantfile? Okay, there it's showing us a sample Vagrantfile. It's also explaining us the concept of that, all the options. And instead of writing the multi VM Vagrantfile which we can do, no big deal, we copy paste the content from the documentation, we make the changes, we can do that. But I wanted to introduce ChatGPT, that's why I'm doing it through ChatGPT. So I'm going to put this content over here, multivm Vagrantfile with web01, Ubuntu20, web02 Ubuntu20 db01 with centos7. Private IP for all the vms. And provisioning for db01. I also said, Set hostname also. Let's see what it does. Now it may generate a different output for you compared to mine or same? Let's find out. Okay, it's also started explaining all the lines in the Vagrantfile. So you have the code ready over here but if you are thinking that, "Hey, ChatGPT is doing everything, what am I going to do?" And think about it, what did I put? Multi VM Vagrantfile, I should know what is a multi VM Vagrantfile. Web01, I have to put box name. Web02, I have to put box name db01. What are private IPs in the Vagrantfile, and what is provisioning? I should already know this, I should know what to ask? So it's not going to replace you, it's going to be your assistant. Okay, let's copy this code, and let's go to VS Code. I'm going to create a new folder over here. New folder, we'll name it as multivm. And in this folder, let's create a new file, and we have to make sure the file name is Vagrantfile, V capital, no extension. And we will paste the content here. We are not gonna take it for granted, we'll make changes. One thing is 56, is the range. And I'll say 41, 56. 41, 56, Now I could have given this one also in ChatGPT, but let's do something by ourself in this lecture. Okay, 56.42. okay, this provisioning is active there, let's put some packages here. I will say, wget unzip mysql, not mysql... The package name is mariadb-server. So in Cento OS, the package name is mariadb-server for the MySQL database. And this command will change it to systemctl start mariadb, that's the service name. systemctl enable mariadb. Okay, let's save this, If you want to make any other change, you can do that. See, this is how we can set the hostname. You have to give a name to the VM here, web01, and then this will be in this pipe symbols. We have web zero, and that's why the settings are web01.vm.box, web01.vm.hostname. Similarly, you look here it's web02, the settings are like that. Web02.vm. There; db01, so whatever is in between this pipe symbol, the settings will be starting with the same name. For programmer it's a class object, so you can easily understand what I'm saying. For non programmer, once again, whatever you see in the pipe symbol name should be similar. So if you happen to copy paste this to add one more extra VM. So when you copy paste, just make sure you make the changes to the names and all the lines. And most importantly the IP address, the IP address cannot be same in one same network. We have 56.41, so we should change it to let's say 42 here, and this one will change it to 43. So the network 192.168.56, this is the network range network IP, we will understand all that in the networking section but till here is the network part. And in the network you cannot have duplicate IP addresses, so IPs should be different. So let's save that, can do Control + S or you can say File, Save. And let's go to Git Bash. Let's go to the folder f/vagrant-vms, and multivm. There's our Vagrantfile and let's bring up the VM Vagrant up. Okay, check global status, make sure all the other VMs are powered off or deleted. We have three VMs over here, if you have any more VMs running, there is a chance you may run out of resource on your computer. So make sure all the other VMs are off or destroyed. So this is going to take some time to bring up the VM. I'm going to pause my screen and resume when this is completed. Okay, observe the VM name, it used to be default but now we have a name to the VM, web01, web02, db01 like that. Web02 is also up, db01 is coming up. So this box was not available on my computer, so it's downloading it. Okay, the VM is up and we used to log in like this; vagrant ssh, right? But now we have multiple VM, so what will it say? This command requires a specific VM name to target in a multi VM environment. So we need to provide the name and we have the name like ssh web01. And we are in, you see that there's the hostname also set. Let's exit from that. Now, the way we are going to manage these multi Vms is mostly through name like, let's say you want to power off one single VM and not all. So you can say, vagrant halt. When you say vagrant halt, it's going to power off all the VMs. So you want to do a specific one, you can say, web02 power off. Same way; destroy or bring vagrant up web02, and like that you can bring up also you can say, vagrant up web02. I'm not going to do that, it takes a very long time. If you want to delete any specific VM, you can say, vagrant destroy web01. But if you say, vagrant destroy, it's going to destroy all the VMs. So you make sure give specific name of the VM unless you don't mean to do it on all the VMs, except Vagrant SSH; there, you definitely need to provide the name of the VM. Anytime where you want to add any extra VM, you can go to your Vagrantfile, you can copy paste the settings, and make the changes. I'm going to change all the web02 to web03. I did that. And also I need to change the IP address. So 42 is already given for db01, I can say 43 here or 44. I'm going to save this file, and come back and I can say, vagrant up web03. So it is going to read the Vagrantfile, look for web03 setting and bring up your VM. Now, what multi VM doesn't mean you put all your VM in one single Vagrantfile. It is for a specific project that we are going to see in the project set up section later for our project vprofile project where we have multiple VMs requirement, we are going to have one single Vagrantfile with everything in it. But for a different project or different use cases, I create a different folder with a different Vagrantfile. So make sure you do that segregation. So play with this; once you are done, destroy --force, force I'm giving, so it doesn't ask me the question yes or no? So I'm just giving vagrant destroy --force, it's going to delete all the VMs now. It deleted all the VMs. Web01, was anyways deleted so it says, "not created. Moving on..." And you should also move on now. Also, you can use ChatGPT to search for the errors that you get. But remember, you need to know what you have to search? Later, you can generate bash scripts. After you learn Bash scripting, you can generate Ansible playbook, you can generate Terraform code, and Jenkins file, and many other things through ChatGPT. So use it, but not always. When you are learning, learn it. Write everything by yourself. Once you are confident on your ability, then try to test ChatGPT or when you get any error. I hope you enjoyed this section. Let's wrap it up with a goodbye, I will see you in the next section.
