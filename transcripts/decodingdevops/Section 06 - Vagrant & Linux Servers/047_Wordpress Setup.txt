# Wordpress Setup

Instructor: Hello and welcome. In a previous lecture we did the setup of the website on CentOS VM, httpd service and deployed HTML template from tooplate.com. In this lecture, we will be setting up an Ubuntu VM with LAMP stack and deploy WordPress template on it. So I want you to open the browser, just simply search WordPress on Ubuntu. You will find first from the Ubuntu itself and many other blogs, DigitalOcean has really very good blogs but here the practice is on Linux and we are going to focus on this Ubuntu documentation, which is really clean and neat and works most of the time. I recommend you do reading, overview, what you will need. So it says, you know, we need Ubuntu 20. That's what we will be running, a VM with Ubuntu 20. The total nine steps, let's quickly go through these steps. So first step is installing dependencies. There are many dependencies, apache2, MySQL server and a lot of PHP libraries. We go next, here the steps are to install WordPress. Next, this is called as configuration change. So we create a file wordpress.conf in Apache configuration and we paste this content over there and here you see it says srv/www/wordpress. So this is the place where we are going to put the HTML template, DocumentRoot. Okay and once we do that, we are going to run few commands, which is going to disable the default Apache website and enable the WordPress website, right, a2ensite wordpress, a2enmod rewrite and this command is to disable the default website. Now you can do it through other commands also but Apache provides this commands for you. This command will create a link. We'll learn about this when we deploy our project, Vprofile project on VM. We're going to do that through the commands. This one sets the redirect, rewrite rule or you can say redirect rule and this disables the default website. Once you do those changes, you need to restart or reload Apache service, so that this changes can take effect. Go next. These are the commands to deploy MySQL database. So MySQL servers, once we install, it'll be running, you are going to log into that. And then this starts the SQL command prompt. Here, the commands that you run are called as SQL queries. So, CREATE DATABASE wordpress and then you create a user that can access that database identified by password. We need to replace this with some password and then this command to grant privilege to this WordPress user on the WordPress database that we create, GRANT SELECT. Now I'm not training you here or teaching you about the SQL queries. We are going to mostly copy-paste this but if you already know SQL, good, if you don't know, no problem, don't worry, we are going to deploy many at that time, the SQL database is in this lecture and you'll get the practice of it. FLUSH PRIVILEGE, it's kind a command to reload, refresh things and quit from the SQL command prompt, okay. Next. This step is for the WordPress to connect to the database. So we copy a configuration file at this location and then we open that file or you can just use the sed command to search for the content and replace it in that file. So it's going to search for this and replace it with the database name WordPress. It's going to search for the username text and replace it with the username WordPress and then with password here, text to replace with the password of the database that you created in step five, we'll see that, just I want you to go through the documentation first and then by using nano editor, we open this file. We can use vim editor also. And this command basically says, you know, open this file with the user www-data, -u option with sudo just says that you know, open and switch to www-data user and open this file. Now this user will get installed, sorry, created automatically when you install Apache 2, you open that file and you look for this text and you're going to replace all this text that you get from this link. So click on this link and this is the text. So you're going to just copy all of this and replace it with this in this file, and that is it. Your WordPress would be set up, then you can access it from the browser. Now it is saying local host, but we are really setting up on the VM. So we are going to use the VM IP in the browser and then you can graphically set your WordPress blog. Okay, I want you to take some time and read all the steps and try to understand as much as possible. You don't need to understand everything, this is just for practice, but try and grasp as much as you can. Once again, installing dependencies, install the package, configuration change of Apache and database and the sixth step is also same. And then access your application. Once you're done looking at this documentation, go to Vagrant Cloud and find the Ubuntu 20 box. Search for Ubuntu, Ubuntu 20 and you can use this Ubuntu/focal64. So copy that name, get into your terminal and let's create a folder for this, in Vagrant VMs I'm doing, we'll call it as just WordPress. Let's get into that. We'll say, Vagrant init and the box name. Let's open the Vagrant file, through vim or graphical editor, your choice. Let's do the same steps that we did previously. Setting up an IP address, static IP 56. 26. Make sure it's different from the other VMs. We can also enable the public network. So we get IP from the wifi router. And ram, we will need around one and a half GB. If you don't have that much resource you can keep it one GB also but one and a half GB will be good. So I'll say 1600. And save and quit. And then we'll see, vagrant up Okay, the VM is up, let's log in. Let's switch to root user. Let's change the host name. Again, not mandatory step, but good to do. Log out and then log in again, switch to root user. Let's clear the screen and let's go to the documentation to fetch all the steps. Okay, first, installing dependency. So I don't want you to copy and paste directly on the terminal, paste it first in a notepad or any editor and then from there, paste it on the terminal, once you're sure. So let's copy this command and let's open notepad, new file. Okay, paste it here and I can add one more extra option here, -y, right. So let's first run apt update. Okay, then to run this, we can directly copy-paste this. Now if you're wondering why this is vertically separated, if you have a very long command in Linux, you can separate that, make it vertical by giving space and a backward slash, right Same way you have to do it in every line. So you can write the command on the terminal vertically also. But wherever there is a separate, wherever you want to do the separation, give a space, a backward slash and then Enter, it goes to the next line. We need to simply copy this, clear the screen and paste it and hit Enter. So while this is happening, let's go to the next step, which is installing WordPress. So let's copy all this command and paste it in the notepad. Now we can copy all these three commands together and paste it as well. Let's copy it and wait until this completes. Okay, we clear the screen. Now, I mostly recommend to avoid pasting multiple command at the same times, until and unless you have already did it, you know it works. Then only do that. Otherwise it creates issues and difficult to find out which command through the error. Okay, so it created a directory, changed the ownership of that directory to www-data and downloading this WordPress from this link, extracting it and putting it into /srv/www folder. Okay, if you do ls -ld on /srv/www, you should see this is owned by www-data and you should see the content also inside that, ls -l, WordPress folder, right, inside that you should see all these files. Now here, srv/www/wordpress is going to act the same way as var/www/html, that we have seen in previous lecture. It's not yet configured. But in the next steps we are going to configure this to use this as the document root directory. Okay, let's go to the next step. Alright, so we have to open this file. Yeah, let's copy this file and let's do vim, paste the link, go into insert mode, make sure you're in insert mode, copy this content carefully, everything. Once again, make sure you're in insert mode before you paste, Shift+Insert to paste. Alright, you see that, DocumentRoot /srv/www/wordpress, save one quit. Right, now I did direct copy-paste on the terminal because I know it works okay, but always put it on notepad and then from there you paste it in the terminal. Okay, few commands. I have put them in the notepad and from there I'm copying and then paste it. Okay, so this command is to enable the WordPress website. This is to set the redirect rule and this is to disable the default webpage. And then you have to reload or restart the apache2 service. You can issue reload as well. So let me quickly show you what those command did actually. So if you do ls -l /etc/apache2/sites-enabled, in this folder, you should see a link, wordpress.conf, that points to a file over here. This place, oh, sorry, not this one, available, right. And this is where we have our configuration file. This enabled, this link that got created with those commands, enabled the WordPress website and we disabled the default website. Okay, let's go next. Okay, over here, you have to be very careful okay, this, we are going to definitely put in the notepad first, one command at a time, slowly because many people make mistakes over here because these are SQL queries, right? So let's copy all these commands, copy this carefully. Okay, you need to replace that with some password and let's keep it simple. But this is just for understanding. We don't make mistake in putting passwords, otherwise passwords should be longer and complicated alphanumeric, it's database. Okay, now let's copy. Now this place, you have to be careful, okay. Copy till here, paste it and you have to remove this arrows from here. This is again to give multi-line SQL commands. But let's not do that because many people make mistakes over here. So I'm going to remove them and give one single command, okay. Space, make sure the space are proper. FLUSH PRIVILEGE, every SQL command ends with a semicolon, observe. Okay, so these are all the commands that we're going to run one by one, copy them. First command is going to log into the SQL as the root user. Once you get into the SQL command prompt, copy the second command, which is to create the database, Shift+Insert, database named WordPress. You can run show databases, should see the database with the name WordPress. Then the next command, this will create the user WordPress at local host on the local machine, identified with the password, admin123. Then the next command to grant privilege to this user. So we GRANT, SELECT, INSERT, UPDATE, DELETE, CREATE, DROP, ALTER on the database wordpress.* means everything in this database to the user, wordpress, which we created here. Next command is to reload everything or refresh and then quit. Okay, database configured. Let's move to the next step. Now, how does WordPress know where is the database, what is the username and password that is going to be with this command, let's copy it into the notepad and all the other commands I'll be copying. Oops. Okay, this is good. This is going to copy an existing configuration to its right location. Let me clear the screen, right, observe it's in /srv/www/wordpress, right, document root, this configuration file and then sed command to simply search and replace. But before we do that, take a look at it properly. Database_name_here will be replaced with wordpress. Username will be replaced with wordpress and password, this one, make sure you replace it with the password that you have set for the database. Right, here, admin123, same I did over there, right. Now let's copy these commands and paste it. We can also validate that, open, select that file, copy that file name, and open it with vim or Cat, right. DB NAME here, there should be wordpress, DB USER, there should be wordpress. If it's different, change it. And then DB PASSWORD is admin123. Whatever password you have given, make sure it appears over here, same one. So search and replace worked fine for me. If it's different for you, make sure you change it and then come out of this file. Okay, final thing, CTRL and click on this link, it opens here. Okay, we are going to open this file. So we're going to run the command, sudo, -w, sorry, -u www-data and that file, but vim, wait a second. So open that file with this user through vim editor and we have to look for these entries. Let's find them, come down, it's here, right. We are going to delete all of this through D, D, D, D, D, D. Okay. Make sure no other things are deleted, only those things get deleted. Okay, copy this content. Make sure you are in the insert mode before you paste it. Shift+Insert, pasted it, escape, save and quit. Okay, this is done. The next step is to access from the browser. Let's find the IP address, ip addr show, 192.168.1.10, I can use this or I can use this one also. I'm going to use the bridge IP this time. Enter that in the browser, hit Enter. So you should see now the WordPress setup steps. So you can continue with this setup. Click on continue, give some title. I'll say devops, username, my name, some password and it'll say install WordPress, but email address, yes. Some fake email address I have given and install WordPress. So you can give any different password if you choose to. Alright, I've saved the password. And log in and says, "Welcome to WordPress," right. So this is the admin user which can set up blogs and then other user can sign up. I don't want to get into that part now. Our main focus was in setting the WordPress database. Now if you get any other kind of error, like unable to connect to database or site showing some weird pages or default pages. So run the history command, check what command did you miss. Check your configuration file contents, especially these two things, wordpress.conf and the other one, this one, /srv/www/wordpress/wp-config.php. Make sure the content is correct. Once you make the changes, you can restart the apache service, apache2 service. Make sure the SALT keys, the username, password, database, everything is correct as per the documentation, verify your database steps once again. Log into the database, say show databases. It should show the database named wordpress. Or you can do the whole steps, all these steps, database steps, once again, restart the service and then try once again from the browser. So these steps will definitely work until and unless you don't make a typographical mistake. Alright, then that is all in this lecture. If you are done here, then log out from the instance, from the VM and do vagrant destroy.
