# Vagrant Sync Directories

-: Instructor] Welcome. In this lecture, we are going to talk about a concept called Vagrant Sync Directory. So in last lecture, we powered off our VM and that VM was coming from this vagrant file. So we have this folder, MyVMs, in that we have vagrant file and we have one hidden file. This is hidden in linux.vagrant. And in this file, we have, on this folder, actually, we have VM specific information like the private key, the login key. When you do vagrant SSH, this is the key that is used to login to the VM. So anyways, let me just close this. So in My Vms, we have .vagrant folder and we have a file called Vagrant file. Now let me go from the terminal into that folder. So I'm currently in Vagrant VMs folder. So I'm going to get into MyVMs. And this I have the vagrant file. You see I don't see it in the command line. If I do LS-a, I see .vagrant and vagrant file. Now I'm going to bring up this VM, vagrant up. So when the VM comes up, you see this information over here. You see two path, one is the host machine path, the other one is the guest machine path. From Virtualization Introduction, you remember we have host machine and guest machine. Host machine, in this case, is my computer, my laptop and guest machine is the VM coming up from this MyVMs folder. Now I'll show you how this works. Just remember these two path. So I will log into the VM, vagrant ssh. Okay, I'm in the VM, let me clear the screen. I'm going to switch to root user and go to the folder/vagrant, right? See this, I'm in this folder. Now I do LS and I see the vagrant file. If I do LS - a, I also see the hidden folder. Okay, now I'm going to run a command that will create 10 file in the VM and then you tell me what's happening. So I'm going to run the command touch file and I'm going to give a range, 1..10. I'm going to give an extension.sh. So this is going to create 10 files with the extension.sh in the VM. And observe this. When I ran this command in the VM, I can see the files in the VM, but the file also appeared over here. What's happening? Is the VM leaking? That's what one of my students said once. Now, it's actually not leaking, it's the same folder /vagrant seems to be a folder in the VM, but this is a folder actually coming from the host machine. MyVMs folder is synced with /vagrant folder in the VM. So what do you do in /vagrant or you do in MyVMs will give the same effect. So if I select all these files and I delete them, then these files will be also deleted from the VM because they're basically the same folder. Okay, now let me close this terminal and open the vagrant file of MyVMs. No, not now. You know, let's close this one. Let's open the vagrant file from Ubuntu folder and look at this line, line number 46. You will see this config.vm.synced folder, host path comma guest path. Now with this setting, you can create your own synced folder. /vagrant is a default sync folder, but you can create your own folders also. You can map any folder from your host machine to your guest machine. So in the MyVMs folder, I'm going to create a new folder. I'm going to name it as scripts. And then in the vagrant file, in the MyVMs vagrant file, I'm going to give this setting. Now either I can copy it from the Ubuntu vagrant file or I can use Copilot. So I'm going to select, do Control A, Control I to open the Copilot chat. And I'm going to ask you to create that setting for me. So this is what I just said, add sync folder, host part scripts in the current folder and guestpart/opt/scripts. Let's hit Enter and let's see what happens. Okay. There's something wrong. Config.vm.sync folder. It says ./script, that is correct. But in the guest part, it has given just scripts. Okay, now I can either ask Copilot to just change that or I'm going to say /opt. Oh wait, first I have to accept this. Yeah, then I can change it. Okay, you see that? Now, I mean, you understood the sync folder setting, but did you understand one very important thing while using AI? You need to know what you are doing, right? The people who say that AI is going to completely replace humans, not completely, we need to be using AI. You know, we need to have an upper hand always. We cannot completely rely on the AI tools. What do you think, yeah? Anyways, that's a debit topic again, but you know, I just made my point. Let's come back to the sync folder. You know, let's see if this file, and in order this setting to take effect, we need to reboot the machine. So we are going to run the command to reboot the VM. Now you already know how to reboot the vagrant VM, right? You know the command. But let's ask the Copilot. So let's open the chat. Let's click on this dropdown and say open chat. And let's ask it. Now Copilot has agent and we can access those agents by running @. If you execute @ over there, you see yet a lot of option, you have @terminal agent, @VScode, you have @workspace. Like I need to know some terminal commands. Yeah, I need to interact with the terminal, I can use @terminal. And now with this I can ask terminal specific question like I'm going to how to reboot a vagrant VM. There It gives me one command and either I can execute it in the terminal or I can copy the command. I always prefer copying the command and executing it myself. But let's try this one. Insert into terminal, hit this. and make sure you check whether it has opened the correct terminal or not. We have two terminals, like I have two terminals open over here and I need to execute vagrant reload in MyVMs folder. That's where my VM is, right? So always verify stuff before you execute it. Don't just straight away executed in the terminal. It might execute on the wrong terminal. And also, please take a look at the command, whether it's showing the right command or not. Let's hit Enter. Vagrant reload and they should reboot the VM. Okay, my VM is up and now you see here we see two path, one is the default sync folder, the other one is the one that we have created or mentioned. And now you can do the test, you can go to the vm, go to this folder, /opt/scripts, create files and you will see it'll also start appearing in the host machine in the scripts path. You know what, I'll show it. Vagrant SSH. Okay, I'm going to get into the folder /opt/scripts. Let me clear the screen. There's nothing over here. Let me expand the scripts. It's already expanded. And let's run the command touch python. Oh, you know what? Coder. I'm going to give a range 1..5.py. See, I created in the guest machine and it appeared in the host machine. Same as the previous one. Now I'm going to show you one feature from GitHub Copilot. I'm going to make some mistakes in my vagrant file. So like, let's see, I'm going to remove this _folder. Now make some other mistake, just save it and then I'm going to run the vagrant reload command. Vagrant reload. Okay, make sure you are in the MyVMs folder. Make some mistake, run vagrant reload or vagrant halt and then vagrant up. Alright, so we already know what is the mistake, but let's just follow this one. So it says there is some configuration mistake, the following setting shouldn't exist synce, right? And we found it in our code that this is the problem, but we don't know what is the problem with it. We are just assuming, we don't know what is the problem. Let's click on this one. You can also do the Control I or Command I to open the chat and then ask it. Or you can just click it. Show code actions. It says modify using Copilot. Or you can just select it and just hit Control I or Command I. And we'll say slash. Now, there are /commands provided by GitHub Copilot, like one of it is fix. You have tests, you have generate, you have other things, explain we have already seen previously. Let's talk about fix here. So now we'll ask Copilot to find the problem and fix it, /fix and hit Enter. And there are two main things over here. I'm going to close this chat, close this terminal. Scroll up. It gives the information. It says the method name is incorrect, it should be like this. Do you accept this? I will say yes, I accept this. Okay, it fixed it. And then again I'm going to go to open chat and we see the previous chat, we can just execute it in the terminal, vagrant reload, hit Enter. Okay, same error again. Okay, I did not save the file, sorry. So I need to save it, Control S. Vagrant reload. Alright, so just make sure you save the file before you execute vagrant reload command. Okay, so in this lecture, we have learned the sync folder concept. And with that we have also seen some GitHub Copilot options like /fix, @terminal to find terminal related commands. And we also ask Copilot to find the synced folder setting for us. Okay, for the next lecture, make sure all the VMs are powered off. I'm going to power off this one, vagrant halt, MyVMs VM I'm going to power off. So run vagrant global status. Make sure you power off, remove all the VMs. And once you're done, join me in the next lecture.
