# Summarize

Instructor: All right, so let's take a look at the architecture diagram once again. The user accessed the load balancer, which is through the correct host path, then the ingress will match it to the Tomcat service. Tomcat service is going to forward the request to the pod where we have our vProfile application running. That needs connectivity to the backend service like database, so it's going to connect to the DBService. And our vProfile application has application properties file where the information is mentioned of the backend services. In this case, literally the Kubernetes service information, like WebPro DB, port 3306, right, like that. So with that information it's going to find a DB with the Pro DB name, right, connect the service. Service is going to forward the request to the pod, database pod. And the data is stored in EBS volume, that is claimed by persistent volume claim and created by storage class. The DB container also requires the password to run MySQL root password. That password is saved in the secret. Okay, so once again, go through all your definition files, compare it with the architecture diagram, make sure you understood everything. I'll also recommend you draw yourself these diagrams and, you know, write your own information. It'll be easier for you when you sit in the project meetings, when you want to really containerize applications. And also very helpful when you're talking in the interview. Okay, now it's time to wrap up stuff. So first we need to delete the ingress controller. Okay, so the command that we use to create the ingress controller. In the same command, we have to just do kubectl delete instead of apply. So that is going to start deleting the controller. Okay, this is going to take some time. Okay, it should also delete the load balancer that was created by it. Okay, next, let's delete everything that we created, one simple command. Let's go to our repository, kubedefs, and we'll say kubectl delete -f, dot. That should delete everything including the PVC. And when we delete the volume claim, it's going to also delete the EBS volume. Okay, once you're done, you can delete the cluster. You can say kops delete cluster, find the previous command, or use the kops validate cluster command. Instead of validate, just put delete and say --yes, and hit Enter. And that is going to take some time, like a lot of time, to delete the entire cluster. And if you're done here, you don't need the Kubernetes cluster moving forward, then you can also remove the hosted zone from route 53. And kOps EC2 instance, I recommend you just shut down and keep it, you know, because there's a lot of prerequisites we did over there. But anyways, if you don't need it, you can delete the kOps EC2 instance also. You have the lecture, you have all the resources, whenever you want, you can create everything from the scratch. So if you're not going to use it for a long time, I recommend you just simply delete everything. Just delete the Kubernetes cluster, after that, delete the kOps instance. Route 53 hosted zone, also makes sure you delete. Okay, and that was all. Right, I know it's long, there's a lot of things that happened that you know, came together over here in a very short time. But if you do it few times and you know, if you make sure you know the architecture, you know the application, like vProfile application here, now you know the basics of Kubernetes. You can anytime do this. All right then, it was great. I also really enjoyed recording this lecture. Thank you so much for watching and completing it. I will see you in the next one.
