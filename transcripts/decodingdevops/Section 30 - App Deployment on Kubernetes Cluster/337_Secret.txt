# Secret

Instructor: All right, so let's get started now. In this lecture, we are going to write a manifest file to create secrets, which is going to store two passwords, one is for database, the other is for RabbitMQ. And I'll tell you why. Let's check the docker-compose file that we used previously. So while we run the MySQL container, the database container we mentioned the image, we need to set this variable, that is mandatory, MYSQL_ROOT_PASSWORD, and it has a value that we have given. Now, this is the database password. We cannot directly put this database password like this for productions. Also in test and staging also you should never put, but in the source code, you should never have the database password. But we need to supply this value, so what do we do? Well, whenever you have such kind of things, then usually you encrypt it. And the encryption key will be somewhere else. So even if someone looks at it, they will see this gibberish values, which they will not be able to decrypt. Now, we are not going to do exactly same thing, encryption-decryption, but we're going to do a similar approach. And with that approach, you can understand we'll be doing encoding and decoding. And encoding-decoding does not need the encryption, the keys. Encoding-decoding is actually not safe and not recommended for production. It is just to prevent accidental exposure, that's all. If someone sees the encoded key, they can a very easily decode it, which we'll see in a moment how we can encode and decode it. So idea in this lecture is not to encrypt, but to prevent the accidental exposure of our password. In Kubernetes, we can use a secret object, which can secretly store our information. And that information can be injected in the pod. So let me show you some example here. Okay, first of all, the types of secret, so opaque, this is what we will be using. This is just encoding and decoding with base 64, nothing so serious. But you can use encryptions with TLS and tokens. You can also store the dockerconfigjson file, which can have the authentication with the Docker Hub account. SSH authentication also you can store. So as I told you, we will be just using opaque. Okay. So, coming back, we have RabbitMQ. We also have the RabbitMQ password, that also we need to encode. So there's two things. So let's write the definition file and see how we can encode these values and store in the secret. So expand kubedefs here and click on the secret.yaml file. Okay, so apiVersion, v1, and kind, Secret. Remember the V is capital again. Then let's write the name. Let's see, we have to give the metadata, naming the metadata, metadata. So name will be app-secret, and the type of this secret is Opaque. Opaque. Okay, now with data, we are going to store the value. We'll give variable name as db-pass, one, this one, the other one will say rmq-pass. Okay, so as I told you, we cannot give this in clear text, right? So we need to encode it. So what is the value of our database password? Well, let's take a look in the source code. src, main, resources, application.properties file, vprodbpass. So this is the branch we'll be using or the Docker image that we built in the containerization project. We use the containers branch over there. That branch also has the same configuration. This branch is just a copy of that branch. So there also you have the same files, you don't need to worry. This will be the same file in the Docker image. And in that we have vprodbpass as the password. So we have to encode this. So open the Git Bash or your Terminal if you're using macOS. Queue this command echo -n, in double quotes give the password, pipe it with base64, and you will get the value. This is the encoded value. So instead of giving vprodbpass, he'll be giving this encoded value. And the secret of type opaque, understand that it is encoded with base 64. It's going to decode it with base 64 only. Okay. So make sure you give that db-pass, colon, and the encoded value there. For RabbitMQ, the password is guest. So let's encode that also. Okay, and that's the encoded value for guest. Let's copy that. Okay, so I'm going to do Control+S. So this is the secret manifest. We have two values over here, db-pass and rmq-pass, and this is what we'll inject in our pod definitions.
