# Ingress

Instructor: Welcome. In this lecture, we are going to create ingress for our Tomcat service. So we know service of type cluster IPs for internal communication. It gives an endpoint for the pod. So you can have multiple replicas of the pod under one service, like a load balancer, but this service is internal load balancer. Now, Tomcat application, our application running in the Tomcat needs to be hosted publicly. So there will be an external communication coming in. So we need an external load balancer and that load balancer should route the request to our internal service. In Kubernetes, that can be done by an ingress controller. Right, so let's say we have a load balancer like application load balancer in AWS, and then we have Kubernetes cluster in via service. How does it connect? How do we connect? We connect the max, connect the application load balancer and forward the request to the node. But when it comes to the node, it does not know what to do with it. So the ingress controller, right, ingress controller, can set up that communication, can manage the external load balancer for you and connect it with the internal load balancer. We're going to use ingress controller, NGINX, NGINX Ingress controller, we will be using and that is going to create an application load balancer for us, and then we can set the Ingress rules. Creating ingress control is pretty easy. You download the definition file and simply run it. It creates all the pods and everything for it. Basically, it gives you a ready-made automated set up where you can just write the Ingress rule and decide what redirects where. So we will create an Ingress rule that will say if the request is coming for, let's say, vProfile.hkhinfotech.com, then route the request to the Tomcat service and the Tomcat service is going to route the request to the pod. So this service will be the Tomcat service of type cluster IP, right? So this definition file is going to create a service of type cluster IP internal, and Ingress is going to forward the request to this service. That in turn, is going to forward it to the pod with this label. Okay, this is one example of Ingress. Ingress, you can have variety of rules. For example, if the URL is coming and it's path at the end is /testpath, then the backend routed to the service with this name and at this port number. So you can do path-based routing, you can do URL-based routing. Right, here's one more example. You can have multiple rules, let's say for foo.bar.com, if there is a slash bar at the end, then redirect it to this service. But if there is a slash forward at the end, then redirect it to that service. Okay, and the API version is networking.k8s.io/v1 kind ingress, and then you mentioned specification, the rules, basically. So let's create an Ingress rule. So go to this file appingress.yaml. Okay, so first apiVersion networking.k8s.io/v1 kind ingress metadata, name of your ingress. So I've given name as vpro-ingress. This annotation, this is the skeleton. Okay, specification, so Ingress class name will be nginx. We will create an ingress controller. Its name will be nginx, nginx ingress controller, that we'll do when we set up the cluster and you can have multiple ingress controller in your Kubernetes cluster. We will be using nginx ingress controller, we'll create that and we'll say, "Now use this ingress controller." Okay, and then you mentioned the rules. So you just see rules. So you can give any number of rules. We just have one rule over here. A number is you have multiple service where you want to route the request from external. So we can give multiple rules. We just have one rule which is just one requirement that if the host name is this, like in the URL, if it's this. So I've given here, vprofile.mydomain. You have to make sure you give your domain over here because we are going to add this rule in the GoDaddy, in the domain registrar that this vprofile, this domain matches to, the load balancer endpoint. This will make more sense when we actually do it, but you know, this basically, the host-based routing. Then we give the protocol http and in that we mention the path and everything. Inside that, paths:, again, path-based, we can have multiple paths. So we're just going to say path /, that means just this host name /. Now after that, there is no extra thing. Just if the user enters this, then route it to the backend, right? So in that, you give the backend. So backend is the service and the name of the service, vproapp-service, right? That's the name of our service, okay? Routed on port number 8080. So this is again, path-based routing only, but we're giving the host name. We're not giving any extra path after that. You know, if you have something like you know, / login or / video, so you can create multiple rules like that. So under same external load balancer, one external load balancer you can have multiple rules like that set through ingress, right? This is an amazing thing for microservices. In microservices, you have many APIs that you need to expose to the outside world. So you can have all that rules of your APIs, especially now, path-based. You know, / videos goes here, / payment goes there, can have a separate, separate cluster of your services, pod clusters of your service, right? Anyways, we just need one and we have created that. So save this definition file and next lecture, we'll commit this source code into the repository and then we're going to launch a Kubernetes cluster, fetch all this data over there and run it, right? And that was all in this lecture. See you in the next one.
