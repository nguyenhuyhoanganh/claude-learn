# Deploy App on K8s Cluster

Instructor: Now before we go to a repository and execute all the definition files, create our manifest, we need ingress controller. This ingress controller is going to create a load balancer for us. So we need that first. Okay? So from the lecture resource, copy the command to create the ingress controller. So Kube CTL apply HNF, and this is the URL. This deploy file is going to create an nginx ingress controller for us for AWS, and this ingress controller is going to manage the load balancer for us. So just copy and paste this command, hit enter. Okay? This will create a namespace Kube CTL get Ns. There is a ingress nginx namespace. We need to make sure all the pods, everything is running in this. Okay, so let me check all the pods in this namespace is running or not. Okay, these are completed and this is running. Let's go and check in AWS console. So should have our control plane and worker node. And now we should have in the load balancer section, we should have an application load balancer. There, you see that, it's provisioning. It's not yet completed. So that is created by the ingress controller that we just created. So we need to wait for some time until it becomes ready. Once this is ready, we can start creating our objects. Deploy ingress, our PVC, right? So let's go. Okay, let's do Kube CTL get nodes, right? So everything is working Kube CTL command is working. Now let's go to our repository. We cloned it. Let's go into that. In that we have Kube defs, go into this folder and here we have a PVC, CAT db PVC. So if I execute this on Kube CTL, this should create a three GB of EBS volume for me. Let me do that. Kube CTL create -F db PVC. Okay. Now let's go to the EC2 console. Volumes. Where is volume? Volumes. Okay, you see this volume and just create it. See the name, you see the PVC there, some ID there. Three GB and created time should be recent. You should see this created time. Recent status will be available. It is available, it will be connected. When we claim this, I mean when we put this claim in the pod definition, when we run the pod definition file's going to connect that this EBS volume to our pod. Okay, let's check the load balancer. Okay? It's in active status, it's time to create everything together now. Let me clear the screen. Make sure you're in this folder. You do Kube CTL create - f.. Now PVC is already created, so it's going to simply ignore that PVC and create the rest of it. Enter, enter. You see we got an error there. DBPVC already exists and we are going to wait for some time until all this becomes ready and available. Then we'll start validating everything. So I'm going to pause my lecture and resume in 10 minutes. Okay, so here I am back. Let's check everything. Let me clear the screen. So let's check the pod, first Kube CTL, get pods and all the four pods are running. That was created by the deployment Kube CTL Get deploy, again, we should also have the service Kube CTL get SVC. Okay, these are the four services that we created, okay? If you find error anywhere, especially in the pod side, do Kube CTL describe, for example like this Kube CTL, describe pod and the webpro DB pod, complete name copy, hit enter. And at the end you should see all the events. So if you made some mistakes, like if you mentioned the wrong image name or anything else, you should see it in the messages. And when you fix your code and you found there was some mistake, you fix the code. After that, you should delete that part. You cannot delete directly. You have deployments, you need to delete the deploy or better, you can just go in the source code, right? You can just say Kube CTL, delete - F and queue the file name and it'll delete the resource that's mentioned in the file. Like maybe I want to delete this one. DB deploy. All right, so I'll do like this and we'll delete the DB deploy, and then I fix the changes I make no, correct my mistakes. And then I again say Kube CTL, create or apply - F and the file path. Okay? Once again, let's do Kube CTL, get pod. Now also, let's see how to match the service if the service is connected to the pod or not for that. We need to get the IP address of the pod. So we'll say Kube CTL will describe pod, and let's say app pod. Let's describe this and let's look. Scroll up. Okay, over there, the top right here, you have the IP address of the pod and the container port is 80. Okay, there it's mentioned. Okay, now let's check the server. Let me clear the screen. Right, so this was the IP address. So I'll say Kube CTL, get SVC. Now I'll say Kube CTL, describe SVC and service name. So in this case we are pro app - service. Now, if this is a functional SER service, that means redirecting the output properly. So you should see the IP address in the endpoint. You should see the IP address of the pod. See that? And the container port 8080. If you don't see this endpoint, then definitely there is some mistake. The mistake could be in the label selector in the selector. Most of the time it's the selector problem that you have given the wrong label name. So once again, if you understood what is the problem, you fix it in your definition file, then you delete that service in this case and create the service once again with the changes. Okay, so for the DB pod, we have created the PVC, right? It should say Kube CTL, get PVC. We should see here the PVC name. You should see some ID there. And this should match with the EBS tag that you see in the EC2 console. Okay? 3 GB of size. Now, PVC is a claim, it just doesn't, it cannot directly go and cannot create the EBSO. And that is created by the storage class. So if you do Kube CTL, get SC, storage class, I should see a storage class here. The name is default here. That's the actual name and is ebs.csi.aws.com, the provisioner. So this is kind of a driver, right? It uses that behind the scene mechanism to create the EBS volume. So in the PVC, when we see the storage class is default, this is what it means, right? In the DB PVC, we have given storage class name default, right? Instead of default. You can also give this name over here, this one, but should not, you know, this might change at the time you create the Kubernetes cluster with Kops, this might be a different name. So there will be one default storage class in the cluster. When we create Kubernetes cluster with Kops, the default storage class will be this one EBS. Okay? Again, this should not be a big concern for us. This should be definitely managed by the storage administrator or the administrator of the Kubernetes. So we should know the storage class name. That's what is the most important part for us. Okay? Now last thing, Kube CTL get ingress to see whether this is working or not. If you do not see this information over here, that means your ingress is not working. Okay? So we created the ingress controller previously. Let ingress controller created the load balancer. Right? Now, when we created the ingress, the ingress job is to map that load balancer to the endpoint, right? And connect it to the server. So we described this ingress, you should see here the address. And in the backends you should see the service name and the service number, and that's the IP address of your servers. A service will also have the IP address. Okay? Now, in order to access it from the browser, we need to give this URL, but this URL does not match us with the load balancer. I mean in the world we need to give in the domain registrar, we need to mention that this name means this endpoint. I'm going to copy this endpoint, log into your GoDaddy account or wherever the domain registrar that you have used. So I'm going to go to the DNS setting of my domain, right? That's the domain. That's why I've given that in the ingress rule. And here I'm going to create a record. I'm going to say C name V profile matches to this endpoint. This is the load balancer of the, sorry, this is the endpoint of the load balancer application load balancer. You can check, it can compare it from here also. Okay, that one. Okay, let's create the record. Let's save it. So this record is created, if I say vprofile.HKHinfoteck.xyz this should resolve to this endpoint, right? And just this is not enough. There should be an ingress rule as we created. So when the request is coming to this ingress and the UR your requesting is the host name is this, then only it is going to route the request to this service. If you just access it directly with this endpoint, it's not going to work. The ingress is going to just stop the request there itself, 'cause there is no rule that says this endpoint should match with anything. The rule is the host name is this and part is slash then forward the request over here, right? You can try that. You just enter that load balancer endpoint. You should get this. But if you enter the proper host name and path, right? This is the host name and path. If you enter this, then it'll work. Okay? So http :// and the host name that you're given in the ingress, right? Enter, there you go. Right? That is going to forward, the request to the service. Service is going to forward to the Tomcat pod and this is where it's coming from, right? Let's log in and test admin vp. Now this should validate the DB service and the DB pod and also the PVC that is connect, okay? Through and through. Very verified everything you can say except Rabbit MQ and m cache. But you know, from start till end, from the user request, till the storage EBS still there. We are verified by just logging in here. Yeah, let's check RabbitMQ. Let's click on that. You should see Rabbit MQ initiated. Let's check the all users for M cache. And you should see the user list. Let's click on a user, should see data is inserted in cache. If you click that user, once again, you know what's going to happen. The data is coming from the cache, right? So this is a whole application setup running on the Kubernetes cluster. We have ingress, we have storage in the middle, we have all our objects running, the pods, the service, everything. Okay? So in real world, comparing it with the real world first, you should be able to containerize your application. Once you containerize, you should be able to write the manifests, right? The Kubernetes definition files for your application. Once that is done, there are other things like helm and CICD, and more things. Okay? But that should be it. In the next lecture, we'll be talking about cleanup. So wrap this up and join me in the next lecture.
