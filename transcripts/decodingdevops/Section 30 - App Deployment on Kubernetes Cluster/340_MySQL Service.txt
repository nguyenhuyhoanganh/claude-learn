# MySQL Service

Instructor: Okay, so the deployment definition creates the pod, and that pod needs to be connected by Tomcat service and also Memcache service. So in Kubernetes, we do not connect pods like this directly, right, we don't give the pod IV because pod are volatile, right, they get deleted, recreated through deployment, so we cannot rely on the pod IP, so we create a service for them, like a load balancer, internal load balancer. Okay, an internal load balancer in AWS, sorry, an internal load balancer in Kubernetes is called a service of type cluster IP, so I just search for it, I got this generative text, I just copy it and just paste it here and then I'm going to start changing the names and the port number also, first is the name, I'm going to give the name of this services vprodb, this is very important, reason I'll tell you. We already discussed that, or we already seen multiple times in this project set up, Myskill will be connected by our Tomcat application or v-profile application is going to connect to Myskill service through application.properties file, the application.properties file has information. Okay, if we go, once again, source, main, resources, application.properties file, right? So application.properties file, says the Myskill service name is vprodb, this needs to resolve into the IP address. So in our Tomcat image that we'll be using for this project has the same application.properties file, it's going to, our v-profile application is going to look for a DB that is with this name vprodb and port number 3306. So we need to be careful while giving the service name, this will be the service name, the service port number, right, so coming back to here. So that's why our service name will be vprodb, it's going to look for a service name, vbprodb, okay, then specification, selector, so find a pod that has so and so label, so our DB pod has this label, app: vprodb, that's how we connect the service to the pod and we give the label. So any port that has this label, app: vprodb, it's going to route the request over there, on port, 3306, so port's here to mention the port number and the target port, okay, so this will be the target port, here we need to give 3306, but I have given here a name, vprodb-port, the reason is, in the DB deployment definition, if we see container port 3306, we have given this name. So like that we can give name to the port in the pod, right, it's a very good thing, you know, really, if you wish to change the back, the port number of your service, you don't need to make changes in the service, right, here you don't need to make any change, you can keep the name as it is in the backend, you can keep changing the port number if you wish to, now only if you build the image like that, anyways, leave that concept. We can give here the port number 3306, so we can give the name of the port that we have given here, okay, this is the front end port, the service is going to listen on port 3306 and route the request to the targetPort, type: ClusterIP internal. Okay, let me restructure it a little bit, okay, selector I have given here, alright, let's save this, okay, great. So we have the service, we have the DB deployment manifest, and we have the PVC, so database is all set, now let's move on to Memcache and then RabbitMQ and then we'll go to Tomcat. So complete this and join me in the next lecture.
