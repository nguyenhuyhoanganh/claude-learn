# RabbitMQ App & Service

Instructor: Okay, in this lecture, we are going to write RabbitMQ definition file, and the service for that. So let's click on this rabbitmqdeploy.yaml, and let's copy everything from dbdeploy.yaml except we'll remove the volume part. The rest we will need. So, we don't need any initContainers and volumes. Secret, we'll need. We don't need to mount the volume anywhere. Okay, now let's change the name from vprodb to RMQ. Here, here, here, here, and here. Now I'll remove this 01. It's not really required. VPRORMQ. Okay, that's the name I'm going to give. VPRORMQ. Vprormq, vprormq and here also. Now this name is not referred anywhere outside except in the service. But application properties file, the name that is given, that is of the service name. So, service name really matters based on our application properties file. But the pod does not matter. We can give same everywhere here. Okay, now image will be RabbitMQ official image, and official RabbitMQ image name is just RabbitMQ. Port number is 5672. And let's give a name. Vprormq-port. Okay, though now we have variable, we need to set two variables. If you remember the Docker Compose file, just open the Docker compose file. For RabbitMQ, we need to set this to user RabbitMQ default user and RabbitMQ default pass. So password, we have stored it in the secret, we can use that one, but this variable we have to set. So environment name of the variable RabbitMQ default pass. Value from secret key reference, this is the same secret, but the key in that secret. Let's check that. It's rmq-pass. Rmq-pass over here. We need to set one more variable, RabbitMQ default user, and that we can just give it. Yes, it's value directly over here. So just hyphen name, the variable name, value, guest, but here the value is coming from the secret. Okay, that is all. Let's just save it. Okay, now let's create a service for this one. So MC RMQ service, let's open this file. And I will open this MC service file because I'm going to copy everything from here. And I'll go to RMQ service file and paste it and let's make changes. First the target port name, let me get some RMQ deploy file, the container port name is with prormq-port. Let's copy that and give it over here. Make sure you're copying things in the right files. Rmqservice.yaml file. This is what we are editing. Front end port will be 5672. And the service name depends on what's in the application properties file. So the service name here is or the RabbitMQ address is with vpromq01 and port is 5672. Let's go to rmqservice.yaml file. And we have to make sure the service name is same what is in application properties file and port number 5672 that you already given. Okay, selector. So let's go to RabbitMQ deploy file. Let's get the label of the pod. That's the label of the pod. Now let's make sure that is the label we give over here. There's the selector. Once again, remember in the service, the service name should match with the configuration file, its front end port should match with the configuration file. Back end information should match with the pod information. Right, like the selector, the back end port number, which will be the container port of the pod. And type is again cluster IP because this is for internal communication only. Save this once again, match it with the code in the Kube app branch, make sure it's same and make sure you did not make any change in the memcache service file or any other file. If I made such changes then revert it back. This lecture we're just editing rmqservice.yaml and rmqdeploy.yaml. Okay, so validate all those things, naming convention, port number, everything checked properly. If everything is good, save it and then join me in the next lecture.
