# Tomcat App & Service

Instructor: Okay, in this lecture, we are going to do our final service, Tomcat. Our VProfile application lives in Tomcat. We have a Docker image already for that. In Docker Hub, in the repository, we already created these images in containerization project. So this image, basically we just need to fetch it and run it. And we need to put a service in front of it. So this is going to be very simple. So for an example, I'm going to use Memcached deployment service and Memcached service. I'll copy this content to first, this content, that's copied. The deployment content, mcdep.yaml, content copy. Open appdeploy, and just paste it, and let's make changes, first, the names. So we have the name as vproapp. So let's make the changes there, vproapp. Also the port name, vproapp-port, and the port, containerPort is 8080. In containerization project, we have built that image, stored it on Docker Hub, and we know the service, Tomcat there, runs on port 8080. That's why the containerPort, 8080. Let's give the image name. Okay, so that is the image vprocontainers/vproapp. Let's copy that name. Right, and that is all really, but just to make it a little more interesting, I'm going to put here init containers. Reason? Well, I want the backend services to all up, DB, RabbitMQ, Memcached. All these services should be up before this pod comes, because we are going to run everything together. I don't want the Tomcat app to come before the backend service comes. So what do I do? Well, we can use init container. There are other ways from Kubernetes also. There are other ways to create a link like that, but I'm going to use here init containers. Init containers are the container that comes up first before the main containers, right? Init container, we'll use BusyBox image, and we'll say, you know, run a command that checks for the backend service if it's up or not. If it's up, then it will complete the command. The command will be completed, and then the container will be dead, and then this Tomcat container will start. Okay, so the init container, the command. It's very complicated, but you know I have taken all this directly from the Kubernetes documentation. I'll show you. Okay, so there is this example. You can just Google. You can just search Kubernetes init containers, and you can come to this documentation. Here, there's the example, right? So the concept of init container is, these containers that you mentioned in the init container, comes up before the main container. This is the main container. It comes up, it executes its task. When the task is completed, this container will be dead. And then the main container start, so BusyBox is the image. It runs a container from that and runs this command. This command is an until loop, Bash scripting. In Bash, we have for, while, and until loop. Until loop is going to run until the command that you have mentioned in that completes and gives you the right output, exit code. In Linux, we have seen the exit code. Zero is the success, and non-zero is a failure. So in until loop, it's running an command, nslookup, which is going to try to resolve the IP address of a given name that you have given. That's what nslookup does. We do nslookup to a URL. For example, we do nslookup kubernetes.io. This should resolve to an IP address, IP address, or any other name that it has. And when it resolves, in Linux, the exit code will be zero. Any command successfully completes in Linux, its exit code is zero. Command fails, it's exit code will be non-zero. So we'll do nslookup and the backend service name like vprodb, right? Literally the Kubernetes service name vprodb we have. And when it resolves to an IP address, it resolves to the backend pod IPs. Then it will return the exit code. Now what that will do is that when it resolves to an IP address, that means the backend service is up and this until loop will complete. The until loop will complete, it is going to complete this command and this container will be dead. So it's the same one. I've just made some little changes here. I've given the service name here, vprodb, dot. Now just the service name will not resolve to the complete name, we have to give vprodb. And this here is a command substitution. This is going to return the namespace where it is running. So it will be vprodb, dot, the namespace, .svc.cluster.local. So this will completely resolve to an IP address if the backend comes up. Until it completes, it's going to keep printing this message, waiting for mydb. Sleep two seconds. So every two seconds, it's going to run this loop, right? We keep giving a command there. Sleep two seconds. So it's going to execute that nslookup command, then execute sleep for two seconds, right? If it doesn't work, then it's going to run that command again, again and again, until this resolves to the IP address. So I kept it for vprodb and vprocache01. If you want, you can keep it for RabbitMQ. This is just an example, it's not really mandatory. Our application, VProfile application, will come up without any problem, but many application like that, depends on the backend service to come up first. Backend should be up. Then only the frontend service comes up. Let's also add the RabbitMQ also over here, oh wait. I need to give the RabbitMQ service name over here. My RabbitMQ service name is vpromq01. Vpromq01. We save it. Okay, and that's done. Save it. Yeah, and also make sure you have given the right name, right? Image name, port number, port name, map it once again with the kube app, branch and check. If everything is good, then we are going to set up the service. Once again, pretty simple. Let's go to appservice.yaml file, let's copy everything from mcservice file and put it over there. Change the name and the port number. So vproapp-service. Again here, the name really does not matter for now, because we're going to create an ingress controller. Ingress, sorry. There we're going to mention this name, vproapp-service is the service name, where it needs to route the request. Okay, frontend port, we are giving 8080, backend port, targetPort also, I'm giving 8080. Or I can give here the name of the container port, right? That we have given over here. We can give that also. But for this one, I'll keep it as 8080. Selector, app, vproapp. That will be the same label, which is mentioned over here. And type ClusterIP, of course, internal communication. Now this will be for ingress. In the next lecture, we're going to create ingress, that is going to connect the load balancer to this service. So save this, and join me the next lecture.
