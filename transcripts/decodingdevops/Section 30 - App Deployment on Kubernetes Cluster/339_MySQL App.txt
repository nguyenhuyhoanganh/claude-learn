# MySQL App

Instructor: Okay, so I'm again and again saying POD, db pod, Tomcat pod this, pod that pod. But we are not actually going to write the pod definition file. We need deployment definition. So if our pod fails or if you want to upgrade our pod, we can use the deployment object, right? If the DB pod fails or whatever pod is managing, if it fails, it's going to recreate that, right? As a job of deployment, you want to update a newer version of your image. Your container image. You can give that image tag to the deployment and deployment will replace it. The, you know, delete the old container and create a new container with the new image. Okay? So copy a deployment definition manifest from either Kubernetes definition file from any generative AI like Chat GPT or anywhere else. I just put it Kubernetes deployment YAML, and I get this, I'm going to use this skeleton and write it from the scratch. So just paste it here and then just make the changes. Okay? First of all, the name, I'm going to give it name as a pro DB also labels, it's important for a service. So when we create the service for db, we're going to say find a part that has so and so label app with pro db label is going to connect that service to this bot. Okay? Replica, we just need one single, we cannot have replica for database. That's a whole different concept that can be managed by still full set, okay? Selector, same app web pro db, right? So any pod that has this label is going to be part of this deployment definition. Okay? Then the template of our pod. So metadata labels, again, same. I'm giving web pro DB. Spec, now in this, the container information, so I'm going to give the name as web pro DB to the container, image will be vpro container / vprofile db. This image I built in the containerization lecture. So you can use the same image or if you have built your own image, it's running with Docker composed, then give your image that will be better. If you don't want to take any risk, you can give my image that's a public image, it'll fetch it and run it. Okay? Ports will come there in a moment. Oh, you know what? Let's give port a name. So - name : the port name I'm giving Vpro DB hyphen port and the container port with P capital 3306. So my skill service that runs in this runs on port 3306. So that's why we are giving the port 3306. Okay? Now as we discussed previously, in order to run this My SQL service from this My SQL container, we need to mention the My SQL root password. Otherwise it does not run. So we need to set an environment variable over here. My SQL route password, so ENV in the same line as Port. Env:- name, the name of the variable and value. Now we cannot give the value directly, right? We created a secret. So value will be from the secret. So that's why it says value from secret key ref name of the secret app hyphen secret, right in the secret, you see the name of our secret is app secret. And in that we have the data DB pass. We need to mention that, right? So secret, key reference name, app secret, and the key DB pass. So it's going to take its value, which is encoded, decoded, and then inject it into this container. So if someone sees this deployment definition file, they won't be able to see the password. So that's the idea. Preventing accidental exposure. That is the only way, only reason that you should use encoding and decoding. Otherwise you should do encryption by using keys in AWS. You can use AWS KM as keys, but they're not free and there are definitely other methods also, but you should always encrypt your data. Okay? Now, so that is the container information. We need to also mention the volume. We need to connect the volume. PVCV, right? Where is the PVC, DBPVC. Its name is DB PV claim. We need to map that with the var lib My SQL. So first we need to mention here the volumes. We need to create the volume that is coming from the persistent volume claim. Okay? So in the same line as the containers, we are given containers and container information we are given here, like then the same line volumes name of the volume I've given as WEBPRO DB data and persistent volume claim, claim name DB PV claim. So this claim, right, this claim which is of three GB, this persistent volume claim of PVC insured is going to create three GB of volume for us. And then we are going, we are saying here that this PVC is this volume. We can say no, we are just giving it a name, right? So we can map it. Now this is the volume that we need to map it with var lib my SQL. We need to mount it to the pods. Var lib my SQL folder. So just below this image I'm giving here, volume mounts, Mount Path Valley MySQL name is this, this volume, right? This volume will be mapped to var lib MySQL. So what happens is it runs the container in our case MySQL service and it has a folder var lib my SQL that it'll connect it with this volume, which basically is the PVC. It gives it three GB of volume. So this three GB of volume gets connected to var lib my SQL okay? Now there is one known issue with this EBS volume. When we create the EBS volume and when we format it, it creates a folder called as Lost plus found. This is common in Linux. Any new partition you create you format, it creates there a folder called Lost plus found. Now what's the problem with that folder? Well, the volume should be basically empty when the my service is coming up first time, my var lib my SQL folder should be empty, right? But what's going to happen that EBS volume will be connected to validate MySQL. That volume will have lost plus found folder. So the MySQL service will say there is already some data in that. So it's not going to start the service. So we need to make sure var lib MySQL is completely empty folder and it won't be because the volume that is coming from EBS will already have a lost plus found folder. So we need to make sure before this container comes up, we take that volume and we remove that lost plus found, right? So before this container comes, so we need to have an in it container. We need to create a container in the same pod that maps the volume, removes the lost plus sound folder and then releases it, okay? So at the end we'll create an in it container. So in the same line as the volumes, the containers volumes we give in it containers. And here you give the name of a container. So I'm going to use an image called Busy Box. It's a very basic Linux image, okay? So -name, busy box image, busy box, latest. Again, it's a very basic Linux image. Just simply runs a container from this Linux image and we are going to map the volume to this. Okay? So volume mounts, same as what we did in db, right? So we give the name WEBPRO DB data, which is the persistent volume claim and map it to var lib MySQL. Basically we can map it to any folder here. It's not a big deal because our intention is just to remove that lost plus found folder. And so we want to run a command RM -rf, you know, lost remove, lost plus found like that. So we're going to give here a command with arguments. So ARGs in square bracket, RM,RF,the path, okay? It should be always, the command should be always in this format. Same like Python list. We have seen similarly, rm-RF and the path. Make sure everything is in the double code and separated by comma. Okay? Once again, this var lib MySQL. We can give any other folder. Also, it doesn't matter, but I just want you to make a point, right? When var lib MySQL connects the volume, there will be a folder lost plus font. That's why I've given this path. Otherwise it can give any path here, right? Make sure the mount path and this path is same here. Okay? Now what's going to happen when we run this, it's going to, before it brings the DB pole, it's going to create this container, map the volume, remove this lost plus found, and any container will be dead. It executes this command. Its job will be done. The container will be dead. Then it is going to bring up the DB part. Now DB part is going to again mount the volume to my skill and it'll be empty because you already removed the loss plus font folder. Okay? That's so difficult to say in the same line again and again, but you got my point, right? The whole intention of using any container here is to remove the folder in minuscule, okay? And that is all. Once again, if you don't intend to write all this definition file, you can just copy it from the Kube app folder. Just make sure you understand all the options that we are using here, okay? And when you do that, just save this file. Alright? That was DB deployment and we did with connected with PVC. And then we also have the secret. In the next lecture, we'll create a service for this one. So it can be connected internally. See you in the next one.
