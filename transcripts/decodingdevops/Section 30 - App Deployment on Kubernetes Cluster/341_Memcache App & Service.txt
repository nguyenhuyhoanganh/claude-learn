# Memcache App & Service

Instructor: Okay, so in this lecture, we are going to set up Memcached pod and also the service for it together. Because Memcached is pretty easy, we just need to fetch an official Docker image for Memcached D and run it in the pod. So you can just copy the basic structure of Kubernetes deployment YAML file, go to your Memcached deployment YAML file and paste it. Just give the name, the image name, Memcached D from Docker hub and port 1 1 2 1 1. Okay, so let's change the name here and the label in the metadata. Okay, so given name as vpromc, like Memcached. Replica will be just having one, we don't need extra. Match label, same here. Vpromc and here also vpromc. This label and this label should definitely match. Then only this part will be the part of this deployment vpromc deployment. Here you can have different name if you wish to, but this and this should definitely match. Then only this pod will be part of this deployment. Okay, now container specification, very easy, just image and port number, that's what matters. Okay, so the name of the container, vpromc. Image Memcached D is the official Docker image. It's going to fetch it from the Docker hub. Port container, port 1 1 2 1 1. That's the port number for Memcached D service running here. And we've given the name to this port that will give it in the service definition of this file. So for service definition for this port, we need the port name and we need the label for the selector. Okay, let's take the dbservice file. Let's copy all the content and go to mcservice and paste it here. And let's change the names, port numbers, and labels. So name of the service depends on the name. What is there in the application properties file. I'll keep this application properties file open over here. So Memcached, the name is vprochace01. The same name we have to give to the service so it can find it. This is mcservice YAML file. I just copy every content from the dbservice file. So that's why you see DBDB here. Okay? Make sure you don't edit the dbservice file. This is the mcservice file you need to edit. Okay, port number, we know 1 1 2 1 1. That's the front end port of the service. So it's going to look for a service with this name on port 1 1 2 1 1. So this should be all the front end information of this service. It's the front end port and the service name. Now the target port. So in mcdeployment definition file, we have the target port name as this vpromc port. So that's where it should route the request. Selector. Selector will be this. Alright, so you see if you know how to set one service in Kubernetes, I'm talking about the whole service part, like the database, the pod deployment service, everything volumes. If you know to do set up one complete service, then other service is similar. Just copy, paste, make the changes. The information matters what is available in the configuration files. Okay, so see if this file control S, control S. Right? Take a look. Once again, make sure if you know this should be same. If you make made some mistake, correct it, you can anytime check the Cube app branch to see the code and match it with your code. Okay, in next lecture we are going to set up Rabbitmq, which will be similar. Rabbitmq deployment will set and then we'll set its servers. So I'll see you in the next one.
