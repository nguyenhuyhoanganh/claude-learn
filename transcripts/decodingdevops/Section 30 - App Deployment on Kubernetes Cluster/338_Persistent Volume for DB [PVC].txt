# Persistent Volume for DB [PVC]

Instructor: Okay, now we need to create a persistent volume claim for our database pod. Okay, let's once again take a look at the docker-compose file. So in the volumes in docker-compose, we create a volume called vprodbdata. This basically creates a folder in the Docker engine, right? And then we map that. Where is that? Right, we see volumes in the db container. We see volumes, we give the volume name and where to map it. Now we have to give similar thing in the DB definition file, and Kubernetes also has this volume concept, but again, it stores it in the local machine. So in Kubernetes, it'll be storing it in the worker node, which is not at all recommended. We need an external storage where it's stored, so we can deconnect, you know, decouple it, we can connect it to other pod. You know, if the pod gets deleted, recreated, we can reconnect this. Our data, our stateful application will be storing and retrieving data, that data should be safe. So for such a concept or requirement, Kubernetes gives us persistent volume. Right, in persistent volumes you need to manage all the storage by yourself. Like this will be the job of the Kubernetes administrator, definitely, to make sure the volumes are available, scalable, its backup, you know, was taken regularly, all those things. It is definitely a job of the administrator. And there's a lot of work that goes behind the scenes, a complete storage management. Now also Kubernetes gives us something called a storage classes, where these kind of things are kind of automated, again, you will need administrator for this one also, because whenever data comes, you need regular backups, maintenance, scalability, all those things. So also we have the Kubernetes cluster with the kOps. kOps creates a storage class. On AWS, it gives us the EBS volumes or also EFS, also it gives. So you don't need to write definitions like this and create a storage class and manage it. So you can just write the persistent volume claims. So when we have the cluster with kOps, Kubernetes cluster with kOps, it is going to have the ready made storage class which can claim for EBS volumes, which can manage EBS volumes basically for us. There is also storage class for EFS. So we just need to write the claim that I need, let's say 3GB, like a consumer, right? I need 3GB of volume, like 30GB of 200GB of volume, and the storage class will take care of it. So, you know, with any generative AI like ChatGPT or anything, or even from the definition file, you can just say, you know, PVC, persistent volume claim definition, and you'll get it, right? So I just did Kubernetes persistent volume claim, and its generative AI is giving me this PVC example. I'm just going to copy this one. Go to kubedefs and the PVC, the dbpvc, I'm going to paste it here. First, I'm gonna change the name, db-pv-claim. I'm also going to give it labels so we can easily recognize it and filter it, labels, I'll say app: vprodb. Okay, so the storage class name is very important. By default, there is a storage class called default. When we create the Kubernetes cluster, I'm going to show this to you. We have a storage class name called as default, that is basically going to give us the EBS. Okay, I need the size of 3GBs, so here I'm going to say storage 3GB. Access mode, ReadWriteOnce. Right, so when we execute this manifest on the Kubernetes cluster, or against the Kubernetes cluster, it's going to go and talk to the storage class called default, and ask it for a 3GB of volume, which it will give. And that PersistentVolumeClaim name will be db-pv-claim. We have given that name. In the pod definition, we'll say connect this volume to /var/lib/mysql, like that. Right, when this all connects together, you'll understand what I'm saying. Let me save this file. Okay, in next lecture, we'll write the DB deployment definition file, where we'll connect this PVC to our DB pod. So join me in the next one.
