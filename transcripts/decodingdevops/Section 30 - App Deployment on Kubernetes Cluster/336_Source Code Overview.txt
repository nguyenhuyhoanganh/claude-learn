# Source Code Overview

Instructor: Okay, so you can open this link, this source code link from your browser, github.com/hkhcoder/vprofile-project and then select the branch, kubeapp. We have one more branch, skelkube, that we will be using from the next lecture when we start writing everything. I just want to show you already written code, all the manifest files. You can check those files and you can match with the architecture diagram so you really get an idea where we are going and what really we need to accomplish in this project. So go to kubeapp first. Here you have. Now, this branch is created from the Docker branch, from the containers branch. You'll have Docker files, you'll have the docker-compose file. So it's the same source code. In this, we have created this folder called kubedefs. So, kubedefs folder. And in that we have all the files. Let's clone this source code in our local machine on VS code and take a look. Click on the repository name somewhere, click on this dropdown, and go to HTTPS, say Copy. Open VS code and click on the Source Control button here. Click on Clone Repository, paste the URL there, and click on that clone URL from so and so, and select a location where you want to save. I'm going to save it in the F Drive, creating a folder kubeapp over there, and I'll say Select as Repository Destination. Open. Okay, let's switch to the branch kubeapp, this one. Right, in kubeapp branch, you have kubedefs. This one. And here you have all the manifests. Now, to enable some support from, you know, while you're writing the Kubernetes definition files, with these definition files, you can install a Kubernetes extension. So just go to extensions and search there for Kubernetes. The first one you can install, Kubernetes. Install, installing. It's going to throw you an error on Kubectl. You can ignore that. Later, we are going to talk about that. Okay, so these are the files that we intend to write. We need to write a secret that is going to store two variables: db-pass and rmq-pass. We have a PVC, PersistentVolumeClaim that is going to claim a volume of 3GB. Its name will be this, right? Then we are going to write the deployment manifest. One is for the app, which is the Tomcat, vproapp. One for a database, one for RabbitMQ, and one for Memcache. And if you see, we have a service definition file for our every pod, and it is of type: ClusterIP. As I said, this will be all for internal communication. For external communication, we will be having ingress. So if the host URL, if the user accesses with this name, it is going to route the request to the load balancer. Load balancer is going to forward the request to the ingress controller, nginx.ingress controller, where this rule will be checked. If the URL that user is asking is this, then forward the request to a service with this name. So it's going to look for a service with the name vproapp-service, and just an example right now, okay, we are going to write this. We'll have a service already with this name, which is going to route the request to app, whose name is vproapp. We'll understand all this. We are going to write all this from the scratch. Just wanted to show you, we have four deployments, four services, of type: ClusterIP, one secret, one PersistentVolumeClaim, and then for front end we have ingress. So all this we need to write. So I've created a skeleton for this in another branch. So click on this branch symbol and switch to the branch skelkube, S-K-E-L-K-U-B-E, skelkube. Switch to that. There's also exactly same branch and replica of the kubapp branch, but here we have kubedefs, all the files we have, they don't have anything, no content. Then this content, we are going to write. So take a look once again on all the definition files, match it with the architecture diagram that you're seeing in the previous lecture. And once you're good, then join me in the next lecture.
