# K8s Cluster Setup & Source Code

Instructor: Okay, so if you have still not set up the Kubernetes cluster, then go to the previous lectures where we set up Kubernetes cluster with kOPs. Follow that and set up the Kubernetes cluster. If you already did that lecture and you deleted your cluster and you have the kOPs EC2 instance and all the other prerequisites, hosted zone everything, then you just need to bring up your kOPs instance and log into this. And from there, we'll run the kops create cluster command and set up the Kubernetes cluster. If you don't wish to do that, then you can just watch this lecture. You already done the definition files. I'll show you how you can run it on the cluster. So I'm going to copy the public IP of the kOPs instance and log into this. Okay, switch to root user, clear the screen. Kops create cluster command, and you have to make sure you have the access key stored over here. You have created the hosted zone in root 53, stored this information in Kubernetes account, DNS records, DNS name records. You have the S3 bucket and all the other things that we did while setting up the cluster. So I'm just going to run this command to set up the cluster. Okay, and then I'm going to run the kops update cluster command that will start setting up the cluster. Kops update cluster, cluster name, hyphen hyphen state, the bucket path, hyphen hyphen yes, hyphen hyphen admin. Okay, this should start creating the cluster, by the time we are going to push our source code to our GitHub repository. Okay, so come to github.com and create a repository. Make sure you're logged into your GitHub account. And give a name. I'm just giving a name as vprokube. Public Repository, don't add anything. Just hit Create Repository. So once it is created, you get this, you click on uploading an existing file. So we can do SSH-based authentication, push our code. But I'm just keeping it simple over here, we're just going to simply take all our file and just upload it over here. So on the VS Code, when I was cloning, I cloned all this in the F drive in kubeapp folder. So go to your location, I'm in F drive, kubeapp folder. Here I have vprofile-project, and let me minimize this. So I'm going to drag and drop, because I want to copy everything. Just select all this and just drag and drop. It is saying there are too many files. Now do it fewer than 100 at a time. So let's do one folder at a time. So first I'll copy the src. Select it, drag and drop. That is also a problem. Just going to copy the kubedefs. Frankly, that's all we need for this project setup. It's going to fetch all the images from Docker Hub. We really don't need the source code. We are not doing any build or anything over here. So you can just upload the kube definition files. And along with that, I'll just put docker-compose and other files also, just in case if you want to see things. So I'll put the Docker files and I'll also put the docker-compose file. Mainly we need the kubedefs folder. And let's make a commit. I'll just say kube defs upload. And I'll just say, commit changes. And that's the URL HTTPS. This is the URL of our repository. So go to that. Just say copy, go to your kOPs instance and clone it over here, I'm going to say git clone insert.
