# Architecture

Instructor: Hello, and welcome. In this lecture, we will understand the architecture of our application setup on Kubernetes cluster. And with that, we'll also take note of what are resources we need to create on the Kubernetes cluster. It's highly important for you to do Containerization project before you do this project. In Containerization project, we created Docker images for Tomcat, database MySQL, and Nginx. We will be using those images in this project. So I'm going to start by the database MySQL service. So in Kubernetes, if you want to run a database or any such service like that, you need to have a pod. So we will be having a database pod. Likewise, we will have a Memcache pod, RabbitMQ pod, and Tomcat pod. This pod will have the container that will be executed from the images that we mentioned. So for Tomcat and for database MySQL, we will use the images that we created in Containerization project. RabbitMQ and Memcache will use official Docker images. Now here I'm showing pod, but we are not going to directly run the pod, we will be having deployment. So we have to write a deployment manifest where we mention the pod information. So we will have one deployment of Tomcat, one deployment of RabbitMQ, one for Memcache, and one for database. And in that we mentioned the pod information, and the pod will have the container information. In Kubernetes, the pod do not directly interact or connect with each other, we use service. For internal communication, we use service of type ClusterIP. So if Tomcat pod or the container running inside the Tomcat pod needs to connect to the database, it is going to connect to the database service, which acts like a load balancer. That service is going to route the request to the database pod, which is running in the deployment. Now, database is a stateful application, stateful means, which stores and retrieves the data, which needs to access the data in the runtime. That kind of application is called a stateful. So database MySQL, which we will be using, is stateful, and the MySQL service stores the data in this location, /var/lib/mysql, this path, by default. Now, if the pod gets deleted or recreated, all the data will be gone, and when it gets created again, there will be new data. We don't want that to happen, so what we will be doing is we will be doing something called a PersistentVolumeClaim. A PersistentVolumeClaim can claim storage from a volume pool. Here we have StorageClass. When we create the Kubernetes cluster with kOps, it creates a StorageClass for EBS, it can understand StorageClass like a driver. Now, pod does not understand and Kubernetes does not understand the EBS or any other external volumes. So StorageClass, in essence, makes that external storage available and you can connect it by using PersistentVolumeClaim. So we can create a definition file where we say the PersistentVolumeClaim so and so name, and we need, let's say, 10 GB of storage. So PersistentVolumeClaim is going to talk to StorageClass and say that, you know, "I need this 5GB of volume." It's the responsibility of the StorageClass to talk to the external storage, create the storage, and give it to the claim. And then you can map that claim with your pod. So in database pod, we are going to say store value, MySQL, on so and so PersistentVolumeClaim, which behind the scene, will get the storage and connect it to the pod. Now, we are going to write all this definition one by one, so you'll understand it much better. Later also, you can come back to this architecture diagram and take a look once you're done writing all the definition files. Okay, so Amazon EBS is the volume that we will be using, I mean not, we will be doing anything, StorageClass will make it available for us for the DB pod. Also, the other thing we have secret. We need to store two password, one is for database and one is for RabbitMQ user. We are going to store its password safely in secret object. So we need to write a definition file for secret, storing two passwords in it, which will be later injected in this pod. Okay, so once again, let's talk about the backend. So we have the service of type ClusterIP for RabbitMQ, Memcache, and DB Pod, so they can communicate internally, this is for internal communication, a secret object where we are going to keep the password for RabbitMQ user and database, a PersistentVolumeClaim that is going to claim for the storage. So what are we looking at here? We need to write one definition for secret, one definition file for our manifest, we need to write one manifest for secret, one manifest for PersistentVolumeClaim. StorageClass will be available there, so two here. And then deployment manifest for RabbitMQ, Memcache, DB, and Tomcat. So four manifest for our pod to run, three for the service, backend service. And then for Tomcat also, we are going to have a service, and that will be also of type ClusterIP. The service of type ClusterIP is for internal communication. It does not have any public IP, so you cannot connect it externally. But we need to host our Tomcat, our Vprofile application running in Tomcat, we want to host it for the external world. So we need a public IP and public endpoint that we are going to manage with Ingress controller. We are going to have an NGINX Ingress Controller. The job of this Ingress controller is to manage an external load balancer for us. So in this case, we are doing everything on AWS, so we'll have an application load balancer. So when we create Ingress controller, NGINX Ingress Controller, it is going to create an application load balancer, and then we will write an Ingress rule, just one more manifest for Ingress that will say if the user is trying to access, let's say vprofile.hqinfotech.com, then route the request to this TomcatService. So whenever a user access this URL that will be mapped to this load balancer, we will also have GoDaddy record where we'll say this URL maps to this load balancer endpoint. And when it comes over here, it'll look at the URL, the URL matches the Ingress, Ingress will say, forward the request to this TomcatService, and then all the backend thing happens. We'll come back to this architecture diagram again and again in this project setup. In the next lecture, we are going to take a look at the source code, then you can match this architecture diagram with the definition file, the manifest that you're going to see in the next lecture. So join me in the next one.
