# AI Suggestions

Instructor: Okay, so far so good. Let's go to the next script. So vars_websetup.sh. This is where we learn how to assign the value to variable, and then use the variables, right? So also this highlights some issues like, for example, this one, yum install $PACKAGE. What does it say? It says, you know, "Use double quote to prevent globbing and word splitting." Also, it gives you a suggestion about this article, go and visit this and read about this, what the problem is and how you can fix it. So, yes, we can give double quotes. You know, let's try Quick Fix. It's saying "Apply fix for this SC2086" issue. And there, it solved the problem, right? Like that, we can do it for others. Let's see this issue, cd $TEMP directory. Now, this is very important. Now, if you don't do CD to a particular directory where you're executing commands, you know, if this fails, you know, let's say there is no value to this variable or if this is from user input, this should fail then, because you don't want your code or your commands to execute in the wrong folder. So it is saying that, you know, instead of, you know, just mentioning it like this, use this method. Now, cd into this. Or, if this fails, then exit. I'll say fix, wait, hold on, "Fix using Copilot." So it's running that /fix. And we say Accept this. So now, this is one way, or, you know, if you think that there is some problem, you can also, you know, just select, let me just do Control + Z to it. Select and open the chat, and we'll say /fix, can use the slash functions, so /fix. Okay, but this is when you find that there is a problem. Anyways, it is already suggesting you that there is a problem, and I always recommend you use the extensions along with the GitHub Copilot extension like we have installed batch ID. There are more extensions, so now together, if you use these extensions with the AI, in this case Copilot, if you use all of them together, you will get the best results. And, once again, you can just select the entire code and you can say, "Improve this code." It is going to add functions and many other things as we have seen in the previous script. Let's take a look at, wait, let me save this first, let's take a look at other scripts. So these in all, we just used to learn the concept. So, you know, if you want, you can ignore this or you can improve this, command_subs.sh. Okay, there, if you see, it says, you know, "This is not a recommended method. We have used backticks for command substitution." It is saying, "Use the $(...) quick fix." Apply fix. Right, we talked about this, there's backticks and then there is $(...). So, you know, you the existing written code, you can use these extensions together and it is going to give you suggestions, you better apply them, and then also test them if it works as per your expectation. Okay, let's go to the next one, userInput.sh. So here it is making suggestion, "Read without -r will mangle backslashes." Now, this time, let's take a look at this shellcheck article. Let's open this in the browser. Okay, so this is the code, problematic code, and this is the correct code, use -r. And the reason it has given, "That read will interpret backslashes before spaces and line feeds." Right, so if you're using backslashes in your input, which is rare, even says it is rare, then this will be a problem. So better use -r. So if you use -r, it's going to strip those white spaces and backward slashes, right? Which you don't intend to, you don't want to backslashes in your input if you don't want. Okay, so you can just select the entire code, you know, use the inline chat and improve this code. Try yourself. Let's go to monit.sh now. And here we have used the $? to check the exit code. And, again, it is making a suggestion over there. And let's just apply this, right, this is saying show. Right, I'm going to select the entire code, control + I or Command + I. "Improve the code as per developmental standards," or best practices. Okay, so again, a better suggestion, it is saying, you know, "Take this variables tool it into the PID_FILE, the FILE part basically, and then check with -f option whether this file exists or not," -: f is going to check if the file exists or not. Again, it has mentioned this set -euo pipefail. So if anything fails in your script, the entire script fails. All right, so the point here of this lecture is look for the suggestions, which, you know, for your existing code. Also just do not blindly apply them. Make the changes, test it in your test machines, like your VMs, and then roll out the changes. Because it might give you suggestions sometimes which does not work or does not give you the desired result. We'll take a look at a few more scripts in the next lecture.
