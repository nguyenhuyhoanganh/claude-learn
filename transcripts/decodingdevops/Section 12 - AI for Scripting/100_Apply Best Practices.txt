# Apply Best Practices

Instructor: Okay, so if you wish to test the script, you can either copy the script to the VM and run it, or you can use the sync folder. You already copied it into the sync folder. So in the VM, in /vagrant folder, you should see your scripts. Make sure you save when you edit your scripts in VS Code. Use Ctrl + S or use this option to save the script first. Okay, now let's go to the next script, websetup.sh. Okay, now we are going to see a few new things in GitHub Copilot. And again, we are also going to learn from the suggestions. Okay, so you do this, select the entire code in websetup.sh and you need to open the chat. You can use Ctrl + I or Cmd + I based on your operating system. So I have Windows, I'm going to do Ctrl + I, okay, first go through the script if you don't remember everything, a simple web setup script, download the artifact from twoplay.com and set up the website. Pretty simple, but can it be better? Of course, it can be. Let's ask Copilot. So improve the code according to developmental best practices. You can write this prompt and hit enter. Okay, it has definitely made suggestions, and good ones actually. First I'm going to accept this and then we'll go through the code. So this line, so this actually is to exit the script if something fails, and this is actually much better, let's understand each options. Let's select this line and again open the chat, in-line chat, Ctrl + I or Command I, and we'll say /explain. And now let's hit enter. So these are the slash commands. It's making some info, giving some information, and it's too small. I'm gonna say view in chat. Okay, this is better. So it says the set, this command is common. Best practice in Bash scripting -e option script to, immediately exit if any command returns a non-zero status already seen in Bash scripting what does non-zero mean, so, right, failure, zero means the previous command was successful, right? Then you have the -u option. Check this out. If the variables are not defined, then it is going to also terminate, and check out this, we have variables. If it's not defined correctly, if you have the wrong values, basically not defined, does not have any value when, and this is very helpful when you take user input. So the user is not entering the value, then the variable is not defined. You don't want your commands to get executed with non-defined variables. It is going to give you weird results. And then you have -o pipefail, so if any command in the pipeline fails, it's going to fail the end task script, basically exit the script, you don't want to continue. Right, good work, so we have seen two things. We have improved our code and we have also seen how to ask the Copilot, the AI tool for the explanation. Next things, let me close this one. This over here, look, this is a function. I just gave a comment here, function, so it has created functions, multiple functions, no wait, it has just created one function. Anyways, let's just take a look at this one and then we'll add more functions. So function basically is a reusable code we're going to see more about this in Python scripting, so something you want to do repetitively. So instead of writing those lines of code again and again, you can create a function, write your line of code over there, and then you can call it whenever you need. For example, this log function. So this is how you define a function in Bash scripting. So log (), and then in-between curly braces you write your code. So we have two echo statements, in-between we have echo dollar I, what is dollar? No sorry, dollar 1, dollar 1, that's the first argument. Now here, this function is getting called, log, and we are passing this text over here or string. This goes over here. This will be the dollar 1. So when you call this, it's going to simply print this lines. It's going to say echo installing packages. And then again, print the hash. Okay, let's select our code. Open in-line chat. I will ask it to add more functions. Okay, it made suggestions and I'm going to accept this. And now if you see if we have installing dependencies, this as a function, installing the packages. And from this function, we are calling another function. And all this functions are called here, main script execution. So these are the functions that are just defined, over here we are calling them, so one by one, all the functions are called. And if you see it's all in right order. So let's save this and you can test it.
