# Write entire code with AI

Instructor: Okay, let's take a look at our remote_websetup folder. We have the multios_websetup script. And we are going to improve this code. So once again, Control + A and Control + I. So improve this code as per development standards, and use functions. Okay, that's making suggestions. And I'm going to accept this one. Or you can just take a look at the code, what it's suggesting, ask more question, you know, make it more interactive, you know, talk to it, develop it as per your need and standards both. So it's the same way what we did in the first script. In the websetup.sh script, we did same thing it is doing. But, also, you see a few more new things, like, for example, this local, local packages. Now, there is scope of variable, global variable and local variable. Local variable, it is just for the function. So select the code, do /explain in the chat, and it will explain that this local is used to define the variable local. Scope will be local in the function, within the function, not outside of it. So once this function exits, this variable is also gone, or its value is gone. Once again, there are suggestions. You can apply them again. So all the functions and then the main function, so there is a main function that calls other function before calling these function. It checks the operating system. It sets the right variable, like we did. But this time, it is in the function. So do this, either, you know, set httpd or set apache2, based on the operating system. And then call all these functions. And finally, you just see here, main. So when you run the script, basically it is going to call main, which is going to test the operating system, and then it is going to call all the other functions. So I'm going to save this one and go to webdeploy.sh. And let's improve this one also Improve as per developmental practices. Okay, that's something, a few things you already know. Let's, oh no, I'm not going to accept this, because I know there's a problem. I tested this code, and it just reads the first host, execute the command on the first host, and then it simply exits, does not go to the next host. And that was the problem with the while, while loop. While loop was giving that problem. So I then changed it to for loop. Now, I already know that problem, right? So I can, while I'm chatting with this, and it's making suggestion, instead of accepting it, I'm just going to ask to use a for loop instead of while loop. You know, that is just a requirement. I'm not saying it's not going to work. Use for loop instead of while loop. Okay, I'm going to accept it. I'm going to tell you one more interesting thing. Now, we know this part from our previous scripting session. We assign the variable, and then we have here condition check, whether, you know, this file exist or not, the host file, this exist or not. If it does not exist, you see that, negation. If it does not exist, then, you know, exit this script. If it exist, now there's another condition. If you know this, if this script does not exist, then also terminate it. Otherwise, simply execute the code. So interesting thing that I want to show you here is we are reading input from a file, right, in the for loop. But here, it is reading input from an array. This is a way to define array in Bash scripting, not define actually access the elements in the array. So what's happening here is we have... Wait, let me save this first. Okay, go to this file, right? So we have the list of host, you know, this simply list of string or text. The first thing is we need to store it into an array. So that's what it did over here, mapfile -t hosts. This takes as an input from this command, grep. So it's reading the file and storing it into an array. Now, array is this list of elements, like, you know, you have variable, and we have its value, a string. An array will be something like this. I'll show you. Something like this, exactly like this, hosts, you give the name of the variable, equal to, and in parenthesis you give the list of strings, list of text you can see here. So that's what it is developing over here with mapfile. And mapfile is designed to read the input, and it's going to put it into this variable. It's reading the input from this grep command. So this command is going to read it one by one and keep storing it into the host, which will become an array. And now we are going to iterate over that array. So how do you iterate? You know you need to have every element, like you want web01, web02. So you give array square bracket, at the array, that is going to fetch the element from the array. Basically, web01 first, it will be web01. And then the script execution will happen. Then, again, it's going to come back. Then the value of host at the array, it will be web02, likewise. Right, and now you can surely test all the scripts, you know, get into your script box, create the folder, copy these files over there, and execute and test it for yourself. If it does not work, if there is some error, you know, you can just select things, and you can say /fix. or if you don't understand anything, you can select the code, and you can say /explain. My point is just don't just take the AI information and keep applying it. Check it. Learn from it. If you know your tools, if you know the skills, you can ask right question to the AI tools. Now, just to prove that, I'm going to do one last thing in this section. I'm going to ask Copilot to develop everything, all the scripts for me, to set up Tomcat. So let's open the inline chat. So click on this, say Open Chat, and I'm going to say here /new to create a new workspace for me. And I'm going to write this message. It's not perfect, but it will do. Write Tomcat setup scripts based on OS, RPM-based, and here, instead of Debian, I'm going to say Ubuntu. So RPM-based, Ubuntu-based, use developmental best practices. There should be another script to push Tomcat setup script and execute it on the list of hosts. So I'm also saying that list of hosts should be coming from another file. Now, this is the same thing what we did with the remote_websetup folder over here, right? We have written all the scripts, and based on all the information that I have, or I can say based on the skills that I have, I'm asking right question. Let's see whether it's right or not. Okay, so it suggested me, there will be this folder, and there will be the scripts. I can click on this. So a separate script for RPM-based Tomcat setup and separate script for Ubuntu-based, and then there is a deploy_tomcat_setup script, right? Again, this is using the developmental best practices. It's checking whether the host file exist or not. It's going to create the array. It's going to run it in the for loop. And based on the operating system, it's going to push the script. Check that out, scp, it's pushing the script to the target machine. You know, let's create the workspace, and let's get it all in one. So I'm just going to select a parent folder. Okay, there tomcat-setup-project. You have a host file here, sample names. You have README.md file, how to execute, what does what, and the scripts folder where you have the deploy_tomcat_setup script. That is going to push the script based on the operating system. So if the OS is Ubuntu, then it's going to push this script. If the OS is RPM-based, like CentOS, it is going to push this script. So now the skills that you have learned in Bash scripting and using the superpower of Copilot, you can develop scripts very easily in very less time. Again, test it in the test environment on the VMs before you actually run it on actual machines. I hope you have learned enough in this AI tool, and you can use these skills in the course and in real time. Thank you so much for watching these lectures. I'll see you in the next section.
