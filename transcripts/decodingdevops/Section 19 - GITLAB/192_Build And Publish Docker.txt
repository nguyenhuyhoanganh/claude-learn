# Build And Publish Docker

All right. So in this lecture we are going to build the Docker image from a Docker file and publish it. And to publish or push our Docker image we will be using GitLab provided container registry. Yes GitLab provides a container registry. If you check here in the deploy section, we have here container registry so we can host our Docker images over here. The best part is the authentication can be done inside. Inside the GitLab environment itself. GitLab provides us variables for the registry URI, for Docker username and for Docker password all inbuilt variable. We can use those variables directly in our pipeline and push the Docker image to the container registry provided by GitLab. Now if not, you can use Docker Hub, you can use Amazon ECR, you can use Google Container Registry, you can use any other third party container registry. But mostly I have seen when people use GitLab, they use this entire ecosystem. We have repository. We have pipelines. We have container registry. We have package registry, security, deployment everything inbuilt into this GitLab. So we will be also doing the same thing. We'll mention a stage where we build the Docker image and publish it to GitLab container registry. So let's open our VSCode. And after security scanning we are going to mention the stage docker build and publish. And before itself we're going to mention it over here in the stages. Now let's add all the steps. So the image that we will be using is Docker image latest version. Now this Docker image just provides the CLI. It does not provide the docker docker daemon. We need the Docker daemon, the service, the docker service running to execute the docker build commands, and GitLab for security reason does not allow us to run the Docker daemon directly over here. We need to mention it as a sidecar by using an option called services. And we mention this docker dent which is docker inside Docker. No space here. Now what happens with this is it runs the Docker CLI container. And as a sidecar to this container it runs a Docker container. So there are two containers here. This is the main container. This is the sidecar that will be the daemon. And in that it will run the Docker service which provides us the docker build time environment. So this container connects to this container. And once again this is for security reason in GitLab. Oh sorry. Okay. We'll provide this needs for this stage. So it should run after the security scan stage. We're going to mention two variables. One is for the image tag and one is to disable the TLS. Variables. Colon this variable Docker underscore underscore certain. We are giving none. Now when the Docker daemon runs in this dent, it creates TLS certificates and the client needs to provide those certificate for the usage of this daemon. Now, since we are doing all this internally, we don't need to do that. So we're disabling that. Even if we provide that, it gives a lot of overhead and it's really not required over here. Now we need to mention the stages. Sorry, the commands to build the Docker image and publish it. And before that we need to provide the authentication also. So with script we're going to execute all those commands. A print message to just say that we are logging into the container registry. And then the command to log in. So this is the command docker login hyphen u the username. This is the inbuilt GitLab variable, which gives us the registry username and then the password we need to provide as a standard input we are providing. And that is coming from this echo message. So it prints the password not over here. This is piped so it passes it as a standard input. So you will get the password over here without even leaking it in the logs. And then the CI registry URI. So the default command to login to Docker registries docker login hyphen you username and password. And at the end the registry URI. So all this we are passing as the variable. And next we can build the Docker image and then push it. Let's put a message building docker image with custom docker file. So docker build hyphen f the path of the docker file. We need to update this docker file. We'll do that in a moment. The tag will be the registry image. This is again inbuilt variable. You can check in the predefined variables documentation. This gives us something like this. The GitLab registry endpoint. Slash your group name. Slash the project name. So that will be our registry name with the repository. So complete repository URI colon the image tag. So image tag we are defining as the current commit message. Sorry not message the current commit ID. We are storing that into this variable and then we're using it and the context is current working directory. So in the current working directory and this will be cloned into this Docker image. This Docker container everything will be cloned and it's going to execute the commands. The path we have mentioned of the docker file. We need to update this docker file. So currently this Docker file pulls the source code from our default GitHub repository. We don't need this. We need to remove these two lines. Let's add here copy dot to a folder called slash app. So all the content will be copied into the slash app of this Maven container. We'll set that as the work directory. So everything executes in slash app where we have our source code. And then the instruction to run the command Mvn install that will build the artifact that should be copied into this image. So copy from build image here the path will be slash app slash target slash v profile hyphen v2 dot var. This will be copied to the location of the default Tomcat application, so make this changes and save this file. Come to your pipeline file and last command will be a simple one just to push this docker image. Docker push the registry image URI which includes the registry URI and the image repository and the tag. So this one it basically takes and push it at this location. So let's save this and let's commit and push. Yes. Commit message Docker build and publish. Accept. Commit message. Let's take a look at the pipeline. The pipeline got triggered. Let's wait until this is completed. All right. So all the jobs completed successfully. Notify is skipped because it says notify on failure. So there's no failure. So this job is skipped. So Docker build and publish also completed. Let's take a look for log. Okay. So take a look over here. So it prepares the executor docker and then pulls our Docker image and runs the container. And it starts the service docker dent. There. It pulls that Docker dent image waiting for the service to be up and running. And the Docker cli wants both become available. It's going to run the command from the docker container. This one. So logging into the container logs in there, you see. Nothing is printed, so it's good. It basically stores all this information into this container file dot docker config dot JSON. Here you have the authentication stored for the Docker registry. Right over here it does the login. And this creates this file. And then building the image the docker build command. And once the docker build completes then we have the docker push command to push our Docker image to GitLab container registry. You see that that is completed. So we should be able to see our image in the container registry. So go to Deploy container registry. That's the repository. If you click on that you should see the image with the tag. If you copy this let's put it over here in the browser. Let's see this. You see the registry URL, the group name, the project name colon, the tag. So that's the complete Docker image information which can be used to pull to a container runtime environment or to the Kubernetes cluster and run the container from this. Right. So that was pretty amazing. We have the container registry in GitLab itself. And along with this GitLab is complete ecosystem. It provides many things like in plan you will be seeing in real time the developers creating issues and tracking them on the issue boards. Click on the issues. So like a product enhancement or bug tracking, everything can be done through this. And this can be linked to your commit your pull request. And that is connected to the pipelines. The developers are going to set the milestones have documentation in the wiki. Right. So this is a complete ecosystem. You have everything related to code build security related. You can do security configurations, static application security testing, Sast infrastructure as a code scanning. Right. Application security testing, container scanning, and many other things. In deployment. You can deploy your builds based on what you are doing. Like you can store it as a container registry, or if you're building packages, you can store it in package registries. You have this operate section over here where you can connect your or your GitLab agent. You can implement to connect it to Kubernetes cluster or Terraform or Google Cloud. For monitor, you can send alerts to any third party alert mechanism. There's also service desk. So you have the whole ecosystem. There's a lot of settings that you can use to make changes on your GitLab project based on your requirements. So this whole ecosystem, I would like you to take some time and explore these different options. This will be all very helpful in project building. So take some time before you move to the next lectures. That should be all in this lecture. I will see you in the next one.
