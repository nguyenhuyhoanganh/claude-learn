# Security & Artifacts

Hello and welcome. In this lecture we will be talking about code scanning, specifically security code scanning, one of the necessary stage in your pipeline before publishing your artifact. There are many popular tools that you can include in your pipeline to do code scanning. I have listed here a few sneak. It can be used for static application security testing and also software composition analysis. Now Sast scans your source code for security issues like SQL injection, XSS insecure functions. C, on the other hand, scans the open source dependencies like Maven, npm, pip, and again, this is also for the known vulnerabilities, the security vulnerabilities. Then you have the most popular name sonar cube or sonar cloud. You can use it for code quality plus security. Both SEM group is a lightweight Sast tool. And then you have Trivy, which is very popular nowadays in a container environment. And again it scans the container also image and also infrastructure as a code like Terraform, cloud formation, Kubernetes, even helm charts. And that's what we will be using in this lecture. So in the GitLab pipeline we need to mention a stage security scan. And the image that we will be using is Aqua TV image from directly from Docker Hub. So going to pull this container, pull this image, run a container and execute some command on that. Before running the command, we need to set the entry point as null. And why that? I'll tell you that in the pipeline setup. So we will be running it after the build stage. So we're going to give their needs build job. And the command that we will be running will be this TV fs file system. So we are scanning file system just the source code not the container image over here. But you can scan the container image also. So format will be JSON format, vulnerability type will be OS and library and output. We are going to store it in this file. The dot at the end represents the current working directory. So you will have the source code in this container and in the source code. It's going to run this command which is going to generate this file in output. So once we have this file we are going to also save it as an artifact. So in this lecture we will be doing code scanning by using Treeview image and storing its output as an artifact. So let's get into it. So open your vscode open GitLab CI dot yml file. So we have a build stage. After build stage I want to run the code scanning stage. In fact I want to run this test job plus code scanning at the same time. And if I want to run that then I need to put here an entry which says needs colon in square bracket. I give the name of the job build job, build hyphen job. That's this job name build hyphen job. So test job will run after the build job. That's what anyways happens. But at the same time I want to run the security scanning job. So in security scanning job. Also, I will mention the same entry which will work like this. After the build job test and code scanning job will run together parallelly. So let's mention the code scanning job after this. So from the first column I have given security hyphen, scan, colon and inside that stage name as security image name Aqua cv colon. Latest and name of the image I have given in indented. I have given inside it because I'm going to add one more entry over here and why and what I'll explain in a moment, but let's do it like this. First we'll add the needs stage needs colon and here build job. So in that way security scan job will also run after the build job like the test job. Same. Both the jobs will run parallelly. Now I need to mention the command script. Colon. And this is the command. Of course I need to give hyphen here and then start the command. So the command is trivy. And that many option that comes with the trivy command. We can go through the documentation of TV. But I want to just scan the source code. So I say FS file system format. That is the output in JSON. Exit code zero. So even if the code scanning fails, it's going to go to the next stage. This is useful when you want to set the quality gates later and needs to send the notification. We'll talk about that in a moment. Vulnerability type OS and library. So operating system related vulnerability and library related vulnerabilities and output stored into this file. Tree view results dot JSON. This you can give anything. And here you have dot at the end. So what happens is at this stage the pipeline will pull this container image run it scan sorry. Clone your source code, all the source code in the container. And in that container where you have source code at that place, it is going to run the TV command. That's what Dot means in the current working directory. And the current working directory will be where you have the source code in the container. Now there is a problem with this with images like this which comes with the tools inbuilt. Now listen to this carefully. There are many tools that you will be using in real time and there are images available for that. So either you can use that image and execute your commands like we are doing with TV over here. The other way is you take a base image like Ubuntu or Debian. You run the container and you install those tools in the container and then run it. So for example, here I can take a Debian image, run the container and install TV in that, and then run the TV command. Now the second option takes a little longer time and we really don't need to install and run. But why people generally do it because of this. The face error while they are using tools built in the container. So take a look at this here. That's the stage where we are running the container, pulling the image, running the container and executing the command. And that's the command. That's the command for this job that has failed. And there's nothing wrong. It's the same command that we are writing in the pipeline. The problem is when this container runs, it already runs the command trivy. Now what happens is the container runs. It runs. The first command it runs is Trivy. That will be the entry point. That will be the entry point, the first command that runs. And with that command again we are saying run Trivy. So the command the complete command will be actually Trivy TV fs which will fail and it is not highlighted anywhere. You simply see that unknown command as search for trivy usage is blah blah blah. Now whenever you see such kind of error, usage is so and so. It's usually the mistake with the command that you have given and not exactly the command. The format, like the container already runs the command. Then you run the command again, plus you pass the options. That's the problem. So to solve the problem, we need to first nullify the first command. The container is running by doing entry point. So we say entry point. The first command will be nothing. Just run the command that I have given. So that way there will be no command. When the container is running it will just execute our command. And after that you might need to do something like maybe set quality gate or something else. So you can use one option here after script. It's after underscore script after underscore script colon. And you can mention the commands that we'll just say ls hyphen la. This is just to show you you might need to run some other commands. Then the output when the pipeline completes the output this file will be also gone. So we need to make sure we save this file as an artifact. So in the same line you need to say artifacts colon. And over here you mention the path and you mention the path. This is in list format. So there could be multiple files that you can mention. So this is that same file that you want to save as an artifact. And we're seeing when always that means anyways, whatever happens pass or fail this stage. Still see if this artifact. Now we also have the artifact from the build job that we would like to save. So let's put that artifact option in the build job as well. So let's scroll down. Let's take this artifacts. I'm running after script two times now. I'll take after script also from here, let's copy this whole thing and put it at the build stage. Okay. In the same column after script have given artifacts and artifact path is different. It's target slash v profile hyphen v two dot var. So you can give an absolute path. Or you can give a regular expression like you can say star dot var. Like if you have jar files, multiple jar files, you can just start or jar or anything that you want to save. You can give the regular expression, okay, let's save this and let's test it. So I'm going to save control S let's commit and push. Give a commit message. Accept the commit message. Okay. The pipeline failed. And let me show you why it failed. So it says YAML, invalid error, security scan, job chosen stage. Security does not exist. So we have not defined the stage in our pipeline. We have mentioned the stage here. Security scan. The stage name is security. But here also we need to add that hyphen security. Save this and commit and push a commit message. Accept the commit. Let's check. Let's go to pipelines. And it's running. Now let's check the stages okay I waited for some time. Now you see the build job completed and test and security executed at the same time. Security already completed. Test is taking a little longer. And now that is also completed. So we are running test and security job parallelly now. And we should also have artifact. We have saved artifact from build job and security scan. Scan job. So here itself in the build section you can see artifacts. So you can see the artifact one minute ago for security scan and build job. Let's click on build job. Sorry. My bad. Uh, downloading it. Let's download it. Okay. Downloads a zip file. If you open that, you should see your artifact. We have mentioned start, but we got the file that we're looking for. Security scan. Let's download this artifact also. So you should see the JSON file in zip format. If you open it opens in the VS code itself. So you see all the vulnerability in the code. Of course the next stage should be to check the vulnerability and decide whether to pass this job or not. And based on that, send the notification also. And that can be easily done with this. Now we're not doing it. I'm just showing you exit codes. We have given zero, so there's no quality gate. Just runs the job and completes it. But if we give one and it finds any vulnerability which are high in the severity, it is going to fail this job. And then you can send the notification after this. You can also mention the severity like you can say hyphen f and severity for what you want to fail it for medium high critical you can decide. I've given all three here just to show you. Now this is tool specific. Very specific. Different tools have different way of setting quality gates. In the DevOps projects course I have used Sonarqube scanner and mentioned a proper quality gates. If you have subscribed to DevOps projects course, you can check GitOps by using GitLab project. I've mentioned everything in detail over there, but as I said here, it's just hands on learning of GitLab. And of course we have project in this course also where we'll see the quality gates and all the other things. For now, let's add a notification mimicking option that sends the notification. I'm going to remove this one and keep it as zero. Let's remove all this. Keep it as it is. Let's save it. Just one final stage. Notify on failure stage name notify script. And just giving echo command over here. There should be something that sends actual notification like slack or email notification. And this job is going to run only on failure. Only run. If the previous job failed then run this. So we're just mimicking the stage here. Of course you will have a command that sends the actual notification. I also need to mention this stage up here in the stages and hyphen notify. Save this. Let's commit and push this a commit message. Accept the commit message. So take a look at this pipeline. How it completes, how you get all the jobs, how it executes parallelly one by one. Do a little bit research on different security code scanning tools that are available in the market, and how to run them in the pipeline. It will be useful for you to make decisions in real time and also talk about this in the interviews. If you work in a project where security is managed by the DevOps itself, or the security is top notch, you will definitely have code scanning security scanning tools. You can work with the security team, understand it, and include it in your pipeline. So if more and more security is included in your DevOps CI CD pipeline, it will be also called as DevSecOps. And this is one of the very mandatory stage in DevSecOps security code scanning. So I recommend you do a little bit of more research from your side. You have already seen how you can include a security scanning stage in your pipeline. The tools will vary based on your project, but now you have hands on on how to do code scanning in your pipeline. So that would be all in this lecture. In the next lecture we are going to build and publish Docker image. So wrap this up and join me in the next one.
