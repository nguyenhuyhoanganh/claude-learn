# First Pipeline

All right. So now we are going to get started with the pipelines. In the previous lecture we created the repository we cloned and we signed it with our VS code. Now this is a git repository and it's same as any other git repository. You have branches, you have tags, you have commits. If you want to set up a pipeline for this source code, you need to create a file in the same repository and that file will be in YAML format. When you have the pipeline file in the repository, your pipeline will be visible over here, build here you have pipelines and you click on pipelines. So right now we do not have any pipeline. But there are some templates that you can go through to understand the syntax, the structure of this pipeline. So click on Try Test template for this hello world. Well we are not creating it like this. We are going to do it in our local machine, in the VSCode and in our repository. We will be having this file dot gitlab ci dot yml. And yes you can create it here also. But we're not going to do that I'm going to cancel this. This is just to show you. So most of this is common to line 17 here. This is where your pipeline begins. So first you define stages the list of stages. So list will be like this. You see stages colon. And then you give the space. And in the same column you define all the stages of your pipeline. So here you have first hyphen space build. Then you have test and you have deploy. So that shows you that this pipeline has three stages which we call it as a job. So first build now here if you see build job here this is a different name. This is for your identification. But stage colon here you have to give the same name as you have given in the stage stages. So stage build and then you have script and you can give the command. Now where does this run. So GitLab like GitHub actions has its own runners. Or you can add runner and you can mention the runner name over there majority of the time, or even when I work in real time. We define a container image name. So what it does, it pulls a container. Container image, creates a container from that and execute your script, your steps inside that container. And where does that container run? If you do not define any runner, it runs on the GitLab predefined runners. Or you can add your own runner and mention that you need to run this container on this specific runner. Now, I said majority of the time we use container, but that's not the only option you can use shell executor. Also, you can just execute shell command let's say on an EC2 instance or any machine. Okay. So it will be like this. You have job. You mentioned the script, you have job, you mentioned the script. And this is the most simplest pipeline. We are going to have many more setting in our pipeline as we create. So let's get started. And we're going to talk about runners and all later. First let's create a sample pipeline. So close this one. Open your VS code and over here itself in your repository click on new file. Give this file this name dot gitlab ci dot yml. Hit enter. First mention the stages. So say stages colon, hit enter, give space hyphen and mention the name of the stage. So first let's start with the build stage. And then we're going to have also the test stage. Just two stage in the first pipeline that we are creating. Let's also define some variables. So say variables colon and then say project underscore name in caps colon and give the name of your project. So I'm going to say V profile. So that's how you define variable in the pipeline. You can of course store variables and secrets in the GitLab that I'm going to show you later. Let's just first work with this. So let's define the build stage first. So I'm going to give a comment that this is going to be the build stage. And I'm going to give name as build hyphen job colon. But then I'm going to say stage colon build. Now this name should match with the stage that you have defined. If it doesn't match this, you're going to get an error. That stage build is not in the stages. You will get this error in the pipeline. I'll show you that later. Now we are using inbuilt runners on GitLab. We are not adding our own runner on that inbuilt runner. We want to run a container and then execute our commands in that container. So I'm going to run npm install on the source code. We have the source code. We have pom.xml file. Here we are going to run mvn dot install to build the stage, and I want to run it on an image that is from Docker Hub, a maven image. So I'm going to define that in image colon maven image. This is in the Docker Hub. And with this tag 3.9.9 hyphen eclipse. Hyphen tambourine hyphen 17. This is available in the Docker Hub. So what's going to happen when it runs the stage on the runner. The inbuilt runner. It's going to fetch that this image in that runner and create a container from that. And on that container I want it to run Mvn install. So I'm going to say script colon and I can give list of commands. So first I'm saying echo building projects. And then I'm giving the command. Once again this command is going to run a container running from this image on inbuilt runner from GitLab. Okay. Now like this I'm going to create a testing stage similar to this. I'm going to simply copy this and create one more stage over here. I'm going to change its name as first the command test stage. Here I'm going to say test job. But the stage name I need to match as this. Exactly same. So I'm going to just say here test same image I would be using. And I'm going to say testing, running unit test to say running unit tests in the project. I'm going to run two commands here Mvn test and Mvn Checkstyle colon Checkstyle. So two different stage. Each stage is going to run a container, then close it once it's done. And I'm going to say that run this stage only on the main branch. So for that purpose I can use here the keyword only colon hyphen main. Now there are better ways to do this which we're going to see in the next lecture. But for now we're just going to keep the same one only main over here also. And also for the test job. Now this will be running only on the main branch. Any other branch, your pipeline will not run the stage because you're just given the name of this branch as main. So only main branch. When you commit, then only it is going to run this pipeline. Okay, now let's save this. And I'm going to commit this commit and push. A commit message first build and test pipeline Except commit message. Okay, once it is committed, it is going to trigger the pipeline. Let's go to the GitLab. Let's go to our project. So you need to go into your project and go to build and pipelines. And you should see the pipeline failed for some reason. Let's take a look. So here it says before you can run the pipelines we need to verify your account. So click on Verify My account. Mention your phone number. You will get an SMS and solve the puzzle okay so after verification the pipeline won't run. We need to trigger this. Once again. The error for the failed pipeline was due to the user not being verified. It is verified. Now let's make one more comment. So in the VS code, let's go to the README.md file and make some changes. Let's add some extra hash. Save this and then we can say commit and push. Give a commit message. Accept the commit. Now go to pipelines once again and you should see your pipeline running. Click on the status running. Here you can see the pipeline was for this commit. The commit ID you can see. You can see what has changed. What was committed. Due to that the pipeline got triggered. And for the branch which branch it is running for that is for main branch. Let's click on this build job. You should see here running with GitLab runner. As I told you there are inbuilt runner that it is using. It's already succeeded. Let me scroll up full log okay. Over here. Preparing docker plus machine executor. So GitLab has many different executors. So you have SSH executor where it where it can SSH to a machine and execute commands which you mentioned or simply shell executor. When you add a runner you can mention this executor as shell and you can run the command. You have parallels or VirtualBox. You have Docker, you have Kubernetes. And this is all useful when you add your own runner. You can define what kind of executor you want. As I told you, majority of the time it's going to be Docker. But then of course you have other use cases also. So here you see it's preparing Docker executor using Docker executor with image. And here you see this message running on the runner name via this. So on that runner it is running the container force. It's cloning our git repository. So you don't need to mention any specific instruction to clone the source code. It's going to clone the source code and then execute your step. So executing step script. In that script you have mentioned an echo command. And then mvn install. Let's go back to the pipelines once again says it's passed both the jobs completed successfully. You can rerun a job if there is any requirement. All right. So that would be all in this lecture. In the next lecture we are going to see many more things more variables, conditions artifacts and many more stuff. So complete this lecture and then join me in the next one.
