# Variables & More

Hello and welcome. In this lecture we are going to talk about variables. So first of all the variables can be defined in the pipeline itself. We have seen that in the previous lecture. We can just say variables colon. And we can give the list of variables that we want. Now these types of variables are simple. We just want to use a keyword multiple times in our pipeline. So we define it at this level. And we don't mind people in the project to see the value of these variables. But then you have variable that you can define in the GitLab UI in the project setting. So in the project settings you go to ci, CD, go to variables. You can say add variables. Now both the variables serves almost same purpose. Which one you choose is based on your requirement. The first one is plain and simple, but this one you have many options when you save these variables. These variables can used in different places. And there's also you can hide these variable value when you save it as a secret. So same variable. Or you can add another variable and you can mention that that this is masked and hidden variable. And that becomes a secret. Things like database password access keys on your AWS account tokens, or any other things that you do not want to reveal in the pipeline, or also even in the settings. The secret variables are not exposed. You cannot edit them. I'm sorry. You cannot see them. You can of course edit them and delete them, but you cannot see in the setting. Also, accessing these variables are pretty simple. In your pipeline you can mention dollar and you can give the variable name. The other way is you can say dollar, and in curly braces you mention the name, the differences. The dollar curly braces gives you some more options, like you can concatenate the variable with some other text. Also, you can set defaults for these variables and also error handling. So in this lecture we're just going to talk about variables. We're going to just store the variables and access them. In the next lecture we will be actually using this variable for more meaningful purpose. So first let's see the variables in action. So open your VSCode and you can see in your pipeline we have defined a variables called project name. And its value is V profile. We have not used it. We'll use it into this echo command. So building the project and let's say dollar in curly brace. Wait. My bad I had given the variables value first okay. So dollar and the variable name. And then you can concatenate with something if you wish to. Or if you don't want you can just keep it as it is. I'm going to say hyphen test. And also over here I'm going to give dollar project hyphen. Yeah. Here okay. So that is defining the variable in the pipeline and accessing it over here itself. Now let's see the variable in the GitLab UI. So you go to your project and you go to settings and here you can see CI, CD, click on that. Here you have all the settings related to CI CD like customizing your pipeline. You have auto DevOps runners. You can add remove select artifacts. You have variables. And this is what we want. Variables. Expand that. Scroll down and you should see here add variables. You click on add variable. You have here type variable that is by default. Or you can also create file. Let's keep it as variable default environment. If you have environment we're using in our pipeline like production test staging, then we can mention the scope of this variable for a particular environment. By default it's for everything for all the environment. Here you have visibility. Now if you keep it as visible it will be it can be seen even in the job logs. Of course you can see it here. The value and also in the job logs in mast. It will be mast in the job logs. You will not be able to see in the logs, but here you should be able to see the value. But if you select masked and hidden then this becomes a secret. Once you save this variable after that you cannot see it in the settings. Also, you have here a few flags protect protect variables. Now this is for protected branches like main branch where you don't want anybody to commit. You create a feature branch or a bug fix branch or any other branch you create, which is not the main branch which is not protected in that branch. This variable won't be accessible if you put a check mark. So if you save a variable that is protected and you are using some other branch which are not protected, you will not be able to access those variables. So first let's keep it as normal visibility the normal variable. Let's uncheck this protect variable option and give some description. Let's set a variable for a Java options which is used for Maven build and the key or the name of the variable. Let's keep it as Java underscore options. And I'm going to take this variable in the pipeline and store it into another variable that is meaningful for Maven its value. I'm giving it in the double quotes. Hyphen capital Zmz 512M. That is, minimum memory that Maven can use 512 MB and maximum 1024. And this is a normal variable. Let's click on Add variables okay. Let me close this. You see this variable is saved. It's expanded. You click on edit and you can see its value. Now let's add another variable I will say this will be masked and hidden on protected branch. Only some description. And here I'm going to name the variable as my hyphen secret and value I am ninja or I am the ninja. Now here I have given my underscore secret and the value. Okay. Let's see. Add variable. Let's close this. And you see it's protected masked hidden expanded. If you click on edit scroll down. You see you cannot see the value. You can either delete it or make some changes. Okay. Let's put them in the pipeline. Now in the variables here itself I'm going to give the actual variable Java underscore opts. And instead of giving its value I'm going to say dollar curly brace. In this curly brace I'm going to mention the actual variable okay. So this is the variable that I have defined in the GitLab UI. And this is its value will be stored into this variable. And this variable will be accessible by this stages. Let's also mention some echo command over here to see these variables. I'm going to say hyphen echo here in double quotes. Java options are dollar. Java opts this variable over here. And also, let's see the secret echo. My secret is that dollar and my secret. That's the secret variable. And let's save this now. One last thing. Predefined variables this will be using in our next lecture. So there predefined ci CD variables that you can use very very helpful in our pipeline to decide various things. So let's see the list of variables as many actually have ci ci commit branch the commit branch name. Now let's say if you want to decide if the commit branch is main I want to trigger the pipeline or the stage or don't want to run it. So you can use this variable in your pipeline. CI commit message if you want to see the commit message. CI commit QT. Very useful in tagging versioning your artifact like Docker images pipeline CI underscore pipeline underscore source. How the pipeline was triggered. Was it due to the commit? Was it due to the pull request, there was any schedule. So based on that also you can make decisions. We will be using this variable in the next lecture. In this lecture let's just print this variable. So I'm going to take this variable included. One more echo statement over here. The source of the pipeline trigger is dollar CI pipeline source for branch dollar CI underscore commit underscore branch. Now observe here I have used the dollar curly braces. And here I have used just the dollar. Both works. But as we have seen here we can do concatenation also. So let's test it. Let's save this file. And let's commit and push commit and push learning variables in the commit message. Accept it. And let's take a look at the pipeline. Let's go to build pipelines okay. It's running. Let's go to the job directory. Build job okay. Open full log jobs full log viewer. And let's take a look. So that's the command building project. And here is the output building the project V profile hyphen test. Then we have the Java options and we see the value. And here I'm saying my secret is that. And it says must yes Ninja is must right now jokes apart. But that's what it means right. It is not visible in the job logs if it's masked. So these are the variables that we have defined. Now these are the variables that are predefined. So the source of the pipeline trigger is push. So we made a push a commit for branch main. So that variable contains the value of the branch on which the commit was done. Or this pipeline is running for that is main. So in this lecture you have seen how to define the variable and how to access them and the difference between a normal variable and masked variable. In the next lecture we'll use these variables for some conditions. So wrap this up and join me in the next lecture.
