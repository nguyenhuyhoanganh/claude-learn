# Triggers & Rules

Let's discuss about rules to include or exclude a job in the pipeline. So if you want to decide whether a particular job should run or not run in the pipeline, you can use rules. These are the rules that you can mention if changes exist. One which can be combined with these keywords allow failure, needs variables or interruptible. Let's see the example directly over here. So a script in a job over here will run if this rule matches or not matches based on the condition. So if this variable value is equal to feature and commit if this both the condition. If this variable value is feature starting with feature that cap represents starting with the text feature, and this condition is also met merge request target branch is not equal to this branch name. So if this entire condition this. If it returns true, then it is saying never, never run this job. Don't worry about the variables. These are the predefined variables which we have seen in the previous lecture. And based on your requirement, you can decide what variable to use. We'll see that with example. So it's going to evaluate this. And then it's going to come to this one. Here it checks this variable value is this. If that is true then the job will be manual. It says. That means it's not going to run automatically. You need to click on the button and decide whether to run it or not. Allow failure. True. That means even if this job fails, still go to the next job. The last condition is just the variable name. Now this would be either true or false. If it's true, the job runs. If it's false, then it's skipped. We'll see all this with real examples. So let's open our VSCode. Let's remove these echo statements. Let's keep that first one. We're also going to remove this only main. This is the old way to decide whether the pipeline runs and it's very limited. The best way is always with the rules. So remove this only main that we can decide with the rules also in both the jobs. So just below the images and before the script I'm going to say rules colon. And then start defining the rule. So I want to create a rule where it should trigger this job. If there is a push on the main branch or on the development branch. So I'm going to say hyphen space if colon this variable that we're using predefined variables. If ci commit branch is equal to main the equal to symbol two equal to or this is a or operator or if the ci commit branch is develop. So if either of this condition matches. If either of this is true, then it will trigger the job. Let's take a look at the quotes. Single quotes starts over here and ends over there. So this all condition is in single quote. And this double quote is only for the keyword to match it with this variable value for the mean. I have given double quote for develop. I have given double quotes. Now I also want to trigger this job. If there is a pull request in GitLab, it's called as merge request. So let's create another rule for that. Let me give a comment. So triggers on merge request. If single quote dollar CI underscore pipeline underscore source. That means what triggers the pipeline. If the merge request triggers the pipeline then this variable value will be merge underscore request underscore event. That will be the value of this variable if it is triggered by a merge request. But if it's triggered by a push then its value will be push. We have seen that in the previous lecture. Now there are a few other triggers. You can trigger the job from the web interface from the GitLab UI. Also, you can add schedule to a job. You can decide when the job runs, let's say every night at 9:00 pm and you want to keep such triggers also those. Also you need to define with the rules. So if the trigger is manual so I can put a condition if this variable its value is web, its value will be web. If it's triggered manually from the UI, that same variable value can be web. It can be merge request event. It can be push based on how it is triggered. Also, if it's scheduled then its value will be scheduled. Otherwise I do not want to run this. So I can say here hyphen one. Never. So only any of this condition matches, then only the job will run. Otherwise never. So this is just to make sure that this job or this stage never runs. If this condition are not matched. So just to be sure, we can put this. Otherwise it's really not mandatory to put whenever. Now let's give the same rules to the testing job. So I'm going to copy all this rules go to the testing job just below image I'm going to paste all this save this and let's commit it. So let's go to commit commit and push a commit message I'm testing some triggers I've given that commit message. Accept this. Let's go to build pipelines. And you see it has triggered the pipeline. That was because this condition matched the first condition itself matched the commit branch is main branch that triggered the pipeline okay. So the pipeline completed successfully. Now let's test a few other things. With this pipeline. We'll create a new branch and make a commit in the new branch. And let's see if that triggers the pipeline. So let's go to the repository tree and click on this drop down where you see main here. Click on this drop down and see new branch. I'll give the branch name as feature nine. Feature 91 creates from the main branch. So when I do that, all the content from the main branch will be copied into the feature 91 branch. And this branch is available in the GitLab repository. Let's fetch it into our local VS code. Let's go to Source Control symbol over here. Click on this three dots and say fetch. Once that is synced, you can switch to the feature branch that you created. You should see here feature 91. And let's make some changes. Let's go to README.md file. Add or remove some hash from here a new change. Let's commit it. Now keep in mind this is going into the feature 91 branch drop down, commit and push. Yes, I commit message updated Readme file. Accept this. Okay. That's done. There's a new commit in the future 91 branch. Let's go to GitLab. Now let's refresh this. You see there is a new commit the commit message. Let's go and check the pipeline whether the pipeline got triggered okay. So nothing right? No pipeline got triggered. That's because you have made a commit in a different branch. But if you have committed or created a branch with develop and made commit, that would have triggered the pipeline. But what if there is a merge request event when that happens? So there is a new branch and you want to merge this branch with the main branch. That's what happened in the development environment. Developers create branches. They make changes into these branches. Once they are tested, they're well and good. They merge it to the main branch. That's how they do it. Merge requests create merge request. So source branch feature 91 which has moved ahead of the main branch. This branch needs to be merged to the main branch. Compare and continue. Give a title to your merge request. I'm keeping it as it is. Scroll down. Who do you want to assign to? I'm saying assign it to me because I'm the only one user, but there could be other users like you are a developer and there is an admin who should review the merge request. So you have to assign it to an admin and you can assign the reviewer also who can review this. So I'm keeping it my user only because I have just one user here. Let's scroll down and there's an option to delete the source branch when the merge request is accepted. I put a check mark. Keep it as it is. Say create merge request. Now this is going to delete the branch. Feature branch when this is accepted. Right. But what happens when I create the merge request. You see the pipeline merge request pipeline a pipeline got created. Let's click on that. The pipeline got triggered. That's because this condition was matched. Merge request event was the trigger and thus variable will have that value. Merge request event. If that is true, then execute the job. This one also the build and testing both jobs. Now this is just a merge request. We have not merged the code. When this pipeline completes we can merge it to the main branch. So let's wait until this pipeline is completed. Okay, both the job completed successfully. Now, do not take this merge request lightly. In fact, in GitOps project setup, everything revolves around this merge request. I have created projects in DevOps project course that shows exactly same. The entire project revolves around the merge request. Any change goes through the merge request only. But right now we are just learning GitLab and how to use it so nothing is right or wrong. Over here we are learning different settings, different styles. Once we are good with this, then we can work on the projects and see how to best leverage these merge requests. But now we need to clean the branch. If you go to sorry, the merge request, if you go to merge request, we have a branch to merge. That was just the merge request and the pipeline is passed. And now we can merge it to the main branch. So click on merge. This is going to delete the source branch. The source branch is the feature 91 branch. So click on merge. Now if you go to the repository you click on the drop down. You just see the main branch. You don't see the feature branch, but that branch exists in our local machine. And we should delete this also. So click on this three dots or go to the terminal say new terminal. I'm using git bash. So I'm going to select the terminal as git bash. If you're not using windows then you can stick to your terminal. For windows you can use PowerShell or you can just switch to git bash from here. Do git checkout main switch to the main branch and from here delete the branch git branch hyphen D and the branch name feature 91. Now run the command git branch hyphen A. You still see the feature 91 branch. We need to prune this. Remove it. Remove this information. The branch is already gone. Run the command git fetch hyphen, hyphen prune and then run git branch hyphen a. You should see only the main branch. So asking us to sync changes. Let's sync the changes okay. This is mandatory to do before we go to the next lecture. So make sure you delete the branch on GitLab if it's not deleted. And then delete it over here from the command line and prune it. Sync everything. Or if this is giving you problem, just close vs code. Delete the branch in git lab and clone the repository. Once again, you can remove this folder and you can clone a new repository from GitLab that will be a fresh repository cloned from GitLab. All right then that would be all in this lecture. I will see you in the next one.
