# Inventory Part 2

-: In previous lecture we have seen how we can set up inventory file. We did it for just one single host. Now we are going to do it for the other two host. And we'll see a few more concepts in inventory like groupings and variables. So I'm going to get into 'vprofile' folder and I'm going to copy from 'exercise1' to 'exercise2'. Okay, let's get into 'exercise2'. Okay, so let's open the inventory file. And now we know how to add one host we can like that add multiple hosts. So copy these four lines. Come to 'web01' and say 4yy, quickly copies the four line. Then just say, come at the last line and say P to paste and let's change 'web01' to 'web02' and come down once again and say P once again. Using via editor copy paste methods, okay? You can use your own copy paste method also whatever you want. Okay, let's change this to 'db01'. Make sure they are all in the same line, okay? YAML space is very important. In the same column I should say, not line. Okay, now everything is same but the IP is different. So we have to check the IP of 'web02'. Get the private IP of 'web02' and change it. And then same for 'db01'. So remove that. Let's get the IP of 'db01'. Private IP once again, paste it there. And let's test it first. So username and login key are same. Okay, the host changed. Okay, so once again ansible. Now we will do ping to 'web02'. That works. Let's do it to 'db01'. Okay, that also works nice, right? But what if like this you have 10, 20, 50, and like that many, many host, right? Doing a ping to each and every machine is going to take like forever, right? So don't worry, there is a concept of grouping. So you need to definitely have the host information first of all, right? In the host column, right? You mentioned the all the host information. And then in the same column of the host, so that was what, two space? One, two, two space. You have to mention this 'children:', and then you need to mention the group name inside this. So I will say 'webservers:', hit enter. 1, 2, 3, 4, 5, 6. And I have to say 'hosts'. So what are the hosts of the group web server? So that's pretty simple now. Hosts are already defined up here, right? 'web01', and once again make sure it's in the same line, and 'web02'. Okay, so this is one group, web servers group and these are the host. Like that we can create multiple groups. So I'm going to again copy paste, 4yy and paste there and this will be 'dbserver'. Again, make sure they are in the same column. Same column. Host 'db01'. So yes, you can have a single host also in a group or you can have multiple host also. You can also have group of groups. So that means, you know, I have here two groups, right? I can group this group together in a parent group, pretty simple. In the same group line I'll give a group name. I'll say 'dc-oregon'. Now, "-" is not at all recommended, It'll give you a warning or it may also give you error if you're using a different version of Ansibles. So don't use "-", you can use "_", okay? Other things, we have to end this 'web01' with a ":" and 'web02' also with the ":" and 'db01' also with the ":". Again, of course the group name also with the ":". Now, I'm going to give here now again two space in the inside, and here you have to say again, 'children:' and you know now the rest, right? You can give the group names. So right, so we have 'webservers:' and 'dbservers:'. So basically we have now three groups, webservers, dbservers, and dc_oregon, or whatever you want to name it. Webservers dbservers are normal group, they contains the host names. But dc_oregon is a parent group that has more children and those children will be the group name, okay? They should be group name and not the host name. It'll look for this group here and find the hosts from that group. So when I say dc_oregon that means all these three host are these two groups, okay? Look at the structure and save and quit. Okay, so now what? Now we have groups on which we can execute modules. The the first module that we learn, vim module, we are going to execute that. Okay, so instead of 'web02' now I'm going to say here the group name 'webservers'. Make sure the group name is correct as whatever is mentioned in the inventory file, otherwise it won't work. Okay, you see when I say 'webservers' there are two host, right? When I say 'dbserver' you know that's just 'db01', right? This is not just about ping, okay? Any module like that you can execute for the group. 'dc_oregon', let's do that. So 'dc_oregon means the two groups which contains the other three host. But if you really want to execute something on all the hosts you can directly here mention it as all if you remember the name in the inventory file, right? All at the beginning, all means all the hosts. You can also use "*" but you have to put it into single code. The single code mention "*". That also means all. You can also do it like this. You can save 'web*'. That means any host that starts with the word web. All right, so similar regular expression that we learned in Linux Star and there. So, so you can use those as well. Okay, now one last thing in the inventory file and we will do it with a different exercise. I'm going to say 'cp -r exercise2' copy with the names, 'exercise3'. Now creating another exercise for this one it's a very simple concept, but very very important concept. We are going to learn more about this incoming lecture. The concept is basically variables. So we have variables at the host level here. Host level variable has highest priority. That means there are other places also where we can define variables, right? Well, we can define variables at the group level. Also, group will have lower priority compared to host. If it does not find variables at the group level, sorry, host level, then only it'll go at the group level. Now we have a group, 'dc_oregon', that contains all the three hosts, right? So if we define these two variables at the group level we don't need to define it at every host level, the login key and the user. Okay, let me copy this and come down and 'dc_oregon, where you have children in the same column mentioned 'vars:', and inside that let's paste this. Okay, I have to make sure it is properly formatted. So we defined it at the group level, at dc_oregon group level. Now we are going to remove this because host level has higher priority anyways, it is going to use the host if it's already defined. So let me remove this. And for 'web02'. And for 'web01'. So now you should only have the IP address Ansible host variable, login user and the key should be defined at the group level. And let's save and quote this, make sure the structure is correct. And now let's test it. 'ansible all -m ping-i inventory' path, oops, space. Okay, that worked. Try to change it and see if it's really using those or not because definitely it needs username and password. I'm gonna just say ec2 -, and you know that's a wrong username, right? So it's going to try to use that user for all the machine and it is going to fail. See that? Look at the error, permission denied. It is using the user called ec2, there is no such user in this machine so that's why permission denied. So permission denied you'll get if the username or the login key any of this is wrong or both of them are wrong. So we know ec2-user is the correct user, right? So either login key or the username, anything is wrong, you will get permission denied. It works now. Okay, so in this lecture we have focused on the inventory file and we used a very simple module called ping to test the login. In the coming lectures we are going to see how we can use modules to make changes to these machines, like installing packages and managing services and files and et cetera. So test all this and join me in the next lecture.
