# Roles

Instructor: Hello and welcome to Ansible Roles session. Before we get and create Ansible roles let's understand why do we do it and how does it really work? Ansible rules is to simplify your playbook content. Now we have a playbook that has so many things. We have the global declaration where we can mention options like become, or even variables. We have lot of variables. We can have them inside the playbook or the outside, like in group vars or the host vars file. We have a lot of tasks, files that we are pushing, templates, handlers, and you could have many more things. We have seen now already playbook where we are provisioning a service, NTP, which is also very simple service but already there's too much of data in our playbook which is becoming difficult to manage now. Already our directory structure is looking something like this. We have configuration file, we have files directory. We can push some files. We have inventory, templates, playbooks, and so many thing in our playbooks, right? Like we can have variables, we have a lot of tasks, handlers, templates, copy module. To make this code more manageable we can use Ansible roles, which really simplifies or segregate all the content from our playbook. But first we need to identify this role of servers that are in in our infrastructure. So you may have some computers that are playing different kinds of roles, some MySQL, some Tomcat some Build Server, some Apache and common setup like what we did. NTP could be common across all your servers, or a monitoring agent, or a logging agent. So some server may play a very common role on every server, You'll have NTP monitoring agents, some configuration as per your organization standard. Some will be playing very specific role like database MySQL or database Oracle or application server like Tomcat. So if you identify the roles then you can create a roles directory structure. So you create a directory called roles. Inside that you create a directory with the role name. That will have sub directories where you can distribute the content of your playbook. Like tasks, you have main.yml file, which will have only the task. Templates will only have the template. Vars will only have the variables in main.yml file. Files will have only the files that you're pushing through copy module. Handlers will have only the handlers in main.yml file. Now here we are distributing our playbook content into different directories. Benefits? Well it's easy to access and make changes. It gives modular structure to your Ansible playbook, or your Ansible code. And this we very specifically use for reusability. If you want to reuse this code, like for let's say for most of your project in your organization you have Tomcat server. So we'll create a role for Tomcat setup that can be used in different different projects. So role is really at the organization level we create so we can reuse it into different different projects or even different different environments. If you are not doing reusability then there is no use of creating roles. So let's see how we can create roles for our playbook we have created so far. Okay, so for this one I'm going to create exercise13 to exercise14. Let's get into exercise14. And I'm going to add a few more tasks in the playbook and variable also, that is just to complicate our playbook so we can simplify it by using roles. So let's open the playbook. I'm going to use a copy module and copy a file. I simply say Dump file. Then I use copy module, and I'm going to say files: files/myfile.txt. And dest /tmp/myfile.txt, right? The source does not exist but we'll create that in a moment. Let's add one more thing. For this task, which creates a folder, I'm going to give here instead of this variable. I'll say mydir, and this variable I'm going to define in the playbook. Now generally we don't do it, it's better we define out of the playbook. but this is just, I'm complicating the playbook. No, this is vars, not var. I will give the variable name as dir. I'll say dir1: /opt/dir22, anything you can give is simply going to create that directory by using the file module, okay? And there's an extra space here, let me remove that. Okay, let's save and quit. So anything, add some variable. Yeah, use it anywhere in your playbook if you want, okay? And we have to create the files folder. For me, the files folder exists, if it's not there for you, create the files folder, and inside that, let's create the file, myfile.txt, going to say myfile.txt, and put whatever content you want. I'm just going to put some special characters here, nothing else. Save and quit, and let's test the playbook. So in our playbook we have variables, in our playbook we have files, we have templates, we have tasks, we have handlers. So understand, right, why we're putting all this, because we are going to simplify this in two roles. Okay, got an error. Undefined variable, mydir is undefined. Let's see, I think I gave the wrong variable name. The variable was supposed to be mydir, sorry, my bad. But you know what happens when you give wrong variable name right? In the task I have given variable is mydir, I have not defined that variable. I give different name. So obviously it will not work. Okay, another error, yes, SRC, or content I need to give in the copy module I gave, what's wrong with me, files here. I have to give here SRC. Okay, now instead of rerecording, I thought, you know, I'll keep this one, these two errors. So you'll also see if this kinds of error happens, you know what does that mean, right? This task was expecting, the copy module was expecting, source or content but we have not defined anything, neither source nor content. And that's a mandatory option for the copy module. So now we gave it, okay, fine, right? The playbook worked, okay? And we have enough material in our repository, says 14 is our repository. Let's install a tree in optional package, aot install tree. So we can see it in a tree format. Alright, let's do cd .., and we'll see tree exercise 14. Okay, so we have Ansible configuration file, we have files folder, we have group vars, we have host_vars, we have templates we have the playbook, right? Most of these things will get simplified and it's very easy to write a role after you have written the playbook, for a working playbook I mean to say. Writing roles from scratch, some task, we need to do a lot of things, but if you have existing working repository of Ansible playbook, converting it into roles is very easy. All you need to do is create a folder named roles, get into that roles folder, and you have to run the command ansible-galaxy init, and the role name. So we will say post-install, right, as the role name. You can give role name anything, you have to just mention then the same role name in the playbook which you're executing. Okay, so we have the tree command. Let's run the tree command here, tree, hit Enter. You see it created all that folder structure for us. Now we are going to simply copy things. So starting with vars, we have vars folder, in that we have main.yml. So we have roles in group vars. Wait, not here, I need to go one level up cd ... All right, so cat group-vars/all file. We have variables here. Let's copy all these variables, and let's open roles/post-install/vars/main.yml. And in here we are going to just paste this, okay? We have also variable in the playbook, cat provisioning.yml. I shouldn't have done cat, we could have done this variable, right? And put that again in the variable file. Okay, so we moved our variables. I'm going to remove group_vars file. I'm going to remove host_vars also. Okay, I'm trying to make things more cleaner. I copied the variables. Okay, now we have files. So I'm going to copy all the files to roles, the role name, files. I'm going to copy all the templates to roles/post-install/templates, okay? Now we have the playbook itself that contains so many things. We are going to do cat to the playbook. And first let's start with the handle. Let's take this handlers, copy all this, and let's open roles/post-install/handlers/main.yml file. Okay? The text goes there. And that's paste the handlers over there. Now see this is in YAML format, right? This is list of handlers. So there should not be all this space here in the beginning, right? How many space we have, let me count it with. So one, two, three, four, four space. I'm going replace all those spaces from the beginning with nothing. And I'm going to do it in Vim editor. So :%s/, and do a caret. One, two, three, four, replace it with nothing and hit Enter. See that, all the four lines from the beginning is replaced with nothing. So basically removed the beginning four lines. Now let's take the task, all the task, a lot of task. So let's copy all the task. Okay, exactly to here. And then let's open the task folder, main.yml file. This is the most important one actually. When we execute the role, this file gets executed task, and when the task needs all those handlers and variable those things will be called, right? So paste the content, and then same thing, :%s/ caret, one, two, three, four, four, forward slash, and a forward slash, right? In between those forward slash, nothing, so replace it with nothing. Okay, done, 63 lines at once. All right, that was interesting. Now let's open the playbook and let's go the beginning. So variables we have removed, right? So we have moved. So now we can remove these variables from here. Task we have removed. So I'm going to remove all the task and even the handlers. Okay, glad I stopped at the right place. And now we have to say here, roles:, and you have to give the list of roles. You can give as many as roles we want. We have just one role, we're going to say - post-install. It's in a list format. So you have multiple roles, you can keep doing that, hyphen and the role name in the same format. And let's save and quit this. Okay? Now look at the our playbook, right? It's now so simple, right? And the directory structure is also looking good now. Okay, we don't need this files and we don't need this templates, okay. We are not going to execute the playbook now, we have one more thing to do. Let's open the playbook. Oh, sorry, not the playbook. We have to open the task roles, role name tasks/main.yml. Okay now template and copy module are smart enough to know where to look for templates and where to look for files so we don't need to give here templates, slash, the file name, okay? It knows the structure, so we have to just give the template name. It is going to, by default, look for templates folder in the roles. In the roles structure it will find the templates folder. In that, we look for the template with this name. Okay, one more thing we have to do, actually, the template should end with .j2. That's not mandatory, but a standard. So we have to replace that with .j2, here also. And remove this template in the beginning. And the file name also, obviously we have to rename it .j2. Okay? Save and quit this. Oh, in the files also we have copy module, right? Yeah, there. So we're just going to remove these files. So anyways, the copy module will look into the files folder of templates, sorry, files folder of the roles. Okay, save and quit this. And we have to rename, let me get into roles, templates, wait, sorry, roles, template name, and then inside that we have templates. Okay, let's rename this to .j2, .j2, okay, same for this one, ubuntu. All right, because in the task we have given the template name as .j2, right? And it will not find if the file with that name does not exist, so that's why we have to rename this one also. Now let's come back to exercise14 folder. And now let's test the playbook. Ansible-playbook provisioning.yaml. Okay, let's look at the output. It's slightly different, you see that? TASK post-Install, it's giving the name of the role and in that this task is there, which it is executing. Okay, that worked, obviously. Now in this playbook we have just given the role, it's so simple playbook. If you want to add more task, you want to add more handlers, yes you can give here also you can see a task, and you can give the task. But now we have the role separate for that particular server for the list of those servers. So if we need to add more tasks we goes into the task main.yml. If you want to add handlers, we go to handlers main.yml. If you want to add variables, we can go to the vars. Well talking about variables, tree roles. So variables we can put in this file where vars main.yml. We can also put it in defaults main.yml. The difference is this one will have low priority. Defaults main.yml will have low priority, vars will have higher priority. So in general, it's a general practice to define variables into the defaults main.yml, right? But in order to make you aware about all the structure I, by default, gave it in the vars folder. But in general, we put it in defaults main.yml file. And roles are created for the organization, so the entire organization will have different, different projects. They can use the same roles. It creates a standard, but then the values might be different, so we handle it through the variables. So we can override the variables, not by directly editing the files, we can do it through the playbook also. We know playbook variables have higher priority. So let's look at the roles, roles, documentation. So Ansible roles, let's go to this first link, roles. So the roles structure, right? We know this well, we read this, it has the information about each and every file. Okay, so let's come here. So roles, you can say - role, and you can mention the path also. If your roles are located in a different folder, by default, the roles folder it is going to look for. But if your roles are in some other path, other directory, then you can specify the path also, right? We did it like this. Now about variables I was talking about, right? If you want, you can overwrite the value of the variables you have to give, vars, variable name and its value, right? So let's say we want to change the NTP server values right? Let's say this is for a different region, let's say for some India region, Mumbai or Hyperabad, right? And these are the values. So let's take this. Right, now let's say these rules are used by some other data center to configure the NTP, right? So you know what? Let's create a different exercise for this one. CP -r exercise14 to exercise15. Let's get into exercise15. Okay, first we will move our variables from vars to defaults. So roles role name, vars/main.yml. Let's take those variables and copy it into roles/post-install/defaults/main.yml. And then we'll remove from vars, because vars has higher priority, so by default it will use that one. Okay, so if we execute the playbook now it is going to work without any problem. It's going to find the variables in the defaults. Let's test that first. Okay, it's working. Now about overriding the variable's value. So in order to define roles first we have to change here, - role, what's going on here, - role: and the role name, which has changed, yeah. And then we have to give here vars, right? So our same variable we can give override one or many variables will override all the variables. So NTP zero. So you can take NTP server value from anywhere whatever you want. I'm taking India's NTP server. Simply going to copy paste this thing and change the values. The other variable was ntp2, sorry, was ntp1, Well we can give a different order also, no problem, but this looks better. And here, change the names. This was one, this was two, and this was three. Okay, so roles, - rule and the role name, and just below that vars and you can override the variables. So we know playbook variable has higher priority. So it is going to use the variable values from the playbook rather than checking from there. Okay, so the configuration files are changing the NTP configuration file. So template module found the variable values are different. Basically the source file is different. So it push new source file, and then the handlers are called which are in a different folder, right? So like this, we have tested everything. We have tested task, variables, handlers. So now you know how to write roles. It's better or easier method is first to write the playbook and then convert that into roles, that's the easier method, right? But there are also ready-made roles available. Ansible Galaxy, galaxy.ansible.com. So here we have roles created by the community. We have variables for, sorry, roles for system, for monitoring, packaging. So instead of writing your entire roles from the scratch you can use these roles and override the values if you want. So let's check for System section. These are into different section. And let's say I want to install Java, you know, based on the operating system, right? I have to make the conditions and all those things. Instead of that, I'm just searching for the role in with Java, geerlingguy, very popular author, Java, right? It has the ratings and scores. Also, quality score is five by five. Community score is 4.3 by five. It's very good. So what you can do is you can just download this role by using this command, Ansible Galaxy install and the role name. And then you should also have examples here, Read Me. Now you have the variables that you can override, how to use this role, right? We know how to use the role but this is the name of the role. And you can make conditions and you can add other task also in the roles. So this is a very simple example. Let's just execute this command and download the role first. So downloads from github.com and stores at this location, right? All you can do is once you have a role, if you just want to use it just like that, you can give here. So I will give it here just before this one. I'll just say hyphen, and I'll give the role name, right? So this is the role name, geerlingguy.java. So this role will get executed first and then our role, let's test it. So this role has all that same structure and it has task based on the operating system. I will get into this role directory structure and we'll see how the task and other things are given, right? But you know, let's see, let's test it, right, whether this works for us. Now based on the operating system, it is going to install the right version of Java for you. So you don't need to go through writing the entire playbook for installing some packages or even setting services. We have roles for setting up database services, and monitoring services, and even NTP, Chrony, all those roles we have ready made available, right? So we have that. But if you ask me in my personal experience I use Ansible Galaxy roles very less. And the reason is pretty simple. If I have to make changes, right, not the variables, changing variables are pretty easy, we have seen that. But if I have other configuration, other things to change then changing these existing roles, which was written by somebody else, we need to do reverse engineering. We need to look at the code, all the task and handlers. We need to make changes many places. And Ansible playbook itself is very easy to write. We have documentation, we have ChatGPT. So one time work, we write it from the scratch, right? But we can reuse our code, we can make changes, we can convert it into roles, we can do many things, right? Let's look at the role which was created by the linga for Java. So in in your home directory there is a .ansible folder, inside that you should have roles in that you should have the role name folder, right? So take a look, similar structure. I'm going to run three command here. You see there are many variables file, there are many task files, right? So it's just for the package. So we just have, you know, one simple template and different task and different variables. Now here the main.yml file is the default file. Whenever you run the playbook, sorry, whenever you run the role, this file task get executed. But you can have file with different names also. And let's see how it is defined. So tasks/main.yml. Okay, see, this is what I was talking about, reverse engineering, okay? So it says include variables. This is a module that can include variables file. So that's a variable. Actually this is a file name. This will be changed into the, this is a fact variable, right? So based on the operating system if it's sent to us, it is going to load. Send us .yml file. Now for it to run here's the condition. Ansible_distribution, if it's FreeBSD, or Fedora, then load this file as variable, right? And then for RedHat, again, you have conditions here. This is just for importing variables. Then task, run this task if the ansible_os_family value is RedHat. So all the RedHat task is defined into a different file. All the Debian task is defined into a different file. So you can do that also, instead of mentioning the task and the condition, you can create a playbook for RedHat separately for Debian, FreeBSD separately. And then you can execute that by using include_tasks module. This is going to simply import this task and run it if this condition matches, right? I use these roles to study them and see how they're writing. These are the industry experts in writing the Ansible playbooks or roles. And we can learn from them, right, in writing our own playbook. So that's how I use the roles from Ansible Galaxy Because documentation is huge, you can use ChatGPT and other stuff, right? But all that intelligence was already used to create such roles, and we can harness their intelligence, right? We can learn from them, right? So there are many other roles to download, and read, and understand from. From here on, I leave it up to you. This requires more creativity and there's many, many many things that you can use in Ansible playbook. Whenever the requirement comes, look for it, search for it. You have documentation, you have ChatGPT, you have Ansible Galaxy rules to read from, and you have to use your own creative mind to go from here. This is all in the roles lecture. In the next lecture we are going to talk about how we can use Ansible for cloud automation, right? So take some time, go through these playbooks. I'm not telling you to memorize them, just go through them, right? So you know, there are many more things to explore, you have many more options in Ansible, right? So wrap this up and then join me in the next lecture.
