# Handlers

-: Okay. This exercise is about handlers. I'm copying exercise 13 to exercise 14. Ah, sorry, my bad. Exercise 12 to exercise 13. Okay, let's get into exercise 13. So we already know the problem. I am executing the playbook and every time it is restarting the service, right. Now, we don't want that. We want it to only restart the service whenever the configuration file is changed. Now you can also use 'when' condition for that but Ansible gives you a much more better way, which is handlers. First I'm going to delete this task, 4dd, this file task, I'm going to bring it up somewhere. Yeah, somewhere here. Just below this banner. Okay. So the reason because I wanted to keep this restarted task at the end. Now, doing handlers are very, very simple. You have the task to restart the service and just in the same line, wherever you have tasks in the same column, sorry, you have to say handlers. Okay? Make sure the handlers are in the same column as of the task. Yeah. So handlers and task may look same but handlers will not get executed until they are notified. And how do we notify them? Well that will be from the task which wants to execute it. So for example, whenever the centers configuration file changes, then I would like to notify. This is what you have to do. You have to say notify colon. Again, notify should be in the same column as the module name and you have to give the name of the handler. So you have to say hyphen and you have to give the name. So this is for CentOS right? So let's give the exact same name, make sure there's no spelling mistake. Now it's in a list format, so you can give n number of handlers over here in the list but we just want to notify it for one handler and this one should be for the other handler, right? Let me just copy this one 2yy and paste it here and change this CentOS to Ubuntu. Okay? Make sure the names are correct. This is for Ubuntu handler, should have exact same name otherwise it'll say handler not found. So once again, whenever there is a change in the task. Now this is not just about restarting the service, okay? It can be used for anything. Whenever there is a change in the task, you can notify to any handler. The handler will get executed only when there is a change in the task. If the task does not have any change you know there is json value, right? Changed true, changed false. So it's going to read that one. So whenever changed value is true then only it is going to notify the handler and handler will get executed. When the changed value is false it is not going to notify, simple. So let's test this. Now we have not made any change to the files. So what do you think? What's going to happen? Will the handler get executed? Let's check that. If your answer is yes, you're right. The handler will not be called because we have not made any change to the source or the destination file. Okay? You see deploy and ntp configuration file. So state is ok, ok, ok. So you see no handler was called. But let's make some change. Let's make change to any one file. Let's do it for CentOS. So only CentOS handler will be called. So we'll not make any actual change. We'll just put some hash, right, which anyways is comment or any space or anything, okay? Just don't change the actual configuration content. So make, you know, give some space or put some hash. Let's execute the playbook. So we may change only to CentOS configuration file. Let's see now. Okay, so the configuration is changed and the handler is executing. That is only for CentOS. You can test same for Ubuntu or both. You can change source or destination anything. Whenever there is a difference in both the files it is going to push the file and the handler will get executed. Now, from my own experience being a DevOps engineer from a long time, we use template module a lot and along with template we use handler. It's a very common or very general combination, templates and handler for configuration files, simple as that, right? By default, but obviously based on the requirement we can use different modules also instead of template. But in general for configuration, we use templates, module and then we obviously need to restart the service, so is handlers. Once again, handler is not only for restarting service you can give any task over there. Let's say you want to add a user when the user is created then you want to push some file content to the user's home directory, right? So you can give the task as user, which creates the user and the handler will be copied to copy some files or anything like that. Just giving you a random example, right? Because generally whenever people see this they think handler is to restart service. That's not the right answer. Handlers will be mostly in an dormant state, right? Whenever there is a work, like you see in Hollywood movies you know we have agents, right? They handlers CI handlers, NSA handlers, right? We've seen those in the movies. Right, so they will be in a dormant state whenever there is a requirement, they will be notified, right? If you've seen Jason Bond movies like that and they get notification and then they execute their task like that. Handlers here, we keep them dormant state. Whenever the notification is sent, they execute the task. Let's take a look at the documentation of handlers. Okay, the page has moved, anyways. Running operations on change. Let's click on that link. Okay, let's look at the example. So see that template module, right? Pushing some configuration, notify to this handler, restart Apache and here you have the handler. Okay? Example of calling multiple handlers. See that, you can call multiple handlers, right? Maybe for a configuration file you need to restart a service and that restart requires restart of another service also. Right? Like that. Okay? Naming handlers, we have to give proper names. Controlling handlers, using variables in handlers. Handlers in roles, roles, yes. That's what we will be looking in the next lecture, we'll be talking about roles. So most of the time it's same, the handlers if you use in roles or in normal playbook. Anyways, we'll be looking at roles in the next lecture. So test all those things. Wrap this up and join me in the next lecture and see for yourself what are roles.
