# Group & Host Variables

Instructor: So we have seen in previous video how we can define variable in the playbook and use them and also how to print them. In this video we'll be seeing inventory based variables so we'll see variables for the groups. We'll see variables for host and also common variables and we will also understand their priority. So let's get to our control machine. Okay, so this is a continuation of previous lecture. I'm going to use the same exercise. Exercise eight. We will be using the same playbook and we have seen how we define variable in the playbook. That was for understanding purpose, but mostly the variables will be defined outside of the playbook. That is for reusability purpose. So I'm going to create a folder here called as group underscore vars same name, okay. Spelling mistake. Okay, group vars. Okay, exactly this name. And you have to create a file inside that with the name all, A L L, all. Okay, no different name. It's the structure of defining variables and I'm going to define here all the three variables that we use there. DB name, I will give it as Sky. DB user, give different than what we did previously okay? Pilot, pilot. DB pass, Aircraft. Okay, so these are three variables that we define. There's the same variable, what we define in the playbook the value is different, that's all. Now let's run the playbook. We have seen variable defining the playbook and here which one will it use? Let's see. Okay, still using the one that is in the playbook that means playbook variable has higher priority and let's open the playbook and let's comment this variables. I'll put hash in front of them. Okay, see one could just put hash in front of all the variables and vars. And let's clear the screen and let's do it once again. Now the variables, it will not find in the playbook it will look for playbook, it will not find, so it will go out and look where will it look? Well it has its structure. Group vars all. See that? The DB name is Sky and the DB user, pilot and there the changes were also done and it's printing also. Okay? Now there are a few other places where variables can be defined. For that we need a different playbook. We'll create a different exercise. Exercise nine, it will be, let's get into exercise nine. Okay, in this I'm going to remove this DB playbook and I'm going to remove this group vars all. We'll create another one once again. I'll create another playbook here called vars precedence dot yaml. Now I want you to understand the precedence of variables vars and so we will pick up variable values from. For that a small task. Okay? One simple task creating the user. The module is Ansible built-in user and we are running it on all the host, okay? And here we are going to define or mention the variable that we want to use. I'll say use RNM. And there's one more option in the user module, comment which is just for the dummy purpose and we can use it to understand stuff here. Now this is a pretty simple task. It is going to create a user and give some comment to that user. So variables we are going to define here itself. USRNM, I'll say play user and comment variable from playbook, okay? Spelling mistake. Anyway, that was just a comment. So we know it's pretty simple what's going to happen? It's going to create a user and it's going to look for the variables here. It's going to find it in the playbook and use it right? Let's execute that. Ansible hyphen playbook. Vars precedence. Right? That worked, but we also wanted to print the output. So let's register the output register USR out and we'll also use debug module to print that. USR out. Okay? Create the user, store the output, and print it simple. Alright? It printed the output but for three hosts, web zero one, web zero two, and web zero, yeah, DB zero one, but it's the whole JS output. I'm just interested in printing the user name and the comment. So for that this is what you have to do. You have to say USR out dot comment, you get access to the comment USR out dot name and the play user. So also we have to use this two times. So first we'll say USR out dot name and one more USR out dot comment. So to access the keys inside the dictionary you have to say variable name, which is the dictionary. USR is a dictionary dot and the key name. Now let's execute it. Okay, there variable from playbook. Now same thing what we did previously, we are going to create a group underscore vars and insert that. We're going to create a file named all and mention this variable over the USRNM colon we'll say common user and for comment we'll say variable from group vars all file. Okay, let's save this. Okay, so we have same variables defined here and same variable defined in the playbook, right? Same variable values are different. So which one will it use? That's right, playbook. Because playbook has higher priority. Right? Still using from playbook. So we have to remove the playbook variable. I simply commented them by putting a hash in front of it and now we know it is going to use variables from all file. I know I'm repeating the same exercise, but I want this, want to be very clear. Okay, precedents. Now in all file, it here, the actually the all means the all the host from the inventory file, okay? This is inventory based variables you can say so all means all the variables for these host, but like that we can define it for the group level. Also we can say web servers and we can define different. So for that we have to create the file with that name, web server's name Right? So I'm changing it. Username is web group and the comment now, whatever I'm writing in the comment is just for your understanding purpose. Okay, so once again we have variables defined in the all file. We have variables defined in the web servers and we have variables. No, we don't have variables in the playbook, right? We commented it. So obviously we have a separate file for web servers groups. So for web zero and web zero two, it is going to use variable from this file and the rest, which is DB zero one, it is going to use it from this file. Let's execute it. Okay? You see that? For web zero one and web zero two it is taking from this file, look at the username and for DB zero one it is taking from all file because there's no separate file for DB. Now we can define variable at the host level, also. For that we need to create a folder called Host vars. And in that we need to place the file with the host name. Let's say we'll do it for web zero two, web zero two user I'll say here and comment variables from host vars web zero two file, right? Let's see when quit, and let's once again check. We have variables in the group vars, group vars all file. We have it in the web servers group file. And then we have host vars web zero two file. You see these are all the same variables, but their values are different, right? So now let's execute the playbook. Okay? So for DB zero it's still taking from the group vars all file because there's no separate file for it. For Y web zero two, there is a separate file right in the host vars folder and for web zero one it is taking from the web servers group file. So you look at the precedents over here now. Host has the highest priority. If the variable is defined at the host level that takes the higher priority, then the priority goes to the group vars, group Server, sorry, group name file. And the last priority goes to the all file. And all this is superseded by the playbook. If the variables are defined in the playbook it is not going to look outside. Okay, let's test it right? Once again, let's uncomma this make it in a proper structure. Okay so we know the highest priority for the playbook. So if I define variable in the playbook, then it is not going to go and look outside it's just going to use them, simple. So it has the highest priority or does it? There is also one more way of passing the variable and that is through command line. Ansible hyphen playbook, hyphen E, I'll say hyphen E USRNM equal to CLI user hyphen E com equal to still I'll say CLI user. So with hyphen E option in the command line you can override the variables value. Now remember the variables are defined in the playbook also now. So let's see who wins now. Hmm, command line? So command line variable has the highest priority, but it is very, very rarely used. It's only we use sometimes for testing things and we very rarely define variables in the playbook. Mostly it'll be outside like group vars, host force. Group vars all file is the most commonplace of defining variable, this file. So you also in the real time start from that group was all file. And then you can use other files based on the requirement. Okay? Ansible variables. You can just simply Google this using variables we have used simple variables, name and their value, like this right? Name and the value. And we have seen how to use it right? In curly braces, inside quotes. So here it is saying, you know without quotes it'll give you this error. So make sure it is with the quotes. Now there are also list variables which is also very commonly used. So instead of giving the value just like that as a string, you define it vertically with hyphen separated in new lines. You can give multiple values to one single variable and when you want to use it, you can use it like this. The variable square bracket and the index number the same what we have seen in Python. So region zero means northeast, region one means southeast, region two means Midwest. There are also dictionary variables. The variables. And you have key and the value. How do we access it? Well, we have already seen, if you want to access a key in the dictionary, we say dictionary name dot the key. Or you can use this format also, but this is most commonly used. So foo dot field one, foo dot field one is going to return one. This is used when you want to group variables together. Registering, we have seen how to register the output. Defining variables in the playbook. We have seen that and defining variables somewhere else. Now, so far we have seen the structure group vars, host vars, those are inventory specific, but you can define variables anywhere else. You just need to say vars underscore files, hyphen and the path of your variable file. Now this will be playbook variables. So this will have higher priority. Remember this, we are importing it in the playbook. This is outside, but it will be imported in the playbook. So this will have higher priority and understanding variable precedents. We have seen the most common usage playbook, group vars, host vars, but there are other places also where you can define variables.
