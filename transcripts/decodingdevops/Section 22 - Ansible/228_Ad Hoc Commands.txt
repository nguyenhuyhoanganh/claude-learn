# Ad Hoc Commands

Instructor: Okay, so we have all set. We have the inventory file and mentioning, which mentions all the detail about the targets and we tested the connection also. Now, let's copy the exercise3 with exercise4. We are actually not going to write any files in this exercise. We are going to execute commands. These commands are called as ad hoc commands. You've already seen that. The ping module that we executed, that was an ad hoc command. Let's look at the documentation, introduction to ad hoc commands. Now, I love this feature of Ansible. I have used many configuration management tools. And every configuration management tool or automation tool, we write scripts with its own format. In Ansible, we use YAML format. We cannot call them scripts also, but just for understanding purpose. Ansible scripts are called as playbooks. We'll see it in the next lecture and it's written in YAML format. We mentioned all the details, right? But what if you want to execute something very quickly like maybe install a package or do something? It's not a good practice. Everything should be through the code, so you can version-control it and keep it in your repositories, right? So everything should be version-controlled. But sometimes, something needs to be done quickly and we can use Ansible ad hoc commands. Basically, whatever we write, playbook instead of that, we can run that in the command line, right? It says ad hoc commands are great for task you repeat rarely, right? For example, if you want to power-off all the machines in your lab for Christmas vacation, you could execute a quick one-liner in Ansible without writing a playbook. The pattern is same that we used for the ping module. So here's one example, ansible atlanta, -a and we are passing the command reboot, /sbin/reboot. So all the machines in that group will get rebooted or you can pass power off or other commands that we are going to see now, okay? So what about the modules? Okay, so we have seen ping module. That's the first module we've seen, the only module. There are many modules like you have ansible.builtin.copy. You mention the source, you mention the destination, okay? Some examples are here, file module, which can change the properties of the file or create folders. Managing packages, you can use, yum module or apt module, a user module to add or remove or change properties of a user, service module to start, stop services, right? So first, let's go through these basic modules and then I'm going to take you through the documentation. Don't worry, you don't need to by-heart these module names as clear-cut documentation will go through them, but let's first test this. Okay, so I'm in exercise4, I'm going to say, ansible webservers. You know what? First, I'll go with just web01 -m and I want to install a package. So I'm going to use the module ansible.builtin.yum. Now what to do with yum? Well, -a arguments, passing the arguments, in double quotes. Argument, name equal to the name of the package, I'll say httpd and state should be, wait, state should be present. Don't worry about by-hearting these things. As I told you, the documentation, you have all the arguments mentioned there, you can use them. So different modules may have different arguments. So name for yum is the name of the package. State, you have to mention present or absent. We'll look at all the other options that we can pass to it. And of course, I have to pass the inventory file path, -i inventory. Now when I run this command, it's going to throw an error. Take a look. Okay, there. Now read the error. First of all, it says failed. What's the message? "This command has to be run under the root user." Right. We are using ec2-user, right? Check the inventory file. The username is ec2-user. Now, that user has a privilege to do pseudo, but it is not going to do, until and unless you tell. So how do we tell, or how do we say? Well, same command, at the end you say, --become, -: -become will actually escalate the privilege. It'll run the pseudo command, pseudo yum install like that or any, whatever module you mentioned there with pseudo privilege. Let's see now. Okay, now look at the output. Changed, right? It made some changes to web01, what was that, results. And telling you, installed, what all it has installed. Right? Now, I'm going to do the same. Once again, just rerun the command. But now instead of web01, I'm going to give the group name. Okay? Now observe. We did it for web01 and now webservers contains web01 plus web02. So what happens now? What is the output? Look at the output. It's important to note. Right? Let's scroll up. Web01, it says success as the message, changed is false. If you check previously, changed was true. Message? "Nothing to do," right? That means web01 is in the same state that you are mentioning or its configuration is same that you are trying to apply. So this is configuration management tool. If the target is in the same state, it is not going to rerun the task for you. It is going to simply say, "Okay, it's in the same state. There's nothing to do, you can continue." But if the target state is different from what you are mentioning, then it is going to apply and then you'll see changed. So web02 was not in the same state. It has applied the changes. And now let's run this command once again. 01, success, web02, success, changed, false. Look at this, changed, false, okay? That means the target is in the same state that you're mentioning. This is called as configuration management. And this is the biggest different from scripts and configuration management. With scripts, the automation, what you do, it simply reapplies. It does not check, until and unless, you add some intelligence to it. It'll simply reapply the change. But in configuration management tool, it is going to compare the state. Focus on my words, it is going to compare the state. If there is a difference, then it'll apply. Now difference could be at the source or at the destination. What do I mean? Well, check this. Now, I'm going to change from state=present to state=absent, right? Now there is a difference between the state. So whatever now you're trying to apply, if it finds difference, it is going to apply those. So package was not absent, it was present. So now it removed it, okay? Let's run it again with present. Right? So someone ask you difference between scripting and configuration management, so you can tell this. Configuration management tools maintain the state of the target. And you can use this word called as idempotent. Configuration management tools are idempotent. What is idempotent? It's the same thing what I was talking about. If the target is in a different state, then only it'll apply the changes. If it's in the same state, it'll not apply the changes and your scripts or your commands are also not going to fail. Okay, now let me quickly write a few other commands. I'm going to clear the screen. Ansible.builtin.service, so service module, name of the service, httpd, not the package name, it's the service name and state, started enabled colon, sorry, equal to yes. So it'll do systemctl start httpd and systemctl enable httpd, both. And the huge output. Running it once again. This time, the changed value will be false, because it's in the same stage, right? Okay, one more task. This time, we're going to copy a file. But for that, we'll create some file first. So I'm going to create a directory here, or you know what? I'll create it here itself. I'll say, vim index.html. I will say, "This is from ansible," or "This is managed by ansible." Okay, so index.html file, we are going to push it to webserver at var/www/html location. So module name is copy and I'm going to remove all these arguments here. Okay, src, source, index.html, which is in the current directory, destination, var/www/html/index.html. You have to mention the complete path, including the file name, okay? Let's hit it. Okay, you see, changed, true. Now, I'm going to rerun that. What is going to happen? I have not made any change in the source or destination. So it's simply going to say changed as false. Right. But if I make any change to source or the destination, right? Let me add, you know what? I'll add some extra full-stops here, all right? A single change, single line space, anything, any difference, it is going to find out and it is going to reapply the change, okay? Let's look at the website also now. Okay, let's first go to the Security group of these instances, Edit inbound rule and we'll add rule here for 80 from anywhere, save rule, or you can say 80 from my IP, right? And let's get the IP of any of the web server, copy its public IP, put it in the browser and you should see the website, which is just the index.html file, right? Now, go ahead, make changes to index.html file, push it, observe the changes. This is configuration management tool. It maintains the configuration of the target. Now, we are just running ad hoc command. The much better picture you'll get in the next lecture when we learn how to write playbooks. So wrap this up and join me in the next lecture.
