# Inventory & Ping Module

Instructor: Okay, so we set the Ansible infrastructure. We have a control machine, Ubuntu operating system. Then we have web01, web02, db01, all send to S9. So first thing will be to establishing connection between control machine and all its targets. Now from here on, I'm going to call these EC2 instances as targets. When I say targets, that means web01, web02, and db01. So control machine needs to connect to target machines, and we know Ansible uses SSH. When we want to do SSH to these machines, to these EC2 instances, we use the command ssh -i, the key path, username, and the IP address. So we need to provide same information to Ansible, so it can SSH. So let's come to control machine. Log into control. First I want you to create a project directory. I'm naming it as simply vprofile. And let's cd into it. Assume this as the Git repository. But we are going to create a different folders for exercises, different exercises. So you can very easily do the revision, look at the previous codes also. So I'm going to create here exercise one. And exercise one will be about writing inventory file. Inventory file. Inventory file basically provides information to Ansible about its targets, its IP address, username, password, login keys, port numbers, whatever it needs to log in to the target machine. It's going to fetch all the information from the inventory file. And there are two ways of writing inventory file. One is INI format and the new one, which is YAML. So we will be using a YAML format to write the inventory file now. So before I start writing, I want you to go through the documentation for inventory. So simply Google Ansible inventory. You should get this link, how to build your inventory. Okay so initially, the documentation might look little confusing, but don't worry, we are going to run through. I'm going to write the whole inventory file, line by line. So if you do not mention any inventory file, by default, Ansible will use this as the inventory file. This is the default location of a default inventory file. And many people use this, but I do not recommend using the default inventory file. Always have inventory in your repository. So then you can fetch it on any machine and you can run your Ansible code. Ansible playbooks, basically. Now if you want to specify your own inventory file, you have to give a path -i and the path of the inventory file, which we'll see in a moment. How to build an inventory file. So this is an example of an inventory file. As I told you, there are two methods, INI and YAML. This what you see here is INI format, and this is the, sorry, this is the new one, YAML format. Okay, don't worry, we'll write it line by line. We'll understand it, it's pretty simple. By the time we complete this inventory exercise, you will very easily understand these inventory files. What I want you to see here is at the very end of this document, you have variables. Ansible_host, where you can mention the IP address or the name of your target. Ansible port, if you don't mention, it's anyways 22 by default. Ansible user, what user to use to connect to the target. Ansible password, but don't use password. Actually never use passwords in inventory file. It's very dangerous. You mention the password then in clear text. Always use the private key file. Like we do, SSH -i we give the key path, right? Similar way we'll be mentioning the key file path. And there are many other variables. You don't need to look at the documentation right now. Let's build the inventory file. Then you come back to the documentation and read, but read it please. Okay so let's write inventory file. Now inventory file name can be anything. And we will just name it as inventory. As I said, you can name it anything, because we anyways mention the path by giving -i. Okay, let's get into insert mode. Now it is in YAML format, okay? So first you have to say all, that means we are going to mention all the host, and colon, make sure everything ends with colon. Hit enter. And inside that, so we can use two space or three space. I'm going to use two space. Here you mention the host information, okay? Host, colon, again, hit enter. Then again inside that, so two space, then two space, total four space. And you give the name of the host that you want. So we can give any name. So I'm going to say web01, colon. And then it's information. Again, hit enter. Now total six space, 1, 2, 3, 4, 5, 6. And now we can mention those variables that we have seen like ansible_user, what user to use to connect to web01. Before that, again six space, we'll mention ansible_host. Now you can mention user first, host later, does not matter. But it looks standard, right? Ansible_host, and then you have to mention the IP address. So let's get the IP address of web01 and use the private IP, okay? Not public, private IP. We have given security group rule also for that. And ansible_user. Now that is sent to us 9 AMI from Amazon, and it's username is EC2-user. Not sent to us, EC2-user. Okay, now what do we need? Well we need login key, right? So we'll mention the variable ansible_ssh_private_key_file. Now don't worry, you don't need to memorize this, you know you can get it from the documentation. Now I already know this because I used Ansible a lot. So most of the variables you know got automatically by hearted. Anyways. Now let's mention the key part. Now we'll just first give the name clientkey.pem. Now this key does not exist. What we are really saying is there is a key with this name in the current folder, in the present working directory. We don't have it yet. We will copy that in a moment. And the name should be exactly same now, okay? So save and quit. So when we launched those EC2 instances, we downloaded the key locally in our machine. So let's exit from here. And I'm going to do cat to that key. Client-key.pem, that is the name that I gave. That was the name that I gave. So do a cat to it. So that's the private key to login. And you have to copy this. Carefully, you know, for macros, there will be an extra percent at the end. Don't copy that, make sure you copy those five hyphen, till there, okay? Be very, very careful in copying this. Copy. Make sure no extra space or any extra characters are copied. Now do SSH to it once again. Okay, let's get into our project directory. Exercise one, LS. Okay, let me clear the screen here. So in the present directory, we have the inventory file. And inventory file says, there should be a key with this name. So I'm going to create it exactly with this name. Remember I had client-key, right? But here the name I have given clientkey. So use proper names, whatever you mentioned in the inventory file. Get into insert mode first, and then paste it. Make sure no percent or anything else, extra space, nothing are copied. Escape colon WQ. Okay, also the login key permission should be 400. When we created it, its permission has changed. It is 664. Okay, you know what? Let's try to connect by using the same key permission. Let's see what error we get. We'll definitely get error. So how do we connect? Well the command is this, Ansible, and you mention the host name, web01, that is the same from the inventory file. And you say -m, -m is for module. Ansible has tons of modules, we'll go through them. The first basic module that we are seeing is ping. Ping is a module that really, actually it will use SSH. It's not the network ping, okay? It is going to do SSH. To web01. And the information -i inventory file. Take the information from this inventory file, okay? Let's hit enter and see what happens. Okay, it is asking us the question, do you want to continue? Yes or no? We discussed about this, right? So it's going to store the fingerprint. But this is a problem. We don't want our execution to be interactive, no questions asked, yes or no, this and that. Surely we can say yes and then it continues. But we add new machines, and every time it ask, yes, it makes it interactive. If you're running it from background, this is not going to work out. So I'm going to say No. And look at the error, it says, failed to connect to the host via SSH, host key verification failed. It's not able to verify that target machine. Anyways. So I'm going to use Ansible configuration to tell Ansible to not check the host keys, just accept the connection. Just say yes by default. How do we do that? Well, we need to create Ansible configuration file. There is a configuration file, sudo cat etc Ansible, ansible.cfg, this file, but really does not contain anything. We have to run this command to generate the Ansible configuration file. So very carefully, switch to root user, and go to this folder, etc Ansible. Do LS, you should find here Ansible configuration file. And this file contains this command. Okay, we're going to copy this command. Okay, before that, let's rename this Ansible configuration file, with, let's say, backup. We don't want to lose that file, okay? So now we'll run this command. Make sure you're in etc Ansible folder. Run this command. And it should create an Ansible configuration file. So Ansible configuration file we use to change its default nature, settings basically. We have a separate lecture on Ansible configuration, we'll talk about it in detail on this. For now, we just need to open this file, Ansible configuration. Again, make sure you're in etc Ansible folder. Open this file. And you have to here search for host_key_checking this setting, right? Made a mistake, yeah, checking. Hit enter. Okay, now in the beginning of this line, there is a semicolon. Go into insert mode and remove that semicolon. Semicolon means the line is commented. So hash or semicolon, these both are used as a comment here. So remove that and instead of true, change it to false. So host key checking, false. Please do not do host key checking, just accept the connection. That is all you need to do. Don't worry about other settings. We'll learn them. Remove semicolon, change it to false, save and quit. Okay now log out, and come back to Ubuntu user. Clear the screen, make sure you're in exercise one folder. And now, let's do once again the Ansible ping. Okay, another error, read. You should read the errors. Then you'll understand what is happening. It says permission 0664 for client key are too open. Okay, first of all, the red color actually is default for failure in Ansible. But you should check this message here. It says unreachable. Why? Well, it is telling you, it's failed to connect. That is because unprotected private key file. Its permission is 644 and it's too open. Okay, so we have to change it to 400 client key. Okay, its permission changes to 400, and let's do once again ping and, and yeah, there. So it's a success. Changed false, that means it did not change anything on that machine because anyways, it's SSH login and come back, it doesn't change anything. And there is a response to our ping, which is pong. Yeah, again, not so funny. I mean, every module will have a response back. We are going to see when we install packages and do other things. We'll see these responses. So we'll stop here in this lecture. In the next lecture, we will add all the other machines and we'll see more amazing features about inventory file. So make sure this results into a success. And after that, join me in the next lecture.
