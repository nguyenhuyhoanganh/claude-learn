# Decision Making

-: Hello and welcome. After this we'll have some series of exercises where we'll be provisioning server, and while we are doing that, we'll be also learning amazing features of Ansible playbooks. We are going to take example of NTP service. We'll set up this service on multiple operating systems sent to us and Ubuntu. We'll be setting some users and groups, and we'll be changing some configuration files on the server. While we are doing that, we'll be learning decision making in our playbook, we'll see loops, templates for configurations or dynamic configurations. We'll see handlers and then we'll see Ansible rules. Now while we are doing this we will be learning new features and also implementing all the features that we have learned so far. Now here we are not really focusing on the NTP service specifically but any service provisioning that you have to do any server provisioning you have to do, how would you do it in general? And then once you have everything in your playbooks then we'll see what are our roles and how to best use them. So, let's get started. Okay, I'm in my exercise folder and I'm going to copy exercise nine to exercise 10. And let's get into exercise 10. We will remove these two playbooks from here, we don't need it here. Let's make a test, Ansible, hyphen m, ping all. Okay, so all the instances are connected. Let's name the playbook as provisioning, dot yaml. Okay, apostrophe, hyphen. Hyphen, name. We'll call it Provisioning servers. Host, we are going to execute on all the hosts, we will need become as yes, and then the tasks. So tasks are pretty simple to start with, we have to install NTP agent on centos first, so the module we know yum. Name, the package name is chrony, and the state will be present. So that's the package name in latest center, center is nine. Now if we run this task, we run this playbook, then it is going to use the Yum module on all the instances all including the Ubuntu. In Ubuntu the module name is different, it's apt. So we have to create one more task for apt and there is a problem with that also, we'll solve that, that's what we are doing in this lecture. To say install NTP agent on Ubuntu, module apt. Name of the package is NTP here, status present. So the problem is this. This will get executed on all the hosts and this will be also getting executed on all the hosts. So this is going to fail. The first Yum task will fail for the zero three, which is Ubuntu, and this is going to fail for all the other instances except for Ubuntu. So we have to put a condition that if the operating system is sent to us, then execute the Yum task. If the OS is Ubuntu, then execute this task. So, in Bash and Python scripting we have seen we use if condition, here instead of if, you have of when. You have to give it in the same column as the module name, and you have to give a condition like we have seen the variable Ansible underscore distribution. We have to say if it's equal to, CentOS. Cent, C capital and OS capital. Same way, we have to give condition for Ubuntu, if the Ansible distribution is Ubuntu, U capital. Now this is the fact variable. We have seen this in previous lectures. And we'll look at the documentation also after we write two more task, which is to start the service. Okay, so start service on center. So center is service name is chronyd. State started and enabled, I'll say yes. Now here also the conditions will apply, right? And similar, we have to do it for the Ubuntu, so I'm going to copy this, all these lines. So start service on Ubuntu and here the service name is just NTP and we have to change the condition here, Ansible distribution, Ubuntu, U capital. So take a look at the entire playbook before you execute it. And we will save and quit. And first we'll do a dry run. Ansible hyphen playbook. The playbook name, hyphen capital C. Okay, I have an error obviously you'll also get this error. Thing is I have given single equal to whereas we need to give here a double equal to, to check the condition. So I'm going to change single equal to to a double equal to. I changed at the four places. Those four places. Let's execute the playbook or dry run. Okay, see you see it's saying skipping for web zero three because the conditions don't match there. Okay, and we caught one more problem while we are executing for web zero three, it is saying no package matching NTP. Actually the package is available, but thing is the Ubuntu machine, we need to run apt update before we install the package. So what we have to do is, we have to put here update underscore cache, colon yes. Now this is an option in the apt module, if you put this to yes, then it is going to run first apt update and then apt install. Let's save and code this and let's test it. Okay, still it is saying no package name because obviously it did not execute the apt update. So we are going to now remove the hyen C and execute it. So it is going to run now actually apt update, and then it'll find the package. Okay, and let's look at the output. So, only one changed, which is installing NTP which was not present in Ubuntu. In CentOS, chrony was already installed and chrony service was already started and enabled. In Ubuntu, the NTP service, any service, when you install in Ubuntu, it is going to automatically start and enable, so that said, okay. Now we have written all this from the scratch. We can get this entire thing from chat gpt, and also by looking at the documentation we can understand about the conditions. So first let's go to the documentation, let's look at the conditions, Ansible when condition Conditional, let's look at this documentation. Okay, let's come down. So the basic condition, right? That's what we have given. When some variable value is equal to so and so, then only execute the task. Otherwise skip it. So you will get in the status skipped. Right? This is based on the fact variable. This is a fact variable Ansible underscore selinux dot status, equal two enabled. This is a fact variable. Now, that was a simple condition, you can give much more complicated condition. Let's take a look at this one. And first thing here Ansible underscore facts in square backer distribution. So this will also, you can give because Ansible underscore facts is a dictionary, in that you have a key called distribution. Its value is equal to CentOS. And is the ending operation what we learn in B scripting also, with two amperes, like if this condition and if this condition, if both the conditions are true then this entire thing will become true. If any of the condition is false, this is going to be false. Or if both are false, then only it'll be, then also it'll be false, so both needs to be true, then only it'll be true. So it is saying CentOS is the distribution and the version is six. If that is true, or there is an OR operator now, and a similar condition here. So either this about condition or this condition, any of the condition is true, then it'll execute the task. And here the task is to simply shut down the operating system. And there's also one more cleaner way of giving the condition and this is an ending operation. So if this and this both are true, so it's in a list format you see that? YAML list format. So if both are true, then only this task will get executed. Right? Also we have a condition to give greater than or equal to. Right? So there are many more examples over here. Mostly the conditions will be simpler, but you, if you need much more complicated, you can get it from the documentation, also. Right? Now let's look at the chat gpt, also. We could have generated the entire thing through chat gpt as well. Let's take a look on that also. Okay, so I'm going to say Ansible playbook to install chrony on centos and NTP on Ubuntu. Also start and enable chrony and NTP service. Let's check. Okay, so here you see the same playbook that we have written is giving to us. So if you write the proper text, right? That means you know what you have to do, then you get very close code, very close playbook here or else you can keep chatting and get the exact playbook that you want. You can copy the code and then you can make changes like you need to make changes here. I don't think here we need to make any change, is all good. Okay? So now instead of spending time too much on chat gpt, I recommend you better check the documentation, read the documentation, look at the condition different ways of giving condition. Those are very common use cases that we will need as a DevOps engineer. So take a look at that and join me in the next lecture where we are going to see loops.
