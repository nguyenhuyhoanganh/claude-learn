# Fact Variables

Instructor: Hello and welcome to Fact Variables section. In this video we will understand how the setup model works and how it generates facts variables and what will be their use cases. As I discussed previously, fact variables are runtime variable. They get generated when setup model gets executed. Like you can have the operating system name, you can have processor cores, kernel versions, Ansible devices, the connected devices, IP address, MAC address, architecture. So these are some examples of fact variables. Now we will run set up model through ad hoc command and we'll see all the fact variables. So let's get into control machine now. Okay, let's get into vprofile, and let's get into exercise nine. And we will execute this playbook first, vars_precedence. Now whenever any playbook executes by default, Ansible will execute this task, gathering facts for all the hosts. And our lecture is based on this one only, gathering facts execute a module called setup. This module collects information about the host in JSON format. And it's only in the runtime. It doesn't really use it unless we want to use those variables from setup module. If you want, we can disable it also, mostly we don't use it. So if we are not using it, you can disable it. You can come here and you can say, just before task, you can say gather_facts: false. Now when you run the playbook, you see, the gathering facts task is not here now. Now we can execute this task through ad hoc also, ansible-m setup. Now, let me clear the screen first and then run it. Ansible-m setup, on, let's say, web zero one. See all that information? This is in JSON format. Let's scroll up. So the whole output is basically a dictionary, a giant dictionary in that you have keys like ansible facts. And its value is another dictionary, and it also has so many keys in it. And some key value is a list, you see the square bracket. And some key values are another dictionary, see those curly braces, key and value. So there are many variables. Ansible_architecture. Now here the word ansible does not mean the ansible control machine. This is all information about web zero one. You can see BIOS date. There are many things. Ansible date and time, you can use it to take backup of files, right. You have complete dictionary for that. IP address, Ansible distribution, the OS name. So let's print this variables. For this, we'll create another playbook. We'll call it as print_facts, this will be fun, .vaml. Three hyphen in the beginning, hyphen name, we'll just say print facts, that's the name of the play. Host, we'll say all the hosts, and we'll mention the task. Tasks. I'll say, first task is to print OS name. Use the module debug. Variable, ansible_distribution. You can check the spelling of the variable by running the setup module. Check once again. Let's execute it. Ansible-playbook, print facts. So gathering facts and then printing it. What happens if we disable gathering facts, let's see that. We'll say gather_facts: false. So now it is going to try to print a variable that does not exist, because gathering facts is not getting executed. And you see the error variable is not defined. So we'll go back and we will change it. We'll remove that gathering facts, or you know what, just keep it here and put a hash in front of it. Now it's time to add one more OS in the mix. We have all sent to us. We will create one more EC2 instance with Ubuntu. We'll name it as web zero three. So log into your EC2 console, launch instance. We'll name it as we vprofile-web03. Ubuntu will keep Ubuntu 22 there. Scroll down, instance Type three two micro is fine. Key, client key, same key. Select existing security group. We have the client security group. And simply launch instance. Okay, by the time this is coming up, let's copy its private IP, and let's update our inventory file. We will copy these two lines to yy, and we will paste it here, change it to web zero three and the IP address. And we will also add this in the group web zero three, colon. Let's save this and let's execute first the ping model. Ansible-m ping web zero three, or you know what, we'll do it for all. You can see what happens. Failed to connect to host, permission denied. Look at the user that it is using. This web zero three is from Ubuntu AMI, the user is Ubuntu. But we have defined global variables, right? But remember what we learned. Host variable takes higher priority. So such cases we should have their own separate variables. The host variables will take higher priority. So it'll check first whether the host has those values or not. If it's not, then only it'll go for global. So now let's try it. Now you see web zero three success. Okay, let's come back to our print facts playbook. Now let's print the ansible distribution name. And you see for web zero three, ansible distribution value is Unbuntu. Let's execute the setup module once again manually. We'll take it for web zero one or anything. So there are many variables over here. Let's use a few other. Let's look at some un-nested dictionary. Ansible memory mb, and its value is a dictionary. So we have to say dot, and we'll say real, that's the ram memory, dot, and we can say free. So let me copy this. Let's open our playbook. Create another task. Print RAM memory, or just ram, you can say. And the value .real.free. Let's execute this and see. Okay, there, so we got the string, right, free was the string, but it was inside the dictionary real. And that dictionary was inside the dictionary ansible memory mb. Let's execute setup once again. Let's find some list. Okay, this one, ansible_processor. It's value is a list. So we have three strings at zeroth position, first position and second position. So it'll print the name of the processor, ansible_processor. Let's open our playbook. Let's add one more task to debug. We'll say print processor name. And that is the variable name. And its value is a list, so to access the element in the list, we have to say square bracket, and I'm going to say two. And let's save this. And let's print it. There, so in this whole lecture, I'm showing you how to access the variables, the fact variables. You can use this in your playbook for various other purpose, like condition to check whether RAM is free or not, and then decide the task should get executed or not. You can use it to name files, push the content into the file. We'll be seeing decision making in the next lecture, and you'll understand how you can use it for your own benefits. So try to print different variables, values. Try to access them, print them. This is a very good exercise. And then join me in the next lecture. See you there.
