# Variables & Debug

Instructor: Welcome to Ansible Variables section. Now, variables are as same as you see in other programming languages in other tools that we have seen previously. Like in Bash, in Python, even in Jenkins. Ansible, also, you can define your own variables. There are existing variables in Ansible that you can use or you can generate variables in the run time. It's as similar as any other programming language or tools. First, we'll be seeing how to define variable in the playbook, which is pretty simple. You just say vars colon and you do variable colon and its value, right? So here you see two variables, HTTP underscore port and sqluser. And this comes just below the task or you can see before task, just below the host configuration, in the global play declaration, you can define variables. We have also seen variables in the inventory file, but that is not where we define the variables, our own custom variables. We have inventory-based variables. That means if you want to define some variable for all the host, we can create a file group vars, underscore vars slash all. Okay? At this part you create a file, file named "all", you define variables. Those variables can be used by all the hosts from our inventory file. Again, this will be our own custom variables. Here we are not talking about the Ansible user, the login keynote, not those. The variables that you want to use in the playbook, like HTTP port, user, a skill user, passwords, and we have better way of managing password. Okay? So the all file will be for all the host, if you want for a particular group, let's say for website, or I want to define some variables, I can give group vars slash group name. The group name that is defining the inventory file. For host specific, host underscore vars slash hostname. So, if I have a host name "web zero one" and I want to give host specific variable, I will define it in "web zero one" file, which is in host vars directory. So, this is defining variable outside of your playbook. We'll see this. Then you have roles in Ansible. We'll be seeing that later in the roles. Also, you have two files where you can define variables, vars and defaults. I don't want you to memorize all this right now. We'll be doing this. My point is there are different places where you can define variables in Ansible. Now, those were our own custom variables. We are defining them. Ansible has also its own variables. The majority of its variable gets generated from the setup module. Now, you don't need to run setup module. When you execute the playbook, the first task that gets executed, gathering facts, that task will run the setup module and generate the fact variables. Okay? Some example of fact variables. When it is running, let's say, on web zero one, you'll have Ansible underscore os underscore family variable and its value will be like operating system, operating system family like Red Hat, Debian. You have processor cores, Ansible underscore processor underscore cores. It'll tell you number of CPU cores. And let's say it's running for web zero one. So, it'll tell you CPU cores of web zero one. Kernel version, Ansible devices, the connected device information. Ansible default ipv4. It'll give you the Mac address Gateway IP. Ansible architecture, whether it's 64 bit, 32 bit. Like that, there'll be many, many, many fact variables that will get generated through setup module. And we can use these variables to put its values in, let's say, configuration files or make decisions based on host information like 64 bit, install this package, 32 bit, install the other package. We can use it with conditions, decision making in our playbook. We'll be seeing that. Okay, so these are called "fact variables". This gets generated automatically when you execute the playbook. The other way is storing the output of a task. We are executing a task or any module that we run returns output in JSON format. We can store that output into a variable and then we can use it, right? So, you execute a task, which will have a module. It returns the output in JSON format and by using a register option, we can store its output into a variable. So, this is runtime variable. We would have stored the output into a variable for a reason. Maybe the next task we are executing needs some decision making or some output of the previous task. So, then, we can use that stored variable value into the next task or maybe just print it. Okay? So talking again, once again, you can define variable in the playbook, your own custom variable. You can define it in group vars, host vars. You can define it in the roles. Then there are variable that gets generated through setup module, gathering facts task. These are runtime variables. Another runtime variables. You store the output of a task into a variable and then you can use it or just print it in the playbook. Now let's see all this one by one. Okay, so let's get started. We will copy exercise seven to exercise eight. We'll get into exercise eight. Okay, so as we are talking about variables we'll start with very simple ones. So let's open the playbook. We'll define some variables in the playbook and we'll use in the playbook. You see I'm stressing on in because it's really not a good way. We are going to see how to define variables outside of the playbook also, which is a good practice. But first for learning purpose, let's define the variables. So before tasks, I'm going to say "vars colon" and still a dictionary format. So, what do you want to define? Let's say starting with the database name we'll say "DB name". DB name, and we'll say, I will say, "electric", just different name. And what else do we have in the playbook, right? So, we have the username, we have the password, let's use that also. So, first, we are defining a variable. Then we'll use them. Current DB user and DB pass will say "tesla", not the Tesla, Tesla company the Tesla inventor of alternate current. Okay, so this is defining variable like that, you can define as many variables you want. How to use it. Well, here, we want to, instead of giving this DB name directly, we are going to give the variable over here. And this is the format. You have to give double codes in that two calibres opening, two Calibres crew closing. And inside that, you have to give the variable name. I know. A little complicated. Not complicated. Little extra characters. In Bash, we have just dollar and the variable name. Here, we have like this. Okay, let's go for the other one. Let's go with this one. Username. DB user and here, DB password. Okay, so those three things I'm using. And how do and when do we use variable, right? Things that come again and again in our scripts, in our code, or the things that might be of reusability, sorry, reusable property. Like, I use this playbook in this project, I use it in another project, things will change, right? So the properties, those changes, those things we use as variable, right? So, same way, any other programming language here, also. So I'll just say, "create database" and here also, create a new database. Okay? Now let's save and quote this and let's test it. Ansible hyphen playbook, db dot yaml. Okay, you see changed, change- We really don't know what changed. Actually, we know, right? But the point is it's not printing all that information, right? Whether it has used those variables or not. But we can use VV and we can see whether it is printing or not, using that variable or not. All right, we see here, DB electric, right? User current. All right? But you can also print from the playbook. Now, if you want, you can have a task which can print variables for you and that you can do by using a module called "debug". You can see hyphen, debug, and you can just see here, "var colon" and just cue the variable name. Or this one maybe. It's a two y y. We paste it. You can also use msg and in double quotes you can give whatever message you want. But now when you're using message, and you're giving everything double code, then you need to put this variable like this, in proper format. And you can put your message. I can see "The DB name is" so and so. Right? Here, we'll print dbuser. So, debug model is used for printing purpose, right? Like we have echo in Bash and print in Python, like that. Here we have debug. You can print a message or you can just print the variable name. For that, you just need to give the name of the variable, don't need to put in curly braces. Now let's save and quit and test it. Oh, I don't want to do hyphen V V, let me cancel this. (notification sounds) Okay? Like that. Just execute in the playbook and see? See that debug module? The name is "electric" and here, just the variable value. Okay? Now since we are using a debug module, let me show you one more thing very quickly. It's pretty simple, very useful, very powerful, when you use it carefully. So, the module executes and it returns output. That output is by default suppressed, right? We see a different kind of output when we execute the playbook. The output is basically in JSON format. So, where does it go, right? Can we use it? Sure we can. This is how we can use it. So, what you have to do is just, in the same column of the module name, you have to say register colon and, sorry, not here. Yeah. Register colon space. And you give a name of a variable, anything. Like I will say, "dbout", like output of this DB module. And so it is going to store all that JSON output into this variable and you can use debug module to print that, right? Hyphen name, print, DB out variable and the module name "debug". Yes, you can do it both way, right? You can give a name to a module, sorry, name to a task and then give the module name, or you can give just hyphen and module name. Like, instead of giving hyphen debug I have given hyphen name and then the module name down here. And I'll simply say here, var colon, and that is the variable. Okay? Once again, take a look. Register is in the same column as the module and you give the variable limit. It'll store all that output into this variable and then with debug, you can simply print it. Oops. Yeah. Okay, so let me clear the screen and let's run the playbook. Okay, see that last task print "DB out variable". And that's the all- Now that was all the output which you got from here because this task was already done. It just printed. Okay, right? So, nothing is changed. And so you can use debug module to print. You can print variables, you can print messages. And mostly it is used for troubleshooting purpose only. That's why it's called a "debug". You really don't need to print messages from the playbook. Playbook anyways is very verbose. It gives you correct messages. If you write your playbook, if you name your task properly, you get proper messages. You don't need to use debug module for that. Okay, so quickly reiterate what we did here. Vas, we defined three variables. We are using debug module to print two of those variable, two different method of printing. And then over here, we are using those variables. This is the syntax of using the variable, double quotes, two curly braces. And also, we have seen how to register output of a module of a task and then you can print it by using debug module. Okay? That is all in this lecture. In the next lecture, we are going to see how to define variable outside of the playbook. So, let me quit from here and I'll see you in the next lecture.
