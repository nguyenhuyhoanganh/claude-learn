# Ansible for AWS

Instructor: Hello and welcome. In this lecture we are going to see how we can use Ansible to manage our AWS services. So it will be basically same thing what we have been doing. We find a module and we use it like we want to create a key pair. We can find a module. We want to launch ec2 instances. We can find module, but this is a different area. We need to do some prerequisites like when we want to create key pair or launch ec2 instance how does Ansible know what is our AWS account and how to log into it? Authentication, authorization. So authentication. So we have to export these two variables which will have the access key ID and secret access key. So when we create an I Am user in AWS, we have an option to create access keys. We create and we export these two variables with their values of access key and secret key. And then our Ansible can connect to our AWS account. And then it shows a sample here, host, local host. The task that we'll be running will be running locally on the control machine which will make API calls to our AWS account. And again, how does it know our AWS account? Through the access key and secret key. Gather facts, false because we don't want to collect information off the control machine itself and task, we can have list of tasks like adding key pair, launching instance, adding load balancer, adding instance under load balancer and all those things. We have modules, huge list of modules on AWS. But first we need to export the access key and secret key. So we'll create an I AM user first. So to create the I AM user I'm going to go to the I AM service, users, add user. I'm going to call this user Ansible admin and go next. Attach policies directly and I'm going to give it administrator access which is very dangerous. You have to be very careful with the access keys of this user. You should not expose it or keep it in git repositories. Create user. Let's click on the user. Let's go to security credentials. Access keys, click on create access key, CLI and say I understand and go next, create access key. Okay, so download the CSV file, save and let's click on done. Okay, so I need to export these two variables. Now obviously I'm going to put the values over here. Access key ID value, I'm going to get from the CSV file and put it. And same for the secret access key. So these are all fake values. I'm going to put the real values over here and run this command once again. But that I will do after I pause my screen and then I'm going to do it. But I'm not going to run this export command on the shell because what happens is when you exit, these variables will be destroyed. Will be gone. Then you need to again run the export command. So better I am going to put them into my Bash RC file at the end. I'm going to take this and I'm going to put the right value and then I'm going to run the source command. I'm going to say source Bash RC or you can just log out and log in. Whenever you log in, Bash RC file will execute and it is going to export your variables. So let me pause my screen, do this and then we'll continue. Okay, I paused the screen. I just did this. I just opened the Bash RC file again I put the right value from CSV file. I did source Bash RC and authentication is done. Now we will start with a very very basic playbook for AWS. We'll create a key pair first. So I will create a directory here, AWS I'll go into that directory AWS oops CD and I will create the Ansible playbook. I will call it as, or I will just say test hyphen AWS dot YML. Okay, hyphen host. And we are going to do it on local host obviously and gather underscore facts, false. And then tasks. So as I told you, first tasks will be to create a key pair. So I'm going to name the task name. I would say create key pair and let's find out the module name to create the key pair. Okay, Ansible module index. And let's go for cloud modules and Amazon. Okay, so you need to scroll down. There's ec2 key create or delete an ec2 key pair. And we will directly go to the example and that's the example to create a key pair. Let's just copy that and just put that in our playbook. I already have given the name I'll just copy the module name and the name of the key. Okay I will link the key as for this one as sample. And let's just save and run this and let's see what happens. Ansible hyphen playbook test AWS dot yaml. Okay, so I want you to look at this error. Now Ansible is written in Python and Python needs some libraries to access different resources like AWS for that. It has a module called as Boto or Boto three, which Ansible is not able to find and we need to install this module. As the Python module and we are going to use the command pip to install this module. And so the command will be basically a pip install boto three, but it's not going to work because we do not have pip. So first we have to install pip. So we have to say sudo apt install Python three hyphen pip hyphen Y. So install PIP first. Okay? And then we say pip tab two times, tab, tab, you see three dot 10 and then install bottle three. And that has installed bottle three. Let's run the playbook once again. Okay, so that error is gone. Now we have another error. It says a region. You need to specify the region. Now there are two ways to specify region in every task you can specify the region. That is we just give here region and we'll give the region code, which is US east one for us. Or you can set a variable, AWS underscore region in caps just like I have set access key and secret key, same way you can set that variable. Okay? So in the module documentation you should find that variable there. AWS underscore region, we can set this variable so you don't need to set for each and every task that also you can do. But let's go with this for now. Let's save and quit and now let's create it. Okay, that worked. But the problem here is it did not return the private key. When we create a key, it's called a key pair, public key and private key. Public key will be there in our AWS account if we go so that's the public key in our AWS account, but we do not have the private key. That's because we did not save the output of this task. The output will be returned in adjacent format, but we need to store it by using a register and we'll say key out, that's the variable. And let's also print this variable name print key and we'll say debug and MSG or I'll just say var here, var, oh I'm giving a hyphen there that is wrong, var and we'll just give the variable key out. But this key is already created so it's not going to return the private key again it just returns only once. So first we need to delete the key we'll delete the sample key. Now if we run the playbook, it should return the private key there. Okay? So that's the private key and now we have to copy it and paste it into a file. But we can do that in the playbook itself. We can create a task to save this content into a file. Only thing is we have to put a condition there. If you see when you create the key, it is going to return this variable private underscore key. But if the key already exists, that variable doesn't doesn't exist, right? Private underscore key. So to access that key, we have to say key out dot key dot private underscore key. Okay? So let's open the playbook once again. And this time, like we are printing, we are going to use a module, copy module to save the content of that file. So we'll say save key, the task name will use the copy module and copy module has an option called as content. We can mention the content in double quotes, but this time the content will be a variable which is key out dot key. Dot private underscore key. And we are going to put a destination here, store it into a file in the current working directory with the name sample dot pem. But you have to put in a condition here, when. The condition will be, when key out dot changed is equal to true, that means if this creates the file if this creates the key, then the changed value will be true, otherwise it will be false. So if it's value changed, value is true then only you need to save the file. Otherwise just skip it. And you don't even need to say double equal to true because it's value will be anyways true or false. So if it's value is true, then it is going to save the file. If it's value is false, it will just skip this task. So let's run this. Okay, you see that doesn't exist private key. So it just simply skipped, but now let's delete that file again. So let's delete the key deleted and now when we run the playbook it will create the private key, return the output, and that we will store. Okay? So it says change there and you should see sample dot pem and if you check its content, that's the private key, okay? We also need to remove this debug options from here. You don't want to print your private key or I will just comment it. I'll just keep it here. I'll just put hash in front of it. Now we are going to add one more task and that will be to launch the ec2 instance. Let's search Ansible AWS modules. Let's look at this first link Amazon dot AWS modules from Amazon. Okay, let's search for ec2. Okay, here itself we have ec2. So create and manage ec2 instances ec2 underscore instance module. Let's click on that. So we have to run this command, which is going to install all the Amazon AWS modules in our control machine. So let's copy this command and let's hit enter. Okay, it's installed. Let's look at the example. Okay, start an instance with a public IP address. Let's copy this whole, let's open our playbook. I'm going to say shift insert to paste here and I'm going to fix everything first all the indentation, okay? And I'm going to change the names. Just start an instance with a public IP address. Well our ec2 instance anyways gets the public IP address so that's not a problem. Okay, let's go one by one, key name. So we have the key name as sample that we created. So either we can give directly here sample or we can give the variable key out dot key dot name that variable here VPC subnet ID. So we will not use this we will let it launch in its default subnet and VPC. So we just put a hash and keep there. Instance type here that you should change to t2 micro don't want such huge instance. Security group, we will keep default network assigned public IP. Now we really don't need to specifically mention that our instance anyways gets the public IP. So we'll put a hash there and we'll just keep it there itself. Image ID will get the image ID. Let's go to our ec2 console, okay? Just make sure you are in the right region. Whatever was mentioned in the playbook mine is now US hyphen West hyphen two and image ID is different for different regions. So let's click on launch instance here and let's find an image ID. Let's come down, we'll keep it default. Amazon Linux 23 and that's the AMI ID. So you can find which AMI you want and find its AMI ID make sure the region is correct. So I'm using now US West two. Okay, let me paste the image ID there. And I also need to mention the region here. Let's mention the region, region, US Hyphen West, hyphen two. And I made a mistake here, the image ID. Okay, that's right. So what else we need? We need actually one very important thing. We need this instance just once exact count there is an option, exact underscore count, colon one. Okay? So that one exact count. And integer value which indicates how many instance that match the filters parameters should be running. Now if we don't mention this and you run the playbook multiple times so that many times the instance will be launched, you'll have multiple instances. So make sure you mention this exact count in your playbook. Okay, I think this is fine. Let's save this and let's test it. Ansible hyphen playbook test AWS yaml, okay it's taking some time and. and it's already there. Done changed. So it returned the output very quickly. There is an option we can use actually wait yes till the instance state be- running state, not this one there wait, wait or not to wait for the desired state. So we can mention state as running and then we can mention wait as true. So then it will wait until the instance state comes to running. Otherwise it'll just return output like this. I mean this also works, but doesn't wait until the instance in a- is in a running state. So let's take a look. You see that the instance is launched and it is in running state, okay? You see how simple is that? Once you get hold of it, you can find your module to use, you can mention it in the playbook, make sure you mention the right parameters. You may face some error generally because of regions AMI ID, subnet ID's. So make sure those information are correct wherever you use any Amazon module, right? So there are many, many modules to try from, but I think this will be more than enough. We have much more better cloud management tool which is Terraform that we will be seeing. So once you're done with this, delete, terminate the instance, remove the key. So that is all in this section. I hope you have learned a lot and you have gathered the right skills, finding using the right module when you fail, look at the error, troubleshoot it and most importantly, use Ansible documentation. That was really a good one. I'll see you in the next section.
