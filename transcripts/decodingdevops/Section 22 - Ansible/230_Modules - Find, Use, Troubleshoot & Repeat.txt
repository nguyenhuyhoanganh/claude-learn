# Modules - Find, Use, Troubleshoot & Repeat

-: Okay, in this lecture we are going to go through some more modules. We will understand how to find a module, how to use it, and if we get into problem, how do we solve it? This will be your very regular work as a DevOps engineer. You need to write playbook. There'll be different kinds of tasks. You need to find modules for your task, use it. If it fails, you need to troubleshoot. First, let's take a look at the module index. You can search "ansible module index". Let's open this link So, you can click on 'All modules' and find all the modules, the list that we have seen in the previous lecture. But then, there is this segregation, right? You have cloud modules, you have database modules, you have files modules. So first, we will assign ourself a task of copying a file from control machine to web servers. And then they will assign one more task on database to add a database and create database users. Let's start first with files modules. So, let's look at the files modules. See there are many different modules in the file section. You have copy. Copy files to remote location. That's what we're looking for. Then there is file module, manage files and file properties. If you want to change later the permission of the file, create a directory. You have archive module, block in file. These modules are used a lot by DevOps. Being a DevOps, our job will be majorly managing files. Their configuration of files could be an archive it could be an artifact, a text file, configuration files, scripts. We deal with files a lot. And these files modules will help you execute those tasks. So, I recommend you take some time and go through all these modules one by one. There are not many. In this lecture, we will be just using the copy module. In later lectures, we'll be using some more modules from this. So, let's look at copy module. We have already seen this in previous lectures. We can directly go to the example. That's what we will be doing. Copy file with owner and permission. We will copy this. We'll copy the copy module, like that. Okay, let me go to vprofile, and here we have exercise 5. Let's copy that. Exercise 5 to exercise 6. Let's get into exercise 6. We will open the playbook. Now, 'web-db'. You know what? I'm going to rename this, 'mv web-db' to just web. We'll not have the 'db' play here just 'web.yaml'. And we will remove the 'db' task, or 'db' play. And we are going to, all right, yeah. We are going to paste the content that we copied from the copy module. And when you copy paste, this is what happens, right? It goes here and there. So, make sure you first thing you do is you correct the structure of yaml. Okay? So, module there and... Okay. Now, let's change the content here. So copy, I'll say, 'index file'. 'index file' And 'src', source. So, we'll be creating a folder called 'files'. And in that we'll place the file index.html. And destination, we will say, 'var/www/html/index.html' also mention the complete path. Also mention the file name, right? Owner will keep it as 'root'. Now, we really don't need the owner and the group. We don't want this. We just wanted to copy the file. That's all. So, you can remove these options. These are not mandatory options or arguments. So, a module may have mandatory option, one or two, and most of the options will be optionals. Or arguments will be optional. Let's look at that. So, looking at the parameters or arguments, you see destination, this is required. That's a mandatory option, okay? If you look at the source, it's not mandatory because there are other ways also. Instead of mentioning the file path to copy, you can also mention the content. You can say content, and you can literally write the text content there and it's going to dump that text content on the destination file, which is required. The few interesting options like backup. You can take backup of the existing file. Now, we are going to push this file and if there in the destination, the same file exists. It is going to take the backup of that file first, and then push your file. We'll use that and we'll see how it works. So we'll say here, 'backup: yes'. Okay? But this file does not exist in the files folder. There's no such folder. So, let's see. Let's create a folder with name, 'files'. And in that, we'll create the file with the name, 'index.html'. Or you could have given other names also. Whatever name or path you give in the playbook, the file path with the same name should exist. We'll write here as you know, "learning modules in ansible". Or whatever you want to write. Just write it. And now, let's test this. Ansible, not Amsible. "ansible-playbook-i inventory", and the playbook path. Let's run it. Okay, so it made some changes, right? Now, I will log into one of the machine, so it pushed at web zero one and web zero two. I'm going to log in at one of the machine and see the backup file. So, the user is 'ec2-user@' the rate, and state the IP. Okay, I'm in the web01. Let's do 'cd/var/www/html'. And let's do 'ls'. And you see the file, 'index.html'? That's the file we pushed. Look at the content and there is the backup file. Okay? And you see it has mentioned the timestamp when the backup was taken. So, 'index.html.19', something, something. So, this was our previous index.html file. So you see, you look at the options, or parameters, and you can find interesting options to use which may enhance your playbook. And look at the examples, of course. Now, this was pretty easy. Let's log out from here. Log out. Let's come back to the control machine. And you know what? I will change the host name of this control machine, just because if I log in, you should see which machine I am in. Control. And hostname control, not mandatory, just okay. So, it says you must be the root user. So, I'm going to say, 'sudo'. Okay, and let me log out and log in again. Okay. So, now you see I'm in the control machine. This is just to show you so you can see where I am logged in because I'm going to log in now to db machines also. Okay, so that was pretty simple, right? We found the module, we used it. No problem, no issues. Because copy is a very simple module. It uses SCP command in the Linux. But there are some modules, or many modules that needs some dependencies. There are some requirements that needs to be fulfilled before you use those modules. And we'll see that particular example now. Now, you have to copy exercise 5 to exercise 7, because we need the db playbook here. Exercise 7. Let's rename this to just the 'db.yaml'. Let's open 'db.yaml'. Okay, so the task is this. This is the database playbook. It installs the database service. Let me remove this play, web play here. Okay. So, this installs mariadb server, it starts the service. I want to do now more. I want to create a database named accounts. And I want to create a user named vprofile in the database in the mysql or mariadb database. So, that's a task assigned to us. The first thing is we know what we have to do. We have to find the module. So, let's go and check the modules for database. Okay so, do we have a database module section. If not, we can simply Google also, "database modules". And if we come down, we have here Mysql. 'Mysql_db'. Now, let's take a look. And we'll directly go to the example section, generally what we do. So, create a new database with name 'bobdata' and that's what you wanted to do. You wanted to create a new database. Let's copy this. Paste it. Yeah. Let's fix the indentation structure 'accounts'. Okay, this is it, right? Plain and simple. Create a new database with the name account name of the database, state, present. Let's save and quit, and let's execute this "ansible-playbook-i inventory and db.yaml". Okay, that failed. Now, remember what I told you, some modules are, there are many modules that needs dependencies. And these dependencies are mostly the Python dependencies. Now, what exactly is happening here? We know Ansible creates Python scripts. It will dump in the destination, which is db01 now, at the remote location and execute those Python scripts. Those Python scripts will need some Python dependency for certain module. For example, a MySQL module is required for Python 2.7 either by MySQL or MySQL-Python. That's the Python dependency or Python module that is required to connect to the database. Now, if you'd have read the documentation properly, in the requirements itself, it is given. The below requirements are needed on the host that executes this module. So, which host is executing this? Db01. The scripts are running on db01. Some Python MySQL libraries. Well you know, most of the Python MySQL libraries can be installed through package manager like yaml apt. And there's also PIP that you can use. We will 'ssh' to db01. And find out the exact package name. Something key to the inventory file db01 'ssh -i clientkey'. 'ec2-user@' The rate, IP. Okay, I'm in db01. This is CentOS machine. And to find the package, we can use yum search, and I'll say, "python". And it will give you a huge list of Python dependencies packages. From that, I'm going to filter 'grep -i mysql'. So if you're lucky, you can find the Python dependency as a package from yaml apt. Otherwise you can use PIP also. So, here we got it. 'Python 3, PyMySQL', this one. Let's copy the name. Now, we can just run yum install by MySQL here at this package name and it will install. But, that will be manual. We need to do everything from the playbook. So once you find out the dependency, log out from the DB machine and open your playbook. And there is a yum package here. Yum task. Let's copy that. I'll say, '4yy'. And then paste it here. I will say, 'install pymysql'. And then, here the package name, the exact package name that we copied. Remove that full stop. And let's test it now. So, it is going to install the dependency first on db01, and then it is going to use that DB module. Let's see if it works. Okay, now we get another error. And a solution to this is simple, actually. But, let's do a Google search and let's see what we get. It's basically a problem with the socket file. I'll tell you in a moment. Let's copy this. Okay, throw the error over there. I should really omit this numbers and all. Anyways. Okay so, something from Stack overflow, "Access denied for user MySQL." Okay, not this one. Let's put "ansible" here. Okay, "Ansible unable to find", blah blah, blah. Problem is basically Ansible is not able to connect to MySQL server be because it does not know how to connect. That's the point. Or should I see the Python scripts that are running, right? They don't know how to connect to it. So, here it is asking us to mention the socket file location. Socket file in Linux is used to connect processes together. If one process wants to connect to other process, uses the socket file. But this is a wrong location of the socket file. In CentOS, the location is different. I think it will be here somewhere mentioned. Let me see. 'Var/lib'. Yes, 'var/lib/mysql is the MySQL or MariaDB default home directory. So, we need to use this one, this path. So, let's copy that option and that path. Let's open the playbook. And there, we need to mention this option. Okay. Let's save and quit. And let's test it. Okay, that work changed. So, you see we got few issues. One was with the dependency and one was with the MySQL server. So, one we found out by ourselves. The other one we found out from the Stack Overflow. You know, I did this purposefully, actually. Now, we have community MySQL module also where all this is already mentioned. So, you just search for "mysql ansible modules" or like that for any service you'll see "Community.Mysql". This is developed by the community, not by the Ansible. I mean, not the Ansible vendor, but the Ansible community. So, ansible_dbmodule. And as usual, we have the requirements. But, we failed first and then we look at the requirement. And in the example, if you see, you have there, right? Also, when you get in the errors, right, you can see, you know, in the notes, commonly found errors. You see Python, MySQL dependencies explicitly mentioning the socket file. All those things are mentioned. But I wanted you to fail on this one and then fix it. But now you know, if you read properly, you won't even get into those problems. But anyways, you will get into problem. Don't worry, it will happen. There are many, many modules and we can find how solutions to fix those issues, right? So now, we will be using this Community MySQL module. I'm going to replace that module with the community MySQL module. So it says, first you need to run this command to install community MySQL module. So, I'm going to copy this command "ansible galaxy collection install community.mysql". Let's run that command. Right? And let's look at the name of the model exactly. 'community.mysql'. So, I open the playbook and I'm going to simply add in the beginning here, 'community.mysql'. I think, yeah. That's the right spelling. 'community.mysql.mysql_db' Okay, let's save and quit. Okay, if we have one more task to do. We also need to add the user. So, let's look at that module. 'Mysql_user module'. Add or remove a user from MySQL database. And for this also, you need to install this dependencies. Not dependencies, these modules actually, in the Ansible control machine. You already did that. In the example, over here, create database. Let's copy this. Let's open DB playbook. Let's paste it there. Let's fix the indentation and everything. Okay, there. I kept it simple. Create database with username 'vprofile' and that's the name of the database and some password, which is very childish. Anyways, I'll keep that. Or, you know what? Let's change it. Let's change it to single code, 'admin943', something. Okay, let's save and quit. And this time we are going to execute the community MySQL module. Okay, again, same error. Access denied for that user route because there's no socket file mentioned. Let's copy that socket file. You see you solve once, and then you can use it as many as times. Okay? So the same option, 'login_unix_socket' and the path of the socket file. And then, let's run the playbook. All right. That is done. Now, that was quite long and intense lecture. We just learned two modules, but from that are three including copy. But with that, we have learned skills of finding the module, using it, troubleshooting it, fixing it, and reusing it. Okay, from here on the journey for you will be easy to write any playbook. But there are many more things with playbook and out of playbook in the Ansible that we are going to see in the coming lectures. So, wrap this up and join me in the next lecture.
