# Setup Ansible & Infra

In this session we are going to set up ansible infrastructure infra as in it's pretty simple. We will have one EC2 instance where we are going to install Ansible and three other EC2 instances with CentOS operating system. Among them two will be web server, one DB server, and we are going to set up everything on web and db by using Ansible. Later, we will be also including ubuntu EC2 instance as a web server. So first let's start launching EC2 instance for Ansible, which in general terminology is called as control machine. So let's launch control machine launch instance. We will call it just control. Ubuntu Ami ubuntu 22 is fine instance type. We will keep it as T2 micro key pair. We will create a key pair, we'll name it as control and make sure it's PEM file. Create key pair. Edit network settings will create security group for it. Pretty simple. We'll just say 22 allowed from my IP. That's all control SG. 22 allowed from my IP. And let's launch instance. By the time this instance comes up we will launch three other EC2 instance, two for web server, one for DB. And we'll launch all at once, all three at once. Let's click on Launch instance. Let's name it as V profile hyphen web zero zero. And I'm going to increase the count here to three. Later we will change the name. All the three EC2 instance will have same name tag. We will change that OS. We will take CentOS. Browse more Amis AWS marketplace. Let's search for CentOS nine. Select this one CentOS stream nine. Buy Amazon Web Services. Click on select this Ami will be free as long as we use it with T2 micro T3 Micro Instance type. So click on subscribe now. The username here you see is EC2 hyphen user login user instance type T2 micro. Make sure you change it to T2 micro. We don't need a bigger instance for this one. T2 micro is fine. We'll create a key and we'll simply call this key as client key dot PEM create key pair. We'll create another security group for this one. We'll name it as client hyphen SG. Same in description. And we need to give here this rule 22 from my IP. That is so we can SSH. But also Ansible is going to connect to these machines by SSH. So it will do the same thing what we are doing. So we also need to allow port 22 from Control Security Group. Be careful in this one. Otherwise Ansible will not be able to SSH or connect to these machines. 22 allowed from Control Security Group. That's all we can launch these instances. Okay so name are all same. So let's change it first one to web zero one second will change it to web zero two. And the third one will name it as DB zero one. Okay, let's log into the control machine and install Ansible on it. Always remember, control means where we have Ansible installed. We don't need to install Ansible in these machines. Ansible will be using SSH and these EC2 instances has already SSH. So we don't need to do any special settings on the client machines. We can say. So let's just copy the public IP of control. Let's open git, bash or terminal for Mac user ssh hyphen I downloads control username ubuntu. And the public IP say yes to connect okay. We are also going to talk a few more things about SSH in Ansible section. Just observe this. Whenever you do SSH to any machine for the first time it will ask you a question. Are you sure you want to continue? So what is this? This is basically showing you an ID of this machine. So you're doing SSH for the first time and it is asking, hey, this is the ID of the machine. Do you know this machine? Do you want to connect to this machine using this fingerprint? It's called as fingerprint. Okay. These are not SSH keys. Okay. These are called as fingerprint. And when we say yes, it will connect. Now let's log out. Let me clear the screen. And now when I do ssh again to the same machine same IP. Observe. It will not ask that question. See that? Okay. Now once again I'm going to exit it. And I'm going to do something here. Now I'm going to remove the content of the file that stores this fingerprint. That file is here in your home directory dot ssh slash known host file. This file contains the fingerprint of all the machine you did. SSH. So far you can do just cat. And you can look at the content and it's huge. We can remove it. Remove the content. So we're going to say cat slash dev slash null redirect that output to dot slash slash known host. Now you will see this file will be empty. We redirected null to this file which cleaned that file. Now when you do ssh what do you think is going to happen. It's going to ask the question. Yes of course because it lost. It does not have any information about this machine now. So again it will ask the question. You see that I say yes and it will store that data. Okay. Let me exit once again. Clear the screen and let's do cat to that file. And you will see it has stored that fingerprint right. Look at the IP address also. And obviously when I do a SSH now the information is already stored. So it knows what machine is this. We already accepted the connection so we can log in. Why I am showing you this is because Ansible is also going to do the same to connect to those web servers and DB servers, or any machine for that matter. And there we are going to see similar question, but we are going to resolve that very differently. First let's install Ansible. Then we'll see those things. Okay. Let's just simply Google. Ansible installation. So it will take you to the documentation docs.ansible.com installing Ansible. Click on that and we'll go through the documentation. It's huge. But these documentation are very readable. In fact you will see that we use documentation a lot. I'm going to take you through the documentation in almost every lecture. Let's look at installing Ansible documentation. So there are different ways of installing Ansible. You can use pip python pip and click on this installing Ansible okay not this one. This is showing you how to install Pip. We are going to use apt okay here installing Ansible on specific operating system. Click on that. Okay so let's look at different OS. So we have fedora EPL open source ubuntu Debian and also now windows. So we will go with ubuntu. Click on ubuntu. It's pretty simple. Commands apt update and add the repository and say APT install Ansible. That's all. So I'm just going to copy only this command apt update all those things I'll run manually. Okay, so I'm going to say sudo apt update. Let's wait for a moment okay. Then I'm going to use shift insert to paste that command. Wait, I do not copy that okay I have to go back copy and shift insert okay. Something's wrong. Okay. For some reason it just threw me out. I'm going to use again shift insert paste there. This is going to add the repository for Ansible. So when we say apt install Ansible it is going to install Ansible from that repository. Okay. So we're going to say now sudo apt install ansible hyphen y. So Ansible is basically a Python library you can say. And as a dependency it is going to install Python three, Python two, whatever the requirements are there. Now Ansible mostly uses Python three and it also uses Python two. Both I shouldn't say mostly, and that depends on many factors which Python interpreter it is going to use. We have different modules in Ansible that we are going to use and different modules uses different Python interpreter, different Python version. So mostly it's at the client side. That means where we are doing the setup web server db server. On that machines Python interpreter will be used. All the code that we are going to run or we are going to write will be translated to Python scripts. And those Python scripts will run on the target machine on the client machine and not on the control machine. Control machine will be running Python scripts only when you mention local host. If you want to run something locally, we'll talk about that anyways. So let's check the Ansible version. Ansible hyphen hyphen version. Now you may see a different version than mine. So what I'm seeing right now is 2.4.6. And Python it is using is Python three. A few more things. Ansible configuration file which we will see talk about in a moment in the coming lectures. And there are a few other things. So that's all is Ansible setup really. Now we will see in the next lecture how we are going to use Ansible to connect to the other EC2 instances that we created, the web server and DB server we'll be testing. And we will be talking about a very important file which is inventory file in the next lecture. So set all this up and join me in the next lecture.
