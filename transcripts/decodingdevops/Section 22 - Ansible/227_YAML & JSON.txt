# YAML & JSON

If you remember pom.XML file from Maven and Jenkins session, you know, it's really difficult to read and also to write. But thankfully now we have moved to a better data structure. We have JSON and YAML. And if you have already followed Python sessions, you already know JSON. Yes, no, I have not taken any special session on JSON. But you know, Python dictionary. So what you see right now on your screen is a Python dictionary. You have a dictionary that has two keys, DevOps and development. This is the same example from our Python session. So dictionary that I have taken from the Python session. A dictionary that has two keys DevOps, its value is a list development and its value is also a list. And I will try to make it little more complicated. So I'll add one more key value pair , and there another key ansible facts. And its value is a dictionary. So we have three keys, DevOps, development, ansible facts, you have value, list, list and a dictionary. Now I know it's already getting difficult to read, but I will make it more readable. Now check this out. I'll just copy everything once again and I will make it more readable. Enter. Okay, there was this extra comma anyway? Now PyCharm is smart. It's already taking everything properly. So what I'm really doing is I'm just making this vertical. Okay, that's the same dictionary, but that is just a vertical version of the dictionary that you have seen. But it is so much easier to read. Now starts over there you have the key DevOps and development and ansible facts. You have the value as list. That's vertical development value is also list. Let me bring this one here. Ansible facts. It's value is a dictionary with just one key pair. And this my friend is JSON. Okay, that was Python dictionary. And this that you have just seen, vertical is JSON and JSON data structure is very famous now or very much adapted or adopted. There are so many tools now using JSON format. And in today's time if I see any tool using XML format, I'll be really worried. Okay, so that is about JSON. Now let's understand YAML. If you understood this, then YAML is a pleasure. Okay, so I'm going to copy this whole JSON again. Yes, I'm going to convert this into YAML and you will love this. Okay. In YAML we don't have opening and closing here. It's just like python indentation. And you don't even need to give double quotes. And the square brackets. I'll just clean this. This will look very beautiful in a moment. So double quotes if you want, you can give that's not mandatory. Like if you have special character at all, you can give double quotes. Okay, a list is like this. Okay, so indentation is very important. Now if you want you can start it here also. But after that hyphen, there should be a space. Okay, let's do it for the development. Also two keys DevOps development. And its value is a list. And list is in this format, a space and the element name. Okay? Or whatever the element is. Okay, let's do it for the third key also. Now its value is a dictionary. And that's it. You have to make sure there is a space after ansible facts. Those two space or three space. And that is YAML. Three keys, devops, development ansible_facts, DevOps key is a list. Sorry. DevOps value is a list. Development value is also a list. Ansible facts value is a dictionary. So key value, if you are having one more key value pair, then again space. Let's say for example version, its value is an integer. So let's check it once again. So that is Python dictionary horizontal. That is JSON and that is YAML. I know which one you liked. That's everyone's choice. YAML. So ansible playbook will be in YAML format. You have seen the execution. When we execute an ansible module, it returns output in the JSON format. And now you should be able to read it. Okay. So this is the output for DB one key name, ansible facts and you have changed and you have ping, ansible facts value is another dictionary discovered Python interpreter. Changed key value is a boolean. Changed is a string. Its value is a boolean. False again. A string ping. Its value is pong. Yeah, I know, not so funny. But in ansible whatever module you execute, it will have an output like ping module output is pong. Changed will be a key that will be present in all the output. Changed will be true if this module if this task execution made any change. So we are executing ping module. Ping model doesn't make any change. It's a soft touch model, it login and come back. So it's not making any change on DB one nor on web one. Or neither on web two. So changed value you will see is false for ping. But if you're installing a package or pushing a configuration file, and when you do it for the first time when it applies the change, the changed value will be true. When you run it once again, if the system is in the same state, the changed value will be false. We'll understand that in the next video. So I hope you understood JSON and YAML in the easiest way. Let me also show you Python documentation for YAML. Oh, sorry. Ansible documentation for YAML. YAML syntax. It talks about YAML syntax. YAML basics. Like this is a list. A three hyphen at the beginning of the file represents that it's a YAML file. But that's optional. It just indicates that this is where it starts. So here you see list of fruits. Let me make it bigger. Too small. List of fruit. Right. A dictionary key named Martin. It's value is a dictionary. Key value pairs. Okay. Indentation, this space is very important. This is a list of dictionary. So you have list. First element is a dictionary. Second element is also dictionary. Right? Martin is a dictionary. Its value is another dictionary. Then you have skills. Its value is a list. Okay? So on whole I will say this is list of dictionary. We have two dictionaries. One is Martin, the other one is Kabita. And its value is also dictionary. But then you have skills key in that, its value is a list. That's what I told you. When you're learning Python, it's very important for you to understand Python data types. If you understand that, you will very easily understand JSON and then you'll very easily understand YAML. To get the link of this documentation, just google ansible YAML and you will get the link syntax and then you can read through it. That will be all in this video. I'll see you in the next one.
