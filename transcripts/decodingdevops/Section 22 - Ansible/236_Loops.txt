# Loops

Instructor: Okay, I'm in exercise folder. I'm going to copy exercise nine. Sorry, exercise 10 to exercise 11. Let's get into exercise 11. Okay as I told you in the previous lecture we are going to talk about loops. Now let's look at the use case first. Okay, so we have a playbook and it installs a package. Just one single package, right? What if we need to install 2, 5, 10, 20 packages? Many, many packages. What if you want to do that? Right do we copy these task again and again for that many times? No, we will use loops. It's pretty simple. Just below this task we have to say loop. Okay, colon and its value will be a list. Now we can give the entire list here or we can have a list variable and we can give this variable value over here. Sorry, variable name over here. First we'll just go with the list. So we'll say hyphen, and first we'll say chrony. And the next package, let's go with some simple packages here, like wget, right? We'll go with git and zip, unzip. Right so these are the list of packages. Now here, instead of the name of the package we have to give the variable. We have to give double quotes, curly brace, two curly brace. And in the two curly brace we have to give a variable item this exact name item, that's the variable name the loop is going to return. So this loop is going to have a variable called item and it is going to loop your task as many as time you have the items. So this task, yum task is going to run how many times? Right five times. 1, 2, 3, 4, 5. And every time the value of item will be changing. First it'll be chrony then it will be wget, git, zip, unzip, like that. Okay same we will give for apt. So I'm going to copy this six yy and going to paste it here. Now we have a condition so we are pasting it below the condition. If there is no condition just directly here itself make sure it's in the same column as the module name. Now here, the package name is not chrony, it's ntp. And here we need to change from the name to the variable name. Okay, I'm thinking of adding one more task. Let's add one more task. Let's test this one first, then we'll add that task. Same task, similar task. Let's see how this one works first. So let's save and quit and let's execute the playbook. Now look at the output. It's going to be a little bigger. Okay, let's take a look. So you see the task is running multiple times, right? For each package. Right on web zero three, ntp, wget and git was already installed. The zip was not installed so it installed that one. Right? Now let's look at the documentation of loops. Ansible loops, let's look at this first one. Okay now there are two types of loop first of all, loop and with_ item. Now this is similar with_ items or you can give loop also but loop gives you much more functionality. Now this is the basic one that we use, right? This is a task to add a user and you can give the list of user over there. Now you can also give a variable and this variable will be a list variable that you need to define variables file like in group works all file, or based on your use case. Now you can pass values as a string, right? One string at a time, or you can pass a dictionary also. Now, when you have a dictionary, you can have multiple keys in the dictionary, like over here it is using item.name. So item will be this one in the first iteration and .name is going to return test user one. And then it has item.groups, right? So another key item.groups, it's value is wheel. So you can pass multiple values also per iteration. Then you need to have a dictionary variable instead of mentioning a string. Now, I love this feature 'cause I've used it many times. Okay and there are much more complex loops also, you see complex loops. Like retrying a task until a condition is met, right? And many other examples, we are, we have over here you can take a look, but for now we'll keep it simple is enough for now. When the need comes, then you can go with the complex one. But mostly we use this one only and the max, the dictionary variables. Now, as you know, we can use ChatGPT also to generate the playbook for us, right? So you can try that also. So I'm giving you many more options don't get confused, right? We start with a basic loop and you know how to write it now it's pretty simple, but whenever you need you have documentation, you have ChatGPT and a simple Google will also help you many times. So test this out. Try to add some more tasks like adding multiple users in the playbook or to the playbook. Try that also and then join me in the next lecture.
