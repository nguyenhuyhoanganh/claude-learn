# File, copy & template modules

Instructor: Now we have to do some file operations. We have configuration file of NTP. We have to update the changes in the NTP configuration file and then restart the service. So let's copy the exercise 11 to exercise 12. Let's get into exercise 12 and now I'm going to go to the documentation, but I'm going to run the top command to engage this session so it doesn't time out. Okay, so Ansible file modules, all the file modules. Okay, so we have many file modules. I told you this in the previous lecture also. As a DevOps engineer, we do a lot of file operations, right? We archive files, so we have archive module. We add or remove content from the file. So we have blockinfile that can update the content of the file. Copy module where we take a file and push it to the target machine. Fetch to fetch the files, opposite of copy. File modules, manage file and file properties like if you want to change the permissions on ownership of existing file, or if you want to create a directory, or if you want to create mp file. Okay, this is very different than copy module. Copy module is to push the file. File module is going to make changes to the existing files in the target location. Finding the file and like many things we have, iso_extract, lineinfile, replace the content of the file. Synchronize which uses rsync to synchronize content between two directories or files. It can be local to local or remote to local also, template, So let's look at the copy module. We will be using template module in this exercise, but let's look at the copy module and we already know copy module, right? But there are some interesting option also. We are going to first update a file or update a file content. Very simple content we have, right? So we can use this example over here, content. So let me copy this. So option here is content, there's no source. We're not pushing an existing file which is in our control machine. So let's open the playbook and let's go to the end, and this is a simple warmup. So I don't need to write anything, I just copied everything, so I'm just going to simply paste it, Shift + Insert, and make the indentation, right? Okay, so I'm going to update or create this file etc/motd banner. This is a Linux banner file. So whenever you log in, whatever the content is in this file, it'll simply print that. So the content we are going to make here as this server is managed by Ansible. So whenever anybody log in, they know that it is managed by Ansible so no manual changes. So we can also write that message, no manual changes please, like that, and we'll name this task to Banner file. Okay, so with copy model you can use a source also. You can use content also. We are using content because we have very simple content, right? If you had one line like that, it's better to use the content option instead of managing a file. Okay, I think there is some indentation issue. Let's look at that. All right, this is not in the right column. I think this should be better. Let's see, we'll quit, let's run it. Okay, that worked, it created a banner file. Let's log in and see if it works and we need to log in actually for one reason. This was a simple warmup, but our actual task is now. We have the NTP or chrony configuration file. We need to update that configuration file, and the best way to update a configuration file is to have your own configuration file and replace it with the existing configuration file, okay? That's the best way we've been doing so far. So let's log in. Let me check the inventory file. Let's get the IP address. We're going to log into one CentOS and one Ubuntu. So first let's log into CentOS, ssh -i. The login key, username ec2-user and the rate and db01 will log in. Okay, we are in and look at the banner, right? Okay, so the chrony configuration file is at this location, etc/chrony.conf. Okay, I'm doing a cat to this file because I need to copy the entire content of this file and dump it in my control machine. Before that, this is the configuration we need to update, pool. This is the NTP server, okay? Now we have the NTP agent which is going to sync the time with the NTP server, so where is the NTP server? Now most of the company will maintain, the bigger companies will maintain their own NTP server and their existing NTP servers in your country on your location nearby which you can sync with. So we'll use existing NTP server and when we want to use that, we need to mention that server name over here, right? So it is going to go to that NTP server, fetch the time, and sync your server with the time of the NTP server. So first, we'll take all this content. I have selected it. I'm going to copy this and exit from here and come back to the control machine, and we will create here a folder called as templates because we'll be using templates module. So I'm going to say vim templates. Inside that I'm going to say ntpconf_centos. Now this name does not matter. We're going to push it with a different name, but let's just have the files first. Okay, so create templates folder. It's not mandatory to create with the name templates. But when we see in the roles, we will need this name. So for now, let's create the folder with the name templates and put your configuration file there. Make sure you name it correctly so you can identify this is for CentOS and this is for Ubuntu. Now let's log into Ubuntu. Let's get the IP, ssh -i. Clientkey, username ubuntu, at the rate, and that's the Ubuntu IP web03. Okay, so we are in web03 and let's switch to route user, and here, oh, wait, sudo -i. Okay, here the NTP configuration file name is etc/ntp.conf, okay? It's near about similar, but of course, it will have some differences because this is for a different operating system altogether. So copy the entire content. Make sure you copy it properly, exit from here. All right, and then in the templates folder, you need to create it with the name ntpconf_ubuntu, right? This is just for identification. As I told you, you can give any name. Okay, now let's update the playbook first, right? Or second task, first, we copied the configuration file in our control machine. Now let's update the playbook. And as I told you, we will be using template module, why? I'll tell that you, I'll talk about that in a moment. So name Deploy ntp agent conf on centos. So two tasks obviously. The module name is template, colon. Now this is similar to the copy module, so we have src option here, right? So we have it in the templates folder, templates/ntpconf_centos. Make sure you give the correct name. Destination will be /etc/chrony.conf. Okay, the destination name matters and we'll also give the option for a backup yes. And obviously the condition when ansible_distribution is equal to CentOS and exactly similar task with the condition changes. Not exactly the many changes we have to make, so let's copy this. Okay, so Deploy ntp conf on, first of all, you have to change the title in Ubuntu and here the configuration file name, we have given Ubuntu. I'll just go Ubuntu, and it's configuration destination name is ntp.conf and the distribution is Ubuntu. That's a lot of changes, right, okay, anyways. Okay, now this is a configuration change. Template module, first of all, works similar to the copy module with some changes which we'll talk in a moment. But anyways, whenever we change a configuration, the next step is we restart the service, right? So after this, we will give the condition to restart the NTP and chrony service, so we already have the task to start the service. You can copy the same task and we are going to make the changes. Okay, first of all, the title name and so I simply copied the task, right? I'm just going to in the beginning give restart here and here the state should be restarted instead of started. There also, restart, and here the state is restarted. Okay, this should work. Let's save and quit and let's execute our playbook. Okay, so we're simply deploying the configuration file. We have not made any changes, but still for web03 it says change. It should have given okay. Let's see what is the difference. Let's look at the file content. Did we put anything extra in the code? Let's go at the beginning. I think let's go at the end. I think we copied an extra space over there. That's why there was a change. But anyways, the configuration file is good and the service got restarted. This is a problem. Even we have not made any configuration change. The service is still restarted for CentOS, and we'll come back to it and we'll fix it. Let's first focus on the template module. So why did I use template module instead of using copy module? Well, there's one simple and very important difference. We know copy module takes the file and directly dumps in the target location. Template model is intelligent. It will read the file if you have any template, any Jinja2 template. Now what's Jinja2 template? Well, it's a whole story for itself, but to begin with, we are using variables, right? We define variables, and then we use variable in the playbook, right? Now the structure that we give double quotes, curly braces, that's actually the Jinja2 templating. In Jinja2 templating, you have variables and conditions also that you can put, so you can design your configuration file based on some conditions or loops also. And template module is going to read your template file, look for any templating that you have done, and then from that, extract the actual content and then push it to the target location. Let's start with very simple one by defining the variable. I think I have group-vars over here. Yes, group-vars/all file, and in this file is already two variables. If you don't have, create this file, group_vars/all file. I'm going to give here the NTP server name. I'm going to say for NTP server ntp0:, and let me get, well, let me see. When I code this file, I don't want it to hang now. So NTP server, where do we find NTP server? Simply we can Google. Now I'm using Oregon region, Oregon, Oregon. I don't know how to pronounce it also properly, but let's see if I can use some NTP server from USA. So I just searched NTP servers in Oregon and well, I can go with this one which is in North America, right? So generally you have four NTP server to sync with. If not one, it syncs with the other one. So let's take this value, copy this, and let's open a group_vars/all file, and let's give here NTP server value as this one, ntp1 and ntp1. Okay, so these are the four variables, ntp0, ntp1, ntp2, ntp3, and these four are the values for it. So I got it directly from the internet, okay? No big deal, let's save this file. You can mention any NTP server you want, whatever location. You can Google and find the NTP servers. It's just for the example, right? So we have four NTP server variables defined and now we can use it in our template. So let's go to the template. First, let's go to ntp centos. Let's go at the beginning. So here you have option pool and you have to give the NTP server detail. I'm gonna copy that, Y, Y. One, two, three, okay, and I'm going to change this values. Instead of the static values, I'm going to give here the variable the same way we do it in the playbook, okay? So I will remove this. I'll say double quotes, curly brace, and here I'll say ntp0 and I should have copied after this, right? No big deal, I'm gonna copy it, Y, Y. One, two, three, going to remove this one. And here I'll change it to ntp0, one, two, three. One, this one to two, and the last one to three. All right, so these are the four variables that we have defined group-vars/all file and now we can use the variables in the templates. Now if this is a copy module, it is going to dump the exact same content in the target. But if it's the template module, then it's going to read, it's going to find is there any template? Yes, there is, there are variables. It's going to find its values, change the content, create a different file, right? With the values, and then push that file to the target location, right? Interesting, let's save and quit and let's do the same for the Ubuntu. Now don't worry about the NTP configuration. We are not learning NTP configuration here. Here you have pool, similar thing. Already four values here. Let's change these four values to the variables. Okay, so same thing, what I did in the CentOS configuration file. Variables instead of the static values. Now think about it. If I want to really change the NTP server details, I don't need to change it in both configuration file. I need to just change in the variable file, right? If like that, you have multiple configuration file out in general files where we use similar values or same values. We can put their variables and we can just change the value of variable whenever we want, and all the configuration files will be affected. Ansible playbook, let's execute the playbook. Okay, seems that worked. All right, configuration file changed there, there, and the service got restarted and we are happy, right? The service is getting restarted, but every time I run the playbook, every time the service is getting restarted. That is the problem, right? We are keeping that one for the next lecture. We'll add one more task just to show you the problem. So now I'm going to once again, update the playbook and adding some tasks. Let's say we have some tasks to create a folder, right? So to create a folder, you can use file module. Let me give a name first. Create a folder, okay? File module, okay, let's look at the documentation. File modules, manage files and file properties. Let's look at the example. Okay, then create a directory if it does not exist, so you have to say path of the directory and you have to say state as directory, and you can give the permission also. I'm just going to copy this two line, right? Paste it there, path etc, some directory. Now this doesn't matter. You can give any path, whatever you want. I'm going to say etc or I'll say opt/test21, okay? What we are trying to do is we are adding one new task in the playbook, right? And only that task should get executed because all the other things are good, right? So we add one task. We execute the playbook. Let's see what happens. Okay, so it created the folder, right? But along with that, it restarted the service. The configuration file was not changed, right? But again, the service got restarted. Do we really want that? No, right, in a production environment, this can disrupt so many things which is really not a good thing. We need to restart the service only when we need to restart the service. Not unnecessary, and how to handle that? That's what we are going to see in the next lecture which is about handlers. So wrap this up and join me in the next lecture.
