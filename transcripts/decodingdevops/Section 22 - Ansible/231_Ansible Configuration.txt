# Ansible Configuration

Instructor: Welcome to Ansible configuration section. We'll be seeing different Ansible configuration settings in this video, understand them, and also we'll see how to use them in the right order. So far we did not make any change to Ansible configuration but it's still working. That means Ansible has default configuration and that should be fine. But if you want to change the defaults, like for example, Ansible will connect to Linux machine by using SSH on port 22. That's the default port for SSH. And Ansible will also use that by default but for some reason, maybe security reasons, you change the port number of SSH on your servers, like web server, database server. You change the SSH port number from 22 to something else, maybe 2020. Now in this case, Ansible will try to access these machines on port 22 and it'll fail because that's its default nature. It's going to go on port 22 for SSH and that's default. And when you want to change defaults you change Ansible configuration. There are different levels Ansible configuration. The first is the global level, which we have already seen. /etc/ansible/ansible.cfg. We made also a change there. host_key_checking false, we said. That's the global Ansible configuration file and that will have the last priority. The first priority goes to a variable, ansible_config. If you set this variable, export this variable and set the path of Ansible configuration, then Ansible will use this configuration file and not the global one. If you did not mention the steps over there or write settings over there then it'll go for the global one. Second priority goes to Ansible configuration file in the current working directory, ansible.cfg, your project working directory or in your repository. We are doing exercise two, exercise three so we should create ansible.cfg file in exercise three for example, and that will have lower priority compared to the variable but higher than the global config file. Third priority goes to ansible.cfg file, a hidden file you create in your home directory. That will have the third priority. And the last priority goes to the global Ansible configuration file. So mostly we use ansible.cfg file in the current directory. Being a DevOps, you will be writing Ansible playbooks and you'll be doing version control. You'll be putting that in the repository, which will be used by the entire team or everyone in the project. So everyone should have the same setting. So you will commit ansible.cfg in the repository itself. Because if you set .ansible.cfg, that will be at the system level. And this fourth one is also at the system level. And the first one, that is also setting up a variable at the system level. The second one is the only repository specific and we'll be also using that. But first we'll go through ansible.cfg file global. We'll understand the settings. So let's read the ansible.cfg file. So the ansible.cfg file is at the global location. Switch to root user, sudo -i. And we'll open vim /etc/ansible/ansible.cfg. This file we already generated because the default file has very few settings. Sorry, no settings. It has just command to generate the Ansible configuration file. We did that, we generated Ansible configuration file. And this is a huge file. If I set the line number, it is more than a thousand lines. Okay, let me go to the top. So there are different sections. One is the defaults that contains the maximum settings. Are the most commonly used settings, but if you go at the end, you have different sections. You see variable section, powershell, and many other sections, like inventory sections winrm, ssh_connection settings sudo_become_plugin, su_become_plugin, and many other. I don't want you to go through the whole file but just keep in mind, Ansible has its default nature and whenever you want to change that nature you come to Ansible configuration file. There is privilege_escalation section we will be using. So we are looking at the global Ansible configuration file but we will be using a local ansible.cfg file. That's very much required and you should be using that file in your repository instead of relying on global Ansible configuration file. Okay, I'm at the top once again. So hash and semicolon are for comment. We have seen that, right? And there are many settings here like for example, force_colors. If you don't want the colors while you're executing the playbook... I mean, who doesn't like colors, right? But anyways, if you want to disable that there is an option. Okay, like that, the option for ask password. By default, Ansible will not ask password. It'll try to use the key or the password. If it doesn't find, it will fail. But if you want Ansible to ask password to log into the target machine, you can enable that. ask_pass you can say true and it'll ask you then password. debug=False, by default it's not going to do the debug but if you want it to debug also while running the playbook you can enable that. forks, this is interesting. Now let's say you have a group of hosts, let's say web server, and that contains 20 host. Web servers group that contains 20 hosts. So you can mention Ansible should be connecting to all the machine at a time, or five machine at a time, or one machine at a time. Now this depends on your Ansible or control machines resources. CPU, RAM, and the network also, bandwidth also. So if you have more, you can give more number of host. It can automate many machines at a time. But keep in mind this is not the limitation of Ansible, this is limitation of the control machine where your Ansible is running. Alright, let's look at some other settings. Inventory file, you can mention where should be the inventory file every time we are specifying inventory path, right? You can mention that in configuration file then you don't need to mention it every time you execute the playbook. log_path, by default Ansible does not store any log of execution, but if you want you can mention a path and it's going to store all the output in the log file. So see, like that, there are many settings. I can go on and on and on and on. There's documentation also, docs.ansible.com/archive/ansible. I can simply Google it. Okay, Ansible configuration file and configuration file. Okay, so you have all these settings, right? If you click on any of the setting it will tell you the description and how to use it. So let's create our own Ansible configuration file instead of using this global one. So far it was using global Ansible configuration file. So I will go to exercise seven itself. Let's not create a separate exercise for this one. I'm going to place ansible.cfg file over here. Okay, so first I need to mention the section. So first one is defaults in the square bracket and then let's give host_key_checking equal to false. That we gave it first. Also, I will mention inventory. Every time I'm mentioning inventory path. So I'll say inventory equal to ./inventory. So my inventory file name is inventory, okay? So don't get confused. This is the path of my file, this is the setting name. I will also give forks. I'll say Forks equal to five. Now it really doesn't matter. I just have three hosts at the max. It can anyways go with three machine at a time. You can mention the settings just to learn. Okay, I'll also mention log_path, where I want to store the log. By default is not storing log anywhere but I'll say, var/log/ansible.log. Like that I will also mention one more section. privilege_escalation. Now you remember in the playbook we mentioned become, yes. If you have multiple playbooks, all the playbooks we need to mention that we can make this as a global setting also. We can say become equal to true. So any playbook that runs over here in this current folder... Sorry, it's true. Is going to use that setting. And there are a few other settings, like become method which really doesn't matter. Or anyways, I'll give it. become_method is sudo because anyways become means sudo in Linux. become_ask_pass, false. Anyways, the EC2 hyphen user, that is going to do sudo, it's not going to ask any password for it, but in case, just showing you some extra settings. Right, now let's save this file and quit. And I'm going to see ansible-playbook, and this time I'm just going to give the name of the playbook, that's all. I'm not going to say -i inventory because it's already there in the Ansible configuration file. Okay, it's working, but we see one warning over here. It is saying /var/log/ansible is not writeable. We cannot create it. We as in the Ubuntu user is trying to create that file and this location is owned by the root user. So you have to do this, sudo touch /var/log/ansible.log and then you should change its ownership to ubuntu.ubuntu. Okay, now if you run the playbook. You see? No warning. And we'll also look at the log file. Okay, let's take a look. cat /var/log/ansible.log. You see, now it started maintaining the log. If we mention -vv, two levels of verbosity, then we should have more logs. See that? Okay, so there are many, many settings in Ansible configuration file. Don't waste in going through all of them. Whenever you require to change something in Ansible, you want Ansible to do something like this instead of that, right? Then check the configuration settings and make the changes. But always, always, always you should have an Ansible configuration file in the repository where you have your playbook. That, keep in mind. That is all in this lecture about configuration. I'll see you in the next one.
