# Introduction

Hello and welcome to Ansible sessions. Ansible is one of the most popular DevOps tool today and also my personal favorite. We'll understand what is Ansible, how it works, and we're going to see several examples of doing automation with Ansible. But let's first understand a little bit history, the history of automation from my own perspective in general. Also this is how the automation world evolved and also got adopted into the DevOps movement. But before even the DevOps was so popular, we used to do automation. And that was the time the operating system related automation bash scripting was for Linux task automation. Batch scripting was for Windows. And then we started getting other things like database automation. Virtualization came in, cloud computing was getting started. There was a need of doing other kinds of automation also, not just operating system tasks like virtualization task or cloud task or any DB task or any building automation. So programming language system admins or operations team started adopting programming languages like Python, Ruby in an order. If I say Perl, then Python and Ruby. But among all of these three, the Python got more popular because of its huge libraries where we get library to automate various kinds of things. Also PERL was also very popular, but Perl was being too much complicated as too much syntax. I would say in this three, Python won the race in the automation along with the time we got PowerShell also, which was for Windows specific. But from Windows you can automate other things like VMware related automation. And then after that the time came of configuration management tool. Puppet came in and the word configuration management came through Puppet. Puppet was not initially considered as an automation tool. It was more of managing configuration. The perspective of it was, let's say you have many servers, you have cluster of servers, cluster of web server. Let's take example and from that cluster of web server you have configuration. So almost all the web servers in a cluster will be exactly similar. The configuration will be similar, there package, their service status will be similar. But sometimes we used to make changes to those servers and they used to go out of sync. Some configuration file, we made some changes or service state has gone down, service has stopped. Any kind of configuration related change was supposed to be managed through a centralized server. A Puppet server on Puppet server will have the configuration of all of your servers. And Puppet server is going to apply like a law. And all the machines where we want to manage the configuration will have an agent that will regularly query to the Puppet server about its configuration and everything used to be in the right state. So configuration state across your infrastructure was supposed to be managed through Puppet server. But then when the DevOps movement came in, we started adopting Puppet as an automation tool. Along with Puppet, we had a salt stack which was very nice and simple tool to execute commands on remote machine. A combination of Puppet and Salt stack was very famous. Generally we used to run Puppet agents through Salt stack, an initial set up through Salt stack and then most of the configuration through Puppet. So it was the kind of combination of using multiple automation tools. And after that came Chef, now Puppet, especially configuration Management tool. And there was no programming language over there. Puppet is built on Ruby, Salt stack on Python. But we don't need to write the Python code or the Ruby code in that it has its own language. Puppet has its own language, domain specific language DSL. Now Chef came with more manageability and more power to the user. I suppose that's what I personally felt. We had the power of writing Ruby code also through Chef. And there are more templating and more features. The graphical user interface was there, which was very good. It's still there, you can generate reports and stuff like that. But along with all these features, it came with more complications. There are too many moving parts in Chef. You have server, you have client, you have workstation. Now I'm not saying Puppet or Chef is bad, it's all just a matter of the use case or your inclination towards a tool that also matters among all this. So now we have programming, we are scripting, then we are programming language, then we have Configuration Management Tool. And then came Ansible, which was again a configuration management tool developed by Michael Dohan. Ansible was written in Python and out of all this, Ansible is the most simplest automation tool. It was acquired by Red Hat then. And now you have a lot of things in Ansible. You have Ansible tower and you have enterprise versions. So Ansible came with an idea of simplicity, having a very simple code to manage your huge infrastructure. Ansible initially came in more focused on Linux machines. Then the Windows automation was also possible through Ansible. It's still possible through Ansible. And then cloud automation, then networking tool automation, database automation. There's so much of integration came in with Ansible now. So being simple, it also became very powerful automation tool. And also we have TerraForm now cloud formation which are cloud mostly cloud automation. Now Ansible you can do automation, cloud automation with Ansible. But TerraForm is more cloud specific automation tool. And sure, TerraForm also supports some other kind of automation, but specialization is in cloud automation. So this is a little history from my own perspective of how automation has evolved from Bash and batch scripting today to you having Ansible, TerraForm and they're sure there are more automation tool, but I'm talking about the most popular one and famous being in their own specific areas. And we are going to learn Ansible in this session. Let's see some popular use cases of Ansible first automation, any system automation, linux automation, windows automation. You want to set up web services, database services configuration, start the services, restart the services. You can do all those kinds of automation by using Ansible. On the same line Change management. You can manage your production machines, any changes to your production server, you can do it through Ansible. If you write a proper Ansible playbooks, it can go directly into your documentation. It's not going to be very complicated script to understand. If you write titles to your task in Ansible playbook, you can use that in the documentation also. Provisioning you want to set up anything from the scratch. As I said, you can use Ansible to launch cloud instances also like easy to instance you can launch and then you can set up services on it like any front end, back end database, web services. Complete provisioning you can do through Ansible or large scale automation orchestration, combining multiple automation tools and scripts together and execute them in a proper order. You can integrate Ansible with various other tools like Jenkins or any other integration tool or cloud services you can integrate with Ansible. So there's so much of integration with different different tools and services Ansible provide, so you can do complete orchestration. Ansible is really a very simple tool compared to other automation tool or scripts. It's also very clean. So you don't have any agent in Ansible, like in Puppet, Chef, Salt stack, you have the servers. And to manage all the machines from those server, you need to install agent on all the target machines. So if you're managing 100 servers through Puppet, Chef or Salt Stack, you need to install agents in all those 100 machines. In Ansible you don't need to do that. Ansible uses existing connection like for Linux, it uses SSH for Windows, it uses winRM, or it has API based modules also. So based on the target, it will use the right communication methods in its module. So you don't need to have any agent, you don't need to do any setup, and there is no databases and complicated storage in ansible You have YAML ini or text format where you have your playbooks, your scripts, your configuration in these formats, very simple formats. The output is written in the JSON format. The setup is very simple. Of Ansible it is just a Python library, so you can say Pip install Ansible also. Or you can install Ansible through package managers if you want to upgrade them again. Upgrade is also very simple. So you install Ansible and just get started with it like that. So you write the Ansible code, the playbooks, it's going to create some Python scripts for that execute it on A target and return the output. There's no residual software left on the target or even on the same machine. You don't need to write programming language to do any automation. With Ansible you have YAML. So Ansible script or Ansible playbook we call it is written in YAML format, which is very easy to read, which is very easy to write. It's structured and has really very less complication compared to any programming language. That's why they say Ansible is easy to read and write. Ansible has lot of modules to do a particular task. So apart from its system modules, it has modules which are API based. Like you want to launch an easy to instance from Ansible, you can do that. So in that case, Ansible will be using some API based tools to connect. So, being specific, for AWS, it uses boto that uses API. So you can make URL restful calls from Ansible. You can also execute your existing shell command or Windows PowerShell commands from Ansible. Any existing scripts, you have shell scripts, you can execute that also through Ansible. Now how does Ansible connect to its target. Now target being. It could be a Linux Server, Windows Server, it could be a cloud account, it could be a database, it could be network equipment. So for Linux, as I said, it is going to use SSH. So every Linux server will have SSH, and I said server and any Linux Server you take, linux Server operating system, Ubuntu Server, CentOS, RHEL will have SSH running. So you don't need to set up anything, you just need SSH detail and Ansible can connect to Linux machine using SSH. For Windows, it uses Winrm. Again, there will be WinRM service already there. You just need to enable a remote connection. In Windows PowerShell, you have to enable the remote connection so you can connect to Windows through Winrm connection from remotely. For cloud like AWS, Azure, Google Cloud, or any other cloud, it uses APIs. Apart from that, you can also manage network devices like switches and routers through Ansible or even databases. For database, it's going to use its own Python libraries. Like for mysql it has Python MySQL Library. So my point is you don't need to set up an agent on the destination or in the server itself. Well, by the way, there is no server in Ansible. Ansible is called as control machine. In general, you don't call it Ansible server because it doesn't run any service. Like Puppet Server, it runs a service. That's why it's Puppet Server, Chef Server, because it runs a service, or Salt Master, Salt Server, it runs the service. But Ansible does not run any service. It just uses the Python modules, python scripts to connect to the target machine, dump the Python script they're executed, return the output, okay? For Winrm or SSH, it's going to send the Python script to the destination, execute and return the output. But for API task, it's going to execute on the control machine locally, okay? There's no place where it's going to send the script. It's going to execute all the scripts locally. So you have remote execution in Ansible, and you have local execution also, and that's totally dependent on the target. And you don't need to really mention anything you just use the module and you mentioned the destination. All those things are abstracted from you. Ansible will do all the kind of connection and execution behind the scene. So there is no really great architecture of ansible This is just for an understanding purpose how it really works. Executes. So you have to create an inventory file. In inventory file you will have the target machine information. Like for example IP address, usernames password will be mentioned in the inventory file. Ansible has lots of modules in built modules that are supposed to do a particular task. For example installing a package or restarting a service or taking snapshot of EBS volume. So like that there are hundreds and more than 1000 modules in Ansible you write playbook and you mentioned from the inventory file that this is the host and I want to execute so and so module on this host. So that will be a task in playbook. You mentioned the destination. Anyway from the inventory, this is the host and this module needs to get executed. Then Ansible is going to use the configuration create Python scripts, Python packages. Python package means group of Python modules like we have seen in Python group of functions. Together we created a module. Like that group of module is called as Python package. So when you execute, it's going to create Python package, deliver it to the destination. Let's say Linux server executes there and returns the output. If the target is not operating system, probably a cloud account. You're launching EC2 instance let's say. So it's going to create Python package and execute locally. Simplicity is beauty and you have to take care of that in Ansible. Ansible is very simple to use tool compared to other automation and scripts. But it has lots of features. Use the features if you need it. Throughout this Ansible sessions, we are going to use various Ansible features, but we'll use it when the need comes. And I'll make sure that need comes. So you learn all those features. But keep in mind, Ansible was built on the principle of simplicity. So try to keep your code as simple as possible. Being so simple, Ansible is also very powerful. You can do a variety of things with Ansible. As I already told you, always automation. Cloud automation. You can execute commands, you can execute API tasks, you can execute scripts. So as being so powerful, you can use Ansible to do anything in your infrastructure. And you can replace all the other automation tools and just use Ansible. But again, it depends again on the need and use case. Also like if you're doing too much cloud automation, managing a lot of things on cloud account, I think TerraForm will be better than Ansible for cloud automation. Likewise, you make your choice based on your need. But when the time comes, Ansible can do most of the automation that you need. Enough talk now. Let's get into some action.
