# Hands on Docker Containers

Welcome to Docker Hands-on lecture. This one will be also a brief introduction, but this is hands on. Open your git bash or your terminal. Create a direcroy like I have a directory in F drive container into you can create it anywhere. In that dir, place the Vagrantfile, that is in this resource section. Download it and put it in that Dir. This vagrant file. Is based on. Ubuntu 20. And we have some commands over here which will create or which will install docker. engines. So the provisioning part is going to install Docker engine. All these commands, installation commands are in the Docker documentation. Let me take you there. So over here we have get docker in the documentation and we are going to get Docker for Linux. And ubuntu There's ubuntu. And in this document you have the installation commands, so you can go through this commands if you want. But I have put everything in the Vagrantfile. . So we will just bring this VM and let it install Docker engine. So I will do vagrant up. And wait for some time. Okay. VM is up. I am going to log into it, ssh. Let's be the root user, sudo -i Let's clear the screen and let's check the service. systemctl status docker. So you see docker engine is running. Docker application container engine. Okay, let's run our first test. So back to documentation after this installation steps you have here a command docker run hello world. Remember what I said in the previous lecture? Docker run means create a container. Hello world is the image name . This is available in Docker Hub. So let's just copy paste this command or I'll just run it. docker run hello-world Not hell hello-world Okay. So you see it says  unable to find image locally. It's not available in this docker engine, so it's pulling from somewhere. Okay. It says pull complete and then it print something. Now this container. Or should I say This image contains only this much. It. It's going to print. And that's all, its  job is done. This was just for the test. Some more commands, docker images. This will show you available images on your system. Docker PS will show you running containers. And a docker ps -a will show you all the containers. So you see there is a container I.D. This is created from this image and it executed some command slash. /hello and it's status is exited because it's not running. It just came up and did it work, printed everything and then it's dead and its name is relaxed. sammet Okay, that name it has given itself. Now, this session is just for understanding. If you can do and run this command, do it. If you don't want to do it, you can skip it. Also, we have in detail in section 20. Let me clear the screen and let me run one more command docker run. Yes, I'm creating one more container. This time I'm going to say --name. I will name a container. I will say web01 and I will give nginx This is the image name which is again in Docker Hub and I'm going to see here option -d means run it in the background, don't run it in the foreground, -p, small p and I am going to give a host port. Let's say 1980. colon and 80 that's the container port, port 80 is the container port and port 9080 This is the host port, So I am randomly selecting a host port host is here, the virtual machine. Now, containers will be running in an internal network. We cannot access it from outside. So to access it from outside, we have to do something called port mapping. So we access this host IP and we give port 9080. So it routes the request to the container web01 on port 80. I know this is too much to handle for now, but as I said, I just want you to understand technology for now. Details later. So I'm going to just hit it saying unable to find it locally. So it's pulling again. It's going to pull from Docker Hub. Okay. Something happened. Let's do docker ps This will show us running container. And, yes, we see a container web zero one. It is created from nginx image. That is the container ID. And then there's this port mapping. Now I can access this container locally. For that, I need to know its IP address. So I'm going to do docker inspect and the container name. Or I can give container id. And there I see the IP address. This is the IP of the container and I can do this. I am going to do curl http://IP address of the container and the container port. Now we are internal, we are in the Docker engine, the container is running here so we can access it locally by giving its IP in the container port and it should show us the engine default page. And there you go. It's in its HTML format. Of course, the shell cannot read html, it just prints everything. If we check it from the browser, we can see a proper original page. Okay, let's look at the IP address of this VM. IP address. Sure. And this one, the bridge IP. Let's take this IP and let's go to the browser. Let's also check the host port, which is a 9080. So let's take the IP Copy it. I give the IP in the browser colon and 9080 And there you go. This is coming straight from the container. So from the outside, the access from the host port or the node port. And that routes to the container and gets us our data. Now a quick glimpse on how to build our own image. I will create a directory called images. I will cd into that. I will create a file called a Dockerfile. D capital. And I'm going to paste some content over here. Right now. You do need to understand these commands. We will learn them in separate lecture. In Section 20. But I want you to see or hear from which is the base image of ubuntu. So we build our own custom image from a base image. Now this base image usually will be an official image from Docker Hub, but it can be some other image, a custom image also. But there will be a base image. On top of that, we build our data. So let's just save this and we will run a command called a docker build -t. And I will give some name here. tesimg, you can give any name that will be the name of your image. And a dot means the location of the Docker file, which is the current working directory. Let's hit this command and wait for some time. Now this may take a long time. I had already built the image, so it took a very short time. But for you, it will take a very long time to build this image. Let's clear the screen and we will say the command docker images. And we can see every image here. This is basically a web application that I have downloaded from tooplate.com And if you remember, we did this exercise on VMs how to set up a website on a VM. This is similar thing doing in a container and let's just run it Docker run. Hyphen d, hyphen capital P, which will take the host port automatically. Let's give the image name. And now I'm going to hit enter. Something happened. Let's see, docker ps. Okay. And that is our container. And it has taken port 49154. Let's access it from the browser. Let's get the IP address of our VM. That's the IP address. Copy that. And docker ps again. port for 49154. Okay there in the browser IP colon, the host Port, hit enter and there we go. That website I hosted in a container. So this whole lecture was about proving the point. That I was explaining in cotainers lecture. In Docker lecture. I am going to put all these commands and this Docker file into the resource section. You can. Get those commands and execute and test it and prove it yourself. Final thing, I'm going to do a little cleanup here. So. docker ps I'm going to stop all the running container. docker stop web01 and heuristic. hugle Then I'm going to. Remove all the containers, command is docker rm item and paste all the container names. And remove all these containers. Finally, let's remove all the images. Docker rmi to remove the image and I'm giving ID of all the images here one by one. So everything is clean now. No images, no containers, because we really need a clean set up in the next lecture. So if you're following me, then you also clean up everything and join me in the next lecture.
