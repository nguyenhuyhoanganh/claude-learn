# Microservice Project

Instructor: Welcome. In this lecture we will see deployment of a microservice application. The name is EMart. This application was designed by using microservice architecture. In the front end you have an API Gateway created with nginx and this listens at three endpoints, root, slash api, slash web api. When the user access the URL of the website, it'll be redirected to root, which is an app written in Angular. This will give the front end also called client app. And this will in turn connect to slash api, which will be served by an application Mart api NodeJS. And it also needs MongoDB Database. And the MongoDB Database we are going to deploy as a container, but it could be an independent service running from a server or from any other endpoint. This application also has slash web api, which connects to an application, Books api, which is written in Java and this also needs a database, a MySQL Database. So all these apps that you see over here, nginx, Angular, NodeJS, Java, Mongo, MySQL, all these will be containers. EMart app represents an e-commerce application, like Amazon, Flipkart. And then application can start like this and you can keep adding more microservices like maybe another URL for videos, another URL for payment gateway, another URL for cart. So you see, it's very scalable from a developer point of view as well. So we'll see a brief introduction of how to set this up on our virtual machine. And it'll be similar how we have seen for vPro Profile Project, V Profile was monolithic, this is microservice. But from implementing point of view, we'll have a similar thing, we'll have Docker and we'll have Docker Compose. So let's take a look into that. Okay, so I'm going to use the same wagon file which was used in previous lecture, which for me is in F, container, intro, folder. You don't have this magnet file, go download it from the resource and place it into the folder. If you already have, you just need to bring it up. But before we bring up migrant global status, hyphen, hyphen, prune. Check if all the other VMs are powered off. If they are not, power them off and bring up the VM. Vagrant up. VM is up. Let's log in into that. Vagrant SSH and switch to root user. Now this one will be slightly different than a vPro Profile Project on containers. Here we are also going to build the image, but don't worry, it's just one command. Same command. Over here the Docker compose file is going to build the images and run containers from them. So we need the complete source code. Okay, this repository EMart app. Clone the URL. Git clone, shift, insert. Okay, you should have EMart app folder. Get into that. Do LS. And here you have a Docker compose dot YAML file. Let's open this file and you will see there is an extra step over here, build, which is going to build images from Docker files. Again, we will learn about this in detail later in Docker section, but just wanted to show you Docker compose can do this also, can build image, and then run containers from that. Or you can mention the image path, which is available in the Docker hub repository from where it can fetch and run, or it can build and run. So here we have a container called client api, web api, nginx, and eMongo, also EMart db. So let's come out of it. Check if there are any existing images or any container. There's nothing because in the previous lecture, if you remember, I have removed all the images and containers. Okay, so time to build and run. Same command, Docker compose up, hyphen D. Now this is going to take a very long time because it's going to build the images and there are no JS images over here and that takes long time to build. So you can run this command and if this fails for some reason, then you can do this also, Docker compose build first. And once this is completed, then you can see Docker compose up hyphen, sorry, up hyphen D. So I'm going to pause my screen and come back when this is completed. Okay, container seems to be running. Let me check. Docker, compose ps. So we have total six containers running. You can check Docker ps hyphen A. If you have any exited container. Sometimes for some reason the container will get exit into exit state. And don't worry, this composed file will handle it automatically if any container goes into exit state, it is going to restart those containers and make sure they run. So from the browser will be accessing this nginx container, that is running on port 80. So let's check the IP address of this VM and that's the IP address. Let's copy that. Let's go to a browser and we dial the port 80 which is not even necessary. That's the default port for HTTP. So it goes like this, HTTP colon slash slash, like that. And that is the application. Now this is the front end and it's also getting information from the node api. We can also register, click on register. Let's give some email address. Let's enter some password Next. And it's asking ID number. Let's enter some number. Let's give first name and city, I'm going to select maybe New Delhi. And I will say a register. I think that got registered. Now this information is going in the MySQL database. Let's enter the password now to login. And we are in, it says Hi Imran. And we have the product list. And here we have the Booksmart that goes to the Java application books api. And that is all. So this was a glimpse of a microservice application running on Docker platform. Let's do the cleanup now. So Docker composed. Make sure you're in the same folder, EMart app. Docker composed down, which is going to stop and remove all the containers and Docker system, prune hyphen A, which is going to remove all the stopped containers and images and everything that are of no use. Exit from the VM and make sure you do a vagrant halt. So that is all in this lecture. I will see you in the next one.
