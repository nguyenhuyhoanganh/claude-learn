# What are containers

Welcome. In this session, we will talk about containers. How you can isolate your applications. Now this is a brief introduction on containers. We have much detailed information on containers and Docker and Kubernetes in later sections. What you see right now on your screen is a Linux computer. And I am pointing here two important things. One is the operating system, file system or the file system hierarchy of an operating system. At the top level, you have a root directory, underneath. You have many subdirectories like root, boot, bin, var, etc. I think I have explained about this file system in Linux session. If you are not aware about this directory structure, revisit the Linux session. You will find it there. Now these files are configuration files, binary files, libraries. And many other things. The question is, who uses these files? Well, they are the processes of the computer. Like you have Apache Tomcat process, Nginx process, MongoDB process. We also call them as service. But right now, I'm just talking about the process. So there are many, many processos running in the operating system and this process will be part of Process Tree. At the top you have init our systemd process and it's process is 1 And then this is forked into other more processes. And these processes that we are seeing, Tomcat, Nginx  are also part of this process tree. So this is been a structure for a very, very long time. That's how the operating system works. But I want to highlight some problems over here. When we are running multiple main processes or services in one computer like I have here, Tomcat, Nginx and MongoDB, MongoDB is a database, Nginx. X is a web service. Tomcat is an application service. So these processes do different, different things and they are using the same operating system, file system or same directory structure. Now, if I make any change to the configuration, it will effect all the processes. Same with the binaries, same with the libraries. If I'm installing a version of a binary or of a library that is going to effect all the processes here because all the process consumes same thing. So what we need is basically isolation. So to isolate our processes, our services. We use different computers. So in this diagram you see different servers of different computers. So for isolation, I set them into different, different computer. They have their own operating system. Now, these server or computer could be a physical machine. It could be a virtual machine. But the point is, for isolation. I need to set them into different computers. So they have their own files, their own process tree. And they are not disturbed by each other. But this creates more problem. And which is the cost. More computer means you need to spend more money, and if you want to do it for isolation, there is a better solution. And that very solution is or are containers. Now this is similar Linux computer, similar filesystem structure. But what you see here is Apache tomcat  container. What is that? Well, it's just a directory. Yes. And it contains its own filesystem, which looks like the OS file system. And it may look like just another operating system. But it is a miniature operating system or just it's a directory. And like that. Nginx is a separate container. MongoDB is a separate container and they can be all connected together. You will have a virtualized network, virtual network in the computer, which will give IP addresses to your container, which are just directory again. So this solves the isolation problem, but it also gives many other benefits or features like. This is a miniature OS, this is just a directory and it will be light weight. It will be very small, it won't contain everything. For example, for Nginx container, it will only have files that is required to run Nginx process and the Nginx process tree here. PID1 is the Nginx process. In MongoDB. PID1  is MongoDB process. Likewise for any computer, for any containers. And since they are very small, very lightweight, we can. Archive them. And these archive are called us image or container image. The purpose of archiving or creating a container image is , We can ship it. Where? Anywhere you can run container from your desktop and the same container you can archive and run it on production server and how all this happens. We will see that in detail later. But I wanted you to have some idea of containers because because you'll hear this a lot in today's time. So who makes this possible? This containerization? So if we look at this architecture we have on the left side the hardware that is the computer. Now, it could be a virtual hardware. That means it could be a virtual machine also. And virtual machine or a physical computer will have an operating system. And on top of that, we will have a container engine also called a container runtime environment. And this makes it possible to run containers or isolate your processes. The most famous container runtime environment is Docker. Now, how all this works, how Docker works. As I said, we have in detail in later sections, to be specific, in section 20, we have Docker, but in this lecture I will show you a brief introduction on Docker and Little hands-on, so join me in the next lecture.
