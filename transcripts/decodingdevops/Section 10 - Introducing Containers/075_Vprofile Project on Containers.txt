# Vprofile Project on Containers

Instructor: Hello and welcome. In this lecture, we will see how to run our Vprofile application on containers. Now this lecture is more of a trailer of what is coming next in Docker. Everything is ready made over here. We just need to bring up the VM and run our Vprofile containers on it. Later in Docker section, we will see how to build all this from scratch. So if you remember from Vprofile project setup on the VM, we have Nginx, Tomcat, RabbitMQ, MemCached and MySQL and we build separate images for them, sorry, separate VMs for them. In this lecture, we will see how these things will run on the container together. We are going to run this entire setup on a virtual machine. So in the virtual machine, you will have all these containers running and we will access it. So let's see those simple steps now. Download the vagrant files from the resource section as a zip file, extract it. So there's vagrant file for Windows and MacOS Intel and MacOS M1 chip, separate vagrant files. So create a folder or directory in your system. Copy that vagrant file. You can do it anywhere. You don't need to do it exactly the same place where I'm doing. You can copy this to any other folder. I'm creating this container intro and I'll be copying the vagrant file, downloads, vagrant files, and inside that is I think another one, yes. And Windows vagrant file to F/containerIntro. Okay, if you want, you can copy from GraphQL also and just copy and paste your vagrant file into a folder and get into that folder. Okay and that should be the vagrant file. This is for the Windows and MacOS Intel chip. And so this vagrant file contains also commands to install Docker and Docker CLI and other stuff. It has two GB of RAM. Make sure you shut down all the other VMs so you don't run out of resources. Vagrant global-status --prune. Run this command to remove all entry of VMs and check the status of all the vagrant VMs in your machine. Okay, so I have three VMs and all these three VMs are powered off. If you see any of your VM powered on, just go to that folder and power off those VMs, vagrant halt, or delete those VMs and then bring up this VM, vagrant up. This is going to take some time. It is going to install Docker Engine and all those things. It'll take some time. So I'm going to pause my screen and resume when this is completed. Okay, my VM is up. I'm going to log into it, vagrant ssh, and I'm going to switch to root user, sudo -i, and clear the screen. Now, if you want to run multiple containers together, there is a concept of Docker compose in Docker. We'll learn in detail about it, but in short, you have a command, docker compose, this command and this will read a file called dockercompose.yml file. I'll show you that in a moment. So when you run docker compose, you should see the options here. You can see docker compose up to start and run the container. And you can see stop. You can see docker compose down to stop and remove the container and many other options you have. Don't worry about these. All these options, we'll be covering in detail later. But docker compose needs a docker compose file. That file we have available in our repository. We are going to just download and run that file. Okay, so you need to come to a repository on github.com, right? That's our repository and you have to switch to the branch, okay? You click on this dropdown and give the name as docker and click on this docker. It will switch to the branch. Make sure this is docker over here and then you should see a compose folder here. Click on that. And that's the docker-compose.yml file. If you click on this, it should show you the entire content. Again, don't need to understand this entire docker compose file now. It is going to download and run it, that's all. In Docker sections, we'll see how to write this file from scratch and even build the images. There are images here for our Vprofile project. This is the image for the database. This is a ready made Memcached image. This is ready made RabbitMQ image. This is the Tomcat image for Vprofile project and this is the Nginx image for the Vprofile project. So we go to Docker Hub. I want to quickly show you this, where these images are located. You go to Docker Hub and you can search with this name. This is the account name, vprocontainers. Go to Docker Hub and here is that. So that's the account name and you see that these are the images in this account. There are three images. Or you may see more later when I add more images. So I have created these images and stored over here. We'll see later how to create this from the scratch. So for now, what we need to do is click on this raw button here. Right, get that link, copy that link, go to your VM and simply wait. Let's create a folder, compose. Let's get into that folder, say wget and paste that link, Shift + Insert for Git Bash to paste. Okay, my download has failed and I wanted to show you this. I think this is because of the restrictions put on GitHub in India. So if this fails for you also, you can get this compose file from the resource section. Right, so once you download the resource, unzip it, you'll see compose folder, right? Open that through Notepad or whatever you want, and copy this entire content and let's create that file ourself, vim docker-compose.yml. Make sure the spelling is correct and Shift + Insert and paste everything and save and quit. Right, now all you need to do is say docker compose up -d. So -d is to run it in the background. So it's going to read this compose file, bring up all the containers and this is going to take time for the first time because it's going to download all the images. Okay, so it's pulling up all the images. Pulling all the images and it's gonna bring up the container. I'm gonna pause my screen and resume when this is done. Okay, I think all my containers are up. Let me check, docker compose ps. Okay, all five containers are running. Docker images, if we see, we should see those five images from which the containers are created. Now let's access it from the browser, ip addr show, check the IP address of the VM. Right, enp0s9. Okay, this, well kind of IP, okay, 192. 168. 1.7 or 56.82. Okay, any of this, this 192 IP address, not the other one. Okay, any 192, 168 IP, copy that and let's go to the browser. So this is accessing the VM on port 80 and on port 80, we have Nginx container, Nginx container routes to the Tomcat container. We get this login page from the Tomcat container and we can use the username admin_vp and same password and click on login. This should validate the database container also. We're able to log in so database container is working fine. We check RabbitMQ. It should generate queue. So RabbitMQ container is also good. Click on all users. Right, select any of the user. Right, the data is inserted in cache so Memcached container is also working fine. Right, so data is coming from the cache. So that was validation, a quick validation. We know about all this. We did it in Vprofile project, right? So that's how you can pull the container images and run the containers. You see how easy it is to run projects on containers, applications on containers, and that's why one of a very big reason the world is shifting towards the container. And we'll talk in detail about this in Docker lecture. Now you have to do this docker compose down and this is going to stop the container and remove the container. One more command, let me show you, docker system prune -a. This command will remove all the stopped containers and remove all the images if they're not used. So everything will be clean. Then you can log out, exit and power off the VM. Right, now whenever you want, you can power on the VM and do the docker compose up in the same folder where you have compose file and check it yourself whenever you want, right? So that is all in this lecture. We'll see about Docker much in detail in the Docker section.
