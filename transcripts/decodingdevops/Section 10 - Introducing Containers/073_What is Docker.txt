# What is Docker

Docker overview. And that's what we'll be talking about in this lecture. This is straight from the Docker documentation and here we will overview, really revise what we said in the previous lecture. Docker is an open platform for developing shipping and running applications. Here, the word application really means containers that have your process in it so you can develop your own container. Which we will see later how to do and we can ship it. And run it wherever we want. Wherever you have Docker Daemon installed, you can run a Docker containers. A Docker enables you to separate your applications from your infrastructure. So this is what is that isolation we talked about so you can deliver software quickly. Docker platform, docker provides the ability to package and run an application in a loosely isolated environment called a container. Exactly what we talked in the last session. The isolation and security allows you to run many containers simultaneously on a given host, so you don't need to have multiple computers for isolation on the same computer. You can run multiple containers and containers are lightweight because as we discussed, they are miniature OS They just need files to run that particular process. Like Nginx container needs only Nginx files. So you see the link. You can sure read it and you should read it. I want you to see this architecture diagram, which we will be doing in the next lecture. So Docker Host really means a computer. Docker Daemon is the Docker service that we are going to install. So we will create a virtual machine. We will install Docker engine on that and we can run the containers. How do we run the containers? Well they run from the image. And where do we get image from? Registry. So here I want to show you one more. A website or a registry. hub.docker.com Click on Explore and you can see readymade images available here. These are the same container images we're talking about. And you see theres python, there is postgres, this ubuntu which is a miniature ubuntu, traefik, redis, node, mongo, open JDK, MySQL, Golang, Nginx and the list just goes on and on. I'm showing you the official images, but you can create your own images and store it on Docker Hub. We will be doing all that in Section 20. Going back to the diagram. So here you see a docker client. Now, client can be on the same machine, or it can be in a different machine. We will have it on the same machine. So we create a virtual machine, will install Docker engine in that and that we have the client also. And with Docker build command, we can build our own images. From the official images we can pull image, we can pull the official image or unofficial images from the registry link. Docker Hub. docker run command will be to run the containers. When we say "docker run", that means run the container from the image and we will see all this happening in the next lecture. Join me there.
