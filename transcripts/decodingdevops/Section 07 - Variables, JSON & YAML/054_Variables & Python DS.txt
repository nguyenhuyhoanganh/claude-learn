# Variables & Python DS

-: Okay first, let's see variables. And for this exercise, Windows user open Git Bash and MacOS user can open your terminal, not PowerShell and command prompt, Git Bash for Windows, terminal for MacOS. Okay, so let's look at variables. Variables are temporary storage of data in the memory, in the RAM. So they live in the program. So now I open Git Bash, these variables will be living in Git Bash and when I close this, those variables will be gone. The temporary storage and we are not getting too much into detail. We have a separate bash scripting section and Python section to get into detail. This is very, very basic. So first, variable should have a name. So I'm going to give it name as skill or skills or just skill equal to double quotes. That's my value. So equal to no space in between. Skill equal to and the double quotes. In the double quotes, I'm going to say DevOps. So DevOps is the value or the data and this is the storage. And let's hit enter. So it got stored into the variable named skill. Now, in order to print it we can use echo. And in Bash you have to say dollar, or shell, any shell you can say a dollar and the variable name. So we stored the data and we retrieved it. Now, dollar is important over here, if I remove the dollar and just said hit enter like this and it is just going to print the text skill. Also, we can do it like this, we can put it in double quotes and we can say I am learning dollar skill. Okay, so skill is a variable of type string. String generally refers to text, which is put, which we generally put in double quotes or single quote. But when we are trying to access the variable, you can put it in double quotes, but you cannot put it in single quote. If you put it in single quote, and this is specifically for shell I'm talking about, not Python or any other programming language. This is about the shell. So in shell, if you put the variable in the single quote, you see it removes the meaning of dollar. Dollar is just another text now, but if you put it in double quotes, then it works. I'll take one more variable, I'll call it as num equal to some number. This is integer, echo dollar num, we get the value. Okay, so that's a very simple example of a variable storing variable and retrieving variable. We are using echo command to print on the shell, but there are many, many use cases. We'll see this in detail in Bash scripting section. Now let's move towards Python. And for that I recommend you open Brave Browser. You can download install Brave Browser, because we are going to use some online editor. Brave Browser will block the ads and all. So, this is not sponsoring. I generally use Brave Browser to block the ads and popups. So I'm going to open Brave Browser, if you don't have it, pause the video, download, install, and then continue. And now I'm going to simply do a Google search, Python editors online. I'll go with this first one, Programiz or anything you want, you can if you want you can use VS code or if you already use some editor you can use that as well. Okay, so like in Python, we have echo, here we have print and we give the curly, sorry, the parenthesis inside that double quotes and we give the text. Okay, so that's the text or you can say a string to be technically specific. And we can run this as simply prints, right? Let's write our own print statement. Print parenthesis, double quotes. This is my first Python string and let's run it. Okay, also, let's try one more thing. We'll say print parenthesis double quotes. We'll not give anything. We'll see what happens, run. Okay, I have to run once again. Okay, so that gives a simple empty space over there. One new line, I'm just using it as a separator. Also, look over here, there is a hash and some text written over there. Whenever there is a hash in Python or Bash interpreter, it will ignore that. So if you say hash, hello. So it's going to simply ignore that. And after that, let's write another print statement. That is a comment, let's run it. See that, there is nothing there. Let me give an empty here also, so that hash is ignored. Okay, a little zoom. Now, let's come back to the variable. So in Python also we can store variables like that. First we'll look at string variable, string variables. We'll name the variable as skill equal to and double quotes or single quote is very important over here. So when we run this, it's going to store that variable. Just store, okay, we are not retrieving it. We can use the print statement. Double quotes, no sorry, no double quotes, okay, just the variable name. No quotes for the variable here. If you put variable in the quotes, it is not going to work. It will be just like any other text. You see that, the output? I will put one more print statement here, I'll call it as strings. Okay, and don't worry too much about why and how, we are going to get into detail in the Python section. Our main focus is understand the greater structure in Python and then learn JSON and YAML. Okay, now skill is a string variable like that. I'm going to also take one example of integer. I'll say number equal to 123. And I'll say print num. And I'll also use this integers, so run it. Right, so simply storing and retrieving it, or should I say just printing it. So string variable, that's integer variable. And now let's look at the next one which is list. And I'm going to simply comment the other ones. Okay, so far, string, integer, next as I told you, we will look into list. Now list also we have a variable. We can define any name of the variable. I'm going to simply call it as tools equal to. And this is how you define a list. It will be in square bracket, let me put it here in the comments also. List will be in square bracket and list can have strings integers or even nested list inside that. So in double quotes, I will say Jenkins comma separated by comma, say Docker and say Kubernetes. That's the short form of Kubernetes, Terraform. And we can also have integers. So let's put some integer also. So when we run it, it is simply going to store this, but if you want to print it, we can say print parenthesis and just give the name of the variable, which is tools. Run it. Okay, so whenever you see like this, which is in square bracket and the elements are separated by a comma, that is a list in Python and also in JSON, which we'll see in a moment. Now let's look at tuple, which is similar to lists. I'm going to simply copy this and paste it. Okay, tuple, now the difference between, the visible difference should I say, the visible difference between list and tuple is list is in square bracket and tuple is in parenthesis. Same thing, just change the parenthesis there instead of square bracket, run it. So that's the only visible difference. As I said, it's a visible difference. There's an actual difference which you learn in Python. So that's writing and that's retrieving it. But you can also retrieve the exact element if you want from the list or from the tuple. Like for example, if I want to get Jenkins from this list, so I have to say print, the variable name, the list name, square bracket, and the index number, it starts with zero. So at zeroth position you have Jenkins. If you run this, see that, you got Jenkins. Also, there is a negative index range minus one which will be the last one, which is here 90, the integer. Right, so you can do your experiment then 0, 1, 2, 3, 4, like that or minus one minus two, minus three, minus four, minus five. Same you can try for the tuple, same works for the tuple. So list or tuple name, square bracket, and the index number. Now let me comment this tuple, because our main focus will be list. Now in Python, this is called as slicing and you can get more further into this. So I'll take one more example, instead of zero, I'll give one here which will return Docker to me. Right, we get Docker, let's comment this minus, okay, so we get Docker. Now we can also get a range. So if I want to say I want to get Docker, k8s, and Terraform. So 0, 1, 2, 3, 4, so just copy pasting this. So from one, which is the start range colon and the end range 0, 1, 2, 3, 4. So if you want to go till three, then you have to give four. So Docker, K8s, and Terraform. Not the fourth one, fourth one is 90 the integer. But it is given til fourth. And this can be sliced more further. Just one more, please bear with me. Square bracket. So that is a list, right? And we can say list of this one, so zero one. So that will print me K8s. Right, now K8s is a string and this can be sliced more further, but I don't want to go here more further. That is enough for this section. As I told you, in Python, we are going to get much more into deep detail in slicing and how we can retrieve the minute of the minute data. Now let's, let's shift our focus to dictionary. That's the last one here. So I'm going to comment all this slicing, wherever I have I'll keep it till here. Okay, so string, integer, list. Now we are going to look at dictionary. So as the name says dictionary, if you look at any dictionary, it's the name and its meaning, right? So there is a key, which is the name and meaning, which is the value. Like that in dictionary the data will be stored in this format. Key, value. Key, value, and dictionary will be in parenthesis, sorry, curly brace or flower brace as some people call it. So I will create a dictionary with name DevOps. That's the variable name of the dictionary, equal to curly brace, open and close curly brace. And key and value, the data will be stored in key value pairs. So first we'll say, okay, it's a string. So double quotes, skill, colon. So skill is the key and its value, we'll say DevOps. Okay, so that's one pair, comma. And we can give a space also. And we'll say here, year. So skill is a string, its value is also a string. Year is a string and its value will give an integer, we'll say 2023, comma. And I'll give two more and its value I will keep it as empty string. And I'll give one more, GitOps colon. Okay, now look carefully here we have strings. We have a string called DevOps, right? Its value is a string. That's why skill is string. Year is an integer variable because it's value is integer, tech again string, GitOps again string. Its value is double quotes, right? That double quotes anyways in Python means string. Now let's print it, print DevOps, okay, without the double quotes, just give the variable name, print. Okay, once again take a look, take a look. DevOps, which is a string. 1, 2, 3, that's an integer. This is a list in square bracket and dictionary which is in the curly braces. Key, value. Now, if you remember in list what we did, we have used the index number to access the element. But in dictionary, we don't have index number, we have the key name. So skill is a key. Year is a key, tech is a key, GitOps is a key. DevOps is the value, which is string, 2023, value. This is empty string, this is empty string. Now how do we access a value? It will be similar to list, except instead of giving index number, we give the key name. So check this out. So print DevOps, which is a dictionary, square bracket. In double quotes we give the key name. So skill is the key and its value is DevOps. So this should print DevOps, there. We can do more slicing. That is a string and we can slice a string. So just one more example here. One simple example actually. So skill, square bracket and then, sorry, one second, square bracket. And the index number. Now string can also be sliced by using index number, same like list. So if I say here, zero, so I should get D in return. And then it's the same thing, what we have seen in the list. We can give start range end range also. But for now don't, don't get into that. Just understand the data structure. In the next lecture, we are going to create more complicated dictionary. And from that dictionary, we are going to understand what is JSON and YAML. So do some hands-on over here. Focus on the syntax, the commas, the colon, the double quotes. All this comes with practice, which we are going to do in Python. For now, do same what I am doing. Store different types of variable and simply print them. That's all. Do some more hands-on and then join me in the next lecture.
