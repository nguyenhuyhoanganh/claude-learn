# Introduction

Instructor: Hello and welcome. In this section of the codes, we are going to learn what is JSON and YAML through Python data structure. This will be rather very simple section, simple lecture, but extremely important. So first we will see what are variables. Then we will get into Python variables, Python data structure, like strings, integer, list, tuple, and dictionary. After gathering this knowledge of Python variables, we are going to understand JSON, based on Python data structure. And from JSON we will understand what is a YAML. Now, initially this section may look out of place, but as I said in the beginning, it is extremely important. In DevOps, we are going to see various tools and technologies applications, and most of these tools use either JSON or YAML or both for data exchange. So as a DevOps engineer, it's extremely important for us to understand, read, and write this data format. Now, this is not the only data format that different tools uses in DevOps but most of them relies on JSON and YAML. And slowly in this course you will realize how important it is to understand JSON and YAML for a DevOps engineer. And the best way to learn is by doing it. So first we will see variable, which is a temporary storage in the memory. Then Python data structures, how to write different types of Python variables, store it, and retrieve it. Then we are going to convert that Python data structure into JSON and from JSON, we are going to decode YAML. So let's get into action now.
