# DNS Route 53

Instructor: Okay, so our vprofile application will be living in app01 and that will need the backend service connection. Database, memcache, RabbitMQ. So how does our application knows the backend services, right? So coming again back to the source code, SRC, main, resources, application.properties file. Right, here we have db01, that's the host name. Now this should resolve to an IP address because network connectivity happens through IP address. Name is just for understanding, right? Once again, if we add IP addresses over here, like here, db01 IP here, memcache IP. And if something goes wrong with the instance, we delete it and we create a new instance, then we again need to update the source code or the application properties file. We need to update with new IP. Not a good idea. Always remember in the configurations files we should have the names, not IP addresses. So in local project setup we have seen, we have IP to name mapping in ETC host file. Right, in ETC host file. So this name will be resolved to IP address in the host file, but that won't work out here in the cloud. I mean, it will work out. It's not a good or standard practice. We need a private DNS service, a DNS service that can resolve name to IP address. So we can conveniently give the names in the configuration file and the DNS will resolve that. So what is the DNS service on AWS? It's Route 53. Route 53 or Route 53, whatever you want to call it. So Route 53 is a DNS service where you can create domains. I mean you can buy domains, you can create DNS entries for your domain. We're looking for very simple thing. We don't need a public DNS service, we just need to resolve this internally just for our instances. So we need to create a hosted zone for that where we can create the record like DB01 results to so and so IP address. So we go to create hosted zone. You will give a name. I will give it as vprofile, wait, vprofile.in. If you want, you can give anything else, but make sure you put that same entry in the application properties file, okay? Otherwise, keep whatever I'm doing over here and you have to go with private hosted zone, not the public private. We just need internal resolution, that's all. Okay, region. So I'm using North Virginia. I hope you're also using North Virginia. Right, where is the North Virginia? Okay, there, it's just N. Virginia and VPC. There will be one default VPC that we have to select and let's just create hosted zone. Okay, when you create the hosted zone here, you can create the records, Okay, hosted zones once again. Vprofile.in. Okay, now let's get the private IP of our instances. Go to EC2 service. Go to our instances. Okay, starting with db01, get the private IP, not the public IP, okay? Just internal DNS resolution. Just get the private IP. Let's create the record. Create record. And you need to give here db01. So the host name will be db01.vprofile.in. This, we need to add in the application properties file. Here you put the value. Okay, this record type is A record. A record means name to IP mapping. Okay, C name is different. C name is name to name mapping. Here the value will be another name, maybe like a load balance or URL, okay? But we are doing A records, name to IP mapping and just say create record. Be very careful creating this record. Make sure you're giving right name and right IP address. MC01, let's get the IP address of MC01. Private IP address. Again, private IP, not public IP, Private IP, create record. Last one. RMQ01. Just get the private IP of RMQ01. Okay, make sure you check the name and IP mapping. Create records. Okay, after making this entry, I also want you to validate so you are sure. First, check all these records are having private IP, 172 or whatever the private IP is. There's no public IP here. And also if this is really resolving this name to an IP address. So for that, let's go to app01 instance, take its public IP and SSH to this instance. SSH username is Ubuntu, because this is an Ubuntu AMI, app01. And over here you can just do this ping -c 4 and just say db01.vprofile, right? Vprofile.in, whatever domain you created, just check. And there's a reply coming back. More importantly, you see here the IP address. Make sure this is the right IP of your db01. Like that, verify all the three instances. Right, one last entry that we need to create is for app01. So create record. I'll say here, app01, and this is not mandatory because we anyways using load balancer to connect to app server. But you know, since we are having all the entries, let's also have app01 entry. Again, not mandatory for this project. Create record. Okay, app01 need all this information. It's nobody connecting to app01 with the name. So you don't need this entry. Anyways, we created all these four entries and validate, check everything is correct or not. If you find everything is good, then wrap this up and join me in the next lecture.
