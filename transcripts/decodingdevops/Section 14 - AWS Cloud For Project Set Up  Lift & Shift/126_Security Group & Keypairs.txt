# Security Group & Keypairs

Instructor: Hello and welcome. In this lecture, we are going to create security groups and key pairs. So we need to create total three security groups. One will be for the load balancer, Application Load Balancer, which will listen from the internet traffic. So the rules will be HTTP and HTTPS. Initially, it'll be HTTP, but later, we'll remove that and we'll keep only HTTPS. So the port number will be default, 443, allow from anywhere. Next security group will be for the application, our Tomcat instances. So Tomcat service listens on port 8080. So we have to mention here a rule, port 8080 allowed from the load balancer security group. We'll also allow port 22 from our IP so we can SSH to these instances. Now our vprofile application living in the Tomcat instance will connect to the backend services. Our vprofile application will connect to backend instances for three services. One is MySQL, port number 3306; Memcache, port number 11211; and RabbitMQ on port 5672. So here we have to give first three rules for Memcache, for MySQL, for RabbitMQ, these three port numbers allowed from the Tomcat instance security group, and also the port 22 from My IP so we can SSH to this instance. We can find out this port number in our application.properties file for application. So if you come to the source code, and you can be in any branch, I'm on awsliftandshift branch because this is the branch we will be using in this project. So we can check in src, main, resources, application.properties file. Now here we have db01:3306, MySQL port 3306. Memcache, 11211. And RabbitMQ, 5672. Okay, so once again, three security group: load balancer, Tomcat instance, and backend security group. Okay, so let's get started. Let's go to EC2 service and start creating the security group. Let's go to Security groups. You can go from here or you can click over here, Security Groups. So first, we will create security group for the load balancer. So click on Create security group. Scroll up, give security group a name, vprofile-ELB-SG. And let's give some description. Security Group for the vprofile load balancer. Scroll down. Very carefully edit inbound rules. Do not touch the outbound rules. It should remain the same. If you make changes to the outbound rules, your instance will not get any internet traffic. So add inbound rules. So we will select first HTTP, allowed from any IPv4. Once again, same thing, HTTP, allowed from any IPv6. Anywhere because we are hosting it for the public on the internet. That's why the anywhere rule. And also, we have to add the rule for HTTPS because later, when we set up the load balancer, we are going to add a domain certificate that we created in this AWS signup lecture. In the prerequisite section, we created a certificate that we will be using so we can use then HTTPS secure connection. Again, same thing, any IPv4. And then, HTTPS, any IPv6. Okay, we updated the rules, and once again, do not touch outbound rules. Say Create security group. Okay, let's go over the next one. Create security group. This is for the application Tomcat instance. Name: vprofile-app-sg. And description: Security group for tomcat app server. Inbound rules, add rules. So Tomcat listens on port 8080, so we will here say, I'll keep it Custom TCP, I'll say 8080, allowed from the load balancer security group. So in the source, you just type sg and select the load balancer security group. You can give some description: Allows traffic from vprofile load balancer. And also, we need to SSH to the instance, so we'll say 22 from My IP. And again, if you're using broadband, normal internet connection, your public IP will change, so if you're not able to SSH to the instance, let's say the next day, make sure you edit the rule and you again say My IP so it'll update your IP. Okay. Again, do not touch the outbound rules. Create security group. Okay, the last one, the backend security group. Let's give it a name. So, vprofile backend security group. And the description I have given, which makes things much clearer: Security group for mysql, memcache &amp; rabbitmq allow from tomcat, or I should say allowed from, allowed from tomcat app server. Let's add the rule. We need to add first, there's three rules, first for MySQL, you can just directly type 3306, or you can also search for MySQL here, which by default is 3306. And the source should be the app security group, okay? App, not the load balancer security group. Make sure you select this properly here. And the next rule will be 11211. This is for Memcache. Once again, same source. Source will be app security group. Okay. Then 5672. Again, source is the app security group. And port 22 from My IP so we can SSH to this instance. And there's also one more thing that people do. Instead of adding one rule at a time, you can also do this. All traffic allowed from the security group of the application. This makes things easier, but from a security point of view, this is not a good idea. All right. Just make sure this is the application security group. If you're again confused about the port number, check the application.properties file. src, main, resources, application.properties file, right? 3306, 11211, and 5672. Okay, again, if you're not able to SSH, make sure you update the rule of My IP. Okay, after adding all the rules, making sure outbound rule is this, we will create the security group. Okay, now we need to add one more rule over here. The services will be also interacting with each other, like Memcache will be connecting to MySQL. Now, these rules are set on the instance. The security group is not a container where you put the instances. When you create the instance, it borrows all these rules from the security group that you select, so every instance will have the security group. So if MySQL instance does not have a rule that says allow 3306 from Memcache instance, then the request from Memcache to MySQL will fail. So we need to add one rule here. After creating the security group, you can add that rule. Just say Edit inbound rules and we'll add a rule here which says, all traffic allowed from itself, its own security group, there, vprofile backend security group. See, we are editing the vprofile backend security group and we are saying all the traffic is allowed within itself. So when you give this rule, did I select it right? Once again, backend security group, okay? So all the instances within the security group can connect with each other on any port. And this rule is perfectly safe, provided the instances are not compromised. Okay, let's save this rule. Okay, now let's go to key pairs and create a login key for our instance. So I'll say Create key pair. I'll give key a name, vprofile-prod-key. .pem format, because I use Git Bash or Terminal. If you are going to use PuTTY, then you can use .ppk format. And say Create key pair. Okay, key is downloaded. Okay, so once again, we created three security group, app for the load balancer, for the Tomcat instance, and for the backend instances. I know I'm stressing this too much, but in real time, this is very essential for you to understand. Being a DevOps engineer, you need to have this networking knowledge, firewall knowledge. If you don't, you'll end up allowing all traffic from anywhere, which is a huge security risk. So understanding the port number of your service and the flow, right? Like the Tomcat instance connects to these backend services. So we can give the security group of the Tomcat instances as a source, so no other traffic is coming from no other place. All right, then that would be all in this lecture. I will see you in the next one where we will create these instances and set all these services. See you in the next one.
