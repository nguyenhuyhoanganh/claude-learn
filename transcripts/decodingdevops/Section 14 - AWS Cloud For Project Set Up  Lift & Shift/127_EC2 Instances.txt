# EC2 Instances

Instructor: All right, in this lecture, we are going to set up four EC2 instances, one for MySQL, second for Memcache, third, RabbitMQ, fourth one, the Tomcat instance. We are going to provision this instance and set up all the services by using the user data script, and while we launch these instances, we have to make sure we put them in the right security group. These three instances goes into backend security group, Tomcat instance goes into the app security group, and then in the later lecture, we'll see the load balancer, when we create, we put it in the load balancer security group. Okay, first thing we'll do is we will clone our source code, so come to github.com/hkhcoder/vprofile-project. Copy the link. So I'm going to clone it in the F drive that I did in the previous project, but I'll remove that repository first, so in the hkhcoder, I cloned the repository and we made some changes. We just should delete this now, and then let's open VSCode. Okay, let's click on this button over here, source control, and clone repository. Paste the URL over there, click on it, and select your location where you want to clone it. I'm going to keep it in F drive, hkhcoder, and I'll say select repository destination. Okay, open in the same window. So by default, you will get the main branch. We need to switch from main branch to AWS lift and shift branch. So click on this and select AWS lift and shift, all in small, click on this branch. Here, you get this folder, user data. Expand that. So first, we will launch the DB instance, so we're going to use this mysql.sh script in the user data to set up the MySQL service. Now, this is not so much different from what we used in vprofile project local setup when we set up our vprofile project on the virtual machines using Vagrant. So this is the similar script, except few changes. We will be using Amazon Linux 2023 AMI, not the CentOS AMI, and in this, you have the MariaDB package name as mariadb105-server. So that is the name of the package which we install. We start and enable MariaDB service. We clone our source code. Now, in manual provisioning, we have seen, after we install MariaDB service, we run a command, MySQL secure installation, which will ask us the question of the root password, removing the test databases and all, but this will be all happening in the background, so we have some SQL queries here that is going to execute those things for us, removing test users and all, and once that is done, we create the database, we create admin user and grant it privilege on the accounts database from locally and from remotely, and we deploy the DB schema file. This is the same thing what we did in vprofile project local setup. So let's go ahead, launch the instance, and execute the script on that. Okay, I'm on my AWS console, again, north Virginia region I'm in, same place where I created the security group. Just check once again if you have the security groups. I have all the security groups. Let's go instances, and I'm going to say launch instances. I will name it as vprofile-db01. We'll add some more tag. We'll give the project tag here. The value will be vprofile. Now, in real time, you will have some naming conventions, some more things to add, like the environment owner, so make sure you follow the right standards as per your organization or as per your project and give proper naming convention to your instances, not just on the fly, you decide something and give the name. There should be a standard. If there is no standard, you have to make sure you set the standard, and we'll also make sure the tags are on the volumes. Make sure you don't miss this in the real time. Your volumes should also have tag so you don't accidentally delete some volumes or maybe delete the instance or volumes are just there, just like that, eating up your space. Okay, next, go and select an AMI, so we are going with Amazon Linux 2023 AMI. We're keeping this as a standard in this project, except for Tomcat. That we will set on Ubuntu, and I'll let you know why later. Okay, coming down, instance type, we'll keep T2 micro, or if you have T3 Micro, that is a standard. You can select that one. Just make sure you have free tier eligible there. Okay, key pair, we already created key pair, vprofile prod key. We'll select that. Network settings, edit, and come to select existing security group and select your security group. So this is a database instance, and we have a separate security group for that, vprofile backend security group. Make sure you select that and then come down and click on advanced details. Scroll till the end, and here, we're going to put the MySQL script, so I'm going to go to my VSCode and make sure I copy everything from here, including the first line. Go back and just paste it over there. This first line is important. Otherwise, user data script won't run. Also, one more reason your script won't work is if you have made changes to the security group outbound rules, you will not get any internet and won't be able to download any softwares. So that is it. Launch instance. Okay, the instance launch is in progress. Let's go and look at the second instance script, memcache.sh. It's pretty simple, install memcache, start and enable it, change the configuration file, so Memcache service listens on remote connection. For that, we replace the local IP with 0.0.0.0, then we restart the Memcache service, so it starts listening to the remote connection. We execute this command so Memcache listens on port 11211. That simple. Just let's launch the instance. Let's go back to the EC2 instances. Click on launch instances, vprofile-mc01, additional tag, project, I will add project, vprofile, make sure the volumes are also tagged. Select the AMI as Amazon Linux 2023 AMI, free tier eligible. Instance type, T2 micro is fine, free tier eligible. If you have T3 micro free tier eligible, you can select that also. Key pair, vprofile prod key. Network setting, edit, and make sure to select the backend security group. Backend. Goes in the same security group like MySQL, and then advanced details, user data, let's copy your script. It's a pretty simple script. Let's paste it here and say launch instance. Okay, let's go and check the next script, rabbitmq.sh. Now, this script is very different from what we used in local setup of vprofile, 'cause that was done on CentOS, and the same method does not work on Amazon Linux 2023 AMI because it has different repositories. It's almost the same operating system, but the repositories are different, so I got these commands from the RabbitMQ documentation. So first three commands are simple. We import sign-in keys for RabbitMQ repository, Erlang repository, which is the dependency of RabbitMQ, Erlang, then we have here a command that downloads a file from our repository. I have created a file in our repository, so downloads and keeps at this location. Let's take a look at that file. So our source code, this local, we cloned it from GitHub, so there's this file. This is the information of a repository for Erlang and RabbitMQ. This I got from the RabbitMQ documentation, so one way is manually you create this file. The other way is what I did. I just created this file, stored it in our source code, and we just download it in the instance. I can put all this also in the script. By using cat command, we can create the file, but the script will look very ugly, so I use this method. So in our source code, it's going to go to this branch, and here, we have this repository file. It's going to download it from here, because that's what it does. It keeps that file at this location, etc/yum.repos.d, and we run DNF update, install socat and logrotate dependency for RabbitMQ, and then we install Erlang and RabbitMQ. Start and enable the service. After this, it is the same what we did in the local project. We changed the configuration, we add the test user, we give it administrator tag, we give it permission, and we restart RabbitMQ servers. Okay, so let's go and launch the instance and execute the script on it. Click on launch instances, give it tag, name, vprofile-rmq01, project, vprofile. Make sure to tag the volumes also. Come down. Same AMI, Amazon Linux 2023 AMI, T2 micro or T3 micro, free tier eligible, whichever, key pair, vprofile prod key, network settings, edit, select security group, and select the backend security group once again, backend security group. User data, let's copy the script. Make sure you copy the whole thing. Paste it, and let's launch the instance. So the last instance, then, Tomcat, very simple script. Let's go to tomcat_ubuntu.sh, that's right. We're going to use Ubuntu AMI for this one, Ubuntu 2024. Reason? Well, we did use CentOS in the local setup project. I wanted to have a different operating system for this one, Ubuntu. I want to show you how simple it is to use Ubuntu. Most of the packages are available in the repositories, so if we use CentOS and we need to download the latest Tomcat, you have seen what all we need to go through. We download the binary, extract it, add Tomcat user, create systemd file, and we do a lot of things, but here in Ubuntu, it's pretty simple, apt install tomcat10, that is it. And this is available in Ubuntu 2024, or simply we just call it as Ubuntu 24 server. Older than that, we have Tomcat 9 in the repository. So that is all we need to do. We just need to set up the Tomcat service. To build and deploy, that we'll see in the later lecture, where we build the source code locally on our computer, on our laptop, and then upload it through S3 bucket. It's going to be fun, so I want you to keep that separate so you have some more extra learning. Okay, so let's quickly launch this instance. Let's go to launch instances. Name, vprofile-app01. Make sure to tag the volume. Project, vprofile, tag the volume. Okay, here, the AMI will be Ubuntu 24. Make sure that's 24 LTS HVM SSD volume type, free tier eligible. If you don't see it right away, click on this and find it from here and make sure to go with 24. T2 micro is fine, key pair, the same key pair, network settings, edit, select. Now, which security group do we select over here? That's right, app security group. Tomcat instance goes into the app security group, and then, when it connects to these backend instances, these backend instance security group rules will allow the connection. So the app security group, advanced details, user data. Let's copy the script, and let's say launch instance. All right. Now, close these extra tabs over here. Remove this over here, the filter. We see all the instances. Now, app instance will be still executing the script and all. Let's verify all these instances one by one. Let's check the service of every instance. db01, select the db01, get its public IP, and open your terminal. Let's do ssh -i. My key is in the Downloads/vprofile-prod-key. Username is ec2-user. ec2-user, this is the DB instance. Amazon Linux 2023 AMI, the user is ec2-user. If you're doing an SSH, after one day, your public IP will change. You need to update the security group rule then. You can just go to security group, edit inbound rule, and 22 from my IP, and then it should work. Okay, we're in. Let's check the MySQL service, first of all, systemctl status, mariadb, if I can type. MariaDB. Okay, it says active, running up here. Q to quit from here, and let's log into the database, mysql -u, user, admin, -p, password, admin123, database name accounts. Once again, do not type password on the command line like this. I'm typing because I want to show you that this is the password so you don't get confused. It's in the script, but in real time, do not type password like that. Just hit enter, it is going to ask you password. If you just give hyphen P, it will ask you the password and you can enter the password. We are in the database, and let's see, show tables. Okay, that's good. Let's exit from here and exit from the instance as well, exit, exit, and let's clear the screen, and let's log into the next instance, which will be Memcache. Select the Memcache instance and get its public IP. Make sure the right instance is selected, otherwise, you'll copy the wrong public IP. Let's log in, yes. Switch to root user, systemctl status memcached. Okay, active, running. That's good. All right, let's exit from here, and let's go to the next instance, which will be RabbitMQ. We can remove this IP. Okay, let's get the RabbitMQ IP, rmq01, select it. Again, make sure you're copying the right public IP. Validate the name of the instance. Okay, let's log in. Sudo -i. Systemctl status rabbitmq-server. Okay, active, running. Let's exit from here. Okay, we're good. If any of the instance you see that there is a problem, the service is not running, or you're getting any error, then you don't need to do any troubleshooting, because we just launched the instance and put the user data script. Simply delete the instance, launch a new instance, and make sure you're selecting the right security group and using the right script. Sometimes it happens, we use different script for different instances and we look for a different service. It happens when we're doing so much, so don't need to do troubleshooting here, because there's nothing we're doing manually. It's everything automatically through user data. So delete the instance, launch the instance, make sure security group rules, outbound rules are clear, you have not updated the outbound security group rules. Make sure you do, as I've shown you in the lecture, selecting the right AMI, right security group, then it will work. Even after doing all this, if it's still not working, contact me through Q and A and I will help you. App instance, we'll verify in the next lectures, but for now, that would be all in this lecture. If you're going to continue in the next lecture, keep the instances running. If you're going to continue it later, make sure you shut down these instances, power off, and then when you continue, you can power on the instances. In the next lecture, we're going to look into the private DNS entries, so wrap this up and join me in the next one.
