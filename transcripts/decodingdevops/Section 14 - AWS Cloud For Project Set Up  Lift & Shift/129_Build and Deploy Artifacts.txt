# Build and Deploy Artifacts

Instructor: All right. In this lecture we are going to build the artifact from the source code on our computer and we are going to push it to S3 bucket. And on the Tomcat instance app zero one, we are going to fetch that artifact and deploy to the Tomcat service. So on our computer we are going to build the artifact from the source code by using Maven and Maven needs JDK. Also we need to have AWSCLI so we can communicate with the S3 bucket. So Maven, JDK, and AWSCLI on our computer, we'll check that. We need an S3 bucket. So we'll create an S3 bucket for the authentication from our computer to S3 bucket, we need IAM keys. So we need to create IAM user generate AWS IAM keys, stored on our laptop, so this authentication can happen. In Tomcat server, again, we need AWSCLI to communicate with S3 bucket. For authentication, we will create IAM roles that will have permission to access S3 bucket and apply this IAM role on the EC2 instance Tomcat server app zero one. Okay, so first we'll do this part. We'll create an S3 bucket IAM user with IAM keys and IAM roles. Then we'll check a computer for the right versions of our softwares. Once that is done, we'll build the artifact and deploy to the app zero one instance, so let's first start with AWS. Let's do these three things. S3 bucket, IAM keys and IAM roles. I am on my AWS management console in North Virginia region and I'm going to the service S3. I'll say "Create Bucket", general purpose Bucket name I have given as vprofile-las-artifacts, you cannot give the same name, you have to give a different bucket name just to make it unique, you can add some numbers. Bucket names needs to be unique. So give a different name and just simply create it. Take a note of S3 bucket name. We will be using this in the command line. Talking about the command line. We need IAM access keys to store it on our laptop. So I'm going to go to IAM service, go to users, say create user, give user a name. Vprofile S3 Admin, that's the name I'm giving. Go next, attach policies directly select this option and search for S3. We just need S3 access. So we'll just give S3 full access and go next, see create user. Let's generate the access keys. So click on the user, go to security credentials, scroll down and see create access key. CLI use case. That's what we want to use. Command line. I understand the about recommendation and say next, create access keys. I'm going to click on download CSV file. So if you miss this, if you don't know the access key and secret key, now if you don't store it, save it, then you lose it, you're not recovered. You have to generate new access keys. So let's store it. You have store it in my downloads and say done. Okay, next we will create IAM roles. Now roles works like this. You can apply it on any AWS servers like most of them, like you can go to the instance, select the instance, see action actions, security, modify IAM role and you can select the role from the dropdown. Okay, let's cancel this. Let's create the role first, let's go to roles. We'll say create role, AWS service for AWS service. We are applying this role, use case ease for EC2 and go next and select the permission. So we'll search for Amazon S3 Full access, come down, go next, let's give role a name, S3 admin I'm giving it and see create role. Okay, at the same time now, I'll apply this role. So select your instance app zero one, action, security, modify IAM role, drop down and select the S3 admin role. Now this role has the permission, S3 full access. So from our instance, we can run AWSCLI command to authenticate with the S3 service and only S3 service because it has just the permission for S3. If you need more permission, you can add, you can edit the role and you can add more permission or remove the permissions. Okay, so we're done with this S3 bucket created IAM keys and IAM roles created. We have also applied the IAM role to our instance. Now we'll come to our own computer. We need to build the artifact. So we need Maven. Maven needs JDK, and we also need AWSCLI command to push the artifact to S3 bucket. Okay, so come to your VS code or the editor that you're using. Now when we build the artifact, our artifact will have application.properties file that will have the backend service information. So first we need to make sure the backend service information is correct. So go to SRC, main resources, open application.properties file and we have here DB 01. Our host name will be DB 01 .V profile.N. So in Route 53, we created the record right DB 01 V profile in points to the private IP of DB 01 instance. Okay, so we have to make sure we have the right information in our code. So DB 01 V.profile.n and then MC 01 V.profile.n. And same with RMQ 01 .V.profile.n. Make sure to save this and did Control S, you can just click on file and click on save also. So now it's time to build the artifact from the source code. For that we need Maven and GDK and right version of it and let's check all that and build it and all we can do it through via score. We don't need to open separate Git Bash and within in VS code, you can do it like this. Control shift P and you get this, you have to search over here, select default profile and you'll get this here terminal select default profile. Click on that and from here you can select command prompt Git bash, PowerShell. So I'm going to select Git Bash, for Macs user, you can just select terminal from the list and once you select that, click on view and click on terminal here. And you see that, you see that Git Bash profile over here and you see you are in this path. So if you're not able to open this, you can just simply open Git Bash or terminal and go to your source code in this folder. Wherever you clone the source code, we can do it from there also. First, let's validate we have MVN-version command to check the version of Maven. So 3.9.9, this is what you need and Java 17. If you see different version than this, then go back to pre sites section. We have installing software lecture there. Over there, check how to install these versions, right versions of these tools based on your operating system, Mac OS or Windows user. And then install these tools. Once you have this, you can build the artifact. One more thing, you will check AWS. Just type AWS here and you should get this kind of output. That means AWSCLI is also installed. If it's not installed, again, check the lecture, installing software's lecture and install AWSCLI also. Okay, so everything is good. Now it's time to build the artifact. So I'm going to run the command MVN install. Okay? Make sure it is in this folder. So it reads all the files and builds the artifact. And once it builds the artifact, you should see a target folder over here. Let's hit enter. This is going to take some time. Okay, the build got completed successfully. I see a target folder over here. If you expand this, you should see here V profile-V2.war, that's the artifact. This is an archive of this folder. Okay? So once it's extracted on Tomcat server, it should have this folder, this content of the folder. You should have their web INF classes in that you should have application properties file. Okay, now it's time to copy the artifact to the S3 bucket. But before that we need to do one more thing. We need to run this command AWS configure. They should ask us the access key and secret key that we can store on a computer. Just run AWS space configure. So I'm going to put here the access key and the secret key. So from my downloads, I have opened the access key and secret key CSV file. You can use any editor and open that file and it's quite dangerous to show access key and secret key like this. But I'm going to make sure, after I complete this recording, I'm going to delete the access keys and even delete the user. Very, very careful in this part with the access keys, if you accidentally expose it and make sure quickly you delete those keys or remove those keys or even disable the user. Some people by mistake put their access keys in the source code repository, public repository. And what happens in that case, your keys will be taken from the public repository and people will start doing bitcoin mining in your AWS account and many other things that they can do hijack your account, demand ransom. Again, ransom also going to demand on bitcoins. So there are auto bots on the internet, sorry, I mean to say bots. Bots on the internet that scans this public repository and find these kind of information and use it. So be very careful. So I'm going to copy the access key here first, separated by comma there and put the access key there. Hit enter, then the secret key, separated by the comma. And it's also asking a default region. Mine is US East one, North Virginia. So I copy that. Default format, JSON. If you accidentally make some mistake and store the wrong keys, wrong information, you can edit it yourself. This is the path of the file, ~/n your home directory.aws/credentials. This file will have the access key and the secret key. So if you make a mistake, you can anytime edit it. And also there's another file config file that will have the region and output. If you make a mistake there, you can edit this file also. You can use via editor or just normal editors. Okay, so we have the artifact in the target folder, vPro-V2.war. So we're going to push it to S3 bucket, so AWS S3, the service name cp, the action, copy, and we give the source target/ vPro profile V2.war, and the destination S3: -- and your bucket name. If you have a sub folder, you can give a forward slash and give the sub folder name over there. I'm just going to copy it here itself and the S3 bucket directly, hit enter. Okay, the upload completed. If you're getting any error over here like authentication, then make sure you store the access keys. Once again, if you're sure the access keys are correct, then check the permission attached to that IAM user, it should be S3 full access permission. You can run AWS S3 LS and the bucket path and you can list the content in the bucket. See the artifact is there. Okay, now it's time to log into our app zero one instance and fetch the artifact from there. Okay, so go to app zero one and get its public IP, again, app zero and public IP. Make sure SSH is allowed in the security group. SSH-I downloads, the key part. Username is Ubuntu, at the rate and the IP address. Okay, if you're getting connection timed out, edit the security group inbound Rule 22 from my IP, save it once again. Okay, let me switch to root user. Clear the screen. So we need to have AWSCLI on this instance. Usually we run the app command on Ubuntu, but there's also another package manager called snap. So that should be the command to install AWSCLI through snap. Snap install AWS-CLI--classic and hit enter. Okay, there it is installed. Let's run AWS command and it's installed. So let's copy the artifact from the S3 bucket. So the command will be AWS S3 CP source will be your S3 bucket, S3 : // the bucket name. That's my bucket name, / the artifact name and destination, tmp. Let's hit enter. Okay, that was faster. So we have artifact at this location. Now, it's time to deploy the artifact. First we will stop the Tomcat service. So system CTL, stop, Tomcat 10 can just say Tomcat 10. Okay, seeing some changes happened in the source configuration file. And we need to run system CTL daemon reload command. I did not make any change. It's just a warning. Still let's just run system CTL daemon reload. And then let's stop the Tomcat service. Okay, now we'll remove the default application on Tomcat. So it is in this path. You do LSV /var/ lib/Tomcat 10, that's the home directory of Tomcat on Ubuntu. And this you have web apps folder here, you should have the root folder. That's the default application. We need to remove that. Make sure you give the RM command, rm-rf. And this path, exactly this path, remove it. And now we'll copy our artifact with the same name. It'll say CP/tmp/ vprofile-V2.War. This path ends with root, sorry dot war. Okay, that should be the path. Hit enter, and let's start the Tomcat service. When we start the Tomcat service, that time it is going to extract it into root folder. So let's say start system ctl, start Tomcat 10, and let's do LS to web apps. Okay, still not extracted. Let's check once again. There. Okay, in few minutes our application will come up. Next lecture, we are going to set up a load balancer and access our application through the load balancer. So wrap this up and join me in the next lecture.
