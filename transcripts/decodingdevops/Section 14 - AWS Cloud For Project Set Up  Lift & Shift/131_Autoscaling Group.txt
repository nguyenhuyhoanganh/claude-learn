# Autoscaling Group

Instructor: All right, so in this lecture, we are going to create auto-scaling group for our app01 instance so it scales out automatically when the load increases and scale in when the load decreases. So we need three things for auto-scaling group. First we need the AMI of the instance. Second, we need the launch template, the information on how to launch the instance, what security group to select, what key pair to select, et cetera, et cetera. That's launch template. And then the auto-scaling group itself where we mention this launch template. So first let's start by creating the AMI of this instance. So select this instance. Make sure you selected app01. Go to action, image and templates and say create image. Let's give AMI a name, vprofile-las-app-ami. And the description also, I'm giving the same. And just say create image. See it's currently creating AMI. It gives the link, let's click on that. It should take us to the AMI. All right, we can see the AMI or you can just go to here, images and click on AMI also. Let's see it's state. It's in pending state. This is gonna take some time to become available. By the time we can create the launch template. So let's go to launch template. Here in the instances section, launch templates. Say create launch templates. Let's give it a name, vprofile-las-app-LT. Scroll down and select the AMI. You have to go to my AMIs and find your AMI. This might not be still created, but you get the name in the list, right? So that's the AMI. It's the only AMI I have here. I'm going to select that. Instance type, t2 micro, or t3 micro whichever is under free tier eligible. Key pair, we have the login key, vprofile-prod-key. Network setting, we have to select the existing security group and that is app security group. This is the app instance. So app security group. Resource tags. Let's see, add new tag. That's give first the name tag. So we're just going to give vprofile-app, no 01, 02. So tag the instances and volumes. We'll add one more tag for the project, project vprofile and make sure you select the volumes tag also. Okay, come down, expand this advanced details. We need to give the IAM role if you want to. It's really not necessary now because in CI/CD, in Jenkins, we'll see how we can automatically deploy the artifact, right? But in real time, there may be cases where you need to add roles to your instances. Many times, many, many times you will see you need to add roles to the instances. And in the auto-scaling group, if you want to do that, you have to make sure in the launch template, whenever the instance is launched, you have to select here IAM instance profile and the role. So when the instance gets launched, it gets launched with this role. And then there are many other settings that we are going to skip and say create launch template. Okay, so launch template is created successfully. Now the final thing, we need to create the auto scaling groups. Go to auto scaling groups and say create auto scaling group. This is going to scale out instances when the load increases and scale in, reduce the number of instances when the load decreases. So you'll have a minimum size, you can mention a maximum size, how much maximum number of instances it can go, and you can have a desired capacity. Desired capacity is the one that it will try to maintain, but minimum it will go till the number of instance that you mentioned. So let's say you said minimum number of instances are two. So it's not going to go less than two ever, even if it's trying to scale in. It cannot go less than two. If your desired capacity is four, so it will try to maintain four, but in a scaling event, it cannot go less than two. Same for maximum, even the scale out happens, it cannot breach the maximum size. Okay, say create auto scaling group. Let's give this a name, vprofile-las-app-asg. Launch template. We already created the launch template, so select that. So again, in the scale out event, when the instance are launched, it is going to use this information to add the instances. All right, we'll select the security group key pair, all the things from the launch template information. Go next. Network, where do you really want to launch this instance? So there's the default, VPC default network will keep that as it is and the availability zone you can select, so I'm going to select all the availability zones. And if you wanna restrict it to some availability zone, you can select those zones only, and then go next. So load balancer, we want our instances to get added under the load balancer. So we have to say attach to an existing load balancer. We already have a load balancer. So if your classic load balancer, you can just select the classic load balancer, but if you have an application load balancer, which we created, then it will create or add it under the target group. So select the target group from there, we have our target group, select that. Now health checks. So load balancer, or our target group conducts the health check on the instances. We have that covered. We also want to do health check on the load balancer. So you can say turn on Elastic Load Balancing health check. Now the benefit of this is, load balancer conducts the health check on the specified port on the path. If there is some issue with the service, the instance will become unhealthy and then the auto-scaling group can remove that instance and create a new instance. But if you don't select that, then unhealthy and healthy will be only if the instance is down or up, but actually the service will not be checked. But if you select this, the health check, and the load balancer will conduct the health check on the instance. And even if the service goes down, it will declare the instance as unhealthy. So we don't want any unhealthy instance in our auto scaling group, right? So that's what this health check will do. And then go next. So group size, desired capacity. So I'll say just keep it one. You can increase to two also if you want, but I'm just keeping it one. Minimum one. So scale in till minimum these many instances. Maximum, I'll say four. Okay, so automatic scaling, how does it decide whether to scale out or scale in, right? That comes by default from target tracking scaling policies. Here, there are metrics, CPU utilization, network out, which is very popular for web applications. Any case, CPU utilization is the best bet and you can give a value. So if the CPU utilization is more than 50%, then it is going to add more instances. If it is less than 50% CPU utilization, it's going to scale in. Now this will be the average of all the instances. Okay, so based on the CPU utilization, you can scale out and scale in. You can also disable scale in. If you're not interested in scaling in, just scale out, do not scale in, you decide when to scale in, then you can put this check mark on. And then at the end over here, you have instance scale-in protection. Now this is different than what we have seen now. So what this does is if the instance is unhealthy, it is not going to remove it if you put a check mark here, it's not going to remove this instance. In turn, it is going to not divert any traffic to this instance. It will launch a new instance and this is when you really want to log into the instance and troubleshoot. You don't want the instance to get terminated automatically. Right, you want to find the root cause, why it is, you know, scaling, or you know, it's having any problem, why it's unhealthy. You want to check that, you want to log into the instance. So if you put this check mark, it won't delete the instance. In turn, it will not direct the traffic over there, launch a new instance and send the traffic to the new instance. Okay, I'm going to just uncheck this and go next. Okay, notification if you want any notification to be sent. All right, so if you followed the prerequisite lecture, we did set an SNS topic, an email address, and you can select that to send notification. You can say add notification, select here, SNS topic and you can send the events like launch, terminate, fail to launch or fail to terminate. You can select which event you want and then you can go next. So all these four events, I'll get the information. Go next, and say create auto scaling group. Okay, by the time this is happening, I want you to do one more thing. So if you have given more than one instance, then you need to do, this is specific for our vprofile application. Okay, go to target groups, go to select your target group, go to its attributes, click on edit, and here you have an option, stickiness. Turn on stickiness. Now what this does is now it will make sure if you reaches to let's say instance one, you know app01, it's going to make sure that you are always connecting to app01 again and again. And you can mention the stickiness duration also over here like one day or you know, there's other options also over here. So what happens is whenever you access a website, it takes a cookie and inject it in your browser. With the help of that cookie information, when you try to access that website again, you know in our case, you know, it's going to go to the same instance, let's say app01 or app02, wherever you're connected. Now why I say it's important for vprofile application, because our application cannot handle authentication like this. It's a pretty simple application. So if you're authenticated from app01, you'll be authenticated with app01. If you're routed to app02, you'll be logged out. Then you need to log in again. So in order to prevent that, make sure you turn on stickiness and save changes. Again, this is very specific to vprofile application. Some other application where you work may also have this one and you can turn on stickiness in that case. Okay, so auto scaling group launched one instance. Let's check in the target group, targets. Okay, so we have the instance in the target group vprofile app and it is in healthy state. Now I don't want vprofile app01, so I can just unselect that and I can see deregister and that's going to take some time to deregister that instance. And then we can delete that app01 instance 'cause I want the auto scaling group to maintain the number of instance for me. I don't want to add any instance manually there. So just remove app01 action. Oh sorry, instance state, terminate. Don't worry, we also have the AMI of the instance. So even if something goes wrong, we can launch the instance once again. Okay, so we need to wait for some time in order to deregister the instance and that's the only instance available or one or many instances are available in the target group. Okay, you see it just says draining in the target group. And so once it's completely gone from the target group, instance is deleted, you can once again check from the URL whether you are able to access the website or not. We'll validate all that and we'll summarize it in the last lecture. All right, so take some five, 10 minutes of break and then come back and join in the next lecture.
