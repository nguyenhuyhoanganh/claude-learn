# Load Balancer & DNS

Instructor: Okay, so all the services, we have installed and set them. Now before we create the load balancer, let's actually check whether the Tomcat instance is responding or not. So go to our app01 and we are going to access it on port 8080 from the browser. So first in the security group, we have to make sure we have the rules. So go to the app0 app security group and edit inbound rule. And we have 8080 from the load balancer security group. We are going to add one more rule, 8080 from my IP. Okay, this just will check whether the service is up and running or not. So add this rule and get the instance public IP, app01 public IP, and put it in the browser. The port number on which Tomcat runs is 8080. So give that port, and you should get the login page. Okay, but this is just a test. We really don't want to use it like this. You want to do it through load balancer. So you can close this for now and let's create the load balancer. So go to load balancing section. First we need to create a target group. So create a target group. So we're creating the target group of instances. So make sure instances is selected over there. Come down, let's give target group a name, vprofile-las-tg. Protocol port number, we have to give here 8080. Okay, the protocol is HTTP, but the port number of our instance, Tomcat runs on port 8080. We've just seen it. So make sure you give 8080 over there, scroll down. And in the health check also, we have to do here advanced health check settings and we have to override the HTTP protocol port number, 'cause by default it is going to take 80, we have to give 8080. Okay, so two places here, 8080 over there and in the health check also. Also there is one more place that will do it in the next screen. So click on next, find your app01 instance, put a check mark over there and say here, make sure that's 8080, and say include as pending below. Okay, the instance should come over here, app01, port 8080. If you see something else, then you remove that and add it again, app01 on port 8080. Say create target group. Okay, so target group is created. Now let's create the load balancer. So the load balancer is going to listen on port 80 and also on port 443. That will be a final, 443. But HTTPS connection, right? So HTTPS connection needs a certificate. We did create AWS ACM certificate, public certificate in the prerequisites lecture. We purchased a domain, we created AWS ACM certificate to match with that domain. If you have not done that, and if you want to do it, this is the time, but if you don't want to buy a domain and you just want to watch, you can create the load balancer, you can do the exercise, then you need to skip the 443 HTTPS part. Okay, so ACM certificate manager, I'll show you the certificate. So I have a certificate for this domain, hkhinfoteck.xyz. I did purchase the domain from GoDaddy. I've shown you that in the prerequisites lecture. If you want to do that now, you can buy the domain, create the certificate, and then continue with the lecture. If you still don't want to buy the domain, that is also fine. You can still do the lecture, except you cannot do the HTTPS connection part. Okay, so let me go to the load balancer back and say create load balancer. Application load balancer for HTTP and HTTPS traffic. That's what you want to do. Click on create there and give load balancer a name, vprofile-las-elb. Scrolling down, network mapping VPC and select all the availability zones that you see over there. Okay, coming down. By default it will be in the default security group, so remove that and select our load balancer security group that we created in the security group lecture. Listeners and routing here. So it's going to listen on port 80 and route the request to the target group that we created. Now we need HTTPS connection. Now if you don't want to have the HTTPS connection, you can just move on, but if you want to have, you can say add listener tag. I'm sorry, my bad. It's add listener, this one, click on this and select here, HTTPS. It's going to by default, take the port number and select the target group. And when you do this, you need to select the certificate from the ACM. Okay, so if you scroll down the security policies, sorry, here, default SSL and TLS certificate. From ACM, drop down and select here, certificate. So mine is hkhinfo.xyz, this one. Okay, this you'll only get if you select here HTTPS. Okay, now over here get few more options like AWS Web Application Firewall, WAF, Global Accelerator. We can skip all this part and come down and say create load balancer. Okay, the load balancer is created. Let's click here on the load balancer. And this is gonna take some time to provision. And by the time we can go to this load balancer URL and add it in our domain record. Mine is in GoDaddy, so I'm going to log into my GoDaddy account. Okay, so this is only for people who bought the domain either from GoDaddy or anywhere else. If you have not done it, you can skip this part. If you have that, then go to your GoDaddy account. Go to my products. Okay, scroll down here. Okay, that's my domain. I'm going to say go to DNS. Again, here we are going to create the CNAME record. So click on add new record, choose type CNAME. So name of my website, I'm giving vprofileapp. So URL will be like this, vprofileapp.hkhinfotech.xyz. You can choose something else if you want. And in the value, we are going to give the load balancer DNS name, load balancer endpoint. So paste the load balancer endpoint over here, DNS name and say save. Okay, that's done. Now people who do not have the domain, they can just access the website by taking the load balancer endpoint, right? Copy it and put it in the browser. It will work out, but you're not going to get any HTTPS connection. Okay, it says active load balancer is active. Let's also check the target group if the instance are healthy or not. So go to the target group, open your target group and go to health checks. Nope, targets, refresh this. Okay, mine says status as healthy. For some reason, it is showing unhealthy for you, make sure the Tomcat service is running on the app01 instance, check that. Next thing you check the security group of the Tomcat instance. Does it allow port 8080 from the load balancer or not? If that is also correct, then check the health checks. That should be port 8080 over here. If all these three things are working, maximum chance, it should show healthy. You know, make sure you refresh and check again. It takes time. It performs few health check before it declares it as healthy. So if it's unhealthy, you're going to check the Tomcat service. You are going to check security group allows from the load balancer or not. And then you do the health checks, you check the port number is 8080 or not. Okay, let's come to load balancer and check it from the URL of the load balancer. Maximum chance it is going to show up. This is going on port 80 HTTP connection. It's working fine. You'll see here it says not secure because we are not using HTTPS connection. And again, this is a wrong domain. I mean not for our HTTPS connection. So I'm going to open a separate tab and I'm going to do now https://vprofileapp.hkhinfoteck.xyz HTTPS connection, hit Enter. Okay, if you check here, vprofileapp.hkhinfoteck.xyz, it shows all valid certificate. You can click over here and you can see, it says connection is secure. You click on that and you can see certificate is valid. If you click on that, you should see the certificate given for hkhinfoteck.xyz given by Amazon. Amazon gives the detail of the certificate. That is coming, all information is coming from the certificate manager. Okay, this certificate, and we have applied this certificate on the load balancer. If we go to load balancer, there, HTTPS-443, if you click on that, go to certificates, you should see a certificate over there. Okay, now let's log in and find out the other details, like DB connection. So log in with the username, admin_vp, same password. If you're able to log in, the DB connection is verified because the user information is stored in the database. So if you are able to log in, the DB connection is working. Check RabbitMQ, queue is generated. So RabbitMQ connection is also working. Click on all users to check Memcache. Select any user. You see it says data is from DB and data is inserted in cache. Go back. Click on the same user once again and you should see the message data is from cache. So all the services, everything is verified. So everything is good, but we also want our instances to scale out automatically when the load increases, right? When there are more users connecting to our website, we want it to automatically scale out. So in the next lecture, we are going to see autoscaling group, how we can do it for our project. So wrap this up and join me in the next lecture.
