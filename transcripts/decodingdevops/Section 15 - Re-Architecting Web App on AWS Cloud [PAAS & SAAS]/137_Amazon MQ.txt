# Amazon MQ

Okay, so by now, we have created Amazon RDS MySQL instance, ElastiCache for Memcached. We need one more, RabbitMQ, and for that, we are going to use Amazon MQ, so let's go ahead and go to Amazon MQ service and create our RabbitMQ instance. So, here, you can search for Amazon MQ or just RabbitMQ. You will see here Amazon MQ. Just make sure it's Amazon MQ and not Amazon Q. That is a different AI tool. So, Amazon MQ is fully managed service. You don't need to create subnet groups and all those things. Just click on Create brokers, Get started. Make sure you select RabbitMQ and go Next. There is Cluster deployment option, but we are going to go with single instance only because this is for learning purpose. In real time, you might need a cluster deployment for production workloads, but here, just click on Single-instance broker. Go Next. Broker name, I'm keeping vprofile-rearch-rabbitmq. And broker instance type, now here, we do not have the 1GB option. The minimum is one CPU and 4GB, so select that, and don't worry, we're going to anyways delete it once we are done. For username, I have given rabbit. For password, I have given BlueBunny980. So, whatever password you're giving over here, make sure you save it somewhere. I'm keeping it in my sticky note. Don't need to give the same password. You can give any 12 character. Mix it with name and number and make sure there is a capital letter. Open Additional settings, Broker engine version. Select the engine version as 3.13. Don't go with 4.2, just go with three dot. Any three is fine. This is more a stable, more matured engine version and it works with vprofile application also. Scroll down. Monitoring CloudWatch logs. In real time, you will need logs, so make sure in real time, you put this check mark on. Access type, Private access. Our vprofile application is going to internally access the RabbitMQ service within AWS internal network, the default VPC, so use case is definitely Private access, so make sure you select that. Scroll down. So, as I already told you, we just have default VPC on default subnet, so make sure this is selected, use default VPC and subnet. Security group, select existing security group, and select vprofile-backend-sg. Data encryption, now, there's no option to disable encryption over here, and this is fine. Our vprofile source code, which is an awsrefactor branch, is customized to encrypt the traffic for RabbitMQ, so this will work. Maintenance, again, no preferences. Add new tag, just give a name tag here, vprofile-rearch-rabbitmq, and say Next. Just go through all the settings, check the RabbitMQ engine version, check the password, make sure you save your password. Scroll down and say Create broker. Now, this is also going to take some time, so we have all our three backend services created, but there's one thing that we need to do in the RDS instance, in our MySQL instance. We need to initialize the database with our SQL file, db_backup.sql file. That's what we're going to do in the next lecture, so wrap this up and join me in the next one.
