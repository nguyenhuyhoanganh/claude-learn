# DB Initialization

Okay, so in this lecture, we are going to initialize our database. That means we are going to use schema of vprofile application and deploy it in this RDS instance. So let's open this RDS instance. And that is the endpoint, this one, from vprofile till here, .com. Or you can just click here on the endpoints. Just just find the endpoint. That's the endpoint. And this is port 3306. Now this is private RDS instance, we cannot access it through the internet. It can be accessed only within the VPC, so we are going to launch an EC2 instance in this region, North Virginia region. So this region has one default VPC, which is this one. So when we launch the instance, by default, it gets inside this VPC. We will SSH to our instance, and from there, we can access this RDS instance. So if we can access, we can also deploy our schema. So copy this endpoint. So I'm gonna just paste it in the sticky note. So that's the endpoint. This is the username, this is the password. So we have all the details. If you click on code snippets, this is the command like this, we are going to execute the command in our RDS, sorry, in our EC2 instance. So let's launch an EC2 instance, install MySQL client in that. And like this, we are going to access our RDS instance. So go to EC2. So I'll launch an Ubuntu instance, install MySQL and access the RDS database from there. I'll just simply call it as mysql-client. This is a temporary instance. We are going to delete it once we initialize our database. I'll keep as Ubuntu, Ubuntu Server 24.0 is fine, t2.micro. Key pair, we'll select the key pair that we created earlier, or you can just create a new key pair for this one. Security group, let's go edit and create a new security group for this, vpro-mysql-client-sg, and we'll give rule 22 from my IP, so we can SSH to this instance. And that is all, just launch instance. Let's get its details. So that's the public IP. Let's do SSH. So open your terminal, ssh -i, part of your key, username ubuntu@ and the IP address. Yes. Let me clear the screen. Let's become the root user. So to connect to database from here, we need MySQL client, and the DB schema is in our source code. So we need Git command to clone the source code. So we need to install that. So let's say, apt update &amp;&amp; and then run the command apt install mysql-client git -y. So it's going to run the update command and install our tools. Okay, it's installed. Now we need two things. We need the database details, RDS endpoint, username, password. So RDS information we need, that's one. Second, we need to make sure we allow this instance security group to talk to RDS security group. So you can access the RDS security group from here itself. So in the EC2, we go to security groups. We have the backend security group. In this, we have RDS. And this is the security group that we just created for EC2 instance. So first we'll take the security group ID... Security group ID of the client security group, then we'll go to the backend security group, inbound rules. Again, inbound rules. Be careful, not outbound. Edit inbound rules. See add rule. We'll select here MySQL, which is port 3306 allowed from the security group of MySQL client, so instance can talk to RDS. Say save rules. Now let's get database details. Let's get the endpoint of RDS. And in the sticky note, I have already saved the username and password. First, let's try to log in. So we'll say, mysql -h and the endpoint, space -u for the user name admin -p and the password. So you have already seen my password, so I'm putting that in the command line, but I said this in the previous project, also, never put password like this in the command line. Just use -p, hit Enter, it'll ask password for you. All right, this is just for convenience. Database name accounts, hit Enter, and we should be able to log in. Okay, now exit from here. Clone the source code. Okay, so from your browser you can open github.com/hkhcoder/vprofile-project. From the dropdown, just copy the HTTPS link, go back to your terminal and you can say git clone. So you should have the source code. You can go to the source code directory. Shift to the branch awsrefactor. Okay? And this we have in src/main/resources/. We have our db_backup file. We need to deploy this schema. So use the up arrow and go back to your MySQL login command. Make sure the accounts database is selected there. Input redirection and path of your SQL file, src/main/resources/db_backup.sql, okay? You have to make sure you are in this folder, otherwise you can give absolute path of the file. Hit Enter. Now up arrow once again, remove this. Keep it till accounts, hit Enter. You should be logged in. Say show tables and let's check. So we have the tables created. Our database is initialized. It's ready to use for our vprofile project. And now you can delete this instance. If you're not planning to use it for anything else, you can delete the instance. Go back to instances, select your instance. Instance state, terminate. So the whole purpose of using this instance was to just initialize the database. And since the database instance is private, we need to do it through another EC2 instance in the same VPC, the same network. Okay, now we can see our backend is ready. In the next lecture, we will create the Beanstalk instance, where we are going to host our vprofile application. So join me in the next lecture.
