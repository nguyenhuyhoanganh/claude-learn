# Security Group And Keypairs

Instructor: So in our application stack, we have Elastic Beanstalk, which will create EC2 instances where we can host our vprofile application. For this EC2 instance, Elastic Beanstalk will create a security group. Beanstalk will also have one more security group that will be for the load balancer that it creates. And there will be automatically rules set by Beanstalk to allow traffic from load balancer security group to application security group. Now the backend services, we have Amazon MQ, RDS, and ElastiCache. For these backend services, we will create a security group, we'll call it as backend security group. And when we create these services, we will make sure that they all come in the same backend security group. And we also need to add one rule in the backend security group that says, allow all traffic within itself so these services can interact with each other. And later when we create Beanstalk, and the Beanstalk security group is created that time, we need to update the rules in the security group of backend saying, traffic from Beanstalk security group is allowed to interact with these services. So that's what we'll do in this lecture. We'll create backend security group and we'll also create a key pair, login key pair for the Beanstalk EC2 instance. So I'm in my AWS management console, I'm in North Virginia region. I will go to EC2 service to create the security group. Let me go to security groups and say create security group. Let's give security group a name. So vprofile backend security group because this is for backend. I'm giving the name vprofile-rearch as in rearchitecture backend security group. So we'll know the security group is for a rearchitecturing project. And you don't need to add any rules over here. Just keep it as it is. Make sure outbound rules are like this and just say create security group. Yeah, once the security group is created, then you go to edit inbound rules, say add rule, and you have to give here all traffic there. All traffic allowed within itself. So its own security group ID you have to give in the source. And that's why we first created the security group without any rule. So after creating the security group, we will get its security group ID. So this rule we are adding, so all the backend services, ElastiCache, RabbitMQ, RDS, they can all interact with each other. Once you add this rule, give some description and you can say, save rules. And that's all, this is the only rule that we are going to add now. Later when we create Beanstalk, when we get the Beanstalk security group ID, that time we'll edit the security group again. That is in later lecture. Now let's go and create the key pair. So let's go to key pairs. Now this key pair we are creating, so we can log into Beanstalk EC2 instance. It's not mandatory to log in. Everything will be managed by Beanstalk. But in case where we need to do troubleshooting, that time, we need to log into the instance to see what's happening inside. So let's say create key pair. Let's give it a name, vprofile-rearch-key. And create key pair. That is all in this lecture. In the next lecture we will see how to create RDS instance. So wrap this up and join me in the next one.
