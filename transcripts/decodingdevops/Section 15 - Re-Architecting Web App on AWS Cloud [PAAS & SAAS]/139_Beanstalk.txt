# Beanstalk

Hello and welcome. In this lecture we are going to set up Elastic Beanstalk instance. Our environment. Before we do this, I want you to recall the previous project where we set up Tomcat on an EC2 instance. We did so many things we created. First the security group key pair. We launched the instance and we installed Tomcat. On that we created target group Load balancer. And when everything was good, we created an Ami launch template and created an auto scaling group. So so many things we did. And then we want to deploy the artifact. We built the artifact locally on our system and through S3 bucket we deployed it on Tomcat. Beanstalk gives you all this as a single suit. So when you create a beanstalk environment, you select Tomcat. It gives you everything. Auto scaling group ami S3 bucket for artifact, CloudWatch, monitoring logs, many other things, and it's very easy to tweak its settings whenever we need and super easy to do the deployment. As it says, it's an easy to use service for deploying and scaling web applications and services deployed with Java DotNet, PHP, Node.js, Python, Ruby, go, and Docker in later project. When we do Docker images, when we transform, when we transform our profile application into Docker that time, this Docker concept will make sense to you. You can also run your Docker images on Beanstalk. And the best part is there is no additional charge for Elastic Beanstalk. It charges you only or you have to pay only for the service that Beanstalk gives you. Like EC2 volumes, whatever. But as a whole automation suit, it doesn't charge you for anything. So we'll create this Beanstalk environment now, but before that we are going to create some IAM roles for Beanstalk. So search for IAM, go to roles. So Beanstalk creates a role. You can select an option or you can select an existing role. But that role lacks some permission which will be a problem for our project. So we don't want to use that role, or we don't want Beanstalk to create a role. We will create a role and select when we create Beanstalk instance, create role AWS service. You can use select the use case as EC2 and go next. Now in the permission search for the word bean, you need to select these four policies. Administrator access AWS Elastic Beanstalk AWS Elastic Beanstalk Custom Role platform for EC2 role. AWS Elastic Beanstalk role. SNS for notifications AWS Elastic Beanstalk web tier. So these four roles you need to. Sorry four policies you need to select. And then you can go next. Let's give it a name V profile Rearch been role. Keep same in the description and say create role. Remember the name of the role because we need to select it in the beanstalk. So now come to Amazon Elastic Beanstalk and say create application. So environment here web server environment. We will be using Tomcat. Our application will be running on that definitely a web environment or a website. Give application a name. Come down environment name. So in Beanstalk when we say environment that actually means the different kind of environment a project can have like dev test, staging, production like that. And this environment basically consists of all these services auto scaling group, EC2 instance, S3 bucket, everything. So in one Beanstalk application you can have multiple environments dev, staging, production like that. Okay. Next the domain it will give you a URL to access your application. So let me give a name here I'll say Pro re arch. You cannot give the same name. You have to give a different name because this needs to be unique. Check availability. It says this is available. So make sure whatever name you give you check availability. Then that will be the URL to access your application okay. Platform platform. Definitely we need Tomcat. So select Tomcat there and platform branch. So we will be using Tomcat ten and Corretto 1721 Also works for Project Keep 21, so corridor 21 means Java 21 with Tomcat ten on top of it. Platform version. You can keep the recommended over here. Application code first, sample application later. We're going to upload our app, our artifact over there, then presets the configuration. So we'll go for custom and we'll make the changes. You can have single instance but definitely we will not be using single instance. We'll have auto scaling group load balancer everything. So we'll go for custom configuration go next. Configure service access. Here we need to select the role the service role and the instance profile role that we have created. If you do not see here the AWS Elastic Beanstalk service role, then you can create it with this button. I'm going to show you how to do that. First let me delete this service role. So I go to IAM roles and I'm deleting. You don't need to do this if you already have the role. I'm just showing you if the role is missing, how can you create it? So I'm deleting this role and I'm going to click on here refresh. You see I do not see any role over here. The drop down is not there. So I'm going to click on Create Role here. Go next next. So it has selected the policies automatically and it has given the name as well. Just click on Create Role. And then when you come back refresh this and you should see your role. Service role created over here in instance profile. Select the role that we have created previously and in EC2 key pair select your key pair. Make sure you select all these options and then go next VPC. We just have one default VPC. We are going to select that one instance setting. Make sure you select here public IP address activated. So the EC2 instance that gets created in Beanstalk will have a public IP instance. Subnets. You can select all of it, and this might give me error when I go next. Let's see if we get that error. That's some zone is not available or something like that. We can uncheck that zone. But for now let's just select all the zones database. So we already created our RDS database instance. Beanstalk also gives you that option. When you create Beanstalk you can create RDS instance with that. But that will be coupled. I mean you can decouple it in previous time as in not very previous very recently means that gives you option to decouple the RDS instance. But from the beginning what we did, we created RDS instance separately. So it's decoupled with Beanstalk. So we can manage both separately. So not select anything for the database. In Beanstalk you already have it. Let's just scroll down and let's go next. Oh wait uh add new tag. We'll just say project is view profile. Go. Next we will change the root volume from container default to general purpose three GP3. There are some recent issues with Beanstack. It is trying to use launch configuration which is outdated, and you will get an error that Beanstack should be using launch template. So if you use GP3 it will use launch template. But if you select here container default it will use launch configuration which is outdated. So make sure you select GP3 over here. Scroll down CloudWatch monitoring interval five minutes is fine. You can also have every minute monitoring but that will be not free. Scroll down okay. Security group. Now we want Beanstack to create its own security group. And then we can edit its rules. So keep that as it is. Capacity now here. So we want multiple instances load balanced. And we select that. It will create a load balancer application load balancer. And we need minimum two instances and maximum four. Again I'm just giving a random number. You need to select this based on your project requirement. And remember you can anytime change these things. Maximum number minimum number. You can anytime change. So starting with minimum two because load balance we need minimum two. And I want to show you how the deployment happens when I upload the artifact, how it's like one at a time. Or we can select those options. So for that reason I'm keeping minimum two instances over here. Okay. Coming down. Okay. Instance type T2 small. So that won't be free right. So we're going to select here T2 micro okay. So from the list find T2 micro or T3 micro also is fine. Just go with T2 micro. Scaling triggers. This is for the auto scaling group. And let's see the metrics. So your CPU utilization you have network out. These two are the most used one CPU utilization and network out for web application I'm saying Thing you know. Network out simply means, you know, it's serving the pages out. You know, when more users, that means more network out will be there. You know, server will respond more. So it's a very good trigger to scale out or scale in, right? Whenever the network out is increasing, that means more users are coming. You can see the auto scaling group can add more instances. When it's reducing network out is reducing, then it is going to delete the instances. So it's a very, very much apt trigger, you know, to scale out or scale in for web application. Let's come down load balancer visibility public. Definitely. We are hosting it for the internet for the public. So we'll keep the visibility as public and all the zones are selected over here. Load balancer type application load balancer. We need network load balancer is high performing. It gives you static IP. It's really amazing but it's expensive. So we'll keep application load balancer. It's apt for HTTP and https traffic listeners. So from previous project we remember Tomcat runs on port 8080 but not in Beanstalk. Beanstalk it runs on port 80. Load balancer port is also 80. So it takes the request. This is the load balancer port listener port so it listens on port 80 and routes the back request to port 80 only on EC2 instance and we'll keep it as it is later we're going to change it to Https connection, but for now let's keep it 80 okay. For processes, um, you have to make one change over here. This is the health check. Select that action edit. And you see here sessions in this enable stickiness. In previous project you remember we created a target group where we add the instance. The target group will conduct the health check all those things. This is the same settings. And there I talked about the stickiness. Our view profile application needs stickiness. If the user connects from load balancer to a particular instance, that user needs to be connected to that instance only if the load balancer throws the user to another instance, the user needs to again login. Now that's the problem with our view profile application, but also in many other applications. If you're working in some project, that may be the case. So if you want that you enable stickiness. So user is connected to that particular instance. Now otherwise you log in you refresh the page again it will log you out. Then again you need to log in. So keep stickiness for our project. It is important. And save it. Okay. Other things you can keep default and just go next monitoring health reporting. We need to keep here enhanced. This is very useful when we do deployments. And we have multiple instances in the auto scaling group. It will check the health of the instance. If it is good, it is going to do the deployment on the next instance. So in such cases these metrics will come handy for the deployment purpose. So keep it enhanced and come down. Update. Let's keep it as it is minor and patch. Again it depends. For your project you want to do automatic updates or not. These are minor patch update like tomcat new version. Something changed those kind of updates. Email notification you can give an email address. So in an event of some changes happening on your environment on your application it will send you notifications. Okay, now rolling update and deployment is the best part in Beanstalk. And very interesting topic for DevOps engineers or even architects. So we have the environment. How do we do deployment when we deploy our artifact a new application code, then do we bring down the entire application stack or we want user to be connected and do a rolling deployment. Right. Like one instance get upgraded, then the other instance, then the other instance. So you can select all these amazing options over here. So by default it says all at once. So let's say you have ten instances. You're uploading your new artifact. It's going to bring down all the ten instances, update it and then bring it back to the load balancer. That is definite downtime. This is the fastest one cheapest one. But there is a downtime. So let's look at the other policies rolling in rolling you can mention the percentage and you can or you can mention the fixed number of instances. Let's say you have ten instances. You will see one instance at a time or two instance at a time, three instances at a time. Or you can give a percentage. You say, you know, you have ten instances and you say, you know, I give you like, you know, 20%, right? Right. Yeah. Down 20%. Right. So what does that mean? Two instances at a time like that, you cannot afford to give 50%. Reducing your application capacity to 50% is very bad. User will feel that lag and your application may even hang halt, right? So 10% 20% is, you know, fair enough. But what if you really don't want any kind of lag for the user, right. Then you can select rolling with additional batch size. What this will do is let's say you have ten instances. You selected percentage capacity 20. So it's going to add two new instances upgraded. Then remove the two old instances right. So at the time of the upgrade you will have more instances not less. When the upgrade is finished, then it will be back to the same amount of instances. That is ten. If you selected that yeah I would say this is really good and safe but comes with additional cost. But what if something goes wrong at the time of upgrade, right. If you don't want any kind of issue, then you can go with immutable. The safest one, but also expensive. What it will do. So let's say you have again ten instances. So it's going to create ten new instances upgraded. When everything is good it is going to route the traffic to ten new instances and then delete the old ten instances. Safest one but expensive. Okay, so the next one is traffic splitting. And I want you to read this one. Okay. In Beanstack that's the last policy traffic splitting. And you can mention how much of the traffic will be split like in the percentage, let's say 10% or 20% like that. So what does that mean? This is also called as canary testing. It's also one of the interview question. So when you select that and you select, you know 10%. So what happens in that case Beanstack. Let's say your ten instance again. Okay. You say 10%. So it's going to launch one new instance and redirect 10% of your traffic to the new instance. And that's why it says you direct some incoming client traffic to your new application version to verify applications health. You need to be sure whether it's the new version is working or not as per the user need. Right? If everything is good, it's going to again do 10% at a time or whatever percentage you have mentioned. Now I want you to read this documentation. You can just search Beanstack deployment policy on Google and you can come to this documentation. Read this. This is very useful for you. And you want to really make decision in the project deployment and also for the interview purpose. Okay. Coming back now we are not going to do this one. We will be just doing rolling and we'll mention the batch size as 50%. So we have two instance minimum. So one instance at a time if it scales out we'll have maximum for instance. So 50% means two instance at a time. Okay. To test the other policies you need to have more number of instances. Okay then scroll down. Come till the end is one option over here. Might be interested or useful for any project. Some project require you to set some variables like database variable name, port number or any configuration like that. Need to set you variables. How do you do that? You can do it through environment properties. You don't need anything in our project so we'll just go next okay. Review it. Make sure whatever we discussed you selected everything properly instance profile roll network security group right number of instances review everything and if you're good then click on submit. Okay. It's going to take some time to launch this Beanstalk environment. After this is ready we'll take a look at it. If everything is good then we can build the artifact and upload it to this beanstalk environment. All that we'll see in the next lecture. So wrap this up and join me in the next one.
