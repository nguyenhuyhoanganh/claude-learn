# Elastic Cache

Okay, so our V profile application also needs cache, Memcached, which will cache DB query and we are going to use Amazon ElastiCache service for that. So let's go ahead and create Amazon ElastiCache. So search for ElastiCache in AWS console. Let's open it in a new tab. So we are going to create Memcached. There are two more engine here, I mean caches, Valkey and Redis. Also, we are going to create Memcached, but before that, we are going to create parameter group and subnet group same like RDS. So click on Parameter group, click on Create parameter group. Let's give parameter group a name, vprofile-rearch-cache-paragrp. Same I'm giving in the description. So give something like that a proper name so you can identify it easily. Family, you are going to select memcached1.6. 1.6. Once you're done, click on Create. You should see here your parameter group and you should see many options over here, which you can change as per the requirement comes. Now let's also create a subnet group. So go to Subnet groups, click on Create subnet group, give subnet group a name. So I'm giving vprofile-rearch-cache-subgrp. Same I'm giving in the description. As you know, we just have one VPC, the default VPC. So make sure you select that one. And you see here the subnets are already selected, selected subnets, but if you want to unselect or select something else, you can click on Manage and do that. Let's keep default because we have default VPC and default subnet in that. Let's keep it as it is. Now let's go back to Memcached caches. You can click here Memcached caches and say Create Memcached cache. Make sure Memcached is selected over here. Deployment option, you have Serverless where you just give the name. And there are Default settings and it will just create, that's it, as simple as it is. But I want to show you more options, so I'm going to click on Node-based cluster. There's an option of Cluster cache, so select that. Easy create will just select the default options, the recommended best practices, but click on Cluster cache so we can select all the options. Location, AWS Cloud, but you can manage an on-premise ElastiCache or Memcached from here. Keep it AWS Cloud. Cluster name, so I'm giving vprofile-rearch-cache. Engine version, you can keep here 1.6.22. Anything 1.6 is fine, just keep it 1.6.22. Port number, check here, 11211. If you remember the application properties file of our V profile application, there we have the default port of Memcached, which is 11211. Let's take a look over there. So I'm in the GitHub repository hkhcoder. I'm in the branch awsrefactor, src, main, resources, application.properties file. Now this file or application will use to connect to the backend services, MySQL here, and then Memcached. Check here, the database port 3306, we kept default in RDS. In Memcached, 11211. For RabbitMQ, it's 5671 in this branch, in refactor branch, 5671. So for Memcached, we are keeping the port 11211, which is same in the configuration file. Parameter group, select your parameter group. Node type, find the smallest one. So just type t3 and you will find here t3.micro, half a GB of RAM, which is fine. Number of nodes, single node, one. Connectivity, choose the existing subnet group. So select your subnet group, which is the collection of all these subnets. Availability Zone placements, so say no preferences 'cause anyways, we just have single instance, so it really doesn't matter. Let's click on Next. Now over here, make sure you uncheck here Encryption in transit. Our V profile application doesn't have encryption in transit for Memcached by default. So make sure you uncheck that. Click here Manage in the security groups and select vprofile-backend-sg, Choose. Maintenance window, no preference is fine. Click on Add new tag, let's give here name, and give the same name vprofile-rearch-cache, and click on Next. Take a look at all the options. The engine version 1.6.22, port number, and more importantly, the node type, t3.micro. If you made any mistake, just click on Edit and revert the changes. Once you're good, click on Create. Now this is going to take some time to create. By the time, let's go to the next lecture and see how we can create RabbitMQ by using Amazon Q.
