# Build & Deploy Artifact

Instructor: All right, so it's time to deploy the artifact, it's pretty simple. You can just click here, Upload and deploy. And you can say, Choose file, select the artifact, version it, and deploy it. But we need to build our artifact and we also need to make sure our code contains the backend service information. So first I'll collect the backend service information like the RDS endpoint. I'll keep it in my sticky notes. So, I already did save the username and password for this. Next, I'll go to Amazon MQ, select our broker. Come down, you have here Connections. Over here, you have this URL. After the slash slash, copy that, and put it in the sticky note. Now observe the port number, it's 5671. In application properties file, we have port number as 5672. So we need to also edit the port number of RabbitMQ in the configuration file, right? We'll take this port number as it is. Let's check ElastiCache. Okay, in the Resources, Memcached caches, select that. And that's the configuration endpoint. Copy that, again, I'm saving it in the sticky notes. We also need the username and password for RabbitMQ. While creating, I created a user called rabbit, and my password was this, right? So make sure you collect all the backend service information. Let's make sure this 11211 is here. Okay, now let's clone the source code in our local machine, build it, and then upload it. So go to github.com/hkhcoder/vprofile-project and copy the URL, HTTPS. I'm going to put it in my F drive, hkhcoder folder I have, and in that, I already have a repository, but I'm going to delete this one. It's the old one. I will open Visual Studio Code. Okay, click on this button over here, Source Control. Clone Repository. Paste the repository, hit Enter. Select the destination. I'm keeping it in my F drive, hkhcoder folder, Open. Okay, very important. Switch to the branch awsrefactor. So click on this branch over here and select awsrefactor. All small, this branch exactly. Okay, let's update the application properties file. So go to src, main, resources, application.properties, right? So here instead of db01, we'll give the DB endpoint, RDS endpoint. Be careful when you're pasting this one. It's mysql:// and the endpoint, :3306. Username is admin. The password is different. That's the password. Okay, let's get the Memcached endpoint. Yup, that one. Okay, make sure this is the Memcached endpoint, and that's the RDS endpoint. Port 11211, that is same. So we don't need to make any change there. RabbitMQ. So let me copy the RabbitMQ endpoint. All right, so replace rmq01 with the RabbitMQ endpoint, and port number is 5671. So make sure you change that. Username we have given, I mean, I have given rabbit, and the password. Check it. Once again, be careful. If you make mistake over here, then our application won't be able to connect to backend services. In that case, you can make the changes again, fix it, and then build the artifact again, and again, you have to deploy it. Okay, so save this, Control + S, or Control + S like this. Okay, now we can build the source code. So just hit Control + Shift + P, and type, Control + Shift + P, and type default profile. Right, so select default profile. So Terminal: Select Default Profile, select that. And select your default terminal. I'm using Git Bash. If you're using MacBook, then you will select here, just the Terminal. So I'm selecting Git Bash. And then View, and I'll say Terminal. So it opens the Git Bash terminal over here itself. Should be the path of your repository, the branch, awsrefactor. Run the command mvn -version to see what's the Maven version. It should be Maven 3.9.9, and Java version should be 17 or higher. If you see different version, make sure you uninstall it. If you're using Windows, you can use Chocolatey command, choco uninstall, and then install the right version. Check the software installation lecture for the right command to install right version of Java and Maven. For MacBook, you can use Brew. Again, check the software installation lecture for this. It's in the prerequisite, but if you have the right version, all you need to do is run mvn install. Hit Enter. Okay, so the artifact should be created. If the build is success, you should see a target folder here, and you should see the artifact, vprofile-v2.war. Now we can upload this artifact to our Beanstalk environment. So go to Beanstalk, make sure you're in your Beanstalk environment. You'll get an option here, Upload and deploy. Click on that. Say Choose file. Go to the location, mine is hkhcoder, vprofile-project, and target, vprofile-v2.war, select that. And let's give some version over here. I will say vprofile-rearch-beanapp-version-1.9, just some random number, I'm giving. Deployment preference, so we have rolling. And batch size is 50%. If you want, at the time of deployment, also you can change the policy, but we'll keep it as it is, and we'll say Deploy. Okay, this will take some time. It's going to upload the artifact first and then start the deployment. Okay, it started the deployment. Let's look in the Events. Nothing in the Events for now. Elastic Beanstalk is updating your environment. Soon you should see events appearing over here about the deployment. Okay, there, application update progress. So we have two instances, right? Deployment policy said 50%. So one instance will be upgraded at a time. Okay, this is gonna take some time. I'll pause the lecture and resume it to show you the events. Okay, anyways here. Batch 1, starting application deployment. It's also showing the instance ID. Okay, so once the batch 1 is completed, you know, how will it know, right? So when it deploys to one instance, let me go to, so we have two instances, right, two instances. So in the target group, right? Go to Target Groups, you should see the target group. And if you see here, we have two instances, yeah. I'll refresh this, both are showing healthy now. But as soon as the deployment happens, one instance will become unhealthy. When the deployment completes, the instance will become healthy. When the instance become healthy, then it will move to the next instance. Okay, you see? It says draining. All right, if you keep refreshing, you can see that. And this should match with the event. Okay, it's already started application deployment on the second instance. Okay, so we'll wait for some time. I'm going to pause my lecture and resume when the deployment is completed. Okay, so the application update completed. It says 46 seconds ago. Okay, that's nice. So the health should be okay. And this should be the URL. If you click on this, we should get the login page. Nice. But you see here it says not secure. We want HTTPS secure connection. And as we did in the previous project, we need HTTPS 443 and we need the ACM certificate. So if you have not purchased the domain, and if you don't want to get it, you can go ahead and perform the test, log into the account, admin_vpn, admin_vpn, password, and verify everything. If you have the domain and you have the ACM certificate that we did previously, then we can add that connection. So follow me. So first we need to add the ACM certificate. So we'll go to Configuration. If we scroll down, we should see Instance traffic and scaling options here. Let's edit this one, and scroll down. We need to find the load balancer listeners, coming down. Load balancer settings. Okay, listener there. So we can say here, Add listener, we'll select the protocol to HTTPS, and it should select the port number. It's not, okay, let me type 443. Okay, in the SSL certificate, so I'll be using this one, hkhinfoteck.xyz. SSL policy. Yeah, that one, 2021-06. And just save it. And the saving is not enough. We have to apply the changes, click on Apply. Okay? And that is gonna take some time. It's going to make the changes in the target group. Load balancer also, it will add the listeners. By the time, we'll go to the GoDaddy account and add the entries. So copy this URL. Go to GoDaddy. GoDaddy or whatever domain registrar, you have taken your domain from, go to the domain and manage DNS. See? Add New Record. And here the type will be CNAME. We'll be mapping a name to a name. So name, I'm giving vprorearch. Can give any name you want. And the value will be the endpoint, all right, our Beanstalk endpoint, that one. And say Save. So our URL will become vprorearch, dot, the domain name, right? So we need to wait for some time when by the time this URL is published. And then we can access it on HTTPS secure protocol. Okay, so after waiting for some time, https://, the name that I have given, and dot, the domain, right? So mine is vprorearch.hkhinfoteck.xyz. And hit Enter. And there, okay, secure connection. If you click on this, you should see connection is secure. You can see the certificate is valid. The certificate is for this one. Okay, now let's log in to check the other things. We'll log in with the username admin_vp, and the password, admin_vp. So if this works, the database connectivity will be verified. And yes, that is working. Okay, we can see the page, can check the page. And you can also verify the backend services. Let's check Memcached. And username, you see? Data is inserted in cache. Go back. All right, so the application is verified. If you have come this far, it's really great job. You have understood how to use cloud managed services in an application. There are many other cloud managed services that you can use in the project that you're working. This project set would have given you the confidence that you need to use this cloud managed services. Really amazing job. In the next lecture, we'll see how to use CloudFront to distribute your traffic around the world, so you can have global audience. Does not matter where you host your servers, in which region, if you're global audience, you can use CloudFront service to deliver your services around the world. That will be all in this lecture. I will see you in the next one.
