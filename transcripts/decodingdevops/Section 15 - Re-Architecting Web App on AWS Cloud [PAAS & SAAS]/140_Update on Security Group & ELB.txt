# Update on Security Group & ELB

Instructor: Okay, our Beanstalk environment is up and running. If you click on this URL, we should see the default application hosted by Beanstalk. That will be changed by our Vprofile application in the next lecture. Okay, so by now we have RDS, we have Amazon MQ, we have Memcached, and we have Beanstalk environment. A Vprofile application living in the Beanstalk environment needs to connect to backend services, and they are in a security group. Vprofile backend security group that we created. We need to make sure the backend security group rule allows connection from the Beanstalk security group. So do that in this lecture. Okay, so let's take a look in the instances section. Okay, so we should have two instances running from Beanstalk. We should have one auto-scaling group that created these instances. If we go to the instances, if we select any of the instances, Security, we see here the security group. Let's get the security group ID. Let's click on it, and copy the security group ID. Make sure this is not the load balancer security group. If you go to Security Groups, you should see here instance security group and a load balancer security group. So if we need the ID of the instance security group. We'll go to the backend security group. Select the backend security group, make sure it's written here, backend security group. Go to its inbound rule. Add rule, All traffic. Allowed from the Beanstalk security group. And Beanstalk instance security group, not load balancer security group. You can write some description, "Allow traffic from Bean instance security group," so you can identify security group rule is for what. And say Save rules. Okay. I also want you to do one more thing. Go to every service, get its endpoint backend services, its endpoint, port number, username and password, and keep it into a file ready that we will need it in the next lecture where we'll build the artifact and deploy to Beanstalk environment. I'll see you in the next lecture.
