# RDS

Alright, so v-profile application needs database, MySQL database to be specific, and we are going to use Amazon RDS for that. So let's go ahead and spin an RDS instance. Okay, so let's find RDS service. Aurora and RDS. So we need a specifically MySQL database. You see here an option create with express configuration in seconds. This is for Aurora PostgreSQL. We want to create a MySQL database so we can go with this option. But before this we are going to create two things, parameter groups and subnet group. Now in real time, there will be DB geeks who wants to change the configuration, the settings, they need full customization of the database, but Amazon RDS doesn't give any option to, you know, SSH and change the configuration file. But it gives us a parameter groups. So when you create a MySQL or any Amazon RDS instance, it uses the default parameter group, but we can create our own parameter group and make the changes when we need. So click over here, you will see an option for parameter group. Click over there. In the default, you will see a default parameter group. If you don't see, ignore it anyways. We need to create our own parameter group. So click on create parameter group, give parameter group a name. So I'm giving "vprofile-rds-rearch-paragrp." And in the description also I'm giving the same. So give a name that makes more sense like this. And then in engine type, you need to select the database that you want to create. So it gives you configuration for that. So you want MySQL Community, click on that. Parameter group family. So we are going to launch MySQL 5 and... I'm sorry, we are going to launch MySQL 8 and we can go with MySQL 8.4 as this works with our v-profile application. So stick to MySQL 8.4, I have tested it. And in the type make sure it's DB parameter group and not the cluster parameter group because we are going to launch just one single RDS instance and then click on create. Now, let's click on the parameter group that is created. So it comes with all the default configuration, a lot and lot of configuration around like 27 pages. Don't worry, you don't need to understand these things, but just keep in mind that whenever you get a requirement of changing a configuration for your RDS instance, you can find that parameter, that configuration over here and make the changes. The next one is the database administrator might need more control over the network. So every region has a default VPC, and in that there are default subnets, but in real time, they might create a separate VPC or you might need to create your own VPC and your own subnet in that. And in MySQL database in Amazon RDS, you can select those subnets through subnet group. So click here on subnet groups, click on create DB subnet group. Give subnet group a name. So I'm giving "vprofile-rds-rearch-subgrp" and same I'm giving in the description. Now check here or VPC. You see a default VPC, but you might have your own separate VPC for your project or for your database. You need to select that VPC. Here we have just the default VPC, we're going to select that. And in a VPC, which is a network, you have subnets. Now you see the list of zones over here. One zone can have many subnets. Let's just select all the subnets. I'm sorry, let's select all these zones. Here we are selecting all these zones. So select all these zones and then you can see here the list of subnets in those zones. So for us-east-1d, there is this subnet. Now it might be specific in your project case you need to select a specific subnet id. So based on that you can select that subnet id. But here we are just going to select all the subnets. So I've selected all these zones and I've selected all these subnets from that zone. Now while creating this subnet group, you might get an error that this particular zone or subnet is not available. There is an issue with that, you can remove that from the list. Let's try to create with selecting all and it got created without any issues, at least for me. But if you get any error, remove that specific subnet or zone and then create it again. Okay, so we have parameter group and we have subnet group. Now let's go to dashboard and click on create with full configuration. Click on create. This is with full configuration. You can select every settings. So based on your project requirement, you might need a specific kind of database. For v-profile we have MySQL. So we are going to select here MySQL. So there is a full configuration option, easy create. We are going to select full configuration because I want to show you all the settings. In templates, we have production, dev/test, and sandbox. So do not select production. This is going to create a multi-zone database cluster, which will be costlier, just select dev and test. We still get to see all the options and we can create the most cost-efficient RDS database for us. If you want to be very careful and do not want to take any chances of any extra charges, then you can select sandbox. So you see everything, the production style, the multi-zone will be grayed out and you will get option of all free tier. But I'm going to select here dev and test and still select the most cost efficient options, free in fact. So dev and test and select single-zone database. Okay, scroll down and select here MySQL engine version. So 8.4.8 works fine. I have tested it 'til here. So now stick to 8.4. The minor version can be anything but stick to 8.4 until I update the lecture. Scroll down, give database a name. So I'm giving "vprofile-rds-rearch." Now I have said the name of database. This is the name of the RDS instance. In RDS instance which is MySQL, we are going to create a database called accounts. That is the requirement of our v-profile project. So let me correct myself. This is the name of the RDS instance. For credentials, the username, let's keep admin and for credential for saving the password, we are going to say self-managed. There's an option to save all these passwords in AWS Secret Manager, but that is not free. So keep self-managed and click on auto generate. Scroll down. Instance configuration. So in real time where you have many users connected to your application, you might need an database that has good EBS bandwidth, good RAM, good CPU. But for our use case, which is learning and testing, we're going to select here burstable class. And from here you can keep here t3.micro, t2.micro. just make sure here you have just one GB of RAM. Once again, be careful here select t3.micro, t2.micro. Scroll down in storage. GP3 is fine. You see here the option of provision IOPS, which is faster, but let's keep GP3 which is cost efficient for us. And make sure you reduce this allocated storage to 20 GB. It'll be 200 GB by default because we have selected dev and test option up there. So if you have selected sandbox, you will get here 20 GB anyways. And scroll down. Additional configuration. So enable storage auto scaling. You can keep this option on because anyways, we are not going to add any more storage. This feature allows storage to increase after a specific threshold is exceeded. So we are not going to put any data, so even if you enable it, it doesn't matter. But for a safer side, just uncheck it. Connectivity, keep it as it is. We're going to manage the connectivity from our v-profile application to this RDS instance. VPC, just keep it default. DB subnet group. Just make sure your DB subnet group is selected over here. We have created this DB subnet group, so make sure that is selected. Public access not required. Our v-profile application is going to connect to this RDS instance privately inside the AWS network. So we definitely don't need public access. Now here, public access means from the internet. Security group. We are going to create... Oh sorry, select our own security group. So just remove this default from here and find your v-profile backend security group. All our backend service goes in the same security group. Scroll down. Additional configuration. We have the port number 3306. So let's keep it as it is. Default one. Monitoring, keep it same, database insight - standard. You can uncheck this enhanced monitoring. We don't need it for our use case, which is learning. But in time you might need or you would definitely need the enhanced monitoring. And based on this input you can scale out or scale in your RDS instance and manage cost and performance, both. Logs. So RDS doesn't give any logs. We don't have any as such login. And we can see the log files, but there's an option that can send all the logs to CloudWatch. In real time, you might need to select this. Additional configuration. So initial database names. So for v-profile application, we need a database called as accounts. Make sure there's no spelling mistakes. DB parameter group. So select our parameter group that we have created earlier. Backup. Enable automated backup. We are not using it for production use cases, so we can uncheck this. We don't need any automated backups. You can keep encryption as it is. Scroll down. Maintenance window. Auto minor version upgrade. If you put this check mark on, then any minor version will be automatically upgraded. Really does not matter because we are going to anyways delete our RDS instance once we are done. Maintenance window, let's keep it as no preference. When you make any configuration change to your RDS instance, then that configuration can be applied at a maintenance window that you have selected over here. For our use case now we don't need this option, so we can just say no preference. Now, estimated monthly cost, do not worry. This is monthly cost. We are just going to keep it for a few hours and we have selected all free tier options. So don't worry about the cost, just click on create database. Now as soon as you click on create a database, do not refresh or don't do anything, just click here on view connection details and copy this password. This is your RDS instance password, username is admin and this is the password. And make sure you save it somewhere, like a sticky note or somewhere because we need to give this information in our v-profile application. So our application can connect to or authenticate to our RDS instance. So I'm just keeping here the password and the username. And once this is ready, then I am also going to get an end point, but it will take some time. By the time, let's go ahead and create ElastiCache. So join me in the next lecture.
