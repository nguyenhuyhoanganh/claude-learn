# Homebrew for MacOS

Instructor: Hello and welcome. In this video, we will see how to install Homebrew on macOS. Homebrew is a tool to install softwares or packages. We will be installing some softwares, or tools in the next lecture, and we will be using Homebrew on macOS. So, let's search for Homebrew. So, open your browser, search for Homebrew, and click on the first link brew.sh. There you see a link or the command. You just need to copy this command and run this command on the terminal. So, let's open the terminal. All right, let's paste the command on the terminal, command V, hit enter, enter the password, hit enter. After the installation, you might see two commands that you need to enter on the terminal. If you do not see, ignore them. If you see, follow the next steps. I'm going to paste those commands. This one. Now, you don't see this after the installation here because I have already installed brew, but you will see this command. You can copy and paste this in the terminal. That's one command. And the next one. After running these two commands, you can start using the brew command. So, if you just type brew and hit enter, you should see some output like this. Now once again, if you do not see those commands, echo and eval, you don't need to enter them. But if you see, then only you need to enter. Now by using brew command you can install software. So, you can search for your software like we will be installing a tool called Maven. So, let's search for Maven. And you get the command of how to install Maven. So just copy this and run it in your terminal and Maven will get installed. That's one example like that you can search for any software, for example JDK, or you can search for let's say Vagrant which we will be using, right. So, you get the command, so just find your software, copy the command, run it in the terminal and the software will get installed. So that is all in this video. I will see you in the next one where we will install softwares by using brew. See you in the next one.
