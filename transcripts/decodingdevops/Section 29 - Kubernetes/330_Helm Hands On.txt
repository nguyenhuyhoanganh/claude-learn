# Helm Hands On

Okay, so for this exercise you should have a Kubernetes cluster running and nginx ingress controller installed. So I have Kubernetes cluster I have created it. With this command and then with Kops update cluster command I'm going to set up the ingress controller, same as what we did in the previous ingress exercise. I'll also put this command in the resource okay. So I applied it. It should start creating the ingress controller by the time. Let's copy the kube config file. So I'm on my Kops instance. And from here I am going to copy this kube config file into my local machine. So I'm going to copy this entire kube config file first. Start to end everything I'm going to copy this I'm going to open another git bash terminal. Open my kube config file. If you don't have create one. I'm going to first create the folder. Create the folder cube. And then in that I'm going to create the config file and paste this content over here. If you already have it you can remove the existing content and just replace that. Okay. Once this is copied you should have the cube CTL command in your system. So if you're using windows Mac OS can use this command brew install Kubernetes hyphen CLI. So that way we will have the cube ctl. So I'm going to run kubectl get nodes spelling mistake get nodes. Okay so I do have access to my Kubernetes cluster. Okay. Now we are going to open VSCode. And we are going to install one extension called Amazon Cube. So go to extensions and search for Amazon Cube. This one. So the name says the most capable generative AI powered. Now, seriously, I have used many AI powered assistant for writing my code, and so far, in my opinion, Amazon Q is the best in terms of DevOps. Of course I'm saying okay. Anyways, that's my view. Let's install it. We also have copilot so we can use copilot also, but for helm chart definitely use Amazon. Q no kidding. Okay, I think it's installed. I'm going to go to this option. You see this option over here Amazon Q you see this symbol. Click on it and you're going to say use for free and continue. It's going to open up your browser. Let's click on. So here in the basic details I'm going to give here the email address that is for my AWS account okay. After filling in necessary details putting password verification code everything you will get this option say allow access for your VS code. Okay, it took a while for me to get It this chat login. If you are facing still some issue like it's still loading, you can click on this sign out, close VSCode, open again and then again try to sign in. It should work. Okay, now create a folder somewhere on your system with the name WordPress. Then open that folder from VSCode. I have created a folder WordPress hyphen in F drive, so I'm going to open that file. Open folder f drive WordPress K8's select folder. Okay, so as we discussed, the idea is to first create a WordPress application. So we need the WordPress app and MySQL DB. So one way is we can simply, you know, open a browser, Google it and find the definition files, copy paste them, make changes. The other way is to ask Amazon developer to create it for us. So let's try the browser first. So I did some googling and I found this documentation of deploying WordPress and MySQL with persistent volumes. So let's take a look what it has. So we have the service definition here. A service will get created with port 3306 for the app WordPress selector. And there is also a persistent volume claim of 20 GB. So it will create based on the storage class that we have. Like we are running Kubernetes cluster on AWS by using cops. So if we use this storage class is going to create an EBS volume of size 20 GB. That will be our persistent volume claim. And then we can map this as a volume to our MySQL app. Anyways, let's take a look again like a deployment WordPress MySQL. So the image is MySQL 8.0. And take a look over here. MySQL root password is defined from a secret. So we need to create secret as well. MySQL database variable. Database name WordPress MySQL user variable. That will also be WordPress MySQL password coming from a secret okay. And if you see volume mounts here, here you see volumes. So this will be the name of the volume. And it is coming from a persistent volume claim. So if we do this EBS volume will get created as the persistent volume claim. And that will be attached to var lib MySQL mount point. So that is about MySQL. So we have MySQL deployment and we have a PVC and we have a service. Let's scroll down. So we see WordPress here service WordPress of type load balancer. So it will create a load balancer will be not doing this. We'll be using ingress controller and a persistent volume claim again of 20 GB for this one deployment. So it's deploying Container of this image WordPress. This will be downloaded again from Docker Hub and there is some tag to it. Some variables define MySQL password that will be coming from the secret. Again persistent volume claim that will be mapped as a volume to var HTML. So we can take these things, you know, create our files and change the settings that we need and run it. You know what. No thank you will use Amazon Q developer. So go back to your VS code. Make sure you're on the right folder that you selected. Go to Amazon Q. It should open the chat over here. And over here you can put your question to generate the Kubernetes definition file okay. So before we write the prompt I want to show you a few things. So we have nginx ingress controller write an ingress controller of type nginx. And we are going to ask all this Kubernetes to create a PVC for us persistent volume claim. We need to define the storage class over there. So if we do cube CTL get SC storage class, we should see a storage class named as default. And the provisioner is aws EBS. Now this by default when we create Kubernetes cluster with cops on AWS. Of course it creates this storage class. So we're going to mention this as the storage class. So you should have all the information before you write the prompt. And don't expect that you know in the first attempt itself you will get everything right. I have tried it several prompt to get the exact thing that I need. First I was struggling with GitHub Copilot and I tried different other generative AI also. I also use ChatGPT many other things and there are so many edits after you write the to write. I know after I write the prompt, Amazon Q was the one that got everything correct, but I tried, you know, give few more, try to get the no write code that I was looking for. So I'm going to show you the prompt. So that's the prompt. WordPress set up Kubernetes definition files. And I'm saying separate files for WordPress app, MySQL app service DB service, PVC and ingress PVC should use storage class default secret file should contain all db users and db password for MySQL and WordPress both. Now the reason this prompt is so long is because I tried other things. Few attempts to get the right prompt, you know? First was giving everything in one single file or few files. Everything in one. The storage class was not default. Something else, and the secret file was also a little scrambled. So slowly I got to this prompt and of course finally ingress will be nginx with hostname and I've given the hostname over here. So let me change that one to WordPress maybe. So make sure you change this with the domain that you have, you have created a hosted zone. Sorry, the the domain that you purchased. Make sure that domain is there because we're going to also need to edit those DNS records. I'm going to use GoDaddy. So I'm using that domain from GoDaddy. And once this is created I am going to add this WordPress entry over there as well. So I'm going to put all this into the lecture resource. I'm going to hit this and let's see what happens. So Amazon Q takes longer time than any other code assistant I have seen. But it gets the optimum result okay. So it's working and it's going to think it's going to generate one file at a time okay. So it's work is completed. It's also telling what it has done. So it has created many files. Let's take a look. Let's click on this button over here. So we have MySQL deployment. Oh I forgot to change one thing here. It should be MySQL 8.0. There is no 5.0. It's going to throw error. So make sure you change that. Or better put this in the prompt itself that you use this particular image. So it generates this or anything else. Anyways I'm going to zip this content and put this in the lecture resource. So you can use this also the lecture resource. Or you can ask Amazon Q to develop it for you. So we have MySQL deployment. We have PVC. You see storage class name default and has given ten GB of size. Let's reduce it to just one GB. So PVC or persistent volume claim for the storage class works like this. You already need to have a storage class with this name. As per this definition file, you can have another. You can create other storage class as well since we are using Kubernetes cluster created by cops. So we have a storage class with the name default. The provisioner here is AWS hyphen EBS. So when we submit this information that I need a persistent volume claim with this name access mode read write once storage class name default and we are requesting the resource of one GB. Storage is one GB. So it's going to go and talk to this storage class, which in turn will create an EBS volume of one GB size. And remember its name is MySQL hyphen PVC. Now if we check in the MySQL deployment, if we scroll down here we are creating a volume. This is the name of the volume. And the volume is coming from the persistent volume claim. And that's the name. So that means the PVC with this name should exist. So it is going to declare that as this volume with this name and then this, this name over here, if you see in volume months this name, this is the volume is mounted at this path. So this is how the PVC works. and we have MySQL service with the name WordPress MySQL port 3306 and the selector that will match with MySQL deployment. And then for WordPress WordPress deployment. Let's look at the image. So we have the image name as WordPress latest. Fine. That's okay. And we need to pass some variables to this container because it needs the access to the database. And for that purpose we have to define these variables WordPress db user WordPress db name of WordPress db host. So all this information, some are coming from secret and some are directly defined. Like this. Let's take a look at the secret. So here you have the secret the MySQL root password, MySQL password, database name and MySQL user. We have a PVC for WordPress app, also five GB in size. Let's reduce it to Two three GB. Control S to save. So go through all this definition files. They are pretty self-explanatory based on what we did previously in this Kubernetes section. And once you have gone through all these files, we can work on converting these files into helm charts. So I request you to pause this lecture and go through the files and then resume. Now we can create helm charts first. We'll do it manually. Once we understand the structure then we are going to use Amazon Q developer in the next lecture to develop the helm charts from these files. So go to your browser and search for install helm. Or you can come to this link. So you can install helm through Package manager. Like for Mac OS you can use brew install helm For windows. Open chocolatey as administrator and execute this command. Choco install. Kubernetes. Hyphen. Helm. Okay, helm is installed for me. Now I am going to open the terminal from VSCode. New terminal for windows user. You can switch to git bash in the terminal. Make sure you are in the right folder. Execute pwd and here execute the command helm create and the chart name. So I'm going to give WP hyphen chart. And this should create a folder structure. If you see here on the left side, here you have the values dot YAML file, where you can mention the values of your variables and the templates. You can put your templates like you have for deployment.yaml, dot YAML, ingress, dot YAML, service, dot YAML, service account dot YAML. So based on the resource that you're using, you can add those definition file as the templates. So let's take a look at deployment.yaml. So if you see here instead of the name like we directly give the name of the deployment. Instead of that there's something else over here. These are the variables or the structure. So two curly braces in between that you see include including helm is used to import something from outside. So include WP hyphen chart dot full name is going to give the full name of the chart in specification we are giving here replica, but this replica value will not be directly given. There is a condition over here. First let's look at the variable values. Values dot replica. Count. Now when it says values it's going to check the values dot YAML file. And that's going to look for a variable called replica count. So let's go to Values.yaml. You see the variable the replica count. Its value is one. So either you can give it directly. But if you have some condition like for example over here it says if this variable is not defined values dot auto scaling dot enabled. If its value is not true, then assign this variable. So values dot auto scaling dot enabled. So once again we go to values dot YAML file. We look for the variable auto scaling in that you have another variable enabled. Its value is false okay. Like that in values dot YAML file. If you see there are some other variables like readiness probe dot http deep get dot path will give you this. Okay. Now let's look at the chart dot YAML file. This is the file which says the name of the chart, a description type and version. And if you see if you read here this is the chart version. This version number should incremented should be incremented each time you make changes to the chart and its templates. So when we are doing any upgrade right when we're making changes to our templates, the variable values, then we should change the version of our chart. There's one more version over here. App version. This is different and this is different. This is the chart version. This is your application version. So value of app version will be something like now we're deploying WordPress. So WordPress version or your own custom application if you have the developer will give some version or there will be a release mechanism where the version will be given like we have seen in CI CD. So those versions will be given here. Now if you are going to create your own helm chart from Kubernetes files, then we are going to basically remove these things. So I'm going to select all the files that I'm not going to use here and replace it with mine. So first I'm going to delete this. All the templates. This also can be used to conduct tests. So I'm not going to use that. So I'm going to delete that test as well. Values dot YAML file I'm going to define my own variables. So I'm going to basically select this and delete everything and save the file. Now we are going to copy these files or templates our Kubernetes definition files. We're going to copy and put it into the templates. And then we start modifying our template. So for the name I'm going to replace that with with the two curly brace. And I'm going to put the include there. Okay. So include VPI dot full name. Sorry, this should be WP hyphen chart dot full name. Now this is my app deployment. So I'm going to say hyphen app. So this is using the existing variable. But I want to define the variable and use it. So take some example of image. So here I'm going to replace these two things the image name and the tag name with the variable. So first I'm going to you know it's already code suggested me. So values dot mysql dot image dot repository colon values dot image mysql dot image dot tag. So I need to create this variable. First I'm going to save this file. And I'm going to go to values. I'm going to declare the variable MySQL and this a dictionary variable. Here I'm going to give image. In that we have repository code suggestion. Let me remove this. And the tag a dot zero. So this way you can define variables over here and use this variable in your templates. And you can use other templating mechanism like conditions and width. There are few other things that we'll be seeing in the next lecture. So I wanted you to understand the structure of helm chart. So when you develop helm charts by using any code assistant, and if things don't work as you expected, you know what to change where. Otherwise you really don't need to write all these files manually. Use code assistant like Copilot, Amazon, Q anything of your choice. So that's what we'll be doing in the next lecture. So just go through the structure, try to make sense of it. Create some test helm charts. Go through the structure. Look at the code, try to understand it. And once you're done, join me in the next lecture where we're going to create the complete helm chart for our WordPress application. So wrap this up and join me in the next one.
