# Extras

Hello and welcome. In this lecture, we will see few extra things which you may or may not use in your real life, depending on how you're working on a project. But they are really very simple things and I thought you should know them. So I'm going to show few more amazing features of Kubernetes, starting with Taints and Toleration. I'll just search for Taints. It should show me Taints and Toleration documentation. So, Taint and Toleration, you can understand it like this. Think of it like paint a node with something with some information. And then Toleration means a Pod that can tolerate that paint can only go inside. Think of it like this. So, for example, I taint a node. Again, think of it like painting a node taint node and the node name and key and a value pair like a variable No Schedule. What this means is the node will be painted with this variable and the effect will be no schedule. So read this. This means that no Pod will be able to schedule onto node one unless it has matching Toleration. So if a Pod wants to run on a node that has this information, then you need to create the Pod with this Toleration. Okay? Together you can understand. First is painting with this information. Then no Pod can run on it if it has this information. So how do you give information toleration? So, in your Pod definition, you can say Toleration key the key name operator equal to this. It's like condition. If this key value is this and the effect is no schedule, then run on it. If a Pod does not have this Toleration, it will not run on that Node. Okay, one example of a Pod definition. Here there is a Toleration. So it says if this key exists, not just equal even if this key exists and the effect is no schedule, then this part can be running on that Node, right? The first step is paint, which is like painting. And the second step is giving Toleration on your Pod. Maybe you want to restrict a node to run Pod that has this specific Toleration. Only those Pod can run over there, not other pods like that. Okay, that's Taints and Toleration. The next thing I want to show you is limits. Link there. Okay, so just scroll down here. So if you want to run a Pod and you want to reserve a specific amount of memory and CPU to it, then you have to say Resources Request. That means I'm requesting I need this much memory and this much CPU. Only then this Pod will run a node that does not have this much memory and CPU. Then the Pod will not run on that Node. Or if no nodes exist with this much resource, then this Pod will be in a pending state that's reserving. But then there is limit how much maximum it can use. If you do not mention this the Pod may use unlimited or on the namespace. Also, if the limit is set, it can go to that limit. But you can restrict a Pod to run on this much specific memory and this much specific CPU. Okay, so request is reserving, limit is restricting. Okay, next thing I want to show you is a Job, which is very simple. It's just another way of running Pod Jobs. And then we'll see cron Jobs. Jobs is basically a container that has a specific command or script that runs and completed a specific time. It's not a continuously running container like nginx, MySQL. These are continuously running container. But if you want something that runs your work and then returns the output, then you can use Job, Kind, Job. And you mentioned the container information and the command that it will be running. It could be a script or it could be some command. Maybe you're running batch processing or something that works for a definite period of time and returns the output. Then you have to use Job for that. This is similar to Pod, except Pod will run continuously and Job will run for a specific period of time. Then there is also Cron Job. This is similar to Cron Job of Linux. So scheduled tasks, you want to run something for a particular time or like an alarm, right? At this particular time, run this task like that. So you say Kind, Cron, Job and schedule you give. When do you want to run it? Just like a Cron tab format. So Star basically means run it every minute. Every minute, every hour, every date, first to 31st, every month, January to December, and every day of the week, Sunday to Saturday. This simply means run it every minute. What container? You specify container, and this container is running this command and it will run every minute. It's going to start run it, then the next minute and the next minute. Or you can specify I want to run at this particular time. Scheduling for that, you have Cron Job. The difference between Job and Cron Job just runs when you run it and just completes returns the information. Cron Job runs at a specific interval, specific time, like an alarm. Okay, one last thing we'll see is DaemonSet. Now, DaemonSet may sound similar to deployment. It runs a Pod, Pod runs a container. But in deployment, we mentioned a Replica. And DaemonSet will also have Replica, but it is the number of worker nodes. Or you can include the master node. Also, let's say you have four worker nodes. And if you run a Pod with DaemonSet, you'll have four Pod on each worker node. So it says a DaemonSet ensures that all or some nodes run a copy of a Pod. As nodes are added to the cluster, pods are added to them. So if you have four worker node, you have four Pod running. With DaemonSet, you add a fifth worker node. You will have now one more fifth Pod running on the fifth worker node. And if you remove the nodes, the spot which is running from DaemonSet on that node will be also gone. So why do you need this? As I said, these things are mostly administration thing. DaemonSet. We use usually to collect logs or do monitoring of your worker nodes or even master node. And frankly, I have not seen any other use case for DaemonSet. Definitely there will be, but as far as my experience goes, I have used DaemonSet for collecting logs and monitoring nodes and that also not directly. You will have some readymade solutions like Prometheus, Grafana, these things fluentd D, right, which collects logs and metrics. So you will just have readymade manifest definition file. You just run them, they run as a DaemonSet. So let's see. In our cluster, do we have any DaemonSet? I'm going to say kubectl get ds, means domain a means on all the namespaces. Okay, we have two DaemonSet. This is in the cube system namespace. The DaemonSet name is EBS CSI node and it has and this contains three because we have total three, right, master and to work or not, but it depends on how you created a DaemonSet. So we'll see this example in the documentation. For a DaemonSet, the name is fluentd-elasticsearch. Fluentd is to collect logs. As I told you Damon's, it is usually used to collect logs and metrics and monitoring so selector, it has template which contains your Pod information. So label and inspect, inspect. First thing you see is Tolerations. This simply says that if a node has this key, if this key exists, which is this key will be for the master node. Master node will be tainted with this key. If it exists and the effect is no schedule, then you can run this Pod on this master node or a master node that contains this particular key. So it is going to run on master node. Also, if you do not give this, then the Pod will not run on master node by default, master nodes are tainted to not run any Pod on them. If you have toleration, then only it will run on MasterNode. So that's a Toleration. So container flu and D, that's the image. And we are also giving here limits and request. So when you run monitoring logging solution, you don't want it to take up all your resources because the other resources, other workload may be critical, more critical than this. So usually it's limited and it will also have requests because it may need some amount of resource to collect your logs or metrics. So this is a sample DaemonSet. You can just copy it and just simply run it. So let's do that. Let's copy all this, copy sampled YAML and just save and quit. Yeah, I'm not really making any change. kubectl apply. Okay, the DaemonSet got created. Let's say kubectl get DSL. I think it is running in Cube system. And you see it's running three. Two on worker nodes and one on master node. And since we have given Toleration, that's why it's also running on master node. Otherwise, it won't run on master node. If you do not put the Toleration, it won't run on master node. CTL get Pod and Cube System. Okay. You see Pod, there are three pods, but they are running on all the three nodes that they are running on one worker node. DaemonSet will always distribute among the nodes. And it works the same way as deployment here. After, if you try to delete a Pod, the DaemonSet will recreate on that node. If the node is gone, then only the Pod of that DaemonSet will be gone. Otherwise, it will keep running as long as the worker node is running or even master node. So these were some extras. Mostly. This is the administration part of Kubernetes. But I thought you should know them since they are very easy to know. Right. Okay. That is all in this video. I will see you in the next one.
