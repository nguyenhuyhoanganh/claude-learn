# Kubectl CLI & Cheatsheet

Hello and welcome. In this lecture we will talk about kubectl tricks or tips. Now no matter how many trips and tricks we have, it's difficult to remember all of them. So Kubernetes has created an amazing document for that cheat sheet. Just search for it kubectl cheat sheet and it has these many regularly used commands that we need in our day to day Kubernetes operations. For example, kubectl config view which will show us the configuration. Cube config? Yeah, kubectl config view and it is showing the keep config file. It's not showing all the certificate, it's just hiding them. That's one example like that, you have many commands here. You can see the output in JSON format and you can selectively get the right keys. So for example, in coupon sick file you have users. So you have to say dot users and its value is a list so that's the square bracket in that dot name which will display the first user. kubectl config get context or show me the current context or use a particular context. You want to change the default context to something else. You can have multiple context in the same kubeconfig file. When you are managing multiple Kubernetes clusters, you can have one coupon SIG file that has information of all the clusters and you can select the cluster, you can select the context and use it. So these are some commands for regularly used command for cube config. Then you have kubectl apply. As we have seen, you can apply a manifest, you can give a path, you can give multiple files, you can mention a directory that may have multiple definitions, multiple manifests, and all of them will be applied. Or you can give a URL and at that location you already have definition files. The manifest file is just going to use them and apply it. Okay, kubectl create deployment NGINX image NGINX this is going to start or create a deployment with a pod which has the container images enginex imperative way. But with this, I will show you one cool trick to create the manifest. So let's come to our shell. So that's actually my cops VM. I have the cubeconfig file. There cubeconfig which will be used by kubectl. The cluster is up and running. I have cubeconfig file and I'm going to run kubectl run. Sorry. Let's start with run first. Run means run a pod. Okay, so kubectl run and I give a name. I will say now if I hit enter, it is going to create a pod and the container image will be engineix and the name everything will be engineer pod. But I'm not going to create it. This is imperative. I already told you that we always go with declarative. So what I do is I say run equal to client. This is dry. Then like just show me how you're going to do, but don't do it. And it's output in YAML format and I'm going to redirect it to a file. I'll say Ng Pod. So all that output goes to that file. It has not applied. It's a dry run, right? So let's see that file. Now you have a pod definition file, right? That's declarative. Now you can open that file and make the required changes if you want to just avoid going to the Kubernetes documentation. So creation timestamp, I'll remove that. I will remove DNS policy, I will remove restart policy, I will remove status, I'll remove resources. And maybe I can add like port numbers or volumes and stuff like that. So save a quote and I can just say kubectl apply to that definition file like that. One more example like this for the deployment kubectl and create run is only for the pod. Otherwise it's always create CTL create I will say create a deployment for me. And the name I'm going to say NGDEP. That will be the name of the deployment image. That's minimum you have to give when you are creating deployment image. There are other options also you can pass. But I'm going to just do a dry run for this equal to client O Yammel and redirect the output to a file. I'll say NGDEP YAML. All right, I see NGDEP YML that's deployment definition file. And again, you can open, make the changes and just apply it. This is just if you want to avoid going to the documentation to fetch the manifest settings, you can just do it like this. This is very helpful in Kubernetes exams, the certification exam where the time is very critical, you get documentation in the exam, but going there, copying pasting just avoids that time. Make sure you get a very good hands on this one if you're going to go for exams and also regular operation. Also, let's go back to the cheat sheet now. Okay. kubectl create job. Hello. This is to create a job. Alright, imperative kubectl explain pods many other regularly used commands giving and finding information. get pods -o wide. We talked about this in lecture two, one, seven different levels of logging where we talked about this getting information describe. You might also need to try these things. Output in JSON format. In curly brace it says dot items means that's going to be the primary key in the JSON star means all the values from the list and from that metadata. Inside that you'll have labels. Inside that you'll have version. So if you just view this in a YAML format or specifically JSON format, if you view, you can find out all the keys and the list and you can construct it. You should give a try to this one. Okay, let's go down. So many quick commands. Okay, updating resources, set an image. We talked about this in the deployment rollout history. Undo restarted deployment which will restart all your pod in the rolling fashion. Expose. This is to create a service imperative cube CTL label add label to a pod. Patching resources. This will be also helpful but again try avoid this in productions in real time. Always go through definitions manifest. But you can patch a resource. You can patch a deployment as in you can change a specific value. On this example kubectl patch deployment patch this deployment type JSON and what do I need to patch means what do I really need to change? So you need to give this in a JSON format. In that deployment, it's going to find that and patch it. Change basically editing resource scaling scale out skill in deleting resources. If you created something with the definition file, you can just say delete -f and the definition file path, it is going to delete everything from that manifest. You can delete it based on the labels also and basically delete pods and services that has this particular label interacting with running pods logs we talked about this. One thing I want to show you here is this one. In a pod you may have multiple containers. So if you have multiple containers, you can mention the container name -C and the container name. It will get you logs of that particular container. You'll can use labels also. kubectl run is to run the pod as we have seen. So run it and -i --tty means just login to it. This is the name and this is the image. So this basically says run the container from this image, open a shell and interact with that shell. If you want a direct login, you can use kubectl exec Stdn TTY MyPod that will be your pod name. Bin Bash which will open a shell on that running pod copying files and directories to and from containers interacting with the deployments and services. Port forwarding without creating a service or on that service, do a port forwarding. These are for the administration interacting with nodes and cluster kubectl cordon, cordon basically means make this node unscheduleable so no new pod will be running. On this note, when you want to bring it down for the maintenance then drain will remove all the workload from that node. So you coordinate so new thing doesn't run over there. Then you drain it. So you remove all the workload from that which migrates to healthy worker nodes. And once you're done with your maintenance activity then you can uncordon it. So new parts can start running on it. uncordon doesn't really mean that it's going to bring back your previous workload. It is just going to open up to take the new workload. This is if you're really Kubernetes administrator then you really need these commands handy right? So the cheat sheet again, do not by-heart. This is already available. But if you keep running this regularly it will be automatically by-hearted. But really a very good cheat sheet. I use it very regularly. Really good job Kubernetes on this one. Okay, so that is all in this video. Go ahead, try these things. Create pod, create deployment, Create services. Use these commands. Test them. Get comfortable with kubectl CLI. You will really need it every day. So that is all in this video. I will see you in the next one. Bye.
