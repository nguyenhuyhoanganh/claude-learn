# Ingress

Hello and welcome. In this lecture we will learn about ingress. And here, the first line in the documentation says an API object that manages external access to the services in a cluster, typically Http or even Https. Now, ingress is specifically for the external access that comes from outside, from the Internet. You are running an application in the Pod inside the Kubernetes cluster. Let's say it is a web site, web application, a website. So how will the user access it? Yes, we have seen service and we have seen service of type node port and load balancer. But that is just not enough. Because when you have many applications, especially microservices, then controlling different access from externally, like access based on a URL path or a port number, there are many kinds of rules that goes in. All those rules can be managed inside ingress. Ingress may provide load balancing, SSL termination. So the Https based access, you can terminate the SSL connection on the ingress. So internally in Kubernetes, you don't need to handle it. So, again, what is ingress? Ingress exposes Http and Https routes from outside the cluster. Think of this as a load balancer, where you can set various kinds of rules. Like if someone access on videos, it goes to this service. As it says, exposes Http, Https routes from outside the cluster to services within the cluster. So you will have service on top of your Pod, and on top of the service there will be ingress. Look at this diagram. User accesses ingress, which will basically a load balancer. And there'll be some routing rule based on how you are accessing it. I'm sorry, it's going to route to a service that intern will route to the Pod. So here the service will be of type cluster, IP, internal service, ingress exposes it to the outside world. Okay, prerequisite, what do you need? You need an ingress controller. There are many types of ingress controller out there. This market is really going up and up. There are many types of controllers, starting with NGINX. Let's just see the list. Let's click on this ingress controller. So we have AKS application, gateway, ingree controller, which is in Azure. Ambassador, Apache, BFE, Contour Glue, NGINX ingress controller, which is the most commonly I have seen, but for more kinds of features and all. There are many other different kinds of controllers. Skipper, traefik, and there's many more. Actually, we don't have this whole list here. So we'll take example of NGINX ingress controller. So we have to first install the controller and we'll set right rules and everything. But before we go there, I want you to revisit Microservices Lecture, which is an introduction to container, where I have explained about a micro service application emart and how there are multiple rules API, Web API or root directory, how there are multiple rules. I will show you the diagram. This one, I have explained this in the Micro Services lecture. So here we have an API gateway Nginx. Now if we are doing it without ingress then we need to create a pod. We need to have it of NGINX container and then we have to set rules inside this. If it comes go here, API go there. And this is for this one app like that you may be having multiple apps or you may be having more micro services. So all these different different rules based on different paths, different port numbers, different domain name, how do you route it? So all that intelligence can be handled by ingress controller and this can be better understood by actually doing it. So let's go ahead, let's install ingress controller and let's set up an application which we will access from the internet. Okay, we will go to NGINX ingress controller website and we'll see how to install it first. Okay, I will say install ingress nginx. Okay, that one will see installation with manifest, not this one. Nginx ingress controller content quick start AWS we have it for AWS. Let's click on AWS and it is in AWS. We use network load balancer to expose the NGINX ingress controller. Let me tell you basically this ingress controller is a pod, an nginx pod and in front of it you will have a network load balancer, the service of type load balancer, network load balancer and there will be many more things that goes into this. This is the command basically is a kubectl apply and this URL which has lot of manifest that will create our controller. So let's copy that and let's run it. Okay, I'm in my kops VM paste this command and run it. Okay, it did a lot of things first. Primarily it created a namespace kubectl get NS and you see the namespace. Let's see everything in this namespace kubectl get all ingress nginx. Let's see what we have. We have some pods here, three pods, a service of type load balancer and that is the load balancer. You can check in your database console. You should see a load balancer getting created there. And users will access this load balancer basically. And we'll have an Nginx controller where we'll set rules. So based on what the request comes, it's going to route it to the correct service. So we have deployment, replica set and some jobs. Okay, there you see the network load balancer. It is provisioning and it should also have a target group. Okay, so there is a target group and this two worker nodes which are the target and this node port 32718 and initial health is going on. So it will come up in some time. By the time this comes, let me tell you what we will be doing now. Okay, I'm in this directory called vprofile. I will put this into the resource section here we have three definitions. One is the deployment. Let's check that. YAML, that's the deployment name. My app replica one, the image is the vprofile Tomcat image. You can use this one. Or if you have already created your own image in the containerisation project, then you can mention that image. Also, this is the Tomcat image. It's on my docker hub account. And container port 8080. So that should create one part for us. And then we have a service definition here the service of type cluster IP. It is for the pod which has the label this. So front end port is 8080 and target port is 8080. That's the container port. And this is the front end port for the cluster IP. So this is an internal service. When I create this too, it can be accessed only internally, cannot be accessed from outside. But this ingress definition, ingress basically means kind. Ingress basically means rules. The rules that you are going to create for your service. This rules are for the service. So let's see the rules. Rules. This is the first rule we have. Only rule we have host this one. So if you access, if you go to your browser and you say vprofile.groophy.in On path /login so it's a vprofile.groophy.in/login. So when you hit that, it comes to the network load balancer and it comes to your ingress controller. And it is going to route this request to this service, which is our service. Cluster IP, my app on port 8080. But it will only come to this service if you access through this URL and this path. Now obviously you cannot access it through this. You have to you must have purchased your own domain. Create a CNAME record over there, which I will show you in a moment how to do. First, let me create this deployment object kubectl apply. They should create one part for us and they should create the service. Next thing is you will go to your domain provider. I have groophy in domain in GoDaddy. So I'm going to go to GoDaddy. Yeah, like that. Go to GoDaddy. Before that I will go to my load balancer and I will copy the DNS name. Now I'm going to create a CNAME record for this URL. Okay, I'm in my GoDaddy and I have created a CNAME record. I will show you what I did. I just hit add CNAME and I see vprofile. Now, I already created this record because it takes time to update over the internet. So I already created this record. So you have to create this record and wait for some time. So just say add record. It will create the record. Make sure there is no extra full stop or anything at the end. Just the load balancer endpoint on whatever subdomain or whatever hostname you want to give. I have given vprofile. So that should point to this vprofile.groophy.in should point to this. Now, as I said, I have already created this record. Let me show you there. Already created this record. Okay, next step is to finally bind everything togetherctl. Describe as we see. Keep in mind always when you work with ingress, we create ingress for the service. So we have this service. The service should be in a working state. So how service of type cluster IP and you see endpoint that's the Pod IP and the port number of the container. So you should see this. The service should be in a working condition and we are creating ingress rule for this service. Now forget about Pod and everything. When you create ingress. Keep service in mind. I'm creating ingress for the service like that, which will remove some complexity when you're doing it. Now, let's go back to our ingress definition. Okay, so once again, guys, vprofile.groophy.in points to our network load balancer. We mentioned that in GoDaddy and we say login. Then it is going to route the request to this particular service that is in working condition. Okay? So let's finally create this service before I do this. You know what, let's just do it. Apply pro ingress. Okay, so you understood the flow. First, I created the controller. Second, I created the deployment. Third, I created DNS record DNS C name record for load balancer. And then finally we created ingress. Now, if you're working in a project, controller will be already there and you will be adding new apps. So all you need to do is deploy your app deployment and create ingress. Okay, I missed one thing. So I created the flow once again, controller first deployment, then the service service should be working. And then you create that DNS C name record for your load balancer. Again, if you're working in the project, this will be already done. Then you create the ingress kubectl get. All right, you see that vprofile.groophy.in It's going to go to this address. Okay, let's go there and check from the browser now. Okay, so this is what it is showing. And I know this would have happened because the routing is done internally in our application, if you directly access login, it just shows this page. It doesn't load the images and all that HTML CSS content. But I wanted to show you how routing works based on the path. The actual change we have to do. Now I'm going to delete this ingress. So I don't need to mention /login because that application already routes it internally. I'll just say slash and now create this. Okay, so you have to wait for a moment. I just kept --watch, so it was just watching. And then when I see the load balancer URL updated over here, it should be working. Now let's try it once again. Okay, so just hit the URL and it routes it to the login page. Okay, one more time. Now, that's the correct rule for our vprofile application. But some application, especially micro services, the routing should come externally. So you have to create ingress rules like that. Before we close this session, let me show you a few more things. From the documentation ingress, there are many kinds of rules that you can give. So path based routing, what we did was path based routing. We give the path and when we access it on that path, then it routes to that service. But if you access on another path, it may route it to another service. So you can set up different different rules like this host based routing based on the host name. So if you access with this host name that is your URL, then it is going to route it to the service and the path is root directory like we did. This is what we did. We have given it based on the host name. So if I say vprofile.groophy.in then it goes to our service. Okay? If you give a different host name, it goes to a different service like that. There are many kinds of rules. This is beyond the scope of our course. But feel free to go through the documentation, check forth based routing, host based routing, path based routing and make sure once you're done with this exercise, you delete the ingress. Not just the ingress rule, the ingress controller. Because network load balancer is not free. kubectl get NS. You can say kubectl delete NS directly delete the namespace. You can do that. That's one way of deleting you delete the entire namespace, it deletes everything with it. The other way of deleting one more way, I'm not deleting this now. So based on this definition file, I can also delete when I say apply, it created. When I say delete, it's going to delete everything that it reads from this manifest. So it's going to take some time to delete. It's going to wait for the load balance and everything to delete and clear. So take your time. Go through this video. I know this was a little longer video in Kubernetes. So before I leave you on this one, remember we create ingress for the service in microservices you will have requirement. If I access it at API, it goes here. This should ring the bell. Oh, I need to create English rule. Or if I access with this host name, it should go to this particular part. So when there is if I access this then it should go like that. Then these calls for ingress rules. All right then I'll leave you here. Go ahead, do your experiment. I will see you in the next lecture here.
