# Minikube for K8s Setup

In this video we will see how to set up Kubernetes cluster through Minikube and Cops. First, let's understand how to do it through minikube. It's pretty simple. It's one node cluster for testing. So you always open the PowerShell as admin install chocolatey. Once you have chocolatey, you can install minikube through chocolatey by running simple command choco install minikube kubernetes CLI kubernetes CLI that's the kubectl so you can connect it to the cluster. You open PowerShell again, close and run once again and you see minikube start. When you see minikube start, it is going to launch one single VM on your Oracle VM virtual box. So you should already have Oracle VM virtual box on your system. That's it. The cluster will be up and running. We'll see how to use that. Then we are going to set up Kubernetes cluster with kops. You need a few prerequisites for that. You need to purchase a domain to maintain our Kubernetes DNS records like I have groophy in from GoDaddy. We're going to create one VM and set up few tools on that kops kubectl SSH keys AWS CLI on AWS we are going to do a few more setup like we're going to create s3 bucket, an IAM user for the CLI route, 53 hosted zones. OK, let's see all the steps one by one. First for minikube and then for kops you can clone the source code vprofile project source code. So I'm going to clone the source code. If you have it already cloned, just get into the repository and do get pull so you get the latest changes. Let's see if you already have it, just do a get pull inside that to get all the latest changes. We are already up to date, we just cloned it. We are going to use a branch Kubernetes set up so you have different directories over here. First we're going to do minikube. So let's get into minikube and there is a simple piece of document. First you need to install chocolatey from prerequisite video. You should already have chocolatey on your system. If you don't have run this command and have chocolatey installed. Once you have chocolatey installed from PowerShell, close the PowerShell open up again. Open PowerShell with administrator we'll copy this command to set up chocolatey in case if you're getting error over here like restricted or not allowed then turn off your antivirus for a moment. On this command. You should have the latest version of chocolate tea on your system. Once you have done this, close PowerShell and start again with admin privilege. Next we're going to run this command choko install minikube which will install the minikube tool and Kubernetes CLI which is the command line tool to connect to the Kubernetes cluster. Okay, now I have latest version of minikube and Kubernetes CLI tool. I can close this now and I can use my git bash which is much more convenient. Check minikube. If you still don't see minikube command, then close, get bash and start again. So we are going to say minikube start which is going to start a local Kubernetes cluster and then we'll check its status. So minikube start, it's going to take some time because it's going to download an image and then bring up a VM on Oracle VM virtual box. Okay, it says done. The Kubernetes cluster is up. You can see a VM. You can check minikube of sorry, kubectl get nodes and you should see your one node which is master node. So kubectl which we installed through chocolatey is using a file which is in your home directory kubeconfig at this path kubeconfig file, the kube config file, you have the cluster detail over here. kubeconfig file is used by the kubectl command. So the server detail the name of the cluster. This is the cluster detail. Down here you have users minikube and client certificate, client key for the user and then context. Context marries the user with the cluster. So you say in context cluster the name of the cluster minikube which is this one user minikube. So that's the minikube user with the credentials to login. So when I say kubectl get nodes, it is using that kubeconfig file to access our cluster. Now let's run a few more tests. Let's go to Kubernetes documentation learning environment Minikube all right, so we just did this minikube cluster is up and now we can test by running kubectl create deployment. So this command created a deployment named hello minikube. It has fetched an image from Google container registry kubectl get pod and you should see a pod running from the deployment kubectl get deploy. So this deploy is running this pod. Okay, let's expose it. So it creates a service which we can then access kubectl expose deployment deployment name --type node port on port 8080 OK, to check the URL of our deployment of our service, run this command and you should get a URL so we can access our application, this URL and you should get this and that just test that your minikube cluster is up and running. You're able to create pod deployment and expose service for it. So in our next project, we are going to host a Java application on Kubernetes cluster. In this project we are just seeing the setup by using different different tools. So one minikube is done. Now let's wrap it up. kubectl get SVC will first delete our service. kubectl delete deploy. Hello minikube. So delete the deployment and it's going to delete your pod as well. Now let's stop the minikube cluster. Minikube stop and then finally you can say minikube delete and you should have now no VM over here. So that's good. Hands on on a kubernetes cluster.
