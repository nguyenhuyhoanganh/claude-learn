# Config Map

Hello and welcome. In this lecture we will talk about variables also about configuration how you can inject data in the pod. Now we know pods are disposable. If we want to make change we have to delete them and create a new pod with a new image, new container image. So we have the persistent changes but while we are running the pod we can also inject variables and configuration in the pod. First let's talk about environment variable which is very simple assigning a variable just like what we've seen in Bash and Python. So in a part definition file you have to do this env, - name of the variable value accounts same one more variable. Now what happens is when you run this pod definition file which has this container, MySQL container this container will have two variables which we have created MySQL underscore database and MySQL root password. You can do echo dollar this variable in the container and you will see its value. So it basically exports these variables in the container. That's how you can set variables for the pod. But really this goes beyond this. When you're running any application set any stack you will have many variables and also configurations that can change over time. You can use something called as ConfigMaps to store all your variables and configuration at one place and then whenever you want in any pod you can inject these variables or files which will be configuration files mostly. So let's see first we'll see an imperative way of how to set variables in a ConfigMap. Once again ConfigMap here is collection of variables so imperative command line which you should avoid but you should anyways know how to do this kubectl create ConfigMap name of the ConfigMap from hyphen literal equal to variable equal to value like that you can give multiple variables from literal and you can mention multiple variables and you have to apply this. And when you say kubectl get CM, you will get all your ConfigMaps and if you want to see information inside that you can do YAML and it's going to show the content all the keys these are called as keys here MySQL database, MySQL root password are the key in this ConfigMap so I can say this ConfigMap DB config has two keys MySQL database, MySQL root password and those keys will have some values associated with it. You can also describe a ConfigMap and you can see the content. So this is creating ConfigMap imperative we can also do a declarative like this and we will go to documentation and find out and we'll do the examples also those exercise but in short the smallest ConfigMap what you see right now on your screen DB ConfigMap data not spec here we have data, two keys and their values and then you apply and you create it. Now you have variables stored in a conflict map. How do you inject? In a pod? You're creating a pod. How do you inject? Well, one way is you can say env from and this will be inside the container, not at the pod level, at the container level. envFrom -configMapRef,, name of the ConfigMap. Now, if this ConfigMap has 10, 20, how many ever variables All those variables will be exported in this container. All of them. But what if you want to be selective, you want to select a key from a ConfigMap, then you have to go with ConfigMap, key reference. So now env here we have env from do you see the difference? envFrom here env, there's single variable, name of the variable. This will be the variable in the container and its value from ConfigMap, key reference, name of the ConfigMap and the key. Now here the name of the variable and name of the key are same. Usually I do this to avoid confusion, but here this is the variable and here this is the key in the ConfigMap. So the value of this key will be stored into this variable in the container. I know this could be a little confusing. Try to stay with me. We'll do the exercise and you do it a few times to really get hands on this ConfigMap. In short, understand ConfigMaps, collection of variables and you can inject those variables in the pod and you have all the help in the documentation. So let's go to the documentation, find some examples. Okay? ConfigMaps, as I told you. Now, these ConfigMaps can consume config map as environment, variable, command line argument, or as configuration files in a volume. Yes, we'll see what that means. So there's an example of a ConfigMap. Let's take it, let's copy that, let's create it and then we'll understand it. So I'm going to say sample CM dot YAML. Okay, let's understand this ConfigMap. That's the kind that's the name of your ConfigMap game Demo and it has this variable, or should I say key. This is one key. That's the value of the key. Three, one other key. That's the value. Now here the value is just a string. But over here, game property. This key value is some data. This is all the same like variable, but it's multi line data. And there are also one more key. So this ConfigMap has four keys. Two are pretty simple, just normal kind of variables. And the other two keys has some multi line content. And you can store this multi line content as a file in the container. Yes, in a container you can set a file where this content will be stored. Again, try to stay with me. ConfigMap has four keys here. Okay, let's create it. kubectl apply cm, we get a warning there. This may be because I created this config map previously game demo and it was already created, I created like 14 minutes ago. So I tried to do it once again and that's why that warning. Let's clear this. Okay, let's say ConfigMap game demo YAML, see that entire ConfigMap you can see all your keys there. Okay? I told you, multi line content, right? See that? And there is a backward \n which means a new line character. Okay, so we created the ConfigMap and now let's see how we can inject it in a pod. Okay, so it says there are four different ways that you can use ConfigMap to configure a container inside a pod. So four ways to get this data in your container. One is as commands and arguments. You can have commands and arguments coming from here environment variables for a container. So we have seen that example in the slides add a file in a read only volume for application to read. This is also we'll see how you can create a file in the container. That file will have your config map keys values of those keys. So this is the example. Let's again take it copy this. I know lot of data. Don't worry, it's simple. Same here till the containers. env environment variables, one variable is this player initial lives from ConfigMap key reference. So this is the ConfigMap in that there is a key this one, its value will be stored into this variable. We have seen that in the slide also another one, same way configMapRef. So two variables value coming from ConfigMap. Right? Now there's also one more thing here you see volume mounts ConfigMap can be also mounted as volumes. Okay? So this is the name of the volume and it will be mounted at /config in the container you'll have config directory where all the content from this volume will be created. So what is this content? Well, if you scroll down, you have volumes section here and that's your volume name. See that? Same name ConfigMap. Now we have seen volumes in previous lecture that was host path here the volume is of type ConfigMap. Yes, a volume can be also of a type ConfigMap and that's the ConfigMap name items. These are the key game properties, user interface properties. If we go back to our ConfigMap, these are those keys game properties, user interface properties if you see multiline content, right? And we're going to store this into a file. That file name will be this game properties and user interface properties same name you can give any other name. This will be the name of the file. This file. These two files will be created in /config directory. Now pause the video. Look at it one more time. Take your time. Understand this. This is used a lot injecting configuration as a volume. So basically you will have some configuration which you want to put in the container. You can do it like this. First you create the ConfigMap. And then you mentioned it as a volume. And that volume you're going to map at some directory. Okay? So four things. Once again, when we run the pod, the container will have this variable, this variable. And it will have two files in this directory. Let's create it. Okay, I'm saying it once again. If you are having some confusion, rewind watch it again. kubectl get pod And now we are going to log in inside this pod. Yes, that we can do it like this. kubectl exec --stdin --tty , pod name and the shell path. Now, we don't have /bin/bash here. We have /bin/sh. Well how do I know this /bin/sh? Well, we created the container with Alpine image. Alpine image does not have bash shell. It has just sh shell. Again, I did this previously, so I know that. Okay, run this command and you get a shell there in the container. I'm inside the container. Now, let's look at the volumes first /config You see two files. I told you it's going to create two files, right? Let's look at the content game. I have to go inside that, right? /config Game properties. See that? That content is coming from that key. And it is here. User interface properties. So like that, you can create files in the container from ConfigMap. Now, two variables. These are the variables that we have declared, right? Player initial lives. Let's do an echo. Echo dollar. There you go. That's the value. Check the second one. UI properties, file name, echo dollar. Okay, we have seen how we are injecting data in the pod. We have seen how to do it through variables. We have seen how to do it through files. And all those variables and files are stored in ConfigMap. If you still have doubts on this, watch the video again. Pause whenever it's necessary, because lecture will be dependent on this lecture. So wrap this up and join me in the next lecture.
