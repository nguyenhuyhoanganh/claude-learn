# Volumes

Hello and welcome. In this lecture we will talk about Kubernetes volumes. Now volumes and there is one more persistent volume is a whole topic in itself. And then you should have some storage knowledge, storage background to manage the backend storage. And that is really out of the scope of this course. But being a DevOps or a developer, when you write the definition files, when you write the manifest, you should know how to map a volume to your Pod. And that's what we learn in this lecture. Now, there are many kinds of volumes that Kubernetes support. One of it is AWS Elastic Block Store EBS, which we learn in AWS. You can also map an EBS volume to your Pod, which I have explained how to do in project lecture. And there are many other types of volume it supports. We have Azure Disk, ceph,cinder, FC Fiber Channel, Flocker, GCE persistent Disk. If you're using Google Cloud, GlusterFS, Gluster from Red Hat, I have used it. This is what we can do very easily. Local volume NFS Port Works, which is a very enterprise popular solution with Kubernetes for storage Vsphere. If you're using VMware vsphere VMDK volume from VMware vsphere. How you can mount that host path. This is what we will be doing. Now, there are many options and based on what project and company you're working for, they may use a storage solution. Your job will be to map that particular storage volume to your Kubernetes volume. So we'll do one example over here with Host path. Host path means on the worker node, we pick up a directory and we map that as a volume to our Pod. Let's look at the definition file. So there your usual Pod definition file container information. Here you have volume mounts mount Path. Where do you want to mount it? In the container. So in the container at /test-pd, it's going to map a volume called test volume. So where is that volume coming from? From here, volumes test volume and it is of type host path means on a worker node, it's going to pick up a path which is /data. And we are mentioning type as a directory. So /data is a directory and that is the volume name. This volume test volume will be mounted at Slash PD. So in short, whatever data the container stores at this directory testpd will get automatically stored in data of your worker node. As I said, in the upcoming project, you will see how to map an EBS volume to your Pod. Now, better storage solution is Persistent Volume, where volume is created separately. And then in your Pod definition file it will be mounted. You won't have volumes much information in the Pod. It will be a separate object managed by an administrator. So we'll take this, but we'll make some changes also this time. So let's copy this, back to my kops VM. I'm going to create a manifest called mysqlpod.yaml. Let's paste all this. Okay, there. I'm going to make some changes here. I'm going to give pod name as dbpod container name. Okay, image we will give here MySQL five seven. That's an image from Docker Hub. Let me fix this here. Container name will say MySQL. Okay, mount Path. Now MySQL container is going to store all its data in /var/lib/mysql . We did this in Docker. See MySQL container, we have stored all the data of /var/lib/mysql to a docker volume. This time, similar thing. But this is a Kubernetes volume. And let's give the volume name as dbvol. And that should be the volume name host Path. I will just say okay, I'll just keep that /data. Now, this is not a production one. This is for your understanding. In production you'll have a better volume. Like EBS volume, Google Persistent Disk, Azure Volume or Cluster or NFS something that is a proper storage solution. So wherever the storage is coming from being a DevOps, your job should be to take that volume and map it to your pod in the right container directory where it stores the data. So let's save and quit this and let's test it. kubectl apply. Okay, that got created. Let's wait for a moment. Let's check its status. kubectl get Pod. Okay, it's still creating CTL describe Pod. Okay, so we have an error here. It says Host path type check field data is not a directory. That means that directory does not exist and it is not going to create it. It should be already there in your worker node. Now this is for real time. This /data directory would be an external storage mapped to your worker nodes. But now it's not there. So we have to make a little change. We will change the option MySQL Pod. So we'll give an option here directory or create. That means if this directory does not exist, create it. Okay, let's delete our pod kubectl. Delete Pod. Okay, got deleted. I'm going to apply it once again. And let's describe this once again. And there it worked. So you will see here in the volume section host part data. And if you come up here, mount /var/lib/mysql  is coming from dbvol which is the volume name. Now, whatever data our container stores at /var/lib/mysql is going to come to the worker node /data. Even if this part is deleted, we still have our data safe in data directory of the worker node. As I said previously, this is not for production. Production. You'll have a proper volume of storage mechanism set and you have to map that storage volume to your pod. I have shown one example in the upcoming project video how you can take an EBS volume and map it to your pod. And that is all in this video. Let's delete the pod that goes to clean up and join me in the next lecture.
