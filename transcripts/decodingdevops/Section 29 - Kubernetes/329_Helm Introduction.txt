# Helm Introduction

Welcome. In this lecture we are going to work on helm, the Kubernetes package manager. By using helm, you can manage your application as a single package on Kubernetes cluster. So far we have seen many objects in Kubernetes. And to run a complete application we will use multiple objects like deployment service ingress for the back end. We might need volumes, secret config maps, daemonset, statefulset many other things. So managing these objects in separate file can be overwhelming. So by using helm you can convert these definition files into single helm chart. So an application will be basically the bundle of this helm chart or the bundle of these different Kubernetes object into helm chart. So you deploy your application as a single suit. And then you can manage that application like that by using simple helm commands like helm install my app, helm uninstall or change the settings or versions. Now in this way, if you look, helm really acts like a package manager, but package package manager for your application, you can version your application. You can do templating, minor update, minor changes. You can easily deploy by using helm. And if we talk about DevOps and that's what we do, we deploy application, we manage applications, we upgrade them. So helm really makes our life easier. When we are deploying application on Kubernetes cluster, we can easily manage it. Let's look at some terminology. So charts or helm charts are basically collection of your Kubernetes objects. Your Kubernetes resources like deployments servers, ingress volumes, etc.. So the chart is collection of all these pre-configured resources. We also have repositories in helm. We can store these charts in helm repositories. There are also some predefined repositories from where we can download ready made applications and run it. When you run the helm chart, when you deploy the helm chart, it's called as release. So an instance of a chart in the Kubernetes cluster is called release. One of the most important part of this helm charts are the values, which are basically the variables or the defined values of the variables. Let's look at the structure of helm. So first we create the helm chart by this command helm create. And you give the name of your application like my app. Then it creates a directory structure. What you see in the left hand side WordPress chart. That's an example. In templates you have all the resources and they will generally not have hard coded values like image name or port number won't be defined. There will be the variable name mentioned there and its value will be defined in Values.yaml file chart. Dot YAML file will have the name of the chart, its version, which you can change. So once you have this chart created for your application by simple commands, you can deploy this entire application as a single chart. So you can say helm, install your name off the release and the path where this directory structure exists. And when you happen to make any change like you change the image tag or some port number or anything, you can make the changes in the values.yaml file, or you can have a separate values.yaml file as well. And once you make change, you can just say Helm upgrade and it will apply the changes. If you want to remove your application, you can just say helm uninstall and there are many other commands like helm, list, etc. as I said, there are repositories in helm. You can store your chart in repositories and download and install. From there you can use existing repositories where there are predefined charts. You can add the repository like this helm repo, add name of the chart and URL. And then basically when you do helm install it's going to download those charts from that repository and run it. Okay. Now let's look at what we are going to do in this lecture. The exercise this will be very interesting. We are going to take WordPress setup. We are going to create necessary resources for it like deployment service volumes, PVC, persistent volume claim ingress and secret. So we have here the WordPress application running and the database MySQL database that supports it. So we'll have two deployments. One for the app, one for the DB. Then same for service PVC and one for ingress. And in secret we're going to store the passwords. And we're going to run it directly from the Kubernetes definition files. And then we are going to use AI generative AI to convert these into helm charts. We can do it manually also. But Frankly, after, you know, the code assistance tools like Copilot and many other tools, we are going to use Amazon Q in this lecture. So after these tools emerged in market, we started using these to write our code. And then we just change it as per requirements. I'm going to show you how you can use Amazon Q developer Q, which is an AI generative AI from Amazon. So we're going to use that to generate the helm charts. Once we develop our charts we are going to install them and run them make some changes. You know look at the basic helm commands. And once we are through with that we are going to see more features from helm again by using Amazon Q, the code assistant, we're going to ask it to deploy or implement best practices, development best practices. And then it's going to add many more things from helm into our charts into our templates. And then we're going to study those templates. So enough talking. Let's get started.
