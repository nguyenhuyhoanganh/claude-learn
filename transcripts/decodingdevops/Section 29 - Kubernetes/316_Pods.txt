# Pods

Instructor: Pods. Now we'll understand what exactly is pods, how to run it and how to use it. So as discussed previously about pods, the container runs inside the pod and pod is the most basic unit in Kubernetes. It's the most smallest unit. It just represents a process running in your cluster. And as I said previously, the most common use case is running one container per pod. If you are going to go with multi container, then the other containers will be helper containers. So pod is a wrapper, a boundary around your containers. So Kubernetes manages pods rather than managing containers directly. So we have the abstractions. We're going to give commands or execution for the pod, not for the container directly. When we are going with multi-container pod, we have to keep in mind these containers will be coupled with each other in a pod and they will be sharing same resource that is provided by the pod, one main container and the other helper containers, sidecar or init container or both. So if you're running Tomcat, you'll create one pod for it. If you're running MySQL, another pod. If you're running RabbitMQ, another pod. And if you want high availability, so you need to have multiple pods horizontally scaled, not multiple container in a pod, but multiple pods. So multiple Tomcat pods or multiple NGINX pod horizontally scaled. We can manage that very easily in Kubernetes. Now, as we have been running containers with docker run command and then we have seen Docker Compose, which was much more better way to manage or run your containers. Similar way you can run direct pod workload on the Kubernetes cluster or you can do it through definition file. Definition file is the most suitable way because then you will have infrastructure as code. So you don't need to run long, long commands on your shell. You put everything in the definition file and you run the definition file. A definition file will have these entries, apiVersion, metadata and spec. Most of the definition file will have these four entries and if you're taking example of a pod definition file, then the kind will be pod. The object is pod and it has a apiVversion: v1, which is the stable version. But you have other objects like service, deployment, ingress and you can check in the documentation what is their latest version. Like for service, it's v1. If you're using deployment, it's apps/v1. Some new object comes in, it'll have beta version. Then slowly you'll get v1 stable releases. So apiVersion and kind. Then metadata. Metadata has information about the pod, data about the data, like name of the pod or you can give labels, key-value pairs like we have tags on AWS. Now the technical part. Inspect. But before that, let's understand that's a YAML file. We already covered these concepts in Python and YAML and Python and Ansible. String dictionaries. So apiVersion value is a string, v1. Kind value is a string: pod. Metadata value is a dictionary. Key-value pairs. Then again, key and then value is another dictionary. In spec, we have the technical details. Containers, it's a pod definition file, so we'll have containers inside that. It's a dictionary and its value is a list. So you can have multiple containers, that's why the value is in a list format like - name, name of the container, image, the image. Along with the image, you can provide other information that you have, like the port numbers, right? So ports, you can specify, its value's also list. So name of a port and then container port and the port number. So this is a very simple pod definition file. If we execute it, this is going to run a pod. We'll have this container created from httpd image, running on port number 80. Okay, how do we create it? Well, we say kubectl create -f and the definition file, you can say kubectl get pod to get its information. You can see its status will be changing as it is coming to the running state. So once you have a pod running or any objects running for that matter, you can get more detailed information about it by using kubectl describe. Like we have inspect in Docker, here we have describe. Kubectl describe pod and the name of the pod. It's gonna give you detailed information about the pod. At the end, you will see events, which is very good for troubleshooting. You will see what's happening, scheduled a pod. So default-scheduler is scheduling pod. then kubelet is pulling it, okay? You can also edit a pod, but not everything in the pod is editable. Most of the things are non-editable, but there are other ways to edit it. You have deployment object. So you can say kubectl get pod, name of the pod, -o yaml. It's gonna give you much detail information of the pod in a YAML format. You can even output that content to some other file. To edit a pod, you have kubectl edit pod and pod name and you can edit few things. Okay, now, let's see all this in real time. Now, we're going to see about pod, how to run a pod. So through the definition file we said. So let me create a directory over here. I will call it just definitions. Let's get into that. So let's create our pod definition file first. So I'm just gonna say pod and I get into that. So I'll run my Tomcat pod first. Okay, from the vProfile project, the one that we have containerized, if you have already stored it in your Docker Hub, you can use that image. So I will call it a vproapppod.yaml. Okay, YAML file, three hyphens and let's get started. Okay, so kind: Pod, P capital there, apiVersion, there V capital, v1, metadata. Name, let's give name. I will say vproapp. Labels. So I'm gonna give the same label. Okay, then spec, specification, technical details. Okay, containers and its value is in a list format. So a hyphen, name, name of the container, I will say just appcontainer. Image. Let's mention the image. So I have this image imranvisualpath Docker Hub account. That's fresh Tomcat app. Looks like fresh tomato, and the tag V7. Anyways, so that's my image. You mentioned your own image that you have built in the containerization. Ports, so name of the port. I will say vproapp-port, and then containerPort, P capital, colon, and the port number 8080. Okay, this is not port mapping, this is the port number of the container that is exposed, okay? We say expose and the port number in the Dockerfile, that port. Port mapping is done very different. We are going to see that next after pod. It's done through service. We'll be seeing that next. So this is my pod definition file and that's all I really need for now. Let's save and quit. The only thing you need to worry about here is the indentation. Otherwise, in the documentation, all the settings are given. You really don't need to by hard this. Okay, time to run my pod. So I'm gonna say kubectl create -f vproapppod.yaml. Okay, it says created. Kubectl get pod. Okay, it's saying it's getting created, container creating. Let's say kubectl describe pod and let's see what's really happening. Okay, you can see the events. Okay, at the end you see the events. So from default-scheduler, scheduler is doing something. It's saying successfully I have assigned this particular node. So you can really see your pod is running on which node. So we have two node. Out of it, it's running on this node and then that gives the control to kubelet in that worker node and kubelet is saying it's pulling the image. Okay, it's not auto refresh, so we have to run it once again. Let's see. Okay, that worked. It says I have started the container. Nice. Kubectl get pod. Okay, so you see the name of the pod? It says 1/1 ready. 1/1 means how many containers you have specified, out of that, how many are running? So we just have one container and that's running. Okay, this is so cool. Kubectl delete pod to delete your pod. Okay, before we did it, few information we'll see about the pod. So when you run the describe command, you can see this pod will have an IP address. Okay, you can see the container running inside that. That's the container ID, on what port it's running. Okay, and you can, in the event section, see if there's something wrong, container is not running, you can see what is the problem. Okay, that's all really about the pod for now. We are going to see next what is service and we then going to do the port mapping kind of stuff through service and even load balancer. And then we should be able to access it from the outside world. Okay, that's all in the pod for now. I'll see you in the next video.
