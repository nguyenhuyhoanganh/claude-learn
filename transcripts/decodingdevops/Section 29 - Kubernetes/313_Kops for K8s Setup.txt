# Kops for K8s Setup

Instructor: Okay. To set up Kubernetes cluster with Kops we need to have, we need to do some prerequisites, first you need to have a domain where we are going to use it for Kubernetes DNS records. So like I have a domain from GoDaddy, groophy.in. So you should also have, if you've gone through the prerequisites video. We need a Linux vm. We can use vagrant vm but I'm going to use EC2 instance. If you wish to use your vagrant vm, you can do that. This VM will be only to set up the Kops, kubectl, shh keys, and awscli. So this will be our base machine to launch the Kubernetes cluster with Kops, this will be not part of the Kubernetes cluster. And this VM we are going to install Kops cube CTL, which going to generate SSH keys and we are going to install a AWS CLI and also a configure AWS CLI. In AWS account, we need an S3 bucket an IAM user for our AWS CLI, we're gonna need a Route 53 hosted zone, which will be our subdomain. So after creating the hosted zone, we are going to give its entry. We'll get the NS servers, name server URLs. We are going to give its entry in our domain. I'm going to use GoDaddy and I'm going to create four DNS records, NS record, which is going to point out to a subdomain to name server from AWS Route 53. So domain on GoDaddy, subdomain on Amazon Route 53. Okay, so let's do the prerequisites first. Okay, I'm on my AWS management console and I'm in North Virginia region. The code is US East one. We are going to launch an EC2 instance over here. And from there we are going to execute Kops command that will create the Kubernetes cluster. So the instance that we will launch now is not part of the cluster, we're just using it to launch the cluster. So let's launch the instance first and then we'll install all the necessary tools in that. So click on launch instances. I'll simply name it as Kops Ubuntu, AMI, Ubuntu 24, free tier eligible instance Type T2 micro is fine, we just need to execute the commands from here. Key pair will create a key, I will name it as Kops key, PEM format, create key pair, okay, network settings. Edit, we'll create a security group. We'll name it as Kops SG, same in the description, 22 from my IP. We need to assess such to this instance and then simply say launch instance. Okay? So we are going to run Kops command from this instance and Kops is going to talk to all the AWS services. And in order to do that we need to install AWS CLI in this and set the access keys. So we need an user, IM user that has the privilege to do that. So let's create an IM user. So go to IM service, users, create user, let's give it a name. Kops admin, go Next, policies, attach policies directly. And we are going to give it administrator access. Kops command is going to do a lot of things, going to touch lot of services starting from VPC. It is going to create a VPC in that, it's going to create auto scaling group access, the S3 bucket access Route 53. So it needs access to many services. So I'm simply giving it as administrator access. Go next, create user. Okay, let's create the access key for this user. Click on the user, go to security credentials and say create access key. CLI, I understand, and go next, create access key. Okay, now you can download the CSV file, but I'm going to keep it as it is. I'm going to copy directly from here and store it in my instance. So let's SH to our Kops instance yet it's public ip. Open your Git Bash or terminal ssh -i path of the key open tool is the user, add the public IP of Kops. Yes. Okay, let me clear the screen. Let's switch to root user and I'm going to run first the command apt update. Okay, apt update completed successfully. Let's install AWS CLI. So snap install AWS-CLI --classic. So we are going to install AWS CLI with snap. Okay, now let's store the access key. So we'll run the command AWS configure. Let's get the access key. If you have downloaded the CSV file, you can open that. I'm just going to copy it from here. Copy access key. Copy the secret key. I know you can see my secret key, but I'm going to delete this user as soon as the recording is completed. My region code is US east one, so I'm going to give that, format JSON. Make sure you check the region name before you enter. I wanted to create in North Virginia, US east one. So I've given us east one. Okay, next we need to generate the SSH key, SSH-keygen. Hit Enter, enter, enter, enter. Okay, now you should have the SSH key over here. This is the public key and this is the private key. Okay, now we need to install the Kops tool itself so, we can clear the screen. Let's go to the browser. Just search for install Kops in your browser and it should come to this documentation. Pretty simple step. We download the binary, we make it executable and we move it to USR local bin. So let's copy this command, let's make it executable and move it to USR local bins. Now when you run the command Kops, you should see this output, that means Kops is installed. Now we also need to install Kube CTL that we will be using later that will the command line tool to access our cluster. So just search for install Kube CTL. It's in the Kubernetes documentation, install tools and we need to install it on Linux. So let's click on this link and we can download it by using curl command. So let's click on that or you can use the package manager also like Apt or snap. What I'm going to use this method. So this command is going to download the binary of Kube CTL. So we're going to learn in detail about this Kube CTL binary CLI, how to use it. Let's download it first and then, okay, this command over here, this install command is going to make this binary executable and copy to USR local bin all at once. Okay, now let's run this command to check whether it's installed or not. Cube, CTL version -- client. Okay, it's installed, don't worry, you're going to learn Kube CTL in detail. Now let me show you the command that we will be running from here. So that is the command that is going to create the cluster, Kops create cluster. So we need to have Kops binary, which we have installed. We're going to give it a name. We'll come to that in a moment. We mentioned the bucket state. Where do you need to store the information of this cluster? So basically this command is going to just create the information of the cluster in this S3 bucket. So we need to create an S3 bucket and then with Kops update cluster command it is going to actually apply all that configuration and create the cluster. So what do we need now we need to create a bucket and we also need to create the DNS zone. I'll come to that in a moment. We are going to create this zone in Route 53 where we have all the records which say like for example API means the control plane node, they'll have the IP address of the master node, the control plane like that for the worker node also. And we can create more records over there as we need. But Kops is initially going to create all this records by itself. But first we need to create that hosted zone in Route 53. So first let's create the S3 bucket and then we will create the hosted zone. So let's go to the S3 service. I will see create bucket, geneal purpose and we need to give bucket a name. The name needs to be unique. You cannot give the same name that I have given. So I'm giving this name Kops state 956. I've given this number just to make this bucket unique. You need to give something else and simply create the bucket. That's all. I'm going to make sure I store it over here. You can download this command from the resource section, not now. I'll give you time for that. Right no, just note down the bucket name or you can anytime go to S3 dashboard and get the bucket name. Okay, now the last thing we need to do is we need to create the DNS zone, the hosted zone we call it in Route 53. So let's go to Route 53. Search for Route 53. Route 53 is a DNS service from AWS where you can create a hosted zone. Basically a place to manage the records, the DNS records. You can also purchase a domain, but the domain from here are expensive compared to other service provider like GoDaddy or other provider. But it gives lot many other benefits. So that's why it's expensive. But we are going to do some cost management. We already have the domain. So I have it from GoDaddy and my domain is HKHInfotech.xyz To create a hosted zone, a hosted public zone, you need to have a domain. So you don't have a domain here, I already have it from GoDaddy. I'll see you how you can manage this. So first click on create hosted zone. You need to give the name. So I'm going to give the name as kubevpro., and my domain name, why I've given that, I will come there in a moment. So just give that, give some name like kubevpro.your domain name, the domain that you already have purchased. Give that domain name exactly as it is. Don't give the domain name that I have given. This is mine. I'm going to go there and make the changes. So give your domain name and make sure it's public hosted zone and say create hosted zone. Okay? So when you create this hosted zone, it is going to give you a record Kubevpro.HKHinfoteck.XYZ. This points to this DNS server. So these are the four DNS servers that will hold all your records. Okay? Now the problem here is first the Kubevpro needs to be resolved and for that is going to go to this domain, HK infoteck.xyz. Let me explain you like this. So let's say this should be the record, API.kubevpro.HKHinfoteck.xyz. This needs to resolve to the IP address of the master node. So first your DNS servers wherever you're located is going to go and check at this domain. Does it have any record for Kubevpro? Now this domain is from GoDaddy and there is no record for Kubevpro over there. So it's going to immediately fail. It's not going to go here, also API. So what we will do now is we'll create a record in GoDaddy, like I have it from GoDaddy's, that is domain. So I'm going to create a record over there that says it points to these DNS servers. So when we do that, it's going to go first to GoDaddy. Okay? So for Kubevpro it is going to go to GoDaddy and in GoDaddy we'll have the NS record that points to the Route 53 NS servers, these servers, right? So then it is going to go to Route 53 and there in the hosted zone will have a record that says API IP address is so and so. Oh, this should be the master node, my bad. Now this record will be automatically created by Kops. All we need to do is go to GoDaddy and create NS record that points to the Route 53 NS servers. So I go to GoDaddy here to my domain, I say DNS. Here we have all the records. I'm going to say add new record and it'll be NS or name server. Let's give the name Kubevpro here because that is the hosted zone that we created in Route 53, Kubevpro. And we need four records over here. So I'm going to just click it three more times, NS, NS, NS, and give it here in the name here and here. Okay, now let's go to Route 53 hosted zone. Let's copy this values one by one, copy the first value and paste it over here. Get the second value. So these are all the NS servers. Just make sure this name matches with the hosted zone name that you have created. If you're all good, then say save all records. Okay, so I have all these four records created and I know this is more confusing, but once the cluster is created I'm going to show you how this actually works. So when you see it's working mechanism, then you will better understand it. Okay, now from this lecture resource, download this text file where you will have this command, open that text file and make the required changes starting with the name. So have the right name, Kubevpro.HKHinfoteck.XYZ bucket. That is the S3 bucket that I have created. So that is the correct name. Zones, so I'm going to use US East 1A and US East 1B North Virginia, node count two. So two worker node will create it. Node size will be T3 small. So this is not going to be free, but once our work is done, I'll show you how to delete the cluster so you can save money. Don't worry for some time it won't be much. Control plane size, T3 medium, yes we need T3 medium because it's going to run so many services. DNS zone, that's the same where you have the name, should be exactly same. Node volume size. So it's going to have an EBS volume and we are mentioning the size as 12 GB. That's minimum, if we don't mention it, it's going to create a huge volume size. So just to save the cost we are giving the size over here in real time. Don't give the size, let it create its own volume of its own size, which would be very much required for the production use case. Control volume size also 12 GB and SSH public key. Make sure this path is correct. So we are creating it with the root user. So in the root user home directory, .SSH ID_ed pub. So check the proper name to LS ~ /.ssh, get the proper name and make sure you put the right name over there. Okay, it's time to create the cluster now. Just two commands and we will be done. So copy this command, Kops create cluster, go to your terminal, Git bash and paste it and hit enter. Now this should create the configuration in the S3 bucket. Okay, now we can run the Kops update cluster command and we can give the name of our cluster -- and yes --- admin. But remember now we have mentioned the bucket. Now every time we run the Kops command, we need to mention the bucket, the state. There is a variable also that you can set, but that will be in the runtime only. You can export a variable and mention the bucket. But we are not going to run the Kops command too many times. So this is fine, Kops update cluster. Okay, it's the same command except we have given the bucket path -- state S3 and the bucket path. Hit enter. Now this will start creating the cluster. This is going to take some time. So I'm going to pause my recording and resume when this is completed. If you need a break like 10, 15 minutes break, you can take it now. Okay, so I was waiting like for 15 minutes and after that I'm going to run this command Kops validate cluster -- name, the name of the cluster -- state equal to S3 : // and the bucket name. So give the name of your and the right bucket name. Hit enter. If everything is good, you should get this, cluster is ready. You should see the control plane, the master node, and you should see the two worker nodes. Okay, so if you remember we also installed Kube CTL. Now what happens is when the Kops creates the cluster, it creates a cube config file. If you do LS-A, you should see a folder here, .cube. In this folder you have a file called config. Let's take a look in this file, I'll just clear the screen. I'll say cat.kube config and a lot of information. But let's look at the most important information that we want. So right now we are looking for the clusters world. So now we'll be using the command KubeCTL, we'll say Kube CTL get nodes. It'll get the information. But how does it know where to fetch all this information from? Kube CTL command goes and checks the master node for the information. Here you should have server. And here you should have the server name, with this name it is going to access it. So if you look over here, http://api.Kubevpro.hkhinfoteck.xyz. Now this needs to resolve to an IP address. If not, this is not going to work right? The other things are the certificate and login information. So let's run the command Kube CTL, get nodes and it's working. So this uses, the Kube CTL uses the Kube config file to access the information from the master node. That is the control plane node. So how is it resolving the API part? So let's go to AWS console. If you go to Route 53 hosted zone, open your hosted zone and if you see here refresher, you should have a record here, API.Kubevpro. And you should see the IP address over here. That's the public IP of your master node. There's also one more record here, API.internal that should have the private IP of the worker node, sorry, master node, control plane. So let's go to EC2 instances. You should see here a control plane node and two worker nodes. If you check the public IP and the private IP, this will be matching with the record that was created here. And all this was done automatically by Kops, right? So when it wants to resolve this api.Kubevpro.hkhinfoteck.xyz, it's going to go to this domain, which is GoDaddy here, is going to check Kubevpro. And this is going to point to the hosted zone, sorry, the Route 53 name server, the DNS server. Then it is going to go to here and check for API and it'll find the IP address. Alright, so this was done automatically. Now it is also going to create a VPC. If you go to VPC when within the co squad, the first thing it does, it creates the VPC. In the V PCs you should see a VPC for your Kubernetes cluster, it should have all the subnets and everything. Instances, it manages through autoscaling group. So if you go to autoscaling group, you should see three autoscaling group, one for the master node, the two other for the worker nodes. So for autoscaling group you should also have the launch templates. The instances do have the security group. If you go to security, you should see the security group of all these instances. So all this is managed by the Kops, right? So I recommend you take a look and once you are done, you can delete the cluster. Don't keep it running. When you need to create, you can anytime run the Kops create cluster command and Kops update cluster command to create the cluster. I recommend you delete if you are not using it because it is instance of bigger size. So the command to delete the cluster is simply Kops delete cluster, the name of the cluster, -- state. And at the end you need to give -- Yes. So this one command should delete your cluster. Again, it is going to take some time to delete the cluster because it's going to clean all the things instances, auto scaling, groove, VPC, everything it's going to delete, even the DNS records. So when you are not using cluster deleted, you can anytime create it. Just running this command, Kops create cluster command and Kops update cluster command. Once the cluster is deleted, you can also shut down the Kops EC2 instance, but only after it deletes the cluster on it is completed, then you can shut down the Kops EC2 instance whenever you need. You can power on and execute your command to create the cluster. Right then, that will be all in this lecture. I will see you in the next one.
