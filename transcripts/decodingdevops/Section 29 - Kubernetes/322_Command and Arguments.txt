# Command and Arguments

Hello and welcome. In this lecture, we will talk about commands and arguments. How can you pass commands and arguments to a container in a pod? So keep in mind, pod does does not execute your command. The container does. The container which is inside the pod will run the commands. Okay, so you are seeing right now docker file reference documentation. While we are building an image, we specify what command the container will run. When we say docker run, it will run that command which is mentioned in the CMD. So I see some example here. So we're building an image from Ubuntu, and in CMD, it's mentioned echo and some path, sorry, some string. So that is the command that your container will run. When you run the container from this image, it is going to run this command. You see one more example there. So these will be the commands that usually starts the container process. Not usually, actually, this is the command that starts the container process. There's also ENTRYPOINT. So ENTRYPOINT also does the same thing. But ENTRYPOINT has higher priority. If you have CMD and ENTRYPOINT both in a docker file. When you run the container, it's going to run first the command mention in the ENTRYPOINT, and then it will run the command mentioned in CMD. Or if they are used together, usually ENTRYPOINT will be the command and CMD will be the argument. I will make this point understand, or I'll help you understand this point. Okay, so let's say in the docker file we have CMD. In CMD, we have echi hi. So when we say docker run and the image name I have given here as printer. So I built an image from the docker file, and when I say docker run printer, it's going to execute echi hi that's CMD in ENTRYPOINT. The second example you see, in ENTRYPOINT, I have mentioned echo and not the argument. Now, when I say docker run printer, I have to pass the argument. If I don't pass the argument, well, echo will not do anything. And if there is some command that necessarily needs argument, the container itself will fail. If you do not pass the argument here, we pass the argument hi. So it will run echi hi, the last one together, ENTRYPOINT and CMD together. So when I build image with this docker file and I say docker run printer, the image name, it will say echi hi. But now I can also overwrite the argument. I can say docker run printer. Hello. So hello will be the argument. Hi. Argument will not be used. Hello. Argument will be superseding this argument. So the container will run the command echo hello. So that's how command and entry point work together in a container. In Kubernetes, we run container inside the pod. If we want to execute commands or override arguments from the pod definition file, how do we do that? We'll see that next. So I'm in Kubernetes documentation commands an argument for a container. You see that said container, not Pod. As I told you at the beginning of this lecture, container runs your command pod or container is inside the pod. But we use Pod in Kubernetes directly, we do not use container. So we have to mention commands and argument at the pod level in the pod definition. So this is one example here, normal pod definition file. And we have the image debian command. That's how you can mention the command in square bracket, double quotes. You have given the command here print env, which is Linux command which prints variable and args that is going to pass the argument. So it's going to be print env, host name and Kubernetes underscore port. These are two variables it's going to print. So let's test that. Let's copy this. OK, I'm in the kops VM. I am going to create a file, I'll call it as com.yaml, that's all. And I'll just print a paste here all those things and save and quit. And let's apply that kubectl apply. Okay, executed that kubectl get pod. Okay, continue creating. Let's wait for a moment. Okay, now it says completed not running. Well, if the process is continuously running, it will say running. But our command was print env, which is just a command it executes and then it's dead. So container is dead. kubectl logs and I will give the name of the pod. And there we can see. So this is what it has printed, right? Host name of the container which is command demo and that is the Kubernetes port. Keep in mind completed status you will get when the container has completed the execution of the command. And these kinds of containers are helpful when you want to run some processing. Maybe you want to run some scripts that does some work for a specified period of time that will be completed. This won't be the container like MySQL or Tomcat or nginx that continuously runs. So these kinds of commands and argument is helpful when you have use cases, when you want to run something which returns some output and that's it, your work is done, that time, commands and argument will be helpful. So I want you to do some experiment with this. Instead of printenv, use echo and put some variables there, or just some strings. I will show you one more example back here in the documentation. Here we have env This is env, used to specify a variable. So variable name is message and its value is hello world. So now command is echo and in arguments we have given a variable. That's how you can give that variable dollar message. So its value will be basically hello world. So test it yourself. Try this, create another product file, give your name, test this, see how it works. And that is all in this lecture. I will see you in the next one.
