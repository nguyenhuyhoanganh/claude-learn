# Replica Set

ReplicaSet. This one's easy to understand. ReplicaSet maintains a Replica of your pod. What does that mean? And why do we really need it? Okay, so you have a pod running on a node and users are accessing it. The pod is running, let's say, some web application. And for some reason the pod goes down, the users won't be able to access the service. And that's it, end of the story. Someone needs to login, delete the pod, recreate it, fix the problem. But if the pod is running with ReplicaSet ReplicaSet object you mentioned, these are the pods. I want to run. These many Replicas I want to run off this pod. ReplicaSet will do that for you. Any pod crashes, it can recreate a new pod for you. And you can mention Replicas for scaling. You can add more pods into the ReplicaSet. You can remove pods from the ReplicaSet. And when I say Replica, it doesn't really need to be really Replicas. You can just say one also. Replica One, you will see. If I have to mention one, why will I use ReplicaSet? Well, for the health checks. If your pad goes down, you don't need to manually recreate it. ReplicaSet will do it for you automatically. And the best part is you're mentioning more. Replicas scheduler will distribute your pods across multiple worker nodes. So in an event of the entire node crashing, pods running on that node can get recreated on healthy worker nodes. If you have pod running on a node without ReplicaSet, just you created a pod and that node goes down. Pod goes down. That's it. End of the story. But if it is created with a Replica Set, it will recreate that pod for you. Okay, let's see how to do it. Let's go to the documentation. Okay, I'm in. Documentation, says purpose is to maintain stable set of Replica pod running at any given time. It is often used to guarantee the availability of specified number of identical parts. It's a guarantee. You said three Replicas. It's going to give you three. If something goes wrong with one or two, it will recreate. We'll always maintain those three Replicas. And you can read about how it works. When to use ReplicaSet. It's written over there. I just want to take this definition file directly and let's bring it in sublime text editor and read this. Let's copy this. Okay. All right. API version apps/v1. Not just v1 This is new style of API versioning for the newer objects. And older objects like that will have but the newer object, like Replicas Set deployments ingress will have API versioning in this format. Apps. v1 for all apps. kind ReplicaSet, metadata, name, labels. You can give as many as labels you want. We use labels for filtering, inspect. That's the specification. Replicas Three. You can mention the number of Replicas. Let's just keep that for now. Selector match label. And I'll come to that in a moment. Let's see. In templates, you have this all Pod information from metadata till here. So metadata. This is about the label of the Pod tier frontend. Now this label is matching exactly the same match label here. So this simply means that any Pod that has this label that is in this replica if you have existing Pod with the same label you mentioned replica Three, you already have one Pod with the same label. Then this ReplicaSet will create just two Pod. Because one already exists with this label. So that's how it selects the Pod. That's why it's in the selector section. Spec container, name of a container and an image. So let's just copy this. Let's come to a terminal. If you have kubectl set up locally, you can just do it in your own machine. I'm in the Kops VM. I'll say Replica. I'll just say Replica YAML. Let's put everything here. Let's save and exit. If you choose to change the names like the name of the ReplicaSet or the name of the Pod, you can do that. I'll just use the same one. I'll say kubectl create or apply. Okay, I will run kubectl get rs, ReplicaSet. Short of that, it says OK design. Three current three are ready. kubectl get Pod. All right, so you see three Pod. Now you'll see why this three three times. So decided this is what I mentioned. This will be my Replica. Current are three. How many pods got created? And out of that, how many are ready? I mean, your Pod may have some problem, right? So you may have current as three and you may have already as maybe two. Maybe one pod is still getting created. Or maybe having some problem not getting created. It doesn't have resource. There could be many problems, right? So our end goal is to have three ready parts. Okay, if you have three pods and you see that's the name of the ReplicaSet hyphen and it gave some ID there. And one by one out of one container, one is running. If you have multiple containers, you can have three container, maybe in a pod. And you can see out of three, one is running. It may be like that if all of your container doesn't work. But we have one container in the Pod and one is running. Okay, now that's the Replica. We can scale up or scale out, or we can scale in or scale down. But before I do that, I want to just delete one of the pod. kubectl delete pod, not one. I'll delete two parts. Delete this pod and I'll delete this pod. Let's assume these two parts are running on a worker node. And that worker node crashed. Okay, so when it crashed, it's going to delete the pods, but then the ReplicaSet. How that was so fast. You know, these are two new pods. Look at their age. 4 seconds. How quickly they got created. Because it's just a process time. How we can scale down or scale up. The best way is always declarative. So better always make the change in the definition file. Change the Replica size. I will say Replica I want maybe five save and quit. I'm scaling out or scaling up. I say kubectl apply. Okay, let's say kubectl get pod. I have five now. Like that, you can scale down other ways. Is kubectl through command line imperative way we'll see scale --replicas equal to I will say one and rs slash the ReplicaSet name frontend And that's my ReplicaSet name. Okay, let's say kubectl get pod. You see that scale down. You can again scale up. But this time, I'm going to scale with another way. kubectl edit Rs. And I gave the name of the ReplicaSet. But you can do it two ways. You can say RS slash frontend like this. Or you can say rs space frontend. Okay, now let's find Replicas. Okay, replica One. I'll say I need Replica Two. And save and quit. That's it. Done. Okay, these last two methods this one and this one is not at all recommended in production. Always be going to do it through definition files, through manifest and we apply the changes. And if you are thinking, hey, where did this command come from? Do I need to remember it? Well, I did it so many times, so I remember it. But trust me, you don't need to remember it. Let me take you to kubectl Cheat sheet. Okay, it says Cheat sheet, but this is from official documentation. So we'll go through the cheap cheat sheet later. I want to simply take you to command where I scaled it. Let's find out. There scaling resources. So, Cheat Sheet is many commands in one place which you regularly use. It's really very good documentation. You should really bookmark this. I use this a lot. So that's how you can find those commands. Okay, we have the documentation for ReplicaSet here. You can read about it. But it's pretty simple. Just to maintain the replica, you use ReplicaSet. Once you're done delete your ReplicaSet, kubectl delete Rs. Because you cannot really delete pods here, they will get created again. So you just need to delete the ReplicaSet and that in turn will delete the pod gone, right? Play with this. Wrap this up and join me in the next video.
