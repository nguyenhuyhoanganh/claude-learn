# Secret

Hello and welcome. In this lecture we will talk about secrets. In previous lecture we have seen ConfigMap and we see config map stores variables or files in clear text. So if you're storing things like passwords or your docker registry credentials, which you will inject in your pod definition file, then these informations which are sensitive like passwords will be in clear text in your repository, because all the definitions file that you write will be in your repository, in a git repository. So who have access to that git repository can see the passwords in clear text, which is very bad. So you can store variables encoded or encrypted by using secret and then you can inject it safely in your pod. An imperative method. First kubectl create secret generic. Now there are different types of secret. If you just want to store the variable encoded, the type will be generic, secret name from literal variable equal to value. And you will think, hey, it's same as ConfigMap. Well, almost. But when you create this, what happens when you check the secret, you will see its value is encoded here. The value we have given is some complex password. But if you see the value is something gibberish. So Secret will encode the value and it's encode, not encryption. I will show you quickly how you can encode any text. So I'm on a linux shell, you can do it in git bash, also echo -n And I will say secret pass. And I'm going to pipe it with base64. And that's the encoded value of this. And it is encoded with base64. You can decode this also with base64. So you really see it's, not encrypting it. So if I really want to decode this, I'm going to just say echo single quote. I will put this, there is two equal to there, should not miss that and pipe it with base64 and give an option there. See that it's decoded. So you will say then how is it safe if anybody can decode it? Well, it will be stored into your Kubernetes control plane. So if you have access to that, then yes, you can get the value and you can decode it. But this is more of a best practice to avoid accidental exposure. But I will tell you this. If you really want to encrypt, there is a way which is out of the scope here. You need to use encryption keys and you can encrypt this data as well. Just notice there is a method in Kubernetes to encode or also encrypt your data. Okay? So that's imperative. But if you want to do declarative, then you have to first encode your data. And then when you create a secret declarative kind secret type Opaque data key and the value that you give, you would have already encoded it, otherwise there's no point. And then in the pod definition file, you can read it like this. You can say env from secret reference in config map, we have config map reference and the name of the secret. Now one secret can have multiple secret keys. So if it has multiple, it's going to export all of them. But if you want to be selective, you can say value from secret, key reference, name of the secret and the key. Now this is the simplest form of using secret. Secret has some other types as well. Let's check that in the documentation. Okay? The secret documentation as in not the documentation secret, it's the documentation about the secret in Kubernetes and it's pretty long. Over here you have types of secret Opaque, that's the one we have just seen in the slides. You can also store service account token which will use to authenticate docker configuration. Docker configuration. In JSON you'll say what is this docker configuration? Well, this will be the information of your docker registries. You can also store ssh-auth, tls certificates, Opaque We have just seen this in the slide. Okay, this one is really popularly used to store your docker registry credentials. So when you store your docker image into Docker Hub or any other registry like docker Hub and it's a private registry, so far we just mentioned the image name and it pulls from the docker hub that those are the public images, public repository. But when you store your project images, those will be in a private registry and you need to pass username passwords and email addresses this information. So first in the Kubernetes, which is going to pull your image, you should create a secret of type docker registry. Give a name and you can say the email address. You can mention the username, you can mention the password and the server. Also if it's not Docker Hub, if it's something else, your own custom registry. Many companies use their own registries also. So you can mention the URL and the port number. And if you see when you check the secret in json information, json output, you can see it's stored into json data and it is encoded and you can decode it with base64 -D option. Okay, this documentation, I'll put the link in the resource section, how you can pull an image from a private registry. First you need to create the secret. So creating the secret. Now this is a generic secret and in this you have information from JSON file. You can directly create the secret of type registry and you can pass all the details that will create a JSON file or create a secret by providing credentials on the command line. Secret of type docker registry and you mentioned all the details, docker username, docker password. Now using that secret, that docker registry secret from the pod is pretty simple. When you mention your container name, container image, you need to say image pull secret. So where should we get the information to login to the registry? So you can give the name of the secret that you already created. It's going to use information from the secret login to the docker registry and fetch your image. This is a very popular use case or method to pull your image from private registry in Kubernetes. Okay, let's do a quick example or exercise of secret. First we are going to encode two text for username and password. I'm going to say n and I will say username admin and let me encode it and let's do it for some password. Also I'll say my secret pass. Okay. Now let's create a definition file. We'll put all this information in the secret and create it vim myscret.yaml okay, that's the file. Pretty simple, kind secret name of the secret. This is the data to key username and password. And these are the encoded values that I have already encoded. Type Opaque I will put this in the resource section. But you first encode your username and password and then put it in the file. Let's create it kubectl create -f mysecret.yaml That should create the secret. Now let's create a pod. vim readsecret.yaml. Okay, I'm creating a container. Let me tell you, I got all this from the Kubernetes documentation. I will show you that there from the secret documentation. These are two variables that will be exported in the redis container and the value is coming from the secret, my secret. And that's the key username. Same as another password or sorry, another secret. So you just make sure you create the secret with this name. And the keys are this. If you make change if you give different secret name, make sure you change it over here. So based on this definition file, I have already created my secret. Let me save and quit this. Let's create it. kubectl create secret. Okay, let's check our pod. Okay, our pod is running. We will connect to it. Okay, kubectl exec --stdin --tty That's our pod name. And /bin/bash. Let's hope we have /bin/bash in redis, it's there. We are in. Now let's do echo dollar username. That was the key name. The variable name is different. Echo dollar secret username. See that we encoded that right? But it has decoded it secret password there. So again, last thing, let's go back to our documentation. See that was the variable and its value is coming from the secret. This specific key. I know this one and the previous video which is about variable configuration and secret, It's little complex. But you really need to know this as a DevOps Because when you run your pod you will inject data into that. You will inject credentials, you will inject secret encoded variables or just normal variables or configuration files. And for that config maps and secrets are very useful. Now, secret goes beyond this. Also you can encrypt as I said for that you need encryption keys. That is beyond the scope over here in this lecture but before you go over there, I want you to do very good hands on on config map and secrets. So try this. Test it, wrap this up and join me in the next lecture.
