# Helm with AI

Okay. So finally in this lecture we are going to use Amazon Queue to develop the helm chart. So the basic helm charts that we created in the last lecture to learn the structure just delete the delete that folder. So I'm going to delete this folder WP hyphen chart. Okay. So all I have are those are these Kubernetes definition files. And I'm going to ask Amazon Cube Developer. So click on this open a new chat. Let me expand this. So we just need to write the prompt write prompt. So it reads our definition files and create charts for us. So this is my prompt. Create helm charts from these Kubernetes definitions file. Now since it's in the same folder it's single folder root folder I have all definition file. I'm saying these Kubernetes definition file. Otherwise you can mention the folder name even if you don't mention. If you just ask a question to create helm charts, it will find your Kubernetes resources and create charts. I'm saying use release name in the metadata. So when we deploy our helm charts, we'll give a release name. So I'm saying that in the metadata. Use that name, replace other hard coded values into variables and add those variables in values. Dot YAML file. Once you write this prompt hit enter. It's going to take some time. Started looking for the files. Sees one directory listed. Eight files started working. Started generating chart dot YAML. So let's take a moment. Oh wait, there is some error. So it says let me create the directory structure first. So it is asking us to create. Run this command. Now I can just say run. So it creates that folder structure. Okay it failed. Let's run this command once again. Okay. That command also failed. Something is wrong here. Okay. It's using MD command to create the folder. Mkdir is not working. I don't think it's using the bash shell. macOS user won't have this problem. Okay, anyways, just look for the question that is asking. Execute them. Once you create the folder structure, the rest it will take care of. Okay, so I'm going to pause the recording right now and resume when this is completed okay. The work is completed. Let's take a look. So we see a folder WordPress hyphen chart. There's one empty folder here. I think this was created because it was trying to create the chart. Anyways I removed that. So WordPress hyphen chart. Here you have values dot YAML file. You see all the variables defined. Here you see chart dot YAML file. That's the version of the chart. That's our WordPress application version which we're going to change in the templates. We should see all the templates for starting with MySQL deployment. So as I said in the release. So in the metadata give release name. This will be the name of the release. That's how I want to name my release or my resources. So if we say the release name is WordPress. So here it will be replaced by WordPress. Hyphen MySQL okay. The image you see those values. Environment variable MySQL root password. The value is coming from a secret. And the secret name is this release name hyphen MySQL. Hyphen secret. So that will be the secret name. Let's go to the secret file WordPress secret. See that? That's the name of the secret release dot name. Hyphen MySQL secret. And from that it's looking for the keys. These are different keys. And this will be replaced with these values. So values dot secret dot MySQL root password. Let's go to values file values dot secret dot MySQL root password. So this is the text. It's in clear text format. We should be encrypting this. But that is for us to change things. But this is going to be encoded in the secret here with base64 encoding. And that will be assigned to this key. And this is used in the MySQL deployment there. Okay. So it has created a basic structure for you which you can use. We can improve it. Also we will test this template first and then we'll make the changes. But one more interesting thing over here with values dot MySQL dot resource. Let's take a look over here. So it is saying that this resources will be replaced with this template values dot mysql dot resources. So values dot MySQL dot resources. It's basically empty here. But if you define some structure over here, like if you give some variables like this, then this whole section will be taken or sorry will be referred. And then you can give the variable name directly over there. You don't need to say values dot mysql, dot resources, dot blah blah blah. You are already referring it to this dictionary and then you can directly give the name of the variables over there. Okay, so go through all these files one by one MySQL PVC. Here it says this is condition based. So if values dot MySQL dot persistence enabled if this value is true. Now open both the files side by side. So the condition is if values dot MySQL dot persistence dot enabled. If this returns true which is values dot mysql dot persistence dot enabled is values true. If this is true, then only this template will be created. And keep in mind when there is an if in templating of helm, there will be an end also. So in this between block this is condition based. The whole file basically. So we'll look more into this one. And we're going to add many more things into this templates again with code Assistant. But before that, let's see how to deploy these charts. Use it and then we'll make more changes. Let's open the terminal I'm switching to git bash. Make sure you are in the right folder. You should see the chart over here WordPress chart. First we're going to check the syntax. So the command will be helm lint. And we give the path of the folder. So it's almost perfect. We have no mistakes I have tried many other code assistant, and it's only the Amazon Q developer that has given me an error free chart. Other code assistants I have tried, and usually there are some or the other mistakes. Okay, now there's another command. We can really check if the templates are working or not. We can do helm template, template and the path of our chart. Is going to scan through all the templates and see whether they are now getting the right variable's value or not. You can check it yourself. If you find any mistake, you can look into that template and check the variable value. If you're good, you can deploy it. So let's now deploy this so the command will be helm. Install the name of the release I'm going to give it as WP and the path of the chart hyphen n I'm going to give a name space here wp hyphen ns. And this namespace does not exist. So I'm going to give one option here which will create the namespace also. So with this command first it's going to create a namespace with this name and deploy chart with this name wp name by reading all the templates from here. So let's execute this. Okay. It's deployed with one warning. It is saying Kubernetes dot ingress class is deprecated. Please use spec dot ingress class name instead. We can check helm list. We need to mention the namespace hyphen and wp hyphen nss. So we see the revision number. We see the time stamp status as deployed chart name and its version. Let's look at all the resources kubectl get all hyphen and wp hyphen nss. Okay so we see two pod running. We see two services. We see deployment replica set for ingress. We need to just do get ingress. Okay I think there is a problem in the ingress. The class. It is not showing as nginx. And this could be an issue. Let's describe this ingress. So actually we see the ingress working. And I mean you see the address of the load balancer. If you don't see it yet, wait for some time. It will be there. It takes some time. But ingress class name it says none here. It should be nginx. So we are going to go to the WordPress ingress. And here we are going to add one setting. Ingress class name C colon nginx. Save this. And then let's change the version of the chart because we are going to upgrade it. So from 0.1. 1 to 0 .1.1 sorry, it was 0.1.0. So I'm just incrementing it by one. The third one will be for the bug fix. So once we upgrade the chart chart and the version of the chart and we put this setting, make sure you save all the files. And then let's upgrade this. So we have to run this command helm upgrade the name of our release. Okay, this option, I should remove the path of the chart. Oh, wait. I need to give the path of the chart. I'm sorry. Hyphen n and the namespace name. Hit enter, so it's going to read the changes and apply it. Okay. It says revision two is deployed. Let's check the ingress once again kubectl describe ingress okay. Now we see here ingress class nginx. Okay let's list the helm. Helm list. Now I need to give the namespace hyphen n wp hyphen ns okay. So the chart version we see that's WordPress 0.1.1. This one is the app version okay. Now in order to check it from the browser, you need to add this load balancer endpoint in your DNS record with this name WordPress. So you can do it two way. Either you can go to your domain domain registrar like GoDaddy and add the entry WordPress maps to this load balancer endpoint, or you can open the host file in your computer and make the change. Okay, I've logged into my GoDaddy account. I'm going to go to my products, I find my domain and I'm going to say DNS. The UI of this might change here and there. Just remember you need to go to the DNS records of your domain. See add new record type will be CNM because we are mapping a name to a name. So WordPress, that's what name we have given in the ingress, right? WordPress maps to this one. So select that and do right click. It will get copied and paste the value there and say save. Now after doing doing this this might take some time. It might take a very long time. Also based on the ISP internet service provider that caches these records. So run Nslookup command. If you are not able to resolve it, try it with Google DNS. I'll show it. So first do like this Nslookup. Nslookup. And your host path. If it resolves well and good. If it does not resolve. Try it with 8.8.8.8. That's the Google DNS. So if it's working with this and it's not working with this, that means you need to use this in your DNS record. I mean the domain resolver. I'll show how you can do it on Windows Machine. Or you just simply wait for some time. It will work. So in windows is how you can do it. Just run windows R and type this NP dot cpl. Find your adapter Wi-Fi or Ethernet. What are you using? Right click go to properties. Click on this Internet protocol version. Go to properties here and over here. Select this option and enter the DNS. As you see on my screen. 8.8.8.8 and the second 18.8.4.4 and say, okay, close this and then you can try once again or before this. You can execute this command also in your PowerShell. Open PowerShell as administrator and run this command ipconfig slash flush DNS. Then you can run the command nslookup, WordPress or whatever your domain is and it should work. Now use this URL in the browser. So open up your browser, enter the URL and you should see this page. WordPress. Select the language. Go. Continue. Fill in this detail like I'm going to put site title as DevOps. Some username as admin password. Also, I'm going to keep it simple for now. Confirm and some email address over here. Discourage search engines from indexing this site. Install WordPress all this is set. Click on login and enter the username and password that you have used. I have used admin admin login and you should see your WordPress blog up and running. Okay, so this is all well and good, but this can be improved further. I'm talking about the code for the helm charts. So to close everything here and go back to Amazon Q Developer Chat and ask it to improve my code as per developmental standards or best practices, that's what I've put in the prompt. Improve helm charts as per developmental best practices. And let's hit enter. Let's see what changes it makes. Okay, it took some long time, but let's see what changes it made. Let's go to our files starting with MySQL Deployment.yaml. So in the name it's using include which is going to include or import from outside. So it's going to give the name WordPress MySQL dot full name. That will be the chart name for labels. Also it is using include okay where you see this indentation. This is basically to give indentation for this is so you know the correct YAML syntax should come over here. So four lines of indentation four columns of indentation. See most of the places that use include to give the chart name there. Here we see width values dot mysql dot resources nothing same as previously. Go to values dot mysql dot resources okay, their values dot mysql dot resources. So this thing this entire block should appear over here okay. Over here. So all the variables you know in that indentation will should be coming over here. Let's look at few other files MySQL PVC. So here we have condition and again include MySQL service similar thing WordPress deployment A similar thing we have include we have direct values like values dot WordPress dot service port. And this is what the resources gain. Importing the entire block over there. Okay. We have condition if this in WordPress ingress values dot ingress dot enabled. If this is true then this template will get created in annotation with values dot ingress dot annotation. So the entire block will be imported over here from the values dot YAML file. All right. So the reason I'm going through these new new settings is to show you how you can learn from the I. We are using it as a code assistant. We can also learn from the I the best practices. So do check all the files and see what are the best practices. There's one way of mastering your technologies or your skills. The other way is going through the documentation and experimenting with many things, right? Instead of that, you can use the help of AI tools like ChatGPT, Amazon, Q, GitHub, Copilot, Cloud Journey. There are many code assistants now, so when you ask it to improve your code, it is going to show you suggestions, going to give you things that you don't know. So that way you learn new things. Okay. So I'm going to stop over here. We can go on and on. On this one I want you to do a helm link to your new charts. You know the changed charts and do helm template to see how these values are resolved. Then uninstall the WordPress chart, install a new release, test it, play with it. It's impossible to master all this in just one lecture. It will only work out when you keep practicing. When you keep doing it. Repetitive things, repetitive changes. When you experiment, you're going to learn. But use these AI tools as much as possible. Code assistant and learn from them. That is the way forward in today's time. So if you're done here, you can uninstall the charts. You can delete the namespace. If you're not going to use the Kubernetes cluster, you can delete it. I'm going to simply delete the Kubernetes cluster. I'm going to create it later for other lectures. So I'm going to say delete cluster. It's going to delete the cluster once the cluster is deleted. When whenever I need it again I'm going to do Kops create cluster command to create it once again. Yeah. So I have already everything ready. I'm just going to use Kops create cluster command to create whenever I need it. Now one last thing. Also go through the helm documentation. The really good documentation. You can go through the introduction quick start guide, which most of the stuff we already did go to the topics, how to's, chart development, tips and tricks, best practices you have, chart template guide you have. So whenever you need to develop the helm charts from your Kubernetes definition files, you have help from the code assistance assistant like Amazon Q copilot. You also have the documentation, so take your time to explore this. I hope you have learned and enjoyed a lot in this lecture. I will see you in the next one.
