# Deployment

Hello and welcome. In this lecture we will talk about deployment. This is one of the most used object by us. When I say us, I mean DevOps. If you see that most of the DevOps work is involved around deployments, making changes, making regular changes to the application, to the code. And by using deployment object, you can upgrade your apps, upgrade your pod images, basically roll back, something goes wrong and it's going to be gracefully in a rolling fashion. So by definition it says deployment Controller provides declarative updates. You declare it in a definition file for pods and Replica Set. So deployment uses Replica Set. Replica Set, we have seen in the previous lecture. It makes changes to your pod or it maintains the Replica of your pod. So you give a definition of deployment. This in turn creates Replica Set. And Replica Set will maintain your pod. And when you want, you can make changes. You can define the desired state. I want this particular image tag, these mini Replicas. And when you apply this deployment controller will change the actual state, which could be an older image tag to the desired state, which could be a new image tag at a controlled rate. So it is going to do it in a rolling fashion. So if you have three Replica of the pod, it's going to do it one at a time. Changes on the pod will always happen by deleting the existing pod and creating a new pod. So that delete and recreation will be in a controlled rate, one by one. And as I said, deployment creates a Replica Set to manage the number of pods. Okay, so if you say currently your image tag is V One, which you mentioned in the pod definition container information container, you mentioned image so and so and so and so tag. And you can mention in the deployment that I want to upgrade it to a newer tag, maybe a v2 tag. You have a new tag to your image in the registry and you apply that, it's going to upgrade it one by one. And if something goes wrong, it can also roll back. How does it so easily roll back? I will show you that. If you remember auto scaling from AWS, it does it similarly. So that's a sample definition file of deployment. We will check it from the documentation also. But I want to just compare this definition with Replication Controller or Replica Set definition and you see they are almost same. Pause the video and check compare them. Few differences are there which you will spot like selector. Here we have match labels and then the label of the pod. Here selector just have the name of the label. Now let's go to the documentation, get some sample deployment definition and test it. Deployment. You can read about it. There are different use cases where you would use deployment. You want to mention a new state of your pod. You want to roll back, you can scale up, you can scale down. You can pause the rollout. Creating deployment. That's the sample definition file. Let's copy that. Okay, I'm in my kops vm. I'm going to create here deployment YAML. And I just paste everything. If you have existing Pod with the same names, kindly delete them. Let's check it one by one. API version apps/v1 just like Replica Set kind deployment, metadata, name of your deployment, label of your deployment spec. Replicas Three. This information is for the Replica Set. Selector match labels. That's the same label of your pod in template. You have this Pod information here from metadata till here, it's all Pod information. So label of the pod, specification of the containers, container name, container image, port number. I will just save it and run it. kubectl -f apply deployment.yml. There it goes, kubectl get deploy. And this, as I said, we'll create a Replica Set. So you can say "rs" and you can find the Replica Set. That's the deployment name. And that's the name of the Replica Set. And Replica Set will in turn create Pod for us. Okay, I am going to describe this kubectl describe pod and take a pod. Let's say describe or you can do YAML or Hyphen JSON. I just want to show you here this one image. That's the tag. One dot, 14 dot two. And I can make changes to it. Let's go back to the definition and let's see. Okay, scroll down. You will see updating a deployment. And that's the command kubectl set. Image deployment names, deployment NGINX deployment. That's the deployment name, NGINX. This is the container name, not the pod name. Container name equal to the image and the tag. Now, I know this is imperative, and I told you not to do imperative, but you can still test it. The best way is just change your definition file and apply it. But let's take this for now. I'm copying it all back to my terminal. One dot, 14 dot two. That's the tag. And one more thing. Their container name is NGINX. So we have to say nginx equal to and the image name. Let's clear the screen. Oh, I just paste it and just executed it. Okay, so now I have mentioned the change here. Let's quickly say kubectl get deploy. It has done it properly. Quickly, let's say kubectl get pod. 9 seconds, 15 seconds, 21 seconds. So it just happened right now. It changed. Everything is gone and recreated. That happened very fast. So run kubectl get pod, immediately. So you can see how it rolls. Let's describe one of the pod. And this is not going to be the pod that this pod has already deleted. These are the new pods. So better you give the new name and let's see the image. You see that the tag is updated on all the pots. Basically deleted the pots and recreated a new one. kubectl get rs Now here you will see how it does it. You see two replicas here. Now, this is the older one. Three minutes and 28 seconds old. When we created deployment, it created this replica set. Now, when I made a change, it created a new replica set. So it will slowly make this to zero. And slowly it will increase the number over here. Now you can roll back this. You can go back to the previous replica set. And for that, let's go to the documentation again. Okay, this command, you can check the status of your rollout when you did, what happened? get rs You can see two replica set. We just seen it describe deployments. You can describe the deployment. And there also you can see the image tag. Okay, rolling back. A deployment. How do you roll back? Well, one way is directly you just do the same command and you mentioned the image tag. It's going to create third replica set. But if you just want to just roll back to the previous replica set you have. Come on. Okay, just going to show you the history of the deployments. Yeah. Rolling back to previous revision. Undo. Undo will just roll back to the previous revision, lets take that, Oh, I just did it rollout undo. And let's say kubectl get rs. I see. Now the old replica set has gone to zero. Sorry. The new replica set has gone to zero. The old replica set got new pods. kubectl get pod. And let's describe a pod. And this time, I'm just going to grip the image name pipe grip image. You see that? That's the older tag. You can also see all the previous revisions. Rollout history. And you see revision number two, revision number three. And you can also roll back with the revision number. So you can say Roll out, undo. And at the end, you can say F and two revision. And you can give the revision number. It will roll back to that particular revision. You may have 1015 different versions revision numbers. And you can roll back to any of the revision number you want. Scaling you can scale by using the scale command. Scale replicas in replica set. Also, we did this. We set "rs" and we give the replica set name. Same way here we said deployment and the deployment name replicas and the number of replicas. One final thing. Let's delete our deployment. kubectl get deploy. Delete deploy and let's give the name. As I told you previously, I keep repeating myself, but it's very important. You should do everything through definitions. File manifest imperative is for learning, testing purpose. But in real time, especially in productions, do it through definition files. So go ahead, spend some time. Get your hands dirty. Really dirty. This is the object we use a lot. Make sure you understand it well. Very well. Clean the deployment and join me in the next lecture.
