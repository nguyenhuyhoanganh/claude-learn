# Kube Config

Welcome. When I first executed my kubectl commands, in few minutes, I started wondering how this kubectl is connecting to the Kubernetes cluster. How does it know where is my master node and how does it get authenticated and get all that information from the cluster or even create information in the cluster? Well, the answer is kubeconfig file. When we created the cluster by using kops or by any other method. When we create the Kubernetes cluster, we get a file called kubeconfig file. We'll see where it is located. But this file contains these information cluster information users that it is going to use to access the cluster. And it's plural. So you can have multiple clusters, multiple users namespaces, which we'll see what are they in next lecture and authentication mechanism. Like when you want to do SSH, you need the IP address, you need the username and you need the password or the login key. Like that. kubectl needs cluster information, user information, authentication mechanism and also namespace. This is how a kubeconfig file will look like with actual information. This is just a sample. So you will have clusters information and you see it's in a list format. So you'll have multiple cluster information, you'll have users information, multiple user information context. Context basically tells for this particular cluster. Use this user to log in and also it will have user login information the authentication. Now let's see our actual coup config file. Let's go to our terminal. I'm inside this kops VM. This is easy to instance from where I created the cluster and when I created the cluster. In my home directory, you see a directory called kube. My home directory means the Ubuntu user home directory. I'm over here and over here, we have dot kube. And in the dot kubedirectory we will see a file called config. Let's open this file vim kube or we'll just use less kubeconfig. All right. Not so easy to understand at first glance, but it's really those information we talked about. We have clusters and this is one cluster and this is the certificate for that cluster. This is the URL. This is the URL of your master node. API server APS server location. Now, why is this API server information API server lives in the master node. And when we created the Kubernetes cluster by using kops, it did one more thing. In the route 53 hosted zone, we see one record that is created by kops API and its value is the IP address of our master node. Read that IP and let me take you to the EC2 dashboard master node. And you see the IP address and the API server we know lives in the control plane. In the master node. kubectl connects to the API server. That means to the master node. Let's go back to your terminal. So api.kubevpro.groophy.in basically means the IP address of our master node name. That's the name of the cluster that it has given. So this is all cluster information like that. You can have multiple cluster information in this file, you can have multiple kubernetes cluster and you can feed the information over here in the same format. Then let's go to the end of this file shift g. Okay, see lot of things. Let me scroll up users. So that's the user name. It is same as the cluster name user. And this is the certificate for the user. And then here we have the key. So this will be used to authenticate. It's going to use this like when we do SSH, we have username and we have a login key similar to that. And then in the middle of this we have context. Context really marries cluster with the user. That means for this cluster use this user. Don't get confused, the same name. But this is username and this is the cluster name. And then you have current context. Now you can have multiple cluster information, multiple user information and multiple context. But kubectl command, when you run by default, it will use your current context. If you wish we can change the context also while running the kubectl command that I will show you in the cheat sheet section how to do that. So one last thing before we move on to the next lecture. Documentation. So I simply search for kubeconfig and I came here. This is the documentation of the kubeconfig file. You have detailed information about what is context kubectl config used context. You can set a context, you can say kubectl configview to see your context. Let's run that command so it shows your context and you see the certificates are all hidden here. Let's go back to the documentation again, few more things. You can pass a queue config file you can mention by default it will use kubeconfig file. But if you want, you can specify different location of your kubeconfig file also by using this flag context. There can be many context. When you work in a large scale environment, there'll be multiple contexts and you can mention which context do you want to use. You can also mention flags like I want to use this user and I want to use this cluster. And if your cluster is really behind a proxy server, you can mention a proxy server URL also. So take a look at the documentation before you move to the next lecture. I will put the link in the resource section. OK, one more last thing before we move to the next lecture. I'm back in my kops VM and I'm going to do cat kubeconfig or better you can be in any directory and you can run this command I will give here tilde that means my home directory config file and I'm going to copy this file, I copy the whole content, I'm going to open another git bash terminal. I will create a directory. I'm in my local machine on my laptop. I'm creating a directory kube. And in that directory I'll create the file kube config and I paste all the content here. Make sure you copy and paste properly, otherwise this will not work. Save and Quit this is the documentation of how to install kubectl on Linux. Or you can also see how to do it on Windows. Install tools kubectl on Linux, on Mac OS or on Windows. I have used chocolatey to install on Windows. That's the command I have used choco install kubernetes CLI so based on your operating system, you can install kubectl. You have kubeconfig file and you can use kubectl from your laptop. So once you install kubectl, then you can say kubectl that's tested kubectl get nodes like that and then you can continue using your kubectl command from your laptop. Now, kubeconfig file, you can have it anywhere. You can use it in Jenkins to deploy or apply or make changes to your kubernetes objects. You can have it from ansible. You can run it from anywhere. You need to have kubeconfig file. We will see later how to use it from other tools like Jenkins also. So in this course you can see me on the kops VM or you can see me working on my laptop. Just keep in mind I have my kube. Config file saved in this location. That's why I'm able to use it. So set kubectl and kubeconfig file on your laptop. Check it, test it and join me in the next lecture.
