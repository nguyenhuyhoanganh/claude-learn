# Service

Service. If you want to expose your pod, the application running in the pod as a network service like you want to host NGINX or Tomcat, any frontend or even for the back end, you need to use service with your pod. Then service is similar to load balancers like what load balancers does ELB in AWS, same service will be doing for the Pod. So for the starters, it's really not so difficult to understand. Just like a load balancer and you want to expose your pod communicating with each other, you need service in front of your pod. So why was really service created? Why not just simple pod mapping? Well, we know pods are disposable I said containers are disposable. But yes, same is with the pod. So you want to make any change, we give a different container image name. We need to create a different pod for that, right? When we are making change, we upgrade the image and we replace the pod. So pods are mortal and they have IP address? Yes, and since they are mortal, their IP is not static. So you need something static like an endpoint behind the endpoint you can change things. So service gives that endpoint like what ELB does elastic load balancer does to EC2 instances it gives us static endpoints so we can access elastic load balancer behind the change. You can keep deleting and creating your EC two instances similar way service does it. In Kubernetes, you have three types of service. One is node port which is similar to port mapping. We have seen in docker a host port you pick up and you map it with the container port. It's exactly the same way. And mostly it's for nonprod purpose, not for production, not for exposing the frontend to the production. And only this is to expose your pod to the outside network. That's why you can use node port. The other one is cluster IP if you don't want to expose it to outside network but internal like Tomcat connecting to MySQL. So for MySQL you need a static endpoint so you can create a service of type cluster IP. Cluster IP for internal communication. So here there will be no port mapped to your node internal communication. Third one you have is load balancer. This is again to expose to the outside network for production use cases. We are running a Tomcat pod and I want users from the internet to access that. I will create the service of type load balancer and on AWS, it's going to create actually an elastic load balancer and map my Pod to it. So every pod or a cluster of pod that are running, you need service in front of that if it's a network, if it's providing a network service, okay, like a frontend, we will have a service for back end RabbitMQ ElastiCache you'll have again service in front of it. MySQL, you'll have a service in front of it. That's how the communication happens between pods and between user to the pods. First, we'll understand service of type node port. Node port. So let's say you have two pods running in a node. So that blue color square or rectangle is your node worker node. And you have two pods running over here. Pod will have label and Pod will have IP address. And of course, the container running inside that. So the nginx container is running on both the pods, one container per Pod and it's exposed on port 80. Okay, pod has an IP address and a port number to access and it has a label. Now, service will be like a load balancer. As I said in front of it. Service will have a static IP address, which will not change until unless you don't delete it. It will have a frontend port like a load balancer has a frontend port. This is internal frontend port. It's for internal communication. And it has back in port. And the back end port will be the port number of the container, obviously. Okay, so Service, like a load balancer, has a frontend port and a back end port. But how does it know which Pod it has to route the request to? There could be hundreds of pods running like this. Well, it is going to match the label selector. When we create a service, we give label selector. We give a label name. Any Pod that has this label app frontend, any Pod, it's going to forward the request to that Pod on port 80. So I have two part. If I run exactly third part, exactly same with same label, my third part will be automatically included under the Service auto discovery. Okay, this is internal. When you say node port, you have to mention a node port number. So let's say 30,001. So a node port or host port 30,001 will be mapped to your service. When you access the node by giving its IP address and the node port, the request will be forwarded to the Service. The Service is going to forward the request to the Pod and Pod will send it to the container. And the same way it comes back observe. There are multiple port over here. 30,001 is the node port for the outside communication outside network. It sends it to the Service. Service has an internal frontend port 80. It's going to send the request on the back end port 80, which is the container port. Okay, so your node port and your service internal front end port can be same or can be different. Back end port and the container port should be exactly same. And label selector should match with the label of the Pod. So when you're creating a service, two things are very important. Matching the label selector and the back end port. That is the service of type node port. And this is a sample definition file kind service API version V one metadata name spec you give the port information. So type Nodeport. You are specifically saying that I want to create a service of type node port. And then you give the port number target port that is the back end port 80. Port 80 means it's a frontend port, but internal frontend port. You cannot access it from the outside network. Node port is for the outside network. So if I access this node on port 30,005, it's going to forward the request to any Pod that has this label. Okay, app frontend on port 80. So if you know your Pod, you know its label, you know sorry, it's container port. Then you can very easily write a service definition file. Again, same thing to create kubectl. Create the definition file. Get SVC to get information about the service. Describe SVC detailed information about the service. Okay, so first, we are going with the node port. Okay. I have a Tomcat part running one single. I'm going to create a service in front of it with Nodeport So let's do that. Okay, so I have this definition file and from that I have created the Pod. Well, you can do the same. Exact same man. I'm going to go one level up. Let me rename this Pod to app. Let's get into app directory. On the same directory, we'll have the service definition file. Let's print this. Two important things we have to get from this definition file or the Pod information is the label and the port number. Now let's write our service definition file. So we're going to say vproapp-nodeport.yaml. Okay, so the usual kind service as capital API version V one metadata name spec spec contains all the port information. So I'm going to say ports. And its value is a list. So Hyphen, I'll give the internal port first. I'll say port and I will say just 80 90 node port. This is external frontend port. Node port value starts from 300. So I'm going to say 30,001. Okay. Now target port p capital I can give the port number 80 80 directly. That's my container port running in the Pod. But I also have an option to give the port name which I have defined in the definition file. Let's go with this first. Protocol TCP and the selector. Now selector is going to start over here. So we have created the selector app pro app for the Pod. Okay. And type of course node Pod. Okay, let's once again match it with our Pod definition file. Okay. Label selector app vproapp. And that's right. That's the label for Pod. Target port at target port at. I also have the name for the port, so I can give the name. I will edit that. Instead of 8080, I will give the name. There's too many numbers. Name should be better here now. Okay, vproapp-port really means the port utility. Okay, time to create it. kubectl create -f, the definition file vproapp-nodeport Okay, there is a syntax error. Let's fix that first. This is line number ten. Oh, actually, it's a very serious problem. Selector is at the same level as the ports and as the type. Let me take this one. Also at the same level. Okay, that worked. Let's check. kubectl get SVC. Okay, you are seeing two is service. One is for the Kubernetes. You can ignore that for now. And that's our service. Okay, that's the name type node port cluster IP. And these are the two frontend port. Okay, this is the internal frontend port. This is external frontend port. Okay, you can check the name of your service. Okay, so you see the IP static IP of your service. What I want to show you is endpoint. That's the IP address of your Pod. It has automatically mapped it. Okay, I'm going to say kubectl describe Pod and I'm going to grep from that IP, you see that's the pod IP. You see that at end point, automatically it has mapped with port 8080. We did not mention we just mentioned the label selector. So automatically it's discovered and mapped. Okay, this is so cool. Now let's access it from the outside. 30,001. That's our port number. Let's go. Okay, I should have all my worker node and master node here. Okay, so the worker node master node. I just need to change the security group. That's the im role. Bad, bad. Go back. So, worker node security group. I'm going to say all traffic, all traffic from my IP because I will be accessing it on different different port. Okay, now it really doesn't matter which node our Pod is running. We are going to access the service. And service is across all the node. Even at the master node. You can even access the master node also. So let's get its IP and let's give the port number 30,001. And voila. Our Tomcat application of vprofile is running on Kubernetes cluster. Okay, but through node port, we'll see now load balancer, which is much more convenient for productions. Okay, now I'm going to delete the service kubectl, delete SVC and the service name. Okay, the reason I deleted because I'm going to create a service of type load balancer now. So I'm going to copy everything from the node port definition to balancer. Now, when I say here load balancer, actually I mean a load balancer, an elastic load balancer. Okay, so let's change things. Okay? First, the type is not going to be node port. It is going to be of type load balancer. Okay, if you're not going to have node port, but node port will be still created automatically. And this port, this is going to be the port number of your load balancer. So I'll just keep it 80. So once the load balancer created, you can access it on port 80. It should route the request to your parts. Okay, let's save this kubectl. Create pro app, load balancer, get SVC and you should see the endpoint of the load balancer. If you go to AWS console load balancer, you should even see that load balancer. Yeah, that's a load balancer. Okay, let's see the instances. So it has two worker node, okay. But they will be mapped on a port number. So frontend port is 80, as we mentioned the definition file and 32654, that is the node port. You'll be saying, hey, you said there is no node port. Of course there is a node port, but we have not mentioned it. Okay? So that is going to become healthy in few minutes. By the time let's understand how this works. This service of type load balancer. So if you understood node port, same as the node port previous story, on top of that there'll be a load balancer. Okay? So you access the load balancer. It's going to route the request to any of the worker node on the port number on the node port. Okay? And we did not specify the node port. So it's going to pick up a random node port. It has a range. It's going to pick up from that random port number and assign it to the worker node. Okay? Like that load balancer, we access it throughout the request to the internal service and that's going to forward the request to your Pod. Now, service is across your cluster. It's not on a worker node. It's not a Pod, it's not a container. It is rule or some rules, proxy rules. So it gets created across all your nodes. Okay? All the worker node, even the master node will have the rules that if the request comes on, this port is going to route it to the right Pod. Okay, so instance are in service. Now we can access the load balancer, right? And I don't need to mention any port. And there you go. So this is the service of type load balancer. And this was service of type node port. The third one is cluster IP. We will be seeing that in a project. But cluster IP is really without any external port. No node port will be there. Okay? It will be mostly for the frontend service to refer to the back end service or back end service interacting with each other, they will need service of type cluster IP. So for nginx referring back to the Tomcat Pod, you need to create a service of type cluster IP. All right? So you just mentioned type cluster IP. Instead of node port or load balancer, just cluster IP and you give the target port and the frontend port. That's all. No node port, no load balancer. Now internally, if any other Pod wants to interact with your Tomcat Pod, it's going to access the service on port 8080. Okay? So on the frontend, you can have type of node port or load balancer in the back end services you'll have service of type cluster IP and load balancer for production use cases well that's all in service for now you can clean your resources delete your pod delete your service keep CTL get pod or if you really want to see if everything you can say kubectl get all everything running in the current name space we'll see what is namespace later so I'm going to delete kubectl delete pod yes, like that also you can give pod slash and the pod name same for the service you can give service slash and the service name now when you're deleting that service it's going to delete your load balancer also okay let's run it all once again we are good all right that's all in the next video we'll be talking about replication controller how we can have replica of our pods for high availability.
