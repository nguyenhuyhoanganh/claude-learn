# ELB Hands On Part 1

Hello and welcome. In this lecture we are going to work on Elastic Load Balancer ELB. So first we need to have an instance running a website. So I'm going to do it from 2.com. I'll use this template inner peace I have given the script to set up this website in the lecture resource. So you can download from the lecture resource the script to set up this, which we are going to use in the user data. Through this exercise we are going to find inner peace, inner peace, inner peace, inner peace. Dinner please. Dinner with peace, inner peace, inner peace. This is not working at all. Yep. That's not gonna work. We will find inner peace by this exercise. The first, let's launch the EC2 instance. Launch instance web zero one. Now you can take Amazon Linux, ubuntu or any other Linux operating system. The script in this lecture resource is going to work for any Linux operating system. So I'm going to stick to Amazon Linux 2023 instance type T2 micro. Keep it. Let's create name inner peace key. Download the key. Let's create the security group. Click on edit here. Inner security. That's the name of the security group 22 from my IP and port 80. From anywhere. Oh sorry my IP. Make sure you select port 80 also from my IP. Later we'll have some work on the security group because we are not going to directly access this instance website. We are going to access it through the load balancer. And I'm going to teach you some important points to keep in mind when doing security group later. So make sure you set it port 80 from my IP and now go to Advanced Details User Data. Download the script from this lecture resource and put it over here. So this is a multi OS script for Linux. So it can be an RPM based or Debian based. It's going to set up Inner Peace website on CentOS type or Debian type like ubuntu. So let's launch instance. Okay, so wait for like few minutes, 2 minutes or 5 minutes and then check the IP address. Take the public IP of this instance after few minutes and put it in the browser. And you should find the Inner Peace website, or maybe some other site that you have set up. Anyways, you need to have this up and running. Now before we set the load balancer for this instance, we are going to see two more things EMI and templates. So if we need group of instances usually this will be the case behind the load balancer. There will be multiple instances that will be basically replica of each other. So to create a replica of the instance or VM, you need an image from which you can directly create the replica. So in EC2 we have to we go to the instance. You can select the instance and you go to action. You say image and templates and you say create image. Now what this does is this basically takes the snapshot of the EBS volume of the EBS volume plus the metadata. So snapshot is just simply the copy of the EBS volume. But EMI is the snapshot plus the metadata, the instance information. So we'll get this page. Give the image name. I'm just giving it as inner peace image. When you create an Ami there is an option of reboot instance. I mean to there's an option to uncheck that. Basically when the image is taken the instance will get rebooted. But for some reason you do not want any downtime. This is a production live instance. You don't want any reboot. Then you can uncheck this option and that takes a longer time to create Ami. We're going to keep it as it is. Let it reboot the instance and simply say create image. Now you can go to the Amis section and you can see the image being created. It's not yet created, it's in the pending state. So let's wait for a few minutes until this becomes available. By the time I'm going to edit this tag name tag and give the same name Inner Peace image in the snapshot. Also, you should see a snapshot getting created for this Ami. Okay, let's wait until this is available. Okay. After waiting for a few minutes, around five minutes. And the status is available now, the status is available and we can start launching instance from the Army directly. But before that, let's look at few options that will be helpful for you. So in the actions if you go you see you have few options. You have copy EMI option if you click on it and here you get the destination. So what that means is if you want to copy an instance from one region to another region, let me repeat that. If you want to copy an instance from one region to another region, you need to create its Ami. And then you can do the copy option and select the destination region. And then you simply click on copy Ami. In few minutes the Ami will be copied to other region. It will take more than few minutes actually. And while copying, you can also make sure you encrypt the Ami. You don't need to do this, I'm just showing you. So this is pretty interesting interview question. If you want to copy an instance from one region to another region. That's how you do. Then you have edit permissions. You can share your EMI with other AWS account. You can give the account details over here. First you can say add account ID, you mentioned the account ID and you can say share EMI, and that EMI will be available in the account ID that you shared. So that is how to copy an EMI or instance from one account to another account. So it's not a direct copy. You need to take always the EMI. If you're not using the EMI, you need to deregister the EMI. I'll show you this at the end. You need to deregister the EMI and then you need to delete the snapshot. There is also an option to build the image automatically through EC2 Image Builder service. Now this won't make much sense. Now, once you learn the CI CD process from the upcoming sessions and few projects at that time, it will start making sense. But just keep in mind there is a service that can build the image automatically like we built it manually. There's a way to build it automatically and also keep patching updating your Ami regularly. That can be done automatically. You can also call it as image pipeline. Okay, now let's get back to what we are doing. So we created this image so we can launch as many as instance we want very quickly. So we can have a cluster of instances which we can put behind a load balancer. So one way is we just say launch instance from Ami. This will be as same as or as simple as launching the instance that we have seen previously. We give the name of the instance we select our Ami. Your Ami comes in the My Amis section. And then you have to mention the instance type, the key pair security group, all the other things as we did previously. So this is one way and this is manual way. But there is also one another way which are called as or which is called as launch template. That means you can save all this information into a template and you just say launch instance from the template and that's it. The instance will be up and running in a minute or so. So I'm going to cancel this and go to Launch templates. So streamline simplify and standardize instance launches. So basically you save the all the information that you need to launch the instance into a template. And then you simply launch.
