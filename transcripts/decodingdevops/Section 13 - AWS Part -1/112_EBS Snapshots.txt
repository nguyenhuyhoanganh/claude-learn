# EBS Snapshots

Instructor: Welcome. This is the continuation of previous EBS lecture. In this lecture, we are going to talk about snapshots. Snapshots are a backup of EBS volumes. So we're going to do a lab. We're going to conduct some exercise through which we are going to understand the benefits of snapshot, how to use it, when to use it, and what are other features of snapshots. So make sure you have that web01 instance. If it's not there, create it once again. If you're following from the previous lecture, then first, we are going to detach the existing volume. So ssh to your instance and check df -h. You should see it is mounted. We are going to unmount it. Before that, let me show you one more thing. So first, I'm going to do cd into this volume in this directory, I'm in this directory. And if you want to check a directory is used by which process, there is a command called lsof. You can say lsof and you can give the path of a directory to see if there are any processes. So you see it says cwd. That means somebody's cded into that directory in that folder. And that's the process ID. If there is any other process that is connected to it, like there is none, you can do lsof and the directory. You should not see anything. But if you see any process, you can kill that process to free that directory. Then only you can unmount it because if I try to unmount it like this, if there is already a process, like I did cd and if I try to unmount, it should give me an error. Target is busy. So you can either close the process, like in this case I can just do cd out of this folder and I can then unmount it. It will work. We also have the entry in the fstab file. So let's remove that entry from the fstab file. So open fstab file at the last line, go to the last line and do dd to remove the last line. Remove only the last line. Don't remove anything else. Save and quit. Now let's detach this volume, moso-web01. Action, detach volume. Now, if the volume is busy, you will not be able to detach it. You might also need to do force detach, but we have freed the volume so it should be detached. Still shows in use. Let me refresh. Okay, it's in available. Now you can delete this volume. Action and delete volume. Okay, now we will see one more use case where we are going to run MySQL service, MariaDB service, and its data will be stored into a separate volume. And when you want to do such kind of thing, you need to create the volume first, partition it, create the directory, and then install the service. So it automatically, when the service starts, it automatically starts storing the data on the separate partition. So follow this exercise. First of all, create volume. Gp3, we'll keep it as five GB. My instance is in 1c. Check your instance is in which zone and select that zone. Tag it. We'll call it as db01-mysql-vol. Create volume and select that volume. Action. Okay, does not show the option to attach. Let me refresh it, select it. Action, attach volume and select your instance. Attach volume. Wait, I need to select the device name. I'm going to select sdh, attach volume. Let's go back to our instance and rename it as db01. SSH to the instance. And let's run fdisk -l. We should see a new volume here. The old one is detached. It's gone. So you'll not be able to see that. Now, same thing, we'll create the partition. Fdisk and the path of your hard disk. And to create new partition, p for primary, partition number one, first sector. And just hit enter to go to the last sector. W to write. Let's run fdisk -l. The partition is created. Let's format it. Mkfs.xfs. This time, let's do it xfs. Okay, now we are going to create a directory, mkdir /var/lib/mysql. Now, I know when I install the MySQL service, MariaDB service, when I run it, it creates a folder, /var/lib/mysql, and stores the data on that. I'm creating that folder beforehand and I'm going to mount it. I'm just doing a temporary mount right now. Later we can do the permanent mount. Let's do df - h and we should see it is mounted. Okay, so the partition is ready, the folder is ready, it's mounted. Now it's time to install the MariaDB service. So the package name is mariadb105-server. So say dnf install mariadb105-server -y to install it. Okay, the service is installed. Let me clear the screen and let's restart. I mean, start the MariaDB service. Now, before that, let me show you one thing. Ls /var/lib/mysql. You should see nothing over here. But when we start the service, systemctl start mariadb. And now let's do ls. And you should see the data. Now, we know this folder is connected to the partition. It's mounted to the partition. So this data is going to go to this partition, right? So DB data is getting stored in a separate partition. And what happens if data is lost or corrupted? You will say, "If I have backup, I can recover." So EBS provides you an option snapshot, snapshots. You can take snapshot of a volume. When you take the snapshot first time, it's going to copy all the data. Later, when you take more snapshots, it's going to copy the incremental data. So if I select this volume, DB volume, action and say create snapshot, it is going to create a snapshot of five GB. Let's name it. Let's give some description. Let's give it a tag, name tag and say create snapshot. Let's go to snapshots section and see. You will see a snapshot. Status is pending. We're going to wait until the snapshot is completely created. Okay, so after a few minutes, the snapshot got completed successfully. So now let's try to remove the data, you know, corrupt the data anyway. So I'm gonna simply remove /var/lib/mysql and remove everything from that. And let's try to restart the server, systemctl restart mariadb. And you should get an error. And if you run the general sudo, you'll see you have an exited command. It's going to throw a lot of errors and tell you that there is some problem with the data missing. So we have a snapshot. We can recover the data from the snapshot. First, let's detach the volume. Umount /var/lib/mysql. Let's check, df -h. If it's giving you the error that it is connected somehow, run lsof command on the directory, find the process ID and kill it, kill -9, and kill the processes and then unmount it. So let's go to volumes and let's first of all, rename this volume to let's say corrupted, corrupted volume. And now we are going to create a new volume from the snapshot that already have the actual data. So select your snapshot. Snapshot, not the volumes. Go to snapshot, select your snapshot, action and say create volume from snapshot. A few other options, we'll take a look at them in a moment. Create volume. So when you create a volume from a snapshot, you have some nice options, like you can change the volume type, right? Our requirement is to recover the data, but if you have a requirement to change the volume type, right? From general purpose to provisioned IOPS, in DB case, that is done. When you have, you know, huge access, huge I/O, then you can change from general purpose to provisioned IOPS. And to do that, take a snapshot, create a new volume from the snapshot, but we're going to keep it as just general purpose three. And you can change the volume size also, increase it. Decreasing it, there is a chance that, you know, you might lose the data. But anyways, you have snapshot. But increasing is always an option. You can change the IOPS. You can also change the zone. You can move a volume from one zone to other zone by taking the snapshot. If you want to move the volume and connect it to an instance that is in another zone, let's say it's in 1d, 1f, whatever, take the snapshot of the volume, create a new volume and select the zone where you want to move it, right? But our requirement is we're going to keep it. I mean, I'm going to keep it in 1c because my instance is in 1c. Also, you can encrypt an unencrypted volume through a snapshot. Let's give a tag, name tag. I'm just giving it recovered so I know I can easily identify it. Create volume. Let's go to volumes and let's refresh. So we have the recovered volume and we have the corrupted volume. We don't need the corrupted volume, so we'll just remove this. First, we need to detach the volume. Okay, I'm detaching the corrupted volume. It's detached, it's in available state. Now I'm going to select it and remove it. Delete volume. Okay, so if I go to now fdisk -l, right? Fdisk -l, I should see only the root partitions. But now I'm going to connect the recovered volume. Action, attach volume, select my instance, and give it device name and say attach volume. Okay, now let's check it once again, fdisk -l. We see the volume, we see the partition in it, and it has all the data. If you mount it back, mount partition path and the folder. Let's run df -h. It is connected. Let's do ls and let's see if the data exists or not. And voila, the data is back. Let's restart the service, systemctl restart mariadb. The service got restarted. Let's check its status, systemctl restart mariadb. And if we check, it's active and running, so everything is good, we are back in the game. So you have seen benefits of snapshots. You can create a volume from the snapshot and while creating the volume, you can change many parameters, like volume type. You can change its encryption level, but primarily, you need to take the snapshot of the volume. You can keep taking the snapshot on regular interval. The first snapshot will be the full backup of the volume. The latest snapshots that will come after that will be the incremental backup. You see some more options. If you click on actions in the snapshot, you should see create volume from snapshot. That's what we did. You can create image from snapshot, the AMI. We'll talk about the image creation later. You can copy a snapshot. So if you click copy snapshot, let's see what options you get. Or you can select the region. So if you want to copy data from one region to other region, you can do it through snapshot. So the snapshot will be copied to a different region, and then you can go to that region and create a volume from that snapshot. So if someone asks you or if you have a requirement, how to move data, how to move the EBS data from one region to other region, it's through snapshot. Take a snapshot and copy the snapshot to the target region, right? We are not gonna do that because it's not a free exercise, but super easy to do. You know, you can just select action, copy snapshot, select the region, and wait for some time. You go to that region, you should see the snapshot. I want to show you one more option with this. If you go to actions, snapshot settings, modify permissions, you can make a snapshot public or you can share it with different AWS account. So if you want to move data from one AWS account to other AWS account, you can use snapshot, right? You can say add account, you give the account ID, say add. And the snapshot should be appearing in the other region, sorry, other AWS account, right? So not just the backup and recovery, but many other thing you can do with snapshot. So once you're done with this exercise for start clearing the snapshot, we're going to do the cleanup now. So create, sorry, delete snapshot. That's one thing you need to do first, delete a snapshot. Let's delete the instance first. So select your instance. Instance state, terminate and terminate, delete. So once you root that, this will remove the root volume, but the volume that we have created, that will not be removed, right? So this volume will get removed, but the volume that we have created, that will not get removed. So you need to remove those volumes that are created by you. So select that volume and say delete. Right, detach. Okay, yeah, of course, detach. But anyways, if the instance gets deleted, it'll get automatically detached. So you need to wait for a moment. Let's check the instances. It's still shutting down. So check its status. Once it's deleted, then only go and remove the volume. Okay, it's terminated now. Now let me go to volumes, refresh. So I see this volume, right? It says available state. Action and say delete volume. Delete. Okay, let's go to dashboard now, and let's refresh and check what do we have? We have zero running instances. We have one key pair, two security groups. No volumes, no snapshot. Make sure you have zero volumes, zero snapshots. So wrap this up quickly. We have many more things to learn in this AWS section. The next section, we're going to start with the load balancers. So clean up everything and join me in the next lecture.
