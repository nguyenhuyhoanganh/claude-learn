# Ec2 Quick Start

Instructor: Welcome to AWS console. In this lecture, we are going to see how to launch a virtual machine on AWS cloud, also called as EC2 instance. So, whenever you hear the word as EC2 or EC2 instances, or instances, in AWS, that means the virtual machine or virtual servers. So, while we're launching this instance, we are also going to see a lot of features and options in EC2. First, let's start with the region. So, I'm in North Virginia region. And usually, I stay in United States region because they're cheaper compared to some other. Some regions are expensive, but do not worry. Anyways, we are going to use most of the things free tier. But I still recommend you stay in any of the United States region. Region has a code. So, I'm in us-east-1 or I can say North Virginia. Now, let's search for the service EC2 over here, EC2. You can see here EC2 virtual servers in the cloud. Click on it. Here, it says Create, manage, and monitor virtual servers in the cloud. Okay. Now, let's click on this Dashboard. Okay, so this information over here about the resources, this is not auto refresh, so you make sure you click on refresh to get the latest status. You scroll down a little bit. You should see information over here. This service is operating normally. If there is any issue with the region, you should get the message over here. So, the region that I have selected is us-east-1, North Virginia, and we know the region is just a label. Region is not the data center. Zones are the physical data centers. So, us-east-1a, that's the zone, one of the zone in the region, us-east-1. So, likewise, you have then 1a, 1b, 1c, 1d. Any region you select, you will have minimum two zones. Also, take a look over here on the left side, you have many sections. You have the Dashboard that we are seeing right now. You have Instances. Again, instances means the virtual machine on AWS. Our virtual servers on AWS are called EC2 instances. You have Instances, you have Instances Types, a few other things that we'll see one by one. Images, Amazon Machine Image. Like in Vagrant, we have seen, we have Vagrant boxes like that. In AWS, we have AMI. So, AMIs are the Vagrant boxes of AWS, you can say like that. Just for understanding, but just call them as AMI. Then, you have Elastic Block Store. These are the hard disks, Volumes, Snapshot. We'll see them one by one later. Network &amp; Security, you see Security Groups. Those are the firewalls. Elastic IPs, reserved public IP. You can reserve a public IP and you can attach it to your instance. And few other things. You have Key Pairs, the login keys, Network Interfaces, Load Balancers, Auto Scaling. We'll see all these things one by one. And you know what? Enough talking now. Let's start launching an instance. So, you can click over here, Launch instance, or you can go to Instances and you can click over here in Launch Instances. Okay, let's click on Launch Instances. Okay, so the first one is naming the instance, also called as tags. So, first will be the name tag, which I'm going to give it as web01. You can add more tags, you click on add additional tags. You get this option. So, name web01, that will be the tag on the instances. But you can also tag other things like the volumes, the network interfaces. So, when we create this instance, we'll see that we have volumes and network interfaces on the instances. Add new tag. Let's give another tag. Let's say Project, Titan, the project name. Let's say I'm launching this instance for the Project Titan. And again, I'm going to volume, give the tag to the volume, to the network interfaces. Now, like that, you can add more tag. Tags are very good for filtering if you want to look for all the instances that belongs to Titan Project in the search, you can just queue Titan, and you'll get the list of all the instances. Or the environment tag like you know, dev environment, Q environment, production environment. So, like Env, and I'm going to say here Dev. So, the project you're working on will have the naming convention. You can use those naming conventions and give the proper tagging. Let's come down. Now, selecting the AMI, which image would you select to launch your instance? There is no OS installation here. We need to select the ready-made image. So, first, you have the common options, Amazon Linux, which is very similar to CentOS. I should say, no, exactly similar, except you have few changes over here like the download repositories. The software repositories will be different. Otherwise, most of it is same. So, like you have CentOS 9. Here, you have Amazon Linux 2023. You have Amazon Linux 2. That is the equivalent of CentOS 7 or RHEL 7. And this lecture, we'll go with Amazon Linux 2023 AMI. But then, we'll also look at other AMIs. So, the common options to macOS, you have Ubuntu, you have Windows, Red Hat, SUSE Linux, Debian, and you can search for more AMIs. So, if you click on Browse more AMIs. So, first section is quick start the popular AMIs. Now, take a look, scroll down, take a look at the AMI, read about them. And then, there is the marketplace where you can search for AMIs with softwares as well. Like if you're looking for a specific software like OpenVPN. So, if you select this, you launch the instance, you have the software inbuilt into that. Look at the charges. Like for example, this one, the streaming engine, Wowza. It is $85 per month, plus few other things. So, be careful while selecting the AMI. You have Splunk Enterprise. This will come with the Splunk software. And when you launch it later, you can give the license keys and stuff. So, some software comes with the licensing, some you can purchase license and add the information later. So, if we come to search over here and let's search for let's say, CentOS. Let's hit Enter and let's see what we get. So, we have CentOS Stream 9, CentOS Stream 8, CentOS 10. So, most of the CentOS will be free, but some of them will be with support and will be some charges to it. Most of them you see $0 per year, it's free. So, take a look, search for your favorite operating system or software over here, and just search. Don't do anything. Go back to Quick Start. Remove this entry. And make sure to select Amazon Linux 2023 AMI where you see Free tier eligible. That means we can select a free tier instance and this AMI. So, the entire instance will be free of charge. Okay, one final thing, there is community AMI. Now, these are the AMIs published by people like, you can also create your own AMI and publish it. I never use these AMIs. I always trust in building my own AMI instead of using community AMI. So, I go to AWS Marketplace most of the time. I create the instance, then I create my own custom AMI, and that's the standard actually. Okay, anyways, let's go back to Amazon Linux 2023 AMI in the quick start, select that. Confirm changes. Once again, take a look what AMI you have selected. Look at the username, ec2-user. So, that user, we are going to use to log into this instance. This is a Linux-based instance. So, we are going to do SSH. And to do SSH, we need the username and we need password or the login key that we'll see in a moment. Anyways, let's scroll down and look at the instance type. Instance type basically tells you how much CPU, how much RAM memory, and few other things like the storage speed and network speed, all those things, all those information, the resource information that you need for the instance. Now, t2.micro or t3.micro will be free tier-eligible. So, we are going to go with t2.micro right now. But if you click on dropdown, you should see a huge list, and you should see there, the number of CPUs, the number of memory, and the charges associated with that. If we go with t2.micro, there won't any charge whatsoever. If you wanna see more detail about this instance type, just search on the internet, EC2 instance type, and you can come to this documentation. You have different sections over here. General purpose, the M series, the most popular one. You should see here how many RAMs, CPU, towards. Like if you go with m8g.medium, one CPU, 4 GB memory, network speed, and EBS bandwidth, that's the storage bandwidth, the hard disk. You have Compute Optimized where you have the C series, which is for processor or CPU-intensive workloads. Memory Optimized, like for example, databases, accelerated computing you have. You have Storage Optimized, you have all this. So, based on your requirement, you can select one or you can later change it as well. Anyways, people most of the time in the beginning, in the project goals with M series, then based on the load, based on the computing, they changed the instance type. Let's come back. Let's make sure we have t2.micro. Be careful with this. Select t2.micro. Next is key pair, the login key. How do we log in? So, we can click on Create new key pair. As the name says, it's pair, you'll get the public IP and private IP. Public IP will be in the AWS and private key will be downloaded in your machine. So, let's give this a name. I'm going to say webloginkey. You have two type over here, .pem, we are going to go with .pem, P-E-M. Then, you have .ppk that is used for PuTTY or PuTTY. We are going to use Git Bash or Terminal. So, we'll go with .pem format. Say Create key pair. So, when you do that, it is going to download the private key. This is the private key. It's in my Downloads folder. So, this is the login key and this is the lock also called as the public key. Okay, next, network settings. So, these information, we'll talk about them in later lectures like the VPC subnet, auto-assigned public IP. What I want to show here first is the security group firewall, mandatory for every instance. So, if you do not create your own security group, this console will create it for you and you need to mention the rules over there. So, we are going to say here, Edit, click on Edit over here. Now, when you go here, you see security group name. We are going to change it to web-sg. Okay, in the description, I'm going to give the same or you can give some meaningful description or message over here. Here comes the rule, inbound rule, the traffic coming towards the instance. So, here, it's selected as type ssh. SSH port number is 22. Source types, from where you want to allow port 22, from where the access will be coming. So, I'm going to say My IP, when I click on the My IP, it's going to automatically find my public IP. It's my internet public IP. And also, this changes. I have this public IP right now. Maybe tomorrow it will be different. If you do not have a static public IP, most of the people don't have static public IP. For the router, they'll have the dynamic one, which changes regularly. So, when it changes, you need to update this rule. And we'll see that once we launch the instance. For now, just say ssh here in the type, source type, My IP. It will search your IP and really give some description. Allow 22 from my office. Any meaningful description you can give for this role. Scroll down. Storage type, sorry, yeah, type and size both. So, gp3, general purpose 3, the most common one right now, and the size, 8 GB. We'll keep it as it is in detail lecture. Later lecture we are going to see this in detail. Scroll down and you should see here Advanced details. Expand this and come till the end over here. User data, these are the commands or the script, basically commands only, that you want your instance to run when it's launched. In Vagrant, we have seen Vagrant provisioning same, same as that. But first line, now this is a Linux instance, so we need to give here #!/bin/bash. We know what is this from the bash scripting lecture, it's going to open up a bash shell and execute all the command that we mentioned down here. So, I'm going to say dnf install, wait, wrong spelling, dnf install httpd -y. And I'm going to say systemctl start httpd. And I'm gonna say systemctl enable httpd. Like that, you can give your commands over here. And when this instance is launched, it's ready, it's going to execute all those commands. So, let's keep that and let's click on Launch instance. Now, I have used dnf command over there because it's Amazon Linux, it's same as CentOS. You can use YUM install or DNF install. So, most of the CentOS command will work on Amazon Linux 2023. Okay, the instance is launched. This is the instance ID. Every resource in AWS will have an ID. So, this is the instance ID. If you click on that, it's going to open a new tab, and bring you over here where it will search for the instance type and show you. You can later remove this. You can click on this refresh. So, you see the name tag, web01. The instance ID, you see the instance state, it's in running state right now. Instance type, t2.micro. Status check, initializing. So, AWS will perform two health check on your instance. One is on the hardware, one is on the VM. And this will take time, but the instance, you can log in, you can use before even the health check completes. In which zone it was created, we have not selected any zone. If you do not select any zone, AWS will decide for you which zone your instance will be launched in. You'll get a public DNS name for your instance. This is accessible publicly. This name will resolve to a public IP, this one. Your public IP, you have elastic IP. We'll talk about that in later lecture. Elastic IP and public IPv4 both are public IPs, but elastic IP is reserve one. As I said, we'll talk about that in detail later. Security group, the login key, and the launch time. Please, please take a look at all this information. We are going to need them later. So, pause this lecture and go through all these details over here of the instance. EMI ID, there are many other information over here later. When you want to look for the information, you know where to go. You have status and alarms, you have monitoring, where you can see the CP utilization and few other things. In security, you have the security group name or the ID. In networking, you have the public IP, private IP, and all the networking-related details. You have storage. So, please pause the lecture and go through all this information. We'll go through them one by one in the lectures as we move forward. But I want you to be comfortable. Okay, let's get the public IP of the instance. Wait, before that. Take a look over here. You selected the instance and in the instance state, you can stop the instance, reboot, you can terminate. Action, there are many other things. One option is connect. If you click on Connect, it's going to show you methods of connecting. It will show you the username, the public IP. You can see here SSH client. If you're using any SSH client like we are going to use, we are going to use Git Bash. It has the SSH inbuilt. So, if you're using any SSH client, you can copy this command, ssh-i. The login key, ec2-user, at the rate, the public IP, public name of the instance. So, I'm going to copy this and go to Git Bash, and simply paste this and let's see what happens. Okay. Okay, it says, Warning: Identity file webloginkey.pem not accessible: No such file or directory. That is because I am in my home directory at this path. And this key does not exist over here. I have downloaded it and it went to the Downloads folder. ls, I can give here ~/Downloads/. So, tilde means this path, home directory path. And the webloginkey. Let me do ls -l. So, the login key exists in my Downloads folder. So, make sure you find out where is your Downloads folder, the path. Or if you're downloaded anywhere else, you can either switch to that folder, execute the command, or you can mention the path. So, let me clear the screen now and I'm going to do ssh -i Downloads. Because I'm already in my home folder, in the home folder, I have Downloads, and that I have webloginkey. I give the username, ec2-user, at the rate, and I go to my EC2 console. I'm going to get the public IP over here, copy the public IP, and then paste it over here. So, you can use the public IP or you can use the public DNS name. I use public IP all the time. Now, if I hit Enter, it's going to ask me, do you want to log in? Yes, sure. I accept this, yes. And I am in. Okay, let me exit. Let me clear the screen. Once again, let me show you, if I give a wrong username, let's say I have given just ec2 instead of ec2-user, permission denied. So, if I give a wrong login key or if I give wrong username, I will get this message. Let's fix this, ec2-user, and let's give a wrong public IP. Let's say 908. Anything, just give something wrong. Let's see what happens. It just could not resolve. I don't know what is this IP. So, look at the error message and understand what the error is. Permission denied is mostly authentication. You're able to reach this and this machine is denying, I do not allow this. You've given the wrong credential. If you get something like this, it's not even able to reach there. If you get a message for the no such file or directory, that means the login key, you have given wrong. Okay, let's once again come back. So, I'm going to give here ec2-user. Okay, I am in. I'm going to clear the screen now and switch to root user, sudo -i. Let me show you a few details, cat /etc/os-release. This is Amazon Linux 2023. It's same like Fedora or CentOS. And we executed, if you remember, we executed commands. systemctl status httpd. Let's see if it's installed or not. Yeah, it says active (running). Nice. Now, it should have a default webpage also. So, if I say curl local host. You know what? I'll do like this. http://127.0.0.1. So, that means the local IP. It shows something, some message, HTML page. It says it works. So, I'm accessing it from internally, it's working. Let me show you the process. ps -ef | grep http. You should see the Apache processes running. And let's see, ss -tunlp | grep. And I'll give the process ID over here, 3374. It says the port 80 is open for this process. So, I can access it locally. I see the process running, I see the port number. Now, let me exit over the internet. So, if I'm to access it over the internet, I'm going to take its public IP. Public IP, not private IP. Private IP is internal. Public IP is accessible over the internet. So, I'm going to open the browser, http://, I give the IP, and I'll say, colon, port 80. Now, I don't even need to give port 80 because HTTP default port is 80. You're right. So, I can just remove this :80. I'm just going to hit this and let's see what happens. You know, it's just loading. It's not going to show anything. It's going to fail. Why? Any guesses? Come on, pause the lecture and think. Everything is running. Still, I'm not able to access it. We see the process, we see the port number. Let me go back to the instances. Go to Security. I go to the Security group, click on that. All right, I'm going to go to Edit inbound rules. We are editing inbound rules. Click on that. Here you see SSH 22 is allowed from my IP, but HTTP or port 80 is not allowed. So, I'm going to add the rule and I'm going to say drop down, and search for HTTP. I can either do this or I can say, just Custom TCP, I'll say Custom TCP, and I can give the port number 80. Both means same. Okay, let's keep both. Now, allowed from My IP, you can do this. Or if you want to expose it to the world, you're hosting a website, you can say any IPv4. And if some people are having IPv6, you have to say allow from any IPv6. Look at this. This means all IPv4, this means all IPv6. So, I'm going to remove this one, delete this one from My IP. I'm allowing it from anywhere, any IPv4 and any IPv6. And I'm going to say Save rules. Now, once again, let's go back to the browser and let's refresh. You see that? It shows the page, it works. Okay, now we are done here. Let's delete this instance. So, select the instance. Now, you can always refresh and check your instance. Select your instance. Instance state, and I'm going to terminate. Terminate means delete instance, gone, poof. I know you'll have questions like, how to recover if the instances are gone, we'll talk about that later. Let's just delete this instance for now. You now know how to launch an instance. Now, before moving to the next lecture, do this exercise at least three times. It's free, so please do it. For the practice, we're going to see best practices and the best ways to use and launch instances. I'll see you in the next lecture.
