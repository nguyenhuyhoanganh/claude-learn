# More in Ec2 Part2

Okay if you have few more things remaining that I need to show you about the EC2 instance. And after we see these things, we're going to jump into the next service on AWS. So in the last lecture we created this instance. And it has a public IP and a private IP. Now once you create the instance the private IP is static. That means until and unless you do not delete this instance, this private IP is reserved for your instance. But that is not the case with the public IP. This public IP is dynamic. If you shut down this instance, this IP will be released back and when you power on the instance, you will get a new public IP. Take a look. I'm going to power off this instance. So I'm going to say instance date stop. Stop instance. If you refresh after a few seconds you will see the static IP is there but the public IP is gone. Now when you power on instance I'll say action start instance. In few seconds you're going to get a new public IP different than the old one. Now majority of the cases this is not a big problem because we will have load balancer and other services that access the instance. But if you need a static public IP for your EC2 instance, then you need to get into the elastic IP. So network and security you see here elastic IPS. If you click on this here you can allocate an elastic IP for yourself. Now this is a unique IP address in the world that will be reserved for you. So there is a limit. AWS gives you five elastic IP. If you need more then you need to talk to the support. And also elastic IPS are not free. There will be some charges. So you can skip this practice and you can just watch it. It's pretty simple, but if you still want to do it, you can do it. The charges are really negligible. So once again elastic IP is a static public IP. You have five. So I'm going to ask for my first elastic IP allocate elastic IP address. You select the region you're going to keep it same. Just say allocate. So you got a public IP over here. Oh sorry. An elastic IP over here which is public. And select that and say see action and see Associate Elastic IP address. Click on this. Now here you get two options instance and network interface. So every instance will by default have a network interface. But you can have multiple network interface. And every network interface can have different elastic IP. If you want. So let me cancel this and show the network interface to you. So we have this EC2 instance. Select the instance you go to networking. If you scroll down till the end here you will see interface. This is the interface. Now the IP is allocated to the network interface. So you have the private IP attached to this network interface. And the public IP is also for this network interface. This is the default network interface of your instance. You can have more network interfaces as well. If you go into network and security network interfaces, you will see your network interface, your instance name. And you can see here you can create more network interface and attach it to your instance. And as I said, you don't need to really do all this in this lecture. You can just watch it. If you need to understand these options. The instance can have multiple network network interface, and each network interface can have different IP, private and public both. So let's go back to the elastic IPS. That's our elastic IP. Let's select it and say action associate elastic IP address. And now you see you can select instance or network interface. Now you know what network interface means. But anyways we'll select the instance and we'll select the instance name. Now this instance has just one network interface. And this IP will be attached to that default network interface of the instance. And then you can say associate. Now if you go back to your instance go to Details Refresh and you will see the elastic IP associated to this instance. And this is static. So if I power off this instance and I power on this IP will remain same. And I'm going to show that to you. So instance state stop instance stop okay. You see the instance is in stopped state. It's powered off but the IP is still there. So as I said in most of the cases you will not need elastic IP to your instance. You will realize after completing this course. But in case if you need a static public IP to your instance, this is how you can get it. Okay, now I'm going to power this on okay. One more thing. The security group that we have seen security Group is also on the network interface. So the private IP, public IP and security group is attached to the network interface of the instance. Right. You go to the networking tab. If you scroll down you will see the network interface. So all this is on the network interface. So anything that is related to the networking of the instance is attached to the network interface. And you can have one or multiple network interface. Each can have their own IPS security groups and elastic IP also. Okay. Now next thing so instance will also have a volume. When we launch the instance we select the army. And the Army gives you a volume the storage. If you go to the storage of the instance you should see here a volume ID you can go to the volumes section. Also here in the left side, if you click on volumes you can see the volume ID, you can see the instance name. Sorry, that's not the instance name. That is the tag that we have given to the instance in the attachment. Here you can see the volume, uh, sorry, the instance ID of the volume. So we're going to have a dedicated lecture on volumes. We're going to get into detail about that. But I just wanted to show you these default things about the instance. So the instance when you launch comes with a default volume. So this is eight GB. Some am I might might give you 20 GB, some might give you 30, some might give you 120. Really depends on the army. Most of the default Linux Amis comes with eight GB. So if you go to dashboard and if you do refresh, you will see here one instance. And you'll see in the running we have one instance, one key pair, two security group, one elastic IP and one volume. Always keep track of your EC2 dashboard. So if you have any, you know, extra volumes or elastic IP, anything. You can find it from here and then you can go there and delete it if you don't need it. Okay. Now let's go to the running instance. Now take some time and explore all these tabs. Look at the different options and settings of the EC2 instance. Keep in mind you're not just learning EC2 service here. You're learning many basic concepts of cloud computing through EC2. All this knowledge will be applied in further lectures, in the projects, and even in real time. So make sure you spend quite good amount of time on EC2. Please do not rush here. Let's take a look at the actions. What we can do. Changes to this instance. So click on actions here. In the instance settings you have many settings that you can change. One of them is change instance type and it's grayed out. So right now we have T2 micro T3 micro for instance we can expand it. We can have a bigger instance type or a smaller instance type. Because you know cloud computing is elastic in nature. But since it's grayed out we need to power off this instance. We're going to change the instance type and then we power on. You don't need to do this because when we change to a bigger instance type, there will be charges on hourly basis. We're going to wrap it up in a few minutes, but still you need to do it. It's pretty simple exercise. I'm just going to show you just watch. So first thing I'm going to power off this instance stop instance. Okay. Remove this filter running because there's no running instance. Now we have a stopped instance. Now I'm going to change instance type. Once again you don't need to do this. Just watch. So go to actions instance setting change instance type. So right now it's T2 micro and I am going to change it I'm going to change it to T2 dot small one step bigger. Here you can see the comparison. So T2 micro has one GB T2 small has two GB one core CPU, one core CPU that remains same. So whenever you change the instance type you get like this a nice comparison, even the costing. And that really helps you decide should you go it? Should you go for it or not? But for now, I'm just going to go for it. I'm going to change it to T2 small. The instance type is changed and now I'm going to power it on instance state start instance. And that simple it is. Okay, now I have a bigger instance type. Let's take a look at other settings. So you have change termination protection where you can enable a protection. Click on this I'm going to enable it. So now this helps in accidental deletion. Some people you know accidentally delete the instance is going to not allow that. You need to disable this protection. Then only you can delete the instance which we'll see once we delete this instance. Okay. Like that there are many settings. Now if we go to networking you see we can attach network interface. We can detach network interface RDS will come to this later. We can disassociate our elastic IP. We can manage IP addresses. Let's take a look at the security change security groups. You can change the security group or you can add multiple security group also. So like I can select a security group over here I say add security group or I can remove the security group. So I just added the default. Then I removed the default again I just wanted to keep the same security group. Save the other setting. You have modify IAM role which we are going to see later. These are basically permission on which servers can access this instance or from this instance what you can access. We'll take a look at it later. Image and templates. We're going to see this in few lectures. We can create an Ami from this instance that will see monitor and troubleshoot. So sometimes this happens. We make some changes at the OS level. And the instance does not come up. And then you can look at the system log. So it's not like live system log. It takes time to load. It's like the monitor the computer monitor to your EC2 instance. But this is not live. You can just download log and take a look. Basically helps with the booting log. Okay. So like that there are many more settings and information about the EC2 instance. As I told you, you're not just learning EC2 servers and its settings, You are learning many cloud computing concepts through EC2 servers, so take your time, explore this, create delete instances, play with it. Just make sure you keep it at T2 micro, T3 micro in the free tier limit and just play with it. I'm going to now terminate this instance. I'm going to say instance terminate delete and I say terminate and I'm going to get an error. That is because I have enabled termination protection. So I'm going to disable that first. So I go to action instance setting change termination protection uncheck this and save. And now I can terminate the instance terminate terminate come to the elastic IPS section. Refresh it. Select the elastic IP that you have say action and say release elastic IP address release elastic IPS are not free, so make sure you release it back to the AWS pool if you find any elastic IP. And that should be all in this lecture. I will see you in the next one.
