# Ec2 Introduction

Hello and welcome. We're going to first learn ec2 service in AWS. Ec2 is one of the most popular service of AWS Its long form is elastic compute cloud. This service is basically is about a virtual machines and the related services. So Ec2  basically provides you Web services for managing and provisioning virtual machines inside Amazon Cloud or Amazon's data centers. You can easily scale up or scale down your resource in ec2. For example, if you have eight GB of RAM, you can go from GB to 16 GB or can even scale down from eight GB to four GB. That's just one example on the RAM part. Then you have CPU network and storage, other features that you can scale up and scale down. So it's elastic in nature. And you're going to pay only for what you're using or how much you're using and a different pricing model on that. We'll talk about it. Ec2 service is very famous for getting integrated with other services like S3, EFS, RDS, DynamoDB and Lambda. Many, many, many other services which will be also doing. Let's quickly talk about the pricing on ec2. First you have OnDemand, which will be using throughout our course. And really, we are really not going to pay for it all the time. We are going to use free tier. But OnDemand is pay per hour. So if you launch an ec2 instance, a virtual machine, you will be paying hourly basis or seconds. also sometime. You have reserved also that you can reserve the capacity for 1 to 3 years and on which you get discounts. You have also spot instances. Spot instances are basically the unused ec2 resources on which you can bid and you get huge, huge discounts on that. But the problem with this is if someone outbids you, your ec2 instance will be gone, but then you use it in the mixture of auto scaling group and all. And this will save you a lot of money. Then you also you have a dedicated host. If you don't want to go with a complete, like,  if you don't want to go with the virtual machine, you can dedicate a complete physical server for yourself, which will be very expensive. We will be doing on demand and in course we'll be using the free tier. Let's talk about the components in the ec2 instance or ec2 service. The launch an ec2 instance. Primarily, you need an AMI, Amazon machine image. This is a ready made virtual machine like vagrant we have seen vagrant boxes here we have AMI and you have a huge list to choose from. You have an instance type. This will be the size of your instance, the compute resource, how much amount of CPU, ram, network speed or storage speed. So you will have a huge variety to choose from. We'll go through them. EBS Elastic BLOCK Store. This will be the storage. So on AMI we primarily have a storage like 8 GB for Linux machine and 30 GB for Windows Machine. And then you have different, different sizes of AMI that will be using the EBS service and you can choose and add your own storage also, which will be seing in the next session. Also to mention EBS is really the virtual hard disks on which you can store your operating system and also store your data. You'll have Tag to every resource in AWS. You can use simple key value pairs and define tags like name of an ec2 instance, the project, the customer who owns this or the environment. You can set as many as tags you want. It is very good for filtering and also for billing. Security group you will have on every ec2 instance. This is basically a firewall for inbound and outbound traffic. To log in to an ec2 instance, you will need a key pair. For Linux machine. These are basically ssh keys, ssh log in keys, so public key and private key. As we have seen in the bash scripting. So you just create a key download it for yourself. The private key, the public key will be injected in the instance so you can use it to log in to your ec2 instance. For Linux machine. You can use the key to do an SSH, for Windows machine. You will use the key to generate the password. Creating ec2 instant is very easy. We're going to see that next. It's basically wizard based. So you choose what AMI you want, you select the instance type and then there'll be some configuration for the instance, like network, it's permission and some scripts to execute. After that you will decide on the storage. This is where EBS comes in. You can add extra storage if you want or you can go with the default AMI storage. Then you select the tags, what tags you want. You can give key value pairs and do as much as tagging you want. Then you create or select a security group which is a firewall. Finally, you review the instance, choose a login key, and the instance gets created. So let's take a quick view of how we're going to create an ec2 instance now.
