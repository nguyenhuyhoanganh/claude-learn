# AWS CLI

Hello and welcome. In this lecture we are going to look at Amazon CLI or command line interface. And we're also going to see how to use Amazon Q which is the Amazon AI assistant used in coding. We can use it to get information via chatting. And there are a few other amazing benefits of Amazon Q first, let's talk about the Amazon CLI. So what we do on AWS through the browser through Amazon console right. Like so far we launched instance. We created key pair or we're going to do many more things. All those things can be also done from the command line interface. By issuing commands. We can do it from our own computer, be it windows, Mac OS or Linux or any other place where we have the command line interface installed. First we need to install aws cli to install it on windows we have the command choco install aws cli. You need to open PowerShell as admin user for Mac OS. Just open the terminal and run the command brew install aws cli. If you're using any Linux operating system, then you need to download this package. With curl command. You unzip it, you execute the script that will install aws cli. So I'm going to open PowerShell as admin user. Execute the command choco install aws cli. If you're using Mac OS, just open terminal and run the command brew install aws cli. AWS cli is installed for me. I'm going to close PowerShell. Now we need to set authentication. So when we open the browser and when we go to Amazon Console, we authenticate with username and password from command line. We need to do it through the access keys. Access keys comes with the IAM user. So go to your Amazon console. AWS console look for the service IAM. Go to the service. Go to users. Click on create user. Give user a name. I'm giving CLI admin go next. So we need to give some permission privilege to this user. So we can use different services. So we click on attach policies directly. IAM has many ready made policies that you can attach to a user. For example, if you search for EC2 we have something like Amazon EC2 Full Access which gives the user complete access on EC2 service but not on other services. But then you can fine tune these policies. Also, you can have read only access or based on the what service you want to use. Now for this lecture we are going to use multiple services. So we are giving administrator access. Now be very very careful with this one. You cannot lose credentials for this user. You cannot expose it to anybody, anywhere. Because if that happens, then that other party is going to get complete access on your AWS account. So be careful. I'm giving administrator access now. So far there are no access we are giving to this user. Just creating the user. There's no credential. There's no nothing attached to this user. Now we click on this user. You see we have administrator access. We go to security credentials. So we are not giving any console access to this user. We scroll down we see here access keys and we say create access keys. Now this is for as it says AWS cli, AWS tools, PowerShell, AWS SDK, AWS API calls. So all programmatic and command line access. So click on Create Access key. Select your use case. So ours is CLI. So select command line interface. See I understand the risk and go. Next create access key. So this is two parts. You have the access key and you have the secret access key. So it works like you know username and password that we have seen but not actually username password. And you see this one time and then it will be gone. So you can download it all into a CSV file. So click on download CSV file. So mine got downloaded into downloads folder and say done. So here you can see the access keys I mean you cannot see the secret access key over here. That's gone. It's downloaded in my downloads folder so I can open it and set it up. But before we go there, just take a look at the actions. You can deactivate an access key. You can delete the access key and one users can have one user can have multiple access keys. You can create multiple access keys. And you can select which one to activate, which one to deactivate. Now always be very cautious. Extra cautious when it comes to the access keys. These are text and there are many instances where people post it on GitHub. Public repositories, public places where there are bots that can scan these keys, get the keys, and will start doing crypto mining. In your AWS account. You'll have many instances and you will have huge bills within a few hours. So be very, very careful with these keys. Do not lose them. Do not put them into public places. Do not show it to anybody. Now let's set these keys. So you must have downloaded the CSV file. Open it in notepad. You can see the access key and secret key over there. So I'm doing right click open with notepad. So this is the access key. And this is my secret key. And I'm going to delete it as soon as the lecture recording is completed. If you're using Windows Open Git Bash. If you're using Mac OS or Linux, open the terminal and run the command AWS configure. This will ask you the access key. So first let's get the access key. Paste it over there. Then get the secret key. It will be comma separated CSV file are comma separated, so copy the secret key. Mention the region. This will be the default region. We can change it whenever we want. So I'm going to use us hyphen hyphen one. So make sure the code is correct. The region code you can check it from your console. So I'm using North Virginia and its code is US hyphen East. Hyphen one output format. We are going to keep it as JSON format. Now this information is going to create few files two files actually if you do ls tilde slash dot aws. So in your home directory there is a folder dot aws. It will have two files config and credentials. Config file will have the region and the output format and credentials file will have the access key and secret key. So if you make a mistake you can manually also go to editor text editor like VI and open it and make the changes. Or you can just run the AWS configure command once again and give your entries. Usually we make mistake while doing copy pasting or typing, and the aws cli does not work. So like for example, now I'm going to run a command which should return me information about the account id user id if all this information is correct that I have given this command should work, but if you get any error then you better run AWS configure command once again and set the details so the command is AWS sts. Get caller hyphen identity. Run this command and this should return the output. If it does not, you get any error. Then run the AWS configure command once again okay, if it's working, let's run our first command to get information about the EC2 instances. AWS, EC2 describe hyphen instances. So it's going to describe all the instances in the current region, which is US East one. And the output will be in the JSON format. And says open. In the more editor, you can hit enter and you can see the details. You can see architecture type, device name, volume, ID, public IP. You can just keep hitting enter until you see everything. Instance type T2 micro to keep hitting. Enter until you come till the end. Or you can just hit Q to quit it. Okay, now there are many AWS services and there are many, many settings. So as I told you, we can do everything through command line, but it's impossible to remember these commands. You should not even remember the commands. That will be a stupid idea. So usually I used to show documentation like aws cli documentation. You go to user guide. You can see all the information over here. Let's get started. This install how to install it authentication access. We have just seen so using AWS, cli or AWS CLI. Examples. Guided command examples like Amazon EC2. How to create key pairs. How to create key. Key pair. Sorry. Security group EC2 instances like one example AWS EC2 run instance, image ID, mi ID count, instance type. So sure you can go through the documentation and find out information. Create your command and execute it. Or you can use the AI tools like ChatGPT. You can use which is great, but AWS also has its own AI Amazon Q developer. That's what we are going to use. You look at this icon over here. You should see it Amazon Q you click on it and you can start chatting. We're going to see Amazon Q later. Also as a code assistant in our VS code we're going to write codes later. Scripting automation code. It's like GitHub Copilot. Also you can use like a code assistant agent AI. But right now we're just going to use it as a chatting tool like ChatGPT and we can get information from it like how to do any tasks. For example, I want to create a key pair. How can I create a key pair by using AWS cli. How to create key pair using AWS cli. Now Amazon EC2 is slower than other AI tools like ChatGPT for chatting, but it is the most precise one that I have seen so far. And for this such simple thing, of course ChatGPT will also give you the right thing. But when we get into detail later into this course, when we do scripting, when we programmatically create AWS resources, that time you will see the power of Amazon Q now anyways, so we get the command AWS, EC2, create key pair key name, and you give the key name that just creates the key. Save the private key to a file. Let's do this one. Let me copy this command. So I'm not going to change anything over here. Just I'm just keeping the key name as my key pair. Dot PEM hit enter. So this should create the public key on our AWS account and private key. In this file. Now this error you see over here the output redirection. So this is going to generate the private key. And this information will be stored into this file. Okay. If we do Cat. So this file would have already created my key pair dot pem. See the private key content. If you go to EC2 the key pair section you can see the public key also over here. Okay. Now next command let's ask create security group. With name my SG with rule 22 from my IP and 80 from anywhere. I'm just being very simple. No prompt prompt modification or refining the prompt. Nothing. Just plain chatting. Just keeping it as simple as I can and see if it gives me the right information or not. Because of course it is saying, you know, get your current IP address. So when you run this command, let's do this. So that's a variable equal to. And then there is a command curl command which accesses a URL that that gets the public IP and stores it into this variable. And echo command is just simply going to print that there. So we have that our public IP and it is stored into this variable. Okay. So the next command is just simply creating the security group. Let me just copy this and paste it. I'm simply doing copy paste but in real time. Definitely. You need to know make the changes, do the proper naming convention and stuff. But as I said, for now I'm just keeping it pretty simple. Okay, so this command created a security group, but without any rules. Let's see how it creates the rules. So this is how it creates the rule AWS EC2 Authorized Security Group ingress the group name, protocol, TCP port number, and cider from where it should be allowed. That is from my IP. Use this command. Nice. Okay, now you must have observed that I'm executing the commands about a task that we already did. And that's how it should be. You should know what you're doing. You should have already done it through graphically, or you should know it somehow. So mastering AWS, CLI or even programmatic access of AWS will not come directly. First, you should know that you should have done that either graphically or you should have worked on it. Then only these commands will make sense. And that's my thumb rule that I teach to everyone in DevOps. Know how to do things manually first before you start automating it, right? If you do not know security group and if I show you this command, or if you know the Amazon Q is showing you the ChatGPT showing you you won't be able to make any sense out of it. If it fails you, you will not know why it failed, right? So always, always learn how to do things manually first and then learn how to automate it. Or in this case just command line. Learn how to do it through command line later. First graphically when you're learning of course. Anyways, let's get back. So another rule to add port 80. Let's take this. So it's you create the security group first and then you add the rule okay. Last command I'm going to ask launch EC2 instance with below details name I have given web zero two key pair. My key pair which we created previously. Security group name Amazon Linux Ami. I've just given the name you can give ami id that is better. So let me get the ami id. So I'm going to say my chat is gone anyways. So launch instance. So that's the ami ID Amazon Linux. Now actually it can find the ID for you based on the region also. But I want it to be very specific okay. So I'm giving all this detail Ahmed. And then instance type. Let's hit enter and let's see the command that it gives us. Or you can ask much more details. You know user data script how to add stuff like that gives you the command. Let's copy it. Let's run this. Okay I'm going to give Q to quit from this. Now let's take a look in the Amazon console. So we have web zero two here. Go to security. You can see the security group. You can see the rules in the security group you can see the key pair name. Now why do we need this command line. Where will we use it. You are going to see that in the upcoming lectures. When we do DevOps, CI, CD pipelines, when we do programmatic access, there are situations where we need the command lines. I'm not asking you to know. Just memorize. Learn all the commands. You should know everything. How to do it through command line. No, you should have the power of CLI in your hand whenever you need. You can use it and trust me, you will need it. So you have documentation. You have Amazon Q, you have ChatGPT you can use all these resources and find the command and execute it. Just don't run it blindly. Take a look see what command is saying and test it before you use it into your scripting, which we'll see later. Once you're done, you can delete the instance, this instance, and make sure you deactivate the access keys that you have created. So I'm going to terminate this instance. Now you can use the command line better. Use the command line. Find out the command how to terminate this instance and then go to IAM. You can either delete the user or delete the access key. Or before you do that, play with AWS a little bit more. See what you can do. Just get to know it a little bit. Know a little bit better. You really don't need to master it, but just some hands on which will be very useful in our further lectures. Okay, confirm and delete this user. Okay then that would be all in this lecture. I will see you in the next one.
