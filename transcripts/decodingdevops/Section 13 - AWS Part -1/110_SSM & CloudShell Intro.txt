# SSM & CloudShell Intro

Hello and welcome. In this lecture we are going to talk about a service called AWS Systems Manager and also Cloud Shell. These are pretty easy services to use and comes very handy. AWS Systems Manager is very powerful. You can manage many nodes at scale when it comes to operations or system administration. One very simple use case is you can directly SSH or get a session to your EC2 instance from here without using any tools. That's what we are going to see. And one more thing. You can run your commands on a node or a group of nodes at one time. Imagine you are managing hundreds of EC2 instances, and you need to execute one command on your hundreds of instances, like restarting a service or running a patch. In that case, AWS Systems Manager comes very handy. You can use the run command feature of Systems Manager and execute commands on multiple machines at one time. So let's see some example. First we are going to launch an EC2 instance and see how we can access it through AWS Systems Manager. So go to EC2 instances and say Launch Instance. I will name it as server one. Selecting ubuntu Ami over here. Ubuntu server 24 free tier eligible. Let's see instance type T two micro will be good or T3 micro, whichever comes in the free tier. Keep your login. You can select the existing key or create a new key. It doesn't matter because we are not going to use key to login. We are going to do it through SSM. So I'm selecting an existing key. Security group. Also I'm creating or I'm selecting the existing security group. Scroll down and say launch instance. Okay. So when we want to connect EC2 instance from SSM, in that case actually EC2 instance connects to the SSM service and EC2 instance needs some permission to connect to SSM. And that permission can be managed by IAM. So let's go to IAM service. So we have already seen if we can create user and assign permission to the user. But what if one service wants to connect to another service? So EC2 wants to connect to SSM. In that case we use roles. So click on roles and say Create Role. Select for what you want to create this role I want to do it for AWS service, specifically EC2. So let's keep that AWS service use case is going to be the EC2 and then go next. Now here you select the policy the permission you need to look for this policy Amazon SSN managed instance core search. For this you can just say SSM managed instance. Also, you can find it the one that ends with core. Select that go. Next give role a name. You can give anything. I'm keeping it as SSM managed instance core and same. I'm going to give it in the description. And let's scroll down and go Create role. So the role is created. Now it's time to attach this role to our instance. So let's go back to our instance. You can select your instance or open it like this and say actions security modify IAM role and select your role from here. Once you select your role click on update IAM role. Okay. Now let's see how we can access it from the SSM. So come to AWS Systems Manager service. You can search it over here and go to Session Manager. Click on start session. Now it might take some time for the instance to show up over here. So if it doesn't show your instance cancel it. Wait for some time. Also check if your IAM role is correctly attached and then you can try again. You can give some reason over here. I have given shell access. Select your instance and then say next over here. Say again next and then say start session. Once you click on start session, it should open a new tab and you will get a session. And once you get it, you can execute your commands like sudo hyphen I switch to root user. You can check your OS and do all the activity that you want. So this way you don't need to manually connect or do SSH. You don't need keys. You don't need to work on the security groups. And for this I'm talking about for the SSH session that we do from git bash or the terminal. So you can have an IAM role ready as soon as you launch the instance. You make sure you attach that IAM role and then you can connect it like this. Okay I'm closing the session now. And let's go back to AWS Systems Manager. Now that we've got the shell access, now if you want to run a command on one or many node you can go to this option run command. Click on run command. Now you will see this list. These are the commands. Or here it's called as the command document that you want to execute. Most of them won't make any sense for now. Later. Once you learn Ansible you can understand Ansible playbook. When you learn CloudWatch and Docker and other services, then these things can start making sense. Right now we just want to run a command. And for that the document name is run shell script. So search with that name run shell script and you can select that document AWS run shell script. Scroll down. And here you can mention your command. So I'm going to say do ls hyphen l en slash var slash log three hyphen m apt update output sudo apt update just the list of command that you want to execute, or your entire shell script over here. And then you can scroll down. You can mention the working directory where you want to run this, and you can execute on the group. You can give the tag. Let's say you want to execute something on project DevOps. You need to make sure that all the instances are tagged like that properly with this tag, and then you can give the tag and its value and it will execute it on the instances that has this tag. It could be one or many. If you have created a resource group, then you can select from the group which will be the group of instances, or you can just execute it manually. You can select your instances from here. So we just have one instance. So I'm going to say take this instance. Or if you have the list you can select from the list. There are a few other options like storing it into an S3 bucket. Once you learn S3 you will understand that and also storing the output into CloudWatch logs. Once again, this we are going to cover later. Once you learn these services, you'll understand these options as well. So once you select your instance, mention your command. You can say run. Now here you see the status. It's in progress. You see the instance ID. Let me refresh this okay. It's a success. It's already done. So you can select your instance and see the output view output. So the output will come in the output section. See the output. And any error that comes will be coming into the error section. So like that you can manage also group of instances. Many instances you can have complete control or a centralized control. From here there are many other options that comes with SSM. You have compliance also in this you can do change management. You know very essential for system administrators. You can store variables over here like parameter store. We're going to do that in some project later. We're going to use the service. You have CloudWatch. You have operations center, incident management many other things. So basically this is an amazing tool for system administrator or operations to manage the entire operation of your fleet of servers. One other thing I want to show you over here is AWS Cloud Shell. We have seen in AWS CLI, if you want to execute AWS commands, first we need to install aws cli, and then we need to set up the access key. And then we can execute the command with Cloud shell. You can do it all from here. You will be logged in with your user account permission. So I've logged in with the root account. And I'm going to show you I can run commands over here like AWS EC2 describe instances. So we have not set any access key. Nothing. We are not install aws cli. That comes everything by default with the cloud shell. So you see the output. I can scroll down, see the output in the JSON format like that AWS S3 ls, which is to list your S3 buckets. You're going to learn S3 in some time. And totally. You can manage your entire AWS account from the cloud shell, and if you need access to any commands you don't know, then anytime you can open the Amazon Q Developer Chat, you can ask the question over here. You get the command and you can execute it on the cloud shell. So everything at one place you can open the cloud shell into a new tab, separate tab also. Right. So in one AWS cloud login you get here Cloud Shell Systems Manager Amazon Q developer as an AI assistant. So you can manage everything from the browser. Now which to use when totally depends on your requirement. I have used all of them for different different requirement and use cases. The point is, as a DevOps, you should know about these tools so you can use them whenever it's required. So feel free to explore. Once you are done, then delete your EC2 instance. Wrap this up and then join me in the next session.
