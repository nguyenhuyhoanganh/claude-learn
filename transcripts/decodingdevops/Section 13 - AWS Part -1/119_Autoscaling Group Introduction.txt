# Autoscaling Group Introduction

Welcome to the auto scaling section of AWS. This is really a very pretty easy service to understand if you know ec2, if you know cloud watch. Then it would be very easy for you to understand, which we have already done in the previous sessions. So auto scale groups service integrates with Cloud Watch and it will monitor your resources on a particular metric that you have selected, like CPU utilization. And if the CPU utilization, for example, is going high, crossing the threshold, it can adjust the capacity. It can add more instances in your auto scaling group. And this is based on the cloud watch alarms. So while we are creating auto scaling group, we also have to create cloud watch alarms to add the capacity or also to remove the capacity, adding the capacity to maintain the performance and remove the capacity to maintain our cost. So auto scaling group is going to use launch configuration or launch template, yes the same launch template we have seen in the ec2 tor the elb exercise. It can use those launch template or launch configuration to launch your instances in the auto scaling group. So the instance launch information will be provided by launch configuration on launch template. To adjust the capacity based on the alarm It uses scaling policies. Auto scaling group has its own policies. Or you can set. policies yourself, step scaling. Like, for example, if the load is going let's say if the CPU is going high more than 60. You can mention that launch, 2 instance, if it's going to 80, launch, 4 instance, or if it's coming down, then remove 2, 3, 4 as per your requirement. Or you can let auto setting group handle all that for you. When you you're creating auto scaling group, you can mention the minimum size of your auto scaling group. So for example, you can say, one is the minimum instance that I need in the auto scaling group. And you can mentioned the desired capacity. So when you say desired capacity is two and minimum is one, it will create two instances in your auto scaling group. And if you're wondering what is minimum, then as the name says, you cannot remove that last instance from your auto scaling group, if you said 1. So auto scaling group based on its scaling policy will also terminate the instance, but it cannot terminate the minimum size and desired capacity will be the usual running instances. Then you can also mention the maximum size based on the scaling policy. If it starts adding the instances, how much maximum instances it can add, if you said 4, then it will not launch more than 4 instance, in an auto scaling group. So to maintain your cost. So let's see the design. So we'll create an auto scaling group which will be powered by your launch configuration to launch instances. So you see that rectangle in the auto scaling group that will be your launch configuration. And based on the Load, let's say,  CPU utilization, you have said is the metric. Alarm will be triggered if it crosses the threshold and scaling policies will trigger the launch of new instances in the auto scaling group or can even reduce the instances based on your scaling policies. Now. Let's see how it can be done. Let's get into action.
