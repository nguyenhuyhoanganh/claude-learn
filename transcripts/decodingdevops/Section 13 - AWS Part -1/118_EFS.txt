# EFS

Hello and welcome. In this lecture we are going to learn about EFS, Amazon File System, Elastic File System. So Amazon EFS is a shared file system. Before we learn this we need to understand what is a shared file system and why do we need it. So let's say we have a website hosted on a group of servers with a front end endpoint of a load balancer, so users access the load balancer. Let's say the users are uploading some images. This is a site where users upload and share images. So request goes to the load balancer. The load balancer routes it to one of the server. User uploads the image, and image gets stored into the hard disk of the server. The server could be EC2 instance, could be a virtual machine, could be a physical server. Anything, any computer, any operating system like this will have its own hard disk where it stores the data. The data consists of, first of all, the all, the operating system, then the application, the services running and the data the service is storing or retrieving. So let's say images are stored on this server's hard disk. Now what happens if the user accesses again the site and the load balancer routes the request to other web server? Let's name it as web zero two. So image was stored on web zero one hard disk. And when the user later comes and it redirects to web zero two, web zero two, hard disk will not have that image. That's the problem right? So that is the problem with the block storage like EBS or any hard disk of a computer is called as a block storage. So the files that needs sharing should be in a shared storage. The storage should be centralized for such kind of files. So we should have something like this. Now in this case the servers will have their own block storage where the operating system and the service resides. But the shared files like for example, images, videos and documents that need sharing those kinds of data should be going. Should be going into a centralized storage, or we can call it as a shared storage. In Linux, the most famous shared storage protocol is NFS. NFS is used as a shared storage, and to build NFS shared storage, we need to host NFS server, which contains all the hard disk and we need to manage this NFS server. So an extra overhead to manage this shared storage, but it is required. Now enter EFS Amazon Elastic File System. It is the shared storage hosted on the cloud on the AWS cloud. So you can simply create it and attach it to your instance. Basically, mount it on your Linux instance. And it's not only for the AWS EC2 instance you can use it for from your data center. Also, you can store data on Amazon EFS as a shared storage. So we'll do this exercise. First we need to spin a web server and then mount the shared storage on it. And we're going to do it for images of our website. EFS is a network service, and a network service on AWS needs a security group. So first we need to create a security group for the EFS. So let's go to the EC2 service. Let's go to security groups. Let's create security group. Let's name it as Inner Peace hyphen hyphen SG same I'm giving in the description inbound rule. Add inbound rule. Now the security group is for the EFS. The request is going to come from our web server our EC2 instance. We need to spin the same inner peace web server. So EFS works on NFS protocols. So in the type we select NFS, the port number is 2049. And the request should be allowed from the security group of our Inner Peace web server. So let's see. So when we launch instance we're going to keep it into the security group. Inner peace security group. Some description allow NFS access from Inner Peace web servers security group. So that's the only rule we need. Once again the security group is for the EFS. The request is going to come from or the access is going to come from this security group. Inner peace web server. Security group. Create security group. Now let's go to EFS. So you can just search here EFS and come to this. You can watch this video. I mean you should watch this video. It's a very nice video explains you about the benefits of EFS, how you can use it. What are the use cases? Really nice small video. Definitely watch this video. And once you're done, come to File systems. Let's create the EFS file system. Now like we create an EBS volume. It is almost similar to that but this is a shared storage. EBS volume can be connected to only one instance at a time, but EFS can be connected to multiple instances at a time. That's what shared storage is. So let's click on Create File System. Let's give it a name. So EC2 instance when we launch has a folder. Images. If you have something else uh in the folder like IMG or something you can use that. So most of the template that we download from 2.com, when we unzip it has a folder images or img. We'll see that when we launch the instance just give the name Inner Peace Hyphen Images or whatever your project name is and click on customize. I'm going to show you many options over here. So file system type. We are keeping it as regional. So in a region which is not Virginia I'm using, it will be available for all the zones. But if you want only for one zone you can select one zone as well. That will save you some cost. But for now the access that we are doing is going to be free. So do not worry about that. So keep it regional. There is an option of taking automated backups. You can put a check mark or just remove it. We are not going to take any backups. Lifecycle management like after the backup is taken, what you want to do after 30 days you can transition into archive to save the cost. So transition into archive. Select this to none. We don't want to do this. Make sure you select this to none. Encryption at rest. So EFS stores all the data encrypted. That's called as encryption at rest. Encryption in transit means when the data is traveling. So EFS stores the data encrypted. If you put this check mark on. Otherwise you can uncheck it if you don't need it in real time. Most cases this will be checked okay. Performance settings if you need high performing storage throughput mode. If you want you can put it as enhanced. You're going to keep it as bursting. Bursting is going to scale whenever it's required, otherwise it will be smaller. This saves cost and hence is just faster. You can check in the additional settings. Here we have performance mode, general purpose or max io will stick to general purpose. Max io is going to or it is used to tolerate higher latencies, but just keep it. General purpose tags. Let's give the name tag inner peace. Hyphen images go next. Okay, so for all the availability zones there are subnets. And on that there is a security group that decides what request can come in. So default security group is selected. Just remove all the default security group from all the subnets and then select the EFS security group that we have created for all the zones the EFS one. And go next. That's all. Just go to next and say create a file system is getting created. It's going to take few seconds to available okay. It's already there. It's available state Okay. Now, how do we access this? Now there are multiple ways of accessing an EFS storage. You can do it through IAM user also. But we are going to use access points. So you need to create an access points. This access points will connect your instance to this EFS file system. So go to Access Points and say Create Access Point. Give file system a name. Choose the file system. So this is for our file system. We can select it from here. So simply select that and scroll down and say Create Access point okay. So the access point is also created. Now how do we connect this to our instance. Or we have a folder images folder. We want to put all the data that it should come to this EFS file system. So let's launch the instance and let's take a look at that. So go to Launch Templates. You should have the launch template for Inner peace. Just simply launch the instance from there. Action launch instance from template. This one instance we need for now launch instance. Okay. It's already in running state, so I'm going to take its public IP and do SSH to it as I downloads inner Peace key EC2 hyphen user and the IP address. Okay. Switching to root user. Okay. So the way we mount a file system is one is manually. The other way is through fstab automatically. When the operating system boots it automatically mounts. We have seen that in EBS lecture. But the point is this Linux operating system does not understand what is EFS or what is NFS. So we need to install some packages. Then only it will understand what is EFS. And then we can mention that information in fstab file to mount it. So first let's go to var dub dub dub HTML. That's where our website data is located. Here I have images folder. You might have images or img based on what template you're using. Let's check the website from the browser. So first let me go to the security group of this inner peace instance. Edit inbound rule. Okay so I have access port 80 allowed from the load balancer security group. I'm going to add here port 80 from my IP. Save rule take its public IP. And let's check it from the browser. So where are the images. Yeah here there are these images. Those are coming from this images folder. I want you to move these images to a different location like create temporary folder temp images and move everything from the images to it. First let me go get into the images folders directly. Yeah. So move everything from the images folder to temp images and let's restart Start the HTTP service. So there are no images here. Let's go to the browser. Refresh the page. I think the images will be already uploaded to this browser, so we need to access it from another browser. Let's open something else. I'm using this DuckDuckGo. There will be nothing cached over here. Let's scroll down. So you see that images are gone. Okay. Just wanted to show you from where the images are coming and now it is uploaded. Then it will be uploaded to the browser and then it will be cached also. So if you want to remove it you have to remove the cache as well. So these are not dynamic images static images. So no upload download here. But just this is an example. Okay so one more reason I have moved the images to temporary images folder is because we are going to use this folder as the mount point. This will be connected to the EFS file system. Then we put images in this. Then it goes to the EFS file system. Right now it's just living in the hard disk. So in order to mount the EFS file system over here, we need to install some tools and set right information in fstab file. Okay so I just searched on the internet install EFS client or EFS utils on Amazon Linux 2023. I'll put this link in the lecture resource also. Or you can simply ask the ChatGPT how to do this. So yum! Install Amazon efs utils. I bet this also stunnel that we need to install. Okay, first let's install this yum install. Yum install. Hyphen y Amazon hyphen fs hyphen utils. So that is on Amazon Linux 2023. But you can install it through different packaging mechanism. Also, like APT on other operating system, you just need to search for how to install Amazon EFS utils on the operating system. The operating system name. You will get the package name and the steps to install it. So mounting file system mounting existing EC2 Linux instance automatically mounting. In this section you have existing EC2 Linux instance and we are using access point EFS access point okay, this one, not IAM authorization, not this this one EFS access point. So copy this and bring it into a notepad okay. So first you need to give file system ID. So this information goes into the etc. fstab files. So here we need to put the file system ID here the mount point path which will be the images folder. And here the access point ID. So let's get this. So that's the file system ID. Let me copy that file system ID colon forward slash. And here the path. So this is var HTML images. This folder. Now you could be having img or something else based on what template from 2.com you used. If you don't see images or IMG, you see anything else you can mount just any folder. Just you need to do this exercise. So here we are going to put that. And EFS is the storage type. And here the access point ID. So that's the access point ID. Yeah okay. So it should be like this. Let's copy all this information now. Let's do CD hit enter open vs vim etc. fstab file. Go to the last line or to get into insert mode and next line and shift insert to paste it. Take a look. Once again this should be the EFS file system id and this will be the access point ID and here the mount point path. So once you do this save and quit and you need to run the command mount hyphen A, so it reads the fstab file and mount it. Before this, let's run df h. Right. So there's no mount point yet. We're going to say mount hyphen A which will read the fstab file. Okay. Now let's run DFI finish. Okay. It's mounted. You see there. Now let's bring back our images. And now when you're doing this I'm copying everything from the images folder to var HTML images. Now see this images folder is connected to the EFS file system. So this images are going into that file system. So it's no more the folder on your local hard disk. It is the NFS mount point. Now let's restart httpd service. Restart httpd service. And I'm going back to that Duckduck browser. Let me refresh this. And this time these images are coming from the EFS file system. It's not on our hard disk. And this whole exercise is free. The file system size is very small, less than five GB. It comes under free tier, so do not worry. The last thing that you need to do is create the image. Am I off this instance? So we can use it in the later later lectures. So your instance is connected to EFS. Mount point say action images and say create image. I'm naming it as inner peace EFS because it has the EFS mount point connected to this. And whenever I launch instance from this EMI, it will automatically go and connect to the EFS file system. Just create image. Okay, wait until this EMI creation is completed. Go to the EMI section. Now you can remove the old EMI. This will keep. Or if you want, you can keep it. We can create a launch template also for this one. We need to do that actually in the further lectures. Let's just keep it till here. Just wait until this. Until this becomes into available state. Wait until that time and then you can delete your instance. Okay so my EMI is in available state the inner peace EFS. Now I am going to delete this EC2 instance just as a part of a clean up. And for now you can keep both instances if you want or sorry, both the EMI or you can delete the older EMI. I'm keeping both just few lectures and then we don't need those Amis. Also, we'll do that cleanup as well. Do not remove your EFS and access point. This comes under free tiers. You don't need to worry about it, but make sure you watch this video EFS video created by AWS. It's pretty simple to use and there's nothing more complication. Just keep in mind whenever you need a shared file system, you can use Amazon EFS. So that would be all in this lecture. I will see you in the next one.
