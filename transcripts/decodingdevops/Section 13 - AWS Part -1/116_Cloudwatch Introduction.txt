# Cloudwatch Introduction

Welcome to the cloud watch session. AWS Cloud Watch is a monitoring service. But now it has expanded too much more than that. It's also a logging solution. And also there are events, there si log in sites and the so many other amazing feature that Cloud Watch provides. But primarily it's a service that will be monitoring performance of your AWS environment. So in a region, if you if you're using any service, it will create standard metrics for it. In other networking, sorry, in other monitoring tool, these things are called checks like for a virtual machine, CPU utilization or disk utilization or network utilization. So for every service cloudwatch is going to provide some standard metrics So you really don't need to set up monitoring. Monitoring is anyways set up through Cloud Watch. Or there is a  by default monitoring set up. If you want. You can make changes. You can add more metrics of your choice, which are called custom metrics also. And then on those metrics, you can set alarms notifications. So metrics is the primary thing in the cloud watch. So if you use EBS volume, you should see some metrics for EBS volume. If you're using EC2, you should see some metrics. So there is an ec2 to instance and ec2 instance will have a monitoring tab. And these metrics are coming from cloud watch. So you have CPU utilization. And currently there is no data available it says because it's going to take some time to collect this data. I have recently just launched this instance, so it's going to take some time to collect the metrics and then you should see the graphs. So CPU utilization. status checks Network in. Network Out. Network packets in. Network packets out. Disk read. Disk Operations. Disk write and disk write Operations. These are the common metrics that you will see or standard metrics that you will see in all the ec2 instance Likewise, if you go to volume section. If you see your volumes, volume will also have monitoring and will be metrics for volumes as well. Read bandwidth, write bandwidth. Read throughput. Write throughput. So you take any service that you're using in your AWS account, it will have metrics which will be set by Cloud Watch. Apart from metrics, it also has events. So AWS events will give you a real time stream of any events that is happening. For example, you launching an instance or Terminating or taking a snapshot or creating a volume. These are events and cloud watch are going to capture all the events. What can you do with those events? Well, you can set triggers from those events. Mostly it's used to integrate with lambda functions. The next thing that Cloud Watch does is logs. Almost all the services will have an option to stream the logs. In ec2 you  don't have a direct option. You can set it from your operating system. From your operating system. You can set an agent which can stream logs to cloud watch service. And about these logs and events, we're going to see this in AWS part two. In this video, we'll be just focusing on metrics. So what we do with the metrics, well, we set alarms on them. If you really want to monitor the activity, you cant be 24 by seven looking at the graphs of cloud watch. But if something goes wrong, if the graph is spiking high, you should get some notifications. So alarms will help set you notifications. So alarms can be integrated with SNS service to send email notification. So we can see if the CPU utilization is crossing above 60. I should receive an email notification. That we will see how to do. We have an ec2 instance and we are going to collect. So cloudwatch is collecting metrics anyways We're going to set alarm on that metrics. We are going to say if my CPU utilization at a certain level, then it should trigger an email notification. So there are three services in play over here. Ec2 instance that is generating the metrics, Cloudwatch which is collecting the metrics, cloud watch is also setting up alarm or we are setting alarms in the cloud watch and then the SNS service. So Ec2, cloud watch and SNS in this example we are going to see together. For CPU utilization e-mail notification. In the second part of the AWS, we will be seeing also how to how to collect metrics, Sorry to collect logs from the ec2 instance and set metrics on the logs. And then based on that, you can send email notification or you can do some other stuff as well. We'll be seeing that in the next part of this. So as of now. We'll be seeing this. So let's get into action.
