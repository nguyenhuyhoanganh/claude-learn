# EBS

Welcome to the Elastic Block Store session. EBS, as we call it, generally are really the virtual hard disks for your EC2 instance. So EBS basically provides you two things. One is the EBS volume, the virtual hard disk, and the snapshot, which will be the backup of EBS volume. So these virtual hard disks we generally call them as EBS volumes. It's a block based storage like an hard disk. It is used to run your EC2 instance operating system, like when you are launching the instance we see in the storage eight GB of volume. That's the EC2 EBS volume, root volume we call it, which stores your operating system data. It can be also used to store your other data like database web servers, file data, which we'll be doing in this exercises. While you are creating an EBS volume, you specify an availability zone, and your instance also should be in the same zone where you have the EBS volume. EBS volume data is automatically replicated within the availability zone, and this is so you can protect from any upcoming failure on your EBS volume at the data center level. But this is within the same availability zone, not in multiple availability zone snapshot. We used to take the backup of the entire volume. There are different types of hard disks, so there are different types of EBS volumes also. First is general purpose and as the name says general purpose. It is used generally for most of your workload. Like you want to run a web service, you will go for a volume type of general purpose. So this volume is SSD solid state drives. And there is also one more in this category Provisioned IOPs. This is used to run large databases where you need really good performance for your database. So the IO speed will be more than the general purpose volume. A higher input output per second. The next type is throughput optimized HDD, used mostly for big data and data warehouses. Then you have cold HDD, which is generally used for file servers and it's really very low cost. General purpose is the most recommended one from AWS. If you are running a general workload, you don't have a specific large databases or big data. So general purpose is the best combination of right pricing and right speed. But then based on the speed or the cost, you can select any other volume type. Last one is magnetic, which is the slowest one and generally recommended for backups and archives. It's also very, very cheap. Let's check the AWS documentation to see more detail about these volume types. So if you do a simple Google EBS volume types, you should come into this documentation. So this SSD solid state drive HDD, the throughput optimized and cold HDD. Then you have some old generation disks as well. You can see this table where you see general purpose. You have new one GP3 and you have GP2. I'm showing you this documentation for use cases. When you really want to make a decision. You can check the use cases. And you can see other details also over here like volume size, how much volume size you can give, what will be the speed. If you are a storage geek, you will know these concepts IOPs. How much is 16,000 and how much is more than that? And if you see provisioned IOPs will have higher IOPs, you will see the use cases. The size can be also bigger. Then if you come down you have HDD and you have two. In this throughput optimized and cold HDD throughput optimized is good for big data data warehouse and also log processing and cold HDD will be for again large data, but which are not so frequently accessed, infrequently accessed, and the previous generation volume type. You have magnetic and you can go maximum one TB in that. And IOPs will be also very low but it will be very cheap. Okay. So for this exercise we will need CentOS or Amazon Linux. We are going to go with Amazon Linux 2023, and we are going to provision it with the script. It's going to simply set up the website from 2.com. I'll put the script in the resource section. You can download it from there. So let's launch the instance. Let me test web zero one Ami Amazon Linux 2023 Ami Instance type will keep it as T2 micro key pair will use the same key. And security group also from the previous setup and advanced details. Scroll down. And put the content over there. Now let's come back over here. Configure storage. You see by default it has eight GB. Now it depends on the Ami that we are selecting for Amazon Linux 2023 Ami. It gives eight GB of default volume. The volume type is GP3. You know you can wish to change this while we are launching it. Or you can add new volumes. Also, we're going to see all that once we launch the instance, we're going to go through some steps to have the proper clarity on this EBS concept. So keep everything default. Just make sure keep the script over there and launch instance. So my instance is up and running. I'm going to SSH and check the server status. So copy the public IP. Open your git bash or terminal. So ssh key path ec2 hyphen user at the rate the public ip ssh. Okay I'm in. Let me switch to root user and first check the service status system CTL status httpd. The service is active running Q to quit this and let's check the files ls var ww HTML. So these are our website files right. And in this you have images folder. So let's do LS to images. Let's check the website from the browser. Let's take its public IP and put it in the browser. So these images that you see on this website are coming from that images folder. And where are these images stored right now. Yeah, it's in that folder. But in which partition? So any computer will have a hard disk. The hard disk of EC2 instance are the EBS volumes. So if you run this Linux command fdisk l this is going to list all the available or connected hard disks. So Linux command in this you see this is the hard disk slash dev SDA. It's eight GB in size and it has few partitions. The partitions are always connected to a folder or a directory. Then only you can access it. You cannot just do CD to this partition. If you run this command df hyphen h, you should see this partition slash dev Sda1 this partition over here, this is connected to or mounted to slash. That's the root directory. And there's no other separate partition to store except this boot dot boot slash EFI. So except this, all the data in this machine is stored in this partition. Because everything goes in root directory. Now if you want to have a separate partition for any particular directory, we need to have another hard disk or any separate partition we need to create. And then we can mount it. That's what we will be doing in this lecture. Let's go back to EC2 console. So that's our instance. If we click on storage we should see here a disk path. Here that's the volume ID. If you click on this this is going to take us to the volumes section over here. And so that's the volume. Let me cancel the search. So there is a name to the volume. If you do not see the name to your volume you know this is the volume coming from your instance. You can check the instance ID attached resource. You can see you can check make sure you give it a proper name. So we're going to name it as web zero one. Hyphen three. Hyphen vol or volume. Let's save it. So now we have a requirement of adding extra storage for our images folder. Let's take a look at the requirement first and then we'll do this EBS lab. So coming back to requirement gathering storage size. If there is a requirement we need five gigs for the web server images. And yeah I have given like just five GB because more than 30 GB in EBS goes out of free tier. So overall you should have 30 GB and less to be in the free tier of EBS. So five gigs for web server images. So we have hosted a web service and that has some images. I want to keep those images into a separate storage. And that storage size should be five gigs. That's the requirement. Okay. So come to volume section we have the root volume over here. We are going to create one extra volume. So click on Create Volume. Volume type. We're going to keep it as GP3. That's the general general purpose latest one. Also you should see some other storage type. As we discussed provisioned IOPs cold HDD throughput optimized and magnetic. We're going to go with general purpose. That's our requirement size. Keep it as five GB for now and scroll down. We need to select the availability zone. So availability zone depends on your instance. If your instance is in one A you need to create the volume in one A, otherwise you will you will not be able to attach the volume. So we need to first check where our instance belongs to. So let me cancel this. Go to instances where zero one is in one c. So I need to create the volume in the same zone. So let's go back to volumes. Volumes there. Create volume GP3 size as five GB and my instance is in one C, so I'm going to create the volume in one C. Please do check where your instance belongs. You can also encrypt the volume. This is called as encryption at rest. So all the data that is stored in your volume will be encrypted. And to encrypt you need an encryption key. So you have a default encryption key. Or you can create a new key with KMS service. I'm just showing you that this option exists. If you have a requirement then you can do this. But we are not going to do that for this lab. But do remember you can encrypt an EBS volume. You can use the default key, or you can create a KMS key and then select that if you're going to try that. Keep in mind KMS keys are not free. The default keys are free, but KMS keys are not free. Okay, make sure to tag the volumes. I'm going to give it a name. So again the name should be meaningful. This shows that this volume belongs for the most project for web zero. One instance is for dev environment and it's for images folder. Let's create volume okay a volume is created. We don't see it. We need to refresh this. Then we can see this volume. Now this volume will be in. If you check the volume state you should see it as available. That means it's not connected to anywhere in use, means it's connected and you can see where it is attached to. Now let's select our volume say action attach volume. You see it shows the availability zone. It's going to show all the instance that is in one see select it and you should see your instance. If it's in the same zone device name click on the drop down and I'm going to select as slash dev slash SDF. Now this one really does not matter because when you attach this volume it's going to anyways. Change the volume name from this one to something like this Xpdf. Let's attach the volume and let's refresh it. Now let's go back to our instance, ssh to your instance. And now if you okay. All right. So now if you check fdisk l you should see the extra volume the extra hard disk connected. That's five GB in size. Now we need to create partition from this. So we can use the fdisk tool in Linux. So you can say fdisk and the path of your disk or name slash dev slash xpdf. Make sure you select this one and not this because this is for the root partition. Hit enter. Now there are many things you can do with this disk. With fdisk utility you can use M for help. So you see all the available options. There are many things that you can do. You can delete a partition. You can list the known partition. You can print the partition and add a new partition n. So let's type n, hit enter. So it's asking is this a primary partition or extended partition. Remember first four partitions are primary. And even if you hit enter it's going to select this. Or you can just type P for primary partition the partition number 1234. Which one do you want to give. So I'm going to say one anyways that is default. Now about the size. Now it shows you first sector. You have to select first sector. And then you need to select the last sector that will decide what will be the size. That's too complicated I understand that. So just hit enter here which will select the first free sector. Now you need to select the last sector. Either you can give the size like plus three g. That means three GB of partition. So your five GB of volume three G means three GB of partition will be created. Two GB will be free. Or if you just hit enter, it is going to select the last free sector that will create the partition of the entire disk. Use P to print. You should see your partition Xccdf one. That's the first partition and the only partition. If you're good with this information, you can use W to write the changes. Now if you do fdisk L, you should see your partition created. So the partition is created but it is a raw volume. It does not have any format. The different types of format we have NTFS, Fat extension, ZFS and Linux extension format, and ZFS are most popularly used in Windows Vista, NTFS, or Fat. You know, in pen drives, USB drives, they are generally fat partition. So you can use this command Mkfs. Just hit tab two times quickly and you should see all the available commands with the partition names, sorry. Format names. So you have extension two, you have extension four, you have Vfat, ZFS, MS-DOS. Also you have fat partition. So we are going to go with extension four. And there's ZFS. Root volume is with XFS. ZFS is used for large volumes faster IO and extension for is just like general general purpose. So anyways we're going to go with extension four. So you say mkfs dot extension for ext4 and you give the path of the partition okay, Xccdf one the partition and is going to format it. And now you can mount it. So let me clear the screen. So if you want to mount it on var dub dub dub HTML images, it's already data in that you know if if we mount it it will get mounted. But this data will be now in the root partition. It will be kind of hidden. You will not be able to see the data. The data will exist when you unmount. You should be able to see the data so you know it's better we move this to some other folder. So I'm going to create a temp folder. I'm going to say img backup something like that. And I'm going to move all this to make all the images to this temporary folder. So I can copy it back later when we mount it. Image backup. Right. So now this will be empty okay. Now let's mount it. Now this is a temporary mount that I'm showing you right now. So mount command slash dev slash SDA F1, the partition path and the folder or directory where you want to mount hit this command. And now if you run the command df hyphen h you should see this directory is mounted on this partition. That means whatever data you put it on this will go into this partition. You can see it's complete size. It's available size. Now if you check your website, you know you can refresh this or open it from a new browser. You will not be able to see the images because there are no image in the images folder. So let's copy back our images which is in temp. I'm going to copy all the images to var HTML images folder. And I'm going to restart the service systemctl restart httpd. And now let's check the website. If you refresh the website you should see all the images back. Now this is a temporary mount. If I reboot this computer this VM this mount point will be detached. So I'm going to show you how to do a permanent mount. First let's unmount it. You mount and you can give the path of the directory. I need to give a space here. Okay. You will not be able to see the mount point. Now let me clear the screen. So I'm going to show you the permanent mount you need to do. VI, slash, etc. fstab this file. Open this file in VI editor. You should see here few entries. So open a new line here. Go to the last line and hit O to get into insert mode and the new line. And first thing you mention is the partition path slash dev slash Xccdf one you can use then tab or space, and then you mention where you want to mount it. Right. So that's the second entry or the second second column where you want to mount it the folder. Like here you see a UID. You can give a ID also to a partition here. That's the ID for the root partition which is giving the partition name directly. Then the folder where it is mounted we're giving images and then the type. So we have formatted it with extension four. Then hit tab and then options. So we're going to say defaults. We're not adding any special options. And again hit tab and say zero space zero one is for FSK and one is for the dump. So we're saying don't do any file system check or don't take any dump when things crashes. Okay. So we're taking all defaults option. Now escape colon wq save and quit. So now see df hyphen. There is nothing mounted. But when you run the command mount hyphen a this command is going to read the fstab file and execute the entries. Now if you do df h you should see it mounted. Now while running mount hyphen aa command. If you get any error, check the entries whether the path is correct or not of the partition. Check the path of the directory. Check the options. Spelling. Typographical error. If everything is good, it should get mounted. Okay, so one last time, restart the httpd service and join me in the next lecture where we're going to see more on the volume side.
