# More In Ec2 Part1

Instructor: Before you launch an instance, let me tell you a general best practice of creating an EC2 instance. When you're launching an EC2 instance, definitely you have some requirements. So first point is gather the requirement. If you get a task like launch a web server with an operating system, so and so, storage and compute size, we are going to jot down the requirement. I'll show you how you can collect those requirements. So first part is gather the requirement. Once you have the requirement, then create the key pair. This you can do while launching the instance also, but do it before you launch the instance. Then create the security group, which is the firewall, and then finally, start launching your instance. And when you're going through the launch process, you can select your security group, you can select your key pair, and you can make all the decisions based on your requirement. Talking about requirement gathering, like if you're going to launch an EC2 instance, you may need some information. And mind you, this is not all the requirement gathering that you would be doing. You could be doing less or more based on your requirements. But some general requirement gathering like operating system, what operating system you're going to choose? Okay, so this will tell you which AMI to go with. Size, RAM, CPU, network, speed, how much you need? Based on that you'll be selecting the instance type, okay? So let's say you are doing it for a development environment, we can go with minimum. Storage size. What you're going to store? First is primarily operating system storage, and then any application that you're running that storage, right? So let's say, we get 10 gigs. The project that you're working on get... You know, such kind of information is helpful while tagging the instance. Services or applications that will be running. Like for example, you could be running SSH, HTTP, MySQL or any other service. Environment, this for which environment. Development, QA staging, it's pre-prod, production environment. Based on that, so many decisions can be made. The login user, who is going to log in, who will be the owner? This is used for tagging also. This is used for, you know... It's helpful for tracking also. And based on all this requirement and the right process, we are going to now launch an EC2 instance. Okay, so back to AWS console in EC2. So in previous lecture, we deleted the instance, and you should see this instance in terminated state. Terminated means gone. This information will be there for two hours but the information will be also gone. If you select this instance and go to instant state, you see you cannot do anything over here. Now let's launch this instance once again, but this time we'll do it properly. So first, we are going to go to key pairs. Network and security. Go to key pairs. We already have a key. We are going to delete this key. To confirm, type Delete, D capital, Delete. Okay, so the public key is gone and the private key that we downloaded in previous lecture is also now useless. So you better go to your downloads folder and delete that key as well, so we don't have any confusion. Okay, so let's click on create key pair. Let's give key a name. Now name is extremely important when you're working in projects. So if you naming convention you remember you should have the project name, the environment name, the key is four. Now you can use one key for all your instances or you can use a separate key for every instance. So let's say if you have 100 instances, you're going to create 100 keys. Both are wrong. You should not have one key for all your instances. Also, you should not have separate key for every instance. You can use, you can divide keys. Let's say you have different types of environment in your project, dev, qa, staging, production. So for every environment, create a separate key. So I'm on tooplate.com and I selected a template, Moso, and let's say this is my project name. So I give the project name hyphen, the environment it is for. So I'm going to say it's for dev environment and I'm going to give it region name also because you might have this instance in different environment. For different environment, you need different keys. You cannot have same key in different environment. Key are region-specific. So this key will be only available in North Virginia when I'm creating. Key five format, pem we are going with, you can use ppk also if you're using PuTTY, but if you're using terminal or Git Bash like me, you can use pem format. Let's give a tag also to this. A name tag, same name, and let's say Create key pair. Okay, private key is downloaded with me in my computer and that's the public key. That will be the lock and this will be the key to open that lock. Next, let's go to security group. Let's go. Yeah, there, Network and security, Security Groups. So we have this security group from the previous instance. Let's delete this. Delete security group. Delete. Now if you get an error that, you know, you're not able to delete the security group because there is a network interface attached to it, in that case, go to network interfaces like any computer has a network adapter. Our VM also we did Vagrant VM on virtual box where we have bridge adapter, different kinds of adapters. Your computer needs a Wi-Fi adapter, ethernet adapter, like that. This EC2 instances also needs network interface. So when we create the instance, it also creates the network interface, and the security group rule that we apply the IPs, everything is for the network interface. That network interface is connected to the instance. So if you see any network interface over here selected, go to Action. First, try to disassociate. Sorry, detach. First, try to detach, and then after that click on Delete to delete that network interface. Go back to security group. Let's create security group. Let's give security group a name. So the project name, hyphen, web. Now this is for web servers, so that's why I'm giving the name web. The security group case is also similar to key pair. You can use one security group for all your instances or you can have separate security group for separate instances. Again, both are wrong. You should divide the security group based on your environment and then the type of service. Let's say all web servers, you're putting it in one security group and those instances or those web servers are for dev environment. According to that, we're giving the naming convention. Now if you're trying to skip naming convention and give whatever you want, trust me, it's a very, very bad practice. You're going to suffer a lot in real time due to this. Right now I cannot tell you who the whole story, it will only come with experience. Don't go there. Use proper naming convention. Now in security group, you have two kinds of rules. Security group is firewall. So in any firewall, you have inbound rules and you have outbound rules. The traffic coming towards your instance, that's inbound. The traffic going out of your instance or of your server is outbound. So I have seen this many people when things are not working when they don't understand the firewalls. What they do is, I'll show you, Add rule, type, All TCP or post All traffic, allowed from any IPv4 and also all traffic allowed from any IPv6. I'm not kidding, but in some very sophisticated projects also I have seen these things. So this rule simply means there's no restriction. There are no gates, no doors, no windows. Anybody can come to the party. It's like that. Now before we move ahead and add these rules, let's take a look once again. This is a firewall, and a virtual firewall that controls the traffic for your instance. You can have one security group connected to multiple instance or you can apply one security group rule to multiple instances. And these are basically rules for your incoming traffic and your outgoing traffic. And they're stateful, so if you are saying inbound traffic, you're allowing port 22 to do SSH 21. The same outbound rule be automatically updated. You don't need to mention that. So two kinds of rules in security group, inbound rule and outbound rule. Inbound rule is the traffic that is coming towards your instance. Outbound, the traffic that is generating from your instance and going out. So if you don't know firewalls, think of them as the watchman or the gatekeeper who has a register, and from the register will be checked who is allowed to come in and also who is allowed to go out, and where are you coming from. That is also important, okay? Watchman probably for a party or a bouncer maybe. So if you're coming from a particular place and you want to go in, it will be checked in the register that is there entry for you. If it's there, you are allowed in. And similar way, if you're going out of the party, are you allowed to go out? That will be weird for humans to do that. But, you know, the outbound rules are set sometimes to increase more security. So let's set proper rules. We will remove these entries. We'll add specific rule. One thing we need to SSH to this instance. So you can select type SSH where it'll select the default port as 22 or you can say Custom TCP rule 22. Now you do not want people to do SSH to your instance except who are managing it. So mostly it'll be your IP, like your corporate office IP. And when you're sitting in that network, you can just do my IP and it'll give you that public IP. And in corporate networks, the public IPs will be static. Okay, so for now, we'll keep this rule. Again, once we deploy our web service, we'll come back and add the rule for port 80. Do not add rules in advance. We know there is SSH, we are going to do SSH, so that's why we're adding this rule. We are not installing anything on that. There's no web service running, so there's no point in adding any rule. Once we do that setup, then we will add the rule. The outbound rules says all traffic can go anywhere. This destination going out. Now if you make any change in the outbound rule, you will not have internet connectivity to your instance. Plain and simple, because the internet traffic goes out on various different ports. Most of the times, this is handled by a proxy. So here, outbound rule will be given for the proxy IP and the proxy server will handle the internet traffic. But for now, we have to keep it as it is. All traffic allowed from anywhere. Do not touch outbound rule ever until unless I tell you to do because we'll need lot of internet stuff in this course, for our instance, of course. Okay, now that is all. You can give the tag. I'm going to give the name tag. Same name, try to give more tags in real time, so it makes more sense. Okay, click Create security group. Okay, so have the key pair and we have the security group. Now let's go ahead and launch our instance. So I'm going to say go to instances and say launch instances. I'm going to give four tags over here. Name, project, environment, owner. Now I usually use these tags if no naming conventions are mentioned, but if naming conventions are mentioned, there is a standard in the organization, you better follow that. Now, resource types. I want to tag my volumes and network interface so I can identify the volumes and network interface belongs to which project, who is the owner and for which instance it's connected to. We'll see about volumes in later lectures. Okay, now come down. For this one, we are going to go with Ubuntu. The latest one, Ubuntu Server 24, free tier eligible. So just select it. You don't need to worry much about this one. Scroll down, t2.micro will keep that as the instance type. Key pair, we have already created the key and we are going to select that one. See, it's easy to understand that this key can be used for this instance, right? This is for Moso project dev environment, North Virginia. We can simply select this key. Network settings, edit, and security group, select security group, drop down, and now we can find our security group. Let's say you have some 50 security group and if you don't have proper naming convention, how hard it'll be to select, right? So that's why I say use proper naming convention. Volume will keep it as it is. As I said, we are going to talk about this in later lecture. Now we are not going to put any user data commands over here. No user data commands or script. Simply launch the instance. Once the instance is launched we're going to SSH and then execute our commands. Okay, let's SSH to our instance. Let's select the instance. You should see the filter over your instance ID, remove this. Okay, so web01, which is in the running state right now. Take its public IP. I'm going to open Git Bash. Use terminal. If you're using macOS, ssh -i. My key is in the downloads folder. The key name is Moso. See, easy to do, right? The username for Ubuntu AMI, official Ubuntu AMI is Ubuntu. At the rate and use the public IP. Hit Enter. Are you sure? Yes. Okay, let's switch to root user. Let's clear the screen and let's execute our command to install. Apache 2, apt update &amp;&amp; apt install apache2 -y. In Ubuntu, when you install the service, most of the time, it will be in running state anyways, you don't need to do anything. Systemctl status apache2. It's active running. Q to quit. Okay, let's download the template. So use Brave browser because it will, you know, avoid too many ads. So find your favorite template or the project name that you have selected. Let's download this. Let's click on Download. Oh wait, we need to get the download URL. So hit F12 if you're using Brave browser. Make sure you are in the network tab and click on Download. Click on this zip. In the header, in the requested URL, you should get this URL, copy this URL. Copy, wget, shift insert to paste in Git Bash. I should download. Okay, should have the zip file. Let's unzip it. Okay, I need to install unzip, apt install unzip. Okay, I have installed unzip. Now let's unzip this. Now let's copy recursively everything from this folder to /var/www/html, and then we'll restart the Apache 2 service. Okay, we'll check the process, apache2. It's running. We'll see the content var/www/html. Content is already there. Let's check the port number ss hyphen tunlp, pipe, grep and use the root process ID, 2579 for me. Okay, it's hyphen, ss hyphen. Okay, so the port number 80 is allowed from anywhere. I think you know where I'm getting at, right? So now let's access our web01. Let's take its public IP. Let's put it in the browser. We know by default. it goes on protocol HTTP and port number 80, right? So when we hit that, we should see our web service, but we want... Why? You remember. That's right. Security group. Security group allows only port 22. Let's go back to our security group. Web01, security. Make sure you selected the running instance. Go the security group and edit inbound rules. Again, not outbound inbound rules. So only 22 is allowed. We are going to add here port 80 allowed from any IPv4 and we're going to say port 80 allowed from any IPv6. Now I know I said allowing from anywhere is bad, but see this, we are hosting it for public, right? In real time, yeah. When we host a website, if you are hosting it for public, then of course we need to allow it from anywhere. So add those rules. Say, Save rules, and then let's refresh and we should see the website. Okay, so that is all in this lecture. Next lecture, we are going to see more settings and features of EC2 instance. So join me the next one.
