# VPC Setup Details

Instructor: In this video, we will be creating the VPC, but before we start doing it, we'll create the blueprint. Okay, so this is the design what we are going to go with. We will have first, we'll give the VPC range, the main network range, and we'll say it is 172, dot, let's say 20.0.0/16, okay? You know that gives us like, more than 65,000 IP address. We need to divide it into smaller subnets. So we will go with four subnets to begin with. Okay, this, you'll have two public subnet, and two private subnet. Again, two for high availability, we are going to divide it among multiple zones, two zones, specifically, okay? So we'll have two zones. We'll decide the region. We'll go with region North California, so region called US West one. It has two zones. You have US-West-1A, and you have US-West-1B. Okay, so you'll have two public subnet in two different zone, two private subnet again distributed in these two zones. So we say total four subnets. So let's URL subnet range 172.20, dot, I'll start with 1.0/24. That's four subnet, 172.20.2.0/24, 172.20.3.0/24, and 172.20.4.0/24. Okay, so two public subnet and two private subnet, and we'll also give the zone names there. Okay, first subnet in US West-1A, second in US West-1B. So that was about the network design. Now, we also have to create some components. We need one internet gateway. We need two NAD gateway, but for the sake of saving cost, we'll just create one single NAD gateway. But if you're doing high availability for NAD gateway, then you should have minimum two NAD gateway distributed in two different zones. You also need one elastic IP, which will be assigned for the NAD gateway. So NAD Gateway will have an elastic IP. Route table, we need two route tables. One will be for the public subnet, and one for the private subnet. So this subnets route table will be attached to these subnets, and it will tell public subnet to go to the internet gateway, and private subnet to go to the NAD gateway, as you see in the diagram. We'll need also one Bastion host or the jump server in the public subnet so we can access instances in the private subnet. Now, that should complete our design, but we are also going to see NACL, network access control list for the public subnet. It's like a firewall, like you have security group. It's gonna be similar to that, but will give you more control, and this is for the subnet. Like security group is for the instance, NACL is for the subnet, and in NACL, you have allow and deny rule both. In security group, you just have the allow rule. So we'll be seeing that once we set up our entire VPC, we'll be seeing NACL also, and we'll be also doing, we also will create one more VPC, and this is so we can see what is VPC peering. So in this second VPC that we are going to create, we are not going to create subnets and the complete thing, we'll just create the VPC, and we'll peer our main VPC with the other VPC that we just created. So with all that information, we can go to AWS console and start creating our VPC.
