# Route Tables

Instructor: So we have a VPC, we have our two public subnet and we have internet gateway. Now we are going to connect this internet gateway to the subnet through route tables. So click on Route Tables. Okay. You should see two route table. One is for the DEFAULT VPC, another one is for vprofile-VPC. No, I did not create this route table. It got created automatically when we created the VPC. So let's change its, now give its name Default-PubRT. That's for default VPC is public route table. And this one for of vprofile-default-RT. We are not going to use this one. We are just giving the name so it's easy to identify. So this is default VPC's public route table. If you look at its routes or routes you will see here entry 0.0.0.0/0 goes to the internet gateway. This is the default VPC's internet gateway. Same way, we are going to create a route table and route the traffic to our internet gateway. So let's click on Create Route Table. I will name it as vpro-public-RT. So to identify that, this is the route table for public subnet and for vprofile-VPC select our VPC here, vprofile-VPC, right? And let's create it. And after that we have to add the rules and subnets. So first we will connect this VPC, sorry, this route table to the subnets. So click on Subnet Association. Say Edit Subnet Associations and select your two public subnet. See, easy to identify because you, because we named it properly, Save Association, okay? So now the final thing. We have the route table, we have associated to our two public subnet but still there is nothing public about these subnets or this route table. This one single entry will make this route table and the subnets public. Click on Edit Routes. Add route 0.0.0.0/0 goes to the internet gateway. Wait a second. Yeah, find the internet gateway. And that's our vprofile internet gateway. This single entry will make the subnets public subnet. Save changes. Okay, so now we have a fully functional public subnet. You can launch instances in that, you should be able to access internet from that instance, which are in this public subnet. Okay, yeah. So I was saying you have a fully functional public subnet. The instance can access the internet but you cannot access the instance because the instance will not have the public IP. So far we have seen, we launch the instance, we get the public IP and we then we SSHH or exit from the browser from the internet, right? Because it has the public IP. But subnets by default will not allocate any public IP. You need to enable that setting. So you need to go to Subnets, find your public subnet. That's our public subnet one. Select the check mark. Public subnet only, okay? Public subnet action. Edit subnet settings. And you see here an option Auto Assign IP Setting. You have to enable this. Now, when you launch instance in this public subnet it'll automatically get a public IP. Let's save this. Let's go to another public subnet. Do the same thing. Action, Edit Subnet Settings, Enable, Save. Okay, we are done here with the public subnets but not with the private subnets. We are going to launch NAT Gateway create route table associate it with the subnets. Same thing, what we did for the public subnet except now we will have a NAT gateway for private subnet. So wrap this up and join me in the next lecture.
