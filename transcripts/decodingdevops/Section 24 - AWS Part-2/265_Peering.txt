# Peering

Instructor: All right, so, so far, you have seen everything in the VPC and the purpose of creating VPC is, our own custom VPC is so we get security through private subnet and high availability through multiple zones. And we distribute our infrastructure in multiple zones through the subnets. But when you're working in a large-scale environment, you will not have just one single VPC. You may have one VPC for the web infrastructure, one maybe for API, one for the database. You may have multiple VPCs. And in order to connect your resource from one VPC to other VPC, you need to do VPC peering. It's very easy to do, but very much required. So we have the VPC and subnets from the previous lecture. We are going to create one more VPC here, not the whole VPC, just the CIDR block. And just for the sake of argument, we are going to create it in a different region. You know, you can create it in a different account also. So let's say I'm going to go to Oregon. You can go in any region of your choice. As I told you, we're not going to create the whole VPC. All right, so that's a default VPC in that Oregon region. I'm going to just give it a name, default, save it just for the identification. And I'm going to create a VPC, just the VPC. I will call it as, let's say, vpro-db. Okay, now the CIDR range, now CIDR range cannot overlap. So our VPC, which is in North California, the CIDR range is 172.20. So here we're going to give 172.21 or 22. Just make sure it doesn't overlap with our previous VPC. /16 range. You can give anything, /16, /24, your choice and let's just say create VPC. Okay, when you create a VPC, you will also see the route table that gets created for that VPC, vpro-db. That's the first one. So let's give it a name. We'll say vprodb-rt. Okay, this is important. I'll come back to it in a moment. Okay, so in the VPC, that's vpro-db VPC, which is in Oregon region. And then you have vprofile-VPC, which is in North California. We're going to connect these two VPCs. That is called as the peering. So go to the peering connections. So the requester will be our vprofile-VPC in the North California region. From here we're going to create a request. Click on create peering connection. I'm going to give it a name, vpro, and I'll just add the NC, like North California. And I'm going to select the requester VPC over here, the VPC that is requesting, which is vprofile-VPC in North California, and it has this CIDR range. Now you can pair with another AWS account VPC also. For that, you have to select another account and give the account ID but we have the VPC in the same account, but in a different region. So let's keep my account. And here in the region, we'll select another region and that is in Oregon. And here, you have to give the VPC ID of the acceptor, which is in Oregon, right? That's the VPC ID. Let's copy that. I think we can copy from here also VPC ID. Copy and let's paste it there. And let's say create peering connection. Okay, so the request we have initiated and you see it says pending acceptance. Now, go to the other region. So I'm in Oregon and go to the peering connection. And you should here see a request. There, pending acceptance. So you can just select this. Now, in real time, be careful who is the requester. Match the VPC ID. And you can also check the account ID also. We'll say action and we'll say Accept request. Accept request. Okay, so in a moment, it became active. So there is now a connection between these two VPCs but there's this connection. But the instance, if you launch, they don't know how to route the traffic. If the IP, let me come back to our VPC, North California VPC. Let's go to the route table, and let's take example of one of the route table, public route table. Or let's go with private route table. Let's click on the routes and let's click on edit routes. Okay, now, if the traffic wants to go to any IP in this range, it is going to route it locally, that's fine. Everything else is going to go to the NAT gateway or Internet Gateway, whatever rule you have given. But what about if the request is going to another VPC, right? That traffic also is going to route to the NAT gateway, Internet Gateway, right? So you need to add a rule here, and you need to give the CIDR range of the target VPC, right? Any traffic that wants to go in this network, the traffic should be routed to just type peer here, peering connection. And you should see your peering connection here, there. So the traffic can be routed to that particular VPC. And this goes both ways. You have to go to the other VPC's route table, route table. So you need to enable this for all the route table that needs the access. So vproddb route, table, routes, edit route, add route and let's give the range, which was 20. Again, pairing connection. Select pairing connection. Keep in mind, this goes both ways. And as many as route tables you have, you need to edit those route tables and make sure you forward the request to that peering connection, only if you want it to forward. If you don't want the public subnet to be routed to the peering connection, then don't give it. Only private subnet you want, only given the private subnet route table. Okay, so that will be functional routing but there is also security group now, right? In the security group, you cannot reference the security group ID, which is in another VPC, in the peering connection VPC. So the only way is you have to give the range, the network range. For example, let me go to a security group and I want to say allow 22 from a particular VPC or instance from that VPC. I have to give either that instance private IP /32 or I can give the entire range. I can say like this, 22.0.0/16 or if there is a specific instance in another VPC, let's say its IP is 10.12/32. Okay, if it's a specific IP, you have to give /32 remember. If it's a range, you need to make sure you give the range. Like if for the entire VPC, you can say 172.22. Sorry. 0.0/16 or maybe a subnet like 2.0/24. Then only you'll be able to access, right? And with that final thought, let's close this. So we have already deleted the resources, which are not free. Now these resources, if you want, you can keep, if you want to delete, you can delete. So Oregon VPC, which is just the VPC, I'm going to delete that VPC. Okay, it says unable to delete because there's a peering connection. So first, you have to remove the peering connection. So action and delete peering connection. Okay, delete related route table entries. Sure, delete it. Okay, peering connection is deleted. Now let's delete the VPC. Be careful. Do not delete the default VPC. Delete VPC and delete. Let's go to North California. Let's see the peering connection here. Okay, that is also deleted. Now we already deleted the NAT gateway and released the elastic IP. Now we can delete the VPC. Now, when you delete the VPC, it's going to delete the entire thing, the rest of it, subnets, route table, internet gateway, all of that. Okay, that was the cleanup. Now I know there are many things that we have covered. It's not easy to grasp all of it at once. The only way to make sure all this thing is imprinted in your mind is with the practice. You have to repeat it again and again until unless you can do all this by yourself without any help. Generally speaking, it's not mandatory to learn the entire VPC setup for a DevOps, but take it from me, from my experience, I consider this knowledge as highly essential for a DevOps engineer. Irrespective of what cloud provider you're using or what you're doing in your work, you should know these entire VPC concept that I have covered. If you're interested more, there are courses or documentation to learn more into VPC and I encourage you to go and learn those things. It'll make you a better DevOps engineer, a better IT professional. With that note, let's close this session. Thank you very much.
