# Website in VPC

Lecturer: All right, last piece. So we have tested bastion host. It's working fine. Now, it's time to launch an instance in the private subnet and not just launch, we are going to set up a website on that from tooplate or any website you can set up. We are then going to create a load balancer which will be in the public subnet and access the website from the load balancer from the internet. The load balancer should be able to route the traffic to the private subnet or instance in the private subnet. Okay, now in order to SSH to the instance launched in the private subnet, we will need the login key. And as we already discussed previously, we need to first SSH into the bastion host. From here, we are going to SSH to the instance in the private subnet. So we need to have the login key stored in the bastion host. So first, we'll create a key pair copied in the bastion host, which key or this key we are going to use to access our instance in the private subnet. So let's go to AWS console. Let's go to key pair. I'm going to create the key pair, give it name as web-key. I can give any name you want. Pem file. Create key pair. Okay, if you're in the bastion host, come out, and now we have to SCP, copy our login key to the bastion host. So the command will be scp -i. You give the path of the bastion instance login key and then you have to mention the file that you want to copy. So it's in the downloads, right? This is the file that we want to copy and the destination username at the rate the bastion host public IP, colon, slash, we'll give it temp for now. Oh, wait, let's give it home/ubuntu. So Ubuntu user will have permission on its home directory. So we can copy this file over there. Let's hit enter and see. Okay, so this file is copied to the bastion host. Let's do ssh to the bastion host. And if you do ls, we're in the home directory, right? So I should see the login key. Okay, let's stay here. Let's launch the instance in the private subnet now. Let's go to the instances. I'll create the security group while I'm creating the instance itself. Launch instance, I'll name it as web01-priv just for the identification. I will use, let's use Amazon Linux 2, but you can use anything you know how to set up a website. Amazon Linux is YUM install and download and set up the website. If you want to use Ubuntu, you can use that. But make sure you know the commands. T2.micro is fine. Key pair. That will be the web key. Make sure you select the right key. Network setting, Edit. Select our vprofile VPC and make sure this goes in the private subnet. So yeah, that one, private subnet 1 or 2, anything is fine. You see, it will not get any public IP and we have to create a security group for it. So we'll say web01-sg is the name and description here. Okay, now ssh from anywhere, definitely not from my IP. Also not possible because you cannot ssh from your IP to this private instance. You have to go to Custom and mention the bastion host security group here, okay? Bastion host security group. And we need to give one more rule here, port 80 allowed from the load balancer security group. But since we don't have a load balancer, we'll do it later. Let's launch this instance. Okay, so my instance is running you see there's no public IP. It has only private IP and the only way to access it through the bastion host, so ssh to the bastion host. I'm already in the bastion host and I already have the key. So I'm going to say ssh -i, the path of the key, username, ec2-user. Because I have used Amazon Linux 2, so that's the username for it. And let's get the private IP of the instance. Okay? Now, remember this resources within the VPC irrespective of which subnet they are in public or private, can access each other by default, provided the right security group rules are given. So web01 is in the private subnet, bastion is in the public subnet. The traffic will route because of the route table entries and because of the security group rule, we have given 22 allowed from bastion. So we can access it on port 22 also. Let's hit Enter. Yes, the response is coming back and we are, no, sorry. We have to give this file permission 400, my mistake. Unprotected prior key file because we copied it. So the file permission changed. We need to make sure it's 400 and then, yes, now we are in Amazon Linux 2 AMI. Okay, let's quickly set up the website. I'm going to install a few packages. Yum install httpd wget to download the website from tooplate and unzip to unzip the template, install these three packages. Let's go to tooplate.com, find whatever template you like. I'm going to go with this one. Let's hit F12 and let's click on download. Okay, not actually going to download, we just need to get the URL. Okay, that header, let's copy this URL. Okay, let's do wget, shift, insert to paste that URL, enter. Okay, does the file exists? Let's do unzip, ls, and let's copy everything to you know where, /var/www/html. Systemctl restart httpd. Let's make sure httpd is also enabled. Okay, so the website is set up. There is no way we can exit from internet. There is no public IP. Even if you assign a public IP, it won't be passing through the NAT gateway. So the only way to access the website in this is through the load balancer which we are going to place it in the public subnet. So let's go to EC2 console and let's create a load balancer. First, of course, we need to create the target group. So create target group for instances. Port 80 is fine. VPC, make sure you select vprofile-VPC. Everything is else is default. Go next. Yeah, of course the target group name web zero, or just I'll say web-tg, vprofile VPC. That's right. Let's go next. Yeah, you should see instance from the VPC, add web01 instance, include a spending below, create target group. Okay, now security group for the load balancer. So go to the Security Groups. Create security group, web-elb-sg. And once again, make sure you select your VPC, vprofile VPC. Everything we are creating in vprofile VPC, so whenever you see the VPC section, make sure you select our VPC. Okay? Now here of course, we can give port 80 from anywhere. This is going to be a publicly accessible website. So port 80 from IPv4 and port 80 from IPv6 and create security group. Okay, final piece, load balancer. Go back to load balancers and click on Create load balancer, Application Load Balancer. Okay, give it a name. Network mapping, make sure you select vprofile VPC again and it should show you the zones. So select the zone and make sure this is going into the public subnet of our VPC, public subnet 1, another zone, you should see here, public subnet 2. This is extremely important. Otherwise the load balancer also goes into the private subnet, load balancer will be inaccessible from the internet. Okay, Security group. And you should see here web-elb security group, Port 80, Listener route to the target group that we created. And that is all create load balancer. Right? Now there is one more thing we need to do. Yes, security group if you got it right. So our instance web01 instance, has a security group. Let's go to that security group of that instance. And its rule says, let's go to edit inbound rule, 22 allowed from the bastion host, but no rule is given to allow access on port 80 from the load balancer security group. And make sure you give that rule port 80 allowed from the load balancer security group. Save rule and wait for five, 10 minutes until the load balancer is in available state, the target group instance is in the healthy state. It's going to take some time. So I'm going to pause my video and resume in five minutes. Okay, the load balancer is active and I check the target group and I see the instance as healthy. If it showing unhealthy for you, wait for some time. Still showing unhealthy, check the security group rule of the instance. It should allow port 80 from the load balancer security group. Okay, now let's go to the load balancers get the load balancer URL and access our website. Okay, so let's copy the DNS name of the load balancer. Open the tab, the URL. Okay, and it looks good. So we have configured the entire VPC and we are using it also. Public, private subnet, everything is working fine. Now, it's safe to delete the NAT gateway and release the elastic IP for now. In the next lecture we are going to do VPC peering and we are just going to see how to do peering. We don't need to perform the whole access and use case for that. Okay? So let's start deleting things one by one. Let's delete the load balancer first. Action, Delete load balancers. Confirm. So first, we need to delete the resources from it, right? Okay, load balancer is gone. Instances, let's wipe out all the instances. Instance state, terminate. Okay, wait until all the instance are terminated and then you have to come to the NAT gateways. The reason I am cleaning up everything or showing the cleanup is so you don't waste extra money through NAT gateway, elastic IP and stuff. So select your NAT gateway, action, delete NAT gateway, type delete to confirm and delete. Okay? This delete is going to take some time. Once it's completely deleted, then you can go to elastic IP and release the elastic IP. But wait for some time until it's all deleted. Okay? The State shows Deleted, but let's see if we can release the elastic IP. It takes time. Even if NAT gate was deleted, it takes time to release the elastic IP. So let's try release elastic IP, Release. Fine. That's good. Okay, elastic IP is also gone. Now, we are safe. Now, don't worry, if you want to, you redo this setup, you just need to create the NAT gateway and go to the route table, private route table go to the route, edit routes or route. You see the NAT Gateway shows black hole because this NAT gateway does not exist. So you deleted it. And whenever you want, you can create the NAT gateway and you can just give the NAT gateway that you have created, right? It's not going to show any NAT gateway. It does show, but really it does not exist, right? Like that, you can select the NAT gateway if you have recreated it. Other things you can keep, don't worry, no charges for other things, internet gateway, all those things are free of cost. In the next lecture, we'll see what is VPC peering and why it is required. So do not delete the VPC subnets, internet gateway, and route table. We will need this in the next lecture. Just delete the NAT gateway elastic IP and keep the rest of it. See you in the next lecture.
