# Ec2 Logs

Instructor: Welcome to the session of Log Management on AWS. First, we'll understand the logs just in general and how we archive them and move to some other storage and then we'll see some different solutions for those things. So I have a web server. It's created by using the AMI Amazon Linux 2. Amazon Linux 2, you also need to use the same AMI because I'm going to run the command specifically for Amazon Linux 2 AMI. So it has a web service running, Wave Cafe. This is the same setup we've been doing in this course. Install HTTPd and a download template from tooplate.com and use that. So whenever user access any website an HTTPd service it generates access log. So let's go to var/log/httpd. It's a CentOS 7 operating system, so log file of httpd will be at this location. You have access_log, if you check that. Okay, I just accessed it for like once or twice and you see that much log it generated, okay? I didn't even click anywhere. Okay, now let me do "tail -f", and let me access it from some other IP now. Okay, you see that I have accessed it from some other IP, and already, I'm clicking on a few places there on the site, and you see the logs are getting generated. So you see, by just access from two place generates this much log, imagine thousands of users or millions of user accessing your web services, how much log it'll generate. Like that, different services, different processes generate different log file, and their size may grow and that will eventually take the disc space. So it's always a good idea to rotate the log. One common thing you can do is archive the logs, send it to some somewhere out of the system, and then you can clean the logs, okay? So we'll create an S3 bucket, we'll archive these logs and send it to S3 bucket, and then we'll delete the logs. So I will go to S3 service. So I'll give the bucket name as wave-web-logs, and I'll give some numbers to make it unique. Okay, I select the same region and I say create bucket. Okay? So that's my bucket. Now let's archive the log files. I will say "tar czvf", say wave-web01-httpdlogs, and I have to give you timestamp also, okay? So 19122020, oops, 2020. I'm trying to go to the future, 2929. Okay, 2020, tar.gz. Okay, I'll say star, so all the log files, like these two log files in the current working directory will be archived with this name. Okay, good. I have it archived. I'm going to create a directory in the temp drive. Let's say, logs-wave, and I will move this log archive to if I can type today, "tmp/logs-wave/". Okay, now I'm going to clean all these files. So I'll say dev/null, redirect the output to access_log file and also error_log. So that's going to clear everything from the log file. You should not have anything now in the access_log. Oh, I think it got generated again. Wait a second, access_log, and then we'll send it to the error_log. Yeah, it's clean now. Okay, now I'm going to use AWS CLI to move this log file, this archive log to S3 bucket. So I'm going to install AWS CLI. I'm in Red Hat machine center, so I'm gonna say yum install awscli. Okay, so we can do this AWS S3. You can run help and you can see what are the available options with S3. So you can use cp, you can copy the log file, you can move, you can remove, okay? There's sync option, right? There's make bucket, remove bucket, and list option. So let's just say "aws s3 ls". It's gonna, it should list all the buckets, but for that we need to set up the credentials. So we need to create an IAM user with access key and secret key, and we need to run the command aws configure give the access key secret key so our awscli command will use those credential to access our AWS account. So let's go to IAM service and create a user. So I will say add user and I will name this as s3-log-admin, programmatic access, which will give us access key and secret key. Let's go next. I need only S3 permission, so I'm going to say S3 full access. Let's go next. Okay, so that creates access key and secret key. So I'm going to copy the access key and paste here. Also the secret key. Now I'm revealing my secret key to you, but I'm going to delete it as soon as this is done. And I specify the region. Default output format, I'll say json. Okay, now let's run aws s3 ls. So this is listing all the buckets in my AWS account. Now I can use aws s3 cp. I can give the path of my log file, and I can say s3://, our S3 bucket name, which is this one, and I can hit enter and it's gonna copy my log file. So that is one way. The other way is you can use aws s3 sync. Okay? Then you give source and destination as directory. Now what's the difference between sync and cp? CP will just copy the file. If you run it again, it's gonna again copy the file, but sync, it's gonna copy the file, the differential data. If you add something over there, like if I say touch temp log files, I'll say "testfileforsync". I'm just creating some text file, okay? Now if I run AWS S3 sync, now we also have the archive in the same directory, but that's already copied so it's not gonna recopy those things. Whatever is not copied, so it's just gonna do the sync. Okay? So this is source and this is destination. And if you have this method of taking backups, then you better create a script for it. You can use Bash, Python or Ansible and then you run it in a Cron job, right? Of course, once you are done, you should remove your log file also. So moving it out of the system was the idea, and we did that. We moved it to the S3 bucket and then we cleaned it. So log files are also safe and we are preserving the disc space also. Now if you need the log, it's not there on the system, you can go through the S3 bucket, download it, access it. But what if you want to stream the live logs? Like regular issues are coming with the product, and you have production system, so your service, your application is going to generate log. You're not gonna give access to the developers to follow production system. You're going to log into the system, fetch the log from the production system, and give it to the developers or who saw you need to pass it for troubleshooting. In such case, it will be a very good idea if you're able to stream the logs to some dashboard so the developers will have access to the dashboard, they will see the logs from their live, and they can give suggestions how to troubleshoot it. Or in general, for something you want to just stream the log outside. Don't want to keep it in the operating system. So for that you can use a CloudWatch, CloudWatch Logs. You can use AWS CloudWatch Logs service to stream logs from your operating system. You will get a dashboard over here from where you can see the logs and you can do other things. You can analyze the log, you can create alarms on the metric, you can create metrics from the logs, you can fetch for some value, and if that value is more than X times you send the notification. Such kinds of things you can do. You can send these logs to Elasticsearch where it can be processed more further. From here you can also send it to S3 bucket for archiving. So there are many benefits if you're using CloudWatch Logs service, so let's see how we can do that. So I want to have the access_log file. The access_log file should be streaming everything to CloudWatch Logs' dashboard. For this, we need to install an agent, CloudWatch agent, and we specify that I want to stream this log. Okay, but before we go there, one more thing. Now we know EC2 instance is accessing the S3 service, and for that we are using authentication to IAM, IAM user access key and secret key. We have a better way, which is called as roles. Roles is a way of giving permission to a service, like for example to EC2 service, to access some other service like S3 service. So you can assign role to EC2 instance with privilege of S3 access and there is no chance of exposing access key and secret key, you don't need to rotate the keys. And creating role is very simple. Again, it's just like creating any other IAM user. You say create role, you select the service that is going to use the role, I'll say EC2, and then we say next. And now we can select the policy. So I will say S3 full access is one thing, so I want to use it for both to access the S3 service and also CloudWatch service. So S3 full access I will give and I will say also CloudWatch. CloudWatch Logs, CloudWatch Logs full access. Say go next. Let's give the rule name, we'll say "log-admin-role". Now a role is not for the humanly access, right? We are going to attach it to EC2 service. So you can give something like that in the description. Okay, we created the role, now we can attach it to our EC2 instance. Okay, but before that first I'll remove the credentials so you can see that it's really working. So I'll remove AWS credentials file, and I'll say aws s3 ls. You see we are getting error, okay? Now let's attach the role that we just created. So select your instance, action, instance setting. Sorry, not instance setting, it's in security, modify IAM role, and you can select the IAM role. We have created log-admin something. All right, log-admin-role and we'll say save. Okay, now let's try. Again, aws s3 ls, and it's working. And I can access S3 servers from this instance, also CloudWatch service. Now we are going to install CloudWatch Logs agent. This CloudWatch Logs agent will take logs from our log file that we are going to specify and stream it to AWS CloudWatch. So you just have to run the command yum install awslogs, this is in Amazon Linux 2. Once again, yum install awslogs. And then there is a service that will get created. We can start that service systemctl start. Or before we start we can just do the configuration of this file, "etc/awslogs/awslogs.conf". Now in this file at the end we have this section. Now this is going to, by default, if we start the awslog, so this going to start sending this log file to CloudWatch. Sorry, this is the file path actually, this is the log group name. It's going to create a group and in that you will have a stream name where you can mention some stream name. And in that stream you will have content of this log file. So what we can do is we can just copy this content, one, two, seven, it's total seven lines so 7yy. I'm just copying this and go at the end of this file and I'm going to do p to paste this content. And let me get into insert mode, give a little space here. Okay, now we'll change the names. So we have to send HTTPd access_log files, so "var/log/httpd". Now here you can give anything if you want. This is just the name of the configuration, right? But here it matters where you have file, here you need to give the right path of the file. So I'm going to remove this, and give our file path, "var/log/httpd/access_log". Now verify the file name and the path, it should be same as it is showing in your system. Stream name, we will give web01-httpd-access. And a log group name, we'll change this one also, wave-web. Also in this log_group_name here, this one also will change. We'll give the same group name, so both the stream comes in the same group. Stream name, also we'll change it here. This is a variable, it'll automatically take the instance ID, but we don't want that. I will give you a "web01-sys-logs". Okay, this looks good, let's save and quit it. And once again, verify this file path. This should exist, cat, I'm going to give the file path. You see this file exists, so verify that the file name that you have given do exist, and then we are going to restart awslogsd, awslogsd. Restart the service, and also we'll enable it. So whenever we reboot the machine, it should come up. Okay, now this should have already started streaming the log files. Let's go and check in CloudWatch Logs. We should be able to see in the CloudWatch some log groups. Yeah, you see that? That's the log group name and you have the log stream there. Okay? And it gets updated regularly. But I want to more specifically now send var/log/httpd/access_log, this file. So let's copy its path and we are going to update var/awslogs/etc/awslogs.conf. Okay? Let's open this file. Go to the end of this file, and you will see the entry for var/log/messages. One, two, three, four, five, six, seven, seven lines. I'll copy that, 7yy, and I'm gonna paste it down here. Okay, file name. Let's make changes. So this is gonna be my file, okay? And log stream name, I'll say web01-httpd-access log. Okay? Log group name I'll keep same. So it goes into the same log group. That's all. Let's save and quit. And let's restart the awslogs service. Restart. Okay, that's good. All right, you see that? HTTPd access_log that comes over there, and you should have the access_log. You don't have much logs. Let's access our web server. It should generate the logs. Yeah, you see that? New logs coming up. All right, so all the logs that is coming in var/log/httpd/access_log is streaming directly over here, okay? Then you can do many things over here. It's a pretty interesting service. So you select your stream and you have option then to export to S3 bucket. You can send it to Elasticsearch or you can send it to to Lambda. So which will trigger some lambda function. You can also create metrics filter. Metric filter is just like, like we have CP utilization metrics for EC2 instance, right? So you can create your own metric based from this log data. So let's say from this, okay, maybe I am looking in that, an IP address, which is probably a hacker IP, okay? That's a test pattern. Okay, so I get, okay? So I'm looking for this IP in the log file, right? So it's showing me all this entry. I go next. Filter name, I'll say, this is in hacker-ip. Metric name, also I'll give same metric namespace. Sorry, that's the namespace. I'll give metric namespace as hackers. Okay? Metric value, I will say... If I see it once, even for once, right? Then I know we should get the data, so created metric filter. So you have a metric and based on the metric, if you want you can create an alarm, all right? If this IP appears to be these many times, you get notification. Now just taking one example, you can create any kind of metric filter based on what you're looking in the log file. So you have seen, you can put just any log file in the configuration and you can stream any logs from your operating system to CloudWatch. And from CloudWatch you can do many other operation. Now one more thing in the logs I will show you, and then we'll end this video because for the other services you have the CloudWatch logs integration for RDS. Most of the services you'll have CloudWatch logs integration. You just put a check mark and the logs get streamed, right? For EC2 operating system, we have seen how to stream the logs to CloudWatch. Now there's also one more thing, load balancer, which can neither stream the logs to CloudWatch, nor it has an operating system. So how can we generate logs from load balancer and then save it somewhere? So for that let's create a load balancer for this web server. So you can go with application load balancer or classic load balancer, anything. I will go with classic load balancer quickly. So I'll say wave-elb. I will create a security group for this, wave-elb-sg. Health check, healthy threshold, I'll just keep two. Add instance, let's select our web server. Okay, so we have a load balancer. Now, this load balancer also generate access logs, right? If you scroll down you will see attributes access log, it's disabled, but you can enable that. You can say configure access log. Enable access log, you can select the interval. You have just two options, 60 minutes or five minutes. Let's say five minutes, and you give the S3 bucket. And the directory name inside that. So let's take the same S3 bucket, okay? And we give that. Inside that we'll give a directory name. So let's also create a directory inside that. Create folder. I will say elb-wave. That's all. Okay? Create folder. Elb-wave. All right, that's my folder name, okay? Okay, so that's the example. Now if I say save, it's going to throw me an error and that is because load balancer is trying to access the S3 bucket, but S3 bucket has its own ways of giving access. You can use your access key secret key, like an IAM user, or you can use roles from the EC2 instance. But for load balancer we cannot attach a role, neither can we log in inside that SSH and set the access keys, right? So the other way is setting access policies on the S3 bucket. Otherwise, you will not be able to send the logs. So on the S3 bucket in the permission section you have bucket policies, okay? So here you have to give some bucket policy there, edit, you have to give the bucket policy, which says allow the write access from the load balancer. So we'll see the documentation for that. So enable access logs for your classic load balancer. If you just do a simple Google, you will find this documentation. Okay, so in this documentation it says first you create a bucket and attach a policy to your S3 bucket. Let's see the policy. Okay, so this is the policy. And let me copy this and take it to Notepad. More specifically, Notepad++. Okay, I will save this file with an extension of json. I will say all types, say s3-policy.json. All right, the reason I saved it because then you get that colors and you get that cascading all those things, right? That's why I saved it. Okay, so S3 policy, let's understand it. So first, well it's in json format, okay? First you have version key. Its value will be this always. Statement, statements are like rules, okay? And it's list of rules, it's a square bracket, you see? A list of rules, so you have this one rule, that's the second one, and that's the third one, okay? So three rules you have. Effect allow. You can have allow and deny. We are specifically saying allow. Principle, a principle means who. Who is going to access, okay? In principle you say AWS, colon, and arn Amazon resource name. Amazon resource, basically, arn is basically part of your service for AWS. Okay? So this particular thing is going to access resource. This is the resource. And what will be the action? S3 PutObject. Okay? There are various other access. And here you have elb-account-id, load balancer account ID, colon, root. So load balancer account ID is region specific. Let's check the documentation. There you have, okay? Load balancer account ID. So our instance, or it's in us-east-2, so us-east-2 Ohio, that's the load balancer account ID, okay? So let's put that. Okay, then the arn of your bucket, AWS S3, then three colon bucket name slash prefix. Let's get that "bucket-name/prefix". Inside that "/AWSlogs" and "aws-account-id", so I'm going to give my AWS account ID here. Okay, I have given some fake account ID, I'm gonna change it that, okay? Change that later to my actual account ID. Okay, again, one more allow access for delivery logs service, again on the S3 bucket. So let's copy that Again over here 'til the end. Same, one more. You just have to give the bucket name here. Okay, this is "GetBucketAcl" for delivery log service. Okay, so this is the policy. Let's copy this policy. Okay, so go to your S3 bucket, edit bucket policy, and paste your bucket policy. Okay, and say save changes. If everything is good, you should be able to store the logs. Okay, let's try it once again. That's the bucket name slash, yeah, that's the bucket name. Bucket name slash the prefix enable. I'll say generate for every five minutes. All right, if you're able to save it that means you're policy, S3 policy's working. And yes, I'm able to save it. Now it should be also creating some test files. If we go to the S3 bucket, objects in elb-wave, you should see AWSLogs directory. So you should see a test log file. If you wait for five minutes then you should be able to see the ELB access log files.
