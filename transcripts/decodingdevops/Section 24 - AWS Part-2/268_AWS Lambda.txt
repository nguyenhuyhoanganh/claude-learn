# AWS Lambda

Hello and welcome. In this lecture we are going to learn AWS Lambda Serverless Computing by AWS. So what is the most common compute service from AWS, EC2 where we launch the instance and we select the resources as in the instance type that defines CPU memory. Then we select the hard disk EBS volume. Once we have it ready, we log into it. And then we install and run our application or services or processes. But what if you. But what if you want to run something, get the result and you're done with it. So if you just need compute resource to run your code, get the result and then you are done with it. In such cases you can use lambda. So you can run your code without provisioning or managing any EC2 instances. And it's not just about the EC2 instances. Anything that you want to run on servers temporarily and get the result For such thing, you can use lambda. Now this is pay as you go model. So you pay only for the compute time that you have used. Let's say your code executes and takes two hours of time. So you will be charged only for the two hours, and you don't even need to worry about the compute resource. It is going to scale automatically based on your workload. Its working is pretty simple. You upload your code as a function and it supports Python, NodeJS, Java, dotnet and also it gives you Amazon Linux OS temporarily to run. So you can have any kind of code. You upload it as a function and then you define the trigger. How do you want to run it? Like for example an S3 event. So AWS activity or activities are full of events. You create a security group, you upload a file on S3 bucket, you power on your instance, you reboot it. All these activities are events. And let's say for a particular event End. You want to execute some code like an S3 bucket. You upload an object and then you want to check that object for compliance. So you can set a trigger, an event trigger which runs your Lambda function. There are also other ways you can use API gateway. You can just call your function from your code from the shell. You can also set rules from EventBridge, or even you can set schedule to run it at a particular time. So you have your function. You set the trigger. When the trigger executes, the lambda executes the code, and then you can decide what happens after the code execution. Maybe you want to upload some result to the S3 bucket, or you want to send a notification that you can integrate in your code, and you don't even need to worry about the compute resource. It scales automatically to handle your requests. So Lambda is an event driven execution. It supports multiple languages. Even the operating system commands you can run, it scales automatically and you don't need to worry about the availability. It's all managed by AWS. Also, it has integration with many AWS services and you can secure the execution by defining IAM permissions, the policies you can define, and of course it's paper request. And based on the compute time you use it. Some common use cases I have just listed a few. It could be anything. In DevOps we can do cleaning of the resources. We can enforce compliance. That's what the exercise we will be doing. In this lecture. We will be scanning all the security groups and will check which rules allow connection from anywhere. And then we can send notifications. So that's what the exercise we will be doing. But there are many more things you can do. Data processing developer uses it for the backend APIs. Any file processing resizing of the images that you upload on S3 bucket. Monitoring or security? You can send alerts with SNS or EventBridge and these are very few. There are many, many examples or use cases that you can think of based on your requirement. Now let's take a look at what you will be doing in this lecture. So let's talk first about the function that we will be creating in Lambda. It will be a Python code that is going to check all the security groups in all the regions, and find any inbound inbound rule that allows traffic from anywhere, for example, 22 allowed from anywhere. Once it finds that it is going to upload its result to the S3 bucket and also send an notification email notification by using SNS. And what triggers this Python function will be EventBridge schedule, which will be running every hour. So every hour it's going to invoke our lambda function that is going to execute our Python code to check for the security group compliance. And when it does that, it needs to have the IAM roles with the correct policies. So in order to check the security group we are going to define an IAM policy. The security group rules security group read permission basically, and also the policy rules to access our S3 bucket to upload the object. The object will be the report in JSON format and also SNS service rule to allow accessing the SNS service to send the email notification. So what do we need for this exercise? We need an S3 bucket. We need an SNS topic. We need IAM roles with the policy to access this S3 bucket and this SNS topic, and also the security group, the AWS Lambda function. We are going to create a function and upload our code in that. And then finally we are going to set EventBridge schedule. So let's get into the action now. First let's go to the S3 bucket say create bucket. I'm naming it as S3 compliance 9 to 1. The number you have to change or give a different name. Your bucket needs to be unique. You can give. You cannot give the same name that I have given. So give a unique bucket name general purpose and just say create bucket. Oh I cannot give it in the capitals. So SG Hyphen compliance 9 to 1 create bucket. Then let's go to IAM and create a policy and the role IAM role. Oh sorry. Before this SNS simple notification service to create a topic. If you already have a topic you can use that one. So give a name. Next step, keep it standard. Give the name. Give the display name as same and simply create the topic. Go to subscriptions. See. Create. Subscription. Select your topic that you have just created in topic and select the protocol as email. That's what we will be using and give your email address over here and say create subscription. Go to your inbox and confirm the subscription and then refresh the page. You should get here confirmed. After you have confirmed the subscription from your inbox, sometimes the mail goes to spam, so please do check that. Okay, next go to email service. We need to create an IAM role with our own custom policy. So first create the policy. Go to policies. Click on Create Policy and then click on JSON. Remove all this. Download the policy document from this lecture resource. Open a text editor and paste the policy. This policy allows EC2 actions to describe region. describe instance, describe security groups, and revoke security group ingress. The other action is for the SNS publish. It is going to use this Arn which is your SNS topic Arn. So you need to put your account ID over here and your topic name SNS topic name. So my SNS topic name is this SG compliance notification. You need to get your topic name and make sure you put it over here. Also, make sure you update this number with your AWS account ID. The next action is on the S3 bucket. Put object, get bucket location and list bucket. You need to give your bucket name over here at both places. So my S3 bucket name is this SG Hyphen Compliance 921. Get your bucket name. Mention your bucket name check. Once again, this is the region. This is your AWS account ID and this is the SNS his topic name over here. Your S3 bucket name at both places. Then copy all this. Go to the policy editor and paste all this over here and say next. Then give policy a name. I have given this S3 compliance policy. If you scroll down, you can see over here your three services that it is going to have access on. So EC2, S3, SNS and you see the access level EC2 for all resources, S3 for multiple and SNS for all the regions. No, sorry, my bad. SNS is for your topic name. So check all those things and go create policy. Okay so the policy is created. Now we need to create the role with this policy. So go to roles. Say create role keep AWS service. And in use case select Lambda because this rule will be for our Lambda function. So you need to select use case Lambda go. Next add permission. You need to find your policy. Over here you can search it with your name, the name that you have given I have given SG compliance policy. Select your policy. Go. Next, give rule a name. I have given SG compliance role and same in the description and then say create role. Okay, next open the code editor or any text editor and get the Lambda function code the Python code from this lecture resource and put it over here. Update your SNS topic account name and the topic name. And over here the S3 bucket. So this Python code is going to go over all the regions. This regions variable is going to collect all the region names. And it's going to iterate on a for loop over all the region and execute this function which goes through all the security groups. So a loop inside a loop. So in every region it's going to scan every security group and find if the Cidr IP allows from anywhere. If yes, then it is going to send email with this message. If not found, it is going to send this message. No. Open security group rules found. SNS publish publish function is going to send this message to the notification. Sorry to the SNS topic that we have defined. And also the S3 object is going to access our S3 bucket and dump the JSON data that we have created here. That will be the message and findings in JSON format. Now let me be clear. I have not written this Python code. I understand this very well, but I have asked ChatGPT to generate this code for me. I have just given the requirement and also the IAM policy. Also I have generated by using ChatGPT. Okay, now let's create the Lambda function. So find and open the Lambda service AWS Lambda. Let you run code without thinking about servers. And that's what basically it is. You can run your code without worrying about where to run. So you create a function, click on that author from scratch. Give function a name. I have given security group compliance checker. Select the runtime. We have Python code. So we are going to select Python 3.13. Our code is written on Python three. I already tested this on this version. It works. Select here. Change default execution role, use an existing role and select the role that we have created as the compliance role and say create function. Okay, wait for some time okay. First before we put the code go to configuration. So code execution takes time a lot of time here the timeout is three second which will be very less. So edit this and change timeout to 2 to 3 minutes. I'll just go with five minutes. It won't take five minutes. It will. It will take, I think, a minute or so. So give a timeout value over here like five minutes and say save. Scroll down click on code. And select this everything and just delete it and paste your code over here. Okay. After that deploy click on deploy. It takes few seconds. After that click on test. So there are no test event. We just want to run it. So we're going to create a new test event. Give event a name I've just given Test Lambda and just say save. We don't give so we can test it. We don't have any event. We're going to use event bridge schedule. But for now to test we need an event okay that already exists. Now click on test and the output should come over here. The execution started. This is going through all the regions. That's why it's taking time. Okay. The execution completed successfully. And you see it has some findings. And this regions there is a security group ID have port number 80 that is allowed from anywhere. Now I have that security group that has port 80 from anywhere. So you better create security group that allows ID from anywhere, 22 from anywhere, just the security group. You don't need to do anything else. So your security group will come in finding okay. So this execution would have also created a file in the S3 bucket. So go to your S3 bucket. Refresh it if you don't find it. And this is the JSON file. Let me open it. And that's the finding. Also it would have sent the email notification. Now definitely we're not going to run this manually. We are going to use EventBridge to schedule this lambda function. Let's say to run for every one hour. So let's go to EventBridge service. So find Amazon EventBridge and go to it over here. Just say create rule. Give rule a name I have given security group compliance schedule. Give some description runs lambda periodically to check open security groups and make sure you select schedule and then go continue. Okay. Scroll down and find recurring schedule. We want recurrent every one hour. Now you can select here rate base schedule. But cron cron based like the Linux cron job. That's the format we're using. So we want minute zero. That means the first minute hour every hour day of the month, every day month every month, day of the week can be anything and year every year. Now this cron job says run it for every hour. That's basically the first minute of the hour. When the hour starts. Right. So that's in the Linux cron job format. Now you see it shows next gen trigger dates. You see it's like every hour. Okay. Um flexible time window. Just say off. So we want it at a specific time, not the flexible window. And then. Yeah, just go next. Okay. Then select AWS Lambda. This is what should be invoked. And find your function. Your function should be listed over here. And then say go next action after completion. We don't want any action. So you can just say none over here. Create a new role for this schedule. I'll just make sure you add some numbers over here. Not too many. So a new role will be created with this name. This rule. This rule is for the Or the event bridge so it can access the AWS Lambda service that IAM role that we created that was for AWS Lambda. This is for EventBridge. So go next. Check everything. If everything is good click on Create schedule. So this is going to take time right. Every hour it is going to send the notification. So you can wait for few hours. And you will see the notification coming to you every minute. If you have configured the notification properly and in the finding, whenever you know in the middle, you can create security group rules, add anywhere, remove it and you see you know how the finding changes based on your rules that you have set. All right so let's define what we did. So we created an S3 bucket. We created SNS topic. We created IAM role with the policies to access the bucket and the topic and also the EC2 security groups. Then we created a Lambda function with the Python code, which checks for the compliance. And finally, we have set Amazon EventBridge scheduled to run every hour. Now this is not just some example, it is actually used. I have used it personally to make sure nobody opens port 22 from anywhere. Sure, there are other ways, but you know, some quick fixes or quick use cases. This comes very handy. Once you are done with this you can remove the EventBridge schedule. Now wait for a few hours. This is free so you don't need to worry about the cost. But if you don't want the notification to be coming every now and then, you can just remove the EventBridge schedule. So you can just go to the EventBridge schedules, find your schedule, and you can delete it so it doesn't invoke your lambda function every hour, but let it run for a few hours. Get the notification so you know that it worked. And that would be all in this lecture. I hope you have enjoyed and learned. Thank you very much for watching and all the best.
