# Create VPC

Lecturer: Alright, so in this lecture we are going to create VPC but not the whole VPC, just the VPC CIDR block which is very easy to create. Actually entire VPC with all the subnets, internet gateway, NAT gateway can be created automatically through this UI here and it's very easy to do also. Provided you know the network range and what exactly you're doing. You have architectural diagram and you have the network range, everything. So we are going to create a VPC range here in this lecture, this one. But you can create the whole thing automatically through the VPC dashboard in the AWS UI. Okay, here we made one mistake. So two NAT gateways will need two IP. So I have mentioned here two, but in reality we are just going to create one NAT gateway and one elastic IP. And that is to save cost because NAT gateways are expensive. If you keep it running for a longer time, you will see the bills that you will get worried about. So we are going to try to keep it as minimum as possible but for the learning purpose we need to create one NAT gateway and one elastic IP at least. And once we start doing it we'll complete the entire VPC section and then we'll do the cleanup as well. So you're not going to see much build, so please don't worry about the builds. So for high availability, you should have two NAT gateway and you should have two elastic IP for them. But to save the cost we are just creating one NAT gateway and one elastic IP. Okay, so let me show you the concept of creating VPC. So come to your VPCs, click on create VPC. Now to just create the VPC range, you can give the name and you can give the CIDR range and you can say create. That is it. It will be created. If you want to create the entire VPC with public private subnet, internet gateway, NAT gateway, then you can click on this one. The UI might change a little bit for you, but more or less it'll be like this. So I'm just showing you over here, we are not going to create it like this. We're going to create it, everything one by one and join them together. That's how we are going to learn the entire VPC setup and understand if something goes wrong. So click on this radio button and we'll do some fill-in-the-blanks and then we'll see the flow and then we'll go back to creating VPC manually. Tag, so let me just queue V profile. Once again, we are just filling the blanks. We are not creating it. So IPV4 CIDR block for the entire VPC, which is this one. So let's copy this range and let's put it there. Let's come down. Tenancy default, that means create it in a shared hardware. If you select your tenancy dedicated it will be very expensive, but you get dedicated hardware for your VPC to run. So keep it default. Number of availability zones, so we'll keep it two. Customize availability zones, so us-west-1a here and then here us-west-1b. And number of public submit two and number of private submit two. Okay, and customize subnet CIDR blocks. So again, fill in the blanks. So we'll say 24 just 1.0 and the second one 24, we're going to keep it 2.0, 243.0 slash 24 and here we'll say 4.0. Now it's really not mandatory to create all the subnet in the same sequence, like 1.0, 2.0, 3.0. You can give different sequence also but this is good to look, you know, easy to identify. Okay, then coming down, so NAT gateway you see the dollar symbol. That means there'll be extra charges. Click on info and find out, but don't worry for now. So if we say one per AZ, one per availability zone, so it will create two NAT gateway because you have two zones. VPC endpoint. Now VPC is a service that will be accessed globally through the internet publicly, but if you want VPC to be accessible privately from the private subnet, you can create VPC endpoints. So as I told you, we are not creating it, we are just looking. So take a look, that's your VPC. It has four subnets distributed in two zones. And let me scroll it like this. So instance that belongs to this subnet, it's traffic will be routed through the route table and will go to the internet gateway. Same will be for the other public subnet also. But private subnet, traffic will be routed through the NAT gateway. See that? See the flow? That's how I did this fill-in-the-blank. So you can have a nice look over here. And also you see the traffic going to VPC endpoints. If you want to access S3 bucket privately you can access it through the VPC endpoints. Okay, so I did all this. So if you click on create VPC, it will create everything for you. But don't do that. Cancel this one. Just to be on a safer side. Cancel that, come back, click on create VPC, select here VPC only and let's give our VPC a name. I will just call it as vprofile-vpc, the network range 172.20.0.0/16, tenancy default, tag name automatically it will take and click on create VPC. So this will create the network range. When you do this, it will also create a default route table. We are not going to use that route table. So you just see the VPC over here you won't see the subnets and other things for it. Then add gateway internet gateway that we are going to create one by one and join them together. The next lecture we are going to create four subnets in this VPC.
