# Default VPC

Instructor: Okay, I am in my AWS management console and I am in a North California region. So switch to North California region, US West 1 because this we will be using. And reason for using this region is because it has just two zones. It's easy to study, learn, and practice VPC in this region. But all the knowledge that you gain in this lecture of setting VPC, you can apply to other regions also. But just to reduce the complexity of the setup while we are learning, we are going with North California region. Search for the service VPC and open it. So in this lecture, first, we are going to study the default VPC. And before I go ahead and explain you about a default VPC, I want you to pause this lecture and go through these different sections. Take some time and get comfortable with this user interface. Once you're good, come back, resume the lecture, and I'll explain you the different concepts of default VPC. Okay, so we have the VPC dashboard. Over here you can see different configurations of VPC, NAT Gateway, Subnet, et cetera. So as I told you first we are going to take a look at the default VPC, so click on Your VPCs. And you will see a VPC over here, it does not have a name. Scroll horizontally, and you will see here default VPC, Yes. Now, you cannot create a default VPC. You can create VPC, but you cannot make it default. If accidentally you happen to delete the default VPC, you have to contact AWS support and they will create it for you. Every region will have a default VPC. And to identify, I usually change the name here to DEFAULT VPC, all in caps, so it's easy to identify. Every region you go, you can go and check different region, you will see the VPC and it'll have this range. 172.31.0.0/16. Now slash 16 gives us around 65,000 IP addresses. Go to Subnets, click on Subnets, and you should find here two subnets for this VPC. These two are public subnet. And how do I know it's public subnet and how you can find out? That I'll show you in a moment, but let's change the name here. Default-pubsub1 and Default-pubsub2. One and two is just for identification. Okay, so these two subnets belong to the VPC that I can know by looking at the VPC ID over here and compare it with the default VPC or whatever VPC it belongs to. You can see it has a range slash 20 slash 20. That will give around 4096 IP address actually, but AWS will reserve five IP addresses per subnet for internal use. Look at the range 16.0, so any IP address, to your instance, private instance, you can see 172.31.16 dot something, not 10, 20, whatever. So by looking at the IP address of your instances, private IP, you can also identify which subnet it belongs to. Also, definitely it'll have a subnet ID as well. And look at the zone, this is in 1b, this is in 1a. Now I told you these are two public subnet because I already knew that, but you can find out whether a subnet is public or private by looking at its route table entries. So click on a route table. Select any subnet, click on the Route table, and you should see a route over here. Destination, this is for the outgoing traffic. Traffic that is going out of the subnet. So if the IP address belongs to this range, which is the VPC range, then route the traffic locally, that means within VPC, but any other IP address. In this networking... In networking, this means all the IP address as 0.0.0.0/0 means all the IP. So any network packet that is going out of the subnet, where should it go? That's the question. So if the IP belongs to this range, then it'll be routed locally, but any other IP address will go to the internet gateway. This is the ID of the internet gateway. You can check this IP ID. You can click on it or you can go to the internet gateway, and you should see the internet gateway. You can see which VPC is attached to, but the route tables, if you go to the Route table, you should find one route table here. You can see the routes here, same thing. And subnet association, which subnet it is associated to, the two public subnets. So if the subnets are routing the traffic through the route table to internet gateway, then that is a public subnet. If the subnets are routing traffic to NAT gateway, that is a private subnet. Rewind it and listen it again. These are also interview questions. And if you're managing VPC, definitely you should know these basic concepts. Traffic going to internet gateway, public subnet. Traffic going to NAT gateway, private subnet. So we have seen a VPC here. We have seen two subnets in that. We have seen a route table for it, and we have seen that the traffic is routed to the internet gateway. Sure, there are other things. Many things you see over here. These are out of the scope of our course, like Egress-only internet gateway, DSCP Option Set, Endpoints, Endpoint Services. But we are going to take a look at NAT Gateways, Elastic IPs, Network ACLs. But there are many other things and you really need to be a network geek to understand the advanced concepts of VPC. So before we move on to the next lecture, keep in mind never delete the default VPC. Don't delete default internet gateways or route tables. By the end of all these lectures, you should be able to create internet gateway and route table and attach it to VPC. But remember, you cannot create a default VPC. You have to ask AWS support if you accidentally delete the default VPC. So let's look in the next lecture how to create VPC and add subnets in that.
