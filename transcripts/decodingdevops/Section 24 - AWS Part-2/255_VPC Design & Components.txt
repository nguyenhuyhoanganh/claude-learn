# VPC Design & Components

Welcome. In this video we will be seeing VPC designs. We'll understand different components in a VPC. So this is a very basic VPC design. So VPC is the main network and then you divide it into smaller subnets. There are two kinds of subnets in VPC. You have public subnet and you have private subnet. Public subnet is the one that can be reached directly from the Internet. So if you have EC2 instance in a public subnet, it can go to the Internet. And a traffic that can originate from the internet can come to your EC2 instance. Like you are doing SSH to an EC two instance by using its public IP. That instance will be in a public subnet. What makes subnet public subnet is the internet gateway. There will be a device called as Internet Gateway that is connected to your public subnet and that will manage to and fro internet connection to your public subnet. You have other kind of subnet, private subnet, where a traffic originating from the internet cannot get directly into the private subnet. You will not have any public IPS to instances in the private subnet. It will only have private IPS. But if these instances wants to access the internet, maybe you want to download a package. You're setting up a database server on the private subnet. You connect to that and when you're trying to download a package, then it is going to connect to the Nat gateway which is in the public subnet. Nat gateway will do the netting magic and it will send the traffic to the Internet. Now think of Nat gateway as your WiFi router. The instances in the private subnet like your laptop. So from your laptop you can connect the internet, but from the Internet you cannot connect your laptop in generally your laptops will have private IPS. Like my laptop here has a private IP IP config. If I run that has a private IP address, it's not the Internet IP. My WiFi router has a public IP assigned by Internet service provider. So if you just Google what is my IP, you should get your public IP that is assigned to your Internet service provider, to you, or more specifically to your WiFi router. Okay, so public subnet is connected to Internet gateway, private subnet is connected to net gateway. So we have seen four components public subnet, private subnet, Internet gateway and Nat gateway. If you see in the design, the Nat gateway will be living in the public subnet because it has to connect to the Internet gateway for Internet traffic. But keep in mind, the traffic that originates from your private subnet can go to the internet and the same traffic will be coming back. But a traffic that is originating on the internet cannot get into your private subnet. So you have Nat gateway that enable instance in a private subnet to connect to the internet or even to the other services in your AWS internet gateway. Again, these are virtual devices, okay? Internet gateway you create, it's going to be totally managed by AWS. You don't need to worry about the high availability redundancy. Everything is managed. Internet gateway is component that allows connection between your instances in your VPC and the internet. So check the diagram once again, pause the screen, check it and then continue. Okay, few other components, same design, VPC, you have the public subnet and the private subnet. Now how does an ECT instance which is in a subnet, knows that it has to go to the Internet gateway or it has to go to the NAT gateway That is decided by the route table. There will be a route table which you're going to attach to your subnet. And the route table will tell EC2 instance that it has to forward the traffic to the internet gateway or a private subnet, it needs to go to the internet. The route table will tell that it has to go to the nat gateway. So, Internet gateway and Nat gateway, both our routers. But Internet gateway is for the direct to and flow internet connection. NAT gateway is for only traffic that goes to Internet. If you want to connect to your private subnet directly from your corporate data center, from your office, from your organization, then you can have a NAT gateway when you dial the VPN, then you can connect to your instances with their private IPS from your own network. Okay, so this is the design of public and private subnet. Private subnet gives you the security, but we also need high availability. When you create a subnet, you assign it to a zone. Basically you create a subnet in a zone. And as we know, we have multiple zones in a region. Minimum two zones will be there in every region. And then you have some regions, you have three, some regions you have six zones. Let's say you have two zones. So you are going to create two public subnets. One in zone one, the other one in zone two. Now why is that? Now, zones are clustered data centers. And if a zone goes down or it's slow, then the other zone and your subnet in the other zone will be live up and running. So if you design your infrastructure properly, take example, you have four web server. You can put two in public subnet and the other two in the other public subnet. So if your zone one goes down, only half of your infrastructure is down. But if you have more instances, you can distribute it much better. If you have more zones, if you're using as per your requirement, you can distribute it as in different zones. Now, same as for the private subnet, you will have multiple private subnet. One in zone one, the other one in the other zone. Again, every subnet will have the route table. Public subnet, route table will tell the traffic to go to Internet gateway. Private subnet route table will send the traffic to Nat gateway. Now, I told you that if you want to connect to your private subnet, you need to dial VPN. But that's not the only way you can connect to your public subnet from the Internet. So you can have an ec2 instance, you can call it as jump server or bastion host. From the Internet, you can connect to your bastion host or to your jump server. And then from there you can connect to the instances which are in private subnet. Because by default, all the subnets in a VPC, public or private, can communicate with each other. So instance in a public subnet can connect to an instance in private subnet and vice versa. Okay? So in this design, we have seen multiple subnets, multiple public subnet, multiple private subnet. We have seen bastion host how you can connect to an instance in a private subnet by connecting to bastion host. So there'll be a jump, right? You get into bastion host, then you jump into your private subnet and Nat gateway. If you need high availability on the Nat gateway, then you need to create Nat gateway in multiple public subnets. So two public subnet, so you create two Nat gateway. Now among all this, what design that you're seeing on the screen? Nat gateway is the only piece that is chargeable. AWS is going to charge you on the NAT gateway. The other things, you create subnets, you create VPC subnets route table. These things, internet gateway, these are not chargeable. So with this design, this minimum design, you achieve high availability on your infrastructure and you also achieve security by using private subnets. So we're going to go with this design and in the next video we will create this entire design and we are going to launch our instances in public and private subnets and we'll see how we're going to access them. So wrap this up and join me in the next video.
