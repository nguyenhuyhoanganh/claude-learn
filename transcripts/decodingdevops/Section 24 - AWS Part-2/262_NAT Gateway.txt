# NAT Gateway

Instructor: Alright, so, so far, whatever we have created is free of cost. We're not going to get any bill for this, but now, we are going to create a NAT Gateway, and that is not free. So if you want to take a break, take it now. Once you start with NAT Gateway, you have to go until you finish this entire VPC. Complete all the lecture and then we are going to do the cleanup. So if you have the time, continue with me or else take a break and come back when you have enough time to complete the rest of this lectures on VPC. Okay, so before we go to the NAT gateway, we have to go to elastic IPs. We need to create one elastic IP that will be associated with the NAT gateway. NAT gateway will have a static public IP, and that is how the NAT gateway connects to the internet. To connect to internet, you need to have a public IP. It can be static or dynamic, but in our case, this entire VPC network is controlled by us and we are not locating dynamic IPs. We are locating a elastic IP, which is a static public IP. So click on locate elastic IP address, and let's give it a tag, a name tag. I'll say vprofile hyphen NAT hyphen Elastic IP. So easy to identify when you want to detach or remove this. Allocate. That's all. You just need to create the elastic IP. We are going to attach it in the NAT Gateway. Go to the NAT Gateways now. Click on Create NAT Gateway. We'll name it as vpro hyphen NAT hyphen Gateway. Selecting the subnet. This is crucial. Now, NAT Gateway serves the private subnet, but it needs to stay in the public subnet so it can connect to the internet through the internet gateway. Once again, NAT Gateway lives in the public subnet and it connects the internet through the internet gateway. So NAT Gateway will be always placed in the public subnet. So place it in public subnet one, sorry, not this one. Vpro public subnet one. Make sure you place it in other right subnet. You can place it in public Subnet one or two, does not matter because we are just creating one NAT gateway. And let's associate elastic IP that we created earlier. If you have not created, you can create it from Hero. So you can click on allocate elastic IP. It'll create, and then you can select it. Okay, that's all. It's pretty simple. Then click on Create NAT Gateway. Okay, it'll take some time to create the NAT gateway. The state is pending. It'll become available in few minutes. By the time it is getting created, let's not waste time. Let's go to the route table. Create the route table for the private subnet and attach it to the NAT gateway. So go to route tables, click on create route table. Let's give it a name with vpro hyphen private hyphen route table. Select our V profile VPC and click on create route table. Okay, let's do the subnet associations first. Go to subnet associations, edit subnet associations, and make sure you select the private subnet, two private subnets. Save. Go to the route. Add route. And all the traffic that doesn't belong to the local goes to the NAT Gateway. And this rule makes your private subnet private because the traffic is routed through the NAT Gateway. Save changes. Let's take a look at the NAT Gateway. It's status. You see that? It's in available state now. Okay, so we again have now a fully functional subnet. This is, these are the two private subnets now, right? Let's go to the subnets. So now we can even start using this public and private subnet both. The instances that you launch in these subnets. We get the IP, private IP for all the instance you launch in any subnet and public IP you will get when you launch the instance in the public subnet, but the public IP only and private IP only, no names. When we launch instance, we also see we get a DNS host name, EC2 hyphen something, something, something, right? 172 hyphen hyphen. For public and private, we both get host names, but that is not by default. So we have to enable that. Go to your VPC, find your VPC action, edit VPC settings, scroll down and say Enable DNS host names. So the instances will also get the host names along with the IPs, public or private both, and say save. Alright, so as per the architectural diagram, we have created VPC. We have created four zones, sorry, four subnets to public to private. We have internet gateway and NAT Gateway connected through the route tables. In the next lecture, we will start using this VPC. We will create bastion host instances in the private subnets and even the load balancer and see how this entire setup works for us. So see you in the next lecture.
