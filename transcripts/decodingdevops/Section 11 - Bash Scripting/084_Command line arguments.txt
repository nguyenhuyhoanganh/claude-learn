# Command line arguments

Welcome, in this lecture, we will talk about command line arguments for our scripts, as we have been seeing arguments for commands like, for example, for ls command, we give the path of a directory. So that path is the argument when you're copying a file, the source and destination, we give basically arguments to the cp command or to the mv command. So we've seen many, many commands and many, many commands needs many, many arguments. How can we programmatically take arguments from our script? For example, we may want to take the URL of the artifact as an argument so user can pass any URL in our script, will deploy that website for the user. So let's go to a script box. Let's clear the screen. I will write a script 4_args.sh OK, these few statements in the scripts are to make you understand how come line arguments work. So I'm printing a message, the value of zero. I will say value of zero is and will see dollar zero there. So $ we know extract the value of the variable, so here is zero, and then one, two, three. These are variable the dollar in front of them represents that. We are trying to access the value of that variable or here, printing, we are printing, the value of dollar 1, dollar 2, dollar three. But we have not stored any values to those variables. Basically, we have not declared those variables. So let's see how these things work. Make the script executable. Now, before I execute the script, I want to show you something. I take a variable X and I declare some value to it. Let's say one, two, three. And I'm going to say echo $x All right, we know that much. But what if I do not have any value to a variable? Basically, let's see. I have not declared any variable, but I'm trying to access it. Let's say $y , which I have not defined or declared. So it just prints empty since we have established this point. Now, let's execute our 4_args.sh, which is going to print 4 variables for us. Zero, one, two, three. So we see the value of zero is the name of the script or path of the script. Value of one. Well, there is nothing, value of 2, there's nothing, value of 3 There's nothing, so basically those are not declared or defined. When we passed the argument. So if I see first as, let's say, Linux. So that is the first argument. It will be stored into the variable one. And you see there. Likewise, let's define the other arguments. Three arguments. In an order, we can pass more arguments also, but we are really not using it, in our, script. So that's how command line arguments work its from zero to nine or should I say one to nine zero is always the name of the script. So you can say zero is reserved for the name of the script from one to nine. You can use command line arguments. OK, let's clear the screen and let's use that in our script. So I'm going to copy our script. 3_vars_websetup.sh and I'm going to say. 5_args And let's open the script and now use the command line argument. Well, it's not going to be difficult. I want to take two command line argument. One will be the URL, so I'm going to comment this one. Oops. Now, I didn't go to the insert mode, OK. It's not here, there and there. So now these two variables does not exist the URL and artifact name. Because I have commented it and I am not going to use the variable, I'm going to use command line arguments. So here I'm going to say $1 And there I'm going to see a $2. So user basically needs to pass two arguments and definitely needs to pass because if the user does not pass, this is going to be empty. So it's going to be, wget empty, which will throw error at us. So user definitely needs to pass the command line argument in this case. So the first will be the URL. The second will be the artifact name. Let's take the URL From here itself. Well, first, let me dismantle everything. And how about we take a different website, and that's the benefits of variable, right? We can vary. OK, I'm here tooplate.com and let me select something else this time. Now, how about the ziggy I like the name. OK, so let's get the URL over here F 12. Oops. Yeah. Open development tools. Network, lets click on the download. OK, there we get it. All right, let's run it. And first is going to be the URL. And second is going to be the artifact name, which will be this. So check out this is now a reusable code, I can use it to deploy any website I want. And that is some intelligent work there. Let's see if it works. All right, looks fine. Let's get the IP. Let's take that. And let's go to our browser. Let's try that and there we go. Welcome to Zigi. So that is how the command line argument works. So, $1, first argument, $2 second argument likewise, you can go till $9. So that is all in this lecture. I will see you in the next one.
