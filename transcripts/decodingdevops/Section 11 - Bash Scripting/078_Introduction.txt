# Introduction

Hello and welcome to the Bash Scripting Session. This is going to be one of the most exciting part in this course, especially if you are new to scripting. If you have never done or done little or no programming or scripting in your life, then you must be doubting yourself. But don't do that. Just follow along all the session and make sure you write all the scripts that I'm showing in the session. The scripts are already in the resources section, but I recommend you write it yourself. And if you're facing issues, errors in your scripts, then you can compare your script with my scripts that I am attaching it in the resources section. And one tip I would like to give you for troubleshooting scripts. From my own experience, I have seen majority of the script failures are because of the typographical mistakes, a spelling mistake here and there, a space you have missed here and there, and you get error. And sometimes it's really very difficult to look at those or find out those syntax error. It could be very, very simple. So look carefully in your script whenever there is an error compare with my script. Just keep doing the exercises and you'll find yourself by the end of this bash scripting session, you would have gained amazing skill in scripting. So what really is scripting or bash scripting? Well, we have system admins from the beginning of the IT industry. And we used Linux servers a lot. And there are many Tasks, a system administrator, will perform to make sure the system is always up and running and healthy and secure, regular patching, regular backups. There are many tasks that sys admin when does the repetitively. Again and again and again. Maybe daily, maybe once in a week. So we call such tasks, robotic tasks. And instead of doing and executing those commands in the Linux system, we put all the commands in a text file. Yes, in a text file, and we let the system execute the file for us, which is a bash script. And the word bash is for the bash shell, if you're doing scripting for the bash shell or in the bash shell, that is called bash scripting, because there are other kinds of shell as well in Linux. We have just sh. We have ksh, zsh. So when we say shell scripting, it could mean any shell. But when we say bash scripting, it means specifically bash shell script. So to automate day to day regular task sys admins. write, bash script in Linux system. Now there are many fancier automation tool in the market. You have Ansible, Puppet, Chef, salt stack, terraform So why do use script or why to learn bash script. When you learn these tools later, you will understand that many of the concepts in those tools are derived from from bash scripts or scripts, for that matter. So if you have good hands on bash scripting, then you can learn those tools very easily and can even master those tools. Moreover, in this course, the bash scripting is to give you confidence and knowledge in scripting. So bash script, basically tells System what to do instead of we executing commands, bash script or the text file will tell the system what commands to execute that is bash script. So join me in the next lecture and see for yourself what really is a bash script.
