# Decision Making part1

Welcome to the decision making part, so far, whatever scripts we have written are really series of commands that executes, our script cannot make any decision. But what if you want your script to make some decisions? If something fails, you want to do something else, or if value of the variable so-and-so, then do this, otherwise do something else. Making the script smart. And that's how we can do it through If statement. So we say "if" we give some condition there. So condition is basically a test case, which we'll compare something with something. And if that is true, then it executes our commands. If that is false, it does not execute our commands. And we can do a few more thing if this fails. We would like to execute something else or something else or something else so we can have "IF" condition, then "Else IF" condition, "Else IF" condition, multiple "Else IF" condition. If everything fails, then finally we can fall back to an "ELSE" and we'll see how that works. So I'm back in my box. Let's write it. If 8if1.sh OK, so what we will do in the script will take a number from a user and we'll compare if it's greater than 100. We print the message that it's greater than 100. What if it's not greater than 100 will print another message. So let's take read and take input from the user. Let's take a variable NUM. Now we will put a condition if square bracket and give a space. Take the variable. So we take the variable that user Enters. We compare it, this is the operator -gt means greater than 100. So two operand, we are comparing with an operator. And if this is true, we say then and we will give  some space so it looks better. If this is true, will say we have entered in the "IF" block and will give a command sleep. So "sleep" waits for three seconds and then we print out a message. Your number is greater than 100. We'll give another echo, an empty line, and then we'll put some commands. Let say date. And we can close it. So "fi" is for closing "if" is for opening. Now, you really don't need to give any space, but it really looks better if you give some space. Because in Python script, this will be mandatory. Here, it's optional. And finally will give a message that execution completed. OK, let's run this or before that. Take a look. Square bracket, a space, variable, space, operator, space. the other operand, space and the square brackets, so that is the syntax. Then you're code "fi" closing, if this is true, it will execute this code, if this is false. It will not execute this. It will just execute the rest of the code. So let's save this. Let's make it executable. Oops. +x And let's run this. OK, it's asking a number. Let's enter something greater than 100, let's say 120. Ok, we have entered the if block, because the condition was true and yes, your number is greater than 100, the date is printed and the rest of the code. Now let's give something less than 100, let's say 50. And you see, the condition was false, so it did not execute our "if" block. What if you want to execute something else. Let's copy the script with the name. Nine. we well say, unserscore there. Now I can put an else block. So we will basically say else. Just before "fi" And we will print another message here. And this command will only get executed if this condition is false. Which simply says your number is less than 100. Let's save this. Let us clear the screen and let's execute that. OK, entera no, I'm going to say, 60, which is less than 100, so it should execute the "else" block if I enter no greater than 100. Then the "if" block.
