# Remote Command Execution

Welcome. In this lecture, we will talk about a remote execution, how we can execute command from script box to Web zero one and zero two, and we will also add one more machine web zero three, which will be Ubuntu machine. So let's check at our Vagrant file. So from a script box, we will execute command that will run on Web 01 and web 02, and I will also add one more machine, which will be Web zero three. And that will be for Ubuntu operating system. Now, this is not mandatory if you want to skip. If you do not have enough resource on your computer, on your laptop, then you can skip this one. But I'm going to do this web 03 because this is going to be Ubuntu operating system and we will be using some techniques in our script that we have learned previously to execute script for a different operating system. Ubuntu Bionic 64. Also, IP address, I have to change. And let's note down the IP address, 10.13, 10.14 and 10.15. OK, now let's bring up all the machines we can just see vagrant up. OK, we set up first, we will set hostname for every machine. So, vagrant ssh web01. So open the /etc/hostname file. And lets give web01 Let's log out from web01, and, log into Web 02. Let's get into web03 now. And this is Ubuntu it is optional, if you want to skip it, you can skip it. OK, let's log out. OK, let's get the IP address of the machines. We are going to add name to IP mapping in the script box. So script Box can refer to all the machines with the names and not IP addresses. But first, we have to save those same IP address in the /etc/host file So let's log into script box. OK, we will open the file /etc/hosts OK, I'm done with that. Now let's try to ping Webb zero one from script box. It should result in the IP address, same Web 02 and also Web 03. So let's try to make a test now. We will say ssh, vagrant, which is the user in that machine, web01 OK, it's asking the question, should I login? Yes, please. And so asking the passwords of vagrant user passwords, vagrant, same password, same username. And we are in. OK. And you see the hostname. Right. We can see where we are logged in. So that was the benefit of setting the hostname. Right, so when we log in to that machine, we can see we're connected to that machine. Lets log out from here. So we will not use the vagrant user. We will create another user called as devops. In all the Web servers, so will log into all the machines one by one. First Web 01. You can do vagrant ssh from the host machine or from here itself, you can log in. I'm going to add the user called as devops, and it will set the password for this user. So will use the user to execute some remote commands, and that will be system commands also like installing packages, adding users. So we need to add this devops user into sudoers file. So I will edit the sudoers file. I will add the user devops in the sudoers file, and I'm going to say NOPASSWD Oops, I made a syntax error. NOPASSWD OK, then log logout from web zero one. Clear the screen now I'm going to log into Web 02. And then do the same thing here. After adding the user, again, I updated into the sudoers file. Then log out from here, Web 02, let's log in to web03 now and again perform the same thing. OK, but we can't do this. There's a problem. We can't log into Web 03, which is Ubuntu operating system with a vagrant user. Basically, we cannot log in with the password. We can log in only with the key. Now, what that means is whenever I was trying to log into Web01, Web 02, it was asking me password. Right there you see it's asking me, password, but for Web 03, it says, permission denied public. It's not even asking the password for vagrant user. So Web 03, which is Ubuntu, does not allow password login. So you have to log out from. The script box and we can log in from here, vagrant, this is a Web 03 from our host machine. This is key based Longin. OK, I'm in web03 and I will show you what is the problem. Just run sudo -i in Web 03. I'm sorry, Web 03 and open this file, /etc/ssh/sshd_config, file. To any Linux machine. If you're trying to login with password and if it throws you that error, public access denied then huge chances. The password login is disabled, so open this file and find the line password authentication. OK, there, line number 56, you can see password authentication. It says no. So my guess was right. We can just say now password authentication. Yes. save & quit and then restart the ssh service. It's ubuntu machine ssh service name is just ssh. And let's go back to script box. Now from Script Box, let's try once again, vagrant@we 03. And you see it's asking the password and we can log in. Alright we are  able to login with password. Now, let's continue with our activity. We are going to add the user. Dev ops, and this is an Ubuntu machine, so I'm going to use the command and user dev ops. It should also ask me the password. And we have to add the users in sudoers files, I'm going to say export EDITOR=vim. and then visudo OK, so devops user in the sudoers file with NOPASSWD All right, guys, so now we are all set to do the remote execution now let's come back to our script box. Let's come home. So what are we doing so far in this lecture was the preparations, so we can execute command over ssh or remote execution. So this is how it works. So when you do a ssh and we will be using the user devops, as I said, and let's say web01 . When you do ssh, it gives us the shell. But if with that we give a command, let's say uptime. Now observe what happens. Look at the prompt. OK. It is asking the password. Let's enter the password. So we logged into Web zero one with the password it executed uptime command, and then we are still in the script box. So we really don't need to login, execute, command and log out. We can execute the command from here itself.
