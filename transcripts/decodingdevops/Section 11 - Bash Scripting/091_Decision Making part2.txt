# Decision Making part2

Welcome. In this lecture, we will see one more statement from the conditions, which is called as 'else if' or "elif". You have seen in the previous lecture, with "if" we give one condition, if that condition is false, we go to "else". But what if you want to check multiple conditions? If this condition is false, then try other condition. If that is also false, then go to the "else". So let's take an example. I'm going to run the command ip addr show, which will show all the network interface. Now, let's see from my script, I want to count how many active network interface are there. So here we see three, but basically we have two. The first one is loopback. So we want to ignore that and we'll see how we'll do that. But one thing that is common across the three interface, let's find something. Let's say mtu. Yeah, that text is common. What if we can count the occurrence of "mtu" ignoring this one, so first, we have to ignore this line. So we'll see. pipe grep -v Yeah. Let's take some text from this line, which is loopback that is unique. So that line is ignored. And now we will count the occurrence of mtu And I want to ignore the case. And I'm just interested in the counting. So I'm going to say c, which will basically give me the number of that occurrence. So it's says, 2. OK, let's clear the screen and let's run this command once again. So number of time, the mtu is occured in 2. two times. So we can say we have two network adapter, but I want to do it through the script So first, I want to store this into a variable. And again, I want to put it in the script. So let's write a script. We'll say in 9_ifelif.sh So the occurance will be stored in the variable value, and now I'm going to check if. Dollar  value is equal to. -eq, equal to one. That means I have one network adapter active. So I want to print that message, but I also want to check if it's greater than one. So I'm going to use a statement called as “elif”and put one more condition there. $value -gt, greater than 1 And I will say, "then". I will say found multiple active interface. Else and there could be a chance there where there is no active adapter, excluding the lookback, you're already excluding the lookback. So we want to print a message, no active interface found and then close it, "fi". "fi" comes at the end, the closing. So we are checking multiple conditions. If this is true, this gets printed. That's it. End of the story. But if this is false, it checks this. If this is true, it executes that. If that is also false, then it finally execute the "else". So one of the statement will get printed and we know which one it is going to be. It is going to be this one we already checked. There are two network active adapters. So let's execute this. So let's run this. And there you go. OK, we have two similar names, let's rename these things. So this I want to rename with the name. Ten, because that's the tenth script. OK, let's run it once again. So by using this knowledge now, we will write a script in the next lecture to monitor process and take action based on that intel. So wrap this up. I will see you in the next lecture.
