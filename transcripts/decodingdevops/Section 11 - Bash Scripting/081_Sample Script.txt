# Sample Script

Welcome in this video, we will take a sample use case, we have some commands to execute which will set up the website for us. So instead of doing that, manually, running all the commands, we are going to write a script to do that. So I'm going to call this script as Web site, setup script. All right, I just say, Web setup. So I know all the commands to set up the website. I'm talking about the similar example we've seen in Linux download it from tooplate.com and set it up so we know all the commands. You're going to simply record all those commands in this text file, which is basically your script. So First Line Shebang Chan=recter And first just simply will record the command, so if you have the command yum install wget unzip httpd -y And if the script executes from a normal user, I would like to give a sudo. -y so it doesn't ask us any question because we have many things to do here year after this, I will create a directory in the /tmp. We'll call it as Web files. And I'm going to use -p option, so if there directly exists, it will not through any error, it will just simply ignore. And then we will do cd into that dir. So basically, we're going to create all our temporary files, in this directory, like we have to download the artifact, so wget and the URL. So I am at tooplate.com and let's get a template. I'm using this health center. So we need this, URL Let's go to the downloads. Lets hit F12 F12 in our browser, it should open up this tab for debugging and we should be in the network tab. Let's click on the download. And you should see there the link to 2098_health.zip, let's copy that link. And let's just paste it here. Next, we would like to unzip that zip file. And hopefully there is a directory inside that with the same name. And we would like to copy all the content to /var/www/html, and then we would like to do systemctl restart httpd. Now, after installing, I would like here it to start first systemctl start httpd and a systemctl enable httpd I will just keep a sudo there. And to copy here also, we need to do so because the district is owned by the root user, so you need to do the sudo. And I think that is it. But also, I would like to do a clean up after this is done, I would like to remove. This dir, which will contain the temporary files. OK, this is good. This should do the trick, but now I'm going to use comments and proper print statements. So it also becomes much more readable. All right, I have done it basically I did two things I made this a script readable for me and I have made the script readable for the user who will be executing it. So I used comments and I use proper print statements. All right. So along with the script that you write, try to do that. So I did that for each and every step. All you can see is section. Same thing comment and making it readable for the user. So let's execute this and test it. And I'm going to use the absolute path. But I can't do that. I mean, the tab doesn't work, I'm not able to complete this because I have to make it executable first. So I can go into that directory, make it executable, or I can just use absolute path. Let's run this. Huh OK, we have some errors also in our script, which we will fix, but really it's not that readable. I mean, it's just printing too much information for the user, which maybe user is not interested. So first, let's fix the problem. The problem is this. When we unzip, we should have the directory with this name and not with the zip extensions. So fix that. And that is, again, on line number 28. So that was one problem. We fixed that. The other thing is just generating too much information. For example, installing package. Maybe I don't want to see all that information. So I'm going to redirect all the information to /dev/null You know what? I'm not going to use &. I'll just use just the arrow >, just the redirection. symbol, because if there is an error, I want to see it on the screen. But if there is no error, then just put the output in the dev null. So that generates some output. And what else generates output, wget, the unzip. Let's redirect this output as well. And Unzip as well. And this is not giving any output, bouncing a service, this is also fine, cleanup also should not give me any output. OK. We did that. How about we also print the status of the service at the end. After we are done with everything we can say sudo systemctl status httpd httpd And also, maybe the content of /var/www/html Let's do that once again. OK, so the service is active, running and we should see the content of our /var/www/html dir. OK, it's time to test it. Let's get the IP address of the VM. ifconfig the IP address. Let's open that in the browser. And the setup is completed. So really, good work in the next video, we will learn about variables. So wrap this up and join me in the next video.
