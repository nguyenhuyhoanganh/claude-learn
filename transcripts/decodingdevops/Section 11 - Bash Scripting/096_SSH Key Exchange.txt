# SSH Key Exchange

So we can do that, but the problem is every time we do it is asking is the password. Because, of course, we need to provide the authentication, but there is also one more way to login in password, which is key based. We are doing password based login, but the key based are considered as the safer login. And for that, all you have to do is, you have to run this command to ssh-keygen, which will generate the keys. So it's key pair, you read there, it's generating public and private keypair. And this is where stores the key in the user's home dir, ~/.ssh And that's the key name. This is the private key name. hit enter and hit enter here again. Enter. So this is the private key and this is the public key. It Works like Lock and Key. So this is going to be the lock, the public key. And private key is going to be the key to open to that lock. So we're going to put this lock on web01,web02,web03, and we will have this key to open that lock and which is not so difficult to do. All we have to do is we have to run ssh-copy-id command. Give the user name on which you want to put to your lock @hostname. So it will take this lock and apply for this user on Web zero one, let's hit enter, it will ask password, enter the password for devops user, and thats it loack has been applied. Let's do the same for web02. And let's do it again, for web03. OK, now let's test it again. Let's clear the screen and now let's see. So, again, remote execution, but this time it is going to take the key. To login. Do you see did not ask the password. Now, the way it works is in your home directory, you have the .ssh dir and in this .ssh You will have the private key. If your key name is this id_rsa, it becomes your default login key. So whenever you are doing a ssh, it is using by default this key to login. So what really is happening is it is actually doing ssh -i .ssh/id_rsa So it's using this key to log into this machine and execute our command. See that now the content of the private key and pub key You should be able to read and understand, I mean not really understand, but you should be able to understand a private key or public key So let's check this key idea. id_rda So you see it says, private key. Now, the key name can be really anything, but when you do a cat, you can see the content. If it's says private key. It's a private key. Priv key will be longer. OK. If you see the public. id_rsa.pub The public will be little shorter. OK, so when this lock matches this key. All right, so they are generated in pairs, together, so when this both matches, you get authenticated. So that's the analogy. So that's all in this video, I will see you in the next one.
