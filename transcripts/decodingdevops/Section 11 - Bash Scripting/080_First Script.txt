# First Script

Welcome. We are going to write our first script and we will place it in a directory, we will crete a dir /opt/scripts /opt/scripts Let's get into the directory. We're going to write all our script in the command line, and we're going to use vim editor. And as you see, we will not be installed by default on that centos box. So we're going to install vim first. So a script is really a text file that contains bash commands, so to write a text file, obviously we are using vim Let's write the script first. firstscript.sh , .sh is in general, the extension of Shell script, but that is not mandatory. Let's open the file. First line in your script will be a Shebang character hash exclamation. That's one character Shebang that really gives or tells the path of the interpretive. So what basically we're telling the script when it executes is that open this interpreter and execute all the commands that I'm writing on that interpreter, if it happens to be a Python script, you'll have the path of Python interpreter. If it's Ruby, you'll have Ruby. But since you are writing bash script, so that is the path of the bash interpreter. And I'm writing a command echo, which really means print. Print what? What are you giving the double code that will be printed. So that's what I want to print. And I'm going to say echo and just hit enter. When this execute, it will just print an empty line. Also, let me write a few other things. I will say the uptime of the system. Is. And I write the command uptime. Memory utilization. And I'll give the command free -m. Then I'll say disk utilization. df -h Ok, that's what my script is, it's going to print some statements and just run the commands, basically we're just going to run these commands. So let's save when quit. And in order to run a script, you have to give its path, absolute or relative path. First Let's go with the relative path So dot, current working directory, forward slash and the script name. So I'm basically giving its relative path, when I do this. I get permission denied. if you check the permission of the file? It does not have executed permission for the user who is root or for that matter, group or others does not have execute permission. So you can't execute it. So we have to make, give it executable permission. So I'm going to chmod +x And the path. And then if I run it. Then that's the output, right? Yeah. It's not much readable, but let's make it more readable. Let's open the script once again. And I want to give gaps, let me set the line numbers, so at line number five. I would like to say echo And let's put some hashas there. After the uptime command complete, I would like to give an empty space again and again. So, yeah, this should be printing well now. This makes the output readable, but we also need to make our script readable. We should be able to understand our script later when we open. Let's after a month or so of what the script is doing instead of reading the entire script and understanding what it is doing. We can give comments. We can use hash and watever we type after that will be ignored by the interpreter. So this is called as comments, so we can say, this script prints System info. And I can give something like this. That's a three hash. And then concluded with 3 hash, so it's like a title. And then here we can say. Checking system uptime. And in line number 14, I'm going to say memory utilization. Now, I know the script is pretty simple and we really don't need to give so many comments, but we have to make this as a habit. So our code becomes more readable. OK, so as you see now, the script is also becoming readable and the output will be also readable. Let's execute it now one more time. Let's clear the screen. And as I said, you can give the absolute path. And you can also give. Sorry we have given the relative path also we can give absolute path, so the absolute path of our script is let's clear the screen first. /opt/scripts/firstscript.sh OK, so you have seen we can give absolute path or relative path. to execute our script. All right, one final time, let's open our script once again. And let's read it quickly. First line is shebang character. Yeah, line number one, shebang character, that is the part of the interpreter, that means open this interpreter and run all these commands on it. We have seen comments hash, hash is comment in bash, Python and Ruby in many other places. That means just ignore everything after that except this one, OK? This is one character. This is not hash. This is hash exclamation shebang character. So we kind of give a title to the script. We give some commands. We give some some comment, and then we give some commands. We give some spaces. And similar way, so  really script is just commands in a text file, which we can execute whenever we want. So that is all in this video. Next video, let's learn more feature in the scripts. So wrap this up and join me in the next video.
