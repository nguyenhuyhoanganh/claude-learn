# Finale Part2

All right, so welcome to the last session of BASC scrioting in last video we have seen. How we can have a script detect operating system and based on that. Execute the right command. In this session, we will push the script to all the remote machines, Web 01, Web 02, Web 03 and then execute it. And for this, we need to write another script. Now, if you don't know how to push files to Linux machines, well, it's pretty simple. Just need to use scp. Now, scp uses ssh, so if your ssh is working, your scp works. So let's create a file, echo. I say. testfile . testfile.txt OK, so let's say this is the file, I want to push it to the other machine, so I'm going to say testfile source and then you give the destination as you go for ssh username at the rate and the hostname or the IP, colon and you to give the path, for example, /tmp/. OK, so that's how you can push the script. All right. Let's try a different dir, the root dir and it says, permission denied because the root directory is owned by root user and devops user is trying to access root dir. So that's why permission denied. Similarly, you can fetch the file also instead of source, you just change from source to destination and destination to source. And it did not ask us any password, anything, because it is using the key. OK, as I said, scp uses ssh protocol, it is basically using our key. .ssh/id_rsa, so it's happening like this. Yeah. To slash home, slash dev ops. If there is no key exchanged, then it will. ask you the password. So now we write a script that is going to push. Our multios websetup script and then execute it. So let's write it. Let me clear the screen first. I want to copy the name. And I'm going to write the script name as Web deploy dot. All right, so I'm going to write the for loop, for host in. cat remhosts file do echo connecting to $host. little cosmetic changes OK, so now let's do the ssh here. Lets also set the hostname as the variable. Oh sorry username as the variable, so I'm going to see USR devops $USR@$host so that will have the hostname. No, mybad We have to do first scp, copy, push our script. that script to this location. :/tmp/ OK, and then simply execute the script. How? Well, ssh and  execute. /tmp/ Yeah. Give the path of the script, and that's it done. Oh, we can do some cosmetics. We can print some messages. All right. So just put everything in between this hashas and printing messages like connecting host one, pushing the script, executing the script. And what this basically contains is two things. It's going to push the script and execute the script. Now, if the script doesn't have sudo, then you should give sudo here. Because our script contains command of installing software, managing service that needs root privileges, so either you do sudo and execute the script or your script should have the sudo commands. And once this is done, I also would like to. Delete. Delete the script, now script generates data, and that data we are cleaning in the script itself, we have the commands for that. But the script also needs to be cleaned. So once the execution is completed, I want to cleanly basically clean execution. OK, remove the data which got created while the execution. So let's  save & quit this Lets make it executable. And it's Showtime. All right, so that worked for Webb zero one. OK. And then this is going for Webbs 02. OK, Web 03 great detected that you see that, and it's using apt there. Nice. OK, that completed. Let's check that. So. The IP address, /etc/hosts All right, because my laptop does not know its website on Web01/02 We don't have it in the host file there, so we'll access all the IP addresses from browser. So that's Web 01, Awesome. It's set up and that's 14, Web 02. Nice. That is also done. And that is Web 03, which was different operating system. Alrighty, right. So you see the amount of power now you have in your fingertips at your fingertips. Imagine this REM file, remote host file. Imagine there are hundreds of machines over there. All right, so you can write a simple script and push it and execute it and manage hundreds of machine, that's how we used to do automation for many, many machine like a decade back. But now we have much more fancier tools like Ansible, which makes our life easier. But if you really want to learn and master those tools, you should have these background of scripting. You should understand how this condition works, how this for loop works, how you can use it to achieve some complex tasks. And trust me, if you can do this, if you have come so far, you have done a good job, a great job, and now you are eligible to learn any automation tool. And not only just eligible to learn, even eligible to master them. So overall, really very good job. Now we can conclude our Bash scripting section. I recommend you go through all the scripts, download from the resource section, go through all the scripts. And once you do this, wrap this up and join me in the next section.
