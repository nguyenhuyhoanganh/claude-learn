# User Input

In this lecture, we will learn about user input, how we can make our script interactive, taking input from the user and then execute the script. So it's done simply by using the command read and the variable name. When you executes a script which has a read statement, it will wait for the user input, user enters a value, it will be stored into the variable, and then we can use that variable. So let's take a simple example. OK, so that's the script. Just some print statement and a read statement. We say enter your skills and then we say read and variable named SKILLS. or I will jusr say SKILL Single variable. And there also I should be reading it now, what read does it wait's for the user input, user enters, a value It gets stored into the variable and then you can use that variable as we are printing it. There's also some options with read, -p, which is for prompt. So it's like a typical prompt. It's going to print this and wait. User enters a value it will store into that variable -s another option that will suppress the input. That means when the user types, user will not be able to read or see, which is mostly used for things like password or any secret. So we're telling user, We are giving user, a prompt for username and a prompt for password, user enters. And then just simply a print statement. So we're printing the user variable. But don't you dare print pass variable, because if you print that, then what's the point of using -s -s? That is, so nobody can see what you're typing. Let's make it executable. And let's run it. There, so it's waiting for the input I'm going to say my skill is cloud computing. So it prints that. And and that is the prompt. So let's give a username and password now I'm going to type I'm typing, typing, typing, but it's really not printing anything. That's because we used -s option to suppress the input. So that's how the read works. But it's really not recommended in devops at least, to make the script interactive because we run scripts from background from some other tools. And we really don't want user to interact with the system. Because user interaction is always error prone. Well, in any case, if you need input from the user you can use read. That is all in this lecture. I will see you in the next one.
