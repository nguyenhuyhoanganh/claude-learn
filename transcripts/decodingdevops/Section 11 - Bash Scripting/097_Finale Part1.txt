# Finale Part1

All right. Welcome to the grand finale of this bash scripting. Whatever we have learned so far, we are going to put all that into use and create really some awesome scripts, or should I say frameworks. Now, whatever we will do in this grand finale is the base of many, many other automation tool out there. If you are able to do what we are doing in this lecture, then you can learn and master any automation tool after this. So we are going to do a Web setup in the script, but so far we have been running the scripts on the script box itself. Now we're going to run our scripts on the web01/02/03 So I'm going to create a separate directory inside this. I will say a remote underscore Web setup. And let's get into this dir. First, I will prepare a host file. OK, you should have a host file like an inventory file. I will say host or I'll give it name as remote host, something like that. And if you're using IP address, give the list of IP addresses here or if you're using host names which you already configured in the host file so it can give the hostname of web01/02/03 Like that. So how many ever you have 10, 12, 20, 500? You can all mention that into this file. So these are going to be your targets. Now let's test the log in to all this machine. And I'm not going to log into them one by one. I'm going to use a for loop. I'm going to use variable host for host in and back ticks, remote host. This semicolon is a Terminator in bash, that means this line Terminates here, so, you know, you can start typing things in the same line. So after this, we have "do" right and we can say do echo $host And done. So what really is going to happen? One by one, every line will keep getting stored into this variable and we're just printing it in a loop. Now, instead of printing, I am going to do an ssh, ssh, I'll give the user name, which was devops we set. And with that will give. command let's say. I'm going to give free -m. You know what? Better you just give hostname. We're just testing the login. Yeah. If you're wondering the same output, that's because the host name command is going to return the hostname But if you try, want to try execute some other command, you can do that. Now, see, it looks so simple, but it is so powerful, imagine you have hundreds of host in that file and you can execute command on them by just one single line of code. Maybe you want to install some software overthere. So I can say sudo yum install git. OK, so that worked. But except for Web 03, you see there, yum command not found. That's coming from web 03 because Web 03 is Ubuntu operating system. And in Ubuntu, we do not have yum command. We have apt command. And we'll see how to fix that. That's what we are here for, actually. So a test is working fine for remote execution, running in a loop. Now our job is to write a script which will do Web setup, as we have been doing on script box, so we will copy. Some existing script and we can modify that, so I'll take this one 3_vars_websetup.sh. Copy that here. And let's rename it. To you going to see. multios Yes, multios, because. We have ubuntu & centos mix, this script was written for centos but we will modify it and make it multios so it works on multiple operating system. And for that, we need to check some conditions. If you're going to run a command yum --help and redirect all its output to. Dev null, because we really don't want to see that output, we are just interested in the exit code. Yes. If the exit code is equal to. Zero, that means it is a centos operating system. I can print that message echo. I will give here 3 spaces Running set up on CentOS OK, then I'm going to just make sure everything comes like this, OK, also we have to make sure we set these variables also. Over here, we are going to not set the variable up there, but when the condition is decided, when it is CentOS, then we are going to say the package is httpd. service is httpd, because for Ubuntu, it will be apache2 So I'm saying here. OK. Proper line spacing only gives it looks good. So have you given all the spaces? And that's the "if" block. If its centos, execute this. And I'm going to say than "else", because the other operating system is ubuntu and we can just then give all the Ubuntu commands. So I'm going to save this file and do a cat to it. So I can copy all this content and just make the changes. So I just copy this. Is all the CentOS commands for the deployment And we are going to be paste everything here, and I'm going to say "fi" I and will make changes as per the Ubuntu operating system, starting with. The comment, set variables for. Ubuntu And so the package is apache2, and the service name is apache2. And we need to give here apt instead of yum, and before this, we need to give in Ubuntu, we have to give sudo apt update. Yeah, and that's it, systemctl start, $SVC, that's a variable. So we don't need to change anything. And that all these are variables, right? You see that? Now, I hope now you understood now the power of variables, right? You can use the existing code, change the variable value, and that's it. All right, so that is a reusable code or multios? All right. Let's see once again. So if this returns the exit code zero, then CentOS deployment, else the Ubuntu deployment. OK, let's test this script locally. Yeah, see set up running on CentOS. Because script box is centos, so that worked. Now in the next video, we will see how to push the script to all the Web server and then execute. That will be the proper remote execution with scripts. So wrap this up and join me in the next lecture.
