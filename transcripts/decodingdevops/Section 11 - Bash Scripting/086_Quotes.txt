# Quotes

Welcome. In this video, we will talk about quotes. Very simple thing, we have two types of quotes in bash, double quotes and single quotes. Let's see how they're different. So you must have seen so far. I've been using double quotes. So we have also single quotes. So when I'm taking a variable, let's take a  variable called SKILL and let's take double quotes and store the value. And let's try to print that. Nice. OK, what if I do it with a single quotes? Now, when we're doing this, we're actually reassigning the value to the variable, so this value will be overwritten from the previous value. So let's print it once again. Hmm. We don't see any difference. Most of the time there is no difference. But now let's take that variable skill and let's put that variable in the double quotes when we're printing. I will see. I have got. $SKILL. $SKILL So the variable is in the double quotes, that's the sentence, and it's working fine. But now let me put this into a single quotes. And now let's see what happens now, observe the output. You see there in double quotes. The variable worked. But in single quotes when I put it  just printing dollars as it is, so thats how single quotes is different. It removes the meaning of special character. Dollar is a special character. It has a special meaning in Bash. What if I put it into a single quotes? It's special, meaning is gone. OK, so what if we have to print a dollar and also a variable in the same sentence? Let's take a variable, VIRUS. So I want to print this message. Due to $VIRUS virus, company have lost nine million dollars , now observe? This is a variable, but this is not I want to print it as it is, but this I want to access the value of the variable. But if I do it in double quotes, this is what I get. Well, this worked, but the nine million is just lost there. Yeah. Which actually is lost as per the statement. But you see, dollar nine actually means the ninth argument. But we don't want it as an argument. We want to print that so we can put that into single quotes, yes. But then this will happen. And this printed as it is literally. But this is not accessing the value. So what do we do in this case? Well, you put it in double quotes, but wherever you have a special character, you put a backward slash, not forward. Backward. What happens the next charactor after that, if it's a special character, will be ignored. That means the next character special meaning will be gone and you will see it like this. So if you're searching for a special character in vim, or if you want to print a special character, then you can also put a backwards slash to remove the special meaning of the next character. Well, that's all in the quotes. I will see you in the next lecture.
