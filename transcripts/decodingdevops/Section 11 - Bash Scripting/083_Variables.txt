# Variables

Welcome in this video, we will be talking about variables. Variables are basically temporary storage in the memory. As we know, processes runs in the memory in the ram. And if the process is dead, all the data, along with that process, all that temporary data will be also lost. So as hard disk is a permanent storage of data, variables are temporary storage in the process, which lives in the memory. So let's see how we can store variables and can retrieve them from the bash shell, or should I say bash process. So I will take a variable called SKILL. Equal to, so skill is the name of the variable, and in that I'm storing a value called as, DevOps a text. And that's it. That's how we store the variable. To retrieve the variable, we have to use the dollar sign dollar and the variable name. Now, if we are trying to print a variable, we know the command to print, which is echo. So I can say echo dollar SKILL which will access the value of the variable. And you see there. But here the dollar is important, if I don't give the dollar, then. Skill is just another text. Let's take another variable called as. Package, if I can type, and I will give in double quotes as httpd wegt unzip. So this is the text basically I'm storing in the variable PACKAGE, and I'll show you how I'm going to use it. I'm going to say yum install dollar PACKAGE. -y, now that package variable will be interpolated with this value that we store, httpd wget and unzip And we see there. Since these packages are already installed, so we get the message already installed. And yes, that's basically is a variable. Now let's implement this knowledge in our Web setup script. So I'll go to scripts directory. Let me clear the screen and let's rename these scripts in a numeric number so we can easily identify them. So first script I'm going to say 1_firstscript.sh Now I'm going to implement our variable's concept in the script, and I would like to copy the script now with a different name, and I'm not going to make changes in this file. But in the next file. So let's open that 3_vars_website.sh. OK, I'm going to first declare the variable on the top of the script. All right, I have declared these variable,  package, svc Service name, httpd, URL from where it will download the artifact, artifact name, which is the directory name that will be when we extract it and temporary directory, a the place where we're going to hold a temporary files. Now, if you're wondering how did I select these things as variables, so use this logic. Things that changes or varies based on different environment or project or use cases like, for example, if I happen to change the website from this URL to something else, then I can just update it or here. I don't need to make change in the script. Also, some things that gets repeated multiple times in the script. For example, the service name is repeated, is repeated multiple times, like when we are starting it, when we are enabling it, when we restarted. So if we declare that as variable, we don't need to change it at multiple places. We can just change it at the variable level. So I have declared the variable. Now I'm going to use it. So instead of this package names, I'm going to just give their dollar package. SVC  for the service, temporary files. URL & artifact name, OK, if you see, I have used this variable smartly with .zip extension over here and here with just the name, so I can use the same variable at two different places for two different use cases. OK, then again, the service there. And finally, the clean up directory. All right, that is done, verify it once again, after declaring and using the variables, If the names are matching once you are good. Save & quit the file You can check it once again by doing cat. Now, before I execute this, I'm going to dismantle everything. And for that, I'm going to write a script to remove it, actually remove the service and all the data first, and then we'll execute the script. OK, so that's a simple script, I'm going to stop a httpd service, httpd. Remove the data and remove the packages. That's really not necessary. But I want you to show the execution properly, so I would like to remove everything first. OK, there. Now let's run our three 3_vars_websetup.sh, which uses variable. Great, everything seems to be fine. Let's check through the browser. And that is working good.
