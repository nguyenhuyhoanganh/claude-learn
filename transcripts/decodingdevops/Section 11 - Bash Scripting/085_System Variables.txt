# System Variables

In this lecture, we will talk about some system variables we have already seen in the last lecture, zero is the name of the script dollar one two nine are the command line arguments. Dollar hash means how many arguments? Dollars at the rate All the arguments. Dollar question mark $? the exit status of the last command . Now, this is interesting, and we are going to use this a lot later. So let's see what this really is and how it works. So let's say I'm executing a command, a free -m. Then I say echo dollar question mark. I'm checking the value of questionmark. Questionmark means the exit code. So I'm checking the value, which is zero. If for some reason, the command that we are running gets failed, maybe we have given wring command or something. So I'm giving a wrong command Too many eeeee in the free. So that says command not found. Now, if we check the value of questionmark. It's 127. That's non-zero. or. Let's give some different arguments. So second argument, blah, blah, blah, something failed. The exit code is one. So basically what this means is if you get zero, that means the last command was a success. If you get a non-zero the last command failed. And we will take a few more variables like a user print. The user name dollar a hostname gives the hostname. Dollar  random, gives as a random number. So we will be using some of these variables in our upcoming scripts. Go ahead, try for yourself.
