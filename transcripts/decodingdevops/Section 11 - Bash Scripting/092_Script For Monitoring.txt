# Script For Monitoring

Instructor: Welcome back. I know in previous scripts you must be thinking "-gt, -eq, what is that?" Yes, you understood that those are operator which are comparing, but where did that come from? Well, Bash has many operators that you can use. Some of them are displayed on the screen right now from the "Decoding DEVOPS Book." Like exclamation is a negation. If anything is true, it will make it false. If anything is false, it will make it true. -: n, if a string length is greater than zero, it will be true. And -z is the opposite of that. Or to compare instead of eq, you can use single equal to, Not equal to, you can use exclamation and the equal to symbol and eq, gt, we have seen. Lt is for less than, and then we have single operators. What I mean by that is they just need single operand. So -d and you give a path and it'll say true if the directory exists. -: e if the file exists, -r if the file exists and it has read permission and there are a few others. Now, we'll use some of them in the upcoming script, but I want to show you the use case of the question mark variable, which is used a lot. So we have seen that if a command is failing, the exit code $? value should be nonzero. But if the command works, its exit code should be zero. Zero means true in Bash scripting and nonzero means false. Or we can just use the numbers. So let's take an example. I'm running the command systemctl status httpd. The process is running. Whenever there is a process running, there will be PID file that will get created. So if httpd is running, there should be a PID file for it, /var/run/httpd/httpd.pid. This file will contain the process ID, but if the process is dead, let's stop this process, then that file will cease to exist. You see? No such file or directory. Now, if we get no such file or directory, then the $? question mark value will be nonzero. Hmm. Now that is something we can use. So let's start the httpd process first. The PID file appears. Now we will write a script that will detect if a process is running. If it's not running, it'll start the process. Renmae the script as 11_monit.sh. And by the way, there is a tool called as Monit that does exactly same thing and you are really writing a script that monitors process and can take action as well. Okay, so script runs that command and the exit code should be one, so $? =, sorry. It should be yeah, -eq 0 if the file exists and we just save, the file exists, that means the process is running. Else we will say the process is not running and we will also start the process. Yeah, that's what we can do. Now, we can really conclude the script. But what if there is a problem and this process does not start? The user should be informed that it tried to start the process, but it did not start. That means we are going to use one more condition there now. We are going to give a nested condition. Okay, there. So I'm starting the process and if this works, the exit code should be zero. And if it's zero, that means the process started successfully. Else, the process failed to start and now you should contact the admin. So there is a nested condition there. Just make sure you're closing the conditions properly. So this is a nested condition inside the else block. And I will also put few other commands like date and I will make it a little more readable for the user. Okay, so some hashes there and we are redirecting the output to dev now because we're really not interested in seeing its output. We just interested in the exit code. Now I said I'm making it readable for the user. If you wanted to make readable for yourself, please use comments. The reason I gave those hashes and date is because I'm going to make it as a regular process, which checks our httpd process and it will also redirect. We are going to also going to redirect all the output to a file, like a log file. And we'll see that in a moment how we'll do that. But let's first save this file and let's test it. Okay, so it says http process is running. Let's try and stop the process and now let's execute the script once again. There, it found out, httpd process is not running, it is starting the process and the process started successfully. Now, this really is a nice script, but it's going to become more awesome if it runs automatically by itself, let's say every minute. So if our process goes down for some reason, it is going to check that and start it and it's going to check that every minute, like a monitoring tool. And for that we need to schedule the script and we are going to do it through cron jobs. So run crontab -e. Oh, before that, let's get the path of our script, /opt/scripts/11_monit.sh. Okay, let's copy the path and let's run crontab -e. Cron jobs are scheduled tasks. So crontab -e is going to edit. Let's open this. It's going to open the Vim Editor. You go into insert mode and then you can give the command that you want to run basically. And when you want to run it, that schedule you need to pass. So we five values over here. I'll put two hashes or a single hash, which will be ignored. So the first value is going to be the minute. The second is going to be the hour of the minute. So I'll just say HH. Then date of the month, like 1st, 2nd, till 31st and then the month itself. I'll give small m here, one being January and 12 being December, like that. And then a day of the week, zero being Sunday, one being Monday like that. So for example, if you want to run something every night at 8:30 PM so I'm going to say 30, the minute, and 8 PM I said, so 20. Day of the month or a date of the month, 1, 2, 3. So I'm going to say here star, that means run it on every date and which month? I'm gonna say run it every month. So just star there. Day of the week, I want to run it, let's say from Monday to Friday. So 1-5, that's a range. Okay, so that's one example, right? And then you can give the command that you want to run. Both are commented, so it's not going to really work. Now, I want to run my script every minute. So I'm going to say star and for hour also, every hour, and then every date, every month and every day of the week, Sunday to Saturday. And this is going to run, but it's going to generate the output that I want to store all that output to a file. I'm going to save it into /var/log/, I'm gonna say /monit_httpd.log. So all the output goes into this file because we need the output to track what's going on with our httpd process. Okay, now let's save this. All right, so we have installed new cron job. Now let's stop our httpd process. It's going to run every minute, so we have to just wait and watch. So it's been more than a minute when I stopped the httpd process, now the log file should have been created by now. There the file is created. Let's look at the content of the file, /var/log/monit. There you see. So it's like more than two minutes now. So first the process I stopped. So it says now I tried to start and it is running at this particular date and time. And then again, it ran after one more minute and then it was already running, right? Let's stop it once again. And you can try that, you know, stop it, start it, right? And you can tell the logs. Okay, there you see? And really congratulations, you have created a monitoring plugin. Now, yeah, there is an actual tool that can do that in a much more sophisticated way, but the point of this whole exercise was to train you on scripting an intelligent script. Okay, now, same script, but with a little different logic. So we are basically checking here the exit code, but we can do one more thing or really the other way. I can just give here the operator -f and give the file. So -f operator, see, single operand, it just checks whether this file exists or not. If this file exists, that means your process is running. Else, it is not running and it tries to start the process. So it's really just the matter of creativity. But at the end of the day, the result matters. Both script does the same work. And trust me, there are many, many more methods to do the same thing. And this will all come with experience and you can make your choices which method to use, or you may have your own style. I'm a big fan of exit codes, especially in Bash scripting, so I usually use that. All right, so we did good work here. Let's see in the next lecture how we are going to do repetitive task through loops. So I'll see you in the next one.
