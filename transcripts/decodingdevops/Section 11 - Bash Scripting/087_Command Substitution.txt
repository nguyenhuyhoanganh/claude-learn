# Command Substitution

In this lecture, we will talk about command substitution. Which you will really need if you really want to write some intelligent script. What command substitution does it it takes the output of a command and stores it into the variable. We can achieve that by using back ticks ` or $(). If you put a command in the  $ parentheses, then it's going to take the output of the command and store it into the variable. So let's see that. So let's see the command uptime. It prints that. I want to store its output into a variable. Let's see. I want to use variable up. And if I put this into double quotes or single quotes or without a quotes, what happens is it just stores the name of the command. That's it. What if I want to take its output and store it into the variable? Then I'm going to use back ticks, not a single quotes. Back ticks ` OK, let's take one more example, let's see um. who, OK, so, who all are logged into the system, who command will return that, so let's see. $ parentheses, and it will take a variable current_users. Now, both does the same thing. OK, now let's use. filtering and get the right value that we're looking for, let's say I want to print a message saying that the free ram is so-and-so, I sort of take this value, so I'm going to do some filtering. I'm going to grep for Mem that gives me that line. And then from that, I'm going to use awk. And. Print. Dollar and lets count it. One, two, three, four, 4th one, 4th field, and I have to put it into curly braces {}. Right, and now I can use back ticks or $ parentheses and store that into a variable that would say free. Underscore Ram. So now I can print a message saying free ram is $FREE_ RAM, the variable mb So you see, if we use little logic, we can filter the right data. And store it into a variable, and then we can use however we want. Now let's see a simple script that will print system information or the health of the system. I have here a script. Which I have already created. So basically, prints Welcome $USER, on $HOSTNAME, so that uses a system variable and then we're using command substitution to get the FREE_RAM size, and we're also taking another variable load and we're taking the load from the uptime command again, filtering with command substitution, We are storing the output, then the root free variable, which takes. The free root partition utilization. And then we are simply printing it all here. Now we will use this script once again later in upcoming lectures. But imagine you have a script like this, that prints system information whenever you login to your system, and I'll show you how we are going to execute the script when we really log into the system. So let's run the script. There you go. Welcome root on script box ,available free ram is 590 mb, current load average is this much and free root partition is this much. So we'll use this intelligence in upcoming lectures. So I'll see you in the next one.
