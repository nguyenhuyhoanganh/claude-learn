# Loops

Loops, running something again and again and again, there are two kinds of loops in bash scripting. for loop and while loop, there are few of other, but we are going to deal with that. So for loops, runs for a definitive number of times. It's basically going to run on a sequence, a list of something. Let's say you want to add 10 users, and the command is useradd The only thing that changes is the username. So you are going to run. You can either run, useradd command 10 times or you can write a for loop that runs, useradd command 10 times for you. And every time something changes, username, basically. Or maybe you have a list of servers where you want to log in and execute the commands so you can run it on a for loop, to log in to these many servers and execute your task. This really saves a lot of time. Let's see how it works. OK, let's write a script. 13_for.sh OK, so this is how it works, this is the syntax. And you give a variable name, let's say, I'm going to give VAR1 "in" and then you give the list of the item or sequence. So I'm going to see java just something. OK, Java .net python ruby php And then I'm going to say, do do what? I run these few commands. Just sprinting, looping, and I'm going to say, value of VAR1 in $VAR1 Oh, we also have to close it. Let me close it. So we have to say done for the closing. So like in "if" you have "if" and "fi" is for closing. Here, "for" begins and "done" is the closing. And in the middle, whatever commands we give, it's going to run again and again until the sequence is exhausted. So first. The value Java will be stored into the variable$ VAR1 It will execute the code. And it's a loop. It's going to come back. It's going to store the next value into the variable, execute it. Then next. Then next, till it, exhaust the sequence. And I would like to put a sleep there. You know, so, it's because, it is going run really super fast, I'm just going to sleep one. So it runs little slow. So let's save this and let's make it executable. And let's run it. It's looping. Yeah. Nice. All right, let's go back. So this these set of commands are going to run at these many times. One, two, three, four, five, five times. All right. And you know what? I can put one more echo here so you can see it all separated. OK, let's see one good example. OK, 14_for.sh OK, so let's see, I have a list of users. All right, just give the proper name, my users and I will give here the user name, I will say, Alpha, Beta, Gamma. And that's all, let's write the loop for usr in Doller my users, do. useradd. $usr. And then I will also run the common id $usr. And then I'll simply say done. Or I will also print some messages here. echo adding user $usr And also, as usual, let's give separator. echo Some hashas. OK, so this loop is going to basically run three times, because we have three. Item in this list. Let's test it.
