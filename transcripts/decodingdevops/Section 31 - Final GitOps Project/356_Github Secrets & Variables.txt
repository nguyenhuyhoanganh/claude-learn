# Github Secrets & Variables

Okay, so as we know, at the end of this continuous integration pipeline, it is going to update the values.yaml file in another repository, vprofile-helm repository. So continuous integration pipeline needs some kind of authentication to this vprofile-helm repository. That will be through token. We are going to create a GitHub token and the username, that is our GitHub account username. These two things it will use to authenticate to this repository and then make a commit. So once again, the pipeline in this vprofile-app repository is going to make a commit in vprofile-helm repository. Now for us, both the repository are in the same account, so we can just go to our account and generate a token. But in your case, if you have in your project you're working, and your helm repository is in a different account, then you need to go to that account and generate the token. Keep in mind, it will be the account where you have the helm repository. Now, let's go to Settings of our account. Scroll at the end, you will see here Developer Settings. Click over here where you see Personal Access Token, also called as PAT. Click on Tokens Classic. Say Generate New Token. Generate New Token Classic. So it might ask you again the login details, so mention the login detail and note what this token is for. So I'm going to say gitops-pipeline. There's the expiration, 30 days. Select here Repository because we're going to make commit to the repository. Scroll down at the end, and say Generate Token. You get the token, this is called as PAT, Personal Access Token. Keep it in your sticky note or in your text pad, wherever you're saving all these details. Also, note down the account name. Mine is devops4sure. Yours will be different for sure. Okay, now let's save all these details into GitHub Secrets and Variables. So go to your repository, app repository, vprofile-app repository. We are going to create a continuous integration pipeline on this one. So here, you need to store all these secrets. So go to Settings, go to Secrets and Variables, click on Actions. These are the secrets and variables for actions, GitHub actions. Open your access key and secret key that you have already saved, downloaded. So these are the secrets that we are going to store. Let's start with Access Key ID. So it should be exactly this because same we are going to give in our pipeline, AWS_ACCESS_KEY_ID. I'll put all these names in the lecture resource so you don't make any typos. So Repository Secret, New Repository Secret, give secret a name. ACCESS_KEY_ID. So copy the Access Key ID. I know you are seeing my access key and secret key, but I'm gonna delete it as soon as this is completed. Add Secret. New Repository Secret. The next one is going to be AWS_SECRET_ACCESS_KEY. Copy everything very carefully because later you cannot view this. You need to just delete it and recreate again if you make any mistakes. Next, let's store Sonar Token. New Repository Secret. SONAR_TOKEN. Let me get the token. The first one is my Sonar Token. Helm Repo User. So this is going to be the GitHub account name. New Repository Secret. HELM_REPO_USER. And mine is devops4sure. Check yours again and make sure that is the same one here. GITOPS_PAT, the personal access token of GitHub. New Repository Secret. SLACK_WEBHOOK. That is Slack Webhook. So these are the information that needs to be secret. That's why we store it in Secrets. You cannot accidentally expose it. You cannot see the secret. The other things like the Region, which is common, and the ECR Repository Name, Helm Repository Name. These we can add as variables. So let's go back. Instead of Secrets, now click on Variables. Click on New Repository Variable. AWS_REGION. So us-east-1, that's the one I'm giving, because I'm using this one. The next variable will be the Repository Name, ECR Repository Name. So mine is vprofileappimg. I told you to keep the same. Let's add that. ECR_REPOSITORY. Add Variable. The last one will be the Helm Repository Name, which is vprofile-helm. And I assume you have also kept the same. So you can add the same one and say Add Variable. Now, look at the difference. Here, you can see the value of the variable, but if you go to Secrets, you cannot see it. If you click on Edit, then there is no option to see it still. You have to just enter a new value. So you have to be very careful while you're saving the secrets. Now, there's also one more variable, but this we are going to define later when we power on our SonarQube server. Because when we power on, we get a new public IP, and that's when we are going to store this into this variable, and then we're going to give the prompt, which will create our pipeline. That is all in this lecture. I'll see you in the next one.
