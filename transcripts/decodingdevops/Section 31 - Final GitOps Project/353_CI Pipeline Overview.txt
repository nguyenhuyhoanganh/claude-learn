# CI Pipeline Overview

All right, so, so far till previous lecture we have set up a GIT repository to store our helm charts in it. Later we are also going to add Argo manifest in this. That is for later. Now we need to focus on V profile app repository and the pipeline that we are going to set for this repository. The end of this pipeline will be updating the latest image name and tag in the helm charts that we have created in the previous lecture. So first we need the application source code. So in this Git repository we are going to copy the SRC folder palm XML file and the docker files. Once we have that in our repository, we're going to commit it and in the pipeline we are going to scan the source code and upload all the result to SonarCube server. So we need a SonarCube server and we are going to launch it by using AWS EC2, which will be simple. Just use this user data, set up the SonarCube server. Once we log in, we are going to generate a token for the admin user and store this in GitHub secrets. That I'll show you later. In this repository, we are also going to create a separate sonar-project.properties file, which will have information about the sonar. The project to create on SonarCube server, the version, the files to scan. All that information will be in this file. Then we are going to create an ECR repository to store our docker images. So this will be on AWS console. And as this pipeline is going to push the image to Amazon ECR, we need authentication. So we are going to create an IM user, give it this policy so it can store images in ECR. And we're going to mention the access key and secret key in GitHub Secrets. We'll also set up a notification system by using Slack. So we are going to create a Slack app, create a web hook, and store the Slack web hook information in GitHub Secrets once again. So in GitHub Secrets, we will have a sonar token, AWS Access key and secret key. So our pipeline can talk to ECR Helm repository user. This pipeline is going to commit the information in Helm Repository. So it needs to log into Helm repository, so username and a token, and then Slack WebBook information. We are also going to store three variables. These are the secret which nobody can see. These are variables. So AWS region. I'm using US East 1. ECR repository name, and helm repository name, which is V Profile-Helm for us. So let's get started from the next lecture, we are going to set up an Ec2 instance, which will have SonarCube server running. Then Amazon ECR repository will create. For authentication from this repository to ECR, we can to store access key and secret key, and to store or to update the values.yamel file. We need this GitHub username and token, so this repository can authenticate with this Helm repository. So join me in the next lecture, let's start the setup.
