# Architecture

So this is our project's GitHub's pipeline flow. But this is also a general GitHub's flow. You see two parts over here. Left-hand side you see GitHub, right-hand side you see AWS. That is because we're using GitHub and AWS, but instead of GitHub, it could be GitLab or any other Git repository hosting platform. Or instead of AWS, it could be GCP or Azure or any other cloud provider. So on GitHub, you see three parts over here. These three parts represents three Git repositories. The first one is a vprofile-infra, that's the name we will be giving, which will contain the Terraform source code. And on GitHub we have GitHub Actions, that is the pipeline, which will make sure these changes are in sync with your EKS cluster on AWS, Kubernetes cluster. The next repository will be vprofile-app. That's the name we will give. It will contain the vprofile application source code, the Java source code. And the third one will be vprofile-helm repository. This will contain the Helm charts and Argo CD manifests, which Argo CD will keep checking and make sure the application configuration is in sync with the Helm charts that you have defined in the Git repository. So let's get started with understanding every part of this, starting with vprofile-app. And before this, let me make this thing absolutely clear. We have three Git repository here, vprofile-infra, vprofile-helm, and vprofile-app. In this we have two pipeline. One will be for vprofile-infra source code, the Terraform pipeline. That will be managed by GitHub Actions. The other one is vprofile-app. Here also will have a GitHub Actions pipeline. So three repositories, two pipeline. This will be just a repository, vprofile-helm repository, because Argo CD is going to fetch the changes and apply. Okay, so let's get started with this one. This one we did so many times, the same CI/CD pipeline. This is a continuous integration pipeline and continuous delivery also. But delivery will be for the images. So in this, the first part, so you know in Git repository we have branches. There will be one main branch, generally we call it as main branch, and developer will create other branches where they'll write new feature or new bug fixes. So let's say developer created a feature branch, has written a feature and committed the source code into this Git repository. Now, this will not do anything. This is not going to trigger any pipeline. When there is a pull request to merge the feature branch with the main branch, whenever there is a PR, pull request, then this pipeline will get triggered. It's going to do the same thing, build and execute unit test, do Checkstyle code analysis, do code analysis with SonarQube, upload all this result to the SonarQube server, which will be an EC2 instance in our case. Here, quality check will be done and the response will be sent back to the Sonar, sorry to the pipeline. Once the pipeline receives that this quality is good, this pipeline will stop. So pull request will complete the continuous integration pipeline. When this is approved and when this is merged to the main branch, so when the pull request is approved and it is merged to main branch, which will be only after there is a good quality of this code, then when the request is merged, then it is going to build the Docker image from the source code and push this Docker image to Amazon ECR. So this new image will be with new tag and this tag information will be updated in the Helm chart. In the Helm chart we'll have values.yaml file. So this is a Helm repository. You have charts folder in that. In this you have vprofile folder. And in the charts, here you have values.yaml file. So this is another repository, Helm repository. In this values.yaml file, the image name and the image tag will be updated. So the image that was pushed to ECR, the same image name will be updated into this Helm repository. And there are also Argo manifests that created an Argo project that is maintaining your vprofile application. This Argo CD controller will be continuously watching this Git repository. In our case, it will be checking the Helm charts. If there is any difference, it is going to apply that difference to your application, like updating the deployment of your vprofile application. So this is the whole flow. Terraform, we are going to get into this once again later in the course when we write the Terraform source code, that will be the at the end. So this will be our steps. First we are going to create these three Git repositories. Then we put the Helm charts into the Helm repository, and there is no pipeline over here. Once we have that ready, once we have the Helm chart, the values.yaml file, everything ready, then we are going to start working on our vprofile application pipeline, which is going to test the source code, build the image, update to ECR, and then at the end update the values.yaml file over here. Once we are done till here, then we are going to start writing the Terraform source code, create our Kubernetes cluster, deploy our Argo manifest, and then everything will come into the play. So in next lecture, we are going to create these three Git repository and sync it with our local system. See you in the next one.
