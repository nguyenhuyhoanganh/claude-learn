# Helm Charts Part 1

Hello and welcome. In this lecture we are going to generate health charts from our Kubernetes definition files of our vPro profile application. Now, we did this already. We generated health charts previously for our V profile project from Kubernetes definition file, it will be same, and let me clearly tell you this project is not about writing every line of code, this project is all about designing, architecting. Once you understand the entire flow of GitHub's pipeline, you can work on any GitHub's project. So most of the time we will be using AI assistance to generate our code, which is health chart in this lecture and moving forward, we will be also generating Terraform source code. There's also one more reason we are not writing anything in this project, that is because we have already done this manually and through AI as well in previous lectures, in previous section, like we generated health charts in Kubernetes section. So we already know how to do that. So open your browser and go to our hkh coder repository, github.com/hkhcoder/vprofile-project, search for the branch kube app. Here we have our Kubernetes definition files in this folder and let's download this entire branch. So click on this dropdown, on code, and say download zip. Download it. Go to your folder where you have this downloaded and extract this. Extract all. Okay. In this extract, you will see here kube devs folder. Copy this and keep it in your health repository. So mine is in Desktop, GitHub's Project, Health and paste it over here. Go back to your VS code and you should see this folder. So we have the secret file, which creates the secret, we have the deployment file for our Tomcat application, ingress information service, same way DB deployment, database PVC, this will create an EBS of volume. We'll see all this, how we are going to do that, but instead of this plain Kubernetes definition file, we are going to generate the health charts. So same definition, same manifest. Instead of the plain manifest, we will be having health charts and that health chart we are going to generate with an AI assistant, we can use GitHub Copilot. So go to extension. If you do not have Copilot, search for GitHub Copilot and say install if you do not have it installed, if you have this already signed in, well and good. If not, then go to this place over here, the bottom right. Here, if it is signed out, click on sign in to use AI features continue with GitHub. Now this will open in default browser. I'm just copying the link and going to a different browser where I have already logged in with this account and I'm going to see continue for devpps4sure account. Open Visual Studio Code. Once I'm just going to close this and start it once again.
