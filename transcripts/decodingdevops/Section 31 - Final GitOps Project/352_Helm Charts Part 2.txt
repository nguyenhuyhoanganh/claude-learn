# Helm Charts Part 2

So we are going to use a GitHub Copilot to generate our Helm chart from this Kubernetes definition files. And we are going to use a prompt, and this is the prompt that we are going to use. Now, it took me some time to generate this prompt and I have generated the prompt from the GitHub Copilot itself. So the way it works is we give a prompt, we get some files, and based on the requirement we give new prompt, we keep chatting until we get finally to a point where we have the right configuration, the right code that we're looking for. Once we have that, we can ask the AI agent to generate a prompt for this discussion that we had so I can generate it at once. So that's how I generated the prompt. I made some more changes in the prompt, so there's no guarantee that this prompt is going to generate exactly same as it generated for me. So use this prompt in GitHub Copilot, see if you get right kind of code. If there is any issue, you can directly use the code, the Helm charts from this lecture resource and put that folder in the Git repository in the VProfile Helm repository. Okay, let's take a look at the prompt. So it says, "Create a Helm chart from Kubernetes manifest in the kubedefs folder." So we have the kubedefs folder where we have all the Kubernetes manifest. From this we want to generate the Helm chart. Requirements: the chart name should be vprofile. Folder structure: there should be a folder called Helm inside that vprofile. This will have the charts. Now, we wanted separate file for every resource type, for app-deployment, a separate file, for db-deployment, separate file. Likewise for memcache and RabbitMQ. For services, you want a different file. For ingress, we want a different file. For secret, so we mentioned all the different file that we need in the template. Then, the most important file here is the values.yaml file. This is what will be updated automatically from our continuous integration pipeline. So as per our requirement, we need separate section for app, db, memcache, RabbitMQ, initcontainers, ingress. Ingress, we will be using AWS ALB ingress, not NGINX ingress. AWS ALB Ingress Controller we'll be using. So separate section of variable for this, for secrets, for dockerregistry. So by default, we are going to keep this disabled, dockerregistry will be disabled by default, but we can enable it. So we are saying in values.yaml file have separate section for all these variables. It should be one level nesting. So I'll show you a sample values.yaml file that you want to generate. So one level nesting means this app and inside that variable, this is one level nesting, and separate variable section for app, db, memcached like this we want. And in templates, we want a separate file for every resource type: app-deployment, separate file, db-deployment, separate file. This is good for management, so this is what we want to generate, right? So these are the variable and this is where the variables are defined, like replicas. So here, Values.app.replicas, that is the value of replicas. So we are most interested into this app image name and image tag, this will be default image name and default as the tag. This we are going to update with our continuous integration pipeline. Okay, so coming back to our prompt. So variables, these are the common variables that you should use, so this is what we are telling it to the GitHub Copilot, right? So these are common variables to use: image, tag, replicas, containerPort, servicePort, storageClass, storageSize, defaultUser. For ingress, you want these variable: ingress enabled, true or false, the host and the servicePort. Now, I know this will be too much to understand for now. Once we generate the entire Helm chart, you'll understand it, but you won't be able to write all the prompt at once in real time, it will be one by one. Like you will give this prompt first, it will generate the folder structure, you will say my variables should have different section like this. So one by one you're going to give one line at a time in real time. But here, we are being a little time conscious so we're doing everything at once. Okay, so let's complete this. So all image tag must be default, which is latest, no empty tag. Sometimes it gives empty tags so I've given here that no empty tag values, there should be some tag, otherwise the Helm chart will fail. Image name and tag should be separate variable. I used to generate this prompt and then it used to give image and tag at the same line so I said, "You know, give it separately." db.storageClass must be gp2, so that I'll show you. So in our kubedefs file, we have the pvc file, this is what create the PVC, and here the name of the PVC, PersistentVolumeClaim, this is the name. The storageClass is default in our kubedefs file. But when we are going to create this infrastructure on AWS EKS cluster, we want it to be gp2, so that's what we are mentioning when we generate the Helm chart, Helm chart values.yaml file. db section storageClass should be gp2. We can also mention the storageSize, so db., I'll just say db.storage size should be 3Gi. Okay, I told you we will be using AWS controller, AWS ALB controller, so ingress must use AWS ALB controller with annotation, and these are the annotation that we want. Let me just separate this. Kubernetes.io/ingress.class name should be alb, scheme should be internet-facing, target-type should be ip, and ingressClassName should be alb. So when we set up our Kubernetes cluster, we are going to install an ingress controller called as AWS ALB controller in that. If you want to use that when you create the ingress file, it should have the class name as alb, scheme as internet-facing, target-type should be ip, and ingressClassName should be alb. If you give this annotation in the ingress file, then it will use the AWS ALB controller, it will create a load balancer, which the users can use to access your application. Now, this has nothing to do with the Helm chart, this is all about the AWS ALB controller. You can put this information later also in the Helm charts. You can think of this as the project requirement. This is nothing specific to vprofile project or this lecture or Helm. The real-time project you're working on might be using a different controller, so you need to give that kind of information over here. Once again, this is not going to happen overnight, you need to spend time with your project, understand the requirement, do testing and then slowly, slowly you come up to this information. And you know what? Let's just copy this prompt and generate the Helm chart, then we can again talk about this. Okay, so open GitHub Chat, GitHub Copilot Chat and paste your prompt over here, hit Enter. And it's going take some time, I'm going to pause the lecture recording and resume when this is completed. Okay, now let's take a look. It's completed, I'm going to click here on Keep. So you should see a folder called helm. In that, you should see folder vprofile, that is the name of the chart. Here you have all the templates. In values.yaml file, you should have this kind of content. Now, I'm not giving any guarantee that with this prompt you'll have exactly same charts as mine. And in order to properly execute this entire project setup, it's better for you that you delete this helm folder, download the helm folder from this lecture resource and copy it in your helm repository. That would be better. Otherwise later also you can do this once you're executing, once Argo CD fetches this charts and deploys on Kubernetes cluster. And if you're getting any issue, that time also you can do that. But anyways, I want you to verify a few things like the structure of values.yaml file should be exactly as you're seeing, app.image should have the image name, tag should have the latest tag. So it should be in the same format, same way for everything. And while scrolling down, you should see here ingress enabled false, make it to true, save this. Make sure you do Ctrl + S because we need ingress, we are going to set up everything through ingress controller access our vprofile application through a load balancer so you should enable this. In host, you make sure you give vprofile., and then your domain that you have. I have domain from GoDaddy, and my domain is hkhinfotek.xyz. You make sure you give your domain. If you do not have a domain and if you don't want to get any domain, you can keep this example.com or anything later when you are actually accessing the application through the browser through a URL, that time you can skip and just watch the lecture. But if you have a domain, make sure you give your domain over here, vprofile. your domain. A few other things to verify will be db, storageClass gp2 should be here. The next thing to check is the ingress.yaml file. Now, we have seen ingress, ingress v accessed through a load balancer. Now we are doing this setup on AWS so we will be using AWS Load Balancer Controller, which we are going to see when we set up load balancer for Argo CD. But if you want to use AWS ALB controller, we need to provide some annotations over here. In this section, you can remove all these annotation and from the document in this lecture resource, you can copy this annotation and paste it over here. So ingress.class, alb, that will be the name of our controller, scheme: internet-facing, target-type: ip address, and certificate-arn, here you need to provide the ARN of your public certificate on AWS that we have created. In this course project side, we created a public certificate through the domain, we have verified it. So in AWS, go to Certificate Manager service, you should see your certificate. If you have not set it, you can ignore it now, later also you can do it or later you can also skip this part as well. But if you have a domain, if you have the public certificate, then go to the public certificate, copy the ARN and replace this ARN, this is mine, so replace this with your ARN. Now, this way it will make sure that when we access it through HTTPS, it will use this certificate. Listen-port 80 and 443, we are keeping both HTTP and HTTPS as default ports. Backend-protocol, this will be HTTP or Tomcat application, which will be deployed with this app-deployment file. It creates that part and that listen on default port, I mean to say default protocol HTTP and not HTTPS, so make sure this is HTTP. You can just copy all the annotation from this lecture resource document and put it over here, just replace your ARN and you will be good. Save this. Once again verify values.yaml file, compare it with my values.yaml file, or better you delete the folder over here and use the helm folder that is in this lecture. What you need to do now is once you're done here, you need to make a Commit &amp; Push. There should be on the main branch. Now, I like it doing through the prompt, I'm sorry, through the terminal, I'm going to open Git Bash. I'm going to go to the Desktop. gitops/vprofile-helm. Clear the screen. I'm gonna check git status, and I'm gonna say git add., git commit -m generated helm charts and git push origin main. And that's it, we're done here. This is what we wanted to do in this lecture. In the next lecture, we are going to do some practical sites for our next repository and pipeline, so join me in the next one.
