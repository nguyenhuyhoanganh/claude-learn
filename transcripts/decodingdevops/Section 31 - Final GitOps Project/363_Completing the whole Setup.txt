# Completing the whole Setup

Okay, so everything is settled for me. I have the ingress, all the other pods and the services running. So I need to get the ingress endpoint so I can add the same information in my GoDaddy account. So I'm going to get the endpoint of the ingress, which is this one, host name you see there. This is the endpoint. I'm gonna copy this. Or you can get it through the command line also. You can say kubectl get ingress -n vprofile, kubectl get ingress -n vprofile is the namespace. So you should get the endpoint that points to the load balancer. We need to add this against the vprofile host name. So I'm in my GoDaddy account, I'm in my domain and I'm going to go to DNS, DNS records, add new record, type CNAME, vprofile as the host name, value will be the endpoint, the load balancer endpoint. Save this and wait for like 5, 10 minutes until this record is published. Then we can exit from the browser. So by the time you're waiting, you can check the status of everything. Also bring up the sonar server. We are going to see the complete flow of our CICD pipeline. So once this is settled, what we'll do is we'll check the pod, the DB pod, sorry, the app pod. We'll see the image and the tag. So here we can see the container image name and it's tag. What we are going to do is once you bring up your sonar server, we are going to make a commit in our app repository and see how everything is pushed forward and finally, our app image is updated automatically by Argo CD. We are also going to see the do's and don'ts in real time. And finally we are going to see how to create Terraform pipeline through GitHub Actions. So take some time, make sure everything is ready, and in a few minutes we'll access our vprofile application from the browser. Okay, let's take a look. So, HTTPS:// vprofile.the domain. Let's check it. I'll write. So the username is admin_vp. The password is admin_vp. So I'm logged in. So my DB pod is working. The DB PVC everything is working. DB is verified. Let's check RabbitMQ. RabbitMQ is also working. Let's check Memcached. Click on all users. Select any user. You should see data inserted in cache. That means the Memcached is also working and next time the data is coming from the caching. So that is all from the deployment point of view. Vprofile application is deployed by Argo CD. It has fetched all the details from the repository, the helm charts on the repository and deployed around Kubernetes cluster. Whenever we make any change in our helm charts, like if you update any image, any port number, anything, it'll make sure, Argo CD will make sure it'll fetch the latest changes and deploy it. And that's what we are gonna test now. So do this, open the app repository from VS code. So I'm going to go to desktop/gitops/vprofile-app. Open this in the VS code. Okay, before we make any changes, I need to make sure I pull the latest changes. So do a Git pull. I know much better what I'm gonna do is, I'm going to just simply delete this repository and clone it once again. So I don't have any conflicts. I need to close the VS code. Okay, let me do it once again. Okay, I'm going to clone this repository once again, vprofile app repository, the SSH endpoint, GIT clone and wait. I need to say DevOps for sure. Now github.com-devOps for sure. That's what is in my SSH config file. Okay, my repository is cloned. I'm going to get into the repository. And I'm going to switch to the branch, feature-x, I think, yes. And then I'm going to open this into VS code. Okay, so I'm in feature X branch and when I make any commit over here and push it to GitHub, it's not going to run the pipeline. Our pipeline is designed to run whenever there is a pull request to the main branch and when there is a push to the main branch. Okay, so just make sure the sonar server is up and running. Copy its public IP. Go to vprofile app. Settings, we need to update the sonar server URL. Go to secrets and variables, actions, variables. Edit sonar host URL. Make sure you change the public IP to the latest one and save it. You need to enter the password of your GitHub account once again. So we are going to test this whole flow now. Whenever we make a commit, nothing happens when we raise this a pull request. When we raise a pull request, right, when we raise a pull request, then the code analysis it happens. If the quality gate is good, then it'll push the Docker image to Amazon ECR, and then it's going to update the image tag into the helm repository. Now whenever there is a change in the helm repository, the Argo CD controller will make sure it is going to fetch the latest changes and deployed. So this is the whole flow. Now we are going to test. So I'm in feature-X branch and I'm going to make a change into the README file. Nothing serious, just updating the README file. I'm just adding some hash. I'm going to save it and I'm going to commit it through the command line. So I'm going to open the terminal. I'm going to switch to GIT Bash. So git add. git commit -m test pipeline 99, just some name and I'm going to say git push origin feature-x. So feature-x branch, we are making a new commit. Okay, now let's go to GitHub. I mean vprofile app repository. In actions, there is nothing yet triggered. Once we raise a pull request, it's already asking us to compare and make a pull request. But anyways, let's go to pull request and say new pull request. Select the feature-x branch. That is going to merge the changes to main branch. That is a request and see create pull request. You can add some title, give some description and say create pull request. Now this PR should trigger the pipeline and code analysis should happen. Now if I go to actions, I should see the pipeline got triggered. So it's going to run different stages and let's wait until this is completed. All right, so our code analysis got completed successfully. Quality gate is good. Now it's time to merge these changes to the main branch. Before you do that, let's take a note of the app image tag. Take a note at this tag or it's better from here, yeah, or you can keep this tab open and another and open another tab. Anyways, take a note of the tag because that should get updated. So now I'm going to go to pull request. That's the open pull request. And let's merge the changes, merge pull request. So some authorized party is going to look at the pull request. If everything checks out, fine, he or she's going to confirm the merge. And when the merge happens, it's going to build a new Docker image, push it to ECR. Let's go to the action, sorry, actions, yeah. See that the merge request started. Building the docker image, push it to ECR and then it's going to update the helm values file into helm repository. So let's wait until this is completed. Or you know what? By the time to open the helm repository, vprofile helm. Okay, so that's the current tag and I'm going to wait until this is completed. Okay, new image is built. Let's check whether it updated the tag. Yes it did update the tag. And let's go to now the Argo CD. This is the current tag. Let me refresh this. Let's see what's going on here. So the thing is updated yet, let's wait for a few minutes, or you can click on sync and sync the latest changes. Oh, you know what? Let's just click on sync, Synchronize. Okay, there you should see, vpro app two seconds ago. It's creating a new pod. And once that is ready, it's going delete it. Okay, this pod is healthy, so it's going delete the another pod, the older pod. Let's check and you should see the latest tag in the new pod. So that's the whole pipeline we have tested. Now, based on your requirement, things might change and you might need to put another pull request in this whole pipeline. We are gonna talk about that in the next lecture. And also going to talk about the Terraform pipeline. So see you the next one.
