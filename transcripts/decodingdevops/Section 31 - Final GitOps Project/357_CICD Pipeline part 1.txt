# CICD Pipeline part 1

Welcome. In this lecture we are going to create GitHub actions pipeline for our repository vprofile app, which is nothing new, except here we are going to at the end update the values.yaml file in a different repository. Apart from that, everything we did previously as well. And we have to do it the right way. We have to make all the changes in a different branch, not the main branch. So open your Git Bash or terminal and go to your repository. Mine is in a desktop gitops vprofile-app. You should be in the main branch. You need to create a new branch. Git branch -c -x, that's the name we are giving. Assuming that the new feature developers are writing in a new branch. Check out to that new branch. Git check out feature -X. Open the VS code. Make sure you are in feature -X branch. So this is the prompt that we are going to use in GitHub Copilot to generate our GitHub actions pipeline. So we have a Java Spring MVC application built with Maven and we need a GitHub action CSCD pipeline with the following details. So first we are going to push the feature branch no pipeline should run. When the pull request is raised for the main branch then it should run Maven build, unit tests, Checkstyle, SonarQube scan, SonarQube quality gate check, and it should block the merge if the quality gate fails. Quality gate will be in SonarQube server. The server will send the information to the pipeline whether it's passed or fail. If it's passed, then you can merge it to the main branch. So when we merge, it should build a docker image pushed to Amazon ECR with the commit ID commit SHA and also the latest tag. Both tags should be there for the image. Then update the image and this tag in the Helm values.file. Values.yaml file in separate GitHub repository. So we have defined the entire flow, which is good. Now details it should use Java 21, Maven, Tomcat WAR Packaging. SonarQube we are using self-hosted on EC2. We are not using SonarCloud. Now there is a reason we are not using SonarCloud because there are too many restrictions on free account. We cannot operate it properly for our project test cases. So we are using EC2 instance. And we're asking it to use these GitHub actions. These are readymade GitHub actions that you can use. This one is popular for SonarQube scanning. This one is popular for SonarQube quality gate check. And we are saying use this existing sonar-project.properties file. Right, this one which has all the details for which file to push. docker file location, which docker file to use to build our app image, which is in this location. We have seen this already. Let's see it once again. docker file, app, multistage, this docker file to use to build the app image. Nothing new. We have seen this previously. We have written this. ECR repository name. This is your ECR repository name. Region, US-East1. This is not required where anyways using the variables, you don't need to parse it also. Helm repository name. We have given values file path, Helm/vprofile/values.yaml. Let's take a look whether this is the correct path or not. So open GitHub and go to your GitHub account. Go to vprofile-Helm/vprofile. In this you have values.yaml. So this path you need to mention helm/vprofile/values.yaml, which is right in the prompt. Okay, structure of the values.yaml file. Important, you need to supply this. That image name will be something like this. Tag will be something like this, so it knows how to update it. Here we are saying that values.yaml file has an app.image, which is this one. An app.tag field that need to be updated using yq. And there's one way you can use cell Linux set command that is too complicated. Yq is yaml editor for search and replace. So it will create, it will download yq and use yq to you know, edit this yaml file. Also mention the secrets that we want to use. Sonar token, access key, secret key, Helm repo user, GitHub's part. Make sure this is the same secret name that we have kept. Also we have Slack. At the end also, we have mentioned the secrets and variables that we have defined in GitHub. So it will know that it has to use these variables and secret. So final ci.yml structure will be this. This will be the pipeline file. Triggers pushed to main, docker plus helm job will run only. If there's a pull request for the main branch the sonar job will run. You can go on and on. You can mention as much as detail as you want or you can do it one at a time. Well, when I get started, I do one at a time. I come to a point where I like the pipeline. This is a working pipeline. Then I ask it to give me a prompt which I can use later to generate the same pipeline. And every time it will make one or the other mistake, which is fine, expected. It cannot literally get inside our brain and look at all the configuration of our brain. So that's fine. But yeah, this prompt will bring you very close to the actual pipeline. But still, use the pipeline which is in this lecture resource so you get a working CICD flow quickly. Later, you can do it yourself manually also. But for the sake of completing this project, use this prompt. Okay, so I'm just copy pasting the prompt over here and it's going to take some time. Let's wait. Okay, it's asking me some question. It wants to check this SonarQube scan action from GitHub. I'll say go ahead, check. So it's going to take some time. Let's wait. Okay, it has generated the pipeline flow. Let's say keep. Now let's take a look. Ci.yaml, it added something, roles to assume. We don't want to use roles. Let's remove it. Oh Slack we have missed. Let's add it later. Let's see in the pipeline what all it did. So this pipeline will run whenever there is a pulled request on main branch and whenever there is a push to the main branch, which is fine. Okay, the variables, I don't know why it again, saved the variable. This is not required. You can directly use this variables anyways. Sonar host URL. We need to create this one, later we'll do before we run the pipeline. Okay, so the job built and build and sonar job. So there's a condition if the GitHub event name is pull request, that means when we raise the pull request this is what it's going to do, it's going to check out the source code, good, set up the JDK 21, nice. Cache. Now we have included something in the prompt that will keep the cache for Maven and sonar so or build becomes faster. So that's why it added these two steps extra over here. Okay, next is this build run unit test and generate checkstyle reports. So checkstyle output format, the file part. Okay, it really made it complicated but clean. I will make it much cleaner. We just want the simple options. Hyphen B is fine, so this option -B is batch mode. Let's keep this one. This prevent it from putting too much extra information like downloading this, downloading that. So I'm going to remove all this. And just add this, so I'm being clean. Verify checkstyle:checkstyle-B. Plain and simple. We know it generates the checkstyle-result.XML file. Okay, so that is going to generate the target folder and all the reports in that. Then it's going to use the SonarQube scanner. This action, to scan our source code, all the information is in the properties file. So this is the token it is using and sonar host URL. This variable we need to mention in GitHub variables. Then it is going to use this action to do the quality gate check. Again, going to parse the token and sonar host URL. It has also mentioned the polling timeout seconds, 600 seconds which is fine, it's going to be completing before this. Okay, so this is still pull request. Once the pull request and this quality gate checks out fine, then when you try to merge to the main branch, you see that? Event name when it is push, and this will happening be only for the main branch check over here. This pipeline will only work for the main branch pull request and main branch push. So here event push is for the main. Then it is going to build the docker image, output ECR registry and image tag. This will be the output when it's going to print this output. And this is the image name and this is the tag. So step, check out the source code, configure the credentials, region, access key area and secret access key. Create ECR repository if needed. It's not required. The step is not required. We have the ECR repository. Okay, anyways, this is going to describe the repository, if it exists, then it is not going to go to the next one. Yeah, there is an OR operand over here. So if this works out fine, it's not going to go and create the repository, which is fine. Okay, the last one is build and push, not last one, the second last. It looks too many commands, but it's pretty simple. It's going to set the ECR registry name, image tag, which is going to be GitHub SHA commit ID, docker build commands mentioning the path of a docker image and the tag that needs to be mentioned. So ECR registry/ECR repository: the image tag. And tag again with the latest, I think. Yeah, with the latest tag. Then AWS ECR login. Push the docker image with this tag, which is the commit id, and push again with the latest tag. And it's just going to echo this information, print this information. Okay, finally update helm. So if the event name is push and also says it needs the docker build and publish this one. Once this one completes, then only it's going to start these steps. So check out the source code, install yq. Clone the helm repository. Yeah. Then use yq to update the app.image and app.tag as per the information. Yeah, this is wrong. It should not be like this over here. I'll talk to Copilot about this one. So initially in values.yaml file, this will be the name of the image and the tag could be anything, but next time it will not be Vprofile containers, vprofile app, it will be the easier information. Then how is it going to find it? The app.image value could be anything, not just this one. Take care of this. And also app.tag also could be anything. Okay, let's accept this. Let's see what changes it makes. Okay, moving on. So the next one will be mentioning this information, the values.yaml file. So it needs to commit and push the changes to helm repository. So it sets the username, sets the email address, say git add dot, git commit, git push. And that is it. Let's save this. Okay, in the next lecture we are going to commit this into the feature branch and then raise a merge request or pull request to the main branch and see how this pipeline works.
