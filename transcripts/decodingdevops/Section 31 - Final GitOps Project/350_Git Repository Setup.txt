# Git Repository Setup

Welcome. In this lecture, we are going to create three repositories on GitHub and set up authentication with our local machine. So you need to have a GitHub account, you need to have a VS Code installed. In VS Code, you need to have Amazon Q extension and make sure Amazon Q is signed in with your AWS account, or you can create another account in Amazon. You should have Git Bash on Windows and Terminal in macOS with Git installed. Log into your GitHub account, click on New. Give repository a name, vprofile-infra. Now this represents there will be Terraform source code to manage your EKS cluster on AWS. Visibility will be Private. README file and gitignore file, this one will generate once our source code is ready, so click on Create repository, go back, click on this GitHub symbol. Let's create the second repository, New, vprofile-helm, Private, Create repository. Third one, New, vprofile-app. This will contain the application source code. Once again, change it to Private and say Create repository. Now, open your Git Bash or Terminal if you're using macOS. Type cd and hit Enter so you are in your Home folder, then do cd .ssh/, come to this folder and run the command as such, keygen, hit Enter. And here give the name of the repository, sorry, of your GitHub account. So mine is devops4sure, so I'm giving this. Hit Enter, Enter, Enter. Do ls and you should see here one public key and the other one private key. Public key, we are going to store in our GitHub account, but first we are going to create some configuration in the config file, so do vim config, open this file. So add this entry if you do not have. You can get this from this lecture resource, so I'm going to say I'm going to go into insert mode and github.com hyphen, my account name, devops4sure. And here you need to change your key name exactly as it is. So mine is devops4sure, so I'm giving this. Now, this will make sure when we clone our GitHub repository in the URL, we are going to change -devops4sure. When we do that, when it matches this name, then it is going to use this private key to log into your GitHub account. Save this file. Once again take a look. The part of the key, you check the name. Now, do a cat to your public key, so mine is devops4sure.pub, so that's my public key so I'm going to copy this. And go to your GitHub account, go to the Settings, SSH and GPG keys, say New SSH key. Give a title. I have given my workstation and paste your public key over here. This name can be anything. Just click on Add SSH key. Make sure this is the public key and not the private key. Public key look like this. So this is the lock we are putting in our GitHub account, Add SSH key. Now take your repository one by one. First I'm going to go to vprofile-infra and click on SSH, you should have this kind of URL, not HTTPS, SSH. Copy this, open your Terminal or Git Bash, cd, Enter, and go to the folder where you want to clone it. So I have a D drive. You know what? For everyone's sake, I'm going to just go to Desktop. I can do it like this ~/Desktop/, and here I'm going to create a folder called gitops. In this folder, I'm going to say git clone and paste that URL over here, Shift + Insert in Git Bash. And you need to change here hyphen, the name that you have given in the configuration file. So mine was host github.com-devops4sure, so I've given that and I hit Enter and that worked. Now, if this is not working for some reason, do this cat ~/.ssh and check your config file content. You see this, this should match github.com-, the name you have given should match with this. If that matches, then it is going to use your key. Okay, now let me clear this screen and clone the other repository as well, git clone vprofile-helm, that is the name. The last one, third one, vprofile-app. You can clone it in any order you want. So at the end of this lecture, you should have these three repositories cloned. Then you get into vprofile-helm, code ., this is going to open this in the VS Code. I trust this author. Okay, this is it for now. In the next lecture, we are going to develop helm charts for our vprofile application, so join me in the next one.
