# EKS Prereqs

Hello and welcome. In this lecture we are going to complete the prerequisites for the setup of EKS cluster with Argo CD. The idea is to write the Terraform code, which spins the EKS cluster on AWS, and also make the configuration changes on the ES cluster so a vPro profile application can run on it. So let's take a look at the prerequisites and then we'll start doing it. So we need a domain which should have already set by now. If you do not have a domain and if you don't want to take one domain or purchase any domain, it's fine, you can watch from here because our V profile application we are going to access it through a proper H3PS URL, secure URL and also Argo CD we are going to access it through secure H3PS URL. And for that you need a domain. So minus hkhinotek.xyz, and I have taken it from GoDaddy. You can do it from anywhere else. I have shown in the political sites of this course how to purchase a domain which is cheaper, and then setting the AWS public certificate for this domain, which is completely free. So I have this domain and I have the public certificate for this domain. Once you have that, you need to create an IM user with Administrator Access policy. This will be for the Terraform code that we will be running from our laptop or on from our computer. We need to run AWS configure to set the access key and secret key in our local machine. So Terraform Pop Pipeline, we are going to set at the end. First we're going to execute the Terraform code from our system and set up the cluster. And in order to do all that execution from our local machine, we need a few tools. So if you're using Chocolatey or Homebrew, based on your operating system, you can install these tools. AWSCLI, Terraform, Kubernetes-cli, Kubernetes-helm, this is for Windows. And one more tool you need to install in Windows eksctl. Same for Mac is also in Macs you're going to just use one group command and install all these tools. In Windows eksctl we are installing with Scoop, all the other tools we are installing with Choco. So let's get started. Let's install these tools. Okay, I'll give this list and all these commands in this lecture resource, please follow them. Same document I'm going to share in this lecture resource. So for Windows, copy this command and open your PowerShell as administrator and then execute all this command. It's gonna take some time to install all these tools. Once these tools are installed, then you need to execute this command. With this you'll get access to Scoop, and then you can say Scoop install eksctl. For Macs just copy this command through install and everything will be set. So take your time, pause the lecture and complete install all these tools. If all the tools are installed, then create an IM user with administrative policy and download the access key and secret key. So go to IM service, say create user, give user a name, attach policies directly, administrator access, generate the access key and secret key. Go to security credential of the user, create access key, CLI. I understand. Next. Now you need to be extremely careful with this access key, it has administrator privileges. So only download it in your machine and save it and keep it in your machine. Once you're done, just delete this key, delete the user. Be very careful with this. Open your access key and secret key, execute the command AWS configure. Set the access key and then the secret key in the region, US east one. That's what I'm using, so set your own region. Okay, I have set my access keys. Now I'm going to open my repository. So you also do the same thing. Go to your repository, vPro profile hyphen infra. So mine is here V profile hyphen infra at this location. I'm going say code dot. This opens in the EVS code. In the next lecture, we are going to write the Terraform code by using GitHub copilot, so join me in the next one.
