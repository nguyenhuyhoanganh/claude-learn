# Sonar Qube Server

Okay, let's initialize the source code first. So let's Open Folder in VS Code and select vprofile-app. (notification chiming) In previous lectures, we downloaded vprofile-project-kubeapp branch. From here, you need to copy src, pom, Docker-file. Copy this, And my location is in Desktop, gitops, vprofile-app, and paste it over here. In VS Code, you should see these files. Don't need to commit anything now. We're going to do it later. Let's launch an EC2 instance for a SonarQube server. So go to your AWS console and go to EC2. Also, in a similar tab, open our hkhcoder repository and switch to branch electron. This one, we used in the Jenkins lectures, and this, you have userdata. Here, you have sonar-setup.sh. Just keep it handy over here. We're going to copy this one. Go back to your EC2, say Launch Instance, Launch without a walkthrough. Give a name. SonarServer, I'm giving. Select Ubuntu. Keep open to 26. Open to Server 26 (HVM), SSD Volume Type, scroll down. Instance type will be bigger, t2.medium. Needs at least four GB of RAM. Create a new key pair. It will mostly not be required to log in SSH to the sonar server. But anyways, let's just create it. I'm giving name as SonarKey pom. Create key pair. Network settings, Edit, and Create security group. Let's give security group a name, sonar-sg. Let's give same in the description. And in the rules, you need to add 22 from your IP and port 80 from anywhere. GitHub Action, which is having the pipeline, is going to upload the result to the SonarQube server. SonarQube server, we have engineers that is listening on port 80. And the request is coming from the internet, so we need to allow it from anywhere. Don't worry, we have the authentication for SonarQube server with the token. So it's not going to be like anonymous login. It will be with authentication. Now, in Advanced details, scroll down. In User data, you need to copy this and paste it over here. Script will execute and set up the SonarQube server, same thing what we did in CI/CD. Launch Instance. This is going to take some time. You need to wait, like five minutes or so. Once the SonarQube server is up and running, we are going to log in, create a token, and then, in later lecture, we're going to store these tokens as secret in GitHub. So I'm going to pause the recording and resume when this is completely set up. Okay, I'm resuming after 10 minutes. Let's copy the public IP of SonarQube server and open it in the browser. So the default username is admin and default password is also admin. Now, I need to reset the password. Give the old password, admin, and give a new password. And Update. Okay, now, open sticky note or any text editor, text pad, where we're going to store a few details, like, first, we're going to store the token. In the next lecture, we're going to store the keys and other thing. All this information, we're going to store in GitHub Secrets later. So here, go to Account, go to My account, click on Security. And in the token, give a name. I'm going to call it as actions. And select token type as User Token, expires in 30 days. Say Generate. You'll get a new token, copy it, and store it somewhere, or do not close this tab. I'm just keeping it in the sticky notes. Now, open your VS Code. And over here, create a new file in this top level location of the folder. File name will be sonar-project.properties. And the content will be this. I'll put this in the lecture resource. So you can simply copy, paste. Now, in previous lectures, we have put all this information in the pipeline itself. This time, we are separating it. The sonar.projectKey, so vprofile-app, that's the name I'm giving. ProjectName, also vprofile-app. Sonar.projectVersion, 1. The source code to scan is the src folder. Binaries is located in target/classes folder. Once we build it with Maven, we'll get this folder. It's the same thing what we did previously, the surefire-reports path, jacoco path, checkstyle path. And here, we are going to put, oh, no, we're not going to put this, just remove this. Only this much is enough. Save this file. And let's commit all this in the main branch, then we're going to continue to the next lecture. So I'm going to do it through Git Bash or Terminal. Okay, so make sure you are in this folder and switch to vprofile-app folder. and say git add . Git commit -m "init". So initial commit like that. And then, git push origin main. And that should be it. We have everything that we need for the sonar. We have the source code here. We have pom.xml file to build it with Maven. We have sonar properties file. We have the sonar token. In next lecture, we are going to create the ECR repository and set up authentication for that by using access keys. So wrap this up and join me in the next lecture.
