# Argo App Sync

Okay, so our Argo CD is installed. We have an Argo CD controller in that. That is going to fetch the Helm charts from this Helm repository. And then when it runs our application pod, the Tomcat web profile application pod, which is stored in Amazon ECR. So the EKS cluster will be fetching or from the EKS cluster, that docker pull command will get executed. And EKS cluster needs permission to access Amazon ECR. So two things. We need to give Argo information about the Git repository, and also in the EKS cluster, we need to set access to Amazon ECR registry or repository. So Git repository we are going to add from the argocd command line. First we need to log into Argo CD from the command line. So this is the command: argocd login and the endpoint of your argocd --username admin. Just make sure you are using your endpoint and not mine. Enter. It's going to ask the password. So I'm going to enter the password. Okay, I'm logged in. Now I'm going to go to the repository, get the repository information. So, vprofile Helm. This repository, Argo CD needs to access this repository. So get the SSH endpoint. So the command will be argocd repo add git@github.com: your account name slash your repository name .git and the SSH key-path. So this is my SSH key path, the private key that we created earlier, if you remember. You can check your key also. So make sure in this command you mention your details, your repository, and your key-path. Okay, this is the same key that we have used to set the Git SS authentication when we are setting the three repositories. Okay, I got this error. I was trying to troubleshoot, I couldn't find any problem, then I tried that command once again and it just worked. You can also add repository from the UI. You can go to the Settings, Repositories, and you can say connect repository and mention all the details, the URL, and you can paste the private key content over here. That we also can do, but you can see that I already have it connected, and it's Successful connection. Okay, now I'm going to attach some policy to our EKS cluster so it can access Amazon ECR. So first get the node group name. So run this command. Or you can go to the EKS cluster and get the node group name from there also. So that's my node group name, vprofile-eks-cluster-node-group. Then find the role name that is attached to the node group. You know what? Let's go from the browser. We go to Compute, node-group. And here you can see Node IAM role, that's the name. So mine is vprofile-eks-cluster-node-role. So we need to add a policy in this role. The policy is AmazonEC2ContainerRegistryReadOnly. You can find this role from AWS console IAM, and you can attach policy like that also. Let's do it through command line. Just make sure you have the right role name. Let's list the attached policies for this role. So we should find this one. Now before we move ahead, let's commit all our Terraform code to our vProfile Infra repository. And then we are going to move to Helm repository. We should generate a gitignore file so it ignores our .terraform folder, and also we will generate the README file. So use GitHub Copilot. So say generate a README file and gitignore file to ignore .terraform folder. Okay, generated the read me file and .gitignore file, which excludes .terraform folder, which is good. And also the log file, which is good. So from command line, get into your repository. Mine is at this part, in the Desktop/gitops folder. I'm gonna say git add . git commit -m "terraform code". And then I'm gonna say git push origin main. Okay, once your code is pushed, the infrastructure code is pushed to GitHub, you can open the Helm repository in VS Code because we are going to create Argo CD manifest and commit it in Helm repository. So I'm going to say Open Folder. So my Helm repository in Desktop, gitops folder. So I'm going to open that. Now you need to fetch the latest changes over here. So you can say Git Pull, or you can go to repository (message chiming) over here and say Sync Changes. Because our pipeline has updated the app image name and tag that happened in the GitHub repository. So we need to fetch the changes to the main branch, or from the command line. Just do Git Pull in the Helm repository. Okay, here we are going to create two manifest. One is for the project, the other one will be for the application. We are going to create two folders here. So I'm going to do it through Terminal again, open the new Terminal, and I'm going to select here the Git Bash Terminal. I'll create the folder called argocd. And in that folder we are going to create two more folders. One is projects, and the other one, we'll name it as apps. Inside projects folder, we are going to create a file named as vprofile-project.yaml. And from this document, copy the project file first. So when we do kubectl apply on this file, it is going to create a project called vprofile in the namespace argocd. Now this is a custom resource of Argo CD, nothing specific to Kubernetes. In spec, we give some description. Source repository, you need to mention here the repository URL, which is the same repository, the Helm repository, because from here it's going to read the charts. So get the SSH URL, mention it there, and in destination you can see namespace vprofile in this Kubernetes cluster. Now this URL means local cluster. The same cluster, Kubernetes cluster where argocd is running, if argocd is going to maintain some other remote Kubernetes cluster, then you need to mention that endpoint over here. Now creating application in Argo CD is always through project. If you do not define a project, then it is going to use the default project. But if you create your own project, then you can set boundaries. So this is the repository and this is the destination. And then you can define the cluster resource whitelist also. Namespaces also. So this way you can define the boundary. Save this file. In the apps folder, create another file, call it as vprofile-app.yaml. Copy this code from apiVersion till the end. Okay, make sure you update the repoURL to your own. This is again the Helm repository. So this manifest is of kind, application is going to create an application. Name of the application will be vprofile. Namespace is argocd. Specification, that's the project name. Source URL, that means it is going to go to this Git repository in the main branch and fetch the charts that are mentioned in this path, and use this values.yaml file. Now if you have multiple environments like dev staging production, you can define different values .yaml file, and then you can mention the path over here. But the charts should be same, should be replicate... I mean you should be able to replicate it. Same chart, but values will be different, like more replicas, more resources, more CPU, more RAM, better, faster, storage, all those things that you can mention in values.yaml file, based on your environment. So destination is the local Kubernetes cluster in the namespace vprofile. syncPolicy: true and true. That means if anyone deletes a resource from the repository, then it'll delete that resource in the Kubernetes cluster also. selfHeal, true. That means if someone changes something in your application, any resource, then it is going to check that and match it, what is defined in the repository. If it doesn't match, then it is going to apply whatever is mentioned in the repository. syncOptions: CreateNamespace, true. If the namespace that is defined over here does not exist in the cluster, then it is going to create it. Save this file also. We are going to commit it later. First, let's create the application, first create the project and then application with kubectl command. So open the Terminal. Let me clear the screen here. Kubectl apply -f first the project. So, argocd. I'm already in argocd folder. So I'm gonna see projects, and vprofile-project.yaml. Okay, before I go to the next one here, the path is wrong. It's helm/vprofile, -chart is not there. Let's confirm that once. In Helm repository, helm/vprofile. Inside that you have Chart and values.yaml file. So make sure it's as per the repository. kubectl apply -f apps, and vprofile-app.yaml. Okay, it says it's created. Let's take a look from the UI. So log into your Argo CD. Let's check in the Projects. We should have project here vprofile, nice. And Applications. Okay, we see the application, it says Healthy already. Status is Unknown. Let's click on this. Oh, okay. Vprofile-chart. I don't think I have saved it. After I made the changes I should save it. Okay, let me delete it and create it once again. Okay, then let me create it. Okay, new progressing. Nice, okay. Already started showing. So db-pv-claim, app-secret. Let's wait until all this is settled. Everything is created.
