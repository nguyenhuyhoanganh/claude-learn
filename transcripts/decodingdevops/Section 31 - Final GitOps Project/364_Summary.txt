# Summary

Okay, so before we begin, just let's make sure that we have the latest change in our local machine. All the repository run git pull, or better, you delete all the repository and clone it again if you have made changes locally and remote both. So I'm doing a git pull on vprofile-app. You see the main hat changes because we have merged to the main branch and that happened in GitHub, so we need to pull the changes locally also, let's also go to vprofile-helm, there also, we need to run a git pull. If you see any conflict, any issue, you don't need to work on it. Just delete the repository and clone it once again. Okay, now let's begin. Actually, I'm not going to do anything in this lecture. I'm going to ask you to do, this lecture is about assigning some tasks to you and make sure you do it. So you need to take care of two things. One is about this CICD pipeline whenever we make a change, okay, so whenever we make a change in the code, we know that it pushes the docker image and updates the values.yaml file. And when the values.yaml file is updated or anything in the health chart is updated, Argo CD is going to fetch and run it. But that may not be the case for you, let's say for your production environment, this should happen with an approval. The Argo should pull the changes based on the approval. So how do you do it? Well, instead of updating the image tag directly in the values.yaml file, you raise a pull request from here to update the values.yaml file. So not a direct change, a pull request. So you need to update this pipeline, vprofile-app pipeline and at the end, instead of updating, there should be a pull request. This is first task you need to do. So once the pull request is created, you need to test it. You need to see that there is a pull request on your GitHub actions in the Helm repository, and then you approve that pull request. Then the Argo will fetch the latest changes. This is the first thing that you need to work on. The second is you need to set up this pipeline, vprofile-infra Terraform pipeline and this is how it should be once you create a new branch in Terraform, in vprofile-infra, it should validate the change and plan the change, validate and plan, that's all. Whenever there is a pull request, validate and plan, let me repeat once again, whenever there's a pull request, validate and plan, and when you merge it to the main branch, then it should give you applied trigger. It should not apply automatically. There should be a manual button where you click on apply and then you should apply the changes to EKS cluster. That means Terraform apply will run only when you click a button manually. So create a branch, raise a pull request. Pull request should run, validate and plan. When it's merged, then it should run apply, not actually run, it should give you a button, you click, it's all doable, it's very easy. When you click on apply, then it should apply the changes. There should be also something like DRIFT detection. It should check the difference. You can do it later. First, you know, complete this validate plan and apply. It should also keep checking your code against the EKS cluster. If there is any changes, then it should give you again, the apply button or apply the changes that you can decide. The third task is to add the Slack notifications. We have created Slack variable, Slack webhook QL, so for these two pipeline there should be a Slack notification. You should get a notification for every changes. You have to achieve all this by using prompt on GitHub Copilot, Amazon Q or whatever code assistance you are using. You create the prompt. As I'm speaking you speak to the GitHub copilot or any code assistance and make sure it is fulfilled. And troubleshoot, when you get the issue, that's how you are going to learn. Once you are done, you need to repeat this, all these lectures, once again, you'll have to create your own document. Yes, you can use the document provided in this lecture resources, but better you create your own document, complete the entire setup twice, thrice, make sure you're comfortable with everything and then only declare this course as complete. So once again, you need to add a pull request here in the Helm repository. You need to create a pipeline for Terraform and you need to add Slack notification. These three things you need to do. Once you're done, once you're good, you can do the follow the cleanup process that I'm going to show you now. Okay the cleanup will be mostly reverse. First, remove the entry from code, add your from your domain register for Argo CD and vprofile is confirmed. If you have no plan of repeating this again, you can delete the Sonar kube server, but I recommend you just stop it and keep it. So whenever you want, you can just power and start you doing your practice, or anyways, it's not a big deal to spin a Sonar server you know how to do it. Before we delete the EKS cluster, we need to delete the controller otherwise there will be load balancer there. So kubectl get ingress -n vprofile, first delete the ingress of vprofile, so kubectl delete ingress, or before that, let me show you, these are the two load balancers. If you check in your region you should see two different load balancer. One is for vprofile, the other one is for Argo CD. Make sure you delete the ingress so it keeps you to your delete ingress, pro-ingress, that's the name, if you have not changed, you'll have the same name, vprofile -n, that's the namespace. For some reason, if you're not able to delete the ingress and the load balancer is still is there, just make sure you delete the load balancer manually from the AWS console, but you have to delete it, otherwise Terraform destroy will also fail, and check the Argo CD ingress. Okay, there it is, kubectl delete ingress, argocd-ingress -n argocd, that's the namespace. Okay, once this is done, you need to delete this service account. So remember this command, eksctl create iamserviceaccount, this is for AWS load balancer controller. We attach a policy, we need to delete this. So instead of eksctl create, you just need to remove the create and update it with delete, delete only this much. Oh, I think I should remove this attached policy also. This is wrong. Yeah, only this much, eksctl delete iamserviceaccount, cluster name, namespace and the name. This will take some time. Okay, this is done. Now we can delete the cluster. This one policy that we have created, if you want, you can delete that policy through the console or from this command, don't need to, policy is not going to create any harm. Okay, now let's me, let me run the Terraform destroy command. Let me go to the repository vprofile -infra, first, terraform init, it's not required, but just to make sure if I have, because I make a lot of changes to the code, so I'm just writing Terraform in it. Okay, terraform destroy. Now keep in mind you just need to run terraform apply. You don't need to go through the whole process of generating the prompt and generating the code eks et cetera. We don't need to do all those things. You can run terraform apply, and from there you can start following. You can continue from this step, eksctl create because this policy will be already there. So you don't need to do anything just terraform apply and then start following steps from here till the end and you get the same setup. But make sure you do the cleanup in the same order that I have shown, and then you can do terraform apply and follow from this step. Every time it'll work. I have tested it multiple times. If you have any doubt, post it in the Q&amp;A, I'll happy to, I'll be happy to answer. Okay terraform destroy, yes, so great job really if you have reached till here, do this project multiple times until you get the complete hang of it. This is a pure gold mine, trust me. This will be a great added advantage in your resume. When you talk in the interview, when you're talking in the project meeting, you will sound professional and confident after this. If you have any problem in executing any step, any command, anything, just take a look at what is the basic that you're missing. It's Linux, it's yaml, it's Kubernetes, it's Helm, and you make sure you practice that and then again come back to the project. But really great job overall. Thank you very much for watching and following it. I wish you all the very best.
