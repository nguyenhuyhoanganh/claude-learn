# ECR and IAM

Okay, so we have SonarQube server set and everything ready for that. Now, we need to create Amazon ECR registry and create an IAM user so pipeline can authenticate with that. So go to AWS console and open ECR. One more thing before you go there, you can power off this instance for now if you're not going to use it, so I'm going to say Stop instance. Okay, let's go to ECR, Elastic Container Registry. Click on Create a repository. Just make sure you are in the right region, same region where you have the Sonar server. It's not mandatory, but still, let's keep same region. So create a repository. Give repository a name. So I'm going to give vprofileappimg, stick to the same name because same thing we are going to mention in the pipeline. If you make any changes, make sure you remember that, that you need to update that in the pipeline. And just say Create. That's all. Vprofileappimg, I-M-G. Now, this is the URI. Let's copy this and put it in the sticky note. Not mandatory. We can anytime go and access this one, but still let's keep it. Okay, so in order to authenticate, we need an IAM user, which will be used by our GitHub Actions pipeline. So go to IAM, go to IAM users, and say Create user. Give user a name. I'm giving GitHub Actions. Not mandatory. You can give different name also. Go Next. Attach policies directly. Now, this user will be used by GitHub Actions to upload your Docker image to ECR. So we need to give right policies. Just search for registry over here. Select this one, AmazonEC2ContainerRegistryFullAccess. This is one policy. Then search for EKS. AmazonEKSClusterPolicy. This one, put a check mark on this. So this ECR registry will be accessed from the ES cluster. So we need to give this policy. Once you select this policy, these two policies, check once again, say Create user. Okay, now click on the username, go to Security credentials, and say access key, Create access key. Select CLI. I understand the above recommendation. And say Next. Create access key, and make sure you download this. Download a CSV file. Now, keep it here. Don't need to copy this one in your text pad or sticky note. We have it in our system so we can anytime open and just save all this detail in GitHub Secrets. Don't misplace this. As soon as you're done with this, you need to deactivate this keys or delete the user itself. Do not put it in the Git repository in any case. GitHub Secret is a different thing, but do not put it in repository. Even though repository's private, still do not keep it. Just do as I'm doing. Okay, this much is enough over here. In the next lecture, we are going to create Slack workspace, a Slack app basically. So we can send notification from our pipeline for pass or fail to our Slack channel. So wrap this up and join me in the next one. Okay, so just search for a Slack login. You will get this URL. If you already have a Slack account, which I think you should have if you followed the previous lectures in Jenkins and all, you can use the same Slack account, you can use the same workspace, but if you do not have, create a new one. So I'm just going to get started with a new one and I'm going to create a new workspace. As I said, you can use the existing workspace, no problem if you already have, just wait until I go and create the app. I'm giving the Slack workspace a name, sentinel. Go Next. I'm just gonna keep the same name of the account. Skip this step. I don't want to invite anybody now. Don't invite anyone. This is fine. Continue with Free. Okay, over here. Now, if you already have Slack account, you should be logged in over here. You need to just create a new channel. Create a new channel. I'm going to call it as vprofile. Wait, where did it go? Create a new channel. vprofile-actions. Go Next. This is going to be Private, Create. Okay, skip for now. Don't want to invite anybody. Okay, now in a separate tab, go to this URL, api.slack.com/apps. Hit Enter. Say Create New App from scratch. I'm going to call this app as vpro-act-notifications. Pick up a workspace. Sentinel, that's the workspace I'm selecting. You can select anything you want. Create app. Then go to Incoming Webhooks, Activate Incoming Webhooks, scroll down, say Add New Webhook, Workspace, Sentinel. Channel that you have created. This is the vprofile-actions I'm selecting. Say Allow. Okay, so that's the test. You can just copy it and run it from your Git bash or terminal if you already have call. This is just to test. So if we run this successfully, we should see Hello, World! in our channel. If you go back to our channel, you see there, we see Hello, World! So this is tested, and this is our webhook. Copy this webhook. And I'm going to store it or just keep it ready in my sticky note. Okay, so I have Sonar token, I have the ECR detail, I have the Slack webhook URL, and I have access keys. In the next lecture, we are going to create one more thing, which is GitHub PAT, a token, Personal Access Token, and all this information, we are going to store it in GitHub Secrets. So wrap this up and join me in the next lecture.
