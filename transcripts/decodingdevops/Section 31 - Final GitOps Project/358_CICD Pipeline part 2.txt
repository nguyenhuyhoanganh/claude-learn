# CICD Pipeline part 2

Okay, so here we go. I'm not sure whether this is going to be working. Most of things are correct as per my knowledge, but things might go wrong. We'll see how it works. Anyways, you'll have this pipeline in this lecturer resource, The Working Pipeline, which you should use in your project. So you can just download it from the lecture resource, copy it in ci.yml and then you can follow me. Okay, so from your vProfile-app repository, you should be in the feature X branch. Okay? So as I'm in desktop GitHub's vProfile app, make sure you are in your repository. Say git add . Git commit - m "Pipeline". That's it. Git push origin, and be careful, feature-x. You need to commit this in the feature branch. Okay? Nothing should happen if you put it into feature branch. The pipeline will only run if you raise a pull request. So I'm going to now power on the SonarQube Server. So power on the SonarQube Server, get its public IP, and we are going to save this variable, the public IP variable, into our app repository. So go to your vProfile-app repository. Go to the settings of this repository, settings. Secrets and variables, actions, variables, new repository variables. Variable name, SONAR_HOST_URL. Just check it once again in our pipeline code. You see vars.SONAR_HOST_URL. This variable is going to fetch from GitHub and store it into this variable. So it should be same. So here, mention http:// and paste the public IP of your SonarCube server. Now, SonarQube Server listen on port 80. Actually, the Nginx server in SonarQube listens on port 80, so colon 80, and it's not mandatory to give the, because this is the default port of HTTP. But if you have a different port number, you need to mention the port number. Remember, this is a URL, not just the IP address. See update variable. Now, if you're just powered on SonarCube server, it's going to take some time, at least five minutes to bring up all its services, so make sure you check whether it's working or not. If it's working, then we can create a pull request. Now, let's go to the repository. If you go to feature-x branch, you should see here our new committed file in this folder, ci.yml. Now, pipeline does not run if we make any commit in feature or any other branch. It'll only work if it is a pull request on main or push. So if you go to actions, you should not see any pipeline that got triggered. As long as you don't push it to the main branch, nothing will happen. Now, we'll raise a pull request. This request is to merge our feature branch into main branch. So that is the request. So we say new full request. So we want to merge the feature X branch into the main branch. Look at the arrow. This is the branch that will get merged. And this is not a merge, this is a request. So when developer makes any code change in feature branch or buck fix branch, any other branch, nothing will happen. They need to merge it to the main branch and they cannot directly merge it to the main branch. There'll be a request. So pull request, or merge request, it's called. And this is one of the principle of GitHub, so even in general development, main branch will always be protected. If you want to make code change, you create a different branch, make the changes and raise a request to merging to, for merging into the main branch. So in DevOps, also in GitHubs, we follow the same principle. We make changes into feature branches and then we raise a pull request or merge request. So here we are raising a pull request. We are saying we want to merge this feature X branch changes to the main branch and we say, create pull request. Now, here we are raising the pull request, we are approving it and we are only merging it. But in real time, there will be different parties that will be doing this. So a pipeline says, whenever there is a merge request or pull request, it should execute our pipeline, which should execute the stage of checking the code quality. So here it started the pipeline. You can click on this and go to the pipeline, or you can go to actions and you should see the pipeline. So this is the commit message that we have given. So this is a pulled request opened by so and so person which triggered the pipeline. So it is checking the code quality. If the code quality is good, then the authorized party will merge it to the main branch. Otherwise, it won't. And our pipeline says, build the code, test, do the code analysis, do quality gates check from SonarCube server. If everything is good, then only we are allowed to merge. You cannot merge this to the main branch without this SonarCube quality gates checks out. So once this completes, we are going to go to SonarCube server and check the quality gates. Okay, this got completed anyway successfully, but we can still go to SonarQube Server. So we should see our project, we profile app over here. Now, we are using default quality gates. If you see for some reason the quality gate is failed, then you know how to do it. You can create your own quality gates, attach it to your project, set it as per your requirement so it passes, because our idea is to complete this project. In real time, there will be definitely rule set and the quality gates, and if our code is not as per the quality gates, it fails and the developer needs to make the code changes and make sure the quality of the code is good. Okay then, in real time, the authorized party will see the quality gate has checked out fine and they are okay to merge it to the main branch. So he or she will log into the GitHub account or whatever platform they're using and merge it to the main branch. So assuming it's somebody who is authorized to do this, goes to the pull request. Okay, for some reason I'm not seeing this pull request, but that's because I made few changes while I was, while I have paused the lecture. Anyways, let's make a new comment, or better, let's generate the README.md file. So go to the chat of GitHub Copilot or any AI tool that you're using and just ask it to generate README.md file. README, generate. Generate README file for this project. So that will be new commit then. Okay, so it got generated. I'll say, keep. I should see a new file, and let's commit this changes to the feature X branch. Okay, so here I am in vProfile app repository. I'm going to say git add . Git commit -m, say Generated README file in the commit, and I'll push the changes. Git push origin feature-x. Now, this should not trigger any pipeline. We are going to raise a pull request to merge it to the main branch. Okay, so let's go to repository. Let's go to pull request, new pull request. Pull request to merge feature X branch with the main branch. And let's say create pull request. You can give or change the title and give some more description and see, create pull request. Let's wait until this pipeline completes successfully. Okay, after one minute, 52 seconds, this got completed successfully, the quality gate checks out fine. Now it's time to go to pull request and merge this change. And in real time, the authorized party is going to check the quality gate. If everything is good, it is going to merge the changes to the main branch. So merge pull request and confirm merge. Now, this merges the code from feature X branch to main branch, and with this, our pipeline should get triggered. So let's go to actions. I should see the pipeline. So now it is building the docker image and it's going to push to Amazon ECR, and after that update the values.yaml file in Helm Repository. So this is going take some time. I'm going to pause the recording and resume when this is completed. Okay, it has pushed the image to Amazon ECR. Let's go and check to our registry. The profile app image. Here, we should see the image tag. So this is the (indistinct) show commit ID, and also it has given latest tag. Let's check if it is updating the Helm files. And that is also done. So let's check the Helm Repository now. Go to your repositories, vProfile Helm. Let's open this. Let's check values.yaml file. And this is good, image and the tag. Okay, so our continuous integration pipeline has updated the latest image on Amazon ECR and it has also made a commit to the Helm values.yaml file. We done here. From the next lecture, we are going to set up the EKS Cluster Argo CD, and at the end, we are going to set up the pipeline for our Terraform code as well. That would be all in this lecture. I'll see you in the next one.
