# Argo CD Setup

Okay, so before you start this lecture, I want you to do one thing. Go to your cluster in EKS, go to access, click on manage. And if you see here only ConfigMap, then change it to EKS API and ConfigMap and click on save changes. And then wait for five minutes and then start and continue this lecture. Okay, so we are almost there. We need to set up the ArgoCD controller, which is going to fetch the Helm charts and deploy it in the EKS cluster. And that's what we are going to do in this lecture. We are going to set up the ArgoCD controller. But with that, we are also going to set up Load Balancer Controller, so users from the internet, can access our vProfile application and we can also access our ArgoCD controller to maintain our vProfile application. First, we are going to set up the AWS Load Balancer Ingress Controller. We need to create an IAM service account for load balancer with a policy. So, we are going to download a policy, create that policy, then create IAM service account, so our cluster can use this policy, this service account with the policy. Then we are going to install a certificate manager. This is a kubectl command if you see. So, we also need to fetch the kubeconfig file. To fetch the kubeconfig file, we're going to run an AWS command, aws eks update-kubeconfig, name of the cluster and name of the region. So, it's going to go to the cluster, fetch the kubeconfig file and store it in our local machine. Then with kubectl command, we are going to install certificate manager in the Kubernetes cluster. Once the certificate manager is ready, then we are going to install the AWS Load Balancer Controller with Helm. So, we are going to add this charts and install load balancer controller. Once we have our load balancer control ready, then we are going to install ArgoCD. First, let's complete till here. I'm going to put all this command in this lecture resource. You need to make few changes like I'm going to keep this section empty. Here, you need to give your account name. And you need to also mention the Kubernetes cluster name. I'm assuming that you're using the same cluster name, but make sure wherever you see the account information, you need to add that. Also you need to give here your VPC ID, your own VPC ID. So, this section also we need to fetch the VPC ID. So, let's start one by one. Let's download the JSON policy first for our EKS cluster. You can be in your home directory. So, I'm gonna just type cd, hit Enter. Use this curl command, this is going to download the JSON policy. Then create IAM Policy with this. So, aws iam create-policy, the policy name and the document that we have just downloaded. So, I already have policy with this name, so it's not going to create it once again. The next command to create the service account, just make sure you replace this section with your account ID. Make sure your cluster name is correct and the region is correct. So, I'm going to copy this and run this command. This takes some time. Okay, this is done. Next, we need to fetch the kubeconfig file. So, run this command aws eks update-config. So, check the location. In your home directory, there should be .kube and config file that got created and the command kubectl get pod. It should start working now. Now, let's install the certificate manager with kubectl command, kubectl apply. The URL is already here. Okay, this creates so many resources, like rollback, services, multiple pods in a namespace. We can say kubectl get all -n cert-manager in this namespace. So, you should see these pods, services, deployment, replicaset. And these pods should be in the running status. Okay, now it's time to install the AWS Load Balancer Controller. And after I execute all this command, you will see a load balancer also. In this command, make sure you set the cluster name correct, your region name, VPC ID. So, you can go to VPC. From AWS Console, we have created a VPC with our Terraform code. Inside that, you have your EKS cluster, or from here also, from vprofile-eks-cluster in the networking, you can find the VPC ID. Okay, so that's my VPC ID. I'm going to copy this and make sure I put this VPC ID here. My region, us-east-1. And the cluster name is right. Okay, first let's run this helm repo update command. So, it's going to add this chart repository, EKS chart into your local machine. Then you need to run helm repo update and then copy and run this command. Make sure you make all the right entries, VPC ID, region, everything. This is going to install AWS Load Balancer Controller charts. After this command completes successfully, you should see a load balancer. And this is going to take some time. So, I'm going to pause the recording and resume when this is completed. Okay, after some time, you can run the command kubectl get pods -n kube-system and grep the load balancer, aws-load-balancer. And you should see two pods running. So, two load balancer pod will be running, kubectl get endpoints and this command. You should see the name of the endpoint and you should see the endpoint's IP addresses. Okay, so we are all set. Now, it's time to install ArgoCD and then we are going to use this AWS load balancer to access it. So, add ArgoCD repository, helm repo add argo and this URL. Then helm repo update. And then use this command to install ArgoCD, helm upgrade argocd argo/argo-cd and this version 9.5.2 that we are using now. It's going to create a namespace, named argocd. This will take some time. Once this is completed, I'm going to resume the recording. Okay, this is done, kubectl get all -n argocd. This will list all the resources in this namespace. So, we have the pod, the controller, repo-server, the argocd-server, which we need to access from the browser. So, you should also see a service, ArgoCD service that points to this pod. So, we need to create an ingress of type load balancer that points to this service. From that ingress, from that load balancer, we are going to access this ArgoCD service. So, open VS Code, create a file, argocd-ingress.yaml. Copy the content from here till here, paste it over there. Okay, so kind ingress, the name, we are going to say argocd-ingress in the namespace argocd. And these are the annotations that you need to give. The ingress class alb, this is going to select our ALB Ingress controller. In certificate-arn, you need to get the certificate ARN and change it with yours. So, if you go to Certificate Manager, you have a certificate for your domain. You can copy this ARN and change it with your ARN over here. Listen port 80 and 443 both, but ssl-redirect 443, so this is going to use HTTPS secure connection. Because we also have the certificate to verify the HTTPS connection. In the rules, argocd-dot, this is my domain, so you need to change it with your domain here. So, from the browser, when we see https:// and this URL, we should be able to access the ArgoCD. Save this file. And I've created in my repository. So, I'm going to go to my repository, which is in desktop/gitops/vprofile-infra, there. Kubectl apply -f and your file name. So, it creates the ingress. Kubectl get ingress -n argocd. You should see the ingress and you should see the load balancer endpoint. Now, we cannot directly access from the endpoint. We need to access it from the URL. So, copy the load balancer endpoint, go to your domain registrar. Mine is GoDaddy. So, I'm going to GoDaddy. In your domain, find the DNS setting. Go to the DNS records, see add new record. Choose the type as CNAME, mention argocd. Yeah, that's right, argocd. This should match with what we have given in our ingress. That one, exactly that, argocd. And here mention the DNS endpoint of your load balancer. You can grab that from here also. Paste it over here and see, save. You need to wait for a few minutes, until this record is published. Then we can exit from the browser. So, I'm going to pause the recording now and grab my coffee and then come back. All right, I'm back and without a coffee. Because I don't drink coffee, at least I don't drink it anymore. So, without any caffeine power with all my brute strength, I'm going to access ArgoCD. So, https://argocd-dot, my domain, hit Enter. And voila! Welcome to ArgoCD. Okay, so, we need the username and password. So, the username is admin and you need to run this command to get the admin password. So, for your, sorry, from your Git Bash or Terminal, execute this kubectl command to get a secret, which is stored initial ArgoCD password. Okay, that is mine. Yours will be different. I don't know. Let's give the username, which is admin and wait and password, the new one, sign in. And we are in. You can reset the password. Oh, here, user info, update password, paste your current password, add a new password and save new password. That should be it. In the next lecture, we are going to fetch our Helm charts from the repository, not we, ArgoCD will do that and run the application over here. So, let's see how that's done in the next lecture.
