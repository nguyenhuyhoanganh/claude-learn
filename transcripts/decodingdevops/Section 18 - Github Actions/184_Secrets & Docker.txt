# Secrets & Docker

Hello and welcome. In this lecture, we are going to build the Docker image from our source code and upload it to Amazon ECR. So these are the steps that we are going to do. First we are going to create Amazon ECR repository. And this is to store our Docker image. Then we need IAM access keys. So our GitHub actions can communicate authenticate with Amazon service. Once we create the IAM access keys we are going to store it in GitHub secrets. Then we're going to work on our Docker file. We already have docker file. We are going to make changes to it. And once we are done with these steps, then we are going to finally create a job that is going to build and publish the Docker image to Amazon ECR. So let's create the ECR repository. First login to your AWS account. I'm in North Virginia, US East one region and I'll be using this region. Find out the service Elastic Container Registry, search for it and open this. Click create a repository. Your repository a name. I'm just giving the profile hyphen app image and simply say create. Now let's create the IAM user with the access keys. We need the access keys. Go to IAM service. Go to users say create user. I'm naming it as actions hyphen ECR. Next attach policies. So we just need it to store the image on ECR. So we are going to search with the name registry this permission EC2 Container registry full access. Sure we can fine tune it, but we are going to keep it normal for now. But we can create our own policy and only give access to our registry. Sorry, our repository. We can also do that, but for now we're just giving it easy to container registry full access and say next and say create user. Go to the user, go to security credentials. Scroll down and see Create Access key CLI. Scroll down I understand and go next click on Create Access key. This should give you the access key and the Secret Access key. We are going to store it safely in GitHub secrets. So go to your GitHub repository and go to settings. Over here. Scroll down where you see secrets and variables. Go to actions. We're going to click over here Manage Environment Secret. We're going to store our secret in the environment production. We're also going to mention that in our job that we are going to use this environment. So we get access to the secrets and variables in this environment. So click on Manage Environment Secrets. Select your environment. So this environment we created in previous lecture. If you don't have this or if you delete it then create a new environment given in production and then open that environment. Click on add environment secret. Give secret a name. Now name can be anything, but I'm using this name and the same name I need to mention in my workflow steps. So for now, if you keep the same name it will be good. It will be easier, but if you choose to give a different secret name, then better you check that and mention the same one in the step in the workflow step. Okay, let's copy the access ID access key, copy the access key, put it over there and say Add Secret. Click once again, add environment secret. We are going to store the secret key AWS secret underscore access underscore key all in caps and store. Copy your secret paste it over there and say add secret. Okay so I have access key ID and I have AWS secret access key. We also need to mention the AWS region. Later, you'll see that when we create the step in our workflow to push the image on Amazon ECR, you also need to mention the region. So the region also I want to store it, but region is not a secret so I can store it into environment variable. So you can just say add environment variable in the same place in the production environment. Say add environment variable and give this variable a name AWS underscore region all in caps. And then mention your region. My region is US East one the same place I created my Amazon ECR repository. So make sure you do check your ECR repository you created in which region and give that region code only and say add variable. Okay, so we created the ECR repository. We created the access keys and stored it. Now we're going to work on the Docker file. So open your VSCode. Here you have a folder called Docker hyphen files. In that you have app and that you have multi-stage. And this is the Docker file that we will be using. Now this is a Docker file. It just fetch the image from our coder repository, builds it, and then copies the artifact into the tomcat image. And when the build completes, we'll have our Docker image with our artifact. But here the source code already exists. Over here we don't need to clone it again. So we're going to make some changes here. Let's remove these two lines. And we want to copy everything from the current working directory inside the container folder. So we're going to see copy dot slash let's say slash app. Now by default if you go inside this folder and build this image then it is going to copy everything from multi-stage folder. But we are going to build this Docker image from this folder from the root directory. So all this content will be copied to the slash app folder. And then we are going to say that this is going to be slash app is going to be our work directory. Work directory slash app. And then we are going to run the command Mvn install. And this command is going to generate the artifact V profile hyphen v2 dot var. And it is going to be in the slash app folder you will have slash target. So this path also we need to fix over here. This command is going to copy from the build image as the name here build image. And we need to mention the path it should be slash app okay. And in that slash target because when we run npm install it's going to create a folder target inside will have the artifact v profile hyphen v2 dot var and that will be copied to the destination. So that's the Dockerfile. Let me save this. So now we have everything ready to create our job to build this Docker image and upload it to Amazon ECR. We're going to do all that in the next lecture. So wrap this up and join me in the next one.
