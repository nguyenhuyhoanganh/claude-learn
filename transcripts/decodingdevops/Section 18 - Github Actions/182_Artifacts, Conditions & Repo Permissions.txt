# Artifacts, Conditions & Repo Permissions

Hello and welcome. In this lecture we are going to see few more things in our workflow, starting with the code repository permission. So when our workflow gets executed, the workflow will get a token GitHub underscore token. This token by default has read and also write permission on the repository. So when we are running it on the main branch, we don't want, usually we don't want our workflow to have permission to write the GitHub repository. So to prevent accidental write in your GitHub repository in the branch, we need to change its permission to read. So we are going to use permission permissions key in our workflow to change the permission of GitHub token. So open your VS code right below the triggers. In the first column you need to give permissions colon space. Hit enter. And say contents colon read. Then this workflow will have permission only to read from our repository. And I'm going to save this control s. Okay. Let's scroll down. Let's look at the next thing. So we know mvn install. When we run it creates the artifact. So it creates a target folder in that this v profile hyphen v two dot var based on your project you might have var jar or anything else. Any other files that you want to consider saving it after the workflow completes because the workflow terminates, all the data with it will also be gone. So there is a ready made action that we can use for this one. Just say hyphen name colon. Give a name to your step. Uses this particular action upload hyphen artifact at v4 and we need to mention what artifact we need to save. So we have to pass some parameter. So we have to say width. And we pass some input parameters. The name of the artifact let's say V profile hyphen app and the path. So I'm going to say target slash star dot bar. So any bar file will be stored as an artifact. Now we get a link from where we can download. It's going to give us a zip file. That zip file will contain all the artifact that is mentioned in the path. And then you can save this file. Okay. Let's look at the last step of this lecture. And that is to show you the conditions if else conditions. Now based on the action of the previous step you need to decide something like you want to send a notification if the previous step failed or you want to do something else if the previous step failed. So I'm going to give a name to this step. Hyphen name colon notify. If the build fails, you're not going to send an actual notification, just print message. The idea is that you know how to use conditions. So it's pretty simple. You say if colon now its value should come either true or false based on what we want to do here, I want to send a notification if the previous step fails. So I'm going to say if and I'm going to use a predefined function in GitHub. Actions. Failure is a failure and you give the parenthesis. This is a function predefined function. So which means if the previous step failed or any of the previous step failed, its value will be true. And if that is true, then execute whatever is below. So here for now I'm just giving here. Run. This command echo and the build job failed. Please check logs. So usually it will be a notification. In such cases you send a notification email notification or slack slack notification or even raise a ticket. But here the idea is to show you the condition if you can use functions like failure. There are other functions like success. There's also always. Always. That means even if the previous step fail, still continue the pipeline or continue the workflow. These are called as status status check functions. So by default it's success. So even if you don't give the success over there is also always like this. You can mention it and it causes the step to always execute and returns true. So no matter what happens, this is going to always return true. And then there is failure that we have used. If the job, if the previous job or previous step has failed, then report the failure and do some action like sending notification. We're going to add a few more conditions with the if not only the status check function, few other things in this workflow, but for now, save this and take a look. Once again, we have permissions content read that gives only read permission to the repository for this workflow. So you don't accidentally commit anything in this branch, which is the main branch here. Then we have seen how we can store artifact. After the workflow completes, we'll still have the artifact. And then we have seen condition, a very simple example of a condition to understand how conditions work. So save the file and then let's commit it. Commit and push. Give a commit message. Read permissions artifacts and conditions. That's the commit message I have given. I accept the commit message. Okay, let's look at the workflow. Now. Let's go to actions. The workflow got triggered by the time this is happening. Let's look few more conditions that we can add in our workflow. So let's open the VSCode. So we have the build job and we also have the testing job. Now I am deciding that I want to run this test Mvn test in the main branch only if the there is a main branch then only. I want to run this command. So it is. If there is the main branch I want to run this, then you know what to do. We have to use the if condition. So over here we are going to say if colon. Now I need a variable that holds the value of the main branch. And that is a GitHub context variable GitHub dot ref. This holds the value of the main branch or sorry, any branch on whichever branch this workflow is running, its value will be that. So it's running on the main branch. Its value will be mean. So if its value is equal to, you have to give double equal to and single quote, and you need to give the branch name in this format. Refs slash heads slash the branch name. So if this condition is true then only ambient test will run, otherwise it will skip it. Next, I also want to print a message that it has skipped the test. Also, let's put the condition here also for the checkstyle. So same thing I'm giving here. And when it skips this I want to print a message also. And this message should also print when this condition matches. So name of the step I'm going to say here GitHub dot ref. If it's not equal to main branch, that means whenever the workflow is running on the branch let's say bug fix, bug fix branch. This workflow is running. So it's going to skip this, skip this, but it's going to print this. That means execute this right. Because the condition will match here. If it's not equal to main branch. Right. And it's going to print this message. So any non main branch. You will see a message that I'm skipping unit test and code analysis. So once you're done save this file and say commit and push. I made a mistake in the workflow. I added one number by mistake. Okay let's commit it and let's see what happens. It's going to throw I think YAML error. It's not going to execute anything. Okay. Give a commit message say accept commit message save okay. We see this as a failure. And you will see here one message. You have an error in the YAML syntax on line 63 or not you will. I am getting this error. So line number 6663. Sorry there is a problem. So I'm going to remove that. Save this again and make commit and push. Now let me check the workflows once again. And now that is running okay. So the workflow is running. It will complete. Once this is completed you can join me in the next lecture. We are going to see a few more things in our workflow.
