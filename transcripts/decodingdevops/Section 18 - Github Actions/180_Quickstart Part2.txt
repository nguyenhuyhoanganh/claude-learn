# Quickstart Part2

Okay. So this is completed successfully. So let's take a look. Once again click on actions. So you can see all your workflow configured over here. If you go back to your code your repository in the repository you should see GitHub.com slash workflows. And you should see your file over here. And see we have mentioned here that it's going to run when there is a push on the main branch, and we are on the main branch. So as soon as we make a commit, the workflow will get triggered. Let's click on the workflow and the workflow. We have two jobs build and testing. Let's click on the first job build. Here you can see the output set up job. So current runner version runner image provisioner operating system runner image. So all this it is doing by itself. We just mentioned that we want to run it on ubuntu latest runner. And you see it's also downloading this action checkout action. Then it executes all the stages in our workflow. So first one is this code. Checkout it clones the source code. Then we have Maven build. These are all the steps right? So in workflow you have the job. And in job you have steps. We define just two steps code checkout and maven build. But the others it takes care of itself. And keep this in mind this is all in runtime. Whatever gets built over here gets destroyed when the job is completed. So we need to take out the artifact if you want to store it. We can define the artifact and then we can see the artifact down here as well. Let's also make some mistakes in our workflow and see what happens. So first mistake I'm going to do I'm going to define a different version over here. Check out at V 16. And then also I'm to change this command or wait I'll change the command over here I'll give it us instead of Maven Mvn I'm going to just say Maven. So I'm going to save this. So I have two mistakes. Let's see how our what happens. Commit and push. Give a commit message. Okay. Click on actions you should see. Ah, okay. It took some time, but it's there. Okay. So it's failed. Let's click on this. So build job failed. Let's click on that. All right. So it says unable to resolve action. Unable to find version V 16. Showing this purposefully because when you are using AI tools to generate the code many times the versions are wrong. That's my own personal experience. So give a different version names. And because of that, the pipeline failed. So make sure you check that the version usually is different, so you should find the right version for your actions that you're defining. So go to marketplace and search for your action that you're using like we're using checkout. So let's search for checkout. Let's check out this is the one that we're using checkout v4. But always look at the example. Okay here it's v5. So let's copy that. What do I need to copy is just one word. So I just give v5. V5 save it. Previously we defined v4 and it worked. We have seen it. Let's test it with v5. And then let's commit and push. This time the error will change. Right now commit and push. Yes. Give a commit message and save it. Accept commit message. Okay let's check once again let's go back to actions. Okay. It's running. Okay. So that's working. You could check out worked. So V5 works you can use v5. Now I want to see this one. Let's go back. Okay so that worked. But the testing is going to fail because we have given a wrong command. So let it run. Okay. Testing started. I want to show you this. Before even you get into the job. You can also see the error over here also. But more detail. You better get into the job and you see the exact step that it failed. So it's clearly maven command not found. Okay, so we know what to do. Let's fix it. And then we'll see a few other things in GitHub actions that we'll be using in the next lectures. Fix Mvn test. Save it. Okay. Commit and push. Give a commit message and say accept. Commit message. Okay. That's running and it will complete in some time. Okay. So that would be the basic working of GitHub actions workflow. Its job and the steps in upcoming lecture. We are going to see many features and use cases of GitHub actions. We'll do something more meaningful. We are going to clone the source code, build it, test it, and then build a Docker image and upload it to Amazon ECR. All that we'll be doing from the next lectures. Before I close this one, I want to show here one last thing and that is variables and also the secrets. So go to settings. Okay. This is the repository setting. Over here you see secrets and variables. Click on actions. So over here you can store secret data like access keys, passwords and all. Secretly these will be stored encrypted. And then you can call it in your workflow. So your two tab here secrets that will store the data encrypted. And then you have variables which are just normal variables that you can define. But before you define variables and secret you need to set environment. So you can click over here on environments. Now environment name as it says it represents it infrastructure environment like dev QA staging production. So let's say you use one variable in your workflow, but its value is different based on the environment for production. It's different for dev and staging it's different. So you can keep your workflow intact. You can use the same variable, same variable name in the workflow, but its value you can define as per the environment. So let's set an environment. Let's click on New Environment and let's give a name I'm giving as production and say configure environment okay. So we have environment. And now we can add variables and secrets into this environment. And also this environment has different settings. No restriction means any job can use this. Variables and secrets from this environment. Then you have protected branch only like the main branch, main branches are usually protected so nobody can commit to them directly. It will be always going through the pull request. So set an environment. Set an environment for protected branch. Then it can be used only by the protected branch. No other branch can use those environment. Or you can define a list of the branches and tags on which the environment can be used. So like that there are settings for this. Let's scroll down and store some variables and secret into this just to show you later we are going to define proper variables and secret to use. So just click on Manage Environment Secrets. Select your environment and here you can add the secret. Add environment secret can give secret a name. So just give the name of the secret and its value. And you say add secret. Similar way you store the environment variable. Give the name of the variable and its value and say add variable. So secrets are stored encrypted. Once you store you cannot see this. But environment variables are different. Like if you go to edit for a secret, you cannot see its value. You can just update the value, but you cannot see the saved value. That's because it's encrypted, doesn't want to show you. But if you go to environment variable edit, you can see the value and then if you want, you can update it. Okay. So play around a little bit over here into the GitHub actions and look at different settings. Also spend some time over here. Go to New Workflow and take a look at a different workflow. For example, the Docker image. If you click on configure, just do not commit anything. Just take a look at the workflow. Different settings you have here. Just cancel changes again go to actions new workflow. If you scroll down you can see deploy to X, build and deploy GKE, OpenShift octopus deploy different kind of workflow. And in fact, when I first started working on GitHub actions, I was mostly learning it through these sample workflows with different settings. But now we have many things we can use the AI tools ChatGPT Copilot, Amazon Q many coding assistant tool we can use to write the code for us. But before we get into those AI tools, we will have some good hands on workflows and different settings inside that. So take your time. Spend some time with this workflow, take a look at these different workflows and then join me in the next lecture.
