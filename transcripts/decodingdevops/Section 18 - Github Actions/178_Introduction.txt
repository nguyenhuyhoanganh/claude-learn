# Introduction

Hello and welcome! In this section of the course we will be learning GitHub actions, a built in ci CD workflow in GitHub. Yes. So you can see in GitHub action is a CI CD platform built into GitHub. That's right. When you log into GitHub account you can see a tab called actions. That's where you can configure your entire CI CD pipeline. So in your GitHub repository you see here actions, you go there. Here you can configure your workflow or your CI CD pipeline. Just keep in mind the pipelines the CI CD pipeline in GitHub actions are called as workflows. This workflow will be written in a text file and that will have the YAML format. That's an example of a workflow. It's in YAML format. All the settings you configure in this text file, and you have to make sure this exists in GitHub workflows folder main dot yml. That will be your main workflow file. So what can you do in this file? Well, of course you can automate your tasks for building, testing, deploying your artifacts, sending notifications, or you can think of any command that you want to run in your pipeline. You can put that in the workflows. So GitHub has become now an ecosystem where we not only have the GitHub repository, we have many other things. And GitHub actions fit into this entire ecosystem. It can leverage this entire ecosystem. So that means it will be very easy to get started with GitHub actions. And that is one of the main reason why people are shifting to GitHub actions, because it's just native. You use GitHub, you can get started with GitHub workflows or GitHub actions, and any language of code you are going to build or deploy Java, Python, Ruby, Node.js or any platform that you have specific Windows or Linux or Mac OS. It supports all their pre-built actions Available in GitHub marketplace, which you can use to perform various tasks in your workflow. You want to clone source code. You want to build Docker images you want to deploy to various environment GCP, AWS, Azure, Kubernetes. You have a huge list of these extensions of these actions. We call it. You want to check or lint your source code to code analysis. You want to check out the source code. You want to do SSH. You want to build and publish Docker images. Code quality. Code review, continuous integration. Deployment. There are many, many available actions that you can use in your workflows. So by default this runs on the predefined runners in your workflow jobs. You can select. Where do you want to run all the steps from the job. So GitHub actions by default provides predefined runners. You can mention you want to run this job on Linux platform or Windows or Mac OS. You can select ubuntu specifically. So these are predefined, but you can also add your own runner. Let's say you want to add an EC2 instance as a runner in your workflow. You can mention that, for example, a security scan that I want to run on an ubuntu machine, I can mention runs on ubuntu latest. Now this is a predefined runner or built in runner available in GitHub. But as I said, you can also add your own custom runners. You can trigger your workflow on various events like a commit on your branch, a pull request. You can schedule a workflow. And there are many other events that happen in your GitHub account. You can use those events as a trigger to run your workflows. So lastly, let's talk about the core concepts we have. Workflow. These are defined in the YAML file. So in general terminology you can also call it as a CI CD pipeline workflow is a CI CD pipeline written in YAML format in workflow. You have various jobs that you can mention like build job, test job, deploy job. And you can define on which runner you want to run the job. Job consists of multiple steps or a single step. Each step means a task. You could be running a script, could be running a command, or you could be using predefined actions available in GitHub marketplace. Mostly your workflow steps will consist of various actions and the runner of course. So these are some common terminologies we use on a day to day basis. Workflow. Job step. Action runner. So now I think you would be clear the use cases of GitHub actions. You can use it to set up continuous integration pipeline or continuous delivery pipeline. You can build your code, test your code. You can do code quality checks, you can do security scans and any other kind of automation or command or scripts that you want to run from your workflow. You can mention it in the job of your workflow. Okay, that was enough talking. Now let's get into action with GitHub actions. So join me in the next lecture.
