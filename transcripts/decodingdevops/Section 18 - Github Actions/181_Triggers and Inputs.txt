# Triggers and Inputs

Hello and welcome. In this lecture we are going to talk about the input we pass to an action and also different triggers for a workflow. So let's take an example of this workflow. I'm going to click on configure to show you a few settings. So let me scroll down here. Okay this one login to GitHub container registry. So it uses this action. And with this it passes few parameters. These are called as inputs. So with an inputs that you can pass to an action. So for example we have checkout action over here. And we need to pass some parameter. Now check out in general is going to clone the source code. But sometimes we need much in depth. We need the history of the previous commits. So we can compare and use it for different purpose, like giving a tag to a Docker image. So in that case we need the previous histories so we can pass an input with this. Fetch hyphen depth the value zero. So it's not going to just clone the source code. It's going to get the previous history commit logs everything which might be required if you're trying to compare previous commit, but not in a in our case we're just going to use the current commit ID. But I wanted to show you that how you can pass input to an action. We are also cloning this source code in the testing job. So let's give it here also. And let me save this. So the market marketplace if we search for a checkout let's take a look. So you see that's how we use it right. We give the action users and we give the action name and the version with. And you see there are so many parameters that we can pass like token, SSH key and many other things fetch depth. That's what we have used. Default is one. If we set it to zero. Indicates all history for all branches and tags. So based on your requirement you can find the different parameters. And you can pass it with the input with the width input. Next we are going to look at the most popular triggers for GitHub actions workflow. We have seen in previous lectures that when we push the content to the main branch, it triggers the workflow. Let's take a look at one more trigger that is popular pull request. You see pull request colon and then you can give the name of the branch or branches in list format. Now you can give either in this format list or in this format also. So like this in square bracket. In double quote you give the branch name comma. And then you can give other branch name like let's say feature branch. So this is one style. The other style is this you can just say branches in the next line give space hyphen. You give the branch name. And like that you can keep increasing the list when you need it. So let's keep this format now in order to show how this works. First we need to commit this. Then raise a pull request for the main branch from a feature branch from another branch in order to remove the confusion, I'm just going to keep it as main branch only, and I'm going to commit these three lines over here, because when I commit it into the main branch, this is going to trigger the workflow. So I'm commenting this out for now. Just keep pull request branches and the main branch name and let's commit and push. Make sure you save the file control S and then commit and push. Give a commit message and say accept commit message. Okay come to your repository. Let's go back to the repository. Let's go to actions. So that did not trigger the workflow. That is because we have commented this. Now let's test this pull request. First you need to create a branch from the main branch. The developer directly do not commit to the main branch. They create a feature branch or bug fix branch. The branch name could be anything based on the requirement. So let's say I'm creating a branch called bug fix and some number here 901. And I say create branch. New branch is created from the main branch. Both the branch main branch and this branch has exactly same code. So we'll pull this branch also in our VS code in our local machine, make some changes in this bug fix branch and then will merge this branch to main branch. Or basically we're going to raise a pull request to merge our code to main branch. So I'm going to open the terminal over here. In the VSCode open terminal. And I'm switching to git bash. If you're using Mac OS or Linux you can stick to your terminal run the command git fetch origin. You see it has pulled a new branch. Now let's switch to that branch. Git checkout bug fix hyphen 901. Okay, switch to that branch you can see over here. Or you could have just clicked it and you could have selected the branch. Now let's make some changes in this branch. I'm going to basically remove these files. So Jenkins file I'm going to remove like delete. And also this vagrant folder I'm going to delete to open the Readme file make some changes over here I'm going to add some extra hash and make sure this is JDK 21. So basically I made some change. Now I'm going to commit this. I'm going to do it through command line. This time git add dot git commit hyphen em. I'm just saying. Fixed bug 901. And I'm going to say git push origin bug fix. Hyphen 901 as a mistake. Bug fix 901 okay, now let's go back to GitHub. So our bug fix branch is fast forwarded. It has moved ahead of the main branch. And let's say everything is fixed. Everything is working. And now we need to merge this code into the main branch. So main branches usually will be protected. And you cannot just directly merge it into the main branch. You need to raise a pull request. You already get an option over here compare and pull request. But let's go to pull request. Let's say new pull request. So let's select here the bug fix branch. So this changes will be merged to the main branch. It will show you what all the changes are being merged. Sure. Okay. You can say create pull request. So let's give some description some description. Scroll down and click on create pull request. So once you reach the pull request you see over here itself the pipeline has triggered, the workflow has triggered. And click on this. Yeah. Or you could have just click on the on the actions and you can see the workflow got triggered. So that's what the pull request trigger means. So when we mention pull request branches and the name of the branch. So now the pull request was raised for the main branch. That's why the workflow got triggered like that. You can add here different branches if it is required. But mostly it will be for main branch. Okay. Let's remove this. Remove these hashes. Let's keep this one also. Just make sure. Push and pull request are in the same column. Save it. Now let's take a look at some other simple workflow triggers. In the same column workflow underscore dispatch. It's pretty simple one. You know, if you want to run the workflow manually, maybe initially this is required for testing purpose. Or if it's not a CI CD workflow, if it's for something else running some commands or something and you want to run workflow manually, not for the commits, then you can use workflow underscore dispatch. That is enough and you will get an option to do that. We'll see that in a moment. Let's take a look at one another trigger that is schedule C schedule colon. And in this you can pass this parameter cron. If you want to give the schedule in the cron job format, let's say every night at 2 a.m. you want to run this workflow? So doesn't matter whether there's a push event, there's a pull request or anything at this time, this workflow will get executed. So the first one is for minute. It's normal cron job format in the Linux. First one is minute. So let's say I want to do it at 210 or maybe in the afternoon. So I'll say 14. So 2:00 pm and ten minutes. This workflow will get triggered. The next one is day of the month like first, second, third till 31st star means every day, every date of the month, and the next one is the month. One. If you give, that means January 12th is December, star means all the month, and the last one is for the day of the week Sunday, Monday, Tuesday. So let's say if you want to do it from Monday to Friday. So zero is Sunday, one is Monday hyphen and five is Friday. So it's Monday to Friday like that. All right. So these are some commonly used triggers to run your GitHub actions workflow. These are common ones but there are many more. So for now save this commit it commit and push. This should trigger the workflow. Give some commit message. So if you see a workflow getting executed, just let it complete. We have the previous pull request that is pending after the pull request. The pipeline was triggered and completed successfully and now we can merge it. Merge pull request and say confirm merge. This is going to merge the code to the main branch. And you also get an option to delete this bug fix branch. So let's click on Delete branch. Let's take a look at the source code once again. So now you have just the main branch. Let's take a look at the workflows. So that's the code that we had in our bugfix branch. Now it's merged to the main branch. And the bugfix branch is also deleted from here. Now it's time to also delete it from our local machine. So open your VS code. First of all switch to the main branch. Then let's open the terminal. New terminal. Okay, here, for some reason it is still in the bugfix branch. I will need to. Yeah I am in the main branch. But anyways just do git checkout main to make sure you are in the main branch. Then run this command git branch hyphen d bugfix hyphen 901 or the branch name that you have given hit enter. If that does not work, use capital D. If you get any error, use capital D and then run the command git fetch hyphen f and prune run git branch hyphen a git Pull to pull any changes left in the main branch. Okay. Take a look. Git checkout main git branch hyphen d. If that doesn't work, then capital D to delete the branch git fetch hyphen hyphen prune git branch hyphen a. To list all the branches, you should see only main branch here git pull to pull all the latest changes from the main branch to your local machine main branch. All right, so that is all. In this lecture we have seen how to pass input to an action. By using width. We can look for the different parameters of an action in the documentation. Then we have seen triggers push to main branch. We already have seen. We have seen pull request workflow dispatch for manual trigger and schedule to run your workflow at a particular time or intervals. That would be all in this lecture. I will see you in the next one.
