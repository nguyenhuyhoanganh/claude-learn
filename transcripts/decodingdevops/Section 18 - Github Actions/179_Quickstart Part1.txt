# Quickstart Part1

Hello and welcome. In this lecture we are going to get quickly started with GitHub actions. So for this lecture we are going to create a GitHub repository and connect it with our VSCode. And then we'll see a sample workflow to understand the syntax and structure of GitHub actions workflow. So login to your GitHub account. If you have not created a GitHub account in prerequisites you create it now. Once you create it, login and click on create repository. Give repository a name I have given Action profile. Choose Visibility Private and just say Create repository. So we will be using SSH connection with our GitHub repository from our VSCode. So we need to create SSH keys and share it with GitHub account. So open your terminal or git bash if you're using windows type CD. Hit enter and say CD ssh. You will get into the hidden SSH folder in the in your user's home directory. Execute this command ssh keygen. Hit enter. Give a name to your keys. Like this I have given actions profile. Just give the name. Do not give any path. Hit enter. Just keep hitting. Enter. You don't need to enter any passphrase. You can clear the screen. You should see a private key and a public key. Now we need to enter this public key into our GitHub account. But before that we are going to create a configuration which will tell SSH to use this private key to login when our GitHub URL is for this repository actions profile. So for that just say VI config. Open this file, go at the end, get into the insert mode and enter this detail when the host is github.com. Hyphen actions profile. So when we clone our source code, we are going to change the URL and add this hyphen over here with actions profile. And if that is the case it is going to use this key this identity file. I'll put this in the lecture resource. Or you can simply type it. Once you are done with this save and quit just make sure the file name is correctly defined. Now let's copy the public key. So to get to the Actions Profile dot pub file and copy this content, go to your GitHub accounts settings. Make sure you click on the GitHub account settings and not the repository settings. In the settings, click on SSH and GPG keys. This is where we stored the SSH keys. And mind you again here we are storing the public key, not the private key. Public key is the smaller one that we have just copied. Click on new SSH key. Give it a name action profile. That's what I have given. And copy the public key over here. And click on add ssh key. Okay. Go back to your repository. Click on ssh and copy this URL. Type CD. Hit enter. Come out of this ssh folder and select a location where you want to clone this source code this repository I will be doing it in the F drive. You can select your own location. Now we have to copy or clone with a proper name because in the configuration such configuration we have mentioned when the host is github.com hyphen actions profile, then it is going to use this key. And in order to do that we have to say git. Clone the url ssh GitHub url after github.com, you have to say hyphen and you have to give this name actions profile, github.com hyphen actions profile and the rest remains the same. Hit enter. When you do this, it is going to use this key to login to the GitHub account. Now you should have a folder with your repository name over here. Just do CD into that. And now we have successfully cloned and authenticated with our GitHub account. Now let's open this into the VSCode. You can just do code dot. If it does not work, then open VSCode directly and open your repository folder from there. Okay so this is an empty repository. We need the source code. So we are going to use the V profile project source code and put over here. So open your browser go to github.com slash slash profile hyphen project. Select the branch docker. Search it with the name Docker docker. We are going to use the source code. Of course we are going to make many changes to it, but this is where we start from. So click on this green dropdown and say download zip. Okay so for me it got downloaded into downloads folder I'm going to open this, copy everything, copy all this content and then go to my action profile repository. It's in F drive. So choose the location that you have selected and paste all the data over here. Let's check in the VS code okay. So we have everything we need to get started with okay. Now come to GitHub account. We're not doing anything from here I'm just showing you. So you understand some basic concepts about GitHub actions. So actions gives you a way to configure CI CD pipeline for your source code in GitHub account itself. So here you have the actions tab if you click on this. So as it says build test deploy your code. This is all done through workflow in GitHub. Actions CI CD pipelines are called as workflows or pipelines are called as workflows. And there are so many sample workflows over here. We'll take a look at it later. But if we click over here on Simple Workflow Configure. Now you see here this action profile. That's our repository folder. In that you have dot GitHub. In that you have folder workflows. And in that you have a file. And this file is in YAML format. And this exactly is your workflow file. Like in Jenkins you have pipeline code. Here you have the workflow. So let's take a look and then we'll configure our own workflow name. That would be the name of your workflow on. That represents how your workflow get kicked in to trigger the workflow or to trigger this pipeline. There are various triggers and based on your requirement, you will be using one. For example, here you have push. That means if there is a commit, there is a push in the repository and you can mention the branch. And you see this is in the list format. The square bracket represents there a list you can have like this multiple branch name also separated by comma in and double quote. Let's see. I have a feature branch. So now what this means is if there is a push in main or feature branch, this workflow will get triggered. Then you have pull request. This we're going to see in the project. So for branches like main there won't be a direct push. It will be through a pull request. And if the request is approved then the code will be merged into the main branch. So it says whenever there is a pull request for the main branch, that is a trigger. Now you might have one or multiple triggers based on your requirement. We're going to see some triggers in the project workflow. Dispatch is for manual. If you want to manually trigger the job, that means you want to just run the workflow. You can set this one okay, so name on how the workflow gets triggered. And then you have jobs. So in job you give the name of the job you mentioned where you want to run. It runs on ubuntu latest. So workflows in GitHub. Runs on runner. There are predefined runners in GitHub. You can also create a runner and add it into GitHub, into GitHub actions. And then you can mention your runner like launch an EC2 instance and add that as a runner. And then you can mention that runner over here. Once again it will be based on requirement but we'll be seeing all those requirements. So please keep in mind this is a YAML format. So everything needs to be structured into the YAML syntax like on. And then there is space over here. And then you have push and pull and workflow dispatch all in the same column. And then jobs come into the column of on on and jobs are in the same column. And then you have space and you have build. That is the job name. You can have multiple job in the jobs section. This is the first job named as build and inside build you have. Then you see the space over there inside that you have runs on. And then you have steps. So steps runs on comes in the build in the same column. We'll see that when we write it in the VS code it will be more readable. Okay so coming back to our here steps. So in the job which is the build job you have steps. So this steps define the different steps like over here hyphen name. Again all the steps will be in the same column. You see that hash that hyphen that hash hyphen this same column. So hyphen name colon and the name of the step. And what you want to do in the step. Now run means just run the command. So run means echo. Hello world. That is going to just run a bash command on the GitHub runner ubuntu latest. So you simply want to run commands. You can just use run, but you will be doing many more things apart from just running the command. Like you need to clone the source code. In such cases you have predefined actions in GitHub. In GitHub marketplace there are predefined actions that you can use like this one. Check out version v4. This is a predefined action. Like in Jenkins you have plugins. You can call this as extensions. These are the actions. If you come to github.com slash marketplace you can go to actions all actions and you will see here many actions like Super Linter which is used to do code analysis on your source code. You have checkout. That's the one that was defined over there. Let's click on that checkout. If you scroll down you can see the examples usage. Give users and see actions and the name of action. And with that there will be different settings like in width you can define what repository reference token, many other things that you can define over here. But mostly it will be simple as like this, because the source code will be in the same place where you're running the GitHub actions. So this is going to clone the source code in the runner. And if you are thinking like here's the source code. And why do I need to clone it? Well, that is because it's not going to be executing here. It's going to create a runner. A runner is like a VM or a container that gets created. And in that all the steps gets executed. So in the runner it is going to clone the source code. And then you can execute your multiple steps into that. Okay. So enough talking. Let's do this and see how all this works together. Okay. So open your VSCode. And at the top level over here click on new folder and give folder a name dot GitHub. Exactly same name. Make sure the dot comes first and in that dot GitHub create new folder. Name it as workflows. And inside the workflows you need to create the file. And we're going to name it as main dot yml that represents this is the main workflow. By default this will get executed. So create with that name main dot. YML just take a look at the path once again. So first give the name name colon. And colon. And let's give the name of our workflow. I will name it as build W.F. like workflow. Now how do I want to run it? I want to run it on give on colon space hit enter I need to give 1 to 2. Space is good. Push colon hit enter. Then you give two space and then you mention the branches. Colon hit enter, then give space hyphen and the name of the branch. Now this means if there is a push on the main branch, the workflow will get triggered. For now we'll just have one workflow. So just one trigger. Hit enter and come to the same column as you have on. And here write jobs colon, hit enter, then give two space and say build. That is the name that we are giving right now. So that's our first job. We have two jobs in this. So I'm going to already define it. I'm going to say this one test testing. So two jobs. Now I'm going to define the steps in the jobs in the build job. I will say runs on ubuntu latest so that we I define the runner. This is the predefined runner in GitHub. We have windows runner and also Mac OS runners. And as I said previously you can also add your own runner. Okay now will be the step. See right steps and you define all your steps. Now steps will be in the list format. So you will have hyphen name name of the step. We'll call it as code checkout. And we are going to use the GitHub actions from marketplace. So users actions which is the actions from marketplace. And that's the name of the action checkout at v4. That's all you need to do to clone this source code inside this runner. Now next step I want to just run Mvn install. So I say hyphen name Maven build run. That will be just run the command Mvn install. Now this is predefined in this runner, so we don't need to install Maven. But yes, there are GitHub marketplace actions which you can use to run Maven commands also, but really not required. We'll see more actions later. Okay, now let's define the testing phase quickly and then let's save it. So testing job runs on ubuntu latest once again same thing we're using. And I've given here needs build. That means it is going to execute first the build and then this in GitHub actions all the jobs runs parallely by default. So if you want it one by one one at a time then you need to give this needs and you need to give the previous job that you want to run before the current job. So I want to run build before the testing starts. That's why I have given needs build and make sure the name matches. And then you mention the steps that you want to do. So steps. Again we do the code checkout. And here I'm executing the Maven test command Mvn test. And here the Checkstyle. So in short I cloned the source code I run Mvn install in the first job. In the second job I again clone the source code. I run Mvn test command and I run Mvn Checkstyle Checkstyle. So this is our sample workflow. Let's just save it, do Ctrl S and then go to this source control. Click on the drop down and say commit and push okay. It appeared a little late anyways because there was all the source code and our workflow workflow together. We are committing give a commit message first test on action. That's what I have given and put a check mark say save okay, as soon as that happens this will trigger the workflow. Let's go back to our repository. Go back to the repository and click on actions. You see that one workflow run first test on actions. All right let it complete. And then once this is done join me in the next lecture.
