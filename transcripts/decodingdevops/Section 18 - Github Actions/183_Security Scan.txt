# Security Scan

Welcome. In this lecture, we are going to add one more new job in our workflow. The job will be to do security scan on our source code, and later you can also expand it to the Docker image that we will be building later. And the artifacts also. Now Aqua Security is a company that builds some tool open source tools. One of them is Trivy. So in the GitHub marketplace, if you search with the name of Aqua Security, the you will get this at the top. Like we conduct unit testing Mvn test or code analysis like Checkstyle like that. Security scanning is also very much used in DevSecOps pipelines. If there are any issues, it highlights the vulnerability, the vulnerabilities it is going to highlight, and then you can decide whether you want to promote that build or you want to stop it. So in this lecture we are just going to add a step sorry add a Stage, build stage which scans the source code and generates the result. And that result we're going to store as an artifact. So if you look at the usage of this action, this is the complete action name Aqua security slash Trivy action. And you pass some input with that. Like for example a Docker image in what format you need the output, what will be the exit code vulnerability types that you want to scan? Like these two are the most famous one OS and library, but there are a few other options. Now we are not going to scan docker image. Now we have not even generated docker image, but we are going to scan the source code and generate the result. So we are going to create a stage something like this. Oh sorry a build something like this where we clone the source code and then we run the scanning. So open VSCode, make sure you are in the main branch, no other branch. And in the same line where you have the build and testing in the same column, you are going to write a new build. I'm calling it a security scan. And then we need to clone the source code. So we need same things. It should run on ubuntu. I need to scan the source code. So I'm copying all this from the testing stage. Some space. Okay. Needs. Also I'm copying here. Needs build. So now what's going to happen is first build job is going to run and then testing job is going to run. Now testing job needs the build job and also security scan job needs the build job. That means these two jobs will run parallelly. And we'll see that. First let's add the security scan action. So let's add the next step over here to scan hyphen name I'm going to give the name. And then the action uses Aqua Security slash TV actions at 0.28.0. So this is the version when I'm seeing it. So you can keep the same version, or you can give a different version. If you see a newer version later, you can use that also now passing parameters with it. So with so we need to specify what to scan and the output and the vulnerability type. So I've just copied it from the documentation. So scan type file system fs scan reference I have given dot. That means the current working directory that includes our source code everything format of output I am saying JSON exit code zero. Now exit code zero means continue with the next step. Do not fail it even if the vulnerabilities are found. But if you set it to one, then if vulnerabilities are found, this is going to fail your workflow. So we're going to keep it zero because there are vulnerabilities. And we'll talk about it. What happens when there are vulnerabilities later. Next we want to store the output because once the workflow completes the output will be also gone. So you want to store the output as the artifact like we did over here in the build stage? Yeah. Like that. So I'm just going to copy this one and make the changes. So let me change the name first. Simply call it as upload preview scan results as artifact. And that action is good with name. Name of the artifact. Let's change the name of the artifact I'm calling it as TV scan results and path. So path is basically this same output. Here we are storing the output into this file. And we're saying this is as an artifact. We are uploading it. So let's save it and test it. See the artifact. So I saved the file control s. And now I'm going to commit and push. Yes. Give a commit message and say accept commit message. Let's take a look at the workflow. Okay. Triggered security scanning. That's the commit message. Okay. Now if you see you have build and after that these two jobs are going to run at the same time. Security scan and testing. The reason both this job runs together is because by default, all the jobs runs at the same time. Parallelism is there in GitHub actions. But if you define the order needs build for security scan. And here also we have given testing needs build. If you want to run security scan after testing, then we need to give security scan needs as testing and in the security scan in the needs. If you have given over here the name as testing, then first build job will run, then testing because that needs build and then it is going to run security scan. If we have given here testing. But that's okay. We can run both the jobs parallelly. There's no problem. So let's keep it as it is. Let's take a look at the artifact. So we are storing two artifact. If you scroll down over here you can see here as we profile app folder we downloaded that downloads a zip file that will contain the artifact, and then we have the scan result. Let's download that. So both are in zip format I'm going to open this one. So downloaded a file in JSON format. Let's open it. This should open in VSCode. Yes. Now the JSON output is useful when you want to upload it somewhere on the dashboard. You want to do analysis on this. The more human readable format is table or here. If you just mention table, that will be more human readable format, but mostly after generating the result, we do take some action with based on the result. So that's why usually you should see the JSON format. Anyways, the idea was to show how to do security scan on your source code by using a GitHub action. And that should be all in this lecture. In the next lecture we are going to see how to build a Docker image from our source code and upload it to Amazon ECR, the container registry. So join me in the next lecture.
