# Build & Publish Job

All right, then, let's get started with the final job of our workflow. Build and publish the Docker image. So open your main.yml file in the workflows and in the same column of the security scan. Create a new job with name, build and publish. So build and publish. That's the name I'm giving and the name attribute also same build and publish. This needs to run on ubuntu latest. In this job we are also going to login to the AWS account with the credentials that we stored in GitHub. Secrets and the environment name was their production. So we need to specify that the environment will be production in the GitHub environment. So we need to just mention here environment colon production. So we get access to to those secrets and environment variable. We want this job to run after all the previous job's complete. So we're going to see needs. And in square bracket we can give the previous job name. Build testing security scan. So it's going to check if all these three jobs are completed. Then only it's going to start build and publish job. And we want to run this only for the main branch. So we're going to give the condition over here if GitHub dot href is equal to the main branch. Now let's include the steps. So say steps colon. And inside that first we need to clone the source code. Now we need to build the Docker image and upload it. And this will be all in one command. And uploading it to ECR needs the authentication with the AWS account. And then we need to log in to the ECR. So there are few actions predefined actions that we can use AWS credentials action that is going to login to authenticate to our AWS account. And then there is login ECR action which is going to log into ECR service. Once we do that, we can build and upload the Docker image to ECR. So let's do that first. So first configuring the credentials that we have already saved. So for that we're going to use this action name configure AWS credentials that I have given. Actually I have copied all this directly from the documentation. So in this action it's clearly mentioned we need to pass these inputs AWS access key ID. And then we mention the access key ID. Now definitely we are not going to put the access key ID over here in plain text. That's why we stored into secrets. And in order to access the secrets we need to give it in this format we say dollar two, curly braces, opening secrets, and the name of the secret. This is exactly the same name I used in previous lecture to store this access key. Then you close the curly brace. Same way the secret key also that is also stored into secrets. And then AWS region that we have stored into environment variable to access that. Now I need to give here. Where's vars dot AWS underscore region. So by using this it is going to connect to our AWS account. And then we need to add one more stage over here. Sorry one more step over here to log in to our ECR. Now this is pretty simple. You just specify the action GitHub action to log into ECR credential is already configured here. So just this action. Of course the name login to Amazon ECR and uses this action. This is going to just simply login to Amazon ECR by using this credentials. Now we are also going to use its output later to get the ECR repository. Basically the output of this login. So if you want to use the output of any step you have to give an ID to it. You say ID colon and you just say login hyphen is here. Now this name we are going to reference in later step. And this is that next step looks complicated but it's simple. Let me explain. First the name. Now in this one step we are going to build the image, tag it and push it to Amazon ECR. All 3 in 1. And for this one we are not going to use any GitHub actions. There are, but let's do it through run. Okay first of all ID build image. So we have given an id a name to the step which we can reference later. Env environment. So to store these variables this will be in the runtime. So ECR underscore registry. This should get you the name of the registry not just name. Actually it's going to be the complete URI except the repository name. So repository this is the complete URI account name dot ECR. This entire URL slash v profile hyphen app image that is the repository name. So you get it till here. That will be your registry. If you're not using ECR, this will be Docker Hub. If you're using Google Container Registry, it will be your Google Container Registry information. So how do we get that? Well we say steps. So that gives access to the steps. And the step name is dot login hyphen ECR. So basically this step from that dot outputs. So when we log in it returns an outputs. And in that there is a key called registry that will have the URL or information about your registry that we are storing into this variable. Now yes, we can just set a variable and assign the value to it. But I wanted to show this to you how in runtime you can access the information and store it into a variable, and then you can use it. Image tags. The Docker image. We have the name colon, and we have a tag like the version of the Docker image that we are assigning it as this variable github.sh. Shh. This is going to get the latest commit ID. So this gets the latest commit ID. It will be very long and it will be storing it into this variable. So this environment is basically to assign value to these two variables which we are using over here. So run basically just runs the command. So when it creates this ubuntu latest VM or container in that it is going to run the command the shell commands. And the pipe here represents we are giving multiple commands at once. So in that case you give this pipe. In the next line you give all the command in one by one. So the first command is docker build hyphen f. You give the path of the docker file that we have created in the previous lecture. So Docker hyphen files in that you have app in that you have multi-stage. And that's the Docker file. So you give the complete path and hyphen T is to tag your image to give the name. So Docker ECR registry which will be this Slash ECR repository. Now this variable we have not defined. We are going to define it in the workflow itself. So that way you can also see how to define environment variable in the workflow. So let's go scroll up over here. Just before the permission let's say env colon. And in the next line let's define the variable ECR underscore repository. Now use it rarely do not usually define the variables in the workflow. It's better you store it into the GitHub secrets and vars instead of putting it over here. But just a use case I'm building so you can see that you can define variables here also and then use it. So I'm going to give it into single quote. Let's get the repository name that is v profile hyphen image V profile hyphen app image. That's the name that I have given. So I'm storing its value into this variable. And then we are simply referring it here. Over here with the repository name. Now for the repository name. So once again docker build hyphen f, the path of the docker file hyphen t to tag the image Docker image. So Docker image is always tagged like this. You give the registry name, slash the repository name colon the image tag docker image tag. So over here this is going to generate the whole URL which is this one. And for the tag we are using the latest GitHub commit. Over here we are storing this variable value. And at the end you have a dot that represents current working directory. That means run this docker build command in the current directory. Current directory means over here in the top level root directory. If we do not specify that dot, then it is going to get into this folder and execute the docker build command, which is what we don't want, because in the docker file we have mentioned copy all the content and we want this content to be copied into slash app folder of the container. So we can build the artifact. That's why we want our Docker build command to execute in this folder. So it gets this all these files and copies into copies it into the Docker container okay. Next is Docker push. So we simply say docker push. And we give the here uri colon the image. So once it is built with this name it's going to simply push it to ECR registry. Because we are already logged in over here to the ECR. And then the last line is just used. So we can refer its output in the later step. We do not have any later step. So we can simply remove this also. So its output. You want to store it into a variable. So if we deploy this container to container running service like ECS then we can refer to that. But we are not doing it over here. So this would be enough. Now it's time to test it. So save this file. Make sure you check the source code from this lecture resource. From this lecture resource you can get this file. Compare it. Make sure you don't make any mistakes, any typographical mistake. Put it into your main.yml file, save it and say commit and push. Give a commit message. Build and publish. Job added a commit message and accept the commit. Now let's take a look. Okay. The workflow is triggered. Let's wait until this completes. All right. My pipeline or workflow got completed successfully. Let's look at the build and publish job. So we have configured AWS credentials that work. Login to Amazon ECR. That worked. Everything worked. The build attack. This will be a long output huge output. Okay till here it builds the docker image and then exporting to image layers here. It's pushing it to the repository you see constructed properly the repository URI. If you go to container registry let's go to our repository. We should see the image. So you see the tag. This is the tag that is the GitHub latest commit ID. If you want we can check it. Let's copy this. Let's go to GitHub. Okay. Here you can see see that commit ID three b. Let's click on that okay. He's just showing the short version of that commit ID there. There is a button to copy the full. Copy that Ctrl f paste it here. You see that it's exactly same right? Then our workflow got completed successfully. Now if you want you can just go to workflow. Click on your workflow name and you can also run it manually. Run workflow because we have workflow dispatch in our pipeline as the event trigger, so we can run it manually. Also take a look at your source code once again. Your workflow file. Make sure you understand all these steps, all these settings. Now, no matter what kind of workflow you set, more or less you're going to use these kinds of steps like environment storing the variables, secrets, conditions. So I've tried to include as much as I could in this small amount of time. And don't worry, you don't need to stress too much. You have now the AI tools which can generate all this code very easily for you. But once you have knowledge on these basics or commonly used steps, actions and settings, then you can easily generate this kind of code or more complicated code by using coding assistants like GitHub, Copilot, Amazon Q, or even. If you want, you can use ChatGPT in the projects. We are going to integrate more tools to continuous delivery also, and many other settings will see in GitHub actions workflow all that in the projects. But that would be all for now. I'll see you in the next one.
