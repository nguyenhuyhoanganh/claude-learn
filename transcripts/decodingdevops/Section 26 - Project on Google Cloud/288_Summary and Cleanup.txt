# Summary and Cleanup

Welcome. In this lecture, we are going to run through the entire flow of this project and do the cleanup. Okay, so the high level flow. First we completed the VPC setup, then we worked on the backend services Cloud SQL and memory store, connecting this with VPC pairing. So instance in the private subnet of our VPC can connect to the backend network of GCP. In third part we worked on the managed instance group that creates and manages our Tomcat vPro profile application in the private subnet with the load balancer in the front end. Finally, we created HTTPS secure connection from our load balancer, which forward the request to the load balancer backend managed instance group. So VPC backend with VPC pairing, managed instance group with load balancer and HTTPS secure connection for our load balancer front end. And the fun part is we did all this through command line, through Google Cloud Shell. Now, we could have done this all through UI also, but here's the benefit. One, you learn the command lines. You can know how to operate GCP through command line. And once you have all the commands, it's very easy to convert this to a Terraform script by using any AI code assistance like GitHub, Copilot or Amazon Q Developer or any other AI assistant tool. And this is what I recommend all the time. First, learn how to complete your entire project set up through command line or manually like we did through Google Cloud Shell one command at a time. This is manual. Once you have mastered the entire flow of your project, then use Terraform to manage your entire cloud infrastructure, which is infrastructure as a code. Now, I want you to go through all the architecture diagrams with all the commands that we have used in this project. Go through this VPC script, then backend, then frontend_1.sh, then frontend_2.sh. Take your time, do not rush. Once you are done, we can do the cleanup process. Also go through the user interface of Google Cloud and verify all the services, everything that we have created or used, and then we can do the cleanup process. So pause this recording, go through everything, and then resume the lecture. Okay, so if you're done, we can do the rollback. And for rollback, I have created the script that you can use or you should use actually, because I don't want you to have some services left over or running that eats up your credits. So the script sets some variable that we're going to set in the in the Google Cloud Shell, like your project ID, region, subnet, names, router, NAT gateway, bastion host and everything. And once those variables are set up, we can start deleting. So rollback will be reversed. VPC will come last. First we need to clean up the forwarding rules related to HTTPS, walk on the load balancer clean up. Then the managed instance group. So if we delete managed instance group, it'll take care of the instance deletion and everything. Then we delete our golden instance. Well, we already have deleted it, so this command is just going to run and won't do anything. Then we clean up the cloud DNS records where we map the cloud SQL and memory store IP to the name that we're going to do the cleanup. Then removing the backend services Cloud SQL and mem cache. And this is going to take a very long time. And there's some condition over there. Once the instance is, the mem cache and SQ is completely deleted, then only it's going to move forward, delete the bastion host. Then we come to the final phase of cleanup VPC starting with firewall. We remove all the firewall rules in the VPC. Yes, firewall rules are part of VPC. Then delete the NAT gateway and the router, delete the subnets of the VPC, all four subnet, and then delete the private service access range. Now, we have created this range so we can allot IP from this to our backend services Cloud SQL and memory store. And this was for the VPC peering part. And once you delete this, this is not going to clean up instantly. This will take some time. Google will hold this range for some time, and because of that you won't be able to delete the VPC itself. So we might need to delete the VPC manually after we delete this, which I'll show you how to do. So once we remove this range, we are going to remove the certificate and SSL or DNS authorization. We're going to do this cleanup. And then after waiting for some time, we are going to delete the VPC pairing and the VPC itself. Now, as you see, I have commented this because this won't work instantly, and you can remove this later also after a few days also. That's fine because this VPC is empty, just a range. So no money will be deduct deducted from your credits. You have $300 credit. So let's open the Google Cloud Shell and fire the script. Okay, I'm in my Google Cloud Shell. I'm going to run gcloud auth login once again. I'm already logged in, but I just wanted to make sure. Okay, that's done. Now I'm going to set my project. So gcloud config set project and my project name. Okay, run the command gcloud projects list and get the project ID. Now, that's my project ID, same name as the name of the project. And then let's work on the variables. So open rollback.sh script, make sure you put the right variables. So that's my project ID. Region, I have the same one, us-central1. And the zone, it is setting as the same, -a. That's my VPC name. So check all your variables, compare with the variable that we have set in the previous set of scripts. If you have not made any changes to these variables, all this will be same except your sub-domain and domain. And mostly I think you would have say set the same sub-domain, but just check your domain. That would be definitely different. So important things to check here is the domain, sub-domain and your project ID. If you think everything is correct, then just copy this entire script. Come to Google Cloud Shell, open up a file. Let's name it as rollback.sh with Vim editor. Get into insert mode and paste everything. You can use Shift + Insert and see when click this file. Escape column WQ. Let's give it executable permission. And let's start executing this script. Now, this is going to take time, lot of time, but don't take any break. Just observe what all it is deleting. So pause the lecture and resume when your deletion is completed. Okay, so the rollback completed successfully, except I see one or two error that is about this vprofile-golden. This instance, as I already said, we deleted this, so this is fine. And the last one, this error was about, yeah, that's the echo statement, and that was not completed properly. And so that is also fine. So overall it's completed. Let's check about the VPC and the VPC peering. We need to delete that manually. So let's go to VPC. VPC networks, vprofile-vpc. And this we take a look here at VPC network peering. Here, we should see this peering. We have to remove this. Delete. Let's see if we can delete it. Okay, nice. This is deleted. Now let's delete the VPC network itself. Make sure you are deleting your vprofile-vpc. Click on Delete VPC Network, give its name and say Delete. And that is all. Now, if you wish to anytime, revise this entire project, you can go through the architecture diagrams and go through all the commands and you would be good. Just to have good practice, just do the project once again, but do not try to memorize any command. It's not necessary. Understanding the flow and the commands will be more than enough. That would be all in this project. I hope you have enjoyed it. I will see you in the next one.
