# Configuring Cloud SQL & Memorystore

In previous lecture, we completed the setup of VPC Bastion Roast roast host. Now I'm not roasting you slip of tongue. So bastion host. And now we are going to create the backend service Cloudsql Memorystore. And do the VPC peering with the VPC that we have created. So let's get into it. Okay so let me go through the flow first. So we are going to create PSC which means private service access. It's basically range of IP address. As you see the command says gcloud compute addresses create the name of the range. It's global. The purpose is VPC peering. And it is for this VPC. Now backend service will take the IP address from this range. Then the VPC peering that is going to connect our VPC to Google services. In our case the Cloudsql and Memorystore. So gcloud services VPC peering. Connect this VPC or VPC with the Google networking services and choose the IP address from this range that we have created earlier. Then create the MySQL on cloud SQL service. So gcloud SQL instances create this. Like in AWS we create RDS. So we give the MySQL version. We give the instance type db f1 micro very small region. And we say no assign IP, do not assign any IP address because the IP address will be given from this range that we have created earlier. Now this is the range that Google will allocate. And from that range IP address for this MySQL instance will be selected. Network or VPC. Now yes we can give this dollar VPC. And this is another way projects slash your project ID global networks dollar VPC. That's the VPC name basically. So this creates the cloud SQL with MySQL eight engine. And then we are going to create the accounts database in that that is required for our project V profile project. We have seen this earlier. Many times we set the root user's password for the MySQL instance. And then we are going to launch the Memorystore memcache. And once we are done with this, we have to log in to the bastion host and initialize our database that we'll see now. So let's get started first with allocating the IP address gcloud compute address create. Create a range for this VPC. Okay that got created. Next command we do VPC peering. So gcloud services VPC peering connect or VPC with the Google backend services and select the IP from this range. Okay this is going to take some time okay. It says operation finished successfully. Let's go to VPC network. VPC networks go to VPC Network Peering and you should see the peering over here. Or you can check it from here. Also you can go to VPC networks. You can select your VPC you see here tab VPC network peering. You can see it. And you can also see here the private service access range. So that's the range that Google has given us. So that's the name that we have given. That's the range service producer VPC peering names. Now let's launch the cloud SQL instance and select this range. So gcloud SQL instances create dollar db. That will be the name of our instance db instance database version MySQL underscore eight underscore zero tier db f1 micro. Very small instance region US central one. Do not assign any IP address because we are selecting IP from the Google PSA range that we have created right from this range network, you give the VPC name or you can just do VPC also here. Now this one is going to take a lot of time. So take a break ten minutes and then come back. Okay. The instance is ready. In console. Just search for cloud SQL and you should see the instance ID, the type and the IP address which will be from the Google PSA range. Now we need to connect to it. Create a database called accounts and set the password for the root user. So our V profile application will be using this file Application.properties to connect to backend. Here we have the name which will later create map it to its IP address in cloud DNS. And we need to have the username as root. And this is the password that is already set there. If you wish to change the password you can do that. But then you have to make sure. Then you set change the password in application.properties file also. But for now, keep it same. Let's create the database named accounts. So gcloud SQL databases create the name of the database accounts. And for this instance db which is our v profile db instance. This one. This will create the database named accounts. Next we're going to set the password for the root user. So gcloud SQL users set password for the user root space backward slash and give these options hyphen hyphen host equal to percent. That means allow from remote. So this root user can access remotely this database. So because our V profile application will be in the VM instance, from there it is going to access the database. So that is a remote access instance. The DB name which is v profile db and the password that we already have set in the variable. Let's hit enter. Okay, this is done. Now let's create the memory store memcache. So the command to create memcache is gcloud. Memcache instances create. And the name of our memcache instance that will be v profile hyphen memcache space backward slash. And we have to use few options. Region will be the US central one a hyphen hyphen node hyphen count equal to one because we want to keep it minimum space backward slash now minimum we need to give two CPU and two GB Ram for memcache to get created. So hyphen hyphen node hyphen CPU equal to two and hyphen hyphen node hyphen memory equal to two GB. So two GB Ram and two CPU. And we need to give the VPC of course. So space backward slash hyphen hyphen authorized hyphen network equal to. In double quotes. You need to give the complete path of your VPC like this. So projects forward, slash the project ID slash global slash networks and in that your VPC name. And with that information let's hit enter. Okay. This one will also take a lot of time to get created by the time we have some work. So we need to create schema in src main resources. We have Dbbackup dot SQL file. We need to run this against this SQL queries against our cloud SQL instance. So for that we are going to SSH to the bastion host. From bastion host we are going to access the cloud SQL which is in the private network. So get your bastion host IP go to VM instances bastion host public IP okay not private IP. Go to the folder where you have already created the key. Such hyphen I. The key name user is DevOps at the rate and the public IP of our bastion host switch to root user. We need to install MySQL client so need to run the command apt install mysql client hyphen y to install mysql client. Then we get the MySQL command. By using that we're going to connect to our cloud SQL instance. Okay let's clear the screen. So MySQL hyphen H the IP address of your cloud SQL instance. So search for cloud SQL here. And that's the private IP. Now we did VPC peering. If we are able to connect to our MySQL instance VPC peering will also be validated. So the IP address space hyphen u. The username is root. And we did set a password hyphen p. And we set the password for the root user. And the password value is in the variable. You can find it there. So copy that. Now exposing like this DB password is very wrong. So instead of giving the password like this in the command line, just say hyphen p space and give the database name accounts that we have created, hit enter. It will ask you the password. Then you can paste your password here shift insert. So we are able to log in and connect to the accounts database. Exit from here in backend.sh script. At the end you have this wget command and the link the GitHub link to download the db backup SQL file. Now this is nothing new. This is what we did previously. Also we initialize the database. So it's the same link except here the branch is GCP. So download it. Use the up arrow to go back to the command input redirection path of Dbbackup SQL file. Hit enter. Enter the db password. Okay now let's log in once again. And you can say show tables. Semicolon. And you should be able to see the table in accounts database. So DB is initialized. We're done here. Let's take a look at the memory store. Now this might get created or might not because it takes time as I told you. But for me it got created in the search. Over here you can search for memcache and find memory store for memcached and you should see your memcache instance. That's the IP address. You see the port number one node, two GB Ram, two CPU, and in the zone the region US central one. So our backend is already and all set. We configured the VPC peering from our VPC to the Google backend services. We created cloud SQL. Initialize the database. We created Memorystore. From the next lecture onwards we are going to work on the front end part. So wrap this up and join me in the next one.
