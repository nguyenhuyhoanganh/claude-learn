# Setting Up Cloud DNS

All right. So we have the back end services ready. And our app server which is in the private subnet will connect to these backend services V profile application. It's not yet deployed. But if we check the configuration of V profile application which is in src main resources application.properties file, we have an endpoint of MySQL and we have an endpoint for memcached service. Now this should resolve to the IP address or private IP address of Cloudsql and Memorystore service. Now this is internal resolution. So for this purpose we need to create a DNS entry. We need to create a private hosted zone. With this domain we profile dot internal. And in that we are going to add entry for DB host resolves to the IP address of cloud SQL instance and Wipro MC to the private IP address of Memorystore. So in this lecture first we are going to create a hosted zone private hosted zone with this domain. And in that domain we are going to add these two entries, the Pro DB and Vipro MC. Okay. So we have these two variables private DNS. Its value is V profile, dot internal and private zone. Its value is V profile hyphen private. So this will be the name of the hosted zone. But the domain is this v profile dot internal under this VPC. So let's create a private hosted zone in cloud DNS servers. So the command is gcloud DNS managed zones create and the name of the zone which is V profile private. This is just the name but the domain name will be v profile dot internal. So we're going to give here space backward slash. Hit enter and enter this option. Hyphen hyphen hyphen name equal to private DNS space backward slash. Hit enter. Mention the VPC network hyphen f and networks equal to VPC space backward slash. So domain can be private and public. We just need it for internal resolution. So we just need to prove it. So we need to give this option here. Hyphen. Visibility. Equal to private space. Backward slash. And let's give some description. Hyphen hyphen description equal to private DNS for v profile. And let's hit enter. Got created. Now if you go to cloud DNS service in cloud DNS service you should see a zone V profile private and the DNS name is V profile dot internal. And here we can create the record again. We're going to do it through command line. So let me run you through the flow of how we're going to create the record through command line. First we need the IP address of database and memcache. Now this is the command. Or you can just go to the console and get the IP address. So gcloud SQL instances describe the name of a DB format. Now this returns the output in JSON format. And from that we are extracting IP address. I'll just run this command and show you the output first. So when you describe any service like SQL instances describe and the name of our database, we run this and we get a huge output by default showing in YAML format. So from this we need to extract this one. This IP we need to extract. So it is in IP address. It's in the top level. Inside that there is a list. It's the first element in the list. So zeroth element and its value we're going to extract. So like this. If we want to extract any specific value you have to do hyphen hyphen format equal to. In double quotes we say value value of IP addresses zeroth element from that. And in that you have a key called IP address. So when we run this you get the private IP address of database. So if you know JSON and YAML or any one of it, you should be able to easily extract the values from the output of describe. Okay. So let's store the IP address of memcache and database into these variables. So we run this command in command substitution and stores this into this variable. So if you do echo dollar DB underscore IP. We should see the database IP. And if we do MC underscore IP we should see the memcache IP. Okay. So once we have the IP. So this is the flow. First we need to start a transaction. And this command creates a YAML file in your current working directory. And then we run the command to add the record in that transaction. So we say add this IP in this with this host name in this zone. And of course the project ID. So we start transaction, we add the entry. Then we execute the transaction. So starting transaction it creates a YAML file. And we add the entry into that YAML file. And it simply uploads that YAML file into the records. And then we list it over here same. We are going to do it with memcache. Start the transaction, add the entry, execute the transaction, and then list it. So let's do it one by one. So gcloud DNS record sets transaction start. For this zone We provide private and the project ID hit enter. You see transaction dot YAML file. You should see a YAML file in your current directory. Okay. Next we add the entry into that file. So gcloud DNS record sets transaction add db ip which is this with this name with db dot private DNS which is v profile dot internal and it's name to IP mapping. So the record type is a a record in the zone private zone. This one V profile private and the project ID. So nothing changed on Google Cloud. Just updated the transaction dot YAML file. Next is we execute the transaction which will add this entry. So gcloud DNS record sets transaction. Execute the zone name and the project ID. And it's done. It says the status is pending. Let's check. Let's list it. So gcloud DNS record sets list zone private zone. So you should see all the entries the previous entries also. And you see V we pro DB and it's private IP address. Okay. So once you understand this you can add any entries. Let's start the transaction once again and add the memcache entry. So same command start the DNS transaction. Add the record. So same command we change here the MC IP. And here we just give the MC. So you can just do up arrow and change these values. And then simply execute the transaction. Same command as the previous one. It's done. It's in pending state. Now let's list it. So you see the entry. So both the records are added. You can see it from the GUI also. Let's refresh the page. All right. So in cloud DNS in your private zone you should see both the entry with DB and with MC. And it's a record because the name maps to the IP address. And that is all in this lecture. In the next lecture we are going to start working on the app server. So join me in the next one.
