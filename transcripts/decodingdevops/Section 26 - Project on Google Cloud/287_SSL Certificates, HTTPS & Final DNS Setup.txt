# SSL Certificates, HTTPS & Final DNS Setup

Okay, so this is so far the flow that we have created for V profile a front end. Now when I say V profile front end and back end here the V profile backend means our database or memcache service or private subnets. These are the backend V profile. Front end is all about it's load balancer and the app server of course which receives the request. And then V profile front end which is the load balancer to application server connection in the in this front end also we have front end and back end in load balancer backend means simply the managed instance group, which is basically just the collection of our instances. So when the request comes from the client, the load balancer will have a forwarding rule which says that the request comes on port 80. Then it should go to HTTP proxy. And for HTTP proxy there will be a URL map created that for this HTTP proxy route. The request to this backend service that basically means our instances or V profile application running on port 8080. Okay, so from internet to HTTP proxy and the URL map, which maps the proxy to the backend backend of the load balancer that sends the request to our instance, which is in the private subnet. So in production in real time we definitely have a URL that will be Https secure connection. So that URL should resolve to the IP address of the load balancer. So we need to create an entry in GoDaddy. And if Https is involved there needs to be a valid certificate that proves your domain in my case. HGH infotech.xyz. This should be proven. So a certificate authority like Google or Amazon or CERT Manager. There are many authorities like that we sign. They prove the validity of your domain. But we are using Google. So in GCP itself we can create a certificate validate it by making the entry in our domain registrar like GoDaddy. It verifies our certificate and that certificate we can use for the Https connection in the load balancer. So the first thing will be to create the certificate and make sure it's validated. Okay. So my domain variable holds this value in HGH infotech.xyz. Your should hold your own domain and subdomain. I have kept as GCP you can keep the same or you can change if you want. Just edit bashrc file and make the changes if it's required. So we need to create a certificate in certificate Manager service. So in GCP first we need to start the DNS authorization that you are the owner of this domain. You have to prove that. So I need to prove that I'm the owner of this domain. And this is how we do it. So I say gcloud certificate manager DNS authorization create or hyphen. The subdomain given here and domain will be this. So if you run this command this should create the DNS authorization with the name auth hyphen GCP. And let's describe this now The same command. Instead of create, you say describe. Okay. Now take a look here DNS resource record. So this is the challenge. This is the name. And this should resolve to this. If you are able to prove this then it is going to give you the certificate. So I'm going to go to GoDaddy I go to my products. So if you have if you're using GoDaddy you can follow the same step or just use your domain registrar. Go to your domains. Sorry your domains DNS record. Go to your domains DNS record. So this is my domain. I go to domain. Go to DNS, click on Add New record. And this is going to be CNAME record because name to name mapping. And the name will be this underscore Acme hyphen challenge what you see over here before the domain name. Copy that. That should be the name. Make sure there is no dot and the domain name here in the value you need to put this data string. So copy this and paste it here in the value. Make sure you remove that dot, that full stop at the end and click on save okay. So the DNS authorization entry is done but the certificate is not yet created. We're going to create the certificate. So in front end Touche. We have this command that is going to create the certificate for us. So gcloud certificate manager certificates create certificate with this name cert hyphen the subdomain I'm giving and the domain for star dot domain like my domain in this case infotech.xyz. The certificate will be also for the subdomains DNS authorization we just created. So this is the DNS authorization is going to check. It's going to check from this that if Acme challenge resolves to that endpoint. So with this command it's going to create the certificate. So certificate is created in few minutes. We should see the certificate as active. But by the time we are going to create a certificate map entry that is basically just a placeholder to keep this certificate from where we can choose it in our load balancer. So these two command and we are done. GCloud certificate manager maps create. This creates a map with this name and add our certificate in that map. So let's execute these two commands gcloud certificate hyphen manager maps create map and the map name. Once again this is just a placeholder to keep our certificate. So we created the placeholder. Let's put our certificate into this. So gcloud certificate manager maps entries. Create this entry for this map which we just created. And add our certificate in that cert hyphen sub domain that certificate. And sorry of course the hostname is there. Okay this is done. Let's describe our certificate. If the time is passed, we should be able to see it in active state. Okay. Let me scroll up. So gcloud certificate manager certificates describe or certificate name. So you should see the status authorized and status active. And now we can start using this certificate. So in the previous one we created the HTTP proxy and the forwarding rule to create https proxy. We need the certificate. And the certificate is active and authorized. So now we are going to create Https proxy. Create the forwarding rule. Add our load balancer IP in GoDaddy and we are done. So that's the command gcloud compute target https proxy create. Connect it to the URL map. Mention the certificate or basically certificate map and then create an Https forwarding rule. So let's wrap this up. Okay so let's create https proxy with this name V profile hyphen https proxy. The URL map that we already created. URL v profile hyphen an URL map and the subdomain. So once again that's the command gcloud compute target https proxy, create proxy name url map, and the certificate certificate map. I should say we created a certificate map so we can just give the certificate map name. Okay that got created. Now let's finally create the forwarding rule okay. So this is the command gcloud compute forwarding rules. Create create forwarding rule with this name target https proxy is this target https proxy. Is this the profile hyphen https hyphen proxy that we just created for the load balancer IP. The static IP that we created is going to take some time okay. Got created. Now let's get the load balancer static IP and add this entry in the domain registrar in GoDaddy Domain registrar. So gcloud compute addresses describe the load balancer hyphen hyphen global. And that is our IP. Let's copy this IP and let's create a record. So add new record. And this is going to be of type A. Give the name vpro GCP and the value load balancer IP. Save it. Okay. You need to wait for a few minutes or maybe an hour sometimes. Once this record is published you should be able to access it on https, vpro gcp dot your domain name. You should see the web page fully functional. You should see the certificate validity. Connection is secure. It says and you can check it's issued by organization Google Trust Services. So that completes the entire flow. Let's talk about it in the next lecture. Let's run through the entire flow and then we'll do the clean up. So join me in the next one.
