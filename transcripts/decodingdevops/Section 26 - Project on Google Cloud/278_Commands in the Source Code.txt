# Commands in the Source Code

All right. So let's open the VS code or any of your favorite editor that you want to use. Go to Source Control. Click on Clone Repository. Go to our V profile project repository github.com slash v profile hyphen project and copy the https link clone repository. Enter the URL and clone from GitHub. I'll just hit enter. Select the folder where you want to save this. I have created a folder in F drive called as GCP underscore v profile. I'm going to select as repository destination. So by default you will be in the local branch. So click on this branch over here and search the branch says GCP and click on that. Now here you will see some script. Don't worry we're not going to execute directly the script. We're going to execute one command at a time. And we understand it and then we run it. But I have divided this entire project into four parts. The first one will be the VPC, then will be the backend, then front end, first part and then front end second part. I'm going to go through it. So first let's go to VPC. Here are some variables that are mentioned. And before you run all these commands over here one by one you need to set all these variables values first. So these variables you need to edit project ID that will be your GCP project ID region like I'm using US central one. Over here you can use the same one app name I'm keeping it as V profile. You can keep the same domain if you have purchased a domain or if you have a domain, you can set this value. Or if you do not have just fake some domain name if you have purchased a domain and if you want to see the Https connection the production style, then use your domain name over here, I'm going to show you what I'm going to set a sub domain. Now I recommend you keep the same one. This will be for your final public URL. So for me it will be like this V profile v Pro gcp.info dot x y z. This will be the URL of the website. My IP. Now here I have set all the IPv4. This will be for the firewall rule, like in AWS security Group we mentioned there. Right. SSH from my IP. Here we are going to do the same thing. So initially I recommend you keep the same. Later we are going to edit the firewall rule and make sure your our own public IP. We mentioned their SSH key. Here we need to mention the public IP key content. Now in AWS when we launch an instance we attach a key pair right. We create a key pair. We attach it when the instance launch. But in GCP that's not the case. There is no such option of creating key pairs. If you want to do SSH to your instance in GCP, you need to make sure before you launch or while you're launching it, you put your key your public key in that. So what we are going to do we're going to generate a key pair while we launch the instance. We're going to inject a public key in the instance and the private key we're going to use to SSH to our instance. Okay. Database password. Now I recommend you keep the same. Otherwise we need to edit the application properties file also. So if you keep the same you don't need to make any changes in application properties file, but if you wish to change it, make sure you remember to change it in the application properties file. Okay, now I know this will be too much initially, but all you need to do is you need to find the variable value and set it in this lecture. Once we execute everything, you will understand the entire flow and everything. Okay, so the other variables just go through them. The public subnet names, the bastion host and all the other names. You don't need to memorize these things. The only reason I am using variable is because you and me, we both can execute same commands. Otherwise, there is a huge chance of making mistakes while you execute the commands. But if we set the variable, then you and me, we both can enter the same command and how we are going to set these variables and all. I'm going to show you that. So what are you going to do now is first go through all the variables and then go through all the commands one by one. Now you don't need to learn or by heart anything over here. Just go through and understand the flow of the execution. We are going to execute this gcloud commands. So focus on this gcloud command. Go through them one by one. See how much you can understand. You don't need to decode the entire command, just understand the flow. Like over here we set the project, then we enable some APIs. Then we create a VPC. Then we create a subnet. So like that go through the flow look at the variable names. See the values that we have set over here. Compare them. Take your time like that. Go through every script first VPC then back end, then front end once then front end two dot. From the next lecture onwards we are going to execute these commands. We're going to type the commands, understand them, look at its output from Google Cloud Console. Like when we create the VPC, we're going to go through the browser Google Cloud Console and see the VPC that it creates. So basically we understand the command we execute it. Then we look at its output like that. We're going to go through each and every command to understand the entire flow. So I'm going to stop. Now you can go ahead and go through this these scripts I'll see you in the next one.
