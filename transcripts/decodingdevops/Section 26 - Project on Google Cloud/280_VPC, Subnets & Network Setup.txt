# VPC, Subnets & Network Setup

Okay. So the command to create VPC is gcloud. Compute networks. Okay. Wait. Computer not computer compute networks create. And we need to give the VPC name. So I'm going to use the variable. So double quotes dollar VPC. So that's the variable that we have set already in bash rc file. And this is going to be a custom subnet VPC. So we have to give one option here hyphen hyphen subnet hyphen mode equal to custom. And just hit enter. It's taking some time. Okay. So that is done. It says the instances on this network will not be reachable until firewall rules are created that we are going to set later. So you're saying basically how you can create the firewall rules and allow the protocol and source range. We're going to do that later. But let's just go to another tab in our gcloud shell. Sorry. In the Google Cloud Console right over here where you see this hamburger symbol. Click on that and find here VPC networks and go to VPC networks. If you scroll down a little bit, you should see here a default VPC and VPC that we have just created. Okay. Right now there's nothing in this. We don't have any subnets or firewall rules or anything or no routing configured. What a VPC is created. Now we are going to create four subnets in this VPC that should appear here in the subnets section. So let's create our four subnets. Okay. So the command to create the subnet is this gcloud compute networks subnet create and the subnet name. So in double quotes we are going to give the variable. And this is the variable that we have set. Pub underscore subnet underscore zero one all in caps. And we have to mention a few options. So give a space give a backward slash and hit enter. So we go in the next line and we can enter the options. So first option is in which network you want to create this subnet. So hyphen hyphen network equal to the VPC name. So we have the variable dollar VPC. And then we need to mention the region. So give a space backward slash hit enter. So mention the region hyphen hyphen region equal to again the variable dollar region. Now remember in creating VPC we did not mention the region. But in creating subnet we have to mention all those things. Okay next will be the range. So give a space backward slash. Hit enter the subnet range. So VPC range I'm taking as this 172.2.0.0 slash 16. So that gives us so that range gives more than 65,000 IP addresses 65,536 to be specific. From that we create a subnet. So first subnet which will be the public subnet. We're giving this value 172.2.1.0 slash 24. That gives total 256 IPS. Now in GCP five IPS will be reserved. So total 251 IP address you'll get in each subnet. Now in decoding DevOps course in VPC, I have explained all this in great detail. If you're having doubts with that, you please go through VPC in decoding DevOps and you will understand all this. Okay. So coming back to the range. So you have to say hyphen hyphen range. And you give the subnet range. So for public subnet one I'm giving this range. Let's hit enter. Okay I made a mistake. It's subnets not subnet. I forgot to add an S there. Okay. So you just use upper arrow and make the changes. If you also made the same mistake. Okay. So that subnet is created now like that let's create the other three subnets. So I'm going to just clear the screen use the up arrow. Now here I'm going to change it to 2.0 and change the variable name over here to zero two okay. Make sure you give the right variable names. Otherwise will be confused later. Okay. So that is the second subnet. Okay. That is created now the third subnet which will be the private subnet one. So dollar proof underscore subnet underscore zero one. And this is the range 3.0. Okay. Last one that's going to be 4.0. And the variable make sure you change the variable names. It's going to be 025 subnet underscore zero two. And the range is 4.0. Do not rush. Make sure all the values are correct. All right. So the fourth subnet also got created. Let's check. So go to VPC networks and go to your VPC. Click on subnets. You can refresh this okay. You should see here the region name. You should see the subnet name. If you see any differences. Any mistake like this is public one public two private one private two. You see anything else you see a different range. Just delete that, select it and delete it and execute that particular command. Just keep in mind you need to have these four subnets with these names and these exact ranges. If you see a difference delete them and create it again okay. So the VPC is created four subnets are created. So what is remaining? That's right. Cloud router, cloud Nat and firewall rules. So let's create cloud router and cloud cloud Nat. Pretty simple two commands and that will be ready. So first we create the router for our VPC. So the command is gcloud. Compute routers create compute compute routers or routers create and give the VPC name. Sorry for the router name of course. So double quote dollar. And the variable that we have used is dollar router. So along with this you need to mention the VPC and the region for which you are creating this router. So space backward slash hit enter. So hyphen hyphen network equal to the VPC which again the VPC variable space backward slash hit enter and the region. So hyphen hyphen region equal to sorry not regions. It's region equal to the variable dollar region okay. That should be it. Let's hit enter okay. That was quick. So the router name is V profile hyphen router. That's the value of this variable. And that's the region and that's the VPC. Let's take a look in the console. So just search for cloud router. Cloud router. Now this is not you won't see this in the VPC like in AWS VPC section. We see everything here. Here the routers are in network connectivity section. So you should see your router with the name V profile hyphen router. You should see the VPC name and the region. Now in this we are going to create a Nat gateway that is going to provide internet access to our private subnet. So come back to your shell Google Cloud Shell give the command gcloud compute routers Nats create and in the double quotes give the variable name dollar Nat. Along with this we are going to provide some options. So space backward slash hit enter first the router on which we want to create this Nat gateway. So hyphen hyphen router and the router name Space. Backward slash. Hit enter. Next thing will be the region. So hyphen. Hyphen. Region. Space. Backward. Slash. Hit enter a Nat gateway. Need to have a public IP so it can communicate to the internet. So we need to allocate it a public IP. We can give that. We can allocate that by giving this option hyphen hyphen hyphen allocate hyphen hyphen external hyphen IPS. I no long option. But what it does, it takes the public IP and attach it to your Nat gateway so your Nat gateway can access the internet space backward slash hit enter. The next option is pretty interesting. Hyphen hyphen nat hyphen all. Hyphen subnet hyphen ranges. Subnet IP ranges. Now what this means is provide natting to all the private subnet. But right now we just have subnets. And there is just the name to the subnet private or public. So how does it decide which is the private subnet. So any VM Without public IP, it has just the private IP will be considered as the instance in the private subnet. Yes. If the VM has the public IP in the private subnet, the private subnet is not private anymore. It just becomes public. So here it's all about configuration. While you are launching your instances that we'll see once we launch the instance. Okay. Now you can hit enter here. All right. So the Nat gateway is created or cloud Nat is created. So in the console let's just search for cloud Nat. Cloud Nat is in the network services section. It's not in the VPC but that's fine as long as we can just search our services we can find it. And also just take a look. Load balancing is also in this network services section. Also the cloud DNS. This is what we will be using. Load balancing cloud DNS. Okay so as far as the VPC is concerned we're done here. The remaining thing is the bastion host and the firewall rules that we're going to do in the next lecture. So wrap this up and join me in the next one.
