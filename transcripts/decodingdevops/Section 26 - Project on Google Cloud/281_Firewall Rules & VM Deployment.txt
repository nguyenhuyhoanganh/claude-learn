# Firewall Rules & VM Deployment

So we created VPC and for subnet with Cloud router and cloud Nat. In this lecture we are going to create a firewall rule for the bastion host and then create the bastion host. This VM instance will be in the public subnet. And then one more firewall rule that will create for the instance which will be in the private subnet. The instance that you see in the private subnet will be our app server, where our view profile application will be living. And that app server will create in next lecture in this lecture firewall rules and bastion host. Now here GCP is very different from AWS. In AWS we create security group. And then we launch the instance. We just select the security group. In GCP we create firewall rule which is like the security group of AWS. And we give a source tag over here sorry target tag. Target tag we give a tag. And then we launch when we launch the instance. While launching the instance we mentioned that tag. So any firewall rule that has that tag will be attached to the instance. So there is no direct attaching. It will be attaching through the tags. So that's one difference. The other difference is in AWS we have key pair. When we launch the instance we select the key pair in GCP. That is not the case if you want key based login while launching the instance itself. We are going to execute some commands through userdata script. In GCP it's called as automation script. And this is the script we will execute. We are going to create a user and that user into Sudoers file. So we get sudo access and inject the key into that user's home directory in SSH authorized keys file. So we already created a key pair. So we're going to use the public key and insert it into this user's home directory. And we can use the private key to log into this instance with this user. So here's the username is DevOps. We can change this one. So let's get started first with the firewall rules. So open your gcloud shell. First, I'm going to list the variables that we will be using for the commands. Okay. So these are the variables. We will be using the SSH key that we have already stored my underscore IP. Now this is all IPv4. It's not really your IP. But later I'll show you how we will be changing this. Tag underscore bastion. That's the value tag underscore app. Now this tag is for the firewall. So we're going to create two firewall one for bastion one for our app server. And when we launch the bastion host we're going to make sure we give this tag. And when we launch App Server we'll make sure we give this tag app. Okay. First let's create the firewall rule for bastion host. So gcloud compute firewall rules create allow SSH internet. That's the name of the firewall rule. Backward slash. Hit enter. And let's give the information. So mention the network. So network will be the VPC equal to. Dollar VPC space. Backward. Slash. Hit. Enter. Hyphen. Allow. From where? So equal to. You have to give the protocol colon the port number. So allow 22 from my IP. So source you give like this. Hyphen hyphen source hyphen ranges equal to the variable dollar. My underscore IP space backward slash hit enter tag. So we're going to give this tag. And the tag we give through hyphen hyphen target tag option hyphen hyphen target hyphen tags equal to. In double quotes you give dollar tag underscore bastion. The value is here bastion. Now this is an inbound rule. So you have to say hyphen hyphen direction equal to ingress. All in caps. And let's hit enter. Now it says the project is not set. I forgot to set the project. So I'm going to say gcloud config set project in double quotes. I'm just going to say dollar project underscore id. Okay now let me run the command once again. Okay. So check the information. That's the name. And this is the VPC. This is ingress rule TCP 22. Let's also check from the UI. So go to VPC and go to firewalls. That's the rule allow SSH internet. So the tag is bastion and source is from all the IP. So if you want to restrict it only to your IP you can edit it here and mention. So you can remove this one and mention your IP. You can just google what is my IP address. Or you can go to this website. You will see your public IP. If you want to restrict only to your IP address you have to remove this ad, your IP address over here and you have to say slash 32. So restricted only to this IP address, not the entire range. And then you can save this, but your public IP will change. So whenever you're not able to do SSH to your bastion host you need to come back here. Edit the rule and make sure you add your latest IP address. And that should be the case in real time. Do not give 0.0.00. That allows all the IP address on port 22. Right now I'm just going to cancel this one. Now let's create the firewall for the private instance which is in the private subnet okay. Same command gcloud compute firewall rules create and the name of the firewall rule mention the VPC allowing 22. Now here there is a source tag and target tag. Target tag will be the tag for this firewall. And source is from the request will be coming. So we are given. We are giving here source as another firewall rule which is this firewall. So if you are in the bastion host, this firewall rule will be applied. So from this firewall rule you can SSH to this firewall. So this is source. This is target. So source can be a range. Source can be a specific IP address like my IP or source can be another firewall rule. And it's an inbound rule. So direction ingress. Let's create this. Okay. This one also got created. You can see here allow ssh bastion. You see here source filter. That's the bastion. Target is app. Now if you launch the instance and if you want to select this firewall rule then you need to give this tag which I'll show you just in a moment. Okay. So we created this firewall. We created this firewall. Now let's create one more firewall. Since we are already in the firewall section, this firewall will be for the app server to allow access from the load balancer. So when the request comes from load balancer, your app server should allow the connection on port 8080 or a Tomcat servers will be running on port 8080 on this instance, so source in this case will be the load balancer. The target will be this app server. Okay. So the same command. Once again this firewall rule is for the app server to allow connection from the load balancer. So the name is allow ELB to app. Give the VPC allow on port 8080. Because Tomcat will be running on port 8080 source ranges. Now this is the public IP or public IP range for the load balancer for the GCP load balancer. This is the public IP range. Once we create our load balancer I'm going to show you the public IP for the load balancer. This will be from this range. That is the reason the source range we have given as this target tab. Target tag is the tag app app server. So when we launch our app server we are going to mention this tag. So this firewall rule is applied to our app server. And it's an inbound rule. So that direction ingress. Let's create this one. Okay. Another firewall rule got created. Allow ELB to app. Target tag is app and this is the IP ranges for the load balancer allowing it on port 8080. In AWS term, we have created a security group for the app server to allow connection from the load balancer in AWS. When we create a load balancer, we create a security group for the load balancer. And load balancer has an end point in GCP. When we create a load balancer, it will get a public IP and that will be dynamic. But we can allocate a public IP and assign it to the load balancer. So load balancer can have static public IP which will be from this range okay. That is too much information for now. Anyways we're going to do load balancer later. Now let's launch the bastion host quickly. So we are going to create this file bastion.sh with this content which is basically commands to create a user called DevOps. Add it into Sudoers file and push our SSH key into it. So just copy it from cat till EOF at the end and go to your gcloud shell. Just paste the content over here so that you can do like this. Or just create a file bastion.sh and put all this data over here. Let me just get to bastion dot S-h okay. So the username will be DevOps. And this is the public key. So earlier we created public key private key right. So content of this file will be injected in the instance. We are going to use this key to login to our bastion host okay. Now the command to launch the instance. So gcloud compute instances create the name of the instance that is the bastion hyphen f and zone. So we are going to launch it in us Central1 a. Yes, that's the zone name, not the region name. In one region you have multiple zones. So we are launching in one a machine type E to micro one gb Ram, one CPU. We want to launch it in the public subnet. So hyphen hyphen subnet we give the subnet name to select our firewall the bastion firewall. We're going to say here hyphen hyphen tags. And the tag that we have used for the firewall image family. So that is the ubuntu image that we are using. This is from the project ubuntu OS cloud. And the startup script will be the bastion.sh file. Let's run this. I'm also going to show you how to do it from the GUI. So just run this command. It's going to take some time. So by the time let me go to Google Cloud Console compute instance VM instances. You see already started launching the instance. So if you want to do it from the GUI, all you have to do is click on Create instances, give the name, select the region and the zone you can select to be specific us Central1 a you select the instance type. So in e2 we have here e2 medium. We can select here e2 micro or E2 small C one shared core one gb ram. That's what we have selected in the OS and storage. You can select here the image. So if I click on change here you can select here first the operating system. So let's say ubuntu and which version of Ubuntu or Ubuntu 24. Now check here you have AMD and you have ARM. So we're going to go with AMD with this 124 I'm not launching it through the GUI, I'm just showing you the options boot disk type. So you have few options over here. Now the balanced one is the most cheapest one SSD is the fastest one, and you can select the size. So I'm just going to say select. In data protection you have snapshot. So a default snapshot you can just say no backup if you don't want any snapshot. Or you can schedule snapshots. So be very careful when you are doing it through the GUI. You know you have snapshot schedules and if you don't delete the instances, it's going to keep taking snapshot. And the monthly estimate can go to $6 for ten GB disk. In networking you can select here the tags while launching the instance will look for firewall rule with the tag and attach that firewall rule. Okay. Yeah. But before that network interfaces I'm going to remove this one. And you can select here your VPC. And you can select the subnet in which you want to launch. You have network service tier premium will cost you something. Standard is good if you're just learning. And if you go to advanced you can see here automation script. Here you can mention the commands like the commands that we are going to run. Right. So once you save all this information you can click on create and the instance will be launched. But as I said I'm not going to launch it through the GUI. If you already have launched through the command line and the bastion host is ready, you see it has an external IP address. So copy the public IP. Go to the location where you have your key saved to SSH. Hyphen I hyphen I give the key name. So mine is GCP. And the username that we have used is DevOps. At the rate the public IP. Hit enter. Yes. And we are in. You can do sudo hyphen I switch to root users. You tested the user login. You can also test the internet apt update. Now this is not just the VM test. We are also testing the VPC. It's in the public subnet. It is getting the internet. We are able to switch to it. So VPC is public. Subnet is verified. Firewall rules are verified. The instance status is verified. So you can keep the login. Or you can log out and log in later. Keep the instance running. In the next lecture we are going to launch the back end services, the cloud SQL, where we need to initialize the database for the MySQL. So for that we will need the bastion host. So once you verified all these things then join me in the next lecture.
