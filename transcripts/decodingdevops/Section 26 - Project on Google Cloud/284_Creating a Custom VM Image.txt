# Creating a Custom VM Image

All right. In this lecture we are going to work on our application server. So this is the flow. We're going to launch a VM instance with this script that will set up the Tomcat service deploy profile application in it. And we'll come back to the script in a moment. Let me just go through the flow first. So we are going to launch the instance same as we launch the bastion host with a starter script. Same here. We're going to launch an EC2 instance with the startup script that is going to set the Tomcat service and V profile application. We are going to wait for some time because it takes time to run all these commands. Once it's ready, we are going to log in to it. We're going to confirm that Tomcat service is running and we profile application is working fine. Then we are going to stop this instance so we can take its snapshot. And the reason of taking the snapshot is so we can create the image. So this is the process. We first take the snapshot. And from that snapshot we create an image a custom image. Same like in AWS we have Ami. Here we have this custom image. This will be our golden image. So once our army is ready, we're going to delete this instance. We don't need it anymore. And then we create an instance template like in AWS we have launch configuration or launch template where we see for this image. When we launch this should be the instance type. This should be the zone region and all the other details that you want to add like firewall rules and stuff. So all the launch information we're going to store into an instance template. And then we're going to create a managed instance group. Same like auto scaling group in AWS. So the whole purpose here is to create an auto scaling group with all the details of our app server. So we create the instance, we create its image. Then we create the template and then we create the auto scaling group. Mentioning that template we define the port number in that auto scaling group. Sorry, in this managed instance group. And then all the other details that we're going to do or see later. First let's work till the template or manage instance group. So this is the script first part you already seen. We create a user added into sudoers file. So this is so we can SSH to our instance and the rest of the part. You have already seen this previously in V profile project automated on the VMs. We have seen this script, the same script except this one works for ubuntu. That one was for CentOS or any redhat rpm based. But anyways, let me just quickly go through it. So that is the Tomcat download URL. We run apt update, we install JDK, we download tomcat binary extract it. So that will be a tarball. We're going to extract that one. We add a Tomcat user. We copy the Tomcat data to Tomcat home directory. We set up this folder. We give it Tomcat user ownership. Then we create the systemctl file so we can manage it through systemctl command that we systemctl enable or start Tomcat like that. Then we download Maven 3.9.9. Unzip it copies at this location. Then we clone our source code. We profile source code and build it by using Maven. We remove the default Tomcat application and we copy our V profile application into Tomcat home folder web apps. Then we start the Tomcat service. So first we need to create this file. And then we're going to launch the instance and mention the script. So copy everything from cat till you see EOF in caps eof not eol. Okay uff. And paste it here in the Google Shell Cloud Shell hit enter. You should see here app golden.sh. Just do a cat to it. At the end of this file you should see systemctl restart tomcat. And at the beginning of this file you should see bin bash. If you see anything else then delete the file. Create it once again. Now let's launch the instance same as we launch the bastion host So the so the instance that we're going to launch its name will be Golden V profile hyphen Golden. We're going to launch it into us Central1 A and it is going to be in the private subnet. Now we are not going to use this instance. We are just going to create image from this instance. So we can put it in public subnet also. But the reason I'm putting it in the private subnet is so we can test test our private subnet. When we launch the instance in the private subnet it is going to execute a script that's going to download packages from the internet. So that is going to test if the internet request goes from the private subnet to the internet through the Nat gateway, uh, cloud Nat, I should say. So that's one thing I want to test. The other thing is I should be able to do ssh from the bastion host, from the bastion host to the instance in the private subnet. So both the things will be tested if we launch it into the private subnet. Okay. So this is the command gcloud compute instances. Create the name zone us Central1 a machine type E2 small E2 micro won't work here because we have Maven running Tomcat running, so it's just going to freeze. We're putting it in the private subnet. We're giving it no public address. No address means no public address. It will have the private IP of course. And the tag is going to be tag app. That is this tag. Now when we launch instance with this tag it is going to look for any firewall rule that has the tag as app. And there are two firewall rules like that. So if you go to firewall rules you should see here the allow ssh bastion. You see it has the tag as app. It allows 22 from the bastion security group. So this firewall rule will be applied to our instance. The other firewall rule is allow load balancer to app. This firewall rule also has the tag as app. And this allows port 8080 connection from the load balancer IP range. So both these firewall rules will be applied to our instance when we mentioned the tag as app and we're setting it up on ubuntu, same as we did for bastion host, and with the script app Golden Dot such that we had just created. So let's launch this instance. So once the instance comes into the running state from that time, wait almost like ten minutes, because it will take all that time to set up your profile application. So if you want to break, take it now and come back after ten minutes. Okay, I've waited enough. Now it's time to test. First we need to copy the login key to bastion host. We do SSH to bastion host. From there we're going to log in to the V profile Golden instance. So go to the location where you have your keys that we created to ssh keygen and do a cat to your private key not the public key. Keep it as it is. Copy the bastion host public IP, not the private IP, the external IP and do ssh hyphen I your private key path username is DevOps at the rate bastion host IP okay. Once you are in switch to root user. Scroll up. Copy the private key. Okay. Do not copy anything. Extra character just from the start till the end. Till here. Copy that content and create a file here. I'll just call it as GCP key dot PEM and paste the content in this shift, insert no extra characters, save and quit and make sure you give it permission as 400. Read only the key name. And now from here we'll use this key. For the user DevOps that we already have copied at the rate and the V profile golden instance private IP. So from public subnet we are getting inside the private subnet right. So if this works, it's going to test our firewall rules. The connectivity from public to private subnet. And it worked. We are in. Okay. Now let's test if our script got executed successfully or not. So check systemctl status. Tomcat. You should see active running. That means all the installation, all the download worked. Hit Q to quit and run this command. Curl http colon slash slash localhost colon 8080. So we can see the v profile application. You should see the HTML file. You should see registration and login. This is the V profile HTML file. So everything is working. Now we are going to take a snapshot and create image from that. Come back to gcloud shell and switch off the instance power off. You can do it from the console. Also, you can do it from the command line like this gcloud compute instances. Stop the name of the instance zone. Let me remove this hyphen. Hyphen quite from here. Oh, I got locked out. Let me log in again. GCloud auth login. And then I set my project. Okay. Let me stop the instance once again. This will take some time. Okay. It stopped. Now let's take a snapshot. A very simple command for that. GCloud compute disks snapshot. The name of the instance and the snapshot name. And the zone name. This will also take some time. Okay. It's done. You should see this snapshot in the VM instance in the Compute Engine instance section in snapshots. So there's a snapshot 2.77 2.17 GB. Now from this snapshot we're going to create the image the custom image. So gcloud compute image create. And the image name. That's the command. Pretty simple gcloud compute images create image name, which we're using this when we profile image and source snapshot from which snapshot you are creating the image you mentioned that which is our view profile snapshot that we have already created. And hyphen storage, hyphen location will be the region where you want to store the custom image. So we're going to keep it in the same region us Central1 a let's hit enter. So this will take some time. Let's go and check the images in the snapshot below. Snapshot you have images and you should see here image getting created. The status is still refreshing. It's going to take some time. Okay, the machine image is ready. Now we can safely delete this instance. So gcloud compute instances instead of stop. You're going to say delete the instance name and the zone. Do you want to continue. Yes okay. The instance is deleted. Join me in the next lecture. We are going to create the launch template and managed instance group.
