# Introduction to the GCP vProfile Project

Instructor: Hello and welcome. In this section you are going to learn Google Cloud Platform, also known as GCP. Now, you already are an AWS Warrior. You have conquered EC2 load balancers, auto scaling group, databases, backend services, DNS, and many other things. You do not need to learn GCP one service at a time, but instead of that we are going to deploy VProfile application on GCP, and this will be production-grade. So before you get started, make sure you have completed these prerequisites. You already have AWS knowledge. You have done AWS Lift &amp; Shift project, the new migrated VProfile application by doing AWS Re Architecture project, and you have knowledge of AWS VPC. If you know all this, it will be very easy to transition from AWS to GCP. Now, let's get to know the service names in GCP that we will be using, which is equivalent to AWS services. So you can think of this as AWS to GCP mapping. So we have three columns over here. The first one is AWS service. Last one is the GCP name of the same service or a similar service. And in the middle you have what the service does. Now by far, you already know what the service does if you see the AWS service name. So AWS VPC service creates the virtual network with public and private subnets. Same thing in GCP it's called as same, VPC. NAT Gateway, which provides internet access to the private subnets. Same thing in GCP is called as Cloud NAT. EC2 instances, virtual machines. Virtual machines on GCP are called as Compute Engine or virtual instances. A custom machine image in AWS is called as AMI, Amazon Machine Image. Same thing in GCP is called as just custom image. When we want to quickly launch VMs from the AMI, we can create VM configuration that is called as Launch Template in AWS, which contains all the information about the instance launch like the security group, key pair, instance type, all the details. Same thing in GCP is called as Instance Template. To automatically scale VM up or down, we have AWS service called Auto Scaling Group. In GCP, that service is called as Managed Instance Group. Now there are two things here. This unmanaged instance group, that is without scaling, and managed instance group that comes with scaling. Now in AWS, both are same. We can set an auto scaling group that does not do scaling. That will be just an unmanaged instance group, and auto scaling group that scales up and down, that will be called as Managed Instance Group in GCP. Okay, a few other things. Load Balancer in AWS, we have Application Load Balancer. Same thing in GCP, it's called as Global HTTPS Load Balancer. The SSL certificate we create in AWS by using Certificate Manager Service, ACM. Same thing in GCP, it's called as Certificate Manager. Managed MySQL database service or any SQL database service in AWS is called as RDS, Relational Database Service. In GCP, that service is called as Cloud SQL. For Memcached, we have ElastiCache. In GCP, that is Memorystore. To manage DNS entries, in AWS, we have Route 53. In GCP, that is called as Cloud DNS. And there are a few other things. I don't want to go through the whole list, but you will get to know once we execute this project. Like in AWS, we have security group. In GCP, we have firewall. Yes, just the name is Firewall Rules. So in this project we are going to deploy a Vprofile application on GCP. And this will be almost similar to re-architecturing project, AWS re-architecturing project, it's similar to that except we have few more extra things that we'll see along the way. The most important thing for any production-grade infrastructure is to have its own network where you can have complete control. And in cloud, it is called as a VPC, Virtual Private Cloud or Virtual Private Network, you can say. Every cloud provider gives you a default network that you can use, but you do not have much control on it. If you want to have more control, more security, you need to create your own network. And that is called as VPC. In AWS we have VPC. And in VPC we have public cloud, I'm sorry, public subnet and private subnet. So this is the general AWS VPC diagram. We have minimum two public subnet and minimum two private subnet distributed among two different zones. Resources in the private subnets cannot be accessed from the internet directly, but the resources like instances or VMs, if they want to access internet, that traffic is going to go through the NAT Gateway, which lives in the public subnet and Nat Gateway routes the traffic to the internet. But the direct traffic from the internet cannot land in private subnet. It can go into public subnet. So let's say if you want to SSH to an instance in the private subnet, there is no direct access. One way is you can use a bastion host, also called as a jump server, which is in the public subnet. You can do SSH to the bastion host and from the bastion host you can SSH to the instance in the private subnet. So you can decide the rules, you can decide the inbound/outbound traffic. You can decide security group firewalls, and you can distribute your infrastructure in multiple zone. Now this is same in GCP. First thing, we are going to create a VPC in GCP with two public subnet and two private subnet. To give access or internet access to the instances in the private subnet, we are going to configure a Cloud NAT, which is same as in AWS, we have NAT Gateway, here we call this Cloud NAT. In AWS VPC, we create root entries, root table entries. But in GCP VPC, we need to configure a cloud router. In AWS, we get a default router, which is created by default automatically, but in GCP we need to create a cloud router and connect it to the VPC. Also, we are going to have a bastion host in the public subnet that will be controlled or its internet access will be controlled by a firewall rules. In AWS, we have security group. In GCP, we have firewall rules. So this is the first thing that we are going to create in this project, VPC on Google Cloud. But now let's look at the entire flow, what we are going to do in this project one by one. So as I said, first thing, we are going to create a VPC in Google Cloud with four subnets, two public, two private, and configure the Cloud NAT, which is like the NAT Gateway. Once again, Cloud NAT is used to give internet access to the private subnet. Once we have that, we are going to configure a bastion host, a VM instance in the public subnet. So we can do SSH, get inside the VPC. Then we are going to create backend services, MySQL 8 and Memcached service for our Vprofile project. So MySQL 8 will be on Cloud SQL and Memcached will be on Memorystore. This will be similar to what we did in AWS re-architecturing project. Used a managed AWS service. In GCP, same thing. We are going to use Cloud SQL and Memorystore. Cloud SQL and Memorystore will be in private, as in private network, but it is not part of the VPC private subnet. And later you'll understand when we deploy all this how we are going to configure it. But when we have it configured, we'll get a IP, a private IP for Cloud SQL and Memorystore. So we are going to create a private DNS entries, which maps the name to the IP address of Cloud SQL and Memorystore. So we are going to access it through the name and not just the IP. Next we are going to create a VM instance with Tomcat and deploy our Vprofile application in it. And then we are going to create its image. Like in AWS we have AMI, we are going to here create custom image. This custom image will be in a managed instance group. In short we call it as MIG. Managed instance group is the auto scaling group where we can manage the minimum instance, maximum instance, and scale out and scale in based on the triggers like more CPU utilization or more HTTP request, we scale out. Same as auto scaling group in AWS. So once again, VPC, bastion host, Cloud SQL and Memorystore, DNS zones, app image, or custom app image, and auto scaling group. Next. The internet traffic is going to route through a load balancer. We are going to have HTTP and HTTPS both in the front end. So from the load balancer, the request will go to the managed instance group through a URL map. I'll explain in detail later what is a URL map. But if you have HTTPS connection, we need an SSL certificate. That we are going to configure in certificate manager service of GCP. We are going to create a certificate, verify it through our domain registrar. I'm going to use GoDaddy, which will confirm the certificate. And finally, when we have the load balancer static public IP, we are going to create an A record in our domain registrar, which will map Name to our load balancer public IP. Yes, in GCP we get the public IP to the load balancer. In AWS, we get an endpoint. And the best part about this project is we are going to do everything through command line. In AWS, we have AWS CLI. In Google Cloud, we have GCloud Shell. And the reason I chose GCloud Shell instead of the console is because in general Google Cloud is managed through command line and Terraform. So if you know manually every step of how to configure your GCloud infrastructure, through command line, then you can transition same into Terraform. You can use many available AI tools like GitHub Copilot, Amazon Q, or even ChatGPT, to convert all the commands into the code. But here we are going to have step by step everything, every command one command at a time, so you understand the flow, you understand the entire infrastructure, how they connect together. This way you are going to learn Google Cloud. You're going to learn it through command line, and then you can transition it to the infrastructure code like Terraform. In the next lecture, we are going to look at the architecture diagrams of VPC backend services and the entire flow from the internet traffic to your instance. So join me in the next one.
