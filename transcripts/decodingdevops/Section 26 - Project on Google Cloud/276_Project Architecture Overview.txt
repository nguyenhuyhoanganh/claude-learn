# Project Architecture Overview

So in the previous lecture you have already seen GCP Virtual Private Cloud VPC. And as I said, that's what is the first thing we are going to configure on Google Cloud. So in AWS we can create a private service like RDS or Activemq and directly connected to the VPC private subnet. But that's not the case in GCP. In GCP, if your private instance residing in the private subnet wants to access a managed service like cloud SQL, we need to configure PSA plus VPC peering. In GCP, these private services like Cloudsql and Memorystore lives in a service producer network. It's a different network, and to connect it to your VPC, we need to configure PSA plus the VPC. Peering PSA is basically the connection or private connection between your VPC and the service producer network. So keep in mind here we have two networks. One is our VPC and the other one is service producer network. So in PSA, basically you do a VPC peering between your VPC network and the GCP service Producer network. So once we have our VPC setup we are going to create Cloudsql Memorystore. We get the public IP sorry private IP. We are going to create an IP range for these services and pair it with our VPC. So that's the second part. First part we create VPC. Second part we create Memorystore Cloudsql and do the peering. So here you have configured your backend services with your private network. So your security is taken care of and your backend is ready. Next comes the front end. Now for front end. How to explain the entire front end. I have created a flow diagram. Before we get into that, just keep in mind the private instance that is your V profile application. So the request is going to come from the internet through load balancer and land into the private subnet to your private instance where we profile application is hosted and that we profile application is going to access the backend services through VPC peering. So once again keep in mind your application server will be in the private subnet of this VPC. Let's go next. So this entire flow consists of four thing. First thing is the internet where your URL website URL will be transitioned to the static public IP of the load balancer. So that's internet part. Then you have Https termination or HTTP redirection. So https or http. Both are going to route the request through the load balancer. In load balancer you will have the front end mentioned the back end services mentioned. The health check probe. That's the third part. And the fourth part is where the request comes in the VPC in the private subnet to your V profile application living on Tomcat instance. Let's decode this one by one. Okay so the first part our internet. So internet users are going to access a URL. So as I said, I'm going to use GoDaddy to map that name to a load balancer IP. So there will be an A record created in GoDaddy DNS entries, which will resolve the URL to the load balancer static public IP. Now, based on you use Https or HTTP, the request will be routed. As I said. In any case, the request will be routed to the load balancer, of course, but there's many configuration that we need to do in GCP in AWS. This is pretty simple, but in GCP, especially in command line we need to add many entries. So let's talk about Https first. User access the URL on https connection. We need to configure a target https proxy. This is the front end of the load balancer. All this is happening in the load balancer itself. Now since this is an Https connection certificate will be there. So we need to create a certificate map entry that says that if the request is this https and this URL, then use this certificate that we would be already configuring. So the user certificate will be validated. If it's validated, then there will be a URL map and URL map. URL map basically is a table which says if the request is coming at this, then for the request at this particular back end. Now this should tell you that this is a layer seven application load balancer where you can have multiple entries. If the user is accessing en route path, then route it to this particular target group. In AWS we have target group. Same thing. In GCP we have URL map. So we can have multiple entries, but we just have one entry, one for https, one for http and both will be same. So you need to understand in Https we need to configure an Https proxy. That is the front end certificate entry and the URL map for HTTP connection. We just need to create a target http proxy that will be the front end to receive on port 80 HTTP traffic on port 80, and then the URL map that says that the if the request is coming on port 80 for route, then route it to this load balancer backend. In any case, Https or http, the request is going to come to the load balancer backend. So we need to configure a backend service in load balancer. In our case the backend will be the managed instance group. The auto scaling group. In AWS we have auto scaling group. Here we have managed instance group. And of course there will be health check like in AWS we configure health check here. Also we have health check that checks at a particular port number. In our case that will be port 80 at http port 8080 of the instance. So this managed instance group are going to manage the instances in our VPCs private subnet. So instance will be created from the template. And in the template it's mentioned to use our own custom image that has our V profile application running on Tomcat service on port 8080. So this is the whole flow. I'm going to put this diagram in the lecture resources. So make sure you understand all the entries. And when we execute commands on Google Cloud Shell, we're going to configure all this one by one. So you can every time or you should every time. Look at these. Look at this diagram when we configure the load balancer entries. So once again internet traffic then the Https termination or HTTP redirect. Both are going to route the request to load balancer backend through URL map. Load balancer backend consists of the managed instance group that will be managing your instances in the private subnet of the VPC. So 1234. But before this we need to configure the VPC. So in the first part we are going to create VPC with four subnets two public two private create bastion host configure it. And then we're going to move to the backend service. So first part VPC second part backend services. Third and fourth part will be the front end. Let's get started with this.
