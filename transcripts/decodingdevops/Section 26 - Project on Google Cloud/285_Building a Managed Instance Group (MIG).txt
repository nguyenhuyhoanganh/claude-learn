# Building a Managed Instance Group (MIG)

In this lecture we want to create the managed instance group like Auto scaling Group we do in AWS. So to create the managed instance group or MiG we need the instance template. A template defines what will be the instance type, which image to use to create the instance subnet region and other details. So the template defines all this. And with this template we can create the managed instance group. And if you want we can also set the auto scaling parameters. Maximum replica count minimum replica trigger for the scaling CPU utilization or even the load balancing utilization. So let's do all this in this lecture. First creating the template. So this is the command gcloud compute instance hyphen template. Create the template name in our case v profile hyphen template. Mention the instance type. The hyphen hyphen machine hyphen type e to micro should be fine just to run the Tomcat service. Now to create the image we mentioned e to medium to small because there was the build process and all those things were happening, so we needed a bigger instance type. But just to run the Tomcat service, E2 micro will be fine. The template will mention our image that we have created the profile image there. But yeah, I just refresh the page. That's the image and private subnet. Of course you already tested it. Our instance in the private subnet. It worked. Region and hyphen. Hyphen. No address all the time whenever you launch instance in the private subnet. If you are working on public private subnet in the VPC and when you launch instances private subnet always make sure there is hyphen found no address so it does not get any external IP address. Tags tag app. So this is the same firewall rule tag which is going to apply the two firewall rules to this instance. Okay so let's create this template. This will get created quickly because just the information about the instance launch. Now next we're going to create the managed instance group and mention this template information. It's pretty simple. We create a MiG with this name V profile hyphen MiG. We mention our template V profile hyphen template and we'll mention the zone us Central1 a. So this is the command gcloud. Compute instance groups managed create and the name of the managed instance group zone us Central1 a and the template that we just created size we are giving two. So it's going to start already creating two instances. Then once we set the auto scaling group based on the trigger, it can scale out and in. But for now it will create two instances. So let's create this. Okay so in Compute Engine section in the instance template you should see the template. You should see here. If you scroll down instance groups. In this instance groups you should see the instance group. It's saying it's already has two instances. So two VM instances got created. Status is ready. So there are two instances in the managed instance group but there is no scaling. We can set the scaling by running this command gcloud compute instance group managed set auto scaling the name of your MiG zone name. Mention the maximum replica, minimum replica, and target CPU utilization. We are keeping it at 0.6. That means 60%. So if the CPU utilization goes above 60%, it's going to start scaling out and this will be 60% of all the instances. But mostly for the load balancer that serves the http https request, this trigger is set target load balancing utilization. So load balancer is utilized more than 80%. It will start scaling out the instances. We don't have any load balancer yet. We're going to set it later. But while creating this auto scaling parameter we'll mention this trigger also. So we have an auto scaling group with two triggers CPU utilization of the instances and load balancing utilization. Let's run this command. Okay that got created successfully. You should see here the information. The status is already active. Okay. So now we are very close to completing our project load balancer. Front end part is remaining. We are in the end game now. Oh I just love seeing this. Join me in the next one.
