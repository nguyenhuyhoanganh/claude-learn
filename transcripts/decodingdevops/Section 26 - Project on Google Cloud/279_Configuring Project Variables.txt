# Configuring Project Variables

All right, show time. In this lecture we are going to create a VPC with Google Cloud Shell. So these are the things we are going to create. First we create a VPC. Then we create four subnets two public two private. We create a cloud router and cloud Nat. Connect that to our private subnet. And two firewall rules one for the SSH to the bastion host. Other to allow SSH from bastion host to the instance in the private subnet. Let's open gcloud shell. So login to your Google Cloud account and click on Activate Cloud Shell. It's better we open it in a separate window. So let's click on this button. Open a new window, run the command GCloud projects list and select the project that we are going to use. Execute the command gcloud config set project and the project name that you want to set. Shift insert to paste. Okay. So the project is set. Now we are going to set variables. So open VPC dot file and copy all the variables. Now usually variables we set in scripts. But the reason we are setting the variable is so you and me can execute same command. For example we have set here project ID and we set the variable. And you might have a different project name I might have a different project name. When we run this command gcloud config set project and the project name instead of this. Then your command and my command will be different right. But we if but if we use the variables, you and me will be executing same command. This is just so in the project setup we are aligned and you don't make such mistakes. So copy all these variables from project ID till the end where you see the tag bastion tag app. Copy this and paste it into a text editor. Notepad is also fine, we just need to change these values. So project ID give your project ID here. So I'm going to remove this. Just keep the double quotes and put your project ID. So that's my project ID project hyphen V profile region I'm using us hyphen central one. It's better you stick to same one. Or even if you want to use different one you can use in US central one, there is a zone called a. So the zone name will be US central one a hyphen a app name. Keep it as V profile. Same domain name. You can give your domain name over here if you have purchased or you can just remove this variable altogether. So my domain is this infotech.xyz subdomain. You can keep the same my IP keep it the same one for now. Once we are able to do ssh, I will show you how you can change and give specific public IP your own public IP. Okay now SSH key. So we need to generate the keys public key and private key. Public key. We're going to take and put it over here. So open terminal git bash. Based on what computer operating system you're using. I'm using windows here. So I have open git bash. If you're using Mac OS open your terminal. And I'm going to go to downloads folder. And here I'm going to create a folder right. Yeah named as GCP keys. I'm going to get into that folder. And here I'm going to create the key. So I'm going to run the command SSH key gen ssh key gen. Run this command and just give the name of the key. So I'm going to simply call it as GCP key. Or you can say GCP key. Okay. That's the name I'm giving the GCP key. And just hit enter enter enter. Now if you do less you should see here a private key and a public key. So this private key we are going to use to login to our bastion host and our backend instance. This public key we need to inject when we launch the instance I'll show you how we can inject that. First, just copy the content of the public key, the public key content. Just do a cat and copy the content. Make sure you don't copy any extra space, just exactly the key content. And over here in between the double quotes, just paste the key okay. That's all you need to do these things. So set the project ID you can keep region and zone same. Set your domain, set your domain name and set the public key content. Everything else you can keep. Same. Now copy everything. Just make sure no other extra things are copied. Just copy all the variables. Go to your gcloud shell authorize and over here do vim tilde slash dot bash rc. Open this file. Go to the end of the file shift g you can use. At the last line. You can just hit O to get into insert mode and get into the next line and use shift Insert to paste all the variables. Just verify everything is correct or not. Okay, here you have this thing. Extra delete delete delete. Okay. It's a comment. It's in. It goes in the next line. So this is fine. We are just on the safer side I'm going to remove this one. Okay. Good okay. So all the variables are saved over here. Save and quit this file. And now do source tilde slash dot bash rc okay. Now run the command gcloud config set project and your project name there to set your project authorize okay let's test few variables. Echo dollar region okay I'm getting the region name. Like like that. You can trust or develop the trust that your variables are set. But before running the command, make sure you always do an echo to the variables that you're going to use in the command, just to be sure that you're doing the right thing. Okay, so in GCP when we consume a service we need to enable the APIs for those services or that particular service. For example, if you're going to use Compute Engine, including the VPC, you need to say gcloud services enable and the name of the API. So once it's enabled then you can start using that service. So here are the APIs that we are going to use in this project. Compute DNS, SQL admin psql hyphen component memcache certificate manager service networking. So you can run one command at a time like gcloud services enable compute Google APIs and then the same command for DNS. Or you can enable all of them at once. So let's copy this command. This one we need to copy because this is just one time use enable the API in your account and then you can start using that service. So shift insert to paste run this. This is going to take some time. Okay. So all those APIs got enabled. Now it took some time but it got completed successfully. Okay. Now let me clear the screen and create the VPC.
