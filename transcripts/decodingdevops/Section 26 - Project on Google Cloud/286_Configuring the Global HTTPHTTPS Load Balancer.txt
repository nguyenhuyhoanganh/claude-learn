# Configuring the Global HTTP/HTTPS Load Balancer

Okay. So we have the managed instance group that can scale out and scale in instance. And they are creating in private subnet of our secure VPC. Now we're going to set this part. So let's work on the load balancer backend first. If you remember in AWS we have target group where we set the port number the backend services where the request should be routed. Similar to that we're going to work on load balancer backend part in this lecture. So we are going to set the port number for the managed instance group which will be eight of our Tomcat. We're going to set a health check. And we need to create a backend service. That's simply a name that this managed instance group is the backend that works on port 8080. And this is the health check like that. And all this information, backend information of backend service will be given in the URL map. And if the request comes on this then this is the backend. Little confusing compared to AWS, but doable. So these are the steps. First, we are going to set ports for our managed instance group port 8080. This will tell the load balancer that for this MiG this is the port number and protocol is HTTP. We are going to set a health check like in AWS target group. We create a health check. So Tomcat servers will be running on port 8080. So health check will be for port 8080. And path is just root directory no subfolder. So set the port number for MiG. Set a health check. Create a backend service. There's nothing in this here. We just create the name of the service backend service and mention the health check and ports. In next command we attach our MiG to the backend service. So at this part our backend is configured. Now we need something that routes the request to the backend. And that will be the URL map. and we'll come to that part later. Let's start creating the port first. So gcloud compute instance group managed set named ports for this MiG. And for this zone the port will be HTTP protocol sorry protocol HTTP and port 8080. Their synonym okay port number is set. Next let's set the health check. So this is the command gcloud compute health checks create HTTP the name of the health check. It's going to be a global health check for port 8080. And path is root directory. So no subfolders. Now here health check is just configured. There's nothing attached to it. It's just a configuration that we're going to attach later. So let's hit enter okay. So the health check is created in Compute Engine. You can go to instance groups. You can see health check refresh this. You should see a health check got created with the name V profile hyphen HC. Now we need to connect this health check to a backend. That backend service will be for the load balancer. Like in AWS. We have target group like that. Here we have backend service. So we'll create a backend service with the name of backend. We are going to attach this health check to that backend. And this is the command gcloud compute backend hyphen services create the name of the backend hyphen global protocol http port name http. Now this is the same port. This is the port for our image. For MiG we created. That is the name we are giving over here. And this is the health check that we have just created. Now this is empty backend service. There's nothing attached to it. We are going to attach it to the backend. Sorry to the MiG, but let's just create first this backend service. And I know this might get a little confusing, but everything will make sense once all those things are connected. So a backend got created. Here we go back to our health check refresh. This you should see in use by v profile hyphen back end if you click on this. So this takes us to the load balancing like in AWS load balancer. We have target group like that. Here. In the load balancing we have backend service as the target group. Now this is an empty backend. There is nothing attached to it. We are going to attach it to our managed instance group. So we have backend with this name V profile hyphen backend. And we have a managed instance group with this name V profile hyphen mig which create instances in this zone us Central1 a. We are going to combine all these things together back in and mig so gcloud compute backend hyphen services add backend the name of the backend. So with this add backend option we are going to add this instance group this managed instance group into the backend in this zone. Let's hit enter okay so our backend is updated. Let's refresh this. So now you should see V profile Hyphen. Meg. It has total four of four. We just mentioned two, so I'm going to check later. What happened. Why did it scale to four. But let's complete the setup first. So you see it says 4 or 4 are healthy and healthy. How it got to know healthy. That is because of the health check okay. So that was all the back end of the load balancer. Now let's work on the front end of the load balancer. So we have managed instance group. We have the health check probes that are running and the backend service that has all these things. That is the load balancer backend. Now load balancer front end. Now there should be some information that should forward the request to this load balancer backend like a mapping kind of thing. Which should say that if the request come on https 443, then route the request to this backend, or if the request comes on http port 80 route the request to this load balancer backend. And that's what exactly is URL map. It creates basically a table and it's matched the following. So let's create the URL map now. So we are going to create URL map with the name view profile URL map and attach it to the back end. As in not attach it to the backend, map it to the backend. This is the command gcloud compute url hyphen maps. Create name of the URL map and the default service that is our backend. So by default it is going to send the request to this backend. Okay. So that is created. Now we need to create an HTTP proxy for this. This is what we need to do it in GCP. We don't need to do this in the AWS load balancer. It's come and it comes in built HTTP proxy is basically a front facing of the load balancer. So we need to create one for HTTP request and one for Https. This one we are doing for HTTP now. So let's create this. So gcloud compute target hyphen http hyphen proxies create. With this name we profile http proxy and map it to this URL map. Okay, now we should already have a load balancer with this http proxy URL map and backend. Let's take a look at it. So just search for load balancing. So you see the load balancer and it says application load balancer type in. Right now you just see the name as V profile hyphen URL map. But once the setup is completed we will see the actual and proper names and everything. Right now we need to reserve a static public IP for the load balancer. And the reason why we will be reserving a static IP address for the load balancer is because we are going to create an entry in domain registrar. So I'll be doing it on GoDaddy. I will say GCP is equal to the load balancer IP. And that IP needs to be static. So I need to create an IP static IP for the load balancer. Okay. So let's run this command gcloud compute addresses create ELB underscore IP. That is going to be the name that's going to be global. Okay. So that got created. And now we just do describe. You can see the IP address gcloud compute addresses. Describe the name hyphen hyphen global. So that's going to be the static public IP of the load balancer. Okay. Now one last thing. And we will have a functional load balancer and the entire setup. Except we won't be having the Https and proper URL. So if you do not have a domain if you are not purchased the domain, the command that I'm going to run now will be the last command. And that would be it for you. So let me show you the command. GCloud compute forwarding hyphen rules. Create the name of the forwarding rule http underscore elb. That's variable. This is the name V profile hyphen http hyphen elb. Now this will be the name of our load balancer. Now it's going to be global Load balancer. And the target HTTP proxy is this HTTP proxy that we just created. That will be the target ports 80. We are doing it for HTTP port 80. And the address is the load balancer IP. Now this variable is going to return the name of the load balancer IP. And that is attached to the IP of the load balancer. Now let's just create and wait for it to complete. Then I'm going to show you the entire flow will understand this. Okay. This got created. Let's go to the load balancer. Let's refresh this page. Okay. So that's the IP address of our load balancer and port 80. Now if we exit through the browser. So you should be able to see the login page of your profile application. And it will take some time. So if you don't see this wait for some time. Make sure you check the health status of your instance group. So your instance should be healthy anyways. And let's log in and check the database also. So username admin underscore vp same password admin underscore VP. If we are able to log in then it is able to connect to the cloud SQL service. And yes we're able to connect to the cloud SQL service from our V profile application. So we have not configured RabbitMQ Elastic Cache but we have configured memcache. So let's click on all users. So it should show you the user list. Let's click on one of the users ID and we see here data is from db and data is inserted in cache. So caching is also working. Click the user once again. Then you should see the data is from cache. So this is awesome. Everything is verified. If you have. If you do not have the domain then you can just watch the next lectures and later you can check the cleanup lecture to see how you can clean up everything. And if you have a domain, then join me. In the next lecture. We're going to set up an Https connection with a proper URL in the next lecture. So see you there.
