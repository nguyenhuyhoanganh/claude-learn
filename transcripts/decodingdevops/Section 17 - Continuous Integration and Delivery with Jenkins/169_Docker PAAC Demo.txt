# Docker PAAC Demo

Instructor: Okay, let's execute the prerequisites and then let's execute the pipeline. I have added here a few more steps. One is installing AWS CLI. It is not required for this lecture, but in later lecture, we'll need it when we deploy our docker image to AWS ECS. We'll see that later, but since we are logging in the Jenkins, so let's install AWS CLI also, which we'll use in the later lecture. And one more plugin, AWS SDK. When you install ECR, it'll install AWS SDK, but if it doesn't, we are going to install this plugin also. This plugin help us save the credentials, access and secret key in the Jenkins. So first we'll start with Jenkins. We are going to install Docker Engine, install AWS CLI, and then we'll come back to AWS. So let's do an SSH to Jenkins. So SSH -i key path. Open to username, add the right the Jenkins public ip. Okay, so I'm in first I'm going to run the command sudo app update. Okay, next time I'm going to run the command sudo snap install aws-cli --classic. This should install AWS CLI. Okay, now let me switch to root user and clear the screen and let's install docker engine. We'll check all the steps in the docker documentation, okay, you can go to this URL, docs.docker.com, or you can just Google docker installation. If you come to this URL, you can go to manuals. When you come to manuals here, you have docker engine, okay? Not the Docker desktop. You have to install docker engine. So click on this dropdown and click on install. Let's go to Ubuntu. Now you'll also see here Docker desktop. Do not click to that. We are going to install docker engine. Click on Ubuntu and let's scroll down. Installation methods. Okay, so these are the commands to set the repository, and then this is the command to install Docker engine and Docker CLI, just need to copy this. There is a copy button over here. Click on that or select everything and copy. And let's just paste it in our terminal. Okay, that is done. Let's now go to the next command, that one, copy that and paste it in the terminal. Let's Q here -y so it doesn't ask us any question. Okay, it's installed. Let's check the service system. CTL status docker. Okay, active running, Q to quit. Okay, so the commands will be something like this. Like for example, I'm going to run Docker images, right? So it's going to list all the available images, but if I switch to Jenkins user, you've seen in the first job, right? We execute, who am I command? It says Jenkins. So Jenkins user executes everything in the Jenkins job. So switch to Jenkins user and let's try the docker command. And you see it says permission denied. Okay? So what we have to do now is we are going to add the Jenkins user in the Docker grou,p exit. Just be back and come to the root user. So when you install Docker engineer also creates a docker group. So you need to add Jenkins user in the Docker group by running this command, usermod -a, -: G, username, docker, and the group, Jenkins. So this is going to add the Docker user in the Jenkins, oh, sorry, Jenkins user in the Docker group. My bad. Okay, this is the Jenkins user. This will be added into the Docker group. Let's hit enter. And now if you run ID Jenkins, this command should show, it is in the Docker group now. Okay, but that's not it. Now we need to restart the Jenkins service so it loads all these things. Or the best way, what I do is we have a chance to reboot the Jenkins. So let's reboot Jenkins. When we do reboot, all the service will get restarted. All the configurations will be loaded. So let's reboot and wait for some time. Okay, by the time the Jenkins is rebooting, let's go ahead and execute the steps to create IAM user with access key and then we'll create ECR registry. So let's go to IAM service, IAM. Let's go to users, click on Create User. Let's give this user a name. I'm going to give it as Jenkins, go next, attach policies, directly select the policy. So this user needs access to two services. One is the ECR to upload the docker image to the registry. And in later lecture, we'll also need the permission to deploy it on Amazon ECS cluster. So we also need to give ECS permission. So let's search for the policy. So just search for container, Amazon EC2, container registry, full access, put a check mark on that. Next ECS, Amazon ECS, full access, put a check mark on that as well. These two policies and go next. Once again, take a look at the policy, take a look at the username and create user. So we need now the access key and security key for this user. So click on the user, go to security credentials, scroll down, access key, see create access key, CLI, scroll down, I understand and say next, create access key. Okay, make sure to download the CSV file and store it somewhere where we can use it later and click on done. So this file will have the access key and secret key. We're going to store this in Jenkins in some time, but let's complete the next step creating the ECR registry. Okay? So just search for ECR. You'll see here, elastic Container Registry. Click on that. Once again, Amazon ECR is a place where we can store docker images and the place where we store docker images are called as registry. Container registry or docker registry. So Amazon ECR is a container registry or a Docker registry. Let's click on create. Let's give repository a name. I'm giving name as vprofileappimg, and that is all you need to do. Just click on create, oh wait, before that just take a look, the repository name and this the account name slash the image name, alright, if you check our pipeline, right? That is it like that. So you have to replace your code with this one, okay? You in the code, you'll see like this, you're going to replace it with your own repository name and the image name. So first let's create, okay, so whenever you want, you can come here and copy the URI over here and put it in your pipeline. We'll do that later. Now let's go to Jenkins. Jenkins UI and let's install the plugins and store the credentials. So we need to do these things, store the credentials and install the plugins. So let me get the public IP of Jenkins, go on Port 8080, login. Okay? So manage Jenkins, reinstall the plugins, first. Plugins, go to available plugins. First search for AWS, and you should see here AWS, Amazon Web Services, SDK all. Put a check mark on that and search also for ECR, Amazon ECR. Search for a Docker plugin, docker. So you want this one? One is the Docker pipeline and this one CloudBees Docker Build and publish, put a check mark on that. So once again, take a look. Amazon web service SDK: All, Amazon ECR, Docker Pipeline and CloudBees Docker, build and publish. Now these are the plugins that we are using, but if you see Docker has so many other plugins, also. Based on your use case, you're going to use those plugins. For our exercises, we are using this plugin. But it's not limited to that. Anyways, once you selected all this plugins, just click on install. Okay, the plugins are installed. Now we need to do one last thing over here. We need to store the access can secret key in Jenkins credentials. So go back to dashboard, manage Jenkins, credentials, system, global credentials, okay, here, add credentials, click on add credentials, and in the kind you have to select AWS credentials. So this option you'll get after installing the AWS-SDK plugin. So select that and let's give a name and description. So I'm giving ID as awscreds and the same name I have given in the pipeline code. So if you're giving something different, make sure you update your pipeline code. Okay? I have given the same name, right? Okay, and let's store the access key and secret key. So open your CSE file, okay? It's extremely risky to show access key and secret key like this. But as soon as I'm done recording this lecture, I'm going to delete these keys. So copy the access key. Copy there, and let's get the secret key. Make sure these things are copied properly. Access key secret key will be separated by comma, so make sure you do not copy the comma. And let's click on create. Okay, so we got almost everything. Now it's time to update our pipeline code and then execute it in Jenkins and see what happens. Okay? Starting with the registry credential is here. That is correct. US East 2, mine is the region, US east 1, right? US east 1 in the same place where we created the registry, US East 1. So make sure you get that. Okay, I've kept US east 1, and this is the same name of the Jenkins credentials. If you're given different name, make sure you put that over there. Then the image name, let's go to ECR. Okay? So that's the URI. Let me copy that. Open the pipeline, okay? And same in the V profile registry. After HTTPS, you have to copy the part, just make sure there is no slash vprofile image, okay? Make sure this is in the same format. This is the image name, and this is the vprofile registry URL. Okay, then it's going to fetch the code, build the code, the unit test, check style, code analysis, quality gates. And over here, here it's going to build a docker image, right? So image name, the one that we just copied right now from ECR that will come over here. Plus there will be the build number that will be used as the docker tag and the path of the docker image. Here we have the docker file once again, right, in our repository in the docker branch, docker files app, multi-stage docker file, right? And this is the docker file. It's going to use to build the docker image and then upload it to Amazon ECR. So vprofile registry, that is URL and register credentials, that is the credential used to log into our AWS account. Okay? I think it's all well and good. I'm going to save this file and copy all the content. Okay? My Jenkins and SonarQube server is up and running. I'm going to go to Jenkins dashboard. New item. I'm going to name it as docker-ci-pipeline, select pipeline and say, okay, let's scroll down till the end, pipeline script. Let's paste our code over there, save it and test it. Let's click on build now. Let's look at the stages. Okay, the pipeline completed successfully. Let's look at the console output. I need to scroll till the end. Okay? This one is the last stage, push Docker login. It is doing a login to our registry, log in succeeded, and there it tags the image. You see the tag? That is the build ID, Docker push, it's pushing and it's completed. Okay, let's go to ECR. Let me refresh this page. Oh, there's a refresh button here as well. Okay, let's click on the image, and you should see here image tag. Okay, now try running the build few more times and you'll see different tags over here. 1, 2, 3, based on the build ID. Now images are uploaded in the ECR, but there's also one more problem. The image is in the Jenkins as well. So we need to add a stage where we remove the image after this upload is completed. Okay? So I'm going to add a stage over here. Let me name it. Remove container images, steps, and I just need one shell command to execute as such in single code docker rmi -f to false. And then there is a command substitution. You've seen command substitution in Linux. So the command is docker images -a -q. It's going to list all the image ID. So this command substitution is going to list all the image IDs and present it to this command, which is going to remove the docker image forcefully. Let's save this file and copy all the content. Let's go to our job, configure, select, and delete the script, and add our new script and say save, and let's execute it. Let me go to the console output and wait until this is completed. Okay, this completed. You see at the end, it is going to delete all the images and like this, our desk space will be freed every time we build a docker image. Okay? So then that is the end of the continuous integration. We can add notifications also, but we have one more thing now to deliver this docker image or this artifact, and then that will trigger, that will start the continuous delivery process. So if you're going to continue to the next lecture, keep the instance running. If you're going to take a break, make sure you shut down your Jenkins and SonarQube instance. I'll see you in the next lecture.
