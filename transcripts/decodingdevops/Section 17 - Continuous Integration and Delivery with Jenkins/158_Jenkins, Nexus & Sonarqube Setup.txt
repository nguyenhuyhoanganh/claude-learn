# Jenkins, Nexus & Sonarqube Setup

Instructor: All right, in this lecture we are going to set up Nexus Server and SonarQube Server on EC2 instance. Nexus will be running on Amazon Linux 2023 and SonarQube on Ubuntu 24. Let's take a look at the steps that we will be using to set up this server. Basically we are using the user data script, so come to the source code URL, github.com/hkcoder/vprofile-project and switch to the branch atom. In this you have the folder user data, and you should have here nexus-setup.sh. It's a simple script, same way we set up Tomcat 10 service also. Like in Tomcat we install JDK, then we download the binary, create the user, create systemd file. Same way here also Nexus Setup. We download the key and set the repository file. We install JDK 17, create the folder, download the binary, extract it, create the Nexus user, create the systemd file for Nexus Service, start and enable Nexus Service. Now let's check the user data script for SonarQube. So sonar-setup.sh. This script will set up three services, SonarQube service itself, database PostgreSQL, and NGINX service is a front end for SonarQube. Like in VProfile project we have NGINX service in the front end, then we have Tomcat, and then we have MySQL database. But we did set up all in three different machines. Here, all in one machine, NGINX, SonarQube Service, and PostgreSQL. Here there are some commands which you may not have seen previously in bash scripting or in Linux. So let me just explain you those things. Now Postgres Service and NGINX Service, sorry SonarQube Service takes a lot of resource from the system, and in order to give more resources to the process of SonarQube, we have to make some operating system level changes. That can be done like this. Open this file systemctl.conf and these are the entries that you need to give over there. This is from the documentation. So the maximum file size that can be opened you limit, and then in limits.conf file, the SonarQube process can open how many files. So these two files needs to be changed, and once you make the changes to these files, you need to reboot the Linux operating system. Apart from that, other things are pretty straightforward. I install JDK, download PostgreSQL, and start the service. Like MySQL service, we create the database, here also we create the database, we grant privilege. The user of the DB will be Sonar, it's password you've seen here, admin123, and then download and set up SonarQube same way as we do Nexus or Tomcat setup. Similar way, download the binary, extract it, create a user, create a systemd file for it, start the Nexus. Sorry, start the SonarQube service. Oh my bad. This is the Sonar properties file. So this file is like we have application properties file for VProfile. We mentioned the database username and password. So this kind of information will be in the sonar.properties file. And then you have the systemd file, start and enable SonarQube service. Then on the front end there's NGINX service we are installing. We take the request on port 80 and then we route the request on port 9,000. This is the port number of SonarQube, right? Like Nexus runs on port 8081, Tomcat on 8080, Jenkins on 8080, SonarQube's web port is 9,000. This is from the configuration file. If you want you can change that. Once everything is done we restart the NGINX service, and then there is definitely a reboot, because in the beginning we made OS level changes. So for that we need to reboot this OS. So there is a reboot command. Now let's go ahead and set up Nexus and SonarQube service one by one. You should already already have Jenkins server. You can shut it down. You don't need to start it for this lecture. I'll click on launch instance and launch Nexus Service. So I'm going to give it name as Nexus Server. OS, I will go with Amazon Linux 2023 AMI. Instance type needs to be bigger, T2 or T3 Medium. We need to go with medium. And we'll just power on when we need it, otherwise most of the time your Nexus and SonarQube both will be down. Remember, whenever you use it then only power on, otherwise keep it down. Key pair. Let's create a new key pair. Nexus key, I'm giving the name. Security group rule edit. Create a new security group. Name I have given Nexus-sg. First, yeah, 22 from my IP, and then one more rule that will say port 8081. So the front end web browser port number is 8081 for Nexus. And we'll see here from my IP. And also one more rule, port 8081 from Jenkins Security Group. The source should be the Jenkins Security Group. Why? Because we will be uploading our artifact to Nexus Service from Jenkins. So Jenkins will connect to Nexus Service on port 8081. So we need to make sure the Jenkins security group is allowed to access Nexus on port 8081. Allow Jenkins to communicate with Nexus. I've just given a description. And in advanced details go to user data and come to the script Nexus. Okay, we are setting up Nexus. So nexus-setup.sh, copy all the content from here, just click on this copy button and paste it. Make sure you check it, and if you're good, launch instance. Let's go for the next instance, which is SonarQube. Launch instance. Sonar Server, Ubuntu, Ubuntu 24, free tier eligible. Instance type, okay, this one also T2 medium or T3 medium. Remember we need 4GB of memory. Key pair, let's create a key, Sonar Key I've given the name. Edit create security group with the name sonar-sg. Okay, so the rule 22 from my IP, and we have NGINX service in the front end. So we can just give here port 80, or you can select HTTP also from the type, and first, yes of course my IP we need to check from the browser, and we need to also give port 80 from the Jenkins Security Group. Why? Because Jenkins will be uploading the code analysis results to SonarQube Server. So it'll contact Sonar on port 80. So make sure you put Jenkins Security Group over here. Allow it on port 80. Give some description, advanced details, user data, okay, open sonar-setup.sh script, copy everything, and put it out there, and say launch instance. Okay, so this will take some time. It's a lot of steps plus it's also going to reboot this machine. Let's go back to Nexus. Let's take its public IP, let's do the SSH to it. So I'm going to open my Git Bash. So ssh -I key pot ec2-user. Remember it's Amazon Linux, so ec2-user is the user. At the public IP. Okay, login, yes. Okay, let me switch to root user and check the Nexus Service. Systemctl status nexus. Okay, it says active, running. The service is running. Right here is the Nexus home directory. Remember this ls/opt/nexus. That's the place. You can check the Java version. That's Java 17. Anyways, the service is running. Let's just check it from the browser as well. So take its public IP, put in the browser, colon port 8081, enter. Okay, so that's the front end. We can click on sign in and login, but lemme talk about one very common problem that you will see while you're accessing Nexus from the browser. If you have some kind of VPN or proxy in your browser or a VPN dialed in, then you may not get the next screen. So I recommend you stop your VPN, browser-based VPN or any normal VPN for now. And then when you stop it, make sure you update the security group rule also of Nexus, your IP might be different. So once you stop your proxy VPN, then you can click on sign in here and you get this window. In this place you have the password, initial password of Nexus. So copy this file path. Go to Nexus where you did ssh do a cat, give the path, hit enter, and that is the password. There is no new line. Okay, keep in mind the square bracket, this is where is the prompt starts. So you have to copy till here, copy this, and give the username that is admin as it says. And the password you need to paste there. Sign in. Okay, if you're not getting this wizard as I'm getting, then definitely there is something that is blocking in your network. Some proxy, some VPN, some firewall. Please do check that. If there's nothing on your computer, it might be in your network. So switch to a different network and try. Okay, go next, password. Let me give some password over here. I'm keeping simple password, but as I told you previously, also in real time, make sure the password is long and complicated. Go next. Okay, we'll enable anonymous access so we can just download from the repository whenever we want. Next and next. Okay, finish. Okay, so we are in. Just do remember the password that you have set, otherwise save it somewhere. I have given username as admin and password as admin123. I'm telling you because you're going to see my Pipeline code. in that also, you'll see the passwords. Whatever you give, make sure you remember it and use it whenever you're using any Nexus plugin. So Nexus is basically a software repository, you can click on browse and you can see different repository. Later I'll show you how you can create repository and store your artifact from Nexus to here. Let's now check SonarQube. I think now this will be up. So copy it's public IP and just put it in the browser. You don't need to enter any port, because there's NGINX running on port 80. So just put the IP anyways, it goes on port 80. Okay, this might take some time. Yeah, okay, it's up. Okay, click on login. And the username is admin and the password is also admin. Do remember to reset this password. Okay, so this is a dashboard. We are going to, automatically from Jenkins, we're going to create projects and upload the results over here and display it. It looks kind of nice, the charts, and graphs, and stuff. Okay, there's one more last thing that we need to do. While creating the security group, I told you Jenkins will contact SonarQube server on port 80, but SonarQube also going to contact Jenkins. It's going to send the results, the status of the test result, whether it passed or failed. And it contacts Jenkins on port 8080, 'cause that's what we'll mention in the SonarQube. So you need to add one rule in Jenkins security group. So go to Jenkins, security, Jenkins Security Group, inbound rule, edit inbound rule. Once again, we are editing Jenkins Security Group. We are allowing connection from SonarQube. We add a rule, we say port 8080 is allowed from the SonarQube Security Group. Where is it? Here. I give some description that allows SonarQube like that. So you will know that this is SonarQube Security Group. Save the rule. Okay, that is it. Now what you can do is you can shut down these instances. We don't need them now. Once we need them, we are going to power on and use it. For now, shut them down, because you see they're not T2 Micro. And then join me in the next lecture.
