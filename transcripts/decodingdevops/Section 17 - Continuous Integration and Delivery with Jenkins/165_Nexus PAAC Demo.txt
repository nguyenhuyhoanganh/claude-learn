# Nexus PAAC Demo

Okay, this is the pipeline code from the previous lecture. We are going to add one more stage over here that is going to take our artifact from Jenkins and upload it to Nexus repository. So if you go to core analysis job, go to any build ID, go to workspaces. In workspaces, you should have a folder called Target. In that we have a vprofile-v2.war. Now this is the artifact, we need to version this artifact and upload it to Nexus repository named vprofile-repo. If you remember, we installed a plugin, nexus artifact uploader plugin. Let's check how we can put that in our pipeline. Nexus artifact Uploader, you should see the documentation. You should see many other examples on GitHub also or you can go to the AI mode and check. So the documentation, you have a Jenkins pipeline example, the plugin name and some options that are passed through this plugin. Let's go to the AI mode. So this is plugin specifically designed to upload artifact jar, war to Sonatype Nexus repository. Let's see the example. So stage, stage name, steps, plugin name, and the options passed with it. So let's copy this and put it in our pipeline and make changes. So check where this stage quality gate ends over here. Give some new line and paste it over here. Fix the indentation, make sure it's properly indented so it looks good. So I'm going to fix this, artifacts. That's the artifact information. So I'm going to make this vertical so it looks a little better. Okay, artifact ID, the classifier, then the file and then the type war. There is another square bracket. This I'm going to make sure it's just below this artifacts. Then this parenthesis, this is for the plugin where it starts. So, bring it here. That curly brace is for this steps. I am going to give it a new line. Okay, this is good. Once again a new line and then this one in the stage. Okay, this looks good. Now let's add the correct details. So Nexus 3, that's the version that is correct. Protocol, http, that is also correct. NexusURL, here we need to put the private IP of Nexus. So let's get it. So make sure you select Nexus server and take its private IP and just paste it over here. This is good. Don't give HTTP or anything else. The protocol is already mentioned here. Group ID, I'm just going to remove this and just say here, QA. Version, now version needs to be dynamic. It should change every time we run the build job. So there is this build ID environment that we can use, which gives us the build ID, 1, 2, 3, it's incremental. And we can also use the build timestamp variable. We install the build timestamp plugin, so we can use that variable as well. Together, it'll give us a unique version of our artifact. So remove it and just put the double codes. To access this environment variable, you need to give here the curly brace. So it'll be like this dollar, open-close curly brace, say ENV, and mention the environment variable. So this one is BUILD_ID-, and I'm going to give another variable, build timestamp, BUILD_TIMESTAMP. Exactly same name because this variable we need to set in Jenkins. The BUILD_TIMESTAMP plugin gives exactly this variable and its value we need to set. So let's go to Jenkins, manage Jenkins system. Search for BUILT_TIMESTAMP. It's here at the end. So this one, so this yy four times, I'm just going to say two yy. That will be like '26, the year. Month is fine, date is fine. And this one I'm going to give here underscore, hour hour-minute. I'm gonna give a hyphen here. I'm going to remove this seconds. So it's yy-MM-dd_HH-mm so hour and minutes. So that's the format that I'm giving. See you can give other old format also if you wish to. Okay, this is good. Now, repository name is vprofile-repo. That is the one that we created in Nexus. Vprofile-repo, this one. Credentials-id, we created the Nexus credential, and its name is just Nexus login. So it's going to use that username and password that we mentioned in this credential. Artifact ID, I'm just going to give the name as vproapp. And in the file, the path of the file is target/vprofile-v2.war. This is what we have seen in the workspace. This is the path and it's type is war. So this is all good, except you need to once again check whether Nexus allows in the security group port 8081 from Jenkins. So let's check that before we do any more changes. So let's go to Nexus. Security groups of Nexus. Edit inbound rule. Well, currently it says 8081 from anywhere. I'm going to change it to my IP and then add one more rule, which is 8081 from the Jenkins Security group. Save the rules. Okay, time to test this one. So let's copy this. Let's create a new pipeline. Go to new item, say vprofile-ci-pipeline we'll say, vprofile-ci-pipeline. Select pipeline. Say okay. Scroll down and paste your new pipeline script. Save it and say build now. Let's check the stages. So I'm going to pause the recording and resume when this pipeline is completed. Okay, it got completed. Now I had few hiccups. That was because the Jenkins ran out of this space. Should not happen with you because you already have 20 gigs of EBS Volume 1 Jenkins. Anyways, this got completed. Let's check the output of publish to Nexus stage. So it says uploading artifact, and you see a URL over here. This is with the prior IP so you won't be able to access it. You need to change the public IP. So it uploaded successfully. If you go to Nexus, go to browse, make sure you select it here, browse, vprofile-repo. So you should start seeing the artifact over here. If you look at the name of the artifacts vproapp, that's the name we have given artifact ID 2. That's the BUILD ID. And then this is the BUILD_TIMESTAMP. At the end, .war. You also get a download link. If you click on it, we'll start downloading the artifact. So run this pipeline a few more times in an interval of at least one minute. So you get different build IDs for different timestamp. So every time you run this pipeline, the pipeline completes. You should see a new artifact with a new version. Maybe first this. So this is a complete continuous integration pipeline, or is it? Well, you see everything, but you don't see the start, you don't see the end, right? So start, we're going to talk about the build triggers later, but in the next lecture, we are gonna talk about notifications. What happens if something fails? You should get notification. DevOps developers should get notification what failed where, and then you or developer can check what went wrong, fix it, and trigger the pipeline once again. So the trigger and the notification that is pending, we are going to see the notification in the next lecture. So if you are done here, and if you're not going to continue to the next lecture, you can shut down all your instance. In the next lecture, we don't need Jenkins and nexus, sorry, we don't need nexus and sonar so you can just shut it down. If you are continuing to the next lecture, just keep at least Jenkins running. Otherwise, shut everything down. For now, that is all. I will see you in the next one.
