# First Build Job

Hello and welcome. In this lecture we are going create our first build job. We are going to fetch the source code from GitHub and build it by using Maven. Along with that, we will be seeing many other settings and features, and also issues that happens at build times. This build job requires three tools Git to fetch the source code. JDK which is the dependency for Maven. And the Maven tool itself. So if we go to Manage Jenkins, Tools, we already see if we have added the tool JDK 17. Let's add one more JDK, which is JDK 21. And this one we have already installed while installing Jenkins. So, just say Add Jenkins, sorry, say Add JDK and give JDK 21 all in caps, and copy this same path. Paste it over here. And instead of 17, just change it to 21 because that's where the path of JDK 21 is. We have already seen this. Let's scroll down. Let's go to Maven Installation. Let's also add one more Maven, Add Maven. And let's say MAVEN3.9.14. And just keep it as it is installed automatically and select the version 3.9.14. So, you see we have two JDK versions and two Maven version. Save this. Go back to Jenkins main page and now let's create the job. Click on a new item. And we're going to call this as vprofile build. And say Freestyle project. Okay. Okay, let's give some description. I'm just saying Vprofile Build. Build job using JDK and Maven. From JDK, if you see, you get a dropdown and you can select which tool you want. So, I'm going to say JDK 17 first. Source code management. We need to fetch the source code and we get an option for Git because we have Git plugins. We are going to talk about plugins in the next lecture also. But since we already have this Git feature, we are going to use it. Once again, keep in mind, you need to understand about the tools. The front end, that is the plugin. And then the backend, which is the actual tool. So, Git plugin is there. We see the Git feature. We also have Git installed in the system. So, we need to fill this detail. First is the repository URL. Now, we know we already seen Git. We know what is repository URL mean. But let's say for any setting, if you do not understand and if you see a question mark there, you can click on it and you get a small help. Like here, it says specify the URL or path of the Git repository. This uses the same syntax as your Git clone command. So, it say Git clone and the URL. Same way you need to give the URL over here. So, let's get the URL. So, go to github.com/hkhcoder/vprofile-project switch to branch electron. Or for now, just click on the dropdown, go to HTTPS and say copy. We're going to use this branch in this lecture, electron. So, just paste the URL over there. Now, here you have credentials. Now, this is a public repository so you can fetch it. But if the repository is private, that will be the case most of the time. Then you need to select the credential. We don't have any credentials saved, so you don't see anything. But we can click on Add, say Global, and you can create the credential. Like, you can save the username and password. You can save SSH username with private key. If you remember in the Git lecture we have seen SSH login with GitHub where we have the username and the private key, right? If you click on this one and go next, you see we can give username and we can enter the private key directly over here. You can say add and just paste the private key. Give the username and you can give description and the ID. So, ID is the identifier. You can just select this ID from the credentials. Now, likewise, you have also username and password and there are a few other things that we'll see slowly in the lectures, upcoming lectures. For now, we don't need this, so just keep it as it is. None. For the branch specifier, we are going to say electron. Nothing special about this branch name. It's just a fancy name that I have given. It's the same vprofile source code. Okay, now scroll down. As I said, triggers and these things we'll see later. Build step. Now, click on Add Build Step. So if you remember from the Maven lecture, we run the command MVN install to build the vprofile source code. So, you can just say execute shell and type the command mvn install. And usually people when they begin, they always go with the commands, but always go with the plugins first. If you don't find the plugin, if you're not able to download or install the plugin that you want to use, then only go for this shell or you know, script. This kind of command. Always go with plugins. So here you see Invoke top level Maven targets. Click on that. And you see the benefit of the plugin. Here you can select the Maven version that you want. Like, I'm gonna select 3.9. Goal is basically the argument that you pass to Maven, like mvn install. So, install is the goal. And if you click on Advanced, you see here, you can mention a different POM XML path. You can mention properties, Java options, settings file for Maven. So, there are many other customization that you get with the plugin. So, that's why I say always go with plugins first. If you're not able to find the plugin, then only go with the commands. Now, we know when we run mvn install, we get the artifact vprofilev2.var. We also want to archive that artifact. So, we're going to click on post-build action. Add post-build action, and we will see archive the artifacts. Files to archive. So, I am saying that any file that ends with... Wait. **/*.var. So, any file that ends with var, even in the sub-directories, you need to archive that, okay? So, pretty simple. Just check once again if you have the URL of Git repository, the branch name electron. We selected Maven 3.9 and goal install, and post-build action, archive the artifact, **/*.var. Let's save this and let's run this build now. You see this loading bar, you can click on this and you can see the live logs that are happening right now. Okay, huge logs. And at the end I see the success, but I wanna show you this. Scroll all the way to the up, click to see the complete log. Okay, so you see the Git repository. It's cloning the Git repository that happened successfully. Now, check here. Unpacking. So, it's downloading Maven 3.9 because we have set there install automatically in the tools. So, it's downloading the zip file of Maven and copying it at this location, var/lib/jenkins/tools and a folder in that you have 3.9 on Jenkins. And then it mentions the complete path of this tool mvn install. So, that's how tools work when you are doing it to Jenkins only. Now, let's run it a few more times. I'm going to go back to the profile build. Click on Build Now. Okay, that got completed successfully. So, now the tool already exists. So, it just mentions the path. Okay, now let's come back. Now, go to configure and change the Maven. And select the Maven default. Now, default means from the system. If the system has Maven, it will execute the command. So, let's save and build it. You see that failed if you click on it. So, you see it's just running the command mvn install. And mvn command does not exist in Jenkins. We have not installed it. So, it says command execution failed. No such file or directory. So, let's do this. Let's SSH to the instance and install Maven, and then execute the job once again. So, copy the Jenkins public IP. Open Git Bash or terminal and just do SSH to this. Open to add add rate the public IP. Switch to root user sudo -i And run the command apt update &amp;&amp; apt install maven -y. We don't know what version of maven it will install, but let's see. So, apt update &amp;&amp; apt install maven -y. Okay, the Maven is installed. Let me check the version, mvn -version. 3.8.7, I think this will not work. Our vprofile source code won't work with 3.8, but let's try that. Okay, so let's run this job once again. We have anyway selected default version of Maven. Okay, it's running. Let's check. It seems it is working, but let's see till it completes. Okay, that worked. I mean, previously there were some test cases that used to fail because of Maven 3.8. But it's working now. Anyways, you see it runs the command mvn install when it just runs the command without the path. That means it is executing the command on the system on the shell. Or if it's Windows machine, it's executing that batch command. So, that command needs to be present in the system. Now, I know I'm focusing again and again on the tools. That is because your majority of the issue with the CI/CD tools will be the tools itself. The build tools or any other kind of tool. People fail to understand how this works under the hood, and that is why it takes a lot of time to resolve those issues. Now, I want you to do this exercise. So, we have JDK 17, we have JDK 21. I want you to install JDK 11 on this Ubuntu machine. And when you are running that job, you need to create a new job and you have to mention a branch name, JDK 11. Now, source code there is a branch called JDK 11. So, when you create a new build job, and if you're using JDK 11 over here, then you need to also change the branch name to JDK 11. Then it's going to fetch the code from JDK 11 branch, right? We also have JDK 21 and a JDK 17, and we have different versions of Maven. So, I want you to play with this build job, create your own new job. Take JDK 11 branch, build it, build the electron branch, see how it fails, and switch to branch JDK 11, see how it works. So, play around with different version, just mismatch so you get a good hands-on practice with these tools. You can also add more versions of Maven and make sure you mix and match different versions and try which jobs work, see the logs, where it fails, what happens? There's nothing right or wrong once again. I don't want your every job to work successfully. But I want you to have good hands-on practice with tools in Jenkins. And this is the starting. So, play with Jenkins. And once you're good, join me in the next lecture.
