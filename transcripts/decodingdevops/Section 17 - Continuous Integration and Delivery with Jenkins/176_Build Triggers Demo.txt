# Build Triggers Demo

Instructor: Okay, let's see our forced trigger, Git Webhook. So to run this job or to run this pipeline, I have to click on Build Now, but I want to run this job whenever there is a commit. Not only commit, there are different events based on different events on your GitHub repository, this job will get triggered. So first, we just copy the URL. 'Til colon 8080. And we will go to our GitHub repository. So go to your repository settings, this is repository setting, not account setting, repository setting, click on Settings. And you will see here option Webhooks, click on that. And say here Add webhook. And paste the URL over here. And after 8080 slash, you have to say GitHub hypen webhook, and a forward slash. Make sure there is a forward slash at the end, no spelling mistake, exactly as it is. In the content type, select application JSON. Don't need to mention any secret here. Now, this webhook will get triggered for a push event or for everything. Or you can select individual events like a branch or a tag gets created. Whenever there is a comment on the commit deployments, there are many options over here, but we will just keep it for push event, so whenever there is a push, trigger the pipeline, like that, we'll see Add webhook. Okay, let's refresh this. Okay, you see this green check mark over here, that means this was delivered. You can click on Recent Deliveries. And you can say that this JSON payload was delivered. Now for some reason if you see a red cross mark over here that is not delivered, then there are a few things you can check, make sure you're giving the right URL. Make sure you selected the right content type. Also, check the Jenkins Security Group. Jenkins Security Group, 8080 should be allowed from anywhere. If it's just your IP, then GitHub will not be able to reach your Jenkins server. Okay, now let's go to our Jenkins job. In the build job, click on Configure. And select here in the Build Triggers, you have to select GitHub hook triggers for GITScm polling. And save this. Okay, now look at the job ID here, that's one. Now I'm going to make a commit in my repository, in this repository, and you will see the job gets triggered. Okay, so I'm here in the repository, and I will just create some file, testfile dot TXT. That's a git add dot. Git commit hyphen M. I'll say test trigger one. Git push origin. Master. Okay, I made a comment, and this should have triggered the job. And there, you see that. So that was GitHub webhook. And next we will see Poll SCM, which is similar but the opposite. So, when I say opposite, that means, in this case, GitHub is triggering the job. But now, Jenkins will check for commits, so we'll say Poll SCM, I'll remove this check mark. So Jenkins will be regularly checking your GitHub commits, any commits on your GitHub repository. And you have to give a schedule in a cron job format, like we did in bash scripting, cron job. Hour, minute, date of month, month, date of week. So, if I want to run this for every minute I have to say star, that means every minute, star, every hour star, every date, one to 31st, star every month. And again, last star, for every day of the week. So total five star, that means run every minute. And we'll say save this. Now you should see here Git Polling Log, click on that. And it says it checked it but did not find any change in your repository, now let's make a... I'll say touch test file two dot TXT. Same thing, Git add dot. Git commit. And Git push. Okay, this is a commit, now Jenkins will be every minute checking for new commit in our repository. And if it finds, it will run this job. Okay, so no changes yet. Okay, see that, changes found, and you see the job ID three, it's in pending state, it will start in a moment. There. Now I know both are similar, gives you similar list result, but here Jenkins will be checking every minute. And GitHub webhook, whenever there is a commit, GitHub will send a JSON payload which will trigger your job. Okay, scheduled job, which is similar to Poll SCM but it is not going to check your source code manager, or your GitHub. It will just run the job at that particular time, so let's see that. Okay, configure. And I'm gonna remove this Poll SCM. Build periodically. Yeah, that's the scheduled one. So again, this is the same cron job format. Again, you can mention interval or a specific time, for example, I want to run it every night at 8:30 PM, so I will say 30 for the minute, 8:00 PM, that is 20. And then third field is day of the month, so I want to run it like on every day. Then star every month, then star every day of the week, or wait, arrange, one to five. Zero is Sunday, one is Monday. So it's like Monday to Friday. Every night, 8:30 PM. Right, so that's building periodically. Now of course we are not going to wait 'til that time. But it's pretty simple, you know how to do it, same cron job format, we have seen it in the cron job in bash scripting, Poll SCM and build periodically, all three are same. Okay, the next one are remote trigger, now this can get slightly complicated, but if you stay with me, follow the document, it will work. Remote trigger means from anywhere, you can trigger that Jenkins job from anywhere, from a script, from other Jenkins server, from your laptop, from some server, anywhere you can run that job, as long as you have the network access to the Jenkins server, you can trigger that Jenkins job, so let's see how to do that. So this is the document that I created for it. It will be in the resource section, you can download it and use it. So we need to do three things here, first is, you need to create a job URL, which is pretty simple to do. Then you need to create a token for this user, this is also simple, the Jenkins user that we log in with, we'll need to create a token for that user. And then we need to generate CRUMB. And the CRUMB will be generated by running this command. It will generate a CRUMB. And all these three things combine together, we are going to use a cURL command. Right, and the final command will be something like this. So you have http colon slash slash admin, that is the username, colon, the token. At the rate, that's the job URL. And hyphen edge, and then the CRUMB. So these three things combine together, and we can trigger this Jenkins job. So, let's do this. Okay, I'll keep a editor handy, I will use this Sublime text editor, just to copy paste things, that's all. Let's go to the Jenkins job, configure. And we'll say trigger builds remotely, example from scripts. And you need to mention authentication token, now you can give any token name over here. I'll just give the name as mybuildtoken. So whatever token name you give, make sure you have to be, it has to be in the URL, and this is the URL, here, copy this URL. And we will need some kind of text editor to paste things. So first, Jenkins underscore URL, let's get the Jenkins URL. Copy the URL 'til 8080. And just paste the URL over here. Remove that extra forward slash. And the token name I have given is is mybuildtoken. Okay, so that is my job URL, the next thing I need is token, and third is CRUMB. Okay, so now let's get the token. So make sure you save this job. The user that you logged in with, click on this dropdown button here and go to Credentials. Sorry, Configure. Click on Configure once again there. Configure. And come to APA token here, say Add New Token. And say Generate. And you should see the token, you can use this to copy it. Token is copied, let's again paste the token. And we'll paste it in this format, admin, colon, the token. Now my login user in the Jenkins is admin, if your user is something else, like your name, use that name. So it's username, colon, token. Next is CRUMB. Now, CRUMB is this long command. WGET, let me copy this command. And show it to you first. Okay, make sure you remove the new line character, backspace from here, space there. Okay, now this should be all in one line. Now in Sublime text editor, there is no horizontal scroller, so, it just shows it in the next line. You can enable though, but this looks better, but make sure you remove any extra space. And we really need to fill these details, WGET auth challenge, user, admin. Password, login password, now mine is pretty simple. So that's the Jenkins username and password you have to give there. Jenkins IP there, so I already have it from here, I can copy this. And that is it, you have to run this command, it should give you the CRUMB, but WGET command is not available by default in Windows. But you can download and install WGET in Git BASH. In MacBook, it will be there, so no problem, open your terminal, run this command, you'll get the CRUMB. But for Windows user, we need to download WGET. So just search on the net, WGET for Git BASH. And the first link itself, how to add more utilities to Git BASH. Click on that. WGET, there's a link, eternallybored. And download the 64-bit ZIP file, ZIP file. Okay, once it's downloaded, click on Open File. Okay, so it opened it in WinRAR here, click on WGET.EXE, and say Extract To. And you need to extract it at this location, C Drive. Program Files, expand that, Program Files, you will see here Git. In this you see mingw64, expand that. Inside that you have bin. In mingw64. And say OK. Now I already have it installed, so I'm just gonna replace it once again, yes to all. And now I should have WGET on my Git BASH, so I'm gonna open next Git BASH. Git BASH terminal was already open, so I just reopened it kind of. Okay, let's copy this URL, make sure this is all in just one line, not multiple lines, so once again. You can just paste it in another Notepad or see if it's in one line. Let's go to Git BASH. And I'm just gonna paste this URL. Hit Enter. And that's the CRUMB. Got it. Copy that. Paste it, so we got all three things now, job URL, admin user token, and the CRUMB, now final thing, is this URL. cURL command, this cURL command, copy this cURL command, bring it to your editor. Hyphen H, backspace space, okay, this is better. Okay, now let's start filling these things, so I'll take first the username and the token. Copy the username and the token. Just paste it here. Okay, admin, colon, the token, add the rate, okay, no space here. Add the rate, at the Jenkins IP, colon 8080 slash job and job name and token, so I'm just gonna copy this, everything. 'Til the IP address. 'Til the IP. And just right over here. Hyphen H and the Jenkins CRUMB, let's copy that Jenkins CRUMB. Okay, okay. A mistake, JJ. Just Jenkins. Okay, I think we are good, let's test it, let's go to Jenkins. Okay, so, the job ID three. Now I'm going to copy this cURL command and paste it in Git BASH. Run it from the Git BASH, you can run it from anywhere. Paste it. Let's hit Enter and let's see the magic. Okay, let's go to Jenkins, and, okay, see that? Fourth job. That got triggered, now that URL, that you can use to run it from a script. And when it says script, it's not just basket, okay, it could be Python, Ruby, any programming language, Ansible, from anywhere you can run that command, and that will trigger your job, and that is with authentication. Okay, the last one is pretty simple, and for that I need to create one more job here. I'll just create test job, I'll say freestyle, okay? And I will just say here, add build step, execute shell, you can do whatever here you want, your choice. I'll say echo. Test, test, test. Save. I guess any kind of job, okay, now. I will say this test job runs only after the build job is completed, so I'm going to go to the build trigger of test job, I will say Build after other projects are completed. And I will mention here, build job name. See that, it shows the job name. And save it. Yeah? That's all, really. So let's test it, let's run the build job. And once the build job completes, let's look at the logs. You see here, triggering new build of a test job. Right, so it triggered the test job, if you go to test job, you will see the test job. Job ID one. That got triggered. So these are some popular Jenkins triggers. As I said, there are more. But these triggers are more than enough to create any successful pipeline. So that is all in this lecture, I will see you in the next one.
