# First Job

Welcome back to Jenkins. Now I know the tools lecture was slightly confusing, but please do not worry. In the next lecture, you'll understand how to use those tools and things will get very clear. So we access tools from jobs. So in this lecture, we are going to understand what is a job, how it works, what are the features and issues with it. There's nothing right and wrong about doing in this lecture, it's just about understanding a Jenkins job. So that's what we will be doing in this lecture, just understand the Jenkins job. So you can click on Create a job over here or you can click on New Item over here. So I'm going to click on New Item here. You need to give job a name. So I'm just going to say FirstJob. Yeah, FirstJob and select Freestyle project. We will be using Pipeline most of the time in the course. Just for understanding purpose, we are using Freestyle project. Click on OK. So freestyle means all graphical. You need to just select options and decide what you need to do. First is the Description. So giving some meaningful description, like we are learning Jenkins job. The few options over here, we'll see them later. Source Code Management, you can select which source code to use and from where to fetch the source code. This, we'll see in the next lecture. Triggers, how the job should get executed automatically, manually, on a scheduled period. Again, we are going to see this later. There's a dedicated lecture for this one. I just want you to see this first, Build Step. So, "Automate your build process with ordered tasks like code compilation, testing and deployment." So most of the magic happens over here, and this needs the settings and features and tools that we call. So click on Add build step. And here, you see a few options. Just go through them like NodeJS, Windows batch command, execute shell command. Now based on the plugins, you will see the features over here. So we have NodeJS plugin, Windows batch command plugin, we have Maven plugin. But for now, just select Execute shell. We'll just execute some shell commands over here and see how this works. So I'm going to see, whoami, that's a Linux command that tells you what user you are, pwd shows present working directory, w command and id command. So these are a bunch of commands that, shell commands that I want to execute on Jenkins. And just save this. So you created your first job that is just some shell commands. Now click on Build Now. Okay, click it a few more times. So job takes time to execute. So click and wait for a few seconds. And here you will start seeing the history. First build ID, second build ID, third build ID. If you click on this green check mark, this means success. If you click on this, you go to the console output. Now read all this carefully and try to understand it. Says, "Started by user Admin." That is the Jenkins homepage username. "Running as a SYSTEM. Building in workspace." This is the path where it is executing these commands. And this is your command, whoami. The output, jenkins. So the job is running with the jenkins user. Pwd, that is where the command is getting executed. W showing you load average, the number of users. Id command shows you the user and its UID and GID. So always keep in mind, Jenkins jobs are running with the jenkins user privilege. Now go back to the job, click on FirstJob, and go to Configure, click on Configure, scroll down and add another build step. Click on Add build step, say Execute shell. And here execute the command sudo apt update. And save this. Now let's run this job, Build Now. Okay, the job has failed. You see that, failed symbol? Click on that. So you see all the commands that we executed previously is working as expected. Sudo apt update is failing. Now can you figure out why? So apt update is a system command. You need sudo's privilege. This job is running with jenkins user privilege, which does not have sudo's access or it is not root user. So definitely jenkins user cannot execute apt update command. So anything fails in the job, the entire job will be declared as failure unless you set an ignore option that we'll see in pipeline later. Now, let's click on Configure once again, remove this command and run this command, cat /proc/cpuinfo, and redirect the output to a file called as cpuinfo.txt. Now along with this, I'm going to run ls -ltr command, and once again, pwd command. Let's save this. So you know, this will run the command and create a file cpuinfo.txt, right? But where and how to access it? Let's run Build Now. Let's take a look at the console output. So it executed our command. And if you're in ls -ltr, you should see that file over there, and it's in this folder. Okay, so Jenkins, there's a folder called workspace. In that, you have FirstJob folder, and in that, you have this file. Once again, take a look. It is owned by the jenkins user and the jenkins group. Now if you go back to the job, you should see here Workspace. In the Workspace, you see the file. You can view this file. So that's the content of cpuinfo file. And you can also choose to download it all files in zip. Okay, just do the simple, simple exercise. I know it's like little childish, but trust me, I'm training you for something that you will be regularly doing as a DevOps engineer. You can also clean the workspace. You can say Wipe Out Current Workspace, which removes everything from the workspace. Now see, this workspace is off this job, the content of First Job folder. If you click on Workspace now, we will see nothing, but if we run Build Now, it'll generate the workspace once again. So if you click on Workspace, you should see this file once again. Okay, one last thing with this job. So click on Configure once again, and if you scroll down at the end, you see here Post-build Action. What do you want to do after the build is completed? Like send an email notification or archive some files and send it somewhere else, et cetera, et cetera, based on your requirement. So if you click on Add post-build action, you'll see many options, but again, based on the plugins like email notification, you see Editable Email Notification, you see some GitHub commit commands. But I'm going to select this one, Archive the artifacts. Files to archive, and I'm going to say here **/*.txt. Now this means find any file that ends with .txt, archive it and keep it. So *.txt anyways means any file that ends with txt. The beginning two stars, slash means recursively. So recursively, even if you have subfolders from any folder, find files that ends with .txt and archive it. That's what this means. So click on Save and say Build Now. Okay, so that's the job, right? Completed successfully. If you go to Workspace, you see that txt file, right, in the workspace. But if I clear this workspace, if I click on Workspace once again, the workspace is gone. But you will see here the Last Successful Artifact. This is from the post-build action. It has archived any file that it finds with .txt extension and kept it over here, which you can then access. So this is the artifact of the job. The workspace keeps the current temporary files. So practice this a few more times, get comfortable with the job. In the next lecture, we'll see a job that actually makes some sense, like building and testing the code. So that is all in this lecture. I will see in the next one.
