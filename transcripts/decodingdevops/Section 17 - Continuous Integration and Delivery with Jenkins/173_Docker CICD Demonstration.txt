# Docker CICD Demonstration

Okay, so this is the pipeline code to deploy container on ECS. We need to mention the cluster name and the service name over here. So let's copy the cluster name and the service name. So the cluster name is vprofile, so I'm going to take it, and in the double quote, mention the cluster name over here. Then the service name, vproappservice. That's my service name that I'm going to mention over here, service, in double quotes, vproappservice. Scroll down. So this is the stage to deploy container to ECS. Make sure the awscreds are already saved and make sure your region is correct. Mine is us-east-1. So make sure all this information is correct and copy this code. And let's go to Jenkins. So log into Jenkins. And first we need to install a plugin, AWS Steps plugin. So go to Manage Jenkins, go to Plugins, Available plugins, search for aws steps, put a check mark and say Install. Now this plugin gives us this withAWS option. So it's going to execute the shell command, but first export all these options. So let's go back to Jenkins main page. Click on a New Item, giving the job name as Vprofile-CICD-ECS, pipeline, OK. Scroll down and paste all your code over here. Save this. Now it's showtime. Let's click on Build Now. Go to Stages. So I'm going to pause the recording right now and resume when this is completed. Okay, so the entire pipeline got completed as successfully. Let's click on Deploy to ecs stage, and let's see the logs. So here it executes the command, aws ecs update-service and returns all the JSON output of the container service. Let's go to the cluster. Let's go to the service. In the service, if you click on deployments and scroll down here, you should see the information. You see here the service vproappservice has stopped 1 running task, deregistered 1 target in the target-group and began draining the connection, and the new deployment completed, and it has reached a ready state, oh sorry, steady state. So this is a pretty simple deployment. We are just forcing the container to fetch a new service, forcing the service, ECS service to fetch a new container. But there are better options like updating the task definition, creating new task definition, different versions of task definition. And let's take a look. So in ChatGPT, I have mentioned that this is my pipeline. Can you suggest me a better ECS deployment options? And I have mentioned my entire pipeline. So let's see. So it say the pipeline is already quite solid, but there are a few issues that you need to take care of. Instead of force new deployment, what you can do is the deployment strategy. Best practice for ECS is to register new task definition. So instead of forcing new deployment, we can create a new task definition, update the image tag dynamically. And this is one example steps, script. This is the groovy way. So search and replace for image URI. In the taskdef.json file. It's going to create a new file, and this new JSON task definition file, it's going to register with aws ecs, and the next task will be deployed to ECS. So similar way, with mentioning the credentials and the region, it calls aws ecs update-service and mention the new task definition. So instead of forcing a new deployment, we can mention a new task definition. That's a good practice. So that I leave up to you. Take it as an exercise. Try to do this. See what problems or issues you get and try to fix it. In later lectures, when we learn Kubernetes and all, that time, we are going to see the better way of deploying our container and running them using Kubernetes. But for now, practice on ECS, get your hands dirty. Now, I'm not saying ECS is not for production, but if you have many, many containers running where all the application runs on container infrastructure, then Kubernetes is the best option. The focus of this section was to train you on CICD and not on containers and Kubernetes. So I kept deployment here simple by using ECS. For now, play with this infrastructure, play with the CICD pipeline, make changes to your experiments. Once you're good, join me in the next lecture. We are going to clean up. And after that, we have few more lectures where we are going to learn a few more amazing and important things in Jenkins, in CICD, like build triggers and more. I'll see you in the next one.
