# Plugins, Versioning & more

Lecturer: Welcome. In this lecture, you are going to learn about plugins, versioning, and few more things. Basically the idea is to train you on plugins and how to use them and when. And also we are going to talk about the concept of variables in Jenkins job. Now, all this information we can use later in pipeline code. So first, let me take you to the plugins section in the Jenkins, you click on Manage Jenkins. And over here you should see Plugins, click on that. So your four sections over here, Updates, the plugins that needs to be updated. Then you have Available Plugins, from where you can search the plugin name and install it. For example, you want to upload the artifacts to S3 bucket. So you can search for a plugin with S3 name, and there is S3 publisher, and there'll be few other with the name S3, depends on what is your use case, you can install that plugin and use it. So all you need to do is find your right plugin, put check mark and you click on Install. The plugin should be installed, and now you should see the S3 options in your jobs. Okay, we'll come to that. Let's go to, once again, Installed plugins. Okay, yeah, now, we're in Available Plugins, right? So you can find a list of plugins, you can search and install and then start using it. Installed plugins will show you the plugins that are installed. You can disable some plugin. For example, you can disable the Ant Plugin. But if you see, not all the plugins you can disable, because some plugins depends on other plugin. For example, Amazon Web Service SDK::Minimal, this plugin we did not install. When we installed S3 plugin, it installed this plugin. So first we need to disable the S3 plugin, and then only you can disable this plugin. This Advanced Settings. From here you can add new plugin as in plugin was developed separately. You developed it, or you got it from some third-party some developers developed it, you can deploy that plugin. You can say choose file and upload the plugin. Okay, now let's go back to our job, Vprofile Build job. Workspace. Well, we have the target, and here we have the artifact, vprofile-v2.war. Now, let's say I want to create more versions of it. Now every time the job runs, it gives me a new version of artifact. If I keep running this job again and again to overwrite this artifact, if we have a newer version every time we run, and that won't be replaced by the previous version, right? So how do we version? Let's create a new job, let's click on New Item. I'll give the name buildartifact, Freestyle and I'll say copy from the build job. So Vprofile Build job, I'll copy it from. Let's come at the end and remove this Archive the Artifact. So and we install that is going to build the artifact. After that, we are going to add one more step. We'll say execute shell and we will say that create a folder first of all, named versions mkdir will give -p option. So if the folder exists, -: p option is not going to throw any error. But if you don't have -p, then it's going to throw an error that folder already exists. Use -p option. Then we'll give the command cp, the target directory slash vprofile-v2.war copy to the versions folder with the name v. Now what do we do? Give the version, right? If we give a static name every time we run the job, it'll have the same version. So this needs to be dynamic. A variable we need to give that changes every time we run the job. Okay. So come to jenkins.io and let's search for variables using environment variables. Let's find that last pipeline code we are going to see that in few lectures, in next lectures in fact. So over here you see using environment variables. So Jenkins has built-in variables. When you run the job, you have Build_ID that gives you the Build ID number, Build_URL, Java_Home, Jenkins_URL, Job_Name and a few others. Now these are very basic things. We'll be seeing proper version mechanism later in the course when we use Nexus repository. Just an exercise to make you aware about variables in the Jenkins. So let's come back and let's give here $BUILD_ID.war, we'll say here maybe vpro. Okay, now let's save this and let's run this job few times. Okay, I executed this job three times. Let me show you the console output. There, if you see vpro3.war has (indistinct). If you go to the Workspace, versions folder is there. Right, so three time executed the job. Every time I get a new version of my artifact. What if I need to pass a version, right? I need to mention specific version. In that case, you need to have a job that is parameterized, which takes information from you. So configure the job and click, put a check mark where this project is parameterized, Add Parameter and you need to get the string parameter. Give the name of the variable version I have given and default value, you can mention if nothing is passed, what will be the default value, that also you can mention. Oh, you can mention some description. Now let's save this and let's see where, wait, we need to configure it once again. Okay, come back. Okay, so this is the variable. We need to mention this while we are versioning. So let's copy that variable. Let's come down, scroll down, okay, there instead of BUILD_ID, we can just put over here $VERSION. I just created another line. This line I put a hash mark so that's commented. So we put a $VERSION, so this variable does not have any value. We need to pass a value when we run the job. So make sure you give this and save this job. Now if you see here, gets the option of Build with Parameters. We don't get Build Now. So if you click on that, now version, we need to enter the value. Now I'm going to give semantic versioning. Let's say 2.3.6, Build. Okay, that's done. Let's go to the Workspace and let's check in the versions. There, the vpro2.3.6.war. We should have given a underscore, a hyphen there, so we can make a change at that level. Okay now, what if I need timestamp? Some artifacts should be stored with the timestamp. In that case, you need to have a variable that generates the timestamp automatically. Now there is a plugin for that. Let's go to Dashboard. Let's go to Manage Jenkins and let's go to Plugins and let's say Installed, sorry, Available Plugins. Now if you really don't know the plugin name, no problem, right? Just open the browser, Google whatever search engine you want and search, for example, timestamp plugin in Jenkins or this in Jenkins or that in Jenkins, you will very quickly get the plugin, that you plugin name that you need to install, right? So there's one plugin, Build Timestamp, let me search it, build timestamp. There. One more thing, the search is not so good, so you need to give proper names. Like I can build space timestamp. Anyways, so we get Build Timestamp, right? So this plugin adds BUILD_TIMESTAMP to Jenkins variable and system properties. So we have BUILD_ID, right? Now this plugin will add one more variable in that, BUILD_TIMESTAMP. So let's put a check mark and let's install it, along with that installing few other thing like SSH server stuff. Anyways, let's go back. Let's go to Dashboard. And we need to set the value of Build Timestamp, the format. So go to Manage Jenkins and all the global settings are here in System, Configure global settings, click on that. After installing Build Timestamp plugin, you should get this option over here, Enable Build_Timestamp. There is a Timezone and here is the pattern. That's year, month, date, hour, minute, seconds. Now I don't want this format. I want to use my own format. That one, date, month and year, just the last two number of the year, underscore hour and minute, and let's save this. There is no hard and fast rule in this. Use your own format if you wish, configure because we are just learning. So you know, whatever works for you. Okay, let's put a hash there, let's comment that and let's add another line. Now we give the variable over here, we put $BUILD_TIMESTAMP, all in caps. Okay, that's done. Let's save this. Okay, if you also need to uncheck this, build parameters. Otherwise you'll always ask us the parameter. Let's configure, and let's uncheck that. Or you can give a default value. So even if you don't pass, you can run the job. Let's just uncheck it for now, save it. Okay, now run this job like three times in an interval of one or two minutes. Make sure there is a minimum interval of one minute. So we get new artifacts every minute. Okay, I executed this job two times. Let's go in Workspace, Versions. And you see there, the versions? Right. Now, this is not a storage, we not going to store different versions of artifact in Jenkins. After that, next step should be to move this artifact out of Jenkins to some storage like S3 or Nexus that we'll see later, right? So if you go to the configure job, we, if you remember we installed the S3 plugin. So we can go to Post-build Action and you should see here Publish artifact to S3 bucket. Right? You don't need to do this, I mean if you want as an exercise, do it. We'll be doing this later. This also needs credential or you can just simply add S3 role to the Jenkins instance. Create a role that has S3 full access. Add that role to Jenkins and then you can mention files to upload, source, destination S3 bucket, all the information and then you can push this artifact to the S3 bucket. Just showing you over here, we'll do these things later. Great. So in this lecture, you have learned about variables in Jenkins. Jenkins has own variable and you can inject more variables into it by using plugins. You're seen plugins, how to install, how to list, how to enable, disable. We'll be using so many plugins in this course. We'll see that later. So don't worry about that part. We also talked about versioning in very, very basic ways. So do some hands-on. Just try your own methods of versioning. Make sure you're comfortable with all this because now we will be starting with Jenkins pipeline code. Things will get very serious from coming lectures. So make sure you are very well-versed with whatever we have covered so far in Jenkins. And then join me from the next lecture. See you.
