# Build Triggers Intro

Professor: Welcome. In this lecture we will see job triggers. So far we are running Jenkins job or the pipeline manually. We click on Build Now button but Jenkins jobs can also execute automatically and there are various triggers which you can use. So you don't need to manually click on a Build Now button. Let's see, some popular triggers. The most famous one, Git Webhooks. In this Git repository like GitHub will send adjacent payload whenever there is a commit in the repository. So your GitHub repository will trigger your Jenkins job. The other one is poll SCM which is just the opposite of Git Webhook. In poll SCM Jenkins will check for commit in Git repository. In an interval that you specified like let's say you said check every five minutes or every minute for any new commit if there is any new commit in the GI repository the job will get triggered. Scheduled job, pretty simple. You mention date and time like an alarm clock in crown job format and Jenkins will make sure your job runs at that particular time or in some intervals that you specified. Remote triggers. Now this is slightly complicated and most of the people will skip this one but I think this is one very helpful trigger for DevOps engineer or architect. By using this you can trigger Jenkins jobs from anywhere, from a script, from an Ansible playbook, from anywhere for that matter. And there are tokens and secrets and URL multiple things goes into that and you get an API call, which you can use to trigger your Jenkins job. And do not worry, I have a piece of document for this one. We'll go through it and we'll set up the trigger. Build after other projects are built. This is pretty simple. You just select that after this job is completed run this particular job. So here trigger is completion of a previous job a previous job gets completed, and then after that your job gets triggered. Now these are some popular triggers, but not all. And of course we cannot cover all the triggers but I'm sure this, these triggers are more than enough. So to set up a proper and successful pipeline. Now let's see the steps that we are going to take in order to learn all these triggers. First we need to create a gate repository. You cannot use my Git repository. Now you have to create your own Git repository. So I will show you how we can do that. Then we'll set up an SSH authentication to our GitHub repository. And we'll create a Jenkinsfile. We'll place it in our Git repository and we'll commit it. Then we'll create a Jenkins job, which will access this jenkinsfile from our Git repository. And then we will start testing our triggers. So step one to four is the setup that we need or prerequisite site that we need in order to learn these triggers. Once you learn how to use different triggers you can put them in any job you want. So what are you waiting for? Let's get into the action. Okay, so first we'll create a Git repository on GitHub. So log into your GitHub account and create a new repository. Click on new. You can create repository with any name. I will give the name as jenkinstriggers and I will keep this as private. You can use private or public because we are not going to store anything that is really private for us. Just a simple jenkinsfile and we'll see create repository. Okay, so repository is created and we will be using the SSH link. So before we clone this Git repository we need to store our SSH keys in our GitHub account. If you already did this in Git section you can just directly start cloning this. If you have not done it, then create the SSH keys open git bash in Windows Macs open terminal and the command SSH-keygen. So it'll generate the private and the public key. Now, I already have the key, so I'm not going to overwrite I'm just going to say no. If you don't have the keys, then just hit enter until the keys gets created and you should find the keys in your home directory, tilde /.SSH and you should see the public key and the private key. We will take the content of the public key we'll copy this content public key again and not the private key. Private key is a very long one. This is the public key. So copy the public key go to your GitHub account, go to settings. This is the accounts setting, not the repository settings. So you need to click over here and come to the settings and go to SSH and GPG keys. Click on new SSH key here and give a title. I will just say my laptop and I'm going to paste the public key here. Again, public key, not the private key here. Add SSH keys. Enter the password if it asks. Okay, public key stored. Now let's go to a repository that we just created. Okay, there Jenkins triggers. And click over here on SSH. And you need to use this link not the HTTPS, SSH link. Just copy it. Let's go to Git Bash. Clear the screen. I will create a directory in F. You can create it anywhere. I will go to this directory and I will say Git clone and paste the URL shift. Insert it. Enter. It'll ask you this. Say yes. Okay, so you cloned the repository. You should see it over here. Now we'll place a jenkinsfile into this. This is will be a very very simple jenkinsfile which will just print a message. So I open my sublime text editor. You can use any editor of your choice and I will have this code over here. Pretty simple pipeline agent, any stages, only one stage. Stage name I have given us build. Step is just to echo this message. So we really not doing any build part over here we are just using the simple jenkinsfile. Now you can store your original jenkinsfile which we already created, but for now, just keep it simple. Just create the simple jenkinsfile and let's save it. Save it. I'm going to save it in F, Gitrepos, jenkinstriggers. This is our repository. I will say file type all files and file name will be Jenkinsfile, J capital. Save it. Okay, once you save this file, and if you'd like to use VIM editor, whatever editor just create the jenkinsfile in the repository. Oops. I don't know. For some reason it named it as jenkinsfiles.groovy. I'm gonna rename it to just jenkinsfile. Yeah, just jenkinsfile. Okay. Now we'll say Git add dot, Git commit -m will say first commit. Git push origin master. Okay, that's done. Let's go to GitHub and check. Let's refresh the page. And we should see the jenkinsfile. Now if you want, you can add other things over here but for this exercise, we just need the simple jenkinsfile. Okay, now let's go to Jenkins and create a job that can fetch this jenkinsfile and run the job. So let's go to Jenkins. Now, before we create the job there is one step that we need to do. And this step is a precautionary measure of for an error that you may get. When you try to access your Git repository from Jenkins. You may get this error host key verification failed. And you may get this because Jenkins is doing SSH to the Git repository. You will see your Git repository, URL here and it'll do an SSH to the GitHub account or GitHub server. And if you remember, whenever you do SSH for the first time to any Linux machine you get a question asked, this is a fingerprint do you accept the connection? Yes or no? You say yes, and then you enter in the shell. Now Jenkins will be also trying to do the same thing. And if you have strict host key checking enabled it'll not accept the connection and you will get this error host key verification failed. So all you need to do is go to manage Jenkins, in the security, configure global security, scroll down, and here you will see get host key verification configuration. In this, you have to select the option accept first connection and say Apply save. Or you can just click on save. So remember, whenever you see this error, host key verification, this is the step that you need to take. Let's go to Jenkins dashboard and type time to create the pipeline. Say create job, you can again, give any name to the job. I'm just going to say build. That's all. Build, pipeline. Say, okay. Let's come down. In the pipeline, I'm going to say pipeline script from SCM, source code manager. And the source code manager is Git we are using and we have to give here the repository, URL. So let's get the repository, URL click on the code and SSH URL again, not HTTPS. We are using SSH URL. Copy this. Paste it. And we need to store the credential. So we are going to say add Jenkins. Now we are using SSH keys, right? So we have to select here SSH username with private key, right? I will give some ID here. I say gitsshkey. Username will be your GitHub account. So I'm giving my GitHub account and private key enter, directly. Add and let's copy the private key now. Open Git Bash or your terminal. cat ~/.ssh/id_rsa. Now this is the private key, right? So let's copy it. Copy. Make sure you copied properly like I did. Do not copy any extra characters. Let's go there and let's paste the private key. Once again, private key in Jenkins, public key in GitHub. And let's say add. Okay, let's select our credential. I'm going to select Git, SSH key. That is my credential. And you see the error just goes away. That means we are authenticated to our repository. And this job, basically pipeline script from SCM from this GIT repository, use this key for login. Branch master because we are storing just in the master branch. Script path. So where is your jenkinsfile? You can give the path. Now our jenkinsfile is just in the top the root directory that's here in the repository itself. So we just give the file name and we'll say save and we will say build now. Okay, so that got completed because it's pretty simple. But the code of the pipeline script that's not the point here. The point is, we are using jenkinsfile from our repository. And we are using SSH authentication. Now this is very safe and now very popular method. Okay? So we created the Git repository on GitHub. We did the SSH authentication. We copied the public key there, cloned the repository. We placed the jenkinsfile, we committed it. We created a Jenkins job, which you have seen just now. Now in the next video, we will test our triggers we'll learn all the different kinds of triggers. So complete this setup and join me in the next lecture.
