# Pipeline As A Code Introduction

Instructor: Okay, so what really is Pipeline as a code? It's Jenkins way of setting up Pipeline automatically by using a file called as Jenkinsfile. J capital there. So we can put this file in our source code or we can write it in the Jenkins job itself. Jenkinsfile really defines the stages in the CI/CD Pipeline. As we create jobs, there'll be different stages in the Jenkinsfile define. Jenkinsfile is a text file and it has its own domain specific language, but it's very close to groovy, but you really don't need to know groovy to write a Jenkinsfile. There are two syntax, one is scripted and the other one is declarative. Declarative is the way forward now and we are going to use declarative in this project. Okay, some concepts in the Pipeline. First of all, you have Pipeline that's gonna be in the main block and everything inside Pipeline will be executed by Jenkins. Nodes or agent, they're both settings. You can define where this Pipeline can get executed, on which node or on which agent. There are stages where the actual execution happen. In stages you have steps. The steps could be commands like Maven install, or Git pull, or upload artifact to Nexus, or any such step that you want to execute from your Pipeline. Let's understand the syntax. That's how it looks. So you have Pipeline block, and in inside that you have all the other settings. Let's understand all the settings one by one. Pipeline, that's the main block of code. So you say Pipeline two curly braces and everything comes inside your Pipeline code. In Pipeline you have things like agent, you can define where the job is going to run. Tools from your global tool configuration, if you have included some tools. Then you can mention the tools like Sonar, Scanner, or Maven, or JDK. Environment will be the environment variable, and stages will be the steps that we are going to execute in our job. So the agent block will look something like this. Agent, you'll give the name of the agent, the label, or you can just say agent any also. So it can run on any node, even master or slave. And tools you give the name and you mention the tool name that you have included in global tool configuration. Other things in Pipelines are environment variable. So setting variable of value so it can be used in your steps, in your stages. Like in this case you see some nexus variable, which we are going to use in our Pipeline. Stages, that's where the main execution goes. In stages you have stage, you can have multiple stage, like one stage that clones the source code, other stage that is going to run Maven build, another stage which can publish the artifact to Nexus, and like that you can have stages to deploy artifact, to do code analysis, you can have a number of stages there. In stage, so you have stages, inside that you have stage, you can have multiple stage, and in stage you have steps. This is where you put the actual command. And also post installation step, like probably you want to send email notification or want to archive the artifact. Something like this. Stage, in parenthesis you give the name of the stage. Then inside that you have steps, like Maven install, and you can have post installation step, like archiving artifact, or you can print some message also like echo. Okay, so in DevOps, everything needs automation. Even your Pipeline is automated with Jenkinsfile. So now let's do some hands-on. So we are going to write a very basic Jenkinsfile first. That simply means the Pipeline code. It's called as Jenkinsfile with J capital. So I'm using VSCode, again you can use any editor of your choice. If you're using VSCode, you can install some extension so it highlights and shows all the curly braces and all the colors properly. So you can go to extensions and search for Jenkinsfile and install this one, or Jenkinsfile Support, this one. Install it. So you just see the highlighting. Or you can even install Groovy. Once you install, just go to file, say new text file, and just save it, save it somewhere with the name Jenkinsfile, J capital. Jenkinsfile, J capital, save it. Okay, let's start writing. So the first thing that you're going to write in the Pipeline code is going to be the Pipeline itself. Pipeline and open curly brace and close it. Make sure cursor is in between the curly braces, hit enter, in between all this is your Pipeline code. Okay, first thing we write in the Pipeline code is, where do you want to run the Pipeline? We know we've been running it on Jenkins Server itself, but in Jenkins there's a concept of agents that we'll be seeing later. So you can select on which agent you want to run the Pipeline. From the Jenkins server, it'll select the agent. So we don't have any agent, so we are going to just say agent any, okay, like that. Agent any. And then we need to mention the tools. Now we are going to create a build Pipeline. So it's going to fetch the code, build the code, test the code, archive the artifact, stuff like that. So we need build tools Maven, and we need JDK. So we need to mention that first. So after agent, we'll just say tools. Tools. Okay, open and close curly brace, and in the middle of it you can mention the tool name. Maven is the tool name in Jenkins, but the tool name, the version that you have added in global tool configuration, that you have to mention over here in double quotes. So in Jenkins, tools, manage, Maven installations. We have given this Maven 3.9 all in caps. Let me copy that. In the double quote you mention that. Now we need JDK's correct version. So we are going to see JDK double quote and we need JDK 17. Well I think it's in caps. JDK 17. Let me confirm it once again. Yeah, it's JDK all in caps and 17. Good, so these are the tools. So the tools curly brace closes over here. After this I'll give some space. Okay, here we are going to write stages. Stages, open close curly brace. In the stages you have stage, and stage will have a name. So in parenthesis you have to give the name of the stage. So first I want to fetch the code. In the single quote I have given fetch code, that's the name. Then space, open close curly brace. Okay, now in between the stage curly braces, you need to mention steps. So mention steps, open close curly brace. And in the steps you're going to mention the plugins, all the steps that you want to execute. Maybe a shell command you want to execute, or maybe a plugin. Now you want to fetch the code and there is a Git plugin. If you go to Git plugin documentation, you will see how to write that plugin in the Pipeline code. First you need to write git the name of the plugin, space, and then you mention parameters. So parameter you give colon and its value. For example, we have to take branch, right? So branch is one parameter, colon, space, and you give the value. So I'm going to give single quote and the branch I'm to give, atom, then comma, separated by the next parameter, URL. So we have seen in freestyle project, when we create a build job, we have different options there, we have Git and then in Git we have, we mentioned branch, we mentioned, you know there is form filling. How do we do it here? So documentation we'll have, but mostly this is the syntax. You give the option name, branch, colon, and it's value, comma, and then you give the next one. How many ever you want to use it, that many you need to give comma separated. So next is URL. In single quote, mention the URL of your Git repository, the HKH coder VProfile Git repository. After this I'm going to add one next stage that will be to run unit tests so that I want to run the command MVN test. How do I do it? You know what I'll do? I'll simply copy the stage. Copy. And just below the stage make sure we have the curly brace end for the stage there, just below it I'm going to paste it and I'm going to make changes. Stage, name, unit test, steps. I just want to run MVN test command so I can use shell options. So in the steps I will simply say sh. If you want to run just the shell command, you can just say sh, and in single quote it can give the shell command. So I'll simply say MVN test. How about that? You see that? How easy was that to create the next stage? Just need to know what you're doing and be careful on opening and closing. Okay, now I want to use MVN install stage. I want to add one more stage. So what do I do? Well again, I'm going to copy this, and I'm going to just paste it here. And instead of unit test, I'm going to replace that with the text build. Build. And step, I'm going to give here MVN install. Now we already executed MVN test before this, right? So I want to skip the test, because if I run MVN install, install phase is going to execute all the previous phases including test. So I'll just skip that. So there is a maven option that we can use, hyphen D, any Maven option you want to pass, you have to use hyphen D. And then you just, without any space, you write that. So skip test. When you pass this to MVN commands it's going to skip the unit test. Okay, so steps are like build stage that we do it in Jenkins jobs. There's also post build action, if you remember. For that we have something called as post. So in the stage itself, this is the stage, from here till here, right here. And in the stage itself, just after the steps curly brace ends, you can give here post. Open and close the curly brace. Make some space in the middle. Success message I want to print if everything is successful, right? So success, open close curly brace. I'll just say archiving artifact. So if this completes successfully, then it'll execute this action, which is echo print archiving artifact. And then we are going to use the archive artifact plugin. So archive artifact is the name of the plugin in that we can pass the option artifacts and it's value as **/*.war. Okay, so we are done writing our Pipeline code. So once again, Pipeline open and close curly brace, and everything is in the Pipeline. And then you have agent, tools, stages. In the tools you have mentioned the tool names that we installed in Jenkins. In stages we have three stages, first stage is to fetch code, second to unit test, third build. In first stage, we have one step, in every stage we have one step, except in the third one we have one step and one post. So let's copy this entire Pipeline, come to Jenkins dashboard, click on new item. I will name it as VProfile Pipeline and select here as Pipeline, select item type as Pipeline, say okay, and scroll down. And over here you can paste to the Pipeline. Now there is an option here, Pipeline script from SCM. That means you fetch the source code and it is going to expect right here, yeah, it's a Pipeline script from SCM. So you mentioned the source code like Git, you mention all the details like repository, branch, credentials, everything. And then you mention the script path. This will be with this name, Jenkinsfile, not mandatory, but mostly it is with this name. And in this file you will have this entire content. But now we will just do this Pipeline script, paste your Pipeline, save it, and click on build now. Okay, let me refresh this page. Here you'll have the stages. Okay, so tool install is good, fetch code is done, now it's doing unit test. But where is the build stage? Oh yeah, I see it now, got executed, then it is showing. Okay, let's go back here. Okay, so this is the entire console output. All right, something goes wrong, not able to figure out, you're going to send this, not the screenshot of Pipeline failed. Okay, stages, there's nothing you can see over here except when you click on it. So when I click on this one, then it shows what's happening. And again, you have to click on this dropdown to see the entire console output. So every stage, separate console output we can very easily see from here. Okay, now let me take you to the source code repository. And here you can see a Jenkinsfile. Click on that. In the main branch, or some branches, and here you see one example of a Pipeline code which fetch the code, and you see something gibberish. Don't worry, we'll understand this. SonarCube code analysis and publish the artifact to SonarCube. And don't worry about looking at all this code. This is just copied from the documentation and we made the changes. Yeah, a few things we did right? But Pipeline code is just one time work for one project and then it's updating that. So you don't need to write it like on a daily basis. Even if you want to write it on daily basis, not a problem. We have good documentation, and Google, and ChatGPT, and everything to help us. And this problem will be just for the beginning. Once you get comfortable with Pipeline code, no turning back. Check the documentation, and documentation, you can say Pipeline, getting started with Pipeline. Yeah, there is a nice video of how to write a Pipeline script. You see everything is given step by step, select the Pipeline, go here, write the stage. You see that? Agent, any stages, stage, steps. So all the syntax is given over here. And you can just simply Google, I want to include the docker build in Jenkinsfile. You'll find so many blogs. Let's also try ChatGPT. So I'm just going to put here Jenkinsfile and gives all the detail different kinds of Pipeline, decorative Pipeline, scripted Pipeline, and example. Nice. Okay, it's going on and on and on and on. Okay, just for initial purpose, read this. Once you get comfortable with this, you don't need to come to even ChatGPT to write your Pipeline. You can just go to the documentation of that plugin and just copy and put it in your Pipeline. I'm just asking ChatGPT, "Write me a Jenkinsfile that will fetch the code from GitHub, build it with Maven, and upload the artifact to S3 bucket." It's writing everything. But you will understand all this only if you know the basic syntax of Jenkinsfile. You can write it by yourself, you know how to do it, then you can understand it. Because when you want to make the changes, like credentials and stuff, we'll see that later. S3 bucket variable. These are the environment variables that it has set, and then it'll be using those environment variables. So it's fetching the code, building the code, and uploading the artifact to the S3 bucket. So we can take this, make our own changes, put it in Jenkins and run it. So that's how you use ChatGPT to write your Pipeline code. Only if you're aware about Jenkins, and Pipeline code, and AWS, Maven, Git, all these things, then only it'll be useful. Not like any Tom, Dick and Harry can come to ChatGPT and become a DevOps or write a Pipeline code from scratch without even understanding anything. That's not going to happen. At least not now. Who knows about the future, right? But anyways, for now, or for years from now, this will be the work. You need to be aware about the basic concepts, then only it'll make sense to take the help of AI. All right, from the next lecture, we are going to do more serious stuff with the Pipeline code. We are going to do code analysis. We are going to run quality gates, we'll version the artifacts, store it in excess, all real time stuff. So if you are done with this, then you can join me in the next lecture.
