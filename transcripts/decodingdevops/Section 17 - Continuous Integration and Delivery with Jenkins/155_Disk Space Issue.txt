# Disk Space Issue

Instructor: Welcome, in this lecture I am going to talk about the disk space issue in Jenkins. Not just Jenkins in general but, this is the use case. In this course you might get this error, your job might fail if you look at the log console output. In the error you might see this no space left on device. So we'll be doing too many things in Jenkins, we'll be installing a lot of plugins. We'll have many jobs that will create data, so workspace size will increase. And Jenkins which we're using is an EC2 instance, by default it comes with 8GB of volume that won't be enough and it will go out of space. What do you do in that case? Well, we can increase the volume. So go the Jenkins. Go to storage. And you should see here this volume, this is 8GB size, by default. So let's click on that root volume. Change its name or let's give a name to it called JenkinsRootVolume I'm giving. Let's edit its size, so actions, modify volume. Will increase its size to 20GB. Now before I increase I want to show you this. You can access to the Jenkins. Switch to root user. Sudo hyphen I. Clear the screen. And if you run the command DF hyphen H you should see the disk space. So that is the partitions 6.8GB showing. If you run the command fdisk hyphen L. There's the disk xvda 8GB and it has this partition xvda1, see that around 7GB. So now let's increase the volume size to 20GB and say modify, modify. Once that is done let's reboot the Jenkins instance. Jenkins Server, instance state, reboot. Wait for a few minutes the Jenkins will come up, then we'll log in and check again. Okay, after around two to three minutes I'm doing access to Jenkins. Let me with to root user. Let's clear the screen. Let's run the command fdisk hyphen L that will show you the partition size. So the volume itself is 20GB. The partition has increased, do you see that? Now DF hyphen H command show 19GB size. So you can do this exercise right away or you can do after witnessing that error. In any case, for this section we will need 20 GB of size in Jenkins. So that will be all in this lecture you can continue with the Jenkins section now.
