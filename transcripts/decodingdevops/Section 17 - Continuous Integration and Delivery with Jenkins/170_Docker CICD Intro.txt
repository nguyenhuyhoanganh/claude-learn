# Docker CICD Intro

Hello and welcome. In this lecture we will talk about deploying our Docker images or should I say we are going to extend our continuous integration pipeline to continuous delivery. So we know continuous integration pipeline developer commits a go to GitHub. Jenkins is going to fetch the code. Run the test. Do code analysis. Upload the result to sonarqube. Check for the quality gate. If everything is good, it is going to publish our Docker image to Amazon ECR. Now we need to host this Docker image or the Docker container to a Docker solution like ECS. We will talk about ECS in the coming lecture, but for now you can understand ECS. As a Docker container hosting platform. So we package our image into Docker. Docker images and then we host our Dockerized application on ECS, Amazon ECS So when we issue the next stage, the next command. Amazon ECS is going to fetch the docker image that we published, the latest image and host it on ECS. So take a look once again at this architectural design of the pipeline. And now I'm going to talk about Docker hosting platforms. Okay. So we containerised or dockerized our application. That is, we created Docker images from that. Now where do we host it? So one local method or normal method is have a docker engine and just said Docker run, mentioned your image name and it runs your application on the docker engine. Now this is very much for testing and local development environment. You do not want to run containers directly on Docker engine because then you need to manage setting up Docker engine and the platform where it is running like easy to instance or virtual machines or physical server, you need to manage the underlying platform and just the Docker engine doesn't give you production related features like high availability, self-healing, and there are many other things. So just running container run Docker engine is good for testing and local development environment, but for production you need to go with Kubernetes. Now, we are not taking Kubernetes now. We have that in the leader section, quite in depth. Kubernetes we are taking and we will see later how we can deploy our Docker images to Kubernetes platform. And you can have a standalone Kubernetes platform. You can use EKS, which is from AWS, elastic Kubernetes service or AKS, Azure Kubernetes service or GKE, Google Kubernetes engine or OpenShift, which is from Red Hat. Really, There are many Kubernetes solutions out there and we are going to talk about all those things in later lectures. But for now we need to deploy our container. And we can use Amazon ECS for that, elastic container service. So Amazon ECS, as the documentation says, it run highly secure, reliable, scalable containers. So this is, I think, the most quick and easy way to run containers reliably or securely. And we can also scale it. Now, before we go to Kubernetes, we'll be using Amazon ECS, so it says, launche thousands of containers across the cloud using preferred CD and automation tools. And that's what we want to do. We are setting up a pipeline and we will deliver our application, dockerized application to ECS platform. All right. So in the next lecture, we will see the pipeline code. In order to do this, which is pretty simple, just one more stage we are going to add and then the prerequisites. That we need to fulfill in order to deploy Docker images to ECS platform. So wrap this up and join me in the next lecture.
