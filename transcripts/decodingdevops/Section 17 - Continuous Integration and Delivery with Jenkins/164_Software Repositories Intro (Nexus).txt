# Software Repositories Intro (Nexus)

Instructor: All right, so we can now build the software, test it, and analyze the code. The next step is to upload the artifact to Nexus Repository. So before we do that, we will understand what exactly is this Nexus Sonatype Repository and why do we really need it. So Nexus OSS Sonatype is a software repository. As the name says, it's a place to store and retrieve softwares. In this lecture, we will see how to store the artifact to the repository, but also, these repositories are used to download dependencies. Like when we run mvn install, it downloads dependencies from the internet. But you can point Maven to download dependency from your own repository, like Nexus Repository. But in this example, we will see how to store our artifact to the repository. The different kinds of repositories, like Maven to store Maven dependencies, apt to store Debian packages, or yum for RPMs. NuGet for .NET, Npm for JavaScript, Docker to store and retrieve Docker images. We'll see in next lectures how to do that for Docker as well. So these are some example. Based on what kind of project you work, you will have that kind of repositories that you will use to store or retrieve softwares. As already discussed, we will be using Nexus Sonatype Repository. It runs on Java. We have already set up the Nexus server, and we have done it through the script, which already have step two, install Java. We're going to use to store artifact, but as said previously, we can use it as a package manager for dependencies as well. And Nexus supports varieties of repositories, like apt, Docker, RubyGems, NuGet, apt, yum. So far, we did fetch the code, build the code, and do the analysis. Then now the next part is to upload our artifact to the Nexus Repository. We will be also using it to version, so we'll have multiple versions of our artifact. Once the artifact is stored in the repository, Upstream or an automation script can fetch that artifact and deploy it to the server. So first let's go and set up the Nexus Repository. Then we'll see how to do the integration. So I already have the Nexus server up and running from our previous lectures. So I'm going to take its public IP and access it on port 8081. We'll click on Sign In. Log in with the username and password that we set previously. We have two main sections here. This is to browse the repository, and this is for the settings. Let's click on this. Let's go to Repositories. And you can see existing repositories over here. We are going to create our own repository. So I say Create repository, and we will select maven hosted. So our use case is to store the artifact, so you have to use the hosted. If your use case is to download dependencies from the repository, then you set proxy. So this will act as a proxy repository, and group is to group both the repositories together. For this one, we have to select maven to host it. Let's click on that, and let's give repository a name. I'm going to say vprofile-repo, and I'll just go down and say Create repository. Now as far as the integration with Jenkins goes, it's pretty straightforward. We are going to use the pipeline as a code and store the artifact to this repository. But before that, we have to set the credentials. So come to Jenkins Dashboard, go to Manage Jenkins. Find Credentials. Okay, click on this one. Global credentials, and over here, you can add credentials. You see sonartoken credentials. That's the secret text that we used in previous lecture. We are going to add one more credential over here. That will be the Nexus login, Nexus username and password. While installing Nexus, we did set Nexus username and password. My user was Admin and password was admin123. And I'm telling you the password because anyways, I'm going to delete these instances once my work is done. But you never share username and password like that. Okay, anyways, so Kind will be Username with password and the username and then the password. I'll give the ID as nexuslogin all in small and in the description also, I'll give the same, and I will say Create. Do remember the name of this credential, nexuslogin. Otherwise, you can come here and check. We're going to put this in the pipeline code. So in the next lecture, we'll see how to write the pipeline code to upload our artifact to Nexus Repository. So I'll see you in the next one.
