# Introduction

Welcome to CI/CD. Continuous integration and continuous delivery. CI/CD is an automation process that requires many tools to deliver your software faster and safer. So it's an automation process, but first, you need to understand what does it automate or what problem it solves. Because being a DevOps engineer, most of your time will be going in CI/CD. That's a high chance. So see, developers write code and they merge all this code into a centralized repository like Git. So there will be multiple developers each merging their code on daily basis multiple times in a day. And this happens for weeks and months. So code is getting merged, but after, sometimes if we build it to test, there will be many conflicts, bugs, and issues that needs to be solved. This is happening because the code is getting merged. So a manual build and release cycle is this. Forget about manual and automation for now. This is the build and release cycle. Developers get requirements, they write code. The code is manually built, manually tested. If there are any issues, it will be fixed. The code will be packaged, deployed to staging or test environment and tested for its features, its issues, and bugs. If everything is good, it is released. Now imagine this process multiple times in a day by multiple developers. So developers, apart from just writing their code, they also need to do build test, unit test, packaging, deploying to servers. I know too many words to chew in for now, but don't worry. We'll learn all these steps manually and automatically, both. For now, just understand there are too many steps that requires to build and release a software. And if this is manual, it will create a lot of issues and bottleneck plus a manual process means there will be manual handoffs. There'll be multiple team working in this entire build and release cycle. So I'm done writing the code now. Can you test it? I'm done testing the code. This is the issue. Can you fix it? And like this, this process goes on and on. So this repeated manual process will slow the team. It will create bottlenecks, and if there are errors and issues, it will take time to resolve. And this is the exact problem CI/CD solves. So continuous integration and continuous delivery. Now listen carefully. Developers write the code and merge into shared repository like git. Now, as soon as there is a commit by the developer in the repository, there should be automatic system that should detect that, build the code and test it, and catch the error right then and there. So no more merging the code for some time. And then after that, testing it, as soon as there is a code change, system will automatically build and test it. This is called a continuous integration. So this is still at the code level. Now continuous delivery goes to the next step. CD involves packaging this tested code and deploying it to servers or the systems where it'll be running and then testing it. Again, this could be with or without manual approval that depends on the project and the requirement. So CI makes sure the code is ready, CD makes sure the software or the application is ready to be released. So the goal is to quickly release the software with less and less errors. Flow is simple, plan, code, build, test, deploy, release. And this is a cycle. So you need automation tools or multiple tools to do this entire CI/CD process. As the developer comes the code, there should be a tool like Jenkins or GitHub actions, or GitLab or Argo CD, any such kind of CI/CD automation tool that checks the code. If there are any changes, it will build it, test it, execute all the different kinds of tests like unit test, integration test then packages the code and delivers to the target environment where it will be tested. Furthermore, if everything is good, it will be ready to release. And for any kind of failure, a notification will be sent to the developer or the required team. If there is any failure, the process starts again. Now, in this section of the course, we are going to use Jenkins as the CI/CD tool, in later section, we also have a GitHub actions and GitLab and Argo CD. But there's a reason why I'm taking Jenkins first. So Jenkins forces you to understand CI/CD basics in the pipelines, build triggers, agents, et cetera, et cetera. There are many things It's highly configurable, so you manually see different, different things, different settings. You will see how CI/CD actually works under the hood. And once you understand Jenkins, switching to any other CI/CD tool becomes much easier. Jenkins is widely used. It's one of the most used CI/CD tool. So Jenkins, you can understand that we are learning the fundamentals and also the complete CI/CD process. Other modern CI/CD tools like GitHub actions, GitLab, Argo CD, they're easier, modern, and they're more automated. So Jenkins is a free open source automation server. It's highly extensible. There's so many plugins, and you can integrate any number of tools with Jenkins like Git, Docker, Kubernetes, Maven, and it runs on Windows, Linux, MacOS,, any kind of operating system. And as I say, it's always better to do it rather than just read it. So let's get into it. On the next lecture, we'll start setting up Jenkins and start learning it. So join me in the next lecture.
