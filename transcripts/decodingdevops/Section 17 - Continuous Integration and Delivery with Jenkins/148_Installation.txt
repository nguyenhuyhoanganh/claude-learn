# Installation

So we discussed many things in the previous lecture. Now to summarize all this, if you hear this code, build, test, deploy, release, or automated build and release cycle, that means CI/CD and there are many CI/CD automation tools available in the market. In this section, we are learning Jenkins, which is a legacy CI/CD automation tool. It is built on Java, you can customize it as per your need. Then there are modern automation CI/CD automation tools like GitHub actions, GitLab, Argo CD, and few more, public cloud providers like AWS, GCP, and Azure has their own CI/CD services. AWS has code build, code deploy, code pipeline. GCP has cloud build, cloud deploy. Azure Pipeline and Azure DevOps from Azure. So you see, I summarized it very quickly, made your life easy. In this lecture, we are going to install a Jenkins on an EC2 instance. And as I told you, Jenkins is built on Java so we need to install Java. On this EC2 instance, we will be using Ubuntu operating system, but Jenkins can be set on many other platforms. So let's get into it. jenkins.io. This is the official Jenkins documentation. So if you click here on documentation, you click on installing Jenkins. You should see here different platform where you can run it. Docker, Kubernetes, that will see later, Linux. When MacOS, Windows, you go to Linux. You should find here Debian/Ubuntu. Click on that. So first, you need to install a Java. So we will be using openJDK 21 and then you will be having commands here to install Jenkins. First, download the Jenkins repository key, then set the Jenkins repository in your Ubuntu operating system, run apt update and apt install Jenkins. So first, let's copy this command to install JDK. Copy this. Open any text editor and instead of GRE over here at the end, you need to change it to JDK. So JRE's Java runtime environment used to run Java software but we will be also developing softwares on this. So we need JDK, we will be building V profile application on Jenkins and to build a Java software you need JDK. So let's say hyphen Y here. Now copy the commands to install Jenkins. Over here, click on copy. At the end over here, just do -y so it doesn't ask us any question. We will be running all this command through EC2 user data. So keep this ready and then come to AWS console. So you can run this on any region, I'm in North Virginia region. Go to EC2 service and say launch instance, launch without a walkthrough, give a name tag. I'm just going to give it as Jenkins server and select Ubuntu AMI, Ubuntu server 24. Scroll down, select the instance type. Now this one will not be free tier. We will be running many things on Jenkins so it's better you select T2 medium and whenever you are not using it, just make sure you power off. Key pair. Let's create a key, then phi. Create key pair. Oh wait, I had to give the name. So I'm going to give it as cicd-key and say create key pair. We'll use the same key for our other tools also. Come to network settings, click on edit and go to security group name. Let's give a name to this security group, jenkins-sg. And same I'm giving in the description. Make sure you give the name to the security group because we will be making changes to the security group later. So I've given jenkins-sg, scroll down and select SSH, from my IP, we need to log into Jenkins, SSH. Now Jenkins is a web-based tool. You can access it from the browser and you need to access it on port 8080. That's where its service is running. Like Tomcat listens on port 8080. Jenkins also listens on port 8080 and we are going to say My IP. Now make sure you keep refreshing this My IP because your public IP will change if you're not able to SSH, if you're not able to access from the browser, make sure you update the security group rules and make sure it's always the right public IP of your network. Scroll down, click on advanced details. Scroll till the end where you see user data and then you can copy your script and paste it over here. Wait a second, you need to give hash exclamation slash bin slash bash and then you paste your commands. If you don't give this, the script will not run. So make sure you give this hash exclamation bin bash. Make sure there is -y in JDK and -y, very installing Jenkins. And for some reason if your Jenkins service is not up and running just SSH to Jenkins and execute all these commands one by one manually. This command is to download the key. This command is to set the repository. This is apt update and apt install Jenkins. Let's launch the instance. so it will take some time to install. So you can check after five minutes or so. Okay, after a few minutes, I'm going to SSH to it. So let's copy its public IP, open Git Bash or terminal based on what you're using. SSH-i, downloads. My key name is CI/CD. Username is Ubuntu because we're using open two operating system and paste the IP address. Okay, I'm in. Let me expand this. I'm going to clear the screen, switch to root user. Now I'm going to run the command Java hyphen version. If you don't see this, if you see some error, that means your commands did not execute properly. Make sure you execute all the commands one by one manually. Also, you should see the Jenkins service running systemctl status jenkins, active running queue to quit from this and the Jenkins home directory is here where Lib Jenkins. So all your Jenkins data is in this folder. Let's say if you want to take a backup and move to Jenkins somewhere else, you can just shut down Jenkins service, archive this folder and move it to the other system. In the other system, you need to again install Jenkins and copy all this data into this folder. Restart Jenkins and you have successfully moved Jenkins to other system. We'll come back to this folder later. One more thing I want to show you is there is a user called Jenkins. This user is going to own this folder via Lib Jenkins. See this Jenkins user, Jenkins group owns this folder. That should be the case all the time. Now let's see it from the browser where we will be accessing it most of the time. So copy its public IP and in the browser say IP;8080. And you should see this page where you need to unlock Jenkins and the initial password is in this file. Take a look, wireless Jenkins, secrets and this is the password. So copy this part and do CAT, let me clear the screen, cat to this file and you should see the initial password. This is the password to unlock Jenkins. So copy it and paste it here. Say continue. The first page you get is to install suggested plugin or select plugins to install. So what are plugins? So see, Jenkins comes with many features and it integrates with many, many tools. And how does it do it? By using plugins. So plugins provide features in Jenkins. Plugins means features. So usually, people click on install suggested plugins and then later, install plugins whenever they need manually. For now, you just click on select plugins to install. So you can see the suggested plugins and these are not all the plugins, these are suggested plugins which you can select or unselect. You see different sections over here like build tools, like building ant project, Gradle, MSBuild, NodeJS. So I'm going to uncheck ant and click on NodeJS. So take a few minutes and go through this list. You don't need to understand anything, just take a look. If you see something that you know, like for example Git or in notification, if you see email extension, these are the plugins. This provides this features which you can use later. We have a separate section on plugins. You are going to go to plugin section many time in this entire section because we will be doing too many things with Jenkins. So we need too many features and I have said too many, too many times and I'm going to stop now. Okay, just click on install. So it's installing these plugins that are checked and once it is done, you will see this page where you can set the username and password, that will be the admin user. So I'm going to name it as admin. I'm going to set a very simple password so I do not forget it for now. So whatever username and password you set, make sure you remember or write it down for now because we will be using Jenkins for a very long time in this course. Later, you can set complicated or complex password. I have just set password as admin one, two, three. I know, I know it's not safe, but this is just for my lab for now. Full name, I'm going to give it as admin and some fake email address like admin at ops.com and say save and continue. Okay, here, you get the Jenkins URL. If you see this IP is the public IP of the EC2 instance and this will change, right? When we power off our EC2 instance and power on, we get a new public IP. So you can anyways access the Jenkins from the new public IP. That's not a problem. But this setting will be saved in its configuration and this later creates some issues. That's one known bug where Jenkins becomes very slow. So what we are going to do it is that we are going to remove this public IP and give some fake URL like I have given jenkins.hkh.zyz is just a trick for that bug. Nothing fancy that you should, you know, learn or remember. Just give some fake name and just say save and finish. Jenkins is ready. Start using Jenkins and welcome to Jenkins. So here, you see option to create a job. Job is like in like, Bash script, we have a script which executes our steps. Here, we have job that executes our step build, test, deploy, et cetera, et cetera. Or pull the code, send notification. All those settings will be in the job. Jenkins settings are in Manage Jenkins. You see this gear symbol, you click on this. From here, you can manage Jenkins. So you have sections like a system for global settings, tools, which tool to configure like JDK, nodeJS, Git, et cetera, plugins. The features you can add, remove, disable, enable many other settings like security credentials. So just take a look, click on the settings, see what they provide. We'll be coming to this section a lot. Let's click on system here. You see here, you have the home directory, var/lib/jenkins. If you scroll down, you see here the Jenkins URL that we have configured. If you made mistakes over there or the previous page, you can fix it over here and then save the settings. Whenever you want to come to the main page of Jenkins, just click on this logo over here, this Jenkins name, click on it and you come over here to the main page. Over here, you have the admin account settings and themes that you can change. Let's say dark. Oh, I like this, but I'm gonna keep it light. Oh, you know what? Dark, dark is good. Okay, like this, just play around with these settings. Don't make real changes. We'll make real changes later. From the next lecture, we'll start seeing what is job, how to create different settings of Jenkins. So wrap this up and join me in the next lecture.
