# Steps for Continuous Integration Pipeline

All right. So now let's see the steps that we will execute to create the entire continuous integration pipeline. So first, we will set up Jenkins. Now, I know we already have Jenkins, so we can use the same. Or you can set up a new Jenkins server. We will set up Nexus server, will set up Sonarqube server, all these three set up will be through user data script, bash scripts. So we launch ec2 instance and set up the servers by using the scripts. Next, we will check on the security group. Even while we're doing the setup of this, we will check the security group and I'll tell you how you can allow connection in between them. Next we will install all the necessary plugins in Jenkins like Nexus plugin, SOnar Qube plug in, git plug in, Maven plug in and all those plug ins we will install one by one. Then we will integrate Nexus with Jenkins and SonarQube with Jenkins. Now Nexus integration is straightforward. We just need to save the credentials. But Soner qube, we have a few steps that we need to execute. Once this entire setup is done, we will write the pipeline script. We write the pipeline script execut it, which will use all these resources. And finally we will set notification. So if anything goes wrong in the pipeline, any job fails. We should get notifications automatically. Now, all the steps may not make much sense now, but this is the roadmap. Whenever you feel that you are lost while setting the pipeline, come and check this video. You will know where you are. All right, so now let's get started. Let's start setting up all these servers first. So join me in the next lecture.
