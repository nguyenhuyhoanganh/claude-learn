# Code Analysis Demonstration

Okay, so let's start writing our pipeline script to do code analysis. But before we write a complete code analysis pipeline, I want to show you a few things. For that purpose, open the pipeline script that we used earlier. I have opened it in Sublime Text editor. You can use editor of your own choice. First thing, just move this unit test. This is opening, this is closing. Copy this. Cut it, just remove it. And this starts here, ends here. So move the unit test stage after the build and one more time copy it. In this stage we are going to do checkstyle code analysis. So remove this name "Unit Test" and write here "Checkstyle Analysis". And over here, now checkstyle we can run through Maven by giving this option checkstyle:checkstyle. Now when you run this, Maven is going to download the checkstyle tools or its binaries and do code analysis with that. So Maven will be doing checkstyle code analysis. Let's test this one first. So copy all this content, go to Jenkins dashboard or create a new job, new item and just say "Checkstyle." Click on pipeline and say, Okay, scroll down and just paste your pipeline script over here. Save it and see, build now. Let's click on stages and let's see here. Show stage names, show stage duration, wait for it to complete. Okay, let's click on this checkstyle. It says success, it's completed. It says there are 641 errors reported by checkstyle 9.3. You see there are some binaries that it is downloading. So these are the dependencies for Maven to execute checkstyle code analysis. Now what I want to show you here is, let's go back to this checkstyle, go to this first job and go to workspaces. Click on this path. Here you should see the target folder. In the target folder, you should see here, checkstyle-result.XML. This file. Click on view. And that's a beautiful view, isn't it? I mean, you can read everything. You can understand everything, right? Just go through it. Only if you are a superhuman. Which, I guess, you're not. So my point here is you need some kind of dashboard, which can read this and show you the analysis that how many errors are there, what are the problems, how we can fix it. And this is where SonarCube server dashboard comes in handy. So checkstyle is just one code analysis tool. There are many code analysis tool like that, you can do scanning of your code and push all this result to SonarCube server and SonarCube server will do the analysis, show you the details more in a human readable format. So how do we push all this result to SonarCube server? Let's Google it. So one thing is for sure that we want to do it through pipeline script. So I've just searched sonar scanner pipeline script. There are sonar documentation. I will go there in a moment. But let's see what the AI says, Gemini. So if you take a look here, it says stage SonarCube analysis, the steps are, sonar withSonarCube environment. This is where your SonarCube plugin is coming in handy. You mentioned the SonarCube server. Now we added the SonarCube server information in Jenkins, right? So this is the same name, you have to keep over here. And script. So you mentioned the tool, again, the tool that we added in Jenkins, and then you mentioned the commands. So sh means the shell commands. So basically the sonar scanner home, which is this tool, /bin/sonar-scanner. And with that you can pass many options that I'll show you in a moment. And then you have here the quality gate stage where it'll check if the code analysis passed or not. That will see in the next lecture. Also, let's go to the documentation. So sonar scanner for Jenkins. Let's scroll down and let's see. Okay, same thing, right? So stage, the name of the stage and the tool path withSonarCube environment. Mentioned the SonarCube server that we have configured and the sonar scanner tool. Now what this will do is this is going to scan the source code withSonarCube scanner CLI. Along with that, you can post many other things over here, which I'll show you in a moment. And all this result, it's going to post it to the SonarCube server. So I am going to copy this and put it in my pipeline script and then work on it. Though frankly, you can use any AI tool, you can use GitHub copilot, ChatGPT or anything else you wish, and you can generate the entire pipeline. But that is for later. First, you need to know how to do it yourself manually, one by one. Once you are good with that, then you can definitely use copilot, any other AI assistant. Okay, so let's go over here. Once this checkstyle completes, let's paste it over here, and let me make sure it is properly indented so it can look good. I'm going to remove this comment. This looks too long. First, I'm going to change the name of the stage. I'm going to just say "Sonar Code Analysis." Now, scannerHome is the variable, that is assigned over here. We can change it if you want, but let's keep it. Now definitely means this is going to be a local variable, not with a global scope. So this is for this stage only. That's what this deaf means. Local variable, a local variable within the scope of this curly brace. And the tool name I have to give. And this is the same tool name that we added in the Jenkins. So let's go to Jenkins, manage Jenkins tools, scroll down, find SonarCube scanner installation there, Sonar 8.0. Make sure it's exactly the name that you have given over there. Let's remove this and add our tool name. Okay, next thing is with SonarCube environment. So this is the sonar server name that we added in Jenkins. So manage Jenkins system, scroll down, this is the name I've given sonar server. So make sure it's the same name over here in the single quote. Okay, now the command. So this command will get executed along with that. We need to pass few other parameters, like what will be the project name on SonarCube server. Also the checkstyle result that we have just seen the XML file, which you are able to read properly, right? Yeah, that file. We also need to post it to SonarCube server. The unit test result that was generated by mvn test, JaCoCo, all the test cases that we have created in the workspace, all those we need to put it to SonarCube dashboard. So along with this command, we need to pass few options that I'll show you in a moment. But first, this is going to be a multi-line command. This is just in one line. If you have to give multi-line, then you need to close it with three single code, open it with three single codes so you can give multi-line commands. Okay, so I'm in ChatGPT. Now you can get all this detail from the sonar documentation also. I just need to know it. Once you know it from anywhere, from ChatGPT documentation, anywhere, then you can use the same information later. So I just ask ChatGPT that along with this pipeline script that I'm writing, I want to upload checkstyle result, I want to give project name, I want to upload JaCoCo result, and all the other results to sonar server. And this is what it has given. This is a multi-line script. This is in triple codes with double codes. I have given single code, doesn't matter, it works. So this is the command and then you are passing the option. So it says -Dsonar.projectKey equal to the project name, then the project name itself. This will be the id, and this will be just the description name. Version, source code, where the source code is located. So ours is also SRC. In our gate repository we have SRC folder where we have the Java code and the Java binaries, the checkstyle reports path. So I check that. And all the other details. So I am going to copy this and just paste it over here. Now I'm going to give in this lecture resource this entire pipeline script. You can copy paste also, but I want you to generate it like this, searching one thing at a time, so you get good hands on practice. Okay, so I have made a few changes. ProjectKey, I have given vprofile, projectTeam, I have given as vprofile, projectVersion I have kept same, 1.0. And the source code folder, SRC/, that is in our source code. The pipeline will fetch the source code, right? And it has a folder, SRC, it has all the Java code. So this part is mentioned over there. For scanning Java binary target/classes. So let's go to a checkstyle pipeline and go to the workspaces of the first build job. You go to target, you should see here a folder called Classes. And same way you have the reports file. Surefire report path, JaCoCo, XML file path. You don't need to write all this manually. This is this project specific vprofile. You work in a different project, you'll have different path, or might be same path. But anyways, get all this from the pipeline script, from this lecture resource and make sure it's the same one. Checkstyle you have just seen in the beginning of this lecture. So you don't need to by heart this, all those things, this depends on the project. What you need to understand it when you scan the code with sonar scanner or any other code analysis tool, you need to pass some parameter to take all this result for the analysis. Okay, now we have to fix a few things here. One thing is that we need to add this plugin inside steps, like we did it in every stage. So we need to add steps, open curly brace, and then do not put closing curly brace over here. Come down over here and add here one more curly brace. Let's do a little indentation. Two step, two step. And this one also will go two step and this one, two step. Okay, if for now this is confusing, then download the lecture resource, that you have proper pipeline script. Just be careful with opening and closing, like steps is opening over here and closing over here. And this plugin information is opening over here, closing over here. Then this curly breeze is for the stage. Now that's one thing. The other thing is a def, as I told you, is for a local variables, but then you need to put it inside a script block. This is a groovy specific thing. This def is groovy specific thing. Now if you want to put groovy in Jenkins pipeline script, you need to put that into script section, like you have steps, you need to put all the steps, everything in the script. Or the best practices, we are going to remove this def and we are going to put this into environment section. We see environment, we open curly brace here and then we close it over there. So in the environment you are mentioning the environment variables. Now this again for the this stage only. This is local for this stage. You can mention also environment variable at the global level over here, before the stages. That we'll see later. But this is a good working piece of code over here. Copy this. Go to Jenkins. Let's create a new job. Go to new item. Give this job name Code Analysis. pipeline, select pipeline. Say okay. Scroll down and paste your script here. Once you're good, save it and build it. Okay, let's look at the stages. So it's going to take some time. I'm going to pause the recording for now. Okay, this failed. I wanted you to see this. So I checked the sonar code analysis that failed, and here I could see 502 bad gateway error for this URL, which is basically SonarCube server. So I checked the public IP of SonarCube and it was down, it was showing the same error from the browser, 502 bad gateway. I did not do anything. I was just looking, going through the errors and then it automatically got refreshed and SonarCube service was coming then from the browser. So there is a glitch, it got restarted for some reason, but now it's a running. So let's run this job once again and let's see what happens. I will just rerun this sonar core analysis job only. Let's see. It's executing the entire pipeline once again. Let's let it complete. Okay, good. The core analysis got completed successfully. Okay, take a look at the output. It's huge. Here at the end, you can see analysis successful, you can find the result, but this is the private IP we cannot access from the browser. We need to definitely go with the public IP of SonarCube. So go to SonarCube dashboard, go to projects, and you should see a project named vprofile. That's what we have given the option. Click on this, right? So it says there's three security issues, 5,200 liability issues, 25 security hotspots. So overall it says pass, because it says the quality gate does not block any of this. And what does that mean? That will see in the next lecture. So go through these errors and issues and then join me in the next one. We are going to create the quality gate there.
