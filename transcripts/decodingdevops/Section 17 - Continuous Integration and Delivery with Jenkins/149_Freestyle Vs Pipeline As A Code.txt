# Freestyle Vs Pipeline As A Code

All right. Before we dive into Jenkins, let me talk about jobs in Jenkins. I will compare two types of jobs in Jenkins freestyle. Vs is this pipeline as a code What is what? And which one should you use? Now, jobs basically means workload, which Jenkins runs for you. Like in bash script, we write a script to accomplish or automate a task like that. In Jenkins, we create jobs. Let's compare these two jobs, freestyle versus pipeline as a code. So freestyle job are all graphical jobs. So you click on create a job and you fill all the information. It's basically a form filling. You run the job and you see it's output. Now that's fine and good, but the problem with this is, you will have many types, many jobs, many jobs that you need to connect together to create a pipeline. Fetch the code. Build the code on another job, then run test another job, to deploy another job, and then you connect all of them together manually, one by one. Now, it's not a problem, but it's a problem when you want to replicate the same thing if you want to do the same thing in other project. A similar thing in another Jenkins. Then you need to do all those click, click hundreds of times to create that pipeline. So these freestyle jobs are just for learning and understanding purpose in few lectures. Further, you will see we have a freestyle job, but that is just so you can explore Jenkins, understand the concepts. It is not recommended in real time now. Yes, I am stressing on now because in today's time. We need everything as a code in DevOps. Even the pipeline should be created through a code. And Jenkins does allow that. And this is called this pipeline as a code. So you create a similar pipeline or same pipeline, which will be written in Groovy Language. So it's similar to creating a job. You create a job. But in that job you mention this entire script and that entire script is your pipeline. That's all. And you can version control it because it will be all text. And it is recommended now because we need everything as a code, so pipeline also as a code. But before we get into Pipeline as a code, we need to explore Jenkins that we will do through free style jobs first. So join me in the next video and see for yourself.
