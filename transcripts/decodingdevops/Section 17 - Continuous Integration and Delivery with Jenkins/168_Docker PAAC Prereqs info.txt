# Docker PAAC Prereqs info

-: Welcome. In this lecture we are going to see the code that we'll use to build the docker image and upload it to Amazon ECR. Let's go through our previous stages. First we fetch the source code. In this case, we are using a branch called docker. I'll show you content of this branch in some time. Then we build the artifact Unit Test, check style, sonar code analysis, and upload the result to sonar dashboard and then the quality gates. Now the next stage was to upload the artifact to an access repository, but now we are going to build the docker image and upload it to Amazon ECR. So docker image will be the artifact. So Jenkins does not have any inbuilt feature to build the docker image and upload to docker registry. So we are going to use a plugin called docker. Now let's understand these two stages that we have added in our pipeline. So the first is the building image. In this we are using a plugin called a docker. We'll install this plugin. This plugin has a function called build and you can understand that it is a function by looking at these parenthesis, you'll understand about functions and how to write your own functions in Python section, which is the next section after this. But I need to explain this to you. In function we can pass arguments. So the build function, you can pass two argument, one will be the image name, separated by comma. There is another argument that is the path of the docker file. Now this docker file will be in our source code. So let's take a look at the source code. So we're going to go to our source code URL and switch to branch docker and check the content. Okay, so we are in the GitHub repository and we are going to switch to the branch docker. In this we have a folder called docker-files. Click on that. Inside that you have a folder called app. Inside that we have a folder called multistage. And here we have the docker file. Now, I don't want you to stress too much on docker file. We need to learn it in much detail in the docker section. What this docker file does is basically it is used to build the image, a docker image, and the instructions are over here, how to build the docker image. So this is a two stage docker file. First stage, we are going to fetch the source code switch to the branch docker, and then run mvn install, which will generate the artifact. The second stage is going to take a Tomcat image from docker hub, remove the default application and copy our artifact that was built in the previous stage here. With this instruction, it's going to copy the artifact in the Tomcat image and will the image build is completed. We'll have a docker image with Tomcat service and our application hosted inside that. So coming back to our code. So docker build command is going to run the docker build command. I'm talking about the docker CLI. In the Jenkins it's going to run the docker build command. Use this name as the image name, image name with the build tag. And it's going to build the docker image by using instructions present in this docker file, which we have just seen. I understand this can be overwhelming now, but once you are through with the docker section, this will be pretty easy for you. You will be writing your own docker file, so please do not worry now. Just focus on the stage. So basically this instruction, this pipeline stage is going to build a docker image for us. And now we need to upload this docker image. So here comes the next stage uploading image. So again, docker plugin. And this time the function is with registry. Here the function was build. With registry function we passed two arguments. One is the registry path, the location of registry, which will be a URL. Now this is a variable. We'll see the variables in the moment. And then the registry credential. So it'll log into the docker registry, which in our case is Amazon, ECR. And push the docker image with the tag. Tag is used for versioning in docker. So we are putting two tags to the same image. One will be the build number, the second one is the latest. Latest is the default tag. So while fetching a docker image, if you don't mention any tag by default is going to use the tag as latest. Okay, now let's come to the variables that we have used. We have used two variables over here, imageName, vprofileRegistry and registryCredentials. Let's take a look at these variables that we have defined over here. The registryCredentials. Now its value will be the credentials that we are going to store in Jenkins. So in Jenkins, like we store credentials like sonar token, Nexus login. Like this, we are going to store AWS credentials, AWS access key and secret key. This is the format for the docker plugin. You mentioned the registry name, you mentioned the region and the credential name. So we are going to go to Jenkins store the access key and secret key in the Jenkins, which docker plugin will use to log into AWS account. The next is image name, image name format is like this. It'll be the account name slash the image name. And where is this coming from? When we create Amazon ECR registry will get this URL and our docker image name should be like this. The account name slash the image name colon, the tag that we are using anyways when we push the docker image. The next one is a vPro profile registry. The URL. If you see this, and this will be same, this is the URL and this is the account name. Okay, now let's prepare a list. What we need to do in order to execute this plugin. So I'm keeping my sticky notes over here and I'm going to jot down the points or the prerequisite that we need to do on Jenkins and AWS in order to execute this pipeline, starting with the credentials. So we need to store the credentials. So for that, we need to go to IAM and create access key and secret key. So we need to go to AWS, create an IAM user with the access key. That's one thing that we need to do. The next, we need to create an ECR registry. So ECR registry is a docker registry from AWS. Here you can store the docker images so that we need to create, so we can give that information over here so we get all these details. Next, let's check at the stages. So plugin, we need to install a plugin called docker. Actually, there are two plugins we need to install docker and docker pipeline. So on Jenkins, we need to do these things. We need to store AWS access keys that will be used called as Jenkins credentials. And we need to install docker engine in the Jenkins. This docker is a plugin, but we also need the docker engine. docker plugin is going to execute docker commands. In order to execute docker commands, you need docker engine and all these are getting executed on Jenkins. So we need to install docker engine in Jenkins. Now also there is also one more plugin we need to install. That is ECR. So these are the things that we need to do. So let's get into the next lecture and execute all the steps and test our pipeline. You can download this pipeline code from the lecture resource. Go through it, take a look, make sure you understand all the steps except the docker file. Don't worry about the docker file. You're going to cover it in detail in docker section. Alright, so these are the steps that we will execute now. First we need to install docker engine in Jenkins, so we can run docker build command from the Jenkins pipeline. And we also need to add this Jenkins user into docker group because Jenkins user will execute docker build command, and Jenkins users should have privilege to execute docker command. And then we will reboot it. We'll also install AWS cli, which is not required for this project, but in the next, when we do continuous delivery, when we deliver the artifact that time we'll need it. So just install it now itself. We will create an IAM user in AWS and we will get access key and secret key. We'll create ECR repository on AWS, again, where we'll store the docker images. And these are the plugins that we need to install ECR, docker pipeline, aws sdk to store the credentials in Jenkins. These three pipelines, sorry, these three plugins we need to install in Jenkins. Then we will store the AWS credentials in Jenkins access key and secret key. And then we will run the pipeline as you have already seen. Now, before you move into the next lecture, I want you to watch this video once again. Understand the flow of the pipeline script and also the flow, the steps that we are to execute. Download the pipeline script from resource section. Take a look, make sure you understand everything. I also recommend you do some research, Google, look at the pipeline documentation, docker pipeline documentation before you actually do it. Alright, then wrap this up and join me in the next lecture.
