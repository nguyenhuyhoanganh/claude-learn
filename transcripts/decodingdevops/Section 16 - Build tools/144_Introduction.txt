# Introduction

Instructor: Hello, and welcome. In this session, we'll be doing Maven, the build tool. So let's see what we are going to do in this. We are going to understand what is a build process. We'll see what are the build tools. We'll understand what is Maven. We'll see different Maven phases. We'll see Maven installation on Windows and Linux both and also with different versions. We'll go through the pom.xml file, which is the build file for Maven, and we'll see some Maven commands. Okay, first let's understand what really is a build process. Maven is a build tool, so what really is build? Well, developer write the code. We call it as simply source code. They're creating a product, a web application, a mobile app, anything that is a source code. They keep committing the changes to the source code, and while they commit the change, they would like to test it. So they will compile the code, and this is especially for the compiled languages, like Java, .NET, C, C#, like that. So they'll be compiled. They'll compile the code, then they'll test it. If it works as per their wish, then they will package it. So compile process is going to generate some files, the output files that they will package it into an archive. So a source is generated or changed programming language, like Java or .NET. Then the compile process will be there, a compile step, where if it's a Java code, a compiler like javac will be used. When it is compiled, the javac compiles it. It's going to generate the class files. There'll be unit test or integration test. So these unit tests are the tests written by the developers to test their own code. This is not the software testing. So a unit of code will be tested by a unit test. If that works fine, then the class files will be packaged. Now, based on what the target is, like, if it's a software for a Windows machine, that there will be EXE or MSI format of packaging or archiving. So EXE, MSI, these are the archive methods or extension. jar/war is for Java. So if a Java web application mostly will be packaged in WAR format or other languages, like Python, Ruby, the interpreted language, it'll be simply archived into Zip format or a tar file. Then also, if there is the step done by the developer of health check, that will be performed by the developers. So health check or code analysis, this process is going to find any upcoming bugs or security vulnerabilities in the code. So in short, this is the entire build process. Now there's a lot of steps. So developers, along with writing their code, regularly committing their code, they also need to regularly do this build process, which is too much work if they have to do it very regularly. So to automate these process, this build process, there are build tools. Build tools simply automate the build process, tools like Maven, which is for Java. It's based on XML format. So you mention in the XML file what you want to do, what are the dependencies, what is the versioning mechanism, all those things, and you run some Maven commands, and Maven tool is going to execute your build phases automatically. You don't need to run them manually, like Ant, which is, again, for Java. But here there is more scripting involved in Ant, and in general, Maven supersedes Ant. MSBuild, which is the Microsoft Build Engine, if you are writing code for the Microsoft product you'll be using build tool like MSBuild. And there's Gradle, which is in Groovy format and NANT, again for Windows. Or Make, which builds the executable program like our RPM WN packages or any program that's executed on the operating system, there's a Make build tool that can build from code to the program. And there are many, many build tools based on your target. Based on the programming language, the right build tool will be decided. So the developers or the software architects will already decide what is the build tool, and they'll set the build process already. We are going to do Maven because we are using a Java source code in this course, which will be built by the Maven tool. So Maven is really a build tool for Java. In Maven, you have multiple phases. You have first validate, then compile, then test. then package. integration, test, verify, install, and deploy. If you trigger any phase or if you start any phase, it will do all the previous phases. Like if you say, "Maven package," it is going to do validate, compile, test, and then package. Well, let's understand this phases. First, the validate phase, so if you're on the command MVN validate, which will just check if the project directory structure is correct and all necessary information is available. If you say compile, it is going to use the compiler, and compile the source code. test is going to execute your unit testing framework. So this is a test on the source code so you don't need to deploy the source code anywhere, or you don't even need to package it. So developer will use some unit testing framework, and your Maven test process is going to invoke that and run all the unit test cases and return the result. Package phase is going to take the compiled source code and package it into right distributable format. Like, if the target is a web application, you'd be packaging it into JAR/WAR format. Now this packaging methods will be already defined in the pom.xml file, and we'll be seeing that. verify is going to run checks on the integration test results to check the quality criteria has been met or not. Install phase is going to install or should I say, download the dependencies and keep it in your local repository. So developer will mention the dependencies of their source code, the dependent library, and packages. They'll mention in pom.xml file, and when you executing any phase if you're saying Maven test, it's going to download the dependencies from the internet from Maven repositories and then use them. Install phase will download all the dependencies and keep it in the local repository so it doesn't need to be downloaded again and again. Then the deploy phase, which will take your package that you have created, and it's going to push it to some remote repository so it can be shared by other developers or the team members or even the DevOps engineers who want to deploy that package to some servers. Let's again check all this and few other phases from this documentation. So you have the build lifecycle in Maven. If you want to get this link directly from Google, just Google for Build Lifecycle Maven, and you should get this link. So different phases and that information, the command line, how you're going to use it, MVN verify to execute the verify phase, MVN clean deploy, which will first execute clean, and then it'll execute deploy. Okay, you have documentation on packaging, plugins, how to mention plugin in pom.xml file. We will go through that file in a moment, some lifecycle references and why they are used, their description, pre-clean, clean, post-clean validate, initialize, built-in lifecycle bindings, and you can see all these different options and why they are used. But mostly, this will be given to you by the developers which phase you need to run and what to expect. So take a look at the documentation, but you really don't need to spend too much time on this. This is majorly developers work. As a DevOps, we should be aware about a few things on a build tool like Maven and that's what we will see in the next lecture. We'll do some hands-on with Maven.
