# Maven, NodeJS & AI

Okay. So I'm going to terminate this instance. We don't need it. That was just to show you how you can do it on a bare metal or no, or on an operating system from scratch. Once you understand this, you can do it really anywhere. Like for example, AWS Cloud Shell. So if you click on this AWS Cloud Shell over here, it's going to open a shell for you. That will be of Amazon Linux 2023. You can expand it. You can open it in a new tab. I'm going to click on this button. It opens up in a new tab. Okay. Preparing the terminal okay. It's ready. So run the command cat etc.. OS hyphen release. Let me increase the font here. Large confirm okay. You see that that's Amazon Linux 2023. Now this is based on RPM. So you can use yum or DNF command. So we'll do the same work what we did in the previous lecture. So if you just search well, you need to use sudo command sudo DNF search for Java, not JDK. Here the package name starts with Java. I'm going to filter it and say grep 21. So I don't think it's there. Let me check with ChatGPT how to install JDK 21 in Amazon Linux or what. It's there. Yeah. So the package name is Java hyphen 21 Amazon Corretto. When you see Corretto, keep in mind this is from AWS. So let's install this sudo dnf install and the package name hyphen y. Okay, that was quick. Let's also install like this Maven Suming. Maven is there in the repository of Amazon Linux. It is there DNF install maven hyphen y. Okay, let's run the command Mvn hyphen version 3.8.4. Now in the previous lecture you have already seen how you can switch between different version of JDK and Maven. You really don't need to do this again. It works with Maven 3.8 and JDK 21. But you know what to do, right? First, let's clone the source code. Okay, this is much faster. Let's get into the folder or repository and run the command Mvn install. So basically we're doing the same thing what we did in ubuntu EC2 instance. Now you're going to do the same thing on Jenkins. But this will be that will be from the Jenkins UI. So my point here is be it any operating system, VM, container, Docker, anything you should know the dependency, the tool versions and how to install those tools. Now you have help of ChatGPT grok, Amazon Q many generative AI you can use to ask the right question and get the commands you do not need to memorize commands, you just need to know the information and how to execute it. Okay, that's a build success less target you should see. Oops. Should see the artifact. That's the output, the package and the reports and stuff. Now what to do with this output that we're going to see in the Jenkins lecture. How to deploy it, how to do CI, CD and stuff that in later lecture. Now I'm going to show you one more source code that is of NodeJS. Let me clear the screen so you can access this URL github.com slash coder slash node. This is one of the service from our microservice application E-mart microservice application. We have from that this is one service. It's written in node NodeJS. And let's see you do not know what to do, how to build it. You have no idea of no maven. You know the build tool and stuff. And now you are working in a new project. Okay, Node.js. You have never seen it. What to do? Well, let's take the help of generative AI again. So let's copy this URL. Or you know what? Better copy this URL which the generative AI can scan. Now I'm using ChatGPT for this one in real time. This will be private repositories and you can use in VS code. You can use your GitHub copilot Amazon Q developer, any inbuilt code assistant. You can clone your source code on that and you can ask questions. Same as what I'm going to do now with ChatGPT. Let's go to ChatGPT. Okay. I'm going to say here is my source code. Tell me more about how to build it or like that. You can ask any question. Right. So it's obviously going to go through it and you know get the information. So saying saying clone this, go into this folder and run npm install. Okay now you can see what is npm install. Really I know what is npm install, but let's see what is npm npm and how to install it. Install it on Amazon Linux 20. Because I'm going to use the cloud shell. You can like that ask any question. You can ask what version of npm should I install? So it says Amazon Linux 2023 does not include NodeJS npm. How can I install. So it has given the command right? So it is saying some NodeJS 18. I guess you can keep asking further question like if you already know what version of node to install, you can ask how to install the node 20, whatever. Yeah, I'm going to just copy this and run it on the cloud shell. Okay. Now before we move move forward let me tell you. So Node. Think of this like we have Java, JDK and npm. Think of it like Maven mvn. So I copied and literally copied all the command at once. Now I'm just going to do it one by one node v. So it says it gives me node JS 20. And let's check npm hyphen v like we check mvn version like that. Here that's npm. Okay so I'm asking another question that I have now npm node. Now how do I build this source code. Now please do not worry, all developers will tell you what version to install and how to build it. All those information you need to get from the developer who developed the application, how to install a specific version, and all these kind of questions you can definitely ask. So saying npm install run this command. So like we have maven install with am I in the source code. Oh I didn't even clone it right. Okay, so let me clone the source code git clone and let's clone this Us clone it. Okay let's go to import node folder. And in that we're going to run npm install. Okay. So like we have pom.xml in Java in node you will see this file package dot JSON. Click on this. So like we have view profile v2 dot where know what version and stuff like that. Here we have the name. We have version description the main file scripts. This basically tells the different phases like in Maven we have different phases. Validate test. Here we can define the phases. Write test means what start means, what dev means what and like we have dependencies in Maven. Here also you see dependencies and their different versions. Now there's not much. We have this entire microservice application where we have multiple apps. This is just one of it, very small part of it, but just. I wanted to give you a taste of other build tools. Okay, so when we run this npm install, it's giving some information, some old lock file. Okay. So this is the lock file. See it's already there in the source code the lock file. So this will get created when you run the npm install command. And this was already uploaded into the GitHub repository. So it's saying it's just clearing the old lock file. Seven packages npm fund nine vulnerabilities. You can fix the vulnerability by running this command npm audit fix. Paste it. Okay it's fixed. Let's just run npm audit now to check if there is any vulnerability. It's there. It's not there. Okay, so run the LS command and you should see these node modules like we have dependencies in Maven that gets downloaded into dot m2 folder. Here you have the node modules folder. You see once you know one build tool it's very easy to relate with others. And of course there will be differences in other build tools like in Maven. We have the artifact that we are going to deploy over here. This everything is the artifact except this node modules and package lock files. You can run the application from here itself npm run. You have different modes dev mode, production mode, and there is no sharp learning curve in that you can very easily learn any other build tool, and if you work with developer, it will be much easier for you. Okay, now if you check the ChatGPT it says after running npm install you can say touch dot env. Create an environment file, mention port number, mongodb url, some secret file. Because it needs the MongoDB backend, we do not have it because we just going through the build tool. So it's showing you know how you can run this application, mention the MongoDB information and then just say node app dot js. That's one common way. The other way is auto reload. One npm install g Node one. Now node one is very interesting. You run the application with Nodemon instead of node and it will going to automatically reload your application. If you make any changes to the source code in the runtime, and you don't need to build it again and deploy it in the runtime. Now this is for testing purpose. When the developer build their application that time, they use this. So they make the changes and then they directly see how it performs. Right. But as I said, we are not getting into this in this lecture. One last thing. It it do. It does have the cache hyphen A. In your home directory hyphen A you will see npm cache. Okay. You can just look into that folder and you will see some cache and some logs. And so there's really more to node than this much. But you must have got an idea now how you can now learn other build tools, or at least start learning other build tools and start using it. Actual use cases we are going to see in the CI, CD in Jenkins and other CI CD projects. For now you should understand about build tool, what it is, how to install it, how to run it, how to check different versions of it. And please do not worry, you will get all the information from the developers and you can use generative AI to get more information. Do your own experiment with the source code before you actually do CI, CD, or before you deploy it on servers. So these lectures were actually prerequisites to continuous integration, continuous delivery sections and CI CD projects. So go through them once again make sure you understand these basic concepts. And once you're done make sure you terminate the instance and delete or close this cloud shell. Also I'm going to say action delete and type delete. Delete the cloud shell. Terminate the instance. You don't need it. Now this was just temporary and that was all in this section. I will see you in the next one.
