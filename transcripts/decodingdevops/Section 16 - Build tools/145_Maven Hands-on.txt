# Maven Hands-on

Hello and welcome. In this lecture we are going to do some hands on on Maven build tool and also on NodeJS. So first open your browser and come to this link github.com slash coder slash profile hyphen project. This is the source code of our V profile project. And when you look at the content you will see one file here Pom.xml. That should tell you that this is a maven project. So this is the Java code in src you will have all your Java code written by the developers. The Pom.xml file is for the maven to get the information. So when we execute any Maven command it reads information from this XML file Pom.xml. So open this file Pom.xml. When the developer creates the code, they will initialize the Maven project that will generate this file, and then they will later keep making changes to this and push it into the repository. So XML really is the information file. If you look here closely you will see here for example you see here version. So you need to understand this syntax at least. So you see version in this symbol and you see a text v2. So this is the opening. And then this is the closing. You see a forward slash that is the closing of Maven wants to read a text or information version. It is going to check the file and look at the information and sees here. That's v2 for URL. You see here the URL for the maven. And then you have cascading information like here. Property starts over here, ends over there. And in between this you have a lot of information. These are basically variables. For example spring version is a variable and its value is 6.0.1 MySQL connector version. Its value is 8.0.33. Just focus on the opening and closing, and in between that you have your data. Okay. So these are some information. These are variables in the properties. And then you have your dependencies and dependencies. You have every dependency. You see this is one dependency. Opening and closing in between you have information about this dependency. So group ID. Artifact ID and the version. Now version is another variable you see here dollar spring dot version. Its value will be defined here. Spring dot version. So you don't need to fully understand this, but you should be able to make changes if required. But most of the time it will be the developers who write the code. They make the changes, but you should have basic idea of the syntax. So you see, you have a lot of dependencies over here. So when you run your maven command it's going to look for dependencies, its version and download it from the Maven repository. We'll see all this in action. We need one Linux machine. So we are going to launch an EC2 instance with ubuntu operating system. So I'm on my AWS Management console in EC2 I'm going to say launch instance. I'm going to name it as builder. For now I'm selecting ubuntu. Later. We're going to also work on Amazon Linux 2023. So base OS can be anything. But you need to have right version of your tools and then the dependencies will get installed on them. So for this one we are going with ubuntu 2024. T2 micro will be sufficient. Let's create a key pair and name it as build key PEM format create key pair. Private key is downloaded. Let me create a new security group. So I'm going to say edit and name it as build SG. Same in description 22 from my IP. That's all. We just need to SSH to it and let's say launch instance. Okay, so I have my instance up and running. We're going to log in to this. And we are going to clone this source code and then build it with Maven. We're going to install the proper dependencies. And I'm going to show some options tips and tricks okay. So let's copy the public IP. Open your git bash or terminal ssh hyphen I downloads. My key name is build key and username is ubuntu at the rate the IP. Yes okay I'm in. Let me switch to root user and clear the screen okay. So we're going to clone the source code. We know the source code can be built by Maven. So we need to install Maven and Maven needs JDK Java Development Kit. So first we need to install JDK. So this is ubuntu. First let me run the APT update command. And then we're going to search for JDK. We can go with JDK 17 and 21. Both works. So in Java you will see these two names JRE and JDK. JRE is Java Runtime Environment and JDK is Java Development Kit. So to build the project you need JDK. So let's search for apt search JDK that will give you a huge list. Let's filter it. Pipe grep and let's say 17 okay so you see here open JDK 17 JDK. That's what we need to install. You see there is same one with JRE. That's Java runtime environment that is just to run the Java application. But in order to do some development work like we are doing you need JDK. So there's also JDK 21. And this also works for project. And as a matter of fact we are going to install both the JDK. And I'm also going to show you how you can switch from one version to another version. First, let's start with JDK 21. Okay, so run this command apt. Install open JDK hyphen 21. Hyphen jdk hyphen y. This should install JDK 21. Okay. It's installed. Let's check the version Java hyphen version. That's the command to check the version it says JDK 21. Now we are going to install Maven so the command will be apt. Install apt install Maven. Now again you can search apt search Maven. It's already there in ubuntu 24. Now Maven version also matters. Our project runs on various versions of Maven. Let me clear the screen and show you the Maven version. So you run the command Mvn hyphen version and you should see the Maven version. So it's 3.8.7 and it's using the default Java version, which is 21. Now our project works on various versions of Maven and JDK 21 and 17, but that won't be the case for every project as it develops. It's your duty to get information from the developer on what version of the software or the tools are required. You need to get this list. If it's Java, get the Java version, Maven version if it's node, get the node version. Likewise, any code, any build tool, you get information from the developers. Of course, they will be having this because they do it on their machine. And based on that, you're going to install that version of that particular tool. Over here we are using 3.8 and Java 21. And I'm going to I'm going to also show you how to switch between these different versions. You can have multiple versions of the same tool okay. Now let's clone the source code. So you're going to say git clone space and get the source code link. So go to the browser. Go to this link github.com slash coder slash profile hyphen project. And over here on the code click on drop down select https. Make sure you select https. Copy this and say git clone and this URL hit enter. That should clone the source code. This is a public repository so anybody can clone it. Okay it's done. And let's and let's now silently get into this folder. Okay. Let me clear the screen PWD LZ check. You should be in this folder. You run the command git status and you should be in the branch local or any other branch. All the branch. Most of the branch. We have the same source code. But check you have pom.xml you have this src folder. That's important. You can be on local branch. Okay. If you see you're in a different branch you can just do git branch hyphen a list all the branches and you can then switch to the branch that you need. Git checkout and the branch name. So we're already in local branch and that is fine. Now let's run our first Maven command Maven validate. That validates the directory structure. Now that's a mistake. That's a mistake. Validate spelling. Yeah. You see there it says unknown lifecycle phase Y date I don't know what to spell it. Okay. Mvn validate. Okay. It should give you a build success. Now let's run mvn test. Now any maven command is going to read it from the Pom.xml file the information. So Mvn test and it's going to get to the source code. Execute the test cases, the unit test cases written by the developers. And once this completes it's going to generate a report in the target folder. So build tools will have the capacity to run the test cases written by the developer. These are called as unit tests. So for this Java code we have this JUnit test cases. And we can execute them by running Mvn test command. Okay. When this is completed you run the LS command and you should see a target folder. This folder will have the reports. The output. Basically target folder in Maven will have the output of your Maven command. So right now we just executed Mvn test. So you should see the reports in this. We're going to talk about this in detail in Jenkins lecture where we do more analysis on this unit tests. Now we are going to run the command Mvn install. This command is going to fail. We are going to see one error. We'll find the root cause of it and then we will fix it. So I'm just going through the different phases of Maven. Now you know we discussed about this. If we run the install phase it's going to execute all the previous phases. And when we install this maven command, it's going to read the pom.xml file and also download dependencies. So if we run Mvn install or any maven command that needs dependencies, you see it is downloading dependencies. You see that downloaded. So a dependency mentioned in Pom.xml will have many more dependencies. So you will see huge list of files getting downloaded. And you see this URL over here. So this is the Global Maven repository from where by default Maven downloads its dependencies. You can have your own developed Maven repository or any other build repository. I have shown this in DevOps projects course how you can build your own repository because that is requirement. In some projects you can have your own proxy repository for your build tools. So these dependencies will be stored. Let me scroll down okay. So. So we'll come back to this error in a moment. But let's talk about first the dependencies where it is stored and the output. So when it downloads the dependencies by default it gets stored in this folder in your home directory you have dot m2. And this you should have a repository folder. And over here you should see this dependencies downloaded. You can anytime remove them. Sometimes you need to remove them. Actually if you're changing the Maven version or if you're changing some dependencies, it's better to remove them. You can do that. So I'm going to clear the screen and show you. So I'm going to say rm hyphen rf tilde. That means your home directory slash dot m2 m2 repository. And in that star. So remove all the folder from the repository folder, remove all the dependency. And when you run the command mvn install it's going to again download it. Or we can also remove the target folder. Remove the target folder. That's the output, right? So we do not have any output. We do not have any dependencies yet. So when we run the install it's going to be clean. Or there is another command. You can say mvn clean install. But clean is just going to remove the target folder not the dependencies. So you can do mvn clean install. That is also one way. So you see now you will see huge list of dependencies that are getting downloaded will go on and on because the dependencies will have many other dependencies. Okay. And now let's look at the build failure. So you have the target folder. You should have now art M2. Okay. So before we get forward into this let's quickly reiterate what all we did so far. So we just launched an EC2 instance. We cloned the source code Scored, right? We are in the source code folder. We then installed JDK, JDK 21 and we installed Maven. Okay, so we have now Maven version 3.8 and JDK 21. We are in the source code folder. We have the source code. We execute the command Mvn install. It creates the target folder and keeps the dependency in this folder in your home directory dot m2 repository. Okay, so now once again let's run Mvn install command. We look at the error. Then we'll fix it okay. So build failure error assembling var problem creating var so var file. So in the target folder you see the artifact. So this is the folder. And it's going to create a var file out of it. The zipped format of this folder actually I should say archive of this folder. So it is saying there is a problem doing that. And that's. Java heap space Java will create a virtual machine JVM. It is called. And that will be doing all these things inside this operating system. Java heap space basically means there is low resource. So we have an EC2 instance is just have 572 579 available now it's like one GB memory. Now we can allocate more space to Maven. So it assumes it has more GB and then it will complete its task. You can also do this. We can take this error and put it into ChatGPT and see what it says. Let's copy this error. I can just simply paste the error. You know it will tell you why you are seeing this. You can give the source code also if you wish to. So it is saying for Linux or Mac, Mac OS sorry export export command to export this variable, set this variable. This says Tzm 1024M. That means, you know, allocate one GB 1024 MB of memory for the Maven option. So let's copy this command and run this and see what happens. So this variable will be exported that will be used by Maven. Maven will think that it has one GB of memory which it does not have clearly but it gets full. Check this out. Run install command. All right. You see that build success right? We clearly do not have one GB of free memory. But it worked and that's fine. Don't worry. It's not going to cause any other harm. We just focus on the build. So if we do ls on the target folder you should see v profile v2 dot var. Now like that there might be other errors. This is definitely not the build error. This is resource issue. But you can have build error like wrong dependency mentioned. And due to that the build fails Fields that also can be fixed. Let's try something. So this is going to be interesting. Clear the screen and remove the target folder. And then remove the. So you know we'll just do a clean setup kind of. So remove this M2 repository folder and let's open the pom.xml file pom.xml vim or VI. You can do open this and search for Jacoco. And here I'm going to downgrade its version to 8.8.8 okay. Save and quit. And then we're going to run the Mvn install command. And let's see now what happens okay. New dependencies are getting downloaded. I'm not sure whether it's going to work or fail, but I'm going to make sure it fails. We'll do take some more steps. Okay. Started witnessing failure. And there's really a lot of And what will be the final output. But I definitely saw some failures. Okay, the build is success, but there are a few failures that I saw previously. Okay. Here. You see error while instrumenting. Blah blah blah. Jacoco 8.8.8. So let's take this and let's give it to ChatGPT with some more information. Really. So I'm going to copy this. I'm going to paste it this but not going to send it I also want to send it the Maven Java version. So I'm going to say Maven hyphen version. You know so it has more clarity on what's really happening, what tools we have. And also I'm going to give it Pom.xml file. Okay. And I'm going to say below is Pom dot XML. Let me get that. So you can just do cat pom.xml or you can just get the pom.xml from the GitHub repository. Okay, so give as much information you can to ChatGPT or any other tool like that. So it has proper context on what it needs to do. Okay. So it says this is a compatibility issue between Jacoco 8.8 and Java 21. Jacoco version prior to 811 do not fully support Java 21. Okay, so the solution it is saying to upgrade the Jacoco to 8.11 right in Pom.xml file. So let's do that now. We already know this, right? So I'm showing you how you can find the problem and then fix it. If you don't even know Java or any other build tool, you can give information to the AI tools. You have Amazon Q as well. Amazon Q developer in VS code, you can clone the source code, build it, and give the information. We'll be seeing that in future further lectures as well. So let's fix this and let's run Mvn clean install this time. And we can clean install. Okay, that is a success. Okay. Now let me quickly tell you how to use different version of JDK Maven different tool and switch between them. So I'm installing open JDK hyphen 17 JDK basically JDK 17. So we installed 17 right. But if I run Java version it shows 21. So how do I switch between these version in Java. So there is a command and you can use till ChatGPT or any other AI tool. So just put a question. I have installed JDK 17 and 21in ubuntu. How do I switch between them. Okay, so this command you don't even need to remember the command, but if it's a regular thing you will remember. But anyways, you have the option of AI tools. So I'm going to copy this command and run it. It's going to give me a prompt where I can choose from. So you see you have Java 21. You have 17. And then you have one more 21. Okay. So you need to select which one zero 1 or 2. So I'm going to say just type one and hit enter. Okay. Now let's clear the screen and let's run the Java hyphen version command. Now I have Java 17 okay. Test whether our code works with that or not. Mvn clean install. Now make sure you're in V Profile project folder. Okay so default Java is now 17. As I told you the code works with 17 and 21. Both okay. So it builds the artifact. So v profile hyphen v two dot var. Again this name is also coming from pom.xml file. Maven reads this from pom.xml. What is the name? What is the version? What is the packaging? Take a look once again at palm dot XML file. Go through it slowly and you will find all the information over there. Now we need to also talk about different build tool version. Different maven version. It's not so straightforward as Java. So if I want a different version of Maven I need to download the binary, unzip it and use it. Download Maven three from archive. Just google this and if you find this link Apache archive distribution directory. Here you see different version of Maven. So we need Maven 3.9.9. Let's click on that and go to binaries. And here you should see Apache Maven 3.9.9. bin dot tar dot JS okay right click on this one copy link address okay let's go to temp folder. I just don't want to download it anywhere else. Just in the temporary folder. So wget and the link. So it will start the download. Okay. It's downloaded. If you see a folder over here you can unzip it. Tar Xvf Apache maven. You will see a folder here. Now I'm going to copy this folder or. Yeah. Okay I'm going to copy it to us, our local bin with the name Maven 3.9. Okay. So that's the folder. Now let me clear the screen LS and in this you have a bin folder. And in that you have the maven command. So if you run the maven command now I need to remove this ls from here. So like we give the maven command Mvn command. It takes the default one, but if you want a non-default one, you need to give its complete path and then you can give the option so hyphen version to show the version. Let's go to our source code folder. So go to V Profile Project. And in that you can run the command. The complete path of this new Maven clean install or whatever face you want. And because this is not straightforward as you know you download it from apt or yum. And then you switch between different version. You need to give the complete path. I mean you can set it as a default also, but it's really not required. Why it's not required. That will learn in Jenkins. Now execute this command. And then if you're good, you can go through the lecture once again and then terminate the instance. In the next lecture we are going to see AWS Cloud Shell. We're going to do the same on AWS Cloud Shell. Also we're going to take a NodeJS source code and build that as well. So wrap this up and join me in the next one.
