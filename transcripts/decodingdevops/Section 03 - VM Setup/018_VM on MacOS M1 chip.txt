# VM on MacOS M1 chip

Instructor: Hello and welcome. This lecture is strictly for people who use a macOS with M1 or M2 or M3 chip, AKA Apple Silicon. Now, there are two popular CPU architecture. One is ARM, the other one is x86. Now, macOS with M1 or M2 or M3 chip uses ARM architecture, and people who use Intel or AMD chip, that is based on x86 architecture. Now, we don't want to get into the differences of it, but you should be aware that there are these different CPU architecture. We are going to do virtual machine setup in this lecture. For that, we are going to use two tools. One is VMware Fusion. This is a hypervisor or a tool to run and maintain your virtual machine. Now, if you use this alone, then you need to do everything manually, but we are going to use one more tool called Vagrant, which is a VM automation tool, Vagrant or Vagrant. So we're going to see steps of how to install and set up these tools and then create two virtual machines, one with Ubuntu operating system, it's a Linux OS, and the other one is sent CentOS, which is again a Linux OS. In some lecture, we will be also using Fedora OS. We will learn in detail about different Linux operating system in Linux section. In this lecture, we are going to just do two virtual machine setup. So let's get started with that. Download this document from the lecture resource. The steps are pretty straightforward. We're just going to execute all of them one by one, first starting with installing Rosetta. Now, Rosetta is a tool which helps you run, you know, x86-based apps on ARM CPU. So you just need to copy this command and open terminal, paste this command, which will start the installation process. Wait for some time. Okay, once Rosetta is installed, we are going to install Vagrant. For that, we are going to use brew command. So copy brew install vagrant command from the document and paste it. So Brew will install Vagrant for us. This might take some time. Okay, it will ask you the password. I enter the password. Now, this will take some time. It's not going to show anything, but the installation is already going on right now. Vagrant installation completed successfully. Let's go to the next step. Now, we need to create an account on Broadcom so we can download VMware Fusion. Now, VMware was acquired by Broadcom, so all the tools from VMware, we need to get it from Broadcom. So go to this link and register yourself. So I'm gonna click on this link. Okay, click on register and fill in the details. Enter your email address and the text shown here. Enter the verification code that you received on your inbox. Enter all these details. Okay, once your account is created or once you're registered, then log into this site. Click on login, enter the details. Okay, I'll give the username. Go next. Enter the password. Okay, once you are here, click over here in this dropdown and select VMware Cloud Foundation and go to My Downloads. Click on this link where it says free software downloads available here. Search for VMware Fusion. Click on it. Now, either you see a list over here or you need to expand it. You can click on this arrow over here, right arrow. It should expand it and you can click on 13.6.2. Click on that. Now over here, you might get a check mark over here. You need to select that, so you accept it and then you can click on HTTPS download. Prior to downloading this file, additional verification is required. Proceed, yes. Okay, fill in some other profile detail like address, city, and stuff. I'm going to fill that and click on Submit. Okay, it sends me back to the same page again. I click on Download. Okay, so I click on this link once again and the download is started. I'm going to wait until this is completed. Okay, once this is completed, just double click on the software. The installation should start and you should see this icon over here, VMware Fusion. You need to double click on this one. Double click and this should start the installation process. Okay, click on Open. Enter your password. Okay, this window should come up. Just say Agree and you need to select this option. I want to license VMware Fusion for personal use. Select that one and going to Continue. Say Done. Okay, this is installed. Just cancel it because we're going to use it through Vagrant. Cancel it. Okay, next step, go to Systems Setting, open your Systems Setting and go to Privacy and Security and go to Accessibility. Here, you should find VMware Fusion. Just toggle the switch. You need to enable this one. If you don't find VMware Fusion here, then you need to click on this plus symbol, enter your password, and then find VMware fusion from the applications here and then you can say Open. But if you already see this, just toggle the switch on and that is all. Okay, now we need to install some, you know, you can call them as drivers or plugins. So first with Brew, we need to install Vagrant VMware provider. So Vagrant should know that there is a VMware tool to use. So you just need to run this Brew command. So copy this Brew install task command, open your terminal and paste the command over there. Enter the password. Okay, this is installed. Let's go to the next step. Next step is to install a Vagrant plugin to use a VMware. So just copy this command and don't worry about plugin and all. We're going to learn this later in the Vagrant section. Just for now, just copy this command and paste it in the terminal to install this plugin. Okay, so all the installation completed successfully. Now, we can create the virtual machines. We're going to do all through command line. Once again, do not worry. By the time you complete Linux section, you'll be master of all these commands. For know, just go through it and then execute all these steps one by one. We'll open the terminal and create a folder. Ubuntu, we'll go into that folder and then we'll create a file called a Vagrantfile with V capital. And we're going to paste this content over there. All this will be through command line. We'll see it in a moment. Once we have this content in the Vagrantfile, we're going to run vagrant up command, which will bring up the virtual machine and then we'll see a few Vagrant commands. Okay, so let me open the terminal. I'm going to run a command called clear, which should clear the screen and I'll just type cd and hit Enter so I can be in my home directory. Directory or a folder means the same thing. You can check you are in which folder by running pwd command. So I'm in my home directory. So if you're in somewhere else, just type cd and hit Enter and you'll come back to your home directory or home folder. Now I'm going to run a command mkdir -p Desktop/vms/ubuntu. Now, you will already have a desktop folder. In this, it'll create a folder named vms. Inside that, it'll create a folder named ubuntu. If you use -p option with mkdir, it'll create all this directory one after another. So just hit Enter and then let's go into this directory, cd Desktop/vms/ubuntu. Next, we are going to create a file named Vagrantfile with the command vim, so type vim space Vagrantfile. Make sure there is no spelling mistake and hit Enter. You need to click on I to get into insert mode. So hit I on your keyboard and you should see here Insert. Now go to the document and copy all this text you see. Select this, copy it, and paste it in the terminal here. Hit Escape and then colon then W and Q. They should save the file and quit. Okay, now once again, check pwd command. You are in which folder. You do ls command, you should list the available files over there. You can use cat command to print the file. You can say cat and give the filename and you should see the content. Now, we will use a vagrant up command to bring up this virtual machine. You have to make sure you are in this folder, ubuntu folder, and run the command vagrant space up and hit Enter. In a few minutes, you will see the VM will get created. If you see this error, then run the vagrant command once again. You can use the up arrow on your keyboard, see the vagrant up command and hit Enter. You should see a window popping up over here. You have to minimize this one. We are not going to use this window. Just minimize this. We are going to be on the terminal all the time. Okay, so the VM is created and to log into the VM, we are going to run the command vagrant ssh. So in the same folder, run the command vagrant space ssh. Hit Enter. And now you will see the prompt will change. See, this is called as the prompt. The prompt is changed. That means we are inside the VM. Here you run the command ip space addr space show. They should show you the IP address of your virtual machine. And don't worry about IP for now. We're going to use it later at the time we'll learn about it. But you should see this IP address. Now, you can exit and you're back on your macOS terminal. And to power off this machine, you can do vagrant halt, which will power off this virtual machine. The VM is off. Now if you want to power it on, once again, you can run the command vagrant up and it will power on the VM. This time, it is going to power on. It's already created. A window pops up once again. Minimize it. Okay, the VM is up. You can run the command vagrant status in the same folder to check the status of the VM in the current folder. And it says this is running. To delete the VM, we can say vagrant destroy and just type Y for yes and this deleted the VM. Now if you do vagrant up, it is going to create the VM once again. Now, whenever I tell you to bring up the Ubuntu VM, you have to do this. You have to go into your home directory first, cd Desktop/vms/ubuntu and then you say vagrant up. This will bring up the VM. And next, you know, vagrant ssh to login. Exit to come out of the VM. You should see your prompt. Just observe the prompt to know where you are. Okay, now we will go back to your home directory. So type cd and hit Enter. Clear the screen. Make sure you are in your home directory. Now, we will create a CentOS VM. So once again, similar process, create a CentOS folder, go into the CentOS folder, cd Desktop/vms/centos and create a file called Vagrantfile. Again, make sure the spelling is correct. Hit Enter. Hit I on your keyboard to get into Insert mode and look for the CentOS settings. There, just copy this and paste it in the VIM editor, escape colon wq, save and quit. ls to list the file. cat to read the file, cat. Vagrantfile and then vagrant up. Bring up the VM. Again, make sure you're in the CentOS folder this time. Bring up the VM. Minimize this window. Okay, VM is up. Once again, vagrant ssh to log in. Look at the prompt. The prompt is changed. I'm in the VM, ip space addr space show to list the IP address. You should see this IP address here. Once you see this, you can exit from the VM and you're back on your macOS terminal. Once again, look at the prompt and you can again do vagrant status to check the status of the VM. It says it's running. So vagrant halt to power off. It's off. vagrant up to bring it up and vagrant destroy to delete it so you can destroy it now, vagrant destroy. Say yes, Y. Okay, anytime you want to check all the VMs created by Vagrant, you can say vagrant global space status. So there are no VMs created now because we destroyed both the VMs. If you have the VMs created, you should see the name and the folder where they live in. Okay, now, on your own, you need to try the same thing to bring up both these VMs. So type cd, hit Enter, clear the screen, come back to your home directory, and then go to Ubuntu folder. Bring it up, bring up the VM, power it off or destroy it. Go to then CentOS, bring up the CentOS VM, then destroy. Remember the path ls Desktop/vms. In this, we have two folders, Ubuntu and CentOS. So whenever I say bring up the CentOS VM, you need to go to the CentOS folder, bring up the VM. When I say Ubuntu, go to the Ubuntu folder and bring up the VM. So that is all in this lecture. In Linux section, we will see how to bring up this VM and use it, learn Linux commands and become the master of command line. Thank you very much for watching. I'll see you in the next one.
