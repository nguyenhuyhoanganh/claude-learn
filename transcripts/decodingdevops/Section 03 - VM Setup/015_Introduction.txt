# Introduction

Instructor: Hello and welcome. In this lecturem we will talk about how to create a virtual machine so we can practice Linux in the next section. So we will be creating two Linux VMs in this section. One is CentOS, the other one is Ubuntu. These are two very popular flavor of Linux operating system and we'll talk about their differences much in detail in Linux section. So there are two methods of creating VMs. One is manual, where we will see each and every step of creating VM and installing operating system on that. This will be wizard-based where we select multiple options in order to create VMs. The second option is automated. You can also automatically create VMs, which will be very simpler. You create a text file, you issue a command, and the VM will be up and running. So why not we directly go with the automated method? It's fast, it's simple, right? Let me tell you the most important thumb rule. If you want to automate something, make sure you know how to do it manually first. When you do a task manually, you know each and every step, you know the entire recipe. And automation is simply assembling all these steps together in a logical way by using a tool, or scripting, or any programming language. Never, ever forget this rule in your DevOps career. If you get a task to automate something, first, do those things manually. Okay, let's talk about the requirement. You need a 64 bit computer with high speed internet connection, and your computer can have Windows 10, 11, MacOS with Intel chip or M1 chip, or even Linux. Even if you have a Linux desktop, still, you are going to create Vms with Linux operating system on that. This is very much required in this course for the practice. Later, we'll be migrating to AWS, so we'll be doing everything on cloud computing, but for initial practice, you need Linux VMs. In the prerequisites section, we have installed many tools. Make sure you have completed that section. You have all the necessary tools to create VMs. First, we'll see the manual step, and these are the tools that we need in that. Oracle VM Virtualbox, which is the hypervisor. Make sure you remember this name, Oracle VM Virtualbox. If you have any other hypervisor, like VMware Desktop or anything else, I recommend you don't use that in this course. You have to use Oracle VM Virtualbox so we are all in the same pace, except MacOS M1 and M2 chip, you will be seeing a different tool. instead of the Virtualbox. You will need ISO file, CentOS and Ubuntu. These ISO files are basically installation files which we are going to attach to our VM. We will need a login tool, Git Bash or Putty. We'll see both. In automated, you still need virtual box for Windows and MacOS Intel chip. If you have MacOS M1 chip, there is a separate lecture on that. Make sure you install those tools. You need Vagrant, and this is the tool that will automatically create VMs for us by issuing very simple commands, which we will see. Okay, so two labs basically. One is to set up VM manually, you need Virtualbox where you're going to create VM following through some steps and then you are going to download an ISO file and install operating system on that VM. The second lab is automatically where you create a Vagrantfile with the box name and you issue the command vagrant up. So enough talking, let's get into action now. See you in the next lecture.
