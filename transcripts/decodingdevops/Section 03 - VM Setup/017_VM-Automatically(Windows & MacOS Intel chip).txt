# VM-Automatically(Windows & MacOS Intel chip)

Instructor: Hello and welcome. In this lecture we are going to talk about a tool called Vagrant. We use this tool to manage virtual machines automatically. So in short, Vagrant is a VM automation tool or it manages VM's lifecycle right from creating, making changes, and cleaning up. Vagrant tool is not a replacement of any hypervisor like VMware or Oracle VM VirtualBox. It does not replace that. It's on top of that. It uses those tools to create and manage virtual machines. Now, Vagrant solves a problem and what is that problem? We should know that. When we create or manage a VM manually, there are many issues with that. Starting with OS installation, there are many steps that we need to go through while installing OS on a virtual machine. That's one problem. There are other problems. It's time consuming process, many steps you have to go through, the installation itself takes huge amount of time, and if you have multiple VMs, multiply that time, and when there are many steps, manual, there is a chance of human error. Automation in general, not only about Vagrant, but any automation address this very specific problem. Human errors. For one or two VM, it's fine, but when you have many VMs like five or six VMs, and you want to replicate the same on another computer, it's really tough, you need to go through the whole process once again. Or you can use documents to show all the installation and provisioning steps of the VM. So there are many issues when you manage the VMs manually, but if you're using Vagrant, it solves all those problems. Starting with OS installation. In Vagrant, there is no OS installation. Yes, Vagrant set up virtual machines by using VM Images or in Vagrant, they're called as Box. These are ready-made virtual machines stored on Vagrant Cloud. You have freely available boxes or images on Vagrant Cloud, which Vagrant will download and keep it in your local computer. And then from that box, You can create as many as VMs you want, as many as times. All the VM settings will be in a file called Vagrantfile, V capital, and you can make changes to the VM settings like RAM, CPU, IP addresses all through this Vagrantfile. This is a text file which you can open and and edit. You can also do provisioning with Vagrant. Provisioning generally means when the OS is completely booted, you want to execute some commands on it. Maybe you're setting up some server, web server, database server, anything you go through so many commands. So all those commands you can mention in the Vagrant file and Vagrant will provision it for you and they're very simple to use a Vagrant commands. You have vagrant init boxname, which mentions the box name, and then you do vagrant up. vagrant up will create the VM if it's already there, it will power on the VM, vagrant ssh to log in, vagrant halt to power off, vagrant destroy to delete. We'll see all these commands. But before that, let's understand the architecture of Vagrant. Okay, so that's how Vagrant work. It's architecture. You have to create a Vagrantfile. You put the VM information over there and you run the command vagrant up. Vagrant is going to read the Vagrantfile, and see if the box, which is the image, is available in your local machine. If it's not available in your local machine, it will go to Vagrant Cloud, download the image, and then it is gonna contact the hypervisor, which in our case is Oracle VM VirtualBox, and that's the default hypervisor. If you want to use VMware Workstation and other, you can use that as well. But the default hypervisor is Oracle VirtualBox. So it will contact the Oracle VirtualBox, and give the information of creating the VM, and then the VM will get created. You can create one or many VM by using Vagrant. The simple to use Vagrant commands like vagrant halt, vagrant reload, vagrant destroy, and when you reload, it's gonna read the Vagrantfile when it's coming up. So any changes will be applied. If you were to log into a Linux VM, you can do vagrant ssh, and like that, there are many more commands. There are very simple steps to create virtual machine with vagrant. First you need to create a folder in your computer. Then you place a Vagrantfile in that folder, you issue the command vagrant up, and then you can use vagrant ssh to log into the VM, and you can use later vagrant halt to power off, or vagrant destroy to delete the VM. So let's get started then. Vagrant is a command line tool. It's not a graphical tool and to execute vagrant commands or to execute commands in general, we are going to use Git Bash in Windows. Mac OS user can use Terminal. So I'm going to open Git Bash, Macs OS user open your Terminal, execute the command pwd. This shows the present working directory. So I'm in C/Users/imran, this is the user's home directory. This is the user imran and imran user is in the imran user's home directory. This symbol over here, tilde, this also represents the home directory. This will change if you move to other directories or folders. Now I will be creating my files in F Drive. If you do not have F Drive or any other drive, you can do it in C Drive. Macs OS user should be using a Desktop. So I will be saying mkdir /f/ vagrant-vms, hit Enter, Mac OS user instead of F, you have to do this, you have to say tilde /Desktop. You can issue this. So this command created a folder called vagrant-vms in F drive, this command created a folder called vagrant-vms in Desktop, Mac OS user used Desktop, Windows user you can use F or any other drive that you have. Now let's get into the F drive. So the command is cd /f/vagrant-vms/. You see here the prompt is changed. I can use pwd to check it. ls command is to list the content of this folder. There's nothing in it. We are going to create two more folders here. mkdir centos a and mkdir ubuntu. We do ls and you should see two folders in vagrant-vms folder. I'm going to issue a command called clear. This is going to just clear the screen. You can always use up arrow, down arrow to see the previously executed commands. Okay, once again ls. And I'm going to get into centos folder here, cd centos. If you see currently I'm in this folder, you can also issue the complete path, cd /f/vagrant-vms /centos/ like this. Or you can do like this cd /f/, get into F drive, then you say cd vagrant-vms, then you can say cd centos. For now, just execute the command. I will be explaining about this path and everything in Linux section. If you are lost, just use pwd, and ls to find out where you are. Okay, let's clear the screen. ls, you see there is no file over here. We are going to place a Vagrantfile over here with the command called vagrant init and the box name, but first we have to find the box name. So I'm going to open Vagrant Cloud. So just Google Vagrant Cloud open that link and we will search here CentOS nine. So here we have ready-made VM images. In Vagrant, we call them as box. So give the name, search for your box. So I've given CentOS nine, so there is generic CentOS nine, and a few other boxes like boxomatic, eurolinux-vagrant. So we will be using this one, eurolinux-vagrant/centos-stream-9, click on that. If you come down, you see it supports VirtualBox and few other hypervisor like libvirt and VMware Workstation. We have VirtualBox so we'll be using this box, and this specific box has few requirements that we need in this Linux section. So we'll specifically using this box and copy its name from here, exactly from here, not from here because it'll copy extra space there. So copy that and go to Git Bash. Make sure you are in the same folder, vagrant init, and paste the box name here, you can use Shift + Insert in Git Bash to paste, or you can right click and paste also. Okay, it says a Vagrantfile has been placed in this directory. If we do ls, we should see the Vagrantfile. We can use cat command to print the content of this file. So cat Vagrantfile, V capital, you can just type Vag and hit Tab, and it will complete the rest of it like that. Okay, so if you come up here, there's so many lines in this file, you don't need to understand these lines. We'll talk about them later in Vagrant section. For now, just look at this line, config vm.box and there's the box name. Now let's say suppose you make a mistake in running vagrant init command and you give the wrong box name. You can anytime open this file and edit it. So you can go from the explorer right here, wherever you have your Vagrantfile, and you can double click on the Vagrantfile, and you can open it through Notepad also or Notepad++, right, so if you make any mistake, just edit and make the change, save the file. That's all, don't do any other changes. We will talk about this later. Come back to Git Bash, again, make sure you are in the correct folder and issue the command of vagrant up. Okay, here you may get some errors, not mandatory but if you get any error which says schannel: next InitializeSecurityContext, or a Vbox hardening zero x eight zero, and you see some numbers over there. This is caused mostly by your antivirus. You need to disable your antivirus and then try once again. Now some antivirus are a little difficult to disable, so you need to make sure you completely disable the antivirus and then do vagrant up once again. If you have any VPN connected, please stop it or disconnect it. If you are in a corporate network, you would be behind a proxy server that will also cause these kind of issues. So try disabling your antivirus, disconnect your VPN, and if you are in a corporate network, make sure you're using some other internet. Okay, in a minute or so, the VM is up. So Vagrant actually downloaded the box from Vagrant Cloud, stored it on our local machine, and from that it created the VM. You can run the command vagrant box list to see the downloaded boxes. You can see that box is downloaded. vagrant status command to check the status of the VM if it's running or not. Okay? It says the VM is running and it's running on VirtualBox. If you open a VirtualBox now, you should see a VM over there. See that? centos_default. That centos is the folder name. I'm just showing you don't need to do anything from the VirtualBox. Okay? Close it or minimize it. To log into this VM, you can issue the command vagrant ssh and it will use the ssh command in the background and log in inside the VM. Okay, see that we are in the VM. Okay, how do we know we are in the VM? Look at the prompt. Okay, it is changed now. This was the prompt of my computer. Now the prompt is changed here. Here you see vagrant as the username. Let me clear the screen. Oh wait, I'm going to issue the command whoami, which says which user you are currently. So it says vagrant, that's the user. If you look here, the user is imran, right, host name, that's localhost. You are not given any specific host name. pwd says you are in /home/vagrant folder of the VM. You can use a command sudo -i. This command will switch from vagrant user to root user. See, prompt is changed and you can run whoami, it says root user. These are just get to know command. We'll talk about these commands in detail later in the next section. When you issue the command exit, it is going to log out. Right now we are root user, so exit means logout from root user. Now we are in vagrant user. If we do exit, it's going to log out from the VM. Right, how do I know I'm logged out? Look at the prompt. Right, if we want to power off this VM, we are going to use the command vagrant halt, which will power off the VM, okay, vagrant vm, vagrant status, it says it's powered off, you can check in the VirtualBox. It should be off, right? If you want to bring it up again, you can just issue the command vagrant up. Now this time the VM already exists, so it's going to power on the VM that's all. If you ever want to reboot this VM, you can issue the command of vagrant reload, which will reboot the VM. Now, when it reboots, it is going to read the new changes in the Vagrantfile. If you make any changes in the Vagrantfile, it is going to apply those changes, but not the box name. If you change the box name, you have to delete the VM and recreate. We'll see the command to delete the VM in a moment. All the time, remember, you need to be in this folder to execute those Vagrant commands. Vagrantfile is in this folder, so you need to be in that folder to execute the Vagrant commands like vagrant up, vagrant ssh, vagrant reload, et cetera. Okay, Vagrant VM is up or just the VM is up. You can use the command vagrant destroy to delete the VM. It's pretty simple. You destroy, it deletes, you do vagrant up, it creates a VM once again. So I'm going to say vagrant destroy. It's going to take a moment to delete the VM. It is going to ask you a question first and you can say Y, yes to delete the VM, and VM is gone, you can check, vagrant status, and you see it says not created the vagrant or the VM is deleted. You can check in the VirtualBox also it's gone now. Okay, but no problem. Make sure you're in the same folder and you issue the command vagrant up. It will create a VM. So this is going to be a new VM, okay? The previous one is gone deleted. So any changes you make in that VM, everything will be gone. It's deleted. vagrant up is creating now a new VM. Now, when it is creating the VM, the box is already present in your computer now, right? So it simply says importing base box. Okay? Now it's not going to again download the box. It's already in our computer. Now I know there's so many Vagrant commands or so many commands, don't worry, I'm gonna just reiterate everything once this is completed and by the time you complete Linux section, you will be a pro in command line. So if you're feeling it difficult, don't worry. This is just the beginning. We'll be practicing command line so much that it becomes very natural to you. Okay? This is newly created VM. I'm going to show you one more command since we are here. vagrant box list. This will list all the downloaded box on your computer. So you see I have some boxes. I have bento/ubuntu-22, geerlingguy/centos7, ubuntu/focal64. You might be just having this one single box. Okay, I'm going to now power off this VM and let's move to the Ubuntu VM now. So vagrant halt to power off, keep it there. Okay, let me clear the screen. Now we have to get into the folder /f/vagrant-vms ubuntu. We are in this folder, right, centos. So I'm going to show you one more command here. It's option actually cd .. This is going to take you one level up, check now you are in vagrant-vms folder, ls, and you can do cd ubuntu. So you can just say ub, right, you can type like that and you can use Tab to complete this, right, if it's that, if that's the only name here, it will complete it automatically. Okay, now I'm in ubuntu folder, or you can just say /f/vagrant-vms, or whatever path you had given previously. You can do like this and like that also, you can come into the ubuntu folder. Okay, let's find out now, ubuntu box. Let's go back to the Vagrant Cloud. Vagrant Cloud. Now here, search with the name ubuntu jammy. This box, this is the one that we will be using, ubuntu/jammy Jellyfish is the complete name of Ubuntu 22 server OS. Now let's copy this name. Let's come to Git Bash. All right, check here. It should be in this folder or you can just directly give the complete path of the folder to reach there. That also we can do and run the command vagrant init. Shift + Insert to paste the box name or just right click and paste. A Vagrantfile is created there and all you need to do now is vagrant up. Okay, the VM is up. Let's check it's status, vagrant status, and it says it is running, right now I want to show you one more command over here. Now we have two VMs, right? When you run vagrant status, it's going to show you VM status of the VM that is in that particular folder, right? But if you want to see status of all the VM, you can say vagrant global -status. Okay so, you see some other VMs also that I had created previously. So this one, right? This is powered off CentOS, and this Ubuntu one, this is running. Now there are some old entries. I'm gonna say --prune. You don't need to do this. If you have some extra VMs that you see through Vagrant but not through VirtualBox, you can run this command to clean this table. Okay? See, all the entries are gone, right? So two VMs CentOS which is powered off and Ubuntu, which is running. You can see the folder they're present in. Right, clear the screen. So once again, vagrant status, right, this VM is running. We can do vagrant ssh to log in. Okay? We are in, you see the prompt is changed. Let me clear the screen. Okay, we are in the VM. sudo -i to switch to root user, right, exit to log out from root user. Once again, exit to log out from the VM itself, right, observe the prompt, okay? Everything of the prompt, the username, the host name, the tilde, the dollar, the hash. We are going to talk about this in detail in Linux section. For now, you're going to observe more, okay, I'm going to do vagrant halt to power off the VM. Once again, I need to be in the same folder. Okay? I'm using up arrow to see previously executed command, vagrant global-status, hyphen, hyphen, global-status. get that one, hit Enter, right, so you can anytime use this command to find out where your VMs are and what are the status, and then you can go to that folder, I'm gonna say cd/f/vagrant-vms/centos seven. Sorry, just centos. And issue the command vagrant up, right now I'm in centos folder. So it's going to bring up this CentOS VM, and if you want, you can observe VirtualBox. Again, observe, don't make any changes through VirtualBox. Everything will be managed through Vagrant now. Okay, see that CentOS VM is coming up, okay, don't make any change once again through VirtualBox, don't power off, power on the OS. Vagrant won't know what's happening and it may give you false status of the VM, okay? And if you are good with this, you can bring up the VM through Vagrant, log in, do all those thing, exit, power off. You can do everything. Then you don't need these two VMs, which we created manually. You can simply remove them. If you want, you can keep them. There's no harm, but we are really not going to use them. Remove, delete all files. Make sure you're deleting the right VM, not the Vagrant VMs. Okay? The old VMs that we created, Ubuntu VM, Remove, Delete all files. Okay? So as per this lecture, you need to create these two VMs, log in, logout, and then vagrant halt power off them, right? And keep them because we will be using them in Linux section to learn Linux. For some reason, CentOS VM takes a really long time to shut down, but that is fine. Let it take time. It's gonna be a graceful shutdown. If it doesn't shut down gracefully, it is going to force and power off. Always before you power off your computer, make sure you power off the VM, okay? Power off the VM first and then power off your computer. Okay? I'm going to clear the screen and show you one command, history. This command will show all the previously executed commands. Okay? Nice view. So I'm scrolling up. Okay, from here, 513, right? So pwd to check the present working directory. cd to get into that folder, or wait before that, we created the folder, right there, right? 502. So create the folder. So Windows user make had given the path slash F or slash C, whatever, Mac OS user had given the command Desktop. We go into that folder, right, create centos folder, create ubuntu folder. We go into centos folder, or we can go to the centos folder by giving the complete path, right? Then we issue the command vagrant init box name, which creates the Vagrantfile. You can print the Vagrantfile content by using cat, vagrant up to bring up the VM, vagrant status to check the status of the VM, vagrant ssh to log into the VM, vagrant halt to power off the VM, vagrant up to power on the created VM, which is already there, it's powered off. You can do vagrant up to power it on, vagrant reload to reboot, vagrant destroy to delete, right vagrant box list to list the available boxes on your computer, right, then we did the similar thing for Ubuntu. We went into the Ubuntu folder, vagrant init the box name, vagrant up, vagrant status, and also vagrant global-status to check the status of all the VMs, right, once again, vagrant halt to power off. Anytime you can use this command to check that the status of all your VMs, and you can move to that folder and power on them or power off them or delete them, right? So don't stop here, do your experiments, but keep these VMs as it is, okay? You can create the VMs in other folders, bring them up, may use different boxes, try them, and make sure you're very good with creating a VM through Vagrant. That's the main motto of this lecture. And also make sure you keep these two VMs, which we created in this lecture. If you destroy them, you can just do vagrant up to create them, vagrant halt power off those VMs. Keep it because we will be using these VMs in the Linux section. So that is all in this lecture. Wrap this up and join me in the next one.
