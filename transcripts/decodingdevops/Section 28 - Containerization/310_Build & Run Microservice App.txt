# Build & Run Microservice App

Hello and welcome in this lecture. We will take this docker compose file and from this file we will build and run our microservices together. For this one, I am going to launch an EC2 instance and run the build process on it. You can do the same on the vagrant VM also. And I will attach the Vagrant file in the resource section. You can use that Vagrant file to build your VM and run the same process over there. But since it is very slow on our local VM, I'm using ec2 instance over here. Now there will be multiple containers there's, DB containers and all the app containers and the API gateway. Everything. So this instance cannot be t two micro. It needs to be t3.medium. And yes, there will be some charges and you just need it for less than an hour. So the charges will be very minute. So I will launch the instance, I will name it as Docker engine. And I need Ubuntu AMI, Ubuntu server. 20 will be fine. As I said, T two micro won't help here. We need t3.medium, t3.medium, four GB Ram. We need two CPU and Internet speed will be also better on this one. Key pair, I will create a key pair type pem and I will name it as docker key. I'll say create key pair. Let me save the key pair. That key pair is selected. Allow SSH. I will just say allow it from my IP. We'll also need Http rule. That is because we will access our Nginx container which is the API gateway. So I'll just say allow Http from the Internet. Size volume size needs to be bigger here at GB will not work. We will need minimum 15 GB. Or you can go with 20 GB also, but 15 GB should be fine. Just for a safe side, I'll give 20 GB type GP. Two is fine. Advanced detail. Now we will need here docker engine and we will need docker compose. And we need to add Ubuntu user in the docker group. So I'm going to put this script it installs docker engine, installs docker-compose add's ubuntu user into the docker group. Three steps. So copy all this. This will be in the resource section. So copy this and put it in user data. For some reason, if this does not work. You login to this EC2 instance and execute the steps to install docker engine and docker compose and add Ubuntu user into docker group. You can do this manually. Also. After the launch, let's launch this instance and we will wait. It's going to launch. Come in and running state and then execute our script. So wait like five to ten minutes and then log in. Okay, I waited for a few minutes and now I'm going to log into this. ssh -i downloads, docker key, ubuntu and the IP Okay, I am in. And..... Let me run first ID command which would verify, yes, the user is in the docker group and the docker group will only come if you have Docker installed. And you should also have Docker Compose, right? Compose is also there. So all we need to do is it's pretty straightforward. We just need to fetch the source code and use the Docker Compose file to build and on our container. So we'll say git clone, get the URL. You should have the repository clone here. And this is what we saw in the last lecture in Vs code. So Docker compose file has build step and how to run the container option. Also we are going to say Docker Compose Build. First we will build, and then after that we will run the container. Because build takes a very long time. We have multiple microservices. So let's click on Docker. Let's hit Docker Compose build. And this will take a very long time. So once you hit build, take your time. Or maybe if you want to take a break, take a break. If you want to look at all that output, you're free to watch. I'm going to pause this video and start when this build completes. All right, so the build process completed. And now we should have images. So we have three images client, which is our angular app hosted in NGINX container. Web API, which is Java hosted in JDK container. emart API, which is NodeJS running on a node JS container. And you see some other containers over there, sorry, images which does not have any name. These are the build images. So when you do multi stage, you'll also see your build images. Now all we need to do is to run them together. Docker Compose up, D to run it in the background. If you want to run it in the foreground, don't use Hyphen D option. So you can see all the output of all the containers. And all means these three images. From these three images will have three containers. Then we have NGINX container, we have a Mongo container and we have a MySQL container, as we have explained in the previous video. If you have doubt in this, once again, take a look at the Docker Compose file and then do Docker Compose up. So it's pulling the Mongo images, mongo, MySQL and NGINX it will pull and it will run the containers from that and plus the three images that are already built, which is going to directly run containers from them. And I have not used Hyphen D option. So everything you will see on the foreground, you will see Web API, that is Java EMart DB, Web API, all the containers you see coming up and their logs are coming on the screen. Okay, I think it's up. And now we will access the API gateway, which is the NGINX container. And we already did port mapping nginx container port 80, map with port 80 of the container. So we just need to access our docker engine, which is the ec2 instance on port 80, and we should see the front end, and then we can log in and see the back end services as well. Okay, so I'm taking the IP address of my docker engine. I put it in the browser and just hit Enter. So basically it's going to do Http and colon port 80, which I don't even need to specify because that's the default port for Http. But let's hit Enter and we see it's loading. Okay, it's up and running. You can click on Register, give your details, any fake email address and a password. It will ask some other information like ID number. So fill in this information, say Register and I then log in and I'm in. And there is the book smart. Try Book smart. Go back. Now, this is a sample application, so it's not fully functional, but we see all the services are connected. That's the books API. And what you see in front of your screen right now, these pages are coming from the Nginx angular content and the login. All that intelligent work coming through the API of node. So all the services are working together. Let's go back. Now, I'm going to do here control C, which will stop all our containers. And now the right way to run it will be using Hyphen D option. So it starts all the container again and runs them in the background there. And then you can say docker PS, see all your containers running there. If you want, you can check logs of individual container, docker logs and the container name. You can see the logs, take some time, play with it. docker-compose ps, docker-compose top top, use all those commands. Once you're good, then you can do the clean up process. You're going to say docker compose down, which will stop and remove all the containers. Stopped and removed all the containers. If you want, you can remove the docker images. Now I know docker compose build. The build step took long time. And if you're thinking if it takes that much longer every time, no, we already have the images. Now, whenever developer makes a code change, we pull the changes. Right now there are no changes, but if there are any changes, we pull the changes and we do docker-compose build. Now, it's not going to build from the scratch. There's already image layers which are already cached, so only the changes will be built. You see, that was faster. I mean, we did not make any change, but even though any changes are there, it will be small changes, simple changes. And in micro services, it's always encouraged to make small, small changes multiple times, rather than making big and huge changes. So any small change, you build the image once again and you run the container and test it. And this all part can be automated by using a CI CD pipeline. Okay, so as I said in the previous lecture, also take your time. Go through the code again, stop the container, remove images, run the build process again. Test it. Get comfortable with all these processes. Once you're done, make sure you stop and remove your docker engine. Because it's T three medium. T three medium we have here. So just stop it. If you want to terminate it, you can terminate it. But if you just stop it, that is also fine. If it's in a stop state, there won't be any extra bills. Only when you run it that time, you will see charges. So I'm just going to stop it? Alright then, that is all in this lecture. I will see you in the next one.
