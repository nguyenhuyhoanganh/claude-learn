# Summarize

Imran: Okay, now let's once again take a look in our source code. Let's summarize this. So we really started writing the Docker-files first, right? But we are going to understand now once again, summarize this by looking at the docker-compose file, because this gives the whole picture of our containerization exercise or project. Now, if you... Once again, take a look, it will be very easy for you to understand the docker-compose file. But we have come to this conclusion because we have all the information that we have gathered so far. So in any project, the primary thing is information gathering, you collect the information. First, knowing how to build and deploy the project, right? You work with the developer and understand the development steps or deployment steps or setup step of the application. You understand the different services that are used in the project, like in vprofile-project, we have a database, we have a Memcached, we have RabbitMQ, we have Tomcat, we have Nginx, right? So, you know, you can start creating a skeleton of a docker-compose file from the very beginning itself. And you can say services and you can give the name of the services, right? And then giving the build step, you mention where will be the Dockerfile, right? So looking at the Dockerfile. Again, Dockerfile is the information of how the application is deployed, right? The setup step, same way you write the Dockerfile. So if you need Maven, you can use OpenJDK. There is also Maven image directly available from Docker Hub. We can use that. But I wanted to have a few more steps over here. One thing I have given over here is clone the source code. This necessarily doesn't need to be cloning here. You can also mount the current repository folder in the container image, right? It's pretty easy, all you can do is you can give here step ADD and you can say dot. In our case, not exactly dot, we need to give dot dot slash dot dot. So dot dot slash means the Docker-files folder, and after that, again dot dot slash means the vprofile-project, this folder, right? That's where our source code is, src file, you have pom.xml file here, right? You can mount it to a folder. We can say, like, vprofile-project. Right, and then you do cd into that and then you can run mvn install directly or maybe you need to checkout to some other branch. Not exactly checkout, you can just give mvn install. But there are a few complications here because of the branches. So I did not mention that step, but ideally you should be doing this step. You can do git clone also, right? So you know how to build the artifact, so build image should be always separate and the app image should be separate. Okay, you should copy the artifact from the build image to the app image. Right, db image, mostly db images will be simple as this. You take a MySQL or the Redis or whatever database in the project is, right? And you put your schemas in inside that, you mention the username, database name, right? It would be mostly easy like this, okay? Coming back again to the docker-compose file. So, now when the container runs, what port number they should be running on, we need to export any variables or not, right? All this information you should be collecting. Again, information gathering will give you all this information, right? So once you can containerize one image, the others will follow along. It will be mostly copy-paste and make changes. So at the end of the day, it will be docker-compose and Dockerfile, these two things. You build it, test it, if it's good, then you push it to Docker Hub. And if that is good, you can commit your source code. And once you have your source code ready... Right, so once you have your source code ready, after this it will be very simple. You fetch the source code wherever you want. You can build through docker-compose and run it. It's that simple, right? Clone the source code, docker-compose up hyphen d, and it is going to build the image and run the container. Okay, let's reiterate the steps that we did. We should know the steps to setup your stack on VMs, locally or remotely anywhere. We should know the steps to setup each and every service in your entire stack. Once you know that, you can find your right Base image from Docker Hub. You can write Dockerfile to customize your image. You can use docker-compose file to run multi containers and test it. Once you're done, you can host the image to Docker Hub. And that's exactly what we did. We cloned our source code. We written Dockerfile with the right data. We said docker build which fetch the Base image from Docker Hub. We used docker-compose to test it. When we find it is good, then we pushed it back to Docker Hub. So that was it. In this project, I hope you have learned a lot and gained some rich experience in containerizing your application. Thank you very much for watching. I'll see you in the next one. (upbeat music)
