# Web Image Dockerfile

Instructor: Okay, in this lecture, we are going to set up image for Nginx. We did the Nginx setup manually and it was pretty simple. We installed the service and then we put our configuration file. That was our configuration file that says if the request comes on port 80, then route the request on the backend on port 8080. So that is only the configurations you want to change in the Nginx image. So if we go to the official Docker image for Nginx, let's find that. Nginx. Okay, so the way here, they're injecting the configuration now. This is HTML file. Let's check how the configuration is changed. Okay, there. So you can run the container and you can, oh, here, yeah. So while you're running the container, you can upload your configuration as a volume. You know, you can connect the volume as a configuration or you can just copy the configuration directly into your image and then build the image. So there are two ways. You can either have an image with the configuration loaded in that, or you can take the default Nginx image from Docker hub. And when you run the container at that time in the runtime, you can inject the configuration. So there are always two ways. Now this is required when you regularly change the configuration. Our configuration is straightforward, just route the request to the Tomcat container and we don't have any other changes in that. So it's better we put the configuration and build the image directly and keep it in our repository. Alright, so like this, we can copy the configuration into this path. Okay, so in the Docker files folder web, we have the empty Docker file and we have this configuration. We hear configuration says it needs to be routed to the request, sorry, it needs to be routed to the container with the name vproapp. So we need to make sure the Tomcat container, when we run in the runtime, its name should be vproapp. That I will talk about when we actually run the containers. For now, all we'll do is we'll take the official Nginx image and we'll just copy this configuration in that. That's how we're going to build the Docker file. Okay, so from Nginx, I don't mention any tags. So it is going to take the latest. It's going to use the latest tag, and that is fine. I put the same two labels and then I need to remove the default configuration. So with run instruction, I'm removing the default configuration, run rmf and rf, and the path of the configuration. Again, this is in the documentation. And then I want to copy this configuration at this location. So with copy instruction, this configuration file will be copied at this location. So when I build this image, it is going to run the container. Remove this configuration file, copy your configuration file, and give us an image with this configuration in that. And that is all we need in this Nginx. Okay, now what are the remaining services? We have memcache and RabbitMQ. And really we don't need to make any changes. We can literally take the official Docker images of memcache and RabbitMQ and just use them. Why? I'll show you. So memcache, how did we set up memcache? We just install memcache. We started the service. That's all. So that can be done directly. Search for memcached. Take its official image. Check this. So memcache is used like that. We don't need to make any changes in the configuration for our project. So we don't need to write any special instruction in the Docker file. The next thing will be the data. So we, there is no data in the memcache, it just stores everything in the runtime. So you don't even need to copy the data. No configuration. So we can just take the image and let's click on this one of the tag over here that is going to show us the Docker file that is used to build this memcached official image. If we check this, you see there is exposed 11211 port. That's what we need. Memcache to runs or to run on port 11211. Alright there, it's mentioned in the documentation and also in application.properties file. Memcache 11211. That's all we need. So you don't need to write a separate Docker file for this one. We can just take the official image. Next is the RabbitMQ. So RabbitMQ is also similar except there are a few changes that can be managed. So we install RabbitMQ service, we start it, and then we create this user called test. And then we give this user an administrator tag. That's the only configuration change we want to make. Let's check the official image RabbitMQ. Okay, so the RabbitMQ official image, it says it's run on default port 5672/ Right, we need to have that 5672 and in our application.properties file. RabbitMQ username here it's given guest guest. So we can use that also, we don't need to manually use the test user based on the configuration. Our configuration says have the guest user. So let's see this documentation. So default user and password. So the RabbitMQ default user, you can mention the username. If you don't mention, it will be guest. See here. Guest and the password is also guest. But if you wish to change, then you can set the environment variable and change the username. But we are good with this. We can use the guest user. We have given that in the configuration file of the source code, the user as guest and password as guest. So we really don't need to make any change in the RabbitMQ. Also, we can directly take the official image. Okay, so we can see that we need an app image, Docker file, we need a DB image, Docker file, and a web image, Docker file. All this we have already created. The other two, we can use the official images. Now in the next lecture we are going to create a Docker compose file. We'll see a service Docker compose that can build these images and run containers together so we can test our application. Alright, so wrap this up then and join me in the next lecture.
