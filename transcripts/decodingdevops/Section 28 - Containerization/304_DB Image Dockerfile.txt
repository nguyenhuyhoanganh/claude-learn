# DB Image Dockerfile

Instructor: Okay, in this lecture we are going to build Docker file for DB for our MySQL service. So come to Docker hub and search for MySQL. That's the MySQL official image. Okay, let's scroll through this. So what all we can do with this Docker image, how can we run the container. -: e MYSQL_ROOT_PASSWORD, that's the password, that's how you can set the root password. Okay, MySQL custom configuration, if we want to upload our own MySQL configuration, then we can connect it through the volume -v option. We mentioned the path of our configuration and the destination will be this one. Environment variable. MYSQL_ROOT_PASSWORD you can set, you can set the database name also with MYSQL_DATABASE variable. You can set the user, you can set the password for that user, right? By setting up these variables, you can make changes in the Docker container, this MySQL container. There's also Docker Secrets. Initializing a fresh instance. So MySQL service will run, but if you want to inject your own schema, how you can do that, that's also mentioned over here. So I recommend you read this. What I'll say is when you start the container, it is going to execute any file that ends with .sh, .sql, or .sql.gz found in /docker-entrypoint-initdb.d. So if you put any file that ends with any of this extension in this folder of this container, it will execute it when it starts. So if you put our SQL file into that folder, when the container starts, it's going to execute those SQL queries and initialize our database, right? Now let's look at the setup, the MySQL setup how we did. Okay, so we installed the service, we start and enabled, that's covered in the Docker image. Setting up the DB name and users. That's how we do it, we run the SQL queries, but in the Docker image, we can set the variables, environment variable, right? Like MYSQL_ROOT_PASSWORD, MYSQL_USER, MYSQL_PASSWORD. Right? Like that. And initializing the database. So we cloned the source code and we have this db_backup.sql file which we run against the database named accounts. So we need to have a database named accounts, and we can run our SQL query on that database. How can we do in this Docker image? Well, we can do this. We can put this file that ends with .sql into this folder. So you see how important it is for you to understand your own setup and then read the documentation on the Docker hub. If you know both, it will be very easy for you to build any Docker file, any Docker image. So that's the most important point that I want to make in this project setup, in this containerization. So when you want to containerize any project, any application, you should know these two things as the information, and then follow the technical details, the instruction names in the Docker file, like from, label, environment, add, copy, stuff like that. Okay, let's start writing the DB Docker file. So I'm going to close this one. Expand Docker-files, db, Dockerfile. Now here I have also placed db_backup.sql file, so you can quickly copy it by using copy instruction. So first we mentioned the base image by using FROM instruction. The image is mysql and the tag is 8.0.33. Once again, in this project, I'm telling you what is the version to use. In real-time, you need to get this information from the developer. And you match that version with the Docker image tag from the Docker hub. Okay, I'm also putting label, not mandatory, but just so you know. Later also you can use these labels. There is really no use case for us for this vprofile-project, but we'll still use it so you can learn some extra instructions. So LABEL "Project" = the project name. Again, this is just like we use tags in AWS. I'll keep this also in the DB image, sorry, in the app image. So we put that in the app image also here. Okay, now we need to set the MySQL root password. We need to set the database with name accounts, and then we need to copy our SQL file. So to set the variable, you have the instruction ENV. So ENV MYSQL_ROOT_PASSWORD, all in caps. This is just as it's shown in the documentation. I'm setting a password. MYSQL_DATABASE, the database name is accounts. Now, how do I know this information, the database name is accounts? Because I have already set this project locally, I got this information from the developers. And also there will be configuration files in your source code that can tell you that. So let me collapse these things. So in the src we have main, we have resources, and application.properties file. We've seen this file many times. But in the containers branch we have different names over here. The username is root, not admin, and the password is vprodbpass. And the database name is same, accounts. We are going to build the source code from this. This application properties file will be in the artifact. So this information really matters a lot. So if you wish to change that in the Docker file, you need to change it over here. Or if you change it here, then you need to change it in the Docker file. All this is very much required to have a functional application. Okay, next we are going to copy this db_backup.sql file into the container. We can do that with the ADD or COPY instruction. We're going to do it with ADD instruction. So it takes this SQL file and it put in this location. Now, why this location? It's clearly mentioned in the documentation. Any file that ends with .sql or .sh will be executed if you put it in this folder. So that's why we are doing that. So when we run the container from this image, it's going to copy this SQL file, and that should initialize the database. This SQL file contains the SQL queries that will get executed and which will create the schema, right? Make sure to save this file. And that is all. Now, you can add here expose 3306 and other MySQL commands, it's really not necessary because that's all we need. MySQL image will always under MySQL service on port 3306. It's there in the documentation. All right, so that will be all in this lecture. In the next lecture, we are going to build the NGINX web image. See you in the next one.
