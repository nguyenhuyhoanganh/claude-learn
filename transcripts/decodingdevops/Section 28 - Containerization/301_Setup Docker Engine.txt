# Setup Docker Engine

Instructor: Alright, in this lecture we are going to set up a Docker engine on a virtual machine. We're going to set up a VM with Vagrant. The Vagrant file is in the source code. We are going to clone the source code, the same source code we will be using in this project. So come to github.com/hkhcoder/vprofile-project, and copy the URL HTTPS link. Copy it. Open your VS code, click on the symbol over here, source control, and click on Clone Repository, and paste the URL, and click on that URL. Select your folder. I'm selecting F Drive. I've created a folder, Containerization, and say Select Repository Destination. Okay, so now we are going to switch to the branch. So click on this branch symbol over here, and select containers branch, select the containers. In this branch, you should see Docker files folder, you should see Vagrant folder. So based on your operating system, we are going to go to that particular folder and bring up the VM. We are going to do it through terminal, or Git Bash. So I'm going to open my Git Bash. Mac OS, you can open your terminal. Go to the folder where you have cloned the source code, and this you should find V profile project. Get inside that. And here, you should see Vagrant folder. Now if you're using Windows or Mac OS with Intel Chip, you go to this folder. If you're using Mac OS with M one, M two, M three chip, go to Mac OS ARM folder. So I will get into Windows folder. Let me clear the screen. So there is the Vagrant file, but before we bring up, let's check if we have any other VMs already running. We are going to shut them down, or delete them. Vagrant Space Global hyphen status. So I have few VMs running. I'm going to shut them down. It's already power off, I'm going to destroy them, and this one I'm going to power off. So I go to that folder, Python scripts, and I'm going to destroy this. Vagrant destroy. It's deleted. I'm going to the Ubuntu to folder, power off this VM. Okay, now let me go back to the folder. D, sorry, F, slash containerization. Go to V profile project Vagrant. And this we have Windows, Mac OS Intel. We have the Vagrant file, and let's bring up the VM Vagrant app. Okay, VM is up, let me log into it. Vagrant SSH. Okay, now let's get the step of installation. Go to the website docs.docker.com. Or you can just Google "Docker documentation". Go to manuals. Where you see Docker engine, click on that dropdown, and click on Install. Keep in mind, we have to install Docker engine, not Docker desktop. We have Ubuntu OS, so click on Ubuntu. Scroll down. Installation methods. Now these are the commands first we need to execute. So execute them one by one, first starting with apt update. I'm going to switch to root user first. So hyphen I, clear the screen, sudo apt update. Copy and paste the next command, which install CA certificates and Curl. Put a hyphen Y at the end. Hit Enter. Copy these three commands together, and paste it over here. Hit Enter. Okay, so we're in apt update, we install CA certificates and Curl. We set up the GPG key for Docker repository, now we are going to add information off the Docker repository. So copy this command carefully from a (indistinct) dev null, paste it here, hit Enter. Now we need to run apt update so it scans the latest repository of Docker. Okay, now it's time to install the Docker engine. So this is the command which installs Docker engines and the other dependencies, so let's copy this command, paste it, and make sure to hit hyphen Y at the end, and hit Enter. Okay, Docker engine is installed. Let's check the status system. CTL status Docker, and you should see it active running. Hit Q to quit. Now we are going to run one more command, which will add the Vagrant user into Docker group. User mode, hyphen small A, capital G, group name Docker, and the username Vagrant. Now what happens is, when you install Docker engine, you can run Docker command with root user, but normal user cannot run it. I'll show you. If you run Docker images, you can run it. Now we did add the Vagrant user into Docker group. You can see that, it's in the Docker group, but then this user need to log out and log in. Now, if I just exit, I go to Vagrant user, and if I run Docker images command, it will give me permission denied. This user needs to load its profile again, so for that, we can just log out from this user and log in again. Now the Vagrant user can run the Docker command. See that? If you're running it on EC two instance, then you can add the open to user into the Docker group. Once you are done, you can shut down the VM, exit from it, and do Vagrant halt. We don't need it now, we'll need it later when we write all our Docker files, and we build our Docker images. So at the time of building the Docker image, we will need this. That time we'll bring up this VM, and build our Docker images. So shut down the VM, and then join me in the next lecture.
