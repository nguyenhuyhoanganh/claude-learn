# Build and Run

Instructor: Okay, so we have everything ready now. We have the Docker Compose file where the Docker files path are mentioned and how to run the container from that image that is also mentioned. So we have all these three Docker files and the Docker Compose file. Now it is time to build these images and run the containers together from this by using Docker Compose. So you need to come to the folder where we have created the VM and installed Docker Engine. So mine is in F drive. So open Git Bash or terminal and come to the folder where we had created previously the Vagrant VM and installed Docker Engine. Mine is in this location, f/containerization/vprofile-project/vagrant Windows folder. So I did power off the VM. I'm going to power on this VM now. Vagrant up. Or you can first check the status also. If you see this running, you do vagrant reload. So my VM is powered off. I'm going to power it on vagrant up. Okay, my VM is up. I log into this, vagrant ssh. Okay, let me clear the screen and I'm going to switch to root user. It's not necessary. We can run Docker command with vagrant user also. We add vagrant user in the Docker group, cd/vagrant. You go to this folder so you can see. So we have the Vagrant file in this folder. We need the Docker Compose file in all the Docker files in this location. So what we are going to do is we come to the VS code, right click on the Docker files folder, see copy and get into the vagrant folder. And make sure you go to the folder based on your OS, the folder that you selected to create Vagrant file. Mine was in this location and paste it over here. And also I'm going to take the Docker Compose file. I'm going to paste it over there, this location. So we need the Docker files and Docker Compose file in the same location. Now if you're on ls, you should see this over here. Now generally the Vagrant file should be in the root directory here itself. But since we have people who use different computers, different operating systems, so we had to create this folder structure for Vagrant. So if you have Vagrant file in the same location, you brought up the VM in this location. So you should automatically see all this in your Vagrant VM. Okay, so here we see the Docker Compose file. We need to run the command Docker Compose build. This command is going to read your Docker Compose file, find all the images that requires the build, and going to run the Docker build command to build the images. And this is going to take some time. So I'm going to pause the recording and resume when this is completed. Okay, so the build is completed. It took some time, but it got completed. Now we should see the Docker images. So if you run the command Docker images, we should see these three images. Right, so we have app image, we have DB image, we have the web image. You can see its size also. So we have not mentioned any tag. So it is giving the tag as latest. Now in the Compose file we have also written how to run this container. So we have to run this command now to check whether it works or not. So docker compose up -d Now first it will check these images are available or not. If these images are not available, then it is going to run Docker Compose build. We already ran build command, so we already have the images. Now it is going to see the information on how to run the containers together from Docker Compose file. So when we hit Enter, it's going to start running the container. It will also pull two images for memcache and RabbitMQ. Now memcache and RabbitMQ image. Those are official images that we are taking. We're not building it. So it's going to pull those images. When it created a network, it created these two volumes that we mentioned. The Compose file and then it created all our containers. And notice the name also vprodb, vproapp, vproweb. And these two containers, I have a different name, I need to make sure it has this name vprocache01 and vpromq01. So I'm going to edit the Docker Compose file. So here we have given the name to vprodb container by using container_name instruction. Here also, we can use the same. I'm going to paste this over here, container_name: vpromq01. And also for caching. Yeah, there. Vprocache01. Okay, save and quit the file and we'll delete everything. I mean just the container, not the images. We're going to see Docker Compose down. That will stop the container and also remove the container. Then we will again run docker compose up -d and that's the correct name. Now let's access this, our application, right? We need to access the Nginx, the web image, the web, sorry, the container, web container. We need to access that. So if you run a docker ps, we should see here the web container. Okay, so the container port is 80 and the host port is 80. So if we access host this VM on port 80, then it is going to route the request to this container on port 80. And then that is Nginx. We already have a configuration in that, that then route the request to the Tomcat. The vproapp and vpro app is going to access the backend services with the application properties file information where it has information of vprodb, vprocache01 and vpromq01. Alright, and this application.properties file, you have the information. So let's get the IP address of this VM. So run the command ip addr show. Okay, 192.168.56.16-enp0s8. That's the IP we have given in the Vagrant file. So this is the IP, let's copy that. Okay, so in the browser we are going to say http://192.168.56.16:80. Now, even if we don't give port 80, that is a default port for http, but I just wanted to make a point over here that when you access the host machine on port 80 there, when you run Docker PS command, you can see the container port of Nginx is port 80 and the host port is 80. So when you access the host on port 80, it is going to route the request to container, the Nginx container on port 80. And Nginx container has the configuration that routes the request to the Tomcat container. So let's see that. We just hit Enter. We should see the webpage. This is coming from Tomcat. Now when we log in, admin_vp, password, admin_vp. This information is stored in the database. If you're able to log in, then it is able to also connect to the DB container in it. Thus it's able to log in. Alright, so we can check RabbitMQ. Click on RabbitMQ. You see RabbitMQ initiated, and we can check memcache by clicking on all users and pick up any user. Data is from DB and data is inserted in cache. So memcache is also working if you click on that user once again. You should see the data is coming from the cache. So all our services are verified. Now it's time to host our images in our Docker hub account. So let's clear the screen. Docker images. So you should have given the name of your image. Like this first should be your account name and then the image name. So mine is vpro containers. So in order to upload those images to our Docker hub account, first we need to log into Docker hub. So we have to say Docker login, and it is going to ask the credentials. Okay, when you execute the command docker login, you should get this. To get this URL, you can do a browser based login. You can just copy this URL, go to your browser, and then it is going to ask you a confirmation code. And this will be the confirmation code that you have to enter. So let's go. So enter one time code. So that is my one time code. Confirm. Okay, now let's check from the command line. Says waiting for authentication in browser and says login succeeded now. Okay, now all you need to do is you have to say docker push, and you give the image name one by one. So I'll first upload this one. First vprofile app. So like that one by one, upload all the three images. Okay, so I've pushed all the three images. You also do the same thing. You can use these images later in the later lectures and Kubernetes we're going to use these images. And once you are done with that, you can do the cleanup starting with the Docker containers. So let's go to the folder where we created the container with Compose. So /vagrant, now you get Compose file. So Docker Compose down. This command should stop the containers and remove the container all at once. And you should also have the volume docker, volume ls if you do. You should see these volumes. So let's remove the volume docker volume rm. Give both the volume names. You can also run the command docker volume prune to remove any unused volume, there is one unused volume. Okay, that is done. Or if you want to run every, if you want to remove everything altogether, you can say docker system prune -a. Yes. So this will stop the container, remove them, remove the images and all the other cache. That's a cleanup. Now, as long as we have the Docker Compose file and all the Docker files, we can anytime build and run these containers. And if you're done with this, you can really exit and stop this VM or even destroy this VM because we have everything in the code. We have the Vagrant file, we can bring up the VM, then we can run the Docker Compose file to build the images and run the containers. So everything we have as a code. So complete the cleanup and bring down the VM. Vagrant halt. And then join me in the next lecture.
