# Overview of Base Image

Instructor: So let's say in the real world, you got a task of containerizing a project. What will be your first step? Well, technically, the first step will be to start writing the Dockerfile, but in order to start writing the Dockerfile, you need to know the services, tools, softwares used in that application, in that project, and their version. That's the first thing you need to note down. What are the different services or tools used? So in our vprofile application, we have MySQL, Tomcat, Maven, JDK, so you need to prepare the list of all the tools and services that are used in the project, and the most important part is their versions, what versions are used. You will get all this information from the developers who create or write the application code. Okay, let's look at the source code and the project set up, what we did earlier. We did set up vprofile-project manually. We have gone through all the steps of installing every service, so go to github.com/hkhcoder/vprofile/project and switch to the branch local here, local branch. Go to Vagrant, manual provisioning, vprofile project setup, this PDF. You can download it in your local machine. If you're not able to download it, you can download it from the resource section, but anyways, you did all this project. The reason why I'm showing you this is, whenever you get into any project, you need to containerize it, you need to first get all the services and their versions. So we are here provisioning nginx, Tomcat, RabbitMQ, Memcache, MySQL, Elasticsearch, that we skipped, and in Tomcat, if you remember, we built the artifact by using JDK and Maven, so if you need little recalling, then I recommend you pause the lecture and go through this PDF, this document, go through all the steps that we executed in order to set up a service, because that is very important. In real time, also, you need to go through the entire setup versions, what configuration changes you made, everything you need to know so you can build a proper Dockerfile that will give you a Docker image. So at the end, what we will be doing is we will have a Docker image for MySQL, Memcache, RabbitMQ, Tomcat, nginx, and there are ready-made images, official Docker images, on Docker Hub. So you can come to Docker Hub, and you can search for your image, like for example, MySQL. So you can take this Docker image, but you will need some customization, like we need to inject our own SQL file into that. For example, we have Tomcat, we need to inject our artifact, nginx, we need to inject some configuration, so the first step is, get to know the services and their setup, and most important, their versions. So make a list of the services. So we have five services, MySQL, Memcache, RabbitMQ, Tomcat, and nginx. Also, remember the tools that we need to use, like in the Tomcat, or before building a Tomcat image, we need to upload our artifact in the Tomcat, and how do we build the artifact? We build it by using Maven, and Maven needs JDK, so let's write that down as well. So this is the list for our vprofile project. Now, in your real project, you may have a longer list or a smaller list, but all the tools and services, make a list of it and jot down the versions. So here are all the versions of the tools and services that we're gonna use in the vprofile project or we used. Now, we are going to use the same version for the Docker images. Let me show you. So on Docker Hub, I search for MySQL. I go to the MySQL Docker image. This is the official Docker image, and a Docker image will have multiple versions. These versions are called as tags in Docker. Tags, you see that? Click on that. So you need to know which tag of the image to use, and that you will know based on the version of your service. So MySQL, we want to use 8.0.33, so let's find if we have that, or you can get a closer one if you don't have that one, but we do have this. You see there are different tags, 9-oraclelinux9, 9oracle, nine, and there comes 8.4.3. I go to the next page. Okay, somewhere after three, four pages, I found this, 8.0.33. So based on the version of your services and tools, you need to find the tag for that official Docker image. Now, I recommend you take note of this and find all this images on Docker Hub with their versions. They might be with different names, but anyways, I found all of them, and I have the tags also, and that will be the next step that I'm going to write over here in this file now, the Docker image name and their tag. Okay, so after scanning through Docker Hub and matching the version, I come up with these tags, so MySQL 8.0.33. there is a MySQL image with this tag. Memcache latest is fine. That matches with 1.6, so Memcache 1.6 version is available with the latest tag. Rabbit MQ 4.0 is available with the latest tag. Maven, this image we'll use to build our artifact. Our vprofile source code will take and build the artifact. For that, we'll need the Maven image, and we need 3.9.9 with JDK 21. So I found this tag, 3.9.9-eclipse-temurin-21-jammy. I know, long tag name, but I only tested it, so I know this is going to work, but when you get a real task, then you need to test different tags. You need to take the closest one and see which one works. You need to try different tags. Tomcat 10 with JDK 21. So we know Tomcat runs on Java, and we need Java 21, and we need Tomcat version 10, so I found a tag that has the tag 10-jdk21. That gives me JDK 21 with Tomcat version 10, and nginx 1.27 matches with the latest tag, so I'll just use the latest tag. So this will be the first step when you containerize any project. Okay, now, coming back to the first task that you need to do, your assignment for this lecture is, go through this document, vprofile project setup, manually. If you did not do this project previously, you should do it on virtual machines. Then only you'll understand the setup. If you need a little recalling, then go through all the steps, the configuration, every step. What do we do with MySQL? We run this command, we create user, we grant privileges, all these steps. Same for Memcache, RabbitMQ, Tomcat, go through all the steps. So that's your first assignment or task for this lecture. The second assignment or task for this lecture is, go to hub.docker.com, go through all the images you have taken list, starting with MySQL, and you are going to read this documentation. These two things are very important. You should know the steps to set up your project, and then how you can make the changes in the Docker image. That you'll get to know through this documentation on Docker Hub, like in MySQL, how do you run a Docker MySQL image? This is the variable you need to export, and that is MySQL root password, so you will know how to set the root password for MySQL, how you can get logs, that's a common command anyways, how you can set the configuration, hyphen V, that is volume, uploading your configuration into this Docker container at this folder. So like this, go through all the Docker images from the list and look at their settings. This is very important, then only you'll be able to containerize in real time. When we write the Dockerfile in upcoming lectures, we'll go through the setup of our services, and then we will see how we can make the changes in the Dockerfile by using all the information that is provided by Docker Hub. So that will be all in this lecture. I will see you in the next one.
