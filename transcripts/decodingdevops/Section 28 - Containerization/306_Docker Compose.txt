# Docker Compose

Instructor: Okay, so we have now three Dockerfile. One is for the app, one is for the db, and one is for the web image. Now, we need to build these three images and run containers from them. Also, we need two more containers, Memcache and RabbitMQ. So, total, we need to run five containers together. Three, we need to build from the image and then run the container. And two, we are going to fetch it from the Docker Hub and run it. So, total of five containers we need to run together, so it's a multi-container environment. And to run multi-containers together, we have a concept of docker-compose. So, let's check the documentation. It says a tool for defining and running multi-container applications. Now, you already know Vagrant. If you need to run multiple VMs together, we use Vagrant. Similar way, if you need to run multiple containers together, you can use docker-compose. Like in Vagrant, there is a Vagrant file. In docker-compose, there is a docker-compose.yml file. It will be in YAML. So, it's easy to read and write. So, you mentioned all the information in the docker-compose file, and then you can start it, run it, stop it, rerun it. So, it's just perfect for the development environment. So, let's take a quick look at the documentation, and then we'll write our own compose file and run it. So, let's click on Introduction to Compose, How Compose works. So, there is a compose.yml file. So, the current name is compose.yml and this is the preferred one, but you can also use docker-compose.yml, which was the previous one. So, we'll rename our docker-compose file to compose.yml or yaml both works. And once we write that file, we can do docker compose up, docker compose down, docker compose logs, ps. There are many other commands that will be seeing when we run it. So, let's take an example. Yeah, this is the sample compose file. So, you say services, services means containers. Then, you give the name to your container. And in that, you mentioned the image that you want to run the container from. You can also mention the Dockerfile path to build it that we'll see in a moment. And then, you have ports which port you need to bind. This will be the host port and the container port. And like that, you can give many other settings or configurations. And this is one container and this is another container, backend. So, this compose file has two containers, one is frontend, the other one is backend. Both there's image mentioned. Here, there's also volumes mentioned. You can also map volumes. You can also manage volumes from the docker-compose file. You have to say volumes. You need to give the name of the volume and you can mention how you want to map the volume. And you can create a separate network or any secret that you want to inject, or any configuration that you want to inject. Okay, let's check Quickstart. So, in this Quickstart guide, you have example of a Python application where you write the Dockerfile, and then you write the docker-compose file to run all these things together. So, creating a folder, get into that folder, create app.py. You don't need to do it. I'm just showing you, because we are going to run our own application, the docker-compose. So, there is this Python applications, a sample application, and then there is a Dockerfile written for it, which shows the information of how to build this image for the Python application. And then, there is a compose file. It's pretty simple. Services, container name, web, build, dot. That means it's going to look for the Dockerfile in the current working directory. Ports. And then, there is another container which is just fetched and it will just run the container from this image basically. So, two containers, web and redis. One is getting built from Dockerfile. The other one, you're just mentioning in the image to fetch and run. And then, you simply see docker-compose up and it is going to create a network and run your containers in that based on the information, the docker-compose file. Okay. If you come down here, this more changes that is done in the same compose file. So, you can make the changes, and then you can rerun it, you can add more options as in when you need it. So, just perfect for the development environment when developers are actually trying to containerize an application or it's your job to containerize a application, which is written by the developers. So, let's create a list of what we need to do actually to write the docker-compose file. What are the containers that we need to mention, the port numbers, everything we'll mention, and then we'll start writing the docker-compose file. Okay, so we need to run total five containers. Okay. So, one is the app, we have db, we have web, and then we have Memcache, and then we have this RabbitMQ. So, for three, app, db, and web, we have the Dockerfile that we are going to build. And for Memcache and RabbitMQ, we have the image from the Docker Hub. Okay, let's also mention the port, the host port, and the container port. Just restructure it, db first. So, let's mention the ports. So, for db, we have Dockerfile, and the port, host port, we'll keep 3306, and the container port is anyways, 3306. We also need to mention the password. There is a variable MYSQL_ROOT_PASSWORD that we definitely need to mention, otherwise we will not be able to run that container. Also, along with that, we'll add a volume to it. We will map a host volume to /var/lib/mysql. That's where all the data is stored, /var/lib/mysql. Okay. So, we need to be very well-aware about what information we need to mention about a container. Now, here, we are dealing with the backend container, the database, so our application, V profile application, which will be the app image. We'll have a application.properties file where the backend information is mentioned. We need to make sure our container information matches with that. All right, so let's go to the source code, src, main, resources, application.properties file. Okay, so for database, we'll be source with the name vprodb. So, we need to make sure a container name is vprodb, because that's how its name will be resolved into an IP address. Port 3306. Database named accounts that is taken care in the Dockerfile. username=root and the password is vprodbpass. So, we need to make sure container has all this information matching. So, we need to put here one more information will be the container name and that needs to be vprodb. Same way I'm going to write all the information about all these containers. Okay, next is Memcache. So, the container name as per the application properties file is vprocache01 and the port is 11211. That's all. Only these two. So, that's the same information. Memcache. Image name we'll mention, that will be from Docker Hub, because we just need the Memcache service running. We don't need any customization in that. ContainerName, vprocache01. And port 11211. So, we keeping host port and container port same, because we really don't have any necessity to change the host port. So, we can keep the same as the container port. Okay, RabbitMQ, container name, vpromq01. Port 5672. Username, guest. Password, guest. So, we also need to add two variables there. Guest and guest for username and password. So, let's put all that information. So, RabbitMQ, again, we can fetch it from the Docker Hub and run it, because we need the service and we just need to add two variables, which is possible. We've seen it in the Docker Hub. For RabbitMQ image, we can mention these two variables for the username and password. We'll see how to do that when we write the docker-compose file. Okay, so that was about three backend services, but now we have the application service. And application service, the app image, we need to run a container from that. So, if we return a Dockerfile for that, now our web container is going to forward the request to the app container. So, right, if you look at the Dockerfile of web, we have a configuration file. The nginx configuration file, it's going to forward the request to the container named vproapp running on port 8080. So, we need to fill this information also. So, app, we're going to build from the Dockerfile. The container name needs to be vproapp and its port needs to be 8080. And I'm adding an extra volume. This is not a necessity, but just to show you one extra option, that's all. Then, we have the web container frontend. Again, we are building it from a Dockerfile. It needs to have a name. Now, this can have any name because it's a frontend one, but we are giving it a name vproweb and the port is obviously 80. The nginx service runs on port 80. So, we are forwarding the request on port 80 or listening on port 80. Now, I have written all this information. In real-time also, when you're writing a docker-compose file like this, you need to have all the information handy, so then it becomes easy for you to write the docker-compose file. This call is requirement gathering. So, we gathered all the requirement. Now, it will become very easy for us to write the compose file. Okay. So, first, we are going to rename this file to compose.yaml. So, let's right click and let's rename. Let's remove the docker- from the beginning, and let's give yaml, that is preferred, but yml is also fine. Okay. So, the first line will be services. That means the containers that we want to run, services with an S. And also, we are mapping volumes. So, we are going to give volumes. So, I already mentioned two volumes that we are going to map. One is going to be with the db, the other one will be with the app. So, give three space and I'm going to give the name of the volume as vprodbdata for the first one. And I'm going to give this curly brace. That means this is going to be the host volume. That means it's going to just create a volume, a folder into your Docker Engine. And since this is just for testing, now, development purpose, so a host volume here is also fine. In Kubernetes, we'll see how we can manage volumes externally. Then, vproappdata. So, these are two volumes that we'll be mapping with our container. So, let's go into services now. I'm going to give here three space and name my first container, which will be vprodb:, and then inside this, I'm going to mention information about the vprodb container. So, I'm going to give here again three space. So, we are building it from the image, I'm sorry, building it from the Dockerfile. So, we need to give here build: and we need to give the path, because it's not in the current working directory, it's in Docker-files, app, over here. So, we need to give the path. So, to give the path, again, next line, three space, and you have to say context. And then, you need to give the path of the Dockerfile. So, I'm going to say ./Docker-file, the folder name, Docker-files/app. I'm sorry, this is for db, so /db. So, in this folder, it is going to look for a file named Dockerfile. That's going to build it. So, with what name it should build? What should be the image name? So, we're going to mention the image name, image:. So, I'm going to give the name of the account that I created in the Docker Hub that is vprocontainers. 'Cause when I upload it on Docker Hub, then by mentioning this name, it will be uploaded. So, it should be account name/the image name. So, it should be vprofiledb. Okay. Good. So, that was the name of the image. Now, the name of the container. Yes, I said this is the name of the container, but then when you run the container with docker-compose, it's going to give, add some prefix and suffix with that vprodb. So, we need to make sure the container name is absolutely same as mentioned in the application.properties file. So, for that, we can do this, container_name: vprodb. Remember that. We have already created or gathered this information. ContainerName, vprodb. Then, the port, let's go for the port. So, ports are mentioned like this. You say ports:, and in the next line, give space, hyphen, space. And in double quote, you mentioned the host port and the container port. Okay, we also talked about the volume. So, let's also mention the volume, so that you can do by using volumes. So, again, next line, hyphen, space, and the host volume name, which is the vprodbdata that we have given over here. And the container volume name. Now, there is no space here, so /var/lib/mysql. Make sure there is no space between this host volume name and the container volume name. Okay, so the next thing is we need to mention the MYSQL_ROOT_PASSWORD. So, that's a variable. We need to mention this variable value, otherwise the db container, the mysql container does not run. So, we need to mention this variable. And that you can do by using environment, environment: and same format, space, hyphen, the variable name equal to its value, the equal to symbol. So, that's the variable and vprodbpass. This value, we need to give, because that's what is in the application.properties file. Remember? vprodbpass. Okay, so next, we have the Memcache. That will be easy. So, I'm going to simply copy-paste this, and then remove things that we don't need, and change stuff. Just going to copy this and make sure it's in the same line here. Okay, this and this should be in the same line. So, let's change the name. This will be the name vprocache01 and there is no build image over here. We just have the image name from the Docker Hub and that is memcached. And so, D. And the container name, vprocache01. Port 11211. And that is all. We don't need volumes and we don't need anything for this. We just need to have this container with running with this port. That's all. Okay, great. Now, next comes the RabbitMQ. Okay, so RabbitMQ. Also, we are going to use the official image. The name is RabbitMQ. So, requirement here is we need to run with the image, the container name, port number, all this we can mention. We are just checking how to mention this username and password variables. So, let's come to RabbitMQ official documentation from Docker Hub. And if we come down, scroll down, so it's going to mention how to run the containers manually and environment variables. Okay, Setting default user and password. So, there is this variable, RABBITMQ_DEFAULT_USER and RABBITMQ_DEFAULT_PASSWORD. So, that's how you can mention. So, same in the docker-compose file, we need to mention the environment and we need to mention the variable and its value. Okay, so in the same line with this container names, we are going to mention the RabbitMQ container name, colon, and its image name. So, we're running official, so image:, the official image name, which it will fetch. Then, the port number supports 5672, mapped with 5672. And the environment variable for username and password. So, environment:, RABBITMQ_DEFAULT_USER=guest, RABBITMQ_DEFAULT_PASS=guest. Again, this is based on the application.properties file. See that name, port, username, password. Okay, next is our Tomcat app image. So, we know Dockerfile, we can mention container name, we know how to do port number and also the volumes. So, let's write that in the docker-compose file. So, let's take example of vprodb to copy this, and then just paste it over here. I have to just make sure it's in the same line, and then I'll change the name. This one will be vproapp. Build path, Docker-files/app. Here, we have our Dockerfile for app image. And here, the container name, vproapp. And the container name vproapp again. Port 8080 mapped with 8080. You see how easy it is if you know how to write one container information, then the other becomes just easy, it's just mostly copy-paste work. Volumes, so volume name will be vproappdata. And the path is /usr/local/tomcat/webapps. We don't need any environment variable for this one. So, again, remove this one. Just make sure everything is correct. You have app, because when you do copy-paste, you tend to make mistake of having the previous things. So, just check that, make sure everything is correct. So, likewise, I'm also going to add the information of vproweb. So, I'm going to copy the same one, vproapp, and then paste it. Let's change the name here to web, the part of the Dockerfile to web. That's the folder name and image name, also web. Container name, now this, we can't give anything else, because not forwarding request is not receiving request from anyone except the user. But we'll keep it as web and the port is 80, mapped with the container port 80. No volume required whatsoever. Okay, make sure the syntax is correct. Make sure everything is in the same line, especially the container names. So, vproweb, I'm just doing an uo arrow, vproapp, same line, go up, vpromq in the same line, vprocache in the same line. Yeah. Okay. Good. Great. So, just check once again, all the information is correct, it matches with application.properties file. And the information that we gathered, everything is correct. If you are sure, do Ctrl + S, and also download this from the resource section, this compose file and match it with your compose file, so there are no mistakes when you're running it. Okay. So, in the next lecture, we'll see how to build these images and run them together, all the five containers together. So, make sure a compose file is right and if everything is good, save everything and join me in the next lecture.
