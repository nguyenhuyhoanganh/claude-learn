# Dockerhub & Dockerfile References

Instructor: Dockerfile reference documentation from Docker Hub. If you don't have hands-on building a Docker image, then you can check it from here how to build a Docker image. You need to have a Dockerfile. Dockerfile, D, capital there. And once you have that Dockerfile, you're given the information about the image, you can run the command docker build -t, your account name, and slash, the image name. Account name will be, this is your account name, right? But when you have organization, then you can create the organization with your name. That will be the account name. But since we just have the account, we don't have any organization, we are going to go to the repositories. See that? That will be considered as our account name. And we are going to create repository over here, that will represent the image. So click on Create repository. We have three images. So let's start with the app. We'll name it as vprofileapp. And we'll keep it public, because private is not free, one is free. But then we need three images. And when you're working in projects, these images will be private mostly, but we are keeping it public, because we want it free. So click on Create. Yeah, let's again click on Repositories. Let's create the next repository, or the image, vprofiledb. And one more, Create repository, vprofileweb, okay? Now once again, in real time, you will have organization. In organization, you're going to create repositories. So here instead of the account name, you'll have organization name, that's one thing. The second thing is the image will not be public, it will be private. So we need three Dockerfiles to build our image and then we are gonna host it. After building it, we are going to push it to Docker Hub, like this, we can build, we can get that output, and then we can push it. So if you don't have hands-on Dockerfile, you can check this documentation. The few instruction, common instruction that we'll be using, FROM instruction in the Dockerfile. The Dockerfile is series of instruction. FROM instruction, we give the base image. We already have figured out our base images, Tomcat, MySQL, and Nginx. We just need to figure out the right tag. So we can mention the base image. Right, this is a simple Dockerfile, FROM, the base image. COPY, copy your file from your machine to the container's directory. RUN is going to execute this command. This is Windows command. We have Linux containers. We're gonna build Linux images. So obviously we'll be using Linux commands. These are the instruction, ADD, COPY, ENV, EXPOSE, FROM, LABEL, STOPSIGNAL, USER, VOLUME, WORKDIR. We'll be using some of them, or most of them. So you can see all the instruction with examples in great detail over here. FROM, CMD. RUN, this is about RUN, the command that we want to execute to build our image in the container. CMD to mention the binaries that we want to run, in order to run the process in the container. One example there, here's one example. LABEL, which are like tags that we have seen in AWS. Key value pairs. MAINTAINER is deprecated. So if you are already using it, stop using it. EXPOSE, when we are running our binary, let's say we're running Tomcat, CATALINA search run. So it's gonna run the service, bind the process on port 8080. So we have to then say EXPOSE 8080. See EXPOSE is used for that. ENV, if you want to set any environment variable, we'll try that. We don't need any environment variable, but we'll just set some environment variable. ADD, if you want to push any file into your container. Like, in this example, any file that start with home will be pushed into the container directory at this location. ADD, and there's also COPY, which are similar, but ADD has more options. ADD can also extract your zip file. COPY, which is similar to ADD. Okay, COPY and ADD will be very useful when you want to put your own customized data while building the image. You want to push a configuration file into your container while you're building the image, or you want to push your artifact, that we are going to do. We have ENTRYPOINT, which will have higher priority than command, where we can mention a command. And with command we can pass the arguments. We'll see that. So for example, this, ENTRYPOINT. When you run a container built from this Dockerfile, as soon as you run the container, it's gonna run the command, top -b, space, -c. So command, whatever you mentioned in CMD, will come as an argument over here, which if you want, you can override it. Like, as in this example, docker run, the name of the image, top -h. These are some samples. Again, if you don't have hands-on experience on Dockerfile, I recommend you try few of them. Like for example, this one, which is very popular. So this is going to fetch an image, Debian, install Apache 2 on it, expose it on port 80 and 443. A volume will be exposed, or multiple volumes, /var/www/, /var/log/apache2, /etc/apache2. So you'll have three volumes which you can map with your host machine. ENTRYPOINT, so as soon as you run the container, it's going to run this command, apachectl -D FOREGROUND. This is a very popular example to practice, one, because this is very easy, and also you would have already used Apache to your httpd. So it's easy to understand this. So you can build a Dockerfile with this, build a container, and test it.
