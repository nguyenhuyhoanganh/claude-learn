# App Image Dockerfile

Instructor: Okay, so now we have all the stages set. Now we can start writing the Docker files. So open your VS Code and make sure you are in the source code that we clone and you are in the branch containers. In this we have a folder structure, Docker-files, if you see. So first we are going to build the app image, the Tomcat image on which our artifact, our V profile application will be hosted. So we have a app folder here and a Docker file. Now there's nothing in this Docker file. This Docker file is empty. I have just created the skeleton over here like DB, web. So in real time you need to create a skeleton like this. And in that, you need to place the Docker file and you have to make sure the Docker file starts with a capital D. Alright, so initially we created this list of the service, the version, and based on that, we found the image and the tag. Now, in order to build the Tomcat image, the app image, we need to give Tomcat a base image. And in that we need to upload our artifact. To build our artifact, we will need Maven. So we profile project setup documentation. We set up first the Docker, sorry, first we set up the Tomcat service in that now we have official Tomcat image we can just search for and go to Docker hub can search for Tomcat. There's the Tomcat image. So we don't need to build this image from the scratch, right? So we run this Tomcat container from this image and it runs the Tomcat service. The service runs on port 8080 and then we can map it with any other port we want. We'll be mapping it with 8080 only. That we'll see in a moment. Catalina base. So this is the home directory where the Tomcat binaries will be located in this location in the Docker image. So it's a pretty simple Docker image. We have Tomcat service running in that. That's all. Now we need to put our artifact in that, right? So in the setup we set up the Tomcat service. Once we have our Tomcat service, then we build the artifact. So here we set up the Maven 3.9.9, you see there. We clone the source code, we make the changes to application properties file, and we run MVN install. Now that gives us the artifact and then we copy this artifact into the home directory of Tomcat in web apps folder. Okay? These kind of information is very important. Check over here, we are putting it in Tomcat home directory in web apps folder with the name root.war. And we check the Tomcat Docker image documentation. You see there is USR, local Tomcat in that we can have web apps in that we can have the artifact with the name root.war. Okay, so this is covered now, right? We need to build the artifact and put it into the Tomcat image. So let's copy these two. This is the Docker image that you want to use. This is the Maven image. That's the Tomcat image. Let's go to our source code and put it over there. So this will be the base image. How do you mention the base image? You give the instruction from capital, right? And this one also from, okay, now this will be a multi-stage Docker file. A multi-stage Docker file is most of the time used to build the artifact and put it into a target image. In this case, it'll be a Tomcat image on this, the artifact will be copied. I will show you that in a moment. So of course the from instruction. Now here we need to put the instruction to build. So we need the source code over here and then we can build with, with Maven con, we can build the artifact. Okay? So give the run instruction and the Git clone command to clone the source code. Okay? So when we build the Docker image, the run instruction is going to execute this command. It's going to bring up a container with Maven image, then run git clone command, and then we need to run the next command, which will CD into that folder and run MV install. Okay, so CD into V profile project folder. So when you clone the source code, we'll get that folder. Ampersand ampersand. We combine the command with Git checkout container. So it's going to switch to the containers branch and then it is going to run MVN install. Now if we don't use this ampersand, we can give the instruction one by one, like run CDV profile project. Next will be run GI checkout. Next will be run MV install. But this is not recommended. That will create more layers in the images. The size of the image will also increase and it'll add more complexity. So it's always recommended that you combine these commands together. So this will be just one layer. Okay? So when we run Docker build is going to bring up a container with Maven image, clone the source code, build the artifact. But we are not going to use this image, this is just used to get the artifact. We need to give this image a name in the build process. So we can say here as build image or any name you want, I'm just giving build underscore image name. And I'll show you now how that will be used. So when we run Docker bill, we get the artifact here. The artifact will be there in the container, not in the image. It'll be in the container with the container that will be running. Now in the next instruction, it's going to bring up a container with Tomcat image and a Tomcat image will have a default application that we need to remove first. Okay, so I add this run instruction and I say RMF and rf user local tomcat web app / star. Now how do I know at this location the default application is there? Well, it's there in the documentation of the Docker image, the Tomcat Docker image. Now if you're not sure about such things, what you can do is you can fetch this image, run a container, log into that container and see for yourself, definitely it'll need some r and d. Okay, wait, I have a spelling mistake here. Yeah, web apps APPS. Because what do we do now? We've removed the default application. Now we need to copy your artifact into into this image. So we have an instruction called copy. Now here's the thing, the artifact is over here and the container that created, that gets created by this image. So how do we copy that to here? Well, for that you have this from you give the image name, build image. I'm saying it as image, but actually it'll be a container that will be, there will be container running with this name build image, because we have mentioned it over here. And in that you have V profile project and we know vPro profile project there is a target folder, and then you have vPro profile-V2.war, okay, so vPro profile project/ target. In that you have the artifact vPro profile-V2.war, and we are going to copy it at this location. USR local Tomcat web apps with the name root.war. So it is going to take this file from the previous image. From the previous container, sorry, and copied over here. Okay, next two instructions are really not mandatory because this is a Tomcat image. It runs on port 8080 and it runs Catalina.sh command to start the Tomcat service. But I'm just keeping it over here because you might have something else. You might need to run it on some different port. Your configuration may be saying some other ports. You need to mention that port over here with expose instruction, CMD to run a script or a command, which starts the process. As I said, this Tomcat image anyways has this CMD instruction that starts the Tomcat process with this command. But if you have something else, some other script that you copied, then you can mention CMD instruction and mention your command. It'll be in square bracket format, command might have options. Our script might have options. So you need to mention that in double codes comma and the option or the argument, everything will be in the double codes and all this will be enclosed in the square bracket. Okay? Now why do we have two separate Docker images here? If you have that question well and good, if not, still I'm going to explain. Well, you know what you can also do is you can have a Tomcat image install Maven in that, build the artifact and then copy there, right? Just have just one image. You can do that. The problem is then that image will become very heavy. It'll have Maven tool. When run Maven runs the command MVN install it is going to download so many dependencies. All that dependencies will increase the size of the image. We just want the in the image. We don't want the other things which is required to build the artifact. We're just interested in the artifact. So in order to reduce the size of the image, we keep the build image separate and the service image separate. So if you have node GS, you will have a separate node GS container to build the artifact and another node GS to run the artifact, to execute or to host the artifact. Always remember, whenever you have application that needs to be built and then hosted, you need to have a multi-stage Docker file. This is a multi-stage Docker file. You build the artifact in a separate image and you copy that artifact into your destination image. Once again, this is a multi-stage Docker file and we create multi-stage Docker file to reduce the size of our image. This Tomcat image that will be hosted will have less size, it won't have Maven and all its dependencies, just the artifact. Alright, once you're done writing this, just to control S, save this. In the next lecture we are going to move to the DB image. We'll see how to create the DB image or write the Docker file for the DB image. Once we are done writing all the Docker files, we are going to build this. So wrap this up and join me in the next lecture.
