# Branches & More

OK, now we are going to talk about branches in git. Branches are a concept where if you have a team who are working on different different features, so let's say one developer working on a different feature or second developer is working on the second feature. And you don't want to disturb this stable copy of the code. So let's say you have a branch called as Main. Or Master, that is your main branch and that we will keep a stable copy over here. If there is any new change that is going to be done by a developer or for a period of time. Like in an agile environment, you will have sprints, different iteration, in iteration one or Sprint one. You may be developing some set of features and then in sprint two, you'll be developing some other features. For that period of time, you can create a new branch which will be copy of this existing branch, make the changes. You can keep making regular changes in that branch, but the main copy will be intact. So the main code will be have the stable copy. And once all the changes are done or everything is stable on the other sprint branch, you can then merge it with the main branch. We'll see how that works. So for that, we need to create some branches. We can do it graphically from GitHub or we can do it locally also. So I do it locally because you learn some extra commands. So git branch -c. Now I am in the main branch and from this I'm going to create a new branch get -c. I will name it as Sprint one. OK, get branch -a will list all the branches. Now I'm going to switch to the other branch. Now in Main Branch, I have this data. If I switch  git checkout sprint1. I will see exactly same data because it's the exact copy of the main branch. Think of them as like, main was a folder you copied another folder named sprint one from that. So it will have exactly same data. But then in sprint one folder, you're going to make changes. So in sprint one branch, I'm going to make changes. So I'll make some drastic changes. So it will be very easy for you to see, very visible. So for that, I'm also going to show you a few new commands. I'm going to remove some files or rename some files. So if you want to remove files. You can run your rm command, but that's not a good idea, you should run git rm, so it will remove the file. Plus, it will also remove it from the index, from the staging area. So I will say git rm and I will say saturn3.py not 3, lets take 6.py, 7.py, 8.py, 9.py OK, good. You can check the status. It is telling us to commit. That means it has already removed it from the index. OK, renaming a file to rename a file, get mv., not just mv. command, get mv. saturn1.py to saturn11.py . OK. I will remove some other files also I made some really drastic change to the Sprint branch, so you can easily see the difference. OK, now I'm going to say git add . git comit I will say Jupiter changes. git push now the changes are done on the Sprint one branch, not on the main branch. So the push is going to be on Origin Sprint1, and that's what Origin is. You mention the branch. OK, now there should be a new branch in GitHub. You can see the Sprint one branch. You can create a branch from here, also from Sprint, one may be able to create another branch sprint Create sprint2 branch. sprint2 is created from Sprint1. So it will have exactly the same data as Sprint one for now, but then it will change. Let's pull the changes. OK, a new branch has pulled let's check out the new branch and git check out Sprint2. I'll create some more file here. And then again, git add ., git commit and git push Origin sprint2 OK, so main, sprint one and sprint2 all are having now different data. OK, but everything was started from the main branch, then sprint one branch, then from that sprint 2 branch. If you want to merge the change back to the main branch. Right. So I will now, you can use to check out or  I can use. Switch also. Now switch. Sprint one. OK, so I'm in Sprint1 branch now. And all the changes in Sprint1 I want to merge with the master branch, OK, so I will go to the master branch. Invalid reference, oh, not master, the name is different now Main. Now, I'm in the main branch now if I want to merge, so currently this is the content and I'm going to merge, say git merge Sprint1. So all the changes from Sprint one branch will be merged into the main branch. OK, I have this debug.log, I'm going to remove that. OK, this file is really creating problems, I can't even remove it, I'm going to put it into .gitignore file . .gitignore file is the file where you can place path or extensions of the file that you want to ignore, so I'll put their *.log. Any file that ends with log should be ignored. You can put all the files that you want to ignore. git status. OK, I'm going to commit and git ignore fileitself, git ignored. OK, let me again switch to Sprint1. OK, here also I will create the git ignore file. OK, now let's switch back again to Main Branch. And let's try to merge it now, git merge sprint1. OK, really, my this file was opened in Google Chrome, and that's why I was not able to remove it or do anything, but now I'm going to remove it. Let me check the status, OK. Everything is clean, so let's do merge once again. OK, so while merging, it is asking us for the commit message or the merge message, sorry, and I'm just going to save and quit this, it has created this, it has opened up in vim editor and put some content if you want. You can update that this is going to be our message for the merge. OK, now you should see Main Branch will be as same as the Sprint1 branch. All right, now let's check out or switch to Sprint1 branch. OK, now both are exactly the same. All right, now I am going to remove the entire repository. Oh, before I do that. Let's get into the repository and push everything, git push. Hyphen all origin, so I'm saying push all the branches. OK, next, I'm going to remove this repository titanwork it's there on GitHub, so I really don't worry. I want to show you actually now another command git clone. I have now no repository, you're here, my repository is on GitHub, so I can just go to my repository. I just need the URL. OK, copy the URL, HTTPS URL and say git clone Now, if it's a private repository, it may ask you authentication, but since we have already saved authentication, so it's not asking again. But if it's a public repository, it will not ask any authentication. OK, so all the changes pulled.
