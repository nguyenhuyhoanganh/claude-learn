# Git Ssh Login

OK, in this video, we are going to see git ssh login with the remote repository. So currently. If you do cat to this file .git/config file, you will see there is an https url. The authentication will be based on usernames and passwords, which really is not a good idea. Then you have remember the password, and store the password, there are chances of exposing the password. So GitHub provides also ssh based login and almost every remote git repository provides ssh based authentication, which will be through keys ssh keys. So first you need to generate ssh keys. So I already have some ssh keys and I'm going to remove them. OK, so just generate the ssh keys, you can say ssh-keygen. So it's going to generate the public and private key at this path. All right. I'm already at this path So I should have the .ssh directory. So I have the public key and I have the private key, and I am going to take the public key content and put it on GitHub. Right. So that's how the public key looks, I'm going to copy this. And go to my GitHub account. OK, so this is an account setting, not the repository setting, so come over here, top right corner and go to settings. You have ssh an gpg keys section, click on that. You can say new ssh keys Gives some title like my laptop and paste The Public Key. OK, make sure you put the public you're here, not the private key. OK, now I'm I will not be using that same repository. Let's go to git-repos and let's clone. The ssh end point. OK, so our Repository titanwork is a private repository and definitely needs an authentication. so not https, ssh. Copy that. And say git clone, it's a private repository, it should definitely check for the authentication, it will match our private key with the public key that we have just uploaded. If it works, if it matches, then we'll be able to clone. Oh. It says there already direcotory that exist here, so I'm going to remove that directory and then try and git clone. OK, you see, it's asking the question, yes or no, fingerprint check, does same like we do ssh, we're going to say yes if it matches it. Yes. And it matched. Of course, it's going to match because I just copied it. So it is working now. I don't need to see a username and password. So this is considered as safer login, ssh login on git repositories compared to https login. Before I wrap up the session, I would like to take you to the cheatsheet of git So Atlassian BitBucket has the git cheatsheet, you can just Google git cheatsheet and you can download it. That's a pdf file So you don't need to remember these commands, right, at least initially, so you have git init, git clone and git config, git add, and also description what that command is doing and git clean, git config. So there are many, many commands, but these are the most popularly used commands or useful commands in the regular day to day work. This this is really very handy. Right. And with that note, with that cheastsheet  I conclude the git session. And that is all you need to know, really, being a devops. So wrap this up and join me in the next session.
