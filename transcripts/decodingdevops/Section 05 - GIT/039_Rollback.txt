# Rollback

-: Okay, in this lecture we are going to see how to rollback. We'll see each and every command how to rollback. But before that, let's ask ChatGPT how to roll back. "Git rollback commands". Okay, so it is saying you can use a combination of git reset git revert and git checkout. And it is showing you the command and the information about it. Don't worry, we are going to do this and learn it but I wanted to show you ChatGPT in case you forget the command or you forgot the the method. Then you can anytime use ChatGPT and ask it or you can give specific command also and ask it to explain. So you can use Git reset you can use Git reset --hard. We, we are going to use both of this. We are going to also use git revert and git checkout. What does that mean? We know git checkouts switches between branches but there's also one more nice use case of git checkout for rollback. We'll be seeing that as well. And there actually a few more ways that we will be seeing in this lecture, but anytime you feel that you're not doing it right, just check with ChatGPT. Is your command correct or not? Ask questions, like I'll say explain git reset --hard with an example. As you see, it's explaining you A, B, C, D this is your branch, or the commit history, sorry. Right, but all this you'll understand when you actually do it, right? In case you don't remember, you can come to ChatGPT and confirm it once again. Because when you're doing versioning you need to be absolutely sure what you're doing. You have to, because you have to push it and share it with your fellow DevOps engineers and developers as well. And sure we can roll back whatever mistakes we do but I think the ChatGPT is a really very good tool to confirm the commands that we are running and we are doing it right or wrong, right? So let's, let's do it ourself and see how we can roll back the changes. Okay? Now we are going to see how to rollback and there are different, different levels where we can rollback. First let's up edit any file and I'll just put some content over here. Okay, I have put some content over here. Okay, now I want to rollback this before even I get into the staging area. So I can say Git checkout and I can give the file name. All went back to how it was. So git checkout and the file name is going to just rollback if you made the changes have and have not gone into staging or commit area. Okay, let's do it once again. Okay, again, few more lines. Actually I'm taking things from some existing playbook. You can put any content you want. Okay? You can see git status. There's also one more command, git diff, okay? Git diff can show you the difference in the file that you have changed, right? So jupiter1.rb file, it is saying that there these lines that were added, right? Now, if I'm good I can continue or if I'm not I can say git checkout and I can rollback. Okay? So let's say I'm good, right? So you can do git status, right? Which says there is a change and then you can say git diff where you can see what exactly is the change. If you're good you're going to say git add index it, oops space. And you can then again check git status and you can say git diff but just git diff command is not going to work now. You have to give an option, git diff --cached. So it's going to do the check, the difference of the file that was staged. So if you're already staged it, you want to see the difference from the current commit and the next that is going to happen git diff --cached. Right. If you want to go back from this level you can say git restore --staged and you can give the file name. Okay, now let's check git status. Says modified. Git diff, right? So back before we are staging. And at every level you can roll back first level git checkout and the file name when you're staged then git restore, git restore --staged and the file name. Okay, that is from the staging area. But what if you're already committed? So now let's again do git add and say git commit now. Git commit -m. Okay, now git status, everything is good Git diff, nothing. Git diff --cashed, nothing. Things are already committed. So git log --oneline. Get commit id. And then you can check the difference between the current commit that you did. Git diff, you can give the previous commit, dot dot and the current commit. And then you can see the difference. Now if you want to roll back from here, if you want to go back to your previous commit, you can just say Git revert Head. So Head is that pointer which is currently pointing to this commit id. And if I say get revert head, so it's gonna go to the previous commit or I can even mention the commit ID git head and I can mention the commit, and at that level the content will go. It will also ask me a message for the reverting, like the commit message. So now rollback. Okay, Git jupiter1.rb. See there is nothing. But now if you do git log --oneline, you should actually see a new commit. Okay? That is for revert, which is assimilar. The content here is assimilar as this one, right? So we are here, we made some changes, we came at this place then we wanted to roll back over here but it created a new commit. So when you revert, it is also going to store the history of what changes you made. But if you don't want to store any history whatsoever then you can use Git reset command. Git reset --hard and you give the commit id. Now here you go back to that particular commit ID and all the commit ID after that all the commit history after that is removed. The data and all the history is removed. So this is a more direct way of a rollback, right? Git revert is a softer way where you are storing the history also. Okay? So let me run the history command when I said history, okay let's talk about now reverting and rollback. So we made changes to this file and if I want to roll back before I stage, I can use Git checkout and the file path, okay? Can roll it back. But if you have already staged the change now you can compare with git diff. If you already staged you can say git diff --cached. If you want to restore from after you already staged, right? And then you want to restore, then you can say git Restore -: -stage and the file name. But if you have already committed and you want to roll back then you can use Git revert or Git reset. So based on your requirement, what you want to do, right? But make sure you use git diff command to really check what are the differences between two commits. So you want to see something is not working now you just made some code change, things are breaking. You want to see the difference from the previous commit, use git diff. It's a very useful command.
