# Outputs

Instructor: Okay, printing out the information and also we can store information from our Terraform state. So when we execute Terraform apply, it returns a lot of output and that is getting stored into Terraform TF state file and we can access information from that file itself directly. We can say, you know, we can go through the aws_instance resource, the resource name, .public_ip, and we can find the public IP. We can use an output block to print this also. So output resources to print content, and we can print our attributes that are already saved. We can also use local-exec to save such information into another file. You want to save IP addresses or IDs. We can store it into a text file. Okay, so output instance, also you can give the name of your resource, what do you want, and value, you can give the path of the attribute that you're looking for. So resourceType dot resource name dot the attribute name. Okay? The element is this: aws_instance.server.public_ip. It might say aws_instance is the resource type. Resource name is the server, so as we give any resource name, right? So like that is just an example server. And attribute name inside that, public_ip. If you know how to access that, then you can store that information to a text file also by using a local-exec provisioner. We have seen remote-exec in the previous section. Local-execute will execute command locally. So we say for example, command = echo. Echo is for printing, right? So you can say print all this content, redirect the output to this text file. Okay, so let's then try that. Okay, for this again we are going to create another exercise, exercise five. Copy exercise four, paste it, change its name. Exercise five. We will open instance.tf file, scroll down and at the end we'll give the output. We already used output in instance_id. There, output. Name of the output, description and its value. So let's copy the same thing. Go to instance.tf file and let's paste it there. So let's say I want to print the public IP of the instance. I will say webPublicIP, and in the value, I'm going to mention similar thing but instant of the AMI and the AMI ID, I'm going to get the public IP of the instance. So our resource, aws_instance.web, let's write that first. We give web dot and we can see the list all here. So I want to print the public IP. All right, that's print, right? Output is going to print like that. Let's print the private IP also. So simply copy paste this output and here let's make some change, webPrivateIP and let's remove this public_ip. We'll say .private_ip, that one. Now what if I want to store this information into a text file? Right, for that you can use the local-exec, the provision at local-exec and execute your command. You can see now echo and you can just print it and redirect it to a file. Well, that way. Make sure this is in the aws.instance resource just after a remote-exec in the same line, provisioner local-exec and command equal to, I'm seeing echo, and the variable. This you can get through self.private_ip also or you can use, like this, the resources and redirect it to a text file. Let's save this and execute this. So I open my Git Bash, you can open your terminal or from VS code. Switch to exercise five, Terraform init first. Terraform format, FMT. Terraform validate. Terraform plan. If you're good, you can do Terraform apply. Okay, so this completed successfully and you should see the outputs over here. Alright, so these are the names that we have used in the resource, the output, basically. It should have also created the file where it has the private IP. Let's check that. There, private_ips.txt. Let's check it from the VS code. There. Right, so there are chances that there will be tasks like that where you need to use that information for something else. So you can, runtime, you can get the information and then you can store it into a file. You can, you know, queue as per your own format, like list of IPS or a AMI IDs, stuff like that. Okay, so that was output. Pretty simple. Let's once again destroy everything and once you're done, join me in the next lecture.
