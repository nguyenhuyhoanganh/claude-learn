# Variables

Narrator: In this section we are going to talk about variables. We'll see how to define variables, how to use it. So you'll really see the Terraform way of consuming variables. So variables as we know, helps us move critical or confidential data out of our script. Also, if there are some values that changes based on your environment on your project, you can define them as variable. So you don't need to make much change in your code, you can just change the value of variables. Or if you are again reusing it, you want to reuse your code for different environment, different project. So you can define things like AMI, tags, keypair, these things as variables in our instance.tf file that we're going to write. So we are here right now, we have instance.tf file, right? We have provider information and we have the resource information. So we have things like region that may change based on where we execute our Terraform scripts. There are things like AMI in this, instance type. These are the things that varies based on different situations. So from here, we'll go here. So we'll take providers out, we'll have a providers.tf file where we'll define the provider which is actually accessing REGION. The value of REGION variable is gonna be var.REGION. So it's a variable and variables will define another file. So let's say we have vars.tf file, where we'll define variable, that's how you define variable in Terraform Variable, variable name in curly braces, you say default and the value. So it means the default value of AWS_REGION is us-west-one, and that you're referring in the providers.tf file, var.REGION. So in vars.tf file we have the variable REGION, and its value is us-west-1. If you're gonna define the access key and secret key, you make sure to define it in different file, terraform.tfvars file. But you can totally skip that because as I told you, do not put your access key and secret key in your scripts in your code. And then we'll have instance.tf file where as of now we are not defining variable, but we'll change that as well in a very different way like this. So now vars.tf file have more variables, ACCESS_KEY, SECRET_KEY, REGION. And also one more interesting variable, variable AMIS, This variable is of format, is of type map, like a dictionary, like a hash. So if you want to access this based on your region, what's the value of your region? We can define the values. So if you want to access this AMI, you're gonna say AMIS. Us-west-1 and you will get that particular AMI. This call us map variable. So if you see in your instance.tf file now, for AMI we have var.AMIS. So that's the variable. And then in square bracket we are giving var.REGION. Inside that AMIS it'll look for a key named as REGION. So, region value currently is us-west-1, So, us-west-1 value is ami and this particular ID. This will be very helpful if you're executing your Terraform scripts for different regions. So based on the region it'll pick up the right AMI ID. So let's see what we are going to do in this exercise. We're going write providers.tf file, vars.tf file where we'll mention all the variables, instance.tf file which is going to access all the variables, we'll apply the changes of course before validating, first we'll validate and then we'll apply the changes, and then we'll make some more changes to the instance to see its state and then we'll reapply the changes. So let's get started with the exercise. Okay, so I'm going to take all the code from Exercise2 and copy to Exercise3. So I'm going to simply right-click over here and we'll say Copy and right-click and Paste, and then right-click and I'm going to rename it to Exercise3. Enter. Okay, so we have all the code from the previous exercise in this exercise and we are going to add variables here. So let's create a file called vars.tf where we'll mention all our variables. So Exercise3, right-click and new file, I'm going to name it as vars.tf. And before defining the variable, I'm going to put the variable where I want to use, like in the provider. Here I have just mentioned the value directly over here. Instead of that, I'm going to say here var.region. And save this. Let's go to instance.tfi file. And the availability_zone also I'm going to say here var.zone, and let me see if this. Okay, now let's define these two variables. We'll come back again and we'll add a few more variables. Let's start with these two. Okay, so I have defined here two variables. That's how you do it. Variable and the variable name, default and the value. So the value of the variable region is this and the value of variable zone1 is this. Did you observe anything? If not, you'll observe it or find it when we execute it. So two variables we have defined in vars.tf file, and instance.tf file we are using those variables. One we are using here and one we are using in the provider. Okay, let's execute and see what happens. So once again you can open the terminal from here. Yeah, make sure to switch to Exercise3. This is a new exercise. Make clear the screen and let's say terraform fmt. So, vars.tf format it corrected. Let's save that. And let's say where terraform validate. Right, something broke. Let's take a look. It says, "Instance.tf line 6, availability_zone. "An input variable with the name 'zone' "has not been declared. "Did you mean 'zone' one?" See that? It's smart. So variable names were similar, right? So it found that you know, it is not zone, it's zone1. But variable name might be different and it might not find what exactly you mean. So make sure variable names matches. So it's zone1, the variable name, right? Let's execute it once again, terraform validate. After making the changes make sure you save it and everything is good. Now let's do terraform plan, right? So in the previous lecture we deleted everything, right? So now it is saying now it has plans to add nine more resources. So we are not going to apply any change now. I want to show you one more variable, and that is for the AMI. Now, code is already smart enough to fetch the AMI ID in the latest AMI ID. But what if we don't want that? We want specific AMI IDs to mention. And based on the region that we change, it should select that particular AMI ID. We already discussed that AMI ID is different for different regions. So we want to have a variable that returns a dynamic value. Not exactly dynamic, but you know lookup, it should you know, look and find the right AMI based on the region. So come to vars.tf file and we are going to create a variable of type lookup. I'm going to say type map. Yes, there are different types of variable. You have string, you have list variables and you have map variable. So generally the value will be like this, we say default and equal to we give the string. But here this is a dictionary that will have multiple key value pairs. So you can say default equal to open close curly brace, and then you can give different AMI IDs. So if the region is us-east-2, then this will be the AMI ID. I mean, here there is no if and but here, they're just a list. We'll see with the lookup function how we can fetch the right AMI ID for our region. So first make sure these AMI IDs are correct. So find your AMI IDs. So go to one of the region that you have mentioned and say Launch Instance. Let's select ubuntu and get this AMI ID. And this one is for us-east-1 here. Paste the value in us-east-1. Let's go and find out for us-east-2. So I switched to Ohio us-east-2. Launch instance, click on ubuntu and get the AMI ID. You see both are same, going to server 24 HVM SSD volume type. But their AMI IDs are different. So come to instance.tf file now. In the AMI here we are going to give var.amiID. ID's capital, amiID. So now it has multiple values, right? So to get that you need to do square bracket like this, and as I said, based on the region I want to fetch the AMI ID. How do I do that? Well here I can mention var.region variable. So what's going to happen now is var.region value is in this case us-east-1, right? So here it'll be us-east-1. So it'll go to var.amiID variable, var.amiID variable. And from this we need to mention the right key. In this case it'll be us-east-1, which it'll take automatically from this variable and get the AMI ID for us. Okay, take a look once again in the vars.tf file and instance.tf file. If you follow the Python lecture this will be easy for you. This basically is a dictionary. So we mentioned the key in the dictionary. Dictionary name dot the key. In this case here its, amiID is the dictionary name, dot its key var.region. That is the key, key name which is another variable, right? We have to make sure we save all this. And let's plan the execution while we had terminal open anyways. So let me go to Exercise3 and I'll say terraform plan. First let's do fmt, format. And then say validate. Okay, everything is successful, and then let's say plan. Alright, so finally the plan will tell you whether it's fetching the variable or not. The validate itself will tell you whether the variable names are correctly mentioned or not. So really we don't need to apply this and wait for some time. This is good. So I recommend you check this documentation also if you need to create other kinds of variables. This is all the information about variables over here. You can just simply Google search variables in Terraform and you can come to this documentation. Declaring an Input Variable. You see we can mention the type of the variable, like string. You can mention list of string, right? And then the value like this. So this is basically the list of string, you can give multiple values comma-separated here. A few other arguments you have here, default type, description, sensitive, so it hides the output of it, like something that you don't want to print. Type constraints, different types. You even have Boolean tool false, description, suppressing values in CLI output, right? By giving sensitive, true. Using the input value variable, we've seen that, var. variable name we can give. Also, when you are applying the changes, you can mention the var file location, which can be some other place as well, right? So use it when you need it. When you have use cases, then you can come to the documentation, find out and put it in your code. But that should be all for this lecture. In the next lecture we are going to talk about provisioners. So wrap this up and join me in the next one.
