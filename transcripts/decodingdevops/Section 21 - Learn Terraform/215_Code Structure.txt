# Code Structure

Instructor: Welcome. In this lecture, we are gonna launch an EC2 instance by using Terraform. but let me tell you, that's not the objective of this lecture. The objective of this lecture is to understand the Terraform code much better, its structure and also the Terraform state. How does it maintains the state? Launching an instance is just an example of what we are trying to achieve. Our major focus here is to understand the code structure of Terraform. So in the previous lecture, we have seen how to fetch a data and print it by using Terraform, and the data was the AWS EMI ID. If you expand here exercise one, you will see a few more thing apart from just the instance ID.tf file, that was our code. There is also a dot terraform folder. This was created when we executed Terraform init. So if you expand it, now, here, this is an executable file for Windows. You don't need to actually open it, but when you do Terraform init, these folders will get created. You have a lock file here, click on that, so this is maintained automatically by Terraform init, it says. It has some information, like the provider that we are using, its version, and then you have the terraform.tfstate file. This file maintains the information. You can think of this as the output when you execute the terraform apply. It does what you want it to do and stores all that information, all that output, into the state file. This is called as the tfstate file. Now, we just maintained the AMI ID, we just fetched the AMI ID, so you can see here, resources, AWS AMI, name, AMI ID, provider, et cetera, et cetera. If you scroll down, you should see here the ID. See, this is what we printed in the output. We said dot ID, and it printed that information, so you can fetch all this information which was created by the state file. This creates in the runtime, and then it will save it. Now, this is locally saved over here. In the upcoming lectures, we are going to see how we can maintain the state remotely. We're going to use S3 bucket for that. Okay, so just take a look, and then we are going to create the second exercise, but not here. Click over here, new folder. Not inside this, yeah. Now, I had to create the exercise two and the exercise one, then I simply dragged it outside like this, and then it says moved. Anyways, lemme close this. Let's go to exercise two, and here, we are going to create a new file that will be seeing the instance ID, because we still want to find the instance ID, so I will just name it as instance ID.tf, and I'm going to copy everything from instance ID here to the instance ID.tf in the exercise two, save it, and I have named it like that, instance ID.tf file, and I'll only put here the instance ID fetching information. To launch an instance, I am going to write another TF file, and also, to launch the instance, we need a key pair and we need the security group, so for those resources also, I'm going to create separate files. So first, let's create a file for key pair., so I'll say keypair.tf and just hit enter. Make sure to give the extension as dot TF. The name, you can choose anything else if you want. One more file I'm going to give is for security group.tf, and another file for the instance, instance.tf. So that's how you create the structure. This will make your code more readable. You can put all the information just in one file and execute, but then it will be difficult for you to maintain and manage that one single file, so it's better you separate your resources into different files. Also, don't just create too many files. If your resources are short, you can just group them into one file also or few files rather than creating file for every resource. Okay, now I'm going to create one more file, and I'm going to name it as provider.tf, just with small P, and I'll tell you why I'm creating that. Our provider will be AWS, and with that, I can mention more information also of the provider, like which region I want to use, what key pairs I want to use. Sorry, I meant to say the access keys. That's not a good idea to put access keys, definitely very, very bad idea to put access keys in the source code, so never do that, but I'm just telling you, you can give more information about a provider. So again, you don't want to put all this information into the instance file because that's separate information, so I'm going to say here provider, and the provider is AWS. That's the name you have to use, exactly same name, and then the resource block, like we create, curly braces. Region, you can mention equal to the region where you want to create something, say, us-east-1. So come to this link, registry.terraform.io/browse/provider, so if you just come over here, you can see all the providers. So we are going to use AWS provider, so let's click on that, and let's click on the documentation over here. Okay, so left-hand side, you see all the resources you will see here to create instances, CloudWatch, and everything. So example usage. How do you use the provider? Terraform required providers, this we don't want, so provider, AWS, and you can mention the region. Here's an example to create a VPC in the same file, but that's not what you're going to do. What else? Your versions. You can mention the different version of your provider, so approximate, that's an approximate symbol, 5.0. Let's see what else you can do. Yeah, access key and secret key. As I said, never do that. I just wanted to show you, you can mention more information. Profiles, if you have multiple profiles for your AWS account, multiple AWS account, you can mention the profiles. Roles, assuming AIM roles. I'm just showing you that you can give more information. You can just get into the documentation and find out and use that information. Here are AWS configuration references, all of them that we can use with the provider. All right, so let's come back to our code. So we just want to mention the region. That is all. Let's just save this file, and we want to create a key pair first, so let's go to the key pair, and from the documentation itself, we'll find out the resource to use or create a key pair. So in the same documentation, just search here, key_pair. Now you can find the resources. As the resources, let's click on that. See AWS key pair, example usage, so that's how you can use it, so I'm going to simply copy this and put it over here. Just hold on a minute, I'm going to explain this. Yeah, so aws_key_pair, that's the name of type of the resource. This is the name. We're gonna change it. I'm going to use the name as dove-key, just the name of the resource. Key name, this will be the name of the key in the AWS console that we'll see, EC2 console, so that will be the name of the key dove-key and public key. Now, here's the content of the public key. Definitely, this is not what we're using to remove this. So key, we know it gets created into pairs, so first, you would generate the key pair, and the public key, we'll put over here, and private key, we'll keep it with us. First, let me just see this one. So open the terminal, terminal, new terminal. Just make sure you are in exercise two or whatever name you have given. Go to that folder. All right, so we should have all these files here, and here, I'm going to generate the key, ssh-keygen, execute that command, hit enter. So it is asking me to save the file path. I'm going to give the name itself so it creates the key here itself, so I will just give the name as dove-key and hit enter and enter and enter, and if we do ls here, so we should see the public key, and we should see the private key now. Let's take a look at the public key, because that's what we want to push. So that's the public key. Let me copy that. So whatever content you see, just select that, so for Bash terminal, you just need to just select it, right click, and that's how it's going to copy, and then come over here in this double quotes and paste it. And it's control S. So this will be the private key to log into the instance. We're going to store it in AWS. So that was the key pair. Now, let's go to security group.tf file. Here, we're going to create the security group. So I search for security group, and we are going to create the instance, that is not the one, VPC, so security group is basically a VPC resource. So I'll click on this, aws_security_group. Let's scroll down, and here's the example. It's big, but don't worry. We understand this, because we already know how to create security group through console, so similar information or almost same information in the code format. So we have resource, aws_security_group, and the name of the resource that's given over here. This will be the name of the security group, the description when we create the security group, remember, we give description also, VPC ID, you can mention the VPC, where it should belong to. If you don't give this, it will create in the default VPC, and then the tag. So this should create the security group, but then this will create the rules inside the security group. So in security group, we have seen we have inbound rule and outbound rule. Here, it's called a ingress, that is inbound, and you have egress, that means outbound rule, and you can create multiple rules. This is one rule, this is one rule, so two inbound rule, because here, it's the resources ingress, and one outbound rule. There's one more, this is for IPv6. So let's see the resource type, name of the resource, and then the security group ID. So security group ID is coming from here, so aws_security_group.allow_tls. That's the name over here, and then dot ID. So that should get the security group ID where this rule will get created, then the CIDR block, that is from where? So here, we can mention our own IP or the 0.0.0/0 rule. That means from anywhere, and the port number. Now, here you will see from and to port. We are going to give the same, so when we create security group manually through console, we just give one port number, but here, there is from and to. We are going to have to keep the same over there. This is not inbound and outbound rule. This is the ingress rule, remember, and the protocol. Similarly, there is one more rule for ingress and one egress, that means the outbound rule, and here, it says CIDR block. That is from anywhere, any IPv4, and IP protocol minus one, this means for all the ports. So outbound rule, we're keeping, all the traffic can go anywhere, and this is for IPv4, and this is for IPv6. So let's copy this, and let's make the changes. Let's paste it here, save it, and then let's start making the changes. So name, I'm going to give dove-sg. That is the name of the resource, but the security group name is here, so I'm going to give here, and here also. description, I'm giving the same. You can give some other description if you wish. VPC ID, I'm going to remove this because I want it to create in default VPC. That is fine. Tag, I'm going to give again the same tag. Okay, now the rule, so I want to give two rules here, 22 allowed from my IP and port 80 allowed from anywhere, so that's the rule type. The rule name, I'm going to say SSH from my IP. Now, security group ID, AWS security group dot the name of the resource, which is dove-sg here, dot dove-sg.id. Now, CIDR from where we want to allow this. I'm going to remove this one and I'm going to give some square bracket, so I'm going to give here my IP. So I'm going to Google and find out what is my IPv4. So I just searched on the internet, what is my IPv4? I'll say this website, what is my IPv4 address? You can get it from any website. So in double quote, I get the IP address, and I have to say slash 32. Slash 32 means exactly this IP, not the range. I can give the range, but that is a topic of networking. We are going to learn about this, the range and all, in AWS part two section, where we learn detail about VPC. So from port 22, here also, to port, same, 22, and 22 SSH port is the protocol is TCP, basically, and I keep on saving it again and again. I recommend you do the same, just control S. The second one, I will say allow HTTP. The security group ID name here, we have to change, dove-sg. CIDR IPv4, and I'm going to give here, so if you want to allow it for IPv6, then here, you have to mention CIDR IPv6, but I'm just keeping it IPv4 for now, just to keep it simple, and I'll say 0.0.0/0. I have to make sure I should put it in double quotes, from port 80 and 80 and here also 80. Okay, control S, let's save this. So that means anywhere, any IPv4, so port 80 is allowed from anywhere, any IPv4. Now, the eggress rule, the outbound rule, we have to make sure we give the outbound rule of the security group. Otherwise, our instance won't be able to reach the internet. Name of this resource, I'm going to say allow all outbound. Once again, the security group name, dove-sg. Sorry, the resource name, not the security group name, and CIDR IPv4 allowed from anywhere, any IPv4, and IP protocol minus one. This basically means all the protocol, all the port number, I mean to say, so all the ports can go destination on any IPv4. I'm going to give for IPv6 also. So here, I'm going to see underscore IPv4, and over here, the next rule, I'm going to say here, allow all outbound_IPv6, security group resource name, dove-sg, and control S, save it. Okay, so check again, syntactically it is correct or not, check the names. I've given the security group name as dove-sg. So all the rules here should have that for the ID. Check that, and wherever you have string, make sure it's in double quotes. This is an integer, so it doesn't need to be in double quote. So let's save this, so our security group resource is also created. Now, the next one will be the instance. So instance TF file, we are going to write in the next lectures. Complete this and join me in the next one.
