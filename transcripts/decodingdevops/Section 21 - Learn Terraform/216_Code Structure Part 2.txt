# Code Structure Part 2

Instructor: All right, so, so far we created a provider.tf file where we mentioned the provider as AWS and we mentioned the region. We created a key pair .tf file where we mentioned the public key. And for that we generated the key pair by SSH -keygen command and all the public key content. We have copied it in the public key section over here. Then we created the security group named dove-sg. And we created here four rules, two ingress rules and two egress rules. 22 allowed from my IP, 80 allowed from anywhere and egress everything allowed for IPv4 and IPv6. So we have everything. Now we are going to launch the instance, you're going to write the code to launch the instance or I should say to maintain the instance. So let's go to the instance .tf file. Coming back to the documentation I just searched for EC2 space instance. So in elastic compute cloud resources, we have few over here. And let's check this one AWS_instance. And here you should see the first example of finding the AMI id. That's what we did in the initial lecture. So here it says how to find the AMI ID and then the resource AWS_instance, mention the AMI id that is coming from the previous data resource. Instance type and tag the instance. But we are going to provide more information. We are going to mention the key pair. We are going to mention the security group, the availability zone. So let's scroll down and that's fine. So there are information and instead of doing that we'll do it a little different. We'll get the minimum information and then we'll find the arguments, right? Like AMI is one argument, instance type is the argument. We'll find the arguments and then we'll mention it one by one. So let's just copy this one. All right, so AWS_instance and name web. Let's keep the same name, ami. How do we get the AMI? Well AMI id. So we do have the instance ID TF file, aws_ami .amiID is it instance .amiID.id. Even if you remove this and we mention the dot, it should show us right? ID, instance type T3 micro is fine. Now I want to mention the key pair. So over here I'm just going to simply do control F and I'm going to see key_pair. Okay, I see this key underscore name. Now check first of all is it the argument or not, argument references. Because there will be argument references and then there will be the data resources, which would be the output. These are called as the attribute references. These are the output when the instance gets created or not. When we execute in the key of state file, it's going to store all this information. So make sure you check the argument references, right? This one when you're writing the code. So key_name, optional key name of the key pair to use for the instance which can be managed using the AWS key pair resource, right? So here it says optional. So it's not mandatory. So if you don't mention you don't have the key, right? But we want to have the key. So most of the argument references will be optional, some will be mandatory. So let's mention that in our code. So you can directly give here the value in double codes dove-key because that will be the key that will be created with this name. Or you can give like this also, aws_key_pair dot dove-key, dot key_name, that will also you can give. For the key name. Let's keep it that. But for security group we definitely need to do that. Okay, so next let's mention the security group. So here we have vps_security_group_ids. Again, this is also optional if you don't mention this, it's going to use the default security group. But let's mention that. Also check one more thing. It says list of security group IDs. Whenever it says list here to give it in the square bracket, the value should be in the square bracket. So vps_security_group_ids equal to square bracket and in this you give the security group ID and that is going to come from here. Security-group.dove-sg-id. And the ID resource will get created later, once we create this, then only we'll get the ID argument, but we can mention it beforehand also. AWS_security_group.dove-sg dot and we have the ID here, see that. Don't put this into the double codes 'cause it's not a string. The value of this will be a string, but this basically is the variable. Okay, we are going to mention a few more things over here like availability zone, availability_zone, that's the name of the argument equal to the value. It's a string. Double codes, I'm mentioning US East 1A. Okay, check our provider, we have the region, US East 1 and the zone we are mentioning is 1A. And this should exist. I cannot give here anything, right? Also, let's change the tag. I want to give name as Dove-Instance. And also I will give one more tag project my tags, you know, we can give as many as we want. So we'll have two tag on this instance. I will give the name as Dove-web and let me save this, okay, so we are good, we can execute this. But I want to mention here one more resource that is the state of the instance that I will do it in the next lecture. Next lecture, we are going to execute this. We are going to do some rnd with this. We're going to make changes, we'll see how it affects the execution. So all that we'll do in the next lecture. So wrap this up and join me in the next one.
