# Provisioners

Narrator: Hello and welcome. In this lecture we are gonna talk about provisioners in Terraform. Just search for Terraform Provisioners and you can come to this documentation. So the first thing we see over here is "Provisioners are a last resort." Now the reason that is said, provisioner is something else that Terraform cannot manage. It can execute it for you, but its state cannot be managed by Terraform. For example, you want to execute a script when the instance comes up. So Terraform has File Provisioner which can push the script and then remote-exec provisioner that can execute the script. But after that, what happens when the script executes, whether it passes, whether it fails, that Terraform cannot detect. I mean, pass and fail is fine, at least that you know the the code execution will fail, but its output and then its state cannot be maintained by Terraform, that you have to keep in mind. Ideally also, the best way is to create an image, an AMI. And we are talking about AWS, right, we create an AMI, keep it ready, and whenever we want, we can launch it by using Terraform. You can also create images by using tools like Packer that very well integrates with Terraform. So if you click on this link over here, Provision infrastructure with Packer, here there are all the steps of how to create an image with Packer. You can mention your script that needs to be executed. Right, in the Terraform code you can mention all the details and build your Packer image. Once you have your image ready, then you can mention the AMI ID in your code, right? But nevertheless, we really want to see the provisioner, because when it comes to real time there will be many time when you need to execute something on the instances when you're creating it. So we want to execute a script. First, let's create the directory Exercise4, and create our script. So, Exercise3 I'm just going to copy and then paste it here itself. Then rename it. Then right-click on this in New File, and I'm going to name the file as web.sh. (indistinct) you can get this from the lecture resource, and this we did several times in the course. We are installing apache2, downloading template from tooplate.com, extracted it, copy to var/www/html and restart apache2. Nothing new, we did this several times. Save this file. Now we need to push this file to the instance that gets created and then executed. So it'll be two step. First, push the script; second, execute the script. So execution will happen on the remote machine on the instance that gets created. So let's take a look at the provisioners in Terraform. So if you click on file provision over here, you can see this is how you can copy the file to the instance. And this is an argument in the aws_instance resource provisioner. So it's not a separate resource. Provisioner name, file; source and destination. So let's copy this. Open instance.tf file. And just after the tag block ends here, you can mention that. Keep in mind this is an argument in the resource, aws_instance, it's not a separate resource. So it should be under this, under this aws_instance block. So let's give the name of our script, which is web.sh, and the destination we want to push it to, let's say temp folder, say tmp/web.sh with the same name. Okay, now comes the execution part. So go over here, remote-exec. So you need to mention the connection information because it is going to do a login. And this is of type ssh. So it's going to do ssh to your instance, which is created. And for ssh you need to mention the username, and not the password, we are going to mention the key. We have the login key in the source code itself. So we can mention the login key path and we also need the public IP. That's how you can get the public IP self.public_ip will give the public IP of this instance. So it's going to do an ssh, and then you can, in line, you can mention the commands that you want to run. Well, we can just mention the shell command to execute the script. That's all. So let's mention the connection. So I'm saying type ssh. User, I'm giving a variable that I'm going to define in the vars.tf file. Let me save this first. Let's go to vars.tf file, and let's create another variable here. Make sure you're editing the vars.tf file for Exercise4 folder. So I've given the variable name webuser, same I have defined in this file, and the value is ubuntu. 'Cause we are launching an instance with AMI ID that is of ubuntu and by default the user ubuntu, we know that. Okay, next in instance.tf file, we mentioned private_key and the key private key, right? That is the private key. So we need to mention here the content of the private key, right? Which is this one, but that will be too weird. So we are using a file function where we can mention the file path. So this file function is basically going to read the content of the file and give the output over there. So this will be replaced by this. Host, self.public_ip, that is going to get the public IP of the instance that gets created. So one more thing, make sure in the security group, 22 is allowed from anywhere or update your public IP over there. If you don't want to give it anywhere, make sure you give the current public IP. Just Google what is my IPv4 and give your IPv4 over there, slash 32, otherwise you can just keep it from anywhere. Okay, now final part, executing. So, provisioner remote-exec. In the block, you have to give inline, and in this square bracket you can mention the commands one-by-one separated by comma. It will make sure all your commands are in double codes and they're separated by comma. This is a list, same like Python list. So the first command that I mentioned is chmod +x. That is to give the execute permission to this. And the next one is sudo, and the path of the script which will execute the script, that's how we have seen it in the Bash scripting lectures. So how you can execute the script. So let me save this file. So make sure all the files are saved instance.tf, web.sh, vars.tf. Once again, we created web.sh file. We mentioned our executable commands over there. Make sure you have -y where you have the apt install command because the script should execute. This should not stop, otherwise the execution will fail. All right, so we mentioned the provisioner file, source and destination. We pushed the script, we give the connection information how to do ssh, and the remote-exec which will execute our script. Time to test it. So I open my Git Bash, you can open your terminal or you can do it from VS Code if you want. Go to Exercise4. And the first command we are going to run is going to be the terraform init. So it completed quickly because we copied everything from Exercise3. So it has everything. Let's do terraform format. Everything good. Let's do terraform validate. It's a success. If you have variable names different, then it will throw you the error. Finally, terraform plan. So, we can apply next. It's a completely new setup. We destroyed everything in the previous lecture, so it's completely new thing. Now, terraform apply. Okay, if you see the remote-exec is executing the script, and we can see the output also. And it says, "Apply complete!" Now we should have an instance with the website running on it. So I go to the region, North Virginia in my case. Instance, there is is instance running. Alright, the instance is running. If we check the security group rules, inbound rule HTTP 80 is allowed from anywhere. 22 is also allowed from anywhere. Instance public IP, let's put it on the browser and if everything went fine we should see the website. Alright there. Now if you're not seeing it, it might take some time for the complete, you know, instant state to come up in the running state. So give like a few minutes, two, three minutes and it will be up. So you see the provisioner works, it works well, but you have to be very careful because now the Terraform cannot maintain the state of your website. For that, you need to use a separate tool or you need to be sure that you're deploying it from an AMI where everything works fine. If you're good with everything then you can say terraform destroy and delete your infrastructure. That would be all in this lecture. I will see you in the next one.
