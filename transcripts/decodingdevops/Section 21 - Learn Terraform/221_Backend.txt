# Backend

Instructor: Okay, this lecture is short but very crucial. So we know so far when we create the Terraform infrastructure, it creates a terraform.tfstate file, which stores the state information. And that's the best part about the Terraform. And it's also the worst part. I'm executing it locally on my machine. The .tfstate file is on my machine. If I'm working in a team, my team members also should have the same state file. I know what you're thinking. Source code, we can put this in the source code. Yes, this will be in the version control system like GitHub, but that is when you are done doing your code, then you are going to push it. Plus there is all crucial critical information that goes with that. So never maintain this in the source code where you have the core of the Terraform itself. We can mention it somewhere else. Like in AWS case, we can store the .tfstate file in S3 bucket. And Terraform has that integration. You have to just mention resource called backend and mention your bucket name, region, and also the path where you want to store the .tfstate file. So first we need to create an S3 bucket and then we'll mention its information in our code. So go to your AWS account and go to S3 service and see Create bucket. Give a unique bucket name. I've given terraformstate and I have added some numbers just so it becomes unique. You have to give a different bucket name. And simply create the bucket. So open your bucket and create a folder in that. I'll just give the name as terraform and I'll just say Create folder. Okay, so get your bucket name and your folder name. Now let's put this information in our Terraform code. So once again, I'm going to copy, paste the exercise, rename it with Exercise6. In this I'll create a file called as backend.tf. Terraform, in the block you have to say backend, and the name of the backend, that is s3 for us in double quotes. And in the block you mention the bucket name. Key, that is basically the path, so your folder name slash any other name that you wish to give. Region, make sure your region is same where you created the bucket. So my bucket is in us-east-1. That's why I have given us-east-1. Okay, save this information. Go to Exercise6. terraform init. So when you do terraform init, it says, "Initializing the backend...". So if you're given wrong bucket name or there is some problem, that is going to throw the error. But in my case, everything is good. So it initialize the backend. Now we can do terraform plan and then I'll do terraform apply. yes. Okay, so the execution completed successfully. So we know we have a terraform.tfstate file, right? That maintains the information, but we said the information should go here. So let's check the terraform.tfstate file. You see there is nothing here. Now let's go to the S3 bucket, terraform/ folder. And here we have a file called backend. Let's click on that and let's click on Open. And now you see all the information is mentioned over here. Now the biggest benefit is you can put the source code in your repository like GitHub or Bitbucket. You're working in a team. They'll have the exact same code, but .tfstate file is going to be in the S3 bucket. And in the execution, the S3 bucket .tfstate file will get updated in the runtime. So everyone in the team will have same .tfstate file coming from a remote location, S3 bucket. So there are other ways also of mentioning your backend, not only S3. You can integrate with HCP Terraform. HCP is an application that is provided by Terraform. So you know, centralized team. So team can, you know, have a centralized location where they can maintain the state, they can store the secret information like passwords and all. You can have access controls, really enterprise level work. But you also get a free account if you have a small team. And so if you're using HCP, then you can mention backend as remote and organization, your organization name that is in the HCP and the workspace that you create there. So then HCP will manage your state file. Otherwise you already have seen the S3. And you can also use Console, Console Database instead of S3. My point is there are different ways of mentioning a centralized location for the state file and you should always use that in real time. Centralized .tfstate file. And if you're done here, you can destroy everything. And then that is all in this lecture. I will see you in the next one.
