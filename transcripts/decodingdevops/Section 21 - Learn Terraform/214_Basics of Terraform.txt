# Basics of Terraform

Instructor: Welcome. In previous lecture I told you we are going to launch an EC2 instance by using Terraform and to launch an EC2 instance, we need to know its AMI ID. So first in this lecture we are going to talk about finding the AMI ID. So there are multiple ways. One of the simplest way is to click on a launch instance and in the quick start you get here the AMI ID. So for that is for Amazon Linux. You can check it for Ubuntu, is the AMI ID for Ubuntu server 24 at HVM SSD volume type. And you can select the different versions of the AMI and find the AMI ID. The other way is you can simply Google or search on the internet, find the so-and-so AMI ID and you will get some website. For example if I were to find Ubuntu AMI ID is Ubuntu Amazon EC2 AMI finder. And here we see the region, the versions, architecture, release date, and the AMI ID. Keep one thing in mind, AMI ID is different for different region. So Ubuntu 24 HVM SSD volume Type AMI ID for the region North Virginia, which is US East one is this. But if you switch the region for the same name of the AMI, you'll get a different ID. So that keep in mind, so I search here Jammy jellyfish, that is Ubuntu 22 for AP South one. If you see same instance type, you see the AMI ID are different for every region. There are also AWS documentation which will tell you how to find an EC2 instance AMI ID, find an AMI using Amazon EC2 console that we have just seen. We can also find it through CLI, Command Line Interface and we can also find it by using Terraform. Yes, that's what we are going to do the first thing. So the basics of Terraform, we are going to learn by finding first the AMI ID, and then we are going to launch the instance. So first I want you to create a folder. I have created an F drive and in that I'm going to create another folder called Exercise one. Now the name of this folders are optional, I mean to say you can give other names if you wish to. I've just given the name Terraform. You can give any other name if you wish to, just giving exercise one because that's where we are going to write our first exercise. Now let's open VS code and open the folder over here which you have created. So I'm going to say file, open folder, and I select my folder. So we are going to write Terraform code and let's install the extension pack from VS code. So click over here extensions and search for Terraform. Now this is not mandatory but this will give you a good help in formatting your code properly. So I'm going to install HashiCorp Terraform, install and this is just as per your choice you can use any other extension or if you don't want to use extension, that is also fine. Okay, now let's create a file. So we are going to go to exercise one here, going to click on this and here we are going to create a new file. I'm going to name it as InstID.TF. Make sure you give the extension as .TF, hit enter. So as I told you, we are going to first find the AMI ID by using Terraform. Now it's not mandatory, it's just an exercise for you to learn the basics of Terraform. You can find the AMI ID from the console from command line. Also you can find it by using Terraform and you can again use that in the same Terraform code also. So let's see how we can find the AMI ID. So Terraform gives us resources first of all from a provider. Providers are like AWS is a provider. So we are not going to do anything with the resources from AWS provider. We just want to fetch the information and it is outside of Terraform. And to get information that is outside of Terraform, we use data sources. That's how we define it. We say data and we give the resource name, which is going to fetch the data for us. And the resource for that is AWS_AMI. You need to put the resource name in the double codes and this is predefined resource in the Terraform. And I'm going to show you how Terraform downloads information of this resource in a moment. So after the resource you give a name to the resource, which is referred in your Terraform code. So I'm giving name as AMI ID. You can give here anything. And then you need to give the resource block in curly brace. So open and close curly brace. And in between this you give the arguments. So for example, from the resource AWS_AMI, one of the argument is most_recent and its value can be true or false. And all this is in the documentations. You don't need to worry, you don't need to buy heart the arguments or even the resource name also. So now to get the AMI ID, we need to put some filters, the search, right? That's how the data is going to, the data over here is going to find the actual data that we are looking for, which is instance ID. So we have an argument called filter. We can give the name of the filter like what we do search. So we going to write here, filter, open and close curly brace and we give here name. That will be the name of the filter. And the name of the filter is name itself for the AMI ID we have name right and what should be the value of the name that we need to give in the filter. So value is equal to, sorry, it's not in double quotes. The value is in the format of a dictionary. A list, I mean to say. Oh my bad it's "values" with an S. So in the square bracket in the double codes I give Ubuntu/images/hvm-ssd/ubuntu-jammy-22. So that's the name from the AMI ID that we get. That's what I'm putting it over here. Hyphen Star. That means this should be in the name. And then hyphen star means you know, star at the end means it could end with any name. Now it could have some version or anything like that. One more filter that I want to give is the hardware virtualization type. I want HVM. So I give one more filter name virtualization type value HVM. And there is also one more argument owners. Canonical has created this EMI on AWS. So we need to give the canonical ID. So the argument will be here owner with an S, owners. So if you're wondering how do I get all this information, I have just basically taken all this from the documentation, which I'll show you in some time. So please do not worry about you know, all these different names. Just understand the structure. The first lecture over here focuses on understanding the Terraform code structure and execution of it. So once again, data resource to fetch the data from outside of Terraform. And we are using a resource type AWS_AMI, which is predefined in Terraform. So this is resource type and this is resource name, name we can give anything we want. And in this curly brace, this is called as the resource block. In resource block we have arguments. So we have most recent, we have filter, we have owners, right? And the resource will have, sorry, the arguments for example like owners will have a value equal to some value. So value could be a Boolean, like true false, it could be a string like in double course here we see it's a string, or it could be a list. And in the list we can have strings. So based on the documentation you need to find out what will be the value of a particular argument. Right, here values in square bracket set off strings, set basically like we have list in Python. So in square bracket we can have the list and list can have multiple values separated by comma and the values can be strings, Booleans, you know, based on the argument we can check in the documentation that we'll see later. Okay, now this is going to get the AMI ID for us that says it has the name so and so and virtualization type is HVM. Now we can use output to print the output of this. So after this block is completed over here you see for me in line number 15, this data block is completed. We are going to write output Block. Output is just like print we have in Python, we have print in Bash, we have Echo, like that in Terraform, we have output. We can give some name to our output like I'll say instance_ID. And then we create the block open and close curly brace. So we are going to have two arguments over here. One is the description, the other one is the value that we want to print. So description equal to in double codes I will write simply AMI ID of open to instance. That's a description, you can write anything that describes your output. And then the next argument value equal to. Now we want to print the instance ID over here. So let me scroll up. So this is how we can fetch the information. We have to give data. .AWS_AMI.AMIID.id. So what's going to happen when we execute this, there's going to generate a whole lot of information. And if you remember in Python we have seen how to fetch information from you know, within a dictionary. Similar way we have to fetch the information from here. So the value will be here. So it is a data.aws_ami. ami id dot. So this is the benefit of that extension that we installed. Terraform extension. Otherwise we need to go to the documentation to find out. So here you have ID, you know you'll get description, you'll get boot mode, other kind of architecture, these kind of information. We just interested in the ID. So we're going to do that .ID. Okay, before we execute, once again this is data source block. This is the output block. Here, this fetch the information. Here we are printing the information. Now I'm going to save this and we will execute this. So I will click over here, I will go to terminal and I'll say new terminal. So I have set the terminal as Git Bash. If you are getting here PowerShell or Command Prompt, you can switch to Git Bash or you can use PowerShell or Command Prompt also, that is also fine. But from the dropdown you can select here Git Bash. My Git Bash is the default one. Now you have to make sure you are in exercise one directory or whatever you created. Do an LS and check. Do you have the instance ID or not? I mean the instance ID.TF file or not? So first we are going to run the command Terraform space FMT format. So check, it'll check the format. If it's wrong, it's going to correct the format. And you see that it uses the indentation just shifted things from here to there. So a standard format it'll give you. And then also check the syntax by using validate terraform space validate. And that is not going to work because first we need to execute the Terraform INIT command. Let me clear the screen. So Terraform INIT, initialize. Now what this will do is it's going to read your TF files or all the TF files present in the current directory. It'll check all the resources and those resource information, the plugins, it is going to download from Terraform registry. So you just execute Terraform in it. See, that says finding latest version of HashiCorp/AWS. It detected that, that that's the provider, installing HashiCorp/AWS. So it's downing that provider information. This might take some time. Okay, so it says Terraform has been successfully initialized. I'm going to clear the screen and I'm going to run now Terraform validate. So now it'll see whether all the arguments and its value, whatever we mentioned is syntactically correct or not? And since configuration is valued, okay, next step is apply. Now we are not creating any resource over here, we are just fetching the information and printing it. But if you're actually creating some resource or maintaining some resource like instances, buckets, AWS, Lambda functions, whatever you do with Google Cloud, if you're maintaining those things, first thing is you need to check that what is actually going to be impacted. You don't want to delete a few things unnecessarily. So you need to check what is going to happen when I apply it and to check that we can use Terraform plan. Now here for this code doesn't make sense, but next lectures this will make more sense. So Terraform Plan is going to basically check what will be the impact of the resource that we execute, right? So it says it's reading this resource and read is completed and the changes to outputs will be the instance ID there in the plan itself. We got the instance ID, right? So that's what is saying. Saying you know, this will be the change, which basically is nothing. If your code you are writing and it may destroy a few things, delete few things, it'll tell you that it's going to destroy these things. And this will be created and this is going to happen. The whole detail. And then the next step will be to apply it. So we're going to set Terraform apply. So when we apply it is going to again print the information. It is going to say, are you okay with this action? And then we'll say yes, if we approve this, basically you have to read this, but as I told you, we are not making any change. So just simply typing, yes. But if this impacts anything, then you definitely read everything. And if you are okay, then only go yes. Right? And it says apply complete, zero added, zero changed, zero destroyed, right? We knew that already. Outputs instance, ID EMI. So a code here, right? Output instance ID value is this is the one, okay, put create, let me clear the screen and let me type history here. So after writing the code, we check Terraform format. FMT. We did Terraform validate it does not work because we have to initialize it first. So always initialize first, Terraform in it and then check Terraform, validate, and then Terraform plan. So what it is going to do, and then Terraform apply finally. So take a look at the code. Once again, I'm not going to explain it again. Take your time. You don't understand a few things, no problem. We going to learn things slowly one by one in the next lectures. In short, we just found the AMI ID by using AWS_AMI resource. We mentioned few arguments here and then we used output and instance idea as a name to print the value that we have fetched from the previous resource. Okay, that is all in this lecture. In the next lecture we are going to launch an EC2 instance by using Terraform. So wrap this up and join me in the next one.
