# Introduction

Instructor: Hello, and welcome to Terraform lectures. Terraform is a science fiction word which means to transform atmosphere of a planet and make it habitable, like what we want to do with Mars, we want to make it habitable so humans can live on planet Mars. Or in the "Superman" movie where you have seen the Kryptonites comes with big gigantic machines to transform Earth's atmosphere so Kryptonites can live on that or survive on that. So we will be also doing same thing, but not with the planet, but with the cloud computing accounts like AWS. We want to make AWS Cloud account more habitable for our application to live, we can use Terraform. It is a tool from HashiCorp and its primary purpose is to manage the infrastructure on the cloud. It's also called as infrastructure as a code. Terraform has become a default standard for cloud automation, and today's time it can manage almost all the cloud providers like AWS, Azure, Google Cloud, Oracle Cloud, even Docker, and even Kubernetes. So let's check the code, how does it look? Let's go to the Terraform style guide. So Terraform has its own language and it's pretty simple, it's just like JSON. And after writing the Terraform code, you can use commands like terraform fmt format or terraform validate to check the right format of your code. So, okay, here, this is how it looks. So you say resource and the name of the resource, aws_instance. Now Terraform knows what does that mean. Then you give the name of the resource, like web01 instance, for example. And then inside that you give the arguments, like how many instance count, ami ID, instance type, security group, et cetera, et cetera. So when you execute this code, the Terraform tool will run that for you. It will create the EC2 instance if it does not exist, and then it will put all this information into a state file. Yes, Terraform maintains the state of the infrastructure now. Terraform knows that it has created this so-and-so instance and its state is running right now. So let's say you shut down that instance and then you execute the Terraform code again. So Terraform will check its state file and compare it with the infrastructure. If there is a difference, it will apply it. So if the EC2 instance is powered off, it is going to power it on. Or if you have changed the name of the instance, it will correct it again. So it will always maintain the state of the infrastructure. So it's not just automation, it's maintaining your infrastructure. And in my opinion, this is why the Terraform has succeeded in cloud automation industries. There are other automation tools also, Terraform is simple, it comes with a lot of plugins, you can install plugins and you can add more functionality. So it's easy, it's extendable, and best part, it maintains the state of your infrastructure, which is very crucial. Okay, so let's install Terraform now and see how we can use it. We'll create a simple EC2 instance for Terraform, and we'll see if it maintains the state or not. So let's click on Install here. So macOS is pretty simple, you just say brew tap hashicorp/tap and brew install hashicorp/tap/terraform. Okay, so you can use the brew package manager to install Terraform. Just check the commands over here. For Windows user, well, we can use choco install terraform, and for Linux for if you're using Ubuntu or CentOS/RHEL you can execute these commands. Just simply come to this URL and get the installation steps. I'm going to install it on the Windows machine by using choco command, so I'm going to open PowerShell as administrator, Run as Administrator, (screen chiming) and I'm going to execute the command, choco install terraform. Okay, do you want to run the program? I'll just say A too so it accepts all the installation scripts. Okay, Terraform is installed, let me check its version, terraform --version. So mine is 1.10.2, yours might be different, Maybe some other latest version. So whatever it is, you can stick to that. Don't need to get to this version. Okay, after installing Terraform, we need to set up the access key, so we are going to automate AWS infrastructure. So we'll go to our AWS account, create an IAM user, and then save its access key in our laptop or in our computer. So let's log into AWS account. Okay, I'm on my AWS management console and I am in IAM service. So search for IAM, come to IAM, go to User and say Create user, give user a name, I'm going to give it as terraadmin, and I'll just say Next, Attach policies directly. So we are going to do a lot of administrative actions on AWS account, so I'm going to give it administrator access, and go Next, Create user. So user is created, let's create the access key. So just click on the username and click on Security credentials, scroll down, Create access key, click on that, CLI Command Line Interface, I understand, go Next, Create access key. Okay, now I'm going to open my Git Bash, macOS you can stick to your terminal. So I'm going to open the Git Bash and I'm going to run the command aws configure, and it's going to ask me the access key, so I'm going to copy the access key and paste it there. And then the secret key, I'm going to copy the secret key and paste it there. And I know you are seeing the secret key it is quite dangerous, but I'm going to remove this user as soon as this lecture recording is completed. Region name, I'll just keep the same us-east-1, or you can just type us-east-1, hit Enter. Output format json. Okay, now the authentication keys are stored, Now we can start using Terraform. So I'm going to open my VS Code and start writing some Terraform scripts.
