# Networking Commands

Welcome in this session, we will be seeing some networking commands in Linux and also on Windows, and for that very purpose, I have created two virtual machines by using vagrant file. It's the same vagrant file that we used in multi VM section of our vagrant. So one vm is a web01, it has a static IP,40.11 and db01 40.12. web01 is running a Web service apache2 on Ubuntu. db01 is running a database service. Maria DB and talking about port numbers apache2 service is running on Port 80. Maria DB Service is running on port 3306 and we will find that out. So first, let me log into the web01. Consider this just a testing ground. You can run these commands from any Linux machine or even some commands on Windows machine, so I'm in web01 We're going to some we're going to run some command, which you need a root privileges, so I will always switch to root user. First, command I'm running is ifconfig. This command will show you all the network interfaces or the active network interfaces, their names. And their IP addresses. So this is connected to the NAT Network. This is the static IP that we have given and this is the loop back address loop back address, lo, this IP address, 127.0.0.1 This is used when the computer is referring to itself. You can say this is the local network interface. And this will be the address of the computer on this network, 192.168.40 network This will be the IP address. So this computer is connected to two network. One is this one. The other one is this one, 10.0.2,    192.168.40 If I've ifconfig command does not work in your operating system, you can try ip addr show ip addr show So ifconfig command is not available in your Linux systems. So ip addr show So this is just to check your own IP address now, if you have any other IP address and you want to see whether it's connected, like I'm going to give IP address of db01, 192.168.40.12 Well, that's the IP address of DB zero one. So I use ping command, ping command sends, 4 network packets, ICMP packet in Windows Machine in Linux machine. It keeps on sending unlimited packets and how to do ctrl+c ctrl+c So it is sending the ICMP packets to this IP address, and when this IP address receives when this network interface, which has this IP address receives, the packet. It is going to reply back. And you can see 12 packets were transmitted, 12 received, zero percent packet loss. So there is a network connectivity from your machine to this IP address. So that's what ping really reveals. Now, if I want to connect to that machine with the name and not with IP address, I will need a DNS server or I can make an entry in /etc/hosts file. You will find in almost every operating system. And you can give the IP address of your target machine. 168, dot 40, dot 12 and the name db01 Then you can refer to that machine with the name, like you can say ping db01 and you will see it resolves to that IP address. So ping command shows whether there is a connectivity or not, it can show if there is any packet loss, But if you want to trace the complete path that your computer will take to reach the target machine, then you can use Traceroute. Now, Traceroute will not be working properly in a virtual machine, virtual box. So I'm going to log out from here in the Windows machine. Either you can use command prompt or powershell or you can use git bash and you can run trace route command. So that's the command tracert And you can give a destination like I will give www.google.in And hit enter. So this command is going to send three packets to the destination, which is Google dot com, that's currently the IP address, and it is going to show me all the hops that it is going to take to reach the Google server. Hops could be the routers, so it reached at this place, then it took the next hop over here, then the next hop, then the next hop. So it is sending three packets. This is the first. This is second. This is the third. It shows the latency also 256 milliseconds for the first packet with the second and third is one millisecond. So we can consider this as one millisecond. Second router is at this place, that's the third one, so totally 8 Hops. It also shows the IP address or the name of the router that it. Went through to reach the Google server. This command is mostly used when you want to see there is any latency in between you, your computer or a computer to some target machine. You can find out if, you know, if you are seeing some you're seeing all three packets having too much latency, then you can find out, OK, the problem is over here and it's not at your local network or not at the server. The problem could be somewhere in the middle on the Internet. OK, let's login back to the web01 Next, command, we are seeing is netstat -antp, with this option, this comand is going to show all the TCP open ports. In the current machine. OK, so we have port number 22, port number 80. It's not showing PID. I have to be the root user, runthe command once again. -antp OK, so it shows for 22 is open, port 80 is open for anyone to access and this is the process ID service name or the process name, right? This is the process. So there's an apache2 service running on port 80. That's the process ID And there's an SSHD service running and that's the process. And you can run ps -ef command and grep for this process I.D. and you should find it, ps -ef grep, let's say one one or I'll do it for a apache three, three, three or you know, I just see a apache2 process. So I find the process ID, 3306. And from that I can find out what port number, if it's using any port number and I will say grep for 3336 . So I can say this process has opened this port, port 80 The new command is also ss -tunlp There, again, 22, 22, you see the process I also want to see the process ID also process name. This this is to see the open ports on your machine. There's also one more command called and nmap and nmap will not be installed. So you have to install nmap, I will say apt install nmap. And if any command it is saying not present, you can just find it on the Internet. The right package and install that, that command will be available then. Now nmap command, use it carefully, because in some countries it's even illegal to use this command and it also depends from where you are running, what is your purpose or don't run nmap on any website, any public website. And if you know what you're doing, OK, use it for troubleshooting purposes. Only if I just hit nmap. It's going to show you lot of options you can say nmap localhost. It shows the open ports, so 22 and 80 are open ports. You can do nmap on a target machine, like I'm going to do it on db01, and that's actually the main purpose of a nmap. Scanning the ports of a target. So on db01, you have port  22 open port 111, rpcbind and 3306 for mysql mysql This is when you're trying to connect two machines together. They are not able to connect because of some issues from some firewall you can use nmap to scan the port, whether they're open or not. OK, next, command is dig. dig is going to show me. DNS look up, so if I said www.google.com So it is. Its IP address is this. There is A record, It is showing me the records in the DNS server, this is the DNS server. This is used to see whether, you know, your DNS resolution from your computer is working or not. That means a name can be resolved to an IP address or any other records. Thers is another command also an nslookup, which will be also similar to this? And you can say this is an older version of dig. Right, again, name, IP address, name, IP address, that's IPv6  by the way, and that's IPv4 ipv4 And this is the DNS server that is resolving and it's that's the port number. dig  and nslook for dn queries. route -n, this command is going to show you the gateways, so for this network interface, the gateway is this. Or you can just run route. route command, you can update the route tables also, but mostly we're using it to just see the gateways. OK, next, command is arp This command is to view or add the content to the kernels ARP table. So kernel will maintain a table of all IP or name and mapping Mac address, and so its saying for db01, the Mac address is this. OK, next, command is mtr. So this is same like tracert, but this is really live, you know, continuously showing, you know, if you want to see where where is the packet loss and if it's frequent, you know, you can just run it. But as you see. From the vm, there is a problem with this, OK, let's logout see if you have mtr command in windows windows you don't have. OK, I'm going to login again, clear the screen, so mtr will say www.google.com . So it is showing all the hop taken and also where do you have any loss. At this place You see, there's some packet loss and I can see the dynamically. So if you're getting a slow Internet or in between two systems is a slow connectivity, you can find out where the problem is, where the packet loss is happening, or also if you're not able to reach to a destination, it will go to a particular place and then it will start failing. 100 percent packet loss will be there. That means the problem is that that particular place you can figure out whether it's your local area network or it's whether it's the server or it's somewhere in the middle. OK, are you can do ctrl+c to that command. I have shown you a nmap command and you can run it on the target machine. It will show you the open ports, but you can also use the telnet command to see whether a particular port is open on the target machine or not. Like, you can say, telnet and you can give the IP address or the hostname, I'm going to give the IP address now. That's the db01 IP, and I'm going to say 3306, so trying to connect from web01 to db01 on 3306 OK, there is says connected, but the mariadb server is rejecting it now, and I'm not willing to connect to this IP address where the port is open, it was connected. You can try 22, which is for ssh. There you go. SSH, it opened the connection you have to use control and the square bracket, hit enter And from that telnet, you can quit. OK, so these are some commands. You have seen I have config IP addr show. ping netstat, we have also seen Traceroute, but that is outside, we have seen it from the Windows machine because in the vm it does not work properly. So run it from your host machine from your host machine. netstat We can find out a process I.D. and then you can grep for what port it is running on, ss, nmap if it's not available install it dig and nslookup for DNS queries. route command to see the Gateway, arp to see the kernels ARP table. mtr, which is seemed similar, like Traceroute, but dynamic. And telnet There are many more Linux networking command, if you just put it on Google, you'll find so many sites and sometimes the other tools are the third party tools are also very helpful. There are many more commands like tcpdump, Wireshark, but for a basic network troubleshooting these things will be enough for you. We will see throughout the course how you're going to use these tools. And that will be all in this video. I will see you in the next one.
