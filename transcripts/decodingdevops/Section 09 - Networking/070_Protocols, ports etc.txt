# Protocols, ports etc

Protocol in general, as we call it, as set of rules. In networking communication  a protocol is a formal specification that will define the procedure to communicate between sender and receiver or vice versa. It defines varieties of things like the format of communication, timing, sequence, how the error will be handled, and a few other things. Like an extra HTTP protocol or FTP protocol or SSH  protocol. So the client and the server that understands this protocols knows how to communicate by following that particular protocol. Talking about protocol of layer 4, transport layer, TCP and UDP protocol, all the other protocol that we see in layer five, six and seven are divided in between these two. So what's the TCP protocol and what's the UDP protocol there? What are the differences between them? TCP protocol is called a reliable protocol. Like, for example, HTTP, when you're sending an email or uploading your picture by using HTTPS, protocol, mostly that will happen, the web communication. So you have a guarantee that your mail will receive at the end in the same format or your picture will be received or uploaded in the same way as you have taken it. So there is reliability over there. It's connection oriented between source and destination. There is a connection. It performs three way handshake before transferring the data. And it's all based on acknowledgement there'll be error detection if something fails, if a packet gets dropped in the middle or corrupted. The error will be found out and the data will be retransmitted. So that's why it's called reliable protocol. So any application that wants a reliable communication like your browser using a website or from your app, you are ordering your food. So developer while developing this application, if they need reliability, they'll use reliable protocols like HTTP, HTTPS, FTP based on the requirement. UDP, on the other hand, is called unreliable protocol. Yes. There is also requirement of unreliable protocol, these protocols are connection less, you know, just send the data and forget about it, whether it reaches its destination or not. It's much faster than TCP, and that's why it is used sometimes, not sometimes by a particular protocol, There is no acknowledgement. So you know, just send  the data and whether it reaches its destination or not, there is no acknowledgement about that. There's no proper sequencing of data. But sometimes speed matters more than reliability. So protocols like DNS, DHCP, TFTP, these falls under UDP. This is where you need fast data transmission like you enter the URL and that has to be transmitted, transferred into an IP address and it has to be done very quickly. DHCP client asking an IP address over the network, it should be faster. so UDP protocol. So that's the difference between TCP and UDP protocol. Protocols will have port numbers. Default port numbers. Like the DNS has 53 in UDP and 53 in TCP. HTTP port number is 80, HTTPS is 443 and so on and so forth. Maybe take a screenshot of this or write it somewhere or by HEARTT. And many other port numbers, default port that we're going to see throughout the course, it's important for you to understand. What port numbers for a particular protocol, a default port? This will be very helpful when we are setting setting firewalls like Security Group or NACL and we are going to do that in AWS and also some places at the operating system level. So if you're wondering what are these numbers associated with, a protocol like a computer has an IP address, a service running inside a computer will be having a port number. For example, you're running a Web service which is running on which is using protocol HTTPS. So the default port 443, that means it's serving the request on port number 443. OK, think about a food court where you have multiple stalls. Stall number one, you have Indian food. Stall number two, you have Chinese, stall number three you have Italian, stall number four, you have Thai. And I already started getting hungry. But my point is here you have a food court. Think of that as an IP address and address. When you're going to a food court, maybe you're asking someone an address of the food court. You go to a food court and there are multiple stalls serving you a cuisine, port number or stall number one, Indian food, stall number two or port number two, serving Chinese food like that in a computer, you'll have port numbers. 22 serving SSH service. Port 80 serving HTTP, Port 443 service HTTPS, 25 SMTP. So in one computer. Which has an IP address can be serving multiple services also. So that's the relation, the IP address and port number thing. And that's OSI 7 Layer model compared with the TCP IP protocol model. Right hand side you have TCP IP protocol model. So application layer in TCP IP = which is equivalent to 5,6,7 layer. So you have protocol at this layer. Telnet, FTP, DHCP, TFTP, SMTP and so many other. Transport layer. You have TCP and UDP. At the network layer Internet, in TCP/IP we call this Internet layer. ICMP ARP, RARP, IP, The IP address and layer two and one that is compared to the network interface layer of TCP IP protocol model. So left hand side you have OSI model and right hand side you have TCP IP model. So I think now you must be starting to make some sense whatever, we are talking about different layers, different protocols, put numbers and of course, the IP address. So when a computer which is running, let's say tomcat  service, which is an application, a Web application service, wants to communicate with a MySQL server to store some data. A Tomcat server is going to send traffic to the destination IP address of my SQL Server and the port number of the service, 3306 That's the default port for MySQL That's how the Tomcat will have an assurity that it is going to reach to the MySQL server. IP with the port number That's how IP address and port numbers work together. When you have two computers or two virtual machine, you want to connect each other. You should know how they're communicating, what IP address they're using and what port number they're communicating on. And, of course, what protocol they're using. In this course, we are going to see a project called as vprofile, it's a Java based project, and there are multiple services in this project. You have Nginx, Tomcat, Rabbitt, MQ, Memcache and MySQL, all these services we are going to host in different, different virtual machines locally and also on the cloud. So every virtual machine will have an IP address running, running a particular service like Nginx, and that will be running on Port 80, for example, like Tomcat on board 8080, RabbitMQ 5672 Memcache 11211, MySQL 3306 They all will be communicating with each other. So when you work in a project, you should know what service is running in which server, what is the IP address. And it's running on what port number. Because when you connect all of them together, there will be firewalls and other things that you need to make changes to.
