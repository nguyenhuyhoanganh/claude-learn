# Modules

Now we'll talk about modules. A module will be a python script or a package that has some functions or methods available in that like random. So I say import random and then I can use methods from that random.randint or random.choice. So random is the module and that's the method inside that. Now this module already exists. What if I want to create my own modules? Which is a very good idea when you have some functions which you regularly use, it's better. You put them into modules or a module and then whenever you need it, you can import those modules and call the methods. So we'll see now how to write our own module. It's very simple. I'm just going to create a script. I will name it as my modules. You know what, let's give a different name. I'll just name it as Modern. Modern like Modern or Modern. Okay. No Hyphen, nothing, just the name. And I will take all my functions that I have written, like this one, vaccine Feedback and let's put variable length argument one. So only the function, OK, not the calling. That's the second. Let's take the third one. Okay. I will also say here import random. So this is my module file and I'm not calling the function from here itself. I'm going to call it from a different script, a different program. So I will create another script. I will name it simply Caller. Okay, name it anything, maybe have something that already exists with that name. Will let me just call Modern. Okay, in order to call the functions that is in my modern module, I have to import it. Import. I'm going to give the name Modern. Not PY, just the name. And if you want, you can just type here Modern, say dot. It should show you all the functions available in that. Okay. Or you can just run a dir method on that. Pass it in dir and let's run it. I have to print it, sorry, print. So there's some prebuilt modules like doc which is going to return the doc string file, the name of the file name package spec. Yeah, that's the one. Order Food, time activity, vaccine Feedback OK, it's time to call them. Let's comment this. So I'll see modern dot, order Food I'll pass all the same values that I used previously here. Okay, let's see how it works. Nice. So I have to give the module name dot and the function name, right? vaccine Feedback. Let's call the last one Modern. Okay, so that's one way of calling your functions in the module. You can import all the functions instead of importing the module name. So let me give a comment over here. So you can do this from the module name. You can say import. And you can give a name of a method, like if you only want to call that particular method, right? Let's say Order food. Okay, like that. Or you can say Star which will import everything. Okay, let's comment this. You know what? Let's create another script for this. Let's go back. Okay, let's keep this as it is and let's create another script. Maybe I'm creating it at the wrong place to create it here. Okay, this should be good. Okay, create is at that level. I'll give this name call modern two. Okay, so here I can see from the module name import Star. Then I can just directly give the method name like order food and I can pass the values. So now I don't have to say modern dot the method name. I can directly give the method name. So these are two ways of calling your functions in a module. Either you say import the module name then you say dot and the function name. Or you can say from modern import a particular function or all the functions like Star. Okay, then calling becomes easy. You just give the name of the method so all your functions will be loaded in the memory. So that is all in the modules. I will see you in the next video.
