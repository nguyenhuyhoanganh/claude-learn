# Python Fabric

Instructor: Hello, and welcome. In this lecture, we are going to see Fabric, a Python library. Now let's say you have hundreds of servers that you manage and you want to execute commands on them remotely, right? From your machine, from your laptop, or from any other computer. You want to execute commands on them by using Python. How do you do that? By using Fabric. Now there's not by default Fabric installed in Python. You need to install it. And if you want to install any library in Python, you have to use pip. Okay, pip is a package installer from Python or for Python. You can say it's the apt or yum of Python. You can install any library, like you want to install Fabric in Python, you can say pip install fabric. So first let's install pip. You know, you can install pip by using apt install in Ubuntu, and then you can install Fabric by using pip, right? Finally, we have to do pip install fabric. So this is one script. You know, come to this URL bootstrap.pypa.io/get-pip.py. So if you run this script on your machine, it is going to install the right version of pip based on your Python version and based on your operating system, okay? Because you know, the operating system version changes, the Python version changes, the command to install pip also changes with it. But with the script, you can install the correct version of pip for you. So get this URL, go to your Python, go to the script box, basically. You can see wget, Shift + insert to paste in Git Bash. Okay, put this URL and it should download pip. That's the script. Sorry, it doesn't download the pip. It downloads the script to install pip, okay? Once the script is downloaded, you have to execute the script. So you have to say python3 and the script path, hit Enter, and this should start installing pip for you. Okay, so pip is installed. Now we are going to install Fabric by using pip. Not only Fabric, you can install anything by using pip for Python. pip install, in single code you give fabric&lt;2.0. Okay, give this command, this should install Fabric for you. Okay, so the Fabric is installed. Now we'll see in a moment how to use Fabric, but before that I want to show you a few more libraries that will be helpful for you if in case you need them. Like for example, if you want to manage Jenkins with Python, how do you do that? If you want to manage AWS or Azure or Google Cloud, anything by Python. So you can install those libraries by using pip. Okay, now since you have pip installed, you can install any library by using pip, any Python library and use it, right? Let's say for example, you want to manage Jenkins by using Python, right? So just search Google. Let's Google python jenkins library. I see this python-jenkins over here. Let's look at the documentation. Yeah, there. So you know, what all you can do with this library, with this Python Jenkins library, you can create jobs, you can copy jobs, you can delete jobs, update jobs, get bill information, et cetera, et cetera, right? The whole list is here, let me scroll down. Let's see some examples. There should be examples. Okay, so Get versions of Jenkins. Let's click on that. And that's example one. So first, you have to install this Jenkins library. Let me go back. Okay, so that's how you install it. pip install python-jenkins. First, that's the step. And then let's go look at the first example. So then you can say import jenkins. You have installed Jenkins library, now you can import it in your script. And then, here you see the first step is to login. So in a variable, we are storing Jenkins. That's the library, .Jenkins. And that's the function, right? That's the method. Parenthesis, and you give URL, username, password that will login into Jenkins. And then you can do stuff like this. So once you have the login stored in this variable, then you can just server.get_whoami or get_version. That is stored in these variables and then it's simply printing. Let's see a few more examples. At server, server.job_counts. Okay, this is the variable which stores the login details, right? It takes a method and store it over here, right? Then in that, you have a lot of functions that you can use, right? So count the number of jobs. Working with Jenkins job. At creating a job, so the job should be in the XML format. Okay, so like this, there are many examples. First, you have to do some R&amp;D. And slowly, slowly you can get the script that you want. Likewise, let's see how we can manage AWS by using Python. So there is a very popular library called Boto. So just search for boto3 python. So Boto3 is like, you know, works with Python 3 like that. Okay, so let's take a look at the quickstart guide first. Click on Quickstart. So here it is, pip install boto3. That's how you can install Boto3, and you can also mention different versions of Boto3 if you want. Now configuration, you have to run aws configure and you store the access key and secret key. 'Cause Boto is going to contact AWS, it's going to use AWS APIs. And for authentication, you can use the access key and secret key. That you can set in the command line by using aws configure. Now let me tell you one very, very important thing over here. There are ways to store access key and secret key in the source code in the Python code itself, but never do that. It's very dangerous. Your access key and secret key should be not in the same place where you have your Python code. Like in this case, we are storing it through command line, which it'll be stored into the home directory, right? With AWS folder credentials. And your Python code will be in some different directory. So that is fine. But storing it in the Python code, you will store it then in the repositories like GitHub and there is a chance of accidentally exposing your keys. So be very careful with this. Okay, so how do we use Boto? Well, as an example of S3, we have to first say import boto3. And a variable is taken here, s3. That's just the name of the variable. You can give anything. But this is the way, boto3.resources. In the parenthesis, there is s3. So it's going to store the S3 class object into this variable. Now you can use this s3, right? So if you want to print all the bucket names, you can say for bucket in s3.buckets.all. So here S3 gets, you know, a class object. In that, you have the method buckets.all. It's run in a for loop and we'll be printing bucket.name, right? This variable. We will have a method called .name and that will have the name of the bucket. Here's how we can print all the bucket name, right? If you want to store a file, upload a file, that's how you can do it. By using with function, you say, you know, open a file as data and then with s3.bucket. The bucket method you can mention in parenthesis the name of the bucket, .put_object, which object you want to upload, Key equal to the file name, right? So that is how you can store a object in the S3 bucket. Like that, you can see many sample examples over here. For example, if you do here Code Examples, and let's go to CloudWatch: creating an alarm in Amazon CloudWatch. Let's click on that. So let's slowly come down. So list alarms of insufficient data through pagination interface. That's how you can do create an alarm here. So boto3.client; in parenthesis, you give the object name CloudWatch. And this is stored into this variable. Now you can use this like cloudwatch.put_metric. And this function is pretty long here. Basically we are passing the arguments, that's all we are doing. In this function, we are passing all these arguments, okay? Like AlarmName, EvaluationPeriod, MetricName, Statistic. So these are basically all the variables that we are passing as parameter to this function that will create the alarm. So like that, there are many examples. You know, we can go on and on on this, but the best way to learn is you actually start doing some experiments with this. But again, be careful with your access keys, right? I just wanted to show, you know, we are using Fabric in this lecture, but I just wanted to show you, you know, there are many other libraries that you can use in DevOps. These are just few examples. Okay, so let's get back to our script box. We'll see how we can start using Fabric for automation. So my VMs are powered off, I checked vagrant status. They're powered off. I'm just bringing them up. Okay, I'm going to login to script box. Switch to root user. Let me clear the screen. We have folder opt/pyscripts. If you don't have this, you can create this folder or you can create your script anywhere if you want. And this I'm going to create a folder called fabric. We'll see into that. Okay, now this is how Fabric works. You need to have a fabfile, fabfile.py. So I'm going to create a file, fabfile.py. And in this, you mentioned the fabric function and that you can execute through command line. Yes, Python functions you can execute through bash shell or from command line. This is very powerful. But before we actually execute something on remote machine, let's take a look, you know, how this works. Okay, so I'm going to INSERT mode, and I'm going to say here from fabric.api import *, that means import all the methods available in fabric.api method, okay? Now before we actually use Fabric methods, I want to write here one function, a normal Python function, and I'll show you how you can call it from command line. So how do we write function in Python? We say def and we give the function name. So let's say I'm going to give this function name as greetings. And I'll say this takes a variable, an argument called msg:, and simply print a message. print, parenthesis, in double quotes I will say first Hello, and a variable. So I'm gonna put the curly braces. Then I'm going to use the format function, .format and give the, oh, the argument msg, which will be the variable. You know what? Here instead of say Hello, I'll say here Good. Okay, so it's a normal Python function, nothing Fabric about this. Just wanted to show you how you can execute Python function from command line by using Fabric. So just save this. And now you need to be the same place where you have the fabfile. And here, you can run the command fab -l, which is going to print the available commands or available functions in the fabfile in the current working directory. You see it says available commands greetings. Okay, so I'm going to call this function, I'm gonna say fab, and you have to give the name of the command, okay? This is a Python function, but in Fabric, we call this as command. And to pass the argument, you have to say colon because greetings takes an argument. Call msg, right? And we have to pass its value. So I'm going to say Morning. If you have multiple values for that function, you can pass it comma separated. Okay, take a look. I executed this passed Morning into that, right? That morning argument was stored over there and then it simply printed it Good, right? You can see the output over here and the end you will see done. That means the function got completed successfully. Okay, now let's do something Fabric about this fabfile. We'll use some fabric function. So we need to write one more function and I will say this function name is a system_info. Just a name, anything you can give. Parenthesis, no arguments I'm taking. And I want to print the disk space of this machine, the script box. So first I'll give print function. I'll say print Disk Space, and ask you full stop. I'm going to use a function called local. local, parenthesis. And in this, you can give the commands, the Linux commands that you want. So df -h. So its local function is coming from here, right? From fabric.api import *. So it loads all the available method. This is one of the method that we are using. local method is used to execute command on the current machine, on the local machine, in this case, the script box. Like that, let's give few more commands. Okay, it's free -m and uptime with just a print message. That's all I've given. You can give any command of your choice. Make sure you know the command that you're giving just works. It's an actual command, because it's going to execute this command on the bash shell. If it's an actual bash command, it'll work and return the output. So let's save this and let's see what we can do. We clear the screen. Again, make sure you're present in the same place where you have the fabfile. You can again do fab -l. Now you see system_info there. So we can say fab system_info. That's the function name. Okay, let's execute this. Okay, now observe the output. That's the print message, localhost. It's saying that the command is executing on the local current machine and the command is df -h, and here's the output. Same with the memory info, same with the uptime command. Now you can make it more readable if you wish to. Not giving some space or maybe store this value into another variable. And from that, extract data. You know, all that you can do through Python, right? Like first after storing it into a variable, you can run it on a for loop, you know, and find the data that you're looking for, let's say available display on a particular partition, stuff like that. Okay, now this was executing command locally, but our main task was to find out how to execute command remotely. So there are other methods available, like you have local like that. There is run, there is sudo. But for that, let's prepare our remote machine. We need to have a user on the remote machine. So we have web01 and web02. So we'll create a user on that, on those machines and add that user into sudo's file. And that user information, we need to give it to the Fabric command. Okay, so let me exit. Make sure web01 and web02 are up and running. I'm just going to do vagrant status here. Okay, it's running. So first, I'll login to web01. Okay, my bad, vagrant ssh web01 I should have said. Okay, sudo -I. Okay, first we need to add a user. I'll say useradd, I'll give the name as devops. Let's give some password to this user. I'll keep the password very simple and even tell you, it's admin123. In real time, you have to make sure you give some complicated password. We will store this into sudo's file. We need to have this user sudo's privilege because we will be executing some root commands from this user. So we'll add it into the sudo's file, just do visudo. Okay, find out this line, root. I simply copy paste this line. Oops, not two times. Okay, so devops user is in the sudo's file and we are giving you NOPASSWD. That means when we do sudo and execute the commands, it'll not ask the sudo's password. Or we can simply add this user into the real group. That also we can do, but let's keep this much for now. Okay, now by default password login will be disabled. So we need to enable password login. So you need to open this file, /etc/ssh/sshd_config. This file, open this file. And find out a line that says PasswordAuthentication. That one, we found it. Password Authentication. Okay, remove the hash in front of it to make sure it's like this. PasswordAuthentication yes. So we can login with password. Save and quit. Now you have to make sure you restart the SSH service after that. So web01 is CentOS machine. It's RPM based. So the SSH service name is SSHD. Okay, this is done. We are going to do the same thing to web02. Login to web02. Let's switch to root user. Let's add the user, same username I'm keeping and we have to make sure we use the same password at least for now. Okay, start the password. I will add this user into the sudo's file, Okay, so make sure you add that line, devops user, and make sure you give here NOPASSWD. And save and quit. And let's also add this user. No, I mean enable the password login. Open this file, /etc/ssh/sshd_config, Find the PasswordAuthentication line and remove a hash in front of it. The line should say like this, PasswordAuthentication yes. Or if it's no, turn it to yes. And save the file and we have to restart the SSHD service. Okay, let's exit from here. We're done. Okay, let's go back to script box and let's switch to root user. Okay, now I'll do one more thing. It's not mandatory, but executing command, you can mention the IP address. But instead of that, using hostname looks much better and it's more manageable. So we'll add IP to host mapping in the etc/host file. So let's get the IP address of web01 and web02. So in the vagrant file, you can get the IP address and then just open etc/host file. We'll add here the IP address of first web01, and then web02 also will add in the same place, it's 38. Now check the IP address properly from the vagrant file. Okay, so that's the IP address of web01. This is the IP address of web02 for me. I've given the hostnames. Okay, let's try to ping it with the hostname. So I'll say ping web01 -c 4. So four counts of packet ping, and that's working. We are getting the reply back and we are also getting the IP address. Okay, good. So we have a user, we have the password for that user for web01 and web02. Now I'm going to share the access, sorry, the SSH keys, I mean. So I'll say ssh-keygen to generate the keys. So I want passwordless login. Key-based login basically. So I'll generate the SSH key, store in root users home directory. And then I'll say ssh-cop-id, the username, @ the hostname. Hit Enter, yes. Enter the password of the devops user. So key is copied. Now let's do the same for web02. Enter the password for devops user. The key is copied. Now we can login without password. That means we can use key-based login. We can see ssh -I ~/.ssh/id_rsa, that's the key, the username devops@web01. Okay, so we are in web01 now. Exit from web01 and you'll come back to script box. And you don't even need to mention this login key because by default is going to use that as the login key. It copied the public key. That's the private key that is going to use always to login by default. So without giving the key path also, it should be able to login there, right? We did all this in bash scripting. Okay, just a revision you can think of. Okay, we are all good, we are all set. It's time to come back to Python Fabric. So we'll go to fabric folder. Okay, so come to fabric folder now and let's open open the fabfile.py, and let's add some new functions over here. I will name this function as remote_exec. and just some name you can give anything of your choice. And colon, I'll directly give the function name, which is run. Okay, local executes command locally on the script box. run is going to execute command on the remote machine. So you mentioned the command. So I'll just say hostname. Normal command. With run, you can execute command for normal user. Root commands, you cannot mention, like you cannot do herey yum install something. Because yum needs sudo's privilege or root privilege. To execute such command, we'll have a different function that we'll see later. So you can give some series of commands over here, like I've given uptime, df -h, free -m. Now I'm using double quotes. Okay, once you've given some commands over here, then you can save and quit this file. Okay, then you can run fab -l. Okay, so we can execute the remote_exec, but we need to also supply the username, the remote machine, which is web01 and web02 we have to mention that. So we have to say fab -H, and I'll give the hostname web01, which trust it to the IP address, right? And then -u, the username, and then the function name. Now see if we want to pass the password, we can say -p and the password, but that password will be stored in the command line, right? So that is the reason we exchange the keys. We copied the public key to web01 and then web02. So we can login with the private key. So fab command will also, by default, use your root user's private key to login to this user on this machine, right? So we can just give -u, username, and the function name. You see that? We'll get the output Executing task, run on the hostname, web01. So you've seen the square bracket, right? web01 everywhere. That means this is happening on web01, not local, right? Command and the output, command and the output, right? Likewise, let's do it also on web02. Just need to change the hostname, right? You understand how powerful this is. You know, if you have hundreds of server, you can execute commands on all of them through command line, just like that. You just need to have the right function. You know, along with this, let's also add other commands like installing some packages. So now here we cannot use run method. We have to use sudo, because this is going to actually, wait, wait a second. Yeah, sudo. It's going to actually elevate the privilege, right? So what command we mentioned here, like for example, we'll say yum install, let's say, unzip zip wget -y, simple packages. So now with sudo function, it is going to run the command like this, sudo yum install, and the package names. Okay, let's try that. Let's save and quit. And let's try this. Clear the screen. Or you can write a totally different function if you wish to. Let's do it on web02. You see that? Disconnecting from web02. So completed, right? So it installed the packages. But here, you can see sudo yum install and the package names and the output. Okay, so now we will write a fabric function that can do these steps. We already did this many times in this course. In Linux, in the bash scripting, in AWS, we install the httpd package. We start and enable the service, we download the artifacts from tooplate.com, extract that artifact into /var/www/html, We restart the service, and then we see we can access the website from the VM's IP or EC2 instance IP address. So now we'll write a fabric function that can do all these steps and we'll execute it against web01 and web02. Okay, so open the fabfile.py and create a function. I will limit as web_setup. And this is going to take two argument. One will be the URL, this will be from tooplate.com. We'll pass the URL as an argument and also the direct free name. So when we download the artifact, we get a zip file. In that zip file, there will be a folder. We need to give the folder name over here. And the reason that I have given this is because the folder name sometimes is different. So we can manually download it, extract it, and see what's the folder name, and then we can mention the URL here and the folder name here. So the first step will be to install httpd package web01 and web02 are sent to us. So we are going to download the package named httpd. Start and enable the service. So first three print function is going to just print a message, installing dependencies. Then we have the sudo function where we install httpd, wget, and unzip. wgate will be used to download the URL and unzip to unzip the artifact that it downloads. Then print a message, Start &amp; enable service and Start &amp; enable httpd service. Again, we need to use sudo method over here, systemctl is a root command. Okay, next we are going to install zip and unzip with local. That means we are going to install it on the script box, because we are going to download the artifact in the script box extracted and then we are going to zip all the file inside that. And then later, we are going to push that zip file to web01 and web02. So printing a message, Downloading and pushing website to webservers. Then with local method, again, this is happening locally. So we say wget -o for output. So the output will be with this name. And here we have %s, that will be replaced by the WEBURL, which we get here. So download the artifact with this name. Then with unzip method, we are going to unzip the zip file that was downloaded in the previous step. Next, so when it is unzipped, you'll get the folder name. So we need to cd into that folder, zip all the content. That will be basically the HTML files, the images and CSS. And then we can push that zip file with all the content to /var/www/html. So Fabric provide as this one with lcd. lcd here means local cd. Into the local folder, it'll do cd. So when we unzip over here, we get the folder. And with this method, we cd into that folder. And with local, we are going to run zip command. That is going to zip everything with the name tooplate.zip. So this zip file will contain the HTML files, the CSS, the images, everything that will be contained in this zip file. When we use a put method, this uses SCP. It is taking three argument. The first is the source, which should be in the local machine. The second argument is the destination, which is on the remote machine, web01, web02. And this folder is owned by root user. This command, put command will execute with devops user. So we need to sudo. So that's why the third argument use_sudo=True. So it's going to push this zip file into this folder with root user privilege. The next step will be to login to the machine and extract this zip file. So with cd. cd here means SSH first to the remote machine and cd into this folder, right? lcd is local cd. Just cd is the remote cd. And then with sudo method, we are saying unzip tooplate.zip. So now /var/www/html will have all the content. Next, we restart the httpd service. So with sudo, systemctl restart httpd, and then just print a message. Right, so in this fabric function, we have seen new methods like with lcd, with cd, and put. Let's test it out. So I'm going to save and quit this. So to execute this fabric function, we need two arguments. One is the URL, which is from tooplate.com; and the other will be the folder that will be inside that. So let's check that. Okay, so from tooplate.com, I select one template. This one. Okay, hit F12 on your keyboard. I'm on the Brave browser. The F12 works over here. And go to Network, click on Download, cancel the download. I just need this link. So click on that zip, pack.zip, and copy this URL. This is the download URL, copy this. Okay, let's execute this function. First, let me list fab -l. So we need to pass the username. So fab -H web01 -u, the username devops, function name web_setup. And we need to pass two argument. One is the URL, which we need to get from here. So you have to say colon and the URL. And let me download this URL and see what is the folder name inside that. Let's click on Download. Let it download. Okay, so that is the folder name. So let me click here. Okay, so the folder name is same basically. Except there is no .zip, but sometimes the folder names are different. So make sure you extract it and see what is the folder name inside that. And then you can execute it. Okay, execute it on both the machine, web01 and web02. Check it by using its IP address from the browser, whether this works or not. Okay, completed successfully. Let me check from the browser now. Just keep in mind it'll take time to write automation scripts. It's not going to happen overnight. So take your time. And most of your time will go in R&amp;D. Very less time will go in actual, you know, writing the script. Most of your time you'll go in R&amp;D, understanding the functions, understanding the methods, you know, and getting the output that you looking for. And there's very less time that will go in final execution, right? So yes, then that will be all in this lecture. I will see you in the next one, thank you.
