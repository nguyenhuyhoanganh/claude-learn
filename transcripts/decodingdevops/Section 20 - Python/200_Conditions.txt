# Conditions

Welcome to the decisionmaking session. Now you don't have to make any decision. Just the Python code is going to make decision. It's similar as what you see in the Bash scripting conditions. If this is true, then execute the code. Else execute something else. Or we can have elseif also. Okay, so this session is going to be very easy. Again, we'll just see the Python way of decision making. So let's create a new script. I'll call it as conditions. First we'll see if condition. Pretty simple. Again, let's compare the integer number, x is equal to 21 and let's say if and now we'll use our comparison operator. We'll say x less than 30. If that is true, then make sure you give that colon there at the end. Okay? That colon hit Enter if you're using PyCharm, it should automatically give you the space. So you don't need to do your indentation yourself. Otherwise, count to three or just hit Enter. If you are in PyCharm editor, it should automatically give the indentation print. I am inside if block and I'll say yes, x is less than 30. Okay? PyCharm asked me to remove the parenthesis, so I removed it. So that is, the best practice is the standards in Python eight standards. So whenever it does something like that, right? Whenever there is a problem, I'll give here parenthesis. So it should highlight something like this. Not red means an error. But even though if it's like this, some off white or yellow color, it should tell you, you can say right click show context actions. It says remove redundant parenthesis. Okay, one more print statement at the end. Let's see. This is the rest of the code. Let's run this. So that condition is true. So it executed the two  print statement. Let's make the value as 31. So the condition is false. So it does not enter and execute your statement. It will execute then again the rest of the code. Okay, just take care of the indentations. So I will give here as 21 and then let's also copy it one more time. There. I'll give it as 31. Okay, that's if. Let's see now if else. So how's the syntax? Let me do a multiline comment on all this. Okay. So just I don't see that output now. Okay, let's execute that nothing good. Okay, let's again take a variable x equal to 31. I'll say if x is less than 31. I'll say if that is true, then I'll keep here as 30. So that is false. So it will not execute a print statement. But I want to do something else. If that if condition is false or else colon and the else code, let's execute. All right. So this condition is false. X less than 30, that is false. So it is going to then execute the else part. Okay? Like if and else, we can also have multiple condition checks by using elif. x equal to 41. If x is greater than 40. Let's make this as also 40. Okay, so x greater than 40. That is false, actually. It should not print that statement. And we say else x is less than 40. Now, that is also not true. X is neither less than 40 or neither greater than 40. It's equal to, right? So if you want to check that test case also we need to do one more test so we can use elif. Okay, we'll say X equal to 40 and we can say yes, x is equal to 40. Let's take that elif first and let's execute the code. So code is syntactically, right, but logically it's wrong. It is saying X is less than 40. That's not true. X is equal to 40. All right, so let's put that elif. Now it should give us right answer. X is equal to 40. Okay, let's take a few more examples with condition. We'll include our data types also list and Tuples and dictionaries. And we'll also use the membership operators. So I will name this program as conditions_variables. Let's put multi line comment. Or you can test your skills now. So far on data types, operators and conditions. Okay, three simple print statement. That is for script. So it's going to print this It organization has various skill sets. Find out your match and enter the value in capitalized. So let's execute that. Three simple print statements. Now let's get our data types. So I will have one list called as DevOps. DevOps skill set in a list format. So I have strings, jenkins, Ansible Bash, et cetera, et cetera. That's a list. Then I will have a Tuple with name development. Okay, few more strings. NodeJS, angularjsjava, net, python. And I'll have two dictionaries. So let's say DevOps and development. These are two skill set that my It organization has. Plus we have some contract employees also So contract employee equal to the dictionary name skill and the employee code key value pairs. Let's have one more person rocky skill, AI and code employee code. Now, this is going to be interactive script. We'll take input from the user. The user enters a value and we'll be checking that whether that exists in all these data structure that we have. So we'll take a variable user underscore skill equal to and we'll take input from user. So you have a function called as input. And this will be prompt for the user, whatever you put in the double quotes. And I'll say enter your desired skill and I'll just show it first how it works. So when I run the program, it's going to take an input from the user that will be stored into the variable user underscore skill. And then we are simply printing it for now. All right, so enter your desired skill. So I have to enter something, simply printing it back. But now we'll do something with that user input. Let's comment that. So we'll say checking or check in the database if we have this skill that's my comment. So database is really the list tuples and the dictionary. So I'll say if user skill in DevOps, if user enters some skill, I'll be checking that in the list DevOps, okay, membership operator checking and that should return true if that exists and then we can print a message to the user. I'll use format function if we have the variable user skill in I'll just give the string over there DevOps team. So if it exists there, it should print it but if it does not exist, I would like to compare it with in other data also other database, data type also. So I'll say elif  user skill in development, I'll check it in the development which is a tuple and if that exists I'll print a message we have user skill in development team or if it does not exist in that DevOps list or development tuple I would like to check in the dictionary and I'm going to check it, I'm going to check the values, skill is the key and I'm going to check the value so I can use some functions and dictionary that returns me keys and values. So I'll say elif user skill in first dictionary.values. Okay, that function is going to return only the values also I would like to check it if that exists in another one. So or operator there, userskill in second one values. Okay, so logical operator or if this is true it's going to return hold as true. If both are false, then only it should return false. So I don't need to write two elif. I can just write one elif, right? In that I can just use my logical operator and do the comparison. Okay, if any of this is true, I'm going to print that message that we have contract employees employee yeah, that should be good. Now if it doesn't find any of the place not in DevOps, not in development, not in the dictionary, counter variable or sorry contract variable then else I'll just say print skill not found and also print a message. Please check if you have entered the value in capitalized because we're just using capitalized value and j capital, a capital, b capital. User may enter anything. Sure, this program is not foolproof but it's a good practice for you. Okay, let's use the print function here also if we have contract employee with user skill okay, let's see how it works once again, let's check three print statement storing the variables. We have list, we have tuples and we have two dictionaries, takes the user input and stores it into the variable user underscore skill and then check whether that value exists in the list, exists in the tuple or exist in dictionary. Now directly we don't do membership operators to dictionary. So dictionary dot values that is going to return me list of values. There are also dictionary dot values. It's going to return me list of values so it can use membership operator if any of the operand returns. True. Everything will be true. Okay, let's test this program. Now, let's run okay, taking the input, enter your desired skill. I will say Jenkins. It says we have Jenkins in DevOps team. Right? Okay, let's run a few more tests. Let's say Python. Something's wrong? No, I think that's right. Well, Python exists in both, so we can actually use membership operator again. And sorry, logical operator. But you know what? Let's not do that. We'll do that in the next programs. We'll enter some other values. Java. Yes. Okay, now let's go for AI. Artificial intelligence. We have contract employees. Check that. Okay, let's now execute something that really does not exist here. I'll say IoT, which does not exist anywhere. So this program was to implement all the previous skills that you have got in Python. And that should be all here. I'll see you in the next video.
