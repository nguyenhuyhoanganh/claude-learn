# Copilot AI for Cloud Automation

All right. In this lecture we are going to achieve the same thing what we did in previous lecture, except this time we are going to use GitHub Copilot. So ChatGPT GitHub Copilot. Both are generative AI. But GitHub Copilot is designed for development and instead of generating the whole code at once, we are going to do it step by step. And once we are good, we are also going to see how to generate again the whole code, create the new workspace with all the information. So I'm going to create a new Python file. I'm going to name it as two plate aws.py. And I'm going to open the inline chart control I or command I. And first let's ask it to create the key pair. Use Python boto3 to create key pair. Okay there it has given the content. Now you can either accept or you can ask more or you can ask some changes like for example change the key pair name. pairing. So either you can accept run this and test it, or if you're good, this is simple. So you can ask it to add more things like create security group. Try to make your sentence as short as possible and as much as specific detail you can add over there okay there. Let's take a look. So it's creating the security group here. It's create the security group. And over here it creates the rule. So two different methods or modules are used in EC2 one create the security group without any rule. And then in that security group add the rules. So maybe you're good with this. And you can accept this. And you can test it if you want. Now if you're really going to test this, make sure you delete this once you created all this resource because you need to add more things over here. Now once again, if you're good with this you select it and again do control I or command I and ask it to create an EC2 instance with the information. So EC2 instance with this security group and key pair with that little more information. Amazon Linux 2023 T2 Micro UserData to set up website from two. Com and if you want to add any other detail, let's see. Okay so first is get it's getting the ami ID and then the user data it's assigning to a variable. Check that cool from back. Yeah. Make sure you check this URL whether this exists or not. Sometimes it takes a fake URL or URL that does not exist. So just take this URL, go to your browser, check whether it's downloading the package or not okay. Take a look at the code if you're good with this. If you maybe you tested it and everything is good, you can accept this. All right I'm going to save this. So maybe now you want to test it, right. Run it and test it. See whether it works, whether it works or not, and then delete all the resources. Because again we're going to add more things. So now I want to add load balancer. So once again I select all the code and I say control I or command I. I want a load balancer. So I'm asking it to add Alb application load balancer and register this instance security group rule to allow port 80 from anywhere. It's me. It won't change security group rule of ELB to allow port 80 from anywhere. Let's check this okay. It's done. That is also completed. And we checked it and everything is fine. Like the lines of code are good and we accept the change and maybe we want to test it again. Okay. It's asking to accept once again. Oh different lines of code. Fine. We accept everything. Let me save the file first. Control s so it's just one script that does everything. You know what if we are going to expand it later, you know, to use more things. And we do this things commonly creating security group, creating load balancers. These are common things. So it's always better to have a modular structure, functions which you can import, and then you can call. So first let's ask you to create the modular structure for the script. So once again select everything command I control I asking it that I need a modular structure for this code. Let's see what it does. You see it started creating the functions for every task like creating security group one function. Get the latest Amazon Linux EMA, another function, launch EC2 instance, and so on and so forth. All the tasks it has created as a function, which is better. And I'm good with this and I accept this change. I accept all the lines and once I'm good, I can review it again. Now all the functions and at the end it will be calling those functions. Yeah. There's the main function that, you know, create assign some variables for subnet IDs VPC ID and they're creating application load balancer. It's calling the function and passing the arguments with the rules also for the security group. Get latest Amazon Linux Ami. Storing it into the variable user data. Storing it as a variable. What? So this is good. But it can be, you know, more better. What if we need more comments. We need to be more readable. So we need comments and doc strings. So let's select the code once again command I or control I slash doc. We can use this command. It will read our code and generate as much as documentation as it can. And later we can add more also okay so it generated documentation doc string for main function. And that is all. Let's accept that. See that main function to automate deployment of application using AWS using Boto3 and all the steps it has given. You know we need more documentation. So I'm going to select once again command or control I, I will ask it to make this more readable. Add comments and docstrings. So in every function now it is creating a docstring. You can call the docstring. When you call the function, you can give the function name slash docstring to call this to see in other scripts that what this function is about, right. So every function if you see it is creating docstrings right. Which made the code more readable or functions more readable. Except all the docstrings. See that we see some issue or we want to make our code better. We do it, you know, step by step, just like agile, iterative. And if you are a developer, if you have developer mindset, you'd be thinking about unit test. You know, before I execute this code we should have unit test cases. So for that also we can use GitHub Copilot. We can again select all the code command or control I slash test command. You can use this to generate the unit test. Configure test frameworks. It's it's asking so I'm going to accept it. Okay. First one unit test standard Python test framework. And this folder root directory Python files ending with test no I'll use this name_test.py. Let me click it view in chat okay. Not do anything I'm going to do it once again control e command I slash test. Yeah. So it's writing a unit test. And how about I accept it. Now I'm not going to go through this code now. So it has just created the code in a separate file. I need to save this file. So I'm going to do Ctrl s to this file. I'm going to give name it as two plate aws_test.py same script name with_test.py okay. So we have our code in this file and the unit test in this file. You see how much we can achieve with this AI tool, this GitHub copilot. And once you are comfortable with this, once you get slowly, slowly comfortable with this copilot, you can even ask it to create the entire workspace with everything complete modular structure. Okay, so one last thing I'm going to show you. Let me close this. And this. Oops I need to save it. Let's save it okay. Now I'm going to open the chat. Click on this drop down say open chat. And over here I'm going to say slash new. So it creates a new workspace. I'm going to mention everything that I know for my projects that I'm going to put all these things in one chat and ask it to create the entire workspace for me. Okay, so I have written Python boto3 modular structure and from the beginning itself I'm saying modular structure to create key pair security group. I have mentioned the rules EC2 instance with all the details user data script for the EC2 instance, create load balancer and all the resource names should be after template name from two 8.com. This one I forgot in this one the previous script. We'll fix that, but let's generate a workspace here first. Okay so it's working. Okay. So it's saying this is the proposed structure. You see it's going to create a folder in that you'll have. So you'll have different files with their functions for key pair security group load balancer. You know let's create it and see. So click on Create Workspace. Select your folder I'm going to keep it same AWS cloud. Select parent folder. Here it should create a folder. You see that. So it's generating all the scripts in the middle. You can cancel it if you want but let it complete now okay it's done. Let me close this chat. Let's take a look. So in SRC we have let's look at the keypair.py okay. So a function to create a key pair. And then you have security group. You have a separate file to create security group you have EC2. There are many mistakes in this code. You know if you go back to Main.py, you can see it's from config import resource underscore name which is capital. And here it's small case. Also it's importing my underscore IP from config. But there is no such variable right. Few other mistakes. If you see here we need to import these files. Either you can say from let's say from EC2 import star from security group import star. Then only these functions will be accessible. Create security group. This belongs to this function. Belongs to this file. Create security group. We have not imported this file in Main.py. We as in the co-pilot. But you might get a different result. I'm just showing you mine. And as the time pass, it grows. These AI tools will become more intelligent. But in any case, you need to take a look. You will find mistakes. So at least now it is not recommended to generate the entire code and go through it, because you will be doing a lot of reverse engineering to fix these problems. The initial method, which I told you step by step add things, test it. If it's working, then keep adding. So you have to use these AI tools as your assistant's assistant, not as a replacement. Okay, anyways, let's go back to our code. So a function to create the pseudo instance we have load balancer. So you see since now now it is separate into different different files. It's easy to read. Again you can go to the individual file and add comments and docstrings. You can ask copilot to add more documentation to it. In the main.py you have the main function where the boto3 is imported. All the other necessary things are imported. So also importing src dot config. So from config which is this one config.py. And so src is the folder src dot config. From this it is importing resource underscore name and my underscore ip. And there is no my underscore IP over here. So we might need to add that. And if we go to that variable you see my IP is not accessed. Quick fix to remove unused import. You won't do that. So this entire workspace is far from perfect. But you know what to do. You know when the things fail, you will know why it is failing what is not defined. And you can try fixing that either manually, or you can ask copilot to do it for you. All right then. So last closing speech, the whole point of talking about this AWS cloud automation with Python by using AI I was to show you how you can achieve all this step by step. So go slowly. Step by step. Take. Take your time. Yeah. People say, you know, generate the entire code and DevOps and developers work will be gone. You see, it is just a tool that we can use to make our life better. And we can code faster, more efficient way. We definitely cannot give complete control. We cannot ask it to do everything and then, you know, run it and expect that it works. So do it always step by step. So in this case, you know, you already had knowledge of AWS. You have knowledge of Python programming, basics of Python programming. You have seen Boto3 and now you're working with Copilot to get your scripts done. So that would be all in this section of the course. I hope you have learned a lot and enjoyed using the AI tools. Thank you very much for practicing with me. I will see you in the next section.
