# Operators

In this video we will be seeing Python operators. The different types of operators in Python to do different kinds of operations. For example, arithmetic operators like plus minus, division, multiplication, modulus. To do arithmetic operations, you have comparison operators like greater than, equal to less than. You have assignment operators to assign values. You have logical operators like add or bitwise operator, membership  and identity operator. Let's see these operators. This time I will be using Jupiter notebook. So let me create a new notebook. I will name it as operators. Okay, first let's see arithmetic operators. Very simple. Okay, let's take two variables. Let's take x and let's take y. Two very popular variables from maths. 2 and I'll say y is equal to five. And let's do addition. Let's say total variable equal to x plus y. That's simple. Print, print total. Let's run it. Answer is seven obviously. So plus is the arithmetic operator. Equal to is the assignment operator. Let's overwrite the value of total. Now equal to x minus y. I also have to print it of course. Print. So the minus arithmetic operator, let's again overwrite its value. Let's do multiplication x star y star is the multiplication operator. Let's do division. Let's divide y by x. Let me increase the value of y. Let's take it to six print. Okay, let's see how we can find the remainder. Let me increase the value of y by seven and let's do modulus. Total is equal to y mod the percent symbol x and let's see the remainder. One. So modulus operator will give you the remainder value. Let's see exponential total is equal to y to the power x that's also called as exponent or you can say to the power. So these are some arithmetic operators. Now we will see comparison operators. Let me add a few more cells there so I can scroll up comparison operator. Let's say the value of A is 30 and value of b is 60. And let's see, let's take a variable out equal to and we'll compare if A is less than b. So that's the less than symbol which in general also it is. Same it's in Python. Let's print the output. So true comparison operator is going to return a boolean value. True or false? You can also put this under parenthesis. When we are doing multiple expressions like this when comparing, it will be helpful. Also let's see greater than a greater than b. which is false. Let's see equal to A is equal to b and the answer will be false of course. And when you want to ask reverse not equal to exclamation and equal to symbol that will be A not equal to b, so that is true. Also A greater than equal to b or A less than equal to b. So A greater than or equal to b, that's false. A value is 30, b value is 60, so that is false. A less than equal to B that is true, a is less than B, a's value is 30 and B's value is 60. So these are comparison operator. Now let's see assignment operators the usual one equal to which we are already seeing where we assign the value to a variable. But we have few other assignment operators. Let's first say that C is equal to one and D is equal to one. So we are assigning the value that's assignment operator. We can also increment the value of C by D. So if I say C plus equal to D which is equal to or similar to C is equal to C plus so value of C is zero incrementing it by one. First we'll get value of C as one so the value of C is zero. If you are incrementing it by one then the value of C should be now one. Let's print C value of C, value of C is one. It was zero previously, let's keep it zero and let's say value of these ten so it works like that there's also decrement operator just plus we say minus. Let's print the value of C and I should comment this because it's going to increment and then decrement you will not be able to see that's minus one. So value of C is zero decrementing it by one minus one. So likewise you can do it for multiplication, for division, for exponent. Next we have logical operators and or so the operator is add and R. So let's take value of A as 40 and let's take value of B as 60 and we need two more variables y, let's say two and I will say X is equal to two and Y is equal to three. So we'll do comparison between these two variables and these two variables and we'll use logical operator to do some operations with them. We know when we use comparison operator it returns true or false. So let's take a variable out. Let's say A is less than B which should return true. And I have another expression which I'll say X is greater than Y, which is false. So this expression is true and this expression is false. And if I'm checking which of them are operation so it will check if any of the operand is true then it's going to return true. So A less than B that is true. And if one operand returns true then R is going to return true and you should see true in the out. Okay? So if the first operand itself returns true it will not check the right hand side operand. Let's change this little bit. We'll say A greater than B which is false and we'll say X less than y. So left hand side operand returns false. If that returns false we'll check the right hand side operand that is returning true so it is going to return true. So any of the operand returns true the output will be true. If both returns false, let's make both operand false. So a greater than B, that is false. X greater than Y that is also false. So it should then return false. So for our operation any of the operand returns true, the whole expression will be true. And then we have and operation. So a greater than B that is false. And we will say now and X less than Y that is true. But the first one is false. If any of them, any of the operand is false, then everything is false. So and is like that very strict. You have to be true on the both side. Or is like if you are true on either of the side, that is fine, but and is more strict. So we have to make both true to get the true. So a less than B that is true. X less than Y, that is also true. Okay, now it should return true. See that if both the operands sorry, my bad, I did and I should do and not or, yeah. if both the operands are true, then only it will return true. There is also a negation. So let's take the expression x less than Y which is true. Value of x is two, value of y is three, x less than y that is true. I have to put an assignment operator there. Now I can negate that, I can say not. So if this expression is returning true, if this operand is returning true, it will just make it as false. Let's print it. You see that? So this was supposed to be true then not operator simply negatied it. It made it false. So it will always do the opposite. Now these are really as simple as any other programming languages operators. But now we'll see membership operator which we use so many times. Now of course these operators also will be using for condition checking. But membership operator is just awesome. Okay, for this I will take a list or a tuple from our previous exercise. So from data type exercise I'm going to take this tuple or list. You can use anything. Okay. So membership operator works like this. So I want to check whether a string called as DevOps exist in this tuple or not. So I can say DevOps, the string, in. So in is the membership operator and I'll give the tuple name and let's see what we get in the answer. So it's like asking does DevOps string exist in this tuple? And the answer is true. It says str1 is not defined. That was a variable. Let's put something over here. Let's put there as IoT and that is also variable which is not defined. It was supposed to be some numbers. So let's give that okay. So membership operator is going to traverse It's going to check in your sequence either a tuple or a list and where it finds. It's just going to return the truth. True. So DevOps string exists in the tuple. We can also negate this. We can also ask the reverse way. We can say DevOps not in, so we are saying devops does not exist in this tuple. But the answer should be false, because that does exist, right? Maybe we look for something else that really does not exist. So let's say AI or I'll just put some number 67 in. So I'm checking this integer exist in this tuple and print me the answer that is false. 67 does not exist. You have 47. Let's try 47. Okay, let's do the opposite. Not in. That's false, because 47 does exist that does not exist. So that is going to return. True. 67 not in first triple. Yes, that's true. It does not exist. Okay, next identity operator. So let's say A is twelve and B is 15. So I'll take a variable result. I'll say A is B. Let's print its value. Let's check it. That's false. So this is same like, you know, comparison operator equal to. Let's make the value of B also as twelve. Let's check now. Okay, there's also the reverse again here that should be false. Okay, let's change the value there 15 again. So it's like equal to comparison operator double equal to and this is like not equal to. Okay with that. That leaves me with one more last operator, which we are not going to cover. Bitwise operator that works on bits and does bit by bit operation. So we're not getting into binaries. So that should be it. It should not be that difficult to understand. I know the session could be boring also plus minus modulus multiplication greater than equal to. But these operators like comparison operators or assignment operators, logical operator, these are very helpful in condition checking. Then your increment operator, which will be very helpful when we're doing looping membership operator. Also we want to check some data exist in some data type. Now, one more thing. With membership operator you can check for if a substring exist in a string. That also you can do not just tuple and list, but also you can check it in a string where you want to see whether dev exist in DevOps. So that should return. True. So practice this, wrap this up and join me in the next video.
