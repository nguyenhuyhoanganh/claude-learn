# OS Tasks

Instructor: Hello, and welcome to Python for Automation. Now in a few videos, we will see how we can use Python to automate operating system task, especially Linux, but before you begin, let me give a disclaimer. All these things that we are going to do now with Python can be done very easily and much better by using Ansible, which we are going to learn next, but the idea of these exercises is for you to learn some programming skills, so when you use any automation tool like Ansible, Terraform, CloudFormation, you will be well prepared. You can master these automation tools if you have some hands-on programming, especially using Python for automation. That being said, you can use Python to do all kinds of automation. So let's see what we are going to do. So first we'll see how to use Python for executing operating system commands, and we'll write a script that is going to add a user, add groups, add user into the group. We'll create directories. We'll assign user and group ownership to the directory. While we are doing that, we'll be also checking condition that if the directory or user exist, do not add them, print the message to the user, but if it does not exist, then create it. So if else condition, we'll have number of users, so it is going to run in a loop. Once we see how to execute or automate local task, we will be seeing then SSH in Python. We'll be specifically seeing the library fabric. By using that, very easily, you can make SSH connection to Linux machines and execute tasks remotely. We'll see a script where we are going to provision web server by using Python Fabric. We'll see what are Python Virtual Environments, and we'll also find various other modules or libraries to integrate Python with other tools or technologies, like Jenkins or AWS. So when the time comes, you can automate AWS task or Jenkins task, you can call Jenkins jobs from your Python scripts. So let's get started. Alright, so for this lecture and the next lecture, we'll need some virtual machines. We'll create those virtual machines. I have uploaded Vagrant file to this lecture resource, so you can download it from the lecture resource. Download this file, pyvms.zip. So there's the zip file in the downloads folder, and I'm going to extract this. So if you have a Windows and MacOS with Intel chip, you can use Vagrant file from this folder. If you have MacOS with M1, M2, M3 chip, you can use Vagrant file from this folder. So copy this file into a folder of your choice. I'm going to create a folder in F drive and copy my Vagrant file there. So in F drive, I have created a pyvms folder and I'll copy the Vagrant file. So from the download pyvms folder, I'm going to go to Windows and copy this Vagrant file, and I'm going to go to the F drive pyvms folder and paste it. So choose and create the folder of your choice. Make sure that you use the right Vagrant file based on your operating system. Open Git bash or terminal for MacOS, and switch to that folder. So I'm going to go to f/pyvms. Now I have this Vagrant file. Let me open this Vagrant file. So three VMs, scriptbox, web01, web02. Scriptbox is Ubuntu, web01 is CentOS 9, and web02 is also Centos 9. They have their own IP address. Take a look. I'm going to quit from this file and bring up this virtual machine. So I'm just going to say first vagrant global-status to check if there are any existing VMs running. If there are, I'm going to go to that folder and power off those VMs. If you see some other VMs that you don't want to use, you can go to that folder and destroy that VM. Once you have a clean environment, you can do vagrant up in this folder. Okay, the virtual machines are coming up. So open VirtualBox. You should see the virtual machine coming up. If you see some VMs are taking long time to come up, just double click on that VM, and this screen should open. It should show some messages running. Keep it open. Do not close it, just minimize it. If you close it, the VM will get powered off. You can just minimize it. Wait for all the VMs to come up. As I said, if some VMs are taking longer to come up, just double click and look at the output. Okay, I'm going to pause the recording and resume when all the VMs are created. Okay, my VMs are up. I'm going to log into ScriptBox, vagrant ssh scriptbox. Okay, I'm going to switch to root user. So let's check the Python version that we have, but before that, let's check the operating system version. So I have here Ubuntu 22, and by default, Ubuntu 22 will have Python 3, python3. If you type python3 and hit enter, you'll get a Python 3 command line interface. If you want to exit, you can just do exit, parentheses, and you can exit from here. In case if you need Python 2, you can install it. So in Ubuntu, you have to say apt update &amp;&amp; apt install python2 -y. Hit enter and it should run apt update and then install Python 2. Okay, after this you should have Python tools, so just so if you just type Python and hit enter, wait, python2, yeah, you should get into the Python, into the Python 2 interpreter. Okay, but we will be using Python 3, but there are many machines today which uses Python 2 so you should know how to now use both the interpreters. Okay, now let me clear the screen and let's get back to the Python 3 interpreter. Just type Python 3 and hit enter. You get Python 3 interpreter, and in this lecture, we will be learning how to execute Linux commands from Python. So we'll be writing script and we'll be mentioning the Linux commands over there, but I want to show you that directly, you cannot run Linux command from Python interpreter. So for example, if I run ls, it's going to throw me an error. ls is not defined because it's not a Python command. It's a Linux command, bash command, so if you want to execute bash Linux command or any operating system command from the Python interpreter, you have to import this library. You have to say import os, and in os, you have a method called system. So you say os system, and in parentheses you give the command in the double quotes. So now if I do ls, it executes. Now this is going to execute the command, show you the output, and return the exit code. So it's returning the exit code 0. Let's try one more command. We'll execute "pwd", okay? Or, who am I? Any command, right? It returns the exit code if the command executes successfully, it returns the exit code as zero, but the command fails. Let's give some gibberish command, which is not a real Linux command. Right now if I execute this, this is going to return the exit code non-zero. So remember this, os.system returns the exit code 0 if the command executes successfully. It returns non-zero if the command fails. Now, OS has really many other methods like system. You can execute any command like that. There are many other methods, so if you use a dir function, now, dir function shows all the available methods in a module. So OS is a module. System is a method or function, also you can say. Let's call it function for now. So system is a function. Like that, there are many other functions. So if you just say dir os, it's going to list all the available functions or methods from OS module. Okay, there are many, some you might understand because you already know Linux, like for example, chown, change ownership, right, chmod, change the mod, chdir, CD, right? There are many. There are really many, many modules. So some of the module that you can use or maybe of your interest, apart from system, like system, we can give any command, but we have os.chdir. That is gonna change to directory like cd. os.getcwd, which is going to return same output as pwd. getpid or chmod, getsize, uname, and like that, there are many other. You can get the whole list of the methods by running dir, as we have seen. So one small example of a script. We import OS, we define a variable path, and we are going to check if os.path.isdir. That's a method. You specify the path of a file. If it exists, it's going to return true, and will print a message, else we can run, or sorry, else if, we can run os.path.is file. Now, isdirectory if it's a directory, is file if it's a file. We print a message that it's a file, else will say file or directory does not exist. That's a pretty simple script. We'll write and see how it works. Okay, now let's come out of this, exit and parentheses, hit enter, it'll exit the interpreter. Let me clear the screen. Let's create a folder in opt. I'll say opt, mkdir /opt/pyscripts. Let me see you into that. We'll create a Python script over here and I'll give the script name as check-file.py. First thing I'm going to say import os. I'll take a variable path. I will assign path of a file. I'll say /temp/testfile.txt. Okay, so it does not exist currently, but we'll check the condition, if os.path.isdir directory, and we give the path. We'll give the variable. If that returns true, we'll just give a print statement. We will see it is a directory. elif, we'll put one more condition, sorry. Yeah, os.path.isfile. We give the path. If that's a file, we are going to print a message, "It is a file," else it does not exist. So either we are checking whether it's a directory or a file, or else we are assuming that it does not exist. We'll say, "File or directory does not exist." Okay, we are running Python 3, so we are going to mention the interpreter path user/bin/python3. Now some interpreter, you have to mention Python 2 or Python 3, whatever script you're writing. If you don't mention the interpreter, the script will fail because, by default, shell will think that it is executing these shell commands, but they're Python commands. So that should be the path of the Python 3 interpreter. Even though, we'll check it. Yes, that exists. Alright, Python 2 interpreter path is this one, python. Okay, let's make our script executable, and let's execute it. So it says file or directory does not exist. That's true because it does not exist. Let's create a directory. Oh, no, file, touch. Oops, I've given it two times. Okay, now the file exists, so it should print a message that file exists, right? So that's how you can run system commands. Okay, now let's do a task which we talked about. We'll create users, group, directories, we'll check conditions, whether they exist or not. So I will name this simply as ostasks.py. So Python 3, let's use interpreter, import os. Now let's create the list of user that we are going to add a variable. So I have list of the user, alpha, beta, gamma. That's my user, and I will print a message, and I'll give this hashes just like a title that I'm adding users. Now we going to execute user add command, and we are going to run user add command in the for loop, which I treats over this list. So every iteration we are going to add one user, but as we add user, we'll also check whether it exists or not. If it does not exist, then only we'll run the command, else, we'll print a message that user already exists. So let's write the for loop. So for user in user list, now I want to check whether first the user exists or not. So I'll run the command id and the user variable, and when I execute that, it will return an exit code. If it does not exist, it will return a non-zero exit code. Let's see it through an interpreter before we run this in the script. Okay, so let's get into the interpreter. I will say import os. I'll take a variable called user, and let's assign the value as alpha. Now I'm going to run the command in os.system, usual command, "id" and I want to put the variable over here. Now, I can't put the variable like this. It's in double quotes, so it'll not be considered as the variable, so I'm going to do string formatting. So I'm gonna say format.format and I will give the variable, and here I will give the curly braces. So id, and this will be replaced by the variable user's value, which is alpha. And when I run any command with os.system, it's going to return the exit code, okay? It's gonna print the message, but I can store this output into a variable. So I'll say ec, like exit code. So let's see, what do we have in the exit code? Oops, not double quotes. So we have the exit code 256, because that user does not exist, okay? But it's going to store only the exit code, not what it's printing, and that's exactly what we want. So let's open our script, and we will see now we'll take a variable exit code. Okay, so os.system "id", it's going to run it on the user variable and this user value will be first alpha in the first iteration. Well, if it does not exist, we are going to get a non-zero exit code, and then we can check if exit code is not equal to zero. Then we'll say we print a message saying that user, which comes from here, does not exist. Adding it, and few more print messages. Oh, let me fix the syntax, right? Okay, so we should get this, print message if the user does not exist, and then it runs the command useradd and the username, which will be replaced by this. So if the user does not exist, it's going to add, else we'll just print a message saying the user already exists. I'm skipping it. We can already test it. So let's log out. Let's make our script executable, and let's run this. Okay, you see it's printing message, user does not exist. Alpha, beta, gamma, and adding it. Now the user should be already added. Now if I run the script once again, it prints the user already exists message. So loop condition, system task, all working together. Now on the same principle, we'll add the group, and we'll add user into the group. So we wanted to add the group named science. So we are checking again in os.system, grep for science in etc/group file. Now if that exists, it's going to return zero. If it does not exist, it's gonna return non-zero exit code, which we are storing into the variable. Again, we are doing the same thing. If exit code not equal to zero, that means group does not exist. Print the message and run the command group add science, else, print the message that group already exists. Let's test this one also. So in the first, it says user does not exist. If I run it once again, it says user group, sorry, group already exists. Skipping it. Now let's add all the three users into the group science. So we are going to iterate again on the loop for user in userlist. That's where we have list of user. So let's print the message. Whenever we iterate, we are just going to say adding user in science group, right? And the variable comes again from the list for every iteration, and let's run the command os.system, give the command usermod -G, the group named science, and here I will give the format. Alright, first the value of user will be alpha. It's gonna print the message and it'll say usermode -G group name and the username alpha, right? And then for beta and then for gamma. Let's test this one as well. Okay, we have a syntax error, line number 37. Okay, there, print, the string should be in parentheses. Okay, let's test it once again. Okay, it says adding user alpha. Let's test id alpha. Yes, it's in the science group. Now I'm not checking any condition over here, but because it really doesn't matter if the user already exists. It's not gonna throw any error if it already exists in the group. Okay, now let's create a directory from our Python script and give the user and group ownership to that directory. Okay, we'll just print the message adding directory if os.path.is directory. We have already seen this example. We are checking whether this directory exists or not. If it exists, we print a message. If it already exists, we are skipping it. Otherwise, we'll say os.mkdir and we give the path. Now we can do os.system, and here we can say mkdir. We can do both ways, but I just wanted to show you this extra way of creating directory also, this method. Okay, so to give the ownership and the permission, again, with os.system, simple command. So first we print the message, assigning permission and ownership to the directory, and with os.system, we are going to say cshown: the group name and the path. So this directory will have a group of signs, and then we'll say permission chmod: 770 to this directory, and our task will be completed. Let's finish this. Okay, again, line number 46. Wrong syntax in the print statement. Parentheses missing. Let's fix that. Let's test it. Okay, so super, that's working. Let's check ls -ds /opt/science_directory. So yes, it's owned by the group science and permission is 770. So we achieved our task, and the purpose of this task was to show you how you can execute commands from Python scripts, and we have seen executing commands locally. In the next video, we will be seeing how we can execute commands remotely by using SSH in Python. So wrap this up, and join me in the next video.
