# Cloud Interaction with Boto3

Okay. So I'm going to ask ChatGPT how to automate AWS cloud task in Python and let's see what we get. So it says it's commonly done using Boto three library. And we can install that by using pip. So we're going to say pip install boto three. And then we need to set the AWS access credentials as well. So that means we need AWS CLI. So once we run AWS configure it's going to ask us the credentials that we're going to set. So first we need to install Boto3. Then we need to create an IAM user. Get the access key set the credentials and then we can start writing the code. So let's look at the code in a moment. But before that let's do the setup. First let's create the IAM user. So let's go to IAM service. Go to users say create user. I'm going to call it as Python admin because I'm going to give administrator access. Go next attach policies directly. And I will say administrator access. Be careful with that because we are going to use access key and secret key. So as soon as you're done with this activity, make sure to delete this user to go to next create user. Now go to the user and go to Security credentials. Get the. Get to the access key. Secret access key CLI command line interface. I understand the risk and go next. So create access key. I'm going to first copy the access key. So in windows you can open your git bash or PowerShell any command line utility basically in Mac OS. Open your terminal and run the command AWS configure. AWS CLI should be already installed for you, so just hit this command. It should ask you for the access key. So paste the access key. Now let's copy the secret access key. So I'm pasting the secret access key which I should not show you. But I'm going to delete it as soon as this is done. So set the region. I'm going to keep it as US East one and format as JSON. I'm going to keep it as default. Let's test the credential. I'm going to run aws s3 ls command. This should list all the S3 buckets for me. So this shows the credentials are working fine. If you get an error, make sure you check the policy access policies administrator access is attached or not and check also the credentials if you're given correct or not. Okay, now let's do the other things. Let's install Boto3. But I'm not going to install Boto3 directly on the system. I'm going to do it on the virtual environment. I'm going to show you what I mean. So right now I can do it like this. You know, pip install boto3. So this gets installed on the system. And then when I run the Python program I need to import this boto3. Maybe I have some other project where I have different version of boto and not only bought some other Python libraries I'm using, I have different version for one project and different versions of libraries for other projects. So how do I segregate that? We can do that by using virtual environment. First create a folder somewhere. So I'm going to create it on F drive. So anywhere. Or you can use your previous project folder in PyCharm just keeping it clean. So I'm creating it somewhere else. And then I get into that folder. And here I'm going to run a command python hyphen m v, e and v. And then you give the name of the environment. So I'm going to say as DevOps okay. Now after completing this if you run the command ls, you should see a folder there with your virtual environment name. And in order to use this you know, let's do ls. Over here you should see some folder structure in windows. You will see scripts in Mac OS or Linux. You should see here bin. So to activate virtual environment in Linux or Mac OS. Run this command. Source the name of the folder. Slash. Bin. Slash. Activate. If you're using windows, then run the name of the folder. The virtual environment. In our case it's DevOps slash scripts slash. Activate. So let's try this to say DevOps slash scripts. Slash activate. So actually nothing happened. If I run this command in PowerShell it will work. But if I'm using git bash then I have to do it like this. Source DevOps scripts activate for Mac OS instead of scripts. The folder name is bin. So when you run this command, your virtual environment will be activated. You should get something like this. The name of the environment in parenthesis. Let me clear the screen. Now if I'm going to install any library like boto3 with Pip. So I'm going to say pip install Boto3. So this is going to install the library in the virtual environment, not on the system directly. Okay, so the Boto3 is installed. Now I'm going to run the command python and hit enter. So I get into Python interpreter and I'm going to say import boto3 to test it. And it works. Now if I exit from here. So type exit and parenthesis exit from the Python interpreter. You see virtual environment is still active. To deactivate you have to run the command deactivate. Now that DevOps virtual environment is deactivated. Now if I run Python, I get into the interpreter and if I say import boto3, you will get an error. If you already have Boto3 installed on your system, of course it will work, but in my case I have not installed boto3 in the system. I have installed it in the virtual environment. Okay, so let me exit. Now before we start writing some script or code, let me show you the basic connection setup and how to use methods for different Python resources like EC2, S3. So for that case I'm going to again activate the environment. So source the folder name DevOps. Inside that I have scripts. And in that we have activate file. So this activated the environment. And I'm going to get into the Python interpreter. So just type Python and hit enter. And I'm going to import Boto three. Just want to show you how to use boto three basic structure how to use the methods. And then we can start writing our scripts. Oh wait. Before this let me create one text file because I'm going to create an S3 bucket and upload a text file. So use anything vim editor or anything and create some text file in the same directory. I'm creating some file solar system dot txt. I'm going to mention the name of the planets. Okay, some file with some content. Then again make sure your environment is active and then get into the interpreter import. I'm going to clear the screen first. Okay Python then import boto3. So Boto3 library has one connect method. I mean sorry client method. By using that you can connect to the service that you want to. So let's take a variable I will name it as S3 equal to Boto3 dot client. So we access that method and we give the name of the service in the parenthesis. That's the argument we are passing. That will be S3. So that connection information will be stored into this variable. Now if you run dear S3 data on S3, you should see a whole list of available methods or functions a huge list. So for example you can see here copy. You can see a create bucket. So I can say S3 dot create bucket. And I can pass some argument which will be the bucket name to create the S3 bucket like that. There are many other things to upload, files to download, file to list to delete or delete object is there, download file is there. So you can check this whole list from the Boto3 documentation. Or you can use ChatGPT or any other AI tool. So let's create a bucket control L I'm going to clean this first okay. So that's what we did first right. We said S3 and we connected to S3 service. So now we have S3 object once again let's can do that. So I'm going to say S3 dot create underscore bucket. That's the method I'm using or function I'm going to give the parenthesis and give the name of the bucket. That's the argument you can pass. But also you need to give the name of the argument. So that will be bucket in B caps equal to and the name of the bucket. So the name of the bucket will be a string. So we need to put it into a single quote or double quotes. And let's give some name. Boto python something and give some numbers. Just make it unique. Don't give same, otherwise you will not be able to create it. So you see we get a response metadata in JSON format HTTP status code 200. That means this worked successfully. And now let's upload a file into this bucket. So I'm going to first list that file that I created I forgot its name. So I'm going to say import os os dot system. Dot system parenthesis. In this I'm going to run the command list okay. Yeah. That's the name of the file. Wrong name I've given solar system instead of system. Anyways I'm going to run the command S3 upload underscore file. That's the method name. Again you can run the command and find that name. And you need to pass your three arguments. First will be the name of the file. Again it's a string. So make sure you get in the single quote? So that's the name of the file, comma. The next argument will be the bucket name. Again single quote. Because it's a string. This is the name of the S3 bucket and the object name, which I'm going to keep the same same name of the file. Or you can change the name okay. Just these three arguments and hit enter. And this should upload the file. Now let's go to AWS console and see ourselves. Right. So I see the S3 bucket got created. And in this I should have the file. So now you got an idea how to interact with AWS cloud services by using Python boto3. In the next lecture we are going to write scripts to interact with other services also like EC2 and the sub services in EC2 like Keypair, security group instances, load balancers. I'm also going to show you how to create complex automation scripts by using generative AI. So wrap this up and join me in the next one.
