# AI for Cloud Automation

All right. In this lecture we are going to write Python boto3 scripts. So I have opened my VSCode. You can also do the same. And I have opened the folder where I have my virtual environment. I created this folder so you can also do the same. Just go to file open folder and select the folder like I have enough Python and AWS cloud this folder. I say select folder and in this I have this virtual environment from the previous lecture. So I'm going to write my scripts over here and then execute it from the terminal of VS code. But before I do that, I'm going to install some extensions that will help me write better code. So go to extensions. And first extension I'm going to install is Python. So this one click on install okay Python extension is installed. Now I'm going to search for Boto3. If I have extension I'll install that as well AWS Boto3 I'll say install trust publisher and install okay. That is also installed. Now let's create the script. What we did in the previous exercise for the S3, create S3 bucket and copy the file into that. So I'm going to create here a new file. I will name it as S3 test.py. First thing we need the boto library in our script. So we are going to write import boto three. Next we need to connect to S3 client. And anyways our co-pilot is trying to complete that. Okay I'm going to use tab create S3 client. And here again this is what we did in the command line. The previous lecture S3 the variable which is taken a variable name equal to boto three dot client. And the service name I have defined three variables over here. Bucket name variable. Where I have given my bucket name. You better take your bucket name and put it over there. File name. File underscore name variable. I'm just taking the same file to upload. And the object name that will be the name of this object, this file in the S3 bucket. So when we upload the file, the file name will be this one. Now let's write the code to do that to push this file to the S3 bucket. Okay. Now copilot already understood what we are trying to do here. So let's use tab, upload the file to S3 and then it's giving a try block. Let's check that out. Try this S3 which is this variable which stores all the method dot upload underscore file in the parenthesis file name. That's our variable bucket name. That's the bucket name and the object name. And once this is completed a print statement that says file file name which will be this file name uploaded to bucket. And the bucket name as this object name. So I'm using Tab to complete this. And of course it will suggest except block except exception as e and an error. And now instead of taking the file name as the variable, I'm going to take it as the user input. So here I'm going to instead of the file name I'm going to mention the user input. So I'm going to say here input. So anyway suggesting I'm going to use Tab to complete. Now object name. Also I want to keep same. So I'm going to simply assign the same variable over here which is file underscore name okay I think this should be it. So it's going to try to upload the file. If it fails it goes to the exception and prints as the exception. So let me save this. And I'm going to open the terminal new terminal. It's opening PowerShell for me. So I'm going to switch here to git bash okay. So I think I'm in the same folder where I have the script. That one test 3.py I mean s3 test.py. But first I need to activate the environment. So I'm going to say source DevOps scripts activate again for Mac OS user. It will be the folder name. In my case it's DevOps slash bin. Instead of scripts you have bin and activate Wait. Okay. The environment is activated. I'm going to give execute permission to the script. Okay. Now, either I can run the script like this. I can say Python three test dot p y, or I can mention the interpreter path like this in my script. Hash exclamation usr bin env space python. So both ways I can do I'm going to just run it python3 test.py. So it's asking the path to upload the file. And as I said I'm going to give the same file name. So which is solar system dot txt it says the file uploaded. Now let me run it once again. And this time I'm going to give some file name which does not exist. And let's see what happens okay. Error uploading the file. So we also tested the try and accept exception handling. Also we have tested we can give better exception handling if you want. But now you don't need to do everything by yourself. So let me reiterate now we have learned Python programming basics of Python programming. We have seen how to do Linux automation by using Python fabric. We have seen OS commands and we have seen then how to interact with AWS cloud services by using Boto3 in Python. Now you are trained enough to use any generative AI and write your code. You don't need to explicitly write every line. You can generate the entire script or even entire workspace to achieve complex tasks. First, let's start with simple thing. Let's say we want to set up a website on an EC2 instance. We need load balancer. We need target group. With that we will need key pair. We need security group. So let's ask ChatGPT to do that for us. So I'm asking it to create a Python boto3 script with trying to write it little neat now. So create python script which achieves below mentioned task. First it should create a key pair and then it should create a security group that allows port 22 from my IP and port 80 from anywhere. Next, I'm asking it to create EC2 instance T2 micro with this key pair and security group and Ami name I have mentioned. And it should have the website setup from 2.com. So it's going to have it in the user data. Okay I've modified the prompt a little bit. It should have the script in user data to set up website from 2.com. Next I'm going to put the load balancer. So create application load balancer register. This instance security group of ELB should allow port 80 from anywhere. And make sure all the zones are selected in ELB. Now when I did previously, the instance was not registering in the load balancer. That was because it took first two zones from the region for load balancer. So load balancer was in first two zones and the instance got created in the third zone. So if the instance is in a different zone than the load balancer, then the instance will not get registered. That's the problem. So that's why I included this point. And I'm just saying print LP end point. And last thing I wanted to give resource name as per the website name. Give name to all AWS resources resource says in the script as per the template name you take from 2.com. So any template that it selects, that should be the name of all the resources like key pair, security group and everything. Let's see what happens okay. So it imports Boto3 requests that we'll see what it is and OS to run OS commands. I think this is for to create the key pair locally. First import time for the time stamp. It has taken variables, defined variables and really it has just given the name to it I was expecting it gives the name of the template. I mean copilot does that. I've tried it with copilot and it gives the name of the template. Anyways. Let's see. We can modify that with prompts. So region us east one. Good if it's taking some other region in which you don't want. Make sure you mention the region as well. Okay, here my IP is a variable which is getting the value of your IP and that is happening through requests. Importing your request. So request dot get it. Access this URL. This generates the public IP your public IP. So this will give the public IP plus the string is added slash 32. So when we add in the security group it will be in proper format your IP slash 32. Now we know all this because we have gone through AWS. So we know AWS. We know Python. We know Boto3. We know how to interact with AWS cloud from Python. So we can write correct prompt or check the script and even modify the prompt and get the expected result. Okay, here it is creating a key pair. So easy to create. Key pair. Key name. Key pair. Here it's creating a file. Over here it is opening a file in the write mode and writing key material. And that file will have 400 permission. So this is the private key that is creating here. So what basically happens here is you know this is going to create a key pair. Right. And then when it creates the key on AWS cloud the private key is downloaded. Right. When we create a key pair the private key is downloaded that downloaded material. It is saving it into this file PEM underscore file name. This file will have it okay. Next creating security group and two rules 22 from my IP Cedar and port 80 from anywhere ami ID that is based on the region. So that's Amazon Linux 2023 ami id from US East one. And here is our user data script. So the template has taken is barista. I was expecting it is going to give the name to all the resource as barista. But anyways let's try this also in GitHub copilot in a moment. Okay, so after I log in and use Chrome browser, I'm getting better results. Starting with the template name, the resource name it has taken as the template name, which is from two slate.com photo folio. We scroll down, you see that in the User Data Photo Folio. Okay. So it's going to create the instance rate for the instance to become running. That is going to create the security group for load balancer that allows port 80 from anywhere. Okay. Here it is creating the load balancer creating the target group. And it should register our instance register target. And finally it's going to print the endpoint of the load balancer. Now in one moment it will be too much overwhelming. So the better way to learn here would be take the script of individual resources. It not take you know check with the prompt create key pair python script. You can use the prompt as you know make it simple or keep it simple. So it's going to avoid try and accept and trying to keep it simple so you can understand. Then later you can add the exception handling and you can add one thing after another. We'll see that in a moment. First let's test this one. So I'm going to copy this code. Create a new file here naming Named as folio set up AWS dot p, y anything and just paste all the content, save it in the terminal. I should have that script and I'm going to execute it with the interpreter. Python folio setup aws.py. So I get an error. No module named request. It's importing request here the request library. And we need to install it. So to install library in Python we have pip pip install. We're going to say request okay. Once we install request through pip then we can execute the script again okay. It started working. So it's creating the key pair. And now you should have a key there. That's the private key. See that. Okay I got some error okay I got some error. Let's see is the load balancer name can only contain characters that are alphanumeric and hyphens okay I'm going to ask this to ChatGPT. By the way, I know what this is, but I'm going to still ask ChatGPT and get the answer or no corrected code. So let me copy it from here. Easy to instance before it fails. Till here and put this in the prompt. Okay, so I said after executing the script I got below error, EC2 instance, blah blah blah and this error. Let's see what it says. It says the problem is invalid LP name and cannot contains underscore. So what's the fix? We can change the template name in the LP. So that's the variable right. That derives that name. So instead of you know giving that let's give a different name to the LP itself. You know I think starting the name with the number is a problem here. Anyways, let's go to the script and make the changes to the LP name itself. LP name this one. It's going to remove this function also F and just going to give some name, I'll just say folio, LP, LP, whatever. I'm just making some change over here. No special character. And for the template also here I'm going to change this one. Remove the underscore. And instead of the number first I'm just going to give Photo Folio. That should be good. Now before I execute this it should have already created EC2 instance security group and key pair. I need to remove that. So I have one running instance. I'm going to delete that. That one because it's going to again create a new instance. Also going to remove the key pairs. Key pairs remove this key. Security groups. Okay. So I have two security group one for instance one for the load balancer. Delete those okay. It's refusing to get deleted. Once again I'm going to try delete. Okay. It says one network interface associated. Maybe the instance is still not terminated. It's gone. Let me try once again. Yeah. Now I can delete. Okay. Once again check in the EC2 dashboard refresh. Make sure you have zero running instance one security group that is default zero key pair. And then let's try our execution of our code once again okay. Started okay great. Another error but similar one target group name cannot have this underscore special character. You know I'm going to remove all this underscore from my script. Delete everything again. And the script is in the variables pretty easy. Or you can ask this to ChatGPT not to put any underscore special character there. Also you'll be underscore let me save this okay. Once again make sure you delete again everything. And you now have the load balancer also. So first thing delete the load balancer. Then delete the instance key pair. Security group. Okay, I'm adding some change for the folio app I have given. Make sure I remove all the underscores from all the variables name. Save the script and test it once again. Now there are many private key that got created. Let's delete those also. Okay, that one I'm going to keep. This is the new one. Okay. Finally it completed successfully. So the script got completed successfully. But we are still not sure whether it has given us the right result or not. Wait for five ten minutes. Check the URL. If everything is good, then we can study the script once again and make our final decision. Okay, now coming to the most important point of this lecture you have seen, we can generate entire code is not too much, but imagine you are accessing or using many other services 1015 services. They might have many sub services and you will get a huge complex script. When you get error, you have to keep troubleshooting it, deleting it, and do this task repetitively until you get the final result. So the best way is always to write small small script, even if you're using. I test it and then you can integrate it together. Okay. So now delete everything. You don't need to worry about whether the website works or not. Just delete everything. And in the next lecture we are going to set up everything one by one. We're going to also verify script or code before we execute. And this time we are going to use GitHub Copilot to do it. So I'm going to delete first the load balancer then the target groups. Then the instances. Key pair. And finally the security group. Once you're done doing the cleanup join me in the next lecture.
