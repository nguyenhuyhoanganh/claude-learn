# Exception Handling

Welcome. In this lecture we are going to talk about exception handling in Python. So for this lecture I'm going to create another folder. So go to new and say directory. And let's give directory a name. Exceptions. And in this let's create a Python file I'm going to say tr one dot p y tr. Like try one thing. You try one dot p y. Okay, let's write a simple code which takes input or integer input from the user and then use ten to divide that number. So I'm going to take a variable number. And I'm going to say in the int function. Well first I have to say equal to in the int function because I want only integer input. So I'm putting it into the integer function. And I'm going to say input. And then I'm going to give a message. To enter something enter a number. So the user enters the number. And this needs to be definitely of type int. And then I'm going to take a variable called result equal to ten divided by that number. So I'm going to just say ten divided by the variable number. Then let's print it. Okay. Now in this f function double quote because we're going to put the variable over there. So we'll give a message first. The result is and we'll give the variable in the curly brace. The variable is again the name result okay a pretty simple program takes the input from the user. Makes sure that this is an integer. And then ten divided by that number. Let me give a space here and let's put a few more print functions here. Program ends here. And happy coding okay let's save this. And let's run this here. Click run. So it's asking for the input. So I'm going to say two and hit enter. And it divides ten by two gives you the result and the program completes. But what happens if the user enters anything other than integer? So let's run this once again. And this time I'm going to give a string. So I'll say ABC. Let's see what happens. The program terminates. You get an error value error invalid literal for int. So this is a value error. It's expecting an integer but we're giving it a string. Maybe I don't want this program to terminate so abruptly but give a better message to the user. And that's what is called as exception handling. So this is an exception. We should know how to handle it. So same program right click to run dot p y. And we're going to say copy. And then right click on the exceptions folder that we created. And we'll say paste. Let's give the name as tr or try two dot p y. Okay. So first I'm going to take this print statement and move it. Head over here. And I'm going to say to try this code first. If this works, it's fine, otherwise the exception. So bring this to the next line and type try colon. That's the syntax. To try the code make sure this is indented properly. So I'm using tab to put this inside. All right. So this basically means try this code. And if there is some message or sorry some mistake some exception then get into except block. So accept and you can give what kind of error you're expecting like the value error. The may the different types of exception in Python. We're going to check the list in a moment. But let's complete this first. So I'm expecting a value error. And if the value error is there then you know do something like you know print this message. So I'm going to say print invalid input and enter a valid number okay let's save this. And let's run this. So first just enter normal integers. I'm going to enter two again. So it enters the try block. Everything goes well and it completes the program. It does not enter into accept so it executes the last three print statement. All right let's run it once again. This time going to put some string. So you see there it says invalid input. It is getting into the exception because it found the exception. The try it found the exception. It entered into the accept block executed our accept code. But again there are some other errors. We have given the print statement which is trying to print the variable result, but it does not exist because this failed. Right. So there is nothing in the result. So result variable is not defined. So now we are getting error for this one. So let's see how to handle that also. So copy tr two dot p y. And then in the exceptions paste it. Let's name it as tr three dot p y. So we need a method where we can execute some code when the try executes successfully. When this code completes successfully, we want one more block and that can be achieved by using else. So we'll get this print statement in the else block and let me save this first. So it works like this. So if the try this code works then the except will be skipped and it goes to the else. But if this fails it gets into the except and then the rest of the program. Let's execute this tr three dot p y run. Let's enter four here. So you see that the result is 2.5. So no except blocks there was no exception. So it directly went to else. After executing try and the rest of the program is completed okay. Very well. Let's execute it once again. This time I'm going to give zero. It's an integer. What happens when you divide any number by zero? Zero Division error. You cannot divide a number by zero. Here it is. Throwing another error. Zero division error. So we can add more exceptions as well. So this time TR4 dot pi. Copy this. Paste once again with the name. Try for dot pi. So I'm going to say add one more block here except and this one will be for zero division error except zero division error colon and a print statement that says you know you cannot divide a number by zero. Right. Let's save this and let's try this one. Let's run this. Let's enter zero. See it enters into that exception and says cannot divide a number by zero. So like this. There are many other kinds of different types of exception. Let's check a list on ChatGPT. So asking a question to ChatGPT, you know, for the python, try and accept. Give me a list of different types of exception I can use in the accept block. So you have many exception. That's the one that we'll be using once after this. This is you know, if you don't know different kinds of error or different types of exception, you can just say accept exception as some variable and then you can print some statement or if you're sure, like for example, arithmetic error you're expecting or zero division error you're expecting. Like we have seen a floating point error. There are many other different types key error, keyboard interrupt, memory error, name error. You know, in my real time experience, I have learned this exception by not executing, failing in the code, and then looking at the message. That's how I used to do it. But anyways, we have now many sophisticated tools. We have AI, so you know, we can easily get stuff or you also have the documentation. You can go through the documentation as well. Import error. You know you're trying to import some library. If that fails you say import export, import error, unable to import, blah blah blah. Okay, so for this program value error zero division error, I cannot really think of any other kind of error in this simple program. But you know, we don't know what to expect. So let's accept it. Sorry. Exception. You know, as a child I always had this problem of accept and expect. See still having it dyslexic anyways. Accept expect. Come on not again okay except exception as e colon. And then let's put some message. So an unexpected error occurred. And that exception will be stored into this variable E. And we just want to print that variable. Oh wait I need to give here a function also a function. And I'm going to put this variable over here curly brace e. So save this file and try to run this program. And as of now, there is no way to take a user input that enters into this exception block. I mean, we can enter string other things, you know, it can get into valueerror zero division error. So we need to make some changes in the code. So it enters this one. So take it as an exercise. Take this code, copy this code, put it into ChatGPT and ask what user input should I take. So it enters into this exception as E block. Okay, spend some time over here. Try and accept or exception handling is very much used in Python, especially when you're doing automation. Things can fail, it will fail and you should know a way to handle those. You know here we're just giving print statement. Maybe we need to do something else, execute some other code, or execute some other function if things fail right. So that should do for now. That would be all in this lecture. I will see you in the next lecture. We are also going to try cloud automation by using Python. So wrap this up and join me in the next lecture.
