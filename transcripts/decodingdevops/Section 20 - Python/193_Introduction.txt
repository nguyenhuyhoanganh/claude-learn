# Introduction

Hello and welcome. In this section of the course we are going to learn basics of Python, and we are going to see how to use Python for automation. So why is it necessary to learn Python for DevOps? Well, I highly recommend to any DevOps engineer to learn Python. One thing is you should know basics of programming. If you're working in the computer industry now, especially for DevOps, there are many automation tools that we will be using. And these automation tools like Jenkins, Terraform, Ansible, Argo, CD, many other all these automation tools derives programming concepts or logic to give you a sophisticated automation platform. In short, if you want to master automation tools, you should have some basics of programming. Also, while working in projects when you're doing automation, sometimes you will see some tasks that cannot be automated by automation tools. You know you need some customized solutions. In that case, you can use Python and get your customized result. And if you have never done programming, please do not worry. Python is very easy to read and very easy to write. Python is an interpreted and interactive language just like Perl PHP, you don't need a compiler to compile the code and then run it. You can just write the Python code and run it on the interpreter itself. You need to have Python interpreter. Python is definitely a beginner's language, so if you're learning programming, you should start with Python. You know, earlier times we used to start with C language. In today's time it's definitely Python. And the best part of Python is there are so many libraries. Now you have a programming language. Then you can extend its feature. Like for example, if you want to use Python for machine learning or web development, you know many things you can achieve with Python. You want to do automation in automation itself. Let's say we need to interact with Jenkins or Terraform or cloud computing providers like AWS, Google Cloud, Azure. So you can install these libraries, these frameworks and you can utilize them and automate your desired tasks. So Python is used almost everywhere web development, automation, data science, machine learning, AI, the list goes on and on. So you have huge support. So in this course we are going to understand the basics of Python Python fundamentals. We're going to write simple automation scripts. We'll also see how to use Python in cloud computing automation. And finally we'll leverage the power of AI and expand more in our Python knowledge. So as I said, Python is very easy to get started with. And first step is of course the installation of Python. If you're using Linux or Mac OS, Python comes by default. In windows you need to install Python. So in simple steps, you know in windows you open PowerShell as administrator and execute install Python and Python will be installed. Same in Mac OS. You want to install Python or upgrade Python, just use brew, install Python and you have Python. Now along with Python, we're going to use few more utilities. In the beginning stage we'll be using Anaconda. I'll show you how it works. And also we'll be using a code editor PyCharm. Now you can use VS code. Also you can use any code editor of your choice. But since we are learning Python, I want to show you the most popular code editor for Python, which is PyCharm. Again, you can use VS code if you want. So let's see how to install Python first okay, so in windows open PowerShell as administrator. In macOS open your terminal. So once you open your PowerShell as admin user execute the command choco install python hyphen y. They should install Python for you. For Mac OS it will be brew install Python. So likewise execute these commands choco install Anaconda three and for macOS brew install hyphen hyphen task anaconda and then choco install PyCharm, Python, community and for macOS brew install hyphen f and cask PyCharm hyphen c. Once all this is installed, reboot your computer once and open Anaconda from your programs menu. So open your start menu and open Anaconda Navigator so Anaconda makes it easier to manage Python environment and launch application from it. It's used for data science, machine learning, scientific computing, but we're going to simply use it to launch various environment that we use for Python. For example, we have here Jupyter Notebook which is a browser based Python interpreter. And it's very user friendly. It's very good in the beginning when you're learning Python, when you write short programs, you can you can write it, execute it, test it there itself. You can use it to launch PyCharm Community Edition. You can open your VSCode also from here. So let's first click on this Jupyter notebook. Click on launch. So this should open your default browser and you should see some URL like this. You should see your folder structure. First we will create a new folder into this. So click on this drop down and say new folder. Let's give this folder a name. So I'm going to say Python hyphen practice. So this is creating this folder in your home directory. So like that create some folder for you separately. And in that you can create your Python scripts. Basically these are called as notebooks here. So click on drop down say Python three. This is going to open a new notebook. Let's give this notebook a name. Let's rename it with first Python notebook. Okay. So in the notebook the first thing you see is this shell. Sorry. Sell sell sell. We can write our Python statements over here. Like the first statement I'm going to write is print and give parenthesis and parenthesis. Give double quotes and write some text like I'm going to write Welcome to Python like that. Some three print statements I have written. And I'm going to now click on this button over here which says, you know, run this cell. So if I click on that it simply executes that. So print in Python. You might have already understood this is just to print like in bash we have echo here. We have print now print in Python three is a function. Function looks like this. You will have the name of the function and you will have parentheses. This is how you call a function or invoke a function. And in the parentheses you pass the argument. So here the argument is a text and do not give any space in between the statement. This is wrong. If you execute this you will get this error. Indentation error will understand that in the next lecture. Just make sure you start all the statements from the first column itself. Okay, so here you can add multiple cells and you can execute them. So once you're done here you can click on save. This notebook will be saved. And when you go back, let me refresh this. This notebook is there in this folder Python practices. If you double click it, it is going to open it into new tab. So as you see this is very good. Now when you're practicing when you're learning Python come back to the navigator and open PyCharm community. This is the IDE. Now you know IDE or any code editor is for professional usage. Click on launch. Once you launch this, you might get a message to import settings from VSCode or other places. You can click on Skip and here you can click on New Project. Let's give this project a name. You should see the path where it is saving this. I'm just giving it a name. My Python scripts. You can give any name you want and click on create. Okay, this should open up the editor. Over here you see the folders. You see the path where it is located. You can right click on this and click on New Python file. So just give the name of the file and it will create a Python file for you. just give the name, that's all and hit enter. It will give the extension automatically. You see dot p y and write the print statements over here. I've just copied it from the Jupyter notebook and now we can run it. You can right click over here and say run this. So it uses the Python environment creates a virtual environment for Python. You see this path v env scripts Python dot x. So it uses the Python from the virtual environment, not the Python from your system. We're going to talk about this virtual environment in later lectures, but basically just uses the Python interpreter and executes your script and you get the output. So in this course we'll be using PyCharm. We'll be using Jupyter Notebook as. And when required you're free to use anything of your choice. You can use VS code. Also we are also going to use VS code in later lectures. So I'm just showing you different methods of invoking Python interpreter. Now use whatever you feel is good for you for now. But anyways, later you will be using editors code editors like you can use PyCharm or VS code. One last thing I want to show you is the Python interpreter. So in windows open Git Bash in macOS, open your terminal and just type Python and hit enter. You should see the version of Python also. And this is the interpreter. Just like we have git bash shell like we have shell, we execute shell commands. This is the Python interpreter. You can call it as Python shell for now just for understanding. But this is called as Python interpreter. And you can interact with it like you can write the statements. But this will only work for the Python statements not the Linux command or windows commands. Not going to work over here. So same thing I'm going to write here and just hit enter. All right. So this is how you can interact with the Python interpreter. Now that would be all in this lecture. In the next lecture we are going to see how to use Python on Linux environment. We're going to talk about different versions of Python and syntax of Python programming language. So wrap this up and join me in the next one.
