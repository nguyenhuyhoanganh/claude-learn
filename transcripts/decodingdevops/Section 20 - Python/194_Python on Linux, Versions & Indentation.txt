# Python on Linux, Versions & Indentation

Okay, in this session first, we'll see how to write python script in a Linux machine and executed. And then we'll be learning basic python syntax. So I have a VM over here, I'm going to log into it. So any Linux machine is fine, a VM ec2 instance, anything. So I have a center 7 And I'm in this, as I told you python will be already installed on Linux machine. You can just see python just type Python and if you hit enter, it will open python interpreter. And if you see that's Python 2.7, not Python 3 in the red hat machines till Enterprise Linux 7, default python version is too, but in Auburn 216 and higher versions. You have default Python 3 python to print. Ate meat is different. It's just like that print in double quotes. You can write statement Okay, I'm going to close this and now let's write a python script. A very simple script that prints something and execute it. So you can use your text editor. So I'm going to use VI M first. I need to install VI m in Center 7. VI M. Python iPhone. Forced hyphen python hyphen code, dot P. Yv generally give the extension dot p. Y to a python script Python program. And then we mention The Interpreter path. Like in Basque ripped you have given hash exclamation bin bash in Python, we give the python interpreter path. So by default it is at this location user bin python In Centos 7 machine. So, that's python if your Python 3, then you'll be giving Python 3. But let's first execute with python 2. Okay, 3 print statement. The second one's of blank, so I'm going to save and quit. And now you have to make it executable chmod. Plus X, your python script path. Once this is executable. Then you can run it by giving the relative path. / script path or you can use the absolute path. So I mean root directory so Or if you have not mentioned The Interpreter path and you don't have executable permission, you can just say Python and you can give your python script path. Okay, that's python to. But you can also install Python 3. So let's search for Python 3 gems, search Python 3 Okay, there are too many python packages. Okay, some of them mostly of our them. Libraries Soul install, this Python 3 Oops, yum. Install Python 3 Python 3. Just Python 3 Ivan. Why? Okay, they are Python 3 is installed. So if you want to invoke The Interpreter, you can just say Python 3 hit enter it opens the python interpreter if you want to do it in your script let's copy our script with name Python 3 and I'm going to open that Python 3 and I will change The Interpreter path Python 3 Now, when I do that, this code will not work. See that. Now it's going to give me syntax error because that's not the right print statement in Python 3 and will change that. So this is the python print statement in Python 2 in Python 3. You have it as a function. Oh and there's no space there. And we learn the syntax, anyways. Okay, that's how the print statement is in Python 3. Okay. Now let's execute first Python 3 code. Okay, so that's how you write python script in Linux machine and execute python 2 or Python 3 red. Let me remind you again, python to support has stopped. So you should just go with Python 3. But sometimes for dependency, we use Python to like, for example, and civil uses python to Linux system, automation is better than by python tools libraries. Or should I say not better more stable. And easier, so we'll use Python to when the need comes. But most of the time we'll be learning Python 3 now. Let's see some basic script difference between bash script and python script and understand python syntax. Okay? So I am in my pycharm editor. Okay, I made font little bigger. Okay, so I'm going to show you a bash script. A simple bash script, and I'll show you same logic in Python script. Okay, these are very this is a very simple bash. Script is a variable x equal to 0 which a condition if the variable X is equal to 0. If that is 0, then we execute this code else. We execute that code and the condition ends over here f. I so it starts at if in and set f y, right? If this is true, there is a then syntax which will execute this else, it will execute that. Now, I know you must be wondering why I'm showing you bash script in Python. Well, just a moment now. I'm going to have exactly same python code. Python syntax. Okay, that's python version of the same script VX is equal to 0 printing something if x is equal to zero and you see there is no square bracket here there is no then write you write the statements else but there is a: okay after if and else and there is no ending, there is no fi there's no closing of if. So how does python know that it has to close it over there right then you have t to other print statement. Let's execute this Let's see. Run. Okay, and you get the results down here. So, it's executing the if Block in the, if block value of x is 0. So it's executing this to print statement. so, the way python syntax is Will be not be any closing. It will be done through this space indentation. So I have given Eve and I have given two space or you can give three space and the second statement is also exactly to space Knight and the third or the line number 9 if you see it starts from the beginning. So that means the if has already end over here And then else. So let me set value of X2, let's say, two and now, let's run it again. So, in the else block value of x is non zero and this statement is out of else. Because again, it starts from the beginning. If I give to space here and here then these two print statement comes under else. Now, now that's the most important syntax in Python indentation. Now in bash script, if we see we giving indentation but it really doesn't matter if you give space here or if you don't give space there, that will still work. Because there is opening and closing, right? If I give space here, the program will still work. Sure, it looks ugly but it works. But in Python if I give, let's say this here and if I mess with the indentation, You know, it's already telling us there is a problem, let's try to execute it, you see it says indentation error expected and indented block right line number 10. So there it was expecting indentation but did not found so through error. Let's do it again. Run it. Okay. Now, at line number 13, it is saying. So this print statement is indented and before that, you have again a print statement. So, print cannot come under print, right? Yeah, I fix it. Right and that works now. Alright if I give to space there and there then these to print statement also comes under else now so that's the most important python syntax otherwise python syntax is very, very easy. So if you're new to this indentation then you will get hold of it in few days. Remember, whenever you get indentation error, that means you messed up somewhere the spaces. And in few days, it will become very natural to you. Then even if you start writing other, Code, you will start giving indentation.
