# Functions part-1

Welcome to Python Functions. In this video we will be seeing how to write our own function. So far we are using existing functions or methods in Python, but now we'll start writing our own functions. Why should you write functions? Well, it's just reusability. You want to use a code many, many times. So instead of writing it again and again, you write a function. You put your code in that and then you call that functions whenever you want. Imagine you'd have written a capitalized function yourself or the code to capitalize the string by yourself. And you need to capitalize the string many many times in your program. So instead of writing that code again and again, you create a function capitalize. And whenever you want to capitalize the string, you simply call it reusability. So let's see how we can write our own function. Let's learn the syntax defining a function. So we write a very simple function. Okay, so you have to say def space and give the function name. Let's say I want to give function name add and parenthesis colon. And then in parenthesis you mention the arguments that user will be passing. Let's say argument one. Argument two, that's just some variable name. Okay, so when you call this function, you have to pass two arguments. That's necessary. So let's say I'm going to just do a total of arg one plus arg two. And then I have to return the value so I can use a return and I'll say return total. That's a function. You have def function name in parentheses. You give the arguments, you write your code and then you return a value. Now return is also not mandatory. Sometimes you maybe want to just print it from the function itself. If you don't give return statement, it will return none value. So that's a general syntax of function. Let's call this function. So you give the function name and then it needs two arguments, so We're going to pass two arguments. Let's say two comma three. So we're passing two integers and let's store its output to a variable and then we'll print that variable. Okay, so let's run it. Run functions. Do you see there? It returned five. We can just print the function itself. We can say print, give the function past two values. I'll say ten, comma 30 oh, sorry, my bad. The wrong function name. Function name is add, not out is the variable that we used. Let's run it. Okay, now let's check some stuff with return. Okay, so I do the same function and I'll give name as adder. Let's keep it here itself. Okay, so in this, what is that format? Okay, here we are calling the function storing its output. When you call a function, it is returning total. It's returning an integer that we are storing it and then we are printing it. Another function. Exactly. Same function just with a different name. But here I'm not going to return anything. Okay? What I will do is I will print it from here. Print total. First I'll just call the function 50. Now I can call the functions like this because this is printing a value, printing something. Oh, wait, wrong. But if it does not print anything, then adder is not going to return me, it's just going to execute the program. If the program is printing, then it's fine. If it's not printing, we won't see anything. And what if you print the print? Okay, I will just comment this. You can see the proper output. Okay, it's only one function now added. Okay. All right, this is where it returned 60, right? It printed 60. Give the same value here once again. And now I will comment this. So I'm saying print. So it executes the function first. That function is printing something and function also returns what it returns. It returns none. Even though you have not given the return statement, there is an invisible return none like this over here, which returns the value, and we are simply printing that value. When we say print, we're printing what adder function is returning, which is basically none. Okay, so uncomment that. Okay, let's write one more example of function and expecting one argument. Okay, so this is a function that's going to do some of all the values that you passed. All the values could be in a list or a tuple format. So I have to pass a list or a tuple of integer to this function. Okay? So I'm going to do x equal to zero and then I'm going to iterate for I in arg So arg again will be a list or a tuple and then simply do x equal to x plus i. So value of x will be first zero plus whatever have passed. It's going to keep adding all the values and then we'll say return x. Okay, I have to make sure I return it. The return should not be in the for loop, okay? Otherwise it's going to just hit return in the first iteration itself, the function will break. Okay, let's call it now let's take a variable out and let's call this sum and let me pass one, two , three, passing simple values. So you can also do the math yourself. Okay, so I call this sum function and it's going to add all these values, return the output, and that output will print. Okay? So one plus two plus three, that's six. So it should return six. Let's see. Nice. Okay, let's do ten plus 20 plus 30. That should be returning 60. So, three values, ten values. Pass any number of value, pass any integers and make sure it's integer. If you try to pass a string, it's going to break. Okay, now let's talk about the arguments. So my function takes one argument, and I have to pass exactly one argument. What if I don't pass any argument, right? So let's try to call this function and let's not pass anything. Okay? Let me just run it. So it's going to throw me an error, some one missing argument. Okay, let me just comment this. Let's run it again. So my function needs one argument and I'm not passing anything. What if I pass multiples, right? I pass let's say ten comma 20. And then I pass one more 30 comma 50. So I'm passing out two arguments, two  list. Well, it says it takes one argument, but I have given two arguments. So it's asking me one argument. So I have to pass one argument. Okay. But this can be flexible. We have some options. We will be seeing how we can pass any number of argument or how we can pass or not pass any argument. But still it works. Okay, so working on the same. So we'll talk about first the default argument. Write a simple function. The functions are going to be very simple. I just want you to look at the arguments. Okay, so function called greetings. Greetings. Yes. Okay. And an argument MSG message. And then simply print that. I'll say, good message. Okay, now we know if I'm calling this function greetings, I have to pass an argument. Okay? Let's say morning. I don't need to store its output anywhere because it's really printing. The function is really printing. So I don't need to store and print it back. Let's run it. If I don't pass anything, we know what's going to happen. It's going to throw us an error. Right? Okay. I want to have a function that should have a default value for my argument. If I don't pass, it should take some default argument. Okay? And for that it's pretty simple. Wherever you have the argument, you just say equal to and give a value. Okay? So I'll say morning. Let's call this function now. Okay? Good morning. So message value is default value is morning. But I can overwrite that. I will say evening. So when I pass evening, evening will be stored into MSG. Okay? And then it's going to write morning to evening. You see that? Good evening. This is called default argument. Now, the order of arguments are also important sometimes in function. So let's write a function that gives feedback to us. Just a print statement based on the vaccine efficacy. So we'll pass two arguments, vaccine name and its efficacy. And it will just return some print statement. So I'll name it as vac_feedback and I'll take two arguments. One will be the name of the vaccine. So I'll take the argument name as vac and efficacy. I hope that's how it's pronounced. Okay, so vac is going to be a string and efficacy is going to be an integer. So print a message vaccine given a percent there. And then we'll compare the value. We'll check the difference. I will compare between two integers. Okay. We'll say efficacy is greater than, let's say 50 and it is less than 75, less than or let's say less than equal to. Then I'll print a message. Seems not so effective. Well, let me give a disclaimer. I don't understand anything about vaccines, so I don't know that will be effective or not. But I'm just printing a message. It needs more trial and I'll compare with some more values. I'll see. If it's greater than 75, less than 90, well, can consider that vaccine. Let's give one more. If efficacy is greater than, greater than or equal to 90, sure will take the shot. And else should be no less than 50, right? If it's less than 50, then needs many more trials. Okay, so our function name is vac underscore feedback and we have to pass well, first I'll pass the name pfizer and the integer. Okay? So I think it's 95 and let's execute this and let's pass a few more values. Let's pass something else unknown that seems dangerous, right? And needs more trials. Okay, good message passing. But what I want to show you here is I need to pass the argument in the right order. What if I pass efficacy first and then the name? Let's try that. Okay, I got an error. Okay, let's comment this to run again. Now, the problem here is 35 will be stored in a vac and it's going to compare efficacy, which is the name. Now, so it's comparing a string with an integer. And that's why we're getting an error not supported between instance of string and integer. So order is very important. But I'm showing you keyword argument where we can pass the order ourselves. We can give the names, keywords arguments. Now, here what I'll do. I'll say this is the efficacy and this is the name of the vaccine. When I do that, then order is not important. If I know the name of the argument, I can pass them in any order. I have to just make sure I give the argument name equal to the value and the same way then it will be passed. So efficacy will be stored in efficacy and vac will be stored in vac. This is called as keywords argument.
