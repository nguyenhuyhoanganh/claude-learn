# Built-in Functions or Methods

Welcome to Python documentation. In this video we'll be talking about built in functions and methods. So Python has lot of built in functions that can help you do variety of things. You don't need to write code for them. So these are list of the built in functions that Python has. We have used some of them like we have print that we've been using a lot type we are using to get the type of variable str. We have not used it but you can use it for type casting. If you want to convert an integer to string, you can just put that integer into the string or the integer variable into the string function and then it's type changes. You have used input to take the user input. There is a length function that's going to give you the length and there are many more you can use as per your need. But the focus of this video is the data type which we have learned string, list, tuple, dictionary. We're going to see functions, built in functions for that. For this I found this w three schools documentation helpful. But you can get all those things in your interpreter also. But this is just a better view. So Python overview, you have their Python built in functions, right? The same list as we've been seeing. And there are examples also if you click on them I want to show you this string methods for example, like for example capitalize, it's going to convert the first character to the upper case or count it returns the number of the times a specified value occurs in a string find if you're looking for a substring in a string. And then you have some methods for list and then you have methods for dictionaries. We have used some dictionary methods, dictionary values to return all the values or you can use dictionary keys to return all keys. So let's see some example for string, list, dictionary. For this one I'm going to use PyCharm because this gives a lot of help in finding the right methods for us. So let's create a script. Let's see first strings built in methods or functions. I have a string here message is the variable and that's the string. Most of them are more than oh, that's a long sentence. Yeah. Okay, so in this sentence if you see c is capital, it's a sentence. So it should be capitalized c should be in upper case. So if I just print it like that, okay, so I can print it like that or I can use a built in method for the print. So after the string you hit a dot. Now in PyCharm, if you're in PyCharm or any ID, Python ID, then it should list all the available methods. Like you see format you have capitalized and few others. You can just scroll down this list, you'll see all the list, the entire list. So let's say capitalize I want to use that. Let's see what it does. So that's what it did, right? It made the first letter in the upper case. But this does not happen on the string. Actually, strings are immutable. So if I print it once again, see, really the value of the variable did not change, right? If you want to change, then you can store that into a variable and then you can print it back. Okay. Like I'll say, message capitalize and then store it back into message. And this message I'll keep as m capital. Okay? So that's a different variable. I'm storing the output of this function into that variable. So that is good. I want to show you here one built in function called as dir If you're not coding on PyCham, but you still want to see the available built in functions, you can use dir function. You can say dir and in that you can give your variable like I want to give the string message. So it's going to return all the built in function for the string. Oh, I have to print it. Sorry, print. So the output of the dir method. So it is printing all the available built in functions. It's in a list format, you see? There is  capitalized, center, there is count. So what do you put there? Let's put a list, right? So that's list built in methods. Okay. So you can use dir function. You can put a string or a list or a tuple or a dictionary and you can see the available built in methods for it. Okay, I'm going to comment that out now. Okay, this is good for now. Okay, let's see a few more example of built in functions that we can use. I'll say message upper. Okay, make the entire thing upper case. Yeah, everything is upper. Or like that you have lower anyways, it's in lower case. Or you have some nice methods that's going to return boolean value. Like you want to check whether it is an alpha numeric or is alphabet, or is ASCII or is decimal, or is digit or is lower. Let's see islower. So it's going to return a boolean value. True or false. So everything is in lower. So it's returning is lower. True. That should return false, right? Yeah, that's false. It's not in upper case, right? Everything is in lower case, right? Or you can like that check if it's alpha numeric. These are good for data validation. So based on, let's say the user entered or you ran some queries, it returned some values. You want to check whether it's alpha or alpha numeric or if it's uppercase lower case. And based on that, you can do the further operation in your code. Okay. Few interesting ones are find which you may be using many times. Like you are looking for some substring in the message string. Okay, maybe ready? Okay. So let's run that. It returns a number 18. Wait, let's comment this? No, that should be fine. Okay, so that's our entire string. And from that string, message string, we are checking whether ready substring exists on that and it does exist, right? And it returns a number. That number is actually the index value where it starts, right? So if you count that from zero, you will reach till 18 where the R starts. And now if you want to just print that substring, you can do a little math. You can say message and you can say starts at 18. And the number of character, so ready, is what? Five. So 18 plus five, you get 23. So we have to give 24 because it's still 24. Let's run that. So you can like that, you know do a string slicing. First find it and then slice out of it if you're looking for something that does not exist. So let's say looking for character, let's say not, which does not exist in the string. So it returns a minus one. Minus one, the last index. So what happens is it's going to travel through this entire string, it reaches till the end. If it did not find reaches till the end. And that the last index value returns minus one, that means it did not find that. Okay, so these are some of them. There are many string functions. Whenever you need, you can use them. Let's take one more example which will be also useful. So I'll say sequence one, just a variable name equal to and I'll give a tuple, I'll say 192. So it's a sequence of string, right? So all these strings I want to join together by my own string. So there's a join function. Let me print that. So I will say join it with this string. Let's say dot and the join function. And I will give the variable sequence one. So all the strings in the sequence will be taken and it will be joined by dot. Do you see that? Now that looks like an IP address and it really does not. Or maybe you have something like that, maybe you know some date in your own pattern you want to print. Okay, like with forward slash, with hyphen maybe. These are some commonly used built in functions by us. I mean to say DevOps. When we get some data, we're doing some cloud automation, something. We get some data, we find that data and we process it. And then based on that data, we do more operations. So in such cases, data validation, finding your string, combining them and these operations will be very helpful. Okay? Now let's go to list. I told you one thing about list is they are mutable. You can change it, but how do you change it? That I did not tell you. So you can edit a list through the builtin functions. Okay, so let's take some example. So, list of mountains. I'm not so good with the geography, but I just kept some name and I just print it just like that. And now I would like to add edit this. I want to add something into this. So I'll take the list. I'll say dot and you'll see all the built in methods. I'll go with append and I'd like to append Oregon mount. Okay. And let's also print it back again once it's appended. Okay, let's run it. Okay, so append is going to add it at the end, right? You can also combine two lists together which is called as extend. Okay. So I want to extend the mountains list with my own list. Let's print it back. Okay, so the mountains list will be combined with this list. Let's run it. Okay, see that? Okay, that's still adding at the end. What if I want to know insert somewhere in the middle. So mountains and actually there is a method called as insert. insert. And I would like to insert it at the second position. Okay, that will be actually the third, right? Like that. And I want to add there mount abu. And again, let's print it back. Now, one thing, let me remind you, when I'm hitting run, it's executing everything again, right? So mountains will be executed again. So variable again, it's going to store the value and then it's going to append. So all the operation is doing again whenever I'm running it. See that there we get in edit comes also deleting. Maybe I would like to delete. So if you say pop it's just going to remove the last one. Okay, last one is satpuda So let's run it. You see that? If I run it many times, that many times it's going to pop. Three times, right? But what if I want to pop or delete a particular item from that, right? So I'm going to say pop whatever is at the third position. I didn't even count what it is. Okay, let's find out. So you got this 01234 five. So k two will be gone. Okay? See that k two is gone. Okay? Tuple. There are not many because tuple is immutable. So you can't make many, can't make any change to it. So I'm going to directly go to now dictionary. Okay? So let's take a dictionary, okay? A dictionary from our previous example. Okay, let's print the dictionary. But I want to print only the keys of it. So dot and you have the keys method, right? That will just give you all the keys, right? So name, skill, code, right? Those are the keys. Or same way you have the values. So you can get all the values. These are the values. You can also clear the entire dictionary. You can say just dot clear. Let's print it again. So everything from this dictionary will be gone, cleared. You see, now we have an empty dictionary, okay? Also we have few more and if we hit a dot to the dictionary, you'll see we've seen values clear keys pop. Okay, you can also remove you have to give the key, copy, get, items, pop item, you have few nice methods useful. So sure, there are so many methods, we really can't go through all of them, but as per your need find this. You can do a simple Google search that I need a method to do so and so, and you may get the result. Okay, you can just check the Python three documentation, or you can just run the methods. Or if you're using PyCharm, you can just give the data type dot and you get the list. So that's all in built in methods and functions. In the next session, the next video, we'll be seeing how to write our own functions. So I'll see you in the next one.
