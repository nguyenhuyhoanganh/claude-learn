# Quotes and Comments

Well, in this video, we'll be seeing quotes and comments. So let me write a new Python program. I will name it as quotes. And comments. Okay, hash is a comment in python python also as same in bash, okay? So this is a single line, comment, Then you also have multi-line comment, you can use this triple quotes. So type double quotes 3 times, it will also automatically end it with three or we just call it as triple quotes. so, I can just say this is Multi-line comment. Okay. Now let's try to execute this. Since this is comment python interpreter will ignore it and you should not see anything. Right? As I said nothing because this is ignored, this is a comment, okay, that can be done same through single quotes. Also, Okay, let's execute it once again. Should see nothing. Okay, now let's see quotes in print statements, okay? So let me assign a variable. I will see skill and that is equal to A string we call this as a string, anything that is in double quotes, or single, quote is string, it's one of the data type. So I'm assigning a string into a variable skill. You can give this space over here, okay? Or if you want, you can remove it, okay? So anything that is in double quotes or triple quote is sorry, double quotes or single, quote, is a string even in triple coat also and now let's print it. So print Francis, I am going to give The variable skill. Okay, let's execute it. Night. So it prints. Now, if I put that into double quotes and execute this then it will be not considered as a variable but considered as a string because it's in double quote All right, so in double quote, this is same as the single quote, but this so if you see a text that is not enclosed in double quotes or single quotes, or triple code then that he's of variable. Like in bash we need to give a dollar in front of a variable here. We don't need to give anything less syntax. Let's execute Okay, so this is a string, this is a string and this is a string variable. Now, paragraph string triple quotes front and I'll give triple quotes and then if it's a paragraph string I can write it in multiple lines. Okay, you see that? That's a string in multiple lines, right? So only that will work. If you have given triple quotes either using the double quotes or the single quote, let's run it. See the output. Okay. Also in single triple quotes, So, if you have a string or text to print, you have to give it in the court. If you don't, let's say, I want to print something. All right? I'll say this is text and if I don't put this into the court and try to execute this, See, I get an error. Invalid syntax, right? So I have to put it into double quotes or single code, then only it will be considered as a string. So that was about quotes and comments.
