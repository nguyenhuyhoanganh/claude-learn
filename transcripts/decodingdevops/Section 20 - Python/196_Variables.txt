# Variables

Okay, now it's time to learn some variables, how to define variables, different types of variables. So let's write a simple Python script for that. And we'll just simply name it as variables. Ok, we already have seen variable assignments, let's do it once again, variable assignments. I'll just give a comment here. So let's say var one space equal to space in double quotes. I say Python. Okay, var two equal to and space, no double quotes. I will give some numbers. 7575, variable three, 3.5. And let's print them. Okay, so print var one. Now, all these three assignments of variable. Where you have space there and here no space. Then other side space. These are all valid. Okay, but what looks good? That looks good. Yeah. So this looks better. Okay, and also these three variables, var one, var two, var three, there are different types of variable. First, this is a string. So variable is of type string. This is an integer. So variable is of type integer. VAR two is integer. VAR three is having value float. So VAR three is variable of type float. Let's print all of them. Print VAR one, VAR two, and VAR three. Run them. Okay, so we have seen single assignment. You can also do multiple assignment. Okay? Like I'm going to say, I'llsay A is equal to B, which is also variable, and B is equal to C, which is also variable. And that is equal to let me give some number 65 integer. So integer 65 is stored in variable C, and then value of variable C is stored in variable B, and value of B is stored in variable A. So all of them will have same value, basically. I know that looks kind of funny, but that's a way of doing multiple assignments. Okay, let's print them. You see ABC, they have the value 65. Okay, now again, multiple assignments, but this time with different different values, okay? So let's say variable x, y , z is equal alpha, beta. I'll give some integer there twelve. Now here comma is a separator, value of x will be alpha, value of y will be beta, and value of z will be twelve. Print. And this time I'm going to print it with a string. I will say variable x, value is and after the double quotes, comma x. So in here, it's going to print the string and comma it's going to print the value of x. That is a variable and I'll give a space there likewise, value of y and value of z. Let's execute this. And you see it has printed the string and it has printed the variables value. I'll add little more variables. I'll add W. Now, the value of W will be alpha and variable z value. Let's give 5.4. So W is string, y is an integer, z is a float. So let's add one more print statement and let's execute it. Now, these are all the variables that we have assigned. So we know what is string, what is an integer and what is a float. But Python interpreter can also tell you that if you ask by using a type function, there is a function called type. If you put the variable in that, it will return its type. So let's print it. You see it says it's a string, likewise X-Y-Z. You see string, string, integer and float. And now same with string. Oh, I'll remove this W. Okay, that is better variable W is and then it's going to return its type, likewise for X-Y-Z. So you have seen what is a string, what is an integer and what is a float. Now we'll see few more data types. So I'll create a new script for this and name it as data types. So first we know string in double quotes, single quote, triple quotes and triple quotes with double quotes and we have seen integers and we have seen floats. Now we'll see another data type which is called as list or in other programming language, this is also called as array, collection of variables. So we can have multiple data types, so we can have strings in that we can have integers list and it will be enclosed in square brackets. So let's take a variable name. I'll just give it first list because this is our first list and it will be in square bracket. So you can have a string there. Now that STR one is alpha. You can just give string just like that in a double quote. Let's give DevOps. You can also have integers in this. You can have integer variables and you can even have floats. So it will be in square bracket and all the elements will be separated by a comma. And that space is not mandatory, but it's good to give, looks good. Let's print it. Let's run the program. Do you see that? So we had given the variable STR one and that's the value of it alpha and we have also given number one and its value is one two three. Now, there is one more which is similar to this and it is called a Tuple or Tuple and it is also a collection of multiple data types. So what's the difference? Well, it's going to be in round bracket. Now you'll be wondering why just the bracket. That's it. No, actually the difference is between Mutability and immutability. List is Mutable and Tuple will be immutable. That means you can edit a list but you cannot edit a Tuple. Sure you can overwrite, but you cannot edit the content. So let's take same example. I will name it as my first Tuple and instead of round bracket, I'm just going to instead of square bracket, I'll just give the round bracket and let's print it. Let's execute the program. So you see visible difference is just the brackets, but as I said, Tuple is immutable. You cannot make changes to it. So think of it like a CD. You can't make changes in the content of CD, right? And think this list like a pen drive. So you can edit content in the pen drive. You can change the data that is in the pen drive. You will say I can overwrite CD. Well, sure you can overwrite Tuple also, but you can't make the change in the content. Okay, the next data type will be dictionary. Well, dictionary will be also collection, but it will be collection of key value pairs. Like a normal dictionary, you have words and its meaning. They're always in pairs. All the data in the dictionary is in pair. Same way here also dictionary all the data, all the elements will be in pair, key and value. So it will be always in key and values. So you'll have key colon and you will have its value and it will be in curly braces. So let's give a variable first dictionary equal to and the curly brace. And then let's give key and value pairs. So let's say name is the key which is a string and its value. I'll give another string my name, I will give another key weight and its value in KG's integer. So I'll say 75. No, that's not my real weight, but I'm trying to achieve that now. Obviously I'm more than that. And what kind of exercise I'm doing to do that? I'll say exercises value and I'll give a list. So I am doing boxing and dancing. Jogging, sure, one more I'd like to add, but I'm not doing that. So that's a dictionary key and value. Now, value can be a string, can be an integer, can be a list, can be any other data type. Let's print it, let's execute. Okay, you see that the visible difference to understand between all these data types is this list will be in square bracket, tuple will be in round bracket and dictionary will be in curly braces. And again there will be key value pairs in dictionary separated by comma and of course the strings will be in quotes. And then you have integer and floats. Okay, one more print statement. I'll give print, I'll say variable first and likewise Tuple and dictionary. Let's execute. Okay. It's very important for you to understand all these data types because when we get into ansible and when we see JSON format, YAML format, we'll be seeing their strings, we'll be seeing list and we'll be seeing dictionaries there. And they'll have almost same syntax, almost. So if you understand this, then I can very easily explain you JSON and YAML. And being the DevOps, you should be very good in reading and writing JSON and YAML. Almost every automation tool today uses JSON or YAML. For example, ansible Playbooks are in YAML format. Their output is returned in JSON format. Cloud formation you have JSON and YAML and nowadays, the new tools that are coming in or new way of configurations are also in YAML format. So stress yourself more. Assign your own key value pairs. Use your own dictionaries. Give your own list tuples. Use your own values. Print them. Print their types. So you get very good hold of this. Okay? One last data type I'll show you which is Boolean, true or false. X is equal to true and let's say Y is equal to false. So that's it. No double quotes, no single quote, just true or false. T capital and F capital. So that's Boolean. So this will be very good when you do condition checking. When we check a condition, it's going to return true or false. And based on that, our logic will get executed. Let's print it. Let's print its type. You see bool. Bool. That means boolean, right? So there is too much printing here. And in the next video, we'll see different types of print, different ways of printing.
