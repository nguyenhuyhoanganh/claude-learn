# Lab Solution - Resource Limits

Instructor: Okay. So, let's go over the labs on resource limits, resource requests and limits. So, the first question is, a pod called rabbit is deployed. Identify the CPU request requirements set on the pod. So, let's do kubectl get pod, and see if there is a rabbit pod. So, let's do a kubectl describe pod rabbit. And, if you look at this section right here, we have limits, CPU limits and CPU requests. So, the CPU requests as requested here is one. So, let's select one. Okay, the next task is to delete the rabbit pod. So, kubectl delete pod rabbit. Okay. Okay, now another pod called elephant has been created. So, let's check that. Yep, so we have a pod called elephant and it fails to get to a running state. So, inspect this pod and identify the reason why it's not running. So, let's do a kubectl describe pod elephant. And, if you look at this section here, called Last State, it says, the reason is OOMKilled, right? So, that's, basically, that indicates that it's a memory issue. So, the reason is OOMKilled. So, let's select that. So, the status OOMKilled indicates that it is failing because the pod ran out of memory. Identify the memory limit set on the pod. So, the memory limit is, as you can see here, it's 10mi. Right? So, that's the memory limit set on this pod. So, the elephant pod runs a process that consumes 15 amount of memory. So, increase the limit of the elephant pod to 20, and delete and recreate the pod if required. So, one thing we have to do is to change it. So, as you can see here, the command used to create this pod is, is the stress command, and it is stresses. So, it basically gets the pod to use 15 megabytes of memory, but the limit is 10. So, that's the reason why it's getting killed. So, we've got to change this to 20. So, one of the ways to do that is to do a kubectl edit pod elephant. But the problem is, we might not be able to change the limit with an edit. So, we're going to, let's give it a shot anyway. So, let's give it 20 and save. And what this says is, that you cannot, basically, edit the limits set to a running pod. So, So, anyway, let's come out of it. But, our changes are saved in this file. So, that's a temporary file, right? So, let's check that. And, if you look at the resource limits that we have set, so it's actually saved here. Our change is saved here. So, what we could do is, we could just use this file to recreate the pod, right? So, we can either delete the pod and then recreate it. Or, an easier way is to just use the kubectl replace command, and then use the force so it actually deletes the existing pod and recreates it, and give that file. Just change it here, kubectl replace force dash F, and then the file name. So it, basically, deletes the elephant pod and then it replaces it, recreates it. So, let's see the status now. And we see that it's in a running state. Let's also verify... that, yep, the limit that we set is now reflecting. Okay? Yep. We've just checked and made sure that it's in a running state. And, the next task is to delete pod. And, we'll just give it a few seconds for the process to terminate. Okay? That's the end of this lab.
