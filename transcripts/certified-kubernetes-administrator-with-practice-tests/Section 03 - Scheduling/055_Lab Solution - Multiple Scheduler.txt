# Lab Solution - Multiple Scheduler

-: Okay, so let's go over the practice test for multiple schedulers. For what is the name of the pod that deploys the default Kubernetes scheduler in this environment? Okay, so let's do a kubectl get pods command in the kube-system namespace. So we're gonna look here. And we see the pod is kube scheduler-control plane. So let's select that. Now, what is the image used to deploy the Kubernetes scheduler? Inspect the kube scheduler, pod and identify the image. Let's do a kubectl describe pod scheduler. And then specify the namespace as kube-system. Okay, so here we have the image and that is case.gcr.io/kube-scheduler version 1.23.0 as of this recording. So I'm gonna select that. Okay, now we've already created the service account and cluster role binding that our custom scheduler will make use of. So all of these are already created. If we'd like to take a look, we can do a get service account my scheduler. Okay. So we see the my scheduler service account and the kube-system namespace and similarly, there are cluster role bindings as well. So that's already done for us. So we don't have to worry about that. Let's go forward. And let's create a ConfigMap that the new scheduler will employ using the concept of ConfigMap as a volume. So we have to create a ConfigMap with a name using the contents of the file, so there's already a file here. And this is the configuration. It will be passed through to the new scheduler that we're gonna create. So the file is there. We just have to create a ConfigMap for it. So we'll do a kubectl create ConfigMap. And the name will be, let's copy and paste. And it has to be created from file. We use the form file as data path to the file. And also specify the name system. This will be kube-system. There's no name. Okay. Let's verify that it's created. It's in the kube-system namespace. Okay, so that's created. Go next. Deploy an additional scheduler to the cluster following the given specification. Use the manifest file provided at root my scheduler. So there's already a file given for us. Let's add root. My scheduler.yaml or just since we are already in that directory, you could just give the file name. And we see that most of the work is already done. The liveness probe, the readiness probe are all set so we don't really have to worry about that. And all we need to do is add this image that's given here and we have to use the same image that's used by the current scheduler. So let's find that out first. Let's do a kubectl pods -a. And this is current scheduler. So do a describe. And kube-system. And let's do it for the image. And let's get this image ID. And we're going to put that in in the file instead of this placeholder here. Okay? And if you look at the rest of the file, it already has the ConfigMap that we created. If you scroll down, you'll see there is the ConfigMap that we created. So that's already configured. And that basically passes through as a volume mount here. We'll talk about ConfigMaps in the upcoming lectures, if you haven't already gone through that yet. And we're going to create this. Let's verify that it's created. In the kube-system namespace, we have my scheduler here. Let's check. Okay, so that's done. And the final step is to create a pod. So there's already a pod definition file given and use it to create a pod with a new custom scheduler. So here we have the NGINX pod definition file and this is pretty straightforward. Just a NGINX pod with the NGINX image. And what we need to do is specify the custom scheduler, which is basically my scheduler given here. So as we learned in the lecture, you have to add a new field here called scheduler name. And then we specify the name of the scheduler we just created. Save that. And then we create a pod. And we make sure pod is running. Okay, so that's it. And that's the end of this lab.
