# Lab Solution -  Taints and Tolerations (Optional)

Instructor: Okay, so let's go over the lab for practicing taints and tolerations. So let's start with the first question. How many nodes exist on the system? Including the control plane? So let's look at the number of nodes and we see that there are two nodes, control plane and node zero one. So the answer is two. Okay, so the next question is, do any taints exist on node zero one node? So for that we're gonna have to take a closer look at node zero one. So we do a kubectl, describe node, node zero one and we see taints right here. And we see that there are none. So there are no taints on this particular node. So I'm gonna select, no. It's going to clear my screen. Okay, so the next question is to create a taint on node zero one with key of spray value of martin and effect of NoSchedule. So we're gonna continue with the, the silly example that we used in the lecture of the spray and martin, and mosquito and B. So here the task is to create a taint. So we're gonna do kubectl taint and let's quickly look at the help. So the command is kubectl taint, and then nodes then the name of the node, and then followed by these values, which are the key has to be spray, and then the value is martin. And then the effect is NoSchedule. So let's do that. So it's kubectl paint, node, and it's node zero one, and then the key is spray and the value is obtain, and the effect is NoSchedule. Okay? Okay, let's confirm that. So we have the spray equals martin and the NoSchedule effect, right? So that's, that's done. So we now have attained on node zero one. Now the goal is to create a new pod with the engine X image, and the pod name as mosquito, right? So we're gonna do a kubectl run, and then, that's the pod name. Image equals engine X. Let's do... Get pods. And we see that, it's created. Ignore the, the, the status for now. Okay, so what is the state of the pod? So it seems to be in a pending state. So select pending. Now why do you think the pod is in a pending state? Is it because image is not available? Is there an error? So let's see, let's do describe pod in the pod name. And here you'll see the, the reason why it's in a pending state. It says that the pod didn't tolerate one node that had taint and spraying martin that the pod didn't tolerate. So basically the pod mosquito cannot tolerate the taint. martin, that's, that's the problem here. So we're gonna select that. Okay, so now we have to create another pod named B with the engine X image. Which has a toleration set to the taint martin. So we're gonna do a kubectl, run command, to create a pod. The name is going to be B, and the image is going to be engine X. And basically, now we have to add toleration to it, but you cannot specify toleration in the command lane. So what we're gonna do is we're gonna do a, a dry run, equals client, to generate AMO file for this pod. So that's the AMO file and we're gonna redirect that to B.AMO. So we have this in, in B.AMO, we're going to edit B.AMO file, and we're going to add in tolerations. So add a section called tolerations. Now if you're not sure of the, the format check out the Kubernetes documentation pages, and search for chains tolerations. And that's the first result. And these are, this is the command for taint, and this is, this is the format for tolerations. So dash key operator value and effect. So that's key. So key is going to be spray, operator, value, and effect. So the value is going to be seen, effect is going to be audio, finally we have operator just to be equal, operator set to equal. You save that... And let's create image. Okay, so it's now in a container creating state. Let's watch the creation process and we can see that it's now in a running state. So this part is now running. Okay, so that's, that's correct. Now noticed the B pod was scheduled on node zero one. So let's see where it was scheduled. And we see the B pod is on node zero one. So basically, now this B was able to tolerate the taint set on node zero one. Now do you see any taints on control plane node? So let's look at the control plane node and let's look at taints. And we see that yes, there is a taint set on the control plane node and it's set to NoSchedule. That's the effect. So yes, it's set, it's set to NoSchedule. Okay, now remove the taint on control plane, which currently has the taint effect of NoSchedule. So we've got to remove that. So first let's get that. So this is the taint, so let's copy that. And the command to remove it is the same kubecolor taint. And you just specify the node, that's control point node. Specify the 10 that we wanna remove and at the end you've got to put a dash or a minus for it to remove that 10. So it says it's untainted. So let's confirm that. Go back to taints and see it's, nothing is set. Just click on check. Okay, so that's, that's done, and now we've got to check the status of the pods again. And we see that the, the pod mosquito, which earlier was in a pending state, is now in a running state. So that's the state of, of the pod mosquito. And then we have to... Let's check at what, on what node is it placed. So as you can see it's now placed on the control plane node. So earlier at, in the beginning, the control plane node has a taint set, set on it, and then we set a taint on node zero one, and then we created the mosquito pod. And so it was not placed on any of the nodes because both of them had a taint then. Then we created the B pod with the toleration on node zero one. So the B pod was able to be placed on node zero one and then we removed the taint on control plane. And without doing anything the not, the, the pod mosquito was automatically placed on control plane because it no longer has any kind of taint on it. So it's, the answer to this is control plane. Okay, So that's the end of this lab.
