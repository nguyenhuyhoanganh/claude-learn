# Lab Solution - DaemonSets (optional)

-: Okay, so let's go through the lab on demo sets. So the first question is, how many demo sets are created in the cluster in all name spaces? So let's do a cube cuttle, hit daemon sets. If you look at the default name space, there are none. But if you wanna look at all name spaces you do a dash A and we see that there are two demo sets in all name spaces as the cube flannel DS and cube proxy. So that's two. So the next question is which name space are the demo sets created in? So if you look at the name space column here we see that they are in the cube system name space. So, select cube system and which of the below is a demo set. So if you look here, we have the cube final DS and cube proxy, these are the demo sets. Should you core DNS, city master. These are not, it's the cube final DS. That's the demo set. On how many nodes are the pods should you'll buy the demo set cube proxy. So if you look at cube proxy here, we see the the design and current ready numbers are one. So it's just, it, it's one. But let's also look at it in detail, so we go describe demo sets cube proxy. Okay, the name space is you system. And that's that, okay? So here you can see the design number of nodes scheduled is one current number of nodes scheduled is one. So it's just, it's one, right? And that's also because if you looked at this cluster, there's only one node and that's the reason it's just one, okay? And what is the image used by the pod, deployed by the cube final DS team set. So let's look at the, let's look at the cube final DS in a bit more detail. So describe demo set cube flannel DS, and then the next space is cube system. And we see that the image is the one under core RS flannel, so that's this one. Now the next one is to deploy a demo set for fluent D login using the given specifications. So one way to get the specification for daemon set is to go to the coordinates documentation pages and get these, right? But since the, the structure is very similar to a deployment, an easier way to just do it from here from the command line is to use the dry run option for a deployment and create a file and then change it and just make the necessary changes, right? So let's assume that we're gonna create a a deployment because there is no create demo set command as of this recording, right? So we're gonna have to do a, create deployment. And the name is what's given here. So it's elastic search, name space is cube system image is this. And then we're gonna do a dry run. You're gonna do dry equals one. And then we're gonna do an output to ammo, right? So we'll write that to just call it find the ammo. Then we're gonna edit this file. So the kind is going to be demo set. And we don't need replicas, because demo sets don't have replicas, so you can actually look at it and compare it here. So there are no replicas, so we're gonna remove that and then we'll get rid of the strategy and this one right here. And the rest of everything looks okay. Let's save that file and just create it. And the name space is Cube System, and we have that demo set created. It's not ready yet, but yeah, that should be okay. So this is, of course a very, a simple example. If you've not provided any kind of options or parameters for this demo set so we're just gonna give it super simple. But yeah, as far as this task is concerned, that's all that's asked. Okay, so that's the end of this lab.
