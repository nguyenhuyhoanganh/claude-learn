# Lab Solution - Static Pods (Optional)

-: Okay, so in this video we're going to go over this lab on static pods. So the first question is, how many static pods exist in the cluster in all of the named spaces? So let's go, let's do get pods dash a to list the pods in all named spaces. Now one way to figure out, or you know differentiate a static pod from the other pods is to look at its name and at the end you'll have the node name appended to it. So this one, this one, this this is a static pod created on control plane. All of all of these three are, and as well as this. So there are four static pods and then there are no others with a visible node name to the end of it. So that's one way to look at it. Another way to be sure is to look at one of the pod in a bit more detail. So let's say for example, this one let's do you could get pod and then we do we provide the name space, which is the system. And, but this is just going to list the pod. It's not gonna give us the information that we need. We need to look at the YAML format of the, of the pod So we're gonna do a dash O and YAML So this is gonna give us a lot of information but from this, what we need to do is we need to look at the owner reference section. So if you scroll down there's this section called owner references. And under this you'll see the owner of this pod is the node the kind is node, and the name is control plane. So that's one way to be sure if a a pod is a static pod or not. And if you look at the screen, and if you look at the same for another pod that is not a static pod cube system to YAML output. And if we look at the owner reference for this right here we see that the owner of this is a replica set right? So, so this is not a static pod. So that's one way to figure that out. There are ways that you can do this for all of the pods like using filters and selectors. We should be able to kind of go through this, traverse this this file and then look at the owner reference. But we haven't learned about filters and you know, advanced kubectl commands yet So we'll skip that for now. But this is an easy way There are two ways to find out if a pod is static or not and one of that is to look for no names at the end of it. And to be sure you can use the the command that I just shared. So we know that there are four static pods and they're all on control plane now, as of now. So let's run the command again. The question is, which of the below components is not deployed as a static pod? So if you look at this, we have the ETCD it's a control plane, ETCD is a static pod, cube PPA server is a static pod cube control manager is a static pod and cube scheduler is a static pod So not Core DNS Core DNS does not seem to be a static pod. So let's select that. Okay, which of the below components is not deployed? It's a static pod. Again, we know that all of these are except for the queue proxy. So the queue proxy is not a static pod. On which notes are the static pods created currently. So all of these, as we just saw, are on control planes. So control plane is answered to this question. Now what is the path of the directory holding the static pod and the definition files? So one way to figure that out is to look at the the cubelet conf. So, so our lib cubelet and then we have the config dot yaml And if you look into this file, so this is the cubelet configuration and we have learned that it is the cubelet that creates the pods. And there is this specification here that says static pod path. And it is currently said to the ETC Kubernetes Manifest directory. So let's look at that directory and we see that this is there are the files that are used to create the static pods. So that is slash etc slash kubernetes slash manifests That's the correct answer to this question. And also remember that this is the path to check the static pod configuration, the static pod path configuration on any given node. Okay, so how many pod definition files are present in the manifest folder? So that's 1, 2, 3, 4 4. And that is equal to the number of static pods that we have here. And what is the Docker image used to deploy the cube APA server as a static pod as a static pod. So if you look at cube APA server Here, so this followed by this and if we look into the image we see that it's kh dot gcr dot io slash cube APA server version 1 point 2 0 0. So as of this recording this is the version you should make sure that whatever the version needs that you are working on make sure you choose that as the answer. Okay? So the next question is to create a static pod named static pod static busy box that uses the busy box image and the command sleep. So we know that the command to create a pod is run and the name is static busy box, the image is busy box, okay And the command is sleep 1000. Now what we need to do is we don't want to create this I said we just wanna get the pod definition file and place it in the static pod path for this node, right? So we're going to do a dry run for this. So dry run here. Now when you, when you use the command option always remember not to place any option that is for the kubectl command after this, right? So anything that you put after this especially after the two dashes here is going to be considered as an option for this command. So do not place anything. So the, the command should always be at the end. So in this case I'm gonna put the dry run before the command option. So any, any option for the kubectl utility should be before the command option. So dry run equals find and then dash O yaml So I get this and okay so I'm going to redirect that to a file. So for now I'm going to put it to static busy box dot yaml Then I'm going to move this to a C Kubernetes manifest folder. And let's see if pod is created Okay, so the pod is now created static busy box control plane and it's in a running state. Okay, so let's go next. Now edit the image on the static pod to use the busy box 1 point 2 8 point 4 image So to make any changes to a static pod you could simply edit the file and save it. So let's just go to edit the file in EDC Kubernetes manifest add pod and then just change this right here busy boxy 1.284 and just save that And if we look at the pod now it's pending. Let's watch the status. Okay, there's a issue. Okay, got it wrong. Sorry about that. Change that now. So that's why you should always verify after you do anything, especially in the exam it's possible that we might make typos different places. So you always verify your work after you're done. Okay, so that's done. Let's wait for the next question. Okay, so we just created a new static pod named Static Green Box, find it and delete it. So let's see to get pods. And we see that there is indeed a static green box and we've, we have got to delete it. Now if we just try to delete the pod like this, okay it says it's deleted, but if you look at it it's gonna be recreated because we know that it's a pod that the cubelet manages so there's it's not possible to delete a static pod just using the delete command. It would delete it, but it would be created again, right? So we need to find the manifest file for that static pod and delete it. So if you look at, if you look at the the EDC Kubernetes manifest directory, we'll see that there's none for the green box. And that's basically because if you look at the name of, of the static pod you see that it's created on node zero one. So we've got to go, go to the node on node zero one and delete it from there. So let's find out how to go to node zero one. So let's look, got a node node command and we see that we have a indeed a control plane and a node zero one node. If we do a dashboard wide we'll see that it has an IP address. So the control plane is at 10, dot 38 dot 1 oh 2 dot 6 And node zero one is at 10 dot 38, 1 oh 2 dot 8 So let's the stage to node zero one. Okay? And, and sometimes if you just depending on how the DS is configured, if you just do SSS to just node zero one, that might work too. But if that isn't working, then you should always look up at the internal IP address following this command and sss do that node. So now we are on node zero one and all we need to do is find the the static file manifest path. So let's go to, let's see, EDC Kubernetes manifest directory And as you see it, it doesn't exist, right? So there is no static green box path here. So we'll need to see why that is. So let's see, let's go back and see the the cubelet configuration and find out what's wrong with the path to the static pod So the, the file is at slash war lib cubelet and then you have config dot yaml And if you look at this file you have the static pod path, and as you can see it's not at war, it's not at EDC Kubernetes manifest It's actually changed. It is at EDC just to mess with you. Right? So that's a change we made just to get you to think a little bit. So, yep. And there indeed, we have the green box dot yaml So all they need to do is just remove this file from here okay? And make sure it's removed. And let's exit and go back to the control plane and list, list the notes and let's watch and wait for it to be terminated. Okay? So the the pod is now being terminated and that's gone. Okay? So that's, that's that. And that's the end of this lab.
