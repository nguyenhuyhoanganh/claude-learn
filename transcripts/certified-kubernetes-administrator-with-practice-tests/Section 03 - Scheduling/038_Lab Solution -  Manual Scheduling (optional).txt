# Lab Solution -  Manual Scheduling (optional)

-: Okay in this video, we're gonna walk over the practice test on manual scheduling. So the question is a pod definition file engine x.yaml is given and you have to create a pod. So let's go over this, but before that let's just quickly explore the environment. So if you're on the get nodes command we see that it's a two node cluster. So that's what we're working with now. And there is a file called engine x.yaml. And by the looks of it it's just a normal pod definition file with the name set to engine X and the image set to engine X as well. So let's go ahead and create this. Okay. and let's click on check. And so the goal of this exercise is just to create the pod you don't have to check the status for now let's go to the next question. So what is the status of the created pod? So we see that it's in a pending state. So that's the answer, it's an a pending state. Okay so why is the pod in a pending state? So inspect the environment for various Kubernetes control plane components. So one thing that we have learned is that's the scheduler that schedules the pod. So if the pod's status is in pending, let's take a closer look at the pod. And we don't see any additional details other than the fact that it's in a pending state. So that basically means the scheduler has not done its job of scheduling the pod on a node. You see if you look at it here the node field is set to none. So let's look at the scheduler. So the scheduler runs as a pod in the cube system name space. And we see that you have the API server, the controller manager, the proxy, the ETCD control plane, but there is no scheduler running. So that could be the reason why this pod is not running and it's in a pending state. So we're gonna say that there's no scheduler present. Okay, so now we have to manually schedule the pod on node zero one. Now we've already created the pod, but it's not assigned to any node. And one thing that we've learned in the previous lecture is that we can manually schedule the pod. So we're gonna edit the engine X study on the file. And I'm going to add a node name property here and we'll set that to node zero one. Okay. and then we, now we've got to recreate the pod. So we have to delete it and recreate it. One way to do that is just to, you know, delete the pod and recreate it. But we can also just use the replace command if we need to do that just in a single command. So we're gonna do a replace force so that's going to delete the pod and then recreate it. And then we're gonna specify the file. So that first, yeah the pod is deleted and then it's just replaced. So let's look at the status of the pod now. You see that it's in a container creating state. Now if you don't want to run the same command multiple times to check the status you could just add the watch option. And what it's gonna do is it's going to continue to monitor that pod and it's gonna update the status if something changes. So it's already in the running state, so we're good. Okay, next. Okay, now schedule the same pod on the control plane node. Right, so again, that's pretty simple. If we go in and make the same change so this is just for additional practice control plane and then we are going to have to do the same thing. Now remember that we cannot move a running pod from one node to another, right? So ultimately a pod is actually a container which is basically another process running on a system. So you cannot move a running process as is from one system to another. So there is no way to basically move pods from systems to systems. You can only delete pods from one system or one node and then recreate it on another. So that was a question that I got from a student. So let's do the replace again and remember to use a force. Be sure you wanna delete and recreate it and wait for it to terminate. Now again, if you look at this, it's taking a while to delete. So that's not a problem with Kubernetes or with our lab environment. It's normal for that to happen. When you terminate or when you delete a pod what happens is in the back end it has to go in and terminate the process that's running. In this case, the is the engine X process. So it basically sends a kill signal to the process. And the process, depending on what process is running and the way that it handles kill signals it might take some time for it to process that request and actually kill, or you know gracefully shut down, right? So that's really the the reason why it takes a few seconds when you delete certain pods. And that's why, that's what happened this time. So it's not because there's an issue with either Kubernetes or the node or the lab environment. That's just how it works. Okay, so let's now check the pod, we can check. See it's running, if you do a wide option then we'll get to see on which node it is scheduled so we can see that it's on the control plane node. Okay. All right, and that's the end of this lab.
