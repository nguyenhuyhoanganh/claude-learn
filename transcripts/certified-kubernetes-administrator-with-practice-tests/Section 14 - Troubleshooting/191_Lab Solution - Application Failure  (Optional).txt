# Lab Solution - Application Failure : (Optional)

-: Hey, so let's go through this practice test on troubleshooting application failure. So the first question is, a simple two tier application is deployed in the alpha name space. It must display a green webpage on success. So click on the app tab at the top of your terminal to view your application. So I can click here to open the application, and I see that it is indeed in a failed state. So if I look at the error message here, we can see some of the environment variables, which, which we can use for while troubleshooting, but it says, Can't connect to MySQL server on MySQL service 3306. Name does not resolve. So basically it looks like this web application is unable to reach the MySQL service because the name, there's some, some issue with the name is not able to resolve it. So let's look at this architecture diagram. So here, so it's a two-tier application. So you have the web service here and you have the database service here, and then you have the user accessing it. So we know that this is most likely a deployment or a pod. The web application is on 8080, and then you have this web service which is on 8080. And then this is the node port that the users are accessing, which is 30081. And then you have the MySQL service, which is a, which is a service that the web application use tries to connect to. And this is the MySQL pod. Now looking at this state. So we know that the web application loads, it's, it's failed, the application is failed, but at least the interface loads. So looking at it, this area seems to be okay. Okay, because this port 30081, this is the port that we are on. So 3081, so that's good. So this patch looks okay. This web service has loaded the application. So this also looks okay. So there's some issue in this area. The application is unable to reach the MySQL service at this name or at this port. So let's first take a look at, at the application. So it's in the alpha name space. So we're gonna do a kubectl, we get pods, dash name, space alpha. And so we're gonna work, we're gonna run a lot of commands in the alpha name space. So it's best if we switch our context and, and set the alpha name space as the default name space, because we don't want to be typing in the name space for each command so let's do a kubectl config, and let's check the help find out the command. So we can use a set context. So we're going to do a set text, and then let's check the help. And what we want to do is we wanna set the name space, we're gonna use a set context, and then the current context. So we wanna set the name space of the current context to the name space equals alpha. Okay? So that's modified. So let's try, we've gotta just get pods now and we see that we can see the pods in the current name space. So as we suspected, the web application is a deployment. So let's do a deploy. So the web app is a deployment, and the SQL server is a, is a pod, and we also have services. And so the SQL server has a, the, MySQL service and the web application has the web service. Okay So one way to, another way to test the, if the web application is accessible or not, is basically at this level of test is to do a curl and then to gonna do a local host because it's accessible on the node port and the port is 3081. And we can see the kind of, you know, this in a, in a text format and we can see that database connection has failed and can't connect to MySQL server on MySQL service. So we're gonna look more into this area, right? So let's see, let's look at the deployment. We're gonna do a kubectl, we're gonna do a kubectl describe deployment web app about MySQL. So that's the deployment name. And here we see that we have the template, the image is simple web app. MySQL port is 8080. And these are some of the environment variables. So the database host is MySQL service, the user is root and the password is this password. So the host is MySQL service. So let's look at the service called MySQL service, Let's do a get service. And we see that the name of the service is not MySQL service It's instead MySQL, right? So that seems to be the problem here. So this, and this should match because from within this application is gonna try and reach a service by the name MySQL service. And this doesn't look like that. So that seems to be the problem. So what we're going to do is we're gonna fix that. So let's do, let's try and edit the service. And we can't edit the service name, but let's give it a shot. All we want to do is change this to MySQL service, right? So let's try and save that. Now, it's not going to allow you to save that, but the changes we made is in this temporary file. So this is what we need. So here you have the service and you have the, MySQL service and everything else seems to be as, as what we want. So the port numbers are there, selector is there, or another approach we could do is to just delete the service and export or expose the service, the MySQL service again. But for now, we could just go ahead with this. So we could do a kubectl delete as we see, and delete the MySQL service. Then we're going to create SVC. Nope, we're gonna create using this file. Okay, let's check service now. And we see that the MySQL service has now been created on port 23306. Okay? So let's go ahead and check this. Okay, when you check this application, it's now Success check our work. Okay, so that's successful, let's proceed to the next one. Okay, so in this one, the same two tier application is deployed in the beta name space and it must display a green web page on success. Click on the app. So let's click on this app to see the status. And as you can see, it's in a failed state. And if you look at this, you have the environment variables. So the database hosts MySQL service, user is root, password, and it says, Can't connect to MySQL server on MySQL service 3306. Now it says connection refused. So it's this connection refused. So let's take a look at it. So first we're going to switch context to beta. We're gonna do a set context config, set context command, and then we're going to, so the current context name space to beta okay? Now we're gonna do a get pods. Pods, and we see the MySQL pod and you have the, the web app pod. So that's fine. And then we also have services for the MySQL service and the web app service. If you look at this again, as before, the, the Port 30081 seems to be correct. So you can, as we can access that here and this whole application loads. So this whole thing should be good, right? And again, it looks like it's a connectivity issue with between the web application and the MySQL server. So this is what we have to verify. Now, last time the issue was with the name, but now looks like the name is correct, it is MySQL service, but we will anyway, also check the deployment on what is set, describe Deploy web app MySQL. And we see that the database host given here is MySQL service and this is MySQL service as well. So that's good. Now let's just look at the service. So this is good. Now let's just focus on this. So we're gonna do a describe service, MySQL service. And we see selector is right, the endpoints it has has detected the endpoint. So that's 10.42.0.12. Let's look at, let's make sure that this endpoint is the pod, which is the MySQL pod. So let's do a kubectl get pods -OY to see the IP of the pod. And we see that the MySQL pod has an IP address, 10.42.0.12. And this is 10.42.0.12. So that's right. But if you look at this right here, it says port 8080. And so the IP is, okay, so then we check the port. So it's port 8080, and 8080 is not the port for the database. So if you look at it here, the port here is 3306 and not 8080. So that seems to be the problem. So let's do an edit as we see MySQL service. And you can see the port here is 3306, which is right, the target port given is 8080, and that is not right. So that should be 3306 as well. So let's edit that. Let's make sure the changes took place. Okay, so now is 3306, and let's check the endpoints. And we see that the endpoint is now 10.42.0.12, 3306. Let's now check our application and yep, can see that it's, it's a success. Let's check our work. Okay, so we're two down. Okay, so the next question is the same two tier application is deployed in the Gamma name space. It must display green webpage and click on the app tab at the top to view, okay, so it, it looks like it's the same question. So let's go and open the application. Okay? So just keeps spinning and meanwhile we'll go here and here our work and switch context to, we'll switch to the gamma namespace, okay? And let's check status of pods and let's check services. So we have both the pods are running and the services are there, but this doesn't look like it's loading. So let's try and find out why. So it looks like the issue is somewhere here. The page itself isn't loading, the service itself isn't loading. So let's start looking at it right from here. So the first thing to look at is this Port 30081, because that is the, the port that we are trying to access 30081, okay? And it's now timed out. So 30081. So if you look at the service called web service, we see that it's set to node port, and the node port is 30081, so that's fine. So that looks good. Next thing is the service itself. So let's check if the service has a endpoints. So we're gonna do describe service, web service, and we see the name, web service, name space is gamma, has the right selectors and has the endpoints. 10.42.0.14. And the ports are 8080 and 8080. So that looks right. Let's make sure the IP address, the endpoints are picked up correctly. So this endpoint is 10.42.0.14, and the web app is on 10.42.0.14 as well. So that checks out. So the next step is, so the web service is good, Let's check the deployment itself. So we do our kubectl get pods, we do describe deployment web app, MySQL, and we see that it has one desired and one available. So that's fine. The image is, looks correct, the database host is MySQL service, user is root password. All of that seems to be okay, but it's still, we're unable to do the deployment. So let's check out the logs because it looks like the, the web server isn't responding. So let's do a logs on the pod. And this seems to be, okay, so this, this application itself, it says it started, it's available on this port. And so that seems to be okay. So however, we are still not able to access the application. So let's anyway, go through and complete our checks because we assume that it's going to be somewhere here. But anyway, let's go down and complete our checks. So the MySQL service is supposed to be on 3306. So let's check that. So we have the MySQL service and it's on 3306. Let's check the endpoint, describe MySQL service. And here you have the endpoints, but it looks like there are no endpoints. So apparently the service hasn't detected MySQL pod as an endpoint, so let's take a look at why that is. So if you look at the selectors right here, it says name equals SQL 00001, and for the pod, so describe POD MySQL. And we know that the labels for this is, is name equals MySQL. So that should be the selector not name equals SQL 0001. And that's why it doesn't have any endpoints. So let's go ahead and edit that, not the pod. We're going to edit the service, MySQL service, and we're going to change the selector to MySQL then save that. Okay, now we're going to do a describe pod, no describe Service MySQL. And we see that we have, we have the service now protected. Let's go and check out our application. Okay, so that's connected successfully. So apparently, even though we were not ab able to access the web application, that's because the web application is trying to access the MySQL service and that service was not responding, which is why the web application was not responding either. So even though we thought that the issue was somewhere here, we went through and we made sure that everything's fine and we just went through the remaining just to check and just to be sure. And that's how we identified that the issue is somewhere here, so that's, that's another approach or technique that you should use. If you're not really sure, I mean, you start with an intuition on, you know, where the issue could be, but if you don't seem, you know, if you can't really figure out, you just keep looking at the, those things just to make sure that they're working as expected. Okay, so let's check our work and let's wait for the next one. Okay, so here the, the same two-tier application is deployed in the Delta name space, and it looks like it's the same setup. So here we have, and it's in a fail state. So let's first change our context to Delta. Okay? Then let's check the pods and let's check our services. So let's check the error message. So the host is MySQL service user is SQL user. Password is password. It says can't connect to MySQL server on MySQL service. 3306 connection refused. Okay, I wanna try that out again, just refresh it. And it says Access denied for user SQL user at 10.42.0.16 using password. So it says Access denied for the user. So, and here you can see the user details. So the DB host has to be my service, the DB user is route. And the password is password. So let's check the, the settings for that. So the web application must access that. MySQL service, and this is where the credentials are stored. So let's do a describe on MySQL, describe deployment on MySQL. Let's look at the settings. So here you have the environment variables and you have the host, which is MySQL service. The user is, it says SQL user, but it's actually root. And the password is password. So this is, this seems to be the problem. So let's edit that and update the user to root. Okay, and let's wait for the bot to be running. So it's running already. It refers to the page. And that's success. So that was the issue here. Let's check that out. And while the new one loads, let's close this one, let's clear our screen. Okay, the next one is the same application is deployed in the Epsilon name set in space. It must do display a green web app webpage on success. So let's open the webpage and we have a similar error message. So it says the environment variables, DB host, MySQL service database. It's not set users, SQL user. And the password access denied for user SQL User, okay? So that's kind of the similar error. So let's take a look at it. But first let's change the context to, Epsilon, okay, and set the parts. And we know that, So it's a similar error message where it says Access denied for SQL user, and that's what we just solve. The user is not SQL user, it's a root user, so let's go ahead and fix that. Edit deployment, web app, SQL. And so here we see the user is SQL user. So let's change that to root. Okay, let's check the bot, if it's running. Let's refresh the page. Okay, so now the error message has changed and it says access denied for user root. Again, so the access is still denied. But the user has now been updated to root. Right? So. The user credentials are either set on the application that's trying to connect to the database or the database itself. Right? So we've already fixed it on the deployment here, but now the issue could be here. So let's check out the pod. Because that's the only other place where anything could be set. And here we see additional details. And here we have the environment variable and the MySQL root password is set to something other than password, which should be this. So that needs to be changed. So let's do a edit pod for MySQL and let's change this password to. BASWRD save that, it's not going to allow us 'cause it's a pod. So we're going to do a kubectl, replace, replace force and give the file. Okay, let's delete it. It's up, it's running. Let's try to refresh the page. Hmm. So now it'd says connection refused. No, okay. That was because we tried too early, the pod was just being initiated. So now at success. So that was the issue. Now another thing to note here is in this case, I mean, you know, these are, it's very simple, straightforward examples of setting environment variables, but these could also be part of config maps. So there could be another config map associated with the MySQL service, which is how it is usually configured. And so if that is the case, then you should inspect the config maps as well. Okay? All right, let's get ready for the final one. Okay, so the same two tier application is deployed in the Zeta name space. So let's first set the context to Zeta and let's check out the application. Okay, so it says bad gateway right up front. So let's start from here. So we're assuming, because it says we're not even able to access the web service, we're gonna start right from the top. So the, the node port is 30081 because that's what we are trying to access here. And let's see the status of the services. And we see that the web service is, has a node board, but here you see it's not on 30081, it's on 30088. So that's what we're gonna fix, edit service, web service. And the node board has to be 30081. We'll save that and let's make sure that it's updated. So it's 30081. Okay? So that's done. We're now able to access the application at least now it says access denied for user SQL, user using password. So that something we have seen already. So let's check out, okay, let's look at the deployment, describe deploy web app MySQL. And we see that there are environment variables. So MySQL service, SQL server, SQL user and password. So let's edit that to change it to root Okay, so the new pod has been deployed. Okay? So now the username is updated, so access, but it's still his access denied for user root. So we have to go down and check the, the password configured on the MySQL service. So let's do a describe pod MySQL. You see that we have the password issue here, the environment variables. So let's edit that. And this should be password, let's save that. That's not going to go through. So let's do a replace this. Okay, let's wait for the pod to run. Okay, so yep. Now that's successful. Check our work. Okay, so that's about it. I know that there's a little bit of repetition on the type of questions and, and that's really in case you didn't get it the first time for you to just get that practice again and, and get it the next time and build some confidence. So, yep, I hope that was a good troubleshooting session and we'll have more coming up in the upcoming labs.
