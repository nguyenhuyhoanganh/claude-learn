# Lab Solution -  Commands and Arguments (Optional)

-: Okay, in this video we're gonna go over the lab on commands and arguments. So let's start. So the first question is, how many pods exist on the system? So let's check the number of pods, and there's just one pod. The second question is what is the command used to run the pod? Now ubuntu-sleeper, though we're going to look at the pod in detail, and we'll see under the containers we have the ubuntu container, and here we have the command, which is sleep 4800. And I do not see any arguments so that's the command. So it's sleep 4800, that's the command that's run by this pod. So there's one right here. Okay, so the next one is to create a pod with the ubuntu image, to run a container to sleep for 5000 seconds, modify the file ubuntu-sleeper-2. So there's already a file given called ubuntu-sleeper 2. It says simple part definition file, you have containers, the name and image, and we want to set it to run sleep for 5000 seconds. So let's modify this file, and set a command, and it has to be to sleep for 5000 seconds, right? Sleep 5000. Now remember that when you specify command, the command is always an array, right. So it has to be in an array format like this. So that's the first thing. So this is, that's one way of specifying an array. And the other way is to just have it like this, this form of an array. So sleep, and then 5000. So something like this, right? So these are two forms. You may follow any of them. And this one right here, this is still not in the correct form because if you look at this, this is an array of a single item. So this array only has one item, and that's called sleep 5000. It's a single kind of item in the area. Whereas here, it's two separate. Now, one of the things to remember is when you're specifying a command, a command always have to have the command as the first item. And that's basically, if you look at this, right, sleep is the command, and 5000 is the argument. So this has to be, sleep has to be a separate string, a separate item in the array like this. And the argument, you could specify the argument either in the same command as another item, or you could provide it as as an argument. So that's, those are two other options. So let's get rid of this. Let's go with this. So this is one approach. The other is to specify arguments as args, and provide the argument here and get rid of it here. So this is, this is more, readable and straightforward. So this way we know that the command is sleep and the argument is 5000. But even if you give the argument as a second array it's ultimately going to combine them both for running so that works too. So that's... That's that. So let's go ahead and create that file. Let's make sure pod enter sleep 2. Okay, and here we see that is in a running state and you have the command and argument as 5000. So let's give it a shot. Okay, so that works. As I said, it could also be just the command as two elements in the area with sleep 5000 like this. Okay, let's proceed. The next task is to create a pod using the file named ubuntu-sleeper-3.yaml. There's another file. Okay, and there's something wrong with it, so try and fix it. So taking a quick look, I don't see anything wrong with it. So you have the image, you have the name, you have the command and the argument which is, also given as a command, which is, which is actually okay. So let's try and create this this pod. And we see that it's a come back as a bad request. And it says, "Cannot unmarshal number into Go struct field containers, spec containers command of type string." So all the elements in the command must be a string, you cannot have a number. So this exercise was just to kind of make sure you know that you don't accidentally put in a number instead of a string. So let's create that. Okay, that should be good. Okay, So the next question is to update the pod ubuntu-sleeper-3 to sleep for 2000 seconds. So we have, let's try, you could pedal edit pod, and it's ubuntu-sleeper-3. And let's see what happens if we just try to edit it, right? So we're going to just change the command to 2000 seconds. Let's try and save that. If you see this, it did not go through, and it says that this is forbidden so pod updates may not change the fields other than these, so the image and active deadline. Again, so there are some, some properties that you can change, and there are some that you can't change. So this here, right here is a property that you cannot just directly edit. So what we'll have to do is go out of it, it's already created a file here with our edits, so we can look at that. It has a lot of other information which can be ignored, but you can see that it has the edit. So we could just do a kubectl replace, place, and force, and provide that file name. So this is one approach that you can use. And what it's gonna do is it's going to delete the pod then recreate the new pod based on the edited one. So this is one of the approach you can use in the exam, if you have to edit something that's not actually editable directly, or you could edit the pod itself, the pod definition that you have locally and update that. And that's also, that's the best practice. It's taking a while for the port to be terminated. And as is mentioned before and that's completely okay, that's unexpected behavior. So when we try to delete a pod, a kill signal is sent to the process that's running in the container inside the pod. And it depends on the process and how much time it takes to handle the kill signal and fully terminate the process. So that's, that delay's expected. Okay, so it's now updated. Let's check the work. Okay, so the next question is to inspect a Dockerfile under root by the color Dockerfile and what command is run at the container startup. So if you were to create an image from this Dockerfile and run it, what would be the command that's run at startup? So we know that entry point is is where is the command that's gonna be run, so it's gonna be python app.py, that's the command that would be run as startup. So that's pretty straightforward. Now let's look at Dockerfile 2. And now you have entry point and the command argument. Now we know that this in Docker, you have both of these. And in this case what command is run at the continuous startup. So you have python app.py, we know that that's the entry point, and these are gonna be going in as arguments for the entry point. So it's gonna be python app.py followed by color red. So python app.py and followed by color red. Okay, the next one is to inspect the two files under directory webapp-color-2. So there's a directory called webapp-color-2 and there are two files. First one is the Dockerfile, and this is a a similar file which has an entry point and, and the CMD parameter. And then we also have... a web. We also have a Kubernetes manifest file for pod. And which uses this Dockerfile, the image created from this Dockerfile, so that's what it says here. Assume the image was created from the Dockerfile. So first we create an image from this and then we use that image here. And when we do that here we are overriding the command with color green. So what would be the end result? So as we learned from the lecture the command always overrides the entry point. And if we had arguments then that would override this command here. So in this case, the command overrides the entry point, so that's gonna be color green. That's, the command I would start with. So it's not going to be any of this, it's actually going to be this. It's not even going to run the app.py. Okay, and the next question is to explore the files under another directory. So let's go to webapp-color-3. There are two files there. So we have webapp-color-3 followed by Dockerfile-2 so that's a similar Dockerfile. And then there's, there's a bug definition file as well. So again, it's the same kind of question. You have an entry point and the command here. Then you have command and arguments here. So as we know, this is, both of these are gonna override both of these, so is the ultimate command that's gonna be run is python app.py followed by the color pink. So python app.py followed by the color pink. Okay, so the final question is to create a part with the given specifications. These, by default, it displays a blue background and set the given command line arguments to change it to green. Okay, so when we run this command, so let's first start with the command to create a pod. So we're gonna do a kubectl run and then followed by the pod name, which is webapp-green and then followed by the image, which is the code cloud webapp color image. And then you have the command line arguments that we need to pass, which are color green. Now, what it says is by default, it runs, it actually runs, it displays a blue background, but we want, that's the behavior of this particular image. We want it to, to run in the green color, and that's, that can be done by passing in this additional argument. So all we need to do is kind of create this and then pass in an additional argument. And one way to do that is we can create a manifest file. So we could do a dry-run=client and then output this to AMO format. Sorry, clicked that wrong. Okay, and then we can go in and add the args section here. So that's one way to do it, but something like this, we should be able to do it in a much easier, more straightforward way. So let me explain how that is. So for example, in general if I had this application locally I would run python app.py or if there's an executable cob:web app I would actually run web app, something like this. And then if changing the color is an option then it would be as easy as just specifying the color to be green, right? So the same way running this application on Kubernetes has to be as easy as that. So basically all you should be able to do is keep kubectl run and then whatever is the name of whatever that it is, the name of that application, so web app followed by, you know, the image name and stuff. And also it should be as easy to customize it by just passing in the color and color like this. So once this, once this application is containerized and a Docker image is built out of it it should be as easy as something like this, right? So there's, there is an option for that. If you look at the help of the kubectl run command you'll see that out of all of these options there's a section here where you can pass in additional arguments, right? So here it says I run the Nginx port, but use custom arguments. So here you have the name, the image but then you have this double dash and then you have all the arguments, right? So there are arguments, if you look at this command there are arguments that are specific for, for the kubectl utility. So you have image which is specifically for the kubectl utility and there may be many other arguments such as all of these dry run labels. All of these are for the kubectl utility, but then the application that runs inside Nginx or the simple web app that we are running that also has options that are very similar to this. So in order to differentiate between the options for kubectl and the options for the application that's running within the container and to specify those, all of those on the same command line you have this double dash option. So this basically separates the options for the kubectl utility and the options that are going to go through to the Nginx application running inside, right? So anything that's before this are specifically options for the kubectl utility. And anything that goes in after this which may also look very much like these, they are going to be passed through to the Nginx application that's running inside, right? So that's, that's the reason why you have this double dash. So if you just use the double dash and then pass in anything, all of these are gonna be options for the application running inside. And then if you want to override the command itself, not just the arguments, but the command itself then you could use the dash dash command option. And again, remember that's before, but if you do that then you can also provide a command and a set of arguments after it, right? So that's why you have this double dash. So let's, let's try and use that approach for this, right? So let's do a kubectl run followed by webapp-green and the image is, this, okay? And now we want to pass in the arguments and the arguments are just color green. So we're going to put the double dash in. And now anything I specify now is gonna be an option that won't be considered by kubectl but it'll be considered by this, this process running inside. So I'm gonna say color and I'm gonna say green, right. So these are gonna be passed through. And if I want to, let's say, so this is gonna be basically generate the command called python then app.py followed by color and green. But if I want to update python 5000 itself, then I could pass in this command option and then pass in the command here, like python, let's say app 2.py or something like that. In this case we don't, we're not going to do that. So I'm going to remove that and just pass this through. Okay, so let's create it, let's check it out. If you look at this, you have args color green and the command is already built into the web app as, as the entry point, right? So we're not going to touch that. Here we, we have just passed through the, the green color. Okay, let's check our work. Alright, so that's, that's about it. I hope that was informational and I'll see you in the next one.
