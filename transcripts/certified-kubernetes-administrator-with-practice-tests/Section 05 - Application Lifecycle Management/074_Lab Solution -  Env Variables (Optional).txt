# Lab Solution -  Env Variables (Optional)

-: Okay, let's go over the lab for environment variables. So the first question is how many pods exist on the system? You see that there's just one pod. Now, what is the environment variable name set on the container in the pod? So let's take a closer look at the pod. Okay? And here we see the environment variable is defined here, and it's app color is set to pink. So it's app underscore color. Now what is the value set on the environment variable app color. It is pink. Now view the web application UI by clicking on the web app color pink here. So clicking on this opens up the web application and it is indeed pink in color. Now, update the environment variable on the pod to display a green background and delete and recreate the pod required. So we are going to do a kubectl edit pod and web app color pod. Now we know that it may not allow us of course it's not gonna allow us to edit this on the pod but we don't really care because we just need the edited or updated manifest file that we can then use to replace the pod. So I'm just going to set it to green here and try and save but it's going to come back saying it's forbidden which is fine. Just going to exit out of that. But here we have the file which has our edits. If you look at it, you have the value green here. So let's use that to replace with force to kill the existing pod and let's run that and wait for that pod to be deleted and the new pod to be created. Okay. Let's check the work. That's done. Let's click on the link here and we see that it is green. Let's proceed. Okay, so the next question is about config maps. So how many config maps exist in the default name space? So I'm gonna do a kubectl get config map and we see that there are two. Okay, the next question is to identify the database host from the config map TB config. So we're gonna do a kubectl describe and use the short form for config map, which is cm. And then DB config is the name of the config map. So here you can see information that's in the config map. So you have the DB host, the DB name, DB port and what we are asked here is to identify the DB host which happens to be SQL zero one dot example dot com. So let's select that. Now, create a new config map for the web app color pod and using the spec below. So we now have to create a new config map. So we're gonna do a kubectl create config map. Let's take a look at the help. And here is the syntax. I'm just showing these help commands. So you could use, you could follow the same approach in your exams to get the commands easily. So here we're given the data which is app color equals dark blue. This is what needs to go within the config map. And so we don't want the the from file option. What we want is the from literal option. So something like this, we can use that command and it is going to replace the name with web app config map. And this is going to be color equals dark blue. Let's verify. Okay, so the config map is created and you have the data app app color and it's set to dark blue. Let's proceed. Now, update the environment variable on the pod to use the newly created config map. So now we've got to go and update the pod. So we have to do kubectl edit pod web app color and here we have the environment variable. So currently the environment variable is set directly as name and value here. But what we want to do is we want to get rid of this and we want to we want to get the environment variables from config map. So we have to use the ENV from syntax. So if you're not sure how that works We go to Kubernetes documentation and look at config map and look at examples. So here you have configure a pod to use a config map. So this is basically what we want. So here you have a config map creation steps. What we want to do is find out how to use config maps as environment variables using config map data something like this. So you have, so you could either do this format where you have specify environment variable, each one and then you use a value from and then specify the config map. Or we could fit in an entire config map itself using the ENV from. So that's what we're going to choose here. So something like this. So we need ENV from, and then config map ref. So we're gonna copy that. And here we're going to get rid of this. Place that here. And this should be web app config app. Okay, let's go ahead and try and save that. It's not gonna work. So we're just going to exit from that and we'll use this file to recreate. We're gonna force replace and then pass in the file. Let's wait for the part to be deleted and recreated. Okay, so that's done. Let's verify that it's done correctly. Okay, so here we see the containers, the web app color and we have the environment variables from the config map configured here. So let's go ahead and check our work. That's correct. And let's verify that it is dark blue. Yep. So the web app has now changed its color to dark blue. Okay, So that's the end of this lab.
