# Demo: Encrypting Secret Data at Rest

Instructor: Okay, so in this video, we're going to look into encrypting secret data at rest. So if you go to the Kubernetes documentation pages and under Tasks, you have Administer a Cluster and Encrypting Secret Data at Rest, you have these following documents. I'm gonna follow this, but also kind of try and explain what we are trying to do. So the first thing, first things first, I'm going to start up a Kubernetes playground. So this is a single node cluster. All right, so this basically uses, it is built with the kubeadm tool and has containerd. I'm just gonna maximize this and we'll just work on the terminal. All right, so first things first, I'm gonna first create a secret object. So I'm going to do a kubectl create secret, and I want a generic secret, so I'll do that, and I'll probably create a secret from literal, so I'm just going to copy this command. Okay, so I'm gonna call it key1 and supersecret. So that's my secret object. So my secret object is created. So if I now do a get secret, I get to see my secret object. And if I do a describe secret, my-secret, I get to see the secret name and the data. If I wanna see a little bit more, I do a dash o yaml and I'm gonna do a get secret. Okay and I get to see the data here. Now if you look here, so this is key1, and this is kind of the encoded format of the secret. So remember that, you know, anyone could take this and decode it and easily view the actual secret, right? So if I do, let me see if I have base64, yup, I do. So gonna get that again. So I'm gonna do a echo, copy and paste this. Let's pass it through base64 and do a decode. And I get to the, the secret. Okay, so that's the first thing to know that the secrets object stored in the secret configuration as you see it here is just Base64 encoded. So if you just do a base64 decode, you're gonna see this. So remember not to create your secret definition files and push it to GitHub or something because anyone could just pick this up and run this command and see the secret, right? So that's the first step, but I wanna make it clear that the scope of this video is not really relevant to this encoding here. We are going to focus on the data that's stored within the etc server. Okay, so after we are done, this part is still gonna remain the same. So we're not really focusing on this part right here. What we're focusing on is how the data is stored in the etcd server, so that's the focus. So let's first look at how this data is stored in the etcd server. Okay, so for that, if you go to the bottom of this, there is this command. It uses the ETCDCTL_API version three. It runs the etcdctl command, passing the CA certificates for authentication, and we're gonna get this particular secret. So the way that it's stored in etcd is it's stored in registry/secrets/default and this is the secret name. So let me first see if we have the etcdctl command line. So we don't have that, so the first step is to get that installed. So I'm just gonna go apt-get install. I think it's it etcd-client. Yup, so if you do not have the etcd, so remember that the etcd server is running in a pod. So you can either ssh into the pod and then run the etcdctl command from within that. Or you wanna run it locally from your control plane node, you could use the etcdctl client utility. Now, if the client command line utility is not available, you have different options to install it on different systems. So let me see. Okay, so I have it installed on my machine using the package etcd-client. So look up for whatever it is the version of etcd that you're running. Okay, so that's there, so now we've got etcdctl utility. So remember that's client only, the server is still running on as a pod. So if you do kubectl get pods dash kube-system, I have the etcd control plane here, right? Okay, so the next step is to run this. So first, remember that you need the certificate files. So let's see if we have that. Yup, we do. So under etcd, you have all the certificates files needed for connecting or authenticating to the etcd server. So that's good. So I'm gonna copy this command. Remember to set the version, ETCDCTL_API version to three, and the name of the secret is secret1. See what I created, get secret, so it's named my-secret, so I'm gonna replace that with my-secret. I'm gonna do a my-secret. If I just run this command, you can see that it kind of gives me kind of a jumbled information, but you can kind of see the secret there as is, right? You can see the secret there in the text format. To see it in this format, you just need to append the hexdump to it because sometimes you may not be able to see it in the right format. So this is the data that's stored in etcd. And if you look closely, you'll see that the secret that I have, the password or whatever it is that I've stored, is actually visible like this. So the data is stored in etcd in an unencrypted format. So anyone with access to etcd will be able to just go through and get all the secrets and other confidential information stored as a secret object. So that's the problem here. So this is the problem that we are trying to solve by enabling encryption at rest in etcd. Now, if you go through this document, first thing that it says is, first we need to determine if encryption at rest is already enabled or not. So this is done with a property called encryption-provider-config. So if I go to my kube-api server, so let's say my kube-api server is running as a process here, and let me just grep for kube-api server, and I get to see the Kube API server process running with all the options. Let's first see if it has the encryption-provider-config option. So that's the first thing that we'll do. This is to check if encryption at rest is already enabled. And as you can see, it does not return a result. So that means this option is not configured. We can also kind of verify this because this is a kubeadm setup, it uses the var /manifest etc/ kubernetes/manifests, yeah, etc/kubernetes/manifests, and here you have the kube-apiserver. So configuration, so if you look at this particular file, you can do a cat, we'll be able to see all the options provided. And here, you don't see the encryption option that we've talked about here. So this means that encryption at rest is not enabled. So that's the first step, and we've already verified that by creating the secrets object. Okay, so the next step is to create a configuration file and then pass it in as this particular option. So that's it, so that's basically the steps required to enable encryption, right? You create a configuration file and you pass it in as an option. So let's look at the configuration file. So this is what it is, so you have the API version, you have the kind, it's encryption configuration, and then you have resources. Now, you can pick and choose which resources you want to encrypt, right? So you have pods and deployments and secrets and services. Do you want to store all of them as encrypted? You might not because not everything is confidential, right? So you need not necessarily encrypt and save all the data about pods and deployments. Here our concern is just secrets. So under Resources, you specify the targets. So this is secrets, that means only the secret objects are going to be encrypted. Now, you can encrypt something using a set of providers, right? So the default one is called identity. The identity provider just means that there's no encryption at all. So you can see the list of providers here, and it says the identity provider says there's no encryption. Resources are written as-is without any encryption. And then you have other providers which are these, and those are listed here. So you have the secretbox, you have the aes, gcm, the aescbc, so all of these are encryption algorithms, different encryption algorithms, and you can see the details about how they encrypt here. So you can choose whichever one you want and provide a key. So this key has a secret. You can provide multiple keys, keys and secrets, and this secret is what will be used for the encryption by the encryption algorithm. Now, one thing to note here is the order. As you can see, providers is a list. So this is the first item in the list, second item in the list, third item, and fourth item in the list. So this order matters because when the encryption happens, at first it uses this to encrypt and then it could use any of these to decrypt, right? So always encryption happens with this. So if identity provider, which has no encryption is the first one, then there's no encryption enabled at all. So that's how it works. So if this is the first one, that means this is what is going to be used for encryption. And since it's identity, it's not going to encrypt anything at all. So if you really want to encrypt the data in etcd, then one of these should be at the top. Okay, whatever is the first one is what's going to be used for encryption. So let's just do this real quick. So we're gonna create a very simple version of this file, which is this, and this, as you can see, we have specified that all the secrets are gonna be encrypted and we're gonna use the aescbc encryption provider. And as you can see, identity is at the bottom. So the first one is aescbc, so this is what is going to be used for encryption. Now, this requires a secret object. So we could generate a 32-byte random key using this. I'm just gonna copy this, I'm gonna get that key first. So that's what our key is going to be. Then I'm gonna copy this and create encryption object file. So I'll call it enc.yaml and I'm gonna paste this, okay, should have copied the secret value first. I'm gonna copy this and I'm gonna go here and replace the secret object here. Okay. Okay, so that's that. So now I have the encryption configuration file. It's currently in my home directory. Now all I need to do is go and edit this file. So that's the next step shown here. So in the kube-apiserver, I'm gonna add this line to point to the encryption file that I just created. Then, of course, we need to, since the file is created locally, I have to mount it inside, so I'm gonna use volumes and volume mount. If you have not gone through the volumes and volume mounts yet, then you might want to take a look at that and come back. But this is pretty straightforward. So here you have the directory, the local directory, and that's on my control panel node here. And this directory is gonna be mapped to this path within the pod. So anything that's available here is gonna be available here. That's it, right? So first, I'm gonna create this local directory so that I can put my file there. Okay, I'm gonna move my encryption file from here to that local directory. Make sure. Yup, so I have the file there. So now I'm gonna edit the kube-apiserver manifest file and then make these changes. Okay, so that's the next step. So I'm gonna go into etc/kubernetes/manifests /kube-apiserver. And the first thing that I'm gonna do is I'm going to add this line. So this is what tells where the encryption configuration file is. So that's the first step. And then I'm gonna go down. So I have two things. I have volume mount. So this is inside my volume, inside the pod where it is gonna be mounted. So I'm gonna copy this. So the directory, the local directory is gonna be mounted into this path. So that's the first step. And then below under mount, I'm going to specify the location of my local directory. Okay, so this is how it works on my local host, the etc/kubernetes/enc directory is going to be mapped to the etc/kubernetes/enc directory inside the pod. And then, so since my enc.yaml file is available locally, that file is gonna be available inside. And that's it. Okay, let me save that. So now that I've saved and made changes to this file, the kube-apiserver should be restarting. So let's just give it a few seconds. Let's see. Okay, so this is now, it's now gone, so I'm gonna wait for it to come back. Waiting. And if you wanna see the status of the kube-apiserver, since we are using containerd, so you could use the crictl command. So you could do the crictl pods to see. It's come up, okay, so it looks like it's ready three seconds ago. Just gonna do, yup, so it's back. Okay, so now let's do a ps aux and grep for kube-apiserver. And let's see if it has, just to confirm if it has the encryption. Yeah, so it looks like it has the encryption provider configured. Okay, so that's good. Now what we're going to do is we're going to create another secrets file. So we're going to create a secret object. So we're gonna do a kubectl create secret generic. Okay, and we are going to, let's pick this, and this time we'll just create the second one, which is key2. So you have key2 and it's called, the value is topsecret. Create that. My secret already exists. I'm gonna say my-secret-2 maybe. Yup. Okay, so I now have two. This is the first one I created. This is the second one I just created after encryption was enabled. So I'm gonna go and run the same command as I did previously to check the status of that within etcd So run this and the name is my-secret-2. Okay, and as you can see, I can no longer see the topsecret. So the value was topsecret and no longer see that here. That means encryption is enabled. Okay, and if I do the same for the old one, I still see the supersecret. Now this is because after encryption is enabled, only things that you create newly will be encrypted. Everything that existed previously won't be re-encrypted. But if you update an existing configuration, then that will be re-encrypted. So one other thing that you have here is to ensure all secrets are encrypted and what this is doing is basically you're getting the secrets and then just replacing them with the same JSON file. So essentially, you're just updating the objects with the same data. So I'm just gonna do that. So that's done. And if I run the same command again, I see that I can no longer see the supersecret. All right, so that's basically encrypting secret data at rest using one of these encryption algorithms. Right, thank you very much and I'll see you in the next one.
