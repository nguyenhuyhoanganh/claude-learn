# Lab Solution - Init Containers (Optional)

-: Okay, so in this video we're going to go through the lab on Init containers. So first let's identify the pod that has an Init container configured. So let's look at the pods that are available. And we see that there are three pods, red, green, and blue. And we need to identify the one that has an Init container available. So from the looks of it from here, it looks like this is the green is the one that has two containers. But let's look at all of the pods in more detail. So let's do a describe, You cut describe pod. And if you don't specify any pod name, it's going to list the description of all the pods. So if you look at red to begin with, it has containers it has red container. And if you look at green, it has two containers. So this is a green container 1 and a green container 2. But none of these are Init containers, right? So in Init containers will be in a separate section called Init containers. But these, these are not Init containers. And if you look at blue here we have an Init container section and we have a container section. So it is blue that has an Init container configured. So if you look at the output of the get pause command remember that it does not show up the detail of Init container here. This does not account for the Init container it only accounts for the actual containers for that pod. So the answer to this is blue. Now what is the image used by the Init container on the blue pod, is still describe and let's see, the Init container it has a container called Init service and the image is busy box. So busy box is the answer. Now what is the state of the Init container on pod blue? So if you look at the state, it says terminated, so terminated is the state. And why is the Init container terminated? And what is the reason? So if you look at the line below, it says the reason is completed. So if you look at this, the way that the Init container is set up, it has an image called busy box, it has a command and it's just a command that runs asleep in a shelf, so it just runs sleep for five seconds. So whenever this pod is created, when this pod was created, this Init container kicks in it just sleeps for five seconds and it just terminates that container and then the actual containers are spun up. So it's a simple setup for this, and that's the reason since this sleep command completed successfully the exit code is zero and that's why the reason is completed. So the process do not crash, the process cannot start, now the process completed successfully and that's why it has exit code zero here. Okay, now we just created a new app named Purple so, let's look at this and how many Init container does it have? So let's look at this odd in a bit more detail. And here you see that you have the Init container section. You have one container here, and you have another one here, so there are two, so that's one, one and one of two and so that's two, two is the answer to the question. Now, what is the state of the pod? So if you look at the state of the pod here, under get pod under the output of get pod, it's Init02. And over here under status you can see that it's in a pending state so the, the state is pending. Now how long after the creation of the pod will the application come up and be available to users? So let's take a look at how this pod is set up. So this pod has a purple container, which is the app right here but it has two Init containers. So you have the warmup 1 and warmup 2 Init containers. And the first one has a command set to sleep for 600 seconds. And the second Init container has a command set to sleep for 1200 seconds so this is gonna run first and then this is gonna run after. So you can have multiple Init containers change like this if required. So this is going to be 10 minutes and this is 20 minutes. So in total, before this actually comes up the Init containers are going to just sleep for 30 minutes. So only after 30 minutes will this come up. So yeah, it's a silly use case but it's just a very simple example to help you understand how the Init containers work. So that's 30 minutes. Now this could be, you know, anything in real life, and this could be things that, you know, prepare a database or prepare some startup scripts, stuff like that. And that is, that has to be run before the actual application comes up. Okay so, update the next question is to update the pod red to use an Init container. So there is a pod red, it has one container and it's in the running state and we need to update it to use an image container that uses a busy box image and sleeps for 20 seconds. So let's do an edit pod red and we have the in container section here, so we're gonna add a section called Init containers and then here we have the image, so the image is busy box, and then we have the names so this could be any name, let just say busy box and as a command, so the command is to sleep for 20 seconds. Okay so that's it, let's save that. And of course it didn't allow us to save so we're just gonna quit, we're gonna do a replace force, and this and let's just wait for the pod to terminate and recreate. Okay, so that's done. Let's check, let's go next. Now a new application orange is deployed, there is something wrong with it, identify and fix the issue and once fixed, wait for the application to run before checking the solution. So let's check the application called Pod. And it has one container and it's in a Init CrashLoopBackOff. So let's take a closer look at why it's failing. So if you look at this pod, we see that it has Init containers, there's one Init container called Init my service, and there's a container called Orange container and it's just a simple app. And here, if you look at this, it says back off, restarting failed container. So one of the containers seems to be, you know failing and restarting. And if you look at it here, it says Init crashed so back off indicates that it is Init container that's failing and that's what is being restarted. So one of the thing that we could look at when that happens is the logs. So you could look at the logs of a pod using the kubectl locks command and we're gonna specify orange as the pod, but we're not gonna have but just running logs is just always going to fetch logs from the orange container. But the orange container in the orange pod is just waiting to be restarted, waiting to start because it's actually in an initializing state. So let's find out what's happening with the let's look at the logs of the container, which is the Init container. So for that you specify that C and then give the container name, which is Init my service, and here you see that it says sleep not found. So the, it seems to be a typo in the sleep command. So if you look at how that Init container is configured you see that there's a command section and you have the sleep command, but it has a typo in it and that's basically the reason it's filling. And you see that the state is terminated, but this time the reason is error as opposed to completed as it is before. And then the exit code is is 127, right? So, and it's just, it just keeps restarting until you know, until it's fixed. So let's go ahead and fix that. Let's do a kubectl edit or Orange and go to Init containers, we're going to fix this. Just fix the typo and we'll save that and surprise, surprise and save that so we're going to just do a kubectl replace, force -f with this file. Okay, so that's replaced. Let's check, the status now. And we see that the Init container has a command which is now correct and it's already terminated and it's completed. Exit code is now zero. Check the status of the pod, it's now in a running state. Let's check, check it now. Okay, so that's done. Well, that's the end of this lab and I'll see you in the next one.
