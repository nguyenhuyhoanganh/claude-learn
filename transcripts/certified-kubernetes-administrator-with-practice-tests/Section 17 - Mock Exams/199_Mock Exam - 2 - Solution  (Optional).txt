# Mock Exam - 2 - Solution : (Optional)

Okay. So what we're going to do is we're going to go over the solutions for the questions in Mock Exam two. Now in question one, it says that we need to solve this question on cluster one control plane. And it says that we have to create a storage class named local SC with the following specifications and set it as the default storage class. So first let's SSH to that cluster one. And let's go ahead and create a storage class. And what I'm going to do is refer to the documentation. And I'm going to search for the storage class. So we can grab an example configuration. And in the file here I'm just going to copy this example. And I'm going to paste that here. And so what we need to do is we have to change a few things. First of all the name has to be local SC. It says that this should be set as the default storage class. So to do that, we have to set this annotation with the default class, and we just have to set it to be true. Okay. So we've got all of that done. The next thing that we do is we have to set the provisioner to be this following provisioner here. So I'll just copy this. And then we'll go to the provisioner down here and just paste that. All right. And then the next two things that we need is a volume binding mode and the volume expansion mode. So it says nothing about a reclaim policy. So we can go ahead and delete this line. Volume expansion should be enabled. And we can see that it's set to true. So that's enabled. We can remove the mount options. Volume binding mode should be wait for first consumer. And we can see it's set for wait for first consumer. And then we can just remove those other two lines. We don't need that. And that's all we need for the following configuration. We can then go ahead and apply it. And that's going to go ahead and complete question one. Let's now move on to question two. Now for question two. It's once again going to be on the cluster one control plane which we're already at. And what we need to do is we have to create a deployment named logging deployment in the namespace logging dash ns with the replica of one. And it's got the following specifications. So the main container should be named app Dash container. And it should use the following image. And it should run the following command to simulate writing logs. And then we have to add a sidecar container name log agent that also uses the BusyBox image and runs the command that's listed here. The log agent should display the entries logged by the app container. So the app container is going to write logs to a file. Then the log agent will grab the logs from that file and then just print them out. So that's all it should be doing. Or actually all it should do is sorry, not print it out, but it should just tail that log file. So that's all it's going to be doing. So let's go ahead and create a deployment I'm going to get a example configuration of deployment from the docs. I'll just grab this here. And I'll just say question two dot YAML. And we can just paste that. Let's go ahead and update all the fields. So the first thing we have to do is change the name. So this should be logging dash deployment. It should be in the logging ns namespace. Doesn't really say anything about this label. So we can go ahead and remove that. It says that we want replicas of one. And then the labels for the pods. It doesn't say it has to be anything specifically. So I'm just going to set this to be app logger, just so that it's a little bit more realistic. And then the first container should be named app Dash container. So we're going to change this to be app Dash container. And the image should be BusyBox. We can remove the ports. And then what this should do is basically run this command. So we have to change the command of the container. This is going to take a list. So we'll do sh c and then. And let me just copy the whole thing and just delete what I don't need. And this should be dash C. And then we can just remove that. Okay. So that will overwrite the default command of the BusyBox container or the image. And then it's going to run this command. So this will write if you see it's going to write some log entry into a file called app dot log. So it's just going to write that text over and over to that file. That's good. Then we have to add a sidecar container. The name should be log dash agent image should be a BusyBox. And then the command that should run. Is the one provided there which is tail dash f. Slash var log app dot log. Now as I said things are going to get a little tricky here because this container is going to be writing log files to var log. App. Log. Inside its container. So this container does not have access to that file. Because this is var log app. Inside the app container var log app. App log in. The log container is completely separate. So we need a way to sync those together. And the way to do that is anytime you want to share data or share a file between two containers in a pod is to use a volume. So we have to create a volume. So let's go under spec and let's create a volume. We're going to call this log volume. And the type is we could use whatever type we want. But since this is a scratch data and we don't actually need the data to persist, we just need it to be available during the lifetime of the pod. We can use the empty volume type, and this will allow us to essentially treat the var log app log and share it between the container. So we have the volume, and then we have to mount it in the same exact location which is var log app inside both of the containers. So here I'll go down and I will do volume mounts. And I'll say name log volume which is referencing this volume. And we have to say where we want to mount it. Which is going to be slash var slash log slash app. And then we can just copy this. And do the same thing here for the second container. So we're taking this volume and we're mounting it in var log app inside the first container. And then var log app inside the second container. So var log app is going to be basically the same folder in both containers. So now they all have access to the same file. And at that point, I think that's all we need. So let's save this. Let's apply this and hope there's no errors. But I assume there's probably okay no typos. That's good. And I'm going to check the pod. And now what we want to do is let's do let's take a look at the logs. For this pod. And we specifically want the log agent container. So I'll do dash c dash log agent. So this is going to be the logs for that specific container. And we want to see if it's spitting things out. So we can see we're seeing entries being spit out by the container. And I could do a dash f here as well. I'm just going to follow. And we can see that it's able to read the log file that the first container is writing to and spit it out in its standard output. So that confirms that they both have access to the same data using the volume and the volume mounts. So that's going to wrap up. Question two. We can now move on to question three. All right. So for this question that says a deployment named web app that is running in the ingress NS namespace and exposed via service name webapp SVC, create an ingress resource called web App Ingress in the same namespace. Traffic to that service. And we can see all of the requirements here. So let's go ahead and just verify the deployment. I always like to take a look and see that it's actually running. So I'll do a kubectl get deploy dash n ingress dash s. We can see it's running. And let's take a look at the service. And we can see the web app SRV. So we're going to now create an ingress. We're going to create an ingress. So I'm going to create a new file. And I'm going to take a look at the documentation. And we're going to see an example configuration for ingress. So I'm just going to search for ingress. It's usually the first result. And we can see we have an example here. And so I'm just going to paste this. So we're going to have to rename the name of ingress. It says it's going to be called WebEx dash ingress the namespace. So this is going to be the same namespace as the deployment and the service. So this has to be ingress dash ns. Okay. So what do we put for the ingress class name. I'm going to actually save this for a second. And we'll do a kubectl get ingress class. And we can see that it's called nginx. So I'm going to change this to be not nginx example. Pole but actually nginx. Okay. And so we've got the base configuration for the ingress. The next thing you have to do to take a look at the parameters we can see here. It's got to be configured for the Host Cloud Ingress app. You got to use a path type prefix route request center path slash. So the root path to the backend service forward traffic to port 80 of the service okay. So that's pretty straightforward. So we've got a rule here but it's not for a specific host. So I'm going to add that here. So I'll just hit enter. And then here we just put in the word host. And I don't know why it always moves over I don't want it to do that. So host code cloud dash ingress dot app. You can see it's going to use path type prefix. We got to change the path. So the path has to be just the root path. So that's this line right here. So we've now knocked out this line and this line, and we have to forward traffic to port 80 of the service. So what's the service name. It's going to be webapp Dash SVC. And it's got a forward traffic on port 80. So that's all we need to do. I'll save this. And we can go ahead and test this out. By sending a curl request. And we can see we're able to access the default nginx document page which confirms that our ingress configuration was correct. So now move on to the next question. So for question four it says create a new deployment called nginx. Deploy with the following image n1 replica. So we've created the deployment. And then we have to upgrade the deployment to version 1.17 using a rolling update. So to do that what I'm going to do is I'm going to do kubectl create deployment. We'll give it the name the question which is nginx deploy. Specify the first image which is going to be nginx one dot 16. And I'm going to just spit this out to a file by doing dash dash dry run equals client dash o YAML. And I'll set this to be question four YAML. So I'll open this. So we get the base deployment file so we can go ahead and apply this. Keep in mind it says that we need to use rolling updates when it comes to the strategy type of deployment on how you actually upgrade to newer versions. There's multiple strategy types, right. You could say the strategy field here. And if you want a rolling update, you have to set type to be rolling update. Now we don't need to do this because this is the default one. So this is the default one. We can actually go ahead and remove this. It's going to perform a rolling update for us. So I'll save this. We can do a kubectl apply f. Now if I do a kubectl get deployment we can see our deployment. If I do a kubectl rollout history deployment. We can see all of the versions. There's only one at the moment. And if you want to see information about that we could say dash revision. Equals one. And we can see that it's running 1.16. So now let's go ahead and change it to version 1.17. So we'll do kubectl set image deployment nginx deploy. And we want to set the container called nginx. X set the image of that to be nginx 1.17 now. And so now if I do roll out history, we can see there's two versions and the latest version. Should be version 1.17. There you go. So we're now done with question four. Let's now move on to question five. So for questions five it says create a new user called John. Grant him access to the cluster using a CSR named John Developer. Create a role developer which should grant John the permissions to create, list, get, update and delete pods in the developer namespace. His private key exists in this location and there's already a certificate signing request at root John Slash CSR. So let's take a look at that. I'm just going to cat those two files. So we've got root CA John Key. So that's his private key. And if you want to see the certificate signing request that they already created, this is the certificate request. Okay. So now what we have to do to give him access to Kubernetes is we have to create a certificate signing request Kubernetes object. So this is the certificate request itself. But we have to create a Kubernetes object. And then that will eventually give him the final certificate that will allow him to access the Kubernetes cluster. So how do we do that? I'm going to go to the documentation and search for certificate signing request. And we're just going to scroll down. Let me see if I can find an example. All right. So it's going to start off looking like that. Let me see if there's a full example. I don't think there's going to be a full example. It doesn't look like there is a full example. There might be managed TLS certificates in a cluster. No, it doesn't look like there's a good, clear example of this. Just some. Maybe here. I thought there was. Oh, so this is probably the one we want. So it gives us the imperative commands to create the certificate signing request. Oh here we go. So this is what it looks like. All right. So remember this. It's going to be under TLS issue a certificate. So this is what it's going to look like. You're going to have kind certificate signing request. You have the request property which is actually the CSR itself. It's going to be base64 encoded. You've got the signer name. Usually leave it to that default value. You could provide an expiration and then it's specific usages. So let's create that I'm going to do VI. Question Five dot YAML. So this is going to be the API version and kind I'll just paste that in. So it's going to be of type certificate signing request metadata. The name should be John Developer. And then the spec here we have to provide the signer name. And we can even see that as of Kubernetes version 1.19 the certificate signing request object expects to sign a name. This is going to be that value that we just see here. So we can just copy this. And then we have to provide the request which is the certificate signing request. So that without closing this I'm just going to go terminal two. We could just cat slash route CA A slash. That's the certificate signing request. But the problem is, we can't just copy this and paste it into here because this property expects it to be base64 encoded. So to encode to base64 encoded we could say cat slash root CA. Transfer. And then to base64 encoded we do pipe base64. And then we want it to spit out in one line. So we say the following command here. So that's going to base64 encoded. And you can see it's all going to spit out in one line. Then we can take this and then we can paste it here. And then the last property that we need to have is going to be the usages. And we're going to use the following which is digital signature. Key Encipherment. And then client auth. Okay. So we have the CSR object. We'll do a kubectl apply f question five dot YAML. And now if I do a kubectl get csr we can see we've got the John developer certificate signing request. That's pending approval. So to approve or reject it we do kubectl certificate approve. And then it would be John Dash developer. So we're approving this certificate signing request. So now if I take a look at this again we can see it's approved. And more importantly if I do a describe actually instead of describe I do to get CSR John developer and then we'll just do dash o YAML. Right. You have the original request here. But if you go down under status it's going to spit out the certificate. So this is the certificate that a user will use to be able to authenticate and talk to your Kubernetes cluster. And this is base64 encoded. So you're going to have to base64 decode it. And then at that point he can use that certificate to authenticate with the Kubernetes cluster so that your user has everything. But at this point we can now authenticate the user. But we also have to now make sure that he can have the proper permissions to perform various operations on our cluster. This is the authorization aspect of Kubernetes. And so this is what we're going to do. We're going to create a role called developer which will grant John the permission to perform all of the following things. So let's go ahead and set that up. I'm going to create a new file. I'll just call this question five. How about dash Rbac. So this is going to contain all the Rbac configuration. So we want to create a role named developer. So we'll start off here. We're going to set the kind to be role. The name is going to be. Developer. And create a role developer which grants permission to perform these operations in the developer namespace. We have to make sure we set the namespace to be development and then rules. So this is going to be what can you do. So we'll say API groups. Now the API group is going to be set to be empty. And that's because we're giving them permissions for pods which are part of the core group. So whenever you use the core group you just leave that empty. You say resources. What resources are we working on? It's going to be pods. And then what can he do? Let's list it out right here. So I'm just going to copy this. And we also have to make sure that we remove that. And then we need everything in quotations. All right. And we can go ahead. And pretty much that's going to be everything we need for the role. But then we also have to set up a role binding to assign this role to John. So I can create another file. But I'm just going to actually just do everything in the same file. So I'm going to create another Kubernetes object. And you can create multiple objects in one file by separating them with three dashes. We're going to create a role binding. And we'll say. The name. And I'll just call this John developer role binding. And the namespace should be development. So what user are we assigning to a role. It's going to be the John user. So I'll say kind. It's going to be user name John. And then the API group. Is rbac.authorization.k.io. And we have to assign them to a role. So we'll say roll ref kind is going to be role. Name. What's the name of the role that's called developer. And an API group. And we can just copy this. And then we can go ahead and apply this. And it looks like there's an error. So it says unknown field subject. And I already know what the problem is. This should not be subject. This should be subjects with an S. So let's try to apply that. So we've got the role the role binding. Let's go ahead and test it out to see if John has the proper access for everything he needs and the way that you could test out access for a user, you could do kubectl auth can I and you can test what you have access to. So if I do auth can I and he can create list get update pods. We'll say create pods. But right now this is going to test if I the current user can do it. But we don't want to test. If I could do it I already know I can do it. We want to test it as a user John. So we do dash dash as equals. John. And then obviously in the namespace development. So John can create pods in the development namespace. But then if I try to use one of the other namespaces that he doesn't have permission for, like the default namespace, you could see he doesn't have permissions. So we've now successfully given John access to the Kubernetes cluster with the proper permissions. All right. So let's go to the next question now. All right. So for question six it says create an nginx pod called nginx resolver using the image nginx and expose it internally with the service called nginx resolver service. And then test. You're able to look up the service and pod name from within the cluster. And so use the image BusyBox 128 for the DNS lookup, record the results in nginx and nginx. Okay, so the first thing that we'll do is we're going to create this nginx pod. So I'll just use the cube CTL run command to do this quickly. So we'll do nginx dash resolver. And the image is going to be nginx. Now we have to create a service for it so that we can access it. And you have to look at the words pretty carefully. But it says expose and expose it internally with the service called nginx resolver service. So this should only be accessible internally from within the cluster. That's a that's basically a code word for saying cluster IP. Because the cluster IP is only accessible from within the cluster, we can't access it from outside the cluster. If you want access outside the cluster, you can use Nodeport or load balancer. But internally we're going to use cluster IP. So I'll say kubectl expose pod nginx resolver. We'll give the name of the service which is nginx dash resolver dash service dash port equals 80. And the target port. Equals 80. And then the type of service is going to be cluster IP. Now let's verify this. So if I do kubectl get service kubectl describe service. Let's see if it's working. And you can see we've got one endpoint which is going to be the one pod that we created. And you can verify that by doing kubectl get pod o wide and you can see if the nginx resolver has an IP address of 1727.1. 18. So we can see that the service is working as it should. So then it says test that you're able to look up the service and pod names from within the cluster. Use the BusyBox 128 for DNS lookup. So we basically have to create an ephemeral container that will allow us to run one command. And then we can just delete it afterwards. So we'll do kubectl run test dash nslookup dash dash image equals BusyBox 128. We'll do rm which means once we detach from it, the pod will get destroyed. Dash dash restart equals never. And then we'll run the command that we want. So if we do nslookup. And then the name of the service which is nginx dash resolver dash service. Let's see what it says. All right. And we can see that it resolved to the IP address of the service. And if I go here we can see the service is one 7220 .175.22. So that did successfully work. And it says to store it in this file here. So we're going to run the same command but output it to a file in slash route slash slash engine x dot SVC. All right. And then now we also get a DNS entry for the pod IP itself. So if you wanted to you could also grab the IP address of the pod which is here. And you do 172. And instead of the dot 17 you replace the dot with the dash 17. So and then dash one dash 18 dot default dot pod. And I'm just going to show you what the output looks like. So the Kubernetes will actually create a DNS entry for the pod IP which looks like this. And we're going to save this to a file which is slash route slash slash nginx dot pod. And that'll wrap up question six. Let's now move on to question seven. So for question seven it says that you need to create a static pad on cluster one node one called nginx dash critical with the image nginx. Make sure that it's recreated restarted automatically in case of failures. And it says use an example path of slash Kubernetes manifest as the static pod. So to create a static pod on node one we have to. If I do kubectl get node we have to create a file that basically the manifest on node one so that it can create the static pod. So first of all I'm going to do kubectl run and it says it should be called nginx critical. So I just want to get the the YAML file for creating a pod real quick. And I'll just do this here. Critical engine x dash critical. That's what I correct. Let me just copy this. Dash dash image equals nginx. And we're going to do dash dash dash run equals client dash o YAML. And we're going to put it out to a file called static YAML. And if I just cat that we have this. So now that we have this I'm going to ssh to node one. Okay. So we're on node one. And I'm going to go to this slash Etsy Kubernetes manifest. This is where we're going to put all of the manifest files for all the static pods. I'm going to create a. I'll just call it static dot YAML. It doesn't really matter what it's called. And before I do that, let me just copy this because we have this. So I'll paste this in here. So we've got the manifest. Oh oh looks like I got disconnected. So let me ssh back. And then gives you target mode. All right. So I've got the file here I'm just going to go ahead and save this. So inside that node because remember we need a static pod in node one. We just have to go to slash Etsy slash Kubernetes manifest. And then we can just create the manifest file for a pod that we want created statically. Aesthetically if we take a look, that's what I have. So now if I exit out of here and go back to my control plane node, I'll do a kubectl get pod. I'm going to do a dash o wide. And you can see nginx critical. And you'll see that when it names a static pod it's going to assign it to the node name. So it does dash node name. And we can see it's on node one. So that is a static pod. And that is how you create a static pod within Kubernetes. So let's now go to question eight. So for question eight it says create a horizontal pod autoscaler with the main back end HPA for the deployment named backend dash deployment in the backend namespace with the webapp dash HPA YAML file located on the root folder. So they've given us a file that has some base configurations. We can just modify that and show that the HPA scales the deployment based on memory utilization, averaging on average between maintaining an average memory usage of 65% across all parts. Configure the HPA with the minimum of three replicas and a maximum of 15. So let's open up that file and it's going to be under the root folder. So it should be here. And so it's called web app dash HPA YAML. Okay. So what we need to do is first name the horizontal pod autoscaler which we have a configuration for as back end HPA. So we're going to change this here. And this is going to be in the backend namespace. So that's good. It's going to target a deployment called backend deployment. So this has to be updated as well. The deployment should be called backend deployment. The minimum replicas should be three. Maximum should be 15 which we have here. So that's good. And then now let's go ahead and set up the metrics. So we need to maintain an average memory usage of 65% across all pods. So we'll say metrics dash type resource. And we'll say resource name is memory. Target. We're going to set that to be utilization. And then average utilization is going to be 65. And that's because of this right here. So then we can go ahead and save this. And then we'll apply. And let's just do a describe just to see what it shows. And we could see we've got minimum replicas, three max 15 metrics resource memory and pods 65%. Perfect. So that wraps up question eight. We can now move on to question nine. All right. So for question ten it says SSH to cluster one. So let's do that before we proceed any further. And it says modify the existing web gateway on A5673 namespace to handle Https traffic on port 443 for Cockeyed.com using TLS certificate stored in a secret named code cloud TLS. Okay, so we have an existing gateway. So let's take a look and say cube CTL get gateway dash NA5673. three. We can see we've got this. I'm going to output this to a YAML file. So that we can make changes to it. And also it says that we have to use the TLS certificate stored in a secret name code called TLS. Let's take a look and make sure that we have that. So I'll do get secret dash and CA 5673. And you can see we've got the TLS. So everything's looking good. Let's open up that file now. Okay. So it says modify the existing gateway to handle Https traffic on port 443 for code Cloud.com. Okay so we got the spec. We can see that it's listening on port 80 and the protocol is HTTP. We needed port 443 and it needs to be Https. So let's change that. And that's going to be https. The hostname. Should be code cloud.com. As per that. And then we need to set up the TLS. So we'll say certificate. Hrefs. Dash name. And then we'll do code cloud dash TLS. So it's going to point to that secret that we saw earlier. And that's all we need to do for this specific configuration. I'll just go ahead and apply that. And that should be everything that we need for question ten. Let's now move on to question 11. So for question 11 it says on the cluster. The team has installed multiple helm charts on different namespace by mistake. Those deployed resources include one of the vulnerable images called code Cloud Slash webapp v1. Find out that release name and uninstall it. Okay, so what we're doing is first of all, we want to list out all of the releases installed. So we'll do Helm list and then we'll do Dash A for all namespaces. And we can see that there's several different ones. There's four of them. And we have to find the release that has an image that matches this name because this is a vulnerable image. So we need to figure out which one it is. So what we could do is we can go one by one and I'll say helm and there's this command helm get manifest. So this will show the final manifest generated by one of these releases. We'll start off with the first one. Passion. And if I print this out, you can see it shows us all the final manifests. And what we're going to do is we're just going to grep for web app color v1. And you can see that the first one we got lucky. And that is the one that has that image. You can go and check each one manually, but this is the only one that has that. So we know that this is the one that we want to uninstall. So we can just run the command helm. And let me actually get the name of that release I'll do helm uninstall the release name. And then the namespace. And so now you've removed that vulnerable image from your Kubernetes cluster. Okay. So let's now move on to question 12. All right. So for this question, it says you are requested to create a network policy to allow traffic from front end apps located in the front end namespace, to back end apps located in the backend namespace, but not from the databases in the database namespace. There are three policies available in the root folder. Apply the most restrictive policy from the provided YAML files to achieve the desired result. Do not delete any existing policies, so we want to find one of the policies. There's three of them that does everything that's being asked of it right here. And it's as restrictive as possible. So I will go to and let's see I'm just going to cat all of these. There's one that's two. That's three. So let's start off with one. And let's take a look and see if it meets the requirements. So remember we want the front end apps in the front end interface to be able to talk to the backend apps Up, but not from the databases in the database namespace. So in this case we can see it's applied to the backend namespace. If we do select all pods in the backend namespace, and we could see that it's going to select a namespace. In this case it's got a namespace selector. So any namespace with access allowed is going to allow them any of the pods in there to talk to the backend pod. And more importantly if I do a kubectl get namespace dash dash show dash labels. Now you can see there's just one label. Really in this case is backend. If we take a look at the front end it's got name front end. And then if we got where's the database. There's no database namespace in this case. Okay. So this isn't really going to match anything in this case. So I don't think this is the one that we want. Now if we go to the next one once again, namespace backend is going to select all pods in that namespace to the backend namespace, and it's going to allow traffic from the front end namespace. And it's going to allow traffic from the database namespace, which is not what we want, because we do not want traffic coming from the databases in the database namespace. So let's go to the third one. We can see this is applied to the backend namespace pod selector. Selecting all pods in the backend namespace ingress coming from name frontend. So this is going to allow traffic coming in from the front end namespace and nobody else. If the database namespace, nothing from that namespace can talk to the backend namespace, because when it comes to an ingress policy, it's just going to allow what we have here. So this is the one that we want. We can go ahead and apply this. So I'll just do a cube CTL. Apply dash f. And that's all we needed to do.
