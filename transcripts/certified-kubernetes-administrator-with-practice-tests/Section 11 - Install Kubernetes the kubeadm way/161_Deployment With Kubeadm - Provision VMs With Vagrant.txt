# Deployment With Kubeadm - Provision VMs With Vagrant

Instructor: In this demo, we will see how to provision VMs for our Kubernetes cluster that will include a single master node and two worker nodes. And we're gonna accomplish this by using two different pieces of software. The first one is going to be VirtualBox. so this is our hypervisor which is ultimately responsible for running our virtual machines. And the other tool is going to be Vagrant, so this is like a automation tool that makes it really easy to spin up a whole bunch of VMs with a specific configuration. So we're gonna make use of Vagrant so that all of us can kind of follow along with the exact same configuration for our VMs and we can get those VMs up and running with just one single command. So the prerequisites, obviously, for this video is you have to install VirtualBox and you have to install Vagrant. So if you take a look at VirtualBox, you could just go to the virtualbox.org and there's going to be a downloads page, and you could just select the specific operating system you're using, and then you could just follow the steps. Pretty simple. And then Vagrant, if you go to the Vagrant documentation, they have a walkthrough on how to install Vagrant as well. Couple of different ways to do that depending on what your operating system is, just use whichever is most convenient for you. Now for Vagrant, we have a Vagrant file which is going to contain all of our configurations for our VMs. And this file has already been written out for all of you guys and you can find that at the repository for the course. So, go ahead and just clone this repository so that you can have access to the Vagrant file and you can use that to spin up the VMs. And to clone the repository, all we have to do is go to this code dropdown right here and just copy the URL of this specific repository. Then once you do that, go to your Terminal and run a git clone, and then pass in the URL, and that's going to clone the repository. All right, so I cleared out the Terminal but after we cloned the repository, it's going to create a folder with all of those files. And so, I'm going to cd into that folder. And within here, if I just do an ls, you should see the Vagrant file right here. So let's open up the Vagrant file and let's take a look at the configuration. All right, and so, in the configuration, you could see here we've set it up to have one master node and two worker nodes. The IP address is for the host, so this has nothing to do with Kubernetes yet. The IP addresses of the actual VMs or the nodes, they're going to make use of the 192.168.56 network. And if you'd like to, you can go ahead and poke around at the rest of the configuration but we don't need to worry too much about that. The great part about Vagrant is we could just run one command and get all of our VMs up with the same exact configuration. So I'm gonna close this out and I'm going to run a vagrant status. And so, what this is gonna do is it's gonna check the status of the VMs. And for these statuses for the VMs, we can see that they're all set to not created. And so, you could see that we've got the three different VMs here, kubemaster, which is going to be our master node, and then the two worker nodes. And they're all in a not created state because we actually haven't brought up the VMs. So let's go ahead and do that now and we can do that with a vagrant up. And this is going to provision all three of those VMs with the exact specifications from the Vagrant file. And so, what it's going to do is it's actually going to pull the Ubuntu Bionic 64 base image. And then once that image is pulled, it's able to then provision all of the different VMs. This part is going to take a decent amount of time to get all of those VMs up and running, so we're just gonna have to sit there and wait. Just understand that it's not a big deal if it takes a long time, it's kind of expected. Okay, so now that it's complete, if you just take a look at the logs, you can see it actually goes step by step and provision the different VMs. So it first started provisioning the kubemaster, after it provisioned that, it then moved down to the kubenode01, which is the first worker node, and then it wrapped up by provisioning the kubenode02. So we've got all three VMs provisioned. And now if I do a vagrant status, we could take a look at the status of the VMs now that they're provisioned. And we can see that all three nodes are in a running state, which is exactly what we want. So now that we've got the three nodes in a running state, how do we connect to it? Well, we do this by running the command vagrant ssh and then the name of the specific node we want to connect to. So if I want to connect to kubemaster, I'm just gonna copy that, and this is automatically going to connect us to the kubemaster node. And so, now you can see, based off of the command prompt, you can see we are now in the kubemaster. And if I just do an ls -la, we can see that I am in fact in the kubemaster. So to log out, or to exit out of the session, I can just do a logout and that's going to take me back to my local machine. And if I once again do a vagrant status, I can then do a vagrant ssh, and I'll connect to kubenode01. And connected to kubenode01, if I just run an uptime, we can see that it's been up for three minutes so we are, in fact, connected to kubenode01. And then I'll log out of this one and then we'll just quickly connect to kubenode02. So I'll do a vagrant ssh kubenode02 and now we're successfully connected onto kubenode02. So that's how you access all of your VMs. So we've got all of our VMs provisioned and we're good to go. So in the next video, we're going to actually bootstrap the Kubernetes cluster using Kubeadm.
