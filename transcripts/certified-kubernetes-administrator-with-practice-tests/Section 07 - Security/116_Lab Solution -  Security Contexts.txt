# Lab Solution -  Security Contexts

Narrator: So for the first question is asking is what is the user use to execute the sleep process within the ubuntu-sleeper pod? So if you ever wanna see, you know, what user you're logged into as on a Linux machine, you can run the command, whoamI? So all we need to do is run that command inside the container, inside the container. So we'll do Kubectl get pods. We can see the Pod Ubuntu-Sleeper, so I'll do Kubectl exec ubuntu-sleeper and then we'll do the two dashes, and now we enter in the command we wanna run. So we just do whoamI. And I forgot an E right here. And we could see that the user that's used to execute the sleep process is the root user, so we'll select root. The next question is asking us to edit the pod Ubuntu-sleeper to run the sleep process with the user ID of 1010. So to do this, I'm gonna first get the configuration of the current pod because it's saying only make the necessary changes. So I'll do Kubectl get pod ubuntu-sleeper and I'll do -o yaml and we'll pipe this out to a file. And I realized I misspelled this. And now we'll open up that file. And so to change the user, we're gonna look for a property called Security Context. Okay, so here we'll go under security context and we just have to add one line in. And we just have to say runAsuser, and then we provide the idea of the user, which would be 1010. So I will save the configuration. Then I'm gonna do a Kubectl delete pod, so I'm gonna delete the current pod. I'll do a --force just so it deletes it a little bit quicker and then now we can apply the configuration that we just created. And I'm gonna do a Kubectl get pod just to make sure that it's up and running and it's running, so let's go ahead and run the validation, and we can go to the next question. The next question is saying a pod definition file named Multi-pod.yaml is given. With what user are the processes in the web container started. So this pod is created with multiple containers and security context defined at the pod and container level. Okay, so let's open up the multi-pod configuration and we wanna find what is the user for the process in the web container? So if we take a look at the configuration, we can see that the security context was set to run as user 1001 and this is at the pod level. But within the container level, we could see, we overrid it for the web container and we've set it to be 1002. So this is going to be more specific and it's gonna override the pod context. So the answer's gonna be 1002. Next question is asking us with what user are the processes in the sidecar container started. So in this case, we could see that there's no container level security context configuration, so it's going to default to the pod level security context, so that's gonna be 1001. The next question is asking us to update the ubuntu-sleeper Pod to run as Root user and with the SYS_TIME capability. The next question is asking us to update the ubuntu-sleeper Pod to run as Root user. We're gonna change it back to the Root user and add the SYS_TIME capabilities. So I'm gonna open up the ubuntu-sleeper configuration file and so first of all, we're going to remove the securitycontext config 'cause we want to run it as the Root user now. So I'll just delete those two lines. Then we're gonna add the SYS_TIME capability. So we're gonna go under the container that we're interested in and I'm just gonna go, it doesn't really matter where, but I'll just go right here and we'll add a security context line. And then under here, we're gonna pass in a property called add. Oh, sorry, not add, capabilities. And then under here, we're gonna add, and this is going to take a list of different capabilities and so we're just gonna add the SYS_TIME and that should be everything we need. So I'll save the configuration. We'll then do a Kubectl delete pod. And then we'll do an apply. All right, so pod has now been created. Let's go ahead and validate that and looks like everything's good to go. And the final question is asking us to update this pod to make use of the NET_ADMIN capability. So we could just go in, this is gonna be pretty simple and under the add, we just add in the extra capability that we want. So I'll say NET_ADMIN, and that's all we have to do. So we will delete this again. And then we will apply it. And we'll run the validation, and that's going to wrap up this assignment.
