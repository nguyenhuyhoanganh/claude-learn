# Lab Solution -  Network Policies (optional)

-: In this video, we'll go over the practice test on network policies. So, how many network policies do you see in the environment? So we've deployed a few web applications, services, and network policies. that inspect the environment, so let's take a look. Seems to be an image, so, we do not have the alias set, the alias. Okay, let's first check out the pod. So we have four pods. So you have the external, internal the MySQL, and payroll pod. So we have the external, we have the internal pod we have the payroll pod and the DB pod which is the MySQL pod. And let's check out the services you have the DB service, the external service, internal service and the Kubernetes service. So these are, sorry, the payroll service. So these are the the services. So each one, each of them are on different ports. So you have 8080 for the payroll service. The external and internal service are both on 8080. Then you have the DB service, which is on 3306. These are the port numbers. And the question is to identify the network policies in the environment. So we could do a get, kubectl get network policies. So you have the payroll policy. You could also do, kubectl get net poll. So that's the short form for network policy. So we'll stick to this going forward. So there is one policy and that's called as "The Payroll Policy". So there's one policy in total. So what is the name of the network policy? That is Payroll Policy. Okay, so which part is the network policy applied on? So if you look at this you have a pod selector and it says, "Name Equals Payroll". So the, the name of the pod is Payroll. And so the pod name is Payroll. So the network policy named "Payroll Policy" is applied on the pod, which is called a "Payroll". So that's this pod. So if you look at the image we have now identified that this is kind of how it looks. So you have the external port, internal port, DB port and then you have the, the payroll port and then network policies applied on the payroll port. Now as we learned from the lectures, by default all ports allow traffic both ingress and egress on all the ports. So there are no restrictions but as soon as you apply a network policy, it's kind of blocked. So everything gets blocked then you have to manually allow stuff. So let's try and answer this question. So what type of traffic is the network policy configured to handle? So let's look at the network policy in more details. So let's do a kubectl describe net poll Payroll Policy. And we see that, we see the pod selector, which we know and it's allowing ingress traffic to port 8080 from the pod. And it says not affecting egress traffic. So there are no rules for egress traffic. That means all egress traffic from this pod to external to outside world is, is allowed. And the only thing that's allowed is ingress traffic. So what type of traffic is the network policy configured to handle, is just ingress. Okay, now what is the impact of the rule configured on this network policy? So looking at this, we know that it allows ingress traffic to port 8080. So this is with the to the port within the, within the pod, from, we have a pod selector and the name is internal. So this rule allows ingress traffic to port 8080 on the payroll pod from the internal pod. So this is basically what is allowed. So traffic to and from payroll part is blocked, now not entirely true because we know that there is one, one route of traffic that is allowed, traffic from internal to payroll pod is blocked traffic to and from the internal pod is blocked. Traffic from internal to payroll pod is allowed. So traffic from internal to payroll pod is allowed. So that's the correct answer. Now what is the impact of the rule configured on this network policy? So in other words, external pod can ping payroll pod so external pod and ping payroll pod, that's not correct. External pod can't do anything. The internal pod can ping payroll pod, the internal pod cannot ping the payroll pod because we have not, we have only allowed TCP, the TCP rule. So that's not gonna be allowed. External port can access port 8080. That's not true because we have not configured anything for the external pod. The internal pod can access port 8080 on payroll pod. So that's allowed, so that, that's the correct answer. Now access the UI of these applications using the link given about the terminal? Okay, so if you look at this you have the external portal and the internal portal. So let's open those up. Okay, so this is the internal facing application the one, the book background and this is the external facing application. Okay? So access the UI of these applications using the link can load this, we have just done that. Now, perform a connectivity test using the user interface in these applications to access the payroll service on, at port 8080. So we're gonna try and see, we've said this in your policy but we're just gonna see it for real. So from the external port and the internal port we're gonna ping, or we're gonna access, try and access this payroll pod on port 8080. So the service to access a payroll port is a payroll service. So we're gonna copy this and from the internal facing application we're gonna try to do a test. And that is successful, from the external facing application we're gonna do a test and that's timed out. So this is working as expected. The answer here is both internal and external applications can, can access payroll, do, no, so that's not correct. Only internal application can access the payroll service. Okay, now perform a connectivity test using the user interface on the terminal application to access the external service at port 80. So we, we wanna see if, so we've, we have identified that this is how it is. Internal port can access a payroll port and external port cannot access a payroll port. Now let's see if the internal port can access the external port. So external service at port 8080 this is the internal application and try and access the external application at port 8080. And that is success. So that is allowed. So let's select successful. Now finally, we have to create a network policy to allow traffic from the internal application only to the payroll service and DB service. See this is the internal application, we wanna block its access to everything else except for the payroll service and the database service, right? So this is what we want to achieve. So basically this is what we want to create. Use the spec que and below. So we have to create an Azure policy named "Internal Policy". And that is this. And we have to, for, so after creating the policy we need to associate it with the internal pod. So that's the, that's the pod specification. Like the pod selector should be for the internal pod. And then the policy is egress. So we we're gonna configure egress traffic. So we're gonna block everything except for the egress traffic to both of these. And then there should be two rules. One is to the payroll pod and the other is to the DB pod which is the MySQL pod as shown here. So let's try and do that. So first let's go to the Kubernetes documentation pages and look for network policies and let's get a template to start with. So we're going to get until here. So let's create a file called "internalpolicy.YMAL". Okay, so the policy name. So the policy name has to be "Internal policy". Then you have the pod selectors. So we basically wanna select this pod. So that's going to be, okay, and let's specify a name, "Internal". Okay, and let's pick the egress rule. So we want, we only want egress rule. So have that here. So we want to have, let's first define what we want. So first we need a one rule for this and another rule for this. So we basically need two rules. Let's add that here. And so the first rule is so we need to specify the pod selector. So pod selector and match labels. So we want to allow traffic to the pod payroll. So we're gonna give it a name, "Payroll". And we also wanna specify boards. So we wanna allow protocol TCP to board. So to payroll its a 8080, okay. So that's the first rule. Same way we're gonna have for the second rule, we need to specify to which pod as well as to which port, right? So let's just check it here. So it should be so each one will have a two and, and port, ports. So it's actually ports. I'm gonna fix that. Okay, and within this we're gonna have pod selector then we're gonna have match labels, and then we have MySQL. So that's the second pod. And then we're gonna have ports. Then we specify the protocol, which happens to be TCP. And the port for SQL is 3306, as you can see here. Okay, so this particular policy, so we're gonna remove ingress from here. So what this does is it creates a policy for the internal, it creates an internal policy for the internal pod using this label. So everything has a, has a label with the name on it. And then we have, we're allowing egress to the payroll pod on port 8080. And then on the MySQL pod on port 3306. Now let's create that policy. Okay, let's do a describe internal policy and we'll verify the pod selector and egress traffic configuration. So we have 8080 to payroll and 3306 to MySQL is configured. You'll have egress policies configured. Let's check it out. Okay, so that's, that's about it. And that's the end of this lab.
