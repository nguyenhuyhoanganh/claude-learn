# Lab Solution - KubeConfig

-: Okay, so let's go through the lab on cube config or security context. So where is the default cube config file located in the current environment? Find the current home directory by looking at the home environment variable. So let's look at home. And home is set to root, which is the current directory we are in. So we know that it's usually under the hidden cube directory. So we have a cube directory here. And then, and you have config here. So if you look at this, you have dot cube slash config and that's really the cube config file. So it's at root dot cube slash config. So root dot cube slash config. So that's the one. Now, how many clusters are defined in the default cube config file? So if you look at this, the clusters, you have one cluster and then you have context. So you just have one cluster. So that's one. How many users are defined in the default cube config file? So here you see the users are just one again. So here you have the user and you have the certificate information here. So it's just one. How many contacts are defined in the default cube config file? If you look at the context, you have one and that's just one. So you just have one context. Now, what is the user configured in the current context? So if you look at the current context, it's Kubernetes admin at Kubernetes. That's the name of the context. And so that is this. So this is that context. And if you look at this, the user is Kubernetes admin. So remember that this is just the name. So you shouldn't assume that this is the actual user, configured, but it so happens that in this case, the naming and convention is this user at the cluster. But you should always look at this field. So this is the actual user that's configured for that context. So it is Kubernetes dash admin. Now, what is the name of the cluster configured in the default cube config file? So the name of the cluster: so this is the cluster and the name is Kubernetes. Okay, so a new cube config file named my cube config is created and it's in the root directory. So let's look at that, my cube config, and it is placed in the root directory. And how many clusters are defined in that cube config file? So let's look at this file. So you have 1, 2, 3, 4, 4 clusters. How many contexts are configured in the my cube config file? If you look at this, you have 1, 2, 3, 4, 4 context. What user is configured in the research context? So if you look at the research context you see that the user is dev user. So that's dev user is that user. Now, what is the name of the client certificate file configured for the AWS user? So you have users here and you have AWS user. The client certificate file. The file name is AWS user dot crt so that's AWS user dot crt. What is the current context set to in the My Cube config file? The current context, as you can see here, is test user at development. So that's the current context. That's test user development. Now, I would like to use the dev user to access test cluster one. I'll set the current context to the right one, so I can do that. And once the right context is identified use the cube config file and use context command. So dev user to access test cluster one. So we're gonna look at contexts and dev user. So this is the dev user. And to access test cluster one there's already a context called research created. So we're going to use that. So what we need to do is use the cube kubectl config command. So cube kubectl config command, and use the context command. And the context that we're going to use is research. Now remember that by default, the cube kubectl config command will use the default cube config file which is in root dot cube slash config. This is in a different file that we want it to work on. So we have to provide this file name like this. Okay, so switched to context research. Let's check it out. And we see that current context, but in the file it's set to research. Okay, let's check the work. Now, we don't want to have to specify the cube config file option on each command. So make the my cube config file the default cube config file. So this is what we want to use going forward. So what we're going to do is gonna move this cube config file as the default one, which is available here. Okay, let's make sure that move was complete. Yep. So going forward, we don't have to. We could do a cube config view and we can see that this is the new view. Let's check it. So this is the new file. Okay, now with the current context set to research, we are trying to access the cluster. However, something seems to be wrong. Identify and fix the issue. So let's do get notes command. We see that there's something wrong. So it says, unable to read client cert atc Kubernetes PKI users, dev user, developer user dot CRT for the dev user. So let's check content of the file. And we see that the dev user has a client certificate which is set to Dev user, User crt. Now all user certificates are stored in this directory. So let's go here, we have the dev user, let's go here. And we see that it's dev user dot crt dev user dot csr, dev user dot key. So it's dev user dot crt, not developer user dot crt. So we're gonna have to modify that. So let's go in and let's change this. So that's fixed. Let's try it again. Yep. That seems to be working. Okay, so that is the end of this lab.
