# Lab Solution - Imperative Commands (optional)

-: Okay, in this video we will go over the practice test for imperative commands. So in this lab we'll get hands on practice with creating Kubernetes objects imperatively. And this is more for, more to help with your exams. So you must be very familiar with imperative commands while you're preparing for the exam. Okay, so let's, let's proceed. So the first task is to deploy a pod named NGINX pod using the NGINX Alpine image. So that we've done before, it's easy. So it is KUBECTL RUN to create a pod and the name is NGINX Pod, and the image is NGINX Alpine. So check it. Okay, That's correct. The next one, deploy a Redis pod using the Redis alpine image, with the labels set to tier db. So that's the similar task but with an additional field. So if you look at KUBECTL RUN command let's see if there's an option to specify the labels. So if you look at this, there are some examples here. This is what we just did. And here you have an option to specify the labels. So dash dash labels equals and then here's the key value pairs. And you can have multiple labels. You can specify multiple labels separated by a comma. So we're gonna use that approach. So let's run the KUBECTL RUN command. The name, the pod is Redis image is Redis Alpine and we're gonna pass on labels. And then the label is tier equals db. Okay, so that's good. So the next one is to create a service called Redis service to expose the Redis application within the cluster on board 6379. So the service name has to be Redis service. The type is cluster IP, and the port is 6379. So for this, we could run the KUBECTL CREATE service command. Let's look at the help. So if it is, what's in the cluster then it's cluster IP. So we could run the cluster IP KUBECTL CREATE SERVICE cluster IP. Let's look at the help. And then you specify the name of the service and the port here, which happens to be 6379. So one of the thing here is that we want to expose the Redis application, right? So with this command, there's no way to specify a selector. This is something that we, we discussed in this lecture, right? So if you look at this there are three ways to create a service. So this is to create a service named Redis service of cluster IP. And we could do a KUBECTL CREATE SERVICE command but then you cannot really specify the selectors, right? Or you could do a KUBECTL EXPOSE command. So ideally for most, for most use cases you could use the KUBECTL EXPOSE command and because it will automatically detect the labels and then use that as selectors for the service, right? So that's, that's the most ideal. The only time you could you should use KUBECTL CREATE SERVICE is when you need to specify a particular node port, right? So here you have an example of creating a node port service. There are two ways to do it with the EXPOSE command and the CREATE SERVICE command. But as of this recording there is no way to specify a node port in the command. So you have to export that to a YAML file and edit it for creating it. Or if you go the other approach, then there's you cannot specify the selector. So that's, that's a challenge here. So here what we are going to do is we're going to use the EXPOSE command. If you look at the help, these are some examples. So we're gonna specify, EXPOSE and the application is Redis. So we don't know if it's a pod or deployment. So let's find that out first. So it's, it's a pod. So it's a Redis pod. So we're gonna do KUBECTL EXPOSE instead of RC we're gonna do a pod or a PO. And then the name, this, I remember that this is the name of the pod and not the service that we wanna create. So the pod has to be Redis. And this should be a way to specify the name of the service, which is using name. So we're gonna use name and specify it as ready service. And then of course we also have to specify the port. And the port is this one, which is 6379. Okay? So that's, that's what we are going to do. KUBECTL EXPOSE Redis, and then we gonna' specify the port, which is 6379. You also need to specify a name that will be Redis service. Okay? I missed specifying pod. So that in, let's, let's check it out. Service Redis, so it is Redis service. So that's the Redis service. Let's see if it's got the right labels and selectors, and also the endpoint. The labels is tier equals db. So it's automatically picked that up and the endpoints indicates that it's automatically discovered the pod. So let's check. Okay, that's good. Okay, the next one is to create a deployment named web app using the image with three replicas. So we're going to do KUBECTL READ DEPLOYMENT command and the name of the deployment is web app image is, it's gonna copy and paste. And then replicas is, is three. Oops, I forgot to specify the dashes. Yep, we're going to check. Okay, so there are, there's zero of three ready? Okay, three of three ready. Okay. So the next one is to create a new pod called Custom NGINX using the NGINX image and expose it on container port 8080. So we know that to create a pod, we have to KUBECTL RUN command and the name of the pod is Custom NGINX. And then the image is NGINX and we have to expose it on container port 8080. So we're talking about specifying the port on the container. So we just use port and specify the port number. We're not really talking about exposing the port by creating a service. This is just specifying what port the container runs the service on. And you can do that with the port option. Okay, let's check our work. That's correct. Okay the next one is to create a new namespace called Dev NS. So that's pretty easy and straightforward. KUBECTL CREATE namespace Dev NS. Okay, the next one is to create a new deployment called Redis deploy in the Dev NS namespace. So we're gonna do KUBECTL CREATE DEPLOYMENT and it's the name is Redis deploy. The image is Redis, replicas has two. So you could do equals two or space two either of that work. And then the name space is Dev NS. So let's make sure that worked as expected. So we have the Redis deploy within the Dev NS namespace and it has two replicas. Let's check our work. Okay? That's correct. Okay, so the final question is to create a pod called ACTPD using the image ACTPD Alpine in the default namespace and then create a service of type cluster IP by the same name. The target port for the service should be 80. So try to do this with as few steps as possible. So there are two ways of doing this. One is you could for first do a KUBECTL RUN command and create the, the pod. And then we know that we could use the KUBECTL EXPOSE command to create a service, but we can actually combine them both in the RUN command itself. So that's done using, so let's first create a pod. So we do a KUBECTL RUN httpd, that's the name of the pod. And the image is supposed to be ACTPD Alpine. And then we have, it's the default name space so we don't have to specify the name space. And the target port is 80. And what we're asked to do is also create a service. One of the options for the wrong command, you look to help is the exposed option. So if you look at, if you look right here it has the exposed option and it's by defaults at default. So if it's such to true create a cluster IP service associated with the pod and it requires the port field. So it's automatically going to create a service with the cluster IP type and it's going to expose the pod internally. So yeah, so that's, so that's what we have to do. So we're going to, there's the same command but we're going to add the expose equals true option to the end of it. So it's, it is created a pod and a service. Let's just verify that. So we have the ACTPD pod, we have the ACTPD service and if we look at this service in more detail we see that it is got, the selector is run equal ACTPD and then it also has the end point discovered. Okay. So that's, that's correct. Right? So that's the, the end of this lab.
