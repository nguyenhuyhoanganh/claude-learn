# Lab Solution - Deployments (optional)

Instructor: Okay, so in this lab we'll get introduced to deployments. So let us begin. The first question is how many pods exist on the system? So let's run, "Kubectl get pods" command and we see there are no pods. And, the next one is, to identify the number of replica assets. And there are none. So the answer is zero. Okay, and now it's a number of deployments. So "deployments", and that's the rule as well. So it's a clean environment. There are no pods, no replica sets or deployments at the moment. But some changes have been made. So we're now check the deployments again. Okay, so there is one deployment. Okay, so the answer is one. Now how many replica sets exist on the system? Now we know that there's one deployment. You do not know how many replica sets, so let's just run the "get RS" command again to check the number of replica sets. So that's, that's one replica set. So basically the deployment called "front end deployment" has now created a replica set, named "front end deployment dash" some, some numbers. So that's the, the name of the replica set that the deployment has created. And then let's also check the number of pods now. And there are four pods. So basically that's what we're trying to understand here. There was nothing before when we created a deployment. It's created a replica set, and then it's also created four pods. Now, out of all the existing pods, how many are ready? So if you look at the status here, out of four pods, zero are ready. So let's select zero. And what is the image used to create the pods, in the new deployment? So let's take a look, at one of the pods in a bit more detail. Describe pod. Round and deployment. And maybe we'll just pick one of, one of the pod. And we see the image used is "busy box 888". So lets select that. Now I know that there's a little bit of repetition between different exercises here, but this is mainly to get you familiar with, some of these commands, right? The get commands, and the describe commands. And I promise that we have more fun stuff coming up in the upcoming labs. So we're just getting started. Okay, so why do you think the deployment is not ready? And as before, we know that it's because the image "busy box 888" does not exist. So let's select that. Okay, so the next task is to create a new deployment using the deployment definition one file in route. So we're already in route, so let's check this file. So let's try to create the deployment using the file. Okay, and it didn't go. It didn't go through, so there is some kind of error. So it says "No kind deployment is registered for version apps slash B one". So if we look at the file. We see that the API version is "App slash B one", which seems to be correct, but the kind is deployment, which is also right. But the issue is that it's case sensitive, right? So the cap, the first character has to be a capital letter. So that's kind of the standard for kind. So the reason we have these kind of exercises is just to help you understand and and learn how to, you know, know what the issue is when you come up across these errors. So oftentimes you'll, you'll face these kind of errors because of these kind of typos or you know what's not being spelled correctly or not in the right case. And you should know what those mean when you, when you see these kind of errors. So there's, there are possibilities that you may mix up the API version or kind and we don't want to spend a lot of time during the exam just not knowing what those are. So that's the reason for these kind of exercises. So I've just fixed that and let's proceed with the creation. Okay, so that went through. And the final task is to create a new deployment with the below attributes. So you have the name, you have the replicas, and you have the image. So to create an object, we know we can run the Kubectl create command, and it's time we wanna create a deployment. And let's take a look at the help for that. And we see there are different options. So you can create a deployment, specify a name with an image. So we have a name and we have an image. But we also wanna specify the number of replicas. So there is a command just for that. So you have the name, the image, and the replica. So that's what we're going to follow. So on the Kubectl, create deployment command and specify the name, which is "Httpd front end". And, the image, is. And it's good to create a practice of copying and pasting from the question as that will also help you in the exam if you are learning this, for the purpose of giving the exam. And the replicas is number three. Okay, so, and also make it a practice, always verify if what you did this right? So Kubectl will get deployed. We have the Httpd front end deployment, and we also wanna make sure. That it goes to a ready state. Yep. So three out of three are ready. Check our work. Okay. So that's done, and that's the end of this lab.
