# Lab Solution - Pods (optional)

-: Okay, so let's go over the solution for the labs on pods. So in this lab we're gonna get familiar with pods. So the first question is how many pods exist on the system? Now, we know that we can get the information about pods using the kubectl get pods command. That's what we learned, so we're gonna run the kubectl get pods command, and you can ignore this line here that says in the current default namespace, so we're gonna talk about namespaces in one of the upcoming lectures, so you don't have to worry about that for now. This is for those who already know what a namespace is. If you know, then just make sure you're checking the number of pods in the current default namespace. Okay, so when we're on the kubectl get pods command, we see the result is zero, there are no resources found. So we're gonna select zero here. Okay so the next is a task, so we've got to create a new pod with the Engine x image, so to create a pod, the easiest way is to run the kubectl run command, and we specify the image, so we have to specify a name and an image, so if you're not sure about the syntax, run the help command, and we see it's the name of the pod followed by the image, this format. So we're gonna run that, it's kubectl, run. And since the, there's no specification about the name of the pod, so we could basically name it anything. But the image name has to be an x. Okay, so the pod is created. Okay, now how many pods are created now? We've created a few more pods, so please check again. So let's run the kubectl get pods command. Okay, so in addition to the Engine x pod that we created, there are a few other pods that were created by us basically, so there are in a total 1, 2, 3, 4 pods at the moment, so let's select four. What is the image used to create the new pods? You must look at one of the new pods in detail to figure this out, so let's pick one of the pods, this one, and we know that to look at it in detail, we've got to run the kubectl describe command. Okay, so now we have additional details about the pod. So this is the name of the pod, and you have the start time, and a number of other details. So the question is, what is the image used to create the new pods, so we know that this tells us the containers, and here you have the image and the image is BusyBox, so let's select BusyBox. Okay, now which nodes are these pods placed on? You must look at all the pods in detail to figure this out. So one way to do this is we've already run the kubectl describe pod command, and we can see that this is on node control plane, and we could actually run the the describe command against each pod, so that's one way to figure that out. Another way, and an easier way, is to just run kubectl get pods command with the wide option, and here you get to see the node and it says control plane, so all of the new pods are created on the control plane node, select Control Plane. Okay, and how many containers are part of the pod, Web App? So there's a new pod created, this one, get pods command to see Web App. Okay, so if, if you look at this column right here, it shows us the total number of containers part of the pod. So this number right here is the total number of containers. So that's one way to figure that out or you could always run the describe command to see that in detail, so here you see containers and then you have one container here, that's Engine x and the other one right here. So those are the two containers, so select two. Then you have what images are used in the new web app pod, you must look at all the pods in detail to figure this out, actually it should be, you must look at all the containers in detail. So here you have two containers and if you look at, if you look here, you see that the image for Engine x is Engine x and the one for Agent x is called Agent x. So there's Engine x and Agent x, those are the images used for those containers, so this one. Okay, now what is a state of the container, Agent x in the pod, Web App, so if you look at the container here and you see there are, there is a state, and it is in a waiting state at the moment. So out of these, it's not running ready or success, it's actually error or waiting, so this is the state. Okay, now why do you think the container, Agent x in pod, Web App, is in an error or waiting state? So try to figure it out from the Events section of the pod. So if you look at one of the section here, under state, you have the reason called as Error Image Pull, or if you just look at it below here in the Event section, you can see the error which is basically this one, it says, "Fail to pull image, Agent x," and, "at docker.io/library/agentx." So this image by the name Agent x does not exist. And that's basically the reason that it's failing. So the correct answer is a docker image with this name doesn't exist on Docker Hub. Okay, now what does the Ready column in the output of the kubectl get pods command indicate? So if you go up here, you have the output of the get pods command and here you see, you have one slash one here and then you have one slash two. So what does this mean, now we know that this is the total containers within the pod and this is basically, of course, the ones that are ready. So at the moment, there's only one ready out of the two, and we know that because this image does not exist, and of course, that's why that container isn't ready yet. So it's basically the total containers that are ready slash just the total containers within the pod. So that's total containers in the pod or a running containers in the pod slash total containers in the pod, so that's the correct answer, okay, so the next task is to delete the Web App. So let's run the kubectl delete command and delete the Web App pod, okay, that's deleted. Let's go to the next one, create a new pod with the name Redis and with the image, Redis123. And the recommendation is to use a pod definition yaml file and the image name is wrong. Yeah, so the image name is wrong. So we could do this in two ways. So as we know, we could run the kubectl run command, and the name of the pod is Redis and the name of the image is Redis123. You could basically just create it like this, but we just wanna get familiar with generating the yaml file and creating it with the pod definition file. So let's use the dry-run option and output the, the commands in a yaml format. Okay, so, so this is basically command and it says, "The dry-run command is deprecated," and it's replaced with the dry-run equals client, So let's run that, okay, so this is the yaml file. So let's direct it to a file, we'll call it Redis.yaml. So we have the yaml definition for that pod in this file. So we're now going to create the pod using kubectl create or apply command. So let's go with the create command. Okay, so it's created, let's verify that it's created. Okay, so it's created and it's in an error image pool state because we have the wrong image name, so that's fine. Let's click on Check, okay, successful. And the last question for this lab is to change the image on this pod to Redis to fix that error that we, you know, purposefully created, and to get the pod in a running state. So again, there are two ways to do that. You could run the kubectl edit command to edit the the pod, or since we already have the Redis file, we could just go in and change the file and update it. So let's go and do that. Okay, I've changed the image name. I'm just going to apply the changes. Okay, let's check, add it. Now, we'll talk about this error message later when we talk about imperative commands and the declarative way that we apply. But for now, let's stick to the basics, and then just make sure the change was successful, all right, so when I run the kubectl get pods command, we see that the pod is in a running state. So run check, all right, okay, so that's the end of the lab, let's hit the End Lab button, and I'll see you in the next one.
