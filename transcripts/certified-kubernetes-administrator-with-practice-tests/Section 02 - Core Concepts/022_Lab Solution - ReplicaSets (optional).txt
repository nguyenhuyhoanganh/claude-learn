# Lab Solution - ReplicaSets (optional)

-: Okay, so, in this video we're gonna go over the solution for the lab on ReplicaSets. So, let's start with the first question. So, the first question is, how many pods exist on the system? So, just like before, we are on the Kubectl get pods command and there are no pods at the moment, so select zero. The next question is how many ReplicaSets exist on the system? So we run the Kubectl get ReplicaSet command and at zero. So, some changes were made and let's check the ReplicaSet now and we see that there's one created. So, let's just select one. Now the question is how many pods are desired in the new ReplicaSet? So we know that from the output, the desired number of pods in the ReplicaSet is number four, so let's select four. Now, what is the image used to create the pods in the new ReplicaSet? So, we can find that out by looking at the ReplicaSet in a bit more detail. And for that we know we have to run the Kubectl describe command. And we're gonna run describe ReplicaSet and then new ReplicaSet and hit the the tab key to auto complete the names of what you're looking for. So, in this output, we see the pod template and the image that's used, it's busy box 777. So, that's the question and here is the answer. So busy box 777. Now how many pods are ready in the new ReplicaSet? So, if you look at the output here, it says the number of pods that are ready are zero. You can also see the same kind of here, the pod status zero running. So, it is zero. Now, why do you think the pods are not ready? So, if you look at this, you have, so we'll need to look at one of these pods in a bit more detail. Of course it's obvious, but I mean, I just wanna show you, if you're not sure what the error is then you've got to look at one of these pods in a bit more detail. So, let's run the Kubectl describe command and look at any one of the pods in a bit more detail. And you see it's basically because it's failed to pull the image busy box 777 so the repository does not exist. So there's no image by the name busy box 777, that's a reason, so let's check that. Now, the question is to delete any one of the pod. Sorry, it's clear from the get pods command and we've got to delete any one of these pods. So, that's done. So, how many pods exist now? Let's run the get pods command again. Let me see that there's still four pods running. So, even though we deleted one the one that we deleted is not here, the one that starts with the WK, but there are still four pods. So, that exercise was just to show that even if you delete a pod, pod of ReplicaSet, it's automatically going to recreate a new one. And if you look at the age here, you can see that the new one that was created was just 17 seconds ago as opposed to the other ones that have been running for three minutes or more. Okay, so why are there still four pods even after you deleted it? It's not because I didn't delete it properly, it's not because you cannot delete a pod from a ReplicaSet, it's because ReplicaSet ensures that the desired number of pods always run. So, the next one is to create a ReplicaSet using the ReplicaSet definition file located at root. So let's look at where that is. So there's one here and there's an issue with the file, so we have to try and fix it. So, let's first try to create it. So Kubectl create this F partial root, ReplicaSet definition on that YAML. So the error is that it's unable to recognize this file, no matches for kind ReplicaSet in version V1. So, there's something wrong here. Either the kind ReplicaSet is incorrect or the version we want is incorrect. So, let's look at the file and we see that the APA version is V1 and kind is ReplicaSet. And as we learned from the previous video, the APA version is incorrect. But if you're not sure what it is, one way to find that out is using the Kubectl explain command. So run the Kubectl explain ReplicaSet command, sorry. And here you can see the version. So, the version is supposed to be apps/V1, so that's the problem. So let's go ahead and fix that apps/V1 and let's try and delete that again. So, it is now being created. Okay, so let's go to the next one. So next task is to fix the issue in the second file and create the ReplicaSet. So let's try that. Let's just first try to create it. Okay, so it says there's an invalid value selector does not match template labels. So, let's take a look at the file here. And we see the API version is apps/V1 which is correct, the kind is ReplicaSet, that also seems to be correct. The metadata is the name and then you have the spec section. So you have replicas, you have selector. So there is something wrong here, which is why it didn't work. Let's find that out. So here it says, invalid value selector does not match the template labels. So, basically the issue here is that you have the labels specified here called tier front end, and then the label for the pod that the ReplicaSet will create is engine X. So, these two should match, otherwise the ReplicaSet cannot basically manage the pods that it creates. So we've got to either change this to front end or change this to engine X but at the end, both of them should match. So, let's change this to engine X, save this file and run it again. And that's now created. Okay, let's go to the next one. Now, we've got to delete the two newly created ReplicaSets. So, let's first look at the status right now. You can also run just RS instead of ReplicaSet, which is the short form. And now we've got delete both of these ReplicaSets. So, go to delete RS, ReplicaSet one and two. You can also give multiple in the same command. Okay, so that's done. Now, fix the original ReplicaSet to use the correct busy box image. So, we know that the first one which is we not only have one, and we know that there are none of the pods are in a ready state. And we also know that because A, the image name is incorrect, so we've got to know fix that. So for this, let's run the Kubectl edit command and edit the new replica step. And let's go into the container section and change the image name to just busy box and save that file and let's check status of pods now on the ReplicaSet, so it's still zero. Let's just give it some time for it to delete the old pods and create new ones with the new image. Write RS. Okay, so when you update a ReplicaSet, even though the image name is updated, the pods are not automatically recreated, right? So you have to either delete and recreate the entire ReplicaSet or you have to delete all the pods so that the ReplicaSet creates new pods with the new image name. So, let's go back and let's look at pods. And you see there's all still in the error state. So, we're just going to go through and delete them. We're gonna give the name of each one and we'll just wait all of them to be terminated. Let's run get pods again. Okay, it's in a creating state and it's in a running state. Let's check the status of the ReplicaSet now and you can see that they're all in a ready state. Let's go to the next one. Now we've got to scale the ReplicaSet to five pods. So let's run the Kubectl scale command and specify the number of replicas to five. Let's check the status and we can see that a new pod has been added. We can also do that using the edit command. So, we could do a Kubectl edit RS and specify the name of the ReplicaSet, and then go here and just change this number to the desired number and then save the file but we're not gonna do that right now. So that's done. And now the task is to scale the ReplicaSet down to two pods. So, this time, let's use the other approach. So let's edit new ReplicaSet. Go to the number here and set it to two. Let's check. Okay, so that's it for this lab. And next we'll look at deployments.
