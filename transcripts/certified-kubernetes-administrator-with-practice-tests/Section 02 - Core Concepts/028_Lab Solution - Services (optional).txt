# Lab Solution - Services (optional)

-: Hey, in this video we're going to walk over the solution for the lab on services. So the first question is how many services exist on the system? So we're gonna run the kubectl get service command or you could also run the kubectl get SVC command. It's a short for service and we can see that there's just one service. So select that. This is a default service created by Kubernetes. Okay. So what is the type of the default Kubernetes service? If you look at the type, here, we see that it's cluster IP. So let's select cluster IP. We'll look at what this service is in particular later when we talk about the Kubernetes architecture, the API server in more detail. And for now, let's just consider it as any other service. So what is the target port configured on the Kubernetes service? So for this, you've got to look at the service in more detail. So we're gonna do a kubectl describe node and we're gonna describe the service named Kubernetes. So here we see the port details, So the port is 443, the target port is 6443. So the question is, what is a target port? So we'll select 6 4 43. Okay, how many labels are configured on the Kubernetes service? So if you look at this right here, so there are two labels. Component is API server and the provider's Kubernetes. So this this service is basically the Kubernetes API server. Uh, we'll discuss more about the API server and this service specifically when we learn about the API server in detail. And then the CK of course, when we get to that. So for now, the answer is two. Okay? The next question is how many endpoints are attached on the Kubernetes service? So for now, we have no idea, uh, what the service is since this service was already created. We don't really know what it is. So we are kind of exploring and finding out more about it. But one thing if we want to look at right here and understand is how many pods this, is the service directing traffic to? So that's what you can see here in the endpoints section here. So endpoints, it says there's, there's this one IPN port. So that's, that's one endpoint. If there are multiple pods that the service is directing traffic to then there would be multiple ports here. Let me quickly explain what endpoints are because we have not really discussed about endpoints. Okay? So here we have a service and uh, three pods. And what we do usually is when we create a pod we know that it has a label to it. So let's say for example, it's set to app and FE for front end or something. And the way that we create a service in order to direct traffic to these pods is we, uh, provide the same labels as selectors to the service. And then what the service does is it identifies all the pods with the same label and then directs traffic to those pods. So once it identifies the pod with the labels that's when the service has these endpoints. So from a, from the perspective of a service endpoints are basically these, the, the pods. The pods that the service has identified that is going to direct traffic to based on the selector specified on the service and the labels on the pods. Now, when we create a service for a set of pods that we might think that depending on the label and the selector, we specified the service is going to direct traffic to those pods but it might be possible that we have another pod which we accidentally created with the same kind of label. So the service is then going to direct traffic to that pod as well. So, and that's when, when we look at the the output of the kubectl describe command that we can identify the additional endpoints apart from what we, what we thought we had configured, right? So we're gonna see, when you look at kubectl describe command for the service we're gonna see that there are four endpoints. But in fact, the, our application was, had a replica set perhaps that just had three endpoints or three pods. So that's where this, the endpoint can help. And just to, just to give you a bit context. So let's say we create, we have a single pod and we create a service, we set a label to FE, but we accidentally set the selector to say FR something that's different. And then when we try to access the service we're not able to get through to the application. And that's when we, you look at the, the description of the service and you realize that the endpoints are zero. So that means the service has not identified any pods right? So, and then, then that's when we can look at the labels and selectors in, in in more detail to identify the root cause. So, that's what endpoints are. It's, it's nothing, but it's a specification of all the pods that that particular service has identified based on the selectors and the labels set on those pods. -: Okay? So in this case, this particular service has just one endpoint. So we're gonna select one here. Okay? The next question is, how many deployments exist on the system now in the current default name space? So let's identify. Boy, there's just one deployment so we're gonna select one. Okay? So the next question is what is the image used to create the pods in the deployment? So we're gonna look at the deployment in a bit more detail and see the images code cloud slash simple web app red. So that's this one. Okay. Are you able to access the web app UI? Try accessing the web application UI using the tab at the top. So, there's the link here and clicking on it we see that it gives us a bad gateway error. So the answer is no we are not able to access the web app UI. So, and that's really because there's no service, right? So we, we have to create a new service to access the web web application using the service definition dot yamo file. So there is a service definition dot yamo file here and we look at it. So this is basically a kind of a file with the basic framework to create a service. So we'll, we'll use that and update the details that are given here, right? So we're gonna set it, give it a name so the node port and all the different ports, also specify the selector. Now if you're not sure about, you know getting this framework, you could go to the Kubernetes documentation pages, search for service and select service, and here you kind of get a sample that you can then copy and use. So we will talk about imperative ways for commands to create service in, in the upcoming lectures. For now, let's just go with this. So the name is web app service. Type is Node port. Target port is 8080. Port is 8080. North port is 30080, and the selector name is Simple Web App. (typing) Okay, let's create (typing) that service. Okay, that's done. Let's check. Yeah, let's try to access this now. Okay, so that's working. So, as I said, in the upcoming lectures, there is a section where we talk about imperative commands and we'll discuss how we can create a service. So there are different ways of creating a service and we'll discuss about that when we get there. For now, that's the end of this lab.
