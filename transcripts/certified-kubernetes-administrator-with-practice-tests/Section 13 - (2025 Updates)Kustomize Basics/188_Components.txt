# Components

(upbeat tone) -: In this lecture, we're gonna take a look at a customized feature called, Components. And so, a component provides the ability to define a reusable piece of configuration logic that can be included in multiple overlays. And so, these are gonna be really useful in situations where you have an application that supports multiple optional features that need to be enabled only in a subset of the overlays. And that's important to understand, a subset of the overlays. Let's say we have a certain set of configurations that we need to enable a certain feature. If we wanted it enabled across all of the overlays, we would just put it in our base configs. But let's say we didn't want it enabled in all of our overlays, only in a subset of them. Well, we could do that pretty easily just by defining them in each one of the overlays. But then, now we're copying and pasting all of the configs. And so, that's not a scalable solution. We usually try to avoid copying and pasting things like that. 'Cause a lot of times it leads to some config drift. If you make changes in one, you forget to make changes in the other. So components are just reusable blocks of Kubernetes configs, that's all it is. Don't complicate it, it's just all of the resources for a specific feature, all of the patches, all of the config maps, secrets, and any other Kubernetes related configs associated with the feature. They go into a component and then you can enable it in multiple overlays, that's it. So let's take a look at a visual example because I think once you visually see how all of this fits together, you'll have a much better understanding of how components work. Now, let's say our application can be deployed in three different variations. We've got the development variation, the premium one for premium customers, and then a self-hosted version for any one of our customers that wants to self-host our application. So we've got those three folders and those would represent the three different overlays in a customized application. And then we obviously have our folder for the base configuration that's gonna be shared across all three overlays. Now, let's say our application has a couple of optional features, and one of those is caching. And let's say that only the premium and the self-hosted version of our application should have caching enabled. And for caching, in this case, let's say we would need to enable a Redis database as well as you know, all the configurations associated with Redis so that we can implement caching in our application. So only these two versions are gonna have caching enabled. And then let's say we also want a external database service like a Postgres database, and that should only be available for development and the premium versions of our application, so these two guys. So this is going to create a little bit of an issue 'cause we have these individual features that only apply to a certain subset of our overlays. So let's take a look at caching as an example. So we're writing out all of our Kubernetes configs for our caching functionality, and we're trying to figure out where exactly do we put it in our folder structure. The first thing you're thinking is, we put it in our base configuration directory. And the problem with doing that is, all three overlays will get the configuration, but remember, only self-hosted and premium should get the caching configuration, we don't want development to get it. So putting it in the base folder is not an option. So what else can we do? Well, we can copy the caching configuration in both of the premium and self-hosted overlays, and that would work just fine. However, if you make changes in one of the folders, you'd have to remember to make changes in the other one. And let's say we over time decide to add a few more variations to how we can deploy our application. We would have to remember to copy it into all of those. So like most other things like developing any applications, you wanna avoid copying and pasting whenever you can. And so, that's why components were created. They were kind of created so that we could have a reusable block of code that we can apply in several different overlays. So we could take our caching configuration, all the Kubernetes config for that, and create a component, a customized component, and then we can just import that component into all of the different overlays that should be using it. So let's take a look at how that would work. So we've got the exact same folder structure with our three overlays and our base configuration, but now we're gonna create an extra folder and we're gonna store all of our components here. And there's going to be two different components, caching and database. So inside the caching folder, we're gonna have all of the Kubernetes configs that are associated with the caching feature. So like I said, that's going to be probably like a Redis instance with all of the configurations and secrets for the authentication side of our Redis database, as well as anything else that we need for our Redis database. As well as, we're gonna have a folder for our external database feature. And that's going to contain all of the configs that we need to implement that specific feature. So they're all isolated into their own folder. Then what we can do is, all of the overlays that need the caching functionality, they can just import that component. So they just have one line in their configs that say, "Hey, I want to import caching." And then for all of the overlays that need the database, they can just import the database component. So all of the logic for the caching functionality sits in one spot, in that caching folder. And all of the logic for the external database functionality sits in the database folder inside the components directory. And that way we get to write it only once and then we could import it into as many overlays as we want. So let's see how we can implement a component. Now if we take a look at our example project structure, we've got a couple of things that we've already seen before. We've got the base directory that's going to contain all of the base configurations for all of our overlays. And we've got our kustomization.yaml file as well as one deployment configuration in there as well for our API. In the bottom, we've got our overlays. So there's the same three overlays that we have, the dev, the premium, and the standalone. And these are just simple overlays. They just got their kustomization.yaml files, no other extra resources. And then finally, in the middle, we've got our new components folder. So this is going to be the folder that contains all of our individual components. And you can see that we've got two different components. We've got the caching component and the database component. So if we take a look at one of these, we'll focus on the database, but the caching is identical almost. You'll see that there's a couple of different files in there. So first of all, we've got our kustomization.yaml file, that's a given, but then we've got two other files. So I've got a deployment-patch.yaml, and a Postgres-depel.yaml. So what are these files? Well, the Postgres-depel.yaml is a deployment for a Postgres database. So within the database folder or any component folder, we're going to have a list of all of the resources that this feature needs. So I took a look at the feature and I realized the only thing I need to create from a Kubernetes perspective is, I need to have a Postgres database. So I have a Postgres deployment inside the Postgres-depl.yaml. So I'm just creating all of the resources I need to get the external database functionality to work. Now, if we take a look at the kustomization.yaml file, the first thing that you'll notice is the apiVersion and the kind are different. Because this is a component, we have to change the version to the one listed there, and the kind to be component, just something to keep in mind. The next thing that we have to do is, we have to import the resources that we created for this component. So in this case, it's just the Postgres deployment. I also figured that since we have a database, we probably need to store a secret for the database so that we know what the password is. So I've defined a secret for the password. And then finally, we have all of our patches. So when you have a feature, you may need to go in and change some things that are in the base configuration folder. So obviously we have our API-depl.yaml in our base directory, and we need to go in and add an environment variable in there so that it knows what's the password to connect to the new database for our external database feature. So if we take a look at the deployment-patch.yaml file, you can see this is a strategic merge patch. And we're going to update the API-deployment in our base configuration, and we're gonna add a brand new environment variable, and that's going to be the database password. The final thing that we have to cover is the kustomization.yaml file in the overlays. So we have to import the components that we created. So if we take a look at the kustomization.yaml file in the dev folder, we're going to import the database component because as I mentioned, this specific deployment model or this variation for dev makes use of the external database feature. So how do we actually import a component? It's very simple. So if we take a look at the top, we've got the importing of our base, that's just usual customization configuration. But then below that, you can see the component section. So that's going to provide the path to the component that we want to make use of in this overlay. So we provide the path to the database component.
