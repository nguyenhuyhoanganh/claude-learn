# Patches Intro

(gentle music) Instructor: In this section, we're gonna take a look at patches. And so kustomize patches provide another method to modifying Kubernetes configs. However, unlike common transformers, patches provide a more surgical approach to targeting one or more specific sections in a Kubernetes resource. So basically, if you want to apply a configuration across the board, like adding a label or assigning a namespace to all of your Kubernetes objects, you're gonna use a common transformer. However, if you wanna apply or change something on one specific object or just a couple of objects, you would use a kustomize patch. So things like updating the number of replicas in a deployment, you would use a kustomize patch to match a specific object and then change that value. Now to create a patch, we have to provide three different parameters. The first one is Operation Type. So what exactly do we want to do? And so I've listed out the three most common operations. Keep in mind, there are a couple more, but they're not as frequently used. So we have add, remove, and replace. So add, you'd perform an operation, an add operation. If we have a list of containers in a deployment, and let's say you wanted to add another container to that list, that would be an add operation. Remove would be the opposite. Let's say you wanted to remove a container from the list, or even remove a label. It could be removing anything. And then finally, we have replace. So replace is taking a value that's provided and then swapping it out with another value. So if the base configs have a replica count of 5, and you say, "Oh, I actually want 10," you could do a replace and replace the replica to a value of 10. Then after we specify the operation, we have to provide a Target. So this is basically specifying the match criteria on exactly what resource do we want to apply the patch on. And so we can match on a couple of different properties like kind or the version or the name of the object or the namespace, labelSelector, or AnnotationSelector. So you can mix and match as many of these as you want to specify the exact Kubernetes object or objects that you wanna match on and keep. So it's not just one, you don't have to just use one. You can use more than one to match on exactly what you want. And then after you match your specific object, you have to then provide a Value. So for the Value property, this is going to be the value that we will either be replaced or added with. So if we are doing an ad operation, we have to provide what exactly do we want to add? If we want to do a replace, what do we want to replace the preexisting config with? And keep in mind, this is not going to be applicable for a remove operation just because if you're removing something, there's no value to be provided. You just specify the Target and then you remove it. Now let's take a look at a kustomize patch in action. So I have an example depl.yaml file here. And let's say that I want to set up a patch so that I can update the name property. Right now it's set up to be api-deployment, and let's say I wanna change it to web-deployment. So let's take a look at what our kustomization.yaml file is ultimately gonna look like for the patch. So in this case, I've got my kustomization.yaml file, and all of the patch configuration is going to fall under a property called patches. And we're gonna have to provide a couple things. And the first thing is going to be what is the target? So what Kubernetes object are we going to match on? And we could specify a couple of different properties. In this case, I'm matching on a kind of deployment. So it's going to look through all of my Kubernetes configs and it's going to match on only things that are of type deployment. And then I'm going to match on a deployment with a specific name of api-deployment. And that's going to match this exact Kubernetes object. Then we have to specify patch, and then we do this | and then a -. So you have to add that, and this is for what is referred to as an inline patch. And I'll go over what a inline patch is in the next slide or two. But for now, just understand that you're gonna have to add that | and -. And then we have to specify our different properties. So in this case, we are going to specify a operation of replace. So you do op:, and then the name of the, or the type of the operation. Because we wanna swap this out with a new value, so that's going to be a type replace. We're not trying to delete it, we're not trying to add a new name. Then we have this property called path. So what exactly is a path? Well, we have to tell kustomize what is the specific property we want to replace or update, and how do we get to it in the YAML tree. So we have to provide a path to that property. So how do we get to the name property? Well, we have to go under metadata. So for the path, we say /metadata, and then we get to the name property. And so then we do /metadata/name. And if your property is nested further down the YAML tree, you would just keep going down the path and just updating each level accordingly. And we'll take a look at a couple more examples just to make sure you guys really understand this idea. And then finally, we're gonna specify the value that we want to update the name. So we're gonna set this to be web-deployment. And so if we take a look at the final depl.yaml config, it's going to be updated with the new name of web-deployment. Now let's take a look at one more example just so that we can reinforce everything that we just learned. And so I've got the same exact deployment here, and this time, we're going to update or replace the value of replica. So it's set to 1, and I wanna change to some other value. So if we take a look at our kustomization.yaml file, we can see that we have the same exact target. So we're gonna match on a Kubernetes deployment with the name of API deployment. And for the patch, we're gonna once again do a replace operation. And for the path, this is where things are gonna be a little bit different. So we have to figure out, how do we get to the replicas property? Well, we have to go under spec, so it's going to be a /spec. And then we have to get to replica, so /spec/replicas. And then we set the value to be 5. And if we take a look at the final depl.yaml file, we can see that the replicas has been updated to a value of 5. In kustomize, there's actually two different ways to define a patch. So till now, we've only been working with what's referred to as a JSON 6902 patch. And so a JSON 6902 patch has two things that you have to provide, the Target, which is going to tell kustomize what Kubernetes object do we want to patch, and then we have to provide the patch details. So what is the operation, what's the specific property we wanna update, and then what's the new value. So that's a JSON 6902 patch. I've provided a link to the RFC. So if you wanna see or read more details about how this works, definitely take a look at that link. It's gonna walk you step by step through all the different features and functionalities it has. But I wanted to go over the second way to define a patch, and that's called a strategic merge patch. And so this is what a strategic merge patch looks like. And you'll notice one thing, if you take a look at all the text, it looks pretty similar to a standard Kubernetes config. It actually looks like the beginning of a regular Kubernetes deployment. And that's exactly what it is. I basically just copied the original API deployment file and I just pasted it in there, and then I deleted all the stuff I don't wanna change. And so this is one of the perks of using a strategic merge patches that it's just using regular Kubernetes configs. And then what it's going to do is it's going to take this new config and it's going to merge it with the old config, and it's going to figure out exactly what's changed and it's going to update those properties. So when you do this method, you have to make sure that you provide basically two things. Once again, we need a way to tell kustomize what Kubernetes object do we wanna update. So in this case, I've provided metadata and then the name of the deployment. So this is just like defining a deployment where you specify the name, this is what's going to let kustomize know what we're trying to change. And then you have to specify the specific properties that you wanna change. So just like you would define it in a regular depl.yaml file, you go under spec and then replicas, because that's the property I wanna change, and then you just provide the new value. So it's going to take that new value of 5 and it's going to merge with the original config. And I think the original one was a value of 1, and it's gonna say, "Hey, we wanna update 1 to a 5," and then it's going to merge that together and give us the final result. And so that's how we strategic merge patch works. Both of these methods work fine. There's no issues with using either one, it's just a matter of personal preference. You can even mix and match, if you'd like. I happen to prefer the strategic merge patch just because I get to use regular Kubernetes configs, and I think it makes it a little bit more readable. But if you guys prefer JSON 6902 patch, definitely go ahead and use that.
