# Patches Dictionary

(pleasant music) Instructor: Let's take a look at another example on how to update a key in a dictionary in our Kubernetes config using a JSON 6902 patch. So I've got my deployment configuration here. And I've got one label called component API, and I wanna update that to be component web. So how do we do that? Let's take a look at the kustomization.yaml file. So we're gonna have our target, which is going to specify what Kubernetes object we wanna match on. So it's gonna look for a deployment configuration with the name of API deployment. Under patch, you can see that the operation is set to replace 'cause we wanna replace component with a new value. We don't want to add or delete a label, we want to replace this one. And then we have to provide the path. So the path is, how do we actually get to the labels property in our config? Well, we have to go under spec, so we do /spec. Then we go under template, so /template. Then we go under metadata, metadata and then labels, labels. And that's going to get us to that dictionary. And then we wanna update the component property. So how do we update component? Well, under the path you finally just provide the name of the specific key we wanna update. So that's gonna be component. And then we provide the new value, so that's going to change it to web. And so the final result is going to be, the label's going to be change to component web. Now, let's take a look at how we can do the same exact thing of changing a key and a dictionary using a strategic merge patch. And so, I've got my usual API deployment configuration. And I wanna change that one label, component API, to be component web. So how do I do that? Well, let's take a look at our kustomization.yaml file. First thing to note is that instead of doing this inline, I've decided I'm gonna use a separate file. You can do this inline as well, I just think it kind of makes it a little cluttered. So I've decided to move it to a separate file. And so, I have the patches section, which is going to list out all of the YAML files that are gonna contain all of my patches. And I've got one file called label-patch.yaml. And if we take a look at label-patch.yaml, you can see that this is basically a copy and paste of our original API deployment configuration. So that's what I did, I copy pasted it into this file. And then what I did is I went in and I deleted all the properties that weren't changing. So like the containers, that's still gonna be an Nginx container. So I just deleted that, and I just keep all the things that it absolutely needs. So the first thing it needs is going to be the metadata and the name. So it has to be able to know what specific Kubernetes object do we want to update, and then the exact property that I wanna change. So I've set the component to be web this time. And so, customize is then gonna take this config, merge it with the original API deployment config. And it's going to see that the only thing that's changing between the two is the component label is getting changed to web. And so, it's going to then change that label and then give us the outputted result. So we saw how we can replace or modify a key and a dictionary using a JSON 6902 patch. Let's now take a look at how we can add a new key. So I've got my deployment configuration. I've got one label called component API, and I want to add in a second label. So I want to add in a label that says, that's gonna be org kodekloud. So what is that gonna look like in our kustomization.yaml file? Well, the first thing that we need to focus on is the operations. So if you take a look under the patch section, the operation gets changed to an add. So this is going to add a new key to that dictionary. And then we have to provide a path to the specific dictionary that we want updating. So we go under spec, then template, then metadata, and then we get down to labels. And then finally, we have to provide the new key. So you provide the key in the path, and then you provide the value. So then this is gonna give us org kodekloud. And the final result is going to be, we have two labels now with the original component API. And then we have the org kodekloud 'cause this is a add operation. Now, let's see how we can add a key to a dictionary using a strategic merge patch. So I've got the same exact deployment configuration with one label, component API. And then in my kustomization.yaml file, I'm gonna use the separate file method just because I didn't wanna do this inline. And if we take a look at what that label-patch.yaml configuration looks like, you can see, once again, I've just copied the original deployment configuration. And then I've just deleted all the things that won't change. And so, under spec/template/metadata/labels, I've added the new label that I want added. And what this is gonna do is it's gonna take that label-patch.yaml file, and it's going to merge it with the API-deployment.yaml file. And it's gonna see that the original API deployment YAML file had a component API label. And then the label-patch.yaml has a label, org kodekloud. It's gonna see both of them are different, and it's gonna merge it together. And the final result is we get both of those labels added. We saw how we can add and replace a key in a dictionary. So now we're gonna take a look at how we can remove a key from a dictionary using a JSON 6902 patch. So this is my deployment configuration. The only difference now is that you can see I have two labels. If we take our kustomization.yaml file, the most important thing to notice is that we've changed the operation to be a remove. And then for the path, we have to provide the path to get to the specific labels dictionary. So we have to go into spec/template/metadata/labels, and then we provide the specific key of the label that we wanna remove. So in this case, I wanna remove org kodekloud. So then I just provide org, and that's going to delete that label. And so, the final result is that I end up with just one label and that's the component API. So now, let's take a look at how we can remove a key from a dictionary using a strategic merge patch. And so, I've got the same two labels here, and the label that I wanna delete is the org kodekloud. So I've got my kustomization.yaml file. This is going to point to a separate file called label-patch.yaml. And in here, the way we're going to actually delete a key is we're gonna set it to null. So I just go under spec/template/metadata/labels, and then I specify org and give it a value of null. And that's going to delete the key effectively.
