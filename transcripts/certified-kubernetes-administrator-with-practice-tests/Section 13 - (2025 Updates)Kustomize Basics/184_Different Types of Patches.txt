# Different Types of Patches

(pleasant music) Instructor: Now, this is gonna complicate things a little bit more, but for both the JSON 6902 patch, as well as the strategic merge patch, there's two different ways you can define a patch. The first one is inline, and that's the one we've kind of been working with till now. And that's where you define the patch within the kustomization.yaml file. The other method is by using a separate file. So what we can do instead is in the kustomization.yaml file, we can provide the target like we normally do. And then we can provide a path to a YAML file that's going to contain all of our patches, so that we don't have to clutter up our kustomization.yaml file. And so, what that's going to look like is, in the replica-patch.yaml file, that's going to contain a list of all of our patches. So in this case, I've just got one. So either one of these methods works. Once again, if you want to just put it in line in your kustomization.yaml file, feel free to do so. If you have a lot of patches, it may start to clutter things up, and then you can move it to a separate file and then use the separate file method. And we can do the same thing for a strategic merge patch. We can define everything in line or, if we want to, we can specify a path to a YAML file that's going to contain all of our patches.
