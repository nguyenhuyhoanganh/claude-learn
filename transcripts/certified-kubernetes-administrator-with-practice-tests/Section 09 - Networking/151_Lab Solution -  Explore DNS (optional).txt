# Lab Solution -  Explore DNS (optional)

-: Okay, in this video we're gonna look at CoreDNS in Kubernetes. So the first question is to identify the DNS solution implemented in this cluster. So let's do set my alias. K equals kubectl. Let's do get pods. Dash n kube system. We see all the pods in the kube system in space and we see that we have the CoreDNS pods. So these are so, so we use CoreDNS as the DNS solution. Okay, the next question is how many pods of the DNS server are deployed? So we see that there are two, so that's the answer to that. There are two plots. Now, what is the name of the service created for accessing CoreDNS? So the name of the service created for accessing CoreDNS, so let's do a service that is we see in the kube system namespace and we see that there's only one service called KubeDNS. So that's the service that's used for accessing the DNS server. So that is kube-DNS. Now, what is the IP of the CoreDNS server that should be configured on the pods to resolve services. So all the pods should have the IP of the CoreDNS service. And that IP is 10.96.0.10. So that's the IP of this service. So that's this one. Now, where is the configuration file located for configuring the CoreDNS service? So let's look at the CoreDNS service. Let's do a system. We see that we have these two pods. So let's take a closer look at those pods to a describe pod in the kube system namespace. And we see additional information. And here is the container that runs the CoreDNS service and it's, this is image that runs the, the CoreDNS, 1.7.0. And these are the port that's that are exposed. But here, if you look at here these are the arguments that are passed to the CoreDNS container. So the config file is at /etc/CoreDNS/corefile. So that's etc, CoreDNS, core file. So that's the configuration file. How is this core file passed into the CoreDNS pod? So is it pulled from gate when it starts or is it configured as a conflict map object? Does it come built in, or is it stored on the, on the master node? So if you look at this, the path etc CoreDNS, let's see if there are any mounts. So there are mounts. So we have a etc CoreDNS mount and it says that it is coming from config volume. And we can also see this if we do, so let's say we do a kubectl, get pod, and we look at it in the yaml format. So if you look at it this way, scroll up. Okay. So this is the spec section. So you can ignore all of this. Always look at the spec section. And we have the containers. So for the container, we have the arguments that we just saw, the image and everything. If you scroll down, you'll see the volume mounts. So you have volume amounts here. And this the etc CoreDNS is the volume mount, and it's coming from a config volume. And so if you have volume mounts, then you have volumes. So you scroll down and receive volumes. So this is where the volumes are configured. And the volume for config mount is the config volume. And here you see, you have the core file and CoreDNS CoreDNS is the config volume. So config map. So this is what indicates, this is what tells us that it's loaded from a config map. Okay, so the next question is, what is the name of the config map object created for core file? So for this volume, this config map, the name of the config map is CoreDNS. So that's, that's the name of the config map. Now, what is the root domain or zone configured for this kubernetes cluster? So to find that out, we have to look into that config map. So let's do that now. So let's do a get config map in the kube system namespace. So we have the CoreDNS. So let's do a describe, describe config map in the kube system namespace. These, these are the DNS config for, for Kubernetes. And right here, this is the root, this is the root domain. So let's cluster.local. Now we have deployed a set of pods and services in the default and payroll namespaces, to inspect them and go to the next question, right? So let's do that. So let's look at get pods. And we see that there are indeed the hr the simple web app and tests. So there's, there are four pods and then there's also another namespace called payroll. And in the payroll namespace there's another called web, right? So there are multiple applications, so that's fine. Now, what name can be used to access the HR web server from the test application? So here, here we have the HR web server and here we have the test application. So we wanna access the HR web server from the test application. So you can execute the curl command, or you can test applications also have a UI. So let's, let's use the UI. I like the UI this is the UI of the test application. And we'll use this to run a connectivity test. And, but first, how do you access the HR application, right? So if you do a HR and assuming it's on 8080, it's not working. So we had to find out what is the service that's used to access this. So let's do a kubectl get svc because that's in the default namespace, and we see that there are multiple services. You, you have the test service and the webservice. I'm assuming the test service is for the test application. And I'm not sure if the webservice is for the HR application. So let's do a describe on the webservice. Service is missing. svc. And we see that the selector here, name equals HR right? So that indicates that the webservice is a service for the HR application. So that, that's what connects them two. So it's webservice. So I'm gonna use webservice and the port, if you see is 80. So that's what I'm gonna do. So I'm going to use webservice and the port 80 and let's run a test. Okay, so it says this is the HR service. So that's, that's good. So that confirms that. So it's, it's webservice. That's what is the answer to this question. Okay, now, which of the names cannot be used to access the HR service from the test pod? So we know that to access the, the HR service from the test pod we just used webservice, that works. Does webservice.default work, it will because we are in the default namespace. So let's try that, default. Yep. So that works. Does webservice.default.svc work? .svc Yes, because svc stands for service and webservice is a service, in the default namespace. So that's, that's fine. The next one is webservice.default.pod. So let's try pod. So that fails. So that's not, that's not the right kind of convention. So this is the right answer to this question. Now, which of the below name can be used to access the payroll service from the test application? So the payroll service as we know is, it must be in the payroll namespace, right? So let's explore that. Let's do a kubectl, get pod, payroll. And then so there's a webservice there. And then let's do a get svc - n payroll. And we see a webservice here. So this is, let's make sure that this is for that. So let's do a kubectl describe, svc, webservice. In the payroll service. The payroll namespace. Okay and, we see that the selector's name equals web app. So that's, that's for this. So yes, the webservice is used for the web application in the payroll service. So in the payroll namespace, we'll just assume that this is the payroll service. So which of the below name can you use to access the payroll service from the test application? So the test application we know is in the default namespace. So if you do webservice from the default namespace it's going to go to the HR app. That's not what we want. We want this, we wanna access the webservice in the payroll namespace. So web is not the correct answer. Webservice.default is gonna go to the HR app, webservice.payroll because it's in the payroll namsespace. So let's try that. And it says this is a payroll. So yeah, so it's webservice.payroll. So that's the answer to this question. Now, which of the below name cannot be used to access the payroll service from the test application? So webservice.payroll, you know it works, webservice.payroll.svc.cluster. So payroll.svc.cluster. So that doesn't work because it should either be, webservice.payroll or it should be webservice.payroll.svc Or it should be webservice.payroll.svc.cluster.local because cluster.local is the root domain, the root zone. So you should always, you should either use cluster.local or you shouldn't use anything at all. So cluster is something that doesn't work. Now we just deployed a web server, web app that accesses a database MySQL server. So let's check that out. Kubectl, get deployment. And we see that there's a web app, indeed. And then it accesses a database, MySQL server, however the web server is failing to connect to the database. So we don't, we don't see a MySQL server here, so let's do a, get, let's do a kubectl, get pods and all namespaces so we can see all the pods. So we can ignore most of these. But if you look at these two, so there are two namespaces and we have the web app, the pod that runs the web app. So it says web app. So this is web app. So this is, this is our application. And if you look here, we have the MySQL on database here. So it is in the payroll service, payroll namespace. So basically this web app is trying to connect to the MySQL pod in the payroll namespace. But it's not able to access. Look at the applications and the web server interface can be seen by clicking the tab web server at the top of the terminal. So here we have the web server. So just open that, and as you can see it is in a failed state. So if you look at the environment variables that were passed to it, it says the db host is MySQL, db database not set, db user route, db password, all of that. So it says the name does not resolve. So the issue is with the name. So if you look at this, it says MySQL, so MySQL is a name. So the web app here is trying to get to the MySQL service here, which is in the payroll namespace. So it's not gonna be able to do that. Let's first see the service configured for it in the payroll namespace. So we have the MySQL service, so the service is there but for the web app to reach the MySQL service it would need to say MySQL.payroll, right? So that, that's how it should be. So let's see where that is configured. So let's do a kubectl describe deployment web app. Web app. And we see that it has environment variables configured here. So MySQL route and, and the password. So let's do edit, deploy and, and edit it. Now of course this password here, this is not a best practice. It should be going in through as a secret or some, some other, you know, a safer method. But we just did that here. Just keep, just to keep this simple. So we have the environment here, which is MySQL, and we're gonna change that to MySQL.payroll. Okay, so you could just say MySQL.payroll or MySQL.payroll.svc. Or MySQL.payroll.svc.cluster.local. Okay, so let's save that. Let's check the pods. Let me see that the older one is terminating and then the new one is already running. So let's go back and check the status here. We see that it's a success. So now we have db host, which is MySQL.payroll. And so that's, that's good. So let's check the work. Okay, so that, that was successful. Now the next question is, from the HR pod, run a nslookup and the MySQL service and redirect the output to a file. So from the HR pod. So we're gonna do a exec HR and then give a command. And what the command we need to run is nslookup, we'll do a MySQL and it says it's not found, so we know that since the HR app is in the default namespace and MySQL is in the payroll namespace, we should be appending payroll to the name. Okay, so that's, that's got us the the IP address of the MySQL port. And you can also see the full name of the MySQL service here. So let's redirect that to this path. Let's make sure that it's redirected successfully and see, see the output. Okay? Yep. So that's, that's the end of this lab. Alright, thank you very much.
