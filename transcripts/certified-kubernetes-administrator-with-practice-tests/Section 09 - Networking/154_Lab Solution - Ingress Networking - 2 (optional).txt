# Lab Solution - Ingress Networking - 2 (optional)

-: Hey, In this video, we're gonna look at ingress networking, in a bit more detail. So we'll, we'll try and deploy an ingress controller. So, we've deployed two applications, "explore" and "setup". So let's check that out. Set, headings first. Let's do pause dash a. And we have, in the app space we have the video and there applications. So, that is now deploying an ingress controller first. So create a name space called "ingress space". So let's, so this is what we are going to do and the first step is to create this name space called "ingress space". So we'll do this one step at a time. So let's do that first. Create name space ingress space. Okay, so that's successful. Now, the NGINX ingress controller requires a ConfigMap object. Create a ConfigMap object in the ingress space. So, we're just gonna create a ConfigMap object. So we'll do a Q call, create conflict map in this and the name should be as given here and genetic configuration. And the name space is ingress space. Okay? So that's done. So if you can see here, so you've just created these two. Now the NGINX ingress controller requires a service account. So create a service account in the ingress space. Name, the name of the service account is ingress service account. So create service account and the ingress space. Okay? We've created the roles and rule bindings for the service account. So let's check that out. Okay, so get roles. So we have the ingress role and bindings we have the ingress role binding Let's check out ingress role in the ingress space. So we see that there are a number of resources that are given access to like the policy Chris name spaces, and these are the works that the actions that it can perform. Okay, let's keep going. Let us now deploy the ingress controller create a deployment using the file given. So there's already a file given called ingress controller and the, you know, the hard stuff is already there in there. So you have, it's called it's gonna be called ingress controller. The service account name that we created is here. The container that it uses is here. The arguments that are passed to it is configured and there are some environment variables as well and the port that is exposed. So these are all done. There are just, there are some issues. So let's try and let's try and create a file from this first, Create a deployment from this file first. So it looks like there are some issues. So there's, there's an issue online. 36 looks like a yamo issue. So let's, let's edit the file and try and fix those. So let's go to line 36. So right here we see that, yeah indeed there's an issue with spacing. So we'll fix that's, try to create it again and we see that namespace, ingress dash not found. So if you look here the namespace is not properly specified. So let's try and edit that and space. Okay, so let's try and create ingress controller now. Let's check out the status in the ingress space. And it's in a continuing creating state. Let's give it some time. And meanwhile we can check the status of our work. So yeah, it's, it's completed successfully. So we have now deployed an ingress controller. Now the next step is to create a service to make the ingress make ingress available to external users. Okay, so the ingress controller is ready. The next step is to create a service called ingress. So we're going to do, what we're going to do is our goal is to expose ingress controller, right? So the easiest way to do that is deploy dash n ingress space. It's called ingress controller. So we're gonna create a service using the exposed command. So we're gonna do expose and what do we wanna expose? We wanna expose the ingress controller deployment. Let's say deploy an ingress controller in the ingress names ingress space Right? And and also there are some specifications given here. So the name has to be ingress so pass in a name called ingress. So let's check out the, the help just to get a bit of bit more idea. So we have the exposed command and then we have the deployment deployment name. If you want a name for the service, you can use dash dash name. That's what we just did. Can also specify the port, so the port and the target port. So we wanna use 80, and the target port is also 80 but the expos command does not allow us to use the node port option. So what we're gonna do is we're gonna create the service first, and then we're just gonna go in and edit the node port. So that's kind of the easiest way to do that. So we'll just take these because that's what we are left with. So let's clear my screen and we're gonna specify the port. So the port and the target port are both 80, okay? So we're gonna do an export deploy ingress controller in the ingress space. We'll name it ingress. And then we have specified these. We can also specify the type. So we'll see dash dash type going to be node port. That's what's given here. Then we have specified the port and the target port. The node port cannot be specified, but we specified this. So, and the right selector. So expo should take care of using the right selector for this, for the deployment. So let's create that. So it's created to get, as we see in the ingress space and it's, it's created, it's a node port. And if you, if you look here at the port that the node port that it used is 32 7 41. That's not what we wanted. So we'll go and edit the service in the ingress space edit service called ingress. And here we can see the details. The only thing we need to do is change this to 3 0 0 8 0. This is actually a simpler way to create a service without having to work with the amo files at all. So that's gonna be useful in the exam. Let's check. Okay, so that's successful. Now the next step is to create the ingress resource to make the applications available at where and watch on the ingress service. So we're gonna create an ingress service. So we'll do a kubectl create ingress in the app space. That's where we wanna, that's where the applications are. And quickly check help. So this is, is what we used earlier. So we're gonna do a create and then give the name of the ingress rule and then we'll just use this rule to specify the rules. So, the name of the ingress resource would just be ingress and where, and watch. And then we'll specify the rules. We don't need the certificate stuff. So we'll remove that. So the rules says where needs to go to the where service. And then we have another rule. Again, we're going to remove this. And this is for the second one. So we can have multiple rules like this, watch. And there's going to go to the watch service. So I'm not sure if it's called the where and watch service. I'm just going to park this for now and let's check the service names in app space. And we see that, okay, it's the where service and it's the video service, not the watch service. And the ports are 80 80. So let's edit that. So the watch is called the video service and Port 80 80. And the where is called the where service. And let's get rid of this for now. Okay? So that's created. Ingress in the app space, see that it's created. Let's do a describe ingress. Ingress, just to app space. And we see that the where is is to the where service and it's identified the, the endpoint for that. And the watch is to the video service. And it's identified the endpoints to that. So let's, let's test it out and see if it's working. So let's do a where, Okay, so it isn't working. Let's do a watch. Okay, so let's go and check out the logs on this applications. So let's do logs. Check this out. And as you can see, the requests aren't even reaching here. Let's check the other service, right? So if the requests were coming in you'd actually see the request coming in. So the requests aren't hitting, hitting this. So that's one thing, and let's try and figure out why. So let's do, let's look at the logs of the ingress controller itself. And the ingress controller as we know is in the ingress space just do a logs of this ingress controller, okay? And if you look here, there seems to be a lot of logs. So app space. So it's definitely hitting the ingress controller but, then you see there are three node eights, right? So the three note eights indicate a redirect, right? So what's happening is every time we're going here as you can see the, the browser here it just keeps redirecting. So it, it looks like it's going into a too, and you can see the error here. So it's going into too many redirects. So, and that is mainly because it is just trying to do a redirect on the SSL, right? So for https, it's trying to do a redirect on https. So what we need to do is we need to edit the Ingress service. Yeah. So here's Ingress service. We need to edit it in the app space to provide in Chris. And then we need to add the annotations, so the annotations for the rewrite target, because as we discussed earlier we don't want it to go to a where and watch URL. And then this is, so to prevent the redirect SSL redirect we have to add the false, the SSL redirect to false. So let's, let's edit that and let's try this again. Okay? So that's, that seems to do the trick. And it's actually working now. Okay, let's check our work. And we've already accessed the application. Okay? So that's, that's the end of this lab.
