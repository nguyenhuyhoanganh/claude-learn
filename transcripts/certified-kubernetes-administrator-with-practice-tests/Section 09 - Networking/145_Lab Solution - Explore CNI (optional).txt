# Lab Solution - Explore CNI (optional)

Instructor: So, for the first question, it's asking us to inspect the kubelet service and identify the container runtime endpoint that's set for this Kubernetes cluster. So, I'm gonna do a ps -aux | grep -i kubelet. And we can see there's a lot of information. So I'm gonna further drill that down, hence, I'm gonna grep for container -runtime. And we can see the --container-runtime-endpoint flag is set to unix:///var/run/containerd/containerd.sock. So I will select this one. What is the path configured with all binaries of CNI supported plug-ins? So all of the plug-ins are going to be stored in /opt/cni/bin. And so, if I then do an ls, we can see all of the binaries here. So, it's going to be /opt/cni/bin. Identify which of the below plugins is not available in the list of available CNI plugins on the host? So let's see, vlan, we have vlan there. We have, let's see, cisco, do we have cisco? Doesn't look like we have cisco. Let's just double check if the other two are there. So bridge, we've got bridge. Dhcp, we've got dhcp. So cisco is not there. What is the CNI plugin configured to be used on this Kubernetes cluster? So to see that, we can just do a ls /etc/cni/net.d. So this is going to tell us which plugin has been configured, and we could see it's flannel. So I'll select flannel. And what binary executable file will be run by the kubelet after a container and its associated namespaces are created? So if I cat that file, and let me actually move into there. Here we can see that the binary that it's gonna run is flannel. And then after that, it's gonna run portmap. So the answer is flannel. And that's gonna match up with this binary here. So we'll say flannel. And that's going to wrap up this lab.
