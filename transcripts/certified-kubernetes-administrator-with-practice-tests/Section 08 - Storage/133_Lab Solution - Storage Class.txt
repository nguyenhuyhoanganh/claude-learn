# Lab Solution - Storage Class

-: Let us now review the lab on storage class. So the first question is how many storage classes exist in the cluster? Let's do a queue code to get storage class. So we see one, we can also do a SC to the storage classes. So that's the short form. We see that there's just one storage class. So let's select one. How about now? How many storage classes exist in the cluster? We just created a few more. So let's try it again. And we see that there are three setting things up. So what is the name of the storage class that does not support dynamic volume provisioning? So we know that the provisionary is what defines how the storage class volumes are provisioned. So the no provisioner is one that does not support dynamic volume provisioning. So that's the provisioner. So that's the storage class that does not support dynamic volume provisioning. So that's local storage. Now what is the volume binding mode used for the storage class? The one identified in the previous question. So for this the volume binding mode is wait for first consumer. So this is the answer. So what is the provisioner used for? The storage class called WoodworksIO, priority high. So this is the stories class called WoodworksIO priority high and provisioner is port works volume. Now is there a persistent volume claim that is consuming the persistent volume called Local PV? So is there a persistent volume claim? So let's look at persistent volumes and persistent volume claims. And there are no persistent volume claims at all. So no is the answer. So create a persistent volume claim by the name of local PVC that should bind to the volume local PV. So we need to create a PVC with the name local PVC. And if it is to bind to the PV they must have capacity, which is, yeah, 500 megabytes. So that's correct. And then the access modes should match. So this is ReadWrite ones and the storage class should also match; so that's local storage. So we're gonna create PVC.YAML with all that information. So let's go and find persistent volume. Let's select persistent volume and let's look at PVC. And here you have PVC. So we'll copy until here. We don't need the the selectors. So let's paste that here. So the name is local PVC. The access mode, as we discuss it, has to be rewrite once, which is fine. We don't need to specify the volume mode for now. And storage is 500 megabytes. And the storage class name is local storage. That's what we saw. Right, so local storage. So that's it. Let's create the PVC. Let's make sure it's created. Okay. Now what is the status of the newly created persistent volume claim? So it's in a pending state. Now why is the PVC in a pending state despite making a valid request to claim the volume call local PV. So we made sure that we got everything right that 500 megabytes of capacity, rewrite ones access model matching, the local storage class is also matching. So then why is it that it was not created? So let's look at the events. So let's do a queue code: describe PVC local PVC. And here we see that the reason is that it's waiting for first consumer, waiting for the first consumer to be created before binding. So as we discussed, the wait for first consumer means that it's not going to bind to a PV if it's not used. So it's just waiting for someone to use it for a ideally a volume to use it. Right, so a pod consuming the volume is not scheduled person volume and claim is match search class not found. So this is the right answer. So the storage class called local storage makes use of volume binding mode set to wait for first consumer. So this will delay the binding and provisioning of of precision volume until a pod using the person volume claim is created. Create a new pod called EngineX with the image X alpine. Let's do that. And the pod should make use of the PVC, local PVC and mound the volume at the part were, WWHGML, the local PV should be in a bound state. So let's create a pod called EngineX with the image EngineX alpine, and then the portrait make use of the local PVC. So for that we're going to have to convert this to ammo. So we're gonna run it, we're gonna create the ammo file redirect it to EngineX. Start the ammo. Okay, so we have all of this details in there. Now what we need to do is we need to first configure the volume and the volume amount, right? So we need, we need the volume section and the volume has to come from the local PVC. So let's look at claims as volumes. And here you have example of using a claim as volume. So you have, you already have a volume section so we just need this. Okay, make sure to fix the format. So we have volumes and the name is going to be, let's name it as local PVC volume. Okay? Now the claim name has to be local PVC. Okay? And the next thing is we need the volume mount which is inside the container. So the volume is on the spec level at the pod and the volume mount is inside the container. So let's do that. We have the volume mount and the volume mount says you can see here is at (indistinct), which is right. And this, the name of the volume is the name that we've given here. It's gonna be this. So if the question does not ask you to use a specific name, you can use any name. Just make sure that this name and this name matches. So that's when this volume is going to get mount to this directory. Okay, let's save that and let's create a file. Let's see the status of the part. Okay, it's created, but it's not in a running stage. Give it a second. Yep, it's in the running stage. Okay, so that's successful. So let's check the status of the PVC now. And we now see that it's in a bound state - so it's in a bound state. So as soon as a volume use that PVC, it went into a bound state. So create a new storage called storage class called delayed volume SC that makes use of the below specs provisioner and volume binding mode. Okay, so let's do that. Let's first create a file called Delete volume SE.ammo. And we need a template for a storage class. That search for storage class. So we're gonna use this. So let's use the first lines. Okay, now the name is going to be delayed volume sc. The provisioner is going to be: no provisioner. And volume Binding Mode.. The volume binding mode is this.. is going to be wait for first consumer. Let's wait for first consumer. Yep. So this seems to be it. So let's create that storage class. Yes, the storage class is created. Let's check it out. Yep. It's created with the No Provisioner provisioner and the wait for first consumer while in binding mount. (indistinct) Okay, That's successful. Yep. So that's the end of this lab.
