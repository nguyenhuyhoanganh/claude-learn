# Lab Solution: Monitor Cluster Components

-: Okay, In this lab we get familiar with monitoring cluster components. So the first question is we have deployed a few pods running workloads so inspect them. Let's do kubectl get pods command. And we see that there are indeed three pods, elephant, lion, and rabbit, and they're all in a running state. So let's go ahead and what we need to do is monitor the resources consumed by each of these pods. And for that we need to deploy the metric server. And for this, we have a get repo with the settings for the metric server that will work for our labs. So we're just going to use that. But if you're trying this in your own environment or for a production environment, so remember do not use this for production use-cases because there are changes that are in here that are done just to make this work with this particular lab exercise and just to make it easy for us to deploy it. So do not use this for a production use-case. Always remember to refer to the official recommendation on the metric server to deploy one in your environment. So let's wait for, okay, so this is cloned. And if you look at this, you have the files here that are required to deploy the metric server. Let's go ahead. Okay. So the next step is to deploy the metric server by creating these components. So we're just going to do a kubectl create-f., that creates all of the objects required to deploy the metric server. Okay, let's go next. And at times it might take a few minutes for the metric servers to start gathering and reporting data. And so if you do a kubectl top node, yeah, it seems to be working already. So yes, that's working now so we can proceed. Okay, the next question is to identify the node that consumes the most CPU. So here you can see two nodes and the CPU consumption of the two. So node 01 has 57 milli cores and the control plane consumes about 470 milli cores. And if you look at it the percentage is just 1%, but still you can see there's a difference between the two. And of course that's because the control plane has all the control plane components running on it. So to answer this question the most CPU consumption is by the control plane. So let's select that. The next one is to identify the node that consumes the most memory. If you look at the memory consumption here, again, we know we can see that the control plane consumes most memory. So answer to this is control plane again. The next question is to identify the pod that consumes the most memory. So the pod this time. So you can get that by running the kubectl top pod command. And here you can see the memory consumption and the CPU consumption. If you look at the memory consumption you can see that it's rabbit. Rabbit consumes the most memory. So that's the answer to this is rabbit. And the next question is to identify the pod that consumes the least CPU. So here you can see it's lion. This consumes one milli core of CPU so select lion and that should be it. So basically that's a quick introduction to deploying the metric server and also just using the commands to look at its reporting and identify basic information. All right, thank you.
