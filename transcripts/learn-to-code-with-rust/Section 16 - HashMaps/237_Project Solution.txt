# Project Solution

So let's walk through a sample solution to the coding challenge. In the previous lesson, this section focused on hash maps, and the first thing we were asked to do is to bring the hash map type into the current file's namespace. So at the very top, I'll utilize my 'use' keyword and reach into my standard library, which is abbreviated 'std' And after two colons I can go into the 'collections' submodule, and in there I'll find my 'HashMap' type. The advantage of this 'use' syntax is I can now refer to 'HashMap' directly inline without needing to provide the complete prefix. So here we have our type. A hash map is a collection type that holds key-value pairs. Each key is a unique identifier for a value. So we use hash maps for association, to create mappings or bridges between two different values. So our next instructions were to declare a 'sauces_to_meals' HashMap that is going to associate various sauces to meals that they go well with. So the keys are going to be string slices, and the values are going to be vectors of string slices. So we're asked to use the 'from' function to create our HashMap. And as a reminder the 'from' function accepts an array of tuples where every tuple holds two elements. The first tuple element will become the eventual key, and the second tuple element will become the corresponding value for that entry into the HashMap that the 'from' function will instantiate and return. So right here I'll declare one tuple per line. For my very first key-value pair, I need a key of ketchup and then a value of a vector. So I'll use my vec! macro here to provide these three string slices. So French fries, Burgers, and Hot Dogs. Then I'll provide a comma and a line break and then declare my second tuple representing the elements that will form my second key-value pair. And so this is going to be "Mayonnaise". And then a vector with the vec! macro consisting of Sandwiches, Burgers, and Coleslaw. And then I will take this and assign it to a 'sauces_to_meals' variable. Now notice because we provided some initial data right here, Rust can infer that the type of the generics are the following. So for the keys, the very first generic Rust knows is going to be string slices. And for the values which is the second generic, those are going to be of type vector of string slices. If we did not provide any initial data, we would either have to explicitly annotate this type or do something with the hash map like insert an eventual key-value pair so that the compiler could deduce the types of the keys and values. If we did nothing by just having an empty hash map, then we need to provide the types for the program to compile, because Rust can't possibly guess what it is that you're storing. All right, so next up, use the 'insert' method to add the following key value pair to the HashMap. So first things first, if we're going to be adding or modifying anything in the HashMap, I need to make it mutable. So I'll add the 'mut' keyword. And here I'll take my hash map, call the 'insert' method. This accepts two arguments. The first is the key, the second is the value. So my key will be "Mustard and my value will again be a vector with the vec! macro of Hot dog, Burgers and Pretzels. It looks like basically every condiment goes well with burgers, which is fun. All right. And if we quickly want to confirm what this hash map looks like, we can print it out right here in Debug formatting using my question mark. Here is 'sauces_to_meals'. And we should have three total key-value pairs. Here is "Mayonnaise", here is "Mustard" and here is "Ketchup". Each one is associated with a value of a vector. And we can similarly say that this hash map has a length of three. The length of a HashMap is a count of its key-value pairs, so neither a key nor a value can live independently of the other. They are intrinsically linked. All right, let's remove this. And next up let's use the 'remove' method to remove the key value pair where "Mayonnaise" is the key. So right here I'll take 'sauces_to_meals'. And I'll call the 'remove' method. That also expects a key as its first argument. And I'm going to pass in my key of "Mayonnaise". Now there is the possibility that the key does not exist and thus there is no value. And thus this method will return an Option. It's going to be the 'None' variant. If the key does not exist, and it's going to be the 'Some' variant holding the value for this key if it does exist. Now there's many different ways to deal with Options. The kind of proper idiomatic Rust way is with something like a 'match', and we're going to do that for the follow up problem. But for now, the easiest way to retrieve the vector inside the Option is to do something like 'unwrap'. Once again, this is a little bit more dangerous because if we have the 'None' variant it's going to break. But I know that I have "Mayonnaise" right here. And it's just for the sake of example. So I can do this just to show you. And again this is going to give us back a vector, the vector for the "Mayonnaise" key. So if I want to print it out I need to make sure that I use my Debug formatting because a vector does not implement the Display trait, only the Debug trait. So this should give us this vector that was the value for the deleted "Mayonnaise" key. And at this point as well, our HashMap will have one less key-value pair within it because we removed "Mayonnaise" and its vector. All right. Next up, use the 'get' method to retrieve the key-value pair where "Mustard" is the key. So once again 'sauces_to_meals'. We'll invoke the 'get' method. Just like 'remove', it accepts the key as the argument. And I will look for the "Mustard" entry. And I will assign this to a variable like 'mustard_meals'. Once again, there is the possibility that the key does not exist. That is why we get an Option and the 'None' variant means Rust was unable to find it. Now if there is a 'Some' variant, what it's going to store is a reference to the value. There is no mutation to the HashMap in this scenario. The reason why this method will give you an Option storing a reference is because if it was not a reference, then the 'Some' variant would take ownership over that corresponding value, and that would create this weird state where the HashMap would have ownership over some of its key-value pairs, but perhaps not the value of one of them. Right. And as we've seen with composite types, whether it be arrays or tuples or structs, generally Rust wants its composite types to be the owner of anything that is nested within them, the elements that are contained inside. So we don't want to transfer ownership of this vector. If we were to move it out to, let's say, a separate variable from our Option, we don't want to take away that ownership from the 'sauces_to_meals' HashMap and therefore to prevent the transfer of ownership, the 'get' method will give you a reference and once again, furthermore, it will be wrapped in an Option because there's the possibility that the key does not exist at all. So here I can do the more idiomatic Rust approach. If our goal is to retrieve the vector and print it out, I'm going to use the 'match' keyword and compare that with 'mustard_meals'. So now I have to provide a arm for both my 'Some' and 'None' variants. So let's do 'Some' here. And if I declare my name for my associated data, you'll notice that it is inferred correctly to be a reference to a vector holding string slices. So here I can provide a block and output some text like "The meals were", and then interpolate the value of 'meals' using my Debug formatting. So again, 'meals' right here is going to be a reference. But we can use it regularly and we're going to be able to interpolate it as long as we use Debug formatting. And then we also need to provide our 'None' variant. That is in case the "Mustard" key does not exist. So here I can println! something like "There were no meals for that sauce. Oh no". In fact, since we're not doing anything besides a println!, we also do not need that additional set of curly braces. So that should do it for us. And there we have it. So let's try running this. Our "Mustard" key does exist. So we should see this vector output through its reference. There it is. And the ownership remains completely within these 'sauces_to_meals' hash map for that act. All right. Finally use the 'entry' and 'or_insert' methods to add the following key-value pair. So here, once again, take my hash map. Call the 'entry' method. Very much like the methods above, it accepts a string with the key that we're targeting. And if I provide "Soy sauce", what's interesting about this in combination with the 'or_insert' method, which is actually called on the 'Entry' struct that entry returns, is again, this key-value pair will only be added if "Soy Sauce" the key does not already exist within the HashMap. If this key does exist, then this method will accomplish nothing, so it's a little bit more safer compared to 'insert'. 'insert' does not care whether something does or does not exist, it's just going to add the key-value pair no matter what. This approach does care and will not insert if this key is already present. Now in this case, "Soy Sauce" is not there. So we are going to add this and here to 'or_insert' I'll provide the corresponding value for that key. So this can be a vector of "Sushi" and "Dumplings". And then I'll provide my semicolon. And now let's output my Hashmap to compare it with the expected result. Now keep in mind a HashMap is supposed to be unordered, or you should assume that it's going to be unordered. If you're looking to keep things in order or in line, you should probably be using another data structure, like an array or tuple. The point of a hash map is to create associations, not to maintain order. And so the reason I mention that is when I output this, if the order of the key-value pairs is different from what you see here, that's totally fine. In fact, it may vary each time you run the file. That's totally fine as well. We are looking for the correct keys and the correct values. So we're looking for mustard, ketchup and soy sauce. Here is mustard. Here is ketchup. Here is soy sauce. So that is exactly what we expect. And I just want to show you one more time that if I swap this with a key that does already exist, like "Ketchup", then we are not going to replace this vector with this one. This code is essentially going to accomplish nothing. So when I look at the key of "Ketchup" in my hash map, I'm going to get its original three element vector: French fries, burgers and hot dogs. All right. So that has been a quick review and introduction to the hash map type. This is basically found in every single programming language in the world. So a very common data structure. With that I will see you in the next lesson.
