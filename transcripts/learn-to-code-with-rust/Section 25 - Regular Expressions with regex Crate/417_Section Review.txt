# Section Review

Congratulations on reaching the end of another course section. As always, let's take a few moments to review everything that we learned. This section focused on regular expressions using the regex crate. So out of the box, the rust language and its standard library don't have support for regular expressions, which are a common feature in pretty much every modern programming language. So the regex library crate is the default community standard for the use of regular expressions in the rust language. A regular expression, often abbreviated to regex, is just a search pattern for text. A regular expression uses symbols to declare a sequence of characters to look for within a target string, and that sequence of characters can include both dynamic and hard coded characters. We can look, for example, for a sequence of two digits in a row, or specifically for the letter Z, right? In whatever order we want. The regex crate uses the terminology haystack to describe the target string that the regex searches within. And that comes from the old expression needle in a haystack, which just means searching for something within something else. So we bring in the regex crate into our cargo.toml file, and then we bring in the regex struct into the module's namespace. We then create a new regex struct using the new constructor function, and that will give you the actual pattern. It's actually going to give it to you within a result. So you want to call unwrap on that to get the actual regex struct itself. And to that new constructor function, you pass a string with the actual regex search pattern. Typically we're going to do that with what's called a raw string, which is just a regular string that is prefixed with a lowercase r, and that just tells rust to parse that string literally. So typically rust is going to look for characters like the slash N for the new line, or the slash T for the tab. And it's going to parse those special characters differently. We don't want to do that in the case of a regular expression string, because there's the potential for conflict with the way that symbols and characters are used when declaring our actual search pattern. So we want every character within our regular expression string to be treated literally as it is, because every slash actually matters in its significance. And thus, we're typically going to create our regex string as a raw string with the prefixed r. Now, as we saw, our regex can match sequences of exact characters and specific actual alphabetic characters specific numeric values in whatever order you want. That is kind of the most basic form of it, where you're literally declaring, hey, I want a lowercase letter k, followed by the exact number two in that sequence. So once we have a regular expression, we of course apply it to find one or more matches within our haystack. So we first took a look at the find method on the regex struct that searches for the first match, or the first occurrence of the regex pattern within the haystack. And that's going to give you back an option enum to account for the possibility that there is no matches. If there is a match within the some variant, you're going to find that match struct, which is a struct that models that match. Right. Meaning that the search pattern was correctly found somewhere within the haystack. So it's going to give you the start byte where the match started, the end byte, and the exact concrete match. So of course, if you're looking literally for two exact characters in a row like w y, well then it's going to tell you that w y was found. But where this becomes really valuable is when your pattern is a lot more dynamic, where it may account for one or more characters, or between 3 and 7 word characters in a row. That's where the actual concrete match within the haystack can totally vary depending on what you're working with. And so the match structure is going to give you all of that metadata. Now if you're looking to find all of the matches of the regex string within the haystack, we can use something like the find inner method, which returns an iterable that you can then traverse over to give you all of the matches. If there are no matches, you're going to get an empty iterator. And if there's more than one, you're simply going to get an iterator of multiple matches. And thus there's no need to worry about options here because there is no possibility of absence at all. Absence will be modeled, of course, by an empty iterator. All right. So of course we introduced a whole bunch of these symbols. They're often called character classes. And they just allow us to specify specific things to target within the pattern. So for example slash D perhaps the most common one matches a single digit. The complement which is slash capital D is going to match a non digit. We talked about this notion that the inverse casing for the same character is usually going to apply the inverse logic. So lowercase d is a digit, capital D is a non digit. The same principle is going to apply for all of these other matches you see here. So slash w is going to match a word character that's going to include alphabetic letters. Digits underscores the complementary capital W is going to match non-word characters like punctuation question marks, spaces other symbols like that. Similarly, slash lowercase s matches a whitespace, and the complementary slash capital S matches any non-whitespace characters. We also talked about word boundaries, which are designated with the slash lowercase b symbol. A word boundary effectively represents the gap or space between a word character and a non-word character. And where that can be really helpful is when you combine the slash B with another character, either after it or before it, and that allows you to look for your matches specifically at the beginning or the end of a given word. So if you say something like lowercase m, then slash b, you're saying I want the letter m before a word boundary, which means right at the end of a word right before. In other words, the occurrence of a non-word character, such as a space. So by itself, slash B isn't particularly useful. But like many of these symbols, they become really powerful when they are combined into a more complex search pattern. So this allows us to basically mark where a word begins or ends in combination with whatever other characters we're looking for at that exact position. All right. We also talked about the special dot meta character. A meta character is a character that has special significance in a regex regular expression, and specifically the dot meta character matches any character at all. One thing I didn't mention is technically, the slash n new line is not included in that set of characters, but basically the dot matches everything and anything, right? A English alphabetic character. Any kind of digit, any kind of symbol. So by itself, it's kind of pointless because it matches everything. But once again, if we combine the dot with other symbols, that's where we can expand the complexity of our patterns. We can look, for example, for the letter P followed by any character whatsoever, followed by the letter Q, right. We can combine these dynamic bits and these hard coded fixed bits to create these more complex patterns. Now if you're looking for the literal dot period character, you have to escape the dot with the backslash to indicate a literal dot within your actual regex search pattern. All right. We also talked about square brackets. We declare them directly within the regex string, and we can place multiple characters inside the square brackets. And that will basically create a match of any of them. So for example this syntax on the screen square brackets will match any one of those three characters in that specific position within your regex search string. Now, because it can get pretty annoying to write out a sequence of alphabetic characters. There are some shortcuts available. For example, you can use a dash to create a character range from a lower character to an upper character. For example, in this example with the parentheses, a dash m means match any lowercase character between lowercase a and lowercase m, so that includes a, b, c, d, e all the way through m. It's important to note that lowercase characters and uppercase characters are treated as totally different sets, so lowercase a through m will not match the complementary uppercase equivalents. So if you want to match any combination of lowercase and uppercase characters, you have to declare them together within these square brackets. So here for example, this will look for any capital letter between A through M or any lowercase letter between a through z. We're capturing the capital equivalence and the lowercase equivalence within the exact same square brackets. All right. We also talked about the ways that we can specify the exact number of matches for the preceding search symbol or character. So for example, after anything, we can provide a pair of curly braces with a number and that will declare the exact number of matches to look for for the preceding symbol or character. So for example, slash d means a digit. So slash D with a pair of curly braces with a four in them means exactly four digits in a row. If you add a comma after, that's going to declare at least that number of matches in a row. So once again, slash D means a digit or a number. So four comma means at least four digits in a row. It has to be at least four. But it can be anything greater than that as well. Five digits in a row, ten digits 15 digits in a row, etc. but at least for now, if we provide two values on either side of the comma, that creates a lower bound and an upper bound. So a range of possible numbers of occurrences for that search symbol. So in this case d means digit four. Comma eight means match between four digits in a row and eight digits in a row, and anything in between. So five, six, seven are also valid. We're placing an upper constraint or upper limit on how high that number can go. We also talked about the special plus, which is another example of a meta character. And that indicates one or more. That's a very common way to replace the more verbose way of writing one or more, which is providing the curly braces and writing one comma. Typically you're going to see the plus because it's a little bit more concise. So for example, slash D+ means at least one digit in a row, or equivalently one or more. All right. We also talked about or logic how we can find either or or in other words this match or this match within the pattern we create a pair of parentheses. And then we use a vertical pipe to declare the different characters or symbols that can match. For example, if I do a slash D and then a vertical pipe and a slash s, I'm saying match either a single digit or a single whitespace in this location, right? And we can combine all of the symbols we learned inside the parentheses. So in this final example in the last bullet we have slash D indicating a digit. Then a pipe which indicates or logic and then slash S with two curly braces. So this means match either a single digit or two whitespaces in a row. So the whitespace is more precise. We're saying exactly two whitespace in a row, but that is combined with the Or logic with a single digit. So we can get really, really complex and nuanced here. All right. We also talked about anchors which allow us to mark or designate the beginning or end of the search string. So for example, if you add your dollar sign that marks the end of the string and that allows you to specifically look for your pattern at the end or termination of a string. So you're going to add that dollar sign symbol after the specific character that you're searching for or characters. So for example here, if we do slash D+, that means one or more digits. And that would be fine by itself, but specifically by adding the dollar sign afterwards. We're more specifically saying one or more digits at the end of the haystack, or at the end of the string that you are searching within. The complementary anchor symbol is the carrot, and that designates the start of the string. So for example, slash w will look for a word character anywhere in the string. But a character slash w is a more precise pattern that says look for one word character specifically at the start of the string. So it's more precise, it's more strict. There's a lower possibility of matches because you're designating where exactly the actual match has to occur within the haystack. All right. We also talked about capture groups, one of the most complex but powerful features in regex. A capture group allows us to isolate and optionally name a chunk or segment or fragment of the regular expression. So if we have a complex regular expression, we can actually basically slice the various portions of it and give each one of those portions a name, and then reuse that name in various operations later. So we wrap that fraction or segment that we want to target in parentheses. That's the first step. And then before the actual symbol within the parentheses we write a question mark. Then a pair of angle brackets with the name of the actual capture group, which is optional. You do not have to give your capture group a name. If you don't, they're going to be accessible by their index position, but typically a name provides both clarity and makes it easier for you to target that capture group later on. So once you have your regex struct, you call the captures method on that you pass in the haystack and you're going to have access to those capture groups. Now, as we just mentioned, if you don't provide them with names, you're going to be able to access them by their index positions. Remember, index position zero in that capture struct is going to represent the full regex match, so the complete match of the entire regular expression within the haystack. The subsequent index positions starting from one and moving forward. For however many capture groups you have, those are actually going to have the matches for the specific capture groups for the actual segments within the larger regular expression. That's pretty powerful stuff. All right. Finally, we talked about the replace all method, which swaps all occurrences of the regex pattern within your haystack with whatever you'd like. And what makes this even more powerful, as we saw than just your basic find and replace feature, is we can actually access the capture group's dynamic value within the replacement. So we declare our capture group with a question mark and angle brackets and the name. Then we can access that value through its capture group name, but within the actual replace all method. For the second argument in that string, you use a dollar sign to prefix any capture group name that will pull out the value for that capture group within the replacement content that you are swapping for every occurrence of that regex in your haystack in your target string. All right, so this is pretty complex stuff. As I mentioned, there are whole textbooks and whole college courses that are dedicated to regular expressions because there's a lot more complexity to cover here. Hopefully, at a minimum, you can see the power of this incredible technology. It allows you to do really, really comprehensive, nuanced, detailed, dynamic, varying searches to pull out whatever target content you want within your larger strings. So super helpful. Obviously, for example, if you're parsing text or transcribing speech to be able to pull out certain details, pull out a phone number, pull out an email address, figure out a person's name. Lots of real world applications to this. So of course rust supports it through the regex crate, but that's all there is to cover in this lesson and in this section. I hope you had fun and I will see you in the next one.
