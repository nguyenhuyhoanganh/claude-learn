# Alphanumeric Characters

The next symbol that I want to introduce you to is lowercase w. This special symbol is going to match any alphabetic characters or any digits. And the parent word that you'll often hear that represents that idea is alpha numeric. So that just means characters plus digits. So this is going to exclude things like spaces periods and any other symbols like the dollar sign or the ampersand. It's strictly going to target characters and digits. So keeping the exact same code from the previous lesson I'm going to run this again. And you can see we're going to start by matching on all of our characters M, Y, Z, etc.. You'll notice we are not going to match on this whitespace right here. This space in between here. We're also not going to match on characters like this period right here or the dot. So here is the match for the zero and 90210. You can see we do not match on either the dot or the space that follows. And then we pick up the next match on the capital I. And as we talked about in the previous lesson, And if we swap this symbol from a lowercase w to a capital w, that is going to do the opposite. It's going to find any match anywhere for anything that is not a character and not a digit. So this is going to match the spaces, the periods, the question mark, etc. it is the inverse of alphanumeric. So here you can take a look. Now we're going to get a match for every space right. Every dot and so on. So it is the inverse of that lowercase w for alphanumeric capital w for non alphanumeric. Right. Very important as well that you prefix that character with that backslash. So that rust and regex understand that it is specifically alphanumeric or non-alphanumeric. Because if you just left w then this would be literally searching for the capital letter w. That's not what we want. We want non-alphanumeric characters. And for all of these special symbols, they're all going to begin with that backslash followed by some kind of character that has symbolic significance in building up that regex search pattern.
