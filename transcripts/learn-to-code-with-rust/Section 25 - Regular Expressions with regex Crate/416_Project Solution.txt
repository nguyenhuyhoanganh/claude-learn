# Project Solution

So let's take a look at a sample solution for the coding challenge in the previous lesson. We begin with a vector of server logs, each of which has a uniform standardized format within each server log string. We have a pair of square brackets that encloses a timestamp such as this one. Then we have a space and the category of the log, such as info or debug. We have some information here until we get to our API endpoint which consists of slash API, a slash, and then the actual endpoint itself. You can see orders right here versus users versus products. Then we have another forward slash and then a unique identifier for that product a numeric ID such as 12 345 here or 9876 here. Now all server logs that we want to gather up are going to have this standardized format. But there are a couple exceptions. For example, there are lines like this that just have a whole bunch of bad data. There's also going to be lines like this that represent valid logs, but don't have any kind of information in the standardized format that we want to pull out. So our goal here is to extract the following four pieces of information from all of the valid strings that contain them the timestamp, the category, the endpoint and the product. And then place those pieces of data as fields into this log entry struct. And then of course return a vector of all of those log entry structs printed out. So we want to go from a vector of this effectively to a vector of log entries one log entry per valid string. And of course, in order to accomplish this, we of course are going to need to use regex to parse the content inside each string to relevant pieces of information that we're then going to stuff inside our log entry construction. So to begin at the very top. Let's bring in the regex struct from the regex crate. So right here use regex and the only name I want to bring in is the regex with a capital R. And down below I'm going to create my actual search pattern. I'm going to call it re. And right here let's do regex new. And I'm going to provide my string here as a raw string preceded by a lowercase r. And I'm going to assume that what I put in here is going to be valid. We are of course going to validate our work over the course of this lesson. But for now, let's assume that we are going to provide a valid pattern. So I will immediately call the unwrap method on this. We're going to get the actual regex struct itself. All right. So now this is a matter of simply walking through this string piece by piece and designating every section of it with the special symbols. Ultimately, in order to get to these extracted pieces of information, I'm going to eventually move to use capture groups, which will allow me to designate names for these various portions or segments. But just to begin, I'm going to just write out the complete regex string without worrying about capture groups. Then I'm just going to make sure that that regex pattern correctly matches all of my target strings here. And then once that's done, then I'll apply my capture groups. And after that I'll build up the final vector. So there are mostly familiar symbols in here. And in case there were anything that we didn't cover before, my hope was that you simply googled it to discover whatever was the appropriate symbol to cover it. So in the case of this piece of text right here, we of course begin with a opening square bracket. The problem with an opening square bracket is that is a common regex symbol, right? In case we're doing something like a pair of square brackets with ABC to indicate either A or B, or C, unfortunately, the opening square bracket here is a special character, but we still need to include it as an actual literal character within our search string. So very much like with the dot meta character and the need to replace it with a slash dot. It's the exact same principle with the square bracket. So to actually indicate a literal square bracket to be matched, we're going to provide a backslash before. And that's going to match literally this character. All right. Then we are going to have the four digit year. So I'm going to write slash D for digit. And then in my curly braces I'll declare exactly four digits in a row. And before I progress I also want to state the obvious here, which is if in your solution you did a regex pattern that was different, slightly different, or completely different. As long as you got the correct data out and the correct data populating into your log entries, that's the most important thing. Many times with regex there is room for variation and creativity. So your answer does not have to be exactly the same as mine. This is just one sample solution. So in this case, I am explicitly searching for exactly four digits in a row Right afterwards we're going to have a dash. So I'm going to add that right here. And then for the month we're also going to have digits I'm going to do slash D again. But this time around we're going to have two in a row like so. Then another dash. And then we're going to have the two digits for the day. So once again slash D with two. And then we're going to have a whitespace before we get to the actual time portion of the timestamp. So right here I'm going to add a single slash s for a single space. And then we're going to have two digits a colon two digits a colon two more digits. And then the closing square bracket. So right here we're going to again do slash D two digits in a row for a colon. That's going to be the colon symbol in here. After that I'm going to do another two digits with slash D two then another colon. Then the final two digit collection right here is once again going to be slash D with two in curly braces, and then similarly to the opening square bracket, we're going to need to escape the closing square bracket. So I'm going to write a backslash and then the closing square bracket right here. So I'm going to continue progressing with this pattern. But at this point I do have enough. And I do want to move on ever so slightly to make sure that we are correctly parsing at least the timestamps from the valid ones right here. And if I come up with an elegant solution, I can also very effectively eliminate these invalid rows right here, simply because they're not going to match this desired regex pattern. So down below I'll declare a new variable. I'm going to call it entries, and this is going to be a vector. For now I'll provide the type as a vector. And I'm going to use that nice magic trick we have, which is providing my angle brackets here, and an underscore which tells rust. Deduce the type of the element stored inside the vector. Deduce or figure out the type of the generic. So if the code on the right is clear enough, rust will be able to figure out the elements inside the vector automatically. So on the right we have our server logs vector. I'm going to call into iter on it to create an iterator. And then I'm going to use a nice method that I have here called filter map. As a reminder, filter map is going to allow us to both filter. In other words, target a subset of elements from the vector to include and also transform them into something else to any other data type I want. So what I want to do is first to filter out all of the valid rows which are going to include these, and then I also want to convert them into a match struct. So I'm actually going to call the find method directly on my regex struct right here, and pass in each of these entry strings. Throughout the iteration, I'm going to show you why this is a very elegant solution, especially in combination with the filter map method. So right here I'm going to provide my closure. And we're going to get each server log. I'll call my closure parameter here log. And then I'm going to take my regex struct represented by re. I'll call the find method, which as a reminder finds the first match in the haystack and gives you back a match struct. And I'm going to pass in my log. So because this is in a iterator, this is going to repeat this logic of finding the pattern in the haystack for each one of my string entries right here. Now the beautiful part about this design is the filter map method expects the closure to return an option. As a reminder, if we return the some variant, we tell filter map to keep that element. And if we return the none variant, we tell filter map to exclude that current element. And then within the some variants associated data, we can also transform that data into whatever shape we want. Now the beautiful part about this is this method by design is going to give us exactly what we want. It's going to give us an option. Enum. Remember that's the way that the find method works. If there is a match to be found, if the search pattern does exist in the haystack, then it's going to give us the some variant with the associated data being the match struct. And if the regular expression search pattern does not exist in the haystack, then the find method will give us none. So this return value of the find method is a very convenient return value for the complete closure itself, because it's going to both filter the valid rows out and automatically transform them into an actual match struct. Right now. At this point, once again, we don't actually have our iterator being completed or fulfilled. So I'm going to call one final method here, which is going to be collect. And since I specified the type on the left hand side, you can see it's going to infer it automatically. Technically speaking, this is going to be a vector of match structs, and those match structs are coming from the regex crate. Let's take a look here. So I'm going to print out my results below. I'm going to say entries. And I'm going to provide my debug formatting and also my prettyprint. Since there's a lot going on here, let's see if we get what we want. So I'll begin and things are looking good. We have a vector of matches and each match right here is going to be per string. Right. The first match of this pattern in the respective string that we are iterating over. So if you take a look here at all of the strings that have timestamps in square brackets, it's going to be 12345678, 910, 11. We should have the exact same number in the output in terms of the matches one, two, three, four, five, six, seven, and then eight, nine, ten, 11. Perfect. And so the beautiful part here is simply through the use of regex, we have effectively eliminated these invalid error lines pretty effectively. Right? And the same principle is going to apply if, for example, we have a line like this which does have a timestamp but doesn't have the required API code at the end with the product and the id, this will automatically similarly be eliminated very elegantly, simply because it's not going to match the pattern that we're going to build up in just a moment to include all of the logic that we're searching for. So let's continue expanding on the regular expression logic here. After our closing square bracket, we're going to have another space right here. So I'm going to add a slash s after that. And then we're going to have one of several possible categories such as info or error or debug or warn. So in order to capture that either or logic I'm going to provide a pair of parentheses here. Remember that allows us to apply either or logic by adding a vertical pipe in between all of our possible options. This time around we have four possible options. So I'm going to write them out separating each of them with a pipe. So our first option is info with all of the characters in uppercase. That's for example represented right here. Then I'll add a vertical pipe and say the next possibility is error. Then another vertical pipe. The next possibility is debug, then another vertical pipe and the final possibility is worn. So once again this is going to match any one of these four directly after the whitespace, directly after the timestamp within the square brackets. So at this point we are right here or right here or right here. Our regular expression is pretty dynamic and can accommodate any one of these possible category matches. Afterwards we're going to have a colon, of course, which I can simply write in line, and then we're going to have a whole bunch of content. Let's do our colon. Let's say we have another slash s for a space. If we want to put it, then we have a whole bunch of content that we really don't care about, right? We want to basically go all the way up until we get to this section right here that we care about, because we want to pull out the endpoint. And the endpoint is going to be part of the section that includes the API information. In this case it's slash API slash orders. So orders is the endpoint. So the kind of logic I want to apply here will be something like just go all the way up until you encounter, let's say API. That's going to be a very elegant way that we can basically ignore whatever we have in the middle here, no matter what the characters are, no matter how many of them there are, we can basically skip over all of it until we get to API. So here's what I'm going to do here. I'm actually going to use the dot meta character because remember that indicates any character whatsoever. If I strictly go over something like digits, that's not going to cover everything, right? If I go for something like spaces, that's not going to cover everything. If I go for something like word characters, that's not going to cover everything we can see in this piece of text right here. There's both spaces and digits and dots and alphabetic characters. There's a lot. So what I'm going to do is just dot and then plus indicating one or more. So literally this is going to match any character whatsoever. And I'm going to allow that to greedily search forward and match everything up until the point that I get to slash API. So here I'm just going to add slash API. So this dot plus again is going to proceed starting from the space right here where the end of the space all the way up to the point that it sees slash API in the given string. And that's going to be exactly what we want. It doesn't matter whether that middle content is this or something like this. We're going to go all the way until we get to slash API. All right. So after slash API we are going to provide another slash forward slash. And this is where we have the actual product name or endpoint like orders or products. And that is going to be a series of alphabetic word characters. So to represent that, since I don't know exactly how many letters we're going to have, I'm going to do a word character with slash W and then a plus to indicate one or more. So this should handle orders. It should handle products users categories, etc.. All right. So after that after we get this noun effectively out of here, we're going to have another forward slash such as this one right here. And finally the last piece here of the puzzle is the sequence of multiple digits in a row. And there are as always varieties here. Here we have two digits. Here we have four. Here we have five. So as you guessed we're going to do slash D for digit and then gracefully handle any number of digits as long as there is at least one by adding a plus. And that should do it. So quite a lot going on here. But let's see if this complete regex string is effectively able to pull out all of the right content from the pieces of text. So I'm going to run our program again. And then we're going to do a quick check to make sure everything is as it looks. All right. So things are looking good so far. I want to confirm exactly how how many entries we have right here. You can even see here in our printed match structs, we're going to have the start byte and the end byte, which are going to be pretty large because we are basically providing the pattern that is matching the whole string. That's necessary because we're going to start segmenting those sections into their capture groups in just a moment. You can see right here we are effectively capturing everything we need. We can see the entire match right here, starting from our timestamp at the beginning, all the way to the end of the actual number right here at the very end. So 12, three, four, five matches this thing right here. Then here's our next match. Here's our next match of nine, nine, nine. We've already excluded this content right here. And my hope is that something like this will also be excluded simply because it does not have the API at the end, which is part of our search pattern. So if I scroll down, I find the value after 88. Let's see where it is. Right here you can see we have very nicely eliminated this a string right here and moved on to the next valid one which is 11111. So so far so good. We have successfully defined a working valid regex pattern. And now the final step of course is going to be subdividing this more complex larger search pattern into smaller capture groups that we can then give a name to tube because once we have names for those capture groups, we can extract the relevant pieces of data and then create the correct struct fields from them. All right. So this is mostly going to be modifying the existing string. I'm going to begin by targeting our timestamp. So our timestamp is going to be this complete collection right here. So after I escape my opening square bracket I have this whole sequence of digits right here before I get to my final closing square bracket, which I also escaped. So I'm going to provide a opening parenthesis right here before the start of my first four digit sequence, and then right here before I escape the closing square bracket, I'm going to provide my closing parenthesis. And that is a bare minimum to establish a capture group, but it doesn't yet have a name. So here after my opening parenthesis. But before the start of this very complex pattern, I'm going to provide my capture group syntax, which will consist of a question mark, then my pair of angle brackets, and then a name. I'm going to call this timestamp, and I then of course need to repeat this exact same capture group logic for the other three pieces of information. So here what's really nice about this second example is we have our four categories right here. And they are very elegantly wrapped in parentheses already because that was a necessity to establish the either or logic. And we don't need to declare any other pair of parentheses. We can just use this one right here and provide our capture group naming syntax directly before. So once again we'll provide a question mark. Then our angle brackets. And I'm going to call this category. All right. The third piece of information we need to collect is the endpoint. That's going to be the word that appears immediately after API. So I'm going to scroll to the right here. And that's going to be this chunk right here slash W for a word character plus for one or more. That's exactly what I want to target and capture. So I will wrap this section in parentheses again and once again at the very start before the slash w I'll provide a question mark. Then my angle brackets and I'm going to call this endpoint. And finally the last value is the actual product ID which is the collection of digits at the end. Once again I'm going to provide parentheses around the slash D+, which is that segment or group, and provide a capture group name by providing a question mark a pair of angle brackets. And let's call this something like ID. So now we have our capture groups. But the only problem is down below we're still using the find method which looks for a match. That's not going to be a big problem. All we can do here is update this method from find to captures, and that will create all of the capture groups immediately for us. And the beautiful part about this design once again is the captures method itself is also going to return an option, and once again it's going to give us exactly what we want. There's only going to be a collection of captures if all of them are met, right? If all of those patterns are found. And so we're going to get those captures in a capture struct in the some variant of an option enum if they exist. Right. And that's exactly what we want to do, because we're going to be able to still filter out strings like this and this and this and don't match the pattern. But then what we're going to package up in the some variant that captures method returns. And then we also return implicitly as part of the filter map closure. So now we're actually going to have a vector of capture objects. So I can continue to perform some additional logic here in the filter map. But I think what I'm going to do to transform this iterator of captures into an iterator of log entries, which is ultimately what I want, is I'm just going to add on one more map method call before I collect everything into my final vector. So once again, in this closure, I'm going to receive each capture at this point, right? The previous iterator gave me a new iterator of captures. You can see the inference take place here. Here is my capture struct that is going to be received per iteration, each one representing the capture struct holding the information of all of the captures specified here. So now what am I going to return out of this closure. It's going to be a complete log entry struct. So here I can even use a implicit return I'm going to specify I want to implicitly return a log entry struct. And I'm going to create it in line with a pair of curly braces. A log entry, of course, is going to need four fields, and those four fields conveniently match the four capture group names that we defined. So let's begin with timestamp. And I have my respective capture for an individual string. And as we saw in the captures lesson, the way we can extract a capture group by its name is going to be providing my square brackets here. And then the name which is going to be timestamp. And again, that name comes from us declaring that exact timestamp name right here as our capture group name. If we chose a different name right here for the capture group, that's totally fine. But then we have to refer to the exact same name right here. The other problem, of course, is that this does not give us a heap string, which is the type of data we have in our log entry timestamp field. So in order to convert this to an actual heap string, I can call two string. That's going to give me exactly what I want. And now all I have to do is repeat this exact same logic for my remaining three fields. Also, equivalently, my three remaining capture groups. So I need a category field. I can reach into my capture struct with square brackets, pull out a capture group by its name, which is going to be category, and convert it to a string with two string. Next field in line is endpoint. Once again I have each individual capture struct. This is going to run as many times as we have captures, which means as many times as. This pattern was effectively identified in however many strings here each time around, because I'm mapping from each one of those captures to a brand new log entry struct, capture will be something different. But when I access something like endpoint here, it's going to give me the dynamic value represented by that capture group name for that given string. So I'm going to get the endpoint. Convert that to a string. And again all of these values are matching exactly what I typed here. So I'm just going to make sure I have no typos. Timestamp category endpoint ID. All right so there we go. And finally I'm going to provide my ID field. And that's going to reach into my category. Sorry not my category my capture struct I'm going to reach into the ID capture group name. Convert that to a string with two string. And then we're going to finally call collect. And now we're going to finally have a vector of log entries. All right. Because we have mapped or in other words converted from a iterator of captures to an iterator of log entries, then collected them in a final data structure of a vector. So now we are printing out all of the vectors contents in debug format pretty printed. Let's take a look at what this looks like, and let's compare the results with what the expectations were. I'm going to scroll up, close my assignment, and this is what we should be getting and this is what we are printing out. So starting from the top we have one, 2345 orders info the correct timestamp. That looks great. Error error. Debug debug. We have info right here for info orders. Here's products. We have error for products we have warn for warn after warn we have an info and info in an error and indeed down below. Here we have an info and info and an error and all of the IDs appear to be matching as it should be, as well as the endpoint value as well as the categories and time stamps. All right. So pretty complex pretty challenging assignment. Hope you had fun with this one. Hopefully it was a good opportunity to both review the regex concepts that we introduced, as well as perhaps introduce a few new syntactical options for building up these search patterns for text. With that, I will see you in the next one.
