# The Dot Metacharacter

The next character will introduce is a special one. It is the dot or the period, and a dot is one example of what's called a meta character. A meta character is a special character in a regular expression search pattern that means something special. So let me give you an example of the dot right here. If I put a dot right here, just a single dot, this is not literally going to look for a dot or period. This is a meta character. It means something special within the context of a regex string, even though we have no backslash. Simply the period by itself is a special character, and what the dot is going to match is any character whatsoever. So this simple regex search pattern, when we run this code you're going to see is actually going to match literally every single character within my text string. You can see starting all the way from zero, going one, two, three, four all the way sequentially down to the very end of the string. Right. So this is going to match any character whatsoever. So of course the next question you might have is, what's the point of this? Isn't this completely useless? Well, very much like with word boundaries in the previous lesson, where the power of this meta character comes from is when we combine it with something else. So for example, here I'm going to instead write the letter I followed by a dot. So what this means is find the sequence of characters that consist of, first up a lowercase letter I that one is mandated, and then any character whatsoever afterwards. So this match is always going to match two characters. The first one is always going to be I, but the second one can be dynamic. It can be anything we want. So you'll notice we're going to get the I that is going to come from is right here. And the second one is going to be IC, which is coming from the IC right here in Rich. So now our search patterns are becoming a lot more interesting and varied right. We don't even know what this is going to match. We don't care. We are writing a complex search pattern that's going to be specifically the letter I followed by anything else. Now that raises another interesting question, which is what if you do actually want to look for a character, right? What if I want to find, for example, this dot right here or this dot right here? So in order to indicate an actual dot, you need to provide a backslash before the dot. Kind of interesting because it's very similar to how we created all of our special search characters in the previous lesson. But a dot by itself means match any character, while a backslash dot specifically searches for the actual period character. So now we're going to get two matches here. One is going to be the dot right here at the end of this sentence. And the other one is right here. And again we can combine this with anything else. So I can say I specifically want to find the letter H followed by a dot. That's going to find that match once in the string right here H followed by the literal character of a period right. Although this is also going to match that. Right. Because it means h followed by any character whatsoever. And that's also going to be coincidentally, the only H followed by any other character anywhere in the string. Right? Just remember, dot means match anything and then slash dot means literally match the actual period within the search string.
