# The find and find_iter Methods

The regex crate documentation uses the terminology haystack to describe the content that we're searching for a match within. This comes from the old expression needle in a haystack, which is an English idiom or expression that means something that is difficult to find. So in this context, the needle is going to represent our regular expression, our search pattern for text. And we're going to be looking to find a match for that search pattern of text within the larger string, which is called the haystack. All right. A little bit of complexity there, especially when you're reading the documentation. But moving on. Earlier on in the course we talked a little bit about escape characters in rust, which are special characters that have special meaning in a string, for example, within a string, if you do slash n right here, remember that indicates a new line character, and that forces a line break when the string is printed. Now, when working with regex, we want to make sure to avoid these kinds of special characters because regex represents its own syntax, its own combination of characters that have symbolic significance. So when working with regular expressions, we're often going to use something called raw strings in rust. And a raw string is simply one that rust is going to interpret literally. So for example, if I create this raw string here, which is going to consist of me just adding the lowercase letter r before the definition of this string, you'll notice it's going to treat the slash n not as a special newline character, not as a special escape character, but rather quite literally as the symbol slash followed by the symbol n. And that's what we want. We want to avoid any potential conflicts or issues with the escape characters that rust has, like slash N like slash T, they mean something different in a regular string than they mean in regex, so we want to avoid any potential issues there, and raw strings will help us with that issue. So moving forward, we're going to be always creating our regular expressions with raw strings, beginning with that lowercase r before the double quotes. So with that said, let's reach into our regex crate, which we set up in the previous lesson, and let's pull in the regex struct right here. And what I'm going to do is I'm going to take that regex struct. And on it there is a new constructor function. And what that is going to accept is the actual search pattern that we want to define. And throughout this section we're going to explore a wide variety of ways that we can define really dynamic complex intricate search patterns. But for now let's start with something super simple. Let's start with a regular expression that represents two sequential characters in a row, specifically the characters U. So once again, I'm going to begin with a raw string, the lowercase r, then my regular string declaration, and then the lowercase characters you see, which are totally arbitrary. So in this scenario where we have no special characters like slashes or dots, a raw string technically isn't needed. This is literally just a string with the letters U, but it's good to get into the habit of adding that raw string syntax for later on. So what this is going to return is actually going to be a result because of the possibility of an invalid regular expression search pattern input. So I'm immediately going to call unwrap on that result and then assign it to a variable. I'm going to call it re for regular expression. And you can see what it is, is that instance of the regex struct. All right. And this you can think of as a reusable search pattern. It is defining a way of looking for content within some other string. And specifically the pattern is defined as the letters u and e in that sequence. Right. And also with that casing. All right. So let's take a look at applying this regex to look for content within a larger string. So right here I'll define a piece of text and I'll make it equal to the string my movie queue like so. And I want to use my regular expression to look for a match within this string. And of course the match is going to actually occur in two places. If I look for the characters Yui as my search pattern, there is one match right here and one match right here. All right, so the first option we have available on our regex struct, represented by re is a method called find. And to find we're going to pass the string that we are looking within. This is the haystack in colloquial terms that we introduced at the start of the lesson. So what I'm looking for my regular expression within what I'm looking to find a match within is going to be my text string from above. Now, what find is actually going to return to us is going to be an option, because there's the possibility that there is a match. In other words, the regular expression does indeed find what it's looking for in here. But there's also the possibility that there is no match, which is going to be represented by the option enums non variant. So here I'm going to provide my match keyword to react to both possible variants. And once again if I have the some variant that indicates that we were able to find a match. And so I'm going to extract that piece of information. I'm just going to call it data for now. And then provide my arm right here. And you can see data is actually going to be a special struct called match. It's called match because it represents an actual match right. It means the regex was successfully able to locate that search pattern within the text that you passed in to the find method. So for now I'm going to print line, and I'm just going to output that match struct in debug format. And then I'm also going to need to cover the non variant right here. And for that we can just print line something like no matches found like so. All right. And that should do it. So let's execute this program and see what we get. So we're going to get the sum variant because indeed you is found within Q. And it's going to find the first match using the find method even though there are two. And you can see this match struct is going to have a bunch of helpful fields that are going to tell us the byte positions at which the match was found. So here you can see the starting byte was byte ten, and the final byte where the match concluded was byte 12. Now, as a reminder, when you're dealing with English alphabetic characters, one character is equal to one byte. But that is not always going to be the case. So you have to be careful if you're working with characters from different languages or dealing with strings that contain things like emojis, because one visual emoji character might be represented as four bytes under the hood. So what we're getting here is going to be the actual byte index positions. So in the case of my movie queue we can actually count along here starting from byte zero. Because in this scenario we only have characters here. The lowercase letter m is at byte position zero. Then y is at one, the space is at two, and moving on this is three four, five, six, seven. These space right here is byte position eight. The q is byte position nine. And then the U which is where our search pattern matches begins at position ten. Then the E right here afterwards is byte position 11. And then it ends at position 12 which is the second U right here. So the end field right here is exclusive. That means we go up to that bytes position but we do not actually include it. Now as I mentioned in this scenario, one English alphabetic character is equal to one byte. That's not always going to be the case. So for example right here I'm going to add an emoji. I'm going to use function e on a mac. And there is going to be some complimentary option on a windows keyboard. You don't necessarily have to follow along. I'm going to look for a nice popcorn emoji right here. And you can see we're going to add just one visual character. But under the hood that is represented with four total bytes. So you can see that's going to push the start all the way to 15 for for the actual bytes of the emoji. And then we also added an additional space right here which is one byte as well. That's why this time around the start is five more than it was the last time around. Right now on this actual data match struct, there are a variety of different methods that are going to give you these pieces of information individually. So right here I'm going to add space for three dynamic pieces of content. And I'm going to take my data match struct. I'm going to call the start method. That is going to give me access to the value in this start field, which is the byte position where the match begins. There's also the end method, which will give us the byte position where the match ends. Once again, this is still the first match. And also there's going to be an As string method which will give us the actual match itself, which in this scenario is going to be the exact same content as we have in our regular expression. But later on, when we build more dynamic regular expressions like any digit, well then this will give you the actual concrete match within the string that you are searching for. Now, of course, the one problem with this is that we're only finding the very first match. Sometimes that's what you want, sometimes it isn't. There is a complementary method on our regex struct that's called find iter. What that is going to do is it's going to give you an iterator of all of the matches. The iterator is going to yield each match as a match struct. Now the cool thing here is if there are no matches, the iterator is simply going to give you nothing. It's going to be empty. And if there are multiple matches, it's going to elegantly account for all of those. So right here I can iterate over this with the for loop. So for each piece of data or for each match, in other words in this iterator right here and comment out this code for now. We're going to get a match each time there is a match, and I can once again go do a print line. This time around, there's actually no need to deal with the option enum, because this time around, if there are no matches, they're simply going to be an empty iterator. So the only way that the content here is going to run is if we already have a match, which basically represents this piece of data right here, right? There's no need to model the notion of absence, because absence is simply modeled by the existence of an empty iterator over which there is nothing to iterate over. Right? So this time around, I'm going to have once again a data match for each match within my original text string. So right here similarly, I can output. Let's do those same three pieces of information. Right here. We're going to invoke the start method, then the end method. And then we're going to do the as string method. And of course the final mistake I have to fix here is I didn't actually pass in the string that we are searching within. So very much like the find method below. We're going to pass in text and that should do it. So now we're going to find every match of the characters you of our search pattern. You see our regular expression within the text string. And for each match we're going to print out its content. And now you can see both matches appear. Here is the first match of you starting at byte position 15 and ending at 17. Here is the exact match which is the letters you. Once again, it's a bit more of a literal search. We're going to see how this can be dynamic later on. And here is our second match right here. The exact same search pattern you has matched a second time starting at byte position 17 ending at 19. All right. So that is a quick introduction to regular expressions. Hopefully the benefit of this technology makes sense. It allows us to define a search pattern for text. And then look for that text either once or multiple times within a larger string. We're going to see more use cases for this in the real world as we progress throughout the next lessons.
