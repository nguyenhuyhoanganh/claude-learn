# Anchors

The next topic we'll cover is anchors or anchor symbols. An anchor or anchor symbol is a special symbol within our regular expression that marks the beginning or end of a string. To anchor means to fix something firmly in place. And what we are fixing here is the position of either the beginning or end of a string. So if we, for example, want to search not just for a digit anywhere in the string, but specifically a digit at the beginning of a string or a digit at the end of the string. Specifically, these special anchor symbols can help us to accomplish that. So let's begin by replacing our search string right here, our haystack. And let's pretend we're dealing with an online URL, some kind of API endpoint on the web. Say it looks something like this. Slash users slash one slash posts slash 353. This might be the kind of endpoint that you see, for example, on a blog application. And let's say that I want to extract the post number right here. And of course, I can target that by searching for one or more digits in a row. Right. And I want to account for whatever length this is going to be. But I also want to make sure that as I search for my digits, I don't include this one right here. This one is just a random digit that applies to users. I want the post number right here. Well, the lucky news for us is that the post number is specifically located at the very end of the string, so we can anchor a regular expression to say look for one or more digits, but specifically only at the end of the string, not just anywhere. So let's update our search pattern right here. I'll clear anything and everything between my double quotes. So as always, I'm going to begin with slash D for digits. And then I'll do plus for at least one of one or more. So you'll notice right here if I run this we're going to get two matches. One for the one right here after users. And then this is the actual match I want which is 353. And if I only want to match this one, I need to make my regular expression search pattern a little bit more precise, right. So here's what I'm going to do. After the slash D plus I'm going to add a special anchor symbol. This is another very special character, just like a dot that has significance in the context of this declaration. And it's going to be a dollar sign. So the dollar sign literally means the end of the string. So the way to read this is one or more digits in sequence, specifically at the end of the string. And that's why the dollar sign is important to place afterwards, because it sort of represents the conclusion or termination of the string you're searching within. And we're looking for our digits before that, immediately before that. So notice this is going to work. And now we're only going to have one match. And it's exactly what we're looking for the 353 at the very end of the string. So the next compliment that I want to show you, the other anchor is the one to mark the beginning of the string or the start. So once again, let's alter our text. Let's say we have something different. Maybe an API endpoint that looks like this slash v3 slash v2 slash prices V9. And this time around, say that I don't just want to pull out the three right here. Say that I actually want to pull out the full starting expression right here slash v3. I want to know the exact version number of our API. How can I write a search pattern that's going to capture just this v3 and not include something like v2 or v9? Well, I can begin with an anchor at the start of the string to say from the beginning of the string, look for a slash, then look for a V and then any number of digits in sequence, because this could be v3 or it could be V1 tomorrow, and so on. So once again right here let's clear our string. We know that we're going to begin with a forward slash as well as a V. So this is not a special symbol. This is just literally looking for the characters slash and then V. And then of course I want to add my dynamic search for digits. So right here I'm going to do slash D and once again put a plus to indicate at least one of. So once again this is totally fine. But this is going to match all three of the slash V's within our text string right here in v3, v2 and v9. So if I want it to be more precise and specifically target this thing, I can add an anchor right here to search explicitly at the beginning of the string to anchor this content. The search pattern to the start of the target string to the start of the haystack. So the dollar sign indicates the end and we placed it at the end. The complementary symbol for the beginning is going to be this carrot symbol, which is shift plus six on your keyboard. So this indicates the beginning of the string. And then at the very beginning of the string look for slash v plus one or more digits in a row. So you can see right here we're going to exactly get slash v3. That's what we want. And if tomorrow we have an updated API that looks like slash v ten, you can see the logic is similarly going to extract just that v ten. Just the content at the very beginning following this search pattern. So hopefully the syntax and the benefit of anchors make sense. They allow us to mark the beginning or end of the string. We use the carrot for the beginning and we use the dollar sign for the end.
