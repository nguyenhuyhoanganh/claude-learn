# Or Logic

Within our search pattern we can apply or logic. In other words, either this character or this character. We do so inside a pair of parentheses and then inside the parentheses. We use a vertical pipe to separate the possibilities. So for example, here is our code from the previous lesson. And currently we're looking for three digits in a row. And then at least one whitespace say that we want a search pattern that is dynamic enough to search for either this amount of whitespace one or more, or the presence of a single dash. So in other words, I want to account for a phone number that is stored like this, but also one that may come in like this with a single dash and have a regex that accommodates both. That is totally doable. So let me return back to the multi space example. The first thing I'm going to do is I'm going to take my declaration for the whitespace by itself. And I'm going to begin by wrapping it inside parentheses. So right here at the very beginning and then right here at the very end. And make sure you add that after the plus because the plus very critically applies to the preceding symbol which is the slash s for whitespace. So by itself this is not going to change anything. I can run the code. We're going to get the exact same result. But now within the parentheses we can use vertical pipes to provide or logic. In fact I believe we can apply more than one vertical pipe. Let's just begin with one though. So after my first symbol, so to speak, which is my slash s plus, I'll apply a vertical pipe which is the Or logic and then provide the other search symbol which for us will be a single dash. Although you are welcome to mix and match, for example more than one whitespace or more than one dash, right? It's however you want to design the pattern. So now the way this pattern reads is three digits in a row exactly, followed by either one or more white space in a row or a single dash. That's the way this reads. And that, of course, is still in between the three digit clump followed by the next three digit clump. So a lot of complexity here. So currently we have multiple white spaces here. So this will work exactly as it did a moment ago and in the previous lesson. But now notice that if I change this to be a dash and this only applies to the space right here, not the space right here, this regex expression will continue to work. It's still going to continue to work because our dash is still accommodating the pattern, right? It's three digits, followed by either one or more whitespace in a row or a single dash in a row. So the pattern is matching on this dash, as well as the rest of the logic right here. And everything is continuing to work. So to repeat you need a pair of parentheses to declare the Or logic. And then you separate the different pieces the either or segments that you want to cover, separating each of them with a pipe in between them.
