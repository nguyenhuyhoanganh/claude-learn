# Randomizing Vector Elements with shuffle Method

Sometimes we may need randomness in other aspects of our code. For example, let's say that we wanted to randomize the order of the elements within a vector. We want to shuffle them up so they are in a random order. The rand crate can assist us with that. So here I'm going to keep my random number generator from the previous lesson and declare a vector here, which I will call candies. And for now this can just store string slices in here. Let's do a couple values. Sour Patch Kids my favorite. Let's do KitKat. Let's do Twix, let's do Snickers and then Starburst. So first things first, we are going to be modifying the order of these elements within my vector. So that is going to mutate the underlying vector. So I need to make candies immutable as well. And then in order to invoke the method that I want which is actually called shuffle, the only way that method is going to be available is if I bring in a special trait, another trait from the rand crate called slice random. So right here I'm going to do use Rand. Then I'm going to go into its seq module which is short for sequence. And then in there we have a trait called slice random. And that is a trait that deals with randomization especially when dealing with slice types. So here we have shuffle and two shuffle. We're going to pass a mutable reference to a random number generator. So right here I'm going to provide a mutable reference to my RNG above. That still must be made mutable because some part of its state internally will be modified. And this is going to randomize or shuffle the order of elements within my vector. So right here I can output my candy's vector after the operation. And you can see the order of elements within. It is going to be totally randomized compared to where it started from. Each time I run this we should get a different order. And that is the shuffle method.
