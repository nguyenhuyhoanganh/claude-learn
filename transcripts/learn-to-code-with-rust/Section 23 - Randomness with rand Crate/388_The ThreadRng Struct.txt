# The ThreadRng Struct

Behind the scenes. The random function from the previous lesson relies on a struct called thread RNG, which is short for random number generator. Like its name suggests, a random number generator or an RNG is a program that produces a sequence of random numbers more than one. So let's bring in the RNG function from the rand crate. I'll replace random here with rng, and within main we can invoke the RNG function to create this thread rng struct. Here I'll assign it to a variable called my rng. The random function from the previous lesson will create a new random number generator. This thing right here each time the random function is invoked. So if you want to generate multiple numbers in sequence, for example, in a loop, using a single consistent random number generator tends to be more efficient than Recalling the random function each time and thus regenerating a new generator each time. The random number generator supports a method called random, very much like the random function from the previous lesson, but this one is a method on the generator. So right here let's declare a variable like random float, and I will take my random number generator from above. Call the random method on that, and very similarly, I need to provide a type annotation so that Russ knows the type of random value to produce. So here I'll provide my turbo operator and designate this to be a float. And there's going to be a few more compiler violations that we need to fix. The first is in order to invoke this random method, we need to bring in a special trait into scope. I'll add it right here and it is called RNG. A little bit confusing because we have the lowercase RNG right here, which creates the actual random number generator. And then we have the RNG with a capital R, and that is the trait itself. Remember, if we invoke a method that comes from a type implementing a specific trait, random is a method that is available from the thread RNG. Implementing the RNG trait. We need to make sure we have the trait in scope in order to enable the invocation of that method. The second thing we need to do is we also need to make the generator instance mutable. So right here I'll add my mute keyword and that should make the violations go away. The reason we need to do this is because the random number generator is keeping track of some kind of internal state. Whatever state is keeping track of the randomness and maybe figuring out the algorithm, or adjusting it, or keeping track of what's already been delivered. There are a lot of complexity to randomness that is sort of abstracted away from us, but somewhere internally in that thread RNG struct, it's mutating or changing some kind of state. Every time we invoke a method on the generator to produce a random value, and thus to enable the mutation of that internal thread RNG state. We need to make sure that we are making it mutable. So now we have everything we need right here. We can invoke printline and pass in our random float created from the generator itself. And now you can see we're going to get a valid value. Now here are where the benefits in efficiency come in. Let's use the same random method to generate ten random values and collect them within a vector. So I'm going to begin by defining a range right here. Let me begin with my range syntax which is a parenthesis. And as a reminder we can define a range type by providing a lower bound a starting point like zero and then two dots, and then an upper bound of where we're ending, which can be something like ten. So this range type represents a sequence of continuous values. In this case the sequence of numbers from zero up to ten. Now this range type is already an iterator. So we can immediately invoke the map adapter method to transform this iterator into another iterator and that accepts a closure. What the closure is going to receive as a closure parameter is each continuous number from zero up to ten, but we don't actually need that value. We just want to perform this logic right here ten times the generation of a random number from our random number generator ten times. So we can actually ignore the closure parameter here by providing an underscore. So now within the body of the closure, what I can do here is I can take my RNG random number generator, call the random method on it, provide my turbo operator, and designate some kind of random value, say a random i8. And so now I have my iterator right here. And finally we want to make sure that we collect the values. So right here I can call the collect method and aggregate them into something like a vector of i8 values. So starting with a range we're going to perform an operation ten times. That operation is going to be calling the random method on the exact same single my random number generator instance, getting that transformed adapter, that new iterator, and then finally collecting all of the values and actually performing that logic. And we're going to collect all of those values in a vector. Let's assign that to a variable like ten random values. And now right here we can output the value of that vector in debug format ten random values. And you can see we're going to get a vector of ten random values. And again the benefit of this is we are relying on the same thread RNG instance rather than creating a brand new one each time, which is what the random function would do behind the hood behind the scenes. So it's a little bit more efficient this way. The thread RNG random number generator has other methods that assist with specific random generation, so let me show you them right now here. For example, let me declare a random number variable set to an i32 And on my random number generator I have a random range method which actually accepts a range type, the exact same type we declared right here, and that allows us to produce a random number from a given sequence. So for example, if I pass in a range of 29 up to 53, this will return a random value between 29 and 53. So right here let me output random number. It's going to be an i32. We designated that here with our variable type annotation. Turbo might also work. And right here you can see we get 36. Right here we get 45 once again falling between that sequence or range. There's also a method called random bool which returns just the boolean either or true or false. And that random bool method right here on my range is actually going to accept a floating point argument that represents the percentage chance of getting true. For example, if I pass in 0.5, that means a 50% chance of getting true. And thus equivalently a 50% chance of getting a false. Let me pass this into my print line right here. I'll provide my interpolated content and do this. And we're going to get a 50% chance of a true and a 50% chance of a false. If I run this program twice, as you saw, we got a true and we got a false. Now I can bump this to something like 0.9, which means a 90% chance of getting it. True, 90% chance of getting a true 10% chance of getting a false. So you can immediately imagine the applications where this kind of design can be helpful. Something like card games or any games that involve luck. There's also a complementary method called random ratio. Let me just paste this below and update this to be random ratio. Random ratio similarly, will return a boolean with some percentage chance some probability. However, here we provide two arguments representing the numerator and denominator or the fractional chance of probability. So for example, passing in one and then two means one divided by two a 50% chance or better put a one inch two chance of getting a boolean of true. So right here you can see we're going to get a false. If I run it again we're going to get a false again. Looks like some bad luck for me. And we get a true again. The reason this can be beneficial is we can pass in some numbers that are just more difficult to express as pure simple floating point values. So for example, nine and 13, as my numerator and denominator means a nine inch 13 chance of some kind of luck, or I can pass in a 11 inch 23 chance of something happening, right? Once again, for games of luck, for card games, that kind of stuff. This kind of logic can be super helpful. All right, so that's just some examples of the helpful methods that are available on the thread RNG random number generator struct. And again it's really helpful sometimes to create a single instance, especially when you're going to be producing a sequence of multiple random values, simply because it tends to be a bit more efficient than creating a new one each time.
