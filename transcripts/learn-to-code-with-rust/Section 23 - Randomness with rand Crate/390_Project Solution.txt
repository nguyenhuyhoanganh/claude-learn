# Project Solution

So let's walk through a sample solution to the coding challenge in the previous lesson. This time around, we were modeling a real world card deck consisting of 52 cards. So first up, we had to begin by defining a suit enum with the four possible suits clubs, spades, hearts, and diamonds. Let's begin with that. Up top I'll define my enum called suit. It will have the four variants of clubs, spades, hearts and diamonds. And we also had to derive the copy, clone and debug traits. I can do that all above on a single line. Hashtag square brackets. Derive. The traits I want are copy, clone and debug. That just allows us very easily to copy this suit instance as needed. Next up we also had to define another enum called rank for the following ranks. All of these are in a typical card deck. The special one here is Joker, which we'll come back to later. So here let's define my enum called rank. And this time around, I'm simply going to copy and paste most of this because it's going to be quite a bit of writing otherwise. And here I'll just adjust this to remove. And and there we have it. There are all of our card ranks. And above I can derive the same three traits which are going to be copy, clone and debug. And that should do it for this bit of the assignment. Next step we had to define a card struct consisting of two fields rank and suit. And they of course are going to be set to the two enums that we just declared. Here we have some special instructions, which is the suit field should be set to an option holding a suit in the sum variant rather than just a suit by itself. And the reason is because the Joker rank technically does not have a suit in a card deck, so it doesn't have that associated connection to a suit, and thus to reflect the possibility of absence, we're going to use our option enum. All right. So down below let's define our new struct and it's going to be called card. And card is going to have a suit. And as I mentioned that is going to be an option where the some variant is going to store a suit enum one of these. And then for our rank that is going to be one of the rank options above. And we can then immediately derive our debug trait implementation. And finally our fourth construct is going to be a struct with a single card's field set to a vector of these card structs. All right. So right here we'll define that struct called deck. It's going to have a single field on it called cards which will be a vector storing the card structs above 52 of them in a standardized deck. And right here I can again derive my debug trait implementation. Next up we have to define a new constructor function on the deck. This function is responsible for creating the deck and also making sure that it stores 52 card struct instances, one for each combination of the four possible suits and 13 main ranks. So four times 13 gives us 52. We are excluding the Joker rank for now. All right. We're also asked to create them, populate them into a vector, and then instantiate and return a deck instance with the cards. So right now everything is going to be in order. All right. So below our struct declaration for deck. Let's implement some methods. So right here deck. And we'll create a new constructor function that will return an instance of the deck. So I'll annotate that type with capital S self. And let's declare an array of all of the possible suits, and then an array of all the possible ranks. And then perform the iteration. So here I'll declare my suits array. And here it's going to be basically referring to every variant within my suit enum. So suit clubs suit spades suit hearts and suit diamonds. There we go. And down below I'll do the exact same thing for my ranks array. I'm going to break this up so it's a little easier to parse. So we're going to have a rank starting at two, three, four, five. This is a little bit verbose, but just the necessary parts of creating the implementation. So all the way through the end of the possible ranks here I am on nine. Pardon that rank ten is the next variant. Then we get to our jack. We have our queen. We have our king and finally our ace. So we're going to leave off the Joker for now. And there is our nice array of ranks. Next up, let's create an empty vector. We're going to populate that vector with 52 card struct instances as we perform a double iteration over suits and ranks. So here I'll create a mutable variable called cards. And here I can do vec new. That's totally fine to create an empty vector. Another option here is with capacity, and that allows us to just designate the size. At the very beginning, we know that this is going to have at least 52 cards when we create it. So rather than letting rust figure out, oh, I'm going to start with enough capacity for five, then expand to ten, then expand to 20. In situations like this, if you know the exact size from the beginning, you can help optimize memory a little bit by just putting it in here. So I know this vector should have the capacity to store at least 52 cards struct instances. I could annotate the type right here to indicate this will be a vector of cards, but once I perform the iteration, the rust analyzer extension and the compiler will be able to figure it out anyway. When I start pushing the actual card struct instances into the cards vector. So down below, let's begin with a for loop. For each suit in my suits array up above, I'm going to turn it into an iterator with into iter that will take ownership over the elements within the suits array. But that shouldn't be a problem because we derive the copy and clone traits above. So we're simply going to make copies of them, and it shouldn't be too much of a problem. We really don't care about what happens to these two arrays when we're done with our iteration. So here we're going to iterate over all four possible suits for each one of those four possible suits. I want to perform a full iteration over my ranks array as well. So right here for each rank in ranks into Iter doing the exact same iterator logic. And again, this logic is technically going to run four times, once for each suit. But it shouldn't be a problem because again, we're just going to be creating copies of all of those respective suit and rank enums. So right here we can of course create our card struct instance. We're going to need a suit. And since we're not dealing with jokers yet, we can just use the sum variant this time around and populate it with the given suit that we are iterating over. For the sum variant. We know we have some present data here for these suits, since we're dealing with the 13 or so valid values for our ranks, we are not yet incorporating the Joker. We're going to get to that in just a little bit. And then for rank, we can just use the shorthand syntax to assign whatever rank we're iterating over to the rank field of this newly created card. So that will give me 52 unique card instances. And then all that's left is actually taking this. And of course adding it to my vector. So I can take my mutable cards vector above, call the push method so that on every iteration I'll create a new card instance and push it into my cards vector. Then the last thing I need to do is actually return my instance and remember, what I'm returning here is not this card's vector, it is the full deck which is going to have the cards field holding the vector. So I will create that new deck instance with self and give it a single card's field, which can automatically be auto assigned through the shorthand syntax to this card's vector. Now populated with 52 card instances, and we're going to return that newly created deck out of this new constructor function. So now we get to the actual application of the randomness ideas introduced throughout the section in their application to the coding challenge. The first challenge here was define a shuffle method on the deck struct that randomizes the order of the cards, and then of course validate that our work works as expected in main. So this method is called shuffle. And that was a little bit of a hint. So here within my imple block for the deck struct let me create a new method called shuffle. This is going to change around the order of the cards in my cards field, which will mutate it as a field and thus mutate the deck struct instance as well. So here I will borrow a mutable reference to the decked struct deck struct. There we go. And of course here this is pretty simple if you knew the right method and the right solution. But we need to create a random number generator using the rand crate, and then pass a mutable reference to that generator to the shuffle method that's going to be available once we also import some required traits. So here let's begin by scrolling to the top. And I'm going to pull in all of the dependencies that I know I'm going to need. So first things first, I'm going to need my actual RNG function to create my random number generator. I'm also going to pull in my RNG trait, because I know there's going to be a lot of randomness related operations, and this is going to enable those kinds of additional methods to be available on the random number generator. And then I'm also going to pull in the complementary trait that we explored that specifically deals with shuffling a collection like a vector. And that is going to be reaching into random into the seq or a sequence submodule, and pulling in this random trait. That should be all of the requirements we need. So down below in the shuffle method on my deck, let's begin by creating my mutable random number generator. I'm going to call it my RNG and simply invoke the RNG function from the rand crate, and then we can take our deck, access the cards field, call, the shuffle method, and what we need to provide here is of course a mutable reference to a random number generator. So right here I'll provide a mutable reference to my RNG above. As a reminder, your random number generator must be made mutable, because inside it's going to be mutating some bit of its own state in order to keep track of the randomness. But that should do it. So now within the body of main, let's go ahead and create our new mutable deck, and I will do so by calling the new constructor function we defined. And we can quickly take a look at what this deck looks like. Right here I'm going to output deck and I need to apply the debug formatting here. And I'm also going to apply some pretty printing because there's going to be quite a lot of card instances to work with. So right here when I execute this code we're going to see here are the 52 cards within the deck. And you can see they are presented in their general order. You can see here we are on the nine of spades. And then here we have ten of spades jack of spades and so on because we are progressing in sequential order. This is coming here from the sequential iteration over the two arrays of suits and ranks. So now let's take our deck and call shuffle on it, and we should get a totally random order for our card deck. Thanks to the use of the Rand crate. So I'm going to run this again. And now of course, we're going to see a totally random order of all of the cards in the deck. Of course, you're going to get a totally different output here, and that's totally fine. I remember hearing back in middle school, elementary school that every single time a deck of cards is shuffled, it's highly probable that the order in which the cards are laid out or shuffled has never been seen before in human history, because the actual amount of possibilities that are possible when you shuffle a deck of cards is 52 factorial, 52 times, 51 times 50 times 49, and that number is astronomically large. So it's highly likely that the way that the cards are shuffled here on screen has never actually been seen before in the history of cards, which is one fun bit of trivia for you. All right, so that's looking good. And with that, we can move on to the next part of the challenge. Next up, we had to define an insert Joker's method on the deck, which should insert two Joker cards into the card deck at two random index positions. As a reminder, a Joker is going to have a nun suit. It does not have a suit like hearts or diamonds, it just has nun. And then of course, validate that we have added those two additional cards with the invocation of the method. All right. So here within my temple block for my deck I'm going to add my insert jokers method. It's also going to mutate the deck instance. So I will provide a mutable reference to the deck instance. And once again I'm going to begin by creating a mutable random number generator. So right here let mute my RNG. Invoke the rng function from the rand crate. So here's what we're going to do. In order to perform this addition, I'm going to create a range that's going to allow me to iterate twice. I'm going to perform two total iterations on every single iteration. I'm going to find a random index within my card deck. So some random position starting from zero to a maximum of 52. That's going to give me a valid index position somewhere within the card deck. I'm then going to create my Joker card instance, which is a regular card instance, and then I'm going to call the insert method on my vector specifically to insert that Joker card at that specified index, which will be randomly selected. So first things first, let's begin by creating our basic iteration that's going to proceed two times. One way I can do that is simply by iterating over a range. So right here I can declare my range with 0..2. This will start at a lower bound of zero and go up to but not including two. And if I combine this with a for loop like for my value in this range, this will perform two iterations, one for the value of zero and one more for the value of one. Now that actual value of 0 or 1 is insignificant to me, I don't actually need it in the context of this loop and what I'm about to do, I just need this range in order to iterate twice, so I can actually replace value right here with an underscore to tell the compiler I don't care about the actual number, I just need something to run twice. All right. So let's begin by declaring a variable called random index. And this will be the index at which I want to inject my new Joker card. So what I can do is I can take my RNG random number generator above. And as a reminder, we can pass the random range method a range, and that will specify a lower bound and an upper bound using a range from which the method will select a possible value. We saw, for example, in the lesson or two ago that if we did 23 as our lower bound and 47 as our upper bound, the method is going to return a value from that range from that interval. So here our interval or sequence from which we want to pull out a value is effectively going to be 0 to 52. Right now I could hard code 52 but I think a more elegant way to do that is to simply get the current length of the card's vector itself, because that's going to give us that value dynamically. So I'm going to start at an index of zero, and I'm going to go up to. And then to get my final index bound I'm going to access Self.cards, which is going to be my vector, and then take its length with Len, that method. Now as a reminder, this is going to give us 52 right now. So this is going to evaluate effectively to 0 to 52. Remember the upper bound here is exclusive. We are going up to it but not including it. So the actual possible values here could be anything from zero up to 51. But that's totally fine because the final index of the last index position within this vector is 51 anyway, because the index starts counting from zero rather than one. So this is going to give us exactly what we want. It's a random number between 0 and 52. More technically zero and 51 inclusive. So now let's create our actual Joker card. So right here Joker card, it's going to be a new card which I'll create in line. We need a suit here which will be none for Joker. And the hardcoded rank is going to be the rank enums Joker variant. There we go. And then to actually insert or inject our new card at a given index position within the cards vector, we can access our cards field for the vector called the insert method. The first argument here is the index where you are inserting. That's going to be random index from above zero 252. And it's going to run twice again. And then what do we insert at that random index is going to be the second argument to the insert method. That is going to be our newly created Joker card from above. And that should do it. All right. So now down below in main after we create our deck we can go ahead and insert our jokers right here. I can shuffle it and then I can take my deck and call insert jokers not insert jokes. Although that would be really funny. It's insert jokers. There we go. That was not intentional, but it is pretty funny that it happened accidentally. All right. So I'm going to run this. And as I scroll up we should see two instances of a card with a rank of joker. I'm going to scroll up a little bit. There it is. There is our first one. And there should be one more as I continue scrolling up. Of course, your order is going to be totally different, but there should be a total of two jokers within the deck. And there they are. All right, so now using a random index, thanks to the rand crate which generated that index, we were able to add two jokers at random positions throughout our card deck. The final method that we were asked to implement was delete random card. This deletion is based on probability, and the method should have a 65% chance of deleting some random card from the deck. We should target the random deleted card by a random index position. All right. So here once again let's define our method. It's going to be called delete random card. Once again, it will accept a mutable reference to the deck instance. Once again, I'm going to need a random number generator. So here I'll declare my my RNG variable. I will invoke the RNG function from the rand crate. And I also need something to help me create this probability to create this 65% chance of whether or not I should take some action. So as a reminder, we have on our random number generator a method called random bool. And that accepts as a float argument the percentage chance that this method returns a true. So if you want a 5050 chance of a true or a false, you just pass in 0.5. But we want slightly higher odds of getting a true. We want a 65% chance. So I'm going to pass in 0.65. And this method will return to us a boolean right. So I'm going to assign that to a variable like should delete card. And if should delete card gives us a true which it will have a 65% chance of being. Then, and only then, I'm going to delete my card. So right here I'm going to write if should delete card. And then once again we're going to follow very similar logic to what we have here. We're going to get a random index. So let random index we're going to ask our random number generator to choose from a range of values with the random range method starting from a lower bound of zero and going up to, but not including an upper bound that is equal to the length of the card's deck, which is going to be a length that is one greater than the final index position. So all of the possible index positions are captured here. And once I have that random index, the complementary method that I can call on my vector to remove an element based on its index position is called remove. And I'm going to pass random index to that method. And again this logic is only going to run if this is true. And this will only be true if this returns true. The random bool method. And that has a 65% chance of happening. All right. So obviously we need to test this in main. Invoke the delete random card method ten times. Hint don't write out the method ten times. Find a more creative way. And then we can print out the length of the cards vector. And if we perform this operation ten times with a 65% chance, obviously there should be ideally not guaranteed, but ideally there's going to be a reduction in the total length of the card deck, which indicates that we did successfully delete some cards from it. So right here let's go ahead and perform another iteration over a range. This is just another elegant way that I can perform some bit of logic ten times. So I'm going to create a range going from zero up to ten, which we'll do ten total iterations. I don't care about the actual sequential values here, like zero, one, two, etc. so I'm just going to provide the underscore again for each value in my ten iterations. I'm going to ignore it and then take my deck and call the delete random card method on it. And that should do it. Ten executions of the method. Each one is going to have a 65% chance of deleting a card. So right here I can take my deck and I can access its cards field and call the Len method to get the length of the deck. So we started with 52. We're going to add two jokers. So we're going to get 54. And then we're going to run this logic. And then we're going to see how many we have. So if it's anything less than 54 which is high probability then the delete random card method is working as expected. So right here you can see we have 48. If we run it again we might have a different number like 47. Exactly. To be expected because we are dealing with probabilities right here. But that should do it. All right. So that is how we can incorporate the rand crate for any kind of random related activities. You can immediately see the application in games of luck such as card games. Obviously, if you're building video games or anything like that, this is also going to be super helpful. The Rand Crate is the rust community standard for generating any kind of random value. So I hope you had fun. And with that I will see you in the next lesson.
