# The Drop Trait

There's one more detail I forgot to mention at the end of the previous lesson, and that is that this code right here will no longer compile, and that is because of the changes we made to the target associated type. In the previous lesson, we specified that the type of target is now generic you, which corresponds to, for example, the data type of the more data field. So if we are returning a immutable reference to more data here and here, we are allowing a mutable reference in order to overwrite the more data field of type u, type u has to match whatever is the value that we are trying to overwrite here. Because remember, this syntax right here is going to secretly use this method behind the scenes to gain a mutable reference. So in this instantiation of custom box, the very first value here is a float64. And as you can see right here, that will be the concrete type that the compiler will infer will fill in for generic type t. Then the second sequential argument is this hello string slice. So the compiler will infer that the type of. You right here is a string slice. And thus if the type of the target associated. Type is you, and that is inherited by the Deref trait, so that the return value is a mutable reference. Here that means that we are specifying a return value of a mutable reference to type u. Type u again is a string slice, so this flow will no longer suffice. And as soon as I change this to be a string slice, I can change this to something like goodbye. This is going to work just fine. Remember, this syntax is using the dereference operator to access a value in memory, and also override it through the use of the Deref mute method. So we're going to get a mutable reference to the more data field right here, which is of type U, and in this specific custom box of type string slice. And we're simply overwriting that more data field with the new value of goodbye. Also a string slice. And this code will compile. All right, so now we can move on to this lesson's content. A smart pointer will typically implement two traits. We've already seen. The first one which is deref. And that enables support for the dereference operator. The deref mute is an optional add on if you want to enable support for overriding using the dereference operator and the equal sign. But the second trait that is very commonly implemented for smart pointers is drop. Now we covered the drop trait in the traits section of the course, but as a quick review, the drop trait and its drop method customize what should happen when a value goes out of scope. So when the program is going to be cleaning up, for example, our custom box, we can intercept or hook into that moment and specify additional logic to run within the drop method. The program will automatically run the drop method when a value goes out of scope. We do not need to manually invoke it, and typically what you place in the drop method is additional cleanup operations. So some side effects that may not be directly related to your values in the program, but maybe relate to things that your program has created. So for example, maybe if your smart pointer wrote some files to the hard disk, the drop method would be a great place to delete those files to clean up some of that work. Maybe if your smart pointer established some kind of connection to a database which exists outside of the rust program, the drop method is a great place to terminate that connection. So whenever you have a complimentary cleanup operation outside the program and you want to hook into the right moment in time to perform that cleanup, that's the purpose of the drop trade. Right now the drop trade is included in the rust prelude, so we can just implement it automatically. So we can very quickly do a very basic example just to see how it works in this lesson. So right here I'm going to implement the drop trait for every type of custom box supporting any two generics T and u. So same syntax here impul for generic types t and u. We're not hard coding any specific types here. We're saying for any generic types T and u belonging to an eventual custom box, we're going to implement the drop trait for a custom box with those two generics T and u. And the only method that is required here is called drop. You can see it receives a mutable reference to the instance itself in case you want to perform some kind of modification. But keep in mind this instance is just about to go out of scope and be cleaned up entirely, so there's not much you can do here. This is sort of right at the end of the lifetime of the given value. But for example, here I can print line and say something like, I'm cleaning up related files on the hard drive. That's maybe something you want to do. Another example of something you might want to do is I'm terminating a network connection or a database connection just to make sure you perform that proper cleanup. Because just because your rust program ends doesn't mean that this part of memory has correctly cleaned up. Let's say this associated connection. Right. Or something that you want to make sure is wrapped up outside of the rust program whenever the rust program stops running. One more thing here I can add print line is just to say something as a reminder, like I'm removing the custom box from memory. So currently we can see down below in main we have a single custom box. So when it reaches the end of the scope, when the main function reaches its end, remember rest is going to automatically run the drop method. We do not have to manually invoke it. So you can see right here I'm cleaning up my related files on the hard drive. I'm terminating a network connection. All of these print lines are coming from the automatically invoked drop method. So if we implement the drop trait, it sort of gives a nice pathway for us to again intercept the moment when our smart pointer is about to go out of scope and be cleaned up and just perform some additional cleanup operations. So most smart pointers you're going to encounter are going to implement both the Deref trait and the complementary drop trait.
