# Project Solution

So let's walk through a sample solution to the coding challenge in the previous lesson. Our goal this time around was to build a text transformation program that transforms a string using a pipeline of one or more transformations. In this scenario, a pipeline just means a sequential sequence of steps, transforming a string and then passing along the result to the next transformation in line. That's all we mean by pipeline. You can think of it as literally a pipe that is passing along data from one step to the next to the next. The transformations should be applicable in any order, and we are asked to build two text transformations. The first one is a whitespace transformation that removes leading whitespace trailing whitespace at the end of the string, or clears the whitespace at both the start and end of the string. And then the second transformation is a case transformation that converts the string to either uppercase or lowercase. So the main function provides an example of the final code that we want. Here we have a few sample inputs and they're all just basic text strings on the heap. And then here is our pipeline which as you can see is a vector storing trait objects. A trait object is simply a instance of some type that promises to implement a specific trait. So this vector actually consists of boxes storing different structs. Whitespace transformer is a struct that we're going to build, and case transformer is a totally different struct instance. But because we packaged them up within boxes, we are able to have a vector of these totally different values. And all of these structs are going to implement the common text transformer trait, which we're also going to build out in this lesson. So this is the sequential pipeline or sequence of steps that we want to apply to a given string. So for example if we start with Homer Simpson, you can see there's both whitespace and a lowercase string. To start, the pipeline here says please remove the whitespace at both the start and end of the string, and then uppercase the next string that you get. So first we're going to remove the whitespace to get just Homer Simpson by itself. And then we want to uppercase it. So the final produced value when we pass our original string and the pipeline vector into this apply transformations function should be this text of Homer Simpson. This pipeline, in other words, specifies the sequence of transformations to apply to this thing in order first this step, then this step in order to arrive at Homer Simpson. Now, specifically, when dealing with the whitespace transformation, we're asked to deal with two specific error scenarios. I'm going to scroll down here and find where that is right here. So this transform method, which will be a requirement of the text transformer trait that we're going to define, also asks us to handle two errors. The first one is if the string contains a pizza emoji, which was just a funny arbitrary decision I made. And the second is if the string is completely empty after the whitespace has been removed. Now, if we have a failure at any point of our pipeline, we don't want to fail the complete pipeline. We simply want to take the string at the beginning of that step, essentially skip that step and print out its error, but still pass along the string to the next transformation in the pipeline. So we can see an example right here of data with a pizza emoji. So at the very first step here, this whitespace transformation we do violate this rule. We do have this error where the string contains a pizza emoji. Therefore we do not want to perform this transformation. We are thus not going to remove the whitespace. We're going to print out an error at this point, but we're still going to pass along the string as it currently exists right here into the next transformation in the pipeline, which is the uppercase transformation. And thus the final result here should be an error message that prints this. And this is from the first step in the pipeline where we had a pizza emoji. But the final output should be an uppercase version of the original string, because again, we have not removed the whitespace because this step failed due to the presence of a pizza emoji. But then this step succeeded just fine and converted the string to uppercase. Here's another example of a string that should trigger an error. And this is going to be this second one in line. If the string is completely empty after whitespace has been removed. So in this case we're going to strip all the whitespace and end up with an empty string. So once again we should print out the content of an error message describing. In this case, the string has nothing left in it. And because that is a failure, we don't actually want to perform the whitespace transformation. We just want to take the string as it arrived at the current step, which currently has about four spaces in it, and pass it along to the next transformation, in this case, a string with four spaces being passed into a uppercase transformation still technically works. Nothing will fail, but there's nothing to transform it to uppercase. So at the end of the day, we should get back an output of a string with four spaces, right? And then still have the error message print from the very first whitespace transformation in the pipeline. So we have a few things to define here. We're going to have our text transformer trait which is going to enable the trait objects. Eventually we're going to then implement the two structs right here that are going to implement the text transformer trait, and thus allow themselves to be packaged as trait objects within this vector. And then finally we're going to define this applied transformations function, which is going to accept the text string as well as the pipeline vector of transformations and return back the output the transform text. That should look like these examples. All right. So with that we can go ahead and get started. So let me scroll to the top here. And we're going to begin with this text transformer trait. It should require a single transform method. And the method will accept an immutable reference to the instance. There's going to be multiple types of struct that implement this trait, and the method should also accept a string slice, and it should return a result where the success data will be the modified transformed string, and eventually each one of these structs implementing this trait will produce that next string to pass along to the next step in the pipeline. But we also have to account for the possibility of failure, and thus we need a result enum. And there the error variant. The error data, in other words, is going to be a dynamic error or trait object. So quite a bit going on here. But this is what it's going to look like. We're going to begin with our basic trait definition. So we have a trait called the text transformer. And we need it to have a single method. And that method is going to be called transform. And as the instructions tell us, we're going to need to accept an immutable reference to the instance which can be a variety of different structs. And then we're going to need a text string. And I'm going to model this as a shrink slice, and this is going to always return regardless of the actual concrete transformation itself. This should always return a result where the success data is going to be a string. So the concrete type filling in for the generic that's going to be stored in the okay variance associated data is going to be a heap string. And this is the critical part where we enable trait objects. The error data has the potential to be dynamic because there's going to be many different types of errors that we can encounter. We can encounter the issue of having a pizza emoji or having an empty string. So in order to enable trait objects, we need to be sure that we don't hardcode a specific single concrete error type here, but rather leave room for a variety of different dynamic errors. So what I'm going to do here is make the type of the error variant a box. And as a reminder, the box needs its own generic to be filled in. And here this box is going to be storing dynamic trait objects. And each trait object is going to implement a shared trait, and that is going to be the uniform error trait found within the Rust standard library. All of Rust's built in error types are going to implement the error trait, and so will our own custom errors that we're going to define in just a moment. And thus, if we do it this way, then any struct that implements the text transformer trait and defines a transform method will be able to return a box within the error variant, storing any kind of trait, object or trait instance whatsoever, as long as it implements the error trait. So here at the very top, I'm going to bring in from the standard library's error submodule the capital E error trait. And I'm going to say this is going to be a box that's going to store dynamic values that are all going to implement the error trait. And make sure to end this with a semicolon. And that's all there is to it. So as a quick reminder if you want to support trait objects, in other words, if you want this box to be able to store any kind of value within it as long as it implements a given trait. In this case, error. It is incredibly important and critical that you place the dying keyword here. That is what tells the compiler that the box is going to be storing trait objects. So now that we've finished defining the text transformer trait, we can move on to defining the very first struct that's going to implement that trait. And that's going to be this whitespace transformer struct that will store start and end fields both set to booleans. And these fields will dictate whether whitespace should be removed from the start, the end or both ends of the string. Once we define that struct, we're going to implement this very trait of text transformer for whitespace transformer. The transform method should remove the whitespace from the appropriate ends of the string, and then return a result with the modified string. And the reason again it's going to be a result. Number one is because the transform trait requirements are such. But secondly, because in the case that the transformation fails for any one of these two reasons, we want to instead return an error variant and each one of these two is going to force us to create a custom error struct as well. For both of these errors, define a new error type that implements the error trait in the transfer method. Check for both of the two scenarios and return the appropriate error type. And you'll need to figure out a way to support different types of errors being returned in the error variant. We sort of automatically took care of this simply by defining this to be a box storing trait objects. All right. So let's go ahead here. We're going to define our struct called whitespace transformer. And it's going to have two fields start and end. Both of which are going to be booleans because those are going to be pieces of state that determine where we're going to remove the whitespace from. And then I'm going to implement my text transformer trait that has been defined above for my whitespace transformer struct. And again the reason I'm going to be implementing this shared trait for all of these structs is so that they can then be interchangeably placed within my pipeline vector. My pipeline vector is going to store a vector of trait objects, which means all of those objects need to implement a shared trait, which is going to be text transformer. So right here, I can use my inference right here to write the beginning of the transfer method from the definition above. Press enter to autocomplete and we need something that's going to honor this signature. So the very first thing I want to handle is this possibility right here. If the string contains a pizza emoji we immediately want to create an error. So right here I can create an if statement. And I'm going to receive my text, which should be my current string or the string that I've either received at the very beginning of all the transformations or from the previous transformation, because these should be applicable in any order whatsoever. And then on my string slice I have a method called contains, which will return a boolean. If the text contains a given substring. And all I'm going to do here is a simple string containing the pizza emoji. So I'm going to access my emoji keyboard, which for me on a mac is going to be the function key followed by E. I'm going to select this pizza right here. Otherwise you can search for it. There we go. And if I contain a pizza emoji in my text string I want to return early. And we know we're going to need to return something that satisfies the return type of being result. It's going to be the error variant. And we know that our error needs to store a box. So I will create a new box right here with box new. And then this box needs some kind of error type that of course implements the error trait. We don't have that yet. So that's going to be the next thing that we're going to define. So right here before I finish this up I'm going to declare another struct right here. And I'm literally going to call it pizza emoji in string. This name is entirely up to us, but it's going to be the struct that is going to model that error. It's not going to have any additional pieces of state. Now keep in mind, in order to implement the error trait, we also need to implement the debug and display traits because the error trait is a substrate of those. So immediately I will derive a debug implementation here and then very quickly implement the display trait. So at the very top of my file I can reach into my formatting submodule and pull in the display trait down below. I can go ahead and implement the display trait for my pizza emoji in string struct. That requires a single method called fmt, which is short for format. And once again, I just hate single variable parameter names. I don't know where I got that hatred from, but I'm going to honor it. So I'm going to change this to formatter. And as a reminder here, the simplest way to get this accomplished is to use the built in right macro pass in the formatter parameter to here, and then provide a string with the contents of the display version of the struct, which will also serve as the error message when the error struct is going to be output. So here I'm going to write something silly like hey, there's a pizza emoji in the text. So cheesy. And that should do it. And now that I've implemented both the debug and display traits, I can also implement the error trait for my pizza emoji in string struct. Don't forget the curly braces. That is the regular implementation syntax. And now I have a struct that implements the error trait, which means if we want to create an instance of the pizza emoji in string struct and place it inside the box, it's going to be satisfactory because it's going to honor the method signature. Right here we are going to return an error which satisfies the result type right here. That error is going to be storing an associated data piece, which is going to be a box that wraps some dynamic object that implements the error trait, which pizza, emoji and string does. So right here in order to create an instance of that struct with no data, I can simply write pizza emoji in string and that should do it. The only reason we're seeing a violation still is because we're not actually returning any. Okay. Variant from here. So now that I've taken care of the pizza issue, we can take care of the removal of the whitespace. And as a reminder, there are a variety of different methods on string slices in rust. To accomplish that, there is trim, which removes whitespace from both the beginning and end of a string. There is trim start, which removes it only from the beginning, and trim end, which removes it only from the end. And there's also the possibility that the user creates a whitespace transformer that has start and end both set to false, in which case they're basically asking for an operation that does nothing, but we still need to accommodate that. So right here, after I check for my pizza, I'm going to do something we haven't seen in a little while, but I'm actually going to assign the combination of an if and else if structure to a variable. And this is totally valid rust syntax. It's going to look something like this. I'm going to say create a variable called transformed. And on the right hand side of an equal sign. I'm going to use an if statement. And the first thing I want to check for is if both start and end are set to true. That means I want to remove whitespace from both the start and end of a string. So here I'm going to say if self-start accessing the start field, which is a boolean, and the end field. So if both of these are true, I'm going to execute this corresponding block and return some value that will be assigned to the transform variable in the scenario that this if block executes. So I'm going to take my text which is my parameter flowing in representing the current state of the string. I'm going to call the trim method on it, which is also going to return back a string slice. Then I'm going to add on an elsif and I want to handle the other possibilities. So there is the possibility that we have only these start fields set to true. If that's the case then this is going to evaluate to false because n is going to be false. So this is covering the scenario that only start is true. If that's the case I want to invoke the complimentary trim start method, which will remove whitespace from the beginning but not the end. Then I'm going to add another Elsif, and I'm going to check if only the end field is set to true. Again, if either start is set to true in this scenario, or end is set to true in this scenario. That's providing a true value, which will allow the contents of the corresponding blocks to execute. So if this evaluates to true, that means I want to take my text and call trim end on it, which is going to remove whitespace from the end but not the beginning. And finally, in the unlikely but possible scenario that both these start and end fields are set to false, this will be false. This will be false. Basically, what I want to do here is change nothing. So I'm simply going to provide text. So now in all four of these possible blocks we're always going to produce a string slice. Except we're going to trim or remove the corresponding whitespace as determined by the field setup right here. Now after we perform that transformation there is the second error that we have to account for, which is if the string is completely empty after whitespace has been removed, we want to return a different error wrapped inside a box wrapped inside an error variant. So on a string slice we can certainly check, for example, for the length of characters or the length in bytes. But there's a nice convenient method here called Isempty, which is basically going to tell us if the string contains no content, if it contains no characters. If that is the case, I want to return once again an error variant satisfying the result enum method signature. This is going to wrap a box, and this is going to store some kind of dynamic trait object that promises to implement the error trait. So just like I did with my pizza emoji in string, I'm going to declare a different struct to model this error right here. I'm going to scroll up here and declare it right here. Below this let's call this struct no content in string. I'm going to immediately derive the debug trait for it. I know I'm going to need that for the error rate in just a moment. Then I'm going to implement the display trait for no content in string. Same principle. It just needs an FMT method in here. Once again I'll change my parameter to formatter. Then here I'll use the right macro pass in the formatter as well as the string representation of my struct, which will also serve as the error message. I'm going to say something like the string has nothing left in it. Now that I have the debug and display traits implemented, I can go ahead and implement the error trait. That's the most critical component because that allows me to wrap it up in the box below. And this is going to be for the no content in string struct. There we go. And don't forget the curly braces. And we do not need a semicolon. And then if I scroll down now we have everything we need to create an instance of the no content string struct. There we have it. So now we have handled both of these scenarios. For both of these errors. Define a new error type that implements the error trait. And so if all of this succeeds we have our trim string. It's not empty because we're going to bypass this if statement check. So at the very end we can return the actual success data. That again is not just going to be the text string, it's going to be an okay variant wrapping it. And furthermore, what text is at this point is actually a string slice. So in order to honor the requirements of having a heap string, I'm going to take this string slice and convert it to a string by calling the two string method. And as soon as I do that, you can see all of the compiler violations go away. So that takes care of our first transformer. But we still have one more to define. If I scroll down here. First up, in order to create our new case transformer struct, we're going to need a complementary case enum. It just has our two possible case variants of uppercase and lowercase. So here I'll scroll down and I'll declare my enum. I'm going to call it case. We have two possible cases which are going to be uppercase and lowercase. And then we're going to define a complimentary transformation struct representing another way that we can change or mold this text. It's going to be called case transformer. So right here here is my case transformer struct. It's going to have a single field called case set to a case enum. And that is going to dictate the kind of text transformation that should occur. Very much like with our whitespace transformer struct above, we're going to implement the exact same text transformer trait for our case transformer struct. So right here we're going to import the text transformer trait that we defined first for our new struct of case transformer. And that requires a single method called transform. I'm going to let autocomplete do its magic. And this one's going to be a little bit simpler. So all we need to do is check for the kind of case we have. And depending on what it is, we need to take this text parameter and convert it to either the complimentary uppercase or lowercase string. So right here I'm simply going to match on self. This is going to be my immutable reference to the current case transformer struct. And I'm going to check what the case field here is storing. And by using a match statement that requires me to respond to both of these possible variants, uppercase and lowercase. And for each one of those two variants, I need to do something different to my text parameter flowing into here. So if I have the uppercase variant, let's start with that. Then what I want to do in this match arm is I want to take my text parameter. I want to call the two uppercase method on it to create a heap string that is in all uppercase. But this is not quite perfect because this is going to return a string, which in turn will return a string from the match statement, and that does not match the type of the method signature. Remember, it is a result where the okay variant is storing the resulting string. It's not the string by itself, so I have to make sure to wrap this in the result. Enums. Okay. Variant. And that should do it. And then I'm going to do the exact same complementary thing for the other variant inside my case enum which is lowercase. Once again, if that is the case, pardon the pun, I'm going to take the arm here and return an okay variant satisfying the result. And the okay variant needs to store a heap string. So I will take my text and convert it to lowercase using the two lowercase method. And that should do it. So we now have two different structs case transformer and whitespace transformer that both implement the shared text transformer trait. So all of these violations down below should be looking good. And the final thing we have left is to define this apply transformations function. That's going to accept a given string as well as again a vector of these structs and apply these transformations in order. You can see here the compiler is now more than happy with our code. Right here we are creating boxes. Each box is storing an instance of the two structs that we defined along with their corresponding field setup. We've declared our vector here to be one. Storing a box full of trait objects. So you can see here the dying keyword is being applied to our text transformer trait. And since both of these structs honor that trait by implementing it, this code is now totally fine. So now we can move on to the apply transformations function. So I will be honest, I think this is one of the biggest parts of this challenge, and that is making all these transformations work together within the body of this function. This requires a bit of knowledge of iterators, or at least that's the solution that I used. And so this is just one possibility for a working solution. So define and apply transformations function that will accept a string and a vector of trait objects that will implement the text transformer trait. Find a way to iterate over the vector, perform a transformation over the starting string, and then pass the resulting string to the next transformation in sequence. If we encounter an error, such as having a pizza emoji, gracefully handle the error by skipping the current transformation. Still printing the error, but skipping the current transformation and passing the current string to the next transformation in line. So here let's define my apply transformations function. And as we see down below, it's going to accept the string itself a heap string. And then this vector of trait objects. So right here I'll do a text parameter set to a string. And then I'm going to have my pipeline which again is going to be a vector. And more specifically a vector that is storing boxes. The box smart pointer is critical here because that is the thing that actually enables you to store trait objects. The box is going to be a wrapper around it. Again, the compiler is satisfied because the box is going to occupy a fixed, consistent, predictable size on the stack regardless of the concrete struct. In this case, the concrete transformer that's going to be storing within the box. The compiler has a predictable size to work with and thus it is satisfied. Now it's going to be a box of trait objects, which means we need the dine keyword, and then the trait that all the trait objects promise to implement, which will be text transformer. And then what this is going to produce as the final return value is actually going to be the actual transformed string on the heap. So I'm going to annotate string here. And now we have our function signature. All right. So the way that I chose to implement this was by using the fold method on an iterator. As a reminder what the fold method does is it takes a starting value and then it takes a closure. And each closure is going to receive the current element in the collection. In this case, the vector that is being iterated over. And you can build up a single value over the course of the entire iteration by taking the current element in the vector, doing something with it, and applying it to this accumulator value as you progress, and then producing the final accumulator value first up at the end of every closure, so that you pass it along to the next element in sequence, and then finally as the final return value of the complete fold method operation. So this is a pretty complex idea, but we did cover it in extensive detail in the iterator section of the course. So if you need a quick refresher, there is a long lesson there that'll talk you through it. I'm going to speed through this implementation a little bit just because it's mostly covering those familiar concepts. So I'll begin by taking my pipeline vector. I'm going to convert it into an iterator using the into iter method. I don't really care about references. I'm totally okay with transferring ownership. And then I'm going to call the fold method. Now as a reminder, the fold method takes two arguments. The first is the starting value of the accumulator. What are we starting with? We're going to start with the text string that is passed in as the parameter. That is going to be the very first string that we are starting with, to which we're going to apply a sequence of transformations. Then the second argument is going to be a closure. So two pipes and then the curly braces. And the closure is going to receive two parameters. The first is going to be accumulator which is going to be the value that we're going to be building up over the course of the iteration. On the very first iteration, it's going to be the text string right here, because we declared it as such. And on the second and subsequent iterations, it's going to be whatever the previous iteration returned from its closure. So the very first time it's text, the second time it's going to be whatever this block returns. So accumulator is going to be our built up value. And then we're also going to receive the current element that we are iterating over in the iterator, which will be each element in our vector which is going to be our transformer, which as we can see here is going to be that box storing some trait object, some value that promises to implement the text transformer struct. All right. So right here in the body of my closure I need to do a couple of things. I need to call the transfer method on my transformer? I don't know exactly what transformers I'm going to have. Obviously here I do here in this scenario, but tomorrow these could be in a reverse order. Or the day after that we can introduce a third transformer in any kind of order. Or maybe these are going to be dynamically determined and created within the vector at runtime based on a user command. So all we know is that we're going to be receiving some kind of transformer struct type. We can pretend we don't know exactly what it is, but the beautiful part is because we are dealing with trade objects, we know that every single value within the iterator implements the text transformer trait, and every kind of value that implements the text transformer trait is going to have a transform method. And that's what we're going to exploit to our advantage. So I'm going to take transformer. That can be either our whitespace transformer or our case transformer. And I know whatever it is it's going to support a transfer method. It's going to need to accept a piece of text. And as a reminder, what we defined here in the actual trade is it's going to receive an actual string slice as the actual text parameter. So we're going to be starting with a heap string right here. But in order to convert it into a string slice, I need to make sure that I provide an actual string slice. Now keep in mind what we want to pass in here is not going to be text, because that's going to be the exact same uniform parameter string that we are passing in right here. We want it to be the accumulator. More specifically, we want to coerce that string into a string slice. So I'm going to provide a reference to the accumulator which will convert the string to a ref str. So in the very first iteration we're going to pass in a ref string. That is going to be the starting value passed into the apply transformations function. But in every subsequent iteration, it's going to be a reference to whatever accumulator is, which should be a reference to the return value of the previous iteration of the previous closure. All right. So this transform method as defined by our trait is going to return a result enum, right? So right here I can use the match statement to react to that result enum. And that result enum is going to have two possible scenarios. The first is that the transformation succeeds. We're going to have the transform string stored as the associated data within our okay variant. That is the exact same thing that is going on right here and right here. And that's the exact same thing that's going on here. So if the transformation succeeds, we're going to have our transform value right here. You can call that new value. And if that's the case I want to directly return it out of my match statement, which will also serve as the implicit return value of the closure, which will thus pass along new value as the next value for accumulator to flow into the next closure, i.e. to flow into the next transformation inline. So we perform a transformation. In this case, for example, we perform the whitespace transformation. We take the resulting heap string and we return it out of the fold closure, and thus it flows into here as the next accumulator for the next transformation in line. Now the other scenario we have to handle, of course, is the error variant. And if we have an error, remember we define this type to be a box that's going to be wrapping a trait object that implements the error trait. So here I can just write error. And we can see here we're not going to know the exact error type. It is because there's actually a variety of possibilities right. We can have a built in error from rust if something goes wrong. On a technical level. We can also have our pizza emoji error or our no content and string error, and we don't know exactly which one it's going to be. We don't know how things are going to fail, but the good thing is we're going to have some type that implements the error trait. All error traits are guaranteed to be printable as text because they support the display and debug traits, and thus we're going to be able to gracefully print it out. So I can use print line here. As a reminder, there is a complimentary e print line macro which is perfect for printing out errors. So here I'm simply going to say something went wrong. And then I'm going to actually interpolate the value of error and get the text representation. And then say something like moving on to next transform. That's exactly what I want to do. Now keep in mind, I still want to move on to the next transformation line, which means I still need to pass on the string as it currently stands unmodified because the transformation failed to the next closure in line. So in this scenario, I don't have the transform value that's only going to be available in the okay variant, but I do have my accumulator here. This is the string as it flowed into the current closure and it's been unchanged. So I'm simply going to return it from my error variant. So in either scenario here, whether I get my new transform string, which is a string type, or if I return the current string that is flowing into this current closure, I'm still returning a consistent type of a string Ring from the match statement, which will thus serve as the consistent return value of this closure, which will thus flow into the next element in line's iteration as the accumulator, and then apply the next transformation in line. So in summary, this will gracefully handle an error by printing out the custom error message, and then returning the untransformed string to the next iteration in line to the next transformer, so that the remainder of the process can complete. And that's all there is to it. So the only thing that remains is running the program in main with the three sample string inputs, and confirming that we see the expected outputs as well as the error messages. So let's begin with the happy case, which is our string of Homer Simpson up above. I'm going to comment out these variable shadows of the text variable. So here is our string. It contains both whitespace as well as a all lowercase string. I'm going to pass it in to my apply transformations function. Here is my vector of transformations that asks to both remove whitespace from both the start and end, and then also to transform it into uppercase. There should be no errors encountered here, so the only output we should see is the actual string of Homer Simpson with no whitespace in all capital letters. And as we can see here, we have an unused lowercase variant, but that's totally fine. Here is our output of Homer Simpson. The next thing that we want to try out is the second possibility here, which is this pizza emoji. And there's also this complementary piece of data in here. So what should happen here is that when we are applying the very first transformation, which is the whitespace transformation right here, we want to fail right here because we have the pizza emoji. And thus we should not actually ever remove the whitespace from the string. Rather, we should print out the error message for the pizza emoji and string, which should be this content right here. Hey, there's a pizza emoji in the text, and then we should pass along the string with the whitespace at the beginning and end to the second transformation in line, which is the case transformer and still uppercase the data right here. So as you can see, the result should still have whitespace at the beginning and end, but we should have the letters in all capital. So right here I'll run it. And you can see we indeed have this. You can see that as well. If you want to confirm that you actually have the whitespace, I can do something like put the word start here and the word end here, just so we can very quickly see it appear after a bunch of whitespace. See right here is the whitespace and right here as well, which confirms that it is still being kept because that transformation failed. And same principle is going to apply to our third and final test input, which is going to be this text right here. This is a string that contains no content whatsoever. So after we perform the trimming of whitespace right here, we're going to now run into this issue, which is going to return a box with a totally different error struct. That's going to be the no content in string struct. So we expect to see this text message. The string has nothing left in it as the output. And then we are going to print that error message. Avoid the removal of the whitespace. Pass it along to the next transformer in line, which is the uppercase one. The uppercase one is not going to do anything because there are no characters here, but we should still see effectively an empty string right here that's technically been uppercase, simply because there was no error to be encountered. But we should reach the end of the pipeline gracefully. So here I'm going to run this. We're going to see here indeed start and end just to mark the beginning and end of our four characters of whitespace. There it is. So everything is working as expected. All right. And of course, if you want to practice some more, you're more than welcome to add a third or fourth text transformer with whatever arbitrary rules you want. If you want to define your own custom errors, that's all there is to cover in this lesson and in this section. So with that, I will see you in the next one.
