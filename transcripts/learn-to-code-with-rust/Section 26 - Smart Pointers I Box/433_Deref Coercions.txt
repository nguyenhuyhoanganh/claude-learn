# Deref Coercions

Deref coercion is a rust feature that simplifies working with references. When given a reference to a type that implements the Deref trait, rust will convert it into a reference of another type if necessary. Deref coercion occurs automatically when we pass a reference as an argument to, say, a function, but the function's parameter doesn't exactly specify or match that reference type. Rust is going to attempt to invoke a sequence of continuous Deref method calls to continually dereference until it hopefully arrives at the parameters expected type, and then the code will just work. The most common example of deref coercion that we've seen so far is the conversion of a ref string. This type right here to a ref str. This type right here. Now the string type is a smart pointer and it does indeed implement the Deref trait. So that is the functionality that enables this conversion to happen. When we pass a ref string into a function, we pass a reference to a type that implements the Deref trait. So if the function defines a ref str parameter, rust will continually invoke the Deref method on the type we pass in trying to get to a ref str. All right, let's take a look at this in action. So what I'm going to do is define a complementary function here I'm going to call it output text. And I'm going to define a text parameter. And it's going to be a string slice. So a ref str and all this function is going to do is it's going to print line and output the value of text. Notice that the type of the text parameter is a hard coded type of a ref str right a string slice. So of course if we try to instantiate a string right here in main, I'm going to call it text. And let's do a string here called hello. Obviously by itself, if I take output text and I pass in text by itself, that is invalid because we're passing in a string where a register is expected. But notice that if I change this to be a ref string, if I borrow a immutable reference to a string, I'm technically passing in this type. This type is not literally equal to this type right here, but yet you'll notice the code compiles just fine so we can pass in a ref string and this will be deref coerced to a ref. Sir, this is an example of this feature called Deref Coercion in action. Rust observes that we are passing in a ref string when the function expects a ref, sir, but the string type implements the Deref trait, and in addition that Deref trait implementation returns the actual string slice. Remember, in the previous lesson, we implemented the rough trade ourselves, and we saw that we could return a reference to something. So when the d-rev trait is implemented on the actual string type by whatever rust developer wrote the string type, it actually has a Deref method that returns this string slice that returns the exact right thing we need in order to make this function and its signature work. Despite the fact that we are starting with a different data type, we are starting with a different data type of a ref string, but we're able to get to or coerce a string slice a ref str by continually calling the Deref method on this thing until we get to a type that does suffice for the parameter. So I want to show you explicitly what is going on here behind the scenes. Whenever we pass in this ref string to output text, here is what happens here. I'm going to declare a variable called the slice and I'm going to take my text string and I'm going to call manually explicitly the Deref method on it. And this is supported because the string type is a smart pointer that implements the Deref trait. And thus this. If I save, it's going to give us a compilation right here. That's only because we're trying to use a method that is the result of a trade implementation. So as a reminder, as soon as we bring in this trait into scope, this for us is going to be the deref trait inside the standard library's op submodule. Once I bring in this, this is going to work. Notice that the Deref method on the string smart pointer is going to give us back a string slice. What's happening here? If I pass in for example, the slice into here, what's happening now across these two lines is exactly what's going on behind the scenes. In the previous example that we had, we're passing in a ref string. Rest calls the Deref method on that type on the original string to get back this string slice and then it passes that in. And now the type that is being passed in of a ref str matches the type of the text parameter. So the code compiles just fine. You can see this works as expected. In fact, rust will continuously perform deref coercion until it gets to the intended parameter type. Let's make this example even more interesting. Right here I'm going to get rid of slice. This was just for the sake of example. And then what I'm going to do is I'm going to put my string within a box right here. I'll declare a my box variable, I'll do box new and I'll bring my text into here. And as a reminder this will transfer ownership into the box. And then my box is the owner. And here what I'm going to feed in is going to be a reference to my box. So this is really interesting right. This example we can get rid of the Deref trait will work just fine. I can show you we're going to get the exact same text to flow. So what is happening here? What is happening here is actually several sequential deref coercions in place. So we're going to start with a reference to a box. The compiler says, hey, a reference to a box is not the same thing as a ref str. But can I deref coerce this by taking the original type of box and calling the Deref method on it? The answer is the box is a smart pointer that does support the Deref method, and that Deref method is going to get back a reference to the internal stored type, which for us is going to be a string. More specifically, we're going to get a ref string. Well, a ref string still doesn't match the intended parameter type of ref str, but the string smart pointer or the string type itself implements the Deref trait, and that Deref method that exists as a result of that implementation will return a ref. Sir. So in summary, rust is going to invoke the Deref method continuously Or in other words, continue performing coercion continuously until it gets a reference that matches the parameter of the function. This is actually a super beneficial feature of the language, because if rust didn't implement deref coercion, we would have to manually keep coercing these types until we arrived at the correct parameter type. We would have to manually keep writing the code just to show you an example of what the code would look like. Obviously, this code would no longer be valid since we are passing in a reference to a box, so we would have to get from a box to an actual ref manually so we could do something like this. This is an example directly from the rust book. Let's declare a variable called value and our goal is for value to be a ref string. So first things first I need to get to the string inside my box right which is a smart pointer. So I have to begin by dereferencing my box. That's going to give us a string. The content stored inside the box on the heap is a string. That's not good enough because we need to get to a string slice. Let's begin by turning this string right here into a string. So I'm going to wrap this in parentheses simply because I need the dereferencing to happen first. And then once I get that string, I'm going to take a reference to that content from inside the box. So now I have a href string. But remember in this scenario that deref coercion did not exist in the language. This would still not be good enough because we're trying to pass in. If we pass in value here, we're still trying to pass in a href string where a href store is expected. So our final step would have to be converting the href string into a href str. We could take a slice using this syntax right here the double square brackets, and then the double dots in here. As a reminder. What this does is it doesn't create a portion in the slice, but it creates a slice that is equal to the full content of the string, the full text. We covered this in the slice portion of the course. This is again a string slice that represents the whole entirety of the string, but its type is now going to be a href strip. And so now if we pass in value right here, we are indeed passing in a href str where a href str is expected. And so the code compiles. But again this would be the code that we would have to write if deref coercion did not exist in the language. But it does. And obviously it greatly simplifies the code that we have to write such that we can just pass in a plain old reference to any type. And as long as rust can continually deref coerce that type into the expected type, we don't need to manually perform all of those conversions ourselves, right? So we can just pass in a basic reference to my box, and you can see this code compiles and works the exact same way. So hopefully the basics and the benefits of deref coercion make sense. And with that I will see you in the next lesson.
