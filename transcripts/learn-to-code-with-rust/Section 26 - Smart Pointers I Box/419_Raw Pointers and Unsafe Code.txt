# Raw Pointers and Unsafe Code

So let's take a look at a simple use of raw pointers. Again, when we choose to create this kind of pointer, we opt out of the rest compiler's safety checks that we get with references. We generally want to avoid using raw pointers unless we absolutely need them for the program we are writing. As the rust book states, the reason why we have raw pointers is because, quote, the underlying computer hardware is inherently unsafe. And if rust didn't let you do these unsafe operations, you couldn't do certain tasks. So raw pointers are a necessity, but they also carry a risk in those situations where you need to use them. So just as a quick reminder, let's review what a regular reference looks like. I'll begin by creating a new string. I'll call it sushi, and I'll assign this to a string of the text yellowtail. And down below I'll create a sushi ref Variable, and I will take my reference right here with the borrow operator and the actual value that I am borrowing, which is my sushi string. So as a reminder, if we have immutable references, which is the case here, we can create as many of them as we want. So down below I can copy and paste this line. Call this sushi ref two. This is totally fine. We can take multiple immutable references and I can print them both out right here and right here. And this is a totally valid program. There are no violations. But then we start to see where the actual compiler and the safety checks come into play. So for example, if I make this a mutable owner or variable and here I borrow a mutable reference and here I borrow immutable reference, the compiler will prohibit the simultaneous existence of two mutable references at the same time, and that is to prevent situations where one mutable reference can change data that another reference does not expect a change. So if you have one mutable reference within your program, you cannot have a coexisting second reference, either immutable second reference or an immutable second reference. You can't have a second reference coexist at all as long as this sushi ref mutable reference is alive. So now let's take a look at the raw pointer equivalent. I'll remove my previous lines of code right here and I'll declare a variable. I'm going to call it sushi raw pointer one. And this is what the syntax is going to look like on the right hand side. I'm going to begin once again with the ampersand symbol. And then I'm going to add some special words. The first one is going to be raw which indicates a raw pointer. You can almost think of it like a bare pointer without any additional protections. And then after that I have to write the word const. And then the actual original owner, which is sushi. So this is creating a raw pointer. This is the equivalent to a pointer in C. So you can see this code is totally fine and this code will compile just fine so far. We're going to see where the compiler allows behavior that it otherwise wouldn't allow with regular references. In just a moment, I want to show you as well that we're going to have this inferred type right here, which is the asterisk const string. This is not to be confused with the dereference operator, which follows a address or a reference to the original value. This is just part of the types declaration, and you are welcome to use this declaration in your type annotation to declare the raw pointer as well. So for example, here as an alternate syntax, I can do sushi raw pointer two. And here I'll declare my type after my colon I'm going to do the asterisk the const and then string. And if I do this syntax right here this annotation, I can actually use the regular reference syntax on the right hand side, which is ampersand sushi. This is the regular borrow operator reference syntax, but specifically because we utilized this type annotation right here. Rust will force this regular reference to a raw pointer instead, so that is also a raw pointer. These two syntax options are equivalent. Both of these raw pointers are immutable, which means the exact same thing as it does with immutable references. This means we can follow the address within the raw pointer to get to the original value, which is this text of yellowtail right here, and we can read the value at the memory address that the raw pointer is storing, but we cannot actually mutate it. Now, if you wanted to create a mutable raw pointer, I'll do that. On the next line I'm going to write sushi raw mutable pointer. In contrast to the two immutable raw pointers above. And here you can see that syntax is going to be the reference symbol, the borrow operator, then the word raw once again. But instead of const, it's going to be the word mute for immutable, and then once again the original owner. So the reason these names exist as they do is because the constant here means immutability, right? It's a constant, fixed, unchanging, immutable raw pointer. This means the opposite. It is immutable raw pointer, which allows us to both follow the address to the original text value of yellowtail, and also mutated as well. There's going to be a lot less restrictions here when it comes to raw pointers. So for example, here I can create a second mutable raw pointer on the line below and call this mutable pointer two. We can, of course create two mutable references on two lines, one after each other. But as we saw a moment ago, we cannot have them coexist. We cannot use them both, for example, in a single print line call. When it comes to these raw, mutable pointers, we can have them coexist. We can have them both access the same piece of data and try to overwrite it at the same time. And it's precisely those kinds of operations that are potentially dangerous. And once again, with references, the rust compiler will prevent that kind of behavior, such as the coexistence of multiple mutable references. But here it will not. Now, it's important to note that the declaration of these raw pointers, either mutable or immutable, does not trigger by itself a compiler violation. You can see I can compile and run this program and everything is working just fine. So the creation of raw pointers is not the problem. The compiler is only going to refuse to compile or show a violation when we try to dereference the raw pointer. In other words, when we try to follow that address to the original value because that is where the danger lies, right? It is not the creation of the address by itself that is dangerous. It is the address potentially leading to data that has been deallocated, which is this yellowtail string right here. The dereference is the potentially unsafe operation now that we have raw pointers. So in order to dereference a raw pointer we're going to need to introduce a new keyword which is unsafe. The unsafe keyword precedes a block of code, and it allows the execution of operations that normally wouldn't be allowed by the compiler. So one example operation that falls into that category is dereferencing a raw pointer. So let me show you that right here. First things first I'm going to add a print line. And I'm going to try to print out the value of sushi raw pointer one. Now we know that if we were to do this with a reference the reference would be dereferenced. In other words, rust would follow that memory address to the original location and would print out the value there. So if we did this with a regular reference, it would print out the word yellowtail. Now you'll notice this code by itself will not compile, and that's simply because a raw pointer doesn't implement the display trait, which is what is used behind the scenes with these curly braces. Let's say we actually want to dereference this raw pointer. In other words, we want to tell rust. Follow the address to get back to the original string being stored on the heap, which is yellowtail, and print it out here because a string does in fact implement the display trait. So as a reminder, when you have any kind of address, whether it be a reference or a raw pointer, which in and of itself is very similar, right. They're both addresses. For that we use the special dereference operator symbol in front, which is the asterisk. We covered this before in the course. It is shift eight. Now notice when we do this our code is still not going to compile. It's going to say dereference a raw pointer is unsafe and requires an unsafe function or block. That's exactly what I was just talking about. It's an operation that by itself with code like this, the compiler will not allow. right? So what we have to do here is add a very special keyword. It's going to be unsafe all lowercase u and s a f e. And then provide a block and into the block. We're going to drag this code. Now it's incredibly important to note because this can be a little bit misleading, is we are not saying that this code has to be unsafe or that it is unsafe. Rather, the unsafe block is our explicit acknowledgement that there is the possibility or the potential for unsafe code in here. We are assuming full responsibility for our actions. You can think of it like the compiler saying, hey, you were trying to do something that is potentially dangerous. You may be trying to access data that no longer exists, which can lead to bugs, which can lead to memory issues, corruption, etc.. Before I allow you to proceed with this action, I want you to acknowledge what you are doing and tell me that you are aware that what you are doing is potentially unsafe. So this doesn't change anything that's going to happen in here. It just basically allows the compiler to breathe a sigh of relief and make us give the explicit acknowledgement that we know that this has the potential to be unsafe. Now, in this scenario, when we access the location where yellowtail lives, this is not actually unsafe because the data indeed does exist, right? When we access the raw pointers value, when we dereference the address at that point the sushi string is still alive. It won't go out of scope until here. It should be in that location in memory. It should still be stored at the address that sushi raw pointer one is storing itself. So when I run this, everything is going to work just fine. We're going to get yellowtail here. Now here's an example of where we can actually create a program with a serious error where we do have a dangling pointer. In other words, an address that is referring to data that may be potentially cleaned up. Now I want to again reiterate the word potentially here. And that's because the way that the rust program is going to interface with your operating system and with the computer's hardware can significantly vary. So this is often called undefined behavior, because there's no concrete rule on what exactly is supposed to happen here. The program may crash, data may become corrupt, or the program may work, or it may work, but inconsistently. The point is that there is a flaw in this design precisely because of the unreliability of it. So here's what we're going to do right here. After the declaration of all of my raw pointers, I'm going to call the drop function, which, as you may recall, allows us to pass in an owner. In this case, I'm going to pass in sushi. And remember what that does is it should deallocate the data right then and there. Right now how that's happening under the hood with the hardware and the operating system and the memory, that may not necessarily be happening right here. It may be happening at the exact same moment as this is running or something that is totally different each time the program runs. The point is, from reading this code top to bottom, we know that this is the symbolic point where we are dropping the yellowtail string so that text should be being cleared from memory, and thus sushi raw pointer one is pointing to a location in memory where yellowtail used to be, where it may no longer be right. It may certainly potentially be deallocated at this point. This is actually unsafe code this time around, right? It's unpredictable. So notice once again the program will compile because we are dealing with raw pointers. Rust has made us acknowledge that this operation is potentially dangerous. And this time around, in contrast to the previous example, this is indeed potentially dangerous. And when I say dangerous again, I don't mean that your operating system is going to crash or anything like that. But this can lead to very broken, very buggy programs because you're basically just playing around with memory and things that used to be there may no longer be there. It may lead to corruption. It may lead to crashing or buggy programs. It's not like it's going to break down your entire computer. It just means that now we're sort of in this weird, undefined, potentially not great state for our program. So I want to show you that when we run this program, I'm not exactly sure what's going to happen. You can see right here we are getting nothing out. So it looks like we're trying to access the data at 0.01, where the address that formerly had yellowtail and it's no longer there. Again, it doesn't mean that our computer is crashing or anything like that. But this is a flaw. This is a bug. It's very common in languages like C or C plus plus to have this kind of code where you dereference an address to get to data that is no longer there. And that's the exact same risk that we have with raw pointers in rust. Again, references help us solve this problem, but references cannot accommodate every kind of data structure and type of program that we want to build. So raw pointers are a lot more flexible. As you saw. We could even obviously create multiple mutable raw pointers. We can dereference multiple raw pointers at the exact same time as well. These can coexist, right? Here I am even using them on the same line. That's a better example. We saw with our two mutable references that those examples were not valid here. They totally are. We have D references to two simultaneously coexisting mutable raw pointers. You can see right now who knows what we're going to get. We're going to get two empty outputs. But the key takeaway is this is potentially unsafe code, that the regular references would absolutely prevent that. Raw pointers do not. Raw pointers do have a lot more risk to them that regular references protect you from. But this also does bias additional flexibility because there are certain operations that regular references cannot properly support. And that's where smart pointers are going to come in. Smart pointers are going to allow us to have a nice abstraction layer or wrapper layer over raw pointers like this, with a little bit more safety attached. On top of that, we're going to dive into that in the very next lesson.
