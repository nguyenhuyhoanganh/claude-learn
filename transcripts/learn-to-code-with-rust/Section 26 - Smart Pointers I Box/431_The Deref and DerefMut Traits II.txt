# The Deref and DerefMut Traits II

In this lesson, I want to take a quick moment to explain why we need to declare this target associated type in the Imple block for the Deref trait. Our previous example was simple because our custom box struct stored a single piece of data, so it made sense that the Deref method would return a reference to that single piece of data, and it would be of the same type as generic type T. But let's consider a more complex example. Let's say that our struct will now store two fields, and we want to enable them to be of different types. So in order to enable that variation in types, I have to add an additional generic here to my custom box declaration. Assuming that I want this second field that I'm about to add to be generic enough to support any kind of type in an instantiation. So here, for example, I can add another generic after a comma and a space I'll call it u. And down below I can declare another field. Let's call it more data. And this can be of type U. Now remember this syntax does not mean that types T and you have to be different, but it does allow them to be different. Because if you only had a single generic type T and you wrote this, then rust would mandate at every point of instantiation that the data field and the more data field have to be of the same type, they have to both be t. Now you can be of a different type which allows us to have syntax like this. So now of course we have to update all of the other corresponding places where custom box is having some kind of methods implemented to support the additional generic. So let's quickly move through that right here. I'll add my two here and add it right here as well. Here I can take my data parameter and also receive a more data parameter set to my type you. And then right here I'll provide the shorthand syntax for the more data field right here as well. That makes that violation go away. Same thing here I'm going to say two and then T right here. There we go. We're going to come back to this in just a moment. And finally right here for the deaf mute trait. I'm going to do a U generic here as well, and a generic here as well. So we're implementing this trait for custom boxes of any type T and u in combination. So no matter what we provide for t and u they can be of different types. We're going to have support for the capacity to dereference both an immutable reference and a mutable reference here. So now all we have to do is provide an additional argument here. Like I mentioned, this can be a totally different type. Let's say I'm going to do something like hello here. And that should do it. And notice here when we are dereferencing here using the asterisk we're relying on in this case the mutable implementation here, the deaf mute. And it's providing a mutable reference to self dot data. So what we're going to be updating here when we mutably dereference the custom box itself is really going to be this data field right here. More data remains completely unaffected because we're not involving it anywhere here in the deaf mute method implementation, say that we wanted to adjust the Deref logic here so that a dereference of custom box returned, a reference to more data instead of a reference to just data. Well, of course here we could update this to refer to the more data field. But that brings up an important question, which is how is rust to know whether the custom box smart pointer, and specifically its Deref method, should dereference to a value of type T or a value of type U. Or more specifically, a reference to type T or a reference to type U. These could hypothetically be two totally different data types. So how is this method supposed to work? If it can be potentially either one of those, remember that the trait was written by some rust designer, and they can't assume all of the ways that this method will be used, but they need to provide a consistent type signature for the sake of the compiler. So this deref trait could be implemented on a struct with just a single field, or five fields, or ten fields. It might even return a reference to a fixed type like an i32. So how do you enable that flexibility? So the solution here is that the Draf trait and the definition of it says we're going to have a Draf method that's going to return a reference to some type basically called target to an associated type. It's going to live here in the implementation called target. And it's here where we gain the dynamism to provide what that type is going to be. If we're providing a return that is a reference to more data, the compiler can say, well, more data is supposed to be of type U, but you told me that the target associated type is type T, so this self that target is evaluating to a reference to T which does not match this syntax right here. So of course we can adjust t here to be u. And that will work just fine. And the compiler is once again going to keep us responsible. And we can apply the exact same principle here. We can return a mutable reference to more data like so. And once again this target type is just inheriting the target associated type definition right here. So in summary, the Deref developers wrote this trait definition so that it includes a Deref method, and that method has to return a consistent value of a consistent type. But since that type can be unpredictable or dynamic or subject to variance, it's instead tied to the target associated type. This definition of the trait. In fact, if I tried to go into this definition and look at what its requirements are, this definition within the actual trait itself says, hey, I don't know everything that target could be, but it's going to be a name called target that has to be provided at the point of the trait implementation. And now that gives it that dynamic flexibility that two different developers implementing this trait for two different structs can specify a totally different value for target and just have it work right. So hopefully that provides a little bit more clarity about the design and why the trait works as it does, and why it requires the target associated type. It basically allows us to provide a dynamic type to serve as the return type, or more specifically, to be associated with the return type here, which is a reference to that given type called target. And that can vary from implementation to implementation of the Deref trait.
