# Vectors are Smart Pointers

In this lesson, I want to show you another example of a recursive data structure. However, the code that we're about to see will compile despite the lack of indirection, despite the fact that we're not using a box or a reference, we're going to have a recursive data structure that does compile. And that might be surprising. And I want us to explore why it compiles. So we're going to be building out a tree. A tree is a data structure that consists of a hierarchy of nodes. A tree has a root node that can have multiple child nodes. And then the child nodes in turn can have their own child nodes. So one common example of a tree is something like an ancestry diagram. You have a grandmother and a grandfather. They might have children, those children have children, and so on, multiple levels or layers down. Another example of a tree would be something like a file system on your computer. A file system very much resembles a tree. There is a root node. We can think of that as the top level folder on the computer. And then that folder holds files as well as other folders. And then that design can continue many levels down. So within a folder you'll have another folder which will have another folder which will have files and so on. So let's begin by defining a new enum. We're going to call it File System Entity. And we're going to try to model out this idea of a file system. And I'm going to provide this enum immediately with a debug trait implementation. And then we're going to define two enum variants. The first is going to be a file representing some file on our system, such as a rust file or a music file. And every file is going to have a name. So I'll use my struct variant syntax here. And a file is going to have a name field set to a string. And then the second enum variant that I'll define will be called folder. And a folder will similarly be a struct variant. It's also going to have a name field set to a string. This is the name of the folder and this is the important part. Here I'll also define another field here called content. And content is going to be made a vector and a vector of file system entities, because within a folder we can store either many files or many folders. Right. Or both. And both a file and a folder classify as a file system entity. So this vector should be able to store either one or both. So I'm going to define the content field to be a vector of file system entity enums. All right. So you'll notice that the code is going to compile just fine. I can run my main function right here. We're not outputting anything. But you can see we have no violations right. Remember the compiler is going to catch any violations with a data structure at the point of its definition. We don't actually have to instantiate it or anything like that. And we saw that example with our linked list enum earlier before we applied the box to our internal content. To wrap it, we saw that the code would not compile regardless of what we wrote in main. So in this situation, the compiler is happy, it's satisfied, and why is it satisfied without a box or a reference for this content field? After all, with this design, wouldn't we run into the same infinite recursion issue? A file system entity folder can potentially hold another folder within the vector, which in turn can hold another folder within that vector, which can store another folder within that contents field vector, and so on. And wouldn't that continue forever and have the potential to create infinite memory? Well, it turns out that the vector, very much like a string, is a smart pointer. That's hopefully a bit of a fun reveal because we've been working with vectors throughout the entire course. But indeed, a vector is an example of a smart pointer. The vector is modeled as a fixed size struct stored on the stack, and then that struct is going to contain a raw pointer that points to heap data. It's also going to hold additional metadata about the vector, such as its length and its capacity. But that satisfies the core definition of what a smart pointer is. It's fundamentally a wrapper around a raw pointer. It hides away that memory address, and it stores additional metadata alongside that address. It's like an enhanced version of a reference, and a vector as a smart pointer behaves like a reference. So what happens here is the vector is going to allocate a single spot of contiguous memory on the heap for the elements it's going to store. But otherwise it functions very similarly to the box right. The vector, much like the box is an own type, it's going to contain a raw pointer. The box is going to store a singular value, while the vector is going to keep track of a contiguous sequential block of memory on the heap. Because a vector can store multiple elements in sequence, and they're going to be clumped together in the computer's memory as well. But otherwise they're very similar data structures, right? It's important to note, again, that the content that the vector stores on the heap can certainly vary in size. So a vector with ten items inside it is going to take up less memory than a 100 item vector. But those ten items or 100 items in the vector are going to be stored on the heap. The vector size on the stack remains fixed. It's just going to be the struct that stores the raw pointer that points to that varying heap data, right? But that raw pointer is going to remain a fixed size on your computer. And then the vector itself is going to be an own type. And because we are dealing with that own type much like a box, we don't have to worry about references or lifetimes. All of that to say, this code is totally valid and the compiler is going to be completely fine with it because it knows it's not going to run into an infinite memory issue, because it knows that the size of this vector is actually predictable. Predictable on the stack, that is. So now that we have a better understanding, let's create a few instances of our file system entity enum. In main, we're going to actually model a few files and folders on a sample computer. Let's begin by creating a sample file. So right here I will of course create a file in our program a rust file within a rust file. Very meta right. So here I'll take my file system entity enum. And I'm going to create an instance of the file variant which needs a name. So I'll provide a name of the file. We can do something arbitrary like my rust code. Let's create another file as well. Let's call it our Python file. Same principle I'll talk to my enum. I'm going to use the file variant that requires a name field, and that's going to represent the name of my file. This can be something like my Python code dot pi. In case you don't know, the extension of files within the Python language is going to be dot pi. And then next up let's create a sample folder. So right here I'll create a variable called code folder, and I'm going to instantiate an instance of the other variant within my file system entity enum, which is folder that's going to need two fields. The first one is name, much like my files. So let's see. I've created a folder on my computer to store all of my programming documents. Maybe I'll call it code stuff. And here for my content field I'll create my vector. I'm going to use the vec macro to do so. And then I'm going to provide the two files from above. So I'll provide rust file in here and Python file in here. So very much like the box. And just as a quick reminder of how the vector works, it's going to take ownership over all of the types that we place inside it. So as soon as we write this code, the rust file and Python file will become invalid. They're going to lose ownership. Ownership is going to move to live inside the vector. And then the vector is going to be owned by the content fields. Right. But notice there are no compiler violations because this vector, this smart pointer is going to handle or abstract away the complexity of storing this data on the heap storing a raw pointer to it somewhere internally, but then hiding that away from us and giving us a basic vector that we can interact with. That's going to be the own type that we actually use, sort of as the gateway to interact with that data. And that's the again, the advantage of smart pointers that they hide away the complexity of working with that heap data. All right. Just as a few more examples here, I'll create let's say a screenplay file. So right here I'll have another file instance. Let's give it a name. Maybe this one is going to be my screenplay dot txt. And then finally at the very top of this tree let's say we create a all documents folder. So right here file system entity folder. And this folder will have a name. Maybe it's the top of my file system. It could be something like documents. And within my documents folder I'm going to have content. And now in this vector I can once again mix and match both a file and a folder or any combination of those that I want, because the enum includes both variants, so both of them are valid to place inside a vector. So maybe this vector is going to store my screenplay as well as my code folder. And we can imagine that is the structure of my computer. So down below I can output the value of all documents in a pretty printed debug format like so. We're going to see something that looks very similar to a hard drive and its contents. Right. So here is our top level documents folder. A folder contains content and it can include many other files. Or it can include nested folders. And those nested folders can in turn recursively contain other nested folders or nested files. And that can continue as many levels or layers down as you want. Once again, that is further proof that we are dealing with a recursive data structure. But once again, because a vector is a smart pointer, we don't have to worry about solving the indirection problem here. We don't need to create a box. We don't need to create a reference. We don't have to deal with the complexity of lifetimes because a vector is already a smart pointer. It just works.
