# Trait Objects II: Handling Multiple Errors

As a reminder, if a function or method can result in a failure, the rust convention is for it to return a result. Enum. The okay variant stores the success data, which is represented by a generic, and the error variant is also going to store associated data. An error value that is modeled with another generic. In this case, you can see they're called T and e. Now, within the Rust Standard library, there are many different error types that are going to fill in for that E generic in a result enum. Usually the language is going to model an error type as a basic struct with some varying fields or methods. But what all of these different error types, or equivalently error structs are going to have in common, is that they're all going to implement a common shared trait called error. So as a quick reminder, let's dive into a sample method that returns a result enum right here. I'll declare a variable called result, and I'll begin with a basic string that includes a text of five. There is a method called parse on a string that allows us to extract an alternative data value from a given string. For example, we can try to parse an integer from the strings. Contents. So for example, here I can apply the turbo fish operator and ask to attempt to extract an I32. Invoke the function or method itself and you can see the return value is a result because it may be successful, or it may be erroneous if there is nothing numeric to extract from the string. So for example, if my string happens to be ABC, there is no number to extract here. And so in order to model the possibility of either a success or an error, we get back here a result. All right. Now notice that the concrete type filling in for the E generic for the associated data that lives within the error variant is this parse int error. This is a specific error available in the rust standard library. Now even with this rust analyzer annotation I can command click or control click into this specific type right here to go to its definition in the Rust Inner Library. And here we can see an example of an error type. It is a basic struct called parse and error. So it's just a common struct. But really what makes it an error type is the implementation of a special error trait. And I can actually look for that definition directly within this file. So right here I'm going to look for impl followed by the word error. This is the exact same trait implementation syntax that we've come to know and love throughout the course. And here we can see where the rust team implemented this error trait for the parse int error struct, and all of the different structs or error structs within the rust standard library are going to have this feature in common. They're all going to implement this error trait, and that's what allows them to distinguish themselves effectively from a regular old struct and effectively communicate that they are modeling some kind of error. So the important takeaway is that it is this error trait that most of Rust's error types are going to implement. I can in fact command click into this trait to see the trait definition itself. Here it is. You can see that this trait is actually a sub trait of the combination of the debug trait and display traits. So that means that if a type like a struct implements the error trait, it must also implement the debug and display traits. And that detail is what enables every error type to be printable, an error needs to be able to communicate what went wrong in a friendly string format, right? Otherwise, it's just a struct with fields, right? So if every type that implements error must also implement debug and display and must be able to present itself in a string format, either a friendly format for the user or a developer friendly format using debug. Right. So this is what enables an error struct to have a textual representation. So if I close all of these files and transition back to main, we can react to this result right here with a basic match statement. And we can take a look at the printability of this exact parse int error struct that is being packaged up within the error variant of the result enum. This is an example of something that will yield the error variant, because the parsing will not succeed due to the fact that there is no integer to extract. So here very quickly I can react to my okay variant. If it succeeds, I am going to get a number. Not in this scenario, but in case my string does have content, I can get the actual numeric i32 here. I can print it out. It's kind of irrelevant for the sake of this example, but I do need it in order to satisfy my match statement. But here, more importantly, when I react this error variant IRR and I assign a name to the associated data, you can see here I'm using the name error, but its type is that parse int error struct that we were just exploring. And you can see right here I am welcome to print out that struct my parse int error in either display or debug format. Because the struct implements the error trait, which also requires the parsing error struct to implement the display and debug traits. So you can see right here this is going to work just fine. This is a use of display for that struct. And this is a use of debug for that struct. Obstruct. All of those examples are going to work. The Rust Standard library consists of multiple types of errors. In fact, multiple types that are literally called error. They are found across many different submodules in the standard library, which allows them to have the exact same name of error. But because they are defined within a submodule that provides the module namespace and enables that duplication of the name error. But what all of these different error types, what all of these different error structs are going to have in common is the implementation of this uniform error trait. And we can use that detail to our advantage. We can use, for example, trait objects to be able to gracefully handle a variety of errors in certain situations where there may be different kinds of errors, different ways for things to go wrong. So this is going to offer us a good opportunity to review some error concepts introduced earlier in the course, but now advanced them and use a box to make our error reactions much more flexible. Let me show you an example of this. I'm going to delete this code, which was for the sake of example. So let's define a new function in our file. And it's going to have two responsibilities. It's going to accept a file path. And it's going to attempt to read the content from a file specified by that file path. And remember that reading operation may result in an error. So it's going to return a result. And then we're also going to attempt to parse a number from the contents within the file, which is a second operation that may also result in an error. Let's begin by defining this function. I'm going to call it read number from file. And this function is going to accept a file path. So I will set a path parameter set to a string slice. We're going to come back to the return type in just a moment. And as a quick reminder we have within our standard library a submodule called FS, which is short for file system. And that file system submodule within it has a top level function called read to string. And what that's going to accept is a file path. So I can pass in the path parameter here. And this is going to attempt to read the contents of a text file in the given path. And if it's able to successfully read it it's going to parse the contents as a string a heap string. And that's going to be the success data or the okay variance data in the result. And if it fails, it's going to give us back a special type of error called an I o error. So I actually want to write out this return type explicitly just to make sure we understand what's going on. So at the very top here I'm going to reach into another submodule called io which is short for input output. And in there we have a struct called error. And this is a struct that models an input output error. Once again I can click into here to show you. Here is that definition very much like the parse int error struct that we saw a few minutes ago. It's a struct that models an error, and this is literally an error struct that also implements the error trait. A little bit confusing because they share the same name, but that's totally fine. A struct can be named error. It can also implement the error trait. All right. So I'm going to write out the type here for the variable I'm going to call it file contents. So the function on the right is going to return a result. We can sort of see the annotation here. It's going to return a result. And if it's successfully able to parse the contents of the file because say the file exists and it's successfully parsed, it's going to collect the heap string inside the okay variants data. So the okay variant is going to store a string. Now the contents here is going to be this error type right here. And I o error is what's going to be stored in the error variant in case of failure. So what I'm actually going to do just to remind us is I'm going to use the as keyword here to give this an alternate name of I o error. Just so remember where it's coming from, especially if we start dealing with multiple structs that happen to be called error that are found within different submodules. It's a good idea to give them names, so it's a quick reminder of which one we're dealing with. So here in the error variant for that generic, it's going to be the I o error. That's going to be the type of the associated data. All right. So now we can see the compiler is happy. So this is indeed the return type of the read to string function a result where the two concrete types filling in for the generics are string and I o error. As a reminder, we can use the question mark operator, also called the try operator, to attempt to extract the okay variance data from a result if it is available. So to get the string. And if that's not available because we have the error variant, the question mark operator will instead immediately return, but it's going to return the result with the error up to the calling function. So, as a reminder of what that looks like, I'm going to remove this annotation right here. I'm going to add the question mark operator here. And notice what happens to the type inference on the left instead of a result. We're going to assume we have the successful string. We can assume we have successfully parsed the string at that point. Because if we have the error variant instead of the okay variant, this question mark will automatically return at this point. So the reason we have a violation here is because if we're going to return the result right here implicitly with the question mark. Operator, we need to make sure we have the return type declared here for the function itself. So right here I'm going to provide my annotation. The question mark operator will return a result. Once again the success data type is still going to be a string. And in the case of failure, we're going to return that I o error. So this is the exact same type we previously had for file contents. Right. So here this is totally fine. The only problem now is we're only returning the error variant in the case of this failure. So if we want to also return the success possibility right here, we want to return the actual file contents. But again within a result enums variant. So in the case of success in the case that we successfully parse the file contents as a string using the question mark operator, we then want to return it. And not just the string itself, but specifically an okay variant wrapping the file contents. All right. And make sure to exclude the semicolon. Save that. And that is totally satisfactory. So once again if this operation fails with the question mark operator we're going to return the error variant wrapping an I o error piece of data. And that satisfies the return type right here. And if we do successfully parse the contents of the file as a string, then file contents, the variable will be a string. We're going to package it up within the okay variant, which also satisfies the result enum type and return that from out of this function. Now recall that the second thing that we wanted this function to do is to parse the numeric contents from the file. We're going to get a string read from the file. We want to extract a number from the content stored within the file. We can pretend that the file is just going to store a single integer value, but it's going to arrive in our program as text. That's just how read toString works. It reads the contents of a file as a regular string. So let's apply the exact same parse method that we saw earlier on in the lesson. So I'm going to delay this return for now. I'm going to take my file contents. I'm going to invoke the parse method on it and use the operator to say I want to try to parse an i32 from here, invoke the method, and then I'm going to assign this to a variable called parsed number. And as we saw earlier, the parse method is another example of a method that returns a result. The problem is the result has a totally different type, right? The success type here inside the okay variant is going to be an I32. But in case of failure, the error here is a totally different error struct. It's not going to be an I o error, it's going to be a parse enter. These are two totally different categories of failure, right? This is an input output error because a file might not be able to be read or may not be exist. This is a mathematical error of being unable to parse an integer. They're totally different types. So if I say okay, well, I know I want to ultimately read the number from the file so I can update this to be an i32. And then I also still need to return the okay variant with my parse number. And of course, in order to actually get the number from here, I want to apply the question mark operator to this as well. I want to extract this i32 from this result to assign it to the parsed number variable. So I can attempt to do the question mark operator. Here you can see the type annotation will successfully change to be an I32. We're going to package up that I32 data within the okay variant of the result enum and return it that satisfies this portion. Unfortunately, as you can see, we're going to have a little bit of a compiler violation. It's going to come from the question mark operator right here. So what exactly is the problem here? The parse method is totally fine. The problem is that there is a mismatch with the type that we declared as the function return value. We said the return value is always going to be a result where the error variant is specifically going to be the I o error. A specific error struct found within the I o submodule. But what we're trying to return here indirectly, implicitly with the question mark operator, is actually a result where the error type is a different type. It's the parse int error error struct, not the I o error struct. So there is a type mismatch here, specifically with the error variant being packaged up within the result. The compiler is absolutely correct. We are not returning here what we promised we would be returning here. Yes, the question mark will return a result, but there is a type mismatch not in the larger enum return value, but specifically in the type of error right here. Filling in for the generic right you can see right here can convert to this standard I o error. I'm getting a totally different error here, which is a parse error. So how do we elegantly solve this problem? We want to return either one of these two errors. But there are two totally different types. And if we hardcode one type here then we're not going to be able to return the other one. We can certainly do I o error here, but then we can't return a parse error on this line and vice versa. If we hard coded this return type to be parse and error. This would be fine, but this line would be unsatisfactory because that would be returning a result where the error variant is an I o error instead. So remember what we learned in the previous lesson. The error types here are different parse int error and I o error, but they both implement the same error trait. Thus, if we can wrap these errors in a box, we can have a solution where we can return two or more different types as trait objects, right? As long as they both implement the same trait, we can use a box to wrap either one, and that will be a satisfactory return value for the compiler. So let's begin by bringing in that common error trait into scope that all errors implement. So right here within my standard library in the error submodule there is that capital E error trait. This is the same error trait that parse int error will implement, and an IO error struct will implement and every other error in rust. They're all going to implement this thing. And what I'm going to declare here is that instead of always returning an IO error here as the concrete type of the error variant, I'm going to return a box. And once again, I'm going to use the exact same trait object syntax as in the previous lesson, a box that's going to store some dynamic type that promises to implement a shared trait. So the syntax once again is going to be the dim keyword. Some dynamic type that can totally vary could be one type, five different types, ten different types, but what they're all going to share in common, regardless of the exact type that they are, is that they're all going to implement the same trait, which is going to be error. So the dying keyword followed by the trait and wrapped up within a box. If I save this, you can see right here the compiler issue goes away. We in fact no longer need this I o error here. It's still going to be implicitly returned. It's just going to be packaged up within a box. If we have an issue right here and then right here, if we have an issue here, we're going to package up the parse int error within the box and return it as well. So I'm going to delete this line. So this code will compile. And what makes it all the more magical as we can see is that we don't even need to manually create the box here. Inline the question mark. The try operator will automatically handle the creation of the box behind the scenes. We are now returning technically a consistent type, so the compiler is satisfied. This function will consistently return a result enum whose okay variant data will be an I32. That happens right here, and whose error variant will always be a box that holds some dynamic type that can vary, but that will promise to implement the error trait. In other words, a trait object. So now you can see the compiler is totally satisfied. Just for extra practice, I want to write out the explicit code that matches what is going on here behind the scenes. So I want to explicitly return the box wrapping the given error just to show you what is being automated right here. So once again, what I'm going to do is I'll start with this line. I'm going to return to a match statement right here. And I'm going to remove the question mark. Operator provide my match curly braces and buy us some space. And we have to react to two possible variants here. The first one is okay. And that's going to wrap the actual files contents which I can call content. And then I can immediately implicitly return it from this block. So this is exactly what the question mark operator is doing. If there is a success, if the file is successfully read, the contents are going to be stored as a string within the okay variant. We're going to extract it from the okay variant and return it back. And thus it's going to become the value assigned to the file contents variable, which will become a string. It's guaranteed to be a string because this is the success path. Now I also want to handle the error variant. And if that's the case we're going to have an error which is going to be our IO error. So what's going to happen here? In order to replicate the exact same functionality as what happens with the question mark? Operator I'm going to return early here. I'm not going to allow the function to progress. Rather, I'm going to return. And what do I need to return? I need to return an error variant in order to satisfy the return type of a result enum. And what is my error variant going to store? It's going to store a box. So I'm going to create a box right here using the new constructor function. And that box is going to store a trait object. Some type could be any dynamic type but some type that must implement the error trait. Well, luckily this IO error being packaged up in the error variant does implement the error trait. So I can just return this error that I've extracted right here. And this name can be whatever you want. You can call this E. But whatever we are extracting here from the error variant, that error struct is what we're going to wrap up in a box which is wrapped up in an error variant, which is what we're going to return right here. Right. And then down below we can do the exact same thing with parsing the actual numeric contents of the string. So right here I can get rid of this. Get rid of this. I'm going to use another match statement and apply the exact same principles here. Remove the question mark. Operator. Create my curly braces for match for the okay variant. If I'm successfully able to parse a number from the given file string, I can take that number and I'm going to return something that satisfies the return type. So it has to be a result. Enum variant I'm going to return the okay variant, wrapping the number that has been parsed from the string. And in the case of failure I'm going to have the error variant storing this specific error. And once again this error is going to be a totally different error object. It's going to be the parse int error which we can see right here in the annotation. So once again to satisfy the return type of the function I need a result enum variant. So I'm going to do the error variant. And then it needs to wrap not just the error by itself, but a box that is wrapping that dynamic error that implements the error trait. So I will create a box right here and feed in my parse int error and do that. And what we have right here on the screen is the exact same thing that we just had with the question mark operator a few moments ago, but written out a little bit more explicitly here, we have to write out the box manually. The question mark operator, luckily will automatically do that for us. It will automatically create a box, wrap up the error from the result if it is an error in that box, and return automatically right there. Otherwise, it's going to extract the data from the okay variant and assign it back to the given variable, and then the function will continue running. So now we are good to test our code. So at the very top level of our project let's create a new text file I'm going to call it value dot txt. Make sure it's not in source. Do make sure it's the very top level of the code base right here. And I'm going to put in a sample number. Because remember the goal is to both read this file as a string and then parse the numeric contents if possible. So there are two possibilities for failure here in the read and in the numeric parsing. So here I'm going to save the value 42. Save the file. Let's come back here. And here in main we can try invoking read number from file from above and passing in my file name which is value dot txt. So right here value dot txt. And that is going to get the return value of the function which is going to be a result. So we once again have to react to either of the two variants here with a match right here. And so for the okay variant I'm going to actually receive the numeric value that's going to be this thing right here. So I'm going to actually get the value in the scenario that both of these operations succeed. And I can print out my statement like the number is value. And then down below I can also react to my error variant. And I can extract the data, the associated data inside the error variant, which is going to be a box wrapping some kind of dynamic type that promises to implement the error trait. Luckily, any type that implements the error trait is also going to implement the debug and display traits, so it is guaranteed to be printable. So right here I can say the error is and print out error. Even though it's a box, that's totally fine. It's going to be able to obviously get the contents from inside the box, which is going to be an error struct of some kind, some struct that implements the error trait, either a parsing error or an I o error, and then print it out in string form. So in this scenario this should work. We do have a valid file with this name that can be parsed, and the contents inside it can be parsed as a number. So we're going to get the number is 42. We can also take a look at both possible failures. So for example let's provide a file that does not exist like nonsense. If that's the case then we're going to encounter a failure right here. So we're going to hit this error match arm in here. And we're going to return the error variant. Storing a box, storing the I o error. And so the error we're going to see here as a result of this line executing is going to be the I o error. So you can see right here the error is no such file or directory. This is the display implementation of the I o error struct that we are returning right here. Explicitly. The second way of course the code can fail is if we have a valid file, but it doesn't actually have any numeric content to parse. So let's change this back to value dot txt. Let's go back into value dot txt and change this to not have a number inside it. Maybe something like nonsense. Save that file. So now when we go back to reading value dot txt, we should be successfully able to read the file contents. So we're not going to return early here. But when we try to parse a number from the string. That's not going to work. So we're going to hit the error variant right here. Return an error variant storing a box that's going to contain the parse int error. That's what this is going to be. And we're not technically going to see parse error here because the type here is a box storing some dynamic error right. It can be either one of these two or a third one or fourth one tomorrow. So the compiler doesn't know exactly what it's going to get because there's two different ways this thing can go wrong. But it knows that whatever it gets is going to be something implementing the error trait. It's a trait object because it's a non-specific type, but one that promises to implement a given trait, which is the error trait. And we're going to be able to print that out. And thus we're going to see the equivalent parse error display trait implementation which is invalid digit found and string. Basically it's not able to read the contents here. So in summary, this box design error is a design pattern that uses trait objects to handle the possibility of different types of errors. Each type of error is just a different struct that implements a consistent error trait. It could be an input output error, a formatting error, a logical error, a parse int error. But all of those different error structs in rust are all going to implement the shared error trait. And that allows us to come up with this elegant design where we can return many different types of error structs, as long as we wrap them within the box. Smart pointer. That's another common use case for the box. In fact, you can even define your own custom error types. And if you do that, you should make sure that they implement the error trait as well. So then you have consistency with the way that rust works with regular rust errors built into the standard library. So we're going to see an example of how we can define a custom error that works the exact same way as these two do in the very next lesson.
