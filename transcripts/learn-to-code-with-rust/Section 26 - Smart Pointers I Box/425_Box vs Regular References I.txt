# Box vs Regular References I

A few lessons ago, when we first tried to declare that our node variant would store a linked list, the compiler didn't compile and it advised us to quote insert some indirection to break the cycle. So we used a box to solve that indirection problem. But it's interesting to note that one of the compilers proposed options was actually a plain regular reference. Indirection is when we store an address to a value rather than the value itself. And just like the box smart pointer, a reference is another way to create indirection. We don't directly store a value, we refer to it indirectly through a reference. So in this lesson, I want to show you what happens if we opt for that reference solution. And that reference implementation is going to work. But I would argue it's design will show us the benefits the box offers in terms of simplicity, primarily in that we don't have to worry about lifetimes. The box is an owned type, so it fits nicely into Rust's ownership model, and we never have to worry about the borrow checker and its concern over lifetimes. So first things first, let's rename our enum that we currently have. I'm going to call it Linkedlist using box. And so down below here I'm going to change this to be linked list using box. And then I'm going to copy and paste the complete declaration down below. And we're going to make a similar example using references instead. So right here I'll do linked list using reference like so. And so. Here I'm going to remove our previous declaration of box. And this time around I'm going to just provide my borrow operator to indicate that the next field is going to be a reference to some value. And that's going to be a reference to another linked list using reference. And once again, that enum which we are within also requires the generic declaration, and that's going to be the exact same T from above. So the next field is going to store a reference to the next node of the list, rather than the whole node itself. All right. So our program previously compiled in this situation, because rust knew the exact amount of memory that a box occupied on the stack. And it should be the exact same case here, because just like box, a plain reference is going to occupy a fixed amount of memory, it's a predictable, unchanging amount of memory. Now, what it points to can certainly vary in size, but the actual size of the pointer itself is going to be fixed. So it's a very similar design on the surface. We do not nest another linked list enum within the node's next field. Rather, the next field stores an address to another linked list enum somewhere in memory and we can be ignorant about. However many nodes come after that. So box creates indirection here and a plain reference creates indirection here. In our first linked list, the box serves as an elegant wrapper around the underlying raw pointer, and it handles all of the complexity of owning that heap data and the reference to it. The box itself is an owned type, so the next field in a given enum here will take ownership over the box, which itself will own the data that it is representing on the heap. It's slightly clever in a way, because we're talking about ownership here, but over a smart pointer which fundamentally just wraps or contains a reference. So its ownership over a reference just kind of slightly funny if you think about it now in comparison with a plain reference right here, the borrow checker is unhappy. You can see it immediately is refusing to compile. So the issue here is actually with lifetimes. If I hover my mouse over, you can see it says missing lifetime specifier. So this is not a recursive data structure issue. It's a basic lifetime issue that we've seen before in the course. Anytime that we try to store a reference inside a larger type, like a struct or an enum, the compiler is going to have the exact same issue because it needs to ensure that the reference is going to be to something that is going to be valid, that is going to be correctly allocated somewhere in memory and has not been deallocated. So if we want the next field to store a reference to another linked list enum, the borrow checker needs to guarantee that the lifetime of the reference to the next linked list node is shorter than the lifetime of the next node in the list. In other words, if we provide a reference to node two from node one to node two needs to outlast node one. Because if node two did not outlast node one, then node one's field would be storing a dangling reference. It would be a reference to memory that has been allocated. So thus the borrow checker is going to demand a lifetime annotation for us to represent that relationship within the enum. So here's how that's going to work right here in the exact same angle brackets where we declared our generic I'm going to declare a sample lifetime. I'm going to call it tick A and then provide a comma. So it's a little bit of an interesting syntax because the angle brackets contain both the definition of the lifetime and the generic. But that's totally normal. In rust you might see both combined together. So now we're going to apply the exact same tick a lifetime right here. We're also going to need to apply it right here using the exact same syntax in these angle brackets. So tick a and then comma t. So once again we're using the lifetime of tic. A. To define the relationship between the outer linked list using reference node and the next field that it's going to contain, we tell the compiler to validate or confirm that the starter or outer linked list enums lifetime is going to be connected to the lifetime of the next field inside the node variant. So this inner linked list needs its own lifetime annotation. And we're saying this thing right here. This outer linked list cannot outlive the reference right here. That the next field is providing a reference to node one cannot outlive the node that it is referencing within its next field. So let's create a concrete instance of each of our two linked lists to show these ideas in action. We're going to start with our new one the reference option. So right here I'll define a variable. Let's call it second node. Once again we're going to use linked list using reference and then two colons. And I want the node variant. And this can represent the second and final node within our linked list. So here let's give it a value of two. Just so we have a nice sequence of incrementing integers. And for the next value remember here we need a reference now rather than a box. So I will provide a reference to the actual empty variant right here. And importantly the empty variant inside this linked list, which is totally distinct from the empty variant right here. So here a reference to linked list using reference to colons and then empty. So you might be curious how this code is working, since we're providing a reference to something that doesn't actually have an owner. Right. So it's an advanced feature in rust called Temporaries. And the way it's supposed to work is if rust dropped the memory that holds this empty node at the immediate end of the declaration of the statement. Well then, this reference declared in line two empty would immediately be dangling. So rust understands the risk there, and it understands that this value of empty is being used as part of a larger expression that takes a reference to it. So basically, it's going to extend the lifetime of this empty variant long enough so that it lives alongside the lifetime of the second node. So basically we are going to provide a reference to something in memory that is going to survive or exist as long as second node does, even though there is no actual concrete owner here or no literal owner that we can see. I'm sure there is some kind of owner somewhere, but we are going to ensure a reference that points to allocated data. The compiler is smart enough to make that happen, even though it is a little weird that we're providing a reference to just a declared instance right here that doesn't have any other owner. All right, so after that, let's declare our first node, which is going to provide a reference to our second node declared above. So once again, linked list using reference will access the node enum variant. This value can be one. It's the start of the list. And then for next we're going to provide a reference to second node above. And there we go. So once again to restate the next field on our first node is a reference to second node. So if second node becomes the allocated first then the next field is pointing to the allocated data. And that's the risk here. So first node cannot outlive second node. But first node can certainly die out vice versa before second node does. Because if first node disappears first well then the next field is gone. And the entire enum right here is Deallocated. But this second node can continue existing by itself. It is this reference right here. That is the risk because the risk is that this reference points to something that has been deallocated. As long as second node is alive, its memory is allocated and there's no risk, right? So this is totally fine. The risk is of first node outliving second node. So let me show you right here I can output the value of first node. In my debug format. We're going to see a very similar output to what we saw in the previous lessons with the box variant. Right here we have our starter node with a value of one. And for its next field it is a reference to another node storing the value two. And then that is the conclusion of the linked list. So just to recap the ideas that I just mentioned, the second node must outlive the first node. So what I can show you right here is if I, for example, drop the first node right here, forcing the compiler to deallocate it. Of course, I cannot print out first node after I drop it from memory, it no longer exists. In fact, ownership moves into the drop function. That's how the allocation occurs. But notice that second node is totally valid here, right? Because the thing that I've deallocated right here is the thing that had the reference. And so it's now gone. Right. But second node exists nicely by itself. There is no reference to any other data. So there's nothing for the borrow checker to be concerned about. It's its own separate independent isolated instance. You can see we can totally print out second node after we have dropped the first node. But this example will not work in reverse right. So if I drop second node first and try to print out first node, that's where the borrow checker is going to be concerned. Because if we drop second node, then the reference to second node on the next field in first node is now a dangling reference. This is now pointing to data that is no longer in memory. We deallocated it right here. So if we try to print out first node, it's no longer going to be a valid operation. The code is not going to compile. So now let's follow up by creating an instance of the linked list using the box. This is going to be very similar to what we had in the previous lesson. And I'm just going to emulate the exact same example as above. So we can see the similarities and differences between them. So I'll just redeclare my second node variable using variable shadowing linked list once again, but using the box version once again we're going to have a node. Once again the value can be two, and for next, instead of a reference we're going to do a box. And that's going to wrap the linked list using boxes empty variant like so. And then right here we can declare first node that's going to be linked list using box. That's going to have a node variant in there that we're going to create an instance of. The value here is going to be one. And for next instead of a plain reference it's going to store the own type of a box. And that box is going to store inside it the second node. Notice we don't need to worry at all about lifetimes. We can just write it like so. And notice if I print out first node both here and here, the outcome is going to be exactly the same. You can see right here it literally is the exact same, despite the fact that our first version here is using references and the second version is using a box instead. So in this second scenario, the next field on the first node becomes the owner of the box which owns the second node, and thus, after these lines of code, second node is no longer the owner at all. Ownership moved from the second node variable to being inside the box, and then the next field in the first node owns that box. So notice if I try to print out second node at all, that is completely illegitimate Legitimate after the previous assignment, ownership moves to the box, which is an owned type. So there are differences here. With the box solution, we still have this chain of links, but modeled in an interesting way where we have pointers to the next node in memory, but yet contained within a box which is an owned type. That's not the exact same thing as we had here, where you saw that we could access second node after we provided a reference to it in the next field, because ownership never transferred. Now there is a slight trade off here in memory because the box, technically speaking, is going to occupy slightly more memory than a plain reference. And that's just because it's a wrapper type that has a little bit more information to store. But I would still argue it's a bit of a cleaner solution here. And the primary reason why is simply the increased complexity due to the lifetime management. Each nested nodes reference in this example must be connected to the lifetime of its parent node, which sort of creates this chain of dependencies that can extend multiple levels down. And that makes the code harder to reason about. The first node's lifetime must end before the lifetime of the second node, but then the lifetime of the second node must end before the lifetime of the third node, and so on. And that relationship can continue as many nodes as you have, right? So the lifetimes of all these nodes becomes coupled. And so in addition to the complexity of the syntax, it just becomes a little bit harder to reason about. In comparison, I'd argue that the box version fits a bit more nicely with Rust's general ownership model. So we just create the second node instance, and then we simply move ownership into the box here. And then first node is sort of the grand owner of all of this nested complexity. And we only have to worry about that. So you'll notice right here we can print out first node. Or we can even drop first node. Right. We don't have to worry about second node at all, because first node is the wrapper container that is storing that second node. More specifically, the box own type that is kind of secretly pointing a reference to that second node. But all of the logic kind of falls back to the first node as the starting point, right? Or the ultimate source of truth as the grand owner sitting atop that entire hierarchy. But in the next lesson, I actually want to show you a concrete example where box is going to enable us to build a certain design that a plain reference cannot. That's going to be another advantage of using a box in this kind of situation. But I did want to demonstrate that using plain references is technically possible, right? Because a plain reference solves the exact same problem as a box does, we need indirection. We need to store an address in the next field, rather than the core value itself, to prevent an infinitely recursive data structure that the compiler cannot ascertain the memory requirements of.
