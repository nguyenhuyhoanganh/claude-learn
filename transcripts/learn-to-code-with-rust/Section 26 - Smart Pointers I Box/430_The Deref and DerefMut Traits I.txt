# The Deref and DerefMut Traits I

A few lessons ago, we saw that we could apply the dereference operator, the asterisk symbol, to the box type. We can see an example here on the screen. Again, the box type is not technically a reference. It's a struct. But being a smart pointer, it can behave like a reference. It can utilize the exact same syntax that we would use with a regular reference, despite the fact that this is an owned type. So we use the dereference operator. We treat the box like we would treat a plain reference, and this code is going to get us the underlying value the box is storing on the heap, which in this example is 3.14. Now a smart pointer is going to implement a critical trait to support exactly this behavior the ability to dereference. And that trait is called Deref. The Deref trait enables a type to behave like a reference. So the implementation of the drift rate is going to define the logic of what should happen when code applies the dereference operator. The asterisk symbol to that type on a plain reference. The dereference operator is going to follow the references address to the original value in memory. On a smart pointer and in our custom implementation of the Deref trait, we can define this operator on our type to do whatever we'd like. Obviously, the point is for us to follow these standard community convention and define the logic to have the same intent, which is that the dereference operator on a custom type should get access to some data that the type is storing through the use of an address, through the use of a reference. So the best way to get familiar with the Deref trait is to build our own custom smart pointer. So in this lesson we're going to write our own implementation of the box smart pointer. In this example is inspired by the official rust book. Now, within our custom box, we're not going to store our data on the heap, but we will honor the core design principle, which is that our custom box will be an owned type. It's going to be a struct that's going to be a wrapper around some data, and it's going to support the use of the dereference operator to access that data. So let's begin by defining our own custom struct right here. I'm going to call it custom box. And we're going to try to emulate the exact same functionality that we have right here. We want this code to work with our own custom box implementation. So I want my custom box to be able to store any data type much like the regular box does. So I will define a single generic of T, and all my struct is going to store is a data field set to type t. The next thing I want to add very much like box is going to be a new constructor function, which will accept a value and then create a new custom box storing that value within the data field. So right here I'm going to use my impulse syntax. And I want to be able to define the new constructor function for any type t. So right here as a reminder that syntax involves declaring your t and angle brackets here and then saying custom box t. So rather than defining the new constructor function for a custom box with a specific type, like specifically an I32 or an F64, we're saying define the new constructor function for a custom box storing any generic type T. So right here I'll create my new function. It's going to take a single parameter called data which will be of type t. And then it's going to return an instance of the custom box. So here I can annotate that type as being capital S self. And in here I'll create an instance of my custom box by providing self. In order to create a custom box struct, I need a single data field and I can make that equal to the data parameter flowing in. All right, so we are halfway there. Here in main I can create my custom box. Let me call that variable custom boxy. And right here I can do custom box new. And to emulate the example up above I can pass in the exact same value 3.14. And this is the critical difference. Right now our custom box is a plain struct and it does not implement the Deref trait, so rust does not know what to do if we apply the dereference operator to our custom box struct. So notice if I try to emulate the example up above, and try to interpolate the value that we get whenever we dereference custom boxy. So applying the asterisk symbol, which we would typically apply to a reference, but can also apply it to a smart pointer like box. Here I'm trying to do the exact same thing with my custom struct. You'll notice this is not valid type custom box float cannot be dereferenced. It's not going to be able to support this behavior until we implement the Deref trait. So to repeat, the implementation of the Deref trait will enable the compiler to apply the dereference operator. The asterisk to the custom box. In other words, we're going to be able to treat this owned type this struct just like a reference. So our custom box will be behaving like a smart pointer. So let's start out by implementing the Deref trait. I'm going to need to bring it into scope. And that trait is found within the standard library, specifically within the ops submodule. And in there we can find our Deref trait. As a reminder from the traits section, the way that we implement a trait is we write impl. And if we once again want to implement the derived trait for a custom box storing any type T right here, I'm going to declare the generic T, then declare the name of my trait, which is D ref. Then the for keyword followed by the type that I am implementing the trait for, which will be a custom box. More specifically, a custom box of any type T, so any kind of custom box that I instantiate, no matter what it's storing, is going to implement the trait. And here we provide our curly braces. The Deref trait requires the definition of a single method called Deref in all lowercase. As a reminder, if we just start typing out the method name right here f and deref, we're going to be able to see this auto assist pop up, and we can press enter to auto complete the basic definition of the method. Remember, it is the Deref trait that defines the requirements that the type must honor, and that includes the methods and their names, the parameters of the methods and the return value. So this is not up to us. So let's take a look at what the DRF method requires. It's going to accept a reference to the instance and return a reference to some data which right now is self colon colon target. We're going to talk about that in just a moment. But I want to first focus on why this signature looks the way it does. Why is this method returning a reference. The reason for this is because if the method did not return a reference, but rather an owned value that came from within custom box, well, then custom box would lose ownership. Ownership would be moved out of self in this scenario, which would be the custom box out of the struct to the owner that would be calling the method. So the intent here is for us to return a reference to return a reference to some data that will continue living within custom box. But we're going to be providing a way for the outside world to access that data. That is the exact same way that a box works. If we were to provide some kind of own type in here, like a vector, we would not be able to extract ownership from inside the box. But we can talk to the box and dereference it to get a reference to that data so that we can read it. That's the same intent here. We're going to return a reference to some data living within our custom box. Now in our scenario, the only data living within custom box happens to be the literal data field. So that's exactly what I'm going to return here a reference to self dot data. Once again this is not sufficient because if data was an owned type like say a vector, this would move ownership out of the custom box. And in addition, it would not honor the requirements of this return type, which is a reference. So here this has to be a reference. Now the second half of this equation is this target type right here. So the DRF trait requires an associated type called target. That target name is not up to us. It was written by whatever developer developed the trait. Now as a reminder an associated type is a type that we have to define in order to implement a trait. So this trait in other words, requires us to explicitly declare a method and also explicitly provide a type. And we covered this briefly in the trade section of the course. Now in this situation, what this target type is going to be is it's going to specify the type of data that the DRF method is going to return a reference to. So it's going to be a reference to some type called target. We're going to define that type target right here. As a reminder. The way that syntax works is we're going to write the actual keyword type. And then the name target which is not up to us. It's whatever the Deref trait developer chose in this case target. And then this is going to be the type. So remember here it's not going to be an actual value like five. It's going to be a type. And for us more specifically it's going to be the generic type T. So this can be a little bit confusing. So let's pause once again and talk about what this is doing. The Deref method here is referring to self, which simply happens to be the type that we are within, the type that we are defining methods for, which is going to be custom box and then target. You can almost think of as being a constant that now lives within self within custom box. So this soft target is simply referring to this thing right here which is type T. So what this reference to self target basically evaluates to behind the scenes is a reference to type T. Our custom box is going to store a data field of type T. We're going to return a reference to that data field, which is going to be ultimately a reference to type T. Now you might be curious, can I get away with something like this? And the answer is, I believe this code is also going to be valid. And you also might be curious why do we need a associated type here? Couldn't we just get away with something like this and get rid of the target associated type entirely? We're going to see an example in the next lesson of the dynamic flexibility that this target associated type gives us, and why specifically the rust development team chose to declare it. Regardless, the target associated type here is going to declare the type of data at the memory address that we're going to return a reference to from the Deref method. So for example, in this example right here we are passing in a float 64. So you can think of target in that example as being a literal f64. And thus rust understands that if we called the Deref method on this type, or equivalently, use the asterisk operator, which is going to invoke the Deref method behind the scenes, that's going to now be a reference to Float64. So whatever custom, type or value of a given type that we pass in here, that type will be assigned to target, and the method will return a reference to that type. So that should be good. And with that we now have the correct Deref implementation. And now this code right here should work just fine. We're going to see two outputs of 3.14 on the right. So to provide additional context on what exactly has happened here, now that we have implemented the Deref trait, we can use the dereference operator, the asterisk on a custom box instance. Even though custom box is not a literal reference, it is a struct. So when rust sees the dereference operator, it says, ah, well, this thing right here isn't literally a reference, but it does implement the Deref trait. So what rust does is it invokes the Deref method. It gets back a reference from the Deref method. In this case it's getting back a reference to the value stored within the data field. So a reference to the location in memory where 3.14 is being stored. And then it also automatically dereferences that reference. So it follows that memory address to the original location where 3.14 is being stored. And then it prints that value right here for us 3.14. So in other words, whenever we use this dereference operator, Rest is going to be invoking the Deref method behind the scenes, which it knows it has access to because the type implements the Deref trait. That's why as soon as we added this, rust enabled this line, which previously would have caused a compiler violation. Just to provide you with another example, we can actually invoke the Deref method, just like a regular method right now on the type. That's exactly what rust is doing behind the scenes. But I can show you right here that the way rust reads this is take custom box right here and call the method on it. This is what is happening behind the scenes. The Dref method that the custom box has will return a reference, right? And then since we have a plain reference here, the regular dereference operator applies to that reference to get to the data value in memory, which is 3.14. So ultimately the smart pointer is sort of behaving as a wrapper around a reference. The Deref method is getting us that reference. Then the asterisk is following that address to the value of 3.14. So obviously this syntax is not strictly necessary. But this is exactly what's happening behind the scenes whenever we just do the dereference operator with custom boxing. Ultimately, this symbol, the dereference operator, needs some kind of reference. And Russ knows since it's not getting a reference, but getting a struct call that types Deref method to get some reference at this symbol can then follow. That's exactly what enables custom box to behave like a reference. It grants access to a reference through the use of its Deref method. Now the Deref method returns a reference to some data inside the structure that we can read, but we still do not have permission to update the data at that memory address, and the regular box smart pointer does indeed support that functionality. So for example here what I can do is this special syntax. I have my Boxee Box smart pointer. I can provide my dereference operator and then Boxee. And this is just reading. But if I want to overwrite the value at that address, I can provide an equal sign. And then a new value which must be a float. Let's say we do something like 6.28 and my semi-colon and notice this is currently invalid, but it's only because Boxee is still an immutable variable. But once I change this to be a mutable one, this code should compile. And you can see when we print out the value at that memory address. Once we've updated it through the reference provided, we are going to get 6.28. And this similar syntax is once again not going to be currently supported with custom box. So here I'm going to try to do the exact same thing with custom box and set it to be 6.28 and make custom box immutable. But this is currently not going to be valid. And that is because we enabled reading using the Dbref trait. But we did not enable writing. And that's going to require the implementation of a second trait which is called Deref mute. So as you might guess, the Deref mute trait is going to be very similar to Deref. Deref enables us to get a immutable reference to some data stored within the type, while the Deref mute trait is going to have a complimentary Deref mute method requirement, that's going to give us a mutable reference to the data being stored inside the custom type, enabling us to override it so you're not obligated to implement both traits. It depends on whether you want the data being stored inside your custom type to be either readable or readable, or both. So if we want to implement a custom box that is very similar to box, we do want to match the functionality here by implementing the complementary Deref mute trait. So that trait is once again going to be found within the Ops submodule. So here I'll provide my curly braces and a comma and bring in Deref mute. It's going to be a very similar implementation down below right here going to import or implement a trait for any type T. And then this is going to be deref mute once again for our custom box type of type T. So custom boxes of any type T will support this. And once again we're going to just write fn. The only required method here is deref mute. We in fact are not going to need to define target as we did here. It's actually going to inherit the target definition from Deref. So Deref mute is actually a sub trait of Dram. So if you're going to be implementing Deref mute, you also need to implement. The Deref trait as well. But you do not have to follow that relationship in reverse. So you can choose to implement Deref without implementing Deref mute. But if you are implementing Deref mute. You do have to implement Deref as well, and it's going to receive the target from this definition right here. So all Deref mute is going to say is give me a mutable reference to whatever target is. So this is equivalently going to be a mutable reference to type T, which is the data that we're storing within our custom box. And as you might guess, the implementation here is going to be very similar to what we have here. The only difference is, instead of returning a plain reference to Self.data, we're going to return a mutable reference. And since we are returning a mutable reference to some data stored inside custom box, the data field, we are providing a avenue to mutable access and overwrite a given value somewhere in memory, represented by data, and we're still not taking ownership of the data field. We are providing a mutable reference so that code can update the value being stored within the custom box through the use of a mutable reference. So now this code that previously would not compile does because as you might imagine, this code is secretly going to call the Deref mute method behind the scenes in order to enable the overwriting of the value stored within the custom box. So now our box and custom box are more alike. We can read the data stored inside through the use of a reference, and we can write the data stored inside through the use of a mutable reference. And that's all there is to cover in this lesson. What we actually did technically is we built our own smart pointer. Custom box is a smart pointer. It is a type, in this case a struct that behaves like a reference, both in terms of reading and in terms of writing, despite the fact that it is not a reference. It is a struct, but it can behave like a reference. And as we saw with box in the previous lessons, there are certain design advantages and certain data structures that we can build using this approach. And so we need the Deref trait to enable us to get some reference to some data inside the type that we can read. And optionally, we can also add the Deref new trait and its complementary Deref method to get access to a mutable reference to some data inside the type as well. And that enables us to use these operators, the dereference operators, to both read and write, despite the fact that we are not working with a reference. So now our custom smart pointer can behave like a reference, despite the fact that it is not literally a technical reference created with the ampersand operator right here. That's the whole point of a smart pointer. It's almost like masquerading as a reference, despite the fact that it is not one. All right, that's all there is to cover in this lesson, so I will see you in the next one.
