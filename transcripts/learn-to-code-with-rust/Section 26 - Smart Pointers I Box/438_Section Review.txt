# Section Review

Congratulations on reaching the end of another course section. As always, let's take a few moments to review everything that we learned. This section introduced us to the Box Smart pointer, so we began with a quick review of pointers and references. A pointer is a variable that contains an address in memory, the address of some other data. The address of some other value. Now, in rust, the most common pointer we work with is a reference which you can think of as a type or category of pointer that is guaranteed to point to valid data. And by valid I simply mean data that has been allocated that is present in memory that is readable. So Rust's borrow checker at the point of compilation will check and guarantee that the references in your program are the ones that you create with the ampersand symbol are going to point to valid allocated data. If your rust program compiles, that is a guarantee that your program does not have any dangling references, which are references to memory where the data has been deallocated where it has been cleared. Rust technically supports raw pointers. A raw pointer is the pointer that we just introduced in the previous slide. It is a variable that stores a memory address, but the important detail is that there are no additional safety checks. So your code can read or attempt to write to a given location where the data may no longer actually be present. So the pointer may point to valid data, but it can also point to deallocated memory. And that is the error type that we have to deal with. If you're working with pointers in other languages like C or C plus plus. Now Rust's default reference will avoid that issue. But if you absolutely have to, you can use raw pointers in rust. And we do so by using that special syntax of ampersand raw const before the value that we want to create a reference to now, whenever you're going to be referencing that raw pointer and trying to access that data, there is the potential of risk, because there is now the potential that you are working with data that has been deallocated. The compiler will not make the same guarantees with raw pointers that it can make with plain references. Therefore, you have to wrap any of your dereference code for raw pointers in that unsafe block of code. You use the unsafe keyword, then you provide the curly braces. Keep in mind that does not mean that the code inside the block has to be unsafe or is prone to error. It simply means that you, the developer, are acknowledging the possibility of that code being risky, of that code having bugs within it, because it's basically opting out of the borrow checker's safety checks. So then we started talking about smart pointers, which allow us to have almost like an elegant interface to working with raw pointers because we don't have to worry about the underlying risk or complexity of a raw pointer, typically pointing to. Heap data. Instead, we simply work with the smart pointer, which handles all of. That for us. So a smart pointer is a type that behaves like a reference. It's typically going to be a struct behind the scenes. And because it's a struct it has the capacity to store additional information and also perform additional actions through methods. That gives it a lot more power than a plain reference. Right. So most smart pointers are going to be built with structs, and they're often going to own and manage their own data, which is typically going to be stored on the heap. So again the advantage of smart pointers is that they are owned types that follow the regular, predictable rules of ownership in rust. But they also behave like pointers that are almost like a wrapper or an abstraction over some kind of raw pointer that's going to be pointing to heap data and managing the complexity of that reading and writing of that data. So two smart pointers that are built into rust that we've experienced many times before are string and vector, and they hopefully demonstrate the benefits of smart pointers. There's a lot of complexity with both the string and the vector, right? You have to manage, for example, with the string a dynamic amount of text stored somewhere on the heap. With a vector, you have to store a dynamic number of elements somewhere on the heap, and that requires the complexity of raw pointers and accessing that data and clearing that data at the moment that the owners go out of scope. But luckily, from a developer perspective, we can just deal with these string and vector as regular owned types. They abstract or hide away that complexity. Our code simply interacts with the string and vector just like our standard owned values. And then when the owner goes out of scope, for example, the variable that is assigned to a string or vector, it deallocates the stack entry for that string vector. And somewhere internally, that smart pointer handles the complexity of cleaning up the raw pointer, as well as the corresponding data storage on the heap. So the next smart pointer that we introduced was the box. And the box is one of the simplest smart pointers. All it does very similar to a string or a vector, is it stores a piece of data on the heap. We can store any kind of data within the box. We simply use the new constructor function, give it some kind of value, and the box is going to be an owned wrapper around a pointer that's going to be pointing to the location of the heap where the box is going to store our specified data. So again, it hides away that complexity. So the size of the heap data or in other words, the size of the data that we're storing inside the box is certainly subject to variance, but the size of the raw pointer inside the box that it is managing is going to be consistent in size. And that is one of the benefits of the box and what enables it to be used in certain situations. And that is that the box's size on the stack stays consistent. So the amount of memory it takes to store that raw pointer or any of the other supplementary things that go into a box that size is not going to change depending on the size of the heap. Data at the raw pointer is going to point to. So the analogy I made was the difference between a house in the real world versus a piece of paper with the address to the house. It doesn't matter if the house has five bedrooms or 100 bedrooms. Five bedrooms versus 100 bedrooms is going to take up a lot different area in the real world. But a piece of paper storing the address to that house can be of a consistent size. It's the exact same principle here. The boxes entry on the stack, where it's fundamentally wrapping and storing a raw pointer to heap data, remains the exact same. And that predictability is what the compiler is looking for whenever it wants to compile a program. So it's not going to care about how much dynamic, unpredictable memory is going to be required specifically on the heap when the program runs. It just cares about the fact that the box on the stack is going to be of a predictable fixed size. So using the box we implemented two recursive data structures the linked list and the binary search tree. So the box again is helpful when we are modeling or designing these recursive data structures, which are simply types that contain themselves. A linked list will consist of a node that may store effectively a connection to the next node in line, so it's a type that effectively contains itself. So a linked list is a data structure that consists of a linear collection of nodes. Each node stores a piece of data and a connection to the next node. And a node is simply that individual unit of data. And similarly, a binary search tree is a tree data structure that just means a data structure that hierarchically consists of parents and children, and that binary search tree enables us to gain the benefit of having a search whenever we perform a check. As a quick review, nodes to the left of each node in a binary search tree are going to have smaller values, and nodes to the right of each node are going to have larger values. But the key takeaway here is not all of the complexity of those methods. It's to showcase an example of where the box smart pointer is really helpful. When you are building these kinds of complex data structures out. There was a lesson or two where we tried to build it out with plain references, and we saw that while that was possible, there was a lot more complexity with regular references. We had to deal, for example, with things like lifetime annotations and dependencies between different nodes. As an own type, the box just greatly simplifies the design of these data structures in rust, and is typically the implementation you're going to see when looking at other people's code. After that, we practiced building our own simple smart pointer, and we introduced the Deref and Deref mute traits, which most smart pointers are going to implement. So the Deref trait defines what happens when code uses the dereference operator on the smart pointer. And remember, this is essential to a smart pointer because its whole purpose is to behave like a reference. It's technically an owned struct that is storing a pointer to some data. Typically, the dereference operator is going to grant us read access through an immutable reference to that piece of data. So the Deref trait requires a Deref method that returns a reference to some data inside the type. Again, this abstracts away the complexity of handling that reference. The box smart pointer is simply allowing us to interact with the box, but it's handling all of that complexity for us. We built our own custom box so we could explore exactly how to enable the dereference operator. The asterisk to just work with the custom box and grant us access to read its internal data through the use of a reference. Now remember that Deref trait also requires the definition of a target associated type, and that's where we provide an actual type that is going to serve as the return value of the Deref method. Technically speaking, the Deref method is going to return a reference to whatever target is, and the reason the rest team designed it this way is to enable a consistent method signature, where Deref will always return a reference to self target. But we can actually customize what target is when we implement the Deref trait for our own custom type, and thus we can actually vary up the return value in every single implementation to be a variety of different things, while honoring the Deref traits method requirements from the trait definition. We also talked about the complementary Deref trait, which allows you to not just access, but write content that is stored within your custom type. So once again, it provides not just the ability to read something through the use of a reference, but gives you back a mutable reference so that you can access that piece of data and then change it. That's exactly what a box smart pointer allows as well. You can use the asterisk syntax, and then the equal sign to overwrite the value stored inside a mutable box. We also talked about Draff coercions, which is a nice feature of rust that allows us to pass in a type that is not exactly the expected type of a function or method. So when given a reference to some type that implements the debuff trait, rust will convert it into a reference of another type if necessary. So the most prominent example of this is converting a ref string argument to a function into a ref str right? Those two are completely distinct different separate types. But yet if you pass a ref string into a function that defines a ref str, the code will compile deref. Coercion will cast effectively from one data type into the other. In fact, rust will continue performing this deref coercion until it hopefully gets to the intended parameter type. If it can't, then it will refuse to compile. But otherwise it will. And that process, as we saw, can actually happen several layers or levels down. So for example, we had our box smart pointer wrap a string, and we saw that we could pass a reference to a box, at which point VF Corporation would first extract the ref string inside it, or rather a reference to the string stored inside the box, and then deref course that one more time into the final ref str argument the string slice that our function was expecting. Next up we talked about trait objects. A trait object is an instance of some type. Doesn't have to be a single type, but can actually be many different types that implements a specific trait, right? It's called a trait object because it's some value that is honoring a trait, but what the specific value is can actually vary. And that adds a lot of dynamism to your program. Because trait objects enable code that does not have to explicitly couple itself to a single uniform concrete type. Now, in order to use a trait object, we have to store the dynamic type or the value of a dynamic type inside the box, and that is because the box can occupy a fixed size on the stack, satisfying the compiler's requirements to have a fixed data type of a predictable size. But again, the actual heap data inside the box, which corresponds to the dynamic trait object that can certainly vary in size. So what's going to happen is at runtime, the program is going to determine the actual exact concrete type or types that are being stored within the boxes, and then invoke the appropriate method on them. So for example, if you have three different types that all implement some method called x at runtime, the program is going to find those three types of values and call the x method on each one. But at compile time, we're going to be able to get away with storing those three different dynamic values of three different dynamic types inside, for example a shared vector. And by the way, that feature of calling the appropriate method on the right type at runtime is called dynamic dispatch. So once again, if you have, for example, a vector of a consistent type the compiler can optimize knowing that all of the data is going to be homogeneous, which means of the same data type. So technically speaking, those programs are going to be faster. But again, the advantage of trade objects is now we can have, for example, vectors of radically different types, five, ten, 100 different types, all of which are going to promise to implement the same trait. And the compiler will confirm that all of the values inside that vector, each one of them wrapped inside a box, by the way, are all going to be implementing that promised shared trait. All right. So more on trait objects. We saw that whenever we want to use them we have to add that special dim keyword, which is short for dynamic before the shared trait that all types will implement. So dynamic means unpredictable or capable of variance. And that's exactly the idea that we're capturing here. So we talked about two common use cases for trait objects. The first one is where we created a vector of dynamic varying types. So we declared the vector type. And then the actual type of the elements inside the vector was declared to be a box. And then we used the special syntax to declare what each box is going to be storing, which was a trait object, which again can be a variety of different dynamic types, all of which are going to implement a shared trait. In this case, that trait was called my trait, for example. Another use case that we explored was returning a result from a function where there is the potential for something erroneous to go wrong, and where there is perhaps the possibility for multiple types of errors, we can have the error variant wrap a box that again is going to wrap that dynamic trait object, because every single error type in rust is going to implement the shared error trait. We can actually return three or 4 or 5 different error types from the same function, as long as we wrap them in a box that promises to store some kind of dynamic type that's going to implement that trait. And we saw how that allowed us to have these really flexible functions that could return a variety of different errors, packaged them up nicely with a single uniform return type. Finally, we saw how we could define our own custom error types. We declare a struct that can store optional data or optional methods, and then we implement that exact same error trait which is found within the error submodule. This is the exact same trait that all error types in rust implement, and that error trait is a substrate of display in debug. So if you're going to implement it on a custom type, that custom type must also implement display and debug. And that is because each type must be representable or printable as a string. And so once we have that display or debug representation, we can transform that custom error struct into places where it needs to be rendered as a piece of text. And we used our own custom error alongside the built in rust errors like parse int error or IO error in our custom function to model a real world business domain problem. So a lot of rust. Error types are going to be technical. They're going to be the kinds of error that can occur in any type of rust program. This is how you can model the idea of something going wrong that pertains to your specific business or domain. Whatever you're building, there may be things that the rust compiler has no knowledge of as far as a value needing to be less than something, or greater than or equal to something, or contained within something else. So we can perform these checks and then return these custom error types that are modeling that idea of something going wrong. And again, if we create a result return value where the errors associated data is that box storing dynamic trait objects that all implement the same error trait. We can use rust error types alongside our own custom errors as well. All right. So that's all there is to cover in this lesson and in this section. Hope you had fun learning about your first smart pointer which is box. And I will see you in the next one.
