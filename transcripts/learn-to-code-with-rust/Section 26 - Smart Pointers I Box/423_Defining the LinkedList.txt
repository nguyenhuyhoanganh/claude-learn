# Defining the LinkedList

So let's build our linked list data structure in rust. We'll begin by defining a new enum. I'm going to call it linked list. And before I define any of the variants I want to derive my debug trait implementation. So right here hash tag square brackets derive debug. The very first variant that I will define within this enum is going to be empty. It will have no associated data. And the intent here is for this variant to represent an empty node within the linked list, which will mark the end of the list. So I may have a linked list that's going to have three nodes. Once we get to the last and final node, it's link so to speak, will not be to another node, but will rather be to a empty variant right here. We'll see how this all connects when we establish our other variant in just a moment. So the second variant here within our enum is going to be called node. Again a node is going to represent an individual unit of data. and within a linked list a node is going to consist of some value. And then a connection, or in other words, a link to the next node. So here I'll use the struct variant for node as a reminder that just looks like this. We're going to provide our curly braces. And we can define fields here very similarly to how we would do so within a struct. So the first field I'm going to have is value. And that will be the actual data that the node stores. And this can be whatever kind of data we want. For the sake of simplicity I'm going to go with a basic data type like an integer. Let's choose something like an i32. So each node within the linked list will store a value like an integer. And then this is the critical component. We also want to have a field let's say next that is going to direct us to or provide a link to the next node in the linked list. This is where the important question comes up. How do we model this? How do we represent this link Let's say I start with something like this. Well, I want my next field here to be potentially another linked list invariant, right? Because if that's the case, if I, for example, do linked list. My intent here is for my next field to be another linked list variant right. Either it's going to be empty, indicating the conclusion of the list. Or my next field could also be another variant of a linked list, which will be a node. And so a node can have a next field that leads to another node, which has a next field that leads to another node all the way until we get to the final node, where the next field will be the empty variant instead. So my hope is that this should be good enough, but let's take a look. What happens when I save the file and the compiler check runs? You can see unfortunately we have a violation. So let's hover our mouse over the violation. We can see right here. Recursive type linked list has infinite size. And if I try to run the program. We're going to get the exact same violation right here, and we'll notice some additional feedback right here, specifically in the assignment of the linked list enum to the next field recursive without indirection, right. So this is an example of a recursive data structure. We are trying to define a field here of next that we're trying to assign to be another linked list enum, which is the enum that we are currently within. Right. It is a recursive data structure because a field inside this enum is going to be the enum itself. And the problem that the compiler has here is that this process can continue to infinity. So rust is concerned that the linked list enum will have an infinite size. As a result, the compiler does not know how much memory a linked list will take up. Right? And that's because of the potential of an infinite number of nodes. Right? If this next field is going to be a linked list set to a node. Right. And then that nodes next field is another linked list node variant. That operation can continue forever, and thus the compiler can't figure out the exact size that this is going to occupy. It doesn't even know how much memory it's going to need to request from the computer, and thus the code is going to not compile. Notice the helpful error message here. Insert some indirection like a box, an RC, or even a plain reference to break the cycle. Right. So if you think about the word cycle, it means a continuous loop of something, right? Something repeating. And that's exactly what we have here. We have a continuous cycle where a linked list can potentially store a linked list, which can potentially store a linked list all the way until infinity. Right. An infinite cycle. So it's asking us to insert some kind of indirection to break that cycle. That's where that terminology is coming from. And recall that earlier on in the section we talked about that word indirection. When we're referring to indirection we're referring to indirect access which means rather than directly accessing a value or in this case directly providing the exact type of value, we instead create one more step of separation or one more step away from that concrete direct access. The way that we accomplish indirection is fundamentally by creating some kind of reference or pointer. Right. Because that means we are one step removed from the final data. We are not directly storing it, but instead we are storing a address that we can use to get to that original value. And that's what it's asking us to do here. It's basically saying, hey, I don't know what the size of this is going to be because it can go on forever. But if you give me some kind of reference, I know the size of a reference. I know the size of a pointer because it's going to be fixed, right? So instead of nesting and storing a linked list within a linked list, within a linked list, we can instead store a pointer or a reference to the linked list. So the way that we're going to model this is less about storing or nesting or containing another linked list or another node, but more so storing an address to another node, right? So the box smart pointer is absolutely perfect in these kinds of situations where you have some kind of recursive type, and thus the compiler cannot predict the exact size. And we need to use a type that will have a fixed, consistent size, where the compiler will know how much memory it's going to need. So here's all we're going to do. We're going to wrap this in a box. Now remember here in the enum declaration we're not actually creating an instance. We're just declaring the type declaration. So what we'll say is the next field is not going to be a linked list. It's not going to store that complete enum data structure that for instance, rather the next field is going to be a box. And that box is going to store a memory address inside it. Right. That will point to some linked list on the heap. So the way that we designate the type of data that's going to live within the box is through its single generic. So right here I will say next will be a field that will be a box, more specifically a box that's going to be storing a linked list. Remember, the box as the smart pointer is going to be the owner of that heap data that's going to store the linked list wherever it is placed. And the box is also going to handle the raw pointer internally. That's going to point to that data. So we have nothing in our main function, but we can still run the program to show you there are no violations. So this is now valid code. The compiler now knows the fixed consistent size of the linked list enum because it knows the fixed, consistent size of the box. Smart pointer. The size of the box smart pointer on the stack is going to stay the exact same, regardless of whether our linked list ends up with one node or 100. And that's because for any individual linked list node, it's not going to be storing a nested collection of 100 nodes or 50 nodes. Rather, the next field is now going to be storing a memory address fundamentally. It's ultimately going to be storing a box, right. But that box smart pointer is just a nice wrapper type container type around that raw pointer that is going to be directing to the next linked list node in line. Right. So it's almost like this literal link or connection or navigation direction or address to the next linked list invariant somewhere in memory, rather than an encapsulated stored concrete instance of the actual linked list node variant. All right. So to summarize our linked list node more specifically its next field will no longer store or contain or nest an actual linked list data structure and actual linked list instance. But more so it's going to store a pointer through the box to another linked list node somewhere else in memory. So this implementation is less about a node holding another node or containing another node, and more about a linked list node pointing to another node, or linking or connecting to another one. So hopefully that distinction is clear and hopefully you understand why the compiler is okay with this. It's because the size of this on the stack is now fixed. It's constant, it's not going to recurse forever, and thus it is predictable. And the program is not worried about an infinite memory requirement from a linked list storing an infinite number of linked lists. Right? So others to cover in this lesson. In the very next one, we'll practice creating a few instances of this linked list.
