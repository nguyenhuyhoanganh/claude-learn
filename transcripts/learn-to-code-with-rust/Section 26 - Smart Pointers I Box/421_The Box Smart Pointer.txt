# The Box Smart Pointer

So let's dive into our first smart pointer, which is a box. The Box smart pointer stores a piece of data of any type on the heap. So the box is an own type that is a container or a wrapper around a raw pointer that will hold the memory address of that allocated heap data. So the box smart pointer abstracts or hides away the complexity of dealing with that raw pointer to the heap data that you specify that you request to be stored. Let me show you a very basic example here. I'll declare a variable called my box and I will access box, which is available at the very top level of Rust capital B box. And then I'll use the new constructor and pass in an arbitrary value. Let's do an integer like 100. So normally rust would store an integer on the stack because it is a fixed size value, but because we used the box Smart pointer, rust will instead find a spot on the. Heap for our value of 100. And this new constructor function will give us back this type. This box smart pointer, which will again be a container or a wrapper around a raw pointer pointing to that heap location where 100 has been stored. So notice immediately the syntactical simplicity compared to previous examples that we've seen dealing with heap data. There are no raw pointers. There is no unsafe code. There are no references. There are no lifetimes. We get back a regular own type that operates very similarly to any other owned type within our program, but this is a clever layer or wrapper around that underlying raw pointer. Like most smart pointers, the box is built with a struct behind the scenes. I can prove this to you by command, clicking into the actual type right here. You can see the box itself is a struct. It's modeled or built as a struct. If I scroll up here, we're going to see a nice bit of documentation. It's a pointer type that uniquely owns a heap allocation of type t t. Here is the generic that the box accepts, which speaks to the fact that this box can store any kind of data that we want whatsoever on the heap. Now, it's worth noting that the box struct itself, including that nested root pointer, will be stored on the stack. So I want to draw the important distinction here, because it will help explain the box's power in future lessons. The size of the heap data that the box's raw pointer will point to can vary in size. The heap data can be large, or it can be small, and the heap excels at that. It is specifically for dynamic varying data, but the box's boxes size on the stack will always be consistent, and that's because the amount of memory that it takes to store a raw pointer doesn't change depending on the size of the data that the raw pointer points to. So going back to a real world analogy, if you're going to store an address on a piece of paper, it doesn't matter whether that address points to a one bedroom apartment or a 100 bedroom mega mansion. The size of the piece of paper for the address can remain fixed, constant, unchanging. Unvarying. Right? It's the exact same principle for the box size on the stack. It stays the exact same. It is the heap data to which the box's raw pointer points to. That is actually the thing that is subject to variance. So once again, if the concept is confusing, think back to a string, because both a box and a string are smart pointers, they both hold a raw pointer internally to some content on the heap, and the string is flexible enough to support. One character in sequence or 1000 characters in sequence. Right. That's the advantage and hides away the complexity of that heat management away from you. The difference, of course, is that a string stores a raw pointer to text, while a box can store a raw pointer to any type of data whatsoever. This right here is an integer, but it can be whatever value we want. It can be any kind of number, a float, a boolean, a struct, an enum. Right? We don't have to worry about how the memory is being handled under the hood, because the box is just a nice little layer above it, but the size of the raw pointer at the box is going to hold within itself. Encapsulate within itself will remain the exact same on the stack, regardless of the type of data or the size of the data that has been allocated on the heap. So let me change this value back to 100. And the next thing I want to cover is this. Earlier on, we defined a smart pointer as quote, a type that behaves like a pointer. We haven't explored that incredibly important feature yet. So this box appears to be a regular owned type. But let's check out what happens when I apply the dereference operator to it. So on the very next line I'll do a print line. I'll provide some dynamic content. I'm going to provide my box. And before it I'm going to use my dereference operator, the asterisk which is shift plus eight. Now again this is something I typically do with a reference right. And in the previous lesson we applied it to a raw pointer which was the same principle. The dereference operator goes to a memory address and reads the value at that memory address. So how are we able to apply dereference operator to an owned type like a box right. This box is actually a struct. We saw that a few moments ago, but yet we are able to apply the regular dereference operator and check it out. It works exactly as you expect. It's going to give you 100. So we'll see the technical implementation for this later in this section. But the reason this is possible goes back to the fundamental definition of what a smart pointer is. A smart pointer is a type that behaves like a pointer. Our mybox smart pointer is behaving exactly the same as a reference, or a raw pointer would, but it's not actually that, right? So what is happening here, as you might guess, is that when we apply this dereference operator to our box own type, it's actually going to dereference the internal raw pointer that the box is storing. It appears like we are interacting with the box itself, but it is very gracefully, elegantly handling that interaction with the nested raw pointer. It is getting that raw pointer. It is going to the memory address that it is storing. And then it's reading the value there and we're getting 100. Even though once again notice no reference syntax here, nothing fancy. We're interacting and applying the dereference operator to an owned type of box to a struct. And it's working elegantly to get us the data that is stored at the heap location that the nested raw pointer holds. The box type implements both the display and debug traits, and they will print out the string representation of the stored value at the heap address. So the exact same thing we have right here. So for example here I can print line I can even embed my box in here. Or of course I could pass it in afterwards right here. And notice that here once again we are providing the own type itself. We are not applying the dereference operator literally, but behind the scenes it's going to accomplish the exact same thing. Rest is going to see a box, it's going to internally go into the box and find the raw pointer that is inside. It's going to follow that address to that location on the heap. It's going to read the value there and we're going to get 100. Once again, we don't even have to worry with these two syntax options with using the dereference operator at all. Remember there is a pointer living in here. But the beautiful part about this syntax once again is that we don't have to worry about it. It just works. So the display and debug trait implementations perform that dereferencing automatically. They navigate to the memory address on the heap, and they locate and print out the stored value of 100. Here's an example. Of course with our debug trait you can see we're going to get the exact same thing. Now the box type is an own type. It does not implement the copy trait. So regular ownership rules will apply. So for example here I can declare a your box variable and assign it to the value of my box. That is going to invalidate my box because your box is the new owner. So now you can see right here if I try to print out my box, that of course is not going to work. You can see the violation while your box is going to continue to work just fine. So what this means again, is that your box has become the new owner of the box, which again is the new owner of the container, the wrapper, the over wrapping layer that is going to encapsulate the raw pointer pointing to the stored heat value of 100. So to summarize, the box is an owned value and then your box is its owner. So when the your box variable goes out of scope at the end of the main function, it will clean up both the stack entry, storing the raw pointer and the heap data of 100. We can treat box like it's a regular value that follows the standard rules of ownership, or we can also dereference it elegantly to get to that raw pointer's contents. But the point is, we do all of this without having to write any unsafe code, or worry about the internal complexity of the stored raw pointer. The box smart pointer is an abstraction over the underlying reference over the underlying pointer to heap data. It hides away and simplifies that complexity. Now this is a contrived example, because fixed size integers like 100 are cheap to copy, and they are stored on the stack. For that reason, there's no reason to store them on the heap. But this example demonstrates that the box can store whatever data we want, and provide a beautiful interface to hide away that complexity of managing a raw pointer to heap memory. It's going to be a lot easier to interact with a box than it is to interact with raw pointers. And also once again, more safe. So we'll see an example of where this specific type can be very useful when implementing a concrete data structure in the following lessons.
