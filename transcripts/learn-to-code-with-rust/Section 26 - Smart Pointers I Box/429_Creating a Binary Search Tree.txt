# Creating a Binary Search Tree

So let's create our binary search tree in rust. I do want to warn you that this can be a pretty complex topic, because it's going to involve both a recursive data structure as well as the concept of recursion itself. As a reminder, recursion is when a function or a method calls itself within its own body. So in this example, there is going to be a lot of complexity. So if it all doesn't make sense at first, I just advise you to be a little bit patient, wait until the end of the lesson and look at the complete code sample that you see, and then trace through the individual steps of what code is going to run. Every time that we keep adding or inserting a new node into our binary search tree. And then hopefully the greater picture becomes a lot clearer. So to review, at a minimum, every node within a binary search tree is going to store some value. For us. That's going to be a plain integer After that there are several variations. A node can have a left child, or it can have a right child, or it can have both a left and a right child. Or it can have no children at all. And we have to account for all of those scenarios in our design. So let's begin by defining a new enum called Binary Search Tree. And immediately I'm going to derive my debug trait implementation above. So our enum is going to store two total variants. The first one is going to be empty just like we had within our linked list implementation. This variant is going to represent the end of a given tree branch. It's not going to store any data, it's just going to be there to represent nothingness or absence. The conclusion of a given tree branch. In fact, when we create a binary search tree from scratch, it's not going to have any nodes. So it's simply going to be this variant. Now the second variant is going to be called node. And I'm going to make that a struct variant so that I can store multiple pieces of information. So here I'll provide my curly braces for that. And our node variant is going to store three total fields. The first is going to be value. That's going to be the actual piece of data that it is storing. So for us that's going to be an integer I'm going to write this as an i32. And here is the critical component. We're also going to define two additional fields that are going to be left and right. So I'm going to write left and right. We're going to talk about what the types should be in just a moment. So if we have a given node in our tree say node with the value five, right. That node can have either a left child or a right child or neither or both. Now, in the case that there is no actual node on the left or right, That's going to be our empty variant right here. That's going to model that there is no literal child. It's more so that the empty variant here is a stand in to represent that left or right child if it is absent. And in comparison, if we do want either a left or right child or both, those children themselves are going to be similarly node instances down below here. So this five node can have a left child. That's going to be another binary search tree node with a value of two and say the right child of this five node, if there is one, is going to be another binary search tree node variant storing the value eight. Right. So I can begin here by writing out the left and right are going to be storing either empty or node. So a binary search tree enum variant. Of course as we know throughout this section, this is immediately going to trigger a compiler violation. And as you might guess, it's because the compiler thinks that this is a infinitely recursive data structure. It says, okay, you can have an instance of this enum, which is going to be node, and that can have left and right fields that are also going to have the potential to be nodes which themselves are going to have left and right fields which have the potential to be the node variants. And that's going to continue till infinity. So of course, the way we solve this problem with recursive data structures is now hopefully familiar. We're going to wrap these in the box smart pointer. So right here I'm going to do box like so. And right here I'm going to do box like. So. So we are saying that left and right are going to be a box smart pointer wrapping one of the two variants of the binary search tree enum. So I want to clarify and repeat that. If we have no children, then left and right is simply going to be a box storing the empty variant. And if we have a left child, then box is going to be storing a node variant which itself is going to have its own value and left and right fields. And if a given node is going to have a right child, that's going to be a box storing another node variant, which itself is going to have value left and right. But for any given node we can have either both children either left or right, or no children at all. So next let's implement a constructor function called new that will create an empty binary search tree. So all that this new constructor function is going to do is just return the empty variant right here. We're going to take care of populating the binary search tree with another method that will define in just a moment. So right here I'm going to add an ample block for my binary search tree. In there I can define my constructor functions or associated functions as well as my methods. For now I'll define a associated function called new, which is going to return an instance of the binary search tree enum. So I can declare the return type here as self. And here I'll simply return the binary search trees. Empty enum variant. This is going to create an instance of it, and the empty variant does not require any additional data to be created. So it's just going to be created right here and returned right here. So for example, here in the body of main, I can declare a tree variable and I can take my binary search tree enum call the new constructor function. And this is going to give me an empty tree. It doesn't technically have any nodes within it. It's just going to be the single empty variant right here. So I'm going to write out tree in debug format. Pretty printed. In fact you can see we now are starting with an empty binary search tree. So the next part is where we're going to write the most complex portion of our logic. So once again be patient. If some of these concepts are a little bit tricky, the next thing we're going to do is define a new method called insert that we can invoke on a binary tree instance such as this one right here that's it's going to insert a new value. It's going to create a new node with a given integer piece of data. And then it's going to find the right place within the tree where it's going to place that node. And the code should be durable enough that it should be able to work on a completely empty tree like this, or a tree with one node, two nodes, five nodes, etc.. We're going to have to be very clever in how we write our code to ensure that that is the case. But first things first. So here let's define our method. I'm going to call it insert. And I know that I'm going to be invoking this method on a sample binary search tree instance. So for example here we can imagine us calling the insert method on the tree and passing in the numeric value that we want to add to the tree, let's say the value five. Sometimes it is easier to see the invocation code, because that gives us a little bit of a hint on how we're going to define our method definition. So we're going to need to first receive the actual instance that the insert method is invoked upon in some way, so I'm going to receive it as a mutable reference right here. Ampersand itself. I don't want to take ownership, but I do want the permission to mutate because as we're going to be adding new nodes, that is a mutative operation, because at some point we're going to be modifying the values in the left or right fields right here. And then of course, the second argument is going to be the actual new value that we're going to be passing in. And this is going to be an i32. All right. This method is not going to return anything. And so now here is what we're going to do. We're going to use the match keyword to match on self. Remember self is the instance that the insert method has been invoked upon, which will be one of the two binary search tree enum variants. So self is going to be either empty or node, and depending on which one we have, we're going to take two completely different pathways of logic. Let's take care of the first scenario, which is hopefully the easier one. Basically the one that we have right here that we call the insert method on a tree that is empty. So more specifically, that's going to be the empty variant of our binary search tree enum, which self is an instance of. So notice here it's going to tell us hey you haven't covered the two situations. You need to cover both empty and node. So I'm going to say if self happens to be the empty variant, and then I'll provide my match pattern right here with that arrow syntax. And this what do I want to do. Well the first thing I want to do is I want to create a new node that's going to be storing my new value. In the example below, that's going to be the value five. And then I basically want to replace my empty node right here with the new node that I've just created. That's going to be storing my new value of five. Let's take care of the creation part. So right here I'll do binary search tree. And I'm going to create a new node Variant of it. Right. This is the logic right here. And this is going to require three fields in order to be successfully created. The first one is going to be value. And that can simply be our new value parameter from above. And the two other fields are going to be left and right. So in this situation, since we're inserting one node at a time, we're not going to have a concrete left and right child node yet. That may come on a future insert method call, but it's not going to come at the moment that we're creating our first node, say with a value of five. So for now we're going to make both left and right a box. That's fundamentally what the type is. And that box has to contain or wrap a binary search tree enum variant. And of course that's going to be our empty variant. There is no left child yet for this new node with a value of five. There is no right child yet with this new node with a value of five. So in here I'll take my binary search tree enum from above and I'll choose the empty variant. So the box is going to be not really a node, not really a child, but a symbolic representation of a missing or empty left child. And I'm going to repeat the exact same logic with my right field. And so I'm going to do box create a new box with new. And this is going to be a right child or more specifically a missing right child. That's going to again be the empty variant. Now this syntax right here is going to create or instantiate that new node with a value of five. But it's not actually part of our grand tree yet right. We're going to invoke the insert method on our tree which is empty. And we're going to create the new node of five right here. But now we need to take this empty node. That tree currently is that we've matched on right here with self. And we need to replace our empty variant with this new node that's going to become our very first node within the tree. So the syntax for that is going to be a little bit interesting. But it's a combination of concepts that we've talked about earlier on in the course. So the syntax looks like this. We're going to do an asterisk self which is dereferencing the reference of self that is getting the memory address where self is. Remember in this situation, self is the instance upon which the insert method is called. So in this scenario it's going to be our empty binary search tree variant. Right here. We're going to get that location with an asterisk. And then with an equal sign we're going to overwrite the data at that memory address. We have permission to overwrite because we borrowed a mutable reference right here. So we're simply going to go to this empty variance memory location. We're going to swap or replace the empty variant there with this newly created node instead. And don't worry, that spot in memory is going to have enough memory to contain either variant of the enum. The rust compiler takes care of that. So basically here we have our empty variant. The very first time we call insert and pass in a value of five. We're going to locate the memory address where this empty is being stored, and we're going to replace it with the node variant instead, storing our new value of five and having no left or right child. All right. So here, if we can imagine our tree looking like this, like it has nothing within it, after we call this method the first time, we should see a tree with exactly one node storing a value of five, and that node is going to have no left child and no right child. I'm just representing those two hypothetical children with M for empty. So we're going to have one total node of five with a empty left child and an empty right child. And that's going to be handled by this pattern within my match statement. But you'll notice our code is not going to compile just yet, because we also have to account for the second possibility for the second variant of what self could be, which is going to be an actual node variant. So imagine here that we invoke insert once again and we pass in a value like two. So now what we're trying to do is add two to an existing tree that has the value five. Right. So now when we're going to invoke insert on the tree, we're not invoking it on our empty variant which we previously had. We're now invoking it on the node variant with a value of five. So this code is not going to run the second time we invoke insert. So now self also has the possibility within this match statement to be the node variant. So we have to account for that as well. So right here I'm going to say I want to account for the other variant that is a compiler requirement of the match statement. And that is going to be the binary search trees node variant. So I'm going to write this out. You're going to notice VS code is going to auto complete after the node variant name. It's going to write the curly braces and write out value left and right. And then I'm going to provide the actual match pattern arm which is the arrow and the curly braces. As a quick reminder of what this syntax does. That means whenever we match on self. Being a node, we're going to have access to the value left and right fields within the body of this procedure. You can almost think of this like a function that's going to run whenever self happens to be this variant of node. And so within here we're going to have access to value left and right. So you can imagine if we are calling insert on this tree with a value of five then value is going to be the value five. And left and right are going to be the box storing the empty left child and the empty right child. All right. So what are we going to do here. Once again it's going to be something really interesting. What we need to do here is insert our new value. Like I said, let's say we're trying to insert a new value of two. But where we're going to add it is going to depend on how this number compares to the current node that we are within, which is five is to equal than five, is to less than five, or is two greater than five? Remember, if the value that we're trying to insert is smaller than the current node's value, then we want to add the new value of two in the left subtree of the current node. So on the left hand side of five on its left chop. And conversely, if the value that we're trying to insert is greater than the current node's value, say, we're instead trying to insert a value like eight. In that situation, the value is greater than five, so it should be the right child of the node with a value of five. So depending on what we're trying to add and its relative comparison to the current node's value of five, that's going to determine where we place it within the tree. So I'm going to need to perform a comparison between the value in the current node that I'm within, and the new value that has been passed in this new value. As a reminder, represents the new integer that I want to add to my tree, and I need to compare that new value with the current node's value as it has been stored. So in order to accomplish that, I'm going to need to bring in another enum into this file scope. This comes from the standard library, and we talked about it a little bit earlier on in the course. So within the standard library in the CMP or comparison module there is an ordering enum. And the ordering enum is going to have three total variants equal less and greater. And they reflect the three possible possibilities that can happen whenever we compare one number against another. The numbers are either going to be equal, or one is going to be less than the other, or one is going to be greater than the other. So right here it's a little bit complex because we are within a match statement, and we're specifically writing the logic to execute in case we have the node variant for self. But in here I'm going to apply another match statement. And what I'm going to do is I'm going to take my new value, which is the new integer that I'm trying to add to my tree that is an i32. And that has a method called CMP, which is short for compare. And what this method expects as an argument is the value to compare the new value against. That's going to be the current node's value represented by the value field, which I have access to. Now this cmp or compare method is going to return an ordering invariant depending on how these two values compare to one another. So I'm going to handle that right here in its own pair of curly braces. You can see right here the compiler is telling me you're not accounting for the three possible variants of the ordering enum that the compare method is going to return. You have to tell me what to do if it's less equal or greater. So the first one that I want to cover here is if the values are equal, which is going to be the ordering enums equal variant. So believe it or not, in most data structures classes, the element of what to do when you're trying to insert a new value that is equal to a given node. Usually it's actually just ignored. It's discarded. We don't store duplicates of any kind within the binary search tree. We're going to follow a very similar approach here. Just for the sake of simplicity. There are more complex implementations of binary search trees that deal with duplicates, but for now we can say, hey, if you're trying to add a new value to the tree that already exists within the tree, just ignore it. So all I'm going to do in this pattern, which I'll designate after my arrow here, is just return an empty tuple. We can think of that as a no op, or in other words, an operation that accomplishes nothing. This is not going to raise a panic. This is not going to fail, but it's also not going to do anything to the tree. It's just going to leave it as is. The method call is basically going to be completely useless. Now, where this actually has value is when we're going to account for the other two ordering enum variants starting with less. So to clarify, this is going to be the variant that is going to be returned when our new value is less than the current node's value. So in other words, when the new number that we are trying to insert into the tree is less than the current node's value, that's when this branch is going to be hit. And here's the code I'm going to write. It's actually going to be a recursive call. And it's going to be pretty complex and complicated right now to figure out why. But I'm going to write the code out. And then we're going to go through a valid example of actually trying to insert multiple nodes into the tree. And hopefully then the code is going to make sense. In the case that my new value that I'm trying to add is less than the current node's value, I'm going to take the left field that I have for my current node. I'm going to call its insert method because remember my left yield is going to be a box that's going to be wrapping a binary search tree in a variant. And regardless of whether that variant is empty or node, it is a valid binary search tree invariant. So it's going to have access to the exact same insert method that we are within. So I'm going to call the insert method recursively on the left field of the current node that I am in. But only if the value that I'm trying to add is lower than or less than the current node's value, and I'm going to pass in new value. Basically what this is doing is it's saying hey, left child that doesn't exist, or perhaps left child that does exist. You take care recursively of figuring out where to place the new value. I don't know where it's going to end up, but I know that the new value that I'm trying to add is less than the current node's value, so it certainly shouldn't end up on the right side of the tree. It should end up on the left. So it's going to call the insert method on the left child. That's going to trigger a recursive call of insert, which we'll walk through step by step in just a moment. So for the third and final ordering enum variant which is greater. If my new value is greater than my current node's value, I want to add the node for my new value on the right hand side. So I'm going to follow the exact same principle. I'm going to take the right field of the current node. I'm going to call it insert method and pass in the new value. And those are going to be recursive calls. And with that if I save you'll notice everything is going to format. But there should be no compiler violations. And we should be good to go to test this code out. So the reason that we have a violation in main is because we're trying to mutate the binary search tree with our insert method, but it has not been declared mutable yet. So very quickly here I'm going to declare my mutable keyword. And I want to show you these things step by step so we understand exactly what's going on. So I'm just going to comment out these two lines so we can review everything from the beginning. So we're going to begin with an empty tree. An empty tree is just going to have the empty variant okay. Then we're going to insert a new value of five. And right now our tree has no nodes whatsoever. So when the insert method runs for the very first time, it's going to run being invoked on this empty variant. And we're going to check what self is. We're going to match this arm right here because we have an empty variant right here. So we're going to create a new node with a value of five. And we're not going to worry about that. Nodes left and right children they're going to be empty. But we're going to create that new node with a value of five. And we're going to replace this empty variant with that newly created node storing a value of five. So after one insert method call notice what's going to happen here. We have a tree consisting of one node. Here it is five. It has no left child and it has no right child. Let's invoke the insert method one more time and let's pass in a simpler value here. Let's start with two. I want us to take a look at what the result is going to look like first. And then we'll walk through the code step by step. So notice at the very top we're still going to have the exact same node value of five. That is the first node we inserted, but now its left field or its left child, which previously was the empty variant is now going to be a newly created node storing our value of two. Let's talk about why that works the way it does. So when we call insert with a value of two we're going to call this method right here. And self is now going to be not the empty variant but actually going to be the node variant. That's because it's the node with a value of five the topmost node. So we're going to get access to that node with a value of five. Three fields value left and right. And we're going to compare that value of five with our new value of two right. So new value is going to be less than five. So it is this ordering enum variant that's going to be matched. And we're going to execute this code. So we're going to take left which was previously the empty variant here within our top level node of a value of five. And we're going to take that left child and we're going to say invoke the insert method on that left child on that empty variant, in other words, and pass in the new value of two. So within the topmost insert method call, we're invoking another insert method call, but on another node within the tree. More specifically, on another child within the tree, we're calling it on the left child, which happens to be the empty variant. So now we're in our second insert method call. We're inside our recursive method call. And so now when we have self it is no longer the value of five. It is the left value which is the empty variant. So we're going to match on that empty variant right here. And that is the time where we're going to say, okay, now it's time to overwrite that empty variant. We are no longer within five. We are now within its left empty variant or its left child equivalently. So we're going to create within our recursive insert method, call our new node with a value of two and overwrite the left child of five, right. So when insert runs a second time, we create this node right here and then. Left was previously empty, but we replaced it with our newly created node. It's the exact same way that five replaced what we previously had here. Now two is previously replacing the empty variant that was the left child, or the left field of the topmost node with a value of five. Let's now observe the exact same process in reverse. I'm going to once again take my tree, call the insert method and pass in a value like eight. All right. So now we're once again calling insert from the topmost level. Let me just run this so you can see what happens. Now we're going to have our topmost node with a value of five. It's going to have both a left node and a right node. And it's the right child that's going to be holding the new value of eight. So once again, let's walk through it from the beginning. We call insert on our binary search tree, which always starts at the top. So we're going to start with this node right here. That is what the insert method is being invoked upon. So we're going to match on self. And that variant right here at the top is not the empty variant. It is the node variant. More specifically it is the node storing a value of five. So we're going to get access to that node's three fields value left and right. And we're going to compare our new value of eight with the current node's value of five. Now this time around we run into a little bit of different logic because eight is going to be greater than five. So we're going to run into this match arm instead. So now instead of calling the insert method on left, which was previously accomplished by this method call, we're instead going to call it on the right empty variant. So then once again we enter into a second recursive call. But this time around we've called insert on the empty variant that was the right field of the top node with a value of five. So now we called it on this equivalent right here before we replaced it right here in the output right was equal to empty right. So we're going to match on that right fields empty variant. We're going to create our new node with a value of eight. And we're going to replace the previously empty variant or equivalently the right field of the topmost node with a value of five. We're going to replace its right field with our newly created node. It's going to store the value of eight. So now this as you can see is extending a total of one level down. Or it has a height of two in the sense that we have five. And then it has two children, two to the left, eight to the right. But as we invoke additional insert method calls right here, for example, let me add something like four and let me add something like 13. You're going to see that the logic is going to recursively continue to apply. So if I do this and I call this, we're going to see that our tree is going to continue to grow in size. And those child nodes like the two and the eight that we just added, are themselves going to expand to have their own right child in this case, for both of them to continue going as many levels or layers down. So each time around, we're going to recursively say, hey, I'm going to compare the current node's value with the new value that I'm trying to add. And based on whether it is smaller than or greater than the current node's value, I'm going to delegate the responsibility of figuring out where it belongs in the tree to the next child in line. So five is going to say, okay, I don't know where four should go, but I know it's less than me, so I'm going to call it on the left node. That's going to call the insert method on this node with a value of two. Two is going to apply the exact same logic of comparing the new value of four with two. Four is greater than two. So this node with a value of two will delegate to calling the insert method on its right child, previously empty, that enters the third recursive call of insert where we match on empty. That's going to be for this right field right here. That's where we create the new node with a value of four, and overwrite the right field of the node with a value of two. Lots and lots of complexity here. So for extra practice and to actually see the utility of the binary search tree when it comes to search, let's define a second method called contains. And that method will accept a numeric value and return a boolean a true or false if that target value exists within the binary search tree. So this method is going to actually perform the search. This is the whole benefit of a binary search tree. We ideally have this balanced data structure that is optimized for finding a value in this efficient algorithm that's going to keep splitting the tree or the search area in half with every comparison. So what our code will need to do is traverse the tree from the top and basically use the same rules. If the current node holds the target value that we're searching for, we'll return true to indicate that the target is contained within the tree. Otherwise we're going to continue down either the left or right subtree, depending on whether the target is less than or greater than the current node's value. So right here I'll define my new method I'm going to call it contains. And we're not going to need to mutate the binary search tree in any way here. We're not going to be adding nodes. We're simply going to be searching for an existing value. So I'm just going to borrow a immutable reference to my instance to self. And then I'll define a target parameter set to an i32. And this represents the value that I'm looking for within the binary search tree. And I'm going to return a boolean here. So true once again is going to indicate that my tree contains the target value. All right. So our code here is actually going to be somewhat similar to the code above. We're going to begin by matching on self which is going to be matching on a variant of the binary search tree enum which can be either empty or the node. So the first one again is going to be empty. Now this one is going to be pretty simple. If we happen to land on an empty node, let's say for the sake of example, the simplest possible case is if you have a completely empty binary search tree like you had here. So if you have an empty variant and you're looking for a target within it, there's no content, there's no actual data within the empty rand. So we know immediately that our target value cannot be contained here. So I'm immediately going to return false. The only way that we can actually locate our target within the binary search tree is if there is a node variant in the first place, and furthermore, if that node Value is equal to the target value, but there's no way that we can find our target value within a empty variant. All right. So now the second variant that we have to cover within our match statement is going to be the node. So right here I'm going to write node. And once again once I press enter we're going to get VSCode to autocomplete the syntax the curly braces and the writing out of the three pieces of data or three fields that I can extract from any given node the value field, the left field, and the right field. And in here I'm once again going to use match and do something very similar. I'm going to say I'm going to take my target, which is the number that I'm searching for. And I'm going to compare it with the current node's value. All right. And here I'm once again going to have to deal with a match statement. So I'm going to write this logic out like so. So once again the compare method is going to return three possible variants Variance equal less or greater. Let's first target equal because this is the one where we have a success right? If we have a node variant and we're looking for a target, and we compare the equality of the target number with the node's value, and the two are indeed equal. Well, that means that our target value exists within the binary search tree, right? It is in this case that node's actual value. We don't want to target the node. We don't want to worry about its location within the tree. We just want to return a basic boolean if our target has been found, and if it is equal to some node's value, then it obviously exists within the tree. So right here I can return true. Now we also have to cover the other two possible variants of the ordering enum. So right here I'm going to go with less to kick things off. So what does it mean if our target is less than the current node's value. It doesn't necessarily mean that our target is not found within the tree. It may still exist within the current tree, but it certainly is not the current node. And here is where the magic of the search algorithm comes into play. If I know that my target value is less than my current node's value, I know that my target cannot be found anywhere in the right subtree of the current node, because all the values in the right subtree of the current node are going to be greater than the current node's value. And I already know that my target is less than my current node's value. So if it is less than, what I'm going to do is I'm going to take my left field of the current node. That's what I've accessed right here. That is the only subtree where there is still the potential for my target to be found. And what I'm going to do here is I'm going to write left contains I'm going to invoke the exact same method that I'm within, but I'm going to call it now on the left field on the left subtree. And I'm once again going to pass in target. All right. We're going to see once again how this plays out in just a moment. And as you might guess there's going to be a complementary line here for the third possible variant within the ordering enum which is going to be greater than. So if my target value that I'm searching for within my tree is greater than the current node's value, I know that there's no way that my target is going to be found in the left side of the current node, right? The left subtree of the current node. Because all of those values are going to be less than that current node's value. And I know my target is greater than the current node's value. So this logic is going to be the inverse. If my target is greater than my current node I want to continue the search in the right subtree. So I can take my right field of the current node call its it contains method, which is this exact same one that we're in and pass in target. And the way this is going to work is this is going to continually progress further and further down until we either get to a place where we get an equal, or we're going to either continue navigating down a left or right subtree up until the point that we get to the end of that relative, left or right subtree and land on an empty node. And if at that whole point, through all those recursive calls, we have not found the target, then we're going to definitively return false. So this is not going to lead to any kind of infinite recursion. We're just going to keep invoking contains on the left and right child until we either get to the end of the tree and we return false, or we land on some node where our target is equal to that current node's value, and we return true instead. And that should do it. That will be our final implementation. So now the only thing that remains is giving this a test run within main. So down below I have our constructed tree. Let's practice invoking the contains method and checking if it correctly returns the proper boolean value. Let's begin with a value that we know is actually contained within the tree, say 13. You can see it's nicely nestled a few levels or layers down. So I'm going to check whether my tree contains the value of 13. And let's first of all get it true. And if it works, and indeed it does, we can then walk through the logic of how exactly the contains method is working. So once again we're going to call contains on the very top of the tree. So we're starting here at the the root right. We're going to call contains we're going to say we're looking for the value 13. In here we're going to match on self. And self is of course not going to be the empty variant. It is an actual valid node storing the value five. So we're going to match on this pattern or arm right here. We're going to get access to the current value of five as well as the five nodes left and right chop. We're going to say okay is the target value of 13 equal to five? The answer of course, is that they're not equal. In fact 13 is greater than five. So because we know that 13 is greater than five, we know that nothing in the left subtree of five can possibly contain the value 13. Right. And if you take a look here, of course the values in the left subtree are two and four. They're both smaller than five, so there's no way that 13 could possibly exist there. If there is a possibility that 13 exists within this binary search tree, it would have to be on the right subtree of five and its right child. So once again, the purpose of binary search tree is that we've eliminated this entire child and all of its children as well. In case this continued multiple levels or layers down, we don't have to worry about searching in that segment of the tree, because we know everything on the left hand side of five is going to be smaller than five. So here we're going to hit this greater. Variant. And we're going to do is we're going to take the right field of five. Right here which is this node right here storing a value of eight. We're going to call the contains method on that. That becomes our first recursive call. We began by calling the contains method on the top node. But now we've called the contains method on this right node. We are asking effectively to transfer the responsibility of continuing to search for the target value of 13 within a different section or node of the binary search tree. So now the contains method begins anew recursively, still with the same target of 13. But now with this being our node that is represented by self, because that is what we invoke the contains method on. So once again we're going to run the match statement on self which is this node right here. And the variant is not empty. In fact it's going to be a node once again storing the value eight as well as the left and right fields. So now we're going to compare the quality of our target value of 13 with our value of eight. The current node's value. So 13 is once again going to be greater than the value of eight. So we know even though the current node has no left children, if it did have left children, we'd know that there's no way that they could possibly contain the value 13, because 13 is greater than eight. So now in our second contains method call. In our first recursive call, we once again have an evaluation of greater because 13 is greater than eight. So we call the contains method on the right child of the current node. So the right child of the node with a value of eight is this node right here with a value of 13. So now we're in our third contains method call. Continuing all the way down we once again match on self. Self is now once again this node right here. So it is not the empty variant. It is this node variant. different. So we get the value of 13 as well as that node's left and right fields. We now compare the comparison or equality of our target of 13 with our current node's value of 13. They are indeed equal. So we know that 13 is found within the tree, so we return true. The exact same logic is going to apply, but in reverse if we look for something in the left hand side. So for example here I can look for a value like four. You can see four right here should return true as well. And indeed it does. The way that works just very quickly once again we call contains on the tree. That's going to start at the top with the node with a value of five. Four is to the left of five. So we know it cannot possibly exist anywhere in the right subtree of five. We also know that it is not five itself, so we continue our search in the left subtree. We then get to the node with a value of two. We are looking for four. Four is not equal to two. We know anything in the left subtree of the node with a value of two cannot possibly be four because four is greater than two. So we continue our search in the right subtree, and then we locate four on our third recursive call because four matches this node. And finally, just to give you an example of where we have a value that does not exist at all, let's say 21, that should give us a false. The way that's going to work is just going to say, okay, I'm going to compare 21 with the value of five. 21 is not equal to five. It can still potentially exist in the right subtree of five, because everything in the left subtree is going to be less than five. So we're going to eliminate this search area and continue the search for 21 right here. At that point we're going to compare 21 to 8. 21 is greater than eight. So it cannot exist in the left subtree. But it has the potential to exist in the right subtree. So then we call the contains method with the node right here. We once again compare 21 with a value of 1321 is greater than 13. So now we call the contains method on the right node right here that is an empty node. So when we call the contains method on that we immediately return false. That I believe is going to be the fourth total recursive call, but it returns false so that bubbles all the way up to the top. And now we get a false. Because 21 is indeed not found within the binary search tree. All right, so lots and lots of complexity in this lesson. Hope you had fun and hopefully the big picture makes sense. Once again this syntax is really, really complicated because it involves match statements. It involves a recursive data structure that has not one but two boxes, both of which are going to be the exact same enum that we are declaring ourselves within. So this is a recursive data structure, which is one of the things that a box gracefully enables. You can only imagine the nightmare of what this would look like to build out with something like references in lifetime's boxes made it easier. But then this also, of course, involves the complexity of recursive method calls, right? Our insert method calls the insert method. Our contains method calls the contains method. Granted, they're calling them on different nodes within the exact same tree, but it's still fundamentally the exact same principle. We have to think about this problem in sort of smaller steps or substeps small divisions, right? We're kind of breaking the problem down into smaller steps as we traverse the layers or dimensions of this tree, navigating downwards to try to either add a node in the right position or locate a value at a given node. So slow down, rewatch the video again and half speed. If you need a little bit more of an explanation here, practice building up this tree from scratch. Practice adding a variety of different values to it just so you see how they distribute themselves around any given node. Hope you had fun! And with that I will see you in the next lesson.
