# Box vs Regular References II

So next up, I want to show you an example where an implementation of this linked list using references won't work at all. A box will be required. So say that instead of keeping everything within the main function, say that we wanted to instantiate these respective linked lists within a separate helper function. Let's take a look at what that's going to look like right here. I'm going to declare a function called create list. And my intent of course will be for this to return a linked list of some sort. Let's start with the reference. And as we can see right here, we are going to get some issues with the lifetimes and the types. But let's hold off on that for now. So right here is my function body. And all I'm going to do is I'm just going to copy and paste all of the code I previously had in main into here. And of course, my intent here is that instead of printing out the first node after I've created it, I simply want to return the first node. So I'm going to write out first node like so. And this code is not going to compile no matter how hard we try. You can see right here we are getting a violation missing lifetime specifier. I'm just going to arbitrarily create a lifetime right here in my angle brackets called tick A. And I'm also going to attach that lifetime right here. And then technically speaking this is also going to ask us for the concrete type filling in for the generic T. Since we have our values here being i32, I can designate it as being i32. And once I save this, we're actually going to see the real issue pop up, which is right here cannot return a value referencing local variable second node. Okay. So what is the issue here. Because our first node stores a reference to second node, we are storing a reference that will become a dangling reference at the conclusion of the create list function. Right. Because we are borrowing a reference to second node. And remember second node as the owner will reach the end of the function scope at line 30 and then deallocate. There is no transfer of ownership of second node to the next field in first node. Because we are borrowing, we are not moving ownership. So the second node is still the owner of the second node here. The owner of the original data that we are providing a reference to in next. So if we try to return first node, the starting node in the linked list, its next field would be a dangling reference by the time this list arrives in the main function. Right? Because the data that next points to would at that point be deallocated, right? This goes back all the way to when we first introduced lifetimes, and the general notions of what a function can return. A function can return a parameter or an owned value, but it cannot return a reference to some data declared within its body, because that data will disappear by the end of the function scope. So this is an example where the reference based solution simply won't work. There's no way for us to preserve second node right here unless we move its ownership to this field, but we can't because the type of the next field is a reference. All right. Now let's take a look at how this solution will work with a box. So I'm just going to once again cut and paste my previous code from main right here. And now. Of course, we no longer have to worry about lifetimes at all so I can get rid of my tick a designation right here and my ticket right here. I still need my i32 or whatever equivalent integer type you want to represent this as. And when I save this, you're going to see. Now there are no more violations here, because the next field will be the owner of the box, which will take ownership of second note right here as soon as we declare it right here. Right. That's the key critical distinction here is that we're using the traditional ownership ideas. So now this content in memory is going to be owned within the box right here. Right. And then that box is owned by the next field which is owned by first node. So when we transfer first node out of here we are basically returning a container of all that data, including all of the cleverly nested boxes that themselves are smart pointers wrapping over those raw pointers. It's almost like a little bit of a cheat code. The only problem we have here, of course, is we still need to update this to be linked list using box, but that should do it now. There's no more violations. So now within the body of main we can declare our list variable and call create list like so and print it out in a nice debug formatted pretty printed manner. And you can see right here everything has been kept and we're still not actually nesting. Again, values within larger data structures we're nesting actually is still technically raw pointers, but they're encapsulated contained cleverly within these box owned types. Right. The box right here is an owned type. But it's going to handle the raw pointer pointing to that heap location where a second node is going to be. It's still storing fundamentally an address, but it behaves like an owned type. So it's sort of this nice cheat code that enables us to fix the design problem that we just had with references. While still technically using pointers and references under the hood, but wrapped up in this nice abstraction layer of the box. And then to follow up, of course, here when the main function ends, that's when the list variable is going to go out of scope, and thus the linked list and all of its contents, all of the nested contents of the next fields within, no matter how many we have, those will all be deallocated at the exact same time. So the outermost node, the first node in this example is going to be deallocated, which in turn will deallocate its nested fields, including the next field, which in turn will deallocate the box that it owns, will deallocate the second node that it contains, and so on, all the way down the chain, no matter how many nodes we have. So the entire thing will be deallocated and the box is stack entry and the associated heap memory that the stack entry points to will be deallocated at the same time as well, however many nodes we have. Hopefully the big picture here makes sense. And with that I will see you in the next lesson.
