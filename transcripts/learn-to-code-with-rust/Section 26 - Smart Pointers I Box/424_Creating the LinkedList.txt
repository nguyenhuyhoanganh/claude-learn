# Creating the LinkedList

So now that we've defined our linked list enum, let's create an actual instance of it. We're going to start with the simplest example, a single node linked list that stores the value one and does not have any other node besides that. So here within main, I'll call this my list, and I'm going to begin by accessing my linked list enum. Provide my double colons. And here I have a choice. So I'm going to have exactly one node. So I'm going to go with the node variant. And that is a struct variant. So as soon as I autocomplete vs code will fill in the syntax. For me this needs both a value as well as a next field populated. Now the value here should be some kind of i32, but it's completely arbitrary, so I'll just provide the value one. And for next. This is where the important part comes in. So what are we going to set next to. In the case of no further node to link to. And in that case that's going to be the empty variant right here. However, if I write linked list empty, this is going to be incorrect because remember the value of the next field is not exactly a linked list enum. Rather, what it is is a box wrapping a linked list enum variant, right? So you can see here the mismatch is going to be. You told me to expect a box wrapping some kind of enum variant. Instead you're giving me the exact same thing. So because the type here is a box, we're going to need to make it a box here. So I'm going to do box new just like we saw with our box instantiation a few lessons ago. And I'll pass in my next variant which represents the empty state or the end or conclusion of the list. So this node doesn't point to anything, it just says this is the end and that should do it. This code should compile. So here I can print out my list. I'm going to print it out in my nicely formatted pretty print debug format. And we're going to see right here. Here is the text representation of the node. You can see its value that represents the data it is storing, as well as its link or connection to the next node in the list, which right now is empty. Next up, let's model a multi node linked list. So we'll have our first node with the value of one direct or link or point to a second node. Let's give it an arbitrary value of two. And then that node can point to another node let's say with the value of three. And that third and final node can represent the end of the list. And so that will direct to the linked lists empty variant. So I'm going to remove this code we had right here. And now what we want in our next field is the actual next node in sequence. Once again we're going to need to wrap it in a box right. Because whatever we put here, whatever enum variant, whether it be empty or node should be wrapped in a box. And that's because we designated the type of the next field right here to be a box wrapping one of those two variants. So right here box new. But this time around instead of providing the empty variant of my linked list enum. I'll instead provide the node so right here linked list colon colon node. That is a struct variant. So here I need to provide my two curly braces and then provide the two required fields of value. And next. So I'll break this up across multiple lines. So it's a little bit easier to see the way that these nodes are structured within each other right. So this next field is going to store a box that is going to store a linked list node, which means rust is going to find some space on the heap to store this variant. And the box is going to store the raw pointer pointing to that heap data. And then that box that owned value is going to be the actual owned value assigned to the next field. So it's important to note the next field is going to take ownership over the box, because the box is the own value here. And then the field is owned by the struct. So here for the value. Once again this is arbitrary. Let's just do two and then I'll provide once again the other required field here which is next. And once again this is going to be Box New. And it's up to me what I want to put in here. If this is the end of the list then I can put a linked list empty variant in here. But let's add one more third node in here. So once again into this box I'm going to place my linked list node variant. And that node variant in order to be instantiated needs the curly braces as well and the two fields as well. Value. And next. So here the value can be something like three. Once again totally arbitrary integer data. And then for next once again I need to provide a box. And since I want this third node to represent the end of the list this time around still satisfying the requirements of the box storing some kind of linked list enum variant. I'm going to go with my other variant, which is empty, which designates the conclusion of the list. So right here linked list colon colon empty. So now you can see how each node is storing an individual piece of data and then linking to the next node in sequence. That is why it's called a linked list, because it is a connection of links, right? Pointers to the next value in line to the next unit of data from this node to this node, to this node to the end of the list. So we should see this nice visually when we print it out. You can see right here is our starter node at the beginning of the list. It stores a value and it points you to the next node. The next node stores a value and it points you to the next node. The third and final node stores the value as well. There are no more nodes, so this one just has a next field equal to empty. Currently our linked list hardcodes a fixed type of an I32 for the value field. We can make our linked list enum more flexible by introducing a generic, and that will allow us to have a linked list where the nodes can store any data type. It will still be a consistent data type for each node within the list, but we're going to be able to expand it to be more than just an I32. So we're going to solve that with a generic. So right here, after my declaration of the enum name. As a reminder, I can provide my angle brackets and choose a name for my generic. T is very common, so T is some future type that will be determined in the future and can certainly vary from instantiation to instantiation of this linked list. So here, instead of value always being an i32, we're going to say that value is going to be of that type T right here. And furthermore, since we declared a linked list type in here linked list, the very same type as here now requires the definition of a generic. So right here, in order for this definition to be valid, we can say that next is going to be a box specifically storing a linked list enum variant and a linked list with a type of T. And that should do it. And that will just ensure that every node is going to have the exact same value of type T, but what type T can be can vary. So you're going to see right here. This should work just fine because in I32 is a totally legitimate value to fill in as the concrete type for type T, but you can see right here if I expand this to something like a Float64, maybe I'll just add some decimal points and values here, like 1.3, 2.6, 3.9. You can see we change the data type, but now our definition is a bit more flexible. And that's a totally valid linked list consisting of float nodes. Now that we've introduced generics, we can come back to the music playlist item example that we had a few lessons ago and actually model that exact same idea using our linked list enum right here. So let me show you how that works. This time around, I'm going to declare separate variables for our different nodes, and I'm going to model a playlist that has three tracks. I'm going to begin with the final track. It's right here. I'll declare a variable, I'm going to call it I'm With You. And once again I'll begin by accessing our linked list enum that has a node variant which requires two fields value and next. This time around, since my linked list supports any kind of data, I can actually make a value a string this time around. Let's make it a string right here. String from this can be the song title of I'm with you. And then, of course, the song needs a connection to the next node, which in this case represents, symbolically, the next track in the playlist. Now, if this track is going to be the end of the playlist, the next track is just going to be the empty variant of the exact same linked list enum. So once again here for next, just like we did above, we need to make sure we wrap it in a box. And if you forget, don't worry, the compiler will tell you. But I need a box wrapping my specific linked list enum variant this time around is going to be linked list empty and that should do it. So this is going to be the final track in my playlist. And it's a node by itself. So now I'm going to build the track right before it. And I'm going to make that previous track link to this node of I'm with you. All right. So I'm almost building the list from a backwards perspective here. So here let's go for the track in the middle. Track two let's call it Skater Boy. Once again linked list node for the value field. This time this can be a string of skater boy, and this once again for its next field needs a box wrapping a linked list enum variant. So here I'll declare the box, and this expects either the empty variant or a node. I have the exact node that I want up above, and that's I'm with you. It's important to note that when I do this, I'm actually going to move ownership of I'm with you into this box. So you'll notice as soon as I do this, I'm not going to be able to actually print out I'm with you because it will no longer be the owner borrow of move value. I'm with you. The value moved ownership to the box and the next field right here in this assignment. All right. So the box is an owned value. It follows all of the regular rules of ownership. All right. So that is track two pointing to track three which points to the end of the list. Let's now add track one for more practice. That track is called complicated and it will represent the start of our linked list once again. We're going to need a node within our list. A node stores a piece of data, which in this case is our song title in the value field. That string can be a name like complicated, and the critical part of a linked list node is besides the actual piece of data, what it stores is the link or connection or pointer to the next node in line. So for this first song in the playlist, that should link to the second song in the playlist, which is going to be Skater Boy. So once again, next, just to show you the mistake, if I write Skater Boy here, the compiler is going to warn us, hey, you're actually assigning the linked list enum to the field that expects a box with a linked list enum. So in case you see a error message like this, it means you forgot to wrap this in a box new. And then once again, the skater boy node or the skater boy name will transfer ownership of this linked list node to this box. Right. And in this box is going to be this node, which itself is going to have a next field that has ownership over this box which wraps this node. And throughout this operation, though, despite the fact that we have a symbolic list right here with this pointing to this, with this, pointing to this, there's probably going to be three areas throughout the heap where the computer is going to find the location to place this data. So this code is going to run first. It's going to find some location for the box storing the empty variant. Then it's going to find this thing right here and find the location for the heap to store. The I'm with you node. And then the exact same principle here. When this code runs it's going to find some more space in the heap because we're using box dot new. And that's where it's going to place this node. And then of course we're also going to have the actual pointer that's going to show the directions to the next address in line. So this is going to store a link to this which is going to store a link to this. Right. And if you forget which part lives on the stack or which part lives on the heap, it doesn't really matter as long as the code compiles. That means that rust is going to take care of all of those pieces for you, right? It's going to store whatever it needs to ensure in the computer memory that there is no infinite recursion, and that this has the address to get to this, and this has the address to get to this. So right here I can print out the value of complicated. And that is the start of our linked list consisting of three nodes. Or equivalently this is also our playlist right. So if I run this we're going to see our playlist in full. Let me in fact add the nice pretty formatting so we can see it written out nicely. And there we go. Here is the start of our playlist. We start with the song of complicated that points us to the next music playlist item. The next song track, which is another song represented by the node variant. Again, that song is Skater Boy. That's going to point us to the next track in the playlist, which is the song I'm With you. That is the third and final song in the playlist, so it doesn't have a next node, but it does have the empty variant, which is another variant of the same linked list enum. All right, so these are a few examples with varying types of how we can build up that linked list. Once again made possible by the indirection that we introduced with the box smart pointer.
