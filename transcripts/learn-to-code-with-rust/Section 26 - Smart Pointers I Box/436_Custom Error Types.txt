# Custom Error Types

In the previous lesson, I mentioned that we could build our own custom error type. We simply define a struct. We implement the error trait on it, the exact same trait. That's something like parse int error implements. And then we can have our own struct function like any one of Rust's built in errors. We can even return it from the error variant inside our read number from file function. So let's say I'll apply an additional silly constraint to our program. My expectation is that the number that we are going to parse from the text file must be greater than 100. If it is not, I'm going to return a new error called number is unimpressive. So let's define that error right here. It's just going to be a basic struct definition struct. And I'm going to call it number is unimpressive. In fact I'm going to add on error to the end. So we don't forget. And this is an example of a struct that I don't even want to contain any kind of stored data, so I don't even need to choose the named field variant or the tuple variant. I can just declare an empty struct like so that stores no data. Now, as a reminder, in order to implement the error trait, we must also implement the debug and display traits for this struct. So debug is going to be easy enough. I can just derive the debug trait implementation and then for display. This is going to be a nice review of the traits section of the course. I'm going to need to quickly implement the display trait. So I'm going to reach into my standard library and inside the FMT module, which I believe is short for formatting. In there we can find the display trait. So here I'm going to implement the display trait for my number is unimpressive error struct. That is going to require a single method which is called fmt. I'm just going to allow the autocomplete to do its work. And as a reminder it accepts the immutable reference to the instance, and it also receives this formatter argument right here. I'm going to rename this parameter to formatter. And we talked about this more in depth in the traits section of the course. But the simplest way to make this work is to use another macro available in rust called right and two right. We can pass in the formatter instance that we get as the second argument right here, more specifically a mutable reference to the formatter. And then the second argument we pass to the right macro is the actual display string or the text representation of our struct. So how do we want to model the number is unimpressive error struct as text. What is its display trait implementation. So for now I'll just do a basic string that says that number is just too small, and the return value of the right macro satisfies the expected return value here of this specialized result enum. This is just a bit of a quick review of what we covered in the trade section. The most important detail here is we now have the debug trait implementation as well as the display trait implementation. We already have error here in scope. So right here I can implement the error trait for number is unimpressive error. And I have to provide the curly braces. But what's interesting is this implementation doesn't actually have any required methods. This is good enough by itself. I can command or control click into error and show you that although the trait does have certain method requirements, for example source and type id, they all have fallbacks. They all have default implementations defined within the trait definition. So thus, if you do not define a custom method, they're all just going to fall back to these implementations within the standard library. And so if ever we need to print out this error, the actual string representation of it of the struct in written form is simply going to be this content right here. So now that we have that set up, we can go ahead and utilize it just like any other error type in rust. So let's expand our read number from file function to return this error struct. If we have a number that is less than 100. That's what we're going to consider a unimpressive number. And that's just an arbitrary decision. So right here first up, instead of forcing this return right here, I'm going to extract the actual number here that I'm parsing from the string from the file here. So I'm just going to return number here and assign the result of this match statement to a variable. I'm going to call it number. And if I'm going to do that and continue writing code after, there's a few more modifications I need to make. Number one, I need to add a semicolon here because I no longer want to utilize the implicit return of the match. And here I also want to make sure I return early. So I return the error variant wrapping the box with the parse int error. So at this juncture, if we reach this point, then the number variable will be an i32 parsed from the file's text contents. So right here I'm going to do a basic if statement. And I'm going to say if my number is less than 100. And this is just an arbitrary condition, it can be whatever business rules we want to apply. If that's the case, I consider the number to be unimpressive, which I will designate to be an error within my program, within my business domain. So right here I can just implicitly return the error variant of the result enum. Remember our error variant has to wrap a box. So I will create a new box with box dot new. And our box needs to wrap some kind of dynamic type that implements the error trait. Luckily our own custom error. This number is unimpressive. Error struct here happens to implement the error trait, so it is a totally valid value to provide right here. So I'm literally going to provide that struct right here. It does not have any associated data or fields. So this actually creates an instance of the struct itself. It simply contains no data within it. It's going to be wrapped up in a box and wrapped up within the error variant. And then here in the else statement, if I have a number greater than 100, I can return the okay variant instead of wrapping the number itself. And this code, as you can see, is totally satisfactory to the compiler because every single possible return value here, whether it be the actual errors, wrapping the boxes of actual mistakes or errors, or the number at the end still satisfy this consistent return value of a result where the success data is an i32 and the error data is a box wrapping some dynamic type that implements the error trait. All right. So now we are good to test this code within the main function. So if I go back into value dot txt right here we currently have nonsense I'm going to change this to be something like 42. That is less than 100. So we should be able to parse the number. Then we're going to enter into this block right here. We're going to return our error variant, which means we're going to print it out right here. And it's going to say the error is blank. And that error message is going to come precisely from our implementation of the display trait right here. So you can see our own custom display string representation of our error is going to look like this. The error is that's coming from here. That number is just too small. That's because that's how we told rust to model our custom error struct as a string. And once again, if this value is larger than 100, say 1500, save that. Well then we're simply going to return the number in the okay variant here. Enter this match arm instead and print that number. Is that value 1500 may not be able to see it right here. There we go. The number is 1500. Key takeaway here is that our own custom error structs function the exact same way as any one of Rust's built in error structs. We just define a struct. We are welcome to store data within it if we want. We are welcome to store enums within it as well. That is another pattern in case you have different variations of the same kind of error. You can store whatever data you want inside whatever methods you want inside. But the key important detail is that you implement the error trait for your custom struct, which also requires the implementation of the display and debug traits. And then you have a descriptive error struct in your program that can be specific to your business domain, and the specific reasons why your program can go wrong. Right. These are kind of high level issues. Like parsing a number from a string incorrectly can be a uniform problem in many different software programs, but if you have something specific that is an error in your domain, like you know that it's an error if the number inside the file is less than 100 or anything equivalent that pertains to your business problem, your business domain, your product, well, then you can define these custom error structs to model those erroneous situations.
