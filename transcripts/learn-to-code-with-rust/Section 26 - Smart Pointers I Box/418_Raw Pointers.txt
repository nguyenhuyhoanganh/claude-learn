# Raw Pointers

Welcome to the next section of the course. In this section, we'll start learning about smart pointers, primarily focusing on the box smart pointer. Before we get there though, I want to stray a bit off the main path and talk about a related topic which is raw pointers. So let's begin with a quick review of the concepts we've explored so far. Rust has variables and a variable is a name for a piece of data. We can assign a variable to any rust type, a string, a number, a vector, etc.. Now the rust book defines a pointer as a variable that contains an address in memory. It's a type of variable whose data is an address to some other data. By storing an address, the pointer refers to or points to some other data. A pointer is the equivalent of us writing down the address of a house on a piece of paper. That piece of paper is the pointer. It's not the original data, or equivalently, the original house. Rather, it's an address that we can follow to get to the house. So the variable for a piece of data represents the direct way to access it. In comparison, a pointer represents an indirect way to access a piece of data. And that's because there's one more additional step that is required. That's why it's called indirect. And that's step of course is taking that address that the pointer is storing and following that address to get to the original value, just like we might follow an address in the real world to get to the house. So the instructions to a house, the address in other words, are an indirect way to access the house. So why do we do this? Why do we store pointers at all? Well, the advantage of pointers is they reduce the total memory that the program requires. Pointers prevent the duplication of the original data, because instead of creating copies, we instead create pointers to the same data. So one analogy I provided earlier on in the course was something like a tourist attraction, say, the Eiffel Tower. Say that a million people want to see or interact with the Eiffel Tower. The expensive thing to do would be to create 1 million copies of the same Eiffel Tower that would cost a lot of money, take a lot of time, be very resource intensive, right? So instead of creating a million Eiffel Towers, what do we do? We give the tourists an address to follow. We can give a million tourists the same address, so that they can follow that address to end up at the same core attraction, the Eiffel Tower. It's a lot cheaper to create 1 million pieces of paper with an address on it, than it is to create 1 million Eiffel Towers. The same principle applies to memory within our programs. If something is really big and requires a lot of memory to store, it's usually a lot easier to create an address to it, i.e. a pointer. In fact, create many addresses or many pointers to it than it is to create a full duplicate of the original value. So throughout the course we've worked a lot with references, and a reference is a type of pointer. It's a specific category of pointer. A reference is a pointer because it represents an address to some memory. The nuanced difference between those two terminologies, pointer and reference, is that a reference in rust is guaranteed to point to valid data. In other words, data that has not been deallocated. So we create a reference with the borrow operator the ampersand symbol. And that reference type is its own valid type. We can assign that reference to a variable. We can package it inside a struct or enum, we can pass it into a function, but it is not the original value, it is an address to that original value. So to repeat. Technically speaking, the terminology pointer in other programming languages means a variable representing a memory address in other languages, such as C and C plus plus. There is no guarantee that there will be valid data at that memory address. There might be or there might not be, and the developer is responsible for ensuring that their code is not accessing deallocated data. So when you dereference a pointer, in other words, when you follow the address to the original location to get the value there. Other languages place the burden on the developer to make sure they're accessing valid data that is still present and there. Rust and its borrow checker have safety checks that we've seen before that will ensure that any reference created with this operator never points to the allocated or invalid data. So rust solves that dangling pointer problem for us. And because of that, the community often uses the term pointer and reference as interchangeable. But again there is that slight distinction between them. And that is that a reference specifically in rust is guaranteed to point to valid data while a pointer is not. You will often hear the terminology raw pointer when describing the pointer that we just talked about. A raw pointer is a variable that stores a memory address without any safety checks or guarantees. So that pointer may point to valid data or it may not. It may accidentally point to memory that has been deallocated with the default reference type. In rust that is not possible, but with raw pointers in other languages it certainly is. So for example, in languages like C or C plus plus the raw pointer is very common. In fact, it is the default pointer in those languages. Those languages do not have a borrow checker like rust does, so there's a lot more risk in writing them now. Developers in those communities still are going to use the terms pointer and reference interchangeably, simply because they basically mean the same thing, right? Whether it is something pointing to an original piece of data or referring to an original piece of data, it's the same idea. It's an indirect way to get to some place, which is why they're often used interchangeably. But there are those nuances between those two terms. Now, as it turns out, rust actually supports raw pointers too. We haven't explored this feature yet, and we will very quickly in the next lesson, but if you want to use raw pointers, you have to choose to opt out of the compiler's safety guarantees that it can make with references, but cannot make with raw pointers. And remember, Rust's safety guarantees is one of the primary reasons why the language is so popular. So references with the regular borrow operator are safer and are generally recommended. You typically do not want to reach for raw pointers in rust, unless you have a very specialized use case or reason for doing so. One common reason is writing libraries that are going to interact with libraries from other languages, like C or C plus plus. In those scenarios, it sort of makes sense to use raw pointers because you're trying to establish a common way of communication between two different languages and ecosystems rust and C. But day to day, you should generally aim to avoid using raw pointers in rust because they carry increased risk. So with that said, in the next lesson we're going to do a quick dive into raw pointers so we can see how they work and some of the dangers associated with them. And as we progress through that code, I want you to keep the following in mind. The advantage of smart pointers, which will introduce later is that they hide away or abstract away both the complexity and the potential danger of raw pointers that we see. There are certain situations where raw pointers are going to be able to solve problems that references cannot, due to their strict safety guarantees. But if we opt into raw pointers, we have to adopt additional risk ourselves. And as we'll come to discover, these smart pointer actually gives us the best of both worlds. It allows us to hold on to a raw pointer or some kind of reference that is more unsafe to data elsewhere, while giving us some additional safety guarantees around that, and also enabling certain operations that would otherwise be impossible or difficult with regular references. So, so far at a very high level. I know this is pretty difficult, but stick with it. We're going to slowly wade through all of these highly technical topics. In this section we're going to learn about raw pointers, smart pointers, and more. And this will give us a better understanding of how rust handles memory.
