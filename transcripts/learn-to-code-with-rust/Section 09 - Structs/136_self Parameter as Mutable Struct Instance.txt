# self Parameter as Mutable Struct Instance

In the previous lesson, we defined our first method on a 'TaylorSwiftSong' struct. It was 'display_song_info' and it defined 'self' to be the immutable struct instance, the actual concrete instance itself, which meant that 'self' took full ownership over the actual struct. So I've taken my comments from the previous lesson, and I've dispersed them throughout my implementation block, because I want to show you an example of all 4 of these possible scenarios. Sometimes we may want our method to actually update some of the fields, and thus we have to define 'self' to be mutable. So let's take a look at that example in this lesson. So right here I'll define another method. So once again within the block that is attached to my 'impl' keyword and the struct that I am defining methods on, I'll define another method with my 'fn' keyword. I'm going to call this method 'double_length', and this method is going to take the duration of the song and double it, and then save it back to the struct instance. So once again, if I provide 'self, this will allow 'self' to take ownership, but 'self' will be immutable. So it's not going to have permission to change any of the struct's fields in order to make this a mutable instance or a mutable parameter. Just like with our parameter definitions, and just like with our variable declarations, we add the 'mut' keyword. I'm going to add that before 'self' and then I'll place a space between them. So now 'self' will still be the struct instance. Rust will pass it in automatically when we invoke 'double_length' So when we invoke 'double_length' on a given struct, that struct will become 'self'. And furthermore 'self' is mutable, so we have permission to edit it in the body of this method. Once again, if you want to annotate your type, the actual type of 'self' is a 'TaylorSwiftSong'. It is that struct. It is that instance. It is that value. So you're welcome to write this. Or once again you can use the capital S 'Self' Right. That is the type and that is the type that is being represented by the block. It is an alias or an alternate name for whatever we write after 'impl'. It's like an indirect way of referencing this thing right here. It is more resilient to change. So 'Self' and 'TaylorSwiftSong' are the exact same thing. And if you want to simplify it even further, this is also the exact same thing. The way that Rust distinguishes between these two methods and what they receive is the presence of the 'mut' keyword. All right. So now in the body of my 'double_length' method, 'self' is my instance. I can access a field like 'duration_secs'. I can multiply it by two using an asterisk ( * ). This will give me the new duration, but I'm not overriding anything yet, so I'll do 'self.duration_secs', provide an equal sign, and assign my new calculation from the right hand side. This is totally valid because Rest evaluates the right hand side of the equal sign first, so it takes the field's current value, multiplies that number by two, gets a new unsigned integer, and then assigns that back or overwrites this field. All right. So now on my 'song' instance right here, I have two methods I can call 'display_song_info' And I can call 'double_length'. Unfortunately, both of those methods take ownership either mutably or immutably. So I can't call both in sequence. Right. Because as soon as I call one, I'm going to lose ownership from 'song', and thus I cannot invoke the follow up method on 'song'. Right? I can't do 'display_song_info' and then 'double_length', because by the time 'display_song_info' runs, we pass ownership to 'self' and 'song' is no longer the owner. Right. So unfortunately, I can't do this. But I can invoke 'double_length' like so. This is totally valid. The only problem now is that we have no way of verifying our work. So one thing that I can do to prove to you that we've updated the data is right here, I can simply print out my struct. So println! and remember we implemented Debug right here. So we can actually print out the complete struct in Debug format. So once again provide my curly braces. And for my value here how do I get the complete struct right. Not a field but the complete struct instance itself. That's going to be 'self'. All right. Now this is not going to work because this is the Display trait implementation. And our struct does not implement the Display trait but it does implement the Debug trait. So if I do a colon to specify a format or a different way to print my string, and then I provide the question mark ( ? ), that is the Debug format. So now this println! will run as part of the 'double_length' method. So if I rerun we're going to see. Here is our updated struct. In fact we can pretty-print it with the hashtag right here. Let's run it again. And now you can see 'duration_secs' has been doubled from 231 to 462 and saved that field. So we have mutated the struct. In this method, we have changed its fields or some part of its state. The only way this syntax is possible with this approach is if we make 'self' mutable. So we're still passing in the instance, we're still passing ownership from 'song' to 'self'. But now we have a mutable parameter of 'self'. And thus we can modify the struct instance right here. Then 'self' goes out of scope and we lose it and it's deallocated from memory. Unless we happen to return 'self', then we can collect it back in the caller. But that's a more roundabout way compared to simply using references. All right. So this is our second example: a mutable struct value where the 'self' parameter takes ownership and does have permission to mutate. I'll see you in the next lesson.
