# self Parameter as Immutable and Mutable References to Struct Instance

So my original plan was to define 4 methods that showcased the 4 different ways that we could define the 'self' parameter. But I realized it would be more advantageous to show you the remaining two options by refactoring or improving our existing two methods: 'display_song_info' and 'double_length'. This will show you not just the new types that we can define, but also how to change an existing method definition from one option to another. We've now seen some of the disadvantages of ownership. We've seen, for example, in the previous lesson that as soon as we call a method, we can no longer call another method because ownership will be gone after the first method invocation. So while this is valid technically, it creates the same problems that we first saw in the ownership section of the course. If we call 'display_song_info', the only way to keep the struct alive is to return 'self' so that the original caller gets it back, and then assigns it to a variable that then takes the ownership away from 'self'. Right. So this design isn't great, and it's not really the Rust way of doing things. The Rust way is to define references instead of ownership. So for this method, we currently take the struct instance immutably. Let's update it to this option. Let's now update it so that it will receive an immutable reference to the struct instance, and now no ownership will be moved. All right. Here's what that syntax looks like. Once again there's three different ways that we can annotate it. So I want to show you all of them. The first option is to keep 'self'. And then for our type we are not going to have a 'TaylorSwiftSong' anymore, right? That's the exact same thing as we currently have. It's an immutable instance. Instead, we want a reference. So we're going to change this to be a different type. Now it's a ref to a 'TaylorSwiftSong' ( &TaylorSwiftSong .) It's an address in memory right. It's a borrow rather than the actual instance itself. Otherwise, the syntax will be the exact same. So if I call 'display_song_info' below now, Rust will understand that what it should pass in to 'display_song_info' behind the scenes as the value of 'self' will be a reference to 'song', so it's not going to pass in 'song' any longer. It's going to pass in a reference to 'song'. This is all automatically handled behind the scenes because we do not explicitly pass in 'self' when we invoke a method. Rust does it for us, but Rust will see that we want a reference to the instance instead of the instance itself, and thus it's going to pass in this behind the scenes. Okay, so this is our first option. Once again capital 'S' 'Self' is another way to refer to the struct name. So we could also annotate this as a reference to 'Self' with a capital 'S'. Same thing. And finally, if you want the most abbreviated way, it looks like this. Not 'self' because that's where we started from, but '&self'. This means the same thing, an immutable reference to the instance. Right, so '&self' is an immutable reference to the instance. All right. Now let's similarly update the 'double_length' method to go from receiving the mutable struct instance or the value to receiving a mutable reference to the struct. Once again, same principles. I'm going to remove 'mut' here. And then for our type we can either write '&mut TaylorSwiftSong'. All right. So once again, 'self' now is not a 'TaylorSwiftSong' instance. It's also not an immutable reference to a 'TaylorSwiftSong' instance. It is a mutable reference to a 'TaylorSwiftSong' instance. I hate to be pedantic and repetitive, but these differences do matter, right? The ampersand ( & ) means a reference. It means the original value is being borrowed. No ownership is changing hands, right? 'self' is not going to be the owner of the original struct instance. It's only going to be the owner of this reference, which is a different type and a different value. It's a memory address. It's not the core struct, and then 'mut' is required after the ampersand ( & ) to specify that that reference is mutable, which means once we follow the address to the location memory, we have permission to modify it. Now remember when we use the dot syntax, even on 'self', Rust automatically dereferences. Technically speaking, 'self' here is not a struct, it's an address to a struct. It's a reference to a struct. But Rust wants to make it easy for us to have parameters that are interchangeable. So this code will work on either a struct or a reference to a struct. Because when Rust sees the dot and it sees a reference, it will automatically dereference. It's going to do this behind the scenes, as you may recall. It's going to take the address and secretly follow it to the original memory location, and thus give us access to the original struct. Ownership is never transferred, but we do have an address that we can follow. #1, that's what a reference is. And #2, with the 'mut' keyword, we say that that reference is mutable. We are allowed to go to the address, and we are allowed to modify or change the value stored at that address, which gives us a way to mutate the struct without losing ownership. So now we can remove this println! We're no longer going to need it. And then once again, instead of 'TaylorSwiftSong', You can simplify this to just 'Self'. '&mut Self' means a mutable reference to a 'TaylorSwiftSong', or whatever happens to be the name of the struct. And then finally, the super short abbreviated version of this is going to be '&mut self'. Right? 'self' is still lowercase. This is our parameter name, but we're specifying that 'self' will be a mutable reference. This is a shortcut for the syntax that we just saw a few seconds ago. So just to reiterate what the 4 shortcut options are, it's 'self' which means immutable instance. It is going to be 'mut self' which means mutable instance. Then it's going to be '&self' which means immutable reference. And then finally '&mut self' which means mutable reference. These are our 4 shortcut options if you want to exclude or omit the manual type annotation. This is how Rust can distinguish between the four possible options for each method definition. They're all different slightly from one another. So that is how Rust knows what you want 'self' to be. And then when you call the respective method, Rust will pass in whatever the method expects automatically. You are welcome to annotate the type out if that provides more clarity, but these are the shortcuts for the 4 respective different ways that we can pass in the instance in some form. All right. So now I can remove this. And now because these methods accept references, we are no longer ever going to lose ownership as far as when we call a method. So I can feel comfortable, for example, displaying the song info, then immediately doubling its length. The only reason we're seeing a violation here is we are currently declaring 'song' as an immutable variable. If we're going to borrow a mutable reference, which 'double_length' does, then we also need to make the original variable mutable. There we go. And then we can once again call 'display_song_info' after we have updated its 'duration_secs' field. So you can see here is our first output where duration is 231. Then we call the 'double_length' method which receives a mutable reference, updates the field, then we invoke 'display_song_info' again. That takes an immutable reference and we output the content. You can see this has been adjusted so everything is working as expected. But the beautiful part here is that these method calls do not move ownership away from the 'song' variable. So this is the approach that you're going to see most frequently. The other ones are still technically possible, and they do have their use cases in certain situations. But these are the options that you're going to see most frequently in the real world. So that is a quick introduction to method definitions, as well as the 4 different ways that you can define the 'self' parameter.
