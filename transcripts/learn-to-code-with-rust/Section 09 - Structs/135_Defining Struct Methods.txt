# Defining Struct Methods

In this lesson, we'll learn how to define methods on a struct. A method is a function that belongs to a type that lives on an instance. We've seen methods before like 'push', 'string' or 'clone' on a string type. To invoke a method, we provide the value. Then we place a dot followed by the method name and a pair of parentheses. A method can accept arguments and it can return a value just like a regular function. For example, the 'push_str' method accepts an input that represents what to concatenate to the end of the String. So the fields of a struct represent its data, and the methods of a struct represent its behavior, its available actions. A method is a procedure. It's a command that the structure can execute when told, and the method is going to have access to the struct instance. And just like with a plain function, a method can choose to receive the instance either as a value or as a reference. And then we also have the option of mutability versus immutability. For both of those scenarios, either the value or the reference. Right. So same principles that we've discussed before. But now these methods are going to live directly on the struct. Our methods can do whatever we want, right? It's up to us to define what kind of methods provide value for the program. Methods exist to help with organization. They're advantage over plain functions is that we can isolate and define all of the common functionalities of a given struct in a single place in our code, and then all of these struct instances created moving forward will have access to those same methods automatically. We define the method once, and then all of our future instances can keep relying on those methods, those shared behaviors, over and over again. Let's begin by defining a new struct above main. I'll use my 'struct' keyword and this struct will be a 'TaylorSwiftSong'. It's going to model a Taylor Swift song, and it's going to have a couple fields. The first is a title which will be a String. The second field is going to be a release year, which represents when the song came out. And I'm going to make this an unsigned 32 bit integer. So a u32, because there's no way that this can be a negative in the real world. It can only be positive or unsigned, and I'll also have the song's duration in seconds. I'll call that field 'duration_secs', and that will also be an unsigned 32 bit integer because it cannot be negative. Duration can only be positive. Immediately above the struct, I'm going to add my attribute to ensure we have a Debug implementation. This will allow us to convert a struct instance into a string for easy printing. So again hashtag. Square brackets. Derive. Debug. And now we have our basic struct definition. Next up I'll create some space for myself. I'll just add two line breaks here. And then below the struct definition we'll use a new keyword called 'impl'. 'impl' is short for implementation. We're going to follow the 'impl' keyword with a space, and then the struct name that we want to define methods for. So for us this is going to be the 'TaylorSwiftSong'. And then we're going to provide a pair of curly braces. So it is within this block, within this pair of curly braces that we're going to declare all of our methods. If you're familiar with other object-oriented programming languages with classes, usually the state, which means the data that lives within a value and the methods, usually they're defined together in one area. In Rust, we define our fields in one section and then our methods separately in the implementation block. All of the content that we define in here will automatically be associated with a 'TaylorSwiftSong'. And Rust knows this because it's in this block that follows this 'impl' keyword and the struct that we are defining the methods for. So inside the block we're going to declare our methods. Methods look just like plain functions. So they're going to begin with the 'fn' keyword and then a method name. So let's define a method called 'display_song_info'. And this method is just going to output some information about the song. Just like with a function, we provide a pair of parentheses where we can provide our parameters, in other words the inputs to the method. And then we provide a pair of curly braces. And that is the body of the method where we define the actual procedural code. What will run when 'display_song_info' runs. The first parameter in a struct method definition is always going to be the keyword 'self'. Now what 'self' is going to be can differ. What it ultimately is going to represent is the struct in some form. We're going to invoke this method on a 'TaylorSwiftSong' instance, and Rust will actually pass in something to represent this 'self' parameter. But what that something is can vary, just like it varied when we saw the four different ways that we could pass a struct into a function a couple lessons ago, we have those same four options here. So just as a quick reminder there's 4 possible combinations, right? The first is we pass in the immutable struct value. And that means the 'self' parameter takes ownership. The second option is the mutable struct value the actual instance itself. The difference here Is again, the 'self' parameter takes ownership of the struct, but this time around has permission to mutate. That's the first two options and the second two, as you might imagine, is an immutable reference to the struct instance, in which case we do not take ownership because 'self' is not the struct instance, but a reference to the struct instance, and it's immutable, so we cannot mutate the underlying instance. And our fourth option is a mutable reference to the struct instance, which once again means no ownership moved and we have permission to mutate. Okay, so if I write out just 'self' by itself, this is the first option. It means the immutable struct value. The 'self' parameter will take ownership over the struct that the 'display_song_info' method is called upon, that it is invoked upon, and 'self' will go out of scope at the end of the method body, and it will be responsible for deallocating the memory belonging to the 'TaylorSwiftSong' struct instance. I want to explain in greater depth the options we have as far as providing the type annotation here, so I can use a colon and provide my type. In this scenario, it's going to be a concrete 'TaylorSwiftSong' instance. So my first option is writing out 'TaylorSwiftSong', right? This is still our first option right here. An immutable struct value. 'self' will be the owner of a concrete instance. And don't worry, by the end of this lesson, we are going to create this instance and invoke this method. So you'll see how it all plays out in the real world. Now, as an alternative to writing out 'TaylorSwiftSong' like this, I can use a special type called 'Self' Notice here that 'Self' is spelled with a capital 'S', right? The parameter name must be 'self' with a lowercase 's', and that is the actual parameter. That is the name that we can use throughout the function body. This capital 'S' 'Self' is the type. It's the kind of thing that 'self' will be. Now as far as this capital 's' 'Self' is, this is an alias or a nickname that represents the type that we are implementing the method on. So you can think of capital 'S' 'Self' as being whatever happens to be after the 'impl' keyword. It's almost like an alias or a nickname for whatever we are implementing the method on. So 'Self' is another way that we can refer to the type 'TaylorSwiftSong' in here. Okay, now, what is the advantage of this approach? The advantage of this type annotation, even though it's the same thing, is that this code will stay the same if the struct name changes. It's more resilient to change. For example, if tomorrow we decide to change this struct to be 'TaylorSwiftPopSong', well, now we're going to need to update the type right here. And furthermore, if we had written out 'TaylorSwiftSong' right here, then we'd need to update it here as well. In fact, for any single method definition that referenced that struct name, we'd have to update it. The reason why capital 'S' 'Self' is more resilient is because it does not need to change. It will always represent a nickname for whatever this is and thus it stays the same. Even if our struct name changes, it's dynamic, right? It will represent whatever type is here. So if this name changes, 'Self' will simply be that new name. So it's more flexible. So three options available to us and they all accomplish the exact same result. First option is this. Second option is 'Self' with a capital 'S'. And furthermore this syntax can be abbreviated one more time to just 'self' by itself. 'self' by itself means the struct value. The actual instance will be passed in immutably. This first option is what the keyword 'self' by itself, or rather the parameter 'self' by itself with nothing else. This is what it will represent. The immutable struct instance. The 'self' parameter is the owner. It takes ownership of the struct instance and it is responsible for deallocating it at the end of the 'display_song_info' method body. So inside a method's body, we can access our struct's fields using the exact same dot syntax. How do we get access to these struct instance? We use the 'self' parameter just like any other, right? So for example let's try println! right here. We'll say something like "Title", provide our pair of curly braces and then we'll reference the 'title' field. Our struct instance is represented by 'self'. That is my struct that I have ownership over. I can do dot and access 'title' and whatever the 'title' string happens to be, we're going to interpolate it right here. We can do the exact same thing below with our other fields. So let's do 'release_year' 'self' is my struct instance. Dot to access a field. Then the field I want is 'release_year'. And one more time: let's do "Duration". Let's do a curly brace and "seconds" and then we'll reference `self.duration_secs`. All right. So now we have a `display_song_info` method. Let's invoke it on a sample struct instance. In main, I can define a variable. Let's call it 'song'. The first thing we need to do is create an actual 'TaylorSwiftSong' instance. I forgot to change this right here, so let's quickly bring this back to just 'TaylorSwiftSong'. This was just for the sake of example here. 'TaylorSwiftSong'. Provide my curly braces. I need three fields. Let's start with 'title'. Let's do a 'String::from' here and give a string of "Blank Space". We also need a 'release_year' field. Let's set that to 2014 and then a 'duration_secs' field. Let's set that to 231 seconds. Almost four minutes. Provide my semicolon here. Now we have a valid instance. How do we invoke a method on a type? Right. You saw it earlier in the course with methods like 'clone' on a String. We reference our value or the variable that represents our value. In this case, 'song'. Then a dot. But instead of accessing a field, we're going to find the method and invoke it with a pair of parentheses. A field is a piece of data that we can read. A method is a behavior or command that we can ask the struct to do, right? Remember, a method is a function that lives inside the struct, right? So we can find the 'display_song_info' method and invoke it with a pair of parentheses. What's going to happen when we invoke 'display_song_info' on a song? It's going to run this method right here. Now, when it comes to the first parameter, which is 'self' in this definition, we do not need to pass it in during invocation. Rust will automatically handle passing in the first argument to a method, and it will represent the type that we defined here. We have these 4 options and we're going to talk about all 4 of them. But in this example, 'self' is the immutable struct instance itself. So that means when we invoke 'display_song_info', this song, this struct instance is what's going to be passed in as the 'self' parameter. So ownership will move from the 'song' variable to the 'self' parameter in this method. And then this code will run and we're going to get our summary. So here we can see all of those println! statements from our method. Our method has successfully ran. Rust has automatically passed in the concrete instance as the argument for the 'self' parameter. But remember with this first option we are moving ownership, which means 'self' is the owner. 'self' will clean up the instance after the scope ends and 'song' is no longer the owner after the method invocation. Therefore, I cannot access any of its fields. I cannot do 'song.title' because 'song' is no longer the owner, right? Once again, ownership was moved. Let's take a look at our error messages from the compiler. Move occurs because 'song' has type. 'TaylorSwiftSong' which does not implement the Copy trait. Okay. At what point is ownership moved? Right here. 'song' was moved due to this method call. By invoking a method like this and defining the 'self' parameter to be this option, the immutable struct instance by value, right? Not a reference. Not the address to a struct, but the actual struct value itself got passed in to this function, just like a parameter. It was 'self'. 'self' took ownership immutably. So we can read any of these fields. We can't write to them because it's not a mutable instance, it's an immutable instance or an immutable parameter, whatever way you want to describe 'self'. Then it's done. And so 'song' is no longer the owner. All right. So this is a quick introduction to methods. Quite a lot to cover. But as we cover these remaining scenarios, hopefully more of the syntax and more of this logic makes sense. A method is just a function belonging to a type. It's a behavior. And just like a function, it can accept parameters and it can produce a return value, a final output. Now the first parameter to any method will be the struct instance itself In some capacity. It might be the instance, or it might be a reference to the instance, and it might be mutable, or it may be immutable, but it's going to be the struct in some form. Because it's a method, it's its whole responsibility is to work on that value, to do something with that struct's data. Right? It could be reading, it could be writing, it could be both. So we need to provide something that will represent the struct in some capacity. Otherwise we can just do a regular function. A method exists to add value to a 'TaylorSwiftSong' and what it is capable of doing. So now, any 'TaylorSwiftSong' struct that we create in our program? This is just one example, but we can have 100. All of them will automatically have access to this 'display_ song_info' method, and if we invoke the method on any one of those instances, Rust will automatically pass in the immutable instance as the value for the 'self' parameter. All right, lots more on this in the upcoming lessons.
