# The Cell Type and Interior Mutability Pattern

Welcome to the next section of the course. In this section, we'll introduce some additional smart pointers, including cell, ref cell, lazy cell, one cell, and RC. As a reminder, a smart pointer is a type that wraps a value and provides additional capabilities beyond a regular reference. You can almost think of it as a deluxe version of a reference. Before we introduce our first type, which is the cell, let's take a moment to discuss a Rust design principle called interior mutability. The word interior means inside of something, and the word mutability means the ability to change. So the term interior mutability means the ability to change what's inside of the value. You might think that we've seen this idea before. For example, if we declare a struct as mutable and we overwrite one of its fields, we are changing an interior field inside a mutable struct. But there's an important nuance in the Rust community definition of the term interior mutability. The specific term interior mutability usually refers to a design pattern where a type enables its internal data to be mutated, even through a shared immutable reference like a regular reference to self. So normally, in order to change a value, we need a mutable reference. In comparison, interior mutability is a pattern where we relax that restriction. We actually allow the program to mutate an otherwise immutable value. It's kind of an interesting idea, and we're going to be exploring it throughout this section. So to begin, let's define a new struct called concert ticket that's going to have three fields. Right here, I'll do concert ticket. And before I forget, I will provide my debug trait implementation in the line above. And let's make sure that concert ticket has three fields. The first will be section, which will be a string. And this is where a person at a concert will be sitting. So they have a section. They also have a seat number, which I will also designate as a string. And then finally, a concert ticket also has this idea of being scanned or admitted into the venue. So here, I will provide a scanned field set to a value of a Boolean. So one of the problems with regular mutability is that it is an all or nothing choice. So if we create an instance of this concert ticket struct and we use the mute keyword, we will declare the entire struct to be mutable. You can thus alter any of the struct fields in code. But what if we wanted to make some of the fields mutable while keeping the other fields immutable? For example, maybe you're a library author and you want to expose a powerful struct such as this one to other users, and you want developers to start using your code to be able to modify certain fields within the struct, certain pieces of state, but you don't want them to touch others, maybe for safety reasons, or maybe to keep the complexity of the API smaller, right? Maybe you want some pieces of state to only be affected by specific methods that they invoke. So the mute keyword, unfortunately, will force us into an either or scenario. We have to opt into either complete immutability, where the user cannot change any of the fields, or complete mutability, where the user can change all of them. So to demonstrate this principle, let's create an instance of the concert ticket struct in main. Right here, I'll declare a ticket variable, and I'll provide my instance of concert ticket. For the section field, we can do a string, let's say section A. Let's say for the seat, we can do another string. Let's do a seat like seat three. And for scanned, all of our tickets can begin, let's say, unscanned with a value of false. So we have immutability by default, so we cannot modify any field here. So for example, if I try to take my ticket and access the scanned field and apply a new value of true, the compiler will not compile. So here, for example, if I hover my mouse over, we can see we cannot assign to the scanned field as ticket is not declared as mutable. And if we choose to make this struct mutable by adding the mute keyword right here, we are now able to overwrite any struct field whatsoever. But maybe that's not what I want. Maybe I only want scanned to be overridable or mutable while keeping the section and seat fields immutable. So now I do have permission to update scanned, but now I can accidentally make a mistake, like also mutating a field that I am not meant to mutate, like, let's say, section. So here, for example, I can provide a new string and change this to something random like kangaroo. So once again, the problem here is we have this all or nothing approach. What if we wanted a middle ground where only the scanned field would be mutable because that's the only one that really has kind of the permission or the real world context to change? As a user enters into a venue, they scan their ticket, and the scan status changes, even though the section and the seat should remain the same throughout the night. So how can we make that a reality? Well, one option that we've seen so far is we can declare this struct in a separate private module, and we can make the scan field public and the others private. We can then define special methods to update only the scan field, but there is some amount of additional complexity there. And that partial mutability through the interior mutability pattern can be achieved with this new smart pointer called cell. So interior mutability using cell will enable us to keep the concert ticket instance right here immutable while providing a way to make just the scanned field mutable. To clarify, the cell smart pointer type is designed specifically for struct fields that implement the copy trait. So integers, floats, booleans, characters, basic primitive values. The word cell in the real world means a small room where a prisoner is locked up, right? Like a prison cell. And it's a similar idea here because we can think of a given value like the scan field as being locked up inside the cell smart pointer, and you can only interact with that field through the cell's controlled interface, through the methods that it exposes. So you will not be able to reach in and grab the value or grab a direct reference to this inner value like the scan field. Rather, the cell type will control access to that mutable field, in our case, the scanned boolean field. So the cell type is often categorized as a smart pointer because it wraps some internal value like this one right here and controls the access to it. But the cell technically doesn't actually manage any heap memory. It's more of a smart container around a piece of data, in this case, a primitive boolean. So let's begin by bringing in the cell smart pointer type. It's going to be found within the standard library. It's right here. I'll utilize the use keyword, reach into std, and then within the cell submodule, there's going to be the cell type, which is a struct. Right here, you can see it is a struct that also represents a smart pointer, and it exists to solve this exact problem. So down below in the concert ticket struct declaration, I'm going to wrap this boolean in my cell. So the cell is a struct type that has one generic, which represents the type of the nested or inner value that it's going to control access to. So now scanned is not just a plain boolean. It is specifically a cell smart pointer type that is going to maintain access to that internal nested boolean value. Once again, the cell type is specifically designed for primitive, simple values that implement the copy trait. So you should not be using cell for something like a complex struct or a vector, but for something like this, it's perfectly okay. So now as you can see down below, this is no longer valid because there's a type mismatch. So here what I'm going to do is I'm going to use the exact same cell type from up above, and there is a new constructor function that will create a new cell smart pointer, and then it will provide a value representing the initial starting state. So now I'm going to pass in false because I want my ticket to begin with an initial state of being false, meaning unscanned. In fact, since I can imagine creating multiple concert ticket structs throughout the program, let's in fact define a constructor function for the concert ticket. So right here, I'll add an impl block for my concert ticket. Let's define a new constructor function. We'll provide a section parameter set to a string and a seat parameter set to a string. We're going to return an instance of the concert ticket, and we can designate that type with capital S self. Here, we can once again use capital S self to create an instance. We'll provide the section and seat parameters in the short form syntax. And here for scanned, once again, we're going to use the exact same code as we have below. So cell will invoke the new constructor function and ensure that every concert ticket begins unscanned with a value of false. Now that we're here, let's define an additional method called admit attendee, which will mutate the actual scanned Boolean. This method will serve as one more abstraction layer so that the end user doesn't have to interact with the structs fields. They can just invoke methods. So right here, I'll define my new method. I'm going to call it admit attendee. And this is the most important detail right here. I'm going to provide a parameter of ampersand self, so an immutable reference to the instance. Notice right here that by default, if we did this, we would not have direct permission to mutate any field of the concert ticket struct throughout this method. So for example, just like we saw in main down below, if I tried to take self and do scanned and set it equal to true, this is going to be a compiler failure because we do not have permission to mutate because this is an immutable reference. Our parameter is an immutable reference. But now we have access to this cell type, which enables the interior mutability pattern. We actually have permission to overwrite the scanned field using a special method, even though our self parameter is marked as immutable. You can almost think of this as a bit of a cheat code. We are opting out of the default compiler checks to gain access to the permission to mutate an internal value inside an otherwise immutable struct. The way we do this is right here. Remember, scanned is no longer technically a Boolean. It is the cell smart pointer. On that cell smart pointer, I have a set method, which sets or writes or overwrites a new value. And here I can provide the new interior value to store inside the cell, which must be a Boolean type. So whenever the user is admitted into the venue, I can change this to true, like so. So again, the interior mutability pattern is a bit of an escape patch because we are now able to overwrite an internal struct field without borrowing a mutable reference to the struct. This operation is still technically safe, and the compiler is happy with this code. So now let's see this in action. The first major change I'm going to make here is I'm going to remove the mute keyword right here. We are going to keep the ticket variable and the concert ticket struct here as an immutable instance by default. We also now have access to the new constructor function. So here we can update this to use the new function, and we can copy and paste these two strings here representing the section and the seat up above. There is section, and here is seat. That's going to be the string with a value of three. We no longer need to provide scan because it's going to be set right here. We can remove all of this code right here. There we go. So once again, notice that ticket is now going to be immutable. Down below, let's go ahead and print out the value of ticket in debug format right here. In fact, I'll pretty print it like so. And then down below, what I'm going to do is take ticket and invoke the admit attendee method on it. And then when I'm done, I'm going to copy and paste this line below. We're going to take a look at what the ticket looks like after the admit attendee method is called. So let's run this program and take a look at what we get. Notice the concert ticket initializes right here. The scan field is a cell smart pointer, and the internal value that it is storing is a Boolean of false. Ticket is technically immutable. There should be no technical way from the original primary Rust rules in order to interiorly mutate any of its internal data. But using the special cell smart pointer, we are in fact able to do that. When we invoke the admit attendee method, which accepts an immutable reference to the concert ticket struct, we are still able to call the set method on the cell to mutate the internal value of the scan field. So now notice after the method invocation, value is now set to true. So at this point, we've seen that the cell smart pointer supports a set method to overwrite the inner stored value. There's also going to be a complimentary get method to read the inner value. So set to write and get to read. Now technically, because Booleans implement the copy trait, whenever we call get, we're actually going to get a copy of the Boolean value stored inside the cell. That's why the cell type requires types to implement the copy trait so that there is no references to even have to deal with. We're simply always going to get back a copy of whatever is being stored inside. So for example, right here, instead of printing out the complete ticket, what I can do is use my print line macro and provide some room for dynamic content. I can take ticket, take scanned, which again is not a Boolean but a cell smart pointer. On there, I have a get method, which will get me the current value of the stored Boolean that it is providing access to and also defending from accidental mutations because the only way to mutate it is using the set method. So here, I can do the exact same thing after I invoke the admit attendee method. And this is just one more example proving that the value of the scan Boolean, more specifically the value of the scan Boolean being locked away within the cell, has mutated from false to true, even though ticket remains an immutable concert ticket struct. So what sacrifice do we have to make to enable code like this? Well, usually types that implement interior mutability will move some checks from compile time to runtime. We're going to see an example of that later on in the section. However, cell is somewhat special and unique. It takes a different approach compared to other interior mutability types like ref cell, which we'll introduce in the next lesson, because cell does not provide any references to its internal value or content. There is no way for us to get a mutable reference to this Boolean or even an immutable reference to the Boolean or any other primitive value being stored inside a cell. It is completely protected with cell administering sort of the protective layer around it that grants us both read access and write access. We can write the value with the set method and retrieve a copy of the value with the get method. So because the cell smart pointer works entirely through copies rather than with references, there are no real borrowing rules to check at all, not at compile time and also not at runtime either. And that is why cell is pretty simple and limited to types that specifically implement the copy trait. Once again, the advantage of this interior mutability pattern is that we can maintain immutability for the larger composite type like our concert to construct while still enabling an internal component or field or some small internal detail to be made mutable, once again, using the cell type. And the design pattern here is called the interior mutability pattern. All right, that's all there is to cover in this lesson, so I will see you in the next one.
