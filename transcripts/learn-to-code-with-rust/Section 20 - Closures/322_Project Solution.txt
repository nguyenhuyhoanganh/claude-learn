# Project Solution

So let's walk through a sample solution to the coding challenge in the previous lesson. First up we had to define two structs: 'SupermarketItem' and 'ShoppingCart'. So here let's begin with a 'struct' called 'SupermarketItem'. And it's going to have a 'name' field set to a String and a 'price' field set to a float. And I will also 'derive' my Debug trait implementation on the line above with my derive Debug syntax. Then I also need a 'ShoppingCart' struct, and a 'ShoppingCart' struct will consist of an 'items' field that is going to be a vector, or a collection of 'SupermarketItem' structs. So here in angle brackets I will describe the generic to be a 'SupermarketItem'. So a vector of that. And similarly I will add my 'derive' attribute to 'derive' a Debug trait implementation. All right. Onto the methods. Define a 'traverse_items' method on the 'ShoppingCart'. It should accept a mutable reference to the instance as well as an 'operation' parameter that will be a closure. So we should use a trait bound to mandate that, and the closure is going to implement the 'FnMut' trait. In addition, the closure should accept a mutable reference to the 'SupermarketItem'. So a lot going on here. So we'll begin with our 'impl' block for our 'ShoppingCart'. And we want a method called 'traverse_items'. And it's going to accept a mutable reference. So I'm going to begin with '& mut self'. And then I also need an 'operation' parameter. And for that parameter, I'm going to declare its type as being of type generic F. So I will declare that generic F right here in angle brackets. And that also allows me to add my 'where' syntax right here and provide a trait bound for that generic, which means a requirement or constraint on the traits that generic F must implement. Generic F can still be a variety of types, as long as the type implements the given trait that I specify here, and as soon as I add that 'FnMut' trait requirement, it will sort of intrinsically force to be an invocable procedure.F So it's actually going to significantly reduce the types that F can be to just basically closures and functions. So here I will declare F with my colon. And I want to declare that it's going to be enforcing the 'FnMut' trait. And I want the closure itself to accept a single mutable reference to 'SupermarketItem'. So here I will declare a mutable reference to a 'SupermarketItem' and provide my curly braces here. So in the body of the 'traverse_items' method, we want to perform an iteration over all of the 'SupermarketItem' structs in the vector using a while loop. And so for each vector element, we're going to be invoking the 'operation' closure that is going to be passed in. We're going to give each invocation of that closure a mutable reference to the 'SupermarketItem' struct that we are iterating over. So this is similar to the example we saw a lesson or two ago, but now we are passing a mutable reference to the closure. So each closure is going to be able to customize how it wants to alter the contents of each 'SupermarketItem' through the actual logic of the closure, which we don't know at this point. That's going to be up to the caller that's going to invoke the 'traverse_items' method. Regardless, we can take care of the operation that is common each time, which is the actual iteration and the invocation of the operation, closure and the passing of our mutable reference to a 'SupermarketItem' to that invoked closure. So here I'll declare a 'final_index' variable. I will take my shopping cart. I'm going to get its 'items' field. That's going to give me the vector. I'm going to call the 'len' method on the vector to get its total count of items, and I'm going to subtract one to get the final index, because the final index is one less than the length because the index starts counting from zero. And here I'll also declare a mutable variable called 'current_index' set to zero. And I'm going to declare a 'while' loop. So as long as 'current_index' is less than or equal to 'final_index', I want the body of the 'while' loop to continue going on. And before I forget, let me make sure to increment 'current_index' by one at the end of each loop to make sure I avoid an infinite loop. And what do I want to do with any given index such as 0? Well, I want to get a mutable reference to the 'SupermarketItem' in my 'items' vector at that index position. It's going to be quite a bit of code on a single line, but it looks like this. So we begin with 'self', which is our 'ShoppingCart' instance. That has an 'items' field that's going to give me a vector. I'm going to use square brackets to index into the vector at a given index, which will be dynamic on each loop, and that is represented by the current index. And this is going to extract the actual 'SupermarketItem' at that index within the vector of items. But I don't want this. I want a reference to it, more specifically a mutable reference to it, because that is what I'm going to be passing in to my 'operation' closure. So this is going to create an immutable reference, but I want a mutable one. So I'm going to add the 'mut' keyword here. And so this is going to extract the actual mutable reference to a 'SupermarketItem'. I can assign it to a variable like 'current_item'. And our inference should hopefully tell us that we have the expected result. And indeed we do a mutable reference to the 'SupermarketItem'. This does not need parentheses since it's just a regular field. And now that I have my current item, I can invoke my 'operation' closure and pass in 'current_item', which already matches the expected signature of being a mutable reference to a 'SupermarketItem'. That's exactly what we mandated right here. So when we pass in the closure in the main function, we're going to receive a mutable reference to each one of these 'SupermarketItem's that exists within my 'items' vector. The only thing we need to add to get this code to compile is to make our 'operation' parameter, the closure mutable with the 'mut' keyword, and that is because the closure itself is technically going to be redeclared each time, because there's multiple invocations of the closure, each with different captured data. Basically, if it tells you that your closure parameter is immutable and it needs it to be mutable, just follow the compiler's instructions and add the 'mut' keyword before the name that needs to be made mutable. In this case, that name is a parameter right here. So we'll add 'mut' right here. And that should do it. All right. So that takes care of everything here. Then we also want to define a 'checkout' method also on a 'ShoppingCart'. So right here, 'checkout'. And it should take ownership of the instance. So I can just pass in 'self'. And it should also define an 'operation' parameter that will also be a closure. But this time our trait bound is going to enforce the FnOnce trait. So once again I'll call my generic F. I will declare it in angle brackets. I'll add my regular curly braces for the method body. I'll add my 'where' keyword after my parameter list, and set F and its constraint to be a generic type that must implement the FnOnce trait, and furthermore, the closure should accept a single parameter of the actual shopping cart. So here I can either declare 'ShoppingCart'. So when I invoke the closure, I will pass in full ownership of a 'ShoppingCart' instance. But as you may also recall, whenever we are within an 'impl' block, we can use the capital s 'Self' keyword to refer to the current entity that we are defining methods on, and we're still within that 'impl' block. So if you want to, you can also write capital S 'Self' here. And that's going to be totally valid as well. So now in the body, all we want to do is invoke the 'operation' parameter. There we go. And pass in the 'ShoppingCart' instance represented in this case by the 'self' parameter. So basically when 'checkout' is called, it's going to take ownership. And we're not going to return 'self'. But what's going to happen is we're going to essentially pass ownership of the 'ShoppingCart' instance to the closure. Right. Because the closure is going to receive it as this parameter right here. And we're passing in ownership of the instance to whatever closure we provide to 'checkout'. So 'checkout' is only going to be able to be called once on the 'ShoppingCart' instance, because it's going to -- basically not deallocate -- but move ownership to the closure. And the closure will have full control over the shopping cart after that point. And that can only occur once. So thus we cannot invoke 'operation' multiple times. And thus it makes sense to place a constraint of FnOnce. The same rules don't apply here. We have 'FnMut', so we are more than welcome to call the 'operation' closure argument as many times as we would like. All right. So now we are ready to actually test this in action. So in the body of 'main', create a 'ShoppingCart' instance. So right here I'll declare a variable called 'shopping_cart'. And I will provide a 'ShoppingCart' struct. And we need two things here. We need an 'items' field set to a vector. And inside the vector we're going to have 'SupermarketItems'. And those need two fields: 'name' and 'price'. So right here I'll create my first 'SupermarketItem'. And I can literally just copy these values right here. Let's do a name of 'APPLE'. And this must be a string. And I currently have a string slice. So I will call the 'to_string' method on that to create a heap 'String'. And I'll do a price of 3.99. Then I will provide my comma right here to separate my 'SupermarketItem' structs. Let me just copy and paste this down below and I will replace this with 'BANANA'. I will replace this with 2.99. I will make sure to provide a semicolon right here to finish up the declaration and assignment of my 'ShoppingCart' instance. And that should do it. All right. Invoke the 'traverse_items' method and pass in a closure. The closure will accept a mutable reference to each 'SupermarketItem' struct. And we want to decrease each item's price by 15%, which we can do by multiplying the existing price by 0.85 and overriding the value of that respective field. Now remember we're going to be updating this value on each 'SupermarketItem', but that lives within the 'ShoppingCart' itself. So we need to make the original owner of the 'ShoppingCart' mutable as well, to enable the operation we're about to do. So right here I'll add the 'mut' keyword. So on a 'ShoppingCart', we have our method called 'traverse_items'. And remember 'traverse_items' is going to accept a closure. We define that requirement right here. And that closure is going to be able to accept a mutable reference to each individual 'SupermarketItem' stored within my 'items' vector. So here I'm not going to be getting any cart. I'm going to be getting an individual item within it, right? So right here I can provide a name for that like 'item'. And you can see my inference will show me it is going to be a mutable reference to a 'SupermarketItem'. So now that I have a mutable reference, I have permission to not just read a field on my 'SupermarketItem', but also modify it. And that's the beautiful part here is each closure can customize how it alters the contents of these 'SupermarketItem's struct through the mutable reference. Right? And then I can do another closure, which I'll do in just a moment. Once again invoking 'traverse_items' but doing something totally different. So I've once again isolated the thing that is common, which is the iteration and the passage of the mutable reference of 'SupermarketItem' to the closure. But I've also allowed variation for each closure to do something different. So you can imagine something like this could be our library code, while somebody else that is taking in our code and reusing it can customize what they do with our 'SupermarketItem' construct in their own closure, and do something totally custom and distinct. All right, so I have my 'item' price. I can take it and lower the price by multiplying this by 0.85. This does not overwrite the field. It just gives us a brand new float. What I can do is I can simply overwrite 'item.price' like so. And the reason we have permission to mutate the 'SupermarketItem' is because we receive a mutable reference. If you want a little bit of a shorthand for this, you can also simplify this to asterisks equals (*=) and that will do the exact same thing. All right. Now here's where the magic comes in. Invoke the 'traverse_items' method again and pass in another closure. This time around, take the 'SupermarketItem''s name, lowercase it, and overwrite the existing 'name' field value. So we are asked to once again change something in each 'SupermarketItem' and we can accomplish that in our custom closure. So once again, I'm going to take my shopping cart. I'm going to navigate over all of the items one at a time, and my closure is going to indicate the procedure to apply for each mutable reference to a 'SupermarketItem'. So once again, here is my closure syntax. I'll define a 'closure' parameter of item. That should be a mutable reference to a 'SupermarketItem'. I can take my 'item'. I can take the 'name' field. Totally different logic from what's going on in here. That is going to be a string, and I can call 'to_lowercase' to get a new heap 'String' in all lowercase. Once again, that's not going to change anything until I overwrite the original 'name' field on my 'SupermarketItem'. So right here I'll do 'item.name = '. And there we have it. We have two different traversals, each of them customizing each 'SupermarketItem' struct in a different manner. So that takes care of that. Next up. And finally declare a 'total_price' variable initialized to 0.0. So right here, I will declare a mutable variable called 'total_price' set to 0.0. And then we want to invoke the 'checkout' method and pass in a closure. So we still have our shopping car. We're going to pass in 'checkout' and provide a closure. Remember what this closure is going to receive. Once I write a parameter like 'cart' here, is it's actually going to receive full ownership of the 'ShoppingCart' instance. We're not actually going to be able to access 'shopping_cart' after the 'checkout' method, because it's going to be completely lost. So let me just write 'shopping_cart' right here and show you, regardless of what happens in my closure, which I will close with a semicolon. At this point, it is no longer the owner, so we do not have access. So we're going to call 'checkout'. 'checkout' is going to receive ownership through 'self' of the 'ShoppingCart' instance. And then it's going to pass in that ownership to my 'operation' closure. That closure can be dynamic. Each time we have a different 'ShoppingCart'. But here is where it's specified. So now my closure is taking ownership of the 'SupermarketItem' instance through its 'cart' parameter. So here for example we can output the 'cart' in Debug format. So let me go ahead and do that and just show you right here that if all of this works so far, then we should see our original shopping cart. But all of our prices should be reduced by 15% thanks to all of our code here. And all of our names should now be in lowercase thanks to our 'traverse_items' code here. So here for example, I'll run that and as you can see, here is our shopping cart. Here is its 'items' field set to a vector which is printed with square brackets. And that vector holds 'SupermarketItem's. You can see all the names have been lowercased and all the prices are reduced by 15%. Wonderful. So the shopping cart is going to be available right here through the 'cart' closure parameter. So the instructions ask us to call the 'traverse_items' closure once again within this closure. And that's totally a valid act because we still have this 'cart' closure parameter that now has full ownership of that cart instance. And that 'ShoppingCart' instance does support a 'traverse_items' method. So right here I can call 'traverse_items'. There we go. And what does 'traverse_items' expect? It expects a closure. All right. So this is kind of interesting because we have a closure passed to 'checkout'. And in that closure we are taking some value. And we're calling a method on that value of 'cart' that also expects a closure. So it's kind of like a nested closure in a sense, but it's totally valid. Now in this case we're going to get a problem because remember that 'traverse_items' is going to be this method right here. And it's going to be passing mutable references to 'SupermarketItem's to its closure. And in order to pass mutable references to 'SupermarketItem's which live within the 'items' field, the original shopping cart is the owner of that 'items' field, so thus 'shopping_cart' must be made mutable in order to allow any changes to any of its items inside its own vector. So once again, it's going to tell us your cart is immutable, and thus, in order to permit 'traverse_items' to modify anything inside that cart, we need to make sure we make this parameter right here inside the closure mutable as well. All right. Now the closure just needs to be written out. That's the only violation here. So this nested closure should increment the 'total_price' value so that it gathers the cumulative sum of all of these 'SupermarketItem''s prices. So we're basically checking out here and we're going to delete or destroy the shopping cart. It's going to be deallocated. And what does that lead to? That leads to the final bill that you pay at the supermarket, which is basically the cumulative sum of all of the items in your shopping cart, in this case, the apple and banana. So I'm going to iterate over every item, and I'm going to get each item one at a time through a mutable reference. So I'm going to call this closure parameter 'item'. And that should be a mutable reference to a 'SupermarketItem', much like it was here and here. And what I want to do with that item is I want to take its price. And this time around, I simply want to add it on to 'item.price'. So I'm going to do two total iterations, one for each one of my supermarket items, apple and banana. I'm going to take the respective prices and add it on to the end of shopping price or 'total_price' rather. And then my shopping cart is deallocated. It's done, but I'm going to have my total supermarket bill accumulated and I want to print that value out. So right here, print out the final value of the 'total_price' variable with a precision of two decimal points and a dollar sign. So here's my dollar sign. Here is my 'total_price' variable. And in order to modify the precision, I'm going to do my format specifier, a colon and .2 to indicate two values after the decimal point. Save that. So after we lowered all of our prices by 15% and then checked out and added them all together, here is our final total bill of $5.93, which is the sum of this and this. All right. So that is a quick review of closures. Lots of complexity here including some things we didn't see before like closures within closures. But hopefully it was a good chance to review many of the concepts that we introduced in this section of the course. All right. That's all there is to cover in this lesson. So I will see you in the next one.
