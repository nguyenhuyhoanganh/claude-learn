# The String.retain Method

Next up, let's explore a method that accepts a closure that implements the 'FnMut' trait. The 'FnMut' trait is a subtrait of the 'FnOnce' trait and 'FnMut' closure can be invoked multiple times and will borrow mutable references to any captured values. So a heap 'String', which I have declared right here and assigned to a 'game_console' variable. A heap 'String' has a method called 'retain' and 'retain' expects a closure of type 'FnMut'. The word 'retain' means to keep or hold on to. So what this method does is it iterates or progresses over the string's characters one at a time, and then it executes the closure once for each character passing in the character as an argument. And that closure dictates whether that given character is going to be kept or discarded. If the closure returns true, then that character is kept or retained, and vice versa. If the closure returns false, then that character is discarded or thrown away, and when the iteration completes, our string is mutated. So, for example, let me provide a closure. In here we are going to receive one character at a time. So this closure will be invoked once per character. So here for example, we have a total of 5, 6, 7, 8, 9, 10, 11 characters. So this closure will be called 11 times, one per character. So I'm going to call my parameter here 'character'. The closure defines one parameter of type 'char' or character. I'm going to take that character and I'm going to use the inequality operator, the exclamation mark followed by the equal sign to compare it to a lowercase letter 'a'. So what this is going to do is it's going to 'retain' or keep the characters that are not equal to a lowercase letter 'a'. So first things first, this right here is a character or a 'char' type. So it is written with single quotes as a reminder. Second thing is the 'retain' method is going to mutate my original string. So I need a 'mut' keyword right here. And then finally, I also need a semicolon right here. So once again the 'retain' method will invoke this closure once per character. So it's going to take P and say "Hey P,  are you not equal to 'a'"?. That gives it a true so it knows to keep or 'retain' P. The 'l' character is invoked one more time with the closure. Again, the 'l' character is not equal to 'a' as well, so the closure returns true and the 'l' character is retained or kept. Finally, we get to an 'a'. That returns a false for the closure because an 'a' not being equal to 'a' is false. So the closure returns false and thus 'retain' discards or throws away that a character. So at the end of this method call, we're going to get back our string with all of the lowercase 'a' characters removed. And here is what that's going to look like. There you go. So now let's take a look at the official method definition so I can Cmd or Ctrl click into 'retain'. And this should be somewhat familiar. Here is our method. Here is our F parameter of type F which is a generic. And once again using the 'where' keyword and the trait bound syntax, they are mandating that the F generic be a type that implements a 'Fn' trait, or a trait that indicates a closure. This time around, F must be of type 'FnMut', which indicates, first things first, a closure that can be called multiple times. This is a requirement because 'retain' will call the closure once for each character, so the closure needs to have the potential to be called multiple times. If we had 'FnOnce', then 'retain' would only have permission to invoke the parameter one total time, and we can't have that. We need to call it as many times as we need to to cover every character in our string. In addition, the requirements state that the closure will accept a single parameter of type character. This is a type in Rust 'char', and it must return a boolean, which is also critical because that is how Rust knows whether to keep a character or not. So, returning back to our implementation, let's as always extract the closure to a separate variable. So here I'll call my variable 'closure' and cut and paste this code the closure inside, making sure to add a semicolon here and then pass in 'closure' right here. And notice that the actual inferred type of our closure variable is 'impl' 'fn'. So technically it's not a closure that implements 'FnMut' because there's technically no mutation going on and thus there's no mutable reference. But that's totally acceptable. The 'Fn' trait itself is a sub trait of 'FnMut', so any function or method that accepts an 'FnMut' closure can also accept an 'Fn' closure. An 'Fn' closure can also run multiple times. It won't move ownership, and it won't move any any captured values at all. Because in this case, I'm pretty sure that character implements the Copy trait. So each time it runs, it's simply going to be getting a copy of the original character anyway. So we are still honoring the parameter type and the return type as well. This closure does expect a character as a parameter and returns a return value of a boolean, but the fact that it's 'Fn' is not going to be problematic. It's totally acceptable. So to show you an example of a more complex closure that does implement the FnMut trait, what we can do here is something a little bit more complex. Let's say that I want to keep all of the characters that I have removed from my original string. So every time that I iterate, let's say I do run into an A, I'm going to remove it using the 'retain' method. But let's say that I want to concatenate every single one of those characters to its own separate string that I'll build up. And this is just to show us how this change in code will change the inferred type of closure. So here, let me declare a separate string, which I will also make mutable called 'deleted_characters', and I'll instantiate it to be just an empty heap 'String'. And now I'm going to take my closure, and I'm going to buy myself some space with a pair of curly braces and write some more expansive logic. So first up, I'll declare a local variable called 'is_not_a'. And this will be the exact same check that I previously had. I'm going to check whether my character that I'm iterating over is not equal to the lowercase 'a'. So this is going to give us the boolean that we want. And we ultimately still want to return it to satisfy the expectations of the 'retain' method and the declaration that it required for its accepted closure argument. But since I have more room to work with here, I can take some additional steps of logic. So, for example, I can use an if statement to check if it's not a. Just like we previously did. I want to return a true here to indicate that this is a value that should be kept, so 'a' should not be kept. If it's not 'a', we return true to indicate. Please keep it because it's not 'a'. And then in an else clause I will ultimately return false. So currently we are just mimicking the exact same example we previously had. But now since I have the extra closure body to work with, I can do something like take my 'deleted_characters' string from above and call the 'push' method and take the current character that I'm iterating over. So this is a character that will not be kept, that will not be retained. And if that's the case, I'm just going to 'push' it on or add it on to the end of deleted characters. So I have a string that keeps track of everything that was removed. So once again, we have the benefit of a closure body to write this more advanced logic. And notice that now the type of our closure variable is inferred to be 'FnMut'. And why is that the case? Because now the body does need to borrow a mutable reference to the 'deleted_character' string in order to call the 'push' method on it. This is still an operation that can run multiple times. In fact, it will because we're going to run into the 'a' character not once, but twice. So on both of those invocations of the closure, we're going to 'push', which means we're going to have an updated mutable reference to the 'deleted_characters' String as it changes. But regardless, it's still satisfactory. It's a type of closure that implements the 'FnMut' trait, and the closure expects a single parameter of type character and returns a 'bool', so we are perfectly satisfying the 'retain' method's closure expectation. We've just altered this to actually be an FnMut, and so after I call the method, I can print out my modified 'game_console' string and also show you all of the characters that have been deleted, which should be the two 'a''s that were removed from "PlayStation". And there you have it. There are two a's that we added using the 'push' method, which forced a mutable reference, which itself forced a different inference of an 'FnMut' closure versus a regular 'Fn' closure. All right. So hopefully more of these examples are helping to elucidate some of these differences between these three traits. And with that I will see you in the next lesson.
