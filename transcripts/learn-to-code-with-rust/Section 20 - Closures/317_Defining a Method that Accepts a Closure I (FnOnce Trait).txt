# Defining a Method that Accepts a Closure I (FnOnce Trait)

Next up, let's invert our thinking. Instead of invoking someone else's method, let's define our own method that will accept a closure as an argument. So we're going to define our closure type as a generic. Then we'll add a trait bound and will require that that generic type implement one of the three 'Fn' traits. Now in the case of inline closure definitions that we've seen before, Rust will infer which one of the three traits that the closure implements based on what its body does. When it comes to our parameter definitions, we get to choose which one of these three 'Fn' traits we would like to mandate for that argument. Just like the developer who wrote the 'unwrap_or_else' method decided for their method. So let's begin by defining a new struct called Vault. And Vault is going to consist of two fields. There's going to be a 'treasure' inside the vault, which will be of type 'String'. And then in order to access the treasure, the user will need to provide a 'password', which will also be a String. And my intent in just a moment will be to define an 'unlock' method where we're going to accept the user's password through a closure, and if it matches, I'm going to give them the treasure. And if it doesn't match, I'm going to give them nothing. Before we do that, let's quickly add our Debug trait implementation above. There we go. And next up let's start defining our methods. So right here I'll declare an 'impl' block for Vault. And as I mentioned we want the Vault type to support an 'unlock' method that will allow the user to provide a password to unlock the vault's treasure. So here I'll define 'unlock'. Here are my parentheses and curly braces. And I want to take ownership of the Vault instance. So I'll declare 'self' as the parameter. And what I want to do is deallocate the Vault immediately after the 'unlock' method runs. And what that's going to simulate or emulate is this notion of the vault having only a single opportunity to be unlocked. The user is going to invoke this 'unlock' method, and they have to crack the vault by getting the password right. Otherwise, the vault disappears. It's kind of like a fun, self-protective mechanism that can mirror like a spy novel or a spy movie, right? You'll see how this works in just a moment. So next up, let's define a parameter called 'procedure'. And this will represent our closure. And I'm going to declare a generic type of F. Here, I'll put my capital F. And in order to enable that I need to also provide my angle brackets here to declare the F generic. The generic name is arbitrary, but F is a common one for a closure because it is short for function. So here we have our procedure parameter set to F. We'll come back to the return type later. Now perhaps the most important part: we have to use the 'where' keyword to add a trait bound to enforce the procedure cannot just be any type F, but specifically a type F that implements an 'Fn' trait indicating that it is invokable, indicating that we can provide parentheses afterwards to call it, to run it. So here, just like with 'unwrap_or_else', I'm going to add my 'where' keyword. Then my generic is F, and I'll provide a constraint or a trait bound with my colon. And then I get to choose which trait I want. Let's use the most flexible trait, which is 'FnOnce'. As a reminder, the 'FnOnce' trait declares that the type is callable, which means it must be a closure or a function, some procedure, and in addition, it mandates that the procedure, the closure, will only be called a maximum of one time within our 'unlock' method body. Every closure will implement the 'FnOnce' trait at a minimum. There would be no point for the closure's existence if it couldn't be invoked at least once. But here we're specifically declaring that 'unlock' will not run the procedure closure argument more than once. In addition, I'll provide an empty pair of parentheses here to indicate that the closure itself will accept no parameters. And then with an arrow, I will indicate that the closure will also return a string. So very important to note here. And we're going to see this when I save and rust-analyzer formats I am not declaring a string as the return value of 'unlock'. I am declaring a string as the return value of the closure that procedure is going to represent. I'm saying procedure has to be of type F, and F is going to be a closure as mandated by the 'FnOnce' trait. And in addition, it must be a closure that accepts no parameters and a closure that returns a type of String. All right. Pretty complex. So we'll come back once again to the return value in just a moment. But in the body of the method let's invoke our procedure closure like so and assign it to a variable like 'password'. So notice a few things here. Number one, the reason the compiler is happy with our parentheses syntax is because it knows that 'procedure' is something that is callable, that is invokable, and that is because we added this constraint right here. If you did not add this line of code, this would be invalid because Rust would have no guarantee that the procedure parameter is something that can be called with the call operator, the parentheses. With this trait bound, with our requirement that F implement the 'FnOnce' trait, we now have that requirement. The second thing is, how does the compiler know that the type of the 'password variable is String? It knows that because that is the return type that we said the closure would return when invoked. That is this line right here. Therefore, Rust knows when 'procedure' is invoked, we're going to get back a String. So what I'm going to do here -- in fact let's rename this to 'user_password'. So it's clearer. We're going to get the user's password through an invokable closure, and we'll take care of that in 'main' a little bit later. But that closure invoked is going to give me a password. I'm going to compare that password with the vault's password which I can get through 'self.password'. And if that is valid, I want to return the treasure. Now there is a possibility that the user's password is incorrect. So I also have to account for the possibility of nothing. So to model this idea of presence or absence, I'm going to wrap up my treasure in a 'Some' variant of the Option 'enum'. Otherwise I'll return the 'None' variant. So here I won't just return 'self.treasure' I'll actually wrap it up in the 'Some' variant indicating that there is some value to return to treasure, otherwise I will return None. And then once again, because the 'self' parameter will take ownership over the vault and I don't return the vault back, then the vault will basically be deallocated and go out of memory. So the program will only have one opportunity on any given vault to invoke the 'unlock' method and try to crack the password to get the treasure that is located within. All right. The only thing we need to do here is now that we understand our return type, we need to annotate the type of the 'unlock' method itself, not the closure as we did here, but the method itself. So here before the 'where' keyword, I will add my return arrow and say that this will return an Option where the concrete type filling in for the generic T representing the type of data in the 'Some' variant, that is going to be a String. And once again, that is because we are returning 'self.treasure' which is of type 'String'. So that's what our 'Some' variant will contain. I want to show you immediately that if we try to invoke the procedure parameter twice, we're going to get a compiler violation. And that is specifically because we mandated the use of 'FnOnce', which promises that 'unlock' will only call 'procedure' a single time. If you wanted the capacity to accept a closure as an argument and be able to run it multiple times in this body, you would have to upgrade to either an 'FnMut' or an 'Fn' trait right here, so this places an additional constraint there. All right. So I'm going to get rid of this. All right. And now we're all set to go in the 'main' function. So here I'm going to instantiate a new Vault assign to a 'vault' variable. So here's vault. For my password, let's do a string of something like "topsecret". And for my treasure, let's do a string of "Gold". All right, so now we have to take our vault. We have to call 'unlock', and we have to pass in a closure that's going to return a String. And it's that String that we're going to compare to the nested password inside the vault. So there's many different ways we can do this. It's just a matter of creative expression. I think a really interesting idea here would be to use the stdin struct's read_line method to ask the user for a password. And then we're going to take that password as a string. We can package that user password up in a closure and then pass that closure in. So we're going to give the user the ability to crack the password. And that's going to look something like this. The very top I'll utilize my 'use' keyword. And I'm going to reach into my standard library 'std' into the 'io' or input/output submodule. And I'm going to get my 'stdin' function. And here I'm going to invoke it to get back my 'Stdin' struct. As a reminder, that has a 'read_line' method. And that method needs a mutable reference to a heap String. It's going to write the user's entry to that heap String. So here I'll declare a variable called 'user_input', which will be a heap String that I will create with the new constructor function. This is going to be mutated. So I need to make sure to make this mutable. And then I can pass in a mutable reference to 'user_input' right here like so. And of course it makes sense to explain to the user what's expected of them. So I will also add a 'println' in between with something like "Please provide a password to crack the vault". Now, as a reminder, the 'read_line' method returns a Result, and technically speaking, we should probably check for either variant. Let's just assume it's going to work. At which point we're going to have the user's entry in the 'user_input' string right here. Now, one other thing I want to do is clear that user entry of the new line character at the end. So very quickly here I'm going to take 'user_input'. I'm going to call the 'trim' method to remove the newline character at the end, as well as any additional whitespace. And I want to reassign this back to 'user_input' The problem is that 'trim' returns a 'str' slice, returns a reference to some portion of memory from the heap String that represents the user's entry without any of the whitespace. So if I want to create a new heap String from this in order to reassign back to the 'user_input' string variable, I need to call 'to_string' here to go from a 'str' slice back to a heap String, and I can take this return value and reassign it back to 'user_input' So basically this is just some really quick cleanup to make sure we have no newline character. Now can we pass in 'user_input' into 'unlock'. The answer is no because 'user_input' is a String. And our 'procedure' parameter is a closure that returns a String. It's a big critical difference and it's based on what we define here. So let's define that closure right here. I'll assign it to a variable called 'hack' because the user is trying to hack the vault. And all I'm going to do here is have a closure that returns the 'user_input' String from above. Because we are moving ownership into the closure and then returning that very same heap 'String', notice that the closure will implement 'FnOnce', because we can't move ownership from 'user_input' into the closure to return back out more than one time. It can only happen once. In fact, where that ownership is going to move is right here when 'unlock' runs and it gets that closure and it invokes it. It's going to get that user entry, that string, and 'user_password' will take ownership away from 'user_input' Right. That can only happen one time. So there's no way that this closure can execute multiple times. So our 'hack' closure conveniently implements the exact same closure type as the 'unlock' method expects. But again, there are scenarios where we can pass in a closure that implements 'Fn' or 'Fn' instead. And we'll see that before the end of the lesson. All right. So here we have 'hack'. And what I'm going to do now is not pass in 'user_input' but pass in our closure. And if we successfully unlock the vault, we're going to get back that 'Some' variant with the treasure. I'm going to assign it to an 'extraction' variable, which should be an Option enum. There we go. We can see an Option potentially holding a String in the 'Some' variant. And I'm just going to quickly print it out in Debug format so we can see what we get here. All right. So now our program is ready to run and let's see what happens. So I'm going to test both scenarios. I'm going to run the code. Please provide a password to crack the vault. Let's first provide the actual password of "topsecret". So what's going to happen here. We're going to get the user's input. We're going to 'trim' the whitespace. We're going to package that string up into our 'hack' closure. And what we're passing to 'unlock' is that closure that will return this String that the user provided. At that point, inside the 'unlock' method, it takes our 'procedure' parameter, which it knows is an invocable procedure, an invocable closure. It will run it to get the user's string of "topsecret". It will compare it to the vault's password, and since in this case it matches, we should get back the treasure of "Gold" wrapped up in a 'Some' variant. If I run this program again and we provide something silly like "Nonsense", same principle, we're going to wrap up the user's String in the 'hack' closure right here. We're going to pass that closure into 'unlock'. It's going to be invoked. 'user_password' is going to be equal to "Nonsense", which is not going to match. So instead the vault will return the 'None' variant and then automatically be deallocated. So after this line, we are not actually able to refer to vault at all, because it will now be deallocated from memory. So here we're just going to get the 'None' variant. All right. So hopefully that makes sense. This is just an example of us defining a method very much like 'unwrap_or_else' that gets to accept a closure. Now here's a variation on the previous code. One alternate thing we could do is to move all of the user input directly into the 'hack' closure itself. Remember, a closure offers you something like a function body. It can contain multiple lines of code, and if we do that, then the closure will create and return a new string each time. And what that's going to do is prevent the movement of ownership, and that's going to actually transfer the type of inferred closure that Rust sees. So for example here, I'm just going to give myself some space here with curly braces. And what I'm going to do is just drag this line into here. I'm going to create my String inside the actual closure. I'm going to 'println' in there as well. I'm going to 'move' this as well and this as well into here. All right. And there we have it. Let me just provide my semicolon here. And that should do it. So I think the important thing here though, and the reason why we're having a compiler violation is we promised that the closure that we pass into 'unlock' would return a string. And that's exactly what I intend. The problem is here, we still have our old reassignment code, so our closure is actually returning nothing, a unit, in other words. And that's not matching the signature that we declared here. So this trimmed heap 'String', we need to return it out of the closure so that it becomes the String that we expect right here. So I can get rid of this code and get rid of the semicolon. Now you can see the inference: 'hack' is a closure that returns a String. That satisfies the requirements. And I can run the code again. And you can see it is going to work. But the key takeaway here is notice now that 'hack' is no longer a closure that implements FnOnce. And once it's actually now 'Fn' because it is capable of running multiple times. And that is because each time it creates a brand new String inside the closure. So there is no transfer of ownership or any capture of values from outside. Then it print_ln, it collects the input, and then it returns that user string back out as the return value of the closure. There's no logic in here that prevents this closure from running multiple times, because there is no one-time operations like the movement of ownership. So it's just a little bit of a variation compared to the previous example that allows us to have a different type. And once again, notice that 'Fn' is perfectly satisfactory for 'FnOnce'. So just because this says 'FnOnce' does not mean that we are limited to passing in an 'FnOnce' closure, we can pass that in, but also an 'FnMut' closure and also an 'Fn' closure. All right. Finally, to close this lesson off, I wanted to give you a quick reminder of the different ways that we can use this syntax to require that a trait be implemented on a specific type, such as a parameter. So this is probably the most common version you see. But as an alternative to the 'where' syntax, you can also directly state this trait requirement when you declare your generic. So here what I can do is after F, provide my colon and just copy this line right here, right here. except I don't need the second F, I just need the declaration and let me clean up the 'where' clause right here. So this accomplishes the exact same thing, except it declares your generic F and its trait requirements in a single location. So here is our generic F, and we are dictating that F be a type that implements the 'FnOnce' trait, a closure that accepts no parameters and returns a String. Most people prefer to add the 'where' clause because it just gives a little bit more breathing room to the declaration of the generics and the requirements. And this can be helpful when you have, say, multiple generics, each with their own trait requirements. It can be a little bit confusing to have all the content here, but nonetheless it accomplishes the exact same result. And finally, the third option you have is to use the 'impl' keyword directly in the parameter definition. And that actually removes the need for the F generic entirely. So what we can do here is just cut this code for later. I'm going to get rid of my F generic and get rid of my curly braces. And here instead of the F, I'm just going to write 'impl' and paste the code I formerly had. So this reads exactly as you might think. We are asking that the 'procedure' parameter be some type that implements a trait. Which trait? The 'FnOnce' trait indicating a closure. So this syntax models the same idea. We are requiring that the compiler validate that any argument we pass in for the 'procedure' parameter must be a closure. It must be an invocable procedure that returns a String. Once again, the 'where' syntax that we saw is probably the most common one you'll see in production Rust code. But all of these are valid options, and they all model the same idea, which is that we are declaring some parameter and we are mandating that it implement a trait. We are requiring that it support some kind of special behavior. In this case, the behavior is being something invocable, which basically means a closure. We're not passing in an integer or a vector or a 'HashMap'. We are requiring the procedure be an actual recipe, a closure that can be called such that we can invoke it like we do here, and have the compiler be totally happy because it understands through either one of these syntax options that that is a valid operation to perform with that 'procedure' parameter. All right. So hopefully all of that makes sense. And with that I will see you in the next lesson.
