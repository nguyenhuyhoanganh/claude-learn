# The move Keyword

Rust has a special keyword called 'move' that will force the movement of ownership of values into a closure, even when the closure won't do so by default. There are certain situations where this 'move' keyword is required in Rust, particularly when dealing with multithreaded programs, which we'll cover later. So take a look at this example. This captures string closure borrows an immutable reference to the 'first_name' string in order to print it. We do not need a mutable reference and we do not need ownership for this operation. And thus Rust infers an immutable borrow, and thus the type of the closure is one that implements the 'Fn' trait. So this closure can be run multiple times, and in addition, the 'first_name' owner remains the owner of the string after the closure's declaration. So now I'm going to override that behavior by adding the 'move' keyword. This will force the closure to take ownership over 'first_name' So in other words, we're overriding the default which would borrow a immutable reference. So here before my two pipes, I'm going to add the 'move' keyword and save. And notice what happens now. Now our 'first_name' owner becomes invalidated. And we force the movement of ownership into the closure at the point of closure declaration. We overwrite that default. So this movement of ownership into the closure by force is less common. And sometimes it's not a great design pattern in a situation like this, since a borrow is sufficient for everything we want to accomplish. But again, there are operations in advanced Rust that will require this pattern of the 'move' keyword. So I want you to be exposed to it and aware of what it does. It's also worth noting that the 'move' keyword will move all values into the closure. Anything that you use in the body of the closure. For example, if I declare a second variable here, let's call it 'last_name' and make this another string of "Wonder". And here I will interpolate the value of 'last_name' as well. The 'move' keyword will move everything that it observes here into being owned by the 'capture_string' closure. So after the declaration on lines 4 to 6, both 'first_name' and 'last_name' are going to lose their respective ownership of their strings. So notice here we have both of these violations. Both 'last_name' and 'first_name' are no longer valid. We're going to see the exact same result if we look at our compiler errors. Here is the alert for 'first_name' And here is the exact same alert for 'last_name': variable moved due to use in closure. More specifically due to the forced use of the 'move' keyword, which overrides the default desire of Rust to simply borrow an immutable reference. Now, the big point of confusion for me was that the type of the closure is 'Fn', indicating that it is a closure that can be executed multiple times, and we can indeed invoke it more than once. Let me show you here. I'm just going to run 'capture_string' three times in sequence and take a look. The code apparently works. This was surprising and very interesting to me. So here's how I like to think about this situation. So it's ultimately how the moved values are used in the closure after the movement of ownership that determines how the compiler will infer the type of the closure and which one of those three 'Fn' traits it chooses. So the two strings here are captured and ownership is moved, but we can say they are not consumed. And when I use the word "consumed", I mean they are not technically lost to another owner. So none of these strings are returned or reassigned to a local variable that will go out of scope or moved to a different owner, like a vector. So in sort of this interesting, nuanced way, the strings are able to keep living alongside with the references to them for the purposes of printing them in the 'println' macro. It's a little bit confusing. So at this point I want to transition to a Stack Overflow page with an explanation that I think does a better job of explaining it than I can. So here we are on Stack Overflow, and I wanted to show you this answer to a question that was very similar to the one that I just asked, which is why does Rust infer the type of the closure to be 'Fn', and why is the closure capable of running multiple times? Why is it not 'FnOnce'? Can't the value only be moved once from its owner? Isn't that an one time operation that will necessitate a closure inference type of 'FnOnce'? So here it says yes, this point is quite confusing and I'd like to hear that because it validates that it's not just me who's struggling, but many other aspiring Rust developers. And here it reads. After I read it, I thought the same as you did that a 'move' closure was necessarily 'FnOnce' and that a non 'move' closure was 'FnMut' or may also be an 'Fn', which is what we saw in our situation. But this is kind of backwards from the real situation. The closure can capture values from the scope where it's created, and 'move' controls how those values go into the closure, either by being moved or by reference, and 'move' forces it to be a movement of ownership rather than reference. But it's how those values are used after they're captured that determines whether the closure is 'FnMut' or not. And here we can swap 'FnMut' for 'Fn' as well. If the body of the closure consumes any value that it captures, then the closure can only be 'FnOnce' after the closure runs the first time and consumes that value. Consumes means uses it to the point that it cannot be used again. Then it cannot run again. So as you mentioned, or here are some examples of what the idea of consumption means. You can consume a value inside a closure by calling 'drop' on it or in other ways, but the most common case is to return it from the closure. The other example we saw, besides returning from the closure was assigning to a local variable. So it is those operations that mean that we are consuming a value, which means using it to the point that it can no longer be used. So in our case, our 'println' moved ownership, but it did not consume the value. There's nothing kind of transferring ownership away to the point that that string or the reference that is needed for 'println' is forever lost. So because of that, I know it's a weird nuance. The closure can technically keep running again and 'println'-ing that string over and over again, right? If the body of the closure does not consume any of its captures, which was our situation, there was no return of the 'move' strings. There was no transfer to another owner. Then it's going to be 'FnMut' or 'Fn', whether it was 'move' or not. If it also doesn't mutate any of its captures, which was our situation, then it's going to be 'Fn', right? It's capable of being run multiple times. Pretty interesting, complex, nuanced thing about Rust that I often forget. But still, the purpose of the 'move' keyword remains the same. It forces the transfer of ownership away from the original owners to the closure in situations that the default inference of Rust, or the default behavior of Rust would not normally do. We are overriding Rust's default, and right now we cannot see the benefit. But there are certain situations which we might discuss later that will require the 'move' keyword to accomplish this exact behavior. To tell Rust, "Hey, we know better than you". We do want to force the movement of ownership in a situation where you would be okay with simply a reference. All right, that's all there is to cover in this lesson. So I will see you in the next one.
