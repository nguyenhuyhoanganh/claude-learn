# Defining a Method that Accepts a Closure II (FnMut Trait)

As always, practice makes perfect. So let's define our own method that will accept a closure type that implements the 'FnMut' trait. So I'll begin by defining a struct called Location. And this will represent a location on a map. And it's going to have a 'name' set to a String and a 'treasures' field, which will be a count of treasures at that location. I can make that a 'u32', an unsigned 32 bit integer, and I will immediately derive a Debug trait implementation above. Afterwards, I'll define another struct called Map and Map is going to have a 'locations' field set to a slice of locations. This is just to have a little bit of variation, so we could try out different syntax. Now if I provide the syntax for a slice which looks like this, it's fundamentally a reference to some collection of Locations. It's going to be a slice of Locations. And the advantage here is by using the slice, we can actually provide a variety of data types. We can provide a slice originating from an array or originating from a vector here, and have the Map struct be able to support either. But the tradeoff here is, as soon as I declare this, the compiler and the borrow checker will be unsatisfied until I provide the lifetime annotation, and that is because our struct is storing a reference in its field. So as a reminder here, after the name of Map, I can provide my angle brackets. Declare a lifetime of 'a. That name is up to us, and then I'll mark this reference with 'a as well. And all this does is it tells Rust that the lifetime of the Map struct will end before the lifetime of the locations referenced. The original data source that we took our slice from to assign to the 'locations' field. We cannot have the Map outlive the original data source, because if it does, then the 'locations' field will be a dangling reference. It will be a reference or a slice to some data that no longer exists. And that's basically what we're promising Rust here. That Map will die out before whatever data source we get a reference to for the 'locations' field. All right. So next up, let's define an explore method on our Map struct. And what this method is going to do is iterate over every available location in this slice and then perform some kind of action with that location. And that action which we can customize, will be represented by an 'action' parameter that will be a closure that will implement the 'FnMut' trait. So the advantage of this 'explore' method is we can pass in a variety of different closures, indicating how we would like to explore all of the locations and have that flexibility on what to do while having the 'explore' method handle the actual iteration, which basically means the progression from the beginning to the end of the slice of locations. We're going to see all of this in just a moment. So when declaring methods for a Map struct because it's type intrinsically involves a lifetime, we need to use the same annotation for lifetimes that we would use for generic types when declaring the 'impl' block. So here, after I add the 'impl' keyword, I will provide room for a generic lifetime. I'll call it 'a, then I'll write Map and then 'a here as well. And then my curly braces for the the methods themselves. What we're saying here is for any hypothetical lifetime 'a, define the following methods for any Map for that lifetime 'a which basically means any map instance whatsoever. Remember, as soon as we do this, the 'a lifetime is part of the map struct's type definition, and thus we cannot use the map type, such as in an 'impl' block, without clarifying the explicit lifetime that is attached to that type. So in order to do this, we need to declare that generic lifetime. 'a here, which basically clarifies for the borrow checker: It's some arbitrary hypothetical lifetime 'a. For any lifetime called 'a, define these methods for a Map with that lifetime. Very much like something like this. For a generic would mean define these methods for any generic type T. It's the same principle here, except for any lifetime whatsoever. All right, now that that complexity is done, we can actually focus on the primary thing we want to do here, which is to define a 'explore' method that accepts a closure. So here is my closure. And I will provide '&self' to accept an immutable reference to the Map. And then here once again I'm going to have my closure parameter. So I'm going to call that 'action'. And I will make that of type F, and F will be a generic that I can declare here in my angle brackets. And then here's my curly braces. And here I will add my 'where' keyword. And I will enforce a trait bound. I will tell Rust to validate that my type of F must be some type that implements the 'FnMut' trait, not 'FnOnce' like we saw a lesson or two ago, but 'FnMut'. And furthermore this closure, this invokable procedure is expected to take a reference to a Location. So what the closure that is going to be passed in will have is a parameter that will accept an argument of a location reference. So the way this is going to work again is 'explore' is going to iterate or progress over this 'locations' collection. And for each location that it has, it's going to pass a reference to that location to the closure. And then it's going to be up to the closure what it wants to do with each location that it receives through a reference. All right. So I'm going to do this. And now let's write our basic iteration logic. So I'm going to use a basic while loop. And in the very next section of the course we're going to see the more graceful or proper idiomatic way to do this in Rust, but this will solve the problem for now. So the first thing I'll do is I'll find the final index in my slice. So I will take my 'locations' slice and I'm going to get its length. And then minus one will give me the final index like so. And then I'm going to declare a mutable variable called 'current_index', which will keep track of what index I'm currently on set to 0, and then I will iterate while my current index is less than or equal to my final index. So as long as this value is less than this one, which is going to go all the way until the end of my slice, I'm going to keep looping. So I'm going to get the current location on a given iteration. And this topic was mentioned in the while loop section way back in the course. So hopefully not too much need to repeat here, but I'm going to get the current location by borrowing and I'm going to reach into my location slice on each iteration in the while loop, and based on whatever current index I have, I'm going to reach into my slice at that index position represented by current index, pull out that location, and because I don't want to take ownership I'm going to provide this borrow operator here. So 'current_location' will indeed be a reference to a Location thus preventing any kind of transfer of ownership. All right. And what do I want to do with that location reference, which will vary on every single loop iteration? I want to invoke my 'action' closure and pass in that location reference. So here I will take my 'action' parameter which is a closure. I can invoke it and pass in current location. And you're going to notice we're going to see a little bit of an issue here. And it says here cannot mutate immutable variable 'action'. Remember, when we have a closure that is going to be potentially borrowing a mutable reference to modify it, the actual declaration of the closure changes each time it runs. In this scenario, let's say we have a slice of three locations. This logic is going to run three times, once for each location, and thus 'action' is going to be invoked multiple times. That's totally fine because we do not have the 'FnOnce' limitation. We have the 'FnMut' trait, which indicates a closure that can be invoked multiple times, but it's still going to be changing. So basically all we need to do, just like with a mutable variable declaration, is make sure our 'action' parameter is marked as mutable with the 'mut' keyword, which basically allows the closure represented by the 'action' parameter to redeclare itself multiple times. Basically, its content is changing over the course of this iteration, but that's not a problem. As long as we tell Rust, we expect it to change with the 'mut' keyword. And then very important part to make sure I don't enter into an infinite loop, I'm going to take my 'current_index' iterator variable and increment it by one. And that just ensures we increase from 0 to 1 to 2 to 3 all the way until we get to the final index, at which point we terminate and the method is done running. All right. So this iteration demonstrates that 'action' has the potential to be invoked multiple times. I can show you very quickly that if I change this to FnOnce, it's going to be invalid to call 'action' multiple times because 'FnOnce' requires that the 'explore' method will only invoke the 'action' closure one time, and that is insufficient. If we're doing an iteration like this, it could be invoking it one time, five times, or ten times, depending on the length of the slice. So it's insufficient. So that just goes to show you we need at least an 'FnMut' here. All right. So now we are good to test this out in the body of 'main'. So let's create a 'locations' array consisting of locations, and then I'm going to provide a reference to that array as my location slice right here. Let's begin by declaring a variable called 'locations', and I'll make a regular array. And let's populate it with two Location structs. So I'll create a Location instance right here. We need a name and a number of treasures. So for the first name, let's do something like "Enchanted Forest". And for treasures, let's do five treasures. And then I'll provide a comma to separate this from my second location struct in this 'locations' array. And this one can have a name of something like Mystic Mountain. And it can have a treasure set to let's say 10. So I have an array consisting of two Location structs. You can feel free to have as many or as few as you'd like. And then I will create a Map assigned to a 'map' variable. So here is my Map 'struct' from above and it needs a 'locations' field set to a slice. So it's up to me what I want to give for this. I can obviously provide the entire 'locations' array as a reference, which constitutes a slice, but I can also, for example, take just one element if I want to as a slice. It really just depends on what portion or chunk or fragment I want to give of the 'locations' array as a slice to be stored here in the 'locations' field, just so that we can test this iteration, which involves hopefully at least two runs, one for each of these location structs. Let's pass in the complete reference to the 'locations' array, which will still constitute a slice thanks to Deref coercion. All right, so that's a lot of the complexity. But now here is where the magic kicks in. We have this 'explore' method right here. We can go ahead and invoke it. And now as we explore our map, as we navigate across every location which the 'explore' method will handle, we can decide what we want to do with every Location. So for example, here we know that the closure must be a type that implements 'FnMut' and accepts a reference to a Location. So right here I can declare my closure and I know that it will receive the Location, more specifically a reference to a Location struct. There you can see that inference kick in, and it's up to me what I want to do. Let's say that I want to tabulate the total amount of treasures that are found in my map. The way I can do that is by traversing all of the locations and summing up all of the 'treasure' fields. So here's how I can do that. I can declare a mutable variable called 'total_treasures' and assign that to 0. And then what can I do as I iterate? Well, I have my Location. I'm going to get both of them in sequence as I do this while loop. And so my closure here is going to be called twice, right? Here is my closure represented by 'action'. It's going to be called once with the first Location, more specifically a reference to it. And then on the second iteration of the while loop, it's going to be called the second time with this second Location, more specifically a reference to it. And if I had more Location structs in here, it would be invoked that number of times. And that's totally fine, because 'FnMut' has the capacity to be invokable multiple times. So here I can take each 'location'. I can access its 'treasures' field. I do have a reference, but that's totally fine. Rust will dereference my reference to get to the Location struct and get its 'treasures' field, an 'u32'. And so I can take that, take my 'total_treasures' mutable variable and increase its count and overwrite the value of 'total_treasures' based on the current number of treasures in my location. And this is an example of a closure that does indeed implement the 'FnMut' trait. And that is because in order to overwrite this 'total_treasures' variable, this closure will need to borrow a mutable reference to that 'total_treasures' variable in order to upgrade it right here. So this closure, if you were to assign it to a separate variable, you would see it is implementing the 'FnMut' trait, right? So after I perform my iteration, which is handled by the 'explore' method, I can take a look at the total number of treasures I've collected, and I'm going to make sure to add a semicolon here. And let's print out the value of 'total_treasures'. And we're going to see here as we iterate over both of them, we have a total of 15 which is 5 plus 10, and that's correct. But here is the beauty of the 'explore' method and the closure pattern. That is, that we can customize what happens on each invocation of the 'explore' method. We can decide to explore our map in a totally different way. For example, say tomorrow we wanted to accomplish something entirely different. Say we wanted to capture all of these location names as strings in a new vector. That's a totally separate operation compared to summing up the total treasures. It involves a totally different field on the Location struct, but we have built our 'explore' method in such a versatile way that it simply takes care of the repetitive part, which is the looping, while allowing the closure to dictate the variety, the differences, right? This closure takes care of accessing the 'treausres' field and adding it to this, and the new closure we're going to define is going to do something totally different. So here, for example, I'll declare another mutable variable. I'm going to call this one 'location_names', and it's going to be a vector of strings, because I want to get all of these strings in a brand new vector, and I will create a new vector with the 'new' constructor function. And then once again, just like above, I'm going to call the 'explore' method. And that expects a closure which we have assigned the 'action' parameter. So here I can provide my closure. Once again it needs to take a 'location' parameter which will be a reference to a Location (&Location). No ownership is ever moved. So we are welcome to call 'explore' multiple times and just keep getting those Location references right here. And this time around, I want to take my Location and get its name. And that is going to be a String. And I want to make sure that I do not take ownership away from the Location struct. So when I get this heap String like "Enchanted Forest", I'm going to call 'clone' on it to get a totally new copy of it. And it's that distinct copy that I'm going to append to the end of my 'location_names' vector. So here I can take 'location_names', call the 'push' method to add on an element to the end, and that will be my String copy. And there you have it. So totally different closure, totally different responsibility. But now, if I take a look afterwards at my 'location_names' vector in Debug format, you can see it's going to store my two location names. Let's see if I'm always missing semicolons. This has been my my big struggle the last two lessons or so. There we go. Okay, so here is my new vector with "Enchanted Forest" and "Mystic Mountain". Once again, take a look at the kind of cleverness of this design. We defined this method that handled the commonality, the parts that are always the same, the fact that we have some slice of some unpredictable length, and we need to figure out the length of that slice and traverse it, navigate it one element at a time. That part stays the exact same each time the 'explore' method runs. But the part that is different, that varies, that we want the capacity to customize by allowing that to be represented by the 'action' parameter, a custom argument, some closure that will be declared in the future that can totally vary. We now have the capacity to do something totally different each time we call the 'explore' method. This is just two different examples, but if, for example, each Location had 10 other fields, well, then we could define ten other types of iterations, ten other types of closures that we could pass to the 'explore' method to do totally different things. And that's the beautiful part, is we've abstracted away the commonality while still giving the room for the flexibility as dictated by the closure. Both of these as well are going to borrow mutable references. This one is going to borrow a mutable reference to the 'total_treasures' variable. This one is going to borrow a mutable reference to the 'location_names' vector in order to call the 'push' method, but neither one of them will take ownership. So both 'location_names' and 'total_treasures', as we can see here and here, remain the owners and are available to use after the invocation of the 'explore' method and the subsequent invocation of the 'action' closure that we pass in. All right, hopefully that makes sense and I will see you in the next lesson.
