# Intro to Closures

Next up, let's take a look at how closures can solve the problem from the previous lesson. Closures can capture values from outside their scope. You can think of capturing a value as saving a piece of data for later use. So our goal in this lesson will be to replace our multiply_by function here with a closure that does retain access to the 'multiplier' variable from outside. Again, a closure is an anonymous function, a function without a name. And we usually use closures in contexts where we need a simple, short term throwaway function. So here's what the syntax for a closure looks like. I'm going to comment out this code first. It's going to begin with two vertical pipes and then a pair of curly braces like so. So you can think of the pipes as the equivalent of parentheses in a function definition. It is between those pipes that we define our parameters and annotate their types and, like functions, a closure can define parameters, and then they can perform a sequence of steps within these curly braces using those parameters, and then produce a final return value. So what I'm going to do is assign this closure. This is a closure. I'm going to assign it to a 'multiply_by' variable. So this is where things get really interesting because for now you can think of a closure as something function-like. But notice how we're using it. We're using it just like a value like 5. We are assigning this function-like thing, a closure, to a variable, just like as if it was any other value. Now I want to clarify that the closure itself has no name. A closure is an anonymous function because it has no name. We are assigning a nameless closure to our own 'multiply_by' variable and treating it just like as if it were any other value. So 'multiply_by', the variable, is a placeholder for that anonymous function, the closure on the right hand side of the equal sign. One interesting detail that I want you to pay attention to is this inferred type right here: impl followed by Fn and parentheses. So for an inline closure declaration like this one, we cannot manually write out this type explicitly. So if I try to, if I just try to put a colon and write the exact syntax out, you're going to see a compiler error that says 'impl Trait is not allowed in the type of variable bindings'. In other words, there are situations where we can use this syntax that we'll talk about later on in this section, but we cannot do so when declaring a closure inline and assigning it directly to a variable like 'multiply_by'. So now that we've removed the type annotation, let's pause here to consider what we're seeing at a high level. We see the 'impl' keyword. Recall that the 'impl' keyword involves traits. And as it turns out, this thing right here, Fn, is a trait and it requires that a type be an invocable procedure. So when I say invocable procedure, I mean something that can be called or executed. So the trait is pretty flexible because functions and closures can both implement this Fn trait, since they both represent invocable procedures, procedures that can be called or executed. So when we introduce traits previously, we learned that they are contracts that a type can choose to honor. A type can implement a trait if it promises to support a certain behavior. In this case, Fn is a trait that represents the behavior of being function-like, of being a sequence of steps that can be run in order, that can accept parameters and produce a return value. In fact, Fn the trait is like an abbreviation for the word function. But I do want to point out that a function is not literally the only type that implements the Fn trait. The closure we see on the right is an example of a type that also implements the Fn trait, but it's not literally the same thing as a function. The Fn trait is abbreviated for function because it indicates something function-like, and it's just a convenient name of Fn to remind you of that function-like Invocable procedure. But a closure is a distinctly different idea from a function, even though the two have very similar purposes and designs. Right, so a closure is not technically the same thing as a function, but it serves the same purpose of being a procedure that accepts inputs and produces a single output. So, to try to keep emulating our multiply_by example, let's provide an i32 return value for this closure. We do so the exact same way as we do with a function. We provide an arrow and then our return type will be an i32. And then in the body of the closure which is within the curly braces, this is where we can write as many lines of code as we want. And just like with the function, we can use the 'return' keyword and provide a return value that matches the type. So let's say return 5. So this closure has no parameters. It declares a return type of an i32 and returns the value 5. And we can invoke this closure like any other function with a pair of parentheses. So here the only issue here is that we are passing in an argument where the closure does not expect one. But if I get rid of this 5 and call 'multiply_by', we should get back the value 5 and print it out. And there we have it. Now I want you to notice that this closure and its type have updated to a different syntax. Right now it says impl Fn which is our trait. But notice now that the return type of an i32 becomes part of the inferred type. So what this is telling us is this closure implements the Fn trait indicating it's an invocable procedure. The empty parentheses right here indicate that the closure accepts no parameters, which is true. We can see that right here. And then this return value type is indicated by the i32. It indicates that the closure is a type that returns an i32. So we are using traits to model a type that is a procedure. And we can get really specific here. So later on we can see that we can require a type to not only be a function-like procedure, like a closure, but also a function like procedure that accepts specific parameters with specific return values of a given type. So in other words, this type here that Rust is inferring is different from the one that we just saw a moment ago when we were returning nothing. It has an additional requirement for the type, which is that it must declare a return value of an i32. We are honoring that with the closure on the right, which is obvious because it is inferring this type from the closure on the right. So again, this inference is really complex because it's capturing not just the logic of an invocable procedure, but all of the pieces that are part of that invocable procedure, such as the parameters and return value type. All right. So now we are good to emulate the example from the previous lesson. So we're going to declare a 'value' parameter just like with a function, but this time around it's going to be within the vertical pipes and I will provide 'value'. And I will give this a type with the exact same syntax of a colon, and it will be an i32. And here I'm going to take 'return' and provide 'value', provide my asterisk, and provide my 'multiplie'r variable that I have on line number 2. And notice that this time around it does work. So now we're going to be able to declare this closure dynamically. It's going to have access to 'multiplier' from line number 2 and multiply this multiplier of 5 by whatever we pass in. So if I provide a value like 2 that should be 2 times 5 and it should return 10. I want to show you that that does indeed work. So this closure is like a function declaration. We declare it right here, the recipe. But we don't cook it until this invocation on this line. So let's talk about this closure and more about this notion of capturing external values. So since integers implement the Copy trait, this closure definition will create a copy of the value 5 from the 'multiplier' variable. In other words, the closure captures the value of 'multiplier'. It saves it for later use. Technically speaking, it saves a copy of this value, 5, in here at the point of declaration of the closure right here. Notice as well that the inferred type of the closure has once again changed to now be impl Fn with i32 here and the return type of i32. So once again, this means that this closure is a type that implements the Fn trait, indicating that it's some kind of Invocable procedure, a function-like construct, a sequence of steps. In addition, the type knows that the closure will accept one parameter of type i32 and it will return a value of type i32 as well. And all of that is part of the inferred type of this closure. So this is technically a different type from the two inferred closure types that we saw before in this lesson. Even though they have similarities, they are all distinctly different types because they have additional criteria. Now we have requirements for both the parameter and the return value. And right now of course, these are just the inferred parts from the value on the right. But we are going to be able to define logic like this later on in this section where we're going to say, for example, we want a method that expects a closure as an argument, and we're going to be able to declare or dictate the requirements of what that closure that is going to be passed in will look like. Not just any closure, not just any function-like thing, but specifically a function-like thing, a closure, with a specific set of parameters with given types and a return value of a given type as well. So let's just finish up with a few more examples. I want to just show you that we have a lot of the exact same flexibility that we have with functions, with closures. So for example, here I can declare a 'product' closure and I can provide my closure on the right. Two pipes and my curly braces and a semicolon, because it is technically a variable assignment. It's a value just like any other. Here I just want to show you what multiple parameters looks like. So I'll declare an 'a' parameter set to an i32 and a 'b' parameter set to an i32 and return an i32 and then in the body of the closure, I will multiply 'a' and 'b' and return it as the product. So notice again what this inferred type looks like. It is a closure because it implements the Fn trait indicating a function-like procedure, but specifically a closure that takes two arguments, both of type i32 and produces a return value of type i32 as well. And below I can similarly invoke my 'product' closure, my anonymous function. So right here I'll do 'product' and pass in, say 3 and 10 and invoke it again, let's say on the next line with 5 and 6. And just like a function, we can reuse that closure in this case multiple times. This just happens to unfortunately be the exact same return value. But let me do something different like 5 times 8. And you can see they are in fact totally different executions. So you can think of this closure for now as no different than a function. But again, one primary advantage, as we saw right here, is that we can access data from the outside world, from the outer scope, and that is going to be really advantageous with certain design patterns that we'll see later on in this section. I also want to point out that just like a regular function, we have a block and we can write as many lines of logic within the closure as we want. So for example, here I can do something like a println! and output something like "Calculating product for you". And we can run this and we're going to see that text output twice before the printing of the eventual return value, which just demonstrates once again that the closure is very similar to a function: accepts parameters, can produce a return value, has multiple lines of logic, potentially if you want, but it's sort of declared in line and is technically nameless and and can be saved to a construct like a variable, or treated just like any other value in the program. So I want you to think to yourself: What can I do with an integer? And pretty much anything that you can do with an integer in Rust, you can do with a closure. You can treat it just like a value. This is now a recipe, and certainly we can invoke it in a function-like manner, but it's a recipe that's also a value. So we can assign it to a variable. We can pass it into a function, we can return it from a function, we can store it within a tuple or a struct. All of those are possible once you start thinking of this closure, this function-like idea, like any other value within the language. That's a good mental model to have. All right. That's all there is to cover in this lesson. So I will see you in the next one.
