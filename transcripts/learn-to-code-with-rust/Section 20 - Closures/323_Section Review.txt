# Section Review

Congratulations on reaching the end of another course section. As always, let's take a few moments to review everything that we learned. This section focused on closures. A closure is an anonymous function. It's a sequence of steps that can accept inputs and return an output. A return value. So closures tie in to this notion of the functional programming paradigm, which is just a way of thinking about code, how to structure it, how to model it. So functional programming views a function, and I'm using function in the broad sense here like any other value. So we can pass a closure as an argument to a function. We can return a closure out of a function. We can assign a closure to a variable. We can store a closure in a collection type. So I always like to think "What can I do with an integer?" which is one of the most primitive data types. And the functional programming paradigm says, "Well, anything you can do with that value, you can do with a function or a function-like construct like a closure". It's no different of a data entity in your program than a Boolean, or an integer or a string, etc. so we declare a closure with a pair of vertical pipes and then some curly braces. We can even omit the curly braces for a one line expression, and that's just serving to demonstrate one of the advantages of closures over plain functions. They tend to be a lot easier to write because they're meant to be used in a much more quick, throwaway one-off situation. So we have our closure inside those pipes. We can define our parameters if the closure accepts inputs, and then we can use our arrow syntax to designate the return value type. And that's the exact same as with a plain function. The compiler can actually infer the types of the closure parameters and the return values based on what the closure is doing in its body, and also based on its first invocation. So many times you're going to be able to get away with omitting or leaving out the types of the parameters and the return value. Many times the compiler can just figure it out based on what the closure is doing, or based on how it is being utilized. All right, so in order to have a closure, we have to have a type that implements one of the three Fn traits. The Fn traits represent some kind of invocable procedure, such as a function or a closure that can be called with parentheses. There are three traits in the hierarchy FnOnce, then FnMut, and then Fn, and they are all subtraits of each other. So FnMut is a sub trait of FnOnce and Fn is a sub trait of FnMute. Remember, Fn is the most restrictive one. So if you declare a closure parameter, for example, that should be of type FnOnce, we can pass in an FnOnce closure there, but we can also pass in a closure of type FnMut or Fn. Similarly, if a closure parameter is of type FnMut, we can pass in an FnMut closure there, but also a closure of type Fn and the Rust documentation will state that this is an additive operation, and that's what it refers to. It refers to the possibilities that are available to pass in for these respective trait parameters when evaluating a closure argument. Remember, the compiler is not just going to validate the actual trait and where it fits in in that hierarchy, but also the closure's parameter types and the return type. So you may pass in a closure with the right trait, but if the parameters do not match up, or the number of them don't match up, or the return type, that is also a violation. So it's getting really specific in how it's validating that you are passing in valid arguments in places where a closure is expected. All right. So closures can capture values. That is their primary advantage over a function: they can grab data from an external scope. So to capture means to save a piece of data for later use at the point where the closure is invoked. And FnOnce's closure captures values by ownership, and thus the closure can only be invoked a total of one time. That is because we cannot move ownership multiple times from the same original owner. Once the closure runs once, then the original owner will lose ownership. Now remember that transfer of ownership happens at the point of the closure's declaration, not the point of its invocation. And let's think about why that is. If Rust allowed something else to take ownership from the original owner, well, then that closure would be modeling a procedure that would then be taking ownership from something that would no longer be the original owner. It would represent invalid code. So because of that, Rust has to mandate that the closure definition transfers ownership rather than the closure's invocation. Next up, we have an FnMut closure that captures values by mutable reference, and that closure can be invoked multiple times. Even if you're modifying some kind of data type that you have a mutable reference to, you can just keep borrowing an updated mutable reference to that data type as it's updating. The key caveat there, though, is because the fundamental internals of the closure are changing to keep recapturing or redeclaring its mutable reference, remember, for those situations, you have to mark your FnMut closure parameter as mutable wWith the 'mut' keyword. You can almost think of the draft of the closure just updating itself or upgrading itself each time it runs, because it needs to keep redeclaring that mutable reference to have the most up-to-date reference to the original data value that is being updated or mutated. All right. And finally we have Fn closures,. An Fn closure captures values by immutable reference or it captures no values at all. And this closure can similarly be invoked multiple times. We also saw the 'move' keyword which forces a closure to take ownership of its referred to values, even when it normally would borrow a reference. We didn't see a particularly good use case for this in this section, but rest assured it is common in Rust, especially when we start dealing with multi-threaded programs. Even with the 'move' keyword, some closures can still be invoked multiple times. One example we saw is if the closure didn't return the moved value, but simply kept printing it, it almost like retained it somewhere in memory to be able to keep reusing it in its borrowed form. But regardless, you don't have to worry about all of these nuances. The compiler will tell you if you're doing something invalid, so don't worry about memorizing all these caveats. Just let the compiler dictate the kind of code that you write. All right, so to define a parameter that accepts a closure, we can declare a generic and apply it to the parameter. And then we place a constraint or a trait bound on that parameter. That trait bound basically tells the compiler to validate that what we're passing in is something that is indeed invocable, that is callable. And so we provide the 'where' keyword, we provide our generic type, which is commonly called F just because that's short for "function". And then we provide our colon and then we choose one of the three Fn traits. Usually as we saw, it's not going to be Fn. That's the most restrictive. Usually it's going to be either FnOnce or FnMut. And remember, we can't just write the generic and the trait. We actually need to provide the specific parameter types of the closure and the return value. That is what the compiler needs to validate at the point where we're going to pass in a sample closure argument for that defined parameter. So we need to provide those parentheses in that actual trait statement in the 'where' clause, and declare what that closure is going to accept, its data types and then the arrow and the return values data type as well. So that is the most common syntax which is declaring the 'where' keyword, but there are two other options available. Alternatively, we can force the trait constraint directly within the angle brackets at the point where we declare the generic. So once again, we just write our generic inside angle brackets like F, then colon, then apply the exact same closure declaration syntax. We can also apply the constraint directly to a parameter just in the parameter list, but for that we use the 'impl' keyword. So impl, then the exact same syntax of the trait and the parentheses and types and all that. That's probably the most infrequent one you'll see, but you'll still encounter it from from time to time. All right. Also worth noting we saw this a lesson or two ago. Plain functions can still be passed into closure parameters. I'm using closure in double quotes here because strictly speaking, the Fn traits don't really dictate or force a closure. A closure is just one of several possibilities for what we can pass in, and certainly the most common one. But what the Fn traits really dictate is just some kind of invocable procedure that can use the parentheses to be invoked, to be called, right? So functions are also invocable procedures. They can be passed in and there are some elegant design patterns that do utilize that. But closures are going to be more common. So finally we saw a couple of examples of closures in Rust. For example, the 'unwrap_or_else' method on an Option will unwrap the Some variant's data, or if it has the None variant, it will invoke the closure that it receives to arrive at that fallback value. So it's very similar to 'unwrap_or', but the 'unwrap_or' method accepts the actual hardcoded fallback value while the 'unwrap_or_else' method allows you to provide a custom closure. So now you have the room or the body of a function to do some kind of logic. You can, for example, have branching logic with an if statement and have different logic for determining what to return. So it buys us a little bit more flexibility. The real primary benefit of closures is that some original method or function can define the reusable logic, such as 'unwrap_or_else' defines the logic for extracting the Some or the None variant, or what to do in each situation. But the closure is like that injectable custom behavior that can vary on each invocation. We saw that with our examples of iteration over a vector in the coding challenge and before that. But even with things like this, it allows a method to handle a lot of the commonality while allowing the closure to handle the variation. Right. Similarly, we saw with the 'retain' method on a string, that accepted a closure that had the Boolean logic to determine if a character should or should not be kept. So once again, the Rust development team can define the complexity of iterating over a string and removing characters, deleting characters, while allowing us to clarify the logic for how a character should be retained. And this is a nice little warm up, because in the very next section, which is iterators, we're going to dive a lot into methods in Rust that accept closures as arguments. So hopefully all of this was beneficial in understanding this data type, which can be tricky, right? Because I think a lot of us are just used to basic primitives like numbers and integers. It's a lot more complex to think about a procedure, a recipe, a sequence of steps as being its own intrinsic data type. But it very much is. A closure is just a sequence of instructions that accepts inputs, that produces an output that we can pass in, like any other data type in other places. That is the entire functional programming paradigm. So you're usually going to find closures in a variety of other languages. Sometimes they're called anonymous functions, sometimes they're called lambdas, but they all facilitate that same purpose. All right. That's all there is to cover here. Hope you had fun and I will see you in the next section.
