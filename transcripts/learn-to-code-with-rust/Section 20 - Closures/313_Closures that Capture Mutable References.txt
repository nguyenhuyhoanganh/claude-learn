# Closures that Capture Mutable References

Next up, let's define a closure that captures a mutable reference. Our closure will mutate the 'numbers' vector by pushing an element to the end of it. First things first, we need to mark the 'numbers' vector as mutable with the 'mut' keyword, because the closure's content will borrow a mutable reference in order to modify it. So in order to create any kind of mutable reference in Rust, the original owner needs to be marked as mutable. And then down below I can declare a new variable called 'add_number', and on the right hand side, I'll provide my closure syntax: two pipes and my curly braces and a semicolon. And here in the body, I'll take my 'numbers' vector and call the 'push' method on the vector and push on a random element like 100. That should append that value to the end. Now you'll notice when I save rust-analyzer will remove the curly braces, but it's still a closure. It's important to note that this closure has not executed yet. We've just defined the procedure, so thus the 'numbers' vector still has its original 6 elements, but the 'add_number' closure has been declared, which borrows a mutable reference for future use. So you can think of line #3 as almost being equivalent to borrowing a mutable reference right on that line. We're going to see some of the limitations that have been introduced into our program, simply from defining this closure in just a moment. I also want you to notice that the type of the 'add_number' closure is now inferred to be something different. It is our second trait: FnMut. Notice it says here FnMut. The FnMut trait is a different one from the Fn trait that we've seen before, but it captures the same intent. This indicates that the type represents some callable procedure that we can invoke with parentheses. However, the FnMut trait indicates that this closure will capture a mutable reference to something in order to modify it. So a closure with FnMut doesn't just read a value, it also updates or writes or mutates the value. And how was the compiler able to figure that out? Once again, based on what we're doing in the body of the closure. The 'push' method appends an element to the end of the vector. In order to do so, it borrows a mutable reference to the vector. That is how the 'push' method is implemented on a vector. So observing that, the compiler knows that the closure is going to need to capture something, either ownership, mutably or a mutable reference in order to be able to have the ability to update the 'numbers' vector, and thus it assumes a mutable reference, and that becomes the inferred type. here. There's also no arrow because the closure doesn't return anything. Technically, it returns a unit and thus we just see impl FnMut without any parameters. The closure doesn't accept anything and also no explicit specified return value. So I'm going to attempt to invoke 'add_number' on the next line. And we're going to see that that is not going to be allowed. You can see right here it says "cannot mutate immutable variable 'add_number'". And 'add_number' is the variable holding our closure. So in order for this code to work, we also have to mark the closure itself as mutable. Invoking a closure is a mutable operation when the closure has a mutable reference. We're going to talk about why that is in just a moment, but for now, I just want to add my 'mut' keyword here to show you that the problem goes away. And I'm going to invoke the closure right here. Then afterwards I'm going to println! and output the value of my 'numbers' vector in Debug format and show you that it did indeed work. So 'numbers' should be these 6 elements plus 100 added to the end. And there we have it. So let's talk about why we need the 'mut' keyword here. Here's how I like to think about it. So the 'add_number' closure will borrow a mutable reference to 'numbers', but it's possible that the 'numbers' vector will be different after each closure execution. We can in fact invoke this closure multiple times. You can think of it like saving the address of a draft of a paper. If 'add_number' is called once, the vector's shape will change to include its additional value, and then 'add_number' needs to redeclare itself to have a mutable reference for the current state of the vector, which now has one more value. So you can think of that mutable reference as needing to update or change itself each time, and thus the closure that is capturing that mutable reference needs to change each time as well. So I like to think of it like the closure being assigned to 'add_number' changing every time we invoke it, and the compiler is going to keep track of those closure changes, and it does so automatically. But we do need to mark the closure with 'mut'. So for example here, if you can imagine this added on 100, say we invoked 'add_number' one more time. Well the second time that the closure runs, the mutable reference to the vector is going to have the updated version of that vector that already has the number 100, and it needs a mutable reference to that updated collection with one more value to have the correct data type to append another element to it the second time the closure runs. So the state, in other words, that we are capturing a mutable reference to is changing on every single closure invocation. So in order to capture that, you can think of the closure as redeclaring itself, and that represents a reassignment, so to speak, to the 'add_number' variable that is representing that closure. So you can see right here this ran fine. This ran fine. But each time those two executed, they were operating with a mutable reference to a different vector. Right. The first time was one with 6 elements and the second time was one with 7 elements. Now remember, the closure is capturing a mutable reference to the closure. And I mentioned that you should think of that as equivalent to a regular mutable reference declaration. So let's take a look at what happens if I try to move my println!, or rather write another one before the invocation of my closure. So notice if I just copy this line right here, the compiler is not going to be happy, right? Notice this. It says "cannot borrow 'numbers' as immutable because it is also borrowed as mutable". Well, where is that mutable borrow taking place? It happens right here on line #3 with the declaration of the closure. Even though it has not been invoked yet, the declaration of the closure needs to borrow a mutable reference for future's sake, for a future invocation at some later point. So this constitutes a mutable borrow. And remember, the rule in Rust is you cannot have another reference once you have a single mutable reference to a value. Well, when we're trying to run println!, we are trying to borrow another immutable reference to 'numbers'. And when I say another, I just mean a second one because our original reference is mutable, we're trying to borrow a second reference to the same value, and the compiler will prohibit that. So the definition, not the invocation, but the definition of the 'add_number' closure by itself captures a mutable reference, and thus the lifetime of this mutable reference is active from the definition of the closure all the way to the end of its last invocation, which happens on line #6. That's why we were able to invoke println! right here, because at that point, the mutable reference's lifetime had ended after this line. But at this point it's still being borrowed because it exists from this span of time or this region of code. And so trying to borrow another immutable reference or mutable reference for that matter, anywhere in the lifetime there of that first mutable reference is invalid. So remember, the lifetime of the mutable reference is active from the definition of the closure, not from the closure's invocation. Once again, this is Rust trying to keep us safe. If Rust allowed us to borrow another reference, then the mutable reference that we capture in the 'add_number' closure could eventually point to data that it did not expect. Imagine, for example, that we permitted another mutable reference outside the closure, and that other mutable reference cleared the vector or added elements to it, or even deallocated it. If that happened, then the mutable reference captured inside the closure would then be pointing to data that has changed since the closure was declared. So you can think of a closure that declares a mutable reference as no different than a mutable reference itself. That's exactly why if our closure declared an immutable reference, then Rust was totally fine with another immutable reference being used later on, because you can have any number of immutable references. At the same time, there is no risk because the data that it is pointing to is not changing. With a mutable reference, there is the possibility of change. So there can only be one writer at a time, and that is going to be our closure by capturing that mutable reference. Once again, you can think of a closure that declares a mutable reference as no different than a mutable reference itself. Once the compiler observes the very last use, as it does right here, then the lifetime is over. After this, and now we are able to output our 'numbers' vector at the end. A reference's lifetime is non-lexical. It does not end at the end of the block. It ends at the last point in the code that it is used. All right. So hopefully a lot of these previously introduced ideas on lifetimes and references are making sense. And also this is our first introduction to this specific alternate trait FnMut. Once again, it just indicates that our closure is going to capture a mutable reference instead of a immutable reference or no captured values at all, which is the Fn trait that we saw in the previous lessons. There's one more trait to explore, and that is FnOnce we'll see that a little bit later. But with that, that's all there is to cover in this lesson, so I will see you in the next one.
