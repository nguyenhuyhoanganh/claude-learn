# Building Option from Scratch

In this lesson, we'll enhance our understanding of the Option enum by building it from scratch. This is a really great way to learn, and that is emulating existing functionality that is already within a language. So we'll build our own enum called MyOption that will be very similar to Option. Let's avoid generics for now, and we'll declare that our MyOption enum will store a 32-bit integer within its Some variant. That will just help to keep things simple. So I'll begin by declaring an enum, and I'm going to call it 'MyOption'. It's going to have two variants, 'Some' and 'None'. For the 'Some' variant, we're going to use our tuple variant syntax so that this variant holds one sequential piece of associated data, which will be an i32. The 'None' variant will hold no associated data. So this is very similar to a regular Option. Above, I'm going to provide my 'derive' syntax, my attribute to derive the Debug trait so that we can very easily print our custom enum. And we can actually utilize this exact same derive syntax to also derive the Copy trait. And this is possible if every nested type implements the Copy trait itself. In this case, we're storing an i32 which does implement Copy. So this syntax will work out of the box. Now if we want to derive the Copy trait, you'll notice we're going to get a compiler error that says "you also need to derive a Clone trait". So we're going to provide Clone here inline after another comma. And we'll talk more about the differences between Copy and Clone later on in the course. But for now this will work as we expect. We're now going to be able to create full copies of a 'MyOption' value whenever, for example, we pass it into a function, or whenever we invoke a method on a 'MyOption' enum. Basically, this emulates the exact functionality that we have on Rust's regular Option enum. Next up, let's use the 'impl' keyword to define some methods for our 'MyOption' enum. So the syntax is 'impl' followed by the type that we are defining methods on. Then the block. And let's define our method called 'unwrap'. Once again, we're emulating the exact functionality that we get on a regular 'Option'. So here I feel comfortable declaring a 'self' parameter. In other words, I will allow this parameter to take ownership. And that's because of our implementation of the Copy trait above. This will now create a copy of the 'MyOption' enum that this method is invoked upon, and thus the original owner will not lose ownership. This is the exact same way that the regular 'Option' enum works. We saw an example a lesson or two ago when we passed it into a function. The original variable maintained ownership because the function received a copy. Same principle will apply here for our method. So we're going to extract or unwrap the value within the 'Some' variant which is going to be an i32. So that is what I will declare as the return value of the method. In the body of 'unwrap', let's use the 'match' keyword combined with self. 'self' in this case is the enum. It is the instance itself. Right? And then I can declare two different pathways of logic depending on which one of the variants I have above. Now here I do need to provide the prefix in this case 'MyOption::Some' because these are not the 'Some' and 'None' variants from Rust's 'Option' enum that are automatically available. These are custom 'Some' and 'None' variants that belong to my custom 'MyOption' enum. So we need to say that this 'Some' variant is handling this variant right here. So as we know we can provide a name for the associated data. Let's say 'value'. And what I want to do is actually return that value out of the match expression, which will also return it as the return value of the 'unwrap' method. So here we can see that 'value' is that stored i32. I'm just going to declare it as the final evaluation of this match arm, and that will basically return it here. Then I need to handle my other arm, which is going to be the 'None' variant of 'MyOption'. Now this one does not have any associated data. So we can move on straight to the arrow syntax. And here as we know from the regular option, if we try to 'unwrap' on an 'Option' that doesn't have a value, in other words an 'Option' that has a 'None' variant, the program panics and we can emulate a similar scenario of having an error in our console by using a macro in Rust called 'panic!' 'panic!' basically means crash the program, stop everything, stop execution, and print an error message to your terminal. And panic! is a macro. So we provide our parentheses here and we can provide an arbitrary error message. So let's try to be a little bit funny and just say "Uh oh". And that should do it. All right. So now we have our custom 'unwrap' method. So we are ready to test the code. So in our main function, let's create two instances of 'MyOption'. One will be holding the 'Some' variant and the other will be holding the 'None' variant. Let's begin with 'Some'. So here is 'some_option' I will reference my custom enum of 'MyOption'. Provide two colons to get to one of its variants. Let's do some and let's provide an i32 value like 100. And here I can println! and provide a dynamic value. Let's take my custom 'MyOption' enum and let's invoke our 'unwrap' method. This is our 'unwrap' method here. This is totally separate from Rust's 'Option', but it is very similar functionality. And we're going to get the value of 100 being pulled out. Let's now do the opposite one which is the 'None' option. So once again we're going to reference our custom 'MyOption' enum. This time we want the 'None' variant. There is no associated data. And once again, I can try to print out the result of calling 'none_option.unwrap', but there will be nothing to unwrap, and thus we're going to get a panic and we're going to get a similar error message. And here is our custom message of "Uh oh". So this works very similarly to how the regular 'Option' works. Just for some extra practice, let's also define the 'unwrap_or' method. So here once again all of our methods will live within the implementation block. So I'll define another method with our function syntax. This one will be called 'unwrap_or'. Once again, I'll define a 'self' parameter to represent the instance of the 'MyOption' enum. This method will take ownership but ownership over a copy, so we're not going to lose the original enum. I can prove this by the way very quickly by showing you that after we unwrap here, I can still output 'some_option' afterwards like so, and you can see it is working just fine, which proves that 'unwrap' took ownership over a copy. The original 'Some' option or the original 'my_option' instance remains valid, so that is just proving to you what's going on here. So getting back to where we were, once again 'unwrap_or' is our method name. And this time around we need to provide an additional parameter which represents the fallback value to return iIn the case that we have a 'None' variant. This will be a type that should be of the same type as what the some variant is storing, because this is the backup. So I'm going to declare this as an i32. And no matter what, this method will also return an i32. Either the integer within my 'Some' variant or my fallback value. So the code is very similar. I'm going to match on 'self', which is my instance. I'm going to provide both of my arms starting with covering the 'Some' variant. If I have the 'Some' variant, I can provide a name for the associated data. Let's once again call it 'value'. And once again, just like with 'unwrap' above, we're going to return value from the match expression, which in turn will return it from the 'unwrap_or' method. Then, for 'MyOption::None', this is where the difference lies. We'll once again provide our arrow. But instead of panicking, what we instead provide as the final return value of the match expression will be our 'fallback_value' parameter. And so now either one of these arms and one is guaranteed to match, but either one of these arms will produce an integer. So now 'unwrap_or' has no chance to panic in comparison to 'unwrap', we're always going to get back a valid integer return value. So here I can call 'unwrap_or' and provide a value like 13. In this case we do have the 'Some' variant. So we're going to unwrap the value inside of it which is still 100. So 'unwrap_or' doesn't need to fall back to that default value. But here's the difference with our 'None' option and our 'None' variant. Now I can call 'unwrap_or' and pass in an arbitrary number like 13. Now we do not have the 'Some' variant. We have the 'None' variant. So when we call this method, we're going to match on this arm and return the fallback value which is 13. And so now instead of a panic, we're going to get a 13. All right so these are two examples of methods that are available on the 'Option' enum in Rust. And hopefully this helps to lend clarity on what's going on behind the scenes. When we use the 'Option' enum within our code. All right. That's all there is to cover in this lesson. So I will see you in the next one.
