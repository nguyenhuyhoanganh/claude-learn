# Top-Level Option Variants

So let's kick this lesson off by talking about the Rust prelude. The Rust prelude is a collection of named constructs that are made available automatically in every Rust program. You can think of the types, functions, macros, and more that are available to use out of the box in Rust. For example, the prelude includes the 'Option' enum, which is why we're able to write 'Option' here directly within our program. So another way to think about it is it's all of the top-level names that are available for us to use in a Rust program. Later on in the course, we'll see that some other features of Rust require us to manually import them. They require us to manually specify specific features that we would like to make available in a given Rust program, but because things like 'Option' are so commonplace, Rust makes them available automatically. All right. That's why we're able to write 'Option' just like this. Now it turns out that the two 'Option' variants, which are 'Some' and 'None', are also automatically available at the top level. So while the syntax that we have on screen is totally valid, we can simplify it even further by removing the 'Option' enum prefix. In other words, we can remove the 'Option' keyword and the two colons and accomplish the exact same result. For example, here I'll just remove everything before 'Some'. Same thing here. And on this line everything before 'None'. So everything still stays the same: 'Some' and 'None' are still variants of the top level 'Option' enum. This is just a syntactic shortcut. It's a simplified form. Both of these variants are just available to use at the top level of Rust just like this, because they are so common, because they are so frequently used. Now, when it comes to this function, we still need to keep the 'Option' as the return type because we are specifying the complete 'Option' enum. So without 'Some' and 'None', we are specifying the top level enum itself, which is 'Option'. But any time we are providing 'Option' as a prefix before 'Some' or 'None', that is where we can remove it. But here I can once again remove this and this and this. This code is functionally the exact same as what we saw in the previous lesson. This is just a shortcut that is made possible because the 'Some' and 'None' variants within the 'Option' enum are made available at the top level of Rust, just like the 'Option' enum is itself.
