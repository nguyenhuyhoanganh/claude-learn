# Project Solution

So let's walk through a sample solution to the coding challenge in the previous lesson. This time around, we were modeling a restaurant and food delivery system. I still don't know where I come up with some of these ideas from time to time, but hopefully you had fun with it, so let's walk through it. First up, we had to define a Food struct with a single name field set to a String. Let's do that above 'main'. So here is my struct called Food and inside the curly braces I will provide a 'name' field set to a String. And then I will derive a Debug trait implementation on the line above with hashtag square brackets. derive is my command and Debug is the trait I want to implement. Afterwards we were asked to define a Restaurant struct. So let's add Restaurant right here. And this one is going to have a 'reservations' field set to a u32, an unsigned 32-bit integer, because we can only have a positive number of reservations. And then we were also asked to add a 'has_mice_infestation' field set to a Boolean. So I'll do that and also derive a Debug implementation for this struct. So same syntax above. Hashtag square brackets. We'll derive the Debug trait. Next up, define a 'chef_special' method on the Restaurant struct and the method will return the Restaurant's famous dish, but it's going to be wrapped in an Option that may contain the Food struct, and that Food struct is going to be within the Some variant. So let's begin with our impl block to define methods for a Restaurant, and my method will be called 'chef_special'. And by default, unless I have a specific reason to take ownership, I'm just going to assume an immutable reference. So I'll provide &self here. And this method will return an Option containing a Food struct. And again an Option is going to store any associated data in the Some variant. The None variant has no associated data. So here I have to designate my return type after the arrow. An Option is going to be insufficient because Option defines a generic, and we need to tell the compiler the exact concrete type that is going to play the role of T, the generic, which also represents the type of data stored in the Some variant. And that is going to be our Food struct from above. So here I'll provide my angle brackets and designate that my concrete type playing the role of the generic is going to be specifically Food. And now the compiler will hold me responsible not just for returning either Some or None, but for making sure that my Some variants store this data type rather than another data type. So moving on. If the restaurant has a mice infestation, return the None variant. There is no chef special. So right here I need to check whether I have a mice infestation. So I can begin with an if statement. And self is my immutable reference. I can use it to access any fields or invoke any methods on my Restaurant. So I will reach into the 'has_mice_infestation' field and that is already a Boolean. So if this gives me back a true, then my if statement represents the state of having a mice infestation. So here I can provide my if statement. And I'm going to do an early return here manually with the explicit return keyword. And I will return the None variant. There is no chef special. The restaurant is flooded with terrible mice. Okay. If the restaurant has less than 12 reservations, return a food instance with the name of "Uni Sashimi" wrapped in the Some variant. And if it has 12 or more, it's also going to be a Food instance also in the Some variant but with a different food name of "Strip Steak". All right, so the remaining logic here is just going to be another if statement. So let's check if the restaurant has less than 12 reservations. So I will reach into self and access the 'reservations' field. That's going to give me my unsigned integer. I'm going to going to check if that is less than 12, and if that is the case, I am going to return here and let us return our Some variant this time around. It's still an Option enum, so we're still satisfying the return type here and here. But this time around, I need to make sure my Some variant stores the associated data of type Food. So I will instantiate a new Food struct. I can do so inline. If you assign it to a separate variable, that's totally fine. I'm just going to do it right here. And of course I need a 'name' field set to a String. So I'll provide a String::from right here. And this is going to be a "Uni Sashimi". And then I will add an else statement to take care of the other scenario where reservations are 12 or more. And it's going to be the exact same thing. I'm going to return the Some variant also storing an associated data of type Food. So here is my Food struct instance and I need to instantiate it with a 'name' field as well. And this is this is going to be a different String. This one is going to be "Strip Steak". So maybe maybe the chef does a nice dinner special of Uni Sashimi which is a bit more rare if they have a small number of guests for the night. So let's save that. We can see the compiler is happy, but we can do one more optimization here. And that is since we know that either this line or this line is going to be the very last one that is going to be evaluated by the compiler if the code gets here, well, then the 'return' keyword is not strictly necessary. And the more proper Rust way to do this is to make an implicit return. So I will remove the return right here and remove the semicolon, which is also critical. And right here I'll do the same thing. I'll remove the 'return' and remove these the semicolon as well. Now either this will be the last evaluation and will become the implicit return value or this will be. In this scenario we do want to explicitly return because if we do not put the 'return' keyword, then nothing's going to happen here. We're going to going to process None but not return it. And then the code is going to try to go into one of these and return Some when we really intend to return None. So this 'return' keyword early is required. The ones here are not. All right. Next up define a 'deliver_burger' method on the Restaurant as well. So right here 'deliver_burger'. Once again, I'm just going to assume an immutable reference. So &self to avoid taking ownership. And because we do not need to mutate anything. And this one is also going to expect an address string slice. So I will define an 'address' parameter set to a string slice that is &str. And it will represent where to deliver the order. It should return a Result type where the Ok variant holds a Food struct, and an Err variant holding a String. So once again, provide my return value. Result by itself is not enough, because that enum requires two concrete types to play the role of its two generics. One of the generics represents the type of data stored in the Ok variant. The other generic represents the type of data stored in the Err. E-r-r variant. So first up we define the type of data in the Ok variant. And that's going to be a regular Food struct. So here I will define Food. And for our Err type, that is going to be a String. So I'm going to declare that here. And then my curly braces. And once again the compiler is going to hold us responsible that we return a Result. And furthermore not just any Result but a Result where the Ok variant stores this type and the Err variant stores this type. If we violate any one of those, or both, even if we return a Result, the compiler will not compile. So these concrete types filling the role of the generics are intrinsically tied to this return type and the function signature. All right. Next up, if the restaurant has a mice infestation, return the Err variant containing a string of "Sorry we have a mice problem". So once again I'll do an if statement. I'll access my instance. Let's take a look at the 'has_mice_infestation' property or field. If it is true, then we want to return, but since we are returning a Result rather than an option, this time around is going to be an Err and it's going to be a String with a text of "Sorry, we have a mice problem". Once again, I want to return early here, so I do not keep going further down and return something else. Next up, if the user's address, which is the parameter is an empty string, return a different error message. We're still returning the Err variant because we're still modeling the idea that something went wrong. But we have a different error message, right? And this one is related to the parameter. So if the user does not give us a address to deliver the burger to because the address is an empty string, then we still want to return a Result, more specifically the Err variant, but holding a different piece of error text inside it. And we're still going to honor the signature that we declared here. So let's check if we have an empty address. This will be another if statement, but this one will apply to the parameter of 'address'. As I mentioned in the hints, we have an 'is_empty' method on a String that will give you back a Boolean if the string is empty, which means it has no characters. It has a length of zero. So if this is the case, then I want to return here as well. Let's return an Err. Same variant, but a different string for its internal nested associated data. This one could be "No delivery address specified". And finally, if we get to the end here and neither of these conditions has been met, we are good to make the delivery. So return the Ok variant containing a Food struct with a name of "Burger". So right here I do not need the return keyword. I'm going to provide my Ok variant, parentheses for the associated data that lives in the Ok variant, which is going to be a Food struct. A Food struct needs a 'name' field, and I will provide that string right here. And it's going to be a burger. Save that. And that should do it. All three of our return values here, Err, Err and Ok are living up to the promised Result return type. That is the beautiful part about a Result in that it is a single type, but it is able to model two different outcomes: success and failure. Just like Option is a single return type, so it seems like it's constricting because it's just one kind of thing, right? But it's one kind of thing that can model two different scenarios. With Option, it is presence or absence, and with Result, it is success or failure. All right. So I think we are all good to go with our definitions. So now all that's left is in the 'main' function practicing with all of our code. So here is our 'main' function. Let's create a Restaurant instance. I'm going to call my first restaurant Mario's, and I'll create a Restaurant instance with 11 reservations and a mice infestation. So for our 'reservations' field, I'll set that to 11 and 'has_mice_infestation' I will set to be true. Invoke the 'chef_special' method and print out its return value. It should be the None variant. So we're going to print out these return values which are all going to be Options and Results in Debug format. So right here println!, provide some dynamic content rendered in Debug formatting, take my Restaurant and call the 'chef_special' method. So just to review this, when we call the 'chef_special' method right here, if we have a mice infestation, we should get this early return and return None. And that's exactly what we expect. So let's run that. We should see the None variant. Perfect. All right. Invoke the 'deliver_burger' method with an argument of 123 Elm Street. So right here. Same thing, except I'm going to call 'deliver_burger' with a string slice argument of "123 Elm Street". And once again, when we run this method, we still have a mice infestation. So we're going to hit this early return. And so we should still get the Err variant this time around with this specific error message. "Sorry we have a mice problem". And there we have it. All right. Now let's try a couple other scenarios. Let's create another Restaurant instance with 15 reservations and no mice infestation. I'm going to call this one Angelo's. And here's my Restaurant. For reservations, I'm going to do 15. And for 'has_mice_infestation', I'll provide a Boolean of false. Here is my semicolon. Let me in fact just copy these two methods down below. And so I'm going to invoke 'chef_special' on Angelo's. We'll come back to this one in just a moment. For 'chef_special', we should be getting the Some variant with a "Strip Steak". So going back to the 'chef_special' method. We do not have a mice infestation. So we're going to bypass this. And we have a total of 15 reservations. So this if statement will not be met. We're going to hit the else clause which means we're going to return the Some variant storing a Food struct representing a strip steak. So that is what we expect to get. And let's take a look if that's what we get. Yep. There it is. And invoke the Invoke the 'deliver_burger' method with an argument of an empty address. So let's get rid of this. Let's see what happens here. This time around we do not have mice infestations, but we do have an empty string. So 'is_empty' is going to return true. We're going to return the Err variant of the Result enum containing this string error message. "No delivery address specified". Let's see. So we have a mice problem. That's because I'm calling it on Mario's. Let's call it on Angelo's. There we go. And there we go. "No delivery address specified". And finally, invoke the 'deliver_burger' method with an argument of a valid address. This time around, I can give something like 123 Elm Street. And now we should get the Ok variant with a Food struct of "Burger". So this is false. This is now false because our address has content. We reached this part. We implicitly return the Ok variant of the Result enum storing a string of burger. And there you have it. All right. That's all there is to cover in this lesson. Hopefully you had fun reviewing these two really primary enums in Rust. You're going to find a lot of methods, including in the Rust standard library, but also in the broader ecosystem. They're going to have the potential for failure, number one, and they're going to have the potential of not having anything of value to give back, nothingness. And the Option and Result enums exist because those two situations are so common that these these tools are just a necessary part of how you think in Rust and how you model those real world scenarios. Different languages have different solutions to this problem of nothingness, as well as handling errors, or sometimes they're called exceptions in other languages. But I really do think that Option and Result are very graceful, elegant ways of dealing with those issues, especially when you combine them with something like a match statement, because you're just going to be able to cover every possible scenario. So Rust is going to force you to account for what I call the happy path, which means everything going well and also the sad path which means something going wrong, like a missing value or like an erroneous operation. All right, so that's all there is to cover in this lesson, and I will see you in the next one.
