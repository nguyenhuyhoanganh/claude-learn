# Project Solution

So let's walk through a sample solution to the coding challenge in the previous lesson. This time around we were modeling a fitness program in Rust. So all I've done so far is I've created a new cargo project from my command line from my terminal, and the instructions told me to call my project "fitness". So I executed the command 'cargo new fitness'. So fitness, of course, becomes the name of the directory that Cargo creates, and also the name of the project which is specified in the 'Cargo.toml' file. Our project is called "fitness", and that's going to be very important later on when we start reaching into our library crate root. Now by default, that 'cargo new' command will create a binary crate, a standalone application. So we already have a 'main.rs' file and I have it open here on the right. So the first instruction we get is that the project will include both a binary crate and a library crate. So in order to support a library crate, we need to create a 'lib.rs' file in the 'src' directory. That 'lib' is short for "library". And remember these file names have to be as they are, right? It's not up to us. If we want a binary crate root, we need to have a 'main.rs' file. And if we want a library crate root, we need to have a 'lib.rs' file. And we can have both and we can have either one, but we cannot have neither. We need at least one crate root. So our project is either going to be a standalone application, or it's going to have shared functionality for other crates to use, or it can be both. And that's what we're going to have in this scenario. So I'll open up my File Explorer. And in source I'll create a new file that must be called 'lib.rs'. That is short for library. And I'm going to drag this file over to the right. All right. So we're going to have three modules: diet, weightlifting, and cardio. And they're going to be declared in our library crate root. So I'm going to start writing our code in libraries. Remember a module is an organizational container. It's an enclosure. It's a namespace that you have that you can wrap around related constructs and entities in your program. It's up to you how you want to organize all of the ideas and things that you're going to model from the real world. So you can imagine in really complex real world domains, you might have 100 structs or enums or functions or methods, and just having them all at the top level in a single file quickly becomes unsustainable and very difficult to parse. So we use modules to create organization in our Rust programs. I like to think of a module as very comparable to a directory or folder on your hard drive. You create a "Music" directory, right, to differentiate it from your "Documents" directory where you place different entities and constructs. It's the exact same idea now, but with Rust code bases. All right. So what I'm going to do is begin with the 'mod' keyword. And I'm going to declare these three modules. Now I'm not going to have any definitions for them yet. And the compiler will be unhappy. But that is the point. I kind of want the compiler to enforce that. We have not defined these modules that we promised will exist in the program, and we need to use the 'mod' keyword so that the compiler is aware to look for these modules once again, either in line in a file or in a folder based structure. So right here in 'lib.rs', I'll begin with 'mod diet', then 'mod weightlifting', and finally 'mod cardio'. So now the compiler knows I need to look for these three modules. And you're not giving me anything. You're not giving me anything in line or file based or folder based. You can see if I hover over here, it'll say unresolved module. I can't find a file of 'weightlifting.rs'. or 'weightlifting/mod.rs' or an inline definition. But that's kind of what I want because as I solve these problems, these warnings should go away. All right. Next up we're going to start defining some of the constructs inside these modules starting with 'diet'. So use an inline module definition. So here I'll provide my curly braces. And we want to begin with a 'NUTRITIONIST' constant. So right here I'll do 'const NUTRITIONIST'. For constants in Rust, we need to provide an explicit type annotation. So I will make this a 'ref str' (&str), a string slice and assign it to be "Norah Nutrition". So once again this is a constant that is now nestled within or living within the 'diet' module, i.e. the 'diet' namespace. So we cannot access the name 'NUTRITIONIST' from anywhere in the program. It is hidden away or defined within 'diet' and furthermore it is private by default. So unless we declare it public with the 'pub' keyword, nothing can access this besides other constructs defined in the same module of 'diet'. So for example, the next instruction is to declare an 'ask_about_program' function that is going to be a regular function declaration. Same syntax for any one of these Rust entities. They're all just nestled within this namespace, this container. So 'ask_about_program' is going to just output text. And it's going to say "The nutritionist is Norah Nutrition". Use the constant. So I'm just going to say "The nutritionist is" and I'm going to interpolate the value of the 'NUTRITIONIST' constant. In fact, let me just copy and paste it so I don't make a spelling error. It's going to use it from above. And notice that we do not need to use the 'pub' keyword with 'NUTRITIONIST' yet, because 'ask_about_program' lives in the same module, so it immediately has access to any other constructs defined within that same module. And similarly, if we had a struct or an enum in here, this function would be free to use it and instantiate it. Once again, they're related entities, they are grouped together and they are welcome to use each other within the same module. All right, that takes care of our first module. The next one is the 'weightlifting' module. And we're asked to create a file module definition for this one as well. So here in 'src', we're going to create a new file. And if our module is called 'weightlifting', our file must have the same name. So 'weightlifting.rs' and this is the file based approach. And as soon as we create this, our error in 'lib.rs' should go away. So I'm going to save this. You'll notice the red squigglies disappear from here. So Rust understands that this module is here. Excellent. And once again because we have a file definition, we do not want to say 'mod weightlifting' in here. That would create a nested 'weightlifting' submodule within the 'weightlifting' module. Right. This file's content, this file scope already defines the 'weightlifting' module. Anything we write in here is automatically nested within the 'weightlifting' module that we declared in the library crate root. So we need a couple of things here. A 'PERSONAL_TRAINER' constant set to "Will Weight". So 'const PERSONAL_TRAINER' It's going to be a string slice set to "Will Weight". We need a similar 'ask_about_program' function that's going to output the text. "The weightlifting trainer is Will Weight". So once again, println! and we're going to say "The weightlifting trainer is Will Weight" interpolating the value of PERSONAL_TRAINER. Notice that this function shares the exact same name as 'ask_about_program' in our 'diet' module. But that's not going to be a problem. We're not going to have any collisions eventually, because we've provided each one of these identically named functions with a distinct namespace, right? It's almost like a prefix or a qualifier. This is our 'ask_about_program' in 'diet'. And this is the function belonging to the 'weightlifting' module. All right. Declare an Exercise struct with a 'name' field and a 'reps' field. All right. So 'struct Exercise'. 'name' field set to String, 'reps' field set to a u32. Derive a Debug implementation. So here hash tag square brackets, derive the debug trait ( #[derive(Debug)] ). And then we are also asked to add a 'new' constructor function to the Exercise struct that returns an Exercise instance. Easy enough. 'impl' keyword. We're implementing a function for 'Exercise'. I'll define my 'new' function as per community conventions. For now, I'm just going to assume that the function is going to receive as many parameters as it needs to provide values for the two fields, so I will accept a 'name' String and a 'reps' u32. It will return an instance of 'self', i.e. 'Exercise', and in here I'll use 'self' as well and use my shorthand syntax to declare the 'name' and 'reps' fields to be equal to the values from the respective parameters. Now all four of these constructs right now: the constant, the function, the struct, its fields as well, I forgot to mention. And the 'new' constructor function are private, so nothing outside of 'weightlifting' can reach into here and utilize these ideas yet. And while I may be tempted to just put a 'pub' everywhere, I don't want to do that yet because I don't want to expose everything. I actually want to find out what my program is going to need. So later on, when we try to access the things that we know, the outside world outside of the 'weightlifting' module will need, that's when I'm going to come back here and actually mark the things public, because it's probable that there's some things that don't need to be exposed. I don't want to expose everything. I only need to expose the things that outside land outside this module needs to utilize. Right. So we'll come back to that a little bit later. All right. So that takes care of 'weightlifting'. Back in our crate root, we still need to take care of 'cardio'. And for that we are asked to use a folder module definition to define that. So once again in order to accomplish that we're going to create a 'cardio' folder containing a 'mod.rs' file. So this time around, the folder name matches the name of the module as is declared in our code. And then within that folder, the 'mod.rs' file must be named 'mod.rs'. We cannot customize it. That is what the compiler is looking for. So this offers me a fun opportunity to show you a nice shortcut in VS code. I learned this like three years into my programming career, and was surprised and shocked that I didn't know it at that point. So you of course, are welcome to create a new folder here in VSCode by clicking this and then creating a new file. But if I want to do both at once, I can actually right-click. Say I'm creating a new file and I'm going to write the full folder name of 'cardio', then a forward slash ( / ) and then my file 'mod.rs'. And then what VSCode is going to do is create the 'cardio' directory because it does not exist, and then place the newly created 'mod.rs' within it. So it's going to do both the folder and the file in one command, which I thought was super cool. Most people probably will not think that, but I think it's cool. All right, so let's come back to our instructions in the 'cardio' module. Declare a 'PERSONAL_TRAINER' constant. Same deal. 'PERSONAL_TRAINER'. String slice set to "Carl Cardio". Really awesome that the trainers named themselves after their expertise. Similarly, we need an 'ask_about_program' function. This should really be a function that outputs the text. "The cardio trainer is Carl Cardio". So println! "The cardio trainer is" And we're going to interpolate the value of 'PERSONAL_TRAINER' from above. And that should do it. Declare a 'CardioTool' enum with two variants: 'Treadmill' and 'Bike' and a derive a Debug implementation. So 'enum CardioTool'. And we're going to have 'Treadmill' in here as well as 'Bike'. And here is my Debug trait implementation. So as you may recall, with a struct, all of its fields are private by default. With an enum, the enum itself is private, but if we mark it public, then all of its variants become public alongside it. So there's no way that you can make one enum variant public and the other private with a struct field. You can do that. All right. Declare an 'Exercise' struct with a 'day' field, a 'tool' field and a 'minutes' field. Derive a Debug implementation. So struct called Exercise. It's going to have a 'day' field set to a String. That is the day of the week that we're exercising, A 'tool' field which is going to be a 'CardioTool' variant from above. And then a 'minutes' field representing how long we worked out, which is an unsigned 32 bit integer. And I will derive a Debug trait implementation here as well. Then we are asked to add a 'new' constructor function to the 'Exercise' struct that returns an 'Exercise' instance. So once again 'impl' to declare methods for 'Exercise'. The community convention is to call my function 'new'. I'm going to pass in all three required parameters. 'day' is going to be a String. 'tool' is a CardioTool. 'minutes' is an unsigned integer. It's going to return 'self' in the body. It's going to instantiate 'self' and provide the three parameters: 'day', 'tool', and 'minutes' as the values for the respective fields. Once again, everything in here right now is private by default. But now as we transition to the library crate route in 'lib.rs', we're going to need to start making a lot of these things public to access them. So use the 'use' keyword to bring the 'CardioTool' enum into scope. That 'CardioTool' enum was defined in the 'cardio' module, which is here in 'mod.rs'. So if I try writing, 'use cardio' and try to access my 'CardioTool', you'll notice Intellisense is not even telling me it's here. And if I try to write it out, we're going to see the violation. It's private. "private enum", so we are not welcome to use it yet. So we know the solution. We go into the module, we go into the specific name, it's 'CardioTool', and we add the 'pub' keyword in front of it. Now back in the library crate root, we are welcome to bring it in. Assign the alias 'CardioExercise' to the 'Exercise' struct from 'cardio'. So once again, if I try to provide my curly braces here to specify multiple imports or imports isn't really the right term. I'm really just kind of bringing the name into scope. But if I try to bring in 'Exercise',  that, you guessed it, is private. So back into our 'cardio' module and I'm going to mark my struct as public. Now we're going to have an 'Exercise' struct in here. And we're also going to have an 'Exercise' struct in the 'weightlifting' module. And we're going to be using both of them. So if I try to bring both of these names, there is the potential for a name collision, a clash. That's why we are asked to assign the alias of 'CardioExercise' specifically to the 'cardio' module's 'Exercise' struct. So here in the library crate root, after I specify the name from the 'cardio' module, I'm going to add the 'as' keyword and give it an alternate name that I can use in this file. And that's going to be 'CardioExercise'. We're going to do the exact same thing for the 'Exercise' struct in 'weightlifting', which means we need to make that public as well. So in 'weightlifting', I'll add my 'pub' keyword here. That will allow me in here to reach into my 'weightlifting' module. And in here we're going to find our own 'Exercise' struct. Same name, totally different struct. And once again I'll add the 'as' keyword, which isn't strictly necessary because this one is now called 'CardioExercise' But as I mentioned, if you're going to give one of them a nickname, you might as well give the other one a nickname for clarity. Right? So this one is going to be our 'WeightliftingExercise'. All right. Define a 'GymWorkout' struct with a 'cardio' field, which is going to be the 'Exercise' struck from the 'cardio' module and a 'weightlifting' field, which is going to be an 'Exercise' struct from the 'weightlifting' module and implement the Debug trait. So now we're assembling this totally new type in our library crate root called 'GymWorkout'. And a 'GymWorkout' is going to consist of both a cardio exercise and a weightlifting exercise. So here I will specify that my type of the 'cardio' field is going to be my 'CardioExercise' from above. Remember, the name we specified is this one right here. So there is actually no name called 'exercise' by itself. There's only 'CardioExercise' and 'WeightliftingExercise'. And similarly I will have a 'weightlifting' field set to be the 'WeightliftingExercise' from above. And then of course we want the Debug trait as well. All right. Define a 'new' constructor function on the 'GymWorkout' struct. So 'impl GymWorkout' It's called 'new'. And what this should do is it should invoke all three of the 'ask_about_program' functions. We have one per module, then return an instance of this 'GymWorkout' struct, and we can pick arbitrary values for all of the required struct fields. And there's going to be quite a few that we need. So first things first, in order to invoke any of our 'ask_about_program' helper functions, they're all private by default. So the first thing we need to do is make them all public. So here in my 'diet' module, same file. But this is still private and so is the const. So here I'll add a 'pub' before 'ask_about_program'. That allows me here to go into 'diet' and invoke the 'ask_about_program' function. And similarly in my 'cardio' module in 'mod.rs', I'm going to make this public. And in 'weightlifting', I'm going to make this public as well with 'pub'. And this time around I could of course import them and give them a different nickname. But I kind of like this syntax where we specify the specific module they're coming from. And since I'm going into that specific module to access a name, I will provide the double colons, the scope resolution operator. So I will go into 'cardio'. And I'm going to have access to whatever I declared to be public. And now I have my 'ask_about_program' function. And same thing with my 'weightlifting' module. I'll go into there and invoke the same function. Notice these functions have the same name, but the compiler is not confused because each one has a qualifier. That is the secret. Each one has a different module. Okay. So now we're doing that. And then we return an instance of the 'GymWorkout' struct. So I can specify that I will return 'self' which is going to be a 'GymWorkout' struct. And here is 'self'. And I will need to provide 'cardio' and 'weightlifting' fields, each of them set to a respective exercise. Now if I go back into these files, you'll notice I have convenient helper functions called 'new' to create them, but that function is going to be private. And also the fields are going to be private. So there's no way for me to actually instantiate either one of these structs. So I'm going to decide to make my 'new' constructor functions public. So I'll add 'pub' right here and I'll add 'pub' right here. So now I have access to each respective struct and I have access to the 'new' constructor function. So here let's begin with 'cardio'. So for 'cardio', I will take my 'CardioExercise' struct, call the 'new' constructor function and then provide the required parameters here. So for 'day', we can provide arbitrary values for 'day'. This can be something like Thursday. Then we're going to need a 'CardioTool'. Luckily we have that name, that enum, available right here. So let's pick a variant on the 'CardioTool' enum. Let's do something like the 'Treadmill'. And finally we're going to have the number of minutes. Let's say we do 30 minutes on the treadmill. And then we'll provide a comma at the end because that is just our first field. And then we also need our 'weightlifting; field. And that is going to be set to a 'WeightliftingExercise'. And that struct also has a 'new' constructor function. And I can call that. And we're going to need to provide a name for this 'WeightliftingExercise' So let's do a string of "Bench Press". And then I know it's getting confusing here, but we need one more thing which is the number of reps. Let's do 8 reps of bench press. All right. So that should do it. Now notice the interesting part here is because we're relying on the 'new' constructor function and those are the ones instantiating the 'CardioExercise' and 'WeightliftingExercise', we did not need to worry about making these structs' fields public. Now if we wanted to manually instantiate 'Exercise' outside of the modules and provide these fields manually in line in a place like the library crate root, then we would need to add 'pub' here. But we have sort of designated the 'new' constructor function as the only way in which we can instantiate these respective structs. And kind of allow this to be the primary entry point. So this we're saying to future readers: "Hey, if you want a 'Exercise' struct from the 'cardio' module, please use the 'new' constructor function like you're used to or you're supposed to." And so we can get really complex here where we can have even pre-configured fields, fields with default starting values. And we can obviously get pretty complex here as far as exposing certain elements that we want, such as the functions that we want to be publicly available while hiding other details, implementation details that may be highly complex or that we don't want outside users to tinker with. We get to decide what we want to expose as the creators of our library, as what's called the public API of the library, the functionalities that other users are going to use. You don't want to have, you know, a million structs and methods and functions exposed because it can be overwhelming. So sometimes you just want to limit the number of entry points into your program, just to reduce the total amount of complexity. All right. So back in our library crate root, we have our 'new' function. So we're returning an instance. Pick arbitrary values. And finally in the binary crate root, we can invoke this 'new' constructor function and print out the 'GymWorkout' struct in Debug format. Now keep in mind this library crate root, everything in here, is going to be private by default as well. So we're not going to be simply able to reach into the 'GymWorkout' and call the 'new' function. In fact, we're not even going to be able to access the 'GymWorkout' struct. But let me show you that in action. So let's transition back into main. So if I want to write something like this, I want to reach into my library crate root of 'lib.rs'. Remember, the name that that crate root is going to have is going to be whatever is the project name in 'Cargo.toml', which is "fitness". So if I want to access something in 'lib.rs', the prefix will be 'fitness'. So here I can say something like 'use' and I want to reach into 'fitness'. But notice Intellisense is not popping up with anything and that is because nothing is public. So if I want to bring in my 'GymWorkout' struct, I need to add the 'pub' keyword here. And now you'll notice I am able to access 'GymWorkout', and I can try to create an instance of it in main. And we're going to have one more problem. But here I'm going to say 'let workout' and I can reach into 'GymWorkout't and try to access its 'new' constructor function, but we're not finding it. And the reason is because it is private. So the same rules that apply to our modules that we needed to make public with the 'pub' keyword, the same problem is going to happen here, where we've now made the 'GymWorkout' struct public, but not the 'new' constructor function. So a file outside of our library crate can have access to 'GymWorkout', although there's nothing you can do with it, right? Because it can't instantiate it. Because there's no public fields and it can't access the 'new' constructor function. So in order to enable that, we need to make sure that the 'new' constructor function is public. There we go. So now we can go ahead and call the 'new' constructor function. We're going to have a 'GymWorkout'. There are no compiler errors. And finally print out the 'GymWorkout' struct in Debug format. So right here I'll provide my Debug format. In fact, let's make it a little bit more elegant by making it pretty printed. And there we have it. So we're going to run our code in just a moment. But just to confirm what's going to happen when we call the 'new' constructor function, we should invoke all of these helper functions, which are going to tell us things like "The nutritionist is blank", "The cardio trainer is Carl", "The weightlifting trainer is blank". So we're going to get information about the program. Then we're going to get our actual 'GymWorkout' struct, which is going to have nested structs representing our cardio workout and our weightlifting program for the day, and we're going to print that all out in Debug format. So we expect to see something like this. So I'm going to run it. And there we go. The fact that the program compiled means that there's no access issues with private fields or private structs or private names. Everything worked as expected. So here are the println!'s from our three respective 'ask_about_program' functions. Here is our top level 'GymWorkout' struct that we declared within our library crate root, 'lib.rs'. Here are the 'cardio' and 'weightlifting' fields. They're each set to an 'Exercise' struct. But remember this 'Exercise' struct and this 'Exercise' struct are different. They were separately defined structs that happened to have the same name of 'Exercise' in the 'weightlifting' module and the 'cardio' module, but from the Debug trait representation's perspective, remember it just prints the struct's name. It doesn't understand the concept of module as far as interpolating that in the in the Debug representation. So from its perspective, it's just some 'Exercise' struct. We can clearly tell they're different because they have totally different fields. But they're both two different 'Exercise' structs. So here is our 'CardioExercise' struct with its respective fields. And same thing with 'weightlifting'. And there you have it. All right. So hopefully you had fun. This was a fun thing for me as well, to try to tackle a real world domain and try to again break up these real world constructs into their respective modules. Once again, there's no right answer as far as how many modules you need, or how much is too much content to pack into one. It's like asking, how do you organize the real world, right? How do you structure together and categorize related concepts? There's no right answer. It's whatever adds clarity and context and feels reasonable for how you want to model some real world business domain in code, and split up and categorize the entities that belong to that domain. So hopefully that makes sense. Hopefully you had fun. And with that, I will see you in the next lesson.
