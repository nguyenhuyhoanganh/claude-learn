# Bonus - Walking through Sample Module Structure

In this optional bonus lesson, I wanted to show you an example of a real world crate to demonstrate that the project structure principles that it follows are the exact same ones as the ones that we've been talking about throughout the section. So now you can see how we can actually navigate a crate to understand how it is storing its various constructs. So here I am on crates.io This is the online repository or, in other words, storage place where we can find user-published library crates. This is code that other Rust developers have written that we are welcome to pull in and use in our own Rust projects, thus sparing us the need to write it from scratch. You can see here there's over 161,000 crates as of the time of this recording. So here we have this nice search bar, and I'm going to search for 'http'. And that is short for the Hypertext Transfer Protocol. What that is, is a standard or a protocol for how we make requests and transmit data on the internet. So when you are navigating from page to page in your browser, such as landing on the page where you are watching this video, your browser behind the scenes is making HTTP requests to a specific server or the specific website that you are on that is giving you that content, that is responding to those HTTP requests. Now, if you're unfamiliar with web development and how the browser works, that's totally fine. The point is, is that this represents a specific real world domain, and there is a helpful HTTP library crate that exists to share code related to this standard. This is a normalized standard, so there's no reason for every Rust team in the world to create a duplicate of this code and spend developer time and money creating it. This is a perfect example of something that we can extract to a library crate and some wonderful developer in the community, or probably many of them have done so for us, so that we are welcome to reuse their code and not have to start from an empty canvas. So what I can do here is click into this crate. You can see right here that there was over, I think 200 million downloads, which is pretty crazy. But if I scroll down here, you can see here is the code that we can add to our 'Cargo.toml' file to pull it into our project, just like we did with the 'fake' crate. And if I scroll down further, we see this repository link. And that will take us to GitHub where we can actually see the source code. This is the actual Rust code that makes up this package, and you'll notice it follows the exact same principles as what we talked about. So here's the 'Cargo.toml' file. And here is the name of our package. In this case, it's going to be 'http'. If I return back to the top level, here is our 'src' directory. Now notice here there's not going to be any 'main.rs' files because this is not a binary crate. It's just a library crate. So what are we looking for? We're looking for a 'lib.rs' file. And lo and behold, here it is. If we click into here, this is our entry point for this library crate. We're going to have a whole bunch of documentation comments. But if I keep going further down we're going to start seeing some familiar constructs. For example, right here we begin with 'mod convert'. That declares a module called 'convert'. So for example, right here in the exact same 'src', directory, you're going to find a 'convert.rs' file. That represents the contents of the 'convert' module. Notice as well something like this right here: 'header'. Also a module right. But this time around there is no 'header.rs' file. So what does that tell us? That means it must be stored in a directory. And there it is. Here is a 'header' directory. And what is that going to contain? It's going to contain a 'mod.rs' file. There it is. And that represents the content of the 'header' module. If I click into this 'header' submodule and I scroll down, we're going to continue seeing the exact same constructs. So for example, here are 3 modules: 'map', 'name', and 'value'. And they correspond to these Rust files right here. These are the contents of these three respective modules. If I return back to 'lib.rs', you're also going to start to see some other familiar ideas. For example, notice here we're using this 'pub use' syntax. So what that's doing is it's starting with our 'crate' prefix to start from the top level of the crate root, in this case, 'lib.rs'. And it's reaching into these respective modules and pulling in these names from within. And then it's also using 'pub use' so they are available from the top level of the library crate root. That means that if somebody wants to reach into the 'http' crate and pull in 'Error' or 'Result', they can do so without needing to specifically provide the full path to go to the 'error' submodule, or the 'extensions' submodule, or the 'header' submodule, etc.. So if I go into another file, let's say 'value.rs' right here, you're going to start to see some other ideas here. For example, the code is relying on the Rust standard library to pull in various constructs from those available resources. Here you're going to see something called 'bytes'. Now 'bytes' is not part of the standard library. But can you guess what that is? It's just another crate. It's a external dependency that this library crate of 'http' is pulling in. And so if we have a name of 'bytes' here, we can expect to find that dependency listed in the 'Cargo.toml' file. So if I go look at that file right here, we're going to see a whole bunch of additional keys, things like the license and the authors and the website and documentation and all that. But if I scroll down, here is a dependency section and there is the 'bytes' library crate that 'http' is dependent on. So it's this entire sequence of hierarchy of dependencies. And their code is relying on other people's code and relying on the standard library and relying on all of these project structure ideas, just like we talked about. And then once they're done developing this code, they publish it on Crates.io, and we are able to pull it into our own project in the exact same manner. So even if some of the code in these Rust files appears unfamiliar, it may just be more advanced concepts, hopefully you can see that the basic syntax for module declaration, for importing, for declaring dependencies, for pulling in requirements from the standard library. It's the exact same syntax in this example library crate and any other library crate as we've seen throughout our own 'warehouse' library crate and binary crate as well throughout this section. So whenever you have a moment, feel free to dive into an open source Rust crate, because it offers you a good opportunity to see how other people think about structuring their Rust code. How much content is proper for a module? What do you store within a module? How do you organize and break down real world concepts? Do you do it by feature or domain or by type, such as only structs or only enums? There's many different variations. There's 160,000 crates to explore, so feel free to dive into some of those sample ones and see how other people have structured their their code. It's always a great way to learn, and that is reading how other people have gone and structured their own Rust projects. So hopefully this was a little bit helpful. A little peek behind the curtain and with that, I will see you in the next lesson.
