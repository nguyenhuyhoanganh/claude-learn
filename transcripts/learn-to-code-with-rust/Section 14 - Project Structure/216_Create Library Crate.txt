# Create Library Crate

So far in this section, we've been working with a binary crate. A binary crate is one that is meant to be compiled to a final executable to a final program. So we have our package, which is our top-level folder right here. And that represents our Rust project. A package is formally a directory that has a 'Cargo.toml' file. And then we have our binary crate which is stored, or at least the foundation of which is stored, in 'main.rs'. So we can also create what's called a library crate simply by adding a file called 'lib.rs' within the 'src' directory. So in 'src', I'll right-click and create a new file and it must be called 'lib.rs'. "lib" is short for library. So as a quick reminder from the lessons earlier on in this course, a package can contain a binary crate. It can contain a library crate. It can contain both a binary crate and a library crate, but it cannot contain neither. So in other words, at a minimum, a package has to have at least one crate within it. A library crate represents code that we want to export to be used by other libraries, but even the binary crate within our project can still rely on a library crate. So you can have a project that has both a final executable and also a collection of library code that that final executable relies on, and then that library code can also be exported and used by other users. So we can, for example, upload our project to 'crates.io', which is the online repository for Rust projects, and then other developers can pull in our project and use our library crate and all of the code within it. That's exactly what we saw with the 'fake' crate a few lessons ago. So now Rust sees a 'main.rs' file, which is its indicator of a binary crate, and it sees a 'lib.rs' file, which is its indicator of a library crate. So this project, this package, this folder technically has two crates, and that's totally fine. So here's what we're going to do with 'lib.rs'. 'lib.rs' is going to be responsible for being the entry point for this project. So it will declare the modules. It will declare our 'inventory' module and our 'orders' module. You can think of this as a separate crate root. The 'lib.rs' file is the crate root for the 'warehouse' library crate, right? While the 'main.rs' file is the crate root similarly for the 'warehouse' binary crate. So what I'm going to do is just drag this over to the side, and I'm just going to cut and paste these two lines over here and these lines. And then what I'm going to try to do is now treat my 'lib.rs' file as a separate library crate. So in other words, I'm going to comment out these lines right here and try to get the exact same functionality. And we're going to learn a few more things about library crates in the process, so we can treat the 'lib.rs' file in our code base as the equivalent of an external library crate called 'warehouse' that we are pulling in. The name of this library crate is 'warehouse', which is coming from the name field within the 'Cargo.toml' file. It's the same crate name for our binary crate and our library crate, and the way that Rust distinguishes between the two is because they're found in different files. 'main.rs' versus 'lib.rs'. Now, how do we reference this content from within our 'main.rs' file? We use the name of the library which is 'warehouse'. So here, in line and typically the convention is to write it after any external crates that you pulled in from crates.io. So this is not technically required, but I'm going to write our import code below 'fake'. I'm once again going to use the 'use' keyword and to refer to the content in our library crate, which is coming from 'lib.rs' on the right hand side. wW use the name 'warehouse', that name specified by 'Cargo.toml'. And then I'm going to provide two colons and we're going to find something interesting. Take a look. It doesn't seem like there are any imports available. Why is that the case? Well, Rust is able to locate this file, but as it turns out, this is a crate root. This is a root module. Nothing in here is actually public. So Rust is able to locate this. But even though we declared a module called 'inventory', it doesn't mean that it has been exposed publicly. This just tells Rust that our library crate of 'warehouse' is going to have an 'inventory' module, so that Rust can keep looking further down in the folder and file structure, but that doesn't mean that anything can reach into the root module or the crate root of our library crate and simply use 'inventory'. So, as you might have guessed, we need to make this module public with the 'pub' keyword, and I'll do the same thing here. So now back here, you'll notice I have the IntelliSense pop up. And now I can access those modules because they have been publicly exposed from our 'warehouse' library crate. And then I can use whatever is within 'inventory', assuming that it is public. So here are all of our familiar constants. Now, how I choose to export various names from my library is up to me. So for example, just like we're pulling in from 'warehouse' here, we can decide that for anybody who's going to be downloading our library crate, we want them to be navigating into the nested 'inventory' submodule and pulling in something like item. That's totally fine if we want to do it that way. But at the top level of our library crate, we can also define what is exportable or available from the top-level library crate root. So for example, if I add 'pub' here and 'pub' here, now all of these names will be directly accessible from anything reaching into 'warehouse', which is effectively the library crate root, which is effectively this file. So here I can now go back to this. And now we have access to all of the names that have been defined on the right hand side. The way I think is it's easiest to think about this is think of 'warehouse' now as no different than 'fake'. It is a crate that we are pulling in, and that crate gets to determine what it exports and what modules it declares and what it makes public. The only difference is now that 'warehouse' is coming from within the same code base, because we have a 'lib.rs' file, and the way that Rust knows that that internal crate should be called 'warehouse' is because that is the name specified by 'Cargo.toml'. So if you have a 'Cargo.toml' with a name and you have a 'lib.rs' file, Rust will make the association and realize that this is the content that you mean. And then if you want to publish your crate to 'crates.io', other developers can use this content but within their own program, within their own separate 'main.rs' file. They can do different things. They can build different programs utilizing different logic, but relying on the shared pieces of functionality that you have made available from your library crate. So here, once again, I can pull in some of our familiar names like 'Item' and 'ProductCategory' and 'FLOOR_SPACE' and 'INVENTORY_MANAGER' and 'ORDERS_MANAGER' And everything should continue to work as expected. So now I can remove these module declarations. They are taken care of by our library crate. I can take care of these things and remove them. And now everything should basically be functioning in the exact same manner. All right. We're going to get the exact same output. But now we have moved in the direction of being able to separate kind of our program's logic versus the reusable pieces, the pieces that we can share with other developers, should we choose to upload this library crate to 'crates.io'. So lots of confusing elements in this lesson, but hopefully the big picture makes sense. And with that, I will see you in the next lesson.
