# Using pub use to Export Names from Submodules

In the previous lessons, we utilized the 'use' keyword to pull in names from modules directly into the crate root, into 'main.rs', but we can actually utilize that syntax inside any module. We can use the 'use' keyword to pull in names from any submodule into the current file scope. For example, here I am in the 'inventory' module in `inventory.rs` and this module declares the existence of the 'products' submodule, which holds the 'Item' struct and the 'ProductCategory' enum. For organizational purposes and simplicity, and if I want to use those items or names in here, I can directly bring them into this namespace with a shortcut. Same exact syntax. So here I can say 'use products'. Then the scope resolution operator. And let's say I want to pull in 'Item' and 'ProductCategory' and now I am welcome to use them within this file without having to provide the full path. For example, here I can say, "Hey manager, how's your coffee?" "What do you think of " And provide my Debug formatting and say here I'll create an instance of my 'ProductCategory' enum set to the 'Ladder' variant. This would all be possible regularly. It's just without the 'use' syntax above. I would have to write my code like this. Now it's simplified because I can directly access 'ProductCategory' like so. And we've seen all this before in previous lessons. Here is the new content. What we can also do is we can add a 'pub' keyword in front of 'use'. So notice the syntax here is 'pub use'. What this does is it exports the 'Item' and 'ProductCategory' names directly from the 'inventory' module. So now we'll be able to access 'Item' and 'ProductCategory' by accessing the 'inventory' module from a top level file. And that top-level file will no longer have to reach further down into 'products' because we'll have 'Item' and 'ProductCategory' available within the 'inventory' module. Now, a top-level file can still reach down into 'products' if it wants to, but this offers an even more simplified way to get access to those names. So this is strictly a matter of structure. It's up to us how we want to organize our code in this situation. Perhaps we just want the crate root like 'main.rs' to not have to worry about the existence of the nested 'products' submodule. It's pretty nice if 'main.rs' can just reach into 'inventory' and pull out 'FLOOR_SPACE' and 'talk_to_manager' and also something like 'Item' and 'ProductCategory' without having to know the nuance that these are actually defined within the nested 'products' submodule. Our files and folder structure do not change. It's only a matter of kind of re-exporting from a different location from the parent 'inventory' module, simplifying the syntax from whatever is pulling in these names. So now in 'main.rs', here is where we are reaching into not just the 'inventory' module, but the 'products' submodule to get access to these names. Now I can simplify this code because of 'pub use' to simply be this. 'Item' and 'ProductCategory' are available at the top level of the 'inventory' module. This code would not be possible without the 'pub use' code that we just wrote. And now an additional advantage here is we're now importing all of these names from the same 'inventory' module, so we can even consolidate this all onto one line. So I'm just going to cut and paste this up above into here. Delete this line. When rust-analyzer formats this. It might reorganize the order of these things. It might not. But there we have it. So there are no compiler issues and everything runs exactly as expected. So the beautiful part here is we can still use submodules to further organize our code to break things down. But then from our top-level module, say 'inventory', we can decide exactly what it wants to expose to anything higher up to simplify this import syntax. So now 'main.rs', the crate root doesn't really care about where these are coming from. There could be five different submodules within 'inventory', but we can export all of their content from the top level of the 'inventory' module with 'pub use'. And then external code doesn't have to worry about the nuances of those respective module structures and directory structures. And so on. All right. So hopefully the benefit here makes sense. And with that I will see you in the next lesson.
