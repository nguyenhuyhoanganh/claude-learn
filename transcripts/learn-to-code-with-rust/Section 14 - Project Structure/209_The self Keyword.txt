# The self Keyword

It is possible to use the 'use' keyword for a module, because a module is just another name within our program. For example, an alternative way that I can write this code and this code is instead of making 'ProductCategory' and 'Item' available directly at the top, I can simply make the 'products' submodule available directly at the top. So for example, instead of this code, I can write 'use inventory' and then specify 'products'. So whatever is the last name that is mentioned here is what is now available. In this case, the last name mentioned is 'products' which is a submodule. So now here I can provide 'products::' and 'products::Item'. It's still a little bit simplified because now I no longer need to provide the 'inventory' module prefix, only 'products'. So different advantages and different developers have different preferences. Now there's another interesting syntax available in Rust and that is when we want to provide both the module and an item or name directly within that module, we can represent that in a single line with the 'use' statement. So for example, say we want to bring in both the 'products' module and the 'ProductCategory' enum directly into this 'main.rs' file. So of course, our first option is to simply have two lines like this. And I'll provide 'products' as well as 'ProductCategory'. So now for 'ProductCategory', I can remove this prefix. And for 'Item', we do not have 'Item' as a top level name, but we do have 'products' so we can write it like this. But now we have two different lines that are technically representing the same import in the sense that we are bringing in names from the same location. So there is a simplification that we can do to consolidate these two lines into one. And it is using the 'self' keyword and it looks like this. I'm going to remove this line. And here I'll provide my curly braces for multiple names. And then I'll provide a special keyword here which is 'self'. So the 'self' keyword will refer to the aforementioned module, which in this case is 'products'. 'self' refers to whatever is the last module before the scope resolution operator. So this accomplishes the same result. It brings the 'products' module into the scope of 'main.rs'. So we can once again continue to use 'products' like we're doing on line #20 and 'ProductCategory' which has been brought in from the 'products' module. We previously saw this 'self' keyword within our method definitions. And it's a similar concept here. The 'self' keyword refers to whatever construct the keyword is nested within. So here, 'self' refers to the module itself before the scope resolution operator which is 'products'. All right. And that should do it. This code should compile just fine. So we are mixing and matching the module availability with the 'ProductCategory' name availability. And the code continues to work the exact same way. That's all there is to cover in this lesson. So I will see you in the next one.
