# Submodules

In the previous lessons, we continued making progress in breaking down our code so that it is stored across multiple files and folders, which hopefully makes it easier to reason about. Right now we have our 'inventory.rs' file, which holds the contents of our 'inventory' module, which is referenced in our 'main.rs' file. Now on our computer, in order to keep our files organized, we create folders and then we create folders within folders. In Rust, it's the exact same concept. We can create modules within other modules. We call those submodules. The word "sub" means underneath just like the word submarine. So a submodule is simply a module that lives within another, underneath another. So our 'inventory' module has a 'ProductCategory' enum and an 'Item' struct. Let's move these two names into a new module called 'products', and the 'products' module will live within the 'inventory' module. The same module declaration rules apply. But now we're going to start from this 'inventory.rs' file. So our first option is the inline solution. For example, I can declare my module right here: 'mod' And I'll call my new module 'products'. And I'm just going to cut and paste this content into this pair of curly braces. Let's move 'talk_to_manager' up just so all of the top level 'inventory' module names are closer together. And there we have it. So now we have an 'inventory' module that within itself contains a 'products' submodule declared inline. The next possible option is to declare 'mod products' without curly braces again within this file. So as an alternative we can just write 'mod products' like this. Let me temporarily comment this out here. The same rules apply because 'mod products' is declared within the 'inventory' module. Rust understands that 'products' is meant to be a nested submodule. How do you think Rust will try to find the contents of this 'products' module if there are no curly braces? The first option is to assume that 'products' exist as a file, and with the file option, that file will be called 'products.rs'. However, there is an additional caveat. That 'products.rs' file cannot live within the 'src' directory, which is the same level as 'inventory.rs'. If we declare that a 'products' submodule lives within 'inventory', then Rust will require a nested 'inventory' folder to represent the 'inventory' module as a directory container for the 'products' submodule. So if we go with this, we will still have an 'inventory.rs' file. But we're also going to have an 'inventory' folder within 'src' that is going to contain the 'products.rs' file that will contain the contents of the 'products' submodule. So the way that I like to remember it is this: Each submodule necessitates an additional level of depth which Rust will represent with another nested folder. So if we declare a submodule, which is what we have here, because we have a module declaration within another module, within the 'inventory' module, Rust will necessitate or require a nested folder for this module. "inventory". And 'inventory' the folder will hold a file called 'product.rs'. Right, now the third technical option is the folder based approach. In that scenario, we would also have an 'inventory' folder, but within the 'inventory' folder to represent the 'products' submodule, we would have a 'products' folder containing a nested 'mod.rs' file. So I'm just going to write this out. Here are three options. Inline, declare module 'products' here or create 'inventory/products.rs' or create an 'inventory' folder, create a 'products' folder and call the module file 'mod.rs'. These are our three options for declaring our 'products' submodule and having Rust locate its contents. So let's go with this solution right here. We're going to create a 'products.rs' file within a new 'inventory' folder. All right. So we're going to delete this. Let's create in 'src/' at the same level as our 'inventory' module, we'll create an 'inventory' folder. This folder exists simply as a container, so Rust understands there's one more level of depth. Rust will be within 'inventory.rs'. Rust will be within the 'inventory' module and understand that 'products' is nested within it, and thus it's going to look for 'products.rs' not at the same level as 'inventory', but within nested within the 'inventory' folder. So here I can create a file called 'products.rs'. This name must match this module name here. All right. So now we can go ahead and cut and paste this content out and put it within 'products.rs'. Once again, I'm going to uncomment. Because Rust understands that this is the 'products' module. Submodule. But same idea. Simply by the file name. We no longer need this additional qualifier, so we can remove line #1 and remove the closing curly brace. Save that. There we have it. And then in 'inventory.rs', we keep this code. We declare our 'products' module. You can think of this 'products' module as its own named item declared within the 'inventory' module. This now is symbolically equivalent to a name like 'talk_to_manager', to a name like 'MANAGER' or 'FLOOR_SPACE'. So now, let's come back to the code in 'main.rs', which is currently failing to compile. We can no longer access the 'ProductCategory' enum because it does not live at the top level of the 'inventory' module. It now lives within the nested 'products' submodule. 'products' is a nested module within 'inventory'. 'products' is its own namespace within the 'inventory' namespace. Okay, so we cannot do this. Let's try providing the additional reference here. So in 'inventory', within that module, we have our 'products' submodule. So I'm going to write it out. And then another scope resolution operator because we want to go into the 'products' submodule. And in there, in the 'products.rs' file representing that submodule, we have our desired enum. Same rules for 'Item'. It's no longer available at the top level of the 'inventory' module. So from the top level 'inventory' module, I'll reach into the 'product' submodule and then try to get 'Item' within 'products'. Unfortunately, as much as this is a step in the right direction, the code will still not compile. We see a new error message: "module 'products' is private". Remember, anything declared within a module is private by default. Well, our new 'products' submodule is a name within 'inventory'. Therefore, it is private by default. It is privately tucked away within 'inventory' so that anything outside of 'inventory' cannot access it. This 'products' submodule is its own named construct, just like any other item here. All right. So what that means is that code in the 'inventory' module here is welcome to use 'products'. But 'inventory' does not expose the 'products' submodule outside of itself. So once again, as you might guess, this nested 'products' submodule needs to be made public with the 'pub' keyword to be able to use higher up in the crate root in 'main.rs' outside of the 'inventory' module. So now from the top level, at the crate root, 'main.rs' can find the 'inventory' module. We tell it of the existence of the 'inventory' module using 'mod inventory' on #1. Rust finds the corresponding 'inventory.rs' file. Then, within the 'inventory' module we ask to access the 'products' submodule. Rust understands that that module exists and that it is public so that it is available to use. So then Rust says I can reach further down into 'products'. What am I looking for next? Next, Rust looks for the 'ProductCategory' enum. That is public. And similarly here within the 'products' submodule, Rust looks for the 'Item' struct and that is public. Both of those are publicly exposed, so we can use those names. And then finally one more level down. Rust looks for the struct fields and they are publicly exposed as well. Now the program compiles just fine. We get the exact same result, but we structured our code a little differently. We now started breaking things up even further so that instead of bloating 'inventory', we now created another further category of organization, of classification. Our 'inventory' consists of some information related to floor space and its own separate, you can think of like a separate discrete domain of information called 'products', with its own separate constructs, its own separate names, items, etc.. All right, hopefully that all makes sense. This has been an introduction to submodules, which are simply modules nested within other modules.
