# Project Solution

So let's walk through a sample solution to the coding challenge in the previous lesson. Here in the main function, the first thing I'm going to do is declare a season variable with the let keyword followed by the variable name. And I'm going to assign it using the equal sign to my favorite season. And that's going to be "Fall". And then we are asked to provide an explicit type annotation. If you're ever unsure, you can always assign a starter value and see what the rust-analyzer extension infers, and simply write out the exact syntax that you see appear right here. So I'll provide my colon right here after my variable name, and then &str, and we'll discuss in greater detail what this symbol means later on in the course. But for now you can think of this as the type annotation for the variable. This describes the kind of data that "Fall" is. So similarly I need to declare a point_scored variable. So once again I'll use my let keyword, then my variable name. We're going to use snake_case formatting for variable names, which means we separate every two subsequent words with underscores and we write everything in lowercase. Otherwise, it's the same principle. I'll provide an equal sign and provide a value of 28. And if I want to provide an explicit type annotation after my name, I'll do my colon and a space and then write the type of an i32. That is an integer or a whole number. All right, it's time to update the team score. That means we need to declare the points_scored variable to be mutable. In order to do that, we use the 'mut' keyword and we place it after. 'let'. The 'mut' keyword must always follow 'let' and this allows the value that is assigned to the points_scored variable to change over the course of the program's execution. So in the next line, I can take points_scored. This time around, I do not need to use let. I just provide my variable and then an equal sign, and I give it its new value of 35. And there we have it. Next up, declare a TOUCHDOWN_POINTS constant at the file level set to the value y. So here outside of main, I'll use my const keyword to declare a constant. That is an unchanging value that is known at compile time. For constants, we use all capital letters, and we separate our words with underscores as well. And for constants as well, we need to declare the explicit type so we cannot get away with a simple value assignment. We do need an initial value, but we also need to explicitly write out the type with the exact same syntax. That is the colon, then the type. This is also an integer, a whole number. So I'll provide an i32 and a value of 6. There we have it. All right, moving on. Let's declare an event_time variable set to a string of "06:00". This time around, I will skip the explicit type declaration and just write my string. And then we want to use variable shadowing to redeclare the same event_time variable, but set to a different value. This time an integer of 6. So for variable shadowing we use the let keyword again and use the exact same variable name. And you can think of this as overwriting or overshadowing the previous declaration. So after line #9 -- say we assign this to 6. After line #9, we move forward with event_time being an integer. This section right here between the initial declaration and the variable shadowing in here. We're still working with this event_time declaration. So here, event_time will be a string. And the most common use case for variable shadowing is when you are converting a value to another data type, but the value fundamentally represents the same value or the same idea. Like in this case, maybe we are trying to represent the start time of something in hours. Maybe it arrives as a string and we want to convert it to the equivalent numeric integer of 6. That's where you're going to see variable shadowing used in Rust. It's for those data transformations for the same piece of data. All right. So we have these declarations. The next step is to use interpolation to print out all of the declared variables and constants in a println! call. And we can practice the different syntax options for doing so. So here I'll add a println! My first option is to directly embed my dynamic values in my curly braces in my interpolations, so I can write something like. "My favorite season is" and interpolate the dynamic value of whatever the season variable represents. I'll keep going. "The team scored points scored". Once again, I'm writing anything dynamic in those curly braces. Those are critical. That's how rust knows to embed whatever point_scored is, in this case, 35, right here. And then I'll keep going. "The event started at event time." and then "A touchdown is worth touchdown points points". That is one advantage of constants. And that is that we are able to refer to them in any of the functions within the file. So here we are grabbing access to this value right here. Provide my semicolon. And let's run the file. We're going to see the output right here. And here are all of my values dynamically injected from their respective variables and constants. That is my first option. The second option is to simply declare all of my dynamic names after the initial string that we provide to the println! macro. So I'm just going to cut and paste this at the end and provide a comma. And then season. And Rust is going to inject this dynamic value into the first curly brace that it sees. I'm going to do the exact same thing with points_scored. And at the end, as I provide these sequential values, I'm going to provide a comma in between each. That is a technical requirement, and then a space just for readability between all of these dynamic values. So if I'm going to have four curly braces or four spots for dynamic content, I need to provide these four values sequentially at the end. So comma and a space. event_time. I'll do the exact same thing for TOUCHDOWN_POINTS right here. Save that. Run the file and we should get the exact same result. There we have it. And finally, each value after the string is assigned a numeric position in line starting from zero. So season is 0, points_scored is 1, event_time is 2 and TOUCHDOWN_POINTS is 3. And you can use those values inside the curly braces to refer to them throughout the string. And this is helpful if you want to reuse the same dynamic value multiple times. In this case we don't, but we can still review the syntax. So here, to accomplish the same result, I'm going to provide the value of season right here. 0 corresponds to the first value right here after the string. And that is season. Right here, I'll provide 1. Right here, I'll provide 2. Right here, I'll provide 3. And we're going to get the exact same output with either one of these three approaches with those dynamic injections. All right. That takes care of that. Let's see what else we have to do here. Declare a favorite_beverage variable set to a string of your favorite drink. So here -- once again, 'let' keyword -- favorite_beverage. And let's do something fun here like "Snapple Apple". It's always delicious but not very healthy. And the compiler warns us that we are not using favorite_beverage anywhere in our program. It's not a fatal error because there's nothing wrong with the program. It's just wasteful. And the compiler is correct that we're not doing anything with this declared name and the string that it represents. So typically the right solution, honestly, is just to remove it if you're not using it. But if you want to tell the compiler to not worry about this, to silence this warning, the first option is the underscore. You'll notice the orange squigglies disappear in VSCode, and the warning will also be gone whenever you actually compile and run the program. That is our first option. The second option is to use the compiler directive. We can do that on the line above. And as a reminder we're going to begin with the hashtag (#). Then our square brackets ( [] ). And because we are writing it here it's going to apply to the immediate line below. And we can apply this exact same syntax to a function. If we were to write this here above main or to the file if we were to place it at the top, but that would require an additional exclamation mark. For this scenario, I'm applying it just to the line below. And here I write the compiler directive, which is just metadata that tells the compiler how to parse the following line of code. By default, it would parse it and give us a warning, but I'm going to use the allow directive to allow the use of unused_variables on the next line. I am allowing or permitting a behavior that the compiler would otherwise find undesirable. I'm not saying this is good practice, because we are once again telling the compiler to ignore something that it is correctly warning us about that can be wasteful. But regardless, this is an example of a compiler directive that gives the compiler additional instructions. We're going to see this syntax many times throughout the course. So now when I execute this, we're not going to see any warnings from the unused favorite_beverage variable. So that's all there is to cover in this lesson. So I will see you in the next one.
