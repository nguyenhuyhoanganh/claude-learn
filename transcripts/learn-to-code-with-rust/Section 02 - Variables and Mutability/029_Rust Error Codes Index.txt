# Rust Error Codes Index

In this lesson, I want to show you how you can find out more information about a specific Rust error. Every time that you run into a compiler error, the compiler will provide a unique error code. You can see right here it says: "For more information about this error" And this is the one that we saw in the previous lesson. It offers this command. And this is a terminal command for the Rust compiler. rustc here. So I'm going to copy this code. And then in VSCode I can click this + button. And that's going to launch a terminal directly within my VSCode editor. So this is the exact same thing as Terminal on a Mac or PowerShell on windows. It's a command line directly built into VSCode. So it's here that I can paste that command that we just saw. You can see it is 'rustc' which is short for Rust compiler, a space, then the actual command is explain with two dashes (--explain), and then that value at the end is E, which is short for error, and then a unique numeric identifier. Every type of error in Rust will have a corresponding error code. This one is 0384, and it's going to give us documentation directly within the terminal for this error. You can see here: "An immutable variable was assigned". It's going to show us a erroneous code example, an example where we have the wrong thing. In this case, it is trying to reassign to an immutable variable. And then a description. And then finally: "To fix this error, add the keyword mut after the keyword let". So super helpful because you do not have to even leave the code editor. This is also completely available online in something called the Rust Error Codes index. So I'm going to drag my browser window over from the other screen. If you Google Rust error codes index, you're going to see this first link. This is a listing of every single error in Rust. And as you can see, this page gets pretty long. So once again, if I know the error that I'm searching for. In this case, we have 0384. I can find it. Click on it and this page will offer the exact same content as you saw in the Terminal but the advantage here is that it's nicely formatted so we can see the proper Rust syntax. And you can even try to run the code on the page. You can see with this example, which is the erroneous one. This information icon will tell us this example deliberately fails to compile, but even that is helpful because if we try to run the incorrect code sample, we're going to see the full compiler output so we can see the same error messages and learn to spot the errors more quickly. So obviously it's not important to go read through these error messages one by one. Rather, when you run into a problem with code, that's the perfect opportunity to take a pause to look up the specific error ecause usually these pages will provide not just a clarity and context, but also will show you a recommended way to fix the problem as well. All right. This is available directly from your Terminal and online as well through the Rust Error Codes index.
