# Asking the User for Input

Let's expand on the example from the previous lesson by asking the user for the name of the file that they would like to read, instead of hard coding it right here, we're going to collect it from the user. As a reminder, we covered this earlier on in the course. In order to collect input, we use the 'stdin' function from the input/output module from the standard library. So first things first up here I'll add another 'use' keyword. And we're going to pull in the input/output submodule which is abbreviated 'io'. And from that module I want to pull in the 'stdin' function which is all lowercase. Now when I invoke that function right here, for example, that function when invoked will return a 'Stdin' struct. Same name but with a capital 'S', and that represents a struct. That struct will have a 'read_line' method and that will collect a line of user input. And it is that 'read_line' method that needs a mutable reference to a heap allocated String. And that's where the actual text content from the user will be written to. So before I do this, the first thing I'm going to do is println! and I'll give the user a message like "Please enter the name of the file you'd like to read". Then, I'll create an empty string. So I'll call my variable 'input', and I'll create an empty String with the 'new' constructor function on my String type. I need to make sure to make it mutable in order to pass in the mutable reference to input to 'read_line' and that is what the method will write to. So here I'm going to pass in a mutable reference to my heap allocated 'input' String above. And if I assign this to a variable like, say, 'user_requested_file', we're going to see that 'read_line' is another example of a method or operation that returns a Result, just like the 'open' function on File from the previous lesson. This is an operation that can either have a success or a failure, and we should ideally react to both. So let's start out with the 'match' keyword once again. And then we're going to modify our solution to what I think is a little bit more of an elegant solution. But let's say we once again start off with a 'match'. So right here I can match on the 'read_line' like so. And we're going to need to cover two arms: 'Ok' and 'Err'. Now as a reminder with the 'Ok' variant it's not actually going to store the user's entry as the associated data. It's going to store the size in bytes of the user's entry. The actual user's entry will be contained in the 'input' variable. Here, the 'input' String that the 'read_line' method wrote to using the mutable reference. This is actually the size in bytes. How, how large was the entry in terms of total memory consumption? So I can give that associated piece of data a name like 'size'. And I can println! something like "The size of the user entry in bytes is {size}". It's not particularly helpful, which is one of the reasons I want to eventually move away from this 'Ok' variant in just a moment, but structurally speaking, it's the exact same match logic that we saw in the previous lesson. We're just reacting to this Result enum. And for the 'Err' right here, I can basically do something very similar to what I have down below. I can output the specific error message to my standard error and then exit the program in case of error. So once again here I will extract the associated piece of data on my 'Err' variant. And once again println! and something like "Something went wrong collecting user input. The error was..." Once again I'll interpolate the error in Debug format. And once again I'll call my 'exit' function within my 'process' submodule with an error code of 1 indicating that something went wrong. Okay, so this is fine. It's going to work as expected, but the 'Ok' variant here feels purposeless. It doesn't really add value, and the only reason I added it here is because it was a technical requirement of the match statement, which requires that we cover every single possible enum variant. So an alternative solution here. If you recall the 'if let' syntax, that syntax allows us to cover a specific enum variant just one, and the one that we really want to target here is the 'Err'. If we have an 'Err', we want to run this code. Otherwise we want to proceed through the remainder of our code. So here's what that's going to look like. I'm going to once again extract this method call to a variable. Let's call it 'user_requested_file' again. Then what I'm going to do is this syntax that you may recall. It's going to begin with 'if let', then on the left hand side of an equal sign, I'm going to specify a specific enum variant that I want to match on. In this case it's going to be our 'Err' variant. And this is coming from the Result enum. But the 'Err' variant inside it is also available at the top level of Rust, because it is included in the Rust prelude. So I can simply reference it like this. It's the exact same thing as that. All right, then I can provide a variable name for the associated piece of data in the 'Err' variant. Let's call it 'error' once again and then provide an equal sign and what to match that specific 'Err' variant against which will be our 'user_requested_file' Result from above. So this is called an 'if let' statement. And it accomplishes two things. The first thing is it runs this block if and only if the 'user_requested_file' variable is an 'Err' variant. So it is an if statement at the end of the day. But the reason it's also including the word 'let' is because it's going to declare an 'error' variable if you have the 'Err' variant and this 'error' variable corresponding to the associated data will be available to use within this block. So it's both running a conditional if statement and declaring a variable with the 'let' keyword, but conditionally. And that's why it's called an 'if let' statement. So what I need in here is what I formerly had in here. So I'm just going to cut and paste it. So if we have the 'Err' variant from 'user_requested_file', output the specific error message, then exit the program. That's going to allow me to remove all of this code down here. So if I have an 'Err' variant, we're going to terminate early. If I have an 'Ok' variant, I don't need the size in bytes. And I will already have the user's entry in my 'input' String. So the code can continue running as expected down here. All right. So at this point on line #15, I'm guaranteed to have the user's input in a valid form. So the next thing I want to do is of course instead of hard coding the file name, let's just pass in the user's entry right here. So we should provide a reference. So I'm going to do a reference to 'input'. That is a String type, so I will provide a ref String ( &String ). But remember, Rust will coerce that to a string slice anyway, so this should work. All right, there's one technical problem in our code, and I want to show you what's going to happen right now I'm going to run the file. It's going to ask us to enter the name of the file. In just a moment I'm going to enter 'story.txt' which I know for a fact exists. We created it in the last lesson, but you'll notice it's not going to be able to find the file. All right. The reason this is happening is because of the 'read_line' method and the way it works. When the user presses the Enter key to terminate their entry, the newline character, the line break represented by this character is captured in the output. So our String of 'input' looks like this. It contains the user's entry, followed by that new line character at the end, and that is contained within the captured String. The problem is, when we try to open a file with this name, a file with this name doesn't exist. Our file name is just the text file without the character at the end, the new line character. So all we need to do to fix this problem is to make sure we call the 'trim' method on our 'input' String. The 'trim' method removes any leading and trailing whitespace from the string. Any extra spaces, including new line characters. So here I can call 'input.trim' which will remove that extra unneeded character. And since the 'trim' method automatically returns a string slice anyway, I no longer need this reference symbol. I will already have a string slice, a string reference, to pass into my 'open' function. So there you have it and that should do it. So let's run the program again. "Please enter the name of the file you'd like to read". I'll provide 'story.txt'. We're not going to run into this 'Err' variant because we're not going to have any problem with input. Then we're going to trim the newline character. The 'open' function will be able to find this file. So it's going to run into the 'Ok' arm on line #17. Extract the file, assign it back to the 'file' variable and output its contents on line #24. There we have it. There is our 'File' struct printing from line #24. And here is our File. Now once again let's try with the failure state. Let's say I provide an invalid file like 'nonsense.txt'. Now once again we're going to be able to successfully capture user input. So we're going to bypass this. But in the case that there was some kind of error collecting user input, we would exit the program. Now we trim the content. We run into the 'Err' variant right here and we quit the program. So there we go. "Something went wrong reading the file" is coming from line #19 and there is our specific error message. We are printing it in Debug format. I believe that these error types as well do implement Display, which might give you a more human readable message. So let's try just removing our Debug formatting and seeing the difference in result. So right here I'll again provide 'nonsense.txt' or any invalid file you can see right here. This does not print that error struct in Debug format. Rather, it just prints a string, a more human readable Display version of the error. It's up to you whatever you want to include. Probably if it's for a non-Rust developer audience, you probably want something like this because it's a little bit more parsable and understandable. But once again, it's in your hands as the developer. All right. So now we are having a program here that actually handles two separate Result enums from two different operations that can result in error. All right. We're going to continue to expand on this example in the following lessons.
