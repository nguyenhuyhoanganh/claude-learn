# Project Solution

So let's walk through a sample solution to the coding challenge in the previous lesson. We were asked to define a 'write_to_file' function that asked the user two questions: the file that they would like to write content to, and then the actual content to write to the file, and then performed that action on behalf of the user. So there's lots of complexity here, but let's begin with our first assignment, which is asking the user for input and collecting it. So I'll declare my 'write_to_file' function. And let's begin with the instructions for the user. So in my first println!, I'll write this exact sentence. "What file would you like to write to?" So the user has some direction on what they're supposed to do. And then I can copy and paste this. And our second question to ask them will be: "What would you like to write to the file?". So in order to collect the user's input, we're going to need to create some mutable heap Strings. We're going to create some empty strings and then update them using the 'read_line' method that we have on our 'stdin' struct, which we're going to get from the 'io' submodule. So let's declare a variable here, I'm going to call it 'requested_file', and this will be the name of the file that the user wants to write to. And this will be an empty String. So I can call the 'new' constructor function on my String type. And we're going to have pretty much identical code down below except the second variable, the second string we're going to need to store is the actual content to write to the file. And I'll call that variable 'contnet'. All right. So the next thing we need to do is to bring in our 'io' submodule and it's 'stdin' function which will give us access to the 'stdin' struct. So at the very top I'll utilize my 'use' keyword and reach into my standard library, 'std', two colons. And I want the 'io' submodule, which is short for input/output. So now within that submodule of 'io', I can find the 'stdin' function. And as a reminder, we invoke this function and we get back a 'Stdin' struct. It is that struct that supports a 'read_line' method to collect one line of user input, so that I can reuse this Stdin struct. I'm just going to assign it to a variable right here. I'm going to call that variable 'input'. And there we have it. You can see its type is this 'Stdin' struct. All right. So now let's go ahead and collect the two entries from the user so I can take my input, my Stdin sdtruct. And as a reminder that has a 'read_line' method. And what 'read_line' accepts is a mutable reference to the String that it is going to write the contents to. So in order to provide a mutable reference, I also need to make my original owner, the String owner, mutable as well. That is the only way we can borrow a mutable reference. So before I forget, I'll add the 'mut' keyword right here and the 'mut' keyword right here. And here we're going to pass in a mutable reference. So &mut requested_file. And that's going to capture the user's input into the 'requested_file' string on the heap. And then I'm going to copy and paste that line down below. And the only thing I'm going to swap here is 'requested_file' with 'content' so we write to the second String. All right. So right now we have no violations or errors as far as anything that's going to fatally crash the program. But you can see these orange squigglies appear. And these are warnings. So as a reminder, the 'read_line' method returns a Result which means it has the capacity for error or failure. There are two variants: Err and Ok, right? Things could go wrong when we're collecting user input. So Rust is concerned that we haven't handled that possibility. We're sort of just progressing forward in the function. So the instructions ask us after we collect the user's two entries as Strings. If something fails in either collection, propagate the error upwards. The 'main' function, which is going to be the caller that invokes 'write_to_file', will handle the error later. So as a reminder, how do we propagate the error up? In other words, how do we return early if we have the Err variant of the Result enum? Well, we can do it the more technical way with something like an 'if let'. But the kind of idiomatic Rust way is to just add that question mark operator, sometimes called the try operator. So as a reminder, what this is going to do is it's going to return early with the Err variant if you have one. And if you have the Ok variant, it's going to progress downwards with that. And at that point we're going to have the user's entry already collected in 'requested_file'. We don't actually need the associated data in the Ok variant. We just need the logic to continue running. So this question mark is correct. And we're going to place a similar one right here. The reason we are seeing a violation is because that try operator, that question mark, indicates a return value of a Result, but we did not declare our exact return type right here to be a Result. So Rust thinks there's a mismatch. It thinks right here that we are trying to return a unit or an empty tuple from 'write_to_file', because there is no explicit return type. And instead we're trying to return a Result. So we need to designate that our return type here is going to be a Result. Now if I just click into 'read_line' by doing either Cmd + click on a Mac or Ctrl + click on a Windows. You can see that the return type here is a 'io::Result'. And that is exactly what we want to designate as our return type as well. Now, we talked about this in a bonus lesson earlier on in this section. But what Result is, this thing right here, is really a redeclaration of the core Result enum that we know and love and have introduced throughout the course. All that it does is it leaves room open for the generic T to still be here to represent the type of the associated data in the Ok variant of the Result, but it hardcodes the specific type for the Err generic in the Result, because every single error that is packaged up in the Err variant is in the subcategory of input/output or io-related errors. So instead of returning the regular generic Result and I hesitate to use the word generic because I'm not using the concept of Rust generics, I mean the more generalized Result enum that this is a more specialized version of. I don't want to return the top level Result. I want to return this specific io::Result, and then I don't even have to worry about providing a concrete type for the error generic. I just need to provide the type for the Ok variant's associated data, and it's generic. So what I can do here is I have access to my 'io' input/output submodule. And if I reach into there right here, I will find that exact Result enum that I want to return. And here, because it already takes care of hardcoding the type of data for the Err variant, I just need to provide one generic to represent the type of the data that's going to be on the Ok variant. And this was a little bit later on, which made this exercise a little bit tricky. But it said right here, after you write to the file, return an Ok variant storing the user's requested file name. And since that requested file name was captured right here as a string. The key takeaway here is that we wanted to capture the string as the actual return data in the Ok variant, and the reason we wanted to do that is just so we wouldn't lose this 'requested_file' String after the function ended, and it was deallocated, and thus by returning it wrapped up in the Ok variant, we would have access to it later in 'main' so that we can print it out. So right here or rather right here, we can specify that this is going to be a String. That is the concrete type filling in for the generic type for the Ok variant's associated data. But now the compiler is unhappy because we're not actually honoring the expected return type. There is nothing in the body of this function that is returning the Ok variant with an associated String. So before I forget, this is not the end of our logic because we still need to write to the file. But after we're done writing to the file, assuming nothing has gone wrong, we want to return the Ok variant and I can just write it inline like so. And then within the Ok variant, we want to package up that associated data. And that is going to be the user's requested file name. And that we have in our 'requested_file' variable above. And if I save that with no semicolon, this will be the implicit return. And this satisfies the return type here. So we will either return the Ok variant, storing a String with the user's requested file name, or we're going to return the Err variant. And that's going to be handled either here or here. And it's going to live up to this promised type. All right. So we want to use the file system module's 'write' function to write the user's specified contents to their requested text file. And I provided the documentation for this function. And I'll bring it up on screen right now. But the important thing here is it's a top level function within the 'fs' or file system submodule, and it accepts the name of the file as well as the content to write to it. So that's exactly what we have. That's exactly what this function expects. So at the very top here, just for simplicity I'm going to bring in my 'fs' submodule. So once again reach into my standard library, 'std', two colons and bring in 'fs', which is my file system submodule, and within that submodule, which I can now reference with 'fs', I have my 'write' function. Now here's something that's going to be interesting. And that is that if we try to pass in the two things we need, which is the file name as well as the content, we're actually going to accidentally transfer ownership. So let's take a look at that in action. I'm going to pass in 'requested_file'. That's a String, and then the 'content'. All right. And then put a semicolon. So the problem here is because we are passing in the String, well then this function's parameter is going to take ownership over it. And then we're going to lose it. And thus we cannot actually return 'requested_file' right here because it is no longer the owner. So the way that we can solve this problem is by passing in a reference instead of the String itself. And here's the beautiful part. Here's the thing that's going to allow us to actually solve two problems at once. And that is, as you may recall, when the user presses the enter key and they submit their text input, Rust will unfortunately gather that Enter, that return key press as a new line character at the end of the string. So what we want to do is remove that additional new line as well as any whitespace, and we can accomplish that with the 'trim' method. So here I can call 'trim' on my requested file. And I want to remove the new line that the user provided or automatically provided accidentally. And the same will apply to 'content' because that will also have a new line character. But the beautiful thing here is the 'trim' method returns a string slice. It returns a reference anyway, because it's going to give you essentially a borrow of just the chunk of the original heap String that doesn't consist of the beginning and trailing whitespace, or the beginning and trailing newline character. So this will actually give us a argument to the 'write' function that will prevent the loss of ownership and solve the new line problem, and allow us to have access to 'requested_file' as the owner of the String right here, so that we can package it up in the Ok variant and return it. The only reason we're seeing a warning here is for the exact same reason we saw it with the 'read_line' method above, and that is that this returns a Result and we're not handling the possibility of error. Luckily, this returns basically the exact same Result as we get from our 'read_line' So we can once again use the question mark operator here, which will tell Rust if you have the Err variant, in other words, if you have an erroneous operation, if something goes wrong writing to this file, take that Err variant, return early, propagate that error back up, and then the caller will be able to handle it. So there's three places in our 'write_to_file' function that things can go wrong. Right here, right here, right here. If at any point it does, we're going to terminate early, propagate the Result Err variant back up. And then when 'main' calls it, it's going to handle it. And if we reach all the way to the end here we know that we have successfully written to the file. Then we're going to return our Ok variant indicating a success and packaging the user's original requested file name so that we can print it out above. All right. So let's take a look here. If the 'write' function fails, propagate the error upwards as well. Your 'write_to_file' functions to return an 'io::Result'. After you write to the file, return an Ok variant storing the user's requested file name. Perfect. Now all that's left is in the 'main' function, use a match statement to react to both variants, Ok and Err, of the Result enum. If everything worked, output the text "Successfully wrote to file" and then populate the file's name, which we're going to have access to from this thing right here. And then if there was any failure, output the error instead, taking the error that we're going to have packaged up in the Err error variant automatically handled by the try operator here, here and here, and then exit the program with a status code of one. So in main, we can invoke 'write_to_file'. That is going to return a Result enum. So we can gracefully use the match keyword to react to both of the possible variants. The first variant is the Ok variant, and that's going to store whatever data we chose to package up, which is the 'requested_file'. So I'm just going to call this 'file_anme' And you're going to notice rust-analyzer will understand that it is a String because that is exactly what we designated right here. So here I can println! and say something like "Successfully wrote to file" and then interpolate that String right here as 'file_name' Then, comma and a line break. And I also need to handle my Err variant. And this is going to package up the specific error. And it might be totally different depending on what went wrong here, here, and here. So right here I'll provide a block because I need a little bit more space. The first thing I'm going to do to make sure I write to the standard error output is I'll use my alternative macro of eprintln! to make sure I write to the standard error. And this will be: "There was an error", and I will just interpolate the value of my 'error', and that should be able to be displayed in Display format. And then even though it's technically not necessary because the program would just end anyway, I do want to designate and specify that something went wrong, so I can gracefully leave the program by calling the 'exit' function in my 'process' submodule with an argument of one indicating an error or a failure. So right here, I will once again use my 'use' keyword to reach into my standard library and access the 'process' submodule. And once again, the only reason we do this is just so we have these names directly available to use in line without having to write 'std::' each time. So now we can reach into our 'process' submodule. In there we have our 'exit' function. Invoke that and provide an argument of one. Close that off. And there you have it. And that should do it. So let's just double check right now. If I open up my file explorer, there's no text file whatsoever right here. Let's go see if we can create one by running our program. So I'm going to run main. That's going to run 'write_to_file' and prompt us for the two user inputs. What file would you like to write to? Let's call our file 'adventure.txt'. The name is up to you. As a reminder, when the user presses Enter, we're going to capture the newline character, but not a problem. When we write to the file, we're going to trim that newline character anyway, so I'll press Enter. We've now collected that string. Now the program wants to collect the other string of the actual content. I can write something like "Woohoo, I'm a Rust developer", so execute that and the program will tell us "Successfully wrote to file adventure.txt" which is coming from the Ok variant. So that means at that point the 'write' function was called. We successfully wrote to a file. We returned from 'write_to_file' with the Ok variant. We matched on that Ok variant right here in the match statement, we extracted the file name: "Successfully wrote to adventure.txt" And the most important thing... if I open up my file explorer, lo and behold, we have an 'adventure.txt' file. If I open that up, take a look. It says "Woohoo, I'm a Rust developer" and that also does not include a line break either because we stripped it right here, because we removed it from the content as well. So there is the user's requested content written to a file name of their choosing. And there you have it. All right. So hopefully you enjoyed this exercise. It allowed us to review a lot of the concepts introduced in this section of the course, as well as previous ones. So input/output as well as the match statement, the Result enum and its specialized variations within these different submodules, collecting input, of course mutable references, and of course, most importantly, the thing that we've been really talking about, which is error handling and some of the recommended idiomatic ways that Rust offers for that, such as the question mark or try operator. Once again, the advantage is we can sort of propagate our errors upwards and handle them with some variations, even in the original caller. So another function could hypothetically reuse our 'write_to_file' logic, but choose to deal with its Ok data or its Err data in a totally different manner. So we've kind of split up those responsibilities. So this function can primarily focus on writing to a file. And then if at any point anything goes wrong it just says hey "I have this error, please take care of it". It sends it back up. All right. And that's such a common pattern that the Rust team added the question mark operator precisely for that use case. All right. So I hope you had fun. And with that I will see you in the next lesson.
