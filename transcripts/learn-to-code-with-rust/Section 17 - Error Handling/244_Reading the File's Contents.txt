# Reading the File's Contents

So at this point, we've asked the user for the file that they would like to read, and we have opened that file. So the next step is actually reading the file and displaying the contents to the user. So on our File struct which we have on line #16 as the direct extraction from the 'Ok' variant, on this File, I have a method called 'read_to_string'. Now, just like the 'read_line' method above right here, this method expects a mutable reference to a heap allocated String, and it is to that string that it is going to write the file's content. So just like above, what I'm going to do is declare a new variable. I'm going to make it a mutable variable. I'm going to call it 'file_contents' and make it an empty String. And I'm going to pass a mutable reference to this String. So. & mut file_contents to my 'read_to_string' method. And that is going to write the contents of the file to this String. And I'll assign this to a variable called 'read_operation'. There's a couple of things we're going to need to talk about as soon as I save this file. The first is by default, this 'read_to_string' method is not going to be available until we bring in a specific trait into this file's namespace. We're going to cover this topic in much greater depth in the next section of the course, but for now, you'll need to trust me. This is just an additional piece of code that we need to add to enable this 'read_to_string' method to work, to function in the first place. And so here from the 'io' input/output submodule, I'm going to add my pair of curly braces to specify multiple imports, and it is from that 'io' submodule that I want to bring in a trait called 'Read' with a capital R, and as soon as we bring that in, this should work just fine. Now you'll notice another error is that the 'read_to_string' method is going to mutate the underlying File struct. So we need to make this 'file' variable mutable as well. Keep in mind when we make this mutable, this is not going to overwrite the contents of the file. This is the actual File struct. This is the File type in Rust, and I'm sure somewhere internally it's keeping track of whether the file has been read, whether the file has been written to, etc. That's why when the 'read_to_string' method is called, it probably changes some internal state or some internal data, some internal fields that are being stored within the File struct somewhere in this implementation, and thus we need to make our original 'file' variable representing our File struct mutable. All right. So that's the second thing. Here's the third detail. Notice what the 'read_to_string' method returns. Just like 'read_line' and just like 'File::open', it returns a Result because there is the potential that something could go wrong with parsing the actual contents of the file and saving them to the 'file_content' String. So once again, I'm going to follow this pattern right here. The 'if let' to make sure we account for the 'Err' variant in this result. So right here I'm going to say 'if let' once again on the left hand side of the equal sign ( = ), we always need the concrete enum variant. And on the right hand side is the variable or value that we are comparing against. So the variant is the same. It is the 'Err' variant within the Result enum. I can provide a name for the associated data. It doesn't matter if it's the same one as above because this variable will only exist for the block anyway. So here I'm going to check whether my 'read_operation' Result has the 'Err' variant. If it does, let's go ahead and once again use the println! to print out a message to standard error. Something like "Something went wrong reading the file as a string. The error was" and I will again interpolate error here. In fact, we're using the same terminology here with reading. So maybe we should change this to opening. That's a little bit more distinct from the message below. And once again, if we have an issue reading the file to a string, we're going to exit the program with 'process::exit' and a status code of 1. So if we reach line number #32, at this point we can guarantee that we ran into the 'Ok' variant of the Result returned by 'read_to_string'. So at that point we should have the file's contents written to 'file_contents' right here. The 'Ok' variant by the way for the 'read_to_string' method, just like the 'read_line' method, stores the number of bytes that have been read from the file to the string. So it's not the actual content, it's not the actual text, it's the size in bytes, and the actual content that has been read from the file will be saved to the 'file_contents' string that we pass a mutable reference to to the 'read_string' method. So this method will accept the mutable reference so it can write the content to 'file_contents', the String. But the Ok variant doesn't store that actual content. It stores the size in bytes and we don't care about that number for now. That's why I went with the 'if let' solution instead of a 'match'. So here let's go ahead and output our 'file_content' String. And that should do it. So I'm going to run our code and let's provide our 'story.txt' file. And there is our content. We have successfully opened and parsed the user-specified file and output its content here in in our program. And there's other ways to collect user input and other ways to read a file. But this is a good basic way to start. Now this is the end of this lesson, but I do want to take a moment to focus on the general pattern that we are seeing throughout the course of these lessons. We have done three operations in the main function, each of which returned a Result enum because each of them could result in an error or a failure, and that was reading the user's input, opening the file and reading the file to a string. And in each situation, what we did is we basically printed out an error message and exited the program. You'll notice there's a lot of similarity in the code here, here and here. And because this practice is so common in Rust, Rust actually offers us a simplified way of condensing this code, simplifying it, refactoring it, but accomplishing the same result which is basically terminating early if we have an error, otherwise progressing forward in the regular function's execution. We're going to take a look at that simplified, nicer way of structuring the code in a lesson or two from now. But this is totally technically valid as well as far as following idiomatic Rust conventions. It's just that it's such a common way that there is an even better way to write code that looks like this. All right. And we'll explore it very shortly.
