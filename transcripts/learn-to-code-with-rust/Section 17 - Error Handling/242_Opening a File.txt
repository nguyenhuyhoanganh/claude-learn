# Opening a File

In this lesson, we'll learn how to open a file using Rust. This will also allow us to review and combine the concepts of the previous lessons. So in the previous lesson we created a text file called 'example.txt' I'm going to right-click here and just rename it to be 'story.txt'. And if you do not have an existing text file, be sure to create one. You can do that by clicking this New file button in VSCode. And it's just going to be a plain text file at the root of your project. So make sure your .txt file is in this top level collection of files. And what we're going to do is to try to open this file using Rust, and then eventually read its contents as well. So I'm going to click into my text file and write some sample content like "Once upon a time". Make sure to save the file. then we're going to close it. All right. Now back in 'main.rs', we're going to use a existing construct from the Rust standard library in order to open this text file. So the Rust standard library includes a file system module which has various functionalities for interacting with the computer's file system, the hard drive, in other words. So we can use Rust to perform the same kind of operations we do daily, like deleting and creating files, deleting and creating directories, making copies, renaming files, etc. Rust offers us the opportunity to programmatically perform these operations in code. So at the very top I'm going to add my 'use' keyword and our standard library is abbreviated 'std'. Then I'll provide my two colons to enter the lobby of the library. And in here I want the file system module which is abbreviated 'fs' for file system. And of course we can bring it in like this. In which case we'll have our 'fs' module name at the top. But I'm going to add an additional set of colons and pull in a specific name which is 'File'. So this is actually a struct that is going to model a file that we can either read or write to within our program. So this 'File' type includes an associated function, so a function on the type itself, and it is called 'open'. As you might guess this function opens a file and then models it as an existing File struct, as a File instance. So I'm going to invoke this function. And we're going to provide a string slice with the file's path which represents the directory where it is on the computer. Since my file is already within the same folder as my Cargo project, I can just write the file name without any kind of directory or prefix before it. So I'm simply going to write 'story.txt', making sure to include the extension of the file. So let's take this return value and assign it to a variable. I'm going to call that 'file' in lowercase and let's take a look at the return type. It is a Result enum. As a reminder, the Result enum type has two possible variants that represent either a success of an operation or a failure, and the two types of variants are 'Ok' and 'Err', both of which store an associated piece of data. So the 'Ok' variant represents a success, and if it is a success, the associated piece of data is this concrete type right here. It will be the actual File struct. And if the opening of the file was unsuccessful, for example, if the file did not exist, we're going to get the second variant, which is the 'Err' variant: 'E-r-r'. And it is also going to store an associated piece of data, which will be an 'Err' type that holds information about the specific error that occurred. Many operations in Rust that have recoverable errors will return this exact Result enum type. And this is a perfect example of a recoverable error, because it's one that our code can anticipate and react to and deal with gracefully, right? It is always a possibility that somehow we request to open a file that doesn't exist. Say, for example, we ask the user for a file name that they would like to open, and they provide an invalid file, maybe accidentally, or maybe they have a typo. In that case, the 'open' function anticipates that possibility and returns a consistent return type, a Result enum that will hold either the File or an error message about why it failed. So how do we deal with an enum like this? There's a wide variety of approaches in Rust, but one very common one is something like a 'match' statement, which will allow us to react to both possibilities. So we're going to do that in just a moment. For now, I want to show you that I can do a println! here, and I'm just going to interpolate the File with my Debug formatting. In fact, I'm going to do the pretty-print just so you can see this file does exist. We just created it. So we're going to see the 'Ok' variant. And its associated data is the entire File struct which stores a bunch of fields about the file. We can see, for example, the exact path where it is on the computer, whether we can read the file, whether we can write to the file and and more fields like that. And let me show you an alternative example. If we provide a file that does not exist, like 'nonsense.txt', the 'open' constructor function or the 'open' associated function will return a consistent Result enum type. But now the concrete variant will be the 'Err' type. The 'Err' type and its associated data is an error type that stores more information about the specific error that occurred. For example, it looks like this is an 'Os' struct which represents an error type stored within the 'Err' variant, and this error type includes information like the kind of error, a not found error message that gives you more context, like no such file or directory, because there's actually different ways that the opening operation can go wrong. It's not just if the file doesn't exist. It's also, for example, if you don't have permission to read the file, there's all kinds of different ways that opening a file can be unsuccessful. But if it's unsuccessful, you're always going to get this 'Err' variant. And that's how the 'open' function can guarantee a consistent return type of a Result enum. All right. So let's write code that's going to account for both scenarios, success or failure. And that will make our program very resilient and capable of adapting to change. So here what I'm going to do is rather than assign the return value of 'open' directly to the 'file' variable, instead I'm going to add my 'match' keyword right here. So I'm going to match on the returned Result enum. And here I'll provide my curly braces and react to both of my expected arms which are 'Ok' and 'Err'. So for 'Ok', I'll provide a name for my associated piece of data, which will be the 'File' struct. In the case of a successful read, I'm just going to call this variable 'file', and then I'm going to directly return it as a reminder. If we utilize this syntax, the final evaluation of the match arm will also be the value that is returned and thus assigned to this 'file' variable. So you can name this something differently like 'success_file' whatever you want. But basically I'm saying if we have an 'Ok' variant, take the associated piece of data inside that 'Ok' variant and just return it as the final product of this evaluation so that the 'File' struct will be assigned to the 'file' variable. And then we'll also include our 'Err' variant. And this will also include its own associated data which will be the 'Err' type. I'm going to give that a name. And here I'll provide my curly braces to have some more extensive code. And what do I want to do if I have an error? Well, first. First up, let's send a message to the standard error. So I will use my eprintln! macro and say something like "Something went wrong reading the file. The error was" and then I'm going to interpolate the associated error that I receive from my 'Err' variant. And I'll make sure to render it with Debug formatting. And then let's decide that the user cannot proceed with the operations that we want to do, such as reading the file if we've gotten an invalid entry. So how about we choose to terminate the program at that point? And if the user wants to try again, they'll have to restart the program and provide a valid file. So here this is a good opportunity to use our 'exit' function. So at the very top I'm going to bring in the 'process' submodule into scope. I could also bring in the 'exit' function directly, up to you. And here, after printing to the standard error, I'm going to invoke the 'exit' function, passing in an argument of 1 indicating that there was some error. So then I'm going to add my semicolon here. And there you have it. If we succeed, then the 'file' variable will be set to a File struct, and all of the code after line 11 will continue to execute. If for some reason we get the 'Err' variant indicating that the opening of the file was unsuccessful, we're going to enter into this arm, send a message to the standard error, and then just exit or terminate or quit the program at this point. So moving forward we don't have to worry about the variant of the file not existing. Everything after line 11 will operate under the premise or assumption that we do have a valid file, because if we don't, we're going to terminate the program right here. So now let's take a look at both the success and failure cases. And what I'm also going to do here as well is I'm going to actually println! and show you the actual File struct that we have extracted from the 'Ok' variant right here. So this line will only run in the success case. So first up, let's start with the actual file that we know exists. So I'll change this back to 'story.txt'. We should have no problem reading this. So we're going to hit the 'Ok' arm which will assign the resulting file from the 'Ok' variant to 'file'. Then we're going to print it right here in Debug formatting. And here is our File struct ready to be worked with. And in future lessons we're going to invoke further methods to read the actual content of the file. This is just the opening of the file. Now let's take a look at the alternative case. So I'll switch this back to 'nonsense.txt'. Now we're going to have an unsuccessful open. So Rust will return a Result enum but with the 'Err' variant, which will require us or rather require Rust to output the error message. Here it is. "Something went wrong reading the file". We were then using the Debug formatting to interpolate the error type, which is its own 'Os' error struct. Here is our content. Notice at that point that we exit or terminate the program. That is because of the 'exit' function. Thus we never reach line #13 and we don't see anything output after line #9. All right. So that is just an example of a common pattern in error handling and how you gracefully handle something like a Result enum. You can basically take what you need from it assuming you have a success operation. Otherwise, you can terminate the program or perhaps you can ask the user to try again. It's all about just gracefully handling situations that have gone wrong by usually printing some kind of error message and then deciding where you want to progress from that point.
