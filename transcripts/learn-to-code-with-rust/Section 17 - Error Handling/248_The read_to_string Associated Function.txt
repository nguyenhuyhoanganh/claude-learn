# The read_to_string Associated Function

So let's kick this lesson off with a bit of good news. And that is that there is an even simpler, cleaner way to accomplish part of the functionality that we have within our 'read_file' function. So I think these previous lessons were valuable because I wanted to introduce first the more inefficient way that we can solve various problems in Rust and then the more optimal, efficient, cleaner way. I want to reiterate that as long as your code gets the problem solved, that's the most important thing. Optimizations like the question mark are wonderful to learn and remember over time, but they are shortcuts for other solutions. And as long as you get a working solution and a program that delivers value, that is the most important thing. You can't possibly memorize all of the operators, all of the methods and types within Rust and the standard library. You just get better accumulating that knowledge over time. That's how you grow as a developer. So the good news is the file system module, this 'fs' module right here, includes a helpful function called 'read_to_string' that accomplishes everything we want. The 'read_to_string' function accepts a file path and then it attempts to open a file, read the contents of the file to a heap String, and then return that String packaged up in the Result enum's 'Ok' variant. And if something fails along the way, the function instead returns the 'io::Error' type packaged up in the Result enum's 'Err' variant. So basically it does everything that we're doing right here: the actual reading of the file and the extraction of the content to a heap String. So let's refactor our code one more time. At the very top here, I'm going to remove File and stop at the 'fs' module. So now the 'fs' name is what is available within my file. And here after we collect the user's input because we still need that, I'm going to access my 'fs' or file system module and, within it, access the 'read_to_string' function. I want to emphasize this is a separate concept from the 'read_to_string' method. This method exists on the File struct. This function exists within the 'fs' module, and all it needs once again is the path to the file. So what I'm going to do is I have the user's input above represented by 'input'. I'm going to call the 'trim' method once again to get rid of the newline at the end. So that is a string slice. I'm giving it a reference to the file to 'read_to_string', and it's going to do exactly what we want. It's going to create a new heap String. It's going to open the file based on the path we gave it, read the contents of the file to a heap String that it will automatically create. And then it's going to return not just a String, but the String wrapped up in the Result enum and the 'Ok' variant. The success variant. So I can remove all of this. The final change I need to make is, since I want this to be my final return value, I need to remove the semicolon. And there we have it. So this lives up to the expected return type. This will return a Result containing a String representing the file's contents. So now we no longer need this Read trait. We can simplify that and run it one more time. Please enter the name of the file. Let's do 'story.txt' And there we have the contents of "Once upon a time". And if we have an error like 'nonsense.txt', we get our expected error output. So much cleaner, much nicer. But what it's accomplishing behind the scenes is exactly the code that we wrote manually over the course of the last couple lessons. All right, that's all there is to cover in this lesson, so I will see you in the next one.
