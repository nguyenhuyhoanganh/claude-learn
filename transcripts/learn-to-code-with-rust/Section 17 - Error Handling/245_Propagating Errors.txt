# Propagating Errors

Next up, let's talk about propagating errors. To propagate an error means to bubble it up or send it up so that it is handled by a higher level piece of code. For example, if a function calls another one the original caller, the calling function can handle the errors that the called function generates and sends back up to the caller. And as confusing as it may sound, it actually allows us to simplify our code and also add some flexibility to it. So if we look at our current code, there are some weaknesses in it. I wouldn't say this code is bad, but there are areas for improvement. The first thing is that our main function is doing a lot. It's handling a lot of responsibility and usually it's ideal if the main function stays pretty small and just kickstarts the main process or logic, and delegates the responsibility of various operations to other smaller functions. Our functions should ideally be very small and have a single responsibility. Here we're doing quite a lot in main. We're asking the user for input, opening a file, reading the file to a string, and handling errors. Right. There's a lot going on. In addition, you're going to start to notice a lot of these repetitions in pattern. So for each one of these 'Err' variants here, here, and here, we're essentially repeating the same logic. We are sending something to the standard error and then exiting the program. And every time we run into an error, we have to write this repetitive verbose code to handle that. Furthermore, there's no way to customize exactly what these error messages are going to be. They're all hard coded here in line. And so the design we're going to move to is going to start to move us in a better, cleaner direction. So first things first: we're going to start this refactor. And refactor by the way, means improving the quality of the code without changing any underlying behavior. So this program is working. We want to keep it working, but just make our code better, easier to parse and understand. So we're going to start by moving all of this logic of reading the file into a new function. We're going to call that function 'read_file'. So first up let's define it below. And then we're going to cut and paste our content there. So below main, I'll define a new function called 'read_file'. So what is this function going to return? That is a good question, right? I propose that what it's going to return is going to be very similar to what 'read_line' and 'open' and 'read_to_string' return, which is a Result enum. And the reason it's going to return a Result is because it's going to represent an operation that may succeed or may fail. If it succeeds, that means we have successfully asked the user for the file to parse, we have read it, we have gotten the String of its contents, and that's what we're going to package in the 'Ok' variant that we're going to send back in the Result enum. And if any one of those three operations fail, we can capture the respective error from that operation and package that up in the 'Err' variant of the Result enum. So we're going to similarly return a Result enum that may or may not contain the success data or the failure error. Now, if we are declaring this Result enum as the return type, we need to provide concrete types for its two generics. As a reminder, Result has these two generics: T and E, and T is the type of the data associated with the 'Ok' variant. And 'E' for error is the type of data that is associated with the 'Err' variant. And we have to know what that is so that Rust can perform its type checking and validate that our 'read_file' function indeed returns types that live up to that promise. So here I'll provide my angle brackets. So for the data in the 'Ok' variant, if we successfully parse the file's contents into a String rather than printing them like we're doing here, let's just return them packaged up within that 'Ok' variant. So the concrete type for the 'Ok' variant's data, its associated data will be the String with the file's contents. Now for the error, this is going to be the error type that is basically the exact same error type that is returned by either 'read_line' or 'open' or 'read_to_string'. Because if we run into that error in any one of these three operations, we're going to package it up in the 'Err' variant of our Result enum and return it right then and there. So whatever these three return is what we want to specify in here. And that is a specific type called 'Error' which is found within the 'io' module. So right here I'm going to reference 'self' just so I can refer to 'io' in the code. And I certainly could pull an Error directly. But for now I'm going to stick with 'self' just so that we have a better reminder of where this error type is coming from. So here for my second concrete type, to play the role of the type of data that is going to be living with the 'Err' 'E-r-r' variant, I'm saying that that type of data will be of this type, the Error type living inside the 'io' input/output submodule. All right. There's actually many different errors in Rust or another way to put it is there's many different types called 'Error' with a capital E. But again each one of them is nested within a different submodule which allows us to further distinguish between them. And of course this error type living within the input/output module relates to errors that deal with input and output, which is why all of these input and output operations like accepting user input, right, or accepting a file input, they all return this exact error type. So this is exactly what we want. So here I'm going to provide my curly braces. And now we can go ahead and copy and paste everything we have in here into 'read_file' All right, so our eventual intent is that in 'main', we're just going to call 'read_file' and get back that Result type that we specified. We can even assign it to a variable now like 'file_result'. All right. So right now Rust is unhappy because we are not returning a Result enum. Right? So now we have to go through our code and correct all of these specific occurrences to make sure we are returning that correct type. So let's proceed from top to bottom. This is totally fine. We're creating a String. We are capturing the user's input, right? Here is the first place where we are running into the 'Err' variant from the 'read_line' method. Rather than printing here and rather than exiting the program, what I'm going to do is propagate my error up. In other words, I'm going to say I'm not going to let 'read_file' worry about what to do with the error. I'm going to send it up. I'm going to send it back to the calling function, which in this case is 'main', so that main can handle it however it would like to. So how can we package up this error and send it back up. What we're going to do is we're going to remove this code and we're going to return early here. And we're going to return the 'Err' variant from our Result enum and to it, for the associated data, I'm going to take the 'error' variable I have right here. And I'm going to package it up within the 'Err' variant and return it. Remember, if we are returning at the end of a function or method, we can omit the 'return' keyword and the semicolon. But if we do it earlier, we have to explicitly write 'return' so that the remainder of the code is not parsed. Rust will stop here. All right. Once again this error is the exact same thing as writing this. This is specifically returning the Result enum, more specifically the 'Err' variant, which is still a category underneath the Result enum. It is one possible variant. We are returning this with an associated data that is the specific error that we got from 'read_line' also, which coincidentally returned a Result enum. Right? And because the Rust prelude makes both Result and Err available at the very top, we can either write it like this or directly like this. All right. So moving on. Here we are opening the file based on the user's input. Once again, in this block of code, rather than printing a message and exiting, I'm going to take my error that I get from the 'open' function call, assuming I get an 'Err' variant which is only in this arm of the match statement anyway, and I'm once again going to do the same thing. I'm going to return the 'Err' variant. Create my own 'Err' variant of the Result enum. Take the error I got from the 'open' function. Package it up and send it back out. And now you can see the Rust formatter will clean this up a little bit further. Once again, the commonality here and here is that I'm propagating my error up. It is a consistent error type of 'io::Error'. Input/output error type. I'm packaging it up inside the 'Err' variant of the Result enum, sending it back up to the caller so that the caller can figure out what to do with this error. All right, one more time here. Same exact thing. Return 'Err' Error. Okay, so we are almost there. But Rust is still unhappy because we are still not returning the actual String data associated with the success variant. That is the 'Ok' variant belonging to Result. So here, instead of printing the file's contents, I'm going to return the 'Ok' variant. This is the contrasting variant that Result has. Right? We return 'Err' here. 'Err' here. here.'Err' This is the other case, the success case. And I want to send up the string containing the file's contents which is this 'file_content' String. So I'm going to provide 'file_contents'. This is the last line of the function. So we do not need the 'return' keyword. And we do not need the semicolon. This will be the success story: packaging up this String. So now you'll notice the compiler errors go away. And now we have simplified the function. All right. So now we have propagated the errors up. Let's handle them right here. So 'file_result', just like with all of the functions that we explored in the previous lessons, is a Result. We follow the exact same design pattern that the Rust development team chose for a lot of these other functions. We have represented the idea of an operation that may yield a success or may yield a failure. So of course, one good construct that we could utilize here is the 'match' keyword. So let's match on 'file_result' and once again provide both arms. The first arm is going to be the 'Ok' variant. And what is the 'Ok' variant going to store? Well Rust knows based on our return type here that the 'Ok' variant's data will be of type String. So if we assign a name to the String, in this case something like 'contents', if we get back the 'Ok' variant which can only occur right here, it will store the contents that we have read from the file. So I can print them out right here in something like a println!, just like we did in the previous lesson. Now here is where the other half of the error propagation equation comes in. I'm going to handle all of my errors in a single spot right here. So here I'll react to the 'Err' variant. And for each of the three 'Err' variants below, I have returned the specific error that I got from the respective functions, whether it is 'read_line' or 'open' or 'read_to_string'. So here I can do my eprintln! and say something like "There was an error" and provide my error with let's say Debug formatting. And there you have it. You can technically add a 'process::exit' here as well, but there's no need to do so because the code is basically going to end here. If there was more code going on here, then it might make sense to add the 'process::exit' back, right? For now, we don't need it. So I can also remove the import. And there you have it. So now this has started us on the right path to refactoring. We have decoupled the main logic. 'main' just kicks things off and it delegates the actual responsibility of reading a file somewhere else. If we decided, for example, to extract this function to a library, a 'main' function in some binary crate besides ours could once again call 'read_file', but handle its errors totally differently. It might print a different error message, or it similarly might do something different with the 'Ok' variant. Maybe instead of printing the string's contents, maybe it'll write it to a file or write it to a database, right? The point is that we have separated the actual logic of reading the file from the handling of the results, right? That's why it's literally called the Result, because this is some operation that may result in a success or a failure. But now 'main' will take care of handling the success or failure and 'read_file's' focus is much more limited and contained. There's only one responsibility here. You could say more arguably, but the primary responsibility is just reading a file. All right. And so now we've kind of moved in a direction of breaking up responsibilities and making the code easier to parse. All right. Let's just run this code and make sure it works. So I'll once again enter 'story.txt' You can see everything continues to work as expected. And if we run it with an error for example: 'hello.txt', we're going to run into a specific error here. In this case, it should be this one. We're going to bubble it or propagate it back up where it's going to flow into this 'Err' variant which will print it out with "There was an error". And here is the specific error that we sent up, bubbled up propagated up from returning it within the Result enum's 'Err' variant right here. Same rules will apply for the other two 'Err' possibilities. All right, so moving in the right direction. But there's even more refactoring to come in the upcoming lessons. And I will see you there.
