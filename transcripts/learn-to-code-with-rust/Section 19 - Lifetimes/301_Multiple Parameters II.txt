# Multiple Parameters II

In this lesson, I want to walk through a lifetime example from the official Rust book. The example involves a function called 'longest' that is going to accept two string slices and return the longest one. As a reminder, we can use the 'len' method, 'l-e-n' on a string slice to get its length in bytes, so the function body will compare the two string slices and return the longer one. So let's write the 'longest' function out, and then we'll provide the lifetime annotations for the parameters and return values. And then we'll walk through a couple sample invocations to see how the borrow checker is going to validate the validity of the lifetimes. So right here we'll declare our 'longest' function. And we'll provide two parameters 'first' and 'second'. Both of them are going to be of type ref str ( &str ). And our return value will also be of type ref str ( &str ). Once again, just like we saw in the previous lesson, this is going to be insufficient for the borrow checker. There is our violation appearing and that is because the borrow checker needs us to tell it the contract or what it is going to enforce in the body of the function. How are these two reference parameters related to the return value? So what I'm going to do is declare a generic lifetime called 'a, and I'm going to mark both the 'first' parameter and the 'second' parameter and the return value with that 'a lifetime. So I want to be clear. This syntax does not mean that the 'first' and 'second' parameters have to have the same lifetime. Rather, what we are declaring is that for some generic lifetime called 'a, for some region of code that both 'first' and 'second' live within, that they share, the return value must be contained within that region of code. To avoid a dangling reference, the return reference must live within the shared region of code that both 'first' and 'second' represent. You can almost think of it like a Venn diagram, where 'first' and 'second' are the different circles, and the lifetime is the shared part in the middle. So the borrow checker doesn't know about all of the future invocations of this function. We're just declaring the contract here for the sake of the function's body. So given that we must return something of type ref str having generic lifetime 'a, we must return either 'first' or 'second' from the body of this function. And furthermore, the syntax enables us to return either 'first' or 'second' as the return value because their type, including both the actual type of ref str ( &str ) and the associated generic lifetime will match that of the return value. Remember, when we add a generic lifetime, it's almost like enhancing or augmenting the type to be more specific. A ref str with generic lifetime 'a is a different type from a ref str with a generic lifetime of 'b. So now we're saying that the returned type is going to match either 'first' or 'second', and thus we can return either. So now in the body here let's begin with our comparison. I'll take 'first', I'll call the 'len' method, and I'll check if it's greater than the number of bytes in my second string slice. So if the first string slice has a greater length, I want to return the 'first' parameter because it is the longest one. So I'll wrap this in an if statement, and I will return 'first', and then in an else clause I will return 'second'. And now notice the compiler is happy. Let's start with an easy invocation example. In the body of main, I'll declare two strings. Let's call my first one 'orlando', and here I'll use a heap allocated String, but I will use deref coercion to convert it to a string slice in just a moment. And this can be Orlando. And then I'll have another variable of 'san_francisco' set to another heap string of "San Francisco". And of course, I can invoke the 'longest' function and pass in a reference to both of my heap strings, thus creating two ref strs from deref coercion. So I'll pass in a reference to Orlando and a reference to San Francisco. I'll get back my return reference and I will assign it to a 'result' variable. And then I'm going to output the value of 'result'. And San Francisco has a longer length or a greater number of bytes than Orlando. So we should see San Francisco here. And we do. So how does the borrow checker reason about this code? Technically speaking, 'orlando' and 'san_francisco' do not even have the same concrete lifetime. Orlando's lifetime is slightly longer than San Francisco's. Right? So the way this works is the technical, concrete lifetime that we're going to pass in for 'longest' is going to be the shorter lifetime of these two, or equivalently, the overlapping lifetime of these two. The region where both of these pieces of data exist, thus guaranteeing that a reference to either one is going to be a valid one. So if we look at the existence of 'orlando' as this region of code, that's its lifetime and the existence of 'san_francisco' as this region of code, this region of code is the overlap between the two, and thus it represents the concrete lifetime, and thus the borrow checker understands that for this lifetime. filling in for 'a, the return reference can only be used within this region or this collection of code. So regardless of whether 'result' returns a reference to this string or a reference to this string where we are using it, this code has access to either one of those pieces of data, so the reference is guaranteed to not be a dangling one. 'result' is guaranteed to be valid regardless of which one we get, and thus the code compiles. Next, let's take a look at a similar example, but one in which 'orlando' and 'san_francico' have clearly different lifetimes. So I'm going to move this entire section into a block, a nested block like this. Remember, that creates a separate independent execution environment. That means 'san_francisco' will be cleaned up at the end of this block. It is no longer available. 'orlando' has a totally different lifetime, and that extends till the end of the function. So you can see 'orlando' is totally fine to reference here. And if we have 1000 lines of code here the example will hold true. So clearly 'orlando' and 'san_francisco' have different lifetimes. orlando's lifetime is potentially a thousand lines long, or the region of code comprised of those thousand lines. While the lifetime of 'san_francisco' is much shorter, it's this region of code once again. However, with this 'a annotation, we are not saying that the lifetimes of the parameters have to be the same. 'orlando' and 'san_francisco' do not have to have the same concrete lifetime, and similarly, the borrow checker is not reasoning that 'first' and 'second' have to have the exact same lifetime. It just means that for the overlap of existence between them, the overlap of life between them, the region for which they both exist, the return value must live within that region, within that lifetime, because that is the only way that we're going to guarantee the avoidance of a dangling reference. So in this example, the borrow checker can make that guarantee. So because 'result' is going to be either 'orlando' or 'san_francisco', the shorter of the two or the overlapping of the two is the lifetime of 'san_francisco'. But the region of code in which we are using the 'result' reference, which could be either one, lives within that shorter shared lifetime. That span is right here. If we tried to move 'result' anywhere else like outside, it would not work. But the point is that regardless of which reference, we return a reference to 'orlando' or a reference to 'san_francisco' and assign it to result. Here in this region on line #14, 'result' knows that it's going to be a valid reference regardless of which string it is providing a reference to. Now let's take a look at an invalid example. So what I'm going to do is rather than declare my 'result' variable here, I'll declare it outside right here. I assign it to the result of the block. So I'll provide my semicolon. And in here I'll still declare my 'san_francisco' string. I'll get rid of my print, and I'll simply return the evaluation of the 'longest' function, getting rid of the semicolon. So my intent here is that whatever the 'longest' function returns, that's the reference that I want to assign to my 'result' variable. The problem is, regardless of what we have afterwards, this code will not compile. Let's think about why that is the case. The lifetime of 'orlando' is this long. The lifetime of 'san_francisco' is this long. The shared overlap between the two, the shared lifetime or region of code is the shorter of the two, which is 'san_francisco'. That means that the reference that the 'longest' function returns can only live within this region of code, because that is the only guarantee that we will avoid a dangling reference. Here in this section of the code. We are absolutely welcome to use a reference to 'orlando'. Right? Because here 'orlando's' lifetime is valid. But the possibility here is that this could be a reference to either 'orlando' or 'san_francisco' and the 'san_francisco' string, one possible reference that this return reference is based off... Well, its lifetime is only as long as the inner block, and therefore, if 'result' is to continue afterwards, there is the possibility of a dangling reference if we provide a reference to 'san_francisco', which will no longer be valid. A reference to 'san_francisco' would extend outside the inner block and create a dangling reference. The 'result' variable has the potential to hold a dangling reference so the code will not compile. So if you wanted this code to compile, you'd have to adjust the lifetime annotations here. Let me show you an example that will allow this code to compile. Rather than one generic lifetime annotation, we're going to provide two. So let's call them 'a and 'b in sequence. We're going to assign lifetime 'b to the 'second' parameter. Now notice what this syntax says. We are specifying that we can only return a value that belongs to 'a, which means technically speaking, the 'first' parameter is the only valid parameter that we can return because it is the only one with a matching type. Technically speaking, we don't even have to annotate 'b for this to work, but let's just keep it for the sake of example so we can distinguish between the two. Now we're saying 'first' and 'second' can have two different lifetimes. They don't have to be shared. But the return reference is connected to lifetime 'a. So you must give me something of lifetime 'a and that can only be first, so this code will not compile. Let's adjust it to a super simple example. Let's just return 'first' like so. Maybe we can print out something like. The second is second just so that we utilize the parameter. And now notice the code will compile and I can output 'result' right here. Let's talk about why this worked and the previous example did not. Well now 'orlando' has this lifetime and that is filling in for generic lifetime 'a. The lifetime of San Francisco is the inner block. It's a much shorter lifetime. And that we can imagine is filling in for generic 'b. And what are we telling the borrow checker? We're saying don't worry about 'b. Don't worry about that lifetime. In other words, don't worry about the lifetime of 'san_francisco' in this example, because you don't have to worry about its persistence. After the function is done running, the function is going to give you the 'first' parameter of type lifetime 'a ref str. So all the Rust needs to guarantee is that the referent that 'first' is coming from is where the return reference is going to live within. So in other words, the borrow checker understands that 'result' will be a reference to effectively 'orlando'. There's no way it can ever be a reference to 'san_francisco' because the borrow checker will not permit it from this function signature. Thus, Rust knows that the 'result' variable is guaranteed to be a valid reference to in this example, 'orlando'. And thus it knows that it cannot be 'san_francisco' here, and thus it cannot be a dangling reference. 'san_francisco' is still going to go out of scope here. Its lifetime will end here, but there's no way that we can have a reference to it because we declared that in our requirements right here. Our requirement is that the return value live within lifetime. 'a and our second parameter which 'san_francisco' is playing the role of has lifetime 'b. So this code will prevent that possibility. And now our code will continue to work right. So it really depends on what you're using your reference parameters for. If they are both related to the return value, you're probably going to need to have them share a identical lifetime annotation. If you simply need a reference parameter for some other situation, such as printing it, but it has no relationship to the return value. That's effectively what we're communicating here, that it doesn't matter that 'a and 'b are different lifetimes for 'first' and 'second', because the return value only cares about lifetime 'a, which corresponds to the 'first' parameter, and thus enables only 'first' to serve as the return value.
