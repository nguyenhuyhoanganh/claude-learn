# Section Review

Congratulations on reaching the end of another course section. As always, let's take a few moments to review everything that we learned. This section focused on lifetimes in Rust. A lifetime is the region of code in which a value is alive, which means functional, valid, useful. Another way to define a lifetime is the time during which a value exists at a particular memory address. Lifetimes exist so that the borrow checker, which is a feature of the Rust compiler, can identify and prevent dangling references. A dangling reference is a reference to data that no longer exists or does not exist. The example I gave is if the reference is an address, then a dangling reference is like an address to a building that is no longer standing. That is no longer there, and in the real world that creates problems and in other programming languages that also creates problems, because your code might expect to follow a reference to legitimate data, but instead be working with no real data. And that, of course, leads to bugs. So a scope is the region of code belonging to a block, and a block is a section between a pair of opening and closing curly braces. And a value's lifetime is often connected to the idea of scope. Not always, but usually. Right. So each value has an owner. And when the owner reaches the end of its scope, usually the end of its block, it cleans up the associated data and thus the value's lifetime ends or concludes. So scope and lifetime, these are concepts that we introduced way back when. They're all related, but they're not always exactly the same thing. We also talked about this idea of references. And all values in Rust have a lifetime, but it is really a reference's lifetime versus just a plain value's lifetime that the borrow checker is concerned about. So we use this term referent and that is the data or original value that a reference borrows. The referent is what the address is pointing to and a reference's lifetime is intrinsically connected to the referent. It's connected to the source of data. So it's not just where we're using the reference in the code. It's also that wherever we're using the reference in the code, we must still be able to follow that address back to the referent, which must still be valid. The reference's lifetime, must be contained fully within the referent's lifetime to avoid a dangling reference. Now, the borrow checker has this cool feature called non-lexical scope for references, which means a reference's lifetime ends at the last place in the code that it is used, rather than at the end of the block. And we saw from the documentation that that helps us a little bit because it allows for certain safe situations that the borrow checker previously thought were unsafe to be able to be valid in our code. All right. So we talked about the differences between generic lifetimes versus concrete lifetimes. A concrete lifetime is an actual realized, real region of code that a value exists in the program, which corresponds to the time that that value exists at its memory address. A generic lifetime is a hypothetical lifetime, a non-specific, non-concrete future lifetime that has the potential to vary. That's why lifetimes are a category of generics. Just like a generic type is not a hard coded type, it's a possible type in the future. It's the same principle with a generic lifetime representing some future hypothetical lifetime. So when we declare lifetime annotations, we are declaring generic lifetimes. We're not going to change any of the existing references' lifetimes. We're not altering any of the implementation. This is simply a statement of truth that we are declaring for our borrow checker to validate. So lifetime annotations identify relationships between reference parameters and reference return values in the function. So an annotation creates this coupling. You can almost think of it like a marker that draws an arrow between the return reference and some of the parameters, or maybe one of the parameters. And remember what that does is it effectively copies the reference parameter's lifetime over to the return reference, which means the returned reference is aware of the original referent and is coupled to its existence. Because the return reference must fully live within the lifetime of the referent, within the existence of the original source of data that it is providing an address to. So how do we declare these generic lifetimes? We begin with the pair of angle brackets, and then we assign the lifetime a short name that has to start with the tick, that is, that apostrophe symbol ( ' ). And 'a is a very common name. And if you have multiple lifetimes, they're usually going to be sequential. So 'a, 'b. And then we just have this marker. We have this identifier called 'a. And we can use that to mark the connections between the references and the return value. We basically need some syntax to create a connection right? In the real world, we can use our finger or an arrow to point from one thing to another. I like to think of the lifetime annotation as that marker that just allows us to indicate to the borrow checker that these two things are connected, that they are related to one another. And you can think of that lifetime annotation as augmenting a type, which means enhancing or adding on to it. So a type of ref str ( &str )  is different from a type of ref str with a generic lifetime tick a ( &'a str ), and those are both different from, say, ref str with a generic lifetime tick b ( &'b str ). It's almost like creating its own separate category or identifier or qualifier. That is the generic lifetime. It adds more specificity to a reference by indicating that it is connected to to some generic lifetime. Marking multiple reference parameters with the same lifetime annotation does not mean that they have to have an identical concrete lifetime. We are not altering any specific concrete lifetimes whenever we use this lifetime annotation syntax. We're just declaring the contract. So if there are different concrete lifetimes being passed in, the borrow checker will take the overlapping lifetime, which is also the shorter or the smaller of the two lifetimes. And it will say that the return reference must live within that smaller, shorter lifetime, because if it did not make that guarantee, then you would have the possibility of a dangling reference, right? It's picking the region of code that is the smaller one, where both the first reference and the second reference will live within, because anything within that region is guaranteed to not be a dangling reference or a reference pointing to any data in that region is guaranteed to not be a dangling reference. The borrow checker will validate that a returned reference to either parameter will be valid, because they're going to be bound or constrained by the smaller lifetime of the two. All right. We also talked about the lifetime elision rules. These are inferences that the borrow checker makes. And if it is able to figure out the relationships, then we no longer need to provide our lifetime annotations. The first rule is that the borrow checker assigns a separate lifetime to each reference parameter, not to every parameter, but to every reference parameter. Then, if there is one reference parameter and the return value is the reference, the borrow checker assumes that the parameter's lifetime is connected or applies to the return value reference's lifetime. That is the only logical way that it can work, because we cannot return a reference to data created in the body of the function or data from an owned parameter, because both of those would be dangling references. So if a return value is a reference, it must be connected to a reference parameter. And if there's only one reference parameter, then it's obvious which one it is connected to. Where we run into problems is when we have multiple reference parameters. Now there are multiple possibilities for the relationship between the parameters and the return value, and that is when we need to manually provide our generic lifetime annotations. The third lifetime elision rule is in a method definition. If you have a parameter that represents a reference to the instance, either an immutable reference or a mutable reference, it doesn't matter. Then the borrow checker will assume that the lifetime of 'self'. In other words, the lifetime of the instance is connected to the return value. So you're probably returning a reference to something that lives inside your instance, say, the struct. And so it makes sense that that reference cannot outlive the struct or it will be pointing to invalid data. Finally, we talked about declaring lifetimes directly within structs. So struct, for example, can sort a field that holds a reference, and so the lifetime of the struct must be connected with the lifetime of the field. In other words, the lifetime of the struct must end or conclude before the lifetime of the field's referent, because if the referent that the field is basing its reference off goes extinct, goes out of scope, or its lifetime ends, then the field is then storing a dangling reference and the field is part of the struct. So in other words, the struct must first have its lifetime end so that its reference field is no longer valid, and then the referent from which that field got its reference from must end after that. The struct and its lifetime must be fully contained within the lifetime of the referent that the field is borrowing a reference to. And that's basically what we're annotating with our annotation. We're declaring the connection between the existence or lifetime of the struct and the existence or lifetime of the field and its referent. And we can get obviously more complex there. So the good news about lifetimes is if you're making a mistake, the borrow checker will catch you. And usually you don't have to be dealing with a lot of complexity in your day to day work with Rust, with lifetimes. Usually where you're going to run into certain issues or need to provide them is, again, if you have functions or methods that have multiple reference parameters and the borrow checker just doesn't know what relationship you would like to establish between the return reference and the parameters, in which case, if you know better than it does, then you just declare it. The code will compile and everything, everything looks good. All right. That's all there is to cover in this lesson and in this section. So I will see you in the next one.
