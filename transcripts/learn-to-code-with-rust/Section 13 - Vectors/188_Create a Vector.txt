# Create a Vector

Welcome to the next section of the course, which is all about vectors. A vector is a data structure that is similar to an array. As a reminder, an array is a sequenced collection of homogeneous data, which means data of the same type. An array looks like this. We create it with a pair of square brackets and we populate it with elements. We separate every two elements with a comma and a space. So this array is populated with three string slices. Now this type annotation for the array is optional because Rust can infer it from the initial value assignment. But if you want to write it out, you provide the square brackets, and then the first value inside the square brackets is the data type. Then you have a semicolon. And then the second type is the array's length. So here the ref str ( &str ) indicates a string slice, a string reference, and the number three indicates a length of 3. And that 3 is very important because it reflects the fact that the array has a fixed length, and that is the array's greatest limitation. It is a fixed size data type. It must have a consistent length throughout the program, so the number of elements within an array cannot change. We cannot add an element or remove an element. And if you have a collection of elements that you need to keep in order, that also has the potential to grow or shrink over time, then an array will no longer be suitable. For that, we have the complementary solution, which is a vector. So you can think of a vector like a more flexible array. A vector is a container that stores values of the same type in order. So this is true from both a code perspective and a computer memory perspective. Vector values are written contiguously in memory, which means sequentially and connected to one another, but the advantage of a vector over an array is that a vector can grow and shrink in size as the program runs, and to enable this functionality, Rust stores vectors on the heap at runtime. That gives it that dynamic area where it can place the vector and also give it the capacity to grow and shrink. So let me remove this code. The type of a vector in Rust is 'Vec'. 'V-e-c', and this type is included in the Rust prelude, so we can use it immediately within our program. The 'Vec' type includes a constructor function which we can access with the scope resolution operator, and one simple one is 'new'. This 'new' function will return a new empty vector, an empty instance, so I can provide my parentheses here to invoke this constructor function, and I'll assign it to a variable called 'pizza_diameters'. We've seen this pattern before with things like Strings. The 'new' function typically returns an empty version of a type. That is a common pattern when it comes to this name in the Rust ecosystem. So the vector type defines one generic, which represents the type of the elements that the vector will hold. In this situation, since Rust cannot infer the type of the elements within an empty vector. The code will not compile until we provide explicit type annotations. So the way I can do so is after my variable name a colon. My type is 'Vec'. Then, we'll provide the familiar angle brackets to provide a concrete type for the generic. And I can say something like an i32 if I want this vector to store integers. An alternative perspective that you can use or alternative syntax rather is the turbofish operator. And the way that looks is right here. After 'Vec', you're going to add the two colons and then the angle brackets with your type. It's a little bit weird because you have 'Vec'. Then you have your turbofish operator, which is that segment right here ending at the closing angle bracket, this thing right here. And then you have the regular scope resolution operator and the 'new' constructor function. But this accomplishes the same result. It tells Rust we're going to be creating a vector. And the concrete type for generic type I think it's called 'T' but it can be called whatever the Rust developer chose represents the type of the element inside the vector. And even though the vector is empty, Rust now knows what we intend to store in it. So vectors do not implement the Display trait, but they do implement the Debug trait out of the box. So we can go ahead and output this vector using our familiar question mark ( ? ) syntax. I'm going to write my variable inline like so. Then provide my colon and my question mark for the Debug formatting. We can see right here it looks very similar to an array because a vector is very similar to an array. It's a sequential collection of homogeneous elements. It just has the potential to grow. So let's declare another vector below to practice, this one of a different data type. So I'll call this variable 'pastas'. Once again, colon, vector, which is 'Vec'. Then I must provide a concrete type for the generic. We can say that this vector will store string slices, so I can do a ref str ( &str ) here. And once again on the right hand side, create my new vector with the 'new' constructor function on the 'Vec' type. An alternative syntax for this, again, if you don't want to annotate on the left is to provide the turbofish operator right here, That turbofis operator is both colons and then the angle brackets with a concrete type. Here I'll provide my string slice. So line 6 and 7 are basically equivalent. And then I can output the value of the 'pastas' variable similarly right here. And we're going to see a similar empty array. Now if we know the starter values that we'd like to populate the array with, we can also use a special macro which is called vec! It's lowercase 'vec' and it ends with the exclamation point, which is common for all macros in Rust, just like println! So with this, we provide a pair of square brackets and then we provide the initial values. So this syntax looks very similar to creating an array, but this is still a vector due to the presence of the vec! macro before the square brackets. So let's say for my 'pizza_diameters' here, I'm going to remove this line. And rather than having an empty vector, I'll have a populated vector. Here, 'vec!' with the exclamation point. Then my square brackets and let's say some sample pizza diameters are 8, 10, 12, and 14. This time around, because my vector has initial data, Rust can infer the type of the element to be an i32 by default, so this syntax is no longer needed. It is optional. You can see right here. If I remove this, Rust will infer the exact same thing of a vector storing i32. However, type annotations can still be helpful if you want to of course replace the default. For example, if I want a vector of i8s,  I can do that. Like so. Same principle for our 'pastas' down below. I can go ahead and remove this type annotation, because Rust will infer it from my assignment on the right hand side. That is, assuming that I have some kind of vector with pre-populated values, which I will have with this vec! macro. So once again, here's my macro. Provide my square brackets, not parentheses like with println! but square brackets. And here I can provide some starter values something like "Rigatoni". Let's do "Angel hair" and "Fettuccine". And now when I print this out we should see our Debug trait representation. And here it is. So just to clarify, if you know all of these values in advance and these collections will never change or are not intended to change over the course of the program, you're more than welcome to use an array. In fact, an array is even better, right? Where the advantage comes in from a vector is when you need the program to be able to add elements or remove elements, so the size of the vector can grow and shrink. You can have a vector start out with 4 elements, then expand to 10, then grow to 6. Or perhaps in a situation where you don't know how many elements you're going to need at runtime, right? Maybe you, for example, are asking the user how many files you want to read in from the disk and storing each file name. Well, the user might ask you for 5, or they might ask you for 10. And if you need to store that collection of either 5 or 10, you need a container that is capable of dynamically expanding, expanding to any size length. And that is where vectors become a much more ideal solution than arrays. All right. So hopefully a vector makes sense. And in the upcoming lessons, we'll see how we can add elements and remove elements from vectors and much more. So I'll see you there.
