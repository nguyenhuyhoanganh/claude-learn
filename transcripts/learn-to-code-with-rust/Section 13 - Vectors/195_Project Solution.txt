# Project Solution

So let's walk through a sample solution to the coding challenge in the previous lesson. This time around, we were modeling a file system on a computer and it's going to consist of files and folders. So first up we have to define two structs beginning with 'File'. And 'File' is going to have a single 'name' field. So let's define our file struct. And inside curly braces, I will define my 'name' field set to a heap String and then also derive the Debug implementation on the line above. Hashtag square brackets. Derive the Debug trait. We're going to have a 'Folder' struct also with a 'name' field set to a String. So right here 'struct Folder'. It's going to have a 'name' field set to a String. And it's going to have a 'contents' field set to a vector of 'File' structs. A folder or directory on your computer is going to consist of many files. And that's what we're trying to simulate here. So we'll declare our 'contents' field and I will declare the type of this field as being a vector. And by itself, this is insufficient because the compiler needs to know a vector of what? What kind of element is this vector going to be storing, and it's going to be storing 'File' structs. So in here I'll declare the type for my generic to be a 'File'. And this is the exact same struct that we have above. And then I will provide my Debug trait implementation above. All right. On the 'Folder' struct, we need to define a 'new' constructor function that accepts a 'name' String. And the method should create and return a new 'Folder' with that name. And for the 'contents' field, we're going to initialize it with a empty vector. So when we create a 'Folder' on our computer, just like in the real world, it's going to start out with having no files within it. And then we create files within that directory. So here to define both my associated functions and my methods I'll add an 'impl' block for my 'Folder' type. And I will define a 'new' constructor function. And if I do not provide any reference to the instance, and I'm using reference in the general term here. So neither a reference nor a parameter for ownership. If I just skip that 'self' keyword entirely, that's how the compiler will know that this is a function that lives on the 'Folder' type, rather than a method on an instance. So we only need one parameter here it's going to be 'name', and it's going to be a String, and it's going to return an instance of 'Folder'. And I can write 'Folder' or use the shortcut of capital S 'Self' which represents the type after the 'impl' keyword anyway. Within the body of the constructor function, I will create a new instance. Once again, I can use either 'Folder' or 'Self', and right here to create a new 'Folder' and implicitly return it. I need to provide a name. I'm going to use the shorthand syntax of just name, which will connect this struct's 'name' field to the 'name' parameter. And then I need one more field which is my 'contents' Vector. So here I'll provide 'contents' and using my 'vec!' macro like so I will create an empty vector. And this time around, Rust knows that this vector will be storing 'File' structs. And that's because it can observe this exact definition right here. All right. So you can see it says I know it's going to be storing 'Files'. All right. Next up we're moving on to methods. So define a 'create_file' method that accepts a 'name' String. And the method should create a new 'File' with that name and add it to the end of the 'contents' vector. So I'm going to define my method called 'create_file' And we know we're going to be modifying some part of the 'Folder' struct's internal state. So that gives us two options. We can either take mutable ownership or borrow a mutable reference. I typically want to go with references so that I don't have to worry about returning the instance back to the caller, so I will take a mutable reference with '&mut self'. And then I also need the name of my file. That's going to be my 'name' parameter set to a String. This method is not going to return anything. And in here, my first responsibility is to create a new 'File' with that name. So I'll declare a 'file' variable. And here I will instantiate a new 'File'. This is the struct from above. And it needs a 'name' field. And I will provide the 'name' parameter that comes in as a parameter. So now I have my 'File' struct and I need to add it to the end of my 'contents' vector, which is a field inside my 'Folder'. So I can take 'self' which represents my 'Folder' instance. I can access its 'contents' field which is a vector, and now I have to know the right method to call on a vector to append an element to its end. And that is going to be 'push'. So I will push an element to the end. And that is going to be my 'File' from above. And then the vector is going to take ownership over that 'File' struct. And that's all we need to do. Next up define a 'delete_file' method that accepts an index parameter of type usize. The method should remove the file at the specified index position from the 'contents' vector, and should also return the 'File' back as the return of the method. So here I'll call my method 'delete_file' Once again, I'm going to be modifying the 'contents' vector, but that is a field of the 'Folder'. It is part of the 'Folder'. So if I'm modifying any nested field on that struct that is a mutable operation, and I need to have either mutable ownership or a mutable reference parameter to enable any code in the body of this method to mutate. So once again I will borrow a mutable reference. Then I will define my 'index' parameter of type usize. And now once again I can take my 'Folder' instance, access the 'contents' field which is a vector, and then I need to find the right method to invoke on my vector to delete a entry based on its index position. And that of course, as we learned in the section, is the 'remove' method and 'remove' is going to take the index position. I provide that directly to my own 'delete_file' method. So I'm going to pass it in here. And the 'remove' method on a vector is going to take out the element and return the element itself. In this case, the element that is being stored within our 'contents' vector is a 'File'. And that's exactly what I want to 'remove'. So notice right now if I save we're going to have one violation. And that is that we did not specify the return type of 'delete_file'. It's going to tell us here. "Hey, you're returning a 'File' which is what the 'remove' method returns, and thus also serves as the implicit return value of our own 'delete_file' method. So all I need to do to fix this is to add my return type arrow and say my 'delete_file' method will return a 'File'. That's exactly what 'remove' will return as well, and I will allow the 'remove' method's returned 'File' to also serve as the return value of my 'delete_file' method. Finally, we need to define a 'get_file' method, and this one is going to accept an 'index' parameter of type usize and should return an Option containing a reference to the 'File' at that index position. So this time around, I am only reading or accessing data. There is no mutation or modification, so I do not need a mutable reference. And I'm going to settle with a regular immutable reference, which I can do with '&self' and then provide my 'index' parameter of type usize, and this is going to return as specified an Option containing a reference to the 'File'. So here I'm declaring my Option enum. That requires us to provide the concrete type for its generic. And it's not going to be a 'File' even though this is technically valid, but it's not what the method is looking for. We are looking for an Option where the 'Some' variant is going to store a different type, and that is a 'File' reference. So very critical to add that ampersand ( & ) in front. So our 'Some' variant is going to borrow a reference to the file which will allow the vector to maintain full ownership over that file. We're just going to get its memory address. So the 'Folder' is going to store its 'contents' vector. And the vector is going to be the owner of all of the 'Files' inside it. So once again 'self' to access my 'Folder', 'contents' to get its internal vector of f'Files'les. And then the method we need to invoke on a vector to get an element at its specific index position is the 'get' method. And here we can pass an index. Once again, make sure to omit the semicolon. And this is going to accomplish exactly what we want. Because remember the 'get' method returns an Option where the 'Some' variant holds a reference to a piece of data at that index position. The reason it returns an Option is because there is a possibility that the specified index does not exist, and in that scenario, the Option enum variant will be 'None'. But it's still modeling the same idea of something that may or may not exist. And if the index is valid, then Rust is going to take that element, borrow a reference to it. Wrap it up in the 'Some' variant, which is one member of the Option enum, and return that 'Some' variant back out. That's what the 'get' method is going to do, and we'll allow that to serve as the return value of 'get_file' as well. So we have all of our methods. Now in the main function we can practice creating this 'Folder' instance and invoking these methods. So in the body of main, let's use the 'new' function to create a 'Folder' instance with a name of your choosing. So here I will declare a 'folder' variable. I'm going to take my 'Folder' type. It has a 'new' constructor function defined on line #13. This needs a String argument. So I'll provide a sample folder name like "Documents". And there we have it. And now we have a folder with an empty 'contents' vector. Call the 'create_file' method two times. Now remember this 'create_file' method is going to modify the vector that is being stored in the 'contents' field. That's why we needed a mutable reference here. That also means we're going to need to make our original owner of 'folder' mutable as well. So I will add the 'mut' keyword. That is a requirement to be able to call the 'create_file' method. So all we need to pass in here is a String with the file name. The method body will handle the abstraction of creating the actual 'File' struct, handling that complexity and adding it on to the end of the 'contents' vector. So let's create a 'File'. Let's of course have fun with it and imagine it's a Rust file. So I will call my file 'main.rs' And then let's call the method one more time on the next line. And let's call this one 'lib.rs'. And then we are asked to print out the 'Folder' in Debug format. And that will allow us to confirm that we have indeed added on these two 'File' structs to the end of my 'contents' vector. So let's println!, let's output the value here in Debug format. And this is going to be our 'Folder'. In fact, since I know it's going to be a lot of nested content, let me use pretty-print here by adding the hashtag in front of my question mark Debug symbol. And there we have it. There is our 'Folder' struct, there is its name, and here is my 'contents' vector represented visually as a pair of square brackets. And here are our two 'Files' that we have added with the 'push' method. First, we added on 'main.rs' to the end. Then we added on 'lib.rs' to the end. And those are both complete 'File' structs. So we have a nested custom data type stored inside a vector, inside a 'contents' field, inside another custom data type called 'Folder'. All right. Next up, let's delete one of the two files using the 'delete_file' method, and then print out the 'Folder' in Debug format to confirm it worked. Easy enough. I will take my 'Folder' and call 'delete_file' on it. We have two total 'File' structs, so our index positions are 0 and 1. Let me call the method with an argument of 1 that should remove 'lib.rs', more specifically the 'File' struct with that name, and if I output the value of my 'Folder' afterwards, we should see it has one less entry. Here is my second output, and indeed the 'contents' vector has only 'main.rs' remaining. That is the original 'File' at index 0. Perfect. Finally, call the 'get_file' method and use a match statement to react to both of the Option variants. For the 'Some' variant, print out the 'File' in Debug format, even though technically it's going to be a reference to a 'File', but same principle. And for the 'None' variant, we want to print out the text. "There was no file". So I will take 'folder'. I will invoke the 'get_file' method. And I need to provide an index position of the 'File' to extract. There is only one index left which is 0. Regardless, this 'get_file' method will return an Option, so I can use my 'match' keyword to react to both the 'Some' and 'None' variants. Let's start out with 'Some', and if there is some data to extract as returned by the 'get' method on my 'contents' vector, it's going to be a reference to the 'File'. I'm just going to call it 'File'. And we should see the type annotation pop up there. We can see the reference to the file. Once again that is what the 'get' method returns. And that ensures that our 'contents' vector is going to remain the owner of the 'File'. And thus we are not accidentally going to take partial ownership away of one of the elements inside the vector, which would be a really weird state. So we have a reference. We have an address which gives us really everything we need in order to go to that location in memory and find out whatever we need with this 'File'. So here, for example, I can still output it in Debug format so I can write something like "Retrieved file" And then I will interpolate the value of my 'file' variable which was declared right here. I gave a name to that reference to 'File'. And here I can print it out in Debug format. And then after a comma and a line break, I need to handle my other Option variant, which is 'None'. And for that I can println! and say exactly as the instructions recommend. "There was no file". All right. So save that. So this time around we are accessing index position 0. That exists. So we're going to get a reference to this file right here with the name of 'main.rs'. We're going to say "Retrieved file" and then print out that 'File' struct in Debug format. And there it is. And we can see the name is 'main.rs'. Now if I try to access a file at an index position that does not exist, for example index 5 does not exist. Not a problem. Our program is not going to crash. The 'get' method is just going to return the 'None' variant, which will also be the return value of my 'get_file' method. And so here I'm going to match on that 'None' variant. So we're going to hit this pattern and output. "There was no file" at index 5. And there you can see our program is gracefully handling the possibilities of having an entry as well as not having an entry. All right. So that's all there is to cover in this exercise. Hope you had a little bit of fun learning more about vectors in Rust. And with that I will see you in the next lesson.
