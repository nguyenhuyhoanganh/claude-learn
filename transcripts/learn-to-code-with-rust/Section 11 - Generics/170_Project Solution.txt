# Project Solution

So let's walk through a sample solution to the coding challenge in the previous lesson. The idea here was to use Rust to model a chat system where users can share audio files, video files, and text messages. So let's begin by defining our 'DigitalContent' enum with its two variants: 'AudioFile' and 'VideoFile'. So above main, I'll declare my enum. It's called 'DigitalContent' and within my curly braces for my enum variants, I'll do 'AudioFile' as well as 'VideoFile'. We are also asked to derive a Debug implementation. So on the line above my enum declaration, I'll add my hash tag, my square brackets and ask Rust and the compiler to derive the Debug trait. That takes care of that. Next up, let's define a 'ChatMessage' struct with two fields: 'content' and 'time', and the struct should define one generic, which will be the type of the 'content' field. The 'time' field will always be a String. So here we'll use our 'struct' keyword to declare a struct. This one is called 'ChatMessage', and we need to declare our generic here, so that we can use that type for one or more of these struct's fields. So here in angle brackets I'll define my generic and call it 'T'. That name is up to us, but 'T' is very common because it is short for type. And then here I'll define my two fields starting with 'content' which will be of that generic type 'T'. So this can vary every time that I create a new instance of my 'ChatMessage' struct. In comparison, the 'time' field which I will declare on the next line will always be a String. So there is no variation on that field and its data type. We are also asked to derive a Debug implementation for the struct, and the syntax is the exact same as it is for the enum. So hashtag square brackets and we'll derive the Debug trait, so we can now print it out with a question mark. All right. Next up, add an 'impl' block for 'ChatMessage' structs whose 'T' type is a 'DigitalContent' enum. So in other words we're going to define methods that are only available on 'ChatMessage' instances where the type of 'T' and thus the type of content happens to be a 'DigitalContent' enum. It will not exist on any other 'ChatMessage' instance where the type of 'T' is not 'DigitalContent'. So in order to accomplish that, I will declare an 'impl' block for 'ChatMessage', and by itself this will be insufficient because the generic type 'T' is part of the 'ChatMessage's' structs type. So you'll notice if I save, the compiler will tell me you are missing the generics. I need you to give me what is going to play the role of 'T'. Now, if I provide my angle brackets here, I can write specifically 'DigitalContent', which is totally valid, but it's important to note that because of this syntax, we are explicitly defining or implementing methods and functions, functionality, for only 'ChatMessage's that have a concrete type of 'DigitalContent' filling in for generic  'T'. So if this, for example, was a string in an instantiation of a 'ChatMessage' struct, then the methods defined here would not be available. They are only going to be available for 'ChatMessage' instances where the type of 'T' is 'DigitalContent'. So here I can define a method. Let's see what it would like us to do. Define a 'consume_entertainment' method. So right here, I'll write my method syntax. And our name is called 'consume_entertainment'. And the method is going to print out the value of the 'content' field. For example, "Watching the AudioFile" There's no mutation or anything like that going on. And I'm going to assume that I do not want to take ownership. So I'm going to borrow a mutable reference using the ampersand self ( &self ). And there we have it. So here we can print a message out using println! Something like "Watching the {}". And then I need to do two things here. One is actually provide the value of that 'DigitalContent' variant that I'm going to directly render. So I'll provide 'self.content'. And remember, because of this definition, the compiler knows that 'self.content' which has type 'T', the compiler knows that type 'T' is going to be 'DigitalContent'. And thus when we add the Debug syntax right here, the colon question mark ( : ? ), Rust can verify that 'DigitalContent', that type of 'T' does indeed implement the Debug trait, and thus it is valid to print it out in Debug format. It knows the exact type of content to be the 'DigitalContent' enum variant, so that defines that method only on that type of 'ChatMessage'. But afterwards we are also asked to add an 'impl' block for 'ChatMessage' structs with any type 'T', so these methods will be available no matter what the type of 'T' is. In other words, they will be available on all 'ChatMessage' instances. And the syntax for that is slightly different. So the problem with simply writing a simple 'ChatMessage' and providing a generic type 'T' here is: think about this example versus this example. How is the compiler to know that we intend for this 'T' to be some generic? It could be a construct in our program, like an enum or a struct that happens to have the name 'T'. So the compiler needs to differentiate between this situation and this situation. So in order to accomplish this result where we are defining a method for any type 'T', for any generic 'T', remember we have to add that generic right here. So since we're calling it 'T' here, we're also calling it 'T' here. And now Rust understands that this 'T' is not a concrete type. It is in fact a generic type 'T'. So these methods will be available for any type 'T' playing the role of this 'T' right here in 'ChatMessage'. So we need a method here called 'retrieve_time'. And it's going to return a String. So I'll once again borrow a mutable reference. And this time around with my arrow, I will designate a return value of String. And the method should return a clone of the 'time' field. So I can access 'self'. I can access my 'time' field. That will give me a String, and I don't want to return it because that would move ownership from my field to the outside world. I instead want to take this string and I want to clone it, and I can do so with the 'clone' method. I will omit the semicolon. So this cloned copy of that heap String will serve as the return value of the 'retrieve_time' method. All right. So now we are set to test these ideas in our main function. So right here we want to create 3 'ChatMessage's, one with each of these variations, one with a string slice for 'T', another with String and another with a 'DigitalContent' enum variant. Let's begin with a 'message' variable, and here I'll declare a 'ChatMessage' instance. And we need two things for this one. We want 'content' set to a string slice. So for 'content', I'll do a nice string slice of "hi lol". And then we need a 'time' field as well which will be a heap String. So I'll use 'String::from' here and provide an arbitrary time or date. Let's just do a date. This is arbitrary, but I'll do "2025-03-12" and then provide my semicolon. And that is our first 'ChatMessage'. And notice that the compiler infers that the concrete type filling in for generic 'T' is a string slice, this ref str ( &str ). All right. Next up we want a 'ChatMessage' with 'content' set to a String, which is a different type from a string slice. So right here I'll call this 'notification'. Once again. here is my 'ChatMessage' struct. I need 'content', and for this field I'm going to use a different type of 'T'. I'll use a heap String, so I'll do a 'String::from' and do something like: "What's your favorite pizza topping?" And then for my other required field of 'time', I can probably just copy this since it's arbitrary. Maybe change this to '04-12' and that should do it. And now we need one more, which is going to be our 'ChatMessage' that actually has the 'DigitalContent' variant up here, this enum. So right here let's call this 'audio'. And I will create a final instance of 'ChatMessage'  for 'content'. This time around, I need to provide a valid variant of the 'DigitalContent' enum. So I'm going to choose 'Audio'. So 'DigitalContent' is my enum. Two colons, and I'll access the 'AudioFile' variant. So now the concrete type of 'T' is 'DigitalContent'. My 'time' field is hard coded to be a String type. So I can copy this value right here and maybe change this to '05'. And now we have our three instances. So reviewing what we defined here, this 'consume_entertainment' method is only available if the type of 'T' is 'DigitalContent'. That means 'consume_entertainment' is only available on this 'audio' instance. It is the only one that has a 'DigitalContent' enum playing the role of 'T'. So if I want to do 'audio' and invoke 'consume_entertainment', that's going to print out "Watching the audio file", we're going to see that content right here. That works as expected. And of course, if I try to invoke the method of 'consume_entertainment' on something else, you'll notice my VSCode rust-analyzer IntelliSense is not popping up, and it's not popping up because this method does not actually exist. It's not supported because we only defined it for 'ChatMessage' structs with a concrete type of 'T' of 'DigitalContent' and 'consume_entertainment' is invalid. Now, the method that does exist on every type of 'ChatMessage' struct is 'retrieve_time' because of this more generic syntax where we define this method for any type 'T'. So for example, here I can do 'message.retrieve_time'. And I'm just going to go ahead and add a println! here so we can see this return string. So I'll provide some room for dynamic content. Provide this. Close this off and I can do the exact same thing below. I will inject my dynamic value, take 'notification', and invoke 'retrieve_time'. The only reason I'm doing this is just to demonstrate that 'retrieve_time' is going to work, which validates that the message is defined on each one of these 'ChatMessage' structs. So same thing here, but I'll swap 'notification' with 'audio' and that should do it. So we should see copies of the respective times. And there they are. Here are all of our dates. Here is our output from the 'consume_entertainment' method on our 'audio' struct. And there's our final date. All right. And that should do. I I think that covers everything that we have written here. And yep, that's it. That is a review of generics. Remember, a generic is just a placeholder for a type. Just like an argument represents a concrete value that we pass in for a parameter. Well, in these situations we are providing the concrete type to fill in for the generic 'T' that we defined here. So 'ChatMessage' and its type is intrinsically linked to this dynamic type that can vary at the points where 'ChatMessage' is actually instantiated. And now you can see we can have these variations. We keep the common core, which is a 'ChatMessage' struct that has a time and a content, but actually add a little bit more of dynamism, because what 'content' can be can now vary, which sort of enables us to reuse certain features of the struct, while also giving it a little bit of variety and room to vary. Right? That is the whole benefit of generics, whether we are using them in functions or structs or enums or other Rust constructs. That's all there is to cover in this lesson, so I will see you in the next one.
