# Generics and impl Blocks II

In the previous lesson, we defined methods for structs with a specific type for generic type 'T'. For example, a String here and a three element array of string slices here. But of course, we also want the ability to define methods for structs with any type 'T', and we need to utilize an alternate syntax to accomplish this. So as we know, removing the brackets entirely and just trying 'TreasureChest' by itself will not work. So this is invalid code. And if I try to do something like this, if I provided angle brackets and provided something like 'T' here, this is also not going to work because Rust is going to look for an actual concrete type called 'T', just like String, just like this array here. So imagine, just for the sake of a contrived example, that you happen to have a struct by the name of 'T', right? Rust thinks that this is an actual concrete type. Another example is maybe we have an enum called 'T', and Rust thinks that we're looking specifically for that concrete type, so I don't want an actual concrete type here. I want it to be generic. I want these methods to exist for any type of 'TreasureChest' or in other words, for any 'TreasureChest' irrespective of what the type of 'T' is. So in order to accomplish that, we're going to use a slightly different syntax. It can be a little bit confusing, but I wanted to explain why we have to do it. And it's to avoid the ambiguity here where Rust will not understand that 'T' is generic, it thinks that 'T' is concrete. So here's what that syntax looks like. I'm going to start from scratch. After the 'impl' keyword, we're going to provide angle brackets. And this allows us to define generics for this implementation block, for this collection of methods. So here I'm going to define a generic called 'T'. Now because Rust sees this syntax here, when I provide my angle brackets and provide a 'T' here, this 'T' is not a concrete type. This 'T' now refers to this generic type 'T' that is declared here. So this syntax basically means define the following methods for all 'TreasureChest' structs for generic type 'T' and generic type 'T' can represent any type in existence. There is no concrete type here, right? 'T' is a different idea here from String. String is an actual realized type that we know at the moment of this 'impl' block declaration. 'T' here is a generic type that we cannot predict in advance. We are defining methods for a variety of 'TreasureChest' instances in the future that will have a variety of different data types for 'T', and we are saying these methods should exist on all of those future variations, regardless of the type of 'T'. That's why we need this introductory syntax to declare the generic 'T', so that Rust understands that this 'T' refers to a generic versus an actual concrete type coincidentally called 'T'. All right. Very complex and confusing concept to describe and explain, but hopefully that makes sense. So now we can define whatever methods we want in here. And these will exist on all of our structs below. So let's define a simple method. I'm going to call it 'capital_captain'. And this will take an immutable reference to the struct instance. So ampersand self ( &self ). I want it to return a String. And all this is going to do is take my captain's name, let's say 'self.captain', which I know is always going to be a String. So I can rely on that guarantee that that's a part of our struct that is not going to have a dynamic type. This will always be a String. So I can reference captain knowing full well what I can do with it. And the Rust compiler will approve of that. And let's take our 'captain' String and call the 'to_uppercase' method to return a new heap allocated string, but with all of the characters in uppercase. Very simple, stupid method, but it will demonstrate the point. So now we're going to be able to invoke this method on every single one of our structs below. So here is 'gold_chest' I can take 'gold_chest' and invoke 'capital_captain' on it. We do not need our Debug syntax, although it should work regardless. And we're going to get "Firebeard" right here that is coming from this String right here. But more importantly, notice that the 'capital_captain' method is supported automatically because these methods exist for all 'TreasureChest' structs of type 'T', where 'T' is a generic type that can be any type whatsoever. In this case, the type of 'T' is a string slice, a ref str ( &str ), but this will accommodate every type of 'T', so thus this method exists. As you might guess, we can do the exact same thing here on 'silver_chest' with a concrete type of String. I can call 'capital_captain' and clean this up, and we're going to get a capitalized version of "Bloodsail". . And finally, for our very last one of 'special_chest', I can take 'special_chest' and invoke the 'capital_captain' method. Once again, totally valid. We're going to get "Bootyplunder". And of course that's because 'capital_captain' is supported as a method on this 'TreasureChest' because of this 'impl' block definition that enables support for that method on structs of all kinds, or 'TreasureChest' structs of type 'T', where 'T' can be any generic type 'T', so it's a little bit more of a abstract definition of methods compared to these definitions right here, which are specifically limiting these methods to concrete types for the value 'T', right? String is the concrete type for 'T' that 'clean_treasure' will exist for. Here, by utilizing this additional bit of syntax, we told Rust that 'T' is not a literal type, not an actual concrete type like a String, or in this case a hypothetical struct named 'T', right? This is a generic 'T', and thus we support this method on all 'TreasureChest' instances. All right, hopefully that makes sense. And with that I will see you in the next lesson.
