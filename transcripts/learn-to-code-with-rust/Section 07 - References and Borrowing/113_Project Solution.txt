# Project Solution

So let's walk through a sample solution to the coding challenge in the previous lesson. We are modeling a road trip and we begin with a 'start_trip' function that should create and return a heap String of "The plan is..." So right here, let's define our 'start_trip' function. It's going to accept no parameters and it's going to return a heap String. And here in the body, I can use my 'from' function to create a new heap String with the content "The plan is..." with those three dots at the end. Make sure to omit the semicolon and that's all there is to it. So now, we can begin our road trip in 'main' by invoking the 'start_trip' function and saving its return value to a 'trip' variable. So 'let trip =' the return value of 'start_trip'. So what we want to do is pass the String to three separate functions that will mutate it without transferring ownership. So first up we begin with a 'visit_philadelphia' function that should concatenate the text "Philadelphia" to the end of the String. So right here we'll define 'visit_philadelphia'. And now we need a way to alter the content in this String that lives on the heap without losing ownership. So 'trip' should remain the owner of this String throughout the entire process. The way we do that is by defining our parameter here to be a mutable reference. We do not want to take ownership. That's the first thing. And we also do not want an immutable reference, because if it was immutable, we would not have permission to modify the String in the body of this function, and we intend to do so by concatenating the text. "Philadelphia". So right here I'll define a parameter. I'm just going to choose 'trip'. But I want to make sure there is no confusion here. This is a separate parameter name from this 'trip' variable. This is just my arbitrary name for the input that is going to flow into the 'visit_philadelphia' function. This is totally separate from what's going on in 'main'. I'm just calling it 'trip' because it makes sense. Now here, the most important thing, which is the type declaration, it is not going to be a String.Tthat would move ownership to the 'trip' parameter. And it's not going to be an immutable reference to a String, which would be this ( &String ). This would allow us to read the String and access it, but not allow us to modify it. We need a mutable reference to a String, which is this syntax: ampersand mut space string ( &mut String ). This function is not going to return anything. We do not need to return the String back because we are not receiving the String. We are receiving a mutable reference to the String. We are receiving the address that we can follow to the String, and once we get there, we are receiving the permission to modify what is at that address. 'trip' is going to remain the owner throughout the entire process. So in here I have a reference, but as we know, we can call a method the exact same way on a reference as we can on the value itself because Rust will automatically dereference. It will take the address that 'trip' represents and go to the original String location in the heap and give us the String so we can call 'push_str' just as if 'trip' were a String, when it is in fact a mutable reference. So here I'm going to 'push_str' and I'm going to concatenate the text "Philadelphia". Add my semicolon, and there you have it. So now in the body of main, we want to invoke the function in main. So I'm going to invoke 'visit_philadelphia' and rust-analyzer was a little bit too happy here. But that's exactly what we want. We want to pass in not 'trip' because that would be passing in the full String. Not an immutable reference to 'trip' because that does not satisfy what the function's expectations are here. Remember, the compiler is not just going to watch out for whether something is a reference or not. It's also going to find a mismatch if we are passing in a immutable reference where a mutable reference is expected and vice versa. So we want to pass in a mutable reference and that syntax is '&mut trip'. Now, in order to create a mutable reference to some original value, we need to make the original value, that owner, mutable as well. So this code will not work until we add 'mut' in front of 'trip'. But here you can see "cannot mutate immutable variable trip". The owner of 'trip' is not expecting this string to change and 'visit_philadelphia' will be changing it. And that's going to create problems, right? So Rust will not allow it until we add the 'mut' here and there we have it. All right. So then after we've done that we want to invoke 'push_str' on the String itself to concatenate the content " and " to the end including the spaces. So notice I can take 'trip' and I can call 'push_str' right here and concatenate separate content like " and  " and why is that important? Well, the mere fact that I am able to call 'push_str' on the 'trip' variable right here and have the compiler be satisfied, that is an indication that 'trip' is still the owner. It is still the owner after 'visit_philadelphia' runs because 'visit_philadelphia' never took ownership. We just passed in a mutable reference to that function. The 'trip' parameter here became the owner not of the String but of the mutable reference. Then 'trip' went out of scope and cleaned up its mutable reference that it was the owner of, the mutable reference. But this 'trip' variable in main is the owner of the String and remained the owner throughout this entire process, and thus we still have permission to use it to call 'push_str' on it here. All right, so the remaining two functions of 'visit_new_york' and 'visit_boston' are pretty much going to be the same thing. So we can quickly run through them. So we're going to define 'visit_new_york'. Once again, we're going to define a 'trip' parameter. The parameter names can totally be different in every function, because they're strictly the name that is limited to the function body. But I'm going to keep it consistent. Once again, we're going to pass in a mutable reference to a String, the exact same thing here in the body. We'll call 'push_str' on our reference, which will dereference and get us the String anyway, so that we can call 'push_str' on it and this time around pass in "New York". And same thing with 'visit_boston'. I'll just knock it out right here. 'trip' is going to be a mutable reference to a String. That gives us permission to call any methods that mutate, such as 'push_str' And here we can add on "Boston" to our trip. So now same thing is going to apply in our main function. We're going to call 'visit_new_york'. And we need to pass in a mutable reference to 'trip'. Once again, rust-analyzer is wonderful for everyday development, but not really helping me out as a teacher here because it's giving me the answer immediately. So not 'trip' not '&trip', but '&mut trip' because we want a mutable reference to the String from above. And then once again we're going to add the word. "  and " after that city. And then one more time we're going to invoke another function, which is 'visit_boston' passing in '&mut trip'. All three of these visit functions are expecting mutable references. None of them take ownership. All of them clean up their mutable references, and after all of this, 'trip' is still the owner. So we're done here. And then we want to invoke the function in main and concatenate a period to the end of the String to complete the sentence. So we are more than welcome to call 'push_str' again. But you may also recall a String supports a 'push' method, which is just for adding on a single character. A 'char' is a different data type in Rust. In this case, we just want a single period, so a character makes sense. And as a reminder, when declaring characters in Rust, we use single quotes and put a dot here. And if you did the 'push_str' approach, that's totally fine. It's just a different way to accomplish the same result. All right, so now we've built up our String, our trip, our itinerary, so to speak. The final step is to define a 'show_itinerary' function that will print out the final version of the String and do so without transferring ownership. And then we want to invoke that function and get this final output. So let's define 'show_itinerary'. And once again we want to not take ownership. So we're definitely going to borrow a reference. But this time around, we're just going to be printing out the String that we're borrowing. So we do not need to mutate it or modify it. So we do not want ownership, but we also do not need a mutable reference because an immutable reference is sufficient. So I can define a 'trip' parameter. We're not going to take ownership like this, but we are going to borrow a immutable reference. So now Rust can validate that we are not doing anything to modify the string in here. If we tried to do something like this, we're going to get an error, right. That's invalid because it's not mutable, but we are not trying to modify it. We're just trying to print it. So here I can println! and embed the value of 'trip'. Once again, it's a reference but that's totally fine because Rust is going to dereference and get the actual String heap content, the text that we want to output. And so now all that's left in main is to invoke 'show_itinerary' and now pass in, not 'trip', because that would transfer ownership and also be a mismatch in the type expectations right here. But we want a immutable reference to 'trip'. We create a reference with the borrow operator, the ampersand ( & ), and do trip. So if everything was done as expected, we should see this output. So "The plan is Philadelphia and New York and Boston." with a period at the end. That's exactly what we see here. And that's all there is to it. So now we have a beautiful and elegant way to avoid the duplication of the String data. We did not clone it. We just kept borrowing references to it, which basically means just asking for the address of where this String lives on the heap, so that these three functions could modify it one at a time in sequence, and this function could just read that content. All right. So that is a quick review of the reference concepts and ownership concepts that we learned about in this section of the course. And with that, I will see you in the next lesson.
