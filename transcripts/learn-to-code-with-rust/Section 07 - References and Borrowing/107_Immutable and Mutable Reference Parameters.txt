# Immutable and Mutable Reference Parameters

Let's pick up where we left off at the end of the previous section. Our problem is that the 'meal' parameter in the 'add_flour' function takes ownership of this String created in the main function. That requires us to return the String back to main to prevent its deallocation. This is not a technical problem, but it's an inconvenience because it expands the code. Every time we want to use this String, we have to keep returning it back to its original function and its original owner. So we're going to fix this problem in just a moment by changing the 'meal' parameter to be a mutable reference. But before we get there, I want to first show you an immutable reference parameter. So let's define a separate function called 'show_my_meal', and this function's purpose will be to simply print out our current meal to the screen. So I'll accept a 'meal' parameter once again set to a String, and here in the body I'll output some text like "Meal steps: " and interpolate the value of my 'meal' parameter. So if we were to leave our function like this, we would run into the exact same problem as we have in 'add_flour' So I'm going to switch things up here by changing the type of my meal' parameter to not be a String, but rather a String reference. Not the actual piece of text, but the address of that piece of text on the heap. So I'm going to add a ampersand ( & ) in front of my String. So that completely changes the type. 'meal' is not a String. 'meal' is a reference to a String, sometimes called a "ref String". Furthermore, we call this type of reference an immutable reference. An immutable reference has permission to read the value at the memory address, but it does not have permission to modify it or mutate it or change it in any way. So we can follow this reference to get to the original heap string. But we cannot, for example, add characters or remove characters. No modification. That's totally fine for this function, because all this function is doing is reading the String at that memory address that the 'meal' parameter represents. It's also important to know that 'meal' will still go out of scope at the end of the function, but 'meal' is not the owner of the original String. Rather, it will be the owner of this string reference. You can think of this reference like an address written on a piece of paper. 'meal' the parameter will clean up that piece of paper storing the address, but throughout the process, 'current_meal' will stay the owner back in the main function. So now back in main, let's invoke 'show_my_meal' and you'll notice that if I simply write 'current_meal' which is my String, we're going to have a compiler error. This is a type mismatch. Why is that the case? Because 'current_meal' is a String while our parameter expects a string reference, right? It says here: expected this type: a &tSring This function expects a memory address, not an actual String itself. So in addition to changing our parameter here, we also need to update our argument and provide the ampersand ( & ) here as well. So now we are passing in a reference. We are not passing in the String, and thus we don't have to worry about ownership, and thus we do not have to return back the String from our function. So now knowing this, we can update 'add_flour' to follow the same formula. The only difference is our parameter here will be a mutable reference. A mutable reference is still an address. However, it has permission to use that address to get the value and then to update the value as well. It's still a reference and it still represents a borrow because ownership is not going to move once we update this function. Ownership is not going to move away from the 'current_meal' owner. But, in addition to borrowing a reference, we're also going to borrow a mutable reference, which will give the function permission to update that text on the heap, and we need that because we are calling 'push_str' right here. So here's what we're going to do. First up let's remove the 'mut' keyword here and then change our parameter type to be a string reference. So here back in main we can immediately update this to be a reference to our 'current_meal' string. All right. Now there's a couple of things that are going to change here. The first thing is we need to also make this reference mutable. So after my ampersand I'm going to add my 'mut' keyword and then a space. So just to clarify: this is a mutable reference to a String. There are 4 possible options for how we can define this parameter. And I want to quickly take a moment to describe all of them. So our first option is defining it like this. This means we are defining a parameter that will take full ownership of the String, and does not have permission to modify it. Our second option, which we just had, is this. This means the 'meal' parameter will still take ownership over the String. In addition, it has permission to modify it. Our third option, which we see in 'show_my_meal', looks like this. This means 'meal' is no longer a String, but rather a reference to a String (&String). It is fundamentally a memory address, but it does not have permission to update the value at that memory address. And finally, our fourth option, which we have right now, is a mutable reference to a String (&mut String). Right. And that means we are not taking ownership of the String. We are receiving a completely different type entirely, a reference and a mutable reference, which means we have permission to go to that memory address, get that String, and update it however we'd like. So that allows us to keep this code of 'push_str'. Now I can remove my return type annotation because I no longer need to return 'meal'. I can also remove it right here because I no longer will be returning it. I also want to show you very quickly that if I use this syntax right here. We're going to see an error because we do not have permission to mutate the string at that memory address. 'meal' is fundamentally a reference. When we call a method on it, Rust knows that we don't mean call a method on the address, but rather call a method on the String at the address. It dereferences the reference in the exact same way as it does so right here. And that's a good design, because no matter whether 'meal' is a reference or the value itself, we can call the same method and have the code continue to work. But we cannot mutate the string unless we make the reference mutable. And that's why we need that code. Now, back in the body of main, we no longer need to assign a return value back. In fact, we're not getting a return value technically. Technically, it's a unit, an empty tuple. But let's not worry about that. I can remove this section of the code, and you'll notice we have one more violation remaining. So what's wrong here? Let's take a look. The parameter type expects a mutable reference to a String. Instead, it's receiving an immutable reference to a String, right? So it's important not just that we match the types as far as providing a reference when one is expected, but also that that reference has the permissions that are needed. So this is passing in an immutable reference to 'current_meal', to that String. That's totally fine for 'show_my_meal', but it's not fine for 'add_flour' because that is not a type match with what we declared here. So the syntax to declare a mutable reference is the exact same: '&mut', and then the actual value itself from which the reference will be created. The original value that we are borrowing by getting its memory address. That's what we're passing in here. And now notice no return value and our code works as expected. So we're going to create a new String, add on "Add flour", then pass in a reference to that String right here to print out "Meal steps: Add flour". And that's all there is to it. All right. So that is how you define function parameters that can be either immutable or mutable references.
