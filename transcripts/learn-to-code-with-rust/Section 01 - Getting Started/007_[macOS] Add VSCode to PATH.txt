# [macOS] Add VSCode to PATH

There's one more thing we need to do to finalize our setup in VSCode, and it involves a feature called the Command Palette. To bring up the Command Palette, you can use the following keyboard shortcut. It's the Command key along with Shift and P. The Command Palette is like a VSCode-specific terminal. We can use it to issue text commands to the editor. We use our arrow keys to navigate the commands and then the Enter key to execute. We can also use the search bar up top to filter the commands. For example, I can search for "preferences" and here are a whole bunch of commands related to preferences. I can filter this down further by adding something like "color" and this command right here. Preferences: Color Theme allows us to set the color theme of the editor so I can press Enter here. Once again, use my arrow keys to navigate my options, and then press Enter to confirm. So that's a quick introduction to the Command Palette. We just want to execute one command in there. So once again to bring it up it is Command + Shift + P. And we want to search for this command: "shell command" And this very first one: "Shell Command: Install 'code' command in PATH' is what we want. So press Enter. It's going to give you this nice confirmation modal. You can press OK. And that is it. So what did we just do. What that just did is it added VSCode to our Terminal. It allows us to use a new Terminal command called "code", c-o-d-e in all lowercase to open VSCode from the Terminal. Furthermore, if you are in a specific directory in your terminal and you want to open that folder, specifically the folder that you are within, you can open that with the command code followed by a space and a dot. The dot means the current directory, so code followed by dot means open VSCode in the current directory and when vs VSCode opens, it's going to show you all the files and folders within that directory. So your Rust projects are going to be regular folders on your computer that consists of multiple files. And you can navigate into any of them and press "code ." to open it specifically in VSCode. The Terminal is not the only way to accomplish this. For example, in VSCode you can go up to get to the main menu. Click File and click Open Folder and that will allow you to choose the folder using the regular Mac Finder tool. But now we have access to the same feature from your Terminal. Later on in this section, you're going to download all of the course materials, and they're going to consist of individual Rust projects, each one of which is going to live within a separate folder. So now you can use your Terminal application to navigate to the correct folder for a given section. And then again just use "code ." to open VSCode within that directory. And that should immediately open the project. All right. That's all set up. We don't have to worry about this anymore or at any point moving forward. And with that, I'll see you in the next lesson.
