# Hello World

Let's begin this lesson by opening the main.rs file in the src/ directory and deleting all of its code. We're going to rewrite this code from scratch in this lesson, so we can discuss what it's doing in greater detail. So as I mentioned, our Rust code lives within files with a .rs extension that is short forRrust. So this file main.rs is an example. Rust code within these files consists of functions. A function is a procedure. It's a collection of steps or sequential instructions for the program to execute. I often like to compare a function with a cooking recip. in the real world. A cooking recipe is a sequence of instructions, right? Step one: mix the egg and the flour. Step two: add the sugar, etc. It's the same idea here, where a function defines a recipe that Rust can run in order. A Rust program will typically consist of many functions. Functions exist for the purpose of organization. Ideally, each function, each recipe, has its own small responsibility. For example, if we were writing a Rust program that read in a file from our hard drive, parsed its contents and wrote something to a new file, we could split that program into three functions: one to read, one to parse, and one to write. There's no right answer as far as how many functions your program needs. It all depends on how you, the developer, want to organize your code and break up the tasks that the program will be doing. Every Rust function begins with the function keyword, which is abbreviated as "fn". So "fn" tells Rust that we are beginning a function declaration, the definition of a recipe. Afterwards, after fn, we add a space and then declare the function's name. Every function will have a distinct name which allows us to distinguish between them. Now every Rust program must have a function called main in all lowercase. So I'm going to write out that function name, right? It's important to note that Rust cares about case sensitivity, so this function name must be written in all lowercase. A main function with a capital M is a different idea. The main function is the entry point to the program. Rust will automatically execute the main function frist. It's going to look in our code for the main function definition and run that automatically. The technical word for executing a function is called calling the function or invoking the function. So we can say something like: When the program runs, Rust will invoke the main function. After the function name, we declare a pair of parentheses, both opening and closing. That's going to be Shift + 9, then Shift + 0 on your keyboard. Now you'll notice as soon as I add my opening parentheses, VSCode and rust-analyzer will automatically provide the closing parentheses. This is expected behavior, and one example of how the text editor makes it easier to write code. You can think of it like a nice autocomplete feature where it saves us a key press. Alright. The parentheses are where we declare what are called parameters. A parameter is an input for a function. An input is something we want to pass into the function for it to use. If you think back to the cooking analogy, we can think of parameters as the ingredients to the recipe. The recipe consists of some sequential steps to follow, but it may require some ingredients or inputs to work with for these steps to run against. And that's what the parameters represent. Now, a function does not need to have parameters. For example, our main function in this scenario will not have any parameters, but the parentheses is where we declare those expected inputs and they are required even if our function will accept no inputs. Afterwards we're going to declare a pair of curly braces. And I'm going to add a space after the closing parentheses. By curly braces, I mean the characters to the right of the "P" on your keyboard, and I have to hold Shift to access them. They look like this, right? This is the opening curly brace. And this is the closing curly brace. Right? So this creates what is called a block. You can think of a block as the section between an opening and closing curly braces. It's a section of code. We can add a few line breaks here. It will make no difference. And the area between the opening and closing curly brace is the code that is going to belong to the main function. This is the code that's going to run when the main function is invoked. We call this section between the opening and curly braces a block. So the function's body, which means its content will be written within these curly braces within this block. And that's how Rust understands that it belongs to the main function. This is where we put the steps belonging to the main recipe. As far as formatting, the common convention you'll see is what we have here. We're often going to place the opening curly brace on the same line as the function name. Then we're going to write our code across several lines. And then we're going to close the function or close the block, provide the closing brace on its own separate line inside the body of the main function. We're going to write one line of code that's going to output text to the screen. It's a long held tradition in programming, you can even say a rite of passage, that the first piece of text that we output is  "Hello World". So we're going to honor that tradition and have our first Rust program simply output the text "Hello World". Now for all of this content inside our main function, the Rust convention is to indent your line of code 4 spaces. So one, two three, four. All of our code will be indented 4 spaces in. And that's simply exists for the benefit of readability. Now on my VSCode, you can see down below here I have Spaces: 4. And that indicates that VSCode sees a tab as equivalent to being 4 spaces. So here I can delete my spaces and press the Tab key to accomplish the same effect, the equivalent of 4 spaces. All right. So in order to output text we'll use something called a macro. A macro is somewhat comparable to a function. It's technically different behind the scenes, but for now we don't have to worry about the differences. Think of a macro just like a function. It's a recipe. It's a procedure that we can utilize. It's somebody else's code that's already been written that's going to be responsible for something else. In this case, the technical responsibility of outputting text to the screen. So our macro is called println! "p r i n t  l n". Line is abbreviated to "ln". And we also have an exclamation mark at the end. You can think of that as a regular character that is part of the macro's name. It's kind of a distinguishing visual feature that reminds us it's a macro, but from Rust's perspective, there is no difference between alphabetic characters versus the exclamation mark. It's just a character that is part of the macro's name. All right, so all macros are going to have the exclamation mark in their name. We are referencing this macro, but we need to provide a pair of parentheses to actually run it. You can think of this as invoking a function. It's very similar. This is where we actually take a recipe, in this case, println! and we cook it. In other words, we tell it follow the instructions in the println! macro and actually execute it. Output this content to the screen. So inside here we need to provide a piece of text. And the way we declare a piece of text is with a pair of opening and closing double quotes. As soon as I add my first one, rust-analyzer will automatically auto complete the closing one. Rust will understand this piece of text by the presence of the double quotes, the start and the end. Otherwise, it would get confused and think that the characters we write in here are some part of Rust syntax. So we need this to create a piece of text. In programming, the technical term for a piece of text is called a string. So when we use these double quotes, we create a string. In here, I'm going to write my content. Hello World All right. You can see that a string can consist of alphabetic characters, but it can also consist of other characters like spaces or digits or symbols. Like, for example, I can pretend I'm really excited and add an exclamation point at the end. That's a valid character to include within a string. Then, Rust sees the closing double quote and knows that the piece of text has concluded, that it's been terminated, and everything moving forward is a regular part of the Rust syntax. Okay, there's one more thing we need to do, and that is we need to terminate the line. We basically need to tell Rust that this line is done, that we've expressed a full thought, a full instruction, a full command, and the way we do that in Rust is by adding a semicolon to the end of a line. Semicolons terminate the line statement. You can think of it like the period at the end of a sentence, right? So with that, we are ready. The next thing we need to do is to save the file. So on a Mac I can use the shortcut Command + S. And on a Windows machine I can use Ctrl + S. Notice that this white little circle in our tab for the file is present. This circle indicates that the file is unsaved. If you have not saved it, then these changes will not have registered because it won't be saved to the file's content on your hard drive. So before you run any program, make sure that you save and the circle goes away because now these changes are in the file. Okay, so finally, the last remaining step is to actually run the program. And luckily the rust-analyzer extension offers this nice Run button above. Now this button is actually going to do a couple of things. The first is it's going to compile the program. It's going to parse our source code and compile it or translate it to the actual executable program on our hard drive. It's a totally separate file. Right? That file can run even if your computer does not have Rust installed. It's the final output, the final program. So the Run button here will create that executable. And then it's going to automatically run it. So compilation and execution. It's technically a two step process, but the Run button here will do it all with one click. So I'm going to press Run. We're going to see a panel pop up. Note that your panel may pop up on a different part of the screen, like the right side or the left side. Don't worry about it. Those differences don't matter. Don't worry about this content right here. Here is the actual core part of our program. Here is our actual executable program outputting the text Hello World, which is exactly what we defined using our println! macro. So this is a valid working Rust program that is reflective of the code that we wrote. Right? So if I want to update this and celebrate and say "Hello World! I am a Rust developer". The process is the same. I'm going to update my code within the main function, save my file, press the Run button, and now we see that this Rust program and its resulting executable output a different piece of text to the screen, which includes the additional content in my string. All right. So quite a lot of concepts explored in this section. But this is the heart of it. This is a valid working Rust program. Every Rust program will include a main function. A function is a procedure of steps in order. In this case, our procedure only has a single step or instruction, and that instruction tells Rust to output content to the screen using the println! macro. The word print simply means output to something. And we'll talk about where the line comes in in a future lesson. But this means output some content to the screen. The content is specified within the parentheses, and we have to tell Rust that it's a piece of text, a string with a pair of double quotes. And finally, we need to terminate the line with a semicolon. All right, I know there's a lot of concepts to explore here. It's kind of hard to introduce Rust or any program like this from the start, because there's a lot of these ideas that are coming at you all at once. But rest assured, every Rust program we're going to write will involve a main function and involve the syntax, so you'll have plenty of chances to practice it throughout the course. All right, that's all there is to cover in this lesson. So I will see you in the next one.
