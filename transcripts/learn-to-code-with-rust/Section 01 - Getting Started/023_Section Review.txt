# Section Review

Welcome to the first section review of the course. In these videos, we'll quickly summarize all of the topics that we introduced in the current section. So if you feel that you have a good understanding of the content, you can feel free to skip these lessons. They are completely optional. So in this introductory Getting Started section, we installed Rust on our computer. Rust is a programming language, which means it's a tool that developers use to write instructions for the computer to execute. A programmer writes what is called source code in Rust, and what source code looks like will vary between different programming languages. Unfortunately, the computer cannot understand the source code that we write, so we need to use the Rust compiler to solve that problem. The Rust compiler is a tool that translates your source code into machine code, into a final program that the computer can understand and execute. When we installed Rust, that included the Rust compiler, which is rustc in your command line. That is your Terminal or PowerShell. But it also installed some supplementary tools like the cargo project manager and utility tools like rustup and rustfmt. rustup helps with things like updating and uninstalling Rust while rustfmt, which is short for format, helps us to visually style our code to a consistent community standard. We also introduced this notion of dependencies. A dependency is a requirement that a piece of software needs to run. Basically means "Software A requires or depends on software B to be able to work". So for example, for macOS users, we needed to install the Xcode Command Line Tools as a dependency. And for Windows users, we needed to install Visual Studio and the C++ tools as a dependency to enable the installation of Rust. Now, specifically within Rust, we also have the idea of dependencies. For example, we have this idea of library crates. A library crate is a collection of Rust code that does not have a main function. Rather, it is code that exists to be reused in other projects. So the code that we write, which comes in the form of binary crates, they can pull in these library crates as dependencies. Other code that we rely on in order to write code of our own. All right. So let's talk about cargo. Cargo is a command line tool that assists with various aspects of Rust project management. For example, cargo new will initialize a new Cargo project. It's going to create the src/ directory, the main.rs file. It's going to give you a Cargo.toml file, basically a nice starter template for getting started working with Rust. The cargo build command will compile the executable, build the final program from your source code. We can also use the cargo run command from our top level project. That will both compile the executable and then immediately run it. Execute the program so we see its output in the terminal. We also have cargo check which checks the code for violations without compiling it. So that can be a little bit of a quicker command to run. If you just want to verify that there are no compiler errors in your code. And finally, cargofmt styles or formats your project code again, according to those community conventions. It doesn't change the structure or logic of the code, only how it looks, only how it visually appears inside the source code. All right, so Rust code consists of functions. A function is a collection of steps to execute in order. Every Rust program must have at least a main function, that is its name, and the program will automatically run the main function when it launches. So the main function is the entry point. It's the starting point of execution. But we can add more than the main function. And we're going to take a look at programs with multiple functions later on in the course. Now, every function consists of the same pieces. There's always the fn keyword which is short for function, then a space and the function name which is a unique identifier, and that is usually written in snake_case, which is when we keep all of the words lowercase but separate every subsequent two with underscores. That is the common community convention. So main only has one word, so it's all lowercase. But later on we'll see functions with multiple words in their name. And they're going to follow this convention. We then have parentheses. And that is where we can accept inputs. Inputs are values or pieces of data that are going to flow into the function when it executes. So we compared a function to a recipe and the inputs to the ingredients. They are the requirements that the function is going to rely on to execute its logic. Even if you accept no inputs like we did in all of our main functions, we still need the parentheses. But that is where those inputs are going to go. We're going to talk more about that later on. Then we add our curly braces, which mark the beginning and end of the function's body. That is the function's procedural logic. All of the lines of code between the opening and closing curly brace make up the logic, or the procedure of what the function is going to do when it is invoked or executed. All right, so what we first did in our main function is we simply output text to the screen: "Hello world!" In order to accomplish that, we relied on a built in macro called println! A macro is very similar to a function. It's a collection of steps that accomplishes some goal, in this case, this macro is built into the language because many different Rust developers want to output text to the screen. That's what println! does. All macros end with an exclamation mark. There are many more macros that we're going to explore throughout the course. They all end with that symbol. Now, writing println! by itself is not enough. That is almost like writing just a recipe name. We have to use parentheses both after macros and after functions to invoke it, which means to actually run or execute the macro at that point of the program's execution. So the println! macro accepts parentheses, and in there we provide a string argument. An argument is just an input. Once again it's something flowing in to the function or in this case the println! macro. So the println! macro needs to know what content to actually output to the screen. And that is what our argument represents. So it accepts a string. Remember, a string is just the technical programmer term for a piece of text, and that piece of text can consist of alphabetic characters, but also things like digits, symbols, so it can include question marks, dots, brackets, dollar signs, etc. it is just a piece of text. It begins with a double quote, and then it ends or terminates with a double quote. Anything in between those is treated as the content of the string. Then, we close the double quote for the string. We close the parentheses to finish up the macro, and then we end the line or the command with a semicolon. Once again, that is like the period at the end of the sentence. As you watch all of the videos in the course, be mindful of whenever I add a semicolon to the end of the line, because it's highly likely that it is required. All right, let's also talk about comments. Two slashes, two forward slashes, create a comment, which is just a line ignored by the compiler that allows us as developers to write content besides code in our Rust files. Right. So we can leave metadata, documentation, information, descriptions, jokes, whatever you want. As soon as the compiler sees the slashes, it ignores everything that immediately follows. So you can have a line that consists of a comment, or you can also add the comment directly at the end of a valid line of code, and the line of code will be executed. Anything after these slashes will be treated as a comment to ignore. We can also create multi-line comments by using that /* syntax, then */ again. It's the inverse. And that just allows us to have a block of text should we want to leave something like that in our code. All right. That brings us to compilation. So we have our program written. It has our commands. It has our main function. It has some comments. Now we need to actually create the executable. So the Rust compiler is responsible for translating your source code into machine code that your computer and its architecture can understand. There are two models or modes in which we can build the executable: debug and release. If we run cargo run by default, it's going to build it in debug mode. Same thing with pressing the Run button from rust-analyzer in VS code. That runs cargo run behind the scenes with debug mode. Debug is for development, and it tends to lead to a larger executable because it's storing additional metadata. Then, when you're happy with your code and you want to build the final product for the end user, you add that additional flag of --release which builds it for the final user. That executable is going to be smaller in size, which means it requires less memory and is going to run faster. But again, it's missing some of those additional debug pieces of information that helps us identify errors in the code. That's all there is to cover in this section. I hope you had fun. And, in the very next one, we'll continue our exploration of the basics of Rust. Looking forward to it and I will see you in the next lesson.
