# Project Solution

So let's walk through a sample solution to the coding challenge. In the previous lesson, I have my Terminal open and the first thing I want to do is use the Cargo tool to create a new Rust project. For that, I'm going to execute "cargo new", followed by a space and my project name, and that is going to be about_me. That is going to create that about_me directory in the current folder. I can navigate into about-me and then open it in VSCode by using "code ." . means the current directory. And I'm going to bring over VSCode from my other window. I'm also going to open up my main.rs file in the src/ directory. That's where I'm going to be writing all of my code, and I'm also going to copy over the comments holding the instructions for the assignment. And there we go. Okay, so we created the project. The next thing is to use the println! macro to output three sentences about yourself. This is just outputting text to the screen. So, in the main function, which automatically runs when the executable runs, this is where I can place that procedural logic to output text to the screen. So of course you can customize this for your own solution. But I'm going to write "My name is Boris". "I live in the New York City area" and "I love teaching". So I'm using the println! macro. I'm providing my parentheses. Inside, I am providing the text to output. Then I am providing the closing parentheses to terminate the macro and then providing the semicolon to terminate that line's statement. Once again, that semicolon is important, and it acts like the period at the end of a sentence. So now that I have this, I can of course run this by pressing the Run button. And we can see the output here. But we also want to practice using the available Terminal tools to accomplish the same result. So here I can press the + sign to open up a fresh Terminal instance directly within VSCode. An alternative way to accomplish this is to go up to your menu right here. Click Terminal and New Terminal. And this is equivalent to the Terminal on your Mac or your PowerShell on your Windows operating system. It's just open directly within VSCode. All right. So there's a couple different ways that we can build the project, run the project, or accomplish both in a single step. So let's begin by using the "rustc" Rust compiler. So I'm going to navigate into my src/ directory using the "cd" command. And it is in here that I have my main.rs file. So the first thing is I can use my Rust compiler which is "rustc". It's an actual program available in my Terminal, in my command line application. And I can follow up rustc with the name of the Rust file with a .rs extension that I want to compile into a binary executable, into actual machine code. So once I provide my file which is main.rs, and execute that command in the same directory, I'm going to have that main file and that is fully separate or decoupled from Rust. In other words, if I were to uninstall Rust and the compiler from my operating system, this main file would still work because it's now separate from Rust. It has instructions that the computer can execute that is going to accomplish what I wrote in the source code right here. So here I can provide ./ to access the current directory. It's going to be the inverse on a Windows computer. You're going to do a \. And I want to target my main file. On a Windows computer, this is going to be main.exe But same idea. I can execute that. And there is the content from my source code. So that is our first option. It is using the Rust compiler directly file by file. This is not the most common solution, but it does work file by file. The next solution is using the cargo tool. So as I scroll up here we can see. Let's compile the main file, then manually run the executable. That's what we did. The next step is compile the project with the cargo tool. Then manually run the executable. So, to accomplish that, what I can do at the top level of my project directory is I can run cargo build. That will build the Rust project, and the search for the initial file that kickstarts that project is going to be src/main.rs So cargo is going to do the exact same thing that we just did. It's going to locate main.rs and compile that. But it's going to place its output, its binary executable, into the target folder. So if I go into target, then I go into debug. In here, I'm going to find the about_me file. If I execute that, it's going to produce the exact same output. As a reminder, the reason this file is called about_me is that name comes from the Cargo.toml file at the top of your project directory. Here is where that name is specified, which is also the name that we provided to the cargo new command, and also the name of the folder that it generated, but it's not basing the name of the executable on main.rs. It's basing it on the name specified right here inside the Cargo.toml file. And that's what it's placing inside the target/debug folder. We are also building the program in debug mode. As a reminder when you want to build it for production, for the final release, you have to attach that additional flag to build it in release mode, which attaches less metadata for the purposes of developer debugging and leads to a smaller executable. All right. So that is cargo build. Of course, the next option we have is cargo run, which will automatically build the executable from the source code and then automatically execute it as well. So it skips us the step of having to manually navigate into the target/debug directory. As another quick reminder, if you ever want to clean up all of the executables to ensure you compile from a empty slate, from a blank canvas, you can execute cargo clean and that is going to remove that target folder. Once again, I can execute cargo run and we'll see the target folder appear again. And once again the compiler is going to try to minimize its total work. So if it sees an executable and also sees that you have not changed the source code in any way, it's not going to bother recompiling. It's just going to say "I already have the executable, there's no need to recompile". So here you can see this process took 0.12 seconds. And if I execute cargo run again you'll notice it says 0.0 seconds. Because it didn't recompile. It just relied on the existing executable that was already there. So that is cargo run which both builds and executes in comparison to cargo build which just builds. There's another option available here. And that is going to be cargo check. That does not build the executable. It just checks the source code for violations for things like syntax errors and any kind of mistakes that prevent the compiler from being able to compile your source code into machine code. So you'll notice it finishes with no listed errors. That means there are no mistakes in our source code. And the reason you might want to use cargo check is because checking the code for violations without building is going to generally be faster. We're not going to see it in a project with just one file. But as you can imagine, as our projects get bigger and involve multiple folders and files, this can be faster in general than cargo run or cargo build. All right. The next step is to add a comment at the top of our source code explaining how to compile the program for new Rust developers. I'm just going to do it really quickly here. The first option for our comments is the two forward slashes ( // ). Another way we can automatically add that is by using Cmd + / on a Mac, or Ctrl + / on a Windows. That will automatically provide the language's command syntax, regardless of what language you are working in. In VSCode. So here I can write something like "To compile and execute, run cargo run from the Terminal or click the Run button". Rust Analyzer must be installed. So notice that for every line that I would like to be a comment, I am beginning it with the two forward slashes. The alternative approach here is to begin with the forward slash and the star or the asterisk, and then close off with the inverse, the asterisk followed by the forward slash, and anything between the starting symbols and the ending symbols will be interpreted as a comment, and that allows us to skip the forward slashes and just write plain English. All right, so that looks good. Next step, let's add some spaces and line breaks to the code so that it is formatted in an ugly manner, and then clean it up with the cargo fmt or format command. So once again, I can just add a couple really nasty indentations here. This obviously is not easy to read. And then what I'm going to do is just run cargo fmt. That is going to go through our complete cargo project, which includes all of the Rust source code files. Right now, we only have one, but this would apply to all of them in a multi-file project. And it's going to format them according to these nice, standardized, aesthetically pleasing Rust community conventions. So now you can see everything is looking nice and pretty. And finally we want to replace the println! macro with an alternative macro called print! It's the exact same synta:, the macro name including the exclamation mark, then the parentheses. And we're going to remove it here and here. And we're going to observe the differences between these two macros. print! also outputs text to the screen. The difference is it does not add a line break to the end of the output. So you can see here, instead of starting on the next line, it just picks up where the previous line left off. So when we print "My name is Boris", it terminates right here and it doesn't add that line break to the end. So "I live in the New York City area", when it is printed, resumes from where the previous print statement concluded. You can even see after "I love teaching", the Terminal starts picking up with its output at the end of that output rather than on the new line, because the output from this print! does not force a new line from which the Terminal can pick up. So it just picks up at the end of the previous output. But it's just another macro. It's another command or prebuilt functionality that is built into Rust, and another one that is dedicated to outputting text to the screen. It's just that print! does not include a line break. All right. So this is our first coding challenge in the course. Hopefully it was a good review of many of the concepts that we introduced in this section. And, with that, I will see you in the next lesson.
