# Compiling and Running Rust File from the Terminal

In the previous lesson, we used the rust-analyzer extension's Run button to compile our executable and run it immediately. In this lesson, I want to show you this two-step process behind the scenes so we have a better understanding of what this Run button automates for us. So here in this output, I can click this plus button right here to open up a fresh Terminal in VSCode. An alternative way to do this is to go to your main menu up top, click Terminal, and then New Terminal. So this is the exact same Terminal program that we have installed on our respective operating system. So on a Windows computer, this might be PowerShell. On a macOS, it might be the Terminal, but it's basically your regular command line application for your operating system but running directly within VSCode. So when we installed Rust, we installed the rust compiler and we are able to run it as a command line program. So here, it's going to be "rustc" -- that is the command line program for the Rust compiler. "rust" for Rust. And then the letter "c" is a shortcut for compiler. So we use this command, this "rustc" program to create an executable program from a Rust file with a .rs extension. That executable, sometimes called the binary executable, is the file that contains what's called the compiled machine code, which is the instructions that can be directly executed by your computer's processor. Your computer does not understand this. This is our source code. This is the code that we understand as developers that is human readable, the machine code that is produced by the compiler in comparison, will look gibberish to us humans, but it holds instructions that the computer can understand and execute. So this Terminal should have opened directly in the same directory as our hello_world project. This is this top level folder right here. So the first thing I want to do is navigate into the src/ directory or source. And it's here that I have my actual main.rs file. So we're going to use "rustc". The Rust compiler, then a space. And then we're going to write our full file name. The file name represents the file that holds our source code. So we're going to write main.rs This file right here is what we're referencing. When I execute this command, the Rust compiler will read the file and create the executable. The compiler will parse our content and create a file for the computer. You should see that file appear within the src/ directory. It's important to note that this file name will differ between windows and macOS computers. On macOS, the file is simply going to have the exact same name as the source file, but with no extension. So our source file was called main.rs So our executable program file is simply called main. On a Windows computer, it should be the exact same thing, except the executable file will have an extension of ".exe". ".exe" is short for executable. It means a file that can be executed or run. It's the exact same idea. It's just some different terminology between the two operating systems. Now notice we have not run the program yet. We just created the executable file using the Rust compiler. So now let's go ahead and run it. Once again, the way that we're going to reference the file is going to be a little bit different between operating systems. On a Mac machine, we're going to do ./ which means the current directory. And then within the current directory which is src/, we're going to reference our program that we want to run which is main. This is not the same thing as main.rs. main.rs is our Rust file. We are referencing the main file which is right here. And this should run the program and output the exact same content that you saw in the previous lesson. If you're on a Windows computer, it's a similar idea, but a different syntax. In order to specify the current folder, you're going to do a dot and then a backslash like this: .\ And then you're going to write your file name which is main.exe Execute that command and you should see the exact same result which is the output of "Hello world! I am a Rust developer." On Windows machines, you might also see a generated PDB file. You can ignore it for now. It's a file that Rust creates for debugging. It has no value for us right now. I also want to clarify that the program that we created here, main, is not going to run on any computer in the world. By default, the Rust compiler will create the executable for the type of system, or the type of computer architecture that the compiler is running on. So for example, for me on my MacBook, the Rust compiler will build the program for my specific computer with its specific architecture and its specific type of chip. On your Windows computer, it's going to do the same thing. It's going to build an executable for your computer with your type of architecture. So if I were to take this main file on my Mac machine and try to run it on a Windows computer, it would not work and vice versa. But if I were to take this file and transfer it to another Mac computer with the same architecture, it would be able to be executed. And furthermore, that computer would not need Rust installed. Once you have your executable program, it's fully decoupled from Rust. The original file, main.rs, was only needed so that the compiler could translate these Rust instructions to the equivalent machine code. Once the machine code exists, it's totally separate from Rust. It exists as instructions for your computer. The same thing is true if we were using any other compiled language. For example, if we were writing using the C programming language, the compiler would take the C program and compile it to an equivalent executable, once again, for the computer to be able to read and understand. The language exists to differentiate in the syntax that we developers want to write, and then the compiler is what actually translates that to machine code that your computer and its architecture can understand. So just to show you this: you don't need to emulate this or follow along, but on a macOS computer, I can run a Terminal command called "file" and follow it up with the file that I want to get an analysis of. So for me, it's going to be the main executable. It's actually going to tell me the kind of architecture this file is compiled or built for. You can see for me it's this 64-bit computer running on an ARM 64, which is a type of CPU, a type of computer chip. This is going to vary between computers. And this is going to vary between computers as well. But the Rest compiler will build it for the current machine. Right. It is possible to customize the Rust compiler settings to produce an executable for any kind of system architecture, but by default it's going to create one for your specific system. All right, that's all there is to cover in this lesson. So I will see you in the next one.
