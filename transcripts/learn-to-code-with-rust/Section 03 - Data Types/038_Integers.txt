# Integers

So now, let's translate the theory from the previous lesson into code within our text editor. I'll begin by declaring a variable. And I'm going to call it eight_bit. And I'm going to assign it a value of -112. By default, Rust will infer the type of an integer to be an i32. We can see rust-analyzer pops up with this helpful hint that Rust is seeing this value as an i32. So if we instead want to declare this variable as storing an 8-bit integer, we have to manually annotate the type. We have to overwrite Rust's default. The way we do so is right after the variable name. We provide a colon, then a space, and then the type. So here, I'll declare a type of an i8 which represents a signed integer occupying only 8 bits or 1 byte in memory. This is a quarter of the size of an i32. So while we benefit from using less memory, we also have a smaller range of values that this i8 can store or represent. This variable can only hold a value between -128 and positive 127. So if I assign an invalid value like -210, the compiler will not compile the program. We could see right here this literal value of -210 is outside of the range for an i8. In fact, it will tell us it does not fit into the type whose range of values is -128 up to positive 127. Right. So our specified value does not fit within the size limits because 8 bits is not enough space, enough memory, in order to represent or model a number this large. The program will also fail to compile if we assign an invalid value for a given type. For example, let's say I call my variable eight_bit_unsigned. Not that the name is super important. What's more important is that I'll annotate the type here as a u8. This is an unsigned integer, so it's still going to occupy 8 bits or 1 byte, which is equivalent to an i8 as far as memory consumption. But now, this can only be 0 or a positive value, and it must be a positive value between 0 and 255. So if I try to assign a larger value like 400, this is going to be invalid because it does not fit within this range. And then if I assign a negative value, any negative value whatsoever like -15, this is also invalid because an unsigned integer does not support negative values. So here are a few more examples of various integer type declarations. I can declare a variable called sixteen_bit_signed. And once again, if I want to explicitly annotate the type, I need a colon followed by a space and then my desired type. So here I'll choose an i16, which has 16 bits or 2 bytes of memory. So the available memory is twice as large as an i8, and thus the range of numbers that this can store is much greater. A 16-bit signed integer, an i16, can store any value between -32,768 and positive 32,767. So the range or scope is much wider. So I can do something like, let's say, -32,500, and that's going to be just fine. And the same rules apply if I want, say a 16-bit unsigned integer. The variable name is entirely up to us. This is the important part. I'm declaring this as au16, which again is 16 bits or 2 bytes. But I no longer have to worry about negative values. So the range that I can go into the positive direction is much larger. For example, I can write 64,000 right here and that's totally valid. Notice if I try to assign 64,000 to this i16, that is invalid because it is outside of its range. We gain the benefit of negative numbers, but the range we can go into the positive direction is halved, right? Because the other half is now the equivalent on the negative side of the zero. Right? So I'll return this back to a working solution. So next up let's declare a few more. I'll declare a 32-bit signed variable. And Rust's default integer type will be an i32. So while I can assign the type explicitly, I don't have to because it's going to be the default. So for example, here I can assign a really large number. Now that I have 32-bits or 4 bytes of capacity, for example, I can model -2,147,483,648. And you can see large numbers like this are supported. They would not be possible with either an i16 or an i8. And here is a 32-bit unsigned integer. Same rules apply. Colon, space, and I'll declare this as an unsigned integer. We now can go twice as far into the positive direction, but what we lose is the ability to model a negative number. So it all depends on what your program needs. So here I can do something like 4,294,967,295. And you can see that is totally valid. There are no compiler issues. Finally, I want to show you one alternate syntax that you might see from time to time for declaring a type. And that is, if you do not want to write out the integer type after the variable name, you can actually declare the type immediately after the value declaration on the right hand side of the equal sign. So for example, if I declare a variable called some_value and I assign it to a simple number like 20. By default, Rust will infer the type or assume a default type of an i32. If I want to model this or represent this as a smaller i8, I can of course declare the type here or write it directly after the value without any spaces. So just like this. i8 right after the value, and now you can see Rust can infer that we want an i8 simply from this declaration right here. And it would be the same syntax for any other type. Here I'm telling Rust to treat it as an i16. Here, I'm telling Rust to treat it as a u16. So if you prefer the syntax, you're more than welcome to use it, but I think this one is the one you're going to see more frequently, because it aligns with how we declare variable types for any type of data in Rust, so it's just more consistent. But this is totally valid. All right. So this has been a quick introduction to integers. And with that I will see you in the next lesson.
