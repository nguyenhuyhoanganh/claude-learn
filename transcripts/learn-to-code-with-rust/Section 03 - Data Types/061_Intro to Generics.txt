# Intro to Generics

So let's take a moment to discuss the type inference that we saw from Rust in the previous lesson. We have our two ranges here. And notice that rust inferred both of them to be a Rust. But then we see these two symbols here and here, the less than sign (  ). These are commonly called angle brackets because they resemble angles, right? From mathematics. And you can see, within the angle brackets, there is another type declared like i32 for this range of integers, and 'char' for this range of characters. So, as you might have guessed, this type is reflecting the type that that range is of. A range of what? Right? A range of integers in this scenario versus a range of characters. In a sense, they are both a similar type and a different type because these are both ranges, but they are ranges of different things, right? Ranges of different types. So this range type uses an important programming concept called a generic. And I want to discuss it in this lesson at a very high level. Later on in the course, we're going to dive into this topic in much greater depth. A generic represents a type argument. We've explored the concept of arguments before. An argument is an input to something. So the text that we pass to println!, for example, is an example of an argument. It is an input that we provide to this macro. We've also seen arguments passed to methods. Those arguments are inputs into the method. So a generic is a argument, but it's an argument that is actually a type. It's not a concrete value. It is a type. It is a kind of data. It is a category of data. So for example the difference between values and types. A value is something like 5. A type is something like i32. It's a kind of data. It's a category of data. Generics exist so that we can provide types like this one as their own arguments. So let's consider an analogy from the real world. Imagine a box. A box is a type. It's a type of thing that can be used for storage. But a box is a special type because it's a container that can hold different types of things inside it. You can use a box to store your books, or you can use a box to store bottles or toys. If you were a box manufacturer and you tried to sell your box product by saying: "Hey, we'd like to sell you this box, but it can only hold books", well, that would severely limit the box's utility. It would only be able to store one type of thing inside it. A box is great precisely because it can hold different items inside of it, which adds to its versatility, its utility, and it's the exact same principle in programming. So you can think of the Range here like the box, it's the starter type, it's like the container. And then imagine that you are the Rust developer who is building out the Range type for the first time. You may not know about all of the possible types of data that are going to be stored or represented within a range. All the possible sequences of values. You might have a range of integers, but even that includes a range of i32, a range of i8s. Right? You could have a range of characters, or perhaps a range of something else entirely, a future type that you can't predict. So the way that we support that possibility of multiple types is we define a generic. A generic is a name for an abstract type. It acts as a placeholder for a future actual type. So you can think of that original Rust developer who built the Range as saying: We're going to introduce this Range type, and it's going to hold values of this generic type. So that generic type is like a placeholder for some future concrete type, right? That generic type can be a variety of different types in the future, but when the developer defines the Range, they define it to accept a generic. They define it to have room for a future concrete type that can vary, and that generic type represents a variety of potential possible types in the future. So if you think of the word generic in the real world, its definition means "not specific". And that's exactly what a generic is. It's not a specific type yet. It's a placeholder for a future specific type. It's the exact same idea as, say, parameters and arguments. A parameter is the name for an expected value, and an argument is the concrete value, the actual input. Similarly, a generic is a name for an expected type, but it's not an actual type. It's just a name for an expected type that is needed. And then the actual specific concrete type becomes something like an i32 in this range, or a 'char' in this range. The i32 and the char become the concrete types that fill in for the placeholder generic type. If we want to explicitly annotate the type of these ranges, we have to provide both Range, which is what it is, and then a concrete type that is going to fill in for the generic, the actual type that is going to be what the Range is storing. And things are made even more complex by the fact that the Range type that we see here is not available at the top level of Rust. So certain types like an i32 or a bool you can use out of the box. Pardon the pun. But something like the Range type is nested in what's called a module, which you can think of like a folder on your computer. Rust has a lot of different names, has a lot of different types and functions. Not all of them are loaded by default. Some of them are nested inside what are called modules. They're just organizational containers in Rust to avoid loading everything up front. And in order to access this Range type to provide the annotation for these two variables, we need to reach into some of those modules. It's very important that we not obsess over these details here, because we're going to dive into them in much greater depth and clarity later. But I do want to show you the syntax here. So what I'm going to do here is after month_days -- once again -- colon to provide my type. Then what I'm going to do is write this: 'std', which is short for standard, and that refers to the standard library, which is Rust's built in collection of features and functionalities. You can almost think of this like the top level lobby of Rust or the top level folder of Rust. Then, we're going to put two colons, and we're going to write out 'ops', which is short for operators. You can think of this double colon syntax for now as going a directory deep into Rust or navigating a folder down into Rust called 'ops'. Then, one more set of two colons in a row. And here we're going to find Range. And this is our Range type. Now if we were to simply write our code like this, this is not going to be sufficient because Rust is going to tell us this Range type is missing a generic. I need one generic argument. What is a generic argument? It's not a value like 5. It is a type like i32. Rust needs to know that type of value that this is going to be a range of. This is going to be a sequence of what? The Range type tells Rust that we have a continuous sequence of something, but what is the type of that something? That's what Rust and the compiler need to know. So the syntax we're going to provide here is those angle brackets, opening and closing, less than sign ( < ) and greater than sign ( > ). And here is where we provide the actual type that is going to be stored within the Range. This is the concrete type that is filling in for the placeholder generic. And here I'm going to write i32. We can also provide another concrete type like i8. That only goes to show the versatility of generics, which is the whole point. The Rust developer doesn't have to say: "You can only build a range of i32s". The Ryst developer who wrote this can say "You can build a Range of anything". You just need to tell me what that type is going to be. I'll write it to be generic. I'll write it to be not specific. It'll be some type stored within this Range, but you tell me what that is when you are making a concrete Range. And that's exactly what we're doing here. So the syntax for 'letters 'is going to be the exact same: colon for our annotation, 'std::ops' ::Range Then angle brackets in which we provide the concrete type to fill in for the generic, to fill in for the placeholder that was defined by the developer who wrote the Range type. And for this we're going to write 'char' or 'char', which is short for character. All right. And of course if there is a mismatch here, like if I write i8, then the compiler will warn us: "Hey, you do have a Range. You told me you needed a Range and you're giving me a value of a Range. But now there's a mismatch in types because you told me to expect a Range of integers." Instead, you're providing me a Range of characters. That allows the compiler to once again catch violations. And thus we can fix this to be a 'char'. So generics can be a tricky and advanced programming concept, but I did want to introduce them early at this high level, just to get us thinking and to realize just how powerful a lot of these features in Rust are. All right. Later on in the course, we're going to review generics again and walk through them in much greater depth and detail, and also discuss how we can define our own generics as well for methods, for functions, and even more. All right. That's all there is to cover in this lesson. So I will see you in the next one.
