# Project Solution

Let's walk through a sample solution to the coding challenge in the previous lesson. First up, we had to declare a variable of type i32, a 32-bit integer assigned to a value of 1337. So here I'll declare a variable called distance. I can choose to provide my explicit type annotation here, even though the default inference for an integer will be an i32 anyway, so this is optional, but we are always welcome to add it here. I'm going to write 1337, and the instructions also ask us to use the underscore character to add a visual separator. I'm going to add one right here. Next up, let's cast the I32 to an I16 integer and assign the result to a separate variable. So here I'll declare a 'miles' variable, and on the right hand side' I'll take my 'distance' variable, and I'll use the 'as' keyword to convert. or the technical word for this is cast my 'distance' i32 above to an i16 here. So here after 'as', I'll write the data type that I want to cast to, and now you'll notice rust-analyzer will infer 'miles' to be of type i16. Next up, declare a floating-point value of your choosing and print it out with 3 digits of precision. So here I'll declare a 'height' variable and I will set it equal to 150.34546. So we have 5 total digits after the decimal point. But I want to print it out with only 3. The way I do that in my println! macro is I provide my 'height' variable in curly braces. This is what I want to inject. And then I provide my colon ( : ) which is my format specifier. It allows me to customize how in this case the 'height' value is going to be printed out. It does not change the underlying value, only its printed representation for this println! And if I want a precision of 3 digits, I'll do .3 and that should do it. So I'll quickly run this file. And here we see 345. Perfect. Next up is Booleans. We want to declare two variables set to Booleans: with_milk and with_sugar. So right here, once again, the 'let' keyword to declare a variable. I'll do with_milk and set that to true. I'll do with_sugar and set that to true as well. Notice that the inferred type is bool. The only two possible values for the bool type are true and false. All right. Next up we want to declare two variables that are going to perform either AND logic OR logic with these two with_milk and with_sugar variables. So first up, we wanted to declare a is_my_type_of_coffee variable. And if it's my type of coffee, I like it with both milk and sugar. So I wanted to combine AND logic. I wanted to say if with_milk is true AND with_sugar is true, then it is my type of coffee. So to perform AND logic, I'm going to use the and operator which is two ampersands in a row ( && ). So, in this case, I'll write with_milk && with_sugar. So is _my_type_of_coffee will be equal to the value true because both of these are true. If either one of these was false, or if both of them were false, then is_my_type_of_coffee would be equal to false as well. Both this and this, these two independent conditions, must both be true in order for the overall expression to evaluate to true as well. On the next line, I'll declare my other variable: is_acceptable_coffee. And, in my opinion, if coffee has either milk or sugar, it is acceptable. I can drink it, I can tolerate it. So here I'm going to be less strict and I'm going to apply OR logic, which asks that either one of the two booleans is true in order to evaluate to a final evaluation of true. So I'll provide the exact same variables, but this time around I'll use two pipes in between ( || ). So with_milk or with_sugar. So here's how this works. If either with_milk or with_sugar is true, then is_acceptable_coffee will also be true. If both of these are true, then is_acceptable_cofee will also be true because at least one condition has been met and both conditions being met satisfy that minimum criteria of at least one. The only way that is_acceptable_coffee will be false is if both of these are false, because then neither condition has been met. All right. Next up, we want to declare an array with 4 i8 integers of your choosing and print it out in Debug representation. So I'll begin with my variable called 'distances'. And then I'll provide my square bracket syntax to declare an array. Now you'll notice the issue here. Once I choose four values like 13, 23, 75 and 100. By default, Rust is going to infer a four-element array of i32s. So if we want i8s, we have to provide an explicit type annotation right here. So here I'll provide my colon. And then I'll use this exact same syntax. But I'll replace the i32 with an i8. So here I'll provide my square brackets. This is not an array declaration. This is me declaring the type of the 'distances' variable. It's going to hold i8 values. Then I place a semicolon and a space, and then I write the length of the array, which is the number of elements that it contains. So I want an array of four elements right here for i8. These all satisfy the criteria. The array length is as expected, and all of these numbers are valid and fall within the range of an acceptable i8. So that works. And then finally to print it out in Debug format. In here, I can provide 'distances' within curly braces. and then I need my Debug specifier which is again the colon. Same exact format specifier that we used right here. But the symbol we use for Debug formatting is the question mark ( ? ). And that's going to work right here. And as a reminder, you can also use pretty-print, which is the hashtag before the question mark ( #? ). That is going to print out each element one per line. And finally, you can also just add 'distances' as a sequential argument to println! So here, 'distances' comes in here. I still use my format specifier and my Debug formatting and I get the exact same result. It's up to you how you want to write it out. Finally, we need to declare a tuple consisting of a previous integer float, boolean, and the array that you previously declared, and print out the tuple in Debug representation. So here, I'll declare my tuple. Let's call it 'combo'. For a tuple, we're going to use parentheses instead of square brackets. Square brackets are for arrays, parentheses are for tuples. And the beautiful part about tuples is they can contain data of different types. It's still kept sequential. And each element is assigned an order in line called its index position, ust like with an array. And just like with an array, the index starts counting from 0. But the advantage of a tuple is that we can store elements of different types. So here I'll provide 'miles'. Let's do 'height'. Let's do 'is_my_type_of_coffee' and let's do 'distances'. So I'm going to store an array inside a tuple. But that's totally valid right. It's a container for different types. You can even see here in the inferred type declaration. Here are all of our scalar types types that represent just a single value. And then the fourth element in the tuple is going to be an array. The fourth element in my tuple composite type is another composite type, another collection type, another container that can store multiple elements. My fourth tuple element is a four element array of i8s. And here I can do my println! Once again, I'll provide my 'combo' variable. And here's my Debug formatting. Let's print it out. And you can see right here is my printed tuple. We have our integer, our float, our boolean, and our array of integers. Let's take a look at what the pretty-printed version looks like. You can see it's kind of interesting. We have our tuple with each element laid out on an individual printed line. And then for the nested array, its elements are also broken up one by line. So pretty interesting design here for Debug formatting. That's all there is to cover in this coding challenge. And with that, I will see you in the next lesson.
