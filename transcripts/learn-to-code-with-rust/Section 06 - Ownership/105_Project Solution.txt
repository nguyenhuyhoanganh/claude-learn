# Project Solution

So let's walk through a sample solution to the coding challenge in the previous lesson. First up, we had to declare an 'is_concert' variable set to a Boolean and I will choose true here. Then we declare an 'is_event' variable and assign it to the value of is 'concert'. So the question is will Rust move ownership? In this situation, the answer is no. Booleans, very much like other primitive simple data types like integers and floats, implement the Copy trait. What that means is in situations like this where there is potential for ambiguity, or different ways that Rust can think about this code, when we're dealing with types that implement the Copy trait, Rust will create a full copy. So 'is_concert' remains the owner of its true Boolean, and we create a full copy of that value and 'is_event' becomes the owner of that copy, of that duplicate, of that clone. So we have two owners and they are each responsible for their own duplicate value of true. So after that line, if I try to output either one of these values or the variables rather, they're both going to be completely valid. We're going to see true and true. And we have two owners. And when they go out of scope at the end of main, they will be responsible for cleaning up their respective true values. So the next challenge here is very similar except we're dealing with string literals, also called string slices. So let's declare a 'sushi' variable set to a string literal of "Salmon". And then a 'dinner' variable assigned to the 'sushi' variable. Is Rust going to move ownership? The answer is once again going to be no. We're going to have a duplicate. Now here, what we're technically dealing with when we have string slices is what's called a reference. And we're going to talk a lot more about that in the next section of the course. But the key takeaway here, and if you're ever unsure, is that you can print out the two names, the two variables side by side, and if both of them are valid after the assignment, then you know that ownership has not moved and a copy has occurred. So similarly, just like with the Boolean example above, 'sushi' and 'dinner' are both valid here, so ownership was not moved. A full copy was made of the string slice, which is a reference. We're going to get "Salmon" "Salmon", okay? So this is where things get interesting. Let's repeat the previous example. But use a heap String instead. So I can use variable shadowing to redeclare my 'sushi' and 'dinner' variables. But this time around for 'sushi', I'll use 'String::from'. I'll use that function. I'll provide "Salmon" right here. And then on the next line, once again, I'll use variable shadowing to redeclare 'dinner' and assign it to be the value of 'sushi'. Is Rust going to move ownership in this situation? The answer is yes. This type of String that lives on the heap does not implement the Copy trait. Therefore, a copy is not made and ownership is moved. 'sushi' is the initial owner right here, and when it is assigned to 'dinner', 'dinner' becomes the new owner of this heap String and 'sushi', the variable, becomes invalid to use in our program. So on the next line, we are more than welcome to use 'dinner', and the compiler is totally fine with that. But we cannot refer to 'sushi' again in the program. And notice we are going to get the compiler error right here. But if you try to compile, you're going to get a wonderful error message that tells you at which point all of these situations occurred. So right here, "move occurs because 'sushi' has type String which does not implement the Copy trait". Then the compiler will tell us where the value moved. Moved on line #1, when 'sushi' the heap String was assigned to 'dinner'. 'dinner' is now the new owner, and therefore when we're trying to use 'sushi' to print it out right here, it's no longer the owner. We cannot use it. We cannot use it here after a move. All right. So in order for me to be able to even continue writing code after line #12, need to comment this out to be able to progress. All right. Next up, we were introduced to the 'clear' method. This modifies or mutates a heap String to have no content. Basically, it removes all of the characters and gives you back an empty heap String. It's one example of a method that mutates a heap String. Another is 'push_str', which concatenates to the end. But the key takeaway here is that's the advantage of a heap String, because we have that dynamic area of the heap to work with, we have the capacity to have data types that can expand and that can shrink, and that can change and mutate over the program's execution. So we are asked to declare an 'eat_meal' function that expects a 'meal' parameter of type String. And we're asked to invoke the 'clear' method on the 'meal' parameter. So right here, I'll take 'meal' and invoke 'clear'. And we're immediately going to see a compiler error. So the issue here as we can see is that we cannot mutate an immutable variable 'meal'. Now, I would prefer to substitute the word "parameter" for the word "variable" right here, but you can almost think of a parameter like a predefined variable anyway. 'meal' is a name that is assigned to a value, and 'meal' is that name, that parameter, that we have access to in the body of the 'eat_meal' function. Just like with variable declarations, our parameter declarations when we write a function are assumed to be immutable by default. That means we are not allowed to call any method or perform any operation that mutates that value. And 'clear' does mutate the String. It changes its total number of characters. So therefore, to tell Rust that we agree to this and that we prepare to mutate the content of the String in the body, we need to mark this parameter as mutable. Just like if we have a variable that's going to change, we put the 'mut' keyword after the 'let' keyword with a variable. With a parameter, we add the exact same 'mut' keyword right here. That is short for mutable. And now we have permission to do any kind of operation to the String in the body of 'eat_meal'. All right. So now in the main function, let's invoke the 'eat_meal' function and pass in your 'sushi' String. And technically speaking, 'dinner' is now the owner of the 'sushi' string, so to speak, the String of "Salmon". So I'm going to pass in 'dinner' right here. And then we are asked to explain what happens when the 'eat_meal' function runs. Describe the complete movement of ownership of the "Salmon" String throughout the program. So from the top, 'sushi' is the initial owner of the "Salmon" String. On line #1, ownership moves from 'sushi' to 'dinner'. That means the 'sushi' name is invalid. In our program, only 'dinner' is valid. Now, when 'eat_meal' is called on line #14, ownership moves once again. It moves from the 'dinner' owner to the new owner of 'meal'. 'meal' is now the owner, which is who is responsible for cleaning up this heap String in the program. And there can only be one owner at a time. So now it's 'meal'. And 'meal' knows after it's done performing whatever logic is done in the body of this function. when the function ends, the 'meal' parameter goes out of scope and it, as the owner, is responsible for clearing up the heap memory. Right? So 'meal' is the final owner. So what's the problem here? The problem is that if we do not find a way to get 'meal' back, or more specifically, the string that 'meal' is the owner of back to main, then at the end of 'eat_meal', we're going to lose that String. So the next and final challenge is: say we want to keep the "Salmon" String after 'eat_meal' is called. How can we continue to have access to the string in the main function? Another way to ask that question is: how can we prevent the String from being deallocated? Now there is the proper way to do so, and we're going to learn that in the next section of the course. But what we learned in this section is we can return the 'meal' back to main. Remember, a return allows us to preserve the value. The only thing that's wrong here. Two things is number #1, I need a semicolon here. And number #2, if I'm going to return a String, I need to specify the return type in my function signature. So right here, I'll provide my arrow and say I will return a String. And that should do it. And now a return value preserves the String. So now it arrives back in main. And technically if we don't do anything after line #14, it would be deallocated because it would no longer have any owner. However, if I assign it to a variable here, like let's say 'fish', well, now 'fish' is the owner and moving forward, 'fish' is what we can access to get that heap String. So 'fish' is still going to be equal to this original text of "Salmon" right here stored on the heap. And you can see right here. Let's see what's oh we cleared it. Of course we cleared it. So we are getting nothing here. Right. We're getting an empty string. But I can just prove to you that if I comment this out, we're going to get it back. And there is "Salmon", right? So we removed all the characters. So we're printing out an empty string. And that's why we see this little whitespace right here. So key takeaway once again complete movement of ownership from 'sushi' to 'dinner' to the 'meal' parameter in 'eat_meal', then return back. 'fish' is the final owner. Then the main function concludes. 'fish' is the owner at that point. So when 'fish' goes out of scope, we can imagine right here it cleans up the "Salmon" heap data, and all of our memory is now deallocated and cleared. And the program concludes. Once again, ownership is one of Rust's most tricky features, but what I recommend is just following your code line by line. And the first question you ask is: is this type implementing the Copy trait? And if it is not, then you assume a movement of ownership and ownership can go from variable to variable. It can go from variable to function parameter. It can go from function parameter back to variable. And there's many other ways that we can have the concept of ownership as well in Rust. We're going to review all of those as we progress throughout the the next section. All right. So hope you had fun with this challenge and I will see you in the next one.
