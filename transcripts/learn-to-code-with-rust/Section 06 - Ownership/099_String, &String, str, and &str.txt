# String, &String, str, and &str

Now that we have a better understanding of references, I want to return back to string literals like the one we see on line #15 and explain them in greater depth. I also want to draw the distinction between the two types of strings in Rust, str and String, as well as references to both of them. So I've written out all four possibilities here as a comment so we can quickly walk through them. So the first option is a String. This is a dynamic piece of text stored on the heap at runtime. This is what we've explored throughout this section. Now we can also create what's called a ref string ( &String ). If we use the borrow operator, the ampersand symbol, in front of that string, we create a reference to the heap String. We take the address of that heap String and we store it elsewhere like a variable. The advantage of this is that we prevent duplication of the heap data. We only create the address itself, which tends to be less memory intensive. So that is a string ( String ) and a ref string ( &String ). Now we also have these two complementary types which is a str versus a ref str ( &str ). So a str is a hardcoded readonly piece of text that we encode directly in the binary. So what we have on #15 is an example of that. When we use double quotes, we create a string literal. We create a string, a piece of text, but one that is known in advance and one that does not have the ability to change. So it's a different category of string compared to this heap String that is created at runtime. The compiler knows what this text is going to be, the exact order of these characters, and it's not going to change and it's not going to mutate. So this piece of text and any piece of text that you declare in double quotes is going to be encoded or embedded directly into the binary executable. I'm going to show you that in just a moment. But whenever the program runs, we're never going to be dealing directly with that 'str'. We're going to be dealing with a 'ref str', a reference to that text. So any time throughout the course where we declared a variable and assigned it to a string literal, you might have noticed that type was declared to be this. And that ampersand symbol is a giveaway that it is a reference. Now what is it a reference to? It is a is it a reference to data that is stored on the stack or the heap? The answer is actually neither. It's a reference to that hard coded text that was embedded into the executable that is then loaded into your computer's memory. So Rust and its compiler takes our source code, creates the binary executable. Then, when the program runs, your computer's memory ingests that running executable, and somewhere in that executable, this text is contained. And so when the program is running at runtime, it's getting a reference to that text, that text that is living in the memory, but that memory is itself loading the binary file's contents. So we're not duplicating this text either. We are just providing a reference so that we can follow that memory address to that "Cookies and Cream" text and utilize it. And this will be true for any hard coded string literal within our program. So what I want to do here is I'm going to run our program and remember behind the scenes that runs 'cargo run', which builds the executable. You do not have to follow along with what I'm about to do, but I want to show you that if I open my File Explorer. As a reminder, we have our 'target' folder and then the 'debug' folder. And in here we have our binary executable. This is the actual program with the machine code that your computer can understand. If we open this file right now or attempt to, you can see it says "this file is not displayed in the text editor because it is either binary or uses an unsupported text encoding". So I'm going to use a little bit of a cheat code here. Once again, you do not have to follow along, but I'm going to click on this final file and I'm going to click "Open With...". And I've installed this extension called "Hex Editor" that is going to enable me to see the contents of the file. So this is a binary executable file which means it just consists of zeros and ones that your computer can understand. This is the direct result of our source code. That's why we have programming languages and source code, so that we can have code that looks like this, that we understand, that is then compiled into machine code that the computer can understand that looks like this. Now this is all zeros and ones at the end of the day. But because there's so many of them, this file is presenting them in an alternate format called hexadecimal, which basically uses a base of 16. We don't have to worry about all of those technical computer science concepts. What I want to show you is that if I use my VSCode Find feature, which is just Cmd + F for me, and I look for that text that I just wrote in my source code, I'm just going to search for "cookies". There it is. In fact, let me close this right here. There is "Cookies and cream" right here embedded directly into the binary executable. Any piece of text that you declare as a string literal with double quotes will follow the same protocol, and it's going to be embedded here. So this is the technical 'str' or str type. But when the program runs we're always going to be dealing with a reference to a 'str' ( &str ), because we're going to be dealing with a reference to the memory in your computer that has loaded this file and thus has access to this content. All right. It's really all there is to cover in this lesson. I just wanted to make sure that these distinctions are easy and clear to understand. It can be complex. And the good news is you don't really have to worry about it too much day to day. All you have to remember is if your text content is going to be changing, mutating, updating, if it's dynamic, if it's unpredictable, you use a heap String. Then if you want to prevent creating duplicate copies of that data to make your program faster, and you need to reuse the same text on the heap, then you create a reference to that heap String. Otherwise, if you know your text content in advance, you just declare it with double quotes, which technically creates a string which will be embedded directly into the binary executable. But from the program's perspective, when it runs, it's going to be operating with a reference to that text hard coded into the binary. And that's why we see a ref str ( &str ) here instead of a str.
