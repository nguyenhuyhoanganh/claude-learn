# Adding MovieTheater Struct

So our libraries file has quickly expanded to hold a lot of content. So the next step is going to be breaking up this crate root and transferring this content to a new module. And that will also pave the way for the introduction of some new testing concepts. So here's what we're going to do. We're going to move the complete museum struct, including all of its methods as well as the tests module to a new module called attractions. And we're going to create that in a separate attractions file. So first things first within my File Explorer as a reminder to create a module. There's a wide variety of ways to do it. One way is to create a file with the module name. And then we'll declare that module using the mod keyword in our crate root. So here let's create a new file called attractions dot js. And into there I'm going to literally cut and paste everything in this file into attractions and then in libraries. I'm going to write mod. Attractions. And that part is critical, so that the rust compiler can identify that an attractions module exists, and that allows it to look for its definition in either the attractions file or in an attractions folder with a mod file. All right, next thing. Or next up, we're going to define a new struct to complement our museum. So a museum is a sample attraction in town. We're going to add another one which is going to be a movie theater. Another thing that we can do around town. So here I'll define a new struct below the museum, above our tests module. I'll call it movie theater. And before I forget, I will immediately derive my debug trait implementation above. And movie theater is going to be somewhat similar to our museum. It's going to have two fields. The first one is going to be movies, and that will be a vector of strings, so very similar to the museum's vector of paintings. But this is a vector of the movies playing at the movie theater. And then we're also going to have a sales field set to an unsigned 32 bit integer. Down below, let's define some sample methods and constructor functions within an ample block for movie theater. So we'll have a new constructor function which will return an instance of the movie theater. Right here I can provide self, and let's initialize the movies field to be an empty vector and initialize our sales to be zero. Let's also define a sell ticket method very similar to what we had on a museum, but this time around we're going to increment a different field because I believe in our museum, the field is called revenue. Here it is called sales. But this is going to borrow a mutable reference to the movie theater instance. And it's going to increment the sales field by 15. It's a little bit cheaper to go to a movie than it is to go to a museum. All right. And then to actually add a movie to my vector, I'll define a method called add movie that will take a mutable reference to the instance, as well as a movie parameter set to a string slice. And what it's going to do is take the movie's vector referenced by self dot movies, and call the push method to add on a new string, which I will also convert to a heap string using the toString method. So we'll take movie. We're going to call toString. Now you'll notice there is a common method across both our movie theater and our museum struct. And it is sell ticket. So whenever you have this kind of similar functionality or similar method name that's trying to accomplish the same purpose with a respective data structures data, a good idea is to enforce that consistency by extracting that functionality to a trait, because the trait will then allow us to mandate that method and make sure that if we update it in one place, we have to then update it in the other. So let's define a trait above. I'm going to do it at the very top of the file. And then we're going to implement it for both our museum as well as our movie theater. So I'm going to call the trait ticket seller. Each one of these structs down below is something that sells tickets to attend. Right. And I'll define a method called sell ticket, just like we have below. And it will accept a mutable reference to the given instance and return nothing. And we already have these methods implemented, but I'm now going to extract them to a separate implementation block for the ticket seller trait. And I'll show you how this all adds up to good tests, as well as an important design pattern called dependency injection in just a lesson or two from now. So let's scroll down here. We're going to implement ticket seller for our museum struct above. And all we have to do is just cut and paste the sell ticket method from above into here, and that should do it. And we're going to perform the exact same logic for our movie theater. So right here we'll implement the ticket seller trait for our movie theater struct and just cut and paste the content from above. And there we have it. So we do not have any custom unit tests for our movie theater struct for now, but we do have our old ones for the museum and they should all continue to run. Now notice when I run cargo test, everything should continue to work as expected, despite the fact that all of this is in a new module now. So cargo tests will still identify all of the test functions. It still needs to know about the existence of the attractions module in order to locate the test functions within it. But again, the crate root in libraries is going to inform it of the existence of that module so that it can locate the module and the tests inside it. So cargo test should continue to run. And you can now see because we are nested within one more module of attractions. That module and its prefix is going to appear right here in the content. So attractions tests module within the attractions module. And then here are our test functions. So for each nested module that we add the test runner output is going to include that full module path starting from the outermost module and progressing inwards through each module until we get to the respective test. And you are welcome to copy and paste this syntax again if you want to run a specific test. So for example this one right here, I can just copy and paste this and provide it after cargo test. Looks like that didn't work quite great. Let me try one more time here. And because it can identify the exact name for it, it's going to be able to elegantly run the test, the individual test that we want. All right. That's all there is to cover in this lesson. And we're going to start introducing some new testing concepts in the follow up lessons. I'll see you there.
