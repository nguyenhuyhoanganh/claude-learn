# Running A Subset of Tests

In this lesson, I want to show you how we can run a subset of tests. Or in other words, a smaller batch or smaller collection of tests based on those that match a given search term. So currently our full test suite has only five tests, so it runs pretty quickly. But you can imagine in a much larger code base it's reasonable to have hundreds if not thousands of tests, and those will add up to a longer running time. Sometimes you don't want to run the complete test suite. You want to specifically target a smaller collection of tests. So the first option you have after the cargo test command is to simply write out the full name of the test that you want to execute. That allows you to run just one test at a time. For example, say that we want to just run this test right here. I can click on the function name right here in VS code, then copy and paste it. So on a mac that's command C. And on a windows computer that's Ctrl C. And I'm going to paste the complete test name right here. Museum can have impressive art collection. And you'll notice when I run my command we're not going to run the complete test suite. Rather we're just going to target this one test right here and then show us a result set that is only targeting one test. In fact, within this syntax, you can actually specify the exact module path that you want to look within. So we know that this test specifically is within this module called tests. So you can write tests in reference to this module name. Then the double colon syntax and then the name of this test. And that allows us to again micro target a specific test within a specific module with a given name. Now what's actually happening here is we're just utilizing a search term. So cargo test is looking for whatever matches the search term that you provide. And in this scenario the term is so specific that it only matches literally this one test. But you can actually provide whatever term you want after. And cargo test will filter your tests to those that match the term. So for example, I can search for something like ticket right here, or any test function that has the test ticket inside it, and that should target only two. This one right here, which has ticket in its title here, and this one right here, which has ticket here. So you'll notice we're going to again filter or limit the total number of tests we write from five down to two. All right. So that's a quick explanation of how you can run a subset or a smaller batch of your tests.
