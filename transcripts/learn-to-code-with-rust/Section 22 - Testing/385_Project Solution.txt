# Project Solution

So let's walk through a sample solution to the coding challenge. In the previous lesson this time around, we were modeling a salad restaurant and we had some starter code to work with. So what we were given is a collection of enums. We have a vegetable enum, we have a protein enum, and we have a dressing enum. And all three of these enums implement a caloric trait, which requires a single calories method that returns the calorie count of each respective variant within that enum. So, for example, a crispy chicken protein has a total of 400 calories. And what we're going to be assembling here is a salad struct. So if I scroll up here to my instructions, our next goal is to build a salad struct that will consist of a protein field, a vegetables field, and a dressing field. A salad can store one protein, any number of vegetables and one dressing in order to accommodate the storage of multiple vegetables within the same salad, we're going to use a vector to store the vegetables. And then we're also asked to derive a debug trait implementation. So we have a total of four functions or methods that we are asked to define on a salad. And we were given the option to either use test driven development or TDD where we write our failing test first, or alternatively to write our implementation first, then write the tests afterwards. So I'm going to be utilizing the TDD approach, and I'm going to ensure that I first have a failing test before I write any implementation code. We're also asked to follow the best practices for our unit tests, and to feel free to utilize any helper crates. So the first thing I'm going to do is indeed add support for the pretty assertions and SES test crates. These are optional, but I find them to be nice to have, especially when we're dealing with fixtures as well as, of course, unequal values that are hard to decipher the differences between within our terminal. So I'm going to open up our cargo.toml file at the top level of our cargo workspace. And here underneath the dev dependencies section, I'm going to list pretty assertions. And I'm going to put that version as 1.4.1. And then below that I'm going to have Rs test. I'm going to put its version as 0.25.0. These are the latest versions as of the time of the recording. And the syntax should work with these version numbers. If you choose to go with a more modern version, that's totally fine and probably recommended. But just keep in mind, as these crates update their internal syntax or terminology or named constructs might change as well. So there's no guarantee that the code will work as you see it written on my screen in this lesson. So I'm going to save this. And just to make sure we fetch and download all of those dependencies, I'm going to run cargo test right here at the top of my SolidWorks project. And you can see we are downloading and compiling all those things. And right now we have a total of zero tests, but everything is passing as expected. All right. So let's begin with the definition of our solid struct. We're also going to need a new constructor function that's going to accept a protein a vegetable vector and a dressing and return an instance of that given salad struct. And then the test we can assert that the three fields of the salad are correctly populated from their respective parameters. So down below below all of my implementation code, as is recommended, I'll declare a module with the mod keyword called tests, and immediately above it I'm going to use my attribute right here hashtag square brackets cfg, which is short for configuration. And I will put test here. And that will make sure that the compiler only includes this code when we are executing cargo test. Or in other words, this module and its code will be excluded whenever we're utilizing any of this code for either our own final executable, or if any other crate wants to pull in our code in our library crate into their project. This will also not be included, which will reduce the size of the final executable that that other binary crate creates utilizing part of our library code. All right. So in here I'll begin with the use keyword. And I'm going to bring in all of the named constructs from above from the outer scope or the outer module. So super of course reaches one level up. Then after the double colon I'll apply the glob operator the asterisk. And that will ensure that any name defined here, such as our enums and our eventual solid struct, are automatically available to be referred to within any test. In this test module, I'm also going to reach into pretty assertions, and I'm going to pull in the assert equals macro. That's going to override the default assert equals macro that rust provides us. And then finally I'm going to reach into CI's test and I'm going to pull in fixture and says test. I'm going to use the CES test annotation above any test function instead of the plain test one. Now, even if our test does not necessarily involve a fixture, there's nothing wrong with using CES test because it's basically a valid replacement for the regular test attribute from rust. All right. So right here let's write our first test. I'm going to use the CES test attribute. And I could say something like salad contains protein vegetables and dressing. And I'm going to write the code that I wish I had or how I imagined my implementation playing out. So of course, let's define a variable called salad. And I assume I'm going to have some kind of salad struct. It does not yet exist, but that's totally fine. This is the purpose of TDW. I'm going to imagine this struct exists and following the conventions of rust, it should have a new constructor function, and that constructor function is going to receive these arguments. So for our protein I'm going to pick an arbitrary one like steak. For my second argument which is going to be a vector of vegetables, let's provide that vector in line here. I can choose whatever arbitrary vegetables I want. Let's do a vector of two, and let's do a sweet potato in the vector. And then another one of a tomato. And for my third argument to the constructor function let's pick a dressing of Italian. So I imagine I'm going to be creating a salad with these three inputs. And that should, if everything is coded correctly, set these three values to their respective struct fields. Typically I like to have one assertion per test, but I'm going to violate that rule right here. And I'm going to write three assertions one per struct field. So here I'll begin with my assert EC macro from above I'm going to take my salad. And I'm going to assume that it should have a protein field. And if the new constructor function did everything correctly then that should be equal to my protein of steak. Then I will add another assertion of equality using assert EQ. I will reach into the vegetables field which should exist on a salad, and that should be a vector. And that vector should of course contain a vegetable of sweet potato and a vegetable of tomato. And that's going to be my second assertion. And finally for my third assertion right here with the assert EQ macro, I will reach into my third struct field which should be dressing, and that should be equal to the Italian dressing. And that should do it. So I should have a failing test. Of course if I run cargo test or cargo t, I should see that violation in my terminal right here. Use of undeclared type solid. And that's totally valid. We can even follow the failures of test driven development to each individual step of progress. So I could literally say, let me just create a salad struct that does nothing to get rid of this error. Then the next error will be a new constructor function doesn't exist, and so on, just progressing one error at a time to the final implementation. Now, because I generally know how to create a constructor function, I'm just going to go ahead and build up a working solution right here. So let's scroll up top below our dressing declaration. I'll declare my salad struct right here. I will immediately add my derived syntax above and derive a debug trait. A salad is going to have a protein field which is set to a protein enum. It's going to have a vegetable field which will be a vector of vegetable enums. And then finally it's going to have a dressing field which is going to be a dressing enum. And to define my new constructor, I will define an import block for a salad, and I will define my new constructor function. It's going to accept a protein parameter set to a protein enum, a vegetables parameter set to a vector of vegetables. It should have a dressing parameter set to a dressing enum, and it should return an instance of salad, which I can represent with capital S self. Here in the body, I can return an instance of my salad represented by self, and use my shorthand to provide protein, vegetables and dressing that should auto populate those three fields with the parameters. And now I can run my test and make sure everything is passing. And indeed it does. All right, let's move on to our next challenge. The second of the two define and is valid method that returns a boolean return a true. If a salad has more than zero vegetables. I guess technically you can have a salad with just a protein and a dressing, but I don't really consider it a salad if it doesn't have vegetables. So that's what this method is supposed to represent. Once again, I'm going to utilize test driven development. So let me scroll down here and let me define another test within my tests module. So right here says test. And I'm going to say salad should have at least one vegetable. And here in the body let's create a sample salad. So for example I can create a salad variable. And I will take my new constructor function. Let me provide a protein here of crispy chicken. Let me provide a vector with a whole bunch of vegetables. Let's do a vegetable of sweet potato. We'll do one of cucumber and one of tomato. And then finally for our dressing, we can do a dressing of ranch that should do it. And then of course, what I'd like to do is invoke a new method that does not yet exist. And that method should return a boolean. So right here I can use the regular assert macro to validate that the argument right here is indeed a boolean. I can take my salad. I can call the is valid method on that, and if that properly returns a boolean because this should give us a true. There is at least one vegetable within the salad. Then the salad is valid and this should pass. So as always, if I run cargo test because we are utilizing TDD, our test will fail because that method is not found. And that's totally valid. So scrolling up I can add my new is valid method, provide a immutable reference to my salad instance and this should return a boolean. And of course what I can do here is take my vegetables vector. Take the Len method, which will give me the total count of items within the vector. In other words, the number of vegetables. And I can see if that is greater than zero. And if that's true, then my salad has vegetables, which makes it a valid salad. Really a silly example, but hopefully it demonstrates the point. So now I have my implementation. I can run cargo test and everything passes once again. Once I have a passing test, I have completed the green phase of TDD, so I'm welcome to improve upon my implementation if there is anything left to improve. Obviously in this scenario this is pretty simple code, so that should do it. All right. Next up the third challenge is to find a calories method that calculates the total calories in the salad. The vegetable protein and dressing. Enums all support a calories method that was mandated by the caloric trait, and that method will return the calories of the item. Remember that vegetables is a vector of potentially multiple vegetable values, and thus you'll have to include all of them in your calculation. This is a place where TDD can really be helpful, so let us once again use TDD to define our new tests. So down below right here I'm going to use my SES test annotation. And I'll define a test. Let's say salad calculates total calories from ingredients. And I think the salad above is a pretty reasonable one to start with. So I'm going to copy and paste it down below. And as soon as I do that, I start realizing, hey, wait a minute, I'm now starting to utilize this solid and it's instantiation in multiple examples. I don't necessarily think that that is necessarily a bad thing, because it also does help to make the test readable. It's sort of self-contained and completely understandable from top to bottom. But if you'd like to, you're certainly welcome to extract this to a fixture function. And that will simply allow us to pass in a solid like this as a parameter right here, and avoid these eight or so lines of redundant, repetitive code. Let's do that. Then. Let's create a fixture up above. So I'm going to scroll above my tests right here and utilize my fixture annotation above a function that will return an instance of a solid. So I'm going to give this fixture function a nice descriptive name. I like to encapsulate everything that is being captured within the given struct. So how about something like this crispy chicken salad with three vegetables and ranch dressing. It's going to be a function that returns a salad. And what I can do right here, of course, is just copy and paste this bunch of code right here up above. Indeed, a crispy chicken salad with three vegetables and ranch dressing. Make sure to omit the semicolon so that you have an implicit return. And now I can use this function name as a parameter list in any test function where I want this instance. So scrolling down, let's focus on this first one right here. The previous one that we wrote I can just provide that parameter. It will be of type salad like. So that allows me to get rid of this declaration or this instance right here, and simply replace my salad right here with my parameter from above. There we go. And I can utilize the exact same fixture right here. So let me copy this again right here. This will be a salad. And now I no longer need this in in line. All right. So now I'm going to assert the equality of my given salad, which I have from above, and I'm going to assume that it should have a method on it called calories. And we can quickly do the math on what the actual calories should be. So keeping what we have in mind here crispy chicken, ranch dressing and three vegetables. If I scroll up here, crispy chicken should be 400, right? Ranch here should be a total of 150. So that's a total of 550 calories. And then we're going to have all of these vegetables represented. So 550 plus 20 is 570. Plus 15 is five 585. And plus 100 is 685. So now we should have a total caloric count within our salad of 685. So scrolling down here let's set that value right here. And this is a perfect example of TDD in action. We don't even have to know the exact implementation of the calories method. We've just declared exactly what its purpose is. It's going to effectively go through all of the important fields and add up the total calories and give us this number. We do not have to know the exact implementation. We're describing the value of this method, and it's so easy to write your implementation first, and then forget to write that test that is going to keep you safe and make sure everything is working as expected, right? If I run cargo test right here, we're going to have a failure because we do not have our calories method on a salad. So let's go ahead and define that method up above within our import block for a salad I'll define my calories method. It's going to take an immutable reference to the salad. We're not modifying anything and it should return a positive calorie count. So I'm going to represent that with a u32 an unsigned 32 bit integer. And let's begin by walking through our simple fields which are protein and dressing. So I will start with protein. That of course is going to be an enum that supports a calories method. So I can invoke that to get the calories of my protein. I'm going to add that to the calories of my dressing. So here. Are self dressing that also implements the same caloric trait. And again the advantage of that trait is that it enforces consistency. So despite the fact that we have different enums, they're all going to support a calories method that should return a u32. And that's how rust knows that this is addable to this. Now with vegetables, we're going to need to get a little bit more creative because it is a vector of vegetables and each one of those vegetables has a calories method. So what I'm going to do is create an iterator, then invoke the map method so that I can progress or traverse over each vegetable and invoke the calories method to get the respective calories return value. And then that iterator of calories. I'm going to then call the sum method on that iterator to create the total cumulative sum of all of the calories. So it's going to look like this. I will refer to my vegetables field. That is going to be my vector. I'm going to call the Iter method right here. That's going to create an iterator of references. Then we're going to call the map method. Map as a reminder is going to take a closure of each individual vegetable like so. And on each individual vegetable right here I can invoke the calories method. That's going to give me the total calories for that given vegetable. We still have an iterator, so in order to actually get a value from this, I can call the sum method that is going to consume the iterator. And the only thing that sum is going to require is the provision of a specific concrete type for the generic. So it knows what kind of numeric type to store your sum as right, because you could do it as a U32 or a U16 and an i8, etc.. So right here after sum, I'm going to use my turbo operator to colons and my angle brackets. And to be consistent with this I'm going to ask for a U32. So now we're going to add our protein, our dressing, and the sum of all of our vegetables. All of those calories together should be the return value of the calories method. Let's see if that indeed gives us 685. And indeed it does, in case you ever get a little too. How would I describe it? Overconfident. And you say, wait a minute, I want to just double check that everything is correct. I always think it's a good idea to just get a failing test. So for example here, I can change this to 600 just to make sure. Okay. Is everything correct? Yeah, I'm promising 600 and my calculation is returning 685. That just kind of confirms in my mind that the test runner is doing everything as it should. All right. So that takes care of that. And now we have one final feature to add. And it is this define a has duplicate vegetables method. It should determine if the salad contains any vegetable more than once. So return a boolean. So as always let's begin with a test. Right here I can define my test using the RHS test annotation I can write a description like salad can identify that it has duplicate vegetables. I don't think my fixture is going to work well here, because my fixture has one of each vegetable, so I can just create a salad for the purposes of this test in line. So right here, here's my salad. I'm not really going to care about the protein or the dressing. So let's just pick an arbitrary value of steak here. And for my vector let's just do the simplest example. We can a vector that has a duplicate in it. So I will do a cucumber vegetable. And then this should be vegetable. And then we're going to do another vegetable of this should be cucumber. Apologies. This is also going to be cucumber. And then as I mentioned our dressing is arbitrary. So I'm just going to do Italian and provide my semicolon. And that should do it. So now I have a salad with duplicate vegetables. So given that I want to validate that my new method will return a boolean, I can just do the assert macro. That's a good tool. Here I can take my salad, invoke the has duplicate vegetables method. I get to choose my method name and its function signature. In other words, what parameters it accepts, what return value it produces. Before I even have to write my implementation, the test sort of forces me to slow down and think about how I want this method to work. I want this method to be called upon a salad. I want it to give me a boolean, right? I get to decide that right here. If I didn't have the instructions above so that I can write the desired output before I even write the actual implementation. That is the beautiful part of TDD. So put a semicolon here. Our test should fail in our terminal, and indeed it does. So let's go ahead and define our new has duplicate vegetables method. So right here scrolling up has duplicate vegetables I'm going to provide an immutable reference to self. The method should return a boolean. And there's I'm sure a wide variety of different ways to solve this problem. I decided to rely on a hash set, since it is a perfect tool in order to remove any kind of duplicates. So I decided to create a hash set and then insert or add every vegetable within my vector into that hash set. And if that hash set has a smaller length than my vector, that means that the vector must contain duplicates, because if every value was unique, then the hash set would have exactly as many entries as the vector. So I'm going to scroll to the very top of my file, and I'm going to utilize my use keyword to reach into my collections submodule within my standard library, and pull in the hash set type. The hash set is an unordered collection type that is capable of removing all duplicate values. It only allows unique values. So here what I'm going to do is start by once again reaching into vegetables. I'm going to clone that vector just to ensure I'm not dealing with any issues of ownership here. Then I'm going to call the interpreter method to create an iterator. And as you may recall, there is a fold method on an iterator that allows you to build up an aggregate or composite type over the course of a complete iteration. So what I'm going to build up here, which is my first argument, is going to be a hash set. So right here I can invoke the new constructor function and a hash set. And rust will require me to tell it what specific type the hash set will be storing. So I need to provide a concrete type for a generic. So right here after my first two colons I can provide a terrific operator. So it's actually going to be two colons. Then my angle brackets, then another two colons. And in here my hash set will store vegetables like so. And then the second argument to fold is my actual closure right here. So I can provide my closure syntax like so. The first value within the closure is going to be the accumulating or aggregate type, and it's up to you to return it in each iteration so that it progresses to the next iteration. So this will ultimately be our hash set. I'm going to call that data. And then I'm also going to receive in the closure the current iterator element that I'm progressing over, which will be each vegetable. And what I want to do within the body of the closure is I want to take my data, which should be my hash set, and I want it to insert every vegetable that I iterate over into it. In order to perform that mutative operation, I need to make sure that I label this as mutable. And then I also need to make sure that I return data right here. So as I traverse, I'm going to take the hash set. I'm going to push the current vegetable into it. And if there are duplicates, the hash set will simply not add the new value. All right. So that should do it. Then what I'm going to do once fold gives me the final hash set. I'm going to call Len on that so that I get an actual count of the total number of entries within my hash set. And then I'm going to check whether that is less than the length of my vegetables vector. And if it is, that means the hash set removed some duplicates, and that means that there were duplicates within my vector. So I can take self dot vegetables and call the Len method on that. Once I save that, you can see all of the violations go away and the code becomes a lot nicer. So once again create an iterator of my vegetables and clone it so we don't have to worry about references. We can just work with full copies of our original vegetable enums, right? Push all those vegetables into a hash set which will remove those duplicates. Take the length of that hash set and make sure that it is less than the total number of vegetables. If that's the case, then we do have duplicate vegetables. If it's not the case, then we have a unique number of vegetables. Right. Or every vegetable is unique rather. All right. So with that we can run cargo test and everything should be passing. And there we go. So everything looks complete. That completes all of the desired requirements and that's all there is to it. Once again, if you chose to utilize a non test driven development approach that is totally valid, it kind of is up to each person. But I do recommend that you experiment and try it out sometime or try out both variations, either tests afterwards or tests first. The worst thing that you can do is simply to omit tests, right? Because hopefully the value of this section has been clear. Tests help identify bugs and regressions in your code. Rust can execute all of these tests and assertions a lot quicker than we can manually instantiate all of these solid instances and validate that these methods, especially complex ones like this, are working as expected. We have the power of automation. We have the power of a programming language that can automatically run through all of this logic in milliseconds, and assert that our program is indeed delivering the value that it is supposed to. That is the benefit of tests. And with that, I will see you in the next lesson.
