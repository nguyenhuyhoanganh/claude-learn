# Documentation Tests

The third kind of category of test that we can write is a documentation test. A documentation test is one that is written directly within documentation. Comments. You may recall from earlier on in the course that we can use three forward slashes above any declaration in our program, such as a method in order to create a documentation comment. This is almost like an enhanced version of a regular comment, because we can execute a command called Cargo Doc in order to transform these documentation comments into browser documentation, so we can apply this syntax above our struct declaration, above our functions or method declarations, and all of that documentation will be gathered within an HTML website when we run the cargo doc command. It's a really simple, elegant way to use our comments to generate living breathing documentation for our code. It is within these documentation comments that we can write valid rust tests that cargo tests will execute. So the code we can write in here is going to be very similar to what we write within our test functions. They're just going to be behind these documentation comments. Unfortunately, as of the time of this recording, Rust Analyzer is not going to give you syntax highlighting or identify any mistakes within these blocks of code because it is treating them strictly as comments, as text to be ignored. In other words, so as you're writing out the contents of this lesson, just be extra careful because you are going to be writing valid rust code. You just have to make sure it is indeed valid. So with that said, let's write a few documentation comments above some sample functions. I'm going to begin with the new constructor function for museum within attractions says. I'm going to begin with a regular comment. So something like the description of creates a new museum instance. The cool thing here is every time I add a line break, VSCode and rust analyzer will automatically assume I am continuing my documentation comment and provide the starting three forward slashes on each line. Another cool thing we can do here that will be visually rendered within the browser is provide a section heading and it's going to appear in larger font, all nice and bolded. And the way we can do so is by providing a hashtag. You can think of that almost like a large heading. And what is very common after you've provided your description of the method, is to provide a heading called examples, and then provide some example usages of this function or method or construct coupled with some sample assertions. Right. So here's what that's going to look like because it's a lot of syntax all at once. First things first, in order to begin a code sample as well as a valid documentation test, I need to provide these three backticks in a row. Three in a row. Unfortunately, my VS code is going to try to complement each opening back tick with another one, but I need a total of three. And that marks the beginning of our test function, so to speak. The beginning of our code sample. We're also going to designate the conclusion of our test function with the exact same syntax of the three backticks. Anything between still needs the three forward slashes, but will constitute the code belonging to the test function. So what I need to do here is I need to provide an example of how I would use the new constructor function. Now, interestingly enough, the museum name is not going to actually be available here within this context, despite the fact that we are within the attractions module and museum is right here, we're not going to have it immediately available within here. You can almost think of this like a separate nested module. So the first thing we need to do is to pull in the museum struct name into this scope. So I'm going to utilize the use keyword. This is valid rust code I'm going to start with my testing. Create route reach into the attraction submodule, which is the file or module that I am currently within, and access the museum struct. I'm literally accessing this thing right here, but now I technically have access to it within this test. Once again, no syntax highlighting, so it's a little bit difficult to write, but we've now pulled in museum into this context. So now let's write a simple test. I'll create a museum instance and assign it to a museum variable. So Museum colon colon new. I am providing a example of this exact functionality. And what I want to assert is that my paintings field should be an empty vector, and my revenue should be equal to zero. So for the empty vector, I'll declare another variable called empty vec, and on the right hand side I'll declare a new vector. I will also need to provide a type annotation, because rust cannot infer the type of an empty vector, or the type of element that is going to hold. So here I'll say this will be a vec holding strings. Then here I'm going to use my regular assert equals macro. This is a valid place to write our tests. Right. So I will access my paintings field inside my museum. And I will compare it to empty vec above and on the line below. I'm going to write one more assertion of equality using the assert equal macro. And that is reaching into the revenue field and comparing that to the value zero. So once again after you write this, just be very careful. Make sure that all of your examples are correct and written out with no syntactical errors. Now you'll notice immediately this button pops up. It says run doc test, which means it recognizes this chunk as an example of a doc test. It's just a regular test, but written within a documentation comment. So I can click this and execute this. And if we wrote all of our code correctly, we can see everything is passing as expected. Just to show you an example of a failure. I'll update the 0 to 1. That should be a mismatch. And when I rerun it, you're going to see we have a failure. Once again left says zero. Right says one. The exact same syntax that you would see using cargo test and the regular assert equal macro. All right. So let's practice one more time using the syntax. And then afterwards I want to show you not just cargo test but also the generated documentation. So let's write one more example for the by painting method. Below will be a little bit quicker here. So first things first three forward slashes to create a documentation comment. Usually it's a good idea to provide a description before you dive into your actual test assertions. So here I will say buys a painting for the museum. I'll create some space here, I'll designate a heading, and I can do that with a hashtag and say examples. You're going to see this visually appear in the browser documentation in just a moment or two from now. And then to actually designate my test, I'm going to do those three backticks. This is the key, by the way, to the left of the one on my keyboard. And again we need three to both begin it and then to close it off. Right. And then anything between is our actual test code. So same principle as above. I need to actually bring in my museum struct into here. And then I need to create an instance so I can copy this code from above. And this time around, if I want to demonstrate how by painting works which mutates the museum, I need to make this a mutable variable. And then down below I can take my museum and call the by painting method on it and buy a painting like Mona Lisa. And then I can assert the equality between my museums paintings field, which should be a vector. So here I'll create a vector in line with the vec macro, and it should have a single field of Mona Lisa. But it's going to be a heap string. So I will call the two string method right here. And that should do it. So once again I'm going to press this button and run our doc test. Once again everything is passing. Now I just want to show you what a full run of cargo test is going to look like. We're going to see the documentation tests appear in our output. Rust views the unit tests and the integration tests as functionally similar. And it looks like I didn't correct this thing right here. There we go. It views the unit test and the integration tests as functionally similar. So it's going to show you all of the unit tests coming from libraries. It's going to show you the integration tests that it's finding from the top level test directory. And then there's going to be a separate section or category for doc tests. And you're going to see the exact same summary. How many tests were run, where is it coming from, what file. And you can see right here the actual designation of the test. Since there was no explicit name provided, there was no test function. It's just going to show you the module. And in this case the struct and the method above which we wrote the documentation comment. And everything right here is passing. So you just saw an example of a failing documentation test and an example of a of a passing documentation test. All right. Now the next thing I want to show you is how we can build the documentation. I want to also note that when you build the documentation, interestingly enough, that command will not run the documentation tests. Only cargo test will run the documentation test. So if there are mistakes and you leave them in your documentation, you are responsible for them. So make sure all of your documentation tests are passing with the cargo test command. Before you build and publish the documentation with the cargo doc command. So what I'm going to do is I'm going to run cargo Doc, which creates some web browser documentation within an HTML file using the documentation comments in our code base. And then I'm also going to add the setting of Dash dash open, which should open the resulting documentation in your web browser of choice. So for me that's Chrome. I'm going to drag over the generated web page. It should look like this, which is very similar to the rust documentation as well as documentation for just about any crate you're going to find. Defined because all of those tools are using documentation comments under the hood. And now notice if I click into attractions and I click into museum. Here is the visual representation of the documentation. Comments. Notice here is our first description. Here is the nice bold heading that was created by us using the hashtag. And it looks like with this, we can even allow ourselves to provide a sample URL that will always lead us there. So when, for example, you publish this online to create that, I know you're going to automatically have generated links to exact sections within your document simply from the benefit of cargo doc creating all of that for you. But because we also used the three Backticks which created our documentation test, we also have valid rust code with syntax highlighting within this generated documentation. Now we can't actually run our test in here, but even this code example gives a user a sense of what it is that this given function or method is accomplishing. Right. You can see, oh, when you create a brand new museum, it's going to have a paintings field and a revenue field. And here's what it's going to be equal to. And you can even click this clipboard over here to copy that code to your clipboard. And if you explore any part of the Rust Standard Library, you're going to see this pattern a lot because they are using documentation tests. Right. So you're going to see, for example, a documentation for the option enum or the result enum. And in all of the examples they're going to give you some valid rust code that happens to be using a assertion. Right. An assertion like assert EC or just the assert one. And you might be thinking, why are all of these code samples using these test based assertions? And that is because they are writing documentation tests within that source code that will be number one executed with cargo test, and then serve as the basis of the documentation that cargo doc will generate for them. So you kind of kill two birds with one stone there. All right. So that is a quick introduction to documentation tests within rust.
