# Section Review

Congratulations on reaching the end of another course section. As always, let's take a few moments to review everything that we learned. In this section. We focused on testing in rust. A test validates that the source code works as expected. We are writing additional rust code whose purpose it is to validate that our main code works as expected. A test is a regular rust function that consists of one or more assertions. An assertion is just a verification of truth. It is us declaring that some statement is valid could be an assertion of equality or inequality, but rust and its test runner are going to validate that our assertions are met. So tests help to identify regressions in the code or in other words, bugs and mistakes. So a regression is technically a bug that has been introduced into working software and where tests can help is they provide automation. You don't need to manually run the code yourself, especially as your system or your business domain grows in scope and complexity. Rather, you can use the power of automation. Because rust and programming languages are much quicker, they can run through thousands of assertions within a few minutes and validate that everything is working as expected. So more specifically, a test is a plain rust function, and we annotate it with that test attribute on the line above the function name. An attribute as a reminder is metadata for the compiler. It changes how the compiler will parse the code that follows. So earlier on in the course, for example, we introduced derive debug and that is an example of another compiler directive or attribute. It informs the compiler that it should provide a default debug trait implementation for the following type. Right? So this attribute tells the cargo test command when we run it that this is not a regular rust function that's meant to be executed by the end user. It is specifically a test function. Now, within that test body, within that function code, we're going to typically involve some use of our source code. The actual core thing that we are testing alongside with our hard coded expected value. So we know what the results should be. For example, if we invoke a function, we know what it should produce, and within the test we can run that function and compare what it outputs with our own expectations. And of course, the two should match if the function is doing what it's doing. So once we have our tests written, we go to our terminal and we execute cargo test or the shortcut cargo T. And that's going to execute the full test suite. It's also going to run those tests in parallel on separate threads. We haven't talked about multithreading in rust yet, but basically rust is going to parallelize all that work. So all of those tests are running basically at the same time concurrently. And the important takeaway there is that your tests should be independent. You should be able to run them in any random order and have each one be an isolated, independent chunk of value. You don't want any tests to be dependent on any other test having run first, right? Your test should be independent and isolated and be capable of being run in any order. So there's many different assertions. As we mentioned, for different verifications of truth. We have the assert equal macro that validates that it's two values are equal. And those values must implement the partial EQ trait, because that is what allows for the equality comparison and the double equal sign as well. Behind the scenes we have the complementary not equals assertion, which is assert, not e that will pass if the two values are not equal to one another. And finally we have the assert macro which accepts just a single argument, and it validates that its argument is true. So this is some assertion of truth for all of these macros. We of course have to end them with the exclamation mark. And then they also accept a final optional argument, which is a custom error message to print in case of test failure. That's actually one advantage of macros in rust compared to plain functions. Functions in rust cannot accept optional arguments, but macros can. So it's up to you if you want to provide a custom error message that will provide more business context on what exactly a failure represents. We also talked about you should panic attribute once again like test. We write it above our function declaration as an annotation and that will validate the code within your test function does indeed create a panic. So that is if you basically want to validate or assert that some amount of code will cause an error when it is executing. All right. So in rust we have different categories of tests. And the first one we covered were unit tests. Unit tests are written directly alongside the implementation code, right alongside the actual core source code and a unit. That exact terminology is somewhat debated, but I like to think of it as a super small, isolated chunk of responsibility, like, for example, a single function and its return value. So typically, what we do to distinguish the test code from the source code is we nest all of our tests within a tests module. So we use the mod keyword, then tests, then our curly braces, and we write our tests in there. Technically speaking, the module named tests is completely arbitrary. You can choose whatever name you want, but it makes sense to follow the community convention of tests because that is all that module is going to contain. Now above that module declaration, since you have that nice wrapper container, we typically add this attribute of cfg test. CFG is short for configuration and test says only include this code within the test configuration. In other words, what it tells the compiler is please exclude the tests and all of that code. When you are building the main executable from the source code, there's no need to ship the test code to the end user who, for example, wants to utilize your library crate. It would only increase the total size of their final program. Tests exist for the benefit of the developer, right? And thus we do not want to include that code within the final produced executable. Now, since you are within a nested test module, you are within a different scope and thus your tests are not automatically going to have access to any named constructs from the outer parent scope, such as your structs or enums or functions. So what we do is we use the super keyword, which is just super, then two colons. And then we typically apply the glob operator which is going to be the asterisk. And that allows us to automatically bring in all the names from the outer scope without having to explicitly list each one one by one, Typically, the glob operator is discouraged, especially when you use it with the use keyword at the very top, because then you're never quite sure what you're bringing in. And there might be things like name collisions, but in this scenario, it's actually a wonderful tool because as you add more named constructs in the outer module, as you say, add another struct. Well, then that name will automatically be pulled in to your tests module and available to be referred to within any test that you write in there. All right. So now you have your tests written. As I mentioned you're going to run cargo test. And that's going to give you a nice summary. It's going to tell you which tests passed, which tests failed. It's going to give you a count of each. It's going to give you the full name of each test, which is typically going to include its module as well as the full test name from your function name. And then for any failing tests, which is the ones that you care about, it's going to show you the output. So for example, if you have the assert EC macro for equality and the two arguments do not match, then the test output will show you. Here's what I expected, and here's what I got. And those two arguments in the output are called left and right. Once again, you could just think of them as the two arguments to the respective macro, the two different values that you pass in, one of which represents the result from your source code and one of which represents your expected result. All right. We also explored some popular community crates that can assist us in our rust testing. The first one we learned about was pretty assertions. For any of these dependencies, we're going to open up our top level project setup file, which is cargo.toml, and then under the dev dependencies section, we can list the crate and its version number. These are development dependencies because once again they're there to assist us as rust developers. They're not source code that should be bundled with the final executable code that we produce. Rather, they are collections of rust code that are there to help us. So the assertions crate, for example, gives your assertions a much more pretty look, right? That's where the name comes from. So for example, whenever there is a Non-match inequality, it's going to give you a better visual output with more color, with more line breaks, things like that. They make it easier to visually identify and parse the differences between your unequal values. So we utilize the use keyword. We bring in the assert equal macro or any complementary macro within our test module, and that automatically overrides Rust's default macro. Rust's default assert equals macro, and once again, it just gives our output a little bit more visual flair. The second testing crate we talked about is SES tests, which assists us with creating fixtures which are preconfigured instances of a given type. So it can get pretty laborious and verbose to have to keep creating the same instance within a test. In our case, we had our museum. We wanted to have a museum with three paintings, which requires the invocations of the by painting method A total of three times that can get pretty laborious or wasteful. So a common regular solution would be just extracting that instantiation to a separate function that you can invoke. And we could do that manually. But of course the CES test crate helps assist us with that and facilitate the kind of automation of that fixture creation. So we annotate a fixture function, which is just a plain rust function that returns an instance with the fixture attribute. And then we update our test function to have the CES test attribute instead of the plain test. And then we provide the fixture function name as a parameter to that test function. And CES test will identify that you are referring to a fixture function. It will automatically invoke that function, receive that return value which will be the fixture, the instance, and make it automatically available as that given parameter. So it kind of automates the creation and the invocation of those fixture functions. You are also welcome to use a fixture function within another fixture function, which allows you to have this really elegant design where you can have lots of small fixture functions that are each responsible for one unit or one struct or enum, let's say within your source code, and then mix and match them. Especially as a lot of these types start interacting with each other, it's often beneficial to not just have one fixture function. That creates a lot of instances within itself, because then it's kind of hard to decouple all of those fixtures working together. If you create lots of small fixture functions, it's a lot easier to combine them later on in a given test. All right. Another thing we talked about is an alternate syntax for our tests, which is instead of writing the typical assertions like assert EQ, we can instead use the result type, the result type we're pretty familiar with by now. It models an evaluation or an operation that could be either successful or erroneous, and that makes it a great fit for a test. So we update the test function to have a return type of a result, and you have to declare the two generics for TNT. Because remember there are the two variants okay. And er and they both need a concrete type for their generic. And then if we want to indicate a test passing we return an okay variant. If you have nothing of value to produce a unit is a common piece of data to include within the okay variant, because the variant needs something to store, right? And then vice versa. If you want to indicate that a test is a failure or should be considered as not passing, you instead return the error ER variant. And that also needs a custom piece of data. Typically that's going to be a string that describes the failure. And in the case of a test failure, that custom error message will be printed in your output. All right. So as I mentioned a unit test is a smaller, usually faster, more lightweight test that is testing for a small independent chunk of value. Maybe a function or a method, or even a struct and integration test is larger in scope. It tests a greater feature or the interaction of multiple units within the system, and these tests are not intended to be either or. It's not like one is good and the other is bad. A good code base should have a good combination of both of these, right? Unit tests tend to be more lightweight. They tend to be less flaky because they're not testing as many things working together. But if you only have unit tests, then you're not testing the greater whole. Integration tests tend to be a little bit slower. They involve more moving pieces, so they're more prone to flakiness because one thing breaking can break the entire feature or system. But that's exactly what that integration test is meant to identify. Right. So for these integration tests, the community convention is to create a top level test directory at the top of your project. And in there you're going to add your plain JS files. They're going to be your test files. And the syntax is otherwise going to be the same. You're going to have regular test functions with assertions within them. Now, in these test files, there's no need for a separate module called test or any module whatsoever, because the cargo test command understands anything within the test folder as being specifically dedicated to tests, so it shouldn't include any of the code in the final executable there by default. Now, it's important that you actually make anything that you're testing public or exportable by marking those contracts with the pub keyword in order to be able to pull them in and test them within your integration tests. And that also serves the benefit of the library crate, right? Because anything that you're making public or exposed to other users who are going to be downloading your library, crate your community code, well, that's the most important code to be tested by you, right? That's the code that's going to be the public interface or what other people are going to be utilizing from you. The private things are not as important to test because you can think of those more like internal implementation details. But the things that you want to make public are the ones that are going to be used. That's the ones that are most critical to test, right? So you shouldn't make something public just for the sake of a test. You should make something public because it's going to be some interface that somebody can work with your crate with, right? And that's what's important to test and pull in within your integration tests as well. We also talked about documentation tests. We create those with a documentation comment which is written with three forward slashes. Basically creates a long section or block of comments. Right. So within that sequence of documentation comments we can use a hash tag to create a section header. And we can use those three little dashes if you will. Or Tildes I forget the exact name to mark the beginning and end of a rust code sample, and that code has access to all of your assertions so that you can run some validations. Now to actually run the documentation tests, you run cargo test, and there's going to be a separate category there for your unit tests, your integration tests and your documentation tests, and then you can run Cargo Doc to actually generate the browser documentation. That is the primary benefit of using documentation comments. That is not going to actually run the tests themselves, but your code samples indicating how a given construct is meant to be used will be included within your final published documentation. So somebody can see how a given struct is meant to be instantiated. And that assertion of equality that you write, for example, will tell the user what a given method is supposed to do, right? So you'll notice in even the rust standard library, and in most crates that have good documentation, the actual documentation for a given structure method is going to include assertions, because those assertions are a good way to demonstrate what is the value or purpose or expected behavior of a given construct within your program. All right. We also talked about a pattern called dependency injection. That is a design pattern. In other words, a recommended way to structure code where a given type will receive its dependencies from the outside world rather than creating those instances itself. So, for example, if you have a constructor function like new, if you hard code a specific dependency in the body of that new function, well, then you're coupling that type to that dependency. There can be no other type that can be created because you're literally hard coding just one given type within that creation, right? So with dependency injection we typically are going to feed in that dependency, that thing that the type needs in order to work through a parameter that allows the outside world at the point of instantiation to provide or inject that dependency in, which also allows for that dependency to vary, right, to be able to be customized. And that way you're not hard coding or coupling or designating just one thing that that type can rely on, but actually a variety of inputs, a variety of dependencies that can be injected. Now it's good to enforce some amount of consistency, like for example, a shared collection of methods. And you can do that by utilizing traits. Because remember a trait is not a concrete type. It is like a contract that mandates that a given type or multiple types live up to the same method interface. So now you can use traits to promise some amount of consistency, right? To promise a common method that you can invoke on whatever dependency you pass in. Even if that exact dependency is subject to variance, you can depend on a consistent method on it, and you can add that constraint to say it's not just any kind of type you want to pass in here. It's some type that promises to uphold some kind of behavior as mandated by that trait. Now, the reason I introduced the design pattern of dependency injection in this section is because it also simplifies testing, because then tests can pass in their own injections, their own dependencies, their own dummies or replacements for the real world dependencies, which allows you to decouple the testing of your current type from the dependencies that it relies on. You don't have to force everything to be an integration test, and if you do not use dependency injection, you're sort of going to force that. Because if type A relies on type B and type A creates type B in its constructor, there's no way to separate those two moving forward. With dependency injection, you allow type B to vary and can thus pass in your own custom type within the test that is good enough to play the role and allow the test to fully focus on testing type A by itself in isolation. All right, so there's a wide variety of ways that we can customize the actual command line arguments that we pass to cargo test both to the exact command itself and also to the actual executable that it produces. Remember, for those arguments that you want to pass to the compiled test runner, you provide another pair of dashes and then you actually provide the command line arguments after that. So for example, we can provide a search term directly after cargo test to filter tests by description or by their name. Right. You can add the dash dash then the dash dash show output to show the standard output from content printed to the screen, like with print line. Even in the case of a test passing, because by default cargo is only going to print content from failing tests. But if you want to see a print line so you can, for example, observe the current state or value of a given type. Even in a passing test, you can customize it with the show output setting. It can also provide something like dash dash lib to run only your library test. Now we'll ignore integration tests in the higher level tests folder. And then there's this super special command that I have a very hard time remembering. But it is dash dash test with single quotes and an asterisk. And that is going to run only your integration tests in the test directory. And I believe there's another setting as well if you only want to run documentation tests. As always, by default I just recommend running the full test suite with cargo test and then filtering down. Once you get to a much larger project where you don't want to wait for all those tests to finish running. Finally, we concluded by talking about test driven development, also known as TDD. This is a testing paradigm, once again a recommended way of writing code, and it encourages writing your tests first before you actually write your implementation. So TDD follows what's called a red green refactor cycle and the red phase. The failure stage consists of writing a failing test. The test, of course, is going to fail because the code isn't written yet, but you are describing the requirements, the value, the expected output, and it typically will force you to write a simpler test with better setup. Right then in the green phase, you make the test pass. And then finally, now that you have the validation that everything is working as expected, then you improve the code by refactoring it, and you have that test and the passing of the test as the validation that you've introduced no regressions or breaking changes into your code. There's many different advantages of TDD. As always, I think the best way to see them is to try it out on a project. You don't even have to try it out on the whole project. Just try it out for a day and see how you like it. But there's many different advantages that I found in my time working with it, including good test coverage, which is guaranteed because your tests are going to come first. They're not going to be neglected. You're typically going to have better test design, because you're going to be focused on small units of value that you are testing. You're typically going to have better source code, better actual implementation code, and the simplification of complexity. So for example, a pattern like dependency injection, you're much more likely to arrive at it naturally if you were to follow TDD. Because once again, if you're writing a test for type A and you realize, hey, I'm going to need to pass in some kind of type B, well, in a test where you're your testing type A and you don't yet know about the existence of type B, you're much more likely to create that type B dependency or the replacement or dummy for it in the test, because the code hasn't been written yet, right? So you're much more likely to micro focus on exactly what it is that you're testing now, and much more likely to lead to better designs. Again, not just for your tests, but for your final implementation. It's hard to make a lot of these theoretical arguments when you haven't had the chance to practice TDD yourself. And I'm also not going to be like a dogmatic person who says you should always do TDD. There are some very hard code evangelists of this methodology, and I think TDD does have value, and I just recommend that you do try it out for yourself in rust or any other language when you have the opportunity, because I do think it does generally improve the quality of your code, not just your test. All right. That's all there is to cover in this section. I hope you enjoyed this discussion on testing in rust. And with that, I will see you in the next lesson.
