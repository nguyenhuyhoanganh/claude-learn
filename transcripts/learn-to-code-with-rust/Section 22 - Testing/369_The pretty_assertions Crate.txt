# The pretty_assertions Crate

In this lesson, we'll introduce the Pretty Assertions Community Crate, which has over 60 million downloads on Crates.io. I could provide my own description of this crate, but I feel that the official documentation does a great job describing it, so I'll rely on it instead. So here on the official documentation page, you can see when you are writing tests in rust, you're probably going to be relying on the assert macro a lot. If a test fails using this macro, which will be due to inequality between the two arguments, rust is going to present all of the details of A and B, which we've also called left and right. But you, the developer, have to spot the differences yourself, which is not always straightforward. So here is an example of the type of output you'll see from the assert equals macro by default. In this case we have the option enum. More specifically, the sum variant and its associated data is a foo struct with various fields, and some of those fields have different values. For example, this Lorem field has a different word here. Here's world, and here is a deviation from that. The ipsum field matches and then dolor is the okay variant. But the associated data within it is not equal. So the two are not equal. But it is visually difficult to identify the differences between left and right. And so it asks us wouldn't that task be much easier with a colorful diff. Diff just means an output that is showing us the differences between the two inputs between left and right. So here you can see we're going to see a output that includes color first of all. And then using these two identifiers left and right we can actually see the pieces that differ and what the values are for the respective inputs. So this one of left and red says the left input. The first argument to assert equals has this content. And the right input in green has this content. And you can even see the letter O within world is highlighted in a more bolder green, so you can visually see that it's actually here that the differences occur. Here you can see as well that it's the space and the letters h o exclamation point that differ in the write output for this dollar field. Right. So the elements that are match are going to be printed out in this nice regular white color. And the ones that don't match are going to be more easier to visually identify. So it's literally called pretty assertions because it makes the assertion output prettier, right? More easily distinguishable as far as the differences between the two. So the good news is you only need one line of code to make it happen, and that is adding this pretty assertions crate as a dependency to your project. And then instead of using the built in macros, we simply bring in the macros that we want to use from the crate instead. And they function the exact same way. They just print the content differently. It's important to note that this crate supports the assert equals and assert, not equals macros, or rather, it is the equivalent that the crate. Offers for the built in ones in rust. Right, so these macros are named the same, but they are totally different implementations of the macros that give us a different output. The assert macro is not supported as of the time of this recording. So how do we set this up? We're going to specify this crate under the dev dependencies section of our cargo.toml. And then we're going to add this code. And that should be all that it requires. So at this point I'm going to transition to VS code. And we're going to head to our cargo yml file. It is here where we configure our dependencies and our developer dependencies. Our developer dependencies are those that we need as developers. But once again they do not need to be added to the final code that our executable will produce. Or if we have a library crate as we do here, if somebody else pulls in our code, such as the museum struct, they do not need this dependency either. So the difference between this dependency section and the new one will declare here in square brackets, which will be dev dependencies. Is these dev dependencies are strictly for the sake of us, the developer. They should have no impact on the final executable, the final code that we're going to be creating in our project. So here is where we put something like pretty assertions because once again it exists purely for the purposes of testing and developer benefit. Right. So under here I'm going to write the name of the crate, which is pretty underscore assertions in all lowercase. Then a space and an equal sign and another space. And here I'm going to designate in quotes the exact version number that I want to pull down of this crate. And I recommend you follow along with the version that you see here, just to ensure there are no conflicts between the video and the code on your end. So I'm going to write the version number 1.4.1 and save my file. All right. So now we can transition back to live. And within my test module here what I'm going to do is add my use keyword in order to create a shortcut to something from the Pretty Assertions crate. It works the exact same way as we would use the use keyword for, let's say, a name within our own project. In fact, we explored the idea of pulling in library crates way back when in the project structure section of the course, but I am going to add my use keyword here within my tests module, because it is protected by this configuration attribute, and once again ensures that what we pull in here is not going to be included in the final executable that will be produced when somebody uses our library code or in other words, our museum struct from above. So here same syntax use, then pretty assertions, then two colons and I can pull in whatever I want. I am no longer using the assert not equals macro. So I'm just going to pull in assert equals. Now notice here the name as it is pulled in does not actually include the exclamation mark. It's just assert equals. But once I save, that's all there is to it. The way that this macro is used is the exact same way that the regular assert equals macro is used. So it's just a nice plug and play variation that is going to continue to work with all of our code. So here I can run cargo test and all of our code should continue to work just fine. I can in fact force a failure to make sure that the pretty assertions crate is working as expected. So I will change the revenue here, and you're going to see the colorful output, which is a good indicator that it is working as expected. And here you can see it is visually highlighting the differences between the five and the four. Just to show you a better example, what I can do is bring this back, get us back to a cargo test that passes completely. I'm going to write a sample test here that's going to quickly compare two hashmaps. You don't necessarily have to follow along because I'm going to delete it very quickly. But just to show you the visual differences in how they can very quickly help, right here I'm going to write a test function called hashmaps. And in here I'm just going to create two hashmaps. And they're going to have some keys and some values in common. So very quickly I'll declare a mutable HashMap called one. I'm going to reach into my collection submodule, access the HashMap type and create a new HashMap. I'm going to quickly call insert here and insert a key of A with a value of two, and on the line below, I will insert a key of B with a value of three. Then I'm going to copy and paste this code below and create another hashmap called two, and on my two HashMap I will similarly insert a couple key value pairs. I'll do a key of B set to three, so that is a key value pair that is going to match up between 1 and 2. But two is also going to have its own unique key value pair, which will be a key of C and a value of four. And then I'm going to assert equals and compare the equality of one and two my two different hash maps. First up I want to show you the regular assert equals macro. So I will comment out my use syntax for the pretty assertion scrape. So now we're going to rely on the default assert equals macro from rust. And you're going to see here it is going to identify that our two hash maps are not equal. But you can see the output is not particularly helpful. So in this case you can see the keys and values actually line up. But if a hash map is not ordered you can actually see different outputs here. So I'm going to try running cargo test again. Let's see if we get something different. There we go. You can now see the B keys no longer line up right. So now it becomes harder to visually identify. Oh B is actually present as a key in both hash maps. And it has the same value here. And then we have to identify the differences between the key of A and the key of C, etc.. Right now I'm going to bring back in Assertequals from our Pretty assertions crate, and now you're going to see a world of difference when I rerun cargo test. So immediately you can see B is always going to show up here in the center, and it's going to be in that plain white font once again indicating that there is no difference between left and right, or equivalently, are one and two. HashMap. And then we're going to see the left hash map corresponding to the first argument passed into assertequals. It's telling us here hey, it has a unique key value pair a key of A assigned to two, and with the right syntax in green, it's going to tell us the second argument represented by the two HashMap that has its own exclusive key value pair, which has the key of C and the value of four. So immediately it's easier to see the difference. And this output is always going to be consistent. Perhaps it's not. I apologize because of the inconsistency in order in a hash map, which is technically unordered, but still using the left and right identifiers, you can see the visual differences between the two much more easily. All right. So that is the pretty assertions crate. And I'm going to be relying on its macros over the default ones in rust moving forward throughout this section.
