# The should_panic Attribute

Within our test configuration, we can assert that a piece of code within the test should raise a panic. We do so using a special annotation called should panic and we write it directly above our test. So this can be helpful if you want to validate or assert that some given branch of logic indeed raises a failure or an error, you want to validate that something goes wrong as expected. So let's update our by painting method here to panic. If we try to exceed the museum's maximum capacity of paintings, and let's say we arbitrarily decide that the museum can store a maximum of three total paintings. So the way that I'm going to implement this, there's a wide variety of ways. But within the Imple block, I'm going to declare a constant. I'm going to call it maximum capacity in all caps. That is the expected practice for a constant. And as a reminder for a constant, you need both an initial value which will not change, as well as an explicit type declaration. So I will make this a u size, an unsigned integer whose exact type will vary depending on the operating system, and here I'll declare a value of three. Then within the body of by painting before I push on the new painting as a string, I'm going to do an if statement check, and I'm going to check if I have a number of paintings, which I will calculate by calling the Len method on my vector. If that count of paintings is greater than or equal to my maximum capacity, and I can access that piece of data by starting with capital S self, which again is in reference to the current type that I am within for the Imple block. So this is the museum. So once I reference self, if I use my scope resolution operator, the double colons here, I will be able to access my maximum capacity Constant. So if I already have three or more paintings at the time when this method is called, I want to panic, which I can do with the panic macro. As a reminder, panic will cause your program to effectively exit or crash. It's going to print out an error in the terminal. So here I can provide a custom error message to output like Museum does not have storage space for another painting. The question is how do I test this logic? How do I test that? When I invoke the by painting method, I should see this panic. Assuming that this condition is met because if I do not check this and I write a unit test only for this, then I am not fully covering all of the possible functionality of this by painting method. So what I'm going to do is add another test. In fact, I'm going to copy this test right now because it has a good setup where we already buy three paintings. So I'm just going to copy and paste this test below and adjust it slightly. So first up, let's change the name here to Museum prohibits adding painting when capacity has been reached. We can get rid of the final assertion right here. So now we have a total of three paintings. And that's totally valid. When I attempt to add a fourth one and invoke by painting a fourth time, that's when we should hit that if statement up above. So let me provide a sample string here like water lilies. And I can show you that at this point we are going to get a panic when the test runner is executing this code, and I believe we're going to see a failure. You can see we have a failure simply because we ran into a regular panic. It's almost like a program crash, which you can think of as crashing the test, so to speak. So our other tests are still passing with okay, but this test is failing not because there are any unmet assertions, but simply because it's effectively crashing. It's mirroring the real world because this would be the be the exact same situation if we were to write this code in something like a main function. So what we want is some kind of way to tell the test runner that this should panic, that this is exactly what we want to validate. Don't fail the test if it fails as expected. If the panic is the one we expect, that is a good thing. So to speak, because we are validating that the code works as expected. So below my test annotation or attribute I'm going to add another one and it's going to be the exact same syntax a hash tag and a pair of square brackets. And this is going to be should underscore panic. Just like the name suggests. It's a test that should raise a panic. So the way that this test will fail now is if it does not raise a panic, if this code that runs in the body of this test function does raise a panic, then we are honoring the promise we told rust to expect here. So now when I rerun cargo test, we're going to have a passing test suite because you can see the test runner is expecting the code that we wrote in here to trigger a panic. There are no assertions. There are no macros in here. But this is really kind of the pseudo assertion, if you will. This is the thing that we are validating in the body of this test. Now, when we run our tests, sometimes we might see the actual output from the panic directly in the output. I've seen it work both ways, but in case you're curious, if you ever want to silence any panic outputs in the course of a test run, you can add the following command line argument. It's not actually going to be an argument to cargo test directly. It's actually going to be an argument to the binary executable that the cargo test compiler command generates. So as a reminder about that we're going to do two hyphens right here then a space. And that ensures that the follow up command line arguments go into the actual executable program, rather than serving as command line arguments to cargo test directly. So I wanted to provide a setting here of dash, dash, quiet and that is going to silence or quiet. The output basically prevent the printing of various assorted error messages and things like that. So if you're ever seeing a panic right here, perhaps even with a proper assertion like should panic. You can always try that. It's just going to remove some of that noisy output to help you better understand what is being printed in the terminal. Another thing that you can do to this should panic attribute is treat it like a macro or a function with an expected parameter. That parameter is literally called expected, and what it allows us to designate is specific text that the error message should contain. So the test runner is going to look for the specified substring that we provide here, right here in our parentheses. And it's only going to pass the test number one if it panics. And number two if the panic message contains the content that we provide here. So this can be helpful if there is the potential for multiple panics, each of which has a different message. And you don't want to just assert on failure, but assert on a specific failure. So what I'm going to do here is write out the word expected, and that is the expected string content, or substring that we are looking for within the panic. And I'm going to look for the words storage space. So as a reminder if I scroll up here, we wrote the message museum does not have storage space. So we are validating number one that we get a panic and that somewhere in the printed panic error message we have the content storage space. That is what we are asserting right here. So of course, if I run this test again we're going to have a fully passing test suite. But if I add something like a Z in here now, we still have a panic as the result of running this code, but not a panic that contains this expected substring. So that is a failure, because we are no longer fully living up to the expectations that we set out for the test. So you can see right here it says expected substring storage spaces was not met. It's going to say panic did not contain the expected string. We did receive a panic message and here it is. That's coming from our own implementation. But the test runner is unsatisfied because it did not contain this exact substring. Sometimes this syntax here, where we actually provide the parameter name, so to speak, of expected along with the equal sign and the value. This is often called a keyword argument. And again it's a slightly different syntax from invoking a function or a method in rust. Because when we invoke a function or method, we just pass in the arguments sequentially. We don't actually have to write out the names of the parameters as they were defined in the function or method definition. Here we do need that. We need to write expected and saying this is the expected substring in the contents of the panic. So I'm going to remove my z here. That was just for the sake of example. Run my test suite again and now we are back to a fully passing test suite. And that's all there is to cover in this lesson.
