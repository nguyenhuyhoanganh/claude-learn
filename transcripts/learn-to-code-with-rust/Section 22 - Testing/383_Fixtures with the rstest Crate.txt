# Fixtures with the rstest Crate

In this lesson, I want to introduce another cargo crate called R's Test. The R's test crate adds macros that enable the creation of fixtures. A fixture is just a pre-configured instance of a type within our program. For example, let's say that we consistently want our tests to have access to a museum instance that holds three paintings. It can get pretty laborious and verbose within our code to keep having to create a museum instance in each test, and calling the by painting method three times to create that specific instance. Now we could extract that instantiation logic to a helper function whose responsibility will be to create and return that predefined instance. That instance is what's called the fixture. And that's basically what the says test crate helps facilitate for us the creation of those helper instances through the use of functions. So let's begin by heading to our Cargo.toml file. And here underneath the dev dependencies section I'm going to add the new developer dependency which will be our test all lowercase. And let's go with the version number 0.25.0. Save that file and that should do it. And then we can transition back to our venue management integration test file. So at the very top here I'm going to bring in from Rs test to available macros slash attributes. And they are called fixture. And our test. And they're both going to play a role. So the first step in order to create a fixture what we do is we declare a basic function that will return an instance of something. And then we annotate that function that's going to produce that instance with this fixture attribute. So right here I'll declare my function. I'm going to call it Museum with three paintings, because this function will be a function that will simply create an instance, and more specifically, it will return that museum. Now this is the important part. Immediately above this function, I'm going to add my attribute, just like we had it with test here. So I'm going to use my hashtag and my square brackets. But this time around it's going to be fixture. And that is critical. So that SES test can understand this function as one that represents a fixture. And we're going to see how it applies to a test function in just a moment. So in here let's write the basic instantiation code for a museum with three paintings. So I will create a mutable museum. And I will take my museum struct and call the new constructor function. And I can invoke the by painting method three times. Let's buy a painting of Mona Lisa. And I'm going to copy and paste this line two more times below, And then we can swap this with the Starry Night. And our final painting can be girl with a Pearl earring. And then, very importantly, I can return the museum instance. All right, so now we have a fixture which is basically a function that returns a preconfigured instance. Now here is where the magic happens. The first thing we need to do is for any test function that we want to be able to utilize a fixture, we need to swap this attribute from test to RHS test, which is the second thing that we brought into scope right here on line number two. Now what that gives us is this superpower. If we were to use the fixture functions function name as a parameter within this test function, rust and Rs test will automatically run that fixture function and create that instance for that parameter automatically. So what I mean by that is if our function name here is museum with three paintings, I want to use that exact function name as my parameter right here. So museum with three paintings must be my parameter name and it will be of type museum. And because this attribute is CES test, CES test will see you are referring to something that I know has a fixture function, so it will automatically run this logic. Get back that museum instance and that becomes the value for this parameter automatically. There's nothing more we need to do. So now in the body of this test function, we no longer need this museum because we have one from our parameter list. So I can get rid of those two lines and simply replace this museum right here with museum with three paintings like so. So I can run my test suite right now to show you nothing has changed. Everything should still be passing. And that will also require, of course, the cargo tool to reach out and fetch and compile the code for Rs test. But there you go. Our tests are still passing, and then I want to prove to you that we are indeed working with this fixture function right here. So one simple way to prove that is to add another assertion right here using assert EC, I can take my venue management struct reach into its venue field, which should be my museum. That museum is going to have a paintings field, which is the vector, and I can call the Len method on that vector to get its length, the number of elements inside it. And I can assert that that is equal to three. So if this is indeed the instance returned from here, this should pass. And indeed it does. Right. And that is the beautiful part about fixtures is we simply reference the function name and we get that preconfigured instance. There's no more need to rewrite this code over and over again inside any test function that needs a museum with three paintings. And there's also no need to manually invoke this because it will automatically be arriving as an instance from the automatically invoked fixture function. Now we can do even better than this. We can actually have a fixture utilize another fixture within itself. So for example, we can create a fixture specifically for a venue management struct, and then within that fixture, utilize another fixture for either museum or for movie theater. Let me show you an example. Let's head to our attractions file. We're going to need to do some basic housekeeping to enable this. And let's find the add movie method here. For our Movie Theater struct, I'm going to make this public by adding the pub keyword in front. And now this is going to allow us to both import the movie theater struct, which is already public, and invoke this method within my integration test. So let's return back to venue management test. And at the very top here, in addition to bringing in the museum struct, let's also bring in my movie theater struct like so. So down below let's create a another sample fixture. So right here I will utilize my fixture attribute very critical above a function that returns some instance. So how about here? I call my fixture Museum Management. And right here this is where the magic happens. I'm going to use an existing fixture within this fixture function. So I'm going to copy this name right here. And this of course is going to be a museum. So I'm utilizing a preconfigured instance coming from here. And this fixture function will return a venue management struct instead. And here I'm going to provide the generic type as being a museum. So now I have a fixture that depends on another fixture which is pretty cool. So here let's create our venue management instance by invoking the new constructor function. And venue management of course needs a venue and we have one immediately available from our parameter list right here. So I can provide that right here. And that should do it. That should create our new venue management. And this of course represents a museum management fixture relying on another one. We can do something very similar by declaring another fixture that is going to rely on a movie theater instead. So for example, here I can have movie theater management, and you might find yourself going down this path where you say, okay, I need to have my movie theater management fixture. So I'm going to return a venue management struct, and its concrete type for the generic will now be a movie theater, and you might find yourself going in this direction where you say, okay, well, I'm going to create my movie theater in here. Let's say I create a movie theater right here with new. I'm going to take that movie theater and call the add movie method on it. And let's say we add a movie of Titanic. And then very similar to above, I can create a new venue management struct and return it implicitly and provide it with our movie theater instead of our museum. And that's totally valid. But as you might notice, we have two different approaches here. And generally, the more that you can break down your fixtures and keep them small and reusable, the more they're going to be applicable in other circumstances. So I think it'd be a really good idea here to instead create a separate fixture for this chunk of code right here, the creation of a pre-configured movie theater instance. So let's make that a reality. So let's head back to our attractions font. Let's make sure we have everything we need to enable that. So here, for example, I'm going to make my two fields on my movie theater struct public with the pub keyword that should do it. And now I think that should take care of it. So back within our venue management test file, we can create a separate fixture for our movie theater. So right here I can create a fixture by annotating my following function. Let's say I declare this one to be a movie theater with one movie, and I can mark this as being a movie theater. And now, of course, I am welcome to create a movie theater if I want to. With the fields. I realize now that we do have our valid constructor function, so maybe it was not worth making the fields public. But regardless, either approach works. I kind of already have this code right here that works. So why don't we just cut and paste it right here? And that can be what we want. So we're going to create a movie theater. We're going to add a movie. And then we're going to return it as our movie theater fixture instance. And once again, in order to utilize it here, all we need to do is just copy and paste this function name as the parameter here and provide the type as being a movie theater. Now we just need to update this argument to be movie theater with one movie to match the parameter name, and that should do it. So now let's go ahead and write another test that utilizes this fixture. So down below, once again, in order to enable our tests to recognize the fixtures that they are utilizing within their parameter list. We have to use the Rs test attribute instead of test above our sample test. So right here we can write something like venue management interacts with movie theater venue like so. And maybe for the sake of consistency we can update the test above to say with museum venue just so they each have their relative description. And here once again, since we have this annotation, we are welcome to use any fixture from above. So if I want let's say this venue management fixture, I can just copy its fixture function name, provide it like here. And this of course will be a venue management with a specific type of movie theater for the generic and Rs test will automatically create this instance by running the fixture function with this name and passing in the instance as a parameter. That's all there is to it. So let's see. I'm going to take my movie theater management. Venue management. Struct and call. Make money on it. That will require me to make this mutable, which I can easily do by adding my mute keyword before my parameter. There we go. And let's add one assertion in here for this venue management instance. Let us take our movie theater management. Let us reach into its venue, which will be a movie theater. And that movie theater is going to have a sales field which should now be equal to 15. All right. So once again, I'm going to run our test suite. Everything should pass just fine. And indeed it does. But once again we are relying on a fixture in movie theater management, which itself is relying on another fixture which is movie theater with one movie. So this design allows us to keep breaking down these pieces into smaller, reusable units. So now another fixture outside of this one could hypothetically reuse this fixture right here. All of these become applicable not just in the use of other fixtures, but also obviously in the use of parameters within our declared tests. Pretty cool stuff.
