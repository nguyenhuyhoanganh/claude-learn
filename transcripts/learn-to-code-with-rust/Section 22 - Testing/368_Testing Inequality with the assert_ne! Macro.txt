# Testing Inequality with the assert_ne! Macro

The complementary macro to assert_eq! is assert_ne!. where "not equals" is actually just abbreviated to the letters "ne", and this assert_ne! macro asserts that two values are not equal to one another. So this one is a less common assertion, but does demonstrate the flexibility of the assertion macros that are available. So here is our first test. And maybe I can update the logic of this test to first sell a ticket, and then assert that the revenue is no longer zero, which was the starting value when the museum was instantiated. I wouldn't say that that is necessarily a better test. I would in fact argue that it would be worse because I would not be testing the actual incrementing of the revenue field by 25, but it will allow us to demonstrate how the macro works. So once again, same syntax. Our macro is called assert_ne! where 'ne' is short for not equals. And like all macros it ends with an exclamation point. And I provide two values here, left and right. But this assertion will pass if the values are not equal to one another. So I can pass in 'museum.revenue' and then 0. So as long as 'sell_ticket' increments revenue by any amount, then this test should pass. And that will validate that 'sell_ticket' does what it is supposed to. So here you can see we are going to get a passing test. And if I for example, comment out this line that will not change revenue and that will cause the test to fail. It's going to be something very similar to what we see in the assert_eq! output. We're going to see left and right. But of course now they're going to be equal because the assert_ne! macro fails if they are equal. It will only pass if they are not equal. And as you can see, they are in this scenario. So as I mentioned, I prefer assert_eq! in this approach because it is a little bit more valid, you could say, or checking more things in asserting the incrementing of revenue exactly by a value of 25. So I'm going to change this back and make sure we have a passing test suite once again. But that is the assert_ne! macro, 'assert ne'.
