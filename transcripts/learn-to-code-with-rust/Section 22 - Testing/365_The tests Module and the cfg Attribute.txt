# The tests Module and the cfg Attribute

There are several categories of tests, and I want to start this lesson off by describing the differences between them. First up, we have the category of unit tests. A unit test targets a small component or unit of a program in isolation. I would say that the test that we wrote in the previous lesson is an example of a unit test. It focuses on one specific method on one specific struct. It is micro-targeted. The complementary type of test is an integration test which tests multiple components of the program interacting with one another. The word integrate means to combine one thing with another so that they become a whole, and that describes the expansiveness of this category of tests. Integration tests. If our program has, for example, multiple structs and enums and functions interacting with each other. An integration test would likely instantiate all of those instances and then test that they communicate or interact together properly. It's testing the larger system, in other words, the greater whole. Now, one detail that makes rust different from other programming languages is that it is common to write our unit tests alongside our implementation code in other languages. We typically separate our source code this stuff from our test code. In rust, it's actually pretty common to write your unit tests directly alongside your implementation code. However, there are some additional steps that we typically take to isolate and identify the actual code itself from our tests. So currently our test function is kind of floating alongside our regular code, and it's a bit difficult to visually identify and differentiate it from the library code above. So to cleanly separate our tests from the source code, what is really common is to declare a separate module. As a reminder, we declare a module using the mod keyword and then the name of our choice. And I'm going to choose the module name tests. I want to point out that the modules name is completely arbitrary. The most important detail as far as your tests, is decorating your functions with the test attribute. That is the key difference that allows the cargo test command to identify the function as a test function. The module is just an organizational container for the sake of clarity. So what I'm going to do is just cut and paste my test into this module. So once again the modules name is completely arbitrary. The most important detail is the declaration. Regardless, the community convention is to call the module tests, which is just logical, right? I don't see a reason to deviate from that module name. Right inside this module, you are welcome to define and use regular rust functions alongside your test functions. So not every function needs this test annotation. You can just have a helper function whose responsibility it is, for example, to instantiate a bunch of instances, right? It's just that the cargo test command will only target and execute the functions that have been explicitly annotated with this test attribute. So this is critical. Now the challenge with this nested test module is that rust no longer recognizes this museum struct, because the museum struct is declared in the outer scope in the parent module, which is the library crate root, we are still within the same file. But remember the module is almost like the rust equivalent of a nested directory. Tests is now a nested submodule within our library crate, and within this nested module scope there is no museum name available. And that's why rust doesn't know what museum is. Now, as you may recall, we do have to mark an item as public with the pub keyword. If we want an external module to reach into the current module and access that name. The same rules do not apply to submodules. In other words, nested modules or child modules, which is what tests is here. So child modules like test have access to the parent modules definitions, regardless of whether those definitions are public or not. The child module just needs to access that definition in order to be able to use it. So to summarize, the tests module and its test functions can access the museum struct or any of these methods or constructor functions. We just need to tell it how to access it. As you may recall from our project structure section of the course, we can use the super keyword to navigate one module upwards to go up. So if I go super or if I write super, that is navigating us up to the library crate route, which is the same file. And then once we are within the parent module, which you can sort of think of as the crate route, technically its name comes from the cargo.toml file, so it should be testing. That is the name of our library crate route in libraries. But super goes up to that high level, the crate route itself. And then if I provide my scope resolution operator the double colons, now I can provide the definition or the location of where the definition of museum is. And now our code is back to functional. So I can run cargo or cargo test and everything is passing once again. The other option that you may recall is the crate keyword. So I can replace super here with crate. Crate provides an absolute path starting from the crate root. So once again starting from Lib says we're starting from the top, and we're saying somewhere at the top of the crate route, there's going to be a museum struct. Find that and call its new constructor function. This is going to provide the exact same result. And once again our tests are passing. Now it can quickly become verbose and annoying to have to keep pulling in names from the parent modules in individual tests. I'm going to change this back to super, but you can imagine if we have ten different test functions inside our tests module, it can be pretty verbose to have to keep writing super each time. So a common syntax that you'll see is to pull in all of the names from the parent module directly within your tests module, and the code is going to look like this. Here within my module declaration. But outside of any test function I'm going to utilize the use keyword, then a space, then super, which once again reaches up one module, almost like going up one Up one directory. Then the scope resolution operator, the double colons, and then the glob operator which is the asterisk asterisk right here. And then end it with a semicolon. So as a reminder, the use keyword makes a name available in the current scope or module. It creates a shortcut to some name found somewhere else. So the super keyword reaches one module up to the library create root, and then we apply the asterisk which is the glob operator. What that does is it brings all of the names from that module that you are accessing into the current scope. So what this will do is it will take all the names defined in the library, create root, whether it be one struct or 100. And it will pull all of those names immediately into the nested tests submodule, which allows us to not have to write super like. So. Now we have access to museum as well as any other named construct from the outer level scope from the outer level module because this line pulls it in. It's a bit of a cheat code, but a nice one because we're going to be able to bring in all the new names from the parent module as they are defined. So if we define two more structs tomorrow, they're automatically going to be available within the tests sub module. And we do not need to prefix any of them with super. Generally the glob operator the asterisk which pulls in all the names. That's generally discouraged due to the potential of name conflicts and ambiguity on where the name comes from, but in this case, it's a bit of an exception and an elegant design, because the source code is in the same file as our tests. Our tests know exactly what they're pulling in. It's whatever is basically defined above it in the file. And as we add more names in the parent scope in the create route, like more structs, more enums, or more functions, they'll instantly become available to use within any of our test function in the test submodule without having to keep providing that super reference in every test. So it's just an elegant way to be able to just write Museum New in here and continue repeating that kind of syntax in any test function that we write afterwards. Now, there's one small problem with our design. As it stands, our tests are totally valid. Everything should still be passing. But the problem is that currently our test code is going to be included in the binary executable that the compiler will build. We don't need this test code for our final end user, for the final rust program, or for any other binary crate that's going to be pulling in our library crate code and our museum struct. So if we include this, we're going to be bloating the content by adding these additional tests that aren't actually needed to run the program. These tests exist for the benefit of us, the developers, for safety and security, to have confidence that everything is everything is working as expected, but the final rust program does not need them. So you'll notice, if I run cargo build here, we're going to get a violation right here. If I scroll up in the output you'll see right here we have our use super call. And it says if this is a test module consider adding this attribute CFG test to the containing module. So what we can do is add this special attribute to our tests module declaration. It's going to look like this. Above the line where I declare my tests module I'm going to provide the exact same attribute syntax which is the hash tag and square brackets. Then I'm going to write cfg a pair of parentheses almost like we're invoking it. And then the word test. So what CFD stands for is configuration or config. This configuration attribute allows us to declare these situations or environments or contexts where the compiler will include or exclude the code in the executable. So what we're saying with this test argument is we are telling rust to only compile and run this code when we execute cargo test. In other words, it will be executed when we run cargo build or cargo run. We are saying we only care about configuring this code for the context or the situation of testing. We do not want to include any of this code in the final compiled executable. To avoid bloating it with unnecessary content. So now when I run cargo test, you're going to see that warning disappear, right? The only reason we're seeing these warnings is because we're not actually using our museums struct anywhere outside of the test, so there's no practical implementation code using it. So you can feel free to ignore these warnings. You can see the previous warning about CFG has disappeared and our tests are still passing. Another advantage of declaring a module for our tests is now. Rust analyzer will provide a run tests button here, and that's going to execute all of the tests within the module, not just one test. So each individual test will have the run test button. So you could run it in isolation. But if your module then has ten or 15 or 100 tests, and you don't want to run all of the tests in your project with cargo test, you just want to focus in on all of the tests within a given file or a given module. Now you have this nice button from Rust Analyzer run tests that will execute all of the tests within that specific module, and you can just keep reusing this tests module name and all of your different code files. It's just going to typically utilize this super syntax to pull in that files named constructs and test those with unit tests with the annotated test functions. All right. So that is a little bit on the tests function and the CFD attribute. And with that I will see you in the next lesson.
