# The NaiveDate Struct

Welcome to the next section of the course. In this section, we'll learn the basics of working with dates and times in rust. How do we model the idea of time? How do we add and subtract time? How do we incorporate time zones? How do we parse time from a string or do the opposite? Convert a time to a string, and so on. So first things first, we'll need to add a library crate called Chrono or Chrono that specializes in working with dates and times. It gives us various constructs, various structs that are designed to model the idea of a moment in time, as well as concepts like duration or spans of time. The word chrono means relating to time, so it's a fitting name for a crate. It's where the word chronological comes from. So within our Date Times project, let's open up the cargo.toml file at the top. And here under the dependencies section I'm going to add Chrono all lowercase Oh, oh. And then I'm going to assign a version to it. And the latest version as of the time of recording is 0.4.41. And then we're also going to include another library that specifically deals with time zones. And that is going to be Chrono Dash TS. TS is short for Time Zone. Once again, all lowercase. And the version here in quotes is going to be 0.10.3. So this crate is going to add additional support for time zones as established by the Internet Assigned Numbers Authority. The Internet Assigned Numbers Authority, sometimes called the Iana, is just a standards organization that establishes rules for the internet, including the collection of standardized time zones that many languages will attempt to live up to or honor in their definition of time. So, with the crate added, let's transition to Main.rs. And the first type that I want to introduce you to from this crate is called Naive Date, and it is a struct. So let's bring it into our module scope at the top level with use reach into the Chrono Crate, and at the very top level we can pull in naive date capital N capital D, so a naive date struct models a calendar day without any associated time zone or time of day information. It's literally just a date a day in history like December 5th, 1995. The word naive means marked by simplicity or deficient in wisdom or judgment. It's a bit of a harsh word, but it's a fitting one for this type. And that's because it's called naive, because it doesn't account for the additional complexities of time zones, or daylight savings times or other calendar challenges. So it's sort of the simplest example of a date type that we can work with from the Chrono Crate. The naive date struct has an associated function called from. Why opt with underscores between the two and this will create a new naive date from its argument. Y stands for year, month and day, and those are the three expected numeric inputs that we're supposed to provide here. And Opt is included because it actually returns an option enum. The option is necessary because there is the possibility of us passing in invalid values. For example, if we pass in March 35th, right, that day doesn't exist. And rather than giving you back a result, it gives you back an option with the non variant. The non variant basically indicates hey there was no successful naive date created. So here I'm going to model my birthday. So I'm going to provide my year first. You can see here the pop up here the year is going to be 1991 for me. Feel free to fill in your details here. The next argument is the month I'm going to provide for. And the final one is the day I'll provide 12. And I'll assign this to a variable called birthday like so. And you can see right here we're going to get back an option that stores the naive date in. It's some variant if it is the some variant. So I'm going to go ahead and output this to the screen. And I'm going to provide my debug formatting here. We can take a look here. And you can see the visual representation. Once we actually download all of the source code for the Chrono Crate and compile it, which will only happen the very first time you run the program. Here you can see the actual visual representation. We need to rerun it again so we can see it clear. Here is the sum variant indeed, and it stores this text based representation of the naive date. It's a very simple textual representation that just includes the year, the month and date separated by a dash. But this is a complete struct that's storing a whole bunch of information within it. So of course I can call unwrap to get the direct naive date struct from the associated data in the sum variant. Here I can just use variable shadowing and just do birthday again. Take birthday from above and call unwrap. I know that I have a valid value from the line above, and now we have that exact same naive date pulled out. You can see here it is without the wrapping sum enclosure. So again, this naive date represents the Kraatz way of modeling a date, a specific calendar day. And this type implements both the display and the debug traits. We can print it out to see a nice string version of the date that includes the year, the month, and the day, all separated by dashes. I also want to show you an example of an invalid input. So I'll remove these two lines right here and pass in an invalid day here. Like let's say 32, there is no month with 32 dates. Same principle. We're going to get back an option enum, but this time around is going to be the none variant. And there's going to be no naive date struct attached. And this can happen, of course, if your data is coming from some dynamic source like perhaps user input or reading from a file or an API response. So if you know that the data is valid, for example, the hard coded value that we provided previously, we saw that it was actually returning a proper some variant storing the naive date in those situations. Calling unwrap on that option to get the naive date is totally reasonable. And in comparison, if you're working with some kind of dynamic data that you cannot predict in advance. That will be the basis of creating your date time. Type in rust. So something dynamic like user input, of course, you should be handling that option much more gracefully with something like a match statement that can account for all of the variants or possibilities of this constructor function. Later on in the section, we're going to dive much deeper into parsing one of these Chronotypes like naive date from a string, but I wanted to show you a basic example. In this lesson, the Chrono Crate is going to give you that functionality out of the box. In fact, it's going to extend some existing rust types to make it super simple for you to create date or datetime types from them. So for example, here I'll remove my code here, and I'll declare my birthday variable again. And this time around I'm just going to declare a basic string, a string slice that's going to store my birthday in a very common date format, which is once again just the year first, then a dash, then the month, Then the day. So this is just a regular string slice. I want to show you right here on the next line, that I can take my birthday string and call a method called parse on it. And what parse tries to do is it tries to extract a new type from the existing string, and the kind of type that I want to parse out. Or in other words, attempt to convert this string into can be a naive date. The exact same struct that we brought into scope on line number one. So here, in order to do that, I have to invoke parse, and I also use the turbo operator to provide the specific type that I want to extract from this string. So right here I'll provide my turbo operator and then invoke the parse function. And here I'll specify that I want a naive date. And that is going to give me a result. Because there is the possibility that that parsing fails. And just very quickly, very much like with unwrap, I'm going to call the expect method here, which as a reminder is just like unwrap. It's going to attempt to take the success variance data. And in the case of failure, it's going to allow us to provide a custom error message. So here I can just say something like unable to parse naive date from a string. This example will work, of course, because what we have here is a valid date, or a string that has a valid date that can be parsed if we pass in a string with a text like nonsense. Of course this will not work and that's why we get back a result. But here I can show you. We do have once again, that exact same naive date type right here. It is a full struct from the Chrono Crate. It obviously has its own fields and methods that give you information about that day, that calendar day, that specific day in time. You can see here it's text representation. You can also see here it does implement the display trait. So I can print it out, but the printed result is the exact same. This looks like text. But remember fundamentally it is the naive date struct for modeling a day and time and moving on throughout the section, we're going to explore more of these types from the Chrono Crate, as well as how we can actually use them to do operations like adding time, subtracting time, calculating the differences between two dates and more.
