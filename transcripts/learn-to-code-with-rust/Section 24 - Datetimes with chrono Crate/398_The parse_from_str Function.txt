# The parse_from_str Function

We've spent the last few lessons manually creating instances of the various types in the Chrono Crate. What's more likely to happen in the real world is that you'll be creating those instances in the program based on another source of data, such as a string that stores a textual representation of a date, time, or a date, or a time. Maybe, for example, your database is going to be storing that datetime information, or maybe a file on the disk. And the goal typically is going to be to take that string, that representation of a date or a time or a datetime and correctly parse the corresponding chrono instance from it. Now, we saw an example of this earlier on in the section when we called the parse method on a string, and we asked to create a naive date struct from it. And that was a very simple example, because Chrono was able to infer the year and the month and the day from the string just because it was written in a very common date time format. But that's not always going to be the case. The way that a date time is represented as a string is subject to a lot of variance. A lot of variation depending on operating systems and file systems and different databases and so on. And so for more complex cases, we have to explicitly tell Chrono and Rust how to parse the various fragments or pieces or components of a date time string. So let me give you an example. Let's say I have a string that's going to arrive in our program. I'll call it someday, and we're going to declare it inline. We can imagine this arrives, say, from some database. Let's say this is how this string is going to store it's datetime information. It's going to start with the day. Let's do 31, then a dash, then the month. But the month is going to be represented by its shortened abbreviation. So let's say October rather than the value ten. Then a dash, let's say the four digit year like 1995, then a space. Ace. Let's say we have the hours, minutes and seconds. Next, each separated by a colon. So let's do 18 for 6:00 in the evening. Colon zero seven. Colon 54. Then another space. And let's say this string also includes the time zone information. And that will be a dash followed by 0600 which is UTC minus six. So again, the way that this string can be constructed is subject to a lot of variants, maybe in another database or in another representation. Maybe these dashes become slashes, or maybe the year is going to come first in an alternate scheme, right? There's a lot of variation, and rust cannot simply infer what you mean. Many times we have to be specifically helpful and explicit in describing the format of this string. So our goal now is going to be to create a date time struct from the contents of this string based on this information. So on the next line I'll declare a variable And I'm going to begin with the date time type that we explored in the previous lesson. That's a fitting type right here, because we do have time zone information as well as date and time. And I'm going to use a special constructor function here called parse from string. And string is abbreviated str. So this function is going to accept two arguments. The first is the string to parse which will be someday. And the second argument is an interesting one. It's also going to be a string. And this is what is typically called the format string. The format string is a string that's going to use a collection of special symbols to designate the various fragments or pieces of this string. And this is not a syntax that you have to memorize. Typically whenever you do this, you just go online to a special cheat sheet, and that cheat sheet is going to show you the symbol as well as its corresponding equivalent in the date time string. So for example, here, just as a quick comment, the symbol percent D for example, is the day. That's not something you have to memorize. It's just something you look up on the cheat sheet and then you say, okay, in order for me to tell rust that the day comes first, I'm going to put percent d first up in my format string. Then I'm literally going to progress through all of the different fragments of this date time string, including the dashes, including the spaces and any other special characters. And I'm going to designate them right here. So let's take a look at this in action right here. I'll begin with percent D, which again is day. Then I have to be very diligent here. I have to remember it's not just immediately the month there is a dash separating the day and the month. So I'm going to include that dash in my format string, then move on to the symbol for the written out abbreviated month name, which is going to be lowercase b. As you might guess, case sensitivity is very important here. So a percentage capital D can mean something totally different from a percentage lowercase d. So be very careful here. So after my month abbreviation. I have another dash and then the four digit year, and the symbol for that is percent capital y. Then I'm going to have a another space. And then we have my hours minutes and seconds. And so the equivalent symbols for those for the hours is going to be percent capital H. Then we have to remember the colon. That's for this thing right here. Then for minutes is going to be percent capital M. And this is where these things become kind of difficult to memorize because percent M can either be the months right or the minutes right. It's kind of hard to remember. So typically again, don't worry about committing this to memory. You just look it up and then you match it to your string. So after my minutes I'm going to have a colon and then I'm going to have my seconds which is percent capital S. Then I have a space. And finally the symbol for the time zone, if it's written like this is going to be percent lowercase d. And there you have it. So you can see right here the parse from string function is going to return a result because there's always the possibility of failure if, for example, we pass an invalid formatting string, or if it's just unable to match the two because the format of the actual text does not match how we're parsing it. There's a wide variety of ways in which this can go wrong, but on the next line here, we can print line and we can output our result in debug formatting. And if we did everything correctly, typed all of our symbols very carefully, we should see the okay variant. And here inside this is the text based representation. But internally as you can see it is an actual date time struct. Right. So we have successfully parsed this information into an equivalent date time. And again if it's totally different tomorrow for example, maybe another system has this. That's totally fine. But then we have to make sure we update this with the complementary symbols or characters at the right locations, so that rust can figure out the mapping between the date time string and the formatting string, which is explaining how to parse that information. So right here you can see we're going to get the okay variant, if we make a mistake, for example, we accidentally put a dash here. We are going to get, of course, the error variant because it's not going to be able to parse the correct time, right. This is typically how you're going to be bringing in the date times into your program from some kind of external data source, right. It's important to understand the underlying date time type or the equivalent complementary structs from the Chrono Crate. First and foremost, before you start, of course, creating them from some other data. All right. That is how you parse a date time from a string. And of course, the other structs we've seen so far in Chrono, the naive date and the naive time. All of those support a similar parse from string function. Of course, they're not going to be able to store all of that additional associated state that only date time does. Maybe, for example, a naive date can parse the day and the year and the month, but it cannot parse the equivalent time or the equivalent time zone. So it really depends on what you need to extract from the string that you are bringing into the program, and parsing a struct instance from.
