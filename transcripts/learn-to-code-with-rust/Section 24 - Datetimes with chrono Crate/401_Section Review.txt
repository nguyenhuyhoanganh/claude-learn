# Section Review

Congratulations on reaching the end of another course section. As always, let's take a few moments to review everything that we learned. This section focused on working with date times using the Chrono Crate, so the Rust Standard library as of right now doesn't include any kind of date time types, and those are types for working with dates or times or both. So the Chrono Library crate is the rust community standard for working with these types of data, which are very common in the real world. Now, the word chrono simply means relating to time. That's where it comes from. And whenever you list the Chrono Crate in your cargo.toml file for your project, of course Rust and Cargo will download and compile the crates code, more specifically the code you use alongside your executable. We also looked at the complimentary Chrono Crate where TS is short for Time Zone, and that crate just adds various name time zones for easy time zone conversion. So if you want a complete list, for example, of cities, so you can easily convert to the time zone of, let's say, Los Angeles, California, or any other city in the world that create that is supplementary, makes that possible. So we began with an exploration of the naive family of structs. And they're called naive simply because they don't deal with the complexities of time zones. The word naive means simple, right? So we talked about naive date, which models a specific calendar day, right? A specific day in time, like September 23rd, 1999. But there is no notion of time zone or time. Then there is the naive time struct, which simply models a time of day and vice versa. It doesn't have any notion of the date or the time zone, and we can combine the naive date and naive time into a naive date time struct, which models a specific moment in time, but once again knows nothing about the time zone. And each of these respective structs are going to have various methods to get whatever relevant details they can offer. So a naive date can tell you its year, its month, its day. A naive time can tell you it's hour. It's minutes and seconds and a naive date time can tell you both of those sets of information. All right. We also talked about the time delta struct, which models a duration or a span of time, a gap of time. There's no specific date information there. It's not like September 8th or anything like that. It represents a duration like five hours, 33 minutes. And so there are various methods like num days, num hours, and num minutes that will return the count of those respective units within the duration. So the number of total minutes within the duration, the number of total days within it. We can arrive at a time delta through a variety of different ways. We can actually get them simply by subtracting one date time from another. Since the distance or gap of time between two concrete date times is a time, delta span and time, we can also, of course, build up time deltas by adding multiple time deltas together or subtracting from one another right. Adding a duration of five hours plus eight hours gives you a new duration of 13 hours and a time Delta can accommodate all of those possibilities. All right. We also talked about the daytime struct. And this is perhaps the most commonly used one, because it's the closest to what you're going to need in the real world. Usually if you're going to be modeling date and time, you're going to need to know the time zone information. So it's the exact same thing as the naive date time, but it is storing that additional information of the actual time zone. And there's a wide variety of different concrete types that can fill in for that generic. The date time accepts that represents the time zone that it is representing. So for example, we looked at the local struct. We created a DateTime instance by calling the now constructor function on the local struct. And that gives us a datetime struct and local here means local to your computer. The operating system that is actually running the rust code itself. So that will give you your local time zone, your computer's time zone. But that may not be what you need. So for example, the UTC struct has a very similar now constructor function that also gives you a DateTime, but that datetime time zone is the UTC time zone, which, in case you're unaware, is that global standard for time zone comparisons. Time zones are going to be described relative to UTC time, so some time zones are hours behind UTC, and other time zones are hours ahead of UTC. That's where values like UTC plus four or UTC minus eight come from, and whenever you have a DateTime struct, you can call the offset method on it to get the actual time zone, which is a critical piece of metadata living within the DateTime. All right, so once you have a DateTime, you're welcome to change its time zone. Or rather create a new DateTime with a modified different time zone by invoking the with timezone method on your DateTime and to it you pass a reference to some time zone, right? That can be a reference to the UTC struct. If you want a UTC time zone, it can be a reference to the local struct if you want your local computer's time zone filling in. Or as we saw with the Cronos crate, you can pass in a reference to some city's time zone, which will again take that exact same moment in time that the DateTime represents. But model that time with the correct time zone standing in. And finally, we talked about the conversion between date times and strings and vice versa. So typically you're going to have data coming into your program usually through text. And you have to take those text date strings and convert them into equivalent date time types in your program. Then typically do some kind of manipulation and then export them back out in their original format, which is strings as well. So we looked at the parse from string function. We explored it on a date time, but it is available on other types within the Chrono Crate, and that will accept a time string and it will convert it into a date time with the specified format string that you provide. The complementary format method is called on the date time type itself, not just literally the date time, but any other Chrono Crate type, and that's going to convert that time based struct into a time string. So both of these functions or methods are going to accept that format string, which just describes the components of the datetime string that you're going to be creating or parsing from. So as we saw, there were special symbols that each designate a various component or unit of time. So percent D for example may indicate the day. And there are similar symbols for the year the month, the hour, the time zone, even sometimes multiple representations of the same unit of value. So for example, you can represent a month by its numeric number or by its abbreviated three letter name, or by its full spelled out name. And all of those options are represented by different symbols. You build up the format string that describes how you want the time string to look, or how it does look, and rust uses that in order to either parse a DateTime from a string or convert the DateTime into that string with that format. All right, that's all there is to cover in this lesson. I hope you had fun in this section and I will see you in the next one.
