# Project Solution

So let's take a look at a sample solution for the coding challenge. In the previous lesson, we began this challenge with a vector consisting of tuples. Our program has received a vector of events from an event tracking software, and each event is stored as a tuple that contains a date time stamp. That's this thing right here, as well as the event description. Unfortunately, some of the data was corrupted in transit, and thus some events are accidentally modeled with a tuple of two values. That of course just means error. So here is our goal with this challenge. First step, we want to filter out the valid events from the vector, and the valid ones are of course the ones with a timestamp and a message for each one of those time stamps, the first elements in the respective tuples. We want to create a date time struct and parse it from the really custom weird format that we have here in our system. And then for each event that is valid, we want to print out the event time, including the year, month, day, hour, minutes and seconds. And very importantly, the time should be printed in UTC time. And finally, perhaps the most challenging part for any event after the first print out the time gap between the current event and the previous one. So this is our goal as far as the output of the program. We can see for this first entry right here we have the time correctly parsed and we print it out right here. You'll notice here it says 8:00 because that is the UTC time representation of this time. Here this is 4:00, but this is four hours behind UTC. So 8:00 is the correct UTC equivalent. Then we print out event message which is this content right here. That's going to be for our first output. And then for any subsequent event starting from our second one, we have the exact same details, but we also have the gap in time But between this event and the previous one. So this event right here on the 19th of April at 8:00 in the evening, and this one right here on the 20th, are going to be a total of 16 hours apart. So four hours until the next day, plus an additional 12 hours is indeed 16 hours. And then the gap is five minutes and 30s as well. All right. So let's begin this problem. The first thing that I want to do is I want to define what is called the format string. That is going to be the magic formula, the string of symbols that's going to tell rust the structure of the contents or fragments in each date time string, so that it can parse it into some kind of Chrono Crate type. And for us, that is going to be a date time, because that is the struct that can model all of the pieces we need the year, month, day, hour, minute, seconds, as well as the time zone right here. And we only need to define that string once, because it's going to be the same uniform format for all four of our valid events right here. So here I'll define a variable called format and define my format string. We begin of course with the four digit year and that is represented by percent. Why? In case you forget any of those conversion details, all you have to do is just head to Google and search rust format string symbols, and you'll be able to find a cheat sheet that will tell you the mapping from each respective symbol to the corresponding thing it represents. So percent capital Y represents a four digit year. Then we have two asterisks. Then we have the month which we can represent with percent m lowercase. Then we have two more asterisks in this really weird format. Then we have the day which is represented by percent D. Then we have a space and two exclamation marks. And once again, I'm just mimicking whatever we have right here. Then a space and then we have the hours, minutes and seconds separated by a colon. That's going to be percent capital H for the hour, colon percent capital M for the minute of the hour. Then colon percent, capital S for the seconds. A space. And finally, this fragment right here is the time zone which I can capture with percent Z. That is our string. So now we have to do a couple of things. I have to iterate over this vector, and I have to both filter out the valid events. These four right here. And then I also have to convert them into a complementary datetime struct. So there's a couple different ways to do this. I decided to go with the filter map iterator method, which allows us to both filter and map or transform each valid value within a single method call. So right here I'll declare a variable called events. We can take our event data vector. We first have to turn it into an iterator. And I chose the interpreter method because I'm not worried about taking ownership. It's totally fine here. And I'm going to call my filter map method. It's totally fine. If you went with the invocation of filter first, then map. This just allows us to do it all in a single single method call. In fact, I'm going to remove this file to the left just so we have a little bit more room to work with. All right. So here I'm going to provide a closure. And what we're going to receive on each iteration is each successive entry in my iterator or my vector which is going to be the tuple. So right here in the closure parameters I'll define my parentheses so that I can give each tuple element a name. And of course the first element in each tuple is the timestamp string which I will call timestamp. And the second is the actual event message which I will call message. All right. So what we want to do here is we want to try to create a date time struct using its parse from string function. Right. So here I can take DateTime. We already have our necessary use statement right here to bring in all of the names from Chrono that are of use into the module scope. So I have DateTime that has a helpful parse from string helper function that accepts two arguments. The first is the actual time string that you're trying to parse, which is represented by timestamp on each iteration. And then of course, the second argument is the format string, which we have defined above. So I will pass in format like so. And let me go ahead and add a semicolon here and here. And let me assign this to a variable like parsed date time. All right. So what this is going to return of course is a result because there is always the possibility that we are not successfully able to create a DateTime. And this is actually going to be helpful because where we want that failure or the error variant of the result enum is for these two error entries right here. So what we can do here is we can match on our parsed date time result and handle the two situations or two invariants differently. If we have the okay variant, that means that the operation was a success. We were successfully able to parse a date time struct from the given string with the given format string. And if that's the case, the okay variant as part of its associated data will store that actual date time. The actual date time struct itself. So right here I'm going to write out date time to get access to that data. And remember in the case of success this is actually going to be that date time that we need right here. Now as another reminder, the way that the filter map method works is it expects us to return an option enum. We return the some variant if we want to keep something, and we return the none variant if we want to discard or exclude something. So if we have a valid date time, we do want to keep it. We have successfully converted the string into the date time. At this point, we just need to make sure we don't lose it as part of the iterator. So I'm going to return some here. And right here I'm actually going to provide a tuple. And the reason I need a tuple is I need to store two elements. You could have chosen another data structure here as well. That's totally fine. But I of course need to have my actual datetime struct from Chrono. But I also need to make sure I don't lose track of my actual message either, because that's going to be needed later on. So basically, we're converting from a tuple of string and string to a tuple of datetime and string. So here I'm going to provide message as well. And then the other scenario we need to handle of course is if we have the error variant in this case for examples like this or this there is an error message but I don't really care about it. So I'm going to provide my underscore here. And what I'm going to return instead is going to be the non variant. And that is going to tell the filter map method to discard or exclude that entry. So as we iterate for example this one is going to be successfully parsed. That means it's going to go into the okay pattern arm right here. We're going to wrap it up in a sum and return it. So filter map is going to keep it in the final iterator that it's going to produce. And same principle for this one and same principle for this one and this one. And in comparison, when it's unable to parse a valid datetime struct from this one, it's going to enter into the error branch. Return none, which tells the filter map method to exclude it. So this is basically going to create a subset of for valid events from our original vector of six total entries. Now there's one more thing I want to do here. And you'll notice that we are receiving this DateTime with the fixed offset filling in for the generic here that has to implement the DateTime trait. This seems like a really good time to take my datetime and basically convert it to one that is storing its timezone as UTC time, because that's basically what I need to be working with later on. And since this is the filter map method, where we are mapping or transforming to the ideal data type that we want all in one place. This is a good place to do it. All right, so this is already a chrono date time type. And as a reminder, we can invoke the with timezone method on that to create a new date time but with a specific time zone. Right. So we are taking all the information right here and we are keeping it the same, except we are changing the time zone and getting back a brand new chrono datetime in that time zone. So as a reminder, what we can do here for UTC is we can pass in a reference to the UTC struct, which is also coming from Chrono. And so now if I return this, we're going to get a date time with an actual UTC time. So now we've defined all of our iterator logic. So the final thing to do is to collect all of the results in a new collection structure. I'm going to once again go with the vector. So right here, after the conclusion of the filter map method, I can call collect. And as a reminder, this can be provided with a turbo operator to designate what we want. So here I can provide a set of double colons and my angle brackets. And technically the type here is going to be a vector. And that vector itself requires a generic that specifies the type of the elements inside the vector. So I'll provide another pair of angle brackets right here. And this is going to technically be a tuple a vector of tuples right. Where each tuple has a first element of a date time. And that date time requires its own generic. So right here I'll provide another pair of angle brackets and say that this will be a date time where the time zone is UTC, and that is just the first entry inside this tuple in parentheses. Then the second element inside the tuple, which itself is the type of element inside the vector right. The second element inside the tuple each time will be a string slice, which I can represent with ampersand str, and this should work except this is going to be capital T. And of course this is really annoying and complicated to write. And this is where we can actually use a cheat code that we learned about a little bit earlier in rust, which is if rust is able to infer what it is that the elements of the vector are, we can try to get it to to figure that out by itself implicitly. The way we do that is we can do a colon right here and the vector type annotation or declaration. Then we can provide our angle bracket syntax and provide an underscore. And underscore basically says, hey, rust tried to figure this out. And I want to show you that if I remove my turbo operator right here, it should be able to basically figure out exactly what it's going to store. It may not be readily apparent here, but the reason it knows is because it understands. Hey, I'm going to be collecting into a vector. You told me that. And what is that vector going to store ultimately? It's going to be storing tuples of whatever you give me here in the sum variant, which is what was kept through the filter map. And it knows that DateTime is a datetime. And then with timezone actually returns a datetime with a concrete type of UTC for the generic. And then it knows that message is a string slice. So this code is actually going to be compiling. It's just fine. And we're not going to have to worry about that complexity of that manual type annotation that we just wrote. Although you are more than welcome to write it right here or right here as well, if you would like. All right. So now we have our events. And the next step is going to be iterating over them and printing out the message that we want. So right here I'm going to go with a very basic for loop. I'm going to iterate over every tuple within my events vector. And remember every tuple is going to store two elements. The first is going to be our datetime for the current loop. And then the message. And hopefully this time around you can see our annotations are working successfully, which means rust absolutely knows that the first element in each tuple entry in the vector is indeed a date time struct from the chrono crate in UTC time, so that is perfect. All right, so next thing we need to do is of course print a message. So right here I'm going to say let's figure out the display time. And so what we're going to do here is we're going to need to print out the actual datetime as a string with a slightly different format obviously, than how it came in here. So let me go back to opening up my coding challenge side by side. We'll take a look at what we're aiming for right here. So you can see right here we're aiming for year month, day then hours minutes and seconds. So as a reminder we can take our date time right here. In fact let me even call it UTC date time because it'll be a nice reminder. I'm going to take my UTC date time. I'm going to call the format method on that, which will do the inverse of what we did above. Rather than converting a string to a DateTime, it's going to convert a date time to a string with a specified format. So here I'm going to provide my string. And the symbols are going to be percent capital Y for a year, then a dash, then percent lowercase m for the month, then a dash, then percent lowercase d for the day. That's this part right here. Then a space. And then we need the hours minutes and seconds. So percent capital H colon percent, capital M colon percent capital S colon will give us that. There's no additional information. And this time should be in UTC time at this point because we converted it right here. All right. So let us write here print out print line. Let's do event time just like on the left. And I'm going to output display time which is the formatted string from above. And then I also want to print line and print out the actual event message. So event message and then message like so. And I want to buy us a little bit of space as well, a little bit of padding in between each output. So I'm also just going to add another print line here at the very end, and we'll come back to that in a second. And let's take a look at what we're working with as the program stands. So I'm going to run this right now. That will make sure everything was parsed correctly and the iteration was successful and all that. And you can see we are getting pretty close. So we have our event time written out. You can see all of the exact times appear to be matching 1205 two 1045 1303 as well as the event messages they're all successfully printing out. So we are almost there. The last thing we need to do is we need to, of course, calculate the duration or the span of time between every subsequent event and the previous one. So for the first one, we don't have to worry about it. But starting from the second one, we have to take this specific time and get the distance between that and this time. And the simple way that we can do that is by simply subtracting two date times from each other. Because as a reminder, that will give us the time delta time delta representing the duration or span of time that doesn't have any notion of date or time or time zone. It's just a length of time, and subtracting two date times from each other will give you that information. Now here's how I solve this problem. And I'm going to write out the solution because there's quite a bit of complexity here. But then we'll talk about what exactly is going on here. So before I begin my iteration, I'll declare a variable here and I'll make it mutable and I'll call it previous event. Because ultimately, as I am progressing throughout this loop, I need to make sure that I can always refer to the previous event that I had in order to get its date time and compare it with the current entry's date time. So for example, when I'm in the for loop and I'm iterating here over this event, I need to make sure that I can get access to this thing right here. And so my variable here is where I'm going to be storing that as I progress through every iteration. So this previous event is going to be storing a date time. But there's also the possibility that it can start out storing nothing at all. For example, on the very first loop this variable needs to be equal to something. So to represent the idea that it may or may not have a date time, I'm actually going to model it as an option. And so if it has a date time I'm going to store it in the sum variant. And if it doesn't at the very beginning of the loop it's going to have a non variant instead. So here I'll provide the type. This is going to be an option where the sum variant which is the generic we provide here is going to be a date time type. And that date time type from Chrono requires its own generic which is the time zone. This will be UTC. And then at the very beginning we're going to provide an initial value of none, which is a valid variant for the option. So this is either going to be none or it's going to be the some variant storing a date time in UTC format. All right. So right here after these two print lines, I'm going to use the if let construct. Now as a reminder, the way this works is we provide a hard coded enum variant on the left here. So in this this case I'm going to provide the some variant. And then if I have a some variant I'm going to be able to give a name to its nested associated data. And if we do have a some variant here in this datetime right here or in this previous event variable, rather, that is going to represent the previous entry that I've stored. Right? So I'm going to call this previous datetime just so it's clear. And then as a reminder the way this works is we provide the hard coded enum variant on the left and then some dynamic value on the right after an equal sign, and the if statement will execute if that is true, and it will also extract the some variants associated data and give it the name previous DateTime which we have access to in the associated block. So I'm going to compare this to my previous event variable, and we'll see how this works when we walk through the code in just a second. So basically this if statement is going to return true if we do have a previous event to compare the current iteration with. So on the very first loop on this event right here, this will simply evaluate to false. And on the second loop, assuming we successfully save this event time to previous event at the end of the loop, which we'll do in just a moment, then we're going to be able to have our previous date time accessible. And now we have previous date time representing the last event where the date time of the last event and we have our current UTC date time as well. So in order to calculate the difference or gap or duration between them, I'll declare a difference variable. I'm going to take my current date time that I'm iterating over, which is UTC date time. That is my date time right here in my current iteration, and I'm going to subtract from it the previous date time that I've stored in the previous event variable. And you can see when we subtract two date times that are both conveniently in the UTC time zone. We're going to get a time delta, right? This is the distance or the gap between these two times, which is modeled as a span of time, which is what a time delta is. So now if I want to simply print out the time since the previous event, I need to calculate the number of hours, minutes and seconds in that time. Delta. And we saw some of the methods that were available for that earlier on in the section. So I'm going to take my difference and to get the hours portion right here on the time Delta, I can call the num hours method, and I can assign it to a variable called hours. And similarly there's going to be a method called num minutes. But the problem with the num minutes method is it's going to give us the total number of minutes within the time delta. So for example, if the time delta is two hours, it's going to give us 120 minutes. We don't want the full number of minutes. We want the remaining number of minutes after we have divided by the total number of hours. You can see right here. For example, we want the value here to be five minutes, right. We want to fit as many hours as we can, which is 16, into the representation of this gap. And then after a full 16 hours, the remaining minutes that are left over is exactly five, right. This is going to give us the total number of minutes in the duration. So the way we very simply mathematically solve this is we take modulo 60. This is the remainder operator which will give us the leftover or the remainder when the number of minutes is divided by 60. So for example, if I have a total of 125 minutes in a duration and I take the remainder when divided by 60, 60 fits twice into 120 and then the remainder left over is going to be five. So this will give us the total number of minutes that are left after we have evenly divided by hours, which are modeled by one hour being 60 minutes long. So this will give us the exact value we need right here and right here. The number of minutes remaining after we have cleanly divided by the hours which we'll have right here. All right. So I'm going to take that and assign that to my minutes variable. And we're going to need to do the exact same thing for seconds. Exact same principle. If I do the seconds method on my duration, it's going to give me the total number of seconds, probably in the thousands, right? I don't need that. I just need the total number of seconds after I have evenly divided all the hours and the minutes. So I'm similarly going to take the modulo of 60, and that will give me the seconds into the respective minute that I need. All right. So now I'm going to print line. I'm going to output the exact content that we want right here I'm going to say time since previous event. And I'm going to interpolate dynamic content. Then H space the dynamic minute content, then M then the dynamic seconds content, then S, and then I need to provide the actual three variables to interpolate, which are hours, minutes, seconds. Let me do that. Looks good. And then very importantly, this is the critical part. The only way this is going to work is if the actual previous event variable is going to have access to the previous event. So at the very end of each iteration, let's say this is the very first one that we're considering right here. We have printed out event time event message. This doesn't run at all because we don't have anything in previous event. Then at the very end of the first iteration, I'm going to take that mutable previous event variable, and I'm going to assign it to be my UTC date time. Right now, this by itself is very close, but it's not quite going to work. Because remember, previous event is not storing a date time. It's storing an option whose some variant could be storing a datetime. So we're starting off with none. In order to actually give it a valid entry. We just need to wrap this in the some variant and that satisfies the type. And that's all there is to it. So just to walk you through a couple iterations of how this is going to look on the very first iteration, we print the event time and event message. There is nothing in previous event. So this entire if statement does not run and we assign previous event to store the date time struct modeling, this time right here. Then we move on to the second iteration of the for loop. So the UTC date time is now this one. Right here we print out the usual display time and message. But now when we run the if statement it is going to evaluate to true because within our previous event variable that we assigned at the end of the previous iteration, we now have previous date time stored as a direct result of storing this the last time around. So now we can extract that previous date time from the sum variant right here, and then run the calculation logic between the previous date time and the current UTC date time right here. So now we have this additional content being printed time since previous event. And then very importantly, we're going to take our second UTC date time, which is the current iteration that we're on, and we're going to save it to previous event so that on the third iteration, the next EDC DateTime can refer to previous date time once again, which that time around will be the second DateTime. Then one more time we're going to assign this date time to previous event so that on the fourth and final iteration, UTC DateTime is this previous date. Time is going to be this. We're going to be able to compare them and print out the difference between them, and then the loop concludes. So let's run the code. We should see everything hopefully working successfully. I'm going to move this. You can see right here time is going to be 16 hours five minutes and three seconds. I'm just going to do some very quick math here to make sure this is right. This is 8:00 on the 19th. So four hours brings us to midnight. Plus another 12 hours into the day gives us a total of 16 hours. There we go. And then five minutes and 30s after zero zero is correct. So the logic here is looking correct. And there we have it. All right. So quite a few interesting concepts in this challenge. Of course we did involve the date time struct from the Chrono Crate as well as the time delta as well. But also there's a lot of combination of different concepts here as well, including things like iteration and filter mapping, including things like manual iteration with the for loop, the if let construct right, and composing together various ideas invoking different methods on these respective date, times and time deltas, converting them to a different time zone, parsing them successfully from a string. So this is kind of more of a real world example in the sense of this can be a real problem that you encounter, and it does involve the use of the Chrono Crate, but obviously combining it with other rust concepts and building blocks that we've also explored throughout the course. So I hope you had fun. And with that, I will see you in the next lesson.
