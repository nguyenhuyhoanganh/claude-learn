# The NaiveTime and NaiveDateTime Structs

The naive date struct models a calendar day. It has no notion of the time of day. Once you introduce the idea of the time, which encompasses hours, minutes, and seconds, you now need to be aware of that extra state. So the complement to the naive date struct is the naive time struct, which represents a time of day. Now a naive time has no notion of date. It doesn't know the concrete calendar month or year. It's just modeling a time like 4:32 p.m.. So let's bring in from Chrono the complementary type here, which is naive time once again modeling some time of day, not a duration which is represented by time delta, but actually some kind of time separate from a date. Right. So here, for example, if I want to model 4:30 a.m., I can take my naive time. And there is a constructor function here called from HMS opt. You'll notice the naming scheme here is very similar to what we had with naive date that had from Ymd year month day. This from HMS is from hours, minutes and seconds and once again it returns an option because of the possibility of invalid inputs. So if I want to model 430 in the morning, my hours here, which is the first argument is four. My second argument of minutes is 30. And then if I want to model zero seconds into that minute, that's a third argument of zero four seconds. And now I have this option storing a naive time. I can take a look at what that looks like right here. Take my 4:30 a.m. and it's an option. So we need to apply our debug formatting. And you can see here is our sum variant. And here is the string based representation of this naive time struct modeling a given moment during a day, a time, right? But it has no notion of an actual date, right? Similarly, if I wanted to model 430 in the afternoon, 4:30 p.m., we have to swap the hour here from being four to being 16. So it's a 24 hour clock. So if you want to model anything after noon, that's going to be a number greater than 12. So this is the equivalent of 4:30 p.m.. And you can see we're going to get that value here. So we've taken a look at naive date. We've taken a look at naive time. The final struct to introduce you to combines the two of them together, a given date with a given time. And that type is called naive date time. So right here, let me bring in the kind of type that is going to be a composite aggregate of a naive date and a naive time, and that is called a naive date time. This is our first introduction to a type that does both. It's a struct that can model a specific calendar day and a specific time during that day. So, for example, say I want to represent the moon landing. The moon landing happened on July 20th, 1969 at 817 UTC time. Now there is no notion of a time zone yet. That's where the naive prefix matters. It's just telling you this is the simpler, less complex version of working with dates and times. It doesn't have to worry about time zones yet, but we're still going to be able to model that specific moment in time. So for the specific day, I can begin with my naive date struct. I'm going to use my from Ymd opt constructor function. The year is 1969, the month is July, which is the seventh and the day was the 20th. And I know these are valid inputs, so I'm going to unwrap the actual naive date from the option. Then for the time, I'm going to again use my naive time and use the function that we just explored from HMS opt from hours, minutes, seconds. Return an option just because of the possibility of invalid inputs. The time was eight in the evening, so that is an hour value of 20 for eight in the evening. And then it was 17 minutes into the hour. I don't know the exact seconds. So I'm just going to put zero. And I'm going to similarly unwrap the naive time struct from the option. So now here I'll create a moon landing variable and I'll use the newly imported naive datetime. This one has a new constructor function, and what it accepts as the two arguments is a naive date and a naive time. It's going to combine them together to create a new naive date time. Once again, time zones are not relevant just yet. This is just modeling that date and time. So I'm going to literally pass in my day and time variables from above. And now we have our naive date time right here. So here I can output this. We can take a look at what moon landing looks like. I believe it's going to need debug formatting. Maybe not. Let's take a look. And there we go. You can see July 20th, 1969 at 817. The text based, string based representation of this naive date time is going to include both the year month, day as well as the hours, minutes and seconds, and the exact same principles are going to apply to this naive date time in regards to adding or subtracting time. So the concepts that we saw in the previous lesson. So for example, right here I can take moon landing which is a slightly different type of naive date time. But I can still for example add and subtract time to it. So for example here I can bring in my time delta to model a duration, not a specific time of day, but a duration like five weeks. And here I can take time Delta and take a look at, for example, what is the day that was 1000 days after the moon landing, right? I want you to notice what we're going to see here. This is the actual day. It's going to be April 15th, 1972. But notice the exact time of day is preserved, right? Because we're literally moving forward 1000 days forward without adjusting anything as far as hours, minutes or seconds. So we're going to end up on this day, almost three years later, but at the exact same time. But now, because we have these specificity of hours, minutes and seconds, things that we can add like hours, minutes and seconds start to become relevant. So for example, if I just copy this code down below and in addition to adding 1000 days, I add something like a time delta of minutes. Remember that is another constructor function available and I add something like 45 minutes. Now you can see we're going to get that exact same date of April 15th, 1972. But now we're progressing forward 45 minutes in time from 8:17 p.m., which gives us 9:02 p.m.. And the same principles will apply with subtraction as well as the add or sub methods. If you wanted to bring in those traits into scope, we're now able to move forwards and backwards through time. But now again, this single struct of naive date time inside itself is holding the information for the day as well as the time.
