# The TimeDelta Struct

The next struct from the Chrono Crate that I want to introduce you to is Time Delta, which models the idea of a duration or a span of time. So let's begin by bringing in time delta into scope right here. Use Chrono and we'll reach in and pull in time Delta. And there are several associated constructor functions on this type. So for example here on Time Delta there is a new constructor function that one is going to accept two numeric arguments. The first one represents the number of seconds and the second argument represents the number of nanoseconds. A nanosecond is a portion of a second. One nanosecond is actually equal to one billionth of a second. That's how precise you can get here. So, for example, to model the idea of a duration or a span of time equal to five seconds, I can pass in five for the seconds parameter and then zero for nanoseconds, and assign this to a variable like five seconds. Now you'll see here that what the new constructor function returns is actually an option, because there is the possibility of invalid arguments. We'll see that in just a moment. But once again, if you have a valid time delta it will be stored within the sum variant here. So here I can use my print line. And let's output the value of five seconds in debug formatting. And we're going to see here it's going to be the sum variant storing the time delta. This is the string representation. And here we can see the sex or seconds field is equal to five. I want to show you what an invalid input is going to look like right here. I can for example, take time delta provide the exact same number of seconds of five and provide something like 1 billion nanoseconds. I'm just going to separate those sets of three digits with underscores. So this is 1,000,000,001 billion nanoseconds is equal to a second. So this technically should be created as six seconds zero Nanoseconds. So this is an example of an invalid input. It's an invalid argument. It's not necessarily a failure which is why it doesn't return a result. It's just going to be the non variant because it's basically invalid inputs to the constructor. So here if I print out the value of invalid you can see right now we're going to get the none variant instead. We can also use the time delta struct to model the idea of a negative duration. So for example here I'll declare another variable of -five seconds. Once again, use the exact same new constructor function, but this time around I'll pass in negative five for the seconds. So this represents a span of time equal to -five seconds. Now, right now a negative span of time may not make much sense, but it will when we start dealing with the idea of adding and subtracting durations from times. Right? If we have a given moment in time and we want to move forward five seconds or back in time to the past five seconds. That's where these differences are going to matter, right? -five seconds or a negative duration means moving backwards some span of time. Right? Some duration of time. So right here similarly I can show you once I pass in my nanoseconds argument and output the value of -five seconds. This is still going to give us a valid some variant storing our time delta struct. You can see right here is our time delta. But the seconds is negative five totally valid. So internally the time delta struct is going to use those small measurement units of seconds and nanoseconds to model some span of time. Those are very small units which allows for a good precision. But obviously we many times want to build up much longer spans of time consisting of minutes or hours or days or months. And we typically don't want to do all that manual calculation of figuring out how many seconds are in a given span of weeks. So because of that, there are also complementary constructor functions available on time. Delta to give you pre-built time delta instances that are going to have the right number of seconds for a given span of time. So for example, right here to model five minutes, I can of course calculate the number of seconds in a minute, which is 60 multiplied by five, etc. but there's also going to be a complementary constructor called minutes. That's going to accept a single argument of the minutes. Here I'm going to provide five. This, interestingly enough, does not return an option. It just returns a time delta. So I guess in this scenario, if you feed in some kind of invalid input like a number that is super, super large, I think it's just going to panic at runtime. But as you can see, it's just a single input. And here if I output my five minutes time delta, it is no longer an option. So we're not dealing with the some variant. But you can see here is a time delta. And here is the calculation of seconds five minutes times 60s per minute is 300 seconds. So internally it's still going to be using state of seconds and nanoseconds. But we can use this to much more easily model a longer span of time. And as you might have guessed, there's going to be complementary functions like hours, days, and weeks. I also want to show you right here that we can pass in negative values here as well. This is going to model the idea of five minutes going backwards in time. Right. And I just want to very quickly show you some variations here. I can literally just copy these examples down below. Here I can represent five hours by switching this to the hours constructor function. Same principle here we can do something similar with days five days there's going to be a complementary days constructor function. And then finally there's going to be another helpful one called weeks. So here I can model the idea of five weeks. Once again the constructor is weeks. I'm going to change all of these to be positive and change this to be five weeks. Right here we can see that five weeks represented as seconds is actually going to be 3,024,000 1000 seconds. Let's take a look at some of the methods available on a Timedelta struct. We have a variety of methods for extracting the number of various measurement units contained in that span of time. So for example, if we just start with this five weeks right here, we can take that Timedelta struct and call a method called numdays, which will give us the number of days represented by that span of time. So five weeks is equal to 35 total days. So you can see right here we're now going to get a integer return value that has given us a more human readable version of that span of time. So five weeks is 3,000,000 seconds. But it's also represented by 35 days. Right. And very similarly there is complementary methods for hours. This is going to be the number of hours. The num hours method will tell us the hours contained within that duration, 840 hours is contained within five weeks. And similarly, there's going to be a minutes method that will tell us the number of minutes represented in five weeks of time, and that is 50,400 minutes. The final concept that I want to show you is that we can use regular mathematical symbols, like the plus operator and the minus sign to add or subtract multiple time deltas from each other. We're basically adding or subtracting two durations from each other to get a new duration. So rust is going to give you back another time delta representing that new span of time. So for example, if I want to combine all of these time deltas together, I can do so right here. I'll declare my variable called total duration. And I'm just going to use the regular plus sign addition. Operator I'm going to take five weeks. That's a time Delta plus five days plus five hours and plus five minutes. We're going to add four time deltas together. We're going to get back a new time delta. And I can print out my total duration. You can see right here it's going to be the exact same time delta struct. And here's the total number of seconds in that span of time. And we can utilize the exact same methods we have here to see the kind of concrete units of time within that. If we're looking, for example, for the number of weeks or days or minutes. So here, for example, I can see how I can represent this time differently. I'm going to provide room for some dynamic value. The number of weeks, some dynamic value for the number of days, some dynamic value for the number of hours in that span of time. Same thing for the number of minutes. And I'm just going to invoke a bunch of methods in sequence on total duration. Let's do num weeks, which we did not see up above. Let's also do the num days for the number of days within that span of time. Let's do the number of hours and let's do the number of minutes. That's going to be the num minutes method. There we go. And let's execute that. Take a look at what we get. So you can see here five weeks 40 days 964 hours 57,895 minutes. That's the representative representation of this span of time in these respective individual units of measurement. This amount of span of time is equivalent to five weeks, equivalent to 40 days, equivalent to 964 hours. You could see it's rounding slightly, right. Because this is 40 days. So it's not quite five weeks, but it's not gotten to six weeks yet, which is 42 days. So it's rounding down to the most complete integer for the total number of weeks. Same principle for days and hours and minutes, but hopefully the basic gist makes sense. Now. Again, there is no concept of date and time yet. We're not adding or subtracting these durations from a specific moment in time. This time delta struct is just modeling the abstract idea of some span of time, some length of time, some duration that can be added or subtracted from a given moment in time, a concrete specific date and time. We're going to take a look at how to do that in the upcoming lessons.
