# The format Method

Next up, let's explore the process in reverse. Once we have a date time, we can perform our transformations, manipulations, calculations, and then we may want to convert it into a string. Maybe in fact export it out of the program in that string based format. So let's begin by creating a date time right here. I will reuse my UTC struct and call the now function, and I will assign this to a UTC time variable. So here of course I have a date time struct. And now I want to convert it into a string. To accomplish that, I can use a special method on my datetime called format. And so format is going to convert this datetime struct into a string representation of our choice. And this method is going to accept a string argument with the exact same formatting rules that we saw in the previous lesson, but applied in reverse. Here we use the formatter string to declare clear how we would like the returned string to look? Where should the year go? If anywhere, should the month be present? In what form? The full month name or its numeric equivalent. Do you want to separate the content by dashes or by slashes, etc.? All of these customizations are up to us, so let me show you a few examples or variations in this lesson. For example, here I can begin with a lowercase m for the month, then a dash. Then let's do a lowercase d for the day, then another dash, and then I'll do percent capital y for the year. So this should give us the month, the day and the year. And you can see right here it is separated by dashes. Of course if I want to customize that and use slashes not a problem. I simply customize this formatter string to provide slashes instead. And there you have it. Let's say I want to customize the year so that instead of giving me the four digit year 2025, I want the two digit year representation, which is just the last two digits. For that, there's going to be a complementary symbol, which is percent lowercase y. That is going to give us just the two digits of the year instead of all four. Let's explore some even more complex variations here. I'm just going to copy and paste this and customize the format string accordingly. We can do something like percent B for an abbreviated month. This is going to be like the written form of May for the month. So m a y. Then a space. Let's do the day which will be percent D. I'll add a comma here and another space. Let's do percent y once again for the two digit year. And then how about we add the hours and minutes and seconds as well because they are contained within a date time struct. So here once again percent capital H for the hours in 24 hour time, then percent M for the minutes after colon, another colon and then percent capital S for the seconds you can see right here. We're now going to get June 4th 25. A totally different representation from above, as well as the complementary hours, minutes and seconds. Once again, this is UTC time. All right. One more example. I'm going to copy and paste this below. I just want to show you some more examples of the various symbols that are available and how powerful we can get. Here, for example, I can write out percent A that is going to be the full day written out. So here, for example, you can see Wednesday is when I'm recording this, or rather Wednesday UTC time. Next day is Wednesday. I'm technically here on Tuesday, but that is the full day name written out. And then let's do something like a space and a percent I that's going to be the 12 hour time. So instead of a 24 hour clock, this will be the 12 hour time. So this will typically require something like an am PM designation, which we'll add in just a second. So that's going to be the 12 hour clock. Let's do a colon and let's do percent m for the minutes. This I meant to say is the hour not the clock. And then I can do something like percent P, which is the am PM designation. So this will literally print out Am or PM as part of the string, which is helpful with a 12 hour clock. And then afterwards I can do something like percent Z which will actually give us the time zone itself. So here you can see Wednesday, 12:42 a.m. UTC. A totally different way to represent this exact same time. We've completely stripped out the seconds and made it a 12 hour clock instead of a 24 hour clock with the Am designation. So again, a lot of flexibility here. Don't worry about memorizing this, but this is how we can take a Chrono Crate type and do the opposite. Convert it into a complementary string, which we typically then send off to whatever needs it. Right? If you got this value from a database to perform some kind of calculations on it, maybe this is you sending the text based string back to the database so that you can overwrite the existing entry, something like that. It all depends on what you want to do with intaking time based data into your program, and then exporting it as well.
