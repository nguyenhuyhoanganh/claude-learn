# The else Statement

In addition to the else if construct, we can also add one else keyword at the end of all of our conditional checks. So the else keyword does not have an associated conditional, but it does have a block, and that else block specifies what to do in case all previous conditionals have evaluated to false. So the else is thus a fallback option. Right? What to do if all else fails? Right. So say I wanted to output the exact same content for a season of "fall" and a season of "spring". Well, I could certainly copy and paste this line of code right here. Right. But we could also accomplish this with an else. I could alternatively say that I can check if the season is "summer". If that fails, check if the season is "winter" and in all other scenarios simply run this bit of logic. So what I can do here is I'm just going to copy this line so I can paste it back in just a moment. Instead of else if, I'm going to just use else. So else always comes at the end. It does not have an associated conditional, but it does have an associated block. It's in there. I'm going to paste my println! And now in all scenarios where this is not true and this is not true, this else will run. So one key difference here is with ifs and else ifs, there's no guarantee that any block will run. It's possible that every conditional will evaluate to false and nothing will execute. With an else, there's always a guarantee that one of these blocks will execute. Either this one, either this one, or if nothing has been met so far, this else is guaranteed to execute. All right. So now we have "spring". So we're going to see the content. "Lots of rain" from entering into our else block. It's important to note that you can combine else with just if. So you do not need an else if. You can combine else if and else like in this scenario. But all you really need is just an if. And so the cool part about that design is that if you have an if -- some conditional -- and then you have an else, you create this construct or this design where one pathway or one branch of logic is guaranteed to execute. It's always going to be either this one or this one. And you're going to see that pattern pop up a lot in code. All right. So there is if which can exist by itself. There is else if which can only be added after an if statement. And then there is else which also can only be added after an if statement, but can also be combined with an else if.
