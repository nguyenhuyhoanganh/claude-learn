# The continue Keyword

Let's pick up where we left off in the previous lesson and introduce the 'continue' keyword. Sometimes, we may want to progress to the start of the next loop without completing the current loop. The 'continue' keyword forces the loop to move to the beginning of the next iteration. Now, the 'break' keyword still terminates the loop completely. 'continue' sort of allows us to terminate the current loop. It moves to the next iteration without having to complete the remainder of the code that's still left in the block. So let's continue with this code from the previous lesson. First up, let's increase 'seconds' here to initialize with a value of 21. And let's say I introduce a new requirement for our program. If we have an even number of seconds in a given loop, I want to output a different message from this one, and I want to decrement or decrease 'seconds' not by 1, but by 3. And once again, that's only if I have an even number of seconds. Otherwise I want this regular code. So first things first, I'll modify my original if statement just to make it a little bit safer. For this example, I'll update this equality check to be a less than or equal to ( <= ) check. So that way, if 'seconds' is not just 0 but any negative number, I will run this logic and terminate the loop. This is just for the scenario that we happen to subtract 3 seconds and go to a negative value instead of exactly 0. It's a little bit more of a safer approach. Then, I'll add another if statement below to check if we have an even number of seconds on a given loop, so I'll take 'seconds', I'll apply my modulo operator, I'll take the remainder when 'seconds' is divided by 2, and I'll compare it to zero. So if we have an even number, I want a different piece of text from this. So how about we write something like "{seconds} seconds". And then I'm just going to provide parentheses and say "(even number)". And then we're going to say: "skipping 3 seconds". The message doesn't matter. The key takeaway is that it's different from what we have here. And in this scenario I'm going to decrement the 'seconds' variable by 3 instead of 1. Now, here's where the key component comes in. If I leave the code like this for an even number. We're going to hit this if statement. We're going to run this bit of logic. But then the code is going to finish and proceed through these two lines. I don't want to do that. I don't want to print this statement and then decrement 'seconds' one more time. I don't want to do that. So of course one alternative is to simply add an else. And this is a totally valid solution. Now we have two branches of logic. But there is an alternative solution and that is the 'continue' keyword. So I'm going to Ctrl + Z or Cmd + Z to get back to my original code. And instead what I'll do is I'll add the 'continue' keyword right here. Remember, what continue does is it tells Rust to stop the current iteration, to not move any further and just start from the beginning of the next loop. You can almost think of this as like an arrow that says: "Go back to the start of the block and start from there". Because we're going to run into 'continue' in this if block, if we continue, we're never going to reach these lines on that loop, on that iteration. So thus we'll just start again from the beginning and continue the logic again. And that should do it. So when I run this program we're going to see 21 seconds. We're not going to hit this if statement because our seconds are still positive. We're not going to hit this if statement because 21 is an odd number. So we're going to print out "21 seconds to blastoff.." and decrement 'seconds' by 1 to 20. Then the loop begins again from the top with 'seconds' equal to 20. So the if statement once again will evaluate to false. This time around, our 'seconds' value of 20 is even So, this condition will evaluate to true. And we're going to run this code. Here we see the output "20 seconds" We're going to decrement 'seconds' by 3. So we're going to get 17. And most importantly with the 'continue' keyword we're going to avoid this logic and restart the loop. So now on the third iteration 'seconds' is equal to 17. We can see that right here. So then the logic will continue over and over again. If we happen to hit this block, we're going to continue the process of decrementing by 3 up until we get to a loop where 'seconds' will be less than or equal to zero. At that point, we're going to print "Blastoff!" and use the 'break' keyword to permanently end the loop, and then the entire program will conclude. All right. So hopefully the difference between the 'break' keyword and the 'continue' keyword make sense. With that, I will see you in the next lesson.
