# The match Statement with Multiple Values and Conditionals

In this lesson, we'll learn how we can match against multiple values in a single arm. So let's start with a simple example that we've seen before. Say I have a 'number' variable set to the value 8. And I once again want to execute different logic depending on whether my 'number' variable is even or odd. Let's pretend, as always, that this value is dynamic. We're just hard coding 8 for now. For the sake of example and simplicity, let's say we can't predict this value in advance, and I want to execute different logic depending on whether it's even or odd. So I'll use my match keyword and then provide 'number' as the value to match against, and then provide my curly braces. Once again, Rust needs to validate that every possible option is taken care of, which in this scenario means every possible integer in the world, right? Quite a lot for us to handle. So this is going to be perhaps the dumbest solution that we can start with, but one that will allow us to introduce a new idea. Right. And that is, we can literally start by writing out every even number that we can think of. Let's maybe do 2, 4, 6, 8 to start. And we can write this all on a single line. We're going to separate every two numbers with a pipe. And that pipe basically includes every one of those values in the match with the number above. Let's write it out first and then we'll talk more about it. I'm going to write the value 2, then a space, and then I'll put a vertical pipe. The character for this is a couple of keyboard characters away from the 'P' key. It's to the right of the 'P' key on my keyboard, and it also requires the Shift key. It's this vertical pipe. Then, I'm going to do a space and put my next value, which is 4. I'll once again add a space and another pipe, then a space in the value 6. And we'll repeat this one more time with the value 8. Then I'll provide my equal sign and greater than symbol ( => ), my arrow, and provide my block. So what this is saying is if 'number' is either 2 or 4 or 6 or 8, run this corresponding block. So on a single line we can match multiple values against 'number' and execute the same block of logic. Right? So this time around I'll add my println! and we can actually interpolate the value of 'number' directly. We have it available from the scope in here. So I'm just going to add in "{number} is even". All right. So that takes care of 4 possible even numbers, but definitely not all of them. But just to show you the syntax one more time, on the next line I can target for example 1, 3 and 5 and output something different like "{number} is odd". At this point, of course our code will not compile because all possible integers have not been matched. So we can take care of this problem with the underscore solution from the previous lesson, which is adding the underscore that handles everything else. It's a wildcard or a catchall that will always pass the check. So we're going to add it to the very end. And if we reach this arm we can just output something like "Unknown for now". So when I save this, our code will compile. And when I run this we're going to see "8 is even". Not a great solution because we're only accounting for a couple even numbers, but it does demonstrate this new syntax of including the pipe in order to specify multiple values that we are applying the same block to. So let's now do a different approach. We're going to actually apply an if statement directly into our pattern. Okay. So I'm going to delete everything I have here. And this is going to be interesting. We're going to begin with an arbitrary name. You can think of this name as a variable that only exists for the sake of the pattern. We need some kind of name that we can use. So I'm going to arbitrarily declare a name called 'value' here. But this can be whatever you want. This can be 'my_num' or 'x', right? I'm just going to call this 'value'. Then, I'm going to apply an if statement directly afterwards. And in my if statement, I am welcome to use my name here, in this case 'value' in my conditional. So I'm going to write if value % two is equal to zero. If the remainder when the value is divided by 2 is 0, then we know we have a even number. So then on the right hand side I can provide my usual arrow syntax, use my println! And here I also have access to the name I chose here, which is value. I can interpolate it and say "{value} is an even number". So we are able to use this name in our conditional and in here, in our actual block of code. Once again, this name is up to us. If we wanted to call this 'x', we certainly could. But if we're calling this 'x', we need to use 'x' right here. And 'x'x right here. Right. If we call this 'my_num' We can use that. But then this has to be 'my_num'. And this has to be 'my_nim'. So the choice is entirely up to us. But whatever we choose, we can use that name within our if statement that immediately follows. And as you might guess, Rust will take the current number, apply this conditional to it and ,if there is a match, if this is the first arm or pattern for which there is a match, in other words, for which the number has a remainder of zero, then we're going to run this code right here, so I can repeat the same logic on the line below, and just demonstrate to you as well that we are welcome to use totally different names. Typically, you're going to see consistency because it's pretty readable to reuse something like 'value' here. But just to show you that Rust is flexible, I can choose another name like 'x'. And here I can say if 'x % 2 != 0', that means I have an odd number. So here I can println! and now interpolate my name of 'x' and say '{x} is an odd number'. This will technically handle every possible scenario because the number is either even or odd. But Rust isn't smart enough to understand this based on our conditional logic. So what we'll need to do is once again add our underscore. And here I can println! and say "Unknown" for now. All right. So this should work. So first up, let's try with an even number. We're going to get "8 is an even number". That means Rust began with these arms from top to bottom. It first evaluated this one substituting 8 for 'value'. If 8 % 2 is 0. That is true. Therefore, we have met our pattern. We're not going to execute any other ones. We are going to execute this block and output "8 is an even number". Here is an example of us satisfying the second arm. I'll provide an odd number. So now, this first arm will not be met or matched. This second arm will be and we're going to see "5 is an odd number". All right. So this line is pretty useless. Unfortunately, it's something that we added because we had to in order to satisfy the Rust compiler. For this type of situation where the compiler is not perfectly smart, but we know for certain that a certain arm will never be met. This arm will never, ever actually be met. There's actually a macro that we can utilize in place of this arbitrary behavior of println! that will satisfy the compiler, and that macro is called unreachable! So right here I'm going to write unreachable! And we're going to provide our exclamation point or mark. All macros in Rust like println! end with the exclamation point. And then I actually have to invoke it like so with a pair of parentheses. Just like println! We don't have to pass it any arguments. It's going to be just fine like this. Once again, this line will never run, but this code is enough to satisfy the Rust compiler, and thus we don't have to provide any arbitrary code like our former println! And secondly, we more explicitly communicate to a reader that this pattern or arm is unreachable. There is no actual way that this line will ever be activated, we just need to add it to satisfy the compiler. The compiler is smart, but it's not perfect, right? So this code will run just fine, so will odd numbers. All right. So that is how we can use multiple values within a match statement.
