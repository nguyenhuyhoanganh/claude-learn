# Project Solution

So let's walk through a sample solution to the coding challenge in the previous lesson. First up, we had to define a 'color_to_number' function that accepted a 'color' parameter, which was a string, and essentially mapped it or connected it to an associated integer value. So for "red", we want to return 1. For "green", we want to return 2. For "blue", 3, and for any other string, we want to return 0. And in this first solution we want to use if,else if, and else clauses. So let's begin here above main. I'll declare my function called 'color_to_number' and it will accept one parameter called 'color', which will be a string. Once again that is ampersand str ( &str ) and it's going to produce a return value of an integer. I'm going to choose an integer type of an i32, but other types are valid here as well, as long as it is a numeric type. All right. So what do I want to do here? I want to begin with an if statement. And let's check this condition first. If our 'color' parameter, if that string is equal to "red", we want to return one1 So in here I'll do if and I'll take whatever 'color' is because it can be different on every invocation. And I'll use the equality operator ( == ) to compare it with the hard coded string of "red". And then I'll provide the logic of what to do in the associated block if it is "red". I want to return the value of 1. Now this is totally valid, but if I realize that there's going to be multiple ifs and else ifs, and that the final evaluation of this function is going to be one of these blocks, I thus know that I can actually implicitly return whatever value the block produces as the final return value of the function itself. Because let's say the color is "red", then this line will be the last one that Rust evaluates. And thus if I simply write 1 without the semicolon, then 1 will serve as the final return value of the 'color_to_number' function itself. All right, as long as I follow this design, this should work and we'll see it in just a moment. So now we want to do another comparison. And that is the 'color' parameter with the string of "green". So because I'm adding on a second condition here I'm going to use else if. We can only use 'else if' once we have an original if statement. And once again this else if condition will only evaluate if this one evaluates to false. So if this has not been met. In other words, if our color is not red, then we're going to move on to this condition. So I will check if our color is green. And similarly in this block I'll return 2 for blue. I'll do the exact same thing. I'll add on another else if and compare my 'color' parameter to a hard coded string of "blue" and provide the value of 3. And finally, I need to account for any other string in the world, and for that I can add on an else clause. Remember, the else is guaranteed to run as long as all of the previous conditions have not been met. So if this is false, this is false, and this is false, else is guaranteed to run. And here I can return 0. And that should do it. So in the body of main, we can practice invoking this function. And let's invoke it with all of our possibilities. So I'll do 'color_to_number' here and pass in an argument of "red". And then I'll also do "blue". Actually let's do "green" since it's next in line. Let's do "blue" and let's do some other color like "purple". So a really good practice before you execute your code is to validate your assumptions. And you do that by stating what you expect the output to be before you actually see it on the screen. So if everything is written correctly, we should get 1 here, then 2, then 3 for blue and then 0 for purple. And that is exactly what we get. Perfect. So the next step is to refactor the function to use the match statement instead. So I'm going to write our solution above. And then I'm going to eventually delete all of this code right here. So with match we're going to provide our match keyword. And then the dynamic value that we want to compare against. We want to match against whatever 'color' happens to be and that will be the parameter. So it's going to be some dynamic value that is flowing into the function that we cannot predict in advance, and that is subject to variance. So since 'color' is a string, the compiler will not compile until we cover every possible string in human existence. Right. So let's begin with the mappings that we absolutely know. We know that for "red", we want to provide a value of 1. So in here we're going to define our patterns or arms. So let's say color is equal to red. I'll write that string here I'll provide my rocket syntax and designate what's going to be the associated value if color happens to be red. And for us that's going to be 1. Now the way this is going to work once I delete this code is this pattern is going to produce a value. Let's say the color is indeed red. It's going to produce a value of 1 for the match statement. And if I don't do anything else, the match statement will be the last logical line that the compiler is going to evaluate. And thus 1 is also going to serve as the implicit return value of the 'color_to_number' function itself. It's going to be produced out of match and then consequently produced out of the 'color_to_number' function as well. That is our first pattern. Let's take care of of our other ones. So I'll provide a comma and a line break. So for "green", we similarly want to connect that to a value of 2. For "blue", we want to connect that to a value of 3. And for any other string in the world, we want to map that to 0. So how do we account for the dynamic situation of any other string? For that we need to use our underscore character. Remember that is a catch all. That is a wild card that will always match. So if all three of these patterns or arms are not valid yet... in other words, if color is not red and it's not green and it's not blue, then it's going to get to the underscore; that will always match. And for this one, for any other possible string, we want to produce a value of 0. Now we can remove this code. And what we have here in our match is functionally equivalent to what we had with if else, if and else. So when I run this code again, I should get the exact same results. One, two, three and zero. And there you have it. All right. So hopefully you can see how a match statement can help us refactor and if and else if construct. All right. Next up define a 'factorial' function that calculates the factorial of a number. The factorial of a number is the product of multiplying a number by every incremental number leading up to it, starting from 1. So, for example, five factorial is basically five times every number less than it. So five times four times three times two times one. If you multiply those numbers you get 120. So my expectation is if I have a 'factorial' function and I give it a value of five, I should get back a return value of 120. And similarly for four, factorial of four is four times three times two times one. That's going to be 24. So if I invoke my 'factorial' function with an argument of four, I should get a return value of 24. And we want to write two solutions. The first one is not going to use recursion, so it's going to use regular iteration. And the second one will use recursion. And both of these can be tough. So it's totally fine if you struggled with this a little bit. Let's begin with the non recursive solution. And I'll declare them in two separate functions. I'll call this one 'factorial_iterative' because I'm going to use iteration. In other words I'm going to use a loop to solve the problem. So I'm going to accept my 'number' parameter. Let's make this an i32. And I'm going to produce an i32 as well. All right. So what do I need to do here. I need to create a loop where I either count up from 1 to my number parameter, or I count down from my 'number' parameter down to one. And for every one of those numbers in between, including my starting point, I want to keep a running product. I want to keep multiplying as I go on each iteration. So let me first begin by declaring a product variable. And when we're multiplying, we need to find some starting point, some starting value that we're going to just multiply against. I'm going to pick a value of one. And we'll see how this works in just a second. And as I iterate I'm going to keep changing my 'product' variable to have the cumulative product up to that point in iteration. And the key takeaway here is because I know that this value is going to be changing, I'm going to keep reassigning this variable on every single iteration. I'm going to make it mutable with the 'mut' keyword. Then I'm going to need another mutable variable to keep track of where I am in the actual progression. So in my solution, I'm going to start at the actual number that I receive. Let's say it's five, and I'm going to count down from 5 to 1. So 54321. I need a variable that keeps track of what number I'm currently on, because I'm going to use that to figure out when I'm going to terminate my loop. And that is going to be when I reach the bottom, which is a value of 1. So I'm going to create a mutable variable called 'count'. And I'm going to initialize it with a value that is equal to whatever my 'number' parameter is. So for example, if I invoke my 'factorial' function right here 'factorial_iterative' and let's say I pass in a value of 5. So I want to count down from 5 to 1. And as I multiply, I want to keep multiplying the product by each number that I am progressing through. And so here what I'm doing is initializing 'count' to be equal to whatever the number is. In this case, I'm passing in five. So I'm going to start count at five and decrement it or decrease it once on each loop. And also once on each loop I'm going to take the current value and multiply it by 'product'. All right. So this is where the magic happens. This is where I'm going to do my loop. I chose to use a while loop. But it's totally fine if you chose a different approach like the like the 'loop' keyword. All that matters here is that you created some kind of iteration, some kind of progression, where we keep repeating some logic until we get to an end. Right? So I decided to use this logic while my 'count' variable is greater than zero. As long as that is true, I want to keep running the logic in this block. Now, of course, if I do nothing here, I'm going to have an infinite loop because 'count' will never change. And if 'count' is set to five in this example, well 5 is always going to be greater than 0. So before I forget, on every iteration, I want to make sure that I decrease 'count' by one. I can of course write it this way. Take the current value of 'count', subtract one and reassign it back to my 'count' variable. So for example, in the first loop we're going to take 5. We're going to subtract one to get 4. And we're going to reassign 'count' to 4. And that's totally fine. But the shorthand syntax for this is just minus equals one ( =1 ). That accomplishes the exact same result. So now I have a loop that's going to going to go 543210. Once it gets to zero, 'count' is no longer greater than zero. So the loop terminates. So I have avoided my infinite loop. Now the next step is of course, taking whatever my 'count' is and simply multiplying what I have in 'product". So I'm going to take 'product', and once again we can write something like this where we take 'product', multiply it by the current 'count' that we are on, taking that product and rewriting whatever we currently have. But the equivalent line to this would look like: product *= count. So just to show you what this would look like on a sample iteration, let's say the very first one. So 'product' is equal to 1 right now and 'count' is equal to 5. We're going to check if 'count' is greater than zero. That is true. So the block evaluates. We're going to take 'product', multiply it by the current 'count' which is 5 and then overwrite the current value in 'product'. So one times five is going to give us five. So at this point, 'product' is going to be equal to 5. Then we're going to decrement 'count' by one. So at the end of the first iteration, the first loop, 'count' is equal to 4. So then we're in a while loop, so the logic begins from the beginning of the next iteration. Rust says "Okay, is a count of 4 greater than 0"? The answer is yes, it is. So we re-evaluate the loop. We create a new iteration. So this time around, 'count' is 4. We're going to take 'count'. We're going to multiply 4 by our product which is 5 to get 20. And then we're going to overwrite 'product' with that value. So 'product' is now 20 after this line. And then we once again decrement 'count' to get to 3. And then we begin in the next loop. Is 3 greater than 0? The answer is yes. So we execute the block again. We take our 'product' of 20. We multiply it by 3 to get 60. And then we overwrite 'product' with its new assignment of 60. Then we decrement 'count' to two. We get back to the start of the while loop. 2 is greater than 0, so we repeat it again. We take our 'product' of 60, multiply it by 2 to get 120. Overwrite 'product' with 120. Decrement 'count' one more time with a value of 1. We start the loop again. 1 is still greater than 0, so we are going to do one more iteration. We take our product of 120. We multiply it by the count of 1. We get 120. We overwrite 'product' so it stays at 120. 'count' is decreased to zero. Then we get here. Back to the beginning. Is 0 greater than 0? That is false. So the loop is done. It's terminated. And at this point 'product' is equal to 120 which is the exact 'factorial' that we want. The only thing that remains is after the loop concludes. We want to make sure that we send that 'product' back up to the caller, back up to the outside world. So we have to return it by writing it as the very last evaluation in our function. And this of course is totally valid as well. But the Rust standard is just to write it like so, and that produces it as the final return value. So with that, I can remove this and let's try running it with 5. Like I said, it should be 120. And we can see 120 here. And if I, for example, pass in a value of 6 that should be equal to factorial of 5 times one more value which is 6. So 120 times 6 should be 720. And we can see it appears to be working. And similarly factorial of 3 should be 3 times 2 times 1, which is 6. And you can see it's working here as well. Okay. So that is the iterative solution. Now let's move on to the arguably more complex implementation. Some would say less complex, but I'll let you be the judge of that. And that is going to be the recursive solution. So I'll declare a separate function. I'm going to call this one 'factorial_recursive'. So my function signature, which is the combination of the parameters and their types as well as the return type, is going to be the exact same as what I have above. The purposes of these two functions is one and the same. They're both going to give me back a number, the factorial of the argument. They're just going to take different solutions, different approaches to solving the same problem. So I'm going to copy exactly what I have here. I'll accept a 'number' parameter set to an i32 and provide a return value of an i32 as well. But here we're going to use recursion. So there's no real magic trick to figuring this out. It really just takes practice. But the key to recursion is trying to identify the common pattern that you see in a problem, or thinking about how a problem can be broken down into a series of smaller subproblems. And I tried to give you a little bit of a hint here, which is for every factorial, that factorial is equal to the number times the factorial of one less than itself. So, for example, the factorial of 5. Let me just write this as a comment. The factorial of 5, If you recognize that this can be treated as five times four times three times two times one. And you recognize that this sequence of values right here is equal to factorial four. Right? This is equal to the factorial four. If you saw that pattern, that was the first step to realizing how you could implement this as a recursive solution. So if you think about factorial four and you thought of that in a similar manner of, well, factorial four wants to take four and multiply it by three times two times one. And then you saw three times two times one and realized, wait a minute, that's factorial of three. You can start to see how the problem starts to break down into smaller subproblems. The factorial of a number is itself times the factorial of one less than itself. In other words, the factorial of five is five times the factorial of one less than itself, which is four. The factorial of 4 is 4 times the factorial of one less than itself, which is 3. So let me write out the solution here one step at a time. Once I see this pattern, I can write it out and it's going to look like this. I'm going to take the current number that I get in, and I'm going to multiply it by the factorial of one less than itself. So I'm going to take the number that I get and subtract 1 from it. So for example, if I pass in a value of 5 right here, it's going to evaluate to exactly what I have right here: 5 times the invocation of the same factorial function with an argument of 4. Of course, the only problem here is I need to make sure that I'm calling the actual recursive function itself, which means the function that I'm within, and it is literally called 'factorial_recursive'. So this is what I'm doing here. The problem with this is that this would create an infinite loop, because there's no point at which this stops, right? We're going to have five times four, then four times three. Eventually we're going to start getting to negative values, because even when we get to zero, we're going to do zero times the 'factorial_recursive' of -1. And then we're going to start going into negative infinity. Right. So it never really concludes. And to prevent that we need our base case. And the base case is going to look like this. Right here, I'm going to write ... once I get to 1, which is going to be on some recursive call, I no longer want to keep going. I don't want to take one times the factorial of zero. I know that this is my stopping point. This is where I want the recursion to conclude and terminate. So here, rather than calling 'factorial_recursive' again, I'm just going to produce a final value and I want that value to be one. Now if I provide this right here, this will be insufficient because Rust will continue past this if statement and evaluate the next line. So this is an example of where I need to actually use the 'return' keyword explicitly to designate that I'm going to stop the function and return right here and prevent this part from being reached. But again, the only time when I care about this if statement being true is when we get to the very last call, when we get to the number being 1, that's when I want to say take one, send it back up to the previous recursive call. Otherwise, just take the current number and keep multiplying by the factorial of one less than itself. And believe it or not, this is the solution. So I want to show you that this is going to work. And to do that, I just want to invoke the function with a sample input. So I'm going to change this to to 'factorial_recursive'. And let's start with a value like 3 and compare both of our functions. They should both produce the same return value. We can see they're both 6. And let's do something like 4. And that should both be 24. So now that you know that it's working, let's walk through how this is going to look like from the program's perspective. And I think four is a is a nice number to do it with. So we're going to call 'factorial_recursive' with number set to 4. So keep that in mind. 'number' is currently set to 4. We're going to check if 'number' is equal to 1. That is false. So we skip the if statement. And then we say that the return value of this function when invoked with an argument of 4, should be the result of taking the current number of 4 and multiplying it by the 'factorial_recursive' of one less than four, which is 3. So in order for us to provide the final return value for this invocation, it needs to kind of pause this execution and start a brand new function execution of 'factorial_recursive'. But this time around with an input of 3. So you can think of this like a separate level or layer or dimension. We are kind of in a pending state here in our first top level invocation. while we wait for 'factorial_recursive' with an argument of 3 to come back with its return value, come back with its answer. So we start anew, almost on a separate level or separate plane with number being set to 3, right? This is the evaluation of this function call right here. So once again we check if 'number' being 3 is equal to one. It's false. So we skip this. And on the second call which is executing right here you can think of it as being right below here we're returning the current number of 3 times 'factorial_recursive' of one less than three, which is 2. So now we have two functions that are pending. Two functions that are awaiting their own recursive calls before they can complete their own execution. So we start a brand new execution of 'factorial_recursive'. But this time around with an argument of 2. We compare 2 with the value of 1. That is false. So the result right here of this invocation is the return value of 2 times 'factorial_recursive' with one less than two, which is 1. So now we have three functions that are pending that all need to evaluate and wait until they get their return value so that they can multiply and bubble the result all the way back up to the original invocation, where number was equal to 4. So now we finally have this evaluation of 'factorial_recursive' with an argument of 1. So once again we start from the top. Finally our parameter of 'number' is equal to 1. So this if statement evaluates to true and we can return back the value of 1. No more recursion. So now at this level, three levels down, this function call has evaluated to two times one, which produces a value of 2. So now the program understands that the return value of this call right here is exactly what we have right here which is 2. So now we start bubbling back up. So now we know that 3 times 2 is 6. And thus the program knows that the return value of 'factorial_recursive' with an argument of 3 is 6. And that bubbles back up. So that function is done with its call. Now we're back all the way at the very first execution, at the very top level, where we started. 4 times 6, we now have a final evaluation. We performed the product to get 24. That becomes the implicit return value of the function. 'factorial_recursive' produces 24 back, gives it back to the caller. We get 24 here, and that's exactly what we print. All right. So I want to be very clear here for those who found this either confusing or those who get it, but who think that they can never get good at this. Recursion is among the most difficult problems in programming. And truth be told, you're probably not going to be dealing with it day to day in your actual code. It just comes up very frequently on coding interviews. And even then, not all the time. So it's not important that you obsess over this idea or think that you are a bad developer. If this is a hard topic, or it's hard for you to reason about this kind of thinking in multiple dimensions. The key takeaway is that you understand what recursion is, and that is a function invoking itself. In our case, the 'factorial_recursive' function calls the 'factorial_recursive' function in its body. The second key takeaway is if you just keep doing that, you're going to go until infinity. You're going to go into a recursive loop. So with recursion we need what's called the base case which is which is this thing right here. And that is a condition that forces the termination of that recursion. We can't just keep going levels and layers down, down, down. We need a point where we stop that so that we can bubble all of those results back up to the original invocation that kind of started that chain reaction, if you will. I do like to think of recursion as this series of chain reactions. Right? Like one explosion sets off, another explosion sets off, another explosion. Again, key takeaway, don't worry if this challenge was hard, I just wanted you to think about recursion and grapple with it a little bit. But don't worry too hard if this was a tough topic, and I really do think that solutions like this are totally fine and are also valid. This hopefully gave you good practice with things like mutable variables, while loops, iteration, changing variables on the fly, understanding the value of a variable, implicit return values, obviously things like match statements and if statements and all that. So lots of topics covered in this section and in this coding challenge. I hope you had fun and I will see you in the next one.
