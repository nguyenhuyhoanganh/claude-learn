# Underscore in a Match Arm

Let's begin this lesson by refactoring our previous if and else if code to utilize the match keyword. The word refactor means "to restructure or improve existing code without altering its design". It means making code better by adopting a cleaner or simpler or more efficient solution while maintaining all of the original functionality. Basically, making the code better. So to replace this, we'll begin with the match keyword, and this time around we will match on the 'season' variable above. Once again, we'll provide our curly braces. And this is where things start to get interesting. A Boolean has a finite set of two possible values in comparison, a string technically has an infinite collection of possible values, right? It can have any number of characters in any order. We are matching on a string, so we can certainly provide "summer" and "winter" and "fall" and "spring" here. And we will. But you'll notice that the compiler will still refuse to compile, because we will have not provided all of the possible options for a string. We're going to see how we can solve that problem in this lesson. Let's begin here with "summer". That is the first thing we want to match with our 'season' variable. And, just like in the previous lesson, we can provide our rocket syntax ( => ) here or the arrow syntax an equal sign and the greater than sign, and then a pair of curly braces for the block of code to execute. If I want the exact same thing as above, I can simply copy and paste this println! over here and then repeat the exact same logic for "winter", which is covered in the else if above. So here is the next thing to match. If 'season' is equal to "winter", here's the associated block and that will execute this line right here. Now if we want to, we can actually just remove the curly braces entirely and just write our println! that will treat the println!'s return value as the final return value of the block. That's totally fine. println! returns an empty tuple, a tuple without any actual elements inside it. That is a valid data type, and we'll be totally fine as the final return value of the entire match expression. So let's simplify this. I'm going to remove my curly braces and I'm going to move println! right here. And that means to separate each possible arm or pattern, I now have to switch to commas. So without the curly brace block syntax, we have to separate with commas. Same thing for "winter". I'll remove my opening brace, my closing brace. Move this on the previous line and add a comma at the end. Now, technically speaking, every match arm must return a value of the same type. They can be different values, but they must be of the same type. For example, in the previous lesson, when we mapped our booleans to integers, both of those arms returned an i32 and the same rules apply here. Technically, we're returning an empty tuple from both of these arms, which should be satisfactory to Rust. We cannot, for example, have one arm return a Boolean, have the next one return an integer and have the next one return a float, because that way there would be no way to assign the return here to a variable, because the variable wouldn't know what data type it would expect. It's kind of Rust's way of ensuring consistency. Every match arm has to produce a value of the same type. Now at this point, the program will not compile. The compiler is concerned because we are not covering all of the possible variations of a string. If I hover my mouse over 'season' here, the error message will state "non-exhaustive patterns". "Ensure that all possible cases are being handled". Well, how can we handle all possible cases if there are technically an infinite number of possible strings? Here's what we can do. We can provide an underscore character ( _ ). That is the character that you can access by holding the Shift key and pressing the key to the right of the 0 on your keyboard. The underscore character ( _ ) is what we call a catch-all pattern or a wildcard pattern. It's a catch-all pattern because it will always match. It will match any possible value. So in this scenario, you can think of an underscore as being any other string in the world. It is functionally the equivalent of an else statement. It will cover all other scenarios. The syntax looks the exact same. Instead of a string, we do an underscore, a space. We'll provide our arrow syntax, equal greater than ( >= ). Then our logic will be this println! right here. And now you'll notice the code will compile. We can execute the program. We should see the same result. We currently have "winter" so we should see "Brr so cold". If I change to something else like "autumn", that should be hit by the else here and the underscore character here. And we should see "Lots of rain!". So once again, we are matching or comparing every arm with whatever value happens to be in the 'season' variable. In the previous lesson, we did Boolean comparisons or Boolean equality. Here we are doing string comparisons or string equality. So our code is producing the exact same result. So I would consider this refactor to be complete. So with that, we can remove this previous implementation. And there we have it. Now very important to note: a match statement is going to look for the first match. And then it's going to execute the associated block and it's not going to progress any further. That is why it is very important to place your underscore character as the very last arm. If you place this arm earlier, it is guaranteed to match, and thus it's going to prevent any future arms from being evaluated. In fact, the compiler will give you a warning if you accidentally do that because it invalidates every following arm. Just to show you this, I can move this -- let's say up here -- and let's change our season back to "winter". With "winter", we want to access this value and output "Brr, so cold". But the match arms are evaluated in order. So when Rust looks at 'season', which is "winter" and says does it match this catchall? Does it match this wild card? The answer is yes. So this is the first arm that will be matched. And thus we're going to see "Lots of rain!". Here is "Lots of rain!". And here are our warnings which we can also see right here in the editor. The warning is "unreachable pattern", right? There is no way for the code to ever logically be able to reach either one of these patterns, and that is exactly what the compiler is warning us about. So of course, the fix here is to always have your underscore character as the very last option in your match. And many times the underscore is a requirement, right? Because with something like strings, there is no way that we can type out every possible string in existence. So we're going to need to use something like an underscore to capture every other scenario. So in this situation, it is literally every other string in the world. I can literally do a string of an empty string or a space. And we're going to hit "Lots of rain!", right? So it's very much like an else statement. It will cover every other scenario, assuming that a previous arm or a previous pattern has not been met. All right. That's all there is to cover in this lesson. So I will see you in the next one.
