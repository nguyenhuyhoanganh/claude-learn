# Project Solution

Let's walk through a sample solution to the coding challenge in the previous lesson. First up, we had to define a function called 'apply_to_jobs' that accepted two inputs, number and title, and output their content in this string. So above main, although the location of the function in the file does not matter, but above main, I'll use my 'fn' keyword to declare a new function called  'apply_to_jobs' . My function names are written in snake_case, which means all lowercase with an underscore separating every two words. Then, I'll provide parentheses for my parameter list, and I'll provide each parameter name, each input in other words, along with its designated type. So first up, I'll have a 'number' parameter, then a colon and a space. And this type will be an i32, an integer. Then, to separate my parameters, I'll do a comma and a space, and my next one will be 'title'. And that will be a string, a piece of text, and the designation for that type is &str. We're going to come back to what this symbol means later on in the course. For now, you should just memorize this designation (&str) as indicating a string, a piece of text. I'm not going to produce an explicit or implicit return value here, so this function will implicitly return a unit, an empty tuple. And if that's the case, I do not need to specify a return type with my arrow. I can immediately declare my braces and declare the logic. So here I'll use my println! macro and just interpolate my variables in this sentence. So "I'm applying to" and I'll dynamically inject my 'number' parameter, whatever that input happens to be. And then the same thing with my 'title' parameter and then jobs. So you can think of your parameters almost like variables that have been pre-assigned to whatever values are going to be passed in each time the  'apply_to_jobs' function is invoked. And the beautiful part here is each invocation can pass in different values. So we retain the commonality of this behavior while still allowing room for customization. So just because we've declared the function does not mean it's going to run. In main, we actually have to invoke  'apply_to_jobs'  and pass in the expected inputs. So we need two of them and they need to match the types specified here. So let's pass in the exact values we see here: 35 and a string of "Rust developer". And we should see "I'm applying to 35 Rust developer jobs". And there we have it. All right. Our next function was is_even. And its responsibility is to accept a number and return a Boolean that tells you whether that number is even or not. So once again, 'fn' to declare a function. This one is called is_even. It's going to accept a single number, and the parameter is also called 'number'. It will be an i32 and this time around I want to specify my return type as a Boolean. So for that, I'll provide my arrow syntax and write Boolean and then provide my curly braces for the function body. So here we are declaring the contract that we want the compiler to hold us responsible to. We are declaring the promise that we are making, that we are going to fulfill in the function body. We have to return a Boolean or the compiler is unhappy. So if I save this, you'll notice the compiler says: "You told me to expect a Boolean return". Right now, there's nothing happening, so you're implicitly returning a unit. That is a mismatch. So here, in order to calculate whether a number is even, we can take its remainder when it is divided by 2 and compare that result to 0. If the remainder when a number is divided by 2 is 0, then it is true that it is even. So, I'll take my integer and we'll use the modulo or remainder operator ( % ), which is this. This is Shift + 5 on your keyboard. I'll take the remainder when number is divided by 0, and using my equality operator ( == ), I will compare that value to 0. This evaluation will produce a Boolean. So if I omit a semicolon and just allow this to be the last line that the compiler will read, then this produced Boolean will also be the Boolean return value that is_even returns back out to the caller. Now, there's nothing wrong with assigning this to say a variable like 'result' and returning 'result' like this. That's totally fine. We can accomplish everything on one line, which is pretty elegant. Once again, if you accidentally add the semicolon, that is like the period at the end of a sentence. That completes that line of direction, and thus the compiler says you're not actually producing a Boolean, you're creating a Boolean and then doing nothing with it on line #6. So this function still returns an implicit unit. So if you are providing a semicolon, you do need the explicit 'return' keyword. But the idiomatic way of doing things in Rust, which means the favored community convention, in other words, is to do implicit returns and just produce your final return value as the result of the last evaluated line without a 'return' keyword. So I will remove that, omit the semicolon, and that should do it. So in 'main', I can invoke is_even with a sample argument like 8. So this function will produce a return value of true for the even number of 8. But right now we're not doing anything with that return value. We're not printing it to the screen. So we're not going to see anything here. But rest assured, we are invoking this function and getting its output back in 'main'. So if I want to see that, I can of course use a println!, provide room for some dynamic content and move my invocation right here. So whatever is_even returns is what I'm going to print with my Display formatting. And there is true. And if I update this to be a odd number like 9, is_even should give me back false. And indeed it does. All right. So we have one more function that we need to declare and it is called 'alphabets'. So right here, we'll do 'alphabets'. And this function is also going to accept a string. We're going to call that parameter 'text'. The parameter names are up to you. But whatever you declare here is what you're going to be obligated to use in the body of the function. So 'text' is also going to be a string. And this function is going to return a composite type. It's going to return a type that is capable of storing multiple values inside it. In this case it's going to be a tuple, more specifically a two element tuple where both of the elements are Booleans. So I'll designate that return type after an arrow right here. Our syntax for the type of a tuple is parentheses, but this is insufficient because this indicates a return type of a unit. An empty tuple. We need a two element tuple, And furthermore, the compiler is not only going to validate the length, but the actual types of the respective values inside that tuple container. So if I want a tuple of two Booleans, I'll specify right here: The first element will be a Boolean, and the second element in my tuple will be a Boolean as well. All right. This is totally different from a Boolean like this. Or a tuple of this, rather. Or a tuple like this. So length matters and the data types matter. I want this. And a tuple's strength is its capacity to store elements of different types. But we are not obligated to use that functionality either. It's totally valid to have a tuple of two Booleans, right? Totally valid as well. So here is my logic. So I want to check whether the 'text' parameter contains the lowercase letter 'a', and that will be the first Boolean that I want to package up in the return tuple. And then also validate whether the text contains the lowercase letter 'z', and that Boolean should be the second one in my returned tuple, and the instructions here recommend using the 'contains' method and provide the documentation for that. So I'm going to bring over that documentation from my other screen. There we have it. And we see a example of the function signature here. And if this is confusing, I always recommend reading the description and also looking at the examples. Sometimes the examples are actually the quickest way to understand what is going on. So in this case the 'contains' method is called on a string. Once again, to call a method we use a dot, then the method name, then parentheses, and then some potential parameters or inputs. So in this case we have a string of "bananas" and 'contains' checks whether that string contains whatever we provide here. So it looks for the presence or the occurrence of this within the string that the method is invoked upon. And we are not obligated to pass in multiple characters. We can just check for the presence of just one character, for example. And it's not going to care how many times it occurs, it's just going to look for at least one occurrence of this substring. All right. So with that back in our code editor, we know that if we have our text string, we can call the 'contains' method and pass in an argument like a lowercase 'a'. And that will give me the Boolean that I want. But that is only one of two Booleans. So if I want my second one, I have to do the exact same thing and check for lowercase letter 'z'. Now I can assign both of these evaluations, these Booleans to variables, and then assign those inside my tuple. So just to show you what that would look like, I can call this variable 'a', call this variable 'b' just for simplicity. And then on the last line, I can produce an implicit return value of my tuple that contains those 'a' and 'b' values from above, which are Booleans, as we can see here. But once again, we can simplify this, knowing that we can produce this return value just on a single line, where we can create our tuple and then call the method on the 'text' string within the tuple as well. Once again, there's nothing wrong with this solution. I think it's very elegant, but this is just really simplifying things and just making things as super clean as possible. Once again, if you're the kind of person who likes assigning variables to describe the data that you're working with, I think that's actually a strength, not a weakness. And if you wrote code like this, I would be more than happy with it. But just to show you the kind of simplest approach we could do is just creating the tuple right here and in the very first position where we need a Boolean, I'm going to paste that code, then provide a comma to separate from my second tuple element, which also needs to be a Boolean. We'll provide the Boolean that is returned by the 'contains' method right here. Remove all this code. We do not need a semicolon here because this will give us a Boolean. This will give us a Boolean. This final tuple is going to store those two Booleans and return itself. And that satisfies the criteria specified right here. All right. So we can go ahead and copy and paste these examples directly into the main function. And I'm just going to remove these invalid bits of syntax right here. And if we take a look at these sample invocations, the string "aardvark" has lowercase letters of "a", but no lowercase z's. So true for a, false for z. That's what we should get here. The zoology string has a z but no a, so it's going to give us false for a and true for z. And then finally zebra has both an a and a z. So true and true for both of those respective characters. And also worth noting that the 'alphabets' function returns a tuple. A tuple does not support the Display trait, but it does support the Debug trait. So if we want to print it out, we have to use the format specifier with the question mark ( ? ). The question mark is what asks us to print it in Debug format. You'll notice if I try to remove this, the compiler understands that we're trying to output a tuple, and tuples do not support the Display trait. So this is insufficient. So I'll add that. Run that. And we can see that the results here: true false false true and true true match the expectations and that's all there is to it. All right. So here we can see a couple of different variations on functions that accept a different number of inputs. 'main' accepts nothing, "is_even' accepts 1 input as does 'alphabets'. 'apply_to_jobs' accepts 2. They all have different ways of declaring their return values and what they are. In fact, if you're returning an implicit unit, you do not even need the arrow syntax. But here we have a Boolean. Here we have a tuple. Your return type does not have to match any of the types of the parameters. It all just depends on what you are accepting into the function and then what you want to produce as the final output, which is the return value. Once again, focus on making lots of really small functions that each have a single responsibility, that each focus on just one calculation or one bit of procedural logic. The way that you build up a really effective program is by taking lots of small units, lots of small building blocks. You can think of each function like a separate building block, like a recipe dedicated to one thing, and then composing those building blocks into more complex logic. So the key takeaway here is try not to write really long functions that have a lot of responsibilities. And there's certain signs you can look for that are indicative of that. One obviously is having a really long function body with, say, 100 lines of code. That function is probably doing too much and you can break it up. But there's also other signs like if your function is accepting ten parameters. Well, why does it need ten pieces of data? Is there logic going on that really involves all ten pieces of data? Can you break up that complexity into smaller building blocks, smaller functions that each chip away at the larger problem, especially if you have some kind of logic that is reusable that you're going to need multiple times? It can help to extract it to a function. Don't be afraid of making lots of small things. That is a generally good piece of programming advice in any programming language that you are working with. All right. That's all there is to cover in this coding challenge. So I will see you in the next lesson.
