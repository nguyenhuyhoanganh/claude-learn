# Project Solution

Let's walk through a sample solution to the coding challenge in the previous lesson. The first thing we had to do is define a 'cereals' array with five 5 Strings. So here I'll declare my 'cereals' variable assigned to an array, which I create with a pair of square brackets. And in here I'll provide my heap Strings. So I have to use the capital 'S' String here. And I'll use the 'from' constructor function to create a String. And my first value will be "Cookie Crisp". I'm going to copy and paste this value a couple times down and then just replace my actual content. So for the second one we'll have "Cinnamon Toast Crunch". Then we have "Frosted Flakes", then we have "Cocoa Puffs", and then we have "Captain Crunch", all very delicious cereals, but of course not good for you. Very sugary. All right, so let's extract some slices. First up, we have to declare a 'first_two' variable that extracts a slice of the first two cereals. So remember, a slice is a borrow or a reference, but to a portion or segment of the original collection type. So we're not borrowing the complete 'cereals' array. Although we could do that. We are borrowing a section of it. It's still fundamentally a borrow. So we have to use the reference operator. Then we provide our array of 'cereals' and then the square brackets to specify the starting point and the ending point. So for the first two cereals, "Cookie Crisp" and "Cinnamon Toast Crunch", I can start at index 0 and pull up to, but not including index 2. As a reminder, what you can do is subtract the first number from the second to get the total number of elements in the slice. So 2 - 0 means 2, and that means there are 2 elements being captured in this slice. Also, as a reminder, if you are starting from the beginning of the array in the reference, you can remove the 0 and that will accomplish the exact same result. So here I can println! and output my slice using Debug formatting, and that should be a reference to a portion of the first 2 elements, which is just these 2. All right. Next up, declare a 'mid_three' variable that extracts a slice of the middle three cereals. So these right here same protocol. We're going to give our borrow operator ( & ) and then provide the original collection type. And this is totally valid. But this is borrowing a reference to the complete array. We just want to borrow a reference to a portion or fragment of it. That is what a slice is. So once again square brackets. And this time around if I want to start at "Cinnamon Toast Crunch", that is the second element. But the index starts counting from 0. So "Cinnamon Toast Crunch" has index position 1. And then I want to go up to "Captain Crunch" because I want to go up to it but not include it. And that will capture this collection of 3 values in the middle. So 0,1 ,2, 3, 4. I want to go up to index 4. 4 - 1 is 3. So this should capture 3 elements. And these are the 3 that we want. I will output 'mid_three' once again with Debug formatting. And that should be "Cinnamon Toast Crunch" through "Cocoa Puffs". And there we have it. All right. Declare a 'last_three' variable that extracts a slice of the last three cereals. Same principle. We're going to do our borrow operator ( & ) 'cereals'. And if we want the last 3, we can start right here. It's the third element. So it is at index position 2 since we start counting from 0. Then, I want to go up to the index position that is one past the last index in the array. So 0 1 2 3 4. "Captain Crunch" is position 4 and I want to include it. So I want to go up to this hypothetical index position 5. It's not going to be included so we don't have to worry. But this will also capture index 4 which is "Captain Crunch". And as a reminder, if you want to start from a position in the array and pull all the way to the end, you can calculate one more than the final index. But an easier way to go to the end is just to omit the value after the double dots, so you can omit the value before to start from the beginning and omit the value after to go all the way to the end. And that's going to give us the last three. So let's output that in Debug formatting with my question mark ( ? ). And we should get "Frosted Flakes" through "Captain Crunch". And there you have it. All right. Using the 'last_three' slice, target the last element which is "Captain Crunch" and replace it with a string of "Lucky Charms". Then print the complete 'cereals' array. So first things first. If we're going to be swapping or changing or mutating even a slice in any way, we need to make the slice mutable. And then the slice is still fundamentally a borrow. So in order to have a mutable reference to something, we also need to make the original owner mutable, so it is aware that one of its references is going to be changing the data that it is the owner of. So you'll notice if I add the 'mut' keyword right here to enable us to overwrite something in the slice. That is the correct syntax, but it is insufficient and the compiler will tell us "cannot mutate immutable variable 'cereals'". Throughout this entire program, 'cereals' is going to remain the owner of this array, right? Because none of these things are going to take ownership. They are all references. They are all borrows. Granted, they are borrows of fragments or portions of the array, but the same rules apply. 'cereals' is going to be the owner, so the owner needs to be made mutable to enable a reference to it to modify all or part of its data. So now that takes care of the permissions. Now we have to target the element within my slice and overwrite it. Now keep in mind from the perspective of the 'last_three' slice, this is how it is viewing its portion or its fragment. And that is it's almost like its own independent section. So if we zoom in on these three elements in that slice, "Frosted Flakes" has index position 0, "Cocoa Puffs" has index 1 and "Captain Crunch" has index 2. Right. "Captain Crunch" has index 2 in the slice, even though it is index 4 in the larger 'cereals' array. We are operating from the perspective of changing the element from the 'last_three' slices perspective. So 'last_three' and then to overwrite I need to provide index 2, which is "Captain Crunch" from its perspective. And then to replace the value at that index. I'm going to provide my new string here after an equal sign. And this can be "Lucky Charms". So this will mutate the string right there, which is also going to affect the original 'cereals' array that 'last_three' is a slice of, right? We are modifying this thing from a view of this slice, but that inherently is going to modify the same element as it is stored in the larger 'cereals' array. So we could print out 'last_three' to confirm that everything worked. But we can also output the value of 'cereals', and that will also confirm that we did what we were supposed to. So if I output 'cereals', my expectation is this is going to swap with "Lucky Charms" and indeed it does. Perfect! So that is array slices. The next thing we had to do is to work a little bit with string slices. So declare a 'cookie_crisp' variable and make it a reference to the "Cookie Crisp" string. That is the very first String in my array. So once again I can target my array and I can target index 0. But if I do this, I will attempt to move ownership of "Cookie Crisp" from the array to the 'cookie_crisp' variable. And that creates a weird place or a weird thing in the program, because then 'cereals', the array, would be the owner of these 4 Strings, but loose ownership of "Cookie Crisp" to the 'cookie_crisp' variable. So 'cereals' is the owner of the array. The array is the owner of any of its nested content, and taking a portion of that out and assigning it to another owner is kind of weird. So Rust will generally prohibit this, but we are more than welcome to borrow a reference. So as soon as I add the borrow operator ( & ) here, 'cookie_crisp' will be a different type. It will be a 'ref string' ( &String ), a reference to a string. And that's exactly what we want. So now let's declare a 'cookie' variable. And it's going to extract a slice of this string. And we want to target the text "Cookie". So we want to get the first 6 characters. So once again, right here I am going to borrow a reference to a starting String. I'm going to borrow a string slice from a String reference. So just like with this syntax right here, we're going to begin with the borrow operator. And we're going to say we want to take some portion or some fragment of something. We want to borrow an address to just this segment of our "Cookie Crisp" above. That is a String reference ( &String ). And then in here I can designate from where to where. So I'm going to specify from byte 0 up to byte 6. And since we're dealing with English alphabetic characters we shouldn't have any issues. And that should do it. So cookie right here is 0, 1, 2, 3, 4, 5, 6. So this space in the middle is index 6. We want to go up to it but not include it. And then once again if I subtract 0 from 6, I get 6. So I know I have 6 characters being extracted here. Or 6 bytes is the more proper way to say that, and that will be the 6 in "Cookie". Once again, if you are pulling from the beginning, you can follow the same rules and omit the 0 before the double dots. That's going to work. And I can output the value of 'cookie'. And we should literally see the text "Cookie". So our variable has the same name as the actual content it holds, which is kind of funny but not really. All right. Next up, declare a 'cocoa_puffs' variable and make it a reference to the "Cocoa Puffs" string. So same principle. We're going to target our 'cereals' array. We're going to borrow a reference to one of its elements. So once I provide a numeric value of the index in here, I'm extracting an element. And I'm actually taking a reference to that element. So you can think of this borrow operator ( & ) as applying to whatever String I am referring to here. So if I want to target "Cocoa Puffs". That is 0, 1, 2, 3. Index position 3. And now I have a String reference ( &String ) to that. And then I want to declare a 'puffs' variable that extracts a slice of the text "Puffs". So it's a little bit of the inverse of the previous problem. Now we want to target the end of the String. So once again borrow operator ( & ). I'm taking a string slice of my "Cocoa Puffs" String. So here I'll provide my square brackets for the bytes to extract. And if I want just the text of "Puffs", we can count here 0, 1, 2, 3, 4, 5, 6. The "P" starts at index 6. I could figure out the final index position and add 1 to it, but of course the easier solution is just to go to the end with double dots, and then we don't have to worry about it at all. All right. So with that I can output the value of the 'puffs' variable, which should be the string "Puffs" with a capital "P". And there you have it. All right. So everything we have here is examples of slices. We have both array slices as well as string slices. Once again they are borrows, they are references, but to portions or segments or fragments of a larger collection whole.
