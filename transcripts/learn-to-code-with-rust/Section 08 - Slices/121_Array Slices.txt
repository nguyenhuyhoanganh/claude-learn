# Array Slices

We can create slices from a variety of different composite types, not just strings. For example, we can declare an array slice, which is a reference to a portion or chunk of an array. We can utilize the same borrowing syntax. We'll start with our ampersand, the borrow operator ( & ), then provide the array, and then a pair of square brackets where we declare the range or interval of elements to target. Now with a string, the numbers inside the brackets represent the starting and ending byte with an array. The numbers inside the brackets represent the starting and ending index positions, but the general principle remains the same. So for example, let's declare an array called 'values' and I'll populate it with 6 integers. We're going to do 4, 8, 15, 16, 23, and 42. And with our semicolon. And there is our six element array. On the very next line, I'll declare a new variable called 'my_slice' and apply the same syntax. So we always begin with the ampersand ( & ). because we are borrowing a reference. We are not taking ownership of either the complete 'values' array or a fragment of it. Rather, we are borrowing. We are borrowing in this case a partial chunk or slice of values, and that slice can be as small or as large as we would like. So here I'll declare my original array, then my square brackets, and then provide my range. For example, if I do this, this represents a slice to the elements starting from index position 0 and going up to but not including index position 3. One tip that you can do here is you can subtract the lower bound from the upper bound to calculate how many elements will be included. So 3 - 0 is 3, which means this slice represents a chunk of memory holding 3 elements. The first 3 elements of the 'values' array: 4, 8, 15. So for example, here I can println! and output my slice. And this will require our Debug formatting like so. And you're going to see we're going to get just the first three elements. Notice the type here as well. This is an array slice. So we have the borrow operator. That is your visual indicator that this is a reference to something. And what is it a reference to? As we can see here, it's a reference to an array of i32s. What's interesting here is that notice that the specific length is not listed here. It's kind of this dynamic slice that could be any number of elements from the original array. And that's actually pretty powerful. For example, here you'll notice that if I change this to 4, 'my_slice' will be a different collection that's going to include 16. But notice that the type here does not include that length. In comparison, the original array type right here does. The 6 right here, representing the length of the array is intrinsically part of its type. With a slice, it's a little bit more dynamic. We're going to see how we can use that to our advantage when we declare functions that accept arrays or array slices in the very next lesson. Now all of the regular syntactical shortcuts apply. For example, if you want to omit the value before the two dots, that means start from the beginning. So this is going to get you the exact same result. Here's another example. I'm just going to redeclare my 'my_slice' variable. This is an example of variable shadowing. And once again, borrow operator ( & ), our original array. And here for example I can pull from index position 2 up to but not including index position 4. 4 - 2 is 2. So this slice will represent 2 elements somewhere in the middle of the array. Or to be more specific, this is a reference or an address to a chunk of memory. A sequence of memory representing these 2 elements right here. So once again, if I output my slice in Debug format, there is that collection at the memory address or the kind of sequential range of memory address that we are representing with the 'my_slice' variable. Once again, if we want to pull to the very end, we can omit the value after the double dots. This means start from index position 2 and go all the way to the end. So index position 2 is our value of 15. So this is going to start from 15 and pull all the way to the end. Same exact principles as a string slice. I do want to show you one important difference here once again between this syntax versus taking a full reference. So for example, if I once again copy this and I utilize this syntax where I omit the value both before and after my double dots, that means create a slice from the beginning of the 'values' array to the end of the 'values' array. This is still a slice because we used our square bracket syntax. So this once again is that example where our portion represents the whole original entity. It doesn't have to, as we can see in all of these examples, but it can. But the important takeaway is this: if I copy this code and I remove our square brackets and just use my regular borrow operator ( & ), now I am borrowing what you can call a full reference, and notice that the types on lines 13 and 16 are different. This type '&[i32]' in square brackets means an array slice. It means a reference to some chunk or portion of an array. This one is much more stricter. It's a reference specifically to a 6-element array of i32s. And that is because without these square brackets, slice syntax, we are borrowing a full reference to the full array, and that array's type is specifically including its length of 6. So the reference to it is a borrow of a 6-element integer array. And this is actually more constricting. Right. Because for example, if we wanted a function to expect a parameter and we declared one like this, we would not be able to pass in a 5-element integer array or a reference to a 5-element integer array, because this reference involves a specific length. All right. So it's a pretty complex idea, but hopefully in the next lesson it'll make more sense when we actually start declaring functions that expect these types of values as arguments. For now, notice it's the exact same syntax to create an array slice as a string slice. We just take our original data source. Then, we use the borrow operator ( & ), our square brackets, and we declare the subset or chunk or fragment or portion or slice, whatever synonym you want to use, that we want to extract from the original collection.
