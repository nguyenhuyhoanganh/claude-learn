# Syntactic Shortcuts

In this lesson, I want to show you some syntactic shortcuts that we can utilize in certain slicing situations. When we create a string slice, it doesn't really matter whether our original source of data is a string literal, which will end up in the executable, or a String which will be allocated on the heap. The string slice will still be created at runtime. The only difference is where it's going to borrow its data from, so you can feel free to use either data source in this lesson and you should see the same result. I'm going to stick with the heap allocated String right here for my 'action_hero' variable, and I have my 'first_name' and my 'last_name' variables from a previous lesson. So let's take a look at our first shortcut with this example. If we are slicing from the very first byte at position 0, we can remove the 0 from the square brackets, we can remove it entirely like so, to accomplish the exact same result. So ..6 means start from the beginning, start from the very first byte at position 0, and then go up to but not including byte 6. It basically saves us that extra character. If you're starting from the beginning, you can either write 0 explicitly or leave it out entirely, and you're going to get the exact same result. If we are slicing from a specific byte to the end of the string, or equivalently, to the end of the byte sequence, just like we are doing with 'last_name' here, in this situation, we can remove the value after the two dots, so I can remove 21 here. What this syntax means is start at byte 7, which is technically the eighth byte since we start counting from 0, and then since we don't specify anything after the two dots, that means to the end of the string or to the end of the byte sequence. So this syntax is helpful because we don't have to figure out the last bytes index position. We don't have to worry about what 'r' is, whether it's 20 or 21, or if we add extra characters at the end of Schwarzenegger here. This syntax will always go to the end from byte 7, which is the eighth byte technically to the end of the string. So now you're going to see "Schwarzenegger" works just fine. If I add any other characters here like this, run the file again, you can see it's automatically going to pull to the end. So it's a little bit more resilient because it can pull that dynamic sequence of however many bytes it takes to get to the end. So I'm going to remove this to get back to our starting state. The final shortcut we have is removing both of the numbers. The number before the two dots and the number after. And what that does is it says create a slice from the beginning of the byte sequence to the end of the byte sequence. So for example, I'll declare a variable here called 'full_name' Once again, provide my ampersand ( & ). Provide my original string. I'm still going to do square brackets, but I'm going to put two dots. So this is equivalent to just doing this. It's basically saying start from the beginning and go all the way to the end. It's a combination of these two syntaxes right here. If there is no value before the two dots, that means start from the beginning. If there is no value after the two dots, that means go to the end. So this syntax means start from the beginning and go to the end. So this is still technically a slice, but it's that special example of a slice that represents a portion that happens to be the complete byte sequence. In other words, the complete original string. So this is pretty much equivalent to taking a full string reference, but it's still technically a slice because we're utilizing the square bracket syntax. So if I output here: "His full name is {full_name}". We're going to see the complete text of "Arnold Schwarzenegger" right here. We are creating a slice, a portion to the entire sequence. All right. So I did want to show you again that a slice can be any portion of the larger string, including the whole. But usually you're going to be using a slice precisely because you want a chunk or a fragment. If you want a reference to the complete string, it's generally easier to just use the ampersand symbol ( & ) by itself without the square brackets. This will be technically a different type. It will be a ref string ( &String ), but it's the same result at the end of the day to taking a slice. It's the entire sequence of characters in this entire content. All right, so this square brackets automatically creates a slice. You can see the type here is different, but it's effectively the same thing as a reference to a string. The advantage of a slice and the difference is in situations like this where you can take a portion or a fragment of the original type.
