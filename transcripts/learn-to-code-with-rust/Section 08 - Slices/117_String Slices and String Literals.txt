# String Slices and String Literals

Next up, let's talk about string slices and string literals. Let's keep all of our working code from the previous lesson. So I've fixed the invalid ending byte right here. This is now 21. Our example is working. This time around, let's use a string literal for our 'action_hero' variable instead of a heap allocated String. As a reminder, a string literal is declared with double quotes, and the Rust compiler embeds that hard coded text into the final executable. Then, when the program runs, that text is available for the entire duration of the program. So in fact, we have an example right here. But let's remove the heap allocated String. So I'll just remove this code right here as well as this. And I want us to pause here and take a look at the type of the 'action_hero' variable. Notice that the type of that variable is also a "ref str" ( &str ), a string slice. This is the exact same type as 'first_name' and 'last_name'. Now why is that the case? Remember: a string slice, this type of "ref str" is an immutable reference to a portion of a string. A portion of some piece of text, which may technically be the whole collection of text as well. The only difference here, compared to the string in the previous lesson, is that this is a reference to a piece of text that is in memory from the binary executable. 'action_hero' is still a reference to a portion of memory stored somewhere else. It is still a string slice. 'action_hero', after all, is not the owner of any kind of data. This is not a 'str' type. This is a 'ref str'. It's a reference, which means 'action_hero' is not owning any of the text. It's just providing a memory address to a portion of text somewhere else. And that happens to be the whole original text in the executable. So this is the equivalent of borrowing the complete house. It's still technically considered a string slice. It just so happens to be that the portion is the complete original content that is stored within your final executable. Now once again, notice that 'first_name' and 'last_name' are string slices as well. They are indeed portions of the original text in the executable as well, which is why they are of the same type. But now this is a segment or a portion, in this case 6 bytes, and in this case 14 bytes. So all three of these variables are string slices. And they showcase the nuance that a slice represents a reference to some portion of a collection of text, including potentially the whole piece of text, like 'action_hero'. It doesn't have to be, but a slice is powerful enough to represent either idea. It's important to note that when we use the borrow operator ( & ) on lines 3 and 6, we do not create a reference to a reference. We do not create a reference to 'action_hero'. We do not create an address to an address in other words. Rather, we use the 'action_hero' reference as the basis for creating a separate independent reference. In other words, the 'first_name' variable is not dependent on the 'action_hero' variable existing after this line. The 'first_name' variable relies on 'action_hero' once on line #3 to identify the chunk in memory and then use it as the basis to create its own independent reference to that same chunk of memory, but just to the first 6 bytes instead of the whole thing. I want to show you that that is the case. So what I'm going to do to simplify this a bit is let's delete 'last_name' right here. And then what I want to do is declare 'action_hero' within a block. And then I'm going to assign that block to our 'first_name' variable. It's going to look a little bit like this. Let's declare 'first_name' and then I'll provide a block on the right hand side. Remember, this is valid syntax and Rust will evaluate the complete block before assigning its final product to 'first_name'. So inside that block, let's declare our 'action_hero' variable. And then I'm going to provide a string slice. So ampersand or the borrow operator ( & ) 'action_hero'. And then I'll similarly do the first 6 bytes making sure to omit the semicolon. So now I'll delete this code. And then afterwards, I'm going to println! and output the value of 'first_name'. And this is going to work. Let me show you that this is going to be "Arnold". So you might be curious. Isn't this creating a dangling reference? After all, if we provide the first 6 bytes of 'action_hero'o and 'action_hero' goes out of scope at the end of this block, won't this refer to something that no longer exists? And the answer is no. This text right here is embedded into the final executable. When the program runs, 'action_hero' becomes a string slice that points to that memory, storing these characters and these bytes. Then, we use that memory location as the basis of creating a separate reference, but only a slice of the first 6 bytes. That's these characters. Then we return that reference and assign it to 'first_name'. So we are not coupled to the 'action_hero' reference. Rather, we use its memory chunk to create our own independent reference, focusing in on a portion of the original chunk in memory. That chunk in memory remains alive even after this block is done running, because that chunk of memory is coming from the hard coded content "Arnold Schwarzenegger" that's been embedded in the file. So we create one reference, then we create a second reference to it to a smaller subset of the original text. Then we return that reference, the block ends, and 'first_name' retains that reference. It is not a dangling reference because the text that it is referring to in memory is still in memory. It's coming from the file, but it's still in memory, and that allows us to access it right here. The key takeaway is anytime you declare a string literal with double quotes, you are creating a string slice. Because when the program runs at runtime, that is a reference to a portion of text owned somewhere else or stored somewhere else. The difference between this example and the previous lesson is in the previous lesson, that text was on the heap and owned by a String. Now, this text is stored in the executable, so every element that is accessing it when the program runs is fundamentally using a reference. It's taking the address of that location in memory where that hard coded string has been loaded and it's just continually referring to either the whole content or a portion or chunk of the content. And either one of those two scenarios qualifies as a slice. Hopefully that makes sense. And with that, I will see you in the next lesson.
