# Traits with Generics

In the previous lesson, we defined a QualityTime struct, and in this implementation we had to force the 'minutes' field to be a 64 bit float. That was because the 'minutes' field right here is what is being returned by the 'amount' method and the 'amount' method, which is a requirement of the Investment trait, mandates a float return value. Now we could technically take minutes as something else and then convert it to a float in the 'amount' method. That is one way that we could solve the issue of representing minutes as something else, because I don't tend to think of quality time minutes as a floating point value. Like I don't usually say I have 124.6 minutes of of quality time, right? So say I would prefer to represent this as something like a whole number. Like maybe a u32 would work here. Positive whole number. Well, the problem is, of course, this isn't going to work because we are specifying a u32 return value while 'amount' requires a float 64. So if we go all the way back up to the Investment trait definition, we can see that we are binding ourselves to have 'amount' and 'set_amount' represent the actual value as a 64-bit float. And now I'd like to add a little bit more flexibility in order to enable that for my Investment trait, I can avoid hardcoding these specific types and replace them instead with a generic. Remember, a generic type is a stand-in or a placeholder for a future concrete type, and we use generics because we don't hard code a specific type, but we enable future implementations to choose what concrete type is going to fill in for that generic. That's why it's called a generic because it's not specific. It's not concrete, it's not realized yet. It's some generic future type that can vary. The syntax is going to be very similar to the generic syntax that we've seen for things like functions, methods, and structs. So after my trait name Investment, I'll add my brackets right here. And I'll define a single generic type called T. This is some future type that Rust will try to figure out based on the future implementations of the Investment trait. So here, rather than specifying that 'amount' has to return a 64-bit float, I'll say that 'amount' will be some method that returns a value of T. All right. Furthermore, what I can do is clean up some of these methods. I think one example is something like 'double_amount' This method multiplies by a floating point value of 2.0, which kind of similarly infers or suggests that we are dealing with floating point values. Because typically if we're multiplying by a floating point, we're multiplying something else that is also a floating point. And I think we can simplify this trait and make it a little bit less specific by removing this 'double_amount' default implementation. And that way, each type that implements Investment will be required to specify how to double its amount in general, without having to hard code the idea of a 2.0 in here. So in other words, let's make this a little bit simpler by not binding to a float and just defining a regular trait requirement without a specific default implementation in the trait definition. So given that 'double_amount' is now going to be defined in all of the implementations below, and that 'double_amount' method will be able to access each struct's respective fields, we also are not going to need a 'set_amount' method here anyway. It existed for the purposes of being a setter, but we're going to be able to double the amount regularly in all of our implementations below, as we'll see in just a moment. So I'm going to remove the 'set_amount' requirement. And now all that's left is to go through all of the the violations below and fix what we see. All right, so let's start with this Taxable trait right here. It is a subtrait that inherits from Investment. Well, Investment now requires a concrete type for T right. It accepts one generic T. And Rust doesn't know what we are defining the Taxable trait as a subtrait of. Of course, there probably is a way to enable generics here for Taxable that then pass on to Investment. We've seen that pattern before, but given that I know that everything here in the Taxable trait's definitions, requirements like the 'tax_bill' method, given that I know that these methods and these constants are working with floats, I can say something like this. I'm going to provide my angle brackets here. And I'll hardcode a specific type of float 64. So what I'm saying here is this. If a type chooses to implement the Taxable trait, then it is also obligated to implement the supertrait, which specifically is the Investment trait's requirements, but with a concrete type of 64-bit float substituting for the generic T. So if something chooses to implement Taxable, then we can look at these required methods from the Investment supertrait, the parent with T being replaced by float64. So any type that implements Taxable, for example, has to implement an 'amount' method that returns a float64. This f64 is what is going to be substituting for the T here for any type that implements Taxable. However, a type can still choose to implement just the parent of Investment, in which case T is still generic, and thus it can choose whatever concrete type it wants to play the role of T. That could be a float, that can be an integer, it can be any variety of float or integer in Rust, etc.. All right. So let's continue through the rest of our implementations. Right here, we are implementing Taxable for the Income struct. If we are implementing the Taxable subtrait, then we also have to implement the Investment supertrait. More specifically the float 64 version of it. So right here, what I'm going to do is specify that I'm not just implementing Investment because that trait technically doesn't exist. It needs an additional qualifier, which is the type for T. I'm specifically implementing this the Investment trait with a concrete type of float 64 for the role of T. So Taxable will be happy and Investment will be happy. Now, the only reason that Rust is dissatisfied is because we need to define a 'double_amount' method. And we no longer need a 'set_amount' method. So for 'double_amount', it's going to be exactly what we expect. We're going to take our 'amount' field up above and just multiply it by two. We can do that with this syntax which is just going to be the asterisk symbol and equal sign. Then 2.0. We're dealing with a float here. Then we can remove 'set_amount' right here. And that should do it for our Income struct. All right. Moving on down below for Bonus. We're going to repeat the exact same logic we are implementing Taxable for Bonus. And as a requirement of implementing the Taxable child trait, we have to implement Investment, which we've clarified more specifically to be an Investment with a concrete type of a 64-bit float for type T. So right here I'll implement my Investment trait with a concrete type of float 64. For T, I will no longer have a 'set_amount' method. For 'double_amount', I can basically copy over this implementation. The only difference is our field here on the struct is called 'value'. So we can set 'self.value', multiply and save after multiplying by 2.0. All right. So now we are down to QualityTime. First things first, I forgot to add the Debug implementation. So let's add that right here. We're going to derive our Debug implementation. And now QualityTime is not going to worry about implementing Taxable. So it's not going to be bound to any kind of float requirements. QualityTime is only going to implement Investment, and Investment allows us the flexibility of choosing the concrete type that we want to substitute for T. So how about we define QualityTime as having a u32? And thus when I implement the Investment trait, I'm going to say specifically implement the u32 version of the Investment trait for QualityTime. All right. And now I just need the 'amount' method. And similarly I'm going to need a 'double_amount' method that no longer has a default implementation in the Investment trait. So I'm not bound to anything above. I can choose what I want this method to do, and all of the implementations above were multiplying by 2.0 because we were dealing with floats. Now, we are dealing with unsigned integers, whole numbers. So I can just take 'minutes' and multiply them by two. So just multiplying an integer by an integer. This of course will now be a u32. And hopefully that should do it. Let's see what else is going on here. It's a little bit off. No field 'minutes' on QualityTime. And that is simply because I have a syntax error here. This needs to be a comma. And there we have it. So now we have 'minutes' being defined as a u32. Our Investment trait supports a generic. So there's a variety of different ways that we can implement the Investment trait for a type, depending on what concrete type we want to substitute for T, which will play a role in affecting the return value of 'amount'. But now we have a trait with some generic, flexible capacity, right? We don't have to define an Investment trait for a float, or for an 'amount' method that returns a float and a separate Investment trait for an 'amount' method that returns a u32. We can capture that all in a single Investment trait with a generic, and have different types implement the same trait with various types substituting for the concrete type T. So that should do it here. So now in the body of 'main', let's go ahead and create one of each type of struct. So here's Income. Here's Bonus. Let's go ahead and create a QualityTime. Here I'm going to change this one to be 'rust_programming_time'. That is the best quality time we all have. And for 'minutes', of course this is now a u32. Let's give us 1000 minutes of Rust programming quality time. All right, so now all of these three structs implement the Investment trait. So they're all going to support a method like 'double_amount'. So for example here I can call 'double_amount' on the Income struct, 'double_amount' on the Bonus struct and 'double_amount' on the QualityTime struct. Everything is working just fine, and I can show you that we are going to double our minutes field on the QualityTime struct. So here I can interpolate my 'rust_programming_time' QualityTime struct, providing the Debug formatting in pretty-print format. You can see right here we've doubled it from 1000 to 2000. But the key takeaway is we've now given the flexibility for our 'amount' method to return more than just a float64. So our code has become more flexible. Tomorrow, we can define a new type that also implements Investment, but with a totally different type for T. And so now we are getting that flexibility while still reusing the idea of a trait and all of the benefits and consistency that it is enforcing. All right. So that is how you define a trait with a generic type.
