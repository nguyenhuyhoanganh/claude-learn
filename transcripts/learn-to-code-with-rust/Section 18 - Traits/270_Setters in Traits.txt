# Setters in Traits

So here's the next challenge in front of us. Let's say that we want to mandate a new method called 'double_amount' on our Taxable trait. And the purpose of this method will be to multiply the existing amount by two and overwrite the original data. So this can represent, for example, a doubling of our income or a doubling of our bonus so that we can calculate the corresponding tax rate. Say that we try a default implementation. So we're going to define it right here in my Taxable trait. And we're quickly going to run into a problem. So here let's say I want to mandate a method called 'double_amount' And I'm going to provide my default implementation here as well. This method is meant to mutate or change some part of the data that's going to live on the type. So I will accept a mutable reference to the instance. And here I'll provide a default implementation. And this will not work. But let's say I assume the existence of an 'amount' field. And then I try to override it by taking my existing amount, which as we saw in the previous lesson, we can retrieve by calling the 'amount' method, which is guaranteed to exist from this line, and then multiplying it by 2.0. So we're trying to take the existing 'amount' from the 'amount' method, multiply it by two and overwrite some 'amount' field. There's just a couple problems here. #1) once again, Rust cannot guarantee the existence of an 'amount' field. In the previous lesson, we tried to read an 'amount' field. Now we're trying to write an 'amount' field, but the same issue exists. A trait cannot guarantee state or some data or some field existing. It can only guarantee an interface, a collection of methods. A trait exists to enforce shared behavior between different types, not necessarily shared state. So this is not valid. In fact, the technical reason why the compiler is unhappy here is because it thinks we're actually referring to this 'amount' method right here. And rather than invoking it with parentheses like we're doing here, it thinks we're doing some weird thing with overriding it. Basically, this is not going to work. So how can we solve this problem? It's going to be a very similar solution to what we did in the previous lesson. What we can do is mandate what's called a setter method. A getter method like 'amount' right here is one whose purpose it is to get some data, to retrieve or read some data. In comparison, a setter method is one whose purpose it is is to set or to write data, right? So one is to read. That is our getter methods. And one is to write which is our setter methods. They set new data. So here's what I'm going to do. Let's say I mandate a method on all types that implement Taxable. And I'm going to call that method 'set_amount'. The name is up to me. And the intent is that this is the method that is going to set the new amount on the given type that is implementing this trait. So what I'm going to say is this method must have the following signature. It's going to accept a mutable reference to the instance. So &mut self. And then I'm going to accept the new amount that I want to provide. That will be the new value to set. And this will be a f64. All right. So I will not provide my default implementation here, but I am creating the requirement for the 'set_amount' method to exist. Right? So now we have to go below to both our Income struct and our Bonus struct and implement 'set_amount'. So here I can define my 'set_amount' method which is required. Here is our signature. I'm going to receive my new amount. So here in my actual implementation code for the method inside my 'impl' block, I am able to access the 'amount' field because Rust knows that it exists for this Income struct. So here I can take 'self.amount' and override it with the new amount value. And that should solve this block. We have another violation for Bonus because we similarly need the 'set_amount' method to be defined to satisfy the trait requirements. Now in this situation our internal field is called 'value', but the logic remains the same. I want to take the 'value' field and override it with new amount like so. All right so now we're almost there. I'm going to go back to my definition here. And here's the last thing we need to switch. Now I cannot overwrite '.amount' because we cannot guarantee the existence of an 'amount' field. But we can guarantee the existence of a 'set_amount' method whose purpose it is to overwrite that data. And that data could indeed be any type of data. It can be a field called 'amount'. It can be a field called 'value'. It can be a some kind of other data structure or data type. Could be a vector, could be a string. Right. It could be a hash map. Every type is welcome to define its data and how it is storing it in any way it wants. All that Rust guarantees is that we're going to have a 'set_amount' method, and we can infer that that is going to be what is going to set that internal state, such that 'double_amount' can now rely on this method to be responsible for the actual writing of the data, the actual setting of the amount. We're not doing the actual writing here. We are delegating to a method whose existence we are going to mandate by writing it within the trait. So here in 'double_amount', I'm going to keep the code on the right. And all I'm going to do is say, I know there is a 'set_amount' method now guaranteed to exist because it is defined right here. And what is the new amount that I want to provide? That is going to be the result that we just had. And there we go. So once again this is a getter and this is a setter. But it's the same principle. We are solving the same problem as in the previous lesson. Because we cannot directly write to state or write to any fields, or even infer the exact type that we are working with within our trait, we instead don't worry about the type and only worry about the methods that it defines, which we can mandate and enforce through the use of traits. Rust is satisfied with this, even though it doesn't know all of the possible types that can implement this, because it knows we can call 'set_amount' because we promised it that a method called 'set_amount' will exist, and we promised it that it can call the 'account' method because we promised it that a 'account' method will exist, right? It has the security from above to know that this code is valid, even though it doesn't know the exact types that are going to implement Taxable, and exactly how those types are going to implement these two methods. But it doesn't need to know the specifics because the trait only defines the interface, the entry point, the methods and what they return and what their parameters are. So now let's take a look down below in the 'main' function. Here I can take my income and call the 'double_amount' method on it. I need to make sure I make it mutable because I will be altering its state. So income. Then I'm going to call the 'double_amount' method. This is relying on our trait implementation right here, which in turn will call the 'set_amount' method and the 'amount' method on my Income struct. And then I can show you that after I double the amount of income that I've earned, my total tax owed should double as well. There we go. And the same principles will apply if I take my Bonus, which I will make mutable and I can double the amount of my bonus, which should also double the amount of tax that I owe on the bonus. And there you have it. Even though these structs are technically different, right? Income has a field called 'amount' and Bonus has a field called 'value', which is different, which is fine. Different things in the real world will require different data types, different field names, etc.. Notice how with the use of traits, we have also made these two distinct structs more similar. They are more alike, even though they are different. We accommodate the differences between them by ensuring that they have a shared interface through the use of traits. And when I say shared interface, I mean we interact with these two different types in the same way. They have a consistent method called 'tax_bill', a consistent method name 'double_amount', a consistent method called 'amount' and 'set_amount'. We are creating this consistency while allowing each type to implement its own differences. And that is the beauty of traits. We are taking different things and making them more alike for the sake of having nice code that is easier to parse and read because our types are able to behave in a similar manner. All right, so this is an example of what could be called a setter method. It is mandated by the trait. But its responsibility is to set some data, to write some data. All right. That's all there is to cover in this lesson. So I will see you in the next one.
