# Multiple Trait Bounds

In this lesson, we'll learn how we can require multiple traits, either for a parameter or bound to a generic. So the first thing I want to do is to split our existing Accommodation trait into two distinct traits: Accommodation and Description, just to break things up. So the first thing I'm going to do is declare a new trait called Description. And in here I'm simply going to cut and paste the 'get_description' method that was formerly in Accommodation. So now we have two traits describing two bits of behavior. And each trait is requiring one method to be implemented to honor that trait. So now we do have to adjust our code below. So Hotel previously was relying on the 'get_description' method. But now in order for that method to exist, even if it's the default one, here we also have to implement the Description trait for Hotel. So down below here I will say implement the Description trait for Hotel. But because I do not want to implement a custom 'get_description' method, I'm just going to leave the block empty. This means if we call 'get_description' like we do in the 'summarize' method on a Hotel, Rust will fall back to the implementation here in the Description trait. Then, as far as our Airbnb struct, you can see it formerly had both of these methods. We similarly need to move this method into our new block for the implementation of the new Description trait. We've basically just split things up. So here the concern is the 'get_description' method is no longer part of the Accommodation trait definition. So Rust thinks that we are defining an invalid method that shouldn't be here. So down below here I'm going to implement another trait. I'm going to say that an Airbnb struct is going to opt in to implementing Description. So 'impl Description' for which type? For Airbnb. And I'm going to cut and paste this code right here. Save that and that should do it. So now let's move on to the 'mix_and_match' function. Say that I want to add an additional requirement here, and that is that the 'first' parameter must be a type that implements not one, but two different traits. I want the 'first' parameter to be able to implement both the Accommodation trait and the Description trait. How can I accomplish this? So what I'm going to do here is after my mutable reference, but before 'impl', I'm going to start a parentheses and then I'm going to close it off right here. And then after Accommodation, I'm going to add a plus sign ( + ). You can think of this as adding additional trait functionality. So I'm going to right 'impl Accommodation + Description'. So the way this reads is exactly as it is written. The 'first' parameter will be a mutable reference to some type that implements both Accommodation and Description. Now in comparison the 'second' parameter promises to implement only the Accommodation trait. We are not requiring that Description be provided on that, or rather that the 'second' type implement Description. So take a look at what's going on here. I'm calling the 'book' method on the 'first' parameter and the 'second' parameter. Because Rust knows that both of these parameters implement the Accommodation trait and the Accommodation trait has a 'book' method, Rust knows that these two types will both support the 'book' method, so this remains valid code. And similarly, if I take my 'first' parameter, I can invoke the 'get_description' method on it. And Rust knows that the 'first' parameter supports the 'get_description' method, because Rust knows that the 'first' parameter is guaranteed to implement the Description trait, which forces the definition of a 'get_description' method. However, if we try to invoke 'get_description' on 'second', we're going to see that that does not work. And the reason is the compiler cannot guarantee that the 'second' parameter is going to have a 'get_description' method, and thus this code will be invalid. So I'm going to get rid of this line right here. So once again we're getting pretty complicated here, but it really is just combining and building upon the concepts that we have talked about in this section. Right? We are now mandating not a concrete type, but simply a type that implements a trait. And here we're now saying implementing two traits. Both requirements must be met, which means two sets of methods that are guaranteed to be defined on the argument that is passed in for the 'first' parameter. All right. So down below, we should be able to call 'mix_and_match' with a mutable reference to our Hotel. Because our Hotel struct is a type that implements Accommodation and Description. And for our 'second' argument of a mutable reference to an Airbnb struct, technically speaking, that type also implements Description. But the only requirement that we are mandating when checking for the 'second' parameter, which is this thing right here, is that it implement the Accommodation trait. The Airbnb struct type does implement Accommodation, so the compiler is happy and this code runs exactly as expected. I also want to show you how we can utilize this multiple trait syntax for this generic trait bound syntax. It's going to be the exact same thing, except we're going to do the plus sign and any secondary trait directly within our generic definition. So here, as a reminder, we have 'book_for_one_night'. We define one generic type T which is any type that implements the Accommodation trait. And we are saying whatever that type T will be when 'book_for_one_night' is invoked, it's going to be the type of the 'entity' parameter. More specifically, 'entity' will be a mutable reference to that type. So once again here, just like below, I will add a plus sign ( + ) and Description. So what this is saying is you can invoke 'book_for_one_night', passing in an 'entity' argument that is a mutable reference to any type T, but the bound or the constraint or limitation that Rust will enforce is whatever concrete type fills in for generic type T, the concrete type must implement both the Accommodation and the Description trait. It cannot implement only one or neither. It must implement both. So now if we invoke 'book_for_one_night' with a mutable reference to either Hotel or Airbnb, either one will work because both of these struct types do implement both of these traits. But if a struct implemented either Accommodation or Description, but not the other one, the compiler would be unhappy. So that is how we can declare multiple trait requirements on a given parameter or connected to a generic that is then used for a parameter. Remember, these are sort of different syntax options to accomplish the same result. All right. And the goal once again is to think less about coding for concrete types and more for coding for the behavior of something. What can something do? Can it serve as an Accommodation that has a 'book' method? Can it serve as a Description that has a 'get_description' method? Because as long as a type is able to fulfill that role, as long as a type is able to perform that behavior, those actions that are mandated by the trait, well then Rust can afford to compile with safety because it knows, for example here, that 'book' will be a valid method, and here that 'get_description' will be a valid method regardless of what type it receives, because it knows the type's available methods because they are mandated by the traits that we declare. That's all there is to cover in this lesson, so I will see you in the next one.
