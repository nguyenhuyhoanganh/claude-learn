# Associated Types I

In this lesson, we'll learn about associated types. An associated type is a placeholder for a type that is required within a trait. So an associated type is like a generic in spirit in the sense that it is a placeholder for a future concrete type, but an associated type is specifically coupled to a trait definition. All right. Let me show you an example of a situation where a trait will require an associated type in order to operate as expected. So I'm going to begin by creating a new struct called Lunch. And this will represent a lunch meal. And every lunch is going to have a cost. So I'm going to add a single 'cost' field that can be a 64 bit float. Immediately above I'll derive my Debug trait implementation. So as we know with many types, specifically numeric ones, we can use the plus symbol ( + ) or the addition operator in order to add two values together. And this functionality is enabled by implementing a trait called Add. We can implement the Add trait for our own custom types, in order to enable them to be added with the exact same symbol. So for example, if we want to be able to add one lunch struct with another lunch struct, we can implement the Add trait and get access to that functionality, and even use the familiar operator of the plus sign to accomplish that addition. So I'm going to begin by using the use keyword to bring in that Add trait. It is found within the standard library in the operations submodule, and it is called Add with a capital A and below I'm going to implement my Add trait for Lunch. The next thing I want to do is actually Command or Control- Click into add so you can see the definition of this trait. It requires a single method called 'add' which will expect an instance. And then 'Rhs' here means right hand side, which means the other value that is going to be added. And then of course the 'add' method is going to define what should be the process, or how do we arrive at the sum of the two values that are being added. But take a look at this return type right here. It is Self::Output. Now in this case, Self just like it has been throughout the course, is in reference to whatever it is being declared, within whatever construct the self keyword is written within. But in this scenario that construct is not a struct. Rather, it is a trait. Here's our trait called Add so Self here is simply in reference to the Add trait within which Self is being used. So in here it is looking for a name called Output with a capital O. As you can see, that is right here. But it doesn't have a concrete type that is actually being assigned. So you can think of Output here as a generic or a placeholder. It is a stand in for some future type that Rust needs, because it is that type that is going to be the return value of the 'add' method. Depending on how we want to define it, we can have our 'add' method return whatever type of value we want. So for example, when we add two Lunch structs together, we can choose to have the return value be a float or an integer or a vector or another Lunch struct. Rust doesn't know what that is going to be, so it says "I just need it to be Output". And what output is, is a type that Rust expects you to give it whenever you're going to be implementing the Add trait. So let me show you how this is going to work in here. I'm going to define the required 'add' method. So there is my IntelliSense. I'm going to press my return key. And notice here that it gets the method definition from the Add trait. This is the exact same thing we saw in the file a second ago. But notice that it is depending on the definition of some type called Output, that must be available so that it can serve as the return value type of the 'add' method. What we're going to do is within this implementation block we're going to write the word 'type'. Then the name Output. And it must be Output because that is what the trait expects. We need to provide a specific concrete type to fill in for Output. So Output is technically called the associated type because it is a type that lives within the trait, in this case the Add trait who we need to provide a concrete type for. So let's say we want to decide that adding two lunches together will be a float64. It's totally arbitrary, but let's start with that example. So I'm going to assign the Output type, the Output associated type to be a float64. So Rust now knows that the 'add' method must return a return value of type 64-bit float. So what we can do, for example, is take our first Lunch struct represented by itself. Let's take its 'cost' field and then let's also add 'rhs.cost'. 'rhs' or right hand side will be the other Lunch struct instance. So now when I add two Lunch structs together, what I'm going to get back is going to be a float because that is what I have chosen. It doesn't have to be a float. It's just an example of one kind of type that we can specify. So this Output associated type is specifically for the Add trait. But there are other traits that may require their own associated types that may have a different name than Output. The reason the Rust team probably chose Output here is because it makes sense in the context of the Output of the 'add' method, but it's not so much the Output name that is important here as the notion that this is a type that belongs to the Add trait that we need to provide a concrete type for. At the point that we are implementing the trait so that the trait's functionality is able to work. If we comment out this line, the code will be invalid. You can see right here not all trait items implemented. Missing Output. So the compiler is telling us you've satisfied the method requirements: you've given me 'add', but I also need a specific type called Output that you need to declare for me within the implementation, because that Output type influences other things like the 'add' method. So we have to write 'type' followed by the name of that expected type, which you can find in the trait definition, then an equal sign and then a concrete type to play that role. All right. So now in 'main', what we can do is create two Lunches. I'll just call them one and two. So here is lunch one. Let's give it a cost of 19.99. Let's create a second Lunch. And this one will have a cost of 29.99. And then the very next line, let's go ahead and add them. So I'm going to provide my dynamic content here. I'm going to add a precision as well which is colon and then a dot and two. So that I round to two decimal places. And then I'm going to take 'one' and add it with 'two'. The exact same addition operator that we've used throughout the course totally works. Now for our custom type, our Lunch struct, because we implemented the Add trait. When Rust sees the plus sign, it will look for an 'add' method. It will find it, and it knows that 'add' must return a float because we specified it here. And so now adding two structs is going to yield a return value of a float. And here is the two Lunch costs summed up for you. Now once again we are not obligated to have a float here. It is up to us. So here's an alternative version where we change it to something different. Let's declare the output to be a Lunch struct. So we're saying when we add one Lunch with another Lunch instead of a float, let's get a brand new Lunch struct, right? The same method signature continues to apply. The 'add' method is going to rely on whatever the Output type is here, which is Lunch. So now the compiler knows that 'add' must return a Lunch struct. This is not a Lunch struct, so the code will not compile. So let's create a brand new Lunch struct. Here I can either provide Lunch or Self. And let's say that if we add two Lunches together, we can simply take the sum of their 'cost' fields, add them up, and return them in a brand new Lunch struct. So here I'm going to declare my 'cost' field. I'm going to take my existing 'self' Lunch struct, take its 'cost' field, add that with the other Lunch struct's 'cost' field. Wrap up that floating point value in the 'cost' field and wrap that in a brand new Lunch struct. And that's what's going to be returned. Now in this scenario, it's just an issue of the Lunch struct not implementing the Display trait, which is what we're trying to do. But if we simplify this to just be the Debug format, that should do it. Once again, one + two is going to call the 'add' method, which will now give us a new Lunch struct with the cost being the result of the two 'cost' fields. The key takeaway is it's up to us to define how addition works for our custom types. We can have the output be a Lunch struct or a float, or a vector, right? Or a HashMap. Doesn't matter. It's up to you to define what that return value is going to be, but Rust needs you to give it the type in order for the trait to be able to understand what the 'add' method is going to return. So that's all an associated type is. It is a type that is associated with, in other words, connected with a trait. And you're going to provide that concrete type for the associated type when you actually implement the given trait. All right. That's all there is to cover in this lesson. So I will see you in the next one.
