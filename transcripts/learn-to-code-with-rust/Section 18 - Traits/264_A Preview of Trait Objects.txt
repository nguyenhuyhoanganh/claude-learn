# A Preview of Trait Objects

In this lesson, we'll introduce trait objects. A trait object is an instance of a type that implements a particular trait, whose methods will be accessed at runtime, using a feature called dynamic dispatch. That is quite a mouthful and it is a difficult concept. So let's introduce this example step by step. So I've deleted most of the code in the main function from the previous lesson, and what I'm left with is just a Hotel struct and an Airbnb struct. Let's say that I wanted to store both of these structs within a single vector. Let's say I declare a variable called 'stays', and I'll begin with my vec! macro. As we'll quickly discover, this is not going to be possible in Rust, because Rust is going to assume that we have a specific type, and that is going to be informed by the very first element. For example, if I write 'hotel' here, you'll notice that Rust will infer the vector to be storing otel structs, more specifically, Hotel structs with a concrete type of String for T. That's totally fine, and it's totally fine if I place additional Hotel structs within the vector. But as soon as I add something different, like an Airbnb, that is considered a completely different type and it does not match. You can see Rust says: "I expected this vector to only hold Hotels. Now I'm finding a struct of Airbnb". It's not going to work. And if I flipped these two around like so, this would also not work because now Rust would infer that it's a vector of Airbnb structs and 'hotel' is a mismatch. So we have a problem either way because these two are fundamentally different types. So let's think of the ways that we have solved this problem throughout this section. We have primarily relied on the 'impl' keyword to give us that dynamism because with 'impl' we don't care about a specific type or a concrete type. We just care about a type that implements a given trait, that has a certain functionality. So you might think that we can get away with something like this. I'll try declaring a type here, manually annotating and saying yes, I'm going to have a vector, but rather than writing a concrete type like Hotel or Airbnb, I can try the 'impl' keyword and then a sample trait, let's say a vector of types that implement the Description trait. So you might think this is going to work. It will not but it is moving us in the right direction. This is ultimately what we want: a vector that is not just storing a single type, but any type as long as it implements Description. So if I save this, this is not going to be valid. And Rust is going to tell us this 'impl' trait syntax which we previously saw being valid for things like function parameters and return values, well, it's not allowed in this type of circumstance, but there is an alternate syntax that is allowed that is going to essentially accomplish the same result. What we're going to do is we're going to replace the 'impl' keyword with another keyword, a special keyword, and it's going to be 'dyn'. 'd-y-n' in lowercase, which is short for dynamic or dynamic dispatch. So let's pause here and describe what's going on here. Dynamic dispatch refers to a process by which a method will be determined and called at runtime. Dynamic dispatch means Rust will figure out the exact type and the exact method to call at runtime, as opposed to knowing the exact type and the exact method at compile time, which is called static dispatch. So when we have static dispatch, the compiler knows the exact type and the exact methods that are going to be on that type that need to be invoked at compile time. For example, if we declared that we had a vector of only Hotel structs, Rust and the compiler know for a fact that if we call a method like 'get_description' on any of those elements in the vector, it must specifically be the Hotel struct and its 'get_description' method. We have declared the type in such a way that Rust knows the exact struct and the exact 'get_description' method, and the compiler can thus optimize at compile time for the resulting executable code. Essentially, everything is known in advance. If we were to have, say, a vector of only Hotels or a vector of only Airbnbs, the type is known, and thus the corresponding methods on the exact types are known as well. Now this is a little bit more dynamic, right? It's a little bit more unpredictable. When we have dynamic dispatch, the program is actually going to figure out the exact type and the exact method that needs to be invoked at runtime, and it needs to do that because of the dynamic natures of the type. The compiler is not going to know the exact concrete types here. Rather, it's going to think of this as any dynamic type, as long as it implements the Description trait. So with this syntax, we can have a vector, have many different types. Right now, we have two. but say we have ten different types tomorrow, all of which implement the Description trait and say the Description trait mandates a 'get_description' method. Rust says it's totally okay for you to have these ten different types, but if you want to call 'get_description' on any one of them, I'm going to figure out the exact 'get_description' method based on the type that is being called on at runtime, when the program actually runs. So we gain flexibility, but Rust at compile time doesn't know the exact type and the exact corresponding method. So here's the trade off. Dynamic dispatch, which we are aiming for in this situation, it tends to be slower than static dispatch, because the compiler cannot optimize for knowing exactly what it's working with. In addition, the exact type and the exact method that will be invoked is determined at runtime. The 'get_description' method on a Hotel struct is a different method than the 'get_description' method on an Airbnb struct, but Rust is only going to figure out the specific version of the 'get_description' method that it needs, in other words, the specific type and the 'get_description' method that lives on it. It's only going to figure that out at runtime, which is generally going to be a little slower than knowing everything at compile time and optimizing the executable in knowledge of that, knowing that hardcoded type. So it is called dynamic dispatch because the word dynamic means subject to change or variance. It is unpredictable. It is not a static or hard coded type. Now, this code will still not compile because dynamic dispatch only works with references. It does not work with just plain types. So I'm going to change this concrete type here, filling in for the generic to not be just any dynamic type that implements the Description trait, but rather a vector of references to a dynamic collection of types or any type that implements the Description trait. So if I update this here, I have to make sure that I provide my immutable reference to my 'hotel' here and my immutable reference to my Airbnb struct here. And now the code should compile. So once again, from Rust's perspective, the compiler technically will not know these concrete types until the program runs. From Rust's perspective, it's now seeing these contents or the contents of the vector as what's called trait objects. We use the term trait object to refer to these instances. A trait object refers to some instance of some type that implements a particular trait, and then that is going to have methods that are going to be accessed at runtime via dynamic dispatch. So in this case we can say the vector stores trait objects. It stores dynamic trait objects, all of which implement the Description trait. The compiler doesn't see them as a Hotel and Airbnb. It sees them as dynamic trait objects, all of which promise to implement the Description trait. Okay, so in this scenario, Rust technically knows the exact types in advance, but with this approach we also have flexibility. So for example, we can populate this vector at runtime based on user input or based on the contents of a file. We ask Rust for this flexibility of dynamic dispatch to enable a vector of any number of elements of any number of types, as long as all of them implement this Description trait. So let me show you how this is going to work down below. I'm going to use println! and I'm going to provide some dynamic content. I'm going to reach into my 'stays' vector with my square bracket syntax, access the element at index position 0 and call the 'get_description' method on it. So this is an example of where the actual dynamic dispatch will occur at runtime. Rust will say: "All I know is that the element at index position 0 is some type that implements the Description trait, which means it's some type that I know has some 'get_description' method". At runtime is when Rust is going to figure out "Oh, the element at index 0 happens to be a Hotel. Make sure to call the 'get_description' method on the Hotel struct that version of that method." And similarly, if I access index position 1, it's at runtime that Rust is going to perform that dynamic dispatch. Figure out what the dynamic element is here. What kind of type is it? Oh, it's an actual Airbnb struct. Make sure to call the Airbnb's 'get_description' method. So technically all invocations are still going to happen at runtime technically speaking, even if the compiler knows what the type is. The difference is when the compiler knows the exact type and method at compile time, it can optimize the executable. Here the executable is going to be a little bit less flexible, but we're going to get this dynamic aspect in our code, which makes it easier to create vectors with dynamic types. Then at runtime, Rust is not going to know whether it's a Hotel or Airbnb, like it would know if we hardcoded the type here. But now at runtime it's going to figure out the exact type and the exact method which gives us the flexibility. So when I run this here, you can see we are getting the respective return values of those two methods. And notice we were able to store them both as references within a single vector. That is the beautiful part of this design. Of course, in the real world we would also typically write an iteration loop, basically code that is going to traverse this vector one by one and call a method like 'get_description' consistently on each element. We're going to talk about that syntax a little bit later on in the course. This is just demonstrating that everything is functional manually by accessing elements at their index positions. So let me give you another example, because the same principle is going to apply to a method like 'book'. The 'book' method is mandated by the Accommodation trait. So here I can update this to be Accommodation. So now I'm storing a vector of dynamic types, all of which promise to implement the Accommodation trait. More specifically, a vector of references to types that implement the Accommodation trait. Now, because the 'book' method is going to mutate the underlying type, I need to make sure that I store mutable references. So our syntax is going to look like this. A vector of mutable references to a dynamic collection of types, all of which implement the Accommodation trait. So of course right now you'll see 'get_description' will no longer work because Rust can't guarantee that these types have a 'get_description' method. All it can verify is that these types will have whatever methods are required by Accommodation, which is actually 'book'. So I'm going to update this to be a mutable reference. I'm going to update this to be a mutable reference. And now down below I can take 'stays', access an index position like so. And now once again, call a method like 'book'. Rust is going to figure out the exact type dynamically when the program runs at runtime, figure out the right 'book' method based on the type that it gets. So here I can call 'book' with Piers and two nights on the next line. There we go. And I of course need to make my 'stays' vector mutable as well because I am updating some part of its internal content. So there we go. And I'm also going to be updating the actual 'hotel' and 'airbnb' state, which means my original structs must be declared as mutable as well, allowing me to take mutable references to them in this vector, storing mutable references to a variety of types, all of which implement the Accommodation trait. That's quite a mouthful. So here I'll access index position 1 once again, because Rust knows that it implements Accommodation. It doesn't know the exact type at compile time technically, but it knows that it will support the 'book' method, which is why the compiler allows the behavior. And here I can book for Amanda, let's say for three nights. And then down below I'm going to output in Debug format. Let's do in fact pretty print debug my 'hotel' struct and my 'airbnb' struct. So once again, the results are going to be what we expect. We're going to update the corresponding internal 'reservations' hash map and 'guests' vector with the data as always. But notice that we did not have to declare a vector of Hotel structs and a vector of Airbnb structs to accomplish code like this. We were able to store them within a single vector because of this dynamic syntax. All right, I know it's a complex topic. I know I talked a lot about it, but hopefully this makes sense. And with that I will see you in the next lesson.
