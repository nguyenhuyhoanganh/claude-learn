# Implementing Trait for Struct II

Let's continue where we left off by defining a second type that is also going to implement the Accommodation trait. So below all of this code, I'll define my second struct. And I'm going to call this one AirBnB. A different type of lodging. So here we're going to have completely different fields. It's up to us how we want to define this type. For an AirBnb, let's say we'll have a 'host' field set to a String. So a Hotel has a name, while an Airbnb has the host who owns the property. And then instead of reservations, let's have a field called 'guests. And furthermore, let's completely vary up the type. Rather than storing a hash map, let's say that 'gurests' is going to be a vector that is going to store tuples of two elements. So every tuple will consist of the guest's name and the number of nights they are staying wrapped up in a tuple. And then we're going to store those tuples inside a vector. It's a completely arbitrary choice of data structure. The key takeaway is that we are storing that data in a different way between the Hotel struct and the Airbnb struct. So here I'll declare the type to be a vector. I'll provide my angle brackets to describe the generic, the type of each element inside the vector, and that will be a tuple. So I'll provide my parentheses. And the tuple will store two elements. The first will be a string, the person's name, and the second will be an unsigned 32-bit integer for the number of nights that they are staying. So these two structs can hold different fields and can use different strategies for data storage. All right. So just like we did above, let's add an 'impl' block for the constructor function to make it easy to create a new Airbnb struct. So here 'impl'. For this block, I am not implementing any trait. I'm just defining the core methods and constructor functions. So I'll just do 'impl' followed by the name of my struct, which is Airbnb. Here, I'll define my constructor function. I'll call it 'new'. Let's allow this function to accept a 'host' parameter set to a string slice, and it will return an instance of the Airbnb. So in here I'll create a new Airbnb instance. And we need two fields which are 'host' and 'guests'. For a host, I'm going to take the string slice parameter, call 'to_string' on it to get a heap allocated String and for 'guests', I'm just going to create an empty vector with the vec! macro like so. So very similar design to what we have for a Hotel but using completely different types. Right. Now so far Airbnb does not opt in to implementing the Accommodation trait. We're going to choose to allow the Airbnb struct to implement that trait. The syntax is going to look just like this. Once again, there can be many 'impl' blocks for the exact same type throughout your program. In fact, if a type chooses to implement, say, ten different traits, then there are going to be ten separate 'impl' blocks, one for each trait. That's not a problem, because Rust is going to consolidate all of the 'impl' blocks' contents into the single type definition, in this case for the Airbnb struct. So down below, once again we begin with the 'impl' keyword. That means we are defining some methods for a type. But now we're not just defining any arbitrary methods which we would put here. We want to specifically define the methods that are going to represent the fulfilling of the Accommodation trait's requirements. So here I'm going to write the name of the trait that I want to implement. Then the 'for' keyword and then the type that I'm implementing them for which is our Airbnb struct. All right. So once again we need two methods. Let's begin with 'get_description'. And we can once again use the format! macro here. This can be something pretty simple. And we can say something like "Please enjoy {}'s apartment". And I'm going to interpolate the value of the 'host' field in here. There we go. We need one more method and that is 'book'. You can see the compiler says "not all trait items have been implemented". We still need 'book', so I will define 'book' here. Let my rust analyzer do the autocomplete. And this is really where the magic happens. Once again, this method body, this implementation code is up to us. But notice that the signature of the 'book' method for both a Hotel struct and an Airbnb struct are the same. They're both going to take a mutable reference to self, a name, and a number of nights, and how we choose to work with that data is entirely up to the implementation. To put it in other words, our Airbnb and our Hotel, despite being different structs, despite being different types, are now made more similar in a sense. We can treat them much more like each other because they support the same interface, at least when it comes to the Accommodation trait. We can rely on a consistent 'book' method existing on either one of these types that will support the same signature. Believe me when I tell you it would be a lot more complicated and confusing if this method was called 'purchase' and this method was called 'book'. Now with traits, we have some amount of consistency between different types. We have common behavior, common methods, common functionality that we can expect on both of these completely different types. But the actual concrete behavior is still up to us and can vary. That's the beauty here. So here we're not working with a hash map like a Hotel is. We're working with a vector so I can take my 'guests' field, get my vector. On a vector, I have a 'push' method, and I'm going to push in an element to the end. We're going to provide our two element tuple which will consist of the guest's name, which I will convert to a string, a heap allocated String. And then I'm also going to provide a second element inside the tuple, which will be the number of nights. All right. So one type is storing the data in a HashMap. A second type is storing the data in a vector. A third type tomorrow can store the data in a totally different data structure, but still honor the Accommodation trait. And that is the beauty. We now have consistency despite the variation. So now here comes the moment of truth. Let's now instantiate our two structs and call the respective methods that have been mandated by the Accommodation trait. Let's begin with our Hotel struct. I'm going to buy us some space here. Let's declare that we're going to have a Hotel, and I'm going to use my helpful constructor function. As a reminder, we just need one parameter, which is the name of the hotel passed in as a string slice. I'm going to call my hotel "The Luxe". And what I can do below is I can try and call a method on it like 'get_description'. And now I know that that method is going to exist because it has been defined on the Hotel right here, and it is defined because we have chosen to opt the Hotel struct into implementing the Accommodation trait. So let's run our code and we're going to see the definition right here. "The Luxe is the pinnacle of luxury". That came from the previous lesson. Well now, let's try to do the exact same thing with our Airbnb. So down below I'll create a separate variable called 'airbnb'. And I'm going to reference my Airbnb struct. That has its own 'new' constructor function that takes a host name. Let's provide "Peter". And then down below the exact same thing. I can take my Airbnb, which is a totally different type, totally different struct, but it's going to have a similarly named 'get_description' method. And that is going to give us the description of the Airbnb. "Please enjoy Peter's apartment". Right. So that is an example of consistency, which is a good thing. Let's now take a look at the other method which is 'book'. Now 'book' is going to mutate whatever instance it is called upon. So if we want to call it on either Hotel or Airbnb, we need to make those mutable. Let's do that right now. I'm going to do 'mut' here and 'mut' here. For Hotel, we now have a 'book' method as mandated by the Accommodation trait that we have opted our Hotel struct into implementing. So let's try booking a hotel for a person for a number of nights. Let's book it for "Piers" for 5 nights, and the Hotel is going to store that within its internal hash map. Right. So what I can do here is I can use my Debug format. Let's in fact pretty-print it. So I'll do a colon, a hashtag, and a question mark just to print it out with one key-value pair per line. And then I'm going to print out my hotel. Let me temporarily comment out these two lines. And we're going to see. Here is our Hotel struct instance. After the 'book' method has been called, you can see the 'reservations' hash map is now storing a key of Piers and a value of 5. So the 'book' method has in fact mutated the Hotel struct and relied on its internal way of storing data to store that reservation. But a different type, like an Airbnb can store it completely differently. So here what I can do is once again call the consistent method of 'book' on an Airbnb. And it's made consistent because we're asking the compiler to mandate that consistency by allowing both of these different types to implement the same trait. So I can once again book a Airbnb for Piers. Maybe this can be three nights, and down below I can print line again, provide my pretty-print format and let's output our Airbnb. This is storing the data in the alternate format, and the reason we're having an issue here, by the way, is because we forgot to implement Debug. Let's quickly do that right here. #[derive(Debug)] The Airbnb struct is storing its data as a vector of tuples. Not a problem. As you can see here, here is our Airbnb. Here is our guests vector with its single element entry. Here is our tuple storing the string and the number of nights. Totally different way of storing things. But the beautiful thing is, both the Hotel type and the Airbnb type are consistent in their available methods and in their general functionality. The functionality or ability or capacity to serve as an accommodation in real world terms, because they have both opted into honoring the Accommodation trait, implementing that trait's methods. So now if we change any method name here, we will be forced to change the method names in all the types that are implementing it. So we are kind of using the compiler to give us a standard of consistency, to standardize, to formalize these functionalities so that they are alike. So now if we look at a type and we know what traits it implements, we know the things that it can do, the behaviors it supports through the shared methods that we know it's going to have.
