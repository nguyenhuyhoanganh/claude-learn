# Implementing the Display Trait on an Enum

In the previous lesson, we defined an Apple struct, and then we implemented the Display trait for that type to allow it to be represented as a human readable string, using the interpolation syntax in our println! So now let's add some more complexity by expanding this 'kind' field on our struct to be an enum instead of a String. So above here I'll define a new enum called AppleType. Let's say for now that we will represent two apple types, or equivalently two enum variants. And they are going to be RedDelicious and GrannySmith. Then, in my struct definition, 'kind' will no longer be a String, but will instead be a variant of the AppleType enum. So if I save this, we're going to see two violations. The first one is right here and we can easily replace this String with a valid variant. So here I can reference my AppleType enum, use my double colon syntax to access a variant, and let's go back to GrannySmith. That's going to solve this problem, but you'll notice it's not going to solve this one. So here we are referencing the 'kind' property on our Apple struct which is our enum. The problem is our AppleType enum itself does not implement the Display trait. Rust doesn't know by default, when it looks at an enum, how it should represent that as a human readable string. That can vary from developer to developer, from enum to enum, from program to program. It all depends on how you want to represent this type right here as a string. And since that type is being used right here and it itself does not implement Display, the compiler is unhappy. So all nested fields on a struct need to implement the Display trait if we're going to be accessing them and using them within an output like this one. So in order to solve this problem, we're going to have to implement the Display trait on our Apple type enum as well. So here I'll add my 'impl' keyword and once again implement the Display trait but this is for an enum instead of a struct. So once again the only requirement of this trait is to have a method called 'fmt'. Once again I'm going to update this 'f' parameter to be 'formatter' just to provide a little bit more clarity. This will automatically be passed in by Rust whenever we use the AppleType enum in any context that requires a string representation. Rust will create the instance of the formatter and pass it in. So in this scenario, we have to handle both of these possible variants. That's not technically true because we could decide on a single uniform string representation for an AppleType. We can just say something like "some type" as a string and that could be used. But of course, let's keep things interesting by actually representing each one of these two variants differently. The way I'm going to accomplish that is by using my 'match' keyword, and I'm going to combine that with 'self'. Remember when we use match with an enum which is what 'self' is, it's an instance of AppleType. We have to provide a match arm or a pattern for each possible variant. So the first variant I'm going to cover is going to be RedDelicious. So here I'll reference my enum, then specifically the RedDelicious variant. Then I'll provide my arrow syntax and then specify what to do if that is the variant. So just like below I'm going to rely on the write! macro, just like below I'm going to pass in formatter right here. And then provide the string that will serve as the representation of a RedDelicious variant of my AppleType enum. So once again, let's rely on our emoji keyboard. So on a macOS, that's going to be the Function key with the letter E. On a windows machine, you can press the Windows key and the period key. So how about for a RedDelicious, I'm going to go with this nice red apple emoji. And I'll add the word delicious. And then I'll also add it to the end just for a little bit of fun. All right. So comma at the end to separate our match arms. Then the other variant we have to cover is going to be the GrannySmith variant. The logic here is very similar. We're going to call the write! macro. We're going to pass in formatter but now offer a totally different string for this variant. So once again, let me rely on my emoji keyboard. And of course if I search for "apple", I'm going to use a green apple for this one. There we go. And I'm going to say that and how about "Granny Smith". And once again copy the apple. Of course, this is totally arbitrary. If you want it to come up with something different or more creative, feel free, but that should do it. So now for either one of our two variants, we're going to provide this resulting string. And this will be the Display trait implementation of the AppleType so when we interpolate 'self.kind' here for Apple, this will be relying on the 'fmt' method for the Display trait implementation for AppleType. So in this dynamic section here, we're actually going to get whatever content is coming from here. So we can probably simplify this and remove this green apple for now. So just interpolate the value of the enum and then the price right here. And let's take a look. So here we have 'lunch_snack' from the previous lesson. We can run that now and take a look. So here it is. Here is the interpolation of 'self.kind' from right here. And in order to get the Display string representation of this 'kind' field which is the AppleType enum, Rust will rely on the format method for AppleType. It's going to get Granny Smith. This whole content is going to be interpolated right here. Then the "for" keyword, then the price. And that's exactly what we see here. And similarly if I want another example, I'll create another variable called 'dinner_snack' and this can be an Apple struct. But this time the kind can be the other variant which is our RedDelicious. Maybe it can have a different price like 1.15. And same principle applies. Now our AppleType is going to implement Display. And the compiler is happy because all of the fields that we are accessing also implement Display. So the compiler knows how to represent them as user facing strings. And now you can see the alternative. Here is our Display trait representation of an Apple with the RedDelicious variant of the AppleType enum. So that is how you implement the Display trait for an enum. Once again, different types, right? Not just different structs in this example, but actually different types. Right? This is an enum. This is a struct. But by being able to implement the same trait for both and defining the same method, we now have this kind of flexibility where we get similar behavior with syntax like this. That is the whole benefit of traits is that we are enforcing consistency, a standard between different types, even if they're technically different, even if what they're storing is different. We now have the stability that comes from consistency. And now our custom types can be modeled or represented as a string, just like various other types. And generally opting in to those Rust conventions is the recommended pattern. So we are using what the language has built and utilized for its own types when we design our own types, and we can accomplish all that by implementing existing traits for our own types. With that, I will see you in the next lesson.
