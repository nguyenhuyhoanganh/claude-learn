# Project Solution

So let's walk through a sample solution to the coding challenge in the previous lesson. First up, we had to bring the Debug trait, the Display trait and the Formatter struct type into the current file scope. And these three names are all found within the 'fmt' or formatting submodule within the standard library. So at the very top I'll utilize my 'use' keyword. I want to reach into my standard library, std. Then two colons. My submodule is called 'fmt'. Two more colons. And then since I want to bring in multiple names, I'll use my curly brace syntax and write them all out separated by commas. We want Debug, then Display, then the Formatter struct. End the line with a semicolon. And we should be good to go. Next step we're going to define a trait of our own. It's called Drinkable, and it has three required methods. So to define a trait we use the trait keyword. Then its name which is Drinkable and then multiple types are going to be able to implement this trait. And if they choose to do so, they have to honor the trait's requirements, its contract, its list of methods. So the first method we need to add is 'consume'. So I'll define that method and it's going to accept a mutable reference. So I'll annotate my parameter as &mut self and provide a semicolon. There is no requested implementation. There's nothing more that's given. So this should be good enough. Next up we want a 'get_data' method that accepts an immutable reference. So same deal. We'll provide our &self this time around, an immutable reference. But we're also asked for this method to return a String. So I will provide my arrow syntax and mandate that this method return a String. Finally, we're asked for a 'stats' method that also accepts an immutable reference, and for this one, we're asked to create a default implementation that outputs the return value of the 'get_data' method above in Display format, and since 'get_data' is a getter, its whole purpose is to retrieve data for us. So right here in this body, I can println!, provide some dynamic content, and render the return value of 'get_data'. The reason the compiler is happy with this is because it knows the 'get_data' method is guaranteed to exist, and because it knows that that method will return a String which supports the Display trait, it knows that it can interpolate its return value right here, as long as any future type implementing the Drinkable trait defines 'get_data' to return a String, which it must, then 'stats' knows that it can display that String right here without running into any violations. Perfect. All right. Next up, define a Milk enum with three variants. So right here: enum Milk. And we want Whole and Oat and Almond. And we can derive Rust's default Debug implementation. So on the line above, I'll use my hashtag square brackets to derive Debug and we are good to go. Next step, define a Coffee struct with one generic T that will serve as the type of the 'kind' field. For our other two fields of milk and ounces, we want some hard coded types, so right here I'll define my struct called Coffee. I need room for one generic, so I'll provide angle brackets after the struct name. I'll call my generic T. And then within my curly braces where I define the structs fields, for my 'kind' field, I will declare that it will be of type T. So when we create multiple Coffee instances later, each one can actually customize the type of T and thus customize the type of the 'kind' parameter or the 'kind' field rather. And then I also need my 'milk' field. That's going to be a Milk enum variant from above, and then an 'ounces' field set to a U32. All right. And finally, we're asked to define a new constructor function for Coffee that returns a new instance. And the function should be available for any generic type T. So in order to define methods as well as functions, we use the impl keyword. And as we'll come to discover, if we simply write Coffee by itself, that is insufficient because the Coffee declaration, its actual type, is specifically tied to a generic. So right here the compiler will tell us you're missing the generic argument. I don't know what you want for T. And the problem is, if we want to define this for any type T, if we simply provide our angle brackets here and write T, this is also going to be incorrect, because in this case, the compiler is going to think it's looking for a concrete type T somewhere in this file scope. Maybe this could be a struct named T or an enum named T. So this syntax only works if we are defining the implementation block for a Coffee with a specific concrete type filling in for T. We don't want that. We want these functions/methods to be available for any Coffee with any type T. So in order to enable that syntax, remember we add the actual generic, the angle brackets, after the impl keyword. Here we declare T. So now Rust and the compiler understand that we are defining functionality for Coffee of any type T. Coffee for any generic T. So here I'll define my 'new' constructor function. And I'm assuming we're going to need all three of our fields passed in as parameters. So I'll do a 'kind' parameter that will be of type T. And then 'milk' will be Milk and 'ounces' will be a u32. I'm going to declare the return value as Self, which is nice because we can avoid worrying about the generic type T. And then in the body, I'll create a new instance of Coffee with Self, provide 'kind', 'milk', and 'ounces'. Save that and that should do it. So let me scroll down to the next challenge right here. So next up we're asked to define a manual Debug trait implementation for a Coffee struct. Of course it's a lot easier to simply go with the default with derive. But this is to practice how we could implement our own custom Debug technical string for one of our custom types. So in order to implement a trait for a type, we're going to write the impl keyword. Then the trait name. This is the Debug trait built by Rust. And then we add the for keyword and then the type we implemented for, which is Coffee. Once again, we run into the same problem. Coffee is a type that specifically includes a generic T, so you'll notice this is insufficient. We're asked to provide T and this will not be enough. And similarly to declare this for any type T, I need to make sure that I declare my angle brackets here for type T, so that then I define this block of functionality for Coffee with any type T. More specifically, I'm defining the Debug trait's requirements for any Coffee of any type T. The only problem now is we are missing the required 'fmt' method that the Debug trait mandates. And the beautiful part here with my IntelliSense and rust-analyzer is if I simply start typing it knows from the Debug trait's definition what we need to define. So I can just press Enter and it will lock it into place. As always, I'm going to change my parameter name here to 'formatter'. I just am not a fan of single letter parameters or single letter variables. And then we're asked to use that struct of formatter and its 'debug_struct' and 'field' methods to output all three of our fields and their respective traits. We're given this hint of "You'll need to place a Debut trait constraint on generic type T", but I want to show you why we're going to need to do that before we do so. So here I can take my 'formatter' parameter. I'll begin by calling the 'debug_struct' method. There's a whole bunch of methods starting with the prefix 'debug' for helping us create string representations of various data types like lists and maps. But we're looking for a 'debug_struct' here because Coffee is a struct. Then we provide a string here. This will be included in the output, and it's totally arbitrary and up to us. I'm just going to write COFFEE in all caps. And then on this, I can invoke a 'field' method. In fact, I can invoke it several times. Into 'field' I'm going to pass two arguments. The first is going to be almost like the key that's going to be output. And the second argument is the corresponding value. So it's almost like a key-value pair that I want to output. And what is common convention here is to provide your field as the key and the field's value as the corresponding value to output for that key. So for my first field, since I see no need for variation between the names of my struct fields and my outputs, I'm just going to go with 'kind' here. And then, as a reminder for the value, we need to provide a reference to the value that we are going to be printing the string representation of here. So I'm going to reference self, which is my Coffee kind. And then very importantly, I need to provide a reference. So I'm going to add my ampersand borrow operator ( & ) in front. And that should do it. Let's go ahead and finish this up. And then we'll come back and address the compiler error we're going to see here. So as a reminder, this implements something called the builder pattern which means we get back the exact same kind of format or string that we're building up here. So we can continue to chain on the 'field' method call and build it up continually until we're done, at which point we can call the 'finish' method to produce the final result. So here for my second 'field' call, I'm going to provide milk and then I need to provide my reference to 'self.milk'. And then one more time I'm going to invoke 'field'. And then I'm going to provide ounces. And then a reference to 'self.ounces'. And then finally I want to call the 'finish' method. And once we do that, a lot of the red squigglies should go away. And there we go. We only have one violation. And I want to talk about why it's happening. So the reason here is that rust doesn't know what T is. T can be any type in the world. Whenever we create a Coffee instance, right, we specify it as a generic type that will serve as the type of the 'kind' field which we are trying to interpolate. The problem is that this whole Debug representation depends on the 'kind' field having a Debug representation itself. It is that Debug representation that is going to be embedded or interpolated within this Debug string that we're going to produce as the technical string representation of the Coffee struct. The compiler cannot guarantee from a generic type T that it doesn't know anything about. That kind will implement the Debug trait, so it says "T doesn't implement Debug", so I cannot guarantee that I can interpolate it in a Debug format. In this string, I want to show you a very similar example. Notice here that milk is totally fine, and that is because we derived the Debug trait implementation right here. So the compiler can guarantee that the Milk enum variant can be represented in its Debug string technical representation. And notice if I get rid of my attribute right here, then we're going to get the exact same issue for this thing right here. Milk doesn't implement debug right now, so the compiler is unsure of how it's going to represent it in Debug string representation for the purposes of the 'milk' field. And then as soon as I bring the attribute back, you can see the problem goes away. So what we can do to tell the compiler that things will be okay is tell it that we promise that type T will implement the Debug trait, and if this type T is some type that implements the Debug trait, this method will only be available for types of T that do implement the Debug trait, in which case the compiler can guarantee that it can be represented right here for the 'kind' field. So what we do here is we add an additional constraint, which is we add our colon right here and write Debug. So it's a little bit of a confusing syntax because we are implementing the Debug trait for Coffee. And we are also mandating that for the type T that Coffee will hold, we're mandating that that type T will be a type that implements the Debug trait. So again, we are creating stricter requirements. In a sense this 'fmt' method and the Debug string representation of a Coffee is now no longer going to be present on any Coffee of any type T. It's only a Coffee with a type T that does implement the Debug trait. The 'new' constructor function will be available on all Coffees, while the Debug string representation of a Coffee will only be available on a Coffee if the type of T that it uses is a type that implements the Debug trait. All right, moving on. Implement the Drinkable trait for a Coffee struct of any type T. So down below, same syntax. But this time around we're implementing our own custom trait that we defined above. So impl Drinkable for Coffee. Once again, we'll need our type T right here. And we'll declare our type T right here as well. There we go. And we're told place a Display trait constraint on generic type T. Let's go ahead and do that right now. So same syntax as above. I'll add Display. Once again what this means is for any functionality that I define in this block it will be available on a Coffee instance if the T type for that Coffee implements the Display trait. All right. And I do realize and apologize for the unintentional humor of saying coffee and tea together in the same sentence. That was truly unintentional. So once again, for Drinkable, we need to honor the Drinkable contract or trait right here. So we need to make sure we define, at least 'consume' and 'get_data'. We can define 'stats' and overwrite it if we want to, but if we don't, that's totally fine because Rust will fall back to this default implementation. So we are told that for the 'consume' method, which I can literally just pop into place by writing 'consume', and having it just write the signature for me based on what we defined in the Drinkable trait for the 'consume' method, it should decrement the 'ounces' field to zero. So 'self.ounces = 0'. The compiler knows that we are dealing with a Coffee, so it knows that that coffee is going to have an 'ounces' field. And it also knows the signature from the trait. So that is the beauty of traits, and that is that each type can customize how it is going to honor that behavior, how it is going to implement that functionality. The trait is not describing exactly what has to happen. Rather, it's defining the contract that a type has to honor. And we are honoring the idea of consuming some drinkable beverage. Coffee is the specific struct type that is honoring or implementing that Drinkable behavior. All right. So in order to make the compiler warning go away, we also need to define 'get_data'. And here we're told the 'get_data' method should return a string with this content where the two dynamic fields come from the 'ounces' and 'kind' fields. So if you want to generate and return a string, one really nice way to do so is with the format! macro format is identical to println!, except instead of printing the content out, it just packages it all up in a heap allocated String and it supports the exact same interpolation syntax. So here I can write "A delicious blank" and then ounce blank. And then self represents my Coffee struct. So I can access self.ounces and self.kind. Make sure to omit my semicolon. And there we have it. There is our Drinkable trait implementation for a Coffee struct. These two methods will be available on the Coffee struct instance if the type of T in a Coffee is a type that implements the Display trait. So next up, let's define our required Soda struct. So below here I'll define my struct of Soda. And we have four required fields: 'calories' set to a u32, 'price' set to a float 64, a 'flavor' field set to a String, and a 'percentage' field set to a u32. Luckily, this time around we can derive Rust's default Debug implementation, so same syntax here. Hash tag, square brackets. Derive my Debug trait. So much easier, I think you would agree, compared to this, but at least now we know the behind the scenes magic that the compiler is doing for us. All right. And then define a 'new' constructor function. So here I will provide an impl block for my Soda struct. Define my 'new' constructor function. It's going to return a Soda instance. The 'percentage' field should initialize with a hard coded value of 100. So we do not need a parameter for that. But the other fields should be set by parameters. So we're going to provide 'calories'. That's going to be u32. 'price' is a float 64 and 'flavor' is a String. This will return an instance of Soda represented by capital S Self. And in here, I can do 'self', provide my calories, my price, my flavor. And then for the final required field of percentage, I can set that to be 100 like so. Next up, we have to implement the Drinkable trait again, but this time around for the Soda struct. So this is the beauty of traits once again is that the behavior that they represent can be similar, but we can implement it differently with various implementations across different types. So what we do on our Soda can totally vary compared to what we do on our Coffee. So right here, after I have my input block for Soda, I will implement my own Drinkable trait. But this time around for Soda and we have two required methods: 'consume' and 'get_data'. So here is 'consume'. And this time around I want to decrement the 'percentage' field to zero. So here 'percentage' can be set to zero. So for Coffee, we decrease its 'ounces' field. I believe that's what we called it. Yep. And for Soda, we decrease the 'percentage' field. So each type can store its own internal data in a different format or using different types. But now we're representing the shared behavior of being consumed or drank for any type that implements the Drinkable trait. All right. Then we also want a 'get_data' method. And this one should return this String with flavor and calories using the dynamic values from the 'flavor' and 'calories' fields. So here I'm going to again use my format! macro and provide my string with "Flavor", some dynamic content "Calories", and some dynamic content. Then I'll interpolate self.flavor and self.calories. And that should do it. All right. Next up after Drinkable, we want to implement the Display trait for the Soda struct. So this time around we're implementing one of Rust's own traits. But the syntax remains the same: impl keyword. Our trait is Display, the for keyword, then our type is Soda. And very much like Debug, for the Display trait we need an 'fmt' method like so. And here I'm going to change this parameter again to 'formatter', although we might not need it. I think actually we will, but we're going to use a different approach here. We're going to use the write! macro. So instead of calling methods on 'formatter', we're simply going to pass in 'formatter' to write!. So remember that macro is available. This is one approach that we can produce the final result here that's going to store the Display string. So whenever we interpolate soda using curly braces, we're now going to get whatever representation we right here. So I will pass in formatter right here, then a string here. And we're asked to do this silly thing with two asterisks, then my dynamic content, then soda, then two more asterisks. And I can interpolate the value of self.flavor here, and write! will produce the exact result type that I want as the return value of this method satisfying the trait's requirements. So now we have a Display trait implementation for our custom Soda type. All right. Next up, implement the Clone trait. So same deal, import keyword. Our trait is Clone for Soda, and in order to do that, we need to define a 'clone' method. In case you forget, just start writing the 'fn' keyword out, and the compiler and rust-analyzer will tell you based on the Clone trait's requirements. So remember, the responsibility of the Clone trait is to create a full duplicate. That's why the return type is Self. It's going to be another Soda instance, but we call 'clone' on an existing Soda instance. And we get a reference to that using the self parameter. So here I can provide self and I'll create a new instance. And we're told the new Soda should copy over the values from the original Soda including making a clone of the flavor field. So here I need calories which I can get from self.calories. I need price which I will get from self.price. These are all numeric types so they all implement the Copy trait. I don't need to worry about losing ownership. Same thing with percentage which can come from self.percentage. The only one that we need to be careful about is flavor, because flavor is a heap String that does not implement the Copy trait. So by default, ownership will move from the 'flavor' field in the original Soda struct to the 'flavor' field in this new clone. I want to avoid that to make sure they are truly clones and independent duplicates. So here I will take that String and call the 'clone' method on that. That will create a copy of the heap String and avoid that problem. All right. Next up we also want to implement the PartialEq trait. So right here same deal. impl PartialEq for my Soda struct. Two Sodas should be considered equal if they have the same price. So here I'll write fn. I need an 'eq' method that returns a boolean. This is declaring the logic for how we're going to deem two Sodas to be equal to one another. So we return true using the criteria that represents two Soda instances being equal. So the logic here says consider them equal if they have the same price. So I will take the soda that 'eq' is invoked upon which remember happens automatically when we use something like the equality operator, the double equal signs ( == ). Although we are welcome to use the 'eq' method as well. But I will take my Soda that the method is called upon. also equal to the Soda on the left hand side of the equality operator. I'm going to take its price field and compare that to my other soda's price field. And if that gives me a true, I will consider these Sodas equal. Finally, we're also asked to implement the Eq subtrait. That's super easy, as long as we're promising to uphold those three additional requirements for the Eq subtrait. Here, I can simply write impl Eq for Soda, promising that I will live up to those requirements and there are no additional required methods. So we are all set right here. So with that, we are all set to verify our work in the main function. So let me scroll down one more time and let's begin here. In the main function, create a Coffee instance with the 'new' constructor function. Use a string slice for the 'kind' parameter. Choose any milk and any number of ounces. Assign it to a 'latte' variable. So here I'll do latte. I'll take my Coffee struct. I'll call the 'new' constructor function, and I'll provide a string slice for kind. And it's going to be "Latte". You can see that Rust now understands that the concrete type filling in for generic T is going to be a string slice. For my milk, I can do, let's say Milk::Oat reaching into that variant and then let's do 32oz. All right. Output the 'latte' variable in Debug format. So let me go ahead and try to use my Debug formatter right here: latte. And let's make sure it works. And there we have it. You can see our custom dDbug implementation. Remember we implemented the Debug trait manually right here. And we got to decide what should be the Debug string representation of our Coffee. When we use the question mark format specifier we decided to have this all caps coffee. And then each one of our fields right here is printed right here alongside with the corresponding value, which is a Debug string representation of this referenced value. Remember we needed this to be a reference which required us sorry about that, which required us to demand that the T type here is some type that implements the Debug trait. So in our specific latte instance below, the concrete type filling in for generic T is a string slice, a ref str, and a ref str does indeed implement the Debug trait. That is why we have support for this method, which is what Rust calls behind the scenes whenever we use the question mark format specifier. And here is our own custom Debug representation. Once again, if you want it to look differently from Rust's default derive Debug version, you can do that this way. And now you have your own custom representation, and you can choose to show some fields, or all fields or whatever fields you think matter. It's all up to you. You're just altering how you want to present the string representation. All right, so with that, we can return to here invoke the 'consume' method. The 'consume' method comes from our Drinkable trait. And it's going to mutate the latte. So we're going to get a violation until we add the 'mut' keyword. And remember 'consume' comes from right here. And that is a requirement of the Drinkable trait. And it's going to lower ounces to zero. So we're asked to output the latte variable again to observe the expected changes. So I expect ounces to decrease from 32 to 0 after the invocation. And indeed it does. So once again, the trait remains the same. The required contract or the methods that it mandates remains the same. But how a type implements the logic of in this case being consumed can vary. Next up, create a Coffee instance with the 'new' constructor function. Sounds good. Another Coffee. This one will be assigned to a 'cappuccino' variable. Here we go. And this time around we're asked to use a heap String for kind instead of a string slice. No problem. Here I'll do String::from and pass in cappuccino. Here I'll do let's do oat again. And this time around let's do. How about 16oz? There we go. And I need one more colon there. All right. Choose any milk and any number of ounces. Assign it to a 'cappuccino' variable. Invoke the 'get_data' method on 'cappuccino' and output the result. Once again, the 'get_data' method comes from being implementing the Display or Drinkable trait rather for a Coffee. But remember the 'get_data' method will return the String. It's not going to actually print it. So if we want to see this, we have to wrap it up in something additional like a println! So here I'm going to println!, going to interpolate some dynamic content which will be the string returned by cappuccino and the return value of the 'get_data' method. Notice here as well as here, we have the inferred types for our respective Coffee instances. The concrete type here is a String. And so we're going to get "A delicious 16 ounce cappuccino". That again is coming from the 'get_data' method that was mandated by the Drinkable trait. Awesome. Create a Soda instance with a 'new' constructor. Choose any value for calories and price. Use a flavor of cherry. Assign it to a 'pepsi' variable. Sounds good to me. So right here, let pepsi and we're going to take our Soda struct this time around. Call 'new' and we need calories. Let's do 300. Price 2.99. And for our flavor, we'll do a String of cherry. That should do it. And output the 'pepsi' variable in Display format. And you should see "Cherry Soda". So I'm going to once again println!, provide some dynamic content. We'll provide 'pepsi' and its Display format. So this should be sufficient. Just the regular pair of curly braces. And you can see here is "cherry soda". Where did this Display string representation of a Soda come from? It came from our manual implementation of the Display trait right here for the Soda struct. We said this is what we want the user facing string representation of our custom Soda struct to look like with these nice little asterisks. And then the flavor embedded before soda. And that's exactly what we get for our Cherry Pepsi soda. All right. So customizing these very common Rust things or features but for our own custom types. All right. Finally use the 'clone' method to create a duplicate of the 'pepsi' variable, assign it to a 'coke' variable. So here I'll create my 'coke' variable. I'll take 'pepsi' and I'll invoke the 'clone' method. Where are we getting the 'clone' method from? We are getting it from the implementation of the Clone trait right here for Soda. So now Rust knows how to create a full duplicate of a Soda struct, our own custom Soda type, so it knows to just simply copy over the non-heap data right here. Then force a creation or clone of the heap String in the 'flavor' field. We're going to get a totally independent Soda struct that is decoupled from our first one, and now we have a full clone in the 'coke' variable. All right. Compare the equality of 'pepsi' and 'coke'. So right here printlln! I'm going to provide some dynamic content. And we can compare the equality by doing 'pepsi' and then two equal signs and 'coke'. And we're going to get true because there is no variation in their price. But more importantly how did we get the design feature of equality? We got that because we chose to implement the PartialEq trait for our custom Soda struct. So we got to define what equality looks like for our own custom types. Just like Rust gets to define what equality might look like for strings or floats or arrays, we get to define what equality means to us for each one of our types, what cloning looks to us for each one of our types. What the Display of a type in either technical or user facing string representation looks like for our custom types. That is the beauty of implementing these Rust traits for our own custom types. All right, invoke the 'consume' method on the 'coke' variable. 'coke.consume', and we'll also need to make the original variable mutable. Now that is going to call the 'consume' method defined right here. Once again required by the Drinkable trait. Once again, the implementation of how Soda handles its consumption is totally different from how Coffee handles it. This one has a 'percentage' field, while Coffee has an 'ounces' field, but that's not a problem. They can both handle the notion of being consumed totally differently. They're both honoring or implementing the same Drinkable trait. And finally, output the 'coke' variable in Debug format. Sounds good right? Here I'll provide that and output 'coke'. And we should have our default Debug implementation right here for a Soda, which is what 'coke' is. So we should get the Rust default one right here. And we can see we have indeed decreased the 'percentage' field to a value of zero. All right. So long, complex, tricky exercise with a lot of great topics covered. But hopefully you had fun. And hopefully you see the benefits of creating this shared behavior or shared functionality between different types by using traits to enforce these contracts, while still allowing each type to be different from the others. With that, I will see you in the next section.
