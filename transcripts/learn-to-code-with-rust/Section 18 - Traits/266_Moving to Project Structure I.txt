# Moving to Project Structure I

There's a lot of content inside our 'main.rs' file. So the next thing I want to do is to start moving our code into a more traditional Rust project structure. Ultimately, I want this main function to be the only content that lives inside 'main.rs', which is the root of our binary crate. Our binary crate is meant to run the main code, the main content, but it's going to rely on these structs and methods and utility functions that are all going to be defined elsewhere. Okay, so a few notes. The first thing is I have changed some of the code in main from the previous lesson. Basically, we've seen all of this before. I'm just creating a Hotel and an Airbnb. I'm calling various methods on both, and I'm utilizing these two utility functions, just so that we are relying on all of the code above. We are currently not utilizing the 'choose_best_place_to_stay' function, so I'm going to delete that. And there we have it. So let's start our refactoring process by moving everything besides the main function to a new file. I'm going to move it to a new file called 'lib.rs'. As a reminder, if I create a 'lib.rs' file in my src/ directory, that 'lib.rs' file will represent a library crate, which is another entry point into our project. And then the binary crate in 'main.rs' is going to be welcome to utilize the library crate code that begins in 'lib.rs'. As a reminder, the name of the library crate and the binary crate are one and the same, and they will come from the definition within the Cargo.toml file that configures this Cargo project. Our project is called 'traits'. So once I move all the code into 'lib.rs', if I want to access it from 'main.rs', I'm going to utilize the 'use' keyword, then 'traits' is my top level module. It's my top level entry point that I will use to access my code inside 'lib.rs'. We're going to see all of this in just a moment. So for now we're going to create a new file in src/ called 'lib.rs'. That is short for library and it must be called this. I'm going to drag this over to a separate panel in VSCode, and I'm going to cut and paste everything besides the main function. So everything from here all the way up top, including my HashMap and Display imports. I'm going to cut this and move this over here. All right. Cool. So now immediately we see a bunch of violations within 'main.rs' because Rust doesn't know where these names are coming from. All right. So we can try at the very top to utilize my 'use' keyword. And Rust does know that 'traits' now exists because it is the library crate, the file that we see on the right hand side. And that 'traits' name is coming from Cargo.toml. But if I use my double colon syntax and try to import, you'll see there's nothing available. Remember, now that we have our content in a separate module or a separate file, we have to deal with the issues of privacy in Rust. By default, nothing is made publicly available. Every name is private by default. So things like these traits and our structs and our utility functions below are all private now to this library crate or to this top level 'traits' module, however you want to think about it. We need to explicitly mark them all as public in order to enable any other code to reach in and access them. So we're literally going to start from the very top here and start marking a lot of these things with the 'pub' keyword for public. Let's start with our traits. I'm going to mark both of our traits as public. So 'pub' right here and 'pub' right here. Remember, as we saw in the previous lesson, if we want to invoke any method that is mandated by a trait implementation, for example, if we want to invoke 'book', which we're doing on a Hotel right here, or 'get_description', which we are calling on Airbnb right here, that means we're going to need to have these traits in this file scope, in this file's namespace, even though we might not explicitly reference the Accommodation trait or the Description trait in 'main', we're going to be using methods that are mandated by those traits. And thus we know we're going to need to have the traits in here, which means we need to make them publicly available. Now, you cannot actually define the visibility of individual methods within the trait. The visibility of these methods is determined by the visibility of the trait. So if we make the Accommodation trait public, that means we are able to call the 'book' method automatically on any type that implements the Accommodation trait. In addition, down below, where we actually provide the implementation of the Accommodation method, we do not need to mark that 'book' method down below as public either, because it's going to be inferred from the public nature of this trait definition. This 'book' method is going to be public because the Accommodation trait is public, and therefore any definition of the 'book' method down below will be public as well. All right. So we have our two traits. We also need to make our Hotel public as well. As a reminder, this is just the Hotel struct name. It does not mean that, for example, the 'new' constructor function will be public by default. For example, here I can show you that I can now start pulling in Hotel. But notice here that I'm still not able to use the 'new' constructor function, because the 'new' function, as well as any methods on the Hotel struct, are private by default. So the name is public, but the function is not. So now within this 'impl' block, I'm going to mark the 'new' constructor function as public as well. Then we move on to this. This is also a definition of a 'summarize' method for a Hotel. I'm utilizing it right here, so I need to make sure I make 'summarize' public as well. Now here are our two implementations of the traits for Hotel. We do not need to mark 'book' as public. It will automatically be marked public by default because of this 'book' belonging to a public trait. I can show you, in fact, that if I add the 'pub' keyword, the code should still work, but Rust will say unnecessary visibility qualifier. In fact, I guess it's not permitted because trait items always share the visibility of their trait. They get it automatically from the definition of the Accommodation trait above, which is public, so this is not necessary. So once again I want to draw the distinction. If you compare this 'impl' block with this 'impl block, these are two different bits of code. Here we're defining methods for Hotel structs of any type T, and we're mandating that type T implement the Display trait. That is a different type of action from implementing a specific trait, in this case Accommodation, for a type, right? This is not implementing the Display trait for Hotel. This is mandating the Display trait to be implemented by type T, which is the type inside the the Hotel struct generic. This is actually implementing the Accommodation trait. And because it is public above, we do not need 'book' to be made public here. All right. So now we've covered Hotel. We need to cover our other things. So right here I'm going to make my Airbnb struct public. Same thing with the 'new' function. Here we are implementing our two traits. So we do not need to mark these as public. Here we have our two utility functions. I'm going to mark them public as well, 'mix_and_match' with 'pub' as well. And now we should hopefully be good to go. And all that remains is bringing in these bits of code into our 'main.rs' file. So right here I'm going to be importing multiple things from the 'traits' library crate. So I'm going to provide my curly braces. Let's begin with our two utility functions down below. And if you provide a different order here, by the way, and you save, the Rust formatting tool will arrange them in a predictable, expected order anyway. So I'm just typing it out the exact way that it prefers. So we have 'book_for_one_night' Notice that my IntelliSense is automatically working now because it sees that 'book_for_one_night' is public from our declaration on the right. So now it knows the names and what's possible. So comma, I also need 'mix_and_match'. Then let's bring in our two structs which are Airbnb as well as Hotel. This should get us closer, but notice we're running into problems with the 'book' method and 'get_description', just like we saw in the previous lesson. It's not that the 'book' method doesn't exist on a Hotel, it does. It's that the 'book' method is a method mandated by the implementation of the Accommodation trait. Items from traits can only be used if the trait is in scope. The Accommodation trait is not within the scope of this file, so the method is not available. That's why we need to bring in both Accommodation and Description, and in order to do that they must be made public, which is why we added 'pub' here. So here I'm going to add Accommodation as well as Description. The formatter is going to reorder them and there we go. We now have no violations. And if I run this code, everything is now working as expected. We are creating instances of our two different struct types and calling respective methods on both. But all of our code now lives separately from main in its own library crate. This is the start of our refactor, and in the next lesson, we're going to continue breaking this code down further so not all the content lives within a single complex long file. All right, that's all there is to cover in this lesson. So I will see you in the next one.
