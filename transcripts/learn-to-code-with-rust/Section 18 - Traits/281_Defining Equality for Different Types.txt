# Defining Equality for Different Types

As we mentioned in the previous lesson, we can define equality between two different types or, more specifically, instances of two different types. I want to show you that if I Command- or Ctrl-click into my PartialEq trait right here, we'll be taken into that trait's definition in the Rust source code. And you'll notice that the PartialEq trait expects a generic, defines a generic called 'Rhs', which is short for "right hand side". That refers to the value being compared to on the right hand side of the equal sign, and it assumes that it is equal to Self. Capital-S Self refers to the type of the instance. Basically, what this means is the default generic that is being assigned here is assumed to be an instance of the same type. So when we defined this PartialEq trait, Rust assumed that we are comparing a Flight with a Flight, and that's what this will implement. However, if we customize the concrete type that we pass to the 'Rhs' generic that is part of the PartialEq trait, we can define equality with a totally different type. So let me show you how that's going to work. Up above, I'm going to copy and paste my struct of Flight. And I'm going to create a separate Flight or a separate struct rather called BusTrip. A BusTrip is very similar to a Flight. It has an origin, it has a destination, and a time when the trip departs. Let's say as well that I can have a very similar constructor function. So I'm going to copy and paste this 'impl' block above and just replace Flight here with Bustrip. Very easy to do because the two share the exact same three fields, but they don't have to, right? This could be three fields with totally different names. All we have to do is know what we're referring to in our equality method, to know what fields are available on that, and then we'll be able to define equality regardless. So down below, after my definition of PartialEq, I'm going to implement PartialEq again. And the way I'm going to distinguish this from the implementation block above is I'm going to provide my angle brackets here and provide a different concrete type for the 'Rhs' generic. Previously, we did not need to do this because the default assignment was inferred to be Self. Now we are going to override that Self default to to make that right hand side type a BusTrip. But I'm still going to implement PartialEq for Flight. So now what I'm going to do is define an equality method again. And now notice how the analyzer can infer once again the definition of the method. But now 'other' is not going to be a reference to Self, which was a Flight in this block. Now it is a reference to a BusTrip, and it knew that from what we provided for the concrete type for the 'Rhs' generic. So now Rust knows that 'other' is going to be a reference to BusTrip with its respective fields, and those fields could totally vary. The type checker is still going to validate that your code is legitimate, because it's going to know what a BusTrip has. They are just the same as a Flight in this situation to show you the similarities between a Flight and a BusTrip, but these could be two radically different structs. So let's say that we define some other logic. Let's say that we decide that a flight and a bus trip are equal if they have the same time. I know that sounds kind of absurd in the real world, but let's make it something slightly different from the implementation above. So I'm going to take my flight, which is still 'self'. That is going to have a 'time' field, and then I'm going to compare it with the 'other' parameter. Now Rust knows the other parameter is a Bustrip. So it has all of the fields that are on a BusTrip here including 'time'. And it's going to validate that. All right. So here in main we have one Flight created. Let's go ahead and define a BusTrip. So I'll create a new BusTrip struct. The equality is going to be determined by the time. So the origin and destination don't really matter. So let's say we start out in Los Angeles. Let's say we're going to go to Tokyo. And let's say our departure time is once again 8 a.m.. So take a look right here. I'm going to println!, provide some dynamic content, and compare 'a' to 'b'. Notice this code is now valid even though 'a' and 'b' are of different types. Notice once again if I comment out this code, it no longer becomes valid. Comparing 'a' with 'a' is valid. Comparing 'a' with any other flight struct is valid, but not comparing it with 'b' because there's no definition. Now we have defined separate logic for comparing a Flight with another Flight, and comparing a Flight with a BusTrip and using the equal sign for both is valid. We can still compare 'a' with 'a', for example, because we have two different definitions. So Rust is going to know which version of the 'eq' method to invoke from which implementation depending on what is being compared. If it's two Flights, then Rust knows the logic for equality is defined here. If it's a Flight and a BusTrip, then that logic is defined here. So you're going to see the two Flights are considered equal, and the Flight and the BusTrip are also considered equal because they have the same time. Now take a look at this example. This is going to be kind of interesting. What happens if I reverse the order of 'b' and 'a'? You might think it's going to work, but it's actually going to lead to a compiler error. And why is it the case? Remember, whenever we use the equal sign behind the scenes, it is a 'eq' method call. And the method call is on the value to the left of the equal sign. So here for example, we're calling 'a.eq' and passing in a reference to 'b'. This is attempting to call an 'eq' method on 'b', but 'b' in this scenario is a BusTrip, and a BusTrip does not define an 'eq' method because we have not implemented the PartialEq trait for a BusTrip More specifically, a PartialEq trait that caters to comparing to a Flight, right? In order for this to work, we need to implement PartialEq with a concrete type of Flight for the 'Rhs' generic, and that needs to be implemented for the BusTrip type this time around. Otherwise, this is not going to work. So if you want to do something very similar to get this to work, for example, right below, let's say here I can once again implement the PartialEq trait by itself. If I leave it like this, this is going to implement equality for comparing two Bustrips. That's not what I want for the code below. I want to be able to compare a BusTrip to a Flight. And now if I define my 'eq' method, we're going to get our BusTrip reference as the 'self' parameter. We're going to get a reference to Flight. Here the logic can be different or the same as it is here. Let's say for simplicity that's going to be the same. So if the Flight and BusTrip have the same time and now you can see we can compare 'b' and 'a'. So Rust is so versatile that you can even define if you want to different equality logic depending on what's on the left hand side and what is on the right hand side of the equal sign. That's probably not a good idea usually. You probably want equality to work the exact same way, but it just goes to show that these operators are invoking methods behind the scenes. And those methods are different depending on what implementation block they are being defined at in. So this logic on line #56 is relying on this 'eq' method right here. All right. So I'm going to run this. And just to show you one more interesting example, let's say I tried to compare 'b' with 'b'. This is also not going to work because we have only defined thePartialEq trait for a BusTrip when comparing it to a Flight. We have not implemented the regular PartialEq trait where 'Rhs' is equal to BusTrip in order to be able to compare a BusTrip to itself, or to another instance of that same type. So we can't even compare 'b' to 'b', which is kind of funny. Once again, an easy way we can get around this is either implementing the PartialEq trait normally like this. That will define the logic once you add your 'eq' method definition. Or of course, if you want to keep things simple and assume that the equality of all of the struct's fields is suitable, then you can derive your PartialEq trait up here, and that will also allow this code to now be functional. All right. Hopefully that makes sense. That is how we define partial equality across different types. You can now start to treat different types of things as being equal if your program benefits from it. Sometimes you don't care about the exact concrete type, but you do want to enable it to be comparable to something else, whether it be by equality, whether it be by less than or greater than, whether it be by sort order, and depending on what traits you implement and what required methods you add to those, you're going to get those functionalities, choose to opt into those functionalities for the custom types that you define.
