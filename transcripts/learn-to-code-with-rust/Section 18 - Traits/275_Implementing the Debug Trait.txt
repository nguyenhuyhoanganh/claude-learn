# Implementing the Debug Trait

Just like we did with the Display trait in the previous lesson, we can also implement the Debug trait for any one of our custom types. Both of these traits indicate that a type can be converted into a string, but as a reminder, the Debug string is meant to be a more technical version. You can assume that the audience there is Rust developers, who are probably going to be more familiar with various Rust formatting concepts. In comparison, the Display trait string is meant to be for the end users of your program. Now, because debugging is so common, the Rust team gives us the ability to derive the Debug trait implementation, and that is just a reasonable default that the Rust team decided on. For example, if you derive the Debug trait for a struct. So for example, here I can add that familiar syntax of derive and Debug. And since we are including a enum here, we're also going to have to implement the Debug trait for the AppleType enum above. And as you might recall, if I add my formatter right here, which is the colon and the question mark ( :? ) behind the scenes, this will rely on the Debug trait implementation's method requirements, which will format our custom types once again into a string, but a more technical string. So you can see the default here for a struct is going to include the struct's names, all of its fields along with their corresponding values. And usually this is good enough, right? Because this is usually for developer debugging. So custom Debug implementations are less common, but they are still possible. So I want to show you what that looks like in this lesson. Just to play around a bit and show you once again how flexible Rust is with all of these ideas. So first things first I'm going to remove these 'derive' lines. If we were to try to both include this line as well as a custom implementation of the Debug trait, we would then have a compiler error. So it's one or the other. We cannot have both. So I'm going to remove this line and this line. And then I'm going to import another trait called Debug that is also found within my formatting submodule. How do we implement a trait? The exact same way: the 'impl' keyword, then my trait name which is Debug and then my type. So when we derive Debug like we saw a moment ago, this is basically what Rust is doing behind the scenes for us. It's much simpler because it only requires one line of code and it's usually good enough. But if we want to customize it, we are more than welcome to. Now the Debug trait has one requirement and that is a format ( 'fmt' ) method. So I'm going to type that out and press Enter to autocomplete. Now you'll notice this 'fmt' method is basically the exact same thing as we have for Display. So it's two methods with the same name. And you might be curious: is this possible? The answer of course is yes. Even though these two methods share the same name, Rust is going to be able to distinguish which one it needs to invoke based on the usage. So for example, if we are using the regular curly brace syntax, Rust will take the given type and call the 'fmt' method that comes from the Display trait implementation. Vice versa, if we use the Debug trait representation, which is the format specifier and the question mark, or alternatively this pretty-print format. In that scenario, Rust will take the type and call the 'fmt' method from the Debug trait implementation. So based on how it is being used, that is how Rust knows which method to call from which trait implementation. All right. So I'll bring this back to where we started from. And once again I'm going to change this to 'formatter'. And same principles apply. I can react to both of my two possible variants underneath my AppleType enum. So I'm going to match on 'self'. Let's do our RedDelicious variant first. And I think the default Debug implementation of a enum is just going to be the variant name. One way that I can customize this to add a little bit more clarity is to actually include the name of the enum itself. That might be helpful and will help us distinguish between our implementation versus the standard derived one. So here I'm once again going to provide my arrow, then the write! macro, then I'll provide my formatter and for my string it's just going to be a hard coded string consisting of the AppleType enum name and the specific variant which will be RedDelicious. I'll provide a comma here and do the exact same thing for my GrannySmith variant. I'll use the write! macro, pass in formatter and provide my enum name, followed by my variant of Granny Smith. Just a nice little way to customize it. All right. And then down below let's also add our own custom Debug trait implementation for our Apple struct as well. So once again, 'impl'. Our trait is Debug. We're going to implement it for our Apple struct which requires a format ('fmt') method. Once again I'm going to call the write! macro. Let's change this to be 'formatter'. I'm usually not a fan of single letter variable or parameter names, but some developers do prefer them. But I like being explicit, so I will change it to 'formatter'. So 'formatter'. And then it's up to us how we want to represent our technical string version of our Apple struct. So here is something a little bit fancy. Let's do our struct name of Apple. Then I'll do a space and do three colons. And then I'll do a nice box with square brackets. I'll do a space in here and write 'Kind', then provide some room for dynamic content. I'll do a comma here, I'll do a 'Price' and then another space for dynamic content and another space once again, totally arbitrary. I'm just making a nice fancy string that is going to be our our Debug string. And of course, since I have two custom places for dynamic content, I'll provide 'self.kind' as well as 'self.price'. Now an important thing to note here is we are using regular curly brace syntax. So this will actually be the Display trait implementation of our 'kind' and 'price' fields. If you wanted the Debug representation for example, for our 'kind' field which is our AppleType enum, in order to accomplish that, I actually need to use the Debug formatting for that enum, so that we rely on this syntax that we have right here, and I'll show you the differences in just a moment. So there we have it. That should do it. So now we have our Debug representations here so we can run our code. And there we have it. There is our nice fancy string that is different from what the derived implementation of the Debug trait would look like, right? Is it beneficial? It's up to you. You may want to display your types in a certain manner that helps you figure out what kind of data it stores. I'm curious what's going to happen if I add the pretty-print version? Let's see if it's any different. It looks like it's not, so doesn't make much difference there. I'm pretty sure there would be a way to customize how it is pretty-printed, but we don't have to worry about that for now. And once again, notice that for the dynamic content here, which is relying on the 'kind' field, we are getting the Debug trait implementation for our AppleType enum. That's why we get the content from right here. If I remove my question mark and my colon, I'm going to get back to the Display trait implementation for the enum, which is going to be slightly different. I believe that is going to be... exactly! What we implemented a few lessons ago. That is coming from the Display trait implementation for Apple type. And that is the logic right here. So once again, we get all this flexibility in customization and how we want to represent these two string versions of both our AppleType enum and our Apple struct. It all depends on how you prefer to see it. All right. That's all there is to cover in this lesson, so I will see you in the next one.
