# Implementing the PartialEq Trait for Enums

In this lesson, I want to show you that we can implement traits on enum types as well. That is the versatility of traits, and that is that they can actually apply to various different types and types of things as well. So for example, let's declare an enum called Musician and there will be two variants of a Musician. Let's say there is a SingerSongwriter who is going to have an associated name, which I will represent as a piece of associated data using the tuple variant. Let's say we're going to store the name as a string. And then another type of a Musician is a Band. And let's say that is going to store a u32, an unsigned 32-bit integer representing how many musicians are in the band. What I can do here is use my 'use' keyword and then provide Musician and then provide my curly braces. And if I write out Band and SingerSongwriter here, that will just allow me to refer to those variants without needing to prefix them with Musician beforehand. Very similar to how we use the 'use' keyword with, for example, importing names from the Rust standard library. Same principle. We're just doing it from an enum that has been declared within the same file. Now we can very similarly derive an implementation for an enum, just like we can for a struct. So for example, if I derive the PartialEq trait, that will enable us to use the equal sign with two different Musician instances, right? The way this works is the default implementation here considers two Musician instances to be equal if they are of the same variant and the associated data is equal as well. So a SingerSongwriter will only be equal to another SingerSongwriter if they have the exact same String value, the exact same name. A SingerSongwriter will never be equal to a Band, and vice versa. So, for example, here in 'main', we can declare a couple musicians. Let's start with Rustin Bieber and that can be a SingerSongwriter. Once again, I do not need to provide my Musician enum prefix because I have the variant available at the top level, and I'm going to take "Rustin" and call the 'to_string' method to get a heap allocated String. On the line below, let's do the exact same thing, except this musician can be Rustin Timberlake. Let's create another Musician down below. Another SingerSongwriter. I'll call this one Holly, just to have a different name. And the name here can be Holly. And then let's also create some bands as well. So how about a band named Rust No One? That's a fun name. And we'll have a Band with five members. We'll have another Band called "Unrustworthy", and that will be a Band with four members. And maybe... How about "Rust for Vengeance", and that will be another Band with five members. Let's take a look at how equality is going to work here. So if I take, for example, Rustin Bieber, and I compare that equal or equality to something like Holly, in this case they are both of the SingerSongwriter variant, but unfortunately the names are different. So that means these two SingerSongwriters will not be considered equal. And right here we're going to get a false. However, if I compare Rustin Bieber with Rustin Timberlake, now we have the exact same variant, which is Singer Sngwriter and the content of the fields or in this case it's the tuple variant so the associated data, but the same principle applies. As long as the associated data is one and the same and equal, now we can see that these two are considered true. Of course, if I compare Rustin Bieber with any Band like "Rust No One", we're going to get false because they are completely different variants. And similarly, if I compare a band like, say, 'rust_no_one' with 'unrustworthy', these are of the same variant, which is Band, but they have a different number of members between them, five versus four. So this will be false as well. And finally, if I compare 'rust_no_one' with 'rust_for_vengeance', we have the same variant of Band with the same associated data of five. Therefore, they will be considered equal as well. So this is the default implementation. Just to practice, let's try implementing the PartialEq trait manually so we can implement a very similar implementation of our own. And that will give us just the chance to practice a lot of these enum-related concepts. So I'm going to remove my 'derive' up here. And as always our our code is going to be very similar to what it is for structs. So what I'm going to do is begin with 'impl'. I'm implementing the PartialEQ trait for Musician. By default, Rust is going to assume that we are comparing a Musician to a Musician, and that will include all the possible variants. I need to define an 'eq' method, which I'll do right here. Now, there are a lot of possibilities to consider here, because we need to compare both the variants and then the data within the variants as well. So what I'm going to do here is begin with a 'match' keyword. And I'm going to begin by checking what 'self' is, which is the original Musician instance, the one that is going to be on the left hand side of the equal sign. So there are two possibilities for what 'self' can be. The first is a SingerSongwriter, and the second is going to be a Band, and we're going to need a match arm or pattern for each one. So if it is a SingerSongwriter, I can get the associated name of that person and then decide what to do. Now this is only looking at the 'self' parameter, which is the original instance. I still need to compare that with the 'other' parameter and what that represents because 'other' could also be either a SingerSongwriter or a Band. So what I'm going to do is in this match arm.. if 'self' is a SingerSongwriter, then I'm going to run code that is going to match on my 'other' parameter. It's kind of an interesting design, but it is valid code because everything that an arm for a match statement represents is what logic to run in case this is the variant. So if the variant of 'self' is SingerSongwriter, we're going to run all of this code which consists of a match arm for the 'other' parameter. So now for the 'other' parameter we have the same situation. It can be one of two variants. So let's begin with SingerSongwriter. And if it is a SingerSongwriter for 'other', I can get the 'other' parameter's singer songwriter's name, and I can assign that to 'other_name' and then provide a match arm for what to do. So basically what this logic is is if 'self' is SingerSongwriter and 'other' is SingerSongwriter, here is the 'match' arm that is going to capture that. And in that 'match' arm, I can take 'name' from the line above and compare the equality of that with 'other_name', basically capturing the respective SingerSongwriter's two names and then comparing them here to give me back the final boolean. Now I'm going to also provide the other variant that I need for this match statement, which is Band. In this case, we do not care about how many musicians are in the Band because regardless of how many are in here, if it's a Band, we know that it's not going to be equal to a SingerSongwriter, which is what 'self' is. So here I'm just going to return a consistent false. So we basically declared the case of what to do if 'self' is a SingerSongwriter. If it's a Band for 'other', return false, and if it's a SingerSongwriter for 'other', compare the names. So that takes care of this match statement. But we also need to take care of the Band variant for the original match statement beginning on line #10. So now if 'self' is a Band, we need to do the exact same logic. So I'm going to extract the number of members if 'self' is a band and then in my match arm, I'm going to match on the 'other' parameter. And once again there can be two possibilities here. If 'other' is a SingerSongwriter, then it's not going to be equal to a Band for 'self', and thus we can consistently return false. So let's check if 'other' is a SingerSongwriter. If it is, we don't care about the singer songwriter's name and we can just return false. And then finally, if 'self' is a Band and 'other' is also a Band, then for the other band we need to get their member count. Let's call that 'other_members' And in this arm, we'll compare the members from the band represented by 'self', with the 'other_members' from the band represented by the 'other' parameter. Pretty complex stuff, but basically what we're doing here is just comparing all of the possible combinations of how 'self' and 'other' could be, right? Whether they are of the same variant, whether they are of the different variant, and if they are of the same variant, whether the data contained within them is equal to one another. So obviously the PartialEq default implementation does the exact same thing and is much easier than writing this code from scratch. But this does give us a good opportunity to review match statements as well as nested match statements and all of the complexity that comes from comparing equality of enums. So here is our original output. False. True. False. False. True. And if we implemented everything correctly, we should get the exact same result. And there we go. So that is how we implement the PartialEq trait manually. All right. That's all there is to cover in this lesson. So I will see you in the next one.
