# Moving to Project Structure II (Multiple Modules)

We are moving in the right direction, but I want to continue to simplify and break down our code. So the next thing I'm going to do is I'm going to break down the 'lib.rs' file so that its content lives within nested submodules. So how we do this is up to us, the developer. But one way that I thought about it was I'm going to create two modules. One is going to be called 'lodging', and it's going to store all of the lodging related functionality. So these traits as well as our two structs and the methods. And then I'll also create a second submodule called 'utils' which is short for utilities. And that module can store these two functions. This is not the only way or the best way to break down the code. It's just one way that we can keep breaking things down and storing these smaller and smaller pieces within these nested files and folders. So as a reminder, there's two ways that we can create modules in Rust: using files and folders. The first is by creating the module file directly in here. So for example if I want a 'utils' module, I can create a new file called 'utils.rs'. And this was all covered in the "Project Structure" section of the course. So you can go back to that if you want a refresher. The second way that we can create a module in the file system is by creating a folder with the module name, and then inside there creating a 'mod.rs' file. So for our second submodule of 'lodging', I'm going to go with that approach. So in here, in src/, I'll create a new folder called 'lodging'. And in 'lodging', I'm going to create a file called 'mod.rs'. All right. So now let's move the respective contents to each file. So here is 'utils'. I'm going to bring it over here. Into 'utils', I'm going to cut and paste my two utility functions, 'book_for_one_night' and 'mix_and_match'. We're going to come back to this file in just a moment. Then in 'mod.rs', which lives inside the 'lodging' directory, I'm going to move the remainder of the content, which is everything right here. Now simply creating these two files on our file system is not enough. We need to make Rust aware from our crate root, in this case our library crate root, we need to make Rust aware of the existence of these two modules, and we do that with the 'mod' keyword. So I'm going to write 'mod lodging' and then 'mod utils'. Remember when Rust sees this code it starts looking for either a 'lodging.rs' file or a 'lodging' folder with a 'mod.rs' file, and equivalently, when it sees 'utils', it looks for a 'utils.rs' file or a 'utils' folder with a 'mod.rs' file. Then it's able to locate those files and folders on the file system and make the connection. All right. So now you can see, our library crate route in 'lib.rs' is more simplified. We can't just store everything within one file especially as our project becomes larger and larger, it becomes harder to reason about if everything is just tucked away in a single file. And even something like this, you can continue to break down further as your project expands. There's many different ways to do so. You can group it by feature, or you can group it by functionality, like having all your structs in one file or your traits in another. There's no single right answer. It all depends on how you prefer to break things down and categorize them into their modules. Okay, so making progress. But there's a couple things we need to fix before our example in 'main.rs' is going to work as expected. So first things first, in our utils file, we are referencing our two traits, Accommodation and Description. And they are defined within our 'lodging' module right here. So we need to make sure that these two traits are in this file scope in order to access them and make them available within our code. There's two ways that we can reason about this. The first is we can think of the 'utils' module, and we can use the 'super' keyword to navigate one module upwards. This this will bring us to the crate root of the library crate. So if I say 'use' here and I use 'super', this is the equivalent of going up one module or going up one directory in hard drive terms. So if I do 'super', that is going to bring me back to the library crate root. So from the library crate root in 'lib.rs', we have two nested submodules, 'lodging' and 'utils'. utils is where we came from, right? So in this case I can specify that I want to go into the 'lodging' submodule and from there pull in Accommodation and Description. So this is accessing the two traits by using basically a relative path. We are navigating upwards one module to then go downwards into 'lodging' and pull out what we need. Alternatively, we can use an alternative syntax which is starting with the 'crate' keyword. The 'crate' keyword automatically starts from the top level crate root. In this case, 'lib.rs'. Now in this case, writing 'super' and 'crate' is equivalent. But you can imagine if you have a deeply nested submodule having to write 'super', then 'super', then 'super' again to keep navigating upwards isn't ideal. So I actually prefer 'crate' because it starts from the very top. And no matter where you are in your current module, and then from the very top of the 'lib.rs' crate root, you can then navigate downwards and identify the modules where you want to pull in names from. So after 'crate', the syntax will be the same: 'lodging', then Accommodation and Description. Now Rust knows what these traits are and it can apply them here. Okay so now there's two more things we need to do. The first is we need to start pulling in the names in 'main.rs' from the respective submodules. However, remember that everything is private by default in Rust. So when we declared this 'mod' keyword of 'lodging' and 'utils', this is not actually publicly accessible from 'main.rs', from our binary crate root. I can show you right here that if I go into 'traits', nothing is actually available because the declaration of the modules in 'lib.rs' on the right is not public by default. it is private. So even here, since we created this file structure and declared modules, if we want this code to be accessible outside of our library crate, so we want other code to be able to import it, we need to mark this as public. So now the two modules of 'lodging' and 'utils' become available from the very top. All right. So we're going to reach into 'lodging'. And now when we reach into 'lodging', we can pull out whatever it defines. So what we need from here is going to be our Hotel struct, our Airbnb struct, our Accommodation trait and our Description trait. That's a good place to start. And then for 'utils', we have a couple options. We can of course just provide 'utils' like this and pull in the two utility functions that we want. But another thing we can do is simply leave 'utils' as the top level name that is pulled into the 'main.rs' namespace, and then that allows us to simply provide 'utils' as a prefix right here for these two functions, which I think lends a little bit of clarity as far as providing context on where these functions are coming from. If we bring in too many names into this single 'main.rs' file, it becomes harder to to reason about. I think with things like structs, it's a little bit easier because there's the capital letters and the obvious syntax, but now we sort of have a little bit of context that these two things are utilities. This is completely a matter of subjective opinion. If you want to bring in these two functions directly, or alternatively if you want to bring in the 'lodging' submodule as your top level name and then here do 'lodging::Hotel', that's totally valid as well. It all depends on what makes sense to you. And also make sure that you follow whatever practice you choose as a consistent standard throughout your project. All right. So now everything should work. I'm going to run this and we're going to get the expected return values printed. All right. So now our project is a little bit easier to reason about. We can now start adding more structs into 'lodging', more utility functions into 'utils'. If we find that those modules are becoming too complicated themselves, then we continue defining further submodules within them and continue utilizing the same privacy ideas to expose whatever we want to external code that is going to be reaching into our library. Great. All right. So hopefully you enjoyed this refactor. And with that I will see you in the next lesson.
