# The cloned Method

A few lessons ago, we introduced the copied adapter method. The copied method converts an iterator of references to a type to an iterator of just the type itself. In other words, if we have an iterator of references like ref T where t is some generic type, when we call copied, we get back an iterator of t. The caveat here is that the t type must implement the copy trait, so the copied method follows each original reference in the first iterator to the original location in memory. It gets each value and then it creates a copy of it, and this will apply to simple data types in rust that implement the copy trait, such as integers such as booleans, floating points, characters, etc.. Now there is a complementary adapter method called cloned that we'll explore in this lesson, and it's very similar. We start with an iterator that has ref elements, and we get back an iterator of t elements. The caveat here is that this is primarily for heap based data that does not implement the copy trait, but does implement the clone trait instead. For example, a heap string does not implement copy, but it does implement clone. So we can create a copy of a heap string. But we have to be explicit by calling the clone method. And that is essentially what the cloned method is going to do. It's going to take each reference, go to the original data call, clone on that, and give you an iterator of those new resulting data types. All right. So lots of complexity going on. But let's take a look at an example. Let's start by declaring an array. I'm going to call it TS. And I will populate it with three heap strings. Let's do a string with our first T which will be hot Earl grey. And I'm just going to copy and paste this. Let's do our second one of iced green, and our third T string is going to be hot matcha. All right, so let's begin by using the iter method to create an iterator of references. So right here I'll just declare a Maltese variable. Here I will take ts call iter to get back a iterator of references ref strings. Right. And then I'm going to call collect. So of course what this is currently going to give us is a vector assuming I specify it here. But the vector is not going to store the original strings. It's going to store string references because we used iter to start. And of course we could do something like into Iter but that would move ownership away from TS. Right. So this example is hopefully familiar. We've seen this before. If I just quickly print out more TS, we're going to see those strings appear as normal text. But remember this is a vector of ref strings string references. Now let's take a look at what happens if we instead call cloned. Remember cloned is an adapter method. So we call it on an existing iterator. So I'm going to inject it right here before the call to collect I have an iterator here of references right. Currently my iterator has ref strings. So instead I'm going to here call cloned. And what that is going to do is convert this from an iterator of this to an iterator of strings. And the way it's going to do that is by making full clones of each of these three strings. It starts out with the references, but it's able to go to each of these three strings in memory, call the clone method on each, and then get back a brand new heap string a full copy of each one of these. So to clarify, we are duplicating all the data in our program here, or at least within the TS array when we call this. The reason we have a violation here, of course, is because now we are not collecting a vector of string references. Now this is indeed a vector of strings. That's what clone does. It takes your iterator of ref T and gives you back an iterator of just t by calling the clone method on each resulting value. So now if I save this, you can see our compiler error goes away and we get the exact same result. But now we are indeed outputting strings. I want it to be clear with you that there's a couple of ways to accomplish the exact same result. So as an alternative approach to clone, we could have done something like this. Let me just comment this out. We can, for example, call map here to transform each value. And here is each original t we're getting and it's coming in as a string reference. Again a ref string. We can take the T and use that string as the basis to create a clone manually explicitly so I can call clone right here and then call collect. What we are doing right here explicitly is basically what the cloned method is doing behind the scenes. It's taking each ref string and creating a full copy of it, a full duplicate by calling the clone method once again, this doubles the amount of memory on the heap, because we double hot Earl grey ice cream and hot matcha to create these clones from the starting references to then accumulate them in the resulting vector, right? Another approach, just to show you a couple of different varieties of how we can accomplish the exact same result, is we can realize that our Teas array actually itself has a clone method. So here I can call clone on my original Teas array. What that will do is it will create a full copy of the Teas array, which will also intrinsically create a full copy of its nested content. So all three of those starter strings. So now we are operating on a full duplicate of our original array. So we no longer even need to call Iter. We can, for example, call into iter to take ownership and then collect. In fact, the entire process is kind of wasteful in general because regardless, we're just going to end up with a vector of strings. And we pretty much have right here in clone already a full copy of the original data set. At this point we have an array of full string copies. The only difference here is now we are ending up with a vector of those strings instead of an array. But it's the same principle, right? We are just creating a full copy of a heap based data type that is originally stored within our TS array. Now I want to show you a more practical example that will also offer us an opportunity to see where different approaches to solving a problem can lead to either more efficient code or less efficient code. So say that instead of wanting a vector of all of the T strings, let's instead say that we wanted a vector of copies of the strings that contain the word hot. So we want full copies of the subset of strings that fit that criteria. Let me show you two solutions, and let's discuss the differences between them. So let's return back to this. Let's say I have TS. I'm going to call iter to create an iterator of references. Then I'm going to call the filter method. I'm going to provide a closure right here. I'm going to receive each t as a ref to a ref string. That's totally fine on a heap string I have a contains method, and I can use that and pass in the substring that I'm looking for. I'm looking for the text of hot. This will give me back a boolean, which is exactly what I want to return out of this closure to tell the filter method how to filter. So this is going to select just the subset of values that satisfy the criteria. But ultimately my filtered collection here will be an iterator of ref strings. I'm still going to have an iterator of ref strings here, because I am starting with a collection of ref strings, and I'm narrowing those down with the filter method. So if I want now to create a clone of those strings that are going to be filtered out, which is only going to be this one and this one. If I call cloned at this point, then I'm only going to end up creating two copies of heap data. I'm going to create a full string clone of hot Earl grey and Hot matcha. Notice that I have not made a full clone of Ice Green because I filter it out first, before I then clone that iterator of teas to just teas. Now once again, this is all a lazy process, but you can think about this as the sequential pipeline of transformations. So because I've done the filter before the copy, I've spared one additional string from being copied right here. This is all lazy. But then again, when I call collect right here, well now we are still going to get our vector of strings, but this time around we're only going to get the two that have the text hot in them, right. So this is our first option and it is the more efficient one. Now I want you to compare that with the next example which will be less efficient. All I'm going to do is to swap the order of the filter and cloned methods, because what this is going to do is something a little bit different. So I'm going to bring cloned up above filter. So now this is actually going to create one more additional copy. Because first we're going to start out with our iterator of three string references. This type right here. Then when I call cloned I'm going to get an iterator of all three string clones. Right. Because I haven't filtered out anything yet. And when I call cloned, it's going to take the existing iterator of references and create an iterator of clones of the original data. But at this point, at this moment of execution, I still have three string references to all three of my original heap strings. So I will create three clones of the original data in the new iterator of strings. Then I will filter that iterator of three strings down to an iterator of two strings, and then finally collect the results in a vector. The key takeaway is that I have now forced the cloning of one additional string element. This I screen right here. Now, of course, in a simple contrived program like this where it's a matter of one extra string or two extra strings, it's not going to be that significant. But you can imagine if I'm working with a larger data set, I've accidentally required more work than is absolutely necessary. So this is a slightly less efficient solution and can be in larger programs, more significantly inefficient because I am forcing additional copies or clones that aren't strictly required to get the final result set that I want. So be mindful of where you structure and where you place these iterators and these adapters in sequence, because it will alter how rust is going to build up that sequential, transformational pipeline of iterators and the resulting collection that you collect them within. All right. That's all there is to cover in this lesson. So I will see you in the next one.
