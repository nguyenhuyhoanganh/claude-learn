# The sum, product, max, min, and count Methods

In the previous lessons, we explored adapter methods which produce new iterators from existing iterators. Let's now explore some methods that consume an iterator. So this category is called consuming adapters because they exhaust the original iterator to produce a new value. We've seen several ways that we can consume an iterator. We can iterate over it with a for loop. We can call the next method manually, or we can utilize a method like collect or fold. And the methods that we'll explore next are similar to collect or fold. They perform some additional logic when they consume the iterator. To give you back one final value. As a reminder, all of these methods are available out of the box as a result of the iterator trait. Once you have a next method defined on an iterator type, rust can figure out all of this extra logic it needs to do to produce some kind of calculation. For example, in the previous lessons we used the reduce and fold methods to calculate the sum of an iterator of numeric values. Well, an iterator actually supports a sum method that gives you the total of all of those values. So we don't need to write that logic by ourselves. It's automatically available out of the box. So for example here I can declare a total variable, take my numbers vector, create an iterator from it, and call the sum method. And if I output the value of total, we're going to get the sum of all six of those numbers, which should be 108. Now there is one important caveat, and that is we do need to provide the explicit type annotation for the total variable. Even though we are starting with an iterator of I32, we can technically sum to a wide variety of different data types. We can, for example, want an i8 or an I16. So for that reason, we have to explicitly tell rust what we would like to return as the type here. It's still going to be the same sum, but represented as what data type. And once I designate an i32, the code will compile. And here we're going to see 108. So some will add everything up. There's also going to be a complementary method called product, that is going to multiply all of the values in the iterator together. So here for example I'll declare my product variable. Also annotate its type as an i32. Take my vector, call the iter method on that call product on my iterator, and that's going to give me the product of multiplying all of those six values together. And you can see we're going to get that large number. Now keep in mind with the Iter method we actually created iterators of references. But of course rust was smart enough to understand what we wanted to do. We did not want to sum up all of the references which are just memory addresses. Rather, rust understood that we wanted to get the sum and the product of the numbers at those memory addresses, so it automatically dereferenced the addresses for us and arrived at this calculation. Keep in mind, if you replaced a method like this with into Iter, that of course would create an create an iterator that would take ownership over the original data. It would move ownership from numbers to this inline iterator. So as soon as we consume that iterator with the sum method, the original data is lost. Right. And thus at that point, numbers is no longer the owner either. So we cannot call it right here because at this point numbers is no longer the owner. So you have to be careful because we basically lost ownership to create an iterator here for a one time sum operation. And then we cannot access the original data source at all. So usually if you want to preserve the original owner you're going to need to be creating iterators of references. And that's going to require the Iter method call. All right. So I'll return this back. And now our code is back to compiling. All right let me show you a few more mathematical methods that we can utilize. There is a max method to return the largest value from the iterator, and a min method to return the smallest value from the iterator. Now, both the max and min methods are going to return an option enum, because there's technically no guarantee of a maximum or a minimum value. If, for example, you have an empty iterator without values. So here, for example, I'll declare a max variable. I will once again take my vector call the iter method to create an iterator of references, and then I will call max. And we're going to get an option once again. Because technically if this vector was dynamic, if we didn't know what values it had, if it was empty, if the iterator from it as a result was empty, there is no maximum or minimum value to extract. Now, in this case, we know definitively that we have a largest value to extract here. So I feel pretty comfortable calling the unwrap method right here. But as always, you should be more careful depending on the code you're writing and what your guarantees are. Notice as always that Max is not going to be an i32. It is a reference to an I32 because our inner method created an iterator of references. We then used those memory addresses to figure out the largest value from that collection of six. But what we return is the memory address where that largest value resides. Of course, if we print out that reference right here in our display format, it's not a problem, because rust will also dereference it and tell us that the largest value is 42, right. And of course, as I mentioned, if there is a max method to get the largest value, there is a complementary min method to get the smallest value. So I'll swap that with that and swap max with min one more time. And that's going to give us a reference to the smallest value which is four right. All right. So one other thing that we can do is actually count the number of elements within the iterator, which is equivalent to the number of iterations it's going to do. So here I'll declare a count method I will take my numbers vector, create an iterator from it. And then I'm going to call count to exhaust it. It's literally an iteration that only exists to count how many iterations there are, right? Because once you have an iterator, you need to exhaust it Exhausted by traversing every single loop to count how many loops you have, right? So it's kind of interesting to think about, but at the end of the day, it's going to give you that number. And since we started from a vector with six elements, the total number or count of elements in my iterator is going to be six as well. Notice here we get back a u size as a reminder that is an unsigned integer, but the exact type a u32 or u64 will vary depending on the architecture of the computer that the rust program is running on. Now, one interesting caveat with a lot of these methods is that how they will work will depend on the iterators element type. Now, in the case of integers or whole numbers, integers implement the ord or order trait which we learned about in the traits section of the course. The order trait indicates that a type can be ordered sequentially, that it can be sorted, and if rust has the guarantee that it can sort it, then obviously it can then extract the extract the smallest or the largest element within it. The order trait is a sub trait of another trait called the partial order, and the order trait is a lot stricter. Now, as it turns out, we cannot use methods like sum or max on an iterator of floating points, because floats are only going to implement the partial order trait, and the reason that floating types like float 64 do not implement the stricter order trait is because they technically support that not a number or Nan value. As a reminder that Nan, not a number, is the result of some kind of invalid mathematical calculation, but the type of Nan is still treated as a float 64. So, for example, here, just as a quick reminder, I'll declare an invalid variable, and on the right hand side I will divide a float of 0.0 by 0.0. So this is an invalid mathematical calculation. Its result is not some kind of valid number. It's not a number. right? But of course, the inferred type here is going to be a float 64. Its type is a float 64. And that of course can be problematic because if we have a collection of floats, let's say here we have a redeclared numbers variable. And I'll make this a vector of floats. We can have, for example, a vector that holds valid floats like 4.68.8. And then if we sneak in an Nan value like 0.0 divided by 0.0. Well, how do you calculate the sum of this. Right. It would be 4.6 plus 8.8 plus not a number you can think of. Not a number as some kind of chaos, right? Or some kind of nonsense. Well, how do you add nonsense to otherwise valid floating point values. Right. And similarly, how do you sort nonsense? Is it the largest value or the smallest value. Right. So with these kinds of situations, you're going to notice that some of these methods are either not going to work or are going to give you very interesting situations. So let me show you here. I'll just populate this vector with two more values. I'll put in another valid float like 6.2. And then I also want to show you that if we access our float64 type itself and then use our double colon syntax, we can reach into the type. This is how we get, for example, associated functions on any type. But a type can also have associated constants, which are just values that live inside that type definition. And for example, we can access the Nan value right here. And basically what this is, is the exact same value that this calculation right here will produce. I can even very quickly print out my numbers vector in debug format to show you this is still considered a vector of float64, even though the third and fifth element are going to be that Nan, not a number value, right? Its value is Nan, but its type is technically float64. So let me show you what's going to happen if, for example, I redeclare a total variable and try to get a float64 sum, I'm going to take my numbers vector, call iter on it, and call sum on the surface, it doesn't seem like there's going to be any problem. The compiler is fine, but of course what's going to happen when we try to add these numbers along with two Nans? Well, we can't really add nonsense to valid floats. So we're actually going to get a final evaluation of Nan. Right. And usually that's not probably what we're going to want. Right. And similarly if I try to for example take the max let's say I want the largest legitimate floating point value. Well, if I try to call iter to create an iterator and call max and then call unwrap, you'll notice right here it's actually going to tell us you can't call the max method on this iterator because the order trait is not implemented for a float 64 type rust doesn't know where 4.6 is going to rank as far as sorting order among not a number, right? So with these kinds of situations, we have to be a little bit more creative if we're looking to get to the same kind of calculations as we have above. Once again, it might be annoying that this doesn't work, but it's because rust is keeping us safe, right? We don't have the possibility of this Nan type with integers, but we do with floats, and that's why rust has to watch out for that possibility. So for example, for total, here's what I can do instead. Let me just comment this out right here. What I can do is say, well, let me begin by filtering out all of my Nan values. Right. I want to target my valid floats, so to speak. Right. So I can call the filter method right here. I will receive each number. And that will include for example my nan. But I think the parameter is fine for now. And then on every float 64 type I actually have a method called is Nan, which will give me back a true if I have an Nan. But of course I don't want to filter out the nan. I want to filter out the inverse, the values that are actual valid floating point numbers, right? So what I can do, as always, is place my exclamation point here to invert this boolean and that will pull out the three floats 4.6, 8.8 and 6.2. Now at this point, because I have Iter here, I still technically have an iterator of references. I'm quickly going to call the copied method to create an iterator of the actual floating point values. And now I know that I have all of the three floats represented in this new iterator. So this was an adapter and this was another adapter. And now I can call something like the fold method which we learned about a few lessons ago. Say that I want to begin with a value of 0.0. Perform the exact same logic we talked about. I'll provide total and current here for my accumulator and my current element, and just return total plus current. And if I output total right now, we should get the cumulative sum of all of the three valid floating point values. And that should be 19.6. So again a little bit more work. But now we can get a bit more creative and utilize the other functionalities. In the cases where methods like these are not going to work. Here's another example from Max. I can perform the exact same logic. I can call my filter method right here and make sure I filter out all of my Nan values. So I'll say number is Nan and that will give me a true if I have a not a number, I want the inverse. So I will flip this to that. And then what I'm going to do is once again call copied. That's going to give me my iterator of valid floats from an iterator of references to floats. Let's do reduce this time around like so. And here I can once again specify, since I know that I have valid floating point values, I can just start with the very first one, right. And assume that is going to be my first accumulator element. That is the difference between fold and reduce. So here for example I can put my accumulator variable name I can do current. And this time around keep in mind we are looking for the max. We're not looking for the sum. Apologies if I misspoke. So I'm going to get the accumulator, which is the value that I am sending to the next iteration. And my current iterator element that I'm working on. And I'm looking for the largest value. So actually what I can do is I can take accumulator. And knowing that that's a float, a floating point supports a method called max. And Max is an interesting method where you call it on one float, you give it another float as the argument and it will return the largest of the two. And that's actually going to work really elegantly, because if I take accumulator, which will be the largest float encountered up until that point, I will call Max and I will pass in current. This is going to figure out which one of the two is the largest, and then return it so that it becomes the accumulator for the next iteration. Now, in this case, it's interesting to use the word accumulator because I'm not technically mutating or changing an existing piece of data. I'm simply choosing the larger one of this batch of two, and passing that larger one on to the next iteration, where it's once again going to be compared to the next element in line. And the largest of those two is going to be selected. And if I continue with that process all the way until the end of the iteration, eventually this method will return the largest maximum value within the iterator. So now if I save this, we should get our result. It's going to be an option because we use the reduce method. But I can show you right here that here that if I output max in debug format, we're going to get the sum variant with 8.8, which indeed is the largest of our valid floats. So once again we got rid of our Nans. We created our iterator of floats from an iterator of float references. And then we figured out this creative way to just arrive at the largest value through the reduce method. In fact, if we hover our mouse over the max method right here, you can see right here returns the maximum of the two numbers. Ignoring Nan. So that's actually a nice little caveat is if you call Max on a float and you pass in something like an Nan or vice versa, if you have an Nan and you call Max on that and you pass in a valid number, it's always going to choose the numeric value as the largest one. Basically just going to discard Nan if it's in either position, either the value being invoked upon or the argument. Basically, what this means is we can actually get rid of this filter method right here and accomplish the same result, because the max method will automatically ignore an Nan in either position, and the same logic of iterating from the beginning to the end, taking each element, comparing it with the next one to see if it is larger, taking the larger one and passing it on. Returning it out of the closure so it progresses to the next iteration where it is compared once again. That's a nice little creative, elegant solution to get the max, and we're going to get the exact same result. All right. So key takeaway as always. The compiler will tell you if you're doing something wrong. Sometimes you're going to be able to have really elegant solutions like sum and product and max sometimes when working with other data types due to the complexities of that type, you might need to get a bit more creative and combine some of the methods that we talked about earlier on in the section. But by now you have all those tools in your tool belt, so I feel confident that you'll be able to figure out some kind of creative solution to get there. There's often many different ways to solve the problem. Once you know all of these different adapter methods and iteration manipulation techniques, that's all there is to cover in this lesson. So I will see you in the next one.
