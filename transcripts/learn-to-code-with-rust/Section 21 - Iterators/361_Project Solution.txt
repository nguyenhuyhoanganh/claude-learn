# Project Solution

So let's walk through a sample solution to the coding challenge. In the previous lesson we had quite a few exercises to work with, but they offered a good opportunity to both review and apply and combine a lot of the iterator concepts introduced throughout the section. So let's read the prompt from the top. Let's imagine that we are running an e-commerce store that sells home appliances, and another developer has left us some starter rust code to work with. We have this product enum, which has four variants for the four products that our business sells. Then we have this customer order struct which represents an online order. So it has a product field set to the product enum above. It has the quantity that the user ordered, and it has a shipped field representing whether or not the order has been shipped to the customer. We also have this complementary new constructor function that returns an instance of the customer order struct. Finally, we have a customer struct. Each customer has a unique numeric identifier that is, this ID field set to an unsigned 32 bit integer. And each customer struct is also going to have an orders field set to a vector of that customer's orders. So this is a vector of this customer order struct above. All right then in the body of main we have an orders vector with the six orders currently within our system. And we also have a customer IDs by order array that stores the customer IDs of each customer who placed the six orders. So this is an example of where our data might come from. Two different sources right. But it is matched by a shared index positions. So for example this first customer order here within our orders Ray was placed by customer two. And the second element within my orders. Vector. This customer order is connected to the customer with an ID of one, and there's going to be a problem later on where of course we have to combine these two data sources together. All right. So this is our starter code. Let's now move on to our first challenge. So first up extract all of the customer orders where the customer ordered a blender product. So our goal is a vector consisting of references to customer order structs. Print out the vector and it should have a total of two orders. Sounds good. So I'm going to declare my variable here. I'm going to call it blender orders. And we're going to start with our original vector, our collection which is orders above. And in order to select a subset of elements from it I'm going to need to filter. So the filter method here is going to be a good choice. But first I need to create an iterator. So I'm going to call the iter method to create an create an iterator that yields immutable references to each element. In this case an immutable reference to each customer order struct. So upon the iterator I can call the filter method and pass in a closure. And here I'm going to receive each order which I can assign the closure parameter name order to. And then I need to filter based on each order's product type. So on that order struct that I'm going to be receiving on each customer order, I have a product field. And I can use the equality operator to compare it to the target enum variant. So I'm going to reach into my product enum. And using my double colons I will specifically access the blender variant. And that should do it. And the reason we're going to be able to compare the equality of this enum from the product field. And this variant is because I have derived the partial EQ and EQ traits for our product enum. So it's as simple as applying the equality operator the two equal signs in a row, and if we literally have the exact same invariant, this will return true. As a reminder, your closure that you pass the filter must return a boolean, and if it returns true, that's going to be the element or elements that are going to be kept. So this is going to transform or adapt. Our iterator still to store yielded elements of references to customer order structs, but now a smaller subset of those. Right. So now I have my target collection. And then I want to actually collect it within my vector. So I'm going to call the collect method right here. And of course to specify what kind of collection type I want, I can either annotate the type of blender orders or provide my turbo operator right here. And I want a vector. And more specifically a vector of references to customer orders. And that's going to work because we start with an iterator of references. So we never take ownership over either the orders vector or any of its internal structs. We just create an iterator of references, filter down those, and then collect those remaining references that fit the criteria into this vector. So now I'm going to print out blender orders. I'm going to apply not just my debug formatting, but also my pretty print which is placing the hashtag in front. And that's just going to allow it to be a lot easier to see. So let's run this code. And here we can see our two customer orders. And both of them indeed have a product of blender. So that takes care of our first problem. Excellent. All right moving on. Let's scroll down here. The boss would like to know the total quantity of microwaves ordered across all customer orders. Filter for the customer orders where the product is a microwave. Extract the quantity field for each customer order. Then calculate the sum of those values and the answer should be six. And then we are asked to solve the challenge with both the filter method, then the map method, as well as the singular filter map method. All right. So right here I'll declare another variable. This will be our microwave count. And it's going to be very similar. We're going to begin by creating an iterator with iter an iterator of references. And similarly we're going to need to filter. So I will use my filter method. Upon the iterator I will receive each order. But this time around we want to select for a different product, which of course is going to be our microwave instead. All right. So that takes care of that. Now previously we gathered all of the elements inside a vector. This time around we want to take the subset of customer orders and get the quantity field for each one. So we really are transforming our iterator from storing references to a customer order to storing the actual quantity number that is a transformation. Or in other words, we can call it a map because we are bridging one type of value to another. We want to progress over this filtered iterator, and for every complete customer order reference, we want to just get one piece of data from it, that being the quantity field. So for that I'm going to call the map method and provide my closure. Once again I'm going to receive each order right here. And from each order I want to get the quantity. So in here I can just take my reference and access the quantity field on it. And now we have a new transformed adapted iterator storing the quantities. So now that I have the collection of numbers that I want, I just need to add them all together. And for that there is the convenient sum method. But I am going to need to provide a concrete type for the generic here representing the type of the numeric value that sum will generate. So I am working of course with quantities here that are unsigned 32 bit integers, since quantity can only be positive. So I think it's reasonable to get a sum that is that same type as well. So here using the turbo operator, I will specify that I want a sum value that will be of type U32. And once I save that our code should compile and we should get a total of six. So I'm going to print this out. I'm also going to comment out our previous code, just so it doesn't take up so much real estate in our output, but we should get a total value of six. And if we look at our entries for microwave, here is our first one. There's a quantity of one. Here is our second and final microwave order with a quantity of five. One plus five is six. And that's exactly what we should see here. And that's indeed what we get. Perfect. That is the total number of microwaves that have been ordered. So the second part of this challenge was to solve the problem using filter map instead. And so I'm just going to adjust my existing implementation here. I'm going to put filter map above. And then once I'm done with this alternate syntax. I will remove my two calls below. So once again with filter map we're going to provide a closure. We are still going to receive each order, but this time around we want to return an option enum. We return some if we want to keep the element. And then for the some variants associated data, we can transform or map the value however we would like and then vice versa. We return a none variant if we want to omit or exclude a specific element. So this time around I'm going to use a if statement. I'm going to check if the order's product is equal to microwave, and this is emulating the functionality I have below in my filter. If this is true, I want to keep the current element the current order, in other words. So I will provide a some return value here. That is a variant of the option enum. And then for the associated data representing what I actually want to store within this represents my opportunity to map or bridge to the eventual value that I want to store. So this is the map portion of filter map, and that's where I'm going to take my order and get the quantity field specifically. And that allows me to both filter and map at the same time within one closure. And then for my else clause in case I have a different product, I want to not keep that specific customer order, so I will return none instead. And that's going to be my replacement. So I can get rid of filter and map right here. Run this and we should get the exact same value of six. And indeed we do. All right. Wonderful. So let's move on to the next challenge right here. The boss would like to pass in a quantity from the command line. And they want to see a printed vector of all of the orders where the quantity of the order is greater than or equal to the user input. So for example, if they provide a command line argument of five, they want to see a vector of the two custom orders with a quantity greater than or equal to five. That would be these two. If the boss does not provide a command line argument or provide some kind of invalid numeric value, we should fall back to printing custom orders with a quantity greater than or equal to two. So there are two components to this. The first thing is we need to parse our command line arguments to extract the single user value to be provided. And then we actually need to apply it to the filter operation. So let's begin with the command line arguments. First. In order to do that we're going to need to rely on the args function which comes from the environment submodule. So at the very top of the file here I will add my use keyword and pull in the env or environment submodule. Then within my main function and I can comment out this code once again to reduce the total output. I will declare a variable called user quantity, and my goal will be for this to eventually be the actual number that I'm going to be comparing all of the customer orders quantity with. So I want this ultimately to be that number, right? Whatever the user provides, such as five or a reasonable fallback of two. So we begin with our env submodule. We're going to call the args function to get back an args struct. That struct supports the iterator trait, which means we can iterate over it with something like a for loop, or call an adapter method on it. That's exactly what we want to do, because the very first entry within this iterator is always going to be the Executable's name. The actual executable that the compiler places within the target directory. We don't care about that name. So I immediately want to skip it. So the first thing I'm going to do is call the skip method and pass in an argument of one to give me back a brand new iterator that skips the first entry in this iterator, which will be the Executable's name. Then I also want to account for the For the possibility that the user gives me too many command line arguments. Maybe they give me something like cargo run five, three, ten, right? And I want to not care about anything except for the very first one. So after I have skipped the very first iterator entry of the executable name, I want to also call the take method and pass in one to take just the next element in line. So if I have an iterator of five command line arguments from the user, I'm going to shorten it to just be an iterator with one entry from the command line. And it's going to be the first user entry, because we're going to skip the executable that is in the first position of our starter iterator. All right. Now remember what the user provides is always going to flow into our program as a string. So the next thing I want to do is convert my string hopefully storing a number into the numeric equivalent. Now I have an iterator of one, but I'm still welcome to call something like the map method on it because my goal here is to transform. I want to take an iterator of one value and transform it into being an iterator that stores a different value. And that different value is the parsed numeric equivalent of the string. So here I'm going to call my map method and pass in a closure. And I'm going to receive each command line argument. So I'm just going to call that quantity because I know already that I'm only going to have one command line argument at a time. So map sort of gives this notion that there may be more than one. But remember we're going to limit our iterator to just one entry. So I know that what I am mapping over is supposed to be the command line entry representing that user's desired quantity, such as the value five right here. So as we can see here, quantity is inferred to be a string. So what I'm going to do is take that string and call the parse method on it, and pass in a turbo operator to designate what kind of data I would like to attempt to convert the string into. And I want a you 32 right here. So let me save that just so we get the benefits of formatting. Now remember the parse method is going to return a result enum because there's the possibility that whatever the user provides cannot be converted to or parsed into an unsigned 32 bit integer. So to account for that, what I'm going to do is add my unwrap or method call right here and fall back to a value of two. So if the user gives me some kind of nonsense string, I'm going to say take this iterator of one total entry. Go over that if we can get a number from it, great. If we can't fall back to two. All right. So at the end of the day we still have an iterator storing a maximum of one entry within it. So what I'm going to do to get that entry is I'm going to call the next method. Now the next method is going to give us back an option. Because remember there's still the possibility of an empty iterator or one that has been exhausted. So for example, if the user gives us no command line arguments then skip is going to skip over the executable file name. Take with an argument of. One is basically going to be insignificant because we have no more entries left in the iterator because the user didn't give us any command line data. So when we map over an empty iterator, we get back an empty iterator. And thus if I call next, there's still the possibility of no entry, and thus this has the potential to return none in the case of an empty command line argument. So in case we get none, in case the user has not given us anything from the command line, I will once again call unwrap or on this option in order to fall back to a reasonable default, which will once again be a value of two. So once again, here we are dealing with the possibility that the user gives us a value, but we cannot parse an integer at all from it. And here we are dealing with the possibility that the user does not give us a command line argument at all. And here you can see finally user quantity is that unsigned 32 bit integer that we want. And now we can use it within our filter. So right here I will declare a orders by quantity variable. And we're going to apply the exact same filter principles from above. Take my orders vector. Call the iter method on it to create an iterator filter each order. But this time around it's going to be based on taking the orders quantity and seeing if it is greater than or equal to whatever the user has asked for, which is going to be represented by my user quantity variable above. That should do it. And then after I have filtered, I want to collect the results again in a vector. So I will call collect and specify that I want to aggregate the gathered results into a vector. Still storing references to customer orders. And that should do it. And with that we can go ahead and print out our orders by quantity. And our solution should be suitable enough to support one command line argument or none. Right? Because we have accounted for the scenario that the user provides us nothing. So let's begin with the ideal case. I'm going to open up my terminal right here, and I'm going to be within my target directory. So I'm going to run cargo run then a space, then two dashes. And then the value that I want to serve as the quantity to filter by. So the problem asks us to provide five to filter for those orders with a quantity greater than or equal to five we should get two. Here they are. And here is our first customer order with a quantity of five. And here is our second one with a quantity of ten. So that works. Now let's take a look at the other scenario which is what happens if we provide no command line arguments at all. And in that case we are going to have an empty iterator right here because map will iterate over nothing. And then when we call next we're going to get none. We're going to fall back to unwrapping that none variant and giving a user quantity of two. and then that user quantity of two will be used for the filtering operation here. So our code should still work without a command line argument. And now we're going to see all of the custom orders with a quantity greater than or equal to two. And indeed we do. So that is looking great. And we can go ahead and close the panel and move on to our very next problem. So next up the boss would like to know how much inventory of each product we need for the orders that are still not shipped. So we want to create a hash map where each key will be a reference to a product such as fridge. And each value will be the sum of that product's quantities, but only across unshipped orders. Make sure to target only Unshipped orders, so we don't care about the orders that have already been shipped, which have a shipped field of true. As set up here in the constructor function, we want to get the Unshipped order and get the quantities of each product by product within a hash map, so that we know how much inventory we need to be able to support those shipments. All right. So quite a lot of complexity here. As always I'm going to comment out our previous examples. And I will begin by declaring a variable called product quantities. So first things first we need to filter for our unshipped orders. So that's going to begin with a regular filter method call. So same principles as above. Take my vector call the iter method to create an iterator of references to customer orders. And I'm going to filter I'm going to provide a closure that receives each order. And this time around I want to check the shipped property. And I want the ones that have not been shipped. So I will compare the value of the shipped field with false. And if that returns true, indicating that the field is false, I want to keep those. So this will return true for the unshipped orders, and those are the ones that I want to ship. So now I have just the unshipped orders. The next step is assembling the hash map. In order to accomplish that, I'm going to use the fold method. Remember, the benefit of a method like fold or reduce is that we can build up one total value over the course of a complete iteration. So we want to start with an empty hash map. And for every unshipped order that we iterate over, we want to use that as the basis of continually mutating the hash map to keep track of every product we encounter, and then summing up the total quantity of that product that remains in our filtered collection of unshipped orders. So what I'm going to do is take this iterator and call the fold method on it. Remember, the fold method takes two arguments. The first is the initial value of the accumulator. So I will provide an empty hash map by using the new constructor function on a hash map. Then I'm going to provide a closure. All right. And the closure is going to going to receive two parameters. The first is going to be the accumulator which is the HashMap to start. And then it's going to be every return value from the previous iteration. And then the second closure parameter is going to be the current element which is going to be each customer order. So for this HashMap I'm going to call that accumulator data here. And then for my current element I'm going to call that order because it's going to be that reference to the customer order like so. All right. So within the body of this closure I need to do two things. I need to add a product as a key if it is the first time encountering it. And I can give it a initial value of zero. Then I need to take the quantity from that order and increment my count for that given product. And then very importantly, I also need to return my HashMap represented by my data closure parameter, so that it makes its way as state or data to the next iteration. So first things first I'm going to declare an entry variable. I will take my data parameter representing my HashMap. And as a reminder, we can call the entry method on a HashMap to conditionally add a key if it does not exist. So what I want as the key here is going to be a reference to a product. So I'm going to take my order. I'm going to take its product which I can access from the product field. But I want to make sure I pass in a reference to that, so that I do not transfer ownership of the product to be inside the HashMap as the key. So I will pass in my reference to my product enum. And as a reminder as well, we can call the or insert method and that will only add the given key and a value if it already does not exist. So the very first time we run into each product type, we're going to add it as a key along with this corresponding value, which I will designate as zero. Now, if the key already exists, that's not a problem. Entry is simply going to give you access to that value, and you're going to be able to modify it. In the case that you already have it. And if it's the first insertion indicating the key has been added with a value of zero, we're going to get the value. In either scenario. So in summary entry is going to have the current count of the current product, whether it is our first time running into it or any subsequent time. All right. So that is going to be a reference. So in order to actually modify that value I'm going to use my dereference operator to take my entry and then use my augmented assignment operator to add the current orders quantity. So if it's my first time running into a product, I add it with a value of zero, and then I increment that quantity count by the current orders quantity. And if I've seen the entry before I'm going to get its current or existing cumulative sum up to that point. And then I'm going to again increment that sum by the current orders quantity. So that's going to perform the right modification. The last step that remains is returning my actual data HashMap. And then I think one additional thing I'm going to need to do is to enable the modification of this HashMap, the mutation of it by adding a new key value pair or incrementing the increasing quantity count. I also need to make sure that I make my data parameter here mutable, so that I have permission to mutate the HashMap and then as well return it. The only reason we're seeing a violation here is because we need to bring in this HashMap type into our file. So at the very top here, I will add my code to reach into the standard library. Let's do it above. Reach into the collection submodule and pull in my hashmap type. And there we go. All right. So that should do it. So this is going to give us a HashMap with keys of references to the product. And we're going to complete the iteration. Increment the quantity as needed. Keep returning the hash map so that it serves as the next value of the data parameter on the next iteration, and so on and so on until we exhaust the iterator. And at that point the final returned piece of data. Here the final hash map, if you will, will become the final return value of the fold method itself. And thus you can see here product quantities is inferred to be that hash map storing a reference to a product and a quantity of a u32. So if I apply my debug formatting right here, I can print out product quantities. Once again, I will apply my pretty print with the hash tag. Let's take a look at that. We should have a hash map that looks like this. Ten for fridge, two for toaster, four for blender. That is exactly what we have here. So we have come up with a map or a hash map that connects our product categories with the total sum of how many products we have to ship across all of our orders. All right. Looking good so far. And that is going to bring us to our last two problems. Next up, our warehouse worker informed us that they've shipped the next Unshipped order. Find the first unshipped order among the customer orders, and change its shipped field to true. So this one is luckily a little bit easier. I'm going to comment out this code. First things first, I need to find the very first element that satisfies a condition not all elements, but just the very first one. So I'm going to call the find method on my iterator here. And then in order to have permission to modify some state or some part of that struct, I also need to make sure that I am borrowing mutable references. So here I'll begin with my orders vector. I will create an iterator of mutable references by calling the permute method. And on that iterator I'm going to call the find method and provide my logic. So for any given order that I'm iterating over, I want to check whether it's shipped field is set to false. All right. So just to provide a comparison, in this example, when we used filter, we looked for all orders that had the criteria of being unshipped. Here we are simply finding the very first one. And we're going to get that as a option. So that option is going to store that mutable reference. So what I'm actually going to do here is I'm going to combine this with the if let construct. Remember the find method returns an option because there's the possibility that no element satisfies the closure. So if there is an element that satisfies the closure, it's going to be the some variant storing that mutable reference. And that's exactly the variant that I'm going to compare with with my if let statement. So here I'm going to say if let if I have specifically the some variant returned from find I'm going to have access to the order. more specifically, a mutable reference to the order. And so I'll provide my equal sign here and then provide my curly braces right here. So again to reiterate, this block will only run if the find method gives me back a some variant. And in case it does, I will also declare an order variable that can be used within the block that immediately comes after. That's why it's called if let because it combines an if statement a conditional check for this. And it also combines that with a variable declaration. We will declare an order variable set to that mutable reference to a customer order, but only in the case that the condition is met. So now I know that order inside my block represents that mutable reference. I can take that order and change its shipped field to true. And that will update our struct's state. So here I can print out our orders in debug format. Let me show you as well. After I add my prettyprint, if I go up here and check which order is the first one that is Unshipped, it should be this one right here, which is our blender. So my expectation is when I output this orders vector, this first customer order should now have its ship field set to true. Let's take a look at that. There it is. Here is our first customer order. Here is that blender. And now it's shipped. Field is set to true. Looking great. And that allows us to progress to the very last problem which as I mentioned, is a very tough one. Let me begin here by commenting out all of our former code. There we go. So the boss would like to see a vector of customer structs. As a reminder, that is this struct right here that has an ID with the unique customer identifier and an orders field set to a vector of the customer's orders. So each customer struct will hold the id and a vector. Find a way to merge the customer orders with the customers who made them, which is gathered up in this supplementary array. Then aggregate that total data into your customer structs. Then finally collect those customer structs within a vector and then sort that customer collection by ID. So quite a lot going on here. But as we take a look at this output it should hopefully make more sense. So we have all of our customer structs within this vector. They are sorted in order by their id. So one before two before three. And then for the orders field we have captured the customer order structs that were originally within our orders vector right here. And the way we were able to associate these orders with the customer who placed them is based on their shared position with the customer. IDs by order array. So here we have six total customer orders. This array has six total user IDs. Some users may have only placed one order, for example Customer three. While some may have placed multiple ones, which is why the orders field is a vector. All right, so it's totally okay if you struggled with this problem or any other one. Previously, this was a tough challenge that required us to combine a lot of different iterator concepts into one. So let's go ahead and approach it step by step. So first things first I need to merge my vector and my array for the purposes of iteration because they are bound together based on the shared index positions of the elements. So I will declare a variable called customers. And my goal will be for this to eventually be my vector right here. So I will begin with my orders vector up top I'm going to call into Iter. I no longer care about borrowing references. I'm okay with taking ownership because this will be the last use of the data. So now I have an iterator that's going to take full ownership over those customer order structs. I now want to combine it based on position with this array right here. And as a reminder for that we can use a helpful method called zip. ZIP allows us to merge two iterators together. And so here we can provide another iterator or something that can be used as the basis of creating an iterator, which for us is going to be our customer IDs by order array. All right. So that gets us a little bit closer. Now as far as the solution to iterate over all of these customer orders and associate them with the right customer. What I chose to go with here is a HashMap. I decided to create a HashMap, where I would store the customer id as the key, and the value for that id is going to be a vector, where I'm going to insert or push all of the customer orders as I iterate over them. So then at the end I'm going to have a HashMap where I'm going to have keys of IDs and values of vectors of customer orders that HashMap is unordered, but that's not a not a problem. I could then aggregate that hash map into the appropriate customer structs, and then gather or collect those customer structs within a vector. So here's what that code looked like. Right here I'm going to call the fold method. Very similar approach to what we did in the previous example. So I'm going to create a new HashMap right here with the new constructor function. And I'm going to provide my closure right here. And so the very first parameter here for the closure is going to be that accumulator. I'm going to call it IDs to orders and make sure it's mutable because I'm going to be modifying it in every iteration. And then remember the second closure parameter is going to be the currently yielded element. But since we called zip as the previous adapter, each currently yielded element will actually be a tuple. Because there are two pieces of data, there is the customer orders struct, which comes from the original orders vector that we created an iterator from. And then the second entry within this tuple is going to be the customer id. Since we zipped up our orders vector with our customer ID array. So we're going to get both the order and the customer ID. So here within my parentheses I will declare a name for my order and customer ID. Again the only reason we have two is specifically because we called the zip method a little bit earlier. All right let me try to save this. So we get a little bit of formatting. I do apologize for kind of the clustered look and feel. But there's a lot going on here. So now within the body of my closure there's a couple of things we need to do. We know at the very end we're going to need to return IDs to orders, since that is going to be the HashMap or the accumulator or the shared data or state that I want to transfer to the next iteration all the way until the end. But before I do that, I want to make sure I modify it. So I'm going to follow a very similar protocol to what we did earlier. I will take my IDs to orders hash map I'm going to look for an entry. I'm going to look for a given key within it, which is going to be the current customer ID that I'm iterating over. It is possible that this is our first time running into this ID, and thus it doesn't exist as a key, or it's possible that it's going to be a subsequent time running into this customer ID, and I have to account for that. So once again, I can use the or insert method, which will handle either situation gracefully. If this is my very first time running into a given customer ID like one, I want to insert it as a key within my hash map with a corresponding value that I will provide right here, and that value will be an empty vector. This is going to be the vector into which I'm going to push all of my customer orders, right. So remember this will either create the key value pair or give you the current value. So this will either give us The vector that already stores customer orders. If this is a subsequent time that we are running into our customer ID, or it's going to give us the newly created empty vector, because this is our first time running into the customer ID and we've just created it. So this is going to basically be our orders vector. We can call it that right. And what I'm going to do here is giving that orders vector I can call it mutable right here. I can take that orders vector and push on the current order that I'm iterating over into that. All right. So again we have our order right here because we are iterating over our orders vector to start. So I have each customer order I'm going to take the ID I'm going to either insert it as a key into the HashMap or get the current value. In either case, orders is going to be a vector, either an empty vector or a populated vector. Regardless, I'm going to push on the current order For that vector. And then I'm going to return the hash map so that it makes its way as the accumulator to the next round, to the next iteration. So here you can see from the inference that we're going to have hash maps where our IDs are i32. And that reflects the customer IDs right here. And then for each key of the user's ID I'm going to have a value where the vector is going to be the customer order. Right now I have this hash map. But what I ultimately want is a vector of customer structs. So I need to convert this hash map into a collection of customers. The problem is I have a hash map here, and in order to call a method like map to convert this collection of IDs and vectors to a customer struct, I need to actually create a brand new iterator. I started with an iterator, but with the fold method I ended up with a hash map. So that's its own regular collection type. So what I'm going to going to do here is on this hash map, I'm going to call into Iter to now create an iterator that's going to take ownership of the hash map and the elements inside it. And now that I have that iterator I can map over that. Now as a reminder, whenever we map over a hash map here in our closure parameter, we're once again going to get a tuple of values, because the yielded element for each round of a hash map iteration is going to include two components the key and the value. That's exactly what we're going to get as the two elements within each tuple on each iteration. Now our key here, the very first tuple element is going to be the customer id. And the second tuple element is going to be the value for that hash map key, which is going to be our customer orders. And you can see that inference pop into place right here. Here's the user ID. And here is all of the orders. Those vectors should be populated with all of the customer order structs. All right. So what do I want to map this tuple. Two. I want to map it to an instance of this customer id struct, right? I'm starting with an iterator of tuples. I want to transform it to an iterator of customer structs. So right here I'm going to return an instance of customer. And the customer struct needs two fields. It needs an ID and it needs an orders vector. But luckily that's exactly what I have extracted from my tuple. So now I have an iterator that's going to store customer structs. The last step is now collecting those customer structs into my desired vector. So here I'm going to call, collect and use my turbo operator to designate that I want a vector of customers. Right. That should do it. I should just add one more closing bracket right here. Right. So we started with our tuple of integers and vectors of customer orders. We created a iterator of customers. Now I am collecting that vector of customers right here. So if I print this out right now using my debug format and pretty print as well, let's do that right here. We can see that we are indeed going to have the desired output with just one missing detail, and that is that they're not sorted yet. And the reason they're not sorted is because we gathered all the IDs within a HashMap, which is an unsorted collection. So when we iterated over that HashMap in no guaranteed deterministic order, we were successfully able to create a whole bunch of customer structs and stored them within a vector, but not in any kind of guaranteed order. So you can see right here ID of customer is three, then two. So they're not sorted right here as ID four. We do have the orders field correctly populated with our vector of customer orders. But now the last remaining step is sorting this vector by customer ID. So to accomplish that, what I'm going to do is first up, I'm going to mark my customers vector as mutable, which I've already done, because I'm going to sort it, which is going to mutate it by changing the order of the elements. And then to complete this, as you may recall, on a vector without creating an iterator, we have a sort by key method that allows us to provide a closure where we provide a closure parameter of each individual element. This is a reference to a customer struct, and then I can designate the field by which I want to sort and sort by. Key is going to sort the vector by that struct field in ascending order from smallest to greatest. And of course I want to target the customer ID field. And that's going to be the number by which I sort the complete container, the complete struct, and then sort by key will place all of those structs in ascending order based on the value in their ID field. So now when I run this we should get the desired result. Here is our outer level vector. Here is each customer struct sorted by ID. Here is one coming before two. Coming before three. And for each customer struct we also have the orders field which is going to store the vector of all of the orders that that customer has placed, which we collected from this original vector right here by creating an iterator, then zipping it up with this, then creating a hash map to establish the associations between customer order and customer ID, then transforming that hash map into a customer struct, then aggregating those customer structs into a vector, and then sorting that vector by struct key. Lots of complexity here. I wanted to toss in a challenge that was nice and hardy, and especially one that involved a lot of these different methods. If we scroll down here, we can see we have a total of looks like six total adapters and iterator constructor methods being invoked here, as well as sort by key. So I wanted a nice hearty challenge that showed you how a lot of these ideas can be combined and chained together to represent that complexity. It's still a pretty elegant solution, in the sense that we only have about ten lines of code that can perform all of that complexity of aggregation and composition and filtering and folding and performing all of these really powerful, transformative operations, still in a very declarative manner. But obviously, there's a lot of nuance because there's there's a lot of complexity in writing this code the right way and choosing the right iterator methods and adapters to compose together, to chain on together in that pipeline to arrive at your desired result. So hopefully, even if you struggled, you had fun. I had fun myself, trying to figure out how to solve this and building up this challenge. So with that, that's all there is to cover in this coding challenge and in this section. So I will see you in the next one.
