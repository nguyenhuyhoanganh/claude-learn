# String Iteration

Sometimes there will be multiple ways to iterate over the same type. In that scenario, the type will provide different methods for different traversals or iterations. If a single Iter method is ambiguous. So here's an example from earlier in the course. If we take something like a string, a string consists of characters, and each character will occupy one or more bytes in memory. Now, one string character that we visually see is not always equal to one stored byte in memory. One English alphabetic character occupies one byte. But for example, a visual character like an emoji may occupy 2 or 4 bytes. So we can iterate over either the characters or the bytes in a string, and due to the ambiguity or uncertainty about what iteration over a string means, rust will of course allow us to iterate over either option, but there is no iter method, or into iter either method or Wintermute method on a string, because there is no certainty about what the developer intends. So we have to use one of the two complementary methods, either chars or bytes, both of which will iterate over something different. The chars or characters method will return an iterator of the individual Unicode values, and each value will be of type char. An actual visual character, and in comparison, the bytes method will return an iterator of each raw byte, which is just a numeric representation of that character or a part of the character sequence. I just want to show you these distinctions in this lesson as just a heads up. It's not too important to memorize all of this, but let's begin by declaring a seafood variable, and I'm going to write the text oyster. It will be a string slice. And here I'm going to add an oyster emoji. So on a mac OS computer I can use the function key in combination with the letter E to bring up my emoji keyboard. On a windows computer, you can press the windows key on your on your keyboard. Then the period key to access an equivalent emoji keyboard. I'm going to look for oyster right here. Select my oyster right here. And I'm just going to remove this space right here and add my semicolon. So let's start with the bytes method which returns an iterator that's going to yield each byte. So here I can say for each byte in. And I'm going to take seafood and call this bytes method. That's one option for how we want to iterate over the string. Now this seafood string is going to occupy a total of ten bytes. That's six bytes for the first six English alphabetic characters and then four bytes for the actual oyster emoji. This character appears to us as one character visually, but it occupies four bytes in memory. That is, it's kind of internal representation. So if I print out each byte and here I'm going to do something a bit different, I'm going to use just the print macro, which avoids the addition of a line break at the end of the output, just so that we have everything presented on a nice sequential line. I'm going to interpolate the value of byte, and then I'm just going to place a forward slash. So it's easy to decipher and notice what byte is. Here is a U8 an unsigned eight bit integer. And we're sort of going to get like the numeric representation of this string and its bytes in memory. So this is more of the way that the computer interprets this text and how it is stored in memory. For us visually we see the characters, but the computer sees it in memory as this collection of bytes in sequence. And afterwards we're still going to have the seafood owner remain the owner, and we're going to be able to print it out just fine. We can see right here it's actually being concatenated to the end because we have no line break. Now the second option we have is going to be the chars method. And that is a different type of iteration. That's an iteration over the actual individual visual characters that we see. So here for example, we could see ten different numbers being output one for each byte, but there are seven characters in comparison in our seafood string. So if I write a for loop here and give my iterator variable a name of character. Here I'm going to call a different method to produce a different iterator, and that method is called chars. Once again there is no iter or into iter method on a string, because once again the rust team did not want to give you a default. If there are multiple ways to progress over a given collection, right, you can do it either one of these two ways. So it's hard to decide what should be the default or the standard. So the language requires us to specify exactly what we are iterating over. In the case of a complex type, like a string. So here if I do a print and I will output character once again with a forward slash, you can see right here we're going to get something totally different. We're going to get the actual visual characters rather than their numeric byte representation. And once again the seafood variable remains the owner of the content. And the same rules are going to apply to heap strings. So if I change this right here to a string from, the exact same principle is going to apply, the string is going to maintain ownership. All right. Now I want to also show you a couple more helpful things to close this lesson off. I'm just going to comment this out so it doesn't distract us. So I'm going to do a quick print line here. And we know that we can take that bites method that we just saw. And that is going to give us an iterator of those bites themselves. And we can of course choose to progress or iterate over them with something like a for loop. But those iterator types are also going to support their own family of methods. They are a valid type, just like any other. So for example, what the bites method gives to us that return value that implements the iterator trait, thus allowing it to be used in a for loop. That type is called bites, and that must be a struct. And that struct supports a method called Len, and that is going to give us the length of the iterator. And that of course for bites is going to be ten. As I mentioned, there are ten total bites within the string. And in comparison, if I take my original seafood string and I call the chars method. Once again we're going to get an iterator and I can call a method on this iterator type, which is actually called chars. It's once again a separate struct that this method returns. Both of these return values will be their own respective trucks that implement the iterator trait. And they're going to support different methods. So this char struct supports a count method. And it's going to give us something very similar which is the count of characters within the iterator. The count of elements. And that is going to be seven. So we can now see that actual numeric difference that we were previously seeing in our manual iteration. But the more important key takeaway here is that what we have here is a bit of a sneak preview of concepts that we're going to be covering a little bit later in this section, because what these methods do is they actually exhaust the iterator by themselves. A for loop is not the only possible way that we can exhaust an iterator. Rather, we can call a method on the iterator to perform the exact same logic of exhaustion. So for example, to create a count to calculate how many elements occur within the iterator, what needs to happen? Well, behind the scenes rust needs to do something like a for loop, where it progresses over all of the elements in the char's iterator and counts how many total there are. Now we're not doing something for each individual element, which is why we don't need a for loop. But this is actually an example of a method that will exhaust the iterator. So if you were to take this code and assign it to a variable and then call the count method, we're actually going to exhaust the iterator. And the variable that this is assigned to would no longer be the owner, it would no longer be valid. And that's just proving to you that manual iteration with something like the for loop is not the only way that we can exhaust an iterator. They're all going to support their own family of methods to do things like the actual traversal, to get some kind of value, to filter for some kind of values, to arrive at a new iterator. There's all kinds of different manipulations that we can do to an iterator through the use of methods. We're going to see more of those examples as we progress throughout this section.
