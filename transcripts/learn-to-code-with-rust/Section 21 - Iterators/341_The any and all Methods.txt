# The any and all Methods

Sometimes we don't care about extracting every element that satisfies a condition, or the first element that satisfies a condition. Rather, sometimes we just want to validate if all of the iterator's elements satisfy a condition, or if at least one of the iterator's elements satisfy a condition. In other words, sometimes we're looking for a boolean, a statement of truth rather than the actual data itself. Now, using approaches like the filter and find methods, we could still arrive at these booleans in a more explicit, verbose manner. For example, if I wanted to check if all of the channels in my channels array were of this channel type of programming tutorials, I could do something like this. I can filter for all of the channels that meet the criteria, collect them in a vector, and then compare the length of the vector with the length of my channels array. If they have the exact same length, that means my filtered subset has all of the original elements, which means all of the original TV channels must have met this criteria of having this channel type. So to perform all logic, I could do something like this. I could print line once again, take my good channels vector, call the Len method and use the equality operator to compare it with the channels array's length, also calculated with the Len method. And that's going to give me the boolean that is reflective of that all logic. This false tells me that it is false that all of the elements in my iterator satisfied this condition. Only two of the TV channels met this criteria out of four. So it is false to say that all of them met it. So this is all logic that I am indirectly calculating. The complement to. This is any logic which is validating if any element in the iterator satisfies a criteria or the other way to put it is at least one satisfies that criteria. So for something like that, we can very similarly use the find method. And that as a reminder is going to give us an option enum. And if that is the some variant, then we know that the find method located a match. And if there's at least one match, well then at least one element satisfies the criteria. So for example to calculate any logic. In other words, if any of the elements satisfies this condition, I could do something like this I can print line, take my good channel option, and on an option we have a method called is sum. And that's going to return true if we have the sum variant inside that option. In this case we do because we have at least one channel with this criteria and this channel type. And thus we're going to get a true right here. So this is all logic and this is any logic. But of course it's a little bit verbose. So of course rust has a couple nice complimentary methods that will accomplish the exact same result with a little bit less code. So first up we have a method called all, and the all method is going to return true if all of the elements in a given iterator satisfy the closures condition. So we're going to replicate what we just saw with the filter method and the length comparison. So here for example, what I can do is declare a variable called all our rust. And I want to check if every element within my channels array satisfies the criteria of having a channel type equal to programming tutorials. So as always, I'll begin with my data source. Then I'm going to create an iterator like with the iter method. And then I'm going to call this method of all. All is going to accept a closure just like Filter and Find. It's going to receive each channel one at a time. It should be through a reference. And there it is. And so here I can perform the exact same logic. I can take the channel struct, I can access its channel type field and use the equality operator to compare it to my channel type. Enums. Programming tutorials. Variant. Now the key takeaway here is that this all method does not return a new iterator. Rather it just returns the direct boolean if all of the channels satisfy this criteria. In other words, if this closure returns true for every single element in my iterator, no matter how many I have, then the final evaluation will be a true. Otherwise, if the condition is not met even for one single entry, then we're going to get a false back. So here, if I output the value of all the rest, in this case we're going to get a false again, because it is false that all of our entries in the channels array are rest, because this condition was not met for all of them. All right. One quick thing that I forgot to mention about the rust documentation in regards to methods like all and filter and find. When you have a function or a closure that returns a boolean like we do right here. The common technical terminology for that, especially in the documentation, is a predicate or a predicate function. So when the documentation says pass in a predicate, it just means passing a function or a closure that returns a boolean. That's all a predicate is. The all method is an example of a method that accepts a predicate, and all of these methods are pretty efficient. Under the hood, they're going to consume as many elements as are needed to figure out the final calculation. So, for example, if rust can immediately figure out that this condition will not be met by all of our elements, which you can technically figure out just by looking at the very first TV channel and knowing that if this is not a programming tutorial channel, then all of them cannot be. Well, as you might guess, it's going to be efficient and it's going to stop the iterator right there, because it knows that it can just give you back a false and vice versa if it sees a condition being met on the very first element, only then is it going to progress to the very next element and keep checking only to the end of the iterator. If every element that it has encountered so far has satisfied the condition. So as always, it's not going to take any more steps than are strictly necessary to figure out the calculation. All right. So that is all logic. And as you might guess to emulate this example, we can perform any logic checking if any element satisfies a criteria. So I can say something like any rest which also means at least one is rust. Same principle I will take my original collection of channels, call the iter method because I need an iterator upon which I can call this method called any. It has the exact same idea we pass in a closure. Here is our channel, and I can even copy and paste this exact logic because it's going to be the exact same. The difference, of course, between these two, like so, is that this any method will return true if any of the elements satisfy this criteria. This will return true if all of the elements satisfy this criteria. So this one is a little bit less strict, but it depends on what you're looking for. So here I will check whether any rust. And in this case we do indeed have at least one TV channel that has this criteria met. And thus we're going to get a true here and get false and true. And that is essentially a replica of the more verbose, explicit way that we figured out this problem at the start of the lesson. So that is the any and all methods once again invoke them on an iterator, pass in a closure or a function. Remember, the technical word for that is predicate passing a predicate that returns a boolean, and it's going to give you back a boolean return value as well that reflects whether all of the elements or any of the elements in the iterator satisfy that Boolean condition.
