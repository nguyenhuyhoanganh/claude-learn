# The reduce Method

In this lesson I want to introduce the complementary reduce method. The reduce method is very similar to fold. In fact, it's what the fold method usually is called in other programming languages. So the first difference between the two methods is that reduce only accepts a closure as its single argument. It does not need us to provide the starting value, the first initial value of the accumulator, because it automatically supplies the first value of the iterator as that starting accumulator value. It does not give us the choice to modify the starting value. It simply chooses the first value in the iterator. The second difference between the two methods is that reduce returns an option enum. And that's because it is possible that the iterator will not have any values, in which case there is the possibility of no starting value and thus no value to return. So if the iterator is empty, reduce will return the invariant. In comparison, the fold method will return your provided starter value in the case that the iterator has no elements. It always has at least one piece of data to work with, which is the first argument to fold and reduce does not have that luxury, and thus in the case of an empty iterator, it will give you none. Let me show you the reduce method. It's going to be very similar. So here is my earnings array from the previous lesson. I will declare my sum. I will take earnings. I will call the into iter method to create an iterator. And I'm going to call reduce. And I'm going to provide the exact same logic as I did in the previous lesson. You can think of this as equivalent to the second argument you pass to fold. It's just the closure. So here I will provide my two parameters. I'll call them total for my accumulator and current for my current iterator element. And I will just do total plus current. And you can see immediately from Rust Analyzer that we are going to get back an option, potentially storing an I32 in its some variant. So I can show you right here that if I print line and output some in my debug format, I'm going to get the some variant storing that total sum of 33. So that's another key difference between fold and reduce. Reduce is going to give you back the option enum and the sum variant specifically if there is the value to give back. So again we did not have to provide a starting value of zero. Rather what reduce did is it used for as the starting value and then progressed from there. You can almost think of it like skipping the first iteration entirely. It just took for the very first entry in my iterator, and then repeated this logic for every subsequent value of seven, nine, and 13 and arrived at the same value. So I do want to show you why the reduce method returns an option, and that is in case you have an empty iterator. So let's say we had an empty array here. By itself this is insufficient because the compiler doesn't know what kind of elements the array is going to store. But I can fix that with a type annotation. So here as a reminder, for annotating the array type I can do a pair of square brackets. I can say that the array will store i32 elements. And then after a semicolon and a space, I will say that its length will be zero. It's kind of weird because the array doesn't store any type of data, but we have to provide some kind of type. And I've just chosen an i32 here. So this is an example of an iterator that has nothing in it. And thus the reduce method cannot use the first entry in that iterator as the starting value. There is nothing to create or accumulate, and thus we're going to get back the none variant instead. So that is the key difference between reduce and fold. The reason that fold can always give you back some value is because if you have an empty iterator, fold still has the first argument you pass to it, and that can be the the final return value, the final accumulator if you will. Here we don't have that design. And that's why the reduce method returns an option instead. So let me give you another practical example. Let's say that I have an array of strings. I'm going to call it address portions. And this will represent the portions of an address that I want to build up. So let's say I first am going to have the street represented as a heap allocated string. So let's do a string here of one, two, three Elm Street. And then let's do my city. Let's call that suburbia. And then after that let's do my state which will be new Jersey. Let's say that I want to build this up into a final single concatenated string. So I want a string that looks like this. I'm going to have my individual fragments separated by a comma and a space. So there's my first entry, then suburbia, then another comma and a space, and finally new Jersey. So first things first. There is a really elegant method on an array called join that accomplishes this automatically. In fact, it's able to do so without even creating an iterator. So here I can show you in a basic print line that I That I can take my address portions array and call the join method. And what this does is it joins or combines together the string contents. It concatenates all of them. And then it also adds the argument that you specify here in between every two subsequent strings. So in other words, what I provide here is what it's going to inject in between every two strings. And I want it to be the comma in the space. So if I provide my string here of a comma to space, we are joining or concatenating these three elements together, separating every two with a comma and a space. So that is going to add the comma and a space between one, two, three Elm Street and Suburbia, as well as between suburbia and New Jersey. So the join method is kind of the proper idiomatic way to accomplish what we're about to do with reduce. That's just the nice nice to know. All right. But let me show you the reduce based approach. So right here I'm going to create a variable called address. And once again I will take my starter collection of address portions. I will create an iterator from it by calling into iter. So I will take ownership of the strings, but that's not a problem. And then I'm going to call reduce and pass in my closure. My closure is going to accept two parameters. The first is my accumulator. And the second is going to be each current element that I'm iterating over each one of these I'm going to call each one of those strings portion since it is a portion of an address. All right. So here in the body I need to specify how to build up the aggregate composite thing that I'm then going to return to provide it to the next iteration. That just goes to show that the logic here of reduce is identical to what it was in fold in the previous lesson. So here is my accumulator. And because I'm using reduce accumulator is automatically going to be 123 Elm Street. In fact you can think of this as starting on the second iteration right here of suburbia. So suburbia will be my portion. And my accumulator will be the first entry in the iterator, which is one, two, three Elm Street. So I want to take my accumulator string of 123 Elm Street, and I want to want to concatenate to the end of it. So first up I'm going to call push string. And I'm going to add my separator which is my comma and a space. And then on the next line I'll take accumulator again. Call push string again to concatenate. One more thing. And then I want to pass in my actual portion here. Now the push string method actually expects not the string but a string reference rather. So it doesn't take ownership. So I need to make sure I borrow here. And then there's a couple more things I need to do. Because the push string method mutates the original string. I need to make sure I mark my accumulator parameter with mute. That just gives me permission to call push string. And then finally I need to specify the accumulator. This built up concatenated string as my final return value. I can write it like so on the final line, because I need to return it so that it becomes the next value of accumulator on my next iteration. So I'm going to concatenate this string. I'm going to extend it or expand it and then pass it on so that the next iteration in this case new Jersey on the next iteration. Accumulator is going to be everything I've built up. Portion will be in new Jersey. I'm going to concatenate a comma and an empty space, then new Jersey, then return the final string wrapped up in my sum variant, and then I'm going to have my complete address right here so I can show you that is going to be the sum variant storing my concatenated string. And there you have it right. Obviously the join method is a little bit cleaner, but it goes to demonstrate the value or the purpose of reduce. Just like with fold, we're building up some composite data type over time using each individual element in the iterator. The difference between this and fold is that the first element in the iterator will become the first initial entry of the accumulator. We do not get to specify it. It's just automatically assumed to be the first entry in the iterator. And because there's the possibility of an empty iterator, there's the possibility of nothing to start from. And that is why reduce returns an option. That's all there is to cover in this lesson. So I will see you in the next one.
