# The enumerate Method

In this lesson, we'll cover the enumerate method in case you're curious. The word enumerate means to mention things one by one, which is kind of a similar definition to the word iterate if you think about it. The enumerate method is an adapter that transforms an iterator such that the new iterator will give you the index position of each element being iterated over. Now it's still going to give you the original element as well. It's just going to package up that original element along with its index position inside a tuple. So it's like enhancing or augmenting the starting iterator to yield one more piece of information, the position of each element, which is the index. Remember the index is a number that represents each element's order in line within the iteration or within its composite data type. And the index starts counting from zero. Sometimes you need the index to perform some kind of logic. Sometimes you care about where the where the element is located within the iterator rather than. Specifically what the element's value is. So let me give you an example. Let's say that I have a vector. I'm going to call it applicants. And these can represent the people who have entered into some kind of prize pool. So here is a vector. Let's do Rob. Let's do Bob, let's do cob. Let's do Alex, piers, John and Dan. And let's say that I'm going to be selecting the winners of this prize pool. And my basic logic is going to be every third person is going to be a winner starting from the very first person. So in this case Rob is going to win the prize. Then if I jump three people ahead, that's going to be one, two, three. Alex is also going to win. And if I jump three more ahead piers John, Dan Dan is also going to be a winner. And I want to extract that subset of those three people from my applicants vector. So this logic is not dependent on the actual strings. I don't care about the actual content. I care about the strings position Inline within the original applicant's vector that's going to form my iterator. So the basic mathematical logic that I can apply is I can take each index position. I can take its remainder when dividing by three. And if that remainder is zero then I know I have every third element. For example Rob at index zero. If I divide zero by three, I have zero with no remainder. And because I have a remainder of zero, I know that should be valid. And similarly if I look at, for example, Alex at index three three divided by three gives you a remainder of zero. And once again, if I use that logic of keep a person if they have a remainder of zero at their index position when divided by three, that should give me Rob, Alex and Dan. They are at index positions zero, three and six. All right. So here's how that's going to look I'll declare my witness variable I'll take my applicants vector I'll create an iterator called into Iter. And that's going to take ownership. But that's totally fine. And right now I just have an iterator of the elements. I don't know their position in line, but I'm going to call the enumerate method which is an adapter. And this is going to give me a brand new iterator that will include that information. Next up I'm going to call the filter method and pass in a closure because I want to narrow down my list. And now filter as a result of enumerate is not just going to receive the names. It's going to receive a tuple of the name and the index. But the interesting caveat here is the index is going to be in the first position within my tuple. So here within my pipes I'll provide my tuple syntax, my parentheses, so I can give a name to each element. My very first element in the tuple will be the index. And then I'll have the actual applicant or the person's name. And so now here what I want to do is perform that mathematical logic. So I care about the index that will take the index I will apply the modulo operator which is shift five. And I will look at the remainder when dividing the index by three, and compare that remainder using the equality operator with zero, and that's going to give me the result that I want. Now notice that the compiler is giving us a warning because we're not using the applicant in any way. And that's correct because we don't actually care about this string slice. We care about its position in line. So one way that we could solve this problem, as the prompt indicates, is to simply give it an underscore in front of the name. Or instead of that, we can just give a regular underscore like so, and that will solve the problem as well. Now keep in mind enumerate creates an iterator of tuples. So when I filter down this subset I don't have a filtered collection of names I have a filtered collection of tuples. Still I have a filtered collection of tuples where the first element is the index and the second element is the actual applicant's name. So I now need to transform that into an iterator of just the people's names, so I can then collect that in my final resulting vector. So I have to call map here. Like so provide once again a closure. And this time around once again I'll provide parentheses because I'm receiving or getting back tuples. Still once again holding the index and the applicant's name in that order. And this time around, from this tuple of two, I want to map that to my final result, my final single value of just the applicant's name. This string slice right here. So I will map from a tuple to a string slice like so. And now I have the problem in reverse, which is basically that I don't need the index. So here I can get rid of this with an underscore. So now I've performed a whole bunch of transformations I'm now ready to collect. So here I'm going to do collect provide my method call, provide my syntax right here of a vector of string slices. Since I never transformed them into heap strings I know there's a whole bunch of warnings now. Hopefully they will go away very soon. It might be a case of a missing syntax, but everything looks pretty solid to me right now. I'm going to print line. I'm going to output the value of winners in debug format and that should do it. Let's see if this works. And indeed it does. Looks like there was some interesting false positives coming from Rust Analyzer. But you can see can see they've all disappeared. And here are our three names Rob at index zero, Alex at index three, and Dan at index six. Now you'll notice that we are using a filter followed by a map. And as you might guess, we can actually consolidate those two into a single filter map method call. As a reminder, filter map must return an option enum which has either a some or a none variant. If it's a some, we are indicating that an element should be kept. And if it's a none, we are indicating that the element should be thrown out. So let me just remove these two method calls right here and replace them with a single filter. Actually a filter map. My apologies. Here is my invocation. Here is my closure. And once again, I'm still going to receive a tuple of the index and the applicant. And now in the body here I can perform a basic if else check to check if my index is evenly divisible by three. So once again same principle if I will take my index, which is the first element in my yielded tuple, I will divide it by three and check the check the remainder and check if that is equal to zero. If that is the case, I want to keep the applicant right. So in order to indicate that I want to keep the current element being iterated over, I will return some. And then the actual associated data that I want to package up is going to be my applicants name. So this is basically performing the filter logic. And this is basically performing the map logic, the transformation of a tuple of data to just a single string slice that I will package up in some. And then here in else I can return none instead. And that should accomplish the exact same result. So when I run this, we're going to see the exact same vector of Rob, Alex and Dan. So that is an introduction to the enumerate method. Once again it's an adapter that transforms an existing iterator to give you the index position alongside the original captured element, whatever the original iterator had alongside the index position of it captured in a tuple. That is going to be the resulting iterator that enumerate is going to produce. With that, I will see you in the next lesson.
