# The filter and find Methods II

So this is a bit of a bonus lesson. I wanted to show you how to apply the filter and fine methods to a more complex data type, like a struct. So far, all of our examples have been working with simple types, and I wanted to show you something at a higher level so you can feel free to just quickly go through this video. The concepts are all familiar, but we're applying them to more complex data. So let's begin by defining a new enum called channel type. And this will represent a category for a channel on television. So we can have a comedy channel, a news channel, and of course a programming tutorials channel. That's the most important one. So immediately above I will add my derived syntax and derive the debug trait. And I also want to derive two more traits which are going to be partial EQ as well as EQ. Now, as a reminder, what these two do is they enable equality comparisons using the double equal sign between two channel type enum instances. The default trait implementation of these will consider two enums equal if they have the same variant. We don't even have to worry about any associated data here, so an equality comparison will literally check if it is the exact same variant between two different channel type enums. And that's a reasonable standard. And we're going to see why we need this equality definition in just a moment. Immediately below I'll declare a struct called TV channel, and every TV channel will have a name set to a string, as well as a channel type that will be of type channel type from above. So it's going to be one of those three options. And I will also derive a debug trait implementation for my struct as well. All right. So now in the body of main let's create some TV channels nested within an array. So right here I'll declare a channels variable set to an array, and I'm just going to break things up so they're easier to read. Here I'm going to create a TV channel. Let's give this one a name of CBS. And for the channel type I can do a channel type of comedy. And that's just one entry. Let's add a couple more for our second TV channel. Let's do a name of Rust Live. And this of course will have a channel type of programming tutorials. Let's do two more. Let's do a TV channel of NBC and its channel type will be news. And finally one more TV channel. This one will be called rust TV. And of course its channel type will also be programming tutorials. Make sure to add my semicolon here and there. We have it. So let's use the filter method to extract all of the good TV channels, which of course are the ones dedicated to rust programming. All right, so down below I'll declare my good channels variable right here, and we'll come back to the vector type in just a moment because let's talk about it as we progress. And we're going to begin with our channels array. We need to first create an iterator. So I'm going to go with iter to create an iterator of references. Now these are going to be references to full TV channel structs. But the same principle applies. So then I'm going to pass my filter method to convert this iterator to a new one. Here is my closure and I'm going to provide a parameter here of channel. Now the type of channel as we're going to see right here is going to be a reference to a reference to a TV channel. Remember filter is going to give you a reference to each value from the original iterator. And we already have an iterator of references. If we wanted an iterator that took ownership, we could do into Iter, but then we would take ownership away from the channels array. So that creates, as always, a series of trade offs that you have to consider. Let's say I wanted to preserve this data, which forces me to take an iterator of references. Well, now I have to deal with the fact that filter is going to get a reference to a reference to each TV channel. Now, as always, rust is going to try its hardest to make things easier for us. And in this case, we're going to be lucky because we will not need to dereference when we are, for example, accessing a field on a struct or invoking a method. In those situations, rust knows automatically to dereference, and it can even do so several levels or layers down. So whether it's a reference to a struct or a reference to a reference to a struct, this syntax will continue to work. And that is just rust again being really beneficial in helping us out. So for each channel I'm going to use my field access syntax. I'm going to do my dot and I want to access its channel type field. And I want to compare that using my equality operator to a channel type of programming tutorials. And in order to enable this equality comparison, that's why I needed to add these two traits right here. Because if we did not have these, then rust would tell us, I don't know how to compare a channel type enum to any other. You have not defined what equality looks like, but now that we have the default implementation, it assumes that they are equal if this condition is met. So for every channel that has a channel type of programming tutorials, we're going to filter that out in this lazy iterator. And that's going to be of course this TV channel and this TV channel right here. All that's left is to call the collect method. All right. So now this code is correct, but it will not compile until we provide the correct annotation. Now as a reminder, if you're either lazy or don't know what you can say is, well, I know that I'm going to get a vector or that I want a vector in this scenario. And if I'm completely confused about what I'm working with, remember you can put this underscore. And if rust can infer then it will. And the code will compile and that will unblock you. However, it's also good to try to actually figure out what we're working with and then validate your assumptions. Because if there's a type mismatch, the compiler will tell you. So is this a vector of TV channel structs? The answer is no. I can prove that to you by providing this code. You can see we're going to get a mismatch from the compiler right. Now why is it the case? Because we are starting with an iterator of references to TV channels. Then using the filter method we are creating a subset of those original values. Well what are those original values from the iter iterator. They are references to TV channels. Filter is just giving us a smaller set of those references to TV channels. So when we collect that result set in a vector with the collect method, it's going to be a vector of the original data type from the Iter method, which is TV channel references. So if I provide my borrow operator here to indicate a vector of references to TV channels, now this code will work. All right. As a reminder, once again, as an alternative, you can provide your turbo fish operator here. Write your syntax like this. And that should do it. You do not need both, even though you can provide both. But I can remove this annotation here. And that's going to work just fine, right? And I can of course print out my good channels vector. And we have debug formatting for that vector as well as for our TV channel struct. So we're going to get our two TV channels here in our vector. Here is our first one of Rust Live. And here is our second one of rust TV. They have been filtered out based on their channel type being the programming variant of the channel type enum. Let's progress to another example. Let's say that we wanted to extract the good channels names. So rather than extracting references to the complete TV channel structs, let's say I wanted a vector of the actual channel names that we have in the name field. So right here I'm going to remove this turbo operator. And to keep myself honest, I'm going to declare that I eventually want good channels to be a vector of strings. So of course at this point the code will not compile. So we have successfully filtered out our rust TV channels. But there's one more step we need to take, and that is taking this iterator consisting of references to TV channels, and then mapping over that subset of two TV channels from the original four and getting the name from each. So how do I perform this same consistent operation from an iterator to a new iterator? For that we need the complementary map method, right? We need to map from a starting value of a reference to a TV channel, to the ending value of the string that we're going to get from its name. So before I collect, I'm going to call the map method. And I'm going to provide my closure right here. And this will receive a channel, which of course will be a reference to a TV channel. And so in here what I can do is access channel dot name. The problem with this is that this is a heap string. So unfortunately if I try to just extract it, it's going to attempt to take ownership and leave the original TV channel struct in a weird state. So what I'm going to do instead is take this heap string, then call the clone method to create a full copy of it. So now the TV channel struct will retain full ownership over its name field. And here I'm just going to create a full clone. Now I have mapped still lazily to an iterator of strings. Then I'm going to collect those strings into a vector. And it is with this method call that we actually perform the iteration and give us back the good channels vector. And so now you can see instead of getting a vector of TV channels or references to TV channels. We actually get these cheap copies of Rust and Rust TV. As one more example, let me show you the find method. It's going to work exactly as we expect. Let's say we want to find the first good TV channel. We want to find something good to watch. So we don't want every result. We just want the very first one in our collection. So same principle. I'll declare a good channel variable. We can skip the annotation here. We're going to begin with our channels array. We're going to call Iter. And that is one advantage of calling iter right here and borrowing references. We did not lose our original data source. And thus we are welcome to perform additional iterator based operations like here. We got all of our good channels. Well, now we're able to do another iteration because the original owner of channels is still the owner, right. So when you're borrowing references you don't have to worry about ownership. Then you have to deal with the complexities of references. So it's always trade offs right. So here is our iterator I'm going to call find on that iterator. That's going to give me back an option. But I need to provide a closure specifying the first match to look for. So this again is once again going to be a channel parameter. That is going to be a reference to a reference to a TV channel. And here, of course, I can do the exact same logic as I have above. Take the channel, take its channel type field and compare it to quality to specifically the programming tutorials variant in my channel type enum, and the very first one that matches if there is one will be wrapped up in the some variant. If there are no matches, we're going to get a non variant regardless. It's a consistent return type of an option. And remember because we started with an iterator of references, the some variant is technically going to be storing a reference to a TV channel rather than the actual TV channel struct itself. And that's totally fine when it comes to this case of printing. So here I can take my good channel option, print that out in debug format. So that's some variant is going to store a reference to my TV channel which itself will be printed in debug format. So here's the some sum variant. Here is the nested reference to a TV channel with all of its information. And indeed it is the very first match where the channel type was met which will be rust live here. All right. And of course you can print it. You can also do some fun stuff like write a match statement and react to it. Right. So here's my sum with channel. And I can give them something funny like great choice to watch. And I'll interpolate the value of channel in debug just to make sure. Of course we reward them, because learning rust is a really good use of your time. Right. And for the non variant we'll say something like there was no rust programming on the TV literally and metaphorically because it's a bad joke. So there we go. Same principle. We're now combining a lot of these different rust ideas, creating iterators, filtering over them, mapping them, gathering the result set, finding the first one using the match construct. So all of these familiar ideas just adding up together to create all this, all this powerful code. All right. That's all there is to cover in this lesson. So I will see you in the next one.
