# The filter_map Method

Next up, let's introduce the filter map method. This method allows us to both filter a subset of elements, and then perform a mapping operation on those filtered elements within a single closure within a single method call. So filter map, like the name suggests, is equivalent to combining a filter method call on an iterator followed immediately by a map method call. So let me show you two different solutions to solve the same problem using those two aforementioned approaches. So let's say I have a variable called stocks and this will be an array of strings. I'll do string slices here. And let's populate this with some stock tickers. And then let's also imagine that the data source where we got this data from is somehow broken, and unfortunately included some empty strings among the otherwise valid values. So for example, we can begin with a random stock like Nvidia Nvda. And then let's imagine that we got an empty string from our bad source of data, maybe some awful database somewhere. Let's populate this array with some other similar values. So I'll do a stock of Apple AAPL then another empty string. And then let's close it off with two more stocks MSFT for Microsoft and Goog for Google. And let's say I want to perform two operations here. The first is I want to filter out the empty strings. I want to just keep the valid stocks. But then I also want to map over that result set. And I want to capitalize all of the stocks and capture those new strings in a vector. So I want to filter. And then I want to map. So here's the first way I can solve this problem. I'll declare a capitalized stocks variable. And I will begin with my stocks. I'm going to call the Iter method to create an iterator of references. And then I'm going to begin with my filter. So I will call the filter method provide the closure and provide my parameter of a stock. And on a string I have a nice method available called is empty. That will give me a true if my string is empty. Now keep in mind we do not want this because if we used this code, we would filter out a subset. But we would filter out these strings right here. The empty strings. I want the inverse I want to keep or filter or select the strings that have text content. So what I can do is use this boolean that I get here. But invert it, flip it around so it is the opposite. So this gives me back a true I really want a false here. And if this gives me back a false, I really want to return a true out of the closure to keep the non-empty stocks. As a reminder, if you have a boolean, you can invert it or flip it. Take the opposite using the exclamation mark. So that will give us what we want. It's going to be an iterator consisting of the filtered strings with content. So now I have my smaller result set. It is on that result set that I want to iterate and perform a transformation on each one. So immediately after I will call the map method provide a closure. Here is my stock. This is going to be the stock with actual content now. And I can take that string and call the two uppercase method that's going to produce a new heap string in all capital letters. And then finally, the last step that remains, of course, is calling the collect method to gather the results up in a vector. The only problem now is we need to provide the annotation. I'm going to go with the turbo operator right here and say that I want a vector of strings. All right. So if I output my capitalized stocks vector in debug format we should see our four stocks in uppercase. And there they are. So we are performing a filtering operation. And we are performing a mapping over that smaller subset immediately after. This is what the filter map method accomplishes with a single method call. So let me show you the alternative way to solve this problem. Once again, I'll redeclare my capitalized stocks variable using variable shadowing. I will take my stocks array. I'm going to call the iter method to create an iterator. And this time around I'm going to invoke just one method which is filter map filter underscore map. It's a single method call. And I'm going to provide it a closure, as always. And within the closure, I'm going to once again receive each element from the iterator, which is again going to be each stock. All right. So now I have to do two things. Again I have to first filter. And then I also have to transform the filtered values into what I want. So here's how the logic works. Unlike filter, the filter map method does not return a boolean. Rather it returns an option enum. Remember, an option enum has two variants, some and none. Some indicates the presence of a value. None indicates the absence of a value. So if we return the some variant, we are indicating that that value should be kept, should be included and vice versa. If we return the none variant, we are indicating that that element should be excluded or omitted. Now, the advantage of the some variant is that we have room for some associated data. And as you might guess, it is within that stored associated data that we can perform our mapping logic, our transformation logic, which will only apply to the data that we want to include in the first place. So let me show you how this is going to work. So here I'm going to begin once again with the same logic. I'm going to check if my stock is empty, but I'm going to wrap it up with an if statement. So I'll say for any given stock that I'm iterating over, I'm going to check if it's empty. If it is empty, I do not want to include it, so I will return the none variant. The non variant indicates do not keep this. Then I'm going to add on my else clause. And if the stock is not empty indicating a valid string with content. In order to tell us that I do want to keep it, but return a consistent return type out of this closure of an option, I will instead designate a some variant. Now remember the some variant has some room for a piece of associated data, and it is here that I can perform my mapping logic the exact same thing we did right here. So assuming we're going to keep this value because we're going to return a some variant only for a non empty stock. I can take my stock and call the two uppercase method on that and accomplish the exact same result as I have above. Here is my filtering logic. Now it's not designated with a boolean like with filter, but rather with an option. And then here is my mapping logic captured within the some variant. If you want to extract this to a couple extra lines of code because there's more complexity, that's totally fine. The key takeaway is we have a little bit more room here to actually perform the transformation. And then the most important thing is actually packaging it up within the some variant. So all that remains afterwards of course is just calling the collect method. And then once again providing the annotation. So I'll do the exact same code as above. Here's my vector of strings. I'm going to copy and paste this line so we can see the comparable vector of strings. And you can see we're going to get the exact same result. This is actually capitalized stocks. So there we go. And there we go. You can see we get the exact same result. And that makes sense because instead of calling filter and map sequentially there is just a complimentary filter map method which accomplishes both in a single step. That's all there is to it. All right. That's all there is to cover in this lesson. So I will see you in the next one.
