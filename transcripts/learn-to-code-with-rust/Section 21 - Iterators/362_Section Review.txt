# Section Review

Congratulations on reaching the end of another course section. As always, let's take a few moments to review everything that we learned. This section focused on iterators. To iterate means to perform an action repeatedly over and over again. Now, in the context of rust, iteration means repeating the same operation on a sequence of items, one item at a time, one element at a time. We are traversing a collection of values one at a time. Now we can perform manual iteration with constructs like the loop keyword and the while keyword, but the idiomatic way, which means the recommended approach is to use the iterator tools built into rust, such as the for loop, such as the collect method and the various adapter methods, etc. the reason for this is simply because it is less prone to error. The logic of iteration remains the exact same each time. We wanted to keep track of where we are in the progression as we progress to the end. But every time we manually write that code with something like a while loop, we introduce the potential for error. So our goal is always to aim for more declarative code rather than imperative code. Declarative code is where we declare our desired outcome and get less into the specifics of how to get there. So something like the for loop abstracts away the complexity of the iteration, thus reducing the chance of error, and thus we should rely on it if we can. So throughout this section, we learned about various traits related to iteration. Starting with the iterator trait. The iterator trait indicates that a type is iterable. That means that we can traverse its elements one at a time. The iterator trait mandates a single method called next, which will Which will return an option enum. The sum variant indicates that there is at least one more element to progress over, and the associated data in that sum variant will be the next iterator element, and in contrast, the none variant indicates that the iterator has been exhausted or consumed. That means there are no more elements left to yield. Now, the beautiful part about this trait is once we implement the next method, we get access to 75 plus helper methods that depend on that next method's implementation. Basically, as soon as rust knows how to traverse over a given iterator, all of the other methods nicely fall into sequence because all of them depend on that same iteration logic of knowing how to access the next value, which is what the next method provides. The iterator trait also requires one associated type called item, and that represents the type of data of each element that this iterator will yield. We also learned about the into iterator trait. This one indicates that a type can be converted into an iterator. In other words, into a type that implements the iterator trait from the previous slide. So, for example, an array and a vector themselves do not implement the iterator trait. Rather, they implement the into iterator trait. So rust can convert them into a separate iterator type, which is a totally distinct separate independent data type. The into iterator trait mandates a method called into iter that will return or produce that iterator. Now, this trait can be particularly confusing because there are two associated types. The item associated type once again represents the type of the iterators yielded elements. So when we convert the current collection type into an iterator, what type of element will that iterator give us on each iteration. That is what item represents. Then the into item associated type within the trait definition actually declares the type of the iterator, the actual type that we're going to be progressing over. So the into item type will be of type iterator because it needs to return that from the into item method. And then the iterator that will be produced will yield values of type item. All right. Pretty complex stuff. Towards the end of the section we also covered the from iterator trait. And this represents the process in reverse. How do we get from an iterator to a collection type. So the from iterator trait indicates that a type can be constructed from an iterator. So for example a vector and a hash set and a string both implement the from iterator trait, which indicate that we can take an iterator and turn them into those respective data types. The from iterator trait mandates an associated function called parameter that will accept an iterator as an argument and return the type that the trait is being implemented for. So, for example, the from iter function on a vector will return a vector. The from iter associated function on a hashset will return a hashset, etc.. Now the from iterator trait requires a generic and very much like the item associated type from the previous slide. This generic represents the type of the iterators yielded elements. What kind of data type will the iterator be giving us on each iteration? On each loop? As a reminder, whenever we call the collect method to convert an iterator into a collection type behind the scenes rust will call that types from iter function to arrive at that data type. All right. So let's get to the for loop which is kind of your basic building block. This is the most common way you're going to iterate in rust. The for loop creates and iterates over an iterator and you can of course pass an iterator inline, but you can also pass a collection that implements the into iterator trait, such as an array directly in line, and rust will automatically create the iterator for you. For the purposes of the for loop, so we use the for keyword. Then we declare an iterator variable that is up to us, and then the in keyword, and then the collection or iterator that we want to traverse and rust will take each yielded element from that iterator and assign it to the iterator variable within the block. We have access to that iterator variable. And we can do whatever we want within the block to it. It's important to note that if you forgot all of your adapter methods and consumer methods, you can basically accomplish whatever operation you want within the body of your for loop with regular rust code. For example, if you forget about the collect method, you can just initialize an empty vector, traverse over another collection, and push your iterator elements into that vector. So you can manually accomplish a lot of things with the for loop, in case you forget how to do them with the method based approach. And that is the beauty of this. It's sort of like the core central building block for performing an iteration, right? The iterator variable becomes unavailable after the iteration completes. And again, within the block, we declare the logic to perform on each yielded element on every single value that the iterator will give you once per loop. So the for loop can run many times, right? It can run for as many elements as there are within the iterator. It's also possible that the iterator is empty, in which case there are no iterations. There are no actual loops to execute. All right, so how do we actually create an iterator? We use a variety of construction methods on our collection types. So first up we learned about the intuitive method that will return an iterator that will yield the elements by value. So the potential danger there is that if the elements in your original collection type, do not implement the copy trait. Ownership will move from the original collection type to the iterator. Which in turn will own the elements themselves and thus if you don't preserve them after the iteration concludes, you're going to lose the original data. So of course, if you simply write for value in collection, that is going to take ownership over the original elements, assuming they do not implement the copy trait, and the equivalent method to create an iterator of that type is calling the into iter method. All right. The second option is the iter method, which also produces an iterator, but one that yields immutable references to the elements. So if you want that, you can either call the iter method on your original collection, such as an array or as a reminder, the shorthand syntax for that is to simply borrow an immutable reference to the original collection. And rust figures. If you want to borrow an immutable reference to the complete collection, you probably want an iterator that's going to yield immutable Mutable references to each successive element. The third and final method is iter mute, which returns an iterator that yields mutable references to the elements. So this is beneficial if you do not want to take ownership away from the original collection, but you do want permission to modify its elements over the course of the iteration. So for that, we can either call the iter mute method or. The syntactical equivalent is borrowing a mutable reference to the original collection. Once again, rust figures that if you want a mutable reference to the original collection type, you probably want to yield a mutable reference to each one of those collections elements during the process of iteration. All right, so let's talk a little bit about yielded elements which are going to get within each iterator loop will depend on your original data type right. So iterators from different types will yield different elements. So an array or a vector iterator will simply give you each one of those elements from the array or the vector one at a time. But something like a hash map is a bit more complex because a hash map consists of pairs of keys and values. So in every iteration, you're actually going to get a tuple consisting of a key and its value. Similarly, with a string, there's actually a variety of different ways by which we can iterate. So for example, if we use the chars method, we're going to get an iterator of the string's characters. And that can be helpful if you want just the visual representation of characters such as your alphabetic letters or your emojis. Conversely, the bytes method is going to give you an iterator of something different the actual numeric bytes, the representations of those characters in memory. So one visual character, like an emoji, can occupy four bytes in memory and can yield four different numbers if you're traversing the iterator, return from bytes, but only give you one character if you are traversing the iterator. Return by chars or characters. Finally, we also covered the lines method, which will give you an iterator of the Of the individual lines of the string. And this can be particularly helpful when, for example, you are reading in a file from the disk where you might have multiple lines and you want to traverse them one at a time. All right, so iterators are lazy. The word lazy here means the exact same thing as it does in the real world. To be lazy means you don't want to do any work until it is absolutely necessary. So what that means in regard to iterators is they will not exhaust themselves or consume themselves. They will not iterate until they are forced to by some kind of method, like collect, or by some kind of operation or construct like a for loop. So if you simply create an iterator and then you do nothing with it, the compiler will actually give you a warning because there's no practical need to create that iterator. Right. So the for loop or the complementary for each method is an example of something that will force the iteration force, the traversal force, the progression over that iterators elements one at a time. As a As a reminder to for each you can pass a closure. That closure parameter will be the equivalent of the iterator variable in a for loop. It will represent each yielded iterator element one at a time. It's just a more method based, closure based approach to declare what you would like to do. Similarly, the collect method is a different type of method. It will force iteration, but for the purposes of producing one final value, which will be some collection type that will aggregate or collect or gather those iterators elements into the collection type of your choosing. So as you saw, we can use collect to actually create a variety of different types like a vector, like a hash set, like a hash map. It's actually one of those interesting features of rust in that the annotation that we provide, either to our variable declaration or using the turbo operator, will actually alter the logic of our program. The type annotation we provide will change how the compiler reasons about the code that it's going to compile and create form from your source code. So pretty interesting stuff. All right. So next up let's talk about consuming methods. These are methods that are going to consume or exhaust the iterator to produce a final value. And the collect method from the previous slide falls into this category. So for example we covered the any method that returns true. If any iterator element satisfies a condition, we pass it a closure that must return a boolean, a true or false. And if any of the iterator elements give you back a true from that closure, that indicates at least one element satisfies that condition, and the any method gives you back a true. Now, the key takeaway is it is a consumer method, right? Or a Exhauster method. It's going to use up the iterator to produce a final value of a boolean. So if you want to get the data back, you need to either create a new iterator or figure out a different way to avoid consuming the iterator. There's also the complementary all method that returns true if every iterator element satisfies the closures condition. We also saw the partition method, which chunks or buckets the iterators elements based on whether they satisfy the closures condition or do not satisfy the closures condition. Once again, this closure that we pass must return a boolean. So the elements that return true are bucketed into one group. The elements that return false from the closure are bucketed into a second group, and then the method returns a tuple consisting of those two groups. Once again, the important thing here is not to remember all of these methods, but just realize the difference between methods that are going to consume or exhaust the iterator versus the adapter methods that we covered that are going to transform the adapter into something else. All right. So here are some more examples of consuming methods. For example, the sum method on a numeric iterator will give you the sum of all of the iterators values. The complementary product method will give you the product, which means multiplying all of the values together. The max and min methods can extract either the largest or the smallest value from within the iterator, but they're going to give you back an option enum. And the reason for that is of course, to account for the possibility of an empty iterator. If you have no values within the iterator, which is possible, and there is no largest or smallest value to extract and the option enum models that idea right? It models the idea of absence versus presence. Similarly, the count method is an interesting one because it gives you back a simple integer that represents the count of the number of elements within the iterator, but that is something that is going to exhaust the iterator, because somewhere behind the scenes, the count method needs to traverse the iterator right one element at a time in order to perform its counting, which is going to use up or consume or exhaust the iterator to give you back a simple numeric value. All right. We also talked about methods that are related to positions. Either we want to extract an element based on its index position within the iterator, or we have some kind of condition that we want to apply, and we want to get the element's index position for the element that satisfies that condition. So for example, the last method returns an option with the last iterator element. The nth method extracts an element from a specific index position within the iterator. Maybe you know it's spot in line within the iterator collection, and that's how you want to target it. The nth method is once again just like last going to return an option enum, because there is the possibility of an empty iterator. The nth back method is the inverse of nth. It's going to give you an iterator element based on index position, but proceeding backwards. So an index of zero means the last element. An index of one indicates the second to last element, and so on. We also saw the position method that one accepts a closure, and that closure must return a boolean. And for the first iterator element that returns true from the closure, rust is going to give you the index position of that element wrapped up in an option enum. The reason it is an option is because it is possible that no iterator element returns true from the closure, and thus there is no index to return, in which case you're going to get the none variant. All right, next up we covered the fold and reduce methods. These two are perhaps two of the most complex ones that you'll deal with. What these two methods do is they exhaust an iterator to build up and produce a single value at the end of the iteration. That value is often called the accumulator because we are accumulating it or building it up over the course of the iteration. So the fold method receives a starting value that is, the starting value of the accumulator, and then it receives a closure as its second argument. And that closure is going to receive the accumulator as its first closure parameter. And then the current yielded iterator element. So the intent here is during each iteration you're typically going to modify the The accumulator, which is going to be that piece of data that persists over the course of the entire iteration using the current element that you have. So it's very important that at the end of the closure, you return the accumulator because the return value from the closure becomes the accumulator closure parameter on the next iteration, and that continues all the way until the end of the iterator. So again, the purpose of the accumulator is to be that shared state that common data that you are persisting or keeping over the course of each iteration, so that you can finish up the iteration and then produce it. And that's how you can build up, for example, a hash map over the course of the iteration. But it's very important that you return the accumulator in each iteration, within each closure, so that it enters the next invocation of the closure, becoming the value of the accumulator parameter. Right. The reduce method is very similar to fold. The difference is it automatically supplies the first iterator element as the starting value of the accumulator, so you only need to give it the closure as an argument. Again, that closure takes two parameters the accumulator and then the current element you're iterating over. It's up to you to adjust the accumulator. So you're probably going to need to use something like the mute keyword to mark it as mutable, to then modify it using the current iterator element, and then send it off to the next iteration with a return value. All right. We also talked about sort and sort by key. We invoke these methods on arrays and vectors. We actually don't even need to create an iterator. We can just call these methods directly on the core collection type. So the sort method mutates an array or vector to store its elements in ascending order. That means for numeric values, we're going to sort them in smallest to largest. And for things like strings, we're going to sort them in alphabetical order. So A to Z, the sort by key method allows us to sort an array of structs, and we get to dictate within the closure that we pass which struct field to field to use as the basis for that sort. We also talked about the Issorted method that's going to return true if the collection is sorted in ascending order. Remember, if you have an array with values sorted in descending order, is sorted will return false because it's looking specifically for an ascending sort. All right. Let's also talk about the reverse method that is going to reverse or invert the order of elements within the collection. Once again you can call this directly on the array or the vector. The way this is advantageous is after you apply your regular ascending sort, you can then call the reverse method to get a descending sort within your collection type. So then you can get your array or vector to store, for example, your numbers in descending order largest to smallest or. The equivalent for strings is reverse alphabetical order, which means from z to a. So again, the reverse method is called directly upon the collection. And it can be confusing because there's also a rev method that is available on an iterator, and the rev method accomplishes the exact same result. It takes an iterator. It's an adapter that transforms it into a new iterator that yields the elements in reverse or opposite order. The two methods are named differently, which can be slightly confusing, but again, reverse is called on the core collection type, while rev is an adapter method on an iterator. So at this point, we've reviewed a lot of methods that consume or exhaust the iterator. The complement to that is adapter methods and adapter method is one that transforms an iterator into another iterator by performing some kind of logical operation. It's important to note that transformation, that performance or action is still lazy, so nothing will be computed until the final iterator that you produce is going to be exhausted or consumed with something like a for loop or a collect method. So for example, you can call methods like filter or map as many times as you like, but if you never exhaust them, the compiler will give you a warning and tell you you haven't actually done anything yet, because the creation of that final iterator and its consumption doesn't happen until you actually use some kind of method or operation that forces the start and progression of that iteration. We can, of course, chain multiple adapter methods in sequence to create a pipeline of sequential transformations to apply. That is the power of these methods. We can layer them on to have a nice declarative pipeline of transformations to apply to each iterator over and over again, until we get to the desired iterator and then consume it. All right. So perhaps the two most common adapter methods are map and filter. They're both going to accept closures. The map method performs an operation on each iterator element to produce an iterator of new values. So the word map means to connect. And that's exactly what it does here. It starts with your starter iterator. It yields each element and it maps or connects that to another element. So the iterator that's going to be produced is going to be of the same length as the original iterator. It's just going to represent a transformation of each value into something else. The filter method selects for iterator elements that satisfy a predicate condition. So we give it a closure that returns a boolean, and it's going to keep the elements that return true from that closure. Once again, the filter doesn't take place when you invoke the filter method. We're just kind of setting up the rules that are going to run in sequence before the actual consumption runs, and it's at that point that the filtering actually takes place. And rust sort of waits until the absolute last moment that it can to do that iteration. And that also allows it to optimize how it's going to write the final compiler code, knowing everything that we intend to do, and it's going to compile it for the most efficient way to solve that problem. We also saw the filter map method, which allows you to perform both a filter and a map in sequence. The way that works is our closure must return an option enum. If you want to keep an element, you return the sum variant and you can specify the mapped data as the associated data within that some variant. So you get to keep it and transform it within the closure, all in one single method call or one closure. And conversely, if you want to discard or exclude an element, you can return a none variant instead. So you can call filter, then map. Or you can just combine them both into filter map. You're going to see both options in the real world. All right so here are some more common adapter methods. We talked about. Enumerate that is going to create an iterator that yields a tuple with the index position which is the position inline of that element along with the actual element itself. Once again this is an adapter. It's a transformer. It takes an iterator. It gives you back a new iterator. It's also important to note that that tuple is going to store the index and the element in that order. That can be a little bit confusing because you're starting with an iterator of elements. So the element is sort of the first value that comes to mind, but it's actually going to be the second element within the tuple that the iterator is going to yield once it's been created from the enumerate method. The zip method merges two iterators together and yields a tuple with the elements from each iterator based on their shared index position. So this can be really helpful when, for example, you have your data collected in two different types, and there's significance in the sense that the elements are related to one another based on their shared index position. Like maybe the elements at index zero and one and two within this array connect to the elements at index zero, one, and two within this second array. But they happen to be separated. So zip allows you to zip them or combine them together and get access to each element based on the shared index position within each original collection type. We also talked about copied and clone that returns an iterator that can, for example, create a copy of each element, but it's important that each element must implement the copy trait in order for this to be possible. And similarly, we have things like the clone method, which returns an iterator that will call clone on every element, but every element within the original iterator must implement the clone trait. We also talked a little bit about efficiency in regards to the order in which you apply these methods. For example, if you intend to both filter and then clone the results, it's better to perform the logic in that order, because then you're going to allow yourself to filter to reduce the original collection into a smaller subset, and then only create clones or copies of those filtered values, which would hopefully be a smaller total number of values to clone. If instead you flip around the order of the cloned and filter methods to have cloned come first. Well, then unfortunately you're going to create clones of every original value to then filter it so simply. The order of these adapter methods can influence the efficiency of your program. All right. Even more adapter methods. We talked about the flat method, which transforms an iterator of collections into a single iterator of flatten values. Basically, this is helpful if you have something like an array of arrays or a vector of vectors. You have the data collected in multiple composite types and you want to flatten it. I always like to think of a hand coming down and squishing these types, so that all of the elements are collected in a single sequence, in one line, in one one dimensional list. And that's what flatten is going to do for you. So each yielded iterator element will be one single value like one number, for example. The skip method bypasses a specified number of elements from the iterator, so it literally skips something if you don't care about a value. Let's say you don't care about the first two values in an iterator. You can call skip with an argument of two to very elegantly create a new iterator that's just going to completely ignore them and give you back a new iterator that starts two elements in compared to the original iterator. The take method limits the iterator by selecting a number of elements from its start. So maybe you have an iterator of ten elements, but you only care about iterating over the first three. So you can call the take method provide three. And now you've shortened or limited the original iterator. Once again, these are all adapters because they're transforming one iterator into another. We also covered the step by method that produces an iterator. That's going to yield values based on specified intervals or steps or sequences. So you can think of this as jumping across multiple index positions all at once. If, for example, you want to progress over an iterator every two elements at a time. In other words, skipping an element for every iteration you can. For example, call step by with an argument of two. To accomplish that result, and as we mentioned earlier, there is the rev method which reverses the order of order of elements within the iterator. And all of these aforementioned methods are adapters. They do not exhaust or consume the iterator. They produce a different type of iterator from the starting one. So we also covered collecting command line arguments. In order to do that, we want to bring in the env submodule from the standard library, and that is short for environment. The environment is basically a module that helps you with any supplementary data around our rust program, such as command line arguments such as the name of the directory we are in, or the name of the file that we are within. It's like the supplementary metadata that we get access to from the outside environment, from the outside world, from the computer in which our rust program is executing. So the environment submodule has an args function, which is short for arguments. And that just means inputs. And it's going to give you back an arg struct that stores the command line arguments that arg struct is going to implement the iterator trait. So you so you can do things like progress over it with a for loop or call adapter methods on it. Remember, the first yielded element from the args iterator will be the executable's file's name. So the actual file that the compiler places within the target debug or target release directory. That file's name is going to be the first yielded element. If you want to use it, you can, but if you want to bypass it, you can of course use an adapter like Skip with an argument of one to simply get rid of it. Similarly, you can call the take method to limit the iterator to the first n elements. For example, if you want to ask the user for only three elements, but the user gives you ten command line arguments, you can call take with an argument of three. To limit the scope of the elements that you receive. You can basically take any size collection based on any number of command line arguments you receive from the user, and limit it to however many you want to take from that starting iterator. So these are again applying the adapter methods we talked about earlier, but simply to a preconfigured iterator that is made available as the return value of the args function. All right. Now when we actually have our program and we run it with something like Cargo Run, we want to add our command line arguments after those double dashes. And the benefit of that syntax is it ensures that your command line arguments flow into your rust program rather than the cargo command. Right. So we don't want in this example, the three command line arguments of kings and queens to apply to cargo run, because that has its own command line arguments that it cares about like release. We want our command line arguments to flow into our program, and the double dashes allows that to happen. Keep in mind, if you forget the double dashes, your program may still work, but there is always that possibility of conflict. If, for example, you have some kind of command line argument called release. There is a possibility that it's going to be misinterpreted because it's going to be applied to the Original commands, so just play it safe and add your double dashes before you begin your command line arguments. Then you can separate your command line arguments by placing a space between each. And finally, those command line arguments are going to arrive in your program as strings. So you have to be careful because you have to convert them into the appropriate data type. Rust is not automatically going to parse an integer or a boolean for you, just because you provide a value like five or true as your command line argument. So we talked about that parse method. We saw as well that there is the possibility that we cannot parse a valid value. Right. If we want to ask the user for an integer to parse from, we're going to get it as a string. So if the user gives us a string of nonsense, there is no integer to parse. And that is why that method returns a result enum. And once again we have to account for that possibility. You can either terminate the program. You can ask the user again. You can try to convert to a reasonable default or use a method like unwrap, or to come back to a reasonable default. There's many different ways. It all depends on how you want to deal with, with the situation of the user giving you invalid data from the outside role. Finally, we talked about how we can use rust to read a directory within the FS or file system module. We have a reader function that is short for read directory. That's going to give you a result where the okay variant is going to store a reader struct. The reader struct similarly implements the iterator trait, which means we can progress over it with something like a for loop. Now, what's interesting here is each yielded element from the reader struct is actually going to be a result. Storing a dirt entry. In the okay variant, we talked about the possibility of all kinds of interesting real world scenarios, like your perhaps inability, permission wise, to be able to read a given directory entry, or maybe the directory entry disappearing from the file system in the time that your program is running. So rust is always going to play it safe and give you back a result in real time because it Because it accounts for the possibility of failure. So you have to handle that situation gracefully. So if you have the okay variant you can extract that dir entry struct. And that represents a directory entry which again is either a file or a folder. Once you have that dirt entry struct it has a path method which will give you the location or path of the actual entity, either the file or the folder. Finally, we learned about the metadata function also within the fs or file system module. That is going to give you a result. Again, in the okay variant it's going to wrap that metadata struct. And that metadata struct includes various methods like is file and is dir that can return booleans to tell you whether you are iterating over a file currently or a directory currently. So depending on which entry you have and what you want to do with it, you can provide an if statement and take different conditional paths based on what you want to do. There's many different multiple idiomatic ways to ways to deal with these results. And that's simply because all of these. Previously mentioned operations can end in failure, right? And result gracefully handles that. And there's many different ways that we can deal with those results. We can use the match keyword the if let construct methods like unwrap or or unwrap or else or the try operator, which is the question mark. And that is a common favored solution, although you're going to see all of these in real world code. And as a quick reminder, the try operator, the question mark is either going to unwrap the okay variants associated data if we have an okay variant, or if you have the error variant error, it's instead going to terminate the function, return early and return that result instead. The error variant more specifically is going to propagate that error variant up as the return value of the function. And as we saw, we can even designate the main function to return a result. And if we propagate an error out of there, it's simply going to be printed to your console, to your terminal, Whenever your rust program runs, that's all there is to cover in this lesson. This section covered quite a lot, but iterators represent one of Rust's most powerful features. Once again, if you know a method that allows you to take a more idiomatic approach, a more declarative approach where you have to wire up less logic and allow rust to do most of the heavy lifting, that reduces your chance of error and is usually going to be the recommended way to solve a problem. But if you don't know the particular method, using a basic vanilla for loop and constructing the logic yourself is also valid. In either scenario, the compiler is going to do its best to produce a final efficient executable anyway to do the best you can. And as you grow your knowledge, you're going to learn even more of these methods that you can apply to create iterators, to adapt, iterators to consume them, and so on, and write more efficient rust code. All right. That's all there is to cover in this lesson and in this very long section. And with that I will see you in the next one.
