# Manual Iteration

Welcome to the next section of the course, which is focused on iteration. The word iterate means to perform repeatedly. In programming terms, iteration means repeating the same operation on a sequence of items, one item at a time. Iteration in rust enables us to progress over one value at a time in collection types such as strings, vectors, or hashmaps. For example, we can iterate over a string one character at a time to count how many times each character occurs. Or we can iterate over a vector, one element at a time, to select only the elements that satisfy some criteria. So we're going to be exploring Rust's powerful iteration features later in the section, but I want to focus this lesson on some of the manual ways that we can iterate. So we can see some of their disadvantages and the advantages that Rust's methods offer us over those approaches. So the code in this lesson will start off with being more imperative. Imperative simply means that the instructions are more explicitly written out and specific, and thus they are more prone to error because they involve a lot of our own work. And as we progress to the final example in this lesson, the iteration code we're going to see is going to become more declarative. And what that word means in programming terms is we move in a direction where, rather than giving explicit instructions on how to do something, we simply describe the result we want. And rust takes care of more of the logic to get us there. And that declarative approach is less prone to error. It's going to make more sense as we see some of these examples. So as we write out the following three iteration options, I want you to think about how each solution improves upon the previous one, particularly in the reduction of the potential for error. So let's say we have this numbers vector with six values and say I want to print out every value in the vector one at a time. So it's a very simple example of a problem that intrinsically has to involve iteration. We have to progress over the vector one element at a time. So our first option for manual iteration is to use the loop keyword. And we saw this keyword way back when in the control flow section. The loop keyword accepts a block that it will execute repeatedly until we force it to terminate with the break keyword. So the loop keyword gives us the ability to repeat, which is sort of the foundation we need for iteration to occur over a collection type, like a vector. So here's what that code could look like. So I'll declare a mutable variable called current index. And this will keep track of what index I'm currently traversing over. I'm going to start it at zero and then increment it to 1 to 2. As I progress over each one of these respective index positions in the vector. And then I will also declare a variable called final index. And in order to calculate that, I can take my vector of numbers, call the Len method. And if I subtract one from that I'm going to get the final index. Because remember, the index starts counting from zero while the length starts counting from one. So here I can begin a loop with the loop keyword. I'm going to perform this block of actions over and over again until I tell Russ to stop. And so it's very important that I avoid an infinite loop by adding some kind of condition here. So I'll do that up front. I'll say if our current index is greater than our final index, when we get to that point on some loop, on some iteration, that's what I'm going to use the break keyword to terminate the loop. So it's very important that at some point I also take my current index and increment it by one, because if it never changes, it's always going to stay zero. That's never going to be greater than the final index, which is going to be a total of five. And so zero is never going to be greater than five. We're never going to hit break and we're going to keep looping unless I add this line right here and get to the point where current index will at some point get to a number that will be greater than final index. So now with that I can simply print line, inject some dynamic content and take my numbers vector and then reach into the current index that I have starting at zero. But then it's going to be one inch the next loop, two on the next loop, three on the next loop, and so on. I'm going to index into that respective index position in the vector of numbers. Print it out, and we're going to get all of our values right here. All right. So this is a fine solution in the sense that it gets the problem solved. But notice that it is prone to error right. We just talked about one potential problem which is an infinite loop. But we can do something wrong like calculate the final index or accidentally have a typo and start from one. There's more developer code here that we wrote, so there's always more potential for error and something going wrong. So let me give you an example of an alternate solution that starts moving us in a better direction. All right. And then the third example will be the best possible option. But let's get there sequentially. So we saw earlier that we also have the wild keyword available. And that's a nice replacement for the loop keyword. Because a while loop automatically handles the termination of the loop, we place a condition after the while keyword that evaluates to a boolean, and the while loop will repeat its block until that condition is no longer met. Until that Boolean evaluates to false. So what's nice immediately about a while loop solution is that we're worrying about one less detail, which is forcing the manual termination. So something like that would look like this I'm going to start with my while loop right here. I'm going to say while my current index is less than or equal to my final index. Because remember I have to invert my logic a little bit. I want this loop to continue running while this condition keeps evaluating to true. And then I can basically just cut and paste this logic right here and remove all of my former code right here. So the same principle occurs. I'm going to print out the vector number at index zero, then increment current index to one, then to two, then to three, and so on. And at the start of each loop, rest is going to check whether current index has become greater than final index. It's really checking for less than, but it's waiting until the moment that it is greater, at which point it will finally terminate the while loop automatically. And in addition to writing less lines of code and getting the working solution, you'll notice there's a little bit less danger here because we no longer have to worry about the manual termination. There's still the potential of an infinite loop here. For example, I can just forget to write this line and cause an infinite loop, but it's already starting to move us in a better direction. And the same principles are going to apply with improving upon this solution. So even with something like this, there's an infinite loop we can accidentally access a nonexistent index. We can mess up our starting index and final index declarations. And another important detail to note as well is that this active iteration, represented by the while loop and my variable declarations, is the exact same each time. Every developer who's ever needed to iterate over a vector would write the exact same code using a while loop. It would be really great if we could just abstract away, or in other words, handle this iteration logic implicitly because the only variation, the only difference between me and 1000 other developers, is really the logic of what we want to do with each element in the vector. It is this stuff that is different. The logic of the iteration over a vector's elements one at a time, doesn't vary between 1000 developers who need to iterate over their vector. But yet we have to keep rewriting these lines precisely because the solution is more imperative. So with that in mind, let's take a look at the final approach for iteration. And this is the recommended idiomatic solution in rust. Precisely because a lot of the complexity is handled for you, and almost magically in the sense that it is implicit and hidden away. So this is an example of what we would call a more declarative solution, because we're not really giving a step by step detailed instructions. Rather, we're relying on a feature of rust that has already been built that will automate or handle some of the complexity of the iteration for us. And for this, we use something called a for loop. It's going to amaze you how simple and clean and elegant this is. So I'm going to remove all of our code right here. And I'm going to begin with a new keyword called for. And we might have explored this before perhaps with range iteration. But let's review it. So we're going to write the for keyword. Then we're going to choose what we call an iterator variable, which is going to represent each element one at a time in our numbers vector or whatever collection type we are iterating over one at a time. So we have to give a name that's going to correspond to each one of these elements, one at a time. This name is up to us. We can call it value number num. It's just some variable that we're going to have access to that's going to represent these values sequentially, one at a time. Since we are iterating over a plural collection of numbers, I always like to do the singular form. So in this case number. But again this name is up to you. So for every number then I'm going to use the in keyword and a space. And then I'm going to write the collection type for us that is numbers. And then I'm going to put a pair of curly braces a block. So this reads exactly as it operates for every number in numbers. What would we like to do. And we specify that within the curly brace. So right here knowing that number is going to be four then eight then 15, I can literally just print it out like so and run that. And we're going to get the exact same program right. So this is the for loop. And it's kind of the rust standard for how you iterate over any collection type. But the most important thing to notice is again, not just the simplicity, but also the reduction in the chance of error. We no longer have to worry about the index. We no longer have to worry about an infinite loop somewhere. Some rust developer has written the logic to have this for keyword, and the capacity to traverse or iterate over each element in a collection type. In this case it's a vector, but the for loop will support a variety of different types, and they've handled or abstracted away the complexity of that iteration and handled it just one time in one place, so that a thousand Russ developers can reuse that code and not have to worry about writing that code by themselves from scratch, thus increasing the capacity for error, and also just the duplication of having to write that code over and over again. So as we've progressed from the start example to this one, I hope you see the improvement that we've had as a result of relying on these kinds of constructs. And it's going to be a similar theme as we explore the various iterators and methods that rust gives us. These are tools like types, as well as methods that enable us to perform these iterations, these traversals over composite types in ways that are built for efficiency and reduced error. We don't have to repeat logic that is going to be common, such that the rust development team has already solved that problem for us. So we're going to be seeing the for loop a lot throughout the section. But this is a nice introduction to the basics of iteration. Once again the progression over a collection type one element or one value at a time.
