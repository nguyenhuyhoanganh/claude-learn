# The iter_mut Method

So as always, there are three ways to deal with a value in rust. We can take ownership away from the original owner. We can borrow an immutable reference, or we can borrow a mutable reference, which means the reference has permission to modify the value without taking ownership. And there is a complementary iteration method for all three of those concepts. We've seen two of them so far. The interpreter method takes ownership of the original collection's data. For example, when we call into iter on a vector, the vector relinquishes control of its elements and its ownership to the newly created iterator. The iterator becomes responsible for the allocating the memory once it is exhausted and done iterating. The complementary iter method borrows the collection and returns an iterator that yields immutable references to the elements, without taking ownership of the of the original collection or its elements. The final method is called iter mute, which creates an iterator with a mutable reference to each element. So here I've declared a flavors array just for a little bit of variety, and it's storing three heap strings. So let's declare an iterator variable. And I'm going to take my flavors array and call iter mute. This time I want you to notice first up that this method returns a totally different type called an iter mute. If I click into here, it's going to be an iter mute struct, and it's found within a totally different part of the rust standard library. Now this is still a type that implements the iterator trait, indicating that it can be iterated over, but it's a totally different struct from what we get with something like the into iter method. Right? So just more complexity behind the scenes. I also want you to notice that rust will prohibit this code from compiling, unless we mark the original owner as mutable. Remember, the original owner must be made aware that its contents that it owns are going to be subject to change. So, for example, if we iterate over this iterator and concatenate some content to the end of each string, each one of those strings is owned still by the flavors array, and thus the flavors owner needs to know that that's going to be changed, and thus it needs to be declared as mutable. So this code will not compile until I add the mute keyword right here. All right. So with that, we are once again ready to begin our for loop. So right here for each flavor in and I'll provide iterator right here. And you guessed it the type of flavor is once again going to be totally different. It's now not going to be the string as we would get with the into iter method. It's not going to be a ref string. As we would get with the iter method, it's going to be a mutable reference to a string. So now we have permission in the body of this loop in the body of this iteration to modify each successive flavor. Each yielded string that we are getting a mutable reference to through this iterator. So, for example, I can take my mutable reference represented by flavor. We know that we can call a method on the reference just as if it were the original type, because rust will dereference. So here I can concatenate some content like ice cream. And because we are borrowing mutable references, the flavors array never loses ownership either. So just like with the iter method from the previous lesson, I can show you that flavors is still totally valid right here. We do not need this. We can see right here. There is our original flavors array, but each string has been changed and modified to have ice cream. But the iterator variable itself is exhausted and also invalidated. So it is no longer available. All right. Flavors is fine, but iterator is not. All right. Now, of course, we do not need to assign the iterator to a separate variable. So if we wanted to consolidate this a little bit, we can absolutely call the iter mute method inline. So here is my flavors array. And I'm going to call inner mute. And we're going to get the exact same result. Flavor is going to be a mutable reference to each string. And finally once again there is a syntactic shortcut. If we do not want to create this iterator manually by invoking the iter mute method, we can instead borrow a mutable reference to the flavors array itself. When rust sees a mutable borrow to the original collection type to the original source of data, it will automatically call iter mute behind the scenes in a for loop. It assumes that we want permission to mutate each individual element in the original collection through a mutable reference, since we are borrowing the original collection as a mutable reference. So once again, the syntax is what you might expect. I'm going to remove items right here and provide not just my borrow operator, but my borrow operator with mute indicating a mutable borrow of flavors. And you can see behind the scenes it's going to call the inner mute method. And thus each flavor that we iterate over in the iterator is going to be a mutable reference to the string. Once again, the flavors array is going to be totally valid after the iterator is exhausted. All right, now here's an important caveat. And it's important because it might lead to some slightly different syntax with certain data types. With certain data types, we still need to use the dereference operator, the asterisk to access the value at the memory address that the mutable reference points to if we want to modify it. And that's because there may not always be an explicit method available, like push string that will force rust to dereference. So sometimes we have to be a little bit more literal. With telling rust we have this mutable reference. Please go to the original address and please change the value there. So for example, let me give you a different type of array. Let me declare a array called school grades. And let's populate it with four integers. Let's do 85, 90, 78 and 92. And let's say what I want to do is iterate over this array. And I want to subtract two from every grade. Right. Well, first things first I want to borrow mutable references to each individual integer. So I'm going to mark my array as mutable. And then down below with the exact same syntax as we have above, I'm going to say for each grade in. And I'm not just going to provide school grades by itself, because that's going to iterate over integers. And I'm not just going to do the borrow operator, because that's going to be an immutable reference to each integer. I'm going to do ampersand mute. So that grade represents a mutable reference to each i32. For each one of these mutable references, we want to access that piece of data and subtract two from the integer. The problem is this syntax right here. Let's say minus equals two on grade is not going to work because at this point, with an operation like subtraction and overwriting, the compiler is not smart enough to dereference. So it actually thinks we're trying to subtract from a reference. Right. It thinks that we're trying to apply a mathematical operation to a mutable reference type, which is invalid. Right? We need to tell rust we're not trying to subtract two from the reference. We're trying to subtract two from the integer value that the reference points to the actual memory address that it refers to. So here the syntax would look like this. We would provide our dereference operator in front of grade and save. And that does it. So what this does is it tells us to follow the mutable reference to the original address where the grade is being stored, subtract two from it, and then overwrite the value at that memory address. So now we're going to decrease each one of these values by two as each one is yielded throughout the iteration in the form of a mutable reference. So here I can print out my school grades array after the iterator completes, like so. Once again, there's no name assigned to the iterator, but it is exhausted and as good as gone. But here we can see we have subtracted two from every array element. So that just goes to show sometimes a method will beautifully dereference for you. Sometimes you'll need to be more explicit by providing the asterisk operator the dereference operator. But the principle is still the same. We are iterating over mutable references. So to summarize, there are three syntax options available to create an iterator. In the for loop, each one is going to invoke a different method behind the scenes, and it matches the three different ways that we can deal with values in rust. So once again, if we do for each value in a collection, that will take ownership over the collection and each element in the collection. So this is this is equivalent to calling the into iter method. That is our first option which is ownership. Let me in fact write that out right here. Then our second option is immutable references. And for that you can write out for value in and then borrow an immutable reference to the collection. And this code is equivalent to taking the original collection and calling the iter method instead. And then you're going to be iterating over immutable references. And finally, the last option for mutable references is going to be for each value in. And you're going to borrow a mutable reference to the entire collection. That is a shortcut. Or alternatively, you can keep the original collection, but call the iter mute method on it to once again produce an iterator that is going to yield one at a time mutable references until it is exhausted. Those are our three options, and they, of course, completely mirror the three different ways that we can work with data in rust.
