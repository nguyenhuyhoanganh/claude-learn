# Project: Wordle

In this lesson, we'll practice the iterator concepts that we've learned throughout the section by building the popular word game Wordle. Wordle was developed by software engineer Josh Wardle, who first published it in October 2021 and then sold it to The New York Times for an undisclosed amount estimated to be over $1 million. So we're going to be able to write out the basic code for this game in probably less than half an hour, which just goes to show you how far a little bit of code can take you. So here I am on the official Wordle website, and in case you're unfamiliar with this game, it's basically a word guessing game. The player has six chances to guess a five letter word, and with every attempt that they make, the game informs them which characters they got right. It's very similar to hangman, which we all used to play back in elementary school. So here I'm going to click this play button and it's going to show us a rundown of the rules. There's only a few of them. So first things first. Let's assume we have that target word that the player is trying to guess. They're going to have six guesses after every entry the user provides. We're going to compare the characters in their word to the characters in the target word by their shared positions. If a character is in the right position, if it has been guessed correctly, we're going to shade that character in green. So let's say we have some target word like water, and the player guesses wordy. In that case, the W is in the right spot, so it's going to be shaded green. That means it's the right letter. If a character is not correct at its position, but exists somewhere else in the target word, it's instead going to be shaded in yellow. So in this example, the letter I is not found in the second position of the target word, but it is found somewhere in the target word. So we're So we're going to shade it in yellow. And finally, if a given character is not found anywhere, it's just going to be written out regularly or shaded in black. So once again green if it absolutely matches at the right position ie if it doesn't match but is somewhere within the target word and black. Otherwise the user has six guesses. So obviously the intent is for them to try to continue guessing words based on what they've gotten right so far. And then they have six guesses to get to the target word. All right. So let's go ahead and transition to VS code and build Wordle in Rust. So first things first. In order to add some color to our console output I'm going to add an external library crate to our project called colored. The colored crate offers a really simple API to change the printed color, or the background of a piece of text that we output. For example, we can call a basic method like blue on a string to output the string in a blue color. We can also do things like bold the text, italicize it, strike through it, alter the background color, etc. so we configure our project dependencies in the cargo.toml file, which is found at the top level of our cargo project. I'm going to open up that. And as a reminder here under the dependencies section we write our dependencies. So these are other library crates somewhere out in the world. And our project will download and pull them in from Crates.io, which is the online repository of rust crates. And then it's going to compile that source code with our own source code to produce the final executable. So the name of our crate that we want is colored. And then we'll provide a space, an equal sign another space. And then as a string I'm going to declare the specific version number that I want of this crate. And we want 3.0.0. And that should do it. So make sure to save the file. And then we can close it and transition back to main. So inside main, we only need to do one thing to enable the full use of this crate. At the very top I'm going to utilize my use keyword. I'm going to reach into that colored library crate that I just added as a dependency, and I'm going to pull in a colorized trait. This is a trait. So this trait will automatically update all strings in rust, both heap strings and string slices to support a whole bunch of methods related to color. That's all there is to it. It's that easy to set up. I can in fact command or control, click into here to show you that colorize is a trait. And you can see here some of the methods that it defines for font colors. There's literally a black function and a red function and a green function. These are all going to be methods living on our strings. All right. That should do it. All right. So now in the real world, Wordle picks the five letter target word from a collection of probably thousands of possibilities. But to keep things simple, let's just pick a hard coded target word for now. It's a lot easier to develop the game with that fixed value, and then later on, we can switch it out for a random word from a collection if we want to. So we need a good five letter target word. And since we love rust with all of our hearts, I'm going to choose the word trait. So this will be the word that the user is trying to guess. So here I'll declare my variable called word. And I will set it equal to trait. All right. So we're going to need to ask the user six times for an entry unless they win by guessing the word earlier than that. And then we need to compare every character from the user's entry with every character in this target word of trait. All right. So let's begin by wiring up what we need to collect the user's actual input from the terminal from the command line. So first things first. In here I will add another use keyword and reach into my standard library and access my I o input output submodule. And that will allow me to get access to its standard instruct, which can collect user input. So here I'll declare an input variable, and as a reminder I can reach into I o and invoke the standard in function to get back a standard instruct, and that supports a read line method to collect input. Now we want the user to have six total rounds. So that represents a loop right? It represents an iteration. We want to ask the user six times in sequence potentially to provide a word and then six times in sequence. We also want to compare the characters of their entry with the characters of trait. So you'll notice the repetition and that is a sign of iteration. So how can we write some kind of logic that is going to run six times? Well, we can do something like a while loop and manually keep track of an iterator variable, right? But again that is more prone to error. Is there a more declarative approach that we can utilize? And the answer is yes. We can use a for loop and use something like a range to iterate a total of six times. So imagine I have a range like this. Let's say I start my range at one and I go up to and including six. This is a range that represents the interval or sequence of integers from 1 to 6. And if I iterate over this range which implements the iterator trait, it's going to yield each number in sequence one, 23456. Now we don't care about that number specifically, but what it will create magically is a loop that runs six times, right? Simply because we are iterating over an iterator that has a length of six that yields six elements. So right here I can literally write something like for number in this range. And now I've declared a magical iteration that's going to run six times. Now as it turns out, this actual number representing these values is not something we're going to need unless we, for example, want to announce the loop number to the user. But since we're not going to need it, we don't even need an iterator variable. It's just a UST a requirement of the for loop construct to force the iteration six times. So what I can do is replace my number iterator variable with an underscore to tell rust. We don't really care about that name, it just exists for necessity. And now we have a loop that's going to run six times. Next up let's collect the user's input. So within the loop I'll declare a mutable string called user input. And I will initialize it to be an empty heap string. Now technically speaking, we could initialize this string outside of the for loop. And at the start of each loop, we can clear it and gather the user's new entry into the same string. But just to keep things simple, I'm going to declare the string each time anew. Every time the iteration starts again. And that just allows a very easy cleanup. That also allows us to not have to remember to clear the string each time. All right. So as a reminder, our standard construct represented by the input variable has a read line method. It It expects a mutable reference to a string to which it's going to write the user's entry from the command line. So right here I'm going to provide a mutable reference to user input above. That's going to return a result. Right now I'm not going to worry too much about the error handling, so I'm just going to call the expect method on that result, which is very similar to unwrap. And I'm going to provide a custom error message and say failed to provide input. We're not going to worry too much about error handling because we just want to build the core game functionality in this lesson. All right. So now we're going to collect the user's entry. We have that collected in our user input heap string. So now of course I have to check whether each character in the user entry matches up with each character in trade. So let's begin with our target word which is trait. I want an iterator of its characters. So for that I can call the chars or characters method that is going to give me an iterator that is going to yield t, r a, etc. but I want to merge it with the characters from the user input string, because that will allow me to compare the characters by their shared index position. That's a pretty good hint. So what we're looking for here is actually the zip adapter method. As you might recall, we invoke the zip method on one iterator. We provide another data source or another iterator, and then it combines or merges the two iterators together and yields a tuple consisting of the elements based on their shared index positions. And that's going to accomplish exactly what we want. It's going to give us the first character in our target word, and then the first character in the user's word, and then repeat the process for every combo, for every pairing based on shared index position. So here I can take my user input, and I can create its own chars iterator and zip the two together. Now to play it safe, just in case the user provides more than five characters. What I can do here is very elegantly chain on the take method and limit my iterator to five characters. So if the user gives me a string of ten characters, I'm just going to ignore everything after the first five by just taking the first five, right? This is also going to solve the problem of the newline character that's going to be collected at the end of the read line method. Remember, when the user presses the enter key, it is attaching that newline character to the slash n to the output. Usually we would handle that with something like the trim method. But the beautiful part about take is if the user provides five characters and then the line break take will automatically eliminate magically that newline character because it's only going to target the first five characters anyway. Now, the one problem that this does not solve is the potential of the user giving us an empty space to start and then their word. And so just to play it super safe, I am going to still call trim on the user input right here before I create my characters iterator and then take the take the first five characters of that, and then zip that up with the characters from the word. All right, so now this is my target iterator. I want to iterate over that. We are already within iteration for this range. We're going to iterate here over this new iterator. So right here I'm going to say for and provide my tuple because I'm going to get two elements. Those two elements are going to be the word character because this is the first thing in line. This is the iterator that we started with. So it is going to be yielding the word character as the very first element within the tuple. And then because we zipped up the user input characters, we're also going to be receiving the user character as well. That matches the index position of the word character. So beautifully done, very elegantly and declaratively designed. All right. So now we have the word character and the user character at each shared index position. So obviously the first thing we should do is compare them to see if they are equal. And if they are we want to print out the content in green. So right here I'm going to add an if statement, and I'm going to compare the equality of the words character with the user's character. And if the two match, I want to print the character in green right here. I'm going to do the print macro, and I'm going to provide room for some dynamic content and then a vertical pipe. And this is just going to give us that nice tactile feel when we output the content. And for this dynamic piece of content I'm going to utilize the format macro, which as a reminder, ends in an exclamation mark and also creates a heap allocated string. The advantage here is we can use all of our familiar interpolation syntax, but get a heap string. So what I'm going to do here is interpolate the value of the user character. And then just to add a little bit more style, I'm going to add a space before and after the character, because we're going to shade this whole thing in green and that's going to give it that nice tile effect. So format is going to give us a heap string. That's what it's going to return. And here after that macro returns and I get back a heap string, I'm going to call a method that we get from colorize called on green and invoke that. And that is going to actually print out that content in green on a green background. That's where the name comes from. We're still going to interpolate some dynamic content in display format, but this is going to be a green string or a string on a green background, and it's going to have that nice look and feel within the terminal. All right. So that covers the first scenario. If we have an exact match at the exact same index position, the next possibility to cover is if it is not a match, but the user's current character is found somewhere else within the word. So I'm going to add an ElseIf and I'm going to take my target word, which is trait. And a string supports a contains method, which is going to give us back a boolean. If the string contains some substring, some smaller text within it. So I'm going to check whether my target word of trait includes the current user's character. We already checked for an exact match, which would take priority. But in case we don't have an exact match, we're going to fall back to the elsif to check for the second condition. And if the user character exists somewhere else within the target word, not at the current index, but somewhere else. We want to do the exact same thing. We want to print out the user's character that they entered, but this time around we want to print it on a yellow background. So here I'm just going to change this method call to on yellow. Once again providing some spacing before and after. It's going to give it a nice tile effect. And then this vertical pipe is also going to split up our characters. You're going to see how this looks in just a moment. And we're going to add one more else clause here for the third and final scenario, which is there is no match and the current character from the user's word doesn't exist anywhere within the target string. So I can once again copy and paste the previous content, but this time around, I'm just going to render it on a black background by calling the on black method. And once again this is nested within our for loop. So this logic is going to run six times. So there's a couple more things I need to do. The first thing is as I was developing this, I ran into a bug where the content would not actually print out in the right color, and I didn't know what the cause of it was. And I looked up the issue on Stack Overflow. And basically the issue said, you need to kind of force the terminal to refresh after this operation so that it can render the colors correctly. And I literally just copied and pasted that code, and that gave me the result I wanted. So to accomplish that, we're going to rely on another function within the I o input output submodule. And in here I can invoke the standard out function. That's going to give me a standard out struct. That struct supports a method called flush which returns a result. And I'm going to just call unwrap on it here. Again, not the safest solution, but again, we're trying to build a world here, so I'm not going to be super concerned with a couple of these less safe approaches. You are welcome, of course, to add something like a match if you want to play it a little safer. All right. So that should do it. Make sure to add it before the conclusion of this block belonging to the nested for loop. All right. And I think in order to call this flush method we also need to make a specific trait available called right. So right here what I'm going to do is actually bring in two things from IO. I'm going to reference self just so that I have the top level IO module available in my file. And I'm also going to bring in the right trait. Remember, in order to utilize a method that is the result of a trait implementation, the trait has to be in scope. So in order to use the flush method, that method is a requirement of some type implementing the right trait. So we need to make sure the right trait is available. And as soon as we do that, we are going to have this code be just fine. And we're also still going to allow io to be referenceable by providing self, and that's why we can still do IO standard in and IO standard out. All right. So apologies for that little bit of complexity. But that should solve our printing problems. And now the only thing we need to do is add a line break. Just so our content is a little bit more parsable in the terminal. We don't even need to provide an argument. This will just create an extra line break. And then we also need to terminate early in case the user has guessed the word correctly, because they shouldn't be playing six times if they got the word on, say, their third try. So what I'm going to do is add one more if statement, and I'm going to say if the target word in this case trait is equal to the user input. And once again remember there is the possibility this time around of the newline character being unaccounted for. We got rid of it here. And then we also took the first five here. We're still dealing with our original user input string. So I'm going to call trim right here to get rid of those newline characters. And if there is there is a match then I can print line and inform the user. You got it. And then say the word is word. And then very importantly, I also add the break keyword. We talked about this keyword earlier in the course. It terminates a loop. And it works the exact same way for a for loop. So it doesn't matter what iteration we are on. If the user gets it right on the first attempt, or the third or the fifth, we're going to enter into this if statement and terminate or break out of the for loop, which also completes the program. And that should do it. That should be Wordle. So let's give it a try. I'm going to run our program. Our word is trait. Let's say we have our first guess. We probably should give the user some prompt like please enter word. But we can see the flashing cursor indicating that the terminal is waiting for input. Let's say I guess paint. All right, so there we go. We can see a and I appear in yellow because they exist somewhere within the word trait but a different positions right. The letter A is second in our entry, but third within trait, and the letter I is found third within our entry, but fourth in trait. So that's looking good. P and N are found nowhere within trait, so they are rendered in regular black. And T is rendered in green because it indeed doesn't just exist, but exists correctly within the target word at position five. So let's guess something a little bit closer. Let's say we guess train using the knowledge we have here. And that's how you play Wordle, right? So now we have our second attempt to train and we're very close. We now have a word where four of the five characters are in the right position. And the letter N is not present in our target word. And so maybe we'll guess trait right now. And that should hit our if statement down below. And there we go. We can see trait printed out. You got it. The word is trait. All right. So the only remaining part that we should probably build if you were publishing this as a professional developer is probably doing something like finding some finding some kind of data source of five letter words, maybe even declaring an array of 1005 letter words and then randomly selecting one from it. But that concept doesn't particularly deal with iterators. So I'm going to call the end of the project right now. But this is basically the entire heart of Wordle. Right? And so with something like 25, maybe 35 lines of code, we basically wrote a project that another developer sold for $1 million. Right. So hopefully this inspires some creativity and inspiration on your end, but more importantly as well, it offers us a great opportunity to review a lot of iterator ideas. Here is an iterator over a range. Here is an iterator over a custom iterator that is nested that we created by combining a lot of adapter methods together, like chars like zip, like take right? Building up these little pieces, merging them together, running this basic comparison logic and taking different branching action depending on each one. So hope you had fun. And with that I will see you in the next lesson.
