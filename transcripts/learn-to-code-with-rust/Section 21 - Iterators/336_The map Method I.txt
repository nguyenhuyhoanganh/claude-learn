# The map Method I

Now that we have a solid understanding of iterator fundamentals, we can get to the fun stuff. Once we have an iterator, we can invoke methods on the iterator to transform it into another iterator. These methods are called adapter methods or just adapters. The word adapt means to make suitable for a new purpose, and that's exactly what these methods accomplish. They apply some kind of transformational logic to the original iterator to create or arrive at a new iterator. So you can think of these adapters as forming a pipeline of transformations to start with your original iterator, and then arrive at the final desired iterator. These adapter methods are available for free simply from the implementation of the iterator trait. They are defined out of the box once a type defines the next method and specifies how to progress to the next value in the iteration. It's worth noting that these adapter methods consume the original iterator. They use it as the basis for creating a brand new iterator. However, the items inside the new adapted iterator are still not consumed or accessed or exhausted until the new iterator is actually exhausted with something like a for loop or a complementary method. So all of these adapters are still lazy. They are just applying these transformations, these potential recipes, to arrive at a new iterator that does something different. So in this lesson, we'll introduce perhaps the most popular adapter method which is map. The map method on an iterator applies a closure to each item in the iterator to transform the original iterators values into a new iterator of different values. It's called map because we are mapping from one value to another. The word map means to connect, so once again this is done in a lazy manner. The newly produced iterator is still going to be lazy. It does not actually do the exact transformation until it is exhausted, but we produce an iterator that would create the necessary transformation. So of course an example would probably be the easiest to understand. Here I have a numbers vector. Let's say that I want to create an iterator consisting of the squares of these numbers. So I'll begin by declaring a my iterator variable. And I'm going to call the iter method. And that of course is going to borrow references to each integer. But so far this is all familiar. Here is where we actually apply the adapter method. On the very next line I'll declare a variable called squares, and I will take my starter iterator, which is my iterator, and I'm going to call the map method on it. And I'm going to invoke it. And I'm going to pass it a closure. The closure is going to receive each element in the original iterator, one at a time. And what the closure should return is the complementary mapped value. What we instead want to be that values result in the newly created iterator that squares will represent. So for example, if I provide my closure syntax here and I declare a parameter of number, this will be a reference to each I 32 right here from the original my iterator iterator to be more exact. So if I want to get the actual square, I can take that number. I can call the power method on it, which raises a number to a certain power. And to get the square, I can raise it to the power of two and return that. Now, unfortunately, we're going to have one small violation here. And the way we fix this is by actually providing the number closure parameter with its explicit type, which is going to be a reference to an I 32. Sometimes the compiler is just not able to fully figure it out, but once you add that parameter, you're going to get a compiling program. So once again my iterator is an iterator of references to I32. The map method transforms my iterator into a new iterator that's going to consist of the squares of those respective numbers. Now this logic has not run yet, but first up, let me show you that my iterator has completely lost ownership, so it is no longer valid. Right here I can try to print out my iterator and you can see bar of moved value. So this adapter method will move ownership from here to the new square's iterator. And my iterator is completely invalid. I also want to show you that if I print out my Square's iterator in debug format, it is still an iterator. I want to show you that what it is storing here, you'll notice are still the original values from my vector that my my iterator variable came from. And the key takeaway here is just to prove to you that the actual transformation of the values to the squares has not occurred yet, because the squares iterator is still lazy. We haven't actually iterated over it with something like a for loop, so it's just storing the recipe of I'm going to take these numbers and I'm going to apply the power method to each one. But it hasn't done so yet because of its laziness. And it won't do so until any operation that forces that consumption, that forces that exhaustion right afterwards as well. We should be able to access our numbers vector as well, because we borrowed references to it in the first place. So that's totally fine. So now here is where we actually exhaust the squares iterator that was adapted from or came from transform from my iterator. Here, I'm going to iterate over every number in squares. And when we actually perform the iteration right here is when this kind of logic will be applied. And we're going to see the square of every number. So here I'm just going to write square and output the value of number. And now we're going to see the squares of four 815, 16 and so on. And there they are. As expected as always, squares is a regular iterator, so after it is exhausted, it also loses its ownership. So squares is completely invalid to use right here. You can think of this as any other iterator that we've played around with in this section. The only difference is it came from a starter iterator called my iterator, and it transformed all of the values in it. Using this map method to their appropriate connected resulting mapped values. All right. So as an alternative to this, let's say we change this to be into Iter. Into Iter takes ownership over the original values. So all that changes is the logic that we have to apply in our map closure. So now we are no longer iterating over a iterator of references. Rather it's an iterator of integers. So I can swap this to just be a regular i32. And once again, my expression of the terminology iterating over isn't even correct right behind the scenes, rust is simply using my iterator as the basis of creating a new squares iterator, but the actual iteration, so to speak, the traversal over the original values to apply this logic to them hasn't occurred yet. It only occurs at this juncture right here when we exhaust the squares iterator, we are using an existing baseline iterator and then performing transformations on it to arrive at a new iterator. That is what an adapter method like map does. It simply transforms, it converts, or it uses an original iterator source as the basis for creating a brand new one. So of course, we do not have to assign all of these separate constructs to variables. We can use something like method chaining to create these iterators on a single line, just to show you that right here, right here I can say for each number. And I'm once again going to take number my vector, I'm going to call into Iter. And then on that I can just copy and paste this line of logic right here, like so, and delete all my former code and that will accomplish the exact same result. So once again take my vector, which by itself is not the iterator, and thus it does not support a map method in many other programming languages. You can take your starter piece of data like a vector or an array, and just call map directly on that. That will not work. In rust, we first have to transform it into an iterator with a method like iter or into iter or iter mute in order to then call map on that to get a brand new iterator, and then to exhaust that final iterator. We do that with something like a for loop, or an equivalent method that forces that iteration behind the scenes, and this is going to yield us the exact same results. Now, it's important to note that with this syntax, a method call like into iter or iter is explicitly required. So we're not going to be able to get away with the simplified syntax of something like a borrow operator. So for example iter creates an iterator of references. And if we tried to say, well, that works just fine, hypothetically, except we'd have to change this to be a reference. But let's say we did do that, like so. That's fine. We could not get away with saying, oh, I know that Iter in a regular iteration is equivalent to a borrow, so I'm going to remove it and try to put my borrow operator right here. This syntax is actually not going to work. We have to use the more explicit verbose method call to explicitly create the iterator upon which we can call the map method. It'll even tell us that right here call into iter first, right? So once I remove that, I can bring this back and it should work. Just goes to show you some of these syntax options are really nice when we're dealing with the simplified way to write something. But other times you need to know the more verbose, method based approach. That's why I wanted to introduce you to both the symbolic way with the borrow operator, as well as the method calls that are happening behind the scenes, because sometimes you will still need to use them. All right. So that is a quick introduction to the map method. And I will see you in the next lesson.
