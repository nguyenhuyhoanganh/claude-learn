# The for Loop with Iterator

We can use the for loop that we saw in the first lesson to iterate over an iterator, which will exhaust it. The iterator is lazy, but the for loop is an example of an action that will start the iteration. So the for loop will automatically call the next method over and over again. Behind the scenes on the iterator and keep unwrapping the value in the some variant. And when the for loop reaches a none variant, which indicates that there are no more values left in the iterator, then the for loop automatically terminates and the iterator is exhausted. So the syntax is exactly what we saw a few lessons ago. I'll say for then a variable for my iteration, I'm going to call it number, and number is going to represent every sequential value in my iterator. And here after my in keyword I will provide my iterator. And here in the block I can output every single number like so. And we're going to get exactly what we expect our numbers in sequence. So to summarize, the iterator took ownership of the original vector collections values and returned an iterator. Therefore, after iteration completes with the for loop, we cannot access any of the original integer elements. They're completely gone. Integers do implement the copy trait, but there's just no way to get them. After the for loop, they were transferred from here to here. Then we exhausted the iterator and did nothing to conserve the integers. We just printed them. So now the data is as good as gone. Here's another important detail. Behind the scenes ownership actually moved from the iterator to the for loop. It's a bit tricky to reason about, because there's no name in the for loop that represents the new owner, but regardless, the for loop became the owner of the iterator. And I can prove this to you by showing you that after the for loop, we cannot access the my iterator variable. We already cannot access my vector because that became invalidated right here. But after iteration, we cannot access my iterator either. In debug format. It's going to say right here borrow of moved value. It is no longer the owner. So it's not just that the iterator is exhausted, it's also completely invalid. Both my vector and my iterator have become invalid at this point because they have lost ownership of their respective values. My vector lost it to my iterator when we called into iter, and then my iterator actually lost ownership to the for loop. Then the for loop concluded, and at that point my iterator is no longer the owner. Now you might be curious, how were we able to iterate over a vector with a for loop in the first lesson of the section, despite the fact that the vector itself does not implement the iterator trait well, what the for loop does automatically is it calls the into iter method on whatever collection you pass in after the in keyword, it does that automatically behind the scenes to create an iterator. So that means what we see explicitly on screen, written out right now, is what happens implicitly when we directly pass a vector to the for loop itself. So for example, I can remove the my iterator variable completely and then use my for loop to iterate over the my vector variable instead. And again, that's not a perfect summary because we're not actually directly iterating over my vector. Rather, the for loop takes the collection on the right hand side of the in keyword. It calls the into iter method behind the scenes to get an iterator, and then it iterates over that, thereby exhausting the iterator. So the my vector variable similarly transfers ownership to the vector and then also to the for loop. So we similarly cannot access my vector. After this iteration, you're going to see the exact same error message right here. Borrow of moved value. But what's happening here implicitly is what we just saw explicitly. Rust and the for loop are just going to create that iterator automatically. So we know that the for loop calls the into iter method on whatever happens after the in keyword. So in the example before this one, if I just bring it back very quickly in this example where we had an iterator, that means that rust took the iterator of my iterator and called into iter on that. So what happens there? Well, it turns out that an iterator like the into iter struct implements the into iterator trait. So an iterator implements the iterator trait, but it also implements the into iterator trait to create an iterator from itself. So when the for loop calls into iter on an existing iterator like it does in this example, the iterator will actually simply return itself when into iter is called upon it. And that's a nice, elegant design because it allows the for loop to support either the original collection type, like the vector, or an iterator made from the original collection type. It will also prevent unnecessary copies from being made because once again, if it gets an iterator, it's not going to create a copy of that iterator. When it invokes into iter, the iterator is just going to return itself. So to summarize, if the for loop gets a vector or another original collection type, rust will create an iterator by calling the into iter method and then exhaust that. Conversely, if the for loop gets an iterator, the for loop will call into iter on that once again, but that will give back the original iterator upon which the method was invoked, sparing the need to create any duplicate iterators. So it's pretty elegant and cool stuff, because once again, rust is doing its best to avoid making unnecessary duplicates. That's all there is to cover in this lesson, so I will see you in the next one.
