# Collecting Command Line Arguments II

Let's apply these command line ideas to a more practical example. Let's say that we are collecting command line arguments for a video player application written in Rust. When the user runs our program, we would like for them to pass us three values. The first will be the name of the video file that they would like to play. The second will be a Boolean indicating whether they would like to enable subtitles, and the third will be a Boolean indicating if they would like to enable high definition. So we're going to try to write a safe program that will handle several different scenarios. Let's first consider the scenario that the user doesn't provide enough arguments. For example, if the user provides no video file name, we'll terminate our program early. If the user provides no setting for either the subtitles or high definition, or both, we can default those settings to be false, so the user should pass anywhere between 1 and 3 arguments in the command line. The video file name is critical, but the other two are optional. We're also going to account for the other scenario, which is the user passing in too many command line arguments. So if the user passes in more than three values, we're just going to ignore anything after the first three. All right. And those are some reasonable assumptions to make to ensure that we account for all of these possibilities. So let's begin by defining a struct called Settings. And that is going to be the struct we're going to construct from the user's command line values. So I'm going to call my struct 'Settings'. And it's going to hold three fields. The first is going to be 'video_file' which will be the name of the file we would like to play. And that will be a String. And then for our two settings I'll give them each fields set to a boolean. So here is 'subtitles'. And that will be a boolean. And we're also going to have 'high_definition'. And that's also going to be a boolean. And I'm immediately going to derive our Debug trait implementation above. Next up let's define a separate function. I'm going to call it 'collect_settings' and it's responsibility will be to gather the command line inputs and parse them and use the data that the user has provided to create an instance of a Settings struct and return it back to 'main'. So here I'm going to return a Settings struct, and we'll write the logic of this function in just a moment. But what I would like to do in 'main' is just collect my settings right here by calling 'collect_settings', getting back that Settings struct and I'm going to print it out in Debug format right here. So my goal will be for 'collect_settings' to handle all of the responsibility of parsing the command line arguments. All right. So let's begin right here. As we saw in the previous lesson, we can take our environment submodule and inside it we have our 'args' function which is going to give us that iterator. I'm going to assign this to a variable called 'args'. All right. Now remember the very first value within the iterator is always going to be the file name. But for the purposes of creating our Settings struct, we don't care about that file name. It's just an additional piece of metadata. Remember we have an iterator here. And thus we have access to all of the adapter methods that we learned about throughout this section. So for example, if I would like to skip the very first element in the iterator that 'args' returns, in other words, if I would like to skip the file name, what I can do is call the 'skip' method right here and pass in an argument of 1. So what this is going to do -- I'm just going to write the example out -- is let's imagine that these are our command line arguments flowing in. And I'm going to also include the name of the executable which will always be the first value within the 'args' iterator. So first step we're going to have something like 'target/debug/iterators'. This will be the first element in the iterator. And then we're going to have some sample command line arguments. Maybe the user will for example provide a file name of 'rust.mp4' before and they'll provide true and let's say false. And then let's say they provide an additional unneeded argument of "nonsense". So first things first. What we're going to do here with 'ski' is we're simply going to tell Rust: create a new iterator from this one by skipping the very first element. So that is going to get us to a point where we now have a new iterator assigned to 'args' that's only going to have these values. So we're going to, in other words, just skip the automatically provided name of the Rust file. All right. And so the next thing in the iterator after we convert it is going to be the name of the video file that we would like to play. And that is also the first field that we need to populate in Settings. So right here I'm going to say 'let video_file'. And I'm going to take my arguments and that is my iterator. And I'm just going to call the 'next' method. That's going to literally give us the very next element in the iterator. It's going to start that process of traversing all of the elements. And we only want the next one. Now there is the possibility that the iterator is empty and thus the 'next' method returns an Option. So what I'm going to do here is I'm going to call the 'unwrap_or_else' method. And I'm going to provide a closure here. And that specifies what to do of course if we have the None variant. If we have the Some variant returned from 'next', then we're going to unwrap its associated data and assign that to 'video_file' That's why Rust can know that this is going to be a String. And in this closure we specify what to do in the case that we have the None variant. In other words, that our iterator has nothing after the initial file name which we just eliminated. So this is going to cover the scenario if the user provides no command line arguments, because in that scenario, our iterator, after we remove the very first element of the Rust file name, is basically going to be empty. It's going to look like this. So this is going to cover that scenario. And in that scenario we can simply terminate our program earlier. So here I'm going to use my eprintln! macro to print to standard error. Say something like "No video file specified". Then I can utilize my 'use' keyword to bring in my 'process' submodule from within the standard library. And as a reminder, in there we have an 'exit' function which is going to terminate our Rust program. At this point, there is no point in progressing because there's nothing we can do without a specified video file to play. And as a reminder, we pass 'exit' a numeric code. And if we want to indicate an error, we pass a number that is equal to one or greater. So I'm going to pass one. All right. So that takes care of our video file. Let's now actually come back to what we have here and think about what we want to do after we account for the file name. We want to take the next two values in line, which are both going to be Booleans. Now keep in mind every value from the command line is going to arrive as a piece of text, as a string. So our responsibility will be to convert or parse these strings as they arrive. They're going to arrive as a string like "true", right? And we need to convert that to an equivalent Boolean. Right. And we'll talk about how to do that in just a moment. But I also want to account for the possibility that we have more arguments than we need. So for example if the user gives us four, we only want to take the ones right here. Now of course we could do something like the 'take' method. And that's going to be a very elegant solution. So what I'm going to do right here actually is I'm going to say after I skip the very first iterator element, which is going to be 'target/debug/iterators', I'm only going to take the next three potential values from the command line, so the user can give me a three. They can give me five, they can give me ten. I'm only going to focus on the next three after I skip the first, which will be automatically provided. So this is an iterator adapter, this being 'skip', but I can just continue to chain on new adapter methods to continually transform my iterator into another. So as you may recall, we have our 'take' method and 'take' is going to create an iterator that yields the first 'n' elements from the iterator that the 'take' method is called on. So this gives us an iterator of ten elements. I'm going to ask that we only take three right. So the first one is going to be the video file name. And the two after that are going to be the Booleans. And anything afterwards will be ignored because our 'args' function will give us all of the command line arguments, no matter how many the user specifies. Here, we're going to limit it to three after we skip the first one. Now we have to also be mindful of the possibility that the user has not given us everything we need. So for example, if they give us a video file name and a true, what this 'take' method is going to do is it's just going to give us an iterator of just these two, because two is the maximum amount that it can collect from the command line. We are going to ask for three. We're going to get two. There's not going to be any kind of technical failure, but it's something we're going to need to account for in future code, which we will do in just a moment. So we're handling the scenario of too many arguments, as we can see right here. And we're also going to need to handle the potential of not enough command line arguments. All right. So here we have started our iteration manually with the 'next' method. So we're sort of right here in the iteration. And so the next thing I want to do is let's assume our happy path. Let's assume that we are getting the next two values in line as Booleans. So I want to apply the exact same transformational logic to both of those booleans. I want to parse them as Booleans from the strings as they arrive in my program. So what I can do here is I can take my 'args' and knowing that I should have a maximum of two values left over because we limited our iterator to three total values, and we just took one right here with this 'next' method call. Let's assume we have two elements remaining in our iterator, both Booleans. In that resulting iterator that I have left over here of two Booleans, I'm going to call the 'map' method because I want to take each one of those setting strings like "true" and "false", and I want to parse the Boolean from them. So notice here, 'setting' is inferred to be a String, because every command line argument from our original args iterator is coming in as a String. So on a String, we have a method called 'parse'. Make sure to write it in here and we're going to invoke 'parse'. And we're also going to do is use the turbofish operator right here to specify what kind of data type we want to extract or parse from the string, because we can try, for example, to parse an integer or a float. In this case, we want to target a Boolean. So we're going to try to extract a Boolean from its string representation. All right. Now the 'parse' method is going to return a Result enum, because there is the possibility that a Boolean cannot be collected or gleaned or figured out from the text. So for example, if the user gives us true, followed by, you know, "nonsense". Well, there's no way to convert this to a Boolean. So in that case we're going to get the Err variant. So what I'm going to do is we're mapping over... So we're going to perform this operation twice for each of our potential Booleans. And for each Result that I get back, I'm going to call the 'unwrap_or' method and say if for some reason you're unable to figure out a Boolean value from that String because it's some kind of nonsense, just fall back to false. So what this basically means is if we cannot figure out whether the user wants subtitles or high definition or either one of those settings, we're just going to say assume false. All right. And I'm going to assign this to a variable called 'settings' because remember iterators are lazy. So what this is doing is just taking the existing iterator and the existing iterator consisting of two elements, and mapping over or declaring the logic to apply to each of the elements within it. This is what's going to happen when the iterator is exhausted. What we still need to do is actually exhaust the iterator, right? So here I'm going to call the 'next' method twice in a row to move on to the next element in the iterator. And then I'm going to collect that subtitles setting as well as my high definition setting. So right here I'm going to declare a 'subtitles' variable. And I'm going to take my iterator which should have a maximum of two elements. I'm going to call 'next' on it. Now 'next' as always is going to return an Option. And here we're going to account for the possibility that the user did not provide me two arguments. So let's for example consider this scenario. Let's say right here the user gave us just the file name. And then a true for subtitles. So in that scenario we're going to skip one. That's going to skip the Rust file name. We're going to take three. But we only have two left to work with. So this args is going to start out with a total of two values. We're going to take the video file name here. We're going to iterate over that. And then settings is going to be left to map over an iterator that just has one "true" inside it. Right. So there is the possibility that we have either 1 or 2 or neither. Let's say in this scenario our iterator that we call 'map' on has no elements remaining. So 'settings' is basically going to be an empty iterator. And thus when we call 'next', we're going to get an Option that's going to store the None variant. So once again to account for that scenario I'm going to call 'unwrap_or' on that Option and say fallback to false. All right. So the reason we're applying this kind of logic twice is one to account for the scenario that there is a command line argument, but we cannot parse a Boolean from it technically speaking. And this one handles the scenario there is no command line argument. And we're going to apply the exact same logic for our next variable of 'high_definition'. I'm going to take my 'settings' iterator, call 'next' upon it one more time to get the potential final value, which should be something like this right here. If it exists, it's going to be wrapped up in the Some variant, and if it doesn't exist, then we're simply going to get the None variant right here. But that's totally fine because we're going to call 'unwrap_or' and fall back to a false value. So what these lines are going to accomplish is if the user gives us two Booleans, we're going to be able to handle it. If they give us one Boolean, we're going to be able to handle it. If they give us zero Booleans, we're going to be able to handle it. If they give us absolute nonsense, we're going to be able to handle it and fall back to false. So basically for any missing value or for any value from the command line that cannot be correctly parsed as a Boolean, we're just going to fall back to a false. And that's going to be handled by these three lines right here. The only necessity is the video file name. And that is why we're going to terminate the program earlier in the case that we are not able to receive it from the command line. Now there's a there's a couple of things that we're going to need to fix, but for now we can start by creating our Settings instance. And we have all three of our fields represented by our variables. So we can simply pass them in here and use the shorthand syntax. So video file, subtitles, and high definition. And I think the only thing that I'm missing here is going to be some required 'mut' keywords in order to allow the iteration to occur. Remember, an iterator must be marked as mutable to perform an iteration, because somewhere internally it's going to be keeping track of that state where we are in the iteration, where we are in the progression. So first things first, in order to call 'next' on args and start that iterator, I need to make sure I mark this one as mutable. That's going to take care of this problem. And remember, 'settings' is its own separate iterator. That is an adapter that was formed by taking 'args', taking ownership over it, and using 'map' to create a brand new iterator from that. And that is what we're calling 'next' on here. So you can see the violation here is "cannot mutate immutable variable settings". Once again, don't worry if you get it wrong. I get it wrong all the time. The compiler will just tell you you need to make this thing mutable. And as soon as we add the 'mut' keyword right here, that should compile and we should be good to go. All right. So that is our program. And now we can try it with a whole bunch of different options from the command line. So let's just start with 'cargo run'. This is going to execute our program without command line arguments. So what's going to happen here? Well, Rust is going to skip the very first element in the iterator which will be the Rust file name, the executable file name that is. And then we're going to take three. But there are no additional command line arguments to parse. So this iterator of 'args' is ultimately going to be empty. Right. Because there's nothing from the command line. And we have just removed the Rust file name. So when we try to call the 'next' method, we're going to get the None variant in this Option enum. We're thus going to fall back to 'unwrap_or_else'. And we're going to run "No video file specified" and exit. And that's exactly what we're going to see here. Right here. No video file specified. That indicates that we cannot progress. All right. So let's move on to the next possibility. Let's say that the user is just going to give us one command line argument, which I will provide after two dashes. And that is going to be our file name, the video file that they would like to play. Let's just call it 'rust.mp4' because of course they're going to be watching Rust tutorials. So now same principle. We're going to get this 'args' iterator. We're going to remove the Rust file name. We're going to take three. But we're only going to have one value in here. Which of course is going to be "rust.mp4". We're going to call 'next' to get that value from the iterator. We're not going to need to fall back to here. And thus we're going to be able to extract that string of "rust.mp4" and assign it to 'video_file'. Then our iterator is basically empty because we have called 'next'. Right. So here after 'video_file' is assigned, our iterator is empty. Thus, 'map'  is basically going to create an adapter over an empty iterator, which means basically nothing of value, right? 'settings' is just an empty iterator. So we're going to call 'next' twice on an empty iterator. Both of those method calls are going to give us back a None variant. And we're going to fall back to false. So 'subtitles' will be false. 'high_definition' will be false. And our video file will be "rust.mp4". So when we print out our Settings struct, we're going to see here is our video file name of "rust.mp4" while subtitles and high definition have all defaulted to be false. Let's walk through a couple other scenarios. Let's say that we now provide the file name and a sample setting. Let's say we pass in true, and this is going to be for our subtitles setting. We would like to enable subtitles. Let's take a look at what we're going to get here. So now what's going to happen is same principle. We're going to be able to extract the video file right here. But we're still going to have our second element left in the iterator after this point, which is going to be the "true" passed in from the command line. But it is coming in as a String. So now we're going to map over this iterator of one element to create a brand new iterator. This is basically what 'map' is doing down below. It's going to successfully parse the Boolean from there. And it's not going to need to unwrap it because we're going to get a valid true here. So now after the settings code runs, we basically have a one element iterator that looks like this. So we're going to be able to call 'next' on that element or on that iterator rather. We're going to get the Option with the Some variant holding this true. And we're going to assign that true to 'subtitles'. Right. So at that point our iterator is going to look like this because there's nothing left within it. So when we call the 'next' method on the iterator, we're once again going to get the None variantm, fall back to the false right here because there is nothing more to extract. So subtitles will be set to true while high definition will be set to false. And there you can see that result. 'video_file' is is "rust.mp4", subtitles is set to true. 'high_definition' is set to false. If I pass in true and true, or two valid Booleans after "rust.mp4", well, now we're basically going to have all of our valid values coming in from the command line. So same principle here. After we extract our video file name, we're going to have an iterator consisting of our two Booleans from the command line: "true" and "true" in this example. So now we're going to map down below to a new iterator that's going to parse the Booleans from the Strings. So now settings is an iterator that looks like this. Then we're going to call the 'next' method on this iterator to get the first value inside it. We're going to get an Option with the Some variant holding true. So we do not need to fall back here. We can just take the value out of the Some variant. Same principle applies for high definition. We're going to get those two entries from out of this iterator. And subtitles and high definition have valid values which are going to be true and true. So just to rerun it again, here we are passing in true and true. And you can see subtitles and high definition are both being set to true. And finally, I want to show you one more scenario that we're going to account for, and that is having one too many values. So for example right here, if I do true true. And then I add another command line argument like "whatever". Well now, you can see everything is going to work as expected. So that's going to be handled right here at the very beginning. We're going to have our args iterator look like this. We're going to have our Rust file name, we're going to have true true. And then "whatever". Everything is going to progress as a String into the program. And I missed of course the actual video file name which is "rust.mp4". So this is what our iterator looks like. That is the args iterator. We're going to skip one. So we're going to remove this first entry in our new adapter. Then we're going to take three. Which means we're going to take the first three and leave out the "whatever" string that was collected from the command line argument right here, as well as any subsequent additional values that we pass in. Right. So here if I do a b c, this is a whole bunch of extra command line arguments in sequence. But it doesn't matter because take(3) is going to just give us back an iterator of the three first command line arguments. And those are the ones that we want to parse the video file from, as well as these two Booleans from, in order to create our Settings struct. All right. So lots of complexity in this lesson. But hopefully you can see how these variations play out and how a lot of these helpful adapter methods like 'skip' and 'take' help us to construct declaratively these beautiful iterators that do what we need them to do, right? We can omit values. We can take a certain number of values. We can convert one iterator to another using something like 'map'. Right. So lots of flexibility here and lots of these powerful methods that are available in the Rust toolbox to help you convert this iterator of external data from the outer environment to another helpful iterator within your program. That's all there is to cover in this lesson, so I will see you in the next one.
