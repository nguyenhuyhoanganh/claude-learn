# The last, nth, nth_back, and position Methods

Sometimes we may want to target an element based on its position within the iterator. In other words, we may not care about utilizing every item inside the iterator. Rather, we just want to progress up until a certain element inside the iterator, usually based on its position. And in this lesson, we'll explore several methods for accomplishing this. So let's begin by declaring a performers variable set to an array of string slices, and I'll populate it with a couple. Let's do RESTful five. Let's do Rest in Peace, which sounds like an awesome metal band. And then let's do Rust and Bieber. So the first method to show you is called last. As the name suggests, it extracts the last value from the iterator in an option enum. Once again, it is possible for the iterator to be empty. So rust has to play it safe and handle that possibility with the option enum. If there is a last element to pull out, rust will give it back to you in the in the some variant. And if the iterator is empty then rust will give you back the non variant instead. So here I'll declare a last variable. I will take my performers array. I need to create an iterator first, and I'm going to start with the into iter method call to create an iterator of non references. Although later on in the lesson I'll show you these examples with iter as well. And so here I have my iterator I can call last. And as you can see the method returns an option enum. In this case, I know for a fact that my performers array is not empty, so I know that this is going to be the some variant, so I feel pretty comfortable with calling the unwrap method here to pull out that data from the some variant and show you that it is indeed the last element of Rust and Bieber. Of course, in the real world, if you're dealing with some kind of dynamic data that you cannot predict in advance, unwrap is a little risky, and I would recommend something more battle tested, like a match statement. Regardless, the last method will pull out the last element in the iterator, so that is targeting an element by its position right now. That's nice and declarative because we need the last thing, and we do not need to provide any additional arguments. But there are even more methods to target specific elements by position. So for example, the nth method spelled like this n th extracts an element based on its index position within the iterator. Remember, the index starts counting at zero. So for example Russell five is index zero. Rest in peace is index one, and so on where this name comes from. Nth is just from the common mathematical term n, which is usually used to refer to some number. If you think about words like fourth, fifth, sixth, seventh, etc., they all end with th. It's the same principle. It's like some number that is some position in line. That's where nth comes from. It's like a generic name for that element by some position. Right. So here I can show you a couple of examples. If let's say I want to target Rust in Peace, which is the second element and thus has an index of one, I can declare a declare a variable called second here. I can take my performers array. I can call into iter on that. Call the nth method and asks to pull out the element at index one, which is the second element. Once again, nth is going to return an option because there is the possibility that the index you provide does not exist. So for example, we may have an iterator of one element, which thus will only have one index position of zero and one would be invalid. So once again rest is going to give us back an option. I know for a fact that that's a valid index. So I can once again call on wrap and show you that second is going to be rust in peace. Let's type that out. And there we go. Rust in peace right here. There's also a complementary method called nth back. And what that does is it starts from the end of the iterator. Keep in mind rust starts counting the reverse index by zeros as well. So if I count from the end of my array or equivalently from the end of my iterator, Rust and Bieber would be index zero. Proceeding backwards. Rust in peace would be one, and Russell five would be two. So, for example, if I want to get the second to last element starting from the end, I can take performers call into iter and then call nth back. So that is the nth element proceeding backwards. So for example, if I provide one that means index one proceeding backwards. And if I unwrap that you can see it's going to be the exact same result of rust in peace. So I will print out second to last because Rust and Bieber is index zero proceeding backwards and Rust and peace is one. So if I provided two right here, that would be rust five, because that is index two assuming you proceed backwards. Right. So it's the exact same as nth in that it's an index position. It's just starting relative to the end of the iterator or the end of the original array. Now in these previous examples, we knew the index position of the element that we wanted to wanted to target. In other scenarios, we want the inverse. We may not know what index an element is located at. Perhaps, for example, we have a dynamically constructed vector. We may want the index position of an element that satisfies a condition. For example, we may not know where Justin Bieber is, but we may want to pull it out by checking each element for having the string Justin Bieber. So, for example, if we want to pull out the index of an element that satisfies some condition or some criteria, we can use a method called position. So here for example, I will declare a target index variable. I will perform the exact same logic. Take performers create and iterator and call the position method. Now to position I'm going to pass a closure. And that closure is going to receive each element one at a time. And if the closure evaluates to true then what rust is going to return is that index position of that element that satisfied the closure. It's going to give it to you, of course, in an option, because there's the possibility that no value matches the closures condition. For example, knowing that I am receiving every element from the original iterator, I can use the equality check to compare it to Rust and Bieber, right? So here I'm going to declare my closure. It's going to return a boolean very similar to something like the filter method. And the first element for which this closure returns a boolean of true. It is that element index position that the method is going to return. That's why it's called position because it doesn't return the element itself. It returns the index position of that element within the iterator. So here I am still going to have an option. I can print it out in debug format right here. So target index and provide my debug formatting. And we're going to see index position two is Rust and Bieber right here. And it is wrapped up in the sum variant because there is the possibility that the value doesn't exist. For example, if I change this to something like Python Bieber, there is no string that matches that closure. So all of them will return false. There is no index to pull out and give you within a some variant. So in that case the position method will return the none variant instead. All right. I also want to update these method calls quickly to show you the inner method in action. So let me just quickly go through all of these and select them all and replace them with Iter. Some of them are just going to work out of the box. Some of them are going to have to adjust. So for example with this call right here, this is going to be technically fine. Notice that since we are creating an iterator of references, when we unwrap from the option enum, that last gives us back to us. We're actually going to get a reference to a string slice, right? And when we dereference it, or when we print it out with the print line, we're still going to get the original string. But there's one more level of references that we have to go through. In this case, either one works and I think into Iter creates one less level of references without losing ownership of the original performers array. So I think it's a little bit more nicer and cleaner. But regardless, this should work, and this should work, and this should work. Now, in comparison here, since we are creating an iterator of references and then position is going to pass in a reference to each element in the iterator. That means that our element closure parameter here is now a reference to a reference, a reference to a string slice, in other words, and thus the equality operator is not going to work. As always, what we need to do is provide our asterisk right here to force the dereference. So now this is going to give us the actual string to compare with Python Bieber. And we're going to get the exact same results as we got previously. And if I change this back to Ruston, we should now get back the valid target index of two wrapped up again in the some variant. All right. So that is an introduction to the last method, which gives you the last element in the iterator and which gives you an element based on its index position and back which does the exact same thing but progressing from the back of the iterator. And then of course, the position method, which gives you the index position of an element that satisfies the closure you pass in.
