# HashMap Iteration

So let's explore one more example of iteration, this time with a hash map. Now the iteration order in a hash map is not guaranteed. It's technically an assortment of key value pairs in no specified order. You should not use a hash map if you depend on the concept of order, you should use a hash map for associations. With that said, I still want to use a hash map example to demonstrate how an iteration over different types can yield different values. And in this scenario, it's because each hash map element actually has two pieces in it, and that is the key as well as the corresponding value. The two are intrinsically linked. They are a pair. So when you iterate over a hash map you're actually going to get two pieces of data. So the hash map type follows the exact same principles of ownership. If we create an iterable with the into item method, we're going to move ownership of both the keys and the values to the iterator, and of course the complementary methods will do references instead. So let's take a look. Here I will bring in my hash map name into scope. So here I will utilize my use keyword reach into the standard library and its collection submodule. And I want to pull out the hash map type. Here I will create a new mutable hash map called todos, and create a new hash map with the new constructor function. And in this hash map I can represent my to do list, which will consist of a task name as well as a completion status that I will model as a basic boolean. True for complete. False for incomplete. So let's quickly call the insert method a couple times on my hash map. I'll insert a key of pick up groceries and that can initialize with false. I'm going to quickly copy and paste this line twice below. Our second task, or our second to do can be steady rust, which of course will be complete. I'll mark this as true since it is the most important thing we do each day. And then one more will be sleep. And of course sleep will also be ignored and thus will be false. All right. So here once again we'll begin with our for loop. And now when we iterate over to dos we're going to get a tuple consisting of each key value pair. We need a tuple because rust needs to provide some kind of composite type that can store both this and this. And what we can do is assign each element in the two element tuple a name right here. So here I will provide a pair of parentheses just like we would for a tuple. And I'll write out the iterator variable name for each key and for each value separated by a comma. So my key is going to be our string slices. In other words our. To do so I will choose a variable name of to do. Then a comma and a space and the value of the boolean will be our completion status. And other than that the syntax remains the same. So here I'll provide the in keyword and we'll iterate over to dos. This is equivalent to calling the interpreter method on the HashMap map itself, and we're going to take full ownership over the data in the iterator. And then unless we save it, we're going to lose it after our iteration completes. So here I can print line and do something like task interpolate the value of task or to do rather then provide a complete piece of text and interpolate the value of completion status. Let's output the value of that. We're going to see our three tasks along with their completion status. And once again, if I try to access my to do's name afterwards, it is invalid. It is no longer the owner because ownership moved to the iterator that was used in the for loop. So once again an alternative is to call either the iter method, or alternatively, we can do the exact same thing by borrowing the hash map and what that is going to do if you pay attention to our respective types is it's going to make each one of those a reference. So now our to do our key is actually going to be a reference to a string slice. It's a reference to a reference because rust will automatically take a reference to whatever the key is. And the key of the string slice is already technically a reference. Same principle applies for the completion status. It was formerly a bool in the previous iteration. Now it's a reference to a boolean. Regardless, we're welcome to use it normally in our print line get the exact same result, but the benefit here is to do's no longer loses ownership because we never move ownership to an iterator. Rather, we just have an iterator of references to both the keys and the values. And of course, our third option is to either call the innermost method or once again, alternatively, simply provide a mutable reference to the original HashMap itself, and that will allow us to receive a mutable reference to both the key and the value. Now, a string slice is a little bit of an exception because it cannot be mutable at all in rust. String slices are not modifiable in any way that's part of their nature, but the boolean. Here we will receive a mutable reference to and thus have the ability to modify that value for every key value pair within our hash map. So let's alter the completion status of every task to true. Now this is another example where we are going to need to use the dereference operator. The asterisk. So simply writing completion status equals true. To try to change these respective values is not going to work. Once again, the compiler accidentally thinks that we are trying to overwrite a reference variable with a new value of true, and that boolean is a different data type from a reference. What we really mean to say is go to the memory address that we are getting here, and because we have a mutable reference, we have permission to edit the value at that memory address. So here once again we'll use our dereference operator before completion status. And that tells rust. Go to the location. Change the value at that memory address to true and that should do it. So now once again we do not lose ownership over our hash map. And furthermore we update it more specifically the values to all be true. All right. So the compiler is still a little bit unhappy. We can see a warning right here as well as right here. And that is primarily because we are not doing anything with this to do variable in the actual body of the for loop. Now, it's still a technical requirement to declare this iterator variable, because the first element in the tuple will always be the key. And we need a positional name for the key, so that the positional name for the iterator variable for the value comes second in line. This is just a technical necessity to be able to get access to this piece of data, but we don't actually care about the name here because we don't intend to use it. So one way that we can fix this problem for the sake of the compiler is to replace this name with an underscore. That magical underscore character tells the compiler, I don't intend to do anything with this value, so I'm not going to grant it a name. And now the compiler understands it's basically just a placeholder to allow us to positionally get to the value in the key value pair and give that a name. All right. That takes care of that. We're not going to see any other warnings. And that is how you iterate over a HashMap.
