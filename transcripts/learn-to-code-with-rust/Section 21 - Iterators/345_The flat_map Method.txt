# The flat_map Method

Next up, let's talk about the flat map method, which, as you might guess from the name, combines the idea of a map and then a flatten. So sometimes your mapping operation will create an iterator of collections. And you may want to take that iterator of nested collections and transform it into an iterator where the values exist on a single level, a single dimension, just like we saw with the flatten method in the previous lesson. So here's an example. Let's say that I have an attendees array, and this will represent the people who are coming to my birthday party. This will be an array of string slices, and each string slice will be a group of people. And their names are going to be separated by a comma and a space. For example, here is the first group of people who are attending. It's going to be Bob, Mary, and Kevin. And once again, I am separating all of the names with a comma and a space. Let's say the second group of attendees is going to be Mike, Robbie, Matt, and Austin. And finally, the third group of attendees is going to be Piers and Liam. So notice that there's a different number of total names within each string. So what I want to do is I want to create a vector consisting of all of these names in a single level or dimension. So the first solution to do this is just to use a regular map method, and then flatten the result set and then collect it. And it's going to look something like this. So here I can declare a attendees variable. Again let's just variable shadow it. And I ultimately want to get a vector of string slices. So we'll begin with our original attendees array. We're going to call iter to create an iterator of references. Then I'm going to call map. And I'm going to get each one of these sequential strings which represents a group of people. So I'm going to call my closure parameter group. Roop. And then on that string, as you may recall, we have a method in rust called split and split slices, or cuts a string at every occurrence of a specified character, also called the delimiter. So I want to split the names here by the occurrence of a comma followed by a space so I can cut them no matter how many names I have in a given group as many times as are needed. So here I'm going to provide the argument to split, which is the pattern to look for. And this is a string. And I want to look for a comma followed by a space. All right. So as you may recall when you call split on a string you get back a struct called split. So ultimately what the map method will produce the adapter will produce is an iterator of split structs. Now the split struct, very much like an array or a vector, represents a collection type that itself implements the iterator trait. What we ultimately have here is an iterator consisting of collection values. Right? Each value inside my iterator. Each split struct stores multiple pieces of data inside it, and in fact the amount of pieces of data can vary. Right. So in this first group, my split struct will store three total names. And the next split struct will store four. And the final split struct will have two. So they're totally different. And I want to collect or aggregate them all in one. So then knowing that I have an iterator of collection types, just like we saw with a vector of arrays in the previous lesson, that is a iterator of collection types. Same principle here, except it is split structs. Same principle though I can call flatten first to say merge all of these together into a single one dimensional iterator and then collect the results with the collect method. And if I do that and now put attendees, we're going to get what we want. So this is going to be a vector of string slices with the original names. And you can see we have first converted our strings into split structs, then sort of flatten them out and collected the results into a single vector of string slices. Now that is using the map method followed by flatten. But this is such a common operation that there is of course, a convenient complementary method called flatMap that does the exact same thing. Once again, flat means lacking any dimensions or any nested content, and map just means to transform one value to another. That's exactly what we're doing. We're transforming from a starter piece of data to a new piece of data, then taking all those aggregated collections and then flattening them down onto one level. So the only difference here is we're going to replace both map and flatten right here with a single flat map method. And all that's going to take is essentially the exact same argument that we previously passed to the map method. So here I'm going to provide my group and I'm going to specify group. And I'm going to ask to split that group string by the occurrence of a comma and a space. So basically the advantage here is we don't need to manually call the flatten method. Rust knows perform this operation to get back a iterator of collections and then flatten that result so that all of the values across those iterators are captured in one kind of total master iterator, if you will, that stores everything in a single sequential flat collection of values. So we're still specifying here the mapping logic. It's just that flat map will automatically flatten the result, the mapped result, which will be an iterator of collection values. And it can be the split struct, or it can be any other aggregated collection type. If you have an iterator of vectors or an iterator of arrays, anything that is an iterator of data types that are composite that store values within them. You can transform with flat map to get there, then flatten the final result and then importantly collect it inside a vector. And we're going to get the exact same result. All right. So that is the flat map method.
