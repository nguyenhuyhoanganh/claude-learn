# Solving a Problem with Iteration

In this lesson, let's solve a classic programming problem that involves iteration. Let's write a function that will accept a sentence and count the number of times that each word in the sentence occurs. We can store the results in a hash map where the keys are going to be the words and the values are going to be their counts. So above main, let's declare our function. It's going to be called count words and it will accept a single parameter called text, which I will make a string slice a ref str and it's going to return a hash map. And that hash map needs two concrete types for the generics. So our keys are going to be the words themselves. And those will be string slices. And the values will be their counts. And I'm going to model those as u32 because the counts can only be positive. And in order to refer to this hash map type, we of course need to bring it into scope. So here I will reach into the standard library's collection submodule and bring in my HashMap type. All right. So first things first we're going to receive our text our sentence. And the first thing we need to do is to split it based on every occurrence of a space that's going to allow us to isolate the words. And once we have the words, we can then iterate over that collection and correctly populate a new HashMap. And each time a word occurs, we're going to enter it or access its existing value, its count and increment that by one. So I'm going to take my text parameter. And on a string we have a method called split whitespace. And if I assign that to a variable like words we're going to see its type is the split whitespace struct. If I click into here, it looks like any other struct. But as you might have guessed, this split whitespace struct does implement the iterator trait, so we can traverse over it with a basic for loop. Before I do that, I want to declare my actual HashMap that is going to store all of my key value pairs my words to their counts. So here I'll declare a mutable HashMap called counts, and here I can just provide the new constructor function to create my HashMap. All right, so now on to the iteration. I want to iterate over every word in this words iterator. So right here I can do for word in words. We don't really plan to reuse the original iterator, so it's totally fine if we consume it in this for loop. We're not going to need it after the fact. So now as we progress over every word, there are two possibilities. Either the key already exists in the HashMap, in which case we want to get it and get its value and increment that by one. Or if it doesn't exist, we want to add the key with its starting count of one, because it is the first time that that word is being encountered. And we can actually create this design really elegantly using the entry method and the or insert methods that we saw earlier on in the HashMap section of the course. It's going to look a little bit like this. I'm going to take my count HashMap, and I'm going to call the entry method. And to that method I'm going to pass the key that I am searching for. And of course for us that's going to be the current word in the iterator that I am iterating over. So that is going to give me an entry struct. Now, as you may recall, the benefit of this entry method over something like the insert method is it can perform different logic depending on whether we have the key or whether we do not. So what you may recall is we have an or insert method here, and that provides the corresponding value to add to this key as a total entry, but only if the key does not already exist. Okay, so what I'm going to do here is provide a value of zero. And I will assign this to a variable called count. And then I'll talk through exactly what's going on here. So what's going to happen here is if our word does not yet exist, if it's our first time finding this key, or rather, not finding this key, this word within our HashMap, then what this code is going to do is it's going to insert that key, that new key with a corresponding value of zero. And furthermore, the or insert method will return a mutable reference to that value. So this is a mutable reference to the actual count, which in this case is going to be zero. The very first time we add that key and its count to the HashMap. Conversely, if the word that we provide here already does exist, we're not going to replace it with a value of zero. Rather, the or insert method will simply return a mutable reference to the existing value. So regardless this count variable is always going to be a mutable reference to the current count of the given word. If it already exists, we're going to simply get a mutable reference to that count like 1 or 2, and if it's just been added, we're just going to get a mutable reference to its newly added value of zero. Regardless, we have a mutable reference to the value, and we have our key value pair in the HashMap. So all that remains is to take that mutable reference to our value and increment it by one. So here, given that count is a mutable reference, I can take whatever count is. It could be zero the very first time that we encounter a word and we just added it. Or it could be one because we already ran into that word before. Regardless, I'm going to take the current count. Use the dereference operator to go to that memory location and overwrite it by incrementing it by one. All right. So once that completes for every word, we're going to repeat the exact same logic, see if it exists. If it does, pull out the current value as a mutable reference. Otherwise, add the key with a value of zero and give me a mutable reference to that zero. And regardless of what the count currently is, we are encountering that word. So increase its total count by one. This again corresponds to the value in our hash map for this given key of the word. Once this iteration completes our counts, hash map will have the data we need. So if I return it right here, that should do it. So let me show you that this is going to work in main right here. I can provide my debug syntax and I can invoke my count words function to give me back a hash map and provide a sentence like Sally sells seashells by the seashore. So in this sentence, the word sea occurs twice, every other word occurs once. So we expect to get those respective representations in the hash map. Here is our hash map. You can see here is the word see and it has a count of two, which is correct. Every other word has a count of one. So looking good so far. Let's expand this example even further by counting the character counts rather than the words. So now in the hash map, I want the keys to be characters and the values to be their respective counts. So now every single time we are iterating over every word we have, I instead want to iterate over every single one of that word's characters and then perform the exact same logic. So right here within my nested loop, within my nested iteration of every word in the words iterator, I have my word. And remember on a string, we can call the chars method to get an iterator consisting of all of the characters within that word. So I'm going to place another nested for loop here, and I'm going to iterate over every character in the current words characters. So for every single word that I'm iterating over, I'm going to create a brand new iterator consisting of that word's characters and progress or traverse over every individual character in that current word. Then what I can do is move this up into here, and this time around, we don't want to add an entry or a key based on the current word. Rather, we want to look for the current character. So same logic will apply. If it is the first time encountering a given character, then we're going to insert it with a value of zero, and count will be a mutable reference to that value of zero. And conversely, if the character has already been encountered, we're going to get a mutable reference to the existing value. We're going to ignore the zero right here, and or insert will just give us a mutable reference to the existing count. And then regardless of either one of those situations, we're going to increment the corresponding count for that character by one, complete the iteration. And now all that's left here. Let's see if I save, if that's going to solve the problem or if we need to do something different. And the issue here is that we need to update our return type, because now our count hash map will no longer store keys of string slices. Now it's storing keys of characters. Remember the chars method is going to give you one character at a time. And this char is a totally separate, distinct different data type. In rust it represents just one character. So our hash map here is now going to store keys of the char or character type, while the values the counts will still remain unsigned integers. So once I fix that, then our return here of the count hash map matches the expectation. So now we can keep the exact same example. If anything, we should actually probably update this to now be count characters, since that is a little bit more appropriate. And if I copy and paste that here now we should get a very similar thing a hash map. But now the keys are going to be our characters and the values are going to be our counts. So for example the letter L occurs a total of six times, the letter A occurs a total of three times, and so on. So this is an example of a problem that involves nested iteration. We create an iterator by using the split whitespace method right here. In fact, if you wanted to you could probably move this whole thing right here. That's totally fine as well. We create our iterator and then we iterate over every word in that iterator. And then for each one of those steps, for those progressions, we take our current word that we are progressing over, and we create an iterator of its characters. And then we iterate over those characters with another for loop that's totally valid. And we use that as the basis to perform some kind of mutative operation and then create this kind of elegant solution. And just a matter of a couple lines. That's all there is to cover in this lesson. So I will see you in the next one.
