# The iter Method

So let's pick up where we left off. We're now familiar with the intuitive method, which takes ownership over our original owned collection type, like a vector or a HashMap in order to create the iterator. Another method that we have available is called iter, just iter by itself. Iter. So I'm going to remove this into and the underscore and call the iter method instead. So the iter method will not take ownership away from the original data source. And the way it accomplishes this is by creating an iterator of references to the values from the original collection. So it's not an iterator of the original values, it's an iterator of references to those original values. More specifically, an iterator of immutable references. So I want you to notice two things here. First up, the inferred type of the number iterator variable here in the for loop became a reference to an I32. It's no longer an I32 because our iterator does not consist of the numbers, it consists of references to those numbers. Secondly, I want to show you that my vector, the original vector of data, is still valid and is still the owner after the iteration. And more importantly, I just need the debug right here. And more importantly, it never even transferred ownership to my iterator right here on line number three. It's totally valid to use right here. However, the iterator still lost its ownership from the my iterator variable to the for loop and then became exhausted and invalidated. Therefore, we cannot use my iterator after the iteration. We can use the original vector, but we cannot use my iterator. We can see here borrow of moved value. All right. So if we want to avoid this explicit verbose syntax of creating the iterator explicitly with the iter method, there is a shortcut. We can borrow the original collection type in the for loop using the borrow operator the ampersand. And when Russ sees that we are borrowing the vector, it assumes that we want an iterator of references as well. So let me remove my iterator right here. Here, let me replace this with my vector. This is equivalent to taking my vector and calling into iter on that. Instead I'm going to provide a borrow operator here. So we are iterating over the reference to my vector. And this complementarily will invoke the iter method behind the scenes to create an iterator of references. So once again notice that the number iterator variable is a reference to an I32 rather than an i32. So this code is just a shortcut for the code that we saw with the Iter method just a moment ago. Same exact idea. This design principle matters, especially when we're dealing with owned types that do not implement the copy trait, because the presence of this borrow operator is going to completely change how that iteration operates. So for example, let's declare a complementary vector. I'm going to call it cities. And here rather than integers I'm going to declare heap based strings. So right here I'll just do two cities inside my vector. The first one will be a city of Phoenix, and the second one will be a city of Dallas. And make sure to provide my semicolon. And once again, if I were to do for city in cities like so, city would be a string, and I would transfer ownership from cities to the iterator, and I would be iterating over every original string. So after the iteration, if I don't preserve the city string, it's as good as gone, right? So I'm going to get my two cities output right here. But of course my original cities vector is no longer accessible because it is no longer the owner, because it transferred ownership to the iterator right here. And thus I had an iterator of string values. Each sequential value that the iterator yields is the original string. So we took ownership from the cities vector to this implicit iterator right here. And that iterator, as it is being exhausted by the for loop, yields each successive string one at a time, and thus we lose access to cities to the iterator and to each original string. Compare that to the complementary example, which is either once again calling the iter method, which will create an iterator of references. So notice here we're going to get city inferred as a ref string. And thus we're still going to be able to print it out like so. And furthermore cities is going to remain the owner of its data. You can see here we are accessing cities afterwards. There are no problems. There is our vector. And as a shortcut to calling the Iter method explicitly, once again the shortcut is simply borrowing the original collection type. When Russ sees the borrow operator in front of our original collection type, it knows in the for loop to instead call the complementary iter method to get an iterator of references. So once again, this thing and this thing are the exact same in the purposes of a for loop. All right. So if you know your symbols you can get away with writing a little bit less code. All right. That's all there is to cover in this lesson. So I will see you in the next one.
