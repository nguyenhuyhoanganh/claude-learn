# The FromIterator Trait

So far we've explored the iterator trait and the into iterator trait. The iterator trait indicates that a type can be iterated over traversed one value at a time. The complementary into iterator trait indicates that a type can be converted into an iterator. For example, a vector implements the into iterator trait because its data can be used to create an iterator type. Now the inverse side of that equation is this trait, here called from iterator. The from iterator trait mandates that a type can create itself from an iterator. For example, the vector type also implements the from iterator trait, indicating that an iterator can be converted into an equivalent vector. It is a vector coming from an iterator, and that's where the where the from iterator trait name comes from. It indicates that a type can be created from an iterator. The from iterator trait mandates a single associated function, not a method, but an associated function that's going to live on the type and it is called from Iter. So this will be called on the type itself like a vector. And it's going to accept an iter parameter which represents the iterator. It must then return self which is the actual type itself, the type that is implementing the trait. So if a vector implements the from iterator trait and defines a from iter associated function, returning self here means that this function must return a vector, which makes sense, right? The function is going to accept some kind of iterator and it's going to create itself from that iterator. That is where the trait name from iterator comes from. It is creating a collection type starting from an iterator. The argument that is passed into this from iter associated function. So that's an introduction at a very high level. But now let's transition to VS code and take a look at this in action. So let's begin here by declaring a simple type that implements the iterator trait. I'm going to go with a range. So right here once again I'll declare a variable called 50 numbers. And I will make it equal to a range of the first 50 numbers. So one through 50. And I'm going to add my equal sign at the end to make an inclusive range that goes up to and includes the value 50 right here. All right. So this range implements the iterator trait. It is an iterator type. I want to create a vector from this iterator. And the vector does indeed implement the from iterator trait indicating that such an operation is possible. So right here I'll declare a result variable. I will take my vec type capital v vec. And remember from iter is not a method on an existing vector instance. It's an associated function living on the vector type. So here I can access from either. The vector supports this function because it implements the from iterator trait. And I'm going to pass from iter a sample iterator like 50 numbers. In fact what I'm going to do here is also call clone on that iterator just to ensure that we don't move ownership. You don't need to do this strictly if you have just one example. I'm just going to do it because there's going to be more examples in just a moment that also involve the original iterator. But regardless, we are still passing in a range which is an iterator type to from iter. And we're going to get back a vector. We are creating a collection type of a vector from an iterator from our range iterator. So here you can see our collection type. In fact, unlike something like the collect method where we need to either use the Bowfish operator or an explicit variable type declaration to annotate the type, we don't need to do that right here. Because Russ sees that since we are calling the from iter function within the vector type. It knows that the collection type that we want to create from the iterator must be a vector, so it can perform that inference for us. This from iter function is actually what the collect method calls behind the scenes. Whenever we call collect, we are asking rust to create a collection type from an iterator to collect the iterators elements in some collection. So when we call collect, rust validates that the type that we would like to create implements the from iter trait, and thus it calls the from iter function to create it. Just to show you this in action, I'll once again redeclare results. If I take my 50 numbers range, my iterator and I once again call clone to create a copy and then call collect on that behind the scenes collect needs to know what it wants to create from this iterator. It doesn't know because there's a wide variety of collection types. So as soon as I provide an annotation, for example, right here using my Turbo fish operator, I say that I want to collect the iterators elements in a vector of i32. When rust sees this, it actually transforms this into an equivalent from iter associated function call. This is what it invokes behind the scenes whenever it sees collect. And for whatever type we provide here any kind of alternative, it looks for that types associated from Iter function as well. So this is going to give us the exact same thing a vector of i32. All right. So here's another example of how we can use something like collect to create a collection from an iterator. Let's do something like a hash set which is a totally different type. So at the very top here I will reach into my collection submodule within the standard library, and I will pull in my hash set. As a reminder, the hash set is an unordered type that is a collection type that only stores unique values, so it prohibits duplicates. So here, for example, I can declare a variable called unique set and on the right hand side. Once again I will do hash set this time around. And that is another type that implements the from iterator trait, indicating that we can create a hash set from an iterator such as arange. So same syntax. It is an associated function called from iter. And I'm going to pass in 50 numbers once again cloned just to make sure we don't transfer ownership. And there you can see we might need to provide our annotation here this time around. So I'll just say hash set and ask for us to figure out the values. And that should work. But regardless we are creating a hash set from an iterator from our 50 numbers range iterator. So here you can see we're going to get our unsorted set. The numbers as they are printed may be different on your screen because they are unsorted. The goal here is to remove duplicates. And there are no duplicates in our range anyway. But it serves to demonstrate the point that we are creating a totally different collection type from our starter iterator. And once again, if I do this exact same thing with the collect method, if I take my 50 numbers range call clone just to avoid the transfer of ownership, and I call collect and I specify that I want to collect the iterators elements into a hash set, which I will indicate with my turbo operator right here. So hash set of I32 behind the scenes. When rust sees the collect method call, it looks at the type that we specify either here or in the type annotation, and it calls that types from iter associated function to create the resulting collection type from the iterator. All right, here's one more example of something totally different. I can, for example, declare another iterator or iterator type something that can be converted into an iterator such as an array. And that will also be sufficient. So here I'll declare a chars array and I will populate it with five characters spelling out the word hello. So I'm just going to do my character syntax which is single quotes, and I will write out the text hello, one character at a time. This is an array which can be converted into an iterator. It is not the iterator itself, but it implements the into iterator trait, indicating that it can become an iterator, and thus it is actually a sufficient argument for the from iter function. So here, for example, I can declare a greeting variable. Take my string type. This is our third type that we are exploring after vec and hashset, and the string type also implements the from iterator trait, indicating that it supports a from iter associated function that can create a string from some kind of iterator. So if I pass in chars behind the scenes, rust will create an iterator from the array. Pass in the iterator into from iter, which in turn will create it into a string. Right. So right here if I output greeting. As you might guess, it's going to merge or concatenate all of those five characters together into our final string right here of hello! But once again, same principle. We are creating some kind of collection type from a starter iterator. So next up, let's practice implementing the from iterator trait ourselves. So what I'm going to do is define a custom struct called playlist. That's going to model a music playlist like on some kind of streaming service like Spotify. And a playlist is going to hold both songs and users, and I'm going to vary up the types I'm going to use for those fields. So here let's declare a struct and I'm going to call it playlist. And I'm going to keep it simple. I'm going to have a songs field set to a vector of strings. And then I'm going to have a users field set to a hash set of strings. These are all just arbitrary data types, and I will immediately derive a debug trait implementation for my playlist. So let's say that I want to implement the from iterator trait for a playlist. So again when I do this, I am not declaring how to turn a playlist into an iterator. I am doing the inverse. I want to declare the logic for how to take an iterator of some kind of element and convert that iterator from being an iterator to being a playlist struct. So here I'm going to implement the from iterator trait for my playlist. Now, as always in rust it's a little bit more trickier than this because our iterator can yield different types of elements, right? Our iterator can yield strings or numbers or structs or tuples. Right. So this from iterator trait requires us to provide a value for its generic. And that generic indicates the type of elements that the iterator will yield. So create a playlist from an iterator of what from an iterator providing what kind of elements. And for each type that the iterator yields, we can actually define a totally different implementation of the from iterator trait. We can create a playlist from an array of strings in a different way that we can create a playlist from an iterator of, say, tuples, right? That's how granular and nuanced we can get in rust. So let's say that I'm going to create a playlist from an iterator that is going to yield tuples of two strings in sequence. This is just an arbitrary example. And it's going to play into the array of data that I will create in main in just a moment. That array of data is going to serve as our iterator from which we are going to create a playlist. But it's important to know that we are specifically implementing the from iterator trait to be able to create a playlist from an iterator that produces elements of type tuple of two strings. All right. So once again, to satisfy the traits requirements we have to implement a from Iter associated function. I'm just going to write it out and press enter. So rust analyzer can autocomplete. And there's a lot of complexity going on here. But this is just the signature of the from iter function that we just saw a moment ago. So basically what it accepts is that iterator right here. That inner parameter is of generic type T. T is something that must implement the into iterator trait, indicating something that is either an iterator or something that can be converted into an iterator. And what this item generic declares is an iterator that is going to produce values of type tuple. And more specifically a tuple of two elements, both of them being strings. Of course, the way that rust analyzer was able to deduce this generic and this function signature was based on our declaration right here. Right. The iterator must yield items or elements of type tuple. And you'll notice here the return type is self. Self is whatever type we are implementing the trait for, which is playlist, which means from iter must return a value of type playlist at the end of the day, which should hopefully make sense because we are creating a playlist from an iterator. So here's the logic I'm going to do here. I'll declare a mutable vector and a mutable hash set to start. So here I'll declare a songs variable set to a empty vector. And here I will declare a users variable set to an empty hash set. And before I forget, I'm going to actually return self, which will be my playlist. And I will provide the songs and users fields which will come directly from my variable declarations above. And remember, we are going to receive this iter parameter which represents our iterator type. That is something that we can progress or iterate over. And that's exactly what I'm going to do here in my for loop. I'm going to progress over Iter. Now what is Iter going to yield for every element a tuple of two strings. So right here within my parentheses I can declare a name for each one of those elements inside the tuple that the iter iterator is going to give to me. I'm going to call that song and user for now. This contract is up to us to define, and I'll show you how this works with a sample array in just a moment. But let's assume that we're going to have an array of tuples of strings. And each one is going to going to be a song and then a user. And if I provide my instant text editor and my block, you can see that rust can deduce that song and user are going to be strings. And that, once again, is coming from our generic type annotation right here. So as we iterate over this iterator, that is going to give us a tuple of a song and a user, I'm just going to push these respective elements into their appropriate collection types. So I will take my songs vector and push the song into it. I will take my user's hash set and call the insert method to add the user into that. So now after my iteration over the iterator is complete, I have my vector and hash set populated, and I will store those resulting collection types in their respective fields in my playlist, and then return my playlist represented by self. All right. So let's take a look at how this is going to work in main. I'm just going to quickly comment out this code so it doesn't bother us in the output. Let's say we have a collection type that is able to yield Values of two element tuples. So here I will create a songs array. And in here I'm going to provide tuples holding strings. Let's say the first string is going to be my song. And my second string is going to be my user who has added that song to the playlist matching what we have right here. So let's say I'm going to have a song of I rust Go on, and a user of Bob. And there is our first tuple, right. I'm going to just copy and paste that tuple so I don't need to worry about the syntax. Moving forward I'm going to paste it let's say two more times making sure to add my commas right here. And for our second song let's do something like a rust of Wind. And that is also going to be added by Bob. And maybe our third song can be something like a rust Worthy Man, and that can be submitted by Sheila. So I have an array that consists of tuples of two strings in a row. So when I convert this array into an iterator, it is going to be an iterator that is going to yield tuples. So the signature of the iterator is going to match exactly what we have defined here, indicating that we can create a playlist from an iterator based off this array. So right here I will declare a variable called playlist. I can mark its type if I want to, although it's not strictly necessary. And here I will take my playlist type which supports the from iter associated function, which accepts either an iterator or some kind of type that can be converted into an iterator, such as our songs array above. So I'm going to pass in songs right here. And as you might guess, it's going to be able to convert our iterator of songs into an equivalent playlist struct. So I'm going to print that out in debug format. And there you have it. So what happened behind the scenes? Well we called from Iter and it ran through all of this logic. It took the iterator, it iterated over every tuple, got the song and user from each, pushed it into each respective data type, then aggregated those composite data types into the playlist that it returned, and it gave us back a playlist. So we defined the exact same logic that, for example, a vector defines in its from iter function to create a vector from some iterator type itself. We declare the exact same logic that we saw here with the vector in here with a hash set, and so on. And as you might guess, since the collect method calls the from iter function behind the scenes, we now have access to the collect method as well from an iterator, and it can produce a playlist for us. So for example, right here, once again I'll declare my playlist variable set to a playlist, and I'm going to take my songs array and I first need to create an iterator from it. I'm going to call into Iter, and it's very important that I do that, because I want to create an iterator that takes ownership, so that it will be an iterator of tuples of strings matching the expected data types. So now I can call collect on that iterator and provide my desired collection type to transform the iterator into. I want a playlist here, and as you might guess, we're going to get the exact same result here. We're going to convert our iterator into a playlist. Or in other words, we're going to create something from our iterator. And this just proves that what collect calls behind the scenes is the from iter function that we defined right here. Very quickly I want to show you that if I change this into something else like Iter, it's no longer going to work because the fundamentals of the iterator change, right? What Iter does instead of into iter is it creates an iterator of references. And we have not defined the logic for creating a playlist from an iterator of references to tuples. And you can see here, if I hover my mouse over, this is going to give us a near perfect summary. It says a value of type playlist cannot be built from an iterator over elements of type, a reference to tuple of two strings, right? We did not implement the from iterator trait to support this type. For the generic a reference to a two element tuple of strings. It does tell us that from iterator is implemented for this type of a regular tuple of two strings in a row, but not for the reference equivalent. And that just goes to show how nuanced we can get with the from iterator definition. Right? We can define how to convert a reference to tuple elements totally differently from how we create a collection type from an iterator a regular tuple element, right? So we did not define this logic of creating a collection type from an iterator. References. And thus this code will not compile. That's why it's very important to make sure we are creating in this scenario, an iterator that supports the from Iter implementation that we defined here. All right. Lots of nuance, lots of complexity as always in rust. That's all there is to cover in this lesson. I hope you had fun and I will see you in the next one.
