# The zip Method

Next up, let's talk about the zip method. The zip method combines two iterators together based on their elements having the same index positions. So we can essentially merge two iterators together into a brand new iterator. And when we iterate over that brand new iterator, we're going to receive a tuple consisting of the respective elements at each shared index position. All right. That was quite a mouthful. So let me show you an example and it should make immediate sense. Let's say that I have two arrays in my program. I'm going to call them first names and last names. So here is first names. And let's populate it with three strings. Let's say Casey, let's do Robert. And then cargo. And let's also have a last names array also holding three strings. This can be Johnson, Smith and Rossman. And let's say that these represent the same three people, and we just happen to gather their first names and last names in separate arrays. So Casey is the first name of the person with a last name of Johnson. Similarly, Robert Smith is a person and similarly cargo rusman is a person. We just happen to have their names across different arrays and at each index position, for example index zero, index one and index two, we have the pairing of the first name and the last name once again just separated across these two different arrays. So zip is going to allow us to combine the two within a single iterator. And that is going to allow us to of course get the full name of the person. So here let me show you an example with a for loop. I'll begin with my starter array. So let's do first names. I'm going to call iter to create an iterator. And I'm going to call the zip method on that iterator. So zip is an example of an adapter. It's going to produce a brand new iterator. And what zip needs is the second iterator or the second data source that is going to merge with first names based on their elements having the same index position. Now you don't actually need to pass in another iterator. Here you can pass in the original data source as well. So in other words, I don't need to create an iterator from last names. I can just pass in last names directly and that should do it. So this gives us a brand new iterator and I can iterate over that with a basic for loop. So here is my for keyword. Here is my in keyword. Now here's a good question. What kind of data are we going to get here since we now have not one but two elements being collected? There's two elements for each pair, right? The first name from the first name's array and the last name from the last name's array. So each element that the iterator is going to yield is actually going to be a tuple. And the tuple is going to store two elements. In this scenario the entry from the first iterator and the entry from the second iterator. In other words, it's going to be the first name and the last name. So here I can provide parentheses syntax. In order to assign a variable name, an iterator variable name to each element within the two element tuple that I'm going to sequentially get once per iteration. So I'm going to call my iterator variables first name and last name. And the reason it's going to be this order is because I call the iter method on first names, right. And then passed in last names to the zip method. If I inverted the position of these two arrays, then I would get the last name first and the first name second within this tuple. All right. So now I of course can print line and show you the person's complete name first and second or rather first name and last name rather. And we're going to get the respective elements based on their shared index positions. So for example at index zero we have Casey in our first array, Johnson in our second array. That's exactly what we're going to get here. And we're going to print out Casey Johnson. And then for the remaining iterations, we get the predictable values right. That is the zip method. Now we can of course combine this with our basic method approaches to, for example, create a vector of the full names. Here's how that would look. Let's say I want a vector of complete names. It would be the exact same syntax, except instead of a for loop we just need some kind of exhaustion method. So let's take first names. Let's call the inner method. Once again, we'll call zip on that iterator and pass in last names. Now I have my iterator, but I need to create the actual heap string consisting of the concatenation of the first name and the last name. So for something like that, I can call the map method. And map is going to follow the exact same principles in its closure as we had here in our for loop, since we have two elements being collected here as the result of zip, what the map closure is going to receive as a parameter is actually a tuple. Once again, a tuple of each first name and last name, and I can utilize the exact same syntax here. I can provide parentheses so that I can assign a name to each respective element in each yielded tuple. So for example, I can once again call this first name and last name. So now I'm going to get those entries. There we go. And then what I need to do in the body of the map method of course is combine them. And one really elegant way that I can concatenate them and also create a brand new heap string from them is of course the format macro, which we introduced a little bit earlier. So here I can interpolate first name followed by a space and then the interpolated last name. This will use those string slices as the basis to create a brand new heap string. And because of map, it's going to return it. So now we've gone from one iterator to another iterator consisting of tuples of elements to another iterator consisting of strings. And of course, all of this is lazy. And now we can go ahead and collect the results of the final iterator. And of course, what I want here, as specified with my turbo operator, is a vector of those strings. There we go. And if I output the complete names, we're predictably going to get the exact same results as we have right here, but collected within a vector. The only thing I need to do here is, of course print it out in debug format. And there you have our result set. Now, you might be curious what happens if the arrays do not have the same length? What if one of them has an extra element or one less element? So for example, let's say I added another string here like Dan. So there's not going to be any kind of error. But what's going to happen is rust will match two elements as long as it can, as long as there is a corresponding or complementary pair in the other collection type. So basically when rust gets to Dan at index position three, it says there's nothing complementary. In the last name's array there is no value at index position three, so therefore it will actually not be included. You can almost think of iteration as stopping here, but basically Russ is going to figure out I don't have anything to match or pair Dan with. So the result is actually going to be the exact same here. We're going to get our vector of three elements. And of course if I add an actual last name here like let's say something like Digman, I don't know, we're going to now get our vector of four elements, and the same rules will apply in reverse. So if my first name's array has only three elements and my last name's array has four, there's only three possible matches or pairs that Russ can make in the zip. And that's exactly what it's going to collect right here. All right. So luckily there's not going to be any kind of error Russ is just going to do as much as it can to pair the respective elements based on their shared index positions within their original collection types. That is the zip method.
