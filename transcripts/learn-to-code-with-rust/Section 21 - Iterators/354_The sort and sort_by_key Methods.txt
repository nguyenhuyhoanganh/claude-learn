# The sort and sort_by_key Methods

Another key concept that we haven't discussed yet is sorting with an array or a vector. We don't even need to create an iterator. There is a sort method that is available on the array and vector type itself that will perform the sort and mutate the underlying data structure. For example, here I'll declare a points array and I will populate it with a couple integers in random order. Let's do three eight, one, 11, five, and I will show you the sort method in just a second. But I also want to show you that the array supports a method called is sorted, which will tell you whether or not the array is sorted. And the logic here is using an ascending sort, which means the smallest number to the largest. In this case, our array is clearly unsorted. So this method gives us back a false. All right. So now we want to call the sort method that is going to change the order of the elements within the points array. So in order to call the method, I first need to make my array mutable with the mute keyword, and then down below I can take points and sort it, and afterwards I can print out my points array in debug format. We're going to be able to see it with its elements in ascending order, smallest to greatest, starting with one and proceeding to 11. And of course, if I invoke the Issorted method after the sort operation, it's now going to give us back a value of a true. Now, if you want the numbers in descending order, in other words, largest to smallest, what you can do is first sort and then reverse the order of the array. The reverse method is available for that, and it reverses or inverts the order of the elements. So for example here I can call the reverse method. It will also mutate the array. And then I'm going to copy and paste these two these two lines below to show you what they give us. So we're going to see the points array now sorted in descending order, starting from the greatest element of 11 down to the smallest element of one. But notice that the is sorted method returns false because again its logic is based on the ascending sort. Even though the elements here are technically sorted, it is in descending order. So is sorted views it as not being sorted. This sort method will work with a wide variety of arrays. For example, it can also work with an array of strings. Here, for example, I'll declare a mutable array called exercises. And let's populate it with a couple of strings. Let's do squat, bench and deadlift all in lowercase. Once again I can take exercises, call sort, then output the value of my exercises array in debug format. And you're going to see that an ascending sort in the context of strings means that the characters that are closest to the start of the alphabet the A's, the B's and the C's and so on are going to be sorted first, and then we're going to proceed to the end of the alphabet. So that's going to be bench, then deadlift, then squat. The exact same way that they would be sorted in something like a dictionary. So here you can see that result. It's important to note that all capital letters come before lowercase letters in Rust's capitalization scheme. So for example, if I changed this final string to deadlift with a capital D, the capital D is considered to come first before all of the lowercase letters. So it's capital A, capital B, capital C all the way to capital Z. And then we begin the alphabet anew and progress through all of the lowercase equivalent characters. That's how rust views the sort. So it's going to place this capital D deadlift before bench and squat which are all lowercase. So it's going to give us deadlift first and then give us bench and squat afterwards. If I change this b to a capital B, then this capital B comes before the capital D, which in turn comes before the lowercase s. So we're going to get this sort order. So that's something to be mindful of. Sorting numbers and strings is relatively easy to reason about. But if we're working with custom data types like our own structs, we need to explicitly tell rust what values in that struct matter when it is sorted, especially when we have a collection type of that struct. So let's begin by defining a custom struct. I'll define it above main and I'm going to call it gas station. And gas station is going to have three fields. We're going to do a snack count field set to a unsigned 32 bit integer. We'll do a manager field set to a string, and we'll do a employee count field set to an unsigned 32 integer as well. And I will immediately derive my debug trait implementation. So a simple gas station struct has two numeric fields as well as a text based field. So rust by default has no clue how we would like to sort a collection of these gas station structs. Now, one option you may recall from the traits section of the course is that we can implement a trait like AWD, which is short for order, which will enable this behavior by default. And if you implement that trait, then you'll be able to call the sort method on an array of gas station structs, just like if it were any other type, like an integer or a string. But an alternative option we have available to us when we have an array of these custom types is a method called sort by key. And that's what I want to show you up next. So first things first let's create a couple gas stations and then we'll stick them within an array. So here I'll declare a mobile variable. And let's create a gas station. Here I'm just going to provide some arbitrary values very quickly. Let's do a snap count of 100. A manager of a string of Meg mobile. Let's do an employee count of three. And down below, I can basically just copy and paste this and reassign it to a couple different variables. So for our second gas station let's change this to Exxon. Let's do a snap count here of 130. Let's do a manager of Eric Exxon. Let's do an employee count of four. And finally for our final gas station, let's call this shell. And let's give it a snap count of 50. Let's give it a manager of Shane Shell. And let's give it an employee count of two. All right. And then, of course, let's package these up within a stops array. So right here I'll declare a mutable array called stops. And I will allow the array to take ownership of all three of my gas station structs. I'm going to put Mobil, Exxon and Shell. So just to show you show you here, if I take stops and I try to call the sort method on it, it's not going to work until we implement the Ord trait. It's going to say the trait bound gas station Ord is not satisfied. Ord or Ord or order is a trait that we can choose to implement for a gas station struct, which will tell rust how to sort it whenever it is used in a collection type, like a vector, like an array, etc. we have not either derived that trait or defined our own custom implementation of it, so therefore the sort method is not supported. However, we do have access to a method called sort by key, and that will allow us to sort this array without implementing the Ord trait. Let me just buy ourselves some space here. So what this is going to accept is a closure. And the closure is going to receive each array element one at a time. So fundamentally this is each gas station. And I can give my closure parameter something like station. And what I want to return from My closure. Body is the field from the struct that I want to use in determining the sort order, and it's going to be sorted in ascending order regardless of what I provide. So if, for example, I want to use, say, snap count, I can specify to take my station or each station within the array and use the snap count field as the basis for determining how to sort it. So once again the order is going to be ascending. So in this case we're going to have shell come first because it has the lowest snap count. Then we're going to have mobile with 100 then x and with 130 we have chosen which field we want to utilize as the basis of sorting. And then we're going to get the structs sorted in that order based on the values of that field. So here, for example, I can output the value of my stops array in debug format. And if I run this one we should see right here. Here's our gas station with 50, then 100, then 130. Now you have to be creative sometimes, because sometimes this is not going to work as you might expect. For example, let's say that I wanted to sort the gas stations by employee count, but I wanted to do it in descending order, so I wanted to do it with the most employees first in my resulting sorted array, then less employees and less employees. Let's do this one step at a time. So here, for example, I can begin by swapping snap count with employee count. So I'm altering the logic by which I would like to sort my collection of gas stations. I'm saying that now the employee count field on each struct is what matters. So this is going to progress us further. But the problem is these sort by default is going to be ascending. So we're going to get the gas station sorted in ascending order by the value in the employee count field. So here for example for our first gas station that has an employee count of two, then the next one is three and the next one is four. Like I said, let's imagine we wanted the reverse. Let's say we wanted the gas station with the most employees to come first. Of course we can reverse this. That's one option. But another thing that we can do is we can try to negate or invert the value here. Because if we were to take these positive values like two, 3 or 4, and we were to take their negative equivalent and turn this into negative four, this into negative three, and this into negative two. Well, all of those values would be in the ascending order, so to speak, that we would want to sort them by. If this was negative four then this would come first before this one and before this one. So what I can do is to try to take my employee count and make it negative, and I can simply try to do that by putting a negative sign in front of it. The problem is that's not going to work because employee count is actually a u32, which is an unsigned 32 bit integer that prohibits negative values. So what we can do to get even more creative is we can first tell rust to cast this or treat this as not an unsigned integer, but a regular signed integer with support for negative values. And then once we convert that number to a signed integer, then provide the negative sign in order to get the negative equivalent. So for each gas station struct I'm going to take its employee count field. I'm going to wrap it in parentheses here. Then I'm going to use the as keyword, which as you may recall, casts or converts one data type to another. So I'm going to take the employee count field a u32 and I'm going to convert it to an I32. Now I have permission to have this piece of data be a positive or a negative number. Then whatever that value is, two or 3 or 4, I'm going to get the negative inverse of it or make it negative. And that is going to allow me to perform the sort that I that I want. And when I run this, we're going to get what we want. This is an array of our three gas station structs, sorted in descending order by employee count. So this gas station with an employee count of four, that's the highest number of employees comes first in our array. And then three and then two. All right. So that is a quick introduction to the sort and sort by key methods. They will mutate the underlying structure, whether it be an array or a vector and many other types as well. And they're going to sort the elements in ascending order. Remember ascending means from smallest to greatest when it comes to numbers. And with something like text or strings, it means alphabetical. So from the start of the alphabet to the end of the alphabet. But remember as well, the capital letters come before lowercase ones. Lots of these small nuances to remember. But of course, if you get the wrong result, take a look at your output, consider why it might be wrong, and then revise your code. That's all there is to cover in this lesson, so I will see you in the next one.
