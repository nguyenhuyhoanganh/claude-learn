# The lines Method

Next up, let's explore another example of an iterator created from a string. The string type supports a 'lines' method, and that returns an iterator of the string's lines one at a time. Each line break designates the end of a line, and this 'lines' method can be helpful when we are reading in a file, for example, and we want to process it one line at a time. So let's begin by creating a file in the root directory of our project. So at the very top, I'll click out here. Then click the "New File" button. And I'm going to call it story.txt. And let's populate this plain text file with some content. I'm going to write "Chapter 1". Then do a line break. Then let's provide some dashes. Another line break, another line break. And I'll write "Once upon a time". Another line break. "In a land far, far away". One more line break. "There lived a Rust developer". So there are now six lines in this file. And our goal in this lesson will be to read in this file and iterate over the content one line at a time. So earlier on in the course, we learned about the 'read_to_string' function, which exists within the file system or 'fs' module. So let's begin by bringing in that direct submodule. So here I will utilize my 'use' keyword, reach into my standard library and pull in 'fs', which is short for file system. And here directly within that module, there is a function called 'read_to_string'. And I can pass that in a string with the name of the file I want to read to a string. So my file name is called "story.txt". Now I'm going to assign this to a variable called 'contents'. And as a reminder, there is the possibility that the specified file does not exist. And that is why the 'read_to_string' function returns a Result. There is the possibility of failure here. You may also recall that we have the try operator, which is the question mark symbol ( ? ). That will attempt to extract the data from the potential Ok variant of a Result. If Rust is able to locate this file, it's going to parse the file's contents into the string that will be stored in the Ok variant. So what I'm going to do is use my question mark operator at the end to try to pull out that String from the Ok variant inside the Result. Now, in order for this to work, I have to update the main function's return type because remember what this does in addition to attempting to pull out the Ok variant's data is if that operation fails, it's instead going to return and propagate the error from the Err, the 'E-r-r' variant, upwards. Now in this case there is no calling function, but that's totally fine. We can still have the 'main' function return a Result type itself, and if it happens to propagate an error upwards, we're just going to see that directly in our Terminal when we run the Rust program. So here is what we're going to do. First up, I'm going to utilize the 'use' keyword again to reach into my standard library and pull in the 'io' submodule that is short for input/output. And here I will designate the return type of 'main' with my arrow, and I will reach into 'io' and pull out its Result type. Now, we covered this in a behind the scenes section earlier on in the course. But what this Result is, is basically just a type alias for the main Result that we have learned so far. All that it does is it leaves room for one generic T representing the type of data in the Ok variant, but then it also hardcodes the E generic representing the type of data in the Err variant to be this specific Error struct down below, which is designated for io errors. The key takeaway here is when we declare this Result return value, we just need to provide one generic instead of two, and that one generic represents the type of data that we will store in the Ok variant for the Result that we return out of the 'main' function. For us, I'm just going to declare that to be a unit, an empty tuple. So I will declare it like so. And we're going to see in just a moment where we're going to return this unit in the case of an Ok variant. In other words, in the case of a success, if our 'main' function executes as expected. All right. So first up, everything is looking good here. The only problem is we are not returning an 'Ok' yet, but we'll get to it in just a moment. But we should have the contents of the file in a heap String at this point. So next up, what I can do is begin a for loop, and I want to iterate over every line in the file. So I can take 'contents', and 'contents' is the complete String representing the whole file's contents, including all of the line breaks. Right. So what I want to do is actually split or cut based on the end of every line, basically by every \n character. So what I can do is take my 'content' String and call the 'lines' method to create an iterator, an iterator of individual lines. So now here, every iteration, I'm going to get one line at a time. The first line, the second line, the third line all the way until we get to all of our original six. So here I can print out my line, and that should just give us all of the file's contents. And then in order to get this to compile, of course I need to make sure that my 'main' function returns a value of the Result enum variant. And so here I will return the Ok variant instead. And as promised, here I will populate the Ok variant's associated data to be a unit, an empty tuple. It really doesn't matter what we package up. We have nothing of value to return. And the unit is a common empty data type to basically signify that situation, that there is nothing of value to give back from the 'main' function. We are just indicating a success. Everything was okay. We are returning a consistent type of a Result. So we need the Ok wrapping the unit, but now we can return either our Err variant right here indirectly from the question mark, the try operator, or return the Ok variant right here with its associated data being an empty tuple. Nothing of value, right? Key takeaway though is these three lines right here. And as you can imagine, when we run the file we're going to see all of the content from the file that we just read in. All right. That is the 'lines' method. It creates an iterator from a string of all of the individual lines within that string. It's going to use the line break designation, the \n to determine where each line ends and the next one begins.
