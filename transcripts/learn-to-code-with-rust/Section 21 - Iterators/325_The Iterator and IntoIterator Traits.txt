# The Iterator and IntoIterator Traits

The word iterate means to traverse a sequence of values one by one, and we use the word iterator to describe any type that represents that sequence of values that can be iterated over. The Rust Standard library defines an iterator trait that is found in the iter submodule, and any type that implements the iterator trait promises that it is iterable, meaning that it can be traversed or navigated over one element, one value at a time. So let's take a look at this trait definition on Rust's official website. It might be a little frightening because it appears that this trait involves 76 methods, but what's fun about the iterator trait is that it only requires one method called next. If we implement the next method, the trait has default implementations for the 75 other helper methods. That's why it says provided methods right here because they are automatically provided for you. Once you define next, these methods below all rely on the next method behind the scenes. So if we provide this base building block which is next, all of this additional functionality becomes instantly available. We just get it for free out of the box. So let's talk about this next method. This next method describes how we are going to navigate to the next sequential item in the iterable. So let's take a look at the methods requirements. The next method receives a mutable reference to the instance that is implementing the iterator trait. The reference must be mutable so that rust can modify the iterators internal state during the iteration. During the traversal, regardless of the exact iterator type that we have, something inside the instance needs to be keeping track of the progress, or in other words, where we are in the iteration and when we need to stop. That requires storing some state, some data that is going to be changing over the course of iteration. And that mutation will inherently involve some change, which is why we're going to accept a mutable reference. So the next method will be called over and over as we iterate over a multi-item iterable. And it's going to produce the next item in sequence. That's where the name comes from. Now this syntax right here of type item is one that we saw briefly in the traits section of the course item. Here is what's called an associated type. And that is just a type that we need to provide. When we implement this iterator trait on a type. This item associated type represents the type of value that the iterator will produce on each step, on each round, on each iteration. So the iterator itself is a type. You can almost think of it like a machine that is producing values, but this associated type of item describes the type of item that that machine will continually output. So notice that the next method returns an option that wraps this item type. This self colon colon item is in reference to this associated item type right here. Remember, self always refers to whatever construct you are within. So here we are within a trait. So self is basically just a keyword for iterator. So it is referring to this associated type called item right here. And saying the next method will return an option where the some variant will be of type item, which is whatever type we provide here whenever we implement this iterator trait for a given type. All right. Now the reason why the next method returns an option is because at some point we'll reach the end of the iteration and we need to return nothing. We need to indicate that there is no more progression to be made. So with every step, with every iteration, there is the possibility that we have the next value in line to provide or that we don't, that we've reached the end of the road so there can be no consistent return value guaranteed all the time because the last iteration will eventually hit a dead end. So rust uses the option enum to elegantly model this design. The next method will return the next value of type item, as long as it can, wrapped in a some variant to indicate the presence of data until it reaches the end, at which point it will start producing the none variant to mark the end of iteration, and we are able to capture both of those scenarios by returning a consistent return type of an option enum. So to summarize, a some variant will hold values of the item type, a type which we are obligated to declare when we implement the iterator trait for a type, and when we are done with iteration, the next method is then going to return the same type of option, but it's going to be the none variant instead. Just like with other traits, the benefit of the iterator trait is that it establishes consistency between different types. Our program can have different data structures, but have a common interface. A common set of methods for traversing over those different types, for progressing over those different types, one element at a time. And that ties into the programming principle of polymorphism, which basically means we can try treating different types as being similar, despite the fact that they are different because they all support a similar interface. And that's generally a good thing. The more you can treat different things as being similar, the more you can not worry about the specific type that you're dealing with, and only worry about the specific methods that it supports, the behaviors that it is capable of performing. All right. So an iterator is any type that implements the iterator trait. But how do we get to that iterator type. We first need to produce it from a collection. All right. So a type can implement another trait called into iterator to describe how to create an iterator from its values. So let me search here for the complimentary into iterator trait, and I know this can be confusing, so we'll talk about it in just a moment. But I'm going to click into this totally separate trait called into iterator. So some sample rust types that implement the into iterator trait include arrays, vectors, hash maps, ranges, even option and result enums. And I want us to focus here because this was a point of confusion for me. So these core types, like vectors or strings themselves are not the iterator, but they implement the into iterator trait. That means that we can take them and transform them into an iterator, into a type that implements the iterator trait that we just saw a minute ago. In order to do that, any type that implements the into iterator trait must define an into iter method that will return an iterator. So in other words, core types like vectors and strings. Define the logic for how to create an iterator from them from their values, but they themselves. The vector in the string, for example, are not the actual iterator themselves. Right. So a vector appears iterable. It's literally a sequential collection of elements, but the vec type itself doesn't implement the iterator trait. Rather, it implements the into iterator trait so that the vector can produce another type from its values, and that new type will implement the iterator trait we just saw. So when we iterate, we're not going to be iterating over the vector. We're going to be iterating over a separate type that we'll create from the vector. And it is that separate type that implements the iterator trait, while the vector implements the into iterator trait. Because this data can be turned into an iterator. So now let's take a look at this into iterator traits definition. It requires an into iter method that will receive self. So it will take ownership over the instance the original source of data. And then it's going to return something of type into Iter. And that once again is an associated type that is referred to right here can be pretty confusing because there are two associated types here item and into iter inside and into iterator trait with an interpreter method. So I know there's a lot to untangle here, but let's take it one step at a time. The into iter method will return the type here into iter specified by the into iter associated type right here that is declared within the trait. So when any type implements the into iterator trait, they will need to provide a type for item, a type for into iter and an into iter method definition and that into iter method definition will return a type of into iter, which is whatever this happens to be. Now notice that this code enforces a trait constraint onto this into iter associated type. Whatever this into iter associated type is which the into iter method will return. That type must be a type that implements the iterator trait. So notice here with this colon syntax. That is the exact same syntax that we use with something like a trait bound with the where keyword. We're saying whatever type into iter has to be, it must implement the iterator trait into. Iter will return a type that implements the iterator trait, which. It hopefully makes sense because the purpose of this method and the purpose of declaring this trait, is to be able to take an existing type, like a vector, and convert it, or produce from it an iterator type. And this is the exact same trait that we just saw a few minutes ago, right? So we can use the compiler to validate that if we define into iter, what it returns must be some type that implements the iterator trait. Now, in order to produce an iterator, rust also needs to know the type of item that the resulting iterator will be yielding or producing sequentially one step at a time, right? So it's one thing to say, hey, you can use this vector to create an iterator, but then rust needs to know, okay, but an iterator of what an iterator that is yielding integers or booleans or strings. It doesn't know. So here this syntax right here is actually providing the item associated type that we just saw on the iterator trait a moment ago. So if I click back into that original definition remember this is the type of data that the iterator is going to be giving you on every loop on every round, right. This thing called item. So you'll notice that this syntax right here also conveniently says item equals self dot item. So this item is in reference to the associated item type in the iterator trait. And it is assigning that associated type to be whatever item is right here. The associated type also called item into the into iterator trait. So basically rust says I need to know what I'm producing, what kind of iterator I'm producing. It will be something that implements the iterator trait. And I also need to know what that iterator is going to be producing or yielding itself. And that is of type item. So if you implement the into iterator trait on a type, you need to have an interpreter method that's going to give you back an Iterator. And furthermore, rust needs to know what that iterator is going to produce. And that's why there's so much complexity here with these associated types. But hopefully the basic gist makes sense. Write this into Iter reference right here is in reference to this associated type called into iter, which has a trait constraint that it must be some kind of iterator, some type that implements the iterator trait. And then here we're specifying or. The rust team specified that the item associated type in iterator will be whatever dynamic type is provided for the item associated type in the into iterator trait. So something like a vector of integers will produce some kind of iterator type where item is going to be set to integers. While a vector of strings may produce a iterator type where item is set to strings, and so on. All right. So this is a quick introduction at a very high level. It's totally okay if a lot of these concepts were a little bit confusing. But hopefully the basic gist makes sense. And we're going to explore this in more technical detail in the code in the upcoming lessons. And I'll see you there.
