# The for_each Method

So super quick lesson here. I know we've covered iteration quite a bit, and I do want to advance to some new concepts very soon. But I also wanted to show you a method called for each that acts as an alternative to the for loop instead of a for loop, we can invoke the for each method on an iterator, and we pass that method a closure that dictates what to do for every element in the iterator. So it's basically the exact same thing, but expressed differently from the for loop. As a reminder, a closure is an anonymous function that we can declare inline as an argument to another function, and it is within that closure that we can specify what to do with each iterator element. So let's refactor the code from the previous lesson. So here instead of the for loop, I'm going to take my words iterator from above and I'm going to call the for each method on it. So I'm going to invoke the method with parentheses, and then the for each method itself accepts a closure argument. And I wanted to introduce for each because you're going to see this pattern of passing in a closure very soon. In all of the upcoming lessons. It's going to be a very common pattern when we're dealing with methods that we invoke on the iterator type itself. So I'm going to pass a closure to my for each method, and what the forEach method is going to give every time it invokes the closure is going to be each successive element in the iterator. In other words, in this scenario, each word. So basically what we have expressed right here is exactly what's happening each time we are going to be invoking the closure. More specifically, it is the internals of the for each method that are going to take care of auto invoking that closure. We just pass in the recipe to run for every word. That's why the method name is for each. We are specifying the logic to run for each word in words, right? So here I here I have my word. And then what I want to do here is take my word and perform the exact same logic I have below. So I'm going to call the chars method to get my iterator of characters. And once again that is an iterator. So it's going to support its own. For each method I can invoke that. And let's make sure to add my semicolon here and here. And once again the for each method needs a closure. So here I'm going to provide my closure. And this closure is going to be passed every character. Right. Because I am working with a iterator of characters this time around. And now within this closure I have access to both my character and my word should I want it. Because remember, the advantage of closures is they can access data. They can capture state from outside of their actual closure definition. So in here I can cut and paste this line right here I can delete this code right here. And what we have on screen is going to accomplish the exact same thing with this first call to foreach I designate what to do for every word in my words. Iterator. With the second call two for each. Here I specify what I'd like to do with each character in the chars struct, also an iterator. Here I perform that logic and we get the exact same result. So whichever approach you prefer, whether it be the for loop or whether it be the foreach method, each one is totally acceptable. One advantage of the for loop is that it does support the break and continue keywords that we saw before. Break will automatically terminate the loop before it's done. Running while continue will progress to the start of the next iteration. You do not have that same functionality with the foreach. It's always going to run until the very end, but regardless, you're going to see both of these options. I would say that the rust community generally favors the for loop over the for each, but each one is technically valid and regardless for each does offer us a good opportunity to review closures and how they effectively work as arguments to functions and methods. And with that, I will see you in the next lesson.
