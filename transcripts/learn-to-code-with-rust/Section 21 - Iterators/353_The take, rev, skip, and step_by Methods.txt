# The take, rev, skip, and step_by Methods

All of the iterations that we've written so far have been for problems where we wanted to start from the beginning of the iterator and use every element within the iterator. Sometimes we may want to exclude certain elements or skip certain elements, or maybe start from a different spot within the iterator. That is not the beginning. So let me show you a few interesting methods for those scenarios. For this lesson, we're going to go with a different iterator type. And we're going to use the range type which we haven't seen in a while. As a reminder, a range represents a consecutive sequence of values. For example, here I'll declare a variable called 50 numbers and I will declare a range type. So as a reminder, a range starts with a lower bound and then ends at an upper bound. It's called a range because it represents an interval or sequence of all of the values that fall within those two bounds. So for example, if I do a one here, then two dots and then the value 50. This creates a range. Now as a reminder with this double dot syntax we're going to go up to 50 but not include it. So this range captures all of the numbers from one up to 50 which really means from one up to and including 49. Right now if you want to include the final value on the right hand side, you can add an extra equal sign right here, and that will change the type to range inclusive. And that is coming from the fact that the 50 is included. So this is going to be the collection of values from 1 to 50. And we can iterate over this range with something like a for loop. So right here I can do something like for each number in my 50 numbers I can print it out right here. I'll put the value of number. And we're going to get one through 50. In fact we're going to get them all printed sequentially line after line. which is why we have this really long output. So maybe we can update this to a print call and add something like a slash at the end, just so we can see the content more easily right here. That's a little bit nicer. All right. So Arange already implements the iterator trait. And thus there's not actually any additional calls under the hood. We're able to just progress over it immediately with a for loop. And ownership will move to the iterator. So I cannot print out 50 numbers after the original iteration. These are all the same principles that we've talked about earlier. All right. So the first method that I want to introduce is the take method. The take method extracts a selected number of elements from the start of the iterator. So this is an iterator adapter that limits the number of elements that will be included. It's going to give you a new iterator that just takes a certain amount from the original iterator. In this case, our range iterator has a total of 50 Values say that I only wanted to take the first 15 of those. Maybe. For example, I read in the lines of a file that has 50 lines of content, but maybe I only care about the first 15. So I basically want to stop iteration. And one way to do that early is to simply limit the scope of the iteration by saying, I don't want this iterator of 50, I just want an iterator of the first 15 of those 50. And that's exactly what the take method will accomplish. So right here, since my range already represents an iterator, an iterator has that method called take. And here I can pass in a value called 15. As you can see right here creates an iterator that yields the first n elements and represents the argument that we pass in, which means we are limiting the original 50 element iterator to a new iterator. That's going to have a total of 15 elements. So we're going to start from one. And now we're going to progress to 15. Now if we want to do this in reverse. In other words, if we want to progress from the end of the range to the beginning, we can call a method called rev. Rev is short for reverse. Rev is an adapter that will produce an iterator in reverse. So for example, if I start with my 50 numbers range iterator here I can call the rev method. That is its own adapter method, which will give me a new iterator that will yield the values in reverse. So starting from 50 then progressing to 49, 48, etc. all the way down to one. And then I'm going to take 15 from the start of that iterator. So that means the first 15 values progressing from the end of the iterator. That's going to be 50, 49, 48 and so on all the way down to 36. All right. So let me return back to normal forward progression. Just so it's not too confusing. But that is the rev adapter method. There's also a fun method called skip. The skip method is an adapter that skips or bypasses a specified number of elements from the start of the iterator. So you can think of it as skipping forward from the start. For example, let's say I take 50 numbers here, and I call the skip method right here, and I pass in an argument of five. Now I am not obligated to combine the skip method with take. This is just an example of a method chain. But what this is going to do is start with an iterator of 1 to 50, and I'm going to skip or bypass the first five elements. You can think of that as jumping ahead to the value six, right. Because I'm skipping one, 234, five. And then starting from position six or number six, I'm then going to take 15 from that point forward. So this iterator of 15 values, skipping the first five from my original 50 element iterator is going to give me this sequence of values. We are starting at one. We are skipping five to get to six, and then starting from six we are taking 15 and we're going to get these values instead. Now keep in mind the order in which we apply these methods in the method chain will affect how the iterator operates. So for example, what's going to happen if we flip around the order of the take and skip methods. Let's call take first and then add skip to the end. So this logic is going to be a little bit different. What's going to happen here is we're going to start with our 50 element range iterator. But then we're going to limit it with the take adapter method to only get the first 15 numbers of that 50 element iterator. So after this point we have a 15 element iterator. Then I'm going to skip the first five elements of that 15 element iterator. And then for the new resulting ten element iterator I'm going to progress over that with my for loop. And thus we're going to get the values starting from six. And then we're going to get a total of ten values printed it out. Right. We took 15, but because we skipped the first five of those 15, we ended up with a total of ten, starting from six and progressing to 15. So the order that we declare these methods is going to matter because it affects when we start the skipping and when we start the taking. Right. So these things of course matter depending on what you want to do. Here's another example. There is a step by method which allows us to iterate in intervals or steps. For example, instead of progressing one iterator value at a time, as we've done in pretty much every previous example, we can use step by to. For example, move in or step by increments of two at a time. So for example, here I'm going to add on the step by method and pass in an arbitrary value of two. So once again these are all examples of adapter methods that are changing how we are iterating. I'm starting with a 50 element iterator. I'm taking the first 15 of those 50 to To produce a new iterator that is 15 elements or iterations long. Then I am skipping the first five of those 15. And then for the remaining ten elements that are represented in this iterator, I'm asking to iterate over them. Not one element at a time, but two elements at a time. So it's almost like skipping each second element that we encounter. So here for example, we're going to start at six like we saw in this previous example. But now we're going to jump forward by two. So you can see we're going from six to 8 to 10 to 12 to 14. So the method is called step by because our steps are now not in sequences of one, but whatever argument. Rather we pass in to here. All right. Now of course for all of these previous examples, we moved ownership from the range to the iterator that we created here. So with all of them we would not be able to output the value of the 50 numbers range afterwards. And because the range is already an iterator there's actually no iter method available on a range. If I try to invoke it right here, you're going to see it's not actually supported, right? So if you want to preserve the original owner in this case the range, there is another method that we can invoke and it is called clone. We have seen this method before and it creates a full duplicate of a value. This is the clone method mandated by the clone trait. And we've seen it before on types like the string as well as the vector. So with clone we create a full copy of the iterator. In this case, our range is already an iterator. We create a full copy of it, which allows us to decouple from the original 50 numbers iterator. So now it is on the clone of 50 elements that we take the first 15, skip the first five of those 15, and then iterate in sequences of two. And so we're going to get the exact same number printed out here. Or numbers rather. But the more important detail is that we're going to also preserve our original range. At the end. We can see it see it printed right here. It's a little bit imperfect because it's added to the end of the original output, but there it is, one through a 50 inclusive, that is this debug printed format of our 50 numbers range. So clone allows us to preserve the original data and bypass the rules of ownership by dealing with a copy. All right. So that is an introduction to a whole bunch of different methods. There's take there's rev there's skip there's step by. But they allow us to manipulate the actual iteration. Not everything has to be from the beginning to end. Not everything has to be in sequences of one step at a time. We can slice and dice. We can take a portion to iterate over. We can skip certain elements. We can progress in reverse. We can progress in multiple steps at a time. It all depends on the structure of the collection data that you have and how you want to progress over it, or manipulate the iterator so that it actually captures the data that you do want to progress over. That's all there is to cover in this lesson, so I will see you in the next one.
