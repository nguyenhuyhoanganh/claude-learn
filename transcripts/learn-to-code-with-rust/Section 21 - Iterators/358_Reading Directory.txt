# Reading Directory

Let's explore one more example of an iterator that might prove helpful in your work. The FS or file system module has a reader associated function that is short for read directory. What that function does is it opens a directory. In other words, a folder on your computer. Within the actual rust program itself, our goal in this lesson will be to iterate over the current project directory that we are in. That would be this iterators project right here. If I open up my File explorer, we can see the contents of my iterators folder. This is our project. This is our workspace. Our goal will be to iterate over every single entry. At the very top level, which includes both the directories right here as well as the files, and we're going to want to print out the content of the files at the top level. So these three files cargo loc cargo.toml and story dot txt which we created in a previous lesson. Our goal will be to print out their actual contents when our rust program executes. Now there's lots of layers of complexity here. So what I've done is I've written out a lot of the top level functions and methods that we're going to be invoking right here, as well as their corresponding return values. So if at any point you get confused, you can feel free to pause the video and review exactly what's happening in sequential order right here, because there's a lot of these advanced ideas being combined all together. All right. So let's begin with the aforementioned read dir function. Once again it is within our FS or file system module. And I'm going to invoke it. And I'm going to pass it the path, which is essentially just the location of the directory that I want to open. Since I want the current directory that I'm within the iterators folder, I can pass in a dot forward slash string. That just means open the current directory. Now when I assign this to a variable, let's say directory. You'll notice that the reader function will return a result. Now the reason it returns a result is because there is the possibility that the read operation may fail. For example, this directory may not exist or you may not have user permission to read it, or there may be a couple other different ways that it fails. But nothing is guaranteed, of course, and rust is always going to play it safe. Now, within the okay variant, if you have one, you're going to notice this. Type A reader that is a struct, and that struct is basically Rust's representation of the opened directory that we have permission to read. All right. So the first thing we need to do is to extract the reader struct. And then once we have the directory, we're actually going to be able to iterate over this reader struct in order to get access to its nested files and folders. So in order to extract this reader that could be in the okay variant, I'm going to use the unwrap or else method here as a reminder. What that is going to do is if we have the okay variant, it's going to extract the associated data to directory. You can see here the inference becomes reader. And if we have the complementary result variant which is error we handle that case right here in the closure that we pass to unwrap or else. So we are going to receive the error which will be the associated data from the error variant. And so here I can do something like use my print line macro to print to the standard error. And I could say something like could not read directory and interpolate the value of my error associated data. And then I also want to terminate the program. So I'll bring in my process submodule within the standard library. And here we can call the exit function to terminate the program early. And as a reminder we pass it a numeric code. And if you wanted to indicate a failure or an error we pass a value of one or greater. So now we have this reader struct assigned to the directory variable. This reader struct implements the iterator trait, so we can actually iterate over it traverse over it with a for loop. So right here I'll begin a for loop. And I'm going to call my iterator variable entry result. And I'll explain why in a moment. But here same syntax for in. And I'm going to iterate over directory that struct implements the iterator trait. Now notice that what the iterator is going to yield as each element is actually going to be another result enum. This in fact may be our very first time seeing an iterator that gives us a result enum. So this enum okay variant on every iteration, if you have the okay variant is going to hold another type of struct called der entry. And that's going to model an entry within the directory that we are iterating over. A directory entry is either a file or a folder. So in every iteration. We're going to get one of these things right, either the folder like VSCode or src, or a name like Cargo lock or Cargo.toml. And by name I mean file. So each one of these entries here is what entry result is going to model. But once again wrapped up within the result enum. The reason the reader struct is going to yield these results is once again, because of the possibility of something going wrong, even on a particular iteration. For example, we may not have permission to read or open a specific file within the directory, or maybe even something that sounds crazy but is totally possible, like a file being deleted in the last second or so. Let's say we're running a long rust script on some server somewhere. Maybe our rust file is going to take a minute to run, and maybe this is some production environment where files are constantly being added and deleted. It's possible that the file that we previously referenced within our iterator is no longer present, right? There's many different ways that things can go wrong. We're working with files and folders that exist outside of the rust program. So there's always the possibility that the environment changed even while our code was running. And that's why rust gives us back a result enum on every loop on every iteration. So the way we're going to handle this is we're going to use the match keyword to react to each result. So right here I'm going to say for match I'm going to say for each entry result we have to provide our two arms to account for both variants. First step is okay. And once again if we have the okay variant we're going to get that entry struct. So here if I provide a name for that associated data, you can see the inference is going to be a door entry. And that is a struct. And right here in debug format I can show you that I can take that dirt entry struct represented by my entry variable name right here. And I can call a method called path. And path is simply going to give us the location or the path of the file or the directory, right? Because it's technically either one as far as what entry could be. So this is basically just going to print out the file or the folder's name for every file or folder that we are iterating over in this iteration. In our current iterators directory, we're going to print out its name with this code. And for the complementary pattern which is the error variant, here I will provide a name of error for its associated data. I'm once again going to send a message to my standard error by using my e print line macro. This time around I'll say could not read entry and output that error. All right, so that should be a good place to start. So with that we can run our program and see some results. Pardon the pun. And there you have it. So here are all of our top level files and folders within the current directory. We started in our current directory right here. We unwrapped the reader struct. We iterated over that reader struct, we got a bunch of results potentially storing dirt entries. For every dirt entry right here, we printed out its path, which included its file name. And that's what we're seeing here. So notice this includes both the files like store txt. As well as the folders like src that are all found within the current iterators project. So before we progress, let's take a moment to refactor this code. We can simplify this code. By utilizing the try operator the question mark. We can actually apply it to both the reader. Function right here and also to this entry result. All right so here's what I'm going to do. I'm going to take these two chunks of code and consolidate them into one chunk of code above. So I'm going to begin by taking this thing right here for each entry result in directory. And rather than assigning my reader struct to a separate variable, I'm just going to take. This chunk right here, which again is not going to give me a reader but is going to give me a result potentially wrapping a reader struct. We're going to take care of that in just a moment, but I'm going to move that logic up here. And then within the curly braces I'm going to move my match logic up here. All right. And then what I'm going to do to add the cherry on top is, once again, to deal with this result that reader returns, I'm instead going to add the question mark operator. And that's going to do something very similar to what we just had with unwrap, or else in the case of an error variant, remember the question mark operator is going to terminate this function early and propagate the error upwards to the caller. In this case, main is our entry point into our program. So there is no other caller. We're just going to see that error in our terminal in our console in case this reader operation goes wrong. In the same way that the question mark operator propagates errors upwards, it also handles the success case or the okay variant. So in So in the case that we are able to successfully read this directory, then the question mark operator will extract the okay variance associated data, which will once again be the read struct that we are iterating over. Right. And then it's going to simply unwrap that data so that we can iterate over that reader struct with the for keyword. The only reason we are having a problem right now is because this question mark operator indicates a result return type. In order to accommodate the possibility of that error variant being returned, but because we do not have an explicit return type right here for the main function, rust assumes that main should be returning a unit, an empty tuple. So because there is an expected type mismatch between the return types, rust thinks it's a unit. While here we're returning a result. That's where the compiler is unhappy. So all we have to do to fix this is to indicate that main is going to return a result type. More specifically, the I o Result. Now remember, the I o result type is like a specialized declaration of the regular result enum, but with a hard coded error type of I o error filling in for that e generic. All right. So we covered this a little bit earlier. So we're going to just quickly fly through this. But I'm going to replace this thing the process submodule with the I o submodule. And then right here I'm going to indicate that the return type of main is going to be a result. And that type requires one concrete type for its generic. And that represents the type of data in the okay variant, the success variant. And I'm going to mark that as being a unit an empty tuple. So to clarify main is not going to return a unit or an empty tuple. It's going to return a result enum which is either going to be an okay variant storing a unit or an empty tuple, or the error variant, which is going to be this data returned by reader, which in case of failure, is going to propagate that error variant upwards and return it out of main. Now, the reason this code is not compiling is because we are not returning anything after the for loop. So if this succeeds, we are going to return a regular unit, even though main now expects an I o result. So now we have the problem in reverse. So all we have to do to fix this problem is add our okay variant right here, which is satisfying the result enum requirement. And make sure we return that. More specifically, an okay variant storing a unit and empty tuple. Omit the semicolon so it represents the final implicit return value. And now because we are returning the expected return type either here with the question mark or here with okay, the compiler is happy and our code gives us the exact same result. Next up, let's move on to refactoring our match statement right here. If we do not care about printing out each error when reading some kind of kind of invalid directory entry, we can just skip printing the errors entirely. And there's a couple of different elegant ways that we can solve this problem. We can begin with something like the if let construct. As a reminder that's going to look something like this. And this is going to replace our complete match statement. I'm going to write if let. Then I'm going to declare the hard coded enum variant that I want to compare against. In this case, I only care about doing something if I have the okay variant. We can say that we're going to ignore any error, but if we have an okay variant, we want to print out that entry's name, right. So here I'm going to check for the hard coded variant of okay. And I'm also going to provide a variable name for its associated data. And I'm going to call that entry. So basically what I'm doing here is trying to mirror what we have right here in this arm inside our match statement. Now on the right hand side of an equal sign, I'll provide the dynamic value to compare with the hard coded okay variant on the left hand side. And of course I want to compare entry result. And in this pair of curly braces I'm going to move the code we previously had right here right here. Close the print line. And that allows us to delete the match. All right. So this is a quick review of the if let construct. It's an if that will only execute this block of code if the condition is met. And it's also a let because it declares a dynamic variable, it declares the entry variable in the case that entry result is an okay variant. And in that scenario, we have an entry variable declared that we can use within the body of the block right here. And thus once again this is going to give us the exact same result. Now we can also use our try. Operator. The question mark right here as well. Let me show you what that code is going to look like. Once again, it's going to simplify things even further. Remember, when we progress over this reader struct that implements the implements the iterator trait. We are getting this result enum. And that result enums. Okay, variant stores the dir entry struct that we want. Well, knowing that every entry result right here, knowing that that is an iterator variable that is a result, and knowing that the try operator, the question mark can be applied to a result, we can simply perform the exact same logic that we have right here in a little bit less code. So right here I can declare a variable called entry and simply take my entry result result type and apply the question mark operator right here. And so now we have our entry representing the dir entry. And once again if entry result happens to be the error variant then this will terminate early and propagate that error upwards. So it's a little bit different from if let because if let will not terminate early. In case of the error variant it's simply going to progress to the next iteration. This will terminate the main function early. But it does allow us to have this simple syntax to extract the data. We care about the associated data within the okay variant here to our entry variable name, which accomplishes the exact same thing as we have here. So now we can remove this and this. And once again have the exact same result in our output. Read the directory. Get the reader struct from it. Iterate over that to get every dirt entry struct from the result that is yielded on every iteration. Make sure we have the okay variant. Make sure we have the dirt entry struct and call the path method on that to get the name of the file or folder that we are currently iterating over. So the final thing we need to do is to print out the contents of our files. As a reminder, we have a couple files right here. We have Cargo.toml and story dot txt. We want to ignore the directories. We're still going to iterate over them. We're going to ignore them. So the next thing we need to do of course is to is to figure out whether a given entry is a file or a folder. Right. Right now we just have their names represented in our output. But we want to distinguish between the ones that are files. And those are the ones that we want to read and output. All right. So here's how we're going to accomplish that. First things first I'm going to remove this print line. And instead I'm going to still call my path method. And I'm going to assign that to a variable called file name. It's going to represent the file name or the path to be more specific, the location of every given entry that we are iterating over. Now the file system module includes a associated function called metadata that will accept a file name or accept something of type path. But let me just show you this very quickly. I can take my file system module in there. I have a metadata function, and to it I can pass a reference to my path buff above and what this is going to give me if I assign it to a metadata variable, is actually going to be another. Result. This is going to be file name. My apologies. So once again this is kind of interesting how. Each one of these operations, whether it be reader or the entry result or metadata, is going to be giving us these results back. But once again, it's because there is always this possibility for error. Now the lucky part here is this result once again represents an I o result. So if we want the exact same logic that we had above, which is basically propagate the error upwards, or keep the data in the okay variant, which in this case is this metadata struct that we'll talk about in just a moment. Once again we can apply the question mark. Operator. And that is going to extract the metadata struct. So if I click into this just like your entry just like reader, this is some kind of struct deep within Rust's source code. But what it allows us to do is to get metadata about a specific file or folder. Metadata is just supplementary information, right or supplementary data that we can find out about what we are currently iterating over. So, for example, this metadata struct has a convenient method on it called is file. That is file method will give you back a boolean true if what you are iterating over is a file rather than a folder. Remember we are iterating over a directory, so each one of these entries that we are iterating over could be a folder metadata. This struct that is going to figure out its information from the name we pass in here. Even file name, by the way, isn't really perfect here because it could be a file name, or it could be a folder name. So maybe we should change this to something like entry name. Just to be a little bit more clear here. Right. But metadata is going to use that entry name to find that entity or construct on your file system and give you a struct with information on it and helper methods to help you deduce stuff about it as well. So I'm going to take that metadata struct and I'm going to call is file on it to find out whether it's a file. You may have also noticed there is a complimentary is dir function that does the inverse. It gives you back a true if you have a directory instead. So if I have a file knowing that I have a file, I can now print out its contents. So the first thing I'm going to do right here is I'm going to print out my entry name in debug format. And then I'm going to add a line break. And then I'm going to add a couple of dashes. And that's just so we're going to know which file, which one of these files we're actually printing out the content for. Then on the line below I'll declare a contents variable. And this again is still going to be happening for every loop in every iteration I'm going to take my file system module. You may recall that that has a read to string function. And that can expect the file name of the thing we want to read. And now we have a guarantee that we have a file name. So I'm going to pass in a reference to entry name from above, which at this point we know is going to be an actual file rather than a folder. And of course, if we try to read a folder to a string, we might not have expected results. In fact, I'm not even sure what's going to happen. But we don't have to worry about it because we know that entryname inside this if block is an actual valid file, right? And so contents is going to give us a result. You guessed it. We can once again apply our try. Operator. Our question mark to extract the actual string. So assuming success, assuming we were able to actually parse a string from the file itself, that string is going to store the file's internal contents, the text inside the file. And if for some reason the read to string function fails, then we return the error upwards, or more specifically, the question mark. The try operator terminates early and sends that error back up. So now that we have the contents, we can print it out. And that should do it. So quite a lot going on. But let's run our program and let's ensure it gives us everything we want. We have three total files that we want to print out. Cargo.toml cargo lock and storage txt. We should see all of them. Output here is cargo.toml. Right. That is coming from our entry name right here along with the dashes on the line after. So there's a nice little dividing line. Then this is the actual content from the cargo.toml file. So our rust program within the iterators directory, read the cargo.toml file from the directory and printed it out here in the output. Here is the cargo lock file. In fact, I can prove to you that it's the exact same thing by literally opening it. You can see here is the file in VS code, and here is its content right here. And here of course is the final file of story dot txt. And there is its content output right here. All right. So lots of complexity in this lesson because we basically did a lot right. We read a directory with the reader function. We then iterated over that directory to get every entry. We then figured out which one of those entries were actual files. And then we read those files in as heap strings to print out those for operations all have the possibility of failure, which is why all of them return results. But we were able to still write a pretty small amount of code total because we utilized the super helpful tri. Operator the question mark right here, right here, right here, and right here. That just allowed us to extract the okay variance data, the success data, the data we need to continue while also terminating early in case of failure. Once again, this is such a common pattern in the rust language. The team specifically added this operator right here so that you don't need to write your own match statements, where you extract the data you want while terminating early. This just does that magically for you with a single character. It allows you to accomplish something like this, and quite frankly, a pretty small number of lines of code. So pretty cool stuff. Pretty powerful program that we were able to build here with only about 10 or 15 lines of code. All right. That's all there is to cover in this lesson. So I will see you in the next one.
