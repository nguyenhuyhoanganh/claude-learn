# The filter and find Methods I

Next up, let's discuss the filter method. The filter method is an adapter that extracts a subset of values from an iterator based on a value satisfying a condition meeting some criteria. We pass the filter method, a closure which rust will execute once for each value in the original iterator at the point of exhaustion. If the closure returns true, then the iterator will keep the value, and if the closure returns false, then the iterator will discard or throw away that value. It's very similar to the retain method that we saw on a string earlier, in that it allows us to designate how we select a certain subset of values from an original collection set. The original iterator. So here's a basic example. Let's begin by declaring a numbers array and I will populate it populate it with ten, 13, 23, two, eight, nine and six. Let's say that I want to create an iterator and then ultimately a vector consisting of just the even numbers from this array. So I will begin by declaring my evens variable. And I want this to be a vector of i32. So we can begin with our array. I'm going to call some method to create an iterator. And we'll start with into iter which will create an iterator of ownership. It will attempt to take ownership over the original integers. Now the interesting caveat here is that integers implement the copy trait. So actually this will not move ownership away from numbers. It's just going to create a full copy of the original integers. This is not always the case. So you have to watch out with the into iter method. But it will be the case with simple primitive types that implement the copy trait like integers, floats, and booleans. All right. So this is our starter iterator. And filter is an example of an adapter. It transforms one iterator into another. So here I will call the filter method and pass it a closure. And within the closure I will accept one parameter which will represent each sequential element from my original iterator. I'm going to call that number. Now what's interesting is the filter method will pass each item to the closure by reference, and this will be the case regardless of what original method we use to create the initial iterator. It's just how filter is defined. Whatever this iterator consists of, filter will give a reference to each one of those values for the purposes of its method. And the reason it has that is probably somewhere in the internals. To avoid accidentally transferring and then losing ownership. Because basically what we need to provide here is a closure that returns a boolean that designates whether to keep or throw away a value. And Russ doesn't want to accidentally move ownership when it's using the original values. To make that boolean calculation. Regardless, we have to be careful because when we're dealing with references or even more complex types, as we'll see in just a second, that may influence the kind of code that we write. Now, the good news is, despite the fact that we are working with a reference to an I32, we're actually going to be able to get away with simply providing our even logic without even using the asterisk. So we can assume, as if number is as good as an i32, it will work just fine. So, as we know, in order to determine whether a number is even, we can take its remainder when divided by two. So I will use the modulo operator to figure out the remainder when number is divided by two. And then I will use the equality operator to compare that to zero. And if this returns true, my number is even. And this is exactly what I want to produce as the return value of the closure that I passed. To filter it must be a boolean. And if it's a true, then we're going to keep that value. In other words, keep the evens. And if it is false then rust will exclude that value. Then once again iterators are lazy. So at this point nothing has happened yet. I'm just designating the transformational logic. The next step in the pipeline on how the eventual collection type will be constructed. When I call the collect method in just a moment. So at this point, no actual filter logic has ran. I'm just defining the recipe. So now if I save this, of course we have a type mismatch because we're not actually producing a vector on the right hand side of our equal sign. But if I call collect, that is going to give us what we want. And below I can output the value of my evens vector like so. And it should consist of just the even numbers from my numbers array. And indeed it does. I also want to show you that my original Numbers array is also valid to use because in this case, even though we used into Iter, which does not borrow by references, but actually returns an iterator of full ownership when it comes to integers, full copies are made anyway, so it's going to actually retain numbers, and numbers will not lose ownership. This would not be the case if, for example, we had an array of heap strings. So you have to watch out there. Now with that said, let's say we did update this call to be Iter and see how that affects the final result. So remember Iter is going to create a iterator of references. So this is an iterator consisting of ref i32. But remember the filter method automatically will take a reference to each original element in the original iterator. So if this is an iterator of ref i32, the number here will be a ref ref I32. Basically a reference to a reference to a number that basically means a memory address that will lead you to another memory address, that will then lead you to the original integer wherever it's stored in memory. So the problem here is that rust cannot be smart enough to figure out how to dereference infinitely. Basically, it starts thinking that we want to apply the modulo operator to a reference, and that's invalid. So rust cannot dereference more than one level down. So we have to be more explicit here with the asterisk the dereference operator. So if I place that symbol in front of a number that is going to allow it to figure out the actual original integer value, and then do the modulo operation to get the true that it needs to perform this calculation and filter. But we still have a problem, and that is that the filter will transform the original iterator and keep the original data type. So we have this starter iterator of references to an to an i32, and we've created this resulting iterator that will be a smaller collection set or a subset, but of the same type which is ref i32. So when we try to call collect here on this smaller result set, rust says that's a mismatch. I'm going to get here a vector of references to I32. Instead, they're trying to get me to assign to a vector of just i32. And that is a mismatch. I can prove that to you by changing this type to be a ref. I32. And now our code will indeed compile. Now if I run this, it's still going to work, because in debug format, when we print out this vector, rust will automatically dereference and give us the original integers printed out. But this is not a vector of integers, it's a vector of references. So we have to be mindful and careful here of what we're dealing with. Now, if you wanted a vector of i32 despite the fact that you're working with Iter here, what you can do here before we call collect is called another method called copied. And this is an interesting one. I'm going to save this to show you. We get the results that we want. I'm going to command or control. Click into copied so I can show you its definition. And this description explains it better than I can. So this part right here this is useful when you have an iterator over but you need an iterator over just t instead. So this was exactly our problem. We had an iterator of references. And we wanted to use an adapter method in this case copied to convert that to be an iterator of the actual original values coming from those references. So this forced another copy of those integers. So now we can actually collect the final iterator which is now going to be an iterator of just I32 instead of ref I32. It basically copies all of the elements based on their original references. So now we have those copies, and then we can collect from the iterator into a into a vector. So once again this creates the iterator. This filter is just an adapter that gives you a different iterator. It's still lazy right? This copied is another adapter that gives you another iterator. Still lazy. And in fact, if you got rid of collect and just wrote this code, the compiler would warn you that nothing is actually happening. We're just defining three sequential transformation recipes in a pipeline, but nothing's happening. So it's when we call collect that the actual exhaustion of the final created iterator occurs. And that's when we gather our vector of i32 right here. All right. So if filter is unable to get any matches, in other words, if the closure here returns false for everything, then we're just going to get a final vector consisting of nothing at all. In this case we didn't see that. But that's just the nice thing to know in case you get an empty result set. Now the filter method will select all values that fit the condition in the closure. In comparison, there is a complementary method called find. And find will locate the first element for which the closure returns true. So not all elements, but just the very first one proceeding from the start of the iterator. So the find method is actually going to stop iteration immediately when it gets a true, and then actually exhaust the remainder of the iterator to prevent any unnecessary traversal. So this method, the find method will actually return an option, because there is the possibility that no element at all evaluates to true in the closure. Right. If you do have an element that evaluates to true, that is going to be wrapped up in the some variant. And if nothing evaluates to true, then you're going to get back the none variant instead. So for example, if I want not just all the evens, but specifically the first, even then I can call the find method instead. So here once again is numbers. I'll call into editor to create an iterator, and then I will call the find method and pass in a closure. Provide my number parameter here. Here it is a reference to an i32. And once again I will do number modulo two equals equals zero. And this time around, as you can see, as soon as I add my semicolon we're going to get an option with an i32 type. So if I output the value of first even in debug format, we're going to see the some variant storing the very first even, which coincidentally is the very first value in line are ten right here. Here's another complementary example. I'm just going to change this to be first odd. Let's get the very first odd value within my iterator. So I'm just going to swap this logic here to be not equals to zero. This will give us the first odd. And I will update this to be first odd. The first odd value in our original array from which the iterator was created is 13. So we're going to get again the some variant indicating there was a match holding the first match, which was 13. All right. So once again, the very first time this closure returns true is the element that will be returned back with filter. It is all of the matches with find. It is the very first match. I just want to show you one example of this find method returning a none variant. And that of course is if the condition is never met for every single entry. So for example, here is my numbers array. I'm going to create an iterator from it using integer pass in find here. Pass in a closure pass in my number parameter. Let's say I say a number that is greater than 100. There is no single value here that is greater than 100. This is an example where unfortunately we once again have to think on the spot. Sometimes the compiler can figure out what we're trying to do, sometimes it can't. This time around, it thinks again that we're trying to apply the greater than operator to a reference. It's invalid. Whenever you see this, just remember you just have to help us figure out exactly what you're comparing, we want to compare the actual number at the memory address to 100. So once again I'll provide the dereference operator here. Now it understands the comparison. And in this case, since there are no values in the original iterator that are greater than 100, if I output the nothing option in debug format, it's going to be the alternate variant, and that's going to be none. All right. So that is what you get when there is no match. Very quickly I also want to show you simply by copying this and this. There are also complementary methods called are find are find is short for write find. And it does the exact same logic but from the end. So it's going to give you the last occurrence of an element that satisfies the closure condition. So in other words, if we wanted the last even we could swap this with the r find method. And that will proceed from the end of the iterator. Looking for the very first element for which disclosure. Evaluates to true. I'm going to swap this to last even. And of course, the last. Even here is going to be the six in the last position. So I can show you that that's going to be our some variant with a value of six. And one more time I'm going to change this to be last odd. Same syntax. Take your original data source. Create an iterator from it. Invoke the find method right find. Think of it as meaning. Start from the right. Start from the end and then proceed to the beginning and invoke this closure once for every element you encounter, and for the very first element that you get a true return that element. That's what our find does. So this, of course, is going to give us the very last odd, which should be the number nine right here. And of course, it's going to be wrapped up in the sum variant as well because it is a match. If there are no matches preceding from the end to the beginning, then as always you're going to get a none variant. All right. So that is a quick introduction to the filter and find methods.
