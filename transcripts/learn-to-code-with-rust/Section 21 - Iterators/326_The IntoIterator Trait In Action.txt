# The IntoIterator Trait In Action

So to review the vector type implements the into iterator trait. That means a vector supports an into iter method that will return a type that implements the iterator trait. So here I have my vector of integers from a few lessons ago. Let's take that vector and let's call the into iter method on it, and let's assign the result to a my iterator variable and observe the inferred data type from rust analyzer. So rust analyzer or the compiler infer the type to be an into iter. And this can be a little bit confusing, but this is actually a shorthand for a struct that is called into iter. I can command or control click into. Here. It's going to take us to a struct definition of a struct called into iter that is actually found within the standard library's vec submodule. So to clarify this into iter struct is totally different from the into iter trait. This is an into iter struct. And the reason those two names can coexist is because they're defined in different modules. We talked about this principle back in the project structure section of the course. So they happen to have a trait called into Iter in one submodule and a struct called into iter within the vec submodule. And that's totally fine. Those two names can coexist. Now it is this into iter struct that implements the iterator trait, which means it can be iterated over with something like the for loop. So it's very confusing. I know because we have a trait that has the exact same name as the return type that the into iter method produces on that vector. All right. Notice as well that the compiler infers the item associated type to be an i32, and that represents the type of value that the iterator my iterator will produce or yield in sequence. And that is derived from the fact that our vector holds i32 values. So if we use the vector as the basis to create an iterator, that iterator is going to be producing whatever the vector had, which in this example, of course, is integers. And that's where that i32 is being calculated from. All right. So one really important detail is that the interpreter method takes ownership over the vector to produce the iterator. So if I tried to print out my vector afterwards it's not going to be valid. Here we're going to see a violation in VS code as soon as I save. There we go. Borrow of moved value. And if I take a look at the compiler output notice right here move occurs because my vector has no implementation of the copy trait. That's what we know. But notice where the actual move occurs. It comes from the interpreter method call. So by default that method is going to transfer ownership to the iterator itself. And thus we cannot access the original data source. I want to show you a very similar complementary example. I'm just going to copy and paste what we have above and replace my integers with booleans, just to show you a different inference. So here I'm going to replace this with false true false. So now we have a vector of booleans. We're going to call interpreter on that. and once again get this type of into itor if I. Command or control click into that. That is once again the exact same into it or struct found within the vec submodule. It's the same struct that we saw in the previous example, but now the concrete type filling in for generic t is a boolean, because this iterator, when it is iterated over, will produce or yield Boolean values instead of integers, right? So it is this vector that implements the into iterator trait, indicating that it can be used as the basis of creating an iterator, but it is actually the into iter struct here that implements the iterator trait itself. This is what we're actually going to be iterating over and ownership will move to my iterator. All right let's do one more example with a different data type. And it's going to be the hash map. So I'll utilize my use keyword at the very top to reach into my standard library. And the collection submodule and the hash map type. And below I can declare a mutable variable called my hash map, and I'll use the new constructor function on a hash map to create an empty hash map, and then let's quickly insert a key value pair to allow the compiler to be happy. So here I'll take my hash map. Call the insert method. These are all arbitrary. Let's do CBS and two. All right. And then once again I'm going to declare a my iterator variable using variable shadowing. But this time around I'm going to take my hash map and call into it or on that. So once again that means the hash map type supports the into iterator trait and thus the into iter method as we saw here, which means it can produce or be used as the basis of a new iterator. And it's that iterator that implements the iterator trait. So what we get back here says into iter. And it looks kind of familiar, but once again gets even more tricky. If we command or click into this, we'll see that this is another into iter struct, but this is one found within the collections submodule. So this is a different into iter struct compared to the into iter struct that's going to be produced from the interpreter method on a vector. So the iterable types we're getting back are going to be different depending on what the original data types are. Right. But what all of these structs are going to have in common is that they're going to implement the iterator trait, indicating that they can be traversed over or progressed one value at a time, right? So once again, if a type can be used as the basis of creating an iterator such as a vector or a HashMap can, they will implement the entire iterator trait. And then what we get back from that interpreter method call will be the type that implements the iterator trait itself. All right. And there's many other types that are going to implement the into iterator trait including types you may not expect such as option and result. So you can try giving those a shot when you have a moment. So to summarize, when a type implements the into iterator trait, the type promises that it can be converted into an iterator. That's where the name comes from. All right lots of complexity but hopefully starting to make bigger sense here. Now that we've dived into it in in VS code. All right. And more on this in the upcoming lessons.
