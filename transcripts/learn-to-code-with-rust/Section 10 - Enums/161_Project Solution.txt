# Project Solution

So let's walk through a sample solution to the coding challenge in the previous lesson. First up we had to define a 'Tier' enum with three variants: 'Gold', 'Silver' and 'Platinum'. So right here I'll use my 'enum' keyword. Then my name is 'Tier'. Provide my curly braces and then write my variants separated by a comma and then a line break for readability. So 'Gold', a comma and a line break. 'Silver' and then one more: 'Platinum'. And then we're asked to derive a Debug implementation. So a directly above the 'Tier' enum, I'll provide my hashtag, my square brackets, the 'derive' directive to the compiler. And I'll ask for a Debug trait implementation. And that should do it. Define a 'Subscription' enum with three variants as well: 'Free', 'Basic' and 'Premum', and derive a Debug implementation for that enum as well. That's going to be the exact same syntax. So the 'enum' keyword. My enum name is 'Subscription'. This is an enum. It is a collection of possible variants. And we need 'Free', 'Basic', and 'Premium'. And I'll once again provide my syntax above to derive the Debug trait implementation. And that's going to be a hashtag, square brackets, 'derive' and Debug in parentheses. All right. So the 'Free' variant should have no associated data. So we are good to go. That accomplishes the goal right here. The 'Basic' variant should be a tuple variant with two pieces of data. So with a tuple variant, we provide a pair of parentheses right here. And then provide the data types for as many elements as this tuple variant is going to contain. One of the advantages of these enum variants is that they can have different kinds of storage mechanisms for their data. Some can store the data sequentially, like in a tuple variant, and some can store the data with field names like a struct variant, and we can mix and match. And when we use something like a 'match' statement, which we'll do in just a moment, we're going to be forced to react to either one of those variant possibilities, and the compiler will validate that we are reacting to the right kind of variant, tuple or struct. So for a tuple variant we are going to provide parentheses. And we're going to have two pieces of data: a 64-bit float to represent the price per month, and an unsigned 32-bit integer representing the number of months. So right here, I'll write my first data type, a float 64 and then a comma and a space, and then a u32. So a two element tuple. We are welcome to have as many elements as we want in this tuple. And remember as well that the advantage of a tuple is that the data can be of different data types if you want. Next up, the 'Premium' variant should be a struct variant. So here instead of parentheses, I can provide curly braces and it needs just one field. A 'tier' field which is going to be a 'Tier' enum value. In other words, a 'Tier' enum variant. So my field name here is up to me. I'm going to choose 'tier' because that matches. And then the actual type of that field living inside the struct, which will be attached as associated data to the 'Premium' variant of the 'Subscription' enum. Right. Kind of a mouthful, but the 'tier' field here will be of type 'Tier', which is the enum we have declared right here. Next up, let's define a 'summarize' method on the 'Subscription' enum. In order to define methods, we need to use the 'impl' keyword. This is the exact same syntax that we use for structs, so 'impl' means implement. We are adding on some kind of functionality to a type, and the type that we want to define the methods on is what follows the 'impl' keyword. So I will add 'Subscription', then provide a pair of curly braces. And in here we can define our method called 'summarize'. So 'fn summarize'. And just like with struct methods, we can choose how we want to receive our instance. We can either get a reference to it or we can take ownership. I'm going to choose to take an immutable reference since there's nothing I need to modify. Rather, what I'm going to be doing is just looking at the specific variant that I have: either 'Free', 'Basic' or 'Premium', and reacting differently to each one of those three possibilities. So I will take a immutable reference, which I can do with ampersand self ( &self ) and provide my curly braces. So the rest of the instructions are all related. Depending on which variant we have, we want to output different text. So let's start with 'Free' here. So knowing that I have to react to all three variants, I thought that an elegant solution here was the 'match' keyword, because 'match' is going to enforce using the compiler that we account for every single 'Subscription' variant in its own match arm. So here I'll use the 'match' keyword. And how do I get the actual 'Subscription' instance. Well, it is represented by my 'self' parameter. Granted, that is a immutable reference, but it's going to work just the same, just as if it were the actual instance being passed in. So here I will provide 'self' which will represent whatever happens to be the 'Subscription' instance that the 'summarize' method is invoked upon. So then, I'll provide my curly braces and the compiler will not compile until I account for all three of these variants. Let's begin with my first one, which is 'Free', so I will provide the enum name of 'Subscription', then the two colons, and then access the 'Free' variant. In this scenario, there is no associated data, so we do not need any additional syntax. But we do need to provide this match pattern with our rocket syntax and then provide a block that designates what to do if the 'self' happens to be the 'Free' 'Subscription' variant. If the instance that 'summarize' is called on is a 'Free' variant, then we're going to run this block of code. And here I can println! this exact content. In fact, I'm just going to copy it. "You have limited access to our site" and let me bring this up. So it's a little bit nicer. There we go. And add a semicolon here even though it should be optional. All right. So this syntax is correct. But the compiler will not be happy until we cover the other two possible variants. So if I hover my mouse over 'self', it'll tell us you're missing the following match arms: 'Basic' and 'Premium' which makes sense. So let's take care of the next one which is going to be our 'Subscription' enum's 'Basic' variant. And this time around we have a tuple variant. So using parentheses, we can extract these two pieces of data in the exact same order as they are defined here. So as mentioned earlier, this represents the price and this represents the number of months. So I'm going to call my parameters 'price' and 'month'. Although these parameters or variables are up to you, you can call them whatever you want. But they're going to represent this data right here. In fact, because we are receiving a immutable reference, it looks like they're actually going to be borrowed references as well. It shouldn't make a difference when we're dealing with types that implement the Copy trait anyway, but it's going to work as expected. We basically have a reference to these two pieces of data, and when we provide our rocket and our curly braces, we can use these two names in the body of this block. So here we want to output this text. So println! It's going to be "You have limited access to the site's premium features for {price} for {months} months". So we're going to dynamically inject the value of the two pieces of data that we pulled from the tuple variant into this string and then print it out. And that should take care of 'Basic'. And then we have one more variant to cover. And that is going to be the 'Premium' variant. And that is a tuple struct. So the syntax to extract its 'tier' field is going to be the exact same as the syntax here. Whatever we use in the declaration is what we use in the extraction as well. So since this is a struct variant, I'll provide my curly braces. And this time around, the names are not up to me because the fields are literally defined in the definition on line number 12. It's called 'tier'. So I will extract that piece of data from the struct variant belonging to the 'Premium' variant. And once again, Rust is going to give us a reference to that 'Tier' enum. So once again, I'll provide my rocket and my curly braces and output the expected text, which is "You have full access to the site's premium features", and then "Your tier is" and we're asked to embed the value of that 'tier' piece of state in Debug format. So I will add the Debug formatting, the colon and then the question mark ( :? ). And that should do it. All right. So we are all set. And now the compiler is happy with this 'summarize' method. So finally, in the main function let's create 3 instances of the 'Subscription' enum and invoke the 'summarize' method on all of them. So for the first one which is 'Free', I'm just going to do it in line. I'm not even going to bother with a variable declaration. I can just take my enum, then my double colons, then reach into a sample variant like 'Free' and that is an instance. Since I don't need anything else, since I don't have any associated data, this creates an actual instance, and I can just put a dot and call the 'summarize' method right here. And if I run that code, we should see this output right here. "You have limited access to the site". Looks like I messed up something right here. This is "month" singular. And I wrote 'months' plural here, so I will fix that. Save that. Let's try one more time. Everybody makes mistakes. And there you have it. All right, we need two more instances. So this time around, I'll declare a variable called 'basic' and take my 'Subscription' enum, double colons and provide my 'Baisc' variant. And we need two pieces of data: the float and the unsigned integer. So I'll do a price of 4.99 for three months. Now I'll take my 'basic' variable and call the 'summarize' method. That's the beautiful part about an implementation block is we now have a consistent interface to this enum. Every 'Subscription' enum that we instantiate will support the same collection of methods. In this case, 'summarize'. But that only goes to show the consistency that we have established between this custom type that we've declared in our program. So now I can invoke that and let me just create the last one as well. I'll create a 'Premium' variable and I will take my 'Subscription' enum and use two colons to access the 'Premium' variant. That one has a struct variant, a struct of associated data, and in order to create it, I need to provide that struct and the value for the 'tier' field, which must be a 'Tier' enum variant. So I will access 'Tier', provide two colons, and let's choose 'Platinum' here. Provide a semicolon here. So now I have my 'Subscription'. I can take that 'Subscription' and call 'summarize' on that. And we should get these respective outputs when I run the program. And there we have it. You can see where we are using the respective data from the tuple variant as well as the data from the struct variant. Here is "Platinum". This is the Debug trait implementation from the 'Tier' enum that we added right here. That allows us to see this string version of "Platinum". All right. That's all there is to cover in this coding challenge. I hope you had fun and I will see you in the next one.
