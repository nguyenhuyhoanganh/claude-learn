# The match Keyword II

As we mentioned in the previous lesson, we are welcome to provide a match arm with a block of code to execute in case that pattern matches. So instead of a direct value, we can instead provide curly braces to create a block. And here we can write as many lines of code as we want. We can also mix and match these syntax options. For example, we can have a direct number provided here as well as a block here. However, Rust needs to guarantee that every match arm produces a consistent type. So if these two other arms are producing an unsigned integer, we need to make sure that we produce a final integer here in this block for the Windows variant. Now we cannot use the 'return' keyword here because this is not technically a function body. But what we can do is utilize the implicit return syntax that we've seen in functions, which is where the last value or expression that we write within this block will be the final produced value. It is what the block will evaluate to in other words. It's important that when we provide that value and in our case it was 39, it's important that we do not add a semicolon, because if we do, then Rust will treat this as a separate line of execution. A full statement of code, a full command, kind of like a termination to the end of a sentence, to the end of a complete thought. We don't want that. So if we leave out the semicolon, then the last line within this block will be the final value that the block will produce. So let's add a println! right here in this block. And I can say something like "Quite an old operating system". All right. Just to show you the error. Let's delete this number very quickly. And notice the warning is "expected an unsigned integer". Rust is expecting each arm or each pattern to produce a consistent value. And now we're not getting anything. So we're technically getting a unit, an empty tuple. But Rust needs a u32. So I'll return back to 39, and that should do it. So now in the main function, I can declare a variable like 'dads_computer', and I'll set it equal to the 'OperatingSystem's' Windows variant. And then I'll use variable shadowing to redeclare my 'age' variable and once again invoke 'years_since_release' and pass in 'dads_computer' this time around and then output the sentence. "My dad's computer is {age} years old". So take a look at the output we're going to see. We're going to see: "My computer's operating system is 34 years old". That's coming from line #10. Then we invoke 'years_since_release' When it's running this match logic, it matches on 'Windows', which forces the execution of this block. So at that point, we output "Quite an old operating system!". Then, we produce 39 out of this block, and that produces 39 out of this match statement, and that produces 39 as the return value of the 'years_since_release' function. We assign that value to 'age' and then print it out right here. So we see 39 years old. So we can add a block to any match arm that we want. All right. That's all there is to cover in this lesson. So I will see you in the next one.
