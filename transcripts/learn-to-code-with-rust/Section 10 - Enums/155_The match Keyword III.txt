# The match Keyword III

If we have an enum variant that stores associated data, we can access that data in a corresponding match arm, but the syntax will vary depending on the kind of enum variant that I have. There will be a different syntax for a tuple variant and for a struct variant, and we'll explore that in this lesson. So let's begin by declaring an enum. Up above I'll declare an enum called 'LaundryCycle', and it's going to have three variants representing how I can do my laundry. The first variant will be called 'Cold' and it will have no associated data. The second variant will be 'Hot'. And here I'm going to use the struct variant and it will hold one field. So I'll provide my curly braces and my field will be 'temperature' and I will make this a unsigned integer a u32. This will be the temperature of the laundry cycle, of the hot laundry cycle. And then one more variant after this. My final variant will be called 'Delicate'. And here I'll use a tuple variant to store one sequential value of type String. So we can imagine that this String will represent the type of clothing that we are washing. So one enum, three different variants inside it, each of them with a different syntax. Next, let's define a function called 'wash_laundry'. It's going to accept a parameter called 'cycle', whose type will be a 'LaundryCycle' inside the body of the function. I'll once again use my 'match' keyword in combination with my 'cycle' parameter. So rust is going to mandate that I cover all 3 of my variants. Let's begin with 'LaundryCycle' and its 'Cold' variant. This variant does not have any associated data. So what I can do here is just define an associated block. There is no data and for now I don't want my 'wash_laundry' function to return anything. I can just output a string to the screen with some helpful information. So here I can println! and say something like "Running the laundry with cold temperature". Then I'll provide a comma after my block. And I still have two more variants to cover, two more arms. So next up we're going to proceed to the next variant, which is 'Hot' right? I kind of spoiled the surprise here, but if we match on the 'Hot' variant, well then we do have associated data that lives inside that struct variant. And we use the exact same syntax: a pair of curly braces after the variant name here and here we can define the fields that we want access to. We have only one field called 'temperature'. And these fields will be available to use in the corresponding block. So once again I'll provide my arrow syntax. Then my curly braces to create a block of code. And in this block I can use the value of the 'temperature' field. And if I have 2 or 3 fields here, I can once again declare them in this pair of curly braces, separated with a comma and a space. So now I can println! and write something like "Running the laundry with a temperature of" and then interpolate the value of 'temperature'. So that is how we access the associated data belonging to a struct variant on the enum. We have one more variant to cover, and that is going to be 'Delicate' and with 'Delicate', our data is stored as a tuple variant, and the syntax, as you might guess, is once again going to mirror what we have here. So instead of curly braces I'll provide a pair of parentheses. Just like in the variant declaration, we have one piece of data in order that will be of type String. And now I can give a name to represent that string. And then whatever name I choose to declare here within the parentheses I will have access to within my block. So this is almost like a variable name for whatever this string is. I'm going to call this one 'fabric_type' But once again the name is entirely up to us, the developer. And if I had multiple values here, like say an i32, then I could provide a sequential name in line within these parentheses as well, and give that value a name. Right. Here we are extracting our fields by name. Here we are extracting our values by order or by position, which mirrors the setup we have here, which is a struct variant versus a tuple variant. Otherwise, the syntax is the same. So I'll provide my arrow syntax and my block. And I can output something like "Running the laundry with a delicate cycle for". And then I'll interpolate the value of 'fabric_type' So now back in the main function, I am ready to invoke my 'wash_laundry' function with a sample variant. In fact, let's invoke it three times with all three variants so we can see all of these syntax options side by side. So let's begin with 'Cold'. I will access my 'LaundryCycle' enum and then provide two colons and access the 'Cold' variant. The 'Cold' variant has no associated data, so we can just run it like so. That code will match this arm in my match, and thus we're going to run the associated block which will print this line "Running the laundry with cold temperature". Then, I can invoke 'wash_laundry' once again. And let's choose our next variant, which will be the 'Hot' variant. The 'Hot' variant has a struct variant, so we need to provide a starter value for the 'temperature' field. Remember, the way we do that is we provide a pair of curly braces here. This is just part of the instantiation protocol of this variant. This variant needs essentially a struct of data, a struct containing one field. This is me defining that concrete field. Then when we have the entire enum instance, that's what we're passing in to 'wash_laundry'. So here I'll provide my 'temperature' field set to an arbitrary value like 100. And let's see what happens here. Now when we invoke 'wash_laundry' on line #9, we once again run this function. We're not going to match on 'Cold'. We are going to match on 'Hot'. And thanks to the syntax, we're going to have access to the temperature field's value, which is 100. And that's exactly what we're going to interpolate in our resulting sentence. Finally, one more time I'll invoke 'wash_laundry'. This time around, my enum variant will be our final one. 'Delicate'. This one has a tuple variant, so I need to provide one value for my expected String. So here I'll do 'String::from' And let's say we're washing silk. And there we have it. So now on this third invocation of 'wash_laundry', the match arm will not be met here. Rust will also not match the 'Hot' arm. This one. The 'Delicate' arm will match. So then Rust says "I'm going to take the piece of associated data you have called string, and I'm going to assign it to the 'fabric_type' parameter". Now I have the ability to use 'fabric_type' directly within my block. Once again, this name can be up to us. This name cannot be because this name is coming directly from the field name within the struct here. Since this is based on order, and all that Rust knows is that we have one string which is first in line, this list of names will match the order of the types here, so these names are up to us for tuple variants. These names are not up to us for struct variants, but you can see how powerful the match statement is because not only will rust validate every possibility, but it's also going to allow us to access the associated data for each enum variant.
