# The match Keyword I

Earlier in the course, in the Control Flow section, we learned about the 'match' keyword. The match keyword compares a given value against a collection of patterns, also called arms, and then it executes a block of code based on the first pattern that matches. The Rust compiler validates that the match handles all possible cases that a value could be, so we can see a quick code sample here. As a reminder, we declare a 'number' variable set to 20. Then we use the 'match' keyword, which is typically followed by a dynamic value. In this case, the 'number' variable. Each one of these is called an arm or a pattern. Rust will evaluate each of these arms one by one until there is a match. So it's going to check whether a number is equal to 5, then 8 and so on. This underscore character ( _ ) is a catch all pattern that will always match. So we must place this pattern at the very end of all of our available options, because it's the last one we want to evaluate. So in this example, Rust will evaluate the arms in order. So it's going to compare the value of the 'number' variable to 5. That is not a match, so this will not be executed. Then Rust will proceed downwards and compare the 'number' variable with 8. Once again, it is not a match, so the corresponding block of code does not execute. And finally it's going to come to the catch-all underscore character ( _ ). That is good enough. That is a match. So this will be the final evaluated block or the successful pattern, the matching pattern. and thus this program will show us its something else which comes from this println! right here. The Rust compiler will validate that the match expression handles every possible case that the dynamic value could be. This makes the match keyword a perfect keyword to use in combination with an enum, because match will validate that every possible variant is accounted for. So we can write logic, branching logic that will execute based on the specific variant that we have, and 'match' will validate that we cover all possible cases. So let's delete this code and let's start by defining a new enum. I'm going to call it 'OperatingSystem' And it's going to represent some popular computer operating systems. So we can add Windows here, and we can do MacOS and we can do Linux. Next let's define a function. I'm going to call it 'years_since_release'. 'And this function will accept a single parameter called 'os'. And the type of OS will be an 'OperatingSystem' enum variant. This function will also return an unsigned integer, a u32. As a reminder, that is an integer that can only be a positive number, so the 'os' parameter will take ownership over the enum or the enum variant that is passed in, but that will be fine for this example. So in the body of this function, I'm going to use the 'match' keyword and follow it up with the dynamic unpredictable value, which is the 'os' parameter. I can invoke this function multiple times with different 'OperatingSystem' variants, and we don't know which one we're going to get in advance. And that is why it's great to combine it with the 'match' keyword. So each option here within my curly braces is called an arm. And right now, Rust is unhappy because we have not covered all of the possible arms. Notice it says you're missing the following match arms. You need to tell me what to do for all of these possible variants, all of these possible scenarios: Windows, MacOS and Linux. So we can write out the patterns one by one. What do we want to match 'os' with? And then if there is a match, what do we do as a result? So let's begin with our first variant. I'm going to write it in line: 'OperatingSystem::Windows' And then I provide this syntax, this arrow syntax, which is an equal sign ( => ). Then a greater than sign. Then on the right hand side, we are welcome to provide a block of execution code that is totally valid. Or we can just write a direct value, and that value will represent what we are matching this enum variant to. Basically, we are building a bridge or a map or an association between the Windows variant in this case and some value. So in this case, this function represents the number of years since the operating system was released. And when I did my research, Windows was 39 years old. All right. So I'll provide a comma here. And Rust is still not happy because we're missing the other two. So let's cover those. So on the next line I'll provide my next matching pattern. Let's compare 'os' with the next variant in operating system which is MacOS. And the number of years since that was released was 23. One more variant left to cover, and that's going to be operating system Linux. Once again, equal sign. greater than sign is the arrow syntax ( => ). You can think of it as pointing to the result, whether that result be a block of code or a single value. And for Linux, it's going to be 34. Now notice when I save, the error goes away. 'match' and Rust are happy because we have covered all possible enum variants. In this scenario, whatever arm Rust matches is what will be evaluated on the right hand side, and furthermore, that will be the final returned expression of the 'match' keyword. This will produce a final value, a final unsigned integer based on one of these values. You may recall earlier in the course, we could actually even assign this to a variable if we want to, right? Because the match expression is going to produce one of these values. Right. But we can also just write it like this without the semicolon, because then the final evaluated number, one of these three, will be the final returned expression or return value from the match expression, and that will also serve as the implicit return value of the 'years_since_release' function. You can see Rust is happy because it understands that these values will be unsigned integers, and thus it understands it's going to get the expected return value. So now back in the main function, let's define a variable called 'my_computer'. And I'm going to set it equal to my MacOS variant. So my enum is 'OperatingSytem::' And the variant will be MacOS. This is a valid instance. So now I can invoke my 'years_since_release' function which expects an 'os' parameter that must be an 'OperatingSystem' variant. So I can pass in 'my_computer'. Once again, this will transfer ownership from the 'my_computer' variable to the 'os' parameter in the 'years_since_release' function. But that's totally fine because I don't intend to use my 'my_ computer' variant after this function is invoked. This function will return back that unsigned integer which I can assign to a variable. Let's call that variable 'age'. So now I can output something like println! "My computer's operating system is {age} years old", and we should see "My computer's operating system is 23 years old". And there we have it. And once again, that's because we passed in a given variant. Then in the body of this function, this is the place where the magic happens. The 'match' keyword matched whatever dynamic value 'os' represents with these arms. It found the first one that matched, which happened to be the MacOs one. It evaluated the right hand side of this, which produced a value of 23. That became the final return value produced by the match expression, which in turn became the implicit return value of the 'years_since_release' function. And then I simply used that value here. So of course, if I swap this with something like 'Linux', that would match the third arm right here and say "34 years old" and so on. But the beautiful part here is that Rust will keep us responsible if we miss a given match arm like here, the program will not compile. It's going to tell us this pattern where this variant of Linux is not covered. Please cover it. Right. So Rust again is watching out for all possible scenarios, and that's what makes it such a powerful and safe language. All right. That's all there is to cover in this lesson. So I will see you in the next one.
