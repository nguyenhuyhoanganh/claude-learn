# Common String Methods (trim, casing, replace, split)

In this lesson, we'll explore some common methods on strings. So I'll begin by declaring a new variable called 'music_genres', and I'll assign this to a string slice consisting of a comma-separated list of music genres. So within the string I'm going to write "Rock". Then I'll add a comma and a space. And then I'll add "Metal". Comma and a space. I'll add "Country" and then comma and a space and I'll add "Rap". And then what I'm going to do for fun is I'm going to add some extra white space, some extra spacing to the beginning and end of the string. So we can pretend that this string arrived in our program from another source. Maybe, for example, we read it from a file and it just so happened to arrive in this not ideal format with all of this extra white space. Luckily, Rust has a family of methods that help us clean up the white space at either the beginning or end, or both sides of a string. So on this string slice, I'm going to invoke a method and interpolate the return value right here. I'm going to take 'music_genres', and I'll begin with the 'trim' method. You can see the description reads "Returns a string slice with leading and trailing whitespace removed". Leading means at the beginning, and trailing means at the end, at the back. So what this will do is remove all of those extra characters and give us a string slice that does not have all that extra whitespace. And in fact, Rust doesn't need to create a heap allocated String. Rather, it can create a slice from a slice because there is no content from the original 'music_genres' slice that needs to be mutated or changed in any way. Rather, we just need to reference the original data and pull out this content right in the middle, ignoring the white space. And that's exactly what the 'trim' method does, so no additional data is created. It's actually very elegant. Now there is a complementary method called 'trim_start'. 'trim_start'. This will do the same thing, but it's only going to remove the white space at the beginning. So we may not be able to see the difference here. But we have removed the white space at the beginning but kept the white space at the end with the 'trim_start' method. And then the inverse of that method is 'trim_end'. That does the opposite. It removes or trims the white space from the end of the string, but keeps the white space at the beginning. So in this output, we see all of our additional spaces from our original declaration on line #2. Usually, you're going to want 'trim', but it all depends on what you're writing. So let's go ahead and trim the 'music_genres' string and reassign it back to the original variable. We can of course use variable shadowing, but instead I'm just going to make this mutable. And then down below I can take 'music_genres'. And on the right hand side, take my original string, call 'trim' to get back a string slice, and then reassign it back to 'music_genres'. Let me show you a couple more methods, and these all revolve around casing. Casing deals with the fact of whether the characters are capital or lowercase, and Rust has various methods for conversion. So for example, here is what 'music_genres' looks like currently. We have a wide variety of casing right here. There is a method available called 'to_uppercase'. That method creates a new heap allocated String with all of the characters in uppercase. We now need a heap allocated String because we need to modify or mutate some part of the original data. Now we're not changing the original string slice because that is written into the executable, and then we have a reference to it. We just use it as the basis to create a new heap String with all of the characters in uppercase. And as you might have guessed, there is a complementary method called 'to_lowercase', and that is going to give you a heap String with all of the characters in lowercase. This can be really helpful when standardizing or normalizing data, especially if it has all kinds of weird cases. Let me show you an example of a method that accepts arguments. There is a nice method available on a string slice called 'replace'. What 'replace' is going to do is it's going to replace all matches of a given character or a pattern, which means a character sequence with another character. So this is just like find and replace on your computer. For example, say I want to replace every lowercase 'a' here with the '@' sign, just as a silly example. So the first argument to 'replace' is a string slice representing what I'm targeting. I want to target or find all occurrences of the lowercase letter 'a'. The second argument is what I want to replace it with. What do I want to replace every lowercase 'a' with? So just arbitrarily I'm going to choose the '@' sign. And we're once again going to get back a heap allocated String because there are adjustments being made. We can't just rely on the original slice. There are some differences and Rust will represent them in a new heap allocated String. And here you can see that all of the 'a''s have been replaced with the '@' sign. One more super helpful method that I want to show you in this lesson is 'split'. 'split' is helpful when we want to split or separate a string based on every occurrence of a character, which is often called a delimiter. So here is a perfect use case. Say I want to take my 'music_genres' string, and I want to extract all of the genres into a vector of string slices. I want a vector of four string slices: Rock, Metal, Country and Rap. So in order to accomplish that, I have to split my original string, now nicely trimmed on line #7 so that I split or cut in other words based on every occurrence of a comma and a space. I used that as what's called the delimiter, which is the identifying character set, at which point I'm going to ask Rust to cut or slice or split the original string. So here's how that works. I'm going to take 'music_genres'. I'm going to call these 'split' method on it, and I'm going to provide what I want to split on, what character do I identify and cut on. And I'm going to provide not just the comma, which would be totally valid, but a comma and a space. So now every occurrence of specifically a comma and a space will be considered a slice point. And I'm going to cut right here, right here and right here. Now if I assign this to a variable like 'genres', we're going to see this returns a type called 'Split' and that is a totally separate type. If we want to collect all of the elements, the split pieces, if you will, into a vector, what we can do is call a method on this 'Split' type, which I believe might be a struct. Yes, it is. So that struct supports a method called 'collect'. And in order for this to work, I have to specify the type that I want to collect these split elements into. And I'm just going to annotate that here as being a vector consisting of or holding elements that are string slices. So 'ref strs' ( &str ) and that should do it. So we're going to take our original string slice. We're going to split it based on every occurrence of a comma and a space, then collect the results into a vector. Vectors do not implement the Display trait, but they do implement the Debug trait. So I can print my genres right here with the question mark syntax ( :? ). And now we can see we have effectively split that original string into a vector of elements. This can be super helpful, for example, when you are splitting an address that is separated by commas, or splitting first name and last name, which can be separated by a space. There's a lot of common use cases for this when working with data. All right. So that is a quick introduction to some not all of the methods that are available on a string, but some very common ones to hopefully get you excited to see how all of these different methods can help us manipulate a string into the desired shape or form. Some of them remove whitespace, some of them change the casing, some of them replace characters, some of them break up a string into pieces, and there's lots more for you to explore in the Rust documentation. All right, that's all there is to cover in this lesson, so I will see you in the next one.
