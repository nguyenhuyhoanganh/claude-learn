# Review of Strings

Welcome to the next section of the course, which is focused on strings. We've already covered strings in great depth, so we'll offer a quick review of the fundamentals in this lesson. And then we'll learn some more common string operations throughout the remainder of the section, so you can feel free to skip this lesson. In this lesson, we're just going to cover previously introduced concepts. A string is a data type representing a piece of text, or alternatively a sequence of characters. When Rust developers use the terminology string, they may be referring to either one of Rust's two string types, and they are 'str' and 'String'. Now we usually see the str, the 's-t-r' in its borrowed form, which is &str, beginning with that borrow operator, the ampersand. And this is also called a string slice. A string slice is usually a reference to an area of the executable where Rust has hardcoded a specific piece of text that we wrote in our source code. Now, technically speaking, a string slice can also come from a heap String reference that has been coerced into a string slice, but the same principle applies. This is not an owned data type, it's a reference. It's a borrow of some text, or a portion or fragment of some text that lives somewhere else. And there is no way to modify this type of string. It is fundamentally a reference. These string slices often come from string literals, which is just a hard coded piece of text that we declare with double quotes. For example, right here, I can declare a variable called 'pirate' and assign it to a string literal in double quotes of "Bloodhook". So this text will be embedded into the binary executable. And when the program runs, we're going to get a reference or a borrow of that complete fragment of text living somewhere else. The point is that we cannot modify it. We can just refer to it, to that sequence of characters existing somewhere else. In comparison, this capital S 'String' which lives on the heap can change. It's growable, it's mutable, it can add characters, remove characters, have the capacity to change, and it also follows the principles of ownership. Okay, so for that, as a reminder, we can create an empty string by referencing the String type and then providing the 'new' associated function. This is also called a constructor function. I'll assign this to 'empty' right here. And 'new' is a common name for any associated function that constructs an empty instance. We also have the complementary 'from' function, which creates a heap String from a string literal. So for example, I can provide even 'pirate' from above and that will create a heap allocated String. Let's reassign this variable to be 'sailer'. So now this type of String with a capital S lives on the heap, but its original source of data is something that's going to be stored in the executable that we produce. Another way that you can create a heap allocated String from a string slice is with a method available on a string slice called 'to_string'. It's a little bit confusing because we technically have a string, but 'to_string' means a heap String. So as an alternative to this syntax, what we can do here is declare a 'bad_guy' variable, provide my string slice of 'pirate', and that supports a 'to_string' method that converts from that ref str ( &str ) to a heap allocated String. These two examples are basically identical. It's just a slightly different syntax. So here I can println! and output 'pirate' as well as if I can type today, as well as 'sailer' and 'bad_guy', and all of these types support the Display trait, so we're going to be able to get our output right here. Now as far as accessing individual elements or characters within the strings, regardless of which type you're working with, Rust does not support accessing an individual index position in a string using square brackets. That syntax will work with other collection types like arrays, but it will not work with strings. For example, let's say I declare a 'first_initial' variable and I try to refer to 'pirate' and access the very first initial at index 0. This is not going to work, as you can see. And also, even if I tried to provide the borrow operator ( & ), this will also not work. And it's not going to work for either one of our two types. So 'pirate' is a string slice, but if I replace it with 'sailor', which is a heap String, once again, neither one of those two index options will work. So why does Rust prohibit this operation? Remember, when we are using square brackets to index into a string, the number here is based on the index positions of the bytes in memory, not the index positions of the characters as they appear to us. Now, technically speaking, one English character is equal to one byte in memory, but that is not always the case. For example, an emoji or a character in a language besides English may visually appear as one character to us, but it may occupy 2 bytes in memory or 4 bytes in memory, and thus Rust is concerned that if we provide only one number, one index position, we have the potential to access a byte that is part of a larger byte sequence representing one character, and thus there is the potential for error here, and thus it will prohibit this behavior. Once again, this syntax would technically work with an example like this. But as soon as we have non-English characters in here, it's not guaranteed to work. Therefore, the only way that we can access individual characters in a string through a byte sequence is by providing a range syntax. In other words, an interval or sequence of bytes starting from a lower bound and progressing to an upper bound. Even if we're targeting just one character, we have to provide a range or interval because Rust feels safer that that is going to encompass a sequence, and thus there's just less capacity for error. So here, for example, if I wanted to provide a range starting from byte 0 and pulling up to but not including byte 1. This is now going to work. So as a reminder with the syntax, we go up to the final number but not including it. So this is just the character at byte position 0, which is a total of 1 byte in between these two. And that's going to be the capital 'B', so I can output that and that'll work like so. And we can see we're going to pull out 'B'. And the same rules will apply if I go back to my string slice. Here I'm creating a string slice from another string slice. But that's totally fine. This is going to work. So if you want to pull out a character or reference it, you need to borrow it and then you need to provide a range in here. Once again, if you have something like an emoji, Rust is concerned that if that emoji has 4 bytes in memory and you only ask for byte 0 or byte 1, you're going to run into a problem. Now, technically speaking, if your emoji occupies 4 bytes and you ask for a sequence of just the first byte of those four, that would still trigger a runtime panic, an error when your program runs. However, there's less chance of that error because at least with this syntax, you can get it right, right? While if Rust allowed just this syntax, there would be no way to get it right with an emoji. Right? So this one at least offers the capacity for the correct syntax and avoiding a runtime issue if you access a byte that is part of a larger sequence. Once again, if you are borrowing, say, an element from an array or a vector, a individual element will work because that syntax in square brackets targets by index positions of the elements. With a string, we are targeting by the index positions of the bytes in memory, and they are not always 1-to-1 with the characters in a string. With English alphabetic characters, they are. But that is not always the case. And Rust has to support the entire Unicode computing standard, which I believe is something like 150,000 characters, so it prefers to play it safe. That is the general disposition of the language. It always is stricter, but for the benefit of safety in the grand scheme. All right. So that has been a quick review of strings in Rust. Lots more operations to learn in this section. So I will see you in the next lesson.
